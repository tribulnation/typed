name: Release core
on:
  pull_request:
    types: [closed]
    branches: [main]
    paths: ['packages/core/**']
permissions:
  id-token: write
  contents: write
jobs:
  check:
    if: github.event.pull_request.merged == true && github.head_ref == 'release/core'
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      published: ${{ steps.check.outputs.published }}
    steps:
      - uses: actions/checkout@v5
        with:
          ref: main
      - id: version
        run: |
          version=$(grep -m1 '^version = ' packages/core/pyproject.toml | sed -E 's/version = "(.*)"/\1/')
          echo "version=$version" >> "$GITHUB_OUTPUT"
      - id: check
        run: |
          code=$(curl -s -o /dev/null -w "%{http_code}" "https://pypi.org/pypi/typed-core/${{ steps.version.outputs.version }}/json")
          if [ "$code" = "200" ]; then
            echo "published=true" >> "$GITHUB_OUTPUT"
          else
            echo "published=false" >> "$GITHUB_OUTPUT"
          fi
  release:
    needs: check
    if: needs.check.outputs.published == 'false'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: main
          fetch-depth: 0
          fetch-tags: true
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -e packages/core
      - run: pip install ruff pyright pytest build
      - run: ruff check --select TID251 packages/core
      - run: pyright packages/core
      - run: python -m build packages/core
      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: packages/core/dist
      - id: previous_tag
        run: |
          previous=$(git tag -l 'core-v*' | sort -V | tail -1)
          echo "previous=$previous" >> "$GITHUB_OUTPUT"
      - run: git tag core-v${{ needs.check.outputs.version }}
      - run: git push origin core-v${{ needs.check.outputs.version }}
      - if: steps.previous_tag.outputs.previous != ''
        uses: softprops/action-gh-release@v2
        with:
          tag_name: core-v${{ needs.check.outputs.version }}
          previous_tag_name: ${{ steps.previous_tag.outputs.previous }}
          generate_release_notes: true
          body: |
            Published to [PyPI](https://pypi.org/project/typed-core/${{ needs.check.outputs.version }}/) as `typed-core==${{ needs.check.outputs.version }}`.
      - if: steps.previous_tag.outputs.previous == ''
        uses: softprops/action-gh-release@v2
        with:
          tag_name: core-v${{ needs.check.outputs.version }}
          generate_release_notes: true
          body: |
            Published to [PyPI](https://pypi.org/project/typed-core/${{ needs.check.outputs.version }}/) as `typed-core==${{ needs.check.outputs.version }}`.
