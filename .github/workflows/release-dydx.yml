name: Release dydx
on:
  pull_request:
    types: [closed]
    branches: [main]
    paths: ['packages/dydx/**']
permissions:
  id-token: write
  contents: write
jobs:
  check:
    if: github.event.pull_request.merged == true && github.head_ref == 'release/dydx'
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      published: ${{ steps.check.outputs.published }}
    steps:
      - uses: actions/checkout@v5
        with:
          ref: main
      - id: version
        run: |
          version=$(grep -m1 '^version = ' packages/dydx/pkg/pyproject.toml | sed -E 's/version = "(.*)"/\1/')
          echo "version=$version" >> "$GITHUB_OUTPUT"
      - id: check
        run: |
          code=$(curl -s -o /dev/null -w "%{http_code}" "https://pypi.org/pypi/typed-dydx/${{ steps.version.outputs.version }}/json")
          if [ "$code" = "200" ]; then
            echo "published=true" >> "$GITHUB_OUTPUT"
          else
            echo "published=false" >> "$GITHUB_OUTPUT"
          fi
  release:
    needs: check
    if: needs.check.outputs.published == 'false'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: main
          fetch-depth: 0
          fetch-tags: true
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -e packages/core
      - run: pip install -e packages/dydx/pkg
      - run: pip install ruff pyright build
      - run: ruff check --select TID251 packages/dydx/pkg
      - run: pyright packages/dydx/pkg
      - run: cp packages/dydx/README.md packages/dydx/LICENSE packages/dydx/pkg/
      - run: python -m build packages/dydx/pkg
      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: packages/dydx/pkg/dist
      - id: previous_tag
        run: |
          previous=$(git tag -l 'dydx-v*' | sort -V | tail -1)
          echo "previous=$previous" >> "$GITHUB_OUTPUT"
      - run: git tag dydx-v${{ needs.check.outputs.version }}
      - run: git push origin dydx-v${{ needs.check.outputs.version }}
      - if: steps.previous_tag.outputs.previous != ''
        uses: softprops/action-gh-release@v2
        with:
          tag_name: dydx-v${{ needs.check.outputs.version }}
          previous_tag_name: ${{ steps.previous_tag.outputs.previous }}
          generate_release_notes: true
          body: |
            Published to [PyPI](https://pypi.org/project/typed-dydx/${{ needs.check.outputs.version }}/) as `typed-dydx==${{ needs.check.outputs.version }}`.
      - if: steps.previous_tag.outputs.previous == ''
        uses: softprops/action-gh-release@v2
        with:
          tag_name: dydx-v${{ needs.check.outputs.version }}
          generate_release_notes: true
          body: |
            Published to [PyPI](https://pypi.org/project/typed-dydx/${{ needs.check.outputs.version }}/) as `typed-dydx==${{ needs.check.outputs.version }}`.
