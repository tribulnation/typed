<!--
Filing a bug in generated code (packages/<slug>/pkg/src/<slug>/, outside that package's
core/)? This is still the right place for it — file it here, not a PR against the
generated file, since the next generation run would just overwrite a hand-applied fix.
See CONTRIBUTING.md for what's generated vs. hand-written.
-->

**Package & version**

<!-- e.g. typed-binance==0.2.0 -->

**What happened**

<!-- The failing call, the response that didn't validate, the type that didn't match. -->

**Expected**

**Minimal reproduction**

```python

```
