# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal
from typed_core.validation import TypedDict
import pydantic

from typed_hyperliquid.info.core import InfoMixin


class DelegatorSummary(TypedDict):
  """Summary of a user's staking position."""

  delegated: str
  """Total HYPE currently delegated to validators, as a decimal string."""
  nPendingWithdrawals: int
  """Number of undelegation withdrawals currently in their unbonding period."""
  totalPendingWithdrawal: str
  """Total HYPE across all pending (unbonding) withdrawals, as a decimal string."""
  undelegated: str
  """HYPE held in the staking balance but not currently delegated to any validator, as a decimal string."""


class StakingSummaryAction(TypedDict):
  type: Literal['delegatorSummary']
  user: str


adapter = pydantic.TypeAdapter(DelegatorSummary)


class StakingSummary(InfoMixin):
  async def staking_summary(
    self,
    *,
    user: str,
    validate: bool | None = None,
  ) -> DelegatorSummary:
    """Query a summary of a user's staking position -- delegated and undelegated HYPE, plus any pending withdrawals -- through Hyperliquid POST /info using request type `delegatorSummary`.

    Args:
      user: Address to query the staking summary for, in 42-character hexadecimal format.
      validate: Validate the response against the generated schema.

    References:
      - [Official docs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint)
    """
    params: StakingSummaryAction = {
      'type': 'delegatorSummary',
      'user': user,
    }
    r = await self.request(params)
    should_validate = self.validate if validate is None else validate
    return adapter.validate_python(r) if should_validate else r
