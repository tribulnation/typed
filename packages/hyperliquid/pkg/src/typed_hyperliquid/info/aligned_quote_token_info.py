# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, TypedDict
from typed_hyperliquid.core import TimestampMillis
from typed_hyperliquid.info.core import InfoCore


class AlignedQuoteTokenInfo(TypedDict):
  """Aligned quote token status for a spot token."""

  isAligned: bool
  """Whether the token is currently designated an aligned quote token."""
  firstAlignedTime: TimestampMillis
  """Millisecond-epoch timestamp the token was first designated an aligned quote token."""
  evmMintedSupply: Decimal
  """Supply of the token minted on HyperEVM, as a decimal string."""
  dailyAmountOwed: list[tuple[str, Decimal]]
  """Schedule of amounts owed to the token under alignment; not documented by Hyperliquid and unobserved in captured examples (the only capture had an unaligned token, so this array was always empty). Row shape inferred from the client core's `list[tuple[str, str]]` type."""
  predictedRate: Decimal
  """Predicted emission rate owed to the token going forward, as a decimal string."""


class Request(TypedDict):
  token: int
  """Spot token index (see spotMeta's per-token `index`)."""


Response = AlignedQuoteTokenInfo | None


class AlignedQuoteTokenInfoEndpoint(InfoCore):
  """Look up a spot token's HIP-3 "aligned quote token" status by token index -- whether it's been designated a quote token aligned to HYPE emissions, and if so, since when and at what rate."""

  async def aligned_quote_token_info(
    self,
    token: int,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Look up a spot token's HIP-3 "aligned quote token" status by token index -- whether it's been designated a quote token aligned to HYPE emissions, and if so, since when and at what rate.

    Args:
      token: Spot token index (see spotMeta's per-token `index`).
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint)
    """
    return await self.request(
      Request(token=token),
      method='POST',
      path='alignedQuoteTokenInfo',
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Response,
    )
