# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, TypedDict
from typed_hyperliquid.info.core import InfoCore


class Request(TypedDict):
  outcome: int
  """Outcome id to look up."""


class SettledOutcomeSideSpec(TypedDict):
  """One tradeable side of the outcome."""

  name: str
  """Display name for this side of the outcome (e.g. `"Yes"`, `"No"`)."""


class SettledOutcomeSpec(TypedDict):
  """The outcome's static spec, as of settlement."""

  outcome: int
  """Outcome id."""
  name: str
  """Outcome display name."""
  description: str
  """Outcome description, encoding its settlement class and parameters."""
  sideSpecs: list[SettledOutcomeSideSpec]
  """The outcome's tradeable sides, always two entries."""
  quoteToken: str
  """Symbol of the spot token this outcome was quoted/settled in."""


class SettledOutcome(TypedDict):
  """The outcome's spec plus its settlement result."""

  spec: SettledOutcomeSpec
  settleFraction: Decimal
  """Fraction of the outcome's Yes side that settled true, as a decimal string (e.g. `"1.0"` fully Yes, `"0.0"` fully No)."""
  details: str
  """Free-text detail on how the settlement fraction was determined, format depending on the outcome's settlement class (e.g. `price:<observedPrice>` for a price-based outcome)."""


Response = SettledOutcome | None


class SettledOutcomeEndpoint(InfoCore):
  """Retrieve a HIP-4 prediction-market outcome's settlement result: the fraction it settled at, and free-text detail on how that fraction was determined. Returns `null` if the outcome has never settled or does not exist."""

  async def settled_outcome(
    self,
    outcome: int,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Retrieve a HIP-4 prediction-market outcome's settlement result: the fraction it settled at, and free-text detail on how that fraction was determined. Returns `null` if the outcome has never settled or does not exist.

    Args:
      outcome: Outcome id to look up.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint/spot)
    """
    return await self.request(
      Request(outcome=outcome),
      method='POST',
      path='settledOutcome',
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Response,
    )
