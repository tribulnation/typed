# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, TypedDict
from typed_hyperliquid.info.core import InfoCore


class PerpDexStatus(TypedDict):
  """A perp dex's aggregate collateral status."""

  totalNetDeposit: Decimal
  """Total USDC net-deposited into the perp dex, as a decimal string."""


class Request(TypedDict):
  dex: str
  """Perp dex name to look up. The empty string represents the first (default) perp dex."""


class PerpDexStatusEndpoint(InfoCore):
  """Retrieve a perp dex's total net deposit -- the aggregate USDC collateral currently backing the dex."""

  async def perp_dex_status(
    self,
    dex: str,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> PerpDexStatus:
    """Retrieve a perp dex's total net deposit -- the aggregate USDC collateral currently backing the dex.

    Args:
      dex: Perp dex name to look up. The empty string represents the first (default) perp dex.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint)
    """
    return await self.request(
      Request(dex=dex),
      method='POST',
      path='perpDexStatus',
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=PerpDexStatus,
    )
