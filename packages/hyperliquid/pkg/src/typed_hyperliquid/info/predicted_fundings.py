# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_hyperliquid.core import TimestampMillis
from typed_hyperliquid.info.core import InfoCore


class PredictedFunding(TypedDict):
  """Predicted funding for a coin on a reference venue."""

  fundingIntervalHours: NotRequired[int]
  """Hours between funding settlements on this venue."""
  fundingRate: Decimal
  """Predicted funding rate, as a decimal string."""
  nextFundingTime: TimestampMillis
  """Time of the next funding settlement, in milliseconds since epoch."""


Response = list[tuple[str, list[tuple[str, PredictedFunding | None]]]]


class PredictedFundings(InfoCore):
  """Retrieve predicted funding rates for every coin, compared across Hyperliquid and other perp venues used as funding-rate references."""

  async def predicted_fundings(
    self,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Retrieve predicted funding rates for every coin, compared across Hyperliquid and other perp venues used as funding-rate references.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint)
    """
    return await self.request(
      None,
      method='POST',
      path='predictedFundings',
      validate=validate,
      transport=transport,
      response_type=Response,
    )
