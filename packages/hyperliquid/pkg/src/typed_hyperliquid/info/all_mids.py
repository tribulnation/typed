# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_hyperliquid.info.core import InfoCore


class Request(TypedDict):
  dex: NotRequired[str]
  """Perp dex name to scope the request to. Defaults to the empty string, which represents the first (default) perp dex; spot mids are only included with the first perp dex."""


Response = dict[str, Decimal]


class AllMids(InfoCore):
  """Retrieve the current mid price for every perp and spot coin. Spot mids are only included alongside the first perp dex's results."""

  async def all_mids(
    self,
    dex: str | None = None,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Retrieve the current mid price for every perp and spot coin. Spot mids are only included alongside the first perp dex's results.

    Args:
      dex: Perp dex name to scope the request to. Defaults to the empty string, which represents the first (default) perp dex; spot mids are only included with the first perp dex.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint)
    """
    request: Request = Request()
    if dex is not None:
      request['dex'] = dex
    return await self.request(
      request,
      method='POST',
      path='allMids',
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Response,
    )
