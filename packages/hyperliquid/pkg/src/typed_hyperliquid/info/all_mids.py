# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict
import pydantic

from typed_hyperliquid.info.core import InfoMixin


class AllMidsAction(TypedDict):
  type: Literal['allMids']
  dex: NotRequired[str]


adapter = pydantic.TypeAdapter(dict[str, str])


class AllMids(InfoMixin):
  async def all_mids(
    self,
    *,
    dex: str | None = None,
    validate: bool | None = None,
  ) -> dict[str, str]:
    """Retrieve the current mid price for every perp and spot coin. Spot mids are only included alongside the first perp dex's results.

    Args:
      dex: Perp dex name to scope the request to. Defaults to the empty string, which represents the first (default) perp dex; spot mids are only included with the first perp dex.
      validate: Validate the response against the generated schema.

    References:
      - [Official docs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint)
    """
    params: AllMidsAction = {
      'type': 'allMids',
    }
    if dex is not None:
      params['dex'] = dex
    r = await self.request(params)
    should_validate = self.validate if validate is None else validate
    return adapter.validate_python(r) if should_validate else r
