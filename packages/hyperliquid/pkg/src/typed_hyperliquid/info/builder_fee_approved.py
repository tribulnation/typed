# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_hyperliquid.info.core import InfoCore


class Request(TypedDict):
  user: str
  """Address of the user granting builder-fee approval, as a 42-character hexadecimal string."""
  builder: str
  """Address of the builder to check approval for, as a 42-character hexadecimal string."""


Response = int


class BuilderFeeApproved(InfoCore):
  """Check the maximum builder fee a user has approved for a given builder address. Returns `0` when the user has not approved this builder at all."""

  async def builder_fee_approved(
    self,
    *,
    user: str,
    builder: str,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Check the maximum builder fee a user has approved for a given builder address. Returns `0` when the user has not approved this builder at all.

    Args:
      user: Address of the user granting builder-fee approval, as a 42-character hexadecimal string.
      builder: Address of the builder to check approval for, as a 42-character hexadecimal string.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint)
    """
    return await self.request(
      Request(user=user, builder=builder),
      method='POST',
      path='maxBuilderFee',
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Response,
    )
