# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_hyperliquid.info.core import InfoCore


class Request(TypedDict):
  dex: NotRequired[str]
  """Perp dex name to scope the request to. Defaults to the empty string, which represents the first (default) perp dex."""


Response = list[str]


class PerpsAtOpenInterestCap(InfoCore):
  """List the coins on a perp dex that are currently at their open interest cap, and therefore only reduce-only for new orders."""

  async def perps_at_open_interest_cap(
    self,
    dex: str | None = None,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """List the coins on a perp dex that are currently at their open interest cap, and therefore only reduce-only for new orders.

    Args:
      dex: Perp dex name to scope the request to. Defaults to the empty string, which represents the first (default) perp dex.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint)
    """
    request: Request = Request()
    if dex is not None:
      request['dex'] = dex
    return await self.request(
      request,
      method='POST',
      path='perpsAtOpenInterestCap',
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Response,
    )
