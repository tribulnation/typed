# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal
from typed_core.validation import TypedDict
import pydantic

from typed_hyperliquid.core import timestamp_millis
from typed_hyperliquid.exchange.core import (
  ExchangeMixin,
  ExchangeResponse,
  raise_on_error,
  sign_user_signed_action,
)


class UserDexAbstractionAction(TypedDict):
  type: Literal['userDexAbstraction']
  user: str
  enabled: bool
  signatureChainId: str
  nonce: int


class UserDexAbstractionResult(TypedDict):
  type: Literal['default']
  """Response discriminator. Hyperliquid returns the generic `default` marker for this action rather than its own action type name."""


adapter = pydantic.TypeAdapter(ExchangeResponse[UserDexAbstractionResult])


class UserDexAbstraction(ExchangeMixin):
  async def user_dex_abstraction(
    self,
    *,
    user: str,
    enabled: bool,
    signature_chain_id: str,
    nonce: int | None = None,
    validate: bool | None = None,
  ) -> UserDexAbstractionResult:
    """Enable or disable HIP-3 DEX abstraction through Hyperliquid POST /exchange using the user-signed `userDexAbstraction` action. When active, actions on HIP-3 perps automatically transfer collateral from the validator-operated USDC perps balance for HIP-3 DEXs where USDC is the collateral token, and from spot otherwise; collateral is returned to the same source when released. Deprecated: prefer `userSetAbstraction`.

    Args:
      user: User address, 42-character hexadecimal. Can be a sub-account of the caller.
      enabled: Whether HIP-3 DEX abstraction should be enabled.
      signature_chain_id: Hex-encoded chain id used when signing the action, e.g. "0xa4b1" for Arbitrum.
      nonce: Optional action nonce. Defaults to the current timestamp.
      validate: Validate the response against the generated schema.

    References:
      - [Official docs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint)
    """
    ts = timestamp_millis.now() if nonce is None else nonce
    action: UserDexAbstractionAction = {
      'type': 'userDexAbstraction',
      'nonce': ts,
      'user': user,
      'enabled': enabled,
      'signatureChainId': signature_chain_id,
    }
    signed_action, sig = sign_user_signed_action(
      action,
      wallet=self.wallet,
      payload_types=[
        {'name': 'hyperliquidChain', 'type': 'string'},
        {'name': 'user', 'type': 'string'},
        {'name': 'enabled', 'type': 'string'},
        {'name': 'nonce', 'type': 'uint64'},
      ],
      primary_type='HyperliquidTransaction:UserDexAbstraction',
      mainnet=self.mainnet,
    )
    result = await self.client.request(
      {
        'action': signed_action,
        'nonce': ts,
        'signature': sig,
        'vaultAddress': None,
        'expiresAfter': None,
      }
    )
    should_validate = self.validate if validate is None else validate
    return raise_on_error(
      adapter.validate_python(result) if should_validate else result
    )
