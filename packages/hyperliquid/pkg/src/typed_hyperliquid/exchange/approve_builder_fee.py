# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_hyperliquid.exchange.core import ExchangeCore


class ApproveBuilderFeeResult(TypedDict):
  type: Literal['default']
  """Response discriminator. Hyperliquid returns the generic `default` marker for this action rather than its own action type name."""


class Request(TypedDict):
  maxFeeRate: str
  """Maximum builder fee rate to approve, as a percent string (e.g. "0.001%")."""
  builder: str
  """Builder address, 42-character hexadecimal."""
  signatureChainId: str
  """Hex-encoded chain id used when signing the action, e.g. "0xa4b1" for Arbitrum."""
  nonce: NotRequired[int]
  """Optional action nonce. Defaults to the current timestamp."""


class ApproveBuilderFee(ExchangeCore):
  """Approve a maximum fee rate for a builder through Hyperliquid POST /exchange using the user-signed `approveBuilderFee` action."""

  async def approve_builder_fee(
    self,
    *,
    max_fee_rate: str,
    builder: str,
    signature_chain_id: str,
    nonce: int | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> ApproveBuilderFeeResult:
    """Approve a maximum fee rate for a builder through Hyperliquid POST /exchange using the user-signed `approveBuilderFee` action.

    Args:
      max_fee_rate: Maximum builder fee rate to approve, as a percent string (e.g. "0.001%").
      builder: Builder address, 42-character hexadecimal.
      signature_chain_id: Hex-encoded chain id used when signing the action, e.g. "0xa4b1" for Arbitrum.
      nonce: Optional action nonce. Defaults to the current timestamp.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint)
    """
    request: Request = Request(
      maxFeeRate=max_fee_rate, builder=builder, signatureChainId=signature_chain_id
    )
    if nonce is not None:
      request['nonce'] = nonce
    return await self.request(
      request,
      method='POST',
      path='approveBuilderFee',
      meta={'scheme': 'user_signed'},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=ApproveBuilderFeeResult,
    )
