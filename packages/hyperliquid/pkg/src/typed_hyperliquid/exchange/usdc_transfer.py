# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_hyperliquid.core import TimestampMillis
from typed_hyperliquid.exchange.core import ExchangeCore


class Request(TypedDict):
  destination: str
  """Destination address, 42-character hexadecimal (e.g. "0x0000000000000000000000000000000000000000")."""
  amount: Decimal
  """USDC amount to send, as a decimal string (e.g. "1" for 1 USDC)."""
  signatureChainId: str
  """Hex-encoded chain id used when signing the action, e.g. "0xa4b1" for Arbitrum."""
  time: NotRequired[TimestampMillis]
  """Action timestamp in epoch milliseconds, used as the nonce. Defaults to the current timestamp when omitted."""
  nonce: NotRequired[int]
  """Optional action nonce. Defaults to the current timestamp."""


class UsdSendResult(TypedDict):
  type: Literal['default']
  """Response discriminator. Hyperliquid returns the generic `default` marker for this action rather than its own action type name."""


class UsdcTransfer(ExchangeCore):
  """Send USDC to another address through Hyperliquid POST /exchange using the user-signed `usdSend` action. This transfer does not touch the EVM bridge."""

  async def usdc_transfer(
    self,
    *,
    destination: str,
    amount: Decimal,
    signature_chain_id: str,
    time: TimestampMillis | None = None,
    nonce: int | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> UsdSendResult:
    """Send USDC to another address through Hyperliquid POST /exchange using the user-signed `usdSend` action. This transfer does not touch the EVM bridge.

    Args:
      destination: Destination address, 42-character hexadecimal (e.g. "0x0000000000000000000000000000000000000000").
      amount: USDC amount to send, as a decimal string (e.g. "1" for 1 USDC).
      signature_chain_id: Hex-encoded chain id used when signing the action, e.g. "0xa4b1" for Arbitrum.
      time: Action timestamp in epoch milliseconds, used as the nonce. Defaults to the current timestamp when omitted.
      nonce: Optional action nonce. Defaults to the current timestamp.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint)
    """
    request: Request = Request(
      destination=destination, amount=amount, signatureChainId=signature_chain_id
    )
    if time is not None:
      request['time'] = time
    if nonce is not None:
      request['nonce'] = nonce
    return await self.request(
      request,
      method='POST',
      path='usdSend',
      meta={'scheme': 'user_signed'},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=UsdSendResult,
    )
