# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, Literal
from typed_core.validation import TypedDict
import pydantic

from typed_hyperliquid.core import timestamp_millis
from typed_hyperliquid.exchange.core import (
  ExchangeMixin,
  ExchangeResponse,
  raise_on_error,
  sign_l1_action,
)


class RegisterStandaloneOutcomeFromTemplateResponse(TypedDict):
  """Successful response to RegisterStandaloneOutcomeFromTemplate."""

  type: str
  """Echoes the action type of the request that produced this response. On the wire this action is a variant of the outer `spotDeploy` action -- `{"type": "spotDeploy", "outcome": {"registerStandaloneOutcomeFromTemplate": {...}}}`, not its own top-level `type` (see `notes`). By analogy with every other captured hyperliquid exchange endpoint (response.type echoes the request's own top-level type), response.type most likely echoes `"spotDeploy"` here, not this action's own variant name -- but this is inferred, not observed, so it is left as a plain string rather than an `enum`."""
  data: dict[str, Any]
  """Action-specific result payload. Not documented at all by HIP-4's doc page. The newly-deployed outcome is referenced afterwards by a numeric index "assigned at creation" (per the doc's Action format section), so the response almost certainly carries that assigned index somewhere -- but the doc names no response field for it, so no such field is declared here rather than guessing its name. Left as an open map rather than asserted empty."""


class RegisterStandaloneOutcomeFromTemplateSubAction(TypedDict):
  id: str
  keywordToValue: list[tuple[str, str]]
  deployerFeeScale: str


class RegisterStandaloneOutcomeFromTemplateWrap0(TypedDict):
  registerStandaloneOutcomeFromTemplate: RegisterStandaloneOutcomeFromTemplateSubAction


class RegisterStandaloneOutcomeFromTemplateAction(TypedDict):
  type: Literal['spotDeploy']
  outcome: RegisterStandaloneOutcomeFromTemplateWrap0


adapter = pydantic.TypeAdapter(
  ExchangeResponse[RegisterStandaloneOutcomeFromTemplateResponse]
)


class RegisterStandaloneOutcomeFromTemplate(ExchangeMixin):
  async def register_standalone_outcome_from_template(
    self,
    *,
    id: str,
    keyword_to_value: list[tuple[str, str]],
    deployer_fee_scale: str,
    expires_after: int | None = None,
    validate: bool | None = None,
  ) -> RegisterStandaloneOutcomeFromTemplateResponse:
    """Deploy a standalone YES/NO outcome market from a standalone-outcome template through Hyperliquid POST /exchange using action type `spotDeploy`, `outcome` variant `registerStandaloneOutcomeFromTemplate`.

    Args:
      id: Standalone-outcome template id to instantiate, e.g. `"abc"`.
      keyword_to_value: Lexicographically-sorted list of `[keyword, value]` pairs, one per the template's declared keywords, supplying exactly one value per keyword.
      deployer_fee_scale: Decimal string in `[0, 10]` controlling the deployer fee share for this outcome. Users pay the base outcome trading fee rate times `scale + max(scale, 1)`: the deployer receives the `scale` component and the protocol the rest, e.g. `"1"` doubles the total fee and splits it evenly. Cannot be negative or greater than `10`.
      expires_after: Optional expiration timestamp for the signed action.
      validate: Validate the response against the generated schema.

    References:
      - [Official docs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/hip-4-deployer-actions)
    """
    ts = timestamp_millis.now()
    sub_action: RegisterStandaloneOutcomeFromTemplateSubAction = {
      'id': id,
      'keywordToValue': keyword_to_value,
      'deployerFeeScale': deployer_fee_scale,
    }
    action: RegisterStandaloneOutcomeFromTemplateAction = {
      'type': 'spotDeploy',
      'outcome': {'registerStandaloneOutcomeFromTemplate': sub_action},
    }
    sig = sign_l1_action(
      action,
      wallet=self.wallet,
      nonce=ts,
      mainnet=self.mainnet,
      vault_address=None,
      expires_after=expires_after,
    )
    result = await self.client.request(
      {
        'action': action,
        'nonce': ts,
        'signature': sig,
        'vaultAddress': None,
        'expiresAfter': expires_after,
      }
    )
    should_validate = self.validate if validate is None else validate
    return raise_on_error(
      adapter.validate_python(result) if should_validate else result
    )
