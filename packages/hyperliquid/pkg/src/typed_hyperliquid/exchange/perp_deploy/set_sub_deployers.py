# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_hyperliquid.core import TimestampMillis
from typed_hyperliquid.exchange.core import ExchangeCore


class PerpDeployDefaultResponse(TypedDict):
  """Acknowledgement returned for a perpDeploy action that carries no per-item batch data."""

  type: Literal['default']
  """Fixed response-kind discriminator for this action."""


class PerpDeploySubDeployerInput(TypedDict):
  """One modification to sub-deployer permissions."""

  variant: Literal[
    'registerAsset2',
    'registerAsset',
    'setOracle',
    'setFundingMultipliers',
    'setFundingInterestRates',
    'haltTrading',
    'setMarginTableIds',
    'setFeeRecipient',
    'setOpenInterestCaps',
    'setSubDeployers',
    'setMarginModes',
    'setDeployerFees',
    'setPerpAnnotation',
    'disableDex',
  ]
  """Which perpDeploy action this permission grant applies to (the sub-action key the sub-deployer is being authorized, or unauthorized, to call)."""
  user: str
  """Address of the sub-deployer."""
  allowed: bool
  """Whether to add (`true`) or remove (`false`) this user from the authorized set for `variant`."""


class Request(TypedDict):
  dex: str
  """Name of the perp dex."""
  subDeployers: list[PerpDeploySubDeployerInput]
  """The sub-deployer permission changes to apply, in order."""
  expiresAfter: NotRequired[TimestampMillis]
  """Optional expiration timestamp for the signed action."""


class SetSubDeployers(ExchangeCore):
  """Add or remove sub-deployer permissions, scoped to individual perpDeploy action variants, for a HIP-3 perp dex through Hyperliquid POST /exchange using perpDeploy sub-action `setSubDeployers`."""

  async def set_sub_deployers(
    self,
    dex: str,
    *,
    sub_deployers: list[PerpDeploySubDeployerInput],
    expires_after: TimestampMillis | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> PerpDeployDefaultResponse:
    """Add or remove sub-deployer permissions, scoped to individual perpDeploy action variants, for a HIP-3 perp dex through Hyperliquid POST /exchange using perpDeploy sub-action `setSubDeployers`.

    Args:
      dex: Name of the perp dex.
      sub_deployers: The sub-deployer permission changes to apply, in order.
      expires_after: Optional expiration timestamp for the signed action.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/hip-3-deployer-actions)
    """
    request: Request = Request(dex=dex, subDeployers=sub_deployers)
    if expires_after is not None:
      request['expiresAfter'] = expires_after
    return await self.request(
      request,
      method='POST',
      path='perpDeploy.setSubDeployers',
      meta={'scheme': 'l1'},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=PerpDeployDefaultResponse,
    )
