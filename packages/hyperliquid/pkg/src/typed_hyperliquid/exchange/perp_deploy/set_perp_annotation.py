# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_hyperliquid.core import TimestampMillis
from typed_hyperliquid.exchange.core import ExchangeCore


class PerpDeployDefaultResponse(TypedDict):
  """Acknowledgement returned for a perpDeploy action that carries no per-item batch data."""

  type: Literal['default']
  """Fixed response-kind discriminator for this action."""


class Request(TypedDict):
  coin: str
  """Ticker of the asset being annotated."""
  category: str
  """Display category for the asset, at most 15 characters. The venue does not publish a closed set of category values, so this is left as a free-form string."""
  description: str
  """Display description for the asset, at most 400 characters."""
  displayName: str | None
  """Display name for the asset, at most 9 characters, or `null` to clear it."""
  keywords: list[str]
  """Search keywords for the asset, at most 2 entries."""
  expiresAfter: NotRequired[TimestampMillis]
  """Optional expiration timestamp for the signed action."""


class SetPerpAnnotation(ExchangeCore):
  """Set display metadata (category, description, display name, keywords) for one asset on a HIP-3 perp dex through Hyperliquid POST /exchange using perpDeploy sub-action `setPerpAnnotation`."""

  async def set_perp_annotation(
    self,
    *,
    coin: str,
    category: str,
    description: str,
    display_name: str | None,
    keywords: list[str],
    expires_after: TimestampMillis | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> PerpDeployDefaultResponse:
    """Set display metadata (category, description, display name, keywords) for one asset on a HIP-3 perp dex through Hyperliquid POST /exchange using perpDeploy sub-action `setPerpAnnotation`.

    Args:
      coin: Ticker of the asset being annotated.
      category: Display category for the asset, at most 15 characters. The venue does not publish a closed set of category values, so this is left as a free-form string.
      description: Display description for the asset, at most 400 characters.
      display_name: Display name for the asset, at most 9 characters, or `null` to clear it.
      keywords: Search keywords for the asset, at most 2 entries.
      expires_after: Optional expiration timestamp for the signed action.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/hip-3-deployer-actions)
    """
    request: Request = Request(
      coin=coin,
      category=category,
      description=description,
      displayName=display_name,
      keywords=keywords,
    )
    if expires_after is not None:
      request['expiresAfter'] = expires_after
    return await self.request(
      request,
      method='POST',
      path='perpDeploy.setPerpAnnotation',
      meta={'scheme': 'l1'},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=PerpDeployDefaultResponse,
    )
