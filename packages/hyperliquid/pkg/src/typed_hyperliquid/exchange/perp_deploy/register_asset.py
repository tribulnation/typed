# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_hyperliquid.core import TimestampMillis
from typed_hyperliquid.exchange.core import ExchangeCore


class PerpDeployDefaultResponse(TypedDict):
  """Acknowledgement returned for a perpDeploy action that carries no per-item batch data."""

  type: Literal['default']
  """Fixed response-kind discriminator for this action."""


class PerpDeployPerpDexSchema(TypedDict):
  """New perp dex parameters. Provide only when initializing a new dex in this call; omit to register another asset on an already-deployed dex."""

  full_name: str
  """Full name of the perp dex."""
  collateral_token: int
  """Collateral token index."""
  oracle_updater: NotRequired[str]
  """User authorized to update oracle prices for this dex. Defaults to the deployer when omitted."""


class PerpDeployRegisterAssetRequestFields(TypedDict):
  """New asset listing parameters."""

  coin: str
  """Ticker of the new asset."""
  sz_decimals: int
  """Number of decimals used for order/position size on this asset."""
  oracle_px: Decimal
  """Initial oracle price for the asset, as a decimal string."""
  margin_table_id: int
  """Margin table id to apply to the asset. Must be non-zero."""
  only_isolated: bool
  """Whether the asset only supports isolated margin."""


class Request(TypedDict):
  assetRequest: PerpDeployRegisterAssetRequestFields
  dex: str
  """Name of the perp dex: 2-4 lowercase characters."""
  maxGas: NotRequired[int]
  """Max gas in native token wei. Omit to use the current deploy auction price; `0` forces a reserve deployment at the current auction price even if the auction has ended (a limited number of reserve deployments are allowed)."""
  schema: NotRequired[PerpDeployPerpDexSchema]
  expiresAfter: NotRequired[TimestampMillis]
  """Optional expiration timestamp for the signed action."""


class RegisterAsset(ExchangeCore):
  """Register a new asset on a builder-deployed HIP-3 perp dex, optionally initializing the dex itself in the same call, through Hyperliquid POST /exchange using perpDeploy sub-action `registerAsset`. If `schema` is omitted, the dex must already exist and this call adds another asset to it. Structurally identical to `registerAsset2` except its asset request carries a boolean `only_isolated` instead of a 3-way `margin_mode`."""

  async def register_asset(
    self,
    asset_request: PerpDeployRegisterAssetRequestFields,
    *,
    dex: str,
    max_gas: int | None = None,
    schema: PerpDeployPerpDexSchema | None = None,
    expires_after: TimestampMillis | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> PerpDeployDefaultResponse:
    """Register a new asset on a builder-deployed HIP-3 perp dex, optionally initializing the dex itself in the same call, through Hyperliquid POST /exchange using perpDeploy sub-action `registerAsset`. If `schema` is omitted, the dex must already exist and this call adds another asset to it. Structurally identical to `registerAsset2` except its asset request carries a boolean `only_isolated` instead of a 3-way `margin_mode`.

    Args:
      asset_request: New asset listing parameters.
      dex: Name of the perp dex: 2-4 lowercase characters.
      max_gas: Max gas in native token wei. Omit to use the current deploy auction price; `0` forces a reserve deployment at the current auction price even if the auction has ended (a limited number of reserve deployments are allowed).
      schema: New perp dex parameters. Provide only when initializing a new dex in this call; omit to register another asset on an already-deployed dex.
      expires_after: Optional expiration timestamp for the signed action.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/hip-3-deployer-actions)
    """
    request: Request = Request(assetRequest=asset_request, dex=dex)
    if max_gas is not None:
      request['maxGas'] = max_gas
    if schema is not None:
      request['schema'] = schema
    if expires_after is not None:
      request['expiresAfter'] = expires_after
    return await self.request(
      request,
      method='POST',
      path='perpDeploy.registerAsset',
      meta={'scheme': 'l1'},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=PerpDeployDefaultResponse,
    )
