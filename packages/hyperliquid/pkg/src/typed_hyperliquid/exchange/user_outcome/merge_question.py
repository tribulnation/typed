# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_hyperliquid.core import TimestampMillis
from typed_hyperliquid.exchange.core import ExchangeCore


class MergeQuestionResult(TypedDict):
  type: Literal['default']
  """Response discriminator. Hyperliquid returns the generic `default` marker for this action rather than its own action type name."""


class Request(TypedDict):
  question: int
  """Numeric index of the HIP-4 question to merge, assigned when it was created."""
  amount: Decimal | None
  """Decimal string amount of Yes shares to merge from each of the question's outcomes into quote token, e.g. "123.0". Null merges the caller's full matched Yes-share balance across the question's outcomes."""
  expiresAfter: NotRequired[TimestampMillis]
  """Optional expiration timestamp for the signed action."""


class MergeQuestion(ExchangeCore):
  """Merge X Yes shares from each outcome associated with the same HIP-4 question into X quote tokens, through Hyperliquid POST /exchange using action type `userOutcome`, variant `mergeQuestion`."""

  async def merge_question(
    self,
    question: int,
    *,
    amount: Decimal | None,
    expires_after: TimestampMillis | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> MergeQuestionResult:
    """Merge X Yes shares from each outcome associated with the same HIP-4 question into X quote tokens, through Hyperliquid POST /exchange using action type `userOutcome`, variant `mergeQuestion`.

    Args:
      question: Numeric index of the HIP-4 question to merge, assigned when it was created.
      amount: Decimal string amount of Yes shares to merge from each of the question's outcomes into quote token, e.g. "123.0". Null merges the caller's full matched Yes-share balance across the question's outcomes.
      expires_after: Optional expiration timestamp for the signed action.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint)
    """
    request: Request = Request(question=question, amount=amount)
    if expires_after is not None:
      request['expiresAfter'] = expires_after
    return await self.request(
      request,
      method='POST',
      path='userOutcome.mergeQuestion',
      meta={'scheme': 'l1'},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=MergeQuestionResult,
    )
