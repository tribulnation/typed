# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_hyperliquid.core import TimestampMillis
from typed_hyperliquid.exchange.core import ExchangeCore


class NegateOutcomeResult(TypedDict):
  type: Literal['default']
  """Response discriminator. Hyperliquid returns the generic `default` marker for this action rather than its own action type name."""


class Request(TypedDict):
  question: int
  """Numeric index of the HIP-4 question that both the source outcome and its siblings belong to."""
  outcome: int
  """Numeric index of the HIP-4 outcome whose No shares are being converted."""
  amount: Decimal
  """Decimal string amount of No shares to convert, e.g. "123.0". Produces an equal amount of Yes shares on every other outcome associated with `question`."""
  expiresAfter: NotRequired[TimestampMillis]
  """Optional expiration timestamp for the signed action."""


class NegateOutcome(ExchangeCore):
  """Convert X No shares of one HIP-4 outcome associated with a question into X Yes shares of every other outcome associated with that question, through Hyperliquid POST /exchange using action type `userOutcome`, variant `negateOutcome`."""

  async def negate_outcome(
    self,
    *,
    question: int,
    outcome: int,
    amount: Decimal,
    expires_after: TimestampMillis | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> NegateOutcomeResult:
    """Convert X No shares of one HIP-4 outcome associated with a question into X Yes shares of every other outcome associated with that question, through Hyperliquid POST /exchange using action type `userOutcome`, variant `negateOutcome`.

    Args:
      question: Numeric index of the HIP-4 question that both the source outcome and its siblings belong to.
      outcome: Numeric index of the HIP-4 outcome whose No shares are being converted.
      amount: Decimal string amount of No shares to convert, e.g. "123.0". Produces an equal amount of Yes shares on every other outcome associated with `question`.
      expires_after: Optional expiration timestamp for the signed action.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint)
    """
    request: Request = Request(question=question, outcome=outcome, amount=amount)
    if expires_after is not None:
      request['expiresAfter'] = expires_after
    return await self.request(
      request,
      method='POST',
      path='userOutcome.negateOutcome',
      meta={'scheme': 'l1'},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=NegateOutcomeResult,
    )
