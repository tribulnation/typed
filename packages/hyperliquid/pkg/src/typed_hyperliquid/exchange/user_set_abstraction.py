# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal
from typed_core.validation import TypedDict
import pydantic

from typed_hyperliquid.core import timestamp_millis
from typed_hyperliquid.exchange.core import (
  ExchangeMixin,
  ExchangeResponse,
  raise_on_error,
  sign_user_signed_action,
)


class UserSetAbstractionAction(TypedDict):
  type: Literal['userSetAbstraction']
  user: str
  abstraction: Literal['disabled', 'unifiedAccount', 'portfolioMargin']
  signatureChainId: str
  nonce: int


class UserSetAbstractionResult(TypedDict):
  type: Literal['default']
  """Response discriminator. Hyperliquid returns the generic `default` marker for this action rather than its own action type name."""


adapter = pydantic.TypeAdapter(ExchangeResponse[UserSetAbstractionResult])


class UserSetAbstraction(ExchangeMixin):
  async def user_set_abstraction(
    self,
    *,
    user: str,
    abstraction: Literal['disabled', 'unifiedAccount', 'portfolioMargin'],
    signature_chain_id: str,
    nonce: int | None = None,
    validate: bool | None = None,
  ) -> UserSetAbstractionResult:
    """Set the caller's account-abstraction mode through Hyperliquid POST /exchange using the user-signed `userSetAbstraction` action.

    Args:
      user: User address, 42-character hexadecimal. Can be a sub-account of the caller.
      abstraction: Account-abstraction mode to set.
      signature_chain_id: Hex-encoded chain id used when signing the action, e.g. "0xa4b1" for Arbitrum.
      nonce: Optional action nonce. Defaults to the current timestamp.
      validate: Validate the response against the generated schema.

    References:
      - [Official docs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint)
    """
    ts = timestamp_millis.now() if nonce is None else nonce
    action: UserSetAbstractionAction = {
      'type': 'userSetAbstraction',
      'nonce': ts,
      'user': user,
      'abstraction': abstraction,
      'signatureChainId': signature_chain_id,
    }
    signed_action, sig = sign_user_signed_action(
      action,
      wallet=self.wallet,
      payload_types=[
        {'name': 'hyperliquidChain', 'type': 'string'},
        {'name': 'user', 'type': 'string'},
        {'name': 'abstraction', 'type': 'string'},
        {'name': 'nonce', 'type': 'uint64'},
      ],
      primary_type='HyperliquidTransaction:UserSetAbstraction',
      mainnet=self.mainnet,
    )
    result = await self.client.request(
      {
        'action': signed_action,
        'nonce': ts,
        'signature': sig,
        'vaultAddress': None,
        'expiresAfter': None,
      }
    )
    should_validate = self.validate if validate is None else validate
    return raise_on_error(
      adapter.validate_python(result) if should_validate else result
    )
