# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, Literal
from typed_core.validation import TypedDict
import pydantic

from typed_hyperliquid.core import timestamp_millis
from typed_hyperliquid.exchange.core import (
  ExchangeMixin,
  ExchangeResponse,
  raise_on_error,
  sign_l1_action,
)


class NamedOutcomeTemplateInstanceParameterNamedOutcomeTemplateInstancesItem(TypedDict):
  """Instantiation of a question-outcome template. Carries no `deployerFeeScale` of its own -- it inherits the parent question's scale."""

  id: str
  """Question-outcome template id to instantiate. Must declare the parent question's own template as its parent template; the same question-outcome template may be instantiated multiple times with different keyword values."""
  keyword_to_value: list[tuple[str, str]]
  """Lexicographically-sorted list of `[keyword, value]` pairs, one per the template's declared keywords. May be empty for a template with no keywords."""


class NamedOutcomeTemplateInstanceSubNamedOutcomeTemplateInstancesItem(TypedDict):
  """Instantiation of a question-outcome template. Carries no `deployerFeeScale` of its own -- it inherits the parent question's scale."""

  id: str
  """Question-outcome template id to instantiate. Must declare the parent question's own template as its parent template; the same question-outcome template may be instantiated multiple times with different keyword values."""
  keyword_to_value: list[tuple[str, str]]
  """Lexicographically-sorted list of `[keyword, value]` pairs, one per the template's declared keywords. May be empty for a template with no keywords."""


class QuestionTemplateInstance(TypedDict):
  id: str
  """Question template id to instantiate, e.g. `"abc"`."""
  keyword_to_value: list[tuple[str, str]]
  """Lexicographically-sorted list of `[keyword, value]` pairs, one per the template's declared keywords."""
  deployer_fee_scale: str
  """Decimal string in `[0, 10]` controlling the deployer fee share for this question's fallback and every associated question outcome. Users pay the base outcome trading fee rate times `scale + max(scale, 1)`: the deployer receives the `scale` component and the protocol the rest, e.g. `"1"` doubles the total fee and splits it evenly. Cannot be negative or greater than `10`."""


class RegisterQuestionFromTemplateResponse(TypedDict):
  """Successful response to RegisterQuestionFromTemplate."""

  type: str
  """Echoes the action type of the request that produced this response. On the wire this action is a variant of the outer `spotDeploy` action -- `{"type": "spotDeploy", "outcome": {"registerQuestionFromTemplate": {...}}}`, not its own top-level `type` (see `notes`). By analogy with every other captured hyperliquid exchange endpoint (response.type echoes the request's own top-level type), response.type most likely echoes `"spotDeploy"` here, not this action's own variant name -- but this is inferred, not observed, so it is left as a plain string rather than an `enum`."""
  data: dict[str, Any]
  """Action-specific result payload. Not documented at all by HIP-4's doc page. The newly-deployed question and its question outcomes are referenced afterwards by numeric indices "assigned at creation" (per the doc's Action format section), so the response almost certainly carries those assigned indices somewhere -- but the doc names no response field for them, so no such field is declared here rather than guessing its name. Left as an open map rather than asserted empty."""


class RegisterQuestionFromTemplateSubAction(TypedDict):
  questionTemplateInstance: QuestionTemplateInstance
  namedOutcomeTemplateInstances: list[
    NamedOutcomeTemplateInstanceSubNamedOutcomeTemplateInstancesItem
  ]


class RegisterQuestionFromTemplateWrap0(TypedDict):
  registerQuestionFromTemplate: RegisterQuestionFromTemplateSubAction


class RegisterQuestionFromTemplateAction(TypedDict):
  type: Literal['spotDeploy']
  outcome: RegisterQuestionFromTemplateWrap0


adapter = pydantic.TypeAdapter(ExchangeResponse[RegisterQuestionFromTemplateResponse])


class RegisterQuestionFromTemplate(ExchangeMixin):
  async def register_question_from_template(
    self,
    *,
    question_template_instance: QuestionTemplateInstance,
    named_outcome_template_instances: list[
      NamedOutcomeTemplateInstanceParameterNamedOutcomeTemplateInstancesItem
    ],
    expires_after: int | None = None,
    validate: bool | None = None,
  ) -> RegisterQuestionFromTemplateResponse:
    """Deploy a question (container) and one or more question outcomes in one action through Hyperliquid POST /exchange using action type `spotDeploy`, `outcome` variant `registerQuestionFromTemplate`.

    Args:
      question_template_instance: Instantiation of the question (container) template.
      named_outcome_template_instances: Question-outcome instantiations to register alongside the question, one per named outcome. Each question-outcome template must declare the question template as its parent.
      expires_after: Optional expiration timestamp for the signed action.
      validate: Validate the response against the generated schema.

    References:
      - [Official docs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/hip-4-deployer-actions)
    """
    ts = timestamp_millis.now()
    sub_action: RegisterQuestionFromTemplateSubAction = {
      'questionTemplateInstance': question_template_instance,
      'namedOutcomeTemplateInstances': named_outcome_template_instances,
    }
    action: RegisterQuestionFromTemplateAction = {
      'type': 'spotDeploy',
      'outcome': {'registerQuestionFromTemplate': sub_action},
    }
    sig = sign_l1_action(
      action,
      wallet=self.wallet,
      nonce=ts,
      mainnet=self.mainnet,
      vault_address=None,
      expires_after=expires_after,
    )
    result = await self.client.request(
      {
        'action': action,
        'nonce': ts,
        'signature': sig,
        'vaultAddress': None,
        'expiresAfter': expires_after,
      }
    )
    should_validate = self.validate if validate is None else validate
    return raise_on_error(
      adapter.validate_python(result) if should_validate else result
    )
