# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_hyperliquid.core import TimestampMillis
from typed_hyperliquid.exchange.core import ExchangeCore


class Request(TypedDict):
  asset: int
  """Asset index of the isolated position to top up, per `info.meta`'s `universe` ordering."""
  leverage: int
  """Target leverage to reach via this top-up, as a whole-number leverage multiplier sent in string form, e.g. "3"."""
  vaultAddress: NotRequired[str]
  """Optional vault address to sign this action on behalf of."""
  expiresAfter: NotRequired[TimestampMillis]
  """Optional expiration timestamp for the signed action."""


class TopUpIsolatedOnlyMarginActionResult(TypedDict):
  """Result of an accepted top-up-isolated-only-margin action."""

  type: Literal['default']
  """Discriminator confirming this is a top-up-isolated-only-margin result. The venue carries no further data alongside it."""


class TopUpIsolatedOnlyMargin(ExchangeCore):
  """Add isolated margin to reach a target leverage, through Hyperliquid POST /exchange using action type `topUpIsolatedOnlyMargin`. An alternate form of `updateIsolatedMargin` that targets a leverage value instead of a USDC amount."""

  async def top_up_isolated_only_margin(
    self,
    *,
    asset: int,
    leverage: int,
    vault_address: str | None = None,
    expires_after: TimestampMillis | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> TopUpIsolatedOnlyMarginActionResult:
    """Add isolated margin to reach a target leverage, through Hyperliquid POST /exchange using action type `topUpIsolatedOnlyMargin`. An alternate form of `updateIsolatedMargin` that targets a leverage value instead of a USDC amount.

    Args:
      asset: Asset index of the isolated position to top up, per `info.meta`'s `universe` ordering.
      leverage: Target leverage to reach via this top-up, as a whole-number leverage multiplier sent in string form, e.g. "3".
      vault_address: Optional vault address to sign this action on behalf of.
      expires_after: Optional expiration timestamp for the signed action.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint)
    """
    request: Request = Request(asset=asset, leverage=leverage)
    if vault_address is not None:
      request['vaultAddress'] = vault_address
    if expires_after is not None:
      request['expiresAfter'] = expires_after
    return await self.request(
      request,
      method='POST',
      path='topUpIsolatedOnlyMargin',
      meta={'scheme': 'l1'},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=TopUpIsolatedOnlyMarginActionResult,
    )
