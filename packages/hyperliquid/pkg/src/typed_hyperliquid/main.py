# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .exchange import Exchange
from .info import Info
from .streams import Streams
from typed_hyperliquid.core.base import ClientBase


class Hyperliquid(ClientBase):
  """Hyperliquid client root — composes the read-only Info surface, the signed Exchange surface, and WebSocket Streams, each independently reachable, over Hyperliquid's HTTP and WebSocket APIs.

  References:
    - [Upstream docs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api)
  """

  @cached_property
  def exchange(self) -> Exchange:
    """Signed, state-changing trading and account actions, served over Hyperliquid's single POST /exchange endpoint — placing, modifying, and canceling orders, transfers, staking, vault operations, and other L1 or user-signed actions.

    References:
      - [Upstream docs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint)
    """
    return Exchange.new(
      self.exchange_client,
      exchange_ws_client=self.exchange_ws_client,
      wallet=self.wallet,
      mainnet=self.mainnet,
      validate=self.validate,
    )

  @cached_property
  def info(self) -> Info:
    """Read-only market data and account-state queries, served over Hyperliquid's single POST /info endpoint — mid prices, order books, candles, positions, funding, and other account and market state, keyed by a `type` discriminator in the request body.

    References:
      - [Upstream docs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint)
    """
    return Info.new(
      self.info_client, info_ws_client=self.info_ws_client, validate=self.validate
    )

  @cached_property
  def streams(self) -> Streams:
    """Real-time WebSocket push channels — order books, trades, candles, mid prices, and per-user events (fills, funding, order updates) — subscribed and unsubscribed over Hyperliquid's WebSocket connection.

    References:
      - [Upstream docs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions)
    """
    return Streams.new(self.streams_client, validate=self.validate)
