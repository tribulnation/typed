# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .account import Account
from .currency import Currency
from .signin import Signin
from .wallet import Wallet
from typed_bit2me.core.endpoint.rpc import RpcEndpoint


class V3(RpcEndpoint):
  """Bit2Me's REST API v3: the latest account/KYC, market-data, sign-in, and wallet surface.

  References:
    - [Upstream docs](https://api.bit2me.com/doc)
  """

  @cached_property
  def account(self) -> Account:
    """Bit2Me's v3 account API: masked account details and initiating v3 identity verification.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/account)
    """
    return Account(client=self.client)

  @cached_property
  def currency(self) -> Currency:
    """Bit2Me's v3 market data: price charts, per-symbol market data, and ticker snapshots.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/market)
    """
    return Currency(client=self.client)

  @cached_property
  def signin(self) -> Signin:
    """v3 sign-in two-factor authentication.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/access)
    """
    return Signin(client=self.client)

  @cached_property
  def wallet(self) -> Wallet:
    """v3 wallet transaction listing.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/wallet)
    """
    return Wallet(client=self.client)
