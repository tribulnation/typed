# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint


CurrencyChartV3Response = list[tuple[int, float | None, float | None]]


class Request(TypedDict):
  ticker: str
  """Ticker naming both currencies as `BASE/QUOTE`, e.g. `BTC/EUR`."""
  temporality: NotRequired[
    list[
      Literal[
        'one-hour',
        'four-hours',
        'twelve-hours',
        'one-day',
        'one-week',
        'one-month',
        'three-months',
        'six-months',
        'one-year',
        'all-time',
      ]
    ]
  ]
  """Chart temporality/candle granularity to fetch. Repeatable to request multiple temporalities in one call."""


class Chart(RpcEndpoint):
  """Returns historic price chart of given ticker"""

  async def chart(
    self,
    ticker: str,
    *,
    temporality: list[
      Literal[
        'one-hour',
        'four-hours',
        'twelve-hours',
        'one-day',
        'one-week',
        'one-month',
        'three-months',
        'six-months',
        'one-year',
        'all-time',
      ]
    ]
    | None = None,
    validate: bool | None = None,
  ) -> CurrencyChartV3Response:
    """Returns historic price chart of given ticker

    Args:
      ticker: Ticker naming both currencies as `BASE/QUOTE`, e.g. `BTC/EUR`.
      temporality: Chart temporality/candle granularity to fetch. Repeatable to request multiple temporalities in one call.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/market/GET/v3/currency/chart)
    """
    request: Request = Request(ticker=ticker)
    if temporality is not None:
      request['temporality'] = temporality
    return await self.request(
      request,
      method='GET',
      path='/v3/currency/chart',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=CurrencyChartV3Response,
    )
