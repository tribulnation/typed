# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_bit2me.types import MillisTimestamp
from typed_bit2me.core.endpoint import RpcEndpoint
from typed_core.validation import validator


class TradingOrderBookResponse(TypedDict):
  asks: NotRequired[list[tuple[Decimal, Decimal]]]
  """Sell orders, sorted by ascending price."""
  bids: NotRequired[list[tuple[Decimal, Decimal]]]
  """Buy orders, sorted by descending price."""
  timestamp: NotRequired[MillisTimestamp]
  symbol: NotRequired[str]
  """Market symbol"""


validate_response = validator(TradingOrderBookResponse)


class OrderBook(RpcEndpoint):
  async def order_book(
    self,
    *,
    symbol: str,
    validate: bool | None = None,
  ) -> TradingOrderBookResponse:
    """Get the order book associated to the market symbol.

    Args:
      symbol: Market symbol to fetch the order book for.
      validate: Whether to validate the response against the expected schema.

    References:
      - [Bit2Me API docs](https://api.bit2me.com/trading-spot-rest#tag/marketdata/GET/v2/trading/order-book)
    """
    params: dict = {
      'symbol': symbol,
    }
    return await self.request(
      'GET',
      '/v2/trading/order-book',
      params=params,
      validator=validate_response,
      validate=validate,
    )
