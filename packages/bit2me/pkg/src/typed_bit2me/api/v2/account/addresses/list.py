# Generated by typed-dev — see CONTRIBUTING.md.
from typed_bit2me.types import UserAddress
from typed_bit2me.core.endpoint import RpcEndpoint
from typed_core.validation import validator

ListResponse = list[UserAddress]

validate_response = validator(ListResponse)


class List(RpcEndpoint):
  async def list(
    self,
    *,
    address_id: str | None = None,
    is_residence: bool | None = None,
    validate: bool | None = None,
  ) -> ListResponse:
    """Get the addresses of a specific user

    Args:
      address_id: The address id
      is_residence: Value of the isResidence field
      validate: Whether to validate the response against the expected schema.

    References:
      - [Bit2Me API docs](https://api.bit2me.com/doc#tag/account/GET/v2/account/address)
    """
    params = {}
    if address_id is not None:
      params['addressId'] = address_id
    if is_residence is not None:
      params['isResidence'] = is_residence
    return await self.authed_request(
      'GET',
      '/v2/account/address',
      params=params,
      validator=validate_response,
      validate=validate,
    )
