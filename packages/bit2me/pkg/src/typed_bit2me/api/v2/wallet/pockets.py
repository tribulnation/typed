# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bit2me.core import TimestampIso
from typed_bit2me.core.endpoint import RpcEndpoint
from typed_core.validation import validator


class WalletAddress(TypedDict):
  """A deposit address belonging to a pocket on a specific network."""

  id: NotRequired[str]
  """Unique identifier of the address record."""
  createdAt: NotRequired[TimestampIso]
  """When the address was created (ISO 8601)"""
  address: NotRequired[str]
  """The wallet address on `network`."""
  network: NotRequired[str]
  """The destination address network"""
  tag: NotRequired[str | None]
  """Memo/tag some networks require to route funds to `address`, or null when not applicable."""


validate_response = validator(list[WalletAddress])


class Pockets(RpcEndpoint):
  async def pockets(
    self,
    pocket_id: str,
    network: str,
    *,
    validate: bool | None = None,
  ) -> list[WalletAddress]:
    """Find all deposit addresses for a pocket on a given network, creating one if none exists yet.

    Args:
      pocket_id: Identifier of the pocket to find or create addresses for.
      network: Blockchain network to find or create the address on (e.g. `ethereum`).
      validate: Whether to validate the response against the expected schema.

    References:
      - [Bit2Me API docs](https://api.bit2me.com/doc#tag/wallet/GET/v2/wallet/pocket/{pocketId}/{network}/address)
    """
    return await self.authed_request(
      'GET',
      f'/v2/wallet/pocket/{pocket_id}/{network}/address',
      validator=validate_response,
      validate=validate,
    )
