# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal
from typed_bit2me.types import EarnWalletResponse
from typed_bit2me.core.endpoint import RpcEndpoint
from typed_core.validation import validator

validate_response = validator(EarnWalletResponse)


class Wallets(RpcEndpoint):
  async def wallets(
    self,
    *,
    user_currency: str | None = None,
    sort_by: Literal['currency', 'balance'] | None = None,
    sort_direction: Literal['ascending', 'descending'] | None = None,
    offset: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> EarnWalletResponse:
    """Get a list of earn wallets

    Args:
      user_currency: Currency to show convertedBalance
      sort_by: Sorting field
      sort_direction: Sorting direction
      offset: Number of wallets to skip before returning results.
      limit: Maximum number of wallets to return.
      validate: Whether to validate the response against the expected schema.

    References:
      - [Bit2Me API docs](https://api.bit2me.com/doc#tag/earn/GET/v2/earn/wallets)
    """
    params = {}
    if user_currency is not None:
      params['userCurrency'] = user_currency
    if sort_by is not None:
      params['sortBy'] = sort_by
    if sort_direction is not None:
      params['sortDirection'] = sort_direction
    if offset is not None:
      params['offset'] = offset
    if limit is not None:
      params['limit'] = limit
    return await self.authed_request(
      'GET',
      '/v2/earn/wallets',
      params=params,
      validator=validate_response,
      validate=validate,
    )
