# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal
from typed_bit2me.core.endpoint import RpcEndpoint
from typed_core.validation import validator

validate_response = validator(list[tuple[int, Decimal | None, Decimal | None]])


class Chart(RpcEndpoint):
  async def chart(
    self,
    *,
    ticker: str,
    temporality: list[
      Literal[
        'one-hour',
        'four-hours',
        'twelve-hours',
        'one-day',
        'one-week',
        'one-month',
        'three-months',
        'six-months',
        'one-year',
        'all-time',
      ]
    ]
    | None = None,
    validate: bool | None = None,
  ) -> list[tuple[int, Decimal | None, Decimal | None]]:
    """Returns historic price chart of given ticker

    Args:
      ticker: Ticker naming both currencies as `BASE/QUOTE`, e.g. `BTC/EUR`.
      temporality: Chart temporality/candle granularity to fetch. Repeatable to request multiple temporalities in one call.
      validate: Whether to validate the response against the expected schema.

    References:
      - [Bit2Me API docs](https://api.bit2me.com/doc#tag/market/GET/v3/currency/chart)
    """
    params: dict = {
      'ticker': ticker,
    }
    if temporality is not None:
      params['temporality'] = temporality
    return await self.authed_request(
      'GET',
      '/v3/currency/chart',
      params=params,
      validator=validate_response,
      validate=validate,
    )
