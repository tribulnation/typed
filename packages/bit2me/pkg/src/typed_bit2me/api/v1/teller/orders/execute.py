# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bit2me.core.endpoint import RpcEndpoint
from typed_core.validation import validator


class ExecuteOrderRequestConfiguration(TypedDict):
  """How the caller should present the provider's flow to the user."""

  type: NotRequired[str]
  """Presentation mode for the provider flow."""
  uri: NotRequired[str]
  """URL of the provider flow to present to the user."""


class TellerOrderExecuteRequest(TypedDict):
  orderId: str


class ExecuteOrderProvider(TypedDict):
  """Details of the payment provider flow the caller must present to the user to complete a `waiting-user` order."""

  requestConfiguration: ExecuteOrderRequestConfiguration


class ExecuteOrderData(TypedDict):
  orderId: str
  """Identifier of the order that was executed."""
  status: Literal['pending', 'waiting-user', 'rejected', 'accepted']
  """Order status after execution."""
  token: NotRequired[str]
  """Provider session token used to complete the order when it is in `waiting-user` status and requires further user interaction."""
  provider: NotRequired[ExecuteOrderProvider]


validate_response = validator(ExecuteOrderData)


class Execute(RpcEndpoint):
  async def execute(
    self,
    teller_order_execute_request: TellerOrderExecuteRequest,
    *,
    validate: bool | None = None,
  ) -> ExecuteOrderData:
    """Execute a card order previously created with `POST /v1/teller/order/proforma`.

    Args:
      teller_order_execute_request: The `orderId` of a deposit proforma previously created with `POST /v1/teller/order/proforma`, to execute.
      validate: Whether to validate the response against the expected schema.

    References:
      - [Bit2Me API docs](https://api.bit2me.com/doc#tag/funding/POST/v1/teller/order)
    """
    return await self.authed_request(
      'POST',
      '/v1/teller/order',
      json=teller_order_execute_request,
      validator=validate_response,
      validate=validate,
    )
