# Generated by typed-dev — see CONTRIBUTING.md.
from typed_bit2me.types import BooleanResultResponse
from typed_bit2me.core.endpoint import RpcEndpoint
from typed_core.validation import validator

validate_response = validator(BooleanResultResponse)


class Ibans(RpcEndpoint):
  async def __call__(
    self,
    *,
    iban: str,
    validate: bool | None = None,
  ) -> BooleanResultResponse:
    """Check whether an IBAN is valid.

    Args:
      iban: Iban to check
      validate: Whether to validate the response against the expected schema.

    References:
      - [Bit2Me API docs](https://api.bit2me.com/doc#tag/funding/GET/v1/teller/iban/validate)
    """
    params: dict = {
      'iban': iban,
    }
    return await self.authed_request(
      'GET',
      '/v1/teller/iban/validate',
      params=params,
      validator=validate_response,
      validate=validate,
    )
