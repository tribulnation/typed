# Generated by typed-dev — see CONTRIBUTING.md.
from typed_bit2me.types import OrderResponse
from typed_bit2me.core.endpoint import RpcEndpoint
from typed_core.validation import validator

validate_response = validator(OrderResponse)


class Cancel(RpcEndpoint):
  async def cancel(self, id: str, *, validate: bool | None = None) -> OrderResponse:
    """Cancel the order by identifier if this order is open or inactive.

    Args:
      id: Identifier of the order to cancel.
      validate: Whether to validate the response against the expected schema.

    References:
      - [Bit2Me API docs](https://api.bit2me.com/trading-spot-rest#tag/trading/DELETE/v1/trading/order/{id})
    """
    return await self.authed_request(
      'DELETE',
      f'/v1/trading/order/{id}',
      validator=validate_response,
      validate=validate,
    )
