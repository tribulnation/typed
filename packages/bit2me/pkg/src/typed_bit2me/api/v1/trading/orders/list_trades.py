# Generated by typed-dev — see CONTRIBUTING.md.
from typed_bit2me.types import TradeResponse
from typed_bit2me.core.endpoint import RpcEndpoint
from typed_core.validation import validator

validate_response = validator(list[TradeResponse])


class ListTrades(RpcEndpoint):
  async def list_trades(
    self,
    id: str,
    *,
    validate: bool | None = None,
  ) -> list[TradeResponse]:
    """Get all trades associated to the order.

    Args:
      id: Identifier of the order whose trades are being listed.
      validate: Whether to validate the response against the expected schema.

    References:
      - [Bit2Me API docs](https://api.bit2me.com/trading-spot-rest#tag/trading/GET/v1/trading/order/{id}/trades)
    """
    return await self.authed_request(
      'GET',
      f'/v1/trading/order/{id}/trades',
      validator=validate_response,
      validate=validate,
    )
