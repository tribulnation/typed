# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any
from typed_bit2me.types import WalletResponse
from typed_bit2me.core.endpoint import RpcEndpoint
from typed_core.validation import validator

validate_response = validator(list[WalletResponse])


class Balance(RpcEndpoint):
  async def __call__(
    self,
    *,
    symbols: Any | None = None,
    validate: bool | None = None,
  ) -> list[WalletResponse]:
    """Retrieve balances of all wallets and blocked balances in active orders.

    Args:
      symbols: Comma separated symbols of the wallets that you want to retrieve
      validate: Whether to validate the response against the expected schema.

    References:
      - [Bit2Me API docs](https://api.bit2me.com/trading-spot-rest#tag/funding/GET/v1/trading/wallet/balance)
    """
    params = {'symbols': symbols} if symbols is not None else None
    return await self.authed_request(
      'GET',
      '/v1/trading/wallet/balance',
      params=params,
      validator=validate_response,
      validate=validate,
    )
