# Generated by typed-dev — see CONTRIBUTING.md.
from typed_bit2me.types import EarnWalletRecord
from typed_bit2me.core.endpoint import RpcEndpoint
from typed_core.validation import validator

validate_response = validator(EarnWalletRecord)


class Get(RpcEndpoint):
  async def get(
    self,
    wallet_id: str,
    *,
    user_currency: str | None = None,
    validate: bool | None = None,
  ) -> EarnWalletRecord:
    """Retrieves full information of selected wallet

    Args:
      wallet_id: Wallet identifier
      user_currency: Currency to show convertedBalance
      validate: Whether to validate the response against the expected schema.

    References:
      - [Bit2Me API docs](https://api.bit2me.com/doc#tag/earn/GET/v1/earn/wallets/{walletId})
    """
    params = {'userCurrency': user_currency} if user_currency is not None else None
    return await self.authed_request(
      'GET',
      f'/v1/earn/wallets/{wallet_id}',
      params=params,
      validator=validate_response,
      validate=validate,
    )
