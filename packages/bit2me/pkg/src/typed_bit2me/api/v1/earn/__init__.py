# Generated by typed-dev — see CONTRIBUTING.md.
from dataclasses import dataclass
from functools import cached_property

from typed_bit2me.core.endpoint import RpcEndpoint
from .movements import Movements
from .summary import Summary
from .wallets import Wallets


@dataclass(kw_only=True, frozen=True)
class Earn(RpcEndpoint):
  @cached_property
  def movements(self) -> Movements:
    return Movements(client=self.client)

  @cached_property
  def summary(self) -> Summary:
    return Summary(client=self.client)

  @cached_property
  def wallets(self) -> Wallets:
    return Wallets(client=self.client)
