# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bit2me.core.endpoint import RpcEndpoint
from typed_core.validation import validator


class EarnUserSummaryResponse(TypedDict):
  currency: str
  """Currency all balances and rewards in this summary are expressed in (the requested `userCurrency`, or the account default)."""
  totalBalance: Decimal
  """Total balance across all Earn wallets, converted to `currency`."""
  totalRewards: Decimal
  """Total accumulated rewards across all Earn wallets, converted to `currency`."""


validate_response = validator(EarnUserSummaryResponse)


class Summary(RpcEndpoint):
  async def __call__(
    self,
    *,
    user_currency: str | None = None,
    validate: bool | None = None,
  ) -> EarnUserSummaryResponse:
    """Retrieves user summary

    Args:
      user_currency: Currency to show balance
      validate: Whether to validate the response against the expected schema.

    References:
      - [Bit2Me API docs](https://api.bit2me.com/doc#tag/earn/GET/v1/earn/summary)
    """
    params = {'userCurrency': user_currency} if user_currency is not None else None
    return await self.authed_request(
      'GET',
      '/v1/earn/summary',
      params=params,
      validator=validate_response,
      validate=validate,
    )
