# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bit2me.core import TimestampIso
from typed_bit2me.core.endpoint import RpcEndpoint
from typed_core.validation import validator


class Entry(TypedDict):
  walletId: str
  """Identifier of the Earn wallet."""
  userId: str
  """Identifier of the user the wallet belongs to."""
  currency: str
  """Currency of the Earn wallet."""
  lockPeriodId: str | None
  """Identifier of the lock period this wallet's rewards are configured under, or null when the wallet has no lock period."""
  rewardCurrency: str
  """Currency the wallet's rewards are paid out in."""
  createdAt: TimestampIso
  """Date and time the reward configuration was created."""
  updatedAt: TimestampIso
  """Date and time the reward configuration was last updated."""


validate_response = validator(list[Entry])


class ListRewardsConfig(RpcEndpoint):
  async def list_rewards_config(self, *, validate: bool | None = None) -> list[Entry]:
    """Retrieves rewards configuration for user wallets

    Args:
      validate: Whether to validate the response against the expected schema.

    References:
      - [Bit2Me API docs](https://api.bit2me.com/doc#tag/earn/GET/v1/earn/wallets/rewards/config)
    """
    return await self.authed_request(
      'GET',
      '/v1/earn/wallets/rewards/config',
      validator=validate_response,
      validate=validate,
    )
