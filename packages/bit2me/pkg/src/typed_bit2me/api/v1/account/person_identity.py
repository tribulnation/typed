# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bit2me.core import TimestampIso
from typed_bit2me.types import BooleanResultResponse
from typed_bit2me.core.endpoint import RpcEndpoint
from typed_core.validation import validator


class UpdatePersonIdentityRequest(TypedDict):
  type: Literal['none', 'idcard', 'passport', 'drivinglicense', 'visa']
  number: str
  expiryDate: TimestampIso
  countryCode: str


validate_response = validator(BooleanResultResponse)


class PersonIdentity(RpcEndpoint):
  async def __call__(
    self,
    update_person_identity_request: UpdatePersonIdentityRequest,
    *,
    validate: bool | None = None,
  ) -> BooleanResultResponse:
    """Update user person identity

    Args:
      update_person_identity_request: The details to update user person identity
      validate: Whether to validate the response against the expected schema.

    References:
      - [Bit2Me API docs](https://api.bit2me.com/doc#tag/account/PUT/v1/account/users/person/identity)
    """
    return await self.authed_request(
      'put',
      '/v1/account/users/person/identity',
      json=update_person_identity_request,
      validator=validate_response,
      validate=validate,
    )
