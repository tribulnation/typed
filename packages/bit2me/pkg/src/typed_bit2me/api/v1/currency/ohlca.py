# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bit2me.core import TimestampIso, timestamp_iso
from typed_bit2me.core.endpoint import RpcEndpoint
from typed_core.validation import validator


class CurrencyOhlcResponse(TypedDict):
  open: str
  """Opening price over the timeframe."""
  high: str
  """Highest price over the timeframe."""
  low: str
  """Lowest price over the timeframe."""
  close: str
  """Closing price over the timeframe."""
  average: str
  """Average price over the timeframe."""


validate_response = validator(CurrencyOhlcResponse)


class Ohlca(RpcEndpoint):
  async def ohlca(
    self,
    symbol: str,
    *,
    time: TimestampIso | None = None,
    timeframe: Literal['1H', '4H', '12H', '1D', '1W', '1M', '1Y'],
    validate: bool | None = None,
  ) -> CurrencyOhlcResponse:
    """Returns OHLCA for the given symbol

    Args:
      symbol: Currency symbol to fetch OHLCA data for.
      time: Reference date-time (ISO format). Defaults to the current time when omitted.
      timeframe: Lookback window the OHLCA figures are computed over, ending at `time`.
      validate: Whether to validate the response against the expected schema.

    References:
      - [Bit2Me API docs](https://api.bit2me.com/doc#tag/market/GET/v1/currency/ohlca/{symbol})
    """
    params: dict = {
      'timeframe': timeframe,
    }
    if time is not None:
      params['time'] = timestamp_iso.dump(time)
    return await self.authed_request(
      'GET',
      f'/v1/currency/ohlca/{symbol}',
      params=params,
      validator=validate_response,
      validate=validate,
    )
