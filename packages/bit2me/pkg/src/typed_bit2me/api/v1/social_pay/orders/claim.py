# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bit2me.types import BooleanResultResponse
from typed_bit2me.core.endpoint import RpcEndpoint
from typed_core.validation import validator


class Entry(TypedDict):
  pocketId: str
  orderId: str


validate_response = validator(BooleanResultResponse)


class Claim(RpcEndpoint):
  async def claim(
    self,
    list_entry: list[Entry],
    *,
    validate: bool | None = None,
  ) -> BooleanResultResponse:
    """Claim user pay orders

    Args:
      list_entry: The pending social-pay orders to claim, each identified by the receiving pocket and the order.
      validate: Whether to validate the response against the expected schema.

    References:
      - [Bit2Me API docs](https://api.bit2me.com/doc#tag/transfers/POST/v1/social-pay/order/claim)
    """
    return await self.authed_request(
      'POST',
      '/v1/social-pay/order/claim',
      json=list_entry,
      validator=validate_response,
      validate=validate,
    )
