# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bit2me.types import LoanAction
from typed_bit2me.core.endpoint import RpcEndpoint
from typed_core.validation import validator


class CreateLoanRequest(TypedDict):
  guaranteeCurrency: str
  """Currency of the collateral to pledge as loan guarantee."""
  guaranteeAmount: str
  """Amount of `guaranteeCurrency` to pledge as collateral."""
  loanCurrency: str
  """Currency to borrow."""
  loanAmount: str
  """Amount of `loanCurrency` to borrow."""


validate_response = validator(LoanAction)


class Create(RpcEndpoint):
  async def create(
    self,
    create_loan_request: CreateLoanRequest,
    *,
    validate: bool | None = None,
  ) -> LoanAction:
    """Create new loan

    Args:
      create_loan_request: New loan parameters: the guarantee currency/amount to pledge and the loan currency/amount to borrow.
      validate: Whether to validate the response against the expected schema.

    References:
      - [Bit2Me API docs](https://api.bit2me.com/doc#tag/loan/POST/v1/loan/orders)
    """
    return await self.authed_request(
      'POST',
      '/v1/loan/orders',
      json=create_loan_request,
      validator=validate_response,
      validate=validate,
    )
