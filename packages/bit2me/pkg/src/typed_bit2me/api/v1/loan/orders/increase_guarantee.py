# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bit2me.types import LoanAction
from typed_bit2me.core.endpoint import RpcEndpoint
from typed_core.validation import validator


class IncreaseGuaranteeRequest(TypedDict):
  guaranteeAmount: str
  """Additional amount of the loan order's guarantee currency to pledge as collateral."""


validate_response = validator(LoanAction)


class IncreaseGuarantee(RpcEndpoint):
  async def increase_guarantee(
    self,
    order_id: str,
    increase_guarantee_request: IncreaseGuaranteeRequest,
    *,
    validate: bool | None = None,
  ) -> LoanAction:
    """Increase a loan order guarantee

    Args:
      order_id: Identifier of the loan order to increase the guarantee of.
      increase_guarantee_request: Additional guarantee amount to pledge on the loan order.
      validate: Whether to validate the response against the expected schema.

    References:
      - [Bit2Me API docs](https://api.bit2me.com/doc#tag/loan/POST/v1/loan/orders/{orderId}/guarantee/increase)
    """
    return await self.authed_request(
      'POST',
      f'/v1/loan/orders/{order_id}/guarantee/increase',
      json=increase_guarantee_request,
      validator=validate_response,
      validate=validate,
    )
