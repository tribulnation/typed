# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, TypedDict
from typed_bit2me.core.endpoint.socket import SocketEndpoint


class CurrencyBalance(TypedDict):
  """Balance held in one currency."""

  currency: str
  """Currency symbol."""
  balance: float
  """Balance available to trade."""
  blockedBalance: float
  """Balance reserved by resting orders."""


class MyBalanceUpdate(TypedDict):
  """The authenticated user's balances after one of them changed."""

  userId: str
  """Owner of the balances."""
  balance: list[CurrencyBalance]
  """One entry per currency the user holds."""


class MyBalance(SocketEndpoint):
  """Stream the authenticated user's balances whenever one of them changes."""

  def my_balance(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[MyBalanceUpdate, Any, Any]:
    """Stream the authenticated user's balances whenever one of them changes.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/trading-spot-websockets)
    """
    return self.subscribe(
      'my-balance',
      None,
      validate=validate,
      response_type=MyBalanceUpdate,
    )
