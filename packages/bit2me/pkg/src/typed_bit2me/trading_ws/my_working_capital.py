# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_bit2me.core.endpoint.socket import SocketEndpoint


class WorkingCapitalLimit(TypedDict):
  """Working-capital limit and rolling 24-hour usage for one currency."""

  currency: NotRequired[str]
  """Currency this working-capital limit applies to."""
  type: NotRequired[Literal['deposit']]
  """Working capital limit type."""
  limit: NotRequired[float]
  """Maximum working capital amount allowed in the rolling 24-hour window."""
  used: NotRequired[float]
  """Working capital amount used in the last 24 hours."""
  remaining: NotRequired[float]
  """Remaining working capital amount available in the rolling 24-hour window."""
  refillThreshold: NotRequired[float]
  """Minimum balance amount that triggers an automatic working capital refill."""


class MyWorkingCapitalUpdate(TypedDict):
  """The authenticated user's working-capital limits after one of them changed."""

  userId: str
  """Owner of the working-capital limits."""
  workingCapital: list[WorkingCapitalLimit]
  """One entry per currency with a configured working-capital limit."""


class MyWorkingCapital(SocketEndpoint):
  """Stream the authenticated user's working-capital limits whenever one of them changes."""

  def my_working_capital(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[MyWorkingCapitalUpdate, Any, Any]:
    """Stream the authenticated user's working-capital limits whenever one of them changes.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/trading-spot-websockets)
    """
    return self.subscribe(
      'my-working-capital',
      None,
      validate=validate,
      response_type=MyWorkingCapitalUpdate,
    )
