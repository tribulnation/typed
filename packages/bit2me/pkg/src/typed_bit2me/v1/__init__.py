# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .account import Account
from .blockchain_manager import BlockchainManager
from .currency import Currency
from .earn import Earn
from .loan import Loan
from .misc import Misc
from .signin import Signin
from .social_pay import SocialPay
from .teller import Teller
from .trading import Trading
from .verifier import Verifier
from .wallet import Wallet
from typed_bit2me.core.endpoint.rpc import RpcEndpoint


class V1(RpcEndpoint):
  """Bit2Me's REST API v1: the original, broadest surface -- account and KYC, wallet, Trading Spot, loan, earn, Teller fiat funding, Social Pay, miscellaneous reference data, blockchain-manager travel-rule orders, and Embed integration callbacks.

  References:
    - [Upstream docs](https://api.bit2me.com/doc)
  """

  @cached_property
  def account(self) -> Account:
    """Bit2Me's user account API: profile retrieval, update and deletion, linked crypto/fiat addresses, sub-account management (including two-factor enrollment), KYC identity verification, and the reference catalogs (employment status, funds origin, professions, purposes) backing onboarding forms.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/account)
    """
    return Account(client=self.client)

  @cached_property
  def blockchain_manager(self) -> BlockchainManager:
    """Travel Rule compliance orders accompanying on-chain transfers.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/funding)
    """
    return BlockchainManager(client=self.client)

  @cached_property
  def currency(self) -> Currency:
    """Bit2Me's v1 market data API: currency conversion quotes, OHLCV candles, spot prices, and pairwise exchange rates.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/market)
    """
    return Currency(client=self.client)

  @cached_property
  def earn(self) -> Earn:
    """Bit2Me Earn: staking/yield wallets, their reward configuration, and the deposit/withdrawal movements and summaries recorded against them.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/earn)
    """
    return Earn(client=self.client)

  @cached_property
  def loan(self) -> Loan:
    """Bit2Me Loan: crypto-backed loan orders (creation, guarantee top-ups, payback), their movement history, and live loan-to-value (LTV) ratios.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/loan)
    """
    return Loan(client=self.client)

  @cached_property
  def misc(self) -> Misc:
    """Reference/catalog data with no other home: supported countries and their regions.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/misc)
    """
    return Misc(client=self.client)

  @cached_property
  def signin(self) -> Signin:
    """Authentication entry points for Bit2Me's Embed integration: exchanging API-key credentials and embed session tokens for the tokens the rest of the API and the embed widget need.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/access)
    """
    return Signin(client=self.client)

  @cached_property
  def social_pay(self) -> SocialPay:
    """Bit2Me Social Pay: creating, listing, and claiming peer-to-peer crypto payment orders shared via link.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/transfers)
    """
    return SocialPay(client=self.client)

  @cached_property
  def teller(self) -> Teller:
    """Bit2Me Teller: fiat on/off-ramp orders (preview, execute, cancel, status), the IBANs used to settle them, and teller-scoped wallet pockets.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/funding)
    """
    return Teller(client=self.client)

  @cached_property
  def trading(self) -> Trading:
    """Bit2Me's Trading Spot REST API: order placement and lifecycle, market data (candles, market configuration, trades), the trading wallet's balance and funding movements, and working-capital limits.

    References:
      - [Upstream docs](https://api.bit2me.com/trading-spot-rest)
    """
    return Trading(client=self.client)

  @cached_property
  def verifier(self) -> Verifier:
    """KYC document verification for sub-accounts.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/kyc)
    """
    return Verifier(client=self.client)

  @cached_property
  def wallet(self) -> Wallet:
    """Bit2Me's core crypto wallet API: wallet pockets (per-asset sub-balances), wallet-wide settings, sending/receiving transactions including their preview and history, and the blockchain networks a currency or the wallet as a whole supports.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/wallet)
    """
    return Wallet(client=self.client)
