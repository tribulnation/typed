# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bit2me.core import TimestampIso
from typed_bit2me.core.endpoint.rpc import RpcEndpoint


class CurrencyOhlcResponse(TypedDict):
  open: str
  """Opening price over the timeframe."""
  high: str
  """Highest price over the timeframe."""
  low: str
  """Lowest price over the timeframe."""
  close: str
  """Closing price over the timeframe."""
  average: str
  """Average price over the timeframe."""


class Request(TypedDict):
  symbol: str
  """Currency symbol to fetch OHLCA data for."""
  time: NotRequired[TimestampIso]
  """Datetime (ISO format)"""
  timeframe: Literal['1H', '4H', '12H', '1D', '1W', '1M', '1Y']
  """Timeframe"""


class Ohlca(RpcEndpoint):
  """Returns OHLCA for the given symbol"""

  async def ohlca(
    self,
    symbol: str,
    *,
    time: TimestampIso | None = None,
    timeframe: Literal['1H', '4H', '12H', '1D', '1W', '1M', '1Y'],
    validate: bool | None = None,
  ) -> CurrencyOhlcResponse:
    """Returns OHLCA for the given symbol

    Args:
      symbol: Currency symbol to fetch OHLCA data for.
      time: Datetime (ISO format)
      timeframe: Timeframe
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/market/GET/v1/currency/ohlca/{symbol})
    """
    request: Request = Request(symbol=symbol, timeframe=timeframe)
    if time is not None:
      request['time'] = time
    return await self.request(
      request,
      method='GET',
      path='/v1/currency/ohlca/{symbol}',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=CurrencyOhlcResponse,
    )
