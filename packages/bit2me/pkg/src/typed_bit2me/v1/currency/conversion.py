# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint


CurrencyConvertResponse = list[float]


RequestKeywords = TypedDict('RequestKeywords', {'from': str})
"""
- `from`: Comma delimited list of base currencies (can be just one in order to use the same currency base for all convertions)
"""


class Request(RequestKeywords):
  to: str
  """The target currency"""
  value: str
  """Comma delimited list of values (number of values must be equal to number of times)"""
  time: str
  """Comma delimited list of times (ISO 8601 or epoch) (number of times must be equal to number of values)"""


class Conversion(RpcEndpoint):
  """Convert the specified amounts to the specified currency"""

  async def conversion(
    self,
    *,
    from_: str,
    to: str,
    value: str,
    time: str,
    validate: bool | None = None,
  ) -> CurrencyConvertResponse:
    """Convert the specified amounts to the specified currency

    Args:
      from_: Comma delimited list of base currencies (can be just one in order to use the same currency base for all convertions)
      to: The target currency
      value: Comma delimited list of values (number of values must be equal to number of times)
      time: Comma delimited list of times (ISO 8601 or epoch) (number of times must be equal to number of values)
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/market/GET/v1/currency/convert)
    """
    request: Request = {'from': from_, 'to': to, 'value': value, 'time': time}
    return await self.request(
      request,
      method='GET',
      path='/v1/currency/convert',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=CurrencyConvertResponse,
    )
