# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint


class Entry(TypedDict):
  fiat: dict[str, float]
  """Exchange rate (in USD) for each supported fiat currency, keyed by ISO 4217 code."""
  crypto: dict[str, float]
  """Exchange rate (in USD) for each supported crypto asset, keyed by asset symbol."""


class Request(TypedDict):
  type: NotRequired[Literal['all', 'fiat', 'crypto']]
  """Currency type"""
  time: str
  """Comma delimited list of times (ISO 8601 or epoch). If this parameter is not specifed, the current time is used"""


CurrencyRateResponse = list[Entry]


class Rates(RpcEndpoint):
  """Return all supported exchange rates in USD."""

  async def rates(
    self,
    type: Literal['all', 'fiat', 'crypto'] | None = None,
    *,
    time: str,
    validate: bool | None = None,
  ) -> CurrencyRateResponse:
    """Return all supported exchange rates in USD.

    Args:
      type: Currency type
      time: Comma delimited list of times (ISO 8601 or epoch). If this parameter is not specifed, the current time is used
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/market/GET/v1/currency/rate)
    """
    request: Request = Request(time=time)
    if type is not None:
      request['type'] = type
    return await self.request(
      request,
      method='GET',
      path='/v1/currency/rate',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=CurrencyRateResponse,
    )
