# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  orderId: str
  """Identifier of the Teller order to cancel."""
  description: str
  """Reason for the cancellation."""


class Cancel(RpcEndpoint):
  """Changes an order status from "waiting-user" to "cancelled" after the user accepting the transaction"""

  async def cancel(
    self, *, order_id: str, description: str, validate: bool | None = None
  ):
    """Changes an order status from "waiting-user" to "cancelled" after the user accepting the transaction

    Args:
      order_id: Identifier of the Teller order to cancel.
      description: Reason for the cancellation.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/funding/POST/v1/teller/order/cancel)
    """
    return await self.request(
      Request(orderId=order_id, description=description),
      method='POST',
      path='/v1/teller/order/cancel',
      meta={},
      validate=validate,
      request_type=Request,
    )
