# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  id: str
  """The order ID or the wallet proforma ID"""


class TellerOrderStatusResponse(TypedDict):
  status: NotRequired[Literal['accepted', 'pending']]
  """The status of the order (accepted or pending)"""


class GetStatus(RpcEndpoint):
  """Gets order status. If no order is found, "pending" status is returned as default value."""

  async def get_status(
    self,
    id: str,
    *,
    validate: bool | None = None,
  ) -> TellerOrderStatusResponse:
    """Gets order status. If no order is found, "pending" status is returned as default value.

    Args:
      id: The order ID or the wallet proforma ID
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/funding/GET/v1/teller/order/status)
    """
    return await self.request(
      Request(id=id),
      method='GET',
      path='/v1/teller/order/status',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=TellerOrderStatusResponse,
    )
