# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint
from typed_bit2me.schemas import LoanAction


class Request(TypedDict):
  guaranteeAmount: str
  """Additional amount of the loan order's guarantee currency to pledge as collateral."""
  orderId: str
  """Identifier of the loan order to increase the guarantee of."""


class IncreaseGuarantee(RpcEndpoint):
  """Increase a loan order guarantee"""

  async def increase_guarantee(
    self,
    *,
    guarantee_amount: str,
    order_id: str,
    validate: bool | None = None,
  ) -> LoanAction:
    """Increase a loan order guarantee

    Args:
      guarantee_amount: Additional amount of the loan order's guarantee currency to pledge as collateral.
      order_id: Identifier of the loan order to increase the guarantee of.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/loan/POST/v1/loan/orders/{orderId}/guarantee/increase)
    """
    return await self.request(
      Request(guaranteeAmount=guarantee_amount, orderId=order_id),
      method='POST',
      path='/v1/loan/orders/{orderId}/guarantee/increase',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=LoanAction,
    )
