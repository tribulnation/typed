# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint
from typed_bit2me.schemas import LoanAction


class Request(TypedDict):
  paybackAmount: str
  """Amount of the loan order's loan currency to pay back."""
  orderId: str
  """Identifier of the loan order to pay back."""


class Payback(RpcEndpoint):
  """Process a payment to refund a specified amount of an loan. This action will complete the loan if the outstanding amount is fully paid."""

  async def payback(
    self,
    *,
    payback_amount: str,
    order_id: str,
    validate: bool | None = None,
  ) -> LoanAction:
    """Process a payment to refund a specified amount of an loan. This action will complete the loan if the outstanding amount is fully paid.

    Args:
      payback_amount: Amount of the loan order's loan currency to pay back.
      order_id: Identifier of the loan order to pay back.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/loan/POST/v1/loan/orders/{orderId}/payback)
    """
    return await self.request(
      Request(paybackAmount=payback_amount, orderId=order_id),
      method='POST',
      path='/v1/loan/orders/{orderId}/payback',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=LoanAction,
    )
