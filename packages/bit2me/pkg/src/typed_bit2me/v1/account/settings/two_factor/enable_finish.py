# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint
from typed_bit2me.schemas import BooleanResultResponse


class Request(TypedDict):
  totp: str
  """Current one-time password from the authenticator app, confirming enrollment."""
  sessionId: NotRequired[str]
  """Session id returned by the enrollment-start step."""
  alwaysRequired: NotRequired[bool]
  """Whether two-factor authentication is required on every login, not just sensitive actions."""


class EnableFinish(RpcEndpoint):
  """Finish the 2FA enablement flow by confirming that the client and server TOTP values match."""

  async def enable_finish(
    self,
    *,
    totp: str,
    session_id: str | None = None,
    always_required: bool | None = None,
    validate: bool | None = None,
  ) -> BooleanResultResponse:
    """Finish the 2FA enablement flow by confirming that the client and server TOTP values match.

    Args:
      totp: Current one-time password from the authenticator app, confirming enrollment.
      session_id: Session id returned by the enrollment-start step.
      always_required: Whether two-factor authentication is required on every login, not just sensitive actions.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/account/PUT/v1/account/settings/sfa)
    """
    request: Request = Request(totp=totp)
    if session_id is not None:
      request['sessionId'] = session_id
    if always_required is not None:
      request['alwaysRequired'] = always_required
    return await self.request(
      request,
      method='put',
      path='/v1/account/settings/sfa',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=BooleanResultResponse,
    )
