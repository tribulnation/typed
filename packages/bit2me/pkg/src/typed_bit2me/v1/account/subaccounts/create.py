# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint
from typed_bit2me.schemas import Phone


class CreateSubaccountResponse(TypedDict):
  userId: str
  """Unique identifier of the newly created subaccount."""


class Request(TypedDict):
  email: str
  """Email address for the new subaccount."""
  name: str
  """First name of the subaccount holder."""
  surname: str
  """Last name of the subaccount holder."""
  phone: Phone


class Create(RpcEndpoint):
  """Create a subaccount."""

  async def create(
    self,
    *,
    email: str,
    name: str,
    surname: str,
    phone: Phone,
    validate: bool | None = None,
  ) -> CreateSubaccountResponse:
    """Create a subaccount.

    Args:
      email: Email address for the new subaccount.
      name: First name of the subaccount holder.
      surname: Last name of the subaccount holder.
      phone:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/account/POST/v1/account/subaccount)
    """
    return await self.request(
      Request(email=email, name=name, surname=surname, phone=phone),
      method='POST',
      path='/v1/account/subaccount',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=CreateSubaccountResponse,
    )
