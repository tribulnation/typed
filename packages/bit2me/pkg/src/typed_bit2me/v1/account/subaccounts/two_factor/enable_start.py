# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint


class InitSubaccountTwoFactorResponse(TypedDict):
  secret: str
  """TOTP secret for the subaccount to register in a Google Authenticator-compatible app to finish enabling 2FA."""


class Request(TypedDict):
  subaccountUserId: str
  """Identifier of the subaccount enrolling in two-factor authentication."""


class EnableStart(RpcEndpoint):
  """Start the Google Authenticator 2FA enablement flow for a subaccount."""

  async def enable_start(
    self,
    subaccount_user_id: str,
    *,
    validate: bool | None = None,
  ) -> InitSubaccountTwoFactorResponse:
    """Start the Google Authenticator 2FA enablement flow for a subaccount.

    Args:
      subaccount_user_id: Identifier of the subaccount enrolling in two-factor authentication.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/account/POST/v1/account/subaccount/sfa)
    """
    return await self.request(
      Request(subaccountUserId=subaccount_user_id),
      method='POST',
      path='/v1/account/subaccount/sfa',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=InitSubaccountTwoFactorResponse,
    )
