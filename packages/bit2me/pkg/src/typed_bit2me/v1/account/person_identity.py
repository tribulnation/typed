# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bit2me.core import TimestampIso
from typed_bit2me.core.endpoint.rpc import RpcEndpoint
from typed_bit2me.schemas import BooleanResultResponse


class Request(TypedDict):
  type: Literal['none', 'idcard', 'passport', 'drivinglicense', 'visa']
  """Identity document type."""
  number: str
  """Identity document number."""
  expiryDate: TimestampIso
  """When the identity document expires."""
  countryCode: str
  """ISO 3166-1 alpha-2 country code that issued the identity document."""


class PersonIdentity(RpcEndpoint):
  """Update user person identity"""

  async def person_identity(
    self,
    type: Literal['none', 'idcard', 'passport', 'drivinglicense', 'visa'],
    *,
    number: str,
    expiry_date: TimestampIso,
    country_code: str,
    validate: bool | None = None,
  ) -> BooleanResultResponse:
    """Update user person identity

    Args:
      type: Identity document type.
      number: Identity document number.
      expiry_date: When the identity document expires.
      country_code: ISO 3166-1 alpha-2 country code that issued the identity document.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/account/PUT/v1/account/users/person/identity)
    """
    return await self.request(
      Request(
        type=type, number=number, expiryDate=expiry_date, countryCode=country_code
      ),
      method='put',
      path='/v1/account/users/person/identity',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=BooleanResultResponse,
    )
