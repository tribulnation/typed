# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint


class DepositEstimationTier(TypedDict):
  id: str
  """Unique identifier of this deposit estimation tier."""
  minimum: int
  """Minimum deposit amount for this tier."""
  maximum: int
  """Maximum deposit amount for this tier."""


class DepositEstimationResponse(TypedDict):
  total: int
  """Total number of deposit estimation tiers returned."""
  data: list[DepositEstimationTier]
  """Deposit estimation tiers, each a minimum/maximum deposit amount band."""


class DepositEstimations(RpcEndpoint):
  """Return all deposit estimates."""

  async def deposit_estimations(
    self,
    *,
    validate: bool | None = None,
  ) -> DepositEstimationResponse:
    """Return all deposit estimates.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/account/GET/v1/account/deposit-estimation)
    """
    return await self.request(
      None,
      method='GET',
      path='/v1/account/deposit-estimation',
      meta={},
      validate=validate,
      response_type=DepositEstimationResponse,
    )
