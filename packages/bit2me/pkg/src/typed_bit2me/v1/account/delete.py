# Generated by typed-dev — see CONTRIBUTING.md.
from typed_bit2me.core.endpoint.rpc import RpcEndpoint


class Delete(RpcEndpoint):
  """Delete user account"""

  async def delete(self, *, validate: bool | None = None) -> None:
    """Delete user account

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/account/DELETE/v1/account)
    """
    return await self.request(
      None,
      method='DELETE',
      path='/v1/account',
      meta={},
      validate=validate,
      response_type=None,
    )
