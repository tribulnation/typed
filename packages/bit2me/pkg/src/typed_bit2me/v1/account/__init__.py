# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .addresses import Addresses
from .delete import Delete
from .deposit_estimations import DepositEstimations
from .employment_statuses import EmploymentStatuses
from .funds_origins import FundsOrigins
from .get import Get
from .identity_verification import IdentityVerification
from .person_identity import PersonIdentity
from .professions import Professions
from .purposes import Purposes
from .settings import Settings
from .subaccounts import Subaccounts
from .update import Update


class Account(
  Delete,
  DepositEstimations,
  EmploymentStatuses,
  FundsOrigins,
  Get,
  PersonIdentity,
  Professions,
  Purposes,
  Update,
):
  """Bit2Me's user account API: profile retrieval, update and deletion, linked crypto/fiat addresses, sub-account management (including two-factor enrollment), KYC identity verification, and the reference catalogs (employment status, funds origin, professions, purposes) backing onboarding forms.

  References:
    - [Upstream docs](https://api.bit2me.com/doc#tag/account)
  """

  @cached_property
  def addresses(self) -> Addresses:
    """Registering and removing an account's linked crypto/fiat addresses.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/account)
    """
    return Addresses(client=self.client)

  @cached_property
  def identity_verification(self) -> IdentityVerification:
    """Starting the account's identity verification, checking its status, and retrying a failed attempt.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/kyc)
    """
    return IdentityVerification(client=self.client)

  @cached_property
  def settings(self) -> Settings:
    """Account-wide settings -- currently two-factor authentication (SFA) enrollment.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/account)
    """
    return Settings(client=self.client)

  @cached_property
  def subaccounts(self) -> Subaccounts:
    """Listing and creating sub-accounts, including their own two-factor enrollment.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/account)
    """
    return Subaccounts(client=self.client)
