# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint


class IdentityVerificationRetryEligibilityResponse(TypedDict):
  result: bool
  """Whether the user is currently allowed to retry the identity verification process."""


class GetRetryWindow(RpcEndpoint):
  """Gets a response based on whether the user can retry the verification process"""

  async def get_retry_window(
    self,
    *,
    validate: bool | None = None,
  ) -> IdentityVerificationRetryEligibilityResponse:
    """Gets a response based on whether the user can retry the verification process

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/kyc/GET/v1/account/verify/identity/verification/retry)
    """
    return await self.request(
      None,
      method='GET',
      path='/v1/account/verify/identity/verification/retry',
      meta={},
      validate=validate,
      response_type=IdentityVerificationRetryEligibilityResponse,
    )
