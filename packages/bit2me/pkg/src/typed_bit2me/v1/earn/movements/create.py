# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint


class CreateEarnMovementResponse(TypedDict):
  walletMovementId: str
  """Identifier of the wallet-level ledger entry recorded for this movement."""
  movementId: str
  """Identifier of the created Earn movement."""


class Request(TypedDict):
  currency: str
  """Valid currency symbol"""
  amount: str
  """Amount of `currency` to deposit or withdraw."""
  type: Literal['deposit', 'withdrawal']
  """Whether this movement is a deposit into, or a withdrawal from, Earn."""
  lockPeriod: NotRequired[str]
  """Identifier of the lock period to deposit under, for a locked Earn product."""
  withholdingAmount: NotRequired[str]
  """Amount of `currency` withheld (e.g. for tax purposes) from the movement."""
  termsAndConditions: NotRequired[str]
  """Identifier of the terms and conditions the user accepted for this movement."""


class Create(RpcEndpoint):
  """Create a deposit or withdrawal movement in Earn."""

  async def create(
    self,
    *,
    currency: str,
    amount: str,
    type: Literal['deposit', 'withdrawal'],
    lock_period: str | None = None,
    withholding_amount: str | None = None,
    terms_and_conditions: str | None = None,
    validate: bool | None = None,
  ) -> CreateEarnMovementResponse:
    """Create a deposit or withdrawal movement in Earn.

    Args:
      currency: Valid currency symbol
      amount: Amount of `currency` to deposit or withdraw.
      type: Whether this movement is a deposit into, or a withdrawal from, Earn.
      lock_period: Identifier of the lock period to deposit under, for a locked Earn product.
      withholding_amount: Amount of `currency` withheld (e.g. for tax purposes) from the movement.
      terms_and_conditions: Identifier of the terms and conditions the user accepted for this movement.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/earn/POST/v1/earn/movements)
    """
    request: Request = Request(currency=currency, amount=amount, type=type)
    if lock_period is not None:
      request['lockPeriod'] = lock_period
    if withholding_amount is not None:
      request['withholdingAmount'] = withholding_amount
    if terms_and_conditions is not None:
      request['termsAndConditions'] = terms_and_conditions
    return await self.request(
      request,
      method='POST',
      path='/v1/earn/movements',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=CreateEarnMovementResponse,
    )
