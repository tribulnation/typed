# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint


class EarnUserSummaryResponse(TypedDict):
  currency: str
  """Currency all balances and rewards in this summary are expressed in (the requested `userCurrency`, or the account default)."""
  totalBalance: float
  """Total balance across all Earn wallets, converted to `currency`."""
  totalRewards: float
  """Total accumulated rewards across all Earn wallets, converted to `currency`."""


class Request(TypedDict):
  userCurrency: NotRequired[str]
  """Valid currency symbol"""


class Summary(RpcEndpoint):
  """Retrieves user summary"""

  async def summary(
    self,
    user_currency: str | None = None,
    *,
    validate: bool | None = None,
  ) -> EarnUserSummaryResponse:
    """Retrieves user summary

    Args:
      user_currency: Valid currency symbol
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/earn/GET/v1/earn/summary)
    """
    request: Request = Request()
    if user_currency is not None:
      request['userCurrency'] = user_currency
    return await self.request(
      request,
      method='GET',
      path='/v1/earn/summary',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=EarnUserSummaryResponse,
    )
