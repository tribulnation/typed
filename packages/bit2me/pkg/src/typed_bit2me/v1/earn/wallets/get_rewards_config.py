# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint
from typed_bit2me.schemas import EarnRewardsConfigResponse


class Request(TypedDict):
  walletId: str
  """Wallet identifier"""


class GetRewardsConfig(RpcEndpoint):
  """Retrieves rewards configuration for selected wallet"""

  async def get_rewards_config(
    self,
    wallet_id: str,
    *,
    validate: bool | None = None,
  ) -> EarnRewardsConfigResponse:
    """Retrieves rewards configuration for selected wallet

    Args:
      wallet_id: Wallet identifier
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/earn/GET/v1/earn/wallets/{walletId}/rewards/config)
    """
    return await self.request(
      Request(walletId=wallet_id),
      method='GET',
      path='/v1/earn/wallets/{walletId}/rewards/config',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=EarnRewardsConfigResponse,
    )
