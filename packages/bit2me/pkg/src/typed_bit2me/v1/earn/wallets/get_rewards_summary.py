# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint


class AccumulatedReward(TypedDict):
  currency: NotRequired[str]
  """Currency of the accumulated reward amount."""
  amount: NotRequired[str]
  """Reward amount accumulated in this currency."""


class Request(TypedDict):
  walletId: str
  """Wallet identifier"""
  userCurrency: NotRequired[str]
  """Valid currency symbol"""


class TotalConvertedReward(TypedDict):
  """Sum of all accumulated rewards, converted into a single currency."""

  amount: NotRequired[str]
  """Converted total reward amount."""
  currency: NotRequired[str]
  """Currency the total reward was converted to (the requested `userCurrency`, or the account default)."""


class WalletRewardsSummaryResponse(TypedDict):
  accumulatedRewards: NotRequired[list[AccumulatedReward]]
  """Rewards accumulated for the wallet, broken down by currency."""
  totalConvertedReward: NotRequired[TotalConvertedReward]


class GetRewardsSummary(RpcEndpoint):
  """Retrieves full list of rewards summary for selected wallet"""

  async def get_rewards_summary(
    self,
    *,
    wallet_id: str,
    user_currency: str | None = None,
    validate: bool | None = None,
  ) -> WalletRewardsSummaryResponse:
    """Retrieves full list of rewards summary for selected wallet

    Args:
      wallet_id: Wallet identifier
      user_currency: Valid currency symbol
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/earn/GET/v1/earn/wallets/{walletId}/rewards/summary)
    """
    request: Request = Request(walletId=wallet_id)
    if user_currency is not None:
      request['userCurrency'] = user_currency
    return await self.request(
      request,
      method='GET',
      path='/v1/earn/wallets/{walletId}/rewards/summary',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=WalletRewardsSummaryResponse,
    )
