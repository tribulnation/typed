# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  rewardCurrency: str
  """Currency to obtain the reward"""
  walletId: str
  """Wallet identifier"""


class UpdateEarnWalletRewardCurrencyResponse(TypedDict):
  result: bool
  """Whether the reward currency update succeeded."""


class UpdateRewardsConfig(RpcEndpoint):
  """Update wallet reward configuration for selected wallet"""

  async def update_rewards_config(
    self,
    *,
    reward_currency: str,
    wallet_id: str,
    validate: bool | None = None,
  ) -> UpdateEarnWalletRewardCurrencyResponse:
    """Update wallet reward configuration for selected wallet

    Args:
      reward_currency: Currency to obtain the reward
      wallet_id: Wallet identifier
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/earn/PATCH/v1/earn/wallets/{walletId}/rewards/config)
    """
    return await self.request(
      Request(rewardCurrency=reward_currency, walletId=wallet_id),
      method='PATCH',
      path='/v1/earn/wallets/{walletId}/rewards/config',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=UpdateEarnWalletRewardCurrencyResponse,
    )
