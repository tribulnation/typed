# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint
from typed_bit2me.schemas import EarnWalletRecord


class Request(TypedDict):
  walletId: str
  """Wallet identifier"""
  userCurrency: NotRequired[str]
  """Valid currency symbol"""


class Get(RpcEndpoint):
  """Retrieves full information of selected wallet"""

  async def get(
    self,
    *,
    wallet_id: str,
    user_currency: str | None = None,
    validate: bool | None = None,
  ) -> EarnWalletRecord:
    """Retrieves full information of selected wallet

    Args:
      wallet_id: Wallet identifier
      user_currency: Valid currency symbol
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/earn/GET/v1/earn/wallets/{walletId})
    """
    request: Request = Request(walletId=wallet_id)
    if user_currency is not None:
      request['userCurrency'] = user_currency
    return await self.request(
      request,
      method='GET',
      path='/v1/earn/wallets/{walletId}',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=EarnWalletRecord,
    )
