# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint
from typed_bit2me.schemas import BooleanResultResponse


class Entry(TypedDict):
  pocketId: str
  orderId: str


Request = list[Entry]


class Claim(RpcEndpoint):
  """Claim user pay orders"""

  async def claim(
    self,
    claim_social_pay_orders_request: Request,
    *,
    validate: bool | None = None,
  ) -> BooleanResultResponse:
    """Claim user pay orders

    Args:
      claim_social_pay_orders_request:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/transfers/POST/v1/social-pay/order/claim)
    """
    return await self.request(
      claim_social_pay_orders_request,
      method='POST',
      path='/v1/social-pay/order/claim',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=BooleanResultResponse,
    )
