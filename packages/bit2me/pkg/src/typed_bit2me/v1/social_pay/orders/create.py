# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint
from typed_bit2me.schemas import PayOrderData, PayOrderType, Phone


class Request(TypedDict):
  pocketId: str
  """Identifier of the wallet pocket the order pays from."""
  type: PayOrderType
  email: NotRequired[str]
  """Email address to share the payment order with."""
  phone: NotRequired[Phone]
  alias: NotRequired[str]
  """Alias to share the payment order with."""
  amount: str
  """Amount of `currency` to send."""
  currency: str
  """Valid currency symbol"""
  note: NotRequired[str]
  """Optional note attached to the order."""


class Create(RpcEndpoint):
  """Creates a new payment order"""

  async def create(
    self,
    *,
    pocket_id: str,
    type: PayOrderType,
    email: str | None = None,
    phone: Phone | None = None,
    alias: str | None = None,
    amount: str,
    currency: str,
    note: str | None = None,
    validate: bool | None = None,
  ) -> PayOrderData:
    """Creates a new payment order

    Args:
      pocket_id: Identifier of the wallet pocket the order pays from.
      type:
      email: Email address to share the payment order with.
      phone:
      alias: Alias to share the payment order with.
      amount: Amount of `currency` to send.
      currency: Valid currency symbol
      note: Optional note attached to the order.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/transfers/POST/v1/social-pay/order)
    """
    request: Request = Request(
      pocketId=pocket_id, type=type, amount=amount, currency=currency
    )
    if email is not None:
      request['email'] = email
    if phone is not None:
      request['phone'] = phone
    if alias is not None:
      request['alias'] = alias
    if note is not None:
      request['note'] = note
    return await self.request(
      request,
      method='POST',
      path='/v1/social-pay/order',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=PayOrderData,
    )
