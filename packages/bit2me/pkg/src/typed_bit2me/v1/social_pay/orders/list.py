# Generated by typed-dev — see CONTRIBUTING.md.
from typed_bit2me.core.endpoint.rpc import RpcEndpoint
from typed_bit2me.schemas import PayOrderData


SocialPayPendingOrdersResponse = list[PayOrderData]


class List(RpcEndpoint):
  """Get all pending pay order of a user"""

  async def list(
    self, *, validate: bool | None = None
  ) -> SocialPayPendingOrdersResponse:
    """Get all pending pay order of a user

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/transfers/GET/v1/social-pay/order)
    """
    return await self.request(
      None,
      method='GET',
      path='/v1/social-pay/order',
      meta={},
      validate=validate,
      response_type=SocialPayPendingOrdersResponse,
    )
