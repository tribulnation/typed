# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bit2me.core import TimestampSeconds
from typed_bit2me.core.endpoint.rpc import RpcEndpoint
from typed_bit2me.schemas import TokenResponse


class Request(TypedDict):
  accessToken: str
  """The Embed integrator's own access token to exchange."""


class Session(TypedDict):
  """The embed session opened by this sign-in."""

  id: str
  """Session identifier."""
  expirationTime: TimestampSeconds
  """Session expiration time, in Unix epoch seconds. (Documented upstream as milliseconds, but the recorded example's 10-digit value -- `1715087310` -- confirms seconds; see `notes`.)"""


class SignInEmbedResponse(TypedDict):
  accessToken: TokenResponse
  refreshToken: TokenResponse
  session: NotRequired[Session]


class Embed(RpcEndpoint):
  """Generate access and refresh tokens for the embed session and subsequent API calls."""

  async def embed(
    self,
    access_token: str,
    *,
    validate: bool | None = None,
  ) -> SignInEmbedResponse:
    """Generate access and refresh tokens for the embed session and subsequent API calls.

    Args:
      access_token: The Embed integrator's own access token to exchange.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/access/POST/v1/signin/embed)
    """
    return await self.request(
      Request(accessToken=access_token),
      method='POST',
      path='/v1/signin/embed',
      meta={'public': True},
      validate=validate,
      request_type=Request,
      response_type=SignInEmbedResponse,
    )
