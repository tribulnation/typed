# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint
from typed_bit2me.schemas import TokenResponse


class Request(TypedDict):
  userId: NotRequired[str]
  """Identifier of the user to sign in as. Omit to sign in as the API key's own owner."""


class SignInApiKeyResponse(TypedDict):
  accessToken: TokenResponse


class ApiKey(RpcEndpoint):
  """Authorize a user with an API key."""

  async def api_key(
    self,
    user_id: str | None = None,
    *,
    validate: bool | None = None,
  ) -> SignInApiKeyResponse:
    """Authorize a user with an API key.

    Args:
      user_id: Identifier of the user to sign in as. Omit to sign in as the API key's own owner.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/access/POST/v1/signin/apikey)
    """
    request: Request = Request()
    if user_id is not None:
      request['userId'] = user_id
    return await self.request(
      request,
      method='POST',
      path='/v1/signin/apikey',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=SignInApiKeyResponse,
    )
