# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  identityFileType: Literal[
    'backDocument',
    'frontDocument',
    'selfie',
    'livenessFrontDocument',
    'livenessBackDocument',
    'livenessSelfie',
  ]
  """The identity file type to download"""
  subAccountId: str
  """The identifier of the subaccount"""


Response = str


class Subaccounts(RpcEndpoint):
  """Download an identity file of a subaccount"""

  async def subaccounts(
    self,
    identity_file_type: Literal[
      'backDocument',
      'frontDocument',
      'selfie',
      'livenessFrontDocument',
      'livenessBackDocument',
      'livenessSelfie',
    ],
    *,
    sub_account_id: str,
    validate: bool | None = None,
  ) -> Response:
    """Download an identity file of a subaccount

    Args:
      identity_file_type: The identity file type to download
      sub_account_id: The identifier of the subaccount
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/kyc/GET/v1/verifier/subaccount/file)
    """
    return await self.request(
      Request(identityFileType=identity_file_type, subAccountId=sub_account_id),
      method='GET',
      path='/v1/verifier/subaccount/file',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
