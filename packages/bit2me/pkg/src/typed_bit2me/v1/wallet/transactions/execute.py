# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint
from typed_bit2me.schemas import CreatedResourceIdResponse


class Request(TypedDict):
  proforma: str
  """Identifier of the proforma transaction to execute."""
  concept: NotRequired[str]
  """Optional concept overriding the one set on the proforma."""
  note: NotRequired[str]
  """Optional note overriding the one set on the proforma."""


class Execute(RpcEndpoint):
  """Executes a previuosly created transaction"""

  async def execute(
    self,
    *,
    proforma: str,
    concept: str | None = None,
    note: str | None = None,
    validate: bool | None = None,
  ) -> CreatedResourceIdResponse:
    """Executes a previuosly created transaction

    Args:
      proforma: Identifier of the proforma transaction to execute.
      concept: Optional concept overriding the one set on the proforma.
      note: Optional note overriding the one set on the proforma.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/wallet/POST/v1/wallet/transaction)
    """
    request: Request = Request(proforma=proforma)
    if concept is not None:
      request['concept'] = concept
    if note is not None:
      request['note'] = note
    return await self.request(
      request,
      method='POST',
      path='/v1/wallet/transaction',
      meta={'two_factor': True},
      validate=validate,
      request_type=Request,
      response_type=CreatedResourceIdResponse,
    )
