# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint
from typed_bit2me.schemas import BooleanResultResponse


class Request(TypedDict):
  note: str
  """New note for the transaction."""
  id: str
  """The transaction id"""


class Update(RpcEndpoint):
  """Updates some data of the specified transaction"""

  async def update(
    self,
    *,
    note: str,
    id: str,
    validate: bool | None = None,
  ) -> BooleanResultResponse:
    """Updates some data of the specified transaction

    Args:
      note: New note for the transaction.
      id: The transaction id
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/wallet/PUT/v1/wallet/transaction/{id})
    """
    return await self.request(
      Request(note=note, id=id),
      method='put',
      path='/v1/wallet/transaction/{id}',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=BooleanResultResponse,
    )
