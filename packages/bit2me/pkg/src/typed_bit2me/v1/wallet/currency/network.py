# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint


class CurrencyNetwork(TypedDict):
  id: str
  """Bit2Me's internal network identifier (e.g. `ethereum`, `polygon`), matching `id` in `GET /v1/wallet/network`."""
  name: str
  """Human-readable network name."""
  nativeCurrencyCode: str
  """Symbol of this network's native gas/settlement currency."""
  feeCurrencyCode: str
  """Symbol of the currency network fees for this currency are actually charged in."""
  hasTag: bool
  """Whether a deposit or withdrawal on this network requires an address tag or memo (e.g. XRP Ledger's destination tag)."""


class Request(TypedDict):
  symbol: str
  """Currency symbol to list supported networks for, uppercase (e.g. `ETH`, `BTC`)."""


CurrencyNetworksResponse = list[CurrencyNetwork]


class Network(RpcEndpoint):
  """List the blockchain networks a currency can be deposited or withdrawn over."""

  async def network(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> CurrencyNetworksResponse:
    """List the blockchain networks a currency can be deposited or withdrawn over.

    Args:
      symbol: Currency symbol to list supported networks for, uppercase (e.g. `ETH`, `BTC`).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/wallet)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/v1/wallet/currency/{symbol}/network',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=CurrencyNetworksResponse,
    )
