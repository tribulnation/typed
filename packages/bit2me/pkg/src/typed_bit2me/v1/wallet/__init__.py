# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .currency import Currency
from .network import Network
from .pockets import Pockets
from .settings import Settings
from .transactions import Transactions


class Wallet(Network, Settings):
  """Bit2Me's core crypto wallet API: wallet pockets (per-asset sub-balances), wallet-wide settings, sending/receiving transactions including their preview and history, and the blockchain networks a currency or the wallet as a whole supports.

  References:
    - [Upstream docs](https://api.bit2me.com/doc#tag/wallet)
  """

  @cached_property
  def currency(self) -> Currency:
    """Per-currency network metadata for Bit2Me's wallet: which blockchain networks a given currency can be deposited or withdrawn over.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/wallet)
    """
    return Currency(client=self.client)

  @cached_property
  def pockets(self) -> Pockets:
    """Creating, listing, updating, and deleting wallet pockets.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/wallet)
    """
    return Pockets(client=self.client)

  @cached_property
  def transactions(self) -> Transactions:
    """Sending wallet transactions, previewing them, listing their history, and retrieving or updating a specific one.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/wallet)
    """
    return Transactions(client=self.client)
