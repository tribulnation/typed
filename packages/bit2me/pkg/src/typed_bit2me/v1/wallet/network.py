# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint


class WalletNetwork(TypedDict):
  id: str
  """Bit2Me's internal network identifier (e.g. `ethereum`, `polygon`)."""
  name: str
  """Human-readable network name."""
  nativeCurrencyCode: str
  """Symbol of this network's native gas/settlement currency."""
  feeCurrencyCode: str
  """Symbol of the currency network fees on this network are actually charged in."""
  hasTag: bool
  """Whether a deposit or withdrawal on this network requires an address tag or memo."""
  isCaseSensitive: bool
  """Whether deposit/withdrawal addresses on this network are case-sensitive."""
  isMainnet: bool
  """Whether this is the network's production mainnet, as opposed to a test network (e.g. Bitcoin Testnet)."""


WalletNetworksResponse = list[WalletNetwork]


class Network(RpcEndpoint):
  """List every blockchain network Bit2Me's wallet supports."""

  async def network(self, *, validate: bool | None = None) -> WalletNetworksResponse:
    """List every blockchain network Bit2Me's wallet supports.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/wallet)
    """
    return await self.request(
      None,
      method='GET',
      path='/v1/wallet/network',
      meta={'public': True},
      validate=validate,
      response_type=WalletNetworksResponse,
    )
