# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint


class CreatePocketResponse(TypedDict):
  id: str
  """Id of the newly created pocket."""


class Request(TypedDict):
  currency: str
  """Valid currency symbol"""
  name: str
  """Display name for the new pocket."""


class Create(RpcEndpoint):
  """Create a new pocket"""

  async def create(
    self,
    *,
    currency: str,
    name: str,
    validate: bool | None = None,
  ) -> CreatePocketResponse:
    """Create a new pocket

    Args:
      currency: Valid currency symbol
      name: Display name for the new pocket.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/wallet/POST/v1/wallet/pocket)
    """
    return await self.request(
      Request(currency=currency, name=name),
      method='POST',
      path='/v1/wallet/pocket',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=CreatePocketResponse,
    )
