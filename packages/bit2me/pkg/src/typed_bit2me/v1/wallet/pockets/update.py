# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint
from typed_bit2me.schemas import BooleanResultResponse, PocketColor


class Request(TypedDict):
  id: str
  """Identifier of the pocket to update."""
  name: NotRequired[str]
  """New display name for the pocket."""
  color: NotRequired[PocketColor]


class Update(RpcEndpoint):
  """Update pocket data"""

  async def update(
    self,
    *,
    id: str,
    name: str | None = None,
    color: PocketColor | None = None,
    validate: bool | None = None,
  ) -> BooleanResultResponse:
    """Update pocket data

    Args:
      id: Identifier of the pocket to update.
      name: New display name for the pocket.
      color:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/wallet/PUT/v1/wallet/pocket)
    """
    request: Request = Request(id=id)
    if name is not None:
      request['name'] = name
    if color is not None:
      request['color'] = color
    return await self.request(
      request,
      method='put',
      path='/v1/wallet/pocket',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=BooleanResultResponse,
    )
