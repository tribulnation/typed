# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_bit2me.core import TimestampIso
from typed_bit2me.core.endpoint.rpc import RpcEndpoint
from typed_bit2me.schemas import PocketColor


class Request(TypedDict):
  id: NotRequired[str]
  """With this parameter you can specify the pocket you want to get the data from.
If this parameter is not set, all pockets of the user are returned"""


class Entry(TypedDict):
  id: str
  """Pocket id."""
  name: str
  """Pocket name, as set by the user."""
  color: PocketColor
  currency: str
  """Valid currency symbol"""
  balance: Decimal
  """Available balance in the pocket."""
  blockedBalance: Decimal
  """Balance in the pocket that is currently blocked (e.g. reserved by a pending operation) and not available to spend."""
  createdAt: TimestampIso
  """When the pocket was created (ISO 8601)"""


ListWalletPocketsResponse = list[Entry]


class Get(RpcEndpoint):
  """Get the data of a specific pocket or the data of all pockets of the user (if the *id* param is not set)"""

  async def get(
    self,
    id: str | None = None,
    *,
    validate: bool | None = None,
  ) -> ListWalletPocketsResponse:
    """Get the data of a specific pocket or the data of all pockets of the user (if the *id* param is not set)

    Args:
      id: With this parameter you can specify the pocket you want to get the data from.
    If this parameter is not set, all pockets of the user are returned
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/wallet/GET/v1/wallet/pocket)
    """
    request: Request = Request()
    if id is not None:
      request['id'] = id
    return await self.request(
      request,
      method='GET',
      path='/v1/wallet/pocket',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=ListWalletPocketsResponse,
    )
