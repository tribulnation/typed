# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, NotRequired, TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint
from typed_bit2me.schemas import WalletResponse


class Request(TypedDict):
  symbols: NotRequired[Any]
  """Comma separated symbols of the wallets that you want to retrieve"""


TradingBalanceResponse = list[WalletResponse]


class Balance(RpcEndpoint):
  """Retrieve balances of all wallets and blocked balances in active orders."""

  async def balance(
    self,
    symbols: Any | None = None,
    *,
    validate: bool | None = None,
  ) -> TradingBalanceResponse:
    """Retrieve balances of all wallets and blocked balances in active orders.

    Args:
      symbols: Comma separated symbols of the wallets that you want to retrieve
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/trading-spot-rest#tag/funding/GET/v1/trading/wallet/balance)
    """
    request: Request = Request()
    if symbols is not None:
      request['symbols'] = symbols
    return await self.request(
      request,
      method='GET',
      path='/v1/trading/wallet/balance',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=TradingBalanceResponse,
    )
