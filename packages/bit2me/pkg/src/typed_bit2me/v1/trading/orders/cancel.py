# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint
from typed_bit2me.schemas import OrderResponse


class Request(TypedDict):
  id: str
  """Order identifier"""


class Cancel(RpcEndpoint):
  """Cancel the order by identifier if this order is open or inactive."""

  async def cancel(self, id: str, *, validate: bool | None = None) -> OrderResponse:
    """Cancel the order by identifier if this order is open or inactive.

    Args:
      id: Order identifier
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/trading-spot-rest#tag/trading/DELETE/v1/trading/order/{id})
    """
    return await self.request(
      Request(id=id),
      method='DELETE',
      path='/v1/trading/order/{id}',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=OrderResponse,
    )
