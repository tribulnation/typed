# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint
from typed_bit2me.schemas import TradeResponse


class Request(TypedDict):
  id: str
  """Order identifier"""


TradingOrderTradesResponse = list[TradeResponse]


class ListTrades(RpcEndpoint):
  """Get all trades associated to the order."""

  async def list_trades(
    self,
    id: str,
    *,
    validate: bool | None = None,
  ) -> TradingOrderTradesResponse:
    """Get all trades associated to the order.

    Args:
      id: Order identifier
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/trading-spot-rest#tag/trading/GET/v1/trading/order/{id}/trades)
    """
    return await self.request(
      Request(id=id),
      method='GET',
      path='/v1/trading/order/{id}/trades',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=TradingOrderTradesResponse,
    )
