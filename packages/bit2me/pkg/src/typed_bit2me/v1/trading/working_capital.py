# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  symbols: NotRequired[str]
  """Comma-separated currencies to retrieve working-capital limits for, for example `BTC,EUR`. Omit to retrieve all."""


class WorkingCapitalLimit(TypedDict):
  """Working-capital limit and rolling 24-hour usage for one currency."""

  currency: NotRequired[str]
  """Currency this working-capital limit applies to."""
  type: NotRequired[Literal['deposit']]
  """Working capital limit type."""
  limit: NotRequired[float]
  """Maximum working capital amount allowed in the rolling 24-hour window."""
  used: NotRequired[float]
  """Working capital amount used in the last 24 hours."""
  remaining: NotRequired[float]
  """Remaining working capital amount available in the rolling 24-hour window."""
  refillThreshold: NotRequired[float]
  """Minimum balance amount that triggers an automatic working capital refill."""


WorkingCapitalLimits = list[WorkingCapitalLimit]


class WorkingCapital(RpcEndpoint):
  """Retrieve configured working capital limits and daily usage for the authenticated user."""

  async def working_capital(
    self,
    symbols: str | None = None,
    *,
    validate: bool | None = None,
  ) -> WorkingCapitalLimits:
    """Retrieve configured working capital limits and daily usage for the authenticated user.

    Args:
      symbols: Comma-separated currencies to retrieve working-capital limits for, for example `BTC,EUR`. Omit to retrieve all.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/trading-spot-rest#tag/Funding/operation/getWorkingCapital)
    """
    request: Request = Request()
    if symbols is not None:
      request['symbols'] = symbols
    return await self.request(
      request,
      method='GET',
      path='/v1/trading/working-capital',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=WorkingCapitalLimits,
    )
