# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint
from typed_bit2me.schemas import WalletResponse


class Request(TypedDict):
  fromPocketId: str
  """Bit2Me pocket identifier"""
  amount: str
  """Amount of `currency` to deposit."""
  currency: str
  """Valid currency symbol"""


class RequestDeposit(RpcEndpoint):
  """Request deposit funds into your Pro account from a Bit2Me Wallet."""

  async def request_deposit(
    self,
    *,
    from_pocket_id: str,
    amount: str,
    currency: str,
    validate: bool | None = None,
  ) -> WalletResponse:
    """Request deposit funds into your Pro account from a Bit2Me Wallet.

    Args:
      from_pocket_id: Bit2Me pocket identifier
      amount: Amount of `currency` to deposit.
      currency: Valid currency symbol
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/trading-spot-rest#tag/funding/POST/v1/trading/wallet/deposit)
    """
    return await self.request(
      Request(fromPocketId=from_pocket_id, amount=amount, currency=currency),
      method='POST',
      path='/v1/trading/wallet/deposit',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=WalletResponse,
    )
