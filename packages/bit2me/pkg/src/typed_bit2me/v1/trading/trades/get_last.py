# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint
from typed_bit2me.schemas import OrderSide


class Request(TypedDict):
  symbol: str
  """Market symbol"""
  limit: NotRequired[float]
  """The number of trades to retrieve"""


TradingLastTradesResponse = list[list[OrderSide | float]]


class GetLast(RpcEndpoint):
  """Get a list of last trades. Returns the last 50 trades by default."""

  async def get_last(
    self,
    symbol: str,
    *,
    limit: float | None = None,
    validate: bool | None = None,
  ) -> TradingLastTradesResponse:
    """Get a list of last trades. Returns the last 50 trades by default.

    Args:
      symbol: Market symbol
      limit: The number of trades to retrieve
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/trading-spot-rest#tag/marketdata/GET/v1/trading/trade/last)
    """
    request: Request = Request(symbol=symbol)
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/v1/trading/trade/last',
      meta={'public': True},
      validate=validate,
      request_type=Request,
      response_type=TradingLastTradesResponse,
    )
