# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint


class Entry(TypedDict):
  iso: str
  """ISO 3166-2 region code."""
  iso3: NotRequired[str]
  """ISO 3166-2 alpha-3 region code."""
  name: str
  """Region name, localized per `langCode`."""
  fips: str
  """Federal Information Processing Standard  (https://en.wikipedia.org/wiki/Federal_Information_Processing_Standards)"""


class Request(TypedDict):
  countryISOCode: str
  """ISO 3166-1 alpha-2 code of the country to list regions for."""
  langCode: NotRequired[Literal['EN', 'ES']]
  """Language fo localize country names"""


ListCountryRegionsResponse = list[Entry]


class Countries(RpcEndpoint):
  """Get country regions"""

  async def countries(
    self,
    country_isocode: str,
    *,
    lang_code: Literal['EN', 'ES'] | None = None,
    validate: bool | None = None,
  ) -> ListCountryRegionsResponse:
    """Get country regions

    Args:
      country_isocode: ISO 3166-1 alpha-2 code of the country to list regions for.
      lang_code: Language fo localize country names
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/misc/GET/v1/misc/country/{countryISOCode}/region)
    """
    request: Request = Request(countryISOCode=country_isocode)
    if lang_code is not None:
      request['langCode'] = lang_code
    return await self.request(
      request,
      method='GET',
      path='/v1/misc/country/{countryISOCode}/region',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=ListCountryRegionsResponse,
    )
