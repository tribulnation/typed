# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .account import Account
from .currency import Currency
from .earn import Earn
from .loan import Loan
from .trading import Trading
from .wallet import Wallet
from typed_bit2me.core.endpoint.rpc import RpcEndpoint


class V2(RpcEndpoint):
  """Bit2Me's REST API v2: a narrower, revised surface layered alongside v1 -- account address book, market asset catalog, Earn catalog, loan currency configuration, Trading Spot market data, and per-network wallet addresses.

  References:
    - [Upstream docs](https://api.bit2me.com/doc)
  """

  @cached_property
  def account(self) -> Account:
    """Bit2Me's v2 account address book: registering and listing an account's saved crypto withdrawal addresses.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/account)
    """
    return Account(client=self.client)

  @cached_property
  def currency(self) -> Currency:
    """Bit2Me's v2 asset catalog: listing supported currencies/assets and their per-asset metadata.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/market)
    """
    return Currency(client=self.client)

  @cached_property
  def earn(self) -> Earn:
    """Bit2Me's v2 Earn catalog: current APYs, supported Earn assets, and available Earn wallets.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/earn)
    """
    return Earn(client=self.client)

  @cached_property
  def loan(self) -> Loan:
    """Bit2Me Loan currency configuration (v2).

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/loan)
    """
    return Loan(client=self.client)

  @cached_property
  def trading(self) -> Trading:
    """Bit2Me's v2 Trading Spot market data: live order book depth and ticker snapshots.

    References:
      - [Upstream docs](https://api.bit2me.com/trading-spot-rest#tag/marketdata)
    """
    return Trading(client=self.client)

  @cached_property
  def wallet(self) -> Wallet:
    """Bit2Me's v2 wallet API: per-network deposit addresses for a wallet pocket, and the v2 transaction listing.

    References:
      - [Upstream docs](https://api.bit2me.com/doc#tag/wallet)
    """
    return Wallet(client=self.client)
