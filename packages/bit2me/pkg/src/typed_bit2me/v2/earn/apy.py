# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint


class Extra(TypedDict):
  daily: float
  """Annual percentage yield for the daily reward type."""
  weekly: float
  """Annual percentage yield for the weekly reward type."""
  monthly: float
  """Annual percentage yield for the monthly reward type."""


EarnApyV2Response = dict[str, Extra]


class Apy(RpcEndpoint):
  """Get current annual percentage yields by currency. Value of currency is an object wich reward type as a key and annual percentage yield as value"""

  async def apy(self, *, validate: bool | None = None) -> EarnApyV2Response:
    """Get current annual percentage yields by currency. Value of currency is an object wich reward type as a key and annual percentage yield as value

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/earn/GET/v2/earn/apy)
    """
    return await self.request(
      None,
      method='GET',
      path='/v2/earn/apy',
      meta={'public': True},
      validate=validate,
      response_type=EarnApyV2Response,
    )
