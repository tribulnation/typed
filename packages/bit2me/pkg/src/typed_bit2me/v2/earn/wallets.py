# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint
from typed_bit2me.schemas import EarnWalletResponse


class Request(TypedDict):
  userCurrency: NotRequired[str]
  """Valid currency symbol"""
  sortBy: NotRequired[Literal['currency', 'balance']]
  """Sorting field"""
  sortDirection: NotRequired[Literal['ascending', 'descending']]
  """Sorting direction"""
  offset: NotRequired[int]
  """Number of wallets to skip before returning results."""
  limit: NotRequired[int]
  """Maximum number of wallets to return."""


class Wallets(RpcEndpoint):
  """Get a list of earn wallets"""

  async def wallets(
    self,
    user_currency: str | None = None,
    *,
    sort_by: Literal['currency', 'balance'] | None = None,
    sort_direction: Literal['ascending', 'descending'] | None = None,
    offset: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> EarnWalletResponse:
    """Get a list of earn wallets

    Args:
      user_currency: Valid currency symbol
      sort_by: Sorting field
      sort_direction: Sorting direction
      offset: Number of wallets to skip before returning results.
      limit: Maximum number of wallets to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/doc#tag/earn/GET/v2/earn/wallets)
    """
    request: Request = Request()
    if user_currency is not None:
      request['userCurrency'] = user_currency
    if sort_by is not None:
      request['sortBy'] = sort_by
    if sort_direction is not None:
      request['sortDirection'] = sort_direction
    if offset is not None:
      request['offset'] = offset
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/v2/earn/wallets',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=EarnWalletResponse,
    )
