# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bit2me.core.endpoint.rpc import RpcEndpoint
from typed_bit2me.schemas import MillisTimestamp


class Request(TypedDict):
  symbol: str
  """Market symbol"""


class TradingOrderBookResponse(TypedDict):
  asks: NotRequired[list[tuple[float, float] | tuple[float, float, float]]]
  """Sell orders, sorted by ascending price. Most markets send a 2-element row (price, volume); some stablecoin/low-liquidity markets send a 3-element row instead, with a trailing total quote-currency value -- see this endpoint's own `notes`."""
  bids: NotRequired[list[tuple[float, float] | tuple[float, float, float]]]
  """Buy orders, sorted by descending price. Most markets send a 2-element row (price, volume); some stablecoin/low-liquidity markets send a 3-element row instead, with a trailing total quote-currency value -- see this endpoint's own `notes`."""
  timestamp: NotRequired[MillisTimestamp]
  symbol: NotRequired[str]
  """Market symbol"""


class OrderBook(RpcEndpoint):
  """Get the order book associated to the market symbol."""

  async def order_book(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> TradingOrderBookResponse:
    """Get the order book associated to the market symbol.

    Args:
      symbol: Market symbol
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://api.bit2me.com/trading-spot-rest#tag/marketdata/GET/v2/trading/order-book)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/v2/trading/order-book',
      meta={'public': True},
      validate=validate,
      request_type=Request,
      response_type=TradingOrderBookResponse,
    )
