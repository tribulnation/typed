# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_core.util import StreamManager
from typed_core.validation import validator
from binance.core import Timestamp
from binance.core.endpoint.stream import StreamEndpoint


class CoinMIndexPriceEvent(TypedDict):
  """Index price update for one pair."""

  e: Literal['IndexUpdate']
  """Event type."""
  E: Timestamp
  """Event time."""
  s: str
  """Pair."""
  p: str
  """Index price."""


class IndexPrice(StreamEndpoint):
  """Index price stream"""

  def __call__(
    self,
    pair: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[CoinMIndexPriceEvent]:
    """Index price stream

    Args:
      pair: Underlying pair, e.g. `BTCUSD`. The venue requires stream names to use lowercase symbols on the wire.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/ws-streams/~#index-price-stream)
    """
    _validator = validator[CoinMIndexPriceEvent](CoinMIndexPriceEvent)
    return self.subscribe(
      f'{pair.lower()}@indexPrice', validator=_validator, validate=validate
    )
