# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_core.util import StreamManager
from typed_core.validation import validator
from binance.core import Timestamp
from binance.core.endpoint.stream import StreamEndpoint


class CoinMMarkPriceKline(TypedDict):
  """The kline/candlestick itself."""

  t: Timestamp
  """Kline start time."""
  T: Timestamp
  """Kline close time."""
  s: str
  """Symbol."""
  i: Literal[
    '1m',
    '3m',
    '5m',
    '15m',
    '30m',
    '1h',
    '2h',
    '4h',
    '6h',
    '8h',
    '12h',
    '1d',
    '3d',
    '1w',
    '1M',
  ]
  """Interval."""
  f: int
  """First update ID."""
  L: int
  """Last update ID."""
  o: str
  """Open price."""
  c: str
  """Close price."""
  h: str
  """High price."""
  l: str
  """Low price."""
  v: str
  """Volume. Always `"0"` for a mark price kline -- there is no real trading volume behind a mark price."""
  n: int
  """Number of basic data points."""
  x: bool
  """Whether this kline is closed."""
  q: str
  """Base asset volume. Always `"0"` for a mark price kline."""
  V: str
  """Taker buy volume. Always `"0"` for a mark price kline."""
  Q: str
  """Taker buy base asset volume. Always `"0"` for a mark price kline."""
  B: str
  """Ignore -- unused field, kept for wire compatibility. Always `"0"` for a mark price kline."""


class CoinMMarkPriceKlineEvent(TypedDict):
  """One mark price kline/candlestick update."""

  e: Literal['markPrice_kline']
  """Event type."""
  E: Timestamp
  """Event time."""
  ps: str
  """Pair."""
  k: CoinMMarkPriceKline


class MarkPriceKline(StreamEndpoint):
  """Mark price kline/candlestick streams"""

  def __call__(
    self,
    symbol: str,
    interval: Literal[
      '1m',
      '3m',
      '5m',
      '15m',
      '30m',
      '1h',
      '2h',
      '4h',
      '6h',
      '8h',
      '12h',
      '1d',
      '3d',
      '1w',
      '1M',
    ],
    *,
    validate: bool | None = None,
  ) -> StreamManager[CoinMMarkPriceKlineEvent]:
    """Mark price kline/candlestick streams

    Args:
      symbol: Contract symbol, e.g. `BTCUSD_PERP`. The venue requires stream names to use lowercase symbols on the wire.
      interval: Kline interval.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/ws-streams/~#mark-price-kline-candlestick-streams)
    """
    _validator = validator[CoinMMarkPriceKlineEvent](CoinMMarkPriceKlineEvent)
    return self.subscribe(
      f'{symbol.lower()}@markPriceKline_{interval}',
      validator=_validator,
      validate=validate,
    )
