# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.ws_rpc import WsRpcEndpoint


class UsdMWsAlgoOrder(TypedDict):
  """USD-M Futures algo (conditional) order."""

  algoId: int
  """Algo order id."""
  clientAlgoId: str
  """Client algo order id."""
  algoType: Literal['CONDITIONAL']
  """Algo order family."""
  orderType: Literal[
    'STOP_MARKET', 'TAKE_PROFIT_MARKET', 'STOP', 'TAKE_PROFIT', 'TRAILING_STOP_MARKET'
  ]
  """Underlying order type."""
  symbol: str
  """Trading symbol."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: Literal['BOTH', 'LONG', 'SHORT']
  """Position side."""
  timeInForce: Literal['IOC', 'GTC', 'FOK']
  """Time in force."""
  quantity: str
  """Order quantity."""
  algoStatus: str
  """Algo order status. Left as a bare string -- its documented closed set isn't confirmed in this docs-only pass; see notes."""
  triggerPrice: str
  """Trigger price."""
  price: str
  """Order price."""
  icebergQuantity: NotRequired[str | None]
  """Iceberg quantity, or null when not an iceberg order."""
  selfTradePreventionMode: Literal[
    'NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER'
  ]
  """Self-trade prevention mode."""
  workingType: Literal['MARK_PRICE', 'CONTRACT_PRICE']
  """Price type used to evaluate the trigger condition."""
  priceMatch: NotRequired[
    Literal[
      'OPPONENT',
      'OPPONENT_5',
      'OPPONENT_10',
      'OPPONENT_20',
      'QUEUE',
      'QUEUE_5',
      'QUEUE_10',
      'QUEUE_20',
    ]
  ]
  """Auto price-match mode."""
  closePosition: bool
  """Whether this is a Close-All order."""
  priceProtect: bool
  """Whether price protection is enabled."""
  reduceOnly: bool
  """Whether the order is reduce-only."""
  createTime: int
  """Algo order creation time, milliseconds since epoch."""
  updateTime: int
  """Last update time, milliseconds since epoch."""
  triggerTime: int
  """Time the order was triggered, milliseconds since epoch. 0 if not yet triggered."""
  goodTillDate: int
  """Auto-cancel time for timeInForce GTD, milliseconds since epoch."""


class NewAlgoOrder(WsRpcEndpoint):
  """Send in a new algo (conditional) order -- TP/SL and trailing-stop orders. This is where the conditional order types dropped from order.place (STOP_MARKET/TAKE_PROFIT_MARKET/STOP/TAKE_PROFIT/TRAILING_STOP_MARKET) now live."""

  async def new_algo_order(
    self,
    *,
    algo_type: Literal['CONDITIONAL'],
    symbol: str,
    side: Literal['BUY', 'SELL'],
    type: Literal[
      'STOP_MARKET', 'TAKE_PROFIT_MARKET', 'STOP', 'TAKE_PROFIT', 'TRAILING_STOP_MARKET'
    ],
    position_side: Literal['BOTH', 'LONG', 'SHORT'] | None = None,
    time_in_force: Literal['IOC', 'GTC', 'FOK'] | None = None,
    quantity: str | None = None,
    price: str | None = None,
    trigger_price: str | None = None,
    working_type: Literal['MARK_PRICE', 'CONTRACT_PRICE'] | None = None,
    price_match: Literal[
      'OPPONENT',
      'OPPONENT_5',
      'OPPONENT_10',
      'OPPONENT_20',
      'QUEUE',
      'QUEUE_5',
      'QUEUE_10',
      'QUEUE_20',
    ]
    | None = None,
    close_position: bool | None = None,
    price_protect: bool | None = None,
    reduce_only: bool | None = None,
    activate_price: str | None = None,
    callback_rate: str | None = None,
    client_algo_id: str | None = None,
    new_order_resp_type: Literal['ACK', 'RESULT'] | None = None,
    self_trade_prevention_mode: Literal[
      'NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER'
    ]
    | None = None,
    good_till_date: int | None = None,
    validate: bool | None = None,
  ) -> UsdMWsAlgoOrder:
    """Send in a new algo (conditional) order -- TP/SL and trailing-stop orders. This is where the conditional order types dropped from order.place (STOP_MARKET/TAKE_PROFIT_MARKET/STOP/TAKE_PROFIT/TRAILING_STOP_MARKET) now live.

    Args:
      algo_type: Algo order family. Only CONDITIONAL is currently documented.
      symbol: Trading symbol.
      side: Order side.
      type: Algo order type.
      position_side: Default BOTH for One-way Mode; LONG or SHORT for Hedge Mode. Must be sent in Hedge Mode.
      time_in_force: Time in force. Default GTC.
      quantity: Order quantity.
      price: Order price.
      trigger_price: Trigger price for the conditional order.
      working_type: Price type used to evaluate the trigger condition. Default CONTRACT_PRICE.
      price_match: Auto price-match mode, only available for LIMIT/STOP/TAKE_PROFIT orders. Cannot be sent together with price.
      close_position: Close-All flag, used with STOP_MARKET or TAKE_PROFIT_MARKET.
      price_protect: Used with STOP_MARKET or TAKE_PROFIT_MARKET. Default false.
      reduce_only: Cannot be sent in Hedge Mode; cannot be sent with closePosition=true. Default false.
      activate_price: Used with TRAILING_STOP_MARKET orders. Defaults to the latest price.
      callback_rate: Used with TRAILING_STOP_MARKET orders.
      client_algo_id: A unique id among open algo orders. Auto-generated if not sent. Must match ^[.A-Z:/a-z0-9_-]{1,36}$.
      new_order_resp_type: Response type. Default ACK.
      self_trade_prevention_mode: Self-trade prevention mode. Default NONE.
      good_till_date: Order auto-cancel time for timeInForce GTD, mandatory when timeInForce is GTD.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/ws-api/trade#new-algo-order)
    """
    params: dict = {
      'algoType': algo_type,
      'symbol': symbol,
      'side': side,
      'type': type,
    }
    if position_side is not None:
      params['positionSide'] = position_side
    if time_in_force is not None:
      params['timeInForce'] = time_in_force
    if quantity is not None:
      params['quantity'] = quantity
    if price is not None:
      params['price'] = price
    if trigger_price is not None:
      params['triggerPrice'] = trigger_price
    if working_type is not None:
      params['workingType'] = working_type
    if price_match is not None:
      params['priceMatch'] = price_match
    if close_position is not None:
      params['closePosition'] = close_position
    if price_protect is not None:
      params['priceProtect'] = price_protect
    if reduce_only is not None:
      params['reduceOnly'] = reduce_only
    if activate_price is not None:
      params['activatePrice'] = activate_price
    if callback_rate is not None:
      params['callbackRate'] = callback_rate
    if client_algo_id is not None:
      params['clientAlgoId'] = client_algo_id
    if new_order_resp_type is not None:
      params['newOrderRespType'] = new_order_resp_type
    if self_trade_prevention_mode is not None:
      params['selfTradePreventionMode'] = self_trade_prevention_mode
    if good_till_date is not None:
      params['goodTillDate'] = good_till_date
    _Response = UsdMWsAlgoOrder
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'algoOrder.place', params=params, validator=_validator, validate=validate
    )
