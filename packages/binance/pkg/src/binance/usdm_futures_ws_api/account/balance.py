# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_core.validation import validator
from binance.core.endpoint.ws_rpc import WsRpcEndpoint


class UsdMWsBalance(TypedDict):
  """USD-M Futures account balance for one asset."""

  accountAlias: str
  """Unique account alias, generated by the server."""
  asset: str
  """Asset name."""
  balance: str
  """Wallet balance."""
  crossWalletBalance: str
  """Cross wallet balance."""
  crossUnPnl: str
  """Unrealized PnL of cross positions."""
  availableBalance: str
  """Available balance."""
  maxWithdrawAmount: str
  """Maximum transferable/withdrawable amount."""
  marginAvailable: bool
  """Whether the asset can be used as margin in multi-assets mode."""
  updateTime: int
  """Last update time, in milliseconds."""


class Balance(WsRpcEndpoint):
  """Query account balance information, per asset."""

  async def balance(self, *, validate: bool | None = None) -> list[UsdMWsBalance]:
    """Query account balance information, per asset.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/ws-api/account#futures-account-balance)
    """
    _Response = list[UsdMWsBalance]
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'account.balance', validator=_validator, validate=validate
    )
