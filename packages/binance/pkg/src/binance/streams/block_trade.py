# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_core.util import StreamManager
from typed_core.validation import validator
from binance.core import Timestamp
from binance.core.endpoint.stream import StreamEndpoint


class BlockTradeEvent(TypedDict):
  """One executed block trade."""

  e: Literal['blockTrade']
  """Event type."""
  E: Timestamp
  """Event time."""
  s: str
  """Symbol."""
  t: int
  """Block trade ID."""
  p: str
  """Price."""
  q: str
  """Quantity."""
  T: Timestamp
  """Trade time."""
  m: bool
  """Whether the buyer was the maker."""


class BlockTrade(StreamEndpoint):
  """Block trade stream"""

  def __call__(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[BlockTradeEvent]:
    """Block trade stream

    Args:
      symbol: Trading pair symbol, e.g. `BTCUSDT`. The venue requires stream names to use lowercase symbols on the wire.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/web-socket-streams.md#block-trade-streams)
    """
    _validator = validator[BlockTradeEvent](BlockTradeEvent)
    return self.subscribe(
      f'{symbol.lower()}@blockTrade', validator=_validator, validate=validate
    )
