# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_core.util import StreamManager
from typed_core.validation import validator
from binance.core import Timestamp
from binance.core.endpoint.stream import StreamEndpoint


class ReferencePriceEvent(TypedDict):
  """Reference price update for one symbol."""

  e: Literal['referencePrice']
  """Event type."""
  s: str
  """Symbol."""
  r: str | None
  """Reference price. `null` when no reference price is available."""
  t: Timestamp
  """Engine timestamp when the reference price was valid."""


class ReferencePrice(StreamEndpoint):
  """Reference price stream"""

  def __call__(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[ReferencePriceEvent]:
    """Reference price stream

    Args:
      symbol: Trading pair symbol, e.g. `BTCUSDT`. The venue requires stream names to use lowercase symbols on the wire.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/web-socket-streams.md#reference-price-streams)
    """
    _validator = validator[ReferencePriceEvent](ReferencePriceEvent)
    return self.subscribe(
      f'{symbol.lower()}@referencePrice', validator=_validator, validate=validate
    )
