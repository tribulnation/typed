# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_core.util import StreamManager
from typed_core.validation import validator
from binance.core import Timestamp
from binance.core.endpoint.stream import StreamEndpoint


class CompositeIndexComponent(TypedDict):
  """One constituent asset of a composite index."""

  b: str
  """Base asset."""
  q: str
  """Quote asset."""
  w: str
  """Weight in quantity."""
  W: str
  """Weight in percentage."""
  i: str
  """Index price."""


class CompositeIndexEvent(TypedDict):
  """Composite index information for one index symbol."""

  e: str
  """Event type."""
  E: Timestamp
  """Event time."""
  s: str
  """Symbol."""
  p: str
  """Price."""
  C: str
  """Base asset category."""
  c: list[CompositeIndexComponent]
  """Composition -- each constituent asset's weight in this index."""


class CompositeIndex(StreamEndpoint):
  """Composite index symbol information stream"""

  def __call__(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[CompositeIndexEvent]:
    """Composite index symbol information stream

    Args:
      symbol: Trading pair symbol, e.g. `BTCUSDT`. The venue requires stream names to use lowercase symbols on the wire.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/ws-streams/market#composite-index-symbol-information-streams)
    """
    _validator = validator[CompositeIndexEvent](CompositeIndexEvent)
    return self.subscribe(
      f'{symbol.lower()}@compositeIndex', validator=_validator, validate=validate
    )
