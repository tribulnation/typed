# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class PmMarginAccountRepay(TypedDict):
  """Repay for a margin loan."""

  tranId: NotRequired[int]
  """Transaction ID."""


class MarginRepay(RpcEndpoint):
  """Margin Account Repay"""

  async def margin_repay(
    self,
    *,
    asset: str,
    amount: float,
    validate: bool | None = None,
  ) -> PmMarginAccountRepay:
    """Repay for a margin loan.

    Args:
      asset: Asset name.
      amount: Amount.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-derivatives-trading-portfolio-margin/api/rest-api/trade#margin-account-repay)
    """
    params: dict = {
      'asset': asset,
      'amount': amount,
    }
    _Response = PmMarginAccountRepay
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/papi/v1/repayLoan',
      params=params,
      validator=_validator,
      validate=validate,
    )
