# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class PmBnbTransfer(TypedDict):
  """Transfer BNB in and out of UM."""

  tranId: NotRequired[int]
  """Transaction ID."""


class BnbTransfer(RpcEndpoint):
  """BNB transfer"""

  async def bnb_transfer(
    self,
    *,
    amount: float,
    transfer_side: Literal['TO_UM', 'FROM_UM'],
    validate: bool | None = None,
  ) -> PmBnbTransfer:
    """Transfer BNB in and out of UM.

    Args:
      amount: Amount.
      transfer_side: Transfer direction.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-derivatives-trading-portfolio-margin/api/rest-api/account#bnb-transfer)
    """
    params: dict = {
      'amount': amount,
      'transferSide': transfer_side,
    }
    _Response = PmBnbTransfer
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/papi/v1/bnb-transfer',
      params=params,
      validator=_validator,
      validate=validate,
    )
