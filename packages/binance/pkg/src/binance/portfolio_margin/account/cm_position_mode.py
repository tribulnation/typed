# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class PmCmCurrentPositionMode(TypedDict):
  """Get user's position mode (Hedge Mode or One-way Mode ) on EVERY symbol in CM."""

  dualSidePosition: NotRequired[bool]
  """\"true": Hedge Mode; "false": One-way Mode."""


class CmPositionMode(RpcEndpoint):
  """Get CM Current Position Mode"""

  async def cm_position_mode(
    self,
    *,
    validate: bool | None = None,
  ) -> PmCmCurrentPositionMode:
    """Get user's position mode (Hedge Mode or One-way Mode ) on EVERY symbol in CM.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-derivatives-trading-portfolio-margin/api/rest-api/account#get-cm-current-position-mode)
    """
    _Response = PmCmCurrentPositionMode
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET', '/papi/v1/cm/positionSide/dual', validator=_validator, validate=validate
    )
