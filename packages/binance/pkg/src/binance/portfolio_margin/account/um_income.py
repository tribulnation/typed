# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core import Timestamp, timestamp
from binance.core.endpoint.rpc import RpcEndpoint


class PmUmIncomeHistory(TypedDict):
  """PmUmIncomeHistory."""

  symbol: NotRequired[str]
  """Trade symbol, if existing."""
  incomeType: NotRequired[
    Literal[
      'TRANSFER',
      'WELCOME_BONUS',
      'REALIZED_PNL',
      'FUNDING_FEE',
      'COMMISSION',
      'INSURANCE_CLEAR',
      'REFERRAL_KICKBACK',
      'COMMISSION_REBATE',
      'API_REBATE',
      'CONTEST_REWARD',
      'CROSS_COLLATERAL_TRANSFER',
      'OPTIONS_PREMIUM_FEE',
      'OPTIONS_SETTLE_PROFIT',
      'INTERNAL_TRANSFER',
      'AUTO_EXCHANGE',
      'DELIVERED_SETTELMENT',
      'COIN_SWAP_DEPOSIT',
      'COIN_SWAP_WITHDRAW',
      'POSITION_LIMIT_INCREASE_FEE',
    ]
  ]
  """Income type."""
  income: NotRequired[str]
  """Income amount."""
  asset: NotRequired[str]
  """Income asset."""
  info: NotRequired[str]
  """Extra information."""
  time: NotRequired[Timestamp]
  """Event time."""
  tranId: NotRequired[str]
  """Transaction ID."""
  tradeId: NotRequired[str]
  """Trade ID, if existing."""


class UmIncome(RpcEndpoint):
  """Get UM Income History"""

  async def um_income(
    self,
    *,
    symbol: str | None = None,
    income_type: Literal[
      'TRANSFER',
      'WELCOME_BONUS',
      'REALIZED_PNL',
      'FUNDING_FEE',
      'COMMISSION',
      'INSURANCE_CLEAR',
      'REFERRAL_KICKBACK',
      'COMMISSION_REBATE',
      'API_REBATE',
      'CONTEST_REWARD',
      'CROSS_COLLATERAL_TRANSFER',
      'OPTIONS_PREMIUM_FEE',
      'OPTIONS_SETTLE_PROFIT',
      'INTERNAL_TRANSFER',
      'AUTO_EXCHANGE',
      'DELIVERED_SETTELMENT',
      'COIN_SWAP_DEPOSIT',
      'COIN_SWAP_WITHDRAW',
      'POSITION_LIMIT_INCREASE_FEE',
    ]
    | None = None,
    start_time: Timestamp | None = None,
    end_time: Timestamp | None = None,
    page: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> list[PmUmIncomeHistory]:
    """Get UM Income History.

    Args:
      symbol: Trading pair symbol.
      income_type: Income type.
      start_time: Timestamp in ms to get funding from INCLUSIVE.
      end_time: Timestamp in ms to get funding until INCLUSIVE.
      page: Page number.
      limit: Number of results returned.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-derivatives-trading-portfolio-margin/api/rest-api/account#get-um-income-history)
    """
    params = {}
    if symbol is not None:
      params['symbol'] = symbol
    if income_type is not None:
      params['incomeType'] = income_type
    if start_time is not None:
      params['startTime'] = timestamp.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp.dump(end_time)
    if page is not None:
      params['page'] = page
    if limit is not None:
      params['limit'] = limit
    _Response = list[PmUmIncomeHistory]
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/papi/v1/um/income',
      params=params,
      validator=_validator,
      validate=validate,
    )
