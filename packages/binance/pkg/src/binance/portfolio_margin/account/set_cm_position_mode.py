# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class PmChangeCmPositionMode(TypedDict):
  """Change user's position mode (Hedge Mode or One-way Mode ) on EVERY symbol in CM."""

  code: NotRequired[int]
  """Code."""
  msg: NotRequired[str]
  """Msg."""


class SetCmPositionMode(RpcEndpoint):
  """Change CM Position Mode"""

  async def set_cm_position_mode(
    self,
    *,
    dual_side_position: Literal['true', 'false'],
    validate: bool | None = None,
  ) -> PmChangeCmPositionMode:
    """Change user's position mode (Hedge Mode or One-way Mode ) on EVERY symbol in CM.

    Args:
      dual_side_position: \"true": Hedge Mode; "false": One-way Mode.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-derivatives-trading-portfolio-margin/api/rest-api/account#change-cm-position-mode)
    """
    params: dict = {
      'dualSidePosition': dual_side_position,
    }
    _Response = PmChangeCmPositionMode
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/papi/v1/cm/positionSide/dual',
      params=params,
      validator=_validator,
      validate=validate,
    )
