# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.util import StreamManager
from typed_core.validation import validator
from binance.core import Timestamp
from binance.core.endpoint.stream import StreamEndpoint


class RpiDepthEvent(TypedDict):
  """Incremental bid/ask updates (including RPI orders) for one symbol."""

  e: str
  """Event type."""
  E: Timestamp
  """Event time."""
  T: Timestamp
  """Transaction time."""
  s: str
  """Symbol."""
  U: int
  """First update ID in event."""
  u: int
  """Final update ID in event."""
  pu: int
  """Final update ID in the previous stream event."""
  b: list[tuple[str, str]]
  """Bid updates."""
  a: list[tuple[str, str]]
  """Ask updates."""
  ps: NotRequired[str]
  """(After UM/CM stream-host migration) Pair symbol."""
  st: NotRequired[int]
  """(After UM/CM stream-host migration) Symbol type: 1 = UM, 2 = CM."""


class RpiDepth(StreamEndpoint):
  """RPI diff. book depth stream"""

  def __call__(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[RpiDepthEvent]:
    """RPI diff. book depth stream

    Args:
      symbol: Trading pair symbol, e.g. `BTCUSDT`. The venue requires stream names to use lowercase symbols on the wire.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/ws-streams/public#rpi-diff-book-depth-streams)
    """
    _validator = validator[RpiDepthEvent](RpiDepthEvent)
    return self.subscribe(
      f'{symbol.lower()}@rpiDepth@500ms', validator=_validator, validate=validate
    )
