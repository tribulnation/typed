# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class CoinMOpenInterestStat(TypedDict):
  """Open interest for one period bucket."""

  pair: str
  """Underlying pair."""
  contractType: Literal['ALL', 'PERPETUAL', 'CURRENT_QUARTER', 'NEXT_QUARTER']
  """Contract type."""
  sumOpenInterest: str
  """Sum of open interest, in contracts."""
  sumOpenInterestValue: str
  """Sum of open interest, in base asset."""
  timestamp: int
  """End time of the period, in milliseconds since epoch."""


class OpenInterestHist(RpcEndpoint):
  """Open interest statistics for a pair and contract type, bucketed by period."""

  async def open_interest_hist(
    self,
    *,
    pair: str,
    contract_type: Literal['ALL', 'PERPETUAL', 'CURRENT_QUARTER', 'NEXT_QUARTER'],
    period: Literal['5m', '15m', '30m', '1h', '2h', '4h', '6h', '12h', '1d'],
    limit: int | None = None,
    start_time: int | None = None,
    end_time: int | None = None,
    validate: bool | None = None,
  ) -> list[CoinMOpenInterestStat]:
    """Open interest statistics for a pair and contract type, bucketed by period.

    Args:
      pair: Underlying pair.
      contract_type: Contract type.
      period: Bucket period.
      limit: Number of records to return.
      start_time: Start time, in milliseconds since epoch.
      end_time: End time, in milliseconds since epoch.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/rest-api/market-data#open-interest-statistics)
    """
    params: dict = {
      'pair': pair,
      'contractType': contract_type,
      'period': period,
    }
    if limit is not None:
      params['limit'] = limit
    if start_time is not None:
      params['startTime'] = start_time
    if end_time is not None:
      params['endTime'] = end_time
    _Response = list[CoinMOpenInterestStat]
    _validator = validator[_Response](_Response)
    return await self.request(
      'GET',
      '/futures/data/openInterestHist',
      params=params,
      validator=_validator,
      validate=validate,
    )
