# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class CoinMIndexConstituent(TypedDict):
  """One exchange's contribution to the index price."""

  exchange: str
  """Exchange identifier."""
  symbol: str
  """Trading symbol on that exchange."""
  price: str
  """Price on that exchange."""
  weight: str
  """Weight given to this exchange in the index."""


class CoinMIndexConstituents(TypedDict):
  """Index price constituents of a symbol."""

  symbol: str
  """Trading symbol."""
  time: int
  """Time, in milliseconds since epoch."""
  constituents: list[CoinMIndexConstituent]
  """Constituent exchanges and their weights."""


class Constituents(RpcEndpoint):
  """Index price constituents of a symbol -- the exchanges and weights that make up its index price."""

  async def constituents(
    self,
    *,
    symbol: str,
    validate: bool | None = None,
  ) -> CoinMIndexConstituents:
    """Index price constituents of a symbol -- the exchanges and weights that make up its index price.

    Args:
      symbol: Symbol -- in practice, the underlying spot-style pair/asset name (e.g. "BTCUSD", "BTCUSDT"), not a COIN-M contract symbol like "BTCUSD_PERP" (rejected live). See notes.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/rest-api/market-data#query-index-price-constituents)
    """
    params: dict = {
      'symbol': symbol,
    }
    _Response = CoinMIndexConstituents
    _validator = validator[_Response](_Response)
    return await self.request(
      'GET',
      '/dapi/v1/constituents',
      params=params,
      validator=_validator,
      validate=validate,
    )
