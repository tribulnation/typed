# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class CoinMBasis(TypedDict):
  """Basis (futures price vs. index price spread) for one period bucket."""

  indexPrice: str
  """Index price."""
  contractType: Literal['PERPETUAL', 'CURRENT_QUARTER', 'NEXT_QUARTER']
  """Contract type."""
  basisRate: str
  """Basis rate."""
  futuresPrice: str
  """Futures price."""
  annualizedBasisRate: str
  """Annualized basis rate."""
  basis: str
  """Basis value (futures price minus index price)."""
  pair: str
  """Underlying pair."""
  timestamp: int
  """Start time of the period, in milliseconds since epoch."""


class Basis(RpcEndpoint):
  """Basis data (spread between futures price and index price) for a pair and contract type."""

  async def basis(
    self,
    *,
    pair: str,
    contract_type: Literal['PERPETUAL', 'CURRENT_QUARTER', 'NEXT_QUARTER'],
    period: Literal['5m', '15m', '30m', '1h', '2h', '4h', '6h', '12h', '1d'],
    limit: int | None = None,
    start_time: int | None = None,
    end_time: int | None = None,
    validate: bool | None = None,
  ) -> list[CoinMBasis]:
    """Basis data (spread between futures price and index price) for a pair and contract type.

    Args:
      pair: Underlying pair.
      contract_type: Contract type.
      period: Bucket period.
      limit: Number of records to return.
      start_time: Start time, in milliseconds since epoch.
      end_time: End time, in milliseconds since epoch.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/rest-api/market-data#basis)
    """
    params: dict = {
      'pair': pair,
      'contractType': contract_type,
      'period': period,
    }
    if limit is not None:
      params['limit'] = limit
    if start_time is not None:
      params['startTime'] = start_time
    if end_time is not None:
      params['endTime'] = end_time
    _Response = list[CoinMBasis]
    _validator = validator[_Response](_Response)
    return await self.request(
      'GET',
      '/futures/data/basis',
      params=params,
      validator=_validator,
      validate=validate,
    )
