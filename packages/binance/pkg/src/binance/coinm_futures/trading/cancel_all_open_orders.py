# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class CoinMActionResult(TypedDict):
  """Acknowledgement of the action."""

  code: int
  """Result code."""
  msg: str
  """Result message."""


class CancelAllOpenOrders(RpcEndpoint):
  """Cancel All Open Orders"""

  async def cancel_all_open_orders(
    self,
    *,
    symbol: str,
    validate: bool | None = None,
  ) -> CoinMActionResult:
    """Cancel all open orders on a symbol.

    Args:
      symbol: Symbol.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/rest-api/trade#cancel-all-open-orders)
    """
    params: dict = {
      'symbol': symbol,
    }
    _Response = CoinMActionResult
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'DELETE',
      '/dapi/v1/allOpenOrders',
      params=params,
      validator=_validator,
      validate=validate,
    )
