# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class CoinMAdlQuantileBySide(TypedDict):
  """ADL quantile (0-4) per position side."""

  LONG: NotRequired[int]
  """ADL quantile for the LONG position in Hedge Mode."""
  SHORT: NotRequired[int]
  """ADL quantile for the SHORT position in Hedge Mode."""
  HEDGE: NotRequired[int]
  """ADL quantile for a cross-margined Hedge-Mode symbol."""
  BOTH: NotRequired[int]
  """ADL quantile for the One-way Mode position."""


class CoinMAdlQuantile(TypedDict):
  """A symbol's ADL quantile by position side."""

  symbol: str
  """Symbol."""
  adlQuantile: CoinMAdlQuantileBySide


class PositionAdlQuantileEstimation(RpcEndpoint):
  """Position ADL Quantile Estimation"""

  async def position_adl_quantile_estimation(
    self,
    *,
    symbol: str | None = None,
    validate: bool | None = None,
  ) -> list[CoinMAdlQuantile]:
    """Query each position's auto-deleverage (ADL) quantile. Values update every 30s. 0-4 shows queue position/likelihood of ADL from low to high. `HEDGE` replaces `BOTH` when the symbol's Hedge-Mode positions are cross-margined; in that case `LONG` and `SHORT` both show the same cross-margined value.

    Args:
      symbol: Symbol. If omitted, ADL quantiles for every symbol with an open position are returned.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/rest-api/trade#position-adl-quantile-estimation)
    """
    params = {}
    if symbol is not None:
      params['symbol'] = symbol
    _Response = list[CoinMAdlQuantile]
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/dapi/v1/adlQuantile',
      params=params,
      validator=_validator,
      validate=validate,
    )
