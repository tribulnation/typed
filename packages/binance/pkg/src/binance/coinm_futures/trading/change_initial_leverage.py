# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class CoinMLeverageResult(TypedDict):
  """The leverage that was set."""

  leverage: int
  """The new initial leverage."""
  maxQty: NotRequired[str]
  """Maximum order quantity, in the base asset, allowed at this leverage."""
  symbol: str
  """Symbol."""


class ChangeInitialLeverage(RpcEndpoint):
  """Change Initial Leverage"""

  async def change_initial_leverage(
    self,
    *,
    symbol: str,
    leverage: int,
    validate: bool | None = None,
  ) -> CoinMLeverageResult:
    """Change the account's initial leverage on a symbol. In Hedge Mode, the LONG and SHORT positions of one symbol share the same initial leverage and total notional.

    Args:
      symbol: Symbol.
      leverage: Target initial leverage.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/rest-api/trade#change-initial-leverage)
    """
    params: dict = {
      'symbol': symbol,
      'leverage': leverage,
    }
    _Response = CoinMLeverageResult
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/dapi/v1/leverage',
      params=params,
      validator=_validator,
      validate=validate,
    )
