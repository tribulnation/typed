# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class DeliveryPrice(TypedDict):
  """Settlement price at one quarterly contract delivery."""

  deliveryTime: int
  """Settlement time, in milliseconds since epoch."""
  deliveryPrice: float
  """Settlement price at delivery."""


class DeliveryPriceEndpoint(RpcEndpoint):
  """Historical settlement prices for a quarterly-contract pair's deliveries."""

  async def delivery_price(
    self,
    *,
    pair: str,
    validate: bool | None = None,
  ) -> list[DeliveryPrice]:
    """Historical settlement prices for a quarterly-contract pair's deliveries.

    Args:
      pair: Underlying pair, e.g. BTCUSDT.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/market-data#quarterly-contract-settlement-price)
    """
    params: dict = {
      'pair': pair,
    }
    _Response = list[DeliveryPrice]
    _validator = validator[_Response](_Response)
    return await self.request(
      'GET',
      '/futures/data/delivery-price',
      params=params,
      validator=_validator,
      validate=validate,
    )
