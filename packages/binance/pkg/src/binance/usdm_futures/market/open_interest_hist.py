# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class OpenInterestStat(TypedDict):
  """Open interest at one period bucket."""

  symbol: str
  """Trading symbol."""
  sumOpenInterest: str
  """Aggregate open interest, in contracts."""
  sumOpenInterestValue: str
  """Aggregate open interest, in notional value."""
  CMCCirculatingSupply: NotRequired[str]
  """Circulating supply figure sourced from CoinMarketCap."""
  timestamp: int
  """Period end time, in milliseconds since epoch."""


class OpenInterestHist(RpcEndpoint):
  """Historical open interest statistics for a symbol, bucketed by period."""

  async def open_interest_hist(
    self,
    *,
    symbol: str,
    period: Literal['5m', '15m', '30m', '1h', '2h', '4h', '6h', '12h', '1d'],
    limit: int | None = None,
    start_time: int | None = None,
    end_time: int | None = None,
    validate: bool | None = None,
  ) -> list[OpenInterestStat]:
    """Historical open interest statistics for a symbol, bucketed by period.

    Args:
      symbol: Trading symbol, e.g. BTCUSDT.
      period: Bucket width for each returned data point.
      limit: Number of data points to return.
      start_time: Start time, in milliseconds since epoch.
      end_time: End time, in milliseconds since epoch.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/market-data#open-interest-statistics)
    """
    params: dict = {
      'symbol': symbol,
      'period': period,
    }
    if limit is not None:
      params['limit'] = limit
    if start_time is not None:
      params['startTime'] = start_time
    if end_time is not None:
      params['endTime'] = end_time
    _Response = list[OpenInterestStat]
    _validator = validator[_Response](_Response)
    return await self.request(
      'GET',
      '/futures/data/openInterestHist',
      params=params,
      validator=_validator,
      validate=validate,
    )
