# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class BasisRecord(TypedDict):
  """Futures/index basis at one period bucket."""

  pair: str
  """Underlying pair."""
  contractType: Literal['PERPETUAL', 'CURRENT_QUARTER', 'NEXT_QUARTER']
  """Contract type this basis was computed for."""
  futuresPrice: str
  """Futures market price."""
  indexPrice: str
  """Index price."""
  basis: str
  """Absolute difference: futures price minus index price."""
  basisRate: str
  """Proportional difference between futures and index prices."""
  annualizedBasisRate: NotRequired[str]
  """Basis rate, annualized."""
  timestamp: int
  """Period start time, in milliseconds since epoch."""


class Basis(RpcEndpoint):
  """Futures basis (difference between futures price and index price) for a pair and contract type, bucketed by period."""

  async def basis(
    self,
    *,
    pair: str,
    contract_type: Literal['PERPETUAL', 'CURRENT_QUARTER', 'NEXT_QUARTER'],
    period: Literal['5m', '15m', '30m', '1h', '2h', '4h', '6h', '12h', '1d'],
    limit: int | None = None,
    start_time: int | None = None,
    end_time: int | None = None,
    validate: bool | None = None,
  ) -> list[BasisRecord]:
    """Futures basis (difference between futures price and index price) for a pair and contract type, bucketed by period.

    Args:
      pair: Underlying pair, e.g. BTCUSDT.
      contract_type: Contract type.
      period: Bucket width for each returned data point.
      limit: Number of data points to return.
      start_time: Start time, in milliseconds since epoch.
      end_time: End time, in milliseconds since epoch.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/market-data#basis)
    """
    params: dict = {
      'pair': pair,
      'contractType': contract_type,
      'period': period,
    }
    if limit is not None:
      params['limit'] = limit
    if start_time is not None:
      params['startTime'] = start_time
    if end_time is not None:
      params['endTime'] = end_time
    _Response = list[BasisRecord]
    _validator = validator[_Response](_Response)
    return await self.request(
      'GET',
      '/futures/data/basis',
      params=params,
      validator=_validator,
      validate=validate,
    )
