# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class ChangeMarginTypeResult(TypedDict):
  """Result of changing a symbol's margin type."""

  code: int
  """Result code."""
  msg: NotRequired[str]
  """Result message."""


class MarginType(RpcEndpoint):
  """Change a symbol's margin type (ISOLATED or CROSSED)."""

  async def margin_type(
    self,
    *,
    symbol: str,
    margin_type: Literal['ISOLATED', 'CROSSED'],
    validate: bool | None = None,
  ) -> ChangeMarginTypeResult:
    """Change a symbol's margin type (ISOLATED or CROSSED).

    Args:
      symbol: Trading symbol, e.g. BTCUSDT.
      margin_type: Target margin type.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/trade#change-margin-type)
    """
    params: dict = {
      'symbol': symbol,
      'marginType': margin_type,
    }
    _Response = ChangeMarginTypeResult
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/fapi/v1/marginType',
      params=params,
      validator=_validator,
      validate=validate,
    )
