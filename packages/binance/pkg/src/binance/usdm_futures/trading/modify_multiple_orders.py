# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core import Timestamp
from binance.core.endpoint.rpc import RpcEndpoint


class BatchOrderError(TypedDict):
  """Per-element failure returned in place of an order when a batch element is rejected."""

  code: int
  """Binance error code for this element."""
  msg: str
  """Human-readable error message for this element."""


class ModifiedFuturesOrder(TypedDict):
  """One successfully modified order."""

  orderId: int
  """Order id."""
  symbol: str
  """Trading symbol."""
  pair: str
  """Underlying contract pair."""
  status: str
  """Order status."""
  clientOrderId: str
  """Client order id, either caller-supplied or venue-generated."""
  modifyId: NotRequired[int]
  """Caller-supplied modification id, echoed back when provided in the request."""
  price: str
  """Order price."""
  origQty: str
  """Original order quantity."""
  executedQty: str
  """Cumulative filled quantity."""
  cumQty: str
  """Cumulative filled quantity."""
  timeInForce: Literal['GTC', 'IOC', 'FOK', 'GTX', 'GTD', 'RPI']
  """Time in force."""
  type: Literal[
    'LIMIT',
    'MARKET',
    'STOP',
    'STOP_MARKET',
    'TAKE_PROFIT',
    'TAKE_PROFIT_MARKET',
    'TRAILING_STOP_MARKET',
  ]
  """Order type."""
  reduceOnly: bool
  """Whether the order only reduces an existing position."""
  closePosition: bool
  """Whether the order closes the entire position (Close-All)."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: Literal['BOTH', 'LONG', 'SHORT']
  """Position side."""
  stopPrice: str
  """Trigger price for a conditional order. Ignored for LIMIT and MARKET orders."""
  workingType: Literal['MARK_PRICE', 'CONTRACT_PRICE']
  """Price type used to trigger conditional orders."""
  priceProtect: bool
  """Whether price protection is active for a conditional order's trigger."""
  origType: Literal[
    'LIMIT',
    'MARKET',
    'STOP',
    'STOP_MARKET',
    'TAKE_PROFIT',
    'TAKE_PROFIT_MARKET',
    'TRAILING_STOP_MARKET',
  ]
  """Order type at creation, unaffected by later amendments."""
  priceMatch: Literal[
    'OPPONENT',
    'OPPONENT_5',
    'OPPONENT_10',
    'OPPONENT_20',
    'QUEUE',
    'QUEUE_5',
    'QUEUE_10',
    'QUEUE_20',
  ]
  """Price match mode used to auto-align the order price to the order book."""
  selfTradePreventionMode: Literal[
    'NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER'
  ]
  """Self-trade prevention mode."""
  goodTillDate: int
  """Auto-cancel time for a GTD order, as a Unix epoch in milliseconds."""
  updateTime: Timestamp
  """Time the order was last updated."""


class ModifyMultipleOrders(RpcEndpoint):
  """Modify multiple orders in one request. Parameter rules per order are the same as Modify Order."""

  async def modify_multiple_orders(
    self,
    *,
    batch_orders: str,
    validate: bool | None = None,
  ) -> list[ModifiedFuturesOrder | BatchOrderError]:
    """Modify multiple orders in one request. Parameter rules per order are the same as Modify Order.

    Args:
      batch_orders: JSON-encoded array of order modifications, e.g. '[{"orderId":1,"symbol":"BTCUSDT","side":"BUY","quantity":"2","price":"30000"}]'. Max 5 orders. Each element carries the same fields as Modify Order: orderId, origClientOrderId, symbol, side, quantity, price, priceMatch, stopPrice, modifyId.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/trade#modify-multiple-orders)
    """
    params: dict = {
      'batchOrders': batch_orders,
    }
    _Response = list[ModifiedFuturesOrder | BatchOrderError]
    _validator = validator[_Response](_Response)  # type: ignore
    return await self.authed_request(
      'PUT',
      '/fapi/v1/batchOrders',
      params=params,
      validator=_validator,
      validate=validate,
    )
