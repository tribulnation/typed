# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class PositionMode(TypedDict):
  """The account's current position mode, applied across every symbol."""

  dualSidePosition: bool
  """true: Hedge Mode; false: One-way Mode."""


class PositionModeEndpoint(RpcEndpoint):
  """Get the user's position mode (Hedge Mode or One-way Mode) on every symbol."""

  async def position_mode(self, *, validate: bool | None = None) -> PositionMode:
    """Get the user's position mode (Hedge Mode or One-way Mode) on every symbol.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/account#get-current-position-mode)
    """
    _Response = PositionMode
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET', '/fapi/v1/positionSide/dual', validator=_validator, validate=validate
    )
