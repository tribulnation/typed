# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class FuturesAdlQuantileBySide(TypedDict):
  """ADL quantile per position side, 0-4 (low to high risk of auto-deleverage)."""

  LONG: NotRequired[int]
  """ADL quantile for the LONG position in Hedge Mode."""
  SHORT: NotRequired[int]
  """ADL quantile for the SHORT position in Hedge Mode."""
  HEDGE: NotRequired[int]
  """Sign value only for crossed-margined Hedge Mode positions; the numeric value itself can be ignored."""
  BOTH: NotRequired[int]
  """ADL quantile for the position in One-way Mode."""


class FuturesAdlQuantileRow(TypedDict):
  """ADL quantile estimation for one symbol."""

  symbol: str
  """Trading symbol."""
  adlQuantile: FuturesAdlQuantileBySide


class AdlQuantile(RpcEndpoint):
  """Position ADL (Auto-Deleverage) Quantile Estimation. Values update every 30s; 0-4 shows the queue position and possibility of ADL from low to high. For One-way Mode or isolated-margined Hedge Mode positions, LONG/SHORT/BOTH quantiles are returned. For crossed-margined Hedge Mode positions, HEDGE is returned instead of BOTH as a sign, and LONG/SHORT share one value calculated on both sides' unrealized pnl."""

  async def adl_quantile(
    self,
    *,
    symbol: str | None = None,
    validate: bool | None = None,
  ) -> list[FuturesAdlQuantileRow]:
    """Position ADL (Auto-Deleverage) Quantile Estimation. Values update every 30s; 0-4 shows the queue position and possibility of ADL from low to high. For One-way Mode or isolated-margined Hedge Mode positions, LONG/SHORT/BOTH quantiles are returned. For crossed-margined Hedge Mode positions, HEDGE is returned instead of BOTH as a sign, and LONG/SHORT share one value calculated on both sides' unrealized pnl.

    Args:
      symbol: Trading symbol. If omitted, ADL quantiles for every symbol are returned.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/trade#position-adl-quantile-estimation)
    """
    params = {}
    if symbol is not None:
      params['symbol'] = symbol
    _Response = list[FuturesAdlQuantileRow]
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/fapi/v1/adlQuantile',
      params=params,
      validator=_validator,
      validate=validate,
    )
