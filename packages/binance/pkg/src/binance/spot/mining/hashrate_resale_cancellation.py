# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class HashrateResaleCancellationResponse(TypedDict):
  """Hashrate resale cancellation response frame. This product line wraps its business data in {code,msg,data} on the wire, and the core does not strip it -- see spec/core.md's Envelope section."""

  code: NotRequired[int]
  """Response code. 0 indicates success."""
  msg: NotRequired[str]
  """Response message."""
  data: NotRequired[bool]
  """Whether the cancellation succeeded."""


class HashrateResaleCancellation(RpcEndpoint):
  """Cancel an active hashrate resale configuration created by `spot.mining.hashrate_resale_request`."""

  async def hashrate_resale_cancellation(
    self,
    *,
    config_id: int,
    user_name: str,
    validate: bool | None = None,
  ) -> HashrateResaleCancellationResponse:
    """Cancel an active hashrate resale configuration created by `spot.mining.hashrate_resale_request`.

    Args:
      config_id: Resale configuration identifier, from `spot.mining.hashrate_resale_request` or `spot.mining.hashrate_resale_list`.
      user_name: Mining account the hashrate was resold from.

    References:
      - [Official docs](https://developers.binance.com/docs/mining/rest-api#cancel-hashrate-resale-configuration)
    """
    params: dict = {
      'configId': config_id,
      'userName': user_name,
    }
    _Response = HashrateResaleCancellationResponse
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/mining/hash-transfer/config/cancel',
      params=params,
      validator=_validator,
      validate=validate,
    )
