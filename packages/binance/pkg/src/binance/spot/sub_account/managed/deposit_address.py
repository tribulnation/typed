# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class ManagedSubAccountDepositAddress(TypedDict):
  """The Managed Sub-Account's deposit address."""

  coin: NotRequired[str]
  """Coin the address accepts."""
  address: NotRequired[str]
  """Deposit address."""
  tag: NotRequired[str]
  """Address tag/memo, when the network requires one."""
  url: NotRequired[str]
  """Block explorer URL for the address."""


class DepositAddress(RpcEndpoint):
  """Get a Managed Sub-Account's deposit address for one coin, for the investor master account."""

  async def deposit_address(
    self,
    *,
    email: str,
    coin: str,
    network: str | None = None,
    amount: float | None = None,
    validate: bool | None = None,
  ) -> ManagedSubAccountDepositAddress:
    """Get a Managed Sub-Account's deposit address for one coin, for the investor master account.

    Args:
      email: Managed Sub-Account email.
      coin: Coin to get the deposit address for.
      network: Network to get the deposit address on. Networks can be found via `GET /sapi/v1/capital/deposit/address`. Defaults to the coin's default network.
      amount: Deposit amount. Required when `network` is `LIGHTNING`.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-sub-account/api/rest-api/managed-sub-account#get-managed-sub-account-deposit-address)
    """
    params: dict = {
      'email': email,
      'coin': coin,
    }
    if network is not None:
      params['network'] = network
    if amount is not None:
      params['amount'] = amount
    _Response = ManagedSubAccountDepositAddress
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/sapi/v1/managed-subaccount/deposit/address',
      params=params,
      validator=_validator,
      validate=validate,
    )
