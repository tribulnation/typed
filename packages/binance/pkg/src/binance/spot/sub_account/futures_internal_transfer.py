# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class SubAccountFuturesAssetTransferResult(TypedDict):
  """The executed transfer."""

  success: NotRequired[bool]
  """Whether the transfer succeeded."""
  txnId: NotRequired[str]
  """Transaction id of the transfer."""


class FuturesInternalTransfer(RpcEndpoint):
  """Transfer a futures asset directly between two sub-accounts, for the master account."""

  async def __call__(
    self,
    *,
    from_email: str,
    to_email: str,
    futures_type: int,
    asset: str,
    amount: float,
    validate: bool | None = None,
  ) -> SubAccountFuturesAssetTransferResult:
    """Transfer a futures asset directly between two sub-accounts, for the master account.

    Args:
      from_email: Email of the sending sub-account.
      to_email: Email of the receiving sub-account.
      futures_type: Which futures product: `1` = USD-M (USDT-margined) futures, `2` = COIN-M (coin-margined) futures.
      asset: Asset to transfer.
      amount: Amount to transfer.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-sub-account/api/rest-api/asset-management#sub-account-futures-asset-transfer)
    """
    params: dict = {
      'fromEmail': from_email,
      'toEmail': to_email,
      'futuresType': futures_type,
      'asset': asset,
      'amount': amount,
    }
    _Response = SubAccountFuturesAssetTransferResult
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/sub-account/futures/internalTransfer',
      params=params,
      validator=_validator,
      validate=validate,
    )
