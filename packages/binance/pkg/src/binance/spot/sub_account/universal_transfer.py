# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class UniversalTransferResult(TypedDict):
  """The executed transfer."""

  tranId: NotRequired[int]
  """Transfer id."""
  clientTranId: NotRequired[str]
  """Caller-supplied transfer id, if one was given."""


class UniversalTransfer(RpcEndpoint):
  """Transfer an asset between any two accounts/wallets in the master account's hierarchy, for the master account."""

  async def __call__(
    self,
    *,
    from_email: str | None = None,
    to_email: str | None = None,
    from_account_type: Literal[
      'SPOT', 'USDT_FUTURE', 'COIN_FUTURE', 'MARGIN', 'ISOLATED_MARGIN'
    ],
    to_account_type: Literal[
      'SPOT', 'USDT_FUTURE', 'COIN_FUTURE', 'MARGIN', 'ISOLATED_MARGIN'
    ],
    client_tran_id: str | None = None,
    symbol: str | None = None,
    asset: str,
    amount: float,
    validate: bool | None = None,
  ) -> UniversalTransferResult:
    """Transfer an asset between any two accounts/wallets in the master account's hierarchy, for the master account.

    Args:
      from_email: Email of the sending account. Omit to transfer from the master account.
      to_email: Email of the receiving account. Omit to transfer into the master account.
      from_account_type: Account type to transfer from.
      to_account_type: Account type to transfer to.
      client_tran_id: Caller-supplied transfer id. Must be unique.
      symbol: Isolated margin symbol. Only used when either account type is `ISOLATED_MARGIN`.
      asset: Asset to transfer.
      amount: Amount to transfer.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-sub-account/api/rest-api/asset-management#universal-transfer)
    """
    params: dict = {
      'fromAccountType': from_account_type,
      'toAccountType': to_account_type,
      'asset': asset,
      'amount': amount,
    }
    if from_email is not None:
      params['fromEmail'] = from_email
    if to_email is not None:
      params['toEmail'] = to_email
    if client_tran_id is not None:
      params['clientTranId'] = client_tran_id
    if symbol is not None:
      params['symbol'] = symbol
    _Response = UniversalTransferResult
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/sub-account/universalTransfer',
      params=params,
      validator=_validator,
      validate=validate,
    )
