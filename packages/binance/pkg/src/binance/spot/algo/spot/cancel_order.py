# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class AlgoCancelAck(TypedDict):
  """Acknowledgement that an Algo order was cancelled."""

  algoId: int
  """Algo order identifier that was cancelled."""
  success: bool
  """Whether the cancellation was accepted."""
  code: int
  """Response code. `0` on success."""
  msg: str
  """Response message. `"OK"` on success."""


class CancelOrder(RpcEndpoint):
  """Cancel Algo Order (TRADE)"""

  async def cancel_order(
    self,
    *,
    algo_id: int,
    validate: bool | None = None,
  ) -> AlgoCancelAck:
    """Cancel an open spot TWAP order.

    Args:
      algo_id: Algo order identifier to cancel.

    References:
      - [Official docs](https://developers.binance.com/docs/algo/spot-algo/Cancel-Algo-Order)
    """
    params: dict = {
      'algoId': algo_id,
    }
    _Response = AlgoCancelAck
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'DELETE',
      '/sapi/v1/algo/spot/order',
      params=params,
      validator=_validator,
      validate=validate,
    )
