# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class InterestRebateBalanceRecord(TypedDict):
  """One interest rebate balance change record."""

  type: NotRequired[Literal['ADD', 'DEDUCT', 'INTEREST_OFFSET']]
  """Record type."""
  groupId: NotRequired[int]
  """Loan group (risk unit) ID. Present only for `INTEREST_OFFSET` records."""
  rebateAsset: NotRequired[str]
  """Rebate asset (USDT)."""
  delta: NotRequired[str]
  """Amount change. Positive for `ADD`, negative for `DEDUCT`/`INTEREST_OFFSET`."""
  liabilityAsset: NotRequired[str]
  """Liability asset. Present only for `INTEREST_OFFSET` records."""
  deductedInterest: NotRequired[str]
  """Deducted interest, denominated in the liability asset. Present only for `INTEREST_OFFSET` records."""
  exchangeRate: NotRequired[str]
  """Exchange rate snapshot. Present only for `INTEREST_OFFSET` records."""
  createTime: NotRequired[int]
  """Record creation time, in milliseconds."""


class InterestRebateBalanceRecordsPage(TypedDict):
  """One page of interest rebate balance change records."""

  total: NotRequired[int]
  """Total number of records."""
  rows: NotRequired[list[InterestRebateBalanceRecord]]
  """Matching change records."""


class InterestRebateBalanceRecords(RpcEndpoint):
  """Query change records for an institutional loan interest rebate balance: `ADD` (admin grant), `DEDUCT` (admin deduction), and `INTEREST_OFFSET` (system interest deduction)."""

  async def interest_rebate_balance_records(
    self,
    *,
    type: Literal[0, 1, 2] | None = None,
    start_time: int | None = None,
    end_time: int | None = None,
    current: int | None = None,
    size: int | None = None,
    validate: bool | None = None,
  ) -> InterestRebateBalanceRecordsPage:
    """Query change records for an institutional loan interest rebate balance: `ADD` (admin grant), `DEDUCT` (admin deduction), and `INTEREST_OFFSET` (system interest deduction).

    Args:
      type: Record type filter. Returns all types when omitted.
      start_time: Start time in milliseconds.
      end_time: End time in milliseconds.
      current: Page number.
      size: Page size.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-institutional-loan/api/rest-api/borrow-repay#query-interest-rebate-balance-records)
    """
    params = {}
    if type is not None:
      params['type'] = type
    if start_time is not None:
      params['startTime'] = start_time
    if end_time is not None:
      params['endTime'] = end_time
    if current is not None:
      params['current'] = current
    if size is not None:
      params['size'] = size
    _Response = InterestRebateBalanceRecordsPage
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/sapi/v1/margin/loan-group/interest-rebate-balance/records',
      params=params,
      validator=_validator,
      validate=validate,
    )

  async def interest_rebate_balance_records_paged(
    self,
    *,
    type: Literal[0, 1, 2] | None = None,
    start_time: int | None = None,
    end_time: int | None = None,
    size: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[InterestRebateBalanceRecordsPage]:
    """Yield successive pages of `interest_rebate_balance_records`.

    Requests `current` from 1 upwards and stops once it has covered the `total` items
    the response reports, or after `max_pages` pages when one is given.
    """
    current = 1
    pages = 0
    while True:
      response = await self.interest_rebate_balance_records(
        type=type,
        start_time=start_time,
        end_time=end_time,
        size=size,
        current=current,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      total = response.get('total') if response is not None else None
      total = int(total) if total is not None else None
      if total is None or size is None or pages * size >= total:
        break
      current += 1
