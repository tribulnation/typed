# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class RiskUnitMember(TypedDict):
  """One account bound to the risk unit."""

  email: NotRequired[str]
  """Account registered email."""
  type: NotRequired[Literal['CREDIT', 'COLLATERAL']]
  """Credit sub-account or collateral sub-account."""
  enableMargin: NotRequired[bool]
  """`true` to include the account's Margin wallet as collateral, `false` to exclude it."""
  enableSpot: NotRequired[bool]
  """`true` to include the account's Spot wallet as collateral, `false` to exclude it."""


class ActiveRiskUnit(TypedDict):
  """One active risk unit."""

  groupId: NotRequired[int]
  """Risk unit unique identifier."""
  members: NotRequired[list[RiskUnitMember]]
  """Accounts bound to this risk unit."""
  createTime: NotRequired[int]
  """Risk unit creation timestamp, in milliseconds."""


class ActiveRiskUnits(RpcEndpoint):
  """Retrieve the list of active institutional loan risk units. Callable only with the parent account API key."""

  async def active_risk_units(
    self,
    *,
    validate: bool | None = None,
  ) -> list[ActiveRiskUnit]:
    """Retrieve the list of active institutional loan risk units. Callable only with the parent account API key.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-institutional-loan/api/rest-api/account#query-active-risk-units)
    """
    _Response = list[ActiveRiskUnit]
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/sapi/v1/margin/loan-groups/activated',
      validator=_validator,
      validate=validate,
    )
