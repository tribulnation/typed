# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class MarginAsset(TypedDict):
  """One cross margin asset's borrow/collateral details."""

  assetFullName: str
  """Full name of the asset."""
  assetName: str
  """Asset symbol, e.g. BNB."""
  isBorrowable: bool
  """Whether this asset can be borrowed on margin."""
  isMortgageable: bool
  """Whether this asset can be used as collateral."""
  userMinBorrow: str
  """Minimum amount this account can borrow, as a decimal string."""
  userMinRepay: str
  """Minimum amount this account can repay in one transaction, as a decimal string."""


class AllAssets(RpcEndpoint):
  """Get All Margin Assets"""

  async def all_assets(
    self,
    *,
    asset: str | None = None,
    validate: bool | None = None,
  ) -> list[MarginAsset]:
    """Query cross margin asset details -- whether an asset is borrowable and mortgageable (usable as collateral), and its minimum borrow/repay amounts. Returns all margin assets, or a single asset when `asset` is given.

    Args:
      asset: Asset to filter by, e.g. BNB. Omit to return all margin assets.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/market-data#get-all-margin-assets)
    """
    params = {}
    if asset is not None:
      params['asset'] = asset
    _Response = list[MarginAsset]
    _validator = validator[_Response](_Response)
    return await self.request(
      'GET',
      '/sapi/v1/margin/allAssets',
      params=params,
      validator=_validator,
      validate=validate,
    )
