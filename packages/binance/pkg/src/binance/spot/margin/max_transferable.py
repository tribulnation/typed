# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class MarginMaxTransferable(TypedDict):
  """Max transfer-out amount details for one asset."""

  amount: str
  """Amount currently transferable out of the margin account."""
  borrowLimit: NotRequired[str]
  """Max amount limited by the account level. Same field name Binance reuses from Query Max Borrow's response; here it caps the transfer-out amount rather than a borrow amount."""


class MaxTransferable(RpcEndpoint):
  """Query max transfer-out amount"""

  async def max_transferable(
    self,
    *,
    asset: str,
    isolated_symbol: str | None = None,
    validate: bool | None = None,
  ) -> MarginMaxTransferable:
    """Query the maximum amount of an asset this account can currently transfer out of margin, crossed or for a specific isolated symbol. If `isolatedSymbol` is not sent, crossed margin data is returned.

    Args:
      asset: Asset to query.
      isolated_symbol: Isolated margin symbol. If not sent, crossed margin data is returned.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/transfer#query-max-transfer-out-amount)
    """
    params: dict = {
      'asset': asset,
    }
    if isolated_symbol is not None:
      params['isolatedSymbol'] = isolated_symbol
    _Response = MarginMaxTransferable
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/sapi/v1/margin/maxTransferable',
      params=params,
      validator=_validator,
      validate=validate,
    )
