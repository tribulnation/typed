# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class MarginCancelOrderResult(TypedDict):
  """Result of canceling a single margin order."""

  symbol: str
  """Trading pair symbol."""
  orderId: int
  """Order id."""
  origClientOrderId: str
  """Client order id of the order as it was before this cancellation."""
  clientOrderId: str
  """Client id assigned to this cancellation request."""
  price: str
  """Order price."""
  origQty: str
  """Original order quantity."""
  executedQty: str
  """Cumulative filled quantity."""
  cummulativeQuoteQty: str
  """Cumulative quote asset transacted quantity."""
  status: Literal[
    'NEW',
    'PENDING_NEW',
    'PARTIALLY_FILLED',
    'FILLED',
    'CANCELED',
    'PENDING_CANCEL',
    'REJECTED',
    'EXPIRED',
    'EXPIRED_IN_MATCH',
  ]
  """Order status."""
  timeInForce: Literal['GTC', 'IOC', 'FOK']
  """Time in force."""
  type: Literal[
    'LIMIT',
    'MARKET',
    'STOP_LOSS',
    'STOP_LOSS_LIMIT',
    'TAKE_PROFIT',
    'TAKE_PROFIT_LIMIT',
    'LIMIT_MAKER',
  ]
  """Order type."""
  side: Literal['BUY', 'SELL']
  """Order side."""


class CancelOrder(RpcEndpoint):
  """Cancel an active order for a margin account. Either orderId or origClientOrderId must be sent."""

  async def cancel_order(
    self,
    *,
    symbol: str,
    is_isolated: Literal['TRUE', 'FALSE'] | None = None,
    order_id: int | None = None,
    orig_client_order_id: str | None = None,
    new_client_order_id: str | None = None,
    validate: bool | None = None,
  ) -> MarginCancelOrderResult:
    """Cancel an active order for a margin account. Either orderId or origClientOrderId must be sent.

    Args:
      symbol: Trading pair symbol, e.g. BNBUSDT.
      is_isolated: Margin account type for this order: isolated when TRUE, crossed (default) when FALSE.
      order_id: Order id. Either orderId or origClientOrderId must be sent.
      orig_client_order_id: Client order id of the order to cancel. Either orderId or origClientOrderId must be sent.
      new_client_order_id: Client id to uniquely identify this cancel. Automatically generated if not sent.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/trade#margin-account-cancel-order)
    """
    params: dict = {
      'symbol': symbol,
    }
    if is_isolated is not None:
      params['isIsolated'] = is_isolated
    if order_id is not None:
      params['orderId'] = order_id
    if orig_client_order_id is not None:
      params['origClientOrderId'] = orig_client_order_id
    if new_client_order_id is not None:
      params['newClientOrderId'] = new_client_order_id
    _Response = MarginCancelOrderResult
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'DELETE',
      '/sapi/v1/margin/order',
      params=params,
      validator=_validator,
      validate=validate,
    )
