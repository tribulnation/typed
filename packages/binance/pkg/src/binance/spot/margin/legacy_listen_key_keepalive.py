# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class LegacyListenKeyKeepalive(RpcEndpoint):
  """Keep a cross margin user data stream alive (legacy path), preventing it from timing out. Streams close after 60 minutes; a keepalive is recommended about every 30 minutes."""

  async def legacy_listen_key_keepalive(
    self,
    *,
    listen_key: str,
    validate: bool | None = None,
  ) -> dict[str, Any]:
    """Keep a cross margin user data stream alive (legacy path), preventing it from timing out. Streams close after 60 minutes; a keepalive is recommended about every 30 minutes.

    Args:
      listen_key: Listen key returned by `legacy_listen_key_create`, identifying the cross margin stream to extend.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/margin-api.md)
    """
    params: dict = {
      'listenKey': listen_key,
    }
    _Response = dict[str, Any]
    _validator = validator[_Response](_Response)
    return await self.request(
      'PUT',
      '/sapi/v1/userDataStream',
      params=params,
      validator=_validator,
      validate=validate,
    )
