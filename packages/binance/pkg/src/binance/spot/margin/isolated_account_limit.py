# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class IsolatedMarginAccountLimit(TypedDict):
  """This account's isolated margin account count and limit."""

  enabledAccount: int
  """Number of isolated margin accounts currently enabled."""
  maxAccount: int
  """Maximum number of isolated margin accounts this user is allowed to enable."""


class IsolatedAccountLimit(RpcEndpoint):
  """Query how many isolated margin accounts this user has enabled, and the maximum number allowed."""

  async def isolated_account_limit(
    self,
    *,
    validate: bool | None = None,
  ) -> IsolatedMarginAccountLimit:
    """Query how many isolated margin accounts this user has enabled, and the maximum number allowed.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/account#query-enabled-isolated-margin-account-limit)
    """
    _Response = IsolatedMarginAccountLimit
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/sapi/v1/margin/isolated/accountLimit',
      validator=_validator,
      validate=validate,
    )
