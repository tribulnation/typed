# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class MarginUserAsset(TypedDict):
  """One asset's balance within a cross margin account."""

  asset: str
  """Asset symbol, e.g. BTC."""
  borrowed: str
  """Amount of this asset currently borrowed."""
  free: str
  """Amount of this asset available for trading or withdrawal."""
  interest: str
  """Accrued, unpaid interest owed on the borrowed amount."""
  locked: str
  """Amount of this asset locked in open orders."""
  netAsset: str
  """Net balance of this asset (free plus locked minus borrowed and interest)."""


class MarginAccountDetails(TypedDict):
  """This account's cross margin account details."""

  created: bool
  """Whether a cross margin account has been created for this user."""
  borrowEnabled: bool
  """Whether cross margin borrowing is enabled for this account."""
  marginLevel: str
  """Account margin level, the ratio of total assets to total liabilities (in BTC). Falling below the venue's margin-call and liquidation thresholds triggers a margin call or forced liquidation, respectively."""
  collateralMarginLevel: str
  """Collateral margin level, an alternate risk ratio used for accounts holding non-BTC collateral."""
  totalAssetOfBtc: str
  """Total value of all assets held, denominated in BTC."""
  totalLiabilityOfBtc: str
  """Total value of all liabilities (borrowed principal plus accrued interest), denominated in BTC."""
  totalNetAssetOfBtc: str
  """Net asset value (total assets minus total liabilities), denominated in BTC."""
  totalCollateralValueInUSDT: str
  """Total collateral value backing this account, denominated in USDT."""
  totalOpenOrderLossInUSDT: NotRequired[str]
  """Estimated total loss this account would realize if all currently open orders were filled, denominated in USDT."""
  tradeEnabled: bool
  """Whether trading is currently enabled for this account."""
  transferEnabled: bool
  """Whether asset transfers are enabled for this account overall, separate from the in/out-specific flags below."""
  transferInEnabled: bool
  """Whether transferring assets into this margin account is currently enabled."""
  transferOutEnabled: bool
  """Whether transferring assets out of this margin account is currently enabled."""
  withdrawCollateralMarginLevel: NotRequired[str]
  """Collateral margin level used specifically for withdrawal eligibility checks."""
  accountType: str
  """Cross margin account tier."""
  userAssets: list[MarginUserAsset]
  """Per-asset balances held in this cross margin account."""


class Account(RpcEndpoint):
  """Query this account's cross margin account details: risk metrics, per-asset balances, and enabled trading/transfer flags."""

  async def account(self, *, validate: bool | None = None) -> MarginAccountDetails:
    """Query this account's cross margin account details: risk metrics, per-asset balances, and enabled trading/transfer flags.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/account#query-cross-margin-account-details)
    """
    _Response = MarginAccountDetails
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET', '/sapi/v1/margin/account', validator=_validator, validate=validate
    )
