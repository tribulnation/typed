# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class Response200(TypedDict):
  """Empty object returned on success."""


class SmallLiabilityExchange(RpcEndpoint):
  """Exchange this margin account's small liabilities (debt balances below the exchangeable threshold) named in `assetNames` into a single liability asset. Use `spot.margin.small_liability_exchange_coins` to discover which assets are eligible."""

  async def small_liability_exchange(
    self,
    *,
    asset_names: str,
    validate: bool | None = None,
  ) -> Response200:
    """Exchange this margin account's small liabilities (debt balances below the exchangeable threshold) named in `assetNames` into a single liability asset. Use `spot.margin.small_liability_exchange_coins` to discover which assets are eligible.

    Args:
      asset_names: Comma-separated list of asset names to exchange, e.g. "BTC,ETH". See `spot.margin.small_liability_exchange_coins` for eligible assets.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/trade#small-liability-exchange)
    """
    params: dict = {
      'assetNames': asset_names,
    }
    _Response = Response200
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/margin/exchange-small-liability',
      params=params,
      validator=_validator,
      validate=validate,
    )
