# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class MarginManualLiquidationResult(TypedDict):
  """A single liability repaid by this manual liquidation."""

  asset: str
  """Borrowed asset being liquidated."""
  interest: str
  """Accrued interest repaid, as a decimal string."""
  principal: str
  """Loan principal repaid, as a decimal string."""
  liabilityAsset: str
  """Asset the liability was denominated in."""
  liabilityQty: float
  """Total liability quantity repaid."""


class ManualLiquidation(RpcEndpoint):
  """Margin Manual Liquidation"""

  async def manual_liquidation(
    self,
    *,
    type: Literal['MARGIN', 'ISOLATED'],
    symbol: str | None = None,
    validate: bool | None = None,
  ) -> list[MarginManualLiquidationResult]:
    """Manually trigger liquidation of this margin account's (or an isolated symbol's) debt, repaying liabilities from available collateral.

    Args:
      type: Margin account type to liquidate.
      symbol: Isolated symbol to liquidate. Required when `type` is `ISOLATED`.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/trade#margin-manual-liquidation)
    """
    params: dict = {
      'type': type,
    }
    if symbol is not None:
      params['symbol'] = symbol
    _Response = list[MarginManualLiquidationResult]
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/margin/manual-liquidation',
      params=params,
      validator=_validator,
      validate=validate,
    )
