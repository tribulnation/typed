# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class MarginAccountSummary(TypedDict):
  """This account's personal margin-level risk thresholds."""

  normalBar: NotRequired[str]
  """Account's currently max borrowable amount with sufficient system availability -- the margin level below which borrowing is still allowed."""
  marginCallBar: NotRequired[str]
  """Margin level below which a margin call is triggered."""
  forceLiquidationBar: NotRequired[str]
  """Margin level below which positions are force-liquidated."""


class TradeCoeff(RpcEndpoint):
  """Get this margin account's personal margin-level thresholds: the borrowable bar, the margin-call bar, and the forced-liquidation bar."""

  async def trade_coeff(self, *, validate: bool | None = None) -> MarginAccountSummary:
    """Get this margin account's personal margin-level thresholds: the borrowable bar, the margin-call bar, and the forced-liquidation bar.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/account#get-summary-of-margin-account)
    """
    _Response = MarginAccountSummary
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET', '/sapi/v1/margin/tradeCoeff', validator=_validator, validate=validate
    )
