# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core import Timestamp
from binance.core.endpoint.rpc import RpcEndpoint


class MarginOrderListOrder(TypedDict):
  """One order belonging to the order list, identified minimally."""

  symbol: str
  """Trading pair symbol."""
  orderId: int
  """Order id."""
  clientOrderId: str
  """Client order id, either caller-supplied or venue-generated."""


class MarginOtoOrderReport(TypedDict):
  """One order-list leg's acknowledgement, returned when placing a new margin OTO or OTOCO."""

  symbol: str
  """Trading pair symbol."""
  orderId: int
  """Order id."""
  orderListId: int
  """Id of the order list this order belongs to."""
  clientOrderId: str
  """Client order id, either caller-supplied or venue-generated."""
  transactTime: Timestamp
  """Time the order was processed."""
  price: str
  """Order price."""
  origQty: str
  """Original order quantity."""
  executedQty: str
  """Cumulative filled quantity."""
  cummulativeQuoteQty: str
  """Cumulative quote asset transacted quantity."""
  status: Literal[
    'NEW',
    'PENDING_NEW',
    'PARTIALLY_FILLED',
    'FILLED',
    'CANCELED',
    'PENDING_CANCEL',
    'REJECTED',
    'EXPIRED',
    'EXPIRED_IN_MATCH',
  ]
  """Order status. The pending leg(s) of a freshly placed OTO/OTOCO report PENDING_NEW until the working order fills."""
  timeInForce: Literal['GTC', 'IOC', 'FOK']
  """Time in force."""
  type: Literal[
    'LIMIT',
    'MARKET',
    'STOP_LOSS',
    'STOP_LOSS_LIMIT',
    'TAKE_PROFIT',
    'TAKE_PROFIT_LIMIT',
    'LIMIT_MAKER',
  ]
  """Order type."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  stopPrice: NotRequired[str]
  """Price at which the leg's stop triggers. Present only for STOP_LOSS/STOP_LOSS_LIMIT legs."""
  selfTradePreventionMode: Literal[
    'EXPIRE_TAKER', 'EXPIRE_MAKER', 'EXPIRE_BOTH', 'NONE'
  ]
  """Self-trade prevention mode in effect for the order."""


class MarginNewOtoOrderResult(TypedDict):
  """The placed margin OTO order list."""

  orderListId: int
  """Id of the order list."""
  contingencyType: Literal['OTO']
  """Contingency type of the order list."""
  listStatusType: Literal['RESPONSE', 'EXEC_STARTED', 'UPDATED', 'ALL_DONE']
  """Status of the order list."""
  listOrderStatus: Literal['EXECUTING', 'ALL_DONE', 'REJECT']
  """Execution status of the order list."""
  listClientOrderId: str
  """Client id for the entire order list, either caller-supplied or venue-generated."""
  transactionTime: Timestamp
  """Time the order list was processed."""
  symbol: str
  """Trading pair symbol."""
  isIsolated: bool
  """Whether the order list belongs to an isolated margin account."""
  orders: list[MarginOrderListOrder]
  """The working and pending legs making up the OTO, identified minimally."""
  orderReports: list[MarginOtoOrderReport]
  """Per-leg order acknowledgements. The pending leg reports PENDING_NEW until the working leg fully fills."""


class NewOtoOrder(RpcEndpoint):
  """Place a new one-triggers-the-other (OTO) order list for a margin account, comprised of 2 orders. The first, the working order, must be LIMIT or LIMIT_MAKER; only it goes on the order book initially. The second, the pending order, can be any type except a MARKET order using quoteOrderQty; it is only placed once the working order is fully filled. If either order is individually canceled, the other is also canceled or expired. An OTO adds 2 orders to the unfilled order count and to the EXCHANGE_MAX_NUM_ORDERS/MAX_NUM_ORDERS filters."""

  async def new_oto_order(
    self,
    *,
    symbol: str,
    is_isolated: Literal['TRUE', 'FALSE'] | None = None,
    list_client_order_id: str | None = None,
    new_order_resp_type: Literal['ACK', 'RESULT', 'FULL'] | None = None,
    side_effect_type: Literal['NO_SIDE_EFFECT', 'MARGIN_BUY'] | None = None,
    self_trade_prevention_mode: Literal[
      'EXPIRE_TAKER', 'EXPIRE_MAKER', 'EXPIRE_BOTH', 'NONE'
    ]
    | None = None,
    auto_repay_at_cancel: bool | None = None,
    working_type: Literal['LIMIT', 'LIMIT_MAKER'],
    working_side: Literal['BUY', 'SELL'],
    working_client_order_id: str | None = None,
    working_price: float,
    working_quantity: float,
    working_iceberg_qty: float | None = None,
    working_time_in_force: Literal['GTC', 'IOC', 'FOK'] | None = None,
    pending_type: Literal[
      'LIMIT',
      'MARKET',
      'STOP_LOSS',
      'STOP_LOSS_LIMIT',
      'TAKE_PROFIT',
      'TAKE_PROFIT_LIMIT',
      'LIMIT_MAKER',
    ],
    pending_side: Literal['BUY', 'SELL'],
    pending_client_order_id: str | None = None,
    pending_price: float | None = None,
    pending_stop_price: float | None = None,
    pending_trailing_delta: float | None = None,
    pending_quantity: float,
    pending_iceberg_qty: float | None = None,
    pending_time_in_force: Literal['GTC', 'IOC', 'FOK'] | None = None,
    validate: bool | None = None,
  ) -> MarginNewOtoOrderResult:
    """Place a new one-triggers-the-other (OTO) order list for a margin account, comprised of 2 orders. The first, the working order, must be LIMIT or LIMIT_MAKER; only it goes on the order book initially. The second, the pending order, can be any type except a MARKET order using quoteOrderQty; it is only placed once the working order is fully filled. If either order is individually canceled, the other is also canceled or expired. An OTO adds 2 orders to the unfilled order count and to the EXCHANGE_MAX_NUM_ORDERS/MAX_NUM_ORDERS filters.

    Args:
      symbol: Trading pair symbol, e.g. BNBUSDT.
      is_isolated: Margin account type for this order: isolated when TRUE, crossed (default) when FALSE.
      list_client_order_id: Arbitrary unique id among open order lists. Automatically generated if not sent. A new order list with the same listClientOrderId is accepted only when the previous one is filled or completely expired; distinct from workingClientOrderId and pendingClientOrderId.
      new_order_resp_type: Response shape to return for each leg's orderReports entry.
      side_effect_type: Margin borrow side effect of this order list. Defaults to NO_SIDE_EFFECT.
      self_trade_prevention_mode: Self-trade prevention mode. The allowed values depend on the symbol's configuration.
      auto_repay_at_cancel: Only relevant when sideEffectType is MARGIN_BUY: whether the debt generated by the order should be repaid automatically once the order is canceled. Defaults to true.
      working_type: Order type of the working order. Initially, only the working order goes on the order book.
      working_side: Order side of the working order.
      working_client_order_id: Arbitrary unique id among open orders for the working order. Automatically generated if not sent.
      working_price: Price of the working order.
      working_quantity: Quantity of the working order.
      working_iceberg_qty: Iceberg quantity for the working order. Usable only if workingTimeInForce is GTC.
      working_time_in_force: Time in force for the working order.
      pending_type: Order type of the pending order. The pending order is only placed on the order book once the working order is fully filled. MARKET orders using quoteOrderQty are not supported.
      pending_side: Order side of the pending order.
      pending_client_order_id: Arbitrary unique id among open orders for the pending order. Automatically generated if not sent.
      pending_price: Price of the pending order.
      pending_stop_price: Trigger price of the pending order. Used with STOP_LOSS/TAKE_PROFIT or STOP_LOSS_LIMIT/TAKE_PROFIT_LIMIT.
      pending_trailing_delta: Trailing delta for the pending order. Used with STOP_LOSS/TAKE_PROFIT or STOP_LOSS_LIMIT/TAKE_PROFIT_LIMIT.
      pending_quantity: Quantity of the pending order.
      pending_iceberg_qty: Iceberg quantity for the pending order. Usable only if pendingTimeInForce is GTC.
      pending_time_in_force: Time in force for the pending order.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/trade#margin-account-new-oto)
    """
    params: dict = {
      'symbol': symbol,
      'workingType': working_type,
      'workingSide': working_side,
      'workingPrice': working_price,
      'workingQuantity': working_quantity,
      'pendingType': pending_type,
      'pendingSide': pending_side,
      'pendingQuantity': pending_quantity,
    }
    if is_isolated is not None:
      params['isIsolated'] = is_isolated
    if list_client_order_id is not None:
      params['listClientOrderId'] = list_client_order_id
    if new_order_resp_type is not None:
      params['newOrderRespType'] = new_order_resp_type
    if side_effect_type is not None:
      params['sideEffectType'] = side_effect_type
    if self_trade_prevention_mode is not None:
      params['selfTradePreventionMode'] = self_trade_prevention_mode
    if auto_repay_at_cancel is not None:
      params['autoRepayAtCancel'] = auto_repay_at_cancel
    if working_client_order_id is not None:
      params['workingClientOrderId'] = working_client_order_id
    if working_iceberg_qty is not None:
      params['workingIcebergQty'] = working_iceberg_qty
    if working_time_in_force is not None:
      params['workingTimeInForce'] = working_time_in_force
    if pending_client_order_id is not None:
      params['pendingClientOrderId'] = pending_client_order_id
    if pending_price is not None:
      params['pendingPrice'] = pending_price
    if pending_stop_price is not None:
      params['pendingStopPrice'] = pending_stop_price
    if pending_trailing_delta is not None:
      params['pendingTrailingDelta'] = pending_trailing_delta
    if pending_iceberg_qty is not None:
      params['pendingIcebergQty'] = pending_iceberg_qty
    if pending_time_in_force is not None:
      params['pendingTimeInForce'] = pending_time_in_force
    _Response = MarginNewOtoOrderResult
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/margin/order/oto',
      params=params,
      validator=_validator,
      validate=validate,
    )
