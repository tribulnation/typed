# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class SpecialKeyIpUpdate(RpcEndpoint):
  """Update the IP whitelist of a special (low-latency trading) API key. Omit `symbol` to update the cross margin special key's whitelist, or set it to update an isolated margin symbol's special key whitelist."""

  async def special_key_ip_update(
    self,
    *,
    ip: str,
    symbol: str | None = None,
    validate: bool | None = None,
  ) -> dict[str, Any]:
    """Update the IP whitelist of a special (low-latency trading) API key. Omit `symbol` to update the cross margin special key's whitelist, or set it to update an isolated margin symbol's special key whitelist.

    Args:
      ip: Trusted IP addresses to whitelist, comma-separated. Can be added in batches; max 30 per API key.
      symbol: Isolated margin trading pair, e.g. BTCUSDT. Omit to update the cross margin special key's whitelist; set to update an isolated margin symbol's special key whitelist instead.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/trade#edit-ip-for-special-key)
    """
    params: dict = {
      'ip': ip,
    }
    if symbol is not None:
      params['symbol'] = symbol
    _Response = dict[str, Any]
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'PUT',
      '/sapi/v1/margin/apiKey/ip',
      params=params,
      validator=_validator,
      validate=validate,
    )
