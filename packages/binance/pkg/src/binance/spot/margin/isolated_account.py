# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class IsolatedMarginBaseAsset(TypedDict):
  """Balance and permissions for this pair's base asset."""

  asset: str
  """Base asset symbol, e.g. BTC."""
  borrowEnabled: bool
  """Whether borrowing the base asset is enabled on this isolated pair."""
  borrowed: str
  """Amount of the base asset currently borrowed."""
  free: str
  """Amount of the base asset available for trading or withdrawal."""
  interest: str
  """Accrued, unpaid interest owed on the borrowed base asset."""
  locked: str
  """Amount of the base asset locked in open orders."""
  netAsset: str
  """Net balance of the base asset (free plus locked minus borrowed and interest)."""
  netAssetOfBtc: str
  """Net balance of the base asset, converted to BTC."""
  repayEnabled: bool
  """Whether repaying the base asset loan is enabled on this isolated pair."""
  totalAsset: str
  """Total base asset balance (free plus locked)."""


class IsolatedMarginQuoteAsset(TypedDict):
  """Balance and permissions for this pair's quote asset."""

  asset: str
  """Quote asset symbol, e.g. USDT."""
  borrowEnabled: bool
  """Whether borrowing the quote asset is enabled on this isolated pair."""
  borrowed: str
  """Amount of the quote asset currently borrowed."""
  free: str
  """Amount of the quote asset available for trading or withdrawal."""
  interest: str
  """Accrued, unpaid interest owed on the borrowed quote asset."""
  locked: str
  """Amount of the quote asset locked in open orders."""
  netAsset: str
  """Net balance of the quote asset (free plus locked minus borrowed and interest)."""
  netAssetOfBtc: str
  """Net balance of the quote asset, converted to BTC."""
  repayEnabled: bool
  """Whether repaying the quote asset loan is enabled on this isolated pair."""
  totalAsset: str
  """Total quote asset balance (free plus locked)."""


class IsolatedMarginAccountAsset(TypedDict):
  """One isolated margin pair's account state."""

  baseAsset: IsolatedMarginBaseAsset
  quoteAsset: IsolatedMarginQuoteAsset
  symbol: str
  """Isolated margin trading pair, e.g. BTCUSDT."""
  isolatedCreated: bool
  """Whether this isolated margin pair account has been created."""
  enabled: bool
  """Whether this isolated margin pair is currently enabled for trading."""
  marginLevel: str
  """This isolated pair's margin level, the ratio of total assets to total liabilities."""
  marginLevelStatus: str
  """This isolated pair's risk status derived from its margin level."""
  marginRatio: str
  """This isolated pair's margin ratio."""
  indexPrice: str
  """Index price used to value this isolated pair's assets."""
  liquidatePrice: str
  """Price at which this isolated pair would be forcibly liquidated."""
  liquidateRate: str
  """Liquidation rate applied to this isolated pair."""
  tradeEnabled: bool
  """Whether trading is currently enabled on this isolated pair."""


class IsolatedMarginAccountInfo(TypedDict):
  """This account's isolated margin account info."""

  assets: list[IsolatedMarginAccountAsset]
  """One entry per isolated margin pair held by this account."""
  totalAssetOfBtc: str
  """Total value of all isolated assets held, denominated in BTC."""
  totalLiabilityOfBtc: str
  """Total value of all isolated liabilities, denominated in BTC."""
  totalNetAssetOfBtc: str
  """Net asset value across all isolated pairs, denominated in BTC."""


class IsolatedAccount(RpcEndpoint):
  """Query this account's isolated margin account info. If `symbols` is omitted, all isolated pairs the account holds are returned; otherwise only the isolated pairs named in `symbols`."""

  async def isolated_account(
    self,
    *,
    symbols: str | None = None,
    validate: bool | None = None,
  ) -> IsolatedMarginAccountInfo:
    """Query this account's isolated margin account info. If `symbols` is omitted, all isolated pairs the account holds are returned; otherwise only the isolated pairs named in `symbols`.

    Args:
      symbols: Comma-separated isolated margin symbols to filter by, e.g. "BTCUSDT,BNBUSDT,ADAUSDT". Up to 5 symbols. Omit to return all isolated pairs.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/account#query-isolated-margin-account-info)
    """
    params = {}
    if symbols is not None:
      params['symbols'] = symbols
    _Response = IsolatedMarginAccountInfo
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/sapi/v1/margin/isolated/account',
      params=params,
      validator=_validator,
      validate=validate,
    )
