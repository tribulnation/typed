# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core import Timestamp
from binance.core.endpoint.rpc import RpcEndpoint


class PredictionActiveOrder(TypedDict):
  """One active prediction order."""

  orderId: NotRequired[str]
  """Internal order ID."""
  vendorOrderId: NotRequired[str]
  """Vendor-side (on-chain) order identifier."""
  vendor: NotRequired[str]
  """Prediction liquidity vendor executing this order."""
  marketTopicId: NotRequired[int]
  """Market topic ID."""
  slug: NotRequired[str]
  """URL-friendly topic slug."""
  marketTopicTitle: NotRequired[str]
  """Market topic title."""
  marketId: NotRequired[int]
  """Market ID."""
  marketTitle: NotRequired[str]
  """Market title."""
  outcome: NotRequired[str]
  """Outcome name this order trades."""
  outcomeIndex: NotRequired[int]
  """Zero-based outcome index."""
  status: NotRequired[str]
  """Order status."""
  side: NotRequired[Literal['BUY', 'SELL']]
  """Trade direction."""
  orderType: NotRequired[Literal['MARKET', 'LIMIT']]
  """Order type."""
  createTime: NotRequired[Timestamp]
  """Time the order was created."""
  modifyTime: NotRequired[Timestamp]
  """Time the order was last modified."""
  makerUsdtAmount: NotRequired[str]
  """Order amount in USDT terms, as a decimal string."""
  makerShareQty: NotRequired[str]
  """Order quantity in outcome shares, as a decimal string."""
  filledUsdtAmount: NotRequired[str]
  """Filled amount in USDT terms, as a decimal string."""
  filledShareQty: NotRequired[str]
  """Filled quantity in outcome shares, as a decimal string."""
  fillPercentage: NotRequired[str]
  """Fraction of the order filled so far, as a decimal string."""
  price: NotRequired[str]
  """Order price, as a decimal string."""
  marketProviderFee: NotRequired[str]
  """Fee charged by the market provider, as a decimal string."""
  networkFee: NotRequired[str]
  """On-chain network fee, as a decimal string."""


class PredictionActiveOrderList(TypedDict):
  """This user's active prediction orders."""

  total: NotRequired[int]
  """Total number of active orders matching the filter."""
  offset: NotRequired[int]
  """Pagination offset used for this page."""
  limit: NotRequired[int]
  """Page size used for this page."""
  orders: NotRequired[list[PredictionActiveOrder]]
  """Active orders on this page."""


class ActiveOrders(RpcEndpoint):
  """Get active (open) prediction orders for the authenticated user."""

  async def active_orders(
    self,
    *,
    wallet_address: str,
    trade_side: Literal['BUY', 'SELL'] | None = None,
    l1category: str | None = None,
    market_id: int | None = None,
    offset: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PredictionActiveOrderList:
    """Get active (open) prediction orders for the authenticated user.

    Args:
      wallet_address: User's prediction wallet address.
      trade_side: Filter by trade side.
      l1category: Filter by level-1 category.
      market_id: Filter by market ID.
      offset: Pagination offset. Default `0`.
      limit: Page size. Default `20`, range 1-100.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/trade#query-active-orders)
    """
    params: dict = {
      'walletAddress': wallet_address,
    }
    if trade_side is not None:
      params['tradeSide'] = trade_side
    if l1category is not None:
      params['l1Category'] = l1category
    if market_id is not None:
      params['marketId'] = market_id
    if offset is not None:
      params['offset'] = offset
    if limit is not None:
      params['limit'] = limit
    _Response = PredictionActiveOrderList
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/sapi/v1/w3w/wallet/prediction/order/list',
      params=params,
      validator=_validator,
      validate=validate,
    )

  async def active_orders_paged(
    self,
    *,
    wallet_address: str,
    trade_side: Literal['BUY', 'SELL'] | None = None,
    l1category: str | None = None,
    market_id: int | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[PredictionActiveOrderList]:
    """Yield successive pages of `active_orders`.

    Advances `offset` by `(limit if limit is not None else 20)` and stops once it has
    covered the `total` items the response reports, or after `max_pages` pages when one
    is given.
    """
    offset = 0
    pages = 0
    while True:
      response = await self.active_orders(
        wallet_address=wallet_address,
        trade_side=trade_side,
        l1category=l1category,
        market_id=market_id,
        limit=limit,
        offset=offset,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      total = response.get('total') if response is not None else None
      total = int(total) if total is not None else None
      if total is None or limit is None or pages * limit >= total:
        break
      offset += limit if limit is not None else 20
