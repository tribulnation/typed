# Generated by typed-dev — see CONTRIBUTING.md.
from json import dumps
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class PredictionCancelFailure(TypedDict):
  """One order that could not be canceled."""

  orderId: NotRequired[str]
  """Internal order ID that failed to cancel."""
  reason: NotRequired[str]
  """Reason the cancellation failed."""


class PredictionCancelInfo(TypedDict):
  """One order to cancel."""

  orderId: str
  """Internal order ID to cancel."""


class PredictionBatchCancelResult(TypedDict):
  """Results of a batch order cancellation."""

  canceled: NotRequired[list[str]]
  """Order IDs successfully canceled."""
  failed: NotRequired[list[PredictionCancelFailure]]
  """Orders that could not be canceled."""


class BatchCancel(RpcEndpoint):
  """Cancel one or more active prediction orders in a single request. Requires SAS authorization. `vendor` does not need to be supplied per item; the server sets it automatically for every item in the batch.

  Known bracket-notation encoding trap: this endpoint's nested `cancelInfoList[i].orderId` fields use indexed bracket notation in the form-encoded body, and Binance's SAPI signature verification runs over the raw, unencoded canonical string. Mainstream HTTP libraries (Python `requests`, Java `HttpURLConnection`/`URI`, Go `net/url`, Node.js `url`) automatically percent-encode `[` to `%5B` and `]` to `%5D`, which changes the signed bytes and produces `-1022 Signature for this request is not valid`. Workarounds: build the raw body string by hand and send it over a low-level HTTP API that does not normalize URLs -- Python's `http.client`, Java's `HttpURLConnection` writing raw body bytes, or Go's `strings.NewReader` in place of `url.Values.Encode()`."""

  async def batch_cancel(
    self,
    *,
    wallet_address: str,
    wallet_id: str,
    cancel_info_list: list[PredictionCancelInfo] | None = None,
    validate: bool | None = None,
  ) -> PredictionBatchCancelResult:
    """Cancel one or more active prediction orders in a single request. Requires SAS authorization. `vendor` does not need to be supplied per item; the server sets it automatically for every item in the batch.

    Known bracket-notation encoding trap: this endpoint's nested `cancelInfoList[i].orderId` fields use indexed bracket notation in the form-encoded body, and Binance's SAPI signature verification runs over the raw, unencoded canonical string. Mainstream HTTP libraries (Python `requests`, Java `HttpURLConnection`/`URI`, Go `net/url`, Node.js `url`) automatically percent-encode `[` to `%5B` and `]` to `%5D`, which changes the signed bytes and produces `-1022 Signature for this request is not valid`. Workarounds: build the raw body string by hand and send it over a low-level HTTP API that does not normalize URLs -- Python's `http.client`, Java's `HttpURLConnection` writing raw body bytes, or Go's `strings.NewReader` in place of `url.Values.Encode()`.

    Args:
      wallet_address: User's prediction wallet address.
      wallet_id: Wallet ID.
      cancel_info_list: Orders to cancel, indexed from 0 as `cancelInfoList[0].orderId`, `cancelInfoList[1].orderId`, etc. -- see the encoding trap above.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/trade#batch-cancel-orders)
    """
    params: dict = {
      'walletAddress': wallet_address,
      'walletId': wallet_id,
    }
    if cancel_info_list is not None:
      params['cancelInfoList'] = dumps(cancel_info_list, separators=(',', ':'))
    _Response = PredictionBatchCancelResult
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/w3w/wallet/prediction/trade/batch-cancel',
      params=params,
      validator=_validator,
      validate=validate,
    )
