# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class PredictionRedeemStatus(TypedDict):
  """On-chain status of a redeem transaction."""

  txHash: NotRequired[str]
  """Redeem transaction hash."""
  status: NotRequired[Literal['PENDING', 'CONFIRMED', 'FAILED', 'NOT_FOUND']]
  """On-chain transaction status."""


class RedeemStatus(RpcEndpoint):
  """Query the on-chain transaction status of a previously submitted redeem request."""

  async def redeem_status(
    self,
    *,
    wallet_address: str,
    tx_hash: str,
    validate: bool | None = None,
  ) -> PredictionRedeemStatus:
    """Query the on-chain transaction status of a previously submitted redeem request.

    Args:
      wallet_address: User's prediction wallet address.
      tx_hash: Redeem transaction hash.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/redeem#get-redeem-status)
    """
    params: dict = {
      'walletAddress': wallet_address,
      'txHash': tx_hash,
    }
    _Response = PredictionRedeemStatus
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/sapi/v1/w3w/wallet/prediction/redeem/status',
      params=params,
      validator=_validator,
      validate=validate,
    )
