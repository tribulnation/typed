# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class FeeGroupMember(TypedDict):
  """A sub-account assigned to a fee group, with its effective commission markup rates."""

  subUserId: NotRequired[int]
  """Sub-user ID."""
  groupCode: NotRequired[str]
  """Fee group code."""
  groupName: NotRequired[str]
  """Fee group name."""
  makerMarkup: NotRequired[str]
  """Maker markup rate, as a decimal string."""
  takerMarkup: NotRequired[str]
  """Taker markup rate, as a decimal string."""


class QueryOneMembersFeeGroupResponse(TypedDict):
  """Single-member fee group lookup response frame. This product line wraps its business data in {code,message,data} on the wire, and the core does not strip it -- see spec/core.md's Envelope section."""

  code: NotRequired[str]
  """Response code. "000000" indicates success."""
  message: NotRequired[str]
  """Response message."""
  data: NotRequired[FeeGroupMember]


class MemberFeeGroup(RpcEndpoint):
  """Query which commission fee group a single sub-account is assigned to."""

  async def member_fee_group(
    self,
    *,
    sub_user_id: int,
    validate: bool | None = None,
  ) -> QueryOneMembersFeeGroupResponse:
    """Query which commission fee group a single sub-account is assigned to.

    Args:
      sub_user_id: Sub-user ID.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-vip-caas/api/rest-api/fee-groups#query-one-members-fee-group)
    """
    params: dict = {
      'subUserId': sub_user_id,
    }
    _Response = QueryOneMembersFeeGroupResponse
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/sapi/v1/vip/caas/commission/member-fee-group',
      params=params,
      validator=_validator,
      validate=validate,
    )
