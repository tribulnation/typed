# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class FeeGroup(TypedDict):
  """A commission fee group."""

  groupCode: NotRequired[str]
  """Fee group code."""
  groupName: NotRequired[str]
  """Fee group name."""
  makerMarkup: NotRequired[str]
  """Maker markup rate, as a decimal string."""
  takerMarkup: NotRequired[str]
  """Taker markup rate, as a decimal string."""
  subAccountCount: NotRequired[int]
  """Number of sub-accounts currently assigned to this fee group."""
  createdTime: NotRequired[int]
  """Millisecond epoch time the fee group was created."""
  updatedTime: NotRequired[int]
  """Millisecond epoch time the fee group was last updated."""


class CreateFeeGroupResponse(TypedDict):
  """Fee group creation response frame. This product line wraps its business data in {code,message,data} on the wire, and the core does not strip it -- see spec/core.md's Envelope section."""

  code: NotRequired[str]
  """Response code. "000000" indicates success."""
  message: NotRequired[str]
  """Response message."""
  data: NotRequired[FeeGroup]


class FeeGroupCreate(RpcEndpoint):
  """Create a new commission fee group for the authenticated master account. Requires the API key's "trade" option to be enabled."""

  async def fee_group_create(
    self,
    *,
    group_name: str,
    maker_markup: float,
    taker_markup: float,
    validate: bool | None = None,
  ) -> CreateFeeGroupResponse:
    """Create a new commission fee group for the authenticated master account. Requires the API key's "trade" option to be enabled.

    Args:
      group_name: Fee group name (max 32 characters).
      maker_markup: Maker markup rate, e.g. 0.002.
      taker_markup: Taker markup rate, e.g. 0.003.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-vip-caas/api/rest-api/fee-groups#create-fee-group)
    """
    params: dict = {
      'groupName': group_name,
      'makerMarkup': maker_markup,
      'takerMarkup': taker_markup,
    }
    _Response = CreateFeeGroupResponse
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/vip/caas/commission/fee-groups',
      params=params,
      validator=_validator,
      validate=validate,
    )
