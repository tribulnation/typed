# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class BrokerSpotTransfer(TypedDict):
  """Result of a spot transfer between broker/master and a sub-account."""

  txnId: str
  """Transfer transaction identifier."""
  clientTranId: str
  """Caller-supplied idempotency id echoed back, if any."""


class SubAccountTransferSpot(RpcEndpoint):
  """Sub Account Transfer (SPOT)"""

  async def sub_account_transfer_spot(
    self,
    *,
    from_id: str | None = None,
    to_id: str | None = None,
    client_tran_id: str | None = None,
    asset: str,
    amount: float,
    validate: bool | None = None,
  ) -> BrokerSpotTransfer:
    """Transfer a spot asset between the master account and a sub-account, or between two sub-accounts.

    Args:
      from_id: Source sub-account. Transfers from the master account if omitted.
      to_id: Destination sub-account. Transfers to the master account if omitted.
      client_tran_id: Caller-supplied idempotency id. Max 32 characters.
      asset: Asset to transfer.
      amount: Amount to transfer.

    References:
      - [Official docs](https://binance-docs.github.io/Brokerage-API/Brokerage_Operation_Endpoints/)
    """
    params: dict = {
      'asset': asset,
      'amount': amount,
    }
    if from_id is not None:
      params['fromId'] = from_id
    if to_id is not None:
      params['toId'] = to_id
    if client_tran_id is not None:
      params['clientTranId'] = client_tran_id
    _Response = BrokerSpotTransfer
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/broker/transfer',
      params=params,
      validator=_validator,
      validate=validate,
    )
