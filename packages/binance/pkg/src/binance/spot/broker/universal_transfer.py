# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class BrokerUniversalTransfer(TypedDict):
  """Result of a universal transfer."""

  txnId: int
  """Transfer transaction identifier."""
  clientTranId: str
  """Caller-supplied idempotency id echoed back, if any."""


class UniversalTransfer(RpcEndpoint):
  """Universal Transfer"""

  async def universal_transfer(
    self,
    *,
    from_id: str | None = None,
    to_id: str | None = None,
    from_account_type: Literal['SPOT', 'USDT_FUTURE', 'COIN_FUTURE'],
    to_account_type: Literal['SPOT', 'USDT_FUTURE', 'COIN_FUTURE'],
    client_tran_id: str | None = None,
    asset: str,
    amount: float,
    validate: bool | None = None,
  ) -> BrokerUniversalTransfer:
    """Transfer an asset between any two account/product types (spot, USDⓈ-M futures, COIN-M futures) across the master account and sub-accounts.

    Args:
      from_id: Source sub-account. Master account if omitted.
      to_id: Destination sub-account. Master account if omitted.
      from_account_type: Source account type.
      to_account_type: Destination account type.
      client_tran_id: Caller-supplied idempotency id. Max 32 characters.
      asset: Asset to transfer.
      amount: Amount to transfer.

    References:
      - [Official docs](https://binance-docs.github.io/Brokerage-API/Brokerage_Operation_Endpoints/)
    """
    params: dict = {
      'fromAccountType': from_account_type,
      'toAccountType': to_account_type,
      'asset': asset,
      'amount': amount,
    }
    if from_id is not None:
      params['fromId'] = from_id
    if to_id is not None:
      params['toId'] = to_id
    if client_tran_id is not None:
      params['clientTranId'] = client_tran_id
    _Response = BrokerUniversalTransfer
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/broker/universalTransfer',
      params=params,
      validator=_validator,
      validate=validate,
    )
