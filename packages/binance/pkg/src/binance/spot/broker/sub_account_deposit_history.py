# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, Literal, TypedDict
from typed_core.validation import validator
from binance.core import Timestamp, timestamp
from binance.core.endpoint.rpc import RpcEndpoint


class BrokerSubAccountDeposit(TypedDict):
  """One sub-account deposit."""

  depositId: int
  """Deposit identifier."""
  subAccountId: str
  """Sub-account the deposit landed in."""
  address: str
  """Deposit address credited."""
  amount: str
  """Amount deposited."""
  coin: str
  """Asset deposited."""
  insertTime: Timestamp
  """Time the deposit was recorded."""
  transferType: int
  """Whether this was an internal transfer. Documented example is `0`; the venue does not declare this as a closed enum."""
  network: str
  """Network the deposit arrived over."""
  status: Literal[0, 1, 6]
  """Deposit status."""
  txId: str
  """On-chain transaction id."""


class SubAccountDepositHistory(RpcEndpoint):
  """Get Sub Account Deposit History"""

  async def sub_account_deposit_history(
    self,
    *,
    sub_account_id: str | None = None,
    coin: str | None = None,
    status: Literal[0, 1, 6] | None = None,
    start_time: Timestamp | None = None,
    end_time: Timestamp | None = None,
    limit: int | None = None,
    offset: int | None = None,
    validate: bool | None = None,
  ) -> list[BrokerSubAccountDeposit]:
    """Get deposit history across sub-accounts.

    Args:
      sub_account_id: Sub-account identifier. All sub-accounts if omitted.
      coin: Filter by deposited asset.
      status: Filter by deposit status.
      start_time: Start of the query window, UTC ms. Defaults to 7 days before now.
      end_time: End of the query window, UTC ms. Defaults to now.
      limit: Maximum records to return. Default 500.
      offset: Number of records to skip. Default 0.

    References:
      - [Official docs](https://binance-docs.github.io/Brokerage-API/Brokerage_Operation_Endpoints/)
    """
    params = {}
    if sub_account_id is not None:
      params['subAccountId'] = sub_account_id
    if coin is not None:
      params['coin'] = coin
    if status is not None:
      params['status'] = status
    if start_time is not None:
      params['startTime'] = timestamp.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp.dump(end_time)
    if limit is not None:
      params['limit'] = limit
    if offset is not None:
      params['offset'] = offset
    _Response = list[BrokerSubAccountDeposit]
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/sapi/v1/broker/subAccount/depositHist',
      params=params,
      validator=_validator,
      validate=validate,
    )

  async def sub_account_deposit_history_paged(
    self,
    *,
    sub_account_id: str | None = None,
    coin: str | None = None,
    status: Literal[0, 1, 6] | None = None,
    start_time: Timestamp | None = None,
    end_time: Timestamp | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[list[BrokerSubAccountDeposit]]:
    """Yield successive pages of `sub_account_deposit_history`.

    Advances `offset` by `len(response)` and stops on the first page shorter than
    `limit`, or after `max_pages` pages when one is given.
    """
    offset = 0
    pages = 0
    while True:
      response = await self.sub_account_deposit_history(
        sub_account_id=sub_account_id,
        coin=coin,
        status=status,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        offset=offset,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      if not response or (limit is not None and len(response) < limit):
        break
      offset += len(response)
