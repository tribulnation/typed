# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_core.validation import validator
from binance.core import Timestamp
from binance.core.endpoint.rpc import RpcEndpoint


class BrokerSubAccountApiIpRestrictionDeleted(TypedDict):
  """Resulting IP whitelist after deletion."""

  subaccountId: str
  """Sub-account identifier."""
  apikey: str
  """API key this restriction applies to."""
  ipList: list[str]
  """Remaining whitelisted IP addresses."""
  updateTime: Timestamp
  """Time this restriction was last updated."""


class DeleteSubAccountApiIpRestriction(RpcEndpoint):
  """Delete IP Restriction for Sub Account Api Key"""

  async def delete_sub_account_api_ip_restriction(
    self,
    *,
    sub_account_id: str,
    sub_account_api_key: str,
    ip_address: str | None = None,
    validate: bool | None = None,
  ) -> BrokerSubAccountApiIpRestrictionDeleted:
    """Remove one (or all) IP addresses from a sub-account API key's whitelist.

    Args:
      sub_account_id: Sub-account identifier.
      sub_account_api_key: Sub-account API key.
      ip_address: IP address to remove. Removes the whole whitelist (unrestricts the key) if omitted.

    References:
      - [Official docs](https://binance-docs.github.io/Brokerage-API/Brokerage_Operation_Endpoints/)
    """
    params: dict = {
      'subAccountId': sub_account_id,
      'subAccountApiKey': sub_account_api_key,
    }
    if ip_address is not None:
      params['ipAddress'] = ip_address
    _Response = BrokerSubAccountApiIpRestrictionDeleted
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'DELETE',
      '/sapi/v1/broker/subAccountApi/ipRestriction/ipList',
      params=params,
      validator=_validator,
      validate=validate,
    )
