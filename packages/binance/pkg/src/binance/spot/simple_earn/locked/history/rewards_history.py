# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class LockedRewardsHistoryEntry(TypedDict):
  """One locked product reward payment."""

  positionId: NotRequired[int]
  """Identifier of the locked position the reward accrued on."""
  time: NotRequired[int]
  """Millisecond epoch time the reward was recorded."""
  asset: NotRequired[str]
  """Asset the reward was paid in."""
  lockPeriod: NotRequired[str]
  """Lock duration of the position in days, as a numeric string."""
  amount: NotRequired[str]
  """Reward amount, as a decimal string."""
  type: NotRequired[str]
  """Reward type."""


class LockedRewardsHistoryPage(TypedDict):
  """One page of this account's locked product rewards history."""

  rows: NotRequired[list[LockedRewardsHistoryEntry]]
  """Matching records on this page."""
  total: NotRequired[int]
  """Total number of matching records."""


class RewardsHistory(RpcEndpoint):
  """Get this account's Simple Earn locked product rewards history."""

  async def rewards_history(
    self,
    *,
    position_id: str | None = None,
    asset: str | None = None,
    start_time: int | None = None,
    end_time: int | None = None,
    current: int | None = None,
    size: int | None = None,
    validate: bool | None = None,
  ) -> LockedRewardsHistoryPage:
    """Get this account's Simple Earn locked product rewards history.

    Args:
      position_id: Restrict results to this locked position.
      asset: Restrict results to this underlying asset.
      start_time: Millisecond epoch start of the queried window.
      end_time: Millisecond epoch end of the queried window.
      current: Currently querying page. Starts from 1.
      size: Number of results per page.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-simple-earn/api/rest-api/flexible-locked#get-locked-rewards-history)
    """
    params = {}
    if position_id is not None:
      params['positionId'] = position_id
    if asset is not None:
      params['asset'] = asset
    if start_time is not None:
      params['startTime'] = start_time
    if end_time is not None:
      params['endTime'] = end_time
    if current is not None:
      params['current'] = current
    if size is not None:
      params['size'] = size
    _Response = LockedRewardsHistoryPage
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/sapi/v1/simple-earn/locked/history/rewardsRecord',
      params=params,
      validator=_validator,
      validate=validate,
    )

  async def rewards_history_paged(
    self,
    *,
    position_id: str | None = None,
    asset: str | None = None,
    start_time: int | None = None,
    end_time: int | None = None,
    size: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[LockedRewardsHistoryPage]:
    """Yield successive pages of `rewards_history`.

    Requests `current` from 1 upwards and stops once it has covered the `total` items
    the response reports, or after `max_pages` pages when one is given.
    """
    current = 1
    pages = 0
    while True:
      response = await self.rewards_history(
        position_id=position_id,
        asset=asset,
        start_time=start_time,
        end_time=end_time,
        size=size,
        current=current,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      total = response.get('total') if response is not None else None
      total = int(total) if total is not None else None
      if total is None or size is None or pages * size >= total:
        break
      current += 1
