# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class ConvertQuote(TypedDict):
  """A quote for a token pair conversion."""

  quoteId: NotRequired[str]
  """Quote identifier, passed to `spot.convert.accept_quote` to execute the trade."""
  ratio: NotRequired[str]
  """Conversion ratio, `toAmount` / `fromAmount`."""
  inverseRatio: NotRequired[str]
  """Inverse conversion ratio, `fromAmount` / `toAmount`."""
  validTimestamp: NotRequired[int]
  """Millisecond epoch timestamp after which the quote can no longer be accepted."""
  toAmount: NotRequired[str]
  """Amount that would be credited to `toAsset`."""
  fromAmount: NotRequired[str]
  """Amount that would be debited from `fromAsset`."""


class GetQuote(RpcEndpoint):
  """Request a quote for a token pair conversion. The returned `quoteId` is accepted by `spot.convert.accept_quote` within `validTime` to execute the trade."""

  async def get_quote(
    self,
    *,
    from_asset: str,
    to_asset: str,
    from_amount: float | None = None,
    to_amount: float | None = None,
    wallet_type: Literal[
      'SPOT',
      'FUNDING',
      'EARN',
      'SPOT_FUNDING',
      'FUNDING_EARN',
      'SPOT_FUNDING_EARN',
      'SPOT_EARN',
    ]
    | None = None,
    valid_time: Literal['10s', '30s', '1m'] | None = None,
    validate: bool | None = None,
  ) -> ConvertQuote:
    """Request a quote for a token pair conversion. The returned `quoteId` is accepted by `spot.convert.accept_quote` within `validTime` to execute the trade.

    Args:
      from_asset: Asset the user spends.
      to_asset: Asset the user receives.
      from_amount: Amount debited from `fromAsset`. Either this or `toAmount` is required.
      to_amount: Amount credited to `toAsset`. Either this or `fromAmount` is required.
      wallet_type: Wallet(s) the conversion draws from.
      valid_time: How long the returned quote stays acceptable.

    References:
      - [Official docs](https://developers.binance.com/docs/convert/trade)
    """
    params: dict = {
      'fromAsset': from_asset,
      'toAsset': to_asset,
    }
    if from_amount is not None:
      params['fromAmount'] = from_amount
    if to_amount is not None:
      params['toAmount'] = to_amount
    if wallet_type is not None:
      params['walletType'] = wallet_type
    if valid_time is not None:
      params['validTime'] = valid_time
    _Response = ConvertQuote
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/convert/getQuote',
      params=params,
      validator=_validator,
      validate=validate,
    )
