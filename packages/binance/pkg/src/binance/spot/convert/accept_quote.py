# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class ConvertAcceptedOrder(TypedDict):
  """The order created by accepting a Convert quote."""

  orderId: NotRequired[str]
  """Order identifier, queryable via `spot.convert.order_status`."""
  createTime: NotRequired[int]
  """Millisecond epoch order creation timestamp."""
  orderStatus: NotRequired[Literal['PROCESS', 'ACCEPT_SUCCESS', 'SUCCESS', 'FAIL']]
  """Order status right after acceptance."""


class AcceptQuote(RpcEndpoint):
  """Accept a previously requested quote by its `quoteId`, executing the conversion."""

  async def accept_quote(
    self,
    *,
    quote_id: str,
    validate: bool | None = None,
  ) -> ConvertAcceptedOrder:
    """Accept a previously requested quote by its `quoteId`, executing the conversion.

    Args:
      quote_id: Quote identifier returned by `spot.convert.get_quote`.

    References:
      - [Official docs](https://developers.binance.com/docs/convert/trade)
    """
    params: dict = {
      'quoteId': quote_id,
    }
    _Response = ConvertAcceptedOrder
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/convert/acceptQuote',
      params=params,
      validator=_validator,
      validate=validate,
    )
