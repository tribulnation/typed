# Generated by typed-dev — see CONTRIBUTING.md.
from json import dumps
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class AddFuturesIpWhitelistsResult(TypedDict):
  """Result of adding the IP address(es) to the whitelist."""

  status: NotRequired[str]
  """Response status, e.g. `OK`."""
  type: NotRequired[str]
  """Response category, e.g. `GENERAL`."""
  code: NotRequired[str]
  """Response code; `"000000000"` on success."""


class FuturesIpWhitelistAdd(RpcEndpoint):
  """Add IP address(es) to the FAPI-MM or FSTREAM-MM futures whitelist endpoint."""

  async def futures_ip_whitelist_add(
    self,
    *,
    endpoint: Literal['FAPI-MM', 'FSTREAM-MM'],
    ip_addresses: list[str],
    validate: bool | None = None,
  ) -> AddFuturesIpWhitelistsResult:
    """Add IP address(es) to the FAPI-MM or FSTREAM-MM futures whitelist endpoint.

    Args:
      endpoint: Futures IP whitelist endpoint to add the IP address(es) to.
      ip_addresses: IP addresses to add to the whitelist.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-vip-service/api/rest-api/~#add-futures-ip-whitelists)
    """
    params: dict = {
      'endpoint': endpoint,
      'ipAddresses': dumps(ip_addresses, separators=(',', ':')),
    }
    _Response = AddFuturesIpWhitelistsResult
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/vip/vip-portal/futures/ip-whitelists',
      params=params,
      validator=_validator,
      validate=validate,
    )
