# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class GiftCardTokenLimit(TypedDict):
  """One coin's tradable amount limits when paid for with `baseToken`."""

  coin: NotRequired[str]
  """Coin buyable as a gift card."""
  fromMin: NotRequired[float]
  """Minimum `baseToken` amount for this coin."""
  fromMax: NotRequired[float]
  """Maximum `baseToken` amount for this coin."""


class GiftCardTokenLimitResponse(TypedDict):
  """Token limit response frame. This product line wraps its business data in {code,message,data,success} on the wire, and the core does not strip it -- see spec/core.md's Envelope section."""

  code: NotRequired[str]
  """Response code. "000000" indicates success."""
  message: NotRequired[str]
  """Response message."""
  data: NotRequired[list[GiftCardTokenLimit]]
  """Coins buyable with `baseToken`."""
  success: NotRequired[bool]
  """Whether the call succeeded."""


class TokenLimit(RpcEndpoint):
  """Query which stablecoin-denominated gift cards `baseToken` can buy, and each one's tradable amount limits."""

  async def token_limit(
    self,
    *,
    base_token: str,
    validate: bool | None = None,
  ) -> GiftCardTokenLimitResponse:
    """Query which stablecoin-denominated gift cards `baseToken` can buy, and each one's tradable amount limits.

    Args:
      base_token: Token to pay with, e.g. BUSD.

    References:
      - [Official docs](https://developers.binance.com/docs/gift_card/market-data)
    """
    params: dict = {
      'baseToken': base_token,
    }
    _Response = GiftCardTokenLimitResponse
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/sapi/v1/giftcard/buyCode/token-limit',
      params=params,
      validator=_validator,
      validate=validate,
    )
