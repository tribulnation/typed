# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core import Timestamp
from binance.core.endpoint.rpc import RpcEndpoint


class OcoOrderReportAck(TypedDict):
  """Minimal order-list leg acknowledgement, returned when newOrderRespType is ACK."""

  symbol: str
  """Trading pair symbol."""
  orderId: int
  """Order id."""
  orderListId: int
  """Id of the order list this order belongs to."""
  clientOrderId: str
  """Client order id, either caller-supplied or venue-generated."""
  transactTime: Timestamp
  """Time the order was processed."""


class OcoOrderReportFill(TypedDict):
  """One trade that filled part or all of this order-list leg."""

  price: str
  """Fill price."""
  qty: str
  """Filled quantity."""
  commission: str
  """Commission charged on the fill."""
  commissionAsset: str
  """Asset the commission was charged in."""
  tradeId: int
  """Trade id of the fill."""


class OcoOrderReportResult(TypedDict):
  """Order-list leg acknowledgement with fill/status detail, returned when newOrderRespType is RESULT."""

  symbol: str
  """Trading pair symbol."""
  orderId: int
  """Order id."""
  orderListId: int
  """Id of the order list this order belongs to."""
  clientOrderId: str
  """Client order id, either caller-supplied or venue-generated."""
  transactTime: Timestamp
  """Time the order was processed."""
  price: str
  """Order price."""
  origQty: str
  """Original order quantity."""
  executedQty: str
  """Cumulative filled quantity."""
  origQuoteOrderQty: str
  """Original quoteOrderQty, or "0" when quoteOrderQty was not used."""
  cummulativeQuoteQty: str
  """Cumulative quote asset transacted quantity."""
  status: Literal[
    'NEW',
    'PENDING_NEW',
    'PARTIALLY_FILLED',
    'FILLED',
    'CANCELED',
    'PENDING_CANCEL',
    'REJECTED',
    'EXPIRED',
    'EXPIRED_IN_MATCH',
  ]
  """Order status."""
  timeInForce: Literal['GTC', 'IOC', 'FOK']
  """Time in force."""
  type: Literal[
    'LIMIT',
    'MARKET',
    'STOP_LOSS',
    'STOP_LOSS_LIMIT',
    'TAKE_PROFIT',
    'TAKE_PROFIT_LIMIT',
    'LIMIT_MAKER',
  ]
  """Order type."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  workingTime: Timestamp
  """Time the order started working on the order book. -1 if not yet working."""
  selfTradePreventionMode: Literal[
    'NONE', 'EXPIRE_MAKER', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'DECREMENT', 'TRANSFER'
  ]
  """Self-trade prevention mode in effect for the order."""
  stopPrice: NotRequired[str]
  """Price at which the algorithmic order triggers. Present for STOP_LOSS, TAKE_PROFIT, STOP_LOSS_LIMIT and TAKE_PROFIT_LIMIT orders."""
  icebergQty: NotRequired[str]
  """Quantity for the iceberg order. Present only when an iceberg quantity was sent in the request."""
  strategyId: NotRequired[int]
  """Id labeling the order strategy this order is part of. Present when a strategy id was sent in the request."""
  strategyType: NotRequired[int]
  """Type labeling the order strategy this order is part of. Present when a strategy type was sent in the request."""
  trailingDelta: NotRequired[int]
  """Delta price change required before order activation. Present for trailing stop orders."""
  trailingTime: NotRequired[Timestamp]
  """Time the trailing order became active and started tracking price changes. Present only for trailing stop orders."""
  pegPriceType: NotRequired[Literal['PRIMARY_PEG', 'MARKET_PEG']]
  """Peg reference type. Present only for pegged orders."""
  pegOffsetType: NotRequired[Literal['PRICE_LEVEL']]
  """Peg offset type. Present only for pegged orders that requested one."""
  pegOffsetValue: NotRequired[int]
  """Peg offset value. Present only for pegged orders that requested one."""
  peggedPrice: NotRequired[str]
  """Current price the order is pegged at. Present only for pegged orders, once determined."""
  expiryReason: NotRequired[
    Literal[
      'NONE',
      'REJECTED',
      'EXCHANGE_CANCELED',
      'OCO_TRIGGER',
      'OTO_PHASE_ONE_EXPIRED',
      'UNFILLED_IOC_QUANTITY_EXPIRED',
      'UNFILLED_FOK_ORDER_EXPIRED',
      'INSUFFICIENT_LIQUIDITY',
      'EXECUTION_RULE_PRICE_RANGE_EXCEEDED',
    ]
  ]
  """Cause of the order's expiration. Present when the order has expired."""
  preventedMatchId: NotRequired[int]
  """Id of a prevented match, usable together with symbol to query it. Present only when the order expired due to self-trade prevention."""
  preventedQuantity: NotRequired[str]
  """Order quantity that expired due to self-trade prevention. Present only when the order expired due to self-trade prevention."""


class OrderListOrder(TypedDict):
  """One order belonging to the order list, identified minimally."""

  symbol: str
  """Trading pair symbol."""
  orderId: int
  """Order id."""
  clientOrderId: str
  """Client order id, either caller-supplied or venue-generated."""


class OcoOrderReportFull(TypedDict):
  """Full order-list leg acknowledgement with fill detail, returned when newOrderRespType is FULL."""

  symbol: str
  """Trading pair symbol."""
  orderId: int
  """Order id."""
  orderListId: int
  """Id of the order list this order belongs to."""
  clientOrderId: str
  """Client order id, either caller-supplied or venue-generated."""
  transactTime: Timestamp
  """Time the order was processed."""
  price: str
  """Order price."""
  origQty: str
  """Original order quantity."""
  executedQty: str
  """Cumulative filled quantity."""
  origQuoteOrderQty: str
  """Original quoteOrderQty, or "0" when quoteOrderQty was not used."""
  cummulativeQuoteQty: str
  """Cumulative quote asset transacted quantity."""
  status: Literal[
    'NEW',
    'PENDING_NEW',
    'PARTIALLY_FILLED',
    'FILLED',
    'CANCELED',
    'PENDING_CANCEL',
    'REJECTED',
    'EXPIRED',
    'EXPIRED_IN_MATCH',
  ]
  """Order status."""
  timeInForce: Literal['GTC', 'IOC', 'FOK']
  """Time in force."""
  type: Literal[
    'LIMIT',
    'MARKET',
    'STOP_LOSS',
    'STOP_LOSS_LIMIT',
    'TAKE_PROFIT',
    'TAKE_PROFIT_LIMIT',
    'LIMIT_MAKER',
  ]
  """Order type."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  workingTime: Timestamp
  """Time the order started working on the order book. -1 if not yet working."""
  selfTradePreventionMode: Literal[
    'NONE', 'EXPIRE_MAKER', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'DECREMENT', 'TRANSFER'
  ]
  """Self-trade prevention mode in effect for the order."""
  stopPrice: NotRequired[str]
  """Price at which the algorithmic order triggers. Present for STOP_LOSS, TAKE_PROFIT, STOP_LOSS_LIMIT and TAKE_PROFIT_LIMIT orders."""
  icebergQty: NotRequired[str]
  """Quantity for the iceberg order. Present only when an iceberg quantity was sent in the request."""
  strategyId: NotRequired[int]
  """Id labeling the order strategy this order is part of. Present when a strategy id was sent in the request."""
  strategyType: NotRequired[int]
  """Type labeling the order strategy this order is part of. Present when a strategy type was sent in the request."""
  trailingDelta: NotRequired[int]
  """Delta price change required before order activation. Present for trailing stop orders."""
  trailingTime: NotRequired[Timestamp]
  """Time the trailing order became active and started tracking price changes. Present only for trailing stop orders."""
  pegPriceType: NotRequired[Literal['PRIMARY_PEG', 'MARKET_PEG']]
  """Peg reference type. Present only for pegged orders."""
  pegOffsetType: NotRequired[Literal['PRICE_LEVEL']]
  """Peg offset type. Present only for pegged orders that requested one."""
  pegOffsetValue: NotRequired[int]
  """Peg offset value. Present only for pegged orders that requested one."""
  peggedPrice: NotRequired[str]
  """Current price the order is pegged at. Present only for pegged orders, once determined."""
  expiryReason: NotRequired[
    Literal[
      'NONE',
      'REJECTED',
      'EXCHANGE_CANCELED',
      'OCO_TRIGGER',
      'OTO_PHASE_ONE_EXPIRED',
      'UNFILLED_IOC_QUANTITY_EXPIRED',
      'UNFILLED_FOK_ORDER_EXPIRED',
      'INSUFFICIENT_LIQUIDITY',
      'EXECUTION_RULE_PRICE_RANGE_EXCEEDED',
    ]
  ]
  """Cause of the order's expiration. Present when the order has expired."""
  preventedMatchId: NotRequired[int]
  """Id of a prevented match, usable together with symbol to query it. Present only when the order expired due to self-trade prevention."""
  preventedQuantity: NotRequired[str]
  """Order quantity that expired due to self-trade prevention. Present only when the order expired due to self-trade prevention."""
  fills: list[OcoOrderReportFill]
  """Trades that filled this order-list leg."""


class OcoResult(TypedDict):
  """The placed OCO order list."""

  orderListId: int
  """Id of the order list."""
  contingencyType: Literal['OCO', 'OTO']
  """Contingency type of the order list."""
  listStatusType: Literal['RESPONSE', 'EXEC_STARTED', 'UPDATED', 'ALL_DONE']
  """Status of the order list."""
  listOrderStatus: Literal['EXECUTING', 'ALL_DONE', 'REJECT']
  """Execution status of the order list."""
  listClientOrderId: str
  """Client id for the entire order list, either caller-supplied or venue-generated."""
  transactionTime: Timestamp
  """Time the order list was processed."""
  symbol: str
  """Trading pair symbol."""
  orders: list[OrderListOrder]
  """The two orders making up the OCO, identified minimally."""
  orderReports: list[OcoOrderReportAck | OcoOrderReportResult | OcoOrderReportFull]
  """Per-leg order acknowledgements. Each entry's shape is discriminated by the request's newOrderRespType parameter."""


class Oco(RpcEndpoint):
  """Send in a new OCO (one-cancels-the-other) order list, deprecated in favor of POST /api/v3/orderList/oco. An OCO adds 2 orders to the EXCHANGE_MAX_ORDERS and MAX_NUM_ORDERS filters. Price restrictions: for a SELL, limit price > last price > stop price; for a BUY, limit price < last price < stop price. Both legs must share the same quantity, except iceberg quantities which may differ."""

  async def oco(
    self,
    *,
    symbol: str,
    list_client_order_id: str | None = None,
    side: Literal['BUY', 'SELL'],
    quantity: str,
    limit_client_order_id: str | None = None,
    price: str,
    limit_strategy_id: int | None = None,
    limit_strategy_type: int | None = None,
    limit_iceberg_qty: str | None = None,
    trailing_delta: int | None = None,
    stop_client_order_id: str | None = None,
    stop_price: str,
    stop_strategy_id: int | None = None,
    stop_strategy_type: int | None = None,
    stop_limit_price: str | None = None,
    stop_iceberg_qty: str | None = None,
    stop_limit_time_in_force: Literal['GTC', 'IOC', 'FOK'] | None = None,
    new_order_resp_type: Literal['ACK', 'RESULT', 'FULL'] | None = None,
    self_trade_prevention_mode: Literal[
      'NONE', 'EXPIRE_MAKER', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'DECREMENT', 'TRANSFER'
    ]
    | None = None,
    validate: bool | None = None,
  ) -> OcoResult:
    """Send in a new OCO (one-cancels-the-other) order list, deprecated in favor of POST /api/v3/orderList/oco. An OCO adds 2 orders to the EXCHANGE_MAX_ORDERS and MAX_NUM_ORDERS filters. Price restrictions: for a SELL, limit price > last price > stop price; for a BUY, limit price < last price < stop price. Both legs must share the same quantity, except iceberg quantities which may differ.

    Args:
      symbol: Trading pair symbol.
      list_client_order_id: Arbitrary unique id for the entire order list. Automatically generated if not sent.
      side: Order side, shared by both legs.
      quantity: Quantity for both orders of the order list.
      limit_client_order_id: Arbitrary unique id for the limit order leg. Automatically generated if not sent.
      price: Limit price of the limit order leg.
      limit_strategy_id: Id labeling the limit order leg within an order strategy.
      limit_strategy_type: Type labeling the limit order leg's strategy. Cannot be less than 1000000.
      limit_iceberg_qty: Iceberg quantity, used to make the LIMIT_MAKER leg an iceberg order.
      trailing_delta: Trailing delta for the stop order leg. See Trailing Stop order FAQ.
      stop_client_order_id: Arbitrary unique id for the stop loss/stop loss limit leg. Automatically generated if not sent.
      stop_price: Trigger price of the stop order leg.
      stop_strategy_id: Id labeling the stop order leg within an order strategy.
      stop_strategy_type: Type labeling the stop order leg's strategy. Cannot be less than 1000000.
      stop_limit_price: Limit price for the stop order leg, making it STOP_LOSS_LIMIT. If provided, stopLimitTimeInForce is required.
      stop_iceberg_qty: Iceberg quantity, used with the STOP_LOSS_LIMIT leg to make it an iceberg order.
      stop_limit_time_in_force: Time in force for the stop order leg when it is STOP_LOSS_LIMIT.
      new_order_resp_type: Response shape to return for each leg's orderReports entry.
      self_trade_prevention_mode: Self-trade prevention mode. The allowed values depend on the symbol's configuration.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/rest-api.md)
    """
    params: dict = {
      'symbol': symbol,
      'side': side,
      'quantity': quantity,
      'price': price,
      'stopPrice': stop_price,
    }
    if list_client_order_id is not None:
      params['listClientOrderId'] = list_client_order_id
    if limit_client_order_id is not None:
      params['limitClientOrderId'] = limit_client_order_id
    if limit_strategy_id is not None:
      params['limitStrategyId'] = limit_strategy_id
    if limit_strategy_type is not None:
      params['limitStrategyType'] = limit_strategy_type
    if limit_iceberg_qty is not None:
      params['limitIcebergQty'] = limit_iceberg_qty
    if trailing_delta is not None:
      params['trailingDelta'] = trailing_delta
    if stop_client_order_id is not None:
      params['stopClientOrderId'] = stop_client_order_id
    if stop_strategy_id is not None:
      params['stopStrategyId'] = stop_strategy_id
    if stop_strategy_type is not None:
      params['stopStrategyType'] = stop_strategy_type
    if stop_limit_price is not None:
      params['stopLimitPrice'] = stop_limit_price
    if stop_iceberg_qty is not None:
      params['stopIcebergQty'] = stop_iceberg_qty
    if stop_limit_time_in_force is not None:
      params['stopLimitTimeInForce'] = stop_limit_time_in_force
    if new_order_resp_type is not None:
      params['newOrderRespType'] = new_order_resp_type
    if self_trade_prevention_mode is not None:
      params['selfTradePreventionMode'] = self_trade_prevention_mode
    _Response = OcoResult
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/api/v3/order/oco',
      params=params,
      validator=_validator,
      validate=validate,
    )
