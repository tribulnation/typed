# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class LocalEntityWithdrawSubmission(TypedDict):
  """Result of submitting a travel rule withdrawal request."""

  trId: NotRequired[int]
  """Travel rule record id."""
  accepted: NotRequired[bool]
  """Whether the withdrawal request was accepted."""
  info: NotRequired[str]
  """Status message, e.g. "Withdraw request accepted"."""


class Apply(RpcEndpoint):
  """Submit a withdrawal request for local entities that require travel rule, including the travel rule questionnaire answers. Use this instead of spot.wallet.capital.withdraw.apply whenever spot.wallet.local_entity.questionnaire_requirements reports a non-"NIL" questionnaireCountryCode for the account."""

  async def apply(
    self,
    *,
    coin: str,
    address: str,
    amount: float,
    questionnaire: str,
    withdraw_order_id: str | None = None,
    network: str | None = None,
    address_tag: str | None = None,
    transaction_fee_flag: bool | None = None,
    name: str | None = None,
    wallet_type: Literal[0, 1] | None = None,
    validate: bool | None = None,
  ) -> LocalEntityWithdrawSubmission:
    """Submit a withdrawal request for local entities that require travel rule, including the travel rule questionnaire answers. Use this instead of spot.wallet.capital.withdraw.apply whenever spot.wallet.local_entity.questionnaire_requirements reports a non-"NIL" questionnaireCountryCode for the account.

    Args:
      coin: Coin to withdraw.
      address: Withdrawal address.
      amount: Amount to withdraw.
      questionnaire: JSON-formatted questionnaire answers, URL-encoded. Contents differ per local entity; see the entity's own Withdraw Questionnaire Contents page.
      withdraw_order_id: Caller-supplied id for the withdrawal (i.e. the client's own internal withdraw id).
      network: Withdrawal network. If omitted, the coin's default network is used; if the address doesn't match that default network, the withdrawal is rejected. See the `networkList` of a coin in spot.wallet.capital.config.get_all's response for valid values.
      address_tag: Secondary address identifier, for coins like XRP, XMR, etc.
      transaction_fee_flag: For an internal transfer: true returns the fee to the destination account, false returns it to the departure account.
      name: Description of the withdrawal address. Neither the SDK nor the rendered docs elaborate beyond the parameter name itself.
      wallet_type: Wallet to withdraw from: 0 spot wallet, 1 funding wallet. Defaults to the account's currently selected wallet under Wallet -> Fiat and Spot/Funding -> Deposit.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/travel-rule#withdraw-travel-rule)
    """
    params: dict = {
      'coin': coin,
      'address': address,
      'amount': amount,
      'questionnaire': questionnaire,
    }
    if withdraw_order_id is not None:
      params['withdrawOrderId'] = withdraw_order_id
    if network is not None:
      params['network'] = network
    if address_tag is not None:
      params['addressTag'] = address_tag
    if transaction_fee_flag is not None:
      params['transactionFeeFlag'] = transaction_fee_flag
    if name is not None:
      params['name'] = name
    if wallet_type is not None:
      params['walletType'] = wallet_type
    _Response = LocalEntityWithdrawSubmission
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/localentity/withdraw/apply',
      params=params,
      validator=_validator,
      validate=validate,
    )
