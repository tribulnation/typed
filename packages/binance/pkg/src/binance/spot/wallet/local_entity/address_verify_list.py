# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class AddressVerificationQuestionnaire(TypedDict):
  """Questionnaire answers submitted for this address's verification."""

  sendTo: NotRequired[int]
  """Recipient type code. Not enumerated on this endpoint's own documentation."""
  satoshiToken: NotRequired[str]
  """Token/coin identifier used in the address-ownership verification (satoshi test) flow."""
  isAddressOwner: NotRequired[int]
  """Whether the caller confirmed ownership of the address, as a coded value. Not enumerated on this endpoint's own documentation."""
  verifyMethod: NotRequired[int]
  """Verification method code. Not enumerated on this endpoint's own documentation."""


class AddressVerification(TypedDict):
  """One address's verification status and questionnaire detail."""

  status: NotRequired[str]
  """Verification status of the address. Not enumerated on this endpoint's own documentation beyond the example value "PENDING"; see notes."""
  token: NotRequired[str]
  """Coin/token symbol."""
  network: NotRequired[str]
  """Blockchain network."""
  walletAddress: NotRequired[str]
  """The address being verified."""
  addressQuestionnaire: NotRequired[AddressVerificationQuestionnaire | None]
  """Questionnaire answers submitted for this address's verification, if any."""


class AddressVerifyList(RpcEndpoint):
  """Fetch the address verification list, so the caller can check the status and other details for addresses stored in the Address Book that are undergoing (or have undergone) verification."""

  async def __call__(
    self,
    *,
    validate: bool | None = None,
  ) -> list[AddressVerification] | dict[str, Any]:
    """Fetch the address verification list, so the caller can check the status and other details for addresses stored in the Address Book that are undergoing (or have undergone) verification.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/travel-rule#fetch-address-verification-list)
    """
    _Response = list[AddressVerification] | dict[str, Any]
    _validator = validator[_Response](_Response)  # type: ignore
    return await self.authed_request(
      'GET', '/sapi/v1/addressVerify/list', validator=_validator, validate=validate
    )
