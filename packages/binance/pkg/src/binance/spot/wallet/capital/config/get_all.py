# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class CoinNetwork(TypedDict):
  """Deposit/withdraw configuration for one coin on one network."""

  network: str
  """Network code, e.g. `ETH` or `BSC`."""
  coin: str
  """Coin this network entry belongs to."""
  withdrawIntegerMultiple: str
  """Withdrawal amount must be an integer multiple of this value on this network."""
  isDefault: bool
  """Whether this is the coin's default network."""
  depositEnable: bool
  """Whether deposit is enabled on this network."""
  withdrawEnable: bool
  """Whether withdrawal is enabled on this network."""
  depositDesc: str
  """Reason deposit is disabled on this network. Present only when `depositEnable` is false."""
  withdrawDesc: str
  """Reason withdrawal is disabled on this network. Present only when `withdrawEnable` is false."""
  specialTips: str
  """Special tips shown to the caller for deposits/withdrawals on this network, e.g. when both a memo and an address are required."""
  specialWithdrawTips: NotRequired[str]
  """Special tips shown to the caller specifically for withdrawals on this network."""
  name: str
  """Network's full display name, e.g. `BNB Smart Chain (BEP20)`."""
  resetAddressStatus: NotRequired[bool]
  """Whether the deposit address for this coin/network is currently being reset."""
  addressRegex: str
  """Regular expression a deposit address on this network must match."""
  memoRegex: str
  """Regular expression a memo/tag on this network must match."""
  withdrawFee: str
  """Fixed fee charged on withdrawals over this network."""
  withdrawMin: str
  """Minimum withdrawal amount on this network."""
  withdrawMax: str
  """Maximum withdrawal amount on this network."""
  withdrawInternalMin: NotRequired[str]
  """Minimum amount for an internal (off-chain, Binance-to-Binance) transfer on this network."""
  depositDust: NotRequired[str]
  """Deposits below this amount on this network are treated as dust and not credited."""
  minConfirm: int
  """Minimum number of on-chain confirmations before a deposit is credited."""
  unLockConfirm: int
  """Number of on-chain confirmations before a credited balance is unlocked for use."""
  sameAddress: bool
  """Whether this network reuses one deposit address across users, requiring a memo/tag (`addressTag`) to distinguish them."""
  withdrawTag: NotRequired[bool]
  """Whether a memo/tag is required for withdrawals on this network. When `true`, `addressTag` must be supplied to `capital.withdraw.apply`; when `false`, the network does not support a tag and supplying `addressTag` raises error -4106 (`TAG_NOT_SUPPORTED_FOR_NETWORK`)."""
  estimatedArrivalTime: NotRequired[int]
  """Estimated arrival time for a deposit/withdrawal on this network, in minutes."""
  busy: NotRequired[bool]
  """Whether this network is currently congested."""
  contractAddressUrl: NotRequired[str]
  """URL template for viewing this coin's token contract on a block explorer for this network."""
  contractAddress: NotRequired[str]
  """Token contract address on this network."""
  denomination: NotRequired[int]
  """Denomination multiplier applied to amounts on this network."""


class CoinConfig(TypedDict):
  """Balance and deposit/withdraw configuration for one coin, across every network it supports."""

  coin: str
  """Coin symbol."""
  depositAllEnable: bool
  """Whether deposit is enabled for this coin on at least one network."""
  withdrawAllEnable: bool
  """Whether withdrawal is enabled for this coin on at least one network."""
  name: str
  """Coin's full name."""
  free: str
  """Available (unlocked) balance."""
  locked: str
  """Balance locked in open orders."""
  freeze: str
  """Frozen balance."""
  withdrawing: str
  """Balance currently in an unconfirmed withdrawal."""
  ipoing: str
  """Balance locked in an ongoing IPO subscription."""
  ipoable: str
  """Balance available for IPO subscription."""
  storage: str
  """Balance held in cold storage."""
  isLegalMoney: bool
  """Whether this is a fiat/legal-money asset rather than a crypto coin."""
  trading: bool
  """Whether spot trading is enabled for this coin."""
  networkList: list[CoinNetwork]
  """Deposit/withdraw configuration for each network this coin supports."""


class GetAll(RpcEndpoint):
  """Get information of every coin available for deposit and withdraw for this account, including its per-network deposit/withdraw configuration. This is the canonical source for `network`, `isDefault`, `withdrawTag`, and `withdrawIntegerMultiple` cross-referenced by other Capital endpoints."""

  async def get_all(self, *, validate: bool | None = None) -> list[CoinConfig]:
    """Get information of every coin available for deposit and withdraw for this account, including its per-network deposit/withdraw configuration. This is the canonical source for `network`, `isDefault`, `withdrawTag`, and `withdrawIntegerMultiple` cross-referenced by other Capital endpoints.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/capital#all-coins-information)
    """
    _Response = list[CoinConfig]
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET', '/sapi/v1/capital/config/getall', validator=_validator, validate=validate
    )
