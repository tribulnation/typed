# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class RwusdRedemption(TypedDict):
  """Result of a RWUSD redemption."""

  success: NotRequired[bool]
  """Whether the redemption succeeded."""
  receiveAmount: NotRequired[str]
  """Amount of USDC credited, as a decimal string."""
  fee: NotRequired[str]
  """Redemption fee, as a decimal string."""
  arrivalTime: NotRequired[int]
  """Millisecond epoch time the credited amount is expected to arrive."""


class Redeem(RpcEndpoint):
  """Redeem RWUSD to USDC."""

  async def __call__(
    self,
    *,
    amount: float,
    type: Literal['FAST', 'STANDARD'],
    validate: bool | None = None,
  ) -> RwusdRedemption:
    """Redeem RWUSD to USDC.

    Args:
      amount: Amount to redeem, in RWUSD.
      type: Redemption type. Defaults to `STANDARD`.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-simple-earn/api/rest-api/rwusd#redeem-rwusd)
    """
    params: dict = {
      'amount': amount,
      'type': type,
    }
    _Response = RwusdRedemption
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/rwusd/redeem',
      params=params,
      validator=_validator,
      validate=validate,
    )
