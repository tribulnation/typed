# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class BlvtUserLimit(TypedDict):
  """This account's daily subscription/redemption limit for one leveraged token."""

  tokenName: str
  """Leveraged token name."""
  userDailyTotalPurchaseLimit: str
  """Remaining daily subscription limit, in USDT."""
  userDailyTotalRedeemLimit: str
  """Remaining daily redemption limit, in USDT."""


class UserLimit(RpcEndpoint):
  """BLVT User Limit Info (USER_DATA)"""

  async def user_limit(
    self,
    *,
    token_name: str | None = None,
    validate: bool | None = None,
  ) -> list[BlvtUserLimit]:
    """Get this account's daily subscription/redemption limits per leveraged token.

    Args:
      token_name: Leveraged token name, e.g. `BTCDOWN`, `BTCUP`.

    References:
      - [Official docs](https://github.com/binance/binance-api-swagger)
    """
    params = {}
    if token_name is not None:
      params['tokenName'] = token_name
    _Response = list[BlvtUserLimit]
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/sapi/v1/blvt/userLimit',
      params=params,
      validator=_validator,
      validate=validate,
    )
