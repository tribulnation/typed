# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class OnChainYieldsAccount(TypedDict):
  """Summary of this account's On-chain Yields staking holdings."""

  totalAmountInBTC: NotRequired[str]
  """Total value of all On-chain Yields staking holdings, denominated in BTC, as a decimal string."""
  totalAmountInUSDT: NotRequired[str]
  """Total value of all On-chain Yields staking holdings, denominated in USDT, as a decimal string."""
  totalFlexibleAmountInBTC: NotRequired[str]
  """Total value of flexible On-chain Yields holdings, denominated in BTC, as a decimal string."""
  totalFlexibleAmountInUSDT: NotRequired[str]
  """Total value of flexible On-chain Yields holdings, denominated in USDT, as a decimal string."""
  totalLockedInBTC: NotRequired[str]
  """Total value of locked On-chain Yields holdings, denominated in BTC, as a decimal string."""
  totalLockedInUSDT: NotRequired[str]
  """Total value of locked On-chain Yields holdings, denominated in USDT, as a decimal string."""


class Account(RpcEndpoint):
  """Get this account's On-chain Yields staking account summary (total value across flexible and locked positions)."""

  async def __call__(self, *, validate: bool | None = None) -> OnChainYieldsAccount:
    """Get this account's On-chain Yields staking account summary (total value across flexible and locked positions).

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-staking/api/rest-api/on-chain-yields#on-chain-yields-account)
    """
    _Response = OnChainYieldsAccount
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET', '/sapi/v1/onchain-yields/account', validator=_validator, validate=validate
    )
