# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class OnChainYieldsLockedSubscription(TypedDict):
  """Result of an On-chain Yields locked product subscription."""

  purchaseId: NotRequired[int]
  """Identifier of the created subscription (purchase)."""
  positionId: NotRequired[str]
  """Identifier of the created locked position."""
  amount: NotRequired[str]
  """Subscribed amount, as a decimal string."""
  success: NotRequired[bool]
  """Whether the subscription succeeded."""


class Subscribe(RpcEndpoint):
  """Subscribe an amount of an asset into an On-chain Yields locked product."""

  async def __call__(
    self,
    *,
    project_id: str,
    amount: str,
    auto_subscribe: bool | None = None,
    source_account: Literal['SPOT', 'FUND', 'ALL'] | None = None,
    redeem_to: Literal['SPOT', 'FLEXIBLE'] | None = None,
    channel_id: str | None = None,
    client_id: str | None = None,
    validate: bool | None = None,
  ) -> OnChainYieldsLockedSubscription:
    """Subscribe an amount of an asset into an On-chain Yields locked product.

    Args:
      project_id: Locked product identifier to subscribe to, as returned by `locked.list`.
      amount: Amount to subscribe, as a decimal string.
      auto_subscribe: Whether the position should auto-renew at maturity.
      source_account: Wallet to debit the subscription amount from.
      redeem_to: Wallet the position is redeemed to at maturity. Takes effect when `autoSubscribe` is false.
      channel_id: Optional channel identifier for this subscription.
      client_id: Optional client-supplied identifier for this subscription, echoed back as `clientId` in `locked.history.subscription_record`.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-staking/api/rest-api/on-chain-yields#subscribe-on-chain-yields-locked-product)
    """
    params: dict = {
      'projectId': project_id,
      'amount': amount,
    }
    if auto_subscribe is not None:
      params['autoSubscribe'] = auto_subscribe
    if source_account is not None:
      params['sourceAccount'] = source_account
    if redeem_to is not None:
      params['redeemTo'] = redeem_to
    if channel_id is not None:
      params['channelId'] = channel_id
    if client_id is not None:
      params['clientId'] = client_id
    _Response = OnChainYieldsLockedSubscription
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/onchain-yields/locked/subscribe',
      params=params,
      validator=_validator,
      validate=validate,
    )
