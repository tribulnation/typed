# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class SolStakingEvent(TypedDict):
  """One SOL staking subscription event."""

  time: NotRequired[int]
  """Millisecond epoch time the subscription was requested."""
  asset: NotRequired[str]
  """Asset staked (SOL)."""
  amount: NotRequired[str]
  """Amount of `asset` staked, as a decimal string."""
  distributeAsset: NotRequired[str]
  """Asset the subscription was distributed in."""
  distributeAmount: NotRequired[str]
  """Amount of `distributeAsset` distributed, as a decimal string."""
  exchangeRate: NotRequired[str]
  """Exchange rate applied to this subscription, as a decimal string."""
  status: NotRequired[str]
  """Subscription status."""


class SolStakingHistoryPage(TypedDict):
  """One page of SOL staking subscription history."""

  rows: NotRequired[list[SolStakingEvent]]
  """Matching solstakingevent records on this page."""
  total: NotRequired[int]
  """Total number of matching records across all pages."""


class StakingHistory(RpcEndpoint):
  """Query this account's SOL staking subscription history."""

  async def staking_history(
    self,
    *,
    purchase_id: int | None = None,
    start_time: int | None = None,
    end_time: int | None = None,
    current: int | None = None,
    size: int | None = None,
    validate: bool | None = None,
  ) -> SolStakingHistoryPage:
    """Query this account's SOL staking subscription history.

    Args:
      purchase_id: Restrict results to this staking subscription identifier.
      start_time: Millisecond epoch start of the queried window. See the endpoint notes for the default window applied when this and `endTime` are omitted.
      end_time: Millisecond epoch end of the queried window. See the endpoint notes for the default window applied when this and `startTime` are omitted.
      current: Page index, starting from 1.
      size: Number of records to return per page.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-staking/api/rest-api/sol-staking#get-sol-staking-history)
    """
    params = {}
    if purchase_id is not None:
      params['purchaseId'] = purchase_id
    if start_time is not None:
      params['startTime'] = start_time
    if end_time is not None:
      params['endTime'] = end_time
    if current is not None:
      params['current'] = current
    if size is not None:
      params['size'] = size
    _Response = SolStakingHistoryPage
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/sapi/v1/sol-staking/sol/history/stakingHistory',
      params=params,
      validator=_validator,
      validate=validate,
    )

  async def staking_history_paged(
    self,
    *,
    purchase_id: int | None = None,
    start_time: int | None = None,
    end_time: int | None = None,
    size: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[SolStakingHistoryPage]:
    """Yield successive pages of `staking_history`.

    Requests `current` from 1 upwards and stops once it has covered the `total` items
    the response reports, or after `max_pages` pages when one is given.
    """
    current = 1
    pages = 0
    while True:
      response = await self.staking_history(
        purchase_id=purchase_id,
        start_time=start_time,
        end_time=end_time,
        size=size,
        current=current,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      total = response.get('total') if response is not None else None
      total = int(total) if total is not None else None
      if total is None or size is None or pages * size >= total:
        break
      current += 1
