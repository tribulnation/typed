# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class VipLoanOngoingOrder(TypedDict):
  """One ongoing VIP loan order."""

  orderId: NotRequired[int]
  """Loan order ID."""
  loanCoin: NotRequired[str]
  """Borrowed coin."""
  totalDebt: NotRequired[str]
  """Total outstanding debt (principal plus interest)."""
  loanRate: NotRequired[str]
  """Current loan interest rate. For flexible loans, this is the flexible rate."""
  residualInterest: NotRequired[str]
  """Outstanding unpaid interest."""
  collateralAccountId: NotRequired[str]
  """Collateral account ID(s), comma-separated."""
  collateralCoin: NotRequired[str]
  """Collateral coin(s), comma-separated."""
  totalCollateralValueAfterHaircut: NotRequired[str]
  """Total collateral value after the haircut is applied."""
  lockedCollateralValue: NotRequired[str]
  """Collateral value currently locked against this loan."""
  currentLTV: NotRequired[str]
  """Current loan-to-value ratio, as a decimal string."""
  expirationTime: NotRequired[int]
  """Loan expiration time, millisecond epoch. For flexible loans, this value is `0`."""
  loanDate: NotRequired[str]
  """Date the loan was originated."""
  loanTerm: NotRequired[str]
  """Loan term. For flexible loans, this value is `open term`."""


class VipLoanOngoingOrdersPage(TypedDict):
  """One page of ongoing VIP loan orders."""

  rows: NotRequired[list[VipLoanOngoingOrder]]
  """Matching ongoing orders."""
  total: NotRequired[int]
  """Total number of matching orders."""


class OngoingOrders(RpcEndpoint):
  """Check VIP loan ongoing orders. Available to VIP users only."""

  async def ongoing_orders(
    self,
    *,
    order_id: int | None = None,
    collateral_account_id: int | None = None,
    loan_coin: str | None = None,
    collateral_coin: str | None = None,
    current: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> VipLoanOngoingOrdersPage:
    """Check VIP loan ongoing orders. Available to VIP users only.

    Args:
      order_id: Restrict results to a specific loan order.
      collateral_account_id: Restrict results to a specific collateral account.
      loan_coin: Restrict results to a specific loan coin.
      collateral_coin: Restrict results to a specific collateral coin.
      current: Current page number, starting from 1.
      limit: Number of records per page.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-vip-loan/api/rest-api/user-information#get-viploan-ongoing-orders)
    """
    params = {}
    if order_id is not None:
      params['orderId'] = order_id
    if collateral_account_id is not None:
      params['collateralAccountId'] = collateral_account_id
    if loan_coin is not None:
      params['loanCoin'] = loan_coin
    if collateral_coin is not None:
      params['collateralCoin'] = collateral_coin
    if current is not None:
      params['current'] = current
    if limit is not None:
      params['limit'] = limit
    _Response = VipLoanOngoingOrdersPage
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/sapi/v1/loan/vip/ongoing/orders',
      params=params,
      validator=_validator,
      validate=validate,
    )

  async def ongoing_orders_paged(
    self,
    *,
    order_id: int | None = None,
    collateral_account_id: int | None = None,
    loan_coin: str | None = None,
    collateral_coin: str | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[VipLoanOngoingOrdersPage]:
    """Yield successive pages of `ongoing_orders`.

    Requests `current` from 1 upwards and stops once it has covered the `total` items
    the response reports, or after `max_pages` pages when one is given.
    """
    current = 1
    pages = 0
    while True:
      response = await self.ongoing_orders(
        order_id=order_id,
        collateral_account_id=collateral_account_id,
        loan_coin=loan_coin,
        collateral_coin=collateral_coin,
        limit=limit,
        current=current,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      total = response.get('total') if response is not None else None
      total = int(total) if total is not None else None
      if total is None or limit is None or pages * limit >= total:
        break
      current += 1
