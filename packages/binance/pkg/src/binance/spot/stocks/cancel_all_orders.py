# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class EquityCancelAllOrdersAck(TypedDict):
  """Acknowledgement of a cancel-all-open-orders request."""

  success: NotRequired[bool]
  """`true` when the cancel-all request was accepted by upstream. Per-order outcomes live in `/order/history`."""


class CancelAllOrders(RpcEndpoint):
  """Cancel all open orders for the caller. No request body is required -- the scope is all of the caller's open orders. The downstream service handles idempotency; to inspect per-order outcomes, query `/order/history` after the call."""

  async def cancel_all_orders(
    self,
    *,
    validate: bool | None = None,
  ) -> EquityCancelAllOrdersAck:
    """Cancel all open orders for the caller. No request body is required -- the scope is all of the caller's open orders. The downstream service handles idempotency; to inspect per-order outcomes, query `/order/history` after the call.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/rest-api/trade#cancel-all-equity-orders)
    """
    _Response = EquityCancelAllOrdersAck
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/equity/order/cancel-all',
      validator=_validator,
      validate=validate,
    )
