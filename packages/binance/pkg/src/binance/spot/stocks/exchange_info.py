# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core import Timestamp
from binance.core.endpoint.rpc import RpcEndpoint


class EquitySymbolInfo(TypedDict):
  """One tradable US-equity symbol and its trading rules."""

  symbol: NotRequired[str]
  """US-equity ticker, e.g. `AAPL`."""
  tradability: NotRequired[Literal['BUY_SELL', 'BUY', 'SELL', 'NONE']]
  """Trading direction allowed."""
  tradabilityUpdateTime: NotRequired[Timestamp]
  """Last time the `tradability` value was updated."""
  overnightSupported: NotRequired[bool]
  """Whether the symbol supports overnight trading."""
  fractionable: NotRequired[bool]
  """Whether fractional shares are supported during the regular session."""
  fractionableEh: NotRequired[bool]
  """Whether fractional shares are supported during extended hours."""
  extendedSession: NotRequired[bool]
  """Whether extended-session trading is enabled."""
  maxNumOrders: NotRequired[int]
  """Maximum number of open orders a user may have for this symbol."""
  stepSize: NotRequired[str]
  """Lot size -- minimum increment for `quantity`."""
  multiplierUp: NotRequired[str]
  """Upper price multiplier limit relative to reference."""
  multiplierDown: NotRequired[str]
  """Lower price multiplier limit relative to reference."""
  minQty: NotRequired[str]
  """Minimum allowed `quantity`."""
  maxQty: NotRequired[str]
  """Maximum allowed `quantity`."""
  minNotional: NotRequired[str]
  """Minimum order notional (USD)."""
  maxNotional: NotRequired[str]
  """Maximum order notional (USD)."""
  listingTime: NotRequired[Timestamp]
  """Listing timestamp."""
  delistingTime: NotRequired[Timestamp | None]
  """Scheduled delisting timestamp; `null` if not scheduled for delisting."""


class EquityExchangeInfo(TypedDict):
  """Exchange trading rules and symbol list."""

  timezone: NotRequired[str]
  """Always `UTC`."""
  symbols: NotRequired[list[EquitySymbolInfo]]
  """List of tradable symbols with trading rules."""


class ExchangeInfo(RpcEndpoint):
  """Returns current exchange trading rules and the list of tradable US-equity symbols. MARKET_DATA endpoint -- requires an API key (`X-MBX-APIKEY`) but no signature. Response is served from a server-side cache."""

  async def exchange_info(
    self,
    *,
    symbol: str | None = None,
    validate: bool | None = None,
  ) -> EquityExchangeInfo:
    """Returns current exchange trading rules and the list of tradable US-equity symbols. MARKET_DATA endpoint -- requires an API key (`X-MBX-APIKEY`) but no signature. Response is served from a server-side cache.

    Args:
      symbol: Filter to a single US-equity ticker, e.g. `AAPL`. When omitted, returns all active symbols. An unknown ticker returns an empty `symbols` array (HTTP 200), not an error.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/rest-api/market-data#exchange-info)
    """
    params = {}
    if symbol is not None:
      params['symbol'] = symbol
    _Response = EquityExchangeInfo
    _validator = validator[_Response](_Response)
    return await self.request(
      'GET',
      '/sapi/v1/equity/market/exchangeInfo',
      params=params,
      validator=_validator,
      validate=validate,
    )
