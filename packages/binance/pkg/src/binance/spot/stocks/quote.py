# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class EquityQuote(TypedDict):
  """Latest best bid / best ask for a single US-equity ticker."""

  symbol: NotRequired[str]
  """US-equity ticker."""
  bidPrice: NotRequired[str]
  """Best bid price (USD)."""
  askPrice: NotRequired[str]
  """Best ask price (USD)."""
  bidSize: NotRequired[int]
  """Best bid size (shares)."""
  askSize: NotRequired[int]
  """Best ask size (shares)."""


class Quote(RpcEndpoint):
  """Returns the latest best bid / best ask for a single US-equity ticker. The returned value is at most ~5 seconds stale. Returns an empty response body (not the literal `null`) when no quote is available for the supplied ticker (e.g. the ticker is unknown, halted, or de-listed)."""

  async def quote(self, *, symbol: str, validate: bool | None = None) -> EquityQuote:
    """Returns the latest best bid / best ask for a single US-equity ticker. The returned value is at most ~5 seconds stale. Returns an empty response body (not the literal `null`) when no quote is available for the supplied ticker (e.g. the ticker is unknown, halted, or de-listed).

    Args:
      symbol: US-equity ticker, e.g. `AAPL`, `TSLA`. Case-insensitive; uppercased server-side.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/rest-api/market-data#latest-quote)
    """
    params: dict = {
      'symbol': symbol,
    }
    _Response = EquityQuote
    _validator = validator[_Response](_Response)
    return await self.request(
      'GET',
      '/sapi/v1/equity/market/quote',
      params=params,
      validator=_validator,
      validate=validate,
    )
