# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from binance.core.endpoint.rpc import RpcEndpoint


class EquityCancelOrderAck(TypedDict):
  """Acknowledgement of an equity order cancellation request."""

  orderId: NotRequired[str]
  """Echoes the requested order id."""
  status: NotRequired[Literal['S', 'F']]
  """Acknowledgement code. Not an order lifecycle status -- use `/order/detail` for lifecycle poll."""


class CancelOrder(RpcEndpoint):
  """Cancel a single open order by its `orderId`."""

  async def cancel_order(
    self,
    *,
    order_id: str,
    validate: bool | None = None,
  ) -> EquityCancelOrderAck:
    """Cancel a single open order by its `orderId`.

    Args:
      order_id: Equity order id returned by `/order/place` or a query endpoint.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/rest-api/trade#cancel-equity-order)
    """
    params: dict = {
      'orderId': order_id,
    }
    _Response = EquityCancelOrderAck
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/equity/order/cancel',
      params=params,
      validator=_validator,
      validate=validate,
    )
