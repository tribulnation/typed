# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_core.util import StreamManager
from typed_core.validation import validator
from binance.core import Timestamp
from binance.core.endpoint.stream import StreamEndpoint


class IndexPriceEvent(TypedDict):
  """Index price update for one underlying asset."""

  e: Literal['indexPrice']
  """Event type."""
  E: Timestamp
  """Event time."""
  s: str
  """Underlying asset symbol, e.g. `ETHUSDT`."""
  p: str
  """Index price."""


class IndexArr(StreamEndpoint):
  """Index price streams"""

  def __call__(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[list[IndexPriceEvent]]:
    """Index price streams

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-options/api/ws-streams/market#index-price-streams)
    """
    _validator = validator[list[IndexPriceEvent]](list[IndexPriceEvent])
    return self.subscribe('!index@arr', validator=_validator, validate=validate)
