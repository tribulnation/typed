# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.util import StreamManager
from typed_core.validation import validator
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class CoinMMarkPriceEvent(TypedDict):
  """Mark price, index price, and funding rate update for one symbol."""

  e: Literal['markPriceUpdate']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Symbol."""
  p: str
  """Mark price."""
  P: str
  """Estimated settlement price. Only meaningful in the last hour before settlement."""
  i: str
  """Index price."""
  r: str
  """Funding rate. Perpetual symbols only -- an empty string on delivery symbols."""
  T: TimestampMillis
  """Next funding time. Perpetual symbols only -- 0 on delivery symbols."""
  st: NotRequired[Literal[1, 2]]
  """Symbol type: 1 = UM, 2 = CM. Added after the UM/CM stream-infrastructure migration; see notes."""


class MarkPriceArr(StreamEndpoint):
  """Mark price of all symbols of a pair"""

  def __call__(
    self,
    pair: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[list[CoinMMarkPriceEvent]]:
    """Mark price of all symbols of a pair

    Args:
      pair: Underlying pair, e.g. `BTCUSD`. The venue requires stream names to use lowercase symbols on the wire.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/ws-streams/~#mark-price-of-all-symbols-of-a-pair)
    """
    _validator = validator[list[CoinMMarkPriceEvent]](list[CoinMMarkPriceEvent])
    return self.subscribe(
      f'{pair.lower()}@markPrice', validator=_validator, validate=validate
    )
