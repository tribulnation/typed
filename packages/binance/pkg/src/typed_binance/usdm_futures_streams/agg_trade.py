# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.util import StreamManager
from typed_core.validation import validator
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class AggTradeEvent(TypedDict):
  """One aggregated taker trade."""

  e: str
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Symbol."""
  a: int
  """Aggregate trade ID."""
  p: str
  """Price."""
  q: str
  """Quantity, including any trades involving RPI orders."""
  nq: NotRequired[str]
  """Quantity excluding trades involving RPI orders."""
  f: int
  """First trade ID aggregated into this trade."""
  l: int
  """Last trade ID aggregated into this trade."""
  T: TimestampMillis
  """Trade time."""
  m: bool
  """Whether the buyer was the market maker."""
  st: NotRequired[int]
  """(After UM/CM stream-host migration) Symbol type: 1 = UM, 2 = CM."""


class AggTrade(StreamEndpoint):
  """Aggregate trade stream"""

  def __call__(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[AggTradeEvent]:
    """Aggregate trade stream

    Args:
      symbol: Trading pair symbol, e.g. `BTCUSDT`. The venue requires stream names to use lowercase symbols on the wire.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/ws-streams/market#aggregate-trade-streams)
    """
    _validator = validator[AggTradeEvent](AggTradeEvent)
    return self.subscribe(
      f'{symbol.lower()}@aggTrade', validator=_validator, validate=validate
    )
