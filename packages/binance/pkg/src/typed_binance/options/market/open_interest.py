# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class OpenInterest(TypedDict):
  """Open interest for one option symbol."""

  symbol: NotRequired[str]
  """Option trading pair, formatted UNDERLYING-EXPIRYDATE-STRIKE-C|P, e.g. BTC-260925-145000-C (a BTC call expiring 2026-09-25 with strike 145000)."""
  sumOpenInterest: NotRequired[str]
  """Open interest, in contracts."""
  sumOpenInterestUsd: NotRequired[str]
  """Open interest, in USD."""
  timestamp: NotRequired[TimestampMillis]
  """Timestamp."""


class OpenInterestEndpoint(RpcEndpoint):
  """Get open interest for a specific underlying asset on a specific expiration date."""

  async def open_interest(
    self,
    *,
    underlying_asset: str,
    expiration: str,
    validate: bool | None = None,
  ) -> list[OpenInterest]:
    """Get open interest for a specific underlying asset on a specific expiration date.

    Args:
      underlying_asset: Underlying asset, e.g. BTC.
      expiration: Expiration date, e.g. 221225.

    References:
      - [Official docs](https://developers.binance.com/docs/derivatives/option/market-data#open-interest)
    """
    params: dict = {
      'underlyingAsset': underlying_asset,
      'expiration': expiration,
    }
    _Response = list[OpenInterest]
    _validator = validator[_Response](_Response)
    return await self.request(
      'GET',
      '/eapi/v1/openInterest',
      params=params,
      validator=_validator,
      validate=validate,
    )
