# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class OrderBook(TypedDict):
  """Order book depth for one option symbol."""

  bids: NotRequired[list[tuple[str, str]]]
  """Bid (buy) orders, best price first."""
  asks: NotRequired[list[tuple[str, str]]]
  """Ask (sell) orders, best price first."""
  T: NotRequired[TimestampMillis]
  """Transaction time."""
  lastUpdateId: NotRequired[int]
  """Update ID."""


class Depth(RpcEndpoint):
  """Check order-book depth on a specific option symbol."""

  async def depth(
    self,
    *,
    symbol: str,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> OrderBook:
    """Check order-book depth on a specific option symbol.

    Args:
      symbol: Option trading pair, formatted UNDERLYING-EXPIRYDATE-STRIKE-C|P, e.g. BTC-260925-145000-C (a BTC call expiring 2026-09-25 with strike 145000).
      limit: Number of price levels per side. Default 100, max 1000. Valid values: 10, 20, 50, 100, 500, 1000.

    References:
      - [Official docs](https://developers.binance.com/docs/derivatives/option/market-data#order-book)
    """
    params: dict = {
      'symbol': symbol,
    }
    if limit is not None:
      params['limit'] = limit
    _Response = OrderBook
    _validator = validator[_Response](_Response)
    return await self.request(
      'GET', '/eapi/v1/depth', params=params, validator=_validator, validate=validate
    )
