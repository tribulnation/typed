# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class CancelAllResult(TypedDict):
  """Acknowledgement of a cancel-all request."""

  code: NotRequired[int]
  """Result code."""
  msg: NotRequired[str]
  """Result message."""


class AllOpenOrdersByUnderlyingCancel(RpcEndpoint):
  """Cancel all active orders on a specified underlying."""

  async def all_open_orders_by_underlying_cancel(
    self,
    *,
    underlying: str,
    validate: bool | None = None,
  ) -> CancelAllResult:
    """Cancel all active orders on a specified underlying.

    Args:
      underlying: Underlying asset, e.g. BTCUSDT.

    References:
      - [Official docs](https://developers.binance.com/docs/derivatives/option/trade#cancel-all-option-orders-by-underlying)
    """
    params: dict = {
      'underlying': underlying,
    }
    _Response = CancelAllResult
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'DELETE',
      '/eapi/v1/allOpenOrdersByUnderlying',
      params=params,
      validator=_validator,
      validate=validate,
    )
