# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Ack(TypedDict):
  """Response body; the source SDK types this endpoint's response as `None` (no response model at all), unlike its `order_new`/`order_extend`/`order_accept` siblings which all echo the settled order."""


class OrderCancel(RpcEndpoint):
  """Cancel a block trade order."""

  async def order_cancel(
    self,
    *,
    block_order_matching_key: str,
    validate: bool | None = None,
  ) -> Ack:
    """Cancel a block trade order.

    Args:
      block_order_matching_key: Block trade matching key.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-options/api/rest-api/market-maker-block-trade#cancel-block-trade-order)
    """
    params: dict = {
      'blockOrderMatchingKey': block_order_matching_key,
    }
    _Response = Ack
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'DELETE',
      '/eapi/v1/block/order/create',
      params=params,
      validator=_validator,
      validate=validate,
    )
