# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any
from typed_binance.core.endpoint.rpc import RpcEndpoint


OptionsListenKeyCloseResult = dict[str, Any]


class ListenKeyClose(RpcEndpoint):
  """Close the account's Options user data stream."""

  async def listen_key_close(
    self,
    *,
    validate: bool | None = None,
  ) -> OptionsListenKeyCloseResult:
    """Close the account's Options user data stream.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-options/api/rest-api/user-data-streams)
    """
    return await self.request(
      None,
      method='DELETE',
      path='/eapi/v1/listenKey',
      meta={'security': 'USER_STREAM', 'signed': False},
      validate=validate,
      response_type=OptionsListenKeyCloseResult,
    )
