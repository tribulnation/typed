# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class OptionsListenKey(TypedDict):
  """Listen key identifying an Options user data stream."""

  listenKey: str
  """Listen key used to connect to the Options user data WebSocket stream."""


class ListenKeyCreate(RpcEndpoint):
  """Start a new Options user data stream and return a listen key used to connect to it over WebSocket. The stream closes after 60 minutes unless kept alive; if the account already has an active listen key, that key is returned and its validity extended."""

  async def listen_key_create(
    self, *, validate: bool | None = None
  ) -> OptionsListenKey:
    """Start a new Options user data stream and return a listen key used to connect to it over WebSocket. The stream closes after 60 minutes unless kept alive; if the account already has an active listen key, that key is returned and its validity extended.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-options/api/rest-api/user-data-streams)
    """
    return await self.request(
      None,
      method='POST',
      path='/eapi/v1/listenKey',
      meta={'security': 'USER_STREAM', 'signed': False},
      validate=validate,
      response_type=OptionsListenKey,
    )
