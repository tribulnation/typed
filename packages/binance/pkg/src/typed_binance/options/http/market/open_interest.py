# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class OpenInterest(TypedDict):
  """Open interest for one option symbol."""

  symbol: str
  """Option trading pair, formatted UNDERLYING-EXPIRYDATE-STRIKE-C|P, e.g. BTC-260925-145000-C (a BTC call expiring 2026-09-25 with strike 145000)."""
  sumOpenInterest: Decimal
  """Open interest, in contracts."""
  sumOpenInterestUsd: Decimal
  """Open interest, in USD."""
  timestamp: TimestampMillis
  """Timestamp."""


class Request(TypedDict):
  """Request parameters."""

  underlyingAsset: str
  """Underlying asset."""
  expiration: str
  """Expiration date."""


OpenInterestList = list[OpenInterest]


class OpenInterestEndpoint(RpcEndpoint):
  """Get open interest for a specific underlying asset on a specific expiration date."""

  async def open_interest(
    self,
    *,
    underlying_asset: str,
    expiration: str,
    validate: bool | None = None,
  ) -> OpenInterestList:
    """Get open interest for a specific underlying asset on a specific expiration date.

    Args:
      underlying_asset: Underlying asset.
      expiration: Expiration date.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/docs/derivatives/option/market-data#open-interest)
    """
    return await self.request(
      Request(underlyingAsset=underlying_asset, expiration=expiration),
      method='GET',
      path='/eapi/v1/openInterest',
      meta={'security': 'NONE', 'signed': False},
      validate=validate,
      request_type=Request,
      response_type=OpenInterestList,
    )
