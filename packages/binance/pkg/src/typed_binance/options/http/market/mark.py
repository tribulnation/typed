# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class MarkPrice(TypedDict):
  """Mark price and Greeks for one option symbol."""

  symbol: str
  """Option trading pair, formatted UNDERLYING-EXPIRYDATE-STRIKE-C|P, e.g. BTC-260925-145000-C (a BTC call expiring 2026-09-25 with strike 145000)."""
  markPrice: Decimal
  """Mark price."""
  bidIV: Decimal
  """Implied volatility, buy side."""
  askIV: Decimal
  """Implied volatility, sell side."""
  markIV: Decimal
  """Implied volatility, mark."""
  delta: Decimal
  """Option delta."""
  theta: Decimal
  """Option theta."""
  gamma: Decimal
  """Option gamma."""
  vega: Decimal
  """Option vega."""
  highPriceLimit: Decimal
  """Current highest allowed buy price."""
  lowPriceLimit: Decimal
  """Current lowest allowed sell price."""
  riskFreeInterest: Decimal
  """Risk-free interest rate used in pricing."""


class Request(TypedDict):
  """Request parameters."""

  symbol: NotRequired[str]
  """Option trading pair, formatted UNDERLYING-EXPIRYDATE-STRIKE-C|P, e.g. BTC-260925-145000-C (a BTC call expiring 2026-09-25 with strike 145000)."""


MarkPriceList = list[MarkPrice]


class Mark(RpcEndpoint):
  """Option mark price and Greek info."""

  async def mark(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> MarkPriceList:
    """Option mark price and Greek info.

    Args:
      symbol: Option trading pair, formatted UNDERLYING-EXPIRYDATE-STRIKE-C|P, e.g. BTC-260925-145000-C (a BTC call expiring 2026-09-25 with strike 145000).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/docs/derivatives/option/market-data#option-mark-price)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='/eapi/v1/mark',
      meta={'security': 'NONE', 'signed': False},
      validate=validate,
      request_type=Request,
      response_type=MarkPriceList,
    )
