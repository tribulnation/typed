# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class ExerciseHistoryRecord(TypedDict):
  """One historical exercise record."""

  symbol: str
  """Option trading pair, formatted UNDERLYING-EXPIRYDATE-STRIKE-C|P, e.g. BTC-260925-145000-C (a BTC call expiring 2026-09-25 with strike 145000)."""
  strikePrice: Decimal
  """Strike price."""
  realStrikePrice: Decimal
  """Real (settlement) strike price."""
  expiryDate: TimestampMillis
  """Exercise time."""
  strikeResult: Literal['REALISTIC_VALUE_STRICKEN', 'EXTRINSIC_VALUE_EXPIRED']
  """Exercise result. REALISTIC_VALUE_STRICKEN means the option was exercised in the money; EXTRINSIC_VALUE_EXPIRED means it expired out of the money."""


class Request(TypedDict):
  """Request parameters."""

  underlying: NotRequired[str]
  """Underlying asset."""
  startTime: NotRequired[TimestampMillis]
  """Start time."""
  endTime: NotRequired[TimestampMillis]
  """End time."""
  limit: NotRequired[int]
  """Result-set size."""


ExerciseHistoryList = list[ExerciseHistoryRecord]


class ExerciseHistory(RpcEndpoint):
  """Get historical exercise records. REALISTIC_VALUE_STRICKEN means exercised; EXTRINSIC_VALUE_EXPIRED means expired out of the money."""

  async def exercise_history(
    self,
    underlying: str | None = None,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> ExerciseHistoryList:
    """Get historical exercise records. REALISTIC_VALUE_STRICKEN means exercised; EXTRINSIC_VALUE_EXPIRED means expired out of the money.

    Args:
      underlying: Underlying asset.
      start_time: Start time.
      end_time: End time.
      limit: Result-set size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/docs/derivatives/option/market-data#historical-exercise-records)
    """
    request: Request = Request()
    if underlying is not None:
      request['underlying'] = underlying
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/eapi/v1/exerciseHistory',
      meta={'security': 'NONE', 'signed': False},
      validate=validate,
      request_type=Request,
      response_type=ExerciseHistoryList,
    )
