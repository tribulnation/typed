# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class IndexPrice(TypedDict):
  """Spot index price for an option underlying."""

  time: TimestampMillis
  """Time."""
  indexPrice: Decimal
  """Spot index price for the underlying."""


class Request(TypedDict):
  """Request parameters."""

  underlying: str
  """Underlying asset."""


class Index(RpcEndpoint):
  """Get spot index price for an option underlying."""

  async def index(self, underlying: str, *, validate: bool | None = None) -> IndexPrice:
    """Get spot index price for an option underlying.

    Args:
      underlying: Underlying asset.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/docs/derivatives/option/market-data#index-price)
    """
    return await self.request(
      Request(underlying=underlying),
      method='GET',
      path='/eapi/v1/index',
      meta={'security': 'NONE', 'signed': False},
      validate=validate,
      request_type=Request,
      response_type=IndexPrice,
    )
