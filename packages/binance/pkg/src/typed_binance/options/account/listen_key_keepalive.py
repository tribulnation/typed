# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class ListenKeyKeepalive(RpcEndpoint):
  """Extend the validity of the current Options user data stream's listen key by 60 minutes from now."""

  async def listen_key_keepalive(
    self,
    *,
    listen_key: str | None = None,
    validate: bool | None = None,
  ) -> dict[str, Any]:
    """Extend the validity of the current Options user data stream's listen key by 60 minutes from now.

    Args:
      listen_key: Listen key returned by `listen_key_create`, identifying the Options stream to extend. Not a credential -- it is the stream identifier, and the account has at most one active Options listen key at a time.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-options/api/rest-api/user-data-streams)
    """
    params = {}
    if listen_key is not None:
      params['listenKey'] = listen_key
    _Response = dict[str, Any]
    _validator = validator[_Response](_Response)
    return await self.request(
      'PUT',
      '/eapi/v1/listenKey',
      params=params,
      validator=_validator,
      validate=validate,
    )
