# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .http import Http
from .private_streams import PrivateStreams
from .streams import Streams
from typed_binance.options.core import OptionsBase


class Options(OptionsBase):
  """Binance Options -- REST, market-data streams, and the private user-data stream, together.

  References:
    - [Upstream docs](https://developers.binance.com/docs/derivatives/options-trading/general-info)
  """

  @cached_property
  def http(self) -> Http:
    """Binance Options' REST API -- account, market-data, trading, block-trade, and market-maker endpoints, signed with API-key/secret.

    References:
      - [Upstream docs](https://developers.binance.com/docs/derivatives/options-trading/general-info)
    """
    return Http(client=self.http_client)

  @cached_property
  def private_streams(self) -> PrivateStreams:
    """Options' User Data Stream -- a single listenKey-scoped WebSocket connection pushing account, Greek, and order events; no subscribe frame, the venue starts pushing on connect.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/products/derivatives-trading-options/user-data-streams)
    """
    return PrivateStreams(client=self.private_streams_client)

  @cached_property
  def streams(self) -> Streams:
    """Binance Options' public market-data WebSocket streams -- trades, order book (depth/diff-depth/book-ticker), klines, mark price, index, open interest, and ticker.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-options/api/ws-streams/market)
    """
    return Streams(client=self.streams_client)
