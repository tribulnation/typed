# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.private_stream import PrivateStreamEndpoint


class AccountUpdateEvent(TypedDict):
  """Pushed on account deposit/withdrawal, a position info change, or periodically every 10s while a position is open. All monetary fields are in USDT."""

  e: Literal['ACCOUNT_UPDATE']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  T: TimestampMillis
  """Transaction time."""
  eq: Decimal
  """Account equity, in USDT."""
  aeq: Decimal
  """Account adjusted equity, in USDT."""
  b: Decimal
  """Account wallet balance, in USDT."""
  m: Decimal
  """Position value."""
  u: Decimal
  """Unrealized PnL."""
  i: Decimal
  """Initial margin, in USDT."""
  M: Decimal
  """Maintenance margin, in USDT."""


class ListenKeyExpiredEvent(TypedDict):
  """Pushed when the listenKey backing this connection expires. Unrelated to socket disconnection; only fires for a listenKey that was genuinely valid and then expired. No further user-data events arrive on this connection after it, until a new listenKey is used."""

  e: Literal['listenKeyExpired']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  listenKey: NotRequired[str]
  """The listenKey that expired."""


class OptionsBalanceUpdate(TypedDict):
  """One margin asset balance changed by this update."""

  a: NotRequired[str]
  """Margin asset."""
  b: NotRequired[Decimal]
  """Account balance."""
  bc: NotRequired[Decimal]
  """Balance change, excluding PnL and commission."""


class OptionsGreek(TypedDict):
  """One underlying's option Greeks."""

  u: NotRequired[str]
  """Underlying."""
  d: NotRequired[Decimal]
  """Delta."""
  g: NotRequired[Decimal]
  """Gamma."""
  t: NotRequired[Decimal]
  """Theta."""
  v: NotRequired[Decimal]
  """Vega."""


class OptionsOrderUpdateDetail(TypedDict):
  """The order this event is about."""

  s: str
  """Symbol."""
  c: str
  """Client order id."""
  S: Literal['BUY', 'SELL']
  """Side."""
  o: Literal['LIMIT']
  """Order type."""
  f: Literal['GTC', 'IOC', 'FOK', 'GTX']
  """Time in force."""
  q: Decimal
  """Original quantity."""
  p: Decimal
  """Original price."""
  ap: Decimal
  """Average price."""
  x: Literal['NEW', 'CANCELED', 'EXPIRED', 'TRADE']
  """Execution type."""
  X: Literal['NEW', 'PARTIALLY_FILLED', 'FILLED', 'CANCELED', 'EXPIRED']
  """Order status."""
  i: int
  """Order id."""
  l: NotRequired[Decimal]
  """Order last-filled quantity."""
  z: NotRequired[Decimal]
  """Order filled accumulated quantity."""
  L: NotRequired[Decimal]
  """Last filled price."""
  N: NotRequired[str]
  """Commission asset. Only present when a commission was charged."""
  n: NotRequired[Decimal]
  """Commission, negative means a fee charge. Only present when a commission was charged."""
  T: NotRequired[TimestampMillis]
  """Order trade time."""
  t: NotRequired[int]
  """Trade id."""
  b: NotRequired[Decimal]
  """Bids quantity."""
  a: NotRequired[Decimal]
  """Ask quantity."""
  m: NotRequired[bool]
  """Whether this trade is the maker side."""
  R: NotRequired[bool]
  """Whether this order only reduces an existing position."""
  ot: NotRequired[Literal['LIMIT']]
  """Original order type."""
  rp: NotRequired[Decimal]
  """Realized profit of the trade."""
  V: NotRequired[Literal['NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER']]
  """Self-trade prevention mode."""


class OptionsPositionUpdate(TypedDict):
  """One position changed by this update."""

  s: NotRequired[str]
  """Symbol."""
  c: NotRequired[Decimal]
  """Position quantity."""
  p: NotRequired[Decimal]
  """Position value."""
  a: NotRequired[Decimal]
  """Average entry price."""


class RiskLevelChangeEvent(TypedDict):
  """Pushed on an account risk-level change between NORMAL and REDUCE_ONLY. Only applies to VIP/Market Maker accounts, automatically placed into REDUCE_ONLY when margin balance falls short of maintenance margin; re-evaluated only on a funds transfer, trade fill, or option expiry."""

  e: Literal['RISK_LEVEL_CHANGE']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: Literal['NORMAL', 'REDUCE_ONLY']
  """Risk level. Only applies to VIP and Market Maker accounts; re-evaluated on funds transfer, trade fill, or option expiry."""
  mb: Decimal
  """Margin balance."""
  mm: Decimal
  """Maintenance margin."""


class BalancePositionUpdateEvent(TypedDict):
  """Pushed only when a balance or position actually changes -- an unfilled or cancelled order alone does not push it. Only the symbols of changed positions are included."""

  e: Literal['BALANCE_POSITION_UPDATE']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  T: TimestampMillis
  """Transaction time."""
  m: Literal['DEPOSIT', 'WITHDRAW', 'ORDER']
  """Event reason type."""
  B: list[OptionsBalanceUpdate]
  """One margin asset balance changed by this update."""
  P: list[OptionsPositionUpdate]
  """One position changed by this update."""


class GreekUpdateEvent(TypedDict):
  """Pushed when a position changes, and periodically every 10s while a position is open."""

  e: Literal['GREEK_UPDATE']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  T: TimestampMillis
  """Transaction time."""
  G: list[OptionsGreek]
  """One underlying's option Greeks."""


class OrderTradeUpdateEvent(TypedDict):
  """Pushed when an order is created or its status changes."""

  e: Literal['ORDER_TRADE_UPDATE']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  T: TimestampMillis
  """Transaction time."""
  o: OptionsOrderUpdateDetail


Payload = (
  AccountUpdateEvent
  | OrderTradeUpdateEvent
  | BalancePositionUpdateEvent
  | GreekUpdateEvent
  | RiskLevelChangeEvent
  | ListenKeyExpiredEvent
)


class UserData(PrivateStreamEndpoint):
  """One always-on connection carrying every private user-data event for this account on this product, multiplexed with no way to subscribe to a subset -- connecting with a live listenKey already embedded in the URL path is the subscription. Each pushed frame is one of the event variants below, discriminated by its own `e` field."""

  def user_data(
    self,
    listen_key: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[Payload, Any, Any]:
    """One always-on connection carrying every private user-data event for this account on this product, multiplexed with no way to subscribe to a subset -- connecting with a live listenKey already embedded in the URL path is the subscription. Each pushed frame is one of the event variants below, discriminated by its own `e` field.

    Args:
      listen_key: Live listenKey for this connection, minted by `options.account.listen_key_create` and kept alive per its own docstring.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/products/derivatives-trading-options/user-data-streams)
    """
    return self.subscribe(
      listen_key,
      validate=validate,
      response_type=Payload,
    )
