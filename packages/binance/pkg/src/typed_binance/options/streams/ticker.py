# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class Ticker24hrEvent(TypedDict):
  """24hr rolling-window ticker statistics for one option symbol."""

  e: Literal['24hrTicker']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Option symbol."""
  p: str
  """Price change."""
  P: str
  """Price change percent."""
  w: str
  """Weighted average price."""
  c: str
  """Last price."""
  Q: str
  """Last quantity."""
  o: str
  """Open price."""
  h: str
  """High price."""
  l: str
  """Low price."""
  v: str
  """Trading volume, in contracts."""
  q: str
  """Trade amount, in quote asset."""
  O: TimestampMillis
  """Statistics open time."""
  C: TimestampMillis
  """Statistics close time."""
  F: int
  """First trade ID."""
  L: int
  """Last trade ID."""
  n: int
  """Total number of trades."""


class Ticker(StreamEndpoint):
  """Pushes 24hr rolling-window ticker statistics for one option symbol, every 1000ms."""

  def ticker(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[Ticker24hrEvent, Any, Any]:
    """Pushes 24hr rolling-window ticker statistics for one option symbol, every 1000ms.

    Args:
      symbol: Option symbol, e.g. `BTC-250627-50000-C`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-options/api/ws-streams/public#hour24-ticker)
    """
    return self.subscribe(
      f'{symbol}@ticker',
      validate=validate,
      response_type=Ticker24hrEvent,
    )
