# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class IndexPriceEvent(TypedDict):
  """Index price update for one underlying asset."""

  e: Literal['indexPrice']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Underlying asset symbol, e.g. `ETHUSDT`."""
  p: str
  """Index price."""


Payload = list[IndexPriceEvent]


class IndexArr(StreamEndpoint):
  """Pushes the index price for every underlying asset with active option listings, as an array, every 1000ms."""

  def index_arr(
    self, *, validate: bool | None = None
  ) -> StreamManager[Payload, Any, Any]:
    """Pushes the index price for every underlying asset with active option listings, as an array, every 1000ms.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-options/api/ws-streams/market#index-price-streams)
    """
    return self.subscribe(
      '!index@arr',
      None,
      validate=validate,
      response_type=Payload,
    )
