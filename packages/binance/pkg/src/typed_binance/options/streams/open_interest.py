# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class OpenInterestEvent(TypedDict):
  """Open interest update for one option symbol."""

  e: Literal['openInterest']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Option symbol."""
  o: str
  """Open interest, in contracts."""
  h: str
  """Open interest, in USDT."""


Payload = list[OpenInterestEvent]


class OpenInterest(StreamEndpoint):
  """Pushes open interest, one entry per option symbol, for one underlying asset on one expiration date, every 60s."""

  def open_interest(
    self,
    *,
    underlying: str,
    expiration_date: str,
    validate: bool | None = None,
  ) -> StreamManager[Payload, Any, Any]:
    """Pushes open interest, one entry per option symbol, for one underlying asset on one expiration date, every 60s.

    Args:
      underlying: Underlying asset, e.g. `BTCUSDT`.
      expiration_date: Expiration date, formatted `YYMMDD`, e.g. `220930`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-options/api/ws-streams/market#open-interest)
    """
    return self.subscribe(
      f'{underlying}@openInterest@{expiration_date}',
      validate=validate,
      response_type=Payload,
    )
