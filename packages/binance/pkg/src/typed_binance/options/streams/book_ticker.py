# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class BookTickerEvent(TypedDict):
  """Best bid/ask price and quantity update for one option symbol's order book."""

  e: Literal['bookTicker']
  """Event type."""
  u: int
  """Order book update ID."""
  s: str
  """Option symbol."""
  b: str
  """Best bid price."""
  B: str
  """Best bid quantity."""
  a: str
  """Best ask price."""
  A: str
  """Best ask quantity."""
  T: TimestampMillis
  """Transaction time."""
  E: TimestampMillis
  """Event time."""


class BookTicker(StreamEndpoint):
  """Pushes any update to the best bid or ask price or quantity, in real time, for one option symbol."""

  def book_ticker(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[BookTickerEvent, Any, Any]:
    """Pushes any update to the best bid or ask price or quantity, in real time, for one option symbol.

    Args:
      symbol: Option symbol, e.g. `BTC-250627-50000-C`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-options/api/ws-streams/public#individual-symbol-book-ticker-streams)
    """
    return self.subscribe(
      f'{symbol}@bookTicker',
      validate=validate,
      response_type=BookTickerEvent,
    )
