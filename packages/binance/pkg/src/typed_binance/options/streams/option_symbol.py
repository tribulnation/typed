# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class NewSymbolEvent(TypedDict):
  """Info for one newly-listed option symbol."""

  e: Literal['optionSymbol']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Option symbol."""
  ps: str
  """Underlying asset of the contract."""
  qa: str
  """Quotation asset."""
  d: Literal['CALL', 'PUT']
  """Option type."""
  sp: str
  """Strike price."""
  dt: TimestampMillis
  """Delivery (expiry) date/time."""
  u: int
  """Contract unit -- the quantity of the underlying asset represented by a single contract."""
  ot: TimestampMillis
  """Onboard (listing) date/time."""
  cs: str
  """Contract status."""


class OptionSymbol(StreamEndpoint):
  """Pushes info for a newly-listed option symbol, every 50ms."""

  def option_symbol(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[NewSymbolEvent, Any, Any]:
    """Pushes info for a newly-listed option symbol, every 50ms.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-options/api/ws-streams/market#new-symbol-info)
    """
    return self.subscribe(
      '!optionSymbol',
      None,
      validate=validate,
      response_type=NewSymbolEvent,
    )
