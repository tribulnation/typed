# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_core.util import StreamManager
from typed_core.validation import validator
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class TradeEvent(TypedDict):
  """One executed option trade."""

  e: Literal['trade']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  T: TimestampMillis
  """Trade completed time."""
  s: str
  """Option symbol."""
  t: int
  """Trade ID."""
  p: str
  """Price."""
  q: str
  """Quantity, always positive."""
  X: Literal['MARKET', 'BLOCK']
  """Trade type."""
  S: str
  """Direction. Only `"BUY"` has been observed; the venue does not document this field's full value set."""
  m: bool
  """Whether the buyer was the market maker."""


class Trade(StreamEndpoint):
  """Trade streams"""

  def __call__(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[TradeEvent]:
    """Trade streams

    Args:
      symbol: Option symbol, e.g. `BTC-250627-50000-C`.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-options/api/ws-streams/public#trade-streams)
    """
    _validator = validator[TradeEvent](TradeEvent)
    return self.subscribe(
      f'{symbol.lower()}@optionTrade', validator=_validator, validate=validate
    )
