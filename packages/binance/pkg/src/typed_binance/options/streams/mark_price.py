# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class MarkPriceEvent(TypedDict):
  """Mark price and Greeks update for one option symbol."""

  s: str
  """Option symbol."""
  mp: str
  """Mark price."""
  E: TimestampMillis
  """Event time."""
  e: Literal['markPrice']
  """Event type."""
  i: str
  """Index price."""
  P: str
  """Estimated settle price, only meaningful in the 30 minutes before settlement starts."""
  bo: str
  """Best buy (bid) price."""
  ao: str
  """Best sell (ask) price."""
  bq: str
  """Best buy (bid) quantity."""
  aq: str
  """Best sell (ask) quantity."""
  b: str
  """Buy implied volatility."""
  a: str
  """Sell implied volatility."""
  hl: str
  """Buy maximum price."""
  ll: str
  """Sell minimum price."""
  vo: str
  """Mark implied volatility."""
  rf: str
  """Risk-free interest rate."""
  d: str
  """Delta."""
  t: str
  """Theta."""
  g: str
  """Gamma."""
  v: str
  """Vega."""


Payload = list[MarkPriceEvent]


class MarkPrice(StreamEndpoint):
  """Pushes mark price, Greeks, implied volatility, and best bid/ask for every option symbol on one underlying asset, as an array, every 1000ms."""

  def mark_price(
    self,
    underlying: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[Payload, Any, Any]:
    """Pushes mark price, Greeks, implied volatility, and best bid/ask for every option symbol on one underlying asset, as an array, every 1000ms.

    Args:
      underlying: Underlying asset, e.g. `BTCUSDT`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-options/api/ws-streams/market#option-mark-price)
    """
    return self.subscribe(
      f'{underlying}@optionMarkPrice',
      validate=validate,
      response_type=Payload,
    )
