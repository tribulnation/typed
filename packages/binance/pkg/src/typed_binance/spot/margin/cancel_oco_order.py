# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class MarginCancelOcoOrderReport(TypedDict):
  """One order-list leg's cancellation acknowledgement."""

  symbol: str
  """Trading pair symbol."""
  origClientOrderId: str
  """Client order id of the leg as it was before this cancellation."""
  orderId: int
  """Order id."""
  orderListId: int
  """Id of the order list this order belongs to."""
  clientOrderId: str
  """Client id assigned to this cancellation request."""
  price: str
  """Order price."""
  origQty: str
  """Original order quantity."""
  executedQty: str
  """Cumulative filled quantity."""
  cummulativeQuoteQty: str
  """Cumulative quote asset transacted quantity."""
  status: Literal[
    'NEW',
    'PENDING_NEW',
    'PARTIALLY_FILLED',
    'FILLED',
    'CANCELED',
    'PENDING_CANCEL',
    'REJECTED',
    'EXPIRED',
    'EXPIRED_IN_MATCH',
  ]
  """Order status."""
  timeInForce: Literal['GTC', 'IOC', 'FOK']
  """Time in force."""
  type: Literal[
    'LIMIT',
    'MARKET',
    'STOP_LOSS',
    'STOP_LOSS_LIMIT',
    'TAKE_PROFIT',
    'TAKE_PROFIT_LIMIT',
    'LIMIT_MAKER',
  ]
  """Order type."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  stopPrice: str
  """Price at which the leg's stop triggers. "0.000000" for the non-stop leg."""


class MarginOrderListOrder(TypedDict):
  """One order belonging to the order list, identified minimally."""

  symbol: str
  """Trading pair symbol."""
  orderId: int
  """Order id."""
  clientOrderId: str
  """Client order id, either caller-supplied or venue-generated."""


class MarginCancelOcoOrderResult(TypedDict):
  """Result of canceling an entire margin OCO order list."""

  orderListId: int
  """Id of the canceled order list."""
  contingencyType: Literal['OCO']
  """Contingency type of the order list."""
  listStatusType: Literal['RESPONSE', 'EXEC_STARTED', 'UPDATED', 'ALL_DONE']
  """Status of the order list."""
  listOrderStatus: Literal['EXECUTING', 'ALL_DONE', 'REJECT']
  """Execution status of the order list."""
  listClientOrderId: str
  """Client id for the entire order list, either caller-supplied or venue-generated."""
  transactionTime: TimestampMillis
  """Time the cancellation was processed."""
  symbol: str
  """Trading pair symbol."""
  isIsolated: bool
  """Whether the order list belongs to an isolated margin account."""
  orders: list[MarginOrderListOrder]
  """The legs that made up the canceled OCO, identified minimally."""
  orderReports: list[MarginCancelOcoOrderReport]
  """Per-leg cancellation acknowledgements."""


class CancelOcoOrder(RpcEndpoint):
  """Cancel an entire margin OCO order list. Canceling an individual leg cancels the entire OCO. Either orderListId or listClientOrderId must be provided."""

  async def cancel_oco_order(
    self,
    *,
    symbol: str,
    is_isolated: Literal['TRUE', 'FALSE'] | None = None,
    order_list_id: int | None = None,
    list_client_order_id: str | None = None,
    new_client_order_id: str | None = None,
    validate: bool | None = None,
  ) -> MarginCancelOcoOrderResult:
    """Cancel an entire margin OCO order list. Canceling an individual leg cancels the entire OCO. Either orderListId or listClientOrderId must be provided.

    Args:
      symbol: Trading pair symbol, e.g. BNBUSDT.
      is_isolated: Margin account type for this order: isolated when TRUE, crossed (default) when FALSE.
      order_list_id: Id of the order list to cancel. Either orderListId or listClientOrderId must be provided.
      list_client_order_id: Client id of the order list to cancel. Either orderListId or listClientOrderId must be provided.
      new_client_order_id: Client id to uniquely identify this cancel. Automatically generated if not sent.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/trade#margin-account-cancel-oco)
    """
    params: dict = {
      'symbol': symbol,
    }
    if is_isolated is not None:
      params['isIsolated'] = is_isolated
    if order_list_id is not None:
      params['orderListId'] = order_list_id
    if list_client_order_id is not None:
      params['listClientOrderId'] = list_client_order_id
    if new_client_order_id is not None:
      params['newClientOrderId'] = new_client_order_id
    _Response = MarginCancelOcoOrderResult
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'DELETE',
      '/sapi/v1/margin/orderList',
      params=params,
      validator=_validator,
      validate=validate,
    )
