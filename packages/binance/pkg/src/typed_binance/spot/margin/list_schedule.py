# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class MarginListSchedule(TypedDict):
  """One scheduled listing event."""

  listTime: TimestampMillis
  """Millisecond epoch the listing takes effect."""
  crossMarginAssets: list[str]
  """Cross margin assets being listed."""
  isolatedMarginSymbols: list[str]
  """Isolated margin symbols being listed."""


class ListSchedule(RpcEndpoint):
  """Get List Schedule"""

  async def list_schedule(
    self,
    *,
    validate: bool | None = None,
  ) -> list[MarginListSchedule]:
    """Query the upcoming listing schedule for cross margin assets and isolated margin symbols becoming newly available for margin trading.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/market-data#get-list-schedule)
    """
    _Response = list[MarginListSchedule]
    _validator = validator[_Response](_Response)
    return await self.request(
      'GET', '/sapi/v1/margin/list-schedule', validator=_validator, validate=validate
    )
