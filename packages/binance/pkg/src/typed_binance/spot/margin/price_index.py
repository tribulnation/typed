# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class MarginPriceIndex(TypedDict):
  """Margin price index for one symbol."""

  calcTime: TimestampMillis
  """Millisecond epoch this price index was calculated."""
  price: str
  """Price index value, as a decimal string."""
  symbol: str
  """Trading pair symbol, e.g. BNBBTC."""


class PriceIndex(RpcEndpoint):
  """Query Margin PriceIndex"""

  async def price_index(
    self,
    *,
    symbol: str,
    validate: bool | None = None,
  ) -> MarginPriceIndex:
    """Query the margin price index for a symbol, used to value cross margin collateral and calculate liquidation risk.

    Args:
      symbol: Trading pair to query, e.g. BNBBTC.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/market-data#query-margin-priceindex)
    """
    params: dict = {
      'symbol': symbol,
    }
    _Response = MarginPriceIndex
    _validator = validator[_Response](_Response)
    return await self.keyed_request(
      'GET',
      '/sapi/v1/margin/priceIndex',
      params=params,
      validator=_validator,
      validate=validate,
    )
