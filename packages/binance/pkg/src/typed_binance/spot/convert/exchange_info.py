# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class ConvertPair(TypedDict):
  """One convertible token pair and its tradable amount limits."""

  fromAsset: NotRequired[str]
  """Asset the user spends."""
  toAsset: NotRequired[str]
  """Asset the user receives."""
  fromAssetMinAmount: NotRequired[str]
  """Minimum `fromAsset` amount tradable in one convert."""
  fromAssetMaxAmount: NotRequired[str]
  """Maximum `fromAsset` amount tradable in one convert."""
  toAssetMinAmount: NotRequired[str]
  """Minimum `toAsset` amount tradable in one convert."""
  toAssetMaxAmount: NotRequired[str]
  """Maximum `toAsset` amount tradable in one convert."""


class ExchangeInfo(RpcEndpoint):
  """Query for all convertible token pairs and each pair's upper/lower amount limits."""

  async def exchange_info(
    self,
    *,
    from_asset: str | None = None,
    to_asset: str | None = None,
    validate: bool | None = None,
  ) -> list[ConvertPair]:
    """Query for all convertible token pairs and each pair's upper/lower amount limits.

    Args:
      from_asset: Asset the user spends. Either this or `toAsset` (or both) must be supplied.
      to_asset: Asset the user receives. Either this or `fromAsset` (or both) must be supplied.

    References:
      - [Official docs](https://developers.binance.com/docs/convert/market-data)
    """
    params = {}
    if from_asset is not None:
      params['fromAsset'] = from_asset
    if to_asset is not None:
      params['toAsset'] = to_asset
    _Response = list[ConvertPair]
    _validator = validator[_Response](_Response)
    return await self.request(
      'GET',
      '/sapi/v1/convert/exchangeInfo',
      params=params,
      validator=_validator,
      validate=validate,
    )
