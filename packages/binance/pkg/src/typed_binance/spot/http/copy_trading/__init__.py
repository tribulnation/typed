# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .futures import Futures
from typed_binance.core.endpoint.rpc import RpcEndpoint


class CopyTrading(RpcEndpoint):
  """Copy Trading endpoints, currently only for Futures lead traders -- checking futures lead-trader qualification and the whitelist of symbols approved for futures copy trading; see `copy_trading/futures/router.json` for detail. The catalog's own Copy Trading category root has no real content of its own (resolves to the full product catalog); linking the closest real page instead (the `futures` sub-grouping's own).

  References:
    - [Upstream docs](https://developers.binance.com/en/docs/catalog/advanced-trading-copy-trading/api/rest-api/future-copy-trading)
  """

  @cached_property
  def futures(self) -> Futures:
    """Futures Copy Trading endpoints for a lead trader -- checking whether this account currently qualifies as a futures lead trader, and the whitelist of symbols approved for futures copy trading.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/advanced-trading-copy-trading/api/rest-api/future-copy-trading)
    """
    return Futures(client=self.client)
