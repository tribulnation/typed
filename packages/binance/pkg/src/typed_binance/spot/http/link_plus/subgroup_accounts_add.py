# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Request parameters."""

  groupId: int
  """Sub-group ID."""
  accountIds: list[int]
  """Sub-account IDs to add to the sub-group."""


class SubgroupAccountsAdd(RpcEndpoint):
  """Add one or more sub-accounts to a sub-group under the master account."""

  async def subgroup_accounts_add(
    self,
    group_id: int,
    *,
    account_ids: list[int],
    validate: bool | None = None,
  ):
    """Add one or more sub-accounts to a sub-group under the master account.

    Args:
      group_id: Sub-group ID.
      account_ids: Sub-account IDs to add to the sub-group.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-link-plus/api/rest-api/~#add-accounts-to-sub-groups)
    """
    return await self.request(
      Request(groupId=group_id, accountIds=account_ids),
      method='POST',
      path='/sapi/v1/vip/link-plus/subgroups/{groupId}/accounts',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      request_type=Request,
    )
