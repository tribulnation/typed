# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Request parameters."""

  groupId: int
  """Sub-group ID."""
  groupName: str
  """Updated name of the sub-group."""


class SubgroupNameUpdate(RpcEndpoint):
  """Update the name of a sub-group under the master account."""

  async def subgroup_name_update(
    self,
    group_id: int,
    *,
    group_name: str,
    validate: bool | None = None,
  ):
    """Update the name of a sub-group under the master account.

    Args:
      group_id: Sub-group ID.
      group_name: Updated name of the sub-group.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-link-plus/api/rest-api/~#update-name-of-sub-groups)
    """
    return await self.request(
      Request(groupId=group_id, groupName=group_name),
      method='PUT',
      path='/sapi/v1/vip/link-plus/subgroups/{groupId}/name',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      request_type=Request,
    )
