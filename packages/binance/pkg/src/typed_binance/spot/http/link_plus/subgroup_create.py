# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Request parameters."""

  groupName: str
  """Name of the sub-group."""


class SubgroupCreate(RpcEndpoint):
  """Create a new sub-group under the master account."""

  async def subgroup_create(self, group_name: str, *, validate: bool | None = None):
    """Create a new sub-group under the master account.

    Args:
      group_name: Name of the sub-group.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-link-plus/api/rest-api/~#create-new-sub-groups)
    """
    return await self.request(
      Request(groupName=group_name),
      method='POST',
      path='/sapi/v1/vip/link-plus/subgroups',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      request_type=Request,
    )
