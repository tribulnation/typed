# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class MiningBonus(TypedDict):
  """One extra bonus record."""

  time: NotRequired[TimestampMillis]
  """Millisecond epoch record timestamp."""
  coinName: NotRequired[str]
  """Coin the bonus is denominated in."""
  type: NotRequired[Literal[1, 2, 3, 4, 6, 7]]
  """Bonus type: 1 merged mining, 2 activity bonus, 3 rebate, 4 smart pool, 6 income transfer, 7 pool savings."""
  profitAmount: NotRequired[float]
  """Bonus amount."""
  status: NotRequired[Literal[0, 1, 2]]
  """Payout status: 0 unpaid, 1 paying, 2 paid."""


class Request(TypedDict):
  """Request parameters."""

  algo: str
  """Mining algorithm, e.g. sha256."""
  userName: str
  """Mining account."""
  coin: NotRequired[str]
  """Coin to filter by, e.g. BTC."""
  startDate: NotRequired[TimestampMillis]
  """Millisecond epoch start of the queried window."""
  endDate: NotRequired[TimestampMillis]
  """Millisecond epoch end of the queried window."""
  pageIndex: NotRequired[int]
  """Page number."""
  pageSize: NotRequired[int]
  """Number of rows per page."""


class MiningBonusListData(TypedDict):
  """Paged extra bonus list."""

  otherProfits: NotRequired[list[MiningBonus]]
  """Extra bonus records on this page."""
  totalNum: NotRequired[int]
  """Total number of matching records."""
  pageSize: NotRequired[int]
  """Number of records returned per page."""


class MiningBonusListResponse(TypedDict):
  """Extra bonus list response frame. This product line wraps its business data in {code,msg,data} on the wire, and the core does not strip it -- see spec/core.md's Envelope section."""

  code: NotRequired[int]
  """Response code. 0 indicates success."""
  msg: NotRequired[str]
  """Response message."""
  data: NotRequired[MiningBonusListData]


class BonusList(RpcEndpoint):
  """Query this mining account's extra bonus income (e.g. from merged mining or promotions)."""

  def bonus_list_paged(
    self,
    *,
    algo: str,
    user_name: str,
    coin: str | None = None,
    start_date: TimestampMillis | None = None,
    end_date: TimestampMillis | None = None,
    page_size: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[MiningBonus, int]:
    """Query this mining account's extra bonus income (e.g. from merged mining or promotions).

    Paged variant of `bonus_list`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      algo: Mining algorithm, e.g. sha256.
      user_name: Mining account.
      coin: Coin to filter by, e.g. BTC.
      start_date: Millisecond epoch start of the queried window.
      end_date: Millisecond epoch end of the queried window.
      page_size: Number of rows per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/docs/mining/rest-api#extra-bonus-list)
    """

    async def next(page_index: int) -> tuple[list[MiningBonus], int | None]:
      response = await self.bonus_list(
        algo=algo,
        user_name=user_name,
        coin=coin,
        start_date=start_date,
        end_date=end_date,
        page_size=page_size,
        page_index=page_index,
        validate=validate,
      )
      rows_0 = response.get('data')
      rows = rows_0.get('otherProfits') if rows_0 is not None else None
      rows = rows if rows is not None else []
      total_0 = response.get('data')
      total = total_0.get('totalNum') if total_0 is not None else None
      total = int(total) if total is not None else None
      if (
        total is None or page_size is None or (page_index - 1 + 1) * page_size >= total
      ):
        return rows, None
      return rows, page_index + 1

    return PaginatedResponse(1, next)

  async def bonus_list(
    self,
    *,
    algo: str,
    user_name: str,
    coin: str | None = None,
    start_date: TimestampMillis | None = None,
    end_date: TimestampMillis | None = None,
    page_index: int | None = None,
    page_size: int | None = None,
    validate: bool | None = None,
  ) -> MiningBonusListResponse:
    """Query this mining account's extra bonus income (e.g. from merged mining or promotions).

    Args:
      algo: Mining algorithm, e.g. sha256.
      user_name: Mining account.
      coin: Coin to filter by, e.g. BTC.
      start_date: Millisecond epoch start of the queried window.
      end_date: Millisecond epoch end of the queried window.
      page_index: Page number.
      page_size: Number of rows per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/docs/mining/rest-api#extra-bonus-list)
    """
    request: Request = Request(algo=algo, userName=user_name)
    if coin is not None:
      request['coin'] = coin
    if start_date is not None:
      request['startDate'] = start_date
    if end_date is not None:
      request['endDate'] = end_date
    if page_index is not None:
      request['pageIndex'] = page_index
    if page_size is not None:
      request['pageSize'] = page_size
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/mining/payment/other',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=MiningBonusListResponse,
    )
