# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class HashrateResaleRequestResponse(TypedDict):
  """Hashrate resale request response frame. This product line wraps its business data in {code,msg,data} on the wire, and the core does not strip it -- see spec/core.md's Envelope section."""

  code: NotRequired[int]
  """Response code. 0 indicates success."""
  msg: NotRequired[str]
  """Response message."""
  data: NotRequired[int]
  """Identifier (`configId`) of the newly created resale configuration."""


class Request(TypedDict):
  """Request parameters."""

  userName: str
  """Mining account the hashrate is resold from."""
  algo: str
  """Transfer algorithm."""
  startDate: TimestampMillis
  """Millisecond epoch resale start time."""
  endDate: TimestampMillis
  """Millisecond epoch resale end time."""
  toPoolUser: str
  """Mining account the hashrate is resold to."""
  hashRate: int
  """Hashrate to resell, in h/s."""


class HashrateResaleRequest(RpcEndpoint):
  """Resell part of this mining account's hashrate to another mining account for a fixed time window."""

  async def hashrate_resale_request(
    self,
    *,
    user_name: str,
    algo: str,
    start_date: TimestampMillis,
    end_date: TimestampMillis,
    to_pool_user: str,
    hash_rate: int,
    validate: bool | None = None,
  ) -> HashrateResaleRequestResponse:
    """Resell part of this mining account's hashrate to another mining account for a fixed time window.

    Args:
      user_name: Mining account the hashrate is resold from.
      algo: Transfer algorithm.
      start_date: Millisecond epoch resale start time.
      end_date: Millisecond epoch resale end time.
      to_pool_user: Mining account the hashrate is resold to.
      hash_rate: Hashrate to resell, in h/s.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/docs/mining/rest-api#hashrate-resale-request)
    """
    return await self.request(
      Request(
        userName=user_name,
        algo=algo,
        startDate=start_date,
        endDate=end_date,
        toPoolUser=to_pool_user,
        hashRate=hash_rate,
      ),
      method='POST',
      path='/sapi/v1/mining/hash-transfer/config',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=HashrateResaleRequestResponse,
    )
