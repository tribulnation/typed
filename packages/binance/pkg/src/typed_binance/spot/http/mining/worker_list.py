# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class MiningWorker(TypedDict):
  """One miner."""

  workerId: NotRequired[str]
  """Miner identifier."""
  workerName: NotRequired[str]
  """Miner name."""
  status: NotRequired[Literal[1, 2, 3]]
  """Miner status: 1 valid, 2 invalid, 3 failure (matches the `workerStatus` filter's non-zero values)."""
  hashRate: NotRequired[float]
  """Real-time hashrate."""
  dayHashRate: NotRequired[float]
  """Daily average hashrate."""
  rejectRate: NotRequired[float]
  """Real-time rejection rate."""
  lastShareTime: NotRequired[TimestampMillis]
  """Millisecond epoch timestamp of the last submitted share."""


class Request(TypedDict):
  """Request parameters."""

  algo: str
  """Mining algorithm, e.g. sha256."""
  userName: str
  """Mining account."""
  pageIndex: NotRequired[int]
  """Page number."""
  sort: NotRequired[Literal[0, 1]]
  """Sort order."""
  sortColumn: NotRequired[Literal[1, 2, 3, 4, 5]]
  """Column to sort by: 1 miner name, 2 real-time hashrate, 3 daily average hashrate, 4 real-time rejection rate, 5 last submission time."""
  workerStatus: NotRequired[Literal[0, 1, 2, 3]]
  """Miner status filter."""


class MiningWorkerListData(TypedDict):
  """Paged miner list."""

  workerDatas: NotRequired[list[MiningWorker]]
  """Miners on this page."""
  totalNum: NotRequired[int]
  """Total number of miners matching the filter."""
  pageSize: NotRequired[int]
  """Number of miners returned per page."""


class MiningWorkerListResponse(TypedDict):
  """Miner list response frame. This product line wraps its business data in {code,msg,data} on the wire, and the core does not strip it -- see spec/core.md's Envelope section."""

  code: NotRequired[int]
  """Response code. 0 indicates success."""
  msg: NotRequired[str]
  """Response message."""
  data: NotRequired[MiningWorkerListData]


class WorkerList(RpcEndpoint):
  """Query this mining account's miners."""

  async def worker_list(
    self,
    *,
    algo: str,
    user_name: str,
    page_index: int | None = None,
    sort: Literal[0, 1] | None = None,
    sort_column: Literal[1, 2, 3, 4, 5] | None = None,
    worker_status: Literal[0, 1, 2, 3] | None = None,
    validate: bool | None = None,
  ) -> MiningWorkerListResponse:
    """Query this mining account's miners.

    Args:
      algo: Mining algorithm, e.g. sha256.
      user_name: Mining account.
      page_index: Page number.
      sort: Sort order.
      sort_column: Column to sort by: 1 miner name, 2 real-time hashrate, 3 daily average hashrate, 4 real-time rejection rate, 5 last submission time.
      worker_status: Miner status filter.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/docs/mining/rest-api#request-for-miner-list)
    """
    request: Request = Request(algo=algo, userName=user_name)
    if page_index is not None:
      request['pageIndex'] = page_index
    if sort is not None:
      request['sort'] = sort
    if sort_column is not None:
      request['sortColumn'] = sort_column
    if worker_status is not None:
      request['workerStatus'] = worker_status
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/mining/worker/list',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=MiningWorkerListResponse,
    )
