# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class EquityTokenizedAsset(TypedDict):
  """One tokenized asset and its underlying equity mapping."""

  assetCode: str
  """Tokenized asset code, e.g. `AAPLB`."""
  assetName: str
  """Human-readable display name of the tokenized asset."""
  underlyingEquitySymbol: str
  """Underlying US-equity ticker, e.g. `AAPL`. Use this when calling `/tokenized/mint` or `/tokenized/redeem`."""
  multiplier: Decimal
  """Conversion multiplier between the underlying equity and the tokenized asset."""
  multiplierValid: bool
  """Whether the multiplier is currently valid and applicable."""


EquityTokenizedAssetList = list[EquityTokenizedAsset]


class TokenizedAssets(RpcEndpoint):
  """Returns the list of all tokenized assets currently available for mint / redeem, together with each asset's underlying equity symbol and conversion multiplier. MARKET_DATA endpoint -- requires an API key (`X-MBX-APIKEY`) but no signature. Response is served from a server-side cache."""

  async def tokenized_assets(
    self,
    *,
    validate: bool | None = None,
  ) -> EquityTokenizedAssetList:
    """Returns the list of all tokenized assets currently available for mint / redeem, together with each asset's underlying equity symbol and conversion multiplier. MARKET_DATA endpoint -- requires an API key (`X-MBX-APIKEY`) but no signature. Response is served from a server-side cache.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/rest-api/market-data#tokenized-assets)
    """
    return await self.request(
      None,
      method='GET',
      path='/sapi/v1/equity/market/tokenized-assets',
      meta={'security': 'MARKET_DATA', 'signed': False},
      validate=validate,
      response_type=EquityTokenizedAssetList,
    )
