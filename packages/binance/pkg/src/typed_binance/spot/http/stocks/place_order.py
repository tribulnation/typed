# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class EquityOrderAck(TypedDict):
  """Acknowledgement of an equity order placement request."""

  status: NotRequired[Literal['S', 'F']]
  """Acknowledgement code. Not an order lifecycle status -- to poll lifecycle, call `/order/detail` or `/order/history`."""
  orderId: NotRequired[str]
  """Order id (UUID)."""
  clientOrderId: NotRequired[str]
  """Echoes the supplied or server-generated client order id."""


class Request(TypedDict):
  """Request parameters."""

  symbol: str
  """US stock ticker, e.g. `AAPL`, `TSLA`. Must be a symbol with tokenization enabled -- check via `/market/tokenized-assets`."""
  quoteAsset: NotRequired[str]
  """Quote asset. Defaults to `USDC`; must be within the server's allowed set."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  orderType: Literal['MARKET', 'LIMIT']
  """Order type."""
  price: NotRequired[str]
  """Required for `LIMIT`; forbidden for `MARKET`. Maximum 2 decimal places."""
  quantity: NotRequired[str]
  """Required for `LIMIT` (both sides) and `SELL MARKET`; forbidden for `BUY MARKET`."""
  notional: NotRequired[str]
  """Required for `BUY MARKET`; forbidden for `LIMIT` and `SELL MARKET`."""
  timeInForce: NotRequired[Literal['DAY', 'GTC']]
  """`DAY` (default) / `GTC`. `GTC` is only supported for `LIMIT` orders; a fractional-share `GTC` order must be paired with `tradingSession = EXTENDED` or `24H`."""
  tradingSession: NotRequired[Literal['RTH', 'EXTENDED', '24H']]
  """Required for `LIMIT`; forbidden for `MARKET`."""
  walletType: NotRequired[Literal['CARD', 'MAIN']]
  """Payment wallet for `BUY` orders: `CARD` (default) / `MAIN`. `SELL` orders always settle to `CARD`."""
  clientOrderId: NotRequired[str]
  """Client-supplied order id. Format `^[a-zA-Z0-9-_]{32,36}$`. Auto-generated when omitted."""
  tokenize: NotRequired[bool]
  """Whether to tokenize the purchased stock asset upon settlement. Default `true`. Set to `false` to receive the underlying equity directly instead of a tokenized asset."""


class PlaceOrder(RpcEndpoint):
  """Place a new equity order. Supports all combinations of `LIMIT` / `MARKET` x `BUY` / `SELL`. For `LIMIT BUY` orders the commission fee is automatically computed and reserved by the server at placement time -- callers submit `price` and `quantity` only, no `fee` field is required.

  **Field combination matrix**

  | Side | OrderType | Required | Forbidden |
  | --- | --- | --- | --- |
  | BUY | LIMIT | `price`, `quantity`, `tradingSession` | `notional` |
  | BUY | MARKET | `notional` | `price`, `quantity`, `tradingSession` |
  | SELL | LIMIT | `price`, `quantity`, `tradingSession` | `notional` |
  | SELL | MARKET | `quantity` | `price`, `notional`, `tradingSession` |

  **Fractional shares**: when `quantity` has a decimal component, or an order is placed by `notional`, it is treated as a fractional-share order. A fractional-share `GTC` order must be paired with `tradingSession = EXTENDED` or `24H`.

  Rate limit: 200 requests / min (UID)."""

  async def place_order(
    self,
    *,
    symbol: str,
    quote_asset: str | None = None,
    side: Literal['BUY', 'SELL'],
    order_type: Literal['MARKET', 'LIMIT'],
    price: str | None = None,
    quantity: str | None = None,
    notional: str | None = None,
    time_in_force: Literal['DAY', 'GTC'] | None = None,
    trading_session: Literal['RTH', 'EXTENDED', '24H'] | None = None,
    wallet_type: Literal['CARD', 'MAIN'] | None = None,
    client_order_id: str | None = None,
    tokenize: bool | None = None,
    validate: bool | None = None,
  ) -> EquityOrderAck:
    """Place a new equity order. Supports all combinations of `LIMIT` / `MARKET` x `BUY` / `SELL`. For `LIMIT BUY` orders the commission fee is automatically computed and reserved by the server at placement time -- callers submit `price` and `quantity` only, no `fee` field is required.

    **Field combination matrix**

    | Side | OrderType | Required | Forbidden |
    | --- | --- | --- | --- |
    | BUY | LIMIT | `price`, `quantity`, `tradingSession` | `notional` |
    | BUY | MARKET | `notional` | `price`, `quantity`, `tradingSession` |
    | SELL | LIMIT | `price`, `quantity`, `tradingSession` | `notional` |
    | SELL | MARKET | `quantity` | `price`, `notional`, `tradingSession` |

    **Fractional shares**: when `quantity` has a decimal component, or an order is placed by `notional`, it is treated as a fractional-share order. A fractional-share `GTC` order must be paired with `tradingSession = EXTENDED` or `24H`.

    Rate limit: 200 requests / min (UID).

    Args:
      symbol: US stock ticker, e.g. `AAPL`, `TSLA`. Must be a symbol with tokenization enabled -- check via `/market/tokenized-assets`.
      quote_asset: Quote asset. Defaults to `USDC`; must be within the server's allowed set.
      side: Order side.
      order_type: Order type.
      price: Required for `LIMIT`; forbidden for `MARKET`. Maximum 2 decimal places.
      quantity: Required for `LIMIT` (both sides) and `SELL MARKET`; forbidden for `BUY MARKET`.
      notional: Required for `BUY MARKET`; forbidden for `LIMIT` and `SELL MARKET`.
      time_in_force: `DAY` (default) / `GTC`. `GTC` is only supported for `LIMIT` orders; a fractional-share `GTC` order must be paired with `tradingSession = EXTENDED` or `24H`.
      trading_session: Required for `LIMIT`; forbidden for `MARKET`.
      wallet_type: Payment wallet for `BUY` orders: `CARD` (default) / `MAIN`. `SELL` orders always settle to `CARD`.
      client_order_id: Client-supplied order id. Format `^[a-zA-Z0-9-_]{32,36}$`. Auto-generated when omitted.
      tokenize: Whether to tokenize the purchased stock asset upon settlement. Default `true`. Set to `false` to receive the underlying equity directly instead of a tokenized asset.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/rest-api/trade#place-equity-order)
    """
    request: Request = Request(symbol=symbol, side=side, orderType=order_type)
    if quote_asset is not None:
      request['quoteAsset'] = quote_asset
    if price is not None:
      request['price'] = price
    if quantity is not None:
      request['quantity'] = quantity
    if notional is not None:
      request['notional'] = notional
    if time_in_force is not None:
      request['timeInForce'] = time_in_force
    if trading_session is not None:
      request['tradingSession'] = trading_session
    if wallet_type is not None:
      request['walletType'] = wallet_type
    if client_order_id is not None:
      request['clientOrderId'] = client_order_id
    if tokenize is not None:
      request['tokenize'] = tokenize
    return await self.request(
      request,
      method='POST',
      path='/sapi/v1/equity/order/place',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=EquityOrderAck,
    )
