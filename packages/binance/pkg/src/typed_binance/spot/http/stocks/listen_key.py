# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class EquityListenKey(TypedDict):
  """Listen key identifying a stock user data stream."""

  listenKey: NotRequired[str]
  """Session identifier for the stock user data stream."""


class ListenKey(RpcEndpoint):
  """Create a new stock user-data-stream `listenKey`, or renew an existing one. Calling this endpoint for the same user with an active `listenKey` extends its validity; if there is no active key, a new one is issued. Use the returned `listenKey` to subscribe to the stock user data stream."""

  async def listen_key(
    self,
    *,
    recv_window: int | None = None,
    validate: bool | None = None,
  ) -> EquityListenKey:
    """Create a new stock user-data-stream `listenKey`, or renew an existing one. Calling this endpoint for the same user with an active `listenKey` extends its validity; if there is no active key, a new one is issued. Use the returned `listenKey` to subscribe to the stock user data stream.

    Args:
      recv_window: The value cannot be greater than `60000`.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/rest-api/user-data-streams#create-renew-listen-key)
    """
    params = {}
    if recv_window is not None:
      params['recvWindow'] = recv_window
    _Response = EquityListenKey
    _validator = validator[_Response](_Response)
    return await self.keyed_request(
      'POST',
      '/sapi/v1/equity/listenKey',
      params=params,
      validator=_validator,
      validate=validate,
    )
