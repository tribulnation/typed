# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class ConvertOrderStatus(TypedDict):
  """Status of a Convert order."""

  orderId: NotRequired[int]
  """Order identifier."""
  orderStatus: NotRequired[Literal['PROCESS', 'ACCEPT_SUCCESS', 'SUCCESS', 'FAIL']]
  """Current order status."""
  fromAsset: NotRequired[str]
  """Asset debited."""
  fromAmount: NotRequired[str]
  """Amount debited."""
  toAsset: NotRequired[str]
  """Asset credited."""
  toAmount: NotRequired[str]
  """Amount credited."""
  ratio: NotRequired[str]
  """Conversion ratio, `toAmount` / `fromAmount`."""
  inverseRatio: NotRequired[str]
  """Inverse conversion ratio, `fromAmount` / `toAmount`."""
  createTime: NotRequired[TimestampMillis]
  """Millisecond epoch order creation timestamp."""


class Request(TypedDict):
  """Request parameters."""

  orderId: NotRequired[str]
  """Order identifier. Either this or `quoteId` is required."""
  quoteId: NotRequired[str]
  """Quote identifier the order was accepted from. Either this or `orderId` is required."""


class OrderStatus(RpcEndpoint):
  """Query the status of a Convert order by its `orderId` or the `quoteId` it was accepted from."""

  async def order_status(
    self,
    *,
    order_id: str | None = None,
    quote_id: str | None = None,
    validate: bool | None = None,
  ) -> ConvertOrderStatus:
    """Query the status of a Convert order by its `orderId` or the `quoteId` it was accepted from.

    Args:
      order_id: Order identifier. Either this or `quoteId` is required.
      quote_id: Quote identifier the order was accepted from. Either this or `orderId` is required.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/docs/convert/trade)
    """
    request: Request = Request()
    if order_id is not None:
      request['orderId'] = order_id
    if quote_id is not None:
      request['quoteId'] = quote_id
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/convert/orderStatus',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ConvertOrderStatus,
    )
