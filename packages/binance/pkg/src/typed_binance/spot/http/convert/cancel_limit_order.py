# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class ConvertLimitOrderCanceled(TypedDict):
  """A canceled Convert limit order."""

  orderId: NotRequired[int]
  """Order identifier."""
  status: NotRequired[str]
  """Order status after cancellation."""


class Request(TypedDict):
  """Request parameters."""

  orderId: int
  """Order identifier to cancel."""


class CancelLimitOrder(RpcEndpoint):
  """Cancel an open Convert limit order by its `orderId`."""

  async def cancel_limit_order(
    self,
    order_id: int,
    *,
    validate: bool | None = None,
  ) -> ConvertLimitOrderCanceled:
    """Cancel an open Convert limit order by its `orderId`.

    Args:
      order_id: Order identifier to cancel.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/docs/convert/trade)
    """
    return await self.request(
      Request(orderId=order_id),
      method='POST',
      path='/sapi/v1/convert/limit/cancelOrder',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ConvertLimitOrderCanceled,
    )
