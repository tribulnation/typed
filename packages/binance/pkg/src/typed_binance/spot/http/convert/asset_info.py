# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class ConvertAssetInfo(TypedDict):
  """One asset's order quantity precision."""

  asset: str
  """Asset symbol."""
  fraction: int
  """Number of decimal places the order quantity is precise to."""


ConvertAssetInfos = list[ConvertAssetInfo]


class AssetInfo(RpcEndpoint):
  """Query supported assets' order quantity precision (decimal fraction) for Convert."""

  async def asset_info(self, *, validate: bool | None = None) -> ConvertAssetInfos:
    """Query supported assets' order quantity precision (decimal fraction) for Convert.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/docs/convert/market-data)
    """
    return await self.request(
      None,
      method='GET',
      path='/sapi/v1/convert/assetInfo',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      response_type=ConvertAssetInfos,
    )
