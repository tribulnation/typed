# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class MarginOrderListOrder(TypedDict):
  """One order belonging to the order list, identified minimally."""

  symbol: str
  """Trading pair symbol."""
  orderId: int
  """Order id."""
  clientOrderId: str
  """Client order id, either caller-supplied or venue-generated."""


class MarginOtoOrderReport(TypedDict):
  """One order-list leg's acknowledgement, returned when placing a new margin OTO or OTOCO."""

  symbol: str
  """Trading pair symbol."""
  orderId: int
  """Order id."""
  orderListId: int
  """Id of the order list this order belongs to."""
  clientOrderId: str
  """Client order id, either caller-supplied or venue-generated."""
  transactTime: TimestampMillis
  """Time the order was processed."""
  price: str
  """Order price."""
  origQty: str
  """Original order quantity."""
  executedQty: str
  """Cumulative filled quantity."""
  cummulativeQuoteQty: str
  """Cumulative quote asset transacted quantity."""
  status: Literal[
    'NEW',
    'PENDING_NEW',
    'PARTIALLY_FILLED',
    'FILLED',
    'CANCELED',
    'PENDING_CANCEL',
    'REJECTED',
    'EXPIRED',
    'EXPIRED_IN_MATCH',
  ]
  """Order status. The pending leg(s) of a freshly placed OTO/OTOCO report PENDING_NEW until the working order fills."""
  timeInForce: Literal['GTC', 'IOC', 'FOK']
  """Time in force."""
  type: Literal[
    'LIMIT',
    'MARKET',
    'STOP_LOSS',
    'STOP_LOSS_LIMIT',
    'TAKE_PROFIT',
    'TAKE_PROFIT_LIMIT',
    'LIMIT_MAKER',
  ]
  """Order type."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  stopPrice: NotRequired[str]
  """Price at which the leg's stop triggers. Present only for STOP_LOSS/STOP_LOSS_LIMIT legs."""
  selfTradePreventionMode: Literal[
    'EXPIRE_TAKER', 'EXPIRE_MAKER', 'EXPIRE_BOTH', 'NONE'
  ]
  """Self-trade prevention mode in effect for the order."""


class Request(TypedDict):
  """Request parameters."""

  symbol: str
  """Trading pair symbol, e.g. BNBUSDT."""
  isIsolated: NotRequired[Literal['TRUE', 'FALSE']]
  """Margin account type for this order: isolated when TRUE, crossed (default) when FALSE."""
  sideEffectType: NotRequired[Literal['NO_SIDE_EFFECT', 'MARGIN_BUY']]
  """Margin borrow side effect of this order list. Defaults to NO_SIDE_EFFECT."""
  autoRepayAtCancel: NotRequired[bool]
  """Only relevant when sideEffectType is MARGIN_BUY: whether the debt generated by the order should be repaid automatically once the order is canceled. Defaults to true."""
  listClientOrderId: NotRequired[str]
  """Arbitrary unique id among open order lists. Automatically generated if not sent. A new order list with the same listClientOrderId is accepted only when the previous one is filled or completely expired; distinct from workingClientOrderId, pendingAboveClientOrderId and pendingBelowClientOrderId."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT', 'FULL']]
  """Response shape to return for each leg's orderReports entry."""
  selfTradePreventionMode: NotRequired[
    Literal['EXPIRE_TAKER', 'EXPIRE_MAKER', 'EXPIRE_BOTH', 'NONE']
  ]
  """Self-trade prevention mode. The allowed values depend on the symbol's configuration."""
  workingType: Literal['LIMIT', 'LIMIT_MAKER']
  """Order type of the working order. Initially, only the working order goes on the order book."""
  workingSide: Literal['BUY', 'SELL']
  """Order side of the working order."""
  workingClientOrderId: NotRequired[str]
  """Arbitrary unique id among open orders for the working order. Automatically generated if not sent."""
  workingPrice: float
  """Price of the working order."""
  workingQuantity: float
  """Quantity of the working order."""
  workingIcebergQty: NotRequired[float]
  """Iceberg quantity for the working order. Usable only if workingTimeInForce is GTC."""
  workingTimeInForce: NotRequired[Literal['GTC', 'IOC', 'FOK']]
  """Time in force for the working order."""
  pendingSide: Literal['BUY', 'SELL']
  """Order side shared by the pending above and pending below legs."""
  pendingQuantity: float
  """Quantity shared by the pending above and pending below legs."""
  pendingAboveType: Literal['LIMIT_MAKER', 'STOP_LOSS', 'STOP_LOSS_LIMIT']
  """Order type of the pending above leg."""
  pendingAboveClientOrderId: NotRequired[str]
  """Arbitrary unique id among open orders for the pending above order. Automatically generated if not sent."""
  pendingAbovePrice: NotRequired[float]
  """Limit price of the pending above leg. Usable if pendingAboveType is LIMIT_MAKER or STOP_LOSS_LIMIT."""
  pendingAboveStopPrice: NotRequired[float]
  """Trigger price of the pending above leg. Usable if pendingAboveType is STOP_LOSS or STOP_LOSS_LIMIT."""
  pendingAboveTrailingDelta: NotRequired[float]
  """Trailing delta for the pending above leg."""
  pendingAboveIcebergQty: NotRequired[float]
  """Iceberg quantity for the pending above leg. Usable only if pendingAboveTimeInForce is GTC."""
  pendingAboveTimeInForce: NotRequired[Literal['GTC', 'IOC', 'FOK']]
  """Time in force for the pending above leg."""
  pendingBelowType: NotRequired[Literal['LIMIT_MAKER', 'STOP_LOSS', 'STOP_LOSS_LIMIT']]
  """Order type of the pending below leg."""
  pendingBelowClientOrderId: NotRequired[str]
  """Arbitrary unique id among open orders for the pending below order. Automatically generated if not sent."""
  pendingBelowPrice: NotRequired[float]
  """Limit price of the pending below leg. Usable if pendingBelowType is LIMIT_MAKER or STOP_LOSS_LIMIT."""
  pendingBelowStopPrice: NotRequired[float]
  """Trigger price of the pending below leg. Usable if pendingBelowType is STOP_LOSS or STOP_LOSS_LIMIT."""
  pendingBelowTrailingDelta: NotRequired[float]
  """Trailing delta for the pending below leg."""
  pendingBelowIcebergQty: NotRequired[float]
  """Iceberg quantity for the pending below leg. Usable only if pendingBelowTimeInForce is GTC."""
  pendingBelowTimeInForce: NotRequired[Literal['GTC', 'IOC', 'FOK']]
  """Time in force for the pending below leg."""


class MarginNewOtocoOrderResult(TypedDict):
  """The placed margin OTOCO order list."""

  orderListId: int
  """Id of the order list."""
  contingencyType: Literal['OTOCO']
  """Contingency type of the order list."""
  listStatusType: Literal['RESPONSE', 'EXEC_STARTED', 'UPDATED', 'ALL_DONE']
  """Status of the order list."""
  listOrderStatus: Literal['EXECUTING', 'ALL_DONE', 'REJECT']
  """Execution status of the order list."""
  listClientOrderId: str
  """Client id for the entire order list, either caller-supplied or venue-generated."""
  transactionTime: TimestampMillis
  """Time the order list was processed."""
  symbol: str
  """Trading pair symbol."""
  isIsolated: bool
  """Whether the order list belongs to an isolated margin account."""
  orders: list[MarginOrderListOrder]
  """The working, pending-above and pending-below legs making up the OTOCO, identified minimally."""
  orderReports: list[MarginOtoOrderReport]
  """Per-leg order acknowledgements. The pending legs report PENDING_NEW until the working leg fully fills."""


class NewOtocoOrder(RpcEndpoint):
  """Place a new one-triggers-a-one-cancels-the-other (OTOCO) order list for a margin account, comprised of 3 orders. The first, the working order, must be LIMIT or LIMIT_MAKER; only it goes on the order book initially, exactly like an OTO's working order. Once the working order fully fills, the two pending orders (pending above and pending below) are placed as an OCO pair, following the same rules as the plain OCO order list. An OTOCO adds 3 orders to the unfilled order count and to the EXCHANGE_MAX_NUM_ORDERS/MAX_NUM_ORDERS filters."""

  async def new_otoco_order(
    self,
    *,
    symbol: str,
    is_isolated: Literal['TRUE', 'FALSE'] | None = None,
    side_effect_type: Literal['NO_SIDE_EFFECT', 'MARGIN_BUY'] | None = None,
    auto_repay_at_cancel: bool | None = None,
    list_client_order_id: str | None = None,
    new_order_resp_type: Literal['ACK', 'RESULT', 'FULL'] | None = None,
    self_trade_prevention_mode: Literal[
      'EXPIRE_TAKER', 'EXPIRE_MAKER', 'EXPIRE_BOTH', 'NONE'
    ]
    | None = None,
    working_type: Literal['LIMIT', 'LIMIT_MAKER'],
    working_side: Literal['BUY', 'SELL'],
    working_client_order_id: str | None = None,
    working_price: float,
    working_quantity: float,
    working_iceberg_qty: float | None = None,
    working_time_in_force: Literal['GTC', 'IOC', 'FOK'] | None = None,
    pending_side: Literal['BUY', 'SELL'],
    pending_quantity: float,
    pending_above_type: Literal['LIMIT_MAKER', 'STOP_LOSS', 'STOP_LOSS_LIMIT'],
    pending_above_client_order_id: str | None = None,
    pending_above_price: float | None = None,
    pending_above_stop_price: float | None = None,
    pending_above_trailing_delta: float | None = None,
    pending_above_iceberg_qty: float | None = None,
    pending_above_time_in_force: Literal['GTC', 'IOC', 'FOK'] | None = None,
    pending_below_type: Literal['LIMIT_MAKER', 'STOP_LOSS', 'STOP_LOSS_LIMIT']
    | None = None,
    pending_below_client_order_id: str | None = None,
    pending_below_price: float | None = None,
    pending_below_stop_price: float | None = None,
    pending_below_trailing_delta: float | None = None,
    pending_below_iceberg_qty: float | None = None,
    pending_below_time_in_force: Literal['GTC', 'IOC', 'FOK'] | None = None,
    validate: bool | None = None,
  ) -> MarginNewOtocoOrderResult:
    """Place a new one-triggers-a-one-cancels-the-other (OTOCO) order list for a margin account, comprised of 3 orders. The first, the working order, must be LIMIT or LIMIT_MAKER; only it goes on the order book initially, exactly like an OTO's working order. Once the working order fully fills, the two pending orders (pending above and pending below) are placed as an OCO pair, following the same rules as the plain OCO order list. An OTOCO adds 3 orders to the unfilled order count and to the EXCHANGE_MAX_NUM_ORDERS/MAX_NUM_ORDERS filters.

    Args:
      symbol: Trading pair symbol, e.g. BNBUSDT.
      is_isolated: Margin account type for this order: isolated when TRUE, crossed (default) when FALSE.
      side_effect_type: Margin borrow side effect of this order list. Defaults to NO_SIDE_EFFECT.
      auto_repay_at_cancel: Only relevant when sideEffectType is MARGIN_BUY: whether the debt generated by the order should be repaid automatically once the order is canceled. Defaults to true.
      list_client_order_id: Arbitrary unique id among open order lists. Automatically generated if not sent. A new order list with the same listClientOrderId is accepted only when the previous one is filled or completely expired; distinct from workingClientOrderId, pendingAboveClientOrderId and pendingBelowClientOrderId.
      new_order_resp_type: Response shape to return for each leg's orderReports entry.
      self_trade_prevention_mode: Self-trade prevention mode. The allowed values depend on the symbol's configuration.
      working_type: Order type of the working order. Initially, only the working order goes on the order book.
      working_side: Order side of the working order.
      working_client_order_id: Arbitrary unique id among open orders for the working order. Automatically generated if not sent.
      working_price: Price of the working order.
      working_quantity: Quantity of the working order.
      working_iceberg_qty: Iceberg quantity for the working order. Usable only if workingTimeInForce is GTC.
      working_time_in_force: Time in force for the working order.
      pending_side: Order side shared by the pending above and pending below legs.
      pending_quantity: Quantity shared by the pending above and pending below legs.
      pending_above_type: Order type of the pending above leg.
      pending_above_client_order_id: Arbitrary unique id among open orders for the pending above order. Automatically generated if not sent.
      pending_above_price: Limit price of the pending above leg. Usable if pendingAboveType is LIMIT_MAKER or STOP_LOSS_LIMIT.
      pending_above_stop_price: Trigger price of the pending above leg. Usable if pendingAboveType is STOP_LOSS or STOP_LOSS_LIMIT.
      pending_above_trailing_delta: Trailing delta for the pending above leg.
      pending_above_iceberg_qty: Iceberg quantity for the pending above leg. Usable only if pendingAboveTimeInForce is GTC.
      pending_above_time_in_force: Time in force for the pending above leg.
      pending_below_type: Order type of the pending below leg.
      pending_below_client_order_id: Arbitrary unique id among open orders for the pending below order. Automatically generated if not sent.
      pending_below_price: Limit price of the pending below leg. Usable if pendingBelowType is LIMIT_MAKER or STOP_LOSS_LIMIT.
      pending_below_stop_price: Trigger price of the pending below leg. Usable if pendingBelowType is STOP_LOSS or STOP_LOSS_LIMIT.
      pending_below_trailing_delta: Trailing delta for the pending below leg.
      pending_below_iceberg_qty: Iceberg quantity for the pending below leg. Usable only if pendingBelowTimeInForce is GTC.
      pending_below_time_in_force: Time in force for the pending below leg.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/trade#margin-account-new-otoco)
    """
    request: Request = Request(
      symbol=symbol,
      workingType=working_type,
      workingSide=working_side,
      workingPrice=working_price,
      workingQuantity=working_quantity,
      pendingSide=pending_side,
      pendingQuantity=pending_quantity,
      pendingAboveType=pending_above_type,
    )
    if is_isolated is not None:
      request['isIsolated'] = is_isolated
    if side_effect_type is not None:
      request['sideEffectType'] = side_effect_type
    if auto_repay_at_cancel is not None:
      request['autoRepayAtCancel'] = auto_repay_at_cancel
    if list_client_order_id is not None:
      request['listClientOrderId'] = list_client_order_id
    if new_order_resp_type is not None:
      request['newOrderRespType'] = new_order_resp_type
    if self_trade_prevention_mode is not None:
      request['selfTradePreventionMode'] = self_trade_prevention_mode
    if working_client_order_id is not None:
      request['workingClientOrderId'] = working_client_order_id
    if working_iceberg_qty is not None:
      request['workingIcebergQty'] = working_iceberg_qty
    if working_time_in_force is not None:
      request['workingTimeInForce'] = working_time_in_force
    if pending_above_client_order_id is not None:
      request['pendingAboveClientOrderId'] = pending_above_client_order_id
    if pending_above_price is not None:
      request['pendingAbovePrice'] = pending_above_price
    if pending_above_stop_price is not None:
      request['pendingAboveStopPrice'] = pending_above_stop_price
    if pending_above_trailing_delta is not None:
      request['pendingAboveTrailingDelta'] = pending_above_trailing_delta
    if pending_above_iceberg_qty is not None:
      request['pendingAboveIcebergQty'] = pending_above_iceberg_qty
    if pending_above_time_in_force is not None:
      request['pendingAboveTimeInForce'] = pending_above_time_in_force
    if pending_below_type is not None:
      request['pendingBelowType'] = pending_below_type
    if pending_below_client_order_id is not None:
      request['pendingBelowClientOrderId'] = pending_below_client_order_id
    if pending_below_price is not None:
      request['pendingBelowPrice'] = pending_below_price
    if pending_below_stop_price is not None:
      request['pendingBelowStopPrice'] = pending_below_stop_price
    if pending_below_trailing_delta is not None:
      request['pendingBelowTrailingDelta'] = pending_below_trailing_delta
    if pending_below_iceberg_qty is not None:
      request['pendingBelowIcebergQty'] = pending_below_iceberg_qty
    if pending_below_time_in_force is not None:
      request['pendingBelowTimeInForce'] = pending_below_time_in_force
    return await self.request(
      request,
      method='POST',
      path='/sapi/v1/margin/order/otoco',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=MarginNewOtocoOrderResult,
    )
