# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class ListenKeyKeepalive(RpcEndpoint):
  """Keep a cross margin user data stream alive, extending its validity by another 60 minutes. Streams close automatically after 60 minutes without a keepalive."""

  async def listen_key_keepalive(
    self,
    *,
    listen_key: str | None = None,
    validate: bool | None = None,
  ) -> dict[str, Any]:
    """Keep a cross margin user data stream alive, extending its validity by another 60 minutes. Streams close automatically after 60 minutes without a keepalive.

    Args:
      listen_key: Listen key returned by `listen_key_create`, identifying the cross margin stream to extend. Not a credential — it is the stream identifier, and the account has at most one active cross margin listen key at a time.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/user-data-stream#keepalive-user-data-stream)
    """
    params = {}
    if listen_key is not None:
      params['listenKey'] = listen_key
    _Response = dict[str, Any]
    _validator = validator[_Response](_Response)
    return await self.keyed_request(
      'PUT',
      '/sapi/v1/margin/listen-key',
      params=params,
      validator=_validator,
      validate=validate,
    )
