# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class MarginOrder(TypedDict):
  """A margin account order."""

  symbol: str
  """Trading pair symbol."""
  orderId: int
  """Order id."""
  clientOrderId: str
  """Client order id, either caller-supplied or venue-generated."""
  price: str
  """Order price. "0.00000000" for MARKET orders that were not given a price."""
  origQty: str
  """Original order quantity."""
  executedQty: str
  """Cumulative filled quantity."""
  cummulativeQuoteQty: str
  """Cumulative quote asset transacted quantity."""
  status: Literal[
    'NEW',
    'PENDING_NEW',
    'PARTIALLY_FILLED',
    'FILLED',
    'CANCELED',
    'PENDING_CANCEL',
    'REJECTED',
    'EXPIRED',
    'EXPIRED_IN_MATCH',
  ]
  """Order status."""
  timeInForce: Literal['GTC', 'IOC', 'FOK']
  """Time in force."""
  type: Literal[
    'LIMIT',
    'MARKET',
    'STOP_LOSS',
    'STOP_LOSS_LIMIT',
    'TAKE_PROFIT',
    'TAKE_PROFIT_LIMIT',
    'LIMIT_MAKER',
  ]
  """Order type."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  stopPrice: str
  """Price at which the algorithmic order triggers. "0.00000000" for orders without a stop price."""
  icebergQty: str
  """Quantity for the iceberg order. "0.00000000" when the order is not an iceberg order."""
  time: TimestampMillis
  """Time the order was created."""
  updateTime: TimestampMillis
  """Time the order was last updated."""
  isWorking: bool
  """Whether the order is working on the order book."""
  isIsolated: bool
  """Whether the order belongs to an isolated margin account."""
  selfTradePreventionMode: Literal[
    'EXPIRE_TAKER', 'EXPIRE_MAKER', 'EXPIRE_BOTH', 'NONE'
  ]
  """Self-trade prevention mode in effect for the order."""


class Request(TypedDict):
  """Request parameters."""

  symbol: NotRequired[str]
  """Trading pair symbol. Returns open orders for all symbols if omitted. Required when isIsolated is TRUE."""
  isIsolated: NotRequired[Literal['TRUE', 'FALSE']]
  """Margin account type for this order: isolated when TRUE, crossed (default) when FALSE."""


Response = list[MarginOrder]


class OpenOrders(RpcEndpoint):
  """Query all open orders for a margin account. If symbol is not sent, open orders for all symbols are returned, and the request is weighted by the number of symbols currently trading on the exchange. isIsolated=TRUE requires symbol to be sent."""

  async def open_orders(
    self,
    symbol: str | None = None,
    *,
    is_isolated: Literal['TRUE', 'FALSE'] | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Query all open orders for a margin account. If symbol is not sent, open orders for all symbols are returned, and the request is weighted by the number of symbols currently trading on the exchange. isIsolated=TRUE requires symbol to be sent.

    Args:
      symbol: Trading pair symbol. Returns open orders for all symbols if omitted. Required when isIsolated is TRUE.
      is_isolated: Margin account type for this order: isolated when TRUE, crossed (default) when FALSE.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/trade#query-margin-accounts-open-orders)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    if is_isolated is not None:
      request['isIsolated'] = is_isolated
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/margin/openOrders',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
