# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class MarginTrade(TypedDict):
  """A single executed margin trade."""

  commission: Decimal
  """Commission charged on this trade, as a decimal string."""
  commissionAsset: str
  """Asset the commission was charged in."""
  id: int
  """Trade ID."""
  isBestMatch: bool
  """Whether this trade was the best price match available at execution."""
  isBuyer: bool
  """Whether this account was the buyer in the trade."""
  isMaker: bool
  """Whether this account was the maker in the trade."""
  orderId: int
  """ID of the order this trade filled."""
  price: Decimal
  """Trade price, as a decimal string."""
  qty: Decimal
  """Trade quantity, as a decimal string."""
  symbol: str
  """Symbol this trade was executed on."""
  isIsolated: bool
  """Whether this trade was executed on an isolated margin account."""
  time: TimestampMillis
  """Millisecond timestamp the trade was executed."""


class Request(TypedDict):
  """Request parameters."""

  symbol: str
  """Trading symbol, e.g. BNBUSDT."""
  isIsolated: NotRequired[Literal['TRUE', 'FALSE']]
  """Whether to query the isolated margin account for `symbol`. Defaults to cross margin when omitted."""
  startTime: NotRequired[TimestampMillis]
  """Query period start, as milliseconds since epoch."""
  endTime: NotRequired[TimestampMillis]
  """Query period end, as milliseconds since epoch."""
  fromId: NotRequired[int]
  """Trade ID to start from (inclusive) — trades with an ID greater than or equal to this value are returned. If omitted, the most recent trades are returned."""
  limit: NotRequired[int]
  """Maximum number of trades to return."""


Response = list[MarginTrade]


class MyTrades(RpcEndpoint):
  """Query this margin account's executed trades for a symbol. If `fromId` is set, trades with an ID greater than or equal to it are returned; otherwise the most recent trades are returned."""

  async def my_trades(
    self,
    symbol: str,
    *,
    is_isolated: Literal['TRUE', 'FALSE'] | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    from_id: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Query this margin account's executed trades for a symbol. If `fromId` is set, trades with an ID greater than or equal to it are returned; otherwise the most recent trades are returned.

    Args:
      symbol: Trading symbol, e.g. BNBUSDT.
      is_isolated: Whether to query the isolated margin account for `symbol`. Defaults to cross margin when omitted.
      start_time: Query period start, as milliseconds since epoch.
      end_time: Query period end, as milliseconds since epoch.
      from_id: Trade ID to start from (inclusive) — trades with an ID greater than or equal to this value are returned. If omitted, the most recent trades are returned.
      limit: Maximum number of trades to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/trade#query-margin-accounts-trade-list)
    """
    request: Request = Request(symbol=symbol)
    if is_isolated is not None:
      request['isIsolated'] = is_isolated
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if from_id is not None:
      request['fromId'] = from_id
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/margin/myTrades',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
