# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class MarginListenKey(TypedDict):
  """Listen key identifying a cross margin user data stream."""

  listenKey: str
  """Listen key used to connect to the cross margin user data WebSocket stream."""


class ListenKeyCreate(RpcEndpoint):
  """Start a new cross margin user data stream and return a listen key used to connect to it over WebSocket. The stream closes after 60 minutes unless kept alive with `listen_key_keepalive`; if the account already has an active cross margin listen key, that key is returned and its validity extended."""

  async def listen_key_create(self, *, validate: bool | None = None) -> MarginListenKey:
    """Start a new cross margin user data stream and return a listen key used to connect to it over WebSocket. The stream closes after 60 minutes unless kept alive with `listen_key_keepalive`; if the account already has an active cross margin listen key, that key is returned and its validity extended.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/user-data-stream#start-user-data-stream)
    """
    return await self.request(
      None,
      method='POST',
      path='/sapi/v1/margin/listen-key',
      meta={'security': 'USER_STREAM', 'signed': False},
      validate=validate,
      response_type=MarginListenKey,
    )
