# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Request parameters."""

  symbol: NotRequired[str]
  """Isolated margin trading pair, e.g. BTCUSDT. Omit to list cross margin special keys; set to list an isolated margin symbol's special keys instead."""


class SpecialKeyListEntry(TypedDict):
  """One special key."""

  apiKey: str
  """API key."""
  ip: str
  """Whitelisted IP addresses for this key, comma-separated. `0.0.0.0` is Binance's initial placeholder state before any IP has been set, not a real trusted address."""
  apiName: str
  """Name of the special key."""
  permissionMode: Literal['TRADE', 'READ']
  """Permission mode of the special key, applicable to Ed25519 keys (see spot.margin.special_key_create's `permissionMode`)."""


SpecialKeyListResponse = list[SpecialKeyListEntry]


class SpecialKeyList(RpcEndpoint):
  """List this account's special (low-latency trading) API keys. Omit `symbol` to list cross margin special keys, or set it to list an isolated margin symbol's special keys."""

  async def special_key_list(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> SpecialKeyListResponse:
    """List this account's special (low-latency trading) API keys. Omit `symbol` to list cross margin special keys, or set it to list an isolated margin symbol's special keys.

    Args:
      symbol: Isolated margin trading pair, e.g. BTCUSDT. Omit to list cross margin special keys; set to list an isolated margin symbol's special keys instead.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/trade#query-special-key-list)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/margin/api-key-list',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SpecialKeyListResponse,
    )
