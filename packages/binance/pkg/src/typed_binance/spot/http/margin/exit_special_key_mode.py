# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any
from typed_binance.core.endpoint.rpc import RpcEndpoint


SpecialKeyModeExitResponse = dict[str, Any]


class ExitSpecialKeyMode(RpcEndpoint):
  """Exit special key (low-latency trading) mode for this account, reverting it out of the VIP7+ low-latency API key program."""

  async def exit_special_key_mode(
    self,
    *,
    validate: bool | None = None,
  ) -> SpecialKeyModeExitResponse:
    """Exit special key (low-latency trading) mode for this account, reverting it out of the VIP7+ low-latency API key program.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/trade#exit-special-key-mode)
    """
    return await self.request(
      None,
      method='POST',
      path='/sapi/v1/margin/exit-special-key-mode',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      response_type=SpecialKeyModeExitResponse,
    )
