# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class MarginCapitalFlow(TypedDict):
  """One margin capital flow record."""

  id: int
  """Capital flow record ID."""
  tranId: int
  """Transaction ID this record is associated with."""
  timestamp: TimestampMillis
  """Millisecond epoch timestamp this record occurred."""
  asset: str
  """Asset this record affected."""
  symbol: str
  """Isolated margin symbol this record belongs to, or the empty margin-account context for cross margin."""
  type: Literal[
    'TRANSFER',
    'BORROW',
    'REPAY',
    'BUY_INCOME',
    'BUY_EXPENSE',
    'SELL_INCOME',
    'SELL_EXPENSE',
    'TRADING_COMMISSION',
    'BUY_LIQUIDATION',
    'SELL_LIQUIDATION',
    'REPAY_LIQUIDATION',
    'OTHER_LIQUIDATION',
    'LIQUIDATION_FEE',
    'SMALL_BALANCE_CONVERT',
    'COMMISSION_RETURN',
    'SMALL_CONVERT',
  ]
  """Capital flow event type."""
  amount: str
  """Signed amount of this capital flow event, in `asset`."""


class Request(TypedDict):
  """Request parameters."""

  asset: NotRequired[str]
  """Asset to filter by, e.g. BNB. Omit to return all assets."""
  symbol: NotRequired[str]
  """Isolated margin symbol to query. Required when querying isolated margin capital flow; omit for cross margin."""
  type: NotRequired[
    Literal[
      'TRANSFER',
      'BORROW',
      'REPAY',
      'BUY_INCOME',
      'BUY_EXPENSE',
      'SELL_INCOME',
      'SELL_EXPENSE',
      'TRADING_COMMISSION',
      'BUY_LIQUIDATION',
      'SELL_LIQUIDATION',
      'REPAY_LIQUIDATION',
      'OTHER_LIQUIDATION',
      'LIQUIDATION_FEE',
      'SMALL_BALANCE_CONVERT',
      'COMMISSION_RETURN',
      'SMALL_CONVERT',
    ]
  ]
  """Capital flow event type to filter by."""
  startTime: NotRequired[TimestampMillis]
  """Query period start, as milliseconds since epoch. Only the last 90 days of data are queryable."""
  endTime: NotRequired[TimestampMillis]
  """Query period end, as milliseconds since epoch."""
  fromId: NotRequired[int]
  """Capital flow record ID to page from. If set, records with an ID greater than this value are returned; otherwise the most recent records are returned."""
  limit: NotRequired[int]
  """Maximum number of records to return."""


Response = list[MarginCapitalFlow]


class CapitalFlow(RpcEndpoint):
  """Query this account's cross or isolated margin capital flow -- transfers, borrows, repays, trade proceeds/costs, liquidation events, and related asset movements."""

  async def capital_flow(
    self,
    *,
    asset: str | None = None,
    symbol: str | None = None,
    type: Literal[
      'TRANSFER',
      'BORROW',
      'REPAY',
      'BUY_INCOME',
      'BUY_EXPENSE',
      'SELL_INCOME',
      'SELL_EXPENSE',
      'TRADING_COMMISSION',
      'BUY_LIQUIDATION',
      'SELL_LIQUIDATION',
      'REPAY_LIQUIDATION',
      'OTHER_LIQUIDATION',
      'LIQUIDATION_FEE',
      'SMALL_BALANCE_CONVERT',
      'COMMISSION_RETURN',
      'SMALL_CONVERT',
    ]
    | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    from_id: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Query this account's cross or isolated margin capital flow -- transfers, borrows, repays, trade proceeds/costs, liquidation events, and related asset movements.

    Args:
      asset: Asset to filter by, e.g. BNB. Omit to return all assets.
      symbol: Isolated margin symbol to query. Required when querying isolated margin capital flow; omit for cross margin.
      type: Capital flow event type to filter by.
      start_time: Query period start, as milliseconds since epoch. Only the last 90 days of data are queryable.
      end_time: Query period end, as milliseconds since epoch.
      from_id: Capital flow record ID to page from. If set, records with an ID greater than this value are returned; otherwise the most recent records are returned.
      limit: Maximum number of records to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/account#query-cross-isolated-margin-capital-flow)
    """
    request: Request = Request()
    if asset is not None:
      request['asset'] = asset
    if symbol is not None:
      request['symbol'] = symbol
    if type is not None:
      request['type'] = type
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if from_id is not None:
      request['fromId'] = from_id
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/margin/capital-flow',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
