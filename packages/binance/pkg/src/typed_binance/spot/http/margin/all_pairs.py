# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class MarginPair(TypedDict):
  """One cross margin trading pair."""

  base: str
  """Base asset of the pair."""
  id: int
  """Pair identifier."""
  isBuyAllowed: bool
  """Whether buying is currently allowed on margin for this pair."""
  isMarginTrade: bool
  """Whether margin trading is enabled for this pair."""
  isSellAllowed: bool
  """Whether selling is currently allowed on margin for this pair."""
  quote: str
  """Quote asset of the pair."""
  symbol: str
  """Trading pair symbol, e.g. BNBBTC."""


class Request(TypedDict):
  """Request parameters."""

  symbol: NotRequired[str]
  """Trading pair to filter by, e.g. BNBBTC. Omit to return all cross margin pairs."""


Response = list[MarginPair]


class AllPairs(RpcEndpoint):
  """Query all cross margin trading pairs, or a single pair when `symbol` is given, including buy/sell permissions and whether margin trading is enabled for the pair."""

  async def all_pairs(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Query all cross margin trading pairs, or a single pair when `symbol` is given, including buy/sell permissions and whether margin trading is enabled for the pair.

    Args:
      symbol: Trading pair to filter by, e.g. BNBBTC. Omit to return all cross margin pairs.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/market-data#get-all-cross-margin-pairs)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/margin/allPairs',
      meta={'security': 'MARKET_DATA', 'signed': False},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
