# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class QuerySpecialKeyResponse(TypedDict):
  """The special key's details."""

  apiKey: NotRequired[str]
  """API key."""
  ip: NotRequired[str]
  """Whitelisted IP addresses for this key, comma-separated. `0.0.0.0` is Binance's initial placeholder state before any IP has been set, not a real trusted address."""
  apiName: NotRequired[str]
  """Name of the special key."""
  permissionMode: NotRequired[Literal['TRADE', 'READ']]
  """Permission mode of the special key, applicable to Ed25519 keys (see spot.margin.special_key_create's `permissionMode`)."""


class Request(TypedDict):
  """Request parameters."""

  apiKey: str
  """The special (low-latency trading) API key to query. Added from live evidence -- the docs-only pass (SDK-sourced) omitted this parameter entirely and declared `symbol` optional-only; Binance instead returned BadRequest(400, -1102, "Mandatory parameter 'apiKey' was not sent") when called with just `symbol`. See spec/status.md."""
  symbol: NotRequired[str]
  """Isolated margin trading pair, e.g. BTCUSDT. Omit to query the cross margin special key; set to query an isolated margin symbol's special key instead."""


class SpecialKey(RpcEndpoint):
  """Query this account's special (low-latency trading) API key. Omit `symbol` to query the cross margin special key, or set it to query an isolated margin symbol's special key."""

  async def special_key(
    self,
    *,
    api_key: str,
    symbol: str | None = None,
    validate: bool | None = None,
  ) -> QuerySpecialKeyResponse:
    """Query this account's special (low-latency trading) API key. Omit `symbol` to query the cross margin special key, or set it to query an isolated margin symbol's special key.

    Args:
      api_key: The special (low-latency trading) API key to query. Added from live evidence -- the docs-only pass (SDK-sourced) omitted this parameter entirely and declared `symbol` optional-only; Binance instead returned BadRequest(400, -1102, "Mandatory parameter 'apiKey' was not sent") when called with just `symbol`. See spec/status.md.
      symbol: Isolated margin trading pair, e.g. BTCUSDT. Omit to query the cross margin special key; set to query an isolated margin symbol's special key instead.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/trade#query-special-key)
    """
    request: Request = Request(apiKey=api_key)
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/margin/apiKey',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=QuerySpecialKeyResponse,
    )
