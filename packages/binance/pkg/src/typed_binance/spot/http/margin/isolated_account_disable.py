# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class IsolatedMarginAccountStatus(TypedDict):
  """Result of enabling or disabling an isolated margin account."""

  success: bool
  """Whether the operation succeeded."""
  symbol: str
  """Isolated margin trading symbol the operation applied to."""


class IsolatedAccountDisable(RpcEndpoint):
  """Disable an isolated margin account for a specific symbol. Each trading pair can only be disabled once every 24 hours."""

  async def isolated_account_disable(
    self,
    *,
    symbol: str,
    validate: bool | None = None,
  ) -> IsolatedMarginAccountStatus:
    """Disable an isolated margin account for a specific symbol. Each trading pair can only be disabled once every 24 hours.

    Args:
      symbol: Isolated margin trading symbol to disable, e.g. BNBUSDT.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/account#disable-isolated-margin-account)
    """
    params: dict = {
      'symbol': symbol,
    }
    _Response = IsolatedMarginAccountStatus
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'DELETE',
      '/sapi/v1/margin/isolated/account',
      params=params,
      validator=_validator,
      validate=validate,
    )
