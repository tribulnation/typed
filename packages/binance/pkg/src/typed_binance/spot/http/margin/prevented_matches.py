# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class MarginPreventedMatch(TypedDict):
  """One margin order expired due to self-trade prevention."""

  symbol: str
  """Symbol the taker order was placed on."""
  preventedMatchId: int
  """Prevented match ID."""
  takerOrderId: int
  """ID of the order that would have taken this match."""
  makerSymbol: str
  """Symbol the maker order was placed on."""
  makerOrderId: int
  """ID of the order that would have made this match."""
  tradeGroupId: int
  """Trade group ID shared by taker and maker under the applicable self-trade prevention mode."""
  selfTradePreventionMode: Literal[
    'NONE', 'EXPIRE_MAKER', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'DECREMENT', 'TRANSFER'
  ]
  """Self-trade prevention mode that caused this match to be prevented."""
  price: str
  """Price the match would have occurred at, as a decimal string."""
  makerPreventedQuantity: str
  """Maker quantity prevented from matching, as a decimal string."""
  transactTime: TimestampMillis
  """Millisecond timestamp this prevented match was recorded."""


class Request(TypedDict):
  """Request parameters."""

  symbol: str
  """Symbol to query."""
  isIsolated: NotRequired[Literal['TRUE', 'FALSE']]
  """Whether to query the isolated margin account for `symbol`. Defaults to cross margin when omitted."""
  preventedMatchId: NotRequired[int]
  """Prevented match ID to query. See the operation description for supported parameter combinations."""
  orderId: NotRequired[int]
  """Order ID to query prevented matches for. See the operation description for supported parameter combinations."""
  fromPreventedMatchId: NotRequired[int]
  """Prevented match ID to start from (inclusive), used together with `orderId`. See the operation description for supported parameter combinations."""


Response = list[MarginPreventedMatch]


class PreventedMatches(RpcEndpoint):
  """Query this margin account's orders that were expired due to self-trade prevention (STP). Supported parameter combinations: `symbol` + `preventedMatchId`; `symbol` + `orderId`; `symbol` + `orderId` + `fromPreventedMatchId` (`limit` defaults to 500); `symbol` + `orderId` + `fromPreventedMatchId` + `limit`."""

  async def prevented_matches(
    self,
    symbol: str,
    *,
    is_isolated: Literal['TRUE', 'FALSE'] | None = None,
    prevented_match_id: int | None = None,
    order_id: int | None = None,
    from_prevented_match_id: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Query this margin account's orders that were expired due to self-trade prevention (STP). Supported parameter combinations: `symbol` + `preventedMatchId`; `symbol` + `orderId`; `symbol` + `orderId` + `fromPreventedMatchId` (`limit` defaults to 500); `symbol` + `orderId` + `fromPreventedMatchId` + `limit`.

    Args:
      symbol: Symbol to query.
      is_isolated: Whether to query the isolated margin account for `symbol`. Defaults to cross margin when omitted.
      prevented_match_id: Prevented match ID to query. See the operation description for supported parameter combinations.
      order_id: Order ID to query prevented matches for. See the operation description for supported parameter combinations.
      from_prevented_match_id: Prevented match ID to start from (inclusive), used together with `orderId`. See the operation description for supported parameter combinations.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/trade#query-prevented-matches)
    """
    request: Request = Request(symbol=symbol)
    if is_isolated is not None:
      request['isIsolated'] = is_isolated
    if prevented_match_id is not None:
      request['preventedMatchId'] = prevented_match_id
    if order_id is not None:
      request['orderId'] = order_id
    if from_prevented_match_id is not None:
      request['fromPreventedMatchId'] = from_prevented_match_id
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/margin/myPreventedMatches',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
