# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class MarginAvailableInventory(TypedDict):
  """Available borrow inventory for a margin account type."""

  assets: dict[str, str]
  """Available inventory per asset, as decimal strings keyed by asset symbol."""
  updateTime: TimestampMillis
  """Millisecond epoch this inventory snapshot was last updated."""


class Request(TypedDict):
  """Request parameters."""

  type: Literal['MARGIN', 'ISOLATED']
  """Margin account type to query available inventory for."""


class AvailableInventory(RpcEndpoint):
  """Query the amount of each asset currently available to borrow for the given margin account type."""

  async def available_inventory(
    self,
    type: Literal['MARGIN', 'ISOLATED'],
    *,
    validate: bool | None = None,
  ) -> MarginAvailableInventory:
    """Query the amount of each asset currently available to borrow for the given margin account type.

    Args:
      type: Margin account type to query available inventory for.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/market-data#query-margin-available-inventory)
    """
    return await self.request(
      Request(type=type),
      method='GET',
      path='/sapi/v1/margin/available-inventory',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=MarginAvailableInventory,
    )
