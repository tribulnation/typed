# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class ListenKeyClose(RpcEndpoint):
  """Close the account's cross margin user data stream."""

  async def listen_key_close(
    self,
    *,
    listen_key: str,
    validate: bool | None = None,
  ) -> dict[str, Any]:
    """Close the account's cross margin user data stream.

    Args:
      listen_key: Listen key to close, returned by `listen_key_create`.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api/rest-api/user-data-stream#close-user-data-stream)
    """
    params: dict = {
      'listenKey': listen_key,
    }
    _Response = dict[str, Any]
    _validator = validator[_Response](_Response)
    return await self.keyed_request(
      'DELETE',
      '/sapi/v1/margin/listen-key',
      params=params,
      validator=_validator,
      validate=validate,
    )
