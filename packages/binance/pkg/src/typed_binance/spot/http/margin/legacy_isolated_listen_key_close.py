# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


Ack = dict[str, Any]


class Request(TypedDict):
  """Request parameters."""

  symbol: str
  """Isolated margin trading pair symbol whose user data stream to close, e.g. `BTCUSDT`."""
  listenKey: str
  """Listen key returned by `legacy_isolated_listen_key_create`, identifying the isolated margin stream to close."""


class LegacyIsolatedListenKeyClose(RpcEndpoint):
  """Close an isolated margin user data stream (legacy path)."""

  async def legacy_isolated_listen_key_close(
    self,
    *,
    symbol: str,
    listen_key: str,
    validate: bool | None = None,
  ) -> Ack:
    """Close an isolated margin user data stream (legacy path).

    Args:
      symbol: Isolated margin trading pair symbol whose user data stream to close, e.g. `BTCUSDT`.
      listen_key: Listen key returned by `legacy_isolated_listen_key_create`, identifying the isolated margin stream to close.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/margin-api.md)
    """
    return await self.request(
      Request(symbol=symbol, listenKey=listen_key),
      method='DELETE',
      path='/sapi/v1/userDataStream/isolated',
      meta={'security': 'USER_STREAM', 'signed': False},
      validate=validate,
      request_type=Request,
      response_type=Ack,
    )
