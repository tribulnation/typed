# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Request parameters."""

  email: NotRequired[str]
  """Managed sub-account email."""


class SubAccountTradeInfo(TypedDict):
  """One day's trading totals for one sub-account."""

  userId: int
  """Sub-account user id."""
  btc: Decimal
  """BTC-valued spot trading volume for the day. Corrected to `string` from live evidence (Binance's usual decimal-as-string convention, e.g. `"0"`), not `number` as first specced."""
  btcFutures: Decimal
  """BTC-valued futures trading volume for the day. Corrected to `string` from live evidence (Binance's usual decimal-as-string convention, e.g. `"0"`), not `number` as first specced."""
  btcMargin: Decimal
  """BTC-valued margin trading volume for the day. Corrected to `string` from live evidence (Binance's usual decimal-as-string convention, e.g. `"0"`), not `number` as first specced."""
  busd: Decimal
  """BUSD-valued spot trading volume for the day. Corrected to `string` from live evidence (Binance's usual decimal-as-string convention, e.g. `"0"`), not `number` as first specced."""
  busdFutures: Decimal
  """BUSD-valued futures trading volume for the day. Corrected to `string` from live evidence (Binance's usual decimal-as-string convention, e.g. `"0"`), not `number` as first specced."""
  busdMargin: Decimal
  """BUSD-valued margin trading volume for the day. Corrected to `string` from live evidence (Binance's usual decimal-as-string convention, e.g. `"0"`), not `number` as first specced."""
  date: TimestampMillis
  """Date of the totals."""


class SubAccountTransactionStatistics(TypedDict):
  """Trailing-30-day trading totals."""

  recent30BtcTotal: Decimal
  """BTC-valued total trading volume over the last 30 days."""
  recent30BtcFuturesTotal: Decimal
  """BTC-valued futures trading volume over the last 30 days."""
  recent30BtcMarginTotal: Decimal
  """BTC-valued margin trading volume over the last 30 days."""
  recent30BusdTotal: Decimal
  """BUSD-valued total trading volume over the last 30 days."""
  recent30BusdFuturesTotal: Decimal
  """BUSD-valued futures trading volume over the last 30 days."""
  recent30BusdMarginTotal: Decimal
  """BUSD-valued margin trading volume over the last 30 days."""
  tradeInfoVos: list[SubAccountTradeInfo]
  """Per-day breakdown of the trailing-30-day totals."""


class TransactionStatistics(RpcEndpoint):
  """Query a sub-account's trailing-30-day transaction statistics, for the master account."""

  async def transaction_statistics(
    self,
    email: str | None = None,
    *,
    validate: bool | None = None,
  ) -> SubAccountTransactionStatistics:
    """Query a sub-account's trailing-30-day transaction statistics, for the master account.

    Args:
      email: Managed sub-account email.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-sub-account/api/rest-api/account-management#query-sub-account-transaction-statistics)
    """
    request: Request = Request()
    if email is not None:
      request['email'] = email
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/sub-account/transaction-statistics',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SubAccountTransactionStatistics,
    )
