# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Request parameters."""

  email: NotRequired[str]
  """Sub-account email. Omit to return every sub-account's status."""


class SubAccountMarginFuturesStatus(TypedDict):
  """Margin/Futures enablement status of one sub-account."""

  email: str
  """Sub-account email."""
  isSubUserEnabled: bool
  """Whether the sub-account is enabled."""
  isUserActive: bool
  """Whether the sub-account is active."""
  insertTime: TimestampMillis
  """Time the sub-account was created."""
  isMarginEnabled: bool
  """Whether Margin trading is enabled for the sub-account."""
  isFutureEnabled: bool
  """Whether Futures trading is enabled for the sub-account."""
  mobile: str
  """Bound mobile number, or an empty string if none is bound. Corrected from live evidence: the docs-only pass guessed `integer` (`0` if unbound), but this account's real sub-account response had a bare empty string."""


Response = list[SubAccountMarginFuturesStatus]


class Status(RpcEndpoint):
  """Get one or all sub-accounts' Margin/Futures enablement status, for the master account."""

  async def status(
    self,
    email: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Get one or all sub-accounts' Margin/Futures enablement status, for the master account.

    Args:
      email: Sub-account email. Omit to return every sub-account's status.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-sub-account/api/rest-api/account-management#get-sub-accounts-status-on-margin-or-futures)
    """
    request: Request = Request()
    if email is not None:
      request['email'] = email
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/sub-account/status',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
