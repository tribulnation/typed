# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class SubAccountApiKeyPermissions(TypedDict):
  """The API key's updated permissions."""

  apiName: NotRequired[str]
  """API key name."""
  apikey: NotRequired[str]
  """API key."""
  canTrade: NotRequired[bool]
  """Spot & Margin trading permission."""
  canMarginLoanRepay: NotRequired[bool]
  """Margin borrow/repay permission."""
  canFuturesTrade: NotRequired[bool]
  """Futures trading permission."""
  canUniversalTransfer: NotRequired[bool]
  """Universal transfer permission."""
  canVanillaOptions: NotRequired[bool]
  """Vanilla options (EAPI) trading permission."""
  timestamp: NotRequired[TimestampMillis]
  """Time the permissions were last updated."""


class ModifyApiKeyPermission(RpcEndpoint):
  """Modify the trading permissions of a sub-account API key, for the master account."""

  async def __call__(
    self,
    *,
    email: str,
    sub_account_api_key: str,
    can_trade: bool | None = None,
    can_margin_loan_repay: bool | None = None,
    can_futures_trade: bool | None = None,
    can_universal_transfer: bool | None = None,
    can_vanilla_options: bool | None = None,
    validate: bool | None = None,
  ) -> SubAccountApiKeyPermissions:
    """Modify the trading permissions of a sub-account API key, for the master account.

    Args:
      email: Sub-account email.
      sub_account_api_key: Sub-account API key.
      can_trade: Spot & Margin trading permission.
      can_margin_loan_repay: Margin borrow/repay permission.
      can_futures_trade: Futures trading permission.
      can_universal_transfer: Universal transfer permission.
      can_vanilla_options: Vanilla options (EAPI) trading permission.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-sub-account/api/rest-api/api-management#modify-sub-account-api-key-permission)
    """
    params: dict = {
      'email': email,
      'subAccountApiKey': sub_account_api_key,
    }
    if can_trade is not None:
      params['canTrade'] = can_trade
    if can_margin_loan_repay is not None:
      params['canMarginLoanRepay'] = can_margin_loan_repay
    if can_futures_trade is not None:
      params['canFuturesTrade'] = can_futures_trade
    if can_universal_transfer is not None:
      params['canUniversalTransfer'] = can_universal_transfer
    if can_vanilla_options is not None:
      params['canVanillaOptions'] = can_vanilla_options
    _Response = SubAccountApiKeyPermissions
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/sub-account/subAccountApiPermission',
      params=params,
      validator=_validator,
      validate=validate,
    )
