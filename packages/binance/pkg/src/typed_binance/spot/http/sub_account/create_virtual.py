# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Request parameters."""

  subAccountString: str
  """Arbitrary string used to derive the virtual sub-account's generated email address."""


class VirtualSubAccount(TypedDict):
  """The created virtual sub-account."""

  email: NotRequired[str]
  """Generated email address of the new virtual sub-account."""


class CreateVirtual(RpcEndpoint):
  """Create a virtual sub-account under the master account. Binance generates a placeholder email for the sub-account from the supplied string; no real mailbox is required or contacted."""

  async def create_virtual(
    self,
    sub_account_string: str,
    *,
    validate: bool | None = None,
  ) -> VirtualSubAccount:
    """Create a virtual sub-account under the master account. Binance generates a placeholder email for the sub-account from the supplied string; no real mailbox is required or contacted.

    Args:
      sub_account_string: Arbitrary string used to derive the virtual sub-account's generated email address.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-sub-account/api/rest-api/account-management#create-avirtual-sub-account)
    """
    return await self.request(
      Request(subAccountString=sub_account_string),
      method='POST',
      path='/sapi/v1/sub-account/virtualSubAccount',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=VirtualSubAccount,
    )
