# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Request parameters."""

  email: NotRequired[str]
  """Filter to one sub-account's email."""
  isFreeze: NotRequired[str]
  """Filter by freeze status: `"true"` or `"false"`."""
  page: NotRequired[int]
  """Page number."""
  limit: NotRequired[int]
  """Page size."""


class SubAccountListItem(TypedDict):
  """One sub-account under the master account."""

  subUserId: int
  """Sub-account user id."""
  email: str
  """Sub-account email."""
  remark: str
  """Free-text remark set on the sub-account."""
  isFreeze: bool
  """Whether the sub-account is frozen."""
  createTime: TimestampMillis
  """Time the sub-account was created."""
  isManagedSubAccount: bool
  """Whether this is a Managed Sub-Account."""
  isAssetManagementSubAccount: bool
  """Whether this is an Asset Management (custody) sub-account."""


class SubAccountList(TypedDict):
  """The matching sub-accounts."""

  subAccounts: list[SubAccountListItem]
  """Sub-accounts on this page."""


class List(RpcEndpoint):
  """Query the master account's sub-account list."""

  async def list_paged(
    self,
    *,
    email: str | None = None,
    is_freeze: str | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[SubAccountList]:
    """Query the master account's sub-account list.

    Paged variant of `list`: Requests `page` from 1 upwards and stops on the first page shorter than `limit`, or after `max_pages` pages when one is given.

    Args:
      email: Filter to one sub-account's email.
      is_freeze: Filter by freeze status: `"true"` or `"false"`.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-sub-account/api/rest-api/account-management#query-sub-account-list)
    """
    page = 1
    pages = 0
    while True:
      response = await self.list(
        email=email, is_freeze=is_freeze, limit=limit, page=page, validate=validate
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      rows = response.get('subAccounts') if response is not None else None
      if not rows or (limit is not None and len(rows) < limit):
        break
      page += 1

  async def list(
    self,
    *,
    email: str | None = None,
    is_freeze: str | None = None,
    page: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> SubAccountList:
    """Query the master account's sub-account list.

    Args:
      email: Filter to one sub-account's email.
      is_freeze: Filter by freeze status: `"true"` or `"false"`.
      page: Page number.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-sub-account/api/rest-api/account-management#query-sub-account-list)
    """
    request: Request = Request()
    if email is not None:
      request['email'] = email
    if is_freeze is not None:
      request['isFreeze'] = is_freeze
    if page is not None:
      request['page'] = page
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/sub-account/list',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SubAccountList,
    )
