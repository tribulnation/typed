# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Request parameters."""

  email: str
  """Sub-account email."""
  includeSource: NotRequired[bool]
  """Whether to include the `sourceAddress` field in each record. Defaults to `false`."""
  coin: NotRequired[str]
  """Filter by coin."""
  status: NotRequired[Literal[0, 1, 6, 7, 8]]
  """Deposit status: `0` = pending, `1` = success, `6` = credited but cannot withdraw, `7` = wrong deposit, `8` = waiting user confirmation."""
  startTime: NotRequired[TimestampMillis]
  """Start of the query window."""
  endTime: NotRequired[TimestampMillis]
  """End of the query window."""
  limit: NotRequired[int]
  """Page size."""
  offset: NotRequired[int]
  """Number of records to skip."""
  txId: NotRequired[str]
  """Filter by transaction id."""


class SubAccountDeposit(TypedDict):
  """One deposit into the sub-account."""

  id: str
  """Deposit record id."""
  amount: str
  """Deposit amount."""
  coin: str
  """Coin deposited."""
  network: str
  """Network the deposit arrived on."""
  status: Literal[0, 1, 6, 7, 8]
  """Deposit status: `0` = pending, `1` = success, `6` = credited but cannot withdraw, `7` = wrong deposit, `8` = waiting user confirmation."""
  address: str
  """Deposit address credited."""
  addressTag: NotRequired[str]
  """Address tag/memo, when the network uses one."""
  txId: str
  """On-chain transaction id."""
  insertTime: TimestampMillis
  """Time the deposit was recorded."""
  transferType: int
  """Whether this was an on-chain deposit or an internal transfer."""
  confirmTimes: str
  """Confirmations received, as `"current/required"`."""
  unlockConfirm: int
  """Confirmations required before the deposit unlocks for use."""
  walletType: int
  """Wallet the deposit was credited to."""


Response = list[SubAccountDeposit]


class DepositHistory(RpcEndpoint):
  """Get a sub-account's deposit history, for the master account."""

  async def deposit_history(
    self,
    *,
    email: str,
    include_source: bool | None = None,
    coin: str | None = None,
    status: Literal[0, 1, 6, 7, 8] | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    offset: int | None = None,
    tx_id: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get a sub-account's deposit history, for the master account.

    Args:
      email: Sub-account email.
      include_source: Whether to include the `sourceAddress` field in each record. Defaults to `false`.
      coin: Filter by coin.
      status: Deposit status: `0` = pending, `1` = success, `6` = credited but cannot withdraw, `7` = wrong deposit, `8` = waiting user confirmation.
      start_time: Start of the query window.
      end_time: End of the query window.
      limit: Page size.
      offset: Number of records to skip.
      tx_id: Filter by transaction id.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-sub-account/api/rest-api/asset-management#get-sub-account-deposit-history)
    """
    request: Request = Request(email=email)
    if include_source is not None:
      request['includeSource'] = include_source
    if coin is not None:
      request['coin'] = coin
    if status is not None:
      request['status'] = status
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    if offset is not None:
      request['offset'] = offset
    if tx_id is not None:
      request['txId'] = tx_id
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/capital/deposit/subHisrec',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
