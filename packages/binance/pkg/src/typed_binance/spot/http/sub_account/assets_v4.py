# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Request parameters."""

  email: str
  """Sub-account email."""


class SubAccountAssetBalanceV4(TypedDict):
  """One asset's spot balance."""

  freeze: NotRequired[str]
  """Amount frozen (e.g. pending a distribution)."""
  withdrawing: NotRequired[str]
  """Amount currently being withdrawn."""
  asset: NotRequired[str]
  """Asset symbol."""
  free: NotRequired[str]
  """Amount available."""
  locked: NotRequired[str]
  """Amount locked in open orders."""


class SubAccountAssetsV4(TypedDict):
  """The sub-account's spot asset balances."""

  balances: list[SubAccountAssetBalanceV4]
  """Per-asset balances."""


class AssetsV4(RpcEndpoint):
  """Get a sub-account's spot asset balances, for the master account, with balances returned as strings."""

  async def assets_v4(
    self,
    email: str,
    *,
    validate: bool | None = None,
  ) -> SubAccountAssetsV4:
    """Get a sub-account's spot asset balances, for the master account, with balances returned as strings.

    Args:
      email: Sub-account email.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-sub-account/api/rest-api/asset-management#query-sub-account-assets-asset-management)
    """
    return await self.request(
      Request(email=email),
      method='GET',
      path='/sapi/v4/sub-account/assets',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SubAccountAssetsV4,
    )
