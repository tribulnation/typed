# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Request parameters."""

  fromEmail: NotRequired[str]
  """Filter to transfers sent from this email. Cannot be sent together with `toEmail`."""
  toEmail: NotRequired[str]
  """Filter to transfers sent to this email. Cannot be sent together with `fromEmail`."""
  clientTranId: NotRequired[str]
  """Filter by caller-supplied transfer id."""
  startTime: NotRequired[TimestampMillis]
  """Start of the query window."""
  endTime: NotRequired[TimestampMillis]
  """End of the query window."""
  page: NotRequired[int]
  """Page number."""
  limit: NotRequired[int]
  """Page size."""


class UniversalTransferRecord(TypedDict):
  """One universal transfer."""

  tranId: NotRequired[int]
  """Transfer id."""
  fromEmail: NotRequired[str]
  """Email of the sending account."""
  toEmail: NotRequired[str]
  """Email of the receiving account."""
  asset: NotRequired[str]
  """Asset transferred."""
  amount: NotRequired[str]
  """Amount transferred."""
  createTimeStamp: NotRequired[int]
  """Time the transfer was created."""
  fromAccountType: NotRequired[
    Literal['SPOT', 'USDT_FUTURE', 'COIN_FUTURE', 'MARGIN', 'ISOLATED_MARGIN']
  ]
  """Account type transferred from."""
  toAccountType: NotRequired[
    Literal['SPOT', 'USDT_FUTURE', 'COIN_FUTURE', 'MARGIN', 'ISOLATED_MARGIN']
  ]
  """Account type transferred to."""
  status: NotRequired[str]
  """Transfer status."""
  clientTranId: NotRequired[str]
  """Caller-supplied transfer id, if one was given."""


class UniversalTransferHistory(TypedDict):
  """Matching universal transfer records."""

  result: list[UniversalTransferRecord]
  """Transfer records on this page."""
  totalCount: int
  """Total number of matching transfer records."""


class UniversalTransferHistoryEndpoint(RpcEndpoint):
  """Query universal transfer history, for the master account."""

  def universal_transfer_history_paged(
    self,
    *,
    from_email: str | None = None,
    to_email: str | None = None,
    client_tran_id: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[UniversalTransferRecord, int]:
    """Query universal transfer history, for the master account.

    Paged variant of `universal_transfer_history`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      from_email: Filter to transfers sent from this email. Cannot be sent together with `toEmail`.
      to_email: Filter to transfers sent to this email. Cannot be sent together with `fromEmail`.
      client_tran_id: Filter by caller-supplied transfer id.
      start_time: Start of the query window.
      end_time: End of the query window.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-sub-account/api/rest-api/asset-management#query-universal-transfer-history)
    """

    async def next(page: int) -> tuple[list[UniversalTransferRecord], int | None]:
      response = await self.universal_transfer_history(
        from_email=from_email,
        to_email=to_email,
        client_tran_id=client_tran_id,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        page=page,
        validate=validate,
      )
      rows = response.get('result')
      rows = rows if rows is not None else []
      total = response.get('totalCount')
      total = int(total) if total is not None else None
      if total is None or limit is None or (page - 1 + 1) * limit >= total:
        return rows, None
      return rows, page + 1

    return PaginatedResponse(1, next)

  async def universal_transfer_history(
    self,
    *,
    from_email: str | None = None,
    to_email: str | None = None,
    client_tran_id: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    page: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> UniversalTransferHistory:
    """Query universal transfer history, for the master account.

    Args:
      from_email: Filter to transfers sent from this email. Cannot be sent together with `toEmail`.
      to_email: Filter to transfers sent to this email. Cannot be sent together with `fromEmail`.
      client_tran_id: Filter by caller-supplied transfer id.
      start_time: Start of the query window.
      end_time: End of the query window.
      page: Page number.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-sub-account/api/rest-api/asset-management#query-universal-transfer-history)
    """
    request: Request = Request()
    if from_email is not None:
      request['fromEmail'] = from_email
    if to_email is not None:
      request['toEmail'] = to_email
    if client_tran_id is not None:
      request['clientTranId'] = client_tran_id
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if page is not None:
      request['page'] = page
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/sub-account/universalTransfer',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=UniversalTransferHistory,
    )
