# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Request parameters."""

  fromEmail: NotRequired[str]
  """Filter to transfers sent from this email. Cannot be sent together with `toEmail`."""
  toEmail: NotRequired[str]
  """Filter to transfers sent to this email. Cannot be sent together with `fromEmail`."""
  startTime: NotRequired[TimestampMillis]
  """Start of the query window."""
  endTime: NotRequired[TimestampMillis]
  """End of the query window."""
  page: NotRequired[int]
  """Page number."""
  limit: NotRequired[int]
  """Page size. The upstream docs state a default of 500 and a maximum of 200 for this parameter, which is internally contradictory; left unresolved pending a live call (spec-authoring rule 7)."""


SubAccountSpotTransferKeywords = TypedDict(
  'SubAccountSpotTransferKeywords', {'from': str}
)
"""
- `from`: Email of the sending account.
"""


class SubAccountSpotTransfer(SubAccountSpotTransferKeywords):
  """One spot asset transfer between sub-accounts."""

  to: str
  """Email of the receiving account."""
  asset: str
  """Asset transferred."""
  qty: str
  """Amount transferred."""
  status: str
  """Transfer status."""
  tranId: int
  """Transfer id."""
  time: TimestampMillis
  """Time the transfer was executed."""


Response = list[SubAccountSpotTransfer]


class SpotTransferHistory(RpcEndpoint):
  """Query the history of spot asset transfers between sub-accounts (or between a sub-account and the master account), for the master account."""

  async def spot_transfer_history_paged(
    self,
    *,
    from_email: str | None = None,
    to_email: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[Response]:
    """Query the history of spot asset transfers between sub-accounts (or between a sub-account and the master account), for the master account.

    Paged variant of `spot_transfer_history`: Requests `page` from 1 upwards and stops on the first page shorter than `limit`, or after `max_pages` pages when one is given.

    Args:
      from_email: Filter to transfers sent from this email. Cannot be sent together with `toEmail`.
      to_email: Filter to transfers sent to this email. Cannot be sent together with `fromEmail`.
      start_time: Start of the query window.
      end_time: End of the query window.
      limit: Page size. The upstream docs state a default of 500 and a maximum of 200 for this parameter, which is internally contradictory; left unresolved pending a live call (spec-authoring rule 7).
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-sub-account/api/rest-api/asset-management#query-sub-account-spot-asset-transfer-history)
    """
    page = 1
    pages = 0
    while True:
      response = await self.spot_transfer_history(
        from_email=from_email,
        to_email=to_email,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        page=page,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      if not response or (limit is not None and len(response) < limit):
        break
      page += 1

  async def spot_transfer_history(
    self,
    *,
    from_email: str | None = None,
    to_email: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    page: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Query the history of spot asset transfers between sub-accounts (or between a sub-account and the master account), for the master account.

    Args:
      from_email: Filter to transfers sent from this email. Cannot be sent together with `toEmail`.
      to_email: Filter to transfers sent to this email. Cannot be sent together with `fromEmail`.
      start_time: Start of the query window.
      end_time: End of the query window.
      page: Page number.
      limit: Page size. The upstream docs state a default of 500 and a maximum of 200 for this parameter, which is internally contradictory; left unresolved pending a live call (spec-authoring rule 7).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-sub-account/api/rest-api/asset-management#query-sub-account-spot-asset-transfer-history)
    """
    request: Request = Request()
    if from_email is not None:
      request['fromEmail'] = from_email
    if to_email is not None:
      request['toEmail'] = to_email
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if page is not None:
      request['page'] = page
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/sub-account/sub/transfer/history',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
