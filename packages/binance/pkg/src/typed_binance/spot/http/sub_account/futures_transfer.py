# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class FuturesTransferResult(TypedDict):
  """The executed transfer."""

  txnId: NotRequired[str]
  """Transaction id of the transfer."""


class Request(TypedDict):
  """Request parameters."""

  email: str
  """Sub-account email."""
  asset: str
  """Asset to transfer."""
  amount: float
  """Amount to transfer."""
  type: Literal[1, 2, 3, 4]
  """Transfer direction: `1` = spot to USD-M futures, `2` = USD-M futures to spot, `3` = spot to COIN-M futures, `4` = COIN-M futures to spot."""


class FuturesTransfer(RpcEndpoint):
  """Transfer an asset between a sub-account's Spot and Futures wallets, for the master account."""

  async def futures_transfer(
    self,
    *,
    email: str,
    asset: str,
    amount: float,
    type: Literal[1, 2, 3, 4],
    validate: bool | None = None,
  ) -> FuturesTransferResult:
    """Transfer an asset between a sub-account's Spot and Futures wallets, for the master account.

    Args:
      email: Sub-account email.
      asset: Asset to transfer.
      amount: Amount to transfer.
      type: Transfer direction: `1` = spot to USD-M futures, `2` = USD-M futures to spot, `3` = spot to COIN-M futures, `4` = COIN-M futures to spot.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-sub-account/api/rest-api/asset-management#futures-transfer-for-sub-account)
    """
    return await self.request(
      Request(email=email, asset=asset, amount=amount, type=type),
      method='POST',
      path='/sapi/v1/sub-account/futures/transfer',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FuturesTransferResult,
    )
