# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Request parameters."""

  subEmail: NotRequired[str]
  """Email of the collateral or credit sub-account to transfer from. Required when called by the parent account (error `-27026` if omitted); when called by a credit or collateral account, omitted defaults to the caller's own email, and a wrong value errors `-3003`."""
  asset: str
  """Asset to transfer, for example `USDT` or `USDC`."""
  amount: float
  """Amount to transfer. The amount actually transferred is `min(risk unit max transfer-out amount, collateral account max transfer-out amount)`."""


class TransferOut(RpcEndpoint):
  """Transfer assets from a risk unit's Margin collateral or credit account to a Spot account sharing the same Spot UID, when the risk unit's LTV thresholds and/or the relevant product's transfer-out rules are met. Callable by each credit/collateral account with its own API key, or by the parent account. To move funds further, out of the risk unit entirely, first transfer to the Spot wallet with this endpoint, then use the Universal Transfer API to move assets out. All other transfer scenarios -- credit-to-collateral transfers within the risk unit (no LTV restriction, parent account API key only) and Spot-collateral-to-outside-account transfers -- go through the Universal Transfer API instead."""

  async def transfer_out(
    self,
    *,
    sub_email: str | None = None,
    asset: str,
    amount: float,
    validate: bool | None = None,
  ):
    """Transfer assets from a risk unit's Margin collateral or credit account to a Spot account sharing the same Spot UID, when the risk unit's LTV thresholds and/or the relevant product's transfer-out rules are met. Callable by each credit/collateral account with its own API key, or by the parent account. To move funds further, out of the risk unit entirely, first transfer to the Spot wallet with this endpoint, then use the Universal Transfer API to move assets out. All other transfer scenarios -- credit-to-collateral transfers within the risk unit (no LTV restriction, parent account API key only) and Spot-collateral-to-outside-account transfers -- go through the Universal Transfer API instead.

    Args:
      sub_email: Email of the collateral or credit sub-account to transfer from. Required when called by the parent account (error `-27026` if omitted); when called by a credit or collateral account, omitted defaults to the caller's own email, and a wrong value errors `-3003`.
      asset: Asset to transfer, for example `USDT` or `USDC`.
      amount: Amount to transfer. The amount actually transferred is `min(risk unit max transfer-out amount, collateral account max transfer-out amount)`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-institutional-loan/api/rest-api/transfer#risk-unit-transfer)
    """
    request: Request = Request(asset=asset, amount=amount)
    if sub_email is not None:
      request['subEmail'] = sub_email
    return await self.request(
      request,
      method='POST',
      path='/sapi/v1/margin/loan-group/transfer-out',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      request_type=Request,
    )
