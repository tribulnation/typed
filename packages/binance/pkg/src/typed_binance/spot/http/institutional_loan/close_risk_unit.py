# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class ClosedRiskUnit(TypedDict):
  """The closed risk unit."""

  groupId: NotRequired[int]
  """Risk unit unique identifier."""
  status: NotRequired[Literal['CLOSED']]
  """Closure result. `CLOSED` indicates the risk unit was closed successfully."""


class Request(TypedDict):
  """Request parameters."""

  groupId: int
  """Risk unit unique identifier."""


class CloseRiskUnit(RpcEndpoint):
  """Close an institutional loan risk unit, unlinking all its collateral accounts. Callable only with the credit account API key. The risk unit must be active and its outstanding institutional loan liabilities must already be repaid."""

  async def close_risk_unit(
    self,
    group_id: int,
    *,
    validate: bool | None = None,
  ) -> ClosedRiskUnit:
    """Close an institutional loan risk unit, unlinking all its collateral accounts. Callable only with the credit account API key. The risk unit must be active and its outstanding institutional loan liabilities must already be repaid.

    Args:
      group_id: Risk unit unique identifier.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-institutional-loan/api/rest-api/account#close-risk-unit)
    """
    return await self.request(
      Request(groupId=group_id),
      method='DELETE',
      path='/sapi/v1/margin/loan-group',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ClosedRiskUnit,
    )
