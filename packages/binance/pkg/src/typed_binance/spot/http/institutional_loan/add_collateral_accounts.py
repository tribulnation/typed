# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class AddCollateralAccountResult(TypedDict):
  """Result of adding the collateral account."""

  status: NotRequired[int]
  """Result status code. `0` indicates success."""


class AddCollateralAccounts(RpcEndpoint):
  """Add a new collateral account to an institutional loan risk unit. Callable only with the parent account API key. For a credit account, `enableMargin` must be `true`. A collateral sub-account can be added but not removed through this endpoint -- `enableSpot`/`enableMargin` may only be switched from `false` to `true`, never back; contact the Binance Margin team to remove a credit or collateral sub-account from the risk unit."""

  async def add_collateral_accounts(
    self,
    *,
    group_id: int,
    sub_email: str,
    enable_spot: bool,
    enable_margin: bool,
    validate: bool | None = None,
  ) -> AddCollateralAccountResult:
    """Add a new collateral account to an institutional loan risk unit. Callable only with the parent account API key. For a credit account, `enableMargin` must be `true`. A collateral sub-account can be added but not removed through this endpoint -- `enableSpot`/`enableMargin` may only be switched from `false` to `true`, never back; contact the Binance Margin team to remove a credit or collateral sub-account from the risk unit.

    Args:
      group_id: Risk unit unique identifier.
      sub_email: Email of the sub-account to add as a collateral account.
      enable_spot: Include the sub-account's Spot wallet as collateral. Can only be switched from `false` to `true`, never back.
      enable_margin: Include the sub-account's Margin wallet as collateral. Must be `true` for a credit account. Can only be switched from `false` to `true`, never back.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-institutional-loan/api/rest-api/account#add-collateral-accounts)
    """
    params: dict = {
      'groupId': group_id,
      'subEmail': sub_email,
      'enableSpot': enable_spot,
      'enableMargin': enable_margin,
    }
    _Response = AddCollateralAccountResult
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/margin/loan-group/edit-member',
      params=params,
      validator=_validator,
      validate=validate,
    )
