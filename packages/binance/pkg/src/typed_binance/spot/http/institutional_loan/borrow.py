# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Request parameters."""

  groupId: int
  """Risk unit unique identifier."""
  assetName: str
  """Asset to borrow, for example `USDT`, `USDC`, `BTC`, or `U`."""
  amount: float
  """Amount of the asset to borrow."""


class RiskUnitBorrowResult(TypedDict):
  """The submitted risk unit borrow."""

  transactionId: NotRequired[int]
  """Transaction unique identifier."""
  amount: NotRequired[float]
  """Amount of the asset borrowed."""
  status: NotRequired[str]
  """Borrow submission status."""


class Borrow(RpcEndpoint):
  """Submit a borrow request for a risk unit. Callable only with the credit account API key, which must have the `Enable Spot & Margin Trading` permission. `amount` is the quantity of the borrowed asset; the maximum allowable amount is subject to a collateral ratio that depends on the credit account's margin mode."""

  async def borrow(
    self,
    group_id: int,
    *,
    asset_name: str,
    amount: float,
    validate: bool | None = None,
  ) -> RiskUnitBorrowResult:
    """Submit a borrow request for a risk unit. Callable only with the credit account API key, which must have the `Enable Spot & Margin Trading` permission. `amount` is the quantity of the borrowed asset; the maximum allowable amount is subject to a collateral ratio that depends on the credit account's margin mode.

    Args:
      group_id: Risk unit unique identifier.
      asset_name: Asset to borrow, for example `USDT`, `USDC`, `BTC`, or `U`.
      amount: Amount of the asset to borrow.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-institutional-loan/api/rest-api/borrow-repay#risk-unit-borrow)
    """
    return await self.request(
      Request(groupId=group_id, assetName=asset_name, amount=amount),
      method='POST',
      path='/sapi/v1/margin/loan-group/borrow',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=RiskUnitBorrowResult,
    )
