# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class PmProBankruptcyLoanRepayHistoryRow(TypedDict):
  """One repay record."""

  asset: NotRequired[str]
  """Asset name."""
  amount: NotRequired[str]
  """Repay amount."""
  repayTime: NotRequired[TimestampMillis]
  """Repay time."""


class Request(TypedDict):
  """Request parameters."""

  startTime: NotRequired[TimestampMillis]
  """Start time."""
  endTime: NotRequired[TimestampMillis]
  """End time."""
  size: NotRequired[int]
  """Number of results returned."""
  current: NotRequired[int]
  """Currently querying page. Start from 1."""


class PmProBankruptcyLoanRepayHistory(TypedDict):
  """Bankruptcy loan repay history."""

  total: NotRequired[int]
  """Total number of records."""
  rows: NotRequired[list[PmProBankruptcyLoanRepayHistoryRow]]
  """Repay history rows."""


class BankruptcyLoanRepayHistory(RpcEndpoint):
  """Query repay history of pmloan for portfolio margin pro."""

  def bankruptcy_loan_repay_history_paged(
    self,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    size: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[PmProBankruptcyLoanRepayHistoryRow, int]:
    """Query repay history of pmloan for portfolio margin pro.

    Paged variant of `bankruptcy_loan_repay_history`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      start_time: Start time.
      end_time: End time.
      size: Number of results returned.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-derivatives-trading-portfolio-margin-pro/api/rest-api/account#query-portfolio-margin-pro-bankruptcy-loan-repay-history)
    """

    async def next(
      current: int,
    ) -> tuple[list[PmProBankruptcyLoanRepayHistoryRow], int | None]:
      response = await self.bankruptcy_loan_repay_history(
        start_time=start_time,
        end_time=end_time,
        size=size,
        current=current,
        validate=validate,
      )
      rows = response.get('rows')
      rows = rows if rows is not None else []
      total = response.get('total')
      total = int(total) if total is not None else None
      if total is None or size is None or (current - 1 + 1) * size >= total:
        return rows, None
      return rows, current + 1

    return PaginatedResponse(1, next)

  async def bankruptcy_loan_repay_history(
    self,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    size: int | None = None,
    current: int | None = None,
    validate: bool | None = None,
  ) -> PmProBankruptcyLoanRepayHistory:
    """Query repay history of pmloan for portfolio margin pro.

    Args:
      start_time: Start time.
      end_time: End time.
      size: Number of results returned.
      current: Currently querying page. Start from 1.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-derivatives-trading-portfolio-margin-pro/api/rest-api/account#query-portfolio-margin-pro-bankruptcy-loan-repay-history)
    """
    request: Request = Request()
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if size is not None:
      request['size'] = size
    if current is not None:
      request['current'] = current
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/portfolio/pmloan-history',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PmProBankruptcyLoanRepayHistory,
    )
