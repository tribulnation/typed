# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class PmProBnbTransfer(TypedDict):
  """BNB transfer."""

  tranId: NotRequired[int]
  """Transaction ID."""


class Request(TypedDict):
  """Request parameters."""

  amount: float
  """Transfer amount."""
  transferSide: Literal['TO_UM', 'FROM_UM']
  """Transfer direction."""


class BnbTransfer(RpcEndpoint):
  """BNB transfer can be between Margin Account and USDM Account."""

  async def bnb_transfer(
    self,
    amount: float,
    *,
    transfer_side: Literal['TO_UM', 'FROM_UM'],
    validate: bool | None = None,
  ) -> PmProBnbTransfer:
    """BNB transfer can be between Margin Account and USDM Account.

    Args:
      amount: Transfer amount.
      transfer_side: Transfer direction.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-derivatives-trading-portfolio-margin-pro/api/rest-api/account#bnb-transfer)
    """
    return await self.request(
      Request(amount=amount, transferSide=transfer_side),
      method='POST',
      path='/sapi/v1/portfolio/bnb-transfer',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PmProBnbTransfer,
    )
