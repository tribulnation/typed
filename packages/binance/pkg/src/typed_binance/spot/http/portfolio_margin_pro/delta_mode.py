# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class PmProDeltaModeStatus(TypedDict):
  """Delta mode status."""

  deltaEnabled: NotRequired[bool]
  """Whether Delta mode is enabled."""


class DeltaMode(RpcEndpoint):
  """Query the Delta mode status of current account."""

  async def delta_mode(self, *, validate: bool | None = None) -> PmProDeltaModeStatus:
    """Query the Delta mode status of current account.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-derivatives-trading-portfolio-margin-pro/api/rest-api/account#get-delta-mode-status)
    """
    return await self.request(
      None,
      method='GET',
      path='/sapi/v1/portfolio/delta-mode',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      response_type=PmProDeltaModeStatus,
    )
