# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class BrokerSubAccountBnbBurnStatus(TypedDict):
  """This sub-account's BNB Burn settings."""

  subAccountId: int
  """Sub-account identifier."""
  spotBNBBurn: bool
  """Whether spot/margin trading fees are paid in BNB."""
  interestBNBBurn: bool
  """Whether margin loan interest is paid in BNB."""


class Request(TypedDict):
  """Request parameters."""

  subAccountId: str
  """Sub-account identifier."""


class BnbBurnStatus(RpcEndpoint):
  """Get a sub-account's BNB Burn settings."""

  async def bnb_burn_status(
    self,
    sub_account_id: str,
    *,
    validate: bool | None = None,
  ) -> BrokerSubAccountBnbBurnStatus:
    """Get a sub-account's BNB Burn settings.

    Args:
      sub_account_id: Sub-account identifier.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://binance-docs.github.io/Brokerage-API/Brokerage_Operation_Endpoints/)
    """
    return await self.request(
      Request(subAccountId=sub_account_id),
      method='GET',
      path='/sapi/v1/broker/subAccount/bnbBurn/status',
      meta={'security': 'SIGNED', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerSubAccountBnbBurnStatus,
    )
