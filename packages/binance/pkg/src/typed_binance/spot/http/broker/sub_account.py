# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class BrokerSubAccount(TypedDict):
  """One sub-account."""

  subaccountId: str
  """Sub-account identifier."""
  email: str
  """Sub-account email."""
  tag: str
  """Tag supplied at creation, if any."""
  makerCommission: float
  """Spot maker commission rate for this sub-account."""
  takerCommission: float
  """Spot taker commission rate for this sub-account."""
  marginMakerCommission: float
  """Margin maker commission rate override, or `-1` if unset."""
  marginTakerCommission: float
  """Margin taker commission rate override, or `-1` if unset."""
  createTime: TimestampMillis
  """Time this sub-account was created."""


class Request(TypedDict):
  """Request parameters."""

  subAccountId: NotRequired[str]
  """Sub-account identifier. All sub-accounts if omitted."""
  page: NotRequired[int]
  """Page number, 1-indexed. Default 1."""
  size: NotRequired[int]
  """Records per page. Default 500, max 500."""


BrokerSubAccountList = list[BrokerSubAccount]


class SubAccount(RpcEndpoint):
  """List sub-accounts under this broker account."""

  async def sub_account_paged(
    self,
    sub_account_id: str | None = None,
    *,
    size: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[BrokerSubAccountList]:
    """List sub-accounts under this broker account.

    Paged variant of `sub_account`: Requests `page` from 1 upwards and stops on the first page shorter than `size`, or after `max_pages` pages when one is given.

    Args:
      sub_account_id: Sub-account identifier. All sub-accounts if omitted.
      size: Records per page. Default 500, max 500.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://binance-docs.github.io/Brokerage-API/Brokerage_Operation_Endpoints/)
    """
    page = 1
    pages = 0
    while True:
      response = await self.sub_account(
        sub_account_id, size=size, page=page, validate=validate
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      if not response or (size is not None and len(response) < size):
        break
      page += 1

  async def sub_account(
    self,
    sub_account_id: str | None = None,
    *,
    page: int | None = None,
    size: int | None = None,
    validate: bool | None = None,
  ) -> BrokerSubAccountList:
    """List sub-accounts under this broker account.

    Args:
      sub_account_id: Sub-account identifier. All sub-accounts if omitted.
      page: Page number, 1-indexed. Default 1.
      size: Records per page. Default 500, max 500.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://binance-docs.github.io/Brokerage-API/Brokerage_Operation_Endpoints/)
    """
    request: Request = Request()
    if sub_account_id is not None:
      request['subAccountId'] = sub_account_id
    if page is not None:
      request['page'] = page
    if size is not None:
      request['size'] = size
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/broker/subAccount',
      meta={'security': 'SIGNED', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerSubAccountList,
    )
