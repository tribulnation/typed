# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, Sequence, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class BrokerFuturesRebateRecord(TypedDict):
  """One futures commission rebate."""

  subaccountId: str
  """Sub-account this rebate accrued from."""
  income: str
  """Rebate amount."""
  asset: str
  """Asset the rebate was paid in."""
  symbol: str
  """Futures symbol the underlying trade was on."""
  tradeId: int
  """Trade identifier the rebate accrued from."""
  time: TimestampMillis
  """Time the rebate was recorded."""
  status: int
  """Rebate status. Documented example is `1`; the venue does not declare this as a closed enum."""


class Request(TypedDict):
  """Request parameters."""

  futuresType: Literal[1, 2]
  """Futures product to filter by."""
  startTime: TimestampMillis
  """Start of the query window, UTC ms."""
  endTime: TimestampMillis
  """End of the query window, UTC ms."""
  page: NotRequired[int]
  """Page number, 1-indexed. Default 1."""
  size: NotRequired[int]
  """Records per page. Default 10, max 100."""


BrokerFuturesRebateRecordList = list[BrokerFuturesRebateRecord]


class CommissionRebateRecentRecordFutures(RpcEndpoint):
  """Get recent futures commission rebate records across sub-accounts."""

  def commission_rebate_recent_record_futures_paged(
    self,
    futures_type: Literal[1, 2],
    *,
    start_time: TimestampMillis,
    end_time: TimestampMillis,
    size: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[BrokerFuturesRebateRecord, int]:
    """Get recent futures commission rebate records across sub-accounts.

    Paged variant of `commission_rebate_recent_record_futures`: Requests `page` from 1 upwards and stops on the first page shorter than `size`. Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      futures_type: Futures product to filter by.
      start_time: Start of the query window, UTC ms.
      end_time: End of the query window, UTC ms.
      size: Records per page. Default 10, max 100.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://binance-docs.github.io/Brokerage-API/Brokerage_Operation_Endpoints/)
    """

    async def next(page: int) -> tuple[Sequence[BrokerFuturesRebateRecord], int | None]:
      response = await self.commission_rebate_recent_record_futures(
        futures_type,
        start_time=start_time,
        end_time=end_time,
        size=size,
        page=page,
        validate=validate,
      )
      rows = response
      if not rows or len(rows) < (min(size, 100) if size is not None else 10):
        return rows, None
      return rows, page + 1

    return PaginatedResponse(1, next)

  async def commission_rebate_recent_record_futures(
    self,
    futures_type: Literal[1, 2],
    *,
    start_time: TimestampMillis,
    end_time: TimestampMillis,
    page: int | None = None,
    size: int | None = None,
    validate: bool | None = None,
  ) -> BrokerFuturesRebateRecordList:
    """Get recent futures commission rebate records across sub-accounts.

    Args:
      futures_type: Futures product to filter by.
      start_time: Start of the query window, UTC ms.
      end_time: End of the query window, UTC ms.
      page: Page number, 1-indexed. Default 1.
      size: Records per page. Default 10, max 100.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://binance-docs.github.io/Brokerage-API/Brokerage_Operation_Endpoints/)
    """
    request: Request = Request(
      futuresType=futures_type, startTime=start_time, endTime=end_time
    )
    if page is not None:
      request['page'] = page
    if size is not None:
      request['size'] = size
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/broker/rebate/futures/recentRecord',
      meta={'security': 'SIGNED', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerFuturesRebateRecordList,
    )
