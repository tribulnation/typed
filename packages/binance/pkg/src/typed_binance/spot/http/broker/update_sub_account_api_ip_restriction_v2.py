# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class BrokerSubAccountApiIpRestrictionV2(TypedDict):
  """Updated IP restriction for a sub-account API key."""

  status: Literal['1', '2']
  """IP restriction status now in effect."""
  ipList: list[str]
  """Whitelisted IP addresses."""
  updateTime: TimestampMillis
  """Time this restriction was last updated."""
  apiKey: str
  """API key this restriction applies to."""


class Request(TypedDict):
  """Request parameters."""

  subAccountId: str
  """Sub-account identifier."""
  subAccountApiKey: str
  """Sub-account API key."""
  status: Literal['1', '2']
  """IP restriction status: `1` = unrestricted, `2` = restrict to `ipAddress` only."""
  ipAddress: NotRequired[str]
  """Static IPs to whitelist, comma-separated. Required when `status` is `2`."""


class UpdateSubAccountApiIpRestrictionV2(RpcEndpoint):
  """Set a sub-account API key's IP restriction status and whitelist in one call (v2 of ip_restriction; the v1 pair above only add/get/delete — this both updates status and (re)sets the whitelist)."""

  async def update_sub_account_api_ip_restriction_v2(
    self,
    *,
    sub_account_id: str,
    sub_account_api_key: str,
    status: Literal['1', '2'],
    ip_address: str | None = None,
    validate: bool | None = None,
  ) -> BrokerSubAccountApiIpRestrictionV2:
    """Set a sub-account API key's IP restriction status and whitelist in one call (v2 of ip_restriction; the v1 pair above only add/get/delete — this both updates status and (re)sets the whitelist).

    Args:
      sub_account_id: Sub-account identifier.
      sub_account_api_key: Sub-account API key.
      status: IP restriction status: `1` = unrestricted, `2` = restrict to `ipAddress` only.
      ip_address: Static IPs to whitelist, comma-separated. Required when `status` is `2`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://binance-docs.github.io/Brokerage-API/Brokerage_Operation_Endpoints/)
    """
    request: Request = Request(
      subAccountId=sub_account_id, subAccountApiKey=sub_account_api_key, status=status
    )
    if ip_address is not None:
      request['ipAddress'] = ip_address
    return await self.request(
      request,
      method='POST',
      path='/sapi/v2/broker/subAccountApi/ipRestriction',
      meta={'security': 'SIGNED', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerSubAccountApiIpRestrictionV2,
    )
