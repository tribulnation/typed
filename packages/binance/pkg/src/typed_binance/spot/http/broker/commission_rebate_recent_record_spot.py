# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class BrokerSpotRebateRecord(TypedDict):
  """One spot commission rebate."""

  subaccountId: str
  """Sub-account this rebate accrued from."""
  income: str
  """Rebate amount."""
  asset: str
  """Asset the rebate was paid in."""
  symbol: str
  """Trading symbol the underlying trade was on."""
  tradeId: int
  """Trade identifier the rebate accrued from."""
  time: TimestampMillis
  """Time the rebate was recorded."""
  status: int
  """Rebate status. Documented example is `1`; the venue does not declare this as a closed enum."""


class Request(TypedDict):
  """Request parameters."""

  subAccountId: NotRequired[str]
  """Sub-account identifier. All sub-accounts if omitted."""
  startTime: NotRequired[TimestampMillis]
  """Start of the query window, UTC ms. Defaults to 7 days before now."""
  endTime: NotRequired[TimestampMillis]
  """End of the query window, UTC ms. Defaults to now."""
  page: NotRequired[int]
  """Page number, 1-indexed. Default 1."""
  size: NotRequired[int]
  """Records per page. Default 500, max 500."""


BrokerSpotRebateRecordList = list[BrokerSpotRebateRecord]


class CommissionRebateRecentRecordSpot(RpcEndpoint):
  """Get recent spot commission rebate records across sub-accounts."""

  async def commission_rebate_recent_record_spot_paged(
    self,
    sub_account_id: str | None = None,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    size: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[BrokerSpotRebateRecordList]:
    """Get recent spot commission rebate records across sub-accounts.

    Paged variant of `commission_rebate_recent_record_spot`: Requests `page` from 1 upwards and stops on the first page shorter than `size`, or after `max_pages` pages when one is given.

    Args:
      sub_account_id: Sub-account identifier. All sub-accounts if omitted.
      start_time: Start of the query window, UTC ms. Defaults to 7 days before now.
      end_time: End of the query window, UTC ms. Defaults to now.
      size: Records per page. Default 500, max 500.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://binance-docs.github.io/Brokerage-API/Brokerage_Operation_Endpoints/)
    """
    page = 1
    pages = 0
    while True:
      response = await self.commission_rebate_recent_record_spot(
        sub_account_id,
        start_time=start_time,
        end_time=end_time,
        size=size,
        page=page,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      if not response or (size is not None and len(response) < size):
        break
      page += 1

  async def commission_rebate_recent_record_spot(
    self,
    sub_account_id: str | None = None,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    page: int | None = None,
    size: int | None = None,
    validate: bool | None = None,
  ) -> BrokerSpotRebateRecordList:
    """Get recent spot commission rebate records across sub-accounts.

    Args:
      sub_account_id: Sub-account identifier. All sub-accounts if omitted.
      start_time: Start of the query window, UTC ms. Defaults to 7 days before now.
      end_time: End of the query window, UTC ms. Defaults to now.
      page: Page number, 1-indexed. Default 1.
      size: Records per page. Default 500, max 500.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://binance-docs.github.io/Brokerage-API/Brokerage_Operation_Endpoints/)
    """
    request: Request = Request()
    if sub_account_id is not None:
      request['subAccountId'] = sub_account_id
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if page is not None:
      request['page'] = page
    if size is not None:
      request['size'] = size
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/broker/rebate/recentRecord',
      meta={'security': 'SIGNED', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerSpotRebateRecordList,
    )
