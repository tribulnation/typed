# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import NotRequired, Sequence, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class BrokerSubAccountApiKeyInfo(TypedDict):
  """One sub-account API key."""

  subaccountId: str
  """Sub-account identifier."""
  apikey: str
  """API key."""
  canTrade: bool
  """Whether spot trading is enabled."""
  marginTrade: bool
  """Whether margin trading is enabled."""
  futuresTrade: bool
  """Whether futures trading is enabled."""


class Request(TypedDict):
  """Request parameters."""

  subAccountId: str
  """Sub-account identifier."""
  subAccountApiKey: NotRequired[str]
  """Sub-account API key. All of the sub-account's keys if omitted."""
  page: NotRequired[int]
  """Page number, 1-indexed. Default 1."""
  size: NotRequired[int]
  """Records per page. Default 500, max 500."""


BrokerSubAccountApiKeyList = list[BrokerSubAccountApiKeyInfo]


class SubAccountApiKey(RpcEndpoint):
  """List API keys belonging to a sub-account."""

  def sub_account_api_key_paged(
    self,
    *,
    sub_account_id: str,
    sub_account_api_key: str | None = None,
    size: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[BrokerSubAccountApiKeyInfo, int]:
    """List API keys belonging to a sub-account.

    Paged variant of `sub_account_api_key`: Requests `page` from 1 upwards and stops on the first page shorter than `size`. Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      sub_account_id: Sub-account identifier.
      sub_account_api_key: Sub-account API key. All of the sub-account's keys if omitted.
      size: Records per page. Default 500, max 500.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://binance-docs.github.io/Brokerage-API/Brokerage_Operation_Endpoints/)
    """

    async def next(
      page: int,
    ) -> tuple[Sequence[BrokerSubAccountApiKeyInfo], int | None]:
      response = await self.sub_account_api_key(
        sub_account_id=sub_account_id,
        sub_account_api_key=sub_account_api_key,
        size=size,
        page=page,
        validate=validate,
      )
      rows = response
      if not rows or len(rows) < (min(size, 500) if size is not None else 500):
        return rows, None
      return rows, page + 1

    return PaginatedResponse(1, next)

  async def sub_account_api_key(
    self,
    *,
    sub_account_id: str,
    sub_account_api_key: str | None = None,
    page: int | None = None,
    size: int | None = None,
    validate: bool | None = None,
  ) -> BrokerSubAccountApiKeyList:
    """List API keys belonging to a sub-account.

    Args:
      sub_account_id: Sub-account identifier.
      sub_account_api_key: Sub-account API key. All of the sub-account's keys if omitted.
      page: Page number, 1-indexed. Default 1.
      size: Records per page. Default 500, max 500.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://binance-docs.github.io/Brokerage-API/Brokerage_Operation_Endpoints/)
    """
    request: Request = Request(subAccountId=sub_account_id)
    if sub_account_api_key is not None:
      request['subAccountApiKey'] = sub_account_api_key
    if page is not None:
      request['page'] = page
    if size is not None:
      request['size'] = size
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/broker/subAccountApi',
      meta={'security': 'SIGNED', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerSubAccountApiKeyList,
    )
