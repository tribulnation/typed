# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class BrokerSubAccountCoinmFuturesAssetV3(TypedDict):
  """One sub-account's summary."""

  subAccountId: str
  """Sub-account identifier."""
  totalWalletBalanceOfUsdt: NotRequired[str]
  """Total futures wallet balance, valued in USDT."""
  totalUnrealizedProfitOfUsdt: NotRequired[str]
  """Total unrealized PnL, valued in USDT."""
  totalMarginBalanceOfUsdt: NotRequired[str]
  """Total margin balance, valued in USDT."""
  futuresEnable: bool
  """Whether futures is enabled for this sub-account."""


class BrokerSubAccountUsdmFuturesAssetV3(TypedDict):
  """One sub-account's summary."""

  subAccountId: str
  """Sub-account identifier."""
  totalInitialMargin: NotRequired[str]
  """Total initial margin required."""
  totalMaintenanceMargin: NotRequired[str]
  """Total maintenance margin required."""
  totalWalletBalance: NotRequired[str]
  """Total futures wallet balance."""
  totalUnrealizedProfit: NotRequired[str]
  """Total unrealized PnL."""
  totalMarginBalance: NotRequired[str]
  """Total margin balance."""
  totalPositionInitialMargin: NotRequired[str]
  """Initial margin held by open positions."""
  totalOpenOrderInitialMargin: NotRequired[str]
  """Initial margin held by open orders."""
  futuresEnable: bool
  """Whether futures is enabled for this sub-account."""
  asset: NotRequired[str]
  """Asset the amounts above are denominated in."""


class Request(TypedDict):
  """Request parameters."""

  subAccountId: NotRequired[str]
  """Sub-account identifier. All sub-accounts if omitted."""
  futuresType: Literal[1, 2]
  """Futures product to summarize."""
  page: NotRequired[int]
  """Page number, 1-indexed. Default 1."""
  size: NotRequired[int]
  """Records per page. Default 10, max 20."""


class BrokerSubAccountFuturesSummaryV3Coinm(TypedDict):
  """Page of per-sub-account summaries. `futuresType=2` (COIN-M) shape."""

  data: list[BrokerSubAccountCoinmFuturesAssetV3]
  """Per-sub-account summaries."""
  timestamp: TimestampMillis
  """Time this snapshot was computed."""


class BrokerSubAccountFuturesSummaryV3Usdm(TypedDict):
  """Page of per-sub-account summaries. `futuresType=1` (USDⓈ-M) shape."""

  data: list[BrokerSubAccountUsdmFuturesAssetV3]
  """Per-sub-account summaries."""
  timestamp: TimestampMillis
  """Time this snapshot was computed."""


Response = BrokerSubAccountFuturesSummaryV3Usdm | BrokerSubAccountFuturesSummaryV3Coinm


class SubAccountFuturesSummaryV3(RpcEndpoint):
  """Get futures asset totals across sub-accounts for one futures product. `futuresType`: `1` = USDⓈ-M Futures, `2` = COIN-M Futures — the response shape differs by which was requested."""

  async def sub_account_futures_summary_v3_paged(
    self,
    sub_account_id: str | None = None,
    *,
    futures_type: Literal[1, 2],
    size: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[Response]:
    """Get futures asset totals across sub-accounts for one futures product. `futuresType`: `1` = USDⓈ-M Futures, `2` = COIN-M Futures — the response shape differs by which was requested.

    Paged variant of `sub_account_futures_summary_v3`: Requests `page` from 1 upwards and stops on the first page shorter than `size`, or after `max_pages` pages when one is given.

    Args:
      sub_account_id: Sub-account identifier. All sub-accounts if omitted.
      futures_type: Futures product to summarize.
      size: Records per page. Default 10, max 20.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://binance-docs.github.io/Brokerage-API/Brokerage_Operation_Endpoints/)
    """
    page = 1
    pages = 0
    while True:
      response = await self.sub_account_futures_summary_v3(
        sub_account_id,
        futures_type=futures_type,
        size=size,
        page=page,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      rows = response.get('data') if response is not None else None
      if not rows or (size is not None and len(rows) < size):
        break
      page += 1

  async def sub_account_futures_summary_v3(
    self,
    sub_account_id: str | None = None,
    *,
    futures_type: Literal[1, 2],
    page: int | None = None,
    size: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get futures asset totals across sub-accounts for one futures product. `futuresType`: `1` = USDⓈ-M Futures, `2` = COIN-M Futures — the response shape differs by which was requested.

    Args:
      sub_account_id: Sub-account identifier. All sub-accounts if omitted.
      futures_type: Futures product to summarize.
      page: Page number, 1-indexed. Default 1.
      size: Records per page. Default 10, max 20.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://binance-docs.github.io/Brokerage-API/Brokerage_Operation_Endpoints/)
    """
    request: Request = Request(futuresType=futures_type)
    if sub_account_id is not None:
      request['subAccountId'] = sub_account_id
    if page is not None:
      request['page'] = page
    if size is not None:
      request['size'] = size
    return await self.request(
      request,
      method='GET',
      path='/sapi/v3/broker/subAccount/futuresSummary',
      meta={'security': 'SIGNED', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
