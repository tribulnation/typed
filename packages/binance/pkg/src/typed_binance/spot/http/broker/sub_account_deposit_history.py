# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class BrokerSubAccountDeposit(TypedDict):
  """One sub-account deposit."""

  depositId: int
  """Deposit identifier."""
  subAccountId: str
  """Sub-account the deposit landed in."""
  address: str
  """Deposit address credited."""
  amount: str
  """Amount deposited."""
  coin: str
  """Asset deposited."""
  insertTime: TimestampMillis
  """Time the deposit was recorded."""
  transferType: int
  """Whether this was an internal transfer. Documented example is `0`; the venue does not declare this as a closed enum."""
  network: str
  """Network the deposit arrived over."""
  status: Literal[0, 1, 6]
  """Deposit status."""
  txId: str
  """On-chain transaction id."""


class Request(TypedDict):
  """Request parameters."""

  subAccountId: NotRequired[str]
  """Sub-account identifier. All sub-accounts if omitted."""
  coin: NotRequired[str]
  """Filter by deposited asset."""
  status: NotRequired[Literal[0, 1, 6]]
  """Filter by deposit status."""
  startTime: NotRequired[TimestampMillis]
  """Start of the query window, UTC ms. Defaults to 7 days before now."""
  endTime: NotRequired[TimestampMillis]
  """End of the query window, UTC ms. Defaults to now."""
  limit: NotRequired[int]
  """Maximum records to return. Default 500."""
  offset: NotRequired[int]
  """Number of records to skip. Default 0."""


BrokerSubAccountDepositList = list[BrokerSubAccountDeposit]


class SubAccountDepositHistory(RpcEndpoint):
  """Get deposit history across sub-accounts."""

  async def sub_account_deposit_history_paged(
    self,
    *,
    sub_account_id: str | None = None,
    coin: str | None = None,
    status: Literal[0, 1, 6] | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[BrokerSubAccountDepositList]:
    """Get deposit history across sub-accounts.

    Paged variant of `sub_account_deposit_history`: Advances `offset` by `len(response)` and stops on the first page shorter than `limit`, or after `max_pages` pages when one is given.

    Args:
      sub_account_id: Sub-account identifier. All sub-accounts if omitted.
      coin: Filter by deposited asset.
      status: Filter by deposit status.
      start_time: Start of the query window, UTC ms. Defaults to 7 days before now.
      end_time: End of the query window, UTC ms. Defaults to now.
      limit: Maximum records to return. Default 500.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://binance-docs.github.io/Brokerage-API/Brokerage_Operation_Endpoints/)
    """
    offset = 0
    pages = 0
    while True:
      response = await self.sub_account_deposit_history(
        sub_account_id=sub_account_id,
        coin=coin,
        status=status,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        offset=offset,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      if not response or (limit is not None and len(response) < limit):
        break
      offset += len(response)

  async def sub_account_deposit_history(
    self,
    *,
    sub_account_id: str | None = None,
    coin: str | None = None,
    status: Literal[0, 1, 6] | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    offset: int | None = None,
    validate: bool | None = None,
  ) -> BrokerSubAccountDepositList:
    """Get deposit history across sub-accounts.

    Args:
      sub_account_id: Sub-account identifier. All sub-accounts if omitted.
      coin: Filter by deposited asset.
      status: Filter by deposit status.
      start_time: Start of the query window, UTC ms. Defaults to 7 days before now.
      end_time: End of the query window, UTC ms. Defaults to now.
      limit: Maximum records to return. Default 500.
      offset: Number of records to skip. Default 0.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://binance-docs.github.io/Brokerage-API/Brokerage_Operation_Endpoints/)
    """
    request: Request = Request()
    if sub_account_id is not None:
      request['subAccountId'] = sub_account_id
    if coin is not None:
      request['coin'] = coin
    if status is not None:
      request['status'] = status
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    if offset is not None:
      request['offset'] = offset
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/broker/subAccount/depositHist',
      meta={'security': 'SIGNED', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerSubAccountDepositList,
    )
