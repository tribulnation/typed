# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class BrokerSubAccountCoinmFuturesCommissionInfo(TypedDict):
  """COIN-M futures commission for one sub-account pair."""

  subAccountId: int
  """Sub-account identifier."""
  pair: str
  """COIN-M futures pair."""
  makerCommission: int
  """Effective maker commission."""
  takerCommission: int
  """Effective taker commission."""


class Request(TypedDict):
  """Request parameters."""

  subAccountId: str
  """Sub-account identifier."""
  pair: NotRequired[str]
  """COIN-M futures pair. All pairs if omitted."""


BrokerSubAccountCoinmFuturesCommissionList = list[
  BrokerSubAccountCoinmFuturesCommissionInfo
]


class SubAccountCoinmFuturesCommission(RpcEndpoint):
  """Query a sub-account's COIN-M futures commission rates."""

  async def sub_account_coinm_futures_commission(
    self,
    *,
    sub_account_id: str,
    pair: str | None = None,
    validate: bool | None = None,
  ) -> BrokerSubAccountCoinmFuturesCommissionList:
    """Query a sub-account's COIN-M futures commission rates.

    Args:
      sub_account_id: Sub-account identifier.
      pair: COIN-M futures pair. All pairs if omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://binance-docs.github.io/Brokerage-API/Brokerage_Operation_Endpoints/)
    """
    request: Request = Request(subAccountId=sub_account_id)
    if pair is not None:
      request['pair'] = pair
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/broker/subAccountApi/commission/coinFutures',
      meta={'security': 'SIGNED', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerSubAccountCoinmFuturesCommissionList,
    )
