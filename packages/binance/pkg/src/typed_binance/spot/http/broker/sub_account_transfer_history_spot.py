# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class BrokerSpotTransferRecord(TypedDict):
  """One historical spot transfer."""

  fromId: str
  """Source account."""
  toId: str
  """Destination account."""
  asset: str
  """Asset transferred."""
  qty: str
  """Quantity transferred."""
  time: TimestampMillis
  """Time the transfer was processed."""
  txnId: str
  """Transfer transaction identifier."""
  clientTranId: str
  """Caller-supplied idempotency id, if any."""
  status: Literal['INIT', 'PROCESS', 'SUCCESS', 'FAILURE']
  """Transfer status. If showAllStatus is true, four values are possible (INIT/PROCESS/SUCCESS/FAILURE); if false (the default), only three (INIT/PROCESS/SUCCESS)."""


class Request(TypedDict):
  """Request parameters."""

  fromId: NotRequired[str]
  """Filter by source sub-account."""
  toId: NotRequired[str]
  """Filter by destination sub-account."""
  clientTranId: NotRequired[str]
  """Filter by caller-supplied idempotency id."""
  showAllStatus: NotRequired[bool]
  """Include transfers of every status, not just successful ones. Default false."""
  startTime: NotRequired[TimestampMillis]
  """Start of the query window, UTC ms."""
  endTime: NotRequired[TimestampMillis]
  """End of the query window, UTC ms."""
  page: NotRequired[int]
  """Page number, 1-indexed. Default 1."""
  limit: NotRequired[int]
  """Records per page. Default 500, max 500."""


BrokerSpotTransferList = list[BrokerSpotTransferRecord]


class SubAccountTransferHistorySpot(RpcEndpoint):
  """Get historical spot transfers between the master account and sub-accounts."""

  async def sub_account_transfer_history_spot_paged(
    self,
    *,
    from_id: str | None = None,
    to_id: str | None = None,
    client_tran_id: str | None = None,
    show_all_status: bool | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[BrokerSpotTransferList]:
    """Get historical spot transfers between the master account and sub-accounts.

    Paged variant of `sub_account_transfer_history_spot`: Requests `page` from 1 upwards and stops on the first page shorter than `limit`, or after `max_pages` pages when one is given.

    Args:
      from_id: Filter by source sub-account.
      to_id: Filter by destination sub-account.
      client_tran_id: Filter by caller-supplied idempotency id.
      show_all_status: Include transfers of every status, not just successful ones. Default false.
      start_time: Start of the query window, UTC ms.
      end_time: End of the query window, UTC ms.
      limit: Records per page. Default 500, max 500.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://binance-docs.github.io/Brokerage-API/Brokerage_Operation_Endpoints/)
    """
    page = 1
    pages = 0
    while True:
      response = await self.sub_account_transfer_history_spot(
        from_id=from_id,
        to_id=to_id,
        client_tran_id=client_tran_id,
        show_all_status=show_all_status,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        page=page,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      if not response or (limit is not None and len(response) < limit):
        break
      page += 1

  async def sub_account_transfer_history_spot(
    self,
    *,
    from_id: str | None = None,
    to_id: str | None = None,
    client_tran_id: str | None = None,
    show_all_status: bool | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    page: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> BrokerSpotTransferList:
    """Get historical spot transfers between the master account and sub-accounts.

    Args:
      from_id: Filter by source sub-account.
      to_id: Filter by destination sub-account.
      client_tran_id: Filter by caller-supplied idempotency id.
      show_all_status: Include transfers of every status, not just successful ones. Default false.
      start_time: Start of the query window, UTC ms.
      end_time: End of the query window, UTC ms.
      page: Page number, 1-indexed. Default 1.
      limit: Records per page. Default 500, max 500.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://binance-docs.github.io/Brokerage-API/Brokerage_Operation_Endpoints/)
    """
    request: Request = Request()
    if from_id is not None:
      request['fromId'] = from_id
    if to_id is not None:
      request['toId'] = to_id
    if client_tran_id is not None:
      request['clientTranId'] = client_tran_id
    if show_all_status is not None:
      request['showAllStatus'] = show_all_status
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if page is not None:
      request['page'] = page
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/broker/transfer',
      meta={'security': 'SIGNED', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerSpotTransferList,
    )
