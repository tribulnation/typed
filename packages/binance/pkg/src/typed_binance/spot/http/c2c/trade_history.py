# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis, timestamp_millis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class C2CTrade(TypedDict):
  """One C2C (peer-to-peer) trade."""

  orderNumber: NotRequired[str]
  """Order identifier."""
  advNo: NotRequired[str]
  """Advertisement identifier the trade was matched against."""
  tradeType: NotRequired[Literal['BUY', 'SELL']]
  """Trade side."""
  asset: NotRequired[str]
  """Cryptocurrency traded."""
  fiat: NotRequired[str]
  """Fiat currency code."""
  fiatSymbol: NotRequired[str]
  """Fiat currency symbol."""
  amount: NotRequired[str]
  """Quantity, in crypto."""
  totalPrice: NotRequired[str]
  """Total order amount, in fiat."""
  unitPrice: NotRequired[str]
  """Unit price, in fiat."""
  orderStatus: NotRequired[
    Literal[
      'PENDING',
      'TRADING',
      'BUYER_PAYED',
      'DISTRIBUTING',
      'COMPLETED',
      'IN_APPEAL',
      'CANCELLED',
      'CANCELLED_BY_SYSTEM',
    ]
  ]
  """Order status."""
  createTime: NotRequired[TimestampMillis]
  """Millisecond epoch order creation timestamp."""
  commission: NotRequired[str]
  """Transaction fee, in crypto."""
  counterPartNickName: NotRequired[str]
  """Counterparty's nickname."""
  payMethodName: NotRequired[str]
  """Payment method identifier."""
  additionalKycVerify: NotRequired[int]
  """KYC verification status: 0 not required, 1 not verified, 2 verified."""
  takerCommissionRate: NotRequired[str]
  """Taker commission rate."""
  takerCommission: NotRequired[str]
  """Taker commission amount."""
  takerAmount: NotRequired[str]
  """Taker trade amount."""
  advertisementRole: NotRequired[Literal['MAKER', 'TAKER']]
  """This account's role in the trade."""


class C2CTradeHistoryResponse(TypedDict):
  """C2C trade history response frame. This product line wraps its business data in {code,message,data,total,success} on the wire, and the core does not strip it -- see spec/core.md's Envelope section."""

  code: NotRequired[str]
  """Response code. "000000" indicates success."""
  message: NotRequired[str]
  """Response message."""
  data: NotRequired[list[C2CTrade]]
  """C2C trade records."""
  total: NotRequired[int]
  """Total number of matching records across all pages."""
  success: NotRequired[bool]
  """Whether the call succeeded."""


class TradeHistory(RpcEndpoint):
  """Query this account's C2C (peer-to-peer) trade history. Only the past 6 months are queryable."""

  async def trade_history(
    self,
    *,
    trade_type: Literal['BUY', 'SELL'] | None = None,
    start_timestamp: TimestampMillis | None = None,
    end_timestamp: TimestampMillis | None = None,
    page: int | None = None,
    rows: int | None = None,
    validate: bool | None = None,
  ) -> C2CTradeHistoryResponse:
    """Query this account's C2C (peer-to-peer) trade history. Only the past 6 months are queryable.

    Args:
      trade_type: Trade side filter.
      start_timestamp: Millisecond epoch start of the queried window. If omitted along with `endTimestamp`, the recent 30 days are returned.
      end_timestamp: Millisecond epoch end of the queried window. Max interval from `startTimestamp` is 30 days.
      page: Page number.
      rows: Number of records per page.

    References:
      - [Official docs](https://developers.binance.com/docs/c2c/rest-api/Get-C2C-Trade-History)
    """
    params = {}
    if trade_type is not None:
      params['tradeType'] = trade_type
    if start_timestamp is not None:
      params['startTimestamp'] = timestamp_millis.dump(start_timestamp)
    if end_timestamp is not None:
      params['endTimestamp'] = timestamp_millis.dump(end_timestamp)
    if page is not None:
      params['page'] = page
    if rows is not None:
      params['rows'] = rows
    _Response = C2CTradeHistoryResponse
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/sapi/v1/c2c/orderMatch/listUserOrderHistory',
      params=params,
      validator=_validator,
      validate=validate,
    )

  async def trade_history_paged(
    self,
    *,
    trade_type: Literal['BUY', 'SELL'] | None = None,
    start_timestamp: TimestampMillis | None = None,
    end_timestamp: TimestampMillis | None = None,
    rows: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[C2CTradeHistoryResponse]:
    """Yield successive pages of `trade_history`.

    Requests `page` from 1 upwards and stops once it has covered the `total` items the
    response reports, or after `max_pages` pages when one is given.
    """
    page = 1
    pages = 0
    while True:
      response = await self.trade_history(
        trade_type=trade_type,
        start_timestamp=start_timestamp,
        end_timestamp=end_timestamp,
        rows=rows,
        page=page,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      total = response.get('total') if response is not None else None
      total = int(total) if total is not None else None
      if total is None or rows is None or pages * rows >= total:
        break
      page += 1
