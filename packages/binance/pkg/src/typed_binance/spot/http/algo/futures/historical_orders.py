# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class FuturesAlgoHistoricalOrder(TypedDict):
  """One futures Algo order."""

  algoId: int
  """Algo order identifier."""
  symbol: str
  """Trading symbol."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: Literal['BOTH', 'LONG', 'SHORT']
  """Position side. `BOTH` outside Hedge Mode, `LONG`/`SHORT` in it."""
  totalQty: str
  """Total order quantity, in the base asset."""
  executedQty: str
  """Executed quantity so far, in the base asset."""
  executedAmt: str
  """Executed notional amount so far, in the quote asset."""
  avgPrice: str
  """Average execution price so far."""
  clientAlgoId: str
  """Client-supplied (or server-generated) unique Algo order identifier."""
  bookTime: TimestampMillis
  """Time the order was booked."""
  endTime: TimestampMillis
  """Time the order finished (0 while still working)."""
  algoStatus: str
  """Algo order status. Documented values include `WORKING`, `DONE`, `CANCELLED`, `FAILED`, but the venue does not declare this as a closed enum."""
  algoType: str
  """Algo strategy. Documented examples are `TWAP` and `VP`, but the venue does not declare this as a closed enum."""
  urgency: Literal['LOW', 'MEDIUM', 'HIGH']
  """Execution urgency selected at order time."""


class Request(TypedDict):
  """Request parameters."""

  symbol: NotRequired[str]
  """Trading symbol, e.g. BNBUSDT."""
  side: NotRequired[Literal['SELL', 'BUY']]
  """Order side."""
  startTime: NotRequired[TimestampMillis]
  """Start of the query window, UTC ms."""
  endTime: NotRequired[TimestampMillis]
  """End of the query window, UTC ms."""
  page: NotRequired[int]
  """Page number, 1-indexed."""
  pageSize: NotRequired[int]
  """Records per page. Range [1, 100]. The venue documents this parameter as a string; specced as `integer` since it is arithmetic input to pagination (spec-authoring rule 7)."""


class FuturesAlgoHistoricalOrders(TypedDict):
  """Historical (finished or cancelled) Algo orders."""

  total: int
  """Number of historical Algo orders returned."""
  orders: list[FuturesAlgoHistoricalOrder]
  """Historical Algo orders."""


class HistoricalOrders(RpcEndpoint):
  """Get historical (finished or cancelled) USDⓈ-M futures Algo orders. Only supported on USDⓈ-M Contracts. Requires `Futures Trading Permission` enabled for the API key."""

  def historical_orders_paged(
    self,
    symbol: str | None = None,
    *,
    side: Literal['SELL', 'BUY'] | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    page_size: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[FuturesAlgoHistoricalOrder, int]:
    """Get historical (finished or cancelled) USDⓈ-M futures Algo orders. Only supported on USDⓈ-M Contracts. Requires `Futures Trading Permission` enabled for the API key.

    Paged variant of `historical_orders`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      symbol: Trading symbol, e.g. BNBUSDT.
      side: Order side.
      start_time: Start of the query window, UTC ms.
      end_time: End of the query window, UTC ms.
      page_size: Records per page. Range [1, 100]. The venue documents this parameter as a string; specced as `integer` since it is arithmetic input to pagination (spec-authoring rule 7).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/docs/algo/future-algo/Query-Historical-Algo-Orders)
    """

    async def next(page: int) -> tuple[list[FuturesAlgoHistoricalOrder], int | None]:
      response = await self.historical_orders(
        symbol,
        side=side,
        start_time=start_time,
        end_time=end_time,
        page_size=page_size,
        page=page,
        validate=validate,
      )
      rows = response.get('orders')
      rows = rows if rows is not None else []
      total = response.get('total')
      total = int(total) if total is not None else None
      if total is None or page_size is None or (page - 1 + 1) * page_size >= total:
        return rows, None
      return rows, page + 1

    return PaginatedResponse(1, next)

  async def historical_orders(
    self,
    symbol: str | None = None,
    *,
    side: Literal['SELL', 'BUY'] | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    page: int | None = None,
    page_size: int | None = None,
    validate: bool | None = None,
  ) -> FuturesAlgoHistoricalOrders:
    """Get historical (finished or cancelled) USDⓈ-M futures Algo orders. Only supported on USDⓈ-M Contracts. Requires `Futures Trading Permission` enabled for the API key.

    Args:
      symbol: Trading symbol, e.g. BNBUSDT.
      side: Order side.
      start_time: Start of the query window, UTC ms.
      end_time: End of the query window, UTC ms.
      page: Page number, 1-indexed.
      page_size: Records per page. Range [1, 100]. The venue documents this parameter as a string; specced as `integer` since it is arithmetic input to pagination (spec-authoring rule 7).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/docs/algo/future-algo/Query-Historical-Algo-Orders)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    if side is not None:
      request['side'] = side
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if page is not None:
      request['page'] = page
    if page_size is not None:
      request['pageSize'] = page_size
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/algo/futures/historicalOrders',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FuturesAlgoHistoricalOrders,
    )
