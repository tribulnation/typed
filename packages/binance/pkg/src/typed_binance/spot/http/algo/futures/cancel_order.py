# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class AlgoCancelAck(TypedDict):
  """Acknowledgement that an Algo order was cancelled."""

  algoId: int
  """Algo order identifier that was cancelled."""
  success: bool
  """Whether the cancellation was accepted."""
  code: int
  """Response code. `0` on success."""
  msg: str
  """Response message. `"OK"` on success."""


class Request(TypedDict):
  """Request parameters."""

  algoId: int
  """Algo order identifier to cancel, e.g. 14511."""


class CancelOrder(RpcEndpoint):
  """Cancel an active USDⓈ-M futures Algo order. Only supported on USDⓈ-M Contracts. Requires `Futures Trading Permission` enabled for the API key."""

  async def cancel_order(
    self,
    algo_id: int,
    *,
    validate: bool | None = None,
  ) -> AlgoCancelAck:
    """Cancel an active USDⓈ-M futures Algo order. Only supported on USDⓈ-M Contracts. Requires `Futures Trading Permission` enabled for the API key.

    Args:
      algo_id: Algo order identifier to cancel, e.g. 14511.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/docs/algo/future-algo/Cancel-Algo-Order)
    """
    return await self.request(
      Request(algoId=algo_id),
      method='DELETE',
      path='/sapi/v1/algo/futures/order',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=AlgoCancelAck,
    )
