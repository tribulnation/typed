# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .futures import Futures
from .spot import Spot
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Algo(RpcEndpoint):
  """Algo Trading endpoints for Spot and USDⓈ-M Futures together -- placing, querying, and cancelling TWAP orders (plus Futures' Volume Participation orders); see the `spot` and `futures` sub-groupings below for per-product-line detail. The catalog's own Algo Trading category root has no real content of its own (resolves to the full product catalog); linking the closest real page instead (the `spot` sub-grouping's own).

  References:
    - [Upstream docs](https://developers.binance.com/en/docs/catalog/advanced-trading-algo-trading/api/rest-api/spot-algo)
  """

  @cached_property
  def futures(self) -> Futures:
    """USDⓈ-M Futures Algo Trading endpoints -- placing a new TWAP or Volume Participation (VP) order plus querying open orders, historical orders, and the sub-orders that make up a given algo order, and cancelling one.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/advanced-trading-algo-trading/api/rest-api/future-algo)
    """
    return Futures(client=self.client)

  @cached_property
  def spot(self) -> Spot:
    """Spot Algo Trading endpoints -- placing a new TWAP order plus querying open orders, historical orders, and the sub-orders that make up a given algo order, and cancelling one.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/advanced-trading-algo-trading/api/rest-api/spot-algo)
    """
    return Spot(client=self.client)
