# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class FuturesAlgoOrder(TypedDict):
  """One futures Algo order."""

  algoId: int
  """Algo order identifier."""
  symbol: str
  """Trading symbol."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: Literal['BOTH', 'LONG', 'SHORT']
  """Position side. `BOTH` outside Hedge Mode, `LONG`/`SHORT` in it."""
  totalQty: str
  """Total order quantity, in the base asset."""
  executedQty: str
  """Executed quantity so far, in the base asset."""
  executedAmt: str
  """Executed notional amount so far, in the quote asset."""
  avgPrice: str
  """Average execution price so far."""
  clientAlgoId: str
  """Client-supplied (or server-generated) unique Algo order identifier."""
  bookTime: TimestampMillis
  """Time the order was booked."""
  endTime: TimestampMillis
  """Time the order finished (0 while still working)."""
  algoStatus: str
  """Algo order status. Documented values include `WORKING`, `DONE`, `CANCELLED`, `FAILED`, but the venue does not declare this as a closed enum."""
  algoType: str
  """Algo strategy. Documented examples are `TWAP` and `VP`, but the venue does not declare this as a closed enum."""
  urgency: Literal['LOW', 'MEDIUM', 'HIGH']
  """Execution urgency selected at order time."""


class FuturesAlgoOpenOrders(TypedDict):
  """Currently working Algo orders."""

  total: int
  """Number of open Algo orders returned."""
  orders: list[FuturesAlgoOrder]
  """Open Algo orders."""


class OpenOrders(RpcEndpoint):
  """Get all working USDⓈ-M futures Algo orders. Only supported on USDⓈ-M Contracts. Requires `Futures Trading Permission` enabled for the API key."""

  async def open_orders(self, *, validate: bool | None = None) -> FuturesAlgoOpenOrders:
    """Get all working USDⓈ-M futures Algo orders. Only supported on USDⓈ-M Contracts. Requires `Futures Trading Permission` enabled for the API key.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/docs/algo/future-algo/Query-Current-Algo-Open-Orders)
    """
    return await self.request(
      None,
      method='GET',
      path='/sapi/v1/algo/futures/openOrders',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      response_type=FuturesAlgoOpenOrders,
    )
