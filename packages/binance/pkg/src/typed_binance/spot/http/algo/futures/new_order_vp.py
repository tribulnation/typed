# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class AlgoOrderAck(TypedDict):
  """Acknowledgement that an Algo order was accepted for processing."""

  clientAlgoId: str
  """Client-supplied (or server-generated) unique Algo order identifier."""
  success: bool
  """Whether the order was accepted. Does not mean the order will be executed — check `openOrders`/`historicalOrders` for the real status."""
  code: int
  """Response code. `0` on success."""
  msg: str
  """Response message. `"OK"` on success."""


class Request(TypedDict):
  """Request parameters."""

  symbol: str
  """Trading symbol, e.g. BNBUSDT."""
  side: Literal['SELL', 'BUY']
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Default `BOTH` for One-way Mode; `LONG` or `SHORT` for Hedge Mode. Must be sent in Hedge Mode."""
  quantity: float
  """Quantity of the base asset; the notional (quantity * mark price) must be more than the equivalent of 10,000 USDT and less than the equivalent of 1,000,000 USDT."""
  urgency: Literal['LOW', 'MEDIUM', 'HIGH']
  """Relative speed of the current execution."""
  clientAlgoId: NotRequired[str]
  """Unique 32-character client identifier for this Algo order. Auto-generated if omitted."""
  reduceOnly: NotRequired[bool]
  """Default false. Cannot be sent in Hedge Mode; cannot be sent when opening a position."""
  limitPrice: NotRequired[float]
  """Limit price for child orders. Market price is used if omitted."""


class NewOrderVp(RpcEndpoint):
  """Place a new USDⓈ-M futures VP order with the Algo service. Only supported on USDⓈ-M Contracts. Requires `Futures Trading Permission` enabled for the API key. VP has no spot counterpart — it is futures-only. Maximum 10 open Algo orders allowed."""

  async def new_order_vp(
    self,
    *,
    symbol: str,
    side: Literal['SELL', 'BUY'],
    position_side: Literal['BOTH', 'LONG', 'SHORT'] | None = None,
    quantity: float,
    urgency: Literal['LOW', 'MEDIUM', 'HIGH'],
    client_algo_id: str | None = None,
    reduce_only: bool | None = None,
    limit_price: float | None = None,
    validate: bool | None = None,
  ) -> AlgoOrderAck:
    """Place a new USDⓈ-M futures VP order with the Algo service. Only supported on USDⓈ-M Contracts. Requires `Futures Trading Permission` enabled for the API key. VP has no spot counterpart — it is futures-only. Maximum 10 open Algo orders allowed.

    Args:
      symbol: Trading symbol, e.g. BNBUSDT.
      side: Order side.
      position_side: Default `BOTH` for One-way Mode; `LONG` or `SHORT` for Hedge Mode. Must be sent in Hedge Mode.
      quantity: Quantity of the base asset; the notional (quantity * mark price) must be more than the equivalent of 10,000 USDT and less than the equivalent of 1,000,000 USDT.
      urgency: Relative speed of the current execution.
      client_algo_id: Unique 32-character client identifier for this Algo order. Auto-generated if omitted.
      reduce_only: Default false. Cannot be sent in Hedge Mode; cannot be sent when opening a position.
      limit_price: Limit price for child orders. Market price is used if omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/docs/algo/future-algo/Volume-Participation-New-Order)
    """
    request: Request = Request(
      symbol=symbol, side=side, quantity=quantity, urgency=urgency
    )
    if position_side is not None:
      request['positionSide'] = position_side
    if client_algo_id is not None:
      request['clientAlgoId'] = client_algo_id
    if reduce_only is not None:
      request['reduceOnly'] = reduce_only
    if limit_price is not None:
      request['limitPrice'] = limit_price
    return await self.request(
      request,
      method='POST',
      path='/sapi/v1/algo/futures/newOrderVp',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=AlgoOrderAck,
    )
