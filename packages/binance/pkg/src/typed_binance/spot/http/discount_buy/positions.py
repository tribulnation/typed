# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class DiscountBuyPosition(TypedDict):
  """One Discount Buy position."""

  id: NotRequired[str]
  """Position identifier."""
  investAsset: NotRequired[str]
  """Subscription (stablecoin) asset."""
  targetAsset: NotRequired[str]
  """Target (non-stablecoin) asset."""
  depositAmount: NotRequired[str]
  """Amount deposited into this position, as a decimal string."""
  strikePrice: NotRequired[str]
  """Strike price of the accumulator, as a decimal string."""
  knockoutPrice: NotRequired[str]
  """Knock-out price of the accumulator, as a decimal string."""
  duration: NotRequired[int]
  """Product duration in days."""
  settleDate: NotRequired[TimestampMillis]
  """Millisecond epoch settlement date."""
  purchaseStatus: NotRequired[
    Literal[
      'PENDING',
      'PURCHASE_SUCCESS',
      'SETTLED',
      'PURCHASE_FAIL',
      'REFUNDING',
      'REFUND_SUCCESS',
      'SETTLING',
    ]
  ]
  """Purchase status."""
  knockoutApr: NotRequired[str]
  """Annual percentage rate if the product knocks out, as a decimal string."""
  orderId: NotRequired[int]
  """Order identifier of the originating subscription."""


class Request(TypedDict):
  """Request parameters."""

  status: NotRequired[
    Literal[
      'PENDING',
      'PURCHASE_SUCCESS',
      'SETTLED',
      'PURCHASE_FAIL',
      'REFUNDING',
      'REFUND_SUCCESS',
      'SETTLING',
    ]
  ]
  """Restrict results to this position status. `PENDING`: purchasing, result pending; `PURCHASE_SUCCESS`: purchased successfully; `SETTLED`: settlement finished; `PURCHASE_FAIL`: purchase failed; `REFUNDING`: refund in progress; `REFUND_SUCCESS`: refunded to the Spot account; `SETTLING`: settlement in progress. Returns positions in every status when omitted."""
  pageSize: int
  """Number of records per page."""
  pageIndex: int
  """Page index to return, starting from 1."""


class DiscountBuyPositionPage(TypedDict):
  """One page of this account's Discount Buy positions."""

  total: NotRequired[int]
  """Total number of matching positions."""
  list: NotRequired[list[DiscountBuyPosition]]
  """Matching positions on this page."""


class Positions(RpcEndpoint):
  """Get this account's Discount Buy positions."""

  def positions_paged(
    self,
    status: Literal[
      'PENDING',
      'PURCHASE_SUCCESS',
      'SETTLED',
      'PURCHASE_FAIL',
      'REFUNDING',
      'REFUND_SUCCESS',
      'SETTLING',
    ]
    | None = None,
    *,
    page_size: int,
    validate: bool | None = None,
  ) -> PaginatedResponse[DiscountBuyPosition, int]:
    """Get this account's Discount Buy positions.

    Paged variant of `positions`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      status: Restrict results to this position status. `PENDING`: purchasing, result pending; `PURCHASE_SUCCESS`: purchased successfully; `SETTLED`: settlement finished; `PURCHASE_FAIL`: purchase failed; `REFUNDING`: refund in progress; `REFUND_SUCCESS`: refunded to the Spot account; `SETTLING`: settlement in progress. Returns positions in every status when omitted.
      page_size: Number of records per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-discount-buy/api/rest-api/trade#get-discount-buy-positions)
    """

    async def next(page_index: int) -> tuple[list[DiscountBuyPosition], int | None]:
      response = await self.positions(
        status, page_size=page_size, page_index=page_index, validate=validate
      )
      rows = response.get('list')
      rows = rows if rows is not None else []
      total = response.get('total')
      total = int(total) if total is not None else None
      if total is None or (page_index - 1 + 1) * page_size >= total:
        return rows, None
      return rows, page_index + 1

    return PaginatedResponse(1, next)

  async def positions(
    self,
    status: Literal[
      'PENDING',
      'PURCHASE_SUCCESS',
      'SETTLED',
      'PURCHASE_FAIL',
      'REFUNDING',
      'REFUND_SUCCESS',
      'SETTLING',
    ]
    | None = None,
    *,
    page_size: int,
    page_index: int,
    validate: bool | None = None,
  ) -> DiscountBuyPositionPage:
    """Get this account's Discount Buy positions.

    Args:
      status: Restrict results to this position status. `PENDING`: purchasing, result pending; `PURCHASE_SUCCESS`: purchased successfully; `SETTLED`: settlement finished; `PURCHASE_FAIL`: purchase failed; `REFUNDING`: refund in progress; `REFUND_SUCCESS`: refunded to the Spot account; `SETTLING`: settlement in progress. Returns positions in every status when omitted.
      page_size: Number of records per page.
      page_index: Page index to return, starting from 1.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-discount-buy/api/rest-api/trade#get-discount-buy-positions)
    """
    request: Request = Request(pageSize=page_size, pageIndex=page_index)
    if status is not None:
      request['status'] = status
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/accumulator/product/position/list',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=DiscountBuyPositionPage,
    )
