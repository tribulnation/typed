# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class BfusdSubscription(TypedDict):
  """Result of a BFUSD subscription."""

  success: NotRequired[bool]
  """Whether the subscription succeeded."""
  bfusdAmount: NotRequired[str]
  """Amount of BFUSD received, as a decimal string."""


class Subscribe(RpcEndpoint):
  """Subscribe BFUSD."""

  async def __call__(
    self,
    *,
    asset: str,
    amount: float,
    validate: bool | None = None,
  ) -> BfusdSubscription:
    """Subscribe BFUSD.

    Args:
      asset: Asset to subscribe with, e.g. `USDT`. No documented closed set for this field.
      amount: Amount to subscribe with, in `asset`.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-simple-earn/api/rest-api/bfusd#subscribe-bfusd)
    """
    params: dict = {
      'asset': asset,
      'amount': amount,
    }
    _Response = BfusdSubscription
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/bfusd/subscribe',
      params=params,
      validator=_validator,
      validate=validate,
    )
