# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class BfusdSubscription(TypedDict):
  """Result of a BFUSD subscription."""

  success: NotRequired[bool]
  """Whether the subscription succeeded."""
  bfusdAmount: NotRequired[str]
  """Amount of BFUSD received, as a decimal string."""


class Request(TypedDict):
  """Request parameters."""

  asset: str
  """Asset to subscribe with, e.g. `USDT`. No documented closed set for this field."""
  amount: float
  """Amount to subscribe with, in `asset`."""


class Subscribe(RpcEndpoint):
  """Subscribe BFUSD."""

  async def subscribe(
    self,
    asset: str,
    *,
    amount: float,
    validate: bool | None = None,
  ) -> BfusdSubscription:
    """Subscribe BFUSD.

    Args:
      asset: Asset to subscribe with, e.g. `USDT`. No documented closed set for this field.
      amount: Amount to subscribe with, in `asset`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-simple-earn/api/rest-api/bfusd#subscribe-bfusd)
    """
    return await self.request(
      Request(asset=asset, amount=amount),
      method='POST',
      path='/sapi/v1/bfusd/subscribe',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BfusdSubscription,
    )
