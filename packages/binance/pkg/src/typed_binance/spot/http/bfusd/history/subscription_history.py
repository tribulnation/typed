# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class BfusdSubscriptionHistoryEntry(TypedDict):
  """One BFUSD subscription."""

  time: NotRequired[TimestampMillis]
  """Millisecond epoch time the subscription was recorded."""
  asset: NotRequired[Literal['USDC', 'USDT']]
  """Asset subscribed with."""
  amount: NotRequired[str]
  """Subscribed amount, as a decimal string."""
  receiveAsset: NotRequired[str]
  """Asset credited to the account (always BFUSD)."""
  receiveAmount: NotRequired[str]
  """Amount credited to the account, as a decimal string."""
  status: NotRequired[str]
  """Subscription status."""


class Request(TypedDict):
  """Request parameters."""

  asset: NotRequired[Literal['USDC', 'USDT']]
  """Restrict results to this asset."""
  startTime: NotRequired[TimestampMillis]
  """Millisecond epoch start of the queried window."""
  endTime: NotRequired[TimestampMillis]
  """Millisecond epoch end of the queried window."""
  current: NotRequired[int]
  """Currently querying page. Starts from 1."""
  size: NotRequired[int]
  """Number of results per page."""


class BfusdSubscriptionHistoryPage(TypedDict):
  """One page of this account's BFUSD subscription history."""

  rows: NotRequired[list[BfusdSubscriptionHistoryEntry]]
  """Matching records on this page."""
  total: NotRequired[int]
  """Total number of matching records."""


class SubscriptionHistory(RpcEndpoint):
  """Get this account's BFUSD subscription history, sorted descending by time."""

  def subscription_history_paged(
    self,
    asset: Literal['USDC', 'USDT'] | None = None,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    size: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[BfusdSubscriptionHistoryEntry, int]:
    """Get this account's BFUSD subscription history, sorted descending by time.

    Paged variant of `subscription_history`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      asset: Restrict results to this asset.
      start_time: Millisecond epoch start of the queried window.
      end_time: Millisecond epoch end of the queried window.
      size: Number of results per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-simple-earn/api/rest-api/bfusd#get-bfusd-subscription-history)
    """

    async def next(
      current: int,
    ) -> tuple[list[BfusdSubscriptionHistoryEntry], int | None]:
      response = await self.subscription_history(
        asset,
        start_time=start_time,
        end_time=end_time,
        size=size,
        current=current,
        validate=validate,
      )
      rows = response.get('rows')
      rows = rows if rows is not None else []
      total = response.get('total')
      total = int(total) if total is not None else None
      if total is None or size is None or (current - 1 + 1) * size >= total:
        return rows, None
      return rows, current + 1

    return PaginatedResponse(1, next)

  async def subscription_history(
    self,
    asset: Literal['USDC', 'USDT'] | None = None,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    current: int | None = None,
    size: int | None = None,
    validate: bool | None = None,
  ) -> BfusdSubscriptionHistoryPage:
    """Get this account's BFUSD subscription history, sorted descending by time.

    Args:
      asset: Restrict results to this asset.
      start_time: Millisecond epoch start of the queried window.
      end_time: Millisecond epoch end of the queried window.
      current: Currently querying page. Starts from 1.
      size: Number of results per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-simple-earn/api/rest-api/bfusd#get-bfusd-subscription-history)
    """
    request: Request = Request()
    if asset is not None:
      request['asset'] = asset
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if current is not None:
      request['current'] = current
    if size is not None:
      request['size'] = size
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/bfusd/history/subscriptionHistory',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BfusdSubscriptionHistoryPage,
    )
