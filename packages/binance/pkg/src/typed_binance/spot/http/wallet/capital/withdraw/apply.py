# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class WithdrawResult(TypedDict):
  """Result of submitting a withdrawal."""

  id: str
  """Withdrawal id, usable to look this withdrawal up later via `capital.withdraw.history`."""


class Apply(RpcEndpoint):
  """Submit a withdrawal request. If `network` is omitted, the coin's default network is used; see `capital.config.get_all`'s `networkList[].isDefault`. Whether `addressTag` may or must be sent depends on the destination network's `networkList[].withdrawTag`, also from `capital.config.get_all`: when `true`, a memo/tag is required and must be sent as `addressTag`; when `false`, the network does not support one and sending `addressTag` anyway raises error -4106 (`TAG_NOT_SUPPORTED_FOR_NETWORK`). If travel rule applies to the destination (check `GET /sapi/v1/localentity/questionnaire-requirements`), this endpoint is not usable and `POST /sapi/v1/localentity/withdraw/apply` must be used instead."""

  async def apply(
    self,
    *,
    coin: str,
    withdraw_order_id: str | None = None,
    network: str | None = None,
    address: str,
    address_tag: str | None = None,
    amount: float,
    transaction_fee_flag: bool | None = None,
    name: str | None = None,
    wallet_type: Literal[0, 1] | None = None,
    validate: bool | None = None,
  ) -> WithdrawResult:
    """Submit a withdrawal request. If `network` is omitted, the coin's default network is used; see `capital.config.get_all`'s `networkList[].isDefault`. Whether `addressTag` may or must be sent depends on the destination network's `networkList[].withdrawTag`, also from `capital.config.get_all`: when `true`, a memo/tag is required and must be sent as `addressTag`; when `false`, the network does not support one and sending `addressTag` anyway raises error -4106 (`TAG_NOT_SUPPORTED_FOR_NETWORK`). If travel rule applies to the destination (check `GET /sapi/v1/localentity/questionnaire-requirements`), this endpoint is not usable and `POST /sapi/v1/localentity/withdraw/apply` must be used instead.

    Args:
      coin: Coin to withdraw.
      withdraw_order_id: Caller-supplied idempotency id for this withdrawal. Can be used to look this withdrawal up later via `capital.withdraw.history`.
      network: Network to withdraw over. Defaults to the coin's default network; see `capital.config.get_all`.
      address: Destination withdrawal address.
      address_tag: Secondary address identifier (memo/tag) for coins/networks that require one, e.g. XRP, XMR. Required when the destination network's `withdrawTag` (from `capital.config.get_all`) is `true`; must be omitted when it's `false`.
      amount: Amount to withdraw.
      transaction_fee_flag: For an internal transfer (to another Binance account): `true` returns the network fee to the destination account, `false` returns it to the departure account.
      name: Description of the address. Overwrites the address book's saved name for this address, if any.
      wallet_type: Wallet to withdraw from.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/capital#withdraw)
    """
    params: dict = {
      'coin': coin,
      'address': address,
      'amount': amount,
    }
    if withdraw_order_id is not None:
      params['withdrawOrderId'] = withdraw_order_id
    if network is not None:
      params['network'] = network
    if address_tag is not None:
      params['addressTag'] = address_tag
    if transaction_fee_flag is not None:
      params['transactionFeeFlag'] = transaction_fee_flag
    if name is not None:
      params['name'] = name
    if wallet_type is not None:
      params['walletType'] = wallet_type
    _Response = WithdrawResult
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/capital/withdraw/apply',
      params=params,
      validator=_validator,
      validate=validate,
    )
