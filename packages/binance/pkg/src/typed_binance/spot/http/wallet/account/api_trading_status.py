# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class AccountApiTradingStatusIndicator(TypedDict):
  """One indicator reading for a symbol."""

  i: str
  """Indicator name, e.g. "UFR" (Unfilled Ratio), "IFER", or "GCR"."""
  c: int
  """Count of all orders considered for this indicator."""
  v: float
  """Current value of the indicator."""
  t: float
  """Trigger value of the indicator; a lock is triggered when `v` reaches this."""


class AccountApiTradingStatusTriggerCondition(TypedDict):
  """Thresholds that would trigger an API trading lock."""

  GCR: int
  """Number of GTC (good-till-cancel) orders that would trigger a lock."""
  IFER: int
  """Number of FOK/IOC orders that would trigger a lock."""
  UFR: int
  """Number of orders (unfilled ratio) that would trigger a lock."""


class AccountApiTradingStatusData(TypedDict):
  """API trading status detail."""

  isLocked: bool
  """Whether the API trading function is currently locked."""
  plannedRecoverTime: TimestampMillis
  """If API trading is locked, the planned recovery time as milliseconds since epoch. 0 when not locked."""
  triggerCondition: AccountApiTradingStatusTriggerCondition
  indicators: NotRequired[dict[str, list[AccountApiTradingStatusIndicator]]]
  """Unfilled-ratio indicators updated every 30 seconds, keyed by trading symbol. Corrected to optional from live evidence: this account's real (unlocked, no unfilled-ratio history) response omitted the field entirely."""
  updateTime: TimestampMillis
  """Time this status was last updated, as milliseconds since epoch."""


class AccountApiTradingStatus(TypedDict):
  """Wrapper carrying the account's API trading status."""

  data: AccountApiTradingStatusData


class ApiTradingStatus(RpcEndpoint):
  """Fetch the account's API trading status, including whether API trading is currently locked and the unfilled-ratio indicators that would trigger a lock."""

  async def api_trading_status(
    self,
    *,
    validate: bool | None = None,
  ) -> AccountApiTradingStatus:
    """Fetch the account's API trading status, including whether API trading is currently locked and the unfilled-ratio indicators that would trigger a lock.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/account#account-api-trading-status)
    """
    return await self.request(
      None,
      method='GET',
      path='/sapi/v1/account/apiTradingStatus',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      response_type=AccountApiTradingStatus,
    )
