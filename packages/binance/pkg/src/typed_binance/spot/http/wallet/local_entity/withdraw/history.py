# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampIso, TimestampMillis, timestamp_millis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class LocalEntityWithdrawal(TypedDict):
  """One withdrawal's travel rule detail."""

  id: NotRequired[str]
  """Withdrawal record id."""
  trId: NotRequired[int]
  """Travel rule record id."""
  amount: NotRequired[str]
  """Withdrawal amount."""
  transactionFee: NotRequired[str]
  """Network fee charged for the withdrawal."""
  coin: NotRequired[str]
  """Withdrawal coin."""
  withdrawalStatus: NotRequired[int]
  """Withdrawal status code. Not enumerated on this endpoint's own documentation; see notes."""
  travelRuleStatus: NotRequired[Literal[0, 1, 2]]
  """Travel rule status: 0 Completed, 1 Pending, 2 Failed."""
  address: NotRequired[str]
  """Recipient address."""
  txId: NotRequired[str]
  """Transaction hash."""
  applyTime: NotRequired[TimestampIso]
  """Time the withdrawal was requested."""
  network: NotRequired[str]
  """Withdrawal network. May be absent for older withdrawals."""
  transferType: NotRequired[int]
  """Transfer type code. Not enumerated on this endpoint's own documentation; see notes."""
  withdrawOrderId: NotRequired[str]
  """Caller-supplied withdraw order id, if one was provided at submission time."""
  info: NotRequired[str]
  """Status message."""
  confirmNo: NotRequired[int]
  """On-chain confirmation count."""
  walletType: NotRequired[Literal[0, 1]]
  """Wallet the withdrawal was made from: 0 spot wallet, 1 funding wallet."""
  txKey: NotRequired[str]
  """Transaction key."""
  questionnaire: NotRequired[str]
  """JSON-formatted questionnaire answers submitted with this withdrawal."""
  completeTime: NotRequired[TimestampIso]
  """Time the withdrawal completed."""


class History(RpcEndpoint):
  """Fetch withdraw history for local entities that require travel rule."""

  async def history(
    self,
    *,
    tr_id: str | None = None,
    tx_id: str | None = None,
    withdraw_order_id: str | None = None,
    network: str | None = None,
    coin: str | None = None,
    travel_rule_status: Literal[0, 1, 2] | None = None,
    offset: int | None = None,
    limit: int | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    validate: bool | None = None,
  ) -> list[LocalEntityWithdrawal]:
    """Fetch withdraw history for local entities that require travel rule.

    Args:
      tr_id: Comma-separated list of travel rule record ids to filter by.
      tx_id: Comma-separated list of transaction ids to filter by.
      withdraw_order_id: Client-side id for the withdrawal, if one was provided in spot.wallet.capital.withdraw.apply or spot.wallet.local_entity.withdraw.apply.
      network: Network to filter by.
      coin: Coin to filter by.
      travel_rule_status: Travel rule status to filter by: 0 Completed, 1 Pending, 2 Failed.
      offset: Number of records to skip before the returned page.
      limit: Maximum number of records to return.
      start_time: Start of the query window, as milliseconds since epoch. Defaults to 90 days before the current time.
      end_time: End of the query window, as milliseconds since epoch. Defaults to the current time.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/travel-rule#withdraw-history-v1)
    """
    params = {}
    if tr_id is not None:
      params['trId'] = tr_id
    if tx_id is not None:
      params['txId'] = tx_id
    if withdraw_order_id is not None:
      params['withdrawOrderId'] = withdraw_order_id
    if network is not None:
      params['network'] = network
    if coin is not None:
      params['coin'] = coin
    if travel_rule_status is not None:
      params['travelRuleStatus'] = travel_rule_status
    if offset is not None:
      params['offset'] = offset
    if limit is not None:
      params['limit'] = limit
    if start_time is not None:
      params['startTime'] = timestamp_millis.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp_millis.dump(end_time)
    _Response = list[LocalEntityWithdrawal]
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/sapi/v1/localentity/withdraw/history',
      params=params,
      validator=_validator,
      validate=validate,
    )
