# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class DelistSchedule(TypedDict):
  """Symbols scheduled to be delisted at the same time."""

  delistTime: TimestampMillis
  """Scheduled delisting time."""
  symbols: list[str]
  """Symbols scheduled to be delisted at `delistTime`."""


class Request(TypedDict):
  """Request parameters."""

  recvWindow: NotRequired[int]
  """Number of milliseconds the request is valid for after `timestamp`. Cannot exceed 60000."""


Response = list[DelistSchedule]


class DelistScheduleEndpoint(RpcEndpoint):
  """Fetch upcoming spot symbol delistings, grouped by delisting time."""

  async def delist_schedule(
    self,
    recv_window: int | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Fetch upcoming spot symbol delistings, grouped by delisting time.

    Args:
      recv_window: Number of milliseconds the request is valid for after `timestamp`. Cannot exceed 60000.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/others#get-symbols-delist-schedule-for-spot)
    """
    request: Request = Request()
    if recv_window is not None:
      request['recvWindow'] = recv_window
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/spot/delist-schedule',
      meta={'security': 'MARKET_DATA', 'signed': False},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
