# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class LocalEntityDepositV2Questionnaire(TypedDict):
  """Submitted questionnaire summary, present only when `retrieveQuestionnaire` was sent as true."""

  vaspName: NotRequired[str]
  """Name of the VASP that originated the deposit."""
  depositOriginator: NotRequired[int]
  """Deposit originator code."""


class Request(TypedDict):
  """Request parameters."""

  depositId: NotRequired[str]
  """Comma-separated list of wallet deposit ids to filter by. Documented as an integer type upstream despite the comma-separated-list description; specced as a string here — see notes."""
  txId: NotRequired[str]
  """Comma-separated list of transaction ids to filter by."""
  network: NotRequired[str]
  """Network to filter by."""
  coin: NotRequired[str]
  """Coin to filter by."""
  retrieveQuestionnaire: NotRequired[bool]
  """true: include the `questionnaire` field in the response."""
  startTime: NotRequired[TimestampMillis]
  """Start of the query window, as milliseconds since epoch. Defaults to 90 days before the current time."""
  endTime: NotRequired[TimestampMillis]
  """End of the query window, as milliseconds since epoch. Defaults to the current time."""
  offset: NotRequired[int]
  """Number of records to skip before the returned page."""
  limit: NotRequired[int]
  """Maximum number of records to return."""


class LocalEntityDepositV2(TypedDict):
  """One deposit's travel rule detail (v2)."""

  depositId: NotRequired[str]
  """Wallet deposit id."""
  amount: NotRequired[str]
  """Deposit amount."""
  network: NotRequired[str]
  """Deposit network."""
  coin: NotRequired[str]
  """Deposit coin."""
  depositStatus: NotRequired[int]
  """Deposit status code. Not enumerated on this endpoint's own documentation; see notes."""
  travelRuleReqStatus: NotRequired[int]
  """Travel rule requirement status code. Not enumerated on this endpoint's own documentation."""
  address: NotRequired[str]
  """Deposit address."""
  addressTag: NotRequired[str]
  """Secondary address identifier, for coins like XRP, XMR, etc."""
  txId: NotRequired[str]
  """Transaction hash."""
  transferType: NotRequired[int]
  """Transfer type code. Not enumerated on this endpoint's own documentation."""
  confirmTimes: NotRequired[str]
  """On-chain confirmation count, as `<current>/<required>`."""
  requireQuestionnaire: NotRequired[bool]
  """Whether this deposit requires a travel rule questionnaire to be submitted."""
  questionnaire: NotRequired[LocalEntityDepositV2Questionnaire | None]
  """Submitted questionnaire summary, present only when `retrieveQuestionnaire` was sent as true."""
  insertTime: NotRequired[TimestampMillis]
  """Timestamp the record was created."""


Response = list[LocalEntityDepositV2]


class HistoryV2(RpcEndpoint):
  """Fetch deposit history for local entities that require travel rule information (v2, addressed by deposit id rather than wallet transaction id, with an optional embedded questionnaire)."""

  async def history_v2(
    self,
    *,
    deposit_id: str | None = None,
    tx_id: str | None = None,
    network: str | None = None,
    coin: str | None = None,
    retrieve_questionnaire: bool | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    offset: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Fetch deposit history for local entities that require travel rule information (v2, addressed by deposit id rather than wallet transaction id, with an optional embedded questionnaire).

    Args:
      deposit_id: Comma-separated list of wallet deposit ids to filter by. Documented as an integer type upstream despite the comma-separated-list description; specced as a string here — see notes.
      tx_id: Comma-separated list of transaction ids to filter by.
      network: Network to filter by.
      coin: Coin to filter by.
      retrieve_questionnaire: true: include the `questionnaire` field in the response.
      start_time: Start of the query window, as milliseconds since epoch. Defaults to 90 days before the current time.
      end_time: End of the query window, as milliseconds since epoch. Defaults to the current time.
      offset: Number of records to skip before the returned page.
      limit: Maximum number of records to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/travel-rule#deposit-history-v2)
    """
    request: Request = Request()
    if deposit_id is not None:
      request['depositId'] = deposit_id
    if tx_id is not None:
      request['txId'] = tx_id
    if network is not None:
      request['network'] = network
    if coin is not None:
      request['coin'] = coin
    if retrieve_questionnaire is not None:
      request['retrieveQuestionnaire'] = retrieve_questionnaire
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if offset is not None:
      request['offset'] = offset
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/sapi/v2/localentity/deposit/history',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
