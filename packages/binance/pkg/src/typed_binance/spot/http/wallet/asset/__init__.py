# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .bnb_burn import BnbBurn
from .cloud_mining_history import CloudMiningHistoryEndpoint
from .custody_transfer_history import CustodyTransferHistory
from .detail import Detail
from .dividend import Dividend
from .dust import Dust
from .dust_btc import DustBtc
from .dust_convert import DustConvert
from .funding_wallet import FundingWallet
from .open_symbol_list import OpenSymbolList
from .tags import Tags
from .trade_fee import TradeFeeEndpoint
from .transfer import Transfer
from .user_asset import UserAssetEndpoint
from .wallet_balance import WalletBalanceEndpoint


class Asset(
  CloudMiningHistoryEndpoint,
  CustodyTransferHistory,
  Detail,
  Dividend,
  DustBtc,
  FundingWallet,
  OpenSymbolList,
  Tags,
  TradeFeeEndpoint,
  UserAssetEndpoint,
  WalletBalanceEndpoint,
):
  """Wallet's Asset endpoints -- dust conversion, dividend records, trade fees, funding/universal transfers, and per-asset balance/detail lookups.

  References:
    - [Upstream docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/asset)
  """

  @cached_property
  def bnb_burn(self) -> BnbBurn:
    """Wallet Asset's BNB Burn endpoints -- toggling and querying the BNB spot/margin trading fee discount setting. No page of its own -- reusing Wallet's Asset catalog page, which covers this endpoint too.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/asset)
    """
    return BnbBurn(client=self.client)

  @cached_property
  def dust(self) -> Dust:
    """Wallet Asset's Dust endpoints -- converting small asset balances to BNB and fetching that conversion's log history. No page of its own -- reusing Wallet's Asset catalog page, which covers these endpoints too.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/asset)
    """
    return Dust(client=self.client)

  @cached_property
  def dust_convert(self) -> DustConvert:
    """Wallet Asset's Dust-Convert endpoints -- submitting a small-balance-to-BNB conversion and listing which assets are eligible for it. No page of its own -- reusing Wallet's Asset catalog page, which covers these endpoints too.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/asset)
    """
    return DustConvert(client=self.client)

  @cached_property
  def transfer(self) -> Transfer:
    """Wallet Asset's Transfer endpoints -- submitting a universal/funding transfer between account types and fetching its history. No page of its own -- reusing Wallet's Asset catalog page, which covers these endpoints too.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/asset)
    """
    return Transfer(client=self.client)
