# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Request parameters."""

  asset: NotRequired[str]
  """Asset to query. If blank, every asset with a positive balance is returned."""
  needBtcValuation: NotRequired[Literal['true', 'false']]
  """Whether to include each asset's BTC valuation."""


class UserAsset(TypedDict):
  """Spot wallet balance of a single asset."""

  asset: str
  """Asset symbol."""
  free: Decimal
  """Available (unlocked) balance."""
  locked: Decimal
  """Locked balance."""
  freeze: Decimal
  """Frozen balance."""
  withdrawing: Decimal
  """Balance currently being withdrawn."""
  ipoable: Decimal
  """Balance reserved for IPO subscription."""
  btcValuation: Decimal
  """Total balance, denominated in BTC. `"0"` unless `needBtcValuation` is `true`."""


UserAssets = list[UserAsset]


class UserAssetEndpoint(RpcEndpoint):
  """Fetch the account's Spot wallet balances. Only assets with a positive balance are returned unless `asset` is set."""

  async def user_asset(
    self,
    asset: str | None = None,
    *,
    need_btc_valuation: Literal['true', 'false'] | None = None,
    validate: bool | None = None,
  ) -> UserAssets:
    """Fetch the account's Spot wallet balances. Only assets with a positive balance are returned unless `asset` is set.

    Args:
      asset: Asset to query. If blank, every asset with a positive balance is returned.
      need_btc_valuation: Whether to include each asset's BTC valuation.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/asset#user-asset)
    """
    request: Request = Request()
    if asset is not None:
      request['asset'] = asset
    if need_btc_valuation is not None:
      request['needBtcValuation'] = need_btc_valuation
    return await self.request(
      request,
      method='POST',
      path='/sapi/v3/asset/getUserAsset',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=UserAssets,
    )
