# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class SystemStatus(TypedDict):
  """Binance's current system status."""

  status: Literal[0, 1]
  """System status code."""
  msg: Literal['normal', 'system_maintenance']
  """Human-readable system status."""


class SystemStatusEndpoint(RpcEndpoint):
  """Fetch Binance's system status. Public: no API key or signature required."""

  async def system_status(self, *, validate: bool | None = None) -> SystemStatus:
    """Fetch Binance's system status. Public: no API key or signature required.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/others#system-status)
    """
    return await self.request(
      None,
      method='GET',
      path='/sapi/v1/system/status',
      meta={'security': 'System', 'signed': False},
      validate=validate,
      response_type=SystemStatus,
    )
