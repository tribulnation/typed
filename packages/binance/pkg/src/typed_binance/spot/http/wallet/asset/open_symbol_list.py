# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class OpenSymbolBatch(TypedDict):
  """Symbols scheduled to open for trading at a single time."""

  openTime: TimestampMillis
  """Scheduled opening time, as milliseconds since epoch."""
  symbols: list[str]
  """Symbols opening at `openTime`."""


OpenSymbolBatches = list[OpenSymbolBatch]


class OpenSymbolList(RpcEndpoint):
  """Fetch the list of symbols scheduled to open for Spot trading, grouped by their scheduled opening time."""

  async def open_symbol_list(
    self, *, validate: bool | None = None
  ) -> OpenSymbolBatches:
    """Fetch the list of symbols scheduled to open for Spot trading, grouped by their scheduled opening time.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/asset#get-open-symbol-list)
    """
    return await self.request(
      None,
      method='GET',
      path='/sapi/v1/spot/open-symbol-list',
      meta={'security': 'MARKET_DATA', 'signed': False},
      validate=validate,
      response_type=OpenSymbolBatches,
    )
