# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class LocalEntityDeposit(TypedDict):
  """One deposit's travel rule detail."""

  trId: NotRequired[int]
  """Travel rule record id."""
  tranId: NotRequired[int]
  """Wallet transaction id."""
  amount: NotRequired[str]
  """Deposit amount."""
  coin: NotRequired[str]
  """Deposit coin."""
  network: NotRequired[str]
  """Deposit network."""
  depositStatus: NotRequired[int]
  """Deposit status code. Not enumerated on this endpoint's own documentation; see notes."""
  travelRuleStatus: NotRequired[Literal[0, 1, 2]]
  """Travel rule status: 0 Completed, 1 Pending, 2 Failed."""
  travelRuleStatusV2: NotRequired[Literal['PASSED', 'PENDING', 'REJECTED']]
  """Overall travel rule verification status, combining GTR and sanctions screening. PASSED: verification complete, deposit released. PENDING: verification in progress, deposit frozen. REJECTED: verification failed."""
  address: NotRequired[str]
  """Deposit address. Due to network-specific characteristics the returned source address may be inaccurate; if multiple source addresses are found, only the first is returned."""
  addressTag: NotRequired[str]
  """Secondary address identifier, for coins like XRP, XMR, etc."""
  txId: NotRequired[str]
  """Transaction hash."""
  insertTime: NotRequired[TimestampMillis]
  """Timestamp the record was created."""
  completeTime: NotRequired[TimestampMillis]
  """Timestamp the deposit was fully completed and credited."""
  transferType: NotRequired[int]
  """Transfer type code. Not enumerated on this endpoint's own documentation; see notes."""
  confirmTimes: NotRequired[str]
  """On-chain confirmation count, as `<current>/<required>`."""
  requireQuestionnaire: NotRequired[bool]
  """Whether this deposit requires a travel rule questionnaire to be submitted."""
  questionnaire: NotRequired[str]
  """JSON-formatted questionnaire answers already submitted for this deposit, if any."""


class Request(TypedDict):
  """Request parameters."""

  trId: NotRequired[str]
  """Comma-separated list of travel rule record ids to filter by."""
  txId: NotRequired[str]
  """Comma-separated list of transaction ids to filter by."""
  tranId: NotRequired[str]
  """Comma-separated list of wallet transaction ids to filter by."""
  network: NotRequired[str]
  """Network to filter by."""
  coin: NotRequired[str]
  """Coin to filter by."""
  travelRuleStatus: NotRequired[Literal[0, 1, 2]]
  """Travel rule status to filter by: 0 Completed, 1 Pending, 2 Failed."""
  pendingQuestionnaire: NotRequired[bool]
  """true: only return records with a pending deposit questionnaire. false or omitted: return all records."""
  startTime: NotRequired[TimestampMillis]
  """Start of the query window, as milliseconds since epoch. Defaults to 90 days before the current time."""
  endTime: NotRequired[TimestampMillis]
  """End of the query window, as milliseconds since epoch. Defaults to the current time."""
  offset: NotRequired[int]
  """Number of records to skip before the returned page."""
  limit: NotRequired[int]
  """Maximum number of records to return."""


Response = list[LocalEntityDeposit]


class History(RpcEndpoint):
  """Fetch deposit history for local entities that require travel rule."""

  async def history(
    self,
    *,
    tr_id: str | None = None,
    tx_id: str | None = None,
    tran_id: str | None = None,
    network: str | None = None,
    coin: str | None = None,
    travel_rule_status: Literal[0, 1, 2] | None = None,
    pending_questionnaire: bool | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    offset: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Fetch deposit history for local entities that require travel rule.

    Args:
      tr_id: Comma-separated list of travel rule record ids to filter by.
      tx_id: Comma-separated list of transaction ids to filter by.
      tran_id: Comma-separated list of wallet transaction ids to filter by.
      network: Network to filter by.
      coin: Coin to filter by.
      travel_rule_status: Travel rule status to filter by: 0 Completed, 1 Pending, 2 Failed.
      pending_questionnaire: true: only return records with a pending deposit questionnaire. false or omitted: return all records.
      start_time: Start of the query window, as milliseconds since epoch. Defaults to 90 days before the current time.
      end_time: End of the query window, as milliseconds since epoch. Defaults to the current time.
      offset: Number of records to skip before the returned page.
      limit: Maximum number of records to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/travel-rule#deposit-history-travel-rule)
    """
    request: Request = Request()
    if tr_id is not None:
      request['trId'] = tr_id
    if tx_id is not None:
      request['txId'] = tx_id
    if tran_id is not None:
      request['tranId'] = tran_id
    if network is not None:
      request['network'] = network
    if coin is not None:
      request['coin'] = coin
    if travel_rule_status is not None:
      request['travelRuleStatus'] = travel_rule_status
    if pending_questionnaire is not None:
      request['pendingQuestionnaire'] = pending_questionnaire
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if offset is not None:
      request['offset'] = offset
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/localentity/deposit/history',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
