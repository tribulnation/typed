# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class LocalEntityCountry(TypedDict):
  """One country's travel rule support status."""

  countryCode: NotRequired[str]
  """ISO 2-digit country code, lowercase."""
  countryName: NotRequired[str]
  """Country display name."""
  blockType: NotRequired[Literal['supported', 'limited', 'blocked']]
  """Country's support status for travel rule transactions."""
  depositAllowed: NotRequired[bool]
  """Whether deposit is allowed for this country."""
  withdrawalAllowed: NotRequired[bool]
  """Whether withdrawal is allowed for this country."""
  hasRegionRestrictions: NotRequired[bool]
  """Whether this country has region-level restrictions (see `spot.wallet.local_entity.region_list`)."""


class LocalEntityCountryList(TypedDict):
  """Countries in scope for travel rule questionnaires, and when the list was last refreshed."""

  countries: list[LocalEntityCountry]
  """Countries in scope for travel rule questionnaires."""
  lastUpdated: TimestampMillis
  """Last data update timestamp; 0 if the list is empty."""


class CountryList(RpcEndpoint):
  """Query the active country list for travel rule questionnaires. Currently only supports the AU (Australia) local entity."""

  async def country_list(
    self, *, validate: bool | None = None
  ) -> LocalEntityCountryList:
    """Query the active country list for travel rule questionnaires. Currently only supports the AU (Australia) local entity.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/travel-rule#get-country-list)
    """
    return await self.request(
      None,
      method='GET',
      path='/sapi/v1/localentity/country/list',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      response_type=LocalEntityCountryList,
    )
