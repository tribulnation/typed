# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .address_verify_list import AddressVerifyList
from .broker import Broker
from .country_list import CountryList
from .deposit import Deposit
from .questionnaire_requirements import QuestionnaireRequirementsEndpoint
from .region_list import RegionList
from .vasp_list import VaspList
from .withdraw import Withdraw


class LocalEntity(
  AddressVerifyList,
  CountryList,
  QuestionnaireRequirementsEndpoint,
  RegionList,
  VaspList,
):
  """Wallet's Travel Rule endpoints -- deposit/withdrawal submission and history for local entities under travel-rule compliance (questionnaires, VASP info, and supported country/region lists).

  References:
    - [Upstream docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/travel-rule)
  """

  @cached_property
  def broker(self) -> Broker:
    """Wallet Travel Rule's Broker endpoints -- providing deposit VASP/travel-rule information and submitting withdrawals on behalf of broker sub-accounts. No page of its own -- reusing Wallet's Travel Rule catalog page, which covers these endpoints too.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/travel-rule)
    """
    return Broker(client=self.client)

  @cached_property
  def deposit(self) -> Deposit:
    """Wallet Travel Rule's Deposit endpoints -- deposit history and submitting the required travel-rule questionnaire/VASP info for a deposit, across both API versions. No page of its own -- reusing Wallet's Travel Rule catalog page, which covers these endpoints too.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/travel-rule)
    """
    return Deposit(client=self.client)

  @cached_property
  def withdraw(self) -> Withdraw:
    """Wallet Travel Rule's Withdraw endpoints -- submitting a withdrawal with the required travel-rule questionnaire/VASP info and its history, across both API versions. No page of its own -- reusing Wallet's Travel Rule catalog page, which covers these endpoints too.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/travel-rule)
    """
    return Withdraw(client=self.client)
