# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint
from typed_binance.schemas import BnbBurnStatus


class Request(TypedDict):
  """Request parameters."""

  spotBNBBurn: NotRequired[Literal['true', 'false']]
  """Whether to use BNB to pay Spot trading fees."""
  interestBNBBurn: NotRequired[Literal['true', 'false']]
  """Whether to use BNB to pay Margin loan interest."""


class Toggle(RpcEndpoint):
  """Toggle whether BNB Burn is used to pay Spot trading fees and/or Margin loan interest. At least one of `spotBNBBurn`/`interestBNBBurn` must be sent."""

  async def toggle(
    self,
    *,
    spot_bnbburn: Literal['true', 'false'] | None = None,
    interest_bnbburn: Literal['true', 'false'] | None = None,
    validate: bool | None = None,
  ) -> BnbBurnStatus:
    """Toggle whether BNB Burn is used to pay Spot trading fees and/or Margin loan interest. At least one of `spotBNBBurn`/`interestBNBBurn` must be sent.

    Args:
      spot_bnbburn: Whether to use BNB to pay Spot trading fees.
      interest_bnbburn: Whether to use BNB to pay Margin loan interest.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/asset#toggle-bnb-burn-on-spot-trade-and-margin-interest)
    """
    request: Request = Request()
    if spot_bnbburn is not None:
      request['spotBNBBurn'] = spot_bnbburn
    if interest_bnbburn is not None:
      request['interestBNBBurn'] = interest_bnbburn
    return await self.request(
      request,
      method='POST',
      path='/sapi/v1/bnbBurn',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BnbBurnStatus,
    )
