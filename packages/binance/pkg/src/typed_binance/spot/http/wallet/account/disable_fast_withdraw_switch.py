# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any
from typed_binance.core.endpoint.rpc import RpcEndpoint


Ack = dict[str, Any]


class DisableFastWithdrawSwitch(RpcEndpoint):
  """Disable the account's Fast Withdraw Switch. Requires the API key used to have the "trade" permission enabled."""

  async def disable_fast_withdraw_switch(self, *, validate: bool | None = None) -> Ack:
    """Disable the account's Fast Withdraw Switch. Requires the API key used to have the "trade" permission enabled.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/account#disable-fast-withdraw-switch)
    """
    return await self.request(
      None,
      method='POST',
      path='/sapi/v1/account/disableFastWithdrawSwitch',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      response_type=Ack,
    )
