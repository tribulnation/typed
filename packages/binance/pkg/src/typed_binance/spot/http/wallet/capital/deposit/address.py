# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class DepositAddress(TypedDict):
  """A deposit address for one coin on one network."""

  address: str
  """Deposit address."""
  coin: str
  """Coin this address accepts."""
  tag: str
  """Memo/tag to include with the deposit. Empty when the network does not use one."""
  url: str
  """Block-explorer URL for this address."""


class Request(TypedDict):
  """Request parameters."""

  coin: str
  """Coin to fetch a deposit address for."""
  network: NotRequired[str]
  """Network to fetch the deposit address on. Defaults to the coin's default network; see `capital.config.get_all`."""
  amount: NotRequired[float]
  """Deposit amount. Required when `network` is `LIGHTNING`."""


class Address(RpcEndpoint):
  """Fetch a deposit address for a coin, on a given network. If `network` is omitted, the coin's default network is used; see `capital.config.get_all`'s `networkList[].isDefault`."""

  async def address(
    self,
    *,
    coin: str,
    network: str | None = None,
    amount: float | None = None,
    validate: bool | None = None,
  ) -> DepositAddress:
    """Fetch a deposit address for a coin, on a given network. If `network` is omitted, the coin's default network is used; see `capital.config.get_all`'s `networkList[].isDefault`.

    Args:
      coin: Coin to fetch a deposit address for.
      network: Network to fetch the deposit address on. Defaults to the coin's default network; see `capital.config.get_all`.
      amount: Deposit amount. Required when `network` is `LIGHTNING`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/capital#deposit-address)
    """
    request: Request = Request(coin=coin)
    if network is not None:
      request['network'] = network
    if amount is not None:
      request['amount'] = amount
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/capital/deposit/address',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=DepositAddress,
    )
