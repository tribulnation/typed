# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .account import Account
from .asset import Asset
from .capital import Capital
from .local_entity import LocalEntity
from .others import Others
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Wallet(RpcEndpoint):
  """Wallet -- account/asset/capital/travel-rule/others endpoints for balances, asset management, on-chain deposit/withdrawal, and wallet configuration, umbrella'd under Binance's own Wallet API docs.

  References:
    - [Upstream docs](https://developers.binance.com/docs/wallet/Introduction)
  """

  @cached_property
  def account(self) -> Account:
    """Wallet's Account endpoints -- account status, API trading status, API key permissions, account info, the daily account snapshot, and the Fast Withdraw Switch toggle.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/account)
    """
    return Account(client=self.client)

  @cached_property
  def asset(self) -> Asset:
    """Wallet's Asset endpoints -- dust conversion, dividend records, trade fees, funding/universal transfers, and per-asset balance/detail lookups.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/asset)
    """
    return Asset(client=self.client)

  @cached_property
  def capital(self) -> Capital:
    """Wallet's Capital endpoints -- on-chain deposit and withdrawal: addresses, submitting a withdrawal, deposit/withdraw history, and network configuration.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/capital)
    """
    return Capital(client=self.client)

  @cached_property
  def local_entity(self) -> LocalEntity:
    """Wallet's Travel Rule endpoints -- deposit/withdrawal submission and history for local entities under travel-rule compliance (questionnaires, VASP info, and supported country/region lists).

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/travel-rule)
    """
    return LocalEntity(client=self.client)

  @cached_property
  def others(self) -> Others:
    """Wallet's miscellaneous endpoints -- upcoming spot symbol delisting schedule and Binance's overall system status.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/others)
    """
    return Others(client=self.client)
