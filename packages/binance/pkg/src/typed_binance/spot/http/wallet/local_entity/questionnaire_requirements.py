# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class QuestionnaireRequirements(TypedDict):
  """The local entity's travel rule requirement for the current API key's account."""

  questionnaireCountryCode: NotRequired[str]
  """ISO 2-digit country code of the local entity whose travel rule questionnaire applies to this account, uppercase (e.g. "AE"). The rendered docs' own example is a real country code; the SDK's own consumer, `spot.wallet.capital.withdraw.apply`'s documentation, treats a non-"NIL" value here as meaning withdrawals for this account must route through `spot.wallet.local_entity.withdraw.apply` instead of the plain capital withdraw endpoint — see notes."""


class QuestionnaireRequirementsEndpoint(RpcEndpoint):
  """Return user-specific travel rule questionnaire requirement information for the local entity, if any, tied to the current API key."""

  async def questionnaire_requirements(
    self,
    *,
    validate: bool | None = None,
  ) -> QuestionnaireRequirements:
    """Return user-specific travel rule questionnaire requirement information for the local entity, if any, tied to the current API key.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/travel-rule#check-questionnaire-requirements)
    """
    return await self.request(
      None,
      method='GET',
      path='/sapi/v1/localentity/questionnaire-requirements',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      response_type=QuestionnaireRequirements,
    )
