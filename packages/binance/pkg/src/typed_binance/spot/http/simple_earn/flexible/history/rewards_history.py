# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class FlexibleRewardsHistoryEntry(TypedDict):
  """One flexible product reward payment."""

  asset: str
  """Asset the reward was paid in."""
  rewards: str
  """Reward amount, as a decimal string."""
  projectId: str
  """Flexible product identifier the reward accrued on."""
  type: str
  """Reward type."""
  time: TimestampMillis
  """Millisecond epoch time the reward was recorded."""


class Request(TypedDict):
  """Request parameters."""

  productId: NotRequired[str]
  """Restrict results to this flexible product."""
  asset: NotRequired[str]
  """Restrict results to this underlying asset."""
  startTime: NotRequired[TimestampMillis]
  """Millisecond epoch start of the queried window."""
  endTime: NotRequired[TimestampMillis]
  """Millisecond epoch end of the queried window."""
  type: Literal['BONUS', 'REALTIME', 'REWARDS', 'ALL']
  """Restrict results to this reward type. Documented as optional, defaulting to `ALL`, but live evidence (2026-08-09) shows the venue rejects a call omitting it with `-1102 Mandatory parameter 'type' was not sent`; `ALL` must be sent explicitly to get the documented default behavior."""
  current: NotRequired[int]
  """Currently querying page. Starts from 1."""
  size: NotRequired[int]
  """Number of results per page."""


class FlexibleRewardsHistoryPage(TypedDict):
  """One page of this account's flexible product rewards history."""

  rows: list[FlexibleRewardsHistoryEntry]
  """Matching records on this page."""
  total: int
  """Total number of matching records."""


class RewardsHistory(RpcEndpoint):
  """Get this account's Simple Earn flexible product rewards history."""

  def rewards_history_paged(
    self,
    *,
    product_id: str | None = None,
    asset: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    type: Literal['BONUS', 'REALTIME', 'REWARDS', 'ALL'],
    size: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[FlexibleRewardsHistoryEntry, int]:
    """Get this account's Simple Earn flexible product rewards history.

    Paged variant of `rewards_history`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      product_id: Restrict results to this flexible product.
      asset: Restrict results to this underlying asset.
      start_time: Millisecond epoch start of the queried window.
      end_time: Millisecond epoch end of the queried window.
      type: Restrict results to this reward type. Documented as optional, defaulting to `ALL`, but live evidence (2026-08-09) shows the venue rejects a call omitting it with `-1102 Mandatory parameter 'type' was not sent`; `ALL` must be sent explicitly to get the documented default behavior.
      size: Number of results per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-simple-earn/api/rest-api/flexible-locked#get-flexible-rewards-history)
    """

    async def next(
      current: int,
    ) -> tuple[list[FlexibleRewardsHistoryEntry], int | None]:
      response = await self.rewards_history(
        product_id=product_id,
        asset=asset,
        start_time=start_time,
        end_time=end_time,
        type=type,
        size=size,
        current=current,
        validate=validate,
      )
      rows = response.get('rows')
      rows = rows if rows is not None else []
      total = response.get('total')
      total = int(total) if total is not None else None
      if total is None or size is None or (current - 1 + 1) * size >= total:
        return rows, None
      return rows, current + 1

    return PaginatedResponse(1, next)

  async def rewards_history(
    self,
    *,
    product_id: str | None = None,
    asset: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    type: Literal['BONUS', 'REALTIME', 'REWARDS', 'ALL'],
    current: int | None = None,
    size: int | None = None,
    validate: bool | None = None,
  ) -> FlexibleRewardsHistoryPage:
    """Get this account's Simple Earn flexible product rewards history.

    Args:
      product_id: Restrict results to this flexible product.
      asset: Restrict results to this underlying asset.
      start_time: Millisecond epoch start of the queried window.
      end_time: Millisecond epoch end of the queried window.
      type: Restrict results to this reward type. Documented as optional, defaulting to `ALL`, but live evidence (2026-08-09) shows the venue rejects a call omitting it with `-1102 Mandatory parameter 'type' was not sent`; `ALL` must be sent explicitly to get the documented default behavior.
      current: Currently querying page. Starts from 1.
      size: Number of results per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-simple-earn/api/rest-api/flexible-locked#get-flexible-rewards-history)
    """
    request: Request = Request(type=type)
    if product_id is not None:
      request['productId'] = product_id
    if asset is not None:
      request['asset'] = asset
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if current is not None:
      request['current'] = current
    if size is not None:
      request['size'] = size
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/simple-earn/flexible/history/rewardsRecord',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FlexibleRewardsHistoryPage,
    )
