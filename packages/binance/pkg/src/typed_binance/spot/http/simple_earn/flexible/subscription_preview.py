# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class FlexibleSubscriptionPreview(TypedDict):
  """Estimated rewards for a hypothetical flexible product subscription."""

  totalAmount: NotRequired[str]
  """Total amount that would be subscribed, as a decimal string."""
  rewardAsset: NotRequired[str]
  """Asset the base rewards would be paid in."""
  airDropAsset: NotRequired[str]
  """Asset the airdrop portion of rewards would be paid in, when different from `rewardAsset`."""
  estDailyBonusRewards: NotRequired[str]
  """Estimated daily bonus-tier rewards, as a decimal string."""
  estDailyRealTimeRewards: NotRequired[str]
  """Estimated daily real-time rewards, as a decimal string."""
  estDailyAirdropRewards: NotRequired[str]
  """Estimated daily airdrop rewards, as a decimal string."""


class SubscriptionPreview(RpcEndpoint):
  """Preview the estimated rewards for a hypothetical Simple Earn flexible product subscription, without subscribing."""

  async def __call__(
    self,
    *,
    product_id: str,
    amount: str,
    validate: bool | None = None,
  ) -> FlexibleSubscriptionPreview:
    """Preview the estimated rewards for a hypothetical Simple Earn flexible product subscription, without subscribing.

    Args:
      product_id: Flexible product identifier to preview a subscription for.
      amount: Amount to preview subscribing, as a decimal string.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-simple-earn/api/rest-api/flexible-locked#get-flexible-subscription-preview)
    """
    params: dict = {
      'productId': product_id,
      'amount': amount,
    }
    _Response = FlexibleSubscriptionPreview
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/sapi/v1/simple-earn/flexible/subscriptionPreview',
      params=params,
      validator=_validator,
      validate=validate,
    )
