# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class FlexibleSubscriptionRecord(TypedDict):
  """One flexible product subscription."""

  amount: str
  """Subscribed amount, as a decimal string."""
  asset: str
  """Underlying asset subscribed."""
  time: TimestampMillis
  """Millisecond epoch time the subscription was recorded."""
  purchaseId: int
  """Identifier of the subscription."""
  productId: str
  """Flexible product identifier subscribed to."""
  type: str
  """Subscription type (e.g. a normal subscription vs. an automatic reward reinvestment)."""
  sourceAccount: str
  """Wallet the subscription amount was debited from."""
  amtFromSpot: str
  """Portion of the subscribed amount debited from the Spot wallet, as a decimal string."""
  amtFromFunding: str
  """Portion of the subscribed amount debited from the Funding wallet, as a decimal string."""
  status: str
  """Subscription status."""


class Request(TypedDict):
  """Request parameters."""

  productId: NotRequired[str]
  """Restrict results to this flexible product."""
  purchaseId: NotRequired[str]
  """Restrict results to this subscription."""
  asset: NotRequired[str]
  """Restrict results to this underlying asset."""
  startTime: NotRequired[TimestampMillis]
  """Millisecond epoch start of the queried window."""
  endTime: NotRequired[TimestampMillis]
  """Millisecond epoch end of the queried window."""
  current: NotRequired[int]
  """Currently querying page. Starts from 1."""
  size: NotRequired[int]
  """Number of results per page."""


class FlexibleSubscriptionRecordPage(TypedDict):
  """One page of this account's flexible product subscription history."""

  rows: list[FlexibleSubscriptionRecord]
  """Matching records on this page."""
  total: int
  """Total number of matching records."""


class SubscriptionRecord(RpcEndpoint):
  """Get this account's Simple Earn flexible product subscription history."""

  def subscription_record_paged(
    self,
    *,
    product_id: str | None = None,
    purchase_id: str | None = None,
    asset: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    size: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[FlexibleSubscriptionRecord, int]:
    """Get this account's Simple Earn flexible product subscription history.

    Paged variant of `subscription_record`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      product_id: Restrict results to this flexible product.
      purchase_id: Restrict results to this subscription.
      asset: Restrict results to this underlying asset.
      start_time: Millisecond epoch start of the queried window.
      end_time: Millisecond epoch end of the queried window.
      size: Number of results per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-simple-earn/api/rest-api/flexible-locked#get-flexible-subscription-record)
    """

    async def next(current: int) -> tuple[list[FlexibleSubscriptionRecord], int | None]:
      response = await self.subscription_record(
        product_id=product_id,
        purchase_id=purchase_id,
        asset=asset,
        start_time=start_time,
        end_time=end_time,
        size=size,
        current=current,
        validate=validate,
      )
      rows = response.get('rows')
      rows = rows if rows is not None else []
      total = response.get('total')
      total = int(total) if total is not None else None
      if total is None or size is None or (current - 1 + 1) * size >= total:
        return rows, None
      return rows, current + 1

    return PaginatedResponse(1, next)

  async def subscription_record(
    self,
    *,
    product_id: str | None = None,
    purchase_id: str | None = None,
    asset: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    current: int | None = None,
    size: int | None = None,
    validate: bool | None = None,
  ) -> FlexibleSubscriptionRecordPage:
    """Get this account's Simple Earn flexible product subscription history.

    Args:
      product_id: Restrict results to this flexible product.
      purchase_id: Restrict results to this subscription.
      asset: Restrict results to this underlying asset.
      start_time: Millisecond epoch start of the queried window.
      end_time: Millisecond epoch end of the queried window.
      current: Currently querying page. Starts from 1.
      size: Number of results per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-simple-earn/api/rest-api/flexible-locked#get-flexible-subscription-record)
    """
    request: Request = Request()
    if product_id is not None:
      request['productId'] = product_id
    if purchase_id is not None:
      request['purchaseId'] = purchase_id
    if asset is not None:
      request['asset'] = asset
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if current is not None:
      request['current'] = current
    if size is not None:
      request['size'] = size
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/simple-earn/flexible/history/subscriptionRecord',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FlexibleSubscriptionRecordPage,
    )
