# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampIso
from typed_binance.core.endpoint.rpc import RpcEndpoint


class PredictionTransferStatus(TypedDict):
  """Current status of a prediction wallet transfer."""

  transferId: NotRequired[str]
  """Transfer ID."""
  direction: NotRequired[Literal['INBOUND', 'OUTBOUND']]
  """Transfer direction."""
  status: NotRequired[Literal['COMPLETED', 'FAILED', 'PROCESSING', 'PENDING']]
  """Transfer status."""
  fromToken: NotRequired[str]
  """Source token symbol."""
  fromTokenAmount: NotRequired[str]
  """Source amount, as a decimal string."""
  toToken: NotRequired[str]
  """Destination token symbol."""
  toTokenAmount: NotRequired[str]
  """Destination amount, as a decimal string."""
  errorCode: NotRequired[str | None]
  """Error code, or null if the transfer had no error."""
  errorMessage: NotRequired[str | None]
  """Error message, or null if the transfer had no error."""
  createTime: NotRequired[TimestampIso]
  """Time this transfer was created, as an ISO 8601 timestamp with timezone offset (e.g. `2026-05-25T04:00:00.000+00:00`)."""
  updateTime: NotRequired[TimestampIso]
  """Time this transfer was last updated, as an ISO 8601 timestamp with timezone offset."""


class Request(TypedDict):
  """Request parameters."""

  transferId: str
  """Transfer ID returned from `spot.prediction.transfer_inbound` or `spot.prediction.transfer_outbound`."""


class TransferStatus(RpcEndpoint):
  """Query the current status of a prediction wallet transfer by transfer ID. Terminal states are `COMPLETED` and `FAILED`; intermediate states are `PROCESSING` and `PENDING`. Do not poll for `SUCCESS` -- it is not a valid terminal state here."""

  async def transfer_status(
    self,
    transfer_id: str,
    *,
    validate: bool | None = None,
  ) -> PredictionTransferStatus:
    """Query the current status of a prediction wallet transfer by transfer ID. Terminal states are `COMPLETED` and `FAILED`; intermediate states are `PROCESSING` and `PENDING`. Do not poll for `SUCCESS` -- it is not a valid terminal state here.

    Args:
      transfer_id: Transfer ID returned from `spot.prediction.transfer_inbound` or `spot.prediction.transfer_outbound`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/transfer#query-transfer-status)
    """
    return await self.request(
      Request(transferId=transfer_id),
      method='GET',
      path='/sapi/v1/w3w/wallet/prediction/transfer/status',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PredictionTransferStatus,
    )
