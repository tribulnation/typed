# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class PredictionLastTradePrice(TypedDict):
  """Most recent trade price for a prediction market."""

  marketId: int
  """Market ID."""
  lastTradePrice: Decimal
  """Most recent trade price, as a decimal string."""


class Request(TypedDict):
  """Request parameters."""

  marketId: int
  """Market ID. Must be > 0."""


class LastTradePrice(RpcEndpoint):
  """Get the most recent trade price for a prediction market."""

  async def last_trade_price(
    self,
    market_id: int,
    *,
    validate: bool | None = None,
  ) -> PredictionLastTradePrice:
    """Get the most recent trade price for a prediction market.

    Args:
      market_id: Market ID. Must be > 0.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/market-data#query-last-trade-price)
    """
    return await self.request(
      Request(marketId=market_id),
      method='GET',
      path='/sapi/v1/w3w/wallet/prediction/order-book/last-trade-price',
      meta={'security': 'SIGNED', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PredictionLastTradePrice,
    )
