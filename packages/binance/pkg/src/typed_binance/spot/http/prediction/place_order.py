# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class PredictionPlaceOrderResult(TypedDict):
  """The placed order's identifier."""

  orderId: NotRequired[str]
  """Internal order ID."""


class Request(TypedDict):
  """Request parameters."""

  walletAddress: str
  """User's prediction wallet address."""
  walletId: str
  """Wallet ID."""
  quoteId: str
  """Quote ID obtained from `spot.prediction.quote`."""
  timeInForce: Literal['FOK', 'GTC']
  """Time in force. Must match `orderType`: `FOK` for `MARKET`, `GTC` for `LIMIT`."""
  accountType: Literal['SPOT', 'FUNDING']
  """Payment account type."""
  orderType: Literal['MARKET', 'LIMIT']
  """Order type."""
  slippageBps: int
  """Slippage tolerance in basis points. Range 1-10000."""
  priceLimit: NotRequired[str]
  """Limit price. Required when `orderType` is `LIMIT`. Must be > 0."""
  fundingSource: NotRequired[Literal['MPC', 'CEX']]
  """Funding source."""
  fundTransferAmount: NotRequired[str]
  """Amount to auto-transfer into the prediction wallet before ordering, in wei. Must be > 0 if provided."""


class PlaceOrder(RpcEndpoint):
  """Place a prediction order using a previously obtained quote from `spot.prediction.quote`. Requires SAS (Self-Authorization Service) authorization on the wallet. `timeInForce` must match `orderType`: `FOK` for `MARKET` orders, `GTC` for `LIMIT` orders; `priceLimit` is not required for `MARKET` orders but is required, and must be > 0, for `LIMIT` orders."""

  async def place_order(
    self,
    *,
    wallet_address: str,
    wallet_id: str,
    quote_id: str,
    time_in_force: Literal['FOK', 'GTC'],
    account_type: Literal['SPOT', 'FUNDING'],
    order_type: Literal['MARKET', 'LIMIT'],
    slippage_bps: int,
    price_limit: str | None = None,
    funding_source: Literal['MPC', 'CEX'] | None = None,
    fund_transfer_amount: str | None = None,
    validate: bool | None = None,
  ) -> PredictionPlaceOrderResult:
    """Place a prediction order using a previously obtained quote from `spot.prediction.quote`. Requires SAS (Self-Authorization Service) authorization on the wallet. `timeInForce` must match `orderType`: `FOK` for `MARKET` orders, `GTC` for `LIMIT` orders; `priceLimit` is not required for `MARKET` orders but is required, and must be > 0, for `LIMIT` orders.

    Args:
      wallet_address: User's prediction wallet address.
      wallet_id: Wallet ID.
      quote_id: Quote ID obtained from `spot.prediction.quote`.
      time_in_force: Time in force. Must match `orderType`: `FOK` for `MARKET`, `GTC` for `LIMIT`.
      account_type: Payment account type.
      order_type: Order type.
      slippage_bps: Slippage tolerance in basis points. Range 1-10000.
      price_limit: Limit price. Required when `orderType` is `LIMIT`. Must be > 0.
      funding_source: Funding source.
      fund_transfer_amount: Amount to auto-transfer into the prediction wallet before ordering, in wei. Must be > 0 if provided.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/trade#place-order)
    """
    request: Request = Request(
      walletAddress=wallet_address,
      walletId=wallet_id,
      quoteId=quote_id,
      timeInForce=time_in_force,
      accountType=account_type,
      orderType=order_type,
      slippageBps=slippage_bps,
    )
    if price_limit is not None:
      request['priceLimit'] = price_limit
    if funding_source is not None:
      request['fundingSource'] = funding_source
    if fund_transfer_amount is not None:
      request['fundTransferAmount'] = fund_transfer_amount
    return await self.request(
      request,
      method='POST',
      path='/sapi/v1/w3w/wallet/prediction/trade/place-order-bundle',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PredictionPlaceOrderResult,
    )
