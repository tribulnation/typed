# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class PredictionOutboundTransferResult(TypedDict):
  """The created outbound transfer."""

  transferId: NotRequired[str]
  """Transfer ID. Pass this to `spot.prediction.transfer_status` to poll its status."""
  status: NotRequired[str]
  """Initial transfer status."""


class TransferOutbound(RpcEndpoint):
  """Transfer funds from the user's CEX account (SPOT or FUNDING) into the prediction wallet. Requires SAS authorization."""

  async def transfer_outbound(
    self,
    *,
    wallet_id: str,
    wallet_address: str,
    from_token_amount: str,
    account_type: Literal['SPOT', 'FUNDING'],
    source_biz: Literal['USER_TRANSFER', 'PREDICTION_BUY'],
    from_token: str | None = None,
    to_token: str | None = None,
    chain_id: str | None = None,
    validate: bool | None = None,
  ) -> PredictionOutboundTransferResult:
    """Transfer funds from the user's CEX account (SPOT or FUNDING) into the prediction wallet. Requires SAS authorization.

    Args:
      wallet_id: Wallet ID.
      wallet_address: User's prediction wallet address.
      from_token_amount: Transfer amount in wei (18 decimals). Must be > 0. Example: `1000000000000000000` = 1 USDT.
      account_type: Source CEX account.
      source_biz: Business source of the transfer.
      from_token: Source token symbol. Default `USDT`.
      to_token: Destination token symbol. Default `USDT`.
      chain_id: Chain ID. Default `56` (BSC).

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/transfer#create-outbound-transfer)
    """
    params: dict = {
      'walletId': wallet_id,
      'walletAddress': wallet_address,
      'fromTokenAmount': from_token_amount,
      'accountType': account_type,
      'sourceBiz': source_biz,
    }
    if from_token is not None:
      params['fromToken'] = from_token
    if to_token is not None:
      params['toToken'] = to_token
    if chain_id is not None:
      params['chainId'] = chain_id
    _Response = PredictionOutboundTransferResult
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/w3w/wallet/prediction/transfer/outbound',
      params=params,
      validator=_validator,
      validate=validate,
    )
