# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, Sequence, TypedDict
from typed_binance.core import TimestampIso, TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class PredictionOrderHistoryRow(TypedDict):
  """One historical prediction order."""

  orderId: NotRequired[str]
  """Internal order ID."""
  vendorOrderId: NotRequired[str]
  """Vendor-side (on-chain) order identifier."""
  vendor: NotRequired[str]
  """Prediction liquidity vendor that executed this order."""
  marketTopicId: NotRequired[int]
  """Market topic ID."""
  slug: NotRequired[str]
  """URL-friendly topic slug."""
  marketTopicTitle: NotRequired[str]
  """Market topic title."""
  marketId: NotRequired[int]
  """Market ID."""
  marketTitle: NotRequired[str]
  """Market title."""
  outcome: NotRequired[str]
  """Outcome name this order traded."""
  outcomeIndex: NotRequired[int]
  """Zero-based outcome index."""
  status: NotRequired[str]
  """Order status."""
  side: NotRequired[Literal['BUY', 'SELL']]
  """Trade direction."""
  orderType: NotRequired[Literal['MARKET', 'LIMIT']]
  """Order type."""
  createTime: NotRequired[TimestampMillis]
  """Time the order was created."""
  modifyTime: NotRequired[TimestampMillis]
  """Time the order was last modified."""
  terminalTime: NotRequired[TimestampMillis]
  """Time the order reached a terminal state."""
  makerUsdtAmount: NotRequired[Decimal]
  """Order amount in USDT terms, as a decimal string."""
  makerShareQty: NotRequired[Decimal]
  """Order quantity in outcome shares, as a decimal string."""
  filledUsdtAmount: NotRequired[Decimal]
  """Filled amount in USDT terms, as a decimal string."""
  filledShareQty: NotRequired[Decimal]
  """Filled quantity in outcome shares, as a decimal string."""
  fillPercentage: NotRequired[Decimal]
  """Fraction of the order filled, as a decimal string."""
  price: NotRequired[Decimal]
  """Order price, as a decimal string."""
  marketProviderFee: NotRequired[Decimal]
  """Fee charged by the market provider, as a decimal string."""
  networkFee: NotRequired[Decimal]
  """On-chain network fee, as a decimal string."""


class Request(TypedDict):
  """Request parameters."""

  walletAddress: str
  """User's prediction wallet address."""
  l1Category: NotRequired[str]
  """Filter by level-1 category."""
  orderType: NotRequired[Literal['MARKET', 'LIMIT']]
  """Filter by order type."""
  status: NotRequired[str]
  """Filter by order status."""
  startDate: NotRequired[TimestampIso]
  """Start date. Format `yyyy-MM-dd`. Must be <= `endDate`."""
  endDate: NotRequired[TimestampIso]
  """End date. Format `yyyy-MM-dd`. Must be >= `startDate`."""
  offset: NotRequired[int]
  """Pagination offset. Default `0`."""
  limit: NotRequired[int]
  """Page size. Default `20`, range 1-100."""


class PredictionOrderHistoryList(TypedDict):
  """This user's historical prediction orders."""

  total: NotRequired[int]
  """Total number of orders matching the filter."""
  offset: NotRequired[int]
  """Pagination offset used for this page."""
  limit: NotRequired[int]
  """Page size used for this page."""
  orders: NotRequired[list[PredictionOrderHistoryRow]]
  """Historical orders on this page."""


class OrderHistory(RpcEndpoint):
  """Get historical prediction orders (all statuses) for the authenticated user, with optional filters."""

  def order_history_paged(
    self,
    *,
    wallet_address: str,
    l1category: str | None = None,
    order_type: Literal['MARKET', 'LIMIT'] | None = None,
    status: str | None = None,
    start_date: TimestampIso | None = None,
    end_date: TimestampIso | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[PredictionOrderHistoryRow, int]:
    """Get historical prediction orders (all statuses) for the authenticated user, with optional filters.

    Paged variant of `order_history`: Advances `offset` by `len(rows)` and stops once it has covered the `total` items the response reports, or on an empty page. Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      wallet_address: User's prediction wallet address.
      l1category: Filter by level-1 category.
      order_type: Filter by order type.
      status: Filter by order status.
      start_date: Start date. Format `yyyy-MM-dd`. Must be <= `endDate`.
      end_date: End date. Format `yyyy-MM-dd`. Must be >= `startDate`.
      limit: Page size. Default `20`, range 1-100.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/trade#query-order-history)
    """

    async def next(
      offset: int,
    ) -> tuple[Sequence[PredictionOrderHistoryRow], int | None]:
      response = await self.order_history(
        wallet_address=wallet_address,
        l1category=l1category,
        order_type=order_type,
        status=status,
        start_date=start_date,
        end_date=end_date,
        limit=limit,
        offset=offset,
        validate=validate,
      )
      rows = response.get('orders')
      rows = rows if rows is not None else []
      total = response.get('total')
      total = int(total) if total is not None else None
      if not rows or (total is not None and (offset + len(rows)) >= total):
        return rows, None
      return rows, offset + len(rows)

    return PaginatedResponse(0, next)

  async def order_history(
    self,
    *,
    wallet_address: str,
    l1category: str | None = None,
    order_type: Literal['MARKET', 'LIMIT'] | None = None,
    status: str | None = None,
    start_date: TimestampIso | None = None,
    end_date: TimestampIso | None = None,
    offset: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PredictionOrderHistoryList:
    """Get historical prediction orders (all statuses) for the authenticated user, with optional filters.

    Args:
      wallet_address: User's prediction wallet address.
      l1category: Filter by level-1 category.
      order_type: Filter by order type.
      status: Filter by order status.
      start_date: Start date. Format `yyyy-MM-dd`. Must be <= `endDate`.
      end_date: End date. Format `yyyy-MM-dd`. Must be >= `startDate`.
      offset: Pagination offset. Default `0`.
      limit: Page size. Default `20`, range 1-100.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/trade#query-order-history)
    """
    request: Request = Request(walletAddress=wallet_address)
    if l1category is not None:
      request['l1Category'] = l1category
    if order_type is not None:
      request['orderType'] = order_type
    if status is not None:
      request['status'] = status
    if start_date is not None:
      request['startDate'] = start_date
    if end_date is not None:
      request['endDate'] = end_date
    if offset is not None:
      request['offset'] = offset
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/w3w/wallet/prediction/order/history',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PredictionOrderHistoryList,
    )
