# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class MarkupTradeDetail(TypedDict):
  """The markup commission detail of a single underlying trade."""

  orderId: NotRequired[int]
  """Order ID of the underlying trade."""
  subUserId: NotRequired[int]
  """Sub-user ID."""
  groupCode: NotRequired[str]
  """Fee group code."""
  groupName: NotRequired[str]
  """Fee group name."""
  product: NotRequired[str]
  """Product line the trade belongs to."""
  symbol: NotRequired[str]
  """Trading symbol."""
  tradeSide: NotRequired[str]
  """Side of the trade."""
  tradeType: NotRequired[str]
  """Whether the trade was maker or taker."""
  price: NotRequired[str]
  """Trade price, as a decimal string."""
  qty: NotRequired[str]
  """Trade quantity, as a decimal string."""
  baseAsset: NotRequired[str]
  """Base asset of the traded symbol."""
  quoteAsset: NotRequired[str]
  """Quote asset of the traded symbol."""
  quoteQty: NotRequired[str]
  """Trade quote quantity, as a decimal string."""
  baseRate: NotRequired[str]
  """Base commission rate applied, as a decimal string."""
  markupRate: NotRequired[str]
  """Markup rate applied, as a decimal string."""
  totalFeeRate: NotRequired[str]
  """Total fee rate (base rate plus markup rate), as a decimal string."""
  feeAsset: NotRequired[str]
  """Asset the trading fee was charged in."""
  transactionTime: NotRequired[TimestampMillis]
  """Millisecond epoch time the trade was executed."""
  markupAsset: NotRequired[str]
  """Asset the markup income is denominated in."""
  markupAmount: NotRequired[str]
  """Markup income amount, in `markupAsset`, as a decimal string."""
  markupAmountUsdt: NotRequired[str]
  """Markup income amount converted to USDT, as a decimal string."""
  markupConvertRate: NotRequired[str]
  """Conversion rate used to convert the markup income into USDT, as a decimal string."""
  markupConvertTime: NotRequired[TimestampMillis]
  """Millisecond epoch time the markup income conversion occurred."""


class Request(TypedDict):
  """Request parameters."""

  subUserId: NotRequired[int]
  """Sub-user ID filter."""
  groupCode: NotRequired[str]
  """Fee group code filter."""
  symbol: NotRequired[str]
  """Trading symbol filter (e.g., BTCUSDT)."""
  startTime: TimestampMillis
  """Start of search window (milliseconds)."""
  endTime: TimestampMillis
  """End of search window (milliseconds)."""
  pageIndex: int
  """Page index, starting from 1."""
  pageSize: int
  """Page size (1-100)."""


class MarkupTradeDetailPage(TypedDict):
  """One page of trade-level markup fee detail records."""

  total: NotRequired[int]
  """Total number of matching trade detail records."""
  rows: NotRequired[list[MarkupTradeDetail]]
  """Trade detail records on this page."""


class QueryMarkupTradeDetailReportingsResponse(TypedDict):
  """Markup trade detail reporting response frame. This product line wraps its business data in {code,message,data} on the wire, and the core does not strip it -- see spec/core.md's Envelope section."""

  code: NotRequired[str]
  """Response code. "000000" indicates success."""
  message: NotRequired[str]
  """Response message."""
  data: NotRequired[MarkupTradeDetailPage]


class MarkupTradeDetails(RpcEndpoint):
  """Query trade-level markup fee details."""

  def markup_trade_details_paged(
    self,
    *,
    sub_user_id: int | None = None,
    group_code: str | None = None,
    symbol: str | None = None,
    start_time: TimestampMillis,
    end_time: TimestampMillis,
    page_size: int,
    validate: bool | None = None,
  ) -> PaginatedResponse[MarkupTradeDetail, int]:
    """Query trade-level markup fee details.

    Paged variant of `markup_trade_details`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      sub_user_id: Sub-user ID filter.
      group_code: Fee group code filter.
      symbol: Trading symbol filter (e.g., BTCUSDT).
      start_time: Start of search window (milliseconds).
      end_time: End of search window (milliseconds).
      page_size: Page size (1-100).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-vip-caas/api/rest-api/reportings#query-markup-trade-detail-reportings)
    """

    async def next(page_index: int) -> tuple[list[MarkupTradeDetail], int | None]:
      response = await self.markup_trade_details(
        sub_user_id=sub_user_id,
        group_code=group_code,
        symbol=symbol,
        start_time=start_time,
        end_time=end_time,
        page_size=page_size,
        page_index=page_index,
        validate=validate,
      )
      rows_0 = response.get('data')
      rows = rows_0.get('rows') if rows_0 is not None else None
      rows = rows if rows is not None else []
      total_0 = response.get('data')
      total = total_0.get('total') if total_0 is not None else None
      total = int(total) if total is not None else None
      if total is None or (page_index - 1 + 1) * page_size >= total:
        return rows, None
      return rows, page_index + 1

    return PaginatedResponse(1, next)

  async def markup_trade_details(
    self,
    *,
    sub_user_id: int | None = None,
    group_code: str | None = None,
    symbol: str | None = None,
    start_time: TimestampMillis,
    end_time: TimestampMillis,
    page_index: int,
    page_size: int,
    validate: bool | None = None,
  ) -> QueryMarkupTradeDetailReportingsResponse:
    """Query trade-level markup fee details.

    Args:
      sub_user_id: Sub-user ID filter.
      group_code: Fee group code filter.
      symbol: Trading symbol filter (e.g., BTCUSDT).
      start_time: Start of search window (milliseconds).
      end_time: End of search window (milliseconds).
      page_index: Page index, starting from 1.
      page_size: Page size (1-100).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-vip-caas/api/rest-api/reportings#query-markup-trade-detail-reportings)
    """
    request: Request = Request(
      startTime=start_time, endTime=end_time, pageIndex=page_index, pageSize=page_size
    )
    if sub_user_id is not None:
      request['subUserId'] = sub_user_id
    if group_code is not None:
      request['groupCode'] = group_code
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/vip/caas/commission/markup-trade-details',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=QueryMarkupTradeDetailReportingsResponse,
    )
