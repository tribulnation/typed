# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Request parameters."""

  groupCode: str
  """Fee group code."""
  makerMarkup: float
  """New maker markup rate."""
  takerMarkup: float
  """New taker markup rate."""


class UpdateFeeGroupCommissionResponse(TypedDict):
  """Fee group commission update response frame. This product line wraps its business data in {code,message} on the wire, and the core does not strip it -- see spec/core.md's Envelope section. There is no `data` field -- success is signaled by `code` alone."""

  code: NotRequired[str]
  """Response code. "000000" indicates success."""
  message: NotRequired[str]
  """Response message."""


class FeeGroupCommissionUpdate(RpcEndpoint):
  """Update the maker/taker commission markup rates of a fee group. Requires the API key's "trade" option to be enabled."""

  async def fee_group_commission_update(
    self,
    group_code: str,
    *,
    maker_markup: float,
    taker_markup: float,
    validate: bool | None = None,
  ) -> UpdateFeeGroupCommissionResponse:
    """Update the maker/taker commission markup rates of a fee group. Requires the API key's "trade" option to be enabled.

    Args:
      group_code: Fee group code.
      maker_markup: New maker markup rate.
      taker_markup: New taker markup rate.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-vip-caas/api/rest-api/fee-groups#update-fee-group-commission)
    """
    return await self.request(
      Request(groupCode=group_code, makerMarkup=maker_markup, takerMarkup=taker_markup),
      method='PUT',
      path='/sapi/v1/vip/caas/commission/fee-groups/commission',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=UpdateFeeGroupCommissionResponse,
    )
