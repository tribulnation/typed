# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class DeleteFeeGroupResponse(TypedDict):
  """Fee group deletion response frame. This product line wraps its business data in {code,message} on the wire, and the core does not strip it -- see spec/core.md's Envelope section. There is no `data` field -- success is signaled by `code` alone."""

  code: NotRequired[str]
  """Response code. "000000" indicates success."""
  message: NotRequired[str]
  """Response message."""


class FeeGroupDelete(RpcEndpoint):
  """Delete a commission fee group. All sub-accounts must be unassigned from it first. Requires the API key's "trade" option to be enabled."""

  async def fee_group_delete(
    self,
    *,
    group_code: str,
    validate: bool | None = None,
  ) -> DeleteFeeGroupResponse:
    """Delete a commission fee group. All sub-accounts must be unassigned from it first. Requires the API key's "trade" option to be enabled.

    Args:
      group_code: Fee group code.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-vip-caas/api/rest-api/fee-groups#delete-fee-group)
    """
    params: dict = {
      'groupCode': group_code,
    }
    _Response = DeleteFeeGroupResponse
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'DELETE',
      '/sapi/v1/vip/caas/commission/fee-groups',
      params=params,
      validator=_validator,
      validate=validate,
    )
