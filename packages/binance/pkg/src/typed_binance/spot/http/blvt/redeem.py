# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class BlvtRedemption(TypedDict):
  """Result of redeeming (selling) a leveraged token."""

  id: int
  """Redemption record identifier."""
  status: Literal['S', 'P', 'F']
  """Redemption status."""
  tokenName: str
  """Leveraged token name redeemed."""
  redeemAmount: str
  """Token amount redeemed."""
  amount: str
  """Redemption value credited, in USDT."""
  timestamp: TimestampMillis
  """Time the redemption was processed."""


class Redeem(RpcEndpoint):
  """Redeem BLVT (USER_DATA)"""

  async def redeem(
    self,
    *,
    token_name: str,
    amount: float,
    validate: bool | None = None,
  ) -> BlvtRedemption:
    """Redeem (sell) a Binance Leveraged Token back to spot balance.

    Args:
      token_name: Leveraged token name, e.g. `BTCDOWN`, `BTCUP`.
      amount: Token amount to redeem.

    References:
      - [Official docs](https://github.com/binance/binance-api-swagger)
    """
    params: dict = {
      'tokenName': token_name,
      'amount': amount,
    }
    _Response = BlvtRedemption
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/blvt/redeem',
      params=params,
      validator=_validator,
      validate=validate,
    )
