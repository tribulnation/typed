# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint
from typed_binance.schemas import SpotTrade


class Request(TypedDict):
  """Request parameters."""

  symbol: str
  """Symbol to query."""
  limit: NotRequired[int]
  """Number of trades to return."""
  fromId: NotRequired[int]
  """Trade ID to fetch from. Defaults to the most recent trades."""


Response = list[SpotTrade]


class HistoricalTrades(RpcEndpoint):
  """Get older trades."""

  async def historical_trades(
    self,
    symbol: str,
    *,
    limit: int | None = None,
    from_id: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get older trades.

    Args:
      symbol: Symbol to query.
      limit: Number of trades to return.
      from_id: Trade ID to fetch from. Defaults to the most recent trades.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/rest-api.md#old-trade-lookup)
    """
    request: Request = Request(symbol=symbol)
    if limit is not None:
      request['limit'] = limit
    if from_id is not None:
      request['fromId'] = from_id
    return await self.request(
      request,
      method='GET',
      path='/api/v3/historicalTrades',
      meta={'security': 'NONE', 'signed': False},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
