# Generated by typed-dev — see CONTRIBUTING.md.
from datetime import timedelta
from typed_core.exceptions import LogicError
from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint
from typed_binance.schemas import SpotCandle


class Request(TypedDict):
  """Request parameters."""

  symbol: str
  """Symbol to query."""
  interval: Literal[
    '1s',
    '1m',
    '3m',
    '5m',
    '15m',
    '30m',
    '1h',
    '2h',
    '4h',
    '6h',
    '8h',
    '12h',
    '1d',
    '3d',
    '1w',
    '1M',
  ]
  """Kline interval."""
  startTime: NotRequired[TimestampMillis]
  """Timestamp to fetch klines from. Always interpreted in UTC regardless of `timeZone`."""
  endTime: NotRequired[TimestampMillis]
  """Timestamp to fetch klines until. Always interpreted in UTC regardless of `timeZone`."""
  timeZone: NotRequired[str]
  """Timezone kline intervals are interpreted in, as hours and minutes (e.g. '-1:00', '05:45') or just hours (e.g. '0', '8'). Accepted range is [-12:00, +14:00] inclusive. Does not affect `startTime`/`endTime`, which are always UTC."""
  limit: NotRequired[int]
  """Number of klines to return."""


Response = list[SpotCandle]


class Klines(RpcEndpoint):
  """Kline/candlestick bars for a symbol. Klines are uniquely identified by their open time."""

  async def klines_paged(
    self,
    *,
    symbol: str,
    interval: Literal[
      '1s',
      '1m',
      '3m',
      '5m',
      '15m',
      '30m',
      '1h',
      '2h',
      '4h',
      '6h',
      '8h',
      '12h',
      '1d',
      '3d',
      '1w',
      '1M',
    ],
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    time_zone: str | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    allow_truncation: bool = False,
    validate: bool | None = None,
  ) -> AsyncIterator[Response]:
    """Kline/candlestick bars for a symbol. Klines are uniquely identified by their open time.

    Paged variant of `klines`: Moves the `start_time`–`end_time` window forwards by its own width and stops on the first empty window, or after `max_pages` pages when one is given.

    Every request spans the width the caller's own `start_time` and `end_time` state, so choose a window the venue answers in one response — at most `limit` rows. A full page is evidence the window was capped, and raises `LogicError` rather than walking past the rows that were left out; pass `allow_truncation=True` to accept the loss and keep going.

    Args:
      symbol: Symbol to query.
      interval: Kline interval.
      start_time: Timestamp to fetch klines from. Always interpreted in UTC regardless of `timeZone`.
      end_time: Timestamp to fetch klines until. Always interpreted in UTC regardless of `timeZone`.
      time_zone: Timezone kline intervals are interpreted in, as hours and minutes (e.g. '-1:00', '05:45') or just hours (e.g. '0', '8'). Accepted range is [-12:00, +14:00] inclusive. Does not affect `startTime`/`endTime`, which are always UTC.
      limit: Number of klines to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.
      allow_truncation: Accept a truncated page instead of raising `LogicError` when one is detected.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/rest-api.md#klinecandlestick-data)
    """
    if start_time is None or end_time is None:
      raise ValueError(
        '`klines_paged` walks a time window: pass both `start_time` and `end_time`'
      )
    lower = start_time
    upper = end_time
    width = upper - lower
    pages = 0
    while True:
      response = await self.klines(
        symbol=symbol,
        interval=interval,
        start_time=lower,
        end_time=upper,
        time_zone=time_zone,
        limit=limit,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      if not response:
        break
      if not allow_truncation and len(response) >= (
        limit if limit is not None else 500
      ):
        raise LogicError(
          f'`klines_paged` requested the window {lower} to {upper} and the venue returned a full page of {len(response)} rows, so it may hold more; advancing would move past the rows that were left out. Narrow the window, or pass `allow_truncation=True` to accept the loss.'
        )
      lower = upper + timedelta(milliseconds=1)
      upper = lower + width

  async def klines(
    self,
    *,
    symbol: str,
    interval: Literal[
      '1s',
      '1m',
      '3m',
      '5m',
      '15m',
      '30m',
      '1h',
      '2h',
      '4h',
      '6h',
      '8h',
      '12h',
      '1d',
      '3d',
      '1w',
      '1M',
    ],
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    time_zone: str | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Kline/candlestick bars for a symbol. Klines are uniquely identified by their open time.

    Args:
      symbol: Symbol to query.
      interval: Kline interval.
      start_time: Timestamp to fetch klines from. Always interpreted in UTC regardless of `timeZone`.
      end_time: Timestamp to fetch klines until. Always interpreted in UTC regardless of `timeZone`.
      time_zone: Timezone kline intervals are interpreted in, as hours and minutes (e.g. '-1:00', '05:45') or just hours (e.g. '0', '8'). Accepted range is [-12:00, +14:00] inclusive. Does not affect `startTime`/`endTime`, which are always UTC.
      limit: Number of klines to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/rest-api.md#klinecandlestick-data)
    """
    request: Request = Request(symbol=symbol, interval=interval)
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if time_zone is not None:
      request['timeZone'] = time_zone
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/api/v3/klines',
      meta={'security': 'NONE', 'signed': False},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
