# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class ExchangeMaxNumAlgoOrdersFilter(TypedDict):
  """Maximum number of algo orders (STOP_LOSS, STOP_LOSS_LIMIT, TAKE_PROFIT, TAKE_PROFIT_LIMIT) an account may have open across the exchange."""

  filterType: Literal['EXCHANGE_MAX_NUM_ALGO_ORDERS']
  """Filter type discriminator; always "EXCHANGE_MAX_NUM_ALGO_ORDERS" for a ExchangeMaxNumAlgoOrdersFilter."""
  maxNumAlgoOrders: int
  """Maximum number of open algo orders allowed across the exchange."""


class ExchangeMaxNumIcebergOrdersFilter(TypedDict):
  """Maximum number of iceberg orders an account may have open across the exchange."""

  filterType: Literal['EXCHANGE_MAX_NUM_ICEBERG_ORDERS']
  """Filter type discriminator; always "EXCHANGE_MAX_NUM_ICEBERG_ORDERS" for a ExchangeMaxNumIcebergOrdersFilter."""
  maxNumIcebergOrders: int
  """Maximum number of open iceberg orders allowed across the exchange."""


class ExchangeMaxNumOrderListsFilter(TypedDict):
  """Maximum number of order lists an account may have open across the exchange. An OTOCO counts as one order list."""

  filterType: Literal['EXCHANGE_MAX_NUM_ORDER_LISTS']
  """Filter type discriminator; always "EXCHANGE_MAX_NUM_ORDER_LISTS" for a ExchangeMaxNumOrderListsFilter."""
  maxNumOrderLists: int
  """Maximum number of open order lists allowed across the exchange."""


class ExchangeMaxNumOrdersFilter(TypedDict):
  """Maximum number of orders an account may have open across the exchange. Both algo and normal orders count."""

  filterType: Literal['EXCHANGE_MAX_NUM_ORDERS']
  """Filter type discriminator; always "EXCHANGE_MAX_NUM_ORDERS" for a ExchangeMaxNumOrdersFilter."""
  maxNumOrders: int
  """Maximum number of open orders allowed across the exchange."""


class IcebergPartsFilter(TypedDict):
  """Maximum parts an iceberg order can have, computed as CEIL(qty / icebergQty)."""

  filterType: Literal['ICEBERG_PARTS']
  """Filter type discriminator; always "ICEBERG_PARTS" for a IcebergPartsFilter."""
  limit: int
  """Maximum number of iceberg parts allowed."""


class LotSizeFilter(TypedDict):
  """Order quantity bounds and step size."""

  filterType: Literal['LOT_SIZE']
  """Filter type discriminator; always "LOT_SIZE" for a LotSizeFilter."""
  minQty: Decimal
  """Minimum quantity/icebergQty allowed."""
  maxQty: Decimal
  """Maximum quantity/icebergQty allowed."""
  stepSize: Decimal
  """Interval a quantity/icebergQty can be incremented by."""


class MarketLotSizeFilter(TypedDict):
  """Quantity bounds and step size for MARKET orders."""

  filterType: Literal['MARKET_LOT_SIZE']
  """Filter type discriminator; always "MARKET_LOT_SIZE" for a MarketLotSizeFilter."""
  minQty: Decimal
  """Minimum quantity allowed for a MARKET order."""
  maxQty: Decimal
  """Maximum quantity allowed for a MARKET order."""
  stepSize: Decimal
  """Interval a MARKET order's quantity can be incremented by."""


class MaxNumAlgoOrdersFilter(TypedDict):
  """Maximum number of algo orders (STOP_LOSS, STOP_LOSS_LIMIT, TAKE_PROFIT, TAKE_PROFIT_LIMIT) an account may have open on this symbol."""

  filterType: Literal['MAX_NUM_ALGO_ORDERS']
  """Filter type discriminator; always "MAX_NUM_ALGO_ORDERS" for a MaxNumAlgoOrdersFilter."""
  maxNumAlgoOrders: int
  """Maximum number of open algo orders allowed on this symbol."""


class MaxNumIcebergOrdersFilter(TypedDict):
  """Maximum number of iceberg orders (icebergQty > 0) an account may have open on this symbol."""

  filterType: Literal['MAX_NUM_ICEBERG_ORDERS']
  """Filter type discriminator; always "MAX_NUM_ICEBERG_ORDERS" for a MaxNumIcebergOrdersFilter."""
  maxNumIcebergOrders: int
  """Maximum number of open iceberg orders allowed on this symbol."""


class MaxNumOrderAmendsFilter(TypedDict):
  """Maximum number of times a single order on this symbol can be amended."""

  filterType: Literal['MAX_NUM_ORDER_AMENDS']
  """Filter type discriminator; always "MAX_NUM_ORDER_AMENDS" for a MaxNumOrderAmendsFilter."""
  maxNumOrderAmends: int
  """Maximum number of amendments allowed on a single order."""


class MaxNumOrderListsFilter(TypedDict):
  """Maximum number of open order lists an account may have on this symbol. An OTOCO counts as one order list."""

  filterType: Literal['MAX_NUM_ORDER_LISTS']
  """Filter type discriminator; always "MAX_NUM_ORDER_LISTS" for a MaxNumOrderListsFilter."""
  maxNumOrderLists: int
  """Maximum number of open order lists allowed on this symbol."""


class MaxNumOrdersFilter(TypedDict):
  """Maximum number of orders an account may have open on this symbol. Both algo and normal orders count."""

  filterType: Literal['MAX_NUM_ORDERS']
  """Filter type discriminator; always "MAX_NUM_ORDERS" for a MaxNumOrdersFilter."""
  maxNumOrders: int
  """Maximum number of open orders allowed on this symbol."""


class MaxPositionFilter(TypedDict):
  """Maximum position an account may hold in this symbol's base asset (free + locked balance + open BUY order quantity). BUY orders that would exceed it are rejected."""

  filterType: Literal['MAX_POSITION']
  """Filter type discriminator; always "MAX_POSITION" for a MaxPositionFilter."""
  maxPosition: str
  """Maximum base-asset position allowed."""


class MinNotionalFilter(TypedDict):
  """Minimum notional value (price * quantity) allowed for an order."""

  filterType: Literal['MIN_NOTIONAL']
  """Filter type discriminator; always "MIN_NOTIONAL" for a MinNotionalFilter."""
  minNotional: Decimal
  """Minimum notional value allowed."""
  applyToMarket: bool
  """Whether this filter also applies to MARKET orders, using a recent average price in place of the missing order price."""
  avgPriceMins: int
  """Minutes over which the average price is computed when no reference price is set. 0 means the last price is used."""


class NotionalFilter(TypedDict):
  """Acceptable notional value (price * quantity) range for an order."""

  filterType: Literal['NOTIONAL']
  """Filter type discriminator; always "NOTIONAL" for a NotionalFilter."""
  minNotional: Decimal
  """Minimum notional value allowed."""
  applyMinToMarket: bool
  """Whether `minNotional` applies to MARKET orders."""
  maxNotional: Decimal
  """Maximum notional value allowed."""
  applyMaxToMarket: bool
  """Whether `maxNotional` applies to MARKET orders."""
  avgPriceMins: int
  """Minutes over which the average price is computed when no reference price is set. 0 means the last price is used."""


class PercentPriceBySideFilter(TypedDict):
  """Valid order-price range as a multiple of a recent average price, with separate bounds for BUY and SELL."""

  filterType: Literal['PERCENT_PRICE_BY_SIDE']
  """Filter type discriminator; always "PERCENT_PRICE_BY_SIDE" for a PercentPriceBySideFilter."""
  bidMultiplierUp: Decimal
  """Maximum multiple of the average price a BUY order's price may exceed."""
  bidMultiplierDown: Decimal
  """Minimum multiple of the average price a BUY order's price may fall below."""
  askMultiplierUp: Decimal
  """Maximum multiple of the average price a SELL order's price may exceed."""
  askMultiplierDown: Decimal
  """Minimum multiple of the average price a SELL order's price may fall below."""
  avgPriceMins: int
  """Minutes over which the average price is computed when no reference price is set. 0 means the last price is used."""


class PercentPriceFilter(TypedDict):
  """Valid order-price range as a multiple of a recent average price."""

  filterType: Literal['PERCENT_PRICE']
  """Filter type discriminator; always "PERCENT_PRICE" for a PercentPriceFilter."""
  multiplierUp: str
  """Maximum multiple of the average price a price may exceed."""
  multiplierDown: str
  """Minimum multiple of the average price a price may fall below."""
  avgPriceMins: int
  """Minutes over which the average price is computed when no reference price is set. 0 means the last price is used."""


class PriceFilter(TypedDict):
  """Price and stop-price bounds and tick size. Any of the three may be 0, which disables that rule."""

  filterType: Literal['PRICE_FILTER']
  """Filter type discriminator; always "PRICE_FILTER" for a PriceFilter."""
  minPrice: Decimal
  """Minimum price/stopPrice allowed. Disabled when 0."""
  maxPrice: Decimal
  """Maximum price/stopPrice allowed. Disabled when 0."""
  tickSize: Decimal
  """Interval a price/stopPrice can be incremented by. Disabled when 0."""


class RateLimit(TypedDict):
  """One rate limit rule."""

  rateLimitType: Literal['REQUEST_WEIGHT', 'ORDERS', 'RAW_REQUESTS']
  """Which rate limit pool this rule governs."""
  interval: Literal['SECOND', 'MINUTE', 'DAY']
  """Interval unit `intervalNum` counts."""
  intervalNum: int
  """Number of `interval` units this rule's window spans."""
  limit: int
  """Maximum count allowed within the window."""


class Request(TypedDict):
  """Request parameters."""

  symbol: NotRequired[str]
  """Single symbol to query."""
  symbols: NotRequired[list[str]]
  """Symbols to query, e.g. ["BTCUSDT","BNBUSDT"]."""
  permissions: NotRequired[str | list[str]]
  """Account/symbol permission(s) to filter symbols by. Cannot be combined with `symbol` or `symbols`."""
  showPermissionSets: NotRequired[bool]
  """Whether each symbol's `permissionSets` field is populated."""
  symbolStatus: NotRequired[Literal['TRADING', 'HALT', 'BREAK']]
  """Filter for symbols with this trading status. Cannot be combined with `symbol` or `symbols`."""


class SorGroup(TypedDict):
  """One Smart Order Routing group: a base asset and the symbols SOR may route an order for it through."""

  baseAsset: str
  """Base asset this SOR group routes for."""
  symbols: list[str]
  """Symbols SOR may route through for this base asset."""


class TrailingDeltaFilter(TypedDict):
  """Minimum and maximum trailingDelta for trailing-stop orders, split by direction."""

  filterType: Literal['TRAILING_DELTA']
  """Filter type discriminator; always "TRAILING_DELTA" for a TrailingDeltaFilter."""
  minTrailingAboveDelta: int
  """Minimum trailingDelta for STOP_LOSS BUY, STOP_LOSS_LIMIT BUY, TAKE_PROFIT SELL, and TAKE_PROFIT_LIMIT SELL orders."""
  maxTrailingAboveDelta: int
  """Maximum trailingDelta for STOP_LOSS BUY, STOP_LOSS_LIMIT BUY, TAKE_PROFIT SELL, and TAKE_PROFIT_LIMIT SELL orders."""
  minTrailingBelowDelta: int
  """Minimum trailingDelta for STOP_LOSS SELL, STOP_LOSS_LIMIT SELL, TAKE_PROFIT BUY, and TAKE_PROFIT_LIMIT BUY orders."""
  maxTrailingBelowDelta: int
  """Maximum trailingDelta for STOP_LOSS SELL, STOP_LOSS_LIMIT SELL, TAKE_PROFIT BUY, and TAKE_PROFIT_LIMIT BUY orders."""


class SpotSymbol(TypedDict):
  """Trading rules and metadata for one symbol."""

  symbol: str
  """Symbol."""
  status: Literal['TRADING', 'END_OF_DAY', 'HALT', 'BREAK', 'CANCEL_ONLY']
  """Current trading status."""
  baseAsset: str
  """Base asset."""
  baseAssetPrecision: int
  """Decimal precision of the base asset."""
  quoteAsset: str
  """Quote asset."""
  quotePrecision: int
  """Decimal precision of the quote asset. Deprecated; will be removed in a future API version (v4+) in favor of `quoteAssetPrecision`."""
  quoteAssetPrecision: int
  """Decimal precision of the quote asset."""
  baseCommissionPrecision: int
  """Decimal precision of base-asset commissions."""
  quoteCommissionPrecision: int
  """Decimal precision of quote-asset commissions."""
  orderTypes: list[
    Literal[
      'LIMIT',
      'MARKET',
      'STOP_LOSS',
      'STOP_LOSS_LIMIT',
      'TAKE_PROFIT',
      'TAKE_PROFIT_LIMIT',
      'LIMIT_MAKER',
    ]
  ]
  """Order types supported on this symbol."""
  icebergAllowed: bool
  """Whether iceberg orders are allowed on this symbol."""
  ocoAllowed: bool
  """Whether OCO order lists are allowed on this symbol."""
  otoAllowed: bool
  """Whether OTO order lists are allowed on this symbol."""
  opoAllowed: bool
  """Whether OPO (pegged) order lists are allowed on this symbol."""
  quoteOrderQtyMarketAllowed: bool
  """Whether MARKET orders may be sized by quote order quantity on this symbol."""
  allowTrailingStop: bool
  """Whether trailing-stop orders are allowed on this symbol."""
  cancelReplaceAllowed: bool
  """Whether cancel-replace is allowed on this symbol."""
  amendAllowed: bool
  """Whether order amendment is allowed on this symbol."""
  pegInstructionsAllowed: bool
  """Whether pegged-order instructions are allowed on this symbol."""
  isSpotTradingAllowed: bool
  """Whether spot trading is allowed on this symbol."""
  isMarginTradingAllowed: bool
  """Whether margin trading is allowed on this symbol."""
  filters: list[
    PriceFilter
    | PercentPriceFilter
    | PercentPriceBySideFilter
    | LotSizeFilter
    | MinNotionalFilter
    | NotionalFilter
    | IcebergPartsFilter
    | MarketLotSizeFilter
    | MaxNumOrdersFilter
    | MaxNumAlgoOrdersFilter
    | MaxNumIcebergOrdersFilter
    | MaxPositionFilter
    | TrailingDeltaFilter
    | MaxNumOrderAmendsFilter
    | MaxNumOrderListsFilter
  ]
  """Trading rule filters in effect for this symbol."""
  permissions: list[str]
  """Legacy permission list. Observed always empty in current responses; superseded by `permissionSets`."""
  permissionSets: list[list[str]]
  """Symbol permissions, as OR-of-AND groups: an account may trade this symbol if it holds every permission in at least one inner group. Populated only when the request's `showPermissionSets` is true (the default)."""
  defaultSelfTradePreventionMode: Literal[
    'NONE', 'EXPIRE_MAKER', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'DECREMENT', 'TRANSFER'
  ]
  """Self-trade prevention mode applied when an order does not specify one."""
  allowedSelfTradePreventionModes: list[
    Literal[
      'NONE', 'EXPIRE_MAKER', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'DECREMENT', 'TRANSFER'
    ]
  ]
  """Self-trade prevention modes an order on this symbol may request."""


class ExchangeInfo(TypedDict):
  """Current exchange trading rules and symbol information."""

  timezone: str
  """Timezone the exchange operates in."""
  serverTime: TimestampMillis
  """Current server time."""
  rateLimits: list[RateLimit]
  """The exchange's request-weight, order-count, and raw-request rate limits."""
  exchangeFilters: list[
    ExchangeMaxNumOrdersFilter
    | ExchangeMaxNumAlgoOrdersFilter
    | ExchangeMaxNumIcebergOrdersFilter
    | ExchangeMaxNumOrderListsFilter
  ]
  """Exchange-wide trading filters."""
  symbols: list[SpotSymbol]
  """Trading rules and metadata for each symbol in scope."""
  sors: NotRequired[list[SorGroup]]
  """Smart Order Routing groups. Present only when SOR is available."""


class ExchangeInfoEndpoint(RpcEndpoint):
  """Current exchange trading rules and symbol information. If the value provided to `symbol` or `symbols` does not exist, the endpoint raises an invalid-symbol error. No combination of `permissions` with `symbol`/`symbols` is allowed. If `permissions` is not provided, all symbols with `SPOT`, `MARGIN`, or `LEVERAGED` permission are exposed."""

  async def exchange_info(
    self,
    symbol: str | None = None,
    *,
    symbols: list[str] | None = None,
    permissions: str | list[str] | None = None,
    show_permission_sets: bool | None = None,
    symbol_status: Literal['TRADING', 'HALT', 'BREAK'] | None = None,
    validate: bool | None = None,
  ) -> ExchangeInfo:
    """Current exchange trading rules and symbol information. If the value provided to `symbol` or `symbols` does not exist, the endpoint raises an invalid-symbol error. No combination of `permissions` with `symbol`/`symbols` is allowed. If `permissions` is not provided, all symbols with `SPOT`, `MARGIN`, or `LEVERAGED` permission are exposed.

    Args:
      symbol: Single symbol to query.
      symbols: Symbols to query, e.g. ["BTCUSDT","BNBUSDT"].
      permissions: Account/symbol permission(s) to filter symbols by. Cannot be combined with `symbol` or `symbols`.
      show_permission_sets: Whether each symbol's `permissionSets` field is populated.
      symbol_status: Filter for symbols with this trading status. Cannot be combined with `symbol` or `symbols`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/rest-api.md#exchange-information)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    if symbols is not None:
      request['symbols'] = symbols
    if permissions is not None:
      request['permissions'] = permissions
    if show_permission_sets is not None:
      request['showPermissionSets'] = show_permission_sets
    if symbol_status is not None:
      request['symbolStatus'] = symbol_status
    return await self.request(
      request,
      method='GET',
      path='/api/v3/exchangeInfo',
      meta={'security': 'NONE', 'signed': False},
      validate=validate,
      request_type=Request,
      response_type=ExchangeInfo,
    )
