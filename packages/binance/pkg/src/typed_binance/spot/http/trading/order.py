# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class LimitMakerOrder(TypedDict):
  """A limit order that is rejected if it would immediately match as a taker. Requires quantity and price. If pegPriceType is set, price is not required by the venue despite being listed here as required; see the pegged-order notes below."""

  symbol: str
  """Trading pair symbol."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['LIMIT_MAKER']
  """Order type."""
  quantity: str
  """Order quantity, in the base asset."""
  price: str
  """Order price. Required for LIMIT, STOP_LOSS_LIMIT, TAKE_PROFIT_LIMIT and LIMIT_MAKER orders, unless pegPriceType is set."""
  newClientOrderId: NotRequired[str]
  """Caller-supplied id for the order. Automatically generated if not sent. An order sharing a newClientOrderId with an existing one is only accepted once the existing order has filled."""
  strategyId: NotRequired[int]
  """Id labeling the order strategy this order is part of."""
  strategyType: NotRequired[int]
  """Type labeling the order strategy this order is part of. Cannot be less than 1000000."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT', 'FULL']]
  """Response shape to return. MARKET and LIMIT orders default to FULL; all other order types default to ACK."""
  selfTradePreventionMode: NotRequired[
    Literal[
      'NONE', 'EXPIRE_MAKER', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'DECREMENT', 'TRANSFER'
    ]
  ]
  """Self-trade prevention mode. The allowed values depend on the symbol's configuration."""
  pegPriceType: NotRequired[Literal['PRIMARY_PEG', 'MARKET_PEG']]
  """Peg reference for a pegged order: the best same-side price (PRIMARY_PEG) or the best opposite-side price (MARKET_PEG). Usable with LIMIT, LIMIT_MAKER, STOP_LOSS_LIMIT and TAKE_PROFIT_LIMIT orders; makes price optional when set."""
  pegOffsetValue: NotRequired[int]
  """Price level offset from the peg reference, up to 100. Must be sent together with pegOffsetType."""
  pegOffsetType: NotRequired[Literal['PRICE_LEVEL']]
  """Peg offset type. Only PRICE_LEVEL is supported. Must be sent together with pegOffsetValue."""


class LimitOrder(TypedDict):
  """A limit order: filled at price or better. Requires timeInForce, quantity and price. If pegPriceType is set, price is not required by the venue despite being listed here as required by this variant's own required fields -- see the pegged-order notes below; this spec keeps price required for the common, non-pegged case rather than adding a further pegged/non-pegged split."""

  symbol: str
  """Trading pair symbol."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['LIMIT']
  """Order type."""
  timeInForce: Literal['GTC', 'IOC', 'FOK']
  """Time in force. Required for LIMIT, STOP_LOSS_LIMIT and TAKE_PROFIT_LIMIT orders."""
  quantity: str
  """Order quantity, in the base asset."""
  price: str
  """Order price. Required for LIMIT, STOP_LOSS_LIMIT, TAKE_PROFIT_LIMIT and LIMIT_MAKER orders, unless pegPriceType is set."""
  newClientOrderId: NotRequired[str]
  """Caller-supplied id for the order. Automatically generated if not sent. An order sharing a newClientOrderId with an existing one is only accepted once the existing order has filled."""
  strategyId: NotRequired[int]
  """Id labeling the order strategy this order is part of."""
  strategyType: NotRequired[int]
  """Type labeling the order strategy this order is part of. Cannot be less than 1000000."""
  icebergQty: NotRequired[str]
  """Quantity per leg of an iceberg order. Usable with LIMIT, STOP_LOSS_LIMIT and TAKE_PROFIT_LIMIT orders; any order carrying it must set timeInForce to GTC."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT', 'FULL']]
  """Response shape to return. MARKET and LIMIT orders default to FULL; all other order types default to ACK."""
  selfTradePreventionMode: NotRequired[
    Literal[
      'NONE', 'EXPIRE_MAKER', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'DECREMENT', 'TRANSFER'
    ]
  ]
  """Self-trade prevention mode. The allowed values depend on the symbol's configuration."""
  pegPriceType: NotRequired[Literal['PRIMARY_PEG', 'MARKET_PEG']]
  """Peg reference for a pegged order: the best same-side price (PRIMARY_PEG) or the best opposite-side price (MARKET_PEG). Usable with LIMIT, LIMIT_MAKER, STOP_LOSS_LIMIT and TAKE_PROFIT_LIMIT orders; makes price optional when set."""
  pegOffsetValue: NotRequired[int]
  """Price level offset from the peg reference, up to 100. Must be sent together with pegOffsetType."""
  pegOffsetType: NotRequired[Literal['PRICE_LEVEL']]
  """Peg offset type. Only PRICE_LEVEL is supported. Must be sent together with pegOffsetValue."""


class MarketOrder(TypedDict):
  """A market order: filled immediately at the best available price(s). Requires quantity (in the base asset) or quoteOrderQty (in the quote asset) -- the venue requires at least one of the two, which this variant leaves both optional to express rather than splitting further into quantity-only/quoteOrderQty-only sub-variants."""

  symbol: str
  """Trading pair symbol."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['MARKET']
  """Order type."""
  quantity: NotRequired[str]
  """Order quantity, in the base asset."""
  quoteOrderQty: NotRequired[str]
  """Order quantity, in the quote asset. Usable in place of quantity for MARKET orders."""
  newClientOrderId: NotRequired[str]
  """Caller-supplied id for the order. Automatically generated if not sent. An order sharing a newClientOrderId with an existing one is only accepted once the existing order has filled."""
  strategyId: NotRequired[int]
  """Id labeling the order strategy this order is part of."""
  strategyType: NotRequired[int]
  """Type labeling the order strategy this order is part of. Cannot be less than 1000000."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT', 'FULL']]
  """Response shape to return. MARKET and LIMIT orders default to FULL; all other order types default to ACK."""
  selfTradePreventionMode: NotRequired[
    Literal[
      'NONE', 'EXPIRE_MAKER', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'DECREMENT', 'TRANSFER'
    ]
  ]
  """Self-trade prevention mode. The allowed values depend on the symbol's configuration."""


class OrderAck(TypedDict):
  """Minimal order acknowledgement, returned when newOrderRespType is ACK."""

  symbol: str
  """Trading pair symbol."""
  orderId: int
  """Order id."""
  orderListId: int
  """Id of the order list this order belongs to, or -1 if it is not part of one."""
  clientOrderId: str
  """Client order id, either caller-supplied or venue-generated."""
  transactTime: TimestampMillis
  """Time the order was processed."""


class OrderFill(TypedDict):
  """One trade that filled part or all of this order."""

  price: str
  """Fill price."""
  qty: str
  """Filled quantity."""
  commission: str
  """Commission charged on the fill."""
  commissionAsset: str
  """Asset the commission was charged in."""
  tradeId: int
  """Trade id of the fill."""


class OrderResult(TypedDict):
  """Order acknowledgement with fill/status detail, returned when newOrderRespType is RESULT."""

  symbol: str
  """Trading pair symbol."""
  orderId: int
  """Order id."""
  orderListId: int
  """Id of the order list this order belongs to, or -1 if it is not part of one."""
  clientOrderId: str
  """Client order id, either caller-supplied or venue-generated."""
  transactTime: TimestampMillis
  """Time the order was processed."""
  price: str
  """Order price."""
  origQty: str
  """Original order quantity."""
  executedQty: str
  """Cumulative filled quantity."""
  origQuoteOrderQty: str
  """Original quoteOrderQty, or "0" when quoteOrderQty was not used."""
  cummulativeQuoteQty: str
  """Cumulative quote asset transacted quantity."""
  status: Literal[
    'NEW',
    'PENDING_NEW',
    'PARTIALLY_FILLED',
    'FILLED',
    'CANCELED',
    'PENDING_CANCEL',
    'REJECTED',
    'EXPIRED',
    'EXPIRED_IN_MATCH',
  ]
  """Order status."""
  timeInForce: Literal['GTC', 'IOC', 'FOK']
  """Time in force."""
  type: Literal[
    'LIMIT',
    'MARKET',
    'STOP_LOSS',
    'STOP_LOSS_LIMIT',
    'TAKE_PROFIT',
    'TAKE_PROFIT_LIMIT',
    'LIMIT_MAKER',
  ]
  """Order type."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  workingTime: TimestampMillis
  """Time the order started working on the order book."""
  selfTradePreventionMode: Literal[
    'NONE', 'EXPIRE_MAKER', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'DECREMENT', 'TRANSFER'
  ]
  """Self-trade prevention mode in effect for the order."""
  icebergQty: NotRequired[str]
  """Quantity for the iceberg order. Present only when icebergQty was sent in the request."""
  preventedMatchId: NotRequired[int]
  """Id of a prevented match, usable together with symbol to query it. Present only when the order expired due to self-trade prevention."""
  preventedQuantity: NotRequired[str]
  """Order quantity that expired due to self-trade prevention. Present only when the order expired due to self-trade prevention."""
  stopPrice: NotRequired[str]
  """Price at which the algorithmic order triggers. Present for STOP_LOSS, TAKE_PROFIT, STOP_LOSS_LIMIT and TAKE_PROFIT_LIMIT orders."""
  strategyId: NotRequired[int]
  """Id labeling the order strategy this order is part of. Present when strategyId was sent in the request."""
  strategyType: NotRequired[int]
  """Type labeling the order strategy this order is part of. Present when strategyType was sent in the request."""
  trailingDelta: NotRequired[int]
  """Delta price change required before order activation. Present for trailing stop orders."""
  trailingTime: NotRequired[TimestampMillis]
  """Time the trailing order became active and started tracking price changes. Present only for trailing stop orders."""
  usedSor: NotRequired[bool]
  """Whether the order used smart order routing (SOR). Present when the order was placed using SOR."""
  workingFloor: NotRequired[Literal['EXCHANGE', 'SOR']]
  """Whether the order is being filled by SOR or by the order book it was submitted to. Present when the order was placed using SOR."""
  pegPriceType: NotRequired[Literal['PRIMARY_PEG', 'MARKET_PEG']]
  """Peg reference type. Present only for pegged orders."""
  pegOffsetType: NotRequired[Literal['PRICE_LEVEL']]
  """Peg offset type. Present only for pegged orders that requested one."""
  pegOffsetValue: NotRequired[int]
  """Peg offset value. Present only for pegged orders that requested one."""
  peggedPrice: NotRequired[str]
  """Current price the order is pegged at. Present only for pegged orders, once determined."""
  expiryReason: NotRequired[
    Literal[
      'NONE',
      'REJECTED',
      'EXCHANGE_CANCELED',
      'OCO_TRIGGER',
      'OTO_PHASE_ONE_EXPIRED',
      'UNFILLED_IOC_QUANTITY_EXPIRED',
      'UNFILLED_FOK_ORDER_EXPIRED',
      'INSUFFICIENT_LIQUIDITY',
      'EXECUTION_RULE_PRICE_RANGE_EXCEEDED',
    ]
  ]
  """Cause of the order's expiration. Present when the order has expired."""


class StopLossLimitOrder(TypedDict):
  """A stop-loss order that becomes a limit order once stopPrice trades. Requires timeInForce, quantity and price, and either stopPrice or trailingDelta -- the venue requires at least one of the two, which this variant leaves both optional to express. If pegPriceType is set, price is not required by the venue despite being listed here as required; see the pegged-order notes below."""

  symbol: str
  """Trading pair symbol."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['STOP_LOSS_LIMIT']
  """Order type."""
  timeInForce: Literal['GTC', 'IOC', 'FOK']
  """Time in force. Required for LIMIT, STOP_LOSS_LIMIT and TAKE_PROFIT_LIMIT orders."""
  quantity: str
  """Order quantity, in the base asset."""
  price: str
  """Order price. Required for LIMIT, STOP_LOSS_LIMIT, TAKE_PROFIT_LIMIT and LIMIT_MAKER orders, unless pegPriceType is set."""
  stopPrice: NotRequired[str]
  """Price at which a STOP_LOSS, STOP_LOSS_LIMIT, TAKE_PROFIT or TAKE_PROFIT_LIMIT order triggers."""
  trailingDelta: NotRequired[int]
  """Delta for a trailing stop order, combinable with stopPrice."""
  icebergQty: NotRequired[str]
  """Quantity per leg of an iceberg order. Usable with LIMIT, STOP_LOSS_LIMIT and TAKE_PROFIT_LIMIT orders; any order carrying it must set timeInForce to GTC."""
  newClientOrderId: NotRequired[str]
  """Caller-supplied id for the order. Automatically generated if not sent. An order sharing a newClientOrderId with an existing one is only accepted once the existing order has filled."""
  strategyId: NotRequired[int]
  """Id labeling the order strategy this order is part of."""
  strategyType: NotRequired[int]
  """Type labeling the order strategy this order is part of. Cannot be less than 1000000."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT', 'FULL']]
  """Response shape to return. MARKET and LIMIT orders default to FULL; all other order types default to ACK."""
  selfTradePreventionMode: NotRequired[
    Literal[
      'NONE', 'EXPIRE_MAKER', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'DECREMENT', 'TRANSFER'
    ]
  ]
  """Self-trade prevention mode. The allowed values depend on the symbol's configuration."""
  pegPriceType: NotRequired[Literal['PRIMARY_PEG', 'MARKET_PEG']]
  """Peg reference for a pegged order: the best same-side price (PRIMARY_PEG) or the best opposite-side price (MARKET_PEG). Usable with LIMIT, LIMIT_MAKER, STOP_LOSS_LIMIT and TAKE_PROFIT_LIMIT orders; makes price optional when set."""
  pegOffsetValue: NotRequired[int]
  """Price level offset from the peg reference, up to 100. Must be sent together with pegOffsetType."""
  pegOffsetType: NotRequired[Literal['PRICE_LEVEL']]
  """Peg offset type. Only PRICE_LEVEL is supported. Must be sent together with pegOffsetValue."""


class StopLossOrder(TypedDict):
  """A stop-loss order: becomes a market order once stopPrice trades. Requires quantity, and either stopPrice or trailingDelta -- the venue requires at least one of the two, which this variant leaves both optional to express."""

  symbol: str
  """Trading pair symbol."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['STOP_LOSS']
  """Order type."""
  quantity: str
  """Order quantity, in the base asset."""
  stopPrice: NotRequired[str]
  """Price at which a STOP_LOSS, STOP_LOSS_LIMIT, TAKE_PROFIT or TAKE_PROFIT_LIMIT order triggers."""
  trailingDelta: NotRequired[int]
  """Delta for a trailing stop order, combinable with stopPrice."""
  newClientOrderId: NotRequired[str]
  """Caller-supplied id for the order. Automatically generated if not sent. An order sharing a newClientOrderId with an existing one is only accepted once the existing order has filled."""
  strategyId: NotRequired[int]
  """Id labeling the order strategy this order is part of."""
  strategyType: NotRequired[int]
  """Type labeling the order strategy this order is part of. Cannot be less than 1000000."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT', 'FULL']]
  """Response shape to return. MARKET and LIMIT orders default to FULL; all other order types default to ACK."""
  selfTradePreventionMode: NotRequired[
    Literal[
      'NONE', 'EXPIRE_MAKER', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'DECREMENT', 'TRANSFER'
    ]
  ]
  """Self-trade prevention mode. The allowed values depend on the symbol's configuration."""


class TakeProfitLimitOrder(TypedDict):
  """A take-profit order that becomes a limit order once stopPrice trades. Requires timeInForce, quantity and price, and either stopPrice or trailingDelta -- the venue requires at least one of the two, which this variant leaves both optional to express. If pegPriceType is set, price is not required by the venue despite being listed here as required; see the pegged-order notes below."""

  symbol: str
  """Trading pair symbol."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['TAKE_PROFIT_LIMIT']
  """Order type."""
  timeInForce: Literal['GTC', 'IOC', 'FOK']
  """Time in force. Required for LIMIT, STOP_LOSS_LIMIT and TAKE_PROFIT_LIMIT orders."""
  quantity: str
  """Order quantity, in the base asset."""
  price: str
  """Order price. Required for LIMIT, STOP_LOSS_LIMIT, TAKE_PROFIT_LIMIT and LIMIT_MAKER orders, unless pegPriceType is set."""
  stopPrice: NotRequired[str]
  """Price at which a STOP_LOSS, STOP_LOSS_LIMIT, TAKE_PROFIT or TAKE_PROFIT_LIMIT order triggers."""
  trailingDelta: NotRequired[int]
  """Delta for a trailing stop order, combinable with stopPrice."""
  icebergQty: NotRequired[str]
  """Quantity per leg of an iceberg order. Usable with LIMIT, STOP_LOSS_LIMIT and TAKE_PROFIT_LIMIT orders; any order carrying it must set timeInForce to GTC."""
  newClientOrderId: NotRequired[str]
  """Caller-supplied id for the order. Automatically generated if not sent. An order sharing a newClientOrderId with an existing one is only accepted once the existing order has filled."""
  strategyId: NotRequired[int]
  """Id labeling the order strategy this order is part of."""
  strategyType: NotRequired[int]
  """Type labeling the order strategy this order is part of. Cannot be less than 1000000."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT', 'FULL']]
  """Response shape to return. MARKET and LIMIT orders default to FULL; all other order types default to ACK."""
  selfTradePreventionMode: NotRequired[
    Literal[
      'NONE', 'EXPIRE_MAKER', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'DECREMENT', 'TRANSFER'
    ]
  ]
  """Self-trade prevention mode. The allowed values depend on the symbol's configuration."""
  pegPriceType: NotRequired[Literal['PRIMARY_PEG', 'MARKET_PEG']]
  """Peg reference for a pegged order: the best same-side price (PRIMARY_PEG) or the best opposite-side price (MARKET_PEG). Usable with LIMIT, LIMIT_MAKER, STOP_LOSS_LIMIT and TAKE_PROFIT_LIMIT orders; makes price optional when set."""
  pegOffsetValue: NotRequired[int]
  """Price level offset from the peg reference, up to 100. Must be sent together with pegOffsetType."""
  pegOffsetType: NotRequired[Literal['PRICE_LEVEL']]
  """Peg offset type. Only PRICE_LEVEL is supported. Must be sent together with pegOffsetValue."""


class TakeProfitOrder(TypedDict):
  """A take-profit order: becomes a market order once stopPrice trades. Requires quantity, and either stopPrice or trailingDelta -- the venue requires at least one of the two, which this variant leaves both optional to express."""

  symbol: str
  """Trading pair symbol."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['TAKE_PROFIT']
  """Order type."""
  quantity: str
  """Order quantity, in the base asset."""
  stopPrice: NotRequired[str]
  """Price at which a STOP_LOSS, STOP_LOSS_LIMIT, TAKE_PROFIT or TAKE_PROFIT_LIMIT order triggers."""
  trailingDelta: NotRequired[int]
  """Delta for a trailing stop order, combinable with stopPrice."""
  newClientOrderId: NotRequired[str]
  """Caller-supplied id for the order. Automatically generated if not sent. An order sharing a newClientOrderId with an existing one is only accepted once the existing order has filled."""
  strategyId: NotRequired[int]
  """Id labeling the order strategy this order is part of."""
  strategyType: NotRequired[int]
  """Type labeling the order strategy this order is part of. Cannot be less than 1000000."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT', 'FULL']]
  """Response shape to return. MARKET and LIMIT orders default to FULL; all other order types default to ACK."""
  selfTradePreventionMode: NotRequired[
    Literal[
      'NONE', 'EXPIRE_MAKER', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'DECREMENT', 'TRANSFER'
    ]
  ]
  """Self-trade prevention mode. The allowed values depend on the symbol's configuration."""


class OrderFull(TypedDict):
  """Full order acknowledgement with fill detail, returned when newOrderRespType is FULL."""

  symbol: str
  """Trading pair symbol."""
  orderId: int
  """Order id."""
  orderListId: int
  """Id of the order list this order belongs to, or -1 if it is not part of one."""
  clientOrderId: str
  """Client order id, either caller-supplied or venue-generated."""
  transactTime: TimestampMillis
  """Time the order was processed."""
  price: str
  """Order price."""
  origQty: str
  """Original order quantity."""
  executedQty: str
  """Cumulative filled quantity."""
  origQuoteOrderQty: str
  """Original quoteOrderQty, or "0" when quoteOrderQty was not used."""
  cummulativeQuoteQty: str
  """Cumulative quote asset transacted quantity."""
  status: Literal[
    'NEW',
    'PENDING_NEW',
    'PARTIALLY_FILLED',
    'FILLED',
    'CANCELED',
    'PENDING_CANCEL',
    'REJECTED',
    'EXPIRED',
    'EXPIRED_IN_MATCH',
  ]
  """Order status."""
  timeInForce: Literal['GTC', 'IOC', 'FOK']
  """Time in force."""
  type: Literal[
    'LIMIT',
    'MARKET',
    'STOP_LOSS',
    'STOP_LOSS_LIMIT',
    'TAKE_PROFIT',
    'TAKE_PROFIT_LIMIT',
    'LIMIT_MAKER',
  ]
  """Order type."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  workingTime: TimestampMillis
  """Time the order started working on the order book."""
  selfTradePreventionMode: Literal[
    'NONE', 'EXPIRE_MAKER', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'DECREMENT', 'TRANSFER'
  ]
  """Self-trade prevention mode in effect for the order."""
  icebergQty: NotRequired[str]
  """Quantity for the iceberg order. Present only when icebergQty was sent in the request."""
  preventedMatchId: NotRequired[int]
  """Id of a prevented match, usable together with symbol to query it. Present only when the order expired due to self-trade prevention."""
  preventedQuantity: NotRequired[str]
  """Order quantity that expired due to self-trade prevention. Present only when the order expired due to self-trade prevention."""
  stopPrice: NotRequired[str]
  """Price at which the algorithmic order triggers. Present for STOP_LOSS, TAKE_PROFIT, STOP_LOSS_LIMIT and TAKE_PROFIT_LIMIT orders."""
  strategyId: NotRequired[int]
  """Id labeling the order strategy this order is part of. Present when strategyId was sent in the request."""
  strategyType: NotRequired[int]
  """Type labeling the order strategy this order is part of. Present when strategyType was sent in the request."""
  trailingDelta: NotRequired[int]
  """Delta price change required before order activation. Present for trailing stop orders."""
  trailingTime: NotRequired[TimestampMillis]
  """Time the trailing order became active and started tracking price changes. Present only for trailing stop orders."""
  usedSor: NotRequired[bool]
  """Whether the order used smart order routing (SOR). Present when the order was placed using SOR."""
  workingFloor: NotRequired[Literal['EXCHANGE', 'SOR']]
  """Whether the order is being filled by SOR or by the order book it was submitted to. Present when the order was placed using SOR."""
  pegPriceType: NotRequired[Literal['PRIMARY_PEG', 'MARKET_PEG']]
  """Peg reference type. Present only for pegged orders."""
  pegOffsetType: NotRequired[Literal['PRICE_LEVEL']]
  """Peg offset type. Present only for pegged orders that requested one."""
  pegOffsetValue: NotRequired[int]
  """Peg offset value. Present only for pegged orders that requested one."""
  peggedPrice: NotRequired[str]
  """Current price the order is pegged at. Present only for pegged orders, once determined."""
  expiryReason: NotRequired[
    Literal[
      'NONE',
      'REJECTED',
      'EXCHANGE_CANCELED',
      'OCO_TRIGGER',
      'OTO_PHASE_ONE_EXPIRED',
      'UNFILLED_IOC_QUANTITY_EXPIRED',
      'UNFILLED_FOK_ORDER_EXPIRED',
      'INSUFFICIENT_LIQUIDITY',
      'EXECUTION_RULE_PRICE_RANGE_EXCEEDED',
    ]
  ]
  """Cause of the order's expiration. Present when the order has expired."""
  fills: list[OrderFill]
  """Trades that filled this order."""


Request = (
  LimitOrder
  | MarketOrder
  | StopLossOrder
  | StopLossLimitOrder
  | TakeProfitOrder
  | TakeProfitLimitOrder
  | LimitMakerOrder
)


Response = OrderAck | OrderResult | OrderFull


class Order(RpcEndpoint):
  """Place a new spot order. The request body shape depends on type: LIMIT, MARKET, STOP_LOSS, STOP_LOSS_LIMIT, TAKE_PROFIT, TAKE_PROFIT_LIMIT and LIMIT_MAKER each require a different set of additional fields -- see the variant descriptions below and upstream.md for the full type-conditional parameter table and Pegged Orders notes. Pegged-order parameters (pegPriceType, pegOffsetType, pegOffsetValue) are usable with LIMIT, LIMIT_MAKER, STOP_LOSS_LIMIT and TAKE_PROFIT_LIMIT orders."""

  async def order(
    self,
    trading_order_request: Request,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Place a new spot order. The request body shape depends on type: LIMIT, MARKET, STOP_LOSS, STOP_LOSS_LIMIT, TAKE_PROFIT, TAKE_PROFIT_LIMIT and LIMIT_MAKER each require a different set of additional fields -- see the variant descriptions below and upstream.md for the full type-conditional parameter table and Pegged Orders notes. Pegged-order parameters (pegPriceType, pegOffsetType, pegOffsetValue) are usable with LIMIT, LIMIT_MAKER, STOP_LOSS_LIMIT and TAKE_PROFIT_LIMIT orders.

    Args:
      trading_order_request:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/rest-api.md)
    """
    return await self.request(
      trading_order_request,
      method='POST',
      path='/api/v3/order',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
