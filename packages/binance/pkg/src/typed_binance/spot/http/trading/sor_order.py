# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class SorLimitOrder(TypedDict):
  """A limit order routed via smart order routing (SOR): filled at price or better, against liquidity from multiple order books. Requires timeInForce, quantity and price."""

  symbol: str
  """Trading pair symbol."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['LIMIT']
  """Order type. Only LIMIT and MARKET are supported by SOR."""
  timeInForce: Literal['GTC', 'IOC', 'FOK']
  """Time in force. Required for LIMIT orders."""
  quantity: str
  """Order quantity, in the base asset. quoteOrderQty is not supported by SOR."""
  price: str
  """Order price. Required for LIMIT orders."""
  icebergQty: NotRequired[str]
  """Quantity per leg of an iceberg order. Usable with LIMIT orders; requires timeInForce GTC."""
  newClientOrderId: NotRequired[str]
  """Caller-supplied id for the order. Automatically generated if not sent. An order sharing a newClientOrderId with an existing one is only accepted once the existing order has filled."""
  strategyId: NotRequired[int]
  """Id labeling the order strategy this order is part of."""
  strategyType: NotRequired[int]
  """Type labeling the order strategy this order is part of. Cannot be less than 1000000."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT', 'FULL']]
  """Response shape to return. Defaults to FULL."""
  selfTradePreventionMode: NotRequired[
    Literal[
      'NONE', 'EXPIRE_MAKER', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'DECREMENT', 'TRANSFER'
    ]
  ]
  """Self-trade prevention mode. The allowed values depend on the symbol's configuration."""


class SorMarketOrder(TypedDict):
  """A market order routed via smart order routing (SOR): filled immediately at the best available price(s) across multiple order books. Requires quantity; quoteOrderQty is not supported by SOR."""

  symbol: str
  """Trading pair symbol."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['MARKET']
  """Order type. Only LIMIT and MARKET are supported by SOR."""
  quantity: str
  """Order quantity, in the base asset. quoteOrderQty is not supported by SOR."""
  newClientOrderId: NotRequired[str]
  """Caller-supplied id for the order. Automatically generated if not sent. An order sharing a newClientOrderId with an existing one is only accepted once the existing order has filled."""
  strategyId: NotRequired[int]
  """Id labeling the order strategy this order is part of."""
  strategyType: NotRequired[int]
  """Type labeling the order strategy this order is part of. Cannot be less than 1000000."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT', 'FULL']]
  """Response shape to return. Defaults to FULL."""
  selfTradePreventionMode: NotRequired[
    Literal[
      'NONE', 'EXPIRE_MAKER', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'DECREMENT', 'TRANSFER'
    ]
  ]
  """Self-trade prevention mode. The allowed values depend on the symbol's configuration."""


class SorOrderAck(TypedDict):
  """Minimal order acknowledgement, returned when newOrderRespType is ACK."""

  symbol: str
  """Trading pair symbol."""
  orderId: int
  """Order id."""
  orderListId: int
  """Id of the order list this order belongs to, or -1 if it is not part of one."""
  clientOrderId: str
  """Client order id, either caller-supplied or venue-generated."""
  transactTime: TimestampMillis
  """Time the order was processed."""


class SorOrderFill(TypedDict):
  """One trade that filled part or all of this order, via SOR."""

  price: str
  """Fill price."""
  qty: str
  """Filled quantity."""
  commission: str
  """Commission charged on the fill."""
  commissionAsset: str
  """Asset the commission was charged in."""
  tradeId: int
  """Trade id of the fill."""
  matchType: str
  """Type of match that produced this fill, e.g. ONE_PARTY_TRADE_REPORT. Not a documented closed set (absent from enums.md and rest-api.md's prose), so left bare per authoring.md rule 2."""
  allocId: int
  """Allocation id of the fill."""


class SorOrderResult(TypedDict):
  """Order acknowledgement with fill/status detail, returned when newOrderRespType is RESULT. workingFloor and usedSor are always present, since every order placed through this endpoint is routed by SOR."""

  symbol: str
  """Trading pair symbol."""
  orderId: int
  """Order id."""
  orderListId: int
  """Id of the order list this order belongs to, or -1 if it is not part of one."""
  clientOrderId: str
  """Client order id, either caller-supplied or venue-generated."""
  transactTime: TimestampMillis
  """Time the order was processed."""
  price: str
  """Order price."""
  origQty: str
  """Original order quantity."""
  executedQty: str
  """Cumulative filled quantity."""
  origQuoteOrderQty: str
  """Original quoteOrderQty, or "0" when quoteOrderQty was not used."""
  cummulativeQuoteQty: str
  """Cumulative quote asset transacted quantity."""
  status: Literal[
    'NEW',
    'PENDING_NEW',
    'PARTIALLY_FILLED',
    'FILLED',
    'CANCELED',
    'PENDING_CANCEL',
    'REJECTED',
    'EXPIRED',
    'EXPIRED_IN_MATCH',
  ]
  """Order status."""
  timeInForce: Literal['GTC', 'IOC', 'FOK']
  """Time in force."""
  type: Literal[
    'LIMIT',
    'MARKET',
    'STOP_LOSS',
    'STOP_LOSS_LIMIT',
    'TAKE_PROFIT',
    'TAKE_PROFIT_LIMIT',
    'LIMIT_MAKER',
  ]
  """Order type."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  workingTime: TimestampMillis
  """Time the order started working on the order book."""
  selfTradePreventionMode: Literal[
    'NONE', 'EXPIRE_MAKER', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'DECREMENT', 'TRANSFER'
  ]
  """Self-trade prevention mode in effect for the order."""
  icebergQty: NotRequired[str]
  """Quantity for the iceberg order. Present only when icebergQty was sent in the request."""
  preventedMatchId: NotRequired[int]
  """Id of a prevented match, usable together with symbol to query it. Present only when the order expired due to self-trade prevention."""
  preventedQuantity: NotRequired[str]
  """Order quantity that expired due to self-trade prevention. Present only when the order expired due to self-trade prevention."""
  stopPrice: NotRequired[str]
  """Price at which the algorithmic order triggers. Present for STOP_LOSS, TAKE_PROFIT, STOP_LOSS_LIMIT and TAKE_PROFIT_LIMIT orders."""
  strategyId: NotRequired[int]
  """Id labeling the order strategy this order is part of. Present when strategyId was sent in the request."""
  strategyType: NotRequired[int]
  """Type labeling the order strategy this order is part of. Present when strategyType was sent in the request."""
  trailingDelta: NotRequired[int]
  """Delta price change required before order activation. Present for trailing stop orders."""
  trailingTime: NotRequired[TimestampMillis]
  """Time the trailing order became active and started tracking price changes. Present only for trailing stop orders."""
  usedSor: bool
  """Whether the order used smart order routing (SOR). Present when the order was placed using SOR."""
  workingFloor: Literal['EXCHANGE', 'SOR']
  """Whether the order is being filled by SOR or by the order book it was submitted to. Present when the order was placed using SOR."""
  pegPriceType: NotRequired[Literal['PRIMARY_PEG', 'MARKET_PEG']]
  """Peg reference type. Present only for pegged orders."""
  pegOffsetType: NotRequired[Literal['PRICE_LEVEL']]
  """Peg offset type. Present only for pegged orders that requested one."""
  pegOffsetValue: NotRequired[int]
  """Peg offset value. Present only for pegged orders that requested one."""
  peggedPrice: NotRequired[str]
  """Current price the order is pegged at. Present only for pegged orders, once determined."""
  expiryReason: NotRequired[
    Literal[
      'NONE',
      'REJECTED',
      'EXCHANGE_CANCELED',
      'OCO_TRIGGER',
      'OTO_PHASE_ONE_EXPIRED',
      'UNFILLED_IOC_QUANTITY_EXPIRED',
      'UNFILLED_FOK_ORDER_EXPIRED',
      'INSUFFICIENT_LIQUIDITY',
      'EXECUTION_RULE_PRICE_RANGE_EXCEEDED',
    ]
  ]
  """Cause of the order's expiration. Present when the order has expired."""


class SorOrderFull(TypedDict):
  """Full order acknowledgement with fill detail, returned when newOrderRespType is FULL. workingFloor and usedSor are always present, since every order placed through this endpoint is routed by SOR."""

  symbol: str
  """Trading pair symbol."""
  orderId: int
  """Order id."""
  orderListId: int
  """Id of the order list this order belongs to, or -1 if it is not part of one."""
  clientOrderId: str
  """Client order id, either caller-supplied or venue-generated."""
  transactTime: TimestampMillis
  """Time the order was processed."""
  price: str
  """Order price."""
  origQty: str
  """Original order quantity."""
  executedQty: str
  """Cumulative filled quantity."""
  origQuoteOrderQty: str
  """Original quoteOrderQty, or "0" when quoteOrderQty was not used."""
  cummulativeQuoteQty: str
  """Cumulative quote asset transacted quantity."""
  status: Literal[
    'NEW',
    'PENDING_NEW',
    'PARTIALLY_FILLED',
    'FILLED',
    'CANCELED',
    'PENDING_CANCEL',
    'REJECTED',
    'EXPIRED',
    'EXPIRED_IN_MATCH',
  ]
  """Order status."""
  timeInForce: Literal['GTC', 'IOC', 'FOK']
  """Time in force."""
  type: Literal[
    'LIMIT',
    'MARKET',
    'STOP_LOSS',
    'STOP_LOSS_LIMIT',
    'TAKE_PROFIT',
    'TAKE_PROFIT_LIMIT',
    'LIMIT_MAKER',
  ]
  """Order type."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  workingTime: TimestampMillis
  """Time the order started working on the order book."""
  selfTradePreventionMode: Literal[
    'NONE', 'EXPIRE_MAKER', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'DECREMENT', 'TRANSFER'
  ]
  """Self-trade prevention mode in effect for the order."""
  icebergQty: NotRequired[str]
  """Quantity for the iceberg order. Present only when icebergQty was sent in the request."""
  preventedMatchId: NotRequired[int]
  """Id of a prevented match, usable together with symbol to query it. Present only when the order expired due to self-trade prevention."""
  preventedQuantity: NotRequired[str]
  """Order quantity that expired due to self-trade prevention. Present only when the order expired due to self-trade prevention."""
  stopPrice: NotRequired[str]
  """Price at which the algorithmic order triggers. Present for STOP_LOSS, TAKE_PROFIT, STOP_LOSS_LIMIT and TAKE_PROFIT_LIMIT orders."""
  strategyId: NotRequired[int]
  """Id labeling the order strategy this order is part of. Present when strategyId was sent in the request."""
  strategyType: NotRequired[int]
  """Type labeling the order strategy this order is part of. Present when strategyType was sent in the request."""
  trailingDelta: NotRequired[int]
  """Delta price change required before order activation. Present for trailing stop orders."""
  trailingTime: NotRequired[TimestampMillis]
  """Time the trailing order became active and started tracking price changes. Present only for trailing stop orders."""
  usedSor: bool
  """Whether the order used smart order routing (SOR). Present when the order was placed using SOR."""
  workingFloor: Literal['EXCHANGE', 'SOR']
  """Whether the order is being filled by SOR or by the order book it was submitted to. Present when the order was placed using SOR."""
  pegPriceType: NotRequired[Literal['PRIMARY_PEG', 'MARKET_PEG']]
  """Peg reference type. Present only for pegged orders."""
  pegOffsetType: NotRequired[Literal['PRICE_LEVEL']]
  """Peg offset type. Present only for pegged orders that requested one."""
  pegOffsetValue: NotRequired[int]
  """Peg offset value. Present only for pegged orders that requested one."""
  peggedPrice: NotRequired[str]
  """Current price the order is pegged at. Present only for pegged orders, once determined."""
  expiryReason: NotRequired[
    Literal[
      'NONE',
      'REJECTED',
      'EXCHANGE_CANCELED',
      'OCO_TRIGGER',
      'OTO_PHASE_ONE_EXPIRED',
      'UNFILLED_IOC_QUANTITY_EXPIRED',
      'UNFILLED_FOK_ORDER_EXPIRED',
      'INSUFFICIENT_LIQUIDITY',
      'EXECUTION_RULE_PRICE_RANGE_EXCEEDED',
    ]
  ]
  """Cause of the order's expiration. Present when the order has expired."""
  fills: list[SorOrderFill]
  """Trades that filled this order."""


class SorOrder(RpcEndpoint):
  """New order using SOR"""

  async def sor_order(
    self,
    sor_limit_order_sor_market_order: SorLimitOrder | SorMarketOrder,
    *,
    validate: bool | None = None,
  ) -> SorOrderAck | SorOrderResult | SorOrderFull:
    """Place a new spot order using smart order routing (SOR), which can fill against liquidity from multiple order books. Only LIMIT and MARKET order types are supported; quoteOrderQty is not supported (quantity is always required).

    Args:
      sor_limit_order_sor_market_order: New SOR order request. Shape depends on order type (LIMIT or MARKET); see docs/spec/authoring.md rule 0 -- a discriminated-union payload whose required fields differ by variant.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/rest-api.md)
    """
    params = dict(sor_limit_order_sor_market_order)
    _Response = SorOrderAck | SorOrderResult | SorOrderFull
    _validator = validator[_Response](_Response)  # type: ignore
    return await self.authed_request(
      'POST',
      '/api/v3/sor/order',
      params=params,
      validator=_validator,
      validate=validate,
    )
