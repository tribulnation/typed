# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class OrderListOpoOrderReportAck(TypedDict):
  """Minimal order-list leg acknowledgement, returned when newOrderRespType is ACK."""

  symbol: str
  """Trading pair symbol."""
  orderId: int
  """Order id."""
  orderListId: int
  """Id of the order list this order belongs to."""
  clientOrderId: str
  """Client order id, either caller-supplied or venue-generated."""
  transactTime: TimestampMillis
  """Time the order was processed."""


class OrderListOpoOrderReportFill(TypedDict):
  """One trade that filled part or all of this order-list leg."""

  price: str
  """Fill price."""
  qty: str
  """Filled quantity."""
  commission: str
  """Commission charged on the fill."""
  commissionAsset: str
  """Asset the commission was charged in."""
  tradeId: int
  """Trade id of the fill."""


class OrderListOpoOrderReportResult(TypedDict):
  """Order-list leg acknowledgement with fill/status detail, returned when newOrderRespType is RESULT."""

  symbol: str
  """Trading pair symbol."""
  orderId: int
  """Order id."""
  orderListId: int
  """Id of the order list this order belongs to."""
  clientOrderId: str
  """Client order id, either caller-supplied or venue-generated."""
  transactTime: TimestampMillis
  """Time the order was processed."""
  price: str
  """Order price."""
  origQty: str
  """Original order quantity."""
  executedQty: str
  """Cumulative filled quantity."""
  origQuoteOrderQty: str
  """Original quoteOrderQty, or "0" when quoteOrderQty was not used."""
  cummulativeQuoteQty: str
  """Cumulative quote asset transacted quantity."""
  status: Literal[
    'NEW',
    'PENDING_NEW',
    'PARTIALLY_FILLED',
    'FILLED',
    'CANCELED',
    'PENDING_CANCEL',
    'REJECTED',
    'EXPIRED',
    'EXPIRED_IN_MATCH',
  ]
  """Order status."""
  timeInForce: Literal['GTC', 'IOC', 'FOK']
  """Time in force."""
  type: Literal[
    'LIMIT',
    'MARKET',
    'STOP_LOSS',
    'STOP_LOSS_LIMIT',
    'TAKE_PROFIT',
    'TAKE_PROFIT_LIMIT',
    'LIMIT_MAKER',
  ]
  """Order type."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  workingTime: TimestampMillis
  """Time the order started working on the order book. -1 if not yet working."""
  selfTradePreventionMode: Literal[
    'NONE', 'EXPIRE_MAKER', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'DECREMENT', 'TRANSFER'
  ]
  """Self-trade prevention mode in effect for the order."""
  stopPrice: NotRequired[str]
  """Price at which the algorithmic order triggers. Present for STOP_LOSS, TAKE_PROFIT, STOP_LOSS_LIMIT and TAKE_PROFIT_LIMIT orders."""
  icebergQty: NotRequired[str]
  """Quantity for the iceberg order. Present only when an iceberg quantity was sent in the request."""
  strategyId: NotRequired[int]
  """Id labeling the order strategy this order is part of. Present when a strategy id was sent in the request."""
  strategyType: NotRequired[int]
  """Type labeling the order strategy this order is part of. Present when a strategy type was sent in the request."""
  trailingDelta: NotRequired[int]
  """Delta price change required before order activation. Present for trailing stop orders."""
  trailingTime: NotRequired[TimestampMillis]
  """Time the trailing order became active and started tracking price changes. Present only for trailing stop orders."""
  pegPriceType: NotRequired[Literal['PRIMARY_PEG', 'MARKET_PEG']]
  """Peg reference type. Present only for pegged orders."""
  pegOffsetType: NotRequired[Literal['PRICE_LEVEL']]
  """Peg offset type. Present only for pegged orders that requested one."""
  pegOffsetValue: NotRequired[int]
  """Peg offset value. Present only for pegged orders that requested one."""
  peggedPrice: NotRequired[str]
  """Current price the order is pegged at. Present only for pegged orders, once determined."""
  expiryReason: NotRequired[
    Literal[
      'NONE',
      'REJECTED',
      'EXCHANGE_CANCELED',
      'OCO_TRIGGER',
      'OTO_PHASE_ONE_EXPIRED',
      'UNFILLED_IOC_QUANTITY_EXPIRED',
      'UNFILLED_FOK_ORDER_EXPIRED',
      'INSUFFICIENT_LIQUIDITY',
      'EXECUTION_RULE_PRICE_RANGE_EXCEEDED',
    ]
  ]
  """Cause of the order's expiration. Present when the order has expired."""
  preventedMatchId: NotRequired[int]
  """Id of a prevented match, usable together with symbol to query it. Present only when the order expired due to self-trade prevention."""
  preventedQuantity: NotRequired[str]
  """Order quantity that expired due to self-trade prevention. Present only when the order expired due to self-trade prevention."""


class OrderListOrder(TypedDict):
  """One order belonging to the order list, identified minimally."""

  symbol: str
  """Trading pair symbol."""
  orderId: int
  """Order id."""
  clientOrderId: str
  """Client order id, either caller-supplied or venue-generated."""


class OrderListOpoOrderReportFull(TypedDict):
  """Full order-list leg acknowledgement with fill detail, returned when newOrderRespType is FULL."""

  symbol: str
  """Trading pair symbol."""
  orderId: int
  """Order id."""
  orderListId: int
  """Id of the order list this order belongs to."""
  clientOrderId: str
  """Client order id, either caller-supplied or venue-generated."""
  transactTime: TimestampMillis
  """Time the order was processed."""
  price: str
  """Order price."""
  origQty: str
  """Original order quantity."""
  executedQty: str
  """Cumulative filled quantity."""
  origQuoteOrderQty: str
  """Original quoteOrderQty, or "0" when quoteOrderQty was not used."""
  cummulativeQuoteQty: str
  """Cumulative quote asset transacted quantity."""
  status: Literal[
    'NEW',
    'PENDING_NEW',
    'PARTIALLY_FILLED',
    'FILLED',
    'CANCELED',
    'PENDING_CANCEL',
    'REJECTED',
    'EXPIRED',
    'EXPIRED_IN_MATCH',
  ]
  """Order status."""
  timeInForce: Literal['GTC', 'IOC', 'FOK']
  """Time in force."""
  type: Literal[
    'LIMIT',
    'MARKET',
    'STOP_LOSS',
    'STOP_LOSS_LIMIT',
    'TAKE_PROFIT',
    'TAKE_PROFIT_LIMIT',
    'LIMIT_MAKER',
  ]
  """Order type."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  workingTime: TimestampMillis
  """Time the order started working on the order book. -1 if not yet working."""
  selfTradePreventionMode: Literal[
    'NONE', 'EXPIRE_MAKER', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'DECREMENT', 'TRANSFER'
  ]
  """Self-trade prevention mode in effect for the order."""
  stopPrice: NotRequired[str]
  """Price at which the algorithmic order triggers. Present for STOP_LOSS, TAKE_PROFIT, STOP_LOSS_LIMIT and TAKE_PROFIT_LIMIT orders."""
  icebergQty: NotRequired[str]
  """Quantity for the iceberg order. Present only when an iceberg quantity was sent in the request."""
  strategyId: NotRequired[int]
  """Id labeling the order strategy this order is part of. Present when a strategy id was sent in the request."""
  strategyType: NotRequired[int]
  """Type labeling the order strategy this order is part of. Present when a strategy type was sent in the request."""
  trailingDelta: NotRequired[int]
  """Delta price change required before order activation. Present for trailing stop orders."""
  trailingTime: NotRequired[TimestampMillis]
  """Time the trailing order became active and started tracking price changes. Present only for trailing stop orders."""
  pegPriceType: NotRequired[Literal['PRIMARY_PEG', 'MARKET_PEG']]
  """Peg reference type. Present only for pegged orders."""
  pegOffsetType: NotRequired[Literal['PRICE_LEVEL']]
  """Peg offset type. Present only for pegged orders that requested one."""
  pegOffsetValue: NotRequired[int]
  """Peg offset value. Present only for pegged orders that requested one."""
  peggedPrice: NotRequired[str]
  """Current price the order is pegged at. Present only for pegged orders, once determined."""
  expiryReason: NotRequired[
    Literal[
      'NONE',
      'REJECTED',
      'EXCHANGE_CANCELED',
      'OCO_TRIGGER',
      'OTO_PHASE_ONE_EXPIRED',
      'UNFILLED_IOC_QUANTITY_EXPIRED',
      'UNFILLED_FOK_ORDER_EXPIRED',
      'INSUFFICIENT_LIQUIDITY',
      'EXECUTION_RULE_PRICE_RANGE_EXCEEDED',
    ]
  ]
  """Cause of the order's expiration. Present when the order has expired."""
  preventedMatchId: NotRequired[int]
  """Id of a prevented match, usable together with symbol to query it. Present only when the order expired due to self-trade prevention."""
  preventedQuantity: NotRequired[str]
  """Order quantity that expired due to self-trade prevention. Present only when the order expired due to self-trade prevention."""
  fills: list[OrderListOpoOrderReportFill]
  """Trades that filled this order-list leg."""


class OrderListOpoResult(TypedDict):
  """The placed OPO order list."""

  orderListId: int
  """Id of the order list."""
  contingencyType: Literal['OCO', 'OTO']
  """Contingency type of the order list."""
  listStatusType: Literal['RESPONSE', 'EXEC_STARTED', 'UPDATED', 'ALL_DONE']
  """Status of the order list."""
  listOrderStatus: Literal['EXECUTING', 'ALL_DONE', 'REJECT']
  """Execution status of the order list."""
  listClientOrderId: str
  """Client id for the entire order list, either caller-supplied or venue-generated."""
  transactionTime: TimestampMillis
  """Time the order list was processed."""
  symbol: str
  """Trading pair symbol."""
  orders: list[OrderListOrder]
  """The working and pending orders making up the OPO, identified minimally."""
  orderReports: list[
    OrderListOpoOrderReportAck
    | OrderListOpoOrderReportResult
    | OrderListOpoOrderReportFull
  ]
  """Per-leg order acknowledgements. Each entry's shape is discriminated by the request's newOrderRespType parameter."""


class OrderListOpo(RpcEndpoint):
  """Place an OPO (one-triggers-a-pending-order) order list, comprised of 2 orders: a working order (LIMIT or LIMIT_MAKER, the only one on the order book initially) and a pending order placed once the working order is fully filled. An OPO adds 2 orders to the EXCHANGE_MAX_NUM_ORDERS and MAX_NUM_ORDERS filters."""

  async def order_list_opo(
    self,
    *,
    symbol: str,
    list_client_order_id: str | None = None,
    new_order_resp_type: Literal['ACK', 'RESULT', 'FULL'] | None = None,
    self_trade_prevention_mode: Literal[
      'NONE', 'EXPIRE_MAKER', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'DECREMENT', 'TRANSFER'
    ]
    | None = None,
    working_type: Literal['LIMIT', 'LIMIT_MAKER'],
    working_side: Literal['BUY', 'SELL'],
    working_client_order_id: str | None = None,
    working_price: str,
    working_quantity: str,
    working_iceberg_qty: str | None = None,
    working_time_in_force: Literal['GTC', 'IOC', 'FOK'] | None = None,
    working_strategy_id: int | None = None,
    working_strategy_type: int | None = None,
    working_peg_price_type: Literal['PRIMARY_PEG', 'MARKET_PEG'] | None = None,
    working_peg_offset_type: Literal['PRICE_LEVEL'] | None = None,
    working_peg_offset_value: int | None = None,
    pending_type: Literal[
      'LIMIT',
      'MARKET',
      'STOP_LOSS',
      'STOP_LOSS_LIMIT',
      'TAKE_PROFIT',
      'TAKE_PROFIT_LIMIT',
      'LIMIT_MAKER',
    ],
    pending_side: Literal['BUY', 'SELL'],
    pending_client_order_id: str | None = None,
    pending_price: str | None = None,
    pending_stop_price: str | None = None,
    pending_trailing_delta: str | None = None,
    pending_iceberg_qty: str | None = None,
    pending_time_in_force: Literal['GTC', 'IOC', 'FOK'] | None = None,
    pending_strategy_id: int | None = None,
    pending_strategy_type: int | None = None,
    pending_peg_price_type: Literal['PRIMARY_PEG', 'MARKET_PEG'] | None = None,
    pending_peg_offset_type: Literal['PRICE_LEVEL'] | None = None,
    pending_peg_offset_value: int | None = None,
    validate: bool | None = None,
  ) -> OrderListOpoResult:
    """Place an OPO (one-triggers-a-pending-order) order list, comprised of 2 orders: a working order (LIMIT or LIMIT_MAKER, the only one on the order book initially) and a pending order placed once the working order is fully filled. An OPO adds 2 orders to the EXCHANGE_MAX_NUM_ORDERS and MAX_NUM_ORDERS filters.

    Args:
      symbol: Trading pair symbol.
      list_client_order_id: Arbitrary unique id among open order lists. Automatically generated if not sent. A new order list with the same listClientOrderId is accepted only when the previous one is filled or completely expired; distinct from workingClientOrderId and pendingClientOrderId.
      new_order_resp_type: Response shape to return for each leg's orderReports entry.
      self_trade_prevention_mode: Self-trade prevention mode. The allowed values depend on the symbol's configuration.
      working_type: Order type of the working order. Initially, only the working order goes on the order book.
      working_side: Order side of the working order.
      working_client_order_id: Arbitrary unique id among open orders for the working order. Automatically generated if not sent.
      working_price: Price of the working order.
      working_quantity: Quantity of the working order.
      working_iceberg_qty: Iceberg quantity for the working order. Usable only if workingTimeInForce is GTC, or if workingType is LIMIT_MAKER.
      working_time_in_force: Time in force for the working order. Required if workingType is LIMIT.
      working_strategy_id: Id labeling the working order within an order strategy.
      working_strategy_type: Type labeling the working order's strategy. Values smaller than 1000000 are reserved and cannot be used.
      working_peg_price_type: Peg reference type for the working order.
      working_peg_offset_type: Peg offset type for the working order. Must be sent together with workingPegOffsetValue.
      working_peg_offset_value: Peg offset value for the working order. Must be sent together with workingPegOffsetType.
      pending_type: Order type of the pending order. The pending order is only placed on the order book once the working order is fully filled. Any type except MARKET orders using quoteOrderQty is supported.
      pending_side: Order side of the pending order.
      pending_client_order_id: Arbitrary unique id among open orders for the pending order. Automatically generated if not sent.
      pending_price: Price of the pending order. Required if pendingType is LIMIT or STOP_LOSS_LIMIT/TAKE_PROFIT_LIMIT.
      pending_stop_price: Trigger price of the pending order. Used with STOP_LOSS/TAKE_PROFIT or STOP_LOSS_LIMIT/TAKE_PROFIT_LIMIT.
      pending_trailing_delta: Trailing delta for the pending order. Used with STOP_LOSS/TAKE_PROFIT or STOP_LOSS_LIMIT/TAKE_PROFIT_LIMIT.
      pending_iceberg_qty: Iceberg quantity for the pending order. Usable only if pendingTimeInForce is GTC or if pendingType is LIMIT_MAKER.
      pending_time_in_force: Time in force for the pending order. Required if pendingType is LIMIT or STOP_LOSS_LIMIT/TAKE_PROFIT_LIMIT.
      pending_strategy_id: Id labeling the pending order within an order strategy.
      pending_strategy_type: Type labeling the pending order's strategy. Values smaller than 1000000 are reserved and cannot be used.
      pending_peg_price_type: Peg reference type for the pending order.
      pending_peg_offset_type: Peg offset type for the pending order. Must be sent together with pendingPegOffsetValue.
      pending_peg_offset_value: Peg offset value for the pending order. Must be sent together with pendingPegOffsetType.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/rest-api.md)
    """
    params: dict = {
      'symbol': symbol,
      'workingType': working_type,
      'workingSide': working_side,
      'workingPrice': working_price,
      'workingQuantity': working_quantity,
      'pendingType': pending_type,
      'pendingSide': pending_side,
    }
    if list_client_order_id is not None:
      params['listClientOrderId'] = list_client_order_id
    if new_order_resp_type is not None:
      params['newOrderRespType'] = new_order_resp_type
    if self_trade_prevention_mode is not None:
      params['selfTradePreventionMode'] = self_trade_prevention_mode
    if working_client_order_id is not None:
      params['workingClientOrderId'] = working_client_order_id
    if working_iceberg_qty is not None:
      params['workingIcebergQty'] = working_iceberg_qty
    if working_time_in_force is not None:
      params['workingTimeInForce'] = working_time_in_force
    if working_strategy_id is not None:
      params['workingStrategyId'] = working_strategy_id
    if working_strategy_type is not None:
      params['workingStrategyType'] = working_strategy_type
    if working_peg_price_type is not None:
      params['workingPegPriceType'] = working_peg_price_type
    if working_peg_offset_type is not None:
      params['workingPegOffsetType'] = working_peg_offset_type
    if working_peg_offset_value is not None:
      params['workingPegOffsetValue'] = working_peg_offset_value
    if pending_client_order_id is not None:
      params['pendingClientOrderId'] = pending_client_order_id
    if pending_price is not None:
      params['pendingPrice'] = pending_price
    if pending_stop_price is not None:
      params['pendingStopPrice'] = pending_stop_price
    if pending_trailing_delta is not None:
      params['pendingTrailingDelta'] = pending_trailing_delta
    if pending_iceberg_qty is not None:
      params['pendingIcebergQty'] = pending_iceberg_qty
    if pending_time_in_force is not None:
      params['pendingTimeInForce'] = pending_time_in_force
    if pending_strategy_id is not None:
      params['pendingStrategyId'] = pending_strategy_id
    if pending_strategy_type is not None:
      params['pendingStrategyType'] = pending_strategy_type
    if pending_peg_price_type is not None:
      params['pendingPegPriceType'] = pending_peg_price_type
    if pending_peg_offset_type is not None:
      params['pendingPegOffsetType'] = pending_peg_offset_type
    if pending_peg_offset_value is not None:
      params['pendingPegOffsetValue'] = pending_peg_offset_value
    _Response = OrderListOpoResult
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/api/v3/orderList/opo',
      params=params,
      validator=_validator,
      validate=validate,
    )
