# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class OrderRateLimit(TypedDict):
  """This account's unfilled order count and limit for one rate-limit interval."""

  rateLimitType: Literal['REQUEST_WEIGHT', 'ORDERS', 'RAW_REQUESTS']
  """Type of rate limit this entry describes."""
  interval: Literal['SECOND', 'MINUTE', 'DAY']
  """Time unit of the rate-limit interval."""
  intervalNum: int
  """Number of `interval` units in this rate-limit window."""
  limit: int
  """Maximum unfilled orders allowed in this window."""
  count: int
  """Unfilled orders placed by this account in the current window."""


Response = list[OrderRateLimit]


class RateLimitOrder(RpcEndpoint):
  """Displays this account's unfilled order count for all configured order-rate-limit intervals."""

  async def rate_limit_order(self, *, validate: bool | None = None) -> Response:
    """Displays this account's unfilled order count for all configured order-rate-limit intervals.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/rest-api.md)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v3/rateLimit/order',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      response_type=Response,
    )
