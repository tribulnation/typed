# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class OrderListOrder(TypedDict):
  """One order belonging to an order list."""

  symbol: str
  """Symbol this order was placed on."""
  orderId: int
  """Order ID assigned by Binance."""
  clientOrderId: str
  """Client-supplied order ID."""


class Request(TypedDict):
  """Request parameters."""

  fromId: NotRequired[int]
  """Order list ID to start from (inclusive). If supplied, neither `startTime` nor `endTime` can be provided."""
  startTime: NotRequired[TimestampMillis]
  """Start of the time range, in milliseconds."""
  endTime: NotRequired[TimestampMillis]
  """End of the time range, in milliseconds."""
  limit: NotRequired[int]
  """Maximum number of order lists to return."""


class SpotOrderList(TypedDict):
  """A spot order list (OCO/OTO)."""

  orderListId: int
  """Order list ID assigned by Binance."""
  contingencyType: Literal['OCO', 'OTO']
  """Contingency type of the order list."""
  listStatusType: Literal['RESPONSE', 'EXEC_STARTED', 'UPDATED', 'ALL_DONE']
  """Current status of the order list."""
  listOrderStatus: Literal['EXECUTING', 'ALL_DONE', 'REJECT']
  """Current status of the orders in the list."""
  listClientOrderId: str
  """Client-supplied order list ID."""
  transactionTime: TimestampMillis
  """Millisecond timestamp the order list was created or last updated."""
  symbol: str
  """Symbol this order list was placed on."""
  orders: list[OrderListOrder]
  """Orders belonging to this order list."""


Response = list[SpotOrderList]


class AllOrderLists(RpcEndpoint):
  """Retrieve all account order lists based on the provided optional parameters. The time between `startTime` and `endTime` can't be longer than 24 hours. If `fromId` is supplied, neither `startTime` nor `endTime` can be provided."""

  async def all_order_lists(
    self,
    *,
    from_id: int | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Retrieve all account order lists based on the provided optional parameters. The time between `startTime` and `endTime` can't be longer than 24 hours. If `fromId` is supplied, neither `startTime` nor `endTime` can be provided.

    Args:
      from_id: Order list ID to start from (inclusive). If supplied, neither `startTime` nor `endTime` can be provided.
      start_time: Start of the time range, in milliseconds.
      end_time: End of the time range, in milliseconds.
      limit: Maximum number of order lists to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/rest-api.md)
    """
    request: Request = Request()
    if from_id is not None:
      request['fromId'] = from_id
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/api/v3/allOrderList',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
