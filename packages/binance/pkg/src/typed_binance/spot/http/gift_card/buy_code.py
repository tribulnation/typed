# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class GiftCardCode(TypedDict):
  """The newly created gift card."""

  referenceNo: NotRequired[str]
  """Gift card reference number, used to verify the card."""
  code: NotRequired[str]
  """Redemption code. Keep confidential -- anyone holding it can redeem the card."""
  expiredTime: NotRequired[TimestampMillis]
  """Millisecond epoch expiration timestamp."""


class Request(TypedDict):
  """Request parameters."""

  baseToken: str
  """Token paid with, e.g. BUSD."""
  faceToken: str
  """Token the recipient redeems into, e.g. BNB."""
  baseTokenAmount: float
  """Amount of `baseToken` to spend."""


class GiftCardCodeResponse(TypedDict):
  """Gift card creation response frame. This product line wraps its business data in {code,message,data,success} on the wire, and the core does not strip it -- see spec/core.md's Envelope section."""

  code: NotRequired[str]
  """Response code. "000000" indicates success."""
  message: NotRequired[str]
  """Response message."""
  data: NotRequired[GiftCardCode]
  success: NotRequired[bool]
  """Whether the call succeeded."""


class BuyCode(RpcEndpoint):
  """Create a dual-token (fixed-value, discount) Binance Gift Card: pay in `baseToken`, the recipient redeems into `faceToken`."""

  async def buy_code(
    self,
    *,
    base_token: str,
    face_token: str,
    base_token_amount: float,
    validate: bool | None = None,
  ) -> GiftCardCodeResponse:
    """Create a dual-token (fixed-value, discount) Binance Gift Card: pay in `baseToken`, the recipient redeems into `faceToken`.

    Args:
      base_token: Token paid with, e.g. BUSD.
      face_token: Token the recipient redeems into, e.g. BNB.
      base_token_amount: Amount of `baseToken` to spend.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/docs/gift_card/market-data)
    """
    return await self.request(
      Request(
        baseToken=base_token, faceToken=face_token, baseTokenAmount=base_token_amount
      ),
      method='POST',
      path='/sapi/v1/giftcard/buyCode',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=GiftCardCodeResponse,
    )
