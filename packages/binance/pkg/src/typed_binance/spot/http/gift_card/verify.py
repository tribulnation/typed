# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class GiftCardVerification(TypedDict):
  """The gift card's validity and remaining value."""

  valid: NotRequired[bool]
  """Whether the gift card is valid and unredeemed."""
  token: NotRequired[str]
  """Token the card is denominated in."""
  amount: NotRequired[str]
  """Remaining value on the card."""


class Request(TypedDict):
  """Request parameters."""

  referenceNo: str
  """Gift card reference number to verify."""


class GiftCardVerificationResponse(TypedDict):
  """Gift card verification response frame. This product line wraps its business data in {code,message,data,success} on the wire, and the core does not strip it -- see spec/core.md's Envelope section."""

  code: NotRequired[str]
  """Response code. "000000" indicates success."""
  message: NotRequired[str]
  """Response message."""
  data: NotRequired[GiftCardVerification]
  success: NotRequired[bool]
  """Whether the call succeeded."""


class Verify(RpcEndpoint):
  """Verify a Binance Gift Card's validity and remaining value by its reference number."""

  async def verify(
    self,
    reference_no: str,
    *,
    validate: bool | None = None,
  ) -> GiftCardVerificationResponse:
    """Verify a Binance Gift Card's validity and remaining value by its reference number.

    Args:
      reference_no: Gift card reference number to verify.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/docs/gift_card/market-data)
    """
    return await self.request(
      Request(referenceNo=reference_no),
      method='GET',
      path='/sapi/v1/giftcard/verify',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=GiftCardVerificationResponse,
    )
