# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Request parameters."""

  amount: float
  """Amount of SOL to stake."""


class SolStakeResult(TypedDict):
  """Result of a SOL staking subscription."""

  success: NotRequired[bool]
  """Whether the subscription succeeded."""
  bnsolAmount: NotRequired[str]
  """Amount of BNSOL minted for this subscription, as a decimal string."""
  purchaseId: NotRequired[int]
  """Identifier of this staking subscription."""
  exchangeRate: NotRequired[str]
  """SOL/BNSOL exchange rate applied to this subscription, as a decimal string."""


class Stake(RpcEndpoint):
  """Stake an amount of SOL, minting BNSOL in return."""

  async def stake(
    self, amount: float, *, validate: bool | None = None
  ) -> SolStakeResult:
    """Stake an amount of SOL, minting BNSOL in return.

    Args:
      amount: Amount of SOL to stake.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-staking/api/rest-api/sol-staking#subscribe-sol-staking)
    """
    return await self.request(
      Request(amount=amount),
      method='POST',
      path='/sapi/v1/sol-staking/sol/stake',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SolStakeResult,
    )
