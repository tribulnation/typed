# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class SoftStakingSetResult(TypedDict):
  """Result of changing this account's Soft Staking enabled setting."""

  success: NotRequired[bool]
  """Whether the setting was updated successfully."""


class Set(RpcEndpoint):
  """Enable or disable Soft Staking for this account."""

  async def __call__(
    self,
    *,
    soft_staking: bool,
    validate: bool | None = None,
  ) -> SoftStakingSetResult:
    """Enable or disable Soft Staking for this account.

    Args:
      soft_staking: Whether Soft Staking should be enabled for this account.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-staking/api/rest-api/soft-staking#set-soft-staking)
    """
    params: dict = {
      'softStaking': soft_staking,
    }
    _Response = SoftStakingSetResult
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/sapi/v1/soft-staking/set',
      params=params,
      validator=_validator,
      validate=validate,
    )
