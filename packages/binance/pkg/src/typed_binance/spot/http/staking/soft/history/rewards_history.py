# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Request parameters."""

  asset: NotRequired[str]
  """Restrict results to this underlying asset."""
  startTime: NotRequired[TimestampMillis]
  """Millisecond epoch start of the queried window."""
  endTime: NotRequired[TimestampMillis]
  """Millisecond epoch end of the queried window."""
  current: NotRequired[int]
  """Page index, starting from 1."""
  size: NotRequired[int]
  """Number of results per page."""


class SoftStakingRewardsHistoryEntry(TypedDict):
  """One Soft Staking reward payment."""

  asset: str
  """Underlying asset staked."""
  rewards: str
  """Reward amount, as a decimal string."""
  rewardAsset: str
  """Asset the reward was paid in."""
  avgAmount: str
  """Average staked amount the reward was computed over, as a decimal string."""
  time: TimestampMillis
  """Millisecond epoch time the reward was recorded."""


class SoftStakingRewardsHistoryPage(TypedDict):
  """One page of this account's Soft Staking rewards history."""

  rows: list[SoftStakingRewardsHistoryEntry]
  """Matching records on this page."""
  total: int
  """Total number of matching records."""


class RewardsHistory(RpcEndpoint):
  """Get this account's Soft Staking rewards history."""

  def rewards_history_paged(
    self,
    asset: str | None = None,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    size: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[SoftStakingRewardsHistoryEntry, int]:
    """Get this account's Soft Staking rewards history.

    Paged variant of `rewards_history`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      asset: Restrict results to this underlying asset.
      start_time: Millisecond epoch start of the queried window.
      end_time: Millisecond epoch end of the queried window.
      size: Number of results per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-staking/api/rest-api/soft-staking#get-soft-staking-rewards-history)
    """

    async def next(
      current: int,
    ) -> tuple[list[SoftStakingRewardsHistoryEntry], int | None]:
      response = await self.rewards_history(
        asset,
        start_time=start_time,
        end_time=end_time,
        size=size,
        current=current,
        validate=validate,
      )
      rows = response.get('rows')
      rows = rows if rows is not None else []
      total = response.get('total')
      total = int(total) if total is not None else None
      if total is None or size is None or (current - 1 + 1) * size >= total:
        return rows, None
      return rows, current + 1

    return PaginatedResponse(1, next)

  async def rewards_history(
    self,
    asset: str | None = None,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    current: int | None = None,
    size: int | None = None,
    validate: bool | None = None,
  ) -> SoftStakingRewardsHistoryPage:
    """Get this account's Soft Staking rewards history.

    Args:
      asset: Restrict results to this underlying asset.
      start_time: Millisecond epoch start of the queried window.
      end_time: Millisecond epoch end of the queried window.
      current: Page index, starting from 1.
      size: Number of results per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-staking/api/rest-api/soft-staking#get-soft-staking-rewards-history)
    """
    request: Request = Request()
    if asset is not None:
      request['asset'] = asset
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if current is not None:
      request['current'] = current
    if size is not None:
      request['size'] = size
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/soft-staking/history/rewardsRecord',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SoftStakingRewardsHistoryPage,
    )
