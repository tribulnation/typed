# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class ClaimBoostRewardsResult(TypedDict):
  """Result of claiming Boost APR airdrop rewards."""

  success: NotRequired[bool]
  """Whether the claim succeeded."""


class ClaimBoostRewards(RpcEndpoint):
  """Claim this account's accrued Boost APR airdrop rewards for SOL staking."""

  async def claim_boost_rewards(
    self,
    *,
    validate: bool | None = None,
  ) -> ClaimBoostRewardsResult:
    """Claim this account's accrued Boost APR airdrop rewards for SOL staking.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-staking/api/rest-api/sol-staking#claim-boost-rewards)
    """
    return await self.request(
      None,
      method='POST',
      path='/sapi/v1/sol-staking/sol/claim',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      response_type=ClaimBoostRewardsResult,
    )
