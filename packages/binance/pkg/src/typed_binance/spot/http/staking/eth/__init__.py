# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .account import Account
from .history import History
from .quota import Quota
from .redeem import Redeem
from .stake import Stake
from .wbeth import Wbeth


class Eth(Account, Quota, Redeem, Stake):
  """ETH Staking -- subscribe/redeem endpoints for staked ETH and its WBETH wrapper token, plus rate, quota, and rewards-history lookups.

  References:
    - [Upstream docs](https://developers.binance.com/en/docs/catalog/investment-and-services-staking/api/rest-api/eth-staking)
  """

  @cached_property
  def history(self) -> History:
    """ETH Staking history -- staking, redemption, rate, and WBETH-rewards history lookups, documented on the same ETH Staking page as the subscribe/redeem endpoints above it.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/investment-and-services-staking/api/rest-api/eth-staking)
    """
    return History(client=self.client)

  @cached_property
  def wbeth(self) -> Wbeth:
    """WBETH -- the wrapped, transferable token representing staked ETH: wrap/unwrap endpoints and their history, documented on the same ETH Staking page as the rest of the ETH Staking product.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/investment-and-services-staking/api/rest-api/eth-staking)
    """
    return Wbeth(client=self.client)
