# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .eth import Eth
from .on_chain_yields import OnChainYields
from .soft import Soft
from .sol import Sol
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Staking(RpcEndpoint):
  """Staking -- umbrella over ETH Staking, SOL Staking, On-chain Yields, and Soft Staking, each documented on its own separate upstream page (no single combined upstream overview page exists). Closest available citation: the client's own product-level upstream, covering the whole Binance Spot API surface these staking endpoints live under.

  References:
    - [Upstream docs](https://github.com/binance/binance-spot-api-docs)
  """

  @cached_property
  def eth(self) -> Eth:
    """ETH Staking -- subscribe/redeem endpoints for staked ETH and its WBETH wrapper token, plus rate, quota, and rewards-history lookups.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/investment-and-services-staking/api/rest-api/eth-staking)
    """
    return Eth(client=self.client)

  @cached_property
  def on_chain_yields(self) -> OnChainYields:
    """On-chain Yields -- Staking's locked on-chain-yield products: subscribe/redeem endpoints plus position and rewards-history lookups.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/investment-and-services-staking/api/rest-api/on-chain-yields)
    """
    return OnChainYields(client=self.client)

  @cached_property
  def soft(self) -> Soft:
    """Soft Staking -- automatic, no-lockup staking on eligible balances: product list, rewards history, and the toggle to enable or disable it.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/investment-and-services-staking/api/rest-api/soft-staking)
    """
    return Soft(client=self.client)

  @cached_property
  def sol(self) -> Sol:
    """SOL Staking -- stake and redeem SOL, claim rewards, and query staking/redemption/reward history.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/investment-and-services-staking/api/rest-api/sol-staking)
    """
    return Sol(client=self.client)
