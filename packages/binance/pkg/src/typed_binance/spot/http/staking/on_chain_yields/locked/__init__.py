# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .auto_subscribe import AutoSubscribe
from .history import History
from .list import List
from .personal_left_quota import PersonalLeftQuota
from .position import Position
from .redeem import Redeem
from .redeem_option import RedeemOption
from .subscribe import Subscribe
from .subscription_preview import SubscriptionPreview


class Locked(
  AutoSubscribe,
  List,
  PersonalLeftQuota,
  Position,
  Redeem,
  RedeemOption,
  Subscribe,
  SubscriptionPreview,
):
  """Locked on-chain-yield products -- subscribe, redeem, position, quota, and redemption-option endpoints for On-chain Yields' locked tier, documented on the same On-chain Yields page as the account-level lookups above it.

  References:
    - [Upstream docs](https://developers.binance.com/en/docs/catalog/investment-and-services-staking/api/rest-api/on-chain-yields)
  """

  @cached_property
  def history(self) -> History:
    """Locked on-chain-yield history -- subscription, redemption, and rewards history for On-chain Yields' locked tier, documented on the same On-chain Yields page as the subscribe/redeem endpoints above it.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/investment-and-services-staking/api/rest-api/on-chain-yields)
    """
    return History(client=self.client)
