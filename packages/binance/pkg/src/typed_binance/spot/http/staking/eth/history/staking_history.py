# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class EthStakingEvent(TypedDict):
  """One ETH staking subscription event."""

  time: TimestampMillis
  """Millisecond epoch time the subscription was requested."""
  asset: str
  """Asset staked (ETH)."""
  amount: str
  """Amount of `asset` staked, as a decimal string."""
  distributeAsset: str
  """Asset the subscription was distributed in."""
  distributeAmount: str
  """Amount of `distributeAsset` distributed, as a decimal string."""
  conversionRatio: str
  """Conversion ratio applied to this subscription, as a decimal string."""
  status: str
  """Subscription status."""


class Request(TypedDict):
  """Request parameters."""

  purchaseId: NotRequired[int]
  """Restrict results to this staking subscription identifier."""
  startTime: NotRequired[TimestampMillis]
  """Millisecond epoch start of the queried window. See the endpoint notes for the default window applied when this and `endTime` are omitted."""
  endTime: NotRequired[TimestampMillis]
  """Millisecond epoch end of the queried window. See the endpoint notes for the default window applied when this and `startTime` are omitted."""
  current: NotRequired[int]
  """Page index, starting from 1."""
  size: NotRequired[int]
  """Number of records to return per page."""


class EthStakingHistoryPage(TypedDict):
  """One page of ETH staking subscription history."""

  rows: list[EthStakingEvent]
  """Matching ethstakingevent records on this page."""
  total: int
  """Total number of matching records across all pages."""


class StakingHistory(RpcEndpoint):
  """Query this account's ETH staking subscription history."""

  def staking_history_paged(
    self,
    *,
    purchase_id: int | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    size: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[EthStakingEvent, int]:
    """Query this account's ETH staking subscription history.

    Paged variant of `staking_history`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      purchase_id: Restrict results to this staking subscription identifier.
      start_time: Millisecond epoch start of the queried window. See the endpoint notes for the default window applied when this and `endTime` are omitted.
      end_time: Millisecond epoch end of the queried window. See the endpoint notes for the default window applied when this and `startTime` are omitted.
      size: Number of records to return per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-staking/api/rest-api/eth-staking#get-eth-staking-history)
    """

    async def next(current: int) -> tuple[list[EthStakingEvent], int | None]:
      response = await self.staking_history(
        purchase_id=purchase_id,
        start_time=start_time,
        end_time=end_time,
        size=size,
        current=current,
        validate=validate,
      )
      rows = response.get('rows')
      rows = rows if rows is not None else []
      total = response.get('total')
      total = int(total) if total is not None else None
      if total is None or size is None or (current - 1 + 1) * size >= total:
        return rows, None
      return rows, current + 1

    return PaginatedResponse(1, next)

  async def staking_history(
    self,
    *,
    purchase_id: int | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    current: int | None = None,
    size: int | None = None,
    validate: bool | None = None,
  ) -> EthStakingHistoryPage:
    """Query this account's ETH staking subscription history.

    Args:
      purchase_id: Restrict results to this staking subscription identifier.
      start_time: Millisecond epoch start of the queried window. See the endpoint notes for the default window applied when this and `endTime` are omitted.
      end_time: Millisecond epoch end of the queried window. See the endpoint notes for the default window applied when this and `startTime` are omitted.
      current: Page index, starting from 1.
      size: Number of records to return per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-staking/api/rest-api/eth-staking#get-eth-staking-history)
    """
    request: Request = Request()
    if purchase_id is not None:
      request['purchaseId'] = purchase_id
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if current is not None:
      request['current'] = current
    if size is not None:
      request['size'] = size
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/eth-staking/eth/history/stakingHistory',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=EthStakingHistoryPage,
    )
