# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class EthStakingHoldings(TypedDict):
  """Breakdown of ETH staking holdings by token."""

  wbethAmount: NotRequired[str]
  """WBETH balance held from ETH staking, as a decimal string."""
  bethAmount: NotRequired[str]
  """BETH balance held from ETH staking, as a decimal string."""


class EthStakingProfit(TypedDict):
  """Breakdown of trailing 30-day staking profit by token."""

  amountFromWBETH: NotRequired[str]
  """Trailing 30-day profit attributable to WBETH holdings, as a decimal string."""
  amountFromBETH: NotRequired[str]
  """Trailing 30-day profit attributable to BETH holdings, as a decimal string."""


class EthStakingAccount(TypedDict):
  """Summary of this account's ETH staking holdings and profit."""

  holdingInETH: NotRequired[str]
  """Total value of this account's ETH staking holdings, denominated in ETH, as a decimal string."""
  holdings: NotRequired[EthStakingHoldings]
  thirtyDaysProfitInETH: NotRequired[str]
  """Staking profit over the trailing 30 days, denominated in ETH, as a decimal string."""
  profit: NotRequired[EthStakingProfit]


class Account(RpcEndpoint):
  """Get this account's ETH staking summary: current WBETH/BETH holdings and staking profit over the trailing 30 days."""

  async def __call__(self, *, validate: bool | None = None) -> EthStakingAccount:
    """Get this account's ETH staking summary: current WBETH/BETH holdings and staking profit over the trailing 30 days.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-staking/api/rest-api/eth-staking#eth-staking-account)
    """
    _Response = EthStakingAccount
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET', '/sapi/v2/eth-staking/account', validator=_validator, validate=validate
    )
