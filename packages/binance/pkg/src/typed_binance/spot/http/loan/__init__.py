# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .flexible import Flexible
from .stable import Stable
from .vip import Vip
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Loan(RpcEndpoint):
  """Crypto Loan endpoints across Binance's three loan products -- Flexible Rate, Stable Rate, and VIP Loan. Flexible and Stable share one catalog root and VIP Loan is a separate catalog (see `loan/vip/router.json`); this reuses `loan/flexible/router.json`'s link as the closest single citation, since Flexible plus Stable dominate this grouping.

  References:
    - [Upstream docs](https://developers.binance.com/en/docs/catalog/investment-and-services-crypto-loan/api/rest-api/flexible-rate)
  """

  @cached_property
  def flexible(self) -> Flexible:
    """Flexible Loan endpoints -- borrow, repay, and LTV adjustment against a floating-rate crypto loan, plus loanable/collateral asset data and history for borrows, repayments, interest rates, and liquidations.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/investment-and-services-crypto-loan/api/rest-api/flexible-rate)
    """
    return Flexible(client=self.client)

  @cached_property
  def stable(self) -> Stable:
    """Stable Rate Loan endpoints -- history lookups for a fixed-rate crypto loan's income, borrows, repayments, and LTV adjustments.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/investment-and-services-crypto-loan/api/rest-api/stable-rate)
    """
    return Stable(client=self.client)

  @cached_property
  def vip(self) -> Vip:
    """VIP Loan endpoints -- borrowing (floating and fixed rate), renewing, and repaying a VIP Loan, plus collateral/interest-rate/application/order/repayment lookups. Spans three catalog pages (Trade, Market Data, User Information) roughly evenly; this cites the Trade page, which covers the core borrow/renew/repay actions.

    References:
      - [Upstream docs](https://developers.binance.com/en/docs/catalog/investment-and-services-vip-loan/api/rest-api/trade)
    """
    return Vip(client=self.client)
