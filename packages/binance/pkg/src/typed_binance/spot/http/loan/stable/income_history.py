# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class CryptoLoanIncomeRecord(TypedDict):
  """One crypto loan income (balance-affecting event) record."""

  asset: str
  """Asset affected."""
  type: str
  """Event type."""
  amount: str
  """Amount, signed per event type."""
  timestamp: TimestampMillis
  """Millisecond epoch time of the event."""
  tranId: str
  """Transaction ID."""


class Request(TypedDict):
  """Request parameters."""

  asset: NotRequired[str]
  """Asset to filter by."""
  type: NotRequired[
    Literal[
      'borrowIn',
      'collateralSpent',
      'repayAmount',
      'collateralReturn',
      'addCollateral',
      'removeCollateral',
      'collateralReturnAfterLiquidation',
    ]
  ]
  """Income record type to filter by. All types are returned when omitted."""
  startTime: NotRequired[TimestampMillis]
  """Millisecond epoch lower bound. If both `startTime` and `endTime` are omitted, the most recent 7 days are returned."""
  endTime: NotRequired[TimestampMillis]
  """Millisecond epoch upper bound. Maximum interval between `startTime` and `endTime` is 30 days."""
  limit: NotRequired[int]
  """Number of records to return."""


CryptoLoanIncomeRecords = list[CryptoLoanIncomeRecord]


class IncomeHistory(RpcEndpoint):
  """Get Crypto Loans Income History."""

  async def income_history(
    self,
    asset: str | None = None,
    *,
    type: Literal[
      'borrowIn',
      'collateralSpent',
      'repayAmount',
      'collateralReturn',
      'addCollateral',
      'removeCollateral',
      'collateralReturnAfterLiquidation',
    ]
    | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> CryptoLoanIncomeRecords:
    """Get Crypto Loans Income History.

    Args:
      asset: Asset to filter by.
      type: Income record type to filter by. All types are returned when omitted.
      start_time: Millisecond epoch lower bound. If both `startTime` and `endTime` are omitted, the most recent 7 days are returned.
      end_time: Millisecond epoch upper bound. Maximum interval between `startTime` and `endTime` is 30 days.
      limit: Number of records to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-crypto-loan/api/rest-api/stable-rate#get-crypto-loans-income-history)
    """
    request: Request = Request()
    if asset is not None:
      request['asset'] = asset
    if type is not None:
      request['type'] = type
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/loan/income',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CryptoLoanIncomeRecords,
    )
