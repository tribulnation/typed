# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class FlexibleLoanLiquidationHistoryEntry(TypedDict):
  """One flexible loan liquidation history entry."""

  loanCoin: str
  """Loan coin."""
  liquidationDebt: str
  """Debt liquidated."""
  collateralCoin: str
  """Collateral coin."""
  liquidationCollateralAmount: str
  """Collateral amount consumed by the liquidation."""
  returnCollateralAmount: str
  """Collateral amount returned after the liquidation."""
  liquidationFee: str
  """Fee charged for the liquidation."""
  liquidationStartingPrice: str
  """Price at which liquidation started."""
  liquidationStartingTime: TimestampMillis
  """Millisecond epoch time the liquidation started."""
  status: str
  """Liquidation status."""


class Request(TypedDict):
  """Request parameters."""

  loanCoin: NotRequired[str]
  """Loan coin to filter by."""
  collateralCoin: NotRequired[str]
  """Collateral coin to filter by."""
  startTime: NotRequired[TimestampMillis]
  """Millisecond epoch lower bound."""
  endTime: NotRequired[TimestampMillis]
  """Millisecond epoch upper bound."""
  current: NotRequired[int]
  """Current page number, starting from 1."""
  limit: NotRequired[int]
  """Number of records per page."""


class FlexibleLoanLiquidationHistoryPage(TypedDict):
  """One page of flexible loan liquidation history."""

  rows: list[FlexibleLoanLiquidationHistoryEntry]
  """Matching liquidation history entries."""
  total: int
  """Total number of matching history entries."""


class LiquidationHistory(RpcEndpoint):
  """Get Flexible Loan Liquidation History."""

  def liquidation_history_paged(
    self,
    *,
    loan_coin: str | None = None,
    collateral_coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[FlexibleLoanLiquidationHistoryEntry, int]:
    """Get Flexible Loan Liquidation History.

    Paged variant of `liquidation_history`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      loan_coin: Loan coin to filter by.
      collateral_coin: Collateral coin to filter by.
      start_time: Millisecond epoch lower bound.
      end_time: Millisecond epoch upper bound.
      limit: Number of records per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-crypto-loan/api/rest-api/flexible-rate#get-flexible-loan-liquidation-history)
    """

    async def next(
      current: int,
    ) -> tuple[list[FlexibleLoanLiquidationHistoryEntry], int | None]:
      response = await self.liquidation_history(
        loan_coin=loan_coin,
        collateral_coin=collateral_coin,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        current=current,
        validate=validate,
      )
      rows = response.get('rows')
      rows = rows if rows is not None else []
      total = response.get('total')
      total = int(total) if total is not None else None
      if total is None or limit is None or (current - 1 + 1) * limit >= total:
        return rows, None
      return rows, current + 1

    return PaginatedResponse(1, next)

  async def liquidation_history(
    self,
    *,
    loan_coin: str | None = None,
    collateral_coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    current: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> FlexibleLoanLiquidationHistoryPage:
    """Get Flexible Loan Liquidation History.

    Args:
      loan_coin: Loan coin to filter by.
      collateral_coin: Collateral coin to filter by.
      start_time: Millisecond epoch lower bound.
      end_time: Millisecond epoch upper bound.
      current: Current page number, starting from 1.
      limit: Number of records per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-crypto-loan/api/rest-api/flexible-rate#get-flexible-loan-liquidation-history)
    """
    request: Request = Request()
    if loan_coin is not None:
      request['loanCoin'] = loan_coin
    if collateral_coin is not None:
      request['collateralCoin'] = collateral_coin
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if current is not None:
      request['current'] = current
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/sapi/v2/loan/flexible/liquidation/history',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FlexibleLoanLiquidationHistoryPage,
    )
