# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Request parameters."""

  orderId: NotRequired[int]
  """Restrict results to a specific loan order."""
  loanCoin: NotRequired[str]
  """Restrict results to a specific loan coin."""
  startTime: NotRequired[TimestampMillis]
  """Millisecond epoch lower bound. If both `startTime` and `endTime` are omitted, the most recent 90 days are returned."""
  endTime: NotRequired[TimestampMillis]
  """Millisecond epoch upper bound. Maximum interval between `startTime` and `endTime` is 90 days."""
  current: NotRequired[int]
  """Current page number, starting from 1."""
  limit: NotRequired[int]
  """Number of records per page."""


class VipLoanAccruedInterestEntry(TypedDict):
  """One accrued interest record."""

  loanCoin: str
  """Loan coin."""
  principalAmount: str
  """Outstanding principal amount interest accrued against."""
  interestAmount: str
  """Interest amount accrued."""
  annualInterestRate: str
  """Annual interest rate applied, as a decimal string."""
  accrualTime: TimestampMillis
  """Millisecond epoch time interest was accrued."""
  orderId: int
  """Latest order ID for the renewal order."""


class VipLoanAccruedInterestPage(TypedDict):
  """One page of VIP loan accrued interest records."""

  rows: list[VipLoanAccruedInterestEntry]
  """Matching accrued interest records."""
  total: int
  """Total number of matching records."""


class AccruedInterest(RpcEndpoint):
  """Check VIP loan accrued interest record."""

  def accrued_interest_paged(
    self,
    *,
    order_id: int | None = None,
    loan_coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[VipLoanAccruedInterestEntry, int]:
    """Check VIP loan accrued interest record.

    Paged variant of `accrued_interest`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      order_id: Restrict results to a specific loan order.
      loan_coin: Restrict results to a specific loan coin.
      start_time: Millisecond epoch lower bound. If both `startTime` and `endTime` are omitted, the most recent 90 days are returned.
      end_time: Millisecond epoch upper bound. Maximum interval between `startTime` and `endTime` is 90 days.
      limit: Number of records per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-vip-loan/api/rest-api/user-information#get-viploan-accrued-interest)
    """

    async def next(
      current: int,
    ) -> tuple[list[VipLoanAccruedInterestEntry], int | None]:
      response = await self.accrued_interest(
        order_id=order_id,
        loan_coin=loan_coin,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        current=current,
        validate=validate,
      )
      rows = response.get('rows')
      rows = rows if rows is not None else []
      total = response.get('total')
      total = int(total) if total is not None else None
      if total is None or limit is None or (current - 1 + 1) * limit >= total:
        return rows, None
      return rows, current + 1

    return PaginatedResponse(1, next)

  async def accrued_interest(
    self,
    *,
    order_id: int | None = None,
    loan_coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    current: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> VipLoanAccruedInterestPage:
    """Check VIP loan accrued interest record.

    Args:
      order_id: Restrict results to a specific loan order.
      loan_coin: Restrict results to a specific loan coin.
      start_time: Millisecond epoch lower bound. If both `startTime` and `endTime` are omitted, the most recent 90 days are returned.
      end_time: Millisecond epoch upper bound. Maximum interval between `startTime` and `endTime` is 90 days.
      current: Current page number, starting from 1.
      limit: Number of records per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-vip-loan/api/rest-api/user-information#get-viploan-accrued-interest)
    """
    request: Request = Request()
    if order_id is not None:
      request['orderId'] = order_id
    if loan_coin is not None:
      request['loanCoin'] = loan_coin
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if current is not None:
      request['current'] = current
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/loan/vip/accruedInterest',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=VipLoanAccruedInterestPage,
    )
