# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class VipLoanFixedRateBorrowOrder(TypedDict):
  """The submitted fixed-rate VIP loan borrow order."""

  borrowCoin: NotRequired[str]
  """Echo of the requested borrow coin."""
  borrowAmount: NotRequired[str]
  """Actual total borrow amount, aggregated across all matched supply requests."""
  actualReceivedAmount: NotRequired[str]
  """Actual amount received after any fees."""
  collateralCoin: NotRequired[str]
  """Echo of the requested collateral coin(s), comma-separated."""
  collateralAccountId: NotRequired[str]
  """Echo of the requested collateral account ID(s), comma-separated."""
  borrowInterestRate: NotRequired[str]
  """Actual borrow interest rate, weighted average when multiple supply requests were matched."""
  duration: NotRequired[str]
  """Loan duration, formatted as `{loanTerm}Days`, e.g. `30Days`."""
  autoRepay: NotRequired[bool]
  """Echo of the requested auto-repay setting."""
  orderId: NotRequired[int]
  """Order ID."""
  status: NotRequired[Literal['Succeeds', 'Failed', 'Processing']]
  """Order status."""


class FixedRateBorrow(RpcEndpoint):
  """Submit a fixed rate VIP loan borrow request by matching one or more market supply orders."""

  async def fixed_rate_borrow(
    self,
    *,
    supply_request: str,
    borrow_coin: str,
    loan_term: int,
    borrow_uid: int,
    collateral_coin: str,
    collateral_account_id: str,
    auto_repay: bool | None = None,
    validate: bool | None = None,
  ) -> VipLoanFixedRateBorrowOrder:
    """Submit a fixed rate VIP loan borrow request by matching one or more market supply orders.

    Args:
      supply_request: Supply request string, positional encoding (no key). Multiple entries separated by `;`, fields separated by `:`, order: `<requestId>:<interestRate>:<amount>`. Example: `1212:0.12:100;3434:0.13:50`.
      borrow_coin: Coin to borrow.
      loan_term: Loan term in days.
      borrow_uid: Borrow receiving account UID.
      collateral_coin: Collateral coin(s) securing the loan, comma-separated for multiple. Only coin names -- the VIP loan collateral amount is the entire spot account balance.
      collateral_account_id: Collateral account ID(s) securing the loan, comma-separated for multiple.
      auto_repay: `true` to auto-repay at expiration, `false` to auto-convert to a flexible (floating) rate at expiration. Default `true`.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-vip-loan/api/rest-api/trade#vip-loan-fixed-rate-borrow)
    """
    params: dict = {
      'supplyRequest': supply_request,
      'borrowCoin': borrow_coin,
      'loanTerm': loan_term,
      'borrowUid': borrow_uid,
      'collateralCoin': collateral_coin,
      'collateralAccountId': collateral_account_id,
    }
    if auto_repay is not None:
      params['autoRepay'] = auto_repay
    _Response = VipLoanFixedRateBorrowOrder
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/loan/vip/fixed/borrow',
      params=params,
      validator=_validator,
      validate=validate,
    )
