# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Request parameters."""

  orderId: NotRequired[int]
  """Order ID of a loan originally created via the now-removed `POST /sapi/v1/loan/borrow`. Stable-rate loans no longer accept new borrows; this filters history against pre-existing orders."""
  loanCoin: NotRequired[str]
  """Loan coin to filter by."""
  collateralCoin: NotRequired[str]
  """Collateral coin to filter by."""
  startTime: NotRequired[TimestampMillis]
  """Millisecond epoch lower bound. If both `startTime` and `endTime` are omitted, the most recent 90 days are returned."""
  endTime: NotRequired[TimestampMillis]
  """Millisecond epoch upper bound. Maximum interval between `startTime` and `endTime` is 180 days."""
  current: NotRequired[int]
  """Current page number, starting from 1."""
  limit: NotRequired[int]
  """Number of records per page."""


class StableLoanBorrowHistoryEntry(TypedDict):
  """One stable rate loan borrow history entry."""

  orderId: int
  """Loan order ID."""
  loanCoin: str
  """Borrowed coin."""
  initialLoanAmount: str
  """Initial borrowed amount."""
  hourlyInterestRate: str
  """Hourly interest rate at borrow time."""
  loanTerm: str
  """Loan term."""
  collateralCoin: str
  """Collateral coin."""
  initialCollateralAmount: str
  """Initial collateral amount."""
  borrowTime: TimestampMillis
  """Millisecond epoch time the loan was taken out."""
  status: Literal[
    'Accruing_Interest',
    'Overdue',
    'Liquidating',
    'Repaying',
    'Repaid',
    'Liquidated',
    'Pending',
    'Failed',
  ]
  """Borrow order status."""


class StableLoanBorrowHistoryPage(TypedDict):
  """One page of stable rate loan borrow history."""

  rows: list[StableLoanBorrowHistoryEntry]
  """Matching borrow history entries."""
  total: int
  """Total number of matching history entries."""


class BorrowHistory(RpcEndpoint):
  """Get Loan Borrow History for stable rate crypto loans."""

  def borrow_history_paged(
    self,
    *,
    order_id: int | None = None,
    loan_coin: str | None = None,
    collateral_coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[StableLoanBorrowHistoryEntry, int]:
    """Get Loan Borrow History for stable rate crypto loans.

    Paged variant of `borrow_history`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      order_id: Order ID of a loan originally created via the now-removed `POST /sapi/v1/loan/borrow`. Stable-rate loans no longer accept new borrows; this filters history against pre-existing orders.
      loan_coin: Loan coin to filter by.
      collateral_coin: Collateral coin to filter by.
      start_time: Millisecond epoch lower bound. If both `startTime` and `endTime` are omitted, the most recent 90 days are returned.
      end_time: Millisecond epoch upper bound. Maximum interval between `startTime` and `endTime` is 180 days.
      limit: Number of records per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-crypto-loan/api/rest-api/stable-rate#get-loan-borrow-history)
    """

    async def next(
      current: int,
    ) -> tuple[list[StableLoanBorrowHistoryEntry], int | None]:
      response = await self.borrow_history(
        order_id=order_id,
        loan_coin=loan_coin,
        collateral_coin=collateral_coin,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        current=current,
        validate=validate,
      )
      rows = response.get('rows')
      rows = rows if rows is not None else []
      total = response.get('total')
      total = int(total) if total is not None else None
      if total is None or limit is None or (current - 1 + 1) * limit >= total:
        return rows, None
      return rows, current + 1

    return PaginatedResponse(1, next)

  async def borrow_history(
    self,
    *,
    order_id: int | None = None,
    loan_coin: str | None = None,
    collateral_coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    current: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> StableLoanBorrowHistoryPage:
    """Get Loan Borrow History for stable rate crypto loans.

    Args:
      order_id: Order ID of a loan originally created via the now-removed `POST /sapi/v1/loan/borrow`. Stable-rate loans no longer accept new borrows; this filters history against pre-existing orders.
      loan_coin: Loan coin to filter by.
      collateral_coin: Collateral coin to filter by.
      start_time: Millisecond epoch lower bound. If both `startTime` and `endTime` are omitted, the most recent 90 days are returned.
      end_time: Millisecond epoch upper bound. Maximum interval between `startTime` and `endTime` is 180 days.
      current: Current page number, starting from 1.
      limit: Number of records per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-crypto-loan/api/rest-api/stable-rate#get-loan-borrow-history)
    """
    request: Request = Request()
    if order_id is not None:
      request['orderId'] = order_id
    if loan_coin is not None:
      request['loanCoin'] = loan_coin
    if collateral_coin is not None:
      request['collateralCoin'] = collateral_coin
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if current is not None:
      request['current'] = current
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/sapi/v1/loan/borrow/history',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=StableLoanBorrowHistoryPage,
    )
