# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis, timestamp_millis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class StableLoanLtvAdjustmentHistoryEntry(TypedDict):
  """One stable rate loan LTV adjustment history entry."""

  loanCoin: NotRequired[str]
  """Loan coin."""
  collateralCoin: NotRequired[str]
  """Collateral coin."""
  direction: NotRequired[Literal['ADDITIONAL', 'REDUCED']]
  """Whether the adjustment added or reduced collateral."""
  amount: NotRequired[str]
  """Amount of collateral adjusted."""
  preLTV: NotRequired[str]
  """LTV before the adjustment."""
  afterLTV: NotRequired[str]
  """LTV after the adjustment."""
  adjustTime: NotRequired[TimestampMillis]
  """Millisecond epoch time of the adjustment."""
  orderId: NotRequired[int]
  """Loan order ID."""


class StableLoanLtvAdjustmentHistoryPage(TypedDict):
  """One page of stable rate loan LTV adjustment history."""

  rows: NotRequired[list[StableLoanLtvAdjustmentHistoryEntry]]
  """Matching LTV adjustment history entries."""
  total: NotRequired[int]
  """Total number of matching history entries."""


class LtvAdjustmentHistory(RpcEndpoint):
  """Get Loan LTV Adjustment History for stable rate crypto loans."""

  async def ltv_adjustment_history(
    self,
    *,
    order_id: int | None = None,
    loan_coin: str | None = None,
    collateral_coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    current: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> StableLoanLtvAdjustmentHistoryPage:
    """Get Loan LTV Adjustment History for stable rate crypto loans.

    Args:
      order_id: Order ID of a loan originally created via the now-removed `POST /sapi/v1/loan/borrow`. Stable-rate loans no longer accept new borrows; this filters history against pre-existing orders.
      loan_coin: Loan coin to filter by.
      collateral_coin: Collateral coin to filter by.
      start_time: Millisecond epoch lower bound. If both `startTime` and `endTime` are omitted, the most recent 90 days are returned.
      end_time: Millisecond epoch upper bound. Maximum interval between `startTime` and `endTime` is 180 days.
      current: Current page number, starting from 1.
      limit: Number of records per page.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-crypto-loan/api/rest-api/stable-rate#get-loan-ltv-adjustment-history)
    """
    params = {}
    if order_id is not None:
      params['orderId'] = order_id
    if loan_coin is not None:
      params['loanCoin'] = loan_coin
    if collateral_coin is not None:
      params['collateralCoin'] = collateral_coin
    if start_time is not None:
      params['startTime'] = timestamp_millis.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp_millis.dump(end_time)
    if current is not None:
      params['current'] = current
    if limit is not None:
      params['limit'] = limit
    _Response = StableLoanLtvAdjustmentHistoryPage
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/sapi/v1/loan/ltv/adjustment/history',
      params=params,
      validator=_validator,
      validate=validate,
    )

  async def ltv_adjustment_history_paged(
    self,
    *,
    order_id: int | None = None,
    loan_coin: str | None = None,
    collateral_coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[StableLoanLtvAdjustmentHistoryPage]:
    """Yield successive pages of `ltv_adjustment_history`.

    Requests `current` from 1 upwards and stops once it has covered the `total` items
    the response reports, or after `max_pages` pages when one is given.
    """
    current = 1
    pages = 0
    while True:
      response = await self.ltv_adjustment_history(
        order_id=order_id,
        loan_coin=loan_coin,
        collateral_coin=collateral_coin,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        current=current,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      total = response.get('total') if response is not None else None
      total = int(total) if total is not None else None
      if total is None or limit is None or pages * limit >= total:
        break
      current += 1
