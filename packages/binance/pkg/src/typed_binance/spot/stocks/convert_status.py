# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class EquityConvertStatus(TypedDict):
  """Current status of a mint / redeem convert request. Returned as an empty object `{}` when `issuerRequestId` has no matching record."""

  underlyingAsset: NotRequired[str]
  """Underlying US-equity ticker, e.g. `AAPL`."""
  underlyingAssetAmount: NotRequired[str]
  """Quantity of the underlying asset involved."""
  tokenizedAsset: NotRequired[str]
  """Tokenized asset, e.g. `AAPLB`."""
  tokenizedAssetAmount: NotRequired[str]
  """Quantity of the tokenized asset involved."""
  issuerRequestId: NotRequired[str]
  """Echoes the requested id."""
  convertType: NotRequired[Literal['MINT', 'REDEEM']]
  """`MINT` or `REDEEM`."""
  status: NotRequired[Literal['P', 'S', 'F']]
  """Convert status."""
  createdAt: NotRequired[TimestampMillis]
  """Creation time."""
  updatedAt: NotRequired[TimestampMillis]
  """Last update time."""


class ConvertStatus(RpcEndpoint):
  """Query the current status of a single mint / redeem request by its `issuerRequestId`. Returns an empty object when no record matches (caller's ID scope only)."""

  async def convert_status(
    self,
    *,
    issuer_request_id: str,
    convert_type: Literal['MINT', 'REDEEM'],
    validate: bool | None = None,
  ) -> EquityConvertStatus:
    """Query the current status of a single mint / redeem request by its `issuerRequestId`. Returns an empty object when no record matches (caller's ID scope only).

    Args:
      issuer_request_id: Convert request id returned by `/tokenized/mint` or `/redeem`.
      convert_type: `MINT` or `REDEEM`.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/rest-api/tokenized#tokenized-convert-status)
    """
    params: dict = {
      'issuerRequestId': issuer_request_id,
      'convertType': convert_type,
    }
    _Response = EquityConvertStatus
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/sapi/v1/equity/tokenized/convert-status',
      params=params,
      validator=_validator,
      validate=validate,
    )
