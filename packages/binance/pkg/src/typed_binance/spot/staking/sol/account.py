# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class SolStakingAccount(TypedDict):
  """Summary of this account's SOL staking holdings and profit."""

  bnsolAmount: NotRequired[str]
  """BNSOL balance held from SOL staking, as a decimal string."""
  holdingInSOL: NotRequired[str]
  """Total value of this account's SOL staking holdings, denominated in SOL, as a decimal string."""
  thirtyDaysProfitInSOL: NotRequired[str]
  """Staking profit over the trailing 30 days, denominated in SOL, as a decimal string."""


class Account(RpcEndpoint):
  """Get this account's SOL staking summary: current BNSOL holdings and staking profit over the trailing 30 days."""

  async def __call__(self, *, validate: bool | None = None) -> SolStakingAccount:
    """Get this account's SOL staking summary: current BNSOL holdings and staking profit over the trailing 30 days.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/investment-and-services-staking/api/rest-api/sol-staking#sol-staking-account)
    """
    _Response = SolStakingAccount
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET', '/sapi/v1/sol-staking/account', validator=_validator, validate=validate
    )
