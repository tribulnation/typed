# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .http import Http
from .streams import Streams
from .ws import Ws
from typed_binance.spot.core import SpotBase


class Spot(SpotBase):
  """Binance Spot -- REST, market-data streams, and the request/reply WS API, together.

  References:
    - [Upstream docs](https://github.com/binance/binance-spot-api-docs)
  """

  @cached_property
  def http(self) -> Http:
    """Binance Spot's REST API -- market data, account, and trading endpoints served over plain HTTP.

    References:
      - [Upstream docs](https://developers.binance.com/docs/binance-spot-api-docs/rest-api)
    """
    return Http(client=self.http_client)

  @cached_property
  def streams(self) -> Streams:
    """Binance Spot's public WebSocket Market Streams -- unauthenticated, connect-and-subscribe push feeds for trades, order book, klines, and tickers.

    References:
      - [Upstream docs](https://developers.binance.com/docs/binance-spot-api-docs/web-socket-streams)
    """
    return Streams(client=self.streams_client)

  @cached_property
  def ws(self) -> Ws:
    """Binance Spot's WebSocket API -- request/response trading and account commands sent over one persistent connection, as an alternative to the REST API.

    References:
      - [Upstream docs](https://developers.binance.com/docs/binance-spot-api-docs/web-socket-api)
    """
    return Ws(client=self.ws_client)
