# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class BrokerSubAccountFuturesEnabled(TypedDict):
  """Result of enabling futures for a sub-account."""

  subaccountId: str
  """Sub-account identifier."""
  enableFutures: bool
  """Whether futures is now enabled."""
  updateTime: TimestampMillis
  """Time this change took effect."""


class EnableSubAccountFutures(RpcEndpoint):
  """Enable Futures for Sub Account"""

  async def enable_sub_account_futures(
    self,
    *,
    sub_account_id: str,
    futures: bool,
    validate: bool | None = None,
  ) -> BrokerSubAccountFuturesEnabled:
    """Enable USDⓈ-M/COIN-M futures trading for a sub-account.

    Args:
      sub_account_id: Sub-account identifier.
      futures: Enable futures for this sub-account. Only `true` is supported.

    References:
      - [Official docs](https://binance-docs.github.io/Brokerage-API/Brokerage_Operation_Endpoints/)
    """
    params: dict = {
      'subAccountId': sub_account_id,
      'futures': futures,
    }
    _Response = BrokerSubAccountFuturesEnabled
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/sapi/v1/broker/subAccount/futures',
      params=params,
      validator=_validator,
      validate=validate,
    )
