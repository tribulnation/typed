# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class SubAccountAssetBalance(TypedDict):
  """One asset's spot balance."""

  freeze: NotRequired[int]
  """Amount frozen (e.g. pending a distribution)."""
  withdrawing: NotRequired[int]
  """Amount currently being withdrawn."""
  asset: NotRequired[str]
  """Asset symbol."""
  free: NotRequired[float]
  """Amount available."""
  locked: NotRequired[int]
  """Amount locked in open orders."""


class SubAccountAssets(TypedDict):
  """The sub-account's spot asset balances."""

  balances: NotRequired[list[SubAccountAssetBalance]]
  """Per-asset balances."""


class Assets(RpcEndpoint):
  """Get a sub-account's spot asset balances, for the master account."""

  async def __call__(
    self, *, email: str, validate: bool | None = None
  ) -> SubAccountAssets:
    """Get a sub-account's spot asset balances, for the master account.

    Args:
      email: Sub-account email.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/vip-and-institutional-sub-account/api/rest-api/asset-management#query-sub-account-assets)
    """
    params: dict = {
      'email': email,
    }
    _Response = SubAccountAssets
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/sapi/v3/sub-account/assets',
      params=params,
      validator=_validator,
      validate=validate,
    )
