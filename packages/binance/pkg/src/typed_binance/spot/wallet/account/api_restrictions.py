# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class ApiKeyRestrictions(TypedDict):
  """Permissions and restrictions of the API key used to sign this request."""

  ipRestrict: bool
  """Whether this API key is restricted to a whitelist of IP addresses."""
  createTime: TimestampMillis
  """Time this API key was created, as milliseconds since epoch."""
  enableReading: bool
  """Whether this API key can read account data."""
  enableWithdrawals: bool
  """This option allows withdrawing via API. The IP Access Restriction filter must be applied to enable withdrawals."""
  enableInternalTransfer: bool
  """This option authorizes this key to transfer funds between your master account and your sub account instantly."""
  enableMargin: bool
  """This option can be adjusted after the Cross Margin account transfer is completed."""
  enableFutures: bool
  """API Key created before your futures account opened does not support futures API service."""
  permitsUniversalTransfer: bool
  """Authorizes this key to be used for a dedicated universal transfer API to transfer multiple supported currencies. Each business's own transfer API rights are not affected by this authorization."""
  enableVanillaOptions: bool
  """Authorizes this key to Vanilla options trading."""
  enableFixApiTrade: NotRequired[bool]
  """Whether this API key can trade over the FIX API."""
  enableFixReadOnly: NotRequired[bool]
  """Whether this API key has read-only access over the FIX API."""
  enableSpotAndMarginTrading: bool
  """Whether this API key can trade Spot and Margin."""
  enablePortfolioMarginTrading: NotRequired[bool]
  """API Key created before your activate portfolio margin does not support portfolio margin API service."""


class ApiRestrictions(RpcEndpoint):
  """Fetch the permissions and restrictions of the API key used to sign this request."""

  async def api_restrictions(
    self, *, validate: bool | None = None
  ) -> ApiKeyRestrictions:
    """Fetch the permissions and restrictions of the API key used to sign this request.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/account#get-api-key-permission)
    """
    _Response = ApiKeyRestrictions
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET', '/sapi/v1/account/apiRestrictions', validator=_validator, validate=validate
    )
