# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class TradeFee(TypedDict):
  """Trading fee rate for a single symbol."""

  symbol: str
  """Trading symbol."""
  makerCommission: str
  """Maker commission rate."""
  takerCommission: str
  """Taker commission rate."""


class TradeFeeEndpoint(RpcEndpoint):
  """Fetch the account's current Spot trading fee rates."""

  async def __call__(
    self,
    *,
    symbol: str | None = None,
    validate: bool | None = None,
  ) -> list[TradeFee]:
    """Fetch the account's current Spot trading fee rates.

    Args:
      symbol: Trading symbol, e.g. "BNBUSDT". Returns every symbol when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/asset#trade-fee)
    """
    params = {}
    if symbol is not None:
      params['symbol'] = symbol
    _Response = list[TradeFee]
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/sapi/v1/asset/tradeFee',
      params=params,
      validator=_validator,
      validate=validate,
    )
