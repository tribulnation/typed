# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class LocalEntityRegion(TypedDict):
  """One region's travel rule support status."""

  regionName: NotRequired[str]
  """Region/city display name. Use this exact value when answering the region field of a travel rule questionnaire."""
  blockType: NotRequired[Literal['supported', 'limited', 'blocked']]
  """Region's support status for travel rule transactions."""
  depositAllowed: NotRequired[bool]
  """Whether deposit is allowed for this region."""
  withdrawalAllowed: NotRequired[bool]
  """Whether withdrawal is allowed for this region."""


class LocalEntityRegionList(TypedDict):
  """Regions in scope for travel rule questionnaires for one country, and when the list was last refreshed."""

  countryCode: NotRequired[str]
  """Echoed `countryCode`, lowercase."""
  regions: NotRequired[list[LocalEntityRegion]]
  """Regions in scope for travel rule questionnaires in this country."""
  lastUpdated: NotRequired[TimestampMillis]
  """Last data update timestamp; 0 if the list is empty."""


class RegionList(RpcEndpoint):
  """Query the active region/city list for a given country, for local entities whose travel rule questionnaire requires a region answer. Currently only supports the AU (Australia) local entity."""

  async def __call__(
    self,
    *,
    country_code: str,
    validate: bool | None = None,
  ) -> LocalEntityRegionList:
    """Query the active region/city list for a given country, for local entities whose travel rule questionnaire requires a region answer. Currently only supports the AU (Australia) local entity.

    Args:
      country_code: ISO 2-digit country code, from `spot.wallet.local_entity.country_list`.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/travel-rule#get-region-list)
    """
    params: dict = {
      'countryCode': country_code,
    }
    _Response = LocalEntityRegionList
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/sapi/v1/localentity/region/list',
      params=params,
      validator=_validator,
      validate=validate,
    )
