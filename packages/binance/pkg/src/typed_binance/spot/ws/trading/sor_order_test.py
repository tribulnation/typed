# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core.endpoint.ws_rpc import WsRpcEndpoint


class CommissionDiscount(TypedDict):
  """Discount applied to standard commissions when paying fees in BNB."""

  enabledForAccount: bool
  """Whether the BNB fee discount is enabled for the account."""
  enabledForSymbol: bool
  """Whether the BNB fee discount is enabled for the symbol."""
  discountAsset: str
  """Asset the discount is denominated in."""
  discount: str
  """Rate the standard commission is reduced by when paying in discountAsset."""


class SorLimitOrder(TypedDict):
  """A limit order routed via smart order routing (SOR): filled at price or better, against liquidity from multiple order books. Requires timeInForce, quantity and price."""

  symbol: str
  """Trading pair symbol."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['LIMIT']
  """Order type. Only LIMIT and MARKET are supported by SOR."""
  timeInForce: Literal['GTC', 'IOC', 'FOK']
  """Time in force. Required for LIMIT orders."""
  quantity: str
  """Order quantity, in the base asset. quoteOrderQty is not supported by SOR."""
  price: str
  """Order price. Required for LIMIT orders."""
  icebergQty: NotRequired[str]
  """Quantity per leg of an iceberg order. Usable with LIMIT orders; requires timeInForce GTC."""
  newClientOrderId: NotRequired[str]
  """Caller-supplied id for the order. Automatically generated if not sent. An order sharing a newClientOrderId with an existing one is only accepted once the existing order has filled."""
  strategyId: NotRequired[int]
  """Id labeling the order strategy this order is part of."""
  strategyType: NotRequired[int]
  """Type labeling the order strategy this order is part of. Cannot be less than 1000000."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT', 'FULL']]
  """Response shape to return. Defaults to FULL."""
  selfTradePreventionMode: NotRequired[
    Literal[
      'NONE', 'EXPIRE_MAKER', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'DECREMENT', 'TRANSFER'
    ]
  ]
  """Self-trade prevention mode. The allowed values depend on the symbol's configuration."""
  computeCommissionRates: NotRequired[bool]
  """Return the commission rates that would apply to the order, at 20x the weight. Default: false."""


class SorMarketOrder(TypedDict):
  """A market order routed via smart order routing (SOR): filled immediately at the best available price(s) across multiple order books. Requires quantity; quoteOrderQty is not supported by SOR."""

  symbol: str
  """Trading pair symbol."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['MARKET']
  """Order type. Only LIMIT and MARKET are supported by SOR."""
  quantity: str
  """Order quantity, in the base asset. quoteOrderQty is not supported by SOR."""
  newClientOrderId: NotRequired[str]
  """Caller-supplied id for the order. Automatically generated if not sent. An order sharing a newClientOrderId with an existing one is only accepted once the existing order has filled."""
  strategyId: NotRequired[int]
  """Id labeling the order strategy this order is part of."""
  strategyType: NotRequired[int]
  """Type labeling the order strategy this order is part of. Cannot be less than 1000000."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT', 'FULL']]
  """Response shape to return. Defaults to FULL."""
  selfTradePreventionMode: NotRequired[
    Literal[
      'NONE', 'EXPIRE_MAKER', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'DECREMENT', 'TRANSFER'
    ]
  ]
  """Self-trade prevention mode. The allowed values depend on the symbol's configuration."""
  computeCommissionRates: NotRequired[bool]
  """Return the commission rates that would apply to the order, at 20x the weight. Default: false."""


class SorOrderTestEmpty(TypedDict):
  """Empty response when computeCommissionRates is false or omitted."""


class StandardCommissionRates(TypedDict):
  """Standard commission rates on trades from the order."""

  maker: str
  """Maker commission rate."""
  taker: str
  """Taker commission rate."""


class TaxCommissionRates(TypedDict):
  """Tax commission rates for trades from the order."""

  maker: str
  """Maker commission rate."""
  taker: str
  """Taker commission rate."""


Request = SorLimitOrder | SorMarketOrder


class SorOrderTestCommission(TypedDict):
  """Estimated commission rates for the order, returned when computeCommissionRates is true. Unlike order.test's OrderTestCommission, this shape carries no specialCommissionForOrder -- the SOR test endpoint's documented response omits it."""

  standardCommissionForOrder: StandardCommissionRates
  taxCommissionForOrder: TaxCommissionRates
  discount: CommissionDiscount


Response = SorOrderTestEmpty | SorOrderTestCommission


class SorOrderTest(WsRpcEndpoint):
  """Test new order creation and signature/recvWindow using smart order routing (SOR). Creates and validates a new order but does not send it into the matching engine. Accepts every parameter sor.order.place does, plus computeCommissionRates. Only LIMIT and MARKET order types are supported; quoteOrderQty is not supported."""

  async def sor_order_test(
    self,
    trading_sor_order_test_request: Request,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Test new order creation and signature/recvWindow using smart order routing (SOR). Creates and validates a new order but does not send it into the matching engine. Accepts every parameter sor.order.place does, plus computeCommissionRates. Only LIMIT and MARKET order types are supported; quoteOrderQty is not supported.

    Args:
      trading_sor_order_test_request:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/web-socket-api.md#test-new-order-using-sor-trade)
    """
    return await self.request(
      trading_sor_order_test_request,
      path='sor.order.test',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
