# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.ws_rpc import WsRpcEndpoint


class OrderAmendment(TypedDict):
  """One amendment made to an order."""

  symbol: str
  """Symbol the amended order was placed on."""
  orderId: int
  """Order ID that was amended."""
  executionId: int
  """Execution ID assigned to this amendment."""
  origClientOrderId: str
  """Client order ID before this amendment."""
  newClientOrderId: str
  """Client order ID after this amendment."""
  origQty: str
  """Order quantity before this amendment, as a decimal string."""
  newQty: str
  """Order quantity after this amendment, as a decimal string."""
  time: TimestampMillis
  """Millisecond timestamp the amendment was made."""


class Request(TypedDict):
  """Request parameters."""

  symbol: str
  """Symbol to query."""
  orderId: int
  """Order ID to query amendments for."""
  fromExecutionId: NotRequired[int]
  """Execution ID to fetch from (inclusive)."""
  limit: NotRequired[int]
  """Maximum number of amendments to return."""


Response = list[OrderAmendment]


class OrderAmendments(WsRpcEndpoint):
  """Queries all amendments of a single order."""

  async def order_amendments(
    self,
    symbol: str,
    *,
    order_id: int,
    from_execution_id: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Queries all amendments of a single order.

    Args:
      symbol: Symbol to query.
      order_id: Order ID to query amendments for.
      from_execution_id: Execution ID to fetch from (inclusive).
      limit: Maximum number of amendments to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/web-socket-api.md#query-order-amendments-user_data)
    """
    request: Request = Request(symbol=symbol, orderId=order_id)
    if from_execution_id is not None:
      request['fromExecutionId'] = from_execution_id
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      path='order.amendments',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
