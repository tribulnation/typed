# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_binance.core.endpoint.ws_rpc import WsRpcEndpoint


class UserDataSubscription(TypedDict):
  """One active User Data Stream subscription on this session."""

  subscriptionId: int
  """Id of an active subscription on this session."""


Response = list[UserDataSubscription]


class SessionSubscriptions(WsRpcEndpoint):
  """List all active User Data Stream subscriptions on this WebSocket API connection's session."""

  async def session_subscriptions(self, *, validate: bool | None = None) -> Response:
    """List all active User Data Stream subscriptions on this WebSocket API connection's session.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/web-socket-api.md#listing-all-subscriptions)
    """
    return await self.request(
      None,
      path='session.subscriptions',
      meta={'security': 'NONE', 'signed': False},
      validate=validate,
      response_type=Response,
    )
