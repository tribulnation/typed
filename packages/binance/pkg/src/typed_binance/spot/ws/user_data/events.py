# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.ws_rpc import WsRpcEndpoint


class AccountPositionBalance(TypedDict):
  """One asset's balance after the change."""

  a: str
  """Asset."""
  f: Decimal
  """Free balance."""
  l: Decimal
  """Locked balance."""


class BalanceUpdateEvent(TypedDict):
  """Pushed whenever the account's available balance changes due to a deposit, withdrawal, or asset transfer between accounts."""

  e: Literal['balanceUpdate']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  a: str
  """Asset."""
  d: Decimal
  """Balance delta."""
  T: TimestampMillis
  """Clear time."""


class EventStreamTerminatedEvent(TypedDict):
  """Pushed when this connection's own User Data Stream subscription ends -- confirmed live, observed on `userDataStream.unsubscribe`."""

  e: Literal['eventStreamTerminated']
  """Event type."""
  E: TimestampMillis
  """Event time."""


class ExecutionReportEvent(TypedDict):
  """Pushed for every order state change: new, canceled, replaced, rejected, or (partially) filled. The single most detailed User Data Stream event."""

  e: Literal['executionReport']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Symbol."""
  c: str
  """Client order id."""
  S: Literal['BUY', 'SELL']
  """Side."""
  o: Literal[
    'LIMIT',
    'MARKET',
    'STOP_LOSS',
    'STOP_LOSS_LIMIT',
    'TAKE_PROFIT',
    'TAKE_PROFIT_LIMIT',
    'LIMIT_MAKER',
  ]
  """Order type. No confirmed exhaustive enum for this push field specifically -- reuses the order-placement type set, unconfirmed against a live push (this pass placed no real order)."""
  f: Literal['GTC', 'IOC', 'FOK']
  """Time in force. No confirmed exhaustive enum for this push field specifically -- unconfirmed against a live push (this pass placed no real order)."""
  q: Decimal
  """Order quantity."""
  p: Decimal
  """Order price."""
  P: Decimal
  """Stop price."""
  F: Decimal
  """Iceberg quantity."""
  g: int
  """Order list id, or -1 if not part of an order list."""
  C: str
  """Original client order id (the id of the order being canceled, for a cancel report)."""
  x: str
  """Current execution type. Documented only by example (`NEW`) in the field it was sourced from; no exhaustive enum confirmed, so left a bare string rather than guessing the closed set."""
  X: Literal[
    'NEW',
    'PENDING_NEW',
    'PARTIALLY_FILLED',
    'FILLED',
    'CANCELED',
    'PENDING_CANCEL',
    'REJECTED',
    'EXPIRED',
    'EXPIRED_IN_MATCH',
  ]
  """Current order status."""
  r: str
  """Order reject reason. Documented only by example (`NONE`); no exhaustive enum confirmed, so left a bare string rather than guessing the closed set."""
  i: int
  """Order id."""
  l: Decimal
  """Last executed quantity."""
  z: Decimal
  """Cumulative filled quantity."""
  L: Decimal
  """Last executed price."""
  n: Decimal
  """Commission amount."""
  N: str | None
  """Commission asset, or null when no commission was charged."""
  T: TimestampMillis
  """Transaction time."""
  t: int
  """Trade id, or -1 if not a trade."""
  v: int
  """Prevented match id -- only present if the order expired due to self-trade prevention."""
  I: int
  """Execution id."""
  w: bool
  """Whether the order is still on the book."""
  m: bool
  """Whether this side of the trade is the maker."""
  M: bool
  """Ignored field, always present in the documented shape."""
  O: TimestampMillis
  """Order creation time."""
  Z: Decimal
  """Cumulative quote asset transacted quantity."""
  Y: Decimal
  """Last quote asset transacted quantity."""
  Q: Decimal
  """Quote order quantity."""
  W: TimestampMillis
  """Working time -- only present once the order has been placed on the book."""
  V: Literal[
    'NONE', 'EXPIRE_MAKER', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'DECREMENT', 'TRANSFER'
  ]
  """Self-trade prevention mode."""
  d: NotRequired[int]
  """Trailing delta -- trailing stop orders only."""
  D: NotRequired[TimestampMillis]
  """Trailing time -- trailing stop orders only."""
  j: NotRequired[int]
  """Strategy id, if provided at order placement."""
  J: NotRequired[int]
  """Strategy type, if provided at order placement."""
  A: NotRequired[Decimal]
  """Prevented quantity -- self-trade-prevention-expired orders only."""
  B: NotRequired[Decimal]
  """Last prevented quantity -- self-trade-prevention-expired orders only."""
  u: NotRequired[int]
  """Trade group id -- self-trade-prevention-expired orders only."""
  U: NotRequired[int]
  """Counter order id -- self-trade-prevention-expired orders only."""
  Cs: NotRequired[str]
  """Counter symbol -- self-trade-prevention-expired orders only."""
  pl: NotRequired[Decimal]
  """Prevented execution quantity -- self-trade-prevention-expired orders only."""
  pL: NotRequired[Decimal]
  """Prevented execution price -- self-trade-prevention-expired orders only."""
  pY: NotRequired[Decimal]
  """Prevented execution quote quantity -- self-trade-prevention-expired orders only."""
  b: NotRequired[str]
  """Match type -- orders with allocations only. No confirmed exhaustive enum, left a bare string."""
  a: NotRequired[int]
  """Allocation id -- orders with allocations only."""
  k: NotRequired[Literal['EXCHANGE', 'SOR']]
  """Working floor -- orders with potential allocations."""
  uS: NotRequired[bool]
  """Whether Smart Order Routing was used."""
  gP: NotRequired[Literal['PRIMARY_PEG', 'MARKET_PEG']]
  """Pegged price type -- pegged orders only."""
  gOT: NotRequired[Literal['PRICE_LEVEL']]
  """Pegged offset type -- pegged orders only."""
  gOV: NotRequired[int]
  """Pegged offset value -- pegged orders only."""
  gp: NotRequired[Decimal]
  """Pegged price -- pegged orders only."""
  eR: NotRequired[
    Literal[
      'NONE',
      'REJECTED',
      'EXCHANGE_CANCELED',
      'OCO_TRIGGER',
      'OTO_PHASE_ONE_EXPIRED',
      'UNFILLED_IOC_QUANTITY_EXPIRED',
      'UNFILLED_FOK_ORDER_EXPIRED',
      'INSUFFICIENT_LIQUIDITY',
      'EXECUTION_RULE_PRICE_RANGE_EXCEEDED',
    ]
  ]
  """Expiry reason -- expired orders only."""


class ExternalLockUpdateEvent(TypedDict):
  """Pushed when an asset is locked or unlocked by an external system (e.g. a regulatory hold)."""

  e: Literal['externalLockUpdate']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  a: str
  """Asset."""
  d: Decimal
  """Lock/unlock delta amount."""
  T: TimestampMillis
  """Transaction time."""


class ListStatusOrder(TypedDict):
  """One order belonging to this order list."""

  s: str
  """Symbol."""
  i: int
  """Order id."""
  c: str
  """Client order id."""


class ListStatusEvent(TypedDict):
  """Pushed for a change in status of an order list (OCO/OTO and their variants)."""

  e: Literal['listStatus']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Symbol."""
  g: int
  """Order list id."""
  c: str
  """Contingency type. No confirmed exhaustive enum, left a bare string (the documented example is `OCO`)."""
  l: str
  """List status type. No confirmed exhaustive enum, left a bare string (the documented example is `EXEC_STARTED`)."""
  L: str
  """List order status. No confirmed exhaustive enum, left a bare string (the documented example is `EXECUTING`)."""
  r: str
  """List reject reason. No confirmed exhaustive enum, left a bare string (the documented example is `NONE`)."""
  C: str
  """List client order id."""
  T: TimestampMillis
  """Transaction time."""
  O: list[ListStatusOrder]
  """Orders in this list."""


class OutboundAccountPositionEvent(TypedDict):
  """Pushed with an account's asset balances whenever those balances change, triggered by events such as an order placement or cancellation."""

  e: Literal['outboundAccountPosition']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  u: TimestampMillis
  """Time of last account update."""
  B: list[AccountPositionBalance]
  """Balances of every asset that changed."""


class UserDataPush(TypedDict):
  """One push frame delivered on this connection once it has subscribed to the account's User Data Stream (`user_data.subscribe_signature`)."""

  subscriptionId: int
  """Id of the subscription this push belongs to."""
  event: (
    OutboundAccountPositionEvent
    | BalanceUpdateEvent
    | ExecutionReportEvent
    | ListStatusEvent
    | EventStreamTerminatedEvent
    | ExternalLockUpdateEvent
  )
  """The pushed event, discriminated by its own `e` field."""


class Events(WsRpcEndpoint):
  """This account's order and balance events, pushed on this WebSocket API connection once it has subscribed via `userDataStream.subscribe.signature` (`user_data.subscribe_signature`). Binance's WS API has exactly one thing a connection can subscribe to -- `userData` is a fixed local identifier, not a venue concept."""

  def events(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[UserDataPush, Any, Any]:
    """This account's order and balance events, pushed on this WebSocket API connection once it has subscribed via `userDataStream.subscribe.signature` (`user_data.subscribe_signature`). Binance's WS API has exactly one thing a connection can subscribe to -- `userData` is a fixed local identifier, not a venue concept.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/user-data-stream.md)
    """
    return self.subscribe(
      'userData',
      None,
      meta={'signed': True},
      validate=validate,
      response_type=UserDataPush,
    )
