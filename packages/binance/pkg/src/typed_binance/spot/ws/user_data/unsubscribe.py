# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_binance.core.endpoint.ws_rpc import WsRpcEndpoint


class UserDataUnsubscribeResult(TypedDict):
  """Empty result confirming the subscription ended."""


class Unsubscribe(WsRpcEndpoint):
  """End this connection's User Data Stream subscription. Confirmed live to need neither an API key nor a signature."""

  async def unsubscribe(
    self, *, validate: bool | None = None
  ) -> UserDataUnsubscribeResult:
    """End this connection's User Data Stream subscription. Confirmed live to need neither an API key nor a signature.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/docs/binance-spot-api-docs/websocket-api/user-data-stream-requests)
    """
    return await self.request(
      None,
      path='userDataStream.unsubscribe',
      meta={'security': 'NONE', 'signed': False},
      validate=validate,
      response_type=UserDataUnsubscribeResult,
    )
