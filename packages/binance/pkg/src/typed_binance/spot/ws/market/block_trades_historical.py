# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.ws_rpc import WsRpcEndpoint


class BlockTrade(TypedDict):
  """One executed block trade."""

  id: int
  """Block trade ID."""
  price: str
  """Trade price."""
  qty: str
  """Trade quantity, in the base asset."""
  quoteQty: str
  """Trade quantity, in the quote asset."""
  time: TimestampMillis
  """Trade time."""
  isBuyerMaker: bool
  """Whether the buyer was the maker."""


class Request(TypedDict):
  """Request parameters."""

  symbol: str
  """Symbol to query."""
  fromId: int
  """Block trade ID to fetch from."""
  limit: NotRequired[int]
  """Number of block trades to return."""


Response = list[BlockTrade]


class BlockTradesHistorical(WsRpcEndpoint):
  """Get historical block trades."""

  async def block_trades_historical(
    self,
    symbol: str,
    *,
    from_id: int,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get historical block trades.

    Args:
      symbol: Symbol to query.
      from_id: Block trade ID to fetch from.
      limit: Number of block trades to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/web-socket-api.md#historical-block-trades)
    """
    request: Request = Request(symbol=symbol, fromId=from_id)
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      path='blockTrades.historical',
      meta={'security': 'NONE', 'signed': False},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
