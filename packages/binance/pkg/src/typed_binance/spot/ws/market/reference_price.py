# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.ws_rpc import WsRpcEndpoint


class ReferencePrice(TypedDict):
  """The reference price used for pegged-order support on a symbol."""

  symbol: str
  """Symbol."""
  referencePrice: str | None
  """Current reference price, or null when no reference price is currently set for the symbol."""
  timestamp: TimestampMillis
  """Timestamp when the reference price was valid."""


class ReferencePriceUnavailable(TypedDict):
  """Returned (per the upstream doc's own example, at frame status 200) when the symbol has never had a reference price set."""

  code: Literal[-2043]
  """Error code."""
  msg: str
  """Error message."""


class Request(TypedDict):
  """Request parameters."""

  symbol: str
  """Symbol to query."""


Response = ReferencePrice | ReferencePriceUnavailable


class ReferencePriceEndpoint(WsRpcEndpoint):
  """Query the reference price used for pegged-order support on a symbol."""

  async def reference_price(
    self, symbol: str, *, validate: bool | None = None
  ) -> Response:
    """Query the reference price used for pegged-order support on a symbol.

    Args:
      symbol: Symbol to query.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/web-socket-api.md#query-reference-price)
    """
    return await self.request(
      Request(symbol=symbol),
      path='referencePrice',
      meta={'security': 'NONE', 'signed': False},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
