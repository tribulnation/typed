# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core.endpoint.ws_rpc import WsRpcEndpoint
from typed_binance.schemas import SpotTrade


class Request(TypedDict):
  """Request parameters."""

  symbol: str
  """Symbol to query."""
  fromId: NotRequired[int]
  """Trade ID to begin at. Defaults to the most recent trades."""
  limit: NotRequired[int]
  """Number of trades to return."""


Response = list[SpotTrade]


class TradesHistorical(WsRpcEndpoint):
  """Get historical trades. If `fromId` is not specified, the most recent trades are returned."""

  async def trades_historical(
    self,
    symbol: str,
    *,
    from_id: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get historical trades. If `fromId` is not specified, the most recent trades are returned.

    Args:
      symbol: Symbol to query.
      from_id: Trade ID to begin at. Defaults to the most recent trades.
      limit: Number of trades to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/web-socket-api.md#historical-trades)
    """
    request: Request = Request(symbol=symbol)
    if from_id is not None:
      request['fromId'] = from_id
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      path='trades.historical',
      meta={'security': 'NONE', 'signed': False},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
