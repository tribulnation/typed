# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class Kline(TypedDict):
  """The kline/candlestick itself."""

  t: TimestampMillis
  """Kline start time."""
  T: TimestampMillis
  """Kline close time."""
  s: str
  """Symbol."""
  i: Literal[
    '1s',
    '1m',
    '3m',
    '5m',
    '15m',
    '30m',
    '1h',
    '2h',
    '4h',
    '6h',
    '8h',
    '12h',
    '1d',
    '3d',
    '1w',
    '1M',
  ]
  """Interval."""
  f: int
  """First trade ID."""
  L: int
  """Last trade ID."""
  o: str
  """Open price."""
  c: str
  """Close price."""
  h: str
  """High price."""
  l: str
  """Low price."""
  v: str
  """Base asset volume."""
  n: int
  """Number of trades."""
  x: bool
  """Whether this kline is closed."""
  q: str
  """Quote asset volume."""
  V: str
  """Taker buy base asset volume."""
  Q: str
  """Taker buy quote asset volume."""
  B: str
  """Ignore."""


class KlineEvent(TypedDict):
  """One kline/candlestick update."""

  e: Literal['kline']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Symbol."""
  k: Kline


class KlineEndpoint(StreamEndpoint):
  """Pushes updates to the current kline/candlestick, computed in the UTC+0 timezone. Updated every 1000ms for the `1s` interval, every 2000ms for all other intervals."""

  def kline(
    self,
    symbol: str,
    *,
    interval: Literal[
      '1s',
      '1m',
      '3m',
      '5m',
      '15m',
      '30m',
      '1h',
      '2h',
      '4h',
      '6h',
      '8h',
      '12h',
      '1d',
      '3d',
      '1w',
      '1M',
    ],
    validate: bool | None = None,
  ) -> StreamManager[KlineEvent, Any, Any]:
    """Pushes updates to the current kline/candlestick, computed in the UTC+0 timezone. Updated every 1000ms for the `1s` interval, every 2000ms for all other intervals.

    Args:
      symbol: Trading pair symbol, e.g. `btcusdt`. The venue requires stream names to use lowercase symbols on the wire, and this value is used as-is with no casing transform.
      interval: Kline interval.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/web-socket-streams.md#klinecandlestick-streams-for-utc)
    """
    return self.subscribe(
      f'{symbol}@kline_{interval}',
      validate=validate,
      response_type=KlineEvent,
    )
