# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, TypedDict
from typed_binance.core.endpoint.stream import StreamEndpoint


class BookTickerEvent(TypedDict):
  """Best bid/ask price and quantity update for one symbol's order book."""

  u: int
  """Order book update ID."""
  s: str
  """Symbol."""
  b: str
  """Best bid price."""
  B: str
  """Best bid quantity."""
  a: str
  """Best ask price."""
  A: str
  """Best ask quantity."""


class BookTicker(StreamEndpoint):
  """Pushes any update to the best bid or ask price or quantity, in real time, for one symbol. Multiple `<symbol>@bookTicker` streams can be subscribed to over one connection."""

  def book_ticker(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[BookTickerEvent, Any, Any]:
    """Pushes any update to the best bid or ask price or quantity, in real time, for one symbol. Multiple `<symbol>@bookTicker` streams can be subscribed to over one connection.

    Args:
      symbol: Trading pair symbol, e.g. `btcusdt`. The venue requires stream names to use lowercase symbols on the wire, and this value is used as-is with no casing transform.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/web-socket-streams.md#individual-symbol-book-ticker-streams)
    """
    return self.subscribe(
      f'{symbol}@bookTicker',
      validate=validate,
      response_type=BookTickerEvent,
    )
