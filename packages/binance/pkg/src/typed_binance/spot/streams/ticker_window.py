# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class TickerWindowEventPayload(TypedDict):
  """Rolling-window ticker statistics for one symbol over a caller-chosen window size, as pushed on `<symbol>@ticker_<window>`. Shared verbatim by `streams.ticker_window` (one object) and `streams.ticker_window_all` (an array of these, one per changed symbol). Distinct from the REST `TickerWindowFull`/`TickerWindowMini` schemas -- the wire push uses Binance's abbreviated single-letter field names, not the REST response's spelled-out ones."""

  e: str
  """Event type, e.g. `1hTicker`, `4hTicker`, `1dTicker` -- matches the subscribed window size."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Symbol."""
  p: str
  """Absolute price change over the window."""
  P: str
  """Relative price change over the window, in percent."""
  o: str
  """Open price."""
  h: str
  """High price."""
  l: str
  """Low price."""
  c: str
  """Last price."""
  w: str
  """Weighted average price."""
  v: str
  """Total traded base asset volume."""
  q: str
  """Total traded quote asset volume."""
  O: TimestampMillis
  """Statistics open time. Always starts on a minute boundary."""
  C: TimestampMillis
  """Statistics close time -- the current time of the update, so the effective window may be up to 59999ms wider than the subscribed window size."""
  F: int
  """First trade ID considered."""
  L: int
  """Last trade ID considered."""
  n: int
  """Total number of trades."""


class TickerWindow(StreamEndpoint):
  """Pushes rolling-window ticker statistics for one symbol, computed over a caller-chosen window, every 1000ms."""

  def ticker_window(
    self,
    symbol: str,
    *,
    window: Literal['1h', '4h', '1d'],
    validate: bool | None = None,
  ) -> StreamManager[TickerWindowEventPayload, Any, Any]:
    """Pushes rolling-window ticker statistics for one symbol, computed over a caller-chosen window, every 1000ms.

    Args:
      symbol: Trading pair symbol, e.g. `btcusdt`. The venue requires stream names to use lowercase symbols on the wire, and this value is used as-is with no casing transform.
      window: Rolling window size.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/web-socket-streams.md#individual-symbol-rolling-window-statistics-streams)
    """
    return self.subscribe(
      f'{symbol}@ticker_{window}',
      validate=validate,
      response_type=TickerWindowEventPayload,
    )
