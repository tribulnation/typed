# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint
from typed_binance.schemas import OrderBookLevel


class DiffDepthEvent(TypedDict):
  """One order book diff update."""

  e: Literal['depthUpdate']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Symbol."""
  U: int
  """First update ID in this event."""
  u: int
  """Final update ID in this event."""
  b: list[OrderBookLevel]
  """Bid price levels to update."""
  a: list[OrderBookLevel]
  """Ask price levels to update."""


class DiffDepth(StreamEndpoint):
  """Pushes order book price and quantity depth updates for one symbol, every 1000ms, used to locally maintain an order book."""

  def diff_depth(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[DiffDepthEvent, Any, Any]:
    """Pushes order book price and quantity depth updates for one symbol, every 1000ms, used to locally maintain an order book.

    Args:
      symbol: Trading pair symbol, e.g. `btcusdt`. The venue requires stream names to use lowercase symbols on the wire, and this value is used as-is with no casing transform.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/web-socket-streams.md#diff-depth-stream)
    """
    return self.subscribe(
      f'{symbol}@depth',
      validate=validate,
      response_type=DiffDepthEvent,
    )
