# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal
from typed_binance.core.endpoint.stream import StreamEndpoint
from typed_binance.schemas import TickerWindowEvent


Payload = list[TickerWindowEvent]


class TickerWindowAll(StreamEndpoint):
  """Pushes rolling-window ticker statistics for all symbols that changed, as an array, computed over a caller-chosen window, every 1000ms."""

  def ticker_window_all(
    self,
    window: Literal['1h', '4h', '1d'],
    *,
    validate: bool | None = None,
  ) -> StreamManager[Payload, Any, Any]:
    """Pushes rolling-window ticker statistics for all symbols that changed, as an array, computed over a caller-chosen window, every 1000ms.

    Args:
      window: Rolling window size.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/web-socket-streams.md#all-market-rolling-window-statistics-streams)
    """
    return self.subscribe(
      f'!ticker_{window}@arr',
      validate=validate,
      response_type=Payload,
    )
