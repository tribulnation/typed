# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class AvgPriceEvent(TypedDict):
  """Average price update for one symbol."""

  e: Literal['avgPrice']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Symbol."""
  i: str
  """Average price interval, e.g. `5m`."""
  w: str
  """Average price."""
  T: TimestampMillis
  """Last trade time."""


class AvgPrice(StreamEndpoint):
  """Pushes changes in the average price over a fixed time interval, every 1000ms."""

  def avg_price(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[AvgPriceEvent, Any, Any]:
    """Pushes changes in the average price over a fixed time interval, every 1000ms.

    Args:
      symbol: Trading pair symbol, e.g. `btcusdt`. The venue requires stream names to use lowercase symbols on the wire, and this value is used as-is with no casing transform.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/web-socket-streams.md#average-price)
    """
    return self.subscribe(
      f'{symbol}@avgPrice',
      validate=validate,
      response_type=AvgPriceEvent,
    )
