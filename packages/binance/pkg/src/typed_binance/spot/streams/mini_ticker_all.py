# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any
from typed_binance.core.endpoint.stream import StreamEndpoint
from typed_binance.schemas import MiniTickerEvent


Payload = list[MiniTickerEvent]


class MiniTickerAll(StreamEndpoint):
  """Pushes 24hr rolling-window mini-ticker statistics for all symbols that changed, as an array, every 1000ms. Not statistics of the UTC day -- a rolling window over the previous 24 hours. Only tickers that changed are present in the array."""

  def mini_ticker_all(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[Payload, Any, Any]:
    """Pushes 24hr rolling-window mini-ticker statistics for all symbols that changed, as an array, every 1000ms. Not statistics of the UTC day -- a rolling window over the previous 24 hours. Only tickers that changed are present in the array.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/web-socket-streams.md#all-market-mini-tickers-stream)
    """
    return self.subscribe(
      '!miniTicker@arr',
      None,
      validate=validate,
      response_type=Payload,
    )
