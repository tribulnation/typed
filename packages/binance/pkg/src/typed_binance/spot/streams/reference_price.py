# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class ReferencePriceEvent(TypedDict):
  """Reference price update for one symbol."""

  e: Literal['referencePrice']
  """Event type."""
  s: str
  """Symbol."""
  r: str | None
  """Reference price. `null` when no reference price is available."""
  t: TimestampMillis
  """Engine timestamp when the reference price was valid."""


class ReferencePrice(StreamEndpoint):
  """Pushes the reference price for a symbol, updated every 1000ms."""

  def reference_price(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[ReferencePriceEvent, Any, Any]:
    """Pushes the reference price for a symbol, updated every 1000ms.

    Args:
      symbol: Trading pair symbol, e.g. `btcusdt`. The venue requires stream names to use lowercase symbols on the wire, and this value is used as-is with no casing transform.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/web-socket-streams.md#reference-price-streams)
    """
    return self.subscribe(
      f'{symbol}@referencePrice',
      validate=validate,
      response_type=ReferencePriceEvent,
    )
