# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class MiniTickerEventPayload(TypedDict):
  """24hr rolling-window mini-ticker statistics for one symbol, as pushed on `<symbol>@miniTicker`. Shared verbatim by `streams.mini_ticker` (one object) and `streams.mini_ticker_all` (an array of these, one per changed symbol)."""

  e: Literal['24hrMiniTicker']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Symbol."""
  c: str
  """Close price."""
  o: str
  """Open price."""
  h: str
  """High price."""
  l: str
  """Low price."""
  v: str
  """Total traded base asset volume."""
  q: str
  """Total traded quote asset volume."""


class MiniTicker(StreamEndpoint):
  """Pushes 24hr rolling-window mini-ticker statistics for one symbol, every 1000ms. Not statistics of the UTC day -- a rolling window over the previous 24 hours."""

  def mini_ticker(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[MiniTickerEventPayload, Any, Any]:
    """Pushes 24hr rolling-window mini-ticker statistics for one symbol, every 1000ms. Not statistics of the UTC day -- a rolling window over the previous 24 hours.

    Args:
      symbol: Trading pair symbol, e.g. `btcusdt`. The venue requires stream names to use lowercase symbols on the wire, and this value is used as-is with no casing transform.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/web-socket-streams.md#individual-symbol-mini-ticker-stream)
    """
    return self.subscribe(
      f'{symbol}@miniTicker',
      validate=validate,
      response_type=MiniTickerEventPayload,
    )
