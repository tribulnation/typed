# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class BlockTradeEvent(TypedDict):
  """One executed block trade."""

  e: Literal['blockTrade']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Symbol."""
  t: int
  """Block trade ID."""
  p: str
  """Price."""
  q: str
  """Quantity."""
  T: TimestampMillis
  """Trade time."""
  m: bool
  """Whether the buyer was the maker."""


class BlockTrade(StreamEndpoint):
  """Pushes block trade information in real time."""

  def block_trade(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[BlockTradeEvent, Any, Any]:
    """Pushes block trade information in real time.

    Args:
      symbol: Trading pair symbol, e.g. `btcusdt`. The venue requires stream names to use lowercase symbols on the wire, and this value is used as-is with no casing transform.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/web-socket-streams.md#block-trade-streams)
    """
    return self.subscribe(
      f'{symbol}@blockTrade',
      validate=validate,
      response_type=BlockTradeEvent,
    )
