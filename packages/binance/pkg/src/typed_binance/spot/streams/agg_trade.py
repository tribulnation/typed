# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class AggTradeEvent(TypedDict):
  """One aggregated taker trade."""

  e: Literal['aggTrade']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Symbol."""
  a: int
  """Aggregate trade ID."""
  p: str
  """Price."""
  q: str
  """Quantity."""
  f: int
  """First trade ID aggregated into this trade."""
  l: int
  """Last trade ID aggregated into this trade."""
  T: TimestampMillis
  """Trade time."""
  m: bool
  """Whether the buyer was the market maker."""
  M: bool
  """Ignore."""


class AggTrade(StreamEndpoint):
  """Pushes trade information aggregated for a single taker order, in real time."""

  def agg_trade(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[AggTradeEvent, Any, Any]:
    """Pushes trade information aggregated for a single taker order, in real time.

    Args:
      symbol: Trading pair symbol, e.g. `btcusdt`. The venue requires stream names to use lowercase symbols on the wire, and this value is used as-is with no casing transform.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/web-socket-streams.md#aggregate-trade-streams)
    """
    return self.subscribe(
      f'{symbol}@aggTrade',
      validate=validate,
      response_type=AggTradeEvent,
    )
