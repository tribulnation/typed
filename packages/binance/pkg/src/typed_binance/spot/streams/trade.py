# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class TradeEvent(TypedDict):
  """One executed trade."""

  e: Literal['trade']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Symbol."""
  t: int
  """Trade ID."""
  p: str
  """Price."""
  q: str
  """Quantity."""
  T: TimestampMillis
  """Trade time."""
  m: bool
  """Whether the buyer was the market maker."""
  M: bool
  """Ignore."""


class Trade(StreamEndpoint):
  """Pushes raw trade information in real time; each trade has a unique buyer and seller."""

  def trade(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[TradeEvent, Any, Any]:
    """Pushes raw trade information in real time; each trade has a unique buyer and seller.

    Args:
      symbol: Trading pair symbol, e.g. `btcusdt`. The venue requires stream names to use lowercase symbols on the wire, and this value is used as-is with no casing transform.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/web-socket-streams.md#trade-streams)
    """
    return self.subscribe(
      f'{symbol}@trade',
      validate=validate,
      response_type=TradeEvent,
    )
