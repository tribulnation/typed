# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class PmMarginAccountBorrow(TypedDict):
  """Apply for a margin loan."""

  tranId: NotRequired[int]
  """Transaction ID."""


class MarginBorrow(RpcEndpoint):
  """Margin Account Borrow"""

  async def margin_borrow(
    self,
    *,
    asset: str,
    amount: float,
    validate: bool | None = None,
  ) -> PmMarginAccountBorrow:
    """Apply for a margin loan.

    Args:
      asset: Asset name.
      amount: Amount.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-derivatives-trading-portfolio-margin/api/rest-api/trade#margin-account-borrow)
    """
    params: dict = {
      'asset': asset,
      'amount': amount,
    }
    _Response = PmMarginAccountBorrow
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/papi/v1/marginLoan',
      params=params,
      validator=_validator,
      validate=validate,
    )
