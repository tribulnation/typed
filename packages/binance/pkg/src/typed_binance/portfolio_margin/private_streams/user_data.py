# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.private_stream import PrivateStreamEndpoint


class BalanceUpdateEvent(TypedDict):
  """Cross-margin balance update."""

  e: Literal['balanceUpdate']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  a: str
  """Asset."""
  d: Decimal
  """Balance delta."""
  U: int
  """Event update id."""
  T: TimestampMillis
  """Clear time."""


class ConditionalOrderUpdateDetail(TypedDict):
  """The conditional (strategy) order this event is about."""

  s: str
  """Symbol."""
  c: str
  """Strategy client order id."""
  si: int
  """Strategy id."""
  S: Literal['BUY', 'SELL']
  """Side."""
  st: Literal[
    'STOP', 'TAKE_PROFIT', 'STOP_MARKET', 'TAKE_PROFIT_MARKET', 'TRAILING_STOP_MARKET'
  ]
  """Strategy (conditional order) type."""
  f: Literal['GTC', 'IOC', 'FOK', 'GTX']
  """Time in force."""
  q: Decimal
  """Quantity."""
  p: Decimal
  """Price."""
  sp: NotRequired[Decimal]
  """Stop price. Ignore for a TRAILING_STOP_MARKET order."""
  os: Literal['NEW', 'CANCELED', 'EXPIRED', 'TRIGGERED', 'FINISHED']
  """Strategy order status."""
  T: TimestampMillis
  """Order book time."""
  ut: TimestampMillis
  """Order update time."""
  R: NotRequired[bool]
  """Whether this order only reduces an existing position."""
  wt: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """Stop-price working type."""
  ps: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side."""
  cp: NotRequired[bool]
  """Whether this is a close-all order. Pushed with a conditional order."""
  AP: NotRequired[Decimal]
  """Activation price. Only pushed with a TRAILING_STOP_MARKET order."""
  cr: NotRequired[Decimal]
  """Callback rate. Only pushed with a TRAILING_STOP_MARKET order."""
  i: int
  """Order id."""
  V: NotRequired[Literal['NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER']]
  """Self-trade prevention mode."""
  gtd: NotRequired[TimestampMillis]
  """TIF GTD order auto-cancel time. 0 if not applicable."""


class ExecutionReportEvent(TypedDict):
  """Pushed for a cross-margin order update. `x`/Execution type: `NEW` accepted into the engine; `CANCELED` cancelled by the user; `REJECTED` rejected and not processed (only seen on a Cancel-Replace request whose new order placement was rejected but whose cancel succeeded); `TRADE` part or all of the order's quantity filled; `EXPIRED` cancelled per the order type's own rules (e.g. a LIMIT FOK with no fill, a partially-filled LIMIT IOC or MARKET) or by the exchange (e.g. liquidation, maintenance); `TRADE_PREVENTION` expired due to an STP trigger."""

  e: Literal['executionReport']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Symbol."""
  c: str
  """Client order id."""
  S: Literal['BUY', 'SELL']
  """Side."""
  o: str
  """Order type. No documented closed set on this page; example: `LIMIT`."""
  f: Literal['GTC', 'IOC', 'FOK', 'GTX']
  """Time in force."""
  q: Decimal
  """Order quantity."""
  p: Decimal
  """Order price."""
  P: NotRequired[Decimal]
  """Stop price."""
  d: NotRequired[int]
  """Trailing delta. Only present if this was a trailing-stop order."""
  F: NotRequired[Decimal]
  """Iceberg quantity. Not present if this is not an iceberg order."""
  g: NotRequired[int]
  """Order list id. `-1` if this order is not part of a list."""
  C: NotRequired[str]
  """Original client order id. Only present on a cancellation, naming the order being cancelled."""
  x: Literal['NEW', 'CANCELED', 'REJECTED', 'TRADE', 'EXPIRED', 'TRADE_PREVENTION']
  """Current execution type."""
  X: Literal['NEW', 'PARTIALLY_FILLED', 'FILLED', 'CANCELED', 'REJECTED', 'EXPIRED']
  """Current order status."""
  r: NotRequired[str]
  """Order reject reason. Only present on a rejection, as an error code; `NONE` otherwise."""
  i: int
  """Order id."""
  l: Decimal
  """Last executed quantity."""
  z: Decimal
  """Cumulative filled quantity."""
  L: Decimal
  """Last executed price."""
  n: Decimal
  """Commission amount."""
  N: NotRequired[str]
  """Commission asset. Only present when there is a commission amount."""
  T: TimestampMillis
  """Transaction time."""
  t: int
  """Trade id. `-1` if this event is not itself a trade."""
  v: NotRequired[int]
  """Prevented match id. Only present if the order expired due to an STP trigger."""
  I: int
  """Event update id."""
  w: bool
  """Whether the order is still on the book."""
  m: bool
  """Whether this trade is the maker side."""
  O: TimestampMillis
  """Order creation time."""
  Z: NotRequired[Decimal]
  """Cumulative quote-asset transacted quantity."""
  Y: NotRequired[Decimal]
  """Last quote-asset transacted quantity (last price times last quantity)."""
  Q: NotRequired[Decimal]
  """Quote order quantity. Only present if the order specified one."""
  D: NotRequired[TimestampMillis]
  """Trailing time. Only present once a trailing-stop order has activated."""
  j: NotRequired[int]
  """Strategy id. Only present if `strategyId` was given when the order was placed."""
  J: NotRequired[int]
  """Strategy type. Only present if `strategyType` was given when the order was placed."""
  W: NotRequired[TimestampMillis]
  """Working time. Only present once the order has been placed on the book."""
  V: NotRequired[Literal['NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER']]
  """Self-trade prevention mode."""
  u: NotRequired[int]
  """Trade group id. Only present if the account is part of a trade group and the order expired due to an STP trigger."""
  U: NotRequired[int]
  """Counter order id. Only present if the order expired due to an STP trigger."""
  A: NotRequired[Decimal]
  """Prevented quantity. Only present if the order expired due to an STP trigger."""
  B: NotRequired[Decimal]
  """Last prevented quantity. Only present if the order expired due to an STP trigger."""
  Cs: NotRequired[str]
  """Counter symbol. Only present if the order expired due to an STP trigger."""
  pl: NotRequired[Decimal]
  """Prevented execution quantity. Only present if the order expired due to an STP trigger."""
  pL: NotRequired[Decimal]
  """Prevented execution price. Only present if the order expired due to an STP trigger."""
  pY: NotRequired[Decimal]
  """Prevented execution quote quantity. Only present if the order expired due to an STP trigger."""
  eR: NotRequired[str]
  """Expiry reason. Only present if the order has expired."""


class LiabilityChangeEvent(TypedDict):
  """Cross-margin liability update."""

  e: Literal['liabilityChange']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  a: str
  """Asset."""
  t: Literal['BORROW', 'REPAY', 'INTEREST']
  """Liability change type."""
  T: int
  """Transaction id. Not a timestamp despite the name -- an opaque, venue-assigned transaction identifier."""
  p: Decimal
  """Principal."""
  i: Decimal
  """Interest."""
  l: Decimal
  """Total liability."""


class ListenKeyExpiredEvent(TypedDict):
  """Pushed when the listenKey backing this connection expires. Unrelated to socket disconnection; only fires for a listenKey that was genuinely valid and then expired. No further user-data events arrive on this connection after it, until a new listenKey is used."""

  e: Literal['listenKeyExpired']
  """Event type."""
  E: TimestampMillis
  """Event time."""


class MarginAccountBalance(TypedDict):
  """One asset balance possibly changed by this update."""

  a: NotRequired[str]
  """Asset."""
  f: NotRequired[Decimal]
  """Free balance."""
  l: NotRequired[Decimal]
  """Locked balance."""


class OpenOrderLossItem(TypedDict):
  """One asset's open-order loss."""

  a: NotRequired[str]
  """Asset."""
  o: NotRequired[Decimal]
  """Amount."""


class PmAccountBalanceUpdate(TypedDict):
  """One asset balance changed by this update."""

  a: NotRequired[str]
  """Asset."""
  wb: NotRequired[Decimal]
  """Wallet balance."""
  cw: NotRequired[Decimal]
  """Cross wallet balance."""
  bc: NotRequired[Decimal]
  """Balance change, excluding PnL and commission."""


class PmAccountConfigLeverageUpdate(TypedDict):
  """The trade pair's new leverage."""

  s: str
  """Symbol."""
  l: int
  """New leverage."""


class PmAccountPositionUpdate(TypedDict):
  """One position changed by this update."""

  s: NotRequired[str]
  """Symbol."""
  pa: NotRequired[Decimal]
  """Position amount."""
  ep: NotRequired[Decimal]
  """Entry price."""
  cr: NotRequired[Decimal]
  """Pre-fee accumulated realized PnL."""
  up: NotRequired[Decimal]
  """Unrealized PnL."""
  ps: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side."""
  bep: NotRequired[Decimal]
  """Breakeven price."""


class PmAlgoOrderUpdate(TypedDict):
  """The algo (conditional) order this event is about."""

  caid: str
  """Client algo id."""
  aid: int
  """Algo id."""
  at: NotRequired[str]
  """Algo type. No documented closed set; example: `CONDITIONAL`."""
  o: NotRequired[str]
  """Order type. No documented closed set for the algo-order context specifically; example: `TAKE_PROFIT`."""
  s: str
  """Symbol."""
  S: Literal['BUY', 'SELL']
  """Side."""
  ps: Literal['BOTH', 'LONG', 'SHORT']
  """Position side."""
  f: Literal['GTC', 'IOC', 'FOK', 'GTX']
  """Time in force."""
  q: Decimal
  """Quantity."""
  X: Literal[
    'NEW', 'CANCELED', 'TRIGGERING', 'TRIGGERED', 'FINISHED', 'REJECTED', 'EXPIRED'
  ]
  """Algo status."""
  ai: NotRequired[str]
  """Order id assigned once the algo order is placed into the matching engine. Empty until then."""
  ap: NotRequired[Decimal]
  """Average fill price in the matching engine. Only present once the order is triggered and placed."""
  aq: NotRequired[Decimal]
  """Executed quantity in the matching engine. Only present once the order is triggered and placed."""
  act: NotRequired[str]
  """Actual order type in the matching engine. Only present once the order is triggered and placed."""
  tp: NotRequired[Decimal]
  """Trigger price."""
  p: NotRequired[Decimal]
  """Order price."""
  V: NotRequired[Literal['NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER']]
  """Self-trade prevention mode."""
  wt: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """Working type."""
  pm: NotRequired[
    Literal[
      'NONE',
      'OPPONENT',
      'OPPONENT_5',
      'OPPONENT_10',
      'OPPONENT_20',
      'QUEUE',
      'QUEUE_5',
      'QUEUE_10',
      'QUEUE_20',
    ]
  ]
  """Price match mode."""
  cp: NotRequired[bool]
  """Whether this is a close-all order."""
  pP: NotRequired[bool]
  """Whether price protection is active for this trigger."""
  R: NotRequired[bool]
  """Whether this order only reduces an existing position."""
  tt: NotRequired[TimestampMillis]
  """Trigger time. 0 if not yet triggered."""
  gtd: NotRequired[TimestampMillis]
  """Good-till-time for a GTD time-in-force order. 0 if not applicable."""
  rm: NotRequired[str]
  """Reason the algo order failed. Empty when not applicable."""
  ia: NotRequired[bool]
  """Whether this order is activated. Only meaningful for a trailing order."""


class PmOrderUpdateDetail(TypedDict):
  """The order this event is about."""

  s: str
  """Symbol."""
  c: str
  """Client order id. `autoclose-<id>` on a liquidation execution, `adl_autoclose` on ADL."""
  S: Literal['BUY', 'SELL']
  """Side."""
  o: Literal['MARKET', 'LIMIT', 'LIQUIDATION']
  """Order type."""
  f: Literal['GTC', 'IOC', 'FOK', 'GTX']
  """Time in force."""
  q: Decimal
  """Original quantity."""
  p: Decimal
  """Original price."""
  ap: Decimal
  """Average price."""
  sp: str
  """Ignore."""
  x: Literal['NEW', 'CANCELED', 'CALCULATED', 'EXPIRED', 'TRADE']
  """Execution type."""
  X: Literal[
    'NEW', 'PARTIALLY_FILLED', 'FILLED', 'CANCELED', 'EXPIRED', 'EXPIRED_IN_MATCH'
  ]
  """Order status."""
  i: int
  """Order id."""
  M: NotRequired[str]
  """modifyId, only pushed for an AMENDMENT (order modification) event when a modifyId was provided in the request."""
  l: NotRequired[Decimal]
  """Order last-filled quantity."""
  z: NotRequired[Decimal]
  """Order filled accumulated quantity."""
  L: NotRequired[Decimal]
  """Last filled price."""
  N: NotRequired[str]
  """Commission asset. Not pushed when there is no commission."""
  n: NotRequired[Decimal]
  """Commission. Not pushed when there is no commission."""
  T: NotRequired[TimestampMillis]
  """Order trade time."""
  t: NotRequired[int]
  """Trade id."""
  b: NotRequired[Decimal]
  """Bids notional."""
  a: NotRequired[Decimal]
  """Ask notional."""
  m: NotRequired[bool]
  """Whether this trade is the maker side."""
  R: NotRequired[bool]
  """Whether this order only reduces an existing position."""
  ps: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side."""
  rp: NotRequired[Decimal]
  """Realized profit of the trade."""
  st: NotRequired[str]
  """Strategy type. Only pushed when a conditional order triggered this fill."""
  si: NotRequired[int]
  """Strategy id. Only pushed when a conditional order triggered this fill."""
  V: NotRequired[Literal['NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER']]
  """Self-trade prevention mode."""
  gtd: NotRequired[TimestampMillis]
  """TIF GTD order auto-cancel time. 0 if not applicable."""


class RiskLevelChangeEvent(TypedDict):
  """Pushed when the account's position risk ratio is too high, as risk guidance only (not a recommended trading signal). In a highly volatile market the position may already have been liquidated by the time this arrives."""

  e: Literal['RISK_LEVEL_CHANGE']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  u: Decimal
  """uniMMR level."""
  s: Literal['MARGIN_CALL', 'REDUCE_ONLY', 'FORCE_LIQUIDATION']
  """Risk level."""
  eq: Decimal
  """Account equity, in USD value."""
  ae: Decimal
  """Actual equity without collateral rate, in USD value."""
  m: Decimal
  """Total maintenance margin, in USD value."""


class AccountConfigUpdateEvent(TypedDict):
  """Pushed when the account configuration changes -- a per-symbol leverage change."""

  e: Literal['ACCOUNT_CONFIG_UPDATE']
  """Event type."""
  fs: Literal['UM', 'CM']
  """Event business unit: `UM` for USDS-M futures, `CM` for COIN-M futures."""
  E: TimestampMillis
  """Event time."""
  T: TimestampMillis
  """Transaction time."""
  ac: PmAccountConfigLeverageUpdate


class AlgoOrderUpdateEvent(TypedDict):
  """Pushed when an algo (conditional) order is created or its status changes."""

  e: Literal['ALGO_UPDATE']
  """Event type."""
  T: TimestampMillis
  """Transaction time."""
  E: TimestampMillis
  """Event time."""
  fs: Literal['UM', 'CM']
  """Event business unit: `UM` for USDS-M futures, `CM` for COIN-M futures."""
  ao: PmAlgoOrderUpdate


class ConditionalOrderTradeUpdateEvent(TypedDict):
  """Pushed when a conditional (strategy) order is created or its status changes."""

  e: Literal['CONDITIONAL_ORDER_TRADE_UPDATE']
  """Event type."""
  T: TimestampMillis
  """Transaction time."""
  E: TimestampMillis
  """Event time."""
  fs: Literal['UM', 'CM']
  """Event business unit: `UM` for USDS-M futures, `CM` for COIN-M futures."""
  so: ConditionalOrderUpdateDetail


class OpenOrderLossEvent(TypedDict):
  """Cross-margin order-margin stream: potential loss from currently open orders, per asset."""

  e: Literal['openOrderLoss']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  O: list[OpenOrderLossItem]
  """One asset's open-order loss."""


class OrderTradeUpdateEvent(TypedDict):
  """Pushed when an order is created or its status changes."""

  e: Literal['ORDER_TRADE_UPDATE']
  """Event type."""
  fs: Literal['UM', 'CM']
  """Event business unit: `UM` for USDS-M futures, `CM` for COIN-M futures."""
  E: TimestampMillis
  """Event time."""
  T: TimestampMillis
  """Transaction time."""
  i: NotRequired[str]
  """Account alias. Ignore for UM."""
  o: PmOrderUpdateDetail


class OutboundAccountPositionEvent(TypedDict):
  """Sent any time a cross-margin account balance has changed, carrying the assets possibly changed by the event that generated the balance change."""

  e: Literal['outboundAccountPosition']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  u: TimestampMillis
  """Time of last account update."""
  U: int
  """Time update id."""
  B: list[MarginAccountBalance]
  """One asset balance possibly changed by this update."""


class PmAccountUpdateData(TypedDict):
  """The balance/position update."""

  m: Literal[
    'DEPOSIT',
    'WITHDRAW',
    'ORDER',
    'FUNDING_FEE',
    'WITHDRAW_REJECT',
    'ADJUSTMENT',
    'INSURANCE_CLEAR',
    'ADMIN_DEPOSIT',
    'ADMIN_WITHDRAW',
    'MARGIN_TRANSFER',
    'MARGIN_TYPE_CHANGE',
    'ASSET_TRANSFER',
    'OPTIONS_PREMIUM_FEE',
    'OPTIONS_SETTLE_PROFIT',
    'AUTO_EXCHANGE',
    'COIN_SWAP_DEPOSIT',
    'COIN_SWAP_WITHDRAW',
  ]
  """Event reason type."""
  B: list[PmAccountBalanceUpdate]
  """One asset balance changed by this update."""
  P: NotRequired[list[PmAccountPositionUpdate]]
  """One position changed by this update."""
  S: NotRequired[str]
  """Symbol associated with a FUNDING_FEE event. Present only for that reason type."""


class AccountUpdateEvent(TypedDict):
  """Pushed when balance or position changes on the account, including a periodic FUNDING_FEE settlement; unfilled or cancelled orders alone do not push it. Only the symbols whose positions changed are included. A FUNDING_FEE update on a crossed position includes only `a.B` (the funding-fee asset's balance) with no `a.P`; on an isolated position it includes both `a.B` and the one affected `a.P`."""

  e: Literal['ACCOUNT_UPDATE']
  """Event type."""
  fs: Literal['UM', 'CM']
  """Event business unit: `UM` for USDS-M futures, `CM` for COIN-M futures."""
  E: TimestampMillis
  """Event time."""
  T: TimestampMillis
  """Transaction time."""
  i: NotRequired[str]
  """Account alias. Ignore for UM."""
  a: PmAccountUpdateData


Payload = (
  AccountUpdateEvent
  | OrderTradeUpdateEvent
  | OutboundAccountPositionEvent
  | ExecutionReportEvent
  | BalanceUpdateEvent
  | AlgoOrderUpdateEvent
  | ConditionalOrderTradeUpdateEvent
  | OpenOrderLossEvent
  | LiabilityChangeEvent
  | AccountConfigUpdateEvent
  | RiskLevelChangeEvent
  | ListenKeyExpiredEvent
)


class UserData(PrivateStreamEndpoint):
  """One always-on connection carrying every private user-data event for this account on this product, multiplexed with no way to subscribe to a subset -- connecting with a live listenKey already embedded in the URL path is the subscription. Each pushed frame is one of the event variants below, discriminated by its own `e` field."""

  def user_data(
    self,
    listen_key: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[Payload, Any, Any]:
    """One always-on connection carrying every private user-data event for this account on this product, multiplexed with no way to subscribe to a subset -- connecting with a live listenKey already embedded in the URL path is the subscription. Each pushed frame is one of the event variants below, discriminated by its own `e` field.

    Args:
      listen_key: Live listenKey for this connection, minted by `portfolio_margin.account.user_data_stream_start` and kept alive per its own docstring.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/products/derivatives-trading-portfolio-margin/user-data-streams)
    """
    return self.subscribe(
      listen_key,
      validate=validate,
      response_type=Payload,
    )
