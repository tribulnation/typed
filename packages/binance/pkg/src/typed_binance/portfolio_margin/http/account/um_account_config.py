# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class PmUmFuturesAccountConfiguration(TypedDict):
  """Query UM Futures account configuration."""

  feeTier: NotRequired[int]
  """account commission tier."""
  canTrade: NotRequired[bool]
  """if can trade."""
  canDeposit: NotRequired[bool]
  """if can transfer in asset."""
  canWithdraw: NotRequired[bool]
  """if can transfer out asset."""
  updateTime: NotRequired[TimestampMillis]
  """reserved property, please ignore."""
  multiAssetsMargin: NotRequired[bool]
  """Multi Assets Margin."""
  tradeGroupId: NotRequired[int]
  """Trade Group ID."""
  dualSidePosition: NotRequired[bool]
  """\"true": Hedge Mode; "false": One-way Mode."""


class UmAccountConfig(RpcEndpoint):
  """Query UM Futures account configuration."""

  async def um_account_config(
    self,
    *,
    validate: bool | None = None,
  ) -> PmUmFuturesAccountConfiguration:
    """Query UM Futures account configuration.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-derivatives-trading-portfolio-margin/api/rest-api/account#um-futures-account-configuration)
    """
    return await self.request(
      None,
      method='GET',
      path='/papi/v1/um/accountConfig',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      response_type=PmUmFuturesAccountConfiguration,
    )
