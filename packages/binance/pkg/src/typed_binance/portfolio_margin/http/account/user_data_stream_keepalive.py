# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class AccountUserDataStreamKeepaliveResponse(TypedDict):
  """Empty response body."""


class UserDataStreamKeepalive(RpcEndpoint):
  """Keepalive a user data stream to prevent a time out. User data streams will close after 60 minutes. It's recommended to send a ping about every 60 minutes."""

  async def user_data_stream_keepalive(
    self,
    *,
    validate: bool | None = None,
  ) -> AccountUserDataStreamKeepaliveResponse:
    """Keepalive a user data stream to prevent a time out. User data streams will close after 60 minutes. It's recommended to send a ping about every 60 minutes.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-derivatives-trading-portfolio-margin/api/rest-api/user-data-streams#keepalive-user-data-stream)
    """
    return await self.request(
      None,
      method='PUT',
      path='/papi/v1/listenKey',
      meta={'security': 'USER_STREAM', 'signed': False},
      validate=validate,
      response_type=AccountUserDataStreamKeepaliveResponse,
    )
