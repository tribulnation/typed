# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class PmCmAccountTradeList(TypedDict):
  """PmCmAccountTradeList."""

  symbol: NotRequired[str]
  """Trade symbol, if existing."""
  id: NotRequired[int]
  """ID."""
  orderId: NotRequired[int]
  """Normal orderID after trigger if appliable, only have when the strategy is triggered."""
  pair: NotRequired[str]
  """Pair."""
  side: NotRequired[Literal['BUY', 'SELL']]
  """Side."""
  price: NotRequired[str]
  """Price."""
  qty: NotRequired[str]
  """Qty."""
  realizedPnl: NotRequired[str]
  """Realized Pnl."""
  marginAsset: NotRequired[str]
  """Margin Asset."""
  baseQty: NotRequired[str]
  """Base Qty."""
  commission: NotRequired[str]
  """Commission."""
  commissionAsset: NotRequired[str]
  """Commission Asset."""
  time: NotRequired[TimestampMillis]
  """Event time."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """BOTH means that it is the position of One-way Mode."""
  buyer: NotRequired[bool]
  """Buyer."""
  maker: NotRequired[bool]
  """Maker."""


class Request(TypedDict):
  """Request parameters."""

  symbol: NotRequired[str]
  """Trading pair symbol."""
  pair: NotRequired[str]
  """Trading pair."""
  startTime: NotRequired[TimestampMillis]
  """Timestamp in ms to get funding from INCLUSIVE."""
  endTime: NotRequired[TimestampMillis]
  """Timestamp in ms to get funding until INCLUSIVE."""
  fromId: NotRequired[int]
  """Trade ID to fetch from."""
  limit: NotRequired[int]
  """Number of results returned."""


Response = list[PmCmAccountTradeList]


class CmTrades(RpcEndpoint):
  """Get trades for a specific account and CM symbol. Weight: - 20 with `symbol` - 40 with `pair` Security Type: USER_DATA Notes: - Either `symbol` or `pair` must be sent - `symbol` and `pair` cannot be sent together - `pair` and `fromId` cannot be sent together - `OrderId` can only be sent together with symbol - If a `pair` is sent, tickers for all symbols of the `pair` will be returned - The parameter `fromId` cannot be sent with `startTime` or `endTime` - If `startTime` and `endTime` are both not sent, then the last '24 hours' data will be returned. - The time between `startTime` and `endTime` cannot be longer than 24 hours. Args: symbol (Optional[str] = None): pair (Optional[str] = None): start_time (Optional[int] = None): Timestamp in ms to get funding from INCLUSIVE. end_time (Optional[int] = None): Timestamp in ms to get funding until INCLUSIVE. from_id (Optional[int] = None): Trade ID to fetch from. limit (Optional[int] = None): Number of results returned. recv_window (Optional[int] = None): Returns: ApiResponse[CmAccountTradeListResponse] Raises: RequiredError: If a required parameter is missing."""

  async def cm_trades(
    self,
    *,
    symbol: str | None = None,
    pair: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    from_id: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get trades for a specific account and CM symbol. Weight: - 20 with `symbol` - 40 with `pair` Security Type: USER_DATA Notes: - Either `symbol` or `pair` must be sent - `symbol` and `pair` cannot be sent together - `pair` and `fromId` cannot be sent together - `OrderId` can only be sent together with symbol - If a `pair` is sent, tickers for all symbols of the `pair` will be returned - The parameter `fromId` cannot be sent with `startTime` or `endTime` - If `startTime` and `endTime` are both not sent, then the last '24 hours' data will be returned. - The time between `startTime` and `endTime` cannot be longer than 24 hours. Args: symbol (Optional[str] = None): pair (Optional[str] = None): start_time (Optional[int] = None): Timestamp in ms to get funding from INCLUSIVE. end_time (Optional[int] = None): Timestamp in ms to get funding until INCLUSIVE. from_id (Optional[int] = None): Trade ID to fetch from. limit (Optional[int] = None): Number of results returned. recv_window (Optional[int] = None): Returns: ApiResponse[CmAccountTradeListResponse] Raises: RequiredError: If a required parameter is missing.

    Args:
      symbol: Trading pair symbol.
      pair: Trading pair.
      start_time: Timestamp in ms to get funding from INCLUSIVE.
      end_time: Timestamp in ms to get funding until INCLUSIVE.
      from_id: Trade ID to fetch from.
      limit: Number of results returned.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-derivatives-trading-portfolio-margin/api/rest-api/trade#cm-account-trade-list)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    if pair is not None:
      request['pair'] = pair
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if from_id is not None:
      request['fromId'] = from_id
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/papi/v1/cm/userTrades',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
