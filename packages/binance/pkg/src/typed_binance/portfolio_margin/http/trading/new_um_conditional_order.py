# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis, timestamp_millis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class PmNewUmConditionalOrder(TypedDict):
  """Place new UM conditional order."""

  newClientStrategyId: NotRequired[str]
  """New Client Strategy ID."""
  strategyId: NotRequired[int]
  """Strategy ID."""
  strategyStatus: NotRequired[str]
  """Strategy Status."""
  strategyType: NotRequired[
    Literal[
      'STOP', 'STOP_MARKET', 'TAKE_PROFIT', 'TAKE_PROFIT_MARKET', 'TRAILING_STOP_MARKET'
    ]
  ]
  """Strategy Type."""
  origQty: NotRequired[str]
  """Orig Qty."""
  price: NotRequired[str]
  """Price."""
  reduceOnly: NotRequired[bool]
  """Reduce Only."""
  side: NotRequired[Literal['BUY', 'SELL']]
  """Side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """BOTH means that it is the position of One-way Mode."""
  stopPrice: NotRequired[str]
  """please ignore when order type is TRAILING_STOP_MARKET."""
  symbol: NotRequired[str]
  """Trade symbol, if existing."""
  timeInForce: NotRequired[Literal['GTC', 'IOC', 'FOK', 'GTX', 'GTD']]
  """Time In Force."""
  activatePrice: NotRequired[str]
  """activation price, only return with TRAILING_STOP_MARKET order."""
  priceRate: NotRequired[str]
  """callback rate, only return with TRAILING_STOP_MARKET order."""
  bookTime: NotRequired[TimestampMillis]
  """order place time."""
  updateTime: NotRequired[TimestampMillis]
  """last update time."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """Working Type."""
  priceProtect: NotRequired[bool]
  """Price Protect."""
  selfTradePreventionMode: NotRequired[
    Literal['NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER']
  ]
  """self trading preventation mode."""
  goodTillDate: NotRequired[TimestampMillis]
  """order pre-set auot cancel time for TIF GTD order."""
  priceMatch: NotRequired[
    Literal[
      'OPPONENT',
      'OPPONENT_5',
      'OPPONENT_10',
      'OPPONENT_20',
      'QUEUE',
      'QUEUE_5',
      'QUEUE_10',
      'QUEUE_20',
    ]
  ]
  """Price Match."""


class NewUmConditionalOrder(RpcEndpoint):
  """New UM Conditional Order"""

  async def new_um_conditional_order(
    self,
    *,
    symbol: str,
    side: Literal['BUY', 'SELL'],
    strategy_type: Literal[
      'STOP', 'STOP_MARKET', 'TAKE_PROFIT', 'TAKE_PROFIT_MARKET', 'TRAILING_STOP_MARKET'
    ],
    position_side: Literal['BOTH', 'LONG', 'SHORT'] | None = None,
    time_in_force: Literal['GTC', 'IOC', 'FOK', 'GTX', 'GTD'] | None = None,
    quantity: float | None = None,
    reduce_only: Literal['true', 'false'] | None = None,
    price: float | None = None,
    working_type: Literal['MARK_PRICE', 'CONTRACT_PRICE'] | None = None,
    price_protect: Literal['true', 'false'] | None = None,
    new_client_strategy_id: str | None = None,
    stop_price: float | None = None,
    activation_price: float | None = None,
    callback_rate: float | None = None,
    price_match: Literal[
      'OPPONENT',
      'OPPONENT_5',
      'OPPONENT_10',
      'OPPONENT_20',
      'QUEUE',
      'QUEUE_5',
      'QUEUE_10',
      'QUEUE_20',
    ]
    | None = None,
    self_trade_prevention_mode: Literal[
      'NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER'
    ]
    | None = None,
    good_till_date: TimestampMillis | None = None,
    validate: bool | None = None,
  ) -> PmNewUmConditionalOrder:
    """Place new UM conditional order.

    Args:
      symbol: Symbol.
      side: Order side.
      strategy_type: Conditional order strategy type.
      position_side: Default `BOTH` for One-way Mode ; `LONG` or `SHORT` for Hedge Mode. It must be sent in Hedge Mode.
      time_in_force: Time in force.
      quantity: Order quantity, in the base asset.
      reduce_only: \"true" or "false". Cannot be sent in Hedge Mode ; cannot be sent with `closePosition`=`true`.
      price: Order price.
      working_type: stopPrice triggered by: "MARK_PRICE", "CONTRACT_PRICE". Default "CONTRACT_PRICE".
      price_protect: \"true" or "false". Used with `STOP/STOP_MARKET` or `TAKE_PROFIT/TAKE_PROFIT_MARKET` orders.
      new_client_strategy_id: A unique id among open orders. Automatically generated if not sent. Can only be string following the rule: `^[.A-Z:/a-z0-9_-]{1,32}$`.
      stop_price: Used with `STOP/STOP_MARKET` or `TAKE_PROFIT/TAKE_PROFIT_MARKET` orders.
      activation_price: Used with `TRAILING_STOP_MARKET` orders.
      callback_rate: Used with `TRAILING_STOP_MARKET` orders.
      price_match: only avaliable for `LIMIT`/`STOP`/`TAKE_PROFIT` order; can be set to `OPPONENT`/ `OPPONENT_5`/ `OPPONENT_10`/ `OPPONENT_20`: /`QUEUE`/ `QUEUE_5`/ `QUEUE_10`/ `QUEUE_20`; Can't be passed together with `price`.
      self_trade_prevention_mode: `NONE`:No STP / `EXPIRE_TAKER`:expire taker order when STP triggers/ `EXPIRE_MAKER`:expire taker order when STP triggers/ `EXPIRE_BOTH`:expire both orders when STP triggers.
      good_till_date: order cancel time for timeInForce `GTD`, mandatory when `timeInforce` set to `GTD`; order the timestamp only retains second-level precision, ms part will be ignored; The goodTillDate timestamp must be greater than the current time plus 600 seconds and smaller than 253402300799000Mode. It must be sent in Hedge Mode.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/advanced-trading-derivatives-trading-portfolio-margin/api/rest-api/trade#new-um-conditional-order)
    """
    params: dict = {
      'symbol': symbol,
      'side': side,
      'strategyType': strategy_type,
    }
    if position_side is not None:
      params['positionSide'] = position_side
    if time_in_force is not None:
      params['timeInForce'] = time_in_force
    if quantity is not None:
      params['quantity'] = quantity
    if reduce_only is not None:
      params['reduceOnly'] = reduce_only
    if price is not None:
      params['price'] = price
    if working_type is not None:
      params['workingType'] = working_type
    if price_protect is not None:
      params['priceProtect'] = price_protect
    if new_client_strategy_id is not None:
      params['newClientStrategyId'] = new_client_strategy_id
    if stop_price is not None:
      params['stopPrice'] = stop_price
    if activation_price is not None:
      params['activationPrice'] = activation_price
    if callback_rate is not None:
      params['callbackRate'] = callback_rate
    if price_match is not None:
      params['priceMatch'] = price_match
    if self_trade_prevention_mode is not None:
      params['selfTradePreventionMode'] = self_trade_prevention_mode
    if good_till_date is not None:
      params['goodTillDate'] = timestamp_millis.dump(good_till_date)
    _Response = PmNewUmConditionalOrder
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/papi/v1/um/conditional/order',
      params=params,
      validator=_validator,
      validate=validate,
    )
