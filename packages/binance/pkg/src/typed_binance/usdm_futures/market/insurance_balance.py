# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class InsuranceFundAsset0AssetsItem(TypedDict):
  """One asset's balance in the insurance fund."""

  asset: str
  """Asset symbol."""
  marginBalance: str
  """Insurance fund's margin balance in this asset."""
  updateTime: TimestampMillis
  """Snapshot time, in milliseconds since epoch."""


class InsuranceFundAssetItemAssetsItem(TypedDict):
  """One asset's balance in the insurance fund."""

  asset: str
  """Asset symbol."""
  marginBalance: str
  """Insurance fund's margin balance in this asset."""
  updateTime: TimestampMillis
  """Snapshot time, in milliseconds since epoch."""


class InsuranceFundSnapshot0(TypedDict):
  """Insurance fund balance snapshot for one pool."""

  symbols: list[str]
  """Symbols covered by this insurance fund pool."""
  assets: list[InsuranceFundAsset0AssetsItem]
  """Balance of each asset held by the insurance fund."""


class InsuranceFundSnapshotItem(TypedDict):
  """Insurance fund balance snapshot for one pool."""

  symbols: list[str]
  """Symbols covered by this insurance fund pool."""
  assets: list[InsuranceFundAssetItemAssetsItem]
  """Balance of each asset held by the insurance fund."""


class InsuranceBalance(RpcEndpoint):
  """Insurance fund balance snapshot for a symbol's insurance-fund pool, or every pool when omitted."""

  async def insurance_balance(
    self,
    *,
    symbol: str | None = None,
    validate: bool | None = None,
  ) -> InsuranceFundSnapshot0 | list[InsuranceFundSnapshotItem]:
    """Insurance fund balance snapshot for a symbol's insurance-fund pool, or every pool when omitted.

    Args:
      symbol: Trading symbol, e.g. BTCUSDT. Omit to get every insurance fund pool as an array.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/market-data#query-insurance-fund-balance-snapshot)
    """
    params = {}
    if symbol is not None:
      params['symbol'] = symbol
    _Response = InsuranceFundSnapshot0 | list[InsuranceFundSnapshotItem]
    _validator = validator[_Response](_Response)  # type: ignore
    return await self.request(
      'GET',
      '/fapi/v1/insuranceBalance',
      params=params,
      validator=_validator,
      validate=validate,
    )
