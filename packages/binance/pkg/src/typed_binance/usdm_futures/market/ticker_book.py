# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class BookTicker0(TypedDict):
  """Best bid/ask price and quantity for one symbol."""

  symbol: str
  """Trading symbol."""
  bidPrice: str
  """Best bid price."""
  bidQty: str
  """Quantity available at the best bid."""
  askPrice: str
  """Best ask price."""
  askQty: str
  """Quantity available at the best ask."""
  time: TimestampMillis
  """Time this quote was recorded, in milliseconds since epoch."""


class BookTickerItem(TypedDict):
  """Best bid/ask price and quantity for one symbol."""

  symbol: str
  """Trading symbol."""
  bidPrice: str
  """Best bid price."""
  bidQty: str
  """Quantity available at the best bid."""
  askPrice: str
  """Best ask price."""
  askQty: str
  """Quantity available at the best ask."""
  time: TimestampMillis
  """Time this quote was recorded, in milliseconds since epoch."""


class TickerBook(RpcEndpoint):
  """Best bid/ask price and quantity for a symbol, or every symbol when omitted."""

  async def ticker_book(
    self,
    *,
    symbol: str | None = None,
    validate: bool | None = None,
  ) -> BookTicker0 | list[BookTickerItem]:
    """Best bid/ask price and quantity for a symbol, or every symbol when omitted.

    Args:
      symbol: Trading symbol, e.g. BTCUSDT. Omit to get every symbol as an array.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/market-data#symbol-order-book-ticker)
    """
    params = {}
    if symbol is not None:
      params['symbol'] = symbol
    _Response = BookTicker0 | list[BookTickerItem]
    _validator = validator[_Response](_Response)  # type: ignore
    return await self.request(
      'GET',
      '/fapi/v1/ticker/bookTicker',
      params=params,
      validator=_validator,
      validate=validate,
    )
