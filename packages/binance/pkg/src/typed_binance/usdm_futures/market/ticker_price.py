# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class SymbolPriceTicker0(TypedDict):
  """Latest price for one symbol."""

  symbol: str
  """Trading symbol."""
  price: str
  """Latest price."""
  time: TimestampMillis
  """Time this price was recorded, in milliseconds since epoch."""


class SymbolPriceTickerItem(TypedDict):
  """Latest price for one symbol."""

  symbol: str
  """Trading symbol."""
  price: str
  """Latest price."""
  time: TimestampMillis
  """Time this price was recorded, in milliseconds since epoch."""


class TickerPrice(RpcEndpoint):
  """Latest price for a symbol, or every symbol when omitted."""

  async def ticker_price(
    self,
    *,
    symbol: str | None = None,
    validate: bool | None = None,
  ) -> SymbolPriceTicker0 | list[SymbolPriceTickerItem]:
    """Latest price for a symbol, or every symbol when omitted.

    Args:
      symbol: Trading symbol, e.g. BTCUSDT. Omit to get every symbol as an array.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/market-data#symbol-price-ticker)
    """
    params = {}
    if symbol is not None:
      params['symbol'] = symbol
    _Response = SymbolPriceTicker0 | list[SymbolPriceTickerItem]
    _validator = validator[_Response](_Response)  # type: ignore
    return await self.request(
      'GET',
      '/fapi/v1/ticker/price',
      params=params,
      validator=_validator,
      validate=validate,
    )
