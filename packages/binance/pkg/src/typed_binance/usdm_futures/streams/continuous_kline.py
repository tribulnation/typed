# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class ContinuousKline(TypedDict):
  """The kline/candlestick itself."""

  t: TimestampMillis
  """Kline start time."""
  T: TimestampMillis
  """Kline close time."""
  i: Literal[
    '1s',
    '1m',
    '3m',
    '5m',
    '15m',
    '30m',
    '1h',
    '2h',
    '4h',
    '6h',
    '8h',
    '12h',
    '1d',
    '3d',
    '1w',
    '1M',
  ]
  """Interval."""
  f: int
  """First update ID."""
  L: int
  """Last update ID."""
  o: str
  """Open price."""
  c: str
  """Close price."""
  h: str
  """High price."""
  l: str
  """Low price."""
  v: str
  """Volume."""
  n: int
  """Number of trades."""
  x: bool
  """Whether this kline is closed."""
  q: str
  """Quote asset volume."""
  V: str
  """Taker buy volume."""
  Q: str
  """Taker buy quote asset volume."""
  B: str
  """Ignore."""


class ContinuousKlineEvent(TypedDict):
  """One continuous-contract kline/candlestick update."""

  e: str
  """Event type."""
  E: TimestampMillis
  """Event time."""
  ps: str
  """Pair."""
  ct: str
  """Contract type."""
  k: ContinuousKline


class ContinuousKlineEndpoint(StreamEndpoint):
  """Pushes updates to the current kline/candlestick for a continuous contract (a pair + contract type, rather than a single expiring symbol), every 250ms."""

  def continuous_kline(
    self,
    pair: str,
    *,
    contract_type: Literal[
      'perpetual', 'current_quarter', 'next_quarter', 'tradifi_perpetual'
    ],
    interval: Literal[
      '1s',
      '1m',
      '3m',
      '5m',
      '15m',
      '30m',
      '1h',
      '2h',
      '4h',
      '6h',
      '8h',
      '12h',
      '1d',
      '3d',
      '1w',
      '1M',
    ],
    validate: bool | None = None,
  ) -> StreamManager[ContinuousKlineEvent, Any, Any]:
    """Pushes updates to the current kline/candlestick for a continuous contract (a pair + contract type, rather than a single expiring symbol), every 250ms.

    Args:
      pair: Underlying trading pair, e.g. `BTCUSDT`.
      contract_type: Contract type.
      interval: Kline interval.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/ws-streams/market#continuous-contract-kline-candlestick-streams)
    """
    return self.subscribe(
      f'{pair}_{contract_type}@continuousKline_{interval}',
      validate=validate,
      response_type=ContinuousKlineEvent,
    )
