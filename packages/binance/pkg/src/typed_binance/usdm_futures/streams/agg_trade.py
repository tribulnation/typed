# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class AggTradeEvent(TypedDict):
  """One aggregated taker trade."""

  e: str
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Symbol."""
  a: int
  """Aggregate trade ID."""
  p: str
  """Price."""
  q: str
  """Quantity, including any trades involving RPI orders."""
  nq: NotRequired[str]
  """Quantity excluding trades involving RPI orders."""
  f: int
  """First trade ID aggregated into this trade."""
  l: int
  """Last trade ID aggregated into this trade."""
  T: TimestampMillis
  """Trade time."""
  m: bool
  """Whether the buyer was the market maker."""
  st: NotRequired[int]
  """(After UM/CM stream-host migration) Symbol type: 1 = UM, 2 = CM."""


class AggTrade(StreamEndpoint):
  """Pushes market trade information aggregated for fills with the same price and taking side, every 100ms. Only market trades are aggregated -- insurance fund and ADL trades are not."""

  def agg_trade(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[AggTradeEvent, Any, Any]:
    """Pushes market trade information aggregated for fills with the same price and taking side, every 100ms. Only market trades are aggregated -- insurance fund and ADL trades are not.

    Args:
      symbol: Trading pair symbol, e.g. `btcusdt`. The venue requires stream names to use lowercase symbols on the wire, and this value is used as-is with no casing transform.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/ws-streams/market#aggregate-trade-streams)
    """
    return self.subscribe(
      f'{symbol}@aggTrade',
      validate=validate,
      response_type=AggTradeEvent,
    )
