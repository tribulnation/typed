# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class NotionalBracket(TypedDict):
  """One notional bracket."""

  bs: int
  """Notional bracket number."""
  bnf: int
  """Floor notional of this bracket."""
  bnc: int
  """Cap notional of this bracket."""
  mmr: float
  """Maintenance margin ratio for this bracket."""
  cf: int
  """Auxiliary number for quick calculation."""
  mi: int
  """Minimum leverage for this bracket."""
  ma: int
  """Maximum leverage for this bracket."""


class ContractInfoEvent(TypedDict):
  """One contract-info update."""

  e: str
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Symbol."""
  ct: str
  """Contract type."""
  dt: TimestampMillis
  """Delivery date time."""
  ot: TimestampMillis
  """Onboard date time."""
  cs: str
  """Contract status."""
  bks: NotRequired[list[NotionalBracket]]
  """Notional bracket updates. Only present when a bracket changed."""
  st: NotRequired[int]
  """(After UM/CM stream-host migration) Symbol type: 1 = UM, 2 = CM."""


class ContractInfo(StreamEndpoint):
  """Pushes contract info whenever it changes -- listing, settlement, or a notional bracket update. `bks` is only present on a bracket update."""

  def contract_info(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[ContractInfoEvent, Any, Any]:
    """Pushes contract info whenever it changes -- listing, settlement, or a notional bracket update. `bks` is only present on a bracket update.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/ws-streams/market#contract-info-stream)
    """
    return self.subscribe(
      '!contractInfo',
      None,
      validate=validate,
      response_type=ContractInfoEvent,
    )
