# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class AssetIndexEvent(TypedDict):
  """Asset index price for one asset."""

  e: str
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Asset index symbol."""
  i: str
  """Index price."""
  b: str
  """Bid buffer."""
  a: str
  """Ask buffer."""
  B: str
  """Bid rate."""
  A: str
  """Ask rate."""
  q: str
  """Auto exchange bid buffer."""
  g: str
  """Auto exchange ask buffer."""
  Q: str
  """Auto exchange bid rate."""
  G: str
  """Auto exchange ask rate."""


Payload = list[AssetIndexEvent]


class AssetIndexArr(StreamEndpoint):
  """Pushes asset index prices for every asset, as an array, every second."""

  def asset_index_arr(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[Payload, Any, Any]:
    """Pushes asset index prices for every asset, as an array, every second.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/ws-streams/market#asset-index)
    """
    return self.subscribe(
      '!assetIndex@arr',
      None,
      validate=validate,
      response_type=Payload,
    )
