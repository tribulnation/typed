# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class MarkPriceEvent(TypedDict):
  """Mark price and funding rate for one symbol."""

  e: str
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Symbol."""
  p: str
  """Mark price."""
  i: str
  """Index price."""
  P: str
  """Estimated settle price, only meaningful in the last hour before settlement."""
  r: str
  """Funding rate."""
  ap: str
  """Mark price moving average."""
  T: TimestampMillis
  """Next funding time."""
  st: NotRequired[int]
  """(After UM/CM stream-host migration) Symbol type: 1 = UM, 2 = CM."""


Payload = list[MarkPriceEvent]


class MarkPriceArr(StreamEndpoint):
  """Pushes mark price and funding rate for every symbol, as an array, every 3 seconds."""

  def mark_price_arr(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[Payload, Any, Any]:
    """Pushes mark price and funding rate for every symbol, as an array, every 3 seconds.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/ws-streams/market#mark-price-stream-for-all-market)
    """
    return self.subscribe(
      '!markPrice@arr',
      None,
      validate=validate,
      response_type=Payload,
    )
