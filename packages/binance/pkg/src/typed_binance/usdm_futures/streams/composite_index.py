# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class CompositeIndexComponent(TypedDict):
  """One constituent asset of a composite index."""

  b: str
  """Base asset."""
  q: str
  """Quote asset."""
  w: str
  """Weight in quantity."""
  W: str
  """Weight in percentage."""
  i: str
  """Index price."""


class CompositeIndexEvent(TypedDict):
  """Composite index information for one index symbol."""

  e: str
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Symbol."""
  p: str
  """Price."""
  C: str
  """Base asset category."""
  c: list[CompositeIndexComponent]
  """Composition -- each constituent asset's weight in this index."""


class CompositeIndex(StreamEndpoint):
  """Pushes composite index information for an index symbol, every second."""

  def composite_index(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[CompositeIndexEvent, Any, Any]:
    """Pushes composite index information for an index symbol, every second.

    Args:
      symbol: Trading pair symbol, e.g. `btcusdt`. The venue requires stream names to use lowercase symbols on the wire, and this value is used as-is with no casing transform.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/ws-streams/market#composite-index-symbol-information-streams)
    """
    return self.subscribe(
      f'{symbol}@compositeIndex',
      validate=validate,
      response_type=CompositeIndexEvent,
    )
