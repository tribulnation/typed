# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any
from typed_binance.core.endpoint.stream import StreamEndpoint
from typed_binance.schemas import UsdMFuturesTickerEvent


Payload = list[UsdMFuturesTickerEvent]


class TickerArr(StreamEndpoint):
  """Pushes 24hr rolling-window ticker statistics for every symbol that changed, as an array, every 1000ms."""

  def ticker_arr(
    self, *, validate: bool | None = None
  ) -> StreamManager[Payload, Any, Any]:
    """Pushes 24hr rolling-window ticker statistics for every symbol that changed, as an array, every 1000ms.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/ws-streams/market#all-market-tickers-streams)
    """
    return self.subscribe(
      '!ticker@arr',
      None,
      validate=validate,
      response_type=Payload,
    )
