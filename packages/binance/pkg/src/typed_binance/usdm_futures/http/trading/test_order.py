# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class LimitOrder(TypedDict):
  """A limit order: filled at price or better. Requires timeInForce, quantity and price."""

  symbol: str
  """Trading symbol."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['LIMIT']
  """Order type."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. Defaults to BOTH for One-way Mode; must be sent as LONG or SHORT in Hedge Mode."""
  timeInForce: Literal['GTC', 'IOC', 'FOK', 'GTX', 'GTD', 'RPI']
  """Time in force."""
  reduceOnly: NotRequired[bool]
  """Whether the order only reduces an existing position. Cannot be sent in Hedge Mode, and cannot be combined with closePosition."""
  quantity: str
  """Order quantity."""
  price: str
  """Order price."""
  newClientOrderId: NotRequired[str]
  """Caller-supplied id for the order. Automatically generated if not sent. Must match ^[.A-Z:/a-z0-9_-]{1,36}$."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT']]
  """Response shape to return. If RESULT: a MARKET order returns its final FILLED result directly; a LIMIT order with a special timeInForce returns its final FILLED or EXPIRED status directly."""
  priceMatch: NotRequired[
    Literal[
      'OPPONENT',
      'OPPONENT_5',
      'OPPONENT_10',
      'OPPONENT_20',
      'QUEUE',
      'QUEUE_5',
      'QUEUE_10',
      'QUEUE_20',
    ]
  ]
  """Price match mode, auto-aligning the order price to the order book. Only available for LIMIT/STOP/TAKE_PROFIT orders; cannot be sent together with price."""
  selfTradePreventionMode: NotRequired[
    Literal['NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER']
  ]
  """Self-trade prevention mode. Only effective when timeInForce is IOC, GTC or GTD."""
  goodTillDate: NotRequired[TimestampMillis]
  """Order auto-cancel time for timeInForce GTD, as a Unix epoch in milliseconds. Mandatory when timeInForce is GTD; retains only second-level precision; must be greater than the current time plus 600 seconds and smaller than 253402300799000."""


class MarketOrder(TypedDict):
  """A market order: filled immediately at the best available price. Requires quantity."""

  symbol: str
  """Trading symbol."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['MARKET']
  """Order type."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. Defaults to BOTH for One-way Mode; must be sent as LONG or SHORT in Hedge Mode."""
  reduceOnly: NotRequired[bool]
  """Whether the order only reduces an existing position. Cannot be sent in Hedge Mode, and cannot be combined with closePosition."""
  quantity: str
  """Order quantity."""
  newClientOrderId: NotRequired[str]
  """Caller-supplied id for the order. Automatically generated if not sent. Must match ^[.A-Z:/a-z0-9_-]{1,36}$."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT']]
  """Response shape to return. If RESULT: a MARKET order returns its final FILLED result directly; a LIMIT order with a special timeInForce returns its final FILLED or EXPIRED status directly."""
  selfTradePreventionMode: NotRequired[
    Literal['NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER']
  ]
  """Self-trade prevention mode. Only effective when timeInForce is IOC, GTC or GTD."""


class StopMarketOrTakeProfitMarketOrder(TypedDict):
  """A STOP_MARKET or TAKE_PROFIT_MARKET order: a market order that only becomes live once the mark/contract price reaches stopPrice. Requires stopPrice. quantity is required unless closePosition is true (Close-All), which is only usable with these two types and cannot be combined with quantity or reduceOnly -- this variant leaves both quantity and closePosition optional to express the either/or, rather than splitting further into quantity-only/closePosition-only sub-variants."""

  symbol: str
  """Trading symbol."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['STOP_MARKET', 'TAKE_PROFIT_MARKET']
  """Order type."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. Defaults to BOTH for One-way Mode; must be sent as LONG or SHORT in Hedge Mode."""
  reduceOnly: NotRequired[bool]
  """Whether the order only reduces an existing position. Cannot be sent in Hedge Mode, and cannot be combined with closePosition."""
  quantity: NotRequired[str]
  """Order quantity. Cannot be sent together with closePosition=true."""
  stopPrice: str
  """Trigger price: the order activates once the mark/contract price reaches this level."""
  closePosition: NotRequired[bool]
  """Close-All: close the entire position when triggered. Cannot be combined with quantity or reduceOnly; in Hedge Mode, cannot be used with BUY orders in a LONG position or SELL orders in a SHORT position."""
  newClientOrderId: NotRequired[str]
  """Caller-supplied id for the order. Automatically generated if not sent. Must match ^[.A-Z:/a-z0-9_-]{1,36}$."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """Price type used to evaluate the conditional order's trigger. Defaults to CONTRACT_PRICE."""
  priceProtect: NotRequired[bool]
  """Whether price protection is active for the conditional order's trigger: when the trigger price is reached, the gap between mark price and contract price must also be within the symbol's triggerProtect threshold."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT']]
  """Response shape to return. If RESULT: a MARKET order returns its final FILLED result directly; a LIMIT order with a special timeInForce returns its final FILLED or EXPIRED status directly."""
  selfTradePreventionMode: NotRequired[
    Literal['NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER']
  ]
  """Self-trade prevention mode. Only effective when timeInForce is IOC, GTC or GTD."""


class StopOrTakeProfitOrder(TypedDict):
  """A STOP or TAKE_PROFIT order: a limit order that only becomes live once the mark/contract price reaches stopPrice. Requires quantity, price and stopPrice. timeInForce may be sent (defaults to GTC)."""

  symbol: str
  """Trading symbol."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['STOP', 'TAKE_PROFIT']
  """Order type."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. Defaults to BOTH for One-way Mode; must be sent as LONG or SHORT in Hedge Mode."""
  timeInForce: NotRequired[Literal['GTC', 'IOC', 'FOK', 'GTX', 'GTD', 'RPI']]
  """Time in force. May be sent for STOP/TAKE_PROFIT orders; defaults to GTC."""
  reduceOnly: NotRequired[bool]
  """Whether the order only reduces an existing position. Cannot be sent in Hedge Mode, and cannot be combined with closePosition."""
  quantity: str
  """Order quantity. Cannot be sent together with closePosition=true."""
  price: str
  """Order price."""
  stopPrice: str
  """Trigger price: the order activates once the mark/contract price reaches this level."""
  newClientOrderId: NotRequired[str]
  """Caller-supplied id for the order. Automatically generated if not sent. Must match ^[.A-Z:/a-z0-9_-]{1,36}$."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """Price type used to evaluate the conditional order's trigger. Defaults to CONTRACT_PRICE."""
  priceProtect: NotRequired[bool]
  """Whether price protection is active for the conditional order's trigger: when the trigger price is reached, the gap between mark price and contract price must also be within the symbol's triggerProtect threshold."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT']]
  """Response shape to return. If RESULT: a MARKET order returns its final FILLED result directly; a LIMIT order with a special timeInForce returns its final FILLED or EXPIRED status directly."""
  priceMatch: NotRequired[
    Literal[
      'OPPONENT',
      'OPPONENT_5',
      'OPPONENT_10',
      'OPPONENT_20',
      'QUEUE',
      'QUEUE_5',
      'QUEUE_10',
      'QUEUE_20',
    ]
  ]
  """Price match mode, auto-aligning the order price to the order book. Only available for LIMIT/STOP/TAKE_PROFIT orders; cannot be sent together with price."""
  selfTradePreventionMode: NotRequired[
    Literal['NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER']
  ]
  """Self-trade prevention mode. Only effective when timeInForce is IOC, GTC or GTD."""
  goodTillDate: NotRequired[TimestampMillis]
  """Order auto-cancel time for timeInForce GTD, as a Unix epoch in milliseconds. Mandatory when timeInForce is GTD; retains only second-level precision; must be greater than the current time plus 600 seconds and smaller than 253402300799000."""


class TestFuturesOrderResult(TypedDict):
  """The simulated order result."""

  clientOrderId: NotRequired[str]
  """Client order id, either caller-supplied or venue-generated."""
  cumQty: NotRequired[str]
  """Cumulative filled quantity."""
  cumQuote: NotRequired[str]
  """Cumulative quote asset transacted quantity."""
  executedQty: NotRequired[str]
  """Cumulative filled quantity."""
  orderId: NotRequired[int]
  """Order id."""
  avgPrice: NotRequired[str]
  """Average fill price."""
  origQty: NotRequired[str]
  """Original order quantity."""
  price: NotRequired[str]
  """Order price."""
  reduceOnly: NotRequired[bool]
  """Whether the order only reduces an existing position."""
  side: NotRequired[Literal['BUY', 'SELL']]
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side."""
  status: NotRequired[
    Literal[
      'NEW',
      'PARTIALLY_FILLED',
      'FILLED',
      'CANCELED',
      'REJECTED',
      'EXPIRED',
      'EXPIRED_IN_MATCH',
    ]
  ]
  """Order status."""
  stopPrice: NotRequired[str]
  """Trigger price for a conditional order. Ignored for LIMIT and MARKET orders."""
  closePosition: NotRequired[bool]
  """Whether the order closes the entire position (Close-All)."""
  symbol: NotRequired[str]
  """Trading symbol."""
  timeInForce: NotRequired[Literal['GTC', 'IOC', 'FOK', 'GTX', 'GTD', 'RPI']]
  """Time in force."""
  type: NotRequired[
    Literal[
      'LIMIT',
      'MARKET',
      'STOP',
      'STOP_MARKET',
      'TAKE_PROFIT',
      'TAKE_PROFIT_MARKET',
      'TRAILING_STOP_MARKET',
    ]
  ]
  """Order type."""
  origType: NotRequired[
    Literal[
      'LIMIT',
      'MARKET',
      'STOP',
      'STOP_MARKET',
      'TAKE_PROFIT',
      'TAKE_PROFIT_MARKET',
      'TRAILING_STOP_MARKET',
    ]
  ]
  """Order type at creation, unaffected by later amendments."""
  activatePrice: NotRequired[str]
  """Activation price. Only returned for TRAILING_STOP_MARKET orders."""
  priceRate: NotRequired[str]
  """Callback rate. Only returned for TRAILING_STOP_MARKET orders."""
  updateTime: NotRequired[TimestampMillis]
  """Time the order was last updated."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """Price type used to trigger conditional orders."""
  priceProtect: NotRequired[bool]
  """Whether price protection is active for a conditional order's trigger."""
  priceMatch: NotRequired[
    Literal[
      'OPPONENT',
      'OPPONENT_5',
      'OPPONENT_10',
      'OPPONENT_20',
      'QUEUE',
      'QUEUE_5',
      'QUEUE_10',
      'QUEUE_20',
    ]
  ]
  """Price match mode used to auto-align the order price to the order book."""
  selfTradePreventionMode: NotRequired[
    Literal['NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER']
  ]
  """Self-trade prevention mode."""
  goodTillDate: NotRequired[TimestampMillis]
  """Auto-cancel time for a GTD order, as a Unix epoch in milliseconds."""


class TrailingStopMarketOrder(TypedDict):
  """A TRAILING_STOP_MARKET order: a market order that trails the best price reached since placement and triggers once the price pulls back by callbackRate. Requires callbackRate. Binance's own mandatory-parameters table does not list quantity as required for this type (unlike STOP/TAKE_PROFIT), so it is kept optional here rather than guessed as required."""

  symbol: str
  """Trading symbol."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['TRAILING_STOP_MARKET']
  """Order type."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. Defaults to BOTH for One-way Mode; must be sent as LONG or SHORT in Hedge Mode."""
  reduceOnly: NotRequired[bool]
  """Whether the order only reduces an existing position. Cannot be sent in Hedge Mode, and cannot be combined with closePosition."""
  quantity: NotRequired[str]
  """Order quantity."""
  activationPrice: NotRequired[str]
  """Activation price: the trailing stop starts tracking once the market reaches this level. Defaults to the latest price if not sent (supports different workingType values)."""
  callbackRate: str
  """Callback (trailing) rate, as a percent between 0.1 and 5."""
  newClientOrderId: NotRequired[str]
  """Caller-supplied id for the order. Automatically generated if not sent. Must match ^[.A-Z:/a-z0-9_-]{1,36}$."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """Price type used to evaluate the conditional order's trigger. Defaults to CONTRACT_PRICE."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT']]
  """Response shape to return. If RESULT: a MARKET order returns its final FILLED result directly; a LIMIT order with a special timeInForce returns its final FILLED or EXPIRED status directly."""
  selfTradePreventionMode: NotRequired[
    Literal['NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER']
  ]
  """Self-trade prevention mode. Only effective when timeInForce is IOC, GTC or GTD."""


Request = (
  LimitOrder
  | MarketOrder
  | StopOrTakeProfitOrder
  | StopMarketOrTakeProfitMarketOrder
  | TrailingStopMarketOrder
)


class TestOrder(RpcEndpoint):
  """Testing order request; this order will not be submitted to the matching engine. Required fields differ by order type -- see the requestBody variants, identical to usdm_futures.trading.new_order's."""

  async def test_order(
    self,
    trading_test_order_request: Request,
    *,
    validate: bool | None = None,
  ) -> TestFuturesOrderResult:
    """Testing order request; this order will not be submitted to the matching engine. Required fields differ by order type -- see the requestBody variants, identical to usdm_futures.trading.new_order's.

    Args:
      trading_test_order_request:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/trade#test-order)
    """
    return await self.request(
      trading_test_order_request,
      method='POST',
      path='/fapi/v1/order/test',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=TestFuturesOrderResult,
    )
