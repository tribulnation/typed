# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Request parameters."""

  symbol: str
  """Symbol to query trades for."""
  orderId: NotRequired[int]
  """Order ID to filter by. Must be used together with `symbol`."""
  startTime: NotRequired[TimestampMillis]
  """Query window start, as milliseconds since epoch."""
  endTime: NotRequired[TimestampMillis]
  """Query window end, as milliseconds since epoch."""
  fromId: NotRequired[int]
  """Trade ID to fetch from. Defaults to the most recent trades. Cannot be sent together with `startTime` or `endTime`."""
  limit: NotRequired[int]
  """Number of trades to return."""


class UsdMFuturesAccountTrade(TypedDict):
  """A single fill on this account."""

  buyer: NotRequired[bool]
  """Whether this account was the buyer side of the trade."""
  commission: NotRequired[str]
  """Commission charged for this trade, as a decimal string."""
  commissionAsset: NotRequired[str]
  """Asset the commission was charged in."""
  id: NotRequired[int]
  """Trade ID."""
  maker: NotRequired[bool]
  """Whether this account was the maker side of the trade."""
  orderId: NotRequired[int]
  """ID of the order this trade filled."""
  price: NotRequired[str]
  """Fill price, as a decimal string."""
  qty: NotRequired[str]
  """Fill quantity in the base asset, as a decimal string."""
  quoteQty: NotRequired[str]
  """Fill quantity in the quote asset, as a decimal string."""
  baseQty: NotRequired[str]
  """Fill quantity in the base asset for COIN-margined-style accounting, as a decimal string."""
  marginAsset: NotRequired[str]
  """Margin asset for this trade."""
  realizedPnl: NotRequired[str]
  """Realized profit or loss from this trade, as a decimal string."""
  side: NotRequired[Literal['BUY', 'SELL']]
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side this trade applies to."""
  symbol: NotRequired[str]
  """Symbol this trade was made on."""
  pair: NotRequired[str]
  """Underlying pair this trade was made on."""
  time: NotRequired[TimestampMillis]
  """Trade time, as milliseconds since epoch."""


Response = list[UsdMFuturesAccountTrade]


class UserTrades(RpcEndpoint):
  """Get trades for a specific account and symbol. If `startTime` and `endTime` are both omitted, the last 7 days' data is returned; the gap between them cannot exceed 7 days. `fromId` cannot be sent together with `startTime` or `endTime`. Only trades from the past 6 months can be queried."""

  async def user_trades(
    self,
    symbol: str,
    *,
    order_id: int | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    from_id: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get trades for a specific account and symbol. If `startTime` and `endTime` are both omitted, the last 7 days' data is returned; the gap between them cannot exceed 7 days. `fromId` cannot be sent together with `startTime` or `endTime`. Only trades from the past 6 months can be queried.

    Args:
      symbol: Symbol to query trades for.
      order_id: Order ID to filter by. Must be used together with `symbol`.
      start_time: Query window start, as milliseconds since epoch.
      end_time: Query window end, as milliseconds since epoch.
      from_id: Trade ID to fetch from. Defaults to the most recent trades. Cannot be sent together with `startTime` or `endTime`.
      limit: Number of trades to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/trade#account-trade-list)
    """
    request: Request = Request(symbol=symbol)
    if order_id is not None:
      request['orderId'] = order_id
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if from_id is not None:
      request['fromId'] = from_id
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/fapi/v1/userTrades',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
