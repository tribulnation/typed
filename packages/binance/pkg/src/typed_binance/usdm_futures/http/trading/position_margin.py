# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class PositionMarginChangeResult(TypedDict):
  """Result of modifying an isolated position's margin."""

  amount: NotRequired[float]
  """Margin amount changed."""
  code: int
  """Result code."""
  msg: NotRequired[str]
  """Result message."""
  type: Literal[1, 2]
  """1: Add position margin, 2: Reduce position margin."""


class Request(TypedDict):
  """Request parameters."""

  symbol: str
  """Trading symbol, e.g. BTCUSDT."""
  amount: str
  """Margin asset amount to add or reduce."""
  type: Literal[1, 2]
  """1: Add position margin, 2: Reduce position margin."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Default BOTH for One-way Mode; LONG or SHORT for Hedge Mode. Must be sent in Hedge Mode."""


class PositionMargin(RpcEndpoint):
  """Modify isolated position margin for a symbol. Only valid for symbols using ISOLATED margin type."""

  async def position_margin(
    self,
    *,
    symbol: str,
    amount: str,
    type: Literal[1, 2],
    position_side: Literal['BOTH', 'LONG', 'SHORT'] | None = None,
    validate: bool | None = None,
  ) -> PositionMarginChangeResult:
    """Modify isolated position margin for a symbol. Only valid for symbols using ISOLATED margin type.

    Args:
      symbol: Trading symbol, e.g. BTCUSDT.
      amount: Margin asset amount to add or reduce.
      type: 1: Add position margin, 2: Reduce position margin.
      position_side: Default BOTH for One-way Mode; LONG or SHORT for Hedge Mode. Must be sent in Hedge Mode.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/trade#modify-isolated-position-margin)
    """
    request: Request = Request(symbol=symbol, amount=amount, type=type)
    if position_side is not None:
      request['positionSide'] = position_side
    return await self.request(
      request,
      method='POST',
      path='/fapi/v1/positionMargin',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PositionMarginChangeResult,
    )
