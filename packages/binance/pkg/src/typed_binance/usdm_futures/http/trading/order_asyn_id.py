# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Request parameters."""

  downloadId: str
  """Download id, obtained from `usdm_futures.trading.order_asyn`."""


class UsdMFuturesOrderHistoryDownloadLink(TypedDict):
  """Status and download link for a futures order history export job."""

  downloadId: NotRequired[str]
  """Download id this status/link corresponds to."""
  status: NotRequired[Literal['completed', 'processing']]
  """Export job status."""
  url: NotRequired[str]
  """Download link for the export, once `status` is `completed`."""
  notified: NotRequired[bool]
  """Reserved field; not meaningful to callers."""
  expirationTimestamp: NotRequired[TimestampMillis]
  """Time the download link expires, as milliseconds since epoch."""
  isExpired: NotRequired[str]
  """Whether the download link has already expired."""


class OrderAsynId(RpcEndpoint):
  """Get the futures order history download link for a download id previously obtained from `usdm_futures.trading.order_asyn`. The link expires 7 days after it becomes available."""

  async def order_asyn_id(
    self,
    download_id: str,
    *,
    validate: bool | None = None,
  ) -> UsdMFuturesOrderHistoryDownloadLink:
    """Get the futures order history download link for a download id previously obtained from `usdm_futures.trading.order_asyn`. The link expires 7 days after it becomes available.

    Args:
      download_id: Download id, obtained from `usdm_futures.trading.order_asyn`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/account#get-futures-order-history-download-link-by-id)
    """
    return await self.request(
      Request(downloadId=download_id),
      method='GET',
      path='/fapi/v1/order/asyn/id',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=UsdMFuturesOrderHistoryDownloadLink,
    )
