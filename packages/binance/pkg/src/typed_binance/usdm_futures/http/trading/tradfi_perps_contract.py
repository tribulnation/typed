# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class TradfiPerpsContractResult(TypedDict):
  """Result of signing the TradFi-Perps agreement contract."""

  code: int
  """Result code."""
  msg: NotRequired[str]
  """Result message."""


class TradfiPerpsContract(RpcEndpoint):
  """Sign the TradFi-Perps agreement contract, a prerequisite before trading TradFi perpetual contracts (e.g. TRADIFI_PERPETUAL contract type) on USD-M Futures."""

  async def tradfi_perps_contract(
    self,
    *,
    validate: bool | None = None,
  ) -> TradfiPerpsContractResult:
    """Sign the TradFi-Perps agreement contract, a prerequisite before trading TradFi perpetual contracts (e.g. TRADIFI_PERPETUAL contract type) on USD-M Futures.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/trade#futures-tradfi-perps-contract)
    """
    return await self.request(
      None,
      method='POST',
      path='/fapi/v1/stock/contract',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      response_type=TradfiPerpsContractResult,
    )
