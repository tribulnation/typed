# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Request parameters."""

  symbol: NotRequired[str]
  """Trading symbol, e.g. BTCUSDT. Omit to get every symbol as an array."""


class Ticker24hr0(TypedDict):
  """24-hour rolling window price change statistics for one symbol."""

  symbol: str
  """Trading symbol."""
  priceChange: Decimal
  """Absolute price change over the window."""
  priceChangePercent: Decimal
  """Percentage price change over the window."""
  weightedAvgPrice: Decimal
  """Weighted average price over the window."""
  lastPrice: Decimal
  """Most recent trade price."""
  lastQty: Decimal
  """Quantity of the most recent trade."""
  openPrice: Decimal
  """Price at the start of the window."""
  highPrice: Decimal
  """Highest price during the window."""
  lowPrice: Decimal
  """Lowest price during the window."""
  volume: Decimal
  """Total base asset volume traded during the window."""
  quoteVolume: Decimal
  """Total quote asset volume traded during the window."""
  openTime: TimestampMillis
  """Window start time, in milliseconds since epoch."""
  closeTime: TimestampMillis
  """Window end time, in milliseconds since epoch."""
  firstId: int
  """First trade id in the window."""
  lastId: int
  """Last trade id in the window."""
  count: int
  """Number of trades during the window."""


class Ticker24hrItem(TypedDict):
  """24-hour rolling window price change statistics for one symbol."""

  symbol: str
  """Trading symbol."""
  priceChange: Decimal
  """Absolute price change over the window."""
  priceChangePercent: Decimal
  """Percentage price change over the window."""
  weightedAvgPrice: Decimal
  """Weighted average price over the window."""
  lastPrice: Decimal
  """Most recent trade price."""
  lastQty: Decimal
  """Quantity of the most recent trade."""
  openPrice: Decimal
  """Price at the start of the window."""
  highPrice: Decimal
  """Highest price during the window."""
  lowPrice: Decimal
  """Lowest price during the window."""
  volume: Decimal
  """Total base asset volume traded during the window."""
  quoteVolume: Decimal
  """Total quote asset volume traded during the window."""
  openTime: TimestampMillis
  """Window start time, in milliseconds since epoch."""
  closeTime: TimestampMillis
  """Window end time, in milliseconds since epoch."""
  firstId: int
  """First trade id in the window."""
  lastId: int
  """Last trade id in the window."""
  count: int
  """Number of trades during the window."""


Response = Ticker24hr0 | list[Ticker24hrItem]


class Ticker24hr(RpcEndpoint):
  """24-hour rolling window price change statistics for a symbol, or every symbol when omitted."""

  async def ticker_24hr(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """24-hour rolling window price change statistics for a symbol, or every symbol when omitted.

    Args:
      symbol: Trading symbol, e.g. BTCUSDT. Omit to get every symbol as an array.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/market-data#ticker-24hr-price-change-statistics)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='/fapi/v1/ticker/24hr',
      meta={'security': 'NONE', 'signed': False},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
