# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Request parameters."""

  symbol: str
  """Trading symbol, e.g. BTCUSDT."""
  limit: NotRequired[Literal[1000]]
  """Number of price levels to return per side."""


class RpiOrderBook(TypedDict):
  """Order book snapshot for a symbol, including RPI orders."""

  lastUpdateId: int
  """Identifier of the last update reflected in this snapshot."""
  E: int
  """Message output time, in milliseconds since epoch."""
  T: int
  """Transaction time, in milliseconds since epoch."""
  bids: list[tuple[Decimal, Decimal]]
  """Bid price levels, best (highest) bid first."""
  asks: list[tuple[Decimal, Decimal]]
  """Ask price levels, best (lowest) ask first."""


class RpiDepth(RpcEndpoint):
  """Order book depth for a symbol, with Retail Price Improvement (RPI) orders included and aggregated. Crossed price levels are hidden."""

  async def rpi_depth(
    self,
    symbol: str,
    *,
    limit: Literal[1000] | None = None,
    validate: bool | None = None,
  ) -> RpiOrderBook:
    """Order book depth for a symbol, with Retail Price Improvement (RPI) orders included and aggregated. Crossed price levels are hidden.

    Args:
      symbol: Trading symbol, e.g. BTCUSDT.
      limit: Number of price levels to return per side.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/market-data#rpi-order-book)
    """
    request: Request = Request(symbol=symbol)
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/fapi/v1/rpiDepth',
      meta={'security': 'NONE', 'signed': False},
      validate=validate,
      request_type=Request,
      response_type=RpiOrderBook,
    )
