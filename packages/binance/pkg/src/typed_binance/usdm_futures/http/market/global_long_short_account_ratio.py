# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class GlobalAccountRatio(TypedDict):
  """All-account long/short account ratio at one period bucket."""

  symbol: str
  """Trading symbol."""
  longShortRatio: Decimal
  """Ratio of long to short accounts, across all accounts."""
  longAccount: Decimal
  """Proportion of all accounts holding long positions."""
  shortAccount: Decimal
  """Proportion of all accounts holding short positions."""
  timestamp: TimestampMillis
  """Period end time, in milliseconds since epoch."""


class Request(TypedDict):
  """Request parameters."""

  symbol: str
  """Trading symbol, e.g. BTCUSDT."""
  period: Literal['5m', '15m', '30m', '1h', '2h', '4h', '6h', '12h', '1d']
  """Bucket width for each returned data point."""
  limit: NotRequired[int]
  """Number of data points to return."""
  startTime: NotRequired[TimestampMillis]
  """Start time, in milliseconds since epoch."""
  endTime: NotRequired[TimestampMillis]
  """End time, in milliseconds since epoch."""


Response = list[GlobalAccountRatio]


class GlobalLongShortAccountRatio(RpcEndpoint):
  """Long/short account ratio across all accounts (not just top traders) for a symbol, bucketed by period."""

  async def global_long_short_account_ratio(
    self,
    symbol: str,
    *,
    period: Literal['5m', '15m', '30m', '1h', '2h', '4h', '6h', '12h', '1d'],
    limit: int | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Long/short account ratio across all accounts (not just top traders) for a symbol, bucketed by period.

    Args:
      symbol: Trading symbol, e.g. BTCUSDT.
      period: Bucket width for each returned data point.
      limit: Number of data points to return.
      start_time: Start time, in milliseconds since epoch.
      end_time: End time, in milliseconds since epoch.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/market-data#long-short-ratio)
    """
    request: Request = Request(symbol=symbol, period=period)
    if limit is not None:
      request['limit'] = limit
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    return await self.request(
      request,
      method='GET',
      path='/futures/data/globalLongShortAccountRatio',
      meta={'security': 'NONE', 'signed': False},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
