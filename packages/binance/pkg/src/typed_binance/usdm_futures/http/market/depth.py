# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class OrderBook(TypedDict):
  """Order book snapshot for a symbol."""

  lastUpdateId: int
  """Identifier of the last update reflected in this snapshot."""
  E: int
  """Message output time, in milliseconds since epoch."""
  T: int
  """Transaction time, in milliseconds since epoch."""
  bids: list[tuple[Decimal, Decimal]]
  """Bid price levels, best (highest) bid first."""
  asks: list[tuple[Decimal, Decimal]]
  """Ask price levels, best (lowest) ask first."""


class Request(TypedDict):
  """Request parameters."""

  symbol: str
  """Trading symbol, e.g. BTCUSDT."""
  limit: NotRequired[Literal[5, 10, 20, 50, 100, 500, 1000]]
  """Number of price levels to return per side."""


class Depth(RpcEndpoint):
  """Order book depth (bids and asks) for a symbol."""

  async def depth(
    self,
    symbol: str,
    *,
    limit: Literal[5, 10, 20, 50, 100, 500, 1000] | None = None,
    validate: bool | None = None,
  ) -> OrderBook:
    """Order book depth (bids and asks) for a symbol.

    Args:
      symbol: Trading symbol, e.g. BTCUSDT.
      limit: Number of price levels to return per side.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/market-data#order-book)
    """
    request: Request = Request(symbol=symbol)
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/fapi/v1/depth',
      meta={'security': 'NONE', 'signed': False},
      validate=validate,
      request_type=Request,
      response_type=OrderBook,
    )
