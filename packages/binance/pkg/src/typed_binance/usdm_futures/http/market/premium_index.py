# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class MarkPriceInfo0(TypedDict):
  """Mark price, index price, and latest funding rate for one symbol."""

  symbol: str
  """Trading symbol."""
  markPrice: Decimal
  """Mark price."""
  indexPrice: Decimal
  """Index price."""
  estimatedSettlePrice: Decimal
  """Estimated settlement price. Only meaningful in the last hour before settlement."""
  lastFundingRate: Decimal
  """Latest funding rate."""
  interestRate: Decimal
  """Interest rate used in the funding rate calculation."""
  nextFundingTime: TimestampMillis
  """Next funding time, in milliseconds since epoch."""
  time: TimestampMillis
  """Current time, in milliseconds since epoch."""


class MarkPriceInfoItem(TypedDict):
  """Mark price, index price, and latest funding rate for one symbol."""

  symbol: str
  """Trading symbol."""
  markPrice: Decimal
  """Mark price."""
  indexPrice: Decimal
  """Index price."""
  estimatedSettlePrice: Decimal
  """Estimated settlement price. Only meaningful in the last hour before settlement."""
  lastFundingRate: Decimal
  """Latest funding rate."""
  interestRate: Decimal
  """Interest rate used in the funding rate calculation."""
  nextFundingTime: TimestampMillis
  """Next funding time, in milliseconds since epoch."""
  time: TimestampMillis
  """Current time, in milliseconds since epoch."""


class Request(TypedDict):
  """Request parameters."""

  symbol: NotRequired[str]
  """Trading symbol, e.g. BTCUSDT. Omit to get every symbol as an array."""


Response = MarkPriceInfo0 | list[MarkPriceInfoItem]


class PremiumIndex(RpcEndpoint):
  """Mark price, index price, and latest funding rate for a symbol, or for every symbol when omitted."""

  async def premium_index(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Mark price, index price, and latest funding rate for a symbol, or for every symbol when omitted.

    Args:
      symbol: Trading symbol, e.g. BTCUSDT. Omit to get every symbol as an array.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/market-data#mark-price)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='/fapi/v1/premiumIndex',
      meta={'security': 'NONE', 'signed': False},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
