# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class UsdMFuturesSymbolConfig(TypedDict):
  """This account's current margin-type/leverage configuration for one symbol."""

  symbol: NotRequired[str]
  """Symbol."""
  marginType: NotRequired[Literal['ISOLATED', 'CROSSED']]
  """Margin type currently set for this symbol."""
  isAutoAddMargin: NotRequired[bool]
  """Whether isolated margin is automatically topped up for this symbol."""
  leverage: NotRequired[int]
  """Current initial leverage for this symbol."""
  maxNotionalValue: NotRequired[str]
  """Maximum notional value allowed under the current leverage for this symbol."""


class SymbolConfig(RpcEndpoint):
  """Get this account's current per-symbol margin-type/leverage configuration. If `symbol` is omitted, every symbol's configuration is returned."""

  async def symbol_config(
    self,
    *,
    symbol: str | None = None,
    validate: bool | None = None,
  ) -> list[UsdMFuturesSymbolConfig]:
    """Get this account's current per-symbol margin-type/leverage configuration. If `symbol` is omitted, every symbol's configuration is returned.

    Args:
      symbol: Symbol to query configuration for. If omitted, every symbol's configuration is returned.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/account#symbol-configuration)
    """
    params = {}
    if symbol is not None:
      params['symbol'] = symbol
    _Response = list[UsdMFuturesSymbolConfig]
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/fapi/v1/symbolConfig',
      params=params,
      validator=_validator,
      validate=validate,
    )
