# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Request parameters."""

  symbol: NotRequired[str]
  """Symbol to query brackets for. If omitted, brackets for every symbol are returned."""


class UsdMFuturesLeverageBracket0BracketsItem(TypedDict):
  """A single notional/leverage bracket row."""

  bracket: NotRequired[int]
  """Bracket number."""
  initialLeverage: NotRequired[int]
  """Maximum initial leverage allowed for this bracket."""
  notionalCap: NotRequired[int]
  """Notional cap of this bracket."""
  notionalFloor: NotRequired[int]
  """Notional threshold (floor) of this bracket."""
  maintMarginRatio: NotRequired[float]
  """Maintenance margin ratio for this bracket."""
  cum: NotRequired[float]
  """Auxiliary value for quick maintenance-margin calculation."""


class UsdMFuturesLeverageBracketItemBracketsItem(TypedDict):
  """A single notional/leverage bracket row."""

  bracket: NotRequired[int]
  """Bracket number."""
  initialLeverage: NotRequired[int]
  """Maximum initial leverage allowed for this bracket."""
  notionalCap: NotRequired[int]
  """Notional cap of this bracket."""
  notionalFloor: NotRequired[int]
  """Notional threshold (floor) of this bracket."""
  maintMarginRatio: NotRequired[float]
  """Maintenance margin ratio for this bracket."""
  cum: NotRequired[float]
  """Auxiliary value for quick maintenance-margin calculation."""


class UsdMFuturesLeverageBracketGroup0(TypedDict):
  """Notional and leverage brackets for one symbol, returned when `symbol` is given."""

  symbol: NotRequired[str]
  """Symbol these brackets apply to."""
  notionalCoef: NotRequired[float]
  """User-specific symbol bracket multiplier. Present only when this account's bracket for the symbol has been individually adjusted."""
  brackets: NotRequired[list[UsdMFuturesLeverageBracket0BracketsItem]]
  """Notional/leverage brackets for this symbol, ordered from lowest to highest bracket."""


class UsdMFuturesLeverageBracketGroupItem(TypedDict):
  """Notional and leverage brackets for one symbol, as one entry of the all-symbols array."""

  symbol: NotRequired[str]
  """Symbol these brackets apply to."""
  notionalCoef: NotRequired[float]
  """User-specific symbol bracket multiplier. Present only when this account's bracket for the symbol has been individually adjusted."""
  brackets: NotRequired[list[UsdMFuturesLeverageBracketItemBracketsItem]]
  """Notional/leverage brackets for this symbol, ordered from lowest to highest bracket."""


Response = UsdMFuturesLeverageBracketGroup0 | list[UsdMFuturesLeverageBracketGroupItem]


class LeverageBracket(RpcEndpoint):
  """Query this account's notional and leverage brackets. The response shape depends on whether `symbol` is given: with `symbol`, a single bracket group for that symbol; without it, an array of bracket groups for every symbol."""

  async def leverage_bracket(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Query this account's notional and leverage brackets. The response shape depends on whether `symbol` is given: with `symbol`, a single bracket group for that symbol; without it, an array of bracket groups for every symbol.

    Args:
      symbol: Symbol to query brackets for. If omitted, brackets for every symbol are returned.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/account#notional-and-leverage-brackets)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='/fapi/v1/leverageBracket',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
