# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class BookTickerEvent(TypedDict):
  """Best bid/ask price and quantity for one symbol."""

  e: str
  """Event type."""
  u: int
  """Order book update ID."""
  E: TimestampMillis
  """Event time."""
  T: TimestampMillis
  """Transaction time."""
  s: str
  """Symbol."""
  b: str
  """Best bid price."""
  B: str
  """Best bid quantity."""
  a: str
  """Best ask price."""
  A: str
  """Best ask quantity."""
  ps: NotRequired[str]
  """(After UM/CM stream-host migration) Pair symbol."""
  st: NotRequired[int]
  """(After UM/CM stream-host migration) Symbol type: 1 = UM, 2 = CM."""


class BookTickerArr(StreamEndpoint):
  """Pushes any change to the best bid or ask price or quantity, in real time, for all symbols, every 5 seconds."""

  def book_ticker_arr(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[BookTickerEvent, Any, Any]:
    """Pushes any change to the best bid or ask price or quantity, in real time, for all symbols, every 5 seconds.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/ws-streams/public#all-book-tickers-stream)
    """
    return self.subscribe(
      '!bookTicker',
      None,
      validate=validate,
      response_type=BookTickerEvent,
    )
