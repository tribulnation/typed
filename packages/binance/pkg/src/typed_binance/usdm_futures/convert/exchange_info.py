# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class UsdMFuturesConvertPair(TypedDict):
  """A convertible token pair and its amount limits."""

  fromAsset: NotRequired[str]
  """Asset the user would spend."""
  toAsset: NotRequired[str]
  """Asset the user would receive."""
  fromAssetMinAmount: NotRequired[str]
  """Minimum amount of `fromAsset` per conversion."""
  fromAssetMaxAmount: NotRequired[str]
  """Maximum amount of `fromAsset` per conversion."""
  toAssetMinAmount: NotRequired[str]
  """Minimum amount of `toAsset` per conversion."""
  toAssetMaxAmount: NotRequired[str]
  """Maximum amount of `toAsset` per conversion."""


class ExchangeInfo(RpcEndpoint):
  """Query for all convertible token pairs and their upper/lower amount limits. If neither `fromAsset` nor `toAsset` is given, only a partial set of token pairs is returned."""

  async def exchange_info(
    self,
    *,
    from_asset: str | None = None,
    to_asset: str | None = None,
    validate: bool | None = None,
  ) -> list[UsdMFuturesConvertPair]:
    """Query for all convertible token pairs and their upper/lower amount limits. If neither `fromAsset` nor `toAsset` is given, only a partial set of token pairs is returned.

    Args:
      from_asset: Asset the user would spend.
      to_asset: Asset the user would receive.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/convert#list-all-convert-pairs)
    """
    params = {}
    if from_asset is not None:
      params['fromAsset'] = from_asset
    if to_asset is not None:
      params['toAsset'] = to_asset
    _Response = list[UsdMFuturesConvertPair]
    _validator = validator[_Response](_Response)
    return await self.request(
      'GET',
      '/fapi/v1/convert/exchangeInfo',
      params=params,
      validator=_validator,
      validate=validate,
    )
