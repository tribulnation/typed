# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class BatchOrderError(TypedDict):
  """Per-element failure returned in place of an order when a batch element is rejected."""

  code: int
  """Binance error code for this element."""
  msg: str
  """Human-readable error message for this element."""


class NewFuturesOrder(TypedDict):
  """One successfully placed order."""

  clientOrderId: str
  """Client order id, either caller-supplied or venue-generated."""
  cumQty: str
  """Cumulative filled quantity."""
  executedQty: str
  """Cumulative filled quantity."""
  orderId: int
  """Order id."""
  origQty: str
  """Original order quantity."""
  price: str
  """Order price."""
  reduceOnly: bool
  """Whether the order only reduces an existing position."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: Literal['BOTH', 'LONG', 'SHORT']
  """Position side."""
  status: Literal[
    'NEW',
    'PARTIALLY_FILLED',
    'FILLED',
    'CANCELED',
    'REJECTED',
    'EXPIRED',
    'EXPIRED_IN_MATCH',
  ]
  """Order status."""
  stopPrice: str
  """Trigger price for a conditional order. Ignored for LIMIT and MARKET orders."""
  closePosition: bool
  """Whether the order closes the entire position (Close-All)."""
  symbol: str
  """Trading symbol."""
  timeInForce: Literal['GTC', 'IOC', 'FOK', 'GTX', 'GTD', 'RPI']
  """Time in force."""
  type: Literal[
    'LIMIT',
    'MARKET',
    'STOP',
    'STOP_MARKET',
    'TAKE_PROFIT',
    'TAKE_PROFIT_MARKET',
    'TRAILING_STOP_MARKET',
  ]
  """Order type."""
  origType: Literal[
    'LIMIT',
    'MARKET',
    'STOP',
    'STOP_MARKET',
    'TAKE_PROFIT',
    'TAKE_PROFIT_MARKET',
    'TRAILING_STOP_MARKET',
  ]
  """Order type at creation, unaffected by later amendments."""
  updateTime: TimestampMillis
  """Time the order was last updated."""
  workingType: Literal['MARK_PRICE', 'CONTRACT_PRICE']
  """Price type used to trigger conditional orders."""
  priceProtect: bool
  """Whether price protection is active for a conditional order's trigger."""
  priceMatch: Literal[
    'OPPONENT',
    'OPPONENT_5',
    'OPPONENT_10',
    'OPPONENT_20',
    'QUEUE',
    'QUEUE_5',
    'QUEUE_10',
    'QUEUE_20',
  ]
  """Price match mode used to auto-align the order price to the order book."""
  selfTradePreventionMode: Literal[
    'NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER'
  ]
  """Self-trade prevention mode."""
  goodTillDate: TimestampMillis
  """Auto-cancel time for a GTD order, as a Unix epoch in milliseconds."""


class PlaceMultipleOrders(RpcEndpoint):
  """Place multiple orders in one request. Parameter rules per order are the same as New Order."""

  async def place_multiple_orders(
    self,
    *,
    batch_orders: str,
    validate: bool | None = None,
  ) -> list[NewFuturesOrder | BatchOrderError]:
    """Place multiple orders in one request. Parameter rules per order are the same as New Order.

    Args:
      batch_orders: JSON-encoded array of orders to place, e.g. '[{"symbol":"BTCUSDT","side":"BUY","type":"LIMIT","quantity":"1","price":"30000","timeInForce":"GTC"}]'. Max 5 orders. Each element carries the same fields as New Order: symbol, side, positionSide, type, timeInForce, quantity, reduceOnly, price, newClientOrderId, newOrderRespType, priceMatch, selfTradePreventionMode, goodTillDate.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/trade#place-multiple-orders)
    """
    params: dict = {
      'batchOrders': batch_orders,
    }
    _Response = list[NewFuturesOrder | BatchOrderError]
    _validator = validator[_Response](_Response)  # type: ignore
    return await self.authed_request(
      'POST',
      '/fapi/v1/batchOrders',
      params=params,
      validator=_validator,
      validate=validate,
    )
