# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class UsdMFuturesIncomeHistoryDownloadLink(TypedDict):
  """Status and download link for a futures transaction (income) history export job."""

  downloadId: NotRequired[str]
  """Download id this status/link corresponds to."""
  status: NotRequired[Literal['completed', 'processing']]
  """Export job status."""
  url: NotRequired[str]
  """Download link for the export, once `status` is `completed`."""
  notified: NotRequired[bool]
  """Reserved field; not meaningful to callers."""
  expirationTimestamp: NotRequired[TimestampMillis]
  """Time the download link expires, as milliseconds since epoch."""
  isExpired: NotRequired[str]
  """Whether the download link has already expired."""


class IncomeAsynId(RpcEndpoint):
  """Get the futures transaction (income) history download link for a download id previously obtained from `usdm_futures.trading.income_asyn`. The link expires 7 days after it becomes available."""

  async def income_asyn_id(
    self,
    *,
    download_id: str,
    validate: bool | None = None,
  ) -> UsdMFuturesIncomeHistoryDownloadLink:
    """Get the futures transaction (income) history download link for a download id previously obtained from `usdm_futures.trading.income_asyn`. The link expires 7 days after it becomes available.

    Args:
      download_id: Download id, obtained from `usdm_futures.trading.income_asyn`.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/account#get-futures-transaction-history-download-link-by-id)
    """
    params: dict = {
      'downloadId': download_id,
    }
    _Response = UsdMFuturesIncomeHistoryDownloadLink
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/fapi/v1/income/asyn/id',
      params=params,
      validator=_validator,
      validate=validate,
    )
