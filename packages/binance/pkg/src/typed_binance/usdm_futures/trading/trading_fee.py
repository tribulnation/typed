# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class UsdMFuturesCommissionRate(TypedDict):
  """Maker/taker commission rates for a symbol on this account."""

  symbol: NotRequired[str]
  """Symbol these rates apply to."""
  makerCommissionRate: NotRequired[str]
  """Maker commission rate, as a decimal string (e.g. `"0.0002"` for 0.02%)."""
  takerCommissionRate: NotRequired[str]
  """Taker commission rate, as a decimal string (e.g. `"0.0004"` for 0.04%)."""
  rpiCommissionRate: NotRequired[str]
  """RPI (retail price improvement) order commission rate, as a decimal string (e.g. `"0.00005"` for 0.005%)."""


class TradingFee(RpcEndpoint):
  """Get this account's maker/taker commission rate for a symbol."""

  async def trading_fee(
    self,
    *,
    symbol: str,
    validate: bool | None = None,
  ) -> UsdMFuturesCommissionRate:
    """Get this account's maker/taker commission rate for a symbol.

    Args:
      symbol: Symbol to query the commission rate for.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/account#user-commission-rate)
    """
    params: dict = {
      'symbol': symbol,
    }
    _Response = UsdMFuturesCommissionRate
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/fapi/v1/commissionRate',
      params=params,
      validator=_validator,
      validate=validate,
    )
