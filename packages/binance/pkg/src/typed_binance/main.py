# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .coinm_futures import CoinmFutures
from .options import Options
from .portfolio_margin import PortfolioMargin
from .spot import Spot
from .usdm_futures import UsdmFutures
from typed_binance.core.base import BinanceBase


class Binance(BinanceBase):
  """Binance client root -- composes five independently-transported products (Spot, USDⓈ-M Futures, COIN-M Futures, Options, Portfolio Margin), each its own REST/stream/WS-API surface.

  References:
    - [Upstream docs](https://developers.binance.com/en/docs)
  """

  @cached_property
  def coinm_futures(self) -> CoinmFutures:
    """Binance COIN-M Futures -- REST, market-data streams, and the WS API, together.

    References:
      - [Upstream docs](https://developers.binance.com/docs/derivatives/coin-margined-futures/general-info)
    """
    return CoinmFutures.new(self.coinm_futures_clients)

  @cached_property
  def options(self) -> Options:
    """Binance Options -- REST, market-data streams, and the private user-data stream, together.

    References:
      - [Upstream docs](https://developers.binance.com/docs/derivatives/options-trading/general-info)
    """
    return Options.new(self.options_clients)

  @cached_property
  def portfolio_margin(self) -> PortfolioMargin:
    """Binance Portfolio Margin -- REST and the private user-data stream, together.

    References:
      - [Upstream docs](https://developers.binance.com/docs/derivatives/portfolio-margin/general-info)
    """
    return PortfolioMargin.new(self.portfolio_margin_clients)

  @cached_property
  def spot(self) -> Spot:
    """Binance Spot -- REST, market-data streams, and the request/reply WS API, together.

    References:
      - [Upstream docs](https://github.com/binance/binance-spot-api-docs)
    """
    return Spot.new(self.spot_clients)

  @cached_property
  def usdm_futures(self) -> UsdmFutures:
    """Binance USDⓈ-M Futures -- REST, market-data streams, the private user-data stream, and the WS API, together.

    References:
      - [Upstream docs](https://developers.binance.com/docs/derivatives/usds-margined-futures/general-info)
    """
    return UsdmFutures.new(self.usdm_futures_clients)
