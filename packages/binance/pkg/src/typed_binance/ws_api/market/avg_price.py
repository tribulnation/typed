# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.ws_rpc import WsRpcEndpoint


class AveragePrice(TypedDict):
  """The volume-weighted average price for a symbol over a trailing window."""

  mins: int
  """Average price interval, in minutes."""
  price: str
  """Average price."""
  closeTime: TimestampMillis
  """Time of the last trade considered."""


class AvgPrice(WsRpcEndpoint):
  """Current average price"""

  async def avg_price(
    self, *, symbol: str, validate: bool | None = None
  ) -> AveragePrice:
    """Current average price for a symbol.

    Args:
      symbol: Symbol to query.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/web-socket-api.md#current-average-price)
    """
    params: dict = {
      'symbol': symbol,
    }
    _Response = AveragePrice
    _validator = validator[_Response](_Response)
    return await self.request(
      'avgPrice', params=params, validator=_validator, validate=validate
    )
