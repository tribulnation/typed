# Generated by typed-dev — see CONTRIBUTING.md.
from json import dumps
from typing_extensions import Literal
from typed_core.validation import validator
from typed_binance.types import Ticker24hrFull, Ticker24hrMini
from typed_binance.core.endpoint.ws_rpc import WsRpcEndpoint


class Ticker24hr(WsRpcEndpoint):
  """24hr ticker price change statistics"""

  async def ticker_24hr(
    self,
    *,
    symbol: str | None = None,
    symbols: list[str] | None = None,
    type: Literal['FULL', 'MINI'] | None = None,
    symbol_status: Literal['TRADING', 'HALT', 'BREAK'] | None = None,
    validate: bool | None = None,
  ) -> Ticker24hrFull | list[Ticker24hrFull] | Ticker24hrMini | list[Ticker24hrMini]:
    """24-hour rolling window price change statistics. If no symbol is specified, returns information about all symbols currently trading on the exchange, as an array. If you need different window sizes, use the `ticker` (rolling window) request instead.

    Args:
      symbol: Single symbol to query. Cannot be combined with `symbols`. If neither `symbol` nor `symbols` is sent, tickers for all symbols are returned as an array.
      symbols: Symbols to query, e.g. ["BTCUSDT","BNBUSDT"]. Cannot be combined with `symbol`.
      type: Response field set.
      symbol_status: Filter for symbols with this trading status. For a single symbol, a status mismatch returns error -1220 SYMBOL_DOES_NOT_MATCH_STATUS; for multiple or all symbols, non-matching ones are simply excluded from the response.

    References:
      - [Official docs](https://github.com/binance/binance-spot-api-docs/blob/master/web-socket-api.md#24hr-ticker-price-change-statistics)
    """
    params = {}
    if symbol is not None:
      params['symbol'] = symbol
    if symbols is not None:
      params['symbols'] = dumps(symbols, separators=(',', ':'))
    if type is not None:
      params['type'] = type
    if symbol_status is not None:
      params['symbolStatus'] = symbol_status
    _Response = (
      Ticker24hrFull | list[Ticker24hrFull] | Ticker24hrMini | list[Ticker24hrMini]
    )
    _validator = validator[_Response](_Response)  # type: ignore
    return await self.request(
      'ticker.24hr', params=params, validator=_validator, validate=validate
    )
