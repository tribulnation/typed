# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.ws_rpc import WsRpcEndpoint


class CoinMWsBalance(TypedDict):
  """Balance detail for one settlement asset (e.g. BTC, ETH). All monetary fields are denominated in this asset, not USDT."""

  accountAlias: str
  """Unique account code."""
  asset: str
  """Asset symbol."""
  balance: str
  """Account balance, in this asset."""
  withdrawAvailable: str
  """Amount available for withdrawal, in this asset."""
  crossWalletBalance: str
  """Wallet balance for crossed margin, in this asset."""
  crossUnPnl: str
  """Total unrealized profit or loss of crossed positions, in this asset."""
  availableBalance: str
  """Available margin balance, in this asset."""
  updateTime: TimestampMillis
  """Time this balance was last updated."""


Response = list[CoinMWsBalance]


class Balance(WsRpcEndpoint):
  """Check account balance, one entry per settlement asset held by this account."""

  async def balance(self, *, validate: bool | None = None) -> Response:
    """Check account balance, one entry per settlement asset held by this account.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/ws-api/account#futures-account-balance)
    """
    return await self.request(
      None,
      path='account.balance',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      response_type=Response,
    )
