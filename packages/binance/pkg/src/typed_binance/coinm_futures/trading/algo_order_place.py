# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class CoinMNewAlgoOrderResult(TypedDict):
  """The newly-created algo order."""

  algoId: int
  """Algo order ID."""
  clientAlgoId: NotRequired[str]
  """Client algo order ID."""
  algoType: Literal['CONDITIONAL']
  """Algo order category."""
  orderType: NotRequired[
    Literal[
      'LIMIT',
      'MARKET',
      'STOP',
      'STOP_MARKET',
      'TAKE_PROFIT',
      'TAKE_PROFIT_MARKET',
      'TRAILING_STOP_MARKET',
    ]
  ]
  """Underlying order type once triggered."""
  symbol: str
  """Symbol."""
  pair: NotRequired[str]
  """Underlying pair."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side."""
  timeInForce: NotRequired[Literal['GTC', 'IOC', 'FOK', 'GTX']]
  """Time in force."""
  quantity: NotRequired[str]
  """Order quantity, in contracts."""
  algoStatus: str
  """Algo order lifecycle status. Not documented as a closed set anywhere this pass could confirm (the SDK's sibling USD-M model types it as a bare string too) — left bare per rule 2 rather than guessing values."""
  triggerPrice: NotRequired[str]
  """Trigger price."""
  price: NotRequired[str]
  """Order price."""
  reduceOnly: NotRequired[bool]
  """Whether the order is reduce-only."""
  closePosition: NotRequired[bool]
  """Whether the order closes the whole position (Close-All)."""
  priceProtect: NotRequired[bool]
  """Whether the conditional order's trigger is price-protected."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """triggerPrice trigger price type."""
  priceMatch: NotRequired[
    Literal[
      'NONE',
      'OPPONENT',
      'OPPONENT_5',
      'OPPONENT_10',
      'OPPONENT_20',
      'QUEUE',
      'QUEUE_5',
      'QUEUE_10',
      'QUEUE_20',
    ]
  ]
  """Price match mode."""
  selfTradePreventionMode: NotRequired[
    Literal['NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER']
  ]
  """Self-trade prevention mode."""
  activatePrice: NotRequired[str]
  """Activation price. Only returned for TRAILING_STOP_MARKET orders."""
  callbackRate: NotRequired[str]
  """Callback rate. Only returned for TRAILING_STOP_MARKET orders."""
  createTime: NotRequired[TimestampMillis]
  """Algo order creation time."""
  updateTime: NotRequired[TimestampMillis]
  """Last update time."""
  triggerTime: NotRequired[TimestampMillis]
  """Time the condition was last triggered. 0 if never triggered."""


class AlgoOrderPlace(RpcEndpoint):
  """New Algo Order"""

  async def algo_order_place(
    self,
    *,
    algo_type: Literal['CONDITIONAL'],
    symbol: str,
    side: Literal['BUY', 'SELL'],
    type: Literal[
      'LIMIT',
      'MARKET',
      'STOP',
      'STOP_MARKET',
      'TAKE_PROFIT',
      'TAKE_PROFIT_MARKET',
      'TRAILING_STOP_MARKET',
    ],
    position_side: Literal['BOTH', 'LONG', 'SHORT'] | None = None,
    reduce_only: Literal['true', 'false'] | None = None,
    quantity: float | None = None,
    price: float | None = None,
    client_algo_id: str | None = None,
    trigger_price: float | None = None,
    close_position: Literal['true', 'false'] | None = None,
    activation_price: float | None = None,
    callback_rate: float | None = None,
    time_in_force: Literal['GTC', 'IOC', 'FOK', 'GTX'] | None = None,
    working_type: Literal['MARK_PRICE', 'CONTRACT_PRICE'] | None = None,
    price_protect: Literal['true', 'false'] | None = None,
    new_order_resp_type: Literal['ACK', 'RESULT'] | None = None,
    price_match: Literal[
      'NONE',
      'OPPONENT',
      'OPPONENT_5',
      'OPPONENT_10',
      'OPPONENT_20',
      'QUEUE',
      'QUEUE_5',
      'QUEUE_10',
      'QUEUE_20',
    ]
    | None = None,
    self_trade_prevention_mode: Literal[
      'NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER'
    ]
    | None = None,
    validate: bool | None = None,
  ) -> CoinMNewAlgoOrderResult:
    """Send in a new algo (conditional) order. Per the CM/UM Architecture Integration migration (2026-06-10), COIN-M's five stop-family order types (STOP, STOP_MARKET, TAKE_PROFIT, TAKE_PROFIT_MARKET, TRAILING_STOP_MARKET) moved from `new_order` to this endpoint. See `notes` for this spec's confidence caveats.

    Args:
      algo_type: Algo order category. Only `CONDITIONAL` is documented for this migration.
      symbol: Symbol, e.g. BTCUSD_PERP.
      side: Order side.
      type: Underlying order type once triggered. See `notes` — this endpoint's documented purpose (per `new_order`'s migration notice) is the 5 stop/trailing types; LIMIT/MARKET are included only because they're part of the sibling USD-M `algoOrder` endpoint's SDK-documented enum and COIN-M's own value set was not independently confirmed.
      position_side: Position side. `BOTH` for One-way Mode; `LONG` or `SHORT` for Hedge Mode.
      reduce_only: Whether the order is reduce-only. Cannot be sent in Hedge Mode; cannot be sent together with `closePosition=true`.
      quantity: Order quantity, in contracts. Cannot be sent together with `closePosition=true`.
      price: Order price.
      client_algo_id: A unique id among open algo orders. Auto-generated if not sent. Must match `^[.A-Z:/a-z0-9_-]{1,36}$`.
      trigger_price: Trigger price. Used with STOP/STOP_MARKET or TAKE_PROFIT/TAKE_PROFIT_MARKET orders.
      close_position: Close-All: closes the whole position. Used with STOP_MARKET or TAKE_PROFIT_MARKET orders. Cannot be sent together with `quantity` or `reduceOnly`.
      activation_price: Used with TRAILING_STOP_MARKET orders; defaults to the latest price.
      callback_rate: Callback rate, used with TRAILING_STOP_MARKET orders. Min 0.1, max 10, where 1 means 1%.
      time_in_force: Time in force.
      working_type: triggerPrice trigger price type.
      price_protect: Whether the conditional order's trigger is price-protected. Used with STOP/STOP_MARKET or TAKE_PROFIT/TAKE_PROFIT_MARKET orders.
      new_order_resp_type: Response verbosity.
      price_match: Price match mode. Only available for LIMIT/STOP/TAKE_PROFIT orders; cannot be sent together with `price`.
      self_trade_prevention_mode: Self-trade prevention mode. Only effective when `timeInForce` is IOC or GTC.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/rest-api/trade#new-algo-order)
    """
    params: dict = {
      'algoType': algo_type,
      'symbol': symbol,
      'side': side,
      'type': type,
    }
    if position_side is not None:
      params['positionSide'] = position_side
    if reduce_only is not None:
      params['reduceOnly'] = reduce_only
    if quantity is not None:
      params['quantity'] = quantity
    if price is not None:
      params['price'] = price
    if client_algo_id is not None:
      params['clientAlgoId'] = client_algo_id
    if trigger_price is not None:
      params['triggerPrice'] = trigger_price
    if close_position is not None:
      params['closePosition'] = close_position
    if activation_price is not None:
      params['activationPrice'] = activation_price
    if callback_rate is not None:
      params['callbackRate'] = callback_rate
    if time_in_force is not None:
      params['timeInForce'] = time_in_force
    if working_type is not None:
      params['workingType'] = working_type
    if price_protect is not None:
      params['priceProtect'] = price_protect
    if new_order_resp_type is not None:
      params['newOrderRespType'] = new_order_resp_type
    if price_match is not None:
      params['priceMatch'] = price_match
    if self_trade_prevention_mode is not None:
      params['selfTradePreventionMode'] = self_trade_prevention_mode
    _Response = CoinMNewAlgoOrderResult
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/dapi/v1/algoOrder',
      params=params,
      validator=_validator,
      validate=validate,
    )
