# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class CoinMLeverageBracket(TypedDict):
  """One notional bracket."""

  bracket: int
  """Bracket number."""
  initialLeverage: int
  """Maximum leverage available for this bracket."""
  qtyCap: int
  """Upper edge of this bracket's base asset quantity range."""
  qtylFloor: int
  """Lower edge of this bracket's base asset quantity range."""
  maintMarginRatio: float
  """Maintenance margin ratio for this bracket."""
  cum: float
  """Auxiliary number for quick calculation of maintenance amount within this bracket."""


class CoinMSymbolLeverageBracketGroup(TypedDict):
  """Notional brackets for one symbol."""

  symbol: str
  """Trading symbol."""
  notionalCoef: NotRequired[float]
  """User-specific bracket multiplier. Present only when this account has an adjusted bracket for this symbol."""
  brackets: list[CoinMLeverageBracket]
  """Notional brackets, ordered from lowest to highest."""


class LeverageBracketBySymbol(RpcEndpoint):
  """Get the symbol's notional bracket list."""

  async def leverage_bracket_by_symbol(
    self,
    *,
    symbol: str | None = None,
    validate: bool | None = None,
  ) -> list[CoinMSymbolLeverageBracketGroup]:
    """Get the symbol's notional bracket list.

    Args:
      symbol: Trading symbol, e.g. BTCUSD_PERP. Every symbol's brackets are returned when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/rest-api/account#notional-bracket-for-symbol)
    """
    params = {}
    if symbol is not None:
      params['symbol'] = symbol
    _Response = list[CoinMSymbolLeverageBracketGroup]
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/dapi/v2/leverageBracket',
      params=params,
      validator=_validator,
      validate=validate,
    )
