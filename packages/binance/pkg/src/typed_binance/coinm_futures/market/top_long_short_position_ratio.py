# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis, timestamp_millis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class CoinMTopTraderLongShortPositionRatio(TypedDict):
  """Top-trader long/short position ratio for one period bucket."""

  pair: str
  """Underlying pair."""
  longShortRatio: str
  """Long/short ratio."""
  longPosition: str
  """Proportion of top-trader open positions net long, as a decimal fraction (e.g. "0.7019", not a percent string)."""
  shortPosition: str
  """Proportion of top-trader open positions net short, as a decimal fraction (e.g. "0.2981", not a percent string)."""
  timestamp: TimestampMillis
  """End time of the period, in milliseconds since epoch."""


class TopLongShortPositionRatio(RpcEndpoint):
  """The proportion of net long and net short positions to total open positions, among the top 20% of users by margin balance. Long Position % = long positions of top traders / total open positions of top traders; Short Position % is the net-short equivalent; Long/Short Ratio (Positions) = Long Position % / Short Position %."""

  async def top_long_short_position_ratio(
    self,
    *,
    pair: str,
    period: Literal['5m', '15m', '30m', '1h', '2h', '4h', '6h', '12h', '1d'],
    limit: int | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    validate: bool | None = None,
  ) -> list[CoinMTopTraderLongShortPositionRatio]:
    """The proportion of net long and net short positions to total open positions, among the top 20% of users by margin balance. Long Position % = long positions of top traders / total open positions of top traders; Short Position % is the net-short equivalent; Long/Short Ratio (Positions) = Long Position % / Short Position %.

    Args:
      pair: Underlying pair.
      period: Bucket period.
      limit: Number of records to return.
      start_time: Start time, in milliseconds since epoch.
      end_time: End time, in milliseconds since epoch.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/rest-api/market-data#top-trader-long-short-ratio-positions)
    """
    params: dict = {
      'pair': pair,
      'period': period,
    }
    if limit is not None:
      params['limit'] = limit
    if start_time is not None:
      params['startTime'] = timestamp_millis.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp_millis.dump(end_time)
    _Response = list[CoinMTopTraderLongShortPositionRatio]
    _validator = validator[_Response](_Response)
    return await self.request(
      'GET',
      '/futures/data/topLongShortPositionRatio',
      params=params,
      validator=_validator,
      validate=validate,
    )
