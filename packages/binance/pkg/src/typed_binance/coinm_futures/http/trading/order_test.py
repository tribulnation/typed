# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core.endpoint.rpc import RpcEndpoint


class CoinMTestOrderResult(TypedDict):
  """Empty object on success — the order is validated but never sent to the matching engine."""


class LimitOrder(TypedDict):
  """A limit order: filled at price or better. Requires timeInForce, quantity and price."""

  symbol: str
  """Symbol, e.g. BTCUSD_PERP."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['LIMIT']
  """Order type."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. `BOTH` for One-way Mode; `LONG` or `SHORT` for Hedge Mode."""
  timeInForce: Literal['GTC', 'IOC', 'FOK', 'GTX']
  """Time in force."""
  reduceOnly: NotRequired[Literal['true', 'false']]
  """Whether the order is reduce-only. Cannot be sent in Hedge Mode; cannot be sent together with `closePosition=true`."""
  quantity: float
  """Order quantity, in contracts. Cannot be sent together with `closePosition=true`."""
  price: float
  """Order price."""
  newClientOrderId: NotRequired[str]
  """A unique id among open orders. Auto-generated if not sent."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT']]
  """Response verbosity."""
  priceMatch: NotRequired[
    Literal[
      'NONE',
      'OPPONENT',
      'OPPONENT_5',
      'OPPONENT_10',
      'OPPONENT_20',
      'QUEUE',
      'QUEUE_5',
      'QUEUE_10',
      'QUEUE_20',
    ]
  ]
  """Price match mode. Only available for LIMIT/STOP/TAKE_PROFIT orders; cannot be sent together with `price`."""
  selfTradePreventionMode: NotRequired[
    Literal['NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER']
  ]
  """Self-trade prevention mode. Only effective when `timeInForce` is IOC or GTC."""


class MarketOrder(TypedDict):
  """A market order: filled immediately at the best available price. Requires quantity."""

  symbol: str
  """Symbol, e.g. BTCUSD_PERP."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['MARKET']
  """Order type."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. `BOTH` for One-way Mode; `LONG` or `SHORT` for Hedge Mode."""
  reduceOnly: NotRequired[Literal['true', 'false']]
  """Whether the order is reduce-only. Cannot be sent in Hedge Mode; cannot be sent together with `closePosition=true`."""
  quantity: float
  """Order quantity, in contracts. Cannot be sent together with `closePosition=true`."""
  newClientOrderId: NotRequired[str]
  """A unique id among open orders. Auto-generated if not sent."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT']]
  """Response verbosity."""
  selfTradePreventionMode: NotRequired[
    Literal['NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER']
  ]
  """Self-trade prevention mode. Only effective when `timeInForce` is IOC or GTC."""


class StopMarketOrTakeProfitMarketOrder(TypedDict):
  """A STOP_MARKET or TAKE_PROFIT_MARKET order: a market order that only becomes live once the mark/contract price reaches stopPrice. Requires stopPrice. quantity is required unless closePosition is true (Close-All), which is only usable with these two types and cannot be combined with quantity or reduceOnly -- this variant leaves both quantity and closePosition optional to express the either/or, rather than splitting further into quantity-only/closePosition-only sub-variants. See notes -- after the CM/UM migration this endpoint rejects this order type with -4120; kept here because it is still the parameter's documented schema, not a live-reachable variant."""

  symbol: str
  """Symbol, e.g. BTCUSD_PERP."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['STOP_MARKET', 'TAKE_PROFIT_MARKET']
  """Order type."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. `BOTH` for One-way Mode; `LONG` or `SHORT` for Hedge Mode."""
  reduceOnly: NotRequired[Literal['true', 'false']]
  """Whether the order is reduce-only. Cannot be sent in Hedge Mode; cannot be sent together with `closePosition=true`."""
  quantity: NotRequired[float]
  """Order quantity, in contracts. Cannot be sent together with `closePosition=true`."""
  stopPrice: float
  """Trigger price. Used with STOP/STOP_MARKET or TAKE_PROFIT/TAKE_PROFIT_MARKET orders."""
  closePosition: NotRequired[Literal['true', 'false']]
  """Close-All. Used with STOP_MARKET or TAKE_PROFIT_MARKET orders."""
  newClientOrderId: NotRequired[str]
  """A unique id among open orders. Auto-generated if not sent."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """stopPrice trigger price type."""
  priceProtect: NotRequired[Literal['true', 'false']]
  """Whether the conditional order's trigger is price-protected."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT']]
  """Response verbosity."""
  selfTradePreventionMode: NotRequired[
    Literal['NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER']
  ]
  """Self-trade prevention mode. Only effective when `timeInForce` is IOC or GTC."""


class StopOrTakeProfitOrder(TypedDict):
  """A STOP or TAKE_PROFIT order: a limit order that only becomes live once the mark/contract price reaches stopPrice. Requires quantity, price and stopPrice. timeInForce may be sent (defaults to GTC). See notes -- after the CM/UM migration this endpoint rejects this order type with -4120; kept here because it is still the parameter's documented schema, not a live-reachable variant."""

  symbol: str
  """Symbol, e.g. BTCUSD_PERP."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['STOP', 'TAKE_PROFIT']
  """Order type."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. `BOTH` for One-way Mode; `LONG` or `SHORT` for Hedge Mode."""
  timeInForce: NotRequired[Literal['GTC', 'IOC', 'FOK', 'GTX']]
  """Time in force. May be sent for STOP/TAKE_PROFIT orders; defaults to GTC."""
  reduceOnly: NotRequired[Literal['true', 'false']]
  """Whether the order is reduce-only. Cannot be sent in Hedge Mode; cannot be sent together with `closePosition=true`."""
  quantity: float
  """Order quantity, in contracts. Cannot be sent together with `closePosition=true`."""
  price: float
  """Order price."""
  stopPrice: float
  """Trigger price. Used with STOP/STOP_MARKET or TAKE_PROFIT/TAKE_PROFIT_MARKET orders."""
  newClientOrderId: NotRequired[str]
  """A unique id among open orders. Auto-generated if not sent."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """stopPrice trigger price type."""
  priceProtect: NotRequired[Literal['true', 'false']]
  """Whether the conditional order's trigger is price-protected."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT']]
  """Response verbosity."""
  priceMatch: NotRequired[
    Literal[
      'NONE',
      'OPPONENT',
      'OPPONENT_5',
      'OPPONENT_10',
      'OPPONENT_20',
      'QUEUE',
      'QUEUE_5',
      'QUEUE_10',
      'QUEUE_20',
    ]
  ]
  """Price match mode. Only available for LIMIT/STOP/TAKE_PROFIT orders; cannot be sent together with `price`."""
  selfTradePreventionMode: NotRequired[
    Literal['NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER']
  ]
  """Self-trade prevention mode. Only effective when `timeInForce` is IOC or GTC."""


class TrailingStopMarketOrder(TypedDict):
  """A TRAILING_STOP_MARKET order: a market order that trails the best price reached since placement and triggers once the price pulls back by callbackRate. Requires callbackRate. Binance's own new_order docstring does not list quantity as required for this type (unlike STOP/TAKE_PROFIT), so it is kept optional here rather than guessed as required. See notes -- after the CM/UM migration this endpoint rejects this order type with -4120; kept here because it is still the parameter's documented schema, not a live-reachable variant."""

  symbol: str
  """Symbol, e.g. BTCUSD_PERP."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['TRAILING_STOP_MARKET']
  """Order type."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. `BOTH` for One-way Mode; `LONG` or `SHORT` for Hedge Mode."""
  reduceOnly: NotRequired[Literal['true', 'false']]
  """Whether the order is reduce-only. Cannot be sent in Hedge Mode; cannot be sent together with `closePosition=true`."""
  quantity: NotRequired[float]
  """Order quantity, in contracts. Cannot be sent together with `closePosition=true`."""
  activationPrice: NotRequired[float]
  """Used with TRAILING_STOP_MARKET orders."""
  callbackRate: float
  """Callback rate for TRAILING_STOP_MARKET orders."""
  newClientOrderId: NotRequired[str]
  """A unique id among open orders. Auto-generated if not sent."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """stopPrice trigger price type."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT']]
  """Response verbosity."""
  selfTradePreventionMode: NotRequired[
    Literal['NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER']
  ]
  """Self-trade prevention mode. Only effective when `timeInForce` is IOC or GTC."""


class OrderTest(RpcEndpoint):
  """Test Order"""

  async def order_test(
    self,
    limit_order_market_order_stop_or_take_profit_order_stop_market_or_take_profit_market_order_trailing_stop_market_order: LimitOrder
    | MarketOrder
    | StopOrTakeProfitOrder
    | StopMarketOrTakeProfitMarketOrder
    | TrailingStopMarketOrder,
    *,
    validate: bool | None = None,
  ) -> CoinMTestOrderResult:
    """Send in a new order. Required fields differ by order type -- see the requestBody variants.

    Args:
      limit_order_market_order_stop_or_take_profit_order_stop_market_or_take_profit_market_order_trailing_stop_market_order: New order request. Shape depends on order type; see docs/spec/authoring.md rule 0 -- a discriminated-union payload whose required fields differ by variant.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/rest-api/trade#test-order)
    """
    params = dict(
      limit_order_market_order_stop_or_take_profit_order_stop_market_or_take_profit_market_order_trailing_stop_market_order
    )
    _Response = CoinMTestOrderResult
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'POST',
      '/dapi/v1/order/test',
      params=params,
      validator=_validator,
      validate=validate,
    )
