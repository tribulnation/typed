# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class CoinMActionResult(TypedDict):
  """Acknowledgement of the action."""

  code: int
  """Result code."""
  msg: str
  """Result message."""


class Request(TypedDict):
  """Request parameters."""

  dualSidePosition: Literal['true', 'false']
  """\"true" enables Hedge Mode; "false" enables One-way Mode."""


class ChangePositionMode(RpcEndpoint):
  """Change the account's position mode (Hedge Mode or One-way Mode) on every symbol."""

  async def change_position_mode(
    self,
    dual_side_position: Literal['true', 'false'],
    *,
    validate: bool | None = None,
  ) -> CoinMActionResult:
    """Change the account's position mode (Hedge Mode or One-way Mode) on every symbol.

    Args:
      dual_side_position: \"true" enables Hedge Mode; "false" enables One-way Mode.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/rest-api/trade#change-position-mode)
    """
    return await self.request(
      Request(dualSidePosition=dual_side_position),
      method='POST',
      path='/dapi/v1/positionSide/dual',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CoinMActionResult,
    )
