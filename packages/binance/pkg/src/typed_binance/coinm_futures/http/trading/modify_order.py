# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class CoinMModifyOrderResult(TypedDict):
  """The modified order."""

  orderId: int
  """Order ID."""
  symbol: str
  """Symbol."""
  pair: NotRequired[str]
  """Underlying pair."""
  status: Literal['NEW', 'PARTIALLY_FILLED', 'FILLED', 'CANCELED', 'EXPIRED']
  """Order status. Documented closed set (COIN-M common-definition doc)."""
  clientOrderId: NotRequired[str]
  """Client order ID."""
  modifyId: NotRequired[int]
  """User-supplied modification identifier, echoed back if it was sent on the request."""
  price: NotRequired[str]
  """Order price."""
  origQty: NotRequired[str]
  """Original order quantity, in contracts."""
  executedQty: NotRequired[str]
  """Executed quantity, in contracts."""
  cumQty: NotRequired[str]
  """Cumulative filled quantity, in contracts."""
  timeInForce: NotRequired[Literal['GTC', 'IOC', 'FOK', 'GTX']]
  """Time in force."""
  type: NotRequired[
    Literal[
      'LIMIT',
      'MARKET',
      'STOP',
      'STOP_MARKET',
      'TAKE_PROFIT',
      'TAKE_PROFIT_MARKET',
      'TRAILING_STOP_MARKET',
    ]
  ]
  """Order type. See `notes` — after the CM/UM migration this endpoint rejects the five stop-type values with -4120; only LIMIT and MARKET are currently accepted here."""
  reduceOnly: NotRequired[bool]
  """Whether the order is reduce-only."""
  closePosition: NotRequired[bool]
  """Whether the order closes the whole position (Close-All)."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side."""
  stopPrice: NotRequired[str]
  """Stop trigger price. Ignored when order type is TRAILING_STOP_MARKET."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """stopPrice trigger price type."""
  priceProtect: NotRequired[bool]
  """Whether the conditional order's trigger is price-protected."""
  origType: NotRequired[
    Literal[
      'LIMIT',
      'MARKET',
      'STOP',
      'STOP_MARKET',
      'TAKE_PROFIT',
      'TAKE_PROFIT_MARKET',
      'TRAILING_STOP_MARKET',
    ]
  ]
  """Original order type."""
  priceMatch: NotRequired[
    Literal[
      'NONE',
      'OPPONENT',
      'OPPONENT_5',
      'OPPONENT_10',
      'OPPONENT_20',
      'QUEUE',
      'QUEUE_5',
      'QUEUE_10',
      'QUEUE_20',
    ]
  ]
  """Price match mode."""
  selfTradePreventionMode: NotRequired[
    Literal['NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER']
  ]
  """Self-trade prevention mode."""
  updateTime: NotRequired[TimestampMillis]
  """Last update time."""


class Request(TypedDict):
  """Request parameters."""

  symbol: str
  """Symbol."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  orderId: NotRequired[int]
  """Order ID. Either `orderId` or `origClientOrderId` must be sent; `orderId` prevails if both are."""
  origClientOrderId: NotRequired[str]
  """Client order ID."""
  quantity: float
  """New order quantity, in contracts. Required (must be sent together with `price`) after the CM/UM migration."""
  price: float
  """New order price. Required (must be sent together with `quantity`) after the CM/UM migration."""
  priceMatch: NotRequired[
    Literal[
      'NONE',
      'OPPONENT',
      'OPPONENT_5',
      'OPPONENT_10',
      'OPPONENT_20',
      'QUEUE',
      'QUEUE_5',
      'QUEUE_10',
      'QUEUE_20',
    ]
  ]
  """Price match mode. Only available for LIMIT/STOP/TAKE_PROFIT orders; cannot be sent together with `price`."""
  modifyId: NotRequired[int]
  """User-defined modification identifier, echoed back as-is in the response if sent. Not validated for uniqueness."""


class ModifyOrder(RpcEndpoint):
  """Modify an existing LIMIT order's price and/or quantity. Currently only LIMIT order modification is supported; the order is re-queued in the match engine."""

  async def modify_order(
    self,
    *,
    symbol: str,
    side: Literal['BUY', 'SELL'],
    order_id: int | None = None,
    orig_client_order_id: str | None = None,
    quantity: float,
    price: float,
    price_match: Literal[
      'NONE',
      'OPPONENT',
      'OPPONENT_5',
      'OPPONENT_10',
      'OPPONENT_20',
      'QUEUE',
      'QUEUE_5',
      'QUEUE_10',
      'QUEUE_20',
    ]
    | None = None,
    modify_id: int | None = None,
    validate: bool | None = None,
  ) -> CoinMModifyOrderResult:
    """Modify an existing LIMIT order's price and/or quantity. Currently only LIMIT order modification is supported; the order is re-queued in the match engine.

    Args:
      symbol: Symbol.
      side: Order side.
      order_id: Order ID. Either `orderId` or `origClientOrderId` must be sent; `orderId` prevails if both are.
      orig_client_order_id: Client order ID.
      quantity: New order quantity, in contracts. Required (must be sent together with `price`) after the CM/UM migration.
      price: New order price. Required (must be sent together with `quantity`) after the CM/UM migration.
      price_match: Price match mode. Only available for LIMIT/STOP/TAKE_PROFIT orders; cannot be sent together with `price`.
      modify_id: User-defined modification identifier, echoed back as-is in the response if sent. Not validated for uniqueness.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/rest-api/trade#modify-order)
    """
    request: Request = Request(symbol=symbol, side=side, quantity=quantity, price=price)
    if order_id is not None:
      request['orderId'] = order_id
    if orig_client_order_id is not None:
      request['origClientOrderId'] = orig_client_order_id
    if price_match is not None:
      request['priceMatch'] = price_match
    if modify_id is not None:
      request['modifyId'] = modify_id
    return await self.request(
      request,
      method='PUT',
      path='/dapi/v1/order',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CoinMModifyOrderResult,
    )
