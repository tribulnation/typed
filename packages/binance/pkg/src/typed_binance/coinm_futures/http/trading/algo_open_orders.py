# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class CoinMAlgoOpenOrder(TypedDict):
  """One open algo order."""

  algoId: int
  """Algo order ID."""
  clientAlgoId: NotRequired[str]
  """Client algo order ID."""
  algoType: Literal['CONDITIONAL']
  """Algo order category."""
  orderType: NotRequired[
    Literal[
      'LIMIT',
      'MARKET',
      'STOP',
      'STOP_MARKET',
      'TAKE_PROFIT',
      'TAKE_PROFIT_MARKET',
      'TRAILING_STOP_MARKET',
    ]
  ]
  """Underlying order type once triggered."""
  symbol: str
  """Symbol."""
  pair: NotRequired[str]
  """Underlying pair."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side."""
  timeInForce: NotRequired[Literal['GTC', 'IOC', 'FOK', 'GTX']]
  """Time in force."""
  quantity: NotRequired[str]
  """Order quantity, in contracts."""
  algoStatus: str
  """Algo order lifecycle status. Not documented as a closed set anywhere this pass could confirm — left bare per rule 2 rather than guessing values."""
  actualOrderId: NotRequired[str]
  """The real order's ID once the condition has triggered and an order was placed on the book."""
  actualPrice: NotRequired[str]
  """The real order's price once the condition has triggered."""
  triggerPrice: NotRequired[str]
  """Trigger price."""
  price: NotRequired[str]
  """Order price."""
  reduceOnly: NotRequired[bool]
  """Whether the order is reduce-only."""
  closePosition: NotRequired[bool]
  """Whether the order closes the whole position (Close-All)."""
  priceProtect: NotRequired[bool]
  """Whether the conditional order's trigger is price-protected."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """triggerPrice trigger price type."""
  priceMatch: NotRequired[
    Literal[
      'NONE',
      'OPPONENT',
      'OPPONENT_5',
      'OPPONENT_10',
      'OPPONENT_20',
      'QUEUE',
      'QUEUE_5',
      'QUEUE_10',
      'QUEUE_20',
    ]
  ]
  """Price match mode."""
  selfTradePreventionMode: NotRequired[
    Literal['NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER']
  ]
  """Self-trade prevention mode."""
  createTime: NotRequired[TimestampMillis]
  """Algo order creation time."""
  updateTime: NotRequired[TimestampMillis]
  """Last update time."""
  triggerTime: NotRequired[TimestampMillis]
  """Time the condition was last triggered. 0 if never triggered."""


class Request(TypedDict):
  """Request parameters."""

  algoType: NotRequired[Literal['CONDITIONAL']]
  """Filter by algo order category. Only `CONDITIONAL` is documented for this migration."""
  symbol: NotRequired[str]
  """Symbol. If not sent, open algo orders for all symbols are returned."""
  algoId: NotRequired[int]
  """Filter to a single algo order ID."""


CoinMAlgoOpenOrders = list[CoinMAlgoOpenOrder]


class AlgoOpenOrders(RpcEndpoint):
  """Get all open algo (conditional) orders, optionally filtered by symbol. See `algo_order_place`'s `notes` for this group's source-confidence caveat."""

  async def algo_open_orders(
    self,
    algo_type: Literal['CONDITIONAL'] | None = None,
    *,
    symbol: str | None = None,
    algo_id: int | None = None,
    validate: bool | None = None,
  ) -> CoinMAlgoOpenOrders:
    """Get all open algo (conditional) orders, optionally filtered by symbol. See `algo_order_place`'s `notes` for this group's source-confidence caveat.

    Args:
      algo_type: Filter by algo order category. Only `CONDITIONAL` is documented for this migration.
      symbol: Symbol. If not sent, open algo orders for all symbols are returned.
      algo_id: Filter to a single algo order ID.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/rest-api/trade#current-all-algo-open-orders)
    """
    request: Request = Request()
    if algo_type is not None:
      request['algoType'] = algo_type
    if symbol is not None:
      request['symbol'] = symbol
    if algo_id is not None:
      request['algoId'] = algo_id
    return await self.request(
      request,
      method='GET',
      path='/dapi/v1/openAlgoOrders',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CoinMAlgoOpenOrders,
    )
