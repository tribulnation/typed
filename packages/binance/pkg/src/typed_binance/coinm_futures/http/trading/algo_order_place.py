# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class CoinMNewAlgoOrderResult(TypedDict):
  """The newly-created algo order."""

  algoId: int
  """Algo order ID."""
  clientAlgoId: NotRequired[str]
  """Client algo order ID."""
  algoType: Literal['CONDITIONAL']
  """Algo order category."""
  orderType: NotRequired[
    Literal[
      'LIMIT',
      'MARKET',
      'STOP',
      'STOP_MARKET',
      'TAKE_PROFIT',
      'TAKE_PROFIT_MARKET',
      'TRAILING_STOP_MARKET',
    ]
  ]
  """Underlying order type once triggered."""
  symbol: str
  """Symbol."""
  pair: NotRequired[str]
  """Underlying pair."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side."""
  timeInForce: NotRequired[Literal['GTC', 'IOC', 'FOK', 'GTX']]
  """Time in force."""
  quantity: NotRequired[str]
  """Order quantity, in contracts."""
  algoStatus: str
  """Algo order lifecycle status. Not documented as a closed set anywhere this pass could confirm (the SDK's sibling USD-M model types it as a bare string too) — left bare per rule 2 rather than guessing values."""
  triggerPrice: NotRequired[str]
  """Trigger price."""
  price: NotRequired[str]
  """Order price."""
  reduceOnly: NotRequired[bool]
  """Whether the order is reduce-only."""
  closePosition: NotRequired[bool]
  """Whether the order closes the whole position (Close-All)."""
  priceProtect: NotRequired[bool]
  """Whether the conditional order's trigger is price-protected."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """triggerPrice trigger price type."""
  priceMatch: NotRequired[
    Literal[
      'NONE',
      'OPPONENT',
      'OPPONENT_5',
      'OPPONENT_10',
      'OPPONENT_20',
      'QUEUE',
      'QUEUE_5',
      'QUEUE_10',
      'QUEUE_20',
    ]
  ]
  """Price match mode."""
  selfTradePreventionMode: NotRequired[
    Literal['NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER']
  ]
  """Self-trade prevention mode."""
  activatePrice: NotRequired[str]
  """Activation price. Only returned for TRAILING_STOP_MARKET orders."""
  callbackRate: NotRequired[str]
  """Callback rate. Only returned for TRAILING_STOP_MARKET orders."""
  createTime: NotRequired[TimestampMillis]
  """Algo order creation time."""
  updateTime: NotRequired[TimestampMillis]
  """Last update time."""
  triggerTime: NotRequired[TimestampMillis]
  """Time the condition was last triggered. 0 if never triggered."""


class CoinMStopMarketOrTakeProfitMarketAlgoOrder(TypedDict):
  """A STOP_MARKET or TAKE_PROFIT_MARKET algo order: a market order that only becomes live once the mark/contract price reaches triggerPrice. Requires triggerPrice. Either quantity or closePosition=true must be sent; closePosition closes the entire position and cannot be combined with quantity or reduceOnly."""

  algoType: Literal['CONDITIONAL']
  """Algo order category. Only `CONDITIONAL` is documented for this migration."""
  symbol: str
  """Symbol, e.g. BTCUSD_PERP."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['STOP_MARKET', 'TAKE_PROFIT_MARKET']
  """Algo order type."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. `BOTH` for One-way Mode; `LONG` or `SHORT` for Hedge Mode."""
  reduceOnly: NotRequired[Literal['true', 'false']]
  """Whether the order is reduce-only. Cannot be sent in Hedge Mode; cannot be sent together with `closePosition=true`."""
  quantity: NotRequired[float]
  """Order quantity, in contracts. Cannot be sent together with `closePosition=true`."""
  triggerPrice: float
  """Trigger price. Used with STOP/STOP_MARKET or TAKE_PROFIT/TAKE_PROFIT_MARKET orders."""
  closePosition: NotRequired[Literal['true', 'false']]
  """Close-All: closes the whole position. Used with STOP_MARKET or TAKE_PROFIT_MARKET orders. Cannot be sent together with `quantity` or `reduceOnly`."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """triggerPrice trigger price type."""
  priceProtect: NotRequired[Literal['true', 'false']]
  """Whether the conditional order's trigger is price-protected. Used with STOP/STOP_MARKET or TAKE_PROFIT/TAKE_PROFIT_MARKET orders."""
  clientAlgoId: NotRequired[str]
  """A unique id among open algo orders. Auto-generated if not sent. Must match `^[.A-Z:/a-z0-9_-]{1,36}$`."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT']]
  """Response verbosity."""
  selfTradePreventionMode: NotRequired[
    Literal['NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER']
  ]
  """Self-trade prevention mode. Only effective when `timeInForce` is IOC or GTC."""


class CoinMStopOrTakeProfitAlgoOrder(TypedDict):
  """A STOP or TAKE_PROFIT algo order: a limit order that only becomes live once the mark/contract price reaches triggerPrice. Requires quantity, price and triggerPrice; timeInForce may be sent (defaults to GTC)."""

  algoType: Literal['CONDITIONAL']
  """Algo order category. Only `CONDITIONAL` is documented for this migration."""
  symbol: str
  """Symbol, e.g. BTCUSD_PERP."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['STOP', 'TAKE_PROFIT']
  """Algo order type."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. `BOTH` for One-way Mode; `LONG` or `SHORT` for Hedge Mode."""
  timeInForce: NotRequired[Literal['GTC', 'IOC', 'FOK', 'GTX']]
  """Time in force. For type STOP or TAKE_PROFIT, defaults to GTC."""
  reduceOnly: NotRequired[Literal['true', 'false']]
  """Whether the order is reduce-only. Cannot be sent in Hedge Mode; cannot be sent together with `closePosition=true`."""
  quantity: float
  """Order quantity, in contracts. Cannot be sent together with `closePosition=true`."""
  price: float
  """Order price."""
  triggerPrice: float
  """Trigger price. Used with STOP/STOP_MARKET or TAKE_PROFIT/TAKE_PROFIT_MARKET orders."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """triggerPrice trigger price type."""
  priceProtect: NotRequired[Literal['true', 'false']]
  """Whether the conditional order's trigger is price-protected. Used with STOP/STOP_MARKET or TAKE_PROFIT/TAKE_PROFIT_MARKET orders."""
  priceMatch: NotRequired[
    Literal[
      'NONE',
      'OPPONENT',
      'OPPONENT_5',
      'OPPONENT_10',
      'OPPONENT_20',
      'QUEUE',
      'QUEUE_5',
      'QUEUE_10',
      'QUEUE_20',
    ]
  ]
  """Price match mode. Only available for LIMIT/STOP/TAKE_PROFIT orders; cannot be sent together with `price`."""
  clientAlgoId: NotRequired[str]
  """A unique id among open algo orders. Auto-generated if not sent. Must match `^[.A-Z:/a-z0-9_-]{1,36}$`."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT']]
  """Response verbosity."""
  selfTradePreventionMode: NotRequired[
    Literal['NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER']
  ]
  """Self-trade prevention mode. Only effective when `timeInForce` is IOC or GTC."""


class CoinMTrailingStopMarketAlgoOrder(TypedDict):
  """A TRAILING_STOP_MARKET algo order. Requires callbackRate. activationPrice defaults to the latest price if not sent; BUY triggers when the lowest price since placement * (1 + callbackRate) is reached, SELL when the highest price since activationPrice was reached and the latest price falls back by callbackRate."""

  algoType: Literal['CONDITIONAL']
  """Algo order category. Only `CONDITIONAL` is documented for this migration."""
  symbol: str
  """Symbol, e.g. BTCUSD_PERP."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['TRAILING_STOP_MARKET']
  """Algo order type."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. `BOTH` for One-way Mode; `LONG` or `SHORT` for Hedge Mode."""
  reduceOnly: NotRequired[Literal['true', 'false']]
  """Whether the order is reduce-only. Cannot be sent in Hedge Mode; cannot be sent together with `closePosition=true`."""
  quantity: NotRequired[float]
  """Order quantity, in contracts. Cannot be sent together with `closePosition=true`."""
  activationPrice: NotRequired[float]
  """Used with TRAILING_STOP_MARKET orders; defaults to the latest price."""
  callbackRate: float
  """Callback rate, used with TRAILING_STOP_MARKET orders. Min 0.1, max 10, where 1 means 1%."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """triggerPrice trigger price type."""
  clientAlgoId: NotRequired[str]
  """A unique id among open algo orders. Auto-generated if not sent. Must match `^[.A-Z:/a-z0-9_-]{1,36}$`."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT']]
  """Response verbosity."""
  selfTradePreventionMode: NotRequired[
    Literal['NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER']
  ]
  """Self-trade prevention mode. Only effective when `timeInForce` is IOC or GTC."""


Request = (
  CoinMStopOrTakeProfitAlgoOrder
  | CoinMStopMarketOrTakeProfitMarketAlgoOrder
  | CoinMTrailingStopMarketAlgoOrder
)


class AlgoOrderPlace(RpcEndpoint):
  """Send in a new algo (conditional) order. Per the CM/UM Architecture Integration migration (2026-06-10), COIN-M's five stop-family order types (STOP, STOP_MARKET, TAKE_PROFIT, TAKE_PROFIT_MARKET, TRAILING_STOP_MARKET) moved from `new_order` to this endpoint. See `notes` for this spec's confidence caveats."""

  async def algo_order_place(
    self,
    trading_algo_order_place_request: Request,
    *,
    validate: bool | None = None,
  ) -> CoinMNewAlgoOrderResult:
    """Send in a new algo (conditional) order. Per the CM/UM Architecture Integration migration (2026-06-10), COIN-M's five stop-family order types (STOP, STOP_MARKET, TAKE_PROFIT, TAKE_PROFIT_MARKET, TRAILING_STOP_MARKET) moved from `new_order` to this endpoint. See `notes` for this spec's confidence caveats.

    Args:
      trading_algo_order_place_request: Algo order to place. Supported order types under algoType=CONDITIONAL are STOP, TAKE_PROFIT, STOP_MARKET, TAKE_PROFIT_MARKET, and TRAILING_STOP_MARKET.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/rest-api/trade#new-algo-order)
    """
    return await self.request(
      trading_algo_order_place_request,
      method='POST',
      path='/dapi/v1/algoOrder',
      meta={'security': 'TRADE', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CoinMNewAlgoOrderResult,
    )
