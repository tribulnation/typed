# Generated by typed-dev — see CONTRIBUTING.md.
from json import dumps
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class CoinMBatchModifyOrderParams(TypedDict):
  """One order modification within a batch modify request. Same field rules as `modify_order`."""

  orderId: NotRequired[int]
  """Order ID. Required when `origClientOrderId` is not sent."""
  origClientOrderId: NotRequired[str]
  """Client order ID. Required when `orderId` is not sent."""
  symbol: str
  """Symbol."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  quantity: float
  """New order quantity, in contracts. Required after the CM/UM migration."""
  price: float
  """New order price. Required after the CM/UM migration."""
  modifyId: NotRequired[int]
  """User-defined modification identifier, echoed back as-is if sent."""


class CoinMBatchModifyOrderSuccess(TypedDict):
  """A successfully-modified order within the batch."""

  orderId: int
  """Order ID."""
  symbol: str
  """Symbol."""
  pair: NotRequired[str]
  """Underlying pair."""
  status: Literal['NEW', 'PARTIALLY_FILLED', 'FILLED', 'CANCELED', 'EXPIRED']
  """Order status. Documented closed set (COIN-M common-definition doc)."""
  clientOrderId: NotRequired[str]
  """Client order ID."""
  modifyId: NotRequired[int]
  """User-supplied modification identifier, echoed back if it was sent on the request."""
  price: NotRequired[str]
  """Order price."""
  origQty: NotRequired[str]
  """Original order quantity, in contracts."""
  executedQty: NotRequired[str]
  """Executed quantity, in contracts."""
  cumQty: NotRequired[str]
  """Cumulative filled quantity, in contracts."""
  timeInForce: NotRequired[Literal['GTC', 'IOC', 'FOK', 'GTX']]
  """Time in force."""
  type: NotRequired[
    Literal[
      'LIMIT',
      'MARKET',
      'STOP',
      'STOP_MARKET',
      'TAKE_PROFIT',
      'TAKE_PROFIT_MARKET',
      'TRAILING_STOP_MARKET',
    ]
  ]
  """Order type. See `notes` — after the CM/UM migration this endpoint rejects the five stop-type values with -4120; only LIMIT and MARKET are currently accepted here."""
  reduceOnly: NotRequired[bool]
  """Whether the order is reduce-only."""
  closePosition: NotRequired[bool]
  """Whether the order closes the whole position (Close-All)."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side."""
  stopPrice: NotRequired[str]
  """Stop trigger price. Ignored when order type is TRAILING_STOP_MARKET."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """stopPrice trigger price type."""
  priceProtect: NotRequired[bool]
  """Whether the conditional order's trigger is price-protected."""
  origType: NotRequired[
    Literal[
      'LIMIT',
      'MARKET',
      'STOP',
      'STOP_MARKET',
      'TAKE_PROFIT',
      'TAKE_PROFIT_MARKET',
      'TRAILING_STOP_MARKET',
    ]
  ]
  """Original order type."""
  priceMatch: NotRequired[
    Literal[
      'NONE',
      'OPPONENT',
      'OPPONENT_5',
      'OPPONENT_10',
      'OPPONENT_20',
      'QUEUE',
      'QUEUE_5',
      'QUEUE_10',
      'QUEUE_20',
    ]
  ]
  """Price match mode."""
  selfTradePreventionMode: NotRequired[
    Literal['NONE', 'EXPIRE_TAKER', 'EXPIRE_BOTH', 'EXPIRE_MAKER']
  ]
  """Self-trade prevention mode."""
  updateTime: NotRequired[TimestampMillis]
  """Last update time."""


class CoinMBatchOrderError(TypedDict):
  """A batch element that failed."""

  code: int
  """Binance error code for this element."""
  msg: str
  """Error message for this element."""


class ModifyMultipleOrders(RpcEndpoint):
  """Modify Multiple Orders"""

  async def modify_multiple_orders(
    self,
    *,
    batch_orders: list[CoinMBatchModifyOrderParams],
    validate: bool | None = None,
  ) -> list[CoinMBatchModifyOrderSuccess | CoinMBatchOrderError]:
    """Modify up to 5 LIMIT orders in one request. Processed concurrently; the response array preserves the request's order. One order can be modified fewer than 10000 times total.

    Args:
      batch_orders: Order modification list, max 5 orders.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/rest-api/trade#modify-multiple-orders)
    """
    params: dict = {
      'batchOrders': dumps(batch_orders, separators=(',', ':')),
    }
    _Response = list[CoinMBatchModifyOrderSuccess | CoinMBatchOrderError]
    _validator = validator[_Response](_Response)  # type: ignore
    return await self.authed_request(
      'PUT',
      '/dapi/v1/batchOrders',
      params=params,
      validator=_validator,
      validate=validate,
    )
