# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis, timestamp_millis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class CoinMUserTrade(TypedDict):
  """One fill."""

  symbol: str
  """Symbol."""
  id: int
  """Trade ID."""
  orderId: int
  """Order ID."""
  pair: NotRequired[str]
  """Underlying pair."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  price: str
  """Fill price."""
  qty: str
  """Fill quantity, in contracts."""
  realizedPnl: NotRequired[str]
  """Realized PnL from this fill, in the settlement asset."""
  marginAsset: NotRequired[str]
  """Margin asset."""
  baseQty: NotRequired[str]
  """Fill quantity denominated in the pair's base asset."""
  quoteQty: NotRequired[str]
  """Fill quantity denominated in the quote asset."""
  commission: NotRequired[str]
  """Commission charged."""
  commissionAsset: NotRequired[str]
  """Commission asset."""
  time: TimestampMillis
  """Fill time."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. `BOTH` for One-way Mode; `LONG` or `SHORT` for Hedge Mode."""
  buyer: NotRequired[bool]
  """Whether this account was the buyer."""
  maker: NotRequired[bool]
  """Whether this account was the maker."""


class AccountTradeList(RpcEndpoint):
  """Account Trade List"""

  async def account_trade_list(
    self,
    *,
    symbol: str | None = None,
    pair: str | None = None,
    order_id: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    from_id: int | None = None,
    validate: bool | None = None,
  ) -> list[CoinMUserTrade]:
    """Get trades for the account, filtered by symbol or pair.

    Args:
      symbol: Symbol. Either `symbol` or `pair` must be sent; they cannot be sent together.
      pair: Underlying pair. If sent, trades for every symbol of the pair are returned. Cannot be sent together with `fromId`.
      order_id: Order ID. Can only be sent together with `symbol`.
      start_time: Start time. Cannot be sent together with `fromId`.
      end_time: End time. The window between `startTime` and `endTime` cannot exceed 7 days; if neither is sent, the last 7 days are returned.
      from_id: Trade ID to fetch from. Defaults to the most recent trades.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/rest-api/trade#account-trade-list)
    """
    params = {}
    if symbol is not None:
      params['symbol'] = symbol
    if pair is not None:
      params['pair'] = pair
    if order_id is not None:
      params['orderId'] = order_id
    if start_time is not None:
      params['startTime'] = timestamp_millis.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp_millis.dump(end_time)
    if from_id is not None:
      params['fromId'] = from_id
    _Response = list[CoinMUserTrade]
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'GET',
      '/dapi/v1/userTrades',
      params=params,
      validator=_validator,
      validate=validate,
    )
