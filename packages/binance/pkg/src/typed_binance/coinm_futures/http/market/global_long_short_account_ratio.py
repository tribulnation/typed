# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_core.validation import validator
from typed_binance.core import TimestampMillis, timestamp_millis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class CoinMLongShortRatio(TypedDict):
  """Long/short account ratio for one period bucket, across all accounts."""

  pair: str
  """Underlying pair."""
  longShortRatio: str
  """Long/short ratio."""
  longAccount: str
  """Proportion of accounts net long, as a decimal fraction (e.g. "0.1961", not a percent string)."""
  shortAccount: str
  """Proportion of accounts net short, as a decimal fraction (e.g. "0.8015", not a percent string)."""
  timestamp: TimestampMillis
  """End time of the period, in milliseconds since epoch."""


class GlobalLongShortAccountRatio(RpcEndpoint):
  """Long/short account ratio across all accounts holding a position in a pair."""

  async def global_long_short_account_ratio(
    self,
    *,
    pair: str,
    period: Literal['5m', '15m', '30m', '1h', '2h', '4h', '6h', '12h', '1d'],
    limit: int | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    validate: bool | None = None,
  ) -> list[CoinMLongShortRatio]:
    """Long/short account ratio across all accounts holding a position in a pair.

    Args:
      pair: Underlying pair, e.g. BTCUSD.
      period: Bucket period.
      limit: Number of records to return.
      start_time: Start time, in milliseconds since epoch.
      end_time: End time, in milliseconds since epoch.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/rest-api/market-data#long-short-ratio)
    """
    params: dict = {
      'pair': pair,
      'period': period,
    }
    if limit is not None:
      params['limit'] = limit
    if start_time is not None:
      params['startTime'] = timestamp_millis.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp_millis.dump(end_time)
    _Response = list[CoinMLongShortRatio]
    _validator = validator[_Response](_Response)
    return await self.request(
      'GET',
      '/futures/data/globalLongShortAccountRatio',
      params=params,
      validator=_validator,
      validate=validate,
    )
