# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class CoinMTakerBuySellVolume(TypedDict):
  """Taker buy/sell volume for one period bucket."""

  pair: str
  """Underlying pair."""
  contractType: Literal['ALL', 'PERPETUAL', 'CURRENT_QUARTER', 'NEXT_QUARTER']
  """Contract type."""
  takerBuyVol: int
  """Taker buy volume, in contracts."""
  takerSellVol: int
  """Taker sell volume, in contracts."""
  takerBuyVolValue: Decimal
  """Taker buy volume, in base asset."""
  takerSellVolValue: Decimal
  """Taker sell volume, in base asset."""
  timestamp: TimestampMillis
  """Start time of the period, in milliseconds since epoch."""


class Request(TypedDict):
  """Request parameters."""

  pair: str
  """Underlying pair."""
  contractType: Literal['ALL', 'PERPETUAL', 'CURRENT_QUARTER', 'NEXT_QUARTER']
  """Contract type."""
  period: Literal['5m', '15m', '30m', '1h', '2h', '4h', '6h', '12h', '1d']
  """Bucket period."""
  limit: NotRequired[int]
  """Number of records to return."""
  startTime: NotRequired[TimestampMillis]
  """Start time, in milliseconds since epoch."""
  endTime: NotRequired[TimestampMillis]
  """End time, in milliseconds since epoch."""


Response = list[CoinMTakerBuySellVolume]


class TakerBuySellVol(RpcEndpoint):
  """Taker buy volume (total volume of buy orders filled by takers within the period) and taker sell volume (total volume of sell orders filled by takers within the period)."""

  async def taker_buy_sell_vol(
    self,
    pair: str,
    *,
    contract_type: Literal['ALL', 'PERPETUAL', 'CURRENT_QUARTER', 'NEXT_QUARTER'],
    period: Literal['5m', '15m', '30m', '1h', '2h', '4h', '6h', '12h', '1d'],
    limit: int | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Taker buy volume (total volume of buy orders filled by takers within the period) and taker sell volume (total volume of sell orders filled by takers within the period).

    Args:
      pair: Underlying pair.
      contract_type: Contract type.
      period: Bucket period.
      limit: Number of records to return.
      start_time: Start time, in milliseconds since epoch.
      end_time: End time, in milliseconds since epoch.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/rest-api/market-data#taker-buy-sell-volume)
    """
    request: Request = Request(pair=pair, contractType=contract_type, period=period)
    if limit is not None:
      request['limit'] = limit
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    return await self.request(
      request,
      method='GET',
      path='/futures/data/takerBuySellVol',
      meta={'security': 'NONE', 'signed': False},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
