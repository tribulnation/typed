# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class CoinMBookTicker(TypedDict):
  """Best bid/ask price and quantity for one symbol."""

  symbol: str
  """Trading symbol."""
  pair: str
  """Underlying pair."""
  lastUpdateId: int
  """Last update ID."""
  bidPrice: Decimal
  """Best bid price."""
  bidQty: int
  """Best bid quantity."""
  askPrice: Decimal
  """Best ask price."""
  askQty: int
  """Best ask quantity."""
  time: TimestampMillis
  """Time, in milliseconds since epoch."""


class Request(TypedDict):
  """Request parameters."""

  symbol: NotRequired[str]
  """Symbol. Mutually exclusive with `pair`. Omit to get every symbol."""
  pair: NotRequired[str]
  """Underlying pair. Mutually exclusive with `symbol`. Returns every symbol of the pair."""


Response = list[CoinMBookTicker]


class TickerBookTicker(RpcEndpoint):
  """Best price/quantity on the order book, for a symbol, for every symbol of a pair, or for every symbol."""

  async def ticker_book_ticker(
    self,
    *,
    symbol: str | None = None,
    pair: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Best price/quantity on the order book, for a symbol, for every symbol of a pair, or for every symbol.

    Args:
      symbol: Symbol. Mutually exclusive with `pair`. Omit to get every symbol.
      pair: Underlying pair. Mutually exclusive with `symbol`. Returns every symbol of the pair.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/rest-api/market-data#symbol-order-book-ticker)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    if pair is not None:
      request['pair'] = pair
    return await self.request(
      request,
      method='GET',
      path='/dapi/v1/ticker/bookTicker',
      meta={'security': 'NONE', 'signed': False},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
