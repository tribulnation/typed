# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class CoinMCommissionRate(TypedDict):
  """This account's commission rate for a symbol."""

  symbol: str
  """Trading symbol."""
  makerCommissionRate: str
  """Maker commission rate, e.g. "0.00015" for 0.015%."""
  takerCommissionRate: str
  """Taker commission rate, e.g. "0.00040" for 0.040%."""


class Request(TypedDict):
  """Request parameters."""

  symbol: str
  """Trading symbol."""


class CommissionRate(RpcEndpoint):
  """Query this account's maker/taker commission rate for a symbol."""

  async def commission_rate(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> CoinMCommissionRate:
    """Query this account's maker/taker commission rate for a symbol.

    Args:
      symbol: Trading symbol.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/rest-api/account#user-commission-rate)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/dapi/v1/commissionRate',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CoinMCommissionRate,
    )
