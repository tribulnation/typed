# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class CoinMIncome(TypedDict):
  """One income/cash-flow event."""

  symbol: str
  """Trading symbol. Absent for account-level income not tied to a symbol, e.g. TRANSFER."""
  incomeType: Literal[
    'TRANSFER',
    'WELCOME_BONUS',
    'FUNDING_FEE',
    'REALIZED_PNL',
    'COMMISSION',
    'INSURANCE_CLEAR',
    'DELIVERED_SETTELMENT',
  ]
  """Income type."""
  income: str
  """Income amount, in asset. Positive for a credit, negative for a debit."""
  asset: str
  """Asset the income is denominated in."""
  info: str
  """Extra information about this income event."""
  time: TimestampMillis
  """Time of the income event."""
  tranId: str
  """Transaction id."""
  tradeId: str
  """Trade id. Present only for trade-related income, e.g. REALIZED_PNL and COMMISSION; empty otherwise."""


class Request(TypedDict):
  """Request parameters."""

  symbol: NotRequired[str]
  """Trading symbol. Every symbol's income is returned when omitted."""
  incomeType: NotRequired[
    Literal[
      'TRANSFER',
      'WELCOME_BONUS',
      'FUNDING_FEE',
      'REALIZED_PNL',
      'COMMISSION',
      'INSURANCE_CLEAR',
      'DELIVERED_SETTELMENT',
    ]
  ]
  """Income type to filter by. Every income type is returned when omitted."""
  startTime: NotRequired[TimestampMillis]
  """Timestamp in ms to get income from, inclusive."""
  endTime: NotRequired[TimestampMillis]
  """Timestamp in ms to get income until, inclusive."""
  page: NotRequired[int]
  """Page number."""
  limit: NotRequired[int]
  """Maximum number of records to return. Range [1, 1000]; defaults to 100."""


CoinMIncomes = list[CoinMIncome]


class Income(RpcEndpoint):
  """Query income history: transfers, funding fees, realized PnL, commissions, and other account cash-flow events. Returns every income type when incomeType is omitted. The window between startTime and endTime cannot exceed 1 year."""

  async def income(
    self,
    symbol: str | None = None,
    *,
    income_type: Literal[
      'TRANSFER',
      'WELCOME_BONUS',
      'FUNDING_FEE',
      'REALIZED_PNL',
      'COMMISSION',
      'INSURANCE_CLEAR',
      'DELIVERED_SETTELMENT',
    ]
    | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    page: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> CoinMIncomes:
    """Query income history: transfers, funding fees, realized PnL, commissions, and other account cash-flow events. Returns every income type when incomeType is omitted. The window between startTime and endTime cannot exceed 1 year.

    Args:
      symbol: Trading symbol. Every symbol's income is returned when omitted.
      income_type: Income type to filter by. Every income type is returned when omitted.
      start_time: Timestamp in ms to get income from, inclusive.
      end_time: Timestamp in ms to get income until, inclusive.
      page: Page number.
      limit: Maximum number of records to return. Range [1, 1000]; defaults to 100.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/rest-api/account#get-income-history)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    if income_type is not None:
      request['incomeType'] = income_type
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if page is not None:
      request['page'] = page
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/dapi/v1/income',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CoinMIncomes,
    )
