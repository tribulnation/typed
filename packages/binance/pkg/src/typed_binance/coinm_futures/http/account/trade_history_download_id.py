# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class CoinMDownloadId(TypedDict):
  """Download id for an asynchronous history export."""

  avgCostTimestampOfLast30d: int
  """Average time, in milliseconds, taken to prepare a download over the past 30 days. A duration, not a point in time."""
  downloadId: str
  """Download task id, usable with the matching download-link endpoint to retrieve the export's URL."""


class Request(TypedDict):
  """Request parameters."""

  startTime: TimestampMillis
  """Timestamp in ms."""
  endTime: TimestampMillis
  """Timestamp in ms."""


class TradeHistoryDownloadId(RpcEndpoint):
  """Start an asynchronous export of this account's futures trade history for the given time window and get back a download id. The window between startTime and endTime cannot exceed 1 year."""

  async def trade_history_download_id(
    self,
    *,
    start_time: TimestampMillis,
    end_time: TimestampMillis,
    validate: bool | None = None,
  ) -> CoinMDownloadId:
    """Start an asynchronous export of this account's futures trade history for the given time window and get back a download id. The window between startTime and endTime cannot exceed 1 year.

    Args:
      start_time: Timestamp in ms.
      end_time: Timestamp in ms.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/rest-api/account#get-download-id-for-futures-trade-history)
    """
    return await self.request(
      Request(startTime=start_time, endTime=end_time),
      method='GET',
      path='/dapi/v1/trade/asyn',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CoinMDownloadId,
    )
