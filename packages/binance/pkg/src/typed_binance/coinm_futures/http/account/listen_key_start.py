# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_binance.core.endpoint.rpc import RpcEndpoint


class CoinMListenKey(TypedDict):
  """Listen key identifying a user data stream."""

  listenKey: NotRequired[str]
  """Listen key used to connect to the user data WebSocket stream."""


class ListenKeyStart(RpcEndpoint):
  """Start a new user data stream. The stream will close after 60 minutes unless a keepalive is sent. If the account has an active `listenKey`, that `listenKey` will be returned and its validity will be extended for 60 minutes."""

  async def listen_key_start(self, *, validate: bool | None = None) -> CoinMListenKey:
    """Start a new user data stream. The stream will close after 60 minutes unless a keepalive is sent. If the account has an active `listenKey`, that `listenKey` will be returned and its validity will be extended for 60 minutes.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/rest-api/user-data-streams#start-user-data-stream)
    """
    return await self.request(
      None,
      method='POST',
      path='/dapi/v1/listenKey',
      meta={'security': 'USER_STREAM', 'signed': False},
      validate=validate,
      response_type=CoinMListenKey,
    )
