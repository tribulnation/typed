# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.rpc import RpcEndpoint


class CoinMDownloadLink(TypedDict):
  """Status and URL of an asynchronous history export."""

  downloadId: str
  """Download task id."""
  status: Literal['completed', 'processing']
  """Export preparation status."""
  url: str
  """URL the export can be downloaded from once status is completed."""
  notified: bool
  """Whether the caller has already been notified of this export's readiness."""
  expirationTimestamp: TimestampMillis
  """Time after which the download link expires."""
  isExpired: NotRequired[str]
  """Whether the record is expired."""


class Request(TypedDict):
  """Request parameters."""

  downloadId: str
  """Download id, obtained from coinm_futures.account.trade_history_download_id."""


class TradeHistoryDownloadLink(RpcEndpoint):
  """Get the download link for a futures trade history export previously started with coinm_futures.account.trade_history_download_id."""

  async def trade_history_download_link(
    self,
    download_id: str,
    *,
    validate: bool | None = None,
  ) -> CoinMDownloadLink:
    """Get the download link for a futures trade history export previously started with coinm_futures.account.trade_history_download_id.

    Args:
      download_id: Download id, obtained from coinm_futures.account.trade_history_download_id.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/rest-api/account#get-futures-trade-download-link-by-id)
    """
    return await self.request(
      Request(downloadId=download_id),
      method='GET',
      path='/dapi/v1/trade/asyn/id',
      meta={'security': 'USER_DATA', 'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CoinMDownloadLink,
    )
