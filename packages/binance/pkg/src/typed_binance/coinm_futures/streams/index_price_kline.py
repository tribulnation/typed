# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class CoinMIndexPriceKline(TypedDict):
  """The kline/candlestick itself."""

  t: TimestampMillis
  """Kline start time."""
  T: TimestampMillis
  """Kline close time."""
  s: str
  """Symbol placeholder -- always the literal `"0"` for an index kline, since the index has no tradeable symbol."""
  i: Literal[
    '1m',
    '3m',
    '5m',
    '15m',
    '30m',
    '1h',
    '2h',
    '4h',
    '6h',
    '8h',
    '12h',
    '1d',
    '3d',
    '1w',
    '1M',
  ]
  """Interval."""
  f: int
  """First update ID."""
  L: int
  """Last update ID."""
  o: str
  """Open price."""
  c: str
  """Close price."""
  h: str
  """High price."""
  l: str
  """Low price."""
  v: str
  """Volume. Always `"0"` for an index kline -- there is no real trading volume behind the index."""
  n: int
  """Number of basic data points."""
  x: bool
  """Whether this kline is closed."""
  q: str
  """Base asset volume. Always `"0"` for an index kline."""
  V: str
  """Taker buy volume. Always `"0"` for an index kline."""
  Q: str
  """Taker buy base asset volume. Always `"0"` for an index kline."""
  B: str
  """Ignore -- unused field, kept for wire compatibility. Always `"0"` for an index kline."""


class CoinMIndexPriceKlineEvent(TypedDict):
  """One index price kline/candlestick update."""

  e: Literal['indexPrice_kline']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  ps: str
  """Pair."""
  k: CoinMIndexPriceKline


class IndexPriceKline(StreamEndpoint):
  """Pushes index price kline/candlestick data for one pair, every 250ms."""

  def index_price_kline(
    self,
    pair: str,
    *,
    interval: Literal[
      '1m',
      '3m',
      '5m',
      '15m',
      '30m',
      '1h',
      '2h',
      '4h',
      '6h',
      '8h',
      '12h',
      '1d',
      '3d',
      '1w',
      '1M',
    ],
    validate: bool | None = None,
  ) -> StreamManager[CoinMIndexPriceKlineEvent, Any, Any]:
    """Pushes index price kline/candlestick data for one pair, every 250ms.

    Args:
      pair: Underlying pair, e.g. `btcusd`. The venue requires stream names to use lowercase symbols on the wire, and this value is used as-is with no casing transform.
      interval: Kline interval.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/ws-streams/~#index-kline-candlestick-streams)
    """
    return self.subscribe(
      f'{pair}@indexPriceKline_{interval}',
      validate=validate,
      response_type=CoinMIndexPriceKlineEvent,
    )
