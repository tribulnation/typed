# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class CoinMBookTickerEvent(TypedDict):
  """Best bid/ask price and quantity update for one symbol's order book."""

  e: Literal['bookTicker']
  """Event type."""
  u: int
  """Order book update ID."""
  s: str
  """Symbol."""
  ps: str
  """Underlying pair."""
  b: str
  """Best bid price."""
  B: str
  """Best bid quantity."""
  a: str
  """Best ask price."""
  A: str
  """Best ask quantity."""
  T: TimestampMillis
  """Transaction time."""
  E: TimestampMillis
  """Event time."""
  st: NotRequired[Literal[1, 2]]
  """Symbol type: 1 = UM, 2 = CM. Added after the UM/CM stream-infrastructure migration; see notes."""


class BookTickerArr(StreamEndpoint):
  """Best bid/ask price and quantity for every symbol, pushed in real time as each individual update happens (one event per push, not batched into an array -- see notes)."""

  def book_ticker_arr(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[CoinMBookTickerEvent, Any, Any]:
    """Best bid/ask price and quantity for every symbol, pushed in real time as each individual update happens (one event per push, not batched into an array -- see notes).

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/ws-streams/~#all-book-tickers-stream)
    """
    return self.subscribe(
      '!bookTicker',
      None,
      validate=validate,
      response_type=CoinMBookTickerEvent,
    )
