# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class CoinMContractInfoBracket(TypedDict):
  """One notional bracket."""

  bs: int
  """Notional bracket number."""
  bnf: int
  """Floor notional for this bracket."""
  bnc: int
  """Cap notional for this bracket."""
  mmr: float
  """Maintenance margin ratio for this bracket."""
  cf: int
  """Auxiliary number for quick calculation of maintenance amount within this bracket."""
  mi: int
  """Minimum leverage for this bracket."""
  ma: int
  """Maximum leverage for this bracket."""


class CoinMContractInfoEvent(TypedDict):
  """Contract metadata update for one symbol."""

  e: Literal['contractInfo']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Symbol."""
  ps: str
  """Underlying pair."""
  ct: Literal[
    'PERPETUAL',
    'CURRENT_QUARTER',
    'NEXT_QUARTER',
    'CURRENT_QUARTER_DELIVERING',
    'NEXT_QUARTER_DELIVERING',
    'PERPETUAL_DELIVERING',
  ]
  """Contract type."""
  dt: TimestampMillis
  """Delivery (settlement) date/time. A far-future sentinel for a perpetual contract, which never actually delivers."""
  ot: TimestampMillis
  """Onboard (listing) date/time."""
  cs: Literal[
    'PENDING_TRADING',
    'TRADING',
    'PRE_DELIVERING',
    'DELIVERING',
    'DELIVERED',
    'TRADING_HALT',
    'TRADING_CANCEL_ONLY',
  ]
  """Contract trading status."""
  bks: NotRequired[list[CoinMContractInfoBracket]]
  """Notional bracket list. Present only when this update is a bracket change -- absent on a listing or settlement update."""
  st: NotRequired[Literal[1, 2]]
  """Symbol type: 1 = UM, 2 = CM. Added after the UM/CM stream-infrastructure migration; see notes."""


class ContractInfo(StreamEndpoint):
  """Pushes contract metadata changes (new listing, settlement, or notional-bracket update) in real time, for whichever contract changed."""

  def contract_info(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[CoinMContractInfoEvent, Any, Any]:
    """Pushes contract metadata changes (new listing, settlement, or notional-bracket update) in real time, for whichever contract changed.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/ws-streams/~#contract-info-stream)
    """
    return self.subscribe(
      '!contractInfo',
      None,
      validate=validate,
      response_type=CoinMContractInfoEvent,
    )
