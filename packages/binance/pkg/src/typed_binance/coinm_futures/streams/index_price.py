# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class CoinMIndexPriceEvent(TypedDict):
  """Index price update for one pair."""

  e: Literal['IndexUpdate']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Pair."""
  p: str
  """Index price."""


class IndexPrice(StreamEndpoint):
  """Index price for one pair, pushed every 1000ms."""

  def index_price(
    self,
    pair: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[CoinMIndexPriceEvent, Any, Any]:
    """Index price for one pair, pushed every 1000ms.

    Args:
      pair: Underlying pair, e.g. `btcusd`. The venue requires stream names to use lowercase symbols on the wire, and this value is used as-is with no casing transform.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/ws-streams/~#index-price-stream)
    """
    return self.subscribe(
      f'{pair}@indexPrice',
      validate=validate,
      response_type=CoinMIndexPriceEvent,
    )
