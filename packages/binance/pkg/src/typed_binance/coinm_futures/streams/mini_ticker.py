# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class CoinMMiniTickerEvent(TypedDict):
  """24hr rolling-window mini-ticker statistics for one symbol."""

  e: Literal['24hrMiniTicker']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Symbol."""
  ps: str
  """Underlying pair."""
  c: str
  """Close price."""
  o: str
  """Open price."""
  h: str
  """High price."""
  l: str
  """Low price."""
  v: str
  """Total traded volume, in contracts."""
  q: str
  """Total traded base asset volume."""
  st: NotRequired[Literal[1, 2]]
  """Symbol type: 1 = UM, 2 = CM. Added after the UM/CM stream-infrastructure migration; see notes."""


class MiniTicker(StreamEndpoint):
  """24hr rolling-window mini-ticker statistics for one symbol, pushed every 500ms."""

  def mini_ticker(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[CoinMMiniTickerEvent, Any, Any]:
    """24hr rolling-window mini-ticker statistics for one symbol, pushed every 500ms.

    Args:
      symbol: Contract symbol, e.g. `btcusd_perp`. The venue requires stream names to use lowercase symbols on the wire, and this value is used as-is with no casing transform.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/ws-streams/~#individual-symbol-mini-ticker-stream)
    """
    return self.subscribe(
      f'{symbol}@miniTicker',
      validate=validate,
      response_type=CoinMMiniTickerEvent,
    )
