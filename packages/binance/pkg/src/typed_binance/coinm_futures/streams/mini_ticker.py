# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.util import StreamManager
from typed_core.validation import validator
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class CoinMMiniTickerEvent(TypedDict):
  """24hr rolling-window mini-ticker statistics for one symbol."""

  e: Literal['24hrMiniTicker']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Symbol."""
  ps: str
  """Underlying pair."""
  c: str
  """Close price."""
  o: str
  """Open price."""
  h: str
  """High price."""
  l: str
  """Low price."""
  v: str
  """Total traded volume, in contracts."""
  q: str
  """Total traded base asset volume."""
  st: NotRequired[Literal[1, 2]]
  """Symbol type: 1 = UM, 2 = CM. Added after the UM/CM stream-infrastructure migration; see notes."""


class MiniTicker(StreamEndpoint):
  """Individual symbol mini ticker stream"""

  def __call__(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[CoinMMiniTickerEvent]:
    """Individual symbol mini ticker stream

    Args:
      symbol: Contract symbol, e.g. `BTCUSD_PERP`. The venue requires stream names to use lowercase symbols on the wire.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/ws-streams/~#individual-symbol-mini-ticker-stream)
    """
    _validator = validator[CoinMMiniTickerEvent](CoinMMiniTickerEvent)
    return self.subscribe(
      f'{symbol.lower()}@miniTicker', validator=_validator, validate=validate
    )
