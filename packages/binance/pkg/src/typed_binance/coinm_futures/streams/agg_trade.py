# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_binance.core import TimestampMillis
from typed_binance.core.endpoint.stream import StreamEndpoint


class CoinMAggTradeEvent(TypedDict):
  """One aggregated trade for a symbol."""

  e: Literal['aggTrade']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  a: int
  """Aggregate trade ID."""
  s: str
  """Symbol."""
  p: str
  """Price."""
  q: str
  """Quantity, in contracts."""
  f: int
  """First trade ID."""
  l: int
  """Last trade ID."""
  T: TimestampMillis
  """Trade time."""
  m: bool
  """Whether the buyer was the market maker."""
  st: NotRequired[Literal[1, 2]]
  """Symbol type: 1 = UM, 2 = CM. Added after the UM/CM stream-infrastructure migration; see notes."""


class AggTrade(StreamEndpoint):
  """Pushes trade information for one symbol, aggregated for a single taker order, every 100ms."""

  def agg_trade(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[CoinMAggTradeEvent, Any, Any]:
    """Pushes trade information for one symbol, aggregated for a single taker order, every 100ms.

    Args:
      symbol: Contract symbol, e.g. `btcusd_perp`. The venue requires stream names to use lowercase symbols on the wire, and this value is used as-is with no casing transform.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-coin-m-futures/api/ws-streams/~#aggregate-trade-streams)
    """
    return self.subscribe(
      f'{symbol}@aggTrade',
      validate=validate,
      response_type=CoinMAggTradeEvent,
    )
