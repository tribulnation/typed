# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_core.validation import validator
from typed_binance.core.endpoint.ws_rpc import WsRpcEndpoint


class UsdMWsCancelAlgoOrderResult(TypedDict):
  """Result of canceling an algo order."""

  algoId: int
  """Algo order id."""
  clientAlgoId: str
  """Client algo order id."""
  code: str
  """Result code."""
  msg: str
  """Result message."""


class CancelAlgoOrder(WsRpcEndpoint):
  """Cancel an active algo order."""

  async def cancel_algo_order(
    self,
    *,
    algo_id: int | None = None,
    client_algo_id: str | None = None,
    validate: bool | None = None,
  ) -> UsdMWsCancelAlgoOrderResult:
    """Cancel an active algo order.

    Args:
      algo_id: Algo order id. Either this or clientAlgoId is required.
      client_algo_id: Client algo order id. Either this or algoId is required.

    References:
      - [Official docs](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/ws-api/trade#cancel-algo-order)
    """
    params = {}
    if algo_id is not None:
      params['algoId'] = algo_id
    if client_algo_id is not None:
      params['clientAlgoId'] = client_algo_id
    _Response = UsdMWsCancelAlgoOrderResult
    _validator = validator[_Response](_Response)
    return await self.authed_request(
      'algoOrder.cancel', params=params, validator=_validator, validate=validate
    )
