# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_mexc.spot.core import SpotHttpEndpoint


class ListListenKeysResponse(TypedDict):
  """Valid listen-key list response."""

  listenKey: list[str]
  """Currently valid private user-data stream listen keys for the signed account."""


class List(SpotHttpEndpoint):
  """Returns the signed account's currently valid spot user-data stream listen keys."""

  async def list(self, *, validate: bool | None = None) -> ListListenKeysResponse:
    """Returns the signed account's currently valid spot user-data stream listen keys.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/websocket-user-data-streams/listen-key)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v3/userDataStream',
      meta={'signed': True},
      validate=validate,
      response_type=ListListenKeysResponse,
    )
