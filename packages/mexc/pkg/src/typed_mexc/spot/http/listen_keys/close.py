# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_mexc.spot.core import SpotHttpEndpoint


class CloseListenKeyResponse(TypedDict):
  """Listen-key close response."""

  listenKey: str
  """Private user-data stream listen key that was closed and invalidated."""


class Request(TypedDict):
  listenKey: str
  """Existing private user-data stream listen key to close."""


class Close(SpotHttpEndpoint):
  """Closes a spot user-data stream and invalidates its listen key."""

  async def close(
    self,
    listen_key: str,
    *,
    validate: bool | None = None,
  ) -> CloseListenKeyResponse:
    """Closes a spot user-data stream and invalidates its listen key.

    Args:
      listen_key: Existing private user-data stream listen key to close.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/websocket-user-data-streams/listen-key)
    """
    return await self.request(
      Request(listenKey=listen_key),
      method='DELETE',
      path='/api/v3/userDataStream',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CloseListenKeyResponse,
    )
