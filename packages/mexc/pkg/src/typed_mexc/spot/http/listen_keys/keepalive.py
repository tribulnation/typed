# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_mexc.spot.core import SpotHttpEndpoint


class KeepaliveListenKeyResponse(TypedDict):
  """Listen-key keepalive response."""

  listenKey: str
  """Private user-data stream listen key whose validity was extended."""


class Request(TypedDict):
  listenKey: str
  """Existing private user-data stream listen key to extend."""


class Keepalive(SpotHttpEndpoint):
  """Extends an existing spot user-data stream listen key for another 60 minutes from the time of the call."""

  async def keepalive(
    self,
    listen_key: str,
    *,
    validate: bool | None = None,
  ) -> KeepaliveListenKeyResponse:
    """Extends an existing spot user-data stream listen key for another 60 minutes from the time of the call.

    Args:
      listen_key: Existing private user-data stream listen key to extend.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/websocket-user-data-streams/listen-key)
    """
    return await self.request(
      Request(listenKey=listen_key),
      method='PUT',
      path='/api/v3/userDataStream',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=KeepaliveListenKeyResponse,
    )
