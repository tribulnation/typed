# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_mexc.spot.core import SpotHttpEndpoint


class CreateListenKeyResponse(TypedDict):
  """Listen-key creation response."""

  listenKey: str
  """Private user-data stream token used as the `listenKey` query parameter when opening the WebSocket connection."""


class Create(SpotHttpEndpoint):
  """Creates a new spot user-data stream listen key for private account, trade, and order WebSocket subscriptions."""

  async def create(self, *, validate: bool | None = None) -> CreateListenKeyResponse:
    """Creates a new spot user-data stream listen key for private account, trade, and order WebSocket subscriptions.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/websocket-user-data-streams/listen-key)
    """
    return await self.request(
      None,
      method='POST',
      path='/api/v3/userDataStream',
      meta={'signed': True},
      validate=validate,
      response_type=CreateListenKeyResponse,
    )
