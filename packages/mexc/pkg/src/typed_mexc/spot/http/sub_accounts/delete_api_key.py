# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.spot.core import SpotHttpEndpoint


class DeleteApiKeyResponse(TypedDict):
  """Deleted sub-account API key result."""

  subAccount: str | None
  """Sub-account name."""


class Request(TypedDict):
  subAccount: str
  """Sub-account name whose API key should be deleted."""
  apiKey: str
  """API public key to delete."""
  recvWindow: NotRequired[int]
  """Optional signed-request validity window in milliseconds."""


class DeleteApiKey(SpotHttpEndpoint):
  """Deletes an API key from a sub-account under the signed master account."""

  async def delete_api_key(
    self,
    *,
    sub_account: str,
    api_key: str,
    recv_window: int | None = None,
    validate: bool | None = None,
  ) -> DeleteApiKeyResponse:
    """Deletes an API key from a sub-account under the signed master account.

    Args:
      sub_account: Sub-account name whose API key should be deleted.
      api_key: API public key to delete.
      recv_window: Optional signed-request validity window in milliseconds.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/subaccount-endpoints/delete-the-apikey-of-a-sub-account-for-master-account)
    """
    request: Request = Request(subAccount=sub_account, apiKey=api_key)
    if recv_window is not None:
      request['recvWindow'] = recv_window
    return await self.request(
      request,
      method='DELETE',
      path='/api/v3/sub-account/apiKey',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=DeleteApiKeyResponse,
    )
