# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.core import TimestampMillis
from typed_mexc.spot.core import SpotHttpEndpoint


class ApiKeyItem(TypedDict):
  """Sub-account API key record."""

  note: str | None
  """API key note."""
  apiKey: str | None
  """API public key."""
  permissions: str | None
  """API key permissions."""
  ip: str | None
  """API key IP allowlist."""
  creatTime: TimestampMillis | None
  """API key creation time as spelled by the upstream docs."""


class Request(TypedDict):
  subAccount: str
  """Sub-account name whose API keys should be returned."""
  recvWindow: NotRequired[int]
  """Optional signed-request validity window in milliseconds."""


class ApiKeyResponse(TypedDict):
  """Sub-account API key wrapper."""

  subAccount: list[ApiKeyItem]
  """API keys for the requested sub-account."""


class ApiKey(SpotHttpEndpoint):
  """Returns API keys configured for a sub-account under the signed master account."""

  async def api_key(
    self,
    sub_account: str,
    *,
    recv_window: int | None = None,
    validate: bool | None = None,
  ) -> ApiKeyResponse:
    """Returns API keys configured for a sub-account under the signed master account.

    Args:
      sub_account: Sub-account name whose API keys should be returned.
      recv_window: Optional signed-request validity window in milliseconds.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/subaccount-endpoints/query-the-apikey-of-a-sub-account-for-master-account)
    """
    request: Request = Request(subAccount=sub_account)
    if recv_window is not None:
      request['recvWindow'] = recv_window
    return await self.request(
      request,
      method='GET',
      path='/api/v3/sub-account/apiKey',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ApiKeyResponse,
    )
