# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.core import TimestampMillis
from typed_mexc.spot.core import SpotHttpEndpoint


class CreateApiKeyResponse(TypedDict):
  """Created sub-account API key."""

  subAccount: str | None
  """Sub-account name."""
  note: str | None
  """API key note."""
  apiKey: str | None
  """API public key."""
  secretKey: str | None
  """API secret key returned at creation time."""
  permissions: str | None
  """API key permissions."""
  ip: str | None
  """API key IP allowlist."""
  creatTime: TimestampMillis | None
  """API key creation time as spelled by the upstream docs."""


class Request(TypedDict):
  subAccount: str
  """Sub-account name for which to create the API key."""
  note: str
  """API key note."""
  permissions: str
  """Comma-separated API key permissions, such as SPOT_ACCOUNT_READ or SPOT_DEAL_WRITE."""
  ip: NotRequired[str]
  """Optional comma-separated IP allowlist. The official docs allow up to 20 addresses."""
  recvWindow: NotRequired[int]
  """Optional signed-request validity window in milliseconds."""


class CreateApiKey(SpotHttpEndpoint):
  """Creates an API key for a sub-account under the signed master account."""

  async def create_api_key(
    self,
    *,
    sub_account: str,
    note: str,
    permissions: str,
    ip: str | None = None,
    recv_window: int | None = None,
    validate: bool | None = None,
  ) -> CreateApiKeyResponse:
    """Creates an API key for a sub-account under the signed master account.

    Args:
      sub_account: Sub-account name for which to create the API key.
      note: API key note.
      permissions: Comma-separated API key permissions, such as SPOT_ACCOUNT_READ or SPOT_DEAL_WRITE.
      ip: Optional comma-separated IP allowlist. The official docs allow up to 20 addresses.
      recv_window: Optional signed-request validity window in milliseconds.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/subaccount-endpoints/create-an-apikey-for-a-sub-account-for-master-account)
    """
    request: Request = Request(
      subAccount=sub_account, note=note, permissions=permissions
    )
    if ip is not None:
      request['ip'] = ip
    if recv_window is not None:
      request['recvWindow'] = recv_window
    return await self.request(
      request,
      method='POST',
      path='/api/v3/sub-account/apiKey',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CreateApiKeyResponse,
    )
