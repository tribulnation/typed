# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_mexc.spot.core import SpotHttpEndpoint


class Request(TypedDict):
  fromAccount: NotRequired[str]
  """Source account name. Defaults to the master account when omitted."""
  toAccount: NotRequired[str]
  """Destination account name. Defaults to the master account when omitted."""
  fromAccountType: str
  """Source account type, documented as SPOT or FUTURES."""
  toAccountType: str
  """Destination account type, documented as SPOT or FUTURES."""
  asset: str
  """Asset symbol to transfer."""
  amount: Decimal
  """Amount of the asset to transfer."""


class TransferResponse(TypedDict):
  """Sub-account transfer result."""

  tranId: str | int | None
  """Transfer id."""


class Transfer(SpotHttpEndpoint):
  """Transfers an asset between the master account and a sub-account."""

  async def transfer(
    self,
    *,
    from_account: str | None = None,
    to_account: str | None = None,
    from_account_type: str,
    to_account_type: str,
    asset: str,
    amount: Decimal,
    validate: bool | None = None,
  ) -> TransferResponse:
    """Transfers an asset between the master account and a sub-account.

    Args:
      from_account: Source account name. Defaults to the master account when omitted.
      to_account: Destination account name. Defaults to the master account when omitted.
      from_account_type: Source account type, documented as SPOT or FUTURES.
      to_account_type: Destination account type, documented as SPOT or FUTURES.
      asset: Asset symbol to transfer.
      amount: Amount of the asset to transfer.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/subaccount-endpoints/universal-transfer-for-master-account)
    """
    request: Request = Request(
      fromAccountType=from_account_type,
      toAccountType=to_account_type,
      asset=asset,
      amount=amount,
    )
    if from_account is not None:
      request['fromAccount'] = from_account
    if to_account is not None:
      request['toAccount'] = to_account
    return await self.request(
      request,
      method='POST',
      path='/api/v3/capital/sub-account/universalTransfer',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=TransferResponse,
    )
