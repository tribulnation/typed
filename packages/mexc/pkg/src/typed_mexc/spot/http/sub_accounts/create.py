# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.spot.core import SpotHttpEndpoint


class CreateResponse(TypedDict):
  """Created sub-account record."""

  subAccount: str | None
  """Created sub-account name."""
  note: str | None
  """Sub-account note."""


class Request(TypedDict):
  subAccount: str
  """Name to assign to the new sub-account."""
  note: str
  """Operator note stored with the sub-account."""
  recvWindow: NotRequired[int]
  """Optional signed-request validity window in milliseconds."""


class Create(SpotHttpEndpoint):
  """Creates a new sub-account under the signed master account."""

  async def create(
    self,
    *,
    sub_account: str,
    note: str,
    recv_window: int | None = None,
    validate: bool | None = None,
  ) -> CreateResponse:
    """Creates a new sub-account under the signed master account.

    Args:
      sub_account: Name to assign to the new sub-account.
      note: Operator note stored with the sub-account.
      recv_window: Optional signed-request validity window in milliseconds.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/subaccount-endpoints/create-a-sub-accountfor-master-account)
    """
    request: Request = Request(subAccount=sub_account, note=note)
    if recv_window is not None:
      request['recvWindow'] = recv_window
    return await self.request(
      request,
      method='POST',
      path='/api/v3/sub-account/virtualSubAccount',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CreateResponse,
    )
