# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_mexc.spot.core import SpotHttpEndpoint


class LimitOrderRequest(TypedDict):
  """Limit-family spot order: LIMIT, LIMIT_MAKER, IMMEDIATE_OR_CANCEL, or FILL_OR_KILL. Requires both a price and a base-asset quantity."""

  symbol: str
  """Spot symbol."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['LIMIT', 'LIMIT_MAKER', 'IMMEDIATE_OR_CANCEL', 'FILL_OR_KILL']
  """Order type."""
  quantity: Decimal
  """Base-asset quantity."""
  price: Decimal
  """Limit price."""
  newClientOrderId: NotRequired[str]
  """Client order id."""
  recvWindow: NotRequired[int]
  """Signed request validity window in milliseconds."""


class MarketOrderByQuantityRequest(TypedDict):
  """Market spot order sized by base-asset quantity: sells exactly `quantity` of the base asset at market price."""

  symbol: str
  """Spot symbol."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['MARKET']
  """Order type."""
  quantity: Decimal
  """Base-asset quantity to trade at market price."""
  newClientOrderId: NotRequired[str]
  """Client order id."""
  recvWindow: NotRequired[int]
  """Signed request validity window in milliseconds."""


class MarketOrderByQuoteQtyRequest(TypedDict):
  """Market spot order sized by quote-asset amount: spends (buy) or receives (sell) exactly `quoteOrderQty` of the quote asset at market price."""

  symbol: str
  """Spot symbol."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['MARKET']
  """Order type."""
  quoteOrderQty: Decimal
  """Quote-asset amount to trade at market price."""
  newClientOrderId: NotRequired[str]
  """Client order id."""
  recvWindow: NotRequired[int]
  """Signed request validity window in milliseconds."""


class TestOrderResponse(TypedDict):
  """Empty response when validation succeeds."""


Request = (
  LimitOrderRequest | MarketOrderByQuantityRequest | MarketOrderByQuoteQtyRequest
)


class TestOrder(SpotHttpEndpoint):
  """Validates a new order without sending it to the matching engine."""

  async def test_order(
    self,
    request: Request,
    *,
    validate: bool | None = None,
  ) -> TestOrderResponse:
    """Validates a new order without sending it to the matching engine.

    Args:
      request:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/spot-account-trade/test-new-order)
    """
    return await self.request(
      request,
      method='POST',
      path='/api/v3/order/test',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=TestOrderResponse,
    )
