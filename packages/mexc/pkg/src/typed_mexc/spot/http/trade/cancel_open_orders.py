# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_mexc.spot.core import SpotHttpEndpoint


class CancelOpenOrdersItem(TypedDict):
  """Item."""

  clientOrderId: str
  """Client order id."""
  cummulativeQuoteQty: Decimal
  """Executed quote quantity."""
  executedQty: Decimal
  """Executed quantity."""
  orderId: int
  """MEXC order id."""
  orderListId: int
  """Order-list id."""
  origClientOrderId: str
  """Client order id of the canceled order."""
  origQty: Decimal
  """Original quantity."""
  price: Decimal
  """Order price."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  status: Literal['NEW', 'PARTIALLY_FILLED', 'FILLED', 'CANCELED', 'PARTIALLY_CANCELED']
  """Order status."""
  symbol: str
  """Spot symbol."""
  timeInForce: Literal['GTC', 'IOC', 'FOK']
  """Time in force."""
  type: Literal['LIMIT', 'MARKET', 'LIMIT_MAKER', 'IMMEDIATE_OR_CANCEL', 'FILL_OR_KILL']
  """Order type."""


class Request(TypedDict):
  symbol: str
  """One symbol or up to five comma-separated symbols."""
  recvWindow: NotRequired[int]
  """Optional receive window in milliseconds; maximum 60000."""


Response = list[CancelOpenOrdersItem]


class CancelOpenOrders(SpotHttpEndpoint):
  """Cancels pending orders for one or more symbols on the signed account."""

  async def cancel_open_orders(
    self,
    symbol: str,
    *,
    recv_window: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Cancels pending orders for one or more symbols on the signed account.

    Args:
      symbol: One symbol or up to five comma-separated symbols.
      recv_window: Optional receive window in milliseconds; maximum 60000.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/spot-account-trade/cancel-all-open-orders-on-a-symbol)
    """
    request: Request = Request(symbol=symbol)
    if recv_window is not None:
      request['recvWindow'] = recv_window
    return await self.request(
      request,
      method='DELETE',
      path='/api/v3/openOrders',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
