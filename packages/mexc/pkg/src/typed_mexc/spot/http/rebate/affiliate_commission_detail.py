# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_mexc.core import TimestampMillis
from typed_mexc.spot.core import SpotHttpEndpoint


class AffiliateCommissionDetailItem(TypedDict):
  """Affiliate record."""

  type: Literal[1, 2, 3]
  """Commission type: 1 spot, 2 futures, 3 ETF."""
  sourceType: int
  """Source type: 1 referral, 2 sub-affiliate."""
  state: int
  """Commission state."""
  date: TimestampMillis
  """Trade date."""
  uid: str
  """User id."""
  rate: float | str
  """Commission rate."""
  symbol: str
  """Trading symbol."""
  takerAmount: Decimal
  """Taker trade amount."""
  makerAmount: Decimal
  """Maker trade amount."""
  amountCurrency: str
  """Trade amount currency."""
  usdtAmount: Decimal
  """USDT trade amount."""
  commission: Decimal
  """Commission amount."""
  currency: str
  """Commission currency."""


class Request(TypedDict):
  startTime: NotRequired[TimestampMillis]
  """Start time in milliseconds."""
  endTime: NotRequired[TimestampMillis]
  """End time in milliseconds."""
  inviteCode: NotRequired[str]
  """Invite code filter."""
  page: NotRequired[int]
  """Result page."""
  pageSize: NotRequired[int]
  """Records per page; defaults to 10."""
  type: NotRequired[Literal[1, 2, 3]]
  """Commission type: 1 spot, 2 futures, 3 ETF."""


class AffiliateCommissionDetailData(TypedDict):
  """Paginated affiliate data."""

  pageSize: int
  """Number of records requested per page."""
  totalCount: int
  """Total number of matching records."""
  totalPage: int
  """Total number of result pages."""
  currentPage: int
  """Current result page."""
  resultList: list[AffiliateCommissionDetailItem]
  """Affiliate records for the page."""
  totalCommissionUsdtAmount: Decimal
  """Total commission in USDT."""
  totalTradeUsdtAmount: Decimal
  """Total trade volume in USDT."""


class AffiliateCommissionDetailResponse(TypedDict):
  """Affiliate wrapper response."""

  success: bool
  """Whether the request succeeded."""
  code: int
  """Business response code."""
  message: str | None
  """Business response message."""
  data: AffiliateCommissionDetailData


class AffiliateCommissionDetail(SpotHttpEndpoint):
  """Affiliate-only endpoint returning detailed commission records by type, source, date, user, and asset."""

  async def affiliate_commission_detail_paged(
    self,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    invite_code: str | None = None,
    page_size: int | None = None,
    type: Literal[1, 2, 3] | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[AffiliateCommissionDetailResponse]:
    """Affiliate-only endpoint returning detailed commission records by type, source, date, user, and asset.

    Paged variant of `affiliate_commission_detail`: Requests `page` from 1 upwards and stops once it has covered the `data.totalPage` pages the response reports, or after `max_pages` pages when one is given.

    Args:
      start_time: Start time in milliseconds.
      end_time: End time in milliseconds.
      invite_code: Invite code filter.
      page_size: Records per page; defaults to 10.
      type: Commission type: 1 spot, 2 futures, 3 ETF.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/rebate-endpoints/get-affiliate-commission-detail-record-affiliate-only)
    """
    page = 1
    pages = 0
    while True:
      response = await self.affiliate_commission_detail(
        start_time=start_time,
        end_time=end_time,
        invite_code=invite_code,
        page_size=page_size,
        type=type,
        page=page,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      total_0 = response.get('data') if response is not None else None
      total = total_0.get('totalPage') if total_0 is not None else None
      total = int(total) if total is not None else None
      if total is None or pages >= total:
        break
      page += 1

  async def affiliate_commission_detail(
    self,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    invite_code: str | None = None,
    page: int | None = None,
    page_size: int | None = None,
    type: Literal[1, 2, 3] | None = None,
    validate: bool | None = None,
  ) -> AffiliateCommissionDetailResponse:
    """Affiliate-only endpoint returning detailed commission records by type, source, date, user, and asset.

    Args:
      start_time: Start time in milliseconds.
      end_time: End time in milliseconds.
      invite_code: Invite code filter.
      page: Result page.
      page_size: Records per page; defaults to 10.
      type: Commission type: 1 spot, 2 futures, 3 ETF.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/rebate-endpoints/get-affiliate-commission-detail-record-affiliate-only)
    """
    request: Request = Request()
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if invite_code is not None:
      request['inviteCode'] = invite_code
    if page is not None:
      request['page'] = page
    if page_size is not None:
      request['pageSize'] = page_size
    if type is not None:
      request['type'] = type
    return await self.request(
      request,
      method='GET',
      path='/api/v3/rebate/affiliate/commission/detail',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=AffiliateCommissionDetailResponse,
    )
