# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.spot.core import SpotHttpEndpoint


class ReferCodeResponse(TypedDict):
  """Referral code response."""

  referCode: str
  """Referral code for the signed account."""


class Request(TypedDict):
  recvWindow: NotRequired[int]
  """Optional receive window in milliseconds."""


class ReferCode(SpotHttpEndpoint):
  """Returns the referral code associated with the signed account."""

  async def refer_code(
    self,
    recv_window: int | None = None,
    *,
    validate: bool | None = None,
  ) -> ReferCodeResponse:
    """Returns the referral code associated with the signed account.

    Args:
      recv_window: Optional receive window in milliseconds.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/rebate-endpoints/query-refercode)
    """
    request: Request = Request()
    if recv_window is not None:
      request['recvWindow'] = recv_window
    return await self.request(
      request,
      method='GET',
      path='/api/v3/rebate/referCode',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ReferCodeResponse,
    )
