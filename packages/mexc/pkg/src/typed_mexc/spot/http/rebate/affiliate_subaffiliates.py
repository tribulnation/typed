# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typed_core import PaginatedResponse
from typing_extensions import NotRequired, Sequence, TypedDict
from typed_mexc.core import TimestampMillis
from typed_mexc.spot.core import SpotHttpEndpoint


class AffiliateSubaffiliatesItem(TypedDict):
  """Affiliate record."""

  subaffiliateName: str
  """Sub-affiliate display name."""
  subaffiliateMail: str
  """Masked sub-affiliate email."""
  campaign: str | None
  """Campaign name."""
  inviteCode: str
  """Invite code."""
  activationTime: TimestampMillis
  """Sub-affiliate activation time."""
  registered: int
  """Registered-user count."""
  deposited: int
  """Deposited-user count."""
  depositAmount: Decimal
  """Deposit amount."""
  commission: Decimal
  """Commission amount."""


class Request(TypedDict):
  startTime: NotRequired[TimestampMillis]
  """Start time in milliseconds."""
  endTime: NotRequired[TimestampMillis]
  """End time in milliseconds."""
  inviteCode: NotRequired[str]
  """Invite code filter."""
  page: NotRequired[int]
  """Result page."""
  pageSize: NotRequired[int]
  """Records per page; defaults to 10."""


class AffiliateSubaffiliatesData(TypedDict):
  """Paginated affiliate data."""

  pageSize: int
  """Number of records requested per page."""
  totalCount: int
  """Total number of matching records."""
  totalPage: int
  """Total number of result pages."""
  currentPage: int
  """Current result page."""
  resultList: list[AffiliateSubaffiliatesItem]
  """Affiliate records for the page."""


class AffiliateSubaffiliatesResponse(TypedDict):
  """Affiliate wrapper response."""

  success: bool
  """Whether the request succeeded."""
  code: int
  """Business response code."""
  message: str | None
  """Business response message."""
  data: AffiliateSubaffiliatesData


class AffiliateSubaffiliates(SpotHttpEndpoint):
  """Affiliate-only endpoint returning sub-affiliate activation, registration, deposit, campaign, and commission data."""

  def affiliate_subaffiliates_paged(
    self,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    invite_code: str | None = None,
    page_size: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[AffiliateSubaffiliatesItem, int]:
    """Affiliate-only endpoint returning sub-affiliate activation, registration, deposit, campaign, and commission data.

    Paged variant of `affiliate_subaffiliates`: Requests `page` from 1 upwards and stops once it has covered the `data.totalPage` pages the response reports, or on an empty page. Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      start_time: Start time in milliseconds.
      end_time: End time in milliseconds.
      invite_code: Invite code filter.
      page_size: Records per page; defaults to 10.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/rebate-endpoints/get-subaffiliates-data-affiliate-only)
    """

    async def next(
      page: int,
    ) -> tuple[Sequence[AffiliateSubaffiliatesItem], int | None]:
      response = await self.affiliate_subaffiliates(
        start_time=start_time,
        end_time=end_time,
        invite_code=invite_code,
        page_size=page_size,
        page=page,
        validate=validate,
      )
      rows_0 = response.get('data')
      rows = rows_0.get('resultList') if rows_0 is not None else None
      rows = rows if rows is not None else []
      total_0 = response.get('data')
      total = total_0.get('totalPage') if total_0 is not None else None
      total = int(total) if total is not None else None
      if not rows or (total is not None and (page - 1 + 1) >= total):
        return rows, None
      return rows, page + 1

    return PaginatedResponse(1, next)

  async def affiliate_subaffiliates(
    self,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    invite_code: str | None = None,
    page: int | None = None,
    page_size: int | None = None,
    validate: bool | None = None,
  ) -> AffiliateSubaffiliatesResponse:
    """Affiliate-only endpoint returning sub-affiliate activation, registration, deposit, campaign, and commission data.

    Args:
      start_time: Start time in milliseconds.
      end_time: End time in milliseconds.
      invite_code: Invite code filter.
      page: Result page.
      page_size: Records per page; defaults to 10.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/rebate-endpoints/get-subaffiliates-data-affiliate-only)
    """
    request: Request = Request()
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if invite_code is not None:
      request['inviteCode'] = invite_code
    if page is not None:
      request['page'] = page
    if page_size is not None:
      request['pageSize'] = page_size
    return await self.request(
      request,
      method='GET',
      path='/api/v3/rebate/affiliate/subaffiliates',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=AffiliateSubaffiliatesResponse,
    )
