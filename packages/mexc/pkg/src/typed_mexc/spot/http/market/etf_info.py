# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.schemas import EtfInfoItem
from typed_mexc.spot.core import SpotHttpEndpoint


class Request(TypedDict):
  symbol: NotRequired[str]
  """ETF symbol."""


Response = EtfInfoItem | list[EtfInfoItem]


class EtfInfo(SpotHttpEndpoint):
  """Return spot ETF information such as net value, leverage, and fund fee."""

  async def etf_info(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Return spot ETF information such as net value, leverage, and fund fee.

    Args:
      symbol: ETF symbol.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://mexcdevelop.github.io/apidocs/spot_v3_en/#etf-endpoints)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='/api/v3/etf/info',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
