# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_mexc.spot.core import SpotHttpEndpoint


class PingResponse(TypedDict):
  """Empty successful connectivity response."""


class Ping(SpotHttpEndpoint):
  """Test connectivity to the MEXC spot REST API."""

  async def ping(self, *, validate: bool | None = None) -> PingResponse:
    """Test connectivity to the MEXC spot REST API.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/market-data-endpoints/test-connectivity)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v3/ping',
      meta={},
      validate=validate,
      response_type=PingResponse,
    )
