# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_mexc.core import TimestampMillis
from typed_mexc.spot.core import SpotHttpEndpoint


class TimeResponse(TypedDict):
  serverTime: TimestampMillis
  """Server timestamp in milliseconds."""


class Time(SpotHttpEndpoint):
  """Return the current MEXC spot server time."""

  async def time(self, *, validate: bool | None = None) -> TimeResponse:
    """Return the current MEXC spot server time.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/market-data-endpoints/check-server-time)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v3/time',
      meta={},
      validate=validate,
      response_type=TimeResponse,
    )
