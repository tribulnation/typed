# Generated by typed-dev — see CONTRIBUTING.md.
from datetime import timedelta
from decimal import Decimal
from typing_extensions import AsyncIterator, NotRequired, TypedDict
from typed_mexc.core import TimestampMillis
from typed_mexc.spot.core import SpotHttpEndpoint


class AggTrade(TypedDict):
  a: int | str | None
  """Aggregate trade id; can be null."""
  f: int | str | None
  """First trade id; can be null."""
  l: int | str | None
  """Last trade id; can be null."""
  p: Decimal
  """Price."""
  q: Decimal
  """Quantity."""
  T: TimestampMillis
  """Trade timestamp in milliseconds."""
  m: bool
  """Whether buyer was maker."""
  M: bool
  """Whether this was the best match."""


class Request(TypedDict):
  symbol: str
  """Spot symbol."""
  limit: NotRequired[int]
  """Maximum number of aggregate trades."""
  startTime: NotRequired[TimestampMillis]
  """Start timestamp in milliseconds."""
  endTime: NotRequired[TimestampMillis]
  """End timestamp in milliseconds."""


Response = list[AggTrade]


class AggTrades(SpotHttpEndpoint):
  """Return aggregate public trades for a spot symbol."""

  async def agg_trades_paged(
    self,
    symbol: str,
    *,
    limit: int | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[Response]:
    """Return aggregate public trades for a spot symbol.

    Paged variant of `agg_trades`: Moves the `start_time`–`end_time` window backwards by its own width and stops on the first empty window, or after `max_pages` pages when one is given.

    Every request spans the width the caller's own `start_time` and `end_time` state, so choose a window the venue answers in one response: it caps a wider one, and the walk moves past the rows that were left out.

    Args:
      symbol: Spot symbol.
      limit: Maximum number of aggregate trades.
      start_time: Start timestamp in milliseconds.
      end_time: End timestamp in milliseconds.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/market-data-endpoints/compressedaggregate-trades-list)
    """
    if start_time is None or end_time is None:
      raise ValueError(
        '`agg_trades_paged` walks a time window: pass both `start_time` and `end_time`'
      )
    lower = start_time
    upper = end_time
    width = upper - lower
    pages = 0
    while True:
      response = await self.agg_trades(
        symbol, limit=limit, start_time=lower, end_time=upper, validate=validate
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      if not response:
        break
      upper = lower - timedelta(milliseconds=1)
      lower = upper - width

  async def agg_trades(
    self,
    symbol: str,
    *,
    limit: int | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Return aggregate public trades for a spot symbol.

    Args:
      symbol: Spot symbol.
      limit: Maximum number of aggregate trades.
      start_time: Start timestamp in milliseconds.
      end_time: End timestamp in milliseconds.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/market-data-endpoints/compressedaggregate-trades-list)
    """
    request: Request = Request(symbol=symbol)
    if limit is not None:
      request['limit'] = limit
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    return await self.request(
      request,
      method='GET',
      path='/api/v3/aggTrades',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
