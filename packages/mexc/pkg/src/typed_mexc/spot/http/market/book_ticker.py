# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.schemas import BookTicker
from typed_mexc.spot.core import SpotHttpEndpoint


class Request(TypedDict):
  symbol: NotRequired[str]
  """Spot symbol. If omitted, returns all symbols."""


Response = BookTicker | list[BookTicker]


class BookTickerEndpoint(SpotHttpEndpoint):
  """Return best bid and ask for one spot symbol or all symbols."""

  async def book_ticker(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Return best bid and ask for one spot symbol or all symbols.

    Args:
      symbol: Spot symbol. If omitted, returns all symbols.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/market-data-endpoints/symbol-order-book-ticker)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='/api/v3/ticker/bookTicker',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
