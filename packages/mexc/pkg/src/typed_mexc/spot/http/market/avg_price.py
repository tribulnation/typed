# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_mexc.spot.core import SpotHttpEndpoint


class AvgPriceResponse(TypedDict):
  mins: int
  """Average price window in minutes."""
  price: Decimal
  """Average price."""


class Request(TypedDict):
  symbol: str
  """Spot symbol."""


class AvgPrice(SpotHttpEndpoint):
  """Return current average price for a spot symbol."""

  async def avg_price(
    self, symbol: str, *, validate: bool | None = None
  ) -> AvgPriceResponse:
    """Return current average price for a spot symbol.

    Args:
      symbol: Spot symbol.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/market-data-endpoints/current-average-price)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/api/v3/avgPrice',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=AvgPriceResponse,
    )
