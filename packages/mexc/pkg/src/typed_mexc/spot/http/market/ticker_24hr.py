# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.schemas import Ticker24hrStats
from typed_mexc.spot.core import SpotHttpEndpoint


class Request(TypedDict):
  symbol: NotRequired[str]
  """Spot symbol. If omitted, returns all symbols."""


Response = Ticker24hrStats | list[Ticker24hrStats]


class Ticker24hr(SpotHttpEndpoint):
  """Return 24-hour ticker statistics for one spot symbol or all symbols."""

  async def ticker_24hr(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Return 24-hour ticker statistics for one spot symbol or all symbols.

    Args:
      symbol: Spot symbol. If omitted, returns all symbols.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/market-data-endpoints/api-24hr-ticker-price-change-statistics)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='/api/v3/ticker/24hr',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
