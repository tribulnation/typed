# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.core import TimestampMillis
from typed_mexc.spot.core import SpotHttpEndpoint


class OrderBook(TypedDict):
  lastUpdateId: int
  """Last update id."""
  bids: list[list[str]]
  """Bid price and quantity levels."""
  asks: list[list[str]]
  """Ask price and quantity levels."""
  timestamp: TimestampMillis
  """Snapshot timestamp in milliseconds."""


class Request(TypedDict):
  symbol: str
  """Spot symbol."""
  limit: NotRequired[int]
  """Number of bids and asks to return."""


class Depth(SpotHttpEndpoint):
  """Return a spot order book snapshot for a symbol."""

  async def depth(
    self,
    symbol: str,
    *,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> OrderBook:
    """Return a spot order book snapshot for a symbol.

    Args:
      symbol: Spot symbol.
      limit: Number of bids and asks to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/market-data-endpoints/order-book)
    """
    request: Request = Request(symbol=symbol)
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/api/v3/depth',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=OrderBook,
    )
