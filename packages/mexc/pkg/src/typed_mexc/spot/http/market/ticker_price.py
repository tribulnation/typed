# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.schemas import TickerPrice
from typed_mexc.spot.core import SpotHttpEndpoint


class Request(TypedDict):
  symbol: NotRequired[str]
  """Spot symbol. If omitted, returns all symbols."""


Response = TickerPrice | list[TickerPrice]


class TickerPriceEndpoint(SpotHttpEndpoint):
  """Return latest price for one spot symbol or all symbols."""

  async def ticker_price(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Return latest price for one spot symbol or all symbols.

    Args:
      symbol: Spot symbol. If omitted, returns all symbols.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/market-data-endpoints/symbol-price-ticker)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='/api/v3/ticker/price',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
