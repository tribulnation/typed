# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.schemas import MxDeductResponse
from typed_mexc.spot.core import SpotHttpEndpoint


class Request(TypedDict):
  recvWindow: NotRequired[int]
  """Optional receive window in milliseconds; maximum 60000."""


class MxDeductStatus(SpotHttpEndpoint):
  """Returns whether MX deduction for spot commission fees is enabled."""

  async def mx_deduct_status(
    self,
    recv_window: int | None = None,
    *,
    validate: bool | None = None,
  ) -> MxDeductResponse:
    """Returns whether MX deduction for spot commission fees is enabled.

    Args:
      recv_window: Optional receive window in milliseconds; maximum 60000.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/spot-account-trade/query-mx-deduct-status)
    """
    request: Request = Request()
    if recv_window is not None:
      request['recvWindow'] = recv_window
    return await self.request(
      request,
      method='GET',
      path='/api/v3/mxDeduct/enable',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=MxDeductResponse,
    )
