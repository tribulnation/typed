# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_mexc.spot.core import SpotHttpEndpoint


class KycStatusResponse(TypedDict):
  """KYC status response."""

  status: Literal['1', '2', '3', '4']
  """Verification tier: 1 unverified, 2 primary, 3 advanced, 4 institutional."""


class Request(TypedDict):
  recvWindow: NotRequired[int]
  """Optional receive window in milliseconds; maximum 60000."""


class KycStatus(SpotHttpEndpoint):
  """Returns the account KYC verification tier for the signed spot account."""

  async def kyc_status(
    self,
    recv_window: int | None = None,
    *,
    validate: bool | None = None,
  ) -> KycStatusResponse:
    """Returns the account KYC verification tier for the signed spot account.

    Args:
      recv_window: Optional receive window in milliseconds; maximum 60000.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/spot-account-trade/query-kyc-status)
    """
    request: Request = Request()
    if recv_window is not None:
      request['recvWindow'] = recv_window
    return await self.request(
      request,
      method='GET',
      path='/api/v3/kyc/status',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=KycStatusResponse,
    )
