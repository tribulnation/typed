# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.schemas import MxDeductResponse
from typed_mexc.spot.core import SpotHttpEndpoint


class Request(TypedDict):
  mxDeductEnable: bool
  """Set true to enable MX deduction, false to disable it."""
  recvWindow: NotRequired[int]
  """Optional receive window in milliseconds; maximum 60000."""


class MxDeductEnable(SpotHttpEndpoint):
  """Enables or disables MX deduction for spot commission fees."""

  async def mx_deduct_enable(
    self,
    mx_deduct_enable: bool,
    *,
    recv_window: int | None = None,
    validate: bool | None = None,
  ) -> MxDeductResponse:
    """Enables or disables MX deduction for spot commission fees.

    Args:
      mx_deduct_enable: Set true to enable MX deduction, false to disable it.
      recv_window: Optional receive window in milliseconds; maximum 60000.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/spot-account-trade/enable-mx-deduct)
    """
    request: Request = Request(mxDeductEnable=mx_deduct_enable)
    if recv_window is not None:
      request['recvWindow'] = recv_window
    return await self.request(
      request,
      method='POST',
      path='/api/v3/mxDeduct/enable',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=MxDeductResponse,
    )
