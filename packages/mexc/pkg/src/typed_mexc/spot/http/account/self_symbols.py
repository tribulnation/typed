# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.schemas import SymbolsResponse
from typed_mexc.spot.core import SpotHttpEndpoint


class Request(TypedDict):
  recvWindow: NotRequired[int]
  """Optional receive window in milliseconds; maximum 60000."""


class SelfSymbols(SpotHttpEndpoint):
  """Returns the symbols enabled for the signed API key."""

  async def self_symbols(
    self,
    recv_window: int | None = None,
    *,
    validate: bool | None = None,
  ) -> SymbolsResponse:
    """Returns the symbols enabled for the signed API key.

    Args:
      recv_window: Optional receive window in milliseconds; maximum 60000.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/spot-account-trade/user-api-default-symbol)
    """
    request: Request = Request()
    if recv_window is not None:
      request['recvWindow'] = recv_window
    return await self.request(
      request,
      method='GET',
      path='/api/v3/selfSymbols',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SymbolsResponse,
    )
