# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_mexc.core import TimestampMillis
from typed_mexc.spot.core import SpotHttpEndpoint


class Request(TypedDict):
  tranId: str
  """Transfer id."""


class UniversalTransferByTranIdResponse(TypedDict):
  """Universal transfer detail."""

  tranId: str | None
  """Transfer id."""
  clientTranId: str | None
  """Client transfer id."""
  asset: str | None
  """Asset."""
  amount: Decimal | None
  """Amount."""
  fromAccountType: str | None
  """Source account type."""
  toAccountType: str | None
  """Destination account type."""
  symbol: str | None
  """Symbol."""
  status: str | None
  """Transfer status."""
  timestamp: TimestampMillis | None
  """Transfer timestamp."""


class UniversalTransferByTranId(SpotHttpEndpoint):
  """Returns a single universal transfer record by transfer id."""

  async def universal_transfer_by_tran_id(
    self,
    tran_id: str,
    *,
    validate: bool | None = None,
  ) -> UniversalTransferByTranIdResponse:
    """Returns a single universal transfer record by transfer id.

    Args:
      tran_id: Transfer id.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/wallet-endpoints/query-user-universal-transfer-history-by-tranid)
    """
    return await self.request(
      Request(tranId=tran_id),
      method='GET',
      path='/api/v3/capital/transfer/tranId',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=UniversalTransferByTranIdResponse,
    )
