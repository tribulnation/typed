# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_mexc.spot.core import SpotHttpEndpoint


class ConvertibleAssetsItem(TypedDict):
  """Convertible asset."""

  convertMx: Decimal | None
  """Estimated MX amount after fee."""
  convertUsdt: Decimal | None
  """Estimated USDT amount."""
  balance: Decimal | None
  """Convertible balance."""
  asset: str | None
  """Asset."""
  code: str | None
  """Per-asset code."""
  message: str | None
  """Per-asset message."""


Response = list[ConvertibleAssetsItem]


class ConvertibleAssets(SpotHttpEndpoint):
  """Returns dust balances and estimated MX/USDT conversion values for assets that can be converted."""

  async def convertible_assets(self, *, validate: bool | None = None) -> Response:
    """Returns dust balances and estimated MX/USDT conversion values for assets that can be converted.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/wallet-endpoints/get-assets-that-can-be-converted-into-mx)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v3/capital/convert/list',
      meta={'signed': True},
      validate=validate,
      response_type=Response,
    )
