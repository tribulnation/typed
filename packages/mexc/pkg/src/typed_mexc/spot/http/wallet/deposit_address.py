# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.spot.core import SpotHttpEndpoint


class DepositAddressItem(TypedDict):
  """Deposit address."""

  coin: str | None
  """Asset."""
  network: str | None
  """Network."""
  address: str | None
  """Deposit address."""
  memo: str | None
  """Deposit memo."""


class Request(TypedDict):
  coin: str
  """Asset for deposit address lookup."""
  network: NotRequired[str]
  """Optional deposit network filter."""


Response = list[DepositAddressItem]


class DepositAddress(SpotHttpEndpoint):
  """Returns deposit addresses for an asset, optionally filtered by network."""

  async def deposit_address(
    self,
    *,
    coin: str,
    network: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Returns deposit addresses for an asset, optionally filtered by network.

    Args:
      coin: Asset for deposit address lookup.
      network: Optional deposit network filter.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/wallet-endpoints/deposit-address-supporting-network)
    """
    request: Request = Request(coin=coin)
    if network is not None:
      request['network'] = network
    return await self.request(
      request,
      method='GET',
      path='/api/v3/capital/deposit/address',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
