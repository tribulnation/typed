# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.spot.core import SpotHttpEndpoint


class Request(TypedDict):
  coin: str
  """Asset to withdraw."""
  withdrawOrderId: NotRequired[str]
  """Optional client withdrawal order id."""
  network: NotRequired[str]
  """Withdrawal network identifier."""
  address: str
  """Destination withdrawal address."""
  memo: NotRequired[str]
  """Destination memo or tag when required."""
  amount: str
  """Withdrawal amount."""
  remark: NotRequired[str]
  """Optional withdrawal remark."""


class WithdrawApplyItem(TypedDict):
  """Withdrawal application result."""

  id: str | None
  """Withdrawal id."""


Response = list[WithdrawApplyItem]


class WithdrawApply(SpotHttpEndpoint):
  """Deprecated previous withdrawal endpoint that submits a live asset withdrawal request."""

  async def withdraw_apply(
    self,
    *,
    coin: str,
    withdraw_order_id: str | None = None,
    network: str | None = None,
    address: str,
    memo: str | None = None,
    amount: str,
    remark: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Deprecated previous withdrawal endpoint that submits a live asset withdrawal request.

    Args:
      coin: Asset to withdraw.
      withdraw_order_id: Optional client withdrawal order id.
      network: Withdrawal network identifier.
      address: Destination withdrawal address.
      memo: Destination memo or tag when required.
      amount: Withdrawal amount.
      remark: Optional withdrawal remark.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/wallet-endpoints/withdrawpreviousoffline-soon)
    """
    request: Request = Request(coin=coin, address=address, amount=amount)
    if withdraw_order_id is not None:
      request['withdrawOrderId'] = withdraw_order_id
    if network is not None:
      request['network'] = network
    if memo is not None:
      request['memo'] = memo
    if remark is not None:
      request['remark'] = remark
    return await self.request(
      request,
      method='POST',
      path='/api/v3/capital/withdraw/apply',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
