# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.spot.core import SpotHttpEndpoint


class GenerateDepositAddressItem(TypedDict):
  """Deposit address."""

  coin: str | None
  """Asset."""
  network: str | None
  """Network."""
  address: str | None
  """Deposit address."""
  tag: NotRequired[str | None]
  """Deposit address tag."""
  memo: NotRequired[str | None]
  """Deposit memo."""


class Request(TypedDict):
  coin: str
  """Asset for the deposit address."""
  network: str
  """Deposit network."""


Response = list[GenerateDepositAddressItem]


class GenerateDepositAddress(SpotHttpEndpoint):
  """Creates or returns a deposit address for a coin and network."""

  async def generate_deposit_address(
    self,
    *,
    coin: str,
    network: str,
    validate: bool | None = None,
  ) -> Response:
    """Creates or returns a deposit address for a coin and network.

    Args:
      coin: Asset for the deposit address.
      network: Deposit network.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/wallet-endpoints/generate-deposit-address-supporting-network)
    """
    return await self.request(
      Request(coin=coin, network=network),
      method='POST',
      path='/api/v3/capital/deposit/address',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
