# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.spot.core import SpotHttpEndpoint


class ConvertDustItem(TypedDict):
  """Failed conversion."""

  asset: NotRequired[str | None]
  """Asset."""
  message: NotRequired[str | None]
  """Failure message."""
  code: NotRequired[str | None]
  """Failure code."""


class Request(TypedDict):
  asset: str
  """Comma-separated assets to convert; max 15 assets."""


class ConvertDustResponse(TypedDict):
  """Dust conversion response."""

  successList: list[str]
  """Successfully converted assets."""
  failedList: list[ConvertDustItem]
  """Failed conversion entries."""
  totalConvert: str | None
  """Total converted MX amount after fee."""
  convertFee: str | None
  """Conversion fee."""


class ConvertDust(SpotHttpEndpoint):
  """Converts small asset balances into MX."""

  async def convert_dust(
    self,
    asset: str,
    *,
    validate: bool | None = None,
  ) -> ConvertDustResponse:
    """Converts small asset balances into MX.

    Args:
      asset: Comma-separated assets to convert; max 15 assets.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/wallet-endpoints/dust-transfer)
    """
    return await self.request(
      Request(asset=asset),
      method='POST',
      path='/api/v3/capital/convert',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ConvertDustResponse,
    )
