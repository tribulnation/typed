# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.spot.core import SpotHttpEndpoint


class InternalTransferResponse(TypedDict):
  """Internal transfer response."""

  tranId: str | None
  """Transfer id."""


class Request(TypedDict):
  toAccountType: str
  """Recipient account identifier type: EMAIL, UID, or MOBILE."""
  toAccount: str
  """Recipient email, UID, or mobile number."""
  areaCode: NotRequired[str]
  """Area code when using a mobile recipient."""
  asset: str
  """Asset to transfer."""
  amount: str
  """Transfer amount."""


class InternalTransfer(SpotHttpEndpoint):
  """Transfers assets internally to another MEXC account by email, UID, or mobile number."""

  async def internal_transfer(
    self,
    *,
    to_account_type: str,
    to_account: str,
    area_code: str | None = None,
    asset: str,
    amount: str,
    validate: bool | None = None,
  ) -> InternalTransferResponse:
    """Transfers assets internally to another MEXC account by email, UID, or mobile number.

    Args:
      to_account_type: Recipient account identifier type: EMAIL, UID, or MOBILE.
      to_account: Recipient email, UID, or mobile number.
      area_code: Area code when using a mobile recipient.
      asset: Asset to transfer.
      amount: Transfer amount.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/wallet-endpoints/internal-transfer)
    """
    request: Request = Request(
      toAccountType=to_account_type, toAccount=to_account, asset=asset, amount=amount
    )
    if area_code is not None:
      request['areaCode'] = area_code
    return await self.request(
      request,
      method='POST',
      path='/api/v3/capital/transfer/internal',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=InternalTransferResponse,
    )
