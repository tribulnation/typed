# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_mexc.spot.core import SpotHttpEndpoint


class Request(TypedDict):
  fromAccountType: str
  """Source account type, such as SPOT or FUTURES."""
  toAccountType: str
  """Destination account type, such as SPOT or FUTURES."""
  asset: str
  """Asset to transfer."""
  amount: str
  """Transfer amount."""


class UniversalTransferItem(TypedDict):
  """Universal transfer result."""

  tranId: str | None
  """Transfer id."""


Response = list[UniversalTransferItem]


class UniversalTransfer(SpotHttpEndpoint):
  """Transfers assets between the signed user account types such as spot and futures."""

  async def universal_transfer(
    self,
    *,
    from_account_type: str,
    to_account_type: str,
    asset: str,
    amount: str,
    validate: bool | None = None,
  ) -> Response:
    """Transfers assets between the signed user account types such as spot and futures.

    Args:
      from_account_type: Source account type, such as SPOT or FUTURES.
      to_account_type: Destination account type, such as SPOT or FUTURES.
      asset: Asset to transfer.
      amount: Transfer amount.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/wallet-endpoints/user-universal-transfer)
    """
    return await self.request(
      Request(
        fromAccountType=from_account_type,
        toAccountType=to_account_type,
        asset=asset,
        amount=amount,
      ),
      method='POST',
      path='/api/v3/capital/transfer',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
