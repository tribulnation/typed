# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_mexc.spot.core import SpotHttpEndpoint


class CancelWithdrawResponse(TypedDict):
  """Withdrawal cancellation response."""

  id: str | None
  """Canceled withdrawal id."""


class Request(TypedDict):
  id: str
  """Withdrawal id to cancel."""


class CancelWithdraw(SpotHttpEndpoint):
  """Cancels a pending withdrawal by withdrawal id."""

  async def cancel_withdraw(
    self,
    id: str,
    *,
    validate: bool | None = None,
  ) -> CancelWithdrawResponse:
    """Cancels a pending withdrawal by withdrawal id.

    Args:
      id: Withdrawal id to cancel.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/wallet-endpoints/cancel-withdraw)
    """
    return await self.request(
      Request(id=id),
      method='DELETE',
      path='/api/v3/capital/withdraw',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CancelWithdrawResponse,
    )
