# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_mexc.core import TimestampMillis
from typed_mexc.spot.core import SpotHttpEndpoint


class Request(TypedDict):
  fromAccountType: str
  """Source account type filter."""
  toAccountType: str
  """Destination account type filter."""
  startTime: NotRequired[TimestampMillis]
  """Start time in milliseconds."""
  endTime: NotRequired[TimestampMillis]
  """End time in milliseconds."""
  page: NotRequired[int]
  """Page number; default 1."""
  size: NotRequired[int]
  """Page size; max 100."""


class UniversalTransferRecord(TypedDict):
  """Transfer row."""

  tranId: str | None
  """Transfer id."""
  clientTranId: str | None
  """Client transfer id."""
  asset: str | None
  """Asset."""
  amount: Decimal | None
  """Amount."""
  fromAccountType: str | None
  """Source account type."""
  toAccountType: str | None
  """Destination account type."""
  fromSymbol: str | None
  """Source symbol."""
  toSymbol: str | None
  """Destination symbol."""
  status: str | None
  """Transfer status."""
  timestamp: TimestampMillis | None
  """Transfer timestamp."""


class UniversalTransferHistoryPage(TypedDict):
  """Universal transfer history page."""

  rows: list[UniversalTransferRecord]
  """Transfer rows."""
  total: int | None
  """Total records."""


Response = list[UniversalTransferHistoryPage]


class UniversalTransferHistory(SpotHttpEndpoint):
  """Returns universal transfer records between account types for the signed account."""

  async def universal_transfer_history(
    self,
    *,
    from_account_type: str,
    to_account_type: str,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    page: int | None = None,
    size: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Returns universal transfer records between account types for the signed account.

    Args:
      from_account_type: Source account type filter.
      to_account_type: Destination account type filter.
      start_time: Start time in milliseconds.
      end_time: End time in milliseconds.
      page: Page number; default 1.
      size: Page size; max 100.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/spot-v3/wallet-endpoints/query-user-universal-transfer-history)
    """
    request: Request = Request(
      fromAccountType=from_account_type, toAccountType=to_account_type
    )
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if page is not None:
      request['page'] = page
    if size is not None:
      request['size'] = size
    return await self.request(
      request,
      method='GET',
      path='/api/v3/capital/transfer',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
