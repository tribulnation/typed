# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .http import Http
from .streams import Streams
from typed_mexc.spot.core import SpotBase


class Spot(SpotBase):
  """MEXC's Spot trading API — spot market data, spot account/wallet management, and spot order placement, plus their WebSocket counterparts.

  References:
    - [Upstream docs](https://www.mexc.com/api-docs/spot-v3/introduction)
  """

  @cached_property
  def http(self) -> Http:
    """MEXC Spot REST API as a whole -- public market data plus authenticated account/trade, rebate, sub-account, and wallet endpoints. Cites the Introduction page, the REST overview (base URL, request signing, rate limits) that applies across the whole surface; each product area has its own more specific page one or two levels down.

    References:
      - [Upstream docs](https://www.mexc.com/api-docs/spot-v3/introduction)
    """
    return Http(client=self.http_client)

  @cached_property
  def streams(self) -> Streams:
    """MEXC Spot WebSocket API as a whole -- Protocol-Buffers-framed public market streams and the authenticated private user-data streams (account, orders, deals, plus listen-key lifecycle). No single page covers both; reuses the Introduction page's link, which documents WebSocket connection limits alongside REST.

    References:
      - [Upstream docs](https://www.mexc.com/api-docs/spot-v3/introduction)
    """
    return Streams.new(self.streams_client)
