# Generated by typed-dev — see CONTRIBUTING.md.
from typed_mexc.types import BookTicker
from typed_mexc.spot.core import ErrorResponse, SpotMixin
from typed_mexc.core import validator

Response: type[BookTicker | list[BookTicker] | ErrorResponse] = (
  BookTicker | list[BookTicker] | ErrorResponse
)  # type: ignore
adapter = validator(Response)


class BookTickerEndpoint(SpotMixin):
  async def book_ticker(
    self,
    *,
    symbol: str | None = None,
    validate: bool | None = None,
  ) -> BookTicker | list[BookTicker]:
    """Return best bid and ask for one spot symbol or all symbols.

    Args:
      symbol: Spot symbol. If omitted, returns all symbols.
      validate: Validation override for this request.

    Returns:
      The validated endpoint response.

    References:
      - [MEXC API docs](https://mexcdevelop.github.io/apidocs/spot_v3_en/#symbol-order-book-ticker)
    """
    params = {}
    if symbol is not None:
      params['symbol'] = symbol
    r = await self.request('GET', '/api/v3/ticker/bookTicker', params=params)
    return self.output(r, adapter, validate)
