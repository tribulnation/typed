# Generated by typed-dev — see CONTRIBUTING.md.
from typed_mexc.types import EtfInfoItem
from typed_mexc.spot.core import ErrorResponse, SpotMixin
from typed_mexc.core import validator

Response: type[EtfInfoItem | list[EtfInfoItem] | ErrorResponse] = (
  EtfInfoItem | list[EtfInfoItem] | ErrorResponse
)  # type: ignore
adapter = validator(Response)


class EtfInfo(SpotMixin):
  async def etf_info(
    self,
    *,
    symbol: str | None = None,
    validate: bool | None = None,
  ) -> EtfInfoItem | list[EtfInfoItem]:
    """Return spot ETF information such as net value, leverage, and fund fee.

    Args:
      symbol: ETF symbol.
      validate: Validation override for this request.

    Returns:
      The validated endpoint response.

    References:
      - [MEXC API docs](https://mexcdevelop.github.io/apidocs/spot_v3_en/#etf-endpoints)
    """
    params = {}
    if symbol is not None:
      params['symbol'] = symbol
    r = await self.request('GET', '/api/v3/etf/info', params=params)
    return self.output(r, adapter, validate)
