# Generated by typed-dev — see CONTRIBUTING.md.
from typed_mexc.types import SpotTradeRow
from typed_mexc.spot.core import ErrorResponse, SpotMixin
from typed_mexc.core import validator

Response: type[list[SpotTradeRow] | ErrorResponse] = list[SpotTradeRow] | ErrorResponse  # type: ignore
adapter = validator(Response)


class Trades(SpotMixin):
  async def trades(
    self,
    *,
    symbol: str,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> list[SpotTradeRow]:
    """Return recent public trades for a spot symbol.

    Args:
      symbol: Spot symbol.
      limit: Maximum number of trades to return.
      validate: Validation override for this request.

    Returns:
      The validated endpoint response.

    References:
      - [MEXC API docs](https://mexcdevelop.github.io/apidocs/spot_v3_en/#recent-trades-list)
    """
    params = {}
    if symbol is not None:
      params['symbol'] = symbol
    if limit is not None:
      params['limit'] = limit
    r = await self.request('GET', '/api/v3/trades', params=params)
    return self.output(r, adapter, validate)
