# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .market import Market
from .user import User
from typed_mexc.spot.streams.core import SpotStreamsBase


class Streams(SpotStreamsBase):
  """MEXC Spot WebSocket API as a whole -- Protocol-Buffers-framed public market streams and the authenticated private user-data streams (account, orders, deals, plus listen-key lifecycle). No single page covers both; reuses the Introduction page's link, which documents WebSocket connection limits alongside REST.

  References:
    - [Upstream docs](https://www.mexc.com/api-docs/spot-v3/introduction)
  """

  @cached_property
  def market(self) -> Market:
    """Public Spot WebSocket market streams -- aggregated deals, aggregated and single-symbol book ticker, order book depth (incremental and limit), and klines, Protocol-Buffers-framed and requiring no authentication.

    References:
      - [Upstream docs](https://www.mexc.com/api-docs/spot-v3/websocket-market-streams/)
    """
    return Market(client=self.market_client)

  @cached_property
  def user(self) -> User:
    """Authenticated Spot WebSocket user-data streams -- account balance updates, order updates, and trade (deal) updates for the caller's own account. Requires a listen key (see the sibling `listen_keys` grouping) to authenticate the connection.

    References:
      - [Upstream docs](https://www.mexc.com/api-docs/spot-v3/websocket-user-data-streams/)
    """
    return User(client=self.user_client)
