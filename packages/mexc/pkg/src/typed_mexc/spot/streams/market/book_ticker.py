# Generated by typed-dev — see CONTRIBUTING.md.
from dataclasses import dataclass
from typed_core.util import StreamManager
from typed_core.ws.streams import Stream
from typed_mexc.spot.streams.core import Reply, StreamsMixin
from typed_mexc.spot.streams.core.proto import PublicAggreBookTickerV3Api
from typing_extensions import Literal


def channel_name(aggregation: Literal['100ms', '10ms'], symbol: str):
  return f'spot@public.aggre.bookTicker.v3.api.pb@{aggregation}@{symbol}'


@dataclass
class BookTicker(StreamsMixin):
  def book_ticker(
    self,
    aggregation: Literal['100ms', '10ms'],
    symbol: str,
  ) -> StreamManager[PublicAggreBookTickerV3Api, Reply, Reply]:
    """Subscribes to best bid and ask updates for one uppercase spot trading pair with a 100ms or 10ms aggregation cadence. Messages are protobuf-encoded PushDataV3ApiWrapper payloads whose publicAggreBookTicker body contains the current best bid and ask.

    Args:
      aggregation: Update aggregation cadence embedded in the channel name. MEXC documents `100ms` and `10ms`.
      symbol: Uppercase spot trading pair, for example `BTCUSDT`.

    References:
      - [MEXC API docs](https://mexcdevelop.github.io/apidocs/spot_v3_en/#individual-symbol-book-ticker-streams)
    """
    return StreamManager(lambda: self._book_ticker_impl(aggregation, symbol))

  async def _book_ticker_impl(
    self,
    aggregation: Literal['100ms', '10ms'],
    symbol: str,
  ) -> Stream[PublicAggreBookTickerV3Api, Reply, Reply]:
    stream = await self.subscribe(channel_name(aggregation, symbol))

    async def parsed_stream():
      async for proto in stream:
        if proto.public_aggre_book_ticker is not None:
          yield proto.public_aggre_book_ticker

    return Stream(
      reply=stream.reply, stream=parsed_stream(), unsubscribe=stream.unsubscribe
    )
