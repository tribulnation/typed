# Generated by typed-dev — see CONTRIBUTING.md.
from dataclasses import dataclass
from typed_core.util import StreamManager
from typed_core.ws.streams import Stream
from typed_mexc.spot.streams.core import Reply, StreamsMixin
from typed_mexc.spot.streams.core.proto import PublicLimitDepthsV3Api
from typing_extensions import Literal


def channel_name(symbol: str, level: Literal[5, 10, 20]):
  return f'spot@public.limit.depth.v3.api.pb@{symbol}@{level}'


@dataclass
class Depth(StreamsMixin):
  def depth(
    self,
    symbol: str,
    level: Literal[5, 10, 20],
  ) -> StreamManager[PublicLimitDepthsV3Api, Reply, Reply]:
    """Subscribes to limited-level spot order-book depth updates for one uppercase trading pair. Messages are protobuf-encoded PushDataV3ApiWrapper payloads whose publicLimitDepths body contains ask and bid levels.

    Args:
      symbol: Uppercase spot trading pair, for example `BTCUSDT`.
      level: Number of order-book levels per side pushed by the stream.

    References:
      - [MEXC API docs](https://mexcdevelop.github.io/apidocs/spot_v3_en/#partial-book-depth-streams)
    """
    return StreamManager(lambda: self._depth_impl(symbol, level))

  async def _depth_impl(
    self,
    symbol: str,
    level: Literal[5, 10, 20],
  ) -> Stream[PublicLimitDepthsV3Api, Reply, Reply]:
    stream = await self.subscribe(channel_name(symbol, level))

    async def parsed_stream():
      async for proto in stream:
        if proto.public_limit_depths is not None:
          yield proto.public_limit_depths

    return Stream(
      reply=stream.reply, stream=parsed_stream(), unsubscribe=stream.unsubscribe
    )
