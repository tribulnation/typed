# Generated by typed-dev — see CONTRIBUTING.md.
from dataclasses import dataclass
from typed_core.util import StreamManager
from typed_core.ws.streams import Stream
from typed_mexc.spot.streams.core import Reply, StreamsMixin
from typed_mexc.spot.streams.core.proto import PublicBookTickerBatchV3Api


def channel_name(symbol: str):
  return f'spot@public.bookTicker.batch.v3.api.pb@{symbol}'


@dataclass
class BookTickerBatch(StreamsMixin):
  def book_ticker_batch(
    self,
    symbol: str,
  ) -> StreamManager[PublicBookTickerBatchV3Api, Reply, Reply]:
    """Subscribes to batch aggregated best bid and ask updates for one uppercase spot trading pair. Messages are protobuf-encoded PushDataV3ApiWrapper payloads whose publicBookTickerBatch body contains one or more book-ticker items.

    Args:
      symbol: Uppercase spot trading pair, for example `BTCUSDT`.

    References:
      - [MEXC API docs](https://mexcdevelop.github.io/apidocs/spot_v3_en/#individual-symbol-book-ticker-streams-batch-aggregation)
    """
    return StreamManager(lambda: self._book_ticker_batch_impl(symbol))

  async def _book_ticker_batch_impl(
    self,
    symbol: str,
  ) -> Stream[PublicBookTickerBatchV3Api, Reply, Reply]:
    stream = await self.subscribe(channel_name(symbol))

    async def parsed_stream():
      async for proto in stream:
        if proto.public_book_ticker_batch is not None:
          yield proto.public_book_ticker_batch

    return Stream(
      reply=stream.reply, stream=parsed_stream(), unsubscribe=stream.unsubscribe
    )
