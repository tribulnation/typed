# Generated by typed-dev — see CONTRIBUTING.md.
from dataclasses import dataclass
from typed_core.util import StreamManager
from typed_core.ws.streams import Stream
from typed_mexc.spot.streams.core import Reply, UserStreamsMixin
from typed_mexc.spot.streams.core.proto import PrivateAccountV3Api


@dataclass
class Account(UserStreamsMixin):
  def account(self) -> StreamManager[PrivateAccountV3Api, Reply, Reply]:
    """Subscribes to private protobuf account balance updates over a listen-key authenticated spot WebSocket connection.

    References:
      - [MEXC API docs](https://mexcdevelop.github.io/apidocs/spot_v3_en/#spot-account-update)
    """
    return StreamManager(self._account_impl)

  async def _account_impl(self) -> Stream[PrivateAccountV3Api, Reply, Reply]:
    stream = await self.authed_subscribe('spot@private.account.v3.api.pb')

    async def parsed_stream():
      async for proto in stream:
        if proto.private_account is not None:
          yield proto.private_account

    return Stream(
      reply=stream.reply, stream=parsed_stream(), unsubscribe=stream.unsubscribe
    )
