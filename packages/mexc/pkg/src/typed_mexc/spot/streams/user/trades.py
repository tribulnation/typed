# Generated by typed-dev — see CONTRIBUTING.md.
from dataclasses import dataclass
from typed_core.util import StreamManager
from typed_core.ws.streams import Stream
from typed_mexc.spot.streams.core import Reply, UserStreamsMixin
from typed_mexc.spot.streams.core.proto import PrivateDealsV3Api


@dataclass
class Trades(UserStreamsMixin):
  def trades(self) -> StreamManager[PrivateDealsV3Api, Reply, Reply]:
    """Subscribes to private protobuf spot trade updates over a listen-key authenticated spot WebSocket connection.

    References:
      - [MEXC API docs](https://mexcdevelop.github.io/apidocs/spot_v3_en/#spot-account-deals)
    """
    return StreamManager(self._trades_impl)

  async def _trades_impl(self) -> Stream[PrivateDealsV3Api, Reply, Reply]:
    stream = await self.authed_subscribe('spot@private.deals.v3.api.pb')

    async def parsed_stream():
      async for proto in stream:
        if proto.private_deals is not None:
          yield proto.private_deals

    return Stream(
      reply=stream.reply, stream=parsed_stream(), unsubscribe=stream.unsubscribe
    )
