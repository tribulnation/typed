# Generated by typed-dev — see CONTRIBUTING.md.
from dataclasses import dataclass
from typed_core.util import StreamManager
from typed_core.ws.streams import Stream
from typed_mexc.spot.streams.core import Reply, UserStreamsMixin
from typed_mexc.spot.streams.core.proto import PrivateOrdersV3Api


@dataclass
class Orders(UserStreamsMixin):
  def orders(self) -> StreamManager[PrivateOrdersV3Api, Reply, Reply]:
    """Subscribes to private protobuf spot order updates over a listen-key authenticated spot WebSocket connection.

    References:
      - [MEXC API docs](https://mexcdevelop.github.io/apidocs/spot_v3_en/#spot-account-orders)
    """
    return StreamManager(self._orders_impl)

  async def _orders_impl(self) -> Stream[PrivateOrdersV3Api, Reply, Reply]:
    stream = await self.authed_subscribe('spot@private.orders.v3.api.pb')

    async def parsed_stream():
      async for proto in stream:
        if proto.private_orders is not None:
          yield proto.private_orders

    return Stream(
      reply=stream.reply, stream=parsed_stream(), unsubscribe=stream.unsubscribe
    )
