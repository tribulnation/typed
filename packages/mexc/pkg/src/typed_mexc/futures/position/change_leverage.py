# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_mexc.types import FuturesAckResponse
from typed_mexc.futures.core import AuthFuturesMixin
from typed_mexc.core import validator


class AdjustExistingPositionLeverage(TypedDict):
  """Changes leverage for an already-open position, identified by its `positionId`."""

  positionId: int
  """Existing position identifier."""
  leverage: int
  """Target leverage."""


class AdjustLeverageBySymbol(TypedDict):
  """Sets leverage before any position exists, so the margin mode, contract symbol, and position side must be given explicitly instead of a `positionId`."""

  leverage: int
  """Target leverage."""
  openType: Literal[1, 2]
  """Margin mode; 1 isolated, 2 cross."""
  symbol: str
  """Contract symbol."""
  positionType: Literal[1, 2]
  """Position side; 1 long, 2 short."""


adapter = validator(FuturesAckResponse)


class ChangeLeverage(AuthFuturesMixin):
  async def change_leverage(
    self,
    change_leverage_request: AdjustExistingPositionLeverage | AdjustLeverageBySymbol,
    *,
    validate: bool | None = None,
  ) -> FuturesAckResponse:
    """Changes leverage either for an existing position or for a symbol/open-type/side when no position exists.

    Args:
      change_leverage_request: Request parameter.
      validate: Validation override for this request.

    Returns:
      The validated endpoint response.

    References:
      - [MEXC API docs](https://mexcdevelop.github.io/apidocs/contract_v1_en/#switch-leverage)
    """
    params = {}
    r = await self.signed_post(
      '/api/v1/private/position/change_leverage', json=change_leverage_request
    )
    return self.envelope_output(r, adapter, validate)
