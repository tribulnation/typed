# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .market import Market
from .user import User
from typed_mexc.futures.streams.core import FuturesStreamsBase


class Streams(FuturesStreamsBase):
  """MEXC Futures WebSocket API as a whole -- JSON-framed public market-data channels plus the authenticated private user channel (login, then order/position/asset pushes). Cites the WebSocket API overview page, which introduces both.

  References:
    - [Upstream docs](https://www.mexc.com/api-docs/futures/websocket-api/)
  """

  @cached_property
  def market(self) -> Market:
    """Public Futures WebSocket market-data channels -- tickers (all symbols), deals, order book depth (incremental and full), kline, funding rate, index price, and fair price, requiring no authentication.

    References:
      - [Upstream docs](https://www.mexc.com/api-docs/futures/websocket-api/tickers)
    """
    return Market(client=self.market_client)

  @cached_property
  def user(self) -> User:
    """Authenticated Futures WebSocket push channels -- login (required first), then order, position, position-mode, asset, ADL-level, and risk-limit updates for the caller's own account.

    References:
      - [Upstream docs](https://www.mexc.com/api-docs/futures/websocket-api/signature-string)
    """
    return User(client=self.user_client)
