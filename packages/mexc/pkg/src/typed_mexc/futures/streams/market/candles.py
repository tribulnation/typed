# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_mexc.core import TimestampMillis, TimestampSeconds
from typed_mexc.futures.streams.core import FuturesStreamsEndpoint


class Candle(TypedDict):
  """Kline payload for the subscribed contract and interval."""

  symbol: str
  """Contract symbol."""
  interval: Literal[
    'Min1',
    'Min5',
    'Min15',
    'Min30',
    'Min60',
    'Hour4',
    'Hour8',
    'Day1',
    'Week1',
    'Month1',
  ]
  """Kline interval."""
  a: float
  """Total traded amount."""
  c: float
  """Close price."""
  h: float
  """High price."""
  l: float
  """Low price."""
  o: float
  """Open price."""
  q: float
  """Total traded volume."""
  t: TimestampSeconds
  """Window start time in seconds."""


class Parameters(TypedDict):
  symbol: str
  """Futures contract symbol, for example `BTC_USDT`."""
  interval: Literal[
    'Min1',
    'Min5',
    'Min15',
    'Min30',
    'Min60',
    'Hour4',
    'Hour8',
    'Day1',
    'Week1',
    'Month1',
  ]
  """Kline interval."""


class CandlesPushMessage(TypedDict):
  channel: Literal['push.kline']
  """Kline push channel."""
  symbol: str
  """Contract symbol."""
  data: Candle
  ts: TimestampMillis
  """Server timestamp in milliseconds."""


class Candles(FuturesStreamsEndpoint):
  """Subscribes to candlestick updates for a single MEXC futures contract and interval."""

  def candles(
    self,
    symbol: str,
    *,
    interval: Literal[
      'Min1',
      'Min5',
      'Min15',
      'Min30',
      'Min60',
      'Hour4',
      'Hour8',
      'Day1',
      'Week1',
      'Month1',
    ],
    validate: bool | None = None,
  ) -> StreamManager[CandlesPushMessage, Any, Any]:
    """Subscribes to candlestick updates for a single MEXC futures contract and interval.

    Args:
      symbol: Futures contract symbol, for example `BTC_USDT`.
      interval: Kline interval.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/websocket-api/k-line-data)
    """
    return self.subscribe(
      'sub.kline',
      Parameters(symbol=symbol, interval=interval),
      validate=validate,
      request_type=Parameters,
      response_type=CandlesPushMessage,
    )
