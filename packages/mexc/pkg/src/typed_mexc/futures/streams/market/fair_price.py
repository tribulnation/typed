# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_mexc.core import TimestampMillis
from typed_mexc.futures.streams.core import FuturesStreamsEndpoint


class FairPriceUpdate(TypedDict):
  """Fair price payload for the subscribed contract."""

  symbol: str
  """Contract symbol."""
  price: float
  """Fair price."""


class Parameters(TypedDict):
  symbol: str
  """Futures contract symbol, for example `BTC_USDT`."""


class FairPricePushMessage(TypedDict):
  channel: Literal['push.fair.price']
  """Fair-price push channel."""
  symbol: str
  """Contract symbol."""
  data: FairPriceUpdate
  ts: TimestampMillis
  """Server timestamp in milliseconds."""


class FairPrice(FuturesStreamsEndpoint):
  """Subscribes to fair-price updates for a single MEXC futures contract."""

  def fair_price(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[FairPricePushMessage, Any, Any]:
    """Subscribes to fair-price updates for a single MEXC futures contract.

    Args:
      symbol: Futures contract symbol, for example `BTC_USDT`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/websocket-api/fair-price)
    """
    return self.subscribe(
      'sub.fair.price',
      Parameters(symbol=symbol),
      validate=validate,
      request_type=Parameters,
      response_type=FairPricePushMessage,
    )
