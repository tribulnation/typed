# Generated by typed-dev — see CONTRIBUTING.md.
from dataclasses import dataclass
from typing_extensions import TypedDict
from typed_core.util import StreamManager
from typed_core.ws.streams import Stream
from typed_mexc.core import validator
from typed_mexc.futures.streams.core import Reply, StreamsMixin


class FairPriceUpdate(TypedDict):
  """Fair price payload for the subscribed contract."""

  symbol: str
  """Contract symbol."""
  price: float
  """Fair price."""


validate_response = validator(FairPriceUpdate)


@dataclass
class FairPriceStream(StreamsMixin):
  def fair_price(
    self,
    symbol: str,
    *,
    validate: bool = True,
  ) -> StreamManager[FairPriceUpdate, Reply, Reply]:
    """Subscribes to fair-price updates for a single MEXC futures contract.

    Args:
      symbol: Futures contract symbol, for example `BTC_USDT`.
      validate: Validate pushed payloads.

    References:
      - [MEXC API docs](https://mexcdevelop.github.io/apidocs/contract_v1_en/#fair-price-2)
    """
    return StreamManager(lambda: self._fair_price_impl(symbol, validate=validate))

  async def _fair_price_impl(
    self,
    symbol: str,
    *,
    validate: bool = True,
  ) -> Stream[FairPriceUpdate, Reply, Reply]:
    params: dict = {
      'symbol': symbol,
    }
    stream = await self.subscribe('fair.price', params)

    async def parsed_stream():
      async for msg in stream:
        yield validate_response(msg) if self.validate(validate) else msg

    return Stream(
      reply=stream.reply, stream=parsed_stream(), unsubscribe=stream.unsubscribe
    )
