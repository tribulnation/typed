# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_mexc.core import TimestampMillis
from typed_mexc.futures.streams.core import FuturesStreamsEndpoint


class ContractTickerUpdate(TypedDict):
  """Ticker values for the contract."""

  symbol: str
  """Contract symbol."""
  lastPrice: float
  """Last traded price."""
  bid1: float
  """Best bid price."""
  ask1: float
  """Best ask price."""
  volume24: float
  """24-hour contract volume."""
  holdVol: float
  """Open interest or held volume."""
  indexPrice: float
  """Current index price."""
  fairPrice: float
  """Current fair price."""
  fundingRate: float
  """Current funding rate."""
  timestamp: TimestampMillis
  """Ticker timestamp in milliseconds."""


class Parameters(TypedDict):
  symbol: str
  """Futures contract symbol, for example `BTC_USDT`."""


class TickerPushMessage(TypedDict):
  channel: Literal['push.ticker']
  """Ticker push channel."""
  symbol: str
  """Contract symbol."""
  data: ContractTickerUpdate
  ts: TimestampMillis
  """Server timestamp in milliseconds."""


class Ticker(FuturesStreamsEndpoint):
  """Subscribes to one-second ticker updates for a single MEXC futures contract."""

  def ticker(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[TickerPushMessage, Any, Any]:
    """Subscribes to one-second ticker updates for a single MEXC futures contract.

    Args:
      symbol: Futures contract symbol, for example `BTC_USDT`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/websocket-api/get-a-single-ticker)
    """
    return self.subscribe(
      'sub.ticker',
      Parameters(symbol=symbol),
      validate=validate,
      request_type=Parameters,
      response_type=TickerPushMessage,
    )
