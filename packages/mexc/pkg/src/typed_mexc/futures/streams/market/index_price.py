# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_mexc.core import TimestampMillis
from typed_mexc.futures.streams.core import FuturesStreamsEndpoint


class IndexPriceUpdate(TypedDict):
  """Index price payload for the subscribed contract."""

  symbol: str
  """Contract symbol."""
  price: float
  """Index price."""


class Parameters(TypedDict):
  symbol: str
  """Futures contract symbol, for example `BTC_USDT`."""


class IndexPricePushMessage(TypedDict):
  channel: Literal['push.index.price']
  """Index-price push channel."""
  symbol: str
  """Contract symbol."""
  data: IndexPriceUpdate
  ts: TimestampMillis
  """Server timestamp in milliseconds."""


class IndexPrice(FuturesStreamsEndpoint):
  """Subscribes to index-price updates for a single MEXC futures contract."""

  def index_price(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[IndexPricePushMessage, Any, Any]:
    """Subscribes to index-price updates for a single MEXC futures contract.

    Args:
      symbol: Futures contract symbol, for example `BTC_USDT`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/websocket-api/index-price)
    """
    return self.subscribe(
      'sub.index.price',
      Parameters(symbol=symbol),
      validate=validate,
      request_type=Parameters,
      response_type=IndexPricePushMessage,
    )
