# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_mexc.core import TimestampMillis
from typed_mexc.futures.streams.core import FuturesStreamsEndpoint


class AllTicker(TypedDict):
  symbol: str
  """Contract symbol."""
  lastPrice: float
  """Last traded price."""
  volume24: float
  """24-hour contract volume."""
  riseFallRate: float
  """24-hour price change rate."""
  fairPrice: float
  """Current fair price."""


class AllTickersPushMessage(TypedDict):
  channel: Literal['push.tickers']
  """Push channel for all futures tickers."""
  data: list[AllTicker]
  """Ticker rows for futures contracts."""
  ts: TimestampMillis
  """Server timestamp in milliseconds."""


class AllTickers(FuturesStreamsEndpoint):
  """Subscribes to one-second ticker updates for all MEXC perpetual futures contracts."""

  def all_tickers(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[AllTickersPushMessage, Any, Any]:
    """Subscribes to one-second ticker updates for all MEXC perpetual futures contracts.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/websocket-api/tickers)
    """
    return self.subscribe(
      'sub.tickers',
      validate=validate,
      response_type=AllTickersPushMessage,
    )
