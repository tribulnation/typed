# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_mexc.core import TimestampMillis
from typed_mexc.futures.streams.core import FuturesStreamsEndpoint


class FundingRateUpdate(TypedDict):
  """Funding rate payload for the subscribed contract."""

  symbol: str
  """Contract symbol."""
  rate: NotRequired[float]
  """Funding rate as shown in the documented push example."""
  fundingRate: NotRequired[float]
  """Funding rate field named in the response table."""
  nextSettleTime: NotRequired[TimestampMillis]
  """Next funding settlement time when present."""


class Parameters(TypedDict):
  symbol: str
  """Futures contract symbol, for example `BTC_USDT`."""


class FundingRatePushMessage(TypedDict):
  channel: Literal['push.funding.rate']
  """Funding-rate push channel."""
  symbol: str
  """Contract symbol."""
  data: FundingRateUpdate
  ts: TimestampMillis
  """Server timestamp in milliseconds."""


class FundingRate(FuturesStreamsEndpoint):
  """Subscribes to funding-rate updates for a single MEXC futures contract."""

  def funding_rate(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[FundingRatePushMessage, Any, Any]:
    """Subscribes to funding-rate updates for a single MEXC futures contract.

    Args:
      symbol: Futures contract symbol, for example `BTC_USDT`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/websocket-api/funding-rate)
    """
    return self.subscribe(
      'sub.funding.rate',
      Parameters(symbol=symbol),
      validate=validate,
      request_type=Parameters,
      response_type=FundingRatePushMessage,
    )
