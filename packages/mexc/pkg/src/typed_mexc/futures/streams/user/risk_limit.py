# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_mexc.core import TimestampMillis
from typed_mexc.futures.streams.core import FuturesStreamsEndpoint


class RiskLimitUpdate(TypedDict):
  """Risk-limit update data."""

  symbol: str
  """Futures contract symbol."""
  positionType: Literal[1, 2]
  """Position side: 1 long, 2 short."""
  riskSource: int
  """Risk source: 0 other, 1 liquidation service."""
  level: int
  """Current risk-limit level."""
  maxVol: float
  """Maximum position volume for this risk level."""
  maxLeverage: int
  """Maximum leverage ratio for this risk level."""
  mmr: float
  """Maintenance margin rate."""
  imr: float
  """Initial margin rate."""


class RiskLimitPushMessage(TypedDict):
  channel: Literal['push.personal.risk.limit']
  """Private risk-limit push channel."""
  data: RiskLimitUpdate
  ts: TimestampMillis
  """Server timestamp in milliseconds."""


class RiskLimit(FuturesStreamsEndpoint):
  """Receives authenticated futures risk-limit changes after WebSocket login or after applying a risk.limit personal filter."""

  def risk_limit(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[RiskLimitPushMessage, Any, Any]:
    """Receives authenticated futures risk-limit changes after WebSocket login or after applying a risk.limit personal filter.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/websocket-api/risk-limit)
    """
    return self.subscribe(
      'push.personal.risk.limit',
      None,
      validate=validate,
      response_type=RiskLimitPushMessage,
    )
