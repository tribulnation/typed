# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_mexc.core import TimestampMillis
from typed_mexc.futures.streams.core import FuturesStreamsEndpoint


class PositionModeUpdate(TypedDict):
  """Position-mode update data."""

  positionMode: Literal[1, 2]
  """Position mode: 1 hedge, 2 one-way."""


class PositionModePushMessage(TypedDict):
  channel: Literal['push.personal.position.mode']
  """Private position-mode push channel."""
  data: PositionModeUpdate
  ts: TimestampMillis
  """Server timestamp in milliseconds."""


class PositionMode(FuturesStreamsEndpoint):
  """Receives authenticated futures position-mode updates after WebSocket login."""

  def position_mode(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[PositionModePushMessage, Any, Any]:
    """Receives authenticated futures position-mode updates after WebSocket login.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/websocket-api/position-mode-change)
    """
    return self.subscribe(
      'push.personal.position.mode',
      None,
      validate=validate,
      response_type=PositionModePushMessage,
    )
