# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_mexc.core import TimestampMillis
from typed_mexc.futures.streams.core import FuturesStreamsEndpoint


class AdlLevelUpdate(TypedDict):
  """ADL-level update data."""

  adlLevel: int
  """Current ADL level from 1 to 5; docs examples may use 0 while waiting for refresh."""
  positionId: int
  """Position identifier."""


class AdlLevelPushMessage(TypedDict):
  channel: Literal['push.personal.adl.level']
  """Private ADL-level push channel."""
  data: AdlLevelUpdate
  ts: TimestampMillis
  """Server timestamp in milliseconds."""


class AdlLevel(FuturesStreamsEndpoint):
  """Receives authenticated auto-deleveraging level changes for futures positions after WebSocket login or after applying an adl.level personal filter."""

  def adl_level(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[AdlLevelPushMessage, Any, Any]:
    """Receives authenticated auto-deleveraging level changes for futures positions after WebSocket login or after applying an adl.level personal filter.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/websocket-api/liquidation-risk-change)
    """
    return self.subscribe(
      'push.personal.adl.level',
      None,
      validate=validate,
      response_type=AdlLevelPushMessage,
    )
