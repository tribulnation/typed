# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_mexc.core import TimestampMillis
from typed_mexc.futures.streams.core import FuturesStreamsEndpoint


class AssetUpdate(TypedDict):
  """Asset balance data."""

  currency: str
  """Asset currency."""
  positionMargin: float
  """Margin currently assigned to open positions."""
  frozenBalance: float
  """Balance currently frozen."""
  availableBalance: float
  """Balance available for futures trading or transfer."""
  cashBalance: NotRequired[float]
  """Drawable cash balance."""
  bonus: NotRequired[float]
  """Bonus balance."""


class AssetPushMessage(TypedDict):
  channel: Literal['push.personal.asset']
  """Private asset push channel."""
  data: AssetUpdate
  ts: TimestampMillis
  """Server timestamp in milliseconds."""


class Asset(FuturesStreamsEndpoint):
  """Receives authenticated futures asset balance updates after WebSocket login or after applying an asset personal filter."""

  def asset(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[AssetPushMessage, Any, Any]:
    """Receives authenticated futures asset balance updates after WebSocket login or after applying an asset personal filter.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/websocket-api/assets)
    """
    return self.subscribe(
      'push.personal.asset',
      None,
      validate=validate,
      response_type=AssetPushMessage,
    )
