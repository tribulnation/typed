# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_mexc.types import FuturesAckResponse
from typed_mexc.futures.core import AuthFuturesMixin
from typed_mexc.core import validator


class CancelPlanItem(TypedDict):
  """Single trigger order cancellation request."""

  symbol: str
  """Contract symbol."""
  orderId: str
  """Trigger order identifier."""


adapter = validator(FuturesAckResponse)


class CancelPlan(AuthFuturesMixin):
  async def cancel_plan(
    self,
    cancel_plan_request: list[CancelPlanItem],
    *,
    validate: bool | None = None,
  ) -> FuturesAckResponse:
    """Cancels up to 50 futures trigger orders.

    Args:
      cancel_plan_request: Request parameter.
      validate: Validation override for this request.

    Returns:
      The validated endpoint response.

    References:
      - [MEXC API docs](https://mexcdevelop.github.io/apidocs/contract_v1_en/#cancel-the-trigger-order-under-maintenance)
    """
    params = {}
    r = await self.signed_post(
      '/api/v1/private/planorder/cancel', json=cancel_plan_request
    )
    return self.envelope_output(r, adapter, validate)
