# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_mexc.types import FuturesAckResponse
from typed_mexc.futures.core import AuthFuturesMixin
from typed_mexc.core import validator


class CancelStopItem(TypedDict):
  """Single stop-limit trigger cancellation request."""

  stopPlanOrderId: int
  """Stop-limit trigger order identifier."""


adapter = validator(FuturesAckResponse)


class CancelStop(AuthFuturesMixin):
  async def cancel_stop(
    self,
    cancel_stop_request: list[CancelStopItem],
    *,
    validate: bool | None = None,
  ) -> FuturesAckResponse:
    """Cancels up to 50 futures stop-limit trigger orders by stop-plan order id.

    Args:
      cancel_stop_request: Request parameter.
      validate: Validation override for this request.

    Returns:
      The validated endpoint response.

    References:
      - [MEXC API docs](https://mexcdevelop.github.io/apidocs/contract_v1_en/#cancel-the-stop-limit-trigger-order-under-maintenance)
    """
    params = {}
    r = await self.signed_post(
      '/api/v1/private/stoporder/cancel', json=cancel_stop_request
    )
    return self.envelope_output(r, adapter, validate)
