# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .http import Http
from .streams import Streams
from typed_mexc.futures.core import FuturesBase


class Futures(FuturesBase):
  """MEXC's Contract (perpetual futures) API — futures market data, account/position management, and order placement, plus their WebSocket counterparts.

  References:
    - [Upstream docs](https://www.mexc.com/api-docs/futures/update-log)
  """

  @cached_property
  def http(self) -> Http:
    """MEXC Contract (perpetual futures) REST API as a whole -- public market data plus authenticated account/position/order management. Cites the REST integration guide (base URL, request signing, rate limits), which applies across the whole surface; Market and Account/Trade each have their own more specific page one level down.

    References:
      - [Upstream docs](https://www.mexc.com/api-docs/futures/integration-guide)
    """
    return Http(client=self.http_client)

  @cached_property
  def streams(self) -> Streams:
    """MEXC Futures WebSocket API as a whole -- JSON-framed public market-data channels plus the authenticated private user channel (login, then order/position/asset pushes). Cites the WebSocket API overview page, which introduces both.

    References:
      - [Upstream docs](https://www.mexc.com/api-docs/futures/websocket-api/)
    """
    return Streams.new(self.streams_client)
