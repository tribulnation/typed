# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.futures.core import FuturesHttpEndpoint
from typed_mexc.schemas import FuturesAckResponse


class Request(TypedDict):
  """Cancel all futures stop-limit trigger orders request body."""

  positionId: NotRequired[int]
  """Position identifier to scope cancellation."""
  symbol: NotRequired[str]
  """Contract symbol to scope cancellation; omit with positionId to cancel all stop-limit trigger orders."""


class CancelAllStop(FuturesHttpEndpoint):
  """Cancels stop-limit trigger orders by position, by symbol, or across all symbols when no scope is provided."""

  async def cancel_all_stop(
    self,
    position_id: int | None = None,
    *,
    symbol: str | None = None,
    validate: bool | None = None,
  ) -> FuturesAckResponse:
    """Cancels stop-limit trigger orders by position, by symbol, or across all symbols when no scope is provided.

    Args:
      position_id: Position identifier to scope cancellation.
      symbol: Contract symbol to scope cancellation; omit with positionId to cancel all stop-limit trigger orders.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/account-and-trading-endpoints/cancel-all-tpsl-planned-orders)
    """
    request: Request = Request()
    if position_id is not None:
      request['positionId'] = position_id
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='POST',
      path='/api/v1/private/stoporder/cancel_all',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FuturesAckResponse,
    )
