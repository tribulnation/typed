# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_mexc.futures.core import FuturesHttpEndpoint
from typed_mexc.schemas import FuturesAckResponse


class CancelPlanItem(TypedDict):
  """Single trigger order cancellation request."""

  symbol: str
  """Contract symbol."""
  orderId: str
  """Trigger order identifier."""


Request = list[CancelPlanItem]


class CancelPlan(FuturesHttpEndpoint):
  """Cancels up to 50 futures trigger orders."""

  async def cancel_plan(
    self,
    orders: Request,
    *,
    validate: bool | None = None,
  ) -> FuturesAckResponse:
    """Cancels up to 50 futures trigger orders.

    Args:
      orders: Trigger order cancellation requests, maximum 50.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/account-and-trading-endpoints/cancel-planned-orders)
    """
    return await self.request(
      orders,
      method='POST',
      path='/api/v1/private/planorder/cancel',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FuturesAckResponse,
    )
