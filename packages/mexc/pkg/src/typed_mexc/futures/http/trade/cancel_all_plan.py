# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.futures.core import FuturesHttpEndpoint
from typed_mexc.schemas import FuturesAckResponse


class Request(TypedDict):
  """Cancel all futures trigger orders request body."""

  symbol: NotRequired[str]
  """Contract symbol to scope cancellation; omit to cancel all trigger orders."""


class CancelAllPlan(FuturesHttpEndpoint):
  """Cancels all uncompleted trigger orders, optionally scoped to a contract symbol."""

  async def cancel_all_plan(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> FuturesAckResponse:
    """Cancels all uncompleted trigger orders, optionally scoped to a contract symbol.

    Args:
      symbol: Contract symbol to scope cancellation; omit to cancel all trigger orders.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/account-and-trading-endpoints/cancel-all-planned-orders)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='POST',
      path='/api/v1/private/planorder/cancel_all',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FuturesAckResponse,
    )
