# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_mexc.futures.core import FuturesHttpEndpoint
from typed_mexc.schemas import FuturesAckResponse


class Request(TypedDict):
  """Cancel futures order by external id request body."""

  symbol: str
  """Contract symbol."""
  externalOid: str
  """External order identifier."""


class CancelExternalOrder(FuturesHttpEndpoint):
  """Cancels one uncompleted order under a contract by client-supplied external order id."""

  async def cancel_external_order(
    self,
    *,
    symbol: str,
    external_oid: str,
    validate: bool | None = None,
  ) -> FuturesAckResponse:
    """Cancels one uncompleted order under a contract by client-supplied external order id.

    Args:
      symbol: Contract symbol.
      external_oid: External order identifier.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/account-and-trading-endpoints/cancel-by-external-order-id)
    """
    return await self.request(
      Request(symbol=symbol, externalOid=external_oid),
      method='POST',
      path='/api/v1/private/order/cancel_with_external',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FuturesAckResponse,
    )
