# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.futures.core import FuturesHttpEndpoint
from typed_mexc.schemas import FuturesOrder


class Request(TypedDict):
  order_id: str
  """Exchange order id."""


class OrderResponse(TypedDict):
  """Get futures order by id response envelope."""

  success: bool
  """Whether the API request succeeded."""
  code: NotRequired[int]
  """MEXC response code; zero indicates success when present."""
  message: NotRequired[str]
  """Error or status message when present."""
  data: NotRequired[FuturesOrder]


class Order(FuturesHttpEndpoint):
  """Returns a futures order by exchange order id."""

  async def order(
    self, order_id: str, *, validate: bool | None = None
  ) -> OrderResponse:
    """Returns a futures order by exchange order id.

    Args:
      order_id: Exchange order id.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/account-and-trading-endpoints/get-order-information-by-order-id)
    """
    return await self.request(
      Request(order_id=order_id),
      method='GET',
      path='/api/v1/private/order/get/{order_id}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=OrderResponse,
    )
