# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_mexc.futures.core import FuturesHttpEndpoint
from typed_mexc.schemas import FuturesAckResponse


class CancelStopItem(TypedDict):
  """Single stop-limit trigger cancellation request."""

  stopPlanOrderId: int
  """Stop-limit trigger order identifier."""


Request = list[CancelStopItem]


class CancelStop(FuturesHttpEndpoint):
  """Cancels up to 50 futures stop-limit trigger orders by stop-plan order id."""

  async def cancel_stop(
    self,
    orders: Request,
    *,
    validate: bool | None = None,
  ) -> FuturesAckResponse:
    """Cancels up to 50 futures stop-limit trigger orders by stop-plan order id.

    Args:
      orders: Stop-limit trigger order cancellation requests, maximum 50.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/account-and-trading-endpoints/cancel-tpsl-planned-orders)
    """
    return await self.request(
      orders,
      method='POST',
      path='/api/v1/private/stoporder/cancel',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FuturesAckResponse,
    )
