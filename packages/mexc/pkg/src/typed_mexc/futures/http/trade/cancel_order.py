# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.futures.core import FuturesHttpEndpoint


class CancelOrderItem(TypedDict):
  """Per-order cancel result."""

  orderId: int | str
  """Order identifier."""
  errorCode: int
  """Cancel result code; zero indicates success."""
  errorMsg: str
  """Cancel result message."""


Request = list[int | str]


class CancelOrderResponse(TypedDict):
  """Futures cancel response envelope."""

  success: bool
  """Whether the API request succeeded."""
  code: NotRequired[int]
  """MEXC response code; zero indicates success when present."""
  message: NotRequired[str]
  """Error or status message when present."""
  data: NotRequired[list[CancelOrderItem]]
  """Per-order cancel results."""


class CancelOrder(FuturesHttpEndpoint):
  """Cancels up to 50 pending futures orders by order id."""

  async def cancel_order(
    self,
    order_ids: Request,
    *,
    validate: bool | None = None,
  ) -> CancelOrderResponse:
    """Cancels up to 50 pending futures orders by order id.

    Args:
      order_ids: Order id list to cancel, maximum 50.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/account-and-trading-endpoints/cancel-orders)
    """
    return await self.request(
      order_ids,
      method='POST',
      path='/api/v1/private/order/cancel',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CancelOrderResponse,
    )
