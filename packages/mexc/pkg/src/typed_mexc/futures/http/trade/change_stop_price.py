# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.futures.core import FuturesHttpEndpoint
from typed_mexc.schemas import FuturesAckResponse


class Request(TypedDict):
  """Change stop-limit prices for a futures limit order request body."""

  orderId: int
  """Limit order identifier."""
  stopLossPrice: NotRequired[float]
  """Stop-loss price; when both stop-loss and take-profit are empty or 0 the attached stop settings are cancelled."""
  takeProfitPrice: NotRequired[float]
  """Take-profit price; when both stop-loss and take-profit are empty or 0 the attached stop settings are cancelled."""


class ChangeStopPrice(FuturesHttpEndpoint):
  """Updates or clears stop-loss and take-profit prices attached to a futures limit order."""

  async def change_stop_price(
    self,
    order_id: int,
    *,
    stop_loss_price: float | None = None,
    take_profit_price: float | None = None,
    validate: bool | None = None,
  ) -> FuturesAckResponse:
    """Updates or clears stop-loss and take-profit prices attached to a futures limit order.

    Args:
      order_id: Limit order identifier.
      stop_loss_price: Stop-loss price; when both stop-loss and take-profit are empty or 0 the attached stop settings are cancelled.
      take_profit_price: Take-profit price; when both stop-loss and take-profit are empty or 0 the attached stop settings are cancelled.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/account-and-trading-endpoints/modify-tpsl-prices-on-a-limit-order)
    """
    request: Request = Request(orderId=order_id)
    if stop_loss_price is not None:
      request['stopLossPrice'] = stop_loss_price
    if take_profit_price is not None:
      request['takeProfitPrice'] = take_profit_price
    return await self.request(
      request,
      method='POST',
      path='/api/v1/private/stoporder/change_price',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FuturesAckResponse,
    )
