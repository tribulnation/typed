# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.futures.core import FuturesHttpEndpoint
from typed_mexc.schemas import FuturesOrder


class Request(TypedDict):
  symbol: str
  """Contract symbol."""
  external_oid: str
  """External order id."""


class ExternalOrderResponse(TypedDict):
  """Get futures order by external id response envelope."""

  success: bool
  """Whether the API request succeeded."""
  code: NotRequired[int]
  """MEXC response code; zero indicates success when present."""
  message: NotRequired[str]
  """Error or status message when present."""
  data: NotRequired[FuturesOrder]


class ExternalOrder(FuturesHttpEndpoint):
  """Returns a futures order by contract symbol and client-provided external order id."""

  async def external_order(
    self,
    *,
    symbol: str,
    external_oid: str,
    validate: bool | None = None,
  ) -> ExternalOrderResponse:
    """Returns a futures order by contract symbol and client-provided external order id.

    Args:
      symbol: Contract symbol.
      external_oid: External order id.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/account-and-trading-endpoints/get-order-by-external-id)
    """
    return await self.request(
      Request(symbol=symbol, external_oid=external_oid),
      method='GET',
      path='/api/v1/private/order/external/{symbol}/{external_oid}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExternalOrderResponse,
    )
