# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.futures.core import FuturesHttpEndpoint
from typed_mexc.schemas import FuturesOrder


class Request(TypedDict):
  order_ids: str
  """Comma-separated order ids; maximum 50 ids."""


class BatchQueryResponse(TypedDict):
  """Batch query futures orders by id response envelope."""

  success: bool
  """Whether the API request succeeded."""
  code: NotRequired[int]
  """MEXC response code; zero indicates success when present."""
  message: NotRequired[str]
  """Error or status message when present."""
  data: NotRequired[list[FuturesOrder]]
  """Batch order query results."""


class BatchQuery(FuturesHttpEndpoint):
  """Returns multiple futures orders for a comma-separated list of order ids."""

  async def batch_query(
    self,
    order_ids: str,
    *,
    validate: bool | None = None,
  ) -> BatchQueryResponse:
    """Returns multiple futures orders for a comma-separated list of order ids.

    Args:
      order_ids: Comma-separated order ids; maximum 50 ids.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/account-and-trading-endpoints/batch-query-orders-by-order-id)
    """
    return await self.request(
      Request(order_ids=order_ids),
      method='GET',
      path='/api/v1/private/order/batch_query',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BatchQueryResponse,
    )
