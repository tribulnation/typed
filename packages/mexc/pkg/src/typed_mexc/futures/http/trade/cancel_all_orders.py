# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.futures.core import FuturesHttpEndpoint
from typed_mexc.schemas import FuturesAckResponse


class Request(TypedDict):
  """Cancel all futures orders request body."""

  symbol: NotRequired[str]
  """Contract symbol to scope cancellation; omit to cancel all uncompleted orders."""


class CancelAllOrders(FuturesHttpEndpoint):
  """Cancels all uncompleted futures orders, optionally scoped to one contract symbol."""

  async def cancel_all_orders(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> FuturesAckResponse:
    """Cancels all uncompleted futures orders, optionally scoped to one contract symbol.

    Args:
      symbol: Contract symbol to scope cancellation; omit to cancel all uncompleted orders.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/account-and-trading-endpoints/cancel-all-orders-under-a-contract)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='POST',
      path='/api/v1/private/order/cancel_all',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FuturesAckResponse,
    )
