# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .account import Account
from .market import Market
from .position import Position
from .trade import Trade
from typed_mexc.futures.core import FuturesHttpEndpoint


class Http(FuturesHttpEndpoint):
  """MEXC Contract (perpetual futures) REST API as a whole -- public market data plus authenticated account/position/order management. Cites the REST integration guide (base URL, request signing, rate limits), which applies across the whole surface; Market and Account/Trade each have their own more specific page one level down.

  References:
    - [Upstream docs](https://www.mexc.com/api-docs/futures/integration-guide)
  """

  @cached_property
  def account(self) -> Account:
    """Authenticated Futures account endpoints -- asset/assets balances, funding-fee records, tiered fee-rate schedule, and transfer records for the caller's own futures account.

    References:
      - [Upstream docs](https://www.mexc.com/api-docs/futures/account-and-trading-endpoints)
    """
    return Account(client=self.client)

  @cached_property
  def market(self) -> Market:
    """Public Futures market-data endpoints -- server time, contract detail, order book depth (including recent snapshots), index/fair/funding prices, klines, recent trades, tickers, and risk-reserve data. No authentication required.

    References:
      - [Upstream docs](https://www.mexc.com/api-docs/futures/market-endpoints/)
    """
    return Market(client=self.client)

  @cached_property
  def position(self) -> Position:
    """Authenticated Futures position endpoints -- open/historical position listings, plus leverage, margin, position-mode, and risk-limit reads and changes for the caller's own account.

    References:
      - [Upstream docs](https://www.mexc.com/api-docs/futures/account-and-trading-endpoints)
    """
    return Position(client=self.client)

  @cached_property
  def trade(self) -> Trade:
    """Authenticated Futures order, plan-order, and stop-order endpoints -- placement (including batch orders), cancellation (including cancel-all), history/open listings, and lookups.

    References:
      - [Upstream docs](https://www.mexc.com/api-docs/futures/trade/)
    """
    return Trade(client=self.client)
