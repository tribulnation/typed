# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.futures.core import FuturesHttpEndpoint


class Request(TypedDict):
  symbol: str
  """Contract symbol."""


class TieredFeeRateData(TypedDict):
  """Current futures tiered fee rate."""

  level: int
  """Fee tier level."""
  dealAmount: float
  """Last 30 days turnover."""
  walletBalance: float
  """Yesterday wallet balance."""
  makerFee: float
  """Maker fee rate."""
  takerFee: float
  """Taker fee rate."""
  makerFeeDiscount: float
  """Maker fee discount multiplier."""
  takerFeeDiscount: float
  """Taker fee discount multiplier."""


class TieredFeeRateResponse(TypedDict):
  """Get futures tiered fee rate response envelope."""

  success: bool
  """Whether the API request succeeded."""
  code: NotRequired[int]
  """MEXC response code; zero indicates success when present."""
  message: NotRequired[str]
  """Error or status message when present."""
  data: NotRequired[TieredFeeRateData]


class TieredFeeRate(FuturesHttpEndpoint):
  """Returns the signed account tier and maker/taker fee rates for a contract."""

  async def tiered_fee_rate(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> TieredFeeRateResponse:
    """Returns the signed account tier and maker/taker fee rates for a contract.

    Args:
      symbol: Contract symbol.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/account-and-trading-endpoints/get-fee-details)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/api/v1/private/account/tiered_fee_rate',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=TieredFeeRateResponse,
    )
