# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.futures.core import FuturesHttpEndpoint
from typed_mexc.schemas import FuturesAsset


class FuturesAssetsResponse(TypedDict):
  """Get all futures account assets response envelope."""

  success: bool
  """Whether the API request succeeded."""
  code: NotRequired[int]
  """MEXC response code; zero indicates success when present."""
  message: NotRequired[str]
  """Error or status message when present."""
  data: NotRequired[list[FuturesAsset]]
  """All futures asset balances."""


class Assets(FuturesHttpEndpoint):
  """Returns all currency balances for the signed futures account."""

  async def assets(self, *, validate: bool | None = None) -> FuturesAssetsResponse:
    """Returns all currency balances for the signed futures account.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/account-and-trading-endpoints/get-all-account-assets)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v1/private/account/assets',
      meta={'signed': True},
      validate=validate,
      response_type=FuturesAssetsResponse,
    )
