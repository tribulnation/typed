# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.futures.core import FuturesHttpEndpoint
from typed_mexc.schemas import FuturesAsset


class Request(TypedDict):
  currency: str
  """Currency code to query, for example USDT."""


class FuturesAssetResponse(TypedDict):
  """Get single futures asset response envelope."""

  success: bool
  """Whether the API request succeeded."""
  code: NotRequired[int]
  """MEXC response code; zero indicates success when present."""
  message: NotRequired[str]
  """Error or status message when present."""
  data: NotRequired[FuturesAsset]


class Asset(FuturesHttpEndpoint):
  """Returns balance and equity fields for one currency in the signed futures account."""

  async def asset(
    self,
    currency: str,
    *,
    validate: bool | None = None,
  ) -> FuturesAssetResponse:
    """Returns balance and equity fields for one currency in the signed futures account.

    Args:
      currency: Currency code to query, for example USDT.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/account-and-trading-endpoints/get-single-currency-asset-information)
    """
    return await self.request(
      Request(currency=currency),
      method='GET',
      path='/api/v1/private/account/asset/{currency}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FuturesAssetResponse,
    )
