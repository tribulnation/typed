# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.futures.core import FuturesHttpEndpoint


class PositionModeResponse(TypedDict):
  """Get futures position mode response envelope."""

  success: bool
  """Whether the API request succeeded."""
  code: NotRequired[int]
  """MEXC response code; zero indicates success when present."""
  message: NotRequired[str]
  """Error or status message when present."""
  data: NotRequired[int]
  """Position mode: 1 hedge, 2 one-way."""


class PositionMode(FuturesHttpEndpoint):
  """Returns the signed account position mode: 1 hedge mode, 2 one-way mode."""

  async def position_mode(
    self, *, validate: bool | None = None
  ) -> PositionModeResponse:
    """Returns the signed account position mode: 1 hedge mode, 2 one-way mode.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/account-and-trading-endpoints/get-user-position-mode)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v1/private/position/position_mode',
      meta={'signed': True},
      validate=validate,
      response_type=PositionModeResponse,
    )
