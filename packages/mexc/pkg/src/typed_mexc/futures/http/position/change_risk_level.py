# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.futures.core import FuturesHttpEndpoint


class ChangeRiskLevelResponse(TypedDict):
  """Disabled risk-level change response envelope."""

  success: bool
  """Whether the API request succeeded."""
  code: NotRequired[int]
  """MEXC response code; zero indicates success when present."""
  message: NotRequired[str]
  """Error or status message when present."""
  data: NotRequired[None]
  """No success data is returned by the disabled endpoint."""


class Request(TypedDict):
  """Change futures risk level request body."""

  symbol: str
  """Contract symbol whose risk level would be changed."""
  level: int
  """Target risk limit level."""


class ChangeRiskLevel(FuturesHttpEndpoint):
  """Disabled risk-level switch endpoint documented by MEXC; calls return error code 8817 according to the official docs."""

  async def change_risk_level(
    self,
    symbol: str,
    *,
    level: int,
    validate: bool | None = None,
  ) -> ChangeRiskLevelResponse:
    """Disabled risk-level switch endpoint documented by MEXC; calls return error code 8817 according to the official docs.

    Args:
      symbol: Contract symbol whose risk level would be changed.
      level: Target risk limit level.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/account-and-trading-endpoints/change-risk-level)
    """
    return await self.request(
      Request(symbol=symbol, level=level),
      method='POST',
      path='/api/v1/private/account/change_risk_level',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ChangeRiskLevelResponse,
    )
