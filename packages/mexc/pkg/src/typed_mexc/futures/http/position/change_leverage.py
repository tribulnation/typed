# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_mexc.futures.core import FuturesHttpEndpoint
from typed_mexc.schemas import FuturesAckResponse


class AdjustExistingPositionLeverage(TypedDict):
  """Changes leverage for an already-open position, identified by its `positionId`."""

  positionId: int
  """Existing position identifier."""
  leverage: int
  """Target leverage."""


class AdjustLeverageBySymbol(TypedDict):
  """Sets leverage before any position exists, so the margin mode, contract symbol, and position side must be given explicitly instead of a `positionId`."""

  leverage: int
  """Target leverage."""
  openType: Literal[1, 2]
  """Margin mode; 1 isolated, 2 cross."""
  symbol: str
  """Contract symbol."""
  positionType: Literal[1, 2]
  """Position side; 1 long, 2 short."""


Request = AdjustExistingPositionLeverage | AdjustLeverageBySymbol


class ChangeLeverage(FuturesHttpEndpoint):
  """Changes leverage either for an existing position or for a symbol/open-type/side when no position exists."""

  async def change_leverage(
    self,
    request: Request,
    *,
    validate: bool | None = None,
  ) -> FuturesAckResponse:
    """Changes leverage either for an existing position or for a symbol/open-type/side when no position exists.

    Args:
      request:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/account-and-trading-endpoints/modify-leverage)
    """
    return await self.request(
      request,
      method='POST',
      path='/api/v1/private/position/change_leverage',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FuturesAckResponse,
    )
