# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_mexc.futures.core import FuturesHttpEndpoint
from typed_mexc.schemas import FuturesAckResponse


class Request(TypedDict):
  """Change futures position margin request body."""

  positionId: int
  """Position identifier whose margin will be changed."""
  amount: float
  """Margin amount to add or subtract."""
  type: Literal['ADD', 'SUB']
  """Margin change direction: ADD increases margin, SUB decreases margin."""


class ChangeMargin(FuturesHttpEndpoint):
  """Increases or decreases margin on an existing futures position."""

  async def change_margin(
    self,
    position_id: int,
    *,
    amount: float,
    type: Literal['ADD', 'SUB'],
    validate: bool | None = None,
  ) -> FuturesAckResponse:
    """Increases or decreases margin on an existing futures position.

    Args:
      position_id: Position identifier whose margin will be changed.
      amount: Margin amount to add or subtract.
      type: Margin change direction: ADD increases margin, SUB decreases margin.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/account-and-trading-endpoints/modify-position-margin)
    """
    return await self.request(
      Request(positionId=position_id, amount=amount, type=type),
      method='POST',
      path='/api/v1/private/position/change_margin',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FuturesAckResponse,
    )
