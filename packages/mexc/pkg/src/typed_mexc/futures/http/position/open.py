# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.futures.core import FuturesHttpEndpoint
from typed_mexc.schemas import FuturesPosition


class Request(TypedDict):
  symbol: NotRequired[str]
  """Optional contract symbol; omitted returns all open positions."""


class OpenPositionsResponse(TypedDict):
  """Get open futures positions response envelope."""

  success: bool
  """Whether the API request succeeded."""
  code: NotRequired[int]
  """MEXC response code; zero indicates success when present."""
  message: NotRequired[str]
  """Error or status message when present."""
  data: NotRequired[list[FuturesPosition]]
  """Current open futures positions."""


class Open(FuturesHttpEndpoint):
  """Returns current open holding positions for the signed futures account."""

  async def open(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> OpenPositionsResponse:
    """Returns current open holding positions for the signed futures account.

    Args:
      symbol: Optional contract symbol; omitted returns all open positions.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/account-and-trading-endpoints/get-open-positions)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='/api/v1/private/position/open_positions',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=OpenPositionsResponse,
    )
