# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_mexc.futures.core import FuturesHttpEndpoint
from typed_mexc.schemas import FuturesAckResponse


class Request(TypedDict):
  """Change futures position mode request body."""

  positionMode: Literal[1, 2]
  """Target position mode: 1 hedge, 2 one-way."""


class ChangePositionMode(FuturesHttpEndpoint):
  """Switches the account position mode between hedge and one-way mode when no active orders, plan orders, or unfinished positions block the change."""

  async def change_position_mode(
    self,
    position_mode: Literal[1, 2],
    *,
    validate: bool | None = None,
  ) -> FuturesAckResponse:
    """Switches the account position mode between hedge and one-way mode when no active orders, plan orders, or unfinished positions block the change.

    Args:
      position_mode: Target position mode: 1 hedge, 2 one-way.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/account-and-trading-endpoints/modify-user-position-mode)
    """
    return await self.request(
      Request(positionMode=position_mode),
      method='POST',
      path='/api/v1/private/position/change_position_mode',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FuturesAckResponse,
    )
