# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.futures.core import FuturesHttpEndpoint


class SupportCurrenciesResponse(TypedDict):
  """Transferable currencies envelope"""

  success: bool
  """Whether the API request succeeded."""
  code: NotRequired[int]
  """MEXC response code; zero indicates success when present."""
  message: NotRequired[str]
  """Error or status message when present."""
  data: NotRequired[list[str]]
  """Supported transfer currency codes."""


class SupportCurrencies(FuturesHttpEndpoint):
  """Return currencies supported for futures transfers."""

  async def support_currencies(
    self,
    *,
    validate: bool | None = None,
  ) -> SupportCurrenciesResponse:
    """Return currencies supported for futures transfers.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/market-endpoints/get-transferable-currencies)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v1/contract/support_currencies',
      meta={},
      validate=validate,
      response_type=SupportCurrenciesResponse,
    )
