# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.futures.core import FuturesHttpEndpoint
from typed_mexc.schemas import FuturesBookDepth


class Request(TypedDict):
  symbol: str
  """Contract symbol, for example BTC_USDT."""
  limit: NotRequired[int]
  """Depth tier/level count to return."""


class DepthResponse(TypedDict):
  """Depth snapshot envelope"""

  success: bool
  """Whether the API request succeeded."""
  code: NotRequired[int]
  """MEXC response code; zero indicates success when present."""
  message: NotRequired[str]
  """Error or status message when present."""
  data: NotRequired[FuturesBookDepth]


class Depth(FuturesHttpEndpoint):
  """Return a contract order book depth snapshot."""

  async def depth(
    self,
    symbol: str,
    *,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> DepthResponse:
    """Return a contract order book depth snapshot.

    Args:
      symbol: Contract symbol, for example BTC_USDT.
      limit: Depth tier/level count to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/market-endpoints/get-contract-order-book-depth)
    """
    request: Request = Request(symbol=symbol)
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/api/v1/contract/depth/{symbol}',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=DepthResponse,
    )
