# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.futures.core import FuturesHttpEndpoint
from typed_mexc.schemas import FuturesBookDepth


class Request(TypedDict):
  symbol: str
  """Contract symbol, for example BTC_USDT."""
  limit: int
  """Number of latest depth commits to return."""


class DepthCommitsResponse(TypedDict):
  """Depth commits envelope"""

  success: bool
  """Whether the API request succeeded."""
  code: NotRequired[int]
  """MEXC response code; zero indicates success when present."""
  message: NotRequired[str]
  """Error or status message when present."""
  data: NotRequired[list[FuturesBookDepth]]
  """Latest depth commit records."""


class DepthCommits(FuturesHttpEndpoint):
  """Return the latest N depth snapshots/commits for a contract."""

  async def depth_commits(
    self,
    symbol: str,
    *,
    limit: int,
    validate: bool | None = None,
  ) -> DepthCommitsResponse:
    """Return the latest N depth snapshots/commits for a contract.

    Args:
      symbol: Contract symbol, for example BTC_USDT.
      limit: Number of latest depth commits to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/market-endpoints/get-the-last-n-depth-snapshots)
    """
    return await self.request(
      Request(symbol=symbol, limit=limit),
      method='GET',
      path='/api/v1/contract/depth_commits/{symbol}/{limit}',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=DepthCommitsResponse,
    )
