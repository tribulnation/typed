# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.core import TimestampMillis
from typed_mexc.futures.core import FuturesHttpEndpoint


class FundingRateData(TypedDict):
  """Current funding-rate state for a contract."""

  symbol: str
  """Contract symbol."""
  fundingRate: float
  """Current funding rate."""
  maxFundingRate: float
  """Maximum funding rate."""
  minFundingRate: float
  """Minimum funding rate."""
  collectCycle: int
  """Funding collection cycle in hours."""
  nextSettleTime: TimestampMillis
  """Next funding settlement time in milliseconds."""
  timestamp: TimestampMillis
  """System timestamp in milliseconds."""
  fairPrice: float
  """Returned fairPrice field."""
  idxPrice: float
  """Returned idxPrice field."""


class Request(TypedDict):
  symbol: str
  """Contract symbol, for example BTC_USDT."""


class FundingRateResponse(TypedDict):
  """Funding rate envelope"""

  success: bool
  """Whether the API request succeeded."""
  code: NotRequired[int]
  """MEXC response code; zero indicates success when present."""
  message: NotRequired[str]
  """Error or status message when present."""
  data: NotRequired[FundingRateData]


class FundingRate(FuturesHttpEndpoint):
  """Return the current funding-rate state for a contract."""

  async def funding_rate(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> FundingRateResponse:
    """Return the current funding-rate state for a contract.

    Args:
      symbol: Contract symbol, for example BTC_USDT.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/market-endpoints/get-funding-rate)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/api/v1/contract/funding_rate/{symbol}',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=FundingRateResponse,
    )
