# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_mexc.core import TimestampMillis
from typed_mexc.futures.core import FuturesHttpEndpoint


class PingResponse(TypedDict):
  """Server time envelope"""

  success: bool
  """Whether the API request succeeded."""
  code: NotRequired[int]
  """MEXC response code; zero indicates success when present."""
  message: NotRequired[str]
  """Error or status message when present."""
  data: NotRequired[TimestampMillis]
  """Server timestamp in milliseconds."""


class Ping(FuturesHttpEndpoint):
  """Return the current MEXC futures server time."""

  async def ping(self, *, validate: bool | None = None) -> PingResponse:
    """Return the current MEXC futures server time.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/market-endpoints/get-server-time)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v1/contract/ping',
      meta={},
      validate=validate,
      response_type=PingResponse,
    )
