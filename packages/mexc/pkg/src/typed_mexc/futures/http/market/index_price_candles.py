# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_mexc.core import TimestampSeconds
from typed_mexc.futures.core import FuturesHttpEndpoint
from typed_mexc.schemas import FuturesCandle


class Request(TypedDict):
  symbol: str
  """Contract symbol, for example BTC_USDT."""
  interval: NotRequired[
    Literal[
      'Min1',
      'Min5',
      'Min15',
      'Min30',
      'Min60',
      'Hour4',
      'Hour8',
      'Day1',
      'Week1',
      'Month1',
    ]
  ]
  """K-line interval: Min1, Min5, Min15, Min30, Min60, Hour4, Hour8, Day1, Week1, or Month1."""
  start: NotRequired[TimestampSeconds]
  """Start timestamp in seconds."""
  end: NotRequired[TimestampSeconds]
  """End timestamp in seconds."""


class IndexPriceCandlesResponse(TypedDict):
  """Index-price K-line envelope"""

  success: bool
  """Whether the API request succeeded."""
  code: NotRequired[int]
  """MEXC response code; zero indicates success when present."""
  message: NotRequired[str]
  """Error or status message when present."""
  data: NotRequired[FuturesCandle]


class IndexPriceCandles(FuturesHttpEndpoint):
  """Return index-price K-line/candlestick series for a symbol and optional time window."""

  async def index_price_candles(
    self,
    symbol: str,
    *,
    interval: Literal[
      'Min1',
      'Min5',
      'Min15',
      'Min30',
      'Min60',
      'Hour4',
      'Hour8',
      'Day1',
      'Week1',
      'Month1',
    ]
    | None = None,
    start: TimestampSeconds | None = None,
    end: TimestampSeconds | None = None,
    validate: bool | None = None,
  ) -> IndexPriceCandlesResponse:
    """Return index-price K-line/candlestick series for a symbol and optional time window.

    Args:
      symbol: Contract symbol, for example BTC_USDT.
      interval: K-line interval: Min1, Min5, Min15, Min30, Min60, Hour4, Hour8, Day1, Week1, or Month1.
      start: Start timestamp in seconds.
      end: End timestamp in seconds.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.mexc.com/api-docs/futures/market-endpoints/get-index-price-candles)
    """
    request: Request = Request(symbol=symbol)
    if interval is not None:
      request['interval'] = interval
    if start is not None:
      request['start'] = start
    if end is not None:
      request['end'] = end
    return await self.request(
      request,
      method='GET',
      path='/api/v1/contract/kline/index_price/{symbol}',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=IndexPriceCandlesResponse,
    )
