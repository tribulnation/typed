# Generated by typed-dev — see CONTRIBUTING.md.
from betterproto2.grpclib.grpclib_client import MetadataLike
from typed_core.grpc import GrpcEndpoint, wrap_exceptions
from typed_dydx.protos.cosmos.bank.v1beta1 import (
  QueryBalanceRequest,
  QueryBalanceResponse,
  QueryStub,
)


class Balance(GrpcEndpoint):
  """Query one denom balance for an account address."""

  @wrap_exceptions
  async def balance(
    self,
    *,
    address: str,
    denom: str,
    metadata: MetadataLike | None = None,
  ) -> QueryBalanceResponse:
    """Query one denom balance for an account address.

    Args:
      metadata: gRPC metadata forwarded to the call and to every page of a paginated query.

    References:
      - [Official docs](https://docs.cosmos.network/main/build/modules/bank)
    """
    return await QueryStub(self.channel).balance(
      QueryBalanceRequest(address=address, denom=denom), metadata=metadata
    )
