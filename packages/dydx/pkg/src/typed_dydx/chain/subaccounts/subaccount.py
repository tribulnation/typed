# Generated by typed-dev — see CONTRIBUTING.md.
from betterproto2.grpclib.grpclib_client import MetadataLike
from typed_core.grpc import GrpcEndpoint, wrap_exceptions
from typed_dydx.protos.dydxprotocol.subaccounts import (
  QueryGetSubaccountRequest,
  QueryStub,
  QuerySubaccountResponse,
)


class Subaccount(GrpcEndpoint):
  """Query a single subaccount by owner address and subaccount number."""

  @wrap_exceptions
  async def subaccount(
    self,
    owner: str,
    *,
    number: int,
    metadata: MetadataLike | None = None,
  ) -> QuerySubaccountResponse:
    """Query a single subaccount by owner address and subaccount number.

    Args:
      metadata: gRPC metadata forwarded to the call and to every page of a paginated query.

    References:
      - [Official docs](https://docs.dydx.exchange)
    """
    return await QueryStub(self.channel).subaccount(
      QueryGetSubaccountRequest(owner=owner, number=number), metadata=metadata
    )
