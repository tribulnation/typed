# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.grpc import GrpcEndpoint, wrap_exceptions
from typed_dydx.protos.cosmos.auth.v1beta1 import (
  QueryAccountRequest,
  QueryAccountResponse,
  QueryStub,
)


class Account(GrpcEndpoint):
  """Query account details based on address."""

  @wrap_exceptions
  async def account(self, address: str) -> QueryAccountResponse:
    """Query account details based on address.

    References:
      - [Official docs](https://docs.cosmos.network/main/build/modules/auth)
    """
    return await QueryStub(self.channel).account(QueryAccountRequest(address=address))
