# Generated by typed-dev — see CONTRIBUTING.md.
from betterproto2.grpclib.grpclib_client import MetadataLike
from typed_core.grpc import GrpcEndpoint, wrap_exceptions
from typed_dydx.protos.cosmos.auth.v1beta1 import (
  QueryAccountInfoRequest,
  QueryAccountInfoResponse,
  QueryStub,
)


class AccountInfo(GrpcEndpoint):
  """Query account info which is common to all account types."""

  @wrap_exceptions
  async def account_info(
    self,
    address: str,
    *,
    metadata: MetadataLike | None = None,
  ) -> QueryAccountInfoResponse:
    """Query account info which is common to all account types.

    Args:
      metadata: gRPC metadata forwarded to the call and to every page of a paginated query.

    References:
      - [Official docs](https://docs.cosmos.network/main/build/modules/auth)
    """
    return await QueryStub(self.channel).account_info(
      QueryAccountInfoRequest(address=address), metadata=metadata
    )
