# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.grpc import GrpcEndpoint, wrap_exceptions
from typed_dydx.protos.dydxprotocol.clob import (
  QueryLeverageRequest,
  QueryLeverageResponse,
  QueryStub,
)


class Leverage(GrpcEndpoint):
  """Query leverage info for a subaccount."""

  @wrap_exceptions
  async def leverage(
    self,
    owner: str,
    *,
    number: int,
  ) -> QueryLeverageResponse:
    """Query leverage info for a subaccount.

    References:
      - [Official docs](https://docs.dydx.exchange/)
    """
    return await QueryStub(self.channel).leverage(
      QueryLeverageRequest(owner=owner, number=number)
    )
