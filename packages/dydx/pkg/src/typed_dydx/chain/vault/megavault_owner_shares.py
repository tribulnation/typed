# Generated by typed-dev — see CONTRIBUTING.md.
from betterproto2.grpclib.grpclib_client import MetadataLike
from typed_core.grpc import GrpcEndpoint, wrap_exceptions
from typed_dydx.protos.dydxprotocol.vault import (
  QueryMegavaultOwnerSharesRequest,
  QueryMegavaultOwnerSharesResponse,
  QueryStub,
)


class MegavaultOwnerShares(GrpcEndpoint):
  """Query an owner's MegaVault shares, share unlocks, equity and withdrawable equity."""

  @wrap_exceptions
  async def megavault_owner_shares(
    self,
    address: str,
    *,
    metadata: MetadataLike | None = None,
  ) -> QueryMegavaultOwnerSharesResponse:
    """Query an owner's MegaVault shares, share unlocks, equity and withdrawable equity.

    Args:
      metadata: gRPC metadata forwarded to the call and to every page of a paginated query.

    References:
      - [Official docs](https://github.com/dydxprotocol/v4-chain/blob/protocol/v9.6.1/proto/dydxprotocol/vault/query.proto)
    """
    return await QueryStub(self.channel).megavault_owner_shares(
      QueryMegavaultOwnerSharesRequest(address=address), metadata=metadata
    )
