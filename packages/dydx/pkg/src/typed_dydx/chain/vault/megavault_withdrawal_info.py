# Generated by typed-dev — see CONTRIBUTING.md.
from betterproto2.grpclib.grpclib_client import MetadataLike
from typed_core.grpc import GrpcEndpoint, wrap_exceptions
from typed_dydx.protos.dydxprotocol.vault import (
  NumShares,
  QueryMegavaultWithdrawalInfoRequest,
  QueryMegavaultWithdrawalInfoResponse,
  QueryStub,
)


class MegavaultWithdrawalInfo(GrpcEndpoint):
  """Estimate the quote quantums redeemable for a number of MegaVault shares without submitting a withdrawal."""

  @wrap_exceptions
  async def megavault_withdrawal_info(
    self,
    shares_to_withdraw: NumShares | None = None,
    *,
    metadata: MetadataLike | None = None,
  ) -> QueryMegavaultWithdrawalInfoResponse:
    """Estimate the quote quantums redeemable for a number of MegaVault shares without submitting a withdrawal.

    Args:
      metadata: gRPC metadata forwarded to the call and to every page of a paginated query.

    References:
      - [Official docs](https://github.com/dydxprotocol/v4-chain/blob/protocol/v9.6.1/proto/dydxprotocol/vault/query.proto)
    """
    return await QueryStub(self.channel).megavault_withdrawal_info(
      QueryMegavaultWithdrawalInfoRequest(shares_to_withdraw=shares_to_withdraw),
      metadata=metadata,
    )
