# Generated by typed-dev — see CONTRIBUTING.md.
from betterproto2.grpclib.grpclib_client import MetadataLike
from typed_core.grpc import GrpcEndpoint, wrap_exceptions
from typed_dydx.protos.dydxprotocol.vault import (
  QueryStub,
  QueryVaultParamsRequest,
  QueryVaultParamsResponse,
  VaultType,
)


class VaultParams(GrpcEndpoint):
  """Query the quoting parameters and status of a vault by its type and number."""

  @wrap_exceptions
  async def vault_params(
    self,
    type: VaultType,
    *,
    number: int,
    metadata: MetadataLike | None = None,
  ) -> QueryVaultParamsResponse:
    """Query the quoting parameters and status of a vault by its type and number.

    Args:
      metadata: gRPC metadata forwarded to the call and to every page of a paginated query.

    References:
      - [Official docs](https://github.com/dydxprotocol/v4-chain/blob/protocol/v9.6.1/proto/dydxprotocol/vault/query.proto)
    """
    return await QueryStub(self.channel).vault_params(
      QueryVaultParamsRequest(type=type, number=number), metadata=metadata
    )
