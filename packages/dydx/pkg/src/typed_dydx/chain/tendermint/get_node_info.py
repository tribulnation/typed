# Generated by typed-dev — see CONTRIBUTING.md.
from betterproto2.grpclib.grpclib_client import MetadataLike
from typed_core.grpc import GrpcEndpoint, wrap_exceptions
from typed_dydx.protos.cosmos.base.tendermint.v1beta1 import (
  GetNodeInfoRequest,
  GetNodeInfoResponse,
  ServiceStub,
)


class GetNodeInfo(GrpcEndpoint):
  """Query the current node's info."""

  @wrap_exceptions
  async def get_node_info(
    self,
    *,
    metadata: MetadataLike | None = None,
  ) -> GetNodeInfoResponse:
    """Query the current node's info.

    Args:
      metadata: gRPC metadata forwarded to the call and to every page of a paginated query.

    References:
      - [Official docs](https://docs.cosmos.network/main/build/modules/base_tendermint)
    """
    return await ServiceStub(self.channel).get_node_info(
      GetNodeInfoRequest(), metadata=metadata
    )
