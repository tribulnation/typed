# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_dydx.chain.comet.core import CometEndpoint
from typed_dydx.chain.comet.schemas import TxResponse


class Request(TypedDict):
  hash: str
  """Transaction hash to look up. CometBFT HTTP accepts the hash with a `0x` prefix."""
  prove: NotRequired[bool]
  """When true, include a Merkle proof for the transaction."""


class Tx(CometEndpoint):
  """Returns one indexed transaction by hash, including height, index, result metadata, and encoded transaction bytes."""

  async def tx(
    self,
    hash: str,
    *,
    prove: bool | None = None,
    validate: bool | None = None,
  ) -> TxResponse:
    """Returns one indexed transaction by hash, including height, index, result metadata, and encoded transaction bytes.

    Args:
      hash: Transaction hash to look up. CometBFT HTTP accepts the hash with a `0x` prefix.
      prove: When true, include a Merkle proof for the transaction.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cosmos.network/cometbft/latest/api-reference/rpc/info/tx)
    """
    request: Request = Request(hash=hash)
    if prove is not None:
      request['prove'] = prove
    return await self.request(
      request,
      method='GET',
      path='/tx',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=TxResponse,
    )
