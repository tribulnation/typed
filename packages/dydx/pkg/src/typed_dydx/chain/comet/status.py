# Generated by typed-dev — see CONTRIBUTING.md.
from typed_dydx.chain.comet.core import CometEndpoint
from typed_dydx.chain.comet.schemas import StatusResponse


class Status(CometEndpoint):
  """Returns CometBFT node status, including node information, sync state, latest block metadata, and validator information."""

  async def status(self, *, validate: bool | None = None) -> StatusResponse:
    """Returns CometBFT node status, including node information, sync state, latest block metadata, and validator information.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cosmos.network/cometbft/latest/api-reference/rpc/info/status)
    """
    return await self.request(
      None,
      method='GET',
      path='/status',
      meta={},
      validate=validate,
      response_type=StatusResponse,
    )
