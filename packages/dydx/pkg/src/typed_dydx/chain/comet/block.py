# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_dydx.chain.comet.core import CometEndpoint
from typed_dydx.chain.comet.schemas import BlockResponse


class Request(TypedDict):
  height: NotRequired[int]
  """Block height to fetch. Omit to fetch the latest block."""


class Block(CometEndpoint):
  """Returns a CometBFT block at the requested height, or the latest block when height is omitted."""

  async def block(
    self,
    height: int | None = None,
    *,
    validate: bool | None = None,
  ) -> BlockResponse:
    """Returns a CometBFT block at the requested height, or the latest block when height is omitted.

    Args:
      height: Block height to fetch. Omit to fetch the latest block.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cosmos.network/cometbft/latest/api-reference/rpc/info/block)
    """
    request: Request = Request()
    if height is not None:
      request['height'] = height
    return await self.request(
      request,
      method='GET',
      path='/block',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=BlockResponse,
    )
