# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_dydx.chain.comet.core import CometEndpoint
from typed_dydx.chain.comet.schemas import BlockResultsResponse


class Request(TypedDict):
  height: NotRequired[int]
  """Block height whose execution results should be returned. Omit to fetch the latest block results."""


class BlockResults(CometEndpoint):
  """Returns ABCI execution results and finalize-block events for the requested block height."""

  async def block_results(
    self,
    height: int | None = None,
    *,
    validate: bool | None = None,
  ) -> BlockResultsResponse:
    """Returns ABCI execution results and finalize-block events for the requested block height.

    Args:
      height: Block height whose execution results should be returned. Omit to fetch the latest block results.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cosmos.network/cometbft/latest/api-reference/rpc/info/block_results)
    """
    request: Request = Request()
    if height is not None:
      request['height'] = height
    return await self.request(
      request,
      method='GET',
      path='/block_results',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=BlockResultsResponse,
    )
