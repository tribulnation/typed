# Generated by typed-dev — see CONTRIBUTING.md.
from betterproto2.grpclib.grpclib_client import MetadataLike
from typed_core.grpc import GrpcEndpoint, wrap_exceptions
from typed_dydx.protos.dydxprotocol.affiliates import (
  QueryStub,
  ReferredByRequest,
  ReferredByResponse,
)


class ReferredBy(GrpcEndpoint):
  """Query the affiliate that referred an address."""

  @wrap_exceptions
  async def referred_by(
    self,
    address: str,
    *,
    metadata: MetadataLike | None = None,
  ) -> ReferredByResponse:
    """Query the affiliate that referred an address.

    Args:
      metadata: gRPC metadata forwarded to the call and to every page of a paginated query.

    References:
      - [Official docs](https://docs.dydx.exchange/)
    """
    return await QueryStub(self.channel).referred_by(
      ReferredByRequest(address=address), metadata=metadata
    )
