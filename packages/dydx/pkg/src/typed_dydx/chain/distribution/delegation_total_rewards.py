# Generated by typed-dev — see CONTRIBUTING.md.
from betterproto2.grpclib.grpclib_client import MetadataLike
from typed_core.grpc import GrpcEndpoint, wrap_exceptions
from typed_dydx.protos.cosmos.distribution.v1beta1 import (
  QueryDelegationTotalRewardsRequest,
  QueryDelegationTotalRewardsResponse,
  QueryStub,
)


class DelegationTotalRewards(GrpcEndpoint):
  """Query the total rewards accrued by a delegator across all validators."""

  @wrap_exceptions
  async def delegation_total_rewards(
    self,
    delegator_address: str,
    *,
    metadata: MetadataLike | None = None,
  ) -> QueryDelegationTotalRewardsResponse:
    """Query the total rewards accrued by a delegator across all validators.

    Args:
      metadata: gRPC metadata forwarded to the call and to every page of a paginated query.

    References:
      - [Official docs](https://docs.cosmos.network/main/build/modules/distribution)
    """
    return await QueryStub(self.channel).delegation_total_rewards(
      QueryDelegationTotalRewardsRequest(delegator_address=delegator_address),
      metadata=metadata,
    )
