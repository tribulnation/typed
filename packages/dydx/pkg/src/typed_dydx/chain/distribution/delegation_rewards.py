# Generated by typed-dev — see CONTRIBUTING.md.
from betterproto2.grpclib.grpclib_client import MetadataLike
from typed_core.grpc import GrpcEndpoint, wrap_exceptions
from typed_dydx.protos.cosmos.distribution.v1beta1 import (
  QueryDelegationRewardsRequest,
  QueryDelegationRewardsResponse,
  QueryStub,
)


class DelegationRewards(GrpcEndpoint):
  """Query the total rewards accrued by a delegation to one validator."""

  @wrap_exceptions
  async def delegation_rewards(
    self,
    *,
    delegator_address: str,
    validator_address: str,
    metadata: MetadataLike | None = None,
  ) -> QueryDelegationRewardsResponse:
    """Query the total rewards accrued by a delegation to one validator.

    Args:
      metadata: gRPC metadata forwarded to the call and to every page of a paginated query.

    References:
      - [Official docs](https://docs.cosmos.network/main/build/modules/distribution)
    """
    return await QueryStub(self.channel).delegation_rewards(
      QueryDelegationRewardsRequest(
        delegator_address=delegator_address, validator_address=validator_address
      ),
      metadata=metadata,
    )
