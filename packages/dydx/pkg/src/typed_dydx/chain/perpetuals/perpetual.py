# Generated by typed-dev — see CONTRIBUTING.md.
from betterproto2.grpclib.grpclib_client import MetadataLike
from typed_core.grpc import GrpcEndpoint, wrap_exceptions
from typed_dydx.protos.dydxprotocol.perpetuals import (
  QueryPerpetualRequest,
  QueryPerpetualResponse,
  QueryStub,
)


class Perpetual(GrpcEndpoint):
  """Query a perpetual by id."""

  @wrap_exceptions
  async def perpetual(
    self,
    id: int,
    *,
    metadata: MetadataLike | None = None,
  ) -> QueryPerpetualResponse:
    """Query a perpetual by id.

    Args:
      metadata: gRPC metadata forwarded to the call and to every page of a paginated query.

    References:
      - [Official docs](https://docs.dydx.exchange)
    """
    return await QueryStub(self.channel).perpetual(
      QueryPerpetualRequest(id=id), metadata=metadata
    )
