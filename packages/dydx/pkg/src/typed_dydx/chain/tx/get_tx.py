# Generated by typed-dev — see CONTRIBUTING.md.
from betterproto2.grpclib.grpclib_client import MetadataLike
from typed_core.grpc import GrpcEndpoint, wrap_exceptions
from typed_dydx.protos.cosmos.tx.v1beta1 import GetTxRequest, GetTxResponse, ServiceStub


class GetTx(GrpcEndpoint):
  """Query a transaction by its hex-encoded hash."""

  @wrap_exceptions
  async def get_tx(
    self,
    hash: str,
    *,
    metadata: MetadataLike | None = None,
  ) -> GetTxResponse:
    """Query a transaction by its hex-encoded hash.

    Args:
      metadata: gRPC metadata forwarded to the call and to every page of a paginated query.

    References:
      - [Official docs](https://docs.cosmos.network/main/build/modules/tx)
    """
    return await ServiceStub(self.channel).get_tx(
      GetTxRequest(hash=hash), metadata=metadata
    )
