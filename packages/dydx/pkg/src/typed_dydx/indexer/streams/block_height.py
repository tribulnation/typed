# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, NotRequired, TypedDict
from typed_dydx.core import TimestampIso
from typed_dydx.indexer.streams.core import StreamsMixin


class BlockHeightMessageContents(TypedDict):
  blockHeight: int
  """Latest known block height."""
  time: TimestampIso
  """Timestamp of the latest known block."""


class Parameters(TypedDict):
  batched: NotRequired[bool]
  """Reduce incoming messages by batching contents."""


class BlockHeight(StreamsMixin):
  """Subscribe to the indexer block height feed."""

  def block_height(
    self,
    batched: bool | None = None,
    *,
    validate: bool | None = None,
  ) -> StreamManager[BlockHeightMessageContents, Any, Any]:
    """Subscribe to the indexer block height feed.

    Args:
      batched: Reduce incoming messages by batching contents.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.dydx.xyz/indexer-client/websockets#block-height)
    """
    parameters: Parameters = Parameters()
    if batched is not None:
      parameters['batched'] = batched
    return self.subscribe(
      'v4_block_height',
      parameters,
      validate=validate,
      request_type=Parameters,
      response_type=BlockHeightMessageContents,
    )
