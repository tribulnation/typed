# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, NotRequired, TypedDict
from typed_dydx.indexer.schemas import OraclePriceMarket, TradingPerpetualMarket
from typed_dydx.indexer.streams.core import StreamsMixin


class Parameters(TypedDict):
  batched: NotRequired[bool]
  """Reduce incoming messages by batching contents."""


class MarketsMessageContents(TypedDict):
  oraclePrices: NotRequired[dict[str, OraclePriceMarket] | None]
  """Oracle prices, keyed by ticker, changed since the last update."""
  trading: NotRequired[dict[str, TradingPerpetualMarket] | None]
  """Perpetual market trading parameters, keyed by ticker, changed since the last update."""


class Markets(StreamsMixin):
  """Subscribe to the indexer markets feed."""

  def markets(
    self,
    batched: bool | None = None,
    *,
    validate: bool | None = None,
  ) -> StreamManager[MarketsMessageContents, Any, Any]:
    """Subscribe to the indexer markets feed.

    Args:
      batched: Reduce incoming messages by batching contents.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.dydx.xyz/indexer-client/websockets#markets)
    """
    parameters: Parameters = Parameters()
    if batched is not None:
      parameters['batched'] = batched
    return self.subscribe(
      'v4_markets',
      parameters,
      validate=validate,
      request_type=Parameters,
      response_type=MarketsMessageContents,
    )
