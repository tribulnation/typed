# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, NotRequired, TypedDict
from typed_dydx.indexer.schemas import Candle, CandleResolution
from typed_dydx.indexer.streams.core import StreamsMixin


class Parameters(TypedDict):
  market: str
  """Market ticker."""
  resolution: CandleResolution
  batched: NotRequired[bool]
  """Reduce incoming messages by batching contents."""


class Candles(StreamsMixin):
  """Subscribe to the indexer candles feed for a market and resolution."""

  def candles(
    self,
    market: str,
    *,
    resolution: CandleResolution,
    batched: bool | None = None,
    validate: bool | None = None,
  ) -> StreamManager[Candle, Any, Any]:
    """Subscribe to the indexer candles feed for a market and resolution.

    Args:
      market: Market ticker.
      resolution:
      batched: Reduce incoming messages by batching contents.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.dydx.xyz/indexer-client/websockets#candles)
    """
    parameters: Parameters = Parameters(market=market, resolution=resolution)
    if batched is not None:
      parameters['batched'] = batched
    return self.subscribe(
      'v4_candles:{market}/{resolution}',
      parameters,
      validate=validate,
      request_type=Parameters,
      response_type=Candle,
    )
