# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, NotRequired, TypedDict
from typed_dydx.indexer.schemas import SubaccountsNotification
from typed_dydx.indexer.streams.core import StreamsMixin


class Parameters(TypedDict):
  address: str
  """Wallet address."""
  subaccountNumber: int
  """Parent subaccount number."""
  batched: NotRequired[bool]
  """Reduce incoming messages by batching contents."""


class ParentSubaccounts(StreamsMixin):
  """Subscribe to the indexer parent subaccounts feed for an address and parent subaccount number."""

  def parent_subaccounts(
    self,
    address: str,
    *,
    subaccount: int,
    batched: bool | None = None,
    validate: bool | None = None,
  ) -> StreamManager[SubaccountsNotification, Any, Any]:
    """Subscribe to the indexer parent subaccounts feed for an address and parent subaccount number.

    Args:
      address: Wallet address.
      subaccount: Parent subaccount number.
      batched: Reduce incoming messages by batching contents.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.dydx.xyz/indexer-client/websockets#parent-subaccounts)
    """
    parameters: Parameters = Parameters(address=address, subaccountNumber=subaccount)
    if batched is not None:
      parameters['batched'] = batched
    return self.subscribe(
      'v4_parent_subaccounts:{address}/{subaccountNumber}',
      parameters,
      validate=validate,
      request_type=Parameters,
      response_type=SubaccountsNotification,
    )
