# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .data import Data
from .streams import Streams
from typed_dydx.indexer.core import IndexerBase


class Indexer(IndexerBase):
  """dYdX's Indexer — the high-availability read service that indexes chain state and serves it over REST/HTTP and WebSocket, since querying a validator or full node directly for the same data would slow its own consensus participation.

  References:
    - [Upstream docs](https://docs.dydx.xyz/indexer-client)
  """

  @cached_property
  def data(self) -> Data:
    """dYdX Indexer's REST/HTTP API — one-shot queries for markets, candles, orders, fills, positions, transfers, funding payments, rewards, and account/subaccount state.

    References:
      - [Upstream docs](https://docs.dydx.xyz/indexer-client/http)
    """
    return Data(client=self.http_client)

  @cached_property
  def streams(self) -> Streams:
    """dYdX Indexer's WebSocket API — real-time push channels for the order book, trades, candles, markets, block height, and subaccount/parent-subaccount updates.

    References:
      - [Upstream docs](https://docs.dydx.xyz/indexer-client/websockets)
    """
    return Streams(client=self.ws_client)
