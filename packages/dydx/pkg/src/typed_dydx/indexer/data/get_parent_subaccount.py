# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_dydx.indexer.data.core import IndexerMixin
from typed_dydx.indexer.schemas import ParentSubaccount


class Request(TypedDict):
  address: str
  """Wallet address that owns the parent subaccount."""
  parentSubaccountNumber: int
  """Parent subaccount number."""


class GetParentSubaccount(IndexerMixin):
  """Get parent subaccount"""

  async def get_parent_subaccount(
    self,
    address: str,
    *,
    parent_subaccount: int,
    validate: bool | None = None,
  ) -> ParentSubaccount:
    """Get parent subaccount

    Args:
      address: Wallet address that owns the parent subaccount.
      parent_subaccount: Parent subaccount number.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.dydx.xyz/indexer-client/http/accounts#get-parent-subaccount)
    """
    return await self.request(
      Request(address=address, parentSubaccountNumber=parent_subaccount),
      method='GET',
      path='/v4/addresses/{address}/parentSubaccountNumber/{parentSubaccountNumber}',
      meta={'payload': 'subaccount'},
      validate=validate,
      request_type=Request,
      response_type=ParentSubaccount,
    )
