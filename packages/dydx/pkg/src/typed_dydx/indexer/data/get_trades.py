# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_dydx.indexer.data.core import IndexerMixin
from typed_dydx.indexer.schemas import TradesResponse


class Request(TypedDict):
  market: str
  """Perpetual market ticker."""
  startingBeforeOrAtHeight: NotRequired[int]
  """Latest block height to include."""
  limit: NotRequired[int]
  """Maximum number of trades to return."""


class GetTrades(IndexerMixin):
  """Retrieve trades for a perpetual market."""

  async def get_trades(
    self,
    market: str,
    *,
    starting_before_or_at_height: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> TradesResponse:
    """Retrieve trades for a perpetual market.

    Args:
      market: Perpetual market ticker.
      starting_before_or_at_height: Latest block height to include.
      limit: Maximum number of trades to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.dydx.xyz/indexer-client/http#get-trades)
    """
    request: Request = Request(market=market)
    if starting_before_or_at_height is not None:
      request['startingBeforeOrAtHeight'] = starting_before_or_at_height
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/v4/trades/perpetualMarket/{market}',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=TradesResponse,
    )
