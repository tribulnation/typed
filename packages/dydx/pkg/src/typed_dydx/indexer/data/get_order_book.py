# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_dydx.indexer.data.core import IndexerMixin
from typed_dydx.indexer.schemas import OrderBook


class Request(TypedDict):
  market: str
  """Perpetual market ticker."""


class GetOrderBook(IndexerMixin):
  """Retrieve the orderbook for a perpetual market."""

  async def get_order_book(
    self, market: str, *, validate: bool | None = None
  ) -> OrderBook:
    """Retrieve the orderbook for a perpetual market.

    Args:
      market: Perpetual market ticker.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.dydx.xyz/indexer-client/http#get-perpetual-market-orderbook)
    """
    return await self.request(
      Request(market=market),
      method='GET',
      path='/v4/orderbooks/perpetualMarket/{market}',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=OrderBook,
    )
