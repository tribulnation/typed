# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_dydx.core import TimestampIso
from typed_dydx.indexer.data.core import IndexerMixin
from typed_dydx.indexer.schemas import AssetPositionsResponse, PerpetualPositionStatus


class Request(TypedDict):
  address: str
  """Wallet address that owns the account."""
  subaccountNumber: int
  """Subaccount number."""
  status: NotRequired[PerpetualPositionStatus]
  limit: NotRequired[int]
  """Maximum number of results to return."""
  createdBeforeOrAtHeight: NotRequired[int]
  """Restrict results to entries created at or before a specific block height."""
  createdBeforeOrAt: NotRequired[TimestampIso]
  """Restrict results to entries created at or before a specific timestamp."""


class GetAssetPositions(IndexerMixin):
  """Get asset positions"""

  async def get_asset_positions(
    self,
    address: str,
    *,
    subaccount: int,
    status: PerpetualPositionStatus | None = None,
    limit: int | None = None,
    created_before_or_at_height: int | None = None,
    created_before_or_at: TimestampIso | None = None,
    validate: bool | None = None,
  ) -> AssetPositionsResponse:
    """Get asset positions

    Args:
      address: Wallet address that owns the account.
      subaccount: Subaccount number.
      status:
      limit: Maximum number of results to return.
      created_before_or_at_height: Restrict results to entries created at or before a specific block height.
      created_before_or_at: Restrict results to entries created at or before a specific timestamp.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.dydx.xyz/indexer-client/http/accounts#get-asset-positions)
    """
    request: Request = Request(address=address, subaccountNumber=subaccount)
    if status is not None:
      request['status'] = status
    if limit is not None:
      request['limit'] = limit
    if created_before_or_at_height is not None:
      request['createdBeforeOrAtHeight'] = created_before_or_at_height
    if created_before_or_at is not None:
      request['createdBeforeOrAt'] = created_before_or_at
    return await self.request(
      request,
      method='GET',
      path='/v4/assetPositions',
      meta={'payload': 'positions'},
      validate=validate,
      request_type=Request,
      response_type=AssetPositionsResponse,
    )
