# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_dydx.indexer.data.core import IndexerMixin
from typed_dydx.indexer.schemas import ComplianceV2Response


class Request(TypedDict):
  address: str
  """EVM or dYdX address."""


class GetComplianceScreen(IndexerMixin):
  """Retrieve the compliance screening result for an address."""

  async def get_compliance_screen(
    self,
    address: str,
    *,
    validate: bool | None = None,
  ) -> ComplianceV2Response:
    """Retrieve the compliance screening result for an address.

    Args:
      address: EVM or dYdX address.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.dydx.xyz/indexer-client/http/utility#get-compliance-screen)
    """
    return await self.request(
      Request(address=address),
      method='GET',
      path='/v4/compliance/screen/{address}',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=ComplianceV2Response,
    )
