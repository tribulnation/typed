# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_dydx.core import TimestampIso
from typed_dydx.indexer.data.core import IndexerMixin
from typed_dydx.indexer.schemas import (
  HistoricalTradingRewardAggregationsResponse,
  TradingRewardAggregationPeriod,
)


class Request(TypedDict):
  address: str
  """Wallet address that owns the account."""
  period: TradingRewardAggregationPeriod
  limit: NotRequired[int]
  """Maximum number of results to return."""
  startingBeforeOrAt: NotRequired[TimestampIso]
  """Only include aggregations starting at or before this timestamp."""
  startingBeforeOrAtHeight: NotRequired[int]
  """Only include aggregations starting at or before this block height."""


class GetRewardsAggregated(IndexerMixin):
  """Get aggregated trading rewards"""

  async def get_rewards_aggregated(
    self,
    address: str,
    *,
    period: TradingRewardAggregationPeriod,
    limit: int | None = None,
    starting_before_or_at: TimestampIso | None = None,
    starting_before_or_at_height: int | None = None,
    validate: bool | None = None,
  ) -> HistoricalTradingRewardAggregationsResponse:
    """Get aggregated trading rewards

    Args:
      address: Wallet address that owns the account.
      period:
      limit: Maximum number of results to return.
      starting_before_or_at: Only include aggregations starting at or before this timestamp.
      starting_before_or_at_height: Only include aggregations starting at or before this block height.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.dydx.xyz/indexer-client/http/accounts#get-rewards-aggregated)
    """
    request: Request = Request(address=address, period=period)
    if limit is not None:
      request['limit'] = limit
    if starting_before_or_at is not None:
      request['startingBeforeOrAt'] = starting_before_or_at
    if starting_before_or_at_height is not None:
      request['startingBeforeOrAtHeight'] = starting_before_or_at_height
    return await self.request(
      request,
      method='GET',
      path='/v4/historicalTradingRewardAggregations/{address}',
      meta={'payload': 'rewards'},
      validate=validate,
      request_type=Request,
      response_type=HistoricalTradingRewardAggregationsResponse,
    )
