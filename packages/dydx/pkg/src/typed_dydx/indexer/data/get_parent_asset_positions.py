# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_dydx.indexer.data.core import IndexerMixin
from typed_dydx.indexer.schemas import AssetPositionsResponse


class Request(TypedDict):
  address: str
  """Wallet address that owns the account."""
  parentSubaccountNumber: int
  """Parent subaccount number."""


class GetParentAssetPositions(IndexerMixin):
  """Get parent asset positions"""

  async def get_parent_asset_positions(
    self,
    address: str,
    *,
    parent_subaccount: int,
    validate: bool | None = None,
  ) -> AssetPositionsResponse:
    """Get parent asset positions

    Args:
      address: Wallet address that owns the account.
      parent_subaccount: Parent subaccount number.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.dydx.xyz/indexer-client/http/accounts#get-parent-asset-positions)
    """
    return await self.request(
      Request(address=address, parentSubaccountNumber=parent_subaccount),
      method='GET',
      path='/v4/assetPositions/parentSubaccountNumber',
      meta={'payload': 'positions'},
      validate=validate,
      request_type=Request,
      response_type=AssetPositionsResponse,
    )
