# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_dydx.indexer.data.core import IndexerMixin
from typed_dydx.indexer.schemas import Order


class Request(TypedDict):
  orderId: str
  """Order id."""


class GetOrder(IndexerMixin):
  """Retrieve a single order by order id."""

  async def get_order(self, order_id: str, *, validate: bool | None = None) -> Order:
    """Retrieve a single order by order id.

    Args:
      order_id: Order id.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.dydx.xyz/indexer-client/http#get-order)
    """
    return await self.request(
      Request(orderId=order_id),
      method='GET',
      path='/v4/orders/{orderId}',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Order,
    )
