# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_dydx.indexer.data.core import IndexerMixin
from typed_dydx.indexer.schemas import GetMarketsResponse


class Request(TypedDict):
  market: NotRequired[str]
  """Specific market ticker to retrieve, for example `BTC-USD`."""
  limit: NotRequired[int]
  """Maximum number of market entries to return."""


class GetMarkets(IndexerMixin):
  """Retrieve perpetual market metadata from the indexer. When `market` is provided, the request filters to a single market."""

  async def get_markets(
    self,
    market: str | None = None,
    *,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> GetMarketsResponse:
    """Retrieve perpetual market metadata from the indexer. When `market` is provided, the request filters to a single market.

    Args:
      market: Specific market ticker to retrieve, for example `BTC-USD`.
      limit: Maximum number of market entries to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.dydx.xyz/indexer-client/http#get-perpetual-markets)
    """
    request: Request = Request()
    if market is not None:
      request['market'] = market
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/v4/perpetualMarkets',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=GetMarketsResponse,
    )
