# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_dydx.core import TimestampIso
from typed_dydx.indexer.data.core import IndexerMixin
from typed_dydx.indexer.schemas import FundingPaymentsForParentSubaccountResponse


class Request(TypedDict):
  address: str
  """Wallet address that owns the account."""
  parentSubaccountNumber: int
  """Parent subaccount number."""
  limit: NotRequired[int]
  """Maximum number of results to return."""
  afterOrAt: NotRequired[TimestampIso]
  """Restrict results to funding payments created at or after a specific timestamp."""
  page: NotRequired[int]
  """Page number for paginated results."""


class GetFundingPaymentsForParentSubaccount(IndexerMixin):
  """Get funding payments for parent subaccount"""

  async def get_funding_payments_for_parent_subaccount(
    self,
    address: str,
    *,
    parent_subaccount: int,
    limit: int | None = None,
    after_or_at: TimestampIso | None = None,
    page: int | None = None,
    validate: bool | None = None,
  ) -> FundingPaymentsForParentSubaccountResponse:
    """Get funding payments for parent subaccount

    Args:
      address: Wallet address that owns the account.
      parent_subaccount: Parent subaccount number.
      limit: Maximum number of results to return.
      after_or_at: Restrict results to funding payments created at or after a specific timestamp.
      page: Page number for paginated results.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.dydx.xyz/indexer-client/http/accounts#get-funding-payments-for-parent-subaccount)
    """
    request: Request = Request(
      address=address, parentSubaccountNumber=parent_subaccount
    )
    if limit is not None:
      request['limit'] = limit
    if after_or_at is not None:
      request['afterOrAt'] = after_or_at
    if page is not None:
      request['page'] = page
    return await self.request(
      request,
      method='GET',
      path='/v4/fundingPayments/parentSubaccount',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=FundingPaymentsForParentSubaccountResponse,
    )
