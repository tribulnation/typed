# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_dydx.core import TimestampIso
from typed_dydx.indexer.data.core import IndexerMixin
from typed_dydx.indexer.schemas import (
  ListOrdersResponse,
  OrderSide,
  OrderStatus,
  OrderType,
)


class Request(TypedDict):
  address: str
  """Wallet address that owns the subaccount."""
  subaccountNumber: int
  """Subaccount number."""
  limit: NotRequired[int]
  """Maximum number of orders to return."""
  ticker: NotRequired[str]
  """Ticker filter."""
  side: NotRequired[OrderSide]
  status: NotRequired[OrderStatus]
  type: NotRequired[OrderType]
  goodTilBlockBeforeOrAt: NotRequired[int]
  """Latest good-til-block to include."""
  goodTilBlockTimeBeforeOrAt: NotRequired[TimestampIso]
  """Latest good-til-block time to include."""
  returnLatestOrders: NotRequired[bool]
  """Whether to return only the latest orders."""


class ListOrders(IndexerMixin):
  """Retrieve orders for a subaccount with optional filters."""

  async def list_orders(
    self,
    *,
    address: str,
    subaccount: int,
    limit: int | None = None,
    ticker: str | None = None,
    side: OrderSide | None = None,
    status: OrderStatus | None = None,
    type: OrderType | None = None,
    good_til_block_before_or_at: int | None = None,
    good_til_block_time_before_or_at: TimestampIso | None = None,
    return_latest_orders: bool | None = None,
    validate: bool | None = None,
  ) -> ListOrdersResponse:
    """Retrieve orders for a subaccount with optional filters.

    Args:
      address: Wallet address that owns the subaccount.
      subaccount: Subaccount number.
      limit: Maximum number of orders to return.
      ticker: Ticker filter.
      side:
      status:
      type:
      good_til_block_before_or_at: Latest good-til-block to include.
      good_til_block_time_before_or_at: Latest good-til-block time to include.
      return_latest_orders: Whether to return only the latest orders.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.dydx.xyz/indexer-client/http#list-orders)
    """
    request: Request = Request(address=address, subaccountNumber=subaccount)
    if limit is not None:
      request['limit'] = limit
    if ticker is not None:
      request['ticker'] = ticker
    if side is not None:
      request['side'] = side
    if status is not None:
      request['status'] = status
    if type is not None:
      request['type'] = type
    if good_til_block_before_or_at is not None:
      request['goodTilBlockBeforeOrAt'] = good_til_block_before_or_at
    if good_til_block_time_before_or_at is not None:
      request['goodTilBlockTimeBeforeOrAt'] = good_til_block_time_before_or_at
    if return_latest_orders is not None:
      request['returnLatestOrders'] = return_latest_orders
    return await self.request(
      request,
      method='GET',
      path='/v4/orders',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=ListOrdersResponse,
    )
