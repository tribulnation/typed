# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_dydx.indexer.data.core import IndexerMixin
from typed_dydx.indexer.schemas import AddressResponse


class Request(TypedDict):
  address: str
  """Account address."""
  limit: NotRequired[int]
  """Maximum number of subaccounts to return."""


class GetSubaccounts(IndexerMixin):
  """Retrieve subaccounts for an address."""

  async def get_subaccounts(
    self,
    address: str,
    *,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> AddressResponse:
    """Retrieve subaccounts for an address.

    Args:
      address: Account address.
      limit: Maximum number of subaccounts to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.dydx.xyz/indexer-client/http#get-subaccounts)
    """
    request: Request = Request(address=address)
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/v4/addresses/{address}',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=AddressResponse,
    )
