# Generated by typed-dev — see CONTRIBUTING.md.
from typed_dydx.indexer.data.core import IndexerMixin
from typed_dydx.indexer.schemas import TimeResponse


class GetTime(IndexerMixin):
  """Retrieve the indexer's current wall-clock time."""

  async def get_time(self, *, validate: bool | None = None) -> TimeResponse:
    """Retrieve the indexer's current wall-clock time.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.dydx.xyz/indexer-client/http/utility#get-time)
    """
    return await self.request(
      None,
      method='GET',
      path='/v4/time',
      meta={},
      validate=validate,
      response_type=TimeResponse,
    )
