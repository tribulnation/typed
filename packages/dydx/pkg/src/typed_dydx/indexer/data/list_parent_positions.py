# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_dydx.indexer.data.core import IndexerMixin
from typed_dydx.indexer.schemas import ParentPositionsResponse


class Request(TypedDict):
  address: str
  """Wallet address that owns the account."""
  parentSubaccountNumber: int
  """Parent subaccount number."""
  limit: NotRequired[int]
  """Maximum number of results to return."""


class ListParentPositions(IndexerMixin):
  """List parent positions"""

  async def list_parent_positions(
    self,
    address: str,
    *,
    parent_subaccount: int,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> ParentPositionsResponse:
    """List parent positions

    Args:
      address: Wallet address that owns the account.
      parent_subaccount: Parent subaccount number.
      limit: Maximum number of results to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.dydx.xyz/indexer-client/http/accounts#list-parent-positions)
    """
    request: Request = Request(
      address=address, parentSubaccountNumber=parent_subaccount
    )
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/v4/perpetualPositions/parentSubaccountNumber',
      meta={'payload': 'positions'},
      validate=validate,
      request_type=Request,
      response_type=ParentPositionsResponse,
    )
