# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_dydx.indexer.data.core import IndexerMixin
from typed_dydx.indexer.schemas import GetSubaccountResponse


class Request(TypedDict):
  address: str
  """Account address."""
  subaccountNumber: int
  """Subaccount number."""


class GetSubaccount(IndexerMixin):
  """Retrieve a single subaccount."""

  async def get_subaccount(
    self,
    address: str,
    *,
    subaccount: int,
    validate: bool | None = None,
  ) -> GetSubaccountResponse:
    """Retrieve a single subaccount.

    Args:
      address: Account address.
      subaccount: Subaccount number.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.dydx.xyz/indexer-client/http#get-subaccount)
    """
    return await self.request(
      Request(address=address, subaccountNumber=subaccount),
      method='GET',
      path='/v4/addresses/{address}/subaccountNumber/{subaccountNumber}',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=GetSubaccountResponse,
    )
