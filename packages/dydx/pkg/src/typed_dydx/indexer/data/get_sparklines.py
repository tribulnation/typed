# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_dydx.indexer.data.core import IndexerMixin
from typed_dydx.indexer.schemas import Sparkline, SparklineTimePeriod


class Request(TypedDict):
  timePeriod: SparklineTimePeriod


class GetSparklines(IndexerMixin):
  """Retrieve market sparkline price series for the requested time period."""

  async def get_sparklines(
    self,
    time_period: SparklineTimePeriod,
    *,
    validate: bool | None = None,
  ) -> Sparkline:
    """Retrieve market sparkline price series for the requested time period.

    Args:
      time_period:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.dydx.xyz/indexer-client/http/markets#get-sparklines)
    """
    return await self.request(
      Request(timePeriod=time_period),
      method='GET',
      path='/v4/sparklines',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Sparkline,
    )
