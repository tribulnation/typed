# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_dydx.indexer.data.core import IndexerMixin
from typed_dydx.indexer.schemas import ComplianceResponse


class Request(TypedDict):
  address: str
  """Wallet address to screen."""


class GetScreen(IndexerMixin):
  """Screen an address and return whether it is restricted."""

  async def get_screen(
    self,
    address: str,
    *,
    validate: bool | None = None,
  ) -> ComplianceResponse:
    """Screen an address and return whether it is restricted.

    Args:
      address: Wallet address to screen.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.dydx.xyz/indexer-client/http/utility#get-screen)
    """
    return await self.request(
      Request(address=address),
      method='GET',
      path='/v4/screen',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=ComplianceResponse,
    )
