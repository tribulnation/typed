# Generated by typed-dev — see CONTRIBUTING.md.
from typed_dydx.indexer.data.core import IndexerMixin
from typed_dydx.indexer.schemas import HeightResponse


class GetHeight(IndexerMixin):
  """Retrieve the latest block height known by the indexer."""

  async def get_height(self, *, validate: bool | None = None) -> HeightResponse:
    """Retrieve the latest block height known by the indexer.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.dydx.xyz/indexer-client/http/utility#get-height)
    """
    return await self.request(
      None,
      method='GET',
      path='/v4/height',
      meta={},
      validate=validate,
      response_type=HeightResponse,
    )
