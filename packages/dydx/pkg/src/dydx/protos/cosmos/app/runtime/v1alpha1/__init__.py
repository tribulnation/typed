# Generated by the protocol buffer compiler.  DO NOT EDIT!
# sources: cosmos/app/runtime/v1alpha1/module.proto
# plugin: python-betterproto2
# This file has been @generated

__all__ = (
    "Module",
    "StoreKeyConfig",
)

from dataclasses import dataclass

import betterproto2

from dydx.protos.message_pool import default_message_pool

_COMPILER_VERSION = "0.9.0"
betterproto2.check_compiler_version(_COMPILER_VERSION)


@dataclass(eq=False, repr=False)
class Module(betterproto2.Message):
    """
    Module is the config object for the runtime module.
    """

    app_name: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    app_name is the name of the app.
    """

    begin_blockers: "list[str]" = betterproto2.field(
        2, betterproto2.TYPE_STRING, repeated=True
    )
    """
    begin_blockers specifies the module names of begin blockers
    to call in the order in which they should be called. If this is left empty
    no begin blocker will be registered.
    """

    end_blockers: "list[str]" = betterproto2.field(
        3, betterproto2.TYPE_STRING, repeated=True
    )
    """
    end_blockers specifies the module names of the end blockers
    to call in the order in which they should be called. If this is left empty
    no end blocker will be registered.
    """

    init_genesis: "list[str]" = betterproto2.field(
        4, betterproto2.TYPE_STRING, repeated=True
    )
    """
    init_genesis specifies the module names of init genesis functions
    to call in the order in which they should be called. If this is left empty
    no init genesis function will be registered.
    """

    export_genesis: "list[str]" = betterproto2.field(
        5, betterproto2.TYPE_STRING, repeated=True
    )
    """
    export_genesis specifies the order in which to export module genesis data.
    If this is left empty, the init_genesis order will be used for export genesis
    if it is specified.
    """

    override_store_keys: "list[StoreKeyConfig]" = betterproto2.field(
        6, betterproto2.TYPE_MESSAGE, repeated=True
    )
    """
    override_store_keys is an optional list of overrides for the module store keys
    to be used in keeper construction.
    """

    skip_store_keys: "list[str]" = betterproto2.field(
        11, betterproto2.TYPE_STRING, repeated=True
    )
    """
    skip_store_keys is an optional list of store keys to skip when constructing the
    module's keeper. This is useful when a module does not have a store key.
    NOTE: the provided environment variable will have a fake store service.
    """

    order_migrations: "list[str]" = betterproto2.field(
        7, betterproto2.TYPE_STRING, repeated=True
    )
    """
    order_migrations defines the order in which module migrations are performed.
    If this is left empty, it uses the default migration order.
    https://pkg.go.dev/github.com/cosmos/cosmos-sdk/types/module#DefaultMigrationsOrder
    """

    precommiters: "list[str]" = betterproto2.field(
        8, betterproto2.TYPE_STRING, repeated=True
    )
    """
    precommiters specifies the module names of the precommiters
    to call in the order in which they should be called. If this is left empty
    no precommit function will be registered.
    """

    prepare_check_staters: "list[str]" = betterproto2.field(
        9, betterproto2.TYPE_STRING, repeated=True
    )
    """
    prepare_check_staters specifies the module names of the prepare_check_staters
    to call in the order in which they should be called. If this is left empty
    no preparecheckstate function will be registered.
    """

    pre_blockers: "list[str]" = betterproto2.field(
        10, betterproto2.TYPE_STRING, repeated=True
    )
    """
    pre_blockers specifies the module names of pre blockers
    to call in the order in which they should be called. If this is left empty
    no pre blocker will be registered.
    """


default_message_pool.register_message("cosmos.app.runtime.v1alpha1", "Module", Module)


@dataclass(eq=False, repr=False)
class StoreKeyConfig(betterproto2.Message):
    """
    StoreKeyConfig may be supplied to override the default module store key, which
    is the module name.
    """

    module_name: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    name of the module to override the store key of
    """

    kv_store_key: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    the kv store key to use instead of the module name.
    """


default_message_pool.register_message(
    "cosmos.app.runtime.v1alpha1", "StoreKeyConfig", StoreKeyConfig
)
