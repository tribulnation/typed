# Generated by the protocol buffer compiler.  DO NOT EDIT!
# sources: cosmos/autocli/v1/options.proto, cosmos/autocli/v1/query.proto
# plugin: python-betterproto2
# This file has been @generated

__all__ = (
    "AppOptionsRequest",
    "AppOptionsResponse",
    "FlagOptions",
    "ModuleOptions",
    "PositionalArgDescriptor",
    "QueryStub",
    "RpcCommandOptions",
    "ServiceCommandDescriptor",
)

from dataclasses import dataclass
from typing import TYPE_CHECKING

import betterproto2
from betterproto2 import grpclib as betterproto2_grpclib

from dydx.protos.message_pool import default_message_pool

if TYPE_CHECKING:
    from betterproto2.grpclib.grpclib_client import MetadataLike
    from grpclib.metadata import Deadline

_COMPILER_VERSION = "0.9.0"
betterproto2.check_compiler_version(_COMPILER_VERSION)


@dataclass(eq=False, repr=False)
class AppOptionsRequest(betterproto2.Message):
    """
    AppOptionsRequest is the RemoteInfoService/AppOptions request type.
    """

    pass


default_message_pool.register_message(
    "cosmos.autocli.v1", "AppOptionsRequest", AppOptionsRequest
)


@dataclass(eq=False, repr=False)
class AppOptionsResponse(betterproto2.Message):
    """
    AppOptionsResponse is the RemoteInfoService/AppOptions response type.
    """

    module_options: "dict[str, ModuleOptions]" = betterproto2.field(
        1,
        betterproto2.TYPE_MAP,
        map_meta=betterproto2.map_meta(
            betterproto2.TYPE_STRING, betterproto2.TYPE_MESSAGE
        ),
    )
    """
    module_options is a map of module name to autocli module options.
    """


default_message_pool.register_message(
    "cosmos.autocli.v1", "AppOptionsResponse", AppOptionsResponse
)


@dataclass(eq=False, repr=False)
class FlagOptions(betterproto2.Message):
    """
    FlagOptions are options for flags generated from rpc request fields.
    By default, all request fields are configured as flags based on the
    kebab-case name of the field. Fields can be turned into positional arguments
    instead by using RpcCommandOptions.positional_args.
    """

    name: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    name is an alternate name to use for the field flag.
    """

    shorthand: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    shorthand is a one-letter abbreviated flag.
    """

    usage: "str" = betterproto2.field(3, betterproto2.TYPE_STRING)
    """
    usage is the help message.
    """

    default_value: "str" = betterproto2.field(4, betterproto2.TYPE_STRING)
    """
    default_value is the default value as text.
    """

    deprecated: "str" = betterproto2.field(6, betterproto2.TYPE_STRING)
    """
    deprecated is the usage text to show if this flag is deprecated.
    """

    shorthand_deprecated: "str" = betterproto2.field(7, betterproto2.TYPE_STRING)
    """
    shorthand_deprecated is the usage text to show if the shorthand of this flag is deprecated.
    """

    hidden: "bool" = betterproto2.field(8, betterproto2.TYPE_BOOL)
    """
    hidden hides the flag from help/usage text
    """


default_message_pool.register_message("cosmos.autocli.v1", "FlagOptions", FlagOptions)


@dataclass(eq=False, repr=False)
class ModuleOptions(betterproto2.Message):
    """
    ModuleOptions describes the CLI options for a Cosmos SDK module.
    """

    tx: "ServiceCommandDescriptor | None" = betterproto2.field(
        1, betterproto2.TYPE_MESSAGE, optional=True
    )
    """
    tx describes the tx commands for the module.
    """

    query: "ServiceCommandDescriptor | None" = betterproto2.field(
        2, betterproto2.TYPE_MESSAGE, optional=True
    )
    """
    query describes the queries commands for the module.
    """


default_message_pool.register_message(
    "cosmos.autocli.v1", "ModuleOptions", ModuleOptions
)


@dataclass(eq=False, repr=False)
class PositionalArgDescriptor(betterproto2.Message):
    """
    PositionalArgDescriptor describes a positional argument.
    """

    proto_field: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    proto_field specifies the proto field to use as the positional arg. Any
    fields used as positional args will not have a flag generated.
    """

    varargs: "bool" = betterproto2.field(2, betterproto2.TYPE_BOOL)
    """
    varargs makes a positional parameter a varargs parameter. This can only be
    applied to last positional parameter and the proto_field must a repeated
    field. Note: It is mutually exclusive with optional.
    """

    optional: "bool" = betterproto2.field(3, betterproto2.TYPE_BOOL)
    """
    optional makes the last positional parameter optional.
    Note: It is mutually exclusive with varargs.
    """


default_message_pool.register_message(
    "cosmos.autocli.v1", "PositionalArgDescriptor", PositionalArgDescriptor
)


@dataclass(eq=False, repr=False)
class RpcCommandOptions(betterproto2.Message):
    """
    RpcCommandOptions specifies options for commands generated from protobuf
    rpc methods.
    """

    rpc_method: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    rpc_method is short name of the protobuf rpc method that this command is
    generated from.
    """

    use: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    use is the one-line usage method. It also allows specifying an alternate
    name for the command as the first word of the usage text.

    By default the name of an rpc command is the kebab-case short name of the
    rpc method.
    """

    long: "str" = betterproto2.field(3, betterproto2.TYPE_STRING)
    """
    long is the long message shown in the 'help <this-command>' output.
    """

    short: "str" = betterproto2.field(4, betterproto2.TYPE_STRING)
    """
    short is the short description shown in the 'help' output.
    """

    example: "str" = betterproto2.field(5, betterproto2.TYPE_STRING)
    """
    example is examples of how to use the command.
    """

    alias: "list[str]" = betterproto2.field(6, betterproto2.TYPE_STRING, repeated=True)
    """
    alias is an array of aliases that can be used instead of the first word in Use.
    """

    suggest_for: "list[str]" = betterproto2.field(
        7, betterproto2.TYPE_STRING, repeated=True
    )
    """
    suggest_for is an array of command names for which this command will be suggested -
    similar to aliases but only suggests.
    """

    deprecated: "str" = betterproto2.field(8, betterproto2.TYPE_STRING)
    """
    deprecated defines, if this command is deprecated and should print this string when used.
    """

    version: "str" = betterproto2.field(9, betterproto2.TYPE_STRING)
    """
    version defines the version for this command. If this value is non-empty and the command does not
    define a "version" flag, a "version" boolean flag will be added to the command and, if specified,
    will print content of the "Version" variable. A shorthand "v" flag will also be added if the
    command does not define one.
    """

    flag_options: "dict[str, FlagOptions]" = betterproto2.field(
        10,
        betterproto2.TYPE_MAP,
        map_meta=betterproto2.map_meta(
            betterproto2.TYPE_STRING, betterproto2.TYPE_MESSAGE
        ),
    )
    """
    flag_options are options for flags generated from rpc request fields.
    By default all request fields are configured as flags. They can
    also be configured as positional args instead using positional_args.
    """

    positional_args: "list[PositionalArgDescriptor]" = betterproto2.field(
        11, betterproto2.TYPE_MESSAGE, repeated=True
    )
    """
    positional_args specifies positional arguments for the command.
    """

    skip: "bool" = betterproto2.field(12, betterproto2.TYPE_BOOL)
    """
    skip specifies whether to skip this rpc method when generating commands.
    """

    gov_proposal: "bool" = betterproto2.field(13, betterproto2.TYPE_BOOL)
    """
    gov_proposal specifies whether autocli should generate a gov proposal transaction for this rpc method.
    Normally autocli generates a transaction containing the message and broadcast it.
    However, when true, autocli generates a proposal transaction containing the message and broadcast it.
    This option is ineffective for query commands.
    """


default_message_pool.register_message(
    "cosmos.autocli.v1", "RpcCommandOptions", RpcCommandOptions
)


@dataclass(eq=False, repr=False)
class ServiceCommandDescriptor(betterproto2.Message):
    """
    ServiceCommandDescriptor describes a CLI command based on a protobuf service.
    """

    service: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    service is the fully qualified name of the protobuf service to build
    the command from. It can be left empty if sub_commands are used instead
    which may be the case if a module provides multiple tx and/or query services.
    """

    rpc_command_options: "list[RpcCommandOptions]" = betterproto2.field(
        2, betterproto2.TYPE_MESSAGE, repeated=True
    )
    """
    rpc_command_options are options for commands generated from rpc methods.
    If no options are specified for a given rpc method on the service, a
    command will be generated for that method with the default options.
    """

    sub_commands: "dict[str, ServiceCommandDescriptor]" = betterproto2.field(
        3,
        betterproto2.TYPE_MAP,
        map_meta=betterproto2.map_meta(
            betterproto2.TYPE_STRING, betterproto2.TYPE_MESSAGE
        ),
    )
    """
    sub_commands is a map of optional sub-commands for this command based on
    different protobuf services. The map key is used as the name of the
    sub-command.
    """

    enhance_custom_command: "bool" = betterproto2.field(4, betterproto2.TYPE_BOOL)
    """
    enhance_custom_commands specifies whether to skip the service when generating commands, if a custom command already
    exists, or enhance the existing command. If set to true, the custom command will be enhanced with the services from
    gRPC. otherwise when a custom command exists, no commands will be generated for the service.
    """

    short: "str" = betterproto2.field(5, betterproto2.TYPE_STRING)
    """
    short is an optional parameter used to override the short description of the auto generated command.
    """


default_message_pool.register_message(
    "cosmos.autocli.v1", "ServiceCommandDescriptor", ServiceCommandDescriptor
)


class QueryStub(betterproto2_grpclib.ServiceStub):
    """
    RemoteInfoService provides clients with the information they need
    to build dynamically CLI clients for remote chains.
    """

    async def app_options(
        self,
        message: "AppOptionsRequest | None" = None,
        *,
        timeout: "float | None" = None,
        deadline: "Deadline | None" = None,
        metadata: "MetadataLike | None" = None,
    ) -> "AppOptionsResponse":
        """
        AppOptions returns the autocli options for all of the modules in an app.
        """

        if message is None:
            message = AppOptionsRequest()

        return await self._unary_unary(
            "/cosmos.autocli.v1.Query/AppOptions",
            message,
            AppOptionsResponse,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )
