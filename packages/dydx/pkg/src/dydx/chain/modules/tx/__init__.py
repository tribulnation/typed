"""Cosmos tx module.

`get_tx`/`get_txs_event`/`simulate` are generated by `codegen/python.py`'s
`DydxGenerator` from `spec/endpoints/chain/tx/{get_tx,get_txs_event,simulate}/
endpoint.json`. `Broadcast` has no spec -- submitting a real signed transaction needs
the Node/wallet signing pipeline, out of scope for this backend -- and
`get_txs_event_paged` also stays hand-written -- see `get_txs_event_paged.py`'s own
docstring for why. Re-running codegen regenerates `get_tx.py`/`get_txs_event.py`/
`simulate.py` correctly but does not touch this file, `broadcast.py`, or
`get_txs_event_paged.py`; re-add the `Broadcast`/`get_txs_event_paged` (which already
composes `GetTxsEvent` itself, so it isn't listed separately) imports and bases below by
hand afterward.
"""

from .broadcast import Broadcast
from .get_tx import GetTx
from .get_txs_event_paged import GetTxsEventPaged
from .simulate import Simulate

from dydx.chain.core import GrpcEndpoint


class Tx(
  Broadcast,
  GetTx,
  GetTxsEventPaged,
  Simulate,
  GrpcEndpoint,
):
  """Tx endpoints."""
