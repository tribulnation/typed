"""Cosmos bank module.

`balance`/`all_balances` are generated by `codegen/python.py`'s `DydxGenerator` from
`spec/endpoints/chain/bank/{balance,all_balances}/endpoint.json`. The other seven methods
below have no spec yet and stay hand-written -- `typed-dev codegen python dydx` has no way
to know they exist, since nothing in `cli/codegen.py`'s router-generation loop consults
anything but declared endpoint specs. Re-running codegen regenerates `balance.py`/
`all_balances.py` correctly but overwrites this file down to just those two classes;
re-add the seven hand-written imports and bases below by hand afterward until they get
their own specs.
"""

from .all_balances import AllBalances
from .balance import Balance
from .denom_metadata import DenomMetadata
from .denom_metadata_by_query_string import DenomMetadataByQueryString
from .denoms_metadata import DenomsMetadata
from .params import Params
from .spendable_balances import SpendableBalances
from .supply import Supply
from .total_supply import TotalSupply

from dydx.chain.core import GrpcEndpoint


class Bank(
  AllBalances,
  Balance,
  DenomMetadata,
  DenomMetadataByQueryString,
  DenomsMetadata,
  Params,
  SpendableBalances,
  Supply,
  TotalSupply,
  GrpcEndpoint,
):
  """Cosmos bank query group."""
