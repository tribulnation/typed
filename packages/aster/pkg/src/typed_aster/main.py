# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .chain import Chain
from .futures import Futures
from .prediction import Prediction
from .spot import Spot
from typed_aster.core.base import AsterBase


class Aster(AsterBase):
  """Aster: a decentralized exchange for perpetual futures, spot and prediction markets, and its Aster Chain.

  References:
    - [Upstream docs](https://asterdex.github.io/aster-api-website/)
  """

  @cached_property
  def chain(self) -> Chain:
    """Aster Chain: REST (account privacy, staking, deposits, withdrawals and transfers) and the read-only JSON-RPC. Mainnet only.

    References:
      - [Upstream docs](https://asterdex.github.io/aster-api-website/aster-chain/endpoints/)
    """
    return Chain.new(self.chain_clients)

  @cached_property
  def futures(self) -> Futures:
    """Perpetual futures (fapi V3): market data, trading, account, positions, wallet and management, plus market and user-data streams.

    References:
      - [Upstream docs](https://asterdex.github.io/aster-api-website/futures-v3/general-info/)
    """
    return Futures.new(self.futures_clients)

  @cached_property
  def prediction(self) -> Prediction:
    """Prediction markets (papi V3): the spot API shape plus outcome-token mint/burn/split/merge and prediction positions, with market and user-data streams.

    References:
      - [Upstream docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md)
    """
    return Prediction.new(self.prediction_clients)

  @cached_property
  def spot(self) -> Spot:
    """Spot (sapi V3): market data, trading, account and wallet, plus market and user-data streams.

    References:
      - [Upstream docs](https://asterdex.github.io/aster-api-website/spot-v3/general-info/)
    """
    return Spot.new(self.spot_clients)
