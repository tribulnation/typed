# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_aster.core import TimestampMillis
from typed_aster.core.endpoint.rpc import RpcEndpoint


class PredictionPosition(TypedDict):
  """One open position."""

  marketName: str
  """Market name."""
  symbol: str
  """Outcome symbol."""
  assetName: NotRequired[str]
  """Settlement asset, e.g. `USDT`."""
  type: Literal['YES', 'NO']
  """Position direction."""
  openAvgPrice: NotRequired[Decimal]
  """Average open price."""
  cumQty: NotRequired[Decimal]
  """Total position quantity."""
  closeAvgPrice: NotRequired[Decimal]
  """Average close price; `0` when not closed."""
  realizedPnl: NotRequired[Decimal]
  """Realized PnL."""
  closeQty: NotRequired[Decimal]
  """Closed quantity."""
  insertTime: NotRequired[TimestampMillis]
  """Position open time."""
  commissionFee: NotRequired[Decimal]
  """Commission paid."""
  commissionAsset: NotRequired[str]
  """Commission asset."""
  balance: NotRequired[Decimal]
  """Position value (quantity x average open price)."""
  availableBalance: NotRequired[Decimal]
  """Available balance."""


class Request(TypedDict):
  """Positions request."""

  symbol: NotRequired[str]
  """Outcome symbol; every position when omitted."""


Response = list[PredictionPosition]


class List(RpcEndpoint):
  """Get the account's current prediction positions."""

  async def list(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Get the account's current prediction positions.

    Args:
      symbol: Outcome symbol; every position when omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#query-prediction-market-current-positions-user_data)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='prediction/positions',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
