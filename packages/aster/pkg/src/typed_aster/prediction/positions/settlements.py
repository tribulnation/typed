# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_aster.core import TimestampMillis
from typed_aster.core.endpoint.rpc import RpcEndpoint


class PredictionSettlement(TypedDict):
  """One settlement."""

  marketName: str
  """Market name."""
  asset: NotRequired[str]
  """Outcome token asset."""
  symbol: str
  """Outcome symbol."""
  marketStartTime: NotRequired[TimestampMillis]
  """Market start time."""
  marketEndTime: NotRequired[TimestampMillis]
  """Market end (settlement) time."""
  shareAmount: NotRequired[Decimal]
  """Shares held at settlement."""
  settleAsset: NotRequired[str]
  """Settlement asset."""
  settleAmount: NotRequired[Decimal]
  """Gross settlement payout."""
  settleFeeAmount: NotRequired[Decimal]
  """Settlement fee, deducted from the payout."""
  entryPrice: NotRequired[Decimal]
  """Average entry price."""
  settlePrice: NotRequired[Decimal]
  """Final settlement price."""
  realizedPnl: NotRequired[Decimal]
  """Realized PnL (`settleAmount` minus cost basis)."""
  status: NotRequired[str]
  """Settlement status, e.g. `settled`."""


class Request(TypedDict):
  """Settlement history request."""

  symbol: NotRequired[str]
  """Outcome symbol; every record when omitted."""
  startTime: NotRequired[TimestampMillis]
  """Filter by market start time; not after `endTime`."""
  endTime: NotRequired[TimestampMillis]
  """Filter by market end time."""
  limit: NotRequired[int]
  """Number of rows, 1 to 1000."""


Response = list[PredictionSettlement]


class Settlements(RpcEndpoint):
  """Get the account's settlement records of expired and settled markets."""

  async def settlements(
    self,
    symbol: str | None = None,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get the account's settlement records of expired and settled markets.

    Args:
      symbol: Outcome symbol; every record when omitted.
      start_time: Filter by market start time; not after `endTime`.
      end_time: Filter by market end time.
      limit: Number of rows, 1 to 1000.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#query-prediction-market-settlement-histories-user_data)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='prediction/settlementHistories',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
