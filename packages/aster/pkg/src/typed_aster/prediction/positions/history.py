# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_aster.core import TimestampMillis
from typed_aster.core.endpoint.rpc import RpcEndpoint


class PredictionPositionHistory(TypedDict):
  """One closed or settled position."""

  marketName: str
  """Market name."""
  symbol: str
  """Outcome symbol."""
  assetName: NotRequired[str]
  """Settlement asset, e.g. `USDT`."""
  type: Literal['YES', 'NO']
  """Position direction."""
  openAvgPrice: NotRequired[Decimal]
  """Average open price."""
  cumQty: NotRequired[Decimal]
  """Total position quantity."""
  closeAvgPrice: NotRequired[Decimal]
  """Average close price; `0` when not closed."""
  realizedPnl: NotRequired[Decimal]
  """Realized PnL."""
  closeQty: NotRequired[Decimal]
  """Closed quantity."""
  insertTime: NotRequired[TimestampMillis]
  """Position open time."""
  commissionFee: NotRequired[Decimal]
  """Commission paid."""
  commissionAsset: NotRequired[str]
  """Commission asset."""
  closeTime: NotRequired[TimestampMillis]
  """Close or settlement time."""
  status: Literal['close', 'settled']
  """`close` (closed by the user) or `settled` (market settled)."""
  settleFee: NotRequired[Decimal]
  """Settlement fee; only when `status` is `settled`."""
  settleAsset: NotRequired[str]
  """Settlement fee asset."""


class Request(TypedDict):
  """Position history request."""

  symbol: NotRequired[str]
  """Outcome symbol; every position when omitted."""
  startTime: NotRequired[TimestampMillis]
  """Start of the time range; not after `endTime`."""
  endTime: NotRequired[TimestampMillis]
  """End of the time range."""
  limit: NotRequired[int]
  """Number of rows, 1 to 1000."""


Response = list[PredictionPositionHistory]


class History(RpcEndpoint):
  """Get the account's closed or settled prediction positions."""

  async def history(
    self,
    symbol: str | None = None,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get the account's closed or settled prediction positions.

    Args:
      symbol: Outcome symbol; every position when omitted.
      start_time: Start of the time range; not after `endTime`.
      end_time: End of the time range.
      limit: Number of rows, 1 to 1000.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#query-prediction-market-position-history-user_data)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='prediction/positionHistories',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
