# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any
from typed_aster.core.endpoint.user_stream import UserStreamEndpoint
from typed_aster.schemas import ExecutionReport, OutboundAccountPosition


Payload = OutboundAccountPosition | ExecutionReport


class Events(UserStreamEndpoint):
  """Account and order events for the listenKey's account."""

  def events(
    self,
    listen_key: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[Payload, Any, Any]:
    """Account and order events for the listenKey's account.

    Args:
      listen_key: listenKey from `listen_key.start`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#payload-account_update)
    """
    return self.subscribe(
      listen_key,
      validate=validate,
      response_type=Payload,
    )
