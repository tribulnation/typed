# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import TransferResult


class Request(TypedDict):
  """Transfer request."""

  amount: Decimal
  """Amount."""
  asset: str
  """Asset."""
  clientTranId: str
  """Client transaction ID."""
  kindType: Literal['FUTURE_SPOT', 'SPOT_FUTURE']
  """Direction: `FUTURE_SPOT` (futures to spot) or `SPOT_FUTURE` (spot to futures)."""


class Transfer(RpcEndpoint):
  """Transfer an asset between the futures (perp) wallet and the spot wallet that backs prediction trading."""

  async def transfer(
    self,
    amount: Decimal,
    *,
    asset: str,
    client_tran_id: str,
    kind_type: Literal['FUTURE_SPOT', 'SPOT_FUTURE'],
    validate: bool | None = None,
  ) -> TransferResult:
    """Transfer an asset between the futures (perp) wallet and the spot wallet that backs prediction trading.

    Args:
      amount: Amount.
      asset: Asset.
      client_tran_id: Client transaction ID.
      kind_type: Direction: `FUTURE_SPOT` (futures to spot) or `SPOT_FUTURE` (spot to futures).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#perp-spot-transfer-trade)
    """
    return await self.request(
      Request(
        amount=amount, asset=asset, clientTranId=client_tran_id, kindType=kind_type
      ),
      method='POST',
      path='asset/wallet/transfer',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=TransferResult,
    )
