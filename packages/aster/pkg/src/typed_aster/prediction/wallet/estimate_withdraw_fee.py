# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import WithdrawFeeEstimate


class Request(TypedDict):
  """Fee estimate request."""

  chainId: str
  """Destination chain ID: `1` (ETH), `56` (BSC), `42161` (Arbitrum)."""
  asset: str
  """Asset."""


class EstimateWithdrawFee(RpcEndpoint):
  """Estimate the withdrawal fee for an asset on a chain."""

  async def estimate_withdraw_fee(
    self,
    *,
    chain_id: str,
    asset: str,
    validate: bool | None = None,
  ) -> WithdrawFeeEstimate:
    """Estimate the withdrawal fee for an asset on a chain.

    Args:
      chain_id: Destination chain ID: `1` (ETH), `56` (BSC), `42161` (Arbitrum).
      asset: Asset.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#get-withdraw-fee-none)
    """
    return await self.request(
      Request(chainId=chain_id, asset=asset),
      method='GET',
      path='aster/withdraw/estimateFee',
      meta={'sign': 'none'},
      validate=validate,
      request_type=Request,
      response_type=WithdrawFeeEstimate,
    )
