# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any
from typed_aster.core.endpoint.stream import StreamEndpoint
from typed_aster.schemas import MiniTickerEvent


class MiniTicker(StreamEndpoint):
  """Rolling 24-hour mini ticker for one symbol."""

  def mini_ticker(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[MiniTickerEvent, Any, Any]:
    """Rolling 24-hour mini ticker for one symbol.

    Args:
      symbol: Symbol, lowercase, e.g. `btcusdt`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#simplified-ticker-by-symbol)
    """
    return self.subscribe(
      f'{symbol}@miniTicker',
      validate=validate,
      response_type=MiniTickerEvent,
    )
