# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any
from typed_aster.core.endpoint.stream import StreamEndpoint
from typed_aster.schemas import TradeEvent


class Trade(StreamEndpoint):
  """Every trade for one symbol, in real time."""

  def trade(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[TradeEvent, Any, Any]:
    """Every trade for one symbol, in real time.

    Args:
      symbol: Symbol, lowercase, e.g. `btcusdt`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#tick-by-tick-trades)
    """
    return self.subscribe(
      f'{symbol}@trade',
      validate=validate,
      response_type=TradeEvent,
    )
