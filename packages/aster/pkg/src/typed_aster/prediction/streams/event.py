# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_aster.core import TimestampMillis
from typed_aster.core.endpoint.stream import StreamEndpoint


class MarketStartEvent(TypedDict):
  """A round started (`market_start`): its initial oracle price."""

  e: Literal['market_start']
  """Event type."""
  m: str
  """Market (round) name, e.g. `BTC_UP_DOWN_5M_1790237400`."""
  y: str
  """UP (YES) symbol."""
  n: str
  """DOWN (NO) symbol."""
  p: Decimal
  """Initial oracle price."""
  t: TimestampMillis
  """Push time."""


class MarketTwapEvent(TypedDict):
  """One TWAP sample of a round (`market_twap`)."""

  e: Literal['market_twap']
  """Event type."""
  m: str
  """Market (round) name, e.g. `BTC_UP_DOWN_5M_1790237400`."""
  y: str
  """UP (YES) symbol."""
  n: str
  """DOWN (NO) symbol."""
  p: Decimal
  """Sampled oracle price."""
  r: int
  """Remaining samples."""
  t: TimestampMillis
  """Push time."""
  w: NotRequired[Literal['Y', 'N']]
  """Winning side, only on the last sample: `Y` (UP) or `N` (DOWN)."""


Payload = MarketStartEvent | MarketTwapEvent


class Event(StreamEndpoint):
  """Round start prices and TWAP settlement samples of an up/down prediction event."""

  def event(
    self,
    event_name: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[Payload, Any, Any]:
    """Round start prices and TWAP settlement samples of an up/down prediction event.

    Args:
      event_name: Event name, lowercase, e.g. `btc_up_down_5m`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#event-prediction-event-twap-update)
    """
    return self.subscribe(
      f'{event_name}@event',
      validate=validate,
      response_type=Payload,
    )
