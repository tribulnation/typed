# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal
from typed_aster.core.endpoint.stream import StreamEndpoint
from typed_aster.schemas import DepthUpdate


class DiffDepth(StreamEndpoint):
  """Order book changes for one symbol; use with `market.depth` to maintain a local book."""

  def diff_depth(
    self,
    symbol: str,
    *,
    speed: Literal['100ms', '500ms'],
    validate: bool | None = None,
  ) -> StreamManager[DepthUpdate, Any, Any]:
    """Order book changes for one symbol; use with `market.depth` to maintain a local book.

    Args:
      symbol: Symbol, lowercase, e.g. `btcusdt`.
      speed: Update speed.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#incremental-depth-information)
    """
    return self.subscribe(
      f'{symbol}@depth@{speed}',
      validate=validate,
      response_type=DepthUpdate,
    )
