# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class PredictionCommissionRate(TypedDict):
  """Commission rates for one symbol."""

  symbol: str
  """Trading pair, e.g. `BTCUSDT`."""
  makerCommissionRate: Decimal
  """Maker commission rate."""
  takerCommissionRate: Decimal
  """Taker commission rate."""
  settleCommissionRate: NotRequired[Decimal]
  """Settlement commission rate; present on prediction symbols, absent on the spot symbols papi also serves."""


class Request(TypedDict):
  """Commission rate request."""

  symbol: str
  """Trading pair, e.g. `BTCUSDT`."""


class CommissionRate(RpcEndpoint):
  """Get the account's commission rates for a symbol."""

  async def commission_rate(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> PredictionCommissionRate:
    """Get the account's commission rates for a symbol.

    Args:
      symbol: Trading pair, e.g. `BTCUSDT`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#get-symbol-fees)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='commissionRate',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=PredictionCommissionRate,
    )
