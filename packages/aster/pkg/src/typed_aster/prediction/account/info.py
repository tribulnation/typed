# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import AccountInfo


class Request(TypedDict): ...


class Info(RpcEndpoint):
  """Get the current account information and balances."""

  async def info(self, *, validate: bool | None = None) -> AccountInfo:
    """Get the current account information and balances.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#account-information-user_data)
    """
    return await self.request(
      Request(),
      method='GET',
      path='account',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=AccountInfo,
    )
