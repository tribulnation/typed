# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_aster.core import TimestampMillis
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import AccountTransaction


class Request(TypedDict):
  """Transaction history request."""

  asset: NotRequired[str]
  """Asset."""
  type: NotRequired[
    Literal[
      'TRADE_TARGET',
      'TRADE_SOURCE',
      'TRANSFER_SPOT_TO_FUTURE',
      'TRANSFER_FUTURE_TO_SPOT',
      'TRANSFER_SPOT_TO_SPOT',
      'AIRDROP',
      'DIVIDEND',
      'TRANSFER_REFUND',
      'INTERNAL_TRANSFER',
      'TRANSFER',
      'SWAP',
      'COMMISSION_REBATE',
      'CASH_BACK',
      'STAKING_WITHDRAW',
      'STAKING_CLAIM',
      'STAKING_DELEGATE',
      'PREDICTION_CLAIM',
      'PREDICTION_USER_REBATE',
      'PREDICTION_SETTLE',
      'PREDICTION_SETTLE_FEE',
      'PREDICTION_MINT',
      'PREDICTION_BURN',
    ]
  ]
  """Transaction type."""
  startTime: NotRequired[TimestampMillis]
  """Start of the time range."""
  endTime: NotRequired[TimestampMillis]
  """End of the time range."""
  limit: NotRequired[int]
  """Number of records; maximum 1000."""


Response = list[AccountTransaction]


class TransactionHistory(RpcEndpoint):
  """Get balance transaction records (trades, transfers, mints, settlements, ...); the last 7 days when no time range is given."""

  async def transaction_history(
    self,
    asset: str | None = None,
    *,
    type: Literal[
      'TRADE_TARGET',
      'TRADE_SOURCE',
      'TRANSFER_SPOT_TO_FUTURE',
      'TRANSFER_FUTURE_TO_SPOT',
      'TRANSFER_SPOT_TO_SPOT',
      'AIRDROP',
      'DIVIDEND',
      'TRANSFER_REFUND',
      'INTERNAL_TRANSFER',
      'TRANSFER',
      'SWAP',
      'COMMISSION_REBATE',
      'CASH_BACK',
      'STAKING_WITHDRAW',
      'STAKING_CLAIM',
      'STAKING_DELEGATE',
      'PREDICTION_CLAIM',
      'PREDICTION_USER_REBATE',
      'PREDICTION_SETTLE',
      'PREDICTION_SETTLE_FEE',
      'PREDICTION_MINT',
      'PREDICTION_BURN',
    ]
    | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get balance transaction records (trades, transfers, mints, settlements, ...); the last 7 days when no time range is given.

    Args:
      asset: Asset.
      type: Transaction type.
      start_time: Start of the time range.
      end_time: End of the time range.
      limit: Number of records; maximum 1000.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#query-all-orders-user_data)
    """
    request: Request = Request()
    if asset is not None:
      request['asset'] = asset
    if type is not None:
      request['type'] = type
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='prediction/transactionHistory',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
