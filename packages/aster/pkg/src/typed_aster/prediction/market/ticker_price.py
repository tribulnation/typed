# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.prediction.market.schemas import PredictionPriceTicker


class Request(TypedDict):
  """Latest price request."""

  symbol: NotRequired[str]
  """Trading pair; every symbol when omitted (array response, higher weight)."""


Response = PredictionPriceTicker | list[PredictionPriceTicker]


class TickerPrice(RpcEndpoint):
  """Get the latest price for one or every symbol."""

  async def ticker_price(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Get the latest price for one or every symbol.

    Args:
      symbol: Trading pair; every symbol when omitted (array response, higher weight).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#latest-price)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='ticker/price',
      meta={'sign': 'none'},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
