# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import OrderBook


class Request(TypedDict):
  """Order book request."""

  symbol: str
  """Trading pair, e.g. `BTCUSDT`."""
  limit: NotRequired[Literal[5, 10, 20, 50, 100, 500, 1000]]
  """Number of levels per side."""


class Depth(RpcEndpoint):
  """Get the order book."""

  async def depth(
    self,
    symbol: str,
    *,
    limit: Literal[5, 10, 20, 50, 100, 500, 1000] | None = None,
    validate: bool | None = None,
  ) -> OrderBook:
    """Get the order book.

    Args:
      symbol: Trading pair, e.g. `BTCUSDT`.
      limit: Number of levels per side.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#depth-information)
    """
    request: Request = Request(symbol=symbol)
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='depth',
      meta={'sign': 'none'},
      validate=validate,
      request_type=Request,
      response_type=OrderBook,
    )
