# Generated by typed-dev — see CONTRIBUTING.md.
"""Shapes shared by several endpoints of `prediction.market`."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_aster.core import TimestampMillis
from decimal import Decimal


class PredictionBookTicker(TypedDict):
  """Best bid and ask for one symbol."""

  symbol: str
  """Trading pair, e.g. `BTCUSDT`."""
  bidPrice: Decimal
  """Best bid price."""
  bidQty: Decimal
  """Best bid quantity."""
  askPrice: Decimal
  """Best ask price."""
  askQty: Decimal
  """Best ask quantity."""
  time: TimestampMillis
  """Time of the quote."""


class PredictionMarket(TypedDict):
  """One prediction market (a round of an up/down event, or one outcome of a multi-outcome event)."""

  marketName: str
  """Market (round) name, e.g. `BTC_UP_DOWN_5M_1790237100` or `EVENT4_ALGERIA_WIN`."""
  symbols: list[str]
  """The two outcome symbols: `..._YUSDT` (YES/UP) and `..._NUSDT` (NO/DOWN)."""
  assets: list[str]
  """The two outcome assets: `..._Y` (YES/UP) and `..._N` (NO/DOWN)."""
  roundId: int
  """Round ID; `0` for a multi-outcome market."""
  tradingStartTime: TimestampMillis
  """Trading start time."""
  tradingEndTime: TimestampMillis
  """Trading end time; `0` when no end is set."""
  windowStartTime: TimestampMillis
  """Observation window start time."""
  windowEndTime: TimestampMillis
  """Observation window end time; `0` when no end is set."""
  roundStatus: Literal['PREMARKET', 'TRADING', 'SETTLED']
  """Market status."""
  openPrice: Decimal | None
  """Opening oracle price; `null` until known."""
  endPrice: Decimal | None
  """Ending oracle price; `null` before settlement."""
  winAsset: str
  """Winning asset (`..._Y` if YES/UP wins, `..._N` if NO/DOWN); set before settlement too, so only meaningful once `SETTLED`."""
  settleTime: TimestampMillis
  """Settlement time; `0` when not settled."""
  settleHash: str | None
  """On-chain settlement hash; `null` when not settled."""


class PredictionPriceTicker(TypedDict):
  """Latest price for one symbol."""

  symbol: str
  """Trading pair, e.g. `BTCUSDT`."""
  price: Decimal
  """Latest price."""
  type: NotRequired[str]
  """Price source; `listing_price` on prediction symbols that have not traded yet (undocumented, absent otherwise)."""
  time: TimestampMillis
  """Time of the price."""


class PredictionTicker24hr(TypedDict):
  """24-hour rolling window statistics for one symbol."""

  symbol: str
  """Trading pair, e.g. `BTCUSDT`."""
  priceChange: Decimal
  """Price change."""
  priceChangePercent: Decimal
  """Price change percent."""
  weightedAvgPrice: Decimal
  """Weighted average price."""
  lastPrice: Decimal
  """Last price."""
  lastQty: Decimal
  """Last quantity."""
  openPrice: Decimal
  """Open price."""
  highPrice: Decimal
  """High price."""
  lowPrice: Decimal
  """Low price."""
  volume: Decimal
  """Base asset volume."""
  quoteVolume: Decimal
  """Quote asset volume."""
  openTime: TimestampMillis
  """Window open time."""
  closeTime: TimestampMillis
  """Window close time."""
  firstId: int
  """First trade ID."""
  lastId: int
  """Last trade ID."""
  count: int
  """Number of trades."""
  baseAsset: NotRequired[str]
  """Base asset; absent on prediction symbols in the all-symbols list."""
  quoteAsset: NotRequired[str]
  """Quote asset; absent on prediction symbols in the all-symbols list."""
  bidPrice: NotRequired[Decimal]
  """Best bid price; absent on prediction symbols in the all-symbols list."""
  bidQty: NotRequired[Decimal]
  """Best bid quantity; absent on prediction symbols in the all-symbols list."""
  askPrice: NotRequired[Decimal]
  """Best ask price; absent on prediction symbols in the all-symbols list."""
  askQty: NotRequired[Decimal]
  """Best ask quantity; absent on prediction symbols in the all-symbols list."""
