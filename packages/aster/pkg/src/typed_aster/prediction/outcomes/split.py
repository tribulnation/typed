# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.prediction.outcomes.schemas import PredictionOutcomeCounterparty


class Request(TypedDict):
  """Split request."""

  event: str
  """Event identifier, e.g. `EVENT4` (`eventId` in exchange info)."""
  symbol: str
  """YES or NO symbol of the target market (status `TRADING` or `PENDING_TRADING`)."""
  quantity: Decimal
  """Number of outcome token sets to receive; a positive whole number."""
  newClientOrderId: NotRequired[str]
  """Client order ID; generated when omitted."""


class OutcomeSplit(TypedDict):
  """A completed split."""

  clientOrderId: str
  """Client order ID."""
  qty: Decimal
  """Quantity of outcome tokens received per outcome."""
  counterparties: list[PredictionOutcomeCounterparty]
  """Outcome tokens involved and their last prices."""
  sourceNoLastPrice: NotRequired[Decimal]
  """Last price of the source market's NO token."""


class Split(RpcEndpoint):
  """Convert quote asset into outcome tokens of one market of a multi-outcome event."""

  async def split(
    self,
    *,
    event: str,
    symbol: str,
    quantity: Decimal,
    new_client_order_id: str | None = None,
    validate: bool | None = None,
  ) -> OutcomeSplit:
    """Convert quote asset into outcome tokens of one market of a multi-outcome event.

    Args:
      event: Event identifier, e.g. `EVENT4` (`eventId` in exchange info).
      symbol: YES or NO symbol of the target market (status `TRADING` or `PENDING_TRADING`).
      quantity: Number of outcome token sets to receive; a positive whole number.
      new_client_order_id: Client order ID; generated when omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#prediction-market-split-trade)
    """
    request: Request = Request(event=event, symbol=symbol, quantity=quantity)
    if new_client_order_id is not None:
      request['newClientOrderId'] = new_client_order_id
    return await self.request(
      request,
      method='POST',
      path='prediction/split',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=OutcomeSplit,
    )
