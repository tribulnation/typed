# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class OutcomeMint(TypedDict):
  """A completed mint."""

  clientOrderId: str
  """Client order ID."""
  quoteAmount: Decimal
  """Total quote asset spent."""
  yesPrice: NotRequired[Decimal | None]
  """YES token price at mint time (`yesPrice + noPrice = 1`)."""
  noPrice: NotRequired[Decimal | None]
  """NO token price at mint time."""
  pairCount: Decimal
  """Number of YES+NO pairs minted."""


class Request(TypedDict):
  """Mint request."""

  symbol: str
  """The YES symbol of the market (status `TRADING` or `PENDING_TRADING`)."""
  quantity: Decimal
  """Number of YES+NO pairs to mint."""
  newClientOrderId: NotRequired[str]
  """Client order ID; generated when omitted."""


class Mint(RpcEndpoint):
  """Mint YES+NO token pairs: spend quote asset at the current prices and receive equal amounts of both outcome tokens."""

  async def mint(
    self,
    *,
    symbol: str,
    quantity: Decimal,
    new_client_order_id: str | None = None,
    validate: bool | None = None,
  ) -> OutcomeMint:
    """Mint YES+NO token pairs: spend quote asset at the current prices and receive equal amounts of both outcome tokens.

    Args:
      symbol: The YES symbol of the market (status `TRADING` or `PENDING_TRADING`).
      quantity: Number of YES+NO pairs to mint.
      new_client_order_id: Client order ID; generated when omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#prediction-market-mint-trade)
    """
    request: Request = Request(symbol=symbol, quantity=quantity)
    if new_client_order_id is not None:
      request['newClientOrderId'] = new_client_order_id
    return await self.request(
      request,
      method='POST',
      path='prediction/mint',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=OutcomeMint,
    )
