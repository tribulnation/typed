# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class OutcomeBurn(TypedDict):
  """A completed burn."""

  clientOrderId: str
  """Client order ID."""
  quoteAmount: Decimal
  """Total quote asset returned."""
  yesPrice: NotRequired[Decimal | None]
  """Always `null` for a burn."""
  noPrice: NotRequired[Decimal | None]
  """Always `null` for a burn."""
  pairCount: Decimal
  """Number of YES+NO pairs burned."""


class Request(TypedDict):
  """Burn request."""

  symbol: str
  """The YES symbol of the market (status `TRADING`, `PENDING_TRADING` or `CLOSED`)."""
  quantity: Decimal
  """Number of YES+NO pairs to burn."""
  newClientOrderId: NotRequired[str]
  """Client order ID; generated when omitted."""


class Burn(RpcEndpoint):
  """Burn equal amounts of YES and NO tokens to redeem the quote asset; also allowed once the market is `CLOSED`."""

  async def burn(
    self,
    *,
    symbol: str,
    quantity: Decimal,
    new_client_order_id: str | None = None,
    validate: bool | None = None,
  ) -> OutcomeBurn:
    """Burn equal amounts of YES and NO tokens to redeem the quote asset; also allowed once the market is `CLOSED`.

    Args:
      symbol: The YES symbol of the market (status `TRADING`, `PENDING_TRADING` or `CLOSED`).
      quantity: Number of YES+NO pairs to burn.
      new_client_order_id: Client order ID; generated when omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#prediction-market-burn-trade)
    """
    request: Request = Request(symbol=symbol, quantity=quantity)
    if new_client_order_id is not None:
      request['newClientOrderId'] = new_client_order_id
    return await self.request(
      request,
      method='POST',
      path='prediction/burn',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=OutcomeBurn,
    )
