# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.prediction.outcomes.schemas import PredictionOutcomeCounterparty


class Request(TypedDict):
  """Merge request."""

  event: str
  """Event identifier, e.g. `EVENT4` (`eventId` in exchange info)."""
  quantity: Decimal
  """Number of outcome token sets to submit; a positive whole number."""
  newClientOrderId: NotRequired[str]
  """Client order ID; generated when omitted."""


class OutcomeMerge(TypedDict):
  """A completed merge."""

  clientOrderId: str
  """Client order ID."""
  qty: Decimal
  """Total quote asset returned."""
  counterparties: list[PredictionOutcomeCounterparty]
  """Outcome tokens consumed and their last prices."""


class Merge(RpcEndpoint):
  """Return a full set of outcome tokens of a multi-outcome event to redeem the quote asset."""

  async def merge(
    self,
    *,
    event: str,
    quantity: Decimal,
    new_client_order_id: str | None = None,
    validate: bool | None = None,
  ) -> OutcomeMerge:
    """Return a full set of outcome tokens of a multi-outcome event to redeem the quote asset.

    Args:
      event: Event identifier, e.g. `EVENT4` (`eventId` in exchange info).
      quantity: Number of outcome token sets to submit; a positive whole number.
      new_client_order_id: Client order ID; generated when omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#prediction-market-merge-trade)
    """
    request: Request = Request(event=event, quantity=quantity)
    if new_client_order_id is not None:
      request['newClientOrderId'] = new_client_order_id
    return await self.request(
      request,
      method='POST',
      path='prediction/merge',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=OutcomeMerge,
    )
