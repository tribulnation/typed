# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import OperationResult


class Request(TypedDict):
  nonce: NotRequired[int]
  """The nonce to burn, in epoch microseconds: pass the nonce of a pending signed request to invalidate it (a fast cancel). Omit to burn a freshly drawn nonce."""


class Noop(RpcEndpoint):
  """Burn a nonce, so previously sent requests still queued with it are cancelled (best effort)."""

  async def noop(
    self,
    nonce: int | None = None,
    *,
    validate: bool | None = None,
  ) -> OperationResult:
    """Burn a nonce, so previously sent requests still queued with it are cancelled (best effort).

    Args:
      nonce: The nonce to burn, in epoch microseconds: pass the nonce of a pending signed request to invalidate it (a fast cancel). Omit to burn a freshly drawn nonce.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#noop)
    """
    request: Request = Request()
    if nonce is not None:
      request['nonce'] = nonce
    return await self.request(
      request,
      method='POST',
      path='noop',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=OperationResult,
    )
