# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.prediction.trade.schemas import PredictionOrder


class Request(TypedDict):
  """Open orders request."""

  symbol: NotRequired[str]
  """Trading pair; every symbol when omitted (higher weight)."""


Response = list[PredictionOrder]


class OpenOrders(RpcEndpoint):
  """Get all open orders, for one symbol or every symbol."""

  async def open_orders(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Get all open orders, for one symbol or every symbol.

    Args:
      symbol: Trading pair; every symbol when omitted (higher weight).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#current-open-orders-user_data)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='openOrders',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
