# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.prediction.trade.schemas import PredictionOrder


class LimitOrder(TypedDict):
  """A `LIMIT` order."""

  symbol: str
  """Outcome symbol, e.g. `BTC_UP_DOWN_5M_1790237100_YUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['LIMIT']
  """Order type."""
  timeInForce: Literal['GTC', 'IOC', 'FOK', 'GTX']
  """Time in force."""
  quantity: Decimal
  """Quantity."""
  price: Decimal
  """Limit price."""
  newClientOrderId: NotRequired[str]
  """Client order ID, unique among open orders; generated when omitted."""
  nonce: NotRequired[int]
  """Caller-chosen request nonce, in epoch microseconds, signed in place of a freshly drawn one. Choose it (unique, within the venue's time window) when you may later need to reference this request: a guarded cancel of the order, or `noop` to invalidate it while still pending. Omit to let the client draw one."""


class MarketOrder(TypedDict):
  """A `MARKET` order sized in outcome tokens (the way to sell)."""

  symbol: str
  """Outcome symbol, e.g. `BTC_UP_DOWN_5M_1790237100_YUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['MARKET']
  """Order type."""
  quantity: Decimal
  """Base asset quantity."""
  newClientOrderId: NotRequired[str]
  """Client order ID, unique among open orders; generated when omitted."""
  nonce: NotRequired[int]
  """Caller-chosen request nonce, in epoch microseconds, signed in place of a freshly drawn one. Choose it (unique, within the venue's time window) when you may later need to reference this request: a guarded cancel of the order, or `noop` to invalidate it while still pending. Omit to let the client draw one."""


class MarketQuoteOrder(TypedDict):
  """A `MARKET` order sized by the quote amount to spend (the way to buy)."""

  symbol: str
  """Outcome symbol, e.g. `BTC_UP_DOWN_5M_1790237100_YUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['MARKET']
  """Order type."""
  quoteOrderQty: Decimal
  """Quote asset amount."""
  newClientOrderId: NotRequired[str]
  """Client order ID, unique among open orders; generated when omitted."""
  nonce: NotRequired[int]
  """Caller-chosen request nonce, in epoch microseconds, signed in place of a freshly drawn one. Choose it (unique, within the venue's time window) when you may later need to reference this request: a guarded cancel of the order, or `noop` to invalidate it while still pending. Omit to let the client draw one."""


Request = LimitOrder | MarketOrder | MarketQuoteOrder


class PlaceOrder(RpcEndpoint):
  """Place an order on an outcome symbol."""

  async def place_order(
    self,
    place_order_request: Request,
    *,
    validate: bool | None = None,
  ) -> PredictionOrder:
    """Place an order on an outcome symbol.

    Args:
      place_order_request: The order to place, by type.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#place-order-trade)
    """
    return await self.request(
      place_order_request,
      method='POST',
      path='order',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=PredictionOrder,
    )
