# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import OperationResult


class Request(TypedDict):
  """Orders to cancel."""

  symbol: str
  """Trading pair, e.g. `BTCUSDT`."""
  orderIdList: NotRequired[list[int]]
  """Order IDs to cancel."""
  origClientOrderIdList: NotRequired[list[str]]
  """Client order IDs to cancel."""


class CancelAllOpenOrders(RpcEndpoint):
  """Cancel every open order on a symbol, or the listed ones."""

  async def cancel_all_open_orders(
    self,
    symbol: str,
    *,
    order_id_list: list[int] | None = None,
    orig_client_order_id_list: list[str] | None = None,
    validate: bool | None = None,
  ) -> OperationResult:
    """Cancel every open order on a symbol, or the listed ones.

    Args:
      symbol: Trading pair, e.g. `BTCUSDT`.
      order_id_list: Order IDs to cancel.
      orig_client_order_id_list: Client order IDs to cancel.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#cancel-all-open-orders-trade)
    """
    request: Request = Request(symbol=symbol)
    if order_id_list is not None:
      request['orderIdList'] = order_id_list
    if orig_client_order_id_list is not None:
      request['origClientOrderIdList'] = orig_client_order_id_list
    return await self.request(
      request,
      method='DELETE',
      path='allOpenOrders',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=OperationResult,
    )
