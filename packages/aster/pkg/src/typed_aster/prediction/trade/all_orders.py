# Generated by typed-dev — see CONTRIBUTING.md.
from datetime import datetime
from typed_core import PaginatedResponse
from typed_core.exceptions import LogicError
from typing_extensions import NotRequired, Sequence, TypedDict, cast
from typed_aster.core import TimestampMillis, timestamp_millis
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.prediction.trade.schemas import PredictionOrder


class Request(TypedDict):
  """All orders request."""

  symbol: NotRequired[str]
  """Outcome symbol; every symbol when omitted (documented as mandatory)."""
  orderId: NotRequired[int]
  """Return orders from this order ID; cannot be combined with `startTime`/`endTime`."""
  startTime: NotRequired[TimestampMillis]
  """Start of the time range (at most 7 days wide)."""
  endTime: NotRequired[TimestampMillis]
  """End of the time range."""
  limit: NotRequired[int]
  """Number of orders; maximum 1000."""


Response = list[PredictionOrder]


class AllOrders(RpcEndpoint):
  """Get all orders (open, canceled or filled) from the last 7 days. `orderId` cannot be combined with `startTime`/`endTime`."""

  def all_orders_paged(
    self,
    symbol: str | None = None,
    *,
    order_id: int | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[PredictionOrder, tuple[int | None, list[PredictionOrder]]]:
    """Get all orders (open, canceled or filled) from the last 7 days. `orderId` cannot be combined with `startTime`/`endTime`.

    Paged variant of `all_orders`: Walks forwards by moving `order_id` to the latest `orderId` of each page that came back full and stops at the first row whose `time` is past the caller's own `end_time` (rows beyond it are dropped), or on the first page shorter than the venue's row cap. Awaitable (flattens every page) or async-iterable (one page at a time).

    The boundary row a venue re-serves is dropped by its `orderId` value, so no row is duplicated or skipped whichever way the venue bounds its ranges.

    The venue refuses `start_time`/`end_time` alongside `order_id`, so they are sent on the first request only; pass them or `order_id`, not both. Without `order_id`, `start_time` is required.

    Args:
      symbol: Outcome symbol; every symbol when omitted (documented as mandatory).
      order_id: Return orders from this order ID; cannot be combined with `startTime`/`endTime`.
      start_time: Start of the time range (at most 7 days wide).
      end_time: End of the time range.
      limit: Number of orders; maximum 1000.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#query-all-orders-user_data)
    """
    if order_id is not None and (start_time is not None or end_time is not None):
      raise ValueError(
        'all_orders_paged walks by `order_id`, which the venue refuses alongside `start_time`/`end_time`: pass one or the other'
      )
    if order_id is None and start_time is None:
      raise ValueError(
        'all_orders_paged needs `order_id` or `start_time` to start from: without either the venue answers from the wrong end of the range'
      )
    cap: int | None = min(limit, 1000) if limit is not None else 500

    async def next(
      state: tuple[int | None, list[PredictionOrder]],
    ) -> tuple[
      Sequence[PredictionOrder], tuple[int | None, list[PredictionOrder]] | None
    ]:
      def key_of(item: PredictionOrder):
        """One row's `orderId`, normalized to compare against the moving bound."""
        raw = item.get('orderId') if item is not None else None
        return None if raw is None else int(raw)

      def limit_of(item: PredictionOrder):
        """One row's `time`, normalized to compare against `end_time`."""
        raw = item.get('time') if item is not None else None
        return (
          None
          if raw is None
          else timestamp_millis.parse(cast(int, raw))
          if not isinstance(raw, datetime)
          else raw
        )

      pos, carried = state
      response = await self.all_orders(
        symbol,
        order_id=pos,
        start_time=start_time if pos is None else None,
        end_time=end_time if pos is None else None,
        limit=limit,
        validate=validate,
      )
      rows = response
      keys = [key_of(item) for item in rows]
      limit_keys = [limit_of(item) for item in rows]
      far_key = (
        None
        if end_time is None
        else timestamp_millis.parse(timestamp_millis.dump(end_time))
      )
      carried_keys = [key_of(item) for item in carried]
      fresh = [
        item
        for item, key, limit_key in zip(rows, keys, limit_keys)
        if key not in carried_keys
        and (far_key is None or limit_key is None or limit_key <= far_key)
      ]
      values = [key for key in keys if key is not None]
      extreme = max(values) if values else None
      if cap is not None and len(rows) >= cap:
        if extreme is None or extreme == pos:
          raise LogicError(
            f'`all_orders_paged` requested from {pos} and the venue returned a full page of {len(rows)} rows all sharing one `orderId` value; the rest of that value is unreachable and advancing would drop it.'
          )
        if not (
          far_key is not None
          and any(
            limit_key is not None and limit_key > far_key for limit_key in limit_keys
          )
        ):
          return fresh, (
            extreme,
            [item for item, key in zip(rows, keys) if key == extreme],
          )
      elif (
        cap is None
        and extreme is not None
        and extreme != pos
        and not (
          far_key is not None
          and any(
            limit_key is not None and limit_key > far_key for limit_key in limit_keys
          )
        )
      ):
        return fresh, (
          extreme,
          [item for item, key in zip(rows, keys) if key == extreme],
        )
      return fresh, None

    return PaginatedResponse((order_id, []), next)

  async def all_orders(
    self,
    symbol: str | None = None,
    *,
    order_id: int | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get all orders (open, canceled or filled) from the last 7 days. `orderId` cannot be combined with `startTime`/`endTime`.

    Args:
      symbol: Outcome symbol; every symbol when omitted (documented as mandatory).
      order_id: Return orders from this order ID; cannot be combined with `startTime`/`endTime`.
      start_time: Start of the time range (at most 7 days wide).
      end_time: End of the time range.
      limit: Number of orders; maximum 1000.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#query-all-orders-user_data)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    if order_id is not None:
      request['orderId'] = order_id
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='allOrders',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
