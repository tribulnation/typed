# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typed_core.exceptions import LogicError
from typing_extensions import NotRequired, Sequence, TypedDict
from typed_aster.core import TimestampMillis
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import AccountTrade


class Request(TypedDict):
  """Trade history request."""

  symbol: NotRequired[str]
  """Trading pair."""
  orderId: NotRequired[int]
  """Only trades of this order; use together with `symbol`."""
  startTime: NotRequired[TimestampMillis]
  """Start of the time range (at most 7 days wide)."""
  endTime: NotRequired[TimestampMillis]
  """End of the time range."""
  fromId: NotRequired[int]
  """Trade ID to start from; cannot be combined with `startTime`/`endTime`."""
  limit: NotRequired[int]
  """Number of trades; maximum 1000."""


Response = list[AccountTrade]


class UserTrades(RpcEndpoint):
  """Get the account's trade history; the last 7 days when no time range is given."""

  def user_trades_paged(
    self,
    symbol: str | None = None,
    *,
    order_id: int | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    from_id: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[AccountTrade, tuple[int | None, list[AccountTrade]]]:
    """Get the account's trade history; the last 7 days when no time range is given.

    Paged variant of `user_trades`: Walks forwards by moving `from_id` to the latest `id` of each page that came back full and stops on the first page shorter than the venue's row cap. Awaitable (flattens every page) or async-iterable (one page at a time).

    The boundary row a venue re-serves is dropped by its `id` value, so no row is duplicated or skipped whichever way the venue bounds its ranges.

    Args:
      symbol: Trading pair.
      order_id: Only trades of this order; use together with `symbol`.
      start_time: Start of the time range (at most 7 days wide).
      end_time: End of the time range.
      from_id: Trade ID to start from; cannot be combined with `startTime`/`endTime`.
      limit: Number of trades; maximum 1000.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#account-trade-history-user_data)
    """
    cap: int | None = min(limit, 1000) if limit is not None else 500

    async def next(
      state: tuple[int | None, list[AccountTrade]],
    ) -> tuple[Sequence[AccountTrade], tuple[int | None, list[AccountTrade]] | None]:
      def key_of(item: AccountTrade):
        """One row's `id`, normalized to compare against the moving bound."""
        raw = item.get('id') if item is not None else None
        return None if raw is None else int(raw)

      pos, carried = state
      response = await self.user_trades(
        symbol,
        order_id=order_id,
        start_time=start_time,
        end_time=end_time,
        from_id=pos,
        limit=limit,
        validate=validate,
      )
      rows = response
      keys = [key_of(item) for item in rows]
      carried_keys = [key_of(item) for item in carried]
      fresh = [item for item, key in zip(rows, keys) if key not in carried_keys]
      values = [key for key in keys if key is not None]
      extreme = max(values) if values else None
      if cap is not None and len(rows) >= cap:
        if extreme is None or extreme == pos:
          raise LogicError(
            f'`user_trades_paged` requested from {pos} and the venue returned a full page of {len(rows)} rows all sharing one `id` value; the rest of that value is unreachable and advancing would drop it.'
          )
        return fresh, (
          extreme,
          [item for item, key in zip(rows, keys) if key == extreme],
        )
      if cap is None and extreme is not None and extreme != pos:
        return fresh, (
          extreme,
          [item for item, key in zip(rows, keys) if key == extreme],
        )
      return fresh, None

    return PaginatedResponse((from_id, []), next)

  async def user_trades(
    self,
    symbol: str | None = None,
    *,
    order_id: int | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    from_id: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get the account's trade history; the last 7 days when no time range is given.

    Args:
      symbol: Trading pair.
      order_id: Only trades of this order; use together with `symbol`.
      start_time: Start of the time range (at most 7 days wide).
      end_time: End of the time range.
      from_id: Trade ID to start from; cannot be combined with `startTime`/`endTime`.
      limit: Number of trades; maximum 1000.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-prediction-api.md#account-trade-history-user_data)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    if order_id is not None:
      request['orderId'] = order_id
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if from_id is not None:
      request['fromId'] = from_id
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='userTrades',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
