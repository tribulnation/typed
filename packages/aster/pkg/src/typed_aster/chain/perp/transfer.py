# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import TransferResult


class Request(TypedDict):
  asset: str
  """Asset name, e.g. `USDT`."""
  amount: Decimal
  """Transfer amount, greater than 0."""
  clientTranId: str
  """Client-defined transfer id."""
  kindType: Literal['SPOT_FUTURE', 'FUTURE_SPOT']
  """Direction: `SPOT_FUTURE` (spot to perp) or `FUTURE_SPOT` (perp to spot)."""


class Transfer(RpcEndpoint):
  """Transfer an asset between the spot wallet and the perp account."""

  async def transfer(
    self,
    *,
    asset: str,
    amount: Decimal,
    client_tran_id: str,
    kind_type: Literal['SPOT_FUTURE', 'FUTURE_SPOT'],
    validate: bool | None = None,
  ) -> TransferResult:
    """Transfer an asset between the spot wallet and the perp account.

    Args:
      asset: Asset name, e.g. `USDT`.
      amount: Transfer amount, greater than 0.
      client_tran_id: Client-defined transfer id.
      kind_type: Direction: `SPOT_FUTURE` (spot to perp) or `FUTURE_SPOT` (perp to spot).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/aster-chain/endpoints/#wallet-transfer-trade)
    """
    return await self.request(
      Request(
        asset=asset, amount=amount, clientTranId=client_tran_id, kindType=kind_type
      ),
      method='POST',
      path='perp/wallet/transfer',
      meta={
        'sign': 'main',
        'order': ['asset', 'amount', 'clientTranId', 'kindType', 'nonce', 'user'],
      },
      validate=validate,
      request_type=Request,
      response_type=TransferResult,
    )
