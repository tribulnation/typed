# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_aster.core import TimestampMillis
from typed_aster.core.endpoint.rpc import RpcEndpoint


class ChainDepositWithdrawal(TypedDict):
  """One deposit or withdrawal."""

  id: NotRequired[str]
  """Record id."""
  type: NotRequired[Literal['DEPOSIT', 'WITHDRAW']]
  """Record type."""
  asset: NotRequired[str]
  """Asset name."""
  amount: NotRequired[Decimal]
  """Amount."""
  state: NotRequired[str]
  """Record state, e.g. `COMPLETED`."""
  txHash: NotRequired[str]
  """On-chain transaction hash."""
  time: NotRequired[TimestampMillis]
  """Record time."""
  chainId: NotRequired[int]
  """Chain id."""
  accountType: NotRequired[str]
  """Account type, e.g. `perp`."""


class Request(TypedDict):
  chainId: NotRequired[str]
  """Filter by chain id (sent as a string)."""


Response = list[ChainDepositWithdrawal]


class DepositWithdrawHistory(RpcEndpoint):
  """Get the deposit and withdrawal history of the user's perp account."""

  async def deposit_withdraw_history(
    self,
    chain_id: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Get the deposit and withdrawal history of the user's perp account.

    Args:
      chain_id: Filter by chain id (sent as a string).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/aster-chain/endpoints/#depositwithdraw-history-user_data)
    """
    request: Request = Request()
    if chain_id is not None:
      request['chainId'] = chain_id
    return await self.request(
      request,
      method='GET',
      path='perp/deposit-withdraw-history',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
