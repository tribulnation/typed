# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .account import Account
from .perp import Perp
from .rpc import ChainRpc
from .spot import Spot
from .staking import Staking
from .wallet import Wallet
from typed_aster.core.base import ChainBase


class Chain(ChainBase):
  """Aster Chain: REST (account privacy, staking, deposits, withdrawals and transfers) and the read-only JSON-RPC. Mainnet only.

  References:
    - [Upstream docs](https://asterdex.github.io/aster-api-website/aster-chain/endpoints/)
  """

  @cached_property
  def account(self) -> Account:
    """Aster Chain account: privacy mode (PUBLIC/PRIVATE) and transfers to another Aster Chain address.

    References:
      - [Upstream docs](https://asterdex.github.io/aster-api-website/aster-chain/endpoints/#aster-chain-account-endpoints)
    """
    return Account(client=self.client)

  @cached_property
  def perp(self) -> Perp:
    """Aster Chain perp account: withdrawals (EVM and Solana), withdraw limits, deposit/withdraw history and spot/perp transfers.

    References:
      - [Upstream docs](https://asterdex.github.io/aster-api-website/aster-chain/endpoints/#aster-chain-perp-withdraw-transfer-endpoints)
    """
    return Perp(client=self.client)

  @cached_property
  def rpc(self) -> ChainRpc:
    """Aster Chain read-only JSON-RPC 2.0 (`POST /info`): balances, positions, staking summary, open orders and fills of any address, honouring account privacy mode.

    References:
      - [Upstream docs](https://asterdex.github.io/aster-api-website/rpc/endpoints/)
    """
    return ChainRpc(client=self.rpc_client)

  @cached_property
  def spot(self) -> Spot:
    """Aster Chain spot account: withdrawals (EVM and Solana), spot/perp transfers and the SUI deposit address.

    References:
      - [Upstream docs](https://asterdex.github.io/aster-api-website/aster-chain/endpoints/#aster-chain-spot-withdraw-transfer-endpoints)
    """
    return Spot(client=self.client)

  @cached_property
  def staking(self) -> Staking:
    """ASTER staking on Aster Chain: staking position reads, stake/add/extend/claim actions and the network-wide locked total.

    References:
      - [Upstream docs](https://asterdex.github.io/aster-api-website/aster-chain/endpoints/#aster-chain-staking-endpoints)
    """
    return Staking(client=self.client)

  @cached_property
  def wallet(self) -> Wallet:
    """Aster Chain withdrawal helpers: public withdrawal fee estimates.

    References:
      - [Upstream docs](https://asterdex.github.io/aster-api-website/aster-chain/endpoints/#aster-chain-withdraw-endpoints)
    """
    return Wallet(client=self.client)
