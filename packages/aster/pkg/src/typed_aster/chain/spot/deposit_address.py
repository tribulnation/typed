# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import AssetDepositAddress


class Request(TypedDict):
  network: NotRequired[str]
  """Network type; `SUI` when omitted."""


class DepositAddress(RpcEndpoint):
  """Get the user's dedicated deposit address on a network (spot account only; SUI)."""

  async def deposit_address(
    self,
    network: str | None = None,
    *,
    validate: bool | None = None,
  ) -> AssetDepositAddress:
    """Get the user's dedicated deposit address on a network (spot account only; SUI).

    Args:
      network: Network type; `SUI` when omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/aster-chain/endpoints/#get-user-deposit-address-user_data)
    """
    request: Request = Request()
    if network is not None:
      request['network'] = network
    return await self.request(
      request,
      method='GET',
      path='spot/user-deposit-address',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=AssetDepositAddress,
    )
