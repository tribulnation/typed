# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_aster.chain.rpc.schemas import ChainAssetBalance
from typed_aster.core.endpoint.jsonrpc import JsonRpcEndpoint


class ChainPosition(TypedDict):
  """One open position."""

  id: str
  """Position id: `<account>_<symbol>_<positionSide>`."""
  symbol: str
  """Trading pair symbol."""
  collateral: NotRequired[str]
  """Collateral asset (documented; not returned live)."""
  positionAmount: Decimal
  """Position size; negative for short."""
  entryPrice: NotRequired[Decimal]
  """Average entry price (documented; not returned live)."""
  unrealizedProfit: NotRequired[Decimal]
  """Unrealized profit/loss."""
  askNotional: NotRequired[Decimal]
  """Ask notional (documented; not returned live)."""
  bidNotional: NotRequired[Decimal]
  """Bid notional (documented; not returned live)."""
  notionalValue: NotRequired[Decimal]
  """Position notional value."""
  markPrice: NotRequired[Decimal]
  """Mark price (documented; not returned live)."""
  leverage: NotRequired[int]
  """Leverage (documented; not returned live)."""
  isolated: NotRequired[bool]
  """Isolated margin mode (documented; not returned live)."""
  isolatedWallet: NotRequired[Decimal]
  """Isolated wallet balance (documented; not returned live)."""
  adl: NotRequired[int]
  """ADL quantile (documented; not returned live)."""
  positionSide: Literal['BOTH', 'LONG', 'SHORT']
  """Position side."""
  marginValue: NotRequired[Decimal]
  """Margin value (documented; not returned live)."""
  cumRealized: NotRequired[Decimal]
  """Cumulative realized profit/loss (undocumented, returned live)."""


class ChainUnclaimedReward(TypedDict):
  """Unclaimed reward of one asset."""

  asset: str
  """Reward asset (currently `ASTER`)."""
  amount: Decimal
  """Unclaimed amount."""


class Request(TypedDict):
  address: str
  """Wallet address to query. The fills and open-orders methods match it case-sensitively: pass the EIP-55 checksum form (a lowercase address silently returns no rows)."""
  blockTag: NotRequired[str]
  """Block tag; `latest` for the most recent state. Ignored by the venue: every tag, and an omitted one, returns the current state."""


class ChainPositionGroup(TypedDict):
  """Positions of one trading product."""

  tradingProduct: str
  """Trading product, e.g. `perps`."""
  positions: list[ChainPosition]
  """Open positions."""


class ChainStakingSummary(TypedDict):
  """Staking summary; every field is `null` when its amount is 0."""

  totalStakedAmount: NotRequired[Decimal | None]
  """Total staked ASTER."""
  totalUnclaimedRewards: NotRequired[list[ChainUnclaimedReward] | None]
  """Unclaimed rewards."""
  totalPendingStakeAmount: NotRequired[Decimal | None]
  """Total pending stake."""
  totalPendingUnstakeAmount: NotRequired[Decimal | None]
  """Total pending unstake."""


class ChainAccount(TypedDict):
  """Balances, positions and staking of one address."""

  address: str
  """Queried address, echoed as sent."""
  accountPrivacy: Literal['disabled', 'enabled']
  """Account privacy mode."""
  perpAssets: NotRequired[list[ChainAssetBalance]]
  """Perp wallet balances."""
  spotAssets: NotRequired[list[ChainAssetBalance]]
  """Spot wallet balances (undocumented, returned live)."""
  positions: NotRequired[list[ChainPositionGroup]]
  """Open positions, grouped by trading product."""
  staking: NotRequired[ChainStakingSummary]


class GetBalance(JsonRpcEndpoint):
  """Get the balances, positions and staking summary of an address."""

  async def get_balance(
    self,
    *,
    address: str,
    block_tag: str | None = None,
    validate: bool | None = None,
  ) -> ChainAccount:
    """Get the balances, positions and staking summary of an address.

    Args:
      address: Wallet address to query. The fills and open-orders methods match it case-sensitively: pass the EIP-55 checksum form (a lowercase address silently returns no rows).
      block_tag: Block tag; `latest` for the most recent state. Ignored by the venue: every tag, and an omitted one, returns the current state.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/rpc/endpoints/#get-balance)
    """
    request: Request = Request(address=address)
    if block_tag is not None:
      request['blockTag'] = block_tag
    return await self.request(
      request,
      path='aster_getBalance',
      meta={'params': ['address', 'blockTag']},
      validate=validate,
      request_type=Request,
      response_type=ChainAccount,
    )
