# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  periodCode: Literal['26_WEEKS', '52_WEEKS', '104_WEEKS', '156_WEEKS', '208_WEEKS']
  """New lock period; must be longer than the current one."""


class StakingResult(TypedDict):
  """Outcome of a staking action."""

  result: str
  """`SUCCESS` when the action was accepted."""


class UpdateLockPeriod(RpcEndpoint):
  """Extend the lock period of the staking position (on-chain `TokenDelegate` `ExtendStakingTime`)."""

  async def update_lock_period(
    self,
    period_code: Literal['26_WEEKS', '52_WEEKS', '104_WEEKS', '156_WEEKS', '208_WEEKS'],
    *,
    validate: bool | None = None,
  ) -> StakingResult:
    """Extend the lock period of the staking position (on-chain `TokenDelegate` `ExtendStakingTime`).

    Args:
      period_code: New lock period; must be longer than the current one.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-chain.md#update-lock-period-trade)
    """
    return await self.request(
      Request(periodCode=period_code),
      method='POST',
      path='staking/updateLockPeriod',
      meta={'sign': 'main', 'order': ['periodCode', 'nonce', 'user']},
      validate=validate,
      request_type=Request,
      response_type=StakingResult,
    )
