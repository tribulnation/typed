# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_aster.core import TimestampMillis
from typed_aster.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict): ...


class StakingPosition(TypedDict):
  """Staking position."""

  validatorAddress: NotRequired[str]
  """Validator address."""
  stakeAmount: NotRequired[Decimal]
  """Staked amount."""
  periodCode: NotRequired[str]
  """Lock period code; the upstream sample shows `90D`, outside the documented `*_WEEKS` codes, so it is left a bare string."""
  startTime: NotRequired[TimestampMillis]
  """Stake start time."""
  endTime: NotRequired[TimestampMillis]
  """Lock end time."""
  status: NotRequired[Literal['ACTIVE', 'UNLOCKING', 'COMPLETED']]
  """Position status."""


class MyStaking(RpcEndpoint):
  """Get the user's staking position."""

  async def my_staking(self, *, validate: bool | None = None) -> StakingPosition:
    """Get the user's staking position.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-chain.md#get-my-staking-user_data)
    """
    return await self.request(
      Request(),
      method='GET',
      path='staking/myStaking',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=StakingPosition,
    )
