# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  requestedAmount: Decimal
  """Amount to claim, greater than 0 and at most the claimable balance."""


class StakingResult(TypedDict):
  """Outcome of a staking action."""

  result: str
  """`SUCCESS` when the action was accepted."""


class ClaimRewards(RpcEndpoint):
  """Claim accumulated staking rewards (on-chain `TokenDelegate` `ClaimRewards`)."""

  async def claim_rewards(
    self,
    requested_amount: Decimal,
    *,
    validate: bool | None = None,
  ) -> StakingResult:
    """Claim accumulated staking rewards (on-chain `TokenDelegate` `ClaimRewards`).

    Args:
      requested_amount: Amount to claim, greater than 0 and at most the claimable balance.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-chain.md#claim-rewards-trade)
    """
    return await self.request(
      Request(requestedAmount=requested_amount),
      method='POST',
      path='staking/claimRewards',
      meta={'sign': 'main', 'order': ['requestedAmount', 'nonce', 'user']},
      validate=validate,
      request_type=Request,
      response_type=StakingResult,
    )
