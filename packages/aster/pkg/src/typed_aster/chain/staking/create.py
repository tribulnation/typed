# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  validatorAddress: str
  """Target validator's on-chain address."""
  stakeAmount: Decimal
  """Amount to stake, greater than 0."""
  periodCode: Literal['26_WEEKS', '52_WEEKS', '104_WEEKS', '156_WEEKS', '208_WEEKS']
  """Lock period."""


class StakingResult(TypedDict):
  """Outcome of a staking action."""

  result: str
  """`SUCCESS` when the action was accepted."""


class Create(RpcEndpoint):
  """Create a staking position by delegating ASTER to a validator (on-chain `TokenDelegate` `Stake`)."""

  async def create(
    self,
    validator_address: str,
    *,
    stake_amount: Decimal,
    period_code: Literal['26_WEEKS', '52_WEEKS', '104_WEEKS', '156_WEEKS', '208_WEEKS'],
    validate: bool | None = None,
  ) -> StakingResult:
    """Create a staking position by delegating ASTER to a validator (on-chain `TokenDelegate` `Stake`).

    Args:
      validator_address: Target validator's on-chain address.
      stake_amount: Amount to stake, greater than 0.
      period_code: Lock period.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-chain.md#create-staking-trade)
    """
    return await self.request(
      Request(
        validatorAddress=validator_address,
        stakeAmount=stake_amount,
        periodCode=period_code,
      ),
      method='POST',
      path='staking/create',
      meta={
        'sign': 'main',
        'order': ['validatorAddress', 'stakeAmount', 'periodCode', 'nonce', 'user'],
      },
      validate=validate,
      request_type=Request,
      response_type=StakingResult,
    )
