# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_aster.core import TimestampMillis
from typed_aster.core.endpoint.rpc import RpcEndpoint


class ClaimableRewards(TypedDict):
  """Claimable staking rewards."""

  claimableAmount: NotRequired[Decimal]
  """Amount available to claim."""
  asset: NotRequired[str]
  """Reward asset, e.g. `ASTER`."""
  accruedRewards: NotRequired[Decimal]
  """Total accrued rewards."""
  lastClaimTime: NotRequired[TimestampMillis]
  """Last claim time."""


class Request(TypedDict): ...


class ClaimableRewardsEndpoint(RpcEndpoint):
  """Get the user's claimable staking rewards."""

  async def claimable_rewards(
    self, *, validate: bool | None = None
  ) -> ClaimableRewards:
    """Get the user's claimable staking rewards.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-chain.md#get-claimable-rewards-user_data)
    """
    return await self.request(
      Request(),
      method='GET',
      path='staking/claimableRewards',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=ClaimableRewards,
    )
