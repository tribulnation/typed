# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  validatorAddress: str
  """Target validator's on-chain address."""
  stakeAmount: Decimal
  """Additional amount to stake, greater than 0."""


class StakingResult(TypedDict):
  """Outcome of a staking action."""

  result: str
  """`SUCCESS` when the action was accepted."""


class Deposit(RpcEndpoint):
  """Add ASTER to an existing staking position on a validator (on-chain `TokenDelegate` `AddStake`)."""

  async def deposit(
    self,
    validator_address: str,
    *,
    stake_amount: Decimal,
    validate: bool | None = None,
  ) -> StakingResult:
    """Add ASTER to an existing staking position on a validator (on-chain `TokenDelegate` `AddStake`).

    Args:
      validator_address: Target validator's on-chain address.
      stake_amount: Additional amount to stake, greater than 0.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-chain.md#deposit-stake-trade)
    """
    return await self.request(
      Request(validatorAddress=validator_address, stakeAmount=stake_amount),
      method='POST',
      path='staking/deposit',
      meta={
        'sign': 'main',
        'order': ['validatorAddress', 'stakeAmount', 'nonce', 'user'],
      },
      validate=validate,
      request_type=Request,
      response_type=StakingResult,
    )
