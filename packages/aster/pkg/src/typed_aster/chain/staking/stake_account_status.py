# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict): ...


class StakeAccountStatus(TypedDict):
  """Staking account status."""

  stakeAccountStatus: Literal['ACTIVE', 'INACTIVE', 'PENDING']
  """Staking account status."""


class StakeAccountStatusEndpoint(RpcEndpoint):
  """Get the user's staking account status."""

  async def stake_account_status(
    self,
    *,
    validate: bool | None = None,
  ) -> StakeAccountStatus:
    """Get the user's staking account status.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-chain.md#get-staking-account-status-user_data)
    """
    return await self.request(
      Request(),
      method='GET',
      path='staking/stakeAccountStatus',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=StakeAccountStatus,
    )
