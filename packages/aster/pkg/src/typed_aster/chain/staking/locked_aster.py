# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class LockedAster(TypedDict):
  """Network-wide locked ASTER."""

  periodCode: str
  """Lock period the total covers, `208_WEEKS`."""
  totalVolume: float
  """Total locked ASTER."""


class Request(TypedDict): ...


class LockedAsterEndpoint(RpcEndpoint):
  """Get the total ASTER locked across all 208-week staking positions on the network."""

  async def locked_aster(self, *, validate: bool | None = None) -> LockedAster:
    """Get the total ASTER locked across all 208-week staking positions on the network.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/aster-chain/endpoints/#get-locked-aster-none)
    """
    return await self.request(
      Request(),
      method='GET',
      path='staking/getLockedAster',
      meta={'sign': 'none'},
      validate=validate,
      request_type=Request,
      response_type=LockedAster,
    )
