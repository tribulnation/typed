# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  chainId: int
  """Target chain id, e.g. `1` (ETH), `56` (BSC), `42161` (Arbitrum), `101` (Solana)."""
  asset: str
  """Asset name, e.g. `USDT`."""


class WithdrawFeeEstimateResponse(TypedDict):
  """Withdrawal fee estimate."""

  gasPrice: NotRequired[int]
  """Gas price in wei (documented; not returned live)."""
  gasLimit: int
  """Gas limit."""
  nativePrice: NotRequired[float]
  """Native token price in USD (documented as a string; not returned live)."""
  tokenPrice: float
  """Withdrawn asset price in USD."""
  gasCost: float
  """Fee in units of the withdrawn asset."""
  gasUsdValue: float
  """Fee value in USD."""


class EstimateWithdrawFee(RpcEndpoint):
  """Estimate the gas fee of a withdrawal of an asset to a chain."""

  async def estimate_withdraw_fee(
    self,
    chain_id: int,
    *,
    asset: str,
    validate: bool | None = None,
  ) -> WithdrawFeeEstimateResponse:
    """Estimate the gas fee of a withdrawal of an asset to a chain.

    Args:
      chain_id: Target chain id, e.g. `1` (ETH), `56` (BSC), `42161` (Arbitrum), `101` (Solana).
      asset: Asset name, e.g. `USDT`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/aster-chain/endpoints/#estimate-withdraw-fee-none)
    """
    return await self.request(
      Request(chainId=chain_id, asset=asset),
      method='GET',
      path='withdraw/estimateFee',
      meta={'sign': 'none'},
      validate=validate,
      request_type=Request,
      response_type=WithdrawFeeEstimateResponse,
    )
