# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class AccountPrivacyStatus(TypedDict):
  """Account privacy mode."""

  status: Literal['PUBLIC', 'PRIVATE']
  """Account privacy mode."""


class Request(TypedDict): ...


class Status(RpcEndpoint):
  """Get the account's privacy mode."""

  async def status(self, *, validate: bool | None = None) -> AccountPrivacyStatus:
    """Get the account's privacy mode.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/aster-chain/endpoints/#get-account-status-user_data)
    """
    return await self.request(
      Request(),
      method='GET',
      path='account/status',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=AccountPrivacyStatus,
    )
