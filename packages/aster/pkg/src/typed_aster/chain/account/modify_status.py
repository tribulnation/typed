# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class AccountPrivacyStatus(TypedDict):
  """Account privacy mode after the change."""

  status: Literal['PUBLIC', 'PRIVATE']
  """Account privacy mode."""


class Request(TypedDict):
  status: Literal['PUBLIC', 'PRIVATE']
  """New privacy mode: `PUBLIC` or `PRIVATE`."""


class ModifyStatus(RpcEndpoint):
  """Set the account's privacy mode. The change is broadcast to Aster Chain."""

  async def modify_status(
    self,
    status: Literal['PUBLIC', 'PRIVATE'],
    *,
    validate: bool | None = None,
  ) -> AccountPrivacyStatus:
    """Set the account's privacy mode. The change is broadcast to Aster Chain.

    Args:
      status: New privacy mode: `PUBLIC` or `PRIVATE`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/aster-chain/endpoints/#modify-account-status-trade)
    """
    return await self.request(
      Request(status=status),
      method='POST',
      path='account/modify-status',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=AccountPrivacyStatus,
    )
