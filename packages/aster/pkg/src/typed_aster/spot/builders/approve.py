# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Builder approval."""

  builder: str
  """Spot Builder wallet address."""
  maxFeeRate: Decimal
  """Maximum fee rate the builder may charge; at most `0.01`."""
  builderName: NotRequired[str]
  """Builder name."""
  signatureChainId: NotRequired[int]
  """EIP-712 domain chain ID of the signature: `56` (EVM wallets)."""


class Approve(RpcEndpoint):
  """Authorize a Spot Builder and cap the fee rate it may charge on your spot orders."""

  async def approve(
    self,
    *,
    builder: str,
    max_fee_rate: Decimal,
    builder_name: str | None = None,
    signature_chain_id: int | None = None,
    validate: bool | None = None,
  ) -> None:
    """Authorize a Spot Builder and cap the fee rate it may charge on your spot orders.

    Args:
      builder: Spot Builder wallet address.
      max_fee_rate: Maximum fee rate the builder may charge; at most `0.01`.
      builder_name: Builder name.
      signature_chain_id: EIP-712 domain chain ID of the signature: `56` (EVM wallets).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/asterCode/spot-endpoints/#approve-spot-builder-spot_trade)
    """
    request: Request = Request(builder=builder, maxFeeRate=max_fee_rate)
    if builder_name is not None:
      request['builderName'] = builder_name
    if signature_chain_id is not None:
      request['signatureChainId'] = signature_chain_id
    return await self.request(
      request,
      method='POST',
      path='approveBuilder',
      meta={'sign': 'typed', 'action': 'ApproveBuilder'},
      validate=validate,
      request_type=Request,
      response_type=None,
    )
