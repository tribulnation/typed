# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict): ...


class SpotBuilder(TypedDict):
  """One Spot Builder authorization."""

  userAddress: str
  """User main wallet address."""
  builderAddress: str
  """Spot Builder address."""
  maxFeeRate: float
  """Maximum authorized fee rate."""
  builderName: str
  """Builder name; empty when none was given."""


Response = list[SpotBuilder]


class List(RpcEndpoint):
  """List the Spot Builders you have authorized."""

  async def list(self, *, validate: bool | None = None) -> Response:
    """List the Spot Builders you have authorized.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/asterCode/spot-endpoints/#get-spot-builders-user_data)
    """
    return await self.request(
      Request(),
      method='GET',
      path='builder',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
