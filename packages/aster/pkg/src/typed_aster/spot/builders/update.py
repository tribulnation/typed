# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Builder update."""

  builder: str
  """Spot Builder address."""
  maxFeeRate: Decimal
  """New maximum fee rate; at most `0.01`."""
  signatureChainId: NotRequired[int]
  """EIP-712 domain chain ID of the signature: `56` (EVM wallets)."""


class Update(RpcEndpoint):
  """Change the fee rate cap of an approved Spot Builder."""

  async def update(
    self,
    builder: str,
    *,
    max_fee_rate: Decimal,
    signature_chain_id: int | None = None,
    validate: bool | None = None,
  ) -> None:
    """Change the fee rate cap of an approved Spot Builder.

    Args:
      builder: Spot Builder address.
      max_fee_rate: New maximum fee rate; at most `0.01`.
      signature_chain_id: EIP-712 domain chain ID of the signature: `56` (EVM wallets).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/asterCode/spot-endpoints/#update-spot-builder-spot_trade)
    """
    request: Request = Request(builder=builder, maxFeeRate=max_fee_rate)
    if signature_chain_id is not None:
      request['signatureChainId'] = signature_chain_id
    return await self.request(
      request,
      method='POST',
      path='updateBuilder',
      meta={'sign': 'typed', 'action': 'UpdateBuilder'},
      validate=validate,
      request_type=Request,
      response_type=None,
    )
