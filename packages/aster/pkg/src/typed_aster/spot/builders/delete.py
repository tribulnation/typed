# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Builder to revoke."""

  builder: str
  """Spot Builder address to revoke."""
  signatureChainId: NotRequired[int]
  """EIP-712 domain chain ID of the signature: `56` (EVM wallets)."""


class Delete(RpcEndpoint):
  """Revoke a Spot Builder authorization."""

  async def delete(
    self,
    builder: str,
    *,
    signature_chain_id: int | None = None,
    validate: bool | None = None,
  ) -> None:
    """Revoke a Spot Builder authorization.

    Args:
      builder: Spot Builder address to revoke.
      signature_chain_id: EIP-712 domain chain ID of the signature: `56` (EVM wallets).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/asterCode/spot-endpoints/#delete-spot-builder-spot_trade)
    """
    request: Request = Request(builder=builder)
    if signature_chain_id is not None:
      request['signatureChainId'] = signature_chain_id
    return await self.request(
      request,
      method='DELETE',
      path='delBuilder',
      meta={'sign': 'typed', 'action': 'DelBuilder'},
      validate=validate,
      request_type=Request,
      response_type=None,
    )
