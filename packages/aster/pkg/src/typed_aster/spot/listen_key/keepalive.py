# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class ListenKeyKeepalive(TypedDict):
  """An empty object."""


class Request(TypedDict):
  """Keepalive request."""

  listenKey: NotRequired[str]
  """Accepted but ignored: the server derives the listenKey from the account."""


class Keepalive(RpcEndpoint):
  """Extend the listenKey by 60 minutes; send about every 30 minutes."""

  async def keepalive(
    self,
    listen_key: str | None = None,
    *,
    validate: bool | None = None,
  ) -> ListenKeyKeepalive:
    """Extend the listenKey by 60 minutes; send about every 30 minutes.

    Args:
      listen_key: Accepted but ignored: the server derives the listenKey from the account.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/spot-v3/websocket-account-info/#extend-listen-key-validity-period-user_stream)
    """
    request: Request = Request()
    if listen_key is not None:
      request['listenKey'] = listen_key
    return await self.request(
      request,
      method='PUT',
      path='listenKey',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=ListenKeyKeepalive,
    )
