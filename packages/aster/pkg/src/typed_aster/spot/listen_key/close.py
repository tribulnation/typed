# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class ListenKeyClosed(TypedDict):
  """An empty object."""


class Request(TypedDict):
  """Close request."""

  listenKey: NotRequired[str]
  """Accepted but ignored: the server derives the listenKey from the account."""


class Close(RpcEndpoint):
  """Close the user data stream."""

  async def close(
    self,
    listen_key: str | None = None,
    *,
    validate: bool | None = None,
  ) -> ListenKeyClosed:
    """Close the user data stream.

    Args:
      listen_key: Accepted but ignored: the server derives the listenKey from the account.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/spot-v3/websocket-account-info/#close-listen-key-user_stream)
    """
    request: Request = Request()
    if listen_key is not None:
      request['listenKey'] = listen_key
    return await self.request(
      request,
      method='DELETE',
      path='listenKey',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=ListenKeyClosed,
    )
