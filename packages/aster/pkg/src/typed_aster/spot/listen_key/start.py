# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict): ...


class SpotListenKey(TypedDict):
  """The listenKey."""

  listenKey: str
  """The listenKey (64 characters)."""


class Start(RpcEndpoint):
  """Start a user data stream, or return the current listenKey and extend it by 60 minutes."""

  async def start(self, *, validate: bool | None = None) -> SpotListenKey:
    """Start a user data stream, or return the current listenKey and extend it by 60 minutes.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/spot-v3/websocket-account-info/#generate-listen-key-user_stream)
    """
    return await self.request(
      Request(),
      method='POST',
      path='listenKey',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=SpotListenKey,
    )
