# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import Withdrawal


class Request(TypedDict):
  """Withdrawal request."""

  chainId: str
  """Destination chain ID: `1` (ETH), `56` (BSC), `42161` (Arbitrum)."""
  asset: str
  """Asset."""
  amount: Decimal
  """Amount, in token units."""
  fee: Decimal
  """Withdrawal fee, in token units (see `estimate_withdraw_fee`)."""
  receiver: str
  """Receiving address; must be the current account's address."""


class Withdraw(RpcEndpoint):
  """Withdraw an asset to an EVM chain."""

  async def withdraw(
    self,
    *,
    chain_id: str,
    asset: str,
    amount: Decimal,
    fee: Decimal,
    receiver: str,
    validate: bool | None = None,
  ) -> Withdrawal:
    """Withdraw an asset to an EVM chain.

    Args:
      chain_id: Destination chain ID: `1` (ETH), `56` (BSC), `42161` (Arbitrum).
      asset: Asset.
      amount: Amount, in token units.
      fee: Withdrawal fee, in token units (see `estimate_withdraw_fee`).
      receiver: Receiving address; must be the current account's address.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/spot-v3/account&trades/#withdraw-user_data)
    """
    return await self.request(
      Request(chainId=chain_id, asset=asset, amount=amount, fee=fee, receiver=receiver),
      method='POST',
      path='aster/user-withdraw',
      meta={'sign': 'withdraw'},
      validate=validate,
      request_type=Request,
      response_type=Withdrawal,
    )
