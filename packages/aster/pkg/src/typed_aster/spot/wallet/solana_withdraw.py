# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import Withdrawal


class Request(TypedDict):
  """Solana withdrawal request."""

  chainId: Literal['101']
  """Chain ID; always `101` (Solana)."""
  asset: str
  """Asset."""
  amount: Decimal
  """Amount, in token units (no trailing zeros)."""
  fee: Decimal
  """Withdrawal fee, in token units."""
  receiver: str
  """Solana receiving address."""


class SolanaWithdraw(RpcEndpoint):
  """Withdraw an asset to Solana."""

  async def solana_withdraw(
    self,
    chain_id: Literal['101'] = '101',
    *,
    asset: str,
    amount: Decimal,
    fee: Decimal,
    receiver: str,
    validate: bool | None = None,
  ) -> Withdrawal:
    """Withdraw an asset to Solana.

    Args:
      chain_id: Chain ID; always `101` (Solana).
      asset: Asset.
      amount: Amount, in token units (no trailing zeros).
      fee: Withdrawal fee, in token units.
      receiver: Solana receiving address.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/deposit&withdrawal/#withdraw-by-fapiv3-solana-spot)
    """
    return await self.request(
      Request(chainId=chain_id, asset=asset, amount=amount, fee=fee, receiver=receiver),
      method='POST',
      path='aster/user-solana-withdraw',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Withdrawal,
    )
