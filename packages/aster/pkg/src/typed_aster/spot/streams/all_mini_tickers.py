# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any
from typed_aster.core.endpoint.stream import StreamEndpoint
from typed_aster.schemas import MiniTickerEvent


Payload = list[MiniTickerEvent]


class AllMiniTickers(StreamEndpoint):
  """Rolling 24-hour mini tickers for every symbol that changed."""

  def all_mini_tickers(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[Payload, Any, Any]:
    """Rolling 24-hour mini tickers for every symbol that changed.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/spot-v3/websocket-market-streams/#compact-tickers-for-all-symbols-in-the-entire-market)
    """
    return self.subscribe(
      '!miniTicker@arr',
      validate=validate,
      response_type=Payload,
    )
