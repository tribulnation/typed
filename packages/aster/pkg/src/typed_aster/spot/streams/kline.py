# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal
from typed_aster.core.endpoint.stream import StreamEndpoint
from typed_aster.schemas import KlineEvent


class Kline(StreamEndpoint):
  """Updates of the current kline for one symbol and interval."""

  def kline(
    self,
    symbol: str,
    *,
    interval: Literal[
      '1m',
      '3m',
      '5m',
      '15m',
      '30m',
      '1h',
      '2h',
      '4h',
      '6h',
      '8h',
      '12h',
      '1d',
      '3d',
      '1w',
      '1M',
    ],
    validate: bool | None = None,
  ) -> StreamManager[KlineEvent, Any, Any]:
    """Updates of the current kline for one symbol and interval.

    Args:
      symbol: Symbol, lowercase, e.g. `btcusdt`.
      interval: Kline interval.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/spot-v3/websocket-market-streams/#k-line-streams)
    """
    return self.subscribe(
      f'{symbol}@kline_{interval}',
      validate=validate,
      response_type=KlineEvent,
    )
