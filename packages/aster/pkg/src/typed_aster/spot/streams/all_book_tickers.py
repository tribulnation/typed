# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any
from typed_aster.core.endpoint.stream import StreamEndpoint
from typed_aster.schemas import BookTickerEvent


class AllBookTickers(StreamEndpoint):
  """Best bid and ask for every symbol, in real time (one symbol per push)."""

  def all_book_tickers(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[BookTickerEvent, Any, Any]:
    """Best bid and ask for every symbol, in real time (one symbol per push).

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/spot-v3/websocket-market-streams/#best-order-book-information-across-the-entire-market)
    """
    return self.subscribe(
      '!bookTicker',
      validate=validate,
      response_type=BookTickerEvent,
    )
