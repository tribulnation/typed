# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any
from typed_aster.core.endpoint.stream import StreamEndpoint
from typed_aster.schemas import BookTickerEvent


class BookTicker(StreamEndpoint):
  """Best bid and ask for one symbol, in real time."""

  def book_ticker(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[BookTickerEvent, Any, Any]:
    """Best bid and ask for one symbol, in real time.

    Args:
      symbol: Symbol, lowercase, e.g. `btcusdt`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/spot-v3/websocket-market-streams/#best-order-book-information-by-symbol)
    """
    return self.subscribe(
      f'{symbol}@bookTicker',
      validate=validate,
      response_type=BookTickerEvent,
    )
