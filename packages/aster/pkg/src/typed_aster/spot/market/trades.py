# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import PublicTrade


class Request(TypedDict):
  """Recent trades request."""

  symbol: str
  """Trading pair, e.g. `BTCUSDT`."""
  limit: NotRequired[int]
  """Number of trades; maximum 1000."""


Response = list[PublicTrade]


class Trades(RpcEndpoint):
  """Get recent trades."""

  async def trades(
    self,
    symbol: str,
    *,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get recent trades.

    Args:
      symbol: Trading pair, e.g. `BTCUSDT`.
      limit: Number of trades; maximum 1000.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/spot-v3/market-data/#recent-trades-list)
    """
    request: Request = Request(symbol=symbol)
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='trades',
      meta={'sign': 'none'},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
