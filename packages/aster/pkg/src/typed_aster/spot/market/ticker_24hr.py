# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.spot.market.schemas import SpotTicker24hr


class Request(TypedDict):
  """24-hour ticker request."""

  symbol: NotRequired[str]
  """Trading pair; every symbol when omitted (array response, higher weight)."""


Response = SpotTicker24hr | list[SpotTicker24hr]


class Ticker24hr(RpcEndpoint):
  """Get 24-hour rolling window price change statistics."""

  async def ticker_24hr(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Get 24-hour rolling window price change statistics.

    Args:
      symbol: Trading pair; every symbol when omitted (array response, higher weight).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/spot-v3/market-data/#24h-price-change)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='ticker/24hr',
      meta={'sign': 'none'},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
