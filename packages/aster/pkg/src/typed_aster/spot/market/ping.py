# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class Pong(TypedDict):
  """An empty object."""


class Request(TypedDict): ...


class Ping(RpcEndpoint):
  """Test connectivity to the REST API."""

  async def ping(self, *, validate: bool | None = None) -> Pong:
    """Test connectivity to the REST API.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/spot-v3/market-data/#test-server-connectivity)
    """
    return await self.request(
      Request(),
      method='GET',
      path='ping',
      meta={'sign': 'none'},
      validate=validate,
      request_type=Request,
      response_type=Pong,
    )
