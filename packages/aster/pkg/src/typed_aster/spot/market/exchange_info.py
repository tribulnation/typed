# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_aster.core import TimestampMillis
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import (
  LotSizeFilter,
  MarketLotSizeFilter,
  MaxNotionalFilter,
  MaxNumOrdersFilter,
  MinNotionalFilter,
  NotionalFilter,
  PercentPriceBySideFilter,
  PercentPriceFilter,
  PriceFilter,
  RateLimit,
)


class Request(TypedDict): ...


class SpotAsset(TypedDict):
  """One asset."""

  asset: str
  """Asset name."""


class SpotSymbol(TypedDict):
  """One trading pair."""

  symbol: str
  """Trading pair, e.g. `BTCUSDT`."""
  status: Literal['TRADING']
  """Symbol status."""
  baseAsset: str
  """Base asset."""
  quoteAsset: str
  """Quote asset."""
  pricePrecision: int
  """Price precision."""
  quantityPrecision: int
  """Quantity precision."""
  baseAssetPrecision: int
  """Base asset precision."""
  quotePrecision: int
  """Quote asset precision."""
  listingTime: NotRequired[TimestampMillis]
  """Listing time."""
  symbolType: NotRequired[int]
  """Symbol type code (undocumented; `0` observed)."""
  baseAssetAddress: NotRequired[str | None]
  """Base asset token contract address, or `null`."""
  filters: list[
    PriceFilter
    | LotSizeFilter
    | MarketLotSizeFilter
    | MaxNumOrdersFilter
    | MinNotionalFilter
    | MaxNotionalFilter
    | NotionalFilter
    | PercentPriceFilter
    | PercentPriceBySideFilter
  ]
  """Symbol filters."""
  orderTypes: list[
    Literal[
      'LIMIT', 'MARKET', 'STOP', 'TAKE_PROFIT', 'STOP_MARKET', 'TAKE_PROFIT_MARKET'
    ]
  ]
  """Allowed order types."""
  timeInForce: list[Literal['GTC', 'IOC', 'FOK', 'GTX', 'HIDDEN']]
  """Allowed time-in-force values."""
  ocoAllowed: bool
  """Whether OCO orders are allowed."""


class SpotExchangeInfo(TypedDict):
  """Exchange trading rules and symbols."""

  timezone: str
  """Server timezone, e.g. `UTC`."""
  serverTime: TimestampMillis
  """Server time."""
  rateLimits: list[RateLimit]
  """Rate limits."""
  exchangeFilters: list[dict[str, Any]]
  """Exchange-wide filters (not supported yet; always empty)."""
  assets: list[SpotAsset]
  """Listed assets."""
  symbols: list[SpotSymbol]
  """Symbols."""


class ExchangeInfo(RpcEndpoint):
  """Get the exchange trading rules and symbol information."""

  async def exchange_info(self, *, validate: bool | None = None) -> SpotExchangeInfo:
    """Get the exchange trading rules and symbol information.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/spot-v3/market-data/#trading-specification-information)
    """
    return await self.request(
      Request(),
      method='GET',
      path='exchangeInfo',
      meta={'sign': 'none'},
      validate=validate,
      request_type=Request,
      response_type=SpotExchangeInfo,
    )
