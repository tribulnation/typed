# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.spot.market.schemas import SpotOptTicker


class Request(TypedDict):
  """Alternate 24-hour ticker request."""

  symbol: NotRequired[str]
  """Trading pair; every symbol when omitted (array response, higher weight)."""


Response = SpotOptTicker | list[SpotOptTicker]


class Ticker24hrOpt(RpcEndpoint):
  """Get 24-hour statistics in an alternate (CoinGecko-style, snake_case) shape."""

  async def ticker_24hr_opt(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Get 24-hour statistics in an alternate (CoinGecko-style, snake_case) shape.

    Args:
      symbol: Trading pair; every symbol when omitted (array response, higher weight).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/spot-v3/market-data/#24h-price-change)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='ticker/opt/24hr',
      meta={'sign': 'none'},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
