# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.spot.market.schemas import SpotBookTicker


class Request(TypedDict):
  """Best bid/ask request."""

  symbol: NotRequired[str]
  """Trading pair; every symbol when omitted (array response, higher weight)."""


Response = SpotBookTicker | list[SpotBookTicker]


class BookTicker(RpcEndpoint):
  """Get the best bid and ask for one or every symbol."""

  async def book_ticker(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Get the best bid and ask for one or every symbol.

    Args:
      symbol: Trading pair; every symbol when omitted (array response, higher weight).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/spot-v3/market-data/#current-best-order)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='ticker/bookTicker',
      meta={'sign': 'none'},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
