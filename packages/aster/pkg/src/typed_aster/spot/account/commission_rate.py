# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import SymbolCommissionRate


class Request(TypedDict):
  """Commission rate request."""

  symbol: str
  """Trading pair, e.g. `BTCUSDT`."""


class CommissionRate(RpcEndpoint):
  """Get the account's commission rates for a symbol."""

  async def commission_rate(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> SymbolCommissionRate:
    """Get the account's commission rates for a symbol.

    Args:
      symbol: Trading pair, e.g. `BTCUSDT`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/spot-v3/market-data/#get-symbol-fees)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='commissionRate',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=SymbolCommissionRate,
    )
