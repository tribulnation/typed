# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.spot.trade.schemas import SpotOrder


class LimitOrder(TypedDict):
  """A `LIMIT` order."""

  symbol: str
  """Trading pair, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['LIMIT']
  """Order type."""
  timeInForce: Literal['GTC', 'IOC', 'FOK', 'GTX', 'HIDDEN']
  """Time in force (`FOK` is rejected on spot)."""
  quantity: Decimal
  """Quantity."""
  price: Decimal
  """Limit price."""
  newClientOrderId: NotRequired[str]
  """Client order ID, unique among open orders; generated when omitted."""
  selfTradingProtectionMode: NotRequired[Literal[1, 2, 4, 8]]
  """Self-trade protection: 1 NONE, 2 CANCEL_TAKER, 4 CANCEL_MAKER, 8 CANCEL_BOTH; the symbol default when omitted."""
  builder: NotRequired[str]
  """Authorized Spot Builder address to attribute the order to."""
  feeRate: NotRequired[Decimal]
  """Builder fee rate for this order; required when `builder` is sent, at most the approved `maxFeeRate`."""
  nonce: NotRequired[int]
  """Caller-chosen request nonce, in epoch microseconds, signed in place of a freshly drawn one. Choose it (unique, within the venue's time window) when you may later need to reference this request: a guarded cancel of the order, or `noop` to invalidate it while still pending. Omit to let the client draw one."""


class MarketOrder(TypedDict):
  """A `MARKET` order sized in the base asset."""

  symbol: str
  """Trading pair, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['MARKET']
  """Order type."""
  quantity: Decimal
  """Base asset quantity."""
  newClientOrderId: NotRequired[str]
  """Client order ID, unique among open orders; generated when omitted."""
  selfTradingProtectionMode: NotRequired[Literal[1, 2, 4, 8]]
  """Self-trade protection: 1 NONE, 2 CANCEL_TAKER, 4 CANCEL_MAKER, 8 CANCEL_BOTH; the symbol default when omitted."""
  builder: NotRequired[str]
  """Authorized Spot Builder address to attribute the order to."""
  feeRate: NotRequired[Decimal]
  """Builder fee rate for this order; required when `builder` is sent, at most the approved `maxFeeRate`."""
  nonce: NotRequired[int]
  """Caller-chosen request nonce, in epoch microseconds, signed in place of a freshly drawn one. Choose it (unique, within the venue's time window) when you may later need to reference this request: a guarded cancel of the order, or `noop` to invalidate it while still pending. Omit to let the client draw one."""


class MarketQuoteOrder(TypedDict):
  """A `MARKET` order sized by the quote amount to spend or receive."""

  symbol: str
  """Trading pair, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['MARKET']
  """Order type."""
  quoteOrderQty: Decimal
  """Quote asset amount."""
  newClientOrderId: NotRequired[str]
  """Client order ID, unique among open orders; generated when omitted."""
  selfTradingProtectionMode: NotRequired[Literal[1, 2, 4, 8]]
  """Self-trade protection: 1 NONE, 2 CANCEL_TAKER, 4 CANCEL_MAKER, 8 CANCEL_BOTH; the symbol default when omitted."""
  builder: NotRequired[str]
  """Authorized Spot Builder address to attribute the order to."""
  feeRate: NotRequired[Decimal]
  """Builder fee rate for this order; required when `builder` is sent, at most the approved `maxFeeRate`."""
  nonce: NotRequired[int]
  """Caller-chosen request nonce, in epoch microseconds, signed in place of a freshly drawn one. Choose it (unique, within the venue's time window) when you may later need to reference this request: a guarded cancel of the order, or `noop` to invalidate it while still pending. Omit to let the client draw one."""


class StopLimitOrder(TypedDict):
  """A `STOP` or `TAKE_PROFIT` (limit) order."""

  symbol: str
  """Trading pair, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['STOP', 'TAKE_PROFIT']
  """Order type."""
  timeInForce: NotRequired[Literal['GTC', 'IOC', 'FOK', 'GTX', 'HIDDEN']]
  """Time in force."""
  quantity: Decimal
  """Quantity."""
  price: Decimal
  """Limit price."""
  stopPrice: Decimal
  """Trigger price."""
  newClientOrderId: NotRequired[str]
  """Client order ID, unique among open orders; generated when omitted."""
  selfTradingProtectionMode: NotRequired[Literal[1, 2, 4, 8]]
  """Self-trade protection: 1 NONE, 2 CANCEL_TAKER, 4 CANCEL_MAKER, 8 CANCEL_BOTH; the symbol default when omitted."""
  builder: NotRequired[str]
  """Authorized Spot Builder address to attribute the order to."""
  feeRate: NotRequired[Decimal]
  """Builder fee rate for this order; required when `builder` is sent, at most the approved `maxFeeRate`."""
  nonce: NotRequired[int]
  """Caller-chosen request nonce, in epoch microseconds, signed in place of a freshly drawn one. Choose it (unique, within the venue's time window) when you may later need to reference this request: a guarded cancel of the order, or `noop` to invalidate it while still pending. Omit to let the client draw one."""


class StopMarketOrder(TypedDict):
  """A `STOP_MARKET` or `TAKE_PROFIT_MARKET` order."""

  symbol: str
  """Trading pair, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['STOP_MARKET', 'TAKE_PROFIT_MARKET']
  """Order type."""
  quantity: Decimal
  """Quantity."""
  stopPrice: Decimal
  """Trigger price."""
  newClientOrderId: NotRequired[str]
  """Client order ID, unique among open orders; generated when omitted."""
  selfTradingProtectionMode: NotRequired[Literal[1, 2, 4, 8]]
  """Self-trade protection: 1 NONE, 2 CANCEL_TAKER, 4 CANCEL_MAKER, 8 CANCEL_BOTH; the symbol default when omitted."""
  builder: NotRequired[str]
  """Authorized Spot Builder address to attribute the order to."""
  feeRate: NotRequired[Decimal]
  """Builder fee rate for this order; required when `builder` is sent, at most the approved `maxFeeRate`."""
  nonce: NotRequired[int]
  """Caller-chosen request nonce, in epoch microseconds, signed in place of a freshly drawn one. Choose it (unique, within the venue's time window) when you may later need to reference this request: a guarded cancel of the order, or `noop` to invalidate it while still pending. Omit to let the client draw one."""


Request = LimitOrder | MarketOrder | MarketQuoteOrder | StopLimitOrder | StopMarketOrder


class PlaceOrder(RpcEndpoint):
  """Place a spot order."""

  async def place_order(
    self,
    place_order_request: Request,
    *,
    validate: bool | None = None,
  ) -> SpotOrder:
    """Place a spot order.

    Args:
      place_order_request: The order to place, by type.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/spot-v3/account&trades/#place-order-trade)
    """
    return await self.request(
      place_order_request,
      method='POST',
      path='order',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=SpotOrder,
    )
