# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import BatchError
from typed_aster.spot.trade.schemas import SpotOrder


class BatchLimitOrder(TypedDict):
  """A `LIMIT` order."""

  symbol: str
  """Trading pair, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['LIMIT']
  """Order type."""
  timeInForce: Literal['GTC', 'IOC', 'FOK', 'GTX', 'HIDDEN']
  """Time in force (`FOK` is rejected on spot)."""
  quantity: Decimal
  """Quantity."""
  price: Decimal
  """Limit price."""
  newClientOrderId: NotRequired[str]
  """Client order ID, unique among open orders; generated when omitted."""
  selfTradingProtectionMode: NotRequired[Literal[1, 2, 4, 8]]
  """Self-trade protection: 1 NONE, 2 CANCEL_TAKER, 4 CANCEL_MAKER, 8 CANCEL_BOTH; the symbol default when omitted."""


class BatchMarketOrder(TypedDict):
  """A `MARKET` order sized in the base asset."""

  symbol: str
  """Trading pair, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['MARKET']
  """Order type."""
  quantity: Decimal
  """Base asset quantity."""
  newClientOrderId: NotRequired[str]
  """Client order ID, unique among open orders; generated when omitted."""
  selfTradingProtectionMode: NotRequired[Literal[1, 2, 4, 8]]
  """Self-trade protection: 1 NONE, 2 CANCEL_TAKER, 4 CANCEL_MAKER, 8 CANCEL_BOTH; the symbol default when omitted."""


class BatchMarketQuoteOrder(TypedDict):
  """A `MARKET` order sized by the quote amount to spend or receive."""

  symbol: str
  """Trading pair, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['MARKET']
  """Order type."""
  quoteOrderQty: Decimal
  """Quote asset amount."""
  newClientOrderId: NotRequired[str]
  """Client order ID, unique among open orders; generated when omitted."""
  selfTradingProtectionMode: NotRequired[Literal[1, 2, 4, 8]]
  """Self-trade protection: 1 NONE, 2 CANCEL_TAKER, 4 CANCEL_MAKER, 8 CANCEL_BOTH; the symbol default when omitted."""


class BatchStopLimitOrder(TypedDict):
  """A `STOP` or `TAKE_PROFIT` (limit) order."""

  symbol: str
  """Trading pair, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['STOP', 'TAKE_PROFIT']
  """Order type."""
  timeInForce: NotRequired[Literal['GTC', 'IOC', 'FOK', 'GTX', 'HIDDEN']]
  """Time in force."""
  quantity: Decimal
  """Quantity."""
  price: Decimal
  """Limit price."""
  stopPrice: Decimal
  """Trigger price."""
  newClientOrderId: NotRequired[str]
  """Client order ID, unique among open orders; generated when omitted."""
  selfTradingProtectionMode: NotRequired[Literal[1, 2, 4, 8]]
  """Self-trade protection: 1 NONE, 2 CANCEL_TAKER, 4 CANCEL_MAKER, 8 CANCEL_BOTH; the symbol default when omitted."""


class BatchStopMarketOrder(TypedDict):
  """A `STOP_MARKET` or `TAKE_PROFIT_MARKET` order."""

  symbol: str
  """Trading pair, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  type: Literal['STOP_MARKET', 'TAKE_PROFIT_MARKET']
  """Order type."""
  quantity: Decimal
  """Quantity."""
  stopPrice: Decimal
  """Trigger price."""
  newClientOrderId: NotRequired[str]
  """Client order ID, unique among open orders; generated when omitted."""
  selfTradingProtectionMode: NotRequired[Literal[1, 2, 4, 8]]
  """Self-trade protection: 1 NONE, 2 CANCEL_TAKER, 4 CANCEL_MAKER, 8 CANCEL_BOTH; the symbol default when omitted."""


class Request(TypedDict):
  """Orders to place."""

  batchOrders: list[
    BatchLimitOrder
    | BatchMarketOrder
    | BatchMarketQuoteOrder
    | BatchStopLimitOrder
    | BatchStopMarketOrder
  ]
  """Orders to place (1 to 5), sent as a JSON list."""
  nonce: NotRequired[int]
  """Caller-chosen request nonce, in epoch microseconds, signed in place of a freshly drawn one. Choose it (unique, within the venue's time window) when you may later need to reference this request: a guarded cancel of the order, or `noop` to invalidate it while still pending. Omit to let the client draw one."""


Response = list[SpotOrder | BatchError]


class PlaceBatchOrders(RpcEndpoint):
  """Place up to 5 spot orders in one request; they are processed concurrently."""

  async def place_batch_orders(
    self,
    batch_orders: list[
      BatchLimitOrder
      | BatchMarketOrder
      | BatchMarketQuoteOrder
      | BatchStopLimitOrder
      | BatchStopMarketOrder
    ],
    *,
    nonce: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Place up to 5 spot orders in one request; they are processed concurrently.

    Args:
      batch_orders: Orders to place (1 to 5), sent as a JSON list.
      nonce: Caller-chosen request nonce, in epoch microseconds, signed in place of a freshly drawn one. Choose it (unique, within the venue's time window) when you may later need to reference this request: a guarded cancel of the order, or `noop` to invalidate it while still pending. Omit to let the client draw one.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/spot-v3/account&trades/#cancel-all-open-orders-trade)
    """
    request: Request = Request(batchOrders=batch_orders)
    if nonce is not None:
      request['nonce'] = nonce
    return await self.request(
      request,
      method='POST',
      path='batchOrders',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
