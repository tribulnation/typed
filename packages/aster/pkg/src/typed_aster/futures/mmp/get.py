# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class MmpConfig(TypedDict):
  """One symbol's MMP configuration."""

  symbol: str
  """Symbol."""
  windowTimeInMilliseconds: int
  """Measurement window, in ms."""
  frozenTimeInMilliseconds: int
  """Frozen period after a trigger, in ms."""
  qtyLimit: NotRequired[int]
  """Filled-quantity limit."""
  valueLimit: NotRequired[int]
  """Notional limit."""
  deltaLimit: NotRequired[int]
  """Net position change limit."""


class Request(TypedDict):
  """Optional symbol filter."""

  symbol: NotRequired[str]
  """Symbol; every configured symbol when omitted."""


Response = list[MmpConfig]


class Get(RpcEndpoint):
  """Get the account's MMP configuration, for one symbol or all."""

  async def get(
    self, symbol: str | None = None, *, validate: bool | None = None
  ) -> Response:
    """Get the account's MMP configuration, for one symbol or all.

    Args:
      symbol: Symbol; every configured symbol when omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#get-user-mmp-user_data)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='mmp',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
