# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Symbol whose MMP state to reset."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""


Response = bool


class Reset(RpcEndpoint):
  """Reset a symbol's MMP state, lifting a triggered freeze."""

  async def reset(self, symbol: str, *, validate: bool | None = None) -> Response:
    """Reset a symbol's MMP state, lifting a triggered freeze.

    Args:
      symbol: Symbol, e.g. `BTCUSDT`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#reset-user-mmp-user_data)
    """
    return await self.request(
      Request(symbol=symbol),
      method='POST',
      path='mmpReset',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
