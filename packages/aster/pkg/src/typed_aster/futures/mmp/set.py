# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """MMP configuration."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  windowTimeInMilliseconds: int
  """Window, in ms, over which `qtyLimit`, `valueLimit` and `deltaLimit` are measured."""
  frozenTimeInMilliseconds: int
  """Duration, in ms, during which MMP orders are prohibited once a limit is triggered."""
  qtyLimit: NotRequired[int]
  """Maximum cumulative filled quantity (buys and sells) within the window."""
  valueLimit: NotRequired[int]
  """Maximum cumulative notional within the window."""
  deltaLimit: NotRequired[int]
  """Maximum cumulative net position change (buys minus sells) within the window."""


Response = bool


class Set(RpcEndpoint):
  """Set a symbol's market maker protection: when a limit is reached within the window, MMP orders are prohibited for the frozen period."""

  async def set(
    self,
    symbol: str,
    *,
    window_time_in_milliseconds: int,
    frozen_time_in_milliseconds: int,
    qty_limit: int | None = None,
    value_limit: int | None = None,
    delta_limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Set a symbol's market maker protection: when a limit is reached within the window, MMP orders are prohibited for the frozen period.

    Args:
      symbol: Symbol, e.g. `BTCUSDT`.
      window_time_in_milliseconds: Window, in ms, over which `qtyLimit`, `valueLimit` and `deltaLimit` are measured.
      frozen_time_in_milliseconds: Duration, in ms, during which MMP orders are prohibited once a limit is triggered.
      qty_limit: Maximum cumulative filled quantity (buys and sells) within the window.
      value_limit: Maximum cumulative notional within the window.
      delta_limit: Maximum cumulative net position change (buys minus sells) within the window.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#update-user-mmp-user_data)
    """
    request: Request = Request(
      symbol=symbol,
      windowTimeInMilliseconds=window_time_in_milliseconds,
      frozenTimeInMilliseconds=frozen_time_in_milliseconds,
    )
    if qty_limit is not None:
      request['qtyLimit'] = qty_limit
    if value_limit is not None:
      request['valueLimit'] = value_limit
    if delta_limit is not None:
      request['deltaLimit'] = delta_limit
    return await self.request(
      request,
      method='POST',
      path='mmp',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
