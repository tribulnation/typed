# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import OperationResult


class Request(TypedDict):
  """Builder to approve."""

  builder: str
  """Builder address (attribution and fee recipient)."""
  maxFeeRate: Decimal
  """Maximum fee rate the builder may charge; at most `0.001`."""


class Approve(RpcEndpoint):
  """Approve a futures builder and cap the fee rate it may charge. Signed by the main wallet (EIP-712 `ApproveBuilder`)."""

  async def approve(
    self,
    builder: str,
    *,
    max_fee_rate: Decimal,
    validate: bool | None = None,
  ) -> OperationResult:
    """Approve a futures builder and cap the fee rate it may charge. Signed by the main wallet (EIP-712 `ApproveBuilder`).

    Args:
      builder: Builder address (attribution and fee recipient).
      max_fee_rate: Maximum fee rate the builder may charge; at most `0.001`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/asterCode/endpoints/#approve-builder-trade)
    """
    return await self.request(
      Request(builder=builder, maxFeeRate=max_fee_rate),
      method='POST',
      path='approveBuilder',
      meta={'sign': 'typed', 'action': 'ApproveBuilder'},
      validate=validate,
      request_type=Request,
      response_type=OperationResult,
    )
