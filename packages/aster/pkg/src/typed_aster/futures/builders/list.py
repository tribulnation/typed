# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class ApprovedBuilder(TypedDict):
  """An approved builder."""

  userAddress: str
  """User address."""
  builderAddress: str
  """Builder address."""
  maxFeeRate: float
  """Maximum fee rate the builder may charge (a JSON number on the wire)."""
  builderName: NotRequired[str]
  """Builder name."""


class Request(TypedDict):
  """No business parameters."""


Response = list[ApprovedBuilder]


class List(RpcEndpoint):
  """List the builders the user has approved and their fee-rate caps."""

  async def list(self, *, validate: bool | None = None) -> Response:
    """List the builders the user has approved and their fee-rate caps.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/asterCode/endpoints/#get-builders-user_data)
    """
    return await self.request(
      Request(),
      method='GET',
      path='builder',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
