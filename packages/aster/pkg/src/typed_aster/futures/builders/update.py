# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import OperationResult


class Request(TypedDict):
  """New fee-rate cap."""

  builder: str
  """Builder address."""
  maxFeeRate: Decimal
  """New maximum fee rate; at most `0.001`."""


class Update(RpcEndpoint):
  """Change a builder's maximum fee rate. Signed by the main wallet (EIP-712 `UpdateBuilder`)."""

  async def update(
    self,
    builder: str,
    *,
    max_fee_rate: Decimal,
    validate: bool | None = None,
  ) -> OperationResult:
    """Change a builder's maximum fee rate. Signed by the main wallet (EIP-712 `UpdateBuilder`).

    Args:
      builder: Builder address.
      max_fee_rate: New maximum fee rate; at most `0.001`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/asterCode/endpoints/#update-builder-trade)
    """
    return await self.request(
      Request(builder=builder, maxFeeRate=max_fee_rate),
      method='POST',
      path='updateBuilder',
      meta={'sign': 'typed', 'action': 'UpdateBuilder'},
      validate=validate,
      request_type=Request,
      response_type=OperationResult,
    )
