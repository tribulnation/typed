# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import OperationResult


class Request(TypedDict):
  """Builder to revoke."""

  builder: str
  """Builder address."""


class Delete(RpcEndpoint):
  """Revoke a builder's authorization. Signed by the main wallet (EIP-712 `DelBuilder`)."""

  async def delete(
    self, builder: str, *, validate: bool | None = None
  ) -> OperationResult:
    """Revoke a builder's authorization. Signed by the main wallet (EIP-712 `DelBuilder`).

    Args:
      builder: Builder address.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/asterCode/endpoints/#delete-builder-trade)
    """
    return await self.request(
      Request(builder=builder),
      method='DELETE',
      path='builder',
      meta={'sign': 'typed', 'action': 'DelBuilder'},
      validate=validate,
      request_type=Request,
      response_type=OperationResult,
    )
