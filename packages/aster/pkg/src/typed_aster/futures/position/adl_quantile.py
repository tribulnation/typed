# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.futures.position.schemas import FuturesAdlQuantile


class NoAdlQuantile(TypedDict):
  """Empty object: the requested symbol has no open position."""


class Request(TypedDict):
  symbol: NotRequired[str]
  """Symbol, e.g. `BTCUSDT`; every symbol with a position when omitted."""


Response = list[FuturesAdlQuantile] | FuturesAdlQuantile | NoAdlQuantile


class AdlQuantile(RpcEndpoint):
  """Get the ADL (auto-deleveraging) quantile estimation of open positions. Values update every 30 s."""

  async def adl_quantile(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Get the ADL (auto-deleveraging) quantile estimation of open positions. Values update every 30 s.

    Args:
      symbol: Symbol, e.g. `BTCUSDT`; every symbol with a position when omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#position-adl-quantile-estimation-user_data)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='adlQuantile',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
