# Generated by typed-dev — see CONTRIBUTING.md.
"""Shapes shared by several endpoints of `futures.position`."""

from typing_extensions import NotRequired, TypedDict


class AdlQuantileValues(TypedDict):
  """ADL quantile (0 to 4, low to high ADL likelihood) per position side. `BOTH` appears for One-way or isolated Hedge positions; `HEDGE` replaces it (as a flag, value to be ignored) for crossed Hedge positions."""

  LONG: NotRequired[int]
  """Quantile of the `LONG` position."""
  SHORT: NotRequired[int]
  """Quantile of the `SHORT` position."""
  BOTH: NotRequired[int]
  """Quantile of the One-way (`BOTH`) position."""
  HEDGE: NotRequired[int]
  """Flag for crossed Hedge-mode positions; ignore the value."""


class FuturesAdlQuantile(TypedDict):
  """ADL quantiles of one symbol."""

  symbol: str
  """Symbol name."""
  adlQuantile: AdlQuantileValues
