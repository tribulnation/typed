# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_aster.core import TimestampMillis
from typed_aster.core.endpoint.rpc import RpcEndpoint


class PositionRisk(TypedDict):
  """Position on one symbol and side."""

  symbol: str
  """Symbol name."""
  positionAmt: Decimal
  """Position amount; negative for short."""
  entryPrice: Decimal
  """Average entry price."""
  markPrice: Decimal
  """Mark price."""
  unRealizedProfit: Decimal
  """Unrealized profit."""
  liquidationPrice: Decimal
  """Liquidation price; `0` when none."""
  leverage: int
  """Current initial leverage."""
  maxNotionalValue: Decimal
  """Maximum available notional with current leverage."""
  marginType: Literal['cross', 'isolated']
  """Margin type."""
  isolatedMargin: Decimal
  """Isolated margin."""
  isAutoAddMargin: bool
  """Whether auto-add-margin is enabled, as `"true"`/`"false"`."""
  positionSide: Literal['BOTH', 'LONG', 'SHORT']
  """Position side."""
  notional: Decimal
  """Position notional value."""
  isolatedWallet: Decimal
  """Isolated wallet balance."""
  updateTime: TimestampMillis
  """Last update time; `0` when never updated."""


class Request(TypedDict):
  symbol: NotRequired[str]
  """Symbol, e.g. `BTCUSDT`; every symbol when omitted."""


Response = list[PositionRisk]


class Risk(RpcEndpoint):
  """Get current position information: every symbol (including flat positions), or one symbol. Use with the user data stream `ACCOUNT_UPDATE` for timeliness."""

  async def risk(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Get current position information: every symbol (including flat positions), or one symbol. Use with the user data stream `ACCOUNT_UPDATE` for timeliness.

    Args:
      symbol: Symbol, e.g. `BTCUSDT`; every symbol when omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#position-information-v3-user_data)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='positionRisk',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
