# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_aster.core import TimestampMillis
from typed_aster.core.endpoint.stream import StreamEndpoint


class FuturesStreamKline(TypedDict):
  """The current kline."""

  t: TimestampMillis
  """Kline start time."""
  T: TimestampMillis
  """Kline close time."""
  s: str
  """Symbol, e.g. `BTCUSDT`."""
  i: Literal[
    '1m',
    '3m',
    '5m',
    '15m',
    '30m',
    '1h',
    '2h',
    '4h',
    '6h',
    '8h',
    '12h',
    '1d',
    '3d',
    '1w',
    '1M',
  ]
  """Interval."""
  f: int
  """First trade id."""
  L: int
  """Last trade id."""
  o: Decimal
  """Open price."""
  c: Decimal
  """Close price."""
  h: Decimal
  """High price."""
  l: Decimal
  """Low price."""
  v: Decimal
  """Base asset volume."""
  n: int
  """Number of trades."""
  x: bool
  """Whether the kline is closed."""
  q: Decimal
  """Quote asset volume."""
  V: Decimal
  """Taker buy base asset volume."""
  Q: Decimal
  """Taker buy quote asset volume."""
  B: Decimal
  """Ignore."""


class FuturesKlineEvent(TypedDict):
  """Kline update."""

  e: Literal['kline']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Symbol, e.g. `BTCUSDT`."""
  k: FuturesStreamKline


class Kline(StreamEndpoint):
  """Updates to the current kline of one symbol, every 250 ms while it changes."""

  def kline(
    self,
    symbol: str,
    *,
    interval: Literal[
      '1m',
      '3m',
      '5m',
      '15m',
      '30m',
      '1h',
      '2h',
      '4h',
      '6h',
      '8h',
      '12h',
      '1d',
      '3d',
      '1w',
      '1M',
    ],
    validate: bool | None = None,
  ) -> StreamManager[FuturesKlineEvent, Any, Any]:
    """Updates to the current kline of one symbol, every 250 ms while it changes.

    Args:
      symbol: Symbol, lowercase, e.g. `btcusdt`.
      interval: Kline interval.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/websocket-market-streams/#klinecandlestick-streams)
    """
    return self.subscribe(
      f'{symbol}@kline_{interval}',
      validate=validate,
      response_type=FuturesKlineEvent,
    )
