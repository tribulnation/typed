# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any
from typed_aster.core.endpoint.stream import StreamEndpoint
from typed_aster.futures.streams.schemas import FuturesBookTickerEvent


class AllBookTickers(StreamEndpoint):
  """Real-time best bid/ask updates of every symbol."""

  def all_book_tickers(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[FuturesBookTickerEvent, Any, Any]:
    """Real-time best bid/ask updates of every symbol.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/websocket-market-streams/#all-book-tickers-stream)
    """
    return self.subscribe(
      '!bookTicker',
      validate=validate,
      response_type=FuturesBookTickerEvent,
    )
