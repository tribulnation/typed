# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any
from typed_aster.core.endpoint.stream import StreamEndpoint
from typed_aster.futures.streams.schemas import FuturesMarkPriceUpdate


class MarkPrice1s(StreamEndpoint):
  """Mark price and funding rate of one symbol, pushed every second."""

  def mark_price_1s(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[FuturesMarkPriceUpdate, Any, Any]:
    """Mark price and funding rate of one symbol, pushed every second.

    Args:
      symbol: Symbol, lowercase, e.g. `btcusdt`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/websocket-market-streams/#mark-price-stream)
    """
    return self.subscribe(
      f'{symbol}@markPrice@1s',
      validate=validate,
      response_type=FuturesMarkPriceUpdate,
    )
