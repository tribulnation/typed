# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any
from typed_aster.core.endpoint.stream import StreamEndpoint
from typed_aster.schemas import TickerEvent


Payload = list[TickerEvent]


class AllTickers(StreamEndpoint):
  """24-hour rolling window tickers of every symbol that changed, pushed every second."""

  def all_tickers(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[Payload, Any, Any]:
    """24-hour rolling window tickers of every symbol that changed, pushed every second.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/websocket-market-streams/#all-market-tickers-streams)
    """
    return self.subscribe(
      '!ticker@arr',
      validate=validate,
      response_type=Payload,
    )
