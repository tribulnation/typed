# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal
from typed_aster.core.endpoint.stream import StreamEndpoint
from typed_aster.schemas import DepthUpdate


class PartialDepthSpeed(StreamEndpoint):
  """Top bids and asks of one symbol, pushed at a chosen speed."""

  def partial_depth_speed(
    self,
    symbol: str,
    *,
    levels: Literal[5, 10, 20],
    speed: Literal['500ms', '100ms'],
    validate: bool | None = None,
  ) -> StreamManager[DepthUpdate, Any, Any]:
    """Top bids and asks of one symbol, pushed at a chosen speed.

    Args:
      symbol: Symbol, lowercase, e.g. `btcusdt`.
      levels: Number of levels per side.
      speed: Update speed.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/websocket-market-streams/#partial-book-depth-streams)
    """
    return self.subscribe(
      f'{symbol}@depth{levels}@{speed}',
      validate=validate,
      response_type=DepthUpdate,
    )
