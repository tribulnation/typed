# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any
from typed_aster.core.endpoint.stream import StreamEndpoint
from typed_aster.schemas import DepthUpdate


class DiffDepth(StreamEndpoint):
  """Order book changes of one symbol, pushed every 250 ms."""

  def diff_depth(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[DepthUpdate, Any, Any]:
    """Order book changes of one symbol, pushed every 250 ms.

    Args:
      symbol: Symbol, lowercase, e.g. `btcusdt`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/websocket-market-streams/#diff-book-depth-streams)
    """
    return self.subscribe(
      f'{symbol}@depth',
      validate=validate,
      response_type=DepthUpdate,
    )
