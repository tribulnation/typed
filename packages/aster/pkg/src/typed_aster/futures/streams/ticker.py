# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any
from typed_aster.core.endpoint.stream import StreamEndpoint
from typed_aster.schemas import TickerEvent


class Ticker(StreamEndpoint):
  """24-hour rolling window ticker of one symbol, pushed every 500 ms."""

  def ticker(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[TickerEvent, Any, Any]:
    """24-hour rolling window ticker of one symbol, pushed every 500 ms.

    Args:
      symbol: Symbol, lowercase, e.g. `btcusdt`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/websocket-market-streams/#individual-symbol-ticker-streams)
    """
    return self.subscribe(
      f'{symbol}@ticker',
      validate=validate,
      response_type=TickerEvent,
    )
