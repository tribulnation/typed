# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any
from typed_aster.core.endpoint.stream import StreamEndpoint
from typed_aster.futures.streams.schemas import FuturesForceOrderEvent


class AllForceOrders(StreamEndpoint):
  """Latest liquidation order of every symbol per second; nothing is pushed without liquidations."""

  def all_force_orders(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[FuturesForceOrderEvent, Any, Any]:
    """Latest liquidation order of every symbol per second; nothing is pushed without liquidations.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/websocket-market-streams/#all-market-liquidation-order-streams)
    """
    return self.subscribe(
      '!forceOrder@arr',
      validate=validate,
      response_type=FuturesForceOrderEvent,
    )
