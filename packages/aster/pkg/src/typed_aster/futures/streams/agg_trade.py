# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_aster.core import TimestampMillis
from typed_aster.core.endpoint.stream import StreamEndpoint


class AggTradeEventPayload(TypedDict):
  """One aggregate trade."""

  e: Literal['aggTrade']
  """Event type."""
  E: TimestampMillis
  """Event time."""
  s: str
  """Symbol, e.g. `BTCUSDT`."""
  a: int
  """Aggregate trade id."""
  p: Decimal
  """Price."""
  q: Decimal
  """Quantity."""
  f: int
  """First trade id."""
  l: int
  """Last trade id."""
  T: TimestampMillis
  """Trade time."""
  m: bool
  """Whether the buyer is the maker."""


class AggTrade(StreamEndpoint):
  """Aggregate trades of one symbol: fills of one taker order at one price, pushed every 100 ms."""

  def agg_trade(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[AggTradeEventPayload, Any, Any]:
    """Aggregate trades of one symbol: fills of one taker order at one price, pushed every 100 ms.

    Args:
      symbol: Symbol, lowercase, e.g. `btcusdt`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/websocket-market-streams/#aggregate-trade-streams)
    """
    return self.subscribe(
      f'{symbol}@aggTrade',
      validate=validate,
      response_type=AggTradeEventPayload,
    )
