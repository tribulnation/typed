# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any
from typed_aster.core.endpoint.stream import StreamEndpoint
from typed_aster.futures.streams.schemas import FuturesMarkPriceUpdate


Payload = list[FuturesMarkPriceUpdate]


class AllMarkPrice1s(StreamEndpoint):
  """Mark price and funding rate of every symbol, pushed every second."""

  def all_mark_price_1s(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[Payload, Any, Any]:
    """Mark price and funding rate of every symbol, pushed every second.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/websocket-market-streams/#mark-price-stream-for-all-market)
    """
    return self.subscribe(
      '!markPrice@arr@1s',
      validate=validate,
      response_type=Payload,
    )
