# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any
from typed_aster.core.endpoint.stream import StreamEndpoint
from typed_aster.schemas import TradeProEvent


class TradePro(StreamEndpoint):
  """Every trade for one symbol with its on-chain transaction hash and participant addresses."""

  def trade_pro(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[TradeProEvent, Any, Any]:
    """Every trade for one symbol with its on-chain transaction hash and participant addresses.

    Args:
      symbol: Symbol, lowercase, e.g. `btcusdt`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/user-data-streams/#event-tradepro)
    """
    return self.subscribe(
      f'{symbol}@tradepro',
      validate=validate,
      response_type=TradeProEvent,
    )
