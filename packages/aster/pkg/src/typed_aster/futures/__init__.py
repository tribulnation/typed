# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .account import Account
from .agents import Agents
from .announcement import Announcement
from .builder_users import BuilderUsers
from .builders import Builders
from .listen_key import ListenKey
from .market import Market
from .mmp import Mmp
from .position import Position
from .strategy import Strategy
from .streams import Streams
from .sub_accounts import SubAccounts
from .trade import Trade
from .user_stream import UserStream
from .wallet import Wallet
from typed_aster.core.base import SurfaceBase


class Futures(SurfaceBase):
  """Perpetual futures (fapi V3): market data, trading, account, positions, wallet and management, plus market and user-data streams.

  References:
    - [Upstream docs](https://asterdex.github.io/aster-api-website/futures-v3/general-info/)
  """

  @cached_property
  def account(self) -> Account:
    """Futures account: balances, account information, account-wide modes (position, STP, multi-assets), leverage, margin type, brackets, commission and income history.

    References:
      - [Upstream docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/)
    """
    return Account(client=self.client)

  @cached_property
  def agents(self) -> Agents:
    """Agent (API wallet) management: register, approve, update, revoke and list the agents allowed to sign for the account (Aster Code).

    References:
      - [Upstream docs](https://asterdex.github.io/aster-api-website/asterCode/endpoints/)
    """
    return Agents(client=self.client)

  @cached_property
  def announcement(self) -> Announcement:
    """Direct (personal) announcements addressed to the authenticated account.

    References:
      - [Upstream docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#get-direct-announcements-user_data)
    """
    return Announcement(client=self.client)

  @cached_property
  def builder_users(self) -> BuilderUsers:
    """Builder reads: accounts, orders, balances, positions, commission rates and trades of the users who approved the caller as their builder.

    References:
      - [Upstream docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#get-builder-trades-user_data)
    """
    return BuilderUsers(client=self.client)

  @cached_property
  def builders(self) -> Builders:
    """Builder management: approve, update, revoke and list the builders allowed to attach fees to the user's futures orders, with their fee-rate caps (Aster Code).

    References:
      - [Upstream docs](https://asterdex.github.io/aster-api-website/asterCode/endpoints/)
    """
    return Builders(client=self.client)

  @cached_property
  def listen_key(self) -> ListenKey:
    """Create, keep alive and close the listenKey that opens the futures user data stream.

    References:
      - [Upstream docs](https://asterdex.github.io/aster-api-website/futures-v3/user-data-streams/)
    """
    return ListenKey(client=self.client)

  @cached_property
  def market(self) -> Market:
    """Public futures market data: exchange info, order book, trades, klines, mark/index prices, funding and tickers.

    References:
      - [Upstream docs](https://asterdex.github.io/aster-api-website/futures-v3/market-data/)
    """
    return Market(client=self.client)

  @cached_property
  def mmp(self) -> Mmp:
    """Market maker protection (MMP): per-symbol limits on filled quantity, notional and net delta within a time window, which freeze MMP orders once reached.

    References:
      - [Upstream docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#update-user-mmp-user_data)
    """
    return Mmp(client=self.client)

  @cached_property
  def position(self) -> Position:
    """Futures positions: position risk, ADL quantiles, isolated position margin and its change history.

    References:
      - [Upstream docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#position-information-v3-user_data)
    """
    return Position(client=self.client)

  @cached_property
  def strategy(self) -> Strategy:
    """Strategy orders: OTO (one triggers the other), OCO (one cancels the other) and OTOCO, placed, updated and queried as one unit.

    References:
      - [Upstream docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#place-strategy-order-trade)
    """
    return Strategy(client=self.client)

  @cached_property
  def streams(self) -> Streams:
    """Public futures market data streams over WebSocket, subscribed on one combined-stream connection.

    References:
      - [Upstream docs](https://asterdex.github.io/aster-api-website/futures-v3/websocket-market-streams/)
    """
    return Streams(client=self.streams_client)

  @cached_property
  def sub_accounts(self) -> SubAccounts:
    """Sub-accounts: bind or create sub-account wallets under a master account, list and update them, and transfer between them.

    References:
      - [Upstream docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#bind-sub-account-user_data)
    """
    return SubAccounts(client=self.client)

  @cached_property
  def trade(self) -> Trade:
    """Futures order entry and management: place, modify, cancel and query orders (single, batch, guarded), chase orders, auto-cancel, and the account's trade and forced-order history.

    References:
      - [Upstream docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/)
    """
    return Trade(client=self.client)

  @cached_property
  def user_stream(self) -> UserStream:
    """The futures user data stream: account, balance and order events pushed on a listenKey connection.

    References:
      - [Upstream docs](https://asterdex.github.io/aster-api-website/futures-v3/user-data-streams/)
    """
    return UserStream(client=self.user_stream_client)

  @cached_property
  def wallet(self) -> Wallet:
    """Futures wallet: futures/spot transfers, withdrawal limits and history, EVM and Solana withdrawals, and user asset migration.

    References:
      - [Upstream docs](https://asterdex.github.io/aster-api-website/futures-v3/deposit&withdrawal/)
    """
    return Wallet(client=self.client)
