# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class ListenKeyKeptAlive(TypedDict):
  """Empty object on success."""


class Request(TypedDict): ...


class Keepalive(RpcEndpoint):
  """Extend the active listenKey's validity by 60 minutes. Send about every 60 minutes."""

  async def keepalive(self, *, validate: bool | None = None) -> ListenKeyKeptAlive:
    """Extend the active listenKey's validity by 60 minutes. Send about every 60 minutes.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/user-data-streams/#keepalive-user-data-stream-user_stream)
    """
    return await self.request(
      Request(),
      method='PUT',
      path='listenKey',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=ListenKeyKeptAlive,
    )
