# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class ListenKeyClosed(TypedDict):
  """Empty object on success."""


class Request(TypedDict): ...


class Close(RpcEndpoint):
  """Close the user data stream and invalidate its listenKey."""

  async def close(self, *, validate: bool | None = None) -> ListenKeyClosed:
    """Close the user data stream and invalidate its listenKey.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/user-data-streams/#close-user-data-stream-user_stream)
    """
    return await self.request(
      Request(),
      method='DELETE',
      path='listenKey',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=ListenKeyClosed,
    )
