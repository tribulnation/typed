# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class ListenKey(TypedDict):
  """The user data stream key."""

  listenKey: str
  """Key to open the stream with (`<ws>/ws/<listenKey>`)."""


class Request(TypedDict): ...


class Start(RpcEndpoint):
  """Start a user data stream and get its listenKey, valid for 60 minutes. If the account already has an active listenKey, that key is returned and extended by 60 minutes."""

  async def start(self, *, validate: bool | None = None) -> ListenKey:
    """Start a user data stream and get its listenKey, valid for 60 minutes. If the account already has an active listenKey, that key is returned and extended by 60 minutes.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/user-data-streams/#start-user-data-stream-user_stream)
    """
    return await self.request(
      Request(),
      method='POST',
      path='listenKey',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=ListenKey,
    )
