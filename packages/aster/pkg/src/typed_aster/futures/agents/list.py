# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core import TimestampMillis
from typed_aster.core.endpoint.rpc import RpcEndpoint


class Agent(TypedDict):
  """An authorized agent (API wallet)."""

  agentAddress: str
  """Agent address."""
  agentName: str
  """Agent name."""
  ipWhitelist: str
  """IP whitelist, empty when unrestricted."""
  expired: TimestampMillis
  """Expiry time."""
  source: str
  """How the agent was created, e.g. `API` or `builder`."""
  canRead: bool
  """Whether the agent may read account data."""
  canSpotTrade: bool
  """Whether the agent may place spot orders."""
  canPerpTrade: bool
  """Whether the agent may place perpetual futures orders."""
  canWithdraw: bool
  """Whether the agent may initiate withdrawals."""


class Request(TypedDict):
  """No business parameters."""


Response = list[Agent]


class List(RpcEndpoint):
  """List the agents the user has authorized."""

  async def list(self, *, validate: bool | None = None) -> Response:
    """List the agents the user has authorized.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/asterCode/endpoints/#get-agents-user_data)
    """
    return await self.request(
      Request(),
      method='GET',
      path='agent',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
