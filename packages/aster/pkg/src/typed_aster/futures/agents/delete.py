# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import OperationResult


class Request(TypedDict):
  """Agent to revoke."""

  agentAddress: str
  """Agent (signer) address."""


class Delete(RpcEndpoint):
  """Revoke an agent: it can no longer sign for the account. Signed by the main wallet (EIP-712 `DelAgent`)."""

  async def delete(
    self,
    agent_address: str,
    *,
    validate: bool | None = None,
  ) -> OperationResult:
    """Revoke an agent: it can no longer sign for the account. Signed by the main wallet (EIP-712 `DelAgent`).

    Args:
      agent_address: Agent (signer) address.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/asterCode/endpoints/#delete-agent-trade)
    """
    return await self.request(
      Request(agentAddress=agent_address),
      method='DELETE',
      path='agent',
      meta={'sign': 'typed', 'action': 'DelAgent'},
      validate=validate,
      request_type=Request,
      response_type=OperationResult,
    )
