# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.futures.announcement.schemas import FuturesAnnouncement


class Request(TypedDict):
  id: int
  """Announcement id."""


class DirectById(RpcEndpoint):
  """Get one direct announcement by id."""

  async def direct_by_id(
    self,
    id: int,
    *,
    validate: bool | None = None,
  ) -> FuturesAnnouncement:
    """Get one direct announcement by id.

    Args:
      id: Announcement id.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#get-direct-announcement-by-id-user_data)
    """
    return await self.request(
      Request(id=id),
      method='GET',
      path='announcement/directById',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=FuturesAnnouncement,
    )
