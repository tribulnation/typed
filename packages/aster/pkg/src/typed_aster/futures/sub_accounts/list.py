# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """No business parameters."""


class SubAccountBalance(TypedDict):
  """One asset balance."""

  asset: str
  """Asset."""
  balance: Decimal
  """Balance."""


class SubAccount(TypedDict):
  """The master account or one sub-account."""

  address: NotRequired[str]
  """Wallet address."""
  accountId: int
  """Account id."""
  subAccountName: NotRequired[str]
  """Sub-account name (empty for the master account)."""
  parentAccount: bool
  """Whether this entry is the master account."""
  futureBalanceList: NotRequired[list[SubAccountBalance]]
  """Futures wallet balances."""


Response = list[SubAccount]


class List(RpcEndpoint):
  """List the master account and its sub-accounts."""

  async def list(self, *, validate: bool | None = None) -> Response:
    """List the master account and its sub-accounts.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#get-sub-account-list-user_data)
    """
    return await self.request(
      Request(),
      method='GET',
      path='getSubAccountList',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
