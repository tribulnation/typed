# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import OperationResult


class RenameSubAccount(TypedDict):
  """Rename (and optionally set the status of) a sub-account."""

  subSourceAddr: str
  """Sub-account wallet address to update."""
  subAccountName: str
  """New sub-account name."""
  status: NotRequired[Literal['NORMAL', 'FROZEN']]
  """Sub-account status: `NORMAL` (active) or `FROZEN`."""


class SetSubAccountStatus(TypedDict):
  """Freeze or unfreeze a sub-account."""

  subSourceAddr: str
  """Sub-account wallet address to update."""
  status: Literal['NORMAL', 'FROZEN']
  """Sub-account status: `NORMAL` (active) or `FROZEN`."""


Request = RenameSubAccount | SetSubAccountStatus


class Update(RpcEndpoint):
  """Rename a sub-account and/or freeze (`FROZEN`) or unfreeze (`NORMAL`) it. A frozen sub-account cannot transfer. Signed by the master wallet."""

  async def update(
    self,
    update_sub_account_request: Request,
    *,
    validate: bool | None = None,
  ) -> OperationResult:
    """Rename a sub-account and/or freeze (`FROZEN`) or unfreeze (`NORMAL`) it. A frozen sub-account cannot transfer. Signed by the master wallet.

    Args:
      update_sub_account_request: Sub-account changes; at least one of `subAccountName` or `status`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#update-sub-account-trade)
    """
    return await self.request(
      update_sub_account_request,
      method='POST',
      path='updateSubAccount',
      meta={
        'sign': 'main',
        'order': [
          'subSourceAddr',
          'nonce',
          'user',
          'signer',
          'subAccountName',
          'status',
        ],
      },
      validate=validate,
      request_type=Request,
      response_type=OperationResult,
    )
