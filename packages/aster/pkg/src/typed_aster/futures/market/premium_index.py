# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.futures.market.schemas import FuturesMarkPrice


class Request(TypedDict):
  symbol: NotRequired[str]
  """Symbol, e.g. `BTCUSDT`. Omit for every symbol."""


MarkPrices = FuturesMarkPrice | list[FuturesMarkPrice]


class PremiumIndex(RpcEndpoint):
  """Mark price and funding rate, for one symbol or every symbol."""

  async def premium_index(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> MarkPrices:
    """Mark price and funding rate, for one symbol or every symbol.

    Args:
      symbol: Symbol, e.g. `BTCUSDT`. Omit for every symbol.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/market-data/#mark-price)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='premiumIndex',
      meta={'sign': 'none'},
      validate=validate,
      request_type=Request,
      response_type=MarkPrices,
    )
