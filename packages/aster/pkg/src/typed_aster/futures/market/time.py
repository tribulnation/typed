# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core import TimestampMillis
from typed_aster.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict): ...


class ServerTime(TypedDict):
  """Current server time."""

  serverTime: TimestampMillis
  """Server time."""


class Time(RpcEndpoint):
  """Test connectivity to the REST API and get the current server time."""

  async def time(self, *, validate: bool | None = None) -> ServerTime:
    """Test connectivity to the REST API and get the current server time.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/market-data/#check-server-time)
    """
    return await self.request(
      Request(),
      method='GET',
      path='time',
      meta={'sign': 'none'},
      validate=validate,
      request_type=Request,
      response_type=ServerTime,
    )
