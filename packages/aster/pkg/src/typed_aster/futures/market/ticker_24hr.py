# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.futures.market.schemas import FuturesTicker24hr


class Request(TypedDict):
  symbol: NotRequired[str]
  """Symbol, e.g. `BTCUSDT`. Omit for every symbol (weight 40)."""


Tickers24hr = FuturesTicker24hr | list[FuturesTicker24hr]


class Ticker24hr(RpcEndpoint):
  """24-hour rolling window price change statistics, for one symbol or every symbol."""

  async def ticker_24hr(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Tickers24hr:
    """24-hour rolling window price change statistics, for one symbol or every symbol.

    Args:
      symbol: Symbol, e.g. `BTCUSDT`. Omit for every symbol (weight 40).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/market-data/#24hr-ticker-price-change-statistics)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='ticker/24hr',
      meta={'sign': 'none'},
      validate=validate,
      request_type=Request,
      response_type=Tickers24hr,
    )
