# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class RemainingOpenableNotional(TypedDict):
  """Remaining openable notional."""

  remainingOpenableNotionalValue: Decimal
  """Remaining notional (USDT) of the leverage tier; `"-1"` means no cap."""


class Request(TypedDict):
  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  leverage: int
  """Leverage; a positive integer within the highest leverage tier."""


class RemainingOpenableNotionalValue(RpcEndpoint):
  """Remaining notional that can still be opened on a symbol at a leverage: a symbol-level open interest cap, not an account value."""

  async def remaining_openable_notional_value(
    self,
    symbol: str,
    *,
    leverage: int,
    validate: bool | None = None,
  ) -> RemainingOpenableNotional:
    """Remaining notional that can still be opened on a symbol at a leverage: a symbol-level open interest cap, not an account value.

    Args:
      symbol: Symbol, e.g. `BTCUSDT`.
      leverage: Leverage; a positive integer within the highest leverage tier.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-futures-api-v3.md#remaining-openable-notional-value)
    """
    return await self.request(
      Request(symbol=symbol, leverage=leverage),
      method='GET',
      path='remainingOpenableNotionalValue',
      meta={'sign': 'none'},
      validate=validate,
      request_type=Request,
      response_type=RemainingOpenableNotional,
    )
