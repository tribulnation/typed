# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_aster.core import TimestampMillis
from typed_aster.core.endpoint.rpc import RpcEndpoint


class IndexReference(TypedDict):
  """One constituent."""

  exchange: str
  """Source exchange, e.g. `binance`."""
  symbol: str
  """Symbol on the source exchange, e.g. `BTC-USDT`."""
  weight: Decimal
  """Weight in the index."""


class Request(TypedDict):
  symbol: str
  """Symbol, e.g. `BTCUSDT`."""


class IndexReferences(TypedDict):
  """Index constituents of one symbol."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  time: TimestampMillis
  """Data time."""
  references: list[IndexReference]
  """Constituents."""


class IndexReferencesEndpoint(RpcEndpoint):
  """Component exchanges of a symbol's index price and their weights."""

  async def index_references(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> IndexReferences:
    """Component exchanges of a symbol's index price and their weights.

    Args:
      symbol: Symbol, e.g. `BTCUSDT`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/market-data/#index-price-references)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='indexreferences',
      meta={'sign': 'none'},
      validate=validate,
      request_type=Request,
      response_type=IndexReferences,
    )
