# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class AssetLogo(TypedDict):
  """Logo of one asset."""

  assetCode: str
  """Asset."""
  logoUrl: str
  """URL of the logo image."""


class Request(TypedDict): ...


Response = list[AssetLogo]


class AssetLogos(RpcEndpoint):
  """Logo URL of every active asset."""

  async def asset_logos(self, *, validate: bool | None = None) -> Response:
    """Logo URL of every active asset.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-futures-api-v3.md#get-all-asset-logos)
    """
    return await self.request(
      Request(),
      method='GET',
      path='common/asset/all-asset-logo',
      meta={'sign': 'none'},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
