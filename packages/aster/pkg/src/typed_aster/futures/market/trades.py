# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.futures.market.schemas import FuturesPublicTrade


class Request(TypedDict):
  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  limit: NotRequired[int]
  """Number of rows. Default 500; max 1000."""


Response = list[FuturesPublicTrade]


class Trades(RpcEndpoint):
  """Recent market trades (order book fills only; no insurance fund or ADL trades)."""

  async def trades(
    self,
    symbol: str,
    *,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Recent market trades (order book fills only; no insurance fund or ADL trades).

    Args:
      symbol: Symbol, e.g. `BTCUSDT`.
      limit: Number of rows. Default 500; max 1000.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/market-data/#recent-trades-list)
    """
    request: Request = Request(symbol=symbol)
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='trades',
      meta={'sign': 'none'},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
