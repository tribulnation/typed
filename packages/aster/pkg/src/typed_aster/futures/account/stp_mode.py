# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict): ...


class StpMode(TypedDict):
  """Self-Trade Prevention mode."""

  stpMode: Literal['EXPIRE_TAKER', 'EXPIRE_MAKER', 'EXPIRE_BOTH']
  """Current STP mode."""


class StpModeEndpoint(RpcEndpoint):
  """Get the account's Self-Trade Prevention (STP) mode on every symbol."""

  async def stp_mode(self, *, validate: bool | None = None) -> StpMode:
    """Get the account's Self-Trade Prevention (STP) mode on every symbol.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#get-current-stp-mode-user_data)
    """
    return await self.request(
      Request(),
      method='GET',
      path='stpMode',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=StpMode,
    )
