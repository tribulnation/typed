# Generated by typed-dev — see CONTRIBUTING.md.
"""Shapes shared by several endpoints of `futures.account`."""

from typing_extensions import Literal, TypedDict
from typed_aster.core import TimestampMillis
from decimal import Decimal


class FuturesAccountAsset(TypedDict):
  """Margin state of one asset."""

  asset: str
  """Asset name."""
  walletBalance: Decimal
  """Wallet balance."""
  unrealizedProfit: Decimal
  """Unrealized profit."""
  marginBalance: Decimal
  """Margin balance."""
  maintMargin: Decimal
  """Maintenance margin required."""
  initialMargin: Decimal
  """Total initial margin required with current mark price."""
  positionInitialMargin: Decimal
  """Initial margin required for positions with current mark price."""
  openOrderInitialMargin: Decimal
  """Initial margin required for open orders with current mark price."""
  maxWithdrawAmount: Decimal
  """Maximum amount for transfer out."""
  crossWalletBalance: Decimal
  """Crossed wallet balance."""
  crossUnPnl: Decimal
  """Unrealized profit of crossed positions."""
  availableBalance: Decimal
  """Available balance."""
  marginAvailable: bool
  """Whether the asset can be used as margin in Multi-Assets mode."""
  updateTime: TimestampMillis
  """Last update time; `0` when never updated."""


class FuturesAccountPosition(TypedDict):
  """Position on one symbol and side (every symbol is listed, including flat ones)."""

  symbol: str
  """Symbol name."""
  initialMargin: Decimal
  """Initial margin required with current mark price."""
  maintMargin: Decimal
  """Maintenance margin required."""
  unrealizedProfit: Decimal
  """Unrealized profit."""
  positionInitialMargin: Decimal
  """Initial margin required for the position with current mark price."""
  openOrderInitialMargin: Decimal
  """Initial margin required for open orders with current mark price."""
  leverage: int
  """Current initial leverage."""
  isolated: bool
  """Whether the position is isolated."""
  entryPrice: Decimal
  """Average entry price."""
  maxNotional: Decimal
  """Maximum available notional with current leverage."""
  positionSide: Literal['BOTH', 'LONG', 'SHORT']
  """Position side: `BOTH` in One-way mode, `LONG`/`SHORT` in Hedge mode."""
  positionAmt: Decimal
  """Position amount."""
  notional: Decimal
  """Position notional value."""
  isolatedWallet: Decimal
  """Isolated wallet balance."""
  updateTime: TimestampMillis
  """Last update time; `0` when never updated."""


class FuturesAccount(TypedDict):
  """Futures account information. The `total*`, `availableBalance` and `maxWithdrawAmount` totals are denominated in USDT."""

  feeTier: int
  """Account commission tier."""
  canTrade: bool
  """Whether the account can trade."""
  canDeposit: bool
  """Whether the account can transfer in assets."""
  canWithdraw: bool
  """Whether the account can transfer out assets."""
  updateTime: TimestampMillis
  """Last update time; `0` when never updated."""
  totalInitialMargin: Decimal
  """Total initial margin required with current mark price (useless with isolated positions)."""
  totalMaintMargin: Decimal
  """Total maintenance margin required."""
  totalWalletBalance: Decimal
  """Total wallet balance."""
  totalUnrealizedProfit: Decimal
  """Total unrealized profit."""
  totalMarginBalance: Decimal
  """Total margin balance."""
  totalPositionInitialMargin: Decimal
  """Initial margin required for positions with current mark price."""
  totalOpenOrderInitialMargin: Decimal
  """Initial margin required for open orders with current mark price."""
  totalCrossWalletBalance: Decimal
  """Crossed wallet balance."""
  totalCrossUnPnl: Decimal
  """Unrealized profit of crossed positions."""
  availableBalance: Decimal
  """Available balance."""
  maxWithdrawAmount: Decimal
  """Maximum amount for transfer out."""
  assets: list[FuturesAccountAsset]
  """Margin state of every asset."""
  positions: list[FuturesAccountPosition]
  """Positions of every symbol: only `BOTH` in One-way mode, only `LONG`/`SHORT` in Hedge mode."""
