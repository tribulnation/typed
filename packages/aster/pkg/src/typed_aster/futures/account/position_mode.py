# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class PositionMode(TypedDict):
  """Position mode."""

  dualSidePosition: bool
  """`true`: Hedge mode; `false`: One-way mode."""


class Request(TypedDict): ...


class PositionModeEndpoint(RpcEndpoint):
  """Get the account's position mode (Hedge or One-way) on every symbol."""

  async def position_mode(self, *, validate: bool | None = None) -> PositionMode:
    """Get the account's position mode (Hedge or One-way) on every symbol.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#get-current-position-modeuser_data)
    """
    return await self.request(
      Request(),
      method='GET',
      path='positionSide/dual',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=PositionMode,
    )
