# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import OperationResult


class Request(TypedDict):
  dualSidePosition: bool
  """`true`: Hedge mode; `false`: One-way mode. Sent as `true`/`false`."""


class SetPositionMode(RpcEndpoint):
  """Change the account's position mode (Hedge or One-way) on every symbol."""

  async def set_position_mode(
    self,
    dual_side_position: bool,
    *,
    validate: bool | None = None,
  ) -> OperationResult:
    """Change the account's position mode (Hedge or One-way) on every symbol.

    Args:
      dual_side_position: `true`: Hedge mode; `false`: One-way mode. Sent as `true`/`false`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#change-position-modetrade)
    """
    return await self.request(
      Request(dualSidePosition=dual_side_position),
      method='POST',
      path='positionSide/dual',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=OperationResult,
    )
