# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import OperationResult


class Request(TypedDict):
  stpMode: Literal['EXPIRE_TAKER', 'EXPIRE_MAKER', 'EXPIRE_BOTH']
  """`EXPIRE_TAKER` expires the taker order, `EXPIRE_MAKER` the maker order, `EXPIRE_BOTH` both."""


class SetStpMode(RpcEndpoint):
  """Change the account's Self-Trade Prevention (STP) mode on every symbol."""

  async def set_stp_mode(
    self,
    stp_mode: Literal['EXPIRE_TAKER', 'EXPIRE_MAKER', 'EXPIRE_BOTH'],
    *,
    validate: bool | None = None,
  ) -> OperationResult:
    """Change the account's Self-Trade Prevention (STP) mode on every symbol.

    Args:
      stp_mode: `EXPIRE_TAKER` expires the taker order, `EXPIRE_MAKER` the maker order, `EXPIRE_BOTH` both.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#change-stp-mode-trade)
    """
    return await self.request(
      Request(stpMode=stp_mode),
      method='POST',
      path='stpMode',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=OperationResult,
    )
