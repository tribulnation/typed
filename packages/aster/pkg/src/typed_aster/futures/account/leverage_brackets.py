# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class LeverageBracket(TypedDict):
  """One notional bracket."""

  bracket: int
  """Notional bracket number."""
  initialLeverage: int
  """Maximum initial leverage for this bracket."""
  notionalCap: Decimal
  """Cap notional of this bracket."""
  notionalFloor: Decimal
  """Notional threshold of this bracket."""
  maintMarginRatio: Decimal
  """Maintenance margin ratio for this bracket."""
  cum: Decimal
  """Auxiliary number for quick calculation."""


class Request(TypedDict):
  symbol: NotRequired[str]
  """Symbol, e.g. `BTCUSDT`; every symbol when omitted."""


class SymbolLeverageBrackets(TypedDict):
  """Brackets of one symbol."""

  symbol: str
  """Symbol name."""
  brackets: list[LeverageBracket]
  """Notional brackets, lowest first."""


Response = list[SymbolLeverageBrackets]


class LeverageBrackets(RpcEndpoint):
  """Get the notional and leverage brackets of every symbol, or of one symbol."""

  async def leverage_brackets(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Get the notional and leverage brackets of every symbol, or of one symbol.

    Args:
      symbol: Symbol, e.g. `BTCUSDT`; every symbol when omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#notional-and-leverage-brackets-user_data)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='leverageBrackets',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
