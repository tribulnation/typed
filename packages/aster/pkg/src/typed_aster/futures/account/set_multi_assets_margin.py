# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import OperationResult


class Request(TypedDict):
  multiAssetsMargin: bool
  """`true`: Multi-Assets mode; `false`: Single-Asset mode. Sent as `true`/`false`."""


class SetMultiAssetsMargin(RpcEndpoint):
  """Change the account's Multi-Assets mode (Multi-Assets or Single-Asset) on every symbol."""

  async def set_multi_assets_margin(
    self,
    multi_assets_margin: bool,
    *,
    validate: bool | None = None,
  ) -> OperationResult:
    """Change the account's Multi-Assets mode (Multi-Assets or Single-Asset) on every symbol.

    Args:
      multi_assets_margin: `true`: Multi-Assets mode; `false`: Single-Asset mode. Sent as `true`/`false`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#change-multi-assets-mode-trade)
    """
    return await self.request(
      Request(multiAssetsMargin=multi_assets_margin),
      method='POST',
      path='multiAssetsMargin',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=OperationResult,
    )
