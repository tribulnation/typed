# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class MultiAssetsMode(TypedDict):
  """Multi-Assets mode."""

  multiAssetsMargin: bool
  """`true`: Multi-Assets mode; `false`: Single-Asset mode."""


class Request(TypedDict): ...


class MultiAssetsMargin(RpcEndpoint):
  """Get the account's Multi-Assets mode (Multi-Assets or Single-Asset) on every symbol."""

  async def multi_assets_margin(
    self, *, validate: bool | None = None
  ) -> MultiAssetsMode:
    """Get the account's Multi-Assets mode (Multi-Assets or Single-Asset) on every symbol.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#get-current-multi-assets-mode-user_data)
    """
    return await self.request(
      Request(),
      method='GET',
      path='multiAssetsMargin',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=MultiAssetsMode,
    )
