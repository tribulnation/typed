# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.futures.account.schemas import FuturesAccount


class Request(TypedDict): ...


class InfoWithJoinMargin(RpcEndpoint):
  """Get current account information, join-margin view: in Multi-Assets mode the totals include every margin-eligible asset at its USDT value."""

  async def info_with_join_margin(
    self, *, validate: bool | None = None
  ) -> FuturesAccount:
    """Get current account information, join-margin view: in Multi-Assets mode the totals include every margin-eligible asset at its USDT value.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#account-information-v3-user_data)
    """
    return await self.request(
      Request(),
      method='GET',
      path='accountWithJoinMargin',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=FuturesAccount,
    )
