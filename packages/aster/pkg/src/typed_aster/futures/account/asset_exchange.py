# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_aster.core import TimestampMillis
from typed_aster.core.endpoint.rpc import RpcEndpoint


class AssetExchange(TypedDict):
  """Balance of the asset updated by the exchange."""

  accountId: int
  """Account id."""
  asset: str
  """Asset whose balance was updated by the exchange."""
  balance: Decimal
  """Wallet balance of the asset after the exchange."""
  updateTime: TimestampMillis
  """Update time."""


class Request(TypedDict): ...


Response = AssetExchange | None


class AssetExchangeEndpoint(RpcEndpoint):
  """Manually trigger an asset exchange in Multi-Assets mode (an auto asset exchange with a threshold of 0). Only available in Multi-Assets mode (`-4212` otherwise)."""

  async def asset_exchange(self, *, validate: bool | None = None) -> Response:
    """Manually trigger an asset exchange in Multi-Assets mode (an auto asset exchange with a threshold of 0). Only available in Multi-Assets mode (`-4212` otherwise).

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/api-docs/blob/master/V3(Recommended)/EN/aster-finance-futures-api-v3.md#asset-exchange-trade)
    """
    return await self.request(
      Request(),
      method='POST',
      path='assetExchange',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
