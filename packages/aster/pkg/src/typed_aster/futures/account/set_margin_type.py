# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import OperationResult


class Request(TypedDict):
  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  marginType: Literal['ISOLATED', 'CROSSED']
  """Target margin type."""


class SetMarginType(RpcEndpoint):
  """Change the margin type (isolated or crossed) of one symbol."""

  async def set_margin_type(
    self,
    symbol: str,
    *,
    margin_type: Literal['ISOLATED', 'CROSSED'],
    validate: bool | None = None,
  ) -> OperationResult:
    """Change the margin type (isolated or crossed) of one symbol.

    Args:
      symbol: Symbol, e.g. `BTCUSDT`.
      margin_type: Target margin type.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#change-margin-type-trade)
    """
    return await self.request(
      Request(symbol=symbol, marginType=margin_type),
      method='POST',
      path='marginType',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=OperationResult,
    )
