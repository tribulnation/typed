# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_aster.core import TimestampMillis
from typed_aster.core.endpoint.rpc import RpcEndpoint


class FuturesBalance(TypedDict):
  """Balance of one asset in the futures wallet."""

  accountAlias: str
  """Unique account code."""
  asset: str
  """Asset name."""
  balance: Decimal
  """Wallet balance."""
  crossWalletBalance: Decimal
  """Crossed wallet balance."""
  crossUnPnl: Decimal
  """Unrealized profit of crossed positions."""
  availableBalance: Decimal
  """Available balance."""
  maxWithdrawAmount: Decimal
  """Maximum amount for transfer out."""
  marginAvailable: bool
  """Whether the asset can be used as margin in Multi-Assets mode."""
  updateTime: TimestampMillis
  """Last update time; `0` when never updated."""


class Request(TypedDict): ...


Response = list[FuturesBalance]


class Balance(RpcEndpoint):
  """Get the futures wallet balance of every asset."""

  async def balance(self, *, validate: bool | None = None) -> Response:
    """Get the futures wallet balance of every asset.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#futures-account-balance-v3-user_data)
    """
    return await self.request(
      Request(),
      method='GET',
      path='balance',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
