# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class LeverageChange(TypedDict):
  """The symbol's new leverage."""

  leverage: int
  """New initial leverage."""
  maxNotionalValue: Decimal
  """Maximum notional value at this leverage."""
  symbol: str
  """Symbol name."""


class Request(TypedDict):
  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  leverage: int
  """Target initial leverage, 1 to 125."""


class SetLeverage(RpcEndpoint):
  """Change the account's initial leverage on one symbol."""

  async def set_leverage(
    self,
    symbol: str,
    *,
    leverage: int,
    validate: bool | None = None,
  ) -> LeverageChange:
    """Change the account's initial leverage on one symbol.

    Args:
      symbol: Symbol, e.g. `BTCUSDT`.
      leverage: Target initial leverage, 1 to 125.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#change-initial-leverage-trade)
    """
    return await self.request(
      Request(symbol=symbol, leverage=leverage),
      method='POST',
      path='leverage',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=LeverageChange,
    )
