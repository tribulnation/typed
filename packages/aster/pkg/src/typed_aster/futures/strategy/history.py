# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_aster.core import TimestampMillis
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.futures.strategy.schemas import FuturesStrategyOrder


class StrategyHistoryOrderByClientId(TypedDict):
  """Historical strategy order to query. Identified by `clientStrategyId`."""

  clientStrategyId: str
  """Client strategy id."""
  strategyType: Literal['OTO', 'OCO', 'OTOCO']
  """Strategy type."""
  startTime: NotRequired[TimestampMillis]
  """Start time; data older than 90 days cannot be queried."""
  endTime: NotRequired[TimestampMillis]
  """End time."""
  limit: NotRequired[int]
  """Number of results. Default 500, max 1000."""


class StrategyHistoryOrderById(TypedDict):
  """Historical strategy order to query. Identified by `strategyId`."""

  strategyId: int
  """Strategy order id."""
  strategyType: Literal['OTO', 'OCO', 'OTOCO']
  """Strategy type."""
  startTime: NotRequired[TimestampMillis]
  """Start time; data older than 90 days cannot be queried."""
  endTime: NotRequired[TimestampMillis]
  """End time."""
  limit: NotRequired[int]
  """Number of results. Default 500, max 1000."""


Request = StrategyHistoryOrderById | StrategyHistoryOrderByClientId


class History(RpcEndpoint):
  """Query a historical strategy order and its sub-orders. The lookback is at most 90 days."""

  async def history(
    self,
    strategy_history_order_request: Request,
    *,
    validate: bool | None = None,
  ) -> FuturesStrategyOrder:
    """Query a historical strategy order and its sub-orders. The lookback is at most 90 days.

    Args:
      strategy_history_order_request: Historical strategy order to query. Either `strategyId` or `clientStrategyId` is required, not both.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#query-strategy-history-order-user_data)
    """
    return await self.request(
      strategy_history_order_request,
      method='GET',
      path='strategyHistoryOrder',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=FuturesStrategyOrder,
    )
