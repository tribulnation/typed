# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_aster.core import TimestampMillis
from typed_aster.core.endpoint.rpc import RpcEndpoint


class StrategyLimitSubOrder(TypedDict):
  """A `LIMIT` sub-order: `quantity`, `price` and `timeInForce` are required."""

  strategySubId: str
  """Sub-order sequence number, starting from 1; must match its position in the list."""
  securityType: Literal['USDT_FUTURES', 'COIN_FUTURES', 'OPTIONS']
  """Security type of the sub-order."""
  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. Defaults to `BOTH` in One-way Mode; `LONG` or `SHORT` in Hedge Mode, where it must be sent."""
  type: Literal['LIMIT']
  """Order type."""
  quantity: Decimal
  """Order quantity, in the base asset."""
  price: Decimal
  """Limit price."""
  timeInForce: Literal['GTC', 'GTX', 'HIDDEN']
  """Time in force; `IOC` and `FOK` are not supported."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """Price that triggers `stopPrice` (default `CONTRACT_PRICE`)."""
  reduceOnly: NotRequired[Literal['true', 'false']]
  """Reduce-only flag, `"true"` or `"false"`."""
  closePosition: NotRequired[Literal['true', 'false']]
  """Close-position flag, `"true"` or `"false"`."""
  priceProtect: NotRequired[Literal['TRUE', 'FALSE']]
  """Trigger protection flag, `"TRUE"` or `"FALSE"`."""
  clientOrderId: NotRequired[str]
  """Client order id of the sub-order."""
  firstDrivenId: str
  """`strategySubId` of the sub-order driving the first trigger; `"0"` when none. Required by the venue on every sub-order."""
  firstDrivenOn: NotRequired[
    Literal[
      'NEW',
      'PARTIALLY_FILLED_OR_FILLED',
      'FILLED',
      'CANCELED',
      'REPLACED',
      'STOPPED',
      'REJECTED',
      'EXPIRED',
    ]
  ]
  """Event of the driving sub-order that activates the first trigger."""
  firstTrigger: NotRequired[Literal['PLACE_ORDER', 'CANCEL_ORDER']]
  """First trigger action."""
  secondDrivenId: str
  """`strategySubId` of the sub-order driving the second trigger; `"0"` when none. Required by the venue on every sub-order."""
  secondDrivenOn: NotRequired[
    Literal[
      'NEW',
      'PARTIALLY_FILLED_OR_FILLED',
      'FILLED',
      'CANCELED',
      'REPLACED',
      'STOPPED',
      'REJECTED',
      'EXPIRED',
    ]
  ]
  """Event that activates the second trigger."""
  secondTrigger: NotRequired[Literal['PLACE_ORDER', 'CANCEL_ORDER']]
  """Second trigger action."""


class StrategyMarketSubOrder(TypedDict):
  """A `MARKET` sub-order: `quantity` is required."""

  strategySubId: str
  """Sub-order sequence number, starting from 1; must match its position in the list."""
  securityType: Literal['USDT_FUTURES', 'COIN_FUTURES', 'OPTIONS']
  """Security type of the sub-order."""
  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. Defaults to `BOTH` in One-way Mode; `LONG` or `SHORT` in Hedge Mode, where it must be sent."""
  type: Literal['MARKET']
  """Order type."""
  quantity: Decimal
  """Order quantity, in the base asset."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """Price that triggers `stopPrice` (default `CONTRACT_PRICE`)."""
  reduceOnly: NotRequired[Literal['true', 'false']]
  """Reduce-only flag, `"true"` or `"false"`."""
  closePosition: NotRequired[Literal['true', 'false']]
  """Close-position flag, `"true"` or `"false"`."""
  priceProtect: NotRequired[Literal['TRUE', 'FALSE']]
  """Trigger protection flag, `"TRUE"` or `"FALSE"`."""
  clientOrderId: NotRequired[str]
  """Client order id of the sub-order."""
  firstDrivenId: str
  """`strategySubId` of the sub-order driving the first trigger; `"0"` when none. Required by the venue on every sub-order."""
  firstDrivenOn: NotRequired[
    Literal[
      'NEW',
      'PARTIALLY_FILLED_OR_FILLED',
      'FILLED',
      'CANCELED',
      'REPLACED',
      'STOPPED',
      'REJECTED',
      'EXPIRED',
    ]
  ]
  """Event of the driving sub-order that activates the first trigger."""
  firstTrigger: NotRequired[Literal['PLACE_ORDER', 'CANCEL_ORDER']]
  """First trigger action."""
  secondDrivenId: str
  """`strategySubId` of the sub-order driving the second trigger; `"0"` when none. Required by the venue on every sub-order."""
  secondDrivenOn: NotRequired[
    Literal[
      'NEW',
      'PARTIALLY_FILLED_OR_FILLED',
      'FILLED',
      'CANCELED',
      'REPLACED',
      'STOPPED',
      'REJECTED',
      'EXPIRED',
    ]
  ]
  """Event that activates the second trigger."""
  secondTrigger: NotRequired[Literal['PLACE_ORDER', 'CANCEL_ORDER']]
  """Second trigger action."""


class StrategyOrder(TypedDict):
  """A strategy order, as placed."""

  strategyId: int
  """Strategy order id."""
  clientStrategyId: NotRequired[str]
  """Client strategy id."""
  strategyType: Literal['OTO', 'OCO', 'OTOCO']
  """Strategy type."""
  strategyStatus: str
  """Strategy status, e.g. `WORKING`."""
  updateTime: NotRequired[TimestampMillis]
  """Last update time."""
  failureCode: NotRequired[int]
  """Failure code, `0` on success."""
  failureReason: NotRequired[str]
  """Failure reason, empty on success."""


class StrategyStopLimitSubOrder(TypedDict):
  """A `STOP` or `TAKE_PROFIT` sub-order: `quantity`, `price` and `stopPrice` are required; `timeInForce` defaults to `GTC`."""

  strategySubId: str
  """Sub-order sequence number, starting from 1; must match its position in the list."""
  securityType: Literal['USDT_FUTURES', 'COIN_FUTURES', 'OPTIONS']
  """Security type of the sub-order."""
  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. Defaults to `BOTH` in One-way Mode; `LONG` or `SHORT` in Hedge Mode, where it must be sent."""
  type: Literal['STOP', 'TAKE_PROFIT']
  """Order type."""
  quantity: Decimal
  """Order quantity, in the base asset."""
  price: Decimal
  """Limit price."""
  stopPrice: Decimal
  """Trigger price."""
  timeInForce: NotRequired[Literal['GTC', 'GTX', 'HIDDEN']]
  """Time in force; `IOC` and `FOK` are not supported."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """Price that triggers `stopPrice` (default `CONTRACT_PRICE`)."""
  reduceOnly: NotRequired[Literal['true', 'false']]
  """Reduce-only flag, `"true"` or `"false"`."""
  closePosition: NotRequired[Literal['true', 'false']]
  """Close-position flag, `"true"` or `"false"`."""
  priceProtect: NotRequired[Literal['TRUE', 'FALSE']]
  """Trigger protection flag, `"TRUE"` or `"FALSE"`."""
  clientOrderId: NotRequired[str]
  """Client order id of the sub-order."""
  firstDrivenId: str
  """`strategySubId` of the sub-order driving the first trigger; `"0"` when none. Required by the venue on every sub-order."""
  firstDrivenOn: NotRequired[
    Literal[
      'NEW',
      'PARTIALLY_FILLED_OR_FILLED',
      'FILLED',
      'CANCELED',
      'REPLACED',
      'STOPPED',
      'REJECTED',
      'EXPIRED',
    ]
  ]
  """Event of the driving sub-order that activates the first trigger."""
  firstTrigger: NotRequired[Literal['PLACE_ORDER', 'CANCEL_ORDER']]
  """First trigger action."""
  secondDrivenId: str
  """`strategySubId` of the sub-order driving the second trigger; `"0"` when none. Required by the venue on every sub-order."""
  secondDrivenOn: NotRequired[
    Literal[
      'NEW',
      'PARTIALLY_FILLED_OR_FILLED',
      'FILLED',
      'CANCELED',
      'REPLACED',
      'STOPPED',
      'REJECTED',
      'EXPIRED',
    ]
  ]
  """Event that activates the second trigger."""
  secondTrigger: NotRequired[Literal['PLACE_ORDER', 'CANCEL_ORDER']]
  """Second trigger action."""


class StrategyStopMarketSubOrder(TypedDict):
  """A `STOP_MARKET` or `TAKE_PROFIT_MARKET` sub-order: `stopPrice` is required; `quantity` is required unless `closePosition` is `"true"`."""

  strategySubId: str
  """Sub-order sequence number, starting from 1; must match its position in the list."""
  securityType: Literal['USDT_FUTURES', 'COIN_FUTURES', 'OPTIONS']
  """Security type of the sub-order."""
  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. Defaults to `BOTH` in One-way Mode; `LONG` or `SHORT` in Hedge Mode, where it must be sent."""
  type: Literal['STOP_MARKET', 'TAKE_PROFIT_MARKET']
  """Order type."""
  quantity: NotRequired[Decimal]
  """Order quantity, in the base asset."""
  stopPrice: Decimal
  """Trigger price."""
  timeInForce: NotRequired[Literal['GTC', 'GTX', 'HIDDEN']]
  """Time in force; `IOC` and `FOK` are not supported."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """Price that triggers `stopPrice` (default `CONTRACT_PRICE`)."""
  reduceOnly: NotRequired[Literal['true', 'false']]
  """Reduce-only flag, `"true"` or `"false"`."""
  closePosition: NotRequired[Literal['true', 'false']]
  """Close-position flag, `"true"` or `"false"`."""
  priceProtect: NotRequired[Literal['TRUE', 'FALSE']]
  """Trigger protection flag, `"TRUE"` or `"FALSE"`."""
  clientOrderId: NotRequired[str]
  """Client order id of the sub-order."""
  firstDrivenId: str
  """`strategySubId` of the sub-order driving the first trigger; `"0"` when none. Required by the venue on every sub-order."""
  firstDrivenOn: NotRequired[
    Literal[
      'NEW',
      'PARTIALLY_FILLED_OR_FILLED',
      'FILLED',
      'CANCELED',
      'REPLACED',
      'STOPPED',
      'REJECTED',
      'EXPIRED',
    ]
  ]
  """Event of the driving sub-order that activates the first trigger."""
  firstTrigger: NotRequired[Literal['PLACE_ORDER', 'CANCEL_ORDER']]
  """First trigger action."""
  secondDrivenId: str
  """`strategySubId` of the sub-order driving the second trigger; `"0"` when none. Required by the venue on every sub-order."""
  secondDrivenOn: NotRequired[
    Literal[
      'NEW',
      'PARTIALLY_FILLED_OR_FILLED',
      'FILLED',
      'CANCELED',
      'REPLACED',
      'STOPPED',
      'REJECTED',
      'EXPIRED',
    ]
  ]
  """Event that activates the second trigger."""
  secondTrigger: NotRequired[Literal['PLACE_ORDER', 'CANCEL_ORDER']]
  """Second trigger action."""


class StrategyTrailingStopSubOrder(TypedDict):
  """A `TRAILING_STOP_MARKET` sub-order: `quantity` is required."""

  strategySubId: str
  """Sub-order sequence number, starting from 1; must match its position in the list."""
  securityType: Literal['USDT_FUTURES', 'COIN_FUTURES', 'OPTIONS']
  """Security type of the sub-order."""
  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. Defaults to `BOTH` in One-way Mode; `LONG` or `SHORT` in Hedge Mode, where it must be sent."""
  type: Literal['TRAILING_STOP_MARKET']
  """Order type."""
  quantity: Decimal
  """Order quantity, in the base asset."""
  activationPrice: NotRequired[Decimal]
  """Trailing-stop activation price; defaults to the latest price (per `workingType`)."""
  callbackRate: NotRequired[Decimal]
  """Trailing-stop callback rate in percent, from 0.1 to 5 (1 means 1%)."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """Price that triggers `stopPrice` (default `CONTRACT_PRICE`)."""
  reduceOnly: NotRequired[Literal['true', 'false']]
  """Reduce-only flag, `"true"` or `"false"`."""
  closePosition: NotRequired[Literal['true', 'false']]
  """Close-position flag, `"true"` or `"false"`."""
  priceProtect: NotRequired[Literal['TRUE', 'FALSE']]
  """Trigger protection flag, `"TRUE"` or `"FALSE"`."""
  clientOrderId: NotRequired[str]
  """Client order id of the sub-order."""
  firstDrivenId: str
  """`strategySubId` of the sub-order driving the first trigger; `"0"` when none. Required by the venue on every sub-order."""
  firstDrivenOn: NotRequired[
    Literal[
      'NEW',
      'PARTIALLY_FILLED_OR_FILLED',
      'FILLED',
      'CANCELED',
      'REPLACED',
      'STOPPED',
      'REJECTED',
      'EXPIRED',
    ]
  ]
  """Event of the driving sub-order that activates the first trigger."""
  firstTrigger: NotRequired[Literal['PLACE_ORDER', 'CANCEL_ORDER']]
  """First trigger action."""
  secondDrivenId: str
  """`strategySubId` of the sub-order driving the second trigger; `"0"` when none. Required by the venue on every sub-order."""
  secondDrivenOn: NotRequired[
    Literal[
      'NEW',
      'PARTIALLY_FILLED_OR_FILLED',
      'FILLED',
      'CANCELED',
      'REPLACED',
      'STOPPED',
      'REJECTED',
      'EXPIRED',
    ]
  ]
  """Event that activates the second trigger."""
  secondTrigger: NotRequired[Literal['PLACE_ORDER', 'CANCEL_ORDER']]
  """Second trigger action."""


class Request(TypedDict):
  """Strategy order to place."""

  clientStrategyId: NotRequired[str]
  """Client strategy id, unique."""
  strategyType: Literal['OTO', 'OCO', 'OTOCO']
  """Strategy type."""
  subOrderList: list[
    StrategyLimitSubOrder
    | StrategyMarketSubOrder
    | StrategyStopLimitSubOrder
    | StrategyStopMarketSubOrder
    | StrategyTrailingStopSubOrder
  ]
  """Sub-orders, sent as a JSON list: exactly 2 for `OTO`/`OCO`, exactly 3 for `OTOCO`."""
  builder: NotRequired[str]
  """Builder address."""
  feeRate: NotRequired[Decimal]
  """Custom builder fee rate."""


class Place(RpcEndpoint):
  """Place a strategy order: OTO (one triggers the other), OCO (one cancels the other) or OTOCO (one triggers an OCO pair). The trigger links between sub-orders are set with the `first*`/`second*` fields."""

  async def place(
    self,
    *,
    client_strategy_id: str | None = None,
    strategy_type: Literal['OTO', 'OCO', 'OTOCO'],
    sub_order_list: list[
      StrategyLimitSubOrder
      | StrategyMarketSubOrder
      | StrategyStopLimitSubOrder
      | StrategyStopMarketSubOrder
      | StrategyTrailingStopSubOrder
    ],
    builder: str | None = None,
    fee_rate: Decimal | None = None,
    validate: bool | None = None,
  ) -> StrategyOrder:
    """Place a strategy order: OTO (one triggers the other), OCO (one cancels the other) or OTOCO (one triggers an OCO pair). The trigger links between sub-orders are set with the `first*`/`second*` fields.

    Args:
      client_strategy_id: Client strategy id, unique.
      strategy_type: Strategy type.
      sub_order_list: Sub-orders, sent as a JSON list: exactly 2 for `OTO`/`OCO`, exactly 3 for `OTOCO`.
      builder: Builder address.
      fee_rate: Custom builder fee rate.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#place-strategy-order-trade)
    """
    request: Request = Request(strategyType=strategy_type, subOrderList=sub_order_list)
    if client_strategy_id is not None:
      request['clientStrategyId'] = client_strategy_id
    if builder is not None:
      request['builder'] = builder
    if fee_rate is not None:
      request['feeRate'] = fee_rate
    return await self.request(
      request,
      method='POST',
      path='placeStrategyOrder',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=StrategyOrder,
    )
