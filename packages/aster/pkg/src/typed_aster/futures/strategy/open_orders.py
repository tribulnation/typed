# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.futures.strategy.schemas import FuturesStrategyOrder


class StrategyOpenOrderByClientId(TypedDict):
  """Open strategy order to query. Identified by `clientStrategyId`."""

  clientStrategyId: str
  """Client strategy id."""
  strategyType: Literal['OTO', 'OCO', 'OTOCO']
  """Strategy type."""


class StrategyOpenOrderById(TypedDict):
  """Open strategy order to query. Identified by `strategyId`."""

  strategyId: int
  """Strategy order id."""
  strategyType: Literal['OTO', 'OCO', 'OTOCO']
  """Strategy type."""


Request = StrategyOpenOrderById | StrategyOpenOrderByClientId


class OpenOrders(RpcEndpoint):
  """Query a current open strategy order and its sub-orders."""

  async def open_orders(
    self,
    strategy_open_order_request: Request,
    *,
    validate: bool | None = None,
  ) -> FuturesStrategyOrder:
    """Query a current open strategy order and its sub-orders.

    Args:
      strategy_open_order_request: Open strategy order to query. Either `strategyId` or `clientStrategyId` is required, not both.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#query-strategy-open-order-user_data)
    """
    return await self.request(
      strategy_open_order_request,
      method='GET',
      path='strategyOpenOrder',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=FuturesStrategyOrder,
    )
