# Generated by typed-dev — see CONTRIBUTING.md.
"""Shapes shared by several endpoints of `futures.strategy`."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_aster.core import TimestampMillis
from decimal import Decimal


class StrategySubOrderDetail(TypedDict):
  """One sub-order of a strategy order."""

  strategyId: int
  """Strategy order id."""
  orderId: int
  """Order id of this sub-order (assigned even while `PENDING`)."""
  clientOrderId: NotRequired[str]
  """Client order id."""
  status: NotRequired[
    Literal[
      'PENDING', 'NEW', 'PARTIALLY_FILLED', 'FILLED', 'CANCELED', 'REJECTED', 'EXPIRED'
    ]
  ]
  """Order status; `PENDING` for a sub-order not yet placed (its trigger has not fired)."""
  strategySubId: int
  """Sub-order sequence number, from 1."""
  firstDrivenId: NotRequired[int]
  """`strategySubId` driving the first trigger, `0` when none."""
  firstDrivenOn: NotRequired[str | None]
  """Event of the driving sub-order that activates the first trigger; `null` when none (history), and filled with a default (`NEW`/`PLACE_ORDER`) on a sub-order with no driver while the strategy is open."""
  firstTrigger: NotRequired[str | None]
  """First trigger action; `null` when none (history), and filled with a default (`NEW`/`PLACE_ORDER`) on a sub-order with no driver while the strategy is open."""
  secondDrivenId: NotRequired[int]
  """`strategySubId` driving the second trigger, `0` when none."""
  secondDrivenOn: NotRequired[str | None]
  """Event that activates the second trigger; `null` when none (history), and filled with a default (`NEW`/`PLACE_ORDER`) on a sub-order with no driver while the strategy is open."""
  secondTrigger: NotRequired[str | None]
  """Second trigger action; `null` when none (history), and filled with a default (`NEW`/`PLACE_ORDER`) on a sub-order with no driver while the strategy is open."""
  securityType: NotRequired[str]
  """Security type, e.g. `USDT_FUTURES`."""
  symbol: str
  """Symbol."""
  side: NotRequired[Literal['BUY', 'SELL']]
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side."""
  type: NotRequired[
    Literal[
      'LIMIT',
      'MARKET',
      'STOP',
      'STOP_MARKET',
      'TAKE_PROFIT',
      'TAKE_PROFIT_MARKET',
      'TRAILING_STOP_MARKET',
    ]
  ]
  """Order type."""
  timeInForce: NotRequired[Literal['GTC', 'IOC', 'GTX', 'HIDDEN']]
  """Time in force."""
  quantity: NotRequired[Decimal]
  """Order quantity."""
  reduceOnly: NotRequired[bool]
  """Whether the sub-order is reduce-only."""
  closePosition: NotRequired[bool]
  """Whether the sub-order is a Close-All order."""
  price: NotRequired[Decimal]
  """Limit price."""
  avgPrice: NotRequired[Decimal]
  """Average fill price."""
  priceProtect: NotRequired[bool]
  """Whether trigger protection is enabled."""
  stopPrice: NotRequired[Decimal]
  """Trigger price."""
  activatePrice: NotRequired[Decimal | None]
  """Trailing-stop activation price, `null` when not a trailing stop."""
  callbackRate: NotRequired[Decimal | None]
  """Trailing-stop callback rate, `null` when not a trailing stop."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """Price that triggers `stopPrice`."""
  triggerTime: NotRequired[TimestampMillis]
  """Trigger time, `0` when not triggered."""


class FuturesStrategyOrder(TypedDict):
  """A strategy order with its sub-orders."""

  strategyId: int
  """Strategy order id."""
  clientStrategyId: NotRequired[str]
  """Client strategy id."""
  strategyType: Literal['OTO', 'OCO', 'OTOCO']
  """Strategy type."""
  strategyStatus: str
  """Strategy status, e.g. `WORKING` or `EXPIRED`."""
  bookTime: NotRequired[TimestampMillis]
  """Time the strategy was booked."""
  updateTime: NotRequired[TimestampMillis]
  """Last update time."""
  subOrders: list[StrategySubOrderDetail]
  """Sub-orders."""
