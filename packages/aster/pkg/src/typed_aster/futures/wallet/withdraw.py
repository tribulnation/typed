# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import Withdrawal


class Request(TypedDict):
  amount: Decimal
  """Withdrawal amount in token units."""
  chainId: int
  """Withdrawal chain id: `1` (ETH), `56` (BSC) or `42161` (Arbitrum)."""
  asset: str
  """Currency name, e.g. `USDT`."""
  fee: Decimal
  """Withdrawal fee in token units (from the estimate-withdraw-fee API)."""
  receiver: str
  """Receiving address; for EVM, the same address the withdraw signature names."""


class Withdraw(RpcEndpoint):
  """Withdraw from the futures wallet to an EVM chain. The main wallet signs the EIP-712 withdraw `Action` (`userNonce`, `userSignature`); the call itself is agent-signed."""

  async def withdraw(
    self,
    *,
    amount: Decimal,
    chain_id: int,
    asset: str,
    fee: Decimal,
    receiver: str,
    validate: bool | None = None,
  ) -> Withdrawal:
    """Withdraw from the futures wallet to an EVM chain. The main wallet signs the EIP-712 withdraw `Action` (`userNonce`, `userSignature`); the call itself is agent-signed.

    Args:
      amount: Withdrawal amount in token units.
      chain_id: Withdrawal chain id: `1` (ETH), `56` (BSC) or `42161` (Arbitrum).
      asset: Currency name, e.g. `USDT`.
      fee: Withdrawal fee in token units (from the estimate-withdraw-fee API).
      receiver: Receiving address; for EVM, the same address the withdraw signature names.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/deposit&withdrawal/#withdraw-by-fapiv3-evm-futures)
    """
    return await self.request(
      Request(amount=amount, chainId=chain_id, asset=asset, fee=fee, receiver=receiver),
      method='POST',
      path='aster/user-withdraw',
      meta={'sign': 'withdraw'},
      validate=validate,
      request_type=Request,
      response_type=Withdrawal,
    )
