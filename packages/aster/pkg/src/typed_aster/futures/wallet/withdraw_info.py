# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class ChainWithdrawBalance(TypedDict):
  """Withdrawable amounts of one asset on one chain."""

  chainId: int
  """Chain id."""
  spotMaxWithdrawAmount: Decimal
  """Maximum withdrawable spot amount on this chain."""
  perpMaxWithdrawAmount: Decimal
  """Maximum withdrawable futures amount on this chain."""
  chainLimit: Decimal
  """Maximum withdrawable amount on this chain."""
  withdrawFee: Decimal
  """Withdrawal fee on this chain."""


class Request(TypedDict): ...


class AssetWithdrawBalance(TypedDict):
  """Withdrawable amounts of one asset."""

  currency: str
  """Asset name."""
  spotTotalWithdrawAmount: Decimal
  """Total spot balance available for withdrawal."""
  perpTotalWithdrawAmount: Decimal
  """Total futures balance available for withdrawal."""
  dailyLimit: Decimal
  """Remaining daily withdrawal limit for this asset, in USD."""
  chainBalances: dict[str, ChainWithdrawBalance]
  """Per-chain amounts, keyed by chain id."""


class WithdrawInfo(TypedDict):
  """Withdrawal limits and withdrawable balances."""

  userDailyLimit: Decimal
  """User's daily withdrawal limit, in USD."""
  userRemainingDailyLimit: Decimal
  """User's remaining daily withdrawal limit, in USD."""
  totalDailyLimit: Decimal
  """Global daily withdrawal limit, in USD."""
  totalRemainingDailyLimit: Decimal
  """Global remaining daily withdrawal limit, in USD."""
  balances: dict[str, AssetWithdrawBalance]
  """Non-zero asset balances, keyed by asset name."""


class WithdrawInfoEndpoint(RpcEndpoint):
  """Get the withdrawal limits and the withdrawable balances of every non-zero asset, per chain."""

  async def withdraw_info(self, *, validate: bool | None = None) -> WithdrawInfo:
    """Get the withdrawal limits and the withdrawable balances of every non-zero asset, per chain.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/deposit&withdrawal/#get-user-withdraw-infov3)
    """
    return await self.request(
      Request(),
      method='POST',
      path='aster/user-withdraw-info',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=WithdrawInfo,
    )
