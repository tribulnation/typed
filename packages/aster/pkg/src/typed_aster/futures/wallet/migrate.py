# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class MigrationBatch(TypedDict):
  """Started migration batch."""

  batchId: str
  """Batch id, for `migrate_history`."""


class Request(TypedDict): ...


Response = MigrationBatch | None


class Migrate(RpcEndpoint):
  """Migrate every positive-balance asset (up to 300 per batch) of the source account (the signing main wallet, `user`) to the authenticated destination account. The source must have no open positions and no open orders."""

  async def migrate(self, *, validate: bool | None = None) -> Response:
    """Migrate every positive-balance asset (up to 300 per batch) of the source account (the signing main wallet, `user`) to the authenticated destination account. The source must have no open positions and no open orders.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#migrate-user-assets-withdraw)
    """
    return await self.request(
      Request(),
      method='POST',
      path='asset/migrateUser',
      meta={'sign': 'typed', 'action': 'UserAssetMigrate'},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
