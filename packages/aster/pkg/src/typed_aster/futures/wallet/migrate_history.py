# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class MigrationRecord(TypedDict):
  """Migration of one asset."""

  id: int
  """Record id."""
  asset: str
  """Asset name."""
  amount: Decimal
  """Amount migrated."""
  tranId: NotRequired[int | None]
  """Transaction id; `null` until processed."""
  status: Literal['I', 'S', 'F']
  """`I` pending, `S` success, `F` failed."""
  fromStatus: NotRequired[Literal['S', 'F', 'P'] | None]
  """Debit status on the source: `S` success, `F` failed, `P` processing."""
  fromErrorCode: NotRequired[Any]
  """Debit error code; `null` when none (type undocumented)."""
  fromResponse: NotRequired[Any]
  """Debit error response; `null` when none (type undocumented)."""
  toStatus: NotRequired[Literal['S', 'F', 'P'] | None]
  """Credit status on the destination: `S` success, `F` failed, `P` processing."""
  toErrorCode: NotRequired[Any]
  """Credit error code; `null` when none (type undocumented)."""
  toResponse: NotRequired[Any]
  """Credit error response; `null` when none (type undocumented)."""


class Request(TypedDict):
  batchId: str
  """Batch id returned by `migrate`."""


class MigrationHistory(TypedDict):
  """Status of one migration batch."""

  batchId: str
  """Batch id."""
  totalCount: NotRequired[int]
  """Total number of records."""
  successCount: NotRequired[int]
  """Successful records (`status` `S`)."""
  processingCount: NotRequired[int]
  """Processing records (`status` `P`)."""
  failCount: NotRequired[int]
  """Failed records (`status` `F`)."""
  initCount: NotRequired[int]
  """Pending records (`status` `I`)."""
  details: list[MigrationRecord]
  """Per-asset records."""


class MigrateHistory(RpcEndpoint):
  """Get the status of one asset migration batch."""

  async def migrate_history(
    self,
    batch_id: str,
    *,
    validate: bool | None = None,
  ) -> MigrationHistory:
    """Get the status of one asset migration batch.

    Args:
      batch_id: Batch id returned by `migrate`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#migrate-user-assets-history-user_data)
    """
    return await self.request(
      Request(batchId=batch_id),
      method='GET',
      path='asset/migrateUser/history',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=MigrationHistory,
    )
