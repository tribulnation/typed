# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import TransferResult


class Request(TypedDict):
  amount: Decimal
  """Amount to transfer."""
  asset: str
  """Asset, e.g. `USDT`."""
  clientTranId: str
  """Client transaction id."""
  kindType: Literal['FUTURE_SPOT', 'SPOT_FUTURE']
  """`FUTURE_SPOT`: futures to spot; `SPOT_FUTURE`: spot to futures."""


class Transfer(RpcEndpoint):
  """Transfer an asset between the futures and spot wallets."""

  async def transfer(
    self,
    amount: Decimal,
    *,
    asset: str,
    client_tran_id: str,
    kind_type: Literal['FUTURE_SPOT', 'SPOT_FUTURE'],
    validate: bool | None = None,
  ) -> TransferResult:
    """Transfer an asset between the futures and spot wallets.

    Args:
      amount: Amount to transfer.
      asset: Asset, e.g. `USDT`.
      client_tran_id: Client transaction id.
      kind_type: `FUTURE_SPOT`: futures to spot; `SPOT_FUTURE`: spot to futures.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#transfer-between-futures-and-spot-transfer)
    """
    return await self.request(
      Request(
        amount=amount, asset=asset, clientTranId=client_tran_id, kindType=kind_type
      ),
      method='POST',
      path='asset/wallet/transfer',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=TransferResult,
    )
