# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_aster.core import TimestampMillis
from typed_aster.core.endpoint.rpc import RpcEndpoint


class DepositWithdrawRecord(TypedDict):
  """One deposit or withdrawal."""

  id: str
  """Record id."""
  type: Literal['DEPOSIT', 'WITHDRAW']
  """Record type."""
  asset: str
  """Asset name, e.g. `USDT`."""
  amount: Decimal
  """Amount."""
  state: Literal['PROCESSING', 'SUCCESS', 'FAILED']
  """Status."""
  txHash: NotRequired[str]
  """On-chain transaction hash."""
  time: TimestampMillis
  """Record time."""
  chainId: NotRequired[int]
  """Chain id."""
  accountType: NotRequired[Literal['spot', 'perp']]
  """Account type."""


class Request(TypedDict): ...


Response = list[DepositWithdrawRecord]


class DepositWithdrawHistory(RpcEndpoint):
  """Get the deposit and withdrawal history."""

  async def deposit_withdraw_history(self, *, validate: bool | None = None) -> Response:
    """Get the deposit and withdrawal history.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/deposit&withdrawal/#get-deposit-and-withdraw-historyv3)
    """
    return await self.request(
      Request(),
      method='POST',
      path='aster/deposit-withdraw-history',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
