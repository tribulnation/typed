# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.futures.trade.schemas import FuturesOrder
from typed_aster.schemas import BatchError


class CancelBatchOrdersRequestByClientIds(TypedDict):
  """Orders to cancel. Identified by client order ids."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  origClientOrderIdList: list[str]
  """Client order ids (at most 10), sent as a JSON list."""


class CancelBatchOrdersRequestByIds(TypedDict):
  """Orders to cancel. Identified by order ids."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  orderIdList: list[int]
  """Order ids (at most 10), sent as a JSON list."""


Request = CancelBatchOrdersRequestByIds | CancelBatchOrdersRequestByClientIds


Response = list[FuturesOrder | BatchError]


class CancelBatchOrders(RpcEndpoint):
  """Cancel up to 10 orders of one symbol. The reply lists one result per order, each the canceled order or its error."""

  async def cancel_batch_orders(
    self,
    cancel_batch_orders_request: Request,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Cancel up to 10 orders of one symbol. The reply lists one result per order, each the canceled order or its error.

    Args:
      cancel_batch_orders_request: Orders to cancel. Either `orderIdList` or `origClientOrderIdList` is required.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#cancel-multiple-orders-trade)
    """
    return await self.request(
      cancel_batch_orders_request,
      method='DELETE',
      path='batchOrders',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
