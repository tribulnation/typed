# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.futures.trade.schemas import FuturesOrder


class ModifyOrderByClientId(TypedDict):
  """New price and quantity for an open `LIMIT` order. Identified by `origClientOrderId`."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  origClientOrderId: str
  """Client order id."""
  side: NotRequired[Literal['BUY', 'SELL']]
  """Order side; when sent it is validated against the order's actual side."""
  quantity: Decimal
  """New order quantity."""
  price: Decimal
  """New order price."""


class ModifyOrderById(TypedDict):
  """New price and quantity for an open `LIMIT` order. Identified by `orderId`."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  orderId: int
  """Order id."""
  side: NotRequired[Literal['BUY', 'SELL']]
  """Order side; when sent it is validated against the order's actual side."""
  quantity: Decimal
  """New order quantity."""
  price: Decimal
  """New order price."""


Request = ModifyOrderById | ModifyOrderByClientId


class ModifyOrder(RpcEndpoint):
  """Modify the price and quantity of an open `LIMIT` order. A modification that fails the symbol filters is rejected and the original order stays. At most 10000 modifications per order."""

  async def modify_order(
    self,
    modify_order_request: Request,
    *,
    validate: bool | None = None,
  ) -> FuturesOrder:
    """Modify the price and quantity of an open `LIMIT` order. A modification that fails the symbol filters is rejected and the original order stays. At most 10000 modifications per order.

    Args:
      modify_order_request: New price and quantity for an open `LIMIT` order. Either `orderId` or `origClientOrderId` is required.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#modify-order-trade)
    """
    return await self.request(
      modify_order_request,
      method='PUT',
      path='order',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=FuturesOrder,
    )
