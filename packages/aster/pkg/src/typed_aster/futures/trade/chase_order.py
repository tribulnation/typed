# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_aster.core import TimestampMillis
from typed_aster.core.endpoint.rpc import RpcEndpoint


class ChaseOrder(TypedDict):
  """A chase strategy order."""

  strategyId: int
  """Strategy id."""
  clientStrategyId: str
  """Client strategy id."""
  symbol: NotRequired[str]
  """Symbol."""
  side: NotRequired[Literal['BUY', 'SELL']]
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side."""
  quantity: NotRequired[Decimal]
  """Order quantity, in `quantityUnit`."""
  quantityUnit: NotRequired[Literal['BASE', 'QUOTE']]
  """Unit of `quantity`."""
  reduceOnly: NotRequired[bool]
  """Whether the order is reduce-only."""
  chaseOffset: NotRequired[Decimal]
  """Distance from the BBO."""
  chaseOffsetType: NotRequired[Literal['ABSOLUTE', 'PERCENTAGE']]
  """Unit of `chaseOffset`."""
  maxChaseOffset: NotRequired[Decimal]
  """Auto-cancel distance from the original BBO."""
  maxChaseOffsetType: NotRequired[Literal['ABSOLUTE', 'PERCENTAGE']]
  """Unit of `maxChaseOffset`."""
  timeInForce: NotRequired[Literal['GTC', 'IOC', 'GTX', 'HIDDEN']]
  """Time in force."""
  strategyStatus: str
  """Strategy status, e.g. `WORKING`."""
  bookTime: NotRequired[TimestampMillis]
  """Time the strategy was booked."""
  updateTime: TimestampMillis
  """Last update time."""
  strategyType: Literal['CHASE']
  """Strategy type, `CHASE`."""
  failureCode: NotRequired[int]
  """Failure code, `0` on success."""
  failureReason: NotRequired[str]
  """Failure reason, empty on success."""


class Request(TypedDict):
  """Chase order to place."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. Defaults to `BOTH` in One-way Mode; `LONG` or `SHORT` in Hedge Mode, where it must be sent."""
  quantityUnit: Literal['BASE', 'QUOTE']
  """Unit of `quantity`: `BASE` asset or `QUOTE` asset (converted to base at the mark price)."""
  quantity: Decimal
  """Order quantity, in `quantityUnit`."""
  reduceOnly: NotRequired[Literal['true', 'false']]
  """Reduce-only flag, `"true"` or `"false"` (case-insensitive, default `"false"`)."""
  chaseOffset: NotRequired[Decimal]
  """Distance from the BBO to peg at (default `0`); non-negative and a multiple of `tickSize`. BUY price = bid1 - offset, SELL price = ask1 + offset."""
  chaseOffsetType: NotRequired[Literal['ABSOLUTE', 'PERCENTAGE']]
  """Unit of `chaseOffset`; only `ABSOLUTE` is implemented (`PERCENTAGE` returns `UNSUPPORTED_OPERATION`)."""
  maxChaseOffset: NotRequired[Decimal]
  """Maximum distance from the original BBO before the chase auto-cancels; positive. No distance limit when omitted."""
  maxChaseOffsetType: NotRequired[Literal['ABSOLUTE', 'PERCENTAGE']]
  """Unit of `maxChaseOffset` (default `ABSOLUTE`): `ABSOLUTE` in price units (multiple of `tickSize`), `PERCENTAGE` with at most 2 decimals."""
  timeInForce: NotRequired[Literal['GTC', 'IOC', 'GTX', 'HIDDEN']]
  """Time in force (default `GTX`, post-only)."""
  clientStrategyId: NotRequired[str]
  """Client strategy id, at most 28 characters; generated when omitted."""


class ChaseOrderEndpoint(RpcEndpoint):
  """Place a chase strategy order: a BBO-pegged post-only limit order that the strategy service re-pegs to the best bid/ask every second until it fills, is canceled (cancel the order normally), or the market moves beyond `maxChaseOffset` from the original BBO (auto-cancel, reason `OFFSET_CANCELLED`)."""

  async def chase_order(
    self,
    *,
    symbol: str,
    side: Literal['BUY', 'SELL'],
    position_side: Literal['BOTH', 'LONG', 'SHORT'] | None = None,
    quantity_unit: Literal['BASE', 'QUOTE'],
    quantity: Decimal,
    reduce_only: Literal['true', 'false'] | None = None,
    chase_offset: Decimal | None = None,
    chase_offset_type: Literal['ABSOLUTE', 'PERCENTAGE'] | None = None,
    max_chase_offset: Decimal | None = None,
    max_chase_offset_type: Literal['ABSOLUTE', 'PERCENTAGE'] | None = None,
    time_in_force: Literal['GTC', 'IOC', 'GTX', 'HIDDEN'] | None = None,
    client_strategy_id: str | None = None,
    validate: bool | None = None,
  ) -> ChaseOrder:
    """Place a chase strategy order: a BBO-pegged post-only limit order that the strategy service re-pegs to the best bid/ask every second until it fills, is canceled (cancel the order normally), or the market moves beyond `maxChaseOffset` from the original BBO (auto-cancel, reason `OFFSET_CANCELLED`).

    Args:
      symbol: Symbol, e.g. `BTCUSDT`.
      side: Order side.
      position_side: Position side. Defaults to `BOTH` in One-way Mode; `LONG` or `SHORT` in Hedge Mode, where it must be sent.
      quantity_unit: Unit of `quantity`: `BASE` asset or `QUOTE` asset (converted to base at the mark price).
      quantity: Order quantity, in `quantityUnit`.
      reduce_only: Reduce-only flag, `"true"` or `"false"` (case-insensitive, default `"false"`).
      chase_offset: Distance from the BBO to peg at (default `0`); non-negative and a multiple of `tickSize`. BUY price = bid1 - offset, SELL price = ask1 + offset.
      chase_offset_type: Unit of `chaseOffset`; only `ABSOLUTE` is implemented (`PERCENTAGE` returns `UNSUPPORTED_OPERATION`).
      max_chase_offset: Maximum distance from the original BBO before the chase auto-cancels; positive. No distance limit when omitted.
      max_chase_offset_type: Unit of `maxChaseOffset` (default `ABSOLUTE`): `ABSOLUTE` in price units (multiple of `tickSize`), `PERCENTAGE` with at most 2 decimals.
      time_in_force: Time in force (default `GTX`, post-only).
      client_strategy_id: Client strategy id, at most 28 characters; generated when omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#place-chase-order-trade)
    """
    request: Request = Request(
      symbol=symbol, side=side, quantityUnit=quantity_unit, quantity=quantity
    )
    if position_side is not None:
      request['positionSide'] = position_side
    if reduce_only is not None:
      request['reduceOnly'] = reduce_only
    if chase_offset is not None:
      request['chaseOffset'] = chase_offset
    if chase_offset_type is not None:
      request['chaseOffsetType'] = chase_offset_type
    if max_chase_offset is not None:
      request['maxChaseOffset'] = max_chase_offset
    if max_chase_offset_type is not None:
      request['maxChaseOffsetType'] = max_chase_offset_type
    if time_in_force is not None:
      request['timeInForce'] = time_in_force
    if client_strategy_id is not None:
      request['clientStrategyId'] = client_strategy_id
    return await self.request(
      request,
      method='POST',
      path='chase',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=ChaseOrder,
    )
