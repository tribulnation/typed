# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.futures.trade.schemas import FuturesOrder


class GuardedCancelOrderByClientId(TypedDict):
  """Order to cancel. Identified by `origClientOrderId`."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  origClientOrderId: str
  """Client order id."""
  nonce: NotRequired[int]
  """The nonce the targeted order(s) were placed with, in epoch microseconds (pass the one chosen on placement). The guard only cancels orders placed with this nonce. Omit to sign with a freshly drawn nonce."""


class GuardedCancelOrderById(TypedDict):
  """Order to cancel. Identified by `orderId`."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  orderId: int
  """Order id."""
  nonce: NotRequired[int]
  """The nonce the targeted order(s) were placed with, in epoch microseconds (pass the one chosen on placement). The guard only cancels orders placed with this nonce. Omit to sign with a freshly drawn nonce."""


Request = GuardedCancelOrderById | GuardedCancelOrderByClientId


class GuardedCancelOrder(RpcEndpoint):
  """Cancel an active order, guarded by the nonce the order was placed with against duplicate or out-of-order cancellation. Otherwise identical to cancelling an order."""

  async def guarded_cancel_order(
    self,
    guarded_cancel_order_request: Request,
    *,
    validate: bool | None = None,
  ) -> FuturesOrder:
    """Cancel an active order, guarded by the nonce the order was placed with against duplicate or out-of-order cancellation. Otherwise identical to cancelling an order.

    Args:
      guarded_cancel_order_request: Order to cancel. Either `orderId` or `origClientOrderId` is required.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#cancel-order-guarded-trade)
    """
    return await self.request(
      guarded_cancel_order_request,
      method='DELETE',
      path='guardedCancelOrder',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=FuturesOrder,
    )
