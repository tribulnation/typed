# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.futures.trade.schemas import FuturesOrder
from typed_aster.schemas import BatchError


class GuardedCancelBatchOrdersRequestByClientIds(TypedDict):
  """Orders to cancel. Identified by client order ids."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  origClientOrderIdList: list[str]
  """Client order ids (at most 10), sent as a JSON list."""
  nonce: NotRequired[int]
  """The nonce the targeted order(s) were placed with, in epoch microseconds (pass the one chosen on placement). The guard only cancels orders placed with this nonce. Omit to sign with a freshly drawn nonce."""


class GuardedCancelBatchOrdersRequestByIds(TypedDict):
  """Orders to cancel. Identified by order ids."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  orderIdList: list[int]
  """Order ids (at most 10), sent as a JSON list."""
  nonce: NotRequired[int]
  """The nonce the targeted order(s) were placed with, in epoch microseconds (pass the one chosen on placement). The guard only cancels orders placed with this nonce. Omit to sign with a freshly drawn nonce."""


Request = (
  GuardedCancelBatchOrdersRequestByIds | GuardedCancelBatchOrdersRequestByClientIds
)


Response = list[FuturesOrder | BatchError]


class GuardedCancelBatchOrders(RpcEndpoint):
  """Cancel up to 10 orders, guarded by the nonce the orders were placed with. Otherwise identical to cancelling multiple orders."""

  async def guarded_cancel_batch_orders(
    self,
    guarded_cancel_batch_orders_request: Request,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Cancel up to 10 orders, guarded by the nonce the orders were placed with. Otherwise identical to cancelling multiple orders.

    Args:
      guarded_cancel_batch_orders_request: Orders to cancel. Either `orderIdList` or `origClientOrderIdList` is required.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#cancel-multiple-orders-guarded-trade)
    """
    return await self.request(
      guarded_cancel_batch_orders_request,
      method='DELETE',
      path='guardedBatchOrders',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
