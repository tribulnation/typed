# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.futures.trade.schemas import FuturesOrder


class OpenOrderByClientId(TypedDict):
  """Open order to query. Identified by `origClientOrderId`."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  origClientOrderId: str
  """Client order id."""


class OpenOrderById(TypedDict):
  """Open order to query. Identified by `orderId`."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  orderId: int
  """Order id."""


Request = OpenOrderById | OpenOrderByClientId


class OpenOrder(RpcEndpoint):
  """Query one open order. A filled or canceled order answers "Order does not exist"."""

  async def open_order(
    self,
    open_order_request: Request,
    *,
    validate: bool | None = None,
  ) -> FuturesOrder:
    """Query one open order. A filled or canceled order answers "Order does not exist".

    Args:
      open_order_request: Open order to query. Either `orderId` or `origClientOrderId` is required.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#query-current-open-order-user_data)
    """
    return await self.request(
      open_order_request,
      method='GET',
      path='openOrder',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=FuturesOrder,
    )
