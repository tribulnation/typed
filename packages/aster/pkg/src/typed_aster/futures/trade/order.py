# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.futures.trade.schemas import FuturesOrder


class QueryOrderByClientId(TypedDict):
  """Order to query. Identified by `origClientOrderId`."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  origClientOrderId: str
  """Client order id."""


class QueryOrderById(TypedDict):
  """Order to query. Identified by `orderId`."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  orderId: int
  """Order id."""


Request = QueryOrderById | QueryOrderByClientId


class Order(RpcEndpoint):
  """Check an order's status. Orders that are `CANCELED` or `EXPIRED`, have no fill, and were created more than 7 days ago are not found."""

  async def order(
    self,
    query_order_request: Request,
    *,
    validate: bool | None = None,
  ) -> FuturesOrder:
    """Check an order's status. Orders that are `CANCELED` or `EXPIRED`, have no fill, and were created more than 7 days ago are not found.

    Args:
      query_order_request: Order to query. Either `orderId` or `origClientOrderId` is required.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#query-order-user_data)
    """
    return await self.request(
      query_order_request,
      method='GET',
      path='order',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=FuturesOrder,
    )
