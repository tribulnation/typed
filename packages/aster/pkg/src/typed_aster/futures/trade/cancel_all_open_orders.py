# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.schemas import OperationResult


class Request(TypedDict):
  """Symbol whose open orders to cancel."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""


class CancelAllOpenOrders(RpcEndpoint):
  """Cancel every open order of a symbol."""

  async def cancel_all_open_orders(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> OperationResult:
    """Cancel every open order of a symbol.

    Args:
      symbol: Symbol, e.g. `BTCUSDT`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#cancel-all-open-orders-trade)
    """
    return await self.request(
      Request(symbol=symbol),
      method='DELETE',
      path='allOpenOrders',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=OperationResult,
    )
