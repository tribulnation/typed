# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.futures.trade.schemas import FuturesOrder


class CancelOrderByClientId(TypedDict):
  """Order to cancel. Identified by `origClientOrderId`."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  origClientOrderId: str
  """Client order id."""


class CancelOrderById(TypedDict):
  """Order to cancel. Identified by `orderId`."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  orderId: int
  """Order id."""


Request = CancelOrderById | CancelOrderByClientId


class CancelOrder(RpcEndpoint):
  """Cancel an active order."""

  async def cancel_order(
    self,
    cancel_order_request: Request,
    *,
    validate: bool | None = None,
  ) -> FuturesOrder:
    """Cancel an active order.

    Args:
      cancel_order_request: Order to cancel. Either `orderId` or `origClientOrderId` is required.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#cancel-order-trade)
    """
    return await self.request(
      cancel_order_request,
      method='DELETE',
      path='order',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=FuturesOrder,
    )
