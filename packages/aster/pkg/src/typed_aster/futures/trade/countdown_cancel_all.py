# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class CountdownCancelAll(TypedDict):
  """The countdown now in force."""

  symbol: str
  """Symbol."""
  countdownTime: int
  """Countdown in milliseconds."""


class Request(TypedDict):
  """Countdown to set."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  countdownTime: int
  """Countdown in milliseconds (1000 = 1 s); `0` cancels the timer."""


class CountdownCancelAllEndpoint(RpcEndpoint):
  """Cancel all open orders of a symbol when the countdown ends. Call it repeatedly as a heartbeat to push the deadline out; `countdownTime=0` stops the timer. Countdowns are checked about every 10 ms."""

  async def countdown_cancel_all(
    self,
    symbol: str,
    *,
    countdown_time: int,
    validate: bool | None = None,
  ) -> CountdownCancelAll:
    """Cancel all open orders of a symbol when the countdown ends. Call it repeatedly as a heartbeat to push the deadline out; `countdownTime=0` stops the timer. Countdowns are checked about every 10 ms.

    Args:
      symbol: Symbol, e.g. `BTCUSDT`.
      countdown_time: Countdown in milliseconds (1000 = 1 s); `0` cancels the timer.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#auto-cancel-all-open-orders-trade)
    """
    return await self.request(
      Request(symbol=symbol, countdownTime=countdown_time),
      method='POST',
      path='countdownCancelAll',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=CountdownCancelAll,
    )
