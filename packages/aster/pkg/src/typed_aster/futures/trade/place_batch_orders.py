# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.futures.trade.schemas import FuturesOrder
from typed_aster.schemas import BatchError


class BatchLimitOrder(TypedDict):
  """A `LIMIT` order: `timeInForce`, `quantity` and `price` are required."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. Defaults to `BOTH` in One-way Mode; `LONG` or `SHORT` in Hedge Mode, where it must be sent."""
  type: Literal['LIMIT']
  """Order type."""
  timeInForce: Literal['GTC', 'IOC', 'GTX', 'HIDDEN']
  """Time in force."""
  quantity: Decimal
  """Order quantity, in the base asset."""
  reduceOnly: NotRequired[Literal['true', 'false']]
  """Reduce-only flag, `"true"` or `"false"` (default `"false"`). Cannot be sent in Hedge Mode."""
  price: Decimal
  """Limit price."""
  newClientOrderId: NotRequired[str]
  """Client order id, unique among open orders; generated when omitted."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT']]
  """Response type (default `ACK`). `RESULT` returns the final status of a `MARKET` order or an immediate `LIMIT` order."""


class BatchMarketOrder(TypedDict):
  """A `MARKET` order: `quantity` is required; `price`, `timeInForce` and `stopPrice` are rejected."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. Defaults to `BOTH` in One-way Mode; `LONG` or `SHORT` in Hedge Mode, where it must be sent."""
  type: Literal['MARKET']
  """Order type."""
  quantity: Decimal
  """Order quantity, in the base asset."""
  reduceOnly: NotRequired[Literal['true', 'false']]
  """Reduce-only flag, `"true"` or `"false"` (default `"false"`). Cannot be sent in Hedge Mode."""
  newClientOrderId: NotRequired[str]
  """Client order id, unique among open orders; generated when omitted."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT']]
  """Response type (default `ACK`). `RESULT` returns the final status of a `MARKET` order or an immediate `LIMIT` order."""


class BatchStopLimitOrder(TypedDict):
  """A `STOP` or `TAKE_PROFIT` (conditional limit) order: `quantity`, `price` and `stopPrice` are required; `timeInForce` defaults to `GTC`."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. Defaults to `BOTH` in One-way Mode; `LONG` or `SHORT` in Hedge Mode, where it must be sent."""
  type: Literal['STOP', 'TAKE_PROFIT']
  """Order type."""
  timeInForce: NotRequired[Literal['GTC', 'IOC', 'GTX', 'HIDDEN']]
  """Time in force."""
  quantity: Decimal
  """Order quantity, in the base asset."""
  reduceOnly: NotRequired[Literal['true', 'false']]
  """Reduce-only flag, `"true"` or `"false"` (default `"false"`). Cannot be sent in Hedge Mode."""
  price: Decimal
  """Limit price."""
  newClientOrderId: NotRequired[str]
  """Client order id, unique among open orders; generated when omitted."""
  stopPrice: Decimal
  """Trigger price."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """Price that triggers `stopPrice` (default `CONTRACT_PRICE`)."""
  priceProtect: NotRequired[Literal['TRUE', 'FALSE']]
  """Trigger protection flag, `"TRUE"` or `"FALSE"` (default `"FALSE"`)."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT']]
  """Response type (default `ACK`). `RESULT` returns the final status of a `MARKET` order or an immediate `LIMIT` order."""


class BatchStopMarketOrder(TypedDict):
  """A `STOP_MARKET` or `TAKE_PROFIT_MARKET` (conditional market) order for a given `quantity`: `stopPrice` is required and `price` is rejected."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. Defaults to `BOTH` in One-way Mode; `LONG` or `SHORT` in Hedge Mode, where it must be sent."""
  type: Literal['STOP_MARKET', 'TAKE_PROFIT_MARKET']
  """Order type."""
  timeInForce: NotRequired[Literal['GTC']]
  """Time in force; only `GTC` is accepted for this order type."""
  quantity: Decimal
  """Order quantity, in the base asset."""
  reduceOnly: NotRequired[Literal['true', 'false']]
  """Reduce-only flag, `"true"` or `"false"` (default `"false"`). Cannot be sent in Hedge Mode."""
  newClientOrderId: NotRequired[str]
  """Client order id, unique among open orders; generated when omitted."""
  stopPrice: Decimal
  """Trigger price."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """Price that triggers `stopPrice` (default `CONTRACT_PRICE`)."""
  priceProtect: NotRequired[Literal['TRUE', 'FALSE']]
  """Trigger protection flag, `"TRUE"` or `"FALSE"` (default `"FALSE"`)."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT']]
  """Response type (default `ACK`). `RESULT` returns the final status of a `MARKET` order or an immediate `LIMIT` order."""


class BatchTrailingStopOrder(TypedDict):
  """A `TRAILING_STOP_MARKET` order: `callbackRate` is required; `activationPrice` defaults to the latest price."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. Defaults to `BOTH` in One-way Mode; `LONG` or `SHORT` in Hedge Mode, where it must be sent."""
  type: Literal['TRAILING_STOP_MARKET']
  """Order type."""
  timeInForce: NotRequired[Literal['GTC']]
  """Time in force; only `GTC` is accepted for this order type."""
  quantity: Decimal
  """Order quantity, in the base asset."""
  reduceOnly: NotRequired[Literal['true', 'false']]
  """Reduce-only flag, `"true"` or `"false"` (default `"false"`). Cannot be sent in Hedge Mode."""
  newClientOrderId: NotRequired[str]
  """Client order id, unique among open orders; generated when omitted."""
  activationPrice: NotRequired[Decimal]
  """Trailing-stop activation price; defaults to the latest price (per `workingType`)."""
  callbackRate: Decimal
  """Trailing-stop callback rate in percent, from 0.1 to 4 (1 means 1%)."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """Price that triggers `stopPrice` (default `CONTRACT_PRICE`)."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT']]
  """Response type (default `ACK`). `RESULT` returns the final status of a `MARKET` order or an immediate `LIMIT` order."""


class Request(TypedDict):
  """Orders to place."""

  batchOrders: list[
    BatchLimitOrder
    | BatchMarketOrder
    | BatchStopLimitOrder
    | BatchStopMarketOrder
    | BatchTrailingStopOrder
  ]
  """Orders to place (1 to 5), sent as a JSON list. Parameter rules are those of placing a single order."""
  nonce: NotRequired[int]
  """Caller-chosen request nonce, in epoch microseconds, signed in place of a freshly drawn one. Choose it (unique, within the venue's time window) when you may later need to reference this request: a guarded cancel of the order, or `noop` to invalidate it while still pending. Omit to let the client draw one."""


Response = list[FuturesOrder | BatchError]


class PlaceBatchOrders(RpcEndpoint):
  """Place up to 5 orders. They are processed concurrently (matching order not guaranteed); the reply lists one result per order, in request order, each the order or its error."""

  async def place_batch_orders(
    self,
    batch_orders: list[
      BatchLimitOrder
      | BatchMarketOrder
      | BatchStopLimitOrder
      | BatchStopMarketOrder
      | BatchTrailingStopOrder
    ],
    *,
    nonce: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Place up to 5 orders. They are processed concurrently (matching order not guaranteed); the reply lists one result per order, in request order, each the order or its error.

    Args:
      batch_orders: Orders to place (1 to 5), sent as a JSON list. Parameter rules are those of placing a single order.
      nonce: Caller-chosen request nonce, in epoch microseconds, signed in place of a freshly drawn one. Choose it (unique, within the venue's time window) when you may later need to reference this request: a guarded cancel of the order, or `noop` to invalidate it while still pending. Omit to let the client draw one.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#place-multiple-orders-trade)
    """
    request: Request = Request(batchOrders=batch_orders)
    if nonce is not None:
      request['nonce'] = nonce
    return await self.request(
      request,
      method='POST',
      path='batchOrders',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
