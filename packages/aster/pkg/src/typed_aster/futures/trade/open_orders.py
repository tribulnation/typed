# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint
from typed_aster.futures.trade.schemas import FuturesOrder


class Request(TypedDict):
  """Optional symbol filter."""

  symbol: NotRequired[str]
  """Symbol; every symbol when omitted."""


Response = list[FuturesOrder]


class OpenOrders(RpcEndpoint):
  """Get all open orders of a symbol, or of every symbol when `symbol` is omitted (weight 40 instead of 1)."""

  async def open_orders(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Get all open orders of a symbol, or of every symbol when `symbol` is omitted (weight 40 instead of 1).

    Args:
      symbol: Symbol; every symbol when omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://asterdex.github.io/aster-api-website/futures-v3/account&trades/#current-all-open-orders-user_data)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='openOrders',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
