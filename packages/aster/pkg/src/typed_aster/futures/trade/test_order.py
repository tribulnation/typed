# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_aster.core.endpoint.rpc import RpcEndpoint


class ClosePositionOrder(TypedDict):
  """A Close-All `STOP_MARKET` or `TAKE_PROFIT_MARKET` order (`closePosition=true`): closes the whole position on trigger; `quantity` and `reduceOnly` are rejected."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. Defaults to `BOTH` in One-way Mode; `LONG` or `SHORT` in Hedge Mode, where it must be sent."""
  type: Literal['STOP_MARKET', 'TAKE_PROFIT_MARKET']
  """Order type."""
  timeInForce: NotRequired[Literal['GTC']]
  """Time in force; only `GTC` is accepted for this order type."""
  newClientOrderId: NotRequired[str]
  """Client order id, unique among open orders; generated when omitted."""
  stopPrice: Decimal
  """Trigger price."""
  closePosition: Literal['true']
  """Close-All: when triggered, close the whole current position on this side. Cannot be sent with `quantity` or `reduceOnly`."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """Price that triggers `stopPrice` (default `CONTRACT_PRICE`)."""
  priceProtect: NotRequired[Literal['TRUE', 'FALSE']]
  """Trigger protection flag, `"TRUE"` or `"FALSE"` (default `"FALSE"`)."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT']]
  """Response type (default `ACK`). `RESULT` returns the final status of a `MARKET` order or an immediate `LIMIT` order."""
  stpMode: NotRequired[Literal['EXPIRE_TAKER', 'EXPIRE_MAKER', 'EXPIRE_BOTH']]
  """Self-trade prevention mode for this order, overriding the account default."""
  builder: NotRequired[str]
  """Builder address (attribution and fee recipient), for orders placed through an approved builder."""
  feeRate: NotRequired[Decimal]
  """Builder fee rate for this order; must not exceed the `maxFeeRate` the user approved for `builder`."""


class LimitOrder(TypedDict):
  """A `LIMIT` order: `timeInForce`, `quantity` and `price` are required."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. Defaults to `BOTH` in One-way Mode; `LONG` or `SHORT` in Hedge Mode, where it must be sent."""
  type: Literal['LIMIT']
  """Order type."""
  timeInForce: Literal['GTC', 'IOC', 'GTX', 'HIDDEN']
  """Time in force."""
  quantity: Decimal
  """Order quantity, in the base asset."""
  reduceOnly: NotRequired[Literal['true', 'false']]
  """Reduce-only flag, `"true"` or `"false"` (default `"false"`). Cannot be sent in Hedge Mode."""
  price: Decimal
  """Limit price."""
  newClientOrderId: NotRequired[str]
  """Client order id, unique among open orders; generated when omitted."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT']]
  """Response type (default `ACK`). `RESULT` returns the final status of a `MARKET` order or an immediate `LIMIT` order."""
  pegPriceType: NotRequired[Literal['COUNTERPARTY_1', 'QUEUE_1']]
  """BBO peg mode. The engine resolves the price from the order book at trigger time using the BBO plus `pegOffset`."""
  pegOffset: NotRequired[Decimal]
  """Signed offset from the BBO when `pegPriceType` is set: non-positive for BUY, non-negative for SELL, a multiple of `tickSize`."""
  stpMode: NotRequired[Literal['EXPIRE_TAKER', 'EXPIRE_MAKER', 'EXPIRE_BOTH']]
  """Self-trade prevention mode for this order, overriding the account default."""
  builder: NotRequired[str]
  """Builder address (attribution and fee recipient), for orders placed through an approved builder."""
  feeRate: NotRequired[Decimal]
  """Builder fee rate for this order; must not exceed the `maxFeeRate` the user approved for `builder`."""


class MarketOrder(TypedDict):
  """A `MARKET` order: `quantity` is required; `price`, `timeInForce` and `stopPrice` are rejected."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. Defaults to `BOTH` in One-way Mode; `LONG` or `SHORT` in Hedge Mode, where it must be sent."""
  type: Literal['MARKET']
  """Order type."""
  quantity: Decimal
  """Order quantity, in the base asset."""
  reduceOnly: NotRequired[Literal['true', 'false']]
  """Reduce-only flag, `"true"` or `"false"` (default `"false"`). Cannot be sent in Hedge Mode."""
  newClientOrderId: NotRequired[str]
  """Client order id, unique among open orders; generated when omitted."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT']]
  """Response type (default `ACK`). `RESULT` returns the final status of a `MARKET` order or an immediate `LIMIT` order."""
  stpMode: NotRequired[Literal['EXPIRE_TAKER', 'EXPIRE_MAKER', 'EXPIRE_BOTH']]
  """Self-trade prevention mode for this order, overriding the account default."""
  builder: NotRequired[str]
  """Builder address (attribution and fee recipient), for orders placed through an approved builder."""
  feeRate: NotRequired[Decimal]
  """Builder fee rate for this order; must not exceed the `maxFeeRate` the user approved for `builder`."""


class OrderTestResult(TypedDict):
  """Placeholder order: every field is `null` (numbers, booleans) or the string `"null"` (strings); the order was only validated."""

  orderId: NotRequired[None]
  """Always `null`."""
  reduceOnly: NotRequired[None]
  """Always `null`."""
  closePosition: NotRequired[None]
  """Always `null`."""
  priceProtect: NotRequired[None]
  """Always `null`."""
  updateTime: NotRequired[None]
  """Always `null`."""
  symbol: NotRequired[str]
  """Always the string `"null"`."""
  status: NotRequired[str]
  """Always the string `"null"`."""
  clientOrderId: NotRequired[str]
  """Always the string `"null"`."""
  price: NotRequired[str]
  """Always the string `"null"`."""
  avgPrice: NotRequired[str]
  """Always the string `"null"`."""
  origQty: NotRequired[str]
  """Always the string `"null"`."""
  executedQty: NotRequired[str]
  """Always the string `"null"`."""
  cumQuote: NotRequired[str]
  """Always the string `"null"`."""
  timeInForce: NotRequired[str]
  """Always the string `"null"`."""
  type: NotRequired[str]
  """Always the string `"null"`."""
  side: NotRequired[str]
  """Always the string `"null"`."""
  positionSide: NotRequired[str]
  """Always the string `"null"`."""
  stopPrice: NotRequired[str]
  """Always the string `"null"`."""
  workingType: NotRequired[str]
  """Always the string `"null"`."""
  origType: NotRequired[str]
  """Always the string `"null"`."""


class StopLimitOrder(TypedDict):
  """A `STOP` or `TAKE_PROFIT` (conditional limit) order: `quantity`, `price` and `stopPrice` are required; `timeInForce` defaults to `GTC`."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. Defaults to `BOTH` in One-way Mode; `LONG` or `SHORT` in Hedge Mode, where it must be sent."""
  type: Literal['STOP', 'TAKE_PROFIT']
  """Order type."""
  timeInForce: NotRequired[Literal['GTC', 'IOC', 'GTX', 'HIDDEN']]
  """Time in force."""
  quantity: Decimal
  """Order quantity, in the base asset."""
  reduceOnly: NotRequired[Literal['true', 'false']]
  """Reduce-only flag, `"true"` or `"false"` (default `"false"`). Cannot be sent in Hedge Mode."""
  price: Decimal
  """Limit price."""
  newClientOrderId: NotRequired[str]
  """Client order id, unique among open orders; generated when omitted."""
  stopPrice: Decimal
  """Trigger price."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """Price that triggers `stopPrice` (default `CONTRACT_PRICE`)."""
  priceProtect: NotRequired[Literal['TRUE', 'FALSE']]
  """Trigger protection flag, `"TRUE"` or `"FALSE"` (default `"FALSE"`)."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT']]
  """Response type (default `ACK`). `RESULT` returns the final status of a `MARKET` order or an immediate `LIMIT` order."""
  stpMode: NotRequired[Literal['EXPIRE_TAKER', 'EXPIRE_MAKER', 'EXPIRE_BOTH']]
  """Self-trade prevention mode for this order, overriding the account default."""
  builder: NotRequired[str]
  """Builder address (attribution and fee recipient), for orders placed through an approved builder."""
  feeRate: NotRequired[Decimal]
  """Builder fee rate for this order; must not exceed the `maxFeeRate` the user approved for `builder`."""


class StopMarketOrder(TypedDict):
  """A `STOP_MARKET` or `TAKE_PROFIT_MARKET` (conditional market) order for a given `quantity`: `stopPrice` is required and `price` is rejected."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. Defaults to `BOTH` in One-way Mode; `LONG` or `SHORT` in Hedge Mode, where it must be sent."""
  type: Literal['STOP_MARKET', 'TAKE_PROFIT_MARKET']
  """Order type."""
  timeInForce: NotRequired[Literal['GTC']]
  """Time in force; only `GTC` is accepted for this order type."""
  quantity: Decimal
  """Order quantity, in the base asset."""
  reduceOnly: NotRequired[Literal['true', 'false']]
  """Reduce-only flag, `"true"` or `"false"` (default `"false"`). Cannot be sent in Hedge Mode."""
  newClientOrderId: NotRequired[str]
  """Client order id, unique among open orders; generated when omitted."""
  stopPrice: Decimal
  """Trigger price."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """Price that triggers `stopPrice` (default `CONTRACT_PRICE`)."""
  priceProtect: NotRequired[Literal['TRUE', 'FALSE']]
  """Trigger protection flag, `"TRUE"` or `"FALSE"` (default `"FALSE"`)."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT']]
  """Response type (default `ACK`). `RESULT` returns the final status of a `MARKET` order or an immediate `LIMIT` order."""
  stpMode: NotRequired[Literal['EXPIRE_TAKER', 'EXPIRE_MAKER', 'EXPIRE_BOTH']]
  """Self-trade prevention mode for this order, overriding the account default."""
  builder: NotRequired[str]
  """Builder address (attribution and fee recipient), for orders placed through an approved builder."""
  feeRate: NotRequired[Decimal]
  """Builder fee rate for this order; must not exceed the `maxFeeRate` the user approved for `builder`."""


class TrailingStopOrder(TypedDict):
  """A `TRAILING_STOP_MARKET` order: `callbackRate` is required; `activationPrice` defaults to the latest price."""

  symbol: str
  """Symbol, e.g. `BTCUSDT`."""
  side: Literal['BUY', 'SELL']
  """Order side."""
  positionSide: NotRequired[Literal['BOTH', 'LONG', 'SHORT']]
  """Position side. Defaults to `BOTH` in One-way Mode; `LONG` or `SHORT` in Hedge Mode, where it must be sent."""
  type: Literal['TRAILING_STOP_MARKET']
  """Order type."""
  timeInForce: NotRequired[Literal['GTC']]
  """Time in force; only `GTC` is accepted for this order type."""
  quantity: Decimal
  """Order quantity, in the base asset."""
  reduceOnly: NotRequired[Literal['true', 'false']]
  """Reduce-only flag, `"true"` or `"false"` (default `"false"`). Cannot be sent in Hedge Mode."""
  newClientOrderId: NotRequired[str]
  """Client order id, unique among open orders; generated when omitted."""
  activationPrice: NotRequired[Decimal]
  """Trailing-stop activation price; defaults to the latest price (per `workingType`)."""
  callbackRate: Decimal
  """Trailing-stop callback rate in percent, from 0.1 to 5 (1 means 1%)."""
  workingType: NotRequired[Literal['MARK_PRICE', 'CONTRACT_PRICE']]
  """Price that triggers `stopPrice` (default `CONTRACT_PRICE`)."""
  newOrderRespType: NotRequired[Literal['ACK', 'RESULT']]
  """Response type (default `ACK`). `RESULT` returns the final status of a `MARKET` order or an immediate `LIMIT` order."""
  stpMode: NotRequired[Literal['EXPIRE_TAKER', 'EXPIRE_MAKER', 'EXPIRE_BOTH']]
  """Self-trade prevention mode for this order, overriding the account default."""
  builder: NotRequired[str]
  """Builder address (attribution and fee recipient), for orders placed through an approved builder."""
  feeRate: NotRequired[Decimal]
  """Builder fee rate for this order; must not exceed the `maxFeeRate` the user approved for `builder`."""


Request = (
  LimitOrder
  | MarketOrder
  | StopLimitOrder
  | StopMarketOrder
  | ClosePositionOrder
  | TrailingStopOrder
)


class TestOrder(RpcEndpoint):
  """Validate a new order without sending it to the matching engine. Parameters are the same as for placing an order."""

  async def test_order(
    self,
    order_request: Request,
    *,
    validate: bool | None = None,
  ) -> OrderTestResult:
    """Validate a new order without sending it to the matching engine. Parameters are the same as for placing an order.

    Args:
      order_request: Order to validate, one shape per order `type`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://github.com/asterdex/API-demo/blob/main/futures-v3-demo/50_orderTest.js)
    """
    return await self.request(
      order_request,
      method='POST',
      path='order/test',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=OrderTestResult,
    )
