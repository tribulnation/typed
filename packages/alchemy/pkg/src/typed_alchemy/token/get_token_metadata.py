# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_alchemy.core.rpc import ChainRpc
from typed_alchemy.schemas import TokenMetadata


class Request(TypedDict):
  contractAddress: str
  """Token contract address (20-byte hex)."""


class GetTokenMetadata(ChainRpc):
  """Returns name, symbol, decimals, and logo for a token contract via the `alchemy_getTokenMetadata` JSON-RPC method."""

  async def get_token_metadata(
    self,
    contract_address: str,
    *,
    validate: bool | None = None,
  ) -> TokenMetadata:
    """Returns name, symbol, decimals, and logo for a token contract via the `alchemy_getTokenMetadata` JSON-RPC method.

    Args:
      contract_address: Token contract address (20-byte hex).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.alchemy.com/docs/data/token-api/token-api-endpoints/alchemy-get-token-metadata)
    """
    return await self.request(
      Request(contractAddress=contract_address),
      method='POST',
      path='alchemy_getTokenMetadata',
      validate=validate,
      request_type=Request,
      response_type=TokenMetadata,
    )
