# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_alchemy.core.rpc import ChainRpc


class Request(TypedDict):
  contract: str
  """ERC-20 token contract address."""
  owner: str
  """Wallet address that owns the tokens."""
  spender: str
  """Address approved to spend tokens from the owner."""


TokenAllowance = str


class GetTokenAllowance(ChainRpc):
  """Returns the ERC-20 token allowance for a spender and owner via the `alchemy_getTokenAllowance` JSON-RPC method."""

  async def get_token_allowance(
    self,
    *,
    contract: str,
    owner: str,
    spender: str,
    validate: bool | None = None,
  ) -> TokenAllowance:
    """Returns the ERC-20 token allowance for a spender and owner via the `alchemy_getTokenAllowance` JSON-RPC method.

    Args:
      contract: ERC-20 token contract address.
      owner: Wallet address that owns the tokens.
      spender: Address approved to spend tokens from the owner.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.alchemy.com/docs/data/token-api/token-api-endpoints/alchemy-get-token-allowance)
    """
    return await self.request(
      Request(contract=contract, owner=owner, spender=spender),
      method='POST',
      path='alchemy_getTokenAllowance',
      validate=validate,
      request_type=Request,
      response_type=TokenAllowance,
    )
