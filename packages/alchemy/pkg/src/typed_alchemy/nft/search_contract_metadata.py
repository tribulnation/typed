# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, NotRequired, TypedDict
from typed_alchemy.core.rest import ChainNft


class Request(TypedDict):
  query: str
  """Search keyword to match against contract metadata."""


class SearchContractMetadataResponse(TypedDict):
  contracts: NotRequired[list[dict[str, Any]]]
  """Array of matching contract metadata objects."""


class SearchContractMetadata(ChainNft):
  """Searches contract metadata across ERC-721 and ERC-1155 contracts for a given keyword. Beta."""

  async def search_contract_metadata(
    self,
    query: str,
    *,
    validate: bool | None = None,
  ) -> SearchContractMetadataResponse:
    """Searches contract metadata across ERC-721 and ERC-1155 contracts for a given keyword. Beta.

    Args:
      query: Search keyword to match against contract metadata.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.alchemy.com/docs/reference/nft-api-endpoints/nft-api-endpoints/nft-metadata-endpoints/search-contract-metadata-v-3)
    """
    return await self.request(
      Request(query=query),
      method='GET',
      path='/searchContractMetadata',
      validate=validate,
      request_type=Request,
      response_type=SearchContractMetadataResponse,
    )
