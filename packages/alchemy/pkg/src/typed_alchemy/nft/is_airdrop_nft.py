# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_alchemy.core.rest import ChainNft


class IsAirdropNftResponse(TypedDict):
  isAirdrop: NotRequired[bool]
  """True if the token was airdropped, false otherwise."""


class Request(TypedDict):
  contractAddress: str
  """NFT contract address (ERC721 or ERC1155)."""
  tokenId: str
  """Token ID in hex or decimal format."""


class IsAirdropNft(ChainNft):
  """Checks whether a specific NFT token was airdropped (minted by an address different from the recipient)."""

  async def is_airdrop_nft(
    self,
    *,
    contract_address: str,
    token_id: str,
    validate: bool | None = None,
  ) -> IsAirdropNftResponse:
    """Checks whether a specific NFT token was airdropped (minted by an address different from the recipient).

    Args:
      contract_address: NFT contract address (ERC721 or ERC1155).
      token_id: Token ID in hex or decimal format.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.alchemy.com/docs/data/nft-api/api-reference/nft-spam-endpoints/is-airdrop-nft-v-3)
    """
    return await self.request(
      Request(contractAddress=contract_address, tokenId=token_id),
      method='GET',
      path='/isAirdropNFT',
      validate=validate,
      request_type=Request,
      response_type=IsAirdropNftResponse,
    )
