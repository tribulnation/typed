# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_alchemy.core.rest import ChainNft


class IsHolderOfContractResponse(TypedDict):
  isHolderOfContract: NotRequired[bool]
  """True if the wallet owns at least one token in the contract."""


class Request(TypedDict):
  wallet: str
  """Wallet address to check."""
  contractAddress: str
  """NFT contract address (ERC721 or ERC1155)."""


class IsHolderOfContract(ChainNft):
  """Checks whether a given wallet owns any token in a specified NFT contract."""

  async def is_holder_of_contract(
    self,
    *,
    wallet: str,
    contract_address: str,
    validate: bool | None = None,
  ) -> IsHolderOfContractResponse:
    """Checks whether a given wallet owns any token in a specified NFT contract.

    Args:
      wallet: Wallet address to check.
      contract_address: NFT contract address (ERC721 or ERC1155).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.alchemy.com/docs/reference/nft-api-endpoints/nft-api-endpoints/nft-ownership-endpoints/is-holder-of-contract-v-3)
    """
    return await self.request(
      Request(wallet=wallet, contractAddress=contract_address),
      method='GET',
      path='/isHolderOfContract',
      validate=validate,
      request_type=Request,
      response_type=IsHolderOfContractResponse,
    )
