# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_alchemy.core.rest import ChainNft


class SpamContractsResponse(TypedDict):
  contractAddresses: NotRequired[list[str]]
  """List of contract addresses marked as spam."""


class GetSpamContracts(ChainNft):
  """Returns all NFT contract addresses currently marked as spam by Alchemy. Requires Growth plan or higher."""

  async def get_spam_contracts(
    self,
    *,
    validate: bool | None = None,
  ) -> SpamContractsResponse:
    """Returns all NFT contract addresses currently marked as spam by Alchemy. Requires Growth plan or higher.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.alchemy.com/docs/reference/nft-api-endpoints/nft-api-endpoints/nft-spam-endpoints/get-spam-contracts-v-3)
    """
    return await self.request(
      None,
      method='GET',
      path='/getSpamContracts',
      validate=validate,
      response_type=SpamContractsResponse,
    )
