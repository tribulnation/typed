# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_alchemy.core.rest import ChainNft


class NftMetadataRefreshResponse(TypedDict):
  status: NotRequired[Literal['Queued']]
  """Refresh status. 'Queued' when successfully queued for refresh."""
  estimatedMsToRefresh: NotRequired[float]
  """Estimated milliseconds until the metadata refresh completes."""


class Request(TypedDict):
  contractAddress: str
  """Contract address of the token to refresh."""
  tokenId: str
  """Token ID of the token to refresh."""


class RefreshNftMetadata(ChainNft):
  """Queues a cache refresh for a specific NFT token's metadata."""

  async def refresh_nft_metadata(
    self,
    *,
    contract_address: str,
    token_id: str,
    validate: bool | None = None,
  ) -> NftMetadataRefreshResponse:
    """Queues a cache refresh for a specific NFT token's metadata.

    Args:
      contract_address: Contract address of the token to refresh.
      token_id: Token ID of the token to refresh.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.alchemy.com/docs/reference/nft-api-endpoints/nft-api-endpoints/nft-metadata-endpoints/refresh-nft-metadata-v-3)
    """
    return await self.request(
      Request(contractAddress=contract_address, tokenId=token_id),
      method='POST',
      path='/refreshNftMetadata',
      validate=validate,
      request_type=Request,
      response_type=NftMetadataRefreshResponse,
    )
