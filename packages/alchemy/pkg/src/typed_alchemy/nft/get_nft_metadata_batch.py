# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_alchemy.core.rest import ChainNft


class NftMetadataBatchResponse(TypedDict):
  nfts: NotRequired[list[dict[str, Any]]]
  """Array of NFT metadata objects."""


class NftMetadataBatchToken(TypedDict):
  contractAddress: str
  """NFT contract address."""
  tokenId: str
  """Token ID in hex or decimal format."""
  tokenType: NotRequired[Literal['ERC721', 'ERC1155']]
  """Token standard hint (ERC721 or ERC1155) to optimize query."""


class Request(TypedDict):
  tokens: list[NftMetadataBatchToken]
  """List of token objects to fetch metadata for. Maximum 100."""
  tokenUriTimeoutInMs: NotRequired[int]
  """Timeout for URI resolution in milliseconds."""
  refreshCache: NotRequired[bool]
  """If true, forces metadata re-fetch. Defaults to false."""


class GetNftMetadataBatch(ChainNft):
  """Fetches metadata for up to 100 NFTs in a single request. Returns an array of NFT objects."""

  async def get_nft_metadata_batch(
    self,
    tokens: list[NftMetadataBatchToken],
    *,
    token_uri_timeout_in_ms: int | None = None,
    refresh_cache: bool | None = None,
    validate: bool | None = None,
  ) -> NftMetadataBatchResponse:
    """Fetches metadata for up to 100 NFTs in a single request. Returns an array of NFT objects.

    Args:
      tokens: List of token objects to fetch metadata for. Maximum 100.
      token_uri_timeout_in_ms: Timeout for URI resolution in milliseconds.
      refresh_cache: If true, forces metadata re-fetch. Defaults to false.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.alchemy.com/docs/reference/nft-api-endpoints/nft-api-endpoints/nft-metadata-endpoints/get-nft-metadata-batch-v-3)
    """
    request: Request = Request(tokens=tokens)
    if token_uri_timeout_in_ms is not None:
      request['tokenUriTimeoutInMs'] = token_uri_timeout_in_ms
    if refresh_cache is not None:
      request['refreshCache'] = refresh_cache
    return await self.request(
      request,
      method='POST',
      path='/getNFTMetadataBatch',
      validate=validate,
      request_type=Request,
      response_type=NftMetadataBatchResponse,
    )
