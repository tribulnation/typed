# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_alchemy.core.rest import ChainNft


class IsSpamContractResponse(TypedDict):
  isSpamContract: NotRequired[bool]
  """True if the contract is marked as spam, false if it is considered valid."""


class Request(TypedDict):
  contractAddress: str
  """NFT contract address (ERC721 or ERC1155)."""


class IsSpamContract(ChainNft):
  """Checks whether a given NFT contract is classified as spam by Alchemy."""

  async def is_spam_contract(
    self,
    contract_address: str,
    *,
    validate: bool | None = None,
  ) -> IsSpamContractResponse:
    """Checks whether a given NFT contract is classified as spam by Alchemy.

    Args:
      contract_address: NFT contract address (ERC721 or ERC1155).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.alchemy.com/docs/reference/nft-api-endpoints/nft-api-endpoints/nft-spam-endpoints/is-spam-contract-v-3)
    """
    return await self.request(
      Request(contractAddress=contract_address),
      method='GET',
      path='/isSpamContract',
      validate=validate,
      request_type=Request,
      response_type=IsSpamContractResponse,
    )
