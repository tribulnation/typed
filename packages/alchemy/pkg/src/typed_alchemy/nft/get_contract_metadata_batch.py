# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, NotRequired, TypedDict
from typed_alchemy.core.rest import ChainNft


class ContractMetadataBatchResponse(TypedDict):
  contracts: NotRequired[list[dict[str, Any]]]
  """Array of contract metadata objects, one per input address."""


class Request(TypedDict):
  contractAddresses: list[str]
  """List of NFT contract addresses."""


class GetContractMetadataBatch(ChainNft):
  """Fetches collection-level metadata for multiple NFT contracts in a single request."""

  async def get_contract_metadata_batch(
    self,
    contract_addresses: list[str],
    *,
    validate: bool | None = None,
  ) -> ContractMetadataBatchResponse:
    """Fetches collection-level metadata for multiple NFT contracts in a single request.

    Args:
      contract_addresses: List of NFT contract addresses.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.alchemy.com/docs/reference/nft-api-endpoints/nft-api-endpoints/nft-metadata-endpoints/get-contract-metadata-batch-v-3)
    """
    return await self.request(
      Request(contractAddresses=contract_addresses),
      method='POST',
      path='/getContractMetadataBatch',
      validate=validate,
      request_type=Request,
      response_type=ContractMetadataBatchResponse,
    )
