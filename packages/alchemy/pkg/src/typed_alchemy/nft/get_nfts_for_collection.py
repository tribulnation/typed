# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Any, NotRequired, TypedDict
from typed_alchemy.core.rest import ChainNft


class NftsForCollectionResponse(TypedDict):
  nfts: NotRequired[list[dict[str, Any]]]
  """Array of NFT objects in the collection."""
  pageKey: NotRequired[str]
  """Token ID offset for fetching the next page."""
  nextToken: NotRequired[str]
  """Documented pagination token name for fetching the next page."""


class Request(TypedDict):
  contractAddress: NotRequired[str]
  """NFT contract address (ERC721 or ERC1155)."""
  collectionSlug: NotRequired[str]
  """OpenSea collection slug."""
  withMetadata: NotRequired[bool]
  """Include NFT metadata. Defaults to true."""
  startToken: NotRequired[str]
  """Token ID offset for pagination (hex or decimal)."""
  limit: NotRequired[int]
  """Number of NFTs to return. Defaults to 100."""
  tokenUriTimeoutInMs: NotRequired[int]
  """Timeout for metadata fetching in milliseconds. Set to 0 for cache-only."""


class GetNftsForCollection(ChainNft):
  """Retrieves NFTs associated with a specific NFT collection."""

  def get_nfts_for_collection_paged(
    self,
    *,
    contract_address: str | None = None,
    collection_slug: str | None = None,
    with_metadata: bool | None = None,
    limit: int | None = None,
    token_uri_timeout_in_ms: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[dict[str, Any], str]:
    """Retrieves NFTs associated with a specific NFT collection.

    Paged variant of `get_nfts_for_collection`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      contract_address: NFT contract address (ERC721 or ERC1155).
      collection_slug: OpenSea collection slug.
      with_metadata: Include NFT metadata. Defaults to true.
      limit: Number of NFTs to return. Defaults to 100.
      token_uri_timeout_in_ms: Timeout for metadata fetching in milliseconds. Set to 0 for cache-only.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.alchemy.com/docs/reference/nft-api-endpoints/nft-api-endpoints/nft-metadata-endpoints/get-nf-ts-for-collection-v-3)
    """

    async def next(start_token: str) -> tuple[list[dict[str, Any]], str | None]:
      response = await self.get_nfts_for_collection(
        contract_address=contract_address,
        collection_slug=collection_slug,
        with_metadata=with_metadata,
        limit=limit,
        token_uri_timeout_in_ms=token_uri_timeout_in_ms,
        start_token=(start_token or None),
        validate=validate,
      )
      rows = response.get('nfts')
      state = response.get('pageKey')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def get_nfts_for_collection(
    self,
    *,
    contract_address: str | None = None,
    collection_slug: str | None = None,
    with_metadata: bool | None = None,
    start_token: str | None = None,
    limit: int | None = None,
    token_uri_timeout_in_ms: int | None = None,
    validate: bool | None = None,
  ) -> NftsForCollectionResponse:
    """Retrieves NFTs associated with a specific NFT collection.

    Args:
      contract_address: NFT contract address (ERC721 or ERC1155).
      collection_slug: OpenSea collection slug.
      with_metadata: Include NFT metadata. Defaults to true.
      start_token: Token ID offset for pagination (hex or decimal).
      limit: Number of NFTs to return. Defaults to 100.
      token_uri_timeout_in_ms: Timeout for metadata fetching in milliseconds. Set to 0 for cache-only.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.alchemy.com/docs/reference/nft-api-endpoints/nft-api-endpoints/nft-metadata-endpoints/get-nf-ts-for-collection-v-3)
    """
    request: Request = Request()
    if contract_address is not None:
      request['contractAddress'] = contract_address
    if collection_slug is not None:
      request['collectionSlug'] = collection_slug
    if with_metadata is not None:
      request['withMetadata'] = with_metadata
    if start_token is not None:
      request['startToken'] = start_token
    if limit is not None:
      request['limit'] = limit
    if token_uri_timeout_in_ms is not None:
      request['tokenUriTimeoutInMs'] = token_uri_timeout_in_ms
    return await self.request(
      request,
      method='GET',
      path='/getNFTsForCollection',
      validate=validate,
      request_type=Request,
      response_type=NftsForCollectionResponse,
    )
