# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_alchemy.core.rest import ChainNft


class Request(TypedDict):
  address: str
  """Contract address to report."""
  isSpam: bool
  """Whether to mark the address as spam (true) or clear the spam flag (false)."""


SpamReportMessage = str


class ReportSpam(ChainNft):
  """Reports a contract address as spam to Alchemy's spam classification system."""

  async def report_spam(
    self,
    address: str,
    *,
    is_spam: bool,
    validate: bool | None = None,
  ) -> SpamReportMessage:
    """Reports a contract address as spam to Alchemy's spam classification system.

    Args:
      address: Contract address to report.
      is_spam: Whether to mark the address as spam (true) or clear the spam flag (false).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.alchemy.com/docs/reference/nft-api-endpoints/nft-api-endpoints/nft-spam-endpoints/report-spam-v-3)
    """
    return await self.request(
      Request(address=address, isSpam=is_spam),
      method='GET',
      path='/reportSpam',
      validate=validate,
      request_type=Request,
      response_type=SpamReportMessage,
    )
