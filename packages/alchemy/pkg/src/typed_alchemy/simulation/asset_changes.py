# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_alchemy.core.rpc import ChainRpc
from typed_alchemy.schemas import AssetChangesResult, SimulationTransaction


class Request(TypedDict):
  transaction: SimulationTransaction


class AssetChanges(ChainRpc):
  """Simulates a transaction and returns a list of asset changes (token transfers and approvals) without broadcasting via the `alchemy_simulateAssetChanges` JSON-RPC method."""

  async def asset_changes(
    self,
    transaction: SimulationTransaction,
    *,
    validate: bool | None = None,
  ) -> AssetChangesResult:
    """Simulates a transaction and returns a list of asset changes (token transfers and approvals) without broadcasting via the `alchemy_simulateAssetChanges` JSON-RPC method.

    Args:
      transaction:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.alchemy.com/docs/reference/simulation-asset-changes)
    """
    return await self.request(
      Request(transaction=transaction),
      method='POST',
      path='alchemy_simulateAssetChanges',
      validate=validate,
      request_type=Request,
      response_type=AssetChangesResult,
    )
