# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_alchemy.core.rpc import ChainRpc
from typed_alchemy.schemas import AssetChangesResult, SimulationTransaction


AssetChangesResults = list[AssetChangesResult]


class Request(TypedDict):
  transactions: list[SimulationTransaction]
  """Array of 1–3 unsigned transaction objects to simulate sequentially."""


class AssetChangesBundle(ChainRpc):
  """Simulates up to 3 sequential transactions and returns asset changes for each without broadcasting via the `alchemy_simulateAssetChangesBundle` JSON-RPC method."""

  async def asset_changes_bundle(
    self,
    transactions: list[SimulationTransaction],
    *,
    validate: bool | None = None,
  ) -> AssetChangesResults:
    """Simulates up to 3 sequential transactions and returns asset changes for each without broadcasting via the `alchemy_simulateAssetChangesBundle` JSON-RPC method.

    Args:
      transactions: Array of 1–3 unsigned transaction objects to simulate sequentially.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.alchemy.com/docs/reference/simulation-bundle)
    """
    return await self.request(
      Request(transactions=transactions),
      method='POST',
      path='alchemy_simulateAssetChangesBundle',
      validate=validate,
      request_type=Request,
      response_type=AssetChangesResults,
    )
