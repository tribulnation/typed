# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_alchemy.core.rpc import ChainRpc
from typed_alchemy.schemas import ExecutionResult, SimulationCallTransaction


ExecutionResults = list[ExecutionResult]


class Request(TypedDict):
  transactions: list[SimulationCallTransaction]
  """Array of 1–3 unsigned transaction objects to simulate sequentially."""


class ExecutionBundle(ChainRpc):
  """Simulates up to 3 sequential transactions and returns execution traces for each without broadcasting via the `alchemy_simulateExecutionBundle` JSON-RPC method."""

  async def execution_bundle(
    self,
    transactions: list[SimulationCallTransaction],
    *,
    validate: bool | None = None,
  ) -> ExecutionResults:
    """Simulates up to 3 sequential transactions and returns execution traces for each without broadcasting via the `alchemy_simulateExecutionBundle` JSON-RPC method.

    Args:
      transactions: Array of 1–3 unsigned transaction objects to simulate sequentially.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.alchemy.com/docs/reference/simulation-bundle)
    """
    return await self.request(
      Request(transactions=transactions),
      method='POST',
      path='alchemy_simulateExecutionBundle',
      validate=validate,
      request_type=Request,
      response_type=ExecutionResults,
    )
