# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_alchemy.core.rpc import ChainRpc
from typed_alchemy.schemas import ExecutionResult, SimulationCallTransaction


class Request(TypedDict):
  transaction: SimulationCallTransaction


class Execution(ChainRpc):
  """Simulates a transaction and returns decoded execution traces and event logs without broadcasting via the `alchemy_simulateExecution` JSON-RPC method."""

  async def execution(
    self,
    transaction: SimulationCallTransaction,
    *,
    validate: bool | None = None,
  ) -> ExecutionResult:
    """Simulates a transaction and returns decoded execution traces and event logs without broadcasting via the `alchemy_simulateExecution` JSON-RPC method.

    Args:
      transaction:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.alchemy.com/docs/reference/simulation-execution)
    """
    return await self.request(
      Request(transaction=transaction),
      method='POST',
      path='alchemy_simulateExecution',
      validate=validate,
      request_type=Request,
      response_type=ExecutionResult,
    )
