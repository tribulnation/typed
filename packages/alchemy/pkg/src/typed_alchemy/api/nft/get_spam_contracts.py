# Generated by typed-dev — see CONTRIBUTING.md.
"""Spam contracts list."""

from typing_extensions import NotRequired, TypedDict
from dataclasses import dataclass

from typed_core.validation import validator

from typed_alchemy.core.endpoint.rpc import RestEndpoint


class SpamContractsResponse(TypedDict):
  contractAddresses: NotRequired[list[str]]
  """List of contract addresses marked as spam."""


get_spam_contracts_validator = validator(SpamContractsResponse)


@dataclass(kw_only=True, frozen=True)
class GetSpamContracts(RestEndpoint):
  """Spam contracts list."""

  async def get_spam_contracts(
    self,
    *,
    validate: bool | None = None,
  ) -> SpamContractsResponse:
    """Returns all NFT contract addresses currently marked as spam by Alchemy. Requires Growth plan or higher.

    Args:
      validate: Validation override for this request.

    References:
      - [Alchemy API docs](https://www.alchemy.com/docs/reference/nft-api-endpoints/nft-api-endpoints/nft-spam-endpoints/get-spam-contracts-v-3)
    """
    return await self.request(
      'GET',
      '/getSpamContracts',
      validator=get_spam_contracts_validator,
      validate=validate,
    )
