# Generated by typed-dev — see CONTRIBUTING.md.
"""Floor price."""

from typing_extensions import NotRequired, TypedDict
from dataclasses import dataclass

from typed_core.validation import validator

from typed_alchemy.core.endpoint.rpc import RestEndpoint
from typed_alchemy.api.schemas import MarketplaceFloorPrice


class NftFloorPrice(TypedDict):
  """Object with one key per supported marketplace."""

  openSea: NotRequired[MarketplaceFloorPrice]
  looksRare: NotRequired[MarketplaceFloorPrice]


get_floor_price_validator = validator(NftFloorPrice)


@dataclass(kw_only=True, frozen=True)
class GetFloorPrice(RestEndpoint):
  """Floor price."""

  async def get_floor_price(
    self,
    *,
    contract_address: str,
    collection_slug: str | None = None,
    validate: bool | None = None,
  ) -> NftFloorPrice:
    """Retrieves the floor price of an NFT collection on OpenSea and LooksRare marketplaces. Ethereum Mainnet only.

    Args:
      contract_address: NFT contract address (ERC721 or ERC1155).
      collection_slug: OpenSea collection slug for the collection.
      validate: Validation override for this request.

    References:
      - [Alchemy API docs](https://www.alchemy.com/docs/reference/nft-api-endpoints/nft-api-endpoints/nft-sales-endpoints/get-floor-price-v-3)
    """
    params: dict = {
      'contractAddress': contract_address,
    }
    if collection_slug is not None:
      params['collectionSlug'] = collection_slug
    return await self.request(
      'GET',
      '/getFloorPrice',
      params=params,
      validator=get_floor_price_validator,
      validate=validate,
    )
