# Generated by typed-dev — see CONTRIBUTING.md.
"""Refresh NFT metadata."""

from typing_extensions import NotRequired, TypedDict
from dataclasses import dataclass

from typed_core.validation import validator

from typed_alchemy.core.endpoint.rpc import RestEndpoint


class NftMetadataRefreshRequest(TypedDict):
  contractAddress: str
  """Contract address of the token to refresh."""
  tokenId: str
  """Token ID of the token to refresh."""


class NftMetadataRefreshResponse(TypedDict):
  status: NotRequired[str]
  """Returns 'Queued' when successfully queued for refresh."""
  estimatedMsToRefresh: NotRequired[float]
  """Estimated milliseconds until the metadata refresh completes."""


refresh_nft_metadata_validator = validator(NftMetadataRefreshResponse)


@dataclass(kw_only=True, frozen=True)
class RefreshNftMetadata(RestEndpoint):
  """Refresh NFT metadata."""

  async def refresh_nft_metadata(
    self,
    request: NftMetadataRefreshRequest,
    *,
    validate: bool | None = None,
  ) -> NftMetadataRefreshResponse:
    """Queues a cache refresh for a specific NFT token's metadata.

    Args:
      request: Token to refresh.
      validate: Validation override for this request.

    References:
      - [Alchemy API docs](https://www.alchemy.com/docs/reference/nft-api-endpoints/nft-api-endpoints/nft-metadata-endpoints/refresh-nft-metadata-v-3)
    """
    return await self.request(
      'POST',
      '/refreshNftMetadata',
      json=request,
      validator=refresh_nft_metadata_validator,
      validate=validate,
    )
