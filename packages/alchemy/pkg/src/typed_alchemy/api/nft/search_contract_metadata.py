# Generated by typed-dev — see CONTRIBUTING.md.
"""Search contract metadata."""

from typing_extensions import Any, NotRequired, TypedDict
from dataclasses import dataclass

from typed_core.validation import validator

from typed_alchemy.core.endpoint.rpc import RestEndpoint


class SearchContractMetadataResponse(TypedDict):
  contracts: NotRequired[list[dict[str, Any]]]
  """Array of matching contract metadata objects."""


search_contract_metadata_validator = validator(SearchContractMetadataResponse)


@dataclass(kw_only=True, frozen=True)
class SearchContractMetadata(RestEndpoint):
  """Search contract metadata."""

  async def search_contract_metadata(
    self,
    *,
    query: str,
    validate: bool | None = None,
  ) -> SearchContractMetadataResponse:
    """Searches contract metadata across ERC-721 and ERC-1155 contracts for a given keyword. Beta.

    Args:
      query: Search keyword to match against contract metadata.
      validate: Validation override for this request.

    References:
      - [Alchemy API docs](https://www.alchemy.com/docs/reference/nft-api-endpoints/nft-api-endpoints/nft-metadata-endpoints/search-contract-metadata-v-3)
    """
    params: dict = {
      'query': query,
    }
    return await self.request(
      'GET',
      '/searchContractMetadata',
      params=params,
      validator=search_contract_metadata_validator,
      validate=validate,
    )
