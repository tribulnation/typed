# Generated by typed-dev — see CONTRIBUTING.md.
"""Get token allowance."""

from typing_extensions import TypedDict
from dataclasses import dataclass

from typed_core.validation import validator

from typed_alchemy.core.endpoint.rpc import RpcEndpoint


class TokenAllowanceRequest(TypedDict):
  contract: str
  """ERC-20 token contract address."""
  owner: str
  """Wallet address that owns the tokens."""
  spender: str
  """Address approved to spend tokens from the owner."""


get_token_allowance_validator = validator(str)


@dataclass(kw_only=True, frozen=True)
class GetTokenAllowance(RpcEndpoint):
  """Get token allowance."""

  async def get_token_allowance(
    self,
    request: TokenAllowanceRequest,
    *,
    validate: bool | None = None,
  ) -> str:
    """Returns the ERC-20 token allowance for a spender and owner via the `alchemy_getTokenAllowance` JSON-RPC method.

    Args:
      request: Positional params: [{contract, owner, spender}].
      validate: Validation override for this request.

    References:
      - [Alchemy API docs](https://www.alchemy.com/docs/data/token-api/token-api-endpoints/alchemy-get-token-allowance)
    """
    return await self.rpc(
      'alchemy_getTokenAllowance',
      request,
      validator=get_token_allowance_validator,
      validate=validate,
    )
