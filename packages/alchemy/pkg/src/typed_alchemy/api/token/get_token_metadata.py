# Generated by typed-dev — see CONTRIBUTING.md.
"""Get token metadata."""

from dataclasses import dataclass

from typed_core.validation import validator

from typed_alchemy.core.endpoint.rpc import RpcEndpoint
from typed_alchemy.api.schemas import TokenMetadata


get_token_metadata_validator = validator(TokenMetadata)


@dataclass(kw_only=True, frozen=True)
class GetTokenMetadata(RpcEndpoint):
  """Get token metadata."""

  async def get_token_metadata(
    self,
    contract_address: str,
    *,
    validate: bool | None = None,
  ) -> TokenMetadata:
    """Returns name, symbol, decimals, and logo for a token contract via the `alchemy_getTokenMetadata` JSON-RPC method.

    Args:
      contract_address: Positional params: [contractAddress].
      validate: Validation override for this request.

    References:
      - [Alchemy API docs](https://www.alchemy.com/docs/data/token-api/token-api-endpoints/alchemy-get-token-metadata)
    """
    return await self.rpc(
      'alchemy_getTokenMetadata',
      contract_address,
      validator=get_token_metadata_validator,
      validate=validate,
    )
