# Generated by typed-dev — see CONTRIBUTING.md.
"""Simulate asset changes bundle."""

from dataclasses import dataclass

from typed_core.validation import validator

from typed_alchemy.core.endpoint.rpc import RpcEndpoint
from typed_alchemy.api.schemas import AssetChangesResult, SimulationTransaction


asset_changes_bundle_validator = validator(list[AssetChangesResult])


@dataclass(kw_only=True, frozen=True)
class AssetChangesBundle(RpcEndpoint):
  """Simulate asset changes bundle."""

  async def asset_changes_bundle(
    self,
    simulation_transactions: list[SimulationTransaction],
    *,
    validate: bool | None = None,
  ) -> list[AssetChangesResult]:
    """Simulates up to 3 sequential transactions and returns asset changes for each without broadcasting via the `alchemy_simulateAssetChangesBundle` JSON-RPC method.

    Args:
      simulation_transactions: Array of unsigned transactions to simulate sequentially.
      validate: Validation override for this request.

    References:
      - [Alchemy API docs](https://www.alchemy.com/docs/reference/simulation-bundle)
    """
    return await self.rpc(
      'alchemy_simulateAssetChangesBundle',
      simulation_transactions,
      validator=asset_changes_bundle_validator,
      validate=validate,
    )
