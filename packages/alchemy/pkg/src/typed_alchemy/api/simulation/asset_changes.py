# Generated by typed-dev — see CONTRIBUTING.md.
"""Simulate asset changes."""

from dataclasses import dataclass

from typed_core.validation import validator

from typed_alchemy.core.endpoint.rpc import RpcEndpoint
from typed_alchemy.api.schemas import AssetChangesResult, SimulationTransaction


asset_changes_validator = validator(AssetChangesResult)


@dataclass(kw_only=True, frozen=True)
class AssetChanges(RpcEndpoint):
  """Simulate asset changes."""

  async def asset_changes(
    self,
    request: SimulationTransaction,
    *,
    validate: bool | None = None,
  ) -> AssetChangesResult:
    """Simulates a transaction and returns a list of asset changes (token transfers and approvals) without broadcasting via the `alchemy_simulateAssetChanges` JSON-RPC method.

    Args:
      request: Unsigned transaction to simulate.
      validate: Validation override for this request.

    References:
      - [Alchemy API docs](https://www.alchemy.com/docs/reference/simulation-asset-changes)
    """
    return await self.rpc(
      'alchemy_simulateAssetChanges',
      request,
      validator=asset_changes_validator,
      validate=validate,
    )
