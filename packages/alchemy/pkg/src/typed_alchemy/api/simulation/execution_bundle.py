# Generated by typed-dev — see CONTRIBUTING.md.
"""Simulate execution bundle."""

from dataclasses import dataclass

from typed_core.validation import validator

from typed_alchemy.core.endpoint.rpc import RpcEndpoint
from typed_alchemy.api.schemas import ExecutionResult, SimulationCallTransaction


execution_bundle_validator = validator(list[ExecutionResult])


@dataclass(kw_only=True, frozen=True)
class ExecutionBundle(RpcEndpoint):
  """Simulate execution bundle."""

  async def execution_bundle(
    self,
    simulation_call_transactions: list[SimulationCallTransaction],
    *,
    validate: bool | None = None,
  ) -> list[ExecutionResult]:
    """Simulates up to 3 sequential transactions and returns execution traces for each without broadcasting via the `alchemy_simulateExecutionBundle` JSON-RPC method.

    Args:
      simulation_call_transactions: Array of unsigned transactions to simulate sequentially.
      validate: Validation override for this request.

    References:
      - [Alchemy API docs](https://www.alchemy.com/docs/reference/simulation-bundle)
    """
    return await self.rpc(
      'alchemy_simulateExecutionBundle',
      simulation_call_transactions,
      validator=execution_bundle_validator,
      validate=validate,
    )
