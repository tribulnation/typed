# Generated by typed-dev — see CONTRIBUTING.md.
"""Simulate execution."""

from dataclasses import dataclass

from typed_core.validation import validator

from typed_alchemy.core.endpoint.rpc import RpcEndpoint
from typed_alchemy.api.schemas import ExecutionResult, SimulationCallTransaction


execution_validator = validator(ExecutionResult)


@dataclass(kw_only=True, frozen=True)
class Execution(RpcEndpoint):
  """Simulate execution."""

  async def execution(
    self,
    request: SimulationCallTransaction,
    *,
    validate: bool | None = None,
  ) -> ExecutionResult:
    """Simulates a transaction and returns decoded execution traces and event logs without broadcasting via the `alchemy_simulateExecution` JSON-RPC method.

    Args:
      request: Unsigned transaction to simulate.
      validate: Validation override for this request.

    References:
      - [Alchemy API docs](https://www.alchemy.com/docs/reference/simulation-execution)
    """
    return await self.rpc(
      'alchemy_simulateExecution',
      request,
      validator=execution_validator,
      validate=validate,
    )
