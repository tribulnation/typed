# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_alchemy.core.rest import PricesEndpoint
from typed_alchemy.schemas import TokenPriceEntry


class Request(TypedDict):
  symbols: list[str]
  """Array of token ticker symbols to fetch prices for. Maximum 25 symbols per request."""


class SymbolPriceResult(TypedDict):
  symbol: str
  """Token ticker symbol."""
  prices: list[TokenPriceEntry]
  """Array of price objects, one per currency."""
  error: NotRequired[str | None]
  """Error message if the symbol could not be resolved, otherwise null."""


class TokenPricesBySymbol(TypedDict):
  data: list[SymbolPriceResult]
  """Array of price results, one per requested symbol."""


class BySymbol(PricesEndpoint):
  """Fetches current prices for up to 25 tokens identified by their ticker symbols."""

  async def by_symbol(
    self,
    symbols: list[str],
    *,
    validate: bool | None = None,
  ) -> TokenPricesBySymbol:
    """Fetches current prices for up to 25 tokens identified by their ticker symbols.

    Args:
      symbols: Array of token ticker symbols to fetch prices for. Maximum 25 symbols per request.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.alchemy.com/docs/data/prices-api/prices-api-endpoints/prices-api-endpoints/get-token-prices-by-symbol)
    """
    return await self.request(
      Request(symbols=symbols),
      method='GET',
      path='/tokens/by-symbol',
      validate=validate,
      request_type=Request,
      response_type=TokenPricesBySymbol,
    )
