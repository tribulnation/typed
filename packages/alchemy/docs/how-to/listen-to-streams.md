# Streaming

Typed Alchemy currently implements HTTP and JSON-RPC Data API endpoints. It does
not expose WebSocket subscriptions.

Use the HTTP methods for prices, portfolio data, NFT metadata, transfers,
utility calls, and simulations.
