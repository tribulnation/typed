# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_lighter.schemas import SendTxResponse
from typed_lighter.tx.core import TxCore


class Request(TypedDict):
  api_private_key: str
  """Private key of the new API key, 40 bytes of hex (`0x` optional), e.g. from `client.signer.generate_api_key()`. It signs the registration locally and is never sent."""
  api_key_index: int
  """Slot being (re-)registered. The nonce is this slot's own (`0` for a slot never used before)."""
  nonce: NotRequired[int]
  """Explicit nonce of `api_key_index`'s slot, bypassing the client's nonce manager."""


class ChangeApiKey(TxCore):
  """Register a new API key in a slot (`L2ChangePubKey`, tx type 8): the new private key signs the registration locally (only its public key goes on the wire), the L1 wallet authorizes it, and it is submitted over HTTP `sendTx` or WebSocket `jsonapi/sendtx`. The slot needs no configured key: the client only needs the L1 wallet key and one configured API key (any slot, the new one included) to build it with."""

  async def change_api_key(
    self,
    api_private_key: str,
    *,
    api_key_index: int,
    nonce: int | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> SendTxResponse:
    """Register a new API key in a slot (`L2ChangePubKey`, tx type 8): the new private key signs the registration locally (only its public key goes on the wire), the L1 wallet authorizes it, and it is submitted over HTTP `sendTx` or WebSocket `jsonapi/sendtx`. The slot needs no configured key: the client only needs the L1 wallet key and one configured API key (any slot, the new one included) to build it with.

    Args:
      api_private_key: Private key of the new API key, 40 bytes of hex (`0x` optional), e.g. from `client.signer.generate_api_key()`. It signs the registration locally and is never sent.
      api_key_index: Slot being (re-)registered. The nonce is this slot's own (`0` for a slot never used before).
      nonce: Explicit nonce of `api_key_index`'s slot, bypassing the client's nonce manager.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://apidocs.lighter.xyz/docs/api-keys)
    """
    request: Request = Request(
      api_private_key=api_private_key, api_key_index=api_key_index
    )
    if nonce is not None:
      request['nonce'] = nonce
    return await self.request(
      request,
      method='POST',
      path='/api/v1/sendTx',
      meta={'tx_type': 8},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=SendTxResponse,
    )
