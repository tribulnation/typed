# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_lighter.schemas import SendTxResponse
from typed_lighter.tx.core import TxCore


class Request(TypedDict):
  staking_pool_index: int
  """Staking pool account (`systemConfig` `staking_pool_index`)."""
  share_amount: int
  """Shares to redeem."""
  api_key_index: NotRequired[int]
  """API key to sign with; defaults to round-robin over the configured keys."""
  nonce: NotRequired[int]
  """Explicit nonce of `api_key_index`, bypassing the client's nonce manager."""


class Unstake(TxCore):
  """Redeem shares of a staking pool (`L2UnstakeAssets`, tx type 36): signed locally, then submitted over HTTP `sendTx` or WebSocket `jsonapi/sendtx`. The venue allows one unstake per 15 seconds."""

  async def unstake(
    self,
    *,
    staking_pool_index: int,
    share_amount: int,
    api_key_index: int | None = None,
    nonce: int | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> SendTxResponse:
    """Redeem shares of a staking pool (`L2UnstakeAssets`, tx type 36): signed locally, then submitted over HTTP `sendTx` or WebSocket `jsonapi/sendtx`. The venue allows one unstake per 15 seconds.

    Args:
      staking_pool_index: Staking pool account (`systemConfig` `staking_pool_index`).
      share_amount: Shares to redeem.
      api_key_index: API key to sign with; defaults to round-robin over the configured keys.
      nonce: Explicit nonce of `api_key_index`, bypassing the client's nonce manager.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://apidocs.lighter.xyz/docs/manage-public-pools-shares)
    """
    request: Request = Request(
      staking_pool_index=staking_pool_index, share_amount=share_amount
    )
    if api_key_index is not None:
      request['api_key_index'] = api_key_index
    if nonce is not None:
      request['nonce'] = nonce
    return await self.request(
      request,
      method='POST',
      path='/api/v1/sendTx',
      meta={'tx_type': 36},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=SendTxResponse,
    )
