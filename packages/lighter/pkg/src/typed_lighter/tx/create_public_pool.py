# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_lighter.schemas import SendTxResponse
from typed_lighter.tx.core import TxCore


class Request(TypedDict):
  operator_fee: int
  """Operator fee in 1e-6 units (100_000 is 10%)."""
  initial_total_shares: int
  """Initial share supply, bought by the operator; one share is 0.001 USDC (1_000_000 shares is 1_000 USDC)."""
  min_operator_share_rate: int
  """Minimum share of the pool the operator must keep, in 1e-4 units (100 is 1%)."""
  api_key_index: NotRequired[int]
  """API key to sign with; defaults to round-robin over the configured keys."""
  nonce: NotRequired[int]
  """Explicit nonce of `api_key_index`, bypassing the client's nonce manager."""


class CreatePublicPool(TxCore):
  """Create a public pool operated by this master account (`L2CreatePublicPool`, tx type 10): signed locally, then submitted over HTTP `sendTx` or WebSocket `jsonapi/sendtx`. The pool is a new sub-account; the operator funds its initial shares."""

  async def create_public_pool(
    self,
    *,
    operator_fee: int,
    initial_total_shares: int,
    min_operator_share_rate: int,
    api_key_index: int | None = None,
    nonce: int | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> SendTxResponse:
    """Create a public pool operated by this master account (`L2CreatePublicPool`, tx type 10): signed locally, then submitted over HTTP `sendTx` or WebSocket `jsonapi/sendtx`. The pool is a new sub-account; the operator funds its initial shares.

    Args:
      operator_fee: Operator fee in 1e-6 units (100_000 is 10%).
      initial_total_shares: Initial share supply, bought by the operator; one share is 0.001 USDC (1_000_000 shares is 1_000 USDC).
      min_operator_share_rate: Minimum share of the pool the operator must keep, in 1e-4 units (100 is 1%).
      api_key_index: API key to sign with; defaults to round-robin over the configured keys.
      nonce: Explicit nonce of `api_key_index`, bypassing the client's nonce manager.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://apidocs.lighter.xyz/docs/manage-public-pools-shares)
    """
    request: Request = Request(
      operator_fee=operator_fee,
      initial_total_shares=initial_total_shares,
      min_operator_share_rate=min_operator_share_rate,
    )
    if api_key_index is not None:
      request['api_key_index'] = api_key_index
    if nonce is not None:
      request['nonce'] = nonce
    return await self.request(
      request,
      method='POST',
      path='/api/v1/sendTx',
      meta={'tx_type': 10},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=SendTxResponse,
    )
