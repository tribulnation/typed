# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_lighter.schemas import SendTxResponse
from typed_lighter.tx.core import TxCore


class Request(TypedDict):
  to_account_index: int
  """Receiving account."""
  amount: int
  """Amount in the asset's smallest unit (`assetDetails` `decimals`: 1e-6 for USDC, 1e-8 for ETH and LIT)."""
  asset_index: NotRequired[int]
  """Asset id (`assetDetails` `asset_id`); 3 is USDC. Defaults to USDC."""
  from_route: NotRequired[Literal['perps', 'spot']]
  """Side of this account the asset leaves. Defaults to `perps`."""
  to_route: NotRequired[Literal['perps', 'spot']]
  """Side of the receiving account the asset arrives on. Defaults to `perps`."""
  usdc_fee: NotRequired[int]
  """Transfer fee in 1e-6 USDC, as `transferFeeInfo` quotes it for this pair of accounts. Defaults to 0."""
  memo: NotRequired[str]
  """32 bytes of hex (`0x` optional), shown in the L1 message but not part of the L2 hash. Defaults to zeros."""
  api_key_index: NotRequired[int]
  """API key to sign with; defaults to round-robin over the configured keys."""
  nonce: NotRequired[int]
  """Explicit nonce of `api_key_index`, bypassing the client's nonce manager."""


class Transfer(TxCore):
  """Transfer an asset to another account (`L2Transfer`, tx type 12): signed locally, then submitted over HTTP `sendTx` or WebSocket `jsonapi/sendtx`. L1-signed when the client has the L1 wallet key; the venue requires it unless both accounts share a master account."""

  async def transfer(
    self,
    *,
    to_account_index: int,
    amount: int,
    asset_index: int | None = None,
    from_route: Literal['perps', 'spot'] | None = None,
    to_route: Literal['perps', 'spot'] | None = None,
    usdc_fee: int | None = None,
    memo: str | None = None,
    api_key_index: int | None = None,
    nonce: int | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> SendTxResponse:
    """Transfer an asset to another account (`L2Transfer`, tx type 12): signed locally, then submitted over HTTP `sendTx` or WebSocket `jsonapi/sendtx`. L1-signed when the client has the L1 wallet key; the venue requires it unless both accounts share a master account.

    Args:
      to_account_index: Receiving account.
      amount: Amount in the asset's smallest unit (`assetDetails` `decimals`: 1e-6 for USDC, 1e-8 for ETH and LIT).
      asset_index: Asset id (`assetDetails` `asset_id`); 3 is USDC. Defaults to USDC.
      from_route: Side of this account the asset leaves. Defaults to `perps`.
      to_route: Side of the receiving account the asset arrives on. Defaults to `perps`.
      usdc_fee: Transfer fee in 1e-6 USDC, as `transferFeeInfo` quotes it for this pair of accounts. Defaults to 0.
      memo: 32 bytes of hex (`0x` optional), shown in the L1 message but not part of the L2 hash. Defaults to zeros.
      api_key_index: API key to sign with; defaults to round-robin over the configured keys.
      nonce: Explicit nonce of `api_key_index`, bypassing the client's nonce manager.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://apidocs.lighter.xyz/docs/deposits-transfers-and-withdrawals)
    """
    request: Request = Request(to_account_index=to_account_index, amount=amount)
    if asset_index is not None:
      request['asset_index'] = asset_index
    if from_route is not None:
      request['from_route'] = from_route
    if to_route is not None:
      request['to_route'] = to_route
    if usdc_fee is not None:
      request['usdc_fee'] = usdc_fee
    if memo is not None:
      request['memo'] = memo
    if api_key_index is not None:
      request['api_key_index'] = api_key_index
    if nonce is not None:
      request['nonce'] = nonce
    return await self.request(
      request,
      method='POST',
      path='/api/v1/sendTx',
      meta={'tx_type': 12},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=SendTxResponse,
    )
