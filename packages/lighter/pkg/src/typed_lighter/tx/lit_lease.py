# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_lighter.schemas import TxHash
from typed_lighter.tx.core import TxCore


class Request(TypedDict):
  lease_amount: int
  """LIT to lease, in raw units (1 LIT = 100_000_000)."""
  duration_days: int
  """Lease duration in days; one of `leaseOptions`' `duration_days`."""
  fee_amount: int
  """The fee, in raw LIT: `lease_amount * (annual_rate * 100) * duration_days // (360 * 10_000)`, with `annual_rate` from `leaseOptions` for `duration_days`."""
  to_account_index: int
  """Account that collects the fee: `leaseOptions` `lit_incentives_account_index`."""
  usdc_fee: NotRequired[int]
  """Transfer fee in 1e-6 USDC, as `transferFeeInfo` quotes it. Defaults to 0."""
  api_key_index: NotRequired[int]
  """API key to sign with; defaults to round-robin over the configured keys."""
  nonce: NotRequired[int]
  """Explicit nonce of `api_key_index`, bypassing the client's nonce manager."""


class LitLease(TxCore):
  """Lease LIT for a number of days (`POST /api/v1/litLease`): the lease fee is paid as a signed, L1-signed LIT transfer (`L2Transfer`, tx type 12, spot to spot) to the incentives account, submitted together with the lease terms. Needs the L1 wallet key (`eth_private_key`) and an auth token besides the signature."""

  async def lit_lease(
    self,
    *,
    lease_amount: int,
    duration_days: int,
    fee_amount: int,
    to_account_index: int,
    usdc_fee: int | None = None,
    api_key_index: int | None = None,
    nonce: int | None = None,
    validate: bool | None = None,
  ) -> TxHash:
    """Lease LIT for a number of days (`POST /api/v1/litLease`): the lease fee is paid as a signed, L1-signed LIT transfer (`L2Transfer`, tx type 12, spot to spot) to the incentives account, submitted together with the lease terms. Needs the L1 wallet key (`eth_private_key`) and an auth token besides the signature.

    Args:
      lease_amount: LIT to lease, in raw units (1 LIT = 100_000_000).
      duration_days: Lease duration in days; one of `leaseOptions`' `duration_days`.
      fee_amount: The fee, in raw LIT: `lease_amount * (annual_rate * 100) * duration_days // (360 * 10_000)`, with `annual_rate` from `leaseOptions` for `duration_days`.
      to_account_index: Account that collects the fee: `leaseOptions` `lit_incentives_account_index`.
      usdc_fee: Transfer fee in 1e-6 USDC, as `transferFeeInfo` quotes it. Defaults to 0.
      api_key_index: API key to sign with; defaults to round-robin over the configured keys.
      nonce: Explicit nonce of `api_key_index`, bypassing the client's nonce manager.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/litlease)
    """
    request: Request = Request(
      lease_amount=lease_amount,
      duration_days=duration_days,
      fee_amount=fee_amount,
      to_account_index=to_account_index,
    )
    if usdc_fee is not None:
      request['usdc_fee'] = usdc_fee
    if api_key_index is not None:
      request['api_key_index'] = api_key_index
    if nonce is not None:
      request['nonce'] = nonce
    return await self.request(
      request,
      method='POST',
      path='/api/v1/litLease',
      meta={'tx_type': 12, 'submit': 'lit_lease'},
      validate=validate,
      request_type=Request,
      response_type=TxHash,
    )
