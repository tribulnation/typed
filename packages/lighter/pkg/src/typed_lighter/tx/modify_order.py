# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_lighter.schemas import SendTxResponse
from typed_lighter.tx.core import TxCore


class Request(TypedDict):
  market_index: int
  """Market id of the order."""
  order_index: int
  """The order's `order_index`, or its `client_order_index`."""
  base_amount: int
  """New order size in base units, scaled by the market's `supported_size_decimals`."""
  price: int
  """New price, scaled by the market's `supported_price_decimals`."""
  trigger_price: NotRequired[int]
  """New trigger price of a stop-loss/take-profit order, scaled like `price`. Defaults to 0 (none)."""
  order_version: NotRequired[int]
  """Only modify the order while it is still at this `order_version` (as the order payloads report it)."""
  self_trade_behavior: NotRequired[
    Literal['expire-maker', 'expire-taker', 'expire-both', 'reduce']
  ]
  """What happens when the modified order would match another order of the same owner. Defaults to `expire-maker`."""
  self_trade_equality: NotRequired[Literal['account', 'master']]
  """Whether "same owner" means the same account or the same master account. Defaults to `account`."""
  api_key_index: NotRequired[int]
  """API key to sign with; defaults to round-robin over the configured keys."""
  nonce: NotRequired[int]
  """Explicit nonce of `api_key_index`, bypassing the client's nonce manager."""


class ModifyOrder(TxCore):
  """Modify a resting order's size, price and trigger price (`L2ModifyOrder`, tx type 17): signed locally, then submitted over HTTP `sendTx` or WebSocket `jsonapi/sendtx`."""

  async def modify_order(
    self,
    *,
    market_index: int,
    order_index: int,
    base_amount: int,
    price: int,
    trigger_price: int | None = None,
    order_version: int | None = None,
    self_trade_behavior: Literal[
      'expire-maker', 'expire-taker', 'expire-both', 'reduce'
    ]
    | None = None,
    self_trade_equality: Literal['account', 'master'] | None = None,
    api_key_index: int | None = None,
    nonce: int | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> SendTxResponse:
    """Modify a resting order's size, price and trigger price (`L2ModifyOrder`, tx type 17): signed locally, then submitted over HTTP `sendTx` or WebSocket `jsonapi/sendtx`.

    Args:
      market_index: Market id of the order.
      order_index: The order's `order_index`, or its `client_order_index`.
      base_amount: New order size in base units, scaled by the market's `supported_size_decimals`.
      price: New price, scaled by the market's `supported_price_decimals`.
      trigger_price: New trigger price of a stop-loss/take-profit order, scaled like `price`. Defaults to 0 (none).
      order_version: Only modify the order while it is still at this `order_version` (as the order payloads report it).
      self_trade_behavior: What happens when the modified order would match another order of the same owner. Defaults to `expire-maker`.
      self_trade_equality: Whether "same owner" means the same account or the same master account. Defaults to `account`.
      api_key_index: API key to sign with; defaults to round-robin over the configured keys.
      nonce: Explicit nonce of `api_key_index`, bypassing the client's nonce manager.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://apidocs.lighter.xyz/docs/trading)
    """
    request: Request = Request(
      market_index=market_index,
      order_index=order_index,
      base_amount=base_amount,
      price=price,
    )
    if trigger_price is not None:
      request['trigger_price'] = trigger_price
    if order_version is not None:
      request['order_version'] = order_version
    if self_trade_behavior is not None:
      request['self_trade_behavior'] = self_trade_behavior
    if self_trade_equality is not None:
      request['self_trade_equality'] = self_trade_equality
    if api_key_index is not None:
      request['api_key_index'] = api_key_index
    if nonce is not None:
      request['nonce'] = nonce
    return await self.request(
      request,
      method='POST',
      path='/api/v1/sendTx',
      meta={'tx_type': 17},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=SendTxResponse,
    )
