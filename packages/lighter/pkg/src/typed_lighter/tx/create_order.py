# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_lighter.core import TimestampMillis
from typed_lighter.schemas import SendTxResponse
from typed_lighter.tx.core import TxCore


class IocLimitOrder(TypedDict):
  """An immediate-or-cancel limit order: fills what it can at the limit price or better, then cancels the rest. It never rests, so it takes no expiry."""

  order_type: Literal['limit']
  """Order type."""
  market_index: int
  """Market id (`orderBooks` `market_id`)."""
  client_order_index: int
  """Caller-chosen order id (uint48), unique among the account's open orders; `cancel_order` accepts it in place of `order_index`."""
  base_amount: int
  """Order size in base units, scaled by the market's `supported_size_decimals`."""
  is_ask: bool
  """True to sell, false to buy."""
  price: int
  """Limit price, scaled by the market's `supported_price_decimals`."""
  time_in_force: Literal['immediate-or-cancel']
  """Time in force: immediate-or-cancel."""
  reduce_only: NotRequired[bool]
  """Only ever reduce the position. Defaults to false."""
  self_trade_behavior: NotRequired[
    Literal['expire-maker', 'expire-taker', 'expire-both', 'reduce']
  ]
  """What happens when the order would match another order of the same owner. Defaults to `expire-maker`."""
  self_trade_equality: NotRequired[Literal['account', 'master']]
  """Whether "same owner" means the same account or the same master account. Defaults to `account`."""


class LimitOrder(TypedDict):
  """A resting limit order (good-till-time or post-only), optionally with an expiry."""

  order_type: Literal['limit']
  """Order type."""
  market_index: int
  """Market id (`orderBooks` `market_id`)."""
  client_order_index: int
  """Caller-chosen order id (uint48), unique among the account's open orders; `cancel_order` accepts it in place of `order_index`."""
  base_amount: int
  """Order size in base units, scaled by the market's `supported_size_decimals`."""
  is_ask: bool
  """True to sell, false to buy."""
  price: int
  """Limit price, scaled by the market's `supported_price_decimals`."""
  time_in_force: Literal['good-till-time', 'post-only']
  """Time in force. `post-only` is rejected rather than taking liquidity."""
  order_expiry: NotRequired[TimestampMillis]
  """When the order expires, 5 minutes to 30 days out; defaults to the venue's 28 days."""
  reduce_only: NotRequired[bool]
  """Only ever reduce the position. Defaults to false."""
  self_trade_behavior: NotRequired[
    Literal['expire-maker', 'expire-taker', 'expire-both', 'reduce']
  ]
  """What happens when the order would match another order of the same owner. Defaults to `expire-maker`."""
  self_trade_equality: NotRequired[Literal['account', 'master']]
  """Whether "same owner" means the same account or the same master account. Defaults to `account`."""


class MarketOrder(TypedDict):
  """A market order: immediate-or-cancel, bounded by a worst acceptable price."""

  order_type: Literal['market']
  """Order type."""
  market_index: int
  """Market id (`orderBooks` `market_id`)."""
  client_order_index: int
  """Caller-chosen order id (uint48), unique among the account's open orders; `cancel_order` accepts it in place of `order_index`."""
  base_amount: int
  """Order size in base units, scaled by the market's `supported_size_decimals`."""
  is_ask: bool
  """True to sell, false to buy."""
  price: int
  """Worst acceptable execution price, scaled by the market's `supported_price_decimals`; the order is cancelled if it cannot fill at this price or better."""
  reduce_only: NotRequired[bool]
  """Only ever reduce the position. Defaults to false."""
  self_trade_behavior: NotRequired[
    Literal['expire-maker', 'expire-taker', 'expire-both', 'reduce']
  ]
  """What happens when the order would match another order of the same owner. Defaults to `expire-maker`."""
  self_trade_equality: NotRequired[Literal['account', 'master']]
  """Whether "same owner" means the same account or the same master account. Defaults to `account`."""


class StopLossLimitOrder(TypedDict):
  """A stop-loss that places a resting limit order once the trigger price is crossed."""

  order_type: Literal['stop-loss-limit']
  """Order type."""
  market_index: int
  """Market id (`orderBooks` `market_id`)."""
  client_order_index: int
  """Caller-chosen order id (uint48), unique among the account's open orders; `cancel_order` accepts it in place of `order_index`."""
  base_amount: int
  """Order size in base units, scaled by the market's `supported_size_decimals`."""
  is_ask: bool
  """True to sell, false to buy."""
  trigger_price: int
  """Trigger price, scaled by the market's `supported_price_decimals`; compared against the mark price."""
  price: int
  """Limit price, scaled by the market's `supported_price_decimals`."""
  order_expiry: NotRequired[TimestampMillis]
  """When the order expires, 5 minutes to 30 days out. Defaults to the venue's 28 days."""
  reduce_only: NotRequired[bool]
  """Only ever reduce the position. Defaults to false."""
  self_trade_behavior: NotRequired[
    Literal['expire-maker', 'expire-taker', 'expire-both', 'reduce']
  ]
  """What happens when the order would match another order of the same owner. Defaults to `expire-maker`."""
  self_trade_equality: NotRequired[Literal['account', 'master']]
  """Whether "same owner" means the same account or the same master account. Defaults to `account`."""


class StopLossOrder(TypedDict):
  """A stop-loss that sends a market order once the trigger price is crossed."""

  order_type: Literal['stop-loss']
  """Order type."""
  market_index: int
  """Market id (`orderBooks` `market_id`)."""
  client_order_index: int
  """Caller-chosen order id (uint48), unique among the account's open orders; `cancel_order` accepts it in place of `order_index`."""
  base_amount: int
  """Order size in base units, scaled by the market's `supported_size_decimals`."""
  is_ask: bool
  """True to sell, false to buy."""
  trigger_price: int
  """Trigger price, scaled by the market's `supported_price_decimals`; compared against the mark price."""
  price: int
  """Worst acceptable execution price, scaled by the market's `supported_price_decimals`; the order is cancelled if it cannot fill at this price or better."""
  order_expiry: NotRequired[TimestampMillis]
  """When the order expires, 5 minutes to 30 days out. Defaults to the venue's 28 days."""
  reduce_only: NotRequired[bool]
  """Only ever reduce the position. Defaults to false."""
  self_trade_behavior: NotRequired[
    Literal['expire-maker', 'expire-taker', 'expire-both', 'reduce']
  ]
  """What happens when the order would match another order of the same owner. Defaults to `expire-maker`."""
  self_trade_equality: NotRequired[Literal['account', 'master']]
  """Whether "same owner" means the same account or the same master account. Defaults to `account`."""


class TakeProfitLimitOrder(TypedDict):
  """A take-profit that places a resting limit order once the trigger price is crossed."""

  order_type: Literal['take-profit-limit']
  """Order type."""
  market_index: int
  """Market id (`orderBooks` `market_id`)."""
  client_order_index: int
  """Caller-chosen order id (uint48), unique among the account's open orders; `cancel_order` accepts it in place of `order_index`."""
  base_amount: int
  """Order size in base units, scaled by the market's `supported_size_decimals`."""
  is_ask: bool
  """True to sell, false to buy."""
  trigger_price: int
  """Trigger price, scaled by the market's `supported_price_decimals`; compared against the mark price."""
  price: int
  """Limit price, scaled by the market's `supported_price_decimals`."""
  order_expiry: NotRequired[TimestampMillis]
  """When the order expires, 5 minutes to 30 days out. Defaults to the venue's 28 days."""
  reduce_only: NotRequired[bool]
  """Only ever reduce the position. Defaults to false."""
  self_trade_behavior: NotRequired[
    Literal['expire-maker', 'expire-taker', 'expire-both', 'reduce']
  ]
  """What happens when the order would match another order of the same owner. Defaults to `expire-maker`."""
  self_trade_equality: NotRequired[Literal['account', 'master']]
  """Whether "same owner" means the same account or the same master account. Defaults to `account`."""


class TakeProfitOrder(TypedDict):
  """A take-profit that sends a market order once the trigger price is crossed."""

  order_type: Literal['take-profit']
  """Order type."""
  market_index: int
  """Market id (`orderBooks` `market_id`)."""
  client_order_index: int
  """Caller-chosen order id (uint48), unique among the account's open orders; `cancel_order` accepts it in place of `order_index`."""
  base_amount: int
  """Order size in base units, scaled by the market's `supported_size_decimals`."""
  is_ask: bool
  """True to sell, false to buy."""
  trigger_price: int
  """Trigger price, scaled by the market's `supported_price_decimals`; compared against the mark price."""
  price: int
  """Worst acceptable execution price, scaled by the market's `supported_price_decimals`; the order is cancelled if it cannot fill at this price or better."""
  order_expiry: NotRequired[TimestampMillis]
  """When the order expires, 5 minutes to 30 days out. Defaults to the venue's 28 days."""
  reduce_only: NotRequired[bool]
  """Only ever reduce the position. Defaults to false."""
  self_trade_behavior: NotRequired[
    Literal['expire-maker', 'expire-taker', 'expire-both', 'reduce']
  ]
  """What happens when the order would match another order of the same owner. Defaults to `expire-maker`."""
  self_trade_equality: NotRequired[Literal['account', 'master']]
  """Whether "same owner" means the same account or the same master account. Defaults to `account`."""


class Request(TypedDict):
  order: (
    LimitOrder
    | IocLimitOrder
    | MarketOrder
    | StopLossOrder
    | StopLossLimitOrder
    | TakeProfitOrder
    | TakeProfitLimitOrder
  )
  """The order to place, by order type."""
  api_key_index: NotRequired[int]
  """API key to sign with; defaults to round-robin over the configured keys."""
  nonce: NotRequired[int]
  """Explicit nonce of `api_key_index`, bypassing the client's nonce manager."""


class CreateOrder(TxCore):
  """Place an order (`L2CreateOrder`, tx type 14): signed locally, then submitted over HTTP `sendTx` or WebSocket `jsonapi/sendtx`. Prices and sizes are scaled integers. Acceptance only means the API server took the transaction; follow the account's order stream for the outcome."""

  async def create_order(
    self,
    order: LimitOrder
    | IocLimitOrder
    | MarketOrder
    | StopLossOrder
    | StopLossLimitOrder
    | TakeProfitOrder
    | TakeProfitLimitOrder,
    *,
    api_key_index: int | None = None,
    nonce: int | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> SendTxResponse:
    """Place an order (`L2CreateOrder`, tx type 14): signed locally, then submitted over HTTP `sendTx` or WebSocket `jsonapi/sendtx`. Prices and sizes are scaled integers. Acceptance only means the API server took the transaction; follow the account's order stream for the outcome.

    Args:
      order: The order to place, by order type.
      api_key_index: API key to sign with; defaults to round-robin over the configured keys.
      nonce: Explicit nonce of `api_key_index`, bypassing the client's nonce manager.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://apidocs.lighter.xyz/docs/trading)
    """
    request: Request = Request(order=order)
    if api_key_index is not None:
      request['api_key_index'] = api_key_index
    if nonce is not None:
      request['nonce'] = nonce
    return await self.request(
      request,
      method='POST',
      path='/api/v1/sendTx',
      meta={'tx_type': 14},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=SendTxResponse,
    )
