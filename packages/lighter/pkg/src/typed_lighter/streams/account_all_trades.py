# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_lighter.core.endpoint.stream import StreamEndpoint
from typed_lighter.schemas import Trade


class AccountAllTradesSnapshot(TypedDict):
  """The `subscribed/account_all_trades` snapshot."""

  type: Literal['subscribed/account_all_trades']
  """The snapshot sent on subscribe."""
  channel: str
  """The subscribed channel in colon form (`account_all_trades:476`)."""
  trades: dict[str, list[Trade]]
  """Trades by market id (as a string)."""
  daily_volume: float
  """Traded volume over the last day, in USDC."""
  weekly_volume: float
  """Traded volume over the last week, in USDC."""
  monthly_volume: float
  """Traded volume over the last month, in USDC."""
  total_volume: float
  """Traded volume since the account was created, in USDC."""


class AccountAllTradesUpdate(TypedDict):
  """An `update/account_all_trades` push."""

  type: Literal['update/account_all_trades']
  """A push after the snapshot."""
  channel: str
  """The subscribed channel in colon form (`account_all_trades:476`)."""
  trades: dict[str, list[Trade]]
  """Trades by market id (as a string)."""


AccountAllTradesMessage = AccountAllTradesSnapshot | AccountAllTradesUpdate


class AccountAllTrades(StreamEndpoint):
  """Stream an account's trades on every market, with its volume counters."""

  def account_all_trades(
    self,
    account_index: int,
    *,
    validate: bool | None = None,
  ) -> StreamManager[AccountAllTradesMessage, Any, Any]:
    """Stream an account's trades on every market, with its volume counters.

    Args:
      account_index: Account index to follow.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/docs/websocket-reference#account-all-trades)
    """
    return self.subscribe(
      f'account_all_trades/{account_index}',
      meta={},
      validate=validate,
      response_type=AccountAllTradesMessage,
    )
