# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_lighter.core import TimestampMillis
from typed_lighter.core.endpoint.stream import StreamEndpoint


class CrossStats(TypedDict):
  """Cross-margin figures."""

  collateral: Decimal
  """Collateral, in USDC."""
  portfolio_value: Decimal
  """Portfolio value, in USDC."""
  leverage: Decimal
  """Leverage."""
  available_balance: Decimal
  """Available balance, in USDC."""
  margin_usage: Decimal
  """Margin usage, in percent."""
  buying_power: Decimal
  """Buying power, in USDC."""


class TotalStats(TypedDict):
  """Whole-account figures."""

  collateral: Decimal
  """Collateral, in USDC."""
  portfolio_value: Decimal
  """Portfolio value, in USDC."""
  leverage: Decimal
  """Leverage."""
  available_balance: Decimal
  """Available balance, in USDC."""
  margin_usage: Decimal
  """Margin usage, in percent."""
  buying_power: Decimal
  """Buying power, in USDC."""


class UserStats(TypedDict):
  """Account statistics; the top-level figures equal `total_stats`."""

  collateral: Decimal
  """Collateral, in USDC."""
  portfolio_value: Decimal
  """Portfolio value, in USDC."""
  leverage: Decimal
  """Leverage."""
  available_balance: Decimal
  """Available balance, in USDC."""
  margin_usage: Decimal
  """Margin usage, in percent."""
  buying_power: Decimal
  """Buying power, in USDC."""
  account_trading_mode: Literal[0, 1]
  """Account trading mode: `0` classic, `1` unified."""
  cross_stats: CrossStats
  total_stats: TotalStats


class UserStatsUpdate(TypedDict):
  """One `user_stats` frame."""

  type: Literal['subscribed/user_stats', 'update/user_stats']
  """`subscribed/...` for the snapshot sent on subscribe, `update/...` for every later push."""
  channel: str
  """The subscribed channel in colon form (`user_stats:476`)."""
  stats: UserStats
  timestamp: TimestampMillis
  """Server time the frame was sent."""


class UserStatsEndpoint(StreamEndpoint):
  """Stream an account's collateral, portfolio value, leverage, margin usage and buying power, for its cross margin and in total."""

  def user_stats(
    self,
    account_index: int,
    *,
    validate: bool | None = None,
  ) -> StreamManager[UserStatsUpdate, Any, Any]:
    """Stream an account's collateral, portfolio value, leverage, margin usage and buying power, for its cross margin and in total.

    Args:
      account_index: Account index to follow.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/docs/websocket-reference#account-stats)
    """
    return self.subscribe(
      f'user_stats/{account_index}',
      meta={},
      validate=validate,
      response_type=UserStatsUpdate,
    )
