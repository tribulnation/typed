# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_lighter.core.endpoint.stream import StreamEndpoint
from typed_lighter.schemas import AccountPosition, PublicPoolShare


class AccountAllPositionsUpdate(TypedDict):
  """One `account_all_positions` frame."""

  type: Literal['subscribed/account_all_positions', 'update/account_all_positions']
  """`subscribed/...` for the snapshot sent on subscribe, `update/...` for every later push."""
  channel: str
  """The subscribed channel in colon form (`account_all_positions:476`)."""
  positions: dict[str, AccountPosition]
  """Positions by market id (as a string)."""
  shares: list[PublicPoolShare]
  """Public pool shares the account holds."""
  bo_positions: NotRequired[dict[str, Any]]
  """Undocumented; normally `{}`, and absent on testnet."""
  last_funding_round: NotRequired[dict[str, str]]
  """Documented: the funding round by market id, only on a frame sent at a funding payment."""
  last_funding_discount: NotRequired[dict[str, str]]
  """Documented: the funding discount by market id, only on a frame sent at a funding payment."""


class AccountAllPositions(StreamEndpoint):
  """Stream an account's positions on every market and its public pool shares."""

  def account_all_positions(
    self,
    account_index: int,
    *,
    validate: bool | None = None,
  ) -> StreamManager[AccountAllPositionsUpdate, Any, Any]:
    """Stream an account's positions on every market and its public pool shares.

    Args:
      account_index: Account index to follow.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/docs/websocket-reference#account-all-positions)
    """
    return self.subscribe(
      f'account_all_positions/{account_index}',
      meta={},
      validate=validate,
      response_type=AccountAllPositionsUpdate,
    )
