# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_lighter.core.endpoint.stream import StreamEndpoint
from typed_lighter.schemas import PublicPoolInfo


class PoolInfoUpdate(TypedDict):
  """One `pool_info` frame."""

  type: Literal['subscribed/pool_info', 'update/pool_info']
  """`subscribed/...` for the snapshot sent on subscribe, `update/...` for every later push."""
  channel: str
  """The subscribed channel in colon form (`pool_info:281474976710654`)."""
  pool_info: PublicPoolInfo
  """The pool's current details."""


class PoolInfo(StreamEndpoint):
  """Stream a public pool's status, fees, shares, APY, Sharpe ratio and daily return and share price history."""

  def pool_info(
    self,
    account_index: int,
    *,
    validate: bool | None = None,
  ) -> StreamManager[PoolInfoUpdate, Any, Any]:
    """Stream a public pool's status, fees, shares, APY, Sharpe ratio and daily return and share price history.

    Args:
      account_index: Public pool account index.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/docs/websocket-reference#pool-info)
    """
    return self.subscribe(
      f'pool_info/{account_index}',
      meta={},
      validate=validate,
      response_type=PoolInfoUpdate,
    )
