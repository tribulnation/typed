# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict): ...


class ZkLighterInfo(TypedDict):
  """The zkLighter L1 (Ethereum) proxy contract address."""

  contract_address: str
  """Lighter proxy contract on Ethereum L1: holds collateral and takes deposits and priority transactions."""


class Info(RpcEndpoint):
  """Get the zkLighter L1 (Ethereum) proxy contract address."""

  async def info(self, *, validate: bool | None = None) -> ZkLighterInfo:
    """Get the zkLighter L1 (Ethereum) proxy contract address.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/info-1)
    """
    return await self.request(
      Request(),
      method='GET',
      path='/info',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=ZkLighterInfo,
    )
