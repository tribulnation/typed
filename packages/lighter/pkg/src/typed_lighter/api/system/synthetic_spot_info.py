# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_lighter.core import TimestampMillis
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  symbol: str
  """Lighter market symbol, e.g. `US500` or `US100`; markets without synthetic spot info answer `20001`."""


class SyntheticSpotInfo(TypedDict):
  """Synthetic-spot pricing parameters of a perp that tracks a futures-based index."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Extra detail from the server, when it sends any."""
  bps_per_day: float
  """Daily carry applied to the synthetic spot price, in basis points."""
  expiry_time_ms: TimestampMillis
  """Expiry of the underlying futures contract."""
  spot_close_ms: TimestampMillis
  """Last close of the underlying spot index session."""
  source: str
  """Price source, e.g. `pythlazer`."""
  symbol: str
  """Symbol of the underlying futures contract at the source, e.g. `EMU6` (E-mini S&P 500, September 2026)."""


class SyntheticSpotInfoEndpoint(RpcEndpoint):
  """Get the synthetic-spot pricing parameters of a perp that tracks a futures-based index (US equity indices): the source contract, its expiry and the daily carry."""

  async def synthetic_spot_info(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> SyntheticSpotInfo:
    """Get the synthetic-spot pricing parameters of a perp that tracks a futures-based index (US equity indices): the source contract, its expiry and the daily carry.

    Args:
      symbol: Lighter market symbol, e.g. `US500` or `US100`; markets without synthetic spot info answer `20001`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/syntheticspotinfo)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/api/v1/syntheticSpotInfo',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=SyntheticSpotInfo,
    )
