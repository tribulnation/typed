# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict): ...


class SystemConfig(TypedDict):
  """System configuration: well-known account indexes, pool cooldowns and integrator fee caps."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Extra detail from the server, when it sends any."""
  liquidity_pool_index: int
  """Account index of the protocol liquidity pool (LLP)."""
  staking_pool_index: int
  """Account index of the LIT staking pool."""
  funding_fee_rebate_account_index: int
  """Account index that pays funding-fee rebates."""
  market_maker_incentive_account_index: int
  """Account index that pays market-maker incentives."""
  fee_collector_account_index: int
  """Account index that collects trading fees."""
  liquidity_pool_cooldown_period: int
  """Cooldown before liquidity-pool shares can be burned after minting, in milliseconds (300000 = 5 minutes on mainnet)."""
  staking_pool_lockup_period: int
  """Lockup of staked LIT before it can be unstaked, in milliseconds (259200000 = 3 days on mainnet)."""
  max_integrator_perps_maker_fee: int
  """Largest maker fee an integrator may charge on perp orders, in the integer fee units of the integrator-fee attribute of a signed order."""
  max_integrator_perps_taker_fee: int
  """Largest taker fee an integrator may charge on perp orders, in the integer fee units of the integrator-fee attribute of a signed order."""
  max_integrator_spot_maker_fee: int
  """Largest maker fee an integrator may charge on spot orders, in the integer fee units of the integrator-fee attribute of a signed order."""
  max_integrator_spot_taker_fee: int
  """Largest taker fee an integrator may charge on spot orders, in the integer fee units of the integrator-fee attribute of a signed order."""


class SystemConfigEndpoint(RpcEndpoint):
  """Get system configuration: well-known account indexes (liquidity and staking pools, fee collector, incentive accounts), pool cooldowns and integrator fee caps."""

  async def system_config(self, *, validate: bool | None = None) -> SystemConfig:
    """Get system configuration: well-known account indexes (liquidity and staking pools, fee collector, incentive accounts), pool cooldowns and integrator fee caps.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/systemconfig)
    """
    return await self.request(
      Request(),
      method='GET',
      path='/api/v1/systemConfig',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=SystemConfig,
    )
