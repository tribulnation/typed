# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict): ...


class WithdrawalDelay(TypedDict):
  """The current delay of a secure (L1) withdrawal."""

  seconds: int
  """Current expected withdrawal delay, in seconds."""


class WithdrawalDelayEndpoint(RpcEndpoint):
  """Get the current delay of a secure (L1) withdrawal, in seconds."""

  async def withdrawal_delay(self, *, validate: bool | None = None) -> WithdrawalDelay:
    """Get the current delay of a secure (L1) withdrawal, in seconds.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/withdrawaldelay)
    """
    return await self.request(
      Request(),
      method='GET',
      path='/api/v1/withdrawalDelay',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=WithdrawalDelay,
    )
