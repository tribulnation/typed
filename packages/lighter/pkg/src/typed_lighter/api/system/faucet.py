# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint
from typed_lighter.schemas import ResultCode


class Request(TypedDict):
  l1_address: str
  """L1 address to fund; creates its account when it has none."""
  do_l1_transfer: NotRequired[bool]
  """Undocumented flag carried by the testnet app's faucet call."""


class Faucet(RpcEndpoint):
  """Request testnet funds for an L1 address (undocumented; testnet only). The venue refuses while the address's total portfolio value is 100 USD or more (`23201`), and mainnet answers `23202 faucet is disabled in this environment`."""

  async def faucet(
    self,
    l1_address: str,
    *,
    do_l1_transfer: bool | None = None,
    validate: bool | None = None,
  ) -> ResultCode:
    """Request testnet funds for an L1 address (undocumented; testnet only). The venue refuses while the address's total portfolio value is 100 USD or more (`23201`), and mainnet answers `23202 faucet is disabled in this environment`.

    Args:
      l1_address: L1 address to fund; creates its account when it has none.
      do_l1_transfer: Undocumented flag carried by the testnet app's faucet call.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
    """
    request: Request = Request(l1_address=l1_address)
    if do_l1_transfer is not None:
      request['do_l1_transfer'] = do_l1_transfer
    return await self.request(
      request,
      method='GET',
      path='/api/v1/faucet',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=ResultCode,
    )
