# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class ContractAddress(TypedDict):
  """One Lighter-related L1 contract."""

  name: str
  """Contract name, e.g. `ZkLighterContract`, `USDCContract`."""
  address: str
  """Contract address on L1."""


class L1ProviderInfo(TypedDict):
  """One L1 RPC provider and its latest block."""

  chainId: int
  """EVM chain id of the provider."""
  networkId: int
  """EVM network id of the provider."""
  latestBlockNumber: int
  """Latest L1 block number the provider reported (normally `0`)."""


class Request(TypedDict): ...


class ValidatorInfo(TypedDict):
  """One L1 validator."""

  address: NotRequired[str]
  """Validator L1 address."""
  is_active: NotRequired[bool]
  """Whether the validator is active."""


class Layer1BasicInfo(TypedDict):
  """zkLighter L1 information: RPC providers, validators, contracts and latest processed L1 blocks."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Extra detail from the server, when it sends any."""
  l1_providers: list[L1ProviderInfo]
  """L1 RPC providers the sequencer uses."""
  l1_providers_health: bool
  """Whether the L1 providers are healthy."""
  validator_info: list[ValidatorInfo]
  """L1 validators (normally empty)."""
  contract_addresses: list[ContractAddress]
  """Lighter-related L1 contracts."""
  latest_l1_generic_block: int
  """Latest L1 block processed for generic events (normally `0`)."""
  latest_l1_governance_block: int
  """Latest L1 block processed for governance events (normally `0`)."""
  latest_l1_desert_block: int
  """Latest L1 block processed for desert-mode events (normally `0`)."""


class Layer1BasicInfoEndpoint(RpcEndpoint):
  """Get zkLighter L1 information: L1 RPC providers, validators, contract addresses and the latest processed L1 blocks."""

  async def layer1_basic_info(self, *, validate: bool | None = None) -> Layer1BasicInfo:
    """Get zkLighter L1 information: L1 RPC providers, validators, contract addresses and the latest processed L1 blocks.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/layer1basicinfo)
    """
    return await self.request(
      Request(),
      method='GET',
      path='/api/v1/layer1BasicInfo',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Layer1BasicInfo,
    )
