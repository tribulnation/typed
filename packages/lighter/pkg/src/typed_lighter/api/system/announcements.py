# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_lighter.core import TimestampSeconds
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class Announcement(TypedDict):
  """One exchange announcement."""

  title: str
  """Announcement title."""
  content: str
  """Announcement body text."""
  created_at: TimestampSeconds
  """When the announcement was published."""
  expired_at: NotRequired[TimestampSeconds]
  """When the announcement stops being shown; absent when it does not expire."""


class Request(TypedDict): ...


class Announcements(TypedDict):
  """Exchange announcements."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Extra detail from the server, when it sends any."""
  announcements: list[Announcement]
  """Announcements, newest first."""


class AnnouncementsEndpoint(RpcEndpoint):
  """Get exchange announcements (listings, delistings, maintenance), newest first."""

  async def announcements(self, *, validate: bool | None = None) -> Announcements:
    """Get exchange announcements (listings, delistings, maintenance), newest first.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/announcement-1)
    """
    return await self.request(
      Request(),
      method='GET',
      path='/api/v1/announcement',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Announcements,
    )
