# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_lighter.core import TimestampSeconds
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict): ...


class Status(TypedDict):
  """API server status: health code, network id and server time."""

  status: int
  """HTTP-style health code; `200` when the API server is up. Not the `code` envelope field: this endpoint has no `code`."""
  network_id: int
  """Network id of the deployment (`1` on mainnet)."""
  timestamp: TimestampSeconds
  """Server time."""


class StatusEndpoint(RpcEndpoint):
  """Get the API server status: health code, network id and server time."""

  async def status(self, *, validate: bool | None = None) -> Status:
    """Get the API server status: health code, network id and server time.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/status)
    """
    return await self.request(
      Request(),
      method='GET',
      path='/',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Status,
    )
