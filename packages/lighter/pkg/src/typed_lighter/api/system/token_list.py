# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict): ...


class Token(TypedDict):
  """One listed token and its display metadata."""

  symbol: str
  """Token or market symbol shown in the app, e.g. `ETH`, `SPY/USDC`."""
  name: str
  """Display name."""
  logo: str
  """Logo file name, without extension."""
  logo_extension: Literal['svg', 'png', 'jpg']
  """Logo file extension."""
  description_key: str
  """Localization key of the token description."""
  gecko_id: str
  """CoinGecko id; empty when unknown."""
  paprika_id: str
  """CoinPaprika id; empty when unknown."""
  market: Literal['SPOT', 'PERPS']
  """Market type the token is listed on."""
  asset_type: Literal['CRYPTO', 'RWA']
  """Asset class: a crypto asset or a real-world asset (equities, FX, commodities)."""
  categories: list[str]
  """Category tags."""
  is_allowed_mainnet: bool
  """Whether the market is enabled on mainnet."""
  is_asset_allowed_mainnet: bool
  """Whether the token is enabled as a spot asset on mainnet."""
  default_order_book_group_by: int
  """Default price-grouping step of the app's order book view (1, 10 or 100 ticks observed)."""
  backend_symbol: NotRequired[str]
  """Symbol the venue uses internally when it differs from `symbol` (e.g. `1000PEPE`, `rhSPY`)."""


class TokenList(TypedDict):
  """Every listed token."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Extra detail from the server, when it sends any."""
  tokens: list[Token]
  """Listed tokens."""


class TokenListEndpoint(RpcEndpoint):
  """Get every listed token with its display metadata (logo, external ids, categories) and market type."""

  async def token_list(self, *, validate: bool | None = None) -> TokenList:
    """Get every listed token with its display metadata (logo, external ids, categories) and market type.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/tokenlist-1)
    """
    return await self.request(
      Request(),
      method='GET',
      path='/api/v1/tokenlist',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=TokenList,
    )
