# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint
from typed_lighter.schemas import ReferralCode


class Request(TypedDict):
  account_index: int
  """Account index the auth token belongs to (a master or sub-account)."""


class Create(RpcEndpoint):
  """Create the account's referral code (one per account)."""

  async def create(
    self, account_index: int, *, validate: bool | None = None
  ) -> ReferralCode:
    """Create the account's referral code (one per account).

    Args:
      account_index: Account index the auth token belongs to (a master or sub-account).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/referral_create)
    """
    return await self.request(
      Request(account_index=account_index),
      method='POST',
      path='/api/v1/referral/create',
      meta={'auth': 'write'},
      validate=validate,
      request_type=Request,
      response_type=ReferralCode,
    )
