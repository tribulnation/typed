# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint
from typed_lighter.schemas import ReferralCode


class Request(TypedDict):
  account_index: int
  """Account whose referral code to return; the token must cover it."""


class Get(RpcEndpoint):
  """Get an account's referral code."""

  async def get(
    self, account_index: int, *, validate: bool | None = None
  ) -> ReferralCode:
    """Get an account's referral code.

    Args:
      account_index: Account whose referral code to return; the token must cover it.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/referral_get)
    """
    return await self.request(
      Request(account_index=account_index),
      method='GET',
      path='/api/v1/referral/get',
      meta={'auth': 'token'},
      validate=validate,
      request_type=Request,
      response_type=ReferralCode,
    )
