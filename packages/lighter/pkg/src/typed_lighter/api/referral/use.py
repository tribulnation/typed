# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint
from typed_lighter.schemas import ResultCode


class Request(TypedDict):
  l1_address: str
  """L1 address being referred (the token's own)."""
  referral_code: str
  """Another account's referral code."""
  discord: NotRequired[str]
  """Optional Discord handle."""
  telegram: NotRequired[str]
  """Optional Telegram handle."""
  x: NotRequired[str]
  """Optional X (Twitter) handle."""
  signature: NotRequired[str]
  """Optional signature; its content is undocumented."""


class Use(RpcEndpoint):
  """Attach a referral code to an L1 address (can be changed later)."""

  async def use(
    self,
    *,
    l1_address: str,
    referral_code: str,
    discord: str | None = None,
    telegram: str | None = None,
    x: str | None = None,
    signature: str | None = None,
    validate: bool | None = None,
  ) -> ResultCode:
    """Attach a referral code to an L1 address (can be changed later).

    Args:
      l1_address: L1 address being referred (the token's own).
      referral_code: Another account's referral code.
      discord: Optional Discord handle.
      telegram: Optional Telegram handle.
      x: Optional X (Twitter) handle.
      signature: Optional signature; its content is undocumented.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/referral_use)
    """
    request: Request = Request(l1_address=l1_address, referral_code=referral_code)
    if discord is not None:
      request['discord'] = discord
    if telegram is not None:
      request['telegram'] = telegram
    if x is not None:
      request['x'] = x
    if signature is not None:
      request['signature'] = signature
    return await self.request(
      request,
      method='POST',
      path='/api/v1/referral/use',
      meta={'auth': 'write'},
      validate=validate,
      request_type=Request,
      response_type=ResultCode,
    )
