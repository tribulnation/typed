# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint
from typed_lighter.schemas import UpdateResult


class Request(TypedDict):
  account_index: int
  """Account index the auth token belongs to (a master or sub-account)."""
  kickback_percentage: float
  """Kickback, in percent."""


class UpdateKickback(RpcEndpoint):
  """Set the share of referral rewards passed back to referred users. The docs say once per day; the venue enforces once per week (`41008`)."""

  async def update_kickback(
    self,
    account_index: int,
    *,
    kickback_percentage: float,
    validate: bool | None = None,
  ) -> UpdateResult:
    """Set the share of referral rewards passed back to referred users. The docs say once per day; the venue enforces once per week (`41008`).

    Args:
      account_index: Account index the auth token belongs to (a master or sub-account).
      kickback_percentage: Kickback, in percent.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/referral_kickback_update)
    """
    return await self.request(
      Request(account_index=account_index, kickback_percentage=kickback_percentage),
      method='POST',
      path='/api/v1/referral/kickback/update',
      meta={'auth': 'write'},
      validate=validate,
      request_type=Request,
      response_type=UpdateResult,
    )
