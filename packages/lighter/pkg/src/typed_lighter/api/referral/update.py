# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint
from typed_lighter.schemas import UpdateResult


class Request(TypedDict):
  account_index: int
  """Account index the auth token belongs to (a master or sub-account)."""
  new_referral_code: str
  """The new code, e.g. `TYPEDLTR`."""


class Update(RpcEndpoint):
  """Replace the account's referral code with a chosen one (allowed once per account)."""

  async def update(
    self,
    account_index: int,
    *,
    new_referral_code: str,
    validate: bool | None = None,
  ) -> UpdateResult:
    """Replace the account's referral code with a chosen one (allowed once per account).

    Args:
      account_index: Account index the auth token belongs to (a master or sub-account).
      new_referral_code: The new code, e.g. `TYPEDLTR`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/referral_update)
    """
    return await self.request(
      Request(account_index=account_index, new_referral_code=new_referral_code),
      method='POST',
      path='/api/v1/referral/update',
      meta={'auth': 'write'},
      validate=validate,
      request_type=Request,
      response_type=UpdateResult,
    )
