# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class ReferralPointEntry(TypedDict):
  """Points of one referred user."""

  l1_address: str
  """Referred user's L1 address."""
  total_points: float
  """Points the user earned in total."""
  week_points: float
  """Points the user earned last week."""
  total_reward_points: float
  """Reward points the referrer earned from this user in total."""
  week_reward_points: float
  """Reward points the referrer earned from this user last week."""
  reward_point_multiplier: Decimal
  """Share of the user's points credited to the referrer."""


class Request(TypedDict):
  account_index: int
  """Account whose referral points to return; the token must cover it."""


class ReferralPoints(TypedDict):
  """An account's referral points and the points of each user it referred."""

  referrals: list[ReferralPointEntry]
  """Points of each referred user."""
  user_total_points: float
  """The account's own points in total."""
  user_last_week_points: float
  """The account's own points last week."""
  user_total_referral_reward_points: float
  """Referral reward points earned in total."""
  user_last_week_referral_reward_points: float
  """Referral reward points earned last week."""
  reward_point_multiplier: Decimal
  """Share of referred users' points credited to this account (`0.0320` = 3.2%)."""


class Points(RpcEndpoint):
  """Get an account's referral points and the points of each user it referred."""

  async def points(
    self,
    account_index: int,
    *,
    validate: bool | None = None,
  ) -> ReferralPoints:
    """Get an account's referral points and the points of each user it referred.

    Args:
      account_index: Account whose referral points to return; the token must cover it.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
    """
    return await self.request(
      Request(account_index=account_index),
      method='GET',
      path='/api/v1/referral/points',
      meta={'auth': 'token'},
      validate=validate,
      request_type=Request,
      response_type=ReferralPoints,
    )
