# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, Sequence, TypedDict
from typed_lighter.core import TimestampSeconds
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class PnlLeaderboardEntry(TypedDict):
  """One PnL leaderboard entry."""

  rank: int
  """Rank in the requested ordering (1-based)."""
  l1_address: str
  """L1 address of the account owner (unmasked)."""
  account_value: float
  """Account value, in USDC."""
  pnl: float
  """PnL over the window, in USDC."""
  roi: float
  """Return over the window, in percent."""
  volume: float
  """Trading volume over the window, in USDC."""


class Request(TypedDict):
  time_window: NotRequired[Literal['24h', '7d', '30d', 'all']]
  """Time window of the ranking."""
  sort_by: NotRequired[Literal['pnl', 'roi', 'volume', 'account_value']]
  """Ranking key."""
  sort_dir: NotRequired[Literal['asc', 'desc']]
  """Sort direction."""
  limit: NotRequired[int]
  """Entries per page."""
  offset: NotRequired[int]
  """Entries to skip."""
  search: NotRequired[str]
  """Filter by address; a partial address matches nothing."""


class PnlLeaderboard(TypedDict):
  """One page of the PnL leaderboard."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Extra detail from the server, when it sends any."""
  entries: list[PnlLeaderboardEntry]
  """One page of entries."""
  total: int
  """Number of ranked entries in the window."""
  updated_at: TimestampSeconds
  """When the leaderboard was last recomputed."""


class PnlLeaderboardEndpoint(RpcEndpoint):
  """Get the PnL leaderboard over a time window, sorted by PnL, ROI, volume or account value, one page at a time."""

  def pnl_leaderboard_paged(
    self,
    time_window: Literal['24h', '7d', '30d', 'all'] | None = None,
    *,
    sort_by: Literal['pnl', 'roi', 'volume', 'account_value'] | None = None,
    sort_dir: Literal['asc', 'desc'] | None = None,
    limit: int | None = None,
    search: str | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[PnlLeaderboardEntry, int]:
    """Get the PnL leaderboard over a time window, sorted by PnL, ROI, volume or account value, one page at a time.

    Paged variant of `pnl_leaderboard`: Advances `offset` by `len(rows)` and stops once it has covered the `total` items the response reports, or on an empty page. Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      time_window: Time window of the ranking.
      sort_by: Ranking key.
      sort_dir: Sort direction.
      limit: Entries per page.
      search: Filter by address; a partial address matches nothing.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/pnlleaderboard)
    """

    async def next(offset: int) -> tuple[Sequence[PnlLeaderboardEntry], int | None]:
      response = await self.pnl_leaderboard(
        time_window,
        sort_by=sort_by,
        sort_dir=sort_dir,
        limit=limit,
        search=search,
        offset=offset,
        validate=validate,
      )
      rows = response.get('entries')
      rows = rows if rows is not None else []
      total = response.get('total')
      total = int(total) if total is not None else None
      if not rows or (total is not None and (offset + len(rows)) >= total):
        return rows, None
      return rows, offset + len(rows)

    return PaginatedResponse(0, next)

  async def pnl_leaderboard(
    self,
    time_window: Literal['24h', '7d', '30d', 'all'] | None = None,
    *,
    sort_by: Literal['pnl', 'roi', 'volume', 'account_value'] | None = None,
    sort_dir: Literal['asc', 'desc'] | None = None,
    limit: int | None = None,
    offset: int | None = None,
    search: str | None = None,
    validate: bool | None = None,
  ) -> PnlLeaderboard:
    """Get the PnL leaderboard over a time window, sorted by PnL, ROI, volume or account value, one page at a time.

    Args:
      time_window: Time window of the ranking.
      sort_by: Ranking key.
      sort_dir: Sort direction.
      limit: Entries per page.
      offset: Entries to skip.
      search: Filter by address; a partial address matches nothing.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/pnlleaderboard)
    """
    request: Request = Request()
    if time_window is not None:
      request['time_window'] = time_window
    if sort_by is not None:
      request['sort_by'] = sort_by
    if sort_dir is not None:
      request['sort_dir'] = sort_dir
    if limit is not None:
      request['limit'] = limit
    if offset is not None:
      request['offset'] = offset
    if search is not None:
      request['search'] = search
    return await self.request(
      request,
      method='GET',
      path='/api/v1/pnlLeaderboard',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=PnlLeaderboard,
    )
