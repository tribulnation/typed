# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class LivePointsTotal(TypedDict):
  """The live (not yet finalized) points total of an account."""

  code: NotRequired[int]
  """`200` on success."""
  message: NotRequired[str]
  """Extra detail from the server, when it sends any."""
  total_live_points: NotRequired[float]
  """Live points accumulated this period."""


class Request(TypedDict):
  account_index: int
  """Account to query."""


class LiveTotal(RpcEndpoint):
  """Get the live (not yet finalized) points total of an account."""

  async def live_total(
    self,
    account_index: int,
    *,
    validate: bool | None = None,
  ) -> LivePointsTotal:
    """Get the live (not yet finalized) points total of an account.

    Args:
      account_index: Account to query.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
    """
    return await self.request(
      Request(account_index=account_index),
      method='GET',
      path='/api/v1/livePoints/total',
      meta={'auth': 'token'},
      validate=validate,
      request_type=Request,
      response_type=LivePointsTotal,
    )
