# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class AssetDetail(TypedDict):
  """One spot asset: decimals, transfer and withdrawal minimums, index price and collateral parameters."""

  asset_id: int
  """Asset id (`1` ETH, `2` LIT, `3` USDC on mainnet)."""
  symbol: str
  """Asset symbol."""
  l1_decimals: int
  """Decimals of the asset's L1 token contract."""
  decimals: int
  """Decimals of the asset on Lighter (amounts in signed transfers are scaled by this)."""
  min_transfer_amount: Decimal
  """Smallest L2 transfer amount."""
  min_withdrawal_amount: Decimal
  """Smallest withdrawal amount."""
  margin_mode: Literal['enabled', 'disabled', 'priced_only']
  """Whether the asset counts as margin collateral: `enabled`, `disabled`, or `priced_only` (priced for the account value, not usable as margin)."""
  index_price: Decimal
  """Current index price, in USDC."""
  price_decimals: int
  """Decimals of `index_price`."""
  l1_address: str
  """Address of the asset's L1 token contract (the zero address for ETH)."""
  loan_to_value: Decimal
  """Loan-to-value ratio the asset counts at as collateral (0..1)."""
  liquidation_threshold: Decimal
  """Collateral liquidation threshold (0..1)."""
  liquidation_factor: Decimal
  """Collateral liquidation factor (0..1)."""
  liquidation_fee: Decimal
  """Collateral liquidation fee (fraction)."""
  global_supply_cap: Decimal
  """Venue-wide cap on the asset supplied as collateral; `0` = none."""
  user_supply_cap: Decimal
  """Per-account cap on the asset supplied as collateral; `0` = none."""
  total_supplied: Decimal
  """Amount currently supplied as collateral."""
  multiplier: Decimal
  """Asset multiplier (above 1 for yield-bearing wrapped assets)."""


class Request(TypedDict):
  asset_id: NotRequired[int]
  """Only this asset. Omit for every asset."""


class AssetDetails(TypedDict):
  """Details of spot assets."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Extra detail from the server, when it sends any."""
  asset_details: list[AssetDetail]
  """Assets."""


class AssetDetailsEndpoint(RpcEndpoint):
  """Get the details of every spot asset (or one): decimals, transfer/withdrawal minimums, index price, and collateral (margin) parameters."""

  async def asset_details(
    self,
    asset_id: int | None = None,
    *,
    validate: bool | None = None,
  ) -> AssetDetails:
    """Get the details of every spot asset (or one): decimals, transfer/withdrawal minimums, index price, and collateral (margin) parameters.

    Args:
      asset_id: Only this asset. Omit for every asset.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/assetdetails)
    """
    request: Request = Request()
    if asset_id is not None:
      request['asset_id'] = asset_id
    return await self.request(
      request,
      method='GET',
      path='/api/v1/assetDetails',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=AssetDetails,
    )
