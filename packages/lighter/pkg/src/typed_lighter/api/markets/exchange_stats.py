# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class OrderBookStats(TypedDict):
  """24-hour statistics of one market."""

  symbol: str
  """Market symbol."""
  last_trade_price: float
  """Price of the last trade."""
  daily_trades_count: int
  """Trades in the last 24 hours."""
  daily_base_token_volume: float
  """Volume in the last 24 hours, in base units."""
  daily_quote_token_volume: float
  """Volume in the last 24 hours, in quote units."""
  daily_price_change: float
  """Price change over the last 24 hours, in percent."""


class Request(TypedDict): ...


class ExchangeStats(TypedDict):
  """24-hour statistics of every market, plus exchange-wide totals."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Extra detail from the server, when it sends any."""
  total: int
  """Number of markets in `order_book_stats`."""
  order_book_stats: list[OrderBookStats]
  """Per-market 24h statistics."""
  daily_usd_volume: float
  """Exchange-wide volume in the last 24 hours, in USD."""
  daily_trades_count: int
  """Exchange-wide trades in the last 24 hours."""


class ExchangeStatsEndpoint(RpcEndpoint):
  """Get 24-hour statistics of every market plus exchange-wide volume and trade count."""

  async def exchange_stats(self, *, validate: bool | None = None) -> ExchangeStats:
    """Get 24-hour statistics of every market plus exchange-wide volume and trade count.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/exchangestats)
    """
    return await self.request(
      Request(),
      method='GET',
      path='/api/v1/exchangeStats',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=ExchangeStats,
    )
