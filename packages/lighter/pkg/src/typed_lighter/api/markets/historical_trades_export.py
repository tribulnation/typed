# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_lighter.core import DateIso, TimestampIso
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class HistoricalTradesExportData(TypedDict):
  """Link to one day's exported trades."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Optional human-readable detail; usually absent on success."""
  data_url: str
  """Presigned S3 URL of the day's parquet file."""
  expires_at: TimestampIso
  """When `data_url` stops working."""


class Request(TypedDict):
  l1_address: str
  """L1 address of the account that paid for access."""
  date: DateIso
  """UTC day to export (`YYYY-MM-DD`)."""


class HistoricalTradesExport(RpcEndpoint):
  """Export one UTC day of global historical trades as a parquet file: returns a presigned S3 URL valid for 1 hour. Updated daily at 8 PM UTC. Requires a one-time 100 LIT L2 transfer to 0x4FD058F25bE85E459ec552cA8e4C696FD1D34125 from the requesting account."""

  async def historical_trades_export(
    self,
    l1_address: str,
    *,
    date: DateIso,
    validate: bool | None = None,
  ) -> HistoricalTradesExportData:
    """Export one UTC day of global historical trades as a parquet file: returns a presigned S3 URL valid for 1 hour. Updated daily at 8 PM UTC. Requires a one-time 100 LIT L2 transfer to 0x4FD058F25bE85E459ec552cA8e4C696FD1D34125 from the requesting account.

    Args:
      l1_address: L1 address of the account that paid for access.
      date: UTC day to export (`YYYY-MM-DD`).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/export_historicaltrades)
    """
    return await self.request(
      Request(l1_address=l1_address, date=date),
      method='GET',
      path='/api/v1/export/historicalTrades',
      meta={'auth': 'token'},
      validate=validate,
      request_type=Request,
      response_type=HistoricalTradesExportData,
    )
