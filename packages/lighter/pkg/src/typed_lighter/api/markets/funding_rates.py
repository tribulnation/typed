# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class FundingRate(TypedDict):
  """The current funding rate of one market on one venue."""

  market_id: int
  """Lighter market id the rate is matched to."""
  exchange: str
  """Venue, e.g. `lighter`, `binance`, `bybit`, `hyperliquid`."""
  symbol: str
  """Market symbol."""
  rate: float
  """Funding rate of the venue's funding interval, as a fraction."""


class Request(TypedDict): ...


class FundingRates(TypedDict):
  """Current funding rates of Lighter markets and the same markets on other venues."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Extra detail from the server, when it sends any."""
  funding_rates: list[FundingRate]
  """One rate per market and venue."""


class FundingRatesEndpoint(RpcEndpoint):
  """Get the current funding rates of Lighter markets and the same markets on other venues (Binance, Bybit, Hyperliquid). For real-time rates use the `market_stats` stream."""

  async def funding_rates(self, *, validate: bool | None = None) -> FundingRates:
    """Get the current funding rates of Lighter markets and the same markets on other venues (Binance, Bybit, Hyperliquid). For real-time rates use the `market_stats` stream.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/funding-rates)
    """
    return await self.request(
      Request(),
      method='GET',
      path='/api/v1/funding-rates',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=FundingRates,
    )
