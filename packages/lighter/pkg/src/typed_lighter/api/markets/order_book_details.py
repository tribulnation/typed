# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_lighter.core import TimestampMillis
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class MarketConfig(TypedDict):
  """Per-market trading configuration."""

  market_margin_mode: int
  """Margin mode of the market (`0` on every current market; lighter-go margin modes are `0` cross, `1` isolated)."""
  insurance_fund_account_index: int
  """Account index of the market's insurance fund; `281474976710655` (2^48-1) for the default fund."""
  liquidation_mode: int
  """Liquidation mode of the market (`0` on every current market; undocumented)."""
  force_reduce_only: bool
  """Whether only reduce-only orders are accepted (e.g. while delisting)."""
  trading_hours: str
  """Trading-hours schedule of a market with restricted hours (RWA markets); empty when it trades around the clock."""
  funding_fee_discounts_enabled: bool
  """Whether funding-fee discounts apply on this market."""
  hidden: bool
  """Whether the market is hidden from the app."""
  rfq_enabled: bool
  """Whether requests for quote are enabled on this market."""


class Request(TypedDict):
  market_id: NotRequired[int]
  """Only this market. Omit for every market (the venue's `255` sentinel)."""
  filter: NotRequired[Literal['all', 'spot', 'perp']]
  """Only spot or only perp markets; `all` (the default) returns both."""


class SpotOrderBookDetail(TypedDict):
  """Full details of one spot market."""

  symbol: str
  """Market symbol: the base asset for a perp (`ETH`), `BASE/QUOTE` for spot (`ETH/USDC`)."""
  market_id: int
  """Market id. Do not infer the market type from it: read `market_type`."""
  market_type: Literal['perp', 'spot']
  """Perpetual or spot market."""
  base_asset_id: int
  """Asset id of the base asset for spot markets (`assetDetails`); `0` for perps."""
  quote_asset_id: int
  """Asset id of the quote asset for spot markets (`3` = USDC); `0` for perps."""
  status: Literal['inactive', 'active']
  """Whether the market is trading."""
  taker_fee: Decimal
  """Taker fee, in percent."""
  is_taker_fee_enabled: bool
  """Whether the taker fee is charged."""
  maker_fee: Decimal
  """Maker fee, in percent."""
  is_maker_fee_enabled: bool
  """Whether the maker fee is charged."""
  liquidation_fee: Decimal
  """Liquidation fee, in percent (`0` on spot markets)."""
  min_base_amount: Decimal
  """Smallest order size, in base units."""
  min_quote_amount: Decimal
  """Smallest order notional, in quote units."""
  order_quote_limit: Decimal
  """Largest order notional, in quote units (`281474976.710655` = no limit, the uint48 maximum)."""
  supported_size_decimals: int
  """Decimals an order size is scaled by when signing (`base_amount = size * 10^supported_size_decimals`)."""
  supported_price_decimals: int
  """Decimals an order price is scaled by when signing (`price = price * 10^supported_price_decimals`)."""
  supported_quote_decimals: int
  """Decimals of quote amounts (size decimals plus price decimals)."""
  created_at: TimestampMillis
  """When the market was created. Sent as a JSON string of epoch milliseconds."""
  multiplier: Decimal
  """Base-asset multiplier of the market (1 for most; above 1 for yield-bearing wrapped assets such as `rhSPY`)."""
  start_timestamp: NotRequired[int]
  """Undocumented; `0` on every current market (appears to belong to event/outcome-settled markets). Mainnet only."""
  end_timestamp: NotRequired[int]
  """Undocumented; `0` on every current market (appears to belong to event/outcome-settled markets). Mainnet only."""
  open_interest: NotRequired[Decimal]
  """Open interest (`0` on spot markets). Mainnet only."""
  outcome: NotRequired[int]
  """Undocumented; `0` on every current market (appears to belong to event/outcome-settled markets). Mainnet only."""
  operator_account_index: NotRequired[int]
  """Undocumented; account index operating the market (`1` for perps, `0` for spot markets observed). Mainnet only."""
  settlement_cap: NotRequired[int]
  """Undocumented; `0` on every current market (appears to belong to event/outcome-settled markets). Mainnet only."""
  settlement_type: NotRequired[int]
  """Undocumented; `0` on every current market (appears to belong to event/outcome-settled markets). Mainnet only."""
  settlement_price: NotRequired[int]
  """Undocumented; `0` on every current market (appears to belong to event/outcome-settled markets). Mainnet only."""
  default_price: NotRequired[int]
  """Undocumented; `0` on every current market (appears to belong to event/outcome-settled markets). Mainnet only."""
  is_frozen: NotRequired[bool]
  """Undocumented; whether the market is frozen (`false` on every current market). Mainnet only."""
  size_decimals: int
  """Decimals of displayed sizes."""
  price_decimals: int
  """Decimals of displayed prices."""
  daily_chart: dict[str, float]
  """Intraday price chart keyed by timestamp (normally empty)."""
  last_trade_price: float
  """Price of the last trade."""
  daily_trades_count: int
  """Trades in the last 24 hours."""
  daily_base_token_volume: float
  """Volume in the last 24 hours, in base units."""
  daily_quote_token_volume: float
  """Volume in the last 24 hours, in quote units."""
  daily_price_low: float
  """Lowest trade price in the last 24 hours."""
  daily_price_high: float
  """Highest trade price in the last 24 hours."""
  daily_price_change: float
  """Price change over the last 24 hours, in percent."""


class PerpsOrderBookDetail(TypedDict):
  """Full details of one perpetual market."""

  symbol: str
  """Market symbol: the base asset for a perp (`ETH`), `BASE/QUOTE` for spot (`ETH/USDC`)."""
  market_id: int
  """Market id. Do not infer the market type from it: read `market_type`."""
  market_type: Literal['perp', 'spot']
  """Perpetual or spot market."""
  base_asset_id: int
  """Asset id of the base asset for spot markets (`assetDetails`); `0` for perps."""
  quote_asset_id: int
  """Asset id of the quote asset for spot markets (`3` = USDC); `0` for perps."""
  status: Literal['inactive', 'active']
  """Whether the market is trading."""
  taker_fee: Decimal
  """Taker fee, in percent."""
  is_taker_fee_enabled: bool
  """Whether the taker fee is charged."""
  maker_fee: Decimal
  """Maker fee, in percent."""
  is_maker_fee_enabled: bool
  """Whether the maker fee is charged."""
  liquidation_fee: Decimal
  """Liquidation fee, in percent (`0` on spot markets)."""
  min_base_amount: Decimal
  """Smallest order size, in base units."""
  min_quote_amount: Decimal
  """Smallest order notional, in quote units."""
  order_quote_limit: Decimal
  """Largest order notional, in quote units (`281474976.710655` = no limit, the uint48 maximum)."""
  supported_size_decimals: int
  """Decimals an order size is scaled by when signing (`base_amount = size * 10^supported_size_decimals`)."""
  supported_price_decimals: int
  """Decimals an order price is scaled by when signing (`price = price * 10^supported_price_decimals`)."""
  supported_quote_decimals: int
  """Decimals of quote amounts (size decimals plus price decimals)."""
  created_at: TimestampMillis
  """When the market was created. Sent as a JSON string of epoch milliseconds."""
  multiplier: Decimal
  """Base-asset multiplier of the market (1 for most; above 1 for yield-bearing wrapped assets such as `rhSPY`)."""
  start_timestamp: NotRequired[int]
  """Undocumented; `0` on every current market (appears to belong to event/outcome-settled markets). Mainnet only."""
  end_timestamp: NotRequired[int]
  """Undocumented; `0` on every current market (appears to belong to event/outcome-settled markets). Mainnet only."""
  open_interest: float
  """Open interest, in base units."""
  outcome: NotRequired[int]
  """Undocumented; `0` on every current market (appears to belong to event/outcome-settled markets). Mainnet only."""
  operator_account_index: NotRequired[int]
  """Undocumented; account index operating the market (`1` for perps, `0` for spot markets observed). Mainnet only."""
  settlement_cap: NotRequired[int]
  """Undocumented; `0` on every current market (appears to belong to event/outcome-settled markets). Mainnet only."""
  settlement_type: NotRequired[int]
  """Undocumented; `0` on every current market (appears to belong to event/outcome-settled markets). Mainnet only."""
  settlement_price: NotRequired[int]
  """Undocumented; `0` on every current market (appears to belong to event/outcome-settled markets). Mainnet only."""
  default_price: NotRequired[int]
  """Undocumented; `0` on every current market (appears to belong to event/outcome-settled markets). Mainnet only."""
  is_frozen: NotRequired[bool]
  """Undocumented; whether the market is frozen (`false` on every current market). Mainnet only."""
  size_decimals: int
  """Decimals of displayed sizes."""
  price_decimals: int
  """Decimals of displayed prices."""
  daily_chart: dict[str, float]
  """Intraday price chart keyed by timestamp (normally empty)."""
  quote_multiplier: int
  """Quote multiplier of the market."""
  default_initial_margin_fraction: int
  """Default initial margin fraction, in basis points of notional (`500` = 5%, i.e. 20x leverage)."""
  min_initial_margin_fraction: int
  """Smallest initial margin fraction (highest leverage) allowed, in basis points (`200` = 50x)."""
  maintenance_margin_fraction: int
  """Maintenance margin fraction, in basis points."""
  closeout_margin_fraction: int
  """Close-out margin fraction, in basis points."""
  mark_price: Decimal
  """Current mark price."""
  index_price: Decimal
  """Current index price."""
  last_trade_price: float
  """Price of the last trade."""
  daily_trades_count: int
  """Trades in the last 24 hours."""
  daily_base_token_volume: float
  """Volume in the last 24 hours, in base units."""
  daily_quote_token_volume: float
  """Volume in the last 24 hours, in quote units."""
  daily_price_low: float
  """Lowest trade price in the last 24 hours."""
  daily_price_high: float
  """Highest trade price in the last 24 hours."""
  daily_price_change: float
  """Price change over the last 24 hours, in percent."""
  market_config: MarketConfig
  strategy_index: int
  """Undocumented pricing/strategy index of the market (`0` or `2` observed)."""
  market_flags: int
  """Undocumented market flag bits (`0` observed)."""
  funding_premium_multiplier: int
  """Multiplier applied to the premium in the funding-rate formula (`100` observed)."""
  funding_clamp_small: Decimal
  """Inner clamp of the funding rate, in percent."""
  funding_clamp_big: Decimal
  """Outer clamp of the funding rate, in percent."""
  base_interest_rate: Decimal
  """Interest-rate component of the funding rate, in percent."""


class OrderBookDetails(TypedDict):
  """Full details of perp and spot markets."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Extra detail from the server, when it sends any."""
  order_book_details: list[PerpsOrderBookDetail] | None
  """Perpetual markets; `null` when `filter` is `spot`."""
  spot_order_book_details: list[SpotOrderBookDetail] | None
  """Spot markets; `null` when `filter` is `perp`."""


class OrderBookDetailsEndpoint(RpcEndpoint):
  """Get the full details of every market (or one): static metadata plus margin fractions, mark/index prices, 24h statistics and funding parameters. Perps and spot markets come back in separate lists."""

  async def order_book_details(
    self,
    market_id: int | None = None,
    *,
    filter: Literal['all', 'spot', 'perp'] | None = None,
    validate: bool | None = None,
  ) -> OrderBookDetails:
    """Get the full details of every market (or one): static metadata plus margin fractions, mark/index prices, 24h statistics and funding parameters. Perps and spot markets come back in separate lists.

    Args:
      market_id: Only this market. Omit for every market (the venue's `255` sentinel).
      filter: Only spot or only perp markets; `all` (the default) returns both.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/orderbookdetails)
    """
    request: Request = Request()
    if market_id is not None:
      request['market_id'] = market_id
    if filter is not None:
      request['filter'] = filter
    return await self.request(
      request,
      method='GET',
      path='/api/v1/orderBookDetails',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=OrderBookDetails,
    )
