# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint
from typed_lighter.schemas import SimpleOrder


class Request(TypedDict):
  market_id: int
  """Market id."""
  limit: int
  """Orders per side."""


class OrderBookOrders(TypedDict):
  """The resting orders at the top of a market's book."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Extra detail from the server, when it sends any."""
  total_asks: int
  """Number of asks returned."""
  asks: list[SimpleOrder]
  """Asks, lowest price first."""
  total_bids: int
  """Number of bids returned."""
  bids: list[SimpleOrder]
  """Bids, highest price first."""


class OrderBookOrdersEndpoint(RpcEndpoint):
  """Get the resting orders at the top of a market's book, best price first on each side."""

  async def order_book_orders(
    self,
    *,
    market_id: int,
    limit: int,
    validate: bool | None = None,
  ) -> OrderBookOrders:
    """Get the resting orders at the top of a market's book, best price first on each side.

    Args:
      market_id: Market id.
      limit: Orders per side.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/orderbookorders)
    """
    return await self.request(
      Request(market_id=market_id, limit=limit),
      method='GET',
      path='/api/v1/orderBookOrders',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=OrderBookOrders,
    )
