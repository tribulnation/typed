# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_lighter.core import TimestampMillis
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class OrderBook(TypedDict):
  """Static metadata of one market: symbol, type, fees, order minimums and signing decimals."""

  symbol: str
  """Market symbol: the base asset for a perp (`ETH`), `BASE/QUOTE` for spot (`ETH/USDC`)."""
  market_id: int
  """Market id. Do not infer the market type from it: read `market_type`."""
  market_type: Literal['perp', 'spot']
  """Perpetual or spot market."""
  base_asset_id: int
  """Asset id of the base asset for spot markets (`assetDetails`); `0` for perps."""
  quote_asset_id: int
  """Asset id of the quote asset for spot markets (`3` = USDC); `0` for perps."""
  status: Literal['inactive', 'active']
  """Whether the market is trading."""
  taker_fee: Decimal
  """Taker fee, in percent."""
  is_taker_fee_enabled: bool
  """Whether the taker fee is charged."""
  maker_fee: Decimal
  """Maker fee, in percent."""
  is_maker_fee_enabled: bool
  """Whether the maker fee is charged."""
  liquidation_fee: Decimal
  """Liquidation fee, in percent (`0` on spot markets)."""
  min_base_amount: Decimal
  """Smallest order size, in base units."""
  min_quote_amount: Decimal
  """Smallest order notional, in quote units."""
  order_quote_limit: Decimal
  """Largest order notional, in quote units (`281474976.710655` = no limit, the uint48 maximum)."""
  supported_size_decimals: int
  """Decimals an order size is scaled by when signing (`base_amount = size * 10^supported_size_decimals`)."""
  supported_price_decimals: int
  """Decimals an order price is scaled by when signing (`price = price * 10^supported_price_decimals`)."""
  supported_quote_decimals: int
  """Decimals of quote amounts (size decimals plus price decimals)."""
  created_at: TimestampMillis
  """When the market was created. Sent as a JSON string of epoch milliseconds."""
  multiplier: Decimal
  """Base-asset multiplier of the market (1 for most; above 1 for yield-bearing wrapped assets such as `rhSPY`)."""
  start_timestamp: NotRequired[int]
  """Undocumented; `0` on every current market (appears to belong to event/outcome-settled markets). Mainnet only."""
  end_timestamp: NotRequired[int]
  """Undocumented; `0` on every current market (appears to belong to event/outcome-settled markets). Mainnet only."""
  open_interest: NotRequired[Decimal]
  """Open interest, in base units (`0` on spot markets). Mainnet only."""
  outcome: NotRequired[int]
  """Undocumented; `0` on every current market (appears to belong to event/outcome-settled markets). Mainnet only."""
  operator_account_index: NotRequired[int]
  """Undocumented; account index operating the market (`1` for perps, `0` for spot markets observed). Mainnet only."""
  settlement_cap: NotRequired[int]
  """Undocumented; `0` on every current market (appears to belong to event/outcome-settled markets). Mainnet only."""
  settlement_type: NotRequired[int]
  """Undocumented; `0` on every current market (appears to belong to event/outcome-settled markets). Mainnet only."""
  settlement_price: NotRequired[int]
  """Undocumented; `0` on every current market (appears to belong to event/outcome-settled markets). Mainnet only."""
  default_price: NotRequired[int]
  """Undocumented; `0` on every current market (appears to belong to event/outcome-settled markets). Mainnet only."""
  is_frozen: NotRequired[bool]
  """Undocumented; whether the market is frozen (`false` on every current market). Mainnet only."""


class Request(TypedDict):
  market_id: NotRequired[int]
  """Only this market. Omit for every market (the venue's `255` sentinel)."""
  filter: NotRequired[Literal['all', 'spot', 'perp']]
  """Only spot or only perp markets; `all` (the default) returns both."""


class OrderBooks(TypedDict):
  """Static metadata of markets."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Extra detail from the server, when it sends any."""
  order_books: list[OrderBook]
  """Markets."""


class OrderBooksEndpoint(RpcEndpoint):
  """Get the static metadata of every market (or one): symbol, type, fees, order minimums and the decimals prices and sizes are scaled by when signing. Fees are in percent."""

  async def order_books(
    self,
    market_id: int | None = None,
    *,
    filter: Literal['all', 'spot', 'perp'] | None = None,
    validate: bool | None = None,
  ) -> OrderBooks:
    """Get the static metadata of every market (or one): symbol, type, fees, order minimums and the decimals prices and sizes are scaled by when signing. Fees are in percent.

    Args:
      market_id: Only this market. Omit for every market (the venue's `255` sentinel).
      filter: Only spot or only perp markets; `all` (the default) returns both.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/orderbooks)
    """
    request: Request = Request()
    if market_id is not None:
      request['market_id'] = market_id
    if filter is not None:
      request['filter'] = filter
    return await self.request(
      request,
      method='GET',
      path='/api/v1/orderBooks',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=OrderBooks,
    )
