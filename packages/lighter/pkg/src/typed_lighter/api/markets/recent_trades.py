# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint
from typed_lighter.schemas import Trade


class Request(TypedDict):
  market_id: int
  """Market id."""
  limit: int
  """Number of trades."""


class RecentTrades(TypedDict):
  """A market's most recent trades."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Optional human-readable detail; usually absent on success."""
  trades: list[Trade] | None
  """Trades, newest first. robinhood and robinhood-testnet send `null` instead of an empty list for a market with no trades; mainnet and testnet send `[]`."""


class RecentTradesEndpoint(RpcEndpoint):
  """Get the most recent trades of a market, newest first."""

  async def recent_trades(
    self,
    *,
    market_id: int,
    limit: int,
    validate: bool | None = None,
  ) -> RecentTrades:
    """Get the most recent trades of a market, newest first.

    Args:
      market_id: Market id.
      limit: Number of trades.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/recenttrades)
    """
    return await self.request(
      Request(market_id=market_id, limit=limit),
      method='GET',
      path='/api/v1/recentTrades',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=RecentTrades,
    )
