# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class LeaseOption(TypedDict):
  """One LIT lease tier: its duration and annual rate."""

  duration_days: int
  """Lease duration, in days."""
  annual_rate: float
  """Annual rate, in percent (`25` means 25%)."""


class Request(TypedDict): ...


class LeaseOptions(TypedDict):
  """The available LIT lease tiers."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Optional detail message."""
  options: list[LeaseOption]
  """Lease tiers, sorted by duration descending."""
  lit_incentives_account_index: int
  """Account that receives the leasing fee."""


class LeaseOptionsEndpoint(RpcEndpoint):
  """Get the available LIT lease tiers (duration and annual rate), longest first."""

  async def lease_options(self, *, validate: bool | None = None) -> LeaseOptions:
    """Get the available LIT lease tiers (duration and annual rate), longest first.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/leaseoptions)
    """
    return await self.request(
      Request(),
      method='GET',
      path='/api/v1/leaseOptions',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=LeaseOptions,
    )
