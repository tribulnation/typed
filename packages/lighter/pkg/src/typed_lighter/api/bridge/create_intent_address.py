# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class IntentAddress(TypedDict):
  """A CCTP intent (deposit) address."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Optional human-readable detail; absent on most successful replies."""
  intent_address: str
  """Address on the source chain to send USDC to."""


class Request(TypedDict):
  chain_id: str
  """Source chain id, as listed by `deposit_networks` (e.g. `42161`)."""
  from_addr: str
  """L1 address whose Lighter account receives the deposit."""
  amount: str
  """Amount to deposit, as a string (unit undocumented)."""
  is_external_deposit: NotRequired[bool]
  """Undocumented flag."""


class CreateIntentAddress(RpcEndpoint):
  """Create a CCTP intent (deposit) address: USDC sent to it on the source chain is bridged to the L1 address's Lighter account."""

  async def create_intent_address(
    self,
    *,
    chain_id: str,
    from_addr: str,
    amount: str,
    is_external_deposit: bool | None = None,
    validate: bool | None = None,
  ) -> IntentAddress:
    """Create a CCTP intent (deposit) address: USDC sent to it on the source chain is bridged to the L1 address's Lighter account.

    Args:
      chain_id: Source chain id, as listed by `deposit_networks` (e.g. `42161`).
      from_addr: L1 address whose Lighter account receives the deposit.
      amount: Amount to deposit, as a string (unit undocumented).
      is_external_deposit: Undocumented flag.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/createintentaddress)
    """
    request: Request = Request(chain_id=chain_id, from_addr=from_addr, amount=amount)
    if is_external_deposit is not None:
      request['is_external_deposit'] = is_external_deposit
    return await self.request(
      request,
      method='POST',
      path='/api/v1/createIntentAddress',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=IntentAddress,
    )
