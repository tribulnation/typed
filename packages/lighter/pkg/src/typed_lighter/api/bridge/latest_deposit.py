# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_lighter.core import TimestampSeconds
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class Deposit(TypedDict):
  """The most recent bridge deposit of an L1 address, with its progress."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Extra detail from the server, when it sends any."""
  source: str
  """Source network name, e.g. `Arbitrum One`."""
  source_chain_id: str
  """EVM chain id of the source network, as a string (e.g. `42161`)."""
  fast_bridge_tx_hash: str
  """Fast-bridge transaction hash; empty when the deposit did not go through the fast bridge."""
  batch_claim_tx_hash: str
  """Hash of the L1 transaction that claimed the deposit batch."""
  cctp_burn_tx_hash: str
  """Hash of the CCTP burn transaction on the source network."""
  amount: Decimal
  """Deposited amount, in USDC."""
  intent_address: str
  """Intent address the funds were sent to on the source network."""
  status: str
  """Deposit status, e.g. `completed`. The full set is undocumented."""
  step: str
  """Progress step of the bridge flow, as a numeric string (`9` = completed observed)."""
  description: str
  """Human-readable progress, e.g. `Bridge completed`."""
  created_at: TimestampSeconds
  """When the deposit was detected."""
  updated_at: TimestampSeconds
  """When the deposit last changed."""
  is_external_deposit: bool
  """Whether the deposit came from an external address rather than the account's own wallet."""
  is_next_bridge_fast: bool
  """Whether the address's next deposit is eligible for the fast bridge."""


class Request(TypedDict):
  l1_address: str
  """L1 address that made the deposit."""


class LatestDeposit(RpcEndpoint):
  """Get the most recent bridge deposit (Arbitrum, Base, Avalanche, ... through an intent address) of an L1 address, with its progress."""

  async def latest_deposit(
    self, l1_address: str, *, validate: bool | None = None
  ) -> Deposit:
    """Get the most recent bridge deposit (Arbitrum, Base, Avalanche, ... through an intent address) of an L1 address, with its progress.

    Args:
      l1_address: L1 address that made the deposit.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/deposit_latest)
    """
    return await self.request(
      Request(l1_address=l1_address),
      method='GET',
      path='/api/v1/deposit/latest',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Deposit,
    )
