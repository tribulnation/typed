# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class BridgeSupportedNetwork(TypedDict):
  """One network that supports intent-address deposits."""

  name: str
  """Network name, e.g. `Base`."""
  chain_id: str
  """EVM chain id, as a string (e.g. `8453`)."""
  explorer: str
  """Block-explorer URL of the network; empty on testnet."""


class Request(TypedDict): ...


class BridgeSupportedNetworks(TypedDict):
  """Networks that support deposits through an intent address."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Extra detail from the server, when it sends any."""
  networks: list[BridgeSupportedNetwork]
  """Supported networks."""


class DepositNetworks(RpcEndpoint):
  """Get the networks that support deposits through an intent address (CCTP)."""

  async def deposit_networks(
    self,
    *,
    validate: bool | None = None,
  ) -> BridgeSupportedNetworks:
    """Get the networks that support deposits through an intent address (CCTP).

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/deposit_networks)
    """
    return await self.request(
      Request(),
      method='GET',
      path='/api/v1/deposit/networks',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=BridgeSupportedNetworks,
    )
