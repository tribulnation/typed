# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class FastBridgeInfo(TypedDict):
  """The current fast-bridge limit."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Extra detail from the server, when it sends any."""
  fast_bridge_limit: Decimal
  """Current fast-bridge limit, in USDC."""


class Request(TypedDict): ...


class FastBridgeInfoEndpoint(RpcEndpoint):
  """Get the current fast-bridge limit: the largest deposit the fast bridge can front right now."""

  async def fast_bridge_info(self, *, validate: bool | None = None) -> FastBridgeInfo:
    """Get the current fast-bridge limit: the largest deposit the fast bridge can front right now.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/fastbridge_info)
    """
    return await self.request(
      Request(),
      method='GET',
      path='/api/v1/fastbridge/info',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=FastBridgeInfo,
    )
