# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint
from typed_lighter.schemas import RFQResult


class Request(TypedDict):
  market_index: int
  """Market id."""
  base_amount: NotRequired[int]
  """Size in base units, as a positive integer string (scaled by the market's size decimals; `109` = 0.0109 ETH); `64003` otherwise."""
  quote_amount: NotRequired[int]
  """Size in quote units, as an integer string (the unit's scaling is undocumented)."""
  direction: Literal[0, 1]
  """Requested direction, `0` or `1` (Lighter names neither); `-1` is refused."""
  metadata: NotRequired[str]
  """Optional metadata string; its format is undocumented."""


class Create(RpcEndpoint):
  """Request a quote for a market. Makers have up to 1 minute to respond, after which the requester has up to 2 minutes to trade; quotes are not locked (makers place orders on the book). Give either a base or a quote amount."""

  async def create(
    self,
    *,
    market_index: int,
    base_amount: int | None = None,
    quote_amount: int | None = None,
    direction: Literal[0, 1],
    metadata: str | None = None,
    validate: bool | None = None,
  ) -> RFQResult:
    """Request a quote for a market. Makers have up to 1 minute to respond, after which the requester has up to 2 minutes to trade; quotes are not locked (makers place orders on the book). Give either a base or a quote amount.

    Args:
      market_index: Market id.
      base_amount: Size in base units, as a positive integer string (scaled by the market's size decimals; `109` = 0.0109 ETH); `64003` otherwise.
      quote_amount: Size in quote units, as an integer string (the unit's scaling is undocumented).
      direction: Requested direction, `0` or `1` (Lighter names neither); `-1` is refused.
      metadata: Optional metadata string; its format is undocumented.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/rfq_create)
    """
    request: Request = Request(market_index=market_index, direction=direction)
    if base_amount is not None:
      request['base_amount'] = base_amount
    if quote_amount is not None:
      request['quote_amount'] = quote_amount
    if metadata is not None:
      request['metadata'] = metadata
    return await self.request(
      request,
      method='POST',
      path='/api/v1/rfq/create',
      meta={'auth': 'write'},
      validate=validate,
      request_type=Request,
      response_type=RFQResult,
    )
