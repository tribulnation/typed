# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint
from typed_lighter.schemas import RFQResult


class Request(TypedDict):
  rfq_id: int
  """RFQ id."""
  status: Literal['order_created', 'closed']
  """New status."""


class Update(RpcEndpoint):
  """Change the status of the account's own RFQ: mark that an order was created from it, or close it."""

  async def update(
    self,
    rfq_id: int,
    *,
    status: Literal['order_created', 'closed'],
    validate: bool | None = None,
  ) -> RFQResult:
    """Change the status of the account's own RFQ: mark that an order was created from it, or close it.

    Args:
      rfq_id: RFQ id.
      status: New status.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/rfq_update)
    """
    return await self.request(
      Request(rfq_id=rfq_id, status=status),
      method='POST',
      path='/api/v1/rfq/update',
      meta={'auth': 'write'},
      validate=validate,
      request_type=Request,
      response_type=RFQResult,
    )
