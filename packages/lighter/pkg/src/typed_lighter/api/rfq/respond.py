# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint
from typed_lighter.schemas import RFQResult


class Request(TypedDict):
  rfq_id: int
  """RFQ id."""
  status: Literal['acknowledged', 'liquidity_provided', 'not_interested']
  """The maker's answer."""


class Respond(RpcEndpoint):
  """Respond to an RFQ as a market maker (whitelisted maker accounts only)."""

  async def respond(
    self,
    rfq_id: int,
    *,
    status: Literal['acknowledged', 'liquidity_provided', 'not_interested'],
    validate: bool | None = None,
  ) -> RFQResult:
    """Respond to an RFQ as a market maker (whitelisted maker accounts only).

    Args:
      rfq_id: RFQ id.
      status: The maker's answer.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/rfq_respond)
    """
    return await self.request(
      Request(rfq_id=rfq_id, status=status),
      method='POST',
      path='/api/v1/rfq/respond',
      meta={'auth': 'write'},
      validate=validate,
      request_type=Request,
      response_type=RFQResult,
    )
