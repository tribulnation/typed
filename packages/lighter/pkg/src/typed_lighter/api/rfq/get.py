# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint
from typed_lighter.schemas import RFQResult


class Request(TypedDict):
  rfq_id: int
  """RFQ id."""


class Get(RpcEndpoint):
  """Get one RFQ by id."""

  async def get(self, rfq_id: int, *, validate: bool | None = None) -> RFQResult:
    """Get one RFQ by id.

    Args:
      rfq_id: RFQ id.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/rfq_get)
    """
    return await self.request(
      Request(rfq_id=rfq_id),
      method='GET',
      path='/api/v1/rfq/get',
      meta={'auth': 'token'},
      validate=validate,
      request_type=Request,
      response_type=RFQResult,
    )
