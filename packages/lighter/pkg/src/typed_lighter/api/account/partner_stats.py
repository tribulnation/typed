# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_lighter.core import TimestampMillis
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class PartnerStats(TypedDict):
  """An integrator account's fee earnings, volumes, trade counts and unique clients."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Optional human-readable detail; usually absent on success."""
  total_fees_earned: Decimal
  """Integrator fees earned, in USDC."""
  total_taker_fees_earned: Decimal
  """Integrator fees earned on taker fills, in USDC."""
  total_maker_fees_earned: Decimal
  """Integrator fees earned on maker fills, in USDC."""
  total_volume: Decimal
  """Volume routed, in USDC."""
  total_taker_volume: Decimal
  """Taker volume routed, in USDC."""
  total_maker_volume: Decimal
  """Maker volume routed, in USDC."""
  total_trades: int
  """Trades routed."""
  total_taker_trades: int
  """Taker trades routed."""
  total_maker_trades: int
  """Maker trades routed."""
  unique_clients: int
  """Distinct accounts that routed orders through the integrator."""


class Request(TypedDict):
  account_index: int
  """Integrator account index."""
  start_timestamp: NotRequired[TimestampMillis]
  """Start of the range."""
  end_timestamp: NotRequired[TimestampMillis]
  """End of the range."""
  period: NotRequired[Literal['7d', '30d']]
  """Trailing period instead of a range; other values (`1d`, `24h`, `90d`, `1y`, `all`, ...) are refused with `20001`."""


class PartnerStatsEndpoint(RpcEndpoint):
  """Get an integrator (partner) account's fee earnings, volumes, trade counts and unique clients; all-time when no range or period is given."""

  async def partner_stats(
    self,
    account_index: int,
    *,
    start_timestamp: TimestampMillis | None = None,
    end_timestamp: TimestampMillis | None = None,
    period: Literal['7d', '30d'] | None = None,
    validate: bool | None = None,
  ) -> PartnerStats:
    """Get an integrator (partner) account's fee earnings, volumes, trade counts and unique clients; all-time when no range or period is given.

    Args:
      account_index: Integrator account index.
      start_timestamp: Start of the range.
      end_timestamp: End of the range.
      period: Trailing period instead of a range; other values (`1d`, `24h`, `90d`, `1y`, `all`, ...) are refused with `20001`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/partnerstats)
    """
    request: Request = Request(account_index=account_index)
    if start_timestamp is not None:
      request['start_timestamp'] = start_timestamp
    if end_timestamp is not None:
      request['end_timestamp'] = end_timestamp
    if period is not None:
      request['period'] = period
    return await self.request(
      request,
      method='GET',
      path='/api/v1/partnerStats',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=PartnerStats,
    )
