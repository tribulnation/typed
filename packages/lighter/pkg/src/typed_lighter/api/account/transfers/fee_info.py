# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  account_index: int
  """Sending account."""
  to_account_index: NotRequired[int]
  """Receiving account; the venue's default fee when omitted."""


class TransferFeeInfo(TypedDict):
  """The USDC fee of a transfer."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Optional human-readable detail; usually absent on success."""
  transfer_fee_usdc: int
  """Fee in USDC base units (1e-6): `3000000` = 3 USDC, `0` between accounts of the same master."""


class FeeInfo(RpcEndpoint):
  """Get the USDC fee of a transfer from an account (to another account, when given)."""

  async def fee_info(
    self,
    *,
    account_index: int,
    to_account_index: int | None = None,
    validate: bool | None = None,
  ) -> TransferFeeInfo:
    """Get the USDC fee of a transfer from an account (to another account, when given).

    Args:
      account_index: Sending account.
      to_account_index: Receiving account; the venue's default fee when omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/transferfeeinfo)
    """
    request: Request = Request(account_index=account_index)
    if to_account_index is not None:
      request['to_account_index'] = to_account_index
    return await self.request(
      request,
      method='GET',
      path='/api/v1/transferFeeInfo',
      meta={'auth': 'optional'},
      validate=validate,
      request_type=Request,
      response_type=TransferFeeInfo,
    )
