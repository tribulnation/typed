# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class FastWithdrawInfo(TypedDict):
  """An account's fast-withdrawal limits."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Optional human-readable detail; usually absent on success."""
  to_account_index: int
  """Account fast withdrawals are routed through."""
  withdraw_limit: Decimal
  """Remaining fast-withdrawal liquidity."""
  max_withdrawal_amount: Decimal
  """Largest amount the account can fast-withdraw now."""


class Request(TypedDict):
  account_index: int
  """Account index (master account, sub-account or pool)."""


class FastWithdrawInfoEndpoint(RpcEndpoint):
  """Get an account's fast-withdrawal limits: the remaining liquidity and the maximum it can fast-withdraw now."""

  async def fast_withdraw_info(
    self,
    account_index: int,
    *,
    validate: bool | None = None,
  ) -> FastWithdrawInfo:
    """Get an account's fast-withdrawal limits: the remaining liquidity and the maximum it can fast-withdraw now.

    Args:
      account_index: Account index (master account, sub-account or pool).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/fastwithdraw_info)
    """
    return await self.request(
      Request(account_index=account_index),
      method='GET',
      path='/api/v1/fastwithdraw/info',
      meta={'auth': 'token'},
      validate=validate,
      request_type=Request,
      response_type=FastWithdrawInfo,
    )
