# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_lighter.core import TimestampSeconds
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class AccountLimits(TypedDict):
  """An account's tier and limits."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Optional human-readable detail; usually absent on success."""
  max_llp_percentage: int
  """Maximum share of the account's value that may go into the LLP, in percent."""
  max_llp_amount: Decimal
  """Maximum LLP deposit, in USDC."""
  user_tier: str
  """Account tier: `standard`, `plus` or `premium`, per the docs."""
  user_tier_name: str
  """Display name of the tier."""
  can_create_public_pool: bool
  """Whether the account may create a public pool."""
  current_maker_fee_tick: int
  """Current maker fee tier."""
  current_taker_fee_tick: int
  """Current taker fee tier."""
  leased_lit: Decimal
  """Total actively leased LIT."""
  effective_lit_stakes: Decimal
  """Effective staked LIT, including active leases."""
  user_tier_last_update: TimestampSeconds
  """When the tier last changed; tier changes are limited to one per 24 hours."""


class Request(TypedDict):
  account_index: int
  """Account index (master account, sub-account or pool)."""


class Limits(RpcEndpoint):
  """Get an account's tier and limits: LLP deposit caps, fee ticks, leased and staked LIT."""

  async def limits(
    self,
    account_index: int,
    *,
    validate: bool | None = None,
  ) -> AccountLimits:
    """Get an account's tier and limits: LLP deposit caps, fee ticks, leased and staked LIT.

    Args:
      account_index: Account index (master account, sub-account or pool).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/accountlimits)
    """
    return await self.request(
      Request(account_index=account_index),
      method='GET',
      path='/api/v1/accountLimits',
      meta={'auth': 'token'},
      validate=validate,
      request_type=Request,
      response_type=AccountLimits,
    )
