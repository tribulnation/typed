# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint
from typed_lighter.schemas import ResultCode


class Request(TypedDict):
  account_index: int
  """Account index the auth token belongs to (a master or sub-account)."""
  new_tier: Literal['standard', 'plus', 'premium']
  """Tier to switch to."""


class ChangeTier(RpcEndpoint):
  """Switch the L1 address's account tier (Standard, Plus, Premium). A downgrade needs 24 hours since the last change; an upgrade has no cooldown."""

  async def change_tier(
    self,
    account_index: int,
    *,
    new_tier: Literal['standard', 'plus', 'premium'],
    validate: bool | None = None,
  ) -> ResultCode:
    """Switch the L1 address's account tier (Standard, Plus, Premium). A downgrade needs 24 hours since the last change; an upgrade has no cooldown.

    Args:
      account_index: Account index the auth token belongs to (a master or sub-account).
      new_tier: Tier to switch to.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/changeaccounttier)
    """
    return await self.request(
      Request(account_index=account_index, new_tier=new_tier),
      method='POST',
      path='/api/v1/changeAccountTier',
      meta={'auth': 'write'},
      validate=validate,
      request_type=Request,
      response_type=ResultCode,
    )
