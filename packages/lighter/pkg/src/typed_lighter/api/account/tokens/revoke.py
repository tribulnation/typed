# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  token_id: int
  """Id of the token, used to revoke it."""
  account_index: int
  """Account index the auth token belongs to (a master or sub-account)."""


class RevokedApiToken(TypedDict):
  """Result of revoking a read-only API token."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Optional human-readable detail; absent on most successful replies."""
  token_id: int
  """Id of the token, used to revoke it."""
  revoked: bool
  """Whether the token is now revoked."""


class Revoke(RpcEndpoint):
  """Revoke one of the account's read-only API tokens."""

  async def revoke(
    self,
    *,
    token_id: int,
    account_index: int,
    validate: bool | None = None,
  ) -> RevokedApiToken:
    """Revoke one of the account's read-only API tokens.

    Args:
      token_id: Id of the token, used to revoke it.
      account_index: Account index the auth token belongs to (a master or sub-account).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/tokens_revoke)
    """
    return await self.request(
      Request(token_id=token_id, account_index=account_index),
      method='POST',
      path='/api/v1/tokens/revoke',
      meta={'auth': 'write'},
      validate=validate,
      request_type=Request,
      response_type=RevokedApiToken,
    )
