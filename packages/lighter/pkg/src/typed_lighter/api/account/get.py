# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_lighter.core import TimestampMicros, TimestampMillis, TimestampSeconds
from typed_lighter.core.endpoint.rpc import RpcEndpoint
from typed_lighter.schemas import (
  AccountAsset,
  AccountPosition,
  PublicPoolInfo,
  PublicPoolShare,
  SubAccountMetadata,
)


class AccountByIndex(TypedDict):
  by: Literal['index']
  """Look up by account index."""
  value: int
  """Account index."""
  active_only: NotRequired[bool]
  """Hide flat markets the account traded before (those with leverage/margin settings but no position)."""
  cursor: NotRequired[str]
  """Opaque cursor; accepted and validated (`20009 invalid cursor`) but no response carries a next cursor."""


class AccountByL1Address(TypedDict):
  by: Literal['l1_address']
  """Look up by L1 address."""
  value: str
  """L1 address; returns its master account and every sub-account."""
  active_only: NotRequired[bool]
  """Hide flat markets the account traded before (those with leverage/margin settings but no position)."""
  cursor: NotRequired[str]
  """Opaque cursor; accepted and validated (`20009 invalid cursor`) but no response carries a next cursor."""


class ApprovedIntegrator(TypedDict):
  """An integrator approved to charge fees on the account's orders, with its fee caps and approval expiry."""

  account_index: int
  """Integrator account."""
  name: str
  """Integrator name."""
  max_perps_taker_fee: int
  """Maximum perps taker fee the integrator may charge."""
  max_perps_maker_fee: int
  """Maximum perps maker fee the integrator may charge."""
  max_spot_taker_fee: int
  """Maximum spot taker fee the integrator may charge."""
  max_spot_maker_fee: int
  """Maximum spot maker fee the integrator may charge."""
  approval_expiry: int
  """When the approval expires, in epoch milliseconds (per the partner-integration docs); `281474976710655` (2^48-1) means never, which is past the largest representable date, so the field stays a plain integer."""


class PendingUnlock(TypedDict):
  """An unstaking request still in its lockup period."""

  unlock_timestamp: NotRequired[TimestampMillis]
  """When the unstaked assets unlock."""
  asset_index: NotRequired[int]
  """Asset id being unlocked."""
  amount: NotRequired[Decimal]
  """Amount being unlocked."""


class DetailedAccount(TypedDict):
  """One account with positions, balances and pool details."""

  code: int
  """Per-account status code, normally `0`; only the top-level `code` signals an error."""
  message: NotRequired[str]
  """Optional human-readable detail; usually absent on success."""
  account_type: int
  """Account kind. Lighter documents no numeric mapping; observed values: `0` a master account, `1` a sub-account, `2` a user-operated public pool, `3` the LLP, `4` a staking pool."""
  account_trading_mode: Literal[0, 1]
  """`0` classic (separate spot and perps balances), `1` unified (USDC shared by spot and perps)."""
  index: int
  """Account index."""
  l1_address: str
  """L1 address owning the account; the zero address for protocol pools."""
  cancel_all_time: TimestampMillis
  """Time of a scheduled cancel-all (dead man's switch); `0` when none is scheduled, which parses to the Unix epoch (1970-01-01 UTC)."""
  total_order_count: int
  """Open orders of the account."""
  total_isolated_order_count: int
  """Open orders in isolated-margin positions."""
  pending_order_count: int
  """Untriggered conditional orders."""
  available_balance: Decimal
  """Collateral available for new orders."""
  status: int
  """Account status code (values undocumented; `1` active and `0` inactive observed)."""
  collateral: Decimal
  """Perps collateral (USDC)."""
  transaction_time: TimestampMicros
  """Time of the account's last transaction; `0` in `accountsByL1Address`."""
  account_index: int
  """Account index (same as `index`)."""
  name: str
  """Display name; `""` when unset."""
  description: str
  """Display description; `""` when unset."""
  can_invite: bool
  """Whether the account can invite referrals (duplicate of `l1Metadata`)."""
  referral_points_percentage: Decimal | Literal['']
  """Referral points share, in percent; `""` when unset (duplicate of `l1Metadata`)."""
  can_rfq: bool
  """Whether the account may use RFQ."""
  can_rfq_market_ids: list[int]
  """Markets the account may request quotes on; empty means no restriction list."""
  agent_enabled: bool
  """Whether agent trading is enabled for the account (undocumented)."""
  created_at: TimestampSeconds
  """When the account metadata was created; `0` when never set."""
  metadata: SubAccountMetadata
  """The account's free-form metadata, as set with `setAccountMetadata`."""
  positions: list[AccountPosition]
  """Per-market positions, including flat markets the account traded before (unless `active_only`)."""
  bo_positions: NotRequired[list[dict[str, Any]]]
  """Undocumented; an empty list whenever present."""
  assets: list[AccountAsset]
  """Spot balances."""
  total_asset_value: Decimal
  """Total account value, in USDC."""
  cross_asset_value: Decimal
  """Account value in cross margin, in USDC."""
  cross_initial_margin_requirement: Decimal
  """Cross initial margin requirement, in USDC."""
  cross_maintenance_margin_requirement: Decimal
  """Cross maintenance margin requirement, in USDC."""
  pool_info: NotRequired[PublicPoolInfo]
  """Public or staking pool details; present only on pool accounts."""
  shares: list[PublicPoolShare]
  """Shares the account holds in public and staking pools."""
  pending_unlocks: list[PendingUnlock]
  """Unstaking requests still in their lockup period."""
  approved_integrators: NotRequired[list[ApprovedIntegrator]]
  """Integrators approved to charge fees on this account's orders; absent when none."""


Request = AccountByIndex | AccountByL1Address


class DetailedAccounts(TypedDict):
  """Accounts matching the lookup, with positions, balances and pool details."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Optional human-readable detail; usually absent on success."""
  total: int
  """Number of accounts returned."""
  accounts: list[DetailedAccount]
  """Matching accounts."""


class Get(RpcEndpoint):
  """Get accounts by account index, or every account (master and sub-accounts) of an L1 address, with positions, spot balances, pool shares and pool details."""

  async def get(
    self,
    account_lookup: Request,
    *,
    validate: bool | None = None,
  ) -> DetailedAccounts:
    """Get accounts by account index, or every account (master and sub-accounts) of an L1 address, with positions, spot balances, pool shares and pool details.

    Args:
      account_lookup: Which account(s) to look up: by account index or by L1 address.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/account-1)
    """
    return await self.request(
      account_lookup,
      method='GET',
      path='/api/v1/account',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=DetailedAccounts,
    )
