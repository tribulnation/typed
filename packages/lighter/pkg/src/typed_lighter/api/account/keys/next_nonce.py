# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class NextNonce(TypedDict):
  """The next nonce an API key must sign with."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Optional human-readable detail; usually absent on success."""
  nonce: int
  """Next nonce."""


class Request(TypedDict):
  account_index: int
  """Account index (master account, sub-account or pool)."""
  api_key_index: int
  """Key slot."""


class NextNonceEndpoint(RpcEndpoint):
  """Get the next nonce an API key must sign with."""

  async def next_nonce(
    self,
    *,
    account_index: int,
    api_key_index: int,
    validate: bool | None = None,
  ) -> NextNonce:
    """Get the next nonce an API key must sign with.

    Args:
      account_index: Account index (master account, sub-account or pool).
      api_key_index: Key slot.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/nextnonce)
    """
    return await self.request(
      Request(account_index=account_index, api_key_index=api_key_index),
      method='GET',
      path='/api/v1/nextNonce',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=NextNonce,
    )
