# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class MakerOnlyApiKeys(TypedDict):
  """The account's maker-only API key slots."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Optional human-readable detail; usually absent on success."""
  api_key_indexes: list[int]
  """Maker-only key slots."""


class Request(TypedDict):
  account_index: int
  """Account index (master account, sub-account or pool)."""


class MakerOnly(RpcEndpoint):
  """Get the API key slots marked maker-only (post-only, modify, cancel and cancel-all; Premium accounts)."""

  async def maker_only(
    self,
    account_index: int,
    *,
    validate: bool | None = None,
  ) -> MakerOnlyApiKeys:
    """Get the API key slots marked maker-only (post-only, modify, cancel and cancel-all; Premium accounts).

    Args:
      account_index: Account index (master account, sub-account or pool).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/getmakeronlyapikeys)
    """
    return await self.request(
      Request(account_index=account_index),
      method='GET',
      path='/api/v1/getMakerOnlyApiKeys',
      meta={'auth': 'token'},
      validate=validate,
      request_type=Request,
      response_type=MakerOnlyApiKeys,
    )
