# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint
from typed_lighter.schemas import ResultCode


class Request(TypedDict):
  account_index: int
  """Account index the auth token belongs to (a master or sub-account)."""
  api_key_indexes: list[int]
  """Every key to mark maker-only; the list replaces the current one, and an empty list clears it."""


class SetMakerOnly(RpcEndpoint):
  """Replace the account's list of maker-only API keys (post-only orders, modifying them, cancels). Premium accounts only; keys 0-3 are reserved; one change per hour."""

  async def set_maker_only(
    self,
    account_index: int,
    *,
    api_key_indexes: list[int],
    validate: bool | None = None,
  ) -> ResultCode:
    """Replace the account's list of maker-only API keys (post-only orders, modifying them, cancels). Premium accounts only; keys 0-3 are reserved; one change per hour.

    Args:
      account_index: Account index the auth token belongs to (a master or sub-account).
      api_key_indexes: Every key to mark maker-only; the list replaces the current one, and an empty list clears it.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/setmakeronlyapikeys)
    """
    return await self.request(
      Request(account_index=account_index, api_key_indexes=api_key_indexes),
      method='POST',
      path='/api/v1/setMakerOnlyApiKeys',
      meta={'auth': 'write'},
      validate=validate,
      request_type=Request,
      response_type=ResultCode,
    )
