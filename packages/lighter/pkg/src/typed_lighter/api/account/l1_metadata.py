# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_lighter.core import TimestampMillis
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class ReferralProgramKickback(TypedDict):
  """One past change of the referral kickback share."""

  date: TimestampMillis
  """When the kickback share was set."""
  percentage: Decimal
  """Kickback percentage set then."""


class Request(TypedDict):
  l1_address: str
  """Ethereum (L1) address, `0x`-prefixed hex."""


class L1Metadata(TypedDict):
  """Referral settings of an L1 address."""

  code: NotRequired[int]
  """`200` on success."""
  message: NotRequired[str]
  """Optional human-readable detail; usually absent on success."""
  l1_address: str
  """The queried L1 address."""
  can_invite: bool
  """Whether the address can invite referrals."""
  can_create_guild: bool
  """Whether the address can create a guild (undocumented)."""
  referral_points_percentage: Decimal
  """Referral points share, in percent."""
  referral_program_kickback: Decimal
  """Share of referral rewards kicked back to referred users, in percent."""
  referral_program_kickback_history: list[ReferralProgramKickback]
  """Past kickback changes."""


class L1MetadataEndpoint(RpcEndpoint):
  """Get the referral settings of an L1 address: invite rights, referral points share and kickback."""

  async def l1_metadata(
    self, l1_address: str, *, validate: bool | None = None
  ) -> L1Metadata:
    """Get the referral settings of an L1 address: invite rights, referral points share and kickback.

    Args:
      l1_address: Ethereum (L1) address, `0x`-prefixed hex.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/l1metadata)
    """
    return await self.request(
      Request(l1_address=l1_address),
      method='GET',
      path='/api/v1/l1Metadata',
      meta={'auth': 'token'},
      validate=validate,
      request_type=Request,
      response_type=L1Metadata,
    )
