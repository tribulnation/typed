# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint
from typed_lighter.schemas import Orders


class Request(TypedDict):
  account_index: int
  """Account index (master account, sub-account or pool)."""
  market_id: NotRequired[int]
  """Only this market; every market when omitted (the venue's `255` sentinel)."""
  market_type: NotRequired[Literal['all', 'spot', 'perp']]
  """Only spot or only perp markets; both when omitted."""


class Active(RpcEndpoint):
  """Get an account's open orders, including untriggered conditional orders."""

  async def active(
    self,
    *,
    account_index: int,
    market_id: int | None = None,
    market_type: Literal['all', 'spot', 'perp'] | None = None,
    validate: bool | None = None,
  ) -> Orders:
    """Get an account's open orders, including untriggered conditional orders.

    Args:
      account_index: Account index (master account, sub-account or pool).
      market_id: Only this market; every market when omitted (the venue's `255` sentinel).
      market_type: Only spot or only perp markets; both when omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/accountactiveorders)
    """
    request: Request = Request(account_index=account_index)
    if market_id is not None:
      request['market_id'] = market_id
    if market_type is not None:
      request['market_type'] = market_type
    return await self.request(
      request,
      method='GET',
      path='/api/v1/accountActiveOrders',
      meta={'auth': 'token'},
      validate=validate,
      request_type=Request,
      response_type=Orders,
    )
