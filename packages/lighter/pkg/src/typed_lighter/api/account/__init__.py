# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .by_l1_address import ByL1Address
from .change_tier import ChangeTier
from .export import Export
from .get import Get
from .keys import Keys
from .l1_metadata import L1MetadataEndpoint
from .limits import Limits
from .liquidations import Liquidations
from .metadata import Metadata
from .orders import Orders
from .partner_stats import PartnerStatsEndpoint
from .pnl import Pnl
from .position_funding import PositionFundingEndpoint
from .set_metadata import SetMetadata
from .tokens import Tokens
from .transfers import Transfers


class Account(
  ByL1Address,
  ChangeTier,
  Export,
  Get,
  L1MetadataEndpoint,
  Limits,
  Liquidations,
  Metadata,
  PartnerStatsEndpoint,
  Pnl,
  PositionFundingEndpoint,
  SetMetadata,
):
  """Accounts by index or L1 address, metadata, limits, PnL, fundings, liquidations, exports and account settings.

  References:
    - [Upstream docs](https://apidocs.lighter.xyz/reference/account-1)
  """

  @cached_property
  def keys(self) -> Keys:
    """An account's registered API keys, their next nonces, and maker-only key settings.

    References:
      - [Upstream docs](https://apidocs.lighter.xyz/docs/api-keys)
    """
    return Keys(client=self.client)

  @cached_property
  def orders(self) -> Orders:
    """An account's open and closed orders, orders by client index, and its trades.

    References:
      - [Upstream docs](https://apidocs.lighter.xyz/reference/accountactiveorders)
    """
    return Orders(client=self.client)

  @cached_property
  def tokens(self) -> Tokens:
    """Server-issued read-only API tokens: list, create and revoke.

    References:
      - [Upstream docs](https://apidocs.lighter.xyz/reference/tokens)
    """
    return Tokens(client=self.client)

  @cached_property
  def transfers(self) -> Transfers:
    """An account's deposit, withdrawal and transfer history, transfer fees and fast-withdrawal limits.

    References:
      - [Upstream docs](https://apidocs.lighter.xyz/docs/deposits-transfers-and-withdrawals)
    """
    return Transfers(client=self.client)
