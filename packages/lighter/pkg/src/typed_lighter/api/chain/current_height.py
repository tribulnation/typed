# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint


class CurrentHeight(TypedDict):
  """The current L2 block height."""

  code: int
  """`200` on success."""
  message: NotRequired[str]
  """Optional detail message."""
  height: int
  """Height of the latest L2 block."""


class Request(TypedDict): ...


class CurrentHeightEndpoint(RpcEndpoint):
  """Get the current L2 block height. Lighter's mainnet host currently refuses it (CloudFront `403 Request blocked`, raised as `ApiError(403)`); it works on testnet."""

  async def current_height(self, *, validate: bool | None = None) -> CurrentHeight:
    """Get the current L2 block height. Lighter's mainnet host currently refuses it (CloudFront `403 Request blocked`, raised as `ApiError(403)`); it works on testnet.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.
    """
    return await self.request(
      Request(),
      method='GET',
      path='/api/v1/currentHeight',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=CurrentHeight,
    )
