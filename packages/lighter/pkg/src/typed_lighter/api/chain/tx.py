# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint
from typed_lighter.schemas import EnrichedTx


class TxByHash(TypedDict):
  by: Literal['hash']
  """Look the transaction up by hash."""
  value: str
  """Transaction hash (hex, no `0x` prefix)."""


class TxBySequenceIndex(TypedDict):
  by: Literal['sequence_index']
  """Look the transaction up by sequence index."""
  value: int
  """Sequence index of the transaction."""


Request = TxByHash | TxBySequenceIndex


class Tx(RpcEndpoint):
  """Get one L2 transaction by its hash or by its sequence index."""

  async def tx(self, tx_lookup: Request, *, validate: bool | None = None) -> EnrichedTx:
    """Get one L2 transaction by its hash or by its sequence index.

    Args:
      tx_lookup: The transaction to fetch: by hash or by sequence index.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/tx)
    """
    return await self.request(
      tx_lookup,
      method='GET',
      path='/api/v1/tx',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=EnrichedTx,
    )
