# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint
from typed_lighter.schemas import EnrichedTx


class Request(TypedDict):
  hash: str
  """L1 (Ethereum) transaction hash, `0x`-prefixed; without the prefix the lookup answers `21500 transaction not found`."""


class TxFromL1(RpcEndpoint):
  """Get the L2 transaction created by an L1 transaction (a deposit or another priority operation), by the L1 transaction hash."""

  async def tx_from_l1(self, hash: str, *, validate: bool | None = None) -> EnrichedTx:
    """Get the L2 transaction created by an L1 transaction (a deposit or another priority operation), by the L1 transaction hash.

    Args:
      hash: L1 (Ethereum) transaction hash, `0x`-prefixed; without the prefix the lookup answers `21500 transaction not found`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/txfroml1txhash)
    """
    return await self.request(
      Request(hash=hash),
      method='GET',
      path='/api/v1/txFromL1TxHash',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=EnrichedTx,
    )
