# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint
from typed_lighter.schemas import Blocks


class BlockByCommitment(TypedDict):
  by: Literal['commitment']
  """Look the block up by commitment."""
  value: str
  """Block commitment (hex)."""


class BlockByHeight(TypedDict):
  by: Literal['height']
  """Look the block up by height."""
  value: int
  """Block height."""


Request = BlockByHeight | BlockByCommitment


class Block(RpcEndpoint):
  """Get one L2 block by its height or its commitment. Lighter's mainnet host currently refuses it (CloudFront `403 Request blocked`, raised as `ApiError(403)`); it works on testnet."""

  async def block(
    self, block_lookup: Request, *, validate: bool | None = None
  ) -> Blocks:
    """Get one L2 block by its height or its commitment. Lighter's mainnet host currently refuses it (CloudFront `403 Request blocked`, raised as `ApiError(403)`); it works on testnet.

    Args:
      block_lookup: The block to fetch: by height or by commitment.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
    """
    return await self.request(
      block_lookup,
      method='GET',
      path='/api/v1/block',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Blocks,
    )
