# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_lighter.core.endpoint.rpc import RpcEndpoint
from typed_lighter.schemas import ResultCode


class Request(TypedDict):
  notif_id: str
  """Notification id as pushed on the `notification/{account}` stream, e.g. `liq:17:5898`."""
  account_index: int
  """Account index the auth token belongs to (a master or sub-account)."""


class Ack(RpcEndpoint):
  """Acknowledge an account notification (liquidation, deleverage or announcement) so it is no longer pushed on the `notification` stream."""

  async def ack(
    self,
    notif_id: str,
    *,
    account_index: int,
    validate: bool | None = None,
  ) -> ResultCode:
    """Acknowledge an account notification (liquidation, deleverage or announcement) so it is no longer pushed on the `notification` stream.

    Args:
      notif_id: Notification id as pushed on the `notification/{account}` stream, e.g. `liq:17:5898`.
      account_index: Account index the auth token belongs to (a master or sub-account).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/notification_ack)
    """
    return await self.request(
      Request(notif_id=notif_id, account_index=account_index),
      method='POST',
      path='/api/v1/notification/ack',
      meta={'auth': 'token'},
      validate=validate,
      request_type=Request,
      response_type=ResultCode,
    )
