# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_lighter.core import TimestampSeconds
from typed_lighter.explorer.core import ExplorerEndpoint


class ExplorerTxChart(TypedDict):
  """Chart series."""

  types: list[str]
  """Column names of each `data` row (`["timestamp", "count", "uopsCount"]`)."""
  data: list[tuple[TimestampSeconds, int, int]]
  """Rows, oldest first."""


class Request(TypedDict):
  aggregation_period: Literal['1h']
  """Aggregation period."""


class ExplorerTxStatsData(TypedDict):
  """Chart wrapper."""

  chart: ExplorerTxChart


class ExplorerTxStats(TypedDict):
  """Transaction-count chart."""

  data: ExplorerTxStatsData


class TxStats(ExplorerEndpoint):
  """Get transaction counts per period, as a chart series."""

  async def tx_stats(
    self,
    aggregation_period: Literal['1h'] = '1h',
    *,
    validate: bool | None = None,
  ) -> ExplorerTxStats:
    """Get transaction counts per period, as a chart series.

    Args:
      aggregation_period: Aggregation period.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/get_stats-tx)
    """
    return await self.request(
      Request(aggregation_period=aggregation_period),
      method='GET',
      path='/stats/tx',
      validate=validate,
      request_type=Request,
      response_type=ExplorerTxStats,
    )
