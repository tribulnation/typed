# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_lighter.core import TimestampIso
from typed_lighter.explorer.core import ExplorerEndpoint
from typed_lighter.schemas import ExplorerBatchDetails


class ExplorerRecentBatch(TypedDict):
  """A recent batch."""

  batch_number: int
  """Batch number."""
  updated_at: TimestampIso
  """Last update of the batch."""
  batch_size: int
  """Blocks in the batch."""
  batch_status: (
    Literal['nothing_to_execute', 'committed', 'verified', 'executed'] | None
  )
  """L1 status; `null` for a batch not yet committed (the newest one, and every testnet batch)."""
  batch_details: ExplorerBatchDetails | None
  """L1 transactions; `null` alongside `batch_status`."""


Response = list[ExplorerRecentBatch]


class Batches(ExplorerEndpoint):
  """Get the 20 most recent batches."""

  async def batches(self, *, validate: bool | None = None) -> Response:
    """Get the 20 most recent batches.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/get_batches)
    """
    return await self.request(
      None,
      method='GET',
      path='/batches',
      validate=validate,
      response_type=Response,
    )
