# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_lighter.explorer.core import ExplorerEndpoint


class ExplorerMarket(TypedDict):
  """A market."""

  market_index: int
  """Market id."""
  symbol: str
  """Symbol: the base asset for a perp (`ETH`), `BASE/QUOTE` for spot (`ETH/USDC`)."""


Response = list[ExplorerMarket]


class Markets(ExplorerEndpoint):
  """List every market: its id and symbol."""

  async def markets(self, *, validate: bool | None = None) -> Response:
    """List every market: its id and symbol.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/get_markets)
    """
    return await self.request(
      None,
      method='GET',
      path='/markets',
      validate=validate,
      response_type=Response,
    )
