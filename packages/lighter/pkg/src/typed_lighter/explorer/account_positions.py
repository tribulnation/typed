# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_lighter.explorer.core import ExplorerEndpoint
from typed_lighter.schemas import ExplorerAccountPositions


class Request(TypedDict):
  param: int | str
  """Account index, or the checksummed L1 address of a master account (an L1 address resolves to its master account only; the lookup is case-sensitive, a lowercase address is `404 account with l1Address=... not found`)."""


class AccountPositions(ExplorerEndpoint):
  """Get the open positions of an account."""

  async def account_positions(
    self,
    param: int | str,
    *,
    validate: bool | None = None,
  ) -> ExplorerAccountPositions:
    """Get the open positions of an account.

    Args:
      param: Account index, or the checksummed L1 address of a master account (an L1 address resolves to its master account only; the lookup is case-sensitive, a lowercase address is `404 account with l1Address=... not found`).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/get_accounts-param-positions)
    """
    return await self.request(
      Request(param=param),
      method='GET',
      path='/accounts/{param}/positions',
      validate=validate,
      request_type=Request,
      response_type=ExplorerAccountPositions,
    )
