# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, Sequence, TypedDict
from typed_lighter.explorer.core import ExplorerEndpoint
from typed_lighter.schemas import ExplorerLog


class Request(TypedDict):
  param: int | str
  """Account index, or the checksummed L1 address of a master account (an L1 address resolves to its master account only; the lookup is case-sensitive, a lowercase address is `404 account with l1Address=... not found`)."""
  pub_data_type: NotRequired[
    list[
      Literal[
        'Empty',
        'L1Deposit',
        'L1CreateMarket',
        'L1UpdateMarket',
        'L2CreateSubAccount',
        'L2CreatePublicPool',
        'L2UpdatePublicPool',
        'L2MintShares',
        'L2UpdateLeverage',
        'L2UpdateMargin',
        'L2Transfer',
        'Withdraw',
        'Trade',
        'TradeWithFunding',
        'LiquidationTrade',
        'LiquidationTradeWithFunding',
        'BurnedShares',
        'ExitPosition',
        'ExitPositionWithFunding',
        'Deleverage',
        'DeleverageWithFunding',
        'L1RegisterAsset',
        'L1UpdateAsset',
        'L1CreateSpotMarket',
        'L1UpdateSpotMarket',
        'WithdrawV2',
        'L1CreateMarketV2',
        'L1DepositV2',
        'L1UpdateMarketV2',
        'L2TransferV2',
        'L2CreateStakingPool',
        'L2UpdateStakingPool',
        'L2StakeAssets',
        'UnstakeAssets',
        'L1SetSystemConfig',
        'ExecutedPendingUnlock',
        'L2UpdateAccountConfig',
        'L2UpdateMarketConfig',
      ]
    ]
  ]
  """Only logs of these kinds (sent comma-separated). Omit for every kind."""
  limit: NotRequired[int]
  """Logs per page. Values above 100 are silently capped at 100."""
  offset: NotRequired[int]
  """Logs to skip, from the newest."""


Response = list[ExplorerLog]


class AccountLogs(ExplorerEndpoint):
  """Get the logs of an account, newest first, optionally only some kinds of public data."""

  def account_logs_paged(
    self,
    param: int | str,
    *,
    pub_data_type: list[
      Literal[
        'Empty',
        'L1Deposit',
        'L1CreateMarket',
        'L1UpdateMarket',
        'L2CreateSubAccount',
        'L2CreatePublicPool',
        'L2UpdatePublicPool',
        'L2MintShares',
        'L2UpdateLeverage',
        'L2UpdateMargin',
        'L2Transfer',
        'Withdraw',
        'Trade',
        'TradeWithFunding',
        'LiquidationTrade',
        'LiquidationTradeWithFunding',
        'BurnedShares',
        'ExitPosition',
        'ExitPositionWithFunding',
        'Deleverage',
        'DeleverageWithFunding',
        'L1RegisterAsset',
        'L1UpdateAsset',
        'L1CreateSpotMarket',
        'L1UpdateSpotMarket',
        'WithdrawV2',
        'L1CreateMarketV2',
        'L1DepositV2',
        'L1UpdateMarketV2',
        'L2TransferV2',
        'L2CreateStakingPool',
        'L2UpdateStakingPool',
        'L2StakeAssets',
        'UnstakeAssets',
        'L1SetSystemConfig',
        'ExecutedPendingUnlock',
        'L2UpdateAccountConfig',
        'L2UpdateMarketConfig',
      ]
    ]
    | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[ExplorerLog, int]:
    """Get the logs of an account, newest first, optionally only some kinds of public data.

    Paged variant of `account_logs`: Advances `offset` by `len(rows)` and stops on the first page shorter than `limit`. Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      param: Account index, or the checksummed L1 address of a master account (an L1 address resolves to its master account only; the lookup is case-sensitive, a lowercase address is `404 account with l1Address=... not found`).
      pub_data_type: Only logs of these kinds (sent comma-separated). Omit for every kind.
      limit: Logs per page. Values above 100 are silently capped at 100.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/get_accounts-param-logs)
    """

    async def next(offset: int) -> tuple[Sequence[ExplorerLog], int | None]:
      response = await self.account_logs(
        param,
        pub_data_type=pub_data_type,
        limit=limit,
        offset=offset,
        validate=validate,
      )
      rows = response
      if not rows or len(rows) < (min(limit, 100) if limit is not None else 100):
        return rows, None
      return rows, offset + len(rows)

    return PaginatedResponse(0, next)

  async def account_logs(
    self,
    param: int | str,
    *,
    pub_data_type: list[
      Literal[
        'Empty',
        'L1Deposit',
        'L1CreateMarket',
        'L1UpdateMarket',
        'L2CreateSubAccount',
        'L2CreatePublicPool',
        'L2UpdatePublicPool',
        'L2MintShares',
        'L2UpdateLeverage',
        'L2UpdateMargin',
        'L2Transfer',
        'Withdraw',
        'Trade',
        'TradeWithFunding',
        'LiquidationTrade',
        'LiquidationTradeWithFunding',
        'BurnedShares',
        'ExitPosition',
        'ExitPositionWithFunding',
        'Deleverage',
        'DeleverageWithFunding',
        'L1RegisterAsset',
        'L1UpdateAsset',
        'L1CreateSpotMarket',
        'L1UpdateSpotMarket',
        'WithdrawV2',
        'L1CreateMarketV2',
        'L1DepositV2',
        'L1UpdateMarketV2',
        'L2TransferV2',
        'L2CreateStakingPool',
        'L2UpdateStakingPool',
        'L2StakeAssets',
        'UnstakeAssets',
        'L1SetSystemConfig',
        'ExecutedPendingUnlock',
        'L2UpdateAccountConfig',
        'L2UpdateMarketConfig',
      ]
    ]
    | None = None,
    limit: int | None = None,
    offset: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get the logs of an account, newest first, optionally only some kinds of public data.

    Args:
      param: Account index, or the checksummed L1 address of a master account (an L1 address resolves to its master account only; the lookup is case-sensitive, a lowercase address is `404 account with l1Address=... not found`).
      pub_data_type: Only logs of these kinds (sent comma-separated). Omit for every kind.
      limit: Logs per page. Values above 100 are silently capped at 100.
      offset: Logs to skip, from the newest.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/get_accounts-param-logs)
    """
    request: Request = Request(param=param)
    if pub_data_type is not None:
      request['pub_data_type'] = pub_data_type
    if limit is not None:
      request['limit'] = limit
    if offset is not None:
      request['offset'] = offset
    return await self.request(
      request,
      method='GET',
      path='/accounts/{param}/logs',
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
