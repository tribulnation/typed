# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_lighter.explorer.core import ExplorerEndpoint
from typed_lighter.schemas import ExplorerBlock


class Request(TypedDict):
  blockId: int
  """Block height."""


class Block(ExplorerEndpoint):
  """Get one block by height, with its logs and the state of every perp market at that block."""

  async def block(
    self, block_id: int, *, validate: bool | None = None
  ) -> ExplorerBlock:
    """Get one block by height, with its logs and the state of every perp market at that block.

    Args:
      block_id: Block height.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/get_blocks-blockid)
    """
    return await self.request(
      Request(blockId=block_id),
      method='GET',
      path='/blocks/{blockId}',
      validate=validate,
      request_type=Request,
      response_type=ExplorerBlock,
    )
