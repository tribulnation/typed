# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_lighter.explorer.core import ExplorerEndpoint


class ExplorerTotals(TypedDict):
  """Chain totals."""

  blocks: int
  """Blocks produced."""
  batches: int
  """Batches produced."""
  accounts: int
  """Accounts created."""
  txs: int
  """Transactions executed."""


class Total(ExplorerEndpoint):
  """Get the chain totals: blocks, batches, accounts and transactions."""

  async def total(self, *, validate: bool | None = None) -> ExplorerTotals:
    """Get the chain totals: blocks, batches, accounts and transactions.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/get_total)
    """
    return await self.request(
      None,
      method='GET',
      path='/total',
      validate=validate,
      response_type=ExplorerTotals,
    )
