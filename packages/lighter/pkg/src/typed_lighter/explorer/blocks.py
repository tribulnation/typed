# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_lighter.core import TimestampIso
from typed_lighter.explorer.core import ExplorerEndpoint
from typed_lighter.schemas import ExplorerBatchDetails


class ExplorerRecentBlock(TypedDict):
  """A recent block."""

  block_height: int
  """Block height (the `blockId` of `/blocks/{blockId}`)."""
  updated_at: TimestampIso
  """When the block was produced."""
  block_size: int
  """Transactions in the block."""
  batch_status: (
    Literal['nothing_to_execute', 'committed', 'verified', 'executed'] | None
  )
  """L1 status of the batch holding the block; `null` until batched and on L1 (so for every recent block)."""
  batch_details: ExplorerBatchDetails | None
  """L1 transactions of the batch; `null` until committed."""


Response = list[ExplorerRecentBlock]


class Blocks(ExplorerEndpoint):
  """Get the 20 most recent blocks, newest first."""

  async def blocks(self, *, validate: bool | None = None) -> Response:
    """Get the 20 most recent blocks, newest first.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/get_blocks)
    """
    return await self.request(
      None,
      method='GET',
      path='/blocks',
      validate=validate,
      response_type=Response,
    )
