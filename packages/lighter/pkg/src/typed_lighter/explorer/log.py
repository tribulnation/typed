# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_lighter.explorer.core import ExplorerEndpoint
from typed_lighter.schemas import ExplorerLogDetailed


class Request(TypedDict):
  hash: str
  """Transaction hash, hex without `0x`."""


class Log(ExplorerEndpoint):
  """Get one log (an executed transaction with its public data) by transaction hash."""

  async def log(
    self, hash: str, *, validate: bool | None = None
  ) -> ExplorerLogDetailed:
    """Get one log (an executed transaction with its public data) by transaction hash.

    Args:
      hash: Transaction hash, hex without `0x`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/get_logs-hash)
    """
    return await self.request(
      Request(hash=hash),
      method='GET',
      path='/logs/{hash}',
      validate=validate,
      request_type=Request,
      response_type=ExplorerLogDetailed,
    )
