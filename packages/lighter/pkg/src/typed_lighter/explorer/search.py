# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_lighter.explorer.core import ExplorerEndpoint
from typed_lighter.schemas import (
  ExplorerAccountAssets,
  ExplorerAccountPositions,
  ExplorerBatch,
  ExplorerBlock,
  ExplorerLog,
  ExplorerLogDetailed,
)


class Request(TypedDict):
  q: str
  """Block height, batch number, transaction hash, account index or checksummed L1 address."""


class ExplorerAccountResult(TypedDict):
  """A matching account."""

  type: Literal['account']
  """Match kind."""
  account_positions: ExplorerAccountPositions
  """The account's open positions."""
  account_logs: list[ExplorerLog]
  """The account's 100 latest logs, newest first."""
  account_assets: ExplorerAccountAssets
  """The account's asset balances."""


class ExplorerBatchResult(TypedDict):
  """A matching batch."""

  type: Literal['batch']
  """Match kind."""
  batch: ExplorerBatch
  """The matching batch."""


class ExplorerBlockResult(TypedDict):
  """A matching block."""

  type: Literal['block']
  """Match kind."""
  block: ExplorerBlock
  """The matching block."""


class ExplorerLogResult(TypedDict):
  """A matching transaction."""

  type: Literal['log']
  """Match kind."""
  log: ExplorerLogDetailed
  """The matching transaction's log, with its block and batch."""


Response = list[
  ExplorerAccountResult | ExplorerBlockResult | ExplorerBatchResult | ExplorerLogResult
]


class Search(ExplorerEndpoint):
  """Search for a block, batch, transaction or account by block height, batch number, transaction hash, account index or L1 address."""

  async def search(self, q: str, *, validate: bool | None = None) -> Response:
    """Search for a block, batch, transaction or account by block height, batch number, transaction hash, account index or L1 address.

    Args:
      q: Block height, batch number, transaction hash, account index or checksummed L1 address.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/get_search)
    """
    return await self.request(
      Request(q=q),
      method='GET',
      path='/search',
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
