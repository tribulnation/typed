# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_lighter.explorer.core import ExplorerEndpoint
from typed_lighter.schemas import ExplorerBatch


class Request(TypedDict):
  batchId: int
  """Batch number."""


class Batch(ExplorerEndpoint):
  """Get one batch by number: its blocks and L1 status history."""

  async def batch(
    self, batch_id: int, *, validate: bool | None = None
  ) -> ExplorerBatch:
    """Get one batch by number: its blocks and L1 status history.

    Args:
      batch_id: Batch number.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://apidocs.lighter.xyz/reference/get_batches-batchid)
    """
    return await self.request(
      Request(batchId=batch_id),
      method='GET',
      path='/batches/{batchId}',
      validate=validate,
      request_type=Request,
      response_type=ExplorerBatch,
    )
