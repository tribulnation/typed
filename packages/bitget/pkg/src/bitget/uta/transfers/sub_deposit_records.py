# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.transfers.sub_deposit_records` -- generated from `spec/endpoints/`."""

from typed_core.validation import validator
from bitget.types import DepositRecord
from bitget.core.endpoint.rpc import RpcEndpoint


class SubDepositRecords(RpcEndpoint):
  """`uta.transfers.sub_deposit_records`."""

  async def sub_deposit_records(
    self,
    *,
    sub_uid: str,
    coin: str | None = None,
    start_time: int,
    end_time: int,
    limit: int | None = None,
    cursor: str | None = None,
  ) -> list[DepositRecord]:
    """List deposit records credited to a sub-account, authenticated as the master account.

    Args:
      sub_uid: Sub-account UID to list deposit records for.
      coin: Restrict to a single coin.
      start_time: Range start, Unix milliseconds.
      end_time: Range end, Unix milliseconds.
      limit: Maximum entries to return per page.
      cursor: Pagination cursor: the smallest `recordId`/`orderId` from the previous page's rows, not a token returned in the response -- see `notes`.
    """
    params: dict = {
      'subUid': sub_uid,
      'startTime': start_time,
      'endTime': end_time,
    }
    if coin is not None:
      params['coin'] = coin
    if limit is not None:
      params['limit'] = limit
    if cursor is not None:
      params['cursor'] = cursor
    return await self.authed_request(
      'GET',
      '/api/v3/account/sub-deposit-records',
      params=params,
      validator=validator(list[DepositRecord]),
    )
