# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.transfers.transferable_coins` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import validator
from bitget.core.endpoint.rpc import RpcEndpoint


class TransferableCoins(RpcEndpoint):
  """`uta.transfers.transferable_coins`."""

  async def transferable_coins(
    self,
    *,
    from_type: Literal[
      'spot',
      'p2p',
      'coin_futures',
      'usdt_futures',
      'usdc_futures',
      'crossed_margin',
      'isolated_margin',
      'uta',
    ],
    to_type: Literal[
      'spot',
      'p2p',
      'coin_futures',
      'usdt_futures',
      'usdc_futures',
      'crossed_margin',
      'isolated_margin',
      'uta',
    ],
  ) -> list[str]:
    """List coins transferable from one of the caller's own account types to another, authenticated.

    Args:
      from_type: Source Account type: `spot`, `p2p` (OTC), `coin_futures` (coin-margined futures), `usdt_futures` (USDT-margined futures), `usdc_futures` (USDC-margined futures), `crossed_margin`, `isolated_margin`, or `uta` (unified account).
      to_type: Destination Account type: `spot`, `p2p` (OTC), `coin_futures` (coin-margined futures), `usdt_futures` (USDT-margined futures), `usdc_futures` (USDC-margined futures), `crossed_margin`, `isolated_margin`, or `uta` (unified account).

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/account/transfer)
    """
    params: dict = {
      'fromType': from_type,
      'toType': to_type,
    }
    return await self.authed_request(
      'GET',
      '/api/v3/account/transferable-coins',
      params=params,
      validator=validator(list[str]),
    )
