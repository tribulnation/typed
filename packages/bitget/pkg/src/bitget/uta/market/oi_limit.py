# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.market.oi_limit` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class ContractOi(TypedDict):
  """One symbol's open interest limit."""

  symbol: str
  """Trading symbol."""
  notionalValue: str
  """Open interest notional-value cap for a single account on this symbol."""
  totalNotionalValue: str
  """Open interest notional-value cap for this symbol platform-wide."""


class OiLimit(RpcEndpoint):
  """`uta.market.oi_limit`."""

  async def oi_limit(
    self,
    *,
    symbol: str | None = None,
    category: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
  ) -> list[ContractOi]:
    """Get the open interest limit (notional value cap) for futures symbols.

    Args:
      symbol: Restrict to a single symbol.
      category: Futures product family.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/public/OpenInterestLimit)
    """
    params: dict = {
      'category': category,
    }
    if symbol is not None:
      params['symbol'] = symbol
    return await self.request(
      'GET',
      '/api/v3/market/oi-limit',
      params=params,
      validator=validator(list[ContractOi]),
    )
