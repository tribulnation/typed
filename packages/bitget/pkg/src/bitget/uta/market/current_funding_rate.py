# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.market.current_funding_rate` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from bitget.core import Timestamp
from bitget.core.endpoint.rpc import RpcEndpoint


class CurrentFundingRate(TypedDict):
  """One symbol's current funding rate."""

  symbol: str
  """Trading symbol."""
  fundingRate: str
  """Current funding rate."""
  fundingRateInterval: str
  """Funding interval, in hours."""
  nextUpdate: Timestamp
  """Next funding settlement time, Unix millisecond timestamp."""
  minFundingRate: str | None
  """Minimum allowed funding rate. Confirmed live to be `null` for some (typically newer/lower-volume) symbols."""
  maxFundingRate: str | None
  """Maximum allowed funding rate. Confirmed live to be `null` for some (typically newer/lower-volume) symbols."""
  cashDividend: NotRequired[str | None]
  """Cash dividend component of the funding rate. Reality-stock symbols only; `null` otherwise (confirmed live for a non-Reality symbol)."""
  cashDividendNextUpdate: NotRequired[Timestamp | None]
  """Next cash dividend settlement time, Unix millisecond timestamp. Reality-stock symbols only; `null` otherwise (confirmed live for a non-Reality symbol)."""


class CurrentFundingRateEndpoint(RpcEndpoint):
  """`uta.market.current_funding_rate`."""

  async def current_funding_rate(
    self,
    *,
    category: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'] | None = None,
    symbol: str | None = None,
  ) -> list[CurrentFundingRate]:
    """Get the current funding rate for futures symbols.

    Args:
      category: Futures product family.
      symbol: Restrict to a single symbol.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/public/CurrentFundingRate)
    """
    params = {}
    if category is not None:
      params['category'] = category
    if symbol is not None:
      params['symbol'] = symbol
    return await self.request(
      'GET',
      '/api/v3/market/current-fund-rate',
      params=params,
      validator=validator(list[CurrentFundingRate]),
    )
