# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.market.open_interest` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from bitget.core import Timestamp
from bitget.core.endpoint.rpc import RpcEndpoint


class OpenInterestItem(TypedDict):
  """One symbol's open interest."""

  symbol: str
  """Trading symbol."""
  openInterest: str
  """Open interest, in contracts/base coin."""


class OpenInterest(TypedDict):
  """Current open interest."""

  list: list[OpenInterestItem]
  """Matching symbols' open interest."""
  ts: Timestamp
  """Snapshot time, Unix millisecond timestamp."""


class OpenInterestEndpoint(RpcEndpoint):
  """`uta.market.open_interest`."""

  async def open_interest(
    self,
    *,
    category: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    symbol: str | None = None,
  ) -> OpenInterest:
    """Get current open interest for futures symbols.

    Args:
      category: Futures product family.
      symbol: Restrict to a single symbol.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/public/OpenInterest)
    """
    params: dict = {
      'category': category,
    }
    if symbol is not None:
      params['symbol'] = symbol
    return await self.request(
      'GET',
      '/api/v3/market/open-interest',
      params=params,
      validator=validator(OpenInterest),
    )
