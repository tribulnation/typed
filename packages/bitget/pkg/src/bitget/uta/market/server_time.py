# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.market.server_time` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from bitget.core import Timestamp
from bitget.core.endpoint.rpc import RpcEndpoint


class ServerTime(TypedDict):
  """Bitget's current server time."""

  serverTime: Timestamp
  """Server time, Unix millisecond timestamp."""


class ServerTimeEndpoint(RpcEndpoint):
  """`uta.market.server_time`."""

  async def server_time(self) -> ServerTime:
    """Get Bitget's current UTA v3 server time, as a Unix millisecond timestamp. Useful for clients synchronizing their local clock with the exchange server.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/public/ServerTime)
    """
    return await self.request(
      'GET',
      '/api/v3/market/time',
      validator=validator(ServerTime),
    )
