# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.market.score_weights` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class MarketScoreWeight(TypedDict):
  """One symbol's market-maker score weight."""

  category: str
  """Product family the row's symbol belongs to. Confirmed live to be more granular than the `category` request parameter: a `category=FUTURES` request returned rows with `"USDT_FUTURES"` here (underscore-separated, unlike this domain's other `category` fields, which hyphenate). Only `SPOT` and `USDT_FUTURES` were observed live; `COIN_FUTURES`/`USDC_FUTURES` are presumed analogous but unconfirmed, so left as a bare string rather than an invented enum (rule 2)."""
  label: str
  """Score weight grouping label."""
  symbol: str
  """Trading symbol."""
  requiredSpread: str
  """Maximum bid/ask spread required to qualify for this weight."""
  minMakerVolume: str
  """Minimum maker volume required to qualify for this weight."""
  weight: str
  """Score weight."""


class ScoreWeights(RpcEndpoint):
  """`uta.market.score_weights`."""

  async def score_weights(
    self,
    *,
    category: Literal['SPOT', 'FUTURES'] | None = None,
  ) -> list[MarketScoreWeight]:
    """Query market-maker score weights per symbol, used to compute market-maker program rebates.

    Args:
      category: Restrict to a single product family.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/public/ScoreWeights)
    """
    params = {}
    if category is not None:
      params['category'] = category
    return await self.request(
      'GET',
      '/api/v3/market/score-weights',
      params=params,
      validator=validator(list[MarketScoreWeight]),
    )
