# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta_streams.fill` -- generated from `spec/endpoints/`."""

from typing_extensions import Any, Literal, NotRequired
from typed_core.util import StreamManager
from typed_core.validation import TypedDict, validator
from bitget.core import Timestamp
from bitget.core.endpoint.stream import StreamEndpoint


class FeeDetail(TypedDict):
  """One fee charged, in a single coin."""

  feeCoin: str
  """Coin the fee was charged in."""
  fee: str
  """Fee amount, negative when rebated."""


class FillArgResponsemessageArg(TypedDict):
  """Channel identifying object echoed on the `fill` subscription."""

  instType: Literal['UTA']
  """Fixed unified-account instType. All UTA v3 private channels subscribe/push under `"UTA"`, not a per-category value -- unlike Classic v2, which splits private channels by `SPOT`/`USDT-FUTURES`/etc."""
  topic: Literal['fill']
  """Channel name."""


class FillArgResponsereplyArg(TypedDict):
  """Channel identifying object echoed on the `fill` subscription."""

  instType: Literal['UTA']
  """Fixed unified-account instType. All UTA v3 private channels subscribe/push under `"UTA"`, not a per-category value -- unlike Classic v2, which splits private channels by `SPOT`/`USDT-FUTURES`/etc."""
  topic: Literal['fill']
  """Channel name."""


class FillParams(TypedDict):
  """Fill subscription parameters."""

  instType: Literal['UTA']
  """Fixed unified-account instType. All UTA v3 private channels subscribe/push under `"UTA"`, not a per-category value -- unlike Classic v2, which splits private channels by `SPOT`/`USDT-FUTURES`/etc."""


class FillReply(TypedDict):
  """Acknowledgement for a `subscribe`/`unsubscribe` request. Per spec/core.md WebSocket, only `event` is guaranteed present; `arg`/`connId` accompany a successful ack, `code`/`msg` accompany an `error`."""

  event: Literal['subscribe', 'unsubscribe', 'error']
  """Event name: `subscribe` on success, `unsubscribe` on an unsubscribe ack, `error` on failure."""
  code: NotRequired[str]
  """Error code, present only on a failed request."""
  msg: NotRequired[str]
  """Error message, present only on a failed request."""
  arg: NotRequired[FillArgResponsereplyArg]
  connId: NotRequired[str]
  """Connection identifier. UTA v3-only: not present on Classic v2 acks (spec/core.md WebSocket)."""


class FillUpdate(TypedDict):
  """One order fill (execution)."""

  symbol: str
  """Trading pair."""
  orderType: str
  """Order type of the order this fill belongs to."""
  side: str
  """Fill side."""
  orderId: str
  """Order this fill belongs to."""
  execPnl: str
  """Realised PnL attributed to this fill. Futures only."""
  feeDetail: list[FeeDetail]
  """Fees charged against this fill, one entry per fee coin."""
  execTime: Timestamp
  """Fill (transaction) time, Unix millisecond timestamp."""
  tradeScope: str
  """Whether this fill was the taker or maker side of the trade."""
  tradeSide: str
  """Whether this fill opened or closed a position. Futures only."""
  execId: str
  """Venue-assigned execution identifier."""
  execLinkId: str
  """Execution correlation identifier."""
  execPrice: str
  """Execution price."""
  holdSide: str
  """Directional exposure this fill contributes to. Futures only."""
  execValue: str
  """Executed value."""
  category: str
  """Product line the fill/order belongs to."""
  execQty: str
  """Executed quantity."""
  clientOid: str
  """Caller-supplied client order ID of the order this fill belongs to."""
  isRPI: Literal['yes', 'no']
  """Whether this was an RPI (retail price improvement) fill."""


class FillPush(TypedDict):
  """Push frame for the `fill` channel. The core performs no envelope unwrap for streams (spec/core.md WebSocket): callers receive this whole frame."""

  action: Literal['snapshot', 'update']
  """Push data action."""
  arg: FillArgResponsemessageArg
  data: list[FillUpdate]
  """Subscription data."""
  ts: Timestamp
  """Message generation time, Unix epoch milliseconds."""


class Fill(StreamEndpoint):
  """`uta_streams.fill`."""

  def fill(self, inst_type: Literal['UTA']) -> 'StreamManager[FillPush, Any, Any]':
    """Get order-fill (execution) updates across the unified account, private.

    Args:
      inst_type: Fixed unified-account instType. All UTA v3 private channels subscribe/push under `"UTA"`, not a per-category value -- unlike Classic v2, which splits private channels by `SPOT`/`USDT-FUTURES`/etc.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/websocket/private/Fill-Channel)
    """
    params = {
      'instType': inst_type,
    }
    return self.authed_subscribe('fill', params, validator=validator(FillPush))
