# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.copytrading.max_transferable` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class CopyFuturesMaxTransferable(TypedDict):
  """Maximum transferable amount and available balance for a coin in the copy-trading lead account."""

  maxTransferable: str
  """Maximum amount of `coin` that can be transferred into the copy-trading lead account, as a decimal string."""
  available: str
  """Amount of `coin` currently available to transfer, as a decimal string."""


class MaxTransferable(RpcEndpoint):
  """`uta.copytrading.max_transferable`."""

  async def max_transferable(self, *, coin: str) -> CopyFuturesMaxTransferable:
    """Get the maximum amount of a coin that can be transferred into the copy-trading lead account, and the account's currently available balance of that coin.

    Args:
      coin: Coin to query, e.g. `USDT`.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/copytrading/Max-Transferable)
    """
    params: dict = {
      'coin': coin,
    }
    return await self.authed_request(
      'GET',
      '/api/v3/copy/futures/max-transferable',
      params=params,
      validator=validator(CopyFuturesMaxTransferable),
    )
