# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.copytrading.transfer_records` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class CopyFuturesTransferRecord(TypedDict):
  """One transfer between the unified trading account and the copy-trading lead account."""

  transferId: str
  """Identifier of the transfer; pass as `cursor` to page forward past this record."""
  fromType: Literal['spot', 'uta', 'lead']
  """Account the transfer moved funds from."""
  toType: Literal['spot', 'uta', 'lead']
  """Account the transfer moved funds to."""
  amount: str
  """Transferred amount, as a decimal string."""
  coin: str
  """Coin that was transferred, e.g. `USDT`."""
  status: Literal['Successful', 'Failed', 'Processing']
  """Current status of the transfer."""
  createdTime: str
  """Millisecond epoch timestamp the transfer was created, as a string."""


class CopyFuturesTransferRecordList(TypedDict):
  """A page of copy-trading lead account transfer records."""

  list: list[CopyFuturesTransferRecord]
  """Transfer records, most recent first."""


class TransferRecords(RpcEndpoint):
  """`uta.copytrading.transfer_records`."""

  async def transfer_records(
    self,
    *,
    start_time: int | None = None,
    end_time: int | None = None,
    limit: int | None = None,
    cursor: str | None = None,
  ) -> CopyFuturesTransferRecordList:
    """Query the copy-trading lead account's transfer history.

    Args:
      start_time: Start of the time range to query, as a millisecond epoch timestamp.
      end_time: End of the time range to query, as a millisecond epoch timestamp.
      limit: Maximum number of records to return.
      cursor: `transferId` of the last record from the previous page; pass it to page forward. Omit to fetch the most recent records.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/copytrading/Transfer-Record)
    """
    params = {}
    if start_time is not None:
      params['startTime'] = start_time
    if end_time is not None:
      params['endTime'] = end_time
    if limit is not None:
      params['limit'] = limit
    if cursor is not None:
      params['cursor'] = cursor
    return await self.authed_request(
      'GET',
      '/api/v3/copy/futures/transfer-record',
      params=params,
      validator=validator(CopyFuturesTransferRecordList),
    )
