# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.loan.debts` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class LoanDebtInfoLoanInfosItem(TypedDict):
  """Total amount of one coin, either pledged as collateral or owed as a loan."""

  coin: str
  """Coin."""
  amount: str
  """Total amount of `coin`, as a decimal string."""
  amountUsdt: str
  """Total amount of `coin`, converted to USDT, as a decimal string."""


class LoanDebtInfoPledgeInfosItem(TypedDict):
  """Total amount of one coin, either pledged as collateral or owed as a loan."""

  coin: str
  """Coin."""
  amount: str
  """Total amount of `coin`, as a decimal string."""
  amountUsdt: str
  """Total amount of `coin`, converted to USDT, as a decimal string."""


class LoanDebts(TypedDict):
  """Total pledged collateral and outstanding loan balances, by coin."""

  pledgeInfos: list[LoanDebtInfoPledgeInfosItem]
  """Total pledged collateral, one entry per coin."""
  loanInfos: list[LoanDebtInfoLoanInfosItem]
  """Total outstanding loan balances, one entry per coin."""


class Debts(RpcEndpoint):
  """`uta.loan.debts`."""

  async def debts(self) -> LoanDebts:
    """Get the caller's total pledged collateral and outstanding loan balances, across all open loan orders.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/loan/Debts)
    """
    return await self.authed_request(
      'GET',
      '/api/v3/loan/debts',
      validator=validator(LoanDebts),
    )
