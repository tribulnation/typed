# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.loan.revise_pledge` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class LoanRevisePledgeRequest(TypedDict):
  """A collateral adjustment on an open loan order."""

  orderId: str
  """Identifier of the loan order to adjust."""
  amount: float
  """Amount of `pledgeCoin` to add or remove."""
  pledgeCoin: str
  """Pledged collateral coin to adjust."""
  reviseType: NotRequired[Literal['OUT', 'IN']]
  """Direction of the adjustment: `IN` adds collateral, `OUT` removes it."""


class LoanRevisePledgeResult(TypedDict):
  """Result of a collateral adjustment."""

  loanCoin: str
  """Borrowed coin of the adjusted loan order."""
  pledgeCoin: str
  """Pledged collateral coin that was adjusted."""
  afterPledgeRate: str
  """Pledge (collateral) rate after this adjustment, as a decimal string."""


class RevisePledge(RpcEndpoint):
  """`uta.loan.revise_pledge`."""

  async def revise_pledge(
    self,
    loan_revise_pledge_request: LoanRevisePledgeRequest,
  ) -> LoanRevisePledgeResult:
    """Add or remove collateral on an open loan order, adjusting its pledge rate.

    Args:
      loan_revise_pledge_request: Loan order to adjust, collateral coin, amount, and direction.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/loan/Revise-Pledge)
    """
    return await self.authed_request(
      'POST',
      '/api/v3/loan/revise-pledge',
      json=loan_revise_pledge_request,
      validator=validator(LoanRevisePledgeResult),
    )
