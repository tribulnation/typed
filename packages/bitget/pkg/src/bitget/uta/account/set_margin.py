# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.account.set_margin` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class SetMarginRequest(TypedDict):
  """Margin adjustment to apply."""

  category: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Futures product type."""
  symbol: str
  """Trading symbol."""
  posSide: Literal['long', 'short']
  """Position direction."""
  operation: Literal['add', 'remove']
  """Whether to add margin to, or remove margin from, the position."""
  amount: str
  """Margin amount to add or remove."""


class SetMargin(RpcEndpoint):
  """`uta.account.set_margin`."""

  async def set_margin(self, set_margin_request: SetMarginRequest) -> str:
    """Add or remove margin on an isolated-margin futures position, authenticated.

    Args:
      set_margin_request: Margin adjustment.
    """
    return await self.authed_request(
      'POST',
      '/api/v3/account/set-margin',
      json=set_margin_request,
      validator=validator(str),
    )
