# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.account.set_hold_mode` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class SetHoldModeRequest(TypedDict):
  """Target hold mode."""

  holdMode: Literal['one_way_mode', 'hedge_mode']
  """Position-holding mode: single-direction (`one_way_mode`) or long/short hedge (`hedge_mode`)."""


class SetHoldMode(RpcEndpoint):
  """`uta.account.set_hold_mode`."""

  async def set_hold_mode(self, set_hold_mode_request: SetHoldModeRequest) -> str:
    """Set the futures position-holding mode for the unified account, authenticated.

    Args:
      set_hold_mode_request: Target hold mode.
    """
    return await self.authed_request(
      'POST',
      '/api/v3/account/set-hold-mode',
      json=set_hold_mode_request,
      validator=validator(str),
    )
