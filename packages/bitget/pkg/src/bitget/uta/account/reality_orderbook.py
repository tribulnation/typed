# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.account.reality_orderbook` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from bitget.core import Timestamp
from bitget.core.endpoint.rpc import RpcEndpoint


class RealityOrderBook(TypedDict):
  """One orderbook snapshot."""

  symbol: str
  """Trading pair."""
  a: list[tuple[str, str]]
  """Sell (ask) levels, sorted ascending by price."""
  b: list[tuple[str, str]]
  """Buy (bid) levels, sorted descending by price."""
  ts: Timestamp
  """Snapshot time."""


class RealityOrderbook(RpcEndpoint):
  """`uta.account.reality_orderbook`."""

  async def reality_orderbook(self, *, symbol: str) -> RealityOrderBook:
    """Get an orderbook snapshot for a Reality (tokenized-stock) trading pair.

    Args:
      symbol: Reality trading pair, e.g. `RAAPLUSDT`.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/reality/market/Reality-OrderBook)
    """
    params: dict = {
      'symbol': symbol,
    }
    return await self.authed_request(
      'GET',
      '/api/v3/account/reality-orderbook',
      params=params,
      validator=validator(RealityOrderBook),
    )
