# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.account.eligible_symbols` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from bitget.core import Timestamp
from bitget.core.endpoint.rpc import RpcEndpoint


class EligibleSymbol(TypedDict):
  """One symbol the caller's account is eligible to trade."""

  symbol: str
  """Symbol name."""
  category: Literal['SPOT', 'MARGIN', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product type."""
  baseCoin: str
  """Base coin, e.g. `BTC` in `BTCUSDT`."""
  quoteCoin: str
  """Quote coin, e.g. `USDT` in `BTCUSDT`."""
  symbolType: Literal['crypto', 'metal', 'stock', 'commodity']
  """Symbol type."""
  buyLimitPriceRatio: str
  """Buy price limit ratio: the ratio of the buy limit price to the market price, determining the maximum price at which a buy order will be placed."""
  sellLimitPriceRatio: str
  """Sell price limit ratio: the ratio of the sell limit price to the market price, determining the minimum price at which a sell order will be placed."""
  minOrderQty: str
  """Minimum order quantity, in the base coin."""
  maxOrderQty: str
  """Maximum order quantity for a single limit order, in the base coin. `0` means no limit."""
  pricePrecision: str
  """Number of decimal places allowed for the price."""
  quantityPrecision: str
  """Number of decimal places allowed for the quantity."""
  quotePrecision: str
  """Number of decimal places allowed for the quote-coin price on a market order."""
  minOrderAmount: str
  """Minimum order amount, in the quote coin."""
  maxSymbolOrderNum: str
  """Maximum open order count for this symbol."""
  maxProductOrderNum: str
  """Maximum open order count for this product line."""
  status: Literal[
    'listed', 'online', 'limit_open', 'limit_close', 'offline', 'restrictedAPI'
  ]
  """Trading pair status."""
  maintainTime: str
  """Maintenance time window. Empty string when not configured."""
  areaSymbol: NotRequired[Literal['yes', 'no']]
  """Whether this is a region-restricted symbol."""
  maxPositionNum: str
  """Maximum open position count for this symbol."""
  isReality: Literal['yes', 'no']
  """Whether this is a Reality tokenized-stock symbol."""
  launchTime: Timestamp
  """Launch time."""


class EligibleSymbols(RpcEndpoint):
  """`uta.account.eligible_symbols`."""

  async def eligible_symbols(
    self, *, symbol: str | None = None
  ) -> list[EligibleSymbol]:
    """Get the symbols the caller's account is specifically eligible to trade (as opposed to `uta.market.instruments`, which lists every symbol the venue offers regardless of account eligibility) -- notably includes region- and KYC-gated Reality tokenized-stock symbols.

    Args:
      symbol: Restrict to one symbol, e.g. `BTCUSDT`.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/account/Get-Eligible-Symbols)
    """
    params = {}
    if symbol is not None:
      params['symbol'] = symbol
    return await self.authed_request(
      'GET',
      '/api/v3/account/eligible-symbols',
      params=params,
      validator=validator(list[EligibleSymbol]),
    )
