# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.account.switch_to_classic` -- generated from `spec/endpoints/`."""

from typed_core.validation import validator
from bitget.core.endpoint.rpc import RpcEndpoint


class SwitchToClassic(RpcEndpoint):
  """`uta.account.switch_to_classic`."""

  async def switch_to_classic(self):
    """Request switching the account from Unified Trading Account mode back to Classic account mode, authenticated. Only supported for parent accounts. The switch takes about a minute; a successful response only confirms the request was received -- poll `uta.account.switch_status` to confirm completion."""
    return await self.authed_request(
      'POST',
      '/api/v3/account/switch',
    )
