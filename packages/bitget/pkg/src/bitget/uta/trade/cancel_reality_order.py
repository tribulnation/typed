# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.trade.cancel_reality_order` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class CancelRealityOrderRequest(TypedDict):
  """Reality-stock order to cancel."""

  symbol: str
  """Reality-stock trading pair."""
  orderId: NotRequired[str]
  """Client-assigned order identifier, 1-32 characters matching `^[\\.A-Z\\:/a-z0-9_-]{1,32}$`. Either `orderId` or `clientOid` must be supplied on lookups/cancels/modifies; when both are given `orderId` takes precedence."""
  clientOid: NotRequired[str]
  """Client-assigned order identifier, 1-32 characters matching `^[\\.A-Z\\:/a-z0-9_-]{1,32}$`. Either `orderId` or `clientOid` must be supplied on lookups/cancels/modifies; when both are given `orderId` takes precedence."""


class CancelRealityOrderResult(TypedDict):
  """Identifiers of the order the call acted on."""

  orderId: str
  """Venue-assigned order identifier."""
  clientOid: str
  """Client-assigned order identifier echoed back."""


class CancelRealityOrder(RpcEndpoint):
  """`uta.trade.cancel_reality_order`."""

  async def cancel_reality_order(
    self,
    cancel_reality_order_request: CancelRealityOrderRequest,
  ) -> CancelRealityOrderResult:
    """Cancel an unfilled or partially-filled Reality tokenized-stock order.

    Args:
      cancel_reality_order_request: Reality-stock order to cancel.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/trade/Cancel-Reality-Order)
    """
    return await self.authed_request(
      'POST',
      '/api/v3/trade/cancel-reality-order',
      json=cancel_reality_order_request,
      validator=validator(CancelRealityOrderResult),
    )
