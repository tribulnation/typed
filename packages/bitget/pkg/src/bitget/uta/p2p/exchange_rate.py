# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.p2p.exchange_rate` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class P2pExchangeRate(TypedDict):
  """P2P reference exchange rate between a token and a fiat currency."""

  rate: str
  """Reference price of one unit of `token`, in `fiat`, as a decimal string."""


class ExchangeRate(RpcEndpoint):
  """`uta.p2p.exchange_rate`."""

  async def exchange_rate(self, *, token: str, fiat: str) -> P2pExchangeRate:
    """Get the current P2P reference exchange rate between a crypto token and a fiat currency.

    Args:
      token: Crypto token, e.g. `USDT`.
      fiat: Fiat currency, e.g. `USD`.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/p2p/Get-Exchange-Rate)
    """
    params: dict = {
      'token': token,
      'fiat': fiat,
    }
    return await self.authed_request(
      'GET',
      '/api/v3/p2p/exchange-rate',
      params=params,
      validator=validator(P2pExchangeRate),
    )
