# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.institutional_loan.product_infos` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class LoanProductInfo(TypedDict):
  """Risk-management thresholds and supported markets of one institutional loan product."""

  productId: str
  """Identifier of the loan product."""
  leverage: str
  """Leverage available under this product, as a decimal string."""
  supportUsdtContract: Literal['YES', 'NO']
  """Whether USDT-margined futures are supported under this product."""
  supportCoinContract: Literal['YES', 'NO']
  """Whether coin-margined futures are supported under this product."""
  supportUsdcContract: Literal['YES', 'NO']
  """Whether USDC-margined futures are supported under this product."""
  transferLine: str
  """LTV threshold above which transfers out are blocked, as a decimal string."""
  spotBuyLine: str
  """LTV threshold above which spot buys are blocked, as a decimal string."""
  usdtContractOpenLine: str
  """LTV threshold above which opening new USDT-margined futures positions is blocked, as a decimal string."""
  coinContractOpenLine: str
  """LTV threshold above which opening new coin-margined futures positions is blocked, as a decimal string."""
  usdcContractOpenLine: str
  """LTV threshold above which opening new USDC-margined futures positions is blocked, as a decimal string."""
  liquidationLine: str
  """LTV threshold at which forced liquidation begins, as a decimal string."""
  stopLiquidationLine: str
  """LTV threshold at which forced liquidation stops, as a decimal string."""


class ProductInfos(RpcEndpoint):
  """`uta.institutional_loan.product_infos`."""

  async def product_infos(self, *, product_id: str) -> LoanProductInfo:
    """Get the risk-management thresholds and supported markets of an institutional loan product.

    Args:
      product_id: Identifier of the institutional loan product to query.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/ins-loan/Product-Infos)
    """
    params: dict = {
      'productId': product_id,
    }
    return await self.authed_request(
      'GET',
      '/api/v3/ins-loan/product-infos',
      params=params,
      validator=validator(LoanProductInfo),
    )
