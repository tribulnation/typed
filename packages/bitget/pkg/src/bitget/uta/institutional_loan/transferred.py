# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.institutional_loan.transferred` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class LoanTransferred(TypedDict):
  """Quantity of a coin transferred into an institutional loan sub-account."""

  coin: str
  """Coin queried."""
  transfered: str
  """Transferred quantity of `coin`, as a decimal string. Field name as documented upstream (`transfered`, not `transferred`)."""
  userId: str
  """Sub-account user id this quantity belongs to."""


class Transferred(RpcEndpoint):
  """`uta.institutional_loan.transferred`."""

  async def transferred(
    self, *, user_id: str | None = None, coin: str
  ) -> LoanTransferred:
    """Get the quantity of a coin transferred into an institutional loan sub-account.

    Args:
      user_id: Sub-account user id to query. Defaults to the caller's own account.
      coin: Coin to query, e.g. `USDT`.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/ins-loan/Transfered)
    """
    params: dict = {
      'coin': coin,
    }
    if user_id is not None:
      params['userId'] = user_id
    return await self.authed_request(
      'GET',
      '/api/v3/ins-loan/transfered',
      params=params,
      validator=validator(LoanTransferred),
    )
