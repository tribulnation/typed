# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.institutional_loan.bind_uid` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class LoanBindUidRequest(TypedDict):
  """A risk unit sub-account bind/unbind request."""

  riskUnitId: NotRequired[str]
  """Risk unit to bind/unbind the sub-account to. Defaults to the caller's own risk unit."""
  uid: str
  """Sub-account user id to bind or unbind."""
  operate: Literal['bind', 'unbind']
  """Whether to bind or unbind `uid`."""


class LoanUidBinding(TypedDict):
  """Result of a risk unit sub-account bind/unbind operation."""

  riskUnitId: str
  """Risk unit the sub-account was bound to or unbound from."""
  uid: str
  """Sub-account user id that was bound or unbound."""
  operate: Literal['bind', 'unbind']
  """Operation that was performed."""


class BindUid(RpcEndpoint):
  """`uta.institutional_loan.bind_uid`."""

  async def bind_uid(self, loan_bind_uid_request: LoanBindUidRequest) -> LoanUidBinding:
    """Bind or unbind a unified-trading sub-account user id to a risk unit.

    Args:
      loan_bind_uid_request: Sub-account user id, target risk unit, and whether to bind or unbind it.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/ins-loan/Bind-UID)
    """
    return await self.authed_request(
      'POST',
      '/api/v3/ins-loan/bind-uid',
      json=loan_bind_uid_request,
      validator=validator(LoanUidBinding),
    )
