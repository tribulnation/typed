# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.copytrading.broker_current_traces` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class FuturesBrokerCurrentTrace(TypedDict):
  """One currently-open copy trade placed by the trader's followers, as visible to the broker."""

  trackingNo: str
  """Tracking order ID."""
  symbol: str
  """Trading pair."""
  openOrderId: str
  """Opening order ID."""
  posSide: Literal['long', 'short']
  """Position direction."""
  marginMode: Literal['crossed', 'isolated']
  """Margin mode."""
  openLeverage: str
  """Leverage applied when opening."""
  openPriceAvg: str
  """Average entry price."""
  openTime: str
  """Position opening time, as a millisecond epoch string."""
  openSize: str
  """Position quantity."""
  openFee: str
  """Opening fee, in USDT."""
  marginAmount: str
  """Margin used to open the position."""
  followCount: str
  """Number of followers copying this position."""
  stopSurplusPrice: str
  """Preset take-profit price. Empty string if unset."""
  stopLossPrice: str
  """Preset stop-loss price. Empty string if unset."""
  cTime: str
  """Record creation time, as a millisecond epoch string."""


class BrokerCurrentTraces(RpcEndpoint):
  """`classic.copytrading.broker_current_traces`."""

  async def broker_current_traces(
    self,
    *,
    trader_id: str,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    symbol: str | None = None,
    limit: int | None = None,
    start_time: int | None = None,
    end_time: int | None = None,
  ) -> list[FuturesBrokerCurrentTrace]:
    """List the currently-open copy trades placed by a specific trader's followers, futures, as visible to their broker.

    Args:
      trader_id: Trader user ID to look up open trace positions for.
      product_type: Which futures product family this request concerns.
      symbol: Trading pair, e.g. "BTCUSDT". Case-insensitive.
      limit: Results per page.
      start_time: Start of the queried range, as a millisecond timestamp.
      end_time: End of the queried range, as a millisecond timestamp.
    """
    params: dict = {
      'traderId': trader_id,
      'productType': product_type,
    }
    if symbol is not None:
      params['symbol'] = symbol
    if limit is not None:
      params['limit'] = limit
    if start_time is not None:
      params['startTime'] = start_time
    if end_time is not None:
      params['endTime'] = end_time
    return await self.authed_request(
      'GET',
      '/api/v2/copy/mix-broker/query-current-traces',
      params=params,
      validator=validator(list[FuturesBrokerCurrentTrace]),
    )
