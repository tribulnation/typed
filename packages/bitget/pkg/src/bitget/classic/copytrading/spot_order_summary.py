# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.copytrading.spot_order_summary` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class AmountCTimeLastMonthProfitListItem(TypedDict):
  """One point of an amount-over-time series."""

  amount: str
  """Amount for this point, in USDT."""
  ctime: str
  """Timestamp of this point, as a millisecond epoch string."""


class AmountCTimeLastWeekProfitListItem(TypedDict):
  """One point of an amount-over-time series."""

  amount: str
  """Amount for this point, in USDT."""
  ctime: str
  """Timestamp of this point, as a millisecond epoch string."""


class RateCTimeLastMonthRoiListItem(TypedDict):
  """One point of a rate-over-time series."""

  rate: str
  """Rate for this point (e.g. ROI), as a percentage string."""
  ctime: str
  """Timestamp of this point, as a millisecond epoch string."""


class RateCTimeLastWeekRoiListItem(TypedDict):
  """One point of a rate-over-time series."""

  rate: str
  """Rate for this point (e.g. ROI), as a percentage string."""
  ctime: str
  """Timestamp of this point, as a millisecond epoch string."""


class SpotTraderOrderSummary(TypedDict):
  """Aggregate copy-trading performance summary, spot."""

  totalFollowerNum: str
  """Lifetime follower count."""
  currentFollowerNum: str
  """Currently active follower count."""
  maxFollowerNum: str
  """Maximum follower count allowed."""
  tradingOrderNum: str
  """Count of currently open tracked positions."""
  totalpl: str
  """Aggregate realized profit/loss, in USDT."""
  gainNum: str
  """Count of profitable closed positions."""
  lossNum: str
  """Count of losing closed positions."""
  totalEquity: str
  """Current total account equity, in USDT."""
  winRate: str
  """Win rate, as a percentage string."""
  lastWeekRoiList: list[RateCTimeLastWeekRoiListItem]
  """ROI for each of the last 7 days."""
  lastWeekProfitList: list[AmountCTimeLastWeekProfitListItem]
  """Profit for each of the last 7 days, in USDT."""
  lastMonthRoiList: list[RateCTimeLastMonthRoiListItem]
  """ROI for each of the last 30 days."""
  lastMonthProfitList: list[AmountCTimeLastMonthProfitListItem]
  """Profit for each of the last 30 days, in USDT."""


class SpotOrderSummary(RpcEndpoint):
  """`classic.copytrading.spot_order_summary`."""

  async def spot_order_summary(self) -> SpotTraderOrderSummary:
    """Get a trader's aggregate copy-trading performance summary, spot: follower counts, win rate, ROI history.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/copytrading/spot-copytrade/trader/Order-Total-Detail)
    """
    return await self.authed_request(
      'GET',
      '/api/v2/copy/spot-trader/order-total-detail',
      validator=validator(SpotTraderOrderSummary),
    )
