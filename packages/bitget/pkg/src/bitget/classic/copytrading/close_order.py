# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.copytrading.close_order` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class CloseFuturesTraderOrderRequest(TypedDict):
  """Positions to close."""

  trackingNo: NotRequired[str]
  """Tracking order ID to close. Omit together with symbol to close every open position in productType."""
  symbol: NotRequired[str]
  """Trading pair to close. Omit together with trackingNo to close every open position in productType."""
  productType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Which futures product family this request concerns."""


class ClosedFuturesTraderOrder(TypedDict):
  """One closed position."""

  trackingNo: str
  """Tracking order ID."""
  symbol: str
  """Trading pair."""
  productType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Which futures product family this request concerns."""


class CloseOrder(RpcEndpoint):
  """`classic.copytrading.close_order`."""

  async def close_order(
    self,
    close_futures_trader_order_request: CloseFuturesTraderOrderRequest,
  ) -> list[ClosedFuturesTraderOrder]:
    """Close one or all of a trader's open copy positions in a product line, futures. Tracked positions can only be closed through this endpoint.

    Args:
      close_futures_trader_order_request: Positions to close.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/copytrading/future-copytrade/trader/Trader-Order-Close-Positions)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/copy/mix-trader/order-close-positions',
      json=close_futures_trader_order_request,
      validator=validator(list[ClosedFuturesTraderOrder]),
    )
