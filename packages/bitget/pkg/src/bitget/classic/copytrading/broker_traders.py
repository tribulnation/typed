# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.copytrading.broker_traders` -- generated from `spec/endpoints/`."""

from typing_extensions import AsyncIterator, Literal
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class FuturesBrokerTraderColumn(TypedDict):
  """One labeled display metric."""

  describe: str
  """Metric label, e.g. "ROI", "Total PnL"."""
  value: str
  """Formatted metric value, e.g. "551.89" or "$3,920.70"."""


class FuturesBrokerTraderDailyProfitDailyProfitListItem(TypedDict):
  """One day's profit amount."""

  amount: str
  """Cumulative profit amount as of this day, in USDT."""
  cTime: str
  """Day timestamp, as a millisecond epoch string."""


class FuturesBrokerTraderDailyProfitProfit24hListItem(TypedDict):
  """One day's 24-hour profit amount."""

  amount: str
  """Profit amount over this 24-hour window, in USDT."""
  cTime: str
  """Window timestamp, as a millisecond epoch string."""


class FuturesBrokerTraderDailyRateDailyProfitRateListItem(TypedDict):
  """One day's profit rate."""

  rate: str
  """Cumulative profit rate as of this day, as a percentage string."""
  cTime: str
  """Day timestamp, as a millisecond epoch string."""


class FuturesBrokerTraderDailyRateProfitRate24hListItem(TypedDict):
  """One day's 24-hour profit rate."""

  rate: str
  """Profit rate over this 24-hour window, as a percentage string."""
  cTime: str
  """Window timestamp, as a millisecond epoch string."""


class FuturesBrokerTrader(TypedDict):
  """One elite trader affiliated with the broker's agent account."""

  canTrace: Literal['Yes', 'No']
  """Whether this trader can currently be traced/queried by the broker."""
  traderId: str
  """Trader user ID."""
  traderName: str
  """Trader's display name."""
  maxLimit: str
  """Maximum follower count this trader allows."""
  bgbMaxFollowLimit: str
  """Maximum follower count allocatable when paying fees in BGB."""
  followCount: str
  """Current follower count."""
  bgbFollowCount: str
  """Current follower count paying fees in BGB."""
  traderStatus: str
  """Trader status flag; exact value set not documented upstream, left bare -- see notes."""
  currentTradingList: list[str]
  """Trading pairs currently being traded."""
  columnList: list[FuturesBrokerTraderColumn]
  """Display metrics for this trader's public profile card (ROI, PnL, AUM, etc.), as label/value pairs."""
  totalFollowers: str
  """Total follower count, all time."""
  profitCount: str
  """Count of profitable trades."""
  lossCount: str
  """Count of losing trades."""
  tradeCount: str
  """Total trade count."""
  traderPic: str
  """Trader's avatar URL."""
  maxCallbackRate: str
  """Maximum drawdown (callback rate), as a percentage string."""
  averageWinRate: str
  """Average win rate, as a percentage string."""
  dailyProfitRateList: list[FuturesBrokerTraderDailyRateDailyProfitRateListItem]
  """Daily profit-rate history."""
  dailyProfitList: list[FuturesBrokerTraderDailyProfitDailyProfitListItem]
  """Daily profit-amount history."""
  followerTotalProfit: str
  """Total profit earned by this trader's followers, in USDT."""
  profitRate24hList: list[FuturesBrokerTraderDailyRateProfitRate24hListItem]
  """24-hour profit-rate history."""
  profit24hList: list[FuturesBrokerTraderDailyProfitProfit24hListItem]
  """24-hour profit-amount history."""
  lastTradeTime: str
  """Most recent trade time, as a millisecond epoch string."""
  tradeDays: str
  """Number of days this trader has been active."""
  totalEquity: str
  """Total account equity. Confirmed live: the venue masks this as the literal string `"****"` rather than a number, presumably for the broker-view privacy of a trader that isn't the caller's own account."""


class BrokerTraders(RpcEndpoint):
  """`classic.copytrading.broker_traders`."""

  async def broker_traders(
    self,
    *,
    page_no: int | None = None,
    page_size: int | None = None,
  ) -> list[FuturesBrokerTrader]:
    """List the elite traders affiliated with the broker's agent account, futures.

    Args:
      page_no: Page number. Defaults to 1.
      page_size: Results per page.
    """
    params = {}
    if page_no is not None:
      params['pageNo'] = page_no
    if page_size is not None:
      params['pageSize'] = page_size
    return await self.authed_request(
      'GET',
      '/api/v2/copy/mix-broker/query-traders',
      params=params,
      validator=validator(list[FuturesBrokerTrader]),
    )

  async def broker_traders_paged(
    self,
    *,
    page_size: int | None = None,
    max_pages: int | None = None,
  ) -> AsyncIterator[list[FuturesBrokerTrader]]:
    """Yield successive pages of `broker_traders`.

    Requests `pageNo` from 1 upwards and stops on the first page shorter than
    `page_size`, or after `max_pages` pages when one is given.
    """
    page_no = 1
    pages = 0
    while True:
      response = await self.broker_traders(page_size=page_size, page_no=page_no)
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      if not response or (page_size is not None and len(response) < page_size):
        break
      page_no += 1
