# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.copytrading.spot_follower_current_orders` -- generated from `spec/endpoints/`."""

from typing_extensions import AsyncIterator
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class SpotFollowerCurrentOrder(TypedDict):
  """One currently-tracked (open) copy position."""

  trackingNo: str
  """Tracking order ID."""
  traderId: str
  """Copied trader's user ID."""
  buyFillSize: str
  """Quantity filled so far."""
  buyDelegateSize: str
  """Quantity still pending fill."""
  buyPrice: str
  """Average entry (buy) price."""
  unrealizedPL: str
  """Current unrealized profit/loss."""
  buyTime: str
  """Entry time, as a millisecond epoch string."""
  buyFee: str
  """Entry fee."""
  unrealizedPLR: str
  """Current unrealized return, as a percentage string."""
  symbol: str
  """Trading pair."""
  stopSurplusPrice: str | None
  """Take-profit trigger price for this tracked order, if set."""
  stopLossPrice: str | None
  """Stop-loss trigger price for this tracked order, if set."""


class SpotFollowerCurrentOrders(TypedDict):
  """Page of currently-tracked positions."""

  endId: str | None
  """Last tracking order ID in this page, or `null` when `trackingList` is empty. Confirmed live."""
  trackingList: list[SpotFollowerCurrentOrder]
  """Currently-tracked positions."""


class SpotFollowerCurrentOrdersEndpoint(RpcEndpoint):
  """`classic.copytrading.spot_follower_current_orders`."""

  async def spot_follower_current_orders(
    self,
    *,
    symbol: str | None = None,
    trader_id: str | None = None,
    id_less_than: str | None = None,
    id_greater_than: str | None = None,
    start_time: int | None = None,
    end_time: int | None = None,
    limit: int | None = None,
  ) -> SpotFollowerCurrentOrders:
    """List a follower's currently-tracked (open) copy positions, spot.

    Args:
      symbol: Trading pair, e.g. "BTCUSDT". Case-insensitive.
      trader_id: Trader user ID.
      id_less_than: Pagination cursor: return records older than this tracking/profit ID. Pass the previous page's `endId` to walk further back.
      id_greater_than: Pagination cursor: return records newer than this tracking/profit ID.
      start_time: Start of the queried range, as a millisecond timestamp.
      end_time: End of the queried range, as a millisecond timestamp.
      limit: Results per page. Default 20, maximum 50.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/copytrading/spot-copytrade/follower/Query-Current-Orders)
    """
    params = {}
    if symbol is not None:
      params['symbol'] = symbol
    if trader_id is not None:
      params['traderId'] = trader_id
    if id_less_than is not None:
      params['idLessThan'] = id_less_than
    if id_greater_than is not None:
      params['idGreaterThan'] = id_greater_than
    if start_time is not None:
      params['startTime'] = start_time
    if end_time is not None:
      params['endTime'] = end_time
    if limit is not None:
      params['limit'] = limit
    return await self.authed_request(
      'GET',
      '/api/v2/copy/spot-follower/query-current-orders',
      params=params,
      validator=validator(SpotFollowerCurrentOrders),
    )

  async def spot_follower_current_orders_paged(
    self,
    *,
    symbol: str | None = None,
    trader_id: str | None = None,
    id_greater_than: str | None = None,
    start_time: int | None = None,
    end_time: int | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
  ) -> AsyncIterator[SpotFollowerCurrentOrders]:
    """Yield successive pages of `spot_follower_current_orders`.

    Passes each page's token back as `idLessThan` and stops when a response carries no
    `endId`, or after `max_pages` pages when one is given.
    """
    id_less_than: str | None = None
    pages = 0
    while True:
      response = await self.spot_follower_current_orders(
        symbol=symbol,
        trader_id=trader_id,
        id_greater_than=id_greater_than,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        id_less_than=id_less_than,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      id_less_than = response.get('endId') if response is not None else None
      if not id_less_than:
        break
