# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.copytrading.spot_settings` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class SpotTraderLabel(TypedDict):
  """One tag."""

  id: str
  """Tag ID."""
  name: str
  """Tag name."""


class SpotTraderSymbolInfo(TypedDict):
  """One symbol's trade amount overview."""

  maxQuoteSize: str
  """Maximum single order amount, in quote currency."""
  surplusQuoteSize: str
  """Remaining available amount, in quote currency."""
  symbol: str
  """Trading pair."""


class SpotTraderTraceSymbol(TypedDict):
  """One tradeable symbol."""

  enable: Literal['YES', 'NO']
  """Whether this symbol is open for followers to copy."""
  symbol: str
  """Trading pair."""
  minOpenCount: str
  """Minimum trade quantity for this symbol."""


class SpotTraderConfiguration(TypedDict):
  """A trader's own copy-trading configuration."""

  removeLimitUsdt: str
  """Maximum follower asset value, in USDT, that can still be removed."""
  spotInfoList: list[SpotTraderSymbolInfo]
  """Per-symbol elite-trade amount overview."""
  labelList: list[SpotTraderLabel]
  """Tags available to attach to this trader's profile."""
  enable: Literal['YES', 'NO']
  """Whether elite (copy-tradeable) trading is active."""
  showAssetsMap: Literal['YES', 'NO']
  """Whether asset composition is shown publicly."""
  showEquity: Literal['YES', 'NO']
  """Whether total account equity is shown publicly."""
  traceSymbolList: list[SpotTraderTraceSymbol]
  """Trading pairs available for copy trading."""


class SpotSettings(RpcEndpoint):
  """`classic.copytrading.spot_settings`."""

  async def spot_settings(self) -> SpotTraderConfiguration:
    """Get a trader's own copy-trading configuration, spot: asset limits, feature enablement, tagged and tradeable symbols.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/copytrading/spot-copytrade/trader/Config-Query-Settings)
    """
    return await self.authed_request(
      'GET',
      '/api/v2/copy/spot-trader/config-query-settings',
      validator=validator(SpotTraderConfiguration),
    )
