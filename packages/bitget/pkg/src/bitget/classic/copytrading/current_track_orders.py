# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.copytrading.current_track_orders` -- generated from `spec/endpoints/`."""

from typing_extensions import AsyncIterator, Literal
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class FuturesTraderCurrentOrder(TypedDict):
  """One currently-tracked (open) copy position."""

  trackingNo: str
  """Tracking order ID."""
  openOrderId: str
  """Elite trader's opening order ID."""
  symbol: str
  """Trading pair."""
  posSide: Literal['long', 'short']
  """Position direction."""
  openLeverage: str
  """Leverage applied when opening."""
  openPriceAvg: str
  """Average entry price."""
  openTime: str
  """Position opening time, as a millisecond epoch string."""
  openSize: str
  """Position quantity."""
  presetStopSurplusPrice: str
  """Preset take-profit price. Empty string if unset."""
  presetStopLossPrice: str
  """Preset stop-loss price. Empty string if unset."""
  openFee: str
  """Opening fee, in USDT."""
  followCount: str
  """Number of followers copying this position."""


class FuturesTraderCurrentOrders(TypedDict):
  """Page of currently-tracked positions."""

  trackingList: list[FuturesTraderCurrentOrder]
  """Currently-tracked positions."""
  endId: str
  """Last tracking order ID in this page; pass as `idLessThan` to walk older, `idGreaterThan` to walk newer."""


class CurrentTrackOrders(RpcEndpoint):
  """`classic.copytrading.current_track_orders`."""

  async def current_track_orders(
    self,
    *,
    symbol: str | None = None,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    start_time: int | None = None,
    end_time: int | None = None,
    limit: int | None = None,
    id_greater_than: str | None = None,
    id_less_than: str | None = None,
  ) -> FuturesTraderCurrentOrders:
    """List a trader's currently-tracked (open) copy positions, futures.

    Args:
      symbol: Trading pair, e.g. "BTCUSDT". Case-insensitive.
      product_type: Which futures product family this request concerns.
      start_time: Start of the queried range, as a millisecond timestamp.
      end_time: End of the queried range, as a millisecond timestamp.
      limit: Results per page. Default 20, maximum 50.
      id_greater_than: Pagination cursor: return records newer than this tracking/profit ID.
      id_less_than: Pagination cursor: return records older than this tracking/profit ID. Pass the previous page's `endId` to walk further back.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/copytrading/future-copytrade/trader/Trader-Order-Current-Track)
    """
    params: dict = {
      'productType': product_type,
    }
    if symbol is not None:
      params['symbol'] = symbol
    if start_time is not None:
      params['startTime'] = start_time
    if end_time is not None:
      params['endTime'] = end_time
    if limit is not None:
      params['limit'] = limit
    if id_greater_than is not None:
      params['idGreaterThan'] = id_greater_than
    if id_less_than is not None:
      params['idLessThan'] = id_less_than
    return await self.authed_request(
      'GET',
      '/api/v2/copy/mix-trader/order-current-track',
      params=params,
      validator=validator(FuturesTraderCurrentOrders),
    )

  async def current_track_orders_paged(
    self,
    *,
    symbol: str | None = None,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    start_time: int | None = None,
    end_time: int | None = None,
    limit: int | None = None,
    id_greater_than: str | None = None,
    max_pages: int | None = None,
  ) -> AsyncIterator[FuturesTraderCurrentOrders]:
    """Yield successive pages of `current_track_orders`.

    Passes each page's token back as `idLessThan` and stops when a response carries no
    `endId`, or after `max_pages` pages when one is given.
    """
    id_less_than: str | None = None
    pages = 0
    while True:
      response = await self.current_track_orders(
        symbol=symbol,
        product_type=product_type,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        id_greater_than=id_greater_than,
        id_less_than=id_less_than,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      id_less_than = response.get('endId') if response is not None else None
      if not id_less_than:
        break
