# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.institutional_loan.repaid_history` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from bitget.core import Timestamp, timestamp
from bitget.core.endpoint.rpc import RpcEndpoint


class LoanRepaidRecord(TypedDict):
  """One institutional loan repayment record."""

  repayOrderId: str
  """Repayment order id."""
  businessType: str
  """Business type this repayment applies to."""
  repayType: str
  """Repayment type, e.g. manual or automatic."""
  repaidTime: Timestamp
  """Repayment time, Unix millisecond timestamp."""
  coin: str
  """Repaid coin."""
  repaidAmount: str
  """Repaid principal amount, in `coin`."""
  repaidInterest: str
  """Repaid interest amount, in `coin`."""


class RepaidHistory(RpcEndpoint):
  """`classic.institutional_loan.repaid_history`."""

  async def repaid_history(
    self,
    *,
    start_time: Timestamp | None = None,
    end_time: Timestamp | None = None,
    limit: int | None = None,
  ) -> list[LoanRepaidRecord]:
    """Get the caller's institutional loan repayment history.

    Args:
      start_time: Range start, Unix millisecond timestamp.
      end_time: Range end, Unix millisecond timestamp.
      limit: Max entries to return.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/instloan/order/Get-Repaid-History)
    """
    params = {}
    if start_time is not None:
      params['startTime'] = timestamp.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp.dump(end_time)
    if limit is not None:
      params['limit'] = limit
    return await self.authed_request(
      'GET',
      '/api/v2/spot/ins-loan/repaid-history',
      params=params,
      validator=validator(list[LoanRepaidRecord]),
    )
