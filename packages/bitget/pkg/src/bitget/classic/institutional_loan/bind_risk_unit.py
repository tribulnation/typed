# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.institutional_loan.bind_risk_unit` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class BindRiskUnitRequest(TypedDict):
  """A bind/unbind operation for one sub-UID."""

  riskUnitId: NotRequired[str]
  """Risk unit ID. Required when called by the parent account; not required when called by the risk-unit account itself."""
  uid: str
  """Sub-UID to bind or unbind. A risk unit supports up to 50 UIDs."""
  operate: Literal['bind', 'unbind']
  """Operation to perform."""


class BindRiskUnit(RpcEndpoint):
  """`classic.institutional_loan.bind_risk_unit`."""

  async def bind_risk_unit(self, bind_risk_unit_request: BindRiskUnitRequest) -> str:
    """Bind or unbind a sub-UID to an institutional-loan risk unit.

    Args:
      bind_risk_unit_request: UID to bind or unbind.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/instloan/account/Bind-Risk-Unit)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/spot/ins-loan/bind-uid',
      json=bind_risk_unit_request,
      validator=validator(str),
    )
