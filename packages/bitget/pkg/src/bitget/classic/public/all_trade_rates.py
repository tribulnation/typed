# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.public.all_trade_rates` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class SymbolTradeRate(TypedDict):
  """Maker/taker fee rate for one trading pair."""

  symbol: str
  """Trading pair, e.g. `BTCUSDT`."""
  makerFeeRate: str
  """Maker (limit order) fee rate, as a fraction, e.g. `0.0001` = 0.01%."""
  takerFeeRate: str
  """Taker (market order) fee rate, as a fraction, e.g. `0.0004` = 0.04%."""


class AllTradeRates(RpcEndpoint):
  """`classic.public.all_trade_rates`."""

  async def all_trade_rates(
    self,
    *,
    business_type: Literal['spot', 'mix', 'margin'],
  ) -> list[SymbolTradeRate]:
    """Get the maker/taker fee rates applicable to the caller for every symbol within a specified business line.

    Args:
      business_type: Business line to list fee rates for.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/common/public/Get-All-Trade-Rate)
    """
    params: dict = {
      'businessType': business_type,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/common/all-trade-rate',
      params=params,
      validator=validator(list[SymbolTradeRate]),
    )
