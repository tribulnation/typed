# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.public.trade_rate` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class TradeRate(TypedDict):
  """Maker/taker fee rate for one trading pair."""

  makerFeeRate: str
  """Maker (limit order) fee rate, as a fraction, e.g. `0.0002` = 0.02%."""
  takerFeeRate: str
  """Taker (market order) fee rate, as a fraction, e.g. `0.0006` = 0.06%."""


class TradeRateEndpoint(RpcEndpoint):
  """`classic.public.trade_rate`."""

  async def trade_rate(
    self,
    *,
    symbol: str,
    business_type: Literal['spot', 'mix', 'margin'],
  ) -> TradeRate:
    """Get the maker/taker fee rate applicable to the caller for a specific trading pair and business line.

    Args:
      symbol: Trading pair, e.g. `BTCUSDT`.
      business_type: Business line the trading pair belongs to.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/common/public/Get-Trade-Rate)
    """
    params: dict = {
      'symbol': symbol,
      'businessType': business_type,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/common/trade-rate',
      params=params,
      validator=validator(TradeRate),
    )
