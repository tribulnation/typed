# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.public.funding_assets` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class FundingAsset(TypedDict):
  """One coin's funding-account balance."""

  coin: str
  """Coin symbol."""
  available: str
  """Available balance."""
  frozen: str
  """Amount currently frozen."""
  usdtValue: str
  """USDT-denominated value."""


class FundingAssets(RpcEndpoint):
  """`classic.public.funding_assets`."""

  async def funding_assets(self, *, coin: str | None = None) -> list[FundingAsset]:
    """Get the caller's funding-account assets.

    Args:
      coin: Restrict to one coin. Defaults to all coins.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/common/account/Funding-Assets)
    """
    params = {}
    if coin is not None:
      params['coin'] = coin
    return await self.authed_request(
      'GET',
      '/api/v2/account/funding-assets',
      params=params,
      validator=validator(list[FundingAsset]),
    )
