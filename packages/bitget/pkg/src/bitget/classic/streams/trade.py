# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic_streams.trade` -- generated from `spec/endpoints/`."""

from typing_extensions import Any, Literal, NotRequired
from typed_core.util import StreamManager
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.stream import StreamEndpoint


class Trade(TypedDict):
  """One public trade (taker fill)."""

  ts: str
  """Fill (transaction) time, Unix epoch milliseconds as a string."""
  price: str
  """Transaction price."""
  size: str
  """Transaction quantity."""
  side: Literal['buy', 'sell']
  """Taker side of the transaction."""
  tradeId: str
  """Transaction ID."""


class TradeArgResponsemessageArg(TypedDict):
  """Channel identifying object for the `trade` subscription."""

  instType: Literal['SPOT', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product line type: `SPOT`, or a futures product type (`USDT-FUTURES`, `COIN-FUTURES`, `USDC-FUTURES`)."""
  channel: Literal['trade']
  """Channel name."""
  instId: str
  """Subscribed identifier."""


class TradeArgResponsereplyArg(TypedDict):
  """Channel identifying object for the `trade` subscription."""

  instType: Literal['SPOT', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product line type: `SPOT`, or a futures product type (`USDT-FUTURES`, `COIN-FUTURES`, `USDC-FUTURES`)."""
  channel: Literal['trade']
  """Channel name."""
  instId: str
  """Subscribed identifier."""


class TradeParams(TypedDict):
  instType: Literal['SPOT', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product line type: `SPOT`, or a futures product type (`USDT-FUTURES`, `COIN-FUTURES`, `USDC-FUTURES`)."""
  instId: str
  """Product ID to subscribe to, e.g. `BTCUSDT`."""


class TradePush(TypedDict):
  """Push frame for the `TradePush` channel. The core performs no envelope unwrap for streams (see spec/core.md WebSocket): callers receive this whole frame, not one unwrapped `data` element."""

  action: Literal['snapshot', 'update']
  """Push data action: `snapshot` (initial batch), then `update`."""
  arg: TradeArgResponsemessageArg
  data: list[Trade]
  """Subscription data."""
  ts: int
  """Message generation time, Unix epoch milliseconds."""


class TradeReply(TypedDict):
  """Acknowledgement for a `subscribe` request. Per the upstream WebSocket intro page, every field but `event` is documented "No" (optional); `code`/`msg` are populated only when `event` is `error`."""

  event: Literal['subscribe', 'error']
  """Event name: `subscribe` on success, `error` on failure."""
  code: NotRequired[str]
  """Error code, present only on a failed request."""
  msg: NotRequired[str]
  """Error message, present only on a failed request."""
  arg: NotRequired[TradeArgResponsereplyArg]


class TradeEndpoint(StreamEndpoint):
  """`classic_streams.trade`."""

  def trade(
    self,
    inst_type: Literal['SPOT', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    inst_id: str,
  ) -> 'StreamManager[TradePush, Any, Any]':
    """Get public trade (taker fill) data for a product, in real time.

    Args:
      inst_type: Product line type: `SPOT`, or a futures product type (`USDT-FUTURES`, `COIN-FUTURES`, `USDC-FUTURES`).
      inst_id: Product ID to subscribe to, e.g. `BTCUSDT`.

    References:
      - [Official docs](https://www.bitget.com/api-doc/spot/websocket/public/Trades-Channel)
    """
    params = {
      'instType': inst_type,
      'instId': inst_id,
    }
    return self.subscribe('trade', params, validator=validator(TradePush))
