# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.tax.spot_records` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class SpotTaxRecord(TypedDict):
  id: str
  """Unique record id."""
  coin: str
  """Coin symbol."""
  spotTaxType: str
  """Transaction category. The venue documents that a closed set of roughly 90 values exists (e.g. "Interest", "Buy", "Sell") without publishing the full list."""
  amount: str
  """Quantity involved."""
  fee: str
  """Fee charged."""
  balance: str
  """Account balance after the record."""
  bizOrderId: str
  """Related business order id."""
  ts: str
  """Record creation time, ms epoch."""


class SpotRecords(RpcEndpoint):
  """`classic.tax.spot_records`."""

  async def spot_records(
    self,
    *,
    coin: str | None = None,
    start_time: int,
    end_time: int,
    limit: int | None = None,
    id_less_than: str | None = None,
  ) -> list[SpotTaxRecord]:
    """List the authenticated user's spot tax records (trades, transfers, fees, etc.) over a time window of at most 30 days.

    Args:
      coin: Restrict results to a single coin. Returns all coins when omitted.
      start_time: Range start, ms epoch.
      end_time: Range end, ms epoch. Must be at most 30 days after startTime.
      limit: Maximum number of records to return.
      id_less_than: Pagination cursor: return records with id less than this value. Read from the id of the last record on the previous page.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/tax/Get-Spot-Account-Record)
    """
    params: dict = {
      'startTime': start_time,
      'endTime': end_time,
    }
    if coin is not None:
      params['coin'] = coin
    if limit is not None:
      params['limit'] = limit
    if id_less_than is not None:
      params['idLessThan'] = id_less_than
    return await self.authed_request(
      'GET',
      '/api/v2/tax/spot-record',
      params=params,
      validator=validator(list[SpotTaxRecord]),
    )
