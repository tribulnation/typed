# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.broker.trade_volume` -- generated from `spec/endpoints/`."""

from typing_extensions import AsyncIterator
from typed_core.validation import TypedDict, validator
from bitget.core import Timestamp, timestamp
from bitget.core.endpoint.rpc import RpcEndpoint


class BrokerTradeVolume(TypedDict):
  """One sub-account's trading volume."""

  uid: str
  """Sub-account uid."""
  volume: str
  """Total trading volume, converted to USDT."""
  spotVolume: str
  """Spot trading volume, converted to USDT."""
  futureVolume: str
  """Futures trading volume, converted to USDT."""


class TradeVolume(RpcEndpoint):
  """`classic.broker.trade_volume`."""

  async def trade_volume(
    self,
    *,
    start_time: Timestamp | None = None,
    end_time: Timestamp | None = None,
    page_size: int | None = None,
    page_no: int | None = None,
  ) -> list[BrokerTradeVolume]:
    """Get trading volume for the broker's sub-accounts, page-paginated.

    Args:
      start_time: Range start, Unix millisecond timestamp.
      end_time: Range end, Unix millisecond timestamp.
      page_size: Entries per page.
      page_no: Page number, 1-indexed.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/broker/Get-Broker-Trade-Volume)
    """
    params = {}
    if start_time is not None:
      params['startTime'] = timestamp.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp.dump(end_time)
    if page_size is not None:
      params['pageSize'] = page_size
    if page_no is not None:
      params['pageNo'] = page_no
    return await self.authed_request(
      'GET',
      '/api/v2/broker/trade-volume',
      params=params,
      validator=validator(list[BrokerTradeVolume]),
    )

  async def trade_volume_paged(
    self,
    *,
    start_time: Timestamp | None = None,
    end_time: Timestamp | None = None,
    page_size: int | None = None,
    max_pages: int | None = None,
  ) -> AsyncIterator[list[BrokerTradeVolume]]:
    """Yield successive pages of `trade_volume`.

    Requests `pageNo` from 1 upwards and stops on the first page shorter than
    `page_size`, or after `max_pages` pages when one is given.
    """
    page_no = 1
    pages = 0
    while True:
      response = await self.trade_volume(
        start_time=start_time, end_time=end_time, page_size=page_size, page_no=page_no
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      if not response or (page_size is not None and len(response) < page_size):
        break
      page_no += 1
