# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.broker.create_subaccount_deposit_address` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from bitget.core import Timestamp
from bitget.core.endpoint.rpc import RpcEndpoint


class CreateSubaccountDepositAddressRequest(TypedDict):
  """Sub-account deposit address request."""

  subUid: str
  """Uid of the sub-account."""
  coin: str
  """Coin to generate a deposit address for."""
  chain: NotRequired[str]
  """Chain to generate the address on. Required for multi-chain coins."""


class SubaccountDepositAddress(TypedDict):
  """A broker sub-account's deposit address."""

  subUid: str
  """Sub-account uid."""
  coin: str
  """The coin."""
  address: str
  """Deposit address."""
  chain: str
  """Chain the address is on."""
  tag: str
  """Memo/tag required by some chains, empty string when not applicable."""
  url: str
  """Block-explorer URL for the address."""
  cTime: Timestamp
  """Creation time, Unix millisecond timestamp."""


class CreateSubaccountDepositAddress(RpcEndpoint):
  """`classic.broker.create_subaccount_deposit_address`."""

  async def create_subaccount_deposit_address(
    self,
    create_subaccount_deposit_address_request: CreateSubaccountDepositAddressRequest,
  ) -> SubaccountDepositAddress:
    """Create/get a deposit address for a broker sub-account.

    Args:
      create_subaccount_deposit_address_request: Sub-account deposit address request.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/broker/subaccount/Subaccount-Deposit-Address)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/broker/account/subaccount-address',
      json=create_subaccount_deposit_address_request,
      validator=validator(SubaccountDepositAddress),
    )
