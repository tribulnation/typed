# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.broker.modify_subaccount_api_key` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class ModifiedSubaccountApiKey(TypedDict):
  """A modified broker sub-account API key."""

  subUid: str
  """Uid of the sub-account the key belongs to."""
  apiKey: str
  """The API key."""
  label: str
  """Label of the API key."""
  ipList: list[str]
  """IP allowlist bound to the API key."""
  permType: str
  """Permission template applied to the key."""
  permList: list[str]
  """Permissions granted to the API key."""


class ModifySubaccountApiKeyRequest(TypedDict):
  """Broker sub-account API key update."""

  subUid: str
  """Uid of the sub-account the key belongs to."""
  apiKey: str
  """The API key to modify."""
  label: NotRequired[str]
  """New label for the API key."""
  passphrase: str
  """The API key's passphrase, required to authorize the modification."""
  ipList: NotRequired[list[str]]
  """New IP allowlist for the API key."""
  permType: NotRequired[str]
  """New permission template applied to the key."""
  permList: list[
    Literal[
      'withdraw',
      'transfer',
      'spot_trade',
      'contract_trade',
      'read',
      'deposit',
      'margin_trade',
    ]
  ]
  """New permissions to grant the API key."""


class ModifySubaccountApiKey(RpcEndpoint):
  """`classic.broker.modify_subaccount_api_key`."""

  async def modify_subaccount_api_key(
    self,
    modify_subaccount_api_key_request: ModifySubaccountApiKeyRequest,
  ) -> ModifiedSubaccountApiKey:
    """Modify a broker sub-account API key's label, IP allowlist, or permissions.

    Args:
      modify_subaccount_api_key_request: Broker sub-account API key update.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/broker/manage/Modify-Subaccount-Apikey)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/broker/manage/modify-subaccount-apikey',
      json=modify_subaccount_api_key_request,
      validator=validator(ModifiedSubaccountApiKey),
    )
