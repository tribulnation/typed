# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.broker.agent_customer_deposits` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from bitget.core import Timestamp
from bitget.core.endpoint.rpc import RpcEndpoint


class AgentCustomerDeposit(TypedDict):
  """One customer deposit record."""

  orderId: str
  """Deposit order id."""
  uid: str
  """Customer uid."""
  depositTime: Timestamp
  """Deposit time, Unix millisecond timestamp."""
  depositCoin: str
  """Deposited coin."""
  depositAmount: str
  """Deposited amount, in `depositCoin`."""


class AgentCustomerDepositRequest(TypedDict):
  """Filter for an agent's per-customer deposit records."""

  startTime: NotRequired[Timestamp]
  """Range start, Unix millisecond timestamp."""
  endTime: NotRequired[Timestamp]
  """Range end, Unix millisecond timestamp."""
  pageNo: NotRequired[int]
  """Page number, 1-indexed."""
  pageSize: NotRequired[int]
  """Entries per page."""
  uid: NotRequired[str]
  """Restrict to this customer uid."""
  showSub: NotRequired[Literal['yes', 'no']]
  """Whether to include the customer's own sub-accounts in the results."""


class AgentCustomerDeposits(RpcEndpoint):
  """`classic.broker.agent_customer_deposits`."""

  async def agent_customer_deposits(
    self,
    agent_customer_deposit_request: AgentCustomerDepositRequest,
  ) -> list[AgentCustomerDeposit]:
    """Get an agent's per-customer deposit records, page-paginated.

    Args:
      agent_customer_deposit_request: Filter for an agent's per-customer deposit records.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/broker/customer/Get-Customer-Deposit)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/broker/customer-deposit',
      json=agent_customer_deposit_request,
      validator=validator(list[AgentCustomerDeposit]),
    )
