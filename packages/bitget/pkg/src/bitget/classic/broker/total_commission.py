# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.broker.total_commission` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from bitget.core import Timestamp, timestamp
from bitget.core.endpoint.rpc import RpcEndpoint


class TotalCommissionFuturesBreakdown(TypedDict):
  """One day's futures commission breakdown."""

  futuresTradingVolume: str
  """Futures trading volume, converted to USDT."""
  futuresTradingFee: str
  """Futures trading fee charged, converted to USDT."""
  futuresPureTradingFee: str
  """Futures trading fee before broker mark-up, converted to USDT."""
  futuresCommission: str
  """Futures commission earned, converted to USDT."""


class TotalCommissionSpotBreakdown(TypedDict):
  """One day's spot commission breakdown."""

  spotTradingVolume: str
  """Spot trading volume, converted to USDT."""
  spotTradingFee: str
  """Spot trading fee charged, converted to USDT."""
  spotPureTradingFee: str
  """Spot trading fee before broker mark-up, converted to USDT."""
  spotCommission: str
  """Spot commission earned, converted to USDT."""


class BrokerTotalCommission(TypedDict):
  """One day's total commission breakdown."""

  date: str
  """Date this breakdown applies to, `YYYYMMDD`."""
  totalTradingVolume: str
  """Total trading volume across spot and futures, converted to USDT."""
  totalActiveTraders: str
  """Number of sub-accounts that traded this day."""
  totalCommission: str
  """Total commission earned across spot and futures, converted to USDT."""
  spot: TotalCommissionSpotBreakdown
  futures: TotalCommissionFuturesBreakdown


class TotalCommission(RpcEndpoint):
  """`classic.broker.total_commission`."""

  async def total_commission(
    self,
    *,
    start_time: Timestamp | None = None,
    end_time: Timestamp | None = None,
  ) -> list[BrokerTotalCommission]:
    """Get the broker's total daily commission breakdown across spot and futures.

    Args:
      start_time: Range start, Unix millisecond timestamp. Must be set together with `endTime`, or both omitted.
      end_time: Range end, Unix millisecond timestamp. Must be set together with `startTime`, or both omitted.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/broker/Get-Total-Commission)
    """
    params = {}
    if start_time is not None:
      params['startTime'] = timestamp.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp.dump(end_time)
    return await self.authed_request(
      'GET',
      '/api/v2/broker/total-commission',
      params=params,
      validator=validator(list[BrokerTotalCommission]),
    )
