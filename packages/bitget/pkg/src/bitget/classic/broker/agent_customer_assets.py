# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.broker.agent_customer_assets` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from bitget.core import Timestamp
from bitget.core.endpoint.rpc import RpcEndpoint


class AgentCustomerAsset(TypedDict):
  """One customer's total asset balance."""

  balance: str
  """Total asset balance, converted to USDT."""
  uid: str
  """Customer uid."""
  uTime: Timestamp
  """Last update time, Unix millisecond timestamp."""
  remark: str
  """Free-text remark, empty string when not set."""


class AgentCustomerAssetRequest(TypedDict):
  """Filter for an agent's per-customer asset balances."""

  pageNo: NotRequired[int]
  """Page number, 1-indexed."""
  pageSize: NotRequired[int]
  """Entries per page."""
  uid: NotRequired[str]
  """Restrict to this customer uid."""
  showSub: NotRequired[Literal['yes', 'no']]
  """Whether to include the customer's own sub-accounts in the results."""


class AgentCustomerAssets(RpcEndpoint):
  """`classic.broker.agent_customer_assets`."""

  async def agent_customer_assets(
    self,
    agent_customer_asset_request: AgentCustomerAssetRequest,
  ) -> list[AgentCustomerAsset]:
    """Get an agent's per-customer asset balances, page-paginated.

    Args:
      agent_customer_asset_request: Filter for an agent's per-customer asset balances.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/broker/customer/Get-Customer-Asset)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/broker/customer-asset',
      json=agent_customer_asset_request,
      validator=validator(list[AgentCustomerAsset]),
    )
