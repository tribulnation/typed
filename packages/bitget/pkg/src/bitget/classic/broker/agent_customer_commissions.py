# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.broker.agent_customer_commissions` -- generated from `spec/endpoints/`."""

from typing_extensions import AsyncIterator, Literal
from typed_core.validation import TypedDict, validator
from bitget.core import Timestamp, timestamp
from bitget.core.endpoint.rpc import RpcEndpoint


class AgentCustomerCommission(TypedDict):
  """One customer's commission detail for one day/symbol."""

  uid: str
  """Customer uid."""
  date: str
  """Date this entry applies to."""
  coin: str
  """Fee coin."""
  symbol: str
  """Trading pair."""
  productType: str
  """Product type. Known values include `SPOT`, `MARGIN`, `USDT-FUTURES`, `COIN-FUTURES`, `USDC-FUTURES`; set not confirmed closed, see notes."""
  dealAmount: str
  """Traded amount, in quote coin."""
  fee: str
  """Fee charged, in `coin`."""
  feeDeduction: str
  """Fee deducted via discount, in `coin`."""
  activityBonusDeduct: str
  """Fee deducted via activity bonus, in `coin`."""
  spotCouponDeduct: str
  """Fee deducted via spot coupon, in `coin`."""
  futuresCouponDeduct: str
  """Fee deducted via futures coupon, in `coin`."""
  spotFeeDiscountDeduct: str
  """Fee deducted via spot fee discount, in `coin`."""
  negativeMakerFeeDeduct: str
  """Fee deducted via negative maker fee, in `coin`."""
  feePaid: str
  """Fee actually paid, in `coin`."""
  rebateAmount: str
  """Rebate/commission amount earned for this entry, in `coin`."""
  userTotalRebateAmount: str
  """This customer's cumulative rebate amount, in `coin`."""
  dayTotalRebateAmount: str
  """Total rebate amount for this day across customers, in `coin`."""
  totalRebateAmount: str
  """Total cumulative rebate amount across customers, in `coin`."""


class AgentCustomerCommissions(TypedDict):
  """One page of per-customer commission details."""

  endId: str
  """Cursor value for the next page: pass as `idLessThan` to continue."""
  commissionList: list[AgentCustomerCommission]
  """Commission entries in this page."""


class AgentCustomerCommissionsEndpoint(RpcEndpoint):
  """`classic.broker.agent_customer_commissions`."""

  async def agent_customer_commissions(
    self,
    *,
    start_time: Timestamp | None = None,
    end_time: Timestamp | None = None,
    id_less_than: str | None = None,
    limit: int | None = None,
    uid: str | None = None,
    coin: str | None = None,
    symbol: str | None = None,
    show_sub: Literal['yes', 'no'] | None = None,
  ) -> AgentCustomerCommissions:
    """Get an agent's per-customer commission details, cursor-paginated.

    Args:
      start_time: Range start, Unix millisecond timestamp.
      end_time: Range end, Unix millisecond timestamp.
      id_less_than: Pagination cursor: return entries with an id less than this.
      limit: Max entries to return.
      uid: Restrict to this customer uid.
      coin: Restrict to this fee coin.
      symbol: Restrict to this trading pair.
      show_sub: Whether to include the customer's own sub-accounts in the results.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/broker/customer/Get-Customer-Commissions)
    """
    params = {}
    if start_time is not None:
      params['startTime'] = timestamp.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp.dump(end_time)
    if id_less_than is not None:
      params['idLessThan'] = id_less_than
    if limit is not None:
      params['limit'] = limit
    if uid is not None:
      params['uid'] = uid
    if coin is not None:
      params['coin'] = coin
    if symbol is not None:
      params['symbol'] = symbol
    if show_sub is not None:
      params['showSub'] = show_sub
    return await self.authed_request(
      'GET',
      '/api/v2/broker/customer-commissions',
      params=params,
      validator=validator(AgentCustomerCommissions),
    )

  async def agent_customer_commissions_paged(
    self,
    *,
    start_time: Timestamp | None = None,
    end_time: Timestamp | None = None,
    limit: int | None = None,
    uid: str | None = None,
    coin: str | None = None,
    symbol: str | None = None,
    show_sub: Literal['yes', 'no'] | None = None,
    max_pages: int | None = None,
  ) -> AsyncIterator[AgentCustomerCommissions]:
    """Yield successive pages of `agent_customer_commissions`.

    Passes each page's token back as `idLessThan` and stops on the first empty page, or
    after `max_pages` pages when one is given.
    """
    id_less_than: str | None = None
    pages = 0
    while True:
      response = await self.agent_customer_commissions(
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        uid=uid,
        coin=coin,
        symbol=symbol,
        show_sub=show_sub,
        id_less_than=id_less_than,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      rows = response.get('commissionList') if response is not None else None
      if not rows:
        break
      id_less_than = response.get('endId') if response is not None else None
