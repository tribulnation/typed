# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.broker.info` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from bitget.core import Timestamp
from bitget.core.endpoint.rpc import RpcEndpoint


class BrokerInfo(TypedDict):
  """A broker main-account's sub-account capacity."""

  subAccountSize: str
  """Current number of sub-accounts."""
  maxSubAccountSize: str
  """Maximum number of sub-accounts this broker account may create."""
  uTime: Timestamp
  """Last update time, Unix millisecond timestamp."""


class Info(RpcEndpoint):
  """`classic.broker.info`."""

  async def info(self) -> BrokerInfo:
    """Get the broker main-account's sub-account capacity.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/broker/subaccount/Get-Broker-Info)
    """
    return await self.authed_request(
      'GET',
      '/api/v2/broker/account/info',
      validator=validator(BrokerInfo),
    )
