# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.broker.agent_customer_trade_volume` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from bitget.core import Timestamp
from bitget.core.endpoint.rpc import RpcEndpoint


class AgentCustomerTradeVolume(TypedDict):
  """One customer's trading volume for one period."""

  uid: str
  """Customer uid."""
  volumn: str
  """Total trading volume, converted to USDT. (Sic: API field name.)"""
  spotVolume: str
  """Spot trading volume, converted to USDT."""
  futuresVolume: str
  """Futures trading volume, converted to USDT."""
  time: str
  """Period this entry applies to."""


class AgentCustomerTradeVolumeRequest(TypedDict):
  """Filter for an agent's per-customer trading volume."""

  startTime: NotRequired[Timestamp]
  """Range start, Unix millisecond timestamp."""
  endTime: NotRequired[Timestamp]
  """Range end, Unix millisecond timestamp."""
  pageNo: NotRequired[int]
  """Page number, 1-indexed."""
  pageSize: NotRequired[int]
  """Entries per page."""
  uid: NotRequired[str]
  """Restrict to this customer uid."""
  showSub: NotRequired[Literal['yes', 'no']]
  """Whether to include the customer's own sub-accounts in the results."""


class AgentCustomerTradeVolumeEndpoint(RpcEndpoint):
  """`classic.broker.agent_customer_trade_volume`."""

  async def agent_customer_trade_volume(
    self,
    agent_customer_trade_volume_request: AgentCustomerTradeVolumeRequest,
  ) -> list[AgentCustomerTradeVolume]:
    """Get an agent's per-customer trading volume, page-paginated.

    Args:
      agent_customer_trade_volume_request: Filter for an agent's per-customer trading volume.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/broker/customer/Get-Customer-Trade-Volume)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/broker/customer-trade-volume',
      json=agent_customer_trade_volume_request,
      validator=validator(list[AgentCustomerTradeVolume]),
    )
