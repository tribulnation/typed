# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.broker.subaccount_api_key_list` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class SubaccountApiKeyListItem(TypedDict):
  """One broker sub-account API key."""

  subUid: str
  """Uid of the sub-account the key belongs to."""
  label: str
  """Label of the API key."""
  apiKey: str
  """The API key."""
  secretKey: str
  """The API secret key."""
  permType: str
  """Permission template applied to the key."""
  permList: list[str]
  """Permissions granted to the API key."""
  ipList: list[str]
  """IP allowlist bound to the API key."""


class SubaccountApiKeyList(RpcEndpoint):
  """`classic.broker.subaccount_api_key_list`."""

  async def subaccount_api_key_list(
    self,
    *,
    sub_uid: str,
  ) -> list[SubaccountApiKeyListItem]:
    """List a broker sub-account's API keys.

    Args:
      sub_uid: Uid of the sub-account.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/broker/manage/Get-Subaccount-Apikey-List)
    """
    params: dict = {
      'subUid': sub_uid,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/broker/manage/subaccount-apikey-list',
      params=params,
      validator=validator(list[SubaccountApiKeyListItem]),
    )
