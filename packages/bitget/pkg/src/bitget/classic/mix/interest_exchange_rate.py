# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.interest_exchange_rate` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class MixInterestExchangeRateTier(TypedDict):
  """One interest exchange rate tier."""

  tier: str
  """Tier number."""
  minAmount: str
  """Lower bound of this tier's liability amount."""
  maxAmount: str
  """Upper bound of this tier's liability amount."""
  exchangeRate: str
  """Exchange rate applied within this tier."""


class MixInterestExchangeRateGroup(TypedDict):
  """Interest exchange rate tiers for one coin."""

  coin: str
  """Coin the tiers apply to."""
  excahngeRateList: list[MixInterestExchangeRateTier]
  """Tiers for this coin, by liability amount. Confirmed live as the misspelled `excahngeRateList` on the wire (a venue typo, not a typo in this spec)."""


class InterestExchangeRate(RpcEndpoint):
  """`classic.mix.interest_exchange_rate`."""

  async def interest_exchange_rate(self) -> list[MixInterestExchangeRateGroup]:
    """List coin-to-coin interest exchange rate tiers for union-margin liability conversion, public.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/market/Get-Exchange-Rate)
    """
    return await self.request(
      'GET',
      '/api/v2/mix/market/exchange-rate',
      validator=validator(list[MixInterestExchangeRateGroup]),
    )
