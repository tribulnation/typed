# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.position` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import validator
from bitget.types import MixPosition
from bitget.core.endpoint.rpc import RpcEndpoint


class Position(RpcEndpoint):
  """`classic.mix.position`."""

  async def position(
    self,
    *,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    symbol: str,
    margin_coin: str,
  ) -> list[MixPosition]:
    """Get open positions for one symbol, private. A hedge-mode symbol may return both a long and a short row.

    Args:
      product_type: Which futures product family this request concerns.
      symbol: Futures symbol, e.g. "BTCUSDT".
      margin_coin: Margin coin, e.g. "USDT".

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/position/Get-Single-Position)
    """
    params: dict = {
      'productType': product_type,
      'symbol': symbol,
      'marginCoin': margin_coin,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/mix/position/single-position',
      params=params,
      validator=validator(list[MixPosition]),
    )
