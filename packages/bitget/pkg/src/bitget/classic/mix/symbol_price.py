# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.symbol_price` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from bitget.core import Timestamp
from bitget.core.endpoint.rpc import RpcEndpoint


class MixSymbolPrice(TypedDict):
  """Price snapshot for one symbol."""

  symbol: str
  """Futures symbol."""
  price: str
  """Last traded price."""
  indexPrice: str
  """Index price."""
  markPrice: str
  """Mark price, used for margin and liquidation calculations."""
  ts: Timestamp
  """Snapshot time."""


class SymbolPrice(RpcEndpoint):
  """`classic.mix.symbol_price`."""

  async def symbol_price(
    self,
    *,
    symbol: str,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
  ) -> list[MixSymbolPrice]:
    """Get one symbol's last/index/mark price, public.

    Args:
      symbol: Futures symbol, e.g. "BTCUSDT".
      product_type: Which futures product family this request concerns.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/market/Get-Symbol-Price)
    """
    params: dict = {
      'symbol': symbol,
      'productType': product_type,
    }
    return await self.request(
      'GET',
      '/api/v2/mix/market/symbol-price',
      params=params,
      validator=validator(list[MixSymbolPrice]),
    )
