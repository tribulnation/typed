# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.position_tier` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class MixPositionTier(TypedDict):
  """One position-size tier."""

  symbol: str
  """Futures symbol."""
  level: str
  """Tier number."""
  startUnit: str
  """Lower bound of this tier's position size, in base coin."""
  endUnit: str
  """Upper bound of this tier's position size, in base coin."""
  leverage: str
  """Maximum leverage allowed within this tier."""
  keepMarginRate: str
  """Maintenance margin rate applied within this tier."""


class PositionTier(RpcEndpoint):
  """`classic.mix.position_tier`."""

  async def position_tier(
    self,
    *,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    symbol: str,
  ) -> list[MixPositionTier]:
    """List a symbol's position-tier ladder, public. Each tier states the maximum leverage and maintenance-margin rate for a range of position sizes.

    Args:
      product_type: Which futures product family this request concerns.
      symbol: Futures symbol, e.g. "BTCUSDT".

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/market/Get-Position-Tier)
    """
    params: dict = {
      'productType': product_type,
      'symbol': symbol,
    }
    return await self.request(
      'GET',
      '/api/v2/mix/market/query-position-lever',
      params=params,
      validator=validator(list[MixPositionTier]),
    )
