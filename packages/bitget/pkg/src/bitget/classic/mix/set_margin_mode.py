# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.set_margin_mode` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class MixMarginModeSettings(TypedDict):
  """Margin mode now in effect for this symbol/margin coin."""

  symbol: str
  """Futures symbol."""
  marginCoin: str
  """Margin coin."""
  longLeverage: str
  """Leverage applied to long positions (or the single leverage, in one-way mode)."""
  shortLeverage: str
  """Leverage applied to short positions (or the single leverage, in one-way mode)."""
  marginMode: Literal['isolated', 'crossed']
  """Margin mode governing how margin is allocated to a position."""


class MixSetMarginModeRequest(TypedDict):
  """Margin-mode update request."""

  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  productType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Which futures product family the request or response concerns."""
  marginCoin: str
  """Margin coin, e.g. "USDT"."""
  marginMode: Literal['isolated', 'crossed']
  """Margin mode governing how margin is allocated to a position."""


class SetMarginMode(RpcEndpoint):
  """`classic.mix.set_margin_mode`."""

  async def set_margin_mode(
    self,
    mix_set_margin_mode_request: MixSetMarginModeRequest,
  ) -> MixMarginModeSettings:
    """Switch a symbol between isolated and cross margin mode, private.

    Args:
      mix_set_margin_mode_request: Margin-mode update request.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/account/Change-Margin-Mode)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/mix/account/set-margin-mode',
      json=mix_set_margin_mode_request,
      validator=validator(MixMarginModeSettings),
    )
