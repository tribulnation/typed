# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.ticker` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import validator
from bitget.types import MixTicker
from bitget.core.endpoint.rpc import RpcEndpoint


class Ticker(RpcEndpoint):
  """`classic.mix.ticker`."""

  async def ticker(
    self,
    *,
    symbol: str,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
  ) -> list[MixTicker]:
    """Get one symbol's 24h ticker snapshot, public.

    Args:
      symbol: Futures symbol, e.g. `"BTCUSDT"`.
      product_type: Which futures product family this request concerns.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/market/Get-Ticker-Information)
    """
    params: dict = {
      'symbol': symbol,
      'productType': product_type,
    }
    return await self.request(
      'GET',
      '/api/v2/mix/market/ticker',
      params=params,
      validator=validator(list[MixTicker]),
    )
