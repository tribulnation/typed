# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.vip_fee_rate` -- generated from `spec/endpoints/`."""

from typed_core.validation import validator
from bitget.types import MixVipFeeRateTier
from bitget.core.endpoint.rpc import RpcEndpoint


class VipFeeRate(RpcEndpoint):
  """`classic.mix.vip_fee_rate`."""

  async def vip_fee_rate(self) -> list[MixVipFeeRateTier]:
    """List futures VIP fee-rate tiers, public. Tiers are keyed by 30-day trading volume and asset balance; each tier states its taker/maker fee rate and withdrawal allowances.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/market/Get-VIP-Fee-Rate)
    """
    return await self.request(
      'GET',
      '/api/v2/mix/market/vip-fee-rate',
      validator=validator(list[MixVipFeeRateTier]),
    )
