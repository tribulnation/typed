# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.position_adl_rank` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class MixPositionAdlRank(TypedDict):
  """ADL rank for one open position."""

  symbol: str
  """Futures symbol."""
  marginCoin: str
  """Margin coin."""
  adlRank: str
  """Auto-deleveraging rank/level for this position."""
  rank: str
  """Percentile rank underlying `adlRank`."""
  holdSide: Literal['long', 'short']
  """Which side of a hedge-mode position this concerns."""


class PositionAdlRank(RpcEndpoint):
  """`classic.mix.position_adl_rank`."""

  async def position_adl_rank(
    self,
    *,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
  ) -> list[MixPositionAdlRank]:
    """Get the auto-deleveraging (ADL) rank of each open position, private. A higher rank is deleveraged first when the venue must auto-deleverage counterparties to a liquidation.

    Args:
      product_type: Which futures product family this request concerns.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/position/Get-Position-ADL-Rank)
    """
    params: dict = {
      'productType': product_type,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/mix/position/adlRank',
      params=params,
      validator=validator(list[MixPositionAdlRank]),
    )
