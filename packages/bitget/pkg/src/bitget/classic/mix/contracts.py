# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.contracts` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class MixContract(TypedDict):
  """One futures contract."""

  symbol: str
  """Futures symbol."""
  baseCoin: str
  """Base coin."""
  quoteCoin: str
  """Quote coin."""
  buyLimitPriceRatio: str
  """Maximum ratio a buy order's price may exceed the mark price by."""
  sellLimitPriceRatio: str
  """Maximum ratio a sell order's price may undercut the mark price by."""
  feeRateUpRatio: str
  """Ratio by which the taker fee rate may rise under high volatility."""
  makerFeeRate: str
  """Maker fee rate."""
  takerFeeRate: str
  """Taker fee rate."""
  openCostUpRatio: str
  """Ratio by which open cost may rise under high volatility."""
  supportMarginCoins: list[str]
  """Coins that may be used as margin for this symbol."""
  minTradeNum: str
  """Minimum order size, in base coin."""
  priceEndStep: str
  """Price step multiplier used together with `pricePlace`."""
  volumePlace: str
  """Decimal places used for size."""
  pricePlace: str
  """Decimal places used for price."""
  sizeMultiplier: str
  """Order size must be a multiple of this value."""
  symbolType: Literal['perpetual', 'delivery']
  """Contract type."""
  minTradeUSDT: str
  """Minimum order value, in USDT."""
  maxSymbolOrderNum: str
  """Maximum open order count for this symbol."""
  maxProductOrderNum: str
  """Maximum open order count across the product type."""
  maxPositionNum: str
  """Maximum open position count."""
  symbolStatus: str
  """Trading status of the symbol, e.g. listed/delisted state."""
  offTime: str
  """Scheduled delisting time, or `"-1"` when not scheduled."""
  limitOpenTime: str
  """Time trading limits were applied, or `"-1"` when none apply."""
  deliveryTime: str
  """Delivery settlement time, for delivery contracts; `"-1"` for perpetuals."""
  deliveryStartTime: str
  """Delivery countdown start time, for delivery contracts; `"-1"` for perpetuals."""
  launchTime: str
  """Symbol launch time."""
  fundInterval: str
  """Funding interval, in hours."""
  minLever: str
  """Minimum leverage."""
  maxLever: str
  """Maximum leverage."""
  posLimit: str
  """Position limit ratio relative to open interest."""
  maintainTime: str
  """Scheduled maintenance time, or `"-1"` when none scheduled."""
  isRwa: str
  """Whether the symbol represents a real-world/tokenized asset."""


class Contracts(RpcEndpoint):
  """`classic.mix.contracts`."""

  async def contracts(
    self,
    *,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    symbol: str | None = None,
  ) -> list[MixContract]:
    """List futures contracts, public.

    Args:
      product_type: Which futures product family this request concerns.
      symbol: Restrict to a single symbol, e.g. "BTCUSDT".

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/contract/market/Get-All-Symbols-Contracts)
    """
    params: dict = {
      'productType': product_type,
    }
    if symbol is not None:
      params['symbol'] = symbol
    return await self.request(
      'GET',
      '/api/v2/mix/market/contracts',
      params=params,
      validator=validator(list[MixContract]),
    )
