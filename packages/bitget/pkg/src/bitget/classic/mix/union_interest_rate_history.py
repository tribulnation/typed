# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.union_interest_rate_history` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from bitget.core import Timestamp
from bitget.core.endpoint.rpc import RpcEndpoint


class MixUnionInterestRateObservation(TypedDict):
  """One historical interest-rate observation."""

  ts: Timestamp
  """Observation time."""
  annualInterestRate: str
  """Annualized interest rate."""
  dailyInterestRate: str
  """Daily interest rate."""


class MixUnionInterestRateHistory(TypedDict):
  """Historical interest rate series for one coin."""

  coin: str
  """Coin the history is for."""
  historyInterestRateList: list[MixUnionInterestRateObservation]
  """Interest rate observations, most recent first."""


class UnionInterestRateHistory(RpcEndpoint):
  """`classic.mix.union_interest_rate_history`."""

  async def union_interest_rate_history(
    self, *, coin: str
  ) -> MixUnionInterestRateHistory:
    """Historical interest rates for a union-margin coin, public.

    Args:
      coin: Coin to look up, e.g. `"USDT"`.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/market/Get-Union-Interest-Rate-History)
    """
    params: dict = {
      'coin': coin,
    }
    return await self.request(
      'GET',
      '/api/v2/mix/market/union-interest-rate-history',
      params=params,
      validator=validator(MixUnionInterestRateHistory),
    )
