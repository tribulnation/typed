# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.place_tpsl_order` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class MixOrderResult(TypedDict):
  """Identifiers of the order just acted on."""

  orderId: str
  """Order ID assigned by the venue."""
  clientOid: str
  """Client-supplied order ID, echoed back."""


class MixPlaceTpslOrderRequest(TypedDict):
  """TP/SL order request."""

  marginCoin: str
  """Margin coin, e.g. "USDT"."""
  productType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Which futures product family the request or response concerns."""
  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  planType: Literal['profit_plan', 'loss_plan', 'moving_plan', 'pos_profit', 'pos_loss']
  """Which TP/SL family this order is."""
  triggerPrice: str
  """Price that triggers this order."""
  triggerType: NotRequired[Literal['fill_price', 'mark_price']]
  """Price source a trigger is evaluated against."""
  executePrice: NotRequired[str]
  """Limit execution price once triggered. Omit to execute at market."""
  holdSide: Literal['long', 'short', 'buy', 'sell']
  """Side of the position this order closes."""
  size: str
  """Size to close, in base coin. Closes the full position when omitted, per the venue's default (unconfirmed live)."""
  rangeRate: NotRequired[str]
  """Callback range, for a `moving_plan` (trailing) trigger."""
  clientOid: NotRequired[str]
  """Caller-supplied order ID, echoed back on responses."""
  stpMode: NotRequired[Literal['none', 'cancel_taker', 'cancel_maker', 'cancel_both']]
  """Self-trade prevention mode."""


class PlaceTpslOrder(RpcEndpoint):
  """`classic.mix.place_tpsl_order`."""

  async def place_tpsl_order(
    self,
    mix_place_tpsl_order_request: MixPlaceTpslOrderRequest,
  ) -> MixOrderResult:
    """Place a stand-alone take-profit or stop-loss trigger order against an existing position, private.

    Args:
      mix_place_tpsl_order_request: TP/SL order request.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/plan/Place-Tpsl-Order)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/mix/order/place-tpsl-order',
      json=mix_place_tpsl_order_request,
      validator=validator(MixOrderResult),
    )
