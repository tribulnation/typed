# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.accounts` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class MixAccountAssetBalance(TypedDict):
  """One coin's balance."""

  coin: str
  """Coin."""
  balance: str
  """Balance of this coin."""


class MixAccountAssetSummary(TypedDict):
  """Futures account asset detail for one margin coin."""

  marginCoin: str
  """Margin coin."""
  locked: str
  """Amount locked by open orders or pending withdrawals."""
  available: str
  """Amount available to open new positions or withdraw."""
  crossedMaxAvailable: str
  """Maximum amount available under cross margin mode."""
  isolatedMaxAvailable: str
  """Maximum amount available under isolated margin mode."""
  maxTransferOut: str
  """Maximum amount that may be transferred out right now."""
  accountEquity: str
  """Account equity, in margin coin."""
  usdtEquity: str
  """Account equity, converted to USDT."""
  btcEquity: str
  """Account equity, converted to BTC."""
  crossedRiskRate: str
  """Risk rate under cross margin mode."""
  unrealizedPL: str
  """Unrealized profit and loss across positions."""
  coupon: str
  """Coupon (bonus) balance applied toward margin."""
  crossedUnrealizedPL: str
  """Unrealized profit and loss on cross-margin positions."""
  isolatedUnrealizedPL: str
  """Unrealized profit and loss on isolated-margin positions."""
  assetMode: str
  """Asset mode: whether margin is shared across symbols ("union") or held per-symbol ("single")."""
  grant: str
  """Promotional grant credit balance, distinct from `coupon`. Confirmed live as an empty string on an account with no active grant."""
  unionTotalMargin: str
  """Total margin under the union (shared-margin) account model."""
  unionAvailable: str
  """Amount available under the union account model."""
  unionMm: str
  """Maintenance margin required under the union account model."""
  assetList: list[MixAccountAssetBalance]
  """Per-coin balances contributing to this margin coin's equity."""
  isolatedMargin: str
  """Margin allocated under isolated margin mode."""
  crossedMargin: str
  """Margin allocated under cross margin mode."""


class Accounts(RpcEndpoint):
  """`classic.mix.accounts`."""

  async def accounts(
    self,
    *,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
  ) -> list[MixAccountAssetSummary]:
    """List all futures account asset details for a product type, private.

    Args:
      product_type: Which futures product family this request concerns.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/account/Get-Account-List)
    """
    params: dict = {
      'productType': product_type,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/mix/account/accounts',
      params=params,
      validator=validator(list[MixAccountAssetSummary]),
    )
