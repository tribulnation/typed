# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.oi_limit` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class MixOpenInterestLimit(TypedDict):
  """Open-interest notional limit for one symbol."""

  symbol: str
  """Futures symbol."""
  notionalValue: str
  """Per-account open-interest notional limit."""
  totalNotionalValue: str
  """Platform-wide open-interest notional limit."""


class OiLimit(RpcEndpoint):
  """`classic.mix.oi_limit`."""

  async def oi_limit(
    self,
    *,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    symbol: str | None = None,
  ) -> list[MixOpenInterestLimit]:
    """Get open-interest notional limits, public. Returns every symbol of the product type when `symbol` is omitted.

    Args:
      product_type: Which futures product family this request concerns.
      symbol: Restrict to a single symbol, e.g. "BTCUSDT".

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/market/Get-Open-Interest-Limit)
    """
    params: dict = {
      'productType': product_type,
    }
    if symbol is not None:
      params['symbol'] = symbol
    return await self.request(
      'GET',
      '/api/v2/mix/market/oi-limit',
      params=params,
      validator=validator(list[MixOpenInterestLimit]),
    )
