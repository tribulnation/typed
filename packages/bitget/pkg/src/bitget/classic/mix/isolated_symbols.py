# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.isolated_symbols` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class MixIsolatedSymbol(TypedDict):
  """One symbol in isolated margin mode."""

  symbol: str
  """Futures symbol."""
  marginMode: Literal['isolated']
  """Always `"isolated"` for a row in this list."""


class IsolatedSymbols(RpcEndpoint):
  """`classic.mix.isolated_symbols`."""

  async def isolated_symbols(
    self,
    *,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
  ) -> list[MixIsolatedSymbol]:
    """List symbols currently in isolated margin mode under the account, private.

    Args:
      product_type: Which futures product family this request concerns.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/account/Get-Isolated-Symbols)
    """
    params: dict = {
      'productType': product_type,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/mix/account/isolated-symbols',
      params=params,
      validator=validator(list[MixIsolatedSymbol]),
    )
