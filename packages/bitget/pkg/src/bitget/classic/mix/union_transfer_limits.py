# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.union_transfer_limits` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class MixUnionTransferLimits(TypedDict):
  """Transfer-in limit for one coin."""

  coin: str
  """Coin the limit applies to."""
  maxTransferIn: str
  """Maximum amount of this coin that may be transferred in."""


class UnionTransferLimits(RpcEndpoint):
  """`classic.mix.union_transfer_limits`."""

  async def union_transfer_limits(self, *, coin: str) -> MixUnionTransferLimits:
    """Get the maximum amount of one coin that may be transferred into the union-margin account, private.

    Args:
      coin: Coin to look up, e.g. "USDT".

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/account/Get-Union-Transfer-Limits)
    """
    params: dict = {
      'coin': coin,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/mix/account/transfer-limits',
      params=params,
      validator=validator(MixUnionTransferLimits),
    )
