# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.positions` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import validator
from bitget.types import MixPosition
from bitget.core.endpoint.rpc import RpcEndpoint


class Positions(RpcEndpoint):
  """`classic.mix.positions`."""

  async def positions(
    self,
    *,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    margin_coin: str | None = None,
  ) -> list[MixPosition]:
    """List all open positions for a product type, private.

    Args:
      product_type: Which futures product family this request concerns.
      margin_coin: Restrict to one margin coin, e.g. "USDT".

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/position/Get-All-Position)
    """
    params: dict = {
      'productType': product_type,
    }
    if margin_coin is not None:
      params['marginCoin'] = margin_coin
    return await self.authed_request(
      'GET',
      '/api/v2/mix/position/all-position',
      params=params,
      validator=validator(list[MixPosition]),
    )
