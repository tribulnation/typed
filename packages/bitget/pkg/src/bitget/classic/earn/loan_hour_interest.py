# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.earn.loan_hour_interest` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class LoanEstimate(TypedDict):
  """Estimated interest rate and borrowable amount."""

  hourInterest: str
  """Estimated hourly interest rate."""
  loanAmount: str
  """Estimated maximum borrowable amount given the pledge."""


class LoanHourInterest(RpcEndpoint):
  """`classic.earn.loan_hour_interest`."""

  async def loan_hour_interest(
    self,
    *,
    loan_coin: str,
    pledge_coin: str,
    daily: Literal['SEVEN', 'THIRTY'],
    pledge_amount: str,
  ) -> LoanEstimate:
    """Estimate the hourly interest rate and maximum borrowable amount for a loan/collateral coin pair, public.

    Args:
      loan_coin: Coin to borrow.
      pledge_coin: Coin to pledge as collateral.
      daily: Loan term tier.
      pledge_amount: Collateral amount to estimate against.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/loan/Get-Est-Interest-And-Load-Amount)
    """
    params: dict = {
      'loanCoin': loan_coin,
      'pledgeCoin': pledge_coin,
      'daily': daily,
      'pledgeAmount': pledge_amount,
    }
    return await self.request(
      'GET',
      '/api/v2/earn/loan/public/hour-interest',
      params=params,
      validator=validator(LoanEstimate),
    )
