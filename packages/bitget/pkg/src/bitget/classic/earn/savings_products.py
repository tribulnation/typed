# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.earn.savings_products` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from bitget.types import SavingsApyTier
from bitget.core.endpoint.rpc import RpcEndpoint


class SavingsProduct(TypedDict):
  """One Savings product."""

  productId: str
  """Product identifier."""
  coin: str
  """Savings coin."""
  periodType: Literal['flexible', 'fixed']
  """Subscription term structure."""
  period: NotRequired[str]
  """Fixed term length in days. Empty for flexible products."""
  apyType: Literal['single', 'ladder']
  """Interest calculation method."""
  advanceRedeem: NotRequired[str]
  """Whether early redemption before maturity is allowed (`Yes`/`No`). Empty for flexible products, confirmed live."""
  settleMethod: NotRequired[str]
  """Interest settlement timing (`daily`/`maturity`). Empty for flexible products, confirmed live."""
  apyList: list[SavingsApyTier]
  """APY tiers; a single-element list for `apyType: single`."""
  status: Literal[
    'not_started', 'in_progress', 'paused', 'completed', 'sold_out', 'off_line'
  ]
  """Product availability state."""
  productLevel: Literal['beginner', 'normal', 'vip']
  """Product tier classification."""


class SavingsProducts(RpcEndpoint):
  """`classic.earn.savings_products`."""

  async def savings_products(
    self,
    *,
    coin: str | None = None,
    filter: Literal['available', 'held', 'available_and_held', 'all'] | None = None,
  ) -> list[SavingsProduct]:
    """List Savings products, optionally restricted to one coin and/or the caller's participation status.

    Args:
      coin: Restrict to a single coin, e.g. `BTC`.
      filter: Restrict by the caller's participation status.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/savings/Savings-Products)
    """
    params = {}
    if coin is not None:
      params['coin'] = coin
    if filter is not None:
      params['filter'] = filter
    return await self.authed_request(
      'GET',
      '/api/v2/earn/savings/product',
      params=params,
      validator=validator(list[SavingsProduct]),
    )
