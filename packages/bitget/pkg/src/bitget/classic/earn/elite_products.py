# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.earn.elite_products` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from bitget.types import EliteSubscriptionCoin
from bitget.core.endpoint.rpc import RpcEndpoint


class EliteProduct(TypedDict):
  """One Elite product."""

  productId: str
  """Product identifier."""
  coin: str
  """Product base coin."""
  minApr: str
  """Minimum documented annual percentage rate."""
  maxApr: str
  """Maximum documented annual percentage rate."""
  subscriptionCoinList: list[EliteSubscriptionCoin]
  """Coins accepted for subscription."""
  sellOut: str
  """Whether the product is sold out. The reference SDK types this as `'YES' | 'NO' | string`, i.e. it does not treat the set as closed; left bare rather than declaring an enum the venue may extend."""


class EliteProducts(RpcEndpoint):
  """`classic.earn.elite_products`."""

  async def elite_products(self) -> list[EliteProduct]:
    """List Elite (on-chain structured/subscription) products.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/elite/Elite-Product-List)
    """
    return await self.authed_request(
      'GET',
      '/api/v2/earn/elite/product',
      validator=validator(list[EliteProduct]),
    )
