# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.earn.savings_subscribe_info` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from bitget.types import SavingsApyTier
from bitget.core.endpoint.rpc import RpcEndpoint


class SavingsSubscribeInfo(TypedDict):
  """Subscription detail for one Savings product."""

  singleMinAmount: str
  """Minimum amount per subscription."""
  singleMaxAmount: str
  """Maximum amount per subscription."""
  remainingAmount: str
  """Remaining subscribable quota across all users."""
  subscribePrecision: str
  """Decimal precision for the subscription amount."""
  profitPrecision: str
  """Decimal precision for interest amounts."""
  subscribeTime: str
  """Subscription window opening time, ms epoch."""
  interestTime: str
  """Time interest starts accruing, ms epoch."""
  settleTime: str
  """Interest settlement time, ms epoch."""
  expireTime: str
  """Product maturity/expiry time, ms epoch."""
  redeemTime: str
  """Time redemption becomes available, ms epoch."""
  settleMethod: str
  """Interest settlement timing (`daily`/`maturity`). Empty for flexible products, confirmed live."""
  apyList: list[SavingsApyTier]
  """APY tiers."""
  redeemDelay: str
  """Delay, in days, between requesting redemption and receiving funds."""


class SavingsSubscribeInfoEndpoint(RpcEndpoint):
  """`classic.earn.savings_subscribe_info`."""

  async def savings_subscribe_info(
    self,
    *,
    product_id: str,
    period_type: Literal['flexible', 'fixed'],
  ) -> SavingsSubscribeInfo:
    """Get subscription limits and timing for a Savings product before subscribing.

    Args:
      product_id: Product to preview.
      period_type: Term structure of the product.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/savings/Savings-Subscribe-Info)
    """
    params: dict = {
      'productId': product_id,
      'periodType': period_type,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/earn/savings/subscribe-info',
      params=params,
      validator=validator(SavingsSubscribeInfo),
    )
