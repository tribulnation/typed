# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.margin.cross.account.max_borrowable` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class CrossMarginMaxBorrowable(TypedDict):
  """Maximum borrowable amount for one coin under cross margin."""

  maxBorrowableAmount: NotRequired[str]
  """Maximum amount currently borrowable. Changes in real time."""
  coin: NotRequired[str]
  """Coin."""


class MaxBorrowable(RpcEndpoint):
  """`classic.margin.cross.account.max_borrowable`."""

  async def max_borrowable(self, *, coin: str) -> CrossMarginMaxBorrowable:
    """Query the maximum amount currently borrowable under cross margin. Rate limit: 10 requests/second per UID.

    Args:
      coin: Coin to check.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/margin/cross/account/Cross-Max-Borrowable-Amount)
    """
    params: dict = {
      'coin': coin,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/margin/crossed/account/max-borrowable-amount',
      params=params,
      validator=validator(CrossMarginMaxBorrowable),
    )
