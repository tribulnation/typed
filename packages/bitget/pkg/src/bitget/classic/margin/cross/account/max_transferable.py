# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.margin.cross.account.max_transferable` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class CrossMarginMaxTransferable(TypedDict):
  """Maximum transferable amount for one coin under cross margin."""

  coin: NotRequired[str]
  """Coin."""
  maxTransferOutAmount: NotRequired[str]
  """Maximum amount currently transferable out."""


class MaxTransferable(RpcEndpoint):
  """`classic.margin.cross.account.max_transferable`."""

  async def max_transferable(self, *, coin: str) -> CrossMarginMaxTransferable:
    """Query the maximum amount currently transferable out of cross margin. Rate limit: 10 requests/second per UID.

    Args:
      coin: Coin to check.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/margin/cross/account/Get-Cross-Max-Transfer-Out-Amount)
    """
    params: dict = {
      'coin': coin,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/margin/crossed/account/max-transfer-out-amount',
      params=params,
      validator=validator(CrossMarginMaxTransferable),
    )
