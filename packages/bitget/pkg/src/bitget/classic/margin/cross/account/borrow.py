# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.margin.cross.account.borrow` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class CrossMarginBorrowRequest(TypedDict):
  """Cross margin borrow request."""

  coin: str
  """Coin to borrow."""
  borrowAmount: str
  """Amount to borrow, up to 8 decimal places."""
  clientOid: NotRequired[str]
  """Caller-supplied idempotency id."""


class CrossMarginBorrowResult(TypedDict):
  """Result of a margin borrow."""

  loanId: NotRequired[str]
  """Loan id."""
  coin: NotRequired[str]
  """Borrowed coin."""
  borrowAmount: NotRequired[str]
  """Borrowed amount."""


class Borrow(RpcEndpoint):
  """`classic.margin.cross.account.borrow`."""

  async def borrow(
    self,
    cross_margin_borrow_request: CrossMarginBorrowRequest,
  ) -> CrossMarginBorrowResult:
    """Borrow a coin under cross margin. Rate limit: 10 requests/second per UID.

    Args:
      cross_margin_borrow_request: Borrow a coin under cross margin.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/margin/cross/account/Cross-Borrow)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/margin/crossed/account/borrow',
      json=cross_margin_borrow_request,
      validator=validator(CrossMarginBorrowResult),
    )
