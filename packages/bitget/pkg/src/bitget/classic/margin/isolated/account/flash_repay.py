# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.margin.isolated.account.flash_repay` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class IsolatedMarginFlashRepayRequest(TypedDict):
  """Isolated margin flash-repay request."""

  symbolList: NotRequired[list[str]]
  """Trading pairs to repay, up to 100. Omit to repay every isolated pair."""


class IsolatedMarginFlashRepayResultEntry(TypedDict):
  """Flash-repay outcome for one trading pair."""

  repayId: NotRequired[str]
  """Repayment id."""
  symbol: NotRequired[str]
  """Trading pair."""
  result: NotRequired[Literal['success', 'failure']]
  """Outcome of the repayment request."""


class FlashRepay(RpcEndpoint):
  """`classic.margin.isolated.account.flash_repay`."""

  async def flash_repay(
    self,
    isolated_margin_flash_repay_request: IsolatedMarginFlashRepayRequest,
  ) -> list[IsolatedMarginFlashRepayResultEntry]:
    """Immediately repay isolated margin debt for one or more trading pairs, using account balance, without placing a trade. Repays every pair if none is given. Rate limit: 10 requests/second per UID.

    Args:
      isolated_margin_flash_repay_request: Immediately repay isolated margin debt for one or more trading pairs, using account balance, without placing a trade.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/margin/isolated/account/Isolated-Flash-Repay)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/margin/isolated/account/flash-repay',
      json=isolated_margin_flash_repay_request,
      validator=validator(list[IsolatedMarginFlashRepayResultEntry]),
    )
