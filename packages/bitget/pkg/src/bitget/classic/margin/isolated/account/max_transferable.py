# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.margin.isolated.account.max_transferable` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class IsolatedMarginMaxTransferable(TypedDict):
  """Maximum transferable amounts for one trading pair under isolated margin."""

  baseCoin: NotRequired[str]
  """Base coin of the pair."""
  symbol: NotRequired[str]
  """Trading pair."""
  baseCoinMaxTransferOutAmount: NotRequired[str]
  """Maximum amount of the base coin currently transferable out."""
  quoteCoin: NotRequired[str]
  """Quote coin of the pair."""
  quoteCoinMaxTransferOutAmount: NotRequired[str]
  """Maximum amount of the quote coin currently transferable out."""


class MaxTransferable(RpcEndpoint):
  """`classic.margin.isolated.account.max_transferable`."""

  async def max_transferable(self, *, symbol: str) -> IsolatedMarginMaxTransferable:
    """Query the maximum amount currently transferable out of isolated margin. Rate limit: 10 requests/second per UID.

    Args:
      symbol: Trading pair to check.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/margin/isolated/account/Get-Isolated-Max-Transfer-Out-Amount)
    """
    params: dict = {
      'symbol': symbol,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/margin/isolated/account/max-transfer-out-amount',
      params=params,
      validator=validator(IsolatedMarginMaxTransferable),
    )
