# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.margin.isolated.order.cancel` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class IsolatedMarginCancelOrderRequest(TypedDict):
  """Margin order cancellation request. Either `orderId` or `clientOid` must be given."""

  symbol: str
  """Trading pair."""
  orderId: NotRequired[str]
  """Order id. Either this or `clientOid` is required."""
  clientOid: NotRequired[str]
  """Caller-supplied client order id. Either this or `orderId` is required."""


class IsolatedMarginCancelOrderResult(TypedDict):
  """Result of a margin order cancellation."""

  orderId: NotRequired[str]
  """Order id."""
  clientOid: NotRequired[str]
  """Caller-supplied client order id."""


class Cancel(RpcEndpoint):
  """`classic.margin.isolated.order.cancel`."""

  async def cancel(
    self,
    isolated_margin_cancel_order_request: IsolatedMarginCancelOrderRequest,
  ) -> IsolatedMarginCancelOrderResult:
    """Cancel one open isolated margin order. Rate limit: 10 requests/second per UID.

    Args:
      isolated_margin_cancel_order_request: Cancel one open isolated margin order.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/margin/isolated/trade/Isolated-Cancel-Order)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/margin/isolated/cancel-order',
      json=isolated_margin_cancel_order_request,
      validator=validator(IsolatedMarginCancelOrderResult),
    )
