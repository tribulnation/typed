# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.margin.isolated.account.tier_data` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class IsolatedMarginTier(TypedDict):
  """One leverage tier for a trading pair under isolated margin."""

  tier: NotRequired[str]
  """Tier number."""
  symbol: NotRequired[str]
  """Trading pair."""
  leverage: NotRequired[str]
  """Effective leverage at this tier. Global default 10x."""
  baseCoin: NotRequired[str]
  """Base coin of the pair."""
  quoteCoin: NotRequired[str]
  """Quote coin of the pair."""
  baseMaxBorrowableAmount: NotRequired[str]
  """Maximum borrowable amount of the base coin at this tier."""
  quoteMaxBorrowableAmount: NotRequired[str]
  """Maximum borrowable amount of the quote coin at this tier."""
  maintainMarginRate: NotRequired[str]
  """Maintenance margin rate at this tier."""
  initRate: NotRequired[str]
  """Initial margin rate at this tier."""


class TierData(RpcEndpoint):
  """`classic.margin.isolated.account.tier_data`."""

  async def tier_data(self, *, symbol: str) -> list[IsolatedMarginTier]:
    """Query the leverage/margin tier ladder for one trading pair under isolated margin.

    Args:
      symbol: Trading pair to check.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/margin/isolated/account/Isolated-Tier-Data)
    """
    params: dict = {
      'symbol': symbol,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/margin/isolated/tier-data',
      params=params,
      validator=validator(list[IsolatedMarginTier]),
    )
