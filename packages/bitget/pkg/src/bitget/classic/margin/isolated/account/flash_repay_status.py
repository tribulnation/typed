# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.margin.isolated.account.flash_repay_status` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class IsolatedMarginFlashRepayStatusEntry(TypedDict):
  """Flash-repay outcome for one repayment id."""

  repayId: NotRequired[str]
  """Repayment id."""
  status: NotRequired[str]
  """Repayment outcome."""


class IsolatedMarginFlashRepayStatusRequest(TypedDict):
  """Flash-repay status lookup."""

  idList: list[str]
  """Repayment ids to look up, up to 100."""


class FlashRepayStatus(RpcEndpoint):
  """`classic.margin.isolated.account.flash_repay_status`."""

  async def flash_repay_status(
    self,
    isolated_margin_flash_repay_status_request: IsolatedMarginFlashRepayStatusRequest,
  ) -> list[IsolatedMarginFlashRepayStatusEntry]:
    """Query the outcome of one or more previously-submitted isolated margin flash repayments. Rate limit: 10 requests/second per UID.

    Args:
      isolated_margin_flash_repay_status_request: Query the outcome of one or more previously-submitted isolated margin flash repayments.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/margin/isolated/account/Query-Isolated-Flash-Repay-Status)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/margin/isolated/account/query-flash-repay-status',
      json=isolated_margin_flash_repay_status_request,
      validator=validator(list[IsolatedMarginFlashRepayStatusEntry]),
    )
