# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.margin.isolated.account.assets` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from bitget.core import Timestamp
from bitget.core.endpoint.rpc import RpcEndpoint


class IsolatedMarginAsset(TypedDict):
  """One coin balance under this margin account."""

  symbol: NotRequired[str]
  """Trading pair this asset balance is scoped to."""
  coin: NotRequired[str]
  """Coin."""
  totalAmount: NotRequired[str]
  """Total balance."""
  available: NotRequired[str]
  """Available (unfrozen) balance."""
  frozen: NotRequired[str]
  """Balance frozen in open orders."""
  borrow: NotRequired[str]
  """Outstanding borrowed amount."""
  interest: NotRequired[str]
  """Outstanding interest owed."""
  net: NotRequired[str]
  """Net assets (available + frozen - borrow - interest). Liquidation is triggered once this crosses the venue's risk ratio."""
  coupon: NotRequired[str]
  """Trading-bonus balance."""
  cTime: NotRequired[Timestamp]
  """Creation time, Unix millisecond timestamp. Confirmed live: returned as a string, not an integer."""
  uTime: NotRequired[Timestamp]
  """Last update time, Unix millisecond timestamp. Confirmed live: returned as a string, not an integer."""


class Assets(RpcEndpoint):
  """`classic.margin.isolated.account.assets`."""

  async def assets(self, *, symbol: str | None = None) -> list[IsolatedMarginAsset]:
    """Query isolated margin account asset balances. Rate limit: 10 requests/second per UID.

    Args:
      symbol: Trading pair filter. Omit to list every pair.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/margin/isolated/account/Get-Isolated-Assets)
    """
    params = {}
    if symbol is not None:
      params['symbol'] = symbol
    return await self.authed_request(
      'GET',
      '/api/v2/margin/isolated/account/assets',
      params=params,
      validator=validator(list[IsolatedMarginAsset]),
    )
