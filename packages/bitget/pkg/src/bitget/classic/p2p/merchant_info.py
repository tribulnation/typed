# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.p2p.merchant_info` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class MerchantInfo(TypedDict):
  registerTime: str
  """Merchant registration time, ms epoch."""
  nickName: str
  """Merchant display name."""
  merchantId: str
  """Unique merchant id."""
  avgPaymentTime: str | None
  """Average payment time, in minutes. `null` when the merchant has no trade history yet."""
  avgReleaseTime: str | None
  """Average coin release time, in minutes. `null` when the merchant has no trade history yet."""
  totalTrades: str | None
  """Total number of completed trades. `null` when the merchant has no trade history yet."""
  totalBuy: str | None
  """Total number of buy orders. `null` when the merchant has no trade history yet."""
  totalSell: str | None
  """Total number of sell orders. `null` when the merchant has no trade history yet."""
  totalCompletionRate: str | None
  """Completion rate across all trades. `null` when the merchant has no trade history yet."""
  trades30d: str | None
  """Number of trades in the last 30 days. `null` when the merchant has no trade history yet."""
  sell30d: str | None
  """Number of sell orders in the last 30 days. `null` when the merchant has no trade history yet."""
  buy30d: str | None
  """Number of buy orders in the last 30 days. `null` when the merchant has no trade history yet."""
  completionRate30d: str | None
  """Completion rate over the last 30 days. `null` when the merchant has no trade history yet."""
  kycStatus: Literal['yes', 'no']
  """Whether the merchant has completed KYC verification."""
  emailBindStatus: Literal['yes', 'no']
  """Whether an email address is bound to the merchant account."""
  mobileBindStatus: Literal['yes', 'no']
  """Whether a phone number is bound to the merchant account."""
  email: str
  """Bound email address, masked."""
  mobile: str
  """Bound phone number, masked."""


class MerchantInfoEndpoint(RpcEndpoint):
  """`classic.p2p.merchant_info`."""

  async def merchant_info(self) -> MerchantInfo:
    """Get the authenticated user's own P2P merchant profile. Takes no parameters.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/p2p/Get-Merchant-Information)
    """
    return await self.authed_request(
      'GET',
      '/api/v2/p2p/merchantInfo',
      validator=validator(MerchantInfo),
    )
