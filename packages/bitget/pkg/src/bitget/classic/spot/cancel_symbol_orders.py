# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.cancel_symbol_orders` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class SpotCancelSymbolOrdersRequest(TypedDict):
  """Symbol to cancel every open order on."""

  symbol: str
  """Trading pair symbol."""


class SpotCancelSymbolOrdersResult(TypedDict):
  """Result of cancelling every open order on a symbol."""

  symbol: str
  """Trading pair symbol whose orders were cancelled."""


class CancelSymbolOrders(RpcEndpoint):
  """`classic.spot.cancel_symbol_orders`."""

  async def cancel_symbol_orders(
    self,
    spot_cancel_symbol_orders_request: SpotCancelSymbolOrdersRequest,
  ) -> SpotCancelSymbolOrdersResult:
    """Cancel every open order on one symbol.

    Args:
      spot_cancel_symbol_orders_request: Symbol to cancel every open order on.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/trade/Cancel-Symbol-Order)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/spot/trade/cancel-symbol-order',
      json=spot_cancel_symbol_orders_request,
      validator=validator(SpotCancelSymbolOrdersResult),
    )
