# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.history_candles` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import validator
from bitget.core import Timestamp, timestamp
from bitget.core.endpoint.rpc import RpcEndpoint


class HistoryCandles(RpcEndpoint):
  """`classic.spot.history_candles`."""

  async def history_candles(
    self,
    *,
    symbol: str,
    granularity: Literal[
      '1min',
      '5min',
      '15min',
      '30min',
      '1h',
      '4h',
      '6h',
      '12h',
      '1day',
      '3day',
      '1week',
      '1M',
      '6Hutc',
      '12Hutc',
      '1Dutc',
      '3Dutc',
      '1Wutc',
      '1Mutc',
    ],
    end_time: Timestamp,
    limit: int | None = None,
  ) -> list[tuple[str, str, str, str, str, str, str, str]]:
    """Get historical OHLCV candlestick data for a symbol, reaching further back than `candles`. Public, no authentication required.

    Args:
      symbol: Trading pair symbol, e.g. `BTCUSDT`.
      granularity: Candlestick interval.
      end_time: Range end, Unix millisecond timestamp. Candles strictly before this time are returned (confirmed live: the candle whose open time equals `endTime` is excluded).
      limit: Number of candles to return.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/market/Get-History-Candle-Data)
    """
    params: dict = {
      'symbol': symbol,
      'granularity': granularity,
      'endTime': timestamp.dump(end_time),
    }
    if limit is not None:
      params['limit'] = limit
    return await self.request(
      'GET',
      '/api/v2/spot/market/history-candles',
      params=params,
      validator=validator(list[tuple[str, str, str, str, str, str, str, str]]),
    )
