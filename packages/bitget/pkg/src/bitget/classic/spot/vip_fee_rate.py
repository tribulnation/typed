# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.vip_fee_rate` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class SpotVipFeeRate(TypedDict):
  """Spot fee rate schedule for one VIP tier."""

  level: str
  """VIP tier number, as a string. Confirmed live -- not an integer."""
  dealAmount: str
  """30-day trading volume threshold for this tier, as a decimal string."""
  assetAmount: str
  """Account asset threshold for this tier, as a decimal string."""
  takerFeeRate: str
  """Taker fee rate at this tier, as a fraction."""
  makerFeeRate: str
  """Maker fee rate at this tier, as a fraction."""
  btcWithdrawAmount: str
  """Daily BTC-denominated withdrawal limit at this tier, as a decimal string."""
  usdtWithdrawAmount: str
  """Daily USDT-denominated withdrawal limit at this tier, as a decimal string."""


class VipFeeRate(RpcEndpoint):
  """`classic.spot.vip_fee_rate`."""

  async def vip_fee_rate(self) -> list[SpotVipFeeRate]:
    """List the spot maker/taker fee rate schedule for every VIP tier. Public, no authentication required.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/market/Get-Vip-Fee-Rate)
    """
    return await self.request(
      'GET',
      '/api/v2/spot/market/vip-fee-rate',
      validator=validator(list[SpotVipFeeRate]),
    )
