# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.place_order` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from bitget.types import SpotOrderIdResult
from bitget.core.endpoint.rpc import RpcEndpoint


class SpotPlaceOrderRequest(TypedDict):
  """A spot order to place."""

  symbol: str
  """Trading pair symbol, e.g. `BTCUSDT`."""
  side: Literal['buy', 'sell']
  """Order side."""
  orderType: Literal['limit', 'market']
  """Order type."""
  force: Literal['gtc', 'post_only', 'fok', 'ioc']
  """Time-in-force / execution strategy."""
  price: NotRequired[str]
  """Limit price, as a decimal string. Required for a limit order, ignored for a market order."""
  size: str
  """Order size, as a decimal string. In base coin for a limit order; in quote coin for a market buy, base coin for a market sell."""
  clientOid: NotRequired[str]
  """Caller-supplied client order ID, echoed back on the order."""
  triggerPrice: NotRequired[str]
  """Trigger price for a TP/SL order, as a decimal string. Only meaningful when `tpslType` is `tpsl`."""
  tpslType: NotRequired[Literal['normal', 'tpsl']]
  """Whether this is a normal order or a take-profit/stop-loss order."""
  stpMode: NotRequired[Literal['none', 'cancel_taker', 'cancel_maker', 'cancel_both']]
  """Self-trade prevention mode."""
  presetTakeProfitPrice: NotRequired[str]
  """Take-profit price to attach to the position this order opens, as a decimal string."""
  executeTakeProfitPrice: NotRequired[str]
  """Execution price for the attached take-profit order, as a decimal string."""
  presetStopLossPrice: NotRequired[str]
  """Stop-loss price to attach to the position this order opens, as a decimal string."""
  executeStopLossPrice: NotRequired[str]
  """Execution price for the attached stop-loss order, as a decimal string."""


class PlaceOrder(RpcEndpoint):
  """`classic.spot.place_order`."""

  async def place_order(
    self,
    spot_place_order_request: SpotPlaceOrderRequest,
  ) -> SpotOrderIdResult:
    """Place a spot order.

    Args:
      spot_place_order_request: A spot order to place.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/trade/Place-Order)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/spot/trade/place-order',
      json=spot_place_order_request,
      validator=validator(SpotOrderIdResult),
    )
