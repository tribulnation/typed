# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.transfer` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class SpotTransferRequest(TypedDict):
  """An inter-product transfer."""

  fromType: Literal[
    'spot',
    'p2p',
    'coin_futures',
    'usdt_futures',
    'usdc_futures',
    'crossed_margin',
    'isolated_margin',
  ]
  """Bitget product-account type."""
  toType: Literal[
    'spot',
    'p2p',
    'coin_futures',
    'usdt_futures',
    'usdc_futures',
    'crossed_margin',
    'isolated_margin',
  ]
  """Bitget product-account type."""
  amount: str
  """Amount to transfer, as a decimal string."""
  coin: str
  """Coin to transfer."""
  symbol: NotRequired[str]
  """Margin symbol, required when `fromType`/`toType` is an isolated-margin account."""
  clientOid: NotRequired[str]
  """Caller-supplied client ID for this transfer."""


class SpotTransferResult(TypedDict):
  """Result of an inter-product transfer."""

  transferId: str
  """Exchange transfer ID."""
  clientOid: str | None
  """Client ID of the transfer."""


class Transfer(RpcEndpoint):
  """`classic.spot.transfer`."""

  async def transfer(
    self,
    spot_transfer_request: SpotTransferRequest,
  ) -> SpotTransferResult:
    """Transfer funds between the caller's own Bitget product accounts (e.g. spot to margin).

    Args:
      spot_transfer_request: An inter-product transfer.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/wallet/Transfer)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/spot/wallet/transfer',
      json=spot_transfer_request,
      validator=validator(SpotTransferResult),
    )
