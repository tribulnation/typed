# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.sub_main_transfer_records` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from bitget.core import Timestamp, timestamp
from bitget.core.endpoint.rpc import RpcEndpoint


class SpotMainSubTransferRecord(TypedDict):
  """One main-account/sub-account transfer."""

  coin: str
  """Coin transferred."""
  status: str
  """Current status of the transfer."""
  toType: Literal[
    'spot',
    'p2p',
    'coin_futures',
    'usdt_futures',
    'usdc_futures',
    'crossed_margin',
    'isolated_margin',
  ]
  """Bitget product-account type."""
  fromType: Literal[
    'spot',
    'p2p',
    'coin_futures',
    'usdt_futures',
    'usdc_futures',
    'crossed_margin',
    'isolated_margin',
  ]
  """Bitget product-account type."""
  size: str
  """Transferred amount, as a decimal string."""
  ts: Timestamp
  """Transfer time, Unix millisecond timestamp, as a string."""
  clientOid: str
  """Caller-supplied client ID for this transfer."""
  transferId: str
  """Exchange transfer ID."""
  fromUserId: str
  """User ID the transfer moved funds from."""
  toUserId: str
  """User ID the transfer moved funds to."""


class SubMainTransferRecords(RpcEndpoint):
  """`classic.spot.sub_main_transfer_records`."""

  async def sub_main_transfer_records(
    self,
    *,
    coin: str | None = None,
    role: Literal['initiator', 'receiver'] | None = None,
    sub_uid: str | None = None,
    start_time: Timestamp | None = None,
    end_time: Timestamp | None = None,
    client_oid: str | None = None,
    limit: int | None = None,
    id_less_than: str | None = None,
  ) -> list[SpotMainSubTransferRecord]:
    """List transfer records between the main account and its sub-accounts. Callable from either side.

    Args:
      coin: Restrict to a single coin.
      role: Restrict to transfers where the caller was the initiator or the receiver.
      sub_uid: Restrict to transfers involving a single sub-account.
      start_time: Range start, Unix millisecond timestamp.
      end_time: Range end, Unix millisecond timestamp.
      client_oid: Restrict to a single transfer by its client-supplied ID.
      limit: Number of records to return.
      id_less_than: Pagination cursor: return entries with an id less than this value (a `orderId`/`billId`/`tradeId` read out of the last row of the previous page).

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/account/Get-Sub-Main-Trans-Record)
    """
    params = {}
    if coin is not None:
      params['coin'] = coin
    if role is not None:
      params['role'] = role
    if sub_uid is not None:
      params['subUid'] = sub_uid
    if start_time is not None:
      params['startTime'] = timestamp.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp.dump(end_time)
    if client_oid is not None:
      params['clientOid'] = client_oid
    if limit is not None:
      params['limit'] = limit
    if id_less_than is not None:
      params['idLessThan'] = id_less_than
    return await self.authed_request(
      'GET',
      '/api/v2/spot/account/sub-main-trans-record',
      params=params,
      validator=validator(list[SpotMainSubTransferRecord]),
    )
