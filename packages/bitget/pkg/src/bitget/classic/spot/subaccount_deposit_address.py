# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.subaccount_deposit_address` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class SpotDepositAddress(TypedDict):
  """A deposit address for one coin on one chain."""

  address: str
  """Deposit address."""
  chain: str
  """Chain this address is on."""
  coin: str
  """Coin this address accepts."""
  tag: str
  """Memo/tag to include with the deposit, empty if not required."""
  url: str
  """Block-explorer URL for this address."""


class SubaccountDepositAddress(RpcEndpoint):
  """`classic.spot.subaccount_deposit_address`."""

  async def subaccount_deposit_address(
    self,
    *,
    sub_uid: str,
    coin: str,
    chain: str | None = None,
    size: str,
  ) -> SpotDepositAddress:
    """Get (or generate) a sub-account's deposit address for a coin. Called from the main account.

    Args:
      sub_uid: Sub-account user ID to get a deposit address for.
      coin: Coin to get a deposit address for.
      chain: Chain to get a deposit address on. Required for a multi-chain coin.
      size: Reserved for future use; the reference SDK requires it with no documented meaning.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/wallet/Get-Subaccount-Deposit-Address)
    """
    params: dict = {
      'subUid': sub_uid,
      'coin': coin,
      'size': size,
    }
    if chain is not None:
      params['chain'] = chain
    return await self.authed_request(
      'GET',
      '/api/v2/spot/wallet/subaccount-deposit-address',
      params=params,
      validator=validator(SpotDepositAddress),
    )
