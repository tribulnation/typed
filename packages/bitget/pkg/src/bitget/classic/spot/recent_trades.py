# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.recent_trades` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from bitget.core import Timestamp
from bitget.core.endpoint.rpc import RpcEndpoint


class SpotTrade(TypedDict):
  """One public trade."""

  symbol: str
  """Trading pair symbol."""
  tradeId: str
  """Trade ID."""
  side: str
  """Taker side of the trade."""
  price: str
  """Trade price, as a decimal string."""
  size: str
  """Trade size, as a decimal string."""
  ts: Timestamp
  """Trade time, Unix millisecond timestamp, as a string."""


class RecentTrades(RpcEndpoint):
  """`classic.spot.recent_trades`."""

  async def recent_trades(
    self, *, symbol: str, limit: int | None = None
  ) -> list[SpotTrade]:
    """Get the most recent public trades for a symbol. Public, no authentication required.

    Args:
      symbol: Trading pair symbol, e.g. `BTCUSDT`.
      limit: Number of trades to return.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/market/Get-Recent-Trades)
    """
    params: dict = {
      'symbol': symbol,
    }
    if limit is not None:
      params['limit'] = limit
    return await self.request(
      'GET',
      '/api/v2/spot/market/fills',
      params=params,
      validator=validator(list[SpotTrade]),
    )
