# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.transferable_coins` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import validator
from bitget.core.endpoint.rpc import RpcEndpoint


class TransferableCoins(RpcEndpoint):
  """`classic.spot.transferable_coins`."""

  async def transferable_coins(
    self,
    *,
    from_type: Literal[
      'spot',
      'p2p',
      'coin_futures',
      'usdt_futures',
      'usdc_futures',
      'crossed_margin',
      'isolated_margin',
    ],
    to_type: Literal[
      'spot',
      'p2p',
      'coin_futures',
      'usdt_futures',
      'usdc_futures',
      'crossed_margin',
      'isolated_margin',
    ],
  ) -> list[str]:
    """List coins that can be transferred between two given product accounts.

    Args:
      from_type: Source product account.
      to_type: Destination product account.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/wallet/Get-Transfer-Coin-Info)
    """
    params: dict = {
      'fromType': from_type,
      'toType': to_type,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/spot/wallet/transfer-coin-info',
      params=params,
      validator=validator(list[str]),
    )
