# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.cancel_order` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from bitget.types import SpotOrderIdResult
from bitget.core.endpoint.rpc import RpcEndpoint


class SpotCancelOrderRequest(TypedDict):
  """An order to cancel."""

  symbol: str
  """Trading pair symbol."""
  tpslType: NotRequired[Literal['normal', 'tpsl']]
  """Whether this is a normal order or a take-profit/stop-loss order."""
  orderId: NotRequired[str]
  """Exchange order ID of the order to cancel. Either this or `clientOid` is required."""
  clientOid: NotRequired[str]
  """Client order ID of the order to cancel. Either this or `orderId` is required."""


class CancelOrder(RpcEndpoint):
  """`classic.spot.cancel_order`."""

  async def cancel_order(
    self,
    spot_cancel_order_request: SpotCancelOrderRequest,
  ) -> SpotOrderIdResult:
    """Cancel an open spot order.

    Args:
      spot_cancel_order_request: An order to cancel.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/trade/Cancel-Order)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/spot/trade/cancel-order',
      json=spot_cancel_order_request,
      validator=validator(SpotOrderIdResult),
    )
