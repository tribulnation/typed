# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.account_info` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from bitget.core import Timestamp
from bitget.core.endpoint.rpc import RpcEndpoint


class SpotAccountInfo(TypedDict):
  """The caller's spot account profile."""

  userId: str
  """Account user ID."""
  inviterId: str
  """User ID of the account that invited this one, empty if none."""
  ips: str
  """IP allowlist configured on the API key, comma-separated."""
  authorities: list[str]
  """Permissions granted to the API key used for this request."""
  parentId: int
  """Main account's user ID; equal to `userId` for a main account."""
  traderType: str
  """Whether this account is a copy-trading trader/follower/neither."""
  channelCode: str
  """Referral channel code, empty if none."""
  channel: str
  """Referral channel name, empty if none."""
  regisTime: Timestamp
  """Account registration time, Unix millisecond timestamp, as a string."""


class AccountInfo(RpcEndpoint):
  """`classic.spot.account_info`."""

  async def account_info(self) -> SpotAccountInfo:
    """Get the caller's spot account profile.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/account/Get-Account-Info)
    """
    return await self.authed_request(
      'GET',
      '/api/v2/spot/account/info',
      validator=validator(SpotAccountInfo),
    )
