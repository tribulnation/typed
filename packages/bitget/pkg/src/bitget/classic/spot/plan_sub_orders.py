# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.plan_sub_orders` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class SpotPlanSubOrder(TypedDict):
  """One sub-order produced by a triggered plan order."""

  orderId: str
  """Exchange order ID of the resulting normal order."""
  price: str
  """Order price, as a decimal string."""
  type: Literal['limit', 'market']
  """Order type."""
  status: str
  """Current status of the resulting order."""


class PlanSubOrders(RpcEndpoint):
  """`classic.spot.plan_sub_orders`."""

  async def plan_sub_orders(self, *, plan_order_id: str) -> list[SpotPlanSubOrder]:
    """List the executed sub-order(s) a triggered plan order produced.

    Args:
      plan_order_id: Exchange plan-order ID whose executed sub-orders to list.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/plan/Get-Plan-Sub-Order)
    """
    params: dict = {
      'planOrderId': plan_order_id,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/spot/trade/plan-sub-order',
      params=params,
      validator=validator(list[SpotPlanSubOrder]),
    )
