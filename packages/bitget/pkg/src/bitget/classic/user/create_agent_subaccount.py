# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.user.create_agent_subaccount` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from bitget.core import Timestamp
from bitget.core.endpoint.rpc import RpcEndpoint


class AgentSubaccount(TypedDict):
  """An agent sub-account created together with its API key."""

  username: str
  """Sub-account username."""
  subUid: str
  """Uid assigned to the new sub-account."""
  apiKey: str
  """The new API key."""
  secret: str
  """The new API secret key. Shown once, at creation."""
  note: str
  """Free-text note for the sub-account."""
  createdTime: Timestamp
  """Creation time, Unix millisecond timestamp."""


class CreateAgentSubaccountRequest(TypedDict):
  """Agent sub-account to create."""

  username: str
  """Sub-account username. Lowercase letters only, max 20 characters."""
  passphrase: str
  """Passphrase for the sub-account's API key. 8-32 characters, letters and numbers."""
  note: NotRequired[str]
  """Optional free-text note for the sub-account."""


class CreateAgentSubaccount(RpcEndpoint):
  """`classic.user.create_agent_subaccount`."""

  async def create_agent_subaccount(
    self,
    create_agent_subaccount_request: CreateAgentSubaccountRequest,
  ) -> AgentSubaccount:
    """Create a dedicated sub-account for an AI agent instance. Agent sub-accounts cannot be edited after creation.

    Args:
      create_agent_subaccount_request: Agent sub-account to create.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/common/vsubaccount/Create-Agent-Subaccount)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/user/create-agent-subaccount',
      json=create_agent_subaccount_request,
      validator=validator(AgentSubaccount),
    )
