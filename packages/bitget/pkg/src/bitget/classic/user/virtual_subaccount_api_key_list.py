# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.user.virtual_subaccount_api_key_list` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class VirtualSubaccountApiKeyListItem(TypedDict):
  """One virtual sub-account API key."""

  subAccountUid: str
  """Uid of the virtual sub-account the key belongs to."""
  label: str
  """Label of the API key."""
  subAccountApiKey: str
  """The API key."""
  permList: list[str]
  """Permissions granted to the API key."""
  ipList: list[str]
  """IP allowlist bound to the API key."""


class VirtualSubaccountApiKeyList(RpcEndpoint):
  """`classic.user.virtual_subaccount_api_key_list`."""

  async def virtual_subaccount_api_key_list(
    self,
    *,
    sub_account_uid: str,
  ) -> list[VirtualSubaccountApiKeyListItem]:
    """List a virtual sub-account's API keys.

    Args:
      sub_account_uid: Uid of the virtual sub-account to list API keys for.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/common/vsubaccount/Get-Virtual-Subaccount-Apikey-List)
    """
    params: dict = {
      'subAccountUid': sub_account_uid,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/user/virtual-subaccount-apikey-list',
      params=params,
      validator=validator(list[VirtualSubaccountApiKeyListItem]),
    )
