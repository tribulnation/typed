# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.user.batch_create_virtual_subaccount_and_apikey` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from bitget.core.endpoint.rpc import RpcEndpoint


class BatchCreateVirtualSubaccountAndApiKeyRequest(TypedDict):
  """Virtual sub-account plus API key to create together."""

  subAccountName: str
  """Name for the new sub-account."""
  passphrase: str
  """Passphrase to set on the new API key."""
  label: str
  """Label for the new API key."""
  ipList: NotRequired[list[str]]
  """IP allowlist for the new API key. Omit for no IP restriction."""
  permList: NotRequired[list[str]]
  """Permissions to grant the new API key."""


class CreatedVirtualSubaccountApiKey(TypedDict):
  """A virtual sub-account created together with its API key."""

  subAccountUid: str
  """Uid assigned to the new sub-account."""
  subAccountName: str
  """The sub-account name."""
  label: str
  """Label of the new API key."""
  subAccountApiKey: str
  """The new API key."""
  secretKey: str
  """The new API secret key. Shown once, at creation."""
  permList: list[str]
  """Permissions granted to the API key."""
  ipList: list[str]
  """IP allowlist bound to the API key."""


class BatchCreateVirtualSubaccountAndApikey(RpcEndpoint):
  """`classic.user.batch_create_virtual_subaccount_and_apikey`."""

  async def batch_create_virtual_subaccount_and_apikey(
    self,
    batch_create_virtual_subaccount_and_api_key_request: BatchCreateVirtualSubaccountAndApiKeyRequest,
  ) -> list[CreatedVirtualSubaccountApiKey]:
    """Create a virtual sub-account together with an API key for it, in one call.

    Args:
      batch_create_virtual_subaccount_and_api_key_request: Virtual sub-account plus API key to create together.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/common/vsubaccount/Batch-Create-Subaccount-And-Apikey)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/user/batch-create-subaccount-and-apikey',
      json=batch_create_virtual_subaccount_and_api_key_request,
      validator=validator(list[CreatedVirtualSubaccountApiKey]),
    )
