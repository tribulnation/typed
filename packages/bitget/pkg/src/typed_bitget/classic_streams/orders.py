# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.stream import StreamEndpoint
from typed_bitget.schemas import ClassicOrderFeeDetailEntry, ClassicOrdersArg


class Parameters(TypedDict):
  instType: Literal['SPOT', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product line type: `SPOT`, or a futures product type (`USDT-FUTURES`, `COIN-FUTURES`, `USDC-FUTURES`)."""
  instId: NotRequired[str]
  """Product ID, or `default` for all symbols (required for settled futures contracts)."""


class MixOrder(TypedDict):
  """One mix (futures) order lifecycle update (opened, filled, cancelled)."""

  instId: str
  """Product ID, e.g. `ETHUSDT`."""
  orderId: str
  """Order ID."""
  clientOid: str
  """Customized order ID."""
  price: str
  """Order price."""
  size: str
  """Original order amount, in the base coin."""
  posMode: Literal['one_way_mode', 'hedge_mode']
  """Position mode."""
  enterPointSource: str
  """Order source, e.g. `WEB`, `API`, `SYS`, `ANDROID`, `IOS`."""
  tradeSide: Literal[
    'close',
    'open',
    'reduce_close_long',
    'reduce_close_short',
    'burst_close_long',
    'burst_close_short',
    'offset_close_long',
    'offset_close_short',
    'delivery_close_long',
    'delivery_close_short',
    'dte_sys_adl_close_long',
    'dte_sys_adl_close_short',
    'buy_single',
    'sell_single',
    'reduce_buy_single',
    'reduce_sell_single',
    'burst_buy_single',
    'burst_sell_single',
    'delivery_sell_single',
    'delivery_buy_single',
    'dte_sys_adl_buy_in_single_side_mode',
    'dte_sys_adl_sell_in_single_side_mode',
  ]
  """Trade direction. Hedge-mode: `close`/`open`. Netting/liquidation/ADL/delivery variants (hedge-mode): `reduce_close_long`, `reduce_close_short`, `burst_close_long`, `burst_close_short`, `offset_close_long`, `offset_close_short`, `delivery_close_long`, `delivery_close_short`, `dte_sys_adl_close_long`, `dte_sys_adl_close_short`. One-way (single-side) mode: `buy_single`, `sell_single`, `reduce_buy_single`, `reduce_sell_single`, `burst_buy_single`, `burst_sell_single`, `delivery_sell_single`, `delivery_buy_single`, `dte_sys_adl_buy_in_single_side_mode`, `dte_sys_adl_sell_in_single_side_mode`."""
  notionalUsd: str
  """Estimated USD value of the order."""
  orderType: Literal['limit', 'market']
  """Order type."""
  force: str
  """Time-in-force, e.g. `gtc`, `post_only`, `fok`, `ioc`."""
  side: Literal['buy', 'sell']
  """Order direction."""
  posSide: Literal['long', 'short', 'net']
  """Position direction."""
  marginMode: Literal['crossed', 'isolated']
  """Margin mode."""
  marginCoin: str
  """Margin coin."""
  fillPrice: NotRequired[str]
  """Latest fill price."""
  tradeId: NotRequired[str]
  """Latest trade ID."""
  baseVolume: NotRequired[str]
  """Quantity filled by the latest fill."""
  fillTime: NotRequired[TimestampMillis]
  """Latest fill time, Unix epoch milliseconds."""
  fillFee: NotRequired[str]
  """Fee charged on the latest fill, negative."""
  fillFeeCoin: NotRequired[str]
  """Currency of the latest fill fee."""
  tradeScope: NotRequired[Literal['T', 'M']]
  """Liquidity side of the latest fill."""
  accBaseVolume: NotRequired[str]
  """Total filled quantity so far."""
  fillNotionalUsd: NotRequired[str]
  """USD value filled so far."""
  priceAvg: NotRequired[str]
  """Volume-weighted average filled price. `0` if nothing has filled; not pushed once the order is cancelled."""
  status: Literal['live', 'partially_filled', 'filled', 'canceled']
  """Order status."""
  cancelReason: NotRequired[Literal['normal_cancel', 'stp_cancel']]
  """Cancellation reason (non-exhaustive; see the upstream Enumeration page for the full list)."""
  leverage: str
  """Leverage."""
  feeDetail: list[ClassicOrderFeeDetailEntry]
  """Transaction fee line items for this order."""
  pnl: NotRequired[str]
  """Profit."""
  uTime: TimestampMillis
  """Order update time, Unix epoch milliseconds as a string."""
  cTime: TimestampMillis
  """Order creation time, Unix epoch milliseconds as a string."""
  reduceOnly: Literal['yes', 'no']
  """Whether the order is reduce-only."""
  presetStopSurplusPrice: NotRequired[str]
  """Take-profit trigger price, if set."""
  presetStopLossPrice: NotRequired[str]
  """Stop-loss trigger price, if set."""
  presetStopSurplusExecutePrice: NotRequired[str]
  """Take-profit execution price, if set."""
  presetStopLossExecutePrice: NotRequired[str]
  """Stop-loss execution price, if set."""
  stpMode: Literal['none', 'cancel_taker', 'cancel_maker', 'cancel_both']
  """Self-trade-prevention mode."""
  totalProfits: str
  """Total profits."""


class SpotOrder(TypedDict):
  """One spot order lifecycle update (placed, filled, cancelled, or modified)."""

  instId: str
  """Product ID, e.g. `BTCUSDT`."""
  orderId: str
  """Order ID."""
  clientOid: str
  """Customized order ID."""
  size: str
  """Order amount: when `side` is `buy`, the amount of quote coin; when `sell`, the amount of base coin."""
  newSize: str
  """Order quantity, meaning depends on `orderType`/`side`: `limit` -> base coin; `market`+`buy` -> quote coin; `market`+`sell` -> base coin."""
  notional: NotRequired[str]
  """Buy amount, returned only for market-price buys."""
  orderType: Literal['limit', 'market']
  """Order type."""
  force: Literal['gtc', 'post_only', 'fok', 'ioc']
  """Time-in-force. `gtc`: good-till-cancelled. `post_only`: post only. `fok`: fill or kill. `ioc`: immediate or cancel."""
  side: Literal['buy', 'sell']
  """Order direction."""
  fillPrice: NotRequired[str]
  """Latest fill price."""
  tradeId: NotRequired[str]
  """Latest trade ID."""
  baseVolume: NotRequired[str]
  """Quantity filled by the latest fill, in the base coin."""
  fillTime: NotRequired[TimestampMillis]
  """Latest fill time, Unix epoch milliseconds as a string."""
  fillFee: NotRequired[str]
  """Fee charged on the latest fill, negative."""
  fillFeeCoin: NotRequired[str]
  """Currency of the latest fill fee."""
  tradeScope: NotRequired[Literal['T', 'M']]
  """Liquidity side of the latest fill."""
  accBaseVolume: NotRequired[str]
  """Total filled quantity so far."""
  priceAvg: NotRequired[str]
  """Volume-weighted average filled price; `0` if nothing has filled."""
  status: Literal['live', 'partially_filled', 'filled', 'cancelled']
  """Order status."""
  cTime: TimestampMillis
  """Order creation time, Unix epoch milliseconds as a string."""
  uTime: TimestampMillis
  """Order update time, Unix epoch milliseconds as a string."""
  stpMode: Literal['none', 'cancel_taker', 'cancel_maker', 'cancel_both']
  """Self-trade-prevention mode."""
  feeDetail: NotRequired[list[ClassicOrderFeeDetailEntry]]
  """Transaction fee line items for this order."""
  enterPointSource: str
  """Order source, e.g. `WEB`, `API`, `SYS`, `ANDROID`, `IOS`."""


class OrdersPush(TypedDict):
  """Push frame for the `OrdersPush` channel. The core performs no envelope unwrap for streams (see spec/core.md WebSocket): callers receive this whole frame, not one unwrapped `data` element."""

  action: Literal['snapshot', 'update']
  """Push data action: `snapshot` (docs for this channel do not show `action` in the push example table, only inferred from the shared frame shape). Upstream docs and examples for this channel only show `snapshot`; `update` is included because the outer push frame is the same `{action, arg, data, ts}` shape confirmed live for `ticker` (see spec/core.md WebSocket), and most other classic v2 channels document both values for this same field."""
  arg: ClassicOrdersArg
  data: list[SpotOrder | MixOrder]
  """Subscription data."""
  ts: TimestampMillis
  """Message generation time, Unix epoch milliseconds."""


class Orders(StreamEndpoint):
  """Get order lifecycle updates (placed, filled, cancelled, modified), private."""

  def orders(
    self,
    inst_type: Literal['SPOT', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    *,
    inst_id: str | None = None,
    validate: bool | None = None,
  ) -> StreamManager[OrdersPush, Any, Any]:
    """Get order lifecycle updates (placed, filled, cancelled, modified), private.

    Args:
      inst_type: Product line type: `SPOT`, or a futures product type (`USDT-FUTURES`, `COIN-FUTURES`, `USDC-FUTURES`).
      inst_id: Product ID, or `default` for all symbols (required for settled futures contracts).
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/classic/websocket/spot/private/Order-Channel)
    """
    parameters: Parameters = Parameters(instType=inst_type)
    if inst_id is not None:
      parameters['instId'] = inst_id
    return self.subscribe(
      'orders',
      parameters,
      meta={'private': True},
      validate=validate,
      request_type=Parameters,
      response_type=OrdersPush,
    )
