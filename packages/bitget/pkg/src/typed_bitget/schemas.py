# Generated by typed-dev — see CONTRIBUTING.md.
"""Shapes shared by two or more endpoints under the client root, generated from `spec/schemas.json`."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from decimal import Decimal


class ClassicAccountArg(TypedDict):
  """Channel identifying object for the `account` subscription."""

  instType: Literal['SPOT', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product line type: `SPOT`, or a futures product type (`USDT-FUTURES`, `COIN-FUTURES`, `USDC-FUTURES`)."""
  channel: Literal['account']
  """Channel name."""
  coin: Literal['default']
  """Coin filter; only `default` (all coins) is currently supported."""


class ClassicAdlNotificationArg(TypedDict):
  """Channel identifying object for the `adl-noti` subscription."""

  instType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Futures product type: `USDT-FUTURES` (USDT-margined), `COIN-FUTURES` (coin-margined), or `USDC-FUTURES` (USDC-margined)."""
  channel: Literal['adl-noti']
  """Channel name."""
  instId: str
  """Symbol filter, or `default` for all symbols."""


class ClassicAuctionArg(TypedDict):
  """Channel identifying object for the `auction` subscription."""

  instType: Literal['SPOT']
  """Product line type."""
  channel: Literal['auction']
  """Channel name."""
  instId: str
  """Subscribed identifier."""


class ClassicCandleArg(TypedDict):
  """Channel identifying object for a `candle{interval}` subscription."""

  instType: Literal['SPOT', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product line type: `SPOT`, or a futures product type (`USDT-FUTURES`, `COIN-FUTURES`, `USDC-FUTURES`)."""
  channel: str
  """Channel name, `candle{interval}` e.g. `candle1m`, `candle6Hutc` -- the interval is templated into the channel name, not a separate field."""
  instId: str
  """Product ID, e.g. `ETHUSDT`."""


class ClassicEquityArg(TypedDict):
  """Channel identifying object for the `equity` subscription."""

  instType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Futures product type: `USDT-FUTURES` (USDT-margined), `COIN-FUTURES` (coin-margined), or `USDC-FUTURES` (USDC-margined)."""
  channel: Literal['equity']
  """Channel name."""
  instId: str
  """Symbol filter; only `default` is currently supported."""


class ClassicFillArg(TypedDict):
  """Channel identifying object for the `fill` subscription."""

  instType: Literal['SPOT', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product line type: `SPOT`, or a futures product type (`USDT-FUTURES`, `COIN-FUTURES`, `USDC-FUTURES`)."""
  channel: Literal['fill']
  """Channel name."""
  instId: NotRequired[str]
  """Product ID, or `default` for all symbols."""


class ClassicFillFeeDetailEntry(TypedDict):
  """One transaction-fee line item, with deduction detail (fill channel only)."""

  deduction: Literal['yes', 'no']
  """Whether this fee was covered by a deduction (e.g. BGB discount)."""
  totalDeductionFee: str
  """Fee amount covered by deduction."""
  totalFee: str
  """Total fee for this fill."""
  feeCoin: str
  """Currency of the fee."""


class ClassicOrderBookArg(TypedDict):
  """Channel identifying object for a `books{depth}` subscription."""

  instType: Literal['SPOT', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product line type: `SPOT`, or a futures product type (`USDT-FUTURES`, `COIN-FUTURES`, `USDC-FUTURES`)."""
  channel: str
  """Channel name: `books{depth}` -- one of `books`, `books1`, `books5`, `books15`."""
  instId: str
  """Product ID, e.g. `BTCUSDT`."""


class ClassicOrderFeeDetailEntry(TypedDict):
  """One transaction-fee line item."""

  feeCoin: str
  """Currency the transaction fee is charged in."""
  fee: str
  """Transaction fee charged by the platform, as a negative value."""


class ClassicOrdersArg(TypedDict):
  """Channel identifying object for the `orders` subscription."""

  instType: Literal['SPOT', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product line type: `SPOT`, or a futures product type (`USDT-FUTURES`, `COIN-FUTURES`, `USDC-FUTURES`)."""
  channel: Literal['orders']
  """Channel name."""
  instId: NotRequired[str]
  """Product ID, e.g. `BTCUSDT`, or `default` for all symbols. Settled (expired) futures contracts only support `default`."""


class ClassicPlanOrdersArg(TypedDict):
  """Channel identifying object for the `orders-algo` subscription."""

  instType: Literal['SPOT', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product line type: `SPOT`, or a futures product type (`USDT-FUTURES`, `COIN-FUTURES`, `USDC-FUTURES`)."""
  channel: Literal['orders-algo']
  """Channel name."""
  instId: NotRequired[str]
  """Product ID, or `default` for all symbols."""


class ClassicPositionsArg(TypedDict):
  """Channel identifying object for the `positions` subscription."""

  instType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Futures product type: `USDT-FUTURES` (USDT-margined), `COIN-FUTURES` (coin-margined), or `USDC-FUTURES` (USDC-margined)."""
  channel: Literal['positions']
  """Channel name."""
  instId: Literal['default']
  """Symbol filter; only `default` (all symbols) is currently supported."""


class ClassicPositionsHistoryArg(TypedDict):
  """Channel identifying object for the `positions-history` subscription."""

  instType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Futures product type: `USDT-FUTURES` (USDT-margined), `COIN-FUTURES` (coin-margined), or `USDC-FUTURES` (USDC-margined)."""
  channel: Literal['positions-history']
  """Channel name."""
  instId: Literal['default']
  """Symbol filter; only `default` (all symbols) is currently supported."""


class ClassicTickerArg(TypedDict):
  """Channel identifying object for the `ticker` subscription."""

  instType: Literal['SPOT', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product line type: `SPOT`, or a futures product type (`USDT-FUTURES`, `COIN-FUTURES`, `USDC-FUTURES`)."""
  channel: Literal['ticker']
  """Channel name."""
  instId: str
  """Subscribed identifier."""


class ClassicTradeArg(TypedDict):
  """Channel identifying object for the `trade` subscription."""

  instType: Literal['SPOT', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product line type: `SPOT`, or a futures product type (`USDT-FUTURES`, `COIN-FUTURES`, `USDC-FUTURES`)."""
  channel: Literal['trade']
  """Channel name."""
  instId: str
  """Subscribed identifier."""


class DepositAddress(TypedDict):
  """A coin's deposit address. Shared by UTA `Get Deposit Address` and `Get Sub Deposit Address`, which return the identical shape."""

  address: str
  """The deposit address."""
  chain: str
  """Blockchain network."""
  coin: str
  """Coin this address accepts."""
  tag: str | None
  """Address memo/tag, when the chain uses one; `null` otherwise."""
  url: str
  """Block-explorer link for this address."""


class DepositRecord(TypedDict):
  """One deposit record. Shared by UTA `Get Deposit Records` and `Get Sub Deposit Records`, which return the identical shape."""

  orderId: str
  """Deposit order id."""
  recordId: str
  """On-chain transaction hash or internal order id, depending on `dest`."""
  coin: str
  """Coin deposited."""
  type: Literal['deposit']
  """Record type."""
  dest: Literal['on_chain', 'internal_transfer']
  """Whether this deposit arrived on-chain or as an internal transfer from another Bitget user."""
  size: Decimal
  """Deposit amount."""
  status: Literal['pending', 'success', 'fail']
  """Deposit status."""
  fromAddress: str
  """Source address, UID, email, or mobile number, depending on `dest`."""
  toAddress: str
  """Destination address, UID, email, or mobile number, depending on `dest`."""
  chain: str
  """Blockchain network."""
  createdTime: TimestampMillis
  """Creation time, Unix milliseconds as a string."""
  updatedTime: TimestampMillis
  """Last update time, Unix milliseconds as a string."""
  clientOid: str | None
  """Caller-supplied idempotency id, when this deposit's internal-transfer path was initiated with one; `null` otherwise. Confirmed live: an on-chain deposit returns `null`."""


class EliteSubscriptionCoin(TypedDict):
  """One coin an Elite product accepts as a subscription payment currency."""

  subscriptionCoin: str
  """Coin accepted for subscription."""
  precision: str
  """Decimal precision for subscription amounts in this coin."""
  feeRate: Decimal
  """Subscription fee rate charged in this coin."""
  exchangeRate: Decimal
  """Exchange rate from this coin to the product's base coin."""
  remainQuota: NotRequired[Decimal]
  """Remaining subscribable quota in this coin, if limited."""
  minAmount: NotRequired[Decimal]
  """Minimum subscription amount in this coin."""


MixCandle = tuple[TimestampMillis, Decimal, Decimal, Decimal, Decimal, Decimal, Decimal]


class MixFill(TypedDict):
  """One public trade."""

  tradeId: str
  """Trade ID."""
  price: Decimal
  """Trade price."""
  size: Decimal
  """Trade size, in base coin."""
  side: Literal['buy', 'sell']
  """Taker side of the trade."""
  ts: TimestampMillis
  """Trade time."""
  symbol: str
  """Futures symbol."""


MixHoldSide = Literal['long', 'short']


MixKlineInterval = Literal[
  '1m',
  '3m',
  '5m',
  '15m',
  '30m',
  '1H',
  '2H',
  '4H',
  '6H',
  '12H',
  '1D',
  '3D',
  '1W',
  '1M',
  '6Hutc',
  '12Hutc',
  '1Dutc',
  '3Dutc',
  '1Wutc',
  '1Mutc',
]


MixKlineType = Literal['MARKET', 'MARK', 'INDEX']


MixMarginMode = Literal['isolated', 'crossed']


MixOrderSide = Literal['buy', 'sell']


MixOrderType = Literal['limit', 'market']


MixPlanTypeNormalTrack = Literal['normal_plan', 'track_plan']


MixPlanTypeQuery = Literal['normal_plan', 'track_plan', 'profit_loss']


MixPosMode = Literal['one_way_mode', 'hedge_mode']


class MixPosition(TypedDict):
  """One open futures position."""

  marginCoin: str
  """Margin coin."""
  symbol: str
  """Futures symbol."""
  holdSide: Literal['long', 'short']
  """Which side of a hedge-mode position this concerns."""
  openDelegateSize: Decimal
  """Size currently reserved by open orders that would increase this position."""
  marginSize: Decimal
  """Margin allocated to this position."""
  available: Decimal
  """Position size available to close (not reserved by a pending close order)."""
  locked: Decimal
  """Position size reserved by pending close orders."""
  total: Decimal
  """Total position size, in base coin."""
  leverage: Decimal
  """Leverage applied to this position."""
  achievedProfits: Decimal
  """Realized profit already taken on this position."""
  openPriceAvg: Decimal
  """Volume-weighted average entry price."""
  marginMode: Literal['isolated', 'crossed']
  """Margin mode governing how margin is allocated to a position."""
  posMode: Literal['one_way_mode', 'hedge_mode']
  """Position mode: one-way (net position per symbol) or hedge (independent long and short positions per symbol)."""
  unrealizedPL: Decimal
  """Unrealized profit and loss."""
  liquidationPrice: Decimal
  """Estimated liquidation price."""
  keepMarginRate: Decimal
  """Maintenance margin rate applied to this position."""
  markPrice: Decimal
  """Current mark price."""
  breakEvenPrice: Decimal
  """Price at which closing the position would realize zero profit, including fees and funding paid so far."""
  totalFee: Decimal
  """Total fees paid on this position so far."""
  deductedFee: Decimal
  """Portion of fees paid from coupon (bonus) balance."""
  marginRatio: Decimal
  """Current margin ratio."""
  assetMode: str
  """Asset mode: whether margin is shared across symbols ("union") or held per-symbol ("single")."""
  uTime: TimestampMillis
  """Last update time."""
  autoMargin: Literal['on', 'off']
  """Whether automatic margin top-up is enabled for this position."""
  cTime: TimestampMillis
  """Position open time."""


MixProductType = Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']


MixReduceOnly = Literal['YES', 'NO']


MixStpMode = Literal['none', 'cancel_taker', 'cancel_maker', 'cancel_both']


class MixTicker(TypedDict):
  """24h ticker snapshot for one futures symbol."""

  symbol: str
  """Futures symbol."""
  lastPr: Decimal
  """Last traded price."""
  askPr: Decimal
  """Best ask price."""
  bidPr: Decimal
  """Best bid price."""
  bidSz: Decimal
  """Best bid size."""
  askSz: Decimal
  """Best ask size."""
  high24h: Decimal
  """24h high price."""
  low24h: Decimal
  """24h low price."""
  ts: TimestampMillis
  """Snapshot time."""
  change24h: Decimal
  """24h price change ratio."""
  baseVolume: Decimal
  """24h trading volume, in base coin."""
  quoteVolume: Decimal
  """24h trading volume, in quote coin."""
  usdtVolume: Decimal
  """24h trading volume, in USDT."""
  openUtc: Decimal
  """Price at the most recent 00:00 UTC."""
  changeUtc24h: Decimal
  """Price change ratio since the most recent 00:00 UTC."""
  indexPrice: Decimal
  """Index price."""
  fundingRate: Decimal
  """Current funding rate."""
  holdingAmount: Decimal
  """Open interest, in base coin."""
  deliveryStartTime: NotRequired[str | None]
  """Delivery countdown start time, for delivery contracts; confirmed live as `null` (not absent) for perpetuals."""
  deliveryTime: NotRequired[str | None]
  """Delivery settlement time, for delivery contracts; confirmed live as `null` (not absent) for perpetuals."""
  deliveryStatus: str
  """Delivery status, for delivery contracts."""
  open24h: Decimal
  """Price 24h ago."""
  markPrice: Decimal
  """Mark price, used for margin and liquidation calculations."""


MixTimeInForce = Literal['ioc', 'fok', 'gtc', 'post_only']


MixTradeSide = Literal['open', 'close']


MixTriggerPriceType = Literal['fill_price', 'mark_price', 'index_price']


MixTriggerType = Literal['fill_price', 'mark_price']


class MixVipFeeRateTier(TypedDict):
  """One futures VIP fee-rate tier."""

  level: str
  """Tier level."""
  dealAmount: Decimal
  """30-day trading volume threshold for this tier."""
  assetAmount: Decimal
  """Asset balance threshold for this tier."""
  takerFeeRate: Decimal
  """Taker fee rate at this tier."""
  makerFeeRate: Decimal
  """Maker fee rate at this tier."""
  btcWithdrawAmount: Decimal
  """Daily BTC withdrawal allowance at this tier."""
  usdtWithdrawAmount: Decimal
  """Daily USDT withdrawal allowance at this tier."""


class MovePositionOrderResult(TypedDict):
  """One side's order result from a position move."""

  orderId: str
  """Exchange order ID on this side."""
  clientOid: str
  """Client order ID on this side."""
  code: str
  """Error code, if this side's operation failed."""
  msg: str
  """Error message, if this side's operation failed."""


class SavingsApyTier(TypedDict):
  """One APY tier of a ladder-rate Savings product."""

  rateLevel: int
  """Tier ordinal, as a string (e.g. `"1"`)."""
  minStepVal: Decimal
  """Minimum holding amount for this tier."""
  maxStepVal: Decimal
  """Maximum holding amount for this tier."""
  currentApy: Decimal
  """Annual percentage yield for this tier."""


SpotAccountType = Literal[
  'spot',
  'p2p',
  'coin_futures',
  'usdt_futures',
  'usdc_futures',
  'crossed_margin',
  'isolated_margin',
]


SpotBatchMode = Literal['single', 'multiple']


SpotFeeDetail = str


SpotKlineInterval = Literal[
  '1min',
  '5min',
  '15min',
  '30min',
  '1h',
  '4h',
  '6h',
  '12h',
  '1day',
  '3day',
  '1week',
  '1M',
  '6Hutc',
  '12Hutc',
  '1Dutc',
  '3Dutc',
  '1Wutc',
  '1Mutc',
]


SpotOrderForce = Literal['gtc', 'post_only', 'fok', 'ioc']


class SpotOrderIdResult(TypedDict):
  """Identifiers of the affected order."""

  orderId: str
  """Exchange-assigned order ID."""
  clientOid: str
  """Caller-supplied client order ID."""


SpotOrderSide = Literal['buy', 'sell']


SpotOrderType = Literal['limit', 'market']


SpotPlanType = Literal['amount', 'total']


SpotStpMode = Literal['none', 'cancel_taker', 'cancel_maker', 'cancel_both']


SpotTpslType = Literal['normal', 'tpsl']


SpotTriggerType = Literal['fill_price', 'mark_price']


class UtaAccountArg(TypedDict):
  """Channel identifying object echoed on the `account` subscription."""

  instType: Literal['UTA']
  """Fixed unified-account instType. All UTA v3 private channels subscribe/push under `"UTA"`, not a per-category value -- unlike Classic v2, which splits private channels by `SPOT`/`USDT-FUTURES`/etc."""
  topic: Literal['account']
  """Channel name."""


class UtaFillArg(TypedDict):
  """Channel identifying object echoed on the `fill` subscription."""

  instType: Literal['UTA']
  """Fixed unified-account instType. All UTA v3 private channels subscribe/push under `"UTA"`, not a per-category value -- unlike Classic v2, which splits private channels by `SPOT`/`USDT-FUTURES`/etc."""
  topic: Literal['fill']
  """Channel name."""


class UtaKlineArg(TypedDict):
  """Channel identifying object echoed on the `kline` subscription."""

  instType: str
  """Echoed subscription `instType`. Confirmed lowercase (`"spot"`) live for the `spot` category only; other categories' lowercase spelling on the wire is unconfirmed, so this is kept as a bare `str` rather than a guessed closed set (see spec/core.md WebSocket)."""
  topic: Literal['kline']
  """Channel name."""
  symbol: str
  """Subscribed symbol."""
  interval: Literal['1m', '3m', '5m', '15m', '30m', '1H', '4H', '6H', '12H', '1D']
  """Subscribed candle interval."""


class UtaOrderArg(TypedDict):
  """Channel identifying object echoed on the `order` subscription."""

  instType: Literal['UTA']
  """Fixed unified-account instType. All UTA v3 private channels subscribe/push under `"UTA"`, not a per-category value -- unlike Classic v2, which splits private channels by `SPOT`/`USDT-FUTURES`/etc."""
  topic: Literal['order']
  """Channel name."""


class UtaOrderbookArg(TypedDict):
  """Channel identifying object echoed on a `books{depth}` subscription."""

  instType: str
  """Echoed subscription `instType`. Confirmed lowercase (`"spot"`) live for the `spot` category only; other categories' lowercase spelling on the wire is unconfirmed, so this is kept as a bare `str` rather than a guessed closed set (see spec/core.md WebSocket)."""
  topic: Literal['books', 'books1', 'books5', 'books50']
  """Channel name: `books{depth}` -- one of `books`, `books1`, `books5`, `books50`."""
  symbol: str
  """Subscribed symbol."""


class UtaPositionArg(TypedDict):
  """Channel identifying object echoed on the `position` subscription."""

  instType: Literal['UTA']
  """Fixed unified-account instType. All UTA v3 private channels subscribe/push under `"UTA"`, not a per-category value -- unlike Classic v2, which splits private channels by `SPOT`/`USDT-FUTURES`/etc."""
  topic: Literal['position']
  """Channel name."""


class UtaPublicTradeArg(TypedDict):
  """Channel identifying object echoed on the `publicTrade` subscription."""

  instType: str
  """Echoed subscription `instType`. Confirmed lowercase (`"spot"`) live for the `spot` category only; other categories' lowercase spelling on the wire is unconfirmed, so this is kept as a bare `str` rather than a guessed closed set (see spec/core.md WebSocket)."""
  topic: Literal['publicTrade']
  """Channel name."""
  symbol: str
  """Subscribed symbol."""


class UtaStrategyOrderArg(TypedDict):
  """Channel identifying object echoed on the `strategy-order` subscription."""

  instType: Literal['UTA']
  """Fixed unified-account instType. All UTA v3 private channels subscribe/push under `"UTA"`, not a per-category value -- unlike Classic v2, which splits private channels by `SPOT`/`USDT-FUTURES`/etc."""
  topic: Literal['strategy-order']
  """Channel name."""
  symbol: Literal['default']
  """Symbol filter; only `default` (all symbols) is currently documented as supported."""


class UtaTickerArg(TypedDict):
  """Channel identifying object echoed on the `ticker` subscription."""

  instType: str
  """Echoed subscription `instType`. Confirmed lowercase live for `spot` and `usdt-futures` (both echoed verbatim); `coin-futures`/`usdc-futures` wire spelling is unconfirmed, so this is kept as a bare `str` rather than a guessed closed set (see spec/core.md WebSocket)."""
  topic: Literal['ticker']
  """Channel name."""
  symbol: str
  """Subscribed symbol."""
