# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class BrokerRebateInfo(TypedDict):
  """A sub-account's rebate configuration."""

  affiliationType: Literal['affiliate', 'official']
  """Whether the account is under the affiliate or official rebate scheme."""
  userLevel: str
  """Rebate tier/level."""
  clientSpotRebateRatio: str
  """Spot rebate ratio applied to this account, as a fraction."""
  clientFuturesRebateRatio: str
  """Futures rebate ratio applied to this account, as a fraction."""


class Request(TypedDict):
  uid: str
  """Uid of the sub-account."""


class RebateInfo(RpcEndpoint):
  """Get a sub-account's affiliate/official rebate ratios and level."""

  async def rebate_info(
    self, uid: str, *, validate: bool | None = None
  ) -> BrokerRebateInfo:
    """Get a sub-account's affiliate/official rebate ratios and level.

    Args:
      uid: Uid of the sub-account.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-broker-commission/classic-broker-commission#get-rebate-info)
    """
    return await self.request(
      Request(uid=uid),
      method='GET',
      path='/api/v2/broker/rebate-info',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerRebateInfo,
    )
