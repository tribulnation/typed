# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.broker.subaccount_withdrawal` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SubaccountWithdrawalRequest(TypedDict):
  """Broker sub-account withdrawal."""

  subUid: str
  """Uid of the sub-account to withdraw from."""
  coin: str
  """Coin to withdraw."""
  dest: Literal['on_chain', 'internal_transfer']
  """Withdrawal destination type."""
  chain: NotRequired[str]
  """Chain to withdraw on. Required when `dest` is `on_chain` for a multi-chain coin."""
  address: str
  """Destination address (on-chain) or account identifier (internal transfer)."""
  amount: str
  """Amount to withdraw, in `coin`."""
  tag: NotRequired[str]
  """Memo/tag required by some chains."""
  clientOid: NotRequired[str]
  """Caller-supplied idempotency id for this withdrawal."""


class SubaccountWithdrawalResult(TypedDict):
  """A broker sub-account withdrawal order."""

  orderId: str
  """Withdrawal order id."""
  clientOid: str
  """Echoed caller-supplied idempotency id, empty string when not supplied."""


class SubaccountWithdrawal(RpcEndpoint):
  """`classic.broker.subaccount_withdrawal`."""

  async def subaccount_withdrawal(
    self,
    subaccount_withdrawal_request: SubaccountWithdrawalRequest,
    *,
    validate: bool | None = None,
  ) -> SubaccountWithdrawalResult:
    """Withdraw funds from a broker sub-account, on-chain or via internal transfer.

    Args:
      subaccount_withdrawal_request: Broker sub-account withdrawal.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/broker/subaccount/Subaccount-Withdrawal)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/broker/account/subaccount-withdrawal',
      json=subaccount_withdrawal_request,
      validator=validator(SubaccountWithdrawalResult),
      validate=validate,
    )
