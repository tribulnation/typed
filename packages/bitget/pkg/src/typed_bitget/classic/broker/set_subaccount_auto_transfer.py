# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.broker.set_subaccount_auto_transfer` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SetSubaccountAutoTransferRequest(TypedDict):
  """Sub-account auto-transfer configuration."""

  subUid: str
  """Uid of the sub-account."""
  coin: str
  """Coin to auto-transfer. Empty/omitted may mean all coins, per venue convention elsewhere; not confirmed for this endpoint."""
  toAccountType: str
  """Target account type deposits are automatically transferred to."""


class SetSubaccountAutoTransfer(RpcEndpoint):
  """`classic.broker.set_subaccount_auto_transfer`."""

  async def set_subaccount_auto_transfer(
    self,
    set_subaccount_auto_transfer_request: SetSubaccountAutoTransferRequest,
    *,
    validate: bool | None = None,
  ) -> str:
    """Configure automatic transfer of a sub-account's deposits to a target account type.

    Args:
      set_subaccount_auto_transfer_request: Sub-account auto-transfer configuration.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/broker/subaccount/Set-Subaccount-Autotransfer)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/broker/account/set-subaccount-autotransfer',
      json=set_subaccount_auto_transfer_request,
      validator=validator(str),
      validate=validate,
    )
