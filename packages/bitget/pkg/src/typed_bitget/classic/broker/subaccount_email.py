# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.broker.subaccount_email` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SubaccountEmail(TypedDict):
  """A broker sub-account's bound email address."""

  subUid: str
  """Sub-account uid."""
  subaccountName: str
  """Sub-account name."""
  subaccountEmail: str
  """Bound email address."""
  cTime: TimestampMillis
  """Creation time, Unix millisecond timestamp."""
  uTime: TimestampMillis
  """Last update time, Unix millisecond timestamp."""


class SubaccountEmailEndpoint(RpcEndpoint):
  """`classic.broker.subaccount_email`."""

  async def subaccount_email(
    self,
    *,
    sub_uid: str,
    validate: bool | None = None,
  ) -> SubaccountEmail:
    """Get a broker sub-account's bound email address.

    Args:
      sub_uid: Uid of the sub-account.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/broker/subaccount/Get-Subaccount-Email)
    """
    params: dict = {
      'subUid': sub_uid,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/broker/account/subaccount-email',
      params=params,
      validator=validator(SubaccountEmail),
      validate=validate,
    )
