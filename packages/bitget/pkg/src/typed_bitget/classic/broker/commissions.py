# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class BrokerCommission(TypedDict):
  """One sub-account's commission for one symbol."""

  uid: str
  """Sub-account uid."""
  coin: str
  """Fee coin."""
  symbol: str
  """Trading pair."""
  dealtAmount: str
  """Traded amount, in quote coin."""
  totalFee: str
  """Total fee charged, in `coin`."""
  deductedFee: str
  """Fee deducted (e.g. via discount), in `coin`."""
  paidFee: str
  """Fee actually paid, in `coin`."""
  markUpFee: str
  """Broker mark-up fee, in `coin`."""
  totalCommission: str
  """Total commission earned, in `coin`."""


class Request(TypedDict):
  startTime: NotRequired[TimestampMillis]
  """Range start, Unix millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """Range end, Unix millisecond timestamp."""
  pageSize: NotRequired[int]
  """Entries per page."""
  pageNo: NotRequired[int]
  """Page number, 1-indexed."""
  bizType: NotRequired[Literal['spot', 'futures']]
  """Restrict to this business type."""
  subBizType: NotRequired[
    Literal['spot_trade', 'spot_margin', 'usdt_futures', 'usdc_futures', 'coin_futures']
  ]
  """Restrict to this business sub-type."""


Response = list[BrokerCommission]


class Commissions(RpcEndpoint):
  """Get commission details for the broker's sub-accounts, page-paginated."""

  async def commissions_paged(
    self,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    page_size: int | None = None,
    biz_type: Literal['spot', 'futures'] | None = None,
    sub_biz_type: Literal[
      'spot_trade', 'spot_margin', 'usdt_futures', 'usdc_futures', 'coin_futures'
    ]
    | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[Response]:
    """Get commission details for the broker's sub-accounts, page-paginated.

    Paged variant of `commissions`: Requests `pageNo` from 1 upwards and stops on the first page shorter than `page_size`, or after `max_pages` pages when one is given.

    Args:
      start_time: Range start, Unix millisecond timestamp.
      end_time: Range end, Unix millisecond timestamp.
      page_size: Entries per page.
      biz_type: Restrict to this business type.
      sub_biz_type: Restrict to this business sub-type.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-broker-subaccount/classic-broker-subaccount#get-broker-subaccounts-commissions)
    """
    page_no = 1
    pages = 0
    while True:
      response = await self.commissions(
        start_time=start_time,
        end_time=end_time,
        page_size=page_size,
        biz_type=biz_type,
        sub_biz_type=sub_biz_type,
        page_no=page_no,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      if not response or (page_size is not None and len(response) < page_size):
        break
      page_no += 1

  async def commissions(
    self,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    page_size: int | None = None,
    page_no: int | None = None,
    biz_type: Literal['spot', 'futures'] | None = None,
    sub_biz_type: Literal[
      'spot_trade', 'spot_margin', 'usdt_futures', 'usdc_futures', 'coin_futures'
    ]
    | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get commission details for the broker's sub-accounts, page-paginated.

    Args:
      start_time: Range start, Unix millisecond timestamp.
      end_time: Range end, Unix millisecond timestamp.
      page_size: Entries per page.
      page_no: Page number, 1-indexed.
      biz_type: Restrict to this business type.
      sub_biz_type: Restrict to this business sub-type.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-broker-subaccount/classic-broker-subaccount#get-broker-subaccounts-commissions)
    """
    request: Request = Request()
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if page_size is not None:
      request['pageSize'] = page_size
    if page_no is not None:
      request['pageNo'] = page_no
    if biz_type is not None:
      request['bizType'] = biz_type
    if sub_biz_type is not None:
      request['subBizType'] = sub_biz_type
    return await self.request(
      request,
      method='GET',
      path='/api/v2/broker/commissions',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
