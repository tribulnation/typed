# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.broker.commissions` -- generated from `spec/endpoints/`."""

from typing_extensions import AsyncIterator, Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis, timestamp_millis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class BrokerCommission(TypedDict):
  """One sub-account's commission for one symbol."""

  uid: str
  """Sub-account uid."""
  coin: str
  """Fee coin."""
  symbol: str
  """Trading pair."""
  dealtAmount: str
  """Traded amount, in quote coin."""
  totalFee: str
  """Total fee charged, in `coin`."""
  deductedFee: str
  """Fee deducted (e.g. via discount), in `coin`."""
  paidFee: str
  """Fee actually paid, in `coin`."""
  markUpFee: str
  """Broker mark-up fee, in `coin`."""
  totalCommission: str
  """Total commission earned, in `coin`."""


class Commissions(RpcEndpoint):
  """`classic.broker.commissions`."""

  async def commissions(
    self,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    page_size: int | None = None,
    page_no: int | None = None,
    biz_type: Literal['spot', 'futures'] | None = None,
    sub_biz_type: Literal[
      'spot_trade', 'spot_margin', 'usdt_futures', 'usdc_futures', 'coin_futures'
    ]
    | None = None,
    validate: bool | None = None,
  ) -> list[BrokerCommission]:
    """Get commission details for the broker's sub-accounts, page-paginated.

    Args:
      start_time: Range start, Unix millisecond timestamp.
      end_time: Range end, Unix millisecond timestamp.
      page_size: Entries per page.
      page_no: Page number, 1-indexed.
      biz_type: Restrict to this business type.
      sub_biz_type: Restrict to this business sub-type.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/broker/Get-Broker-Subaccounts-Commissions)
    """
    params = {}
    if start_time is not None:
      params['startTime'] = timestamp_millis.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp_millis.dump(end_time)
    if page_size is not None:
      params['pageSize'] = page_size
    if page_no is not None:
      params['pageNo'] = page_no
    if biz_type is not None:
      params['bizType'] = biz_type
    if sub_biz_type is not None:
      params['subBizType'] = sub_biz_type
    return await self.authed_request(
      'GET',
      '/api/v2/broker/commissions',
      params=params,
      validator=validator(list[BrokerCommission]),
      validate=validate,
    )

  async def commissions_paged(
    self,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    page_size: int | None = None,
    biz_type: Literal['spot', 'futures'] | None = None,
    sub_biz_type: Literal[
      'spot_trade', 'spot_margin', 'usdt_futures', 'usdc_futures', 'coin_futures'
    ]
    | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[list[BrokerCommission]]:
    """Yield successive pages of `commissions`.

    Requests `pageNo` from 1 upwards and stops on the first page shorter than
    `page_size`, or after `max_pages` pages when one is given.
    """
    page_no = 1
    pages = 0
    while True:
      response = await self.commissions(
        start_time=start_time,
        end_time=end_time,
        page_size=page_size,
        biz_type=biz_type,
        sub_biz_type=sub_biz_type,
        page_no=page_no,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      if not response or (page_size is not None and len(response) < page_size):
        break
      page_no += 1
