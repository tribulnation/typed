# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Sub-account deposit address request."""

  subUid: str
  """Uid of the sub-account."""
  coin: str
  """Coin to generate a deposit address for."""
  chain: NotRequired[str]
  """Chain to generate the address on. Required for multi-chain coins."""


class SubaccountDepositAddress(TypedDict):
  """A broker sub-account's deposit address."""

  subUid: str
  """Sub-account uid."""
  coin: str
  """The coin."""
  address: str
  """Deposit address."""
  chain: str
  """Chain the address is on."""
  tag: str
  """Memo/tag required by some chains, empty string when not applicable."""
  url: str
  """Block-explorer URL for the address."""
  cTime: TimestampMillis
  """Creation time, Unix millisecond timestamp."""


class CreateDepositAddress(RpcEndpoint):
  """Create/get a deposit address for a broker sub-account."""

  async def create_deposit_address(
    self,
    *,
    sub_uid: str,
    coin: str,
    chain: str | None = None,
    validate: bool | None = None,
  ) -> SubaccountDepositAddress:
    """Create/get a deposit address for a broker sub-account.

    Args:
      sub_uid: Uid of the sub-account.
      coin: Coin to generate a deposit address for.
      chain: Chain to generate the address on. Required for multi-chain coins.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-broker-subaccount/classic-broker-subaccount#create-subaccount-deposit-address)
    """
    request: Request = Request(subUid=sub_uid, coin=coin)
    if chain is not None:
      request['chain'] = chain
    return await self.request(
      request,
      method='POST',
      path='/api/v2/broker/account/subaccount-address',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SubaccountDepositAddress,
    )
