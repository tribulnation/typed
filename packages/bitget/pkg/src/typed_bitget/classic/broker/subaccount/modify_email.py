# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Sub-account email change."""

  subUid: str
  """Uid of the sub-account."""
  subaccountEmail: str
  """New email address to bind to the sub-account."""


Response = str


class ModifyEmail(RpcEndpoint):
  """Change a broker sub-account's bound email address."""

  async def modify_email(
    self,
    *,
    sub_uid: str,
    subaccount_email: str,
    validate: bool | None = None,
  ) -> Response:
    """Change a broker sub-account's bound email address.

    Args:
      sub_uid: Uid of the sub-account.
      subaccount_email: New email address to bind to the sub-account.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-broker-subaccount/classic-broker-subaccount#modify-subaccount-email)
    """
    return await self.request(
      Request(subUid=sub_uid, subaccountEmail=subaccount_email),
      method='POST',
      path='/api/v2/broker/account/modify-subaccount-email',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
