# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Sub-account auto-transfer configuration."""

  subUid: str
  """Uid of the sub-account."""
  coin: str
  """Coin to auto-transfer. Empty/omitted may mean all coins, per venue convention elsewhere; not confirmed for this endpoint."""
  toAccountType: str
  """Target account type deposits are automatically transferred to."""


Response = str


class SetAutoTransfer(RpcEndpoint):
  """Configure automatic transfer of a sub-account's deposits to a target account type."""

  async def set_auto_transfer(
    self,
    *,
    sub_uid: str,
    coin: str,
    to_account_type: str,
    validate: bool | None = None,
  ) -> Response:
    """Configure automatic transfer of a sub-account's deposits to a target account type.

    Args:
      sub_uid: Uid of the sub-account.
      coin: Coin to auto-transfer. Empty/omitted may mean all coins, per venue convention elsewhere; not confirmed for this endpoint.
      to_account_type: Target account type deposits are automatically transferred to.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-broker-subaccount/classic-broker-subaccount#sub-deposit-auto-transfer)
    """
    return await self.request(
      Request(subUid=sub_uid, coin=coin, toAccountType=to_account_type),
      method='POST',
      path='/api/v2/broker/account/set-subaccount-autotransfer',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
