# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  subUid: str
  """Uid of the sub-account."""
  coin: NotRequired[str]
  """Restrict to this coin."""
  assetType: NotRequired[Literal['hold_only', 'all']]
  """Whether to return only coins with a nonzero balance, or all supported coins."""


class SubaccountSpotAsset(TypedDict):
  """One coin's spot balance."""

  coin: str
  """The coin."""
  available: str
  """Available (tradable) balance, in `coin`."""
  frozen: str
  """Frozen balance (e.g. tied up in open orders), in `coin`."""
  locked: str
  """Locked balance (e.g. collateral), in `coin`."""
  uTime: TimestampMillis
  """Last update time, Unix millisecond timestamp."""


class SubaccountSpotAssets(TypedDict):
  """A broker sub-account's spot asset balances."""

  assetsList: list[SubaccountSpotAsset]
  """Per-coin spot balances."""


class SpotAssets(RpcEndpoint):
  """Get a broker sub-account's spot asset balances."""

  async def spot_assets(
    self,
    *,
    sub_uid: str,
    coin: str | None = None,
    asset_type: Literal['hold_only', 'all'] | None = None,
    validate: bool | None = None,
  ) -> SubaccountSpotAssets:
    """Get a broker sub-account's spot asset balances.

    Args:
      sub_uid: Uid of the sub-account.
      coin: Restrict to this coin.
      asset_type: Whether to return only coins with a nonzero balance, or all supported coins.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-broker-subaccount/classic-broker-subaccount#get-subaccount-spot-assets)
    """
    request: Request = Request(subUid=sub_uid)
    if coin is not None:
      request['coin'] = coin
    if asset_type is not None:
      request['assetType'] = asset_type
    return await self.request(
      request,
      method='GET',
      path='/api/v2/broker/account/subaccount-spot-assets',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SubaccountSpotAssets,
    )
