# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Broker sub-account API key to delete."""

  subUid: str
  """Uid of the sub-account the key belongs to."""
  apiKey: str
  """The API key to delete."""


Response = str


class Delete(RpcEndpoint):
  """Delete a broker sub-account API key."""

  async def delete(
    self,
    *,
    sub_uid: str,
    api_key: str,
    validate: bool | None = None,
  ) -> Response:
    """Delete a broker sub-account API key.

    Args:
      sub_uid: Uid of the sub-account the key belongs to.
      api_key: The API key to delete.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-broker-apikey/classic-broker-apikey#delete-subaccount-apikey)
    """
    return await self.request(
      Request(subUid=sub_uid, apiKey=api_key),
      method='POST',
      path='/api/v2/broker/manage/delete-subaccount-apikey',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
