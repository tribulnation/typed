# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CreatedSubaccount(TypedDict):
  """A broker sub-account created successfully."""

  subUid: str
  """Uid assigned to the new sub-account."""
  subaccountName: str
  """The sub-account name."""
  status: str
  """Sub-account status. Closed set not confirmed for this response field; see notes."""
  permList: list[str]
  """Permissions granted to the sub-account."""
  label: str
  """Sub-account label."""
  cTime: TimestampMillis
  """Creation time, Unix millisecond timestamp."""


class Request(TypedDict):
  """Broker sub-account to create."""

  subaccountName: str
  """Name for the new sub-account."""
  label: str
  """Label for the new sub-account."""


class Create(RpcEndpoint):
  """Create a new broker sub-account."""

  async def create(
    self,
    *,
    subaccount_name: str,
    label: str,
    validate: bool | None = None,
  ) -> CreatedSubaccount:
    """Create a new broker sub-account.

    Args:
      subaccount_name: Name for the new sub-account.
      label: Label for the new sub-account.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-broker-subaccount/classic-broker-subaccount#create-subaccount)
    """
    return await self.request(
      Request(subaccountName=subaccount_name, label=label),
      method='POST',
      path='/api/v2/broker/account/create-subaccount',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=CreatedSubaccount,
    )
