# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class ModifiedSubaccount(TypedDict):
  """A modified broker sub-account."""

  subUid: str
  """Sub-account uid."""
  subaccountName: str
  """Sub-account name."""
  status: str
  """Sub-account status. Closed set not confirmed for this response field; see notes."""
  permList: list[str]
  """Permissions granted to the sub-account."""
  label: str
  """Sub-account label."""
  language: str
  """Sub-account locale."""
  cTime: TimestampMillis
  """Creation time, Unix millisecond timestamp."""
  uTime: TimestampMillis
  """Last update time, Unix millisecond timestamp."""


class Request(TypedDict):
  """Broker sub-account permission/status update."""

  subUid: str
  """Uid of the sub-account to modify."""
  permList: list[
    Literal[
      'withdraw',
      'transfer',
      'spot_trade',
      'contract_trade',
      'read',
      'deposit',
      'margin_trade',
    ]
  ]
  """Permissions to grant the sub-account."""
  status: Literal['normal', 'freeze']
  """New sub-account status."""
  language: NotRequired[
    Literal[
      'en_US',
      'zh_CN',
      'ja_JP',
      'vi_VN',
      'zh_TW',
      'ru_RU',
      'es_ES',
      'tr_TR',
      'fr_FR',
      'de_DE',
      'pt_PT',
      'th_TH',
    ]
  ]
  """Sub-account locale for notification messages."""


class Modify(RpcEndpoint):
  """Modify a broker sub-account's permissions, status, and locale."""

  async def modify(
    self,
    sub_uid: str,
    *,
    perm_list: list[
      Literal[
        'withdraw',
        'transfer',
        'spot_trade',
        'contract_trade',
        'read',
        'deposit',
        'margin_trade',
      ]
    ],
    status: Literal['normal', 'freeze'],
    language: Literal[
      'en_US',
      'zh_CN',
      'ja_JP',
      'vi_VN',
      'zh_TW',
      'ru_RU',
      'es_ES',
      'tr_TR',
      'fr_FR',
      'de_DE',
      'pt_PT',
      'th_TH',
    ]
    | None = None,
    validate: bool | None = None,
  ) -> ModifiedSubaccount:
    """Modify a broker sub-account's permissions, status, and locale.

    Args:
      sub_uid: Uid of the sub-account to modify.
      perm_list: Permissions to grant the sub-account.
      status: New sub-account status.
      language: Sub-account locale for notification messages.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-broker-subaccount/classic-broker-subaccount#modify-subaccount)
    """
    request: Request = Request(subUid=sub_uid, permList=perm_list, status=status)
    if language is not None:
      request['language'] = language
    return await self.request(
      request,
      method='POST',
      path='/api/v2/broker/account/modify-subaccount',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=ModifiedSubaccount,
    )
