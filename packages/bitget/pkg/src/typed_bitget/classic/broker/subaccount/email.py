# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  subUid: str
  """Uid of the sub-account."""


class SubaccountEmail(TypedDict):
  """A broker sub-account's bound email address."""

  subUid: str
  """Sub-account uid."""
  subaccountName: str
  """Sub-account name."""
  subaccountEmail: str
  """Bound email address."""
  cTime: TimestampMillis
  """Creation time, Unix millisecond timestamp."""
  uTime: TimestampMillis
  """Last update time, Unix millisecond timestamp."""


class Email(RpcEndpoint):
  """Get a broker sub-account's bound email address."""

  async def email(
    self, sub_uid: str, *, validate: bool | None = None
  ) -> SubaccountEmail:
    """Get a broker sub-account's bound email address.

    Args:
      sub_uid: Uid of the sub-account.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-broker-subaccount/classic-broker-subaccount#get-subaccount-email)
    """
    return await self.request(
      Request(subUid=sub_uid),
      method='GET',
      path='/api/v2/broker/account/subaccount-email',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SubaccountEmail,
    )
