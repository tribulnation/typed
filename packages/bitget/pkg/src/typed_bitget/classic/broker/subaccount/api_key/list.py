# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  subUid: str
  """Uid of the sub-account."""


class SubaccountApiKeyListItem(TypedDict):
  """One broker sub-account API key."""

  subUid: str
  """Uid of the sub-account the key belongs to."""
  label: str
  """Label of the API key."""
  apiKey: str
  """The API key."""
  secretKey: str
  """The API secret key."""
  permType: str
  """Permission template applied to the key."""
  permList: list[str]
  """Permissions granted to the API key."""
  ipList: list[str]
  """IP allowlist bound to the API key."""


Response = list[SubaccountApiKeyListItem]


class List(RpcEndpoint):
  """List a broker sub-account's API keys."""

  async def list(self, sub_uid: str, *, validate: bool | None = None) -> Response:
    """List a broker sub-account's API keys.

    Args:
      sub_uid: Uid of the sub-account.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-broker-apikey/classic-broker-apikey#get-subaccount-apikey)
    """
    return await self.request(
      Request(subUid=sub_uid),
      method='GET',
      path='/api/v2/broker/manage/subaccount-apikey-list',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
