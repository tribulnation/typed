# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  subUid: str
  """Uid of the sub-account."""
  productType: Literal[
    'USDT-FUTURES',
    'COIN-FUTURES',
    'USDC-FUTURES',
    'SUSDT-FUTURES',
    'SCOIN-FUTURES',
    'SUSDC-FUTURES',
  ]
  """Futures product type to report balances for."""


class SubaccountFuturesAsset(TypedDict):
  """One margin coin's futures balance."""

  marginCoin: str
  """The margin coin."""
  available: str
  """Available (tradable) balance, in `marginCoin`."""
  frozen: str
  """Frozen balance, in `marginCoin`."""
  locked: str
  """Locked balance, in `marginCoin`."""
  crossedMaxAvailable: str
  """Maximum available balance under crossed margin mode, in `marginCoin`."""
  isolatedMaxAvailable: str
  """Maximum available balance under isolated margin mode, in `marginCoin`."""
  maxTransferOut: str
  """Maximum amount transferable out, in `marginCoin`."""
  accountEquity: str
  """Total account equity, in `marginCoin`."""
  usdtEquity: str
  """Total account equity, converted to USDT."""
  btcEquity: str
  """Total account equity, converted to BTC."""
  uTime: TimestampMillis
  """Last update time, Unix millisecond timestamp."""


class SubaccountFuturesAssets(TypedDict):
  """A broker sub-account's futures asset balances."""

  assetsList: list[SubaccountFuturesAsset]
  """Per-margin-coin futures balances."""


class FuturesAssets(RpcEndpoint):
  """Get a broker sub-account's futures asset balances for a product type."""

  async def futures_assets(
    self,
    sub_uid: str,
    *,
    product_type: Literal[
      'USDT-FUTURES',
      'COIN-FUTURES',
      'USDC-FUTURES',
      'SUSDT-FUTURES',
      'SCOIN-FUTURES',
      'SUSDC-FUTURES',
    ],
    validate: bool | None = None,
  ) -> SubaccountFuturesAssets:
    """Get a broker sub-account's futures asset balances for a product type.

    Args:
      sub_uid: Uid of the sub-account.
      product_type: Futures product type to report balances for.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-broker-subaccount/classic-broker-subaccount#get-subaccount-future-assets)
    """
    return await self.request(
      Request(subUid=sub_uid, productType=product_type),
      method='GET',
      path='/api/v2/broker/account/subaccount-future-assets',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SubaccountFuturesAssets,
    )
