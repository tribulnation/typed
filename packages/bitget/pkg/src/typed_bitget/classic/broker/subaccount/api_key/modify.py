# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class ModifiedSubaccountApiKey(TypedDict):
  """A modified broker sub-account API key."""

  subUid: str
  """Uid of the sub-account the key belongs to."""
  apiKey: str
  """The API key."""
  label: str
  """Label of the API key."""
  ipList: list[str]
  """IP allowlist bound to the API key."""
  permType: str
  """Permission template applied to the key."""
  permList: list[str]
  """Permissions granted to the API key."""


class Request(TypedDict):
  """Broker sub-account API key update."""

  subUid: str
  """Uid of the sub-account the key belongs to."""
  apiKey: str
  """The API key to modify."""
  label: NotRequired[str]
  """New label for the API key."""
  passphrase: str
  """The API key's passphrase, required to authorize the modification."""
  ipList: NotRequired[list[str]]
  """New IP allowlist for the API key."""
  permType: NotRequired[str]
  """New permission template applied to the key."""
  permList: list[
    Literal[
      'withdraw',
      'transfer',
      'spot_trade',
      'contract_trade',
      'read',
      'deposit',
      'margin_trade',
    ]
  ]
  """New permissions to grant the API key."""


class Modify(RpcEndpoint):
  """Modify a broker sub-account API key's label, IP allowlist, or permissions."""

  async def modify(
    self,
    *,
    sub_uid: str,
    api_key: str,
    label: str | None = None,
    passphrase: str,
    ip_list: list[str] | None = None,
    perm_type: str | None = None,
    perm_list: list[
      Literal[
        'withdraw',
        'transfer',
        'spot_trade',
        'contract_trade',
        'read',
        'deposit',
        'margin_trade',
      ]
    ],
    validate: bool | None = None,
  ) -> ModifiedSubaccountApiKey:
    """Modify a broker sub-account API key's label, IP allowlist, or permissions.

    Args:
      sub_uid: Uid of the sub-account the key belongs to.
      api_key: The API key to modify.
      label: New label for the API key.
      passphrase: The API key's passphrase, required to authorize the modification.
      ip_list: New IP allowlist for the API key.
      perm_type: New permission template applied to the key.
      perm_list: New permissions to grant the API key.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-broker-apikey/classic-broker-apikey#modify-subaccount-apikey)
    """
    request: Request = Request(
      subUid=sub_uid, apiKey=api_key, passphrase=passphrase, permList=perm_list
    )
    if label is not None:
      request['label'] = label
    if ip_list is not None:
      request['ipList'] = ip_list
    if perm_type is not None:
      request['permType'] = perm_type
    return await self.request(
      request,
      method='POST',
      path='/api/v2/broker/manage/modify-subaccount-apikey',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=ModifiedSubaccountApiKey,
    )
