# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Broker sub-account API key to create."""

  subUid: str
  """Uid of the sub-account to create the key for."""
  passphrase: str
  """Passphrase to set on the new API key."""
  label: NotRequired[str]
  """Label for the new API key."""
  ipList: list[str]
  """IP allowlist for the new API key."""
  permType: str
  """Permission template applied to the key."""
  permList: list[
    Literal[
      'withdraw',
      'transfer',
      'spot_trade',
      'contract_trade',
      'read',
      'deposit',
      'margin_trade',
    ]
  ]
  """Permissions to grant the new API key."""


class SubaccountApiKey(TypedDict):
  """A broker sub-account API key created successfully."""

  subUid: str
  """Uid of the sub-account the key belongs to."""
  apiKey: str
  """The new API key."""
  secretKey: str
  """The new API secret key. Shown once, at creation."""
  label: str
  """Label of the new API key."""
  ipList: list[str]
  """IP allowlist bound to the API key."""
  permType: str
  """Permission template applied to the key."""
  permList: list[str]
  """Permissions granted to the API key."""


class Create(RpcEndpoint):
  """Create an API key for a broker sub-account."""

  async def create(
    self,
    *,
    sub_uid: str,
    passphrase: str,
    label: str | None = None,
    ip_list: list[str],
    perm_type: str,
    perm_list: list[
      Literal[
        'withdraw',
        'transfer',
        'spot_trade',
        'contract_trade',
        'read',
        'deposit',
        'margin_trade',
      ]
    ],
    validate: bool | None = None,
  ) -> SubaccountApiKey:
    """Create an API key for a broker sub-account.

    Args:
      sub_uid: Uid of the sub-account to create the key for.
      passphrase: Passphrase to set on the new API key.
      label: Label for the new API key.
      ip_list: IP allowlist for the new API key.
      perm_type: Permission template applied to the key.
      perm_list: Permissions to grant the new API key.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-broker-apikey/classic-broker-apikey#create-subaccount-apikey)
    """
    request: Request = Request(
      subUid=sub_uid,
      passphrase=passphrase,
      ipList=ip_list,
      permType=perm_type,
      permList=perm_list,
    )
    if label is not None:
      request['label'] = label
    return await self.request(
      request,
      method='POST',
      path='/api/v2/broker/manage/create-subaccount-apikey',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SubaccountApiKey,
    )
