# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.broker.create_subaccount_api_key` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CreateSubaccountApiKeyRequest(TypedDict):
  """Broker sub-account API key to create."""

  subUid: str
  """Uid of the sub-account to create the key for."""
  passphrase: str
  """Passphrase to set on the new API key."""
  label: NotRequired[str]
  """Label for the new API key."""
  ipList: list[str]
  """IP allowlist for the new API key."""
  permType: str
  """Permission template applied to the key."""
  permList: list[
    Literal[
      'withdraw',
      'transfer',
      'spot_trade',
      'contract_trade',
      'read',
      'deposit',
      'margin_trade',
    ]
  ]
  """Permissions to grant the new API key."""


class SubaccountApiKey(TypedDict):
  """A broker sub-account API key created successfully."""

  subUid: str
  """Uid of the sub-account the key belongs to."""
  apiKey: str
  """The new API key."""
  secretKey: str
  """The new API secret key. Shown once, at creation."""
  label: str
  """Label of the new API key."""
  ipList: list[str]
  """IP allowlist bound to the API key."""
  permType: str
  """Permission template applied to the key."""
  permList: list[str]
  """Permissions granted to the API key."""


class CreateSubaccountApiKey(RpcEndpoint):
  """`classic.broker.create_subaccount_api_key`."""

  async def create_subaccount_api_key(
    self,
    create_subaccount_api_key_request: CreateSubaccountApiKeyRequest,
    *,
    validate: bool | None = None,
  ) -> SubaccountApiKey:
    """Create an API key for a broker sub-account.

    Args:
      create_subaccount_api_key_request: Broker sub-account API key to create.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/broker/manage/Create-Subaccount-Apikey)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/broker/manage/create-subaccount-apikey',
      json=create_subaccount_api_key_request,
      validator=validator(SubaccountApiKey),
      validate=validate,
    )
