# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .agent import Agent
from .commissions import Commissions
from .info import Info
from .order_commission import OrderCommission
from .rebate_info import RebateInfo
from .subaccount import Subaccount
from .total_commission import TotalCommission
from .trade_volume import TradeVolume


class Broker(
  Commissions, Info, OrderCommission, RebateInfo, TotalCommission, TradeVolume
):
  """Bitget Classic's Broker API: sub-account creation, API key management, deposit/withdrawal on behalf of sub-accounts, and commission/rebate reporting for a broker's own customer base and referred agents.

  References:
    - [Upstream docs](https://www.bitget.com/docs/catalog/classic-broker-subaccount)
  """

  @cached_property
  def agent(self) -> Agent:
    """Bitget Classic's Broker Agent API: customer list, assets, commissions, deposits, trade volume, and KYC status for the sub-customers referred by a broker's agent account.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-affiliate-customer-info)
    """
    return Agent(client=self.client)

  @cached_property
  def subaccount(self) -> Subaccount:
    """Bitget Classic's Broker Sub-Account API: creating and managing a broker's sub-accounts, their email/permissions, deposit addresses and records, withdrawals, auto-transfer, and futures/spot asset balances.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-broker-subaccount)
    """
    return Subaccount(client=self.client)
