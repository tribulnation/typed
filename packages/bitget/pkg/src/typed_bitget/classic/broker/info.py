# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class BrokerInfo(TypedDict):
  """A broker main-account's sub-account capacity."""

  subAccountSize: str
  """Current number of sub-accounts."""
  maxSubAccountSize: str
  """Maximum number of sub-accounts this broker account may create."""
  uTime: TimestampMillis
  """Last update time, Unix millisecond timestamp."""


class Info(RpcEndpoint):
  """Get the broker main-account's sub-account capacity."""

  async def info(self, *, validate: bool | None = None) -> BrokerInfo:
    """Get the broker main-account's sub-account capacity.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-broker-subaccount/classic-broker-subaccount#get-broker-info)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v2/broker/account/info',
      meta={'required': True},
      validate=validate,
      response_type=BrokerInfo,
    )
