# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.earn.sharkfin_subscribe_result` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SharkfinSubscribeStatus(TypedDict):
  """Outcome of a Sharkfin subscription."""

  result: Literal['success', 'fail']
  """Subscription outcome."""
  msg: str
  """Human-readable detail, e.g. a failure reason."""


class SharkfinSubscribeResult(RpcEndpoint):
  """`classic.earn.sharkfin_subscribe_result`."""

  async def sharkfin_subscribe_result(
    self,
    *,
    order_id: str,
    validate: bool | None = None,
  ) -> SharkfinSubscribeStatus:
    """Poll the outcome of a previously submitted Sharkfin subscription order.

    Args:
      order_id: Subscription order identifier.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/sharkfin/Shark-Subscribe-Result)
    """
    params: dict = {
      'orderId': order_id,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/earn/sharkfin/subscribe-result',
      params=params,
      validator=validator(SharkfinSubscribeStatus),
      validate=validate,
    )
