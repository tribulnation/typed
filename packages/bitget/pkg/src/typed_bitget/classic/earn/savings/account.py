# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SavingsAccountSummary(TypedDict):
  """Aggregate Savings account summary."""

  btcAmount: Decimal
  """Current Savings holdings valued in BTC."""
  usdtAmount: Decimal
  """Current Savings holdings valued in USDT."""
  btc24hEarning: Decimal
  """Interest earned in the last 24 hours, in BTC."""
  usdt24hEarning: Decimal
  """Interest earned in the last 24 hours, in USDT."""
  btcTotalEarning: Decimal
  """Cumulative interest earned, in BTC."""
  usdtTotalEarning: Decimal
  """Cumulative interest earned, in USDT."""


class Account(RpcEndpoint):
  """Get the caller's aggregate Savings account summary: current holdings and earnings in BTC and USDT terms."""

  async def account(self, *, validate: bool | None = None) -> SavingsAccountSummary:
    """Get the caller's aggregate Savings account summary: current holdings and earnings in BTC and USDT terms.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/savings/Savings-Account)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v2/earn/savings/account',
      meta={'required': True},
      validate=validate,
      response_type=SavingsAccountSummary,
    )
