# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  productId: str
  """Product identifier."""
  periodType: Literal['flexible', 'fixed']
  """Term structure."""
  orderId: str
  """Order identifier."""


class SavingsSubscribeStatus(TypedDict):
  """Outcome of a Savings subscription."""

  result: Literal['success', 'fail']
  """Subscription outcome."""
  msg: str
  """Human-readable detail, e.g. a failure reason."""


class SubscribeResult(RpcEndpoint):
  """Poll the outcome of a previously submitted Savings subscription order."""

  async def subscribe_result(
    self,
    *,
    product_id: str,
    period_type: Literal['flexible', 'fixed'],
    order_id: str,
    validate: bool | None = None,
  ) -> SavingsSubscribeStatus:
    """Poll the outcome of a previously submitted Savings subscription order.

    Args:
      product_id: Product identifier.
      period_type: Term structure.
      order_id: Order identifier.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/savings/Savings-Subscribe-Result)
    """
    return await self.request(
      Request(productId=product_id, periodType=period_type, orderId=order_id),
      method='GET',
      path='/api/v2/earn/savings/subscribe-result',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SavingsSubscribeStatus,
    )
