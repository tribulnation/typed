# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Savings redemption request."""

  productId: str
  """Product the holding belongs to."""
  orderId: NotRequired[str]
  """Holding order identifier to redeem. The reference SDK marks this optional; likely required to disambiguate which fixed-term holding to redeem when a coin has several, and omittable for flexible products. Unconfirmed against live docs."""
  periodType: Literal['flexible', 'fixed']
  """Term structure of the product."""
  amount: Decimal
  """Amount to redeem."""


class SavingsRedeemResult(TypedDict):
  """Result of a Savings redemption request."""

  orderId: str
  """Order identifier for this redemption; poll redeem-result with it."""
  status: NotRequired[str]
  """Immediate processing status. Not documented as a closed set; poll `classic.earn.savings_redeem_result` for the authoritative outcome."""


class Redeem(RpcEndpoint):
  """Redeem an amount from a Savings holding."""

  async def redeem(
    self,
    *,
    product_id: str,
    order_id: str | None = None,
    period_type: Literal['flexible', 'fixed'],
    amount: Decimal,
    validate: bool | None = None,
  ) -> SavingsRedeemResult:
    """Redeem an amount from a Savings holding.

    Args:
      product_id: Product the holding belongs to.
      order_id: Holding order identifier to redeem. The reference SDK marks this optional; likely required to disambiguate which fixed-term holding to redeem when a coin has several, and omittable for flexible products. Unconfirmed against live docs.
      period_type: Term structure of the product.
      amount: Amount to redeem.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/savings/Savings-Redeem)
    """
    request: Request = Request(
      productId=product_id, periodType=period_type, amount=amount
    )
    if order_id is not None:
      request['orderId'] = order_id
    return await self.request(
      request,
      method='POST',
      path='/api/v2/earn/savings/redeem',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SavingsRedeemResult,
    )
