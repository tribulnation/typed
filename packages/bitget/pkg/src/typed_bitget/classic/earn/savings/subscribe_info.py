# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import SavingsApyTier


class Request(TypedDict):
  productId: str
  """Product identifier."""
  periodType: Literal['flexible', 'fixed']
  """Term structure."""


class SavingsSubscribeInfo(TypedDict):
  """Subscription detail for one Savings product."""

  singleMinAmount: Decimal
  """Minimum amount per subscription."""
  singleMaxAmount: Decimal
  """Maximum amount per subscription."""
  remainingAmount: Decimal
  """Remaining subscribable quota across all users."""
  subscribePrecision: int
  """Decimal precision for the subscription amount."""
  profitPrecision: int
  """Decimal precision for interest amounts."""
  subscribeTime: TimestampMillis
  """Subscription window opening time, ms epoch."""
  interestTime: TimestampMillis
  """Time interest starts accruing, ms epoch."""
  settleTime: TimestampMillis
  """Interest settlement time, ms epoch."""
  expireTime: str
  """Product maturity/expiry time, ms epoch, or empty string for a flexible product with no fixed maturity (confirmed live: no timestamp format is declared, since an empty string isn't a valid epoch-millis value)."""
  redeemTime: str
  """Time redemption becomes available, ms epoch, or empty string for a flexible product with no fixed redemption date (confirmed live: no timestamp format is declared, since an empty string isn't a valid epoch-millis value)."""
  settleMethod: str
  """Interest settlement timing (`daily`/`maturity`). Empty for flexible products, confirmed live."""
  apyList: list[SavingsApyTier]
  """APY tiers."""
  redeemDelay: str
  """Delay, in days, between requesting redemption and receiving funds."""


class SubscribeInfo(RpcEndpoint):
  """Get subscription limits and timing for a Savings product before subscribing."""

  async def subscribe_info(
    self,
    product_id: str,
    *,
    period_type: Literal['flexible', 'fixed'],
    validate: bool | None = None,
  ) -> SavingsSubscribeInfo:
    """Get subscription limits and timing for a Savings product before subscribing.

    Args:
      product_id: Product identifier.
      period_type: Term structure.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/savings/Savings-Subscribe-Info)
    """
    return await self.request(
      Request(productId=product_id, periodType=period_type),
      method='GET',
      path='/api/v2/earn/savings/subscribe-info',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SavingsSubscribeInfo,
    )
