# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  orderId: str
  """Order identifier."""
  periodType: Literal['flexible', 'fixed']
  """Term structure."""


class SavingsRedeemStatus(TypedDict):
  """Outcome of a Savings redemption."""

  result: Literal['success', 'fail']
  """Redemption outcome."""
  msg: str
  """Human-readable detail, e.g. a failure reason."""


class RedeemResult(RpcEndpoint):
  """Poll the outcome of a previously submitted Savings redemption order."""

  async def redeem_result(
    self,
    order_id: str,
    *,
    period_type: Literal['flexible', 'fixed'],
    validate: bool | None = None,
  ) -> SavingsRedeemStatus:
    """Poll the outcome of a previously submitted Savings redemption order.

    Args:
      order_id: Order identifier.
      period_type: Term structure.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/savings/Savings-Redeem-Result)
    """
    return await self.request(
      Request(orderId=order_id, periodType=period_type),
      method='GET',
      path='/api/v2/earn/savings/redeem-result',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SavingsRedeemStatus,
    )
