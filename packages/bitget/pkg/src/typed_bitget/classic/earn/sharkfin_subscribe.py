# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.earn.sharkfin_subscribe` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SharkfinSubscribeRequest(TypedDict):
  """Sharkfin subscription request."""

  productId: str
  """Product to subscribe to."""
  amount: str
  """Amount to subscribe."""


class SharkfinSubscribeResult(TypedDict):
  """Result of a Sharkfin subscription request."""

  orderId: str
  """Order identifier for this subscription; poll subscribe-result with it."""
  status: str
  """Immediate processing status. Not documented as a closed set; poll `classic.earn.sharkfin_subscribe_result` for the authoritative outcome."""


class SharkfinSubscribe(RpcEndpoint):
  """`classic.earn.sharkfin_subscribe`."""

  async def sharkfin_subscribe(
    self,
    sharkfin_subscribe_request: SharkfinSubscribeRequest,
    *,
    validate: bool | None = None,
  ) -> SharkfinSubscribeResult:
    """Subscribe an amount to a Sharkfin product.

    Args:
      sharkfin_subscribe_request: Subscribe an amount to a Sharkfin product.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/sharkfin/Shark-Subscribe)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/earn/sharkfin/subscribe',
      json=sharkfin_subscribe_request,
      validator=validator(SharkfinSubscribeResult),
      validate=validate,
    )
