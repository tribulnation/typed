# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  productId: str
  """Product identifier."""


class SharkfinSubscribeInfo(TypedDict):
  """Subscription detail for one Sharkfin product."""

  productCoin: str
  """Coin the product pays out in."""
  subscribeCoin: str
  """Coin subscribed with."""
  interestTime: TimestampMillis
  """Time interest starts accruing, ms epoch."""
  expirationTime: TimestampMillis
  """Product maturity/expiry time, ms epoch."""
  minPrice: Decimal
  """Lower bound of the price range."""
  currentPrice: Decimal
  """Reference price at preview time."""
  maxPrice: Decimal
  """Upper bound of the price range."""
  minRate: Decimal
  """Minimum rate paid."""
  defaultRate: Decimal
  """Rate paid under typical conditions."""
  maxRate: Decimal
  """Maximum rate paid."""
  period: str
  """Product duration, in days."""
  productMinAmount: Decimal
  """Minimum subscription amount."""
  availableBalance: Decimal
  """Caller's available balance for the subscription coin."""
  userAmount: Decimal
  """Caller's amount already subscribed to this product."""
  remainingAmount: Decimal
  """Remaining subscribable quota across all users."""
  profitPrecision: int
  """Decimal precision for interest amounts."""
  subscribePrecision: int
  """Decimal precision for the subscription amount."""


class SubscribeInfo(RpcEndpoint):
  """Get subscription limits, price range, and timing for a Sharkfin product before subscribing."""

  async def subscribe_info(
    self,
    product_id: str,
    *,
    validate: bool | None = None,
  ) -> SharkfinSubscribeInfo:
    """Get subscription limits, price range, and timing for a Sharkfin product before subscribing.

    Args:
      product_id: Product identifier.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/sharkfin/Shark-Subscribe-Info)
    """
    return await self.request(
      Request(productId=product_id),
      method='GET',
      path='/api/v2/earn/sharkfin/subscribe-info',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SharkfinSubscribeInfo,
    )
