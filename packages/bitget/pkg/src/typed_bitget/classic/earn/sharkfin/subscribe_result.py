# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  orderId: str
  """Order identifier."""


class SharkfinSubscribeStatus(TypedDict):
  """Outcome of a Sharkfin subscription."""

  result: Literal['success', 'fail']
  """Subscription outcome."""
  msg: str
  """Human-readable detail, e.g. a failure reason."""


class SubscribeResult(RpcEndpoint):
  """Poll the outcome of a previously submitted Sharkfin subscription order."""

  async def subscribe_result(
    self,
    order_id: str,
    *,
    validate: bool | None = None,
  ) -> SharkfinSubscribeStatus:
    """Poll the outcome of a previously submitted Sharkfin subscription order.

    Args:
      order_id: Order identifier.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/sharkfin/Shark-Subscribe-Result)
    """
    return await self.request(
      Request(orderId=order_id),
      method='GET',
      path='/api/v2/earn/sharkfin/subscribe-result',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SharkfinSubscribeStatus,
    )
