# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class LoanCurrencyLoanInfo(TypedDict):
  """Borrow-rate info for one loanable coin."""

  coin: str
  """Loanable coin."""
  hourRate7D: Decimal
  """Hourly interest rate for the 7-day tier."""
  rate7D: Decimal
  """Daily interest rate for the 7-day tier."""
  hourRate30D: Decimal
  """Hourly interest rate for the 30-day tier."""
  rate30D: Decimal
  """Daily interest rate for the 30-day tier."""
  minUsdt: Decimal
  """Minimum borrowable amount, USDT-valued."""
  maxUsdt: Decimal
  """Maximum borrowable amount, USDT-valued."""
  min: Decimal
  """Minimum borrowable amount, in `coin`."""
  max: Decimal
  """Maximum borrowable amount, in `coin`."""


class LoanCurrencyPledgeInfo(TypedDict):
  """Pledge-rate info for one collateral coin."""

  coin: str
  """Collateral coin."""
  initRate: Decimal
  """Initial pledge rate required to open a loan."""
  supRate: Decimal
  """Supplementary/warning pledge rate."""
  forceRate: Decimal
  """Forced-liquidation pledge rate."""
  minUsdt: Decimal
  """Minimum pledgeable amount, USDT-valued."""
  maxUsdt: Decimal
  """Maximum pledgeable amount, USDT-valued."""


class Request(TypedDict):
  coin: NotRequired[str]
  """Coin symbol."""


class LoanCurrencies(TypedDict):
  """Public Loan currency info."""

  loanInfos: list[LoanCurrencyLoanInfo]
  """Borrow-rate info, by loanable coin."""
  pledgeInfos: list[LoanCurrencyPledgeInfo]
  """Pledge-rate info, by collateral coin."""


class Currencies(RpcEndpoint):
  """List public borrow-rate and pledge-rate info for Loan currencies, public."""

  async def currencies(
    self,
    coin: str | None = None,
    *,
    validate: bool | None = None,
  ) -> LoanCurrencies:
    """List public borrow-rate and pledge-rate info for Loan currencies, public.

    Args:
      coin: Coin symbol.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/loan/Get-Coin-Infos)
    """
    request: Request = Request()
    if coin is not None:
      request['coin'] = coin
    return await self.request(
      request,
      method='GET',
      path='/api/v2/earn/loan/public/coinInfos',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=LoanCurrencies,
    )
