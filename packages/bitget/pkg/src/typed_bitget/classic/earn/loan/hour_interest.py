# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class LoanEstimate(TypedDict):
  """Estimated interest rate and borrowable amount."""

  hourInterest: Decimal
  """Estimated hourly interest rate."""
  loanAmount: Decimal
  """Estimated maximum borrowable amount given the pledge."""


class Request(TypedDict):
  loanCoin: str
  """Loan coin."""
  pledgeCoin: str
  """Pledge coin."""
  daily: Literal['SEVEN', 'THIRTY']
  """Term tier."""
  pledgeAmount: Decimal
  """Pledge amount."""


class HourInterest(RpcEndpoint):
  """Estimate the hourly interest rate and maximum borrowable amount for a loan/collateral coin pair, public."""

  async def hour_interest(
    self,
    *,
    loan_coin: str,
    pledge_coin: str,
    daily: Literal['SEVEN', 'THIRTY'],
    pledge_amount: Decimal,
    validate: bool | None = None,
  ) -> LoanEstimate:
    """Estimate the hourly interest rate and maximum borrowable amount for a loan/collateral coin pair, public.

    Args:
      loan_coin: Loan coin.
      pledge_coin: Pledge coin.
      daily: Term tier.
      pledge_amount: Pledge amount.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/loan/Get-Est-Interest-And-Load-Amount)
    """
    return await self.request(
      Request(
        loanCoin=loan_coin,
        pledgeCoin=pledge_coin,
        daily=daily,
        pledgeAmount=pledge_amount,
      ),
      method='GET',
      path='/api/v2/earn/loan/public/hour-interest',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=LoanEstimate,
    )
