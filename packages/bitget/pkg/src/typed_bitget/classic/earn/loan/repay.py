# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.earn.loan.repay` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class LoanRepayRequest(TypedDict):
  """Loan repayment request."""

  orderId: str
  """Loan order to repay."""
  amount: NotRequired[str]
  """Amount to repay. Ignored when `repayAll` requests full repayment."""
  repayUnlock: NotRequired[str]
  """Reference SDK types this as a plain (undocumented) string; likely a `yes`/`no`-style flag controlling whether unlocked collateral is released immediately, by analogy with `repayAll`. Left bare pending confirmation."""
  repayAll: str
  """Whether to repay the full outstanding balance. The reference SDK types this as a plain (undocumented) string rather than a boolean or closed literal; likely `yes`/`no` by analogy with other Bitget yes/no string flags (e.g. Savings' `advanceRedeem`), but left bare rather than guessing."""


class LoanRepayResult(TypedDict):
  """Result of a repayment."""

  loanCoin: str
  """Coin that was borrowed."""
  pledgeCoin: str
  """Coin that was pledged."""
  repayAmount: str
  """Total amount repaid."""
  payInterest: str
  """Portion of the repayment applied to interest."""
  repayLoanAmount: str
  """Portion of the repayment applied to principal."""
  repayUnlockAmount: str
  """Collateral amount unlocked by this repayment."""


class Repay(RpcEndpoint):
  """`classic.earn.loan.repay`."""

  async def repay(
    self,
    loan_repay_request: LoanRepayRequest,
    *,
    validate: bool | None = None,
  ) -> LoanRepayResult:
    """Repay an amount, or the full outstanding balance, of an open loan order.

    Args:
      loan_repay_request: Repay an amount, or the full outstanding balance, of an open loan order.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/loan/Repay)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/earn/loan/repay',
      json=loan_repay_request,
      validator=validator(LoanRepayResult),
      validate=validate,
    )
