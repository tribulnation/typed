# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class LoanBorrowResult(TypedDict):
  """Result of a borrow request."""

  orderId: str
  """Identifier of the new loan order."""


class Request(TypedDict):
  """Loan borrow request."""

  loanCoin: str
  """Coin to borrow."""
  pledgeCoin: str
  """Coin to pledge as collateral."""
  daily: Literal['SEVEN', 'THIRTY']
  """Loan term tier."""
  pledgeAmount: NotRequired[str]
  """Collateral amount to pledge. The reference SDK marks both `pledgeAmount` and `loanAmount` optional; documented Loan UX lets a caller size by either the collateral or the borrow amount, so exactly one of the two is expected -- unconfirmed against live docs."""
  loanAmount: NotRequired[str]
  """Amount to borrow. See `pledgeAmount` for the expected either/or relationship."""


class Borrow(RpcEndpoint):
  """Open a new crypto-collateralized loan."""

  async def borrow(
    self,
    *,
    loan_coin: str,
    pledge_coin: str,
    daily: Literal['SEVEN', 'THIRTY'],
    pledge_amount: str | None = None,
    loan_amount: str | None = None,
    validate: bool | None = None,
  ) -> LoanBorrowResult:
    """Open a new crypto-collateralized loan.

    Args:
      loan_coin: Coin to borrow.
      pledge_coin: Coin to pledge as collateral.
      daily: Loan term tier.
      pledge_amount: Collateral amount to pledge. The reference SDK marks both `pledgeAmount` and `loanAmount` optional; documented Loan UX lets a caller size by either the collateral or the borrow amount, so exactly one of the two is expected -- unconfirmed against live docs.
      loan_amount: Amount to borrow. See `pledgeAmount` for the expected either/or relationship.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/loan/Borrow)
    """
    request: Request = Request(loanCoin=loan_coin, pledgeCoin=pledge_coin, daily=daily)
    if pledge_amount is not None:
      request['pledgeAmount'] = pledge_amount
    if loan_amount is not None:
      request['loanAmount'] = loan_amount
    return await self.request(
      request,
      method='POST',
      path='/api/v2/earn/loan/borrow',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=LoanBorrowResult,
    )
