# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class LoanOrder(TypedDict):
  """One ongoing loan order."""

  orderId: str
  """Loan order identifier."""
  loanCoin: str
  """Coin borrowed."""
  loanAmount: Decimal
  """Amount borrowed."""
  interestAmount: Decimal
  """Interest accrued so far."""
  hourInterestRate: Decimal
  """Hourly interest rate applied."""
  pledgeCoin: str
  """Coin pledged as collateral."""
  pledgeAmount: Decimal
  """Amount pledged."""
  pledgeRate: Decimal
  """Current pledge (collateralization) rate."""
  supRate: Decimal
  """Supplementary/warning pledge rate for this order."""
  forceRate: Decimal
  """Forced-liquidation pledge rate for this order."""
  borrowTime: TimestampMillis
  """Time the loan was opened, ms epoch."""
  expireTime: TimestampMillis
  """Loan term expiry time, ms epoch."""


class Request(TypedDict):
  orderId: NotRequired[str]
  """Loan order identifier."""
  loanCoin: NotRequired[str]
  """Loan coin."""
  pledgeCoin: NotRequired[str]
  """Pledge coin."""


Response = list[LoanOrder]


class OngoingOrders(RpcEndpoint):
  """List the caller's currently open (unrepaid) loan orders, optionally filtered."""

  async def ongoing_orders(
    self,
    *,
    order_id: str | None = None,
    loan_coin: str | None = None,
    pledge_coin: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """List the caller's currently open (unrepaid) loan orders, optionally filtered.

    Args:
      order_id: Loan order identifier.
      loan_coin: Loan coin.
      pledge_coin: Pledge coin.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/loan/Get-Ongoing-Orders)
    """
    request: Request = Request()
    if order_id is not None:
      request['orderId'] = order_id
    if loan_coin is not None:
      request['loanCoin'] = loan_coin
    if pledge_coin is not None:
      request['pledgeCoin'] = pledge_coin
    return await self.request(
      request,
      method='GET',
      path='/api/v2/earn/loan/ongoing-orders',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
