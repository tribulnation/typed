# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class LoanRevisePledgeResult(TypedDict):
  """Result of a pledge-rate revision."""

  loanCoin: str
  """Coin that was borrowed."""
  pledgeCoin: str
  """Coin pledged as collateral."""
  afterPledgeRate: Decimal
  """Pledge rate after this revision."""


class Request(TypedDict):
  """Pledge-rate revision request."""

  orderId: str
  """Loan order to revise."""
  amount: Decimal
  """Collateral amount to add or remove, per `reviseType`."""
  pledgeCoin: str
  """Collateral coin to revise."""
  reviseType: Literal['IN', 'OUT']
  """Direction of the revision: `IN` adds collateral, `OUT` removes it."""


class RevisePledge(RpcEndpoint):
  """Adjust an open loan order's collateral amount, changing its pledge rate."""

  async def revise_pledge(
    self,
    *,
    order_id: str,
    amount: Decimal,
    pledge_coin: str,
    revise_type: Literal['IN', 'OUT'],
    validate: bool | None = None,
  ) -> LoanRevisePledgeResult:
    """Adjust an open loan order's collateral amount, changing its pledge rate.

    Args:
      order_id: Loan order to revise.
      amount: Collateral amount to add or remove, per `reviseType`.
      pledge_coin: Collateral coin to revise.
      revise_type: Direction of the revision: `IN` adds collateral, `OUT` removes it.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/loan/Revise-Pledge-Rate)
    """
    return await self.request(
      Request(
        orderId=order_id, amount=amount, pledgeCoin=pledge_coin, reviseType=revise_type
      ),
      method='POST',
      path='/api/v2/earn/loan/revise-pledge',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=LoanRevisePledgeResult,
    )
