# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.earn.loan.revise_pledge` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class LoanRevisePledgeRequest(TypedDict):
  """Pledge-rate revision request."""

  orderId: str
  """Loan order to revise."""
  amount: str
  """Collateral amount to add or remove, per `reviseType`."""
  pledgeCoin: str
  """Collateral coin to revise."""
  reviseType: str
  """Direction of the revision. The reference SDK types this as a plain (undocumented) string; likely an up/down or add/remove pair by analogy with typical margin-rate adjustment APIs, but left bare rather than guessing."""


class LoanRevisePledgeResult(TypedDict):
  """Result of a pledge-rate revision."""

  loanCoin: str
  """Coin that was borrowed."""
  pledgeCoin: str
  """Coin pledged as collateral."""
  afterPledgeRate: str
  """Pledge rate after this revision."""


class RevisePledge(RpcEndpoint):
  """`classic.earn.loan.revise_pledge`."""

  async def revise_pledge(
    self,
    loan_revise_pledge_request: LoanRevisePledgeRequest,
    *,
    validate: bool | None = None,
  ) -> LoanRevisePledgeResult:
    """Adjust an open loan order's collateral amount, changing its pledge rate.

    Args:
      loan_revise_pledge_request: Adjust an open loan order's collateral amount, changing its pledge rate.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/loan/Revise-Pledge-Rate)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/earn/loan/revise-pledge',
      json=loan_revise_pledge_request,
      validator=validator(LoanRevisePledgeResult),
      validate=validate,
    )
