# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.earn.loan.liquidation_records` -- generated from `spec/endpoints/`."""

from typing_extensions import AsyncIterator
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis, timestamp_millis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class LoanLiquidationRecord(TypedDict):
  """One loan liquidation record."""

  orderId: str
  """Loan order identifier."""
  loanCoin: str
  """Coin that was borrowed."""
  pledgeCoin: str
  """Coin that was pledged."""
  reduceTime: TimestampMillis
  """Time the liquidation occurred, ms epoch."""
  pledgeRate: str
  """Pledge rate at the time of liquidation."""
  pledgePrice: str
  """Collateral price used for the liquidation."""
  status: str
  """Liquidation status, same set as the request filter."""
  pledgeAmount: str
  """Collateral amount liquidated."""
  reduceFee: str
  """Fee charged for the liquidation."""
  residueAmount: str
  """Collateral amount remaining after liquidation."""
  runlockAmount: str
  """Collateral amount unlocked by the liquidation."""
  repayLoanAmount: str
  """Loan principal repaid by the liquidation proceeds."""


class LiquidationRecords(RpcEndpoint):
  """`classic.earn.loan.liquidation_records`."""

  async def liquidation_records(
    self,
    *,
    order_id: str | None = None,
    loan_coin: str | None = None,
    pledge_coin: str | None = None,
    status: str | None = None,
    start_time: TimestampMillis,
    end_time: TimestampMillis,
    page_no: int | None = None,
    page_size: int | None = None,
    validate: bool | None = None,
  ) -> list[LoanLiquidationRecord]:
    """List the caller's loan liquidation (forced collateral reduction) records, page-paginated.

    Args:
      order_id: Restrict to one loan order.
      loan_coin: Restrict to one borrowed coin.
      pledge_coin: Restrict to one collateral coin.
      status: Restrict to one liquidation status. The venue does not document this set; left bare.
      start_time: Window start, ms epoch.
      end_time: Window end, ms epoch.
      page_no: Page number.
      page_size: Rows per page.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/loan/Get-Burst-List)
    """
    params: dict = {
      'startTime': timestamp_millis.dump(start_time),
      'endTime': timestamp_millis.dump(end_time),
    }
    if order_id is not None:
      params['orderId'] = order_id
    if loan_coin is not None:
      params['loanCoin'] = loan_coin
    if pledge_coin is not None:
      params['pledgeCoin'] = pledge_coin
    if status is not None:
      params['status'] = status
    if page_no is not None:
      params['pageNo'] = page_no
    if page_size is not None:
      params['pageSize'] = page_size
    return await self.authed_request(
      'GET',
      '/api/v2/earn/loan/reduces',
      params=params,
      validator=validator(list[LoanLiquidationRecord]),
      validate=validate,
    )

  async def liquidation_records_paged(
    self,
    *,
    order_id: str | None = None,
    loan_coin: str | None = None,
    pledge_coin: str | None = None,
    status: str | None = None,
    start_time: TimestampMillis,
    end_time: TimestampMillis,
    page_size: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[list[LoanLiquidationRecord]]:
    """Yield successive pages of `liquidation_records`.

    Requests `pageNo` from 1 upwards and stops on the first page shorter than
    `page_size`, or after `max_pages` pages when one is given.
    """
    page_no = 1
    pages = 0
    while True:
      response = await self.liquidation_records(
        order_id=order_id,
        loan_coin=loan_coin,
        pledge_coin=pledge_coin,
        status=status,
        start_time=start_time,
        end_time=end_time,
        page_size=page_size,
        page_no=page_no,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      if not response or (page_size is not None and len(response) < page_size):
        break
      page_no += 1
