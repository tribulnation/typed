# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.earn.elite_subscribe` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class EliteSubscribeRequest(TypedDict):
  """Elite subscription request."""

  productSubId: str
  """Sub-product to subscribe to, from subscribe-info."""
  amount: str
  """Amount to subscribe."""
  coin: NotRequired[str]
  """Coin to pay with, when the product accepts more than one."""
  paymentAccount: NotRequired[Literal['spot', 'unified']]
  """Account to draw the subscription payment from."""


class EliteSubscribeResult(TypedDict):
  """Result of an Elite subscription request."""

  orderId: str
  """Order identifier for this subscription; poll subscribe-result with it."""


class EliteSubscribe(RpcEndpoint):
  """`classic.earn.elite_subscribe`."""

  async def elite_subscribe(
    self,
    elite_subscribe_request: EliteSubscribeRequest,
    *,
    validate: bool | None = None,
  ) -> EliteSubscribeResult:
    """Subscribe an amount to an Elite product sub-offering.

    Args:
      elite_subscribe_request: Subscribe an amount to an Elite product sub-offering.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/elite/Elite-Subscribe)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/earn/elite/subscribe',
      json=elite_subscribe_request,
      validator=validator(EliteSubscribeResult),
      validate=validate,
    )
