# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class EarnAccountAsset(TypedDict):
  """Aggregate Earn holding for one coin."""

  coin: str
  """Coin held."""
  amount: Decimal
  """Total amount of this coin held across Earn products."""


class Request(TypedDict):
  coin: NotRequired[str]
  """Coin symbol."""


Response = list[EarnAccountAsset]


class AccountAssets(RpcEndpoint):
  """Get the caller's aggregate holdings across all Earn products (Savings, Elite, Sharkfin, and Loan collateral), optionally restricted to one coin."""

  async def account_assets(
    self,
    coin: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Get the caller's aggregate holdings across all Earn products (Savings, Elite, Sharkfin, and Loan collateral), optionally restricted to one coin.

    Args:
      coin: Coin symbol.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/account/Earn-Assets)
    """
    request: Request = Request()
    if coin is not None:
      request['coin'] = coin
    return await self.request(
      request,
      method='GET',
      path='/api/v2/earn/account/assets',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
