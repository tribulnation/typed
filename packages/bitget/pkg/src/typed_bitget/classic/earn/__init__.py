# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .account_assets import AccountAssets
from .elite import Elite
from .loan import Loan
from .savings import Savings
from .sharkfin import Sharkfin


class Earn(AccountAssets):
  """Bitget Classic's Earn API: savings, sharkfin structured products, and BGB Elite lending/staking products, plus the shared Earn account balance endpoints.

  References:
    - [Upstream docs](https://www.bitget.com/docs/catalog/earn-classic-elite)
  """

  @cached_property
  def elite(self) -> Elite:
    """Bitget Classic's On-chain Elite API: subscribing to, redeeming, and querying Elite lending/staking products and their subscription records.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/earn-classic-elite)
    """
    return Elite(client=self.client)

  @cached_property
  def loan(self) -> Loan:
    """Bitget Classic's Crypto Loan API: borrowing against pledged collateral, repayment, pledge-rate adjustment, and debt/interest/liquidation history. This product's own doc pages (under the retired `/api-doc/earn/loan/*` path) now redirect to a generic landing page, so this reuses the grouping's own established Elite-page link rather than inventing one.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/earn-classic-elite)
    """
    return Loan(client=self.client)

  @cached_property
  def savings(self) -> Savings:
    """Bitget Classic's Savings API: subscribing to, redeeming, and querying flexible/fixed savings products and their subscription records. This product's own doc pages (under the retired `/api-doc/earn/savings/*` path) now redirect to a generic landing page, so this reuses the grouping's own established Elite-page link rather than inventing one.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/earn-classic-elite)
    """
    return Savings(client=self.client)

  @cached_property
  def sharkfin(self) -> Sharkfin:
    """Bitget Classic's Sharkfin API: subscribing to, redeeming, and querying Sharkfin structured products and their subscription records. This product's own doc pages (under the retired `/api-doc/earn/sharkfin/*` path) now redirect to a generic landing page, so this reuses the grouping's own established Elite-page link rather than inventing one.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/earn-classic-elite)
    """
    return Sharkfin(client=self.client)
