# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.earn.savings_subscribe_result` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SavingsSubscribeStatus(TypedDict):
  """Outcome of a Savings subscription."""

  result: Literal['success', 'fail']
  """Subscription outcome."""
  msg: str
  """Human-readable detail, e.g. a failure reason."""


class SavingsSubscribeResult(RpcEndpoint):
  """`classic.earn.savings_subscribe_result`."""

  async def savings_subscribe_result(
    self,
    *,
    product_id: str,
    period_type: Literal['flexible', 'fixed'],
    validate: bool | None = None,
  ) -> SavingsSubscribeStatus:
    """Poll the outcome of a previously submitted Savings subscription order.

    Args:
      product_id: Product the subscription order was for.
      period_type: Term structure of the product.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/savings/Savings-Subscribe-Result)
    """
    params: dict = {
      'productId': product_id,
      'periodType': period_type,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/earn/savings/subscribe-result',
      params=params,
      validator=validator(SavingsSubscribeStatus),
      validate=validate,
    )
