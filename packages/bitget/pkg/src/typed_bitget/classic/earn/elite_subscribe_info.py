# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.earn.elite_subscribe_info` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.types import EliteSubscriptionCoin
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class EliteSubscribeInfo(TypedDict):
  """Subscription detail for one Elite product."""

  productSubId: str
  """Sub-product identifier to pass when subscribing."""
  minAmount: str
  """Minimum subscription amount."""
  remainQuota: str
  """Remaining subscribable quota."""
  exchangeRate: str
  """Exchange rate that would be applied."""
  productCoin: str
  """Product base coin."""
  interestTime: TimestampMillis
  """Time interest starts accruing, ms epoch."""
  settleTime: TimestampMillis
  """Interest settlement time, ms epoch."""
  precision: str
  """Decimal precision for the subscription amount."""
  feeRate: str
  """Subscription fee rate."""
  subscriptionCoinList: NotRequired[list[EliteSubscriptionCoin]]
  """Alternate coins accepted for subscription. Present for BGUSD per API docs."""


class EliteSubscribeInfoEndpoint(RpcEndpoint):
  """`classic.earn.elite_subscribe_info`."""

  async def elite_subscribe_info(
    self,
    *,
    product_id: str,
    validate: bool | None = None,
  ) -> EliteSubscribeInfo:
    """Get subscription limits and timing for an Elite product before subscribing.

    Args:
      product_id: Product to preview.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/elite/Elite-Subscribe-Info)
    """
    params: dict = {
      'productId': product_id,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/earn/elite/subscribe-info',
      params=params,
      validator=validator(EliteSubscribeInfo),
      validate=validate,
    )
