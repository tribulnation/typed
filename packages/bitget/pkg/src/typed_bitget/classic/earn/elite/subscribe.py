# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class EliteSubscribeResult(TypedDict):
  """Result of an Elite subscription request."""

  orderId: str
  """Order identifier for this subscription; poll subscribe-result with it."""


class Request(TypedDict):
  """Elite subscription request."""

  productSubId: str
  """Sub-product to subscribe to, from subscribe-info."""
  amount: str
  """Amount to subscribe."""
  coin: NotRequired[str]
  """Coin to pay with, when the product accepts more than one."""
  paymentAccount: NotRequired[Literal['spot', 'unified']]
  """Account to draw the subscription payment from."""


class Subscribe(RpcEndpoint):
  """Subscribe an amount to an Elite product sub-offering."""

  async def subscribe(
    self,
    *,
    product_sub_id: str,
    amount: str,
    coin: str | None = None,
    payment_account: Literal['spot', 'unified'] | None = None,
    validate: bool | None = None,
  ) -> EliteSubscribeResult:
    """Subscribe an amount to an Elite product sub-offering.

    Args:
      product_sub_id: Sub-product to subscribe to, from subscribe-info.
      amount: Amount to subscribe.
      coin: Coin to pay with, when the product accepts more than one.
      payment_account: Account to draw the subscription payment from.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/earn-classic-elite/classic-earn-elite#elite-subscribe)
    """
    request: Request = Request(productSubId=product_sub_id, amount=amount)
    if coin is not None:
      request['coin'] = coin
    if payment_account is not None:
      request['paymentAccount'] = payment_account
    return await self.request(
      request,
      method='POST',
      path='/api/v2/earn/elite/subscribe',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=EliteSubscribeResult,
    )
