# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class EliteSubscribeStatus(TypedDict):
  """Outcome of an Elite subscription."""

  result: str
  """Subscription outcome. The reference SDK types this as `'settled' | 'pending' | 'rejected' | string`, i.e. it does not treat the set as closed; left bare rather than declaring an enum the venue may extend. Candidates observed in the reference SDK: `settled`, `pending`, `rejected`."""


class Request(TypedDict):
  orderId: str
  """Order identifier."""


class SubscribeResult(RpcEndpoint):
  """Poll the outcome of a previously submitted Elite subscription order."""

  async def subscribe_result(
    self,
    order_id: str,
    *,
    validate: bool | None = None,
  ) -> EliteSubscribeStatus:
    """Poll the outcome of a previously submitted Elite subscription order.

    Args:
      order_id: Order identifier.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/earn-classic-elite/classic-earn-elite#get-elite-subscribe-result)
    """
    return await self.request(
      Request(orderId=order_id),
      method='GET',
      path='/api/v2/earn/elite/subscribe-result',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=EliteSubscribeStatus,
    )
