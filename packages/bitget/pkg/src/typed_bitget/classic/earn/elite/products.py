# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import EliteSubscriptionCoin


class EliteProduct(TypedDict):
  """One Elite product."""

  productId: str
  """Product identifier."""
  coin: str
  """Product base coin."""
  minApr: Decimal
  """Minimum documented annual percentage rate."""
  maxApr: Decimal
  """Maximum documented annual percentage rate."""
  subscriptionCoinList: list[EliteSubscriptionCoin]
  """Coins accepted for subscription."""
  sellOut: str
  """Whether the product is sold out. The reference SDK types this as `'YES' | 'NO' | string`, i.e. it does not treat the set as closed; left bare rather than declaring an enum the venue may extend."""


Response = list[EliteProduct]


class Products(RpcEndpoint):
  """List Elite (on-chain structured/subscription) products."""

  async def products(self, *, validate: bool | None = None) -> Response:
    """List Elite (on-chain structured/subscription) products.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/earn-classic-elite/classic-earn-elite#get-elite-product)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v2/earn/elite/product',
      meta={'required': True},
      validate=validate,
      response_type=Response,
    )
