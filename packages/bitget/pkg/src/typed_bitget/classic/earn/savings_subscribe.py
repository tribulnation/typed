# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.earn.savings_subscribe` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SavingsSubscribeRequest(TypedDict):
  """Savings subscription request."""

  productId: str
  """Product to subscribe to."""
  periodType: Literal['flexible', 'fixed']
  """Term structure of the product."""
  amount: str
  """Amount of `coin` to subscribe."""


class SavingsSubscribeResult(TypedDict):
  """Result of a Savings subscription request."""

  orderId: str
  """Order identifier for this subscription; poll subscribe-result with it."""
  status: NotRequired[str]
  """Immediate processing status. The venue does not document a closed set for this field; poll `classic.earn.savings_subscribe_result` for the authoritative outcome (`success`/`fail`) instead of relying on this value."""


class SavingsSubscribe(RpcEndpoint):
  """`classic.earn.savings_subscribe`."""

  async def savings_subscribe(
    self,
    savings_subscribe_request: SavingsSubscribeRequest,
    *,
    validate: bool | None = None,
  ) -> SavingsSubscribeResult:
    """Subscribe an amount of a coin to a Savings product.

    Args:
      savings_subscribe_request: Subscribe an amount of a coin to a Savings product.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/savings/Savings-Subscribe)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/earn/savings/subscribe',
      json=savings_subscribe_request,
      validator=validator(SavingsSubscribeResult),
      validate=validate,
    )
