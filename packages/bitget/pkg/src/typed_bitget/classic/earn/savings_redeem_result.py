# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.earn.savings_redeem_result` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SavingsRedeemStatus(TypedDict):
  """Outcome of a Savings redemption."""

  result: Literal['success', 'fail']
  """Redemption outcome."""
  msg: str
  """Human-readable detail, e.g. a failure reason."""


class SavingsRedeemResult(RpcEndpoint):
  """`classic.earn.savings_redeem_result`."""

  async def savings_redeem_result(
    self,
    *,
    order_id: str,
    period_type: Literal['flexible', 'fixed'],
    validate: bool | None = None,
  ) -> SavingsRedeemStatus:
    """Poll the outcome of a previously submitted Savings redemption order.

    Args:
      order_id: Redemption order identifier.
      period_type: Term structure of the product.

    References:
      - [Official docs](https://www.bitget.com/api-doc/earn/savings/Savings-Redeem-Result)
    """
    params: dict = {
      'orderId': order_id,
      'periodType': period_type,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/earn/savings/redeem-result',
      params=params,
      validator=validator(SavingsRedeemStatus),
      validate=validate,
    )
