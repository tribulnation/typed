# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.public.bot_assets` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class BotAsset(TypedDict):
  """One coin's bot-account balance."""

  coin: str
  """Coin symbol."""
  available: str
  """Available balance."""
  equity: str
  """Total account equity for this coin."""
  bonus: str
  """Trading bonuses."""
  frozen: str
  """Amount currently locked in open orders."""
  usdtValue: str
  """USDT-denominated value."""


class BotAssets(RpcEndpoint):
  """`classic.public.bot_assets`."""

  async def bot_assets(
    self,
    *,
    account_type: Literal['futures', 'spot'] | None = None,
    validate: bool | None = None,
  ) -> list[BotAsset]:
    """Get the caller's trading-bot account assets.

    Args:
      account_type: Restrict to one bot account type.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/common/account/Bot-Assets)
    """
    params = {}
    if account_type is not None:
      params['accountType'] = account_type
    return await self.authed_request(
      'GET',
      '/api/v2/account/bot-assets',
      params=params,
      validator=validator(list[BotAsset]),
      validate=validate,
    )
