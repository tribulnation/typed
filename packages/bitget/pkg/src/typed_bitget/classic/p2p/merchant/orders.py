# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MerchantOrderPaymentField(TypedDict):
  name: str
  """Field name."""
  required: str
  """Whether the field is required."""
  type: str
  """Field type. The venue does not document a closed set of values for this generic payment-method-field descriptor."""
  value: str
  """Field value."""


class Request(TypedDict):
  startTime: NotRequired[TimestampMillis]
  """Range start, ms epoch. When given together with endTime, the span must be at most 90 days."""
  endTime: NotRequired[TimestampMillis]
  """Range end, ms epoch. When given together with startTime, the span must be at most 90 days."""
  idLessThan: NotRequired[str]
  """Pagination cursor: return orders with id less than this value."""
  limit: NotRequired[int]
  """Maximum number of orders to return."""
  status: NotRequired[
    Literal['pending_pay', 'Paid', 'Appeal', 'Completed', 'cancelled']
  ]
  """Restrict to orders in this status."""
  advNo: NotRequired[str]
  """Restrict to orders against this advertisement number."""
  side: NotRequired[Literal['buy', 'sell']]
  """Restrict to orders on this side."""
  coin: NotRequired[str]
  """Restrict to orders in this coin."""
  fiat: NotRequired[str]
  """Restrict to orders in this fiat currency."""
  orderNo: NotRequired[str]
  """Restrict to a single order number."""
  language: NotRequired[Literal['zh-CN', 'en-US']]
  """Response message language."""


class MerchantOrderPaymentInfo(TypedDict):
  """Payment method used for this order."""

  paymethodName: str
  """Payment method name."""
  paymethodId: str
  """Payment method id."""
  paymethodInfo: list[MerchantOrderPaymentField]
  """Payment method fields."""


class MerchantOrder(TypedDict):
  orderId: str
  """Unique order id."""
  orderNo: str
  """Order number."""
  advNo: str
  """Advertisement number this order was placed against."""
  side: Literal['buy', 'sell']
  """Order side."""
  count: str
  """Amount of cryptocurrency."""
  coin: str
  """Cryptocurrency."""
  price: str
  """Unit price."""
  fiat: str
  """Fiat currency."""
  amount: str
  """Total order amount."""
  status: Literal['pending_pay', 'Paid', 'Appeal', 'Completed', 'cancelled']
  """Current order status."""
  buyerRealName: str
  """Buyer's verified name."""
  sellerRealName: str
  """Seller's verified name."""
  paymentTime: TimestampMillis
  """Payment timestamp, ms epoch."""
  releaseTime: TimestampMillis
  """Coin release timestamp, ms epoch."""
  withdrawTime: TimestampMillis
  """Withdrawal timestamp, ms epoch."""
  representTime: TimestampMillis
  """Appeal initiation timestamp, ms epoch."""
  ctime: str
  """Creation time, ms epoch."""
  utime: str
  """Last update time, ms epoch."""
  paymentInfo: MerchantOrderPaymentInfo


class MerchantOrderList(TypedDict):
  orderList: list[MerchantOrder] | None
  """Orders on this page. `null` when the caller has none."""
  minOrderId: str | None
  """Smallest order id in this page's results; pass as idLessThan to fetch the next page. `null` when `orderList` is `null`."""


class Orders(RpcEndpoint):
  """List the authenticated merchant's P2P orders."""

  async def orders_paged(
    self,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    status: Literal['pending_pay', 'Paid', 'Appeal', 'Completed', 'cancelled']
    | None = None,
    adv_no: str | None = None,
    side: Literal['buy', 'sell'] | None = None,
    coin: str | None = None,
    fiat: str | None = None,
    order_no: str | None = None,
    language: Literal['zh-CN', 'en-US'] | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[MerchantOrderList]:
    """List the authenticated merchant's P2P orders.

    Paged variant of `orders`: Passes each page's token back as `idLessThan` and stops on the first empty page, or after `max_pages` pages when one is given.

    Args:
      start_time: Range start, ms epoch. When given together with endTime, the span must be at most 90 days.
      end_time: Range end, ms epoch. When given together with startTime, the span must be at most 90 days.
      limit: Maximum number of orders to return.
      status: Restrict to orders in this status.
      adv_no: Restrict to orders against this advertisement number.
      side: Restrict to orders on this side.
      coin: Restrict to orders in this coin.
      fiat: Restrict to orders in this fiat currency.
      order_no: Restrict to a single order number.
      language: Response message language.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-p2p-p2p/classic-p2p#get-merchant-p2p-orders)
    """
    id_less_than: str | None = None
    pages = 0
    while True:
      response = await self.orders(
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        status=status,
        adv_no=adv_no,
        side=side,
        coin=coin,
        fiat=fiat,
        order_no=order_no,
        language=language,
        id_less_than=id_less_than,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      rows = response.get('orderList') if response is not None else None
      if not rows:
        break
      id_less_than = response.get('minOrderId') if response is not None else None

  async def orders(
    self,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    id_less_than: str | None = None,
    limit: int | None = None,
    status: Literal['pending_pay', 'Paid', 'Appeal', 'Completed', 'cancelled']
    | None = None,
    adv_no: str | None = None,
    side: Literal['buy', 'sell'] | None = None,
    coin: str | None = None,
    fiat: str | None = None,
    order_no: str | None = None,
    language: Literal['zh-CN', 'en-US'] | None = None,
    validate: bool | None = None,
  ) -> MerchantOrderList:
    """List the authenticated merchant's P2P orders.

    Args:
      start_time: Range start, ms epoch. When given together with endTime, the span must be at most 90 days.
      end_time: Range end, ms epoch. When given together with startTime, the span must be at most 90 days.
      id_less_than: Pagination cursor: return orders with id less than this value.
      limit: Maximum number of orders to return.
      status: Restrict to orders in this status.
      adv_no: Restrict to orders against this advertisement number.
      side: Restrict to orders on this side.
      coin: Restrict to orders in this coin.
      fiat: Restrict to orders in this fiat currency.
      order_no: Restrict to a single order number.
      language: Response message language.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-p2p-p2p/classic-p2p#get-merchant-p2p-orders)
    """
    request: Request = Request()
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if id_less_than is not None:
      request['idLessThan'] = id_less_than
    if limit is not None:
      request['limit'] = limit
    if status is not None:
      request['status'] = status
    if adv_no is not None:
      request['advNo'] = adv_no
    if side is not None:
      request['side'] = side
    if coin is not None:
      request['coin'] = coin
    if fiat is not None:
      request['fiat'] = fiat
    if order_no is not None:
      request['orderNo'] = order_no
    if language is not None:
      request['language'] = language
    return await self.request(
      request,
      method='GET',
      path='/api/v2/p2p/orderList',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=MerchantOrderList,
    )
