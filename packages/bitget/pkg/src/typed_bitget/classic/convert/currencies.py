# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class ConvertCurrency(TypedDict):
  """A currency available for Convert (instant-swap), with balance and transaction-size limits."""

  coin: str
  """Coin symbol, e.g. `"ETH"`."""
  available: Decimal
  """Balance of this coin available in the user's account."""
  maxAmount: Decimal
  """Maximum usable quantity: as the `fromCoin` of a conversion, the maximum consumable quantity; as the `toCoin`, the maximum redeemable quantity."""
  minAmount: Decimal
  """Minimum usable quantity: as the `fromCoin` of a conversion, the minimum consumable quantity; as the `toCoin`, the minimum redeemable quantity."""


Response = list[ConvertCurrency]


class Currencies(RpcEndpoint):
  """Get the list of currencies supported by Convert (instant-swap), with each currency's available balance and transaction size limits."""

  async def currencies(self, *, validate: bool | None = None) -> Response:
    """Get the list of currencies supported by Convert (instant-swap), with each currency's available balance and transaction size limits.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-convert/classic-spot-convert#get-convert-coins)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v2/convert/currencies',
      meta={'required': True},
      validate=validate,
      response_type=Response,
    )
