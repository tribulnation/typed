# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.convert.bgb_records` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis, timestamp_millis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class BgbConvertRecordFee(TypedDict):
  """One fee charged on a BGB conversion order."""

  feeCoin: str
  """Coin the fee was charged in."""
  fee: str
  """Fee amount charged."""


class BgbConvertRecord(TypedDict):
  """One BGB conversion order."""

  orderId: str
  """Id of the conversion order."""
  fromCoin: str
  """Coin converted from."""
  fromAmount: str
  """Quantity of `fromCoin` converted."""
  fromCoinPrice: str
  """Price of `fromCoin` used for the conversion."""
  toCoin: str
  """Coin converted to. Always `"BGB"` for this endpoint."""
  toAmount: str
  """Quantity of `toCoin` (BGB) received."""
  toCoinPrice: str
  """Price of `toCoin` (BGB) used for the conversion."""
  feeDetail: list[BgbConvertRecordFee]
  """Fee charged on this conversion."""
  status: str
  """Conversion order status, e.g. `"success"`. The venue does not document its full set of values."""
  ctime: TimestampMillis
  """Time the conversion order was generated."""


class BgbRecords(RpcEndpoint):
  """`classic.convert.bgb_records`."""

  async def bgb_records(
    self,
    *,
    order_id: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    id_less_than: str | None = None,
    validate: bool | None = None,
  ) -> list[BgbConvertRecord]:
    """Get the history of BGB conversion orders, optionally filtered by order id or time window.

    Args:
      order_id: Filter to one BGB conversion order id.
      start_time: Start time, Unix millisecond timestamp.
      end_time: End time, Unix millisecond timestamp. The maximum interval between `startTime` and `endTime` is 90 days.
      limit: Number of records per page. Default 20, maximum 100.
      id_less_than: Pagination cursor. From page 2 onward, set this to the smallest `orderId` seen in the previous response.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/bgb-convert/Get-BGB-Convert-Record)
    """
    params = {}
    if order_id is not None:
      params['orderId'] = order_id
    if start_time is not None:
      params['startTime'] = timestamp_millis.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp_millis.dump(end_time)
    if limit is not None:
      params['limit'] = limit
    if id_less_than is not None:
      params['idLessThan'] = id_less_than
    return await self.authed_request(
      'GET',
      '/api/v2/convert/bgb-convert-records',
      params=params,
      validator=validator(list[BgbConvertRecord]),
      validate=validate,
    )
