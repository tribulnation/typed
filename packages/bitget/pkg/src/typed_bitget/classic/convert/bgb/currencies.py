# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class BgbConvertCoinFee(TypedDict):
  """Fee for converting one coin into BGB."""

  feeRate: Decimal
  """Fee rate charged on this conversion."""
  fee: Decimal
  """Fee amount charged on this conversion."""


class BgbConvertCoin(TypedDict):
  """A currency eligible for BGB conversion, with balance, estimated BGB proceeds and fee."""

  coin: str
  """Coin symbol."""
  available: Decimal
  """Balance of this coin available in the user's account."""
  bgbEstAmount: Decimal
  """Expected amount of BGB redeemable for the full available balance of this coin."""
  precision: int
  """Decimal precision of the BGB amount."""
  feeDetail: list[BgbConvertCoinFee]
  """Fee that applies to converting this coin into BGB."""
  cTime: TimestampMillis
  """Time this entry was generated."""


class BgbConvertCoinList(TypedDict):
  """Currencies eligible for BGB conversion."""

  coinList: list[BgbConvertCoin]
  """One entry per currency eligible for conversion into BGB."""


class Currencies(RpcEndpoint):
  """Get the list of small-balance currencies eligible for bulk conversion into BGB, with each currency's available balance, estimated BGB proceeds and fee."""

  async def currencies(self, *, validate: bool | None = None) -> BgbConvertCoinList:
    """Get the list of small-balance currencies eligible for bulk conversion into BGB, with each currency's available balance, estimated BGB proceeds and fee.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-bgb-convert/classic-spot-bgb-convert#get-bgb-convert-coins)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v2/convert/bgb-convert-coin-list',
      meta={'required': True},
      validate=validate,
      response_type=BgbConvertCoinList,
    )
