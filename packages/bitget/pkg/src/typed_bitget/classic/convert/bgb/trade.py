# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class BgbConvertOrder(TypedDict):
  """One coin's BGB conversion order."""

  coin: str
  """Coin that was converted."""
  orderId: str
  """Id of the conversion order."""


class Request(TypedDict):
  """Coins to convert into BGB, in one bulk request."""

  coinList: list[str]
  """Coin symbols to convert into BGB, e.g. `["EOS", "GROK"]`. Each must be a coin returned by the BGB-convert coins endpoint."""


class BgbConvertResult(TypedDict):
  """Result of a bulk BGB conversion: one order per requested coin."""

  orderList: list[BgbConvertOrder]
  """One entry per coin converted."""


class Trade(RpcEndpoint):
  """Bulk-convert a list of small-balance coins into BGB, using the full available balance of each. Returns one order per requested coin."""

  async def trade(
    self,
    coin_list: list[str],
    *,
    validate: bool | None = None,
  ) -> BgbConvertResult:
    """Bulk-convert a list of small-balance coins into BGB, using the full available balance of each. Returns one order per requested coin.

    Args:
      coin_list: Coin symbols to convert into BGB, e.g. `["EOS", "GROK"]`. Each must be a coin returned by the BGB-convert coins endpoint.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-bgb-convert/classic-spot-bgb-convert#convert-bgb)
    """
    return await self.request(
      Request(coinList=coin_list),
      method='POST',
      path='/api/v2/convert/bgb-convert',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=BgbConvertResult,
    )
