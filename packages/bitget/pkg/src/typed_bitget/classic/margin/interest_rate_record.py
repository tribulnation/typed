# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MarginInterestRateRecord(TypedDict):
  """Current leverage interest rate for one coin."""

  coin: str
  """Coin."""
  dailyInterestRate: Decimal
  """Daily interest rate, decimal form, 8 decimal places."""
  annualInterestRate: Decimal
  """Annual interest rate, decimal form, 8 decimal places."""
  updatedTime: TimestampMillis
  """Unix millisecond timestamp this rate was last updated. Confirmed live: returned as a string, not an integer."""


class Request(TypedDict):
  coin: str
  """Coin."""


class InterestRateRecord(RpcEndpoint):
  """Get the current daily and annual leverage interest rate for one coin. Not parameterized by margin type (applies to both cross and isolated). Not listed in `spec/discovery.md`'s margin bullet; added here (see spec authoring notes). Rate limit: 10 requests/second per IP."""

  async def interest_rate_record(
    self,
    coin: str,
    *,
    validate: bool | None = None,
  ) -> MarginInterestRateRecord:
    """Get the current daily and annual leverage interest rate for one coin. Not parameterized by margin type (applies to both cross and isolated). Not listed in `spec/discovery.md`'s margin bullet; added here (see spec authoring notes). Rate limit: 10 requests/second per IP.

    Args:
      coin: Coin.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-margin-common/classic-margin-common#get-the-leverage-interest-rate)
    """
    return await self.request(
      Request(coin=coin),
      method='GET',
      path='/api/v2/margin/interest-rate-record',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=MarginInterestRateRecord,
    )
