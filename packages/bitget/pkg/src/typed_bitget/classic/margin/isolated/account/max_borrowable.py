# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.margin.isolated.account.max_borrowable` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class IsolatedMarginMaxBorrowable(TypedDict):
  """Maximum borrowable amounts for one trading pair under isolated margin."""

  symbol: NotRequired[str]
  """Trading pair."""
  baseCoin: NotRequired[str]
  """Base coin of the pair."""
  baseCoinMaxBorrowAmount: NotRequired[str]
  """Maximum amount of the base coin currently borrowable. Changes in real time."""
  quoteCoin: NotRequired[str]
  """Quote coin of the pair."""
  quoteCoinMaxBorrowAmount: NotRequired[str]
  """Maximum amount of the quote coin currently borrowable."""


class MaxBorrowable(RpcEndpoint):
  """`classic.margin.isolated.account.max_borrowable`."""

  async def max_borrowable(
    self,
    *,
    symbol: str,
    validate: bool | None = None,
  ) -> IsolatedMarginMaxBorrowable:
    """Query the maximum amount currently borrowable under isolated margin. Rate limit: 10 requests/second per UID.

    Args:
      symbol: Trading pair to check.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/margin/isolated/account/Isolated-Max-Borrowable-Amount)
    """
    params: dict = {
      'symbol': symbol,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/margin/isolated/account/max-borrowable-amount',
      params=params,
      validator=validator(IsolatedMarginMaxBorrowable),
      validate=validate,
    )
