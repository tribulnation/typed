# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class IsolatedMarginMaxTransferable(TypedDict):
  """Maximum transferable amounts for one trading pair under isolated margin."""

  baseCoin: str
  """Base coin of the pair."""
  symbol: str
  """Trading pair."""
  baseCoinMaxTransferOutAmount: Decimal
  """Maximum amount of the base coin currently transferable out."""
  quoteCoin: str
  """Quote coin of the pair."""
  quoteCoinMaxTransferOutAmount: Decimal
  """Maximum amount of the quote coin currently transferable out."""


class Request(TypedDict):
  symbol: str
  """Trading pair."""


class MaxTransferable(RpcEndpoint):
  """Query the maximum amount currently transferable out of isolated margin. Rate limit: 10 requests/second per UID."""

  async def max_transferable(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> IsolatedMarginMaxTransferable:
    """Query the maximum amount currently transferable out of isolated margin. Rate limit: 10 requests/second per UID.

    Args:
      symbol: Trading pair.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-margin-isolated-account/classic-margin-isolated-account#get-isolated-max-transferable)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/api/v2/margin/isolated/account/max-transfer-out-amount',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=IsolatedMarginMaxTransferable,
    )
