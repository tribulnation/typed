# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class IsolatedMarginFlashRepayResultEntry(TypedDict):
  """Flash-repay outcome for one trading pair."""

  repayId: str
  """Repayment id."""
  symbol: str
  """Trading pair."""
  result: Literal['success', 'failure']
  """Outcome of the repayment request."""


class Request(TypedDict):
  """Isolated margin flash-repay request."""

  symbolList: NotRequired[list[str]]
  """Trading pairs to repay, up to 100. Omit to repay every isolated pair."""


Response = list[IsolatedMarginFlashRepayResultEntry]


class FlashRepay(RpcEndpoint):
  """Immediately repay isolated margin debt for one or more trading pairs, using account balance, without placing a trade. Repays every pair if none is given. Rate limit: 10 requests/second per UID."""

  async def flash_repay(
    self,
    symbol_list: list[str] | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Immediately repay isolated margin debt for one or more trading pairs, using account balance, without placing a trade. Repays every pair if none is given. Rate limit: 10 requests/second per UID.

    Args:
      symbol_list: Trading pairs to repay, up to 100. Omit to repay every isolated pair.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-margin-isolated-account/classic-margin-isolated-account#isolated-flash-repay)
    """
    request: Request = Request()
    if symbol_list is not None:
      request['symbolList'] = symbol_list
    return await self.request(
      request,
      method='POST',
      path='/api/v2/margin/isolated/account/flash-repay',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
