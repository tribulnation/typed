# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, Sequence, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class IsolatedMarginFinancialRecord(TypedDict):
  """One capital-flow record."""

  symbol: NotRequired[str]
  """Trading pair."""
  coin: NotRequired[str]
  """Coin."""
  marginId: NotRequired[str]
  """Capital-flow record id."""
  marginType: NotRequired[
    Literal[
      'transfer_in',
      'transfer_out',
      'borrow',
      'repay',
      'liquidation_fee',
      'compensate',
      'deal_in',
      'deal_out',
      'confiscated',
      'exchange_in',
      'exchange_out',
      'sys_exchange_in',
      'sys_exchange_out',
    ]
  ]
  """Capital-flow type. (Named `marginType` by upstream; unrelated to the cross/isolated margin-type path segment.)"""
  amount: NotRequired[Decimal]
  """Capital-flow amount."""
  balance: NotRequired[Decimal]
  """Account balance after this event."""
  fee: NotRequired[Decimal]
  """Fee charged, if any."""
  cTime: NotRequired[TimestampMillis]
  """Creation time, Unix millisecond timestamp. Confirmed live: returned as a string, not an integer."""
  uTime: NotRequired[TimestampMillis]
  """Last update time, Unix millisecond timestamp. Confirmed live: returned as a string, not an integer."""


class Request(TypedDict):
  symbol: str
  """Trading pair."""
  marginType: NotRequired[
    Literal[
      'transfer_in',
      'transfer_out',
      'borrow',
      'repay',
      'liquidation_fee',
      'compensate',
      'deal_in',
      'deal_out',
      'confiscated',
      'exchange_in',
      'exchange_out',
      'sys_exchange_in',
      'sys_exchange_out',
    ]
  ]
  """Capital-flow type."""
  coin: NotRequired[str]
  """Coin."""
  startTime: TimestampMillis
  """Start time."""
  endTime: NotRequired[TimestampMillis]
  """End time."""
  limit: NotRequired[int]
  """Number of results to return. Default 100, maximum 500."""
  idLessThan: NotRequired[str]
  """Pagination cursor."""


class IsolatedMarginFinancialRecords(TypedDict):
  """IsolatedMarginFinancialRecords listing."""

  resultList: list[IsolatedMarginFinancialRecord]
  """Matching records, newest first."""
  maxId: str
  """Largest id in this page of results."""
  minId: str
  """Smallest id in this page of results. Pass as `idLessThan` to fetch the next page."""


class FinancialRecords(RpcEndpoint):
  """Query isolated margin capital-flow history (every balance-affecting event: transfers, borrows, repayments, fees, liquidation, etc). Isolated margin is scoped per trading pair, so `symbol` is required here, unlike the equivalent cross-margin endpoint. Rate limit: 10 requests/second per UID."""

  def financial_records_paged(
    self,
    *,
    symbol: str,
    margin_type: Literal[
      'transfer_in',
      'transfer_out',
      'borrow',
      'repay',
      'liquidation_fee',
      'compensate',
      'deal_in',
      'deal_out',
      'confiscated',
      'exchange_in',
      'exchange_out',
      'sys_exchange_in',
      'sys_exchange_out',
    ]
    | None = None,
    coin: str | None = None,
    start_time: TimestampMillis,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[IsolatedMarginFinancialRecord, str]:
    """Query isolated margin capital-flow history (every balance-affecting event: transfers, borrows, repayments, fees, liquidation, etc). Isolated margin is scoped per trading pair, so `symbol` is required here, unlike the equivalent cross-margin endpoint. Rate limit: 10 requests/second per UID.

    Paged variant of `financial_records`: Passes each page's token back as `idLessThan` and stops on the first empty page. Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      symbol: Trading pair.
      margin_type: Capital-flow type.
      coin: Coin.
      start_time: Start time.
      end_time: End time.
      limit: Number of results to return. Default 100, maximum 500.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-margin-isolated-record/classic-margin-isolated-record#get-isolated-financial-history)
    """

    async def next(
      id_less_than: str,
    ) -> tuple[Sequence[IsolatedMarginFinancialRecord], str | None]:
      response = await self.financial_records(
        symbol=symbol,
        margin_type=margin_type,
        coin=coin,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        id_less_than=(id_less_than or None),
        validate=validate,
      )
      rows = response.get('resultList')
      rows = rows if rows is not None else []
      state = response.get('minId')
      return rows, (state or None) if rows else None

    return PaginatedResponse('', next)

  async def financial_records(
    self,
    *,
    symbol: str,
    margin_type: Literal[
      'transfer_in',
      'transfer_out',
      'borrow',
      'repay',
      'liquidation_fee',
      'compensate',
      'deal_in',
      'deal_out',
      'confiscated',
      'exchange_in',
      'exchange_out',
      'sys_exchange_in',
      'sys_exchange_out',
    ]
    | None = None,
    coin: str | None = None,
    start_time: TimestampMillis,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    id_less_than: str | None = None,
    validate: bool | None = None,
  ) -> IsolatedMarginFinancialRecords:
    """Query isolated margin capital-flow history (every balance-affecting event: transfers, borrows, repayments, fees, liquidation, etc). Isolated margin is scoped per trading pair, so `symbol` is required here, unlike the equivalent cross-margin endpoint. Rate limit: 10 requests/second per UID.

    Args:
      symbol: Trading pair.
      margin_type: Capital-flow type.
      coin: Coin.
      start_time: Start time.
      end_time: End time.
      limit: Number of results to return. Default 100, maximum 500.
      id_less_than: Pagination cursor.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-margin-isolated-record/classic-margin-isolated-record#get-isolated-financial-history)
    """
    request: Request = Request(symbol=symbol, startTime=start_time)
    if margin_type is not None:
      request['marginType'] = margin_type
    if coin is not None:
      request['coin'] = coin
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    if id_less_than is not None:
      request['idLessThan'] = id_less_than
    return await self.request(
      request,
      method='GET',
      path='/api/v2/margin/isolated/financial-records',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=IsolatedMarginFinancialRecords,
    )
