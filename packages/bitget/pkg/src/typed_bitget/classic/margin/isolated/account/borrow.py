# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class IsolatedMarginBorrowResult(TypedDict):
  """Result of a margin borrow."""

  loanId: str
  """Loan id."""
  coin: str
  """Borrowed coin."""
  borrowAmount: Decimal
  """Borrowed amount."""
  symbol: str
  """Trading pair borrowed against."""


class Request(TypedDict):
  """Isolated margin borrow request."""

  symbol: str
  """Trading pair to borrow against."""
  coin: str
  """Coin to borrow."""
  borrowAmount: Decimal
  """Amount to borrow, up to 8 decimal places."""
  clientOid: NotRequired[str]
  """Caller-supplied idempotency id."""


class Borrow(RpcEndpoint):
  """Borrow a coin under isolated margin. Rate limit: 10 requests/second per UID."""

  async def borrow(
    self,
    *,
    symbol: str,
    coin: str,
    borrow_amount: Decimal,
    client_oid: str | None = None,
    validate: bool | None = None,
  ) -> IsolatedMarginBorrowResult:
    """Borrow a coin under isolated margin. Rate limit: 10 requests/second per UID.

    Args:
      symbol: Trading pair to borrow against.
      coin: Coin to borrow.
      borrow_amount: Amount to borrow, up to 8 decimal places.
      client_oid: Caller-supplied idempotency id.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-margin-isolated-account/classic-margin-isolated-account#isolated-borrow)
    """
    request: Request = Request(symbol=symbol, coin=coin, borrowAmount=borrow_amount)
    if client_oid is not None:
      request['clientOid'] = client_oid
    return await self.request(
      request,
      method='POST',
      path='/api/v2/margin/isolated/account/borrow',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=IsolatedMarginBorrowResult,
    )
