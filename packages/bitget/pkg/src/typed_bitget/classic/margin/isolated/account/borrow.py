# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.margin.isolated.account.borrow` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class IsolatedMarginBorrowRequest(TypedDict):
  """Isolated margin borrow request."""

  symbol: str
  """Trading pair to borrow against."""
  coin: str
  """Coin to borrow."""
  borrowAmount: str
  """Amount to borrow, up to 8 decimal places."""
  clientOid: NotRequired[str]
  """Caller-supplied idempotency id."""


class IsolatedMarginBorrowResult(TypedDict):
  """Result of a margin borrow."""

  loanId: NotRequired[str]
  """Loan id."""
  coin: NotRequired[str]
  """Borrowed coin."""
  borrowAmount: NotRequired[str]
  """Borrowed amount."""
  symbol: NotRequired[str]
  """Trading pair borrowed against."""


class Borrow(RpcEndpoint):
  """`classic.margin.isolated.account.borrow`."""

  async def borrow(
    self,
    isolated_margin_borrow_request: IsolatedMarginBorrowRequest,
    *,
    validate: bool | None = None,
  ) -> IsolatedMarginBorrowResult:
    """Borrow a coin under isolated margin. Rate limit: 10 requests/second per UID.

    Args:
      isolated_margin_borrow_request: Borrow a coin under isolated margin.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/margin/isolated/account/Isolated-Borrow)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/margin/isolated/account/borrow',
      json=isolated_margin_borrow_request,
      validator=validator(IsolatedMarginBorrowResult),
      validate=validate,
    )
