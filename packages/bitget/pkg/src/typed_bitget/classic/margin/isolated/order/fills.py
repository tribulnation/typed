# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MarginFeeDetail(TypedDict):
  """Transaction fee breakdown for one fill."""

  deduction: NotRequired[str]
  """Whether the fee was paid with a discount/deduction."""
  feeCoin: NotRequired[str]
  """Coin the fee was charged in."""
  totalDeductionFee: NotRequired[Decimal]
  """Total fee amount covered by a discount/deduction."""
  totalFee: NotRequired[Decimal]
  """Total fee amount charged."""


class Request(TypedDict):
  symbol: str
  """Trading pair."""
  orderId: NotRequired[str]
  """Order id."""
  idLessThan: NotRequired[str]
  """Pagination cursor."""
  startTime: TimestampMillis
  """Start time."""
  endTime: NotRequired[TimestampMillis]
  """End time."""
  limit: NotRequired[int]
  """Number of results to return. Default 100, maximum 500."""


class IsolatedMarginFill(TypedDict):
  """IsolatedMarginFill."""

  orderId: NotRequired[str]
  """Order id."""
  tradeId: NotRequired[str]
  """Fill (trade) id."""
  orderType: NotRequired[Literal['limit', 'market']]
  """Order type."""
  side: NotRequired[Literal['sell', 'buy']]
  """Fill direction."""
  priceAvg: NotRequired[Decimal]
  """Fill price."""
  size: NotRequired[Decimal]
  """Filled quantity."""
  amount: NotRequired[Decimal]
  """Filled amount."""
  tradeScope: NotRequired[str]
  """Whether this side was the taker or the maker."""
  cTime: NotRequired[TimestampMillis]
  """Fill time, Unix millisecond timestamp. Confirmed live: returned as a string, not an integer."""
  uTime: NotRequired[TimestampMillis]
  """Last update time, Unix millisecond timestamp. Confirmed live: returned as a string, not an integer."""
  feeDetail: NotRequired[MarginFeeDetail]


class IsolatedMarginFills(TypedDict):
  """Margin order fills."""

  fills: list[IsolatedMarginFill]
  """Fills, newest first."""
  maxId: str | None
  """Largest id in this page of results. `null` when the page is empty (confirmed live)."""
  minId: str | None
  """Smallest id in this page of results. Pass as `idLessThan` to fetch the next page. `null` when the page is empty (confirmed live)."""


class Fills(RpcEndpoint):
  """Query isolated margin order fills for one trading pair. Rate limit: 10 requests/second per UID."""

  async def fills_paged(
    self,
    *,
    symbol: str,
    order_id: str | None = None,
    start_time: TimestampMillis,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[IsolatedMarginFills]:
    """Query isolated margin order fills for one trading pair. Rate limit: 10 requests/second per UID.

    Paged variant of `fills`: Passes each page's token back as `idLessThan` and stops on the first empty page, or after `max_pages` pages when one is given.

    Args:
      symbol: Trading pair.
      order_id: Order id.
      start_time: Start time.
      end_time: End time.
      limit: Number of results to return. Default 100, maximum 500.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-margin-isolated-trade/classic-margin-isolated-trade#get-isolated-order-fills)
    """
    id_less_than: str | None = None
    pages = 0
    while True:
      response = await self.fills(
        symbol=symbol,
        order_id=order_id,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        id_less_than=id_less_than,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      rows = response.get('fills') if response is not None else None
      if not rows:
        break
      id_less_than = response.get('minId') if response is not None else None

  async def fills(
    self,
    *,
    symbol: str,
    order_id: str | None = None,
    id_less_than: str | None = None,
    start_time: TimestampMillis,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> IsolatedMarginFills:
    """Query isolated margin order fills for one trading pair. Rate limit: 10 requests/second per UID.

    Args:
      symbol: Trading pair.
      order_id: Order id.
      id_less_than: Pagination cursor.
      start_time: Start time.
      end_time: End time.
      limit: Number of results to return. Default 100, maximum 500.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-margin-isolated-trade/classic-margin-isolated-trade#get-isolated-order-fills)
    """
    request: Request = Request(symbol=symbol, startTime=start_time)
    if order_id is not None:
      request['orderId'] = order_id
    if id_less_than is not None:
      request['idLessThan'] = id_less_than
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/api/v2/margin/isolated/fills',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=IsolatedMarginFills,
    )
