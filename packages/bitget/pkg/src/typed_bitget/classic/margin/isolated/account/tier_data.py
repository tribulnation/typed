# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class IsolatedMarginTier(TypedDict):
  """One leverage tier for a trading pair under isolated margin."""

  tier: int
  """Tier number."""
  symbol: str
  """Trading pair."""
  leverage: Decimal
  """Effective leverage at this tier. Global default 10x."""
  baseCoin: str
  """Base coin of the pair."""
  quoteCoin: str
  """Quote coin of the pair."""
  baseMaxBorrowableAmount: Decimal
  """Maximum borrowable amount of the base coin at this tier."""
  quoteMaxBorrowableAmount: Decimal
  """Maximum borrowable amount of the quote coin at this tier."""
  maintainMarginRate: Decimal
  """Maintenance margin rate at this tier."""
  initRate: Decimal
  """Initial margin rate at this tier."""


class Request(TypedDict):
  symbol: str
  """Trading pair."""


Response = list[IsolatedMarginTier]


class TierData(RpcEndpoint):
  """Query the leverage/margin tier ladder for one trading pair under isolated margin."""

  async def tier_data(self, symbol: str, *, validate: bool | None = None) -> Response:
    """Query the leverage/margin tier ladder for one trading pair under isolated margin.

    Args:
      symbol: Trading pair.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-margin-isolated-account/classic-margin-isolated-account#get-isolated-tier-configuration)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/api/v2/margin/isolated/tier-data',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
