# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.margin.cross.account.flash_repay` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CrossMarginFlashRepayRequest(TypedDict):
  """Cross margin flash-repay request."""

  coin: NotRequired[str]
  """Coin to repay. Omit to fully repay every borrowed coin."""


class CrossMarginFlashRepayResult(TypedDict):
  """Result of a cross margin flash repayment."""

  repayId: NotRequired[str]
  """Repayment id."""
  coin: NotRequired[str]
  """Repaid coin. Empty when the whole account was repaid."""


class FlashRepay(RpcEndpoint):
  """`classic.margin.cross.account.flash_repay`."""

  async def flash_repay(
    self,
    cross_margin_flash_repay_request: CrossMarginFlashRepayRequest,
    *,
    validate: bool | None = None,
  ) -> CrossMarginFlashRepayResult:
    """Immediately repay cross margin debt, using account balance, without placing a trade. Repays every borrowed coin if none is given. Rate limit: 10 requests/second per UID.

    Args:
      cross_margin_flash_repay_request: Immediately repay cross margin debt, using account balance, without placing a trade.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/margin/cross/account/Cross-Flash-Repay)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/margin/crossed/account/flash-repay',
      json=cross_margin_flash_repay_request,
      validator=validator(CrossMarginFlashRepayResult),
      validate=validate,
    )
