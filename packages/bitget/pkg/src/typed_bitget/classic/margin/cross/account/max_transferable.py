# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CrossMarginMaxTransferable(TypedDict):
  """Maximum transferable amount for one coin under cross margin."""

  coin: str
  """Coin."""
  maxTransferOutAmount: Decimal
  """Maximum amount currently transferable out."""


class Request(TypedDict):
  coin: str
  """Coin."""


class MaxTransferable(RpcEndpoint):
  """Query the maximum amount currently transferable out of cross margin. Rate limit: 10 requests/second per UID."""

  async def max_transferable(
    self,
    coin: str,
    *,
    validate: bool | None = None,
  ) -> CrossMarginMaxTransferable:
    """Query the maximum amount currently transferable out of cross margin. Rate limit: 10 requests/second per UID.

    Args:
      coin: Coin.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-margin-cross-account/classic-margin-cross-account#get-cross-max-transferable)
    """
    return await self.request(
      Request(coin=coin),
      method='GET',
      path='/api/v2/margin/crossed/account/max-transfer-out-amount',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=CrossMarginMaxTransferable,
    )
