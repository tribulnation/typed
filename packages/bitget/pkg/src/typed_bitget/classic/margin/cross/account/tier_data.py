# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.margin.cross.account.tier_data` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CrossMarginTier(TypedDict):
  """One leverage tier for a coin under cross margin."""

  tier: NotRequired[str]
  """Tier number."""
  leverage: NotRequired[str]
  """Effective leverage at this tier. Global default 3x."""
  coin: NotRequired[str]
  """Coin."""
  maxBorrowableAmount: NotRequired[str]
  """Maximum borrowable amount at this tier."""
  maintainMarginRate: NotRequired[str]
  """Maintenance margin rate at this tier."""


class TierData(RpcEndpoint):
  """`classic.margin.cross.account.tier_data`."""

  async def tier_data(
    self,
    *,
    coin: str,
    validate: bool | None = None,
  ) -> list[CrossMarginTier]:
    """Query the leverage/margin tier ladder for one coin under cross margin. Rate limit: 10 requests/second per IP.

    Args:
      coin: Coin to check.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/margin/cross/account/Cross-Tier-Data)
    """
    params: dict = {
      'coin': coin,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/margin/crossed/tier-data',
      params=params,
      validator=validator(list[CrossMarginTier]),
      validate=validate,
    )
