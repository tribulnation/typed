# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CrossMarginTier(TypedDict):
  """One leverage tier for a coin under cross margin."""

  tier: int
  """Tier number."""
  leverage: Decimal
  """Effective leverage at this tier. Global default 3x."""
  coin: str
  """Coin."""
  maxBorrowableAmount: Decimal
  """Maximum borrowable amount at this tier."""
  maintainMarginRate: Decimal
  """Maintenance margin rate at this tier."""


class Request(TypedDict):
  coin: str
  """Coin."""


Response = list[CrossMarginTier]


class TierData(RpcEndpoint):
  """Query the leverage/margin tier ladder for one coin under cross margin. Rate limit: 10 requests/second per IP."""

  async def tier_data(self, coin: str, *, validate: bool | None = None) -> Response:
    """Query the leverage/margin tier ladder for one coin under cross margin. Rate limit: 10 requests/second per IP.

    Args:
      coin: Coin.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-margin-cross-account/classic-margin-cross-account#get-cross-tier-configuration)
    """
    return await self.request(
      Request(coin=coin),
      method='GET',
      path='/api/v2/margin/crossed/tier-data',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
