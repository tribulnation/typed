# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CrossMarginBorrowResult(TypedDict):
  """Result of a margin borrow."""

  loanId: str
  """Loan id."""
  coin: str
  """Borrowed coin."""
  borrowAmount: Decimal
  """Borrowed amount."""


class Request(TypedDict):
  """Cross margin borrow request."""

  coin: str
  """Coin to borrow."""
  borrowAmount: Decimal
  """Amount to borrow, up to 8 decimal places."""
  clientOid: NotRequired[str]
  """Caller-supplied idempotency id."""


class Borrow(RpcEndpoint):
  """Borrow a coin under cross margin. Rate limit: 10 requests/second per UID."""

  async def borrow(
    self,
    *,
    coin: str,
    borrow_amount: Decimal,
    client_oid: str | None = None,
    validate: bool | None = None,
  ) -> CrossMarginBorrowResult:
    """Borrow a coin under cross margin. Rate limit: 10 requests/second per UID.

    Args:
      coin: Coin to borrow.
      borrow_amount: Amount to borrow, up to 8 decimal places.
      client_oid: Caller-supplied idempotency id.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-margin-cross-account/classic-margin-cross-account#cross-borrow)
    """
    request: Request = Request(coin=coin, borrowAmount=borrow_amount)
    if client_oid is not None:
      request['clientOid'] = client_oid
    return await self.request(
      request,
      method='POST',
      path='/api/v2/margin/crossed/account/borrow',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=CrossMarginBorrowResult,
    )
