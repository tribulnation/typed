# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CrossMarginMaxBorrowable(TypedDict):
  """Maximum borrowable amount for one coin under cross margin."""

  maxBorrowableAmount: Decimal
  """Maximum amount currently borrowable. Changes in real time."""
  coin: str
  """Coin."""


class Request(TypedDict):
  coin: str
  """Coin."""


class MaxBorrowable(RpcEndpoint):
  """Query the maximum amount currently borrowable under cross margin. Rate limit: 10 requests/second per UID."""

  async def max_borrowable(
    self,
    coin: str,
    *,
    validate: bool | None = None,
  ) -> CrossMarginMaxBorrowable:
    """Query the maximum amount currently borrowable under cross margin. Rate limit: 10 requests/second per UID.

    Args:
      coin: Coin.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-margin-cross-account/classic-margin-cross-account#get-cross-max-borrowable)
    """
    return await self.request(
      Request(coin=coin),
      method='GET',
      path='/api/v2/margin/crossed/account/max-borrowable-amount',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=CrossMarginMaxBorrowable,
    )
