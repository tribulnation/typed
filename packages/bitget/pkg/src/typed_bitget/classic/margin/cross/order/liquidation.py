# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CrossMarginLiquidationOrder(TypedDict):
  """CrossMarginLiquidationOrder. Two shapes share this record depending on `type`: a forced order (`place_order`) carries `symbol`/`orderType`; a forced swap (`swap`) carries `fromCoin`/`toCoin`."""

  orderId: NotRequired[str]
  """Order id."""
  symbol: NotRequired[str]
  """Trading pair. Present when the record is a forced order (`type=place_order`)."""
  orderType: NotRequired[Literal['market']]
  """Order type. Present when the record is a forced order (`type=place_order`); always `market`."""
  side: NotRequired[Literal['liquidation_sell', 'liquidation_buy']]
  """Liquidation direction."""
  priceAvg: NotRequired[str]
  """Average execution price."""
  price: NotRequired[str]
  """Order price."""
  fillSize: NotRequired[str]
  """Executed quantity."""
  size: NotRequired[str]
  """Order quantity."""
  amount: NotRequired[str]
  """Executed amount, in USDT."""
  fromCoin: NotRequired[str]
  """Source coin. Present when the record is a forced swap (`type=swap`)."""
  fromSize: NotRequired[str]
  """Source coin quantity. Present when the record is a forced swap (`type=swap`)."""
  toCoin: NotRequired[str]
  """Target coin. Present when the record is a forced swap (`type=swap`)."""
  toSize: NotRequired[str]
  """Target coin quantity. Present when the record is a forced swap (`type=swap`)."""
  cTime: NotRequired[TimestampMillis]
  """Creation time, Unix millisecond timestamp."""
  uTime: NotRequired[TimestampMillis]
  """Last update time, Unix millisecond timestamp."""


class Request(TypedDict):
  type: NotRequired[Literal['swap', 'place_order']]
  """Liquidation record type."""
  symbol: NotRequired[str]
  """Trading pair."""
  fromCoin: NotRequired[str]
  """Source coin."""
  toCoin: NotRequired[str]
  """Target coin."""
  startTime: NotRequired[TimestampMillis]
  """Start time."""
  endTime: NotRequired[TimestampMillis]
  """End time."""
  limit: NotRequired[int]
  """Number of results to return. Default 100, maximum 500."""
  idLessThan: NotRequired[str]
  """Pagination cursor."""


class CrossMarginLiquidationOrders(TypedDict):
  """Forced liquidation orders/swaps."""

  resultList: NotRequired[list[CrossMarginLiquidationOrder] | None]
  """Liquidation records, newest first. `null` when there are none (confirmed live -- this capture pass's own call returned `null` alongside a `null` `idLessThan`, rather than an empty array)."""
  idLessThan: NotRequired[str | None]
  """Pagination cursor for the next page. `null` when `resultList` is `null` (confirmed live)."""


class Liquidation(RpcEndpoint):
  """Query individual forced (liquidation) orders and coin swaps under cross margin. Distinct from `cross.liquidation_history`, which reports whole liquidation episodes rather than the individual orders/swaps within one. Rate limit: 10 requests/second per UID."""

  async def liquidation_paged(
    self,
    type: Literal['swap', 'place_order'] | None = None,
    *,
    symbol: str | None = None,
    from_coin: str | None = None,
    to_coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[CrossMarginLiquidationOrders]:
    """Query individual forced (liquidation) orders and coin swaps under cross margin. Distinct from `cross.liquidation_history`, which reports whole liquidation episodes rather than the individual orders/swaps within one. Rate limit: 10 requests/second per UID.

    Paged variant of `liquidation`: Passes each page's token back as `idLessThan` and stops on the first empty page, or after `max_pages` pages when one is given.

    Args:
      type: Liquidation record type.
      symbol: Trading pair.
      from_coin: Source coin.
      to_coin: Target coin.
      start_time: Start time.
      end_time: End time.
      limit: Number of results to return. Default 100, maximum 500.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-margin-cross-trade/classic-margin-cross-trade#get-cross-liquidation-orders)
    """
    id_less_than: str | None = None
    pages = 0
    while True:
      response = await self.liquidation(
        type,
        symbol=symbol,
        from_coin=from_coin,
        to_coin=to_coin,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        id_less_than=id_less_than,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      rows = response.get('resultList') if response is not None else None
      if not rows:
        break
      id_less_than = response.get('idLessThan') if response is not None else None

  async def liquidation(
    self,
    type: Literal['swap', 'place_order'] | None = None,
    *,
    symbol: str | None = None,
    from_coin: str | None = None,
    to_coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    id_less_than: str | None = None,
    validate: bool | None = None,
  ) -> CrossMarginLiquidationOrders:
    """Query individual forced (liquidation) orders and coin swaps under cross margin. Distinct from `cross.liquidation_history`, which reports whole liquidation episodes rather than the individual orders/swaps within one. Rate limit: 10 requests/second per UID.

    Args:
      type: Liquidation record type.
      symbol: Trading pair.
      from_coin: Source coin.
      to_coin: Target coin.
      start_time: Start time.
      end_time: End time.
      limit: Number of results to return. Default 100, maximum 500.
      id_less_than: Pagination cursor.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-margin-cross-trade/classic-margin-cross-trade#get-cross-liquidation-orders)
    """
    request: Request = Request()
    if type is not None:
      request['type'] = type
    if symbol is not None:
      request['symbol'] = symbol
    if from_coin is not None:
      request['fromCoin'] = from_coin
    if to_coin is not None:
      request['toCoin'] = to_coin
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    if id_less_than is not None:
      request['idLessThan'] = id_less_than
    return await self.request(
      request,
      method='GET',
      path='/api/v2/margin/crossed/liquidation-order',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=CrossMarginLiquidationOrders,
    )
