# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.margin.cross.account.risk_rate` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CrossMarginRiskRate(TypedDict):
  """Cross margin account risk ratio."""

  riskRateRatio: NotRequired[str]
  """Risk ratio: total assets over total liabilities under cross margin."""


class RiskRate(RpcEndpoint):
  """`classic.margin.cross.account.risk_rate`."""

  async def risk_rate(self, *, validate: bool | None = None) -> CrossMarginRiskRate:
    """Query the cross margin account risk ratio (total assets / total liabilities). Rate limit: 10 requests/second per UID.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/margin/cross/account/Get-Cross-Risk-Rate)
    """
    return await self.authed_request(
      'GET',
      '/api/v2/margin/crossed/account/risk-rate',
      validator=validator(CrossMarginRiskRate),
      validate=validate,
    )
