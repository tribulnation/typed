# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.margin.cross.order.cancel` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CrossMarginCancelOrderRequest(TypedDict):
  """Margin order cancellation request. Either `orderId` or `clientOid` must be given."""

  symbol: str
  """Trading pair."""
  orderId: NotRequired[str]
  """Order id. Either this or `clientOid` is required."""
  clientOid: NotRequired[str]
  """Caller-supplied client order id. Either this or `orderId` is required."""


class CrossMarginCancelOrderResult(TypedDict):
  """Result of a margin order cancellation."""

  orderId: NotRequired[str]
  """Order id."""
  clientOid: NotRequired[str]
  """Caller-supplied client order id."""


class Cancel(RpcEndpoint):
  """`classic.margin.cross.order.cancel`."""

  async def cancel(
    self,
    cross_margin_cancel_order_request: CrossMarginCancelOrderRequest,
    *,
    validate: bool | None = None,
  ) -> CrossMarginCancelOrderResult:
    """Cancel one open cross margin order. Rate limit: 10 requests/second per UID.

    Args:
      cross_margin_cancel_order_request: Cancel one open cross margin order.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/margin/cross/trade/Cross-Cancel-Order)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/margin/crossed/cancel-order',
      json=cross_margin_cancel_order_request,
      validator=validator(CrossMarginCancelOrderResult),
      validate=validate,
    )
