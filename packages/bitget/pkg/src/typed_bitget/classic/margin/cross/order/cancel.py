# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CrossMarginCancelOrderResult(TypedDict):
  """Result of a margin order cancellation."""

  orderId: NotRequired[str]
  """Order id."""
  clientOid: NotRequired[str]
  """Caller-supplied client order id."""


class Request(TypedDict):
  """Margin order cancellation request. Either `orderId` or `clientOid` must be given."""

  symbol: str
  """Trading pair."""
  orderId: NotRequired[str]
  """Order id. Either this or `clientOid` is required."""
  clientOid: NotRequired[str]
  """Caller-supplied client order id. Either this or `orderId` is required."""


class Cancel(RpcEndpoint):
  """Cancel one open cross margin order. Rate limit: 10 requests/second per UID."""

  async def cancel(
    self,
    *,
    symbol: str,
    order_id: str | None = None,
    client_oid: str | None = None,
    validate: bool | None = None,
  ) -> CrossMarginCancelOrderResult:
    """Cancel one open cross margin order. Rate limit: 10 requests/second per UID.

    Args:
      symbol: Trading pair.
      order_id: Order id. Either this or `clientOid` is required.
      client_oid: Caller-supplied client order id. Either this or `orderId` is required.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-margin-cross-trade/classic-margin-cross-trade#cross-cancel-order)
    """
    request: Request = Request(symbol=symbol)
    if order_id is not None:
      request['orderId'] = order_id
    if client_oid is not None:
      request['clientOid'] = client_oid
    return await self.request(
      request,
      method='POST',
      path='/api/v2/margin/crossed/cancel-order',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=CrossMarginCancelOrderResult,
    )
