# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CrossMarginVipTierBase(TypedDict):
  """VIP-tier interest rate and borrow limit."""

  level: int
  """VIP tier level."""
  dailyInterestRate: Decimal
  """Daily interest rate at this tier."""
  annualInterestRate: Decimal
  """Annual interest rate at this tier."""
  limit: Decimal
  """Maximum borrowable amount at this tier."""
  discountRate: Decimal
  """Multiplier applied to the standard rate. `1` = no discount, `0.97` = 3% off."""


class Request(TypedDict):
  coin: str
  """Coin."""


class CrossMarginInterestRateAndLimit(TypedDict):
  """Interest rate and borrow limit for one coin under cross margin."""

  coin: str
  """Coin."""
  leverage: Decimal
  """Effective leverage. Default 3, tiers of 3, 5 or 10."""
  transferable: bool
  """Whether this coin is currently transferable."""
  borrowable: bool
  """Whether this coin is currently borrowable."""
  dailyInterestRate: Decimal
  """Non-VIP daily interest rate."""
  annualInterestRate: Decimal
  """Non-VIP annual interest rate."""
  maxBorrowableAmount: Decimal
  """Maximum borrowable amount at the non-VIP rate."""
  vipList: list[CrossMarginVipTierBase]
  """VIP-tier interest rates and limits, richest tier first."""


Response = list[CrossMarginInterestRateAndLimit]


class InterestRateAndLimit(RpcEndpoint):
  """Query cross margin interest rates and borrow limits, including VIP tiers, for one coin. Rate limit: 10 requests/second per UID."""

  async def interest_rate_and_limit(
    self,
    coin: str,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Query cross margin interest rates and borrow limits, including VIP tiers, for one coin. Rate limit: 10 requests/second per UID.

    Args:
      coin: Coin.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-margin-cross-account/classic-margin-cross-account#get-cross-interest-rate-and-max-borrowable)
    """
    return await self.request(
      Request(coin=coin),
      method='GET',
      path='/api/v2/margin/crossed/interest-rate-and-limit',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
