# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.margin.cross.account.flash_repay_status` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CrossMarginFlashRepayStatusEntry(TypedDict):
  """Flash-repay outcome for one repayment id."""

  repayId: NotRequired[str]
  """Repayment id."""
  status: NotRequired[str]
  """Repayment outcome."""


class CrossMarginFlashRepayStatusRequest(TypedDict):
  """Flash-repay status lookup."""

  idList: list[str]
  """Repayment ids to look up, up to 100."""


class FlashRepayStatus(RpcEndpoint):
  """`classic.margin.cross.account.flash_repay_status`."""

  async def flash_repay_status(
    self,
    cross_margin_flash_repay_status_request: CrossMarginFlashRepayStatusRequest,
    *,
    validate: bool | None = None,
  ) -> list[CrossMarginFlashRepayStatusEntry]:
    """Query the outcome of one or more previously-submitted cross margin flash repayments. Rate limit: 10 requests/second per UID.

    Args:
      cross_margin_flash_repay_status_request: Query the outcome of one or more previously-submitted cross margin flash repayments.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/margin/cross/account/Query-Cross-Flash-Repay-Status)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/margin/crossed/account/query-flash-repay-status',
      json=cross_margin_flash_repay_status_request,
      validator=validator(list[CrossMarginFlashRepayStatusEntry]),
      validate=validate,
    )
