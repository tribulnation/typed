# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CrossMarginFlashRepayStatusEntry(TypedDict):
  """Flash-repay outcome for one repayment id."""

  repayId: NotRequired[str]
  """Repayment id."""
  status: NotRequired[str]
  """Repayment outcome."""


class Request(TypedDict):
  """Flash-repay status lookup."""

  idList: list[str]
  """Repayment ids to look up, up to 100."""


Response = list[CrossMarginFlashRepayStatusEntry]


class FlashRepayStatus(RpcEndpoint):
  """Query the outcome of one or more previously-submitted cross margin flash repayments. Rate limit: 10 requests/second per UID."""

  async def flash_repay_status(
    self,
    id_list: list[str],
    *,
    validate: bool | None = None,
  ) -> Response:
    """Query the outcome of one or more previously-submitted cross margin flash repayments. Rate limit: 10 requests/second per UID.

    Args:
      id_list: Repayment ids to look up, up to 100.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-margin-cross-account/classic-margin-cross-account#get-cross-flash-repay-result)
    """
    return await self.request(
      Request(idList=id_list),
      method='POST',
      path='/api/v2/margin/crossed/account/query-flash-repay-status',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
