# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class BotAsset(TypedDict):
  """One coin's bot-account balance."""

  coin: str
  """Coin symbol."""
  available: str
  """Available balance."""
  equity: str
  """Total account equity for this coin."""
  bonus: str
  """Trading bonuses."""
  frozen: str
  """Amount currently locked in open orders."""
  usdtValue: str
  """USDT-denominated value."""


class Request(TypedDict):
  accountType: NotRequired[Literal['futures', 'spot']]
  """Restrict to one bot account type."""


Response = list[BotAsset]


class BotAssets(RpcEndpoint):
  """Get the caller's trading-bot account assets."""

  async def bot_assets(
    self,
    account_type: Literal['futures', 'spot'] | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Get the caller's trading-bot account assets.

    Args:
      account_type: Restrict to one bot account type.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-common-account/classic-common-account#bot-account)
    """
    request: Request = Request()
    if account_type is not None:
      request['accountType'] = account_type
    return await self.request(
      request,
      method='GET',
      path='/api/v2/account/bot-assets',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
