# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class ServerTime(TypedDict):
  """Bitget's current server time."""

  serverTime: TimestampMillis
  """Server time, Unix millisecond timestamp."""


class Time(RpcEndpoint):
  """Get Bitget's current server time, as a Unix millisecond timestamp. Useful for clients synchronizing their local clock with the exchange server."""

  async def time(self, *, validate: bool | None = None) -> ServerTime:
    """Get Bitget's current server time, as a Unix millisecond timestamp. Useful for clients synchronizing their local clock with the exchange server.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-common-public/classic-common#get-server-time)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v2/public/time',
      meta={},
      validate=validate,
      response_type=ServerTime,
    )
