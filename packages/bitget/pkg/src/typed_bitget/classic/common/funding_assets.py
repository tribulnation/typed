# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class FundingAsset(TypedDict):
  """One coin's funding-account balance."""

  coin: str
  """Coin symbol."""
  available: Decimal
  """Available balance."""
  frozen: Decimal
  """Amount currently frozen."""
  usdtValue: Decimal
  """USDT-denominated value."""


class Request(TypedDict):
  coin: NotRequired[str]
  """Restrict to one coin. Defaults to all coins."""


Response = list[FundingAsset]


class FundingAssets(RpcEndpoint):
  """Get the caller's funding-account assets."""

  async def funding_assets(
    self,
    coin: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Get the caller's funding-account assets.

    Args:
      coin: Restrict to one coin. Defaults to all coins.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-common-account/classic-common-account#funding-assets)
    """
    request: Request = Request()
    if coin is not None:
      request['coin'] = coin
    return await self.request(
      request,
      method='GET',
      path='/api/v2/account/funding-assets',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
