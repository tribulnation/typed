# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.copytrading.spot_follower_update_tpsl` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class UpdateSpotFollowerTpslRequest(TypedDict):
  """Take-profit/stop-loss update for one tracking order."""

  trackingNo: str
  """Tracking order ID to modify."""
  stopSurplusPrice: NotRequired[str]
  """Take-profit price. Empty leaves unchanged, `"0"` cancels the existing take-profit, any other value sets/updates it. Either this or stopLossPrice must be provided."""
  stopLossPrice: NotRequired[str]
  """Stop-loss price. Empty leaves unchanged, `"0"` cancels the existing stop-loss, any other value sets/updates it. Either this or stopSurplusPrice must be provided."""


class SpotFollowerUpdateTpsl(RpcEndpoint):
  """`classic.copytrading.spot_follower_update_tpsl`."""

  async def spot_follower_update_tpsl(
    self,
    update_spot_follower_tpsl_request: UpdateSpotFollowerTpslRequest,
    *,
    validate: bool | None = None,
  ) -> str:
    """Set or clear the take-profit/stop-loss prices on one of a follower's open copy positions, spot.

    Args:
      update_spot_follower_tpsl_request: Take-profit/stop-loss update for one tracking order.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/copytrading/spot-copytrade/follower/Setting-Tpsl)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/copy/spot-follower/setting-tpsl',
      json=update_spot_follower_tpsl_request,
      validator=validator(str),
      validate=validate,
    )
