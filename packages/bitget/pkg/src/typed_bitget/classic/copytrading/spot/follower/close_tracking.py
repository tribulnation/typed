# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.copytrading.spot.follower.close_tracking` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CloseSpotFollowerTrackingOrdersRequest(TypedDict):
  """Positions to close."""

  trackingNoList: list[str]
  """Tracking order IDs to close, up to 50."""
  symbol: str
  """Trading pair the tracking orders belong to, e.g. "ETHUSDT"."""


class CloseTracking(RpcEndpoint):
  """`classic.copytrading.spot.follower.close_tracking`."""

  async def close_tracking(
    self,
    close_spot_follower_tracking_orders_request: CloseSpotFollowerTrackingOrdersRequest,
    *,
    validate: bool | None = None,
  ) -> str:
    """Close (sell) one or more of a follower's open copy positions on a symbol, spot. All-or-nothing: either every listed position closes, or none do.

    Args:
      close_spot_follower_tracking_orders_request: Positions to close.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/copytrading/spot-copytrade/follower/Order-Close-Tracking)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/copy/spot-follower/order-close-tracking',
      json=close_spot_follower_tracking_orders_request,
      validator=validator(str),
      validate=validate,
    )
