# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  symbol: NotRequired[str]
  """Trading pair, e.g. "BTCUSDT". Case-insensitive."""
  traderId: NotRequired[str]
  """Trader user ID."""
  idLessThan: NotRequired[str]
  """Pagination cursor: return records older than this tracking/profit ID. Pass the previous page's `endId` to walk further back."""
  idGreaterThan: NotRequired[str]
  """Pagination cursor: return records newer than this tracking/profit ID."""
  startTime: NotRequired[TimestampMillis]
  """Start of the queried range, as a millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """End of the queried range, as a millisecond timestamp."""
  limit: NotRequired[int]
  """Results per page. Default 20, maximum 50."""


class SpotFollowerCurrentOrder(TypedDict):
  """One currently-tracked (open) copy position."""

  trackingNo: str
  """Tracking order ID."""
  traderId: str
  """Copied trader's user ID."""
  buyFillSize: str
  """Quantity filled so far."""
  buyDelegateSize: str
  """Quantity still pending fill."""
  buyPrice: str
  """Average entry (buy) price."""
  unrealizedPL: str
  """Current unrealized profit/loss."""
  buyTime: TimestampMillis
  """Entry time, as a millisecond epoch string."""
  buyFee: str
  """Entry fee."""
  unrealizedPLR: str
  """Current unrealized return, as a percentage string."""
  symbol: str
  """Trading pair."""
  stopSurplusPrice: str | None
  """Take-profit trigger price for this tracked order, if set."""
  stopLossPrice: str | None
  """Stop-loss trigger price for this tracked order, if set."""


class SpotFollowerCurrentOrders(TypedDict):
  """Page of currently-tracked positions."""

  endId: str | None
  """Last tracking order ID in this page, or `null` when `trackingList` is empty. Confirmed live."""
  trackingList: list[SpotFollowerCurrentOrder]
  """Currently-tracked positions."""


class CurrentOrders(RpcEndpoint):
  """List a follower's currently-tracked (open) copy positions, spot."""

  def current_orders_paged(
    self,
    *,
    symbol: str | None = None,
    trader_id: str | None = None,
    id_greater_than: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[SpotFollowerCurrentOrder, str]:
    """List a follower's currently-tracked (open) copy positions, spot.

    Paged variant of `current_orders`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      symbol: Trading pair, e.g. "BTCUSDT". Case-insensitive.
      trader_id: Trader user ID.
      id_greater_than: Pagination cursor: return records newer than this tracking/profit ID.
      start_time: Start of the queried range, as a millisecond timestamp.
      end_time: End of the queried range, as a millisecond timestamp.
      limit: Results per page. Default 20, maximum 50.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-spot-follower/classic-copytrading-spot-copytrade-follower#get-current-copy-trade-orders)
    """

    async def next(
      id_less_than: str,
    ) -> tuple[list[SpotFollowerCurrentOrder], str | None]:
      response = await self.current_orders(
        symbol=symbol,
        trader_id=trader_id,
        id_greater_than=id_greater_than,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        id_less_than=(id_less_than or None),
        validate=validate,
      )
      rows = response.get('trackingList')
      state = response.get('endId')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def current_orders(
    self,
    *,
    symbol: str | None = None,
    trader_id: str | None = None,
    id_less_than: str | None = None,
    id_greater_than: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> SpotFollowerCurrentOrders:
    """List a follower's currently-tracked (open) copy positions, spot.

    Args:
      symbol: Trading pair, e.g. "BTCUSDT". Case-insensitive.
      trader_id: Trader user ID.
      id_less_than: Pagination cursor: return records older than this tracking/profit ID. Pass the previous page's `endId` to walk further back.
      id_greater_than: Pagination cursor: return records newer than this tracking/profit ID.
      start_time: Start of the queried range, as a millisecond timestamp.
      end_time: End of the queried range, as a millisecond timestamp.
      limit: Results per page. Default 20, maximum 50.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-spot-follower/classic-copytrading-spot-copytrade-follower#get-current-copy-trade-orders)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    if trader_id is not None:
      request['traderId'] = trader_id
    if id_less_than is not None:
      request['idLessThan'] = id_less_than
    if id_greater_than is not None:
      request['idGreaterThan'] = id_greater_than
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/api/v2/copy/spot-follower/query-current-orders',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SpotFollowerCurrentOrders,
    )
