# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  traderId: str
  """Trader user ID."""


class SpotFollowerTradeSetting(TypedDict):
  """One symbol's copy-trading settings."""

  maxTraceAmount: Decimal
  """Maximum amount allocated to this symbol."""
  stopLossRation: str
  """Stop-loss ratio, as a percentage string."""
  stopSurplusRation: str
  """Take-profit ratio, as a percentage string."""
  symbol: str
  """Trading pair."""
  traceType: Literal['percent', 'amount', 'count']
  """How position size is derived from the trader's."""


class SpotFollowerTradeSymbolSetting(TypedDict):
  """One symbol's copy-trading limits."""

  maxStopLossRation: Decimal
  """Maximum stop-loss ratio allowed, as a percentage string."""
  maxStopSurplusRation: Decimal
  """Maximum take-profit ratio allowed, as a percentage string."""
  maxTraceAmount: Decimal
  """Maximum amount allowed."""
  maxTraceAmountSystem: Decimal
  """System-imposed maximum amount."""
  maxTraceSize: Decimal
  """Maximum position size allowed."""
  maxTraceRation: Decimal
  """Maximum trace ratio allowed."""
  minStopLossRation: Decimal
  """Minimum stop-loss ratio allowed, as a percentage string."""
  minStopSurplusRation: Decimal
  """Minimum take-profit ratio allowed, as a percentage string."""
  minTraceAmount: Decimal
  """Minimum amount allowed."""
  minTraceSize: Decimal
  """Minimum position size allowed."""
  minTraceRation: Decimal
  """Minimum trace ratio allowed."""
  sliderMaxStopLossRatio: Decimal
  """Maximum stop-loss ratio selectable in the UI slider."""
  sliderMaxStopSurplusRatio: Decimal
  """Maximum take-profit ratio selectable in the UI slider."""
  symbol: str
  """Trading pair."""


class SpotFollowerConfiguration(TypedDict):
  """A follower's copy-trading configuration for one trader."""

  enable: Literal['YES', 'NO']
  """Whether following this trader is currently active."""
  profitRate: Decimal
  """Current profit rate, as a percentage string."""
  settledInDays: int
  """Days since the trader relationship was established."""
  tradeSettingList: list[SpotFollowerTradeSetting]
  """Per-symbol copy-trading settings."""
  tradeSymbolSettingList: list[SpotFollowerTradeSymbolSetting]
  """Per-symbol copy-trading limits."""
  traderHeadPic: str | None
  """URL of the trader's profile picture."""
  traderName: str
  """Trader's display name."""


class Settings(RpcEndpoint):
  """Get a follower's copy-trading configuration for a specific trader, spot: enablement, profit rate, and per-symbol trade settings/limits."""

  async def settings(
    self,
    trader_id: str,
    *,
    validate: bool | None = None,
  ) -> SpotFollowerConfiguration:
    """Get a follower's copy-trading configuration for a specific trader, spot: enablement, profit rate, and per-symbol trade settings/limits.

    Args:
      trader_id: Trader user ID.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-spot-follower/classic-copytrading-spot-copytrade-follower#get-follow-configuration)
    """
    return await self.request(
      Request(traderId=trader_id),
      method='GET',
      path='/api/v2/copy/spot-follower/query-settings',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SpotFollowerConfiguration,
    )
