# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  traderId: str
  """Trader user ID."""


class SpotFollowerTraderSymbols(TypedDict):
  """A trader's current trading pairs."""

  currentTradingList: list[str]
  """Trading pairs the trader currently trades."""


class TraderSymbols(RpcEndpoint):
  """List the trading pairs a followed trader is currently trading, spot."""

  async def trader_symbols(
    self,
    trader_id: str,
    *,
    validate: bool | None = None,
  ) -> SpotFollowerTraderSymbols:
    """List the trading pairs a followed trader is currently trading, spot.

    Args:
      trader_id: Trader user ID.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-spot-follower/classic-copytrading-spot-copytrade-follower#get-traders-current-trading-pair)
    """
    return await self.request(
      Request(traderId=trader_id),
      method='GET',
      path='/api/v2/copy/spot-follower/query-trader-symbols',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SpotFollowerTraderSymbols,
    )
