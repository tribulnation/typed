# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Take-profit/stop-loss update for one tracking order."""

  trackingNo: str
  """Tracking order ID to modify."""
  stopSurplusPrice: NotRequired[str]
  """Take-profit price. Empty leaves unchanged, `"0"` cancels the existing take-profit, any other value sets/updates it. Either this or stopLossPrice must be provided."""
  stopLossPrice: NotRequired[str]
  """Stop-loss price. Empty leaves unchanged, `"0"` cancels the existing stop-loss, any other value sets/updates it. Either this or stopSurplusPrice must be provided."""


Response = str


class ModifyOrderTpsl(RpcEndpoint):
  """Set or clear the take-profit/stop-loss prices on one of a trader's open copy positions, spot."""

  async def modify_order_tpsl(
    self,
    *,
    tracking_no: str,
    stop_surplus_price: str | None = None,
    stop_loss_price: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Set or clear the take-profit/stop-loss prices on one of a trader's open copy positions, spot.

    Args:
      tracking_no: Tracking order ID to modify.
      stop_surplus_price: Take-profit price. Empty leaves unchanged, `"0"` cancels the existing take-profit, any other value sets/updates it. Either this or stopLossPrice must be provided.
      stop_loss_price: Stop-loss price. Empty leaves unchanged, `"0"` cancels the existing stop-loss, any other value sets/updates it. Either this or stopSurplusPrice must be provided.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-spot-trader/classic-copytrading-spot-copytrade-trader#modify-take-profit-and-stop-loss)
    """
    request: Request = Request(trackingNo=tracking_no)
    if stop_surplus_price is not None:
      request['stopSurplusPrice'] = stop_surplus_price
    if stop_loss_price is not None:
      request['stopLossPrice'] = stop_loss_price
    return await self.request(
      request,
      method='POST',
      path='/api/v2/copy/spot-trader/order-modify-tpsl',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
