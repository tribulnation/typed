# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


Response = str


class SpotFollowerCopyTradeSetting(TypedDict):
  """Per-symbol copy-trading configuration, spot."""

  symbol: str
  """Trading pair."""
  traceType: Literal['percent', 'amount', 'count']
  """How position size is derived from the trader's."""
  maxHoldSize: str
  """Maximum following buy quantity."""
  traceValue: str
  """Position-sizing value corresponding to traceType."""
  stopLossRatio: NotRequired[str]
  """Stop-loss ratio, positive integer percent, e.g. "10" for 10%."""
  stopSurplusRatio: NotRequired[str]
  """Take-profit ratio, positive integer percent, e.g. "10" for 10%."""


class Request(TypedDict):
  """Copy-trading settings to apply for one trader."""

  traderId: str
  """Trader user ID to configure settings for."""
  autoCopy: NotRequired[Literal['on', 'off']]
  """Whether to auto-follow new symbols the trader opens."""
  mode: NotRequired[Literal['basic', 'advanced']]
  """Follow mode."""
  settings: list[SpotFollowerCopyTradeSetting]
  """Per-symbol settings."""


class UpdateSettings(RpcEndpoint):
  """Configure a follower's copy-trading settings for a specific trader, spot."""

  async def update_settings(
    self,
    trader_id: str,
    *,
    auto_copy: Literal['on', 'off'] | None = None,
    mode: Literal['basic', 'advanced'] | None = None,
    settings: list[SpotFollowerCopyTradeSetting],
    validate: bool | None = None,
  ) -> Response:
    """Configure a follower's copy-trading settings for a specific trader, spot.

    Args:
      trader_id: Trader user ID to configure settings for.
      auto_copy: Whether to auto-follow new symbols the trader opens.
      mode: Follow mode.
      settings: Per-symbol settings.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-spot-follower/classic-copytrading-spot-copytrade-follower#add-or-modify-following-configurations)
    """
    request: Request = Request(traderId=trader_id, settings=settings)
    if auto_copy is not None:
      request['autoCopy'] = auto_copy
    if mode is not None:
      request['mode'] = mode
    return await self.request(
      request,
      method='POST',
      path='/api/v2/copy/spot-follower/settings',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
