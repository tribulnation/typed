# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  idLessThan: NotRequired[str]
  """Pagination cursor: return records older than this tracking/profit ID. Pass the previous page's `endId` to walk further back."""
  idGreaterThan: NotRequired[str]
  """Pagination cursor: return records newer than this tracking/profit ID."""
  startTime: NotRequired[TimestampMillis]
  """Start of the queried range, as a millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """End of the queried range, as a millisecond timestamp."""
  limit: NotRequired[int]
  """Results per page. Default 100, maximum 100."""
  coin: NotRequired[str]
  """Settlement coin to filter by."""


class SpotTraderProfitShareItem(TypedDict):
  """One profit-share distribution."""

  profitId: str
  """Distribution ID."""
  coin: str
  """Settlement coin."""
  distributeRatio: str
  """Share ratio, as a percentage string, e.g. "8" for 8%."""
  profit: str
  """Amount distributed."""
  followerName: str
  """Follower's display name."""
  profitTime: TimestampMillis
  """Distribution time, as a millisecond epoch string."""


class SpotTraderProfitHistory(TypedDict):
  """Page of historical profit-share distributions."""

  endId: str
  """Last distribution ID in this page; pass as `idLessThan` to walk older, `idGreaterThan` to walk newer."""
  profitList: list[SpotTraderProfitShareItem]
  """Profit-share distributions."""


class ProfitHistory(RpcEndpoint):
  """List a trader's individual historical profit-share distributions, spot."""

  def profit_history_paged(
    self,
    *,
    id_greater_than: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    coin: str | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[SpotTraderProfitShareItem, str]:
    """List a trader's individual historical profit-share distributions, spot.

    Paged variant of `profit_history`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      id_greater_than: Pagination cursor: return records newer than this tracking/profit ID.
      start_time: Start of the queried range, as a millisecond timestamp.
      end_time: End of the queried range, as a millisecond timestamp.
      limit: Results per page. Default 100, maximum 100.
      coin: Settlement coin to filter by.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-spot-trader/classic-copytrading-spot-copytrade-trader#get-history-profit-sharing-details)
    """

    async def next(
      id_less_than: str,
    ) -> tuple[list[SpotTraderProfitShareItem], str | None]:
      response = await self.profit_history(
        id_greater_than=id_greater_than,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        coin=coin,
        id_less_than=(id_less_than or None),
        validate=validate,
      )
      rows = response.get('profitList')
      state = response.get('endId')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def profit_history(
    self,
    *,
    id_less_than: str | None = None,
    id_greater_than: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    coin: str | None = None,
    validate: bool | None = None,
  ) -> SpotTraderProfitHistory:
    """List a trader's individual historical profit-share distributions, spot.

    Args:
      id_less_than: Pagination cursor: return records older than this tracking/profit ID. Pass the previous page's `endId` to walk further back.
      id_greater_than: Pagination cursor: return records newer than this tracking/profit ID.
      start_time: Start of the queried range, as a millisecond timestamp.
      end_time: End of the queried range, as a millisecond timestamp.
      limit: Results per page. Default 100, maximum 100.
      coin: Settlement coin to filter by.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-spot-trader/classic-copytrading-spot-copytrade-trader#get-history-profit-sharing-details)
    """
    request: Request = Request()
    if id_less_than is not None:
      request['idLessThan'] = id_less_than
    if id_greater_than is not None:
      request['idGreaterThan'] = id_greater_than
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    if coin is not None:
      request['coin'] = coin
    return await self.request(
      request,
      method='GET',
      path='/api/v2/copy/spot-trader/profit-history-details',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SpotTraderProfitHistory,
    )
