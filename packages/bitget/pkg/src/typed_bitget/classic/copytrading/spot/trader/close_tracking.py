# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Positions to close."""

  trackingNoList: list[str]
  """Tracking order IDs to close, up to 50."""
  symbol: str
  """Trading pair the tracking orders belong to."""


Response = str


class CloseTracking(RpcEndpoint):
  """Close (sell) one or more of a trader's open copy positions on a symbol, spot. All-or-nothing: either every listed position closes, or none do."""

  async def close_tracking(
    self,
    tracking_no_list: list[str],
    *,
    symbol: str,
    validate: bool | None = None,
  ) -> Response:
    """Close (sell) one or more of a trader's open copy positions on a symbol, spot. All-or-nothing: either every listed position closes, or none do.

    Args:
      tracking_no_list: Tracking order IDs to close, up to 50.
      symbol: Trading pair the tracking orders belong to.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-spot-trader/classic-copytrading-spot-copytrade-trader#sell-and-sell-in-batch)
    """
    return await self.request(
      Request(trackingNoList=tracking_no_list, symbol=symbol),
      method='POST',
      path='/api/v2/copy/spot-trader/order-close-tracking',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
