# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.copytrading.spot.trader.close_tracking` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CloseSpotTrackingOrdersRequest(TypedDict):
  """Positions to close."""

  trackingNoList: list[str]
  """Tracking order IDs to close, up to 50."""
  symbol: str
  """Trading pair the tracking orders belong to."""


class CloseTracking(RpcEndpoint):
  """`classic.copytrading.spot.trader.close_tracking`."""

  async def close_tracking(
    self,
    close_spot_tracking_orders_request: CloseSpotTrackingOrdersRequest,
    *,
    validate: bool | None = None,
  ) -> str:
    """Close (sell) one or more of a trader's open copy positions on a symbol, spot. All-or-nothing: either every listed position closes, or none do.

    Args:
      close_spot_tracking_orders_request: Positions to close.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/copytrading/spot-copytrade/trader/Order-Close-Tracking)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/copy/spot-trader/order-close-tracking',
      json=close_spot_tracking_orders_request,
      validator=validator(str),
      validate=validate,
    )
