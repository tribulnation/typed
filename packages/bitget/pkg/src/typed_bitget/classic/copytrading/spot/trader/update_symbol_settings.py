# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Symbols to add or remove."""

  symbolList: list[str]
  """Trading pairs, up to 50."""
  settingType: Literal['add', 'delete']
  """Whether to add or remove the listed symbols."""


Response = str


class UpdateSymbolSettings(RpcEndpoint):
  """Add or remove which symbols a trader offers for copy trading, spot."""

  async def update_symbol_settings(
    self,
    symbol_list: list[str],
    *,
    setting_type: Literal['add', 'delete'],
    validate: bool | None = None,
  ) -> Response:
    """Add or remove which symbols a trader offers for copy trading, spot.

    Args:
      symbol_list: Trading pairs, up to 50.
      setting_type: Whether to add or remove the listed symbols.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-spot-trader/classic-copytrading-spot-copytrade-trader#set-copytrade-symbols)
    """
    return await self.request(
      Request(symbolList=symbol_list, settingType=setting_type),
      method='POST',
      path='/api/v2/copy/spot-trader/config-setting-symbols',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
