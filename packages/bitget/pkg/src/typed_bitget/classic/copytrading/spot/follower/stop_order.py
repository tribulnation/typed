# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.copytrading.spot.follower.stop_order` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class StopSpotFollowerOrderRequest(TypedDict):
  """Orders to cancel."""

  trackingNoList: list[str]
  """Tracking order IDs to cancel, up to 50."""


class StopOrder(RpcEndpoint):
  """`classic.copytrading.spot.follower.stop_order`."""

  async def stop_order(
    self,
    stop_spot_follower_order_request: StopSpotFollowerOrderRequest,
    *,
    validate: bool | None = None,
  ) -> str:
    """Cancel one or more of a follower's pending copy orders, spot. All-or-nothing: either every listed order cancels, or none do.

    Args:
      stop_spot_follower_order_request: Orders to cancel.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/copytrading/spot-copytrade/follower/Stop-Order)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/copy/spot-follower/stop-order',
      json=stop_spot_follower_order_request,
      validator=validator(str),
      validate=validate,
    )
