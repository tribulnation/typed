# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Orders to cancel."""

  trackingNoList: list[str]
  """Tracking order IDs to cancel, up to 50."""


Response = str


class StopOrder(RpcEndpoint):
  """Cancel one or more of a follower's pending copy orders, spot. All-or-nothing: either every listed order cancels, or none do."""

  async def stop_order(
    self,
    tracking_no_list: list[str],
    *,
    validate: bool | None = None,
  ) -> Response:
    """Cancel one or more of a follower's pending copy orders, spot. All-or-nothing: either every listed order cancels, or none do.

    Args:
      tracking_no_list: Tracking order IDs to cancel, up to 50.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-spot-follower/classic-copytrading-spot-copytrade-follower#stop-the-order)
    """
    return await self.request(
      Request(trackingNoList=tracking_no_list),
      method='POST',
      path='/api/v2/copy/spot-follower/stop-order',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
