# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.copytrading.follower_follow_limit` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class FuturesFollowLimit(TypedDict):
  """Position size limit for one symbol."""

  maxFollowSize: str
  """Maximum position size, in the trading currency."""
  minFollowSize: str
  """Minimum position size, in the trading currency."""
  symbol: str
  """Trading pair."""


class FollowerFollowLimit(RpcEndpoint):
  """`classic.copytrading.follower_follow_limit`."""

  async def follower_follow_limit(
    self,
    *,
    symbol: str | None = None,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    validate: bool | None = None,
  ) -> list[FuturesFollowLimit]:
    """Get the minimum and maximum position size a follower may open on a symbol, futures.

    Args:
      symbol: Trading pair.
      product_type: Which futures product family this request concerns.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/copytrading/future-copytrade/follower/Query-Quantity-Limit)
    """
    params: dict = {
      'productType': product_type,
    }
    if symbol is not None:
      params['symbol'] = symbol
    return await self.authed_request(
      'GET',
      '/api/v2/copy/mix-follower/query-quantity-limit',
      params=params,
      validator=validator(list[FuturesFollowLimit]),
      validate=validate,
    )
