# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class FuturesTraderProfitShareRecord(TypedDict):
  """One profit-share record."""

  coin: str
  """Settlement coin."""
  profit: str
  """Amount distributed."""
  nickName: str
  """Follower's display name."""


class Request(TypedDict):
  coin: NotRequired[str]
  """Settlement coin to filter by."""
  pageSize: NotRequired[int]
  """Results per page. Default 20, maximum 100."""
  pageNo: NotRequired[int]
  """Page number. Defaults to 1."""


Response = list[FuturesTraderProfitShareRecord]


class ProfitShare(RpcEndpoint):
  """List a trader's profit-share records, futures, paginated by page number."""

  async def profit_share_paged(
    self,
    coin: str | None = None,
    *,
    page_size: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[Response]:
    """List a trader's profit-share records, futures, paginated by page number.

    Paged variant of `profit_share`: Requests `pageNo` from 1 upwards and stops on the first page shorter than `page_size`, or after `max_pages` pages when one is given.

    Args:
      coin: Settlement coin to filter by.
      page_size: Results per page. Default 20, maximum 100.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-future-trader/classic-copytrading-future-copytrade-trader#get-profit-share-detail)
    """
    page_no = 1
    pages = 0
    while True:
      response = await self.profit_share(
        coin, page_size=page_size, page_no=page_no, validate=validate
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      if not response or (page_size is not None and len(response) < page_size):
        break
      page_no += 1

  async def profit_share(
    self,
    coin: str | None = None,
    *,
    page_size: int | None = None,
    page_no: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """List a trader's profit-share records, futures, paginated by page number.

    Args:
      coin: Settlement coin to filter by.
      page_size: Results per page. Default 20, maximum 100.
      page_no: Page number. Defaults to 1.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-future-trader/classic-copytrading-future-copytrade-trader#get-profit-share-detail)
    """
    request: Request = Request()
    if coin is not None:
      request['coin'] = coin
    if page_size is not None:
      request['pageSize'] = page_size
    if page_no is not None:
      request['pageNo'] = page_no
    return await self.request(
      request,
      method='GET',
      path='/api/v2/copy/mix-trader/profit-details',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
