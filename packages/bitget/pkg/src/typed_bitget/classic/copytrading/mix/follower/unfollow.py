# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Trader to unfollow."""

  traderId: str
  """Trader user ID to unfollow."""


Response = str


class Unfollow(RpcEndpoint):
  """Stop following a trader, futures."""

  async def unfollow(self, trader_id: str, *, validate: bool | None = None) -> Response:
    """Stop following a trader, futures.

    Args:
      trader_id: Trader user ID to unfollow.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-future-follower/classic-copytrading-future-copytrade-follower#unfollow-the-trader)
    """
    return await self.request(
      Request(traderId=trader_id),
      method='POST',
      path='/api/v2/copy/mix-follower/cancel-trader',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
