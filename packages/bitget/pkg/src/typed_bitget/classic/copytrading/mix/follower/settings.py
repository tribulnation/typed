# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class FuturesFollowerSettingDetail(TypedDict):
  """One symbol's copy-trading settings."""

  symbol: str
  """Trading pair."""
  productType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Which futures product family this request concerns."""
  marginType: Literal['trader', 'specify']
  """Which margin is copied with."""
  marginCoin: str
  """Margin coin, when marginType is `specify`."""
  leverType: Literal['position', 'specify', 'trader']
  """How leverage is chosen."""
  longLeverage: str
  """Long-side leverage."""
  shortLeverage: str
  """Short-side leverage."""
  traceType: Literal['percent', 'amount', 'count']
  """How position size is derived from the trader's."""


class Request(TypedDict):
  traderId: str
  """Trader user ID to look up settings for."""


class FuturesFollowerSettings(TypedDict):
  """A follower's copy-trading settings for one trader."""

  followerEnable: Literal['YES', 'NO'] | None
  """Whether following this trader is currently active. Confirmed live: `null` when the caller is not following this trader."""
  detailList: list[FuturesFollowerSettingDetail] | None
  """Per-symbol settings. Confirmed live: `null` (not `[]`) when the caller is not following this trader."""


class Settings(RpcEndpoint):
  """Get a follower's copy-trading settings for a specific trader, futures."""

  async def settings(
    self,
    trader_id: str,
    *,
    validate: bool | None = None,
  ) -> FuturesFollowerSettings:
    """Get a follower's copy-trading settings for a specific trader, futures.

    Args:
      trader_id: Trader user ID to look up settings for.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-future-follower/classic-copytrading-future-copytrade-follower#get-copy-trade-settings)
    """
    return await self.request(
      Request(traderId=trader_id),
      method='GET',
      path='/api/v2/copy/mix-follower/query-settings',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=FuturesFollowerSettings,
    )
