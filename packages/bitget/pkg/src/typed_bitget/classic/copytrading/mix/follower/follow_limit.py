# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class FuturesFollowLimit(TypedDict):
  """Position size limit for one symbol."""

  maxFollowSize: Decimal
  """Maximum position size, in the trading currency."""
  minFollowSize: Decimal
  """Minimum position size, in the trading currency."""
  symbol: str
  """Trading pair."""


class Request(TypedDict):
  symbol: NotRequired[str]
  """Trading pair."""
  productType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Which futures product family this request concerns."""


Response = list[FuturesFollowLimit]


class FollowLimit(RpcEndpoint):
  """Get the minimum and maximum position size a follower may open on a symbol, futures."""

  async def follow_limit(
    self,
    symbol: str | None = None,
    *,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    validate: bool | None = None,
  ) -> Response:
    """Get the minimum and maximum position size a follower may open on a symbol, futures.

    Args:
      symbol: Trading pair.
      product_type: Which futures product family this request concerns.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-future-follower/classic-copytrading-future-copytrade-follower#get-follow-limit)
    """
    request: Request = Request(productType=product_type)
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='/api/v2/copy/mix-follower/query-quantity-limit',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
