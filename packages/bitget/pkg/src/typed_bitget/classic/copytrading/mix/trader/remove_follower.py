# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Follower to remove."""

  followerUid: str
  """Follower's user ID."""


Response = str


class RemoveFollower(RpcEndpoint):
  """Remove a follower from a trader's copy-trading roster, futures."""

  async def remove_follower(
    self,
    follower_uid: str,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Remove a follower from a trader's copy-trading roster, futures.

    Args:
      follower_uid: Follower's user ID.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
    """
    return await self.request(
      Request(followerUid=follower_uid),
      method='POST',
      path='/api/v2/copy/mix-trader/config-remove-follower',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
