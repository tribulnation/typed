# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class FuturesTraderSymbolSetting(TypedDict):
  """One symbol's copy-trading settings."""

  symbol: str
  """Trading pair."""
  openTrader: Literal['YES', 'NO']
  """Whether this symbol is open for followers to copy."""
  minOpenCount: str
  """Minimum position size followers may open on this symbol."""
  maxLeverage: str
  """Maximum leverage followers may use on this symbol."""
  stopSurplusRatio: str
  """Default take-profit ratio, as a percentage string."""
  stopLossRatio: str
  """Default stop-loss ratio, as a percentage string."""


class Request(TypedDict):
  productType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Which futures product family this request concerns."""


Response = list[FuturesTraderSymbolSetting]


class SymbolSettings(RpcEndpoint):
  """List a trader's per-symbol copy-trading settings, futures: which symbols are open to followers and their leverage/TP-SL ratio limits."""

  async def symbol_settings(
    self,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    *,
    validate: bool | None = None,
  ) -> Response:
    """List a trader's per-symbol copy-trading settings, futures: which symbols are open to followers and their leverage/TP-SL ratio limits.

    Args:
      product_type: Which futures product family this request concerns.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-future-trader/classic-copytrading-future-copytrade-trader#get-copy-trade-symbol-settings)
    """
    return await self.request(
      Request(productType=product_type),
      method='GET',
      path='/api/v2/copy/mix-trader/config-query-symbols',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
