# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .broker import Broker
from .follower import Follower
from .trader import Trader
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Mix(RpcEndpoint):
  """Bitget Classic's Futures Copy Trading API: trader-, follower-, and broker-side endpoints for futures copy trading.

  References:
    - [Upstream docs](https://www.bitget.com/docs/catalog/classic-copytrading-future-trader)
  """

  @cached_property
  def broker(self) -> Broker:
    """Bitget Classic's Futures Copy Trading Broker API: a broker's visibility into its affiliated traders and their pending/historical copy trades. No slug under this domain renders an official page as of this pass (grounded from a community SDK instead, see the endpoints' own `upstream.md`), so this reuses the grouping's own established trader-page link rather than inventing one.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-copytrading-future-trader)
    """
    return Broker(client=self.client)

  @cached_property
  def follower(self) -> Follower:
    """Bitget Classic's Futures Copy Trading Follower API: following/unfollowing traders, follow settings, and a follower's own current and historical copy trades, futures.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-copytrading-future-follower)
    """
    return Follower(client=self.client)

  @cached_property
  def trader(self) -> Trader:
    """Bitget Classic's Futures Copy Trading Trader API: managing followers, trade/profit-sharing settlement, and a trader's own current and historical copy trades, futures.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-copytrading-future-trader)
    """
    return Trader(client=self.client)
