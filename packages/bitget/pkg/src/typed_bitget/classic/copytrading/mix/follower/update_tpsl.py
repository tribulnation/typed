# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Take-profit/stop-loss update for one tracking order."""

  trackingNo: str
  """Tracking order ID to modify."""
  symbol: NotRequired[str]
  """Trading pair."""
  productType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Which futures product family this request concerns."""
  stopSurplusPrice: NotRequired[str]
  """Take-profit price. Empty leaves unchanged, `"0"` cancels the existing take-profit, any other value sets/updates it."""
  stopLossPrice: NotRequired[str]
  """Stop-loss price. Empty leaves unchanged, `"0"` cancels the existing stop-loss, any other value sets/updates it."""


Response = str


class UpdateTpsl(RpcEndpoint):
  """Set or clear the take-profit/stop-loss prices on one of a follower's open copy positions, futures."""

  async def update_tpsl(
    self,
    *,
    tracking_no: str,
    symbol: str | None = None,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    stop_surplus_price: str | None = None,
    stop_loss_price: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Set or clear the take-profit/stop-loss prices on one of a follower's open copy positions, futures.

    Args:
      tracking_no: Tracking order ID to modify.
      symbol: Trading pair.
      product_type: Which futures product family this request concerns.
      stop_surplus_price: Take-profit price. Empty leaves unchanged, `"0"` cancels the existing take-profit, any other value sets/updates it.
      stop_loss_price: Stop-loss price. Empty leaves unchanged, `"0"` cancels the existing stop-loss, any other value sets/updates it.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-future-follower/classic-copytrading-future-copytrade-follower#set-tpsl)
    """
    request: Request = Request(trackingNo=tracking_no, productType=product_type)
    if symbol is not None:
      request['symbol'] = symbol
    if stop_surplus_price is not None:
      request['stopSurplusPrice'] = stop_surplus_price
    if stop_loss_price is not None:
      request['stopLossPrice'] = stop_loss_price
    return await self.request(
      request,
      method='POST',
      path='/api/v2/copy/mix-follower/setting-tpsl',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
