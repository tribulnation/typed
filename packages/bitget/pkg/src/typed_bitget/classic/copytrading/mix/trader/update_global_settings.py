# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Global settings to update. At least one field must be provided."""

  enable: NotRequired[Literal['YES', 'NO']]
  """Whether elite-trading (copy-tradeable) status is active."""
  showTotalEquity: NotRequired[Literal['YES', 'NO']]
  """Whether total account equity is shown publicly."""
  showTpsl: NotRequired[Literal['YES', 'NO']]
  """Whether take-profit/stop-loss prices are shown publicly."""


Response = str


class UpdateGlobalSettings(RpcEndpoint):
  """Update a trader's global copy-trading display preferences, futures: elite-trading visibility, total-equity display, TP/SL price visibility."""

  async def update_global_settings(
    self,
    *,
    enable: Literal['YES', 'NO'] | None = None,
    show_total_equity: Literal['YES', 'NO'] | None = None,
    show_tpsl: Literal['YES', 'NO'] | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Update a trader's global copy-trading display preferences, futures: elite-trading visibility, total-equity display, TP/SL price visibility.

    Args:
      enable: Whether elite-trading (copy-tradeable) status is active.
      show_total_equity: Whether total account equity is shown publicly.
      show_tpsl: Whether take-profit/stop-loss prices are shown publicly.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-future-trader/classic-copytrading-future-copytrade-trader#change-global-copy-trade-setting)
    """
    request: Request = Request()
    if enable is not None:
      request['enable'] = enable
    if show_total_equity is not None:
      request['showTotalEquity'] = show_total_equity
    if show_tpsl is not None:
      request['showTpsl'] = show_tpsl
    return await self.request(
      request,
      method='POST',
      path='/api/v2/copy/mix-trader/config-settings-base',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
