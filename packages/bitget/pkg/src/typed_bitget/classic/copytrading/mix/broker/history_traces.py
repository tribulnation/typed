# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class FuturesBrokerHistoryTrace(TypedDict):
  """One closed copy trade."""

  trackingNo: str
  """Tracking order ID."""
  symbol: str
  """Trading pair."""
  openOrderId: str
  """Opening order ID."""
  closeOrderId: str
  """Closing order ID."""
  posSide: Literal['long', 'short']
  """Position direction."""
  openLeverage: Decimal
  """Leverage applied when opening."""
  openPriceAvg: Decimal
  """Average entry price."""
  openTime: TimestampMillis
  """Position opening time, as a millisecond epoch string."""
  openSize: Decimal
  """Position quantity opened."""
  closeSize: Decimal
  """Position quantity closed."""
  closeTime: TimestampMillis
  """Position closing time, as a millisecond epoch string."""
  closePriceAvg: Decimal
  """Average exit price."""
  openFee: Decimal
  """Opening fee, in USDT."""
  closeFee: Decimal
  """Closing fee, in USDT."""
  marginMode: Literal['crossed', 'isolated']
  """Margin mode."""
  followCount: int
  """Number of followers who copied this trade."""
  marginAmount: Decimal
  """Margin used to open the position."""
  cTime: TimestampMillis
  """Record creation time, as a millisecond epoch string."""


class Request(TypedDict):
  traderId: str
  """Trader user ID to look up trace history for."""
  productType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Which futures product family this request concerns."""
  symbol: NotRequired[str]
  """Trading pair, e.g. "BTCUSDT". Case-insensitive."""
  limit: NotRequired[int]
  """Results per page."""
  startTime: NotRequired[TimestampMillis]
  """Start of the queried range, as a millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """End of the queried range, as a millisecond timestamp."""


class FuturesBrokerHistoryTraces(TypedDict):
  """Page of historical trace records."""

  trackingList: list[FuturesBrokerHistoryTrace]
  """Historical (closed) copy trades placed by the trader's followers, as visible to the broker."""
  endId: str
  """Last tracking order ID in this page; pass as `idLessThan` to walk older, `idGreaterThan` to walk newer."""


class HistoryTraces(RpcEndpoint):
  """List the historical copy trades placed by a specific trader's followers, futures, as visible to their broker."""

  async def history_traces(
    self,
    *,
    trader_id: str,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    symbol: str | None = None,
    limit: int | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    validate: bool | None = None,
  ) -> FuturesBrokerHistoryTraces:
    """List the historical copy trades placed by a specific trader's followers, futures, as visible to their broker.

    Args:
      trader_id: Trader user ID to look up trace history for.
      product_type: Which futures product family this request concerns.
      symbol: Trading pair, e.g. "BTCUSDT". Case-insensitive.
      limit: Results per page.
      start_time: Start of the queried range, as a millisecond timestamp.
      end_time: End of the queried range, as a millisecond timestamp.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
    """
    request: Request = Request(traderId=trader_id, productType=product_type)
    if symbol is not None:
      request['symbol'] = symbol
    if limit is not None:
      request['limit'] = limit
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    return await self.request(
      request,
      method='GET',
      path='/api/v2/copy/mix-broker/query-history-traces',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=FuturesBrokerHistoryTraces,
    )
