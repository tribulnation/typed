# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class FuturesFollowerTrader(TypedDict):
  """One followed trader."""

  certificationType: Literal['Uncertified', 'Certified']
  """Trader verification status."""
  traderId: str
  """Trader user ID."""
  traderName: str
  """Trader's display name."""
  maxFollowLimit: str
  """Maximum position size this follower may allocate to this trader."""
  followCount: str
  """Current follower count for this trader."""
  traceTotalMarginAmount: str
  """Total margin this follower has allocated to this trader."""
  traceTotalNetProfit: str
  """Net profit this follower has earned copying this trader."""
  traceTotalProfit: str
  """Gross profit this follower has earned copying this trader."""
  currentTradingPairs: list[str]
  """Trading pairs currently being copied from this trader."""
  followerTime: TimestampMillis
  """Time this follower first started following, as a millisecond epoch string."""
  bgbMaxFollowLimit: str
  """Maximum position size allocatable when paying fees in BGB."""
  bgbFollowCount: str
  """Current follower count paying fees in BGB."""


class Request(TypedDict):
  startTime: NotRequired[TimestampMillis]
  """Start of the queried range, as a millisecond timestamp. Maximum span with endTime is 3 months."""
  endTime: NotRequired[TimestampMillis]
  """End of the queried range, as a millisecond timestamp. Maximum span with startTime is 3 months."""
  pageNo: NotRequired[int]
  """Page number. Defaults to 1."""
  pageSize: NotRequired[int]
  """Results per page. Default 20, maximum 50."""


Response = list[FuturesFollowerTrader]


class Traders(RpcEndpoint):
  """List the traders a follower is currently following, futures."""

  async def traders_paged(
    self,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    page_size: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[Response]:
    """List the traders a follower is currently following, futures.

    Paged variant of `traders`: Requests `pageNo` from 1 upwards and stops on the first page shorter than `page_size`, or after `max_pages` pages when one is given.

    Args:
      start_time: Start of the queried range, as a millisecond timestamp. Maximum span with endTime is 3 months.
      end_time: End of the queried range, as a millisecond timestamp. Maximum span with startTime is 3 months.
      page_size: Results per page. Default 20, maximum 50.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-future-follower/classic-copytrading-future-copytrade-follower#get-my-traders)
    """
    page_no = 1
    pages = 0
    while True:
      response = await self.traders(
        start_time=start_time,
        end_time=end_time,
        page_size=page_size,
        page_no=page_no,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      if not response or (page_size is not None and len(response) < page_size):
        break
      page_no += 1

  async def traders(
    self,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    page_no: int | None = None,
    page_size: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """List the traders a follower is currently following, futures.

    Args:
      start_time: Start of the queried range, as a millisecond timestamp. Maximum span with endTime is 3 months.
      end_time: End of the queried range, as a millisecond timestamp. Maximum span with startTime is 3 months.
      page_no: Page number. Defaults to 1.
      page_size: Results per page. Default 20, maximum 50.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-future-follower/classic-copytrading-future-copytrade-follower#get-my-traders)
    """
    request: Request = Request()
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if page_no is not None:
      request['pageNo'] = page_no
    if page_size is not None:
      request['pageSize'] = page_size
    return await self.request(
      request,
      method='GET',
      path='/api/v2/copy/mix-follower/query-traders',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
