# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.copytrading.mix.follower.close_positions` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CloseFuturesFollowerPositionsRequest(TypedDict):
  """Filter selecting which positions to close."""

  productType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Which futures product family this request concerns."""
  trackingNo: NotRequired[str]
  """Restrict to this tracking order."""
  symbol: NotRequired[str]
  """Restrict to this trading pair."""
  marginCoin: NotRequired[str]
  """Restrict to this margin coin."""
  marginMode: NotRequired[Literal['isolated', 'cross']]
  """Restrict to this margin mode."""
  holdSide: NotRequired[Literal['long', 'short']]
  """Restrict to this position side. Required in hedge mode, optional in one-way mode."""


class ClosedFuturesFollowerPositions(TypedDict):
  """Closed position order IDs."""

  orderIdList: list[str]
  """IDs of the orders placed to close the matched positions."""


class ClosePositions(RpcEndpoint):
  """`classic.copytrading.mix.follower.close_positions`."""

  async def close_positions(
    self,
    close_futures_follower_positions_request: CloseFuturesFollowerPositionsRequest,
    *,
    validate: bool | None = None,
  ) -> ClosedFuturesFollowerPositions:
    """Close one or more of a follower's open copy positions, futures, filtered by tracking order, symbol, margin coin/mode, or side.

    Args:
      close_futures_follower_positions_request: Filter selecting which positions to close.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/copytrading/future-copytrade/follower/Close-Positions)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/copy/mix-follower/close-positions',
      json=close_futures_follower_positions_request,
      validator=validator(ClosedFuturesFollowerPositions),
      validate=validate,
    )
