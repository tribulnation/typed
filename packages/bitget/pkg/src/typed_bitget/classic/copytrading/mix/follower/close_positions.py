# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class ClosedFuturesFollowerPositions(TypedDict):
  """Closed position order IDs."""

  orderIdList: list[str]
  """IDs of the orders placed to close the matched positions."""


class Request(TypedDict):
  """Filter selecting which positions to close."""

  productType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Which futures product family this request concerns."""
  trackingNo: NotRequired[str]
  """Restrict to this tracking order."""
  symbol: NotRequired[str]
  """Restrict to this trading pair."""
  marginCoin: NotRequired[str]
  """Restrict to this margin coin."""
  marginMode: NotRequired[Literal['isolated', 'cross']]
  """Restrict to this margin mode."""
  holdSide: NotRequired[Literal['long', 'short']]
  """Restrict to this position side. Required in hedge mode, optional in one-way mode."""


class ClosePositions(RpcEndpoint):
  """Close one or more of a follower's open copy positions, futures, filtered by tracking order, symbol, margin coin/mode, or side."""

  async def close_positions(
    self,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    *,
    tracking_no: str | None = None,
    symbol: str | None = None,
    margin_coin: str | None = None,
    margin_mode: Literal['isolated', 'cross'] | None = None,
    hold_side: Literal['long', 'short'] | None = None,
    validate: bool | None = None,
  ) -> ClosedFuturesFollowerPositions:
    """Close one or more of a follower's open copy positions, futures, filtered by tracking order, symbol, margin coin/mode, or side.

    Args:
      product_type: Which futures product family this request concerns.
      tracking_no: Restrict to this tracking order.
      symbol: Restrict to this trading pair.
      margin_coin: Restrict to this margin coin.
      margin_mode: Restrict to this margin mode.
      hold_side: Restrict to this position side. Required in hedge mode, optional in one-way mode.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-future-follower/classic-copytrading-future-copytrade-follower#close-positions)
    """
    request: Request = Request(productType=product_type)
    if tracking_no is not None:
      request['trackingNo'] = tracking_no
    if symbol is not None:
      request['symbol'] = symbol
    if margin_coin is not None:
      request['marginCoin'] = margin_coin
    if margin_mode is not None:
      request['marginMode'] = margin_mode
    if hold_side is not None:
      request['holdSide'] = hold_side
    return await self.request(
      request,
      method='POST',
      path='/api/v2/copy/mix-follower/close-positions',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=ClosedFuturesFollowerPositions,
    )
