# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class FuturesFollowerCurrentOrder(TypedDict):
  """One currently-tracked (open) copy position."""

  trackingNo: str
  """Tracking order ID."""
  traderName: str
  """Copied trader's display name."""
  openOrderId: str
  """Opening order ID."""
  closeOrderId: str
  """Closing order ID, if closed."""
  traderId: str
  """Copied trader's user ID."""
  symbol: str
  """Trading pair."""
  posSide: Literal['long', 'short']
  """Position direction."""
  openLeverage: str
  """Leverage applied when opening."""
  openAvgPrice: str
  """Average entry price."""
  openTime: TimestampMillis
  """Position opening time, as a millisecond epoch string."""
  openSize: str
  """Position quantity."""
  closeAvgPrice: str
  """Average exit price, if closed."""
  closeSize: str
  """Position quantity closed, if closed."""
  openMarginSz: str
  """Margin used to open the position."""
  closeTime: TimestampMillis
  """Position closing time, as a millisecond epoch string, if closed."""


class Request(TypedDict):
  idLessThan: NotRequired[str]
  """Pagination cursor: return records older than this tracking ID. Pass the previous page's `endId` to walk further back."""
  idGreaterThan: NotRequired[str]
  """Pagination cursor: return records newer than this tracking ID."""
  startTime: NotRequired[TimestampMillis]
  """Start of the queried range (copy-trade creation time), as a millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """End of the queried range (copy-trade creation time), as a millisecond timestamp."""
  limit: NotRequired[int]
  """Results per page. Default 20, maximum 100."""
  symbol: NotRequired[str]
  """Trading pair, e.g. "BTCUSDT". Case-insensitive."""
  productType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Which futures product family this request concerns."""
  traderId: NotRequired[str]
  """Restrict to positions copied from this trader."""


class FuturesFollowerCurrentOrders(TypedDict):
  """Page of currently-tracked positions."""

  trackingList: list[FuturesFollowerCurrentOrder] | None
  """Currently-tracked positions. Confirmed live: `null` (not `[]`) when the follower has none."""
  endId: str | None
  """Last tracking order ID in this page, or `null` when `trackingList` is `null`. Confirmed live."""


class CurrentOrders(RpcEndpoint):
  """List a follower's currently-tracked (open) copy positions, futures."""

  def current_orders_paged(
    self,
    *,
    id_greater_than: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    symbol: str | None = None,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    trader_id: str | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[FuturesFollowerCurrentOrder, str]:
    """List a follower's currently-tracked (open) copy positions, futures.

    Paged variant of `current_orders`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      id_greater_than: Pagination cursor: return records newer than this tracking ID.
      start_time: Start of the queried range (copy-trade creation time), as a millisecond timestamp.
      end_time: End of the queried range (copy-trade creation time), as a millisecond timestamp.
      limit: Results per page. Default 20, maximum 100.
      symbol: Trading pair, e.g. "BTCUSDT". Case-insensitive.
      product_type: Which futures product family this request concerns.
      trader_id: Restrict to positions copied from this trader.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-future-follower/classic-copytrading-future-copytrade-follower#get-current-tracking-orders)
    """

    async def next(
      id_less_than: str,
    ) -> tuple[list[FuturesFollowerCurrentOrder], str | None]:
      response = await self.current_orders(
        id_greater_than=id_greater_than,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        symbol=symbol,
        product_type=product_type,
        trader_id=trader_id,
        id_less_than=(id_less_than or None),
        validate=validate,
      )
      rows = response.get('trackingList')
      state = response.get('endId')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def current_orders(
    self,
    *,
    id_less_than: str | None = None,
    id_greater_than: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    symbol: str | None = None,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    trader_id: str | None = None,
    validate: bool | None = None,
  ) -> FuturesFollowerCurrentOrders:
    """List a follower's currently-tracked (open) copy positions, futures.

    Args:
      id_less_than: Pagination cursor: return records older than this tracking ID. Pass the previous page's `endId` to walk further back.
      id_greater_than: Pagination cursor: return records newer than this tracking ID.
      start_time: Start of the queried range (copy-trade creation time), as a millisecond timestamp.
      end_time: End of the queried range (copy-trade creation time), as a millisecond timestamp.
      limit: Results per page. Default 20, maximum 100.
      symbol: Trading pair, e.g. "BTCUSDT". Case-insensitive.
      product_type: Which futures product family this request concerns.
      trader_id: Restrict to positions copied from this trader.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-future-follower/classic-copytrading-future-copytrade-follower#get-current-tracking-orders)
    """
    request: Request = Request(productType=product_type)
    if id_less_than is not None:
      request['idLessThan'] = id_less_than
    if id_greater_than is not None:
      request['idGreaterThan'] = id_greater_than
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    if symbol is not None:
      request['symbol'] = symbol
    if trader_id is not None:
      request['traderId'] = trader_id
    return await self.request(
      request,
      method='GET',
      path='/api/v2/copy/mix-follower/query-current-orders',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=FuturesFollowerCurrentOrders,
    )
