# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class FuturesTraderSymbolSettingUpdate(TypedDict):
  """One symbol's setting update."""

  symbol: str
  """Trading pair."""
  productType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Which futures product family this request concerns."""
  settingType: Literal['ADD', 'DELETE', 'UPDATE']
  """Which operation to apply."""
  stopSurplusRatio: NotRequired[str]
  """Take-profit ratio, 1-400 (percent). Empty leaves unset on add; `"0"` clears the existing setting on update."""
  stopLossRatio: NotRequired[str]
  """Stop-loss ratio, 1-400 (percent). Empty leaves unset on add; `"0"` clears the existing setting on update."""


Response = str


class Request(TypedDict):
  """Per-symbol settings to add, update, or remove."""

  settingList: list[FuturesTraderSymbolSettingUpdate]
  """Settings to apply, up to 50 entries."""


class UpdateSymbolSettings(RpcEndpoint):
  """Add, update, or remove per-symbol copy-trading settings, futures."""

  async def update_symbol_settings(
    self,
    setting_list: list[FuturesTraderSymbolSettingUpdate],
    *,
    validate: bool | None = None,
  ) -> Response:
    """Add, update, or remove per-symbol copy-trading settings, futures.

    Args:
      setting_list: Settings to apply, up to 50 entries.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-future-trader/classic-copytrading-future-copytrade-trader#change-copy-trade-symbol-setting)
    """
    return await self.request(
      Request(settingList=setting_list),
      method='POST',
      path='/api/v2/copy/mix-trader/config-setting-symbols',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
