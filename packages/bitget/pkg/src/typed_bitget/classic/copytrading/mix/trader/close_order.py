# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class ClosedFuturesTraderOrder(TypedDict):
  """One closed position."""

  trackingNo: str
  """Tracking order ID."""
  symbol: str
  """Trading pair."""
  productType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Which futures product family this request concerns."""


class Request(TypedDict):
  """Positions to close."""

  trackingNo: NotRequired[str]
  """Tracking order ID to close. Omit together with symbol to close every open position in productType."""
  symbol: NotRequired[str]
  """Trading pair to close. Omit together with trackingNo to close every open position in productType."""
  productType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Which futures product family this request concerns."""


Response = list[ClosedFuturesTraderOrder]


class CloseOrder(RpcEndpoint):
  """Close one or all of a trader's open copy positions in a product line, futures. Tracked positions can only be closed through this endpoint."""

  async def close_order(
    self,
    *,
    tracking_no: str | None = None,
    symbol: str | None = None,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    validate: bool | None = None,
  ) -> Response:
    """Close one or all of a trader's open copy positions in a product line, futures. Tracked positions can only be closed through this endpoint.

    Args:
      tracking_no: Tracking order ID to close. Omit together with symbol to close every open position in productType.
      symbol: Trading pair to close. Omit together with trackingNo to close every open position in productType.
      product_type: Which futures product family this request concerns.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-future-trader/classic-copytrading-future-copytrade-trader#close-tracking-order)
    """
    request: Request = Request(productType=product_type)
    if tracking_no is not None:
      request['trackingNo'] = tracking_no
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='POST',
      path='/api/v2/copy/mix-trader/order-close-positions',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
