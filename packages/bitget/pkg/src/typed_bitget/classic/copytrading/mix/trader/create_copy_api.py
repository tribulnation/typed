# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.copytrading.mix.trader.create_copy_api` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CopyApiKey(TypedDict):
  """One newly-created copy-trading API key."""

  apikey: str
  """The new API key."""
  secret: str
  """The new API secret. Shown once, at creation."""
  permissions: list[Literal['contract_trade', 'copytrading_trade']]
  """Permissions granted to the new key."""


class CreateCopyApiRequest(TypedDict):
  """New copy-trading API key request."""

  passphrase: str
  """Passphrase for the new key, 8-32 characters, letters and numbers only."""


class CreateCopyApi(RpcEndpoint):
  """`classic.copytrading.mix.trader.create_copy_api`."""

  async def create_copy_api(
    self,
    create_copy_api_request: CreateCopyApiRequest,
    *,
    validate: bool | None = None,
  ) -> list[CopyApiKey]:
    """Create a dedicated API key for the mix (futures) copy-trading trader role.

    Args:
      create_copy_api_request: New API key's passphrase.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/copytrading/future-copytrade/trader/Create-Copy-Api)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/copy/mix-trader/create-copy-api',
      json=create_copy_api_request,
      validator=validator(list[CopyApiKey]),
      validate=validate,
    )
