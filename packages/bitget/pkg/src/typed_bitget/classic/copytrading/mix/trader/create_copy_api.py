# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CopyApiKey(TypedDict):
  """One newly-created copy-trading API key."""

  apikey: str
  """The new API key."""
  secret: str
  """The new API secret. Shown once, at creation."""
  permissions: list[Literal['contract_trade', 'copytrading_trade']]
  """Permissions granted to the new key."""


class Request(TypedDict):
  """New copy-trading API key request."""

  passphrase: str
  """Passphrase for the new key, 8-32 characters, letters and numbers only."""


Response = list[CopyApiKey]


class CreateCopyApi(RpcEndpoint):
  """Create a dedicated API key for the mix (futures) copy-trading trader role."""

  async def create_copy_api(
    self,
    passphrase: str,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Create a dedicated API key for the mix (futures) copy-trading trader role.

    Args:
      passphrase: Passphrase for the new key, 8-32 characters, letters and numbers only.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-future-trader/classic-copytrading-future-copytrade-trader#create-copy-apikey)
    """
    return await self.request(
      Request(passphrase=passphrase),
      method='POST',
      path='/api/v2/copy/mix-trader/create-copy-api',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
