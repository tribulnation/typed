# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class FuturesTraderCurrentOrder(TypedDict):
  """One currently-tracked (open) copy position."""

  trackingNo: str
  """Tracking order ID."""
  openOrderId: str
  """Elite trader's opening order ID."""
  symbol: str
  """Trading pair."""
  posSide: Literal['long', 'short']
  """Position direction."""
  openLeverage: str
  """Leverage applied when opening."""
  openPriceAvg: str
  """Average entry price."""
  openTime: TimestampMillis
  """Position opening time, as a millisecond epoch string."""
  openSize: str
  """Position quantity."""
  presetStopSurplusPrice: str
  """Preset take-profit price. Empty string if unset."""
  presetStopLossPrice: str
  """Preset stop-loss price. Empty string if unset."""
  openFee: str
  """Opening fee, in USDT."""
  followCount: str
  """Number of followers copying this position."""


class Request(TypedDict):
  symbol: NotRequired[str]
  """Trading pair, e.g. "BTCUSDT". Case-insensitive."""
  productType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Which futures product family this request concerns."""
  startTime: NotRequired[TimestampMillis]
  """Start of the queried range, as a millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """End of the queried range, as a millisecond timestamp."""
  limit: NotRequired[int]
  """Results per page. Default 20, maximum 50."""
  idGreaterThan: NotRequired[str]
  """Pagination cursor: return records newer than this tracking/profit ID."""
  idLessThan: NotRequired[str]
  """Pagination cursor: return records older than this tracking/profit ID. Pass the previous page's `endId` to walk further back."""


class FuturesTraderCurrentOrders(TypedDict):
  """Page of currently-tracked positions."""

  trackingList: list[FuturesTraderCurrentOrder]
  """Currently-tracked positions."""
  endId: str
  """Last tracking order ID in this page; pass as `idLessThan` to walk older, `idGreaterThan` to walk newer."""


class CurrentTrackOrders(RpcEndpoint):
  """List a trader's currently-tracked (open) copy positions, futures."""

  def current_track_orders_paged(
    self,
    *,
    symbol: str | None = None,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    id_greater_than: str | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[FuturesTraderCurrentOrder, str]:
    """List a trader's currently-tracked (open) copy positions, futures.

    Paged variant of `current_track_orders`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      symbol: Trading pair, e.g. "BTCUSDT". Case-insensitive.
      product_type: Which futures product family this request concerns.
      start_time: Start of the queried range, as a millisecond timestamp.
      end_time: End of the queried range, as a millisecond timestamp.
      limit: Results per page. Default 20, maximum 50.
      id_greater_than: Pagination cursor: return records newer than this tracking/profit ID.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-future-trader/classic-copytrading-future-copytrade-trader#get-current-tracking-orders)
    """

    async def next(
      id_less_than: str,
    ) -> tuple[list[FuturesTraderCurrentOrder], str | None]:
      response = await self.current_track_orders(
        symbol=symbol,
        product_type=product_type,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        id_greater_than=id_greater_than,
        id_less_than=(id_less_than or None),
        validate=validate,
      )
      rows = response.get('trackingList')
      state = response.get('endId')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def current_track_orders(
    self,
    *,
    symbol: str | None = None,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    id_greater_than: str | None = None,
    id_less_than: str | None = None,
    validate: bool | None = None,
  ) -> FuturesTraderCurrentOrders:
    """List a trader's currently-tracked (open) copy positions, futures.

    Args:
      symbol: Trading pair, e.g. "BTCUSDT". Case-insensitive.
      product_type: Which futures product family this request concerns.
      start_time: Start of the queried range, as a millisecond timestamp.
      end_time: End of the queried range, as a millisecond timestamp.
      limit: Results per page. Default 20, maximum 50.
      id_greater_than: Pagination cursor: return records newer than this tracking/profit ID.
      id_less_than: Pagination cursor: return records older than this tracking/profit ID. Pass the previous page's `endId` to walk further back.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-copytrading-future-trader/classic-copytrading-future-copytrade-trader#get-current-tracking-orders)
    """
    request: Request = Request(productType=product_type)
    if symbol is not None:
      request['symbol'] = symbol
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    if id_greater_than is not None:
      request['idGreaterThan'] = id_greater_than
    if id_less_than is not None:
      request['idLessThan'] = id_less_than
    return await self.request(
      request,
      method='GET',
      path='/api/v2/copy/mix-trader/order-current-track',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=FuturesTraderCurrentOrders,
    )
