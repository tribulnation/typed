# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.tax.p2p_records` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis, timestamp_millis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class P2pTaxRecord(TypedDict):
  id: str
  """Unique record id."""
  coin: str
  """Coin symbol."""
  p2pTaxType: Literal['transfer_in', 'transfer_out', 'sell', 'buy']
  """Transaction category."""
  balance: str
  """Account balance after the record."""
  ts: TimestampMillis
  """Record creation time, ms epoch."""


class P2pRecords(RpcEndpoint):
  """`classic.tax.p2p_records`."""

  async def p2p_records(
    self,
    *,
    coin: str | None = None,
    start_time: TimestampMillis,
    end_time: TimestampMillis,
    limit: int | None = None,
    id_less_than: str | None = None,
    validate: bool | None = None,
  ) -> list[P2pTaxRecord]:
    """List the authenticated user's P2P tax records over a time window of at most 30 days.

    Args:
      coin: Restrict results to a single coin. Returns all coins when omitted.
      start_time: Range start, ms epoch.
      end_time: Range end, ms epoch. Must be at most 30 days after startTime.
      limit: Maximum number of records to return.
      id_less_than: Pagination cursor: return records with id less than this value. Read from the id of the last record on the previous page.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/tax/Get-P2P-Account-Record)
    """
    params: dict = {
      'startTime': timestamp_millis.dump(start_time),
      'endTime': timestamp_millis.dump(end_time),
    }
    if coin is not None:
      params['coin'] = coin
    if limit is not None:
      params['limit'] = limit
    if id_less_than is not None:
      params['idLessThan'] = id_less_than
    return await self.authed_request(
      'GET',
      '/api/v2/tax/p2p-record',
      params=params,
      validator=validator(list[P2pTaxRecord]),
      validate=validate,
    )
