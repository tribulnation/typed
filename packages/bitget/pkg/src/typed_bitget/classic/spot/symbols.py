# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.symbols` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SpotSymbol(TypedDict):
  """One tradable spot symbol and its trading rules."""

  symbol: str
  """Trading pair symbol, e.g. `BTCUSDT`."""
  baseCoin: str
  """Base coin, e.g. `BTC`."""
  quoteCoin: str
  """Quote coin, e.g. `USDT`."""
  minTradeAmount: str
  """Minimum order size in base coin, as a decimal string."""
  maxTradeAmount: str
  """Maximum order size in base coin, as a decimal string."""
  takerFeeRate: str
  """Default taker fee rate, as a fraction."""
  makerFeeRate: str
  """Default maker fee rate, as a fraction."""
  pricePrecision: str
  """Decimal places allowed in an order price."""
  quantityPrecision: str
  """Decimal places allowed in an order quantity."""
  quotePrecision: str
  """Decimal places allowed in a quote-coin amount."""
  minTradeUSDT: str
  """Minimum order notional value in USDT, as a decimal string."""
  status: Literal['online', 'offline', 'gray', 'halt']
  """Trading status of the symbol."""
  buyLimitPriceRatio: str
  """Maximum allowed ratio a buy limit-order price may exceed the current price by."""
  sellLimitPriceRatio: str
  """Maximum allowed ratio a sell limit-order price may fall below the current price by."""
  orderQuantity: str
  """Maximum number of open orders allowed on this symbol."""
  areaSymbol: str
  """Whether this symbol is restricted to specific regions."""
  maxLimitOrderValue: NotRequired[str]
  """Maximum notional value for a limit order, as a decimal string."""
  maxMarketOrderValue: NotRequired[str]
  """Maximum notional value for a market order, as a decimal string."""
  openTime: TimestampMillis
  """Symbol listing time, Unix millisecond timestamp, as a string."""


class Symbols(RpcEndpoint):
  """`classic.spot.symbols`."""

  async def symbols(
    self,
    *,
    symbol: str | None = None,
    validate: bool | None = None,
  ) -> list[SpotSymbol]:
    """List tradable spot symbols and their trading rules. Public, no authentication required.

    Args:
      symbol: Restrict to a single symbol, e.g. `BTCUSDT`.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/market/Get-Symbols)
    """
    params = {}
    if symbol is not None:
      params['symbol'] = symbol
    return await self.request(
      'GET',
      '/api/v2/spot/public/symbols',
      params=params,
      validator=validator(list[SpotSymbol]),
      validate=validate,
    )
