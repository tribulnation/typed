# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import SpotOrderSide


class Request(TypedDict):
  symbol: str
  """Trading pair symbol, e.g. `BTCUSDT`."""
  limit: NotRequired[int]
  """Number of trades to return."""


class SpotTrade(TypedDict):
  """One public trade."""

  symbol: str
  """Trading pair symbol."""
  tradeId: str
  """Trade ID."""
  side: SpotOrderSide
  price: Decimal
  """Trade price, as a decimal string."""
  size: Decimal
  """Trade size, as a decimal string."""
  ts: TimestampMillis
  """Trade time, Unix millisecond timestamp, as a string."""


Response = list[SpotTrade]


class RecentTrades(RpcEndpoint):
  """Get the most recent public trades for a symbol. Public, no authentication required."""

  async def recent_trades(
    self,
    symbol: str,
    *,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get the most recent public trades for a symbol. Public, no authentication required.

    Args:
      symbol: Trading pair symbol, e.g. `BTCUSDT`.
      limit: Number of trades to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-market/classic-spot-market#get-recent-trades)
    """
    request: Request = Request(symbol=symbol)
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/api/v2/spot/market/fills',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
