# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import SpotOrderIdResult, SpotTpslType


class Request(TypedDict):
  """An order to cancel."""

  symbol: str
  """Trading pair symbol."""
  tpslType: NotRequired[SpotTpslType]
  orderId: NotRequired[str]
  """Exchange order ID of the order to cancel. Either this or `clientOid` is required."""
  clientOid: NotRequired[str]
  """Client order ID of the order to cancel. Either this or `orderId` is required."""


class Cancel(RpcEndpoint):
  """Cancel an open spot order."""

  async def cancel(
    self,
    *,
    symbol: str,
    tpsl_type: SpotTpslType | None = None,
    order_id: str | None = None,
    client_oid: str | None = None,
    validate: bool | None = None,
  ) -> SpotOrderIdResult:
    """Cancel an open spot order.

    Args:
      symbol: Trading pair symbol.
      tpsl_type:
      order_id: Exchange order ID of the order to cancel. Either this or `clientOid` is required.
      client_oid: Client order ID of the order to cancel. Either this or `orderId` is required.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-trade/classic-spot-trade#cancel-order)
    """
    request: Request = Request(symbol=symbol)
    if tpsl_type is not None:
      request['tpslType'] = tpsl_type
    if order_id is not None:
      request['orderId'] = order_id
    if client_oid is not None:
      request['clientOid'] = client_oid
    return await self.request(
      request,
      method='POST',
      path='/api/v2/spot/trade/cancel-order',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SpotOrderIdResult,
    )
