# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .batch import Batch
from .cancel import Cancel
from .cancel_replace import CancelReplace
from .cancel_symbol import CancelSymbol
from .fills import Fills
from .history import History
from .info import Info
from .open import Open
from .place import Place
from .plan import Plan


class Order(Cancel, CancelReplace, CancelSymbol, Fills, History, Info, Open, Place):
  """Bitget Classic's Spot Order API: placing, cancelling (single, symbol-wide, and cancel-replace), and querying open/historical spot orders and fills.

  References:
    - [Upstream docs](https://www.bitget.com/docs/catalog/classic-spot-trade)
  """

  @cached_property
  def batch(self) -> Batch:
    """Bitget Classic's Spot Batch Order API: placing and cancelling multiple spot orders in one call.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-spot-trade)
    """
    return Batch(client=self.client)

  @cached_property
  def plan(self) -> Plan:
    """Bitget Classic's Spot Trigger (Plan) Order API: placing, modifying, cancelling, and querying conditional/trigger orders for spot trading.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-spot-plan)
    """
    return Plan(client=self.client)
