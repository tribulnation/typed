# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """A plan order to cancel."""

  clientOid: NotRequired[str]
  """Client order ID of the plan order to cancel. Either this or `orderId` is required."""
  orderId: NotRequired[str]
  """Exchange plan-order ID to cancel. Either this or `clientOid` is required."""


class SpotCancelPlanOrderResult(TypedDict):
  """Result of cancelling a plan order."""

  result: str
  """Cancellation result."""


class Cancel(RpcEndpoint):
  """Cancel one spot plan order."""

  async def cancel(
    self,
    *,
    client_oid: str | None = None,
    order_id: str | None = None,
    validate: bool | None = None,
  ) -> SpotCancelPlanOrderResult:
    """Cancel one spot plan order.

    Args:
      client_oid: Client order ID of the plan order to cancel. Either this or `orderId` is required.
      order_id: Exchange plan-order ID to cancel. Either this or `clientOid` is required.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-plan/classic-spot-plan#cancel-plan-order)
    """
    request: Request = Request()
    if client_oid is not None:
      request['clientOid'] = client_oid
    if order_id is not None:
      request['orderId'] = order_id
    return await self.request(
      request,
      method='POST',
      path='/api/v2/spot/trade/cancel-plan-order',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SpotCancelPlanOrderResult,
    )
