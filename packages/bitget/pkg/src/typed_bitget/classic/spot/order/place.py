# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import (
  SpotOrderForce,
  SpotOrderIdResult,
  SpotOrderSide,
  SpotStpMode,
  SpotTpslType,
)


class SpotLimitOrderRequest(TypedDict):
  """A spot limit order to place."""

  symbol: str
  """Trading pair symbol, e.g. `BTCUSDT`."""
  side: SpotOrderSide
  orderType: Literal['limit']
  """Order type."""
  force: SpotOrderForce
  price: Decimal
  """Limit price, as a decimal string. Required for a limit order, ignored for a market order."""
  size: Decimal
  """Order size, as a decimal string. In base coin for a limit order; in quote coin for a market buy, base coin for a market sell."""
  clientOid: NotRequired[str]
  """Caller-supplied client order ID, echoed back on the order."""
  triggerPrice: NotRequired[str]
  """Trigger price for a TP/SL order, as a decimal string. Only meaningful when `tpslType` is `tpsl`."""
  tpslType: NotRequired[SpotTpslType]
  stpMode: NotRequired[SpotStpMode]
  presetTakeProfitPrice: NotRequired[str]
  """Take-profit price to attach to the position this order opens, as a decimal string."""
  executeTakeProfitPrice: NotRequired[str]
  """Execution price for the attached take-profit order, as a decimal string."""
  presetStopLossPrice: NotRequired[str]
  """Stop-loss price to attach to the position this order opens, as a decimal string."""
  executeStopLossPrice: NotRequired[str]
  """Execution price for the attached stop-loss order, as a decimal string."""


class SpotMarketOrderRequest(TypedDict):
  """A spot market order to place."""

  symbol: str
  """Trading pair symbol, e.g. `BTCUSDT`."""
  side: SpotOrderSide
  orderType: Literal['market']
  """Order type."""
  force: SpotOrderForce
  size: Decimal
  """Order size, as a decimal string. In base coin for a limit order; in quote coin for a market buy, base coin for a market sell."""
  clientOid: NotRequired[str]
  """Caller-supplied client order ID, echoed back on the order."""
  triggerPrice: NotRequired[str]
  """Trigger price for a TP/SL order, as a decimal string. Only meaningful when `tpslType` is `tpsl`."""
  tpslType: NotRequired[SpotTpslType]
  stpMode: NotRequired[SpotStpMode]
  presetTakeProfitPrice: NotRequired[str]
  """Take-profit price to attach to the position this order opens, as a decimal string."""
  executeTakeProfitPrice: NotRequired[str]
  """Execution price for the attached take-profit order, as a decimal string."""
  presetStopLossPrice: NotRequired[str]
  """Stop-loss price to attach to the position this order opens, as a decimal string."""
  executeStopLossPrice: NotRequired[str]
  """Execution price for the attached stop-loss order, as a decimal string."""


Request = SpotLimitOrderRequest | SpotMarketOrderRequest


class Place(RpcEndpoint):
  """Place a spot order."""

  async def place(
    self,
    spot_order_request: Request,
    *,
    validate: bool | None = None,
  ) -> SpotOrderIdResult:
    """Place a spot order.

    Args:
      spot_order_request: A spot order to place.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-trade/classic-spot-trade#place-order)
    """
    return await self.request(
      spot_order_request,
      method='POST',
      path='/api/v2/spot/trade/place-order',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SpotOrderIdResult,
    )
