# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import SpotOrderType


class Request(TypedDict):
  planOrderId: str
  """Exchange plan-order ID whose executed sub-orders to list."""


class SpotPlanSubOrder(TypedDict):
  """One sub-order produced by a triggered plan order."""

  orderId: str
  """Exchange order ID of the resulting normal order."""
  price: str
  """Order price, as a decimal string."""
  type: SpotOrderType
  status: str
  """Current status of the resulting order."""


Response = list[SpotPlanSubOrder]


class SubOrders(RpcEndpoint):
  """List the executed sub-order(s) a triggered plan order produced."""

  async def sub_orders(
    self, plan_order_id: str, *, validate: bool | None = None
  ) -> Response:
    """List the executed sub-order(s) a triggered plan order produced.

    Args:
      plan_order_id: Exchange plan-order ID whose executed sub-orders to list.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-plan/classic-spot-plan#get-plan-sub-order)
    """
    return await self.request(
      Request(planOrderId=plan_order_id),
      method='GET',
      path='/api/v2/spot/trade/plan-sub-order',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
