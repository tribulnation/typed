# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import (
  SpotOrderForce,
  SpotOrderIdResult,
  SpotOrderSide,
  SpotPlanType,
  SpotStpMode,
  SpotTriggerType,
)


class SpotPlanLimitOrderRequest(TypedDict):
  """A spot plan order that submits a limit order once triggered."""

  symbol: str
  """Trading pair symbol."""
  side: SpotOrderSide
  triggerPrice: str
  """Price that triggers submission of the order, as a decimal string."""
  orderType: Literal['limit']
  """Order type."""
  executePrice: str
  """Limit price of the order once triggered, as a decimal string."""
  planType: NotRequired[SpotPlanType]
  size: str
  """Order size, as a decimal string; base coin or quote coin depending on `planType`."""
  triggerType: SpotTriggerType
  clientOid: NotRequired[str]
  """Caller-supplied client order ID, echoed back on the plan order."""
  force: NotRequired[SpotOrderForce]
  stpMode: NotRequired[SpotStpMode]


class SpotPlanMarketOrderRequest(TypedDict):
  """A spot plan order that submits a market order once triggered."""

  symbol: str
  """Trading pair symbol."""
  side: SpotOrderSide
  triggerPrice: str
  """Price that triggers submission of the order, as a decimal string."""
  orderType: Literal['market']
  """Order type."""
  planType: NotRequired[SpotPlanType]
  size: str
  """Order size, as a decimal string; base coin or quote coin depending on `planType`."""
  triggerType: SpotTriggerType
  clientOid: NotRequired[str]
  """Caller-supplied client order ID, echoed back on the plan order."""
  force: NotRequired[SpotOrderForce]
  stpMode: NotRequired[SpotStpMode]


Request = SpotPlanLimitOrderRequest | SpotPlanMarketOrderRequest


class Place(RpcEndpoint):
  """Place a spot plan (conditional/trigger) order: an order that is submitted to the book only once a trigger condition is met."""

  async def place(
    self,
    spot_plan_order_request: Request,
    *,
    validate: bool | None = None,
  ) -> SpotOrderIdResult:
    """Place a spot plan (conditional/trigger) order: an order that is submitted to the book only once a trigger condition is met.

    Args:
      spot_plan_order_request: A plan order to place.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-plan/classic-spot-plan#place-plan-order)
    """
    return await self.request(
      spot_plan_order_request,
      method='POST',
      path='/api/v2/spot/trade/place-plan-order',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SpotOrderIdResult,
    )
