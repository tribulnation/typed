# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.order.plan.modify` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.types import SpotOrderIdResult
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SpotModifyPlanOrderRequest(TypedDict):
  """Changes to apply to an existing plan order."""

  orderId: NotRequired[str]
  """Exchange plan-order ID to modify. Either this or `clientOid` is required."""
  clientOid: NotRequired[str]
  """Client order ID of the plan order to modify. Either this or `orderId` is required."""
  triggerPrice: str
  """New trigger price, as a decimal string."""
  orderType: Literal['limit', 'market']
  """Order type."""
  executePrice: NotRequired[str]
  """New limit price once triggered, as a decimal string."""
  size: str
  """New order size, as a decimal string."""


class Modify(RpcEndpoint):
  """`classic.spot.order.plan.modify`."""

  async def modify(
    self,
    spot_modify_plan_order_request: SpotModifyPlanOrderRequest,
    *,
    validate: bool | None = None,
  ) -> SpotOrderIdResult:
    """Modify an existing spot plan order's trigger/execution price or size.

    Args:
      spot_modify_plan_order_request: Changes to apply to an existing plan order.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/plan/Modify-Plan-Order)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/spot/trade/modify-plan-order',
      json=spot_modify_plan_order_request,
      validator=validator(SpotOrderIdResult),
      validate=validate,
    )
