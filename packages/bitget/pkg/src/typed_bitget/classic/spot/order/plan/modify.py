# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import SpotOrderIdResult, SpotOrderType


class Request(TypedDict):
  """Changes to apply to an existing plan order."""

  orderId: NotRequired[str]
  """Exchange plan-order ID to modify. Either this or `clientOid` is required."""
  clientOid: NotRequired[str]
  """Client order ID of the plan order to modify. Either this or `orderId` is required."""
  triggerPrice: str
  """New trigger price, as a decimal string."""
  orderType: SpotOrderType
  executePrice: NotRequired[str]
  """New limit price once triggered, as a decimal string."""
  size: str
  """New order size, as a decimal string."""


class Modify(RpcEndpoint):
  """Modify an existing spot plan order's trigger/execution price or size."""

  async def modify(
    self,
    *,
    order_id: str | None = None,
    client_oid: str | None = None,
    trigger_price: str,
    order_type: SpotOrderType,
    execute_price: str | None = None,
    size: str,
    validate: bool | None = None,
  ) -> SpotOrderIdResult:
    """Modify an existing spot plan order's trigger/execution price or size.

    Args:
      order_id: Exchange plan-order ID to modify. Either this or `clientOid` is required.
      client_oid: Client order ID of the plan order to modify. Either this or `orderId` is required.
      trigger_price: New trigger price, as a decimal string.
      order_type:
      execute_price: New limit price once triggered, as a decimal string.
      size: New order size, as a decimal string.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-plan/classic-spot-plan#modify-plan-order)
    """
    request: Request = Request(
      triggerPrice=trigger_price, orderType=order_type, size=size
    )
    if order_id is not None:
      request['orderId'] = order_id
    if client_oid is not None:
      request['clientOid'] = client_oid
    if execute_price is not None:
      request['executePrice'] = execute_price
    return await self.request(
      request,
      method='POST',
      path='/api/v2/spot/trade/modify-plan-order',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SpotOrderIdResult,
    )
