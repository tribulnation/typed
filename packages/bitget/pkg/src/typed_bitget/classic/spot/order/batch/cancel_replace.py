# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SpotCancelReplaceOrderRequest(TypedDict):
  """One cancel-and-replace instruction."""

  symbol: str
  """Trading pair symbol."""
  price: str
  """New limit price, as a decimal string."""
  size: str
  """New order size, as a decimal string."""
  orderId: NotRequired[str]
  """Exchange order ID of the order to replace. Either this or `clientOid` is required."""
  clientOid: NotRequired[str]
  """Client order ID of the order to replace. Either this or `orderId` is required."""
  newClientOid: NotRequired[str]
  """Client order ID to assign to the replacement order."""
  presetTakeProfitPrice: NotRequired[str]
  """Take-profit price to attach to the position this order opens, as a decimal string."""
  executeTakeProfitPrice: NotRequired[str]
  """Execution price for the attached take-profit order, as a decimal string."""
  presetStopLossPrice: NotRequired[str]
  """Stop-loss price to attach to the position this order opens, as a decimal string."""
  executeStopLossPrice: NotRequired[str]
  """Execution price for the attached stop-loss order, as a decimal string."""


class SpotCancelReplaceOrderResult(TypedDict):
  """Result of one cancel-and-replace."""

  orderId: str
  """Exchange order ID of the replacement order."""
  clientOid: str | None
  """Client order ID of the replacement order, or `null` if none was assigned."""
  success: str
  """Whether the cancel-and-replace succeeded."""
  msg: str | None
  """Failure reason, or `null` on success."""


class Request(TypedDict):
  """A batch of cancel-and-replace instructions."""

  orderList: list[SpotCancelReplaceOrderRequest]
  """Cancel-and-replace instructions, up to 50."""


Response = list[SpotCancelReplaceOrderResult]


class CancelReplace(RpcEndpoint):
  """Atomically cancel and replace up to 50 existing orders in one call."""

  async def cancel_replace(
    self,
    order_list: list[SpotCancelReplaceOrderRequest],
    *,
    validate: bool | None = None,
  ) -> Response:
    """Atomically cancel and replace up to 50 existing orders in one call.

    Args:
      order_list: Cancel-and-replace instructions, up to 50.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-trade/classic-spot-trade#batch-cancel-existing-order-and-send-new-orders)
    """
    return await self.request(
      Request(orderList=order_list),
      method='POST',
      path='/api/v2/spot/trade/batch-cancel-replace-order',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
