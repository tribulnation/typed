# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Symbols to cancel pending plan orders on."""

  symbolList: NotRequired[list[str]]
  """Symbols to cancel pending plan orders on. Omit to cancel across every symbol."""


class SpotBatchOrderFailure(TypedDict):
  """One order that failed."""

  orderId: str
  """Exchange order ID, if one was assigned before failing."""
  clientOid: str
  """Client order ID."""
  errorMsg: str
  """Failure reason."""
  errorCode: NotRequired[str]
  """Failure error code."""


class SpotBatchOrderSuccess(TypedDict):
  """One order that succeeded."""

  orderId: str
  """Exchange order ID."""
  clientOid: str
  """Client order ID."""


class SpotBatchOrdersResult(TypedDict):
  """Per-order results of a batch operation."""

  successList: list[SpotBatchOrderSuccess]
  """Orders that succeeded."""
  failureList: list[SpotBatchOrderFailure]
  """Orders that failed."""


class BatchCancel(RpcEndpoint):
  """Cancel every pending spot plan order, optionally restricted to a set of symbols."""

  async def batch_cancel(
    self,
    symbol_list: list[str] | None = None,
    *,
    validate: bool | None = None,
  ) -> SpotBatchOrdersResult:
    """Cancel every pending spot plan order, optionally restricted to a set of symbols.

    Args:
      symbol_list: Symbols to cancel pending plan orders on. Omit to cancel across every symbol.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-plan/classic-spot-plan#cancel-plan-orders-in-batch)
    """
    request: Request = Request()
    if symbol_list is not None:
      request['symbolList'] = symbol_list
    return await self.request(
      request,
      method='POST',
      path='/api/v2/spot/trade/batch-cancel-plan-order',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SpotBatchOrdersResult,
    )
