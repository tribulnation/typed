# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.order.plan.batch_cancel` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SpotBatchCancelPlanOrdersRequest(TypedDict):
  """Symbols to cancel pending plan orders on."""

  symbolList: NotRequired[list[str]]
  """Symbols to cancel pending plan orders on. Omit to cancel across every symbol."""


class SpotBatchOrderFailure(TypedDict):
  """One order that failed."""

  orderId: str
  """Exchange order ID, if one was assigned before failing."""
  clientOid: str
  """Client order ID."""
  errorMsg: str
  """Failure reason."""
  errorCode: NotRequired[str]
  """Failure error code."""


class SpotBatchOrderSuccess(TypedDict):
  """One order that succeeded."""

  orderId: str
  """Exchange order ID."""
  clientOid: str
  """Client order ID."""


class SpotBatchOrdersResult(TypedDict):
  """Per-order results of a batch operation."""

  successList: list[SpotBatchOrderSuccess]
  """Orders that succeeded."""
  failureList: list[SpotBatchOrderFailure]
  """Orders that failed."""


class BatchCancel(RpcEndpoint):
  """`classic.spot.order.plan.batch_cancel`."""

  async def batch_cancel(
    self,
    spot_batch_cancel_plan_orders_request: SpotBatchCancelPlanOrdersRequest,
    *,
    validate: bool | None = None,
  ) -> SpotBatchOrdersResult:
    """Cancel every pending spot plan order, optionally restricted to a set of symbols.

    Args:
      spot_batch_cancel_plan_orders_request: Symbols to cancel pending plan orders on.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/plan/Batch-Cancel-Plan-Order)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/spot/trade/batch-cancel-plan-order',
      json=spot_batch_cancel_plan_orders_request,
      validator=validator(SpotBatchOrdersResult),
      validate=validate,
    )
