# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.order.fills` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis, timestamp_millis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SpotOwnFillFeeDetail(TypedDict):
  """Fee charged on this fill."""

  deduction: str
  """Whether the fee was deducted via BGB fee-deduction."""
  feeCoin: str
  """Coin the fee was charged in."""
  totalDeductionFee: str
  """Portion of the fee paid via BGB deduction, as a decimal string."""
  totalFee: str
  """Total fee charged, as a decimal string."""


class SpotOwnFill(TypedDict):
  """One fill of the caller's own order."""

  userId: str
  """Account user ID."""
  symbol: str
  """Trading pair symbol."""
  orderId: str
  """Exchange order ID this fill belongs to."""
  tradeId: str
  """Trade (fill) ID."""
  orderType: Literal['limit', 'market']
  """Order type."""
  side: Literal['buy', 'sell']
  """Order side."""
  priceAvg: str
  """Fill price, as a decimal string."""
  size: str
  """Fill size, as a decimal string."""
  amount: str
  """Fill notional value, as a decimal string."""
  feeDetail: SpotOwnFillFeeDetail
  tradeScope: str
  """Whether the caller was maker or taker on this fill."""
  cTime: TimestampMillis
  """Fill time, Unix millisecond timestamp, as a string."""
  uTime: TimestampMillis
  """Last update time, Unix millisecond timestamp, as a string."""


class Fills(RpcEndpoint):
  """`classic.spot.order.fills`."""

  async def fills(
    self,
    *,
    symbol: str,
    order_id: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    id_less_than: str | None = None,
    validate: bool | None = None,
  ) -> list[SpotOwnFill]:
    """List the caller's own executed spot trade fills.

    Args:
      symbol: Trading pair symbol, e.g. `BTCUSDT`.
      order_id: Restrict to fills of a single order.
      start_time: Range start, Unix millisecond timestamp.
      end_time: Range end, Unix millisecond timestamp.
      limit: Number of fills to return.
      id_less_than: Pagination cursor: return entries with an id less than this value (a `orderId`/`billId`/`tradeId` read out of the last row of the previous page).

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/trade/Get-Fills)
    """
    params: dict = {
      'symbol': symbol,
    }
    if order_id is not None:
      params['orderId'] = order_id
    if start_time is not None:
      params['startTime'] = timestamp_millis.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp_millis.dump(end_time)
    if limit is not None:
      params['limit'] = limit
    if id_less_than is not None:
      params['idLessThan'] = id_less_than
    return await self.authed_request(
      'GET',
      '/api/v2/spot/trade/fills',
      params=params,
      validator=validator(list[SpotOwnFill]),
      validate=validate,
    )
