# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """A cancel-and-replace instruction."""

  symbol: str
  """Trading pair symbol."""
  price: str
  """New limit price, as a decimal string."""
  size: str
  """New order size, as a decimal string."""
  orderId: NotRequired[str]
  """Exchange order ID of the order to replace. Either this or `clientOid` is required."""
  clientOid: NotRequired[str]
  """Client order ID of the order to replace. Either this or `orderId` is required."""
  newClientOid: NotRequired[str]
  """Client order ID to assign to the replacement order."""
  presetTakeProfitPrice: NotRequired[str]
  """Take-profit price to attach to the position this order opens, as a decimal string."""
  executeTakeProfitPrice: NotRequired[str]
  """Execution price for the attached take-profit order, as a decimal string."""
  presetStopLossPrice: NotRequired[str]
  """Stop-loss price to attach to the position this order opens, as a decimal string."""
  executeStopLossPrice: NotRequired[str]
  """Execution price for the attached stop-loss order, as a decimal string."""


class SpotCancelReplaceOrderResult(TypedDict):
  """Result of one cancel-and-replace."""

  orderId: str
  """Exchange order ID of the replacement order."""
  clientOid: str | None
  """Client order ID of the replacement order, or `null` if none was assigned."""
  success: str
  """Whether the cancel-and-replace succeeded."""
  msg: str | None
  """Failure reason, or `null` on success."""


class CancelReplace(RpcEndpoint):
  """Atomically cancel an existing order and place a replacement with a new price and/or size."""

  async def cancel_replace(
    self,
    *,
    symbol: str,
    price: str,
    size: str,
    order_id: str | None = None,
    client_oid: str | None = None,
    new_client_oid: str | None = None,
    preset_take_profit_price: str | None = None,
    execute_take_profit_price: str | None = None,
    preset_stop_loss_price: str | None = None,
    execute_stop_loss_price: str | None = None,
    validate: bool | None = None,
  ) -> SpotCancelReplaceOrderResult:
    """Atomically cancel an existing order and place a replacement with a new price and/or size.

    Args:
      symbol: Trading pair symbol.
      price: New limit price, as a decimal string.
      size: New order size, as a decimal string.
      order_id: Exchange order ID of the order to replace. Either this or `clientOid` is required.
      client_oid: Client order ID of the order to replace. Either this or `orderId` is required.
      new_client_oid: Client order ID to assign to the replacement order.
      preset_take_profit_price: Take-profit price to attach to the position this order opens, as a decimal string.
      execute_take_profit_price: Execution price for the attached take-profit order, as a decimal string.
      preset_stop_loss_price: Stop-loss price to attach to the position this order opens, as a decimal string.
      execute_stop_loss_price: Execution price for the attached stop-loss order, as a decimal string.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-trade/classic-spot-trade#cancel-an-existing-order-and-send-a-new-order)
    """
    request: Request = Request(symbol=symbol, price=price, size=size)
    if order_id is not None:
      request['orderId'] = order_id
    if client_oid is not None:
      request['clientOid'] = client_oid
    if new_client_oid is not None:
      request['newClientOid'] = new_client_oid
    if preset_take_profit_price is not None:
      request['presetTakeProfitPrice'] = preset_take_profit_price
    if execute_take_profit_price is not None:
      request['executeTakeProfitPrice'] = execute_take_profit_price
    if preset_stop_loss_price is not None:
      request['presetStopLossPrice'] = preset_stop_loss_price
    if execute_stop_loss_price is not None:
      request['executeStopLossPrice'] = execute_stop_loss_price
    return await self.request(
      request,
      method='POST',
      path='/api/v2/spot/trade/cancel-replace-order',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SpotCancelReplaceOrderResult,
    )
