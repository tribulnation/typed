# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Symbol to cancel every open order on."""

  symbol: str
  """Trading pair symbol."""


class SpotCancelSymbolOrdersResult(TypedDict):
  """Result of cancelling every open order on a symbol."""

  symbol: str
  """Trading pair symbol whose orders were cancelled."""


class CancelSymbol(RpcEndpoint):
  """Cancel every open order on one symbol."""

  async def cancel_symbol(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> SpotCancelSymbolOrdersResult:
    """Cancel every open order on one symbol.

    Args:
      symbol: Trading pair symbol.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-trade/classic-spot-trade#cancel-order-by-symbol)
    """
    return await self.request(
      Request(symbol=symbol),
      method='POST',
      path='/api/v2/spot/trade/cancel-symbol-order',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SpotCancelSymbolOrdersResult,
    )
