# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.order.open` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis, timestamp_millis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SpotOpenOrder(TypedDict):
  """One currently open (unfilled) spot order."""

  userId: str
  """Account user ID that placed the order."""
  symbol: str
  """Trading pair symbol."""
  orderId: str
  """Exchange order ID."""
  clientOid: str
  """Client order ID."""
  priceAvg: str
  """Average fill price so far, as a decimal string."""
  size: str
  """Order size, as a decimal string."""
  orderType: Literal['limit', 'market']
  """Order type."""
  side: Literal['buy', 'sell']
  """Order side."""
  status: str
  """Current order status."""
  basePrice: str
  """Order price, as a decimal string."""
  baseVolume: str
  """Filled amount in base coin so far, as a decimal string."""
  quoteVolume: str
  """Filled amount in quote coin so far, as a decimal string."""
  enterPointSource: str
  """Origin of the order (e.g. API, web, app)."""
  orderSource: NotRequired[str]
  """Order source classification."""
  presetTakeProfitPrice: str
  """Attached take-profit price, as a decimal string."""
  executeTakeProfitPrice: str
  """Attached take-profit execution price, as a decimal string."""
  presetStopLossPrice: str
  """Attached stop-loss price, as a decimal string."""
  executeStopLossPrice: str
  """Attached stop-loss execution price, as a decimal string."""
  cTime: TimestampMillis
  """Creation time, Unix millisecond timestamp, as a string."""
  uTime: NotRequired[TimestampMillis]
  """Last update time, Unix millisecond timestamp, as a string."""
  tpslType: Literal['normal', 'tpsl']
  """Whether this is a normal order or a take-profit/stop-loss order."""
  triggerPrice: str | None
  """Trigger price for a TP/SL order, as a decimal string, or `null` for a normal order."""


class Open(RpcEndpoint):
  """`classic.spot.order.open`."""

  async def open(
    self,
    *,
    symbol: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    id_less_than: str | None = None,
    limit: int | None = None,
    order_id: str | None = None,
    tpsl_type: Literal['normal', 'tpsl'] | None = None,
    validate: bool | None = None,
  ) -> list[SpotOpenOrder]:
    """List currently open (unfilled or partially-filled) spot orders.

    Args:
      symbol: Trading pair symbol, e.g. `BTCUSDT`.
      start_time: Range start, Unix millisecond timestamp.
      end_time: Range end, Unix millisecond timestamp.
      id_less_than: Pagination cursor: return entries with an id less than this value (a `orderId`/`billId`/`tradeId` read out of the last row of the previous page).
      limit: Number of orders to return.
      order_id: Restrict to a single order ID.
      tpsl_type: Restrict to normal orders or TP/SL orders.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/trade/Get-Unfilled-Orders)
    """
    params = {}
    if symbol is not None:
      params['symbol'] = symbol
    if start_time is not None:
      params['startTime'] = timestamp_millis.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp_millis.dump(end_time)
    if id_less_than is not None:
      params['idLessThan'] = id_less_than
    if limit is not None:
      params['limit'] = limit
    if order_id is not None:
      params['orderId'] = order_id
    if tpsl_type is not None:
      params['tpslType'] = tpsl_type
    return await self.authed_request(
      'GET',
      '/api/v2/spot/trade/unfilled-orders',
      params=params,
      validator=validator(list[SpotOpenOrder]),
      validate=validate,
    )
