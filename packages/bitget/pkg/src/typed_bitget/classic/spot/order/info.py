# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import (
  SpotFeeDetail,
  SpotOrderSide,
  SpotOrderType,
  SpotTpslType,
)


class Request(TypedDict):
  orderId: NotRequired[str]
  """Exchange order ID to look up. Either this or `clientOid` is required."""
  clientOid: NotRequired[str]
  """Client order ID to look up. Either this or `orderId` is required."""


class SpotOrderInfo(TypedDict):
  """Full detail of one spot order."""

  userId: str
  """Account user ID that placed the order."""
  symbol: str
  """Trading pair symbol."""
  orderId: str
  """Exchange order ID."""
  clientOid: str
  """Client order ID."""
  price: Decimal
  """Order price, as a decimal string. `0` for a market order."""
  size: Decimal
  """Order size, as a decimal string."""
  orderType: SpotOrderType
  side: SpotOrderSide
  status: str
  """Current order status."""
  priceAvg: Decimal
  """Average fill price, as a decimal string."""
  baseVolume: Decimal
  """Filled amount in base coin, as a decimal string."""
  quoteVolume: Decimal
  """Filled amount in quote coin, as a decimal string."""
  enterPointSource: str
  """Origin of the order (e.g. API, web, app)."""
  feeDetail: SpotFeeDetail
  orderSource: str
  """Order source classification."""
  tpslType: SpotTpslType
  triggerPrice: str | None
  """Trigger price for a TP/SL order, as a decimal string, or `null` for a normal order."""
  quoteCoin: str
  """Quote coin of `symbol`."""
  baseCoin: str
  """Base coin of `symbol`."""
  cancelReason: str
  """Reason the order was cancelled, empty if not cancelled."""
  cTime: TimestampMillis
  """Creation time, Unix millisecond timestamp, as a string."""
  uTime: TimestampMillis
  """Last update time, Unix millisecond timestamp, as a string."""


Response = list[SpotOrderInfo]


class Info(RpcEndpoint):
  """Get full detail of one or more spot orders by ID."""

  async def info(
    self,
    *,
    order_id: str | None = None,
    client_oid: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get full detail of one or more spot orders by ID.

    Args:
      order_id: Exchange order ID to look up. Either this or `clientOid` is required.
      client_oid: Client order ID to look up. Either this or `orderId` is required.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-trade/classic-spot-trade#get-order-info)
    """
    request: Request = Request()
    if order_id is not None:
      request['orderId'] = order_id
    if client_oid is not None:
      request['clientOid'] = client_oid
    return await self.request(
      request,
      method='GET',
      path='/api/v2/spot/trade/orderInfo',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
