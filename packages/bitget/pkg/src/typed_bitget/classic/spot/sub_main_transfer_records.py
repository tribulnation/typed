# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import SpotAccountType


class Request(TypedDict):
  coin: NotRequired[str]
  """Restrict to a single coin."""
  role: NotRequired[Literal['initiator', 'receiver']]
  """Whether the caller was the transfer's initiator or receiver."""
  subUid: NotRequired[str]
  """Restrict to transfers involving a single sub-account."""
  startTime: NotRequired[TimestampMillis]
  """Range start, Unix millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """Range end, Unix millisecond timestamp."""
  clientOid: NotRequired[str]
  """Restrict to a single transfer by its client-supplied ID."""
  limit: NotRequired[int]
  """Number of records to return."""
  idLessThan: NotRequired[str]
  """Pagination cursor: return entries with an id less than this value (a `orderId`/`billId`/`tradeId` read out of the last row of the previous page)."""


class SpotMainSubTransferRecord(TypedDict):
  """One main-account/sub-account transfer."""

  coin: str
  """Coin transferred."""
  status: str
  """Current status of the transfer."""
  toType: SpotAccountType
  fromType: SpotAccountType
  size: str
  """Transferred amount, as a decimal string."""
  ts: TimestampMillis
  """Transfer time, Unix millisecond timestamp, as a string."""
  clientOid: str
  """Caller-supplied client ID for this transfer."""
  transferId: str
  """Exchange transfer ID."""
  fromUserId: str
  """User ID the transfer moved funds from."""
  toUserId: str
  """User ID the transfer moved funds to."""


Response = list[SpotMainSubTransferRecord]


class SubMainTransferRecords(RpcEndpoint):
  """List transfer records between the main account and its sub-accounts. Callable from either side."""

  async def sub_main_transfer_records(
    self,
    *,
    coin: str | None = None,
    role: Literal['initiator', 'receiver'] | None = None,
    sub_uid: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    client_oid: str | None = None,
    limit: int | None = None,
    id_less_than: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """List transfer records between the main account and its sub-accounts. Callable from either side.

    Args:
      coin: Restrict to a single coin.
      role: Whether the caller was the transfer's initiator or receiver.
      sub_uid: Restrict to transfers involving a single sub-account.
      start_time: Range start, Unix millisecond timestamp.
      end_time: Range end, Unix millisecond timestamp.
      client_oid: Restrict to a single transfer by its client-supplied ID.
      limit: Number of records to return.
      id_less_than: Pagination cursor: return entries with an id less than this value (a `orderId`/`billId`/`tradeId` read out of the last row of the previous page).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-account/classic-spot-account#get-mainsub-transfer-record)
    """
    request: Request = Request()
    if coin is not None:
      request['coin'] = coin
    if role is not None:
      request['role'] = role
    if sub_uid is not None:
      request['subUid'] = sub_uid
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if client_oid is not None:
      request['clientOid'] = client_oid
    if limit is not None:
      request['limit'] = limit
    if id_less_than is not None:
      request['idLessThan'] = id_less_than
    return await self.request(
      request,
      method='GET',
      path='/api/v2/spot/account/sub-main-trans-record',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
