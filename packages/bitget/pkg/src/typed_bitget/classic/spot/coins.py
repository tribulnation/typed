# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.coins` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SpotCoinChain(TypedDict):
  """Deposit/withdrawal configuration for one coin on one chain."""

  chain: str
  """Chain name, e.g. `BTC`, `ERC20`."""
  needTag: bool
  """Whether a memo/tag is required alongside the address."""
  withdrawable: bool
  """Whether withdrawal is currently enabled on this chain."""
  rechargeable: bool
  """Whether deposit is currently enabled on this chain."""
  withdrawFee: str
  """Flat withdrawal fee, as a decimal string."""
  extraWithdrawFee: str
  """Additional withdrawal fee (e.g. a chain congestion surcharge), as a decimal string."""
  depositConfirm: int
  """Block confirmations required before a deposit is credited."""
  withdrawConfirm: int
  """Block confirmations required before a withdrawal is considered final."""
  minDepositAmount: str
  """Minimum deposit amount, as a decimal string."""
  minWithdrawAmount: str
  """Minimum withdrawal amount, as a decimal string."""
  browserUrl: str
  """Block-explorer URL template for this chain."""
  contractAddress: str | None
  """Token contract address on this chain. Confirmed live: `null` (not an empty string) for a native coin."""
  withdrawStep: str
  """Withdrawal amount step size, as a decimal string."""
  withdrawMinScale: int
  """Minimum decimal precision accepted for a withdrawal amount."""
  congestion: str
  """Current chain congestion status."""


class SpotCoin(TypedDict):
  """One coin tradable on Bitget spot."""

  coinId: str
  """Bitget-internal numeric coin ID, as a string."""
  coin: str
  """Coin ticker, e.g. `BTC`."""
  transfer: bool
  """Whether this coin can be transferred between Bitget product accounts."""
  chains: list[SpotCoinChain]
  """Chains this coin can be deposited/withdrawn on."""


class Coins(RpcEndpoint):
  """`classic.spot.coins`."""

  async def coins(
    self,
    *,
    coin: str | None = None,
    validate: bool | None = None,
  ) -> list[SpotCoin]:
    """List coins available on Bitget spot, including per-chain deposit/withdrawal configuration. Public, no authentication required.

    Args:
      coin: Restrict to a single coin, e.g. `BTC`.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/public/Get-Coin-List)
    """
    params = {}
    if coin is not None:
      params['coin'] = coin
    return await self.request(
      'GET',
      '/api/v2/spot/public/coins',
      params=params,
      validator=validator(list[SpotCoin]),
      validate=validate,
    )
