# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.switch_bgb_deduct` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SpotSwitchBgbDeductRequest(TypedDict):
  """Desired BGB fee-deduction state."""

  deduct: bool
  """Whether spot trading fees should be deducted from the BGB balance when available."""


class SwitchBgbDeduct(RpcEndpoint):
  """`classic.spot.switch_bgb_deduct`."""

  async def switch_bgb_deduct(
    self,
    spot_switch_bgb_deduct_request: SpotSwitchBgbDeductRequest,
    *,
    validate: bool | None = None,
  ) -> bool:
    """Turn spot trading-fee deduction via BGB on or off for the caller.

    Args:
      spot_switch_bgb_deduct_request: Desired BGB fee-deduction state.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/account/Switch-Deduct)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/spot/account/switch-deduct',
      json=spot_switch_bgb_deduct_request,
      validator=validator(bool),
      validate=validate,
    )
