# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.withdraw` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.types import SpotOrderIdResult
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SpotWithdrawalRequest(TypedDict):
  """A withdrawal instruction."""

  coin: str
  """Coin to withdraw."""
  transferType: Literal['on_chain', 'internal_transfer']
  """Whether this is an on-chain withdrawal or an internal transfer to another Bitget account."""
  address: str
  """Destination address, or the recipient email/mobile/UID identifier for an internal transfer."""
  chain: NotRequired[str]
  """Chain to withdraw on. Required for an on-chain withdrawal of a multi-chain coin."""
  innerToType: NotRequired[Literal['email', 'mobile', 'uid']]
  """Identifier type `address` holds, for an internal transfer."""
  areaCode: NotRequired[str]
  """Country dialing code, when `innerToType` is `mobile`."""
  tag: NotRequired[str]
  """Destination tag/memo, for a chain that requires one."""
  size: str
  """Amount to withdraw, as a decimal string."""
  remark: NotRequired[str]
  """Caller-supplied note for this withdrawal."""
  clientOid: NotRequired[str]
  """Caller-supplied client ID for this withdrawal."""


class Withdraw(RpcEndpoint):
  """`classic.spot.withdraw`."""

  async def withdraw(
    self,
    spot_withdrawal_request: SpotWithdrawalRequest,
    *,
    validate: bool | None = None,
  ) -> SpotOrderIdResult:
    """Withdraw a coin on-chain or to another Bitget account internally.

    Args:
      spot_withdrawal_request: A withdrawal instruction.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/wallet/Withdrawal)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/spot/wallet/withdrawal',
      json=spot_withdrawal_request,
      validator=validator(SpotOrderIdResult),
      validate=validate,
    )
