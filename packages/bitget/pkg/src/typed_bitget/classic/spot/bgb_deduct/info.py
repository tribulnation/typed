# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SpotBgbDeductInfo(TypedDict):
  """The caller's BGB fee-deduction setting."""

  deduct: str
  """Whether spot trading fees are currently deducted from the BGB balance when available."""


class Info(RpcEndpoint):
  """Get the caller's current spot BGB trading-fee deduction setting."""

  async def info(self, *, validate: bool | None = None) -> SpotBgbDeductInfo:
    """Get the caller's current spot BGB trading-fee deduction setting.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-account/classic-spot-account#get-bgb-deduct-info)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v2/spot/account/deduct-info',
      meta={'required': True},
      validate=validate,
      response_type=SpotBgbDeductInfo,
    )
