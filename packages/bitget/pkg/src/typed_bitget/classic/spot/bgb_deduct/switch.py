# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Desired BGB fee-deduction state."""

  deduct: bool
  """Whether spot trading fees should be deducted from the BGB balance when available."""


Response = bool


class Switch(RpcEndpoint):
  """Turn spot trading-fee deduction via BGB on or off for the caller."""

  async def switch(self, deduct: bool, *, validate: bool | None = None) -> Response:
    """Turn spot trading-fee deduction via BGB on or off for the caller.

    Args:
      deduct: Whether spot trading fees should be deducted from the BGB balance when available.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-account/classic-spot-account#switch-bgb-deduct)
    """
    return await self.request(
      Request(deduct=deduct),
      method='POST',
      path='/api/v2/spot/account/switch-deduct',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
