# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  symbol: str
  """Trading pair symbol, e.g. `BTCUSDT`."""


class SpotFundFlow(TypedDict):
  """Capital flow for one symbol, segmented by trader size. Confirmed live: no `symbol` field on the response frame, and each size tier reports a separate buy-side and sell-side ratio rather than one combined ratio."""

  whaleBuyVolume: Decimal
  """Buy volume attributed to whale-sized traders, as a decimal string."""
  whaleSellVolume: Decimal
  """Sell volume attributed to whale-sized traders, as a decimal string."""
  whaleBuyRatio: Decimal
  """Whale traders' share of total buy volume, as a fraction."""
  whaleSellRatio: Decimal
  """Whale traders' share of total sell volume, as a fraction."""
  dolphinBuyVolume: Decimal
  """Buy volume attributed to dolphin-sized (mid-size) traders, as a decimal string."""
  dolphinSellVolume: Decimal
  """Sell volume attributed to dolphin-sized (mid-size) traders, as a decimal string."""
  dolphinBuyRatio: Decimal
  """Dolphin traders' share of total buy volume, as a fraction."""
  dolphinSellRatio: Decimal
  """Dolphin traders' share of total sell volume, as a fraction."""
  fishBuyVolume: Decimal
  """Buy volume attributed to fish-sized (small) traders, as a decimal string."""
  fishSellVolume: Decimal
  """Sell volume attributed to fish-sized (small) traders, as a decimal string."""
  fishBuyRatio: Decimal
  """Fish traders' share of total buy volume, as a fraction."""
  fishSellRatio: Decimal
  """Fish traders' share of total sell volume, as a fraction."""


class FundFlow(RpcEndpoint):
  """Get capital flow for a symbol segmented by trader size (whale/dolphin/fish buy and sell volumes). Public, no authentication required."""

  async def fund_flow(
    self, symbol: str, *, validate: bool | None = None
  ) -> SpotFundFlow:
    """Get capital flow for a symbol segmented by trader size (whale/dolphin/fish buy and sell volumes). Public, no authentication required.

    Args:
      symbol: Trading pair symbol, e.g. `BTCUSDT`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-common-apidata/classic-common-apidata#get-spot-fund-flow)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/api/v2/spot/market/fund-flow',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=SpotFundFlow,
    )
