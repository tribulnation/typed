# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .account import Account
from .bgb_deduct import BgbDeduct
from .call_auction import CallAuction
from .candles import Candles
from .coins import Coins
from .deposit import Deposit
from .fund_flow import FundFlow
from .historical_trades import HistoricalTrades
from .history_candles import HistoryCandles
from .merge_depth import MergeDepth
from .order import Order
from .orderbook import Orderbook
from .recent_trades import RecentTrades
from .sub_main_transfer_records import SubMainTransferRecords
from .subaccount import Subaccount
from .symbols import Symbols
from .tickers import Tickers
from .transfer import Transfer
from .upgrade_status import UpgradeStatus
from .upgrade_to_uta import UpgradeToUta
from .vip_fee_rate import VipFeeRate
from .whale_net_flow import WhaleNetFlow
from .withdrawal import Withdrawal


class Spot(
  CallAuction,
  Candles,
  Coins,
  FundFlow,
  HistoricalTrades,
  HistoryCandles,
  MergeDepth,
  Orderbook,
  RecentTrades,
  SubMainTransferRecords,
  Symbols,
  Tickers,
  UpgradeStatus,
  UpgradeToUta,
  VipFeeRate,
  WhaleNetFlow,
):
  """Bitget Classic's Spot API: account balances, market data, order placement and management, deposits/withdrawals, transfers, and sub-account operations for spot trading.

  References:
    - [Upstream docs](https://www.bitget.com/docs/catalog/classic-spot-trade)
  """

  @cached_property
  def account(self) -> Account:
    """Bitget Classic's Spot Account API: account assets, bills, account info, and the account's spot/futures upgrade status.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-spot-account)
    """
    return Account(client=self.client)

  @cached_property
  def bgb_deduct(self) -> BgbDeduct:
    """Bitget Classic's Spot BGB Fee-Deduction API: querying and switching whether trading fees are deducted in BGB.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-spot-account)
    """
    return BgbDeduct(client=self.client)

  @cached_property
  def deposit(self) -> Deposit:
    """Bitget Classic's Spot Deposit API: deposit address lookup/modification and deposit records.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-spot-account)
    """
    return Deposit(client=self.client)

  @cached_property
  def order(self) -> Order:
    """Bitget Classic's Spot Order API: placing, cancelling (single, symbol-wide, and cancel-replace), and querying open/historical spot orders and fills.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-spot-trade)
    """
    return Order(client=self.client)

  @cached_property
  def subaccount(self) -> Subaccount:
    """Bitget Classic's Spot Sub-Account API: sub-account asset queries, deposit address/records, and internal sub-account transfers.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-spot-account)
    """
    return Subaccount(client=self.client)

  @cached_property
  def transfer(self) -> Transfer:
    """Bitget Classic's Spot Transfer API: transferring between account types, transfer history, and the list of transferable coins.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-spot-account)
    """
    return Transfer(client=self.client)

  @cached_property
  def withdrawal(self) -> Withdrawal:
    """Bitget Classic's Spot Withdrawal API: withdrawing, cancelling a pending withdrawal, and withdrawal records.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-spot-account)
    """
    return Withdrawal(client=self.client)
