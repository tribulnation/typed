# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.order_info` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SpotOrderInfo(TypedDict):
  """Full detail of one spot order."""

  userId: str
  """Account user ID that placed the order."""
  symbol: str
  """Trading pair symbol."""
  orderId: str
  """Exchange order ID."""
  clientOid: str
  """Client order ID."""
  price: str
  """Order price, as a decimal string. `0` for a market order."""
  size: str
  """Order size, as a decimal string."""
  orderType: Literal['limit', 'market']
  """Order type."""
  side: Literal['buy', 'sell']
  """Order side."""
  status: str
  """Current order status."""
  priceAvg: str
  """Average fill price, as a decimal string."""
  baseVolume: str
  """Filled amount in base coin, as a decimal string."""
  quoteVolume: str
  """Filled amount in quote coin, as a decimal string."""
  enterPointSource: str
  """Origin of the order (e.g. API, web, app)."""
  feeDetail: str
  """Fee breakdown, as compact JSON text (not a parsed object) keyed by the coin the fee was charged in, plus a `newFees` summary entry. Confirmed live on `trade.order_info`/`trade.order_history`: the field is a JSON-encoded string on the wire (e.g. `'{"newFees":{...},"USDC":{...}}'`), not the object the closed-set field names would suggest -- empty string when the order has no fee yet (e.g. a just-cancelled order)."""
  orderSource: str
  """Order source classification."""
  tpslType: Literal['normal', 'tpsl']
  """Whether this is a normal order or a take-profit/stop-loss order."""
  triggerPrice: str | None
  """Trigger price for a TP/SL order, as a decimal string, or `null` for a normal order."""
  quoteCoin: str
  """Quote coin of `symbol`."""
  baseCoin: str
  """Base coin of `symbol`."""
  cancelReason: str
  """Reason the order was cancelled, empty if not cancelled."""
  cTime: TimestampMillis
  """Creation time, Unix millisecond timestamp, as a string."""
  uTime: TimestampMillis
  """Last update time, Unix millisecond timestamp, as a string."""


class OrderInfo(RpcEndpoint):
  """`classic.spot.order_info`."""

  async def order_info(
    self,
    *,
    order_id: str | None = None,
    client_oid: str | None = None,
    validate: bool | None = None,
  ) -> list[SpotOrderInfo]:
    """Get full detail of one or more spot orders by ID.

    Args:
      order_id: Exchange order ID to look up. Either this or `clientOid` is required.
      client_oid: Client order ID to look up. Either this or `orderId` is required.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/trade/Get-Order-Info)
    """
    params = {}
    if order_id is not None:
      params['orderId'] = order_id
    if client_oid is not None:
      params['clientOid'] = client_oid
    return await self.authed_request(
      'GET',
      '/api/v2/spot/trade/orderInfo',
      params=params,
      validator=validator(list[SpotOrderInfo]),
      validate=validate,
    )
