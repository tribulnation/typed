# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.batch_cancel_orders` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SpotBatchCancelOrderItem(TypedDict):
  """One order to cancel."""

  symbol: NotRequired[str]
  """Trading pair symbol, when `batchMode` is `multiple`."""
  orderId: NotRequired[str]
  """Exchange order ID. Either this or `clientOid` is required."""
  clientOid: NotRequired[str]
  """Client order ID. Either this or `orderId` is required."""


class SpotBatchOrderFailure(TypedDict):
  """One order that failed."""

  orderId: str
  """Exchange order ID, if one was assigned before failing."""
  clientOid: str
  """Client order ID."""
  errorMsg: str
  """Failure reason."""
  errorCode: NotRequired[str]
  """Failure error code."""


class SpotBatchOrderSuccess(TypedDict):
  """One order that succeeded."""

  orderId: str
  """Exchange order ID."""
  clientOid: str
  """Client order ID."""


class SpotBatchCancelOrdersRequest(TypedDict):
  """A batch of orders to cancel."""

  symbol: NotRequired[str]
  """Trading pair symbol shared by every order in the batch, when `batchMode` is `single`."""
  batchMode: NotRequired[Literal['single', 'multiple']]
  """Whether every order in the batch shares one `symbol` (`single`) or each specifies its own (`multiple`)."""
  orderList: list[SpotBatchCancelOrderItem]
  """Orders to cancel, up to 50."""


class SpotBatchOrdersResult(TypedDict):
  """Per-order results of a batch operation."""

  successList: list[SpotBatchOrderSuccess]
  """Orders that succeeded."""
  failureList: list[SpotBatchOrderFailure]
  """Orders that failed."""


class BatchCancelOrders(RpcEndpoint):
  """`classic.spot.batch_cancel_orders`."""

  async def batch_cancel_orders(
    self,
    spot_batch_cancel_orders_request: SpotBatchCancelOrdersRequest,
    *,
    validate: bool | None = None,
  ) -> SpotBatchOrdersResult:
    """Cancel up to 50 open spot orders in one call.

    Args:
      spot_batch_cancel_orders_request: A batch of orders to cancel.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/trade/Batch-Cancel-Orders)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/spot/trade/batch-cancel-order',
      json=spot_batch_cancel_orders_request,
      validator=validator(SpotBatchOrdersResult),
      validate=validate,
    )
