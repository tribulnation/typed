# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.call_auction` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SpotCallAuction(TypedDict):
  """Call-auction stage data for one symbol."""

  stage: str | None
  """Current call-auction stage, or `null` when the symbol is not currently in a call-auction period."""
  stageEndTime: TimestampMillis | None
  """End time of the current stage, Unix millisecond timestamp, as a string, or `null` when the symbol is not currently in a call-auction period."""
  estOpeningPrice: str | None
  """Estimated opening price, as a decimal string, or `null` if not yet determinable."""
  matchedVolume: str | None
  """Currently matched volume at the estimated opening price, as a decimal string, or `null` if not yet determinable."""
  auctionEndTime: TimestampMillis | None
  """End time of the whole auction period, Unix millisecond timestamp, as a string, or `null` when the symbol is not currently in a call-auction period."""


class CallAuction(RpcEndpoint):
  """`classic.spot.call_auction`."""

  async def call_auction(
    self,
    *,
    symbol: str,
    validate: bool | None = None,
  ) -> SpotCallAuction:
    """Get call-auction (opening-auction) stage data for a symbol currently in a call-auction period (e.g. a new listing). Public, no authentication required.

    Args:
      symbol: Trading pair symbol, e.g. `BTCUSDT`.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/market/Get-Call-Auction)
    """
    params: dict = {
      'symbol': symbol,
    }
    return await self.request(
      'GET',
      '/api/v2/spot/market/auction',
      params=params,
      validator=validator(SpotCallAuction),
      validate=validate,
    )
