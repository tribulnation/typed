# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.place_plan_order` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.types import SpotOrderIdResult
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SpotPlanLimitOrderRequest(TypedDict):
  """A spot plan order that submits a limit order once triggered."""

  symbol: str
  """Trading pair symbol."""
  side: Literal['buy', 'sell']
  """Order side."""
  triggerPrice: str
  """Price that triggers submission of the order, as a decimal string."""
  orderType: Literal['limit']
  """Order type."""
  executePrice: str
  """Limit price of the order once triggered, as a decimal string."""
  planType: NotRequired[Literal['amount', 'total']]
  """Whether `size` is the base-coin amount to trade (`amount`) or the total quote-coin value (`total`)."""
  size: str
  """Order size, as a decimal string; base coin or quote coin depending on `planType`."""
  triggerType: Literal['fill_price', 'mark_price']
  """Price type the trigger condition is evaluated against."""
  clientOid: NotRequired[str]
  """Caller-supplied client order ID, echoed back on the plan order."""
  force: NotRequired[Literal['gtc', 'post_only', 'fok', 'ioc']]
  """Time-in-force / execution strategy."""
  stpMode: NotRequired[Literal['none', 'cancel_taker', 'cancel_maker', 'cancel_both']]
  """Self-trade prevention mode."""


class SpotPlanMarketOrderRequest(TypedDict):
  """A spot plan order that submits a market order once triggered."""

  symbol: str
  """Trading pair symbol."""
  side: Literal['buy', 'sell']
  """Order side."""
  triggerPrice: str
  """Price that triggers submission of the order, as a decimal string."""
  orderType: Literal['market']
  """Order type."""
  planType: NotRequired[Literal['amount', 'total']]
  """Whether `size` is the base-coin amount to trade (`amount`) or the total quote-coin value (`total`)."""
  size: str
  """Order size, as a decimal string; base coin or quote coin depending on `planType`."""
  triggerType: Literal['fill_price', 'mark_price']
  """Price type the trigger condition is evaluated against."""
  clientOid: NotRequired[str]
  """Caller-supplied client order ID, echoed back on the plan order."""
  force: NotRequired[Literal['gtc', 'post_only', 'fok', 'ioc']]
  """Time-in-force / execution strategy."""
  stpMode: NotRequired[Literal['none', 'cancel_taker', 'cancel_maker', 'cancel_both']]
  """Self-trade prevention mode."""


class PlacePlanOrder(RpcEndpoint):
  """`classic.spot.place_plan_order`."""

  async def place_plan_order(
    self,
    spot_plan_limit_order_request_spot_plan_market_order_request: SpotPlanLimitOrderRequest
    | SpotPlanMarketOrderRequest,
    *,
    validate: bool | None = None,
  ) -> SpotOrderIdResult:
    """Place a spot plan (conditional/trigger) order: an order that is submitted to the book only once a trigger condition is met.

    Args:
      spot_plan_limit_order_request_spot_plan_market_order_request: A plan order to place.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/plan/Place-Plan-Order)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/spot/trade/place-plan-order',
      json=spot_plan_limit_order_request_spot_plan_market_order_request,
      validator=validator(SpotOrderIdResult),
      validate=validate,
    )
