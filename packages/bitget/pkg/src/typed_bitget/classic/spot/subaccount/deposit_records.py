# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  subUid: str
  """Sub-account user ID to list deposits for."""
  coin: NotRequired[str]
  """Restrict to a single coin."""
  startTime: NotRequired[TimestampMillis]
  """Range start, Unix millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """Range end, Unix millisecond timestamp."""
  idLessThan: NotRequired[str]
  """Pagination cursor: return entries with an id less than this value (a `orderId`/`billId`/`tradeId` read out of the last row of the previous page)."""
  limit: NotRequired[int]
  """Number of records to return."""


class SpotSubAccountDepositRecord(TypedDict):
  """One sub-account deposit."""

  orderId: str
  """Exchange deposit order ID."""
  tradeId: str
  """On-chain transaction ID."""
  coin: str
  """Coin deposited."""
  size: str
  """Deposited amount, as a decimal string."""
  status: str
  """Current deposit status."""
  toAddress: str
  """Destination address."""
  dest: str
  """Deposit method (e.g. on-chain vs internal)."""
  chain: str
  """Chain the deposit was made on."""
  fromAddress: str
  """Source address, if known."""
  clientOid: NotRequired[str]
  """Caller-supplied client ID, if this was an internal transfer."""
  confirm: NotRequired[str]
  """Current block confirmations."""
  tag: NotRequired[str]
  """Memo/tag included with the deposit, if any."""
  cTime: TimestampMillis
  """Creation time, Unix millisecond timestamp, as a string."""
  uTime: TimestampMillis
  """Last update time, Unix millisecond timestamp, as a string."""


Response = list[SpotSubAccountDepositRecord]


class DepositRecords(RpcEndpoint):
  """List a sub-account's deposit records. Called from the main account."""

  async def deposit_records(
    self,
    *,
    sub_uid: str,
    coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    id_less_than: str | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """List a sub-account's deposit records. Called from the main account.

    Args:
      sub_uid: Sub-account user ID to list deposits for.
      coin: Restrict to a single coin.
      start_time: Range start, Unix millisecond timestamp.
      end_time: Range end, Unix millisecond timestamp.
      id_less_than: Pagination cursor: return entries with an id less than this value (a `orderId`/`billId`/`tradeId` read out of the last row of the previous page).
      limit: Number of records to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-account/classic-spot-account#get-subaccount-deposit-records)
    """
    request: Request = Request(subUid=sub_uid)
    if coin is not None:
      request['coin'] = coin
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if id_less_than is not None:
      request['idLessThan'] = id_less_than
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/api/v2/spot/wallet/subaccount-deposit-records',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
