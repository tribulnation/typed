# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SpotSubAccountAsset(TypedDict):
  """Balance of one coin in a sub-account."""

  coin: str
  """Coin ticker."""
  available: str
  """Balance available to trade, as a decimal string."""
  limitAvailable: str
  """Balance available under any applicable limit, as a decimal string."""
  frozen: str
  """Balance frozen by open orders, as a decimal string."""
  locked: str
  """Balance locked (e.g. by a pending withdrawal), as a decimal string."""
  uTime: TimestampMillis
  """Last update time, Unix millisecond timestamp, as a string."""


class SpotSubAccountAssets(TypedDict):
  """One sub-account's spot balances."""

  userId: int
  """Sub-account user ID."""
  assetsList: list[SpotSubAccountAsset]
  """This sub-account's balances."""


Response = list[SpotSubAccountAssets]


class Assets(RpcEndpoint):
  """List every sub-account's spot balances, called from the main account."""

  async def assets(self, *, validate: bool | None = None) -> Response:
    """List every sub-account's spot balances, called from the main account.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-account/classic-spot-account#get-sub-accounts-assets)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v2/spot/account/subaccount-assets',
      meta={'required': True},
      validate=validate,
      response_type=Response,
    )
