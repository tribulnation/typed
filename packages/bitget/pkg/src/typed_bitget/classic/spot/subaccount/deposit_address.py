# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  subUid: str
  """Sub-account user ID to get a deposit address for."""
  coin: str
  """Coin to get a deposit address for."""
  chain: NotRequired[str]
  """Chain to get a deposit address on. Required for a multi-chain coin."""
  size: str
  """Reserved for future use; the reference SDK requires it with no documented meaning."""


class SpotDepositAddress(TypedDict):
  """A deposit address for one coin on one chain."""

  address: str
  """Deposit address."""
  chain: str
  """Chain this address is on."""
  coin: str
  """Coin this address accepts."""
  tag: str
  """Memo/tag to include with the deposit, empty if not required."""
  url: str
  """Block-explorer URL for this address."""


class DepositAddress(RpcEndpoint):
  """Get (or generate) a sub-account's deposit address for a coin. Called from the main account."""

  async def deposit_address(
    self,
    *,
    sub_uid: str,
    coin: str,
    chain: str | None = None,
    size: str,
    validate: bool | None = None,
  ) -> SpotDepositAddress:
    """Get (or generate) a sub-account's deposit address for a coin. Called from the main account.

    Args:
      sub_uid: Sub-account user ID to get a deposit address for.
      coin: Coin to get a deposit address for.
      chain: Chain to get a deposit address on. Required for a multi-chain coin.
      size: Reserved for future use; the reference SDK requires it with no documented meaning.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-account/classic-spot-account#get-subaccount-deposit-address)
    """
    request: Request = Request(subUid=sub_uid, coin=coin, size=size)
    if chain is not None:
      request['chain'] = chain
    return await self.request(
      request,
      method='GET',
      path='/api/v2/spot/wallet/subaccount-deposit-address',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SpotDepositAddress,
    )
