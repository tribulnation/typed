# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.subaccount.transfer` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SpotSubAccountTransferRequest(TypedDict):
  """A sub-account transfer."""

  fromType: Literal[
    'spot',
    'p2p',
    'coin_futures',
    'usdt_futures',
    'usdc_futures',
    'crossed_margin',
    'isolated_margin',
  ]
  """Bitget product-account type."""
  toType: Literal[
    'spot',
    'p2p',
    'coin_futures',
    'usdt_futures',
    'usdc_futures',
    'crossed_margin',
    'isolated_margin',
  ]
  """Bitget product-account type."""
  amount: str
  """Amount to transfer, as a decimal string."""
  coin: str
  """Coin to transfer."""
  symbol: NotRequired[str]
  """Margin symbol, required when `fromType`/`toType` is an isolated-margin account."""
  clientOid: NotRequired[str]
  """Caller-supplied client ID for this transfer."""
  fromUserId: str
  """User ID to transfer from."""
  toUserId: str
  """User ID to transfer to."""


class SpotSubAccountTransferResult(TypedDict):
  """Result of a sub-account transfer."""

  transferId: str
  """Exchange transfer ID."""
  clientOid: str
  """Client ID of the transfer."""


class Transfer(RpcEndpoint):
  """`classic.spot.subaccount.transfer`."""

  async def transfer(
    self,
    spot_sub_account_transfer_request: SpotSubAccountTransferRequest,
    *,
    validate: bool | None = None,
  ) -> SpotSubAccountTransferResult:
    """Transfer funds between two sub-accounts, or a sub-account and the main account. Called from the main account.

    Args:
      spot_sub_account_transfer_request: A sub-account transfer.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/wallet/Sub-Transfer)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/spot/wallet/subaccount-transfer',
      json=spot_sub_account_transfer_request,
      validator=validator(SpotSubAccountTransferResult),
      validate=validate,
    )
