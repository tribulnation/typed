# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import SpotAccountType


class SpotSubAccountTransferResult(TypedDict):
  """Result of a sub-account transfer."""

  transferId: str
  """Exchange transfer ID."""
  clientOid: str
  """Client ID of the transfer."""


class Request(TypedDict):
  """A sub-account transfer."""

  fromType: SpotAccountType
  toType: SpotAccountType
  amount: str
  """Amount to transfer, as a decimal string."""
  coin: str
  """Coin to transfer."""
  symbol: NotRequired[str]
  """Margin symbol, required when `fromType`/`toType` is an isolated-margin account."""
  clientOid: NotRequired[str]
  """Caller-supplied client ID for this transfer."""
  fromUserId: str
  """User ID to transfer from."""
  toUserId: str
  """User ID to transfer to."""


class Transfer(RpcEndpoint):
  """Transfer funds between two sub-accounts, or a sub-account and the main account. Called from the main account."""

  async def transfer(
    self,
    *,
    from_type: SpotAccountType,
    to_type: SpotAccountType,
    amount: str,
    coin: str,
    symbol: str | None = None,
    client_oid: str | None = None,
    from_user_id: str,
    to_user_id: str,
    validate: bool | None = None,
  ) -> SpotSubAccountTransferResult:
    """Transfer funds between two sub-accounts, or a sub-account and the main account. Called from the main account.

    Args:
      from_type:
      to_type:
      amount: Amount to transfer, as a decimal string.
      coin: Coin to transfer.
      symbol: Margin symbol, required when `fromType`/`toType` is an isolated-margin account.
      client_oid: Caller-supplied client ID for this transfer.
      from_user_id: User ID to transfer from.
      to_user_id: User ID to transfer to.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-account/classic-spot-account#sub-transfer)
    """
    request: Request = Request(
      fromType=from_type,
      toType=to_type,
      amount=amount,
      coin=coin,
      fromUserId=from_user_id,
      toUserId=to_user_id,
    )
    if symbol is not None:
      request['symbol'] = symbol
    if client_oid is not None:
      request['clientOid'] = client_oid
    return await self.request(
      request,
      method='POST',
      path='/api/v2/spot/wallet/subaccount-transfer',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SpotSubAccountTransferResult,
    )
