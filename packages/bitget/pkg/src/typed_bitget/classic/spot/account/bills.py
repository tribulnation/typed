# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  coin: NotRequired[str]
  """Restrict to a single coin."""
  groupType: NotRequired[
    Literal['deposit', 'withdraw', 'transaction', 'transfer', 'financial', 'other']
  ]
  """Broad category of the ledger entry."""
  businessType: NotRequired[str]
  """Specific business event that produced this ledger entry."""
  startTime: NotRequired[TimestampMillis]
  """Range start, Unix millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """Range end, Unix millisecond timestamp."""
  limit: NotRequired[int]
  """Number of bills to return."""
  idLessThan: NotRequired[str]
  """Pagination cursor: return entries with an id less than this value (a `orderId`/`billId`/`tradeId` read out of the last row of the previous page)."""


class SpotAccountBill(TypedDict):
  """One spot account ledger entry."""

  cTime: TimestampMillis
  """Entry time, Unix millisecond timestamp, as a string."""
  bizOrderId: str
  """ID of the order, deposit, or withdrawal this ledger entry is linked to. Not documented; confirmed present live."""
  coin: str
  """Coin this entry affected."""
  groupType: Literal[
    'deposit', 'withdraw', 'transaction', 'transfer', 'financial', 'other'
  ]
  """Broad category of the ledger entry."""
  businessType: str
  """Specific business event that produced this ledger entry."""
  size: Decimal
  """Signed amount of this entry, as a decimal string."""
  balance: Decimal
  """Resulting balance after this entry, as a decimal string."""
  fees: Decimal
  """Fee charged by this entry, as a decimal string."""
  billId: str
  """Ledger entry ID."""


Response = list[SpotAccountBill]


class Bills(RpcEndpoint):
  """List the caller's spot account ledger entries (bills)."""

  async def bills(
    self,
    *,
    coin: str | None = None,
    group_type: Literal[
      'deposit', 'withdraw', 'transaction', 'transfer', 'financial', 'other'
    ]
    | None = None,
    business_type: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    id_less_than: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """List the caller's spot account ledger entries (bills).

    Args:
      coin: Restrict to a single coin.
      group_type: Broad category of the ledger entry.
      business_type: Specific business event that produced this ledger entry.
      start_time: Range start, Unix millisecond timestamp.
      end_time: Range end, Unix millisecond timestamp.
      limit: Number of bills to return.
      id_less_than: Pagination cursor: return entries with an id less than this value (a `orderId`/`billId`/`tradeId` read out of the last row of the previous page).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-account/classic-spot-account#get-account-bills)
    """
    request: Request = Request()
    if coin is not None:
      request['coin'] = coin
    if group_type is not None:
      request['groupType'] = group_type
    if business_type is not None:
      request['businessType'] = business_type
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    if id_less_than is not None:
      request['idLessThan'] = id_less_than
    return await self.request(
      request,
      method='GET',
      path='/api/v2/spot/account/bills',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
