# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.account.bills` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis, timestamp_millis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SpotAccountBill(TypedDict):
  """One spot account ledger entry."""

  cTime: TimestampMillis
  """Entry time, Unix millisecond timestamp, as a string."""
  bizOrderId: str
  """ID of the order, deposit, or withdrawal this ledger entry is linked to. Not documented; confirmed present live."""
  coin: str
  """Coin this entry affected."""
  groupType: Literal[
    'deposit', 'withdraw', 'transaction', 'transfer', 'financial', 'other'
  ]
  """Broad category of the ledger entry."""
  businessType: str
  """Specific business event that produced this ledger entry."""
  size: str
  """Signed amount of this entry, as a decimal string."""
  balance: str
  """Resulting balance after this entry, as a decimal string."""
  fees: str
  """Fee charged by this entry, as a decimal string."""
  billId: str
  """Ledger entry ID."""


class Bills(RpcEndpoint):
  """`classic.spot.account.bills`."""

  async def bills(
    self,
    *,
    coin: str | None = None,
    group_type: Literal[
      'deposit', 'withdraw', 'transaction', 'transfer', 'financial', 'other'
    ]
    | None = None,
    business_type: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    id_less_than: str | None = None,
    validate: bool | None = None,
  ) -> list[SpotAccountBill]:
    """List the caller's spot account ledger entries (bills).

    Args:
      coin: Restrict to a single coin.
      group_type: Restrict to a broad ledger-entry category.
      business_type: Restrict to a specific business event.
      start_time: Range start, Unix millisecond timestamp.
      end_time: Range end, Unix millisecond timestamp.
      limit: Number of bills to return.
      id_less_than: Pagination cursor: return entries with an id less than this value (a `orderId`/`billId`/`tradeId` read out of the last row of the previous page).

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/account/Get-Account-Bills)
    """
    params = {}
    if coin is not None:
      params['coin'] = coin
    if group_type is not None:
      params['groupType'] = group_type
    if business_type is not None:
      params['businessType'] = business_type
    if start_time is not None:
      params['startTime'] = timestamp_millis.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp_millis.dump(end_time)
    if limit is not None:
      params['limit'] = limit
    if id_less_than is not None:
      params['idLessThan'] = id_less_than
    return await self.authed_request(
      'GET',
      '/api/v2/spot/account/bills',
      params=params,
      validator=validator(list[SpotAccountBill]),
      validate=validate,
    )
