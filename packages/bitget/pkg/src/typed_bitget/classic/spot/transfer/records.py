# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import SpotAccountType


class Request(TypedDict):
  coin: str
  """Restrict to a single coin."""
  fromType: SpotAccountType
  startTime: NotRequired[TimestampMillis]
  """Range start, Unix millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """Range end, Unix millisecond timestamp."""
  clientOid: NotRequired[str]
  """Restrict to a single transfer by its client-supplied ID."""
  limit: NotRequired[int]
  """Number of records to return."""
  idLessThan: NotRequired[str]
  """Pagination cursor: return entries with an id less than this value (a `orderId`/`billId`/`tradeId` read out of the last row of the previous page)."""


class SpotTransferRecord(TypedDict):
  """One inter-product transfer."""

  coin: str
  """Coin transferred."""
  status: str
  """Current status of the transfer."""
  toType: SpotAccountType
  toSymbol: str
  """Destination margin symbol, when transferring into an isolated-margin account; empty otherwise."""
  fromType: SpotAccountType
  fromSymbol: str
  """Source margin symbol, when transferring from an isolated-margin account; empty otherwise."""
  size: str
  """Transferred amount, as a decimal string."""
  ts: TimestampMillis
  """Transfer time, Unix millisecond timestamp, as a string."""
  clientOid: str
  """Caller-supplied client ID for this transfer."""
  transferId: str
  """Exchange transfer ID."""


Response = list[SpotTransferRecord]


class Records(RpcEndpoint):
  """List the caller's own inter-product transfer records (e.g. spot <-> margin, spot <-> futures)."""

  async def records(
    self,
    *,
    coin: str,
    from_type: SpotAccountType,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    client_oid: str | None = None,
    limit: int | None = None,
    id_less_than: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """List the caller's own inter-product transfer records (e.g. spot <-> margin, spot <-> futures).

    Args:
      coin: Restrict to a single coin.
      from_type:
      start_time: Range start, Unix millisecond timestamp.
      end_time: Range end, Unix millisecond timestamp.
      client_oid: Restrict to a single transfer by its client-supplied ID.
      limit: Number of records to return.
      id_less_than: Pagination cursor: return entries with an id less than this value (a `orderId`/`billId`/`tradeId` read out of the last row of the previous page).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-account/classic-spot-account#get-transfer-record)
    """
    request: Request = Request(coin=coin, fromType=from_type)
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if client_oid is not None:
      request['clientOid'] = client_oid
    if limit is not None:
      request['limit'] = limit
    if id_less_than is not None:
      request['idLessThan'] = id_less_than
    return await self.request(
      request,
      method='GET',
      path='/api/v2/spot/account/transferRecords',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
