# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import SpotAccountType


Response = list[str]


class Request(TypedDict):
  fromType: SpotAccountType
  toType: SpotAccountType


class TransferableCoins(RpcEndpoint):
  """List coins that can be transferred between two given product accounts."""

  async def transferable_coins(
    self,
    *,
    from_type: SpotAccountType,
    to_type: SpotAccountType,
    validate: bool | None = None,
  ) -> Response:
    """List coins that can be transferred between two given product accounts.

    Args:
      from_type:
      to_type:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-account/classic-spot-account#get-transferable-coin-list)
    """
    return await self.request(
      Request(fromType=from_type, toType=to_type),
      method='GET',
      path='/api/v2/spot/wallet/transfer-coin-info',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
