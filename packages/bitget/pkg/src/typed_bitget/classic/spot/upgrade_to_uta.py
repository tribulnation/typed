# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.upgrade_to_uta` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SpotUpgradeToUnifiedAccountRequest(TypedDict):
  """Account to upgrade."""

  subUid: NotRequired[str]
  """Sub-account user ID to upgrade. Omit to upgrade the caller's own account."""


class UpgradeToUta(RpcEndpoint):
  """`classic.spot.upgrade_to_uta`."""

  async def upgrade_to_uta(
    self,
    spot_upgrade_to_unified_account_request: SpotUpgradeToUnifiedAccountRequest,
    *,
    validate: bool | None = None,
  ):
    """Start upgrading the caller's (or a specified sub-account's) Classic account to a Unified Trading Account. Available to both main and sub-accounts. A successful response only means the request was accepted -- the upgrade itself takes about a minute; poll `upgrade_status` to confirm completion.

    Args:
      spot_upgrade_to_unified_account_request: Account to upgrade.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/account/Upgrade-Account)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/spot/account/upgrade',
      json=spot_upgrade_to_unified_account_request,
      validate=validate,
    )
