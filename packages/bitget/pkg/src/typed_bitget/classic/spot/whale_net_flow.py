# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  symbol: str
  """Trading pair symbol, e.g. `BTCUSDT`."""


class SpotWhaleNetFlow(TypedDict):
  """Whale net capital flow for one period."""

  volume: Decimal
  """Net inflow (positive) or outflow (negative) attributed to whale holders, as a decimal string. Confirmed live under this name, not `netFlow`."""
  date: TimestampMillis
  """Period timestamp, Unix millisecond timestamp, as a string. Confirmed live under this name, not `ts`."""


Response = list[SpotWhaleNetFlow]


class WhaleNetFlow(RpcEndpoint):
  """Get net capital flow attributed to large ("whale") holders for a symbol. Public, no authentication required."""

  async def whale_net_flow(
    self, symbol: str, *, validate: bool | None = None
  ) -> Response:
    """Get net capital flow attributed to large ("whale") holders for a symbol. Public, no authentication required.

    Args:
      symbol: Trading pair symbol, e.g. `BTCUSDT`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-common-apidata/classic-common-apidata#get-spot-whale-net-flow-data)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/api/v2/spot/market/whale-net-flow',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
