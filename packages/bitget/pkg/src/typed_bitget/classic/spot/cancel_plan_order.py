# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.cancel_plan_order` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SpotCancelPlanOrderRequest(TypedDict):
  """A plan order to cancel."""

  clientOid: NotRequired[str]
  """Client order ID of the plan order to cancel. Either this or `orderId` is required."""
  orderId: NotRequired[str]
  """Exchange plan-order ID to cancel. Either this or `clientOid` is required."""


class SpotCancelPlanOrderResult(TypedDict):
  """Result of cancelling a plan order."""

  result: str
  """Cancellation result."""


class CancelPlanOrder(RpcEndpoint):
  """`classic.spot.cancel_plan_order`."""

  async def cancel_plan_order(
    self,
    spot_cancel_plan_order_request: SpotCancelPlanOrderRequest,
    *,
    validate: bool | None = None,
  ) -> SpotCancelPlanOrderResult:
    """Cancel one spot plan order.

    Args:
      spot_cancel_plan_order_request: A plan order to cancel.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/plan/Cancel-Plan-Order)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/spot/trade/cancel-plan-order',
      json=spot_cancel_plan_order_request,
      validator=validator(SpotCancelPlanOrderResult),
      validate=validate,
    )
