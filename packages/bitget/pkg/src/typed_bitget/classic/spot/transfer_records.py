# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.transfer_records` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis, timestamp_millis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SpotTransferRecord(TypedDict):
  """One inter-product transfer."""

  coin: str
  """Coin transferred."""
  status: str
  """Current status of the transfer."""
  toType: Literal[
    'spot',
    'p2p',
    'coin_futures',
    'usdt_futures',
    'usdc_futures',
    'crossed_margin',
    'isolated_margin',
  ]
  """Bitget product-account type."""
  toSymbol: str
  """Destination margin symbol, when transferring into an isolated-margin account; empty otherwise."""
  fromType: Literal[
    'spot',
    'p2p',
    'coin_futures',
    'usdt_futures',
    'usdc_futures',
    'crossed_margin',
    'isolated_margin',
  ]
  """Bitget product-account type."""
  fromSymbol: str
  """Source margin symbol, when transferring from an isolated-margin account; empty otherwise."""
  size: str
  """Transferred amount, as a decimal string."""
  ts: TimestampMillis
  """Transfer time, Unix millisecond timestamp, as a string."""
  clientOid: str
  """Caller-supplied client ID for this transfer."""
  transferId: str
  """Exchange transfer ID."""


class TransferRecords(RpcEndpoint):
  """`classic.spot.transfer_records`."""

  async def transfer_records(
    self,
    *,
    coin: str,
    from_type: Literal[
      'spot',
      'p2p',
      'coin_futures',
      'usdt_futures',
      'usdc_futures',
      'crossed_margin',
      'isolated_margin',
    ],
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    client_oid: str | None = None,
    limit: int | None = None,
    id_less_than: str | None = None,
    validate: bool | None = None,
  ) -> list[SpotTransferRecord]:
    """List the caller's own inter-product transfer records (e.g. spot <-> margin, spot <-> futures).

    Args:
      coin: Restrict to a single coin.
      from_type: Restrict to transfers originating from this product account.
      start_time: Range start, Unix millisecond timestamp.
      end_time: Range end, Unix millisecond timestamp.
      client_oid: Restrict to a single transfer by its client-supplied ID.
      limit: Number of records to return.
      id_less_than: Pagination cursor: return entries with an id less than this value (a `orderId`/`billId`/`tradeId` read out of the last row of the previous page).

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/account/Get-Transfer-Record)
    """
    params: dict = {
      'coin': coin,
      'fromType': from_type,
    }
    if start_time is not None:
      params['startTime'] = timestamp_millis.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp_millis.dump(end_time)
    if client_oid is not None:
      params['clientOid'] = client_oid
    if limit is not None:
      params['limit'] = limit
    if id_less_than is not None:
      params['idLessThan'] = id_less_than
    return await self.authed_request(
      'GET',
      '/api/v2/spot/account/transferRecords',
      params=params,
      validator=validator(list[SpotTransferRecord]),
      validate=validate,
    )
