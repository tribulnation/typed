# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.upgrade_status` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SpotUpgradeStatus(TypedDict):
  """Current Classic-to-UTA upgrade status."""

  status: Literal['process', 'success', 'fail']
  """Current upgrade status."""
  reason: NotRequired[str]
  """Reason the upgrade is unavailable or failed, e.g. `upgrade_disabled`. Confirmed live present alongside `status: "fail"`; presence on other statuses is unconfirmed, so left optional."""


class UpgradeStatus(RpcEndpoint):
  """`classic.spot.upgrade_status`."""

  async def upgrade_status(
    self,
    *,
    sub_uid: str | None = None,
    validate: bool | None = None,
  ) -> SpotUpgradeStatus:
    """Get the caller's (or a specified sub-account's) Classic-to-UTA upgrade status.

    Args:
      sub_uid: Sub-account user ID to check. Omit to check the caller's own account.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/account/Get-Upgrade-Status)
    """
    params = {}
    if sub_uid is not None:
      params['subUid'] = sub_uid
    return await self.authed_request(
      'GET',
      '/api/v2/spot/account/upgrade-status',
      params=params,
      validator=validator(SpotUpgradeStatus),
      validate=validate,
    )
