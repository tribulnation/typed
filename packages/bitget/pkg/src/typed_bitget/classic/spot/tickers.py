# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  symbol: NotRequired[str]
  """Restrict to a single symbol, e.g. `BTCUSDT`. Omit to list every symbol."""


class SpotTicker(TypedDict):
  """24h ticker statistics for one symbol."""

  symbol: str
  """Trading pair symbol."""
  high24h: Decimal
  """24h high price, as a decimal string."""
  open: Decimal
  """Price 24h ago, as a decimal string."""
  low24h: Decimal
  """24h low price, as a decimal string."""
  lastPr: Decimal
  """Last traded price, as a decimal string."""
  quoteVolume: Decimal
  """24h trading volume in quote coin, as a decimal string."""
  baseVolume: Decimal
  """24h trading volume in base coin, as a decimal string."""
  usdtVolume: Decimal
  """24h trading volume converted to USDT, as a decimal string."""
  bidPr: Decimal
  """Best bid price, as a decimal string."""
  askPr: Decimal
  """Best ask price, as a decimal string."""
  bidSz: Decimal | None
  """Best bid size, as a decimal string. Confirmed live: `null` on a handful of symbols in the unfiltered listing (thin/one-sided books)."""
  askSz: Decimal | None
  """Best ask size, as a decimal string. Confirmed live: `null` on a handful of symbols in the unfiltered listing (thin/one-sided books)."""
  openUtc: Decimal
  """Price at 00:00 UTC today, as a decimal string."""
  ts: TimestampMillis
  """Timestamp this ticker was generated, Unix millisecond timestamp, as a string."""
  changeUtc24h: Decimal
  """Price change ratio since 00:00 UTC today, as a fraction."""
  change24h: Decimal
  """24h price change ratio, as a fraction."""


Response = list[SpotTicker]


class Tickers(RpcEndpoint):
  """Get 24h ticker statistics for one or all spot symbols. Public, no authentication required."""

  async def tickers(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Get 24h ticker statistics for one or all spot symbols. Public, no authentication required.

    Args:
      symbol: Restrict to a single symbol, e.g. `BTCUSDT`. Omit to list every symbol.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-market/classic-spot-market#get-ticker-information)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='/api/v2/spot/market/tickers',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
