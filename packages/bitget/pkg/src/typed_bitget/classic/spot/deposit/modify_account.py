# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """New deposit-destination account for a coin."""

  accountType: str
  """Product account future deposits of `coin` should be credited to."""
  coin: str
  """Coin to change the deposit destination for."""


Response = str


class ModifyAccount(RpcEndpoint):
  """Change which product account a coin's future deposits are credited to."""

  async def modify_account(
    self,
    *,
    account_type: str,
    coin: str,
    validate: bool | None = None,
  ) -> Response:
    """Change which product account a coin's future deposits are credited to.

    Args:
      account_type: Product account future deposits of `coin` should be credited to.
      coin: Coin to change the deposit destination for.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-account/classic-spot-account#modify-deposit-account)
    """
    return await self.request(
      Request(accountType=account_type, coin=coin),
      method='POST',
      path='/api/v2/spot/wallet/modify-deposit-account',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
