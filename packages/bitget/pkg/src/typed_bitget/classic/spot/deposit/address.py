# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.deposit.address` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SpotDepositAddress(TypedDict):
  """A deposit address for one coin on one chain."""

  address: str
  """Deposit address."""
  chain: str
  """Chain this address is on."""
  coin: str
  """Coin this address accepts."""
  tag: str | None
  """Memo/tag to include with the deposit. Confirmed live: `null` (not an empty string) on chains that don't use one, e.g. ERC20 and TRC20."""
  url: str
  """Block-explorer URL for this address."""


class Address(RpcEndpoint):
  """`classic.spot.deposit.address`."""

  async def address(
    self,
    *,
    coin: str,
    chain: str | None = None,
    size: str,
    validate: bool | None = None,
  ) -> SpotDepositAddress:
    """Get (or generate) the caller's deposit address for a coin.

    Args:
      coin: Coin to get a deposit address for.
      chain: Chain to get a deposit address on. Required for a multi-chain coin.
      size: Reserved for future use; the reference SDK requires it with no documented meaning.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/wallet/Get-Deposit-Address)
    """
    params: dict = {
      'coin': coin,
      'size': size,
    }
    if chain is not None:
      params['chain'] = chain
    return await self.authed_request(
      'GET',
      '/api/v2/spot/wallet/deposit-address',
      params=params,
      validator=validator(SpotDepositAddress),
      validate=validate,
    )
