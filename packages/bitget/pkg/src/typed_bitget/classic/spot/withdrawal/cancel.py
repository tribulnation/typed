# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Withdrawal to cancel."""

  orderId: str
  """Exchange withdrawal order ID to cancel."""


Response = str


class Cancel(RpcEndpoint):
  """Cancel a pending withdrawal before it is broadcast."""

  async def cancel(self, order_id: str, *, validate: bool | None = None) -> Response:
    """Cancel a pending withdrawal before it is broadcast.

    Args:
      order_id: Exchange withdrawal order ID to cancel.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-account/classic-spot-account#cancel-withdrawal)
    """
    return await self.request(
      Request(orderId=order_id),
      method='POST',
      path='/api/v2/spot/wallet/cancel-withdrawal',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
