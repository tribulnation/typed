# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.spot.withdrawal.cancel` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SpotCancelWithdrawalRequest(TypedDict):
  """Withdrawal to cancel."""

  orderId: str
  """Exchange withdrawal order ID to cancel."""


class Cancel(RpcEndpoint):
  """`classic.spot.withdrawal.cancel`."""

  async def cancel(
    self,
    spot_cancel_withdrawal_request: SpotCancelWithdrawalRequest,
    *,
    validate: bool | None = None,
  ) -> str:
    """Cancel a pending withdrawal before it is broadcast.

    Args:
      spot_cancel_withdrawal_request: Withdrawal to cancel.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/spot/wallet/Cancel-Withdrawal)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/spot/wallet/cancel-withdrawal',
      json=spot_cancel_withdrawal_request,
      validator=validator(str),
      validate=validate,
    )
