# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import SpotOrderIdResult


class Request(TypedDict):
  """A withdrawal instruction."""

  coin: str
  """Coin to withdraw."""
  transferType: Literal['on_chain', 'internal_transfer']
  """Whether this is an on-chain withdrawal or an internal transfer to another Bitget account."""
  address: str
  """Destination address, or the recipient email/mobile/UID identifier for an internal transfer."""
  chain: NotRequired[str]
  """Chain to withdraw on. Required for an on-chain withdrawal of a multi-chain coin."""
  innerToType: NotRequired[Literal['email', 'mobile', 'uid']]
  """Identifier type `address` holds, for an internal transfer."""
  areaCode: NotRequired[str]
  """Country dialing code, when `innerToType` is `mobile`."""
  tag: NotRequired[str]
  """Destination tag/memo, for a chain that requires one."""
  size: str
  """Amount to withdraw, as a decimal string."""
  remark: NotRequired[str]
  """Caller-supplied note for this withdrawal."""
  clientOid: NotRequired[str]
  """Caller-supplied client ID for this withdrawal."""


class Create(RpcEndpoint):
  """Withdraw a coin on-chain or to another Bitget account internally."""

  async def create(
    self,
    *,
    coin: str,
    transfer_type: Literal['on_chain', 'internal_transfer'],
    address: str,
    chain: str | None = None,
    inner_to_type: Literal['email', 'mobile', 'uid'] | None = None,
    area_code: str | None = None,
    tag: str | None = None,
    size: str,
    remark: str | None = None,
    client_oid: str | None = None,
    validate: bool | None = None,
  ) -> SpotOrderIdResult:
    """Withdraw a coin on-chain or to another Bitget account internally.

    Args:
      coin: Coin to withdraw.
      transfer_type: Whether this is an on-chain withdrawal or an internal transfer to another Bitget account.
      address: Destination address, or the recipient email/mobile/UID identifier for an internal transfer.
      chain: Chain to withdraw on. Required for an on-chain withdrawal of a multi-chain coin.
      inner_to_type: Identifier type `address` holds, for an internal transfer.
      area_code: Country dialing code, when `innerToType` is `mobile`.
      tag: Destination tag/memo, for a chain that requires one.
      size: Amount to withdraw, as a decimal string.
      remark: Caller-supplied note for this withdrawal.
      client_oid: Caller-supplied client ID for this withdrawal.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-spot-account/classic-spot-account#withdraw)
    """
    request: Request = Request(
      coin=coin, transferType=transfer_type, address=address, size=size
    )
    if chain is not None:
      request['chain'] = chain
    if inner_to_type is not None:
      request['innerToType'] = inner_to_type
    if area_code is not None:
      request['areaCode'] = area_code
    if tag is not None:
      request['tag'] = tag
    if remark is not None:
      request['remark'] = remark
    if client_oid is not None:
      request['clientOid'] = client_oid
    return await self.request(
      request,
      method='POST',
      path='/api/v2/spot/wallet/withdrawal',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SpotOrderIdResult,
    )
