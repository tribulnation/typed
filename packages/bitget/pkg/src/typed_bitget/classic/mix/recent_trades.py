# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.recent_trades` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import validator
from typed_bitget.types import MixFill
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class RecentTrades(RpcEndpoint):
  """`classic.mix.recent_trades`."""

  async def recent_trades(
    self,
    *,
    symbol: str,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    limit: int | None = None,
    validate: bool | None = None,
  ) -> list[MixFill]:
    """List the most recent public trades for one symbol, public.

    Args:
      symbol: Futures symbol, e.g. `"BTCUSDT"`.
      product_type: Which futures product family this request concerns.
      limit: Number of trades to return. Range [1, 1000]; defaults to 100.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/market/Get-Recent-Fills)
    """
    params: dict = {
      'symbol': symbol,
      'productType': product_type,
    }
    if limit is not None:
      params['limit'] = limit
    return await self.request(
      'GET',
      '/api/v2/mix/market/fills',
      params=params,
      validator=validator(list[MixFill]),
      validate=validate,
    )
