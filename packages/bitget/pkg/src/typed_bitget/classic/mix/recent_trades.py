# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixFill, MixProductType


class Request(TypedDict):
  symbol: str
  """Futures symbol, e.g. `"BTCUSDT"`."""
  productType: MixProductType
  limit: NotRequired[int]
  """Number of trades to return. Range [1, 1000]; defaults to 100."""


Response = list[MixFill]


class RecentTrades(RpcEndpoint):
  """List the most recent public trades for one symbol, public."""

  async def recent_trades(
    self,
    symbol: str,
    *,
    product_type: MixProductType,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """List the most recent public trades for one symbol, public.

    Args:
      symbol: Futures symbol, e.g. `"BTCUSDT"`.
      product_type:
      limit: Number of trades to return. Range [1, 1000]; defaults to 100.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-market/classic-contract-market#get-recent-transactions)
    """
    request: Request = Request(symbol=symbol, productType=product_type)
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/api/v2/mix/market/fills',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
