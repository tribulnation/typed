# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.set_margin` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixSetMarginRequest(TypedDict):
  """Isolated margin adjustment request."""

  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  productType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Which futures product family the request or response concerns."""
  marginCoin: str
  """Margin coin, e.g. "USDT"."""
  holdSide: Literal['long', 'short']
  """Which side of a hedge-mode position this concerns."""
  amount: str
  """Amount to add (positive) or remove (negative) as margin."""


class SetMargin(RpcEndpoint):
  """`classic.mix.set_margin`."""

  async def set_margin(
    self,
    mix_set_margin_request: MixSetMarginRequest,
    *,
    validate: bool | None = None,
  ) -> str:
    """Add or remove isolated margin for one position, private.

    Args:
      mix_set_margin_request: Isolated margin adjustment request.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/account/Change-Margin)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/mix/account/set-margin',
      json=mix_set_margin_request,
      validator=validator(str),
      validate=validate,
    )
