# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.reverse_position` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixOrderResult(TypedDict):
  """Identifiers of the order just acted on."""

  orderId: str
  """Order ID assigned by the venue."""
  clientOid: str
  """Client-supplied order ID, echoed back."""


class MixReversalOrderRequest(TypedDict):
  """Position reversal request."""

  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  productType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Which futures product family the request or response concerns."""
  marginCoin: str
  """Margin coin, e.g. "USDT"."""
  size: str
  """Size to reverse, in base coin."""
  side: NotRequired[Literal['buy', 'sell']]
  """Order side."""
  tradeSide: NotRequired[Literal['open', 'close']]
  """Whether the order opens a new position or closes an existing one. Required in hedge-mode (`posMode: hedge_mode`); omitted in one-way mode."""
  clientOid: NotRequired[str]
  """Caller-supplied order ID, echoed back on responses."""


class ReversePosition(RpcEndpoint):
  """`classic.mix.reverse_position`."""

  async def reverse_position(
    self,
    mix_reversal_order_request: MixReversalOrderRequest,
    *,
    validate: bool | None = None,
  ) -> MixOrderResult:
    """One-click reverse an existing position: close it and open an equal-size position on the opposite side, private.

    Args:
      mix_reversal_order_request: Position reversal request.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/trade/Click-Backhand)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/mix/order/click-backhand',
      json=mix_reversal_order_request,
      validator=validator(MixOrderResult),
      validate=validate,
    )
