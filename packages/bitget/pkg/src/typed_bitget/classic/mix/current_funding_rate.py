# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.current_funding_rate` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixCurrentFundingRate(TypedDict):
  """Current funding rate for one symbol."""

  symbol: str
  """Futures symbol."""
  fundingRate: str
  """Current funding rate, e.g. `"0.0001"`."""
  fundingRateInterval: str
  """Funding interval, in hours, e.g. `"8"`."""
  nextUpdate: TimestampMillis
  """Next funding settlement time."""
  minFundingRate: str
  """Minimum bound the funding rate is clamped to."""
  maxFundingRate: str
  """Maximum bound the funding rate is clamped to."""


class CurrentFundingRate(RpcEndpoint):
  """`classic.mix.current_funding_rate`."""

  async def current_funding_rate(
    self,
    *,
    symbol: str | None = None,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    validate: bool | None = None,
  ) -> list[MixCurrentFundingRate]:
    """List current funding rates, public. Returns every symbol of the product type when `symbol` is omitted.

    Args:
      symbol: Restrict to one futures symbol, e.g. "BTCUSDT".
      product_type: Which futures product family this request concerns.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/market/Get-Current-Funding-Rate)
    """
    params: dict = {
      'productType': product_type,
    }
    if symbol is not None:
      params['symbol'] = symbol
    return await self.request(
      'GET',
      '/api/v2/mix/market/current-fund-rate',
      params=params,
      validator=validator(list[MixCurrentFundingRate]),
      validate=validate,
    )
