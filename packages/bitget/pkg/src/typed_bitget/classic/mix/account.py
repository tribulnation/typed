# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.account` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixAccountAsset(TypedDict):
  """Futures account asset detail for one symbol."""

  marginCoin: str
  """Margin coin."""
  locked: str
  """Amount locked by open orders or pending withdrawals."""
  available: str
  """Amount available to open new positions or withdraw."""
  crossedMaxAvailable: str
  """Maximum amount available under cross margin mode."""
  isolatedMaxAvailable: str
  """Maximum amount available under isolated margin mode."""
  maxTransferOut: str
  """Maximum amount that may be transferred out right now."""
  accountEquity: str
  """Account equity, in margin coin."""
  usdtEquity: str
  """Account equity, converted to USDT."""
  btcEquity: str
  """Account equity, converted to BTC."""
  crossedRiskRate: str
  """Risk rate under cross margin mode."""
  unrealizedPL: str
  """Unrealized profit and loss across positions."""
  coupon: str
  """Coupon (bonus) balance applied toward margin."""
  crossedUnrealizedPL: str
  """Unrealized profit and loss on cross-margin positions."""
  isolatedUnrealizedPL: str
  """Unrealized profit and loss on isolated-margin positions."""
  assetMode: str
  """Asset mode: whether margin is shared across symbols ("union") or held per-symbol ("single")."""
  grant: str
  """Promotional grant credit balance, distinct from `coupon`. Confirmed live as an empty string on an account with no active grant."""
  crossedMarginLeverage: int
  """Leverage applied under cross margin mode."""
  isolatedLongLever: int
  """Leverage applied to isolated-margin long positions."""
  isolatedShortLever: int
  """Leverage applied to isolated-margin short positions."""
  marginMode: Literal['isolated', 'crossed']
  """Margin mode governing how margin is allocated to a position."""
  posMode: Literal['one_way_mode', 'hedge_mode']
  """Position mode: one-way (net position per symbol) or hedge (independent long and short positions per symbol)."""
  isolatedMargin: str | None
  """Margin allocated under isolated margin mode, or `null` when not applicable to the current margin mode."""
  crossedMargin: str | None
  """Margin allocated under cross margin mode, or `null` when not applicable to the current margin mode."""


class Account(RpcEndpoint):
  """`classic.mix.account`."""

  async def account(
    self,
    *,
    symbol: str,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    margin_coin: str,
    validate: bool | None = None,
  ) -> MixAccountAsset:
    """Get one symbol's futures account asset detail, private.

    Args:
      symbol: Futures symbol, e.g. "BTCUSDT".
      product_type: Which futures product family this request concerns.
      margin_coin: Margin coin, e.g. "USDT".

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/account/Get-Single-Account)
    """
    params: dict = {
      'symbol': symbol,
      'productType': product_type,
      'marginCoin': margin_coin,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/mix/account/account',
      params=params,
      validator=validator(MixAccountAsset),
      validate=validate,
    )
