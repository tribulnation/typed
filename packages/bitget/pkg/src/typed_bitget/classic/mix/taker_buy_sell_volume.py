# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.taker_buy_sell_volume` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixTakerBuySellVolume(TypedDict):
  """Taker buy/sell volume for one time bucket, for one bucket."""

  buyVolume: str
  """Taker buy volume for this bucket."""
  sellVolume: str
  """Taker sell volume for this bucket."""
  ts: TimestampMillis
  """Bucket time."""


class TakerBuySellVolume(RpcEndpoint):
  """`classic.mix.taker_buy_sell_volume`."""

  async def taker_buy_sell_volume(
    self,
    *,
    symbol: str,
    period: str | None = None,
    validate: bool | None = None,
  ) -> list[MixTakerBuySellVolume]:
    """Get taker active buy/sell trading volume for one symbol, public.

    Args:
      symbol: Futures symbol, e.g. "BTCUSDT".
      period: Bucket interval for the series, e.g. "5m"/"1H"/"1D". Optional; defaults to the venue's own default bucket. See endpoint `notes` on why this is not an `enum`.
    """
    params: dict = {
      'symbol': symbol,
    }
    if period is not None:
      params['period'] = period
    return await self.request(
      'GET',
      '/api/v2/mix/market/taker-buy-sell',
      params=params,
      validator=validator(list[MixTakerBuySellVolume]),
      validate=validate,
    )
