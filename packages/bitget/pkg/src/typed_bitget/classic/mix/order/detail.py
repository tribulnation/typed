# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import (
  MixMarginMode,
  MixOrderSide,
  MixOrderType,
  MixPosMode,
  MixProductType,
  MixReduceOnly,
  MixTimeInForce,
)


class MixOrderDetail(TypedDict):
  """Order detail."""

  symbol: str
  """Futures symbol."""
  size: Decimal
  """Order size, in base coin."""
  orderId: str
  """Order ID."""
  clientOid: str
  """Client-supplied order ID."""
  baseVolume: Decimal
  """Filled size so far, in base coin."""
  priceAvg: Decimal
  """Volume-weighted average fill price so far."""
  fee: Decimal
  """Total fees paid on fills so far."""
  price: str
  """Limit price, if a limit order."""
  state: str
  """Order status. Venue does not publish a closed set for this field in the response shape (see endpoint `notes`)."""
  side: MixOrderSide
  force: MixTimeInForce
  totalProfits: Decimal
  """Realized profit from fills of this order."""
  posSide: Literal['long', 'short', 'net']
  """Position side this order concerns. `net` in one-way mode (confirmed live) rather than `long`/`short` -- wider than the shared `MixHoldSide` enum (which is scoped to hedge-mode `long`/`short` per its own description), so specced inline here instead of `$ref`."""
  marginCoin: str
  """Margin coin."""
  presetStopSurplusPrice: str
  """Take-profit trigger price attached to the position this order opened, if any. Confirmed live: empty string when none is attached."""
  presetStopSurplusType: str
  """Trigger price type for the attached take-profit, if any. Undocumented field observed live; empty string when no take-profit is attached."""
  presetStopSurplusExecutePrice: str
  """Execution price for the attached take-profit, if any. Undocumented field observed live; empty string when no take-profit is attached."""
  presetStopLossPrice: str
  """Stop-loss trigger price attached to the position this order opened, if any. Confirmed live: empty string when none is attached."""
  presetStopLossType: str
  """Trigger price type for the attached stop-loss, if any. Undocumented field observed live; empty string when no stop-loss is attached."""
  presetStopLossExecutePrice: str
  """Execution price for the attached stop-loss, if any. Undocumented field observed live; empty string when no stop-loss is attached."""
  quoteVolume: Decimal
  """Filled value so far, in quote coin."""
  orderType: MixOrderType
  leverage: int
  """Leverage in effect when this order was placed."""
  marginMode: MixMarginMode
  reduceOnly: MixReduceOnly
  enterPointSource: str
  """Where the order originated (API, web, app, etc.). Venue does not publish a closed set for this field."""
  tradeSide: Literal['open', 'close', 'buy_single', 'sell_single']
  """Whether the order opens or closes a position. Confirmed live: NOT omitted in one-way mode as the shared `MixTradeSide` enum's own description assumes -- it's present as `buy_single`/`sell_single` instead of `open`/`close`, so specced inline here instead of `$ref`."""
  posMode: MixPosMode
  orderSource: str
  """How the order was generated (normal placement, plan trigger, liquidation, etc.). Venue does not publish a closed set for this field in the response shape; see `MixOrderSource` on list endpoints for the confirmed values from the history-orders filter parameter."""
  cancelReason: str
  """Reason the order was cancelled, if it was."""
  newTradeSide: str | None
  """Undocumented field observed live; confirmed `null` on a filled market order. Not declared upstream."""
  cTime: TimestampMillis
  """Order creation time."""
  uTime: TimestampMillis
  """Last update time."""


class Request(TypedDict):
  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  productType: MixProductType
  orderId: NotRequired[str]
  """Order ID to look up. Either this or `clientOid` is required."""
  clientOid: NotRequired[str]
  """Client order ID to look up. Either this or `orderId` is required."""


class Detail(RpcEndpoint):
  """Get one order's current detail, private. Identify the order by `orderId` or `clientOid`."""

  async def detail(
    self,
    *,
    symbol: str,
    product_type: MixProductType,
    order_id: str | None = None,
    client_oid: str | None = None,
    validate: bool | None = None,
  ) -> MixOrderDetail:
    """Get one order's current detail, private. Identify the order by `orderId` or `clientOid`.

    Args:
      symbol: Futures symbol, e.g. "BTCUSDT".
      product_type:
      order_id: Order ID to look up. Either this or `clientOid` is required.
      client_oid: Client order ID to look up. Either this or `orderId` is required.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-trade/classic-contract-trade#get-order-detail)
    """
    request: Request = Request(symbol=symbol, productType=product_type)
    if order_id is not None:
      request['orderId'] = order_id
    if client_oid is not None:
      request['clientOid'] = client_oid
    return await self.request(
      request,
      method='GET',
      path='/api/v2/mix/order/detail',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=MixOrderDetail,
    )
