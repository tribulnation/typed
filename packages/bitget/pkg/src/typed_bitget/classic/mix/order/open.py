# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import (
  MixHoldSide,
  MixMarginMode,
  MixOrderSide,
  MixOrderType,
  MixPosMode,
  MixProductType,
  MixTimeInForce,
  MixTradeSide,
)


class MixOpenOrder(TypedDict):
  """One open order."""

  symbol: str
  """Futures symbol."""
  size: str
  """Order size, in base coin."""
  orderId: str
  """Order ID."""
  clientOid: str
  """Client-supplied order ID."""
  baseVolume: str
  """Filled size so far, in base coin."""
  fee: str
  """Total fees paid on fills so far."""
  price: str
  """Limit price, if a limit order."""
  priceAvg: str
  """Volume-weighted average fill price so far."""
  status: Literal['live', 'partially_filled']
  """Order status. Only `live`/`partially_filled` orders appear in this open-orders listing, confirmed by `FuturesGetOpenOrdersRequestV2`'s own `status` filter parameter narrowing to these two values."""
  side: MixOrderSide
  force: MixTimeInForce
  totalProfits: str
  """Realized profit from fills of this order."""
  posSide: MixHoldSide
  marginCoin: str
  """Margin coin."""
  quoteVolume: str
  """Filled value so far, in quote coin."""
  leverage: str
  """Leverage in effect when this order was placed."""
  marginMode: MixMarginMode
  enterPointSource: str
  """Where the order originated. Venue does not publish a closed set for this field."""
  tradeSide: MixTradeSide
  posMode: MixPosMode
  orderType: MixOrderType
  orderSource: str
  """How the order was generated. Venue does not publish a closed set for this field in the response shape."""
  cTime: TimestampMillis
  """Order creation time."""
  uTime: TimestampMillis
  """Last update time."""
  presetStopSurplusPrice: str
  """Take-profit trigger price attached to the position this order opens, if any."""
  presetStopSurplusType: str
  """Price source the attached take-profit trigger uses. Venue does not publish a closed set for this field in the response shape."""
  presetStopSurplusExecutePrice: str
  """Execution price for the attached take-profit, if a limit execution was set."""
  presetStopLossPrice: str
  """Stop-loss trigger price attached to the position this order opens, if any."""
  presetStopLossType: str
  """Price source the attached stop-loss trigger uses. Venue does not publish a closed set for this field in the response shape."""
  presetStopLossExecutePrice: str
  """Execution price for the attached stop-loss, if a limit execution was set."""


class Request(TypedDict):
  orderId: NotRequired[str]
  """Restrict to one order."""
  clientOid: NotRequired[str]
  """Restrict to one order, by client order ID."""
  symbol: NotRequired[str]
  """Restrict to one futures symbol, e.g. "BTCUSDT"."""
  productType: MixProductType
  status: NotRequired[Literal['live', 'partially_filled']]
  """Restrict to one order status."""
  idLessThan: NotRequired[str]
  """Pagination cursor: return orders with an order ID before this one. Pass the last page's `endId` to walk further back."""
  startTime: NotRequired[TimestampMillis]
  """Window lower bound, inclusive."""
  endTime: NotRequired[TimestampMillis]
  """Window upper bound, inclusive."""
  limit: NotRequired[int]
  """Rows per page. Defaults to 100."""


class MixOpenOrdersPage(TypedDict):
  """One page of the caller's own open orders."""

  entrustedList: list[MixOpenOrder] | None
  """Open orders. Confirmed live as `null` (not an empty array) when there are no rows."""
  endId: str | None
  """Cursor: pass as `idLessThan` to fetch the next page. Confirmed live as `null` when there is no further page (e.g. the result set is empty)."""


class Open(RpcEndpoint):
  """List the caller's own open (live or partially filled) orders, private."""

  def open_paged(
    self,
    *,
    order_id: str | None = None,
    client_oid: str | None = None,
    symbol: str | None = None,
    product_type: MixProductType,
    status: Literal['live', 'partially_filled'] | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[MixOpenOrder, str]:
    """List the caller's own open (live or partially filled) orders, private.

    Paged variant of `open`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      order_id: Restrict to one order.
      client_oid: Restrict to one order, by client order ID.
      symbol: Restrict to one futures symbol, e.g. "BTCUSDT".
      product_type:
      status: Restrict to one order status.
      start_time: Window lower bound, inclusive.
      end_time: Window upper bound, inclusive.
      limit: Rows per page. Defaults to 100.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-trade/classic-contract-trade#get-pending-orders)
    """

    async def next(id_less_than: str) -> tuple[list[MixOpenOrder], str | None]:
      response = await self.open(
        order_id=order_id,
        client_oid=client_oid,
        symbol=symbol,
        product_type=product_type,
        status=status,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        id_less_than=(id_less_than or None),
        validate=validate,
      )
      rows = response.get('entrustedList')
      state = response.get('endId')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def open(
    self,
    *,
    order_id: str | None = None,
    client_oid: str | None = None,
    symbol: str | None = None,
    product_type: MixProductType,
    status: Literal['live', 'partially_filled'] | None = None,
    id_less_than: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> MixOpenOrdersPage:
    """List the caller's own open (live or partially filled) orders, private.

    Args:
      order_id: Restrict to one order.
      client_oid: Restrict to one order, by client order ID.
      symbol: Restrict to one futures symbol, e.g. "BTCUSDT".
      product_type:
      status: Restrict to one order status.
      id_less_than: Pagination cursor: return orders with an order ID before this one. Pass the last page's `endId` to walk further back.
      start_time: Window lower bound, inclusive.
      end_time: Window upper bound, inclusive.
      limit: Rows per page. Defaults to 100.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-trade/classic-contract-trade#get-pending-orders)
    """
    request: Request = Request(productType=product_type)
    if order_id is not None:
      request['orderId'] = order_id
    if client_oid is not None:
      request['clientOid'] = client_oid
    if symbol is not None:
      request['symbol'] = symbol
    if status is not None:
      request['status'] = status
    if id_less_than is not None:
      request['idLessThan'] = id_less_than
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/api/v2/mix/order/orders-pending',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=MixOpenOrdersPage,
    )
