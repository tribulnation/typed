# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import (
  MixMarginMode,
  MixOrderSide,
  MixProductType,
  MixReduceOnly,
  MixStpMode,
  MixTimeInForce,
  MixTradeSide,
)


class MixBatchOrderFailure(TypedDict):
  """One rejected order."""

  orderId: str
  """Order ID, if one was assigned before rejection."""
  clientOid: str
  """Client-supplied order ID, echoed back."""
  errorMsg: str
  """Human-readable rejection reason."""
  errorCode: str
  """Venue error code for the rejection."""


class MixBatchOrderSuccess(TypedDict):
  """One accepted order."""

  orderId: str
  """Order ID assigned by the venue."""
  clientOid: str
  """Client-supplied order ID, echoed back."""


class MixBatchLimitOrderItem(TypedDict):
  """One futures limit order within a batch placement request."""

  size: Decimal
  """Order size, in base coin."""
  price: Decimal
  """Limit price. Required for a limit order; omit for a market order."""
  side: MixOrderSide
  orderType: Literal['limit']
  """Order type."""
  tradeSide: NotRequired[MixTradeSide]
  force: NotRequired[MixTimeInForce]
  clientOid: NotRequired[str]
  """Caller-supplied order ID, echoed back on responses."""
  reduceOnly: NotRequired[MixReduceOnly]
  presetStopSurplusPrice: NotRequired[str]
  """Take-profit trigger price to attach to the position this order opens."""
  presetStopLossPrice: NotRequired[str]
  """Stop-loss trigger price to attach to the position this order opens."""
  stpMode: NotRequired[MixStpMode]


class MixBatchMarketOrderItem(TypedDict):
  """One futures market order within a batch placement request."""

  size: Decimal
  """Order size, in base coin."""
  side: MixOrderSide
  orderType: Literal['market']
  """Order type."""
  tradeSide: NotRequired[MixTradeSide]
  force: NotRequired[MixTimeInForce]
  clientOid: NotRequired[str]
  """Caller-supplied order ID, echoed back on responses."""
  reduceOnly: NotRequired[MixReduceOnly]
  presetStopSurplusPrice: NotRequired[str]
  """Take-profit trigger price to attach to the position this order opens."""
  presetStopLossPrice: NotRequired[str]
  """Stop-loss trigger price to attach to the position this order opens."""
  stpMode: NotRequired[MixStpMode]


class MixBatchOrderResult(TypedDict):
  """Per-order outcome of a batch request."""

  successList: list[MixBatchOrderSuccess]
  """Orders accepted."""
  failureList: list[MixBatchOrderFailure]
  """Orders rejected."""


class Request(TypedDict):
  """Batch order placement request."""

  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  productType: MixProductType
  marginCoin: str
  """Margin coin, e.g. "USDT"."""
  marginMode: MixMarginMode
  orderList: list[MixBatchLimitOrderItem | MixBatchMarketOrderItem]
  """Orders to place, up to 20."""


class BatchPlace(RpcEndpoint):
  """Place up to 20 futures orders for one symbol in a single request, private. Each order is accepted or rejected independently."""

  async def batch_place(
    self,
    *,
    symbol: str,
    product_type: MixProductType,
    margin_coin: str,
    margin_mode: MixMarginMode,
    order_list: list[MixBatchLimitOrderItem | MixBatchMarketOrderItem],
    validate: bool | None = None,
  ) -> MixBatchOrderResult:
    """Place up to 20 futures orders for one symbol in a single request, private. Each order is accepted or rejected independently.

    Args:
      symbol: Futures symbol, e.g. "BTCUSDT".
      product_type:
      margin_coin: Margin coin, e.g. "USDT".
      margin_mode:
      order_list: Orders to place, up to 20.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-trade/classic-contract-trade#batch-order)
    """
    return await self.request(
      Request(
        symbol=symbol,
        productType=product_type,
        marginCoin=margin_coin,
        marginMode=margin_mode,
        orderList=order_list,
      ),
      method='POST',
      path='/api/v2/mix/order/batch-place-order',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=MixBatchOrderResult,
    )
