# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixProductType, MixStpMode, MixTriggerType


class MixOrderResult(TypedDict):
  """Identifiers of the order just acted on."""

  orderId: str
  """Order ID assigned by the venue."""
  clientOid: str
  """Client-supplied order ID, echoed back."""


class Request(TypedDict):
  """TP/SL order request."""

  marginCoin: str
  """Margin coin, e.g. "USDT"."""
  productType: MixProductType
  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  planType: Literal['profit_plan', 'loss_plan', 'moving_plan', 'pos_profit', 'pos_loss']
  """Which TP/SL family this order is."""
  triggerPrice: Decimal
  """Price that triggers this order."""
  triggerType: NotRequired[MixTriggerType]
  executePrice: NotRequired[str]
  """Limit execution price once triggered. Omit to execute at market."""
  holdSide: Literal['long', 'short', 'buy', 'sell']
  """Side of the position this order closes."""
  size: Decimal
  """Size to close, in base coin. Closes the full position when omitted, per the venue's default (unconfirmed live)."""
  rangeRate: NotRequired[str]
  """Callback range, for a `moving_plan` (trailing) trigger."""
  clientOid: NotRequired[str]
  """Caller-supplied order ID, echoed back on responses."""
  stpMode: NotRequired[MixStpMode]


class Place(RpcEndpoint):
  """Place a stand-alone take-profit or stop-loss trigger order against an existing position, private."""

  async def place(
    self,
    *,
    margin_coin: str,
    product_type: MixProductType,
    symbol: str,
    plan_type: Literal[
      'profit_plan', 'loss_plan', 'moving_plan', 'pos_profit', 'pos_loss'
    ],
    trigger_price: Decimal,
    trigger_type: MixTriggerType | None = None,
    execute_price: str | None = None,
    hold_side: Literal['long', 'short', 'buy', 'sell'],
    size: Decimal,
    range_rate: str | None = None,
    client_oid: str | None = None,
    stp_mode: MixStpMode | None = None,
    validate: bool | None = None,
  ) -> MixOrderResult:
    """Place a stand-alone take-profit or stop-loss trigger order against an existing position, private.

    Args:
      margin_coin: Margin coin, e.g. "USDT".
      product_type:
      symbol: Futures symbol, e.g. "BTCUSDT".
      plan_type: Which TP/SL family this order is.
      trigger_price: Price that triggers this order.
      trigger_type:
      execute_price: Limit execution price once triggered. Omit to execute at market.
      hold_side: Side of the position this order closes.
      size: Size to close, in base coin. Closes the full position when omitted, per the venue's default (unconfirmed live).
      range_rate: Callback range, for a `moving_plan` (trailing) trigger.
      client_oid: Caller-supplied order ID, echoed back on responses.
      stp_mode:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-plan/classic-contract-plan#stop-profit-and-stop-loss-plan-orders)
    """
    request: Request = Request(
      marginCoin=margin_coin,
      productType=product_type,
      symbol=symbol,
      planType=plan_type,
      triggerPrice=trigger_price,
      holdSide=hold_side,
      size=size,
    )
    if trigger_type is not None:
      request['triggerType'] = trigger_type
    if execute_price is not None:
      request['executePrice'] = execute_price
    if range_rate is not None:
      request['rangeRate'] = range_rate
    if client_oid is not None:
      request['clientOid'] = client_oid
    if stp_mode is not None:
      request['stpMode'] = stp_mode
    return await self.request(
      request,
      method='POST',
      path='/api/v2/mix/order/place-tpsl-order',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=MixOrderResult,
    )
