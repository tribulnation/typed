# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixPlanTypeNormalTrack, MixProductType


class MixPlanTriggerSubOrder(TypedDict):
  """One child order."""

  orderId: str
  """Child order ID."""
  price: str
  """Child order price."""
  type: str
  """Child order type. Venue does not publish a closed set for this field."""
  status: str
  """Child order status. Venue does not publish a closed set for this field."""


class Request(TypedDict):
  planType: MixPlanTypeNormalTrack
  planOrderId: str
  """Parent plan order ID."""
  productType: MixProductType


Response = list[MixPlanTriggerSubOrder]


class TriggerSubOrders(RpcEndpoint):
  """List the child orders a triggered plan/track order has produced, private."""

  async def trigger_sub_orders(
    self,
    plan_type: MixPlanTypeNormalTrack,
    *,
    plan_order_id: str,
    product_type: MixProductType,
    validate: bool | None = None,
  ) -> Response:
    """List the child orders a triggered plan/track order has produced, private.

    Args:
      plan_type:
      plan_order_id: Parent plan order ID.
      product_type:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-plan/classic-contract-plan#trigger-sub-order)
    """
    return await self.request(
      Request(planType=plan_type, planOrderId=plan_order_id, productType=product_type),
      method='GET',
      path='/api/v2/mix/order/plan-sub-order',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
