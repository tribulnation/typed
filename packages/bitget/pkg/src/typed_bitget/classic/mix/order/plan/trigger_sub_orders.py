# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.order.plan.trigger_sub_orders` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixPlanTriggerSubOrder(TypedDict):
  """One child order."""

  orderId: str
  """Child order ID."""
  price: str
  """Child order price."""
  type: str
  """Child order type. Venue does not publish a closed set for this field."""
  status: str
  """Child order status. Venue does not publish a closed set for this field."""


class TriggerSubOrders(RpcEndpoint):
  """`classic.mix.order.plan.trigger_sub_orders`."""

  async def trigger_sub_orders(
    self,
    *,
    plan_type: Literal['normal_plan', 'track_plan'],
    plan_order_id: str,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    validate: bool | None = None,
  ) -> list[MixPlanTriggerSubOrder]:
    """List the child orders a triggered plan/track order has produced, private.

    Args:
      plan_type: Plan order kind the parent order was.
      plan_order_id: Parent plan order ID.
      product_type: Which futures product family this order concerns.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/plan/Get-Plan-Sub-Order)
    """
    params: dict = {
      'planType': plan_type,
      'planOrderId': plan_order_id,
      'productType': product_type,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/mix/order/plan-sub-order',
      params=params,
      validator=validator(list[MixPlanTriggerSubOrder]),
      validate=validate,
    )
