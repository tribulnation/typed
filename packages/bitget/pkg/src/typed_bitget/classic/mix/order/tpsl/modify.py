# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.order.tpsl.modify` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixModifyTpslOrderRequest(TypedDict):
  """TP/SL modification request."""

  orderId: NotRequired[str]
  """Order ID to modify. Either this or `clientOid` is required."""
  clientOid: NotRequired[str]
  """Client order ID to modify. Either this or `orderId` is required."""
  marginCoin: str
  """Margin coin, e.g. "USDT"."""
  productType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Which futures product family the request or response concerns."""
  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  triggerPrice: str
  """New trigger price."""
  triggerType: NotRequired[Literal['fill_price', 'mark_price']]
  """Price source a trigger is evaluated against."""
  executePrice: NotRequired[str]
  """New limit execution price. Omit to execute at market."""
  size: str
  """New close size, in base coin."""
  rangeRate: NotRequired[str]
  """New callback range, for a `moving_plan` (trailing) trigger."""


class MixOrderResult(TypedDict):
  """Identifiers of the order just acted on."""

  orderId: str
  """Order ID assigned by the venue."""
  clientOid: str
  """Client-supplied order ID, echoed back."""


class Modify(RpcEndpoint):
  """`classic.mix.order.tpsl.modify`."""

  async def modify(
    self,
    mix_modify_tpsl_order_request: MixModifyTpslOrderRequest,
    *,
    validate: bool | None = None,
  ) -> MixOrderResult:
    """Modify a stand-alone TP/SL order placed via `place-tpsl-order`, private.

    Args:
      mix_modify_tpsl_order_request: TP/SL modification request.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/plan/Modify-Tpsl-Order)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/mix/order/modify-tpsl-order',
      json=mix_modify_tpsl_order_request,
      validator=validator(MixOrderResult),
      validate=validate,
    )
