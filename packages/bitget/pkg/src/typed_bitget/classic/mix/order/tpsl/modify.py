# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixProductType, MixTriggerType


class MixOrderResult(TypedDict):
  """Identifiers of the order just acted on."""

  orderId: str
  """Order ID assigned by the venue."""
  clientOid: str
  """Client-supplied order ID, echoed back."""


class Request(TypedDict):
  """TP/SL modification request."""

  orderId: NotRequired[str]
  """Order ID to modify. Either this or `clientOid` is required."""
  clientOid: NotRequired[str]
  """Client order ID to modify. Either this or `orderId` is required."""
  marginCoin: str
  """Margin coin, e.g. "USDT"."""
  productType: MixProductType
  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  triggerPrice: str
  """New trigger price."""
  triggerType: NotRequired[MixTriggerType]
  executePrice: NotRequired[str]
  """New limit execution price. Omit to execute at market."""
  size: str
  """New close size, in base coin."""
  rangeRate: NotRequired[str]
  """New callback range, for a `moving_plan` (trailing) trigger."""


class Modify(RpcEndpoint):
  """Modify a stand-alone TP/SL order placed via `place-tpsl-order`, private."""

  async def modify(
    self,
    *,
    order_id: str | None = None,
    client_oid: str | None = None,
    margin_coin: str,
    product_type: MixProductType,
    symbol: str,
    trigger_price: str,
    trigger_type: MixTriggerType | None = None,
    execute_price: str | None = None,
    size: str,
    range_rate: str | None = None,
    validate: bool | None = None,
  ) -> MixOrderResult:
    """Modify a stand-alone TP/SL order placed via `place-tpsl-order`, private.

    Args:
      order_id: Order ID to modify. Either this or `clientOid` is required.
      client_oid: Client order ID to modify. Either this or `orderId` is required.
      margin_coin: Margin coin, e.g. "USDT".
      product_type:
      symbol: Futures symbol, e.g. "BTCUSDT".
      trigger_price: New trigger price.
      trigger_type:
      execute_price: New limit execution price. Omit to execute at market.
      size: New close size, in base coin.
      range_rate: New callback range, for a `moving_plan` (trailing) trigger.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-plan/classic-contract-plan#modify-the-stop-profit-and-stop-loss-plan-order)
    """
    request: Request = Request(
      marginCoin=margin_coin,
      productType=product_type,
      symbol=symbol,
      triggerPrice=trigger_price,
      size=size,
    )
    if order_id is not None:
      request['orderId'] = order_id
    if client_oid is not None:
      request['clientOid'] = client_oid
    if trigger_type is not None:
      request['triggerType'] = trigger_type
    if execute_price is not None:
      request['executePrice'] = execute_price
    if range_rate is not None:
      request['rangeRate'] = range_rate
    return await self.request(
      request,
      method='POST',
      path='/api/v2/mix/order/modify-tpsl-order',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=MixOrderResult,
    )
