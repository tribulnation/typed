# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.order.plan.place` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixOrderResult(TypedDict):
  """Identifiers of the order just acted on."""

  orderId: str
  """Order ID assigned by the venue."""
  clientOid: str
  """Client-supplied order ID, echoed back."""


class MixPlanLimitOrderRequest(TypedDict):
  """A trigger (plan) order that places a limit order once its trigger price is reached."""

  planType: Literal['normal_plan', 'track_plan']
  """Plan order kind: a normal trigger plan order, or a trailing (track) plan order."""
  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  productType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Which futures product family the request or response concerns."""
  marginMode: Literal['isolated', 'crossed']
  """Margin mode governing how margin is allocated to a position."""
  marginCoin: str
  """Margin coin, e.g. "USDT"."""
  size: str
  """Order size, in base coin."""
  price: str
  """Limit execution price once triggered."""
  callbackRatio: NotRequired[str]
  """Trailing callback ratio, for a `track_plan` (trailing) order."""
  triggerPrice: str
  """Price that triggers this order."""
  triggerType: Literal['fill_price', 'mark_price']
  """Price source a trigger is evaluated against."""
  side: Literal['buy', 'sell']
  """Order side."""
  tradeSide: NotRequired[Literal['open', 'close']]
  """Whether the order opens a new position or closes an existing one. Required in hedge-mode (`posMode: hedge_mode`); omitted in one-way mode."""
  orderType: Literal['limit']
  """Order type."""
  clientOid: NotRequired[str]
  """Caller-supplied order ID, echoed back on responses."""
  reduceOnly: NotRequired[Literal['YES', 'NO']]
  """Whether the order may only reduce an existing position, never open or increase one."""
  stopSurplusTriggerPrice: NotRequired[str]
  """Take-profit trigger price to attach once the plan order fills."""
  stopSurplusExecutePrice: NotRequired[str]
  """Take-profit limit execution price. Omit to execute at market."""
  stopSurplusTriggerType: NotRequired[
    Literal['fill_price', 'mark_price', 'index_price']
  ]
  """Price source a TP/SL trigger embedded in a plan order is evaluated against."""
  stopLossTriggerPrice: NotRequired[str]
  """Stop-loss trigger price to attach once the plan order fills."""
  stopLossExecutePrice: NotRequired[str]
  """Stop-loss limit execution price. Omit to execute at market."""
  stopLossTriggerType: NotRequired[Literal['fill_price', 'mark_price', 'index_price']]
  """Price source a TP/SL trigger embedded in a plan order is evaluated against."""
  stpMode: NotRequired[Literal['none', 'cancel_taker', 'cancel_maker', 'cancel_both']]
  """Self-trade prevention mode."""


class MixPlanMarketOrderRequest(TypedDict):
  """A trigger (plan) order that places a market order once its trigger price is reached."""

  planType: Literal['normal_plan', 'track_plan']
  """Plan order kind: a normal trigger plan order, or a trailing (track) plan order."""
  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  productType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Which futures product family the request or response concerns."""
  marginMode: Literal['isolated', 'crossed']
  """Margin mode governing how margin is allocated to a position."""
  marginCoin: str
  """Margin coin, e.g. "USDT"."""
  size: str
  """Order size, in base coin."""
  callbackRatio: NotRequired[str]
  """Trailing callback ratio, for a `track_plan` (trailing) order."""
  triggerPrice: str
  """Price that triggers this order."""
  triggerType: Literal['fill_price', 'mark_price']
  """Price source a trigger is evaluated against."""
  side: Literal['buy', 'sell']
  """Order side."""
  tradeSide: NotRequired[Literal['open', 'close']]
  """Whether the order opens a new position or closes an existing one. Required in hedge-mode (`posMode: hedge_mode`); omitted in one-way mode."""
  orderType: Literal['market']
  """Order type."""
  clientOid: NotRequired[str]
  """Caller-supplied order ID, echoed back on responses."""
  reduceOnly: NotRequired[Literal['YES', 'NO']]
  """Whether the order may only reduce an existing position, never open or increase one."""
  stopSurplusTriggerPrice: NotRequired[str]
  """Take-profit trigger price to attach once the plan order fills."""
  stopSurplusExecutePrice: NotRequired[str]
  """Take-profit limit execution price. Omit to execute at market."""
  stopSurplusTriggerType: NotRequired[
    Literal['fill_price', 'mark_price', 'index_price']
  ]
  """Price source a TP/SL trigger embedded in a plan order is evaluated against."""
  stopLossTriggerPrice: NotRequired[str]
  """Stop-loss trigger price to attach once the plan order fills."""
  stopLossExecutePrice: NotRequired[str]
  """Stop-loss limit execution price. Omit to execute at market."""
  stopLossTriggerType: NotRequired[Literal['fill_price', 'mark_price', 'index_price']]
  """Price source a TP/SL trigger embedded in a plan order is evaluated against."""
  stpMode: NotRequired[Literal['none', 'cancel_taker', 'cancel_maker', 'cancel_both']]
  """Self-trade prevention mode."""


class Place(RpcEndpoint):
  """`classic.mix.order.plan.place`."""

  async def place(
    self,
    mix_plan_limit_order_request_mix_plan_market_order_request: MixPlanLimitOrderRequest
    | MixPlanMarketOrderRequest,
    *,
    validate: bool | None = None,
  ) -> MixOrderResult:
    """Place a trigger (plan) order that places a real order once its trigger price is reached, private. May optionally attach take-profit and stop-loss triggers.

    Args:
      mix_plan_limit_order_request_mix_plan_market_order_request: Plan order request.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/plan/Place-Plan-Order)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/mix/order/place-plan-order',
      json=mix_plan_limit_order_request_mix_plan_market_order_request,
      validator=validator(MixOrderResult),
      validate=validate,
    )
