# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import (
  MixHoldSide,
  MixMarginMode,
  MixOrderSide,
  MixOrderType,
  MixPlanTypeQuery,
  MixPosMode,
  MixProductType,
  MixTradeSide,
)


class MixPendingPlanOrder(TypedDict):
  """One pending plan order."""

  planType: str
  """Plan order kind. Venue does not publish a closed set for this response field covering every plan/TP-SL variant (contrast the narrower confirmed request-side enums on `place-plan-order`/`place-tpsl-order`)."""
  symbol: str
  """Futures symbol."""
  size: str
  """Order size, in base coin."""
  orderId: str
  """Order ID."""
  clientOid: str
  """Client-supplied order ID."""
  price: str
  """Limit execution price, if set."""
  executePrice: str
  """Limit execution price once triggered, if set."""
  callbackRatio: str
  """Trailing callback ratio, if a trailing order."""
  triggerPrice: str
  """Price that triggers this order."""
  triggerType: str
  """Price source the trigger is evaluated against. Venue does not publish a closed set for this response field."""
  planStatus: str
  """Plan order status. Venue does not publish a closed set for this response field."""
  side: MixOrderSide
  posSide: MixHoldSide
  marginCoin: str
  """Margin coin."""
  marginMode: MixMarginMode
  enterPointSource: str
  """Where the order originated. Venue does not publish a closed set for this field."""
  tradeSide: MixTradeSide
  posMode: MixPosMode
  orderType: MixOrderType
  orderSource: str
  """How the order was generated. Venue does not publish a closed set for this field."""
  cTime: TimestampMillis
  """Order creation time."""
  uTime: TimestampMillis
  """Last update time."""
  stopSurplusExecutePrice: str
  """Take-profit limit execution price, if attached."""
  stopSurplusTriggerPrice: str
  """Take-profit trigger price, if attached."""
  stopSurplusTriggerType: str
  """Price source the take-profit trigger is evaluated against. Venue does not publish a closed set for this field."""
  stopLossExecutePrice: str
  """Stop-loss limit execution price, if attached."""
  stopLossTriggerPrice: str
  """Stop-loss trigger price, if attached."""
  stopLossTriggerType: str
  """Price source the stop-loss trigger is evaluated against. Venue does not publish a closed set for this field."""


class Request(TypedDict):
  orderId: NotRequired[str]
  """Restrict to one order."""
  clientOid: NotRequired[str]
  """Restrict to one order, by client order ID."""
  symbol: NotRequired[str]
  """Restrict to one futures symbol, e.g. "BTCUSDT"."""
  planType: MixPlanTypeQuery
  productType: MixProductType
  idLessThan: NotRequired[str]
  """Pagination cursor: return orders with an order ID before this one. Pass the last page's `endId` to walk further back."""
  startTime: NotRequired[TimestampMillis]
  """Window lower bound, inclusive."""
  endTime: NotRequired[TimestampMillis]
  """Window upper bound, inclusive."""
  limit: NotRequired[int]
  """Rows per page. Defaults to 100."""


class MixOpenPlanOrdersPage(TypedDict):
  """One page of the caller's own pending plan orders."""

  entrustedList: list[MixPendingPlanOrder] | None
  """Pending plan orders. Confirmed live as `null` (not an empty array) when there are no rows."""
  endId: str | None
  """Cursor: pass as `idLessThan` to fetch the next page. Confirmed live as `null` when there is no further page (e.g. the result set is empty)."""


class Open(RpcEndpoint):
  """List the caller's own pending (not yet triggered) plan/TP-SL orders, private."""

  def open_paged(
    self,
    *,
    order_id: str | None = None,
    client_oid: str | None = None,
    symbol: str | None = None,
    plan_type: MixPlanTypeQuery,
    product_type: MixProductType,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[MixPendingPlanOrder, str]:
    """List the caller's own pending (not yet triggered) plan/TP-SL orders, private.

    Paged variant of `open`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      order_id: Restrict to one order.
      client_oid: Restrict to one order, by client order ID.
      symbol: Restrict to one futures symbol, e.g. "BTCUSDT".
      plan_type:
      product_type:
      start_time: Window lower bound, inclusive.
      end_time: Window upper bound, inclusive.
      limit: Rows per page. Defaults to 100.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-plan/classic-contract-plan#get-pending-trigger-order)
    """

    async def next(id_less_than: str) -> tuple[list[MixPendingPlanOrder], str | None]:
      response = await self.open(
        order_id=order_id,
        client_oid=client_oid,
        symbol=symbol,
        plan_type=plan_type,
        product_type=product_type,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        id_less_than=(id_less_than or None),
        validate=validate,
      )
      rows = response.get('entrustedList')
      state = response.get('endId')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def open(
    self,
    *,
    order_id: str | None = None,
    client_oid: str | None = None,
    symbol: str | None = None,
    plan_type: MixPlanTypeQuery,
    product_type: MixProductType,
    id_less_than: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> MixOpenPlanOrdersPage:
    """List the caller's own pending (not yet triggered) plan/TP-SL orders, private.

    Args:
      order_id: Restrict to one order.
      client_oid: Restrict to one order, by client order ID.
      symbol: Restrict to one futures symbol, e.g. "BTCUSDT".
      plan_type:
      product_type:
      id_less_than: Pagination cursor: return orders with an order ID before this one. Pass the last page's `endId` to walk further back.
      start_time: Window lower bound, inclusive.
      end_time: Window upper bound, inclusive.
      limit: Rows per page. Defaults to 100.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-plan/classic-contract-plan#get-pending-trigger-order)
    """
    request: Request = Request(planType=plan_type, productType=product_type)
    if order_id is not None:
      request['orderId'] = order_id
    if client_oid is not None:
      request['clientOid'] = client_oid
    if symbol is not None:
      request['symbol'] = symbol
    if id_less_than is not None:
      request['idLessThan'] = id_less_than
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/api/v2/mix/order/orders-plan-pending',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=MixOpenPlanOrdersPage,
    )
