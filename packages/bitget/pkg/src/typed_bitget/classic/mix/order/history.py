# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import (
  MixHoldSide,
  MixMarginMode,
  MixOrderSide,
  MixOrderType,
  MixPosMode,
  MixProductType,
  MixTimeInForce,
  MixTradeSide,
)


class MixHistoryOrder(TypedDict):
  """One historical order."""

  symbol: str
  """Futures symbol."""
  size: Decimal
  """Order size, in base coin."""
  orderId: str
  """Order ID."""
  clientOid: str
  """Client-supplied order ID."""
  baseVolume: Decimal
  """Filled size so far, in base coin."""
  fee: Decimal
  """Total fees paid on fills so far."""
  price: str
  """Limit price, if a limit order."""
  priceAvg: Decimal
  """Volume-weighted average fill price so far."""
  side: MixOrderSide
  force: MixTimeInForce
  totalProfits: Decimal
  """Realized profit from fills of this order."""
  posSide: MixHoldSide
  marginCoin: str
  """Margin coin."""
  quoteVolume: Decimal
  """Filled value so far, in quote coin."""
  leverage: int
  """Leverage in effect when this order was placed."""
  marginMode: MixMarginMode
  enterPointSource: str
  """Where the order originated. Venue does not publish a closed set for this field."""
  tradeSide: MixTradeSide
  posMode: MixPosMode
  orderType: MixOrderType
  orderSource: str
  """How the order was generated. Venue does not publish a closed set for this field in the response shape."""
  cTime: TimestampMillis
  """Order creation time."""
  uTime: TimestampMillis
  """Last update time."""
  presetStopSurplusPrice: str
  """Take-profit trigger price attached to the position this order opens, if any."""
  presetStopLossPrice: str
  """Stop-loss trigger price attached to the position this order opens, if any."""
  status: str
  """Order status. Venue does not publish a closed set for this field covering both open and closed orders (contrast `order/orders-pending`'s narrower confirmed `live`/`partially_filled` set)."""
  liqPrice: str
  """Estimated liquidation price at order time, if applicable."""


class Request(TypedDict):
  orderId: NotRequired[str]
  """Restrict to one order."""
  clientOid: NotRequired[str]
  """Restrict to one order, by client order ID."""
  symbol: NotRequired[str]
  """Restrict to one futures symbol, e.g. "BTCUSDT"."""
  productType: MixProductType
  idLessThan: NotRequired[str]
  """Pagination cursor: return orders with an order ID before this one. Pass the last page's `endId` to walk further back."""
  orderSource: NotRequired[
    Literal[
      'normal',
      'market',
      'profit_market',
      'loss_market',
      'Trader_delegate',
      'trader_profit',
      'trader_loss',
      'reverse',
      'trader_reverse',
      'profit_limit',
      'loss_limit',
      'liquidation',
      'delivery_close_long',
      'delivery_close_short',
      'pos_profit_limit',
      'pos_profit_market',
      'pos_loss_limit',
      'pos_loss_market',
    ]
  ]
  """Restrict to orders generated a specific way."""
  startTime: NotRequired[TimestampMillis]
  """Window lower bound, inclusive."""
  endTime: NotRequired[TimestampMillis]
  """Window upper bound, inclusive."""
  limit: NotRequired[int]
  """Rows per page. Defaults to 100."""


class MixHistoryOrdersPage(TypedDict):
  """One page of the caller's own historical orders."""

  entrustedList: list[MixHistoryOrder] | None
  """Historical orders. Confirmed live as `null` (not an empty array) when there are no rows."""
  endId: str | None
  """Cursor: pass as `idLessThan` to fetch the next page. Confirmed live as `null` when there is no further page (e.g. the result set is empty)."""


class History(RpcEndpoint):
  """List the caller's own historical (closed) orders, private."""

  def history_paged(
    self,
    *,
    order_id: str | None = None,
    client_oid: str | None = None,
    symbol: str | None = None,
    product_type: MixProductType,
    order_source: Literal[
      'normal',
      'market',
      'profit_market',
      'loss_market',
      'Trader_delegate',
      'trader_profit',
      'trader_loss',
      'reverse',
      'trader_reverse',
      'profit_limit',
      'loss_limit',
      'liquidation',
      'delivery_close_long',
      'delivery_close_short',
      'pos_profit_limit',
      'pos_profit_market',
      'pos_loss_limit',
      'pos_loss_market',
    ]
    | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[MixHistoryOrder, str]:
    """List the caller's own historical (closed) orders, private.

    Paged variant of `history`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      order_id: Restrict to one order.
      client_oid: Restrict to one order, by client order ID.
      symbol: Restrict to one futures symbol, e.g. "BTCUSDT".
      product_type:
      order_source: Restrict to orders generated a specific way.
      start_time: Window lower bound, inclusive.
      end_time: Window upper bound, inclusive.
      limit: Rows per page. Defaults to 100.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-trade/classic-contract-trade#get-history-order)
    """

    async def next(id_less_than: str) -> tuple[list[MixHistoryOrder], str | None]:
      response = await self.history(
        order_id=order_id,
        client_oid=client_oid,
        symbol=symbol,
        product_type=product_type,
        order_source=order_source,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        id_less_than=(id_less_than or None),
        validate=validate,
      )
      rows = response.get('entrustedList')
      state = response.get('endId')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def history(
    self,
    *,
    order_id: str | None = None,
    client_oid: str | None = None,
    symbol: str | None = None,
    product_type: MixProductType,
    id_less_than: str | None = None,
    order_source: Literal[
      'normal',
      'market',
      'profit_market',
      'loss_market',
      'Trader_delegate',
      'trader_profit',
      'trader_loss',
      'reverse',
      'trader_reverse',
      'profit_limit',
      'loss_limit',
      'liquidation',
      'delivery_close_long',
      'delivery_close_short',
      'pos_profit_limit',
      'pos_profit_market',
      'pos_loss_limit',
      'pos_loss_market',
    ]
    | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> MixHistoryOrdersPage:
    """List the caller's own historical (closed) orders, private.

    Args:
      order_id: Restrict to one order.
      client_oid: Restrict to one order, by client order ID.
      symbol: Restrict to one futures symbol, e.g. "BTCUSDT".
      product_type:
      id_less_than: Pagination cursor: return orders with an order ID before this one. Pass the last page's `endId` to walk further back.
      order_source: Restrict to orders generated a specific way.
      start_time: Window lower bound, inclusive.
      end_time: Window upper bound, inclusive.
      limit: Rows per page. Defaults to 100.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-trade/classic-contract-trade#get-history-order)
    """
    request: Request = Request(productType=product_type)
    if order_id is not None:
      request['orderId'] = order_id
    if client_oid is not None:
      request['clientOid'] = client_oid
    if symbol is not None:
      request['symbol'] = symbol
    if id_less_than is not None:
      request['idLessThan'] = id_less_than
    if order_source is not None:
      request['orderSource'] = order_source
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/api/v2/mix/order/orders-history',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=MixHistoryOrdersPage,
    )
