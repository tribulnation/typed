# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixProductType


class MixOrderResult(TypedDict):
  """Identifiers of the order just acted on."""

  orderId: str
  """Order ID assigned by the venue."""
  clientOid: str
  """Client-supplied order ID, echoed back."""


class Request(TypedDict):
  """Order modification request."""

  orderId: NotRequired[str]
  """Order ID to modify. Either this or `clientOid` is required."""
  clientOid: NotRequired[str]
  """Client order ID to modify. Either this or `orderId` is required."""
  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  productType: MixProductType
  newClientOid: str
  """New client order ID to assign, replacing the previous one."""
  newSize: NotRequired[str]
  """New order size, in base coin. Omit to leave unchanged."""
  newPrice: NotRequired[str]
  """New limit price. Omit to leave unchanged."""
  newPresetStopSurplusPrice: NotRequired[str]
  """New take-profit trigger price. Omit to leave unchanged."""
  newPresetStopLossPrice: NotRequired[str]
  """New stop-loss trigger price. Omit to leave unchanged."""


class Modify(RpcEndpoint):
  """Modify an open limit order's size, price, or attached TP/SL prices, private. Identify the order by `orderId` or `clientOid`."""

  async def modify(
    self,
    *,
    order_id: str | None = None,
    client_oid: str | None = None,
    symbol: str,
    product_type: MixProductType,
    new_client_oid: str,
    new_size: str | None = None,
    new_price: str | None = None,
    new_preset_stop_surplus_price: str | None = None,
    new_preset_stop_loss_price: str | None = None,
    validate: bool | None = None,
  ) -> MixOrderResult:
    """Modify an open limit order's size, price, or attached TP/SL prices, private. Identify the order by `orderId` or `clientOid`.

    Args:
      order_id: Order ID to modify. Either this or `clientOid` is required.
      client_oid: Client order ID to modify. Either this or `orderId` is required.
      symbol: Futures symbol, e.g. "BTCUSDT".
      product_type:
      new_client_oid: New client order ID to assign, replacing the previous one.
      new_size: New order size, in base coin. Omit to leave unchanged.
      new_price: New limit price. Omit to leave unchanged.
      new_preset_stop_surplus_price: New take-profit trigger price. Omit to leave unchanged.
      new_preset_stop_loss_price: New stop-loss trigger price. Omit to leave unchanged.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-trade/classic-contract-trade#modify-order)
    """
    request: Request = Request(
      symbol=symbol, productType=product_type, newClientOid=new_client_oid
    )
    if order_id is not None:
      request['orderId'] = order_id
    if client_oid is not None:
      request['clientOid'] = client_oid
    if new_size is not None:
      request['newSize'] = new_size
    if new_price is not None:
      request['newPrice'] = new_price
    if new_preset_stop_surplus_price is not None:
      request['newPresetStopSurplusPrice'] = new_preset_stop_surplus_price
    if new_preset_stop_loss_price is not None:
      request['newPresetStopLossPrice'] = new_preset_stop_loss_price
    return await self.request(
      request,
      method='POST',
      path='/api/v2/mix/order/modify-order',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=MixOrderResult,
    )
