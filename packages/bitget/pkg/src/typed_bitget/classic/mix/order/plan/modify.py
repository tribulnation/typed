# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import (
  MixPlanTypeNormalTrack,
  MixProductType,
  MixTriggerPriceType,
  MixTriggerType,
)


class MixOrderResult(TypedDict):
  """Identifiers of the order just acted on."""

  orderId: str
  """Order ID assigned by the venue."""
  clientOid: str
  """Client-supplied order ID, echoed back."""


class Request(TypedDict):
  """Plan order modification request."""

  planType: MixPlanTypeNormalTrack
  orderId: NotRequired[str]
  """Order ID to modify. Either this or `clientOid` is required."""
  clientOid: NotRequired[str]
  """Client order ID to modify. Either this or `orderId` is required."""
  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  productType: MixProductType
  newSize: NotRequired[str]
  """New order size, in base coin. Omit to leave unchanged."""
  newPrice: NotRequired[str]
  """New limit execution price. Omit to leave unchanged."""
  newCallbackRatio: NotRequired[str]
  """New trailing callback ratio. Omit to leave unchanged."""
  newTriggerPrice: NotRequired[Decimal]
  """New trigger price. Omit to leave unchanged."""
  newTriggerType: NotRequired[MixTriggerType]
  newStopSurplusTriggerPrice: NotRequired[str]
  """New take-profit trigger price. Omit to leave unchanged."""
  newStopSurplusExecutePrice: NotRequired[str]
  """New take-profit limit execution price. Omit to leave unchanged."""
  newStopSurplusTriggerType: NotRequired[MixTriggerPriceType]
  newStopLossTriggerPrice: NotRequired[str]
  """New stop-loss trigger price. Omit to leave unchanged."""
  newStopLossExecutePrice: NotRequired[str]
  """New stop-loss limit execution price. Omit to leave unchanged."""
  newStopLossTriggerType: NotRequired[MixTriggerPriceType]


class Modify(RpcEndpoint):
  """Modify a trigger (plan) order placed via `place-plan-order`, private."""

  async def modify(
    self,
    plan_type: MixPlanTypeNormalTrack,
    *,
    order_id: str | None = None,
    client_oid: str | None = None,
    symbol: str,
    product_type: MixProductType,
    new_size: str | None = None,
    new_price: str | None = None,
    new_callback_ratio: str | None = None,
    new_trigger_price: Decimal | None = None,
    new_trigger_type: MixTriggerType | None = None,
    new_stop_surplus_trigger_price: str | None = None,
    new_stop_surplus_execute_price: str | None = None,
    new_stop_surplus_trigger_type: MixTriggerPriceType | None = None,
    new_stop_loss_trigger_price: str | None = None,
    new_stop_loss_execute_price: str | None = None,
    new_stop_loss_trigger_type: MixTriggerPriceType | None = None,
    validate: bool | None = None,
  ) -> MixOrderResult:
    """Modify a trigger (plan) order placed via `place-plan-order`, private.

    Args:
      plan_type:
      order_id: Order ID to modify. Either this or `clientOid` is required.
      client_oid: Client order ID to modify. Either this or `orderId` is required.
      symbol: Futures symbol, e.g. "BTCUSDT".
      product_type:
      new_size: New order size, in base coin. Omit to leave unchanged.
      new_price: New limit execution price. Omit to leave unchanged.
      new_callback_ratio: New trailing callback ratio. Omit to leave unchanged.
      new_trigger_price: New trigger price. Omit to leave unchanged.
      new_trigger_type:
      new_stop_surplus_trigger_price: New take-profit trigger price. Omit to leave unchanged.
      new_stop_surplus_execute_price: New take-profit limit execution price. Omit to leave unchanged.
      new_stop_surplus_trigger_type:
      new_stop_loss_trigger_price: New stop-loss trigger price. Omit to leave unchanged.
      new_stop_loss_execute_price: New stop-loss limit execution price. Omit to leave unchanged.
      new_stop_loss_trigger_type:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-plan/classic-contract-plan#modify-trigger-order)
    """
    request: Request = Request(
      planType=plan_type, symbol=symbol, productType=product_type
    )
    if order_id is not None:
      request['orderId'] = order_id
    if client_oid is not None:
      request['clientOid'] = client_oid
    if new_size is not None:
      request['newSize'] = new_size
    if new_price is not None:
      request['newPrice'] = new_price
    if new_callback_ratio is not None:
      request['newCallbackRatio'] = new_callback_ratio
    if new_trigger_price is not None:
      request['newTriggerPrice'] = new_trigger_price
    if new_trigger_type is not None:
      request['newTriggerType'] = new_trigger_type
    if new_stop_surplus_trigger_price is not None:
      request['newStopSurplusTriggerPrice'] = new_stop_surplus_trigger_price
    if new_stop_surplus_execute_price is not None:
      request['newStopSurplusExecutePrice'] = new_stop_surplus_execute_price
    if new_stop_surplus_trigger_type is not None:
      request['newStopSurplusTriggerType'] = new_stop_surplus_trigger_type
    if new_stop_loss_trigger_price is not None:
      request['newStopLossTriggerPrice'] = new_stop_loss_trigger_price
    if new_stop_loss_execute_price is not None:
      request['newStopLossExecutePrice'] = new_stop_loss_execute_price
    if new_stop_loss_trigger_type is not None:
      request['newStopLossTriggerType'] = new_stop_loss_trigger_type
    return await self.request(
      request,
      method='POST',
      path='/api/v2/mix/order/modify-plan-order',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=MixOrderResult,
    )
