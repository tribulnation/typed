# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.order.cancel` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixCancelOrderRequest(TypedDict):
  """Order cancellation request."""

  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  productType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Which futures product family the request or response concerns."""
  marginCoin: NotRequired[str]
  """Margin coin, e.g. "USDT"."""
  orderId: NotRequired[str]
  """Order ID to cancel. Either this or `clientOid` is required."""
  clientOid: NotRequired[str]
  """Client order ID to cancel. Either this or `orderId` is required."""


class MixOrderResult(TypedDict):
  """Identifiers of the order just acted on."""

  orderId: str
  """Order ID assigned by the venue."""
  clientOid: str
  """Client-supplied order ID, echoed back."""


class Cancel(RpcEndpoint):
  """`classic.mix.order.cancel`."""

  async def cancel(
    self,
    mix_cancel_order_request: MixCancelOrderRequest,
    *,
    validate: bool | None = None,
  ) -> MixOrderResult:
    """Cancel one open order, private. Identify the order by `orderId` or `clientOid`.

    Args:
      mix_cancel_order_request: Order cancellation request.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/trade/Cancel-Order)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/mix/order/cancel-order',
      json=mix_cancel_order_request,
      validator=validator(MixOrderResult),
      validate=validate,
    )
