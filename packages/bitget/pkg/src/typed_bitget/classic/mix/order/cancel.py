# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixProductType


class MixOrderResult(TypedDict):
  """Identifiers of the order just acted on."""

  orderId: str
  """Order ID assigned by the venue."""
  clientOid: str
  """Client-supplied order ID, echoed back."""


class Request(TypedDict):
  """Order cancellation request."""

  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  productType: MixProductType
  marginCoin: NotRequired[str]
  """Margin coin, e.g. "USDT"."""
  orderId: NotRequired[str]
  """Order ID to cancel. Either this or `clientOid` is required."""
  clientOid: NotRequired[str]
  """Client order ID to cancel. Either this or `orderId` is required."""


class Cancel(RpcEndpoint):
  """Cancel one open order, private. Identify the order by `orderId` or `clientOid`."""

  async def cancel(
    self,
    *,
    symbol: str,
    product_type: MixProductType,
    margin_coin: str | None = None,
    order_id: str | None = None,
    client_oid: str | None = None,
    validate: bool | None = None,
  ) -> MixOrderResult:
    """Cancel one open order, private. Identify the order by `orderId` or `clientOid`.

    Args:
      symbol: Futures symbol, e.g. "BTCUSDT".
      product_type:
      margin_coin: Margin coin, e.g. "USDT".
      order_id: Order ID to cancel. Either this or `clientOid` is required.
      client_oid: Client order ID to cancel. Either this or `orderId` is required.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-trade/classic-contract-trade#cancel-order)
    """
    request: Request = Request(symbol=symbol, productType=product_type)
    if margin_coin is not None:
      request['marginCoin'] = margin_coin
    if order_id is not None:
      request['orderId'] = order_id
    if client_oid is not None:
      request['clientOid'] = client_oid
    return await self.request(
      request,
      method='POST',
      path='/api/v2/mix/order/cancel-order',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=MixOrderResult,
    )
