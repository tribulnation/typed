# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .batch_cancel import BatchCancel
from .batch_place import BatchPlace
from .cancel import Cancel
from .cancel_all import CancelAll
from .detail import Detail
from .fill_history import FillHistory
from .fills import Fills
from .history import History
from .modify import Modify
from .open import Open
from .place import Place
from .plan import Plan
from .tpsl import Tpsl


class Order(
  BatchCancel,
  BatchPlace,
  Cancel,
  CancelAll,
  Detail,
  FillHistory,
  Fills,
  History,
  Modify,
  Open,
  Place,
):
  """Bitget Classic's Futures Order API: placing, cancelling (single and batch), modifying, and querying open/historical orders and fills, plus flash-close and reversal, for futures trading.

  References:
    - [Upstream docs](https://www.bitget.com/docs/catalog/classic-contract-trade)
  """

  @cached_property
  def plan(self) -> Plan:
    """Bitget Classic's Futures Trigger (Plan) Order API: placing, modifying, cancelling, and querying conditional/trigger orders for futures trading.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-contract-plan)
    """
    return Plan(client=self.client)

  @cached_property
  def tpsl(self) -> Tpsl:
    """Bitget Classic's Futures Take-Profit/Stop-Loss API: placing, modifying, and querying TP/SL orders attached to a futures position.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-contract-plan)
    """
    return Tpsl(client=self.client)
