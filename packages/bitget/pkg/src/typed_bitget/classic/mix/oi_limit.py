# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixProductType


class MixOpenInterestLimit(TypedDict):
  """Open-interest notional limit for one symbol."""

  symbol: str
  """Futures symbol."""
  notionalValue: Decimal
  """Per-account open-interest notional limit."""
  totalNotionalValue: Decimal
  """Platform-wide open-interest notional limit."""


class Request(TypedDict):
  productType: MixProductType
  symbol: NotRequired[str]
  """Restrict to a single symbol, e.g. "BTCUSDT"."""


Response = list[MixOpenInterestLimit]


class OiLimit(RpcEndpoint):
  """Get open-interest notional limits, public. Returns every symbol of the product type when `symbol` is omitted."""

  async def oi_limit(
    self,
    product_type: MixProductType,
    *,
    symbol: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get open-interest notional limits, public. Returns every symbol of the product type when `symbol` is omitted.

    Args:
      product_type:
      symbol: Restrict to a single symbol, e.g. "BTCUSDT".
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-market/classic-contract-market#get-contract-oi-limit)
    """
    request: Request = Request(productType=product_type)
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='/api/v2/mix/market/oi-limit',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
