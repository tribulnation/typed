# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.discount_rate` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixDiscountRateTier(TypedDict):
  """One discount-rate tier."""

  tier: str
  """Tier number."""
  minAmount: str
  """Lower bound of this tier's held amount."""
  maxAmount: str
  """Upper bound of this tier's held amount."""
  discountRate: str
  """Discount rate applied within this tier."""


class MixDiscountRateCoin(TypedDict):
  """Discount-rate tiers for one collateral coin."""

  coin: str
  """Collateral coin."""
  userLimit: str
  """Maximum discounted amount recognised per user."""
  totalLimit: str
  """Maximum discounted amount recognised platform-wide."""
  discountRateList: list[MixDiscountRateTier]
  """Tiers, by held amount."""


class DiscountRate(RpcEndpoint):
  """`classic.mix.discount_rate`."""

  async def discount_rate(
    self,
    *,
    validate: bool | None = None,
  ) -> list[MixDiscountRateCoin]:
    """List asset discount-rate tiers used when valuing non-USDT collateral toward margin, public.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/market/Get-Discount-Rate)
    """
    return await self.request(
      'GET',
      '/api/v2/mix/market/discount-rate',
      validator=validator(list[MixDiscountRateCoin]),
      validate=validate,
    )
