# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.isolated_borrow_rate` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixIsolatedBorrowRate(TypedDict):
  """Isolated borrowing ratio for one time bucket, for one bucket."""

  ts: TimestampMillis
  """Bucket time."""
  borrowRate: str
  """Isolated-margin borrowing ratio for the bucket."""


class IsolatedBorrowRate(RpcEndpoint):
  """`classic.mix.isolated_borrow_rate`."""

  async def isolated_borrow_rate(
    self,
    *,
    symbol: str,
    period: str | None = None,
    validate: bool | None = None,
  ) -> list[MixIsolatedBorrowRate]:
    """Get the isolated-margin borrowing ratio for one symbol, public.

    Args:
      symbol: Futures symbol, e.g. "BTCUSDT".
      period: Bucket interval for the series, e.g. "5m"/"1H"/"1D". Optional; defaults to the venue's own default bucket. See endpoint `notes` on why this is not an `enum`.
    """
    params: dict = {
      'symbol': symbol,
    }
    if period is not None:
      params['period'] = period
    return await self.request(
      'GET',
      '/api/v2/mix/market/isolated-borrow-rate',
      params=params,
      validator=validator(list[MixIsolatedBorrowRate]),
      validate=validate,
    )
