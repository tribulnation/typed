# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.position.close` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixClosePositionFailure(TypedDict):
  """One position that failed to close."""

  orderId: str
  """Order ID, if one was assigned before rejection."""
  clientOid: str
  """Client order ID, if one was assigned before rejection."""
  symbol: str
  """Futures symbol."""
  errorMsg: str
  """Human-readable rejection reason."""
  errorCode: str
  """Venue error code for the rejection."""


class MixClosePositionSuccess(TypedDict):
  """One closed position."""

  orderId: str
  """Order ID of the closing order."""
  clientOid: str
  """Client order ID of the closing order."""
  symbol: str
  """Futures symbol."""


class MixClosePositionsRequest(TypedDict):
  """Flash-close request."""

  symbol: NotRequired[str]
  """Restrict to one futures symbol, e.g. "BTCUSDT". Omit to close every open position of the product type."""
  productType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Which futures product family the request or response concerns."""
  holdSide: NotRequired[Literal['long', 'short']]
  """Which side of a hedge-mode position this concerns."""


class MixClosePositionsResult(TypedDict):
  """Per-position outcome of the flash-close."""

  successList: list[MixClosePositionSuccess]
  """Positions closed."""
  failureList: list[MixClosePositionFailure]
  """Positions not closed."""


class Close(RpcEndpoint):
  """`classic.mix.position.close`."""

  async def close(
    self,
    mix_close_positions_request: MixClosePositionsRequest,
    *,
    validate: bool | None = None,
  ) -> MixClosePositionsResult:
    """Flash-close one or all open positions at market price, private.

    Args:
      mix_close_positions_request: Flash-close request.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/trade/Flash-Close-Position)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/mix/order/close-positions',
      json=mix_close_positions_request,
      validator=validator(MixClosePositionsResult),
      validate=validate,
    )
