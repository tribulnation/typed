# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixPosition, MixProductType


class Request(TypedDict):
  productType: MixProductType
  marginCoin: NotRequired[str]
  """Restrict to one margin coin, e.g. "USDT"."""


Response = list[MixPosition]


class List(RpcEndpoint):
  """List all open positions for a product type, private."""

  async def list(
    self,
    product_type: MixProductType,
    *,
    margin_coin: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """List all open positions for a product type, private.

    Args:
      product_type:
      margin_coin: Restrict to one margin coin, e.g. "USDT".
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-position/classic-contract-position#get-all-positions)
    """
    request: Request = Request(productType=product_type)
    if margin_coin is not None:
      request['marginCoin'] = margin_coin
    return await self.request(
      request,
      method='GET',
      path='/api/v2/mix/position/all-position',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
