# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixPosition, MixProductType


class Request(TypedDict):
  productType: MixProductType
  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  marginCoin: str
  """Margin coin, e.g. "USDT"."""


Response = list[MixPosition]


class Get(RpcEndpoint):
  """Get open positions for one symbol, private. A hedge-mode symbol may return both a long and a short row."""

  async def get(
    self,
    product_type: MixProductType,
    *,
    symbol: str,
    margin_coin: str,
    validate: bool | None = None,
  ) -> Response:
    """Get open positions for one symbol, private. A hedge-mode symbol may return both a long and a short row.

    Args:
      product_type:
      symbol: Futures symbol, e.g. "BTCUSDT".
      margin_coin: Margin coin, e.g. "USDT".
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-position/classic-contract-position#get-single-position)
    """
    return await self.request(
      Request(productType=product_type, symbol=symbol, marginCoin=margin_coin),
      method='GET',
      path='/api/v2/mix/position/single-position',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
