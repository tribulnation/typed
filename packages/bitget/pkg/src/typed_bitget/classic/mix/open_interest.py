# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.open_interest` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixOpenInterestEntry(TypedDict):
  """Open interest for one symbol."""

  symbol: str
  """Futures symbol."""
  size: str
  """Open interest, in base coin."""


class MixOpenInterest(TypedDict):
  """Open interest snapshot."""

  openInterestList: list[MixOpenInterestEntry]
  """Open interest by symbol (single entry for this request)."""
  ts: TimestampMillis
  """Snapshot time."""


class OpenInterest(RpcEndpoint):
  """`classic.mix.open_interest`."""

  async def open_interest(
    self,
    *,
    symbol: str,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    validate: bool | None = None,
  ) -> MixOpenInterest:
    """Get one symbol's current open interest, public.

    Args:
      symbol: Futures symbol, e.g. "BTCUSDT".
      product_type: Which futures product family this request concerns.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/market/Get-Open-Interest)
    """
    params: dict = {
      'symbol': symbol,
      'productType': product_type,
    }
    return await self.request(
      'GET',
      '/api/v2/mix/market/open-interest',
      params=params,
      validator=validator(MixOpenInterest),
      validate=validate,
    )
