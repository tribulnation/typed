# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixProductType


class MixOpenInterestEntry(TypedDict):
  """Open interest for one symbol."""

  symbol: str
  """Futures symbol."""
  size: Decimal
  """Open interest, in base coin."""


class MixOpenInterest(TypedDict):
  """Open interest snapshot."""

  openInterestList: list[MixOpenInterestEntry]
  """Open interest by symbol (single entry for this request)."""
  ts: TimestampMillis
  """Snapshot time."""


class Request(TypedDict):
  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  productType: MixProductType


class OpenInterest(RpcEndpoint):
  """Get one symbol's current open interest, public."""

  async def open_interest(
    self,
    symbol: str,
    *,
    product_type: MixProductType,
    validate: bool | None = None,
  ) -> MixOpenInterest:
    """Get one symbol's current open interest, public.

    Args:
      symbol: Futures symbol, e.g. "BTCUSDT".
      product_type:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-market/classic-contract-market#get-open-interest)
    """
    return await self.request(
      Request(symbol=symbol, productType=product_type),
      method='GET',
      path='/api/v2/mix/market/open-interest',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=MixOpenInterest,
    )
