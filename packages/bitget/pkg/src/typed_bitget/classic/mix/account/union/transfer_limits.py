# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixUnionTransferLimits(TypedDict):
  """Transfer-in limit for one coin."""

  coin: str
  """Coin the limit applies to."""
  maxTransferIn: Decimal
  """Maximum amount of this coin that may be transferred in."""


class Request(TypedDict):
  coin: str
  """Coin to look up, e.g. "USDT"."""


class TransferLimits(RpcEndpoint):
  """Get the maximum amount of one coin that may be transferred into the union-margin account, private."""

  async def transfer_limits(
    self,
    coin: str,
    *,
    validate: bool | None = None,
  ) -> MixUnionTransferLimits:
    """Get the maximum amount of one coin that may be transferred into the union-margin account, private.

    Args:
      coin: Coin to look up, e.g. "USDT".
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-account/classic-contract-account#get-union-transfer-limits)
    """
    return await self.request(
      Request(coin=coin),
      method='GET',
      path='/api/v2/mix/account/transfer-limits',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=MixUnionTransferLimits,
    )
