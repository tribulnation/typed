# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixHoldSide, MixProductType


Response = str


class Request(TypedDict):
  """Isolated margin adjustment request."""

  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  productType: MixProductType
  marginCoin: str
  """Margin coin, e.g. "USDT"."""
  holdSide: MixHoldSide
  amount: Decimal
  """Amount to add (positive) or remove (negative) as margin."""


class SetMargin(RpcEndpoint):
  """Add or remove isolated margin for one position, private."""

  async def set_margin(
    self,
    *,
    symbol: str,
    product_type: MixProductType,
    margin_coin: str,
    hold_side: MixHoldSide,
    amount: Decimal,
    validate: bool | None = None,
  ) -> Response:
    """Add or remove isolated margin for one position, private.

    Args:
      symbol: Futures symbol, e.g. "BTCUSDT".
      product_type:
      margin_coin: Margin coin, e.g. "USDT".
      hold_side:
      amount: Amount to add (positive) or remove (negative) as margin.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-account/classic-contract-account#adjust-position-margin)
    """
    return await self.request(
      Request(
        symbol=symbol,
        productType=product_type,
        marginCoin=margin_coin,
        holdSide=hold_side,
        amount=amount,
      ),
      method='POST',
      path='/api/v2/mix/account/set-margin',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
