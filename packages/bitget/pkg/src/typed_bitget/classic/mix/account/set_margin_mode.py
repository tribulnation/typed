# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixMarginMode, MixProductType


class MixMarginModeSettings(TypedDict):
  """Margin mode now in effect for this symbol/margin coin."""

  symbol: str
  """Futures symbol."""
  marginCoin: str
  """Margin coin."""
  longLeverage: Decimal
  """Leverage applied to long positions (or the single leverage, in one-way mode)."""
  shortLeverage: Decimal
  """Leverage applied to short positions (or the single leverage, in one-way mode)."""
  marginMode: MixMarginMode


class Request(TypedDict):
  """Margin-mode update request."""

  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  productType: MixProductType
  marginCoin: str
  """Margin coin, e.g. "USDT"."""
  marginMode: MixMarginMode


class SetMarginMode(RpcEndpoint):
  """Switch a symbol between isolated and cross margin mode, private."""

  async def set_margin_mode(
    self,
    *,
    symbol: str,
    product_type: MixProductType,
    margin_coin: str,
    margin_mode: MixMarginMode,
    validate: bool | None = None,
  ) -> MixMarginModeSettings:
    """Switch a symbol between isolated and cross margin mode, private.

    Args:
      symbol: Futures symbol, e.g. "BTCUSDT".
      product_type:
      margin_coin: Margin coin, e.g. "USDT".
      margin_mode:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-account/classic-contract-account#change-margin-mode)
    """
    return await self.request(
      Request(
        symbol=symbol,
        productType=product_type,
        marginCoin=margin_coin,
        marginMode=margin_mode,
      ),
      method='POST',
      path='/api/v2/mix/account/set-margin-mode',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=MixMarginModeSettings,
    )
