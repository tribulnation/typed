# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixProductType


class MixIsolatedSymbol(TypedDict):
  """One symbol in isolated margin mode."""

  symbol: str
  """Futures symbol."""
  marginMode: Literal['isolated']
  """Always `"isolated"` for a row in this list."""


class Request(TypedDict):
  productType: MixProductType


Response = list[MixIsolatedSymbol]


class IsolatedSymbols(RpcEndpoint):
  """List symbols currently in isolated margin mode under the account, private."""

  async def isolated_symbols(
    self,
    product_type: MixProductType,
    *,
    validate: bool | None = None,
  ) -> Response:
    """List symbols currently in isolated margin mode under the account, private.

    Args:
      product_type:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-account/classic-contract-account#get-isolated-symbols)
    """
    return await self.request(
      Request(productType=product_type),
      method='GET',
      path='/api/v2/mix/account/isolated-symbols',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
