# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.account.union.config` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixUnionConfig(TypedDict):
  """Union margin configuration."""

  imr: str
  """Liability initial margin rate."""
  mmr: str
  """Liability maintenance margin rate."""
  individualLimit: str
  """Personal liability limit."""
  individualLimitRatio: str
  """Personal liability limit, as a ratio."""


class Config(RpcEndpoint):
  """`classic.mix.account.union.config`."""

  async def config(self, *, validate: bool | None = None) -> MixUnionConfig:
    """Get union-margin configuration parameters (liability margin rates and per-user liability limits), private.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/account/Get-Union-Config)
    """
    return await self.authed_request(
      'GET',
      '/api/v2/mix/account/union-config',
      validator=validator(MixUnionConfig),
      validate=validate,
    )
