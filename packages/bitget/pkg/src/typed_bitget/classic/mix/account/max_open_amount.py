# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixHoldSide, MixOrderType, MixProductType


class MixMaxOpen(TypedDict):
  """Maximum size the given parameters could open."""

  maxOpen: Decimal
  """Maximum order size, in base coin."""


class Request(TypedDict):
  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  productType: MixProductType
  marginCoin: str
  """Margin coin, e.g. "USDT"."""
  posSide: MixHoldSide
  orderType: MixOrderType
  openPrice: NotRequired[str]
  """Price the hypothetical order would open at. Required for a limit order; omit for a market order."""


class MaxOpenAmount(RpcEndpoint):
  """Get the maximum size a new order could open at a given price/leverage, private."""

  async def max_open_amount(
    self,
    *,
    symbol: str,
    product_type: MixProductType,
    margin_coin: str,
    pos_side: MixHoldSide,
    order_type: MixOrderType,
    open_price: str | None = None,
    validate: bool | None = None,
  ) -> MixMaxOpen:
    """Get the maximum size a new order could open at a given price/leverage, private.

    Args:
      symbol: Futures symbol, e.g. "BTCUSDT".
      product_type:
      margin_coin: Margin coin, e.g. "USDT".
      pos_side:
      order_type:
      open_price: Price the hypothetical order would open at. Required for a limit order; omit for a market order.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-account/classic-contract-account#get-max-openable-quantity)
    """
    request: Request = Request(
      symbol=symbol,
      productType=product_type,
      marginCoin=margin_coin,
      posSide=pos_side,
      orderType=order_type,
    )
    if open_price is not None:
      request['openPrice'] = open_price
    return await self.request(
      request,
      method='GET',
      path='/api/v2/mix/account/max-open',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=MixMaxOpen,
    )
