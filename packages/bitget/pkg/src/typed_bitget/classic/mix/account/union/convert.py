# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixUnionConvert(TypedDict):
  """Result of the conversion."""

  usdtAmount: str
  """USDT amount resulting from the conversion."""


class Request(TypedDict):
  """Union margin conversion request."""

  coin: str
  """Coin to convert."""
  amount: str
  """Amount to convert."""


class Convert(RpcEndpoint):
  """Convert a union-margin coin balance to/from USDT, private."""

  async def convert(
    self,
    *,
    coin: str,
    amount: str,
    validate: bool | None = None,
  ) -> MixUnionConvert:
    """Convert a union-margin coin balance to/from USDT, private.

    Args:
      coin: Coin to convert.
      amount: Amount to convert.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-account/classic-contract-account#union-convert)
    """
    return await self.request(
      Request(coin=coin, amount=amount),
      method='POST',
      path='/api/v2/mix/account/union-convert',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=MixUnionConvert,
    )
