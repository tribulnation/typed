# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.account.union.convert` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixUnionConvert(TypedDict):
  """Result of the conversion."""

  usdtAmount: str
  """USDT amount resulting from the conversion."""


class MixUnionConvertRequest(TypedDict):
  """Union margin conversion request."""

  coin: str
  """Coin to convert."""
  amount: str
  """Amount to convert."""


class Convert(RpcEndpoint):
  """`classic.mix.account.union.convert`."""

  async def convert(
    self,
    mix_union_convert_request: MixUnionConvertRequest,
    *,
    validate: bool | None = None,
  ) -> MixUnionConvert:
    """Convert a union-margin coin balance to/from USDT, private.

    Args:
      mix_union_convert_request: Union margin conversion request.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/account/Union-Convert)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/mix/account/union-convert',
      json=mix_union_convert_request,
      validator=validator(MixUnionConvert),
      validate=validate,
    )
