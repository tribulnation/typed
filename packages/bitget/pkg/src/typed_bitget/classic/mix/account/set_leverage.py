# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.account.set_leverage` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixLeverageSettings(TypedDict):
  """Leverage now in effect for this symbol/margin coin."""

  symbol: str
  """Futures symbol."""
  marginCoin: str
  """Margin coin."""
  longLeverage: str
  """Leverage applied to long positions (or the single leverage, in one-way mode)."""
  shortLeverage: str
  """Leverage applied to short positions (or the single leverage, in one-way mode)."""
  crossMarginLeverage: str
  """Leverage applied under cross margin mode."""
  marginMode: Literal['isolated', 'crossed']
  """Margin mode governing how margin is allocated to a position."""


class MixSetLeverageRequest(TypedDict):
  """Leverage update request."""

  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  productType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Which futures product family the request or response concerns."""
  marginCoin: str
  """Margin coin, e.g. "USDT"."""
  leverage: str
  """Leverage to apply."""
  holdSide: NotRequired[Literal['long', 'short']]
  """Which side of a hedge-mode position this concerns."""


class SetLeverage(RpcEndpoint):
  """`classic.mix.account.set_leverage`."""

  async def set_leverage(
    self,
    mix_set_leverage_request: MixSetLeverageRequest,
    *,
    validate: bool | None = None,
  ) -> MixLeverageSettings:
    """Set leverage for one symbol/margin coin, private.

    Args:
      mix_set_leverage_request: Leverage update request.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/account/Change-Leverage)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/mix/account/set-leverage',
      json=mix_set_leverage_request,
      validator=validator(MixLeverageSettings),
      validate=validate,
    )
