# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixHoldSide, MixMarginMode, MixProductType


class MixLeverageSettings(TypedDict):
  """Leverage now in effect for this symbol/margin coin."""

  symbol: str
  """Futures symbol."""
  marginCoin: str
  """Margin coin."""
  longLeverage: Decimal
  """Leverage applied to long positions (or the single leverage, in one-way mode)."""
  shortLeverage: Decimal
  """Leverage applied to short positions (or the single leverage, in one-way mode)."""
  crossMarginLeverage: str
  """Leverage applied under cross margin mode."""
  marginMode: MixMarginMode


class Request(TypedDict):
  """Leverage update request."""

  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  productType: MixProductType
  marginCoin: str
  """Margin coin, e.g. "USDT"."""
  leverage: Decimal
  """Leverage to apply."""
  holdSide: NotRequired[MixHoldSide]


class SetLeverage(RpcEndpoint):
  """Set leverage for one symbol/margin coin, private."""

  async def set_leverage(
    self,
    *,
    symbol: str,
    product_type: MixProductType,
    margin_coin: str,
    leverage: Decimal,
    hold_side: MixHoldSide | None = None,
    validate: bool | None = None,
  ) -> MixLeverageSettings:
    """Set leverage for one symbol/margin coin, private.

    Args:
      symbol: Futures symbol, e.g. "BTCUSDT".
      product_type:
      margin_coin: Margin coin, e.g. "USDT".
      leverage: Leverage to apply.
      hold_side:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-account/classic-contract-account#change-leverage)
    """
    request: Request = Request(
      symbol=symbol, productType=product_type, marginCoin=margin_coin, leverage=leverage
    )
    if hold_side is not None:
      request['holdSide'] = hold_side
    return await self.request(
      request,
      method='POST',
      path='/api/v2/mix/account/set-leverage',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=MixLeverageSettings,
    )
