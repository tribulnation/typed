# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixUnionSwitchUsdt(TypedDict):
  """Available USDT quota for the union-to-single switch."""

  usdtAmount: str
  """USDT amount available for the switch."""


class SwitchUsdt(RpcEndpoint):
  """Get the USDT quota available for switching from union margin back to single margin mode, private."""

  async def switch_usdt(self, *, validate: bool | None = None) -> MixUnionSwitchUsdt:
    """Get the USDT quota available for switching from union margin back to single margin mode, private.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-account/classic-contract-account#get-switch-union-usdt)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v2/mix/account/switch-union-usdt',
      meta={'required': True},
      validate=validate,
      response_type=MixUnionSwitchUsdt,
    )
