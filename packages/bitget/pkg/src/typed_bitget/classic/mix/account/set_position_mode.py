# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.account.set_position_mode` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixPositionModeSettings(TypedDict):
  """Position mode now in effect."""

  posMode: Literal['one_way_mode', 'hedge_mode']
  """Position mode: one-way (net position per symbol) or hedge (independent long and short positions per symbol)."""


class MixSetPositionModeRequest(TypedDict):
  """Position-mode update request."""

  productType: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Which futures product family the request or response concerns."""
  posMode: Literal['one_way_mode', 'hedge_mode']
  """Position mode: one-way (net position per symbol) or hedge (independent long and short positions per symbol)."""


class SetPositionMode(RpcEndpoint):
  """`classic.mix.account.set_position_mode`."""

  async def set_position_mode(
    self,
    mix_set_position_mode_request: MixSetPositionModeRequest,
    *,
    validate: bool | None = None,
  ) -> MixPositionModeSettings:
    """Switch a product type between one-way and hedge position mode, private.

    Args:
      mix_set_position_mode_request: Position-mode update request.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/account/Change-Hold-Mode)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/mix/account/set-position-mode',
      json=mix_set_position_mode_request,
      validator=validator(MixPositionModeSettings),
      validate=validate,
    )
