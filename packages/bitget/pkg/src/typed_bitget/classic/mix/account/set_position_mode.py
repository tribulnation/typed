# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixPosMode, MixProductType


class MixPositionModeSettings(TypedDict):
  """Position mode now in effect."""

  posMode: MixPosMode


class Request(TypedDict):
  """Position-mode update request."""

  productType: MixProductType
  posMode: MixPosMode


class SetPositionMode(RpcEndpoint):
  """Switch a product type between one-way and hedge position mode, private."""

  async def set_position_mode(
    self,
    product_type: MixProductType,
    *,
    pos_mode: MixPosMode,
    validate: bool | None = None,
  ) -> MixPositionModeSettings:
    """Switch a product type between one-way and hedge position mode, private.

    Args:
      product_type:
      pos_mode:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-account/classic-contract-account#change-position-mode)
    """
    return await self.request(
      Request(productType=product_type, posMode=pos_mode),
      method='POST',
      path='/api/v2/mix/account/set-position-mode',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=MixPositionModeSettings,
    )
