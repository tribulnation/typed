# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixHoldSide


Response = str


class Request(TypedDict):
  """Auto-margin toggle request."""

  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  autoMargin: Literal['on', 'off']
  """Whether to enable automatic margin top-up."""
  marginCoin: str
  """Margin coin, e.g. "USDT"."""
  amount: Decimal
  """Amount to add or remove as margin. Required by the venue even though this call's effect is the on/off toggle; consult the position's current margin before setting it."""
  holdSide: MixHoldSide


class SetAutoMargin(RpcEndpoint):
  """Turn automatic margin top-up on or off for one isolated-margin position, private."""

  async def set_auto_margin(
    self,
    *,
    symbol: str,
    auto_margin: Literal['on', 'off'],
    margin_coin: str,
    amount: Decimal,
    hold_side: MixHoldSide,
    validate: bool | None = None,
  ) -> Response:
    """Turn automatic margin top-up on or off for one isolated-margin position, private.

    Args:
      symbol: Futures symbol, e.g. "BTCUSDT".
      auto_margin: Whether to enable automatic margin top-up.
      margin_coin: Margin coin, e.g. "USDT".
      amount: Amount to add or remove as margin. Required by the venue even though this call's effect is the on/off toggle; consult the position's current margin before setting it.
      hold_side:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-account/classic-contract-account#set-isolated-position-auto-margin)
    """
    return await self.request(
      Request(
        symbol=symbol,
        autoMargin=auto_margin,
        marginCoin=margin_coin,
        amount=amount,
        holdSide=hold_side,
      ),
      method='POST',
      path='/api/v2/mix/account/set-auto-margin',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
