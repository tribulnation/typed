# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.account.set_auto_margin` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixSetAutoMarginRequest(TypedDict):
  """Auto-margin toggle request."""

  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  autoMargin: Literal['on', 'off']
  """Whether to enable automatic margin top-up."""
  marginCoin: str
  """Margin coin, e.g. "USDT"."""
  amount: str
  """Amount to add or remove as margin. Required by the venue even though this call's effect is the on/off toggle; consult the position's current margin before setting it."""
  holdSide: NotRequired[Literal['long', 'short']]
  """Which side of a hedge-mode position this concerns."""


class SetAutoMargin(RpcEndpoint):
  """`classic.mix.account.set_auto_margin`."""

  async def set_auto_margin(
    self,
    mix_set_auto_margin_request: MixSetAutoMarginRequest,
    *,
    validate: bool | None = None,
  ) -> str:
    """Turn automatic margin top-up on or off for one isolated-margin position, private.

    Args:
      mix_set_auto_margin_request: Auto-margin toggle request.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/account/Set-Auto-Margin)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/mix/account/set-auto-margin',
      json=mix_set_auto_margin_request,
      validator=validator(str),
      validate=validate,
    )
