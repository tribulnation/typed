# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixHoldSide, MixOrderType, MixProductType


class MixLiquidationPrice(TypedDict):
  """Estimated liquidation price."""

  liqPrice: Decimal
  """Estimated liquidation price."""


class Request(TypedDict):
  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  productType: MixProductType
  marginCoin: str
  """Margin coin, e.g. "USDT"."""
  posSide: MixHoldSide
  orderType: MixOrderType
  openAmount: Decimal
  """Size the hypothetical order would open, in base coin."""
  openPrice: NotRequired[str]
  """Price the hypothetical order would open at. Required for a limit order; omit for a market order."""


class LiquidationPrice(RpcEndpoint):
  """Estimate the liquidation price for a hypothetical order, private."""

  async def liquidation_price(
    self,
    *,
    symbol: str,
    product_type: MixProductType,
    margin_coin: str,
    pos_side: MixHoldSide,
    order_type: MixOrderType,
    open_amount: Decimal,
    open_price: str | None = None,
    validate: bool | None = None,
  ) -> MixLiquidationPrice:
    """Estimate the liquidation price for a hypothetical order, private.

    Args:
      symbol: Futures symbol, e.g. "BTCUSDT".
      product_type:
      margin_coin: Margin coin, e.g. "USDT".
      pos_side:
      order_type:
      open_amount: Size the hypothetical order would open, in base coin.
      open_price: Price the hypothetical order would open at. Required for a limit order; omit for a market order.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-account/classic-contract-account#get-liquidation-price)
    """
    request: Request = Request(
      symbol=symbol,
      productType=product_type,
      marginCoin=margin_coin,
      posSide=pos_side,
      orderType=order_type,
      openAmount=open_amount,
    )
    if open_price is not None:
      request['openPrice'] = open_price
    return await self.request(
      request,
      method='GET',
      path='/api/v2/mix/account/liq-price',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=MixLiquidationPrice,
    )
