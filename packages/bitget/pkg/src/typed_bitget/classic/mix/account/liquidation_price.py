# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.account.liquidation_price` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixLiquidationPrice(TypedDict):
  """Estimated liquidation price."""

  liqPrice: str
  """Estimated liquidation price."""


class LiquidationPrice(RpcEndpoint):
  """`classic.mix.account.liquidation_price`."""

  async def liquidation_price(
    self,
    *,
    symbol: str,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    margin_coin: str,
    pos_side: Literal['long', 'short'],
    order_type: Literal['limit', 'market'],
    open_amount: str,
    open_price: str | None = None,
    validate: bool | None = None,
  ) -> MixLiquidationPrice:
    """Estimate the liquidation price for a hypothetical order, private.

    Args:
      symbol: Futures symbol, e.g. "BTCUSDT".
      product_type: Which futures product family this request concerns.
      margin_coin: Margin coin, e.g. "USDT".
      pos_side: Side the position would be opened on.
      order_type: Order type the hypothetical order would use.
      open_amount: Size the hypothetical order would open, in base coin.
      open_price: Price the hypothetical order would open at. Required for a limit order; omit for a market order.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/account/Get-Liquidation-Price)
    """
    params: dict = {
      'symbol': symbol,
      'productType': product_type,
      'marginCoin': margin_coin,
      'posSide': pos_side,
      'orderType': order_type,
      'openAmount': open_amount,
    }
    if open_price is not None:
      params['openPrice'] = open_price
    return await self.authed_request(
      'GET',
      '/api/v2/mix/account/liq-price',
      params=params,
      validator=validator(MixLiquidationPrice),
      validate=validate,
    )
