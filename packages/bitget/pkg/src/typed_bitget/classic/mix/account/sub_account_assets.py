# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixProductType


class MixSubAccountAsset(TypedDict):
  """One sub-account balance."""

  marginCoin: str
  """Margin coin."""
  locked: str
  """Amount locked by open orders or pending withdrawals."""
  available: str
  """Amount available to open new positions or withdraw."""
  crossedMaxAvailable: str
  """Maximum amount available under cross margin mode."""
  isolatedMaxAvailable: str
  """Maximum amount available under isolated margin mode."""
  maxTransferOut: str
  """Maximum amount that may be transferred out right now."""
  accountEquity: str
  """Account equity, in margin coin."""
  usdtEquity: str
  """Account equity, converted to USDT."""
  btcEquity: str
  """Account equity, converted to BTC."""
  unrealizedPL: str
  """Unrealized profit and loss across positions."""
  coupon: str
  """Coupon (bonus) balance applied toward margin."""


class MixSubAccountAssets(TypedDict):
  """Futures assets held by one sub-account."""

  userId: int
  """Sub-account user ID."""
  assetList: list[MixSubAccountAsset]
  """Balances held by this sub-account."""


class Request(TypedDict):
  productType: MixProductType


Response = list[MixSubAccountAssets]


class SubAccountAssets(RpcEndpoint):
  """List futures account assets for every sub-account under the main account, private."""

  async def sub_account_assets(
    self,
    product_type: MixProductType,
    *,
    validate: bool | None = None,
  ) -> Response:
    """List futures account assets for every sub-account under the main account, private.

    Args:
      product_type:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-account/classic-contract-account#get-subaccount-assets)
    """
    return await self.request(
      Request(productType=product_type),
      method='GET',
      path='/api/v2/mix/account/sub-account-assets',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
