# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.account.set_asset_mode` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixSetAssetModeRequest(TypedDict):
  """Asset-mode update request."""

  productType: Literal['USDT-FUTURES', 'SUSDT-FUTURES']
  """Which futures product family this applies to."""
  assetMode: Literal['single', 'union']
  """Asset mode to switch to: `single` (margin held per-symbol) or `union` (margin shared across symbols)."""


class SetAssetMode(RpcEndpoint):
  """`classic.mix.account.set_asset_mode`."""

  async def set_asset_mode(
    self,
    mix_set_asset_mode_request: MixSetAssetModeRequest,
    *,
    validate: bool | None = None,
  ) -> str:
    """Switch between single-asset and union (multi-asset) margin mode, private.

    Args:
      mix_set_asset_mode_request: Asset-mode update request.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/account/Change-Asset-Mode)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/mix/account/set-asset-mode',
      json=mix_set_asset_mode_request,
      validator=validator(str),
      validate=validate,
    )
