# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixProductType


class MixOpenCount(TypedDict):
  """Maximum position size the given margin/price/leverage could open."""

  size: Decimal
  """Maximum position size, in base coin."""


class Request(TypedDict):
  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  productType: MixProductType
  marginCoin: str
  """Margin coin, e.g. "USDT"."""
  openAmount: Decimal
  """Margin amount available to open the position."""
  openPrice: Decimal
  """Price the hypothetical order would open at."""
  leverage: NotRequired[str]
  """Leverage to evaluate. Defaults to the symbol's currently configured leverage."""


class OpenCount(RpcEndpoint):
  """Estimate the maximum position size a hypothetical order could open, private. Used to pre-flight an order before placing it."""

  async def open_count(
    self,
    *,
    symbol: str,
    product_type: MixProductType,
    margin_coin: str,
    open_amount: Decimal,
    open_price: Decimal,
    leverage: str | None = None,
    validate: bool | None = None,
  ) -> MixOpenCount:
    """Estimate the maximum position size a hypothetical order could open, private. Used to pre-flight an order before placing it.

    Args:
      symbol: Futures symbol, e.g. "BTCUSDT".
      product_type:
      margin_coin: Margin coin, e.g. "USDT".
      open_amount: Margin amount available to open the position.
      open_price: Price the hypothetical order would open at.
      leverage: Leverage to evaluate. Defaults to the symbol's currently configured leverage.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-account/classic-contract-account#my-estimated-open-count)
    """
    request: Request = Request(
      symbol=symbol,
      productType=product_type,
      marginCoin=margin_coin,
      openAmount=open_amount,
      openPrice=open_price,
    )
    if leverage is not None:
      request['leverage'] = leverage
    return await self.request(
      request,
      method='GET',
      path='/api/v2/mix/account/open-count',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=MixOpenCount,
    )
