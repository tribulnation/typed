# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .account import Account
from .historic_trades import HistoricTrades
from .interest_exchange_rate import InterestExchangeRate
from .market import Market
from .oi_limit import OiLimit
from .open_interest import OpenInterest
from .order import Order
from .position import Position
from .recent_trades import RecentTrades
from .union_interest_rate_history import UnionInterestRateHistory


class Mix(
  HistoricTrades,
  InterestExchangeRate,
  OiLimit,
  OpenInterest,
  RecentTrades,
  UnionInterestRateHistory,
):
  """Perpetual futures and delivery contracts (USDT-M, Coin-M) -- Bitget's 'mix' product line: account/leverage settings, market data, order placement and management, and position queries.

  References:
    - [Upstream docs](https://www.bitget.com/docs/catalog/classic-contract-trade)
  """

  @cached_property
  def account(self) -> Account:
    """Bitget Classic's Futures Account API: account/asset queries, leverage and margin-mode configuration, and sub-account futures asset visibility.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-contract-account)
    """
    return Account(client=self.client)

  @cached_property
  def market(self) -> Market:
    """Bitget Classic's Futures Market API: contracts, tickers, order book, trades, candles, funding rate, open interest, and long/short-ratio market data.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-contract-market)
    """
    return Market(client=self.client)

  @cached_property
  def order(self) -> Order:
    """Bitget Classic's Futures Order API: placing, cancelling (single and batch), modifying, and querying open/historical orders and fills, plus flash-close and reversal, for futures trading.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-contract-trade)
    """
    return Order(client=self.client)

  @cached_property
  def position(self) -> Position:
    """Bitget Classic's Futures Position API: current and historical position queries, and ADL ranking, for futures trading.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-contract-position)
    """
    return Position(client=self.client)
