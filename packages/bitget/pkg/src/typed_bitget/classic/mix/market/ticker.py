# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixProductType, MixTicker


class Request(TypedDict):
  symbol: str
  """Futures symbol, e.g. `"BTCUSDT"`."""
  productType: MixProductType


Response = list[MixTicker]


class Ticker(RpcEndpoint):
  """Get one symbol's 24h ticker snapshot, public."""

  async def ticker(
    self,
    symbol: str,
    *,
    product_type: MixProductType,
    validate: bool | None = None,
  ) -> Response:
    """Get one symbol's 24h ticker snapshot, public.

    Args:
      symbol: Futures symbol, e.g. `"BTCUSDT"`.
      product_type:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-market/classic-contract-market#get-ticker)
    """
    return await self.request(
      Request(symbol=symbol, productType=product_type),
      method='GET',
      path='/api/v2/mix/market/ticker',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
