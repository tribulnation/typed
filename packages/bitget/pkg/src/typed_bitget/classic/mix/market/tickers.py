# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixProductType, MixTicker


class Request(TypedDict):
  productType: MixProductType


Response = list[MixTicker]


class Tickers(RpcEndpoint):
  """List 24h ticker snapshots for every symbol of a product type, public."""

  async def tickers(
    self,
    product_type: MixProductType,
    *,
    validate: bool | None = None,
  ) -> Response:
    """List 24h ticker snapshots for every symbol of a product type, public.

    Args:
      product_type:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-market/classic-contract-market#get-all-tickers)
    """
    return await self.request(
      Request(productType=product_type),
      method='GET',
      path='/api/v2/mix/market/tickers',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
