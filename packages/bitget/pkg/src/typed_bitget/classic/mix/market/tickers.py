# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.market.tickers` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import validator
from typed_bitget.types import MixTicker
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Tickers(RpcEndpoint):
  """`classic.mix.market.tickers`."""

  async def tickers(
    self,
    *,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    validate: bool | None = None,
  ) -> list[MixTicker]:
    """List 24h ticker snapshots for every symbol of a product type, public.

    Args:
      product_type: Which futures product family this request concerns.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/market/Get-All-Tickers)
    """
    params: dict = {
      'productType': product_type,
    }
    return await self.request(
      'GET',
      '/api/v2/mix/market/tickers',
      params=params,
      validator=validator(list[MixTicker]),
      validate=validate,
    )
