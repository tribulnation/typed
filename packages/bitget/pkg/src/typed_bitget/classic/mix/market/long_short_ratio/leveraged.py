# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixLeveragedLongShortRatio(TypedDict):
  """Leveraged long/short ratio for one time bucket, for one bucket."""

  ts: TimestampMillis
  """Bucket time."""
  longShortRatio: str
  """Leveraged long/short ratio for the bucket."""


class Request(TypedDict):
  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  coin: NotRequired[str]
  """Margin coin to compute the ratio for, e.g. "USDT"."""
  period: NotRequired[str]
  """Bucket interval for the series, e.g. "5m"/"1H"/"1D". Optional; defaults to the venue's own default bucket. See endpoint `notes` on why this is not an `enum`."""


Response = list[MixLeveragedLongShortRatio]


class Leveraged(RpcEndpoint):
  """Get the leveraged long/short ratio for one margin coin, public."""

  async def leveraged(
    self,
    *,
    symbol: str,
    coin: str | None = None,
    period: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get the leveraged long/short ratio for one margin coin, public.

    Args:
      symbol: Futures symbol, e.g. "BTCUSDT".
      coin: Margin coin to compute the ratio for, e.g. "USDT".
      period: Bucket interval for the series, e.g. "5m"/"1H"/"1D". Optional; defaults to the venue's own default bucket. See endpoint `notes` on why this is not an `enum`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
    """
    request: Request = Request(symbol=symbol)
    if coin is not None:
      request['coin'] = coin
    if period is not None:
      request['period'] = period
    return await self.request(
      request,
      method='GET',
      path='/api/v2/mix/market/long-short-ratio',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
