# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixProductType


class MixSymbolPrice(TypedDict):
  """Price snapshot for one symbol."""

  symbol: str
  """Futures symbol."""
  price: Decimal
  """Last traded price."""
  indexPrice: Decimal
  """Index price."""
  markPrice: Decimal
  """Mark price, used for margin and liquidation calculations."""
  ts: TimestampMillis
  """Snapshot time."""


class Request(TypedDict):
  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  productType: MixProductType


Response = list[MixSymbolPrice]


class SymbolPrice(RpcEndpoint):
  """Get one symbol's last/index/mark price, public."""

  async def symbol_price(
    self,
    symbol: str,
    *,
    product_type: MixProductType,
    validate: bool | None = None,
  ) -> Response:
    """Get one symbol's last/index/mark price, public.

    Args:
      symbol: Futures symbol, e.g. "BTCUSDT".
      product_type:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-market/classic-contract-market#get-mark-index-market-prices)
    """
    return await self.request(
      Request(symbol=symbol, productType=product_type),
      method='GET',
      path='/api/v2/mix/market/symbol-price',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
