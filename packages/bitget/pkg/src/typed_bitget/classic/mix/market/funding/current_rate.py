# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixProductType


class MixCurrentFundingRate(TypedDict):
  """Current funding rate for one symbol."""

  symbol: str
  """Futures symbol."""
  fundingRate: Decimal
  """Current funding rate, e.g. `"0.0001"`."""
  fundingRateInterval: int
  """Funding interval, in hours, e.g. `"8"`."""
  nextUpdate: TimestampMillis
  """Next funding settlement time."""
  minFundingRate: Decimal
  """Minimum bound the funding rate is clamped to."""
  maxFundingRate: Decimal
  """Maximum bound the funding rate is clamped to."""


class Request(TypedDict):
  symbol: NotRequired[str]
  """Restrict to one futures symbol, e.g. "BTCUSDT"."""
  productType: MixProductType


Response = list[MixCurrentFundingRate]


class CurrentRate(RpcEndpoint):
  """List current funding rates, public. Returns every symbol of the product type when `symbol` is omitted."""

  async def current_rate(
    self,
    symbol: str | None = None,
    *,
    product_type: MixProductType,
    validate: bool | None = None,
  ) -> Response:
    """List current funding rates, public. Returns every symbol of the product type when `symbol` is omitted.

    Args:
      symbol: Restrict to one futures symbol, e.g. "BTCUSDT".
      product_type:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-market/classic-contract-market#get-current-funding-rate)
    """
    request: Request = Request(productType=product_type)
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='/api/v2/mix/market/current-fund-rate',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
