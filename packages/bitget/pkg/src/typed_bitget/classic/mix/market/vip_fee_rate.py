# Generated by typed-dev — see CONTRIBUTING.md.
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixVipFeeRateTier


Response = list[MixVipFeeRateTier]


class VipFeeRate(RpcEndpoint):
  """List futures VIP fee-rate tiers, public. Tiers are keyed by 30-day trading volume and asset balance; each tier states its taker/maker fee rate and withdrawal allowances."""

  async def vip_fee_rate(self, *, validate: bool | None = None) -> Response:
    """List futures VIP fee-rate tiers, public. Tiers are keyed by 30-day trading volume and asset balance; each tier states its taker/maker fee rate and withdrawal allowances.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-market/classic-contract-market#vip-fee-rate)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v2/mix/market/vip-fee-rate',
      meta={},
      validate=validate,
      response_type=Response,
    )
