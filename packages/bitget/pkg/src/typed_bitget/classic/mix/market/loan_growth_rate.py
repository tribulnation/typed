# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.market.loan_growth_rate` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixLoanGrowthRate(TypedDict):
  """Loan growth rate for one time bucket, for one bucket."""

  ts: TimestampMillis
  """Bucket time."""
  growthRate: str
  """Loan growth rate for the bucket."""


class LoanGrowthRate(RpcEndpoint):
  """`classic.mix.market.loan_growth_rate`."""

  async def loan_growth_rate(
    self,
    *,
    symbol: str,
    coin: str | None = None,
    period: str | None = None,
    validate: bool | None = None,
  ) -> list[MixLoanGrowthRate]:
    """Get the margin loan growth rate for one symbol, public.

    Args:
      symbol: Futures symbol, e.g. "BTCUSDT".
      coin: Restrict to one margin coin, e.g. "USDT".
      period: Bucket interval for the series, e.g. "5m"/"1H"/"1D". Optional; defaults to the venue's own default bucket. See endpoint `notes` on why this is not an `enum`.
    """
    params: dict = {
      'symbol': symbol,
    }
    if coin is not None:
      params['coin'] = coin
    if period is not None:
      params['period'] = period
    return await self.request(
      'GET',
      '/api/v2/mix/market/loan-growth',
      params=params,
      validator=validator(list[MixLoanGrowthRate]),
      validate=validate,
    )
