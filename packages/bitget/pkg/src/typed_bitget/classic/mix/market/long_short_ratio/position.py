# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixPositionLongShortRatio(TypedDict):
  """Position long/short account ratio for one time bucket, for one bucket."""

  longPositionRatio: Decimal
  """Share of position-holding accounts that are long."""
  shortPositionRatio: Decimal
  """Share of position-holding accounts that are short."""
  longShortPositionRatio: Decimal
  """Ratio of long to short position-holding accounts."""
  ts: TimestampMillis
  """Bucket time."""


class Request(TypedDict):
  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  period: NotRequired[str]
  """Bucket interval for the series, e.g. "5m"/"1H"/"1D". Optional; defaults to the venue's own default bucket. See endpoint `notes` on why this is not an `enum`."""


Response = list[MixPositionLongShortRatio]


class Position(RpcEndpoint):
  """Get the ratio of accounts holding long vs. short positions for one symbol, public."""

  async def position(
    self,
    *,
    symbol: str,
    period: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get the ratio of accounts holding long vs. short positions for one symbol, public.

    Args:
      symbol: Futures symbol, e.g. "BTCUSDT".
      period: Bucket interval for the series, e.g. "5m"/"1H"/"1D". Optional; defaults to the venue's own default bucket. See endpoint `notes` on why this is not an `enum`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
    """
    request: Request = Request(symbol=symbol)
    if period is not None:
      request['period'] = period
    return await self.request(
      request,
      method='GET',
      path='/api/v2/mix/market/position-long-short',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
