# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixProductType


class MixPositionTier(TypedDict):
  """One position-size tier."""

  symbol: str
  """Futures symbol."""
  level: int
  """Tier number."""
  startUnit: Decimal
  """Lower bound of this tier's position size, in base coin."""
  endUnit: Decimal
  """Upper bound of this tier's position size, in base coin."""
  leverage: Decimal
  """Maximum leverage allowed within this tier."""
  keepMarginRate: Decimal
  """Maintenance margin rate applied within this tier."""


class Request(TypedDict):
  productType: MixProductType
  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""


Response = list[MixPositionTier]


class PositionTier(RpcEndpoint):
  """List a symbol's position-tier ladder, public. Each tier states the maximum leverage and maintenance-margin rate for a range of position sizes."""

  async def position_tier(
    self,
    product_type: MixProductType,
    *,
    symbol: str,
    validate: bool | None = None,
  ) -> Response:
    """List a symbol's position-tier ladder, public. Each tier states the maximum leverage and maintenance-margin rate for a range of position sizes.

    Args:
      product_type:
      symbol: Futures symbol, e.g. "BTCUSDT".
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-position/classic-contract-position#get-position-tier)
    """
    return await self.request(
      Request(productType=product_type, symbol=symbol),
      method='GET',
      path='/api/v2/mix/market/query-position-lever',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
