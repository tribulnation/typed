# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.mix.market.funding.time` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MixFundingTime(TypedDict):
  """Next funding time for one symbol."""

  symbol: str
  """Futures symbol."""
  nextFundingTime: TimestampMillis
  """Next funding settlement time."""
  ratePeriod: str
  """Funding interval, in hours."""


class Time(RpcEndpoint):
  """`classic.mix.market.funding.time`."""

  async def time(
    self,
    *,
    symbol: str,
    product_type: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    validate: bool | None = None,
  ) -> list[MixFundingTime]:
    """Get one symbol's next funding settlement time and interval, public.

    Args:
      symbol: Futures symbol, e.g. "BTCUSDT".
      product_type: Which futures product family this request concerns.

    References:
      - [Official docs](https://www.bitget.com/api-doc/contract/market/Get-Symbol-Next-Funding-Time)
    """
    params: dict = {
      'symbol': symbol,
      'productType': product_type,
    }
    return await self.request(
      'GET',
      '/api/v2/mix/market/funding-time',
      params=params,
      validator=validator(list[MixFundingTime]),
      validate=validate,
    )
