# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import MixProductType


class MixFundingTime(TypedDict):
  """Next funding time for one symbol."""

  symbol: str
  """Futures symbol."""
  nextFundingTime: TimestampMillis
  """Next funding settlement time."""
  ratePeriod: int
  """Funding interval, in hours."""


class Request(TypedDict):
  symbol: str
  """Futures symbol, e.g. "BTCUSDT"."""
  productType: MixProductType


Response = list[MixFundingTime]


class Time(RpcEndpoint):
  """Get one symbol's next funding settlement time and interval, public."""

  async def time(
    self,
    symbol: str,
    *,
    product_type: MixProductType,
    validate: bool | None = None,
  ) -> Response:
    """Get one symbol's next funding settlement time and interval, public.

    Args:
      symbol: Futures symbol, e.g. "BTCUSDT".
      product_type:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-contract-market/classic-contract-market#get-next-funding-time)
    """
    return await self.request(
      Request(symbol=symbol, productType=product_type),
      method='GET',
      path='/api/v2/mix/market/funding-time',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
