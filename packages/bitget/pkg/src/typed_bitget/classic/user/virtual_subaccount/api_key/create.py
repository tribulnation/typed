# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.user.virtual_subaccount.api_key.create` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CreateVirtualSubaccountApiKeyRequest(TypedDict):
  """API key to create for a virtual sub-account."""

  subAccountUid: str
  """Uid of the virtual sub-account to create the key for."""
  passphrase: str
  """Passphrase to set on the new API key."""
  label: str
  """Label for the new API key."""
  ipList: NotRequired[list[str]]
  """IP allowlist for the new API key. Omit for no IP restriction."""
  permList: NotRequired[list[str]]
  """Permissions to grant the new API key."""


class VirtualSubaccountApiKey(TypedDict):
  """An API key created for a virtual sub-account."""

  subAccountUid: str
  """Uid of the virtual sub-account the key belongs to."""
  label: str
  """Label of the new API key."""
  subAccountApiKey: str
  """The new API key."""
  secretKey: str
  """The new API secret key. Shown once, at creation."""
  permList: list[str]
  """Permissions granted to the API key."""
  ipList: list[str]
  """IP allowlist bound to the API key."""


class Create(RpcEndpoint):
  """`classic.user.virtual_subaccount.api_key.create`."""

  async def create(
    self,
    create_virtual_subaccount_api_key_request: CreateVirtualSubaccountApiKeyRequest,
    *,
    validate: bool | None = None,
  ) -> VirtualSubaccountApiKey:
    """Create an API key for an existing virtual sub-account.

    Args:
      create_virtual_subaccount_api_key_request: API key to create for a virtual sub-account.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/common/vsubaccount/Create-Virtual-Subaccount-Apikey)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/user/create-virtual-subaccount-apikey',
      json=create_virtual_subaccount_api_key_request,
      validator=validator(VirtualSubaccountApiKey),
      validate=validate,
    )
