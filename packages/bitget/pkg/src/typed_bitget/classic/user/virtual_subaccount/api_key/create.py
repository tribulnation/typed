# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """API key to create for a virtual sub-account."""

  subAccountUid: str
  """Uid of the virtual sub-account to create the key for."""
  passphrase: str
  """Passphrase to set on the new API key."""
  label: str
  """Label for the new API key."""
  ipList: NotRequired[list[str]]
  """IP allowlist for the new API key. Omit for no IP restriction."""
  permList: NotRequired[list[str]]
  """Permissions to grant the new API key."""


class VirtualSubaccountApiKey(TypedDict):
  """An API key created for a virtual sub-account."""

  subAccountUid: str
  """Uid of the virtual sub-account the key belongs to."""
  label: str
  """Label of the new API key."""
  subAccountApiKey: str
  """The new API key."""
  secretKey: str
  """The new API secret key. Shown once, at creation."""
  permList: list[str]
  """Permissions granted to the API key."""
  ipList: list[str]
  """IP allowlist bound to the API key."""


class Create(RpcEndpoint):
  """Create an API key for an existing virtual sub-account."""

  async def create(
    self,
    *,
    sub_account_uid: str,
    passphrase: str,
    label: str,
    ip_list: list[str] | None = None,
    perm_list: list[str] | None = None,
    validate: bool | None = None,
  ) -> VirtualSubaccountApiKey:
    """Create an API key for an existing virtual sub-account.

    Args:
      sub_account_uid: Uid of the virtual sub-account to create the key for.
      passphrase: Passphrase to set on the new API key.
      label: Label for the new API key.
      ip_list: IP allowlist for the new API key. Omit for no IP restriction.
      perm_list: Permissions to grant the new API key.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-common-vsubaccount/classic-common-vsubaccount#create-virtual-subaccount-apikey)
    """
    request: Request = Request(
      subAccountUid=sub_account_uid, passphrase=passphrase, label=label
    )
    if ip_list is not None:
      request['ipList'] = ip_list
    if perm_list is not None:
      request['permList'] = perm_list
    return await self.request(
      request,
      method='POST',
      path='/api/v2/user/create-virtual-subaccount-apikey',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=VirtualSubaccountApiKey,
    )
