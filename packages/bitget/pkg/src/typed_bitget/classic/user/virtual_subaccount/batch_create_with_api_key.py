# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CreatedVirtualSubaccountApiKey(TypedDict):
  """A virtual sub-account created together with its API key."""

  subAccountUid: str
  """Uid assigned to the new sub-account."""
  subAccountName: str
  """The sub-account name."""
  label: str
  """Label of the new API key."""
  subAccountApiKey: str
  """The new API key."""
  secretKey: str
  """The new API secret key. Shown once, at creation."""
  permList: list[str]
  """Permissions granted to the API key."""
  ipList: list[str]
  """IP allowlist bound to the API key."""


class Request(TypedDict):
  """Virtual sub-account plus API key to create together."""

  subAccountName: str
  """Name for the new sub-account."""
  passphrase: str
  """Passphrase to set on the new API key."""
  label: str
  """Label for the new API key."""
  ipList: NotRequired[list[str]]
  """IP allowlist for the new API key. Omit for no IP restriction."""
  permList: NotRequired[list[str]]
  """Permissions to grant the new API key."""


Response = list[CreatedVirtualSubaccountApiKey]


class BatchCreateWithApiKey(RpcEndpoint):
  """Create a virtual sub-account together with an API key for it, in one call."""

  async def batch_create_with_api_key(
    self,
    *,
    sub_account_name: str,
    passphrase: str,
    label: str,
    ip_list: list[str] | None = None,
    perm_list: list[str] | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Create a virtual sub-account together with an API key for it, in one call.

    Args:
      sub_account_name: Name for the new sub-account.
      passphrase: Passphrase to set on the new API key.
      label: Label for the new API key.
      ip_list: IP allowlist for the new API key. Omit for no IP restriction.
      perm_list: Permissions to grant the new API key.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-common-vsubaccount/classic-common-vsubaccount#batch-create-virtual-subaccount-and-apikey)
    """
    request: Request = Request(
      subAccountName=sub_account_name, passphrase=passphrase, label=label
    )
    if ip_list is not None:
      request['ipList'] = ip_list
    if perm_list is not None:
      request['permList'] = perm_list
    return await self.request(
      request,
      method='POST',
      path='/api/v2/user/batch-create-subaccount-and-apikey',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
