# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CreatedSubaccount(TypedDict):
  """One sub-account created successfully."""

  subaAccountUid: str
  """Uid assigned to the new sub-account. (Sic: API field name.)"""
  subaAccountName: str
  """The sub-account name. (Sic: API field name.)"""
  status: Literal['normal', 'freeze']
  """Sub-account status."""
  label: str | None
  """Sub-account label."""
  permList: list[str]
  """Permissions granted to the sub-account."""
  cTime: TimestampMillis
  """Creation time, Unix millisecond timestamp."""
  uTime: TimestampMillis
  """Last update time, Unix millisecond timestamp."""


class FailedSubaccount(TypedDict):
  """One sub-account name that failed to create."""

  subaAccountName: str
  """The sub-account name that failed to create. (Sic: API field name.)"""


class Request(TypedDict):
  """Virtual sub-account names to create."""

  subAccountList: list[str]
  """Sub-account names to create."""


class CreateVirtualSubaccountResult(TypedDict):
  """Result of a virtual sub-account creation batch."""

  failureList: list[FailedSubaccount]
  """Sub-account names that failed to create."""
  successList: list[CreatedSubaccount]
  """Sub-accounts created successfully."""


class Create(RpcEndpoint):
  """Create one or more virtual sub-accounts under the main account."""

  async def create(
    self,
    sub_account_list: list[str],
    *,
    validate: bool | None = None,
  ) -> CreateVirtualSubaccountResult:
    """Create one or more virtual sub-accounts under the main account.

    Args:
      sub_account_list: Sub-account names to create.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-common-vsubaccount/classic-common-vsubaccount#create-virtual-subaccount)
    """
    return await self.request(
      Request(subAccountList=sub_account_list),
      method='POST',
      path='/api/v2/user/create-virtual-subaccount',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=CreateVirtualSubaccountResult,
    )
