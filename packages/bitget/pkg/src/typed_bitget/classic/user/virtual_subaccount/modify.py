# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class ModifyVirtualSubaccountResult(TypedDict):
  """Result of a virtual sub-account modification."""

  result: str
  """Confirmation string, e.g. `success`."""


class Request(TypedDict):
  """Virtual sub-account permission/status update."""

  subAccountUid: str
  """Uid of the virtual sub-account to modify."""
  permList: list[str]
  """Permissions to grant the sub-account."""
  status: Literal['normal', 'freeze']
  """New sub-account status."""


class Modify(RpcEndpoint):
  """Modify a virtual sub-account's permissions and status."""

  async def modify(
    self,
    sub_account_uid: str,
    *,
    perm_list: list[str],
    status: Literal['normal', 'freeze'],
    validate: bool | None = None,
  ) -> ModifyVirtualSubaccountResult:
    """Modify a virtual sub-account's permissions and status.

    Args:
      sub_account_uid: Uid of the virtual sub-account to modify.
      perm_list: Permissions to grant the sub-account.
      status: New sub-account status.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-common-vsubaccount/classic-common-vsubaccount#modify-virtual-subaccount)
    """
    return await self.request(
      Request(subAccountUid=sub_account_uid, permList=perm_list, status=status),
      method='POST',
      path='/api/v2/user/modify-virtual-subaccount',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=ModifyVirtualSubaccountResult,
    )
