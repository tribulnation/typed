# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  subAccountUid: str
  """Uid of the virtual sub-account to list API keys for."""


class VirtualSubaccountApiKeyListItem(TypedDict):
  """One virtual sub-account API key."""

  subAccountUid: str
  """Uid of the virtual sub-account the key belongs to."""
  label: str
  """Label of the API key."""
  subAccountApiKey: str
  """The API key."""
  permList: list[str]
  """Permissions granted to the API key."""
  ipList: list[str]
  """IP allowlist bound to the API key."""


Response = list[VirtualSubaccountApiKeyListItem]


class List(RpcEndpoint):
  """List a virtual sub-account's API keys."""

  async def list(
    self, sub_account_uid: str, *, validate: bool | None = None
  ) -> Response:
    """List a virtual sub-account's API keys.

    Args:
      sub_account_uid: Uid of the virtual sub-account to list API keys for.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-common-vsubaccount/classic-common-vsubaccount#get-subaccount-apikey-list)
    """
    return await self.request(
      Request(subAccountUid=sub_account_uid),
      method='GET',
      path='/api/v2/user/virtual-subaccount-apikey-list',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
