# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class ModifiedVirtualSubaccountApiKey(TypedDict):
  """A modified virtual sub-account API key."""

  subAccountUid: str
  """Uid of the virtual sub-account the key belongs to."""
  label: str
  """Label of the API key."""
  subAccountApiKey: str
  """The API key."""
  secretKey: str
  """The API secret key."""
  permList: list[str]
  """Permissions granted to the API key."""
  ipList: list[str]
  """IP allowlist bound to the API key."""


class Request(TypedDict):
  """Virtual sub-account API key update."""

  subAccountUid: str
  """Uid of the virtual sub-account the key belongs to."""
  subAccountApiKey: str
  """The API key to modify."""
  passphrase: str
  """The API key's passphrase, required to authorize the modification."""
  label: str
  """New label for the API key."""
  ipList: NotRequired[list[str]]
  """New IP allowlist for the API key. Omit for no IP restriction."""
  permList: NotRequired[list[str]]
  """New permissions to grant the API key."""


class Modify(RpcEndpoint):
  """Modify an existing virtual sub-account API key's label, IP allowlist, or permissions."""

  async def modify(
    self,
    *,
    sub_account_uid: str,
    sub_account_api_key: str,
    passphrase: str,
    label: str,
    ip_list: list[str] | None = None,
    perm_list: list[str] | None = None,
    validate: bool | None = None,
  ) -> ModifiedVirtualSubaccountApiKey:
    """Modify an existing virtual sub-account API key's label, IP allowlist, or permissions.

    Args:
      sub_account_uid: Uid of the virtual sub-account the key belongs to.
      sub_account_api_key: The API key to modify.
      passphrase: The API key's passphrase, required to authorize the modification.
      label: New label for the API key.
      ip_list: New IP allowlist for the API key. Omit for no IP restriction.
      perm_list: New permissions to grant the API key.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-common-vsubaccount/classic-common-vsubaccount#modify-virtual-subaccount-apikey)
    """
    request: Request = Request(
      subAccountUid=sub_account_uid,
      subAccountApiKey=sub_account_api_key,
      passphrase=passphrase,
      label=label,
    )
    if ip_list is not None:
      request['ipList'] = ip_list
    if perm_list is not None:
      request['permList'] = perm_list
    return await self.request(
      request,
      method='POST',
      path='/api/v2/user/modify-virtual-subaccount-apikey',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=ModifiedVirtualSubaccountApiKey,
    )
