# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class AgentSubaccount(TypedDict):
  """An agent sub-account created together with its API key."""

  username: str
  """Sub-account username."""
  subUid: str
  """Uid assigned to the new sub-account."""
  apiKey: str
  """The new API key."""
  secret: str
  """The new API secret key. Shown once, at creation."""
  note: str
  """Free-text note for the sub-account."""
  createdTime: TimestampMillis
  """Creation time, Unix millisecond timestamp."""


class Request(TypedDict):
  """Agent sub-account to create."""

  username: str
  """Sub-account username. Lowercase letters only, max 20 characters."""
  passphrase: str
  """Passphrase for the sub-account's API key. 8-32 characters, letters and numbers."""
  note: NotRequired[str]
  """Optional free-text note for the sub-account."""


class CreateAgentSubaccount(RpcEndpoint):
  """Create a dedicated sub-account for an AI agent instance. Agent sub-accounts cannot be edited after creation."""

  async def create_agent_subaccount(
    self,
    *,
    username: str,
    passphrase: str,
    note: str | None = None,
    validate: bool | None = None,
  ) -> AgentSubaccount:
    """Create a dedicated sub-account for an AI agent instance. Agent sub-accounts cannot be edited after creation.

    Args:
      username: Sub-account username. Lowercase letters only, max 20 characters.
      passphrase: Passphrase for the sub-account's API key. 8-32 characters, letters and numbers.
      note: Optional free-text note for the sub-account.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-common-vsubaccount/classic-common-vsubaccount#create-agent-subaccount)
    """
    request: Request = Request(username=username, passphrase=passphrase)
    if note is not None:
      request['note'] = note
    return await self.request(
      request,
      method='POST',
      path='/api/v2/user/create-agent-subaccount',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=AgentSubaccount,
    )
