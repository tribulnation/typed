# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic_streams.plan_orders` -- generated from `spec/endpoints/`."""

from typing_extensions import Any, Literal, NotRequired
from typed_core.util import StreamManager
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.types import ClassicPlanOrdersArg
from typed_bitget.core.endpoint.stream import StreamEndpoint


class MixPlanOrder(TypedDict):
  """One mix (futures) plan/trigger order lifecycle update -- covers preset, partial, position TP/SL and trailing-stop sub-types via `planType`."""

  instId: str
  """Product ID, e.g. `BTCUSDT`."""
  orderId: str
  """Plan (trigger) order ID."""
  clientOid: str
  """Customized plan order ID."""
  triggerPrice: str
  """Trigger price."""
  triggerType: Literal['fill_price', 'mark_price']
  """Trigger reference price."""
  triggerTime: TimestampMillis
  """Trigger time, Unix epoch milliseconds as a string."""
  planType: Literal['pl', 'tp', 'sl', 'ptp', 'psl', 'track', 'mtpsl']
  """Trigger-order sub-type. `pl`: default trigger order. `tp`/`sl`: partial take-profit/stop-loss. `ptp`/`psl`: position take-profit/stop-loss. `track`: trailing stop. `mtpsl`: trailing TP/SL."""
  price: str
  """Order price."""
  executePrice: str
  """Execution price."""
  size: str
  """Original order amount, in the base coin."""
  actualSize: str
  """Actual order amount placed, in the base coin."""
  orderType: Literal['limit', 'market']
  """Order type to place once triggered."""
  side: Literal['buy', 'sell']
  """Order direction."""
  tradeSide: str
  """Trading direction."""
  posSide: str
  """Position direction."""
  marginCoin: str
  """Margin coin."""
  status: Literal['live', 'executed', 'fail_execute', 'cancelled', 'executing']
  """Plan order status."""
  posMode: Literal['one_way_mode', 'hedge_mode']
  """Position mode."""
  enterPointSource: str
  """Order source, e.g. `web`, `API`, `SYS`, `ANDROID`, `IOS`."""
  stopSurplusPrice: NotRequired[str]
  """Take-profit execution price. Meaning depends on `planType`: preset (pl), partial (tp), or position (ptp) take-profit."""
  stopSurplusTriggerPrice: NotRequired[str]
  """Take-profit trigger price; empty when not set."""
  stopSurplusTriggerType: NotRequired[str]
  """Take-profit trigger reference price type; empty when not set."""
  stopLossPrice: NotRequired[str]
  """Stop-loss execution price. Meaning depends on `planType`: preset (pl), partial (sl), or position (psl) stop-loss."""
  stopLossTriggerPrice: NotRequired[str]
  """Stop-loss trigger price."""
  stopLossTriggerType: NotRequired[str]
  """Stop-loss trigger reference price type."""
  uTime: TimestampMillis
  """Update time, Unix epoch milliseconds as a string."""
  cTime: NotRequired[TimestampMillis]
  """Creation time, Unix epoch milliseconds as a string."""
  stpMode: Literal['none', 'cancel_taker', 'cancel_maker', 'cancel_both']
  """Self-trade-prevention mode."""


class PlanOrdersParams(TypedDict):
  instType: Literal['SPOT', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product line type: `SPOT`, or a futures product type (`USDT-FUTURES`, `COIN-FUTURES`, `USDC-FUTURES`)."""
  instId: NotRequired[str]
  """Product ID, or `default` for all symbols."""


class SpotPlanOrder(TypedDict):
  """One spot plan (trigger) order lifecycle update."""

  instId: str
  """Product ID, e.g. `BTCUSDT`."""
  orderId: str
  """Plan (trigger) order ID."""
  clientOid: str
  """Customized plan order ID."""
  triggerPrice: str
  """Trigger price."""
  triggerType: Literal['fill_price', 'mark_price']
  """Trigger reference price."""
  planType: Literal['amount', 'total']
  """Plan-order sizing type."""
  price: str
  """Order price to place once triggered."""
  size: str
  """Original order amount, in the base coin."""
  actualSize: str
  """Actual order amount placed, in the base coin."""
  orderType: Literal['limit', 'market']
  """Order type to place once triggered."""
  side: Literal['buy', 'sell']
  """Order direction."""
  status: str
  """Plan order status."""
  executePrice: str
  """Execution price."""
  enterPointSource: str
  """Order source, e.g. `web`, `API`, `SYS`, `ANDROID`, `IOS`."""
  cTime: TimestampMillis
  """Creation time, Unix epoch milliseconds as a string."""
  uTime: TimestampMillis
  """Update time, Unix epoch milliseconds as a string."""
  stpMode: Literal['none', 'cancel_taker', 'cancel_maker', 'cancel_both']
  """Self-trade-prevention mode."""


class PlanOrdersPush(TypedDict):
  """Push frame for the `PlanOrdersPush` channel. The core performs no envelope unwrap for streams (see spec/core.md WebSocket): callers receive this whole frame, not one unwrapped `data` element."""

  action: Literal['snapshot', 'update']
  """Push action."""
  arg: ClassicPlanOrdersArg
  data: list[SpotPlanOrder | MixPlanOrder]
  """Subscription data."""
  ts: TimestampMillis
  """Message generation time, Unix epoch milliseconds."""


class PlanOrdersReply(TypedDict):
  """Acknowledgement for a `subscribe` request. Per the upstream WebSocket intro page, every field but `event` is documented "No" (optional); `code`/`msg` are populated only when `event` is `error`."""

  event: Literal['subscribe', 'error']
  """Event name: `subscribe` on success, `error` on failure."""
  code: NotRequired[str]
  """Error code, present only on a failed request."""
  msg: NotRequired[str]
  """Error message, present only on a failed request."""
  arg: NotRequired[ClassicPlanOrdersArg]


class PlanOrders(StreamEndpoint):
  """`classic_streams.plan_orders`."""

  def plan_orders(
    self,
    inst_type: Literal['SPOT', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    *,
    inst_id: str | None = None,
  ) -> 'StreamManager[PlanOrdersPush, Any, Any]':
    """Get plan (trigger) order lifecycle updates, private. Pushed when a trigger plan is opened, cancelled, modified, or triggered.

    Args:
      inst_type: Product line type: `SPOT`, or a futures product type (`USDT-FUTURES`, `COIN-FUTURES`, `USDC-FUTURES`).
      inst_id: Product ID, or `default` for all symbols.

    References:
      - [Official docs](https://www.bitget.com/api-doc/spot/websocket/private/Plan-Order-Channel)
    """
    params = {
      'instType': inst_type,
    }
    if inst_id is not None:
      params['instId'] = inst_id
    return self.authed_subscribe(
      'orders-algo', params, validator=validator(PlanOrdersPush)
    )
