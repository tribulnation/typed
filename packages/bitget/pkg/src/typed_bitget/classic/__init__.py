# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .broker import Broker
from .common import Common
from .convert import Convert
from .copytrading import Copytrading
from .earn import Earn
from .institutional_loan import InstitutionalLoan
from .margin import Margin
from .mix import Mix
from .p2p import P2p
from .spot import Spot
from .tax import Tax
from .user import User
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Classic(RpcEndpoint):
  """Bitget Classic -- the account and API system Bitget ran before introducing the Unified Trading Account: spot, margin, futures ('mix'), copy trading, earn, P2P, convert, broker, institutional loan, tax, and account-plumbing endpoints, together.

  References:
    - [Upstream docs](https://www.bitget.com/docs/classic/catalog)
  """

  @cached_property
  def broker(self) -> Broker:
    """Bitget Classic's Broker API: sub-account creation, API key management, deposit/withdrawal on behalf of sub-accounts, and commission/rebate reporting for a broker's own customer base and referred agents.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-broker-subaccount)
    """
    return Broker(client=self.client)

  @cached_property
  def common(self) -> Common:
    """Bitget Classic's shared common endpoints: server time, announcements, platform-wide trade rate/fee data, and aggregate account/funding/bot asset totals -- none of it scoped to spot, mix, or margin specifically. Most require authentication; only time and announcements are public.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-common-public)
    """
    return Common(client=self.client)

  @cached_property
  def convert(self) -> Convert:
    """Bitget Classic's Convert API: quote and execute small-balance/coin-to-coin conversions, including the dedicated BGB conversion flow, and query conversion history.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-spot-convert)
    """
    return Convert(client=self.client)

  @cached_property
  def copytrading(self) -> Copytrading:
    """Bitget Classic's Copy Trading API: trader- and follower-side endpoints for both spot and futures copy trading, covering trade/follower management, orders, and profit-sharing settlement.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-copytrading-future-trader)
    """
    return Copytrading(client=self.client)

  @cached_property
  def earn(self) -> Earn:
    """Bitget Classic's Earn API: savings, sharkfin structured products, and BGB Elite lending/staking products, plus the shared Earn account balance endpoints.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/earn-classic-elite)
    """
    return Earn(client=self.client)

  @cached_property
  def institutional_loan(self) -> InstitutionalLoan:
    """Bitget Classic's Institutional Loan API: risk-unit and LTV management, loan order placement/history, and eligible-symbol/margin-coin lookups for Bitget's institutional lending product.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-instloan-orders)
    """
    return InstitutionalLoan(client=self.client)

  @cached_property
  def margin(self) -> Margin:
    """Bitget Classic's Margin API: cross- and isolated-margin account assets, borrow/repay, order placement, and interest-rate/currency lookups for margin trading.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-margin-cross-trade)
    """
    return Margin(client=self.client)

  @cached_property
  def mix(self) -> Mix:
    """Perpetual futures and delivery contracts (USDT-M, Coin-M) -- Bitget's 'mix' product line: account/leverage settings, market data, order placement and management, and position queries.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-contract-trade)
    """
    return Mix(client=self.client)

  @cached_property
  def p2p(self) -> P2p:
    """Bitget Classic's P2P API: merchant-side advertisement and order management for peer-to-peer fiat trading.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-p2p-p2p)
    """
    return P2p(client=self.client)

  @cached_property
  def spot(self) -> Spot:
    """Bitget Classic's Spot API: account balances, market data, order placement and management, deposits/withdrawals, transfers, and sub-account operations for spot trading.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-spot-trade)
    """
    return Spot(client=self.client)

  @cached_property
  def tax(self) -> Tax:
    """Bitget Classic's Tax Records API: exportable transaction history for spot, margin, futures, and P2P activity, used for tax reporting.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-tax-tax-records)
    """
    return Tax(client=self.client)

  @cached_property
  def user(self) -> User:
    """Bitget Classic's account-holder-level user API: virtual sub-account creation and management, and agent sub-account creation -- account plumbing that sits above spot/mix/margin rather than inside any one of them.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/classic-common-vsubaccount)
    """
    return User(client=self.client)
