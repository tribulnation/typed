# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.institutional_loan.symbols` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class LoanSymbols(TypedDict):
  """Spot symbols tradable under one institutional loan product."""

  productId: str
  """The loan product id."""
  spotSymbols: list[str]
  """Spot trading pairs tradable under this loan product."""


class Symbols(RpcEndpoint):
  """`classic.institutional_loan.symbols`."""

  async def symbols(
    self, *, product_id: str, validate: bool | None = None
  ) -> LoanSymbols:
    """Get the spot trading pairs tradable under a given institutional loan product.

    Args:
      product_id: The loan product id.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/instloan/public/Get-Symbols)
    """
    params: dict = {
      'productId': product_id,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/spot/ins-loan/symbols',
      params=params,
      validator=validator(LoanSymbols),
      validate=validate,
    )
