# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class LoanRepaidRecord(TypedDict):
  """One institutional loan repayment record."""

  repayOrderId: str
  """Repayment order id."""
  businessType: str
  """Business type this repayment applies to."""
  repayType: str
  """Repayment type, e.g. manual or automatic."""
  repaidTime: TimestampMillis
  """Repayment time, Unix millisecond timestamp."""
  coin: str
  """Repaid coin."""
  repaidAmount: str
  """Repaid principal amount, in `coin`."""
  repaidInterest: str
  """Repaid interest amount, in `coin`."""


class Request(TypedDict):
  startTime: NotRequired[TimestampMillis]
  """Range start, Unix millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """Range end, Unix millisecond timestamp."""
  limit: NotRequired[int]
  """Max entries to return."""


Response = list[LoanRepaidRecord]


class RepaidHistory(RpcEndpoint):
  """Get the caller's institutional loan repayment history."""

  async def repaid_history(
    self,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get the caller's institutional loan repayment history.

    Args:
      start_time: Range start, Unix millisecond timestamp.
      end_time: Range end, Unix millisecond timestamp.
      limit: Max entries to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-instloan-orders/classic-instloan-orders#get-repayment-orders)
    """
    request: Request = Request()
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/api/v2/spot/ins-loan/repaid-history',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
