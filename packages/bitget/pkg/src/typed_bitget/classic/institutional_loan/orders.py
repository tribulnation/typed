# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class LoanOrder(TypedDict):
  """One institutional loan order."""

  orderId: str
  """Loan order id."""
  orderProductId: str
  """Loan product id this order was taken under."""
  uid: str
  """Account uid this order belongs to."""
  loanTime: TimestampMillis
  """Loan drawdown time, Unix millisecond timestamp."""
  loanCoin: str
  """Borrowed coin."""
  loanAmount: str
  """Borrowed principal amount, in `loanCoin`."""
  unpaidAmount: str
  """Outstanding principal amount, in `loanCoin`."""
  unpaidInterest: str
  """Outstanding interest amount, in `loanCoin`."""
  repaidAmount: str
  """Repaid principal amount so far, in `loanCoin`."""
  repaidInterest: str
  """Repaid interest amount so far, in `loanCoin`."""
  status: str
  """Loan order status. Closed set not documented; see notes."""


class Request(TypedDict):
  orderId: NotRequired[str]
  """Restrict to this loan order id."""
  startTime: NotRequired[TimestampMillis]
  """Range start, Unix millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """Range end, Unix millisecond timestamp."""


Response = list[LoanOrder]


class Orders(RpcEndpoint):
  """Get the caller's institutional loan orders."""

  async def orders(
    self,
    order_id: str | None = None,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get the caller's institutional loan orders.

    Args:
      order_id: Restrict to this loan order id.
      start_time: Range start, Unix millisecond timestamp.
      end_time: Range end, Unix millisecond timestamp.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-instloan-orders/classic-instloan-orders#get-loan-orders)
    """
    request: Request = Request()
    if order_id is not None:
      request['orderId'] = order_id
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    return await self.request(
      request,
      method='GET',
      path='/api/v2/spot/ins-loan/loan-order',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
