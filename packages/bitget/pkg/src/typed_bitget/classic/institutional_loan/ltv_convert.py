# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class LoanBalanceInfo(TypedDict):
  """One coin's collateral balance."""

  coin: str
  """The collateral coin."""
  price: str
  """Current price of `coin`, in USDT."""
  amount: str
  """Collateral amount, in `coin`."""
  convertedUsdtAmount: str
  """Collateral amount, converted to USDT."""


class LoanUnpaidInfo(TypedDict):
  """One coin's unpaid loan balance."""

  coin: str
  """The loan coin."""
  unpaidQty: str
  """Unpaid principal amount, in `coin`."""
  unpaidInterest: str
  """Unpaid interest amount, in `coin`."""


class Request(TypedDict):
  riskUnitId: NotRequired[str]
  """Restrict to this risk unit id. Omit to use the caller's default/only risk unit."""


class LoanLtvConvert(TypedDict):
  """A risk unit's current loan-to-value snapshot."""

  ltv: str
  """Current loan-to-value ratio, as a fraction."""
  subAccountUids: list[str]
  """Sub-account uids included in this risk unit."""
  unpaidUsdtAmount: str
  """Total unpaid loan amount, converted to USDT."""
  usdtBalance: str
  """Total collateral balance, converted to USDT."""
  unpaidInfo: list[LoanUnpaidInfo]
  """Per-coin unpaid loan balances."""
  balanceInfo: list[LoanBalanceInfo]
  """Per-coin collateral balances."""


class LtvConvert(RpcEndpoint):
  """Get a risk unit's current loan-to-value ratio, unpaid balances and collateral balances, converted to USDT."""

  async def ltv_convert(
    self,
    risk_unit_id: str | None = None,
    *,
    validate: bool | None = None,
  ) -> LoanLtvConvert:
    """Get a risk unit's current loan-to-value ratio, unpaid balances and collateral balances, converted to USDT.

    Args:
      risk_unit_id: Restrict to this risk unit id. Omit to use the caller's default/only risk unit.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-instloan-account/classic-instloan-account#get-ltv)
    """
    request: Request = Request()
    if risk_unit_id is not None:
      request['riskUnitId'] = risk_unit_id
    return await self.request(
      request,
      method='GET',
      path='/api/v2/spot/ins-loan/ltv-convert',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=LoanLtvConvert,
    )
