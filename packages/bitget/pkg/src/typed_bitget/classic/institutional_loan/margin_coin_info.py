# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class LoanMarginCoin(TypedDict):
  """One accepted margin coin."""

  coin: str
  """The margin coin, e.g. `USDT`."""
  convertRatio: str
  """Conversion ratio applied when valuing this coin toward the loan line, as a fraction."""
  maxConvertValue: str
  """Maximum value of this coin counted toward the loan line."""


class Request(TypedDict):
  productId: str
  """The loan product id."""


class LoanMarginCoinInfo(TypedDict):
  """Margin/collateral coins accepted for one institutional loan product."""

  productId: str
  """The loan product id."""
  coinInfo: list[LoanMarginCoin]
  """Accepted margin coins and their conversion ratios."""


class MarginCoinInfo(RpcEndpoint):
  """Get the margin/collateral coins accepted for a loan product and their conversion ratios toward the loan line."""

  async def margin_coin_info(
    self,
    product_id: str,
    *,
    validate: bool | None = None,
  ) -> LoanMarginCoinInfo:
    """Get the margin/collateral coins accepted for a loan product and their conversion ratios toward the loan line.

    Args:
      product_id: The loan product id.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-instloan-public/classic-instloan-public#get-margin-coin-info)
    """
    return await self.request(
      Request(productId=product_id),
      method='GET',
      path='/api/v2/spot/ins-loan/ensure-coins-convert',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=LoanMarginCoinInfo,
    )
