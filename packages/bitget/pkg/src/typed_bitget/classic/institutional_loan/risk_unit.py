# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.institutional_loan.risk_unit` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class LoanRiskUnit(TypedDict):
  """The caller's institutional loan risk unit ids."""

  riskUnitId: list[str]
  """Risk unit ids the caller belongs to."""


class RiskUnit(RpcEndpoint):
  """`classic.institutional_loan.risk_unit`."""

  async def risk_unit(self, *, validate: bool | None = None) -> LoanRiskUnit:
    """List the caller's institutional loan risk unit ids (a risk unit groups a main account with its bound sub-accounts for loan-to-value calculation).

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/instloan/account/Get-Risk-Unit)
    """
    return await self.authed_request(
      'GET',
      '/api/v2/spot/ins-loan/risk-unit',
      validator=validator(LoanRiskUnit),
      validate=validate,
    )
