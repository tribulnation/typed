# Generated by typed-dev — see CONTRIBUTING.md.
"""`classic.institutional_loan.product_info` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class LoanProductInfo(TypedDict):
  """An institutional loan product's risk-control lines."""

  productId: str
  """The loan product id."""
  leverage: str
  """Maximum leverage for this loan product, e.g. `2x`/`4x`."""
  transferLine: str
  """Restrict line for transfer, as a fraction, e.g. `0.6667`."""
  spotBuyLine: str
  """Restrict line for spot buy, as a fraction, e.g. `0.8`."""
  liquidationLine: str
  """Line for liquidation, as a fraction, e.g. `0.9`."""
  stopLiquidationLine: str
  """Line for stop liquidation, as a fraction, e.g. `0.75`."""


class ProductInfo(RpcEndpoint):
  """`classic.institutional_loan.product_info`."""

  async def product_info(
    self,
    *,
    product_id: str,
    validate: bool | None = None,
  ) -> LoanProductInfo:
    """Get an institutional loan product's risk-control lines (leverage, transfer/spot-buy/liquidation thresholds).

    Args:
      product_id: The loan product id.

    References:
      - [Official docs](https://www.bitget.com/api-doc/classic/instloan/public/Get-Product-Info)
    """
    params: dict = {
      'productId': product_id,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/spot/ins-loan/product-infos',
      params=params,
      validator=validator(LoanProductInfo),
      validate=validate,
    )
