# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """A bind/unbind operation for one sub-UID."""

  riskUnitId: NotRequired[str]
  """Risk unit ID. Required when called by the parent account; not required when called by the risk-unit account itself."""
  uid: str
  """Sub-UID to bind or unbind. A risk unit supports up to 50 UIDs."""
  operate: Literal['bind', 'unbind']
  """Operation to perform."""


Response = str


class BindRiskUnit(RpcEndpoint):
  """Bind or unbind a sub-UID to an institutional-loan risk unit."""

  async def bind_risk_unit(
    self,
    *,
    risk_unit_id: str | None = None,
    uid: str,
    operate: Literal['bind', 'unbind'],
    validate: bool | None = None,
  ) -> Response:
    """Bind or unbind a sub-UID to an institutional-loan risk unit.

    Args:
      risk_unit_id: Risk unit ID. Required when called by the parent account; not required when called by the risk-unit account itself.
      uid: Sub-UID to bind or unbind. A risk unit supports up to 50 UIDs.
      operate: Operation to perform.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/classic-instloan-account/classic-instloan-account#bind-unbind-sub-account-uid-to-risk-unit)
    """
    request: Request = Request(uid=uid, operate=operate)
    if risk_unit_id is not None:
      request['riskUnitId'] = risk_unit_id
    return await self.request(
      request,
      method='POST',
      path='/api/v2/spot/ins-loan/bind-uid',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
