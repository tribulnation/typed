# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class P2pBalance(TypedDict):
  """Caller's P2P balance for one token."""

  token: str
  """Token this balance is denominated in."""
  totalBalance: str
  """Total P2P balance, as a decimal string."""
  frozenBalance: str
  """Balance currently frozen by open advertisements or orders, as a decimal string."""
  availableBalance: str
  """Balance currently available to use in new advertisements or orders, as a decimal string."""


class Request(TypedDict):
  token: str
  """Crypto token to get the balance for, e.g. `USDT`."""


class Balance(RpcEndpoint):
  """Get the caller's P2P balance for one token: total, frozen (held by open advertisements/orders), and available."""

  async def balance(self, token: str, *, validate: bool | None = None) -> P2pBalance:
    """Get the caller's P2P balance for one token: total, frozen (held by open advertisements/orders), and available.

    Args:
      token: Crypto token to get the balance for, e.g. `USDT`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/p2p-common/p2p-common#get-balance)
    """
    return await self.request(
      Request(token=token),
      method='GET',
      path='/api/v3/p2p/balance',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=P2pBalance,
    )
