# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.p2p.user_info` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class P2pUserEquityDetail(TypedDict):
  """Caller's current advertisement and pending-order limits."""

  maxAdvSellNum: str
  """Maximum number of concurrent sell advertisements allowed, as a decimal string."""
  maxAdvSellLimit: str
  """Maximum total sell advertisement quantity allowed, as a decimal string."""
  maxAdvBuyNum: str
  """Maximum number of concurrent buy advertisements allowed, as a decimal string."""
  maxAdvBuyLimit: str
  """Maximum total buy advertisement quantity allowed, as a decimal string."""
  totalMaxPendingOrder: str
  """Maximum number of pending orders allowed across all advertisements, as a decimal string."""
  advMaxPendingOrder: str
  """Maximum number of pending orders allowed per advertisement, as a decimal string."""


class P2pUserInfo(TypedDict):
  """The caller's P2P merchant profile."""

  uid: str
  """Caller's user ID."""
  nickName: str
  """Caller's P2P display nickname."""
  accountLevel: Literal['starter', 'advanced', 'master']
  """Caller's P2P merchant account level."""
  completedOrderNum: str
  """Total number of orders completed all-time, as a decimal string."""
  positiveRate: str
  """All-time positive-feedback rate, as a decimal string fraction."""
  completedOrderNum30D: str
  """Number of orders completed in the last 30 days, as a decimal string."""
  completedRate30D: str
  """Order completion rate over the last 30 days, as a decimal string fraction."""
  avgPayTime30D: str
  """Average time to mark an order paid over the last 30 days, as a decimal string."""
  avgReleaseTime30D: str
  """Average time to release an order's asset over the last 30 days, as a decimal string."""
  equityDetail: P2pUserEquityDetail
  registerTime: TimestampMillis
  """Caller's P2P registration time, Unix millisecond timestamp."""


class UserInfo(RpcEndpoint):
  """`uta.p2p.user_info`."""

  async def user_info(self, *, validate: bool | None = None) -> P2pUserInfo:
    """Get the caller's P2P merchant profile: account level, order statistics, and current advertisement/pending-order limits.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/p2p/Get-User-Info)
    """
    return await self.authed_request(
      'GET',
      '/api/v3/p2p/user-info',
      validator=validator(P2pUserInfo),
      validate=validate,
    )
