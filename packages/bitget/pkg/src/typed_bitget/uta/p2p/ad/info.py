# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.p2p.ad.info` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class P2pAdPayMethodRef(TypedDict):
  """Reference to a configured payment method accepted on this advertisement."""

  payMethodId: str
  """Payment method ID."""
  userPayMethodId: NotRequired[str]
  """Caller's specific configured instance of this payment method."""


class P2pAdInfo(TypedDict):
  """Full details of one of the caller's P2P advertisements."""

  advId: str
  """Advertisement ID."""
  side: Literal['buy', 'sell']
  """Advertisement side."""
  priceType: Literal['fixed', 'floating']
  """Whether the advertisement's price is a fixed value (`price`) or a floating premium over the reference market rate (`premium`)."""
  token: str
  """Crypto token of this advertisement."""
  fiat: str
  """Fiat currency of this advertisement."""
  price: str
  """Price per unit of `token`, in `fiat`, as a decimal string."""
  amount: str
  """Total token quantity originally offered, as a decimal string."""
  minAmount: str
  """Minimum fiat amount tradeable against this advertisement, as a decimal string."""
  maxAmount: str
  """Maximum fiat amount tradeable against this advertisement, as a decimal string."""
  payDuration: str
  """Time limit a buyer has to mark an order as paid before it auto-cancels, as a decimal string."""
  soldAmount: str
  """Token quantity already sold from this advertisement, as a decimal string."""
  lastAmount: str
  """Remaining token quantity available on this advertisement, as a decimal string."""
  premium: str
  """Premium over the reference market rate, as a decimal string percentage. Populated when `priceType` is `floating`."""
  turnover: str
  """Total fiat turnover this advertisement has generated, as a decimal string."""
  remark: str
  """Public remark shown on the advertisement."""
  tradeTerm: str
  """Trade terms shown to counterparties before they open an order."""
  payMethodIds: list[P2pAdPayMethodRef]
  """Payment methods accepted for this advertisement."""
  tokenPrecision: str
  """Decimal precision of `token` amounts, as a decimal string."""
  fiatPrecision: str
  """Decimal precision of `fiat` amounts, as a decimal string."""
  market: Literal['public', 'private']
  """Whether this advertisement is publicly listed or a private (direct) offer."""
  avgTime: str
  """Merchant's average release time associated with this advertisement, as a decimal string."""
  createdTime: TimestampMillis
  """Advertisement creation time, Unix millisecond timestamp."""
  updatedTime: TimestampMillis
  """Advertisement last-update time, Unix millisecond timestamp."""


class Info(RpcEndpoint):
  """`uta.p2p.ad.info`."""

  async def info(self, *, adv_id: str, validate: bool | None = None) -> P2pAdInfo:
    """Get full details of one of the caller's P2P advertisements.

    Args:
      adv_id: Identifier of the advertisement to fetch.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/p2p/Get-Ad-Info)
    """
    params: dict = {
      'advId': adv_id,
    }
    return await self.authed_request(
      'GET',
      '/api/v3/p2p/ad-info',
      params=params,
      validator=validator(P2pAdInfo),
      validate=validate,
    )
