# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class P2pPayMethodRef(TypedDict):
  """Reference to one of the caller's configured payment methods."""

  payMethodId: str
  """Payment method ID."""
  userPayMethodId: NotRequired[str]
  """Caller's specific configured instance of this payment method."""


Response = str


class Request(TypedDict):
  """Fields to update on an existing P2P advertisement. Only `advId` is required; other fields are updated when present."""

  advId: str
  """Identifier of the advertisement to update."""
  priceType: NotRequired[Literal['fixed', 'floating']]
  """Whether the advertisement's price is a fixed value (`price`) or a floating premium over the reference market rate (`premium`)."""
  price: NotRequired[float]
  """New fixed price per unit of the advertisement's token, in its fiat currency."""
  premium: NotRequired[float]
  """New premium over the reference market rate, as a percentage."""
  minAmount: NotRequired[float]
  """New minimum fiat amount tradeable against this advertisement."""
  maxAmount: NotRequired[float]
  """New maximum fiat amount tradeable against this advertisement."""
  quantity: NotRequired[float]
  """New total token quantity offered on this advertisement."""
  payMethodIds: NotRequired[list[P2pPayMethodRef]]
  """New set of payment methods the counterparty may use to settle this advertisement."""
  payTimeLimit: NotRequired[int]
  """New time limit, in minutes, a buyer has to mark an order as paid before it auto-cancels."""
  remark: NotRequired[str]
  """New public remark shown on the advertisement."""
  tradeTerms: NotRequired[str]
  """New trade terms shown to counterparties before they open an order."""


class Update(RpcEndpoint):
  """Update fields of an existing P2P advertisement owned by the caller."""

  async def update(
    self,
    *,
    adv_id: str,
    price_type: Literal['fixed', 'floating'] | None = None,
    price: float | None = None,
    premium: float | None = None,
    min_amount: float | None = None,
    max_amount: float | None = None,
    quantity: float | None = None,
    pay_method_ids: list[P2pPayMethodRef] | None = None,
    pay_time_limit: int | None = None,
    remark: str | None = None,
    trade_terms: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Update fields of an existing P2P advertisement owned by the caller.

    Args:
      adv_id: Identifier of the advertisement to update.
      price_type: Whether the advertisement's price is a fixed value (`price`) or a floating premium over the reference market rate (`premium`).
      price: New fixed price per unit of the advertisement's token, in its fiat currency.
      premium: New premium over the reference market rate, as a percentage.
      min_amount: New minimum fiat amount tradeable against this advertisement.
      max_amount: New maximum fiat amount tradeable against this advertisement.
      quantity: New total token quantity offered on this advertisement.
      pay_method_ids: New set of payment methods the counterparty may use to settle this advertisement.
      pay_time_limit: New time limit, in minutes, a buyer has to mark an order as paid before it auto-cancels.
      remark: New public remark shown on the advertisement.
      trade_terms: New trade terms shown to counterparties before they open an order.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/p2p-ad-management/p2p-ad-management#update-ad)
    """
    request: Request = Request(advId=adv_id)
    if price_type is not None:
      request['priceType'] = price_type
    if price is not None:
      request['price'] = price
    if premium is not None:
      request['premium'] = premium
    if min_amount is not None:
      request['minAmount'] = min_amount
    if max_amount is not None:
      request['maxAmount'] = max_amount
    if quantity is not None:
      request['quantity'] = quantity
    if pay_method_ids is not None:
      request['payMethodIds'] = pay_method_ids
    if pay_time_limit is not None:
      request['payTimeLimit'] = pay_time_limit
    if remark is not None:
      request['remark'] = remark
    if trade_terms is not None:
      request['tradeTerms'] = trade_terms
    return await self.request(
      request,
      method='POST',
      path='/api/v3/p2p/ad-update',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
