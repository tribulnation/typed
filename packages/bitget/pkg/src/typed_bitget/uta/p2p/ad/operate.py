# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Lifecycle operation to apply to an existing advertisement."""

  advId: str
  """Identifier of the advertisement to operate on."""
  operation: Literal['publish', 'delist', 'remove']
  """Lifecycle operation to apply."""


Response = str


class Operate(RpcEndpoint):
  """Publish, delist, or remove an existing P2P advertisement owned by the caller."""

  async def operate(
    self,
    adv_id: str,
    *,
    operation: Literal['publish', 'delist', 'remove'],
    validate: bool | None = None,
  ) -> Response:
    """Publish, delist, or remove an existing P2P advertisement owned by the caller.

    Args:
      adv_id: Identifier of the advertisement to operate on.
      operation: Lifecycle operation to apply.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/p2p-ad-management/p2p-ad-management#operate-ad)
    """
    return await self.request(
      Request(advId=adv_id, operation=operation),
      method='POST',
      path='/api/v3/p2p/ad-operate',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
