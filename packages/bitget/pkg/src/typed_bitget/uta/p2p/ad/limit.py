# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.p2p.ad.limit` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class P2pAdLimit(TypedDict):
  """Publishable advertisement token-amount limits for a token/fiat pair."""

  buyMinTokenAmount: str
  """Minimum token amount publishable on a buy advertisement, as a decimal string."""
  buyMaxTokenAmount: str
  """Maximum token amount publishable on a buy advertisement, as a decimal string."""
  sellMinTokenAmount: str
  """Minimum token amount publishable on a sell advertisement, as a decimal string."""
  sellMaxTokenAmount: str
  """Maximum token amount publishable on a sell advertisement, as a decimal string."""


class Limit(RpcEndpoint):
  """`uta.p2p.ad.limit`."""

  async def limit(
    self,
    *,
    token: str,
    fiat: str,
    side: Literal['buy', 'sell'],
    validate: bool | None = None,
  ) -> P2pAdLimit:
    """Get the caller's current minimum/maximum publishable advertisement amounts for a token/fiat pair and side.

    Args:
      token: Crypto token, e.g. `USDT`.
      fiat: Fiat currency, e.g. `USD`.
      side: Advertisement side to get limits for.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/p2p/Get-Ad-Limit)
    """
    params: dict = {
      'token': token,
      'fiat': fiat,
      'side': side,
    }
    return await self.authed_request(
      'GET',
      '/api/v3/p2p/ad-limit',
      params=params,
      validator=validator(P2pAdLimit),
      validate=validate,
    )
