# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.p2p.pay_methods` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class P2pPayMethodInfoField(TypedDict):
  """One structured field of a payment method."""

  name: str
  """Field name."""
  type: Literal['file', 'number', 'txt']
  """Field value type."""
  value: str
  """Field value."""


class P2pPayMethod(TypedDict):
  """One of the caller's configured payment methods."""

  payMethodId: str
  """Payment method ID."""
  payMethodName: str
  """Payment method display name."""
  payMethodUserName: str
  """Caller's name registered with this payment method."""
  userPayMethodId: str
  """Caller's specific configured instance of this payment method, referenced from advertisements."""
  payMethodInfo: list[P2pPayMethodInfoField]
  """Structured fields configured for this payment method, e.g. account number, bank name."""


class PayMethods(RpcEndpoint):
  """`uta.p2p.pay_methods`."""

  async def pay_methods(self, *, validate: bool | None = None) -> list[P2pPayMethod]:
    """List the caller's own configured P2P payment methods.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/p2p/Get-Pay-Method)
    """
    return await self.authed_request(
      'GET',
      '/api/v3/p2p/pay-method',
      validator=validator(list[P2pPayMethod]),
      validate=validate,
    )
