# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class P2pExchangeRate(TypedDict):
  """P2P reference exchange rate between a token and a fiat currency."""

  rate: str
  """Reference price of one unit of `token`, in `fiat`, as a decimal string."""


class Request(TypedDict):
  token: str
  """Crypto token, e.g. `USDT`."""
  fiat: str
  """Fiat currency, e.g. `USD`."""


class ExchangeRate(RpcEndpoint):
  """Get the current P2P reference exchange rate between a crypto token and a fiat currency."""

  async def exchange_rate(
    self,
    *,
    token: str,
    fiat: str,
    validate: bool | None = None,
  ) -> P2pExchangeRate:
    """Get the current P2P reference exchange rate between a crypto token and a fiat currency.

    Args:
      token: Crypto token, e.g. `USDT`.
      fiat: Fiat currency, e.g. `USD`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/p2p-ad-management/p2p-ad-management#get-exchange-rate)
    """
    return await self.request(
      Request(token=token, fiat=fiat),
      method='GET',
      path='/api/v3/p2p/exchange-rate',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=P2pExchangeRate,
    )
