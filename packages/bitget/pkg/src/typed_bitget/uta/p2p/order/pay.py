# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """An order to act on."""

  orderId: str
  """Identifier of the order to confirm payment for."""


Response = str


class Pay(RpcEndpoint):
  """Confirm that the buyer has sent fiat payment for a pending P2P order, moving it from `pending_payment` toward `pending_release`."""

  async def pay(self, order_id: str, *, validate: bool | None = None) -> Response:
    """Confirm that the buyer has sent fiat payment for a pending P2P order, moving it from `pending_payment` toward `pending_release`.

    Args:
      order_id: Identifier of the order to confirm payment for.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/p2p-order-management/p2p-order-management#confirm-payment)
    """
    return await self.request(
      Request(orderId=order_id),
      method='POST',
      path='/api/v3/p2p/order-pay',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
