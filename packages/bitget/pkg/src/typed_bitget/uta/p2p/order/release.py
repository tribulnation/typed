# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.p2p.order.release` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class P2pOrderActionRequest(TypedDict):
  """An order to act on."""

  orderId: str
  """Identifier of the order to release the asset for."""


class Release(RpcEndpoint):
  """`uta.p2p.order.release`."""

  async def release(
    self,
    p2p_order_action_request: P2pOrderActionRequest,
    *,
    validate: bool | None = None,
  ) -> str:
    """Release the seller's held asset for a P2P order once fiat payment has been confirmed received, completing the order.

    Args:
      p2p_order_action_request: An order to act on.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/p2p/Order-Release)
    """
    return await self.authed_request(
      'POST',
      '/api/v3/p2p/order-release',
      json=p2p_order_action_request,
      validator=validator(str),
      validate=validate,
    )
