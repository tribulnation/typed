# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """An order to act on."""

  orderId: str
  """Identifier of the order to release the asset for."""


Response = str


class Release(RpcEndpoint):
  """Release the seller's held asset for a P2P order once fiat payment has been confirmed received, completing the order."""

  async def release(self, order_id: str, *, validate: bool | None = None) -> Response:
    """Release the seller's held asset for a P2P order once fiat payment has been confirmed received, completing the order.

    Args:
      order_id: Identifier of the order to release the asset for.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/p2p-order-management/p2p-order-management#release-asset)
    """
    return await self.request(
      Request(orderId=order_id),
      method='POST',
      path='/api/v3/p2p/order-release',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
