# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class BrokerSubDepositAddress(TypedDict):
  """A broker sub-account's deposit address."""

  subUid: str
  """Sub-account unique identifier."""
  coin: str
  """Coin name."""
  address: str
  """Deposit address."""
  chain: str
  """Chain name."""
  tag: str | None
  """Destination tag, or null for chains that don't use one."""
  url: str
  """Block-explorer URL for this address."""
  cTime: TimestampMillis
  """Creation time, as a millisecond Unix timestamp."""


class Request(TypedDict):
  """Sub-account deposit address lookup parameters."""

  subUid: str
  """Sub-account unique identifier."""
  coin: str
  """Coin name."""
  chain: NotRequired[str]
  """Chain name. Defaults to the coin's primary chain when omitted."""


class DepositAddress(RpcEndpoint):
  """Get the deposit address for a coin on a broker sub-account."""

  async def deposit_address(
    self,
    *,
    sub_uid: str,
    coin: str,
    chain: str | None = None,
    validate: bool | None = None,
  ) -> BrokerSubDepositAddress:
    """Get the deposit address for a coin on a broker sub-account.

    Args:
      sub_uid: Sub-account unique identifier.
      coin: Coin name.
      chain: Chain name. Defaults to the coin's primary chain when omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/broker-broker/broker#get-broker-sub-account-deposit-address)
    """
    request: Request = Request(subUid=sub_uid, coin=coin)
    if chain is not None:
      request['chain'] = chain
    return await self.request(
      request,
      method='POST',
      path='/api/v3/broker/sub-deposit-address',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerSubDepositAddress,
    )
