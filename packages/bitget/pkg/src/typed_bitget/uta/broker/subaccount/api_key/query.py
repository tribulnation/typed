# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class BrokerSubApiKey(TypedDict):
  """A broker sub-account API key."""

  subUid: str
  """Sub-account unique identifier."""
  label: str
  """API key label."""
  apiKey: str
  """The API key."""
  secretKey: str
  """The secret key."""
  permType: Literal['read_write', 'read_only']
  """Whether the API key can write, or read only."""
  permList: list[Literal['uta_trade', 'uta_mgt', 'withdraw']]
  """Permissions granted to the API key."""
  ipList: list[str] | None
  """IP allowlist configured for this key, or null when unrestricted."""


class Request(TypedDict):
  subUid: str
  """Sub-account unique identifier."""


class Query(RpcEndpoint):
  """Retrieve API key information for a broker sub-account."""

  async def query(
    self, sub_uid: str, *, validate: bool | None = None
  ) -> BrokerSubApiKey:
    """Retrieve API key information for a broker sub-account.

    Args:
      sub_uid: Sub-account unique identifier.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/broker-broker/broker#get-broker-sub-account-api-key)
    """
    return await self.request(
      Request(subUid=sub_uid),
      method='GET',
      path='/api/v3/broker/query-sub-apikey',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerSubApiKey,
    )
