# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class BrokerSubAccount(TypedDict):
  """A broker sub-account."""

  subUid: str
  """Sub-account unique identifier."""
  subaccountName: str
  """Sub-account username."""
  subaccountEmail: str | None
  """Sub-account email, or null when none is set."""
  status: Literal['normal', 'freeze']
  """Broker sub-account status."""
  permList: list[
    Literal[
      'withdraw', 'transfer', 'spot_trade', 'contract_trade', 'margin_trade', 'deposit'
    ]
  ]
  """Permissions granted to the sub-account."""
  label: str
  """Sub-account label."""
  language: Literal[
    'en_US',
    'zh_CN',
    'ja_JP',
    'vi_VN',
    'zh_TW',
    'ru_RU',
    'es_ES',
    'tr_TR',
    'fr_FR',
    'de_DE',
    'pt_PT',
    'th_TH',
  ]
  """A broker sub-account's UI language preference."""
  cTime: TimestampMillis
  """Creation time, as a millisecond Unix timestamp."""
  uTime: TimestampMillis
  """Last update time, as a millisecond Unix timestamp."""


class Request(TypedDict):
  limit: NotRequired[int]
  """Items per page. Default 10, maximum 100."""
  cursor: NotRequired[str]
  """Pagination cursor: pass the last `subUid` from the previous query to continue from there."""
  status: NotRequired[Literal['normal', 'freeze']]
  """Broker sub-account status."""


class SubList(TypedDict):
  """A page of broker sub-accounts."""

  subList: list[BrokerSubAccount]
  """Sub-accounts in this page."""


class List(RpcEndpoint):
  """List sub-accounts under the authenticated broker master account."""

  async def list(
    self,
    limit: int | None = None,
    *,
    cursor: str | None = None,
    status: Literal['normal', 'freeze'] | None = None,
    validate: bool | None = None,
  ) -> SubList:
    """List sub-accounts under the authenticated broker master account.

    Args:
      limit: Items per page. Default 10, maximum 100.
      cursor: Pagination cursor: pass the last `subUid` from the previous query to continue from there.
      status: Broker sub-account status.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/broker-broker/broker#get-broker-sub-account-list)
    """
    request: Request = Request()
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    if status is not None:
      request['status'] = status
    return await self.request(
      request,
      method='GET',
      path='/api/v3/broker/sub-list',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SubList,
    )
