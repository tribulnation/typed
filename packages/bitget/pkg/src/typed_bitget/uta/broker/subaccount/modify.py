# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class BrokerSubAccountModified(TypedDict):
  """A broker sub-account after modification."""

  subUid: str
  """Sub-account unique identifier."""
  subaccountName: str
  """Sub-account username."""
  status: Literal['normal', 'freeze']
  """Broker sub-account status."""
  permList: list[
    Literal[
      'withdraw', 'transfer', 'spot_trade', 'contract_trade', 'margin_trade', 'deposit'
    ]
  ]
  """Permissions granted to the sub-account."""
  label: str
  """Sub-account label."""
  cTime: TimestampMillis
  """Creation time, as a millisecond Unix timestamp."""
  uTime: TimestampMillis
  """Last update time, as a millisecond Unix timestamp."""
  language: Literal[
    'en_US',
    'zh_CN',
    'ja_JP',
    'vi_VN',
    'zh_TW',
    'ru_RU',
    'es_ES',
    'tr_TR',
    'fr_FR',
    'de_DE',
    'pt_PT',
    'th_TH',
  ]
  """A broker sub-account's UI language preference."""


class Request(TypedDict):
  """Sub-account modification parameters."""

  subUid: str
  """Sub-account unique identifier."""
  status: NotRequired[Literal['normal', 'freeze']]
  """Broker sub-account status."""
  permList: NotRequired[
    list[
      Literal[
        'withdraw',
        'transfer',
        'spot_trade',
        'contract_trade',
        'margin_trade',
        'deposit',
      ]
    ]
  ]
  """Permissions to grant the sub-account. Replaces the existing permission set."""


class Modify(RpcEndpoint):
  """Modify a sub-account's status and/or permissions."""

  async def modify(
    self,
    sub_uid: str,
    *,
    status: Literal['normal', 'freeze'] | None = None,
    perm_list: list[
      Literal[
        'withdraw',
        'transfer',
        'spot_trade',
        'contract_trade',
        'margin_trade',
        'deposit',
      ]
    ]
    | None = None,
    validate: bool | None = None,
  ) -> BrokerSubAccountModified:
    """Modify a sub-account's status and/or permissions.

    Args:
      sub_uid: Sub-account unique identifier.
      status: Broker sub-account status.
      perm_list: Permissions to grant the sub-account. Replaces the existing permission set.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/broker-broker/broker#modify-broker-sub-account)
    """
    request: Request = Request(subUid=sub_uid)
    if status is not None:
      request['status'] = status
    if perm_list is not None:
      request['permList'] = perm_list
    return await self.request(
      request,
      method='POST',
      path='/api/v3/broker/modify-sub',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerSubAccountModified,
    )
