# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class BrokerSubApiKeyCreated(TypedDict):
  """A newly created broker sub-account API key."""

  subUid: str
  """Sub-account unique identifier."""
  label: str
  """API key label."""
  apiKey: str
  """Generated API key."""
  secretKey: str
  """Generated secret key. Only ever returned at creation."""
  permType: Literal['read_write', 'read_only']
  """Whether the API key can write, or read only."""
  permList: list[Literal['uta_trade', 'uta_mgt', 'withdraw']]
  """Permissions granted to the API key."""
  ipList: list[str] | None
  """IP allowlist configured for this key, or null when unrestricted."""


class Request(TypedDict):
  """Sub-account API key creation parameters."""

  subUid: str
  """Sub-account unique identifier."""
  passphrase: str
  """Passphrase to protect the new API key. Required on every signed request made with it; if lost, the key must be recreated."""
  label: str
  """Label for the API key."""
  ipList: list[str]
  """IP allowlist for the new key. Up to 30 entries."""
  permType: Literal['read_write', 'read_only']
  """Whether the API key can write, or read only."""
  permList: list[Literal['uta_trade', 'uta_mgt', 'withdraw']]
  """Permissions to grant the API key."""


class Create(RpcEndpoint):
  """Create an API key for a broker sub-account."""

  async def create(
    self,
    *,
    sub_uid: str,
    passphrase: str,
    label: str,
    ip_list: list[str],
    perm_type: Literal['read_write', 'read_only'],
    perm_list: list[Literal['uta_trade', 'uta_mgt', 'withdraw']],
    validate: bool | None = None,
  ) -> BrokerSubApiKeyCreated:
    """Create an API key for a broker sub-account.

    Args:
      sub_uid: Sub-account unique identifier.
      passphrase: Passphrase to protect the new API key. Required on every signed request made with it; if lost, the key must be recreated.
      label: Label for the API key.
      ip_list: IP allowlist for the new key. Up to 30 entries.
      perm_type: Whether the API key can write, or read only.
      perm_list: Permissions to grant the API key.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/broker-broker/broker#create-broker-sub-account-api-key)
    """
    return await self.request(
      Request(
        subUid=sub_uid,
        passphrase=passphrase,
        label=label,
        ipList=ip_list,
        permType=perm_type,
        permList=perm_list,
      ),
      method='POST',
      path='/api/v3/broker/create-sub-apikey',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerSubApiKeyCreated,
    )
