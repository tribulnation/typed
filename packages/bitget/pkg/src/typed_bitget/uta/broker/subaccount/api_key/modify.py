# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class BrokerSubApiKeyModified(TypedDict):
  """A broker sub-account API key after modification."""

  subUid: str
  """Sub-account unique identifier."""
  label: str
  """API key label."""
  apiKey: str
  """The API key."""
  permType: Literal['read_write', 'read_only']
  """Whether the API key can write, or read only."""
  permList: list[Literal['uta_trade', 'uta_mgt', 'withdraw']]
  """Permissions granted to the API key."""
  ipList: list[str]
  """IP allowlist configured for this key."""


class Request(TypedDict):
  """Sub-account API key modification parameters."""

  subUid: str
  """Sub-account unique identifier."""
  passphrase: str
  """Passphrase protecting the API key being modified. If lost, the key must be recreated."""
  apiKey: str
  """The API key to modify."""
  label: NotRequired[str]
  """New label for the API key."""
  ipList: NotRequired[list[str]]
  """New IP allowlist for the key. Up to 30 entries."""
  permType: NotRequired[Literal['read_write', 'read_only']]
  """Whether the API key can write, or read only."""
  permList: NotRequired[list[Literal['uta_trade', 'uta_mgt', 'withdraw']]]
  """New permissions for the API key. Replaces the existing permission set."""


class Modify(RpcEndpoint):
  """Modify an existing broker sub-account API key's label, IP allowlist, or permissions."""

  async def modify(
    self,
    *,
    sub_uid: str,
    passphrase: str,
    api_key: str,
    label: str | None = None,
    ip_list: list[str] | None = None,
    perm_type: Literal['read_write', 'read_only'] | None = None,
    perm_list: list[Literal['uta_trade', 'uta_mgt', 'withdraw']] | None = None,
    validate: bool | None = None,
  ) -> BrokerSubApiKeyModified:
    """Modify an existing broker sub-account API key's label, IP allowlist, or permissions.

    Args:
      sub_uid: Sub-account unique identifier.
      passphrase: Passphrase protecting the API key being modified. If lost, the key must be recreated.
      api_key: The API key to modify.
      label: New label for the API key.
      ip_list: New IP allowlist for the key. Up to 30 entries.
      perm_type: Whether the API key can write, or read only.
      perm_list: New permissions for the API key. Replaces the existing permission set.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/broker-broker/broker#modify-broker-sub-account-api-key)
    """
    request: Request = Request(subUid=sub_uid, passphrase=passphrase, apiKey=api_key)
    if label is not None:
      request['label'] = label
    if ip_list is not None:
      request['ipList'] = ip_list
    if perm_type is not None:
      request['permType'] = perm_type
    if perm_list is not None:
      request['permList'] = perm_list
    return await self.request(
      request,
      method='POST',
      path='/api/v3/broker/modify-sub-apikey',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerSubApiKeyModified,
    )
