# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Sub-account API key deletion parameters."""

  subUid: str
  """Sub-account unique identifier."""
  apiKey: str
  """The API key to delete."""


class Delete(RpcEndpoint):
  """Delete an API key from a broker sub-account."""

  async def delete(
    self, *, sub_uid: str, api_key: str, validate: bool | None = None
  ) -> None:
    """Delete an API key from a broker sub-account.

    Args:
      sub_uid: Sub-account unique identifier.
      api_key: The API key to delete.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/broker-broker/broker#delete-broker-sub-account-api-key)
    """
    return await self.request(
      Request(subUid=sub_uid, apiKey=api_key),
      method='POST',
      path='/api/v3/broker/delete-sub-apikey',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=None,
    )
