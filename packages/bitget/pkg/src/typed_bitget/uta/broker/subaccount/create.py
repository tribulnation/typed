# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class BrokerSubAccountCreated(TypedDict):
  """A newly created broker sub-account."""

  subUid: str
  """Sub-account unique identifier."""
  subaccountName: str
  """Sub-account username."""
  subaccountEmail: str
  """Sub-account email. A system-generated virtual email by default, or the bound email if one was separately configured."""
  status: Literal['normal', 'freeze']
  """Broker sub-account status."""
  permList: list[
    Literal[
      'withdraw', 'transfer', 'spot_trade', 'contract_trade', 'margin_trade', 'deposit'
    ]
  ]
  """Permissions granted to the sub-account."""
  label: str
  """Sub-account label."""
  cTime: TimestampMillis
  """Creation time, as a millisecond Unix timestamp."""


class Request(TypedDict):
  """Sub-account creation parameters."""

  subaccountName: str
  """Sub-account username."""
  label: str
  """Label identifying the sub-account. Maximum 50 characters."""


class Create(RpcEndpoint):
  """Create a new sub-account under the authenticated broker master account."""

  async def create(
    self,
    *,
    subaccount_name: str,
    label: str,
    validate: bool | None = None,
  ) -> BrokerSubAccountCreated:
    """Create a new sub-account under the authenticated broker master account.

    Args:
      subaccount_name: Sub-account username.
      label: Label identifying the sub-account. Maximum 50 characters.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/broker-broker/broker#create-broker-sub-account)
    """
    return await self.request(
      Request(subaccountName=subaccount_name, label=label),
      method='POST',
      path='/api/v3/broker/create-sub',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerSubAccountCreated,
    )
