# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.broker.sub_deposit_address` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class BrokerSubDepositAddress(TypedDict):
  """A broker sub-account's deposit address."""

  subUid: str
  """Sub-account unique identifier."""
  coin: str
  """Coin name."""
  address: str
  """Deposit address."""
  chain: str
  """Chain name."""
  tag: str | None
  """Destination tag, or null for chains that don't use one."""
  url: str
  """Block-explorer URL for this address."""
  cTime: TimestampMillis
  """Creation time, as a millisecond Unix timestamp."""


class GetBrokerSubDepositAddressRequest(TypedDict):
  """Sub-account deposit address lookup parameters."""

  subUid: str
  """Sub-account unique identifier."""
  coin: str
  """Coin name."""
  chain: NotRequired[str]
  """Chain name. Defaults to the coin's primary chain when omitted."""


class SubDepositAddress(RpcEndpoint):
  """`uta.broker.sub_deposit_address`."""

  async def sub_deposit_address(
    self,
    get_broker_sub_deposit_address_request: GetBrokerSubDepositAddressRequest,
    *,
    validate: bool | None = None,
  ) -> BrokerSubDepositAddress:
    """Get the deposit address for a coin on a broker sub-account.

    Args:
      get_broker_sub_deposit_address_request: Sub-account deposit address lookup parameters.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/broker/Subaccount-Deposit-Address)
    """
    return await self.authed_request(
      'POST',
      '/api/v3/broker/sub-deposit-address',
      json=get_broker_sub_deposit_address_request,
      validator=validator(BrokerSubDepositAddress),
      validate=validate,
    )
