# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class BrokerCommissionRecord(TypedDict):
  """Commission earned from one sub-account's trading of one symbol, within the queried window."""

  uid: str
  """Sub-account user ID."""
  coin: str
  """Commission-denominating coin."""
  symbol: str
  """Trading symbol."""
  dealtAmount: str
  """Traded amount."""
  totalFee: str
  """Total fee generated."""
  deductedFee: str
  """Fee deducted before commission was calculated."""
  paidFee: str
  """Fee actually paid by the sub-account."""
  markUpFee: str
  """Markup fee."""
  totalCommission: str
  """Total commission earned by the broker."""


class Request(TypedDict):
  startTime: NotRequired[TimestampMillis]
  """Query window start, as a millisecond Unix timestamp. Defaults to yesterday 00:00 UTC when omitted. The window may span at most 30 days."""
  endTime: NotRequired[TimestampMillis]
  """Query window end, as a millisecond Unix timestamp. Defaults to yesterday 23:59 UTC when omitted. The window may span at most 30 days."""
  pageSize: NotRequired[int]
  """Items per page."""
  pageNo: NotRequired[int]
  """Page number."""
  bizType: NotRequired[Literal['spot', 'futures']]
  """Business type."""
  subBizType: NotRequired[
    Literal['spot_trade', 'spot_margin', 'usdt_futures', 'usdc_futures', 'coin_futures']
  ]
  """Business sub-type."""


Response = list[BrokerCommissionRecord]


class Commission(RpcEndpoint):
  """Query commission earned across the authenticated broker master account's sub-accounts."""

  async def commission_paged(
    self,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    page_size: int | None = None,
    biz_type: Literal['spot', 'futures'] | None = None,
    sub_biz_type: Literal[
      'spot_trade', 'spot_margin', 'usdt_futures', 'usdc_futures', 'coin_futures'
    ]
    | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[Response]:
    """Query commission earned across the authenticated broker master account's sub-accounts.

    Paged variant of `commission`: Requests `pageNo` from 1 upwards and stops on the first page shorter than `page_size`, or after `max_pages` pages when one is given.

    Args:
      start_time: Query window start, as a millisecond Unix timestamp. Defaults to yesterday 00:00 UTC when omitted. The window may span at most 30 days.
      end_time: Query window end, as a millisecond Unix timestamp. Defaults to yesterday 23:59 UTC when omitted. The window may span at most 30 days.
      page_size: Items per page.
      biz_type: Business type.
      sub_biz_type: Business sub-type.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/broker-broker/broker#get-broker-commission)
    """
    page_no = 1
    pages = 0
    while True:
      response = await self.commission(
        start_time=start_time,
        end_time=end_time,
        page_size=page_size,
        biz_type=biz_type,
        sub_biz_type=sub_biz_type,
        page_no=page_no,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      if not response or (page_size is not None and len(response) < page_size):
        break
      page_no += 1

  async def commission(
    self,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    page_size: int | None = None,
    page_no: int | None = None,
    biz_type: Literal['spot', 'futures'] | None = None,
    sub_biz_type: Literal[
      'spot_trade', 'spot_margin', 'usdt_futures', 'usdc_futures', 'coin_futures'
    ]
    | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Query commission earned across the authenticated broker master account's sub-accounts.

    Args:
      start_time: Query window start, as a millisecond Unix timestamp. Defaults to yesterday 00:00 UTC when omitted. The window may span at most 30 days.
      end_time: Query window end, as a millisecond Unix timestamp. Defaults to yesterday 23:59 UTC when omitted. The window may span at most 30 days.
      page_size: Items per page.
      page_no: Page number.
      biz_type: Business type.
      sub_biz_type: Business sub-type.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/broker-broker/broker#get-broker-commission)
    """
    request: Request = Request()
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if page_size is not None:
      request['pageSize'] = page_size
    if page_no is not None:
      request['pageNo'] = page_no
    if biz_type is not None:
      request['bizType'] = biz_type
    if sub_biz_type is not None:
      request['subBizType'] = sub_biz_type
    return await self.request(
      request,
      method='GET',
      path='/api/v3/broker/commission',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
