# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class LoanUidBinding(TypedDict):
  """Result of a risk unit sub-account bind/unbind operation."""

  riskUnitId: str
  """Risk unit the sub-account was bound to or unbound from."""
  uid: str
  """Sub-account user id that was bound or unbound."""
  operate: Literal['bind', 'unbind']
  """Operation that was performed."""


class Request(TypedDict):
  """A risk unit sub-account bind/unbind request."""

  riskUnitId: NotRequired[str]
  """Risk unit to bind/unbind the sub-account to. Defaults to the caller's own risk unit."""
  uid: str
  """Sub-account user id to bind or unbind."""
  operate: Literal['bind', 'unbind']
  """Whether to bind or unbind `uid`."""


class BindUid(RpcEndpoint):
  """Bind or unbind a unified-trading sub-account user id to a risk unit."""

  async def bind_uid(
    self,
    *,
    risk_unit_id: str | None = None,
    uid: str,
    operate: Literal['bind', 'unbind'],
    validate: bool | None = None,
  ) -> LoanUidBinding:
    """Bind or unbind a unified-trading sub-account user id to a risk unit.

    Args:
      risk_unit_id: Risk unit to bind/unbind the sub-account to. Defaults to the caller's own risk unit.
      uid: Sub-account user id to bind or unbind.
      operate: Whether to bind or unbind `uid`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/institutional-loan-loan/institutional-loan#bind-unbind-uid-to-risk-unit)
    """
    request: Request = Request(uid=uid, operate=operate)
    if risk_unit_id is not None:
      request['riskUnitId'] = risk_unit_id
    return await self.request(
      request,
      method='POST',
      path='/api/v3/ins-loan/bind-uid',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=LoanUidBinding,
    )
