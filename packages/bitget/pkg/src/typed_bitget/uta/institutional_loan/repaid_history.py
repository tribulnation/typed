# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class LoanRepaidHistoryEntry(TypedDict):
  """One institutional loan repayment order."""

  repayOrderId: str
  """Identifier of the repayment order."""
  businessType: Literal['normal', 'liquidation']
  """Whether this repayment was a normal repayment or a forced liquidation."""
  repayType: Literal['all', 'part']
  """Whether this repayment covered the full loan or only part of it."""
  repaidTime: TimestampMillis
  """Millisecond epoch timestamp the repayment was made."""
  coin: str
  """Coin that was repaid."""
  repayAmount: str
  """Amount of `coin` repaid (principal), as a decimal string."""
  repayInterest: str
  """Amount of `coin` repaid as interest, as a decimal string."""


class Request(TypedDict):
  startTime: NotRequired[TimestampMillis]
  """Start of the time range to query, as a millisecond epoch timestamp."""
  endTime: NotRequired[TimestampMillis]
  """End of the time range to query, as a millisecond epoch timestamp."""
  limit: NotRequired[int]
  """Maximum number of records to return."""


Response = list[LoanRepaidHistoryEntry]


class RepaidHistory(RpcEndpoint):
  """Query the institutional loan account's repayment order history."""

  async def repaid_history(
    self,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Query the institutional loan account's repayment order history.

    Args:
      start_time: Start of the time range to query, as a millisecond epoch timestamp.
      end_time: End of the time range to query, as a millisecond epoch timestamp.
      limit: Maximum number of records to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/institutional-loan-loan/institutional-loan#get-repayment-orders)
    """
    request: Request = Request()
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/api/v3/ins-loan/repaid-history',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
