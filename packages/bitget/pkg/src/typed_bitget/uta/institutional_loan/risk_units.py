# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class RiskUnits(TypedDict):
  """The caller's institutional-loan risk units."""

  riskUnitId: list[str]
  """Identifiers of the caller's risk units."""


class RiskUnitsEndpoint(RpcEndpoint):
  """List the caller's institutional-loan risk unit ids."""

  async def risk_units(self, *, validate: bool | None = None) -> RiskUnits:
    """List the caller's institutional-loan risk unit ids.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/institutional-loan-loan/institutional-loan#get-risk-unit)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v3/ins-loan/risk-unit',
      meta={'required': True},
      validate=validate,
      response_type=RiskUnits,
    )
