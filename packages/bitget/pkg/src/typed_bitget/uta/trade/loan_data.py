# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class LoanDataDebtCoin(TypedDict):
  """Debt in a single coin."""

  coin: str
  """Coin borrowed."""
  debt: str
  """Outstanding debt in this coin."""
  interestFreeAmount: str
  """Portion of the debt currently interest-free."""
  interestRateNextHour: str
  """Interest rate that applies for the coming hour."""


class LoanData(TypedDict):
  """Current UTA auto-borrow loan state."""

  currentLoans: Decimal
  """Total current loan value across all coins."""
  interestPaymentTime: TimestampMillis
  """Time the next interest payment is charged, ms epoch."""
  debtCoinList: list[LoanDataDebtCoin]
  """Per-coin debt breakdown."""


class LoanDataEndpoint(RpcEndpoint):
  """Get the unified trading account's current auto-borrow loan data: total loans and per-coin debt."""

  async def loan_data(self, *, validate: bool | None = None) -> LoanData:
    """Get the unified trading account's current auto-borrow loan data: total loans and per-coin debt.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/trading-position-management/uta-trade-position#get-loan-data)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v3/trade/loan-data',
      meta={'required': True},
      validate=validate,
      response_type=LoanData,
    )
