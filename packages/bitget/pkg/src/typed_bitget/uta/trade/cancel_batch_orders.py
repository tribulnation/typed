# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.trade.cancel_batch_orders` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class BatchCancelOrderItem(TypedDict):
  """One order to cancel within a batch-cancel request."""

  orderId: NotRequired[str]
  """Client-assigned order identifier, 1-32 characters matching `^[\\.A-Z\\:/a-z0-9_-]{1,32}$`. Either `orderId` or `clientOid` must be supplied on lookups/cancels/modifies; when both are given `orderId` takes precedence."""
  clientOid: NotRequired[str]
  """Client-assigned order identifier, 1-32 characters matching `^[\\.A-Z\\:/a-z0-9_-]{1,32}$`. Either `orderId` or `clientOid` must be supplied on lookups/cancels/modifies; when both are given `orderId` takes precedence."""
  category: Literal['SPOT', 'MARGIN', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product type."""
  symbol: str
  """Trading pair, e.g. `BTCUSDT`."""


class BatchCancelResult(TypedDict):
  """Result of one order within a bulk cancel/close/place operation."""

  orderId: str
  """Venue-assigned order identifier."""
  clientOid: str
  """Client-assigned order identifier echoed back."""
  code: NotRequired[str]
  """Per-item result code; `"00000"` on success, a venue error code otherwise."""
  msg: NotRequired[str]
  """Per-item result message."""


class CancelBatchOrders(RpcEndpoint):
  """`uta.trade.cancel_batch_orders`."""

  async def cancel_batch_orders(
    self,
    list_batch_cancel_order_item: list[BatchCancelOrderItem],
    *,
    validate: bool | None = None,
  ) -> list[BatchCancelResult]:
    """Cancel multiple open orders in a single request. Each item succeeds or fails independently.

    Args:
      list_batch_cancel_order_item: Orders to cancel, one entry per order.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/trade/Batch-Cancel-Order)
    """
    return await self.authed_request(
      'POST',
      '/api/v3/trade/cancel-batch',
      json=list_batch_cancel_order_item,
      validator=validator(list[BatchCancelResult]),
      validate=validate,
    )
