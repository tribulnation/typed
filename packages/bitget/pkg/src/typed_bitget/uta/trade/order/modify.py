# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class ModifyOrderResult(TypedDict):
  """Identifiers of the order the call acted on."""

  orderId: str
  """Venue-assigned order identifier."""
  clientOid: str
  """Client-assigned order identifier echoed back."""


class Request(TypedDict):
  """Fields to change on an existing order. `qty` or `price` is required."""

  orderId: NotRequired[str]
  """Client-assigned order identifier, 1-32 characters matching `^[\\.A-Z\\:/a-z0-9_-]{1,32}$`. Either `orderId` or `clientOid` must be supplied on lookups/cancels/modifies; when both are given `orderId` takes precedence."""
  clientOid: NotRequired[str]
  """Client-assigned order identifier, 1-32 characters matching `^[\\.A-Z\\:/a-z0-9_-]{1,32}$`. Either `orderId` or `clientOid` must be supplied on lookups/cancels/modifies; when both are given `orderId` takes precedence."""
  qty: NotRequired[str]
  """New order quantity, in the base coin. Either `qty` or `price` is required."""
  price: NotRequired[Decimal]
  """New order price. Either `qty` or `price` is required."""
  autoCancel: NotRequired[Literal['yes', 'no']]
  """Auto-cancel the original order if the modification fails."""
  symbol: NotRequired[str]
  """Trading pair, e.g. `BTCUSDT`."""
  category: NotRequired[
    Literal['SPOT', 'MARGIN', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  ]
  """Product type."""
  tpTriggerBy: NotRequired[Literal['market', 'mark']]
  """Price type that triggers the attached take-profit. Futures only."""
  slTriggerBy: NotRequired[Literal['market', 'mark']]
  """Price type that triggers the attached stop-loss. Futures only."""
  takeProfit: NotRequired[str]
  """New take-profit trigger price. Pass `"0"` to cancel the attached take-profit."""
  stopLoss: NotRequired[str]
  """New stop-loss trigger price. Pass `"0"` to cancel the attached stop-loss."""
  tpOrderType: NotRequired[Literal['limit', 'market']]
  """Order type the take-profit fires as once triggered."""
  slOrderType: NotRequired[Literal['limit', 'market']]
  """Order type the stop-loss fires as once triggered."""
  tpLimitPrice: NotRequired[str]
  """Execution price for the take-profit, when `tpOrderType` is `limit`."""
  slLimitPrice: NotRequired[str]
  """Execution price for the stop-loss, when `slOrderType` is `limit`."""


class Modify(RpcEndpoint):
  """Modify the price, quantity, or attached take-profit/stop-loss of an open order."""

  async def modify(
    self,
    *,
    order_id: str | None = None,
    client_oid: str | None = None,
    qty: str | None = None,
    price: Decimal | None = None,
    auto_cancel: Literal['yes', 'no'] | None = None,
    symbol: str | None = None,
    category: Literal['SPOT', 'MARGIN', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
    | None = None,
    tp_trigger_by: Literal['market', 'mark'] | None = None,
    sl_trigger_by: Literal['market', 'mark'] | None = None,
    take_profit: str | None = None,
    stop_loss: str | None = None,
    tp_order_type: Literal['limit', 'market'] | None = None,
    sl_order_type: Literal['limit', 'market'] | None = None,
    tp_limit_price: str | None = None,
    sl_limit_price: str | None = None,
    validate: bool | None = None,
  ) -> ModifyOrderResult:
    """Modify the price, quantity, or attached take-profit/stop-loss of an open order.

    Args:
      order_id: Client-assigned order identifier, 1-32 characters matching `^[\\.A-Z\\:/a-z0-9_-]{1,32}$`. Either `orderId` or `clientOid` must be supplied on lookups/cancels/modifies; when both are given `orderId` takes precedence.
      client_oid: Client-assigned order identifier, 1-32 characters matching `^[\\.A-Z\\:/a-z0-9_-]{1,32}$`. Either `orderId` or `clientOid` must be supplied on lookups/cancels/modifies; when both are given `orderId` takes precedence.
      qty: New order quantity, in the base coin. Either `qty` or `price` is required.
      price: New order price. Either `qty` or `price` is required.
      auto_cancel: Auto-cancel the original order if the modification fails.
      symbol: Trading pair, e.g. `BTCUSDT`.
      category: Product type.
      tp_trigger_by: Price type that triggers the attached take-profit. Futures only.
      sl_trigger_by: Price type that triggers the attached stop-loss. Futures only.
      take_profit: New take-profit trigger price. Pass `"0"` to cancel the attached take-profit.
      stop_loss: New stop-loss trigger price. Pass `"0"` to cancel the attached stop-loss.
      tp_order_type: Order type the take-profit fires as once triggered.
      sl_order_type: Order type the stop-loss fires as once triggered.
      tp_limit_price: Execution price for the take-profit, when `tpOrderType` is `limit`.
      sl_limit_price: Execution price for the stop-loss, when `slOrderType` is `limit`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/trading-order-management/uta-trade-order#modify-order)
    """
    request: Request = Request()
    if order_id is not None:
      request['orderId'] = order_id
    if client_oid is not None:
      request['clientOid'] = client_oid
    if qty is not None:
      request['qty'] = qty
    if price is not None:
      request['price'] = price
    if auto_cancel is not None:
      request['autoCancel'] = auto_cancel
    if symbol is not None:
      request['symbol'] = symbol
    if category is not None:
      request['category'] = category
    if tp_trigger_by is not None:
      request['tpTriggerBy'] = tp_trigger_by
    if sl_trigger_by is not None:
      request['slTriggerBy'] = sl_trigger_by
    if take_profit is not None:
      request['takeProfit'] = take_profit
    if stop_loss is not None:
      request['stopLoss'] = stop_loss
    if tp_order_type is not None:
      request['tpOrderType'] = tp_order_type
    if sl_order_type is not None:
      request['slOrderType'] = sl_order_type
    if tp_limit_price is not None:
      request['tpLimitPrice'] = tp_limit_price
    if sl_limit_price is not None:
      request['slLimitPrice'] = sl_limit_price
    return await self.request(
      request,
      method='POST',
      path='/api/v3/trade/modify-order',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=ModifyOrderResult,
    )
