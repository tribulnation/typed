# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.trade.order.countdown_cancel_all` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CountdownCancelAllRequest(TypedDict):
  """Countdown window to arm."""

  countdown: int
  """Seconds until auto-cancel fires if not reset. `5`-`60`, or `0` to disable the feature."""


class CountdownCancelAll(RpcEndpoint):
  """`uta.trade.order.countdown_cancel_all`."""

  async def countdown_cancel_all(
    self,
    countdown_cancel_all_request: CountdownCancelAllRequest,
    *,
    validate: bool | None = None,
  ) -> str | None:
    """Arm or disarm a "dead man's switch": if this endpoint is not called again within `countdown` seconds, every open order on the account is automatically canceled. Callers should re-arm it every few seconds during normal operation.

    Args:
      countdown_cancel_all_request: Countdown window to arm.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/trade/CountDown-Cancel-All)
    """
    _ResponseType = str | None
    adapter = validator[_ResponseType](_ResponseType)  # type: ignore
    return await self.authed_request(
      'POST',
      '/api/v3/trade/countdown-cancel-all',
      json=countdown_cancel_all_request,
      validator=adapter,
      validate=validate,
    )
