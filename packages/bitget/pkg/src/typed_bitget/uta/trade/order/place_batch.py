# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class BatchLimitOrderItem(TypedDict):
  """One limit order within a batch-place request."""

  category: Literal['SPOT', 'MARGIN', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product type."""
  symbol: str
  """Trading pair, e.g. `BTCUSDT`."""
  qty: Decimal
  """Order quantity."""
  price: Decimal
  """Order price. Required for `limit` orders."""
  side: Literal['buy', 'sell']
  """Order side."""
  orderType: Literal['limit']
  """Order type."""
  timeInForce: NotRequired[Literal['ioc', 'fok', 'gtc', 'post_only']]
  """Time-in-force policy for a limit order."""
  posSide: NotRequired[Literal['long', 'short']]
  """Position side. Required in hedge-mode futures."""
  clientOid: NotRequired[str]
  """Caller-assigned order identifier."""
  reduceOnly: NotRequired[Literal['yes', 'no']]
  """Restrict the order to only reduce an existing position. Futures only."""


class BatchMarketOrderItem(TypedDict):
  """One market order within a batch-place request."""

  category: Literal['SPOT', 'MARGIN', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product type."""
  symbol: str
  """Trading pair, e.g. `BTCUSDT`."""
  qty: Decimal
  """Order quantity."""
  side: Literal['buy', 'sell']
  """Order side."""
  orderType: Literal['market']
  """Order type."""
  timeInForce: NotRequired[Literal['ioc', 'fok', 'gtc', 'post_only']]
  """Time-in-force policy for a limit order."""
  posSide: NotRequired[Literal['long', 'short']]
  """Position side. Required in hedge-mode futures."""
  clientOid: NotRequired[str]
  """Caller-assigned order identifier."""
  reduceOnly: NotRequired[Literal['yes', 'no']]
  """Restrict the order to only reduce an existing position. Futures only."""


class BatchOrderResult(TypedDict):
  """Result of one order within a bulk cancel/close/place operation."""

  orderId: str | None
  """Venue-assigned order identifier."""
  clientOid: str
  """Client-assigned order identifier echoed back."""
  code: NotRequired[str]
  """Per-item result code; `"00000"` on success, a venue error code otherwise."""
  msg: NotRequired[str]
  """Per-item result message."""


PlaceBatchOrdersResult = list[BatchOrderResult]


Request = list[BatchLimitOrderItem | BatchMarketOrderItem]


class PlaceBatch(RpcEndpoint):
  """Place multiple orders in a single request. Each item succeeds or fails independently."""

  async def place_batch(
    self,
    place_batch_orders_request: Request,
    *,
    validate: bool | None = None,
  ) -> PlaceBatchOrdersResult:
    """Place multiple orders in a single request. Each item succeeds or fails independently.

    Args:
      place_batch_orders_request: Orders to place, one entry per order.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/trading-order-management/uta-trade-order#batch-order)
    """
    return await self.request(
      place_batch_orders_request,
      method='POST',
      path='/api/v3/trade/place-batch',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=PlaceBatchOrdersResult,
    )
