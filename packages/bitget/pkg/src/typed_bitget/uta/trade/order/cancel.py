# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.trade.order.cancel` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CancelOrderRequest(TypedDict):
  """Order to cancel."""

  orderId: NotRequired[str]
  """Client-assigned order identifier, 1-32 characters matching `^[\\.A-Z\\:/a-z0-9_-]{1,32}$`. Either `orderId` or `clientOid` must be supplied on lookups/cancels/modifies; when both are given `orderId` takes precedence."""
  clientOid: NotRequired[str]
  """Client-assigned order identifier, 1-32 characters matching `^[\\.A-Z\\:/a-z0-9_-]{1,32}$`. Either `orderId` or `clientOid` must be supplied on lookups/cancels/modifies; when both are given `orderId` takes precedence."""
  category: NotRequired[
    Literal['SPOT', 'MARGIN', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  ]
  """Product type."""


class CancelOrderResult(TypedDict):
  """Identifiers of the order the call acted on."""

  orderId: str
  """Venue-assigned order identifier."""
  clientOid: str
  """Client-assigned order identifier echoed back."""


class Cancel(RpcEndpoint):
  """`uta.trade.order.cancel`."""

  async def cancel(
    self,
    cancel_order_request: CancelOrderRequest,
    *,
    validate: bool | None = None,
  ) -> CancelOrderResult:
    """Cancel a single open order. Does not support Reality-stock pairs; use cancel_reality_order for those.

    Args:
      cancel_order_request: Order to cancel.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/trade/Cancel-Order)
    """
    return await self.authed_request(
      'POST',
      '/api/v3/trade/cancel-order',
      json=cancel_order_request,
      validator=validator(CancelOrderResult),
      validate=validate,
    )
