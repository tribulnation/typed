# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.trade.order.modify_batch` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class BatchModifyOrderItem(TypedDict):
  """One order modification within a batch-modify request."""

  orderId: NotRequired[str]
  """Client-assigned order identifier, 1-32 characters matching `^[\\.A-Z\\:/a-z0-9_-]{1,32}$`. Either `orderId` or `clientOid` must be supplied on lookups/cancels/modifies; when both are given `orderId` takes precedence."""
  clientOid: NotRequired[str]
  """Client-assigned order identifier, 1-32 characters matching `^[\\.A-Z\\:/a-z0-9_-]{1,32}$`. Either `orderId` or `clientOid` must be supplied on lookups/cancels/modifies; when both are given `orderId` takes precedence."""
  qty: NotRequired[str]
  """New order quantity."""
  price: NotRequired[str]
  """New order price."""
  autoCancel: NotRequired[Literal['yes', 'no']]
  """Auto-cancel the original order if the modification fails."""


class BatchModifyOrderResult(TypedDict):
  """Identifiers of the order the call acted on."""

  orderId: str
  """Venue-assigned order identifier."""
  clientOid: str
  """Client-assigned order identifier echoed back."""


class ModifyBatch(RpcEndpoint):
  """`uta.trade.order.modify_batch`."""

  async def modify_batch(
    self,
    list_batch_modify_order_item: list[BatchModifyOrderItem],
    *,
    validate: bool | None = None,
  ) -> list[BatchModifyOrderResult]:
    """Modify multiple open orders in a single request.

    Args:
      list_batch_modify_order_item: Order modifications, one entry per order.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/trade/Batch-Modify-Order)
    """
    return await self.authed_request(
      'POST',
      '/api/v3/trade/batch-modify-order',
      json=list_batch_modify_order_item,
      validator=validator(list[BatchModifyOrderResult]),
      validate=validate,
    )
