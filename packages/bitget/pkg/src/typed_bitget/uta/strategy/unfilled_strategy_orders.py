# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.strategy.unfilled_strategy_orders` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class StrategyOrder(TypedDict):
  """One currently-open strategy (algo/conditional) order."""

  orderId: str
  """Venue-assigned identifier of the strategy order."""
  clientOid: NotRequired[str]
  """Client-supplied order ID attached to the order."""
  category: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product type the order was placed against."""
  symbol: str
  """Symbol name, e.g. `BTCUSDT`."""
  qty: str
  """Order quantity in base coin, as a decimal string."""
  posSide: NotRequired[str]
  """Position side the order applies to."""
  status: str
  """Current status of the strategy order."""
  triggerType: NotRequired[Literal['takeProfit', 'stopLoss']]
  """Which leg of a TPSL order this row represents."""
  triggerPrice: NotRequired[str]
  """Trigger price, as a decimal string."""
  createdTime: TimestampMillis
  """Millisecond epoch timestamp the order was created, as a string."""
  updatedTime: TimestampMillis
  """Millisecond epoch timestamp the order was last updated, as a string."""


class UnfilledStrategyOrders(RpcEndpoint):
  """`uta.strategy.unfilled_strategy_orders`."""

  async def unfilled_strategy_orders(
    self,
    *,
    category: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    type: Literal['tpsl', 'trigger'] | None = None,
    validate: bool | None = None,
  ) -> list[StrategyOrder]:
    """List currently-open strategy (algo/conditional) orders.

    Args:
      category: Which futures product family to list.
      type: Restrict to a single strategy order type.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/trade/Unfilled-Strategy-Orders)
    """
    params: dict = {
      'category': category,
    }
    if type is not None:
      params['type'] = type
    return await self.authed_request(
      'GET',
      '/api/v3/trade/unfilled-strategy-orders',
      params=params,
      validator=validator(list[StrategyOrder]),
      validate=validate,
    )
