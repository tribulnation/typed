# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Parameters identifying the strategy order to cancel."""

  orderId: str
  """Venue-assigned identifier of the strategy order to cancel. Either `orderId` or `clientOid` identifies the order; `orderId` takes priority when both are supplied."""
  clientOid: NotRequired[str]
  """Client-supplied order ID of the strategy order to cancel. Either `orderId` or `clientOid` identifies the order."""


class Cancel(RpcEndpoint):
  """Cancel an existing strategy (algo/conditional) order."""

  async def cancel(
    self,
    *,
    order_id: str,
    client_oid: str | None = None,
    validate: bool | None = None,
  ) -> None:
    """Cancel an existing strategy (algo/conditional) order.

    Args:
      order_id: Venue-assigned identifier of the strategy order to cancel. Either `orderId` or `clientOid` identifies the order; `orderId` takes priority when both are supplied.
      client_oid: Client-supplied order ID of the strategy order to cancel. Either `orderId` or `clientOid` identifies the order.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/trading-strategy-trading/uta-strategy#cancel-strategy-order)
    """
    request: Request = Request(orderId=order_id)
    if client_oid is not None:
      request['clientOid'] = client_oid
    return await self.request(
      request,
      method='POST',
      path='/api/v3/trade/cancel-strategy-order',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=None,
    )
