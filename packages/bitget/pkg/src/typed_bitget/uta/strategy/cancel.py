# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.strategy.cancel` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CancelStrategyOrderRequest(TypedDict):
  """Parameters identifying the strategy order to cancel."""

  orderId: str
  """Venue-assigned identifier of the strategy order to cancel. Either `orderId` or `clientOid` identifies the order; `orderId` takes priority when both are supplied."""
  clientOid: NotRequired[str]
  """Client-supplied order ID of the strategy order to cancel. Either `orderId` or `clientOid` identifies the order."""


class Cancel(RpcEndpoint):
  """`uta.strategy.cancel`."""

  async def cancel(
    self,
    cancel_strategy_order_request: CancelStrategyOrderRequest,
    *,
    validate: bool | None = None,
  ):
    """Cancel an existing strategy (algo/conditional) order.

    Args:
      cancel_strategy_order_request: Parameters identifying the strategy order to cancel.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/strategy/Cancel-Strategy-Order)
    """
    return await self.authed_request(
      'POST',
      '/api/v3/trade/cancel-strategy-order',
      json=cancel_strategy_order_request,
      validate=validate,
    )
