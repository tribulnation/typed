# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typed_core import PaginatedResponse
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  category: Literal['SPOT', 'MARGIN', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product type the orders were placed against."""
  type: NotRequired[Literal['tpsl', 'trigger']]
  """Restrict to a single strategy order type."""
  startTime: NotRequired[TimestampMillis]
  """Start of the time range to query, as a millisecond epoch timestamp."""
  endTime: NotRequired[TimestampMillis]
  """End of the time range to query, as a millisecond epoch timestamp."""
  limit: NotRequired[int]
  """Maximum number of records to return."""
  cursor: NotRequired[str]
  """Cursor from a previous page's response; pass it to page forward. Omit to fetch the most recent records."""


class StrategyOrderV3(TypedDict):
  """One historical strategy (algo/conditional) order."""

  orderId: str
  """Venue-assigned identifier of the strategy order."""
  clientOid: str
  """Client-supplied order ID attached to the order."""
  category: Literal['SPOT', 'MARGIN', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product type the order was placed against."""
  symbol: str
  """Symbol name, e.g. `BTCUSDT`."""
  qty: Decimal
  """Order quantity in base coin, as a decimal string."""
  posSide: Literal['long', 'short'] | None
  """Position side the order applied to. `null` for a SPOT/MARGIN order -- confirmed live."""
  status: Literal['pending', 'success', 'failed', 'cancelled', 'submitting']
  """Final or current status of the strategy order."""
  triggerType: NotRequired[Literal['takeProfit', 'stopLoss']]
  """Which leg of a TPSL order this row represents."""
  tpTriggerBy: NotRequired[Literal['market', 'mark'] | None]
  """Take-profit trigger price type. `null` on a standalone trigger order (no TP/SL leg) -- confirmed live."""
  slTriggerBy: NotRequired[Literal['market', 'mark'] | None]
  """Stop-loss trigger price type. `null` on a standalone trigger order (no TP/SL leg) -- confirmed live."""
  takeProfit: NotRequired[str]
  """Take-profit trigger price, as a decimal string."""
  stopLoss: NotRequired[str]
  """Stop-loss trigger price, as a decimal string."""
  tpOrderType: NotRequired[Literal['limit', 'market'] | None]
  """Take-profit order type. `null` on a standalone trigger order (no TP/SL leg) -- confirmed live."""
  slOrderType: NotRequired[Literal['limit', 'market'] | None]
  """Stop-loss order type. `null` on a standalone trigger order (no TP/SL leg) -- confirmed live."""
  tpLimitPrice: NotRequired[str]
  """Take-profit execution price, as a decimal string."""
  slLimitPrice: NotRequired[str]
  """Stop-loss execution price, as a decimal string."""
  triggerBy: NotRequired[Literal['market', 'mark']]
  """Trigger order price type."""
  triggerPrice: NotRequired[Decimal]
  """Trigger order trigger price, as a decimal string."""
  triggerOrderType: NotRequired[Literal['limit', 'market']]
  """Trigger order execution type."""
  triggerOrderPrice: NotRequired[str]
  """Trigger order execution price, as a decimal string."""
  createdTime: TimestampMillis
  """Millisecond epoch timestamp the order was created, as a string."""
  updatedTime: TimestampMillis
  """Millisecond epoch timestamp the order was last updated, as a string."""
  type: Literal['tpsl', 'trigger']
  """Strategy order type."""
  marginMode: NotRequired[Literal['crossed', 'isolated']]
  """Margin mode for the position this order affects. Futures only."""
  ocoParams: NotRequired[dict[str, Any] | None]
  """One-cancels-the-other linkage parameters, when this order is part of an OCO pair. Real shape unconfirmed -- always `null` in every capture so far."""
  trailingStopParams: NotRequired[dict[str, Any] | None]
  """Trailing-stop parameters, when this order is a trailing-stop order. Real shape unconfirmed -- always `null` in every capture so far."""
  icebergParams: NotRequired[dict[str, Any] | None]
  """Iceberg (partially-visible) order parameters, when this order is an iceberg order. Real shape unconfirmed -- always `null` in every capture so far."""
  twapParams: NotRequired[dict[str, Any] | None]
  """TWAP (time-weighted average price) order parameters, when this order is a TWAP order. Real shape unconfirmed -- always `null` in every capture so far."""


class StrategyOrderHistoryList(TypedDict):
  """A page of historical strategy orders."""

  list: list[StrategyOrderV3]
  """Historical strategy orders, most recent first."""
  cursor: str | None
  """Cursor to pass as the `cursor` query parameter to fetch the next page. Present as explicit `null`, not omitted, on the last page — confirmed live."""


class History(RpcEndpoint):
  """Query historical strategy (algo/conditional) orders."""

  def history_paged(
    self,
    category: Literal['SPOT', 'MARGIN', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    *,
    type: Literal['tpsl', 'trigger'] | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[StrategyOrderV3, str]:
    """Query historical strategy (algo/conditional) orders.

    Paged variant of `history`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      category: Product type the orders were placed against.
      type: Restrict to a single strategy order type.
      start_time: Start of the time range to query, as a millisecond epoch timestamp.
      end_time: End of the time range to query, as a millisecond epoch timestamp.
      limit: Maximum number of records to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/trading-strategy-trading/uta-strategy#history-strategy-orders)
    """

    async def next(cursor: str) -> tuple[list[StrategyOrderV3], str | None]:
      response = await self.history(
        category,
        type=type,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('list')
      state = response.get('cursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def history(
    self,
    category: Literal['SPOT', 'MARGIN', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    *,
    type: Literal['tpsl', 'trigger'] | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> StrategyOrderHistoryList:
    """Query historical strategy (algo/conditional) orders.

    Args:
      category: Product type the orders were placed against.
      type: Restrict to a single strategy order type.
      start_time: Start of the time range to query, as a millisecond epoch timestamp.
      end_time: End of the time range to query, as a millisecond epoch timestamp.
      limit: Maximum number of records to return.
      cursor: Cursor from a previous page's response; pass it to page forward. Omit to fetch the most recent records.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/trading-strategy-trading/uta-strategy#history-strategy-orders)
    """
    request: Request = Request(category=category)
    if type is not None:
      request['type'] = type
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/api/v3/trade/history-strategy-orders',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=StrategyOrderHistoryList,
    )
