# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.transfers.transfer` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class TransferRequest(TypedDict):
  """Transfer instruction."""

  fromType: Literal[
    'spot',
    'p2p',
    'coin_futures',
    'usdt_futures',
    'usdc_futures',
    'crossed_margin',
    'isolated_margin',
    'uta',
  ]
  """Source Account type: `spot`, `p2p` (OTC), `coin_futures` (coin-margined futures), `usdt_futures` (USDT-margined futures), `usdc_futures` (USDC-margined futures), `crossed_margin`, `isolated_margin`, or `uta` (unified account)."""
  toType: Literal[
    'spot',
    'p2p',
    'coin_futures',
    'usdt_futures',
    'usdc_futures',
    'crossed_margin',
    'isolated_margin',
    'uta',
  ]
  """Destination Account type: `spot`, `p2p` (OTC), `coin_futures` (coin-margined futures), `usdt_futures` (USDT-margined futures), `usdc_futures` (USDC-margined futures), `crossed_margin`, `isolated_margin`, or `uta` (unified account)."""
  amount: str
  """Amount to transfer, as a decimal string."""
  coin: str
  """Coin to transfer, e.g. `USDT`."""
  symbol: NotRequired[str]
  """Trading pair; required when transferring to or from isolated margin, e.g. `BTCUSDT`."""
  allowBorrow: NotRequired[Literal['yes', 'no']]
  """Automatically borrow margin when the source balance is insufficient."""
  clientOid: NotRequired[str]
  """Caller-supplied idempotency id for this transfer."""


class TransferResult(TypedDict):
  """Result of a submitted transfer."""

  transferId: str
  """Id of the created transfer."""
  clientOid: NotRequired[str | None]
  """Echo of the caller-supplied `clientOid`, when one was submitted."""


class Transfer(RpcEndpoint):
  """`uta.transfers.transfer`."""

  async def transfer(
    self,
    transfer_request: TransferRequest,
    *,
    validate: bool | None = None,
  ) -> TransferResult:
    """Transfer a coin from one of the caller's own account types to another, authenticated.

    Args:
      transfer_request: Transfer instruction.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/account/transfer)
    """
    return await self.authed_request(
      'POST',
      '/api/v3/account/transfer',
      json=transfer_request,
      validator=validator(TransferResult),
      validate=validate,
    )
