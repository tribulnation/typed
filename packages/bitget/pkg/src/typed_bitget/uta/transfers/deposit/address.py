# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import DepositAddress


class Request(TypedDict):
  coin: str
  """Coin to deposit, e.g. `USDT`."""
  chain: NotRequired[str]
  """Blockchain network. Auto-matched to the coin's default chain when omitted."""
  size: NotRequired[str]
  """Deposit amount, as a decimal string. Only applies to the BTC Lightning Network; range 0.000001-0.001 BTC."""


class Address(RpcEndpoint):
  """Get the deposit address for a coin, authenticated."""

  async def address(
    self,
    *,
    coin: str,
    chain: str | None = None,
    size: str | None = None,
    validate: bool | None = None,
  ) -> DepositAddress:
    """Get the deposit address for a coin, authenticated.

    Args:
      coin: Coin to deposit, e.g. `USDT`.
      chain: Blockchain network. Auto-matched to the coin's default chain when omitted.
      size: Deposit amount, as a decimal string. Only applies to the BTC Lightning Network; range 0.000001-0.001 BTC.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/uta-account-deposit-withdrawal/deposit-withdrawal-deposit#get-deposit-address)
    """
    request: Request = Request(coin=coin)
    if chain is not None:
      request['chain'] = chain
    if size is not None:
      request['size'] = size
    return await self.request(
      request,
      method='GET',
      path='/api/v3/account/deposit-address',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=DepositAddress,
    )
