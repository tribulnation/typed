# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.transfers.deposit.records` -- generated from `spec/endpoints/`."""

from typed_core.validation import validator
from typed_bitget.core import TimestampMillis, timestamp_millis
from typed_bitget.types import DepositRecord
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Records(RpcEndpoint):
  """`uta.transfers.deposit.records`."""

  async def records(
    self,
    *,
    start_time: TimestampMillis,
    end_time: TimestampMillis,
    coin: str | None = None,
    order_id: str | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> list[DepositRecord]:
    """List deposit records, authenticated.

    Args:
      start_time: Range start, Unix milliseconds.
      end_time: Range end, Unix milliseconds.
      coin: Restrict to a single coin.
      order_id: Restrict to a single deposit order id.
      limit: Maximum entries to return per page.
      cursor: Pagination cursor: the smallest `recordId`/`orderId` from the previous page's rows, not a token returned in the response -- see `notes`.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/account/deposit/Get-Deposit-Records)
    """
    params: dict = {
      'startTime': timestamp_millis.dump(start_time),
      'endTime': timestamp_millis.dump(end_time),
    }
    if coin is not None:
      params['coin'] = coin
    if order_id is not None:
      params['orderId'] = order_id
    if limit is not None:
      params['limit'] = limit
    if cursor is not None:
      params['cursor'] = cursor
    return await self.authed_request(
      'GET',
      '/api/v3/account/deposit-records',
      params=params,
      validator=validator(list[DepositRecord]),
      validate=validate,
    )
