# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  fromType: Literal[
    'spot',
    'p2p',
    'coin_futures',
    'usdt_futures',
    'usdc_futures',
    'crossed_margin',
    'isolated_margin',
    'uta',
  ]
  """Source Account type: `spot`, `p2p` (OTC), `coin_futures` (coin-margined futures), `usdt_futures` (USDT-margined futures), `usdc_futures` (USDC-margined futures), `crossed_margin`, `isolated_margin`, or `uta` (unified account)."""
  toType: Literal[
    'spot',
    'p2p',
    'coin_futures',
    'usdt_futures',
    'usdc_futures',
    'crossed_margin',
    'isolated_margin',
    'uta',
  ]
  """Destination Account type: `spot`, `p2p` (OTC), `coin_futures` (coin-margined futures), `usdt_futures` (USDT-margined futures), `usdc_futures` (USDC-margined futures), `crossed_margin`, `isolated_margin`, or `uta` (unified account)."""


Response = list[str]


class TransferableCoins(RpcEndpoint):
  """List coins transferable from one of the caller's own account types to another, authenticated."""

  async def transferable_coins(
    self,
    *,
    from_type: Literal[
      'spot',
      'p2p',
      'coin_futures',
      'usdt_futures',
      'usdc_futures',
      'crossed_margin',
      'isolated_margin',
      'uta',
    ],
    to_type: Literal[
      'spot',
      'p2p',
      'coin_futures',
      'usdt_futures',
      'usdc_futures',
      'crossed_margin',
      'isolated_margin',
      'uta',
    ],
    validate: bool | None = None,
  ) -> Response:
    """List coins transferable from one of the caller's own account types to another, authenticated.

    Args:
      from_type: Source Account type: `spot`, `p2p` (OTC), `coin_futures` (coin-margined futures), `usdt_futures` (USDT-margined futures), `usdc_futures` (USDC-margined futures), `crossed_margin`, `isolated_margin`, or `uta` (unified account).
      to_type: Destination Account type: `spot`, `p2p` (OTC), `coin_futures` (coin-margined futures), `usdt_futures` (USDT-margined futures), `usdc_futures` (USDC-margined futures), `crossed_margin`, `isolated_margin`, or `uta` (unified account).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/uta-account-deposit-withdrawal/deposit-withdrawal-transfer#get-transferable-coins)
    """
    return await self.request(
      Request(fromType=from_type, toType=to_type),
      method='GET',
      path='/api/v3/account/transferable-coins',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
