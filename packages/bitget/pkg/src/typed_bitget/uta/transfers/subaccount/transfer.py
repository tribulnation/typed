# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Main-sub transfer instruction."""

  fromType: Literal[
    'spot',
    'p2p',
    'usdt_futures',
    'coin_futures',
    'usdc_futures',
    'crossed_margin',
    'uta',
  ]
  """Source Account type: `spot`, `p2p` (OTC), `usdt_futures`, `coin_futures`, `usdc_futures`, `crossed_margin`, or `uta` (unified account)."""
  toType: Literal[
    'spot',
    'p2p',
    'usdt_futures',
    'coin_futures',
    'usdc_futures',
    'crossed_margin',
    'uta',
  ]
  """Destination Account type: `spot`, `p2p` (OTC), `usdt_futures`, `coin_futures`, `usdc_futures`, `crossed_margin`, or `uta` (unified account)."""
  amount: str
  """Amount to transfer, as a decimal string."""
  coin: str
  """Coin to transfer, e.g. `USDT`."""
  fromUserId: str
  """UID of the transfer's source account."""
  toUserId: str
  """UID of the transfer's destination account."""
  clientOid: str
  """Caller-supplied idempotency id for this transfer."""
  allowBorrow: NotRequired[Literal['yes', 'no']]
  """Automatically borrow margin when the source balance is insufficient."""


class SubTransferResult(TypedDict):
  """Result of a submitted main-sub transfer."""

  transferId: str
  """Id of the created transfer."""
  clientOid: str
  """Echo of the caller-supplied `clientOid`."""


class Transfer(RpcEndpoint):
  """Transfer a coin between the master account and one of its sub-accounts (or between two sub-accounts), authenticated as the master account."""

  async def transfer(
    self,
    *,
    from_type: Literal[
      'spot',
      'p2p',
      'usdt_futures',
      'coin_futures',
      'usdc_futures',
      'crossed_margin',
      'uta',
    ],
    to_type: Literal[
      'spot',
      'p2p',
      'usdt_futures',
      'coin_futures',
      'usdc_futures',
      'crossed_margin',
      'uta',
    ],
    amount: str,
    coin: str,
    from_user_id: str,
    to_user_id: str,
    client_oid: str,
    allow_borrow: Literal['yes', 'no'] | None = None,
    validate: bool | None = None,
  ) -> SubTransferResult:
    """Transfer a coin between the master account and one of its sub-accounts (or between two sub-accounts), authenticated as the master account.

    Args:
      from_type: Source Account type: `spot`, `p2p` (OTC), `usdt_futures`, `coin_futures`, `usdc_futures`, `crossed_margin`, or `uta` (unified account).
      to_type: Destination Account type: `spot`, `p2p` (OTC), `usdt_futures`, `coin_futures`, `usdc_futures`, `crossed_margin`, or `uta` (unified account).
      amount: Amount to transfer, as a decimal string.
      coin: Coin to transfer, e.g. `USDT`.
      from_user_id: UID of the transfer's source account.
      to_user_id: UID of the transfer's destination account.
      client_oid: Caller-supplied idempotency id for this transfer.
      allow_borrow: Automatically borrow margin when the source balance is insufficient.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
    """
    request: Request = Request(
      fromType=from_type,
      toType=to_type,
      amount=amount,
      coin=coin,
      fromUserId=from_user_id,
      toUserId=to_user_id,
      clientOid=client_oid,
    )
    if allow_borrow is not None:
      request['allowBorrow'] = allow_borrow
    return await self.request(
      request,
      method='POST',
      path='/api/v3/account/sub-transfer',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SubTransferResult,
    )
