# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint
from typed_bitget.schemas import DepositRecord


class Request(TypedDict):
  subUid: str
  """Sub-account UID to list deposit records for."""
  coin: NotRequired[str]
  """Restrict to a single coin."""
  startTime: TimestampMillis
  """Range start, Unix milliseconds."""
  endTime: TimestampMillis
  """Range end, Unix milliseconds."""
  limit: NotRequired[int]
  """Maximum entries to return per page."""
  cursor: NotRequired[str]
  """Pagination cursor: the smallest `recordId`/`orderId` from the previous page's rows, not a token returned in the response -- see `notes`."""


Response = list[DepositRecord]


class DepositRecords(RpcEndpoint):
  """List deposit records credited to a sub-account, authenticated as the master account."""

  async def deposit_records(
    self,
    *,
    sub_uid: str,
    coin: str | None = None,
    start_time: TimestampMillis,
    end_time: TimestampMillis,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """List deposit records credited to a sub-account, authenticated as the master account.

    Args:
      sub_uid: Sub-account UID to list deposit records for.
      coin: Restrict to a single coin.
      start_time: Range start, Unix milliseconds.
      end_time: Range end, Unix milliseconds.
      limit: Maximum entries to return per page.
      cursor: Pagination cursor: the smallest `recordId`/`orderId` from the previous page's rows, not a token returned in the response -- see `notes`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
    """
    request: Request = Request(subUid=sub_uid, startTime=start_time, endTime=end_time)
    if coin is not None:
      request['coin'] = coin
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/api/v3/account/sub-deposit-records',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
