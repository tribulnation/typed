# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.transfers.subaccount.transfer_records` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_core.util import PaginatedResponse
from typed_bitget.core import TimestampMillis, timestamp_millis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SubTransferRecord(TypedDict):
  """One main-sub transfer record."""

  transferId: str
  """Transfer id."""
  oldTransferId: NotRequired[str]
  """Legacy transfer id, when this record predates the current id scheme."""
  fromType: str
  """Source account type."""
  toType: str
  """Destination account type."""
  amount: str
  """Amount transferred."""
  coin: str
  """Coin transferred."""
  fromUserId: str
  """UID of the transfer's source account."""
  toUserId: str
  """UID of the transfer's destination account."""
  status: str
  """Transfer status."""
  clientOid: str
  """Caller-supplied idempotency id."""
  createdTime: TimestampMillis
  """Creation time, Unix milliseconds as a string."""
  updatedTime: TimestampMillis
  """Last update time, Unix milliseconds as a string."""


class SubTransferRecordPage(TypedDict):
  """One page of main-sub transfer records."""

  list: list[SubTransferRecord]
  """Transfer records for this page."""
  cursor: NotRequired[str]
  """Opaque cursor for the next page. Absent or empty on the last page."""


class TransferRecords(RpcEndpoint):
  """`uta.transfers.subaccount.transfer_records`."""

  async def transfer_records(
    self,
    *,
    sub_uid: str | None = None,
    role: Literal['initiator', 'receiver'] | None = None,
    coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    client_oid: str | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> SubTransferRecordPage:
    """List transfer records between the caller's master account and its sub-accounts, authenticated.

    Args:
      sub_uid: Restrict to transfers involving a single sub-account UID.
      role: Restrict to transfers where the caller's own account acted in this role.
      coin: Restrict to a single coin.
      start_time: Restrict to transfers at or after this time, Unix milliseconds.
      end_time: Restrict to transfers at or before this time, Unix milliseconds.
      client_oid: Restrict to a single transfer by its caller-supplied idempotency id.
      limit: Maximum number of records to return per page.
      cursor: Opaque pagination cursor from a previous response. Omit for the first page.
    """
    params = {}
    if sub_uid is not None:
      params['subUid'] = sub_uid
    if role is not None:
      params['role'] = role
    if coin is not None:
      params['coin'] = coin
    if start_time is not None:
      params['startTime'] = timestamp_millis.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp_millis.dump(end_time)
    if client_oid is not None:
      params['clientOid'] = client_oid
    if limit is not None:
      params['limit'] = limit
    if cursor is not None:
      params['cursor'] = cursor
    return await self.authed_request(
      'GET',
      '/api/v3/account/sub-transfer-record',
      params=params,
      validator=validator(SubTransferRecordPage),
      validate=validate,
    )

  def transfer_records_paged(
    self,
    *,
    sub_uid: str | None = None,
    role: Literal['initiator', 'receiver'] | None = None,
    coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    client_oid: str | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[SubTransferRecord, str]:
    """Page through `transfer_records`, awaitable (flattens every page) or async-iterable
    (one page at a time).
    """

    async def next(cursor: str) -> tuple[list[SubTransferRecord], str | None]:
      response = await self.transfer_records(
        sub_uid=sub_uid,
        role=role,
        coin=coin,
        start_time=start_time,
        end_time=end_time,
        client_oid=client_oid,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('list')
      state = response.get('cursor')
      return rows, state or None

    return PaginatedResponse('', next)
