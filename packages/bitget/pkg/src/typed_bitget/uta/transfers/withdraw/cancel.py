# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict, cast
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Which withdrawal to cancel."""

  orderId: NotRequired[str]
  """Withdrawal order id to cancel. Either this or `clientOid` must be supplied; `orderId` takes precedence when both are."""
  clientOid: NotRequired[str]
  """Caller-supplied withdrawal order id to cancel."""


Response = Literal['success']


class Cancel(RpcEndpoint):
  """Cancel a withdrawal still within its preliminary review period, authenticated."""

  async def cancel(
    self,
    *,
    order_id: str | None = None,
    client_oid: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Cancel a withdrawal still within its preliminary review period, authenticated.

    Args:
      order_id: Withdrawal order id to cancel. Either this or `clientOid` must be supplied; `orderId` takes precedence when both are.
      client_oid: Caller-supplied withdrawal order id to cancel.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/uta-account-deposit-withdrawal/deposit-withdrawal-withdrawal#cancel-withdrawal)
    """
    request: Request = Request()
    if order_id is not None:
      request['orderId'] = order_id
    if client_oid is not None:
      request['clientOid'] = client_oid
    return await self.request(
      request,
      method='POST',
      path='/api/v3/account/cancel-withdrawal',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=cast(type, Response),
    )
