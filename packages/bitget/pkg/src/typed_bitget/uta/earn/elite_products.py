# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.earn.elite_products` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class EliteSubscriptionCoinItem(TypedDict):
  """Subscription terms for one coin accepted by an Elite product."""

  subscriptionCoin: str
  """Coin accepted for subscription."""
  precision: str
  """Decimal precision the subscription amount is rounded to."""
  feeRate: str
  """Subscription fee rate, as a decimal string."""
  exchangeRate: str
  """Exchange rate from `subscriptionCoin` to the product's underlying `coin`."""
  remainQuota: NotRequired[str]
  """Remaining subscribable quota for this coin."""
  minAmount: NotRequired[str]
  """Minimum subscription amount for this coin."""


class EliteProduct(TypedDict):
  """An Elite structured/subscription product."""

  productId: str
  """Product identifier."""
  coin: str
  """Underlying product coin."""
  minApr: str
  """Minimum annualized percentage rate, as a decimal string."""
  maxApr: str
  """Maximum annualized percentage rate, as a decimal string."""
  subscriptionCoinList: list[EliteSubscriptionCoinItem]
  """Coins this product accepts a subscription in, with per-coin subscription terms."""
  sellOut: Literal['YES', 'NO']
  """Whether the product is currently sold out."""


class EliteProducts(RpcEndpoint):
  """`uta.earn.elite_products`."""

  async def elite_products(self, *, validate: bool | None = None) -> list[EliteProduct]:
    """List Elite (structured/subscription) products available to the unified trading account.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/earn/Elite-Product)
    """
    return await self.authed_request(
      'GET',
      '/api/v3/earn/elite-product',
      validator=validator(list[EliteProduct]),
      validate=validate,
    )
