# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class EliteSubscribeStatus(TypedDict):
  """Settlement status of an Elite subscription order."""

  result: Literal['settled', 'pending', 'rejected']
  """Settlement outcome of the subscription order."""


class Request(TypedDict):
  orderId: str
  """Subscription order identifier, from `uta.earn.elite_subscribe`."""


class SubscribeResult(RpcEndpoint):
  """Get the settlement status of a previously placed Elite subscription order."""

  async def subscribe_result(
    self,
    order_id: str,
    *,
    validate: bool | None = None,
  ) -> EliteSubscribeStatus:
    """Get the settlement status of a previously placed Elite subscription order.

    Args:
      order_id: Subscription order identifier, from `uta.earn.elite_subscribe`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/earn-onchain-earn/onchain-earn#get-elite-subscribe-result)
    """
    return await self.request(
      Request(orderId=order_id),
      method='GET',
      path='/api/v3/earn/elite-subscribe-result',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=EliteSubscribeStatus,
    )
