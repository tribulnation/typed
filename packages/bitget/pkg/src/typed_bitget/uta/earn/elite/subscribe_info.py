# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class EliteSubscriptionCoinItem(TypedDict):
  """Subscription terms for one coin accepted by an Elite product."""

  subscriptionCoin: str
  """Coin accepted for subscription."""
  precision: int
  """Decimal precision the subscription amount is rounded to."""
  feeRate: Decimal
  """Subscription fee rate, as a decimal string."""
  exchangeRate: Decimal
  """Exchange rate from `subscriptionCoin` to the product's underlying `coin`."""
  remainQuota: Decimal
  """Remaining subscribable quota for this coin."""
  minAmount: Decimal
  """Minimum subscription amount for this coin."""


class Request(TypedDict):
  productId: str
  """Product identifier, from the product listing."""


class EliteSubscribeInfo(TypedDict):
  """Current subscription terms for an Elite product."""

  productSubId: str
  """Sub-product identifier to pass when subscribing."""
  minAmount: Decimal
  """Minimum subscription amount, as a decimal string."""
  remainQuota: Decimal
  """Remaining subscribable quota, as a decimal string."""
  exchangeRate: str
  """Exchange rate applied to a subscription placed now."""
  productCoin: str
  """Product's underlying coin."""
  interestTime: TimestampMillis
  """Time interest begins accruing on a subscription placed now, Unix millisecond timestamp."""
  settleTime: TimestampMillis
  """Next settlement time, Unix millisecond timestamp."""
  precision: int
  """Decimal precision the subscription amount is rounded to."""
  feeRate: Decimal
  """Subscription fee rate, as a decimal string."""
  subscriptionCoinList: NotRequired[list[EliteSubscriptionCoinItem]]
  """Coins currently accepted for subscription, with per-coin terms."""


class SubscribeInfo(RpcEndpoint):
  """Get the current subscription terms for one Elite product, ahead of placing a subscription."""

  async def subscribe_info(
    self,
    product_id: str,
    *,
    validate: bool | None = None,
  ) -> EliteSubscribeInfo:
    """Get the current subscription terms for one Elite product, ahead of placing a subscription.

    Args:
      product_id: Product identifier, from the product listing.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/earn-onchain-earn/onchain-earn#get-elite-subscribe-info)
    """
    return await self.request(
      Request(productId=product_id),
      method='GET',
      path='/api/v3/earn/elite-subscribe-info',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=EliteSubscribeInfo,
    )
