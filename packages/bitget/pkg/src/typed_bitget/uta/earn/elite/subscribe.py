# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class EliteSubscribeResult(TypedDict):
  """Result of subscribing to an Elite product."""

  orderId: str
  """Identifier of the created subscription order."""


class Request(TypedDict):
  """A new Elite product subscription."""

  productSubId: str
  """Sub-product identifier, from `uta.earn.elite_subscribe_info`."""
  amount: float
  """Amount to subscribe, in `coin` when set, otherwise in the product's default subscription coin."""
  coin: NotRequired[str]
  """Coin to subscribe with, when the product accepts more than one. Defaults to the product's default subscription coin."""
  paymentAccounts: NotRequired[Literal['spot', 'unified']]
  """Account to draw the subscription amount from. Wire key confirmed live (2026-08-28 UTA gap-fill capture pass) to be the plural `paymentAccounts`, not `paymentAccount` -- the singular form was rejected with ApiError(59054, 'Parameter exception paymentAccounts')."""


class Subscribe(RpcEndpoint):
  """Subscribe to an Elite product."""

  async def subscribe(
    self,
    *,
    product_sub_id: str,
    amount: float,
    coin: str | None = None,
    payment_accounts: Literal['spot', 'unified'] | None = None,
    validate: bool | None = None,
  ) -> EliteSubscribeResult:
    """Subscribe to an Elite product.

    Args:
      product_sub_id: Sub-product identifier, from `uta.earn.elite_subscribe_info`.
      amount: Amount to subscribe, in `coin` when set, otherwise in the product's default subscription coin.
      coin: Coin to subscribe with, when the product accepts more than one. Defaults to the product's default subscription coin.
      payment_accounts: Account to draw the subscription amount from. Wire key confirmed live (2026-08-28 UTA gap-fill capture pass) to be the plural `paymentAccounts`, not `paymentAccount` -- the singular form was rejected with ApiError(59054, 'Parameter exception paymentAccounts').
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/earn-onchain-earn/onchain-earn#elite-subscribe)
    """
    request: Request = Request(productSubId=product_sub_id, amount=amount)
    if coin is not None:
      request['coin'] = coin
    if payment_accounts is not None:
      request['paymentAccounts'] = payment_accounts
    return await self.request(
      request,
      method='POST',
      path='/api/v3/earn/elite-subscribe',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=EliteSubscribeResult,
    )
