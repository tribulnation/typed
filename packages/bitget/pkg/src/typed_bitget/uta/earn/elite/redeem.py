# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class EliteRedeemResult(TypedDict):
  """Result of redeeming an Elite product."""

  orderId: str
  """Identifier of the created redemption order."""


class Request(TypedDict):
  """A new Elite product redemption."""

  productId: str
  """Product identifier."""
  productSubId: str
  """Sub-product identifier, from `uta.earn.elite_redeem_info`."""
  redeemType: Literal['fast', 'standard']
  """Redemption mode to use, from the modes listed by `uta.earn.elite_redeem_info`."""
  amount: float
  """Amount to redeem."""
  receiveAccount: Literal['spot', 'unified']
  """Account to credit the redemption proceeds to."""
  advancedSettle: NotRequired[Literal['yes', 'no']]
  """Whether to opt into advanced settlement for this redemption."""
  coin: NotRequired[str]
  """Coin to receive proceeds in, when the product supports more than one."""


class Redeem(RpcEndpoint):
  """Redeem a held Elite product."""

  async def redeem(
    self,
    *,
    product_id: str,
    product_sub_id: str,
    redeem_type: Literal['fast', 'standard'],
    amount: float,
    receive_account: Literal['spot', 'unified'],
    advanced_settle: Literal['yes', 'no'] | None = None,
    coin: str | None = None,
    validate: bool | None = None,
  ) -> EliteRedeemResult:
    """Redeem a held Elite product.

    Args:
      product_id: Product identifier.
      product_sub_id: Sub-product identifier, from `uta.earn.elite_redeem_info`.
      redeem_type: Redemption mode to use, from the modes listed by `uta.earn.elite_redeem_info`.
      amount: Amount to redeem.
      receive_account: Account to credit the redemption proceeds to.
      advanced_settle: Whether to opt into advanced settlement for this redemption.
      coin: Coin to receive proceeds in, when the product supports more than one.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/earn-onchain-earn/onchain-earn#elite-redeem)
    """
    request: Request = Request(
      productId=product_id,
      productSubId=product_sub_id,
      redeemType=redeem_type,
      amount=amount,
      receiveAccount=receive_account,
    )
    if advanced_settle is not None:
      request['advancedSettle'] = advanced_settle
    if coin is not None:
      request['coin'] = coin
    return await self.request(
      request,
      method='POST',
      path='/api/v3/earn/elite-redeem',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=EliteRedeemResult,
    )
