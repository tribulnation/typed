# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.earn.elite.redeem_info` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class EliteRedeemBgusdReceiveCoin(TypedDict):
  """One BGUSD receive-coin option for a redemption."""

  bgusdReceiveCoin: str
  """Coin receivable via BGUSD conversion."""
  bgusdExchangeRate: str
  """Exchange rate applied to this BGUSD receive-coin option."""


class EliteRedeemMode(TypedDict):
  """One available redemption mode and its terms."""

  redeemFeeRate: str
  """Redemption fee rate for this mode, as a decimal string."""
  remainQuota: str
  """Remaining redeemable quota under this mode, as a decimal string."""
  redeemType: Literal['fast', 'standard']
  """Redemption mode."""
  redeemScale: str
  """Proportion of the holding redeemable under this mode, as a decimal string."""
  redeemDelayDate: str
  """Settlement delay for this mode. Unit not confirmed (docs SPA did not render); kept as a bare string rather than assumed to be a day count or timestamp."""
  minRedeemAmount: str
  """Minimum redeemable amount under this mode, as a decimal string."""
  redeemTime: TimestampMillis
  """Time this mode's terms were computed, or its settlement time, ms epoch (by analogy with the identically-shaped `redeemTime` on the classic `earn.elite_redeem_info` sibling, confirmed live there as a ms-epoch settlement time)."""


class EliteRedeemInfo(TypedDict):
  """Current redemption terms for a held Elite product."""

  productId: str
  """Product identifier."""
  productSubId: str
  """Sub-product identifier to pass when redeeming."""
  productCoin: str
  """Product's underlying coin."""
  subscriptionCoin: str
  """Coin the held position was subscribed in."""
  profitCoin: str
  """Coin accrued profit is denominated in."""
  exchangeRate: str
  """Exchange rate applied to a redemption placed now."""
  totalUnPayInterestAmount: str
  """Accrued interest not yet paid out, as a decimal string."""
  preSettleApr: str
  """Annualized percentage rate that would apply to a pre-settlement (early) redemption."""
  receivedCoin: str
  """Coin proceeds would be received in."""
  unsettledPoints: str
  """Unsettled BG point balance on this holding, as a decimal string."""
  bgusdReceiveCoinList: list[EliteRedeemBgusdReceiveCoin]
  """BGUSD-denominated receive-coin options and their exchange rates, when applicable."""
  redeemModeList: list[EliteRedeemMode]
  """Redemption modes available for this holding."""


class RedeemInfo(RpcEndpoint):
  """`uta.earn.elite.redeem_info`."""

  async def redeem_info(
    self,
    *,
    product_id: str,
    validate: bool | None = None,
  ) -> EliteRedeemInfo:
    """Get the current redemption terms for one held Elite product, ahead of placing a redemption.

    Args:
      product_id: Product identifier, from the product listing or asset holdings.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/earn/Elite-Redeem-Info)
    """
    params: dict = {
      'productId': product_id,
    }
    return await self.authed_request(
      'GET',
      '/api/v3/earn/elite-redeem-info',
      params=params,
      validator=validator(EliteRedeemInfo),
      validate=validate,
    )
