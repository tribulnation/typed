# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.earn.elite_assets` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class EliteAssetProject(TypedDict):
  """One underlying project an Elite holding is allocated to."""

  projectName: str
  """Project name."""


class EliteAsset(TypedDict):
  """The caller's holding in one Elite product."""

  productId: str
  """Product identifier."""
  productCoin: str
  """Product's underlying coin."""
  holdingAmount: str
  """Amount currently held, in `productCoin`, as a decimal string."""
  usdtHoldingAmount: str
  """Amount currently held, converted to USDT, as a decimal string."""
  exchangeRate: str
  """Exchange rate applied to this holding."""
  apr: str
  """Current annualized percentage rate, as a decimal string."""
  minApy: NotRequired[str]
  """Minimum annualized percentage yield, when the product's yield is a range."""
  maxApy: NotRequired[str]
  """Maximum annualized percentage yield, when the product's yield is a range."""
  subscriptionCoin: str
  """Coin this holding was subscribed in."""
  exchangeAmount: str
  """Amount of `subscriptionCoin` originally exchanged into this holding."""
  projectList: NotRequired[list[EliteAssetProject]]
  """Underlying yield-generating projects this holding is allocated across, when applicable."""
  unsettledBGPoints: str
  """Unsettled BG point balance accrued on this holding, as a decimal string."""
  interestCoin: str
  """Coin interest/yield is paid in."""
  totalProfit: str
  """Total profit accrued on this holding to date, as a decimal string."""


class EliteAssets(TypedDict):
  """The caller's holdings across all subscribed Elite products."""

  resultList: list[EliteAsset]
  """One entry per held Elite product."""


class EliteAssetsEndpoint(RpcEndpoint):
  """`uta.earn.elite_assets`."""

  async def elite_assets(self, *, validate: bool | None = None) -> EliteAssets:
    """List the caller's holdings across all subscribed Elite products.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/earn/Elite-Assets)
    """
    return await self.authed_request(
      'GET',
      '/api/v3/earn/elite-assets',
      validator=validator(EliteAssets),
      validate=validate,
    )
