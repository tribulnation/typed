# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.earn.elite_subscribe` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class EliteSubscribeRequest(TypedDict):
  """A new Elite product subscription."""

  productSubId: str
  """Sub-product identifier, from `uta.earn.elite_subscribe_info`."""
  amount: float
  """Amount to subscribe, in `coin` when set, otherwise in the product's default subscription coin."""
  coin: NotRequired[str]
  """Coin to subscribe with, when the product accepts more than one. Defaults to the product's default subscription coin."""
  paymentAccount: NotRequired[Literal['spot', 'unified']]
  """Account to draw the subscription amount from."""


class EliteSubscribeResult(TypedDict):
  """Result of subscribing to an Elite product."""

  orderId: str
  """Identifier of the created subscription order."""


class EliteSubscribe(RpcEndpoint):
  """`uta.earn.elite_subscribe`."""

  async def elite_subscribe(
    self,
    elite_subscribe_request: EliteSubscribeRequest,
    *,
    validate: bool | None = None,
  ) -> EliteSubscribeResult:
    """Subscribe to an Elite product.

    Args:
      elite_subscribe_request: Sub-product, amount, and payment account to subscribe with.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/earn/Elite-Subscribe)
    """
    return await self.authed_request(
      'POST',
      '/api/v3/earn/elite-subscribe',
      json=elite_subscribe_request,
      validator=validator(EliteSubscribeResult),
      validate=validate,
    )
