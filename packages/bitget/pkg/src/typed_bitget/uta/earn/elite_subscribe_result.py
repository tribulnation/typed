# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.earn.elite_subscribe_result` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class EliteSubscribeStatus(TypedDict):
  """Settlement status of an Elite subscription order."""

  result: Literal['settled', 'pending', 'rejected']
  """Settlement outcome of the subscription order."""


class EliteSubscribeResult(RpcEndpoint):
  """`uta.earn.elite_subscribe_result`."""

  async def elite_subscribe_result(
    self,
    *,
    order_id: str,
    validate: bool | None = None,
  ) -> EliteSubscribeStatus:
    """Get the settlement status of a previously placed Elite subscription order.

    Args:
      order_id: Subscription order identifier, from `uta.earn.elite_subscribe`.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/earn/Elite-Subscribe-Result)
    """
    params: dict = {
      'orderId': order_id,
    }
    return await self.authed_request(
      'GET',
      '/api/v3/earn/elite-subscribe-result',
      params=params,
      validator=validator(EliteSubscribeStatus),
      validate=validate,
    )
