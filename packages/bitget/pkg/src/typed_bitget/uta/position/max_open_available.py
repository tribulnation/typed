# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.position.max_open_available` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MaxOpenAvailable(TypedDict):
  """Maximum order size and cost currently available to open."""

  available: str
  """Balance available to fund this order."""
  maxOpen: str
  """Maximum quantity that can be opened on the evaluated side, at the evaluated price."""
  buyOpenCost: str
  """Cost required to open the maximum buy-side quantity."""
  sellOpenCost: str
  """Cost required to open the maximum sell-side quantity."""
  maxBuyOpen: str
  """Maximum quantity that can be opened on the buy side."""
  maxSellOpen: str
  """Maximum quantity that can be opened on the sell side."""


class MaxOpenAvailableRequest(TypedDict):
  """Parameters of the hypothetical order."""

  category: Literal['SPOT', 'MARGIN', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product category to evaluate open capacity for."""
  symbol: str
  """Trading symbol."""
  orderType: Literal['limit', 'market']
  """Order type to evaluate."""
  side: Literal['buy', 'sell']
  """Order side to evaluate."""
  price: NotRequired[str]
  """Limit price to evaluate against. Conditional requirement for `limit` orders not confirmed upstream; see `notes`."""
  size: NotRequired[str]
  """Order size to evaluate. Optional; omit to get the maximum unconstrained capacity."""


class MaxOpenAvailableEndpoint(RpcEndpoint):
  """`uta.position.max_open_available`."""

  async def max_open_available(
    self,
    max_open_available_request: MaxOpenAvailableRequest,
    *,
    validate: bool | None = None,
  ) -> MaxOpenAvailable:
    """Estimate the maximum order size and cost currently available to open, for a hypothetical order.

    Args:
      max_open_available_request: Hypothetical order to evaluate open capacity for.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/account/Max-Open-Available)
    """
    return await self.authed_request(
      'POST',
      '/api/v3/account/max-open-available',
      json=max_open_available_request,
      validator=validator(MaxOpenAvailable),
      validate=validate,
    )
