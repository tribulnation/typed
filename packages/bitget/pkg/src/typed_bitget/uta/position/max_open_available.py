# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MaxOpenAvailable(TypedDict):
  """Maximum order size and cost currently available to open."""

  available: Decimal
  """Balance available to fund this order."""
  maxOpen: str
  """Maximum quantity that can be opened on the evaluated side, at the evaluated price. Confirmed live: empty string on every call made so far -- see `notes`."""
  buyOpenCost: str
  """Cost required to open the maximum buy-side quantity. Confirmed live: empty string on every call made so far -- see `notes`."""
  sellOpenCost: str
  """Cost required to open the maximum sell-side quantity. Confirmed live: empty string on every call made so far -- see `notes`."""
  maxBuyOpen: Decimal
  """Maximum quantity that can be opened on the buy side."""
  maxSellOpen: Decimal
  """Maximum quantity that can be opened on the sell side."""
  maxBuyAvailable: str | None
  """Undocumented field observed live; confirmed `null` on every call made so far. Not declared upstream (see `notes`)."""
  maxSellAvailable: str | None
  """Undocumented field observed live; confirmed `null` on every call made so far. Not declared upstream (see `notes`)."""


class Request(TypedDict):
  """Parameters of the hypothetical order."""

  category: Literal['SPOT', 'MARGIN', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product category to evaluate open capacity for."""
  symbol: str
  """Trading symbol."""
  orderType: Literal['limit', 'market']
  """Order type to evaluate."""
  side: Literal['buy', 'sell']
  """Order side to evaluate."""
  price: NotRequired[str]
  """Limit price to evaluate against. Conditional requirement for `limit` orders not confirmed upstream; see `notes`."""
  size: NotRequired[str]
  """Order size to evaluate. Optional; omit to get the maximum unconstrained capacity."""


class MaxOpenAvailableEndpoint(RpcEndpoint):
  """Estimate the maximum order size and cost currently available to open, for a hypothetical order."""

  async def max_open_available(
    self,
    category: Literal['SPOT', 'MARGIN', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    *,
    symbol: str,
    order_type: Literal['limit', 'market'],
    side: Literal['buy', 'sell'],
    price: str | None = None,
    size: str | None = None,
    validate: bool | None = None,
  ) -> MaxOpenAvailable:
    """Estimate the maximum order size and cost currently available to open, for a hypothetical order.

    Args:
      category: Product category to evaluate open capacity for.
      symbol: Trading symbol.
      order_type: Order type to evaluate.
      side: Order side to evaluate.
      price: Limit price to evaluate against. Conditional requirement for `limit` orders not confirmed upstream; see `notes`.
      size: Order size to evaluate. Optional; omit to get the maximum unconstrained capacity.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/trading-position-management/uta-trade-position#get-max-open-available)
    """
    request: Request = Request(
      category=category, symbol=symbol, orderType=order_type, side=side
    )
    if price is not None:
      request['price'] = price
    if size is not None:
      request['size'] = size
    return await self.request(
      request,
      method='POST',
      path='/api/v3/account/max-open-available',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=MaxOpenAvailable,
    )
