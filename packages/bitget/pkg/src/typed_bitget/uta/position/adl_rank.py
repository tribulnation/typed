# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class PositionAdlRank(TypedDict):
  """ADL rank of one open position."""

  symbol: str
  """Trading symbol."""
  marginCoin: str
  """Coin the position's margin is denominated in."""
  adlRank: str
  """Auto-deleveraging rank tier for this position, as a string digit. Higher indicates higher priority to be auto-deleveraged during a counterparty liquidation event. No documented range was found upstream."""
  holdSide: Literal['long', 'short']
  """Position side this rank applies to."""


Response = list[PositionAdlRank]


class AdlRank(RpcEndpoint):
  """Get the auto-deleveraging (ADL) rank of every currently-open UTA futures position for the authenticated account."""

  async def adl_rank(self, *, validate: bool | None = None) -> Response:
    """Get the auto-deleveraging (ADL) rank of every currently-open UTA futures position for the authenticated account.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/trading-position-management/uta-trade-position#get-position-adl-rank)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v3/position/adlRank',
      meta={'required': True},
      validate=validate,
      response_type=Response,
    )
