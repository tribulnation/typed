# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.market.risk_reserve.hourly` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class RiskReserveHourRecord(TypedDict):
  """One hourly risk reserve balance record."""

  amount: str
  """Balance change amount for this record."""
  balance: str
  """Risk reserve balance as of this record."""
  ts: TimestampMillis
  """Record time, Unix millisecond timestamp."""


class RiskReserveHour(TypedDict):
  """Hourly risk reserve balance history."""

  coin: str
  """Coin the balance is denominated in."""
  riskReserveRecords: list[RiskReserveHourRecord]
  """Hourly balance records, most recent first."""


class Hourly(RpcEndpoint):
  """`uta.market.risk_reserve.hourly`."""

  async def hourly(
    self,
    *,
    category: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    symbol: str,
    margin_coin: str | None = None,
    validate: bool | None = None,
  ) -> RiskReserveHour:
    """Get the hourly risk reserve (insurance fund) balance history for a futures symbol.

    Args:
      category: Futures product family.
      symbol: Trading symbol, e.g. `BTCUSDT`.
      margin_coin: Restrict to a single margin coin.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/public/RiskReserveHour)
    """
    params: dict = {
      'category': category,
      'symbol': symbol,
    }
    if margin_coin is not None:
      params['marginCoin'] = margin_coin
    return await self.request(
      'GET',
      '/api/v3/market/risk-reserve-hour',
      params=params,
      validator=validator(RiskReserveHour),
      validate=validate,
    )
