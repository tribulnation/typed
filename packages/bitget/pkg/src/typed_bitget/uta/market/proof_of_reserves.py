# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class ProofOfReservesCoin(TypedDict):
  """One coin's reserve detail."""

  coin: str
  """Coin."""
  userAssets: Decimal
  """Total user-owned assets of this coin."""
  platformAssets: Decimal
  """Total platform-held assets of this coin."""
  reserveRatio: str
  """Reserve ratio for this coin."""


class ProofOfReserves(TypedDict):
  """Latest proof-of-reserves attestation."""

  merkleRootHash: str
  """Merkle root hash of the attestation."""
  totalReserveRatio: str
  """Aggregate reserve ratio across all coins."""
  list: list[ProofOfReservesCoin]
  """Per-coin reserve detail."""


class ProofOfReservesEndpoint(RpcEndpoint):
  """Get Bitget's latest proof-of-reserves attestation, including the total reserve ratio and per-coin detail."""

  async def proof_of_reserves(self, *, validate: bool | None = None) -> ProofOfReserves:
    """Get Bitget's latest proof-of-reserves attestation, including the total reserve ratio and per-coin detail.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/market-public-config/public-config-proof-of-reserves#get-proof-of-reserves)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v3/market/proof-of-reserves',
      meta={},
      validate=validate,
      response_type=ProofOfReserves,
    )
