# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class FuturesActiveBuySell(TypedDict):
  """Active buy/sell volume data points, one data point."""

  buyVolume: Decimal
  """Active (taker) buy volume for this data point."""
  sellVolume: Decimal
  """Active (taker) sell volume for this data point."""
  ts: TimestampMillis
  """Data point time, Unix millisecond timestamp."""


class Request(TypedDict):
  symbol: str
  """Trading symbol, e.g. `BTCUSDT`."""
  period: NotRequired[Literal['5m', '15m', '30m', '1h', '2h', '4h', '6h', '12h', '1d']]
  """Aggregation window."""


Response = list[FuturesActiveBuySell]


class ActiveBuySell(RpcEndpoint):
  """Get futures active (taker) buy/sell volume data for a symbol."""

  async def active_buy_sell(
    self,
    symbol: str,
    *,
    period: Literal['5m', '15m', '30m', '1h', '2h', '4h', '6h', '12h', '1d']
    | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get futures active (taker) buy/sell volume data for a symbol.

    Args:
      symbol: Trading symbol, e.g. `BTCUSDT`.
      period: Aggregation window.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/market-trading-stats/trading-stats-futures#get-futures-active-buy-sell-volume-data)
    """
    request: Request = Request(symbol=symbol)
    if period is not None:
      request['period'] = period
    return await self.request(
      request,
      method='GET',
      path='/api/v3/market/futures-active-buy-sell',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
