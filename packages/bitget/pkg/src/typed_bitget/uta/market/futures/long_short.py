# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class FuturesLongShort(TypedDict):
  """Long/short volume ratio data points, one data point."""

  longRatio: Decimal
  """Share of taker volume that was long."""
  shortRatio: Decimal
  """Share of taker volume that was short."""
  longShortRatio: Decimal
  """Ratio of long to short taker volume."""
  ts: TimestampMillis
  """Data point time, Unix millisecond timestamp."""


class Request(TypedDict):
  symbol: str
  """Trading symbol, e.g. `BTCUSDT`."""
  period: NotRequired[Literal['5m', '15m', '30m', '1h', '2h', '4h', '6h', '12h']]
  """Aggregation window."""


Response = list[FuturesLongShort]


class LongShort(RpcEndpoint):
  """Get the futures taker long/short volume ratio for a symbol."""

  async def long_short(
    self,
    symbol: str,
    *,
    period: Literal['5m', '15m', '30m', '1h', '2h', '4h', '6h', '12h'] | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get the futures taker long/short volume ratio for a symbol.

    Args:
      symbol: Trading symbol, e.g. `BTCUSDT`.
      period: Aggregation window.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/market-trading-stats/trading-stats-futures#get-futures-long-short-ratio-data)
    """
    request: Request = Request(symbol=symbol)
    if period is not None:
      request['period'] = period
    return await self.request(
      request,
      method='GET',
      path='/api/v3/market/futures-long-short',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
