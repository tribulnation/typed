# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.market.futures.long_short` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class FuturesLongShort(TypedDict):
  """Long/short volume ratio data points, one data point."""

  longRatio: str
  """Share of taker volume that was long."""
  shortRatio: str
  """Share of taker volume that was short."""
  longShortRatio: str
  """Ratio of long to short taker volume."""
  ts: TimestampMillis
  """Data point time, Unix millisecond timestamp."""


class LongShort(RpcEndpoint):
  """`uta.market.futures.long_short`."""

  async def long_short(
    self,
    *,
    symbol: str,
    period: Literal['5m', '15m', '30m', '1h', '2h', '4h', '6h', '12h'] | None = None,
    validate: bool | None = None,
  ) -> list[FuturesLongShort]:
    """Get the futures taker long/short volume ratio for a symbol.

    Args:
      symbol: Trading symbol, e.g. `BTCUSDT`.
      period: Aggregation window. Live-probed against a range of candidate values (from 1m to 30d); confirmed accepted: 5m, 15m, 30m, 1h, 2h, 4h, 6h, 12h.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/public/FuturesLongShort)
    """
    params: dict = {
      'symbol': symbol,
    }
    if period is not None:
      params['period'] = period
    return await self.request(
      'GET',
      '/api/v3/market/futures-long-short',
      params=params,
      validator=validator(list[FuturesLongShort]),
      validate=validate,
    )
