# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.market.futures.position_long_short` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class FuturesPositionLongShort(TypedDict):
  """Long/short open-position ratio data points, one data point."""

  longPositionRatio: str
  """Share of open position size that is long."""
  shortPositionRatio: str
  """Share of open position size that is short."""
  longShortPositionRatio: str
  """Ratio of long to short open position size."""
  ts: TimestampMillis
  """Data point time, Unix millisecond timestamp."""


class PositionLongShort(RpcEndpoint):
  """`uta.market.futures.position_long_short`."""

  async def position_long_short(
    self,
    *,
    symbol: str,
    period: Literal['5m', '15m', '30m', '1h', '2h', '4h', '6h', '12h'] | None = None,
    validate: bool | None = None,
  ) -> list[FuturesPositionLongShort]:
    """Get the futures active long/short open-position ratio for a symbol.

    Args:
      symbol: Trading symbol, e.g. `BTCUSDT`.
      period: Aggregation window. Live-probed against a range of candidate values (from 1m to 30d); confirmed accepted: 5m, 15m, 30m, 1h, 2h, 4h, 6h, 12h.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/public/FuturesPositionLongShort)
    """
    params: dict = {
      'symbol': symbol,
    }
    if period is not None:
      params['period'] = period
    return await self.request(
      'GET',
      '/api/v3/market/futures-position-long-short',
      params=params,
      validator=validator(list[FuturesPositionLongShort]),
      validate=validate,
    )
