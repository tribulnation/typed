# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class FuturesAccountLongShort(TypedDict):
  """Long/short account ratio data points, one data point."""

  longAccountRatio: Decimal
  """Share of accounts holding a net-long position."""
  shortAccountRatio: Decimal
  """Share of accounts holding a net-short position."""
  longShortAccountRatio: Decimal
  """Ratio of long to short accounts."""
  ts: TimestampMillis
  """Data point time, Unix millisecond timestamp."""


class Request(TypedDict):
  symbol: str
  """Trading symbol, e.g. `BTCUSDT`."""
  period: NotRequired[Literal['5m', '15m', '30m', '1h', '2h', '4h', '6h', '12h']]
  """Aggregation window."""


Response = list[FuturesAccountLongShort]


class AccountLongShort(RpcEndpoint):
  """Get the futures active long/short account ratio for a symbol."""

  async def account_long_short(
    self,
    symbol: str,
    *,
    period: Literal['5m', '15m', '30m', '1h', '2h', '4h', '6h', '12h'] | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get the futures active long/short account ratio for a symbol.

    Args:
      symbol: Trading symbol, e.g. `BTCUSDT`.
      period: Aggregation window.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/market-trading-stats/trading-stats-futures#get-futures-active-long-short-account-data)
    """
    request: Request = Request(symbol=symbol)
    if period is not None:
      request['period'] = period
    return await self.request(
      request,
      method='GET',
      path='/api/v3/market/futures-account-long-short',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
