# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.market.futures.account_long_short` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class FuturesAccountLongShort(TypedDict):
  """Long/short account ratio data points, one data point."""

  longAccountRatio: str
  """Share of accounts holding a net-long position."""
  shortAccountRatio: str
  """Share of accounts holding a net-short position."""
  longShortAccountRatio: str
  """Ratio of long to short accounts."""
  ts: TimestampMillis
  """Data point time, Unix millisecond timestamp."""


class AccountLongShort(RpcEndpoint):
  """`uta.market.futures.account_long_short`."""

  async def account_long_short(
    self,
    *,
    symbol: str,
    period: Literal['5m', '15m', '30m', '1h', '2h', '4h', '6h', '12h'] | None = None,
    validate: bool | None = None,
  ) -> list[FuturesAccountLongShort]:
    """Get the futures active long/short account ratio for a symbol.

    Args:
      symbol: Trading symbol, e.g. `BTCUSDT`.
      period: Aggregation window. Live-probed against a range of candidate values (from 1m to 30d); confirmed accepted: 5m, 15m, 30m, 1h, 2h, 4h, 6h, 12h.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/public/FuturesAccountLongShort)
    """
    params: dict = {
      'symbol': symbol,
    }
    if period is not None:
      params['period'] = period
    return await self.request(
      'GET',
      '/api/v3/market/futures-account-long-short',
      params=params,
      validator=validator(list[FuturesAccountLongShort]),
      validate=validate,
    )
