# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class DiscountRateTier(TypedDict):
  """One discount rate tier."""

  tierStartValue: Decimal
  """Collateral value at which this tier begins to apply."""
  discountRate: Decimal
  """Discount rate applied to collateral value within this tier."""


class DiscountRate(TypedDict):
  """One coin's discount rate tiers."""

  coin: str
  """Coin."""
  list: list[DiscountRateTier]
  """Discount rate tiers, in ascending order."""


Response = list[DiscountRate]


class DiscountRateEndpoint(RpcEndpoint):
  """Get the margin collateral discount rate tiers for every coin."""

  async def discount_rate(self, *, validate: bool | None = None) -> Response:
    """Get the margin collateral discount rate tiers for every coin.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/market-trading-risk-rules/trading-risk-rules-discount-rate#get-discount-rate)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v3/market/discount-rate',
      meta={},
      validate=validate,
      response_type=Response,
    )
