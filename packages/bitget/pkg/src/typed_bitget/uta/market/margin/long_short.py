# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.market.margin.long_short` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MarginLongShort(TypedDict):
  """One margin long/short ratio data point."""

  longShortRatio: str
  """Ratio of long to short margin positions."""
  ts: TimestampMillis
  """Data point time, Unix millisecond timestamp."""


class LongShort(RpcEndpoint):
  """`uta.market.margin.long_short`."""

  async def long_short(
    self,
    *,
    symbol: str,
    period: Literal['24h', '30d'] | None = None,
    coin: str | None = None,
    validate: bool | None = None,
  ) -> list[MarginLongShort]:
    """Get the margin long/short ratio for a symbol.

    Args:
      symbol: Trading symbol, e.g. `BTCUSDT`.
      period: Aggregation window.
      coin: Restrict to a single coin's borrow.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/public/MarginLongShort)
    """
    params: dict = {
      'symbol': symbol,
    }
    if period is not None:
      params['period'] = period
    if coin is not None:
      params['coin'] = coin
    return await self.request(
      'GET',
      '/api/v3/market/margin-long-short',
      params=params,
      validator=validator(list[MarginLongShort]),
      validate=validate,
    )
