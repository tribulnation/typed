# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MarginLongShort(TypedDict):
  """One margin long/short ratio data point."""

  longShortRatio: Decimal
  """Ratio of long to short margin positions."""
  ts: TimestampMillis
  """Data point time, Unix millisecond timestamp."""


class Request(TypedDict):
  symbol: str
  """Trading symbol, e.g. `BTCUSDT`."""
  period: NotRequired[Literal['24h', '30d']]
  """Aggregation window."""
  coin: NotRequired[str]
  """Coin."""


Response = list[MarginLongShort]


class LongShort(RpcEndpoint):
  """Get the margin long/short ratio for a symbol."""

  async def long_short(
    self,
    *,
    symbol: str,
    period: Literal['24h', '30d'] | None = None,
    coin: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get the margin long/short ratio for a symbol.

    Args:
      symbol: Trading symbol, e.g. `BTCUSDT`.
      period: Aggregation window.
      coin: Coin.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/market-trading-stats/trading-stats-margin#get-margin-long-short-ratio-data)
    """
    request: Request = Request(symbol=symbol)
    if period is not None:
      request['period'] = period
    if coin is not None:
      request['coin'] = coin
    return await self.request(
      request,
      method='GET',
      path='/api/v3/market/margin-long-short',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
