# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MarginIsolatedBorrow(TypedDict):
  """One isolated margin borrowing ratio data point."""

  borrowRate: Decimal
  """Isolated margin borrowing ratio for this data point."""
  ts: TimestampMillis
  """Data point time, Unix millisecond timestamp."""


class Request(TypedDict):
  symbol: str
  """Trading symbol, e.g. `BTCUSDT`."""
  period: NotRequired[Literal['24h', '30d']]
  """Aggregation window."""


Response = list[MarginIsolatedBorrow]


class IsolatedBorrow(RpcEndpoint):
  """Get the isolated margin borrowing ratio for a symbol."""

  async def isolated_borrow(
    self,
    symbol: str,
    *,
    period: Literal['24h', '30d'] | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get the isolated margin borrowing ratio for a symbol.

    Args:
      symbol: Trading symbol, e.g. `BTCUSDT`.
      period: Aggregation window.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/market-trading-stats/trading-stats-margin#get-isolated-margin-borrowing-ratio-data)
    """
    request: Request = Request(symbol=symbol)
    if period is not None:
      request['period'] = period
    return await self.request(
      request,
      method='GET',
      path='/api/v3/market/margin-isolated-borrow',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
