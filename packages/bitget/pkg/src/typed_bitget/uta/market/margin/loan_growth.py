# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MarginLoanGrowth(TypedDict):
  """One margin loan growth rate data point."""

  growthRate: Decimal
  """Loan growth rate for this data point."""
  ts: TimestampMillis
  """Data point time, Unix millisecond timestamp."""


class Request(TypedDict):
  symbol: str
  """Trading symbol, e.g. `BTCUSDT`."""
  period: NotRequired[Literal['24h', '30d']]
  """Aggregation window."""
  coin: NotRequired[str]
  """Coin."""


Response = list[MarginLoanGrowth]


class LoanGrowth(RpcEndpoint):
  """Get the margin loan growth rate for a symbol."""

  async def loan_growth(
    self,
    *,
    symbol: str,
    period: Literal['24h', '30d'] | None = None,
    coin: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get the margin loan growth rate for a symbol.

    Args:
      symbol: Trading symbol, e.g. `BTCUSDT`.
      period: Aggregation window.
      coin: Coin.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/market-trading-stats/trading-stats-margin#get-margin-loan-growth-rate-data)
    """
    request: Request = Request(symbol=symbol)
    if period is not None:
      request['period'] = period
    if coin is not None:
      request['coin'] = coin
    return await self.request(
      request,
      method='GET',
      path='/api/v3/market/margin-loan-growth',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
