# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.market.margin.loans` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MarginLoan(TypedDict):
  """A coin's margin loan interest rates and limit."""

  dailyInterest: str
  """Daily interest rate."""
  annualInterest: str
  """Annualized interest rate."""
  limit: str
  """Maximum borrowable amount."""
  masterSubLimit: str
  """Maximum borrowable amount across a master account and its sub-accounts combined."""
  platformRemaingQuota: str
  """Remaining platform-wide borrow quota for this coin. Field name matches the venue's own (misspelled) key verbatim."""


class Loans(RpcEndpoint):
  """`uta.market.margin.loans`."""

  async def loans(self, *, coin: str, validate: bool | None = None) -> MarginLoan:
    """Get margin loan interest rates and the borrow limit for a coin.

    Args:
      coin: Coin to query.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/public/MarginLoans)
    """
    params: dict = {
      'coin': coin,
    }
    return await self.request(
      'GET',
      '/api/v3/market/margin-loans',
      params=params,
      validator=validator(MarginLoan),
      validate=validate,
    )
