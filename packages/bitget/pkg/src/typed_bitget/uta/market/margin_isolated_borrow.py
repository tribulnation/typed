# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.market.margin_isolated_borrow` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MarginIsolatedBorrow(TypedDict):
  """One isolated margin borrowing ratio data point."""

  borrowRate: str
  """Isolated margin borrowing ratio for this data point."""
  ts: TimestampMillis
  """Data point time, Unix millisecond timestamp."""


class MarginIsolatedBorrowEndpoint(RpcEndpoint):
  """`uta.market.margin_isolated_borrow`."""

  async def margin_isolated_borrow(
    self,
    *,
    symbol: str,
    period: Literal['24h', '30d'] | None = None,
    validate: bool | None = None,
  ) -> list[MarginIsolatedBorrow]:
    """Get the isolated margin borrowing ratio for a symbol.

    Args:
      symbol: Trading symbol, e.g. `BTCUSDT`.
      period: Aggregation window. Live-probed against a range of candidate values; only these two were accepted (matches the sibling `margin_long_short`'s documented enum).

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/public/MarginIsolatedBorrow)
    """
    params: dict = {
      'symbol': symbol,
    }
    if period is not None:
      params['period'] = period
    return await self.request(
      'GET',
      '/api/v3/market/margin-isolated-borrow',
      params=params,
      validator=validator(list[MarginIsolatedBorrow]),
      validate=validate,
    )
