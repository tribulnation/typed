# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class ContractOi(TypedDict):
  """One symbol's open interest limit."""

  symbol: str
  """Trading symbol."""
  notionalValue: Decimal
  """Open interest notional-value cap for a single account on this symbol."""
  totalNotionalValue: Decimal
  """Open interest notional-value cap for this symbol platform-wide."""


class Request(TypedDict):
  symbol: NotRequired[str]
  """Restrict to a single symbol."""
  category: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product family."""


Response = list[ContractOi]


class OiLimit(RpcEndpoint):
  """Get the open interest limit (notional value cap) for futures symbols."""

  async def oi_limit(
    self,
    symbol: str | None = None,
    *,
    category: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    validate: bool | None = None,
  ) -> Response:
    """Get the open interest limit (notional value cap) for futures symbols.

    Args:
      symbol: Restrict to a single symbol.
      category: Product family.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/market-trading-risk-rules/trading-risk-rules-oi-limit#get-open-interest-limit)
    """
    request: Request = Request(category=category)
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='/api/v3/market/oi-limit',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
