# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  category: Literal['SPOT', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product family."""
  symbol: NotRequired[str]
  """Restrict to a single symbol."""


class Ticker(TypedDict):
  """One symbol's 24-hour ticker statistics."""

  category: Literal['SPOT', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product family."""
  symbol: str
  """Trading symbol."""
  lastPrice: Decimal
  """Last traded price."""
  openPrice24h: Decimal
  """Price 24 hours ago."""
  highPrice24h: Decimal
  """Highest price over the last 24 hours."""
  lowPrice24h: Decimal
  """Lowest price over the last 24 hours."""
  ask1Price: Decimal
  """Best ask price."""
  bid1Price: Decimal
  """Best bid price."""
  bid1Size: Decimal
  """Best bid size."""
  ask1Size: Decimal
  """Best ask size."""
  price24hPcnt: Decimal
  """24-hour price change, as a decimal fraction."""
  volume24h: Decimal
  """24-hour trading volume, in base coin."""
  turnover24h: Decimal
  """24-hour trading turnover, in quote coin."""
  indexPrice: NotRequired[Decimal]
  """Index price. Futures symbols only."""
  markPrice: NotRequired[Decimal]
  """Mark price. Futures symbols only."""
  fundingRate: NotRequired[Decimal]
  """Current funding rate. Futures symbols only."""
  openInterest: NotRequired[Decimal]
  """Open interest. Futures symbols only."""
  deliveryStartTime: NotRequired[TimestampMillis | Literal['']]
  """Delivery settlement start time, Unix millisecond timestamp. Delivery contracts only. Empty string when not applicable (confirmed live)."""
  deliveryTime: NotRequired[TimestampMillis | Literal['']]
  """Delivery time, Unix millisecond timestamp. Delivery contracts only. Empty string when not applicable (confirmed live)."""
  deliveryStatus: NotRequired[str]
  """Delivery status. Delivery contracts only. The upstream SDK documents this as a bare string with no closed set of values -- left bare per rule 2."""


Response = list[Ticker]


class Tickers(RpcEndpoint):
  """Get 24-hour ticker statistics, optionally restricted to a single symbol."""

  async def tickers(
    self,
    category: Literal['SPOT', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    *,
    symbol: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get 24-hour ticker statistics, optionally restricted to a single symbol.

    Args:
      category: Product family.
      symbol: Restrict to a single symbol.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/market-market-data/market-ticker#get-tickers)
    """
    request: Request = Request(category=category)
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='/api/v3/market/tickers',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
