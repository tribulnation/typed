# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.market.position_tier` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class PositionTier(TypedDict):
  """One position tier."""

  tier: str
  """Tier number."""
  minTierValue: str
  """Position value at which this tier begins to apply."""
  maxTierValue: str
  """Position value at which this tier stops applying."""
  leverage: str
  """Maximum leverage allowed within this tier."""
  mmr: str
  """Maintenance margin rate within this tier."""


class PositionTierEndpoint(RpcEndpoint):
  """`uta.market.position_tier`."""

  async def position_tier(
    self,
    *,
    category: Literal['MARGIN', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    symbol: str | None = None,
    coin: str | None = None,
    validate: bool | None = None,
  ) -> list[PositionTier]:
    """Get the leverage/maintenance-margin-rate position tier ladder for a symbol or coin.

    Args:
      category: Product family: cross/isolated margin or a futures family.
      symbol: Restrict to a single symbol. Futures categories.
      coin: Restrict to a single coin. Margin category.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/public/PositionTier)
    """
    params: dict = {
      'category': category,
    }
    if symbol is not None:
      params['symbol'] = symbol
    if coin is not None:
      params['coin'] = coin
    return await self.request(
      'GET',
      '/api/v3/market/position-tier',
      params=params,
      validator=validator(list[PositionTier]),
      validate=validate,
    )
