# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class PositionTier(TypedDict):
  """One position tier."""

  tier: int
  """Tier number."""
  minTierValue: Decimal
  """Position value at which this tier begins to apply."""
  maxTierValue: Decimal
  """Position value at which this tier stops applying."""
  leverage: Decimal
  """Maximum leverage allowed within this tier."""
  mmr: Decimal
  """Maintenance margin rate within this tier."""


class Request(TypedDict):
  category: Literal['MARGIN', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product family."""
  symbol: NotRequired[str]
  """Restrict to a single symbol. Futures categories."""
  coin: NotRequired[str]
  """Coin."""


Response = list[PositionTier]


class PositionTierEndpoint(RpcEndpoint):
  """Get the leverage/maintenance-margin-rate position tier ladder for a symbol or coin."""

  async def position_tier(
    self,
    category: Literal['MARGIN', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    *,
    symbol: str | None = None,
    coin: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get the leverage/maintenance-margin-rate position tier ladder for a symbol or coin.

    Args:
      category: Product family.
      symbol: Restrict to a single symbol. Futures categories.
      coin: Coin.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/market-trading-risk-rules/trading-risk-rules-position-tier#get-position-tier)
    """
    request: Request = Request(category=category)
    if symbol is not None:
      request['symbol'] = symbol
    if coin is not None:
      request['coin'] = coin
    return await self.request(
      request,
      method='GET',
      path='/api/v3/market/position-tier',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
