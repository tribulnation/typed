# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  symbol: str
  """Trading symbol, e.g. `BTCUSDT`."""


class SpotNetFlow(TypedDict):
  """One net capital inflow data point."""

  netFlow: Decimal
  """Net capital inflow for this data point (negative for net outflow)."""
  ts: TimestampMillis
  """Data point time, Unix millisecond timestamp."""


Response = list[SpotNetFlow]


class NetFlow(RpcEndpoint):
  """Get spot 24-hour net capital inflow data for a symbol."""

  async def net_flow(self, symbol: str, *, validate: bool | None = None) -> Response:
    """Get spot 24-hour net capital inflow data for a symbol.

    Args:
      symbol: Trading symbol, e.g. `BTCUSDT`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/market-trading-stats/trading-stats-spot#get-spot-24h-net-capital-inflow-data)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/api/v3/market/spot-net-flow',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
