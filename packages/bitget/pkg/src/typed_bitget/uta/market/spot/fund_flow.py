# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.market.spot.fund_flow` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SpotFundFlow(TypedDict):
  """Fund flow broken out by trader size and side, for one symbol/period."""

  whaleBuyVolume: str
  """Buy volume from whale-sized traders."""
  dolphinBuyVolume: str
  """Buy volume from dolphin-sized traders."""
  fishBuyVolume: str
  """Buy volume from fish-sized (retail) traders."""
  whaleSellVolume: str
  """Sell volume from whale-sized traders."""
  dolphinSellVolume: str
  """Sell volume from dolphin-sized traders."""
  fishSellVolume: str
  """Sell volume from fish-sized (retail) traders."""
  whaleBuyRatio: str
  """Buy volume share attributable to whale-sized traders."""
  dolphinBuyRatio: str
  """Buy volume share attributable to dolphin-sized traders."""
  fishBuyRatio: str
  """Buy volume share attributable to fish-sized (retail) traders."""
  whaleSellRatio: str
  """Sell volume share attributable to whale-sized traders."""
  dolphinSellRatio: str
  """Sell volume share attributable to dolphin-sized traders."""
  fishSellRatio: str
  """Sell volume share attributable to fish-sized (retail) traders."""


class FundFlow(RpcEndpoint):
  """`uta.market.spot.fund_flow`."""

  async def fund_flow(
    self,
    *,
    symbol: str,
    period: Literal['15m', '30m', '1h', '2h', '4h', '1d'] | None = None,
    validate: bool | None = None,
  ) -> SpotFundFlow:
    """Get spot fund flow data for a symbol, broken out by trader size (whale/dolphin/fish) and side.

    Args:
      symbol: Trading symbol, e.g. `BTCUSDT`.
      period: Aggregation window. Live-probed against a range of candidate values (from 1m to 30d); confirmed accepted: 15m, 30m, 1h, 2h, 4h, 1d.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/public/SpotFundFlow)
    """
    params: dict = {
      'symbol': symbol,
    }
    if period is not None:
      params['period'] = period
    return await self.request(
      'GET',
      '/api/v3/market/spot-fund-flow',
      params=params,
      validator=validator(SpotFundFlow),
      validate=validate,
    )
