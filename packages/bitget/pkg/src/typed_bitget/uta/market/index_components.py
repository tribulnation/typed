# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class IndexComponent(TypedDict):
  """One index price constituent."""

  exchange: str
  """Constituent exchange."""
  spotPair: str
  """Constituent spot trading pair."""
  equivalentPrice: Decimal
  """Constituent price, converted to the index's quote coin."""
  weight: Decimal
  """Constituent weight in the index."""


class Request(TypedDict):
  symbol: str
  """Trading symbol, e.g. `BTCUSDT`."""


class IndexPriceComponents(TypedDict):
  """A symbol's index price components."""

  symbol: str
  """Trading symbol."""
  componentList: list[IndexComponent]
  """Constituent spot exchanges/pairs."""


class IndexComponents(RpcEndpoint):
  """Get the index price components (constituent spot exchanges/pairs and their weights) for a symbol."""

  async def index_components(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> IndexPriceComponents:
    """Get the index price components (constituent spot exchanges/pairs and their weights) for a symbol.

    Args:
      symbol: Trading symbol, e.g. `BTCUSDT`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/market-derivatives/derivatives-index-price#get-index-price-components)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/api/v3/market/index-components',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=IndexPriceComponents,
    )
