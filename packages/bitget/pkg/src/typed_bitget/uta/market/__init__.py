# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .candles import Candles
from .cash_dividend_records import CashDividendRecordsEndpoint
from .discount_rate import DiscountRateEndpoint
from .fee_group import FeeGroup
from .fills import Fills
from .funding_rate import FundingRate
from .futures import Futures
from .index_components import IndexComponents
from .instruments import Instruments
from .liquidations import LiquidationsEndpoint
from .margin import Margin
from .oi_limit import OiLimit
from .open_interest import OpenInterestEndpoint
from .orderbook import Orderbook
from .position_tier import PositionTierEndpoint
from .proof_of_reserves import ProofOfReservesEndpoint
from .risk_reserve import RiskReserve
from .rpi import Rpi
from .score_weights import ScoreWeights
from .server_time import ServerTimeEndpoint
from .spot import Spot
from .tickers import Tickers


class Market(
  CashDividendRecordsEndpoint,
  DiscountRateEndpoint,
  FeeGroup,
  Fills,
  IndexComponents,
  Instruments,
  LiquidationsEndpoint,
  OiLimit,
  OpenInterestEndpoint,
  Orderbook,
  PositionTierEndpoint,
  ProofOfReservesEndpoint,
  ScoreWeights,
  ServerTimeEndpoint,
  Tickers,
):
  """Bitget UTA's Market API: candles, tickers, orderbook, trades, funding rate, open interest, and instrument metadata across UTA's unified spot and derivatives products.

  References:
    - [Upstream docs](https://www.bitget.com/docs/catalog/market-market-data)
  """

  @cached_property
  def candles(self) -> Candles:
    """Bitget UTA's Candlestick API: current and historical OHLCV candles across UTA's unified spot and derivatives products.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/market-market-data)
    """
    return Candles(client=self.client)

  @cached_property
  def funding_rate(self) -> FundingRate:
    """Bitget UTA's Funding Rate API: current and historical funding rate data for UTA derivatives.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/market-derivatives)
    """
    return FundingRate(client=self.client)

  @cached_property
  def futures(self) -> Futures:
    """Bitget UTA's Futures Trading Statistics API: aggregate, account-, and position-weighted long/short ratio and active buy/sell volume data for UTA futures.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/market-trading-stats)
    """
    return Futures(client=self.client)

  @cached_property
  def margin(self) -> Margin:
    """Bitget UTA's Margin Trading Statistics API: margin long/short ratio, isolated-margin borrowing ratio, and margin loan growth-rate data for UTA.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/market-trading-stats)
    """
    return Margin(client=self.client)

  @cached_property
  def risk_reserve(self) -> RiskReserve:
    """Bitget UTA's Risk Reserve API: platform-wide, daily, and hourly risk-reserve fund balance data for UTA.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/market-trading-risk-rules)
    """
    return RiskReserve(client=self.client)

  @cached_property
  def rpi(self) -> Rpi:
    """Bitget UTA's RPI (Retail Price Improvement) API: the RPI order book and RPI-eligible symbol list for UTA.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/market-market-data)
    """
    return Rpi(client=self.client)

  @cached_property
  def spot(self) -> Spot:
    """Bitget UTA's Spot Trading Statistics API: spot fund-flow, whale net-flow, and 24-hour net capital inflow data for UTA.

    References:
      - [Upstream docs](https://www.bitget.com/docs/catalog/market-trading-stats)
    """
    return Spot(client=self.client)
