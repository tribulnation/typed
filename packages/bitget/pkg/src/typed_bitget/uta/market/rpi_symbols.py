# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.market.rpi_symbols` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class RpiSymbol(TypedDict):
  """One symbol supporting RPI."""

  category: Literal['spot', 'usdt-futures', 'coin-futures', 'usdc-futures']
  """Product family. Note the lowercase spelling here, unlike this domain's other `category` fields/parameters, which use uppercase (`SPOT`, `USDT-FUTURES`, ...)."""
  symbol: str
  """Trading symbol."""


class RpiSymbols(RpcEndpoint):
  """`uta.market.rpi_symbols`."""

  async def rpi_symbols(self, *, validate: bool | None = None) -> list[RpiSymbol]:
    """List trading pairs that support Retail Price Improvement (RPI) order book depth.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/public/RpiSymbols)
    """
    return await self.request(
      'GET',
      '/api/v3/market/rpi-symbols',
      validator=validator(list[RpiSymbol]),
      validate=validate,
    )
