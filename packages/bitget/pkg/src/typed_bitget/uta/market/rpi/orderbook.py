# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.market.rpi.orderbook` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class RpiOrderBook(TypedDict):
  """RPI-annotated order book depth."""

  a: list[tuple[float, float, float]]
  """Ask (sell) side levels, best ask first."""
  b: list[tuple[float, float, float]]
  """Bid (buy) side levels, best bid first."""
  ts: TimestampMillis
  """Order book snapshot time, Unix millisecond timestamp."""


class Orderbook(RpcEndpoint):
  """`uta.market.rpi.orderbook`."""

  async def orderbook(
    self,
    *,
    category: Literal['SPOT', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    symbol: str,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> RpiOrderBook:
    """Get order book depth for a symbol, broken out into RPI and non-RPI quantity per price level.

    Args:
      category: Product family. Margin is not supported by this endpoint.
      symbol: Trading symbol, e.g. `BTCUSDT`.
      limit: Number of price levels to return per side.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/public/RpiOrderBook)
    """
    params: dict = {
      'category': category,
      'symbol': symbol,
    }
    if limit is not None:
      params['limit'] = limit
    return await self.request(
      'GET',
      '/api/v3/market/rpi-orderbook',
      params=params,
      validator=validator(RpiOrderBook),
      validate=validate,
    )
