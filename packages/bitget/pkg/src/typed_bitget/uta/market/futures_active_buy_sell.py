# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.market.futures_active_buy_sell` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class FuturesActiveBuySell(TypedDict):
  """Active buy/sell volume data points, one data point."""

  buyVolume: str
  """Active (taker) buy volume for this data point."""
  sellVolume: str
  """Active (taker) sell volume for this data point."""
  ts: TimestampMillis
  """Data point time, Unix millisecond timestamp."""


class FuturesActiveBuySellEndpoint(RpcEndpoint):
  """`uta.market.futures_active_buy_sell`."""

  async def futures_active_buy_sell(
    self,
    *,
    symbol: str,
    period: Literal['5m', '15m', '30m', '1h', '2h', '4h', '6h', '12h', '1d']
    | None = None,
    validate: bool | None = None,
  ) -> list[FuturesActiveBuySell]:
    """Get futures active (taker) buy/sell volume data for a symbol.

    Args:
      symbol: Trading symbol, e.g. `BTCUSDT`.
      period: Aggregation window. Live-probed against a range of candidate values (from 1m to 30d); confirmed accepted: 5m, 15m, 30m, 1h, 2h, 4h, 6h, 12h, 1d.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/public/FuturesActiveBuySell)
    """
    params: dict = {
      'symbol': symbol,
    }
    if period is not None:
      params['period'] = period
    return await self.request(
      'GET',
      '/api/v3/market/futures-active-buy-sell',
      params=params,
      validator=validator(list[FuturesActiveBuySell]),
      validate=validate,
    )
