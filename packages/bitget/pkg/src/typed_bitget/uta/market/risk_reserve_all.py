# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.market.risk_reserve_all` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class RiskReserveAllItem(TypedDict):
  """One coin's current risk reserve balance."""

  symbols: list[str]
  """Symbols covered by this coin's risk reserve."""
  coin: str
  """Coin the balance is denominated in."""
  balance: str
  """Current risk reserve balance."""


class RiskReserveAll(TypedDict):
  """Current risk reserve balances."""

  list: list[RiskReserveAllItem]
  """Per-coin risk reserve balances."""


class RiskReserveAllEndpoint(RpcEndpoint):
  """`uta.market.risk_reserve_all`."""

  async def risk_reserve_all(
    self,
    *,
    category: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    validate: bool | None = None,
  ) -> RiskReserveAll:
    """Get the current risk reserve (insurance fund) balance for every futures symbol in a product family.

    Args:
      category: Futures product family.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/public/RiskReserveAll)
    """
    params: dict = {
      'category': category,
    }
    return await self.request(
      'GET',
      '/api/v3/market/risk-reserve-all',
      params=params,
      validator=validator(RiskReserveAll),
      validate=validate,
    )
