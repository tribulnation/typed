# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.market.margin_loan_growth` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MarginLoanGrowth(TypedDict):
  """One margin loan growth rate data point."""

  growthRate: str
  """Loan growth rate for this data point."""
  ts: TimestampMillis
  """Data point time, Unix millisecond timestamp."""


class MarginLoanGrowthEndpoint(RpcEndpoint):
  """`uta.market.margin_loan_growth`."""

  async def margin_loan_growth(
    self,
    *,
    symbol: str,
    period: Literal['24h', '30d'] | None = None,
    coin: str | None = None,
    validate: bool | None = None,
  ) -> list[MarginLoanGrowth]:
    """Get the margin loan growth rate for a symbol.

    Args:
      symbol: Trading symbol, e.g. `BTCUSDT`.
      period: Aggregation window. Live-probed against a range of candidate values; only these two were accepted (matches the sibling `margin_long_short`'s documented enum).
      coin: Restrict to a single coin's borrow.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/public/MarginLoanGrowth)
    """
    params: dict = {
      'symbol': symbol,
    }
    if period is not None:
      params['period'] = period
    if coin is not None:
      params['coin'] = coin
    return await self.request(
      'GET',
      '/api/v3/market/margin-loan-growth',
      params=params,
      validator=validator(list[MarginLoanGrowth]),
      validate=validate,
    )
