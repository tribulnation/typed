# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CopyProfitSummary(TypedDict):
  """Cumulative profit and profit-share totals."""

  totalProfit: str
  """Cumulative profit, in USDT."""
  totalAllocatedProfit: str
  """Cumulative allocated profit share, in USDT."""
  totalPendingProfit: str
  """Cumulative pending profit share, in USDT."""


class ProfitSummary(RpcEndpoint):
  """Summarize the copy-trading lead account's cumulative profit share."""

  async def profit_summary(self, *, validate: bool | None = None) -> CopyProfitSummary:
    """Summarize the copy-trading lead account's cumulative profit share.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/copy-trading-public-private/copy-trading-public-private#get-profit-summary)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v3/copy/futures/profit-summary',
      meta={'required': True},
      validate=validate,
      response_type=CopyProfitSummary,
    )
