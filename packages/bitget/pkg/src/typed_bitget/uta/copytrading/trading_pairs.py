# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.copytrading.trading_pairs` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CopyFuturesMarginDetail(TypedDict):
  """Position-count limits for one margin coin on this trading pair."""

  marginCoin: str
  """Margin coin this limit applies to, e.g. `USDT`."""
  maxLongCount: str
  """Maximum number of concurrent long copy-trading positions allowed for this margin coin."""
  remainingLongCount: str
  """Number of additional long copy-trading positions still available for this margin coin."""
  maxShortCount: str
  """Maximum number of concurrent short copy-trading positions allowed for this margin coin."""
  remainingShortCount: str
  """Number of additional short copy-trading positions still available for this margin coin."""


class CopyFuturesTradingPair(TypedDict):
  """A futures trading pair available for copy trading."""

  symbol: str
  """Trading pair symbol, e.g. `BTCUSDT`."""
  leverage: str
  """Maximum leverage available for this pair under copy trading, as a decimal string."""
  marginDetails: list[CopyFuturesMarginDetail]
  """Per-margin-coin position-count limits for this pair."""


class TradingPairs(RpcEndpoint):
  """`uta.copytrading.trading_pairs`."""

  async def trading_pairs(
    self,
    *,
    validate: bool | None = None,
  ) -> list[CopyFuturesTradingPair]:
    """List the futures trading pairs available for copy trading in the unified trading account, with each pair's leverage and per-margin-coin position-count limits.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/copytrading/Trading-Pairs)
    """
    return await self.authed_request(
      'GET',
      '/api/v3/copy/futures/trading-pairs',
      validator=validator(list[CopyFuturesTradingPair]),
      validate=validate,
    )
