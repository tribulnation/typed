# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.copytrading.position_summary` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CopyFuturesPositionSummary(TypedDict):
  """One current copy-trading futures position held by the lead account."""

  symbol: str
  """Trading pair symbol, e.g. `BTCUSDT`."""
  holdSide: str
  """Position direction."""
  holdSize: str
  """Position size currently held, as a decimal string."""
  avgPrice: str
  """Average entry price of the position, as a decimal string."""
  leverage: str
  """Leverage applied to this position, as a decimal string."""
  marginMode: str
  """Margin mode of the position."""
  margin: str
  """Margin allocated to this position, as a decimal string."""
  markPrice: str
  """Current mark price for the symbol, as a decimal string."""
  liqPrice: str
  """Estimated liquidation price of the position, as a decimal string."""
  positionValue: str
  """Notional value of the position, as a decimal string."""
  unrealizedPnl: str
  """Unrealized profit and loss for this position, as a decimal string."""
  realizedPnl: str
  """Realized profit and loss for this position, as a decimal string."""
  roi: str
  """Return on investment for this position, as a decimal string."""


class PositionSummary(RpcEndpoint):
  """`uta.copytrading.position_summary`."""

  async def position_summary(
    self,
    *,
    validate: bool | None = None,
  ) -> list[CopyFuturesPositionSummary]:
    """Summarize the copy-trading lead account's current futures positions in the unified trading account, one entry per open position.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/copytrading/Position-Summary)
    """
    return await self.authed_request(
      'GET',
      '/api/v3/copy/futures/position-summary',
      validator=validator(list[CopyFuturesPositionSummary]),
      validate=validate,
    )
