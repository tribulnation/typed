# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CopyFuturesTransferResponse(TypedDict):
  """Result of a copy-trading transfer."""

  transferId: str
  """Identifier of the created transfer."""


class Request(TypedDict):
  """A transfer between the unified trading account and the copy-trading lead account."""

  type: Literal['in', 'out']
  """Transfer direction: `in` moves funds from the unified trading account into the copy-trading lead account, `out` moves them back."""
  coin: str
  """Coin to transfer, e.g. `USDT`."""
  amount: float
  """Amount of `coin` to transfer."""


class Transfer(RpcEndpoint):
  """Transfer funds between the unified trading account and the copy-trading lead account."""

  async def transfer(
    self,
    type: Literal['in', 'out'],
    *,
    coin: str,
    amount: float,
    validate: bool | None = None,
  ) -> CopyFuturesTransferResponse:
    """Transfer funds between the unified trading account and the copy-trading lead account.

    Args:
      type: Transfer direction: `in` moves funds from the unified trading account into the copy-trading lead account, `out` moves them back.
      coin: Coin to transfer, e.g. `USDT`.
      amount: Amount of `coin` to transfer.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/copy-trading-public-private/copy-trading-public-private#transfer)
    """
    return await self.request(
      Request(type=type, coin=coin, amount=amount),
      method='POST',
      path='/api/v3/copy/futures/transfer',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=CopyFuturesTransferResponse,
    )
