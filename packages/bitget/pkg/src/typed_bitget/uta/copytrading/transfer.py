# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.copytrading.transfer` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CopyFuturesTransferRequest(TypedDict):
  """A transfer between the unified trading account and the copy-trading lead account."""

  type: Literal['in', 'out']
  """Transfer direction: `in` moves funds from the unified trading account into the copy-trading lead account, `out` moves them back."""
  coin: str
  """Coin to transfer, e.g. `USDT`."""
  amount: float
  """Amount of `coin` to transfer."""


class CopyFuturesTransferResponse(TypedDict):
  """Result of a copy-trading transfer."""

  transferId: str
  """Identifier of the created transfer."""


class Transfer(RpcEndpoint):
  """`uta.copytrading.transfer`."""

  async def transfer(
    self,
    copy_futures_transfer_request: CopyFuturesTransferRequest,
    *,
    validate: bool | None = None,
  ) -> CopyFuturesTransferResponse:
    """Transfer funds between the unified trading account and the copy-trading lead account.

    Args:
      copy_futures_transfer_request: Transfer direction, coin, and amount.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/copytrading/Transfer)
    """
    return await self.authed_request(
      'POST',
      '/api/v3/copy/futures/transfer',
      json=copy_futures_transfer_request,
      validator=validator(CopyFuturesTransferResponse),
      validate=validate,
    )
