# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.loan.borrow_history` -- generated from `spec/endpoints/`."""

from typing_extensions import AsyncIterator, Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis, timestamp_millis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class LoanBorrowHistoryEntry(TypedDict):
  """One historical loan order."""

  orderId: str
  """Identifier of the loan order."""
  loanCoin: str
  """Borrowed coin."""
  pledgeCoin: str
  """Pledged collateral coin."""
  initPledgeAmount: str
  """Initial amount of `pledgeCoin` pledged, as a decimal string."""
  initLoanAmount: str
  """Initial amount of `loanCoin` borrowed, as a decimal string."""
  hourRate: str
  """Hourly interest rate applied to this loan, as a decimal string."""
  daily: str
  """Loan term this order was placed under."""
  borrowTime: TimestampMillis
  """Millisecond epoch timestamp the loan was created."""
  status: str
  """End state of the loan order."""


class BorrowHistory(RpcEndpoint):
  """`uta.loan.borrow_history`."""

  async def borrow_history(
    self,
    *,
    order_id: str | None = None,
    loan_coin: str | None = None,
    pledge_coin: str | None = None,
    status: Literal['ROLLBACK', 'FORCE', 'REPAY'] | None = None,
    start_time: TimestampMillis,
    end_time: TimestampMillis,
    page_num: int | None = None,
    page_size: int | None = None,
    validate: bool | None = None,
  ) -> list[LoanBorrowHistoryEntry]:
    """Query the caller's borrowing history within a time range.

    Args:
      order_id: Restrict to a single loan order.
      loan_coin: Restrict to a single borrowed coin.
      pledge_coin: Restrict to a single pledged coin.
      status: Restrict to loan orders in this end state.
      start_time: Start of the time range to query, as a millisecond epoch timestamp.
      end_time: End of the time range to query, as a millisecond epoch timestamp.
      page_num: Page number to fetch.
      page_size: Number of records per page.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/loan/Borrow-History)
    """
    params: dict = {
      'startTime': timestamp_millis.dump(start_time),
      'endTime': timestamp_millis.dump(end_time),
    }
    if order_id is not None:
      params['orderId'] = order_id
    if loan_coin is not None:
      params['loanCoin'] = loan_coin
    if pledge_coin is not None:
      params['pledgeCoin'] = pledge_coin
    if status is not None:
      params['status'] = status
    if page_num is not None:
      params['pageNum'] = page_num
    if page_size is not None:
      params['pageSize'] = page_size
    return await self.authed_request(
      'GET',
      '/api/v3/loan/borrow-history',
      params=params,
      validator=validator(list[LoanBorrowHistoryEntry]),
      validate=validate,
    )

  async def borrow_history_paged(
    self,
    *,
    order_id: str | None = None,
    loan_coin: str | None = None,
    pledge_coin: str | None = None,
    status: Literal['ROLLBACK', 'FORCE', 'REPAY'] | None = None,
    start_time: TimestampMillis,
    end_time: TimestampMillis,
    page_size: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[list[LoanBorrowHistoryEntry]]:
    """Yield successive pages of `borrow_history`.

    Requests `pageNum` from 1 upwards and stops on the first page shorter than
    `page_size`, or after `max_pages` pages when one is given.
    """
    page_num = 1
    pages = 0
    while True:
      response = await self.borrow_history(
        order_id=order_id,
        loan_coin=loan_coin,
        pledge_coin=pledge_coin,
        status=status,
        start_time=start_time,
        end_time=end_time,
        page_size=page_size,
        page_num=page_num,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      if not response or (page_size is not None and len(response) < page_size):
        break
      page_num += 1
