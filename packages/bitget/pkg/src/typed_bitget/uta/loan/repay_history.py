# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class LoanRepayHistoryEntry(TypedDict):
  """One historical loan repayment."""

  orderId: str
  """Identifier of the loan order that was repaid."""
  loanCoin: str
  """Borrowed coin that was repaid."""
  pledgeCoin: str
  """Pledged collateral coin."""
  repayAmount: str
  """Total amount repaid (principal plus interest), as a decimal string."""
  payInterest: str
  """Portion of `repayAmount` that paid interest, as a decimal string."""
  repayLoanAmount: str
  """Portion of `repayAmount` that paid down principal, as a decimal string."""
  repayUnlockAmount: str
  """Amount of `pledgeCoin` released back to the spot account, as a decimal string."""
  repayTime: TimestampMillis
  """Millisecond epoch timestamp the repayment was made."""


class Request(TypedDict):
  orderId: NotRequired[str]
  """Restrict to a single loan order."""
  loanCoin: NotRequired[str]
  """Restrict to a single borrowed coin."""
  pledgeCoin: NotRequired[str]
  """Restrict to a single pledged coin."""
  startTime: TimestampMillis
  """Start of the time range to query, as a millisecond epoch timestamp."""
  endTime: TimestampMillis
  """End of the time range to query, as a millisecond epoch timestamp."""
  pageNum: NotRequired[int]
  """Page number to fetch."""
  pageSize: NotRequired[int]
  """Number of records per page."""


Response = list[LoanRepayHistoryEntry]


class RepayHistory(RpcEndpoint):
  """Query the caller's loan repayment history within a time range."""

  async def repay_history_paged(
    self,
    *,
    order_id: str | None = None,
    loan_coin: str | None = None,
    pledge_coin: str | None = None,
    start_time: TimestampMillis,
    end_time: TimestampMillis,
    page_size: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[Response]:
    """Query the caller's loan repayment history within a time range.

    Paged variant of `repay_history`: Requests `pageNum` from 1 upwards and stops on the first page shorter than `page_size`, or after `max_pages` pages when one is given.

    Args:
      order_id: Restrict to a single loan order.
      loan_coin: Restrict to a single borrowed coin.
      pledge_coin: Restrict to a single pledged coin.
      start_time: Start of the time range to query, as a millisecond epoch timestamp.
      end_time: End of the time range to query, as a millisecond epoch timestamp.
      page_size: Number of records per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/crypto-loans-crypto-loans/crypto-loans#get-repay-history)
    """
    page_num = 1
    pages = 0
    while True:
      response = await self.repay_history(
        order_id=order_id,
        loan_coin=loan_coin,
        pledge_coin=pledge_coin,
        start_time=start_time,
        end_time=end_time,
        page_size=page_size,
        page_num=page_num,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      if not response or (page_size is not None and len(response) < page_size):
        break
      page_num += 1

  async def repay_history(
    self,
    *,
    order_id: str | None = None,
    loan_coin: str | None = None,
    pledge_coin: str | None = None,
    start_time: TimestampMillis,
    end_time: TimestampMillis,
    page_num: int | None = None,
    page_size: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Query the caller's loan repayment history within a time range.

    Args:
      order_id: Restrict to a single loan order.
      loan_coin: Restrict to a single borrowed coin.
      pledge_coin: Restrict to a single pledged coin.
      start_time: Start of the time range to query, as a millisecond epoch timestamp.
      end_time: End of the time range to query, as a millisecond epoch timestamp.
      page_num: Page number to fetch.
      page_size: Number of records per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/crypto-loans-crypto-loans/crypto-loans#get-repay-history)
    """
    request: Request = Request(startTime=start_time, endTime=end_time)
    if order_id is not None:
      request['orderId'] = order_id
    if loan_coin is not None:
      request['loanCoin'] = loan_coin
    if pledge_coin is not None:
      request['pledgeCoin'] = pledge_coin
    if page_num is not None:
      request['pageNum'] = page_num
    if page_size is not None:
      request['pageSize'] = page_size
    return await self.request(
      request,
      method='GET',
      path='/api/v3/loan/repay-history',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
