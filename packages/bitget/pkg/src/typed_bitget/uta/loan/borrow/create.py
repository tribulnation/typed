# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.loan.borrow.create` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class LoanBorrowRequest(TypedDict):
  """A crypto-collateralized borrow request."""

  loanCoin: str
  """Coin to borrow, e.g. `USDT`."""
  pledgeCoin: str
  """Coin to pledge as collateral, e.g. `BTC`."""
  daily: Literal['SEVEN', 'THIRTY', 'FLEXIBLE']
  """Loan term."""
  pledgeAmount: NotRequired[float]
  """Amount of `pledgeCoin` to pledge. Either this or `loanAmount` must be set."""
  loanAmount: NotRequired[float]
  """Amount of `loanCoin` to borrow. Either this or `pledgeAmount` must be set."""


class LoanBorrowResult(TypedDict):
  """Result of a borrow request."""

  orderId: str
  """Identifier of the created loan order."""


class Create(RpcEndpoint):
  """`uta.loan.borrow.create`."""

  async def create(
    self,
    loan_borrow_request: LoanBorrowRequest,
    *,
    validate: bool | None = None,
  ) -> LoanBorrowResult:
    """Borrow a coin against pledged collateral.

    Args:
      loan_borrow_request: Coin to borrow, coin to pledge, loan term, and either the pledge or loan amount.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/loan/Borrow)
    """
    return await self.authed_request(
      'POST',
      '/api/v3/loan/borrow',
      json=loan_borrow_request,
      validator=validator(LoanBorrowResult),
      validate=validate,
    )
