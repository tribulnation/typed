# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class LoanBorrowOngoing(TypedDict):
  """One currently open (unpaid) loan order."""

  orderId: str
  """Identifier of the loan order."""
  loanCoin: str
  """Borrowed coin."""
  loanAmount: str
  """Amount of `loanCoin` borrowed, as a decimal string."""
  interestAmount: str
  """Accrued interest so far, as a decimal string."""
  hourInterestRate: str
  """Hourly interest rate applied to this loan, as a decimal string."""
  pledgeCoin: str
  """Pledged collateral coin."""
  pledgeAmount: str
  """Amount of `pledgeCoin` pledged, as a decimal string."""
  pledgeRate: str
  """Current pledge (collateral) rate, as a decimal string."""
  supRate: str
  """Supplementary-collateral warning rate threshold, as a decimal string."""
  forceRate: str
  """Forced-liquidation rate threshold, as a decimal string."""
  borrowTime: TimestampMillis
  """Millisecond epoch timestamp the loan was created."""
  expireTime: TimestampMillis
  """Millisecond epoch timestamp the loan term expires."""


class Request(TypedDict):
  orderId: NotRequired[str]
  """Restrict to a single loan order."""
  loanCoin: NotRequired[str]
  """Restrict to a single borrowed coin."""
  pledgeCoin: NotRequired[str]
  """Restrict to a single pledged coin."""


Response = list[LoanBorrowOngoing]


class Ongoing(RpcEndpoint):
  """Query the caller's currently open (unpaid) borrowings."""

  async def ongoing(
    self,
    *,
    order_id: str | None = None,
    loan_coin: str | None = None,
    pledge_coin: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Query the caller's currently open (unpaid) borrowings.

    Args:
      order_id: Restrict to a single loan order.
      loan_coin: Restrict to a single borrowed coin.
      pledge_coin: Restrict to a single pledged coin.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/crypto-loans-crypto-loans/crypto-loans#get-borrow-ongoing)
    """
    request: Request = Request()
    if order_id is not None:
      request['orderId'] = order_id
    if loan_coin is not None:
      request['loanCoin'] = loan_coin
    if pledge_coin is not None:
      request['pledgeCoin'] = pledge_coin
    return await self.request(
      request,
      method='GET',
      path='/api/v3/loan/borrow-ongoing',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
