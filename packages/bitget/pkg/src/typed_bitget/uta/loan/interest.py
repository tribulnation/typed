# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class LoanInterest(TypedDict):
  """Estimated hourly interest and borrowable amount for a loan/pledge coin pair."""

  hourInterest: Decimal
  """Estimated hourly interest, as a decimal string."""
  loanAmount: Decimal
  """Maximum amount of `loanCoin` borrowable against the given pledge, as a decimal string."""


class Request(TypedDict):
  loanCoin: str
  """Coin to borrow, e.g. `USDT`."""
  pledgeCoin: str
  """Coin to pledge as collateral, e.g. `BTC`."""
  daily: Literal['SEVEN', 'THIRTY', 'FLEXIBLE']
  """Loan term."""
  pledgeAmount: float
  """Amount of `pledgeCoin` to pledge."""


class Interest(RpcEndpoint):
  """Get the estimated hourly interest and maximum borrowable amount for a loan/pledge coin pair, given a pledge amount."""

  async def interest(
    self,
    *,
    loan_coin: str,
    pledge_coin: str,
    daily: Literal['SEVEN', 'THIRTY', 'FLEXIBLE'],
    pledge_amount: float,
    validate: bool | None = None,
  ) -> LoanInterest:
    """Get the estimated hourly interest and maximum borrowable amount for a loan/pledge coin pair, given a pledge amount.

    Args:
      loan_coin: Coin to borrow, e.g. `USDT`.
      pledge_coin: Coin to pledge as collateral, e.g. `BTC`.
      daily: Loan term.
      pledge_amount: Amount of `pledgeCoin` to pledge.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/crypto-loans-crypto-loans/crypto-loans#get-loan-interest)
    """
    return await self.request(
      Request(
        loanCoin=loan_coin,
        pledgeCoin=pledge_coin,
        daily=daily,
        pledgeAmount=pledge_amount,
      ),
      method='GET',
      path='/api/v3/loan/interest',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=LoanInterest,
    )
