# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Target account mode to switch to."""

  mode: Literal['basic', 'advanced', 'delta', 'isolated']
  """Target account level. Basic/Advanced mode switching was documented 2026-01-23; the `delta` (delta-neutral) value was added 2026-05-12."""
  targetUid: NotRequired[str]
  """Sub-account UID to switch, when the caller is a parent account switching a sub-account's mode instead of its own."""


class AdjustAccountMode(RpcEndpoint):
  """Switch the unified account's risk level between Basic, Advanced, Isolated, and Delta-neutral mode, authenticated. Only supported for parent accounts."""

  async def adjust_account_mode(
    self,
    mode: Literal['basic', 'advanced', 'delta', 'isolated'],
    *,
    target_uid: str | None = None,
    validate: bool | None = None,
  ) -> None:
    """Switch the unified account's risk level between Basic, Advanced, Isolated, and Delta-neutral mode, authenticated. Only supported for parent accounts.

    Args:
      mode: Target account level. Basic/Advanced mode switching was documented 2026-01-23; the `delta` (delta-neutral) value was added 2026-05-12.
      target_uid: Sub-account UID to switch, when the caller is a parent account switching a sub-account's mode instead of its own.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
    """
    request: Request = Request(mode=mode)
    if target_uid is not None:
      request['targetUid'] = target_uid
    return await self.request(
      request,
      method='POST',
      path='/api/v3/account/adjust-account-mode',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=None,
    )
