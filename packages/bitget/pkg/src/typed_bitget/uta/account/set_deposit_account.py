# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Default deposit account to configure."""

  coin: str
  """Coin to configure, e.g. `BTC`."""
  accountType: Literal['funding', 'unified', 'otc']
  """Destination account for this coin's future deposits. Defaults to `funding` until configured."""


Response = str


class SetDepositAccount(RpcEndpoint):
  """Set the default account (funding, unified, or OTC) that a coin's deposits are credited to, authenticated. Once set, the configuration persists indefinitely; there is no need to reconfigure it on later deposits."""

  async def set_deposit_account(
    self,
    coin: str,
    *,
    account_type: Literal['funding', 'unified', 'otc'],
    validate: bool | None = None,
  ) -> Response:
    """Set the default account (funding, unified, or OTC) that a coin's deposits are credited to, authenticated. Once set, the configuration persists indefinitely; there is no need to reconfigure it on later deposits.

    Args:
      coin: Coin to configure, e.g. `BTC`.
      account_type: Destination account for this coin's future deposits. Defaults to `funding` until configured.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/uta-account-deposit-withdrawal/deposit-withdrawal-deposit#set-up-deposit-account)
    """
    return await self.request(
      Request(coin=coin, accountType=account_type),
      method='POST',
      path='/api/v3/account/deposit-account',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
