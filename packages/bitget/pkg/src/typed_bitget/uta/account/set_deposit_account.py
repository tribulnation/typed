# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.account.set_deposit_account` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SetDepositAccountRequest(TypedDict):
  """Default deposit account to configure."""

  coin: str
  """Coin to configure, e.g. `BTC`."""
  accountType: Literal['funding', 'unified', 'otc']
  """Destination account for this coin's future deposits. Defaults to `funding` until configured."""


class SetDepositAccount(RpcEndpoint):
  """`uta.account.set_deposit_account`."""

  async def set_deposit_account(
    self,
    set_deposit_account_request: SetDepositAccountRequest,
    *,
    validate: bool | None = None,
  ) -> str:
    """Set the default account (funding, unified, or OTC) that a coin's deposits are credited to, authenticated. Once set, the configuration persists indefinitely; there is no need to reconfigure it on later deposits.

    Args:
      set_deposit_account_request: Default deposit account.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/account/Deposit-Account)
    """
    return await self.authed_request(
      'POST',
      '/api/v3/account/deposit-account',
      json=set_deposit_account_request,
      validator=validator(str),
      validate=validate,
    )
