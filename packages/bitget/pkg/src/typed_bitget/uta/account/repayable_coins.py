# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.account.repayable_coins` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class RepayableCoin(TypedDict):
  """One coin available for repayment."""

  coin: str
  """Coin name."""
  size: str
  """Outstanding size owed of this coin."""
  amount: str
  """Outstanding value owed of this coin, in USD."""


class RepayableCoins(TypedDict):
  """Coins available for repayment."""

  repayableCoinList: list[RepayableCoin]
  """Coins currently owed."""
  maxSelection: str
  """Maximum number of coins that may be selected in one `uta.account.repay` call."""


class RepayableCoinsEndpoint(RpcEndpoint):
  """`uta.account.repayable_coins`."""

  async def repayable_coins(self, *, validate: bool | None = None) -> RepayableCoins:
    """List the coins the unified account currently owes and can repay, authenticated."""
    return await self.authed_request(
      'GET',
      '/api/v3/account/repayable-coins',
      validator=validator(RepayableCoins),
      validate=validate,
    )
