# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class FundingAsset(TypedDict):
  """One coin balance held in the funding account."""

  coin: str
  """Coin name."""
  available: Decimal
  """Balance available for trading, transfer, or withdrawal."""
  frozen: Decimal
  """Balance frozen in a pending order or hold."""
  balance: Decimal
  """Total balance of this coin (available plus frozen)."""


class Request(TypedDict):
  coin: NotRequired[str]
  """Restrict to a single coin, e.g. `USDT`. Omit to list every coin."""


Response = list[FundingAsset]


class FundingAssets(RpcEndpoint):
  """List the funding (spot wallet) account balances, authenticated."""

  async def funding_assets(
    self,
    coin: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """List the funding (spot wallet) account balances, authenticated.

    Args:
      coin: Restrict to a single coin, e.g. `USDT`. Omit to list every coin.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
    """
    request: Request = Request()
    if coin is not None:
      request['coin'] = coin
    return await self.request(
      request,
      method='GET',
      path='/api/v3/account/funding-assets',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
