# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class AccountAsset(TypedDict):
  """One coin balance held by the unified account."""

  coin: str
  """Coin name, e.g. `USDT`."""
  equity: Decimal
  """This coin's equity."""
  usdValue: Decimal
  """This coin's equity, in USD."""
  balance: Decimal
  """Total balance of this coin."""
  available: Decimal
  """Balance available for trading or withdrawal."""
  debt: Decimal
  """Outstanding borrowed amount for this coin."""
  locked: Decimal
  """Balance locked in open orders or other holds."""
  bonus: Decimal
  """USDT bonus amount held for this coin."""


class AccountAssets(TypedDict):
  """The unified account's overall equity, margin, and per-coin balances."""

  accountEquity: Decimal
  """Total account equity, in USD."""
  usdtEquity: Decimal
  """Total account equity, in USDT."""
  btcEquity: Decimal
  """Total account equity, in BTC."""
  unrealisedPnl: Decimal
  """Unrealized profit and loss across open positions, in USD."""
  usdtUnrealisedPnl: Decimal
  """Unrealized profit and loss across open positions, in USDT."""
  btcUnrealizedPnl: Decimal
  """Unrealized profit and loss across open positions, in BTC."""
  effEquity: Decimal
  """Effective equity usable as margin, in USD."""
  mmr: Decimal
  """Maintenance margin requirement across open positions, in USD."""
  imr: Decimal
  """Initial margin requirement across open positions, in USD."""
  mgnRatio: Decimal
  """Overall account margin ratio."""
  positionMgnRatio: Decimal
  """Position-level maintenance margin ratio."""
  positionValue: Decimal
  """Total position value across the account, in USD."""
  leverage: Decimal
  """Overall account leverage."""
  assets: list[AccountAsset]
  """Per-coin asset balances; coins with a zero balance are omitted."""


class Assets(RpcEndpoint):
  """Get the unified account overall equity, margin metrics, and per-coin asset balances, authenticated. Only coins with a non-zero balance are returned."""

  async def assets(self, *, validate: bool | None = None) -> AccountAssets:
    """Get the unified account overall equity, margin metrics, and per-coin asset balances, authenticated. Only coins with a non-zero balance are returned.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/uta-account-assets-balance/assets-balance-assets#get-account-assets)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v3/account/assets',
      meta={'required': True},
      validate=validate,
      response_type=AccountAssets,
    )
