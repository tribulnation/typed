# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class OpenInterestLimit(TypedDict):
  """Open-interest limits for the queried symbol."""

  symbol: str
  """Trading symbol queried."""
  singleUserLimit: Decimal
  """Maximum open interest allowed for a single (non-market-maker) account."""
  masterSubLimit: Decimal
  """Maximum combined open interest allowed across a master account and its sub-accounts."""
  marketMakerLimit: Decimal
  """Maximum open interest allowed for a market-maker account."""


class Request(TypedDict):
  symbol: str
  """Trading symbol to query."""
  category: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Futures product type the symbol belongs to."""


class OpenInterestLimitEndpoint(RpcEndpoint):
  """Get the account's open-interest limits for a futures symbol, authenticated."""

  async def open_interest_limit(
    self,
    symbol: str,
    *,
    category: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    validate: bool | None = None,
  ) -> OpenInterestLimit:
    """Get the account's open-interest limits for a futures symbol, authenticated.

    Args:
      symbol: Trading symbol to query.
      category: Futures product type the symbol belongs to.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
    """
    return await self.request(
      Request(symbol=symbol, category=category),
      method='GET',
      path='/api/v3/account/open-interest-limit',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=OpenInterestLimit,
    )
