# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.account.delta_info` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class AccountDeltaCoinPosition(TypedDict):
  """One coin's net position ratio under delta-neutral mode."""

  coin: str
  """Coin name."""
  positionRatio: str
  """This coin's net position ratio."""


class AccountDeltaInfo(TypedDict):
  """Delta-neutral risk metrics for the account."""

  deltaEquityRatio: str
  """The account's delta equity ratio."""
  deltaThreshold: str
  """Delta ratio threshold configured for the account."""
  positionThreshold: str
  """Per-position ratio threshold configured for the account."""
  list: list[AccountDeltaCoinPosition]
  """Per-coin net position ratios."""


class DeltaInfo(RpcEndpoint):
  """`uta.account.delta_info`."""

  async def delta_info(self, *, validate: bool | None = None) -> AccountDeltaInfo:
    """Get delta-neutral mode metrics: equity ratio, risk thresholds, and per-coin net position ratios, authenticated. Only meaningful when the account is in delta-neutral mode (see `uta.account.adjust_account_mode`)."""
    return await self.authed_request(
      'GET',
      '/api/v3/account/delta-info',
      validator=validator(AccountDeltaInfo),
      validate=validate,
    )
