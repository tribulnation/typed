# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.account.eligible_margin_tier` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class EligibleMarginTier(TypedDict):
  """One margin tier."""

  tier: str
  """Tier level, as a decimal string."""
  minTierValue: str
  """Minimum position value for this tier."""
  maxTierValue: str
  """Maximum position value for this tier."""
  leverage: str
  """Effective leverage at this tier."""
  mmr: str
  """Maintenance margin rate at this tier, as a decimal fraction."""


class EligibleMarginTierCoin(TypedDict):
  """One coin's margin tier schedule."""

  coin: str
  """Coin name."""
  tiers: list[EligibleMarginTier]
  """Tier list, ordered by increasing position value."""


class EligibleMarginTierEndpoint(RpcEndpoint):
  """`uta.account.eligible_margin_tier`."""

  async def eligible_margin_tier(
    self,
    *,
    coin: str | None = None,
    validate: bool | None = None,
  ) -> list[EligibleMarginTierCoin]:
    """Get the caller's margin tier schedule (leverage and maintenance-margin-rate by position-value tier), per coin.

    Args:
      coin: Restrict to one coin, e.g. `BTC`.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/account/Get-Eligible-Margin-Tier)
    """
    params = {}
    if coin is not None:
      params['coin'] = coin
    return await self.authed_request(
      'GET',
      '/api/v3/account/eligible-margin-tier',
      params=params,
      validator=validator(list[EligibleMarginTierCoin]),
      validate=validate,
    )
