# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MaxWithdrawal(TypedDict):
  """Maximum withdrawable amounts by source account."""

  coin: str
  """Coin queried."""
  otcMaxWithdrawal: Decimal
  """Maximum withdrawable amount from the OTC account."""
  spotMaxWithdrawal: Decimal
  """Maximum withdrawable amount from the spot account."""
  utaMaxWithdrawal: Decimal
  """Maximum withdrawable amount from the unified trading account."""
  totalMaxWithdrawal: Decimal
  """Maximum withdrawable amount across all sub-accounts combined."""


class Request(TypedDict):
  coin: str
  """Coin to query."""


class MaxWithdrawalEndpoint(RpcEndpoint):
  """Get the maximum withdrawable amount of a coin from the unified account, split by source sub-account, authenticated."""

  async def max_withdrawal(
    self, coin: str, *, validate: bool | None = None
  ) -> MaxWithdrawal:
    """Get the maximum withdrawable amount of a coin from the unified account, split by source sub-account, authenticated.

    Args:
      coin: Coin to query.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
    """
    return await self.request(
      Request(coin=coin),
      method='GET',
      path='/api/v3/account/max-withdrawal',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=MaxWithdrawal,
    )
