# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Target hold mode."""

  holdMode: Literal['one_way_mode', 'hedge_mode']
  """Position-holding mode: single-direction (`one_way_mode`) or long/short hedge (`hedge_mode`)."""


Response = str


class SetHoldMode(RpcEndpoint):
  """Set the futures position-holding mode for the unified account, authenticated."""

  async def set_hold_mode(
    self,
    hold_mode: Literal['one_way_mode', 'hedge_mode'],
    *,
    validate: bool | None = None,
  ) -> Response:
    """Set the futures position-holding mode for the unified account, authenticated.

    Args:
      hold_mode: Position-holding mode: single-direction (`one_way_mode`) or long/short hedge (`hedge_mode`).
      validate: Override this call's response validation; falls back to the client-level default when omitted.
    """
    return await self.request(
      Request(holdMode=hold_mode),
      method='POST',
      path='/api/v3/account/set-hold-mode',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
