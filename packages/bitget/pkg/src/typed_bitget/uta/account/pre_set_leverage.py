# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.account.pre_set_leverage` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class PreSetLeverage(TypedDict):
  """Estimated effect of the previewed leverage change."""

  estMaxOpen: NotRequired[str]
  """Estimated maximum position size openable at the previewed leverage."""
  estMaxBorrowable: NotRequired[str | None]
  """Estimated maximum borrowable amount at the previewed leverage. Confirmed live: `null` for a futures preview, where borrowing doesn't apply."""
  requiredMargin: str
  """Margin required at the previewed leverage."""
  marginChange: str
  """Change in required margin relative to the current leverage."""


class PreSetLeverageEndpoint(RpcEndpoint):
  """`uta.account.pre_set_leverage`."""

  async def pre_set_leverage(
    self,
    *,
    category: Literal['MARGIN', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    symbol: str | None = None,
    coin: str | None = None,
    margin_mode: Literal['isolated', 'cross'],
    leverage: str | None = None,
    long_leverage: str | None = None,
    short_leverage: str | None = None,
    validate: bool | None = None,
  ) -> PreSetLeverage:
    """Preview the effect of a leverage change -- estimated max openable size, max borrowable amount, and required margin -- without applying it, authenticated.

    Args:
      category: Product type to preview leverage for.
      symbol: Trading symbol. Required for a futures preview.
      coin: Coin name. Required for a margin preview.
      margin_mode: Margin mode to preview.
      leverage: Leverage multiplier to preview. Not used when `longLeverage`/`shortLeverage` are set.
      long_leverage: Long-side leverage to preview, isolated two-way mode only.
      short_leverage: Short-side leverage to preview, isolated two-way mode only.
    """
    params: dict = {
      'category': category,
      'marginMode': margin_mode,
    }
    if symbol is not None:
      params['symbol'] = symbol
    if coin is not None:
      params['coin'] = coin
    if leverage is not None:
      params['leverage'] = leverage
    if long_leverage is not None:
      params['longLeverage'] = long_leverage
    if short_leverage is not None:
      params['shortLeverage'] = short_leverage
    return await self.authed_request(
      'GET',
      '/api/v3/account/pre-set-leverage',
      params=params,
      validator=validator(PreSetLeverage),
      validate=validate,
    )
