# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CustomCollateralCoin(TypedDict):
  """One coin eligible for custom collateral configuration."""

  collateralCoin: str
  """Coin name."""


Response = list[CustomCollateralCoin]


class CustomCoins(RpcEndpoint):
  """List the platform-supported coins eligible for custom collateral configuration."""

  async def custom_coins(self, *, validate: bool | None = None) -> Response:
    """List the platform-supported coins eligible for custom collateral configuration.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v3/account/custom-collateral-coins',
      meta={'required': True},
      validate=validate,
      response_type=Response,
    )
