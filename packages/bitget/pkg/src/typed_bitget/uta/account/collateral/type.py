# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CollateralTypeConfig(TypedDict):
  """The account's collateral-type configuration."""

  collateralType: Literal['mainstream', 'all', 'custom']
  """Which coins the account accepts as collateral: `mainstream` coins only, `all` supported coins, or a `custom` list."""
  collateralCoins: NotRequired[str]
  """Comma-separated custom collateral coin list. Only returned when `collateralType` is `custom`."""


class Type(RpcEndpoint):
  """Get the unified account's collateral configuration, authenticated."""

  async def type(self, *, validate: bool | None = None) -> CollateralTypeConfig:
    """Get the unified account's collateral configuration, authenticated.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v3/account/collateral-type',
      meta={'required': True},
      validate=validate,
      response_type=CollateralTypeConfig,
    )
