# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Collateral-type configuration to apply."""

  collateralType: Literal['mainstream', 'all', 'custom']
  """Which coins to accept as collateral."""
  collateralCoins: NotRequired[str]
  """Comma-separated coin list. Required when `collateralType` is `custom`."""


Response = str


class SetType(RpcEndpoint):
  """Configure the unified account's collateral type, authenticated."""

  async def set_type(
    self,
    collateral_type: Literal['mainstream', 'all', 'custom'],
    *,
    collateral_coins: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Configure the unified account's collateral type, authenticated.

    Args:
      collateral_type: Which coins to accept as collateral.
      collateral_coins: Comma-separated coin list. Required when `collateralType` is `custom`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
    """
    request: Request = Request(collateralType=collateral_type)
    if collateral_coins is not None:
      request['collateralCoins'] = collateral_coins
    return await self.request(
      request,
      method='POST',
      path='/api/v3/account/set-collateral-type',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
