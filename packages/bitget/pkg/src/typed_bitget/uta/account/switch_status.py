# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.account.switch_status` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SwitchStatus(TypedDict):
  """Status of the pending switch."""

  status: Literal['processProcessing', 'successSuccess', 'failFailed']
  """Status of the pending account-mode switch. Values as documented upstream -- the doubled wording (`processProcessing`, etc.) is preserved as-is rather than guessed at, pending live confirmation."""


class SwitchStatusEndpoint(RpcEndpoint):
  """`uta.account.switch_status`."""

  async def switch_status(self, *, validate: bool | None = None) -> SwitchStatus:
    """Get the status of a pending Classic-mode switch requested via `uta.account.switch_to_classic`, authenticated. Only supported for parent accounts."""
    return await self.authed_request(
      'GET',
      '/api/v3/account/switch-status',
      validator=validator(SwitchStatus),
      validate=validate,
    )
