# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class RealityOrderBook(TypedDict):
  """One orderbook snapshot."""

  symbol: str
  """Trading pair."""
  a: list[tuple[str, str]]
  """Sell (ask) levels, sorted ascending by price."""
  b: list[tuple[str, str]]
  """Buy (bid) levels, sorted descending by price."""
  ts: TimestampMillis
  """Snapshot time."""


class Request(TypedDict):
  symbol: str
  """Reality trading pair, e.g. `RAAPLUSDT`."""


class RealityOrderbook(RpcEndpoint):
  """Get an orderbook snapshot for a Reality (tokenized-stock) trading pair."""

  async def reality_orderbook(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> RealityOrderBook:
    """Get an orderbook snapshot for a Reality (tokenized-stock) trading pair.

    Args:
      symbol: Reality trading pair, e.g. `RAAPLUSDT`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/reality-market-data/reality-market-data#get-reality-orderbook)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/api/v3/account/reality-orderbook',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=RealityOrderBook,
    )
