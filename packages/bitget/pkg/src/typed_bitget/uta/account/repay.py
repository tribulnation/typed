# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class RepayResponse(TypedDict):
  """Outcome of the repayment."""

  result: str
  """Repayment outcome. Left bare: only 'YES' observed live (2026-08-28 UTA gap-fill capture pass, a real DOGE-debt repayment), no doc confirmation of a full closed set (e.g. a 'NO'/failure value)."""
  repayAmount: Decimal
  """Total amount repaid, in USD."""


class Request(TypedDict):
  """Coins to repay and to pay with."""

  repayableCoinList: list[str]
  """Coins to repay, from `uta.account.repayable_coins`."""
  paymentCoinList: list[str]
  """Coins to pay with, from `uta.account.payment_coins`."""


class Repay(RpcEndpoint):
  """Repay unified-account debt, drawing from the given payment coins, authenticated."""

  async def repay(
    self,
    *,
    repayable_coin_list: list[str],
    payment_coin_list: list[str],
    validate: bool | None = None,
  ) -> RepayResponse:
    """Repay unified-account debt, drawing from the given payment coins, authenticated.

    Args:
      repayable_coin_list: Coins to repay, from `uta.account.repayable_coins`.
      payment_coin_list: Coins to pay with, from `uta.account.payment_coins`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
    """
    return await self.request(
      Request(repayableCoinList=repayable_coin_list, paymentCoinList=payment_coin_list),
      method='POST',
      path='/api/v3/account/repay',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=RepayResponse,
    )
