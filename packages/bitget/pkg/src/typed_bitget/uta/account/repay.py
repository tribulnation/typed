# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.account.repay` -- generated from `spec/endpoints/`."""

from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class RepayRequest(TypedDict):
  """Coins to repay and to pay with."""

  repayableCoinList: list[str]
  """Coins to repay, from `uta.account.repayable_coins`."""
  paymentCoinList: list[str]
  """Coins to pay with, from `uta.account.payment_coins`."""


class RepayResponse(TypedDict):
  """Outcome of the repayment."""

  result: str
  """Repayment outcome."""
  repayAmount: str
  """Total amount repaid, in USD."""


class Repay(RpcEndpoint):
  """`uta.account.repay`."""

  async def repay(
    self,
    repay_request: RepayRequest,
    *,
    validate: bool | None = None,
  ) -> RepayResponse:
    """Repay unified-account debt, drawing from the given payment coins, authenticated.

    Args:
      repay_request: Repayment instruction.
    """
    return await self.authed_request(
      'POST',
      '/api/v3/account/repay',
      json=repay_request,
      validator=validator(RepayResponse),
      validate=validate,
    )
