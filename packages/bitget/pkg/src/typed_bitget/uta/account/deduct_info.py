# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.account.deduct_info` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class DeductInfo(TypedDict):
  """The account's BGB fee deduction setting."""

  deduct: Literal['on', 'off']
  """Whether BGB fee deduction is enabled."""


class DeductInfoEndpoint(RpcEndpoint):
  """`uta.account.deduct_info`."""

  async def deduct_info(self, *, validate: bool | None = None) -> DeductInfo:
    """Get the unified account's current BGB fee deduction setting, authenticated."""
    return await self.authed_request(
      'GET',
      '/api/v3/account/deduct-info',
      validator=validator(DeductInfo),
      validate=validate,
    )
