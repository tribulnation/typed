# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class AccountInfo(TypedDict):
  """The calling account's metadata and API key permissions."""

  userId: str
  """The account's user ID."""
  inviterId: str | None
  """UID of the account that referred this user, or null when there wasn't one."""
  parentId: str | None
  """Parent (main) account UID, set only when the caller is a sub-account."""
  permType: Literal['read_only', 'read_and_write']
  """Authorization level of the API key's permissions. Underscore-separated on the wire (`read_and_write`), confirmed live -- the upstream doc prose shows a hyphenated `read-and-write` instead."""
  permissions: list[str]
  """Granted permission scopes, e.g. `uta_mgt`, `uta_trade`, `withdraw`."""
  regisTime: TimestampMillis
  """Account creation time, Unix milliseconds as a string."""
  ips: str | None
  """Comma-separated IP allowlist for the API key, or null when none is configured."""


class Info(RpcEndpoint):
  """Get the unified account's info and key permissions, authenticated."""

  async def info(self, *, validate: bool | None = None) -> AccountInfo:
    """Get the unified account's info and key permissions, authenticated.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/uta-account-account-settings/account-settings-account-info#get-account-info)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v3/account/info',
      meta={'required': True},
      validate=validate,
      response_type=AccountInfo,
    )
