# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class MaxTransferable(TypedDict):
  """Maximum transferable amounts for the queried coin."""

  coin: str
  """Coin queried."""
  maxTransfer: Decimal
  """Maximum transferable amount using only available (unborrowed) balance."""
  borrowMaxTransfer: Decimal
  """Maximum transferable amount when automatic borrowing is allowed."""


class Request(TypedDict):
  coin: str
  """Coin to query."""


class MaxTransferableEndpoint(RpcEndpoint):
  """Get the maximum amount of a coin transferable out of the unified account, authenticated."""

  async def max_transferable(
    self,
    coin: str,
    *,
    validate: bool | None = None,
  ) -> MaxTransferable:
    """Get the maximum amount of a coin transferable out of the unified account, authenticated.

    Args:
      coin: Coin to query.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
    """
    return await self.request(
      Request(coin=coin),
      method='GET',
      path='/api/v3/account/max-transferable',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=MaxTransferable,
    )
