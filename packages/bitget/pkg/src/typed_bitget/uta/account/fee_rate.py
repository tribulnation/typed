# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class FeeRate(TypedDict):
  """Fee rates for the queried symbol."""

  makerFeeRate: Decimal
  """Maker fee rate for this symbol."""
  takerFeeRate: Decimal
  """Taker fee rate for this symbol."""


class Request(TypedDict):
  category: Literal['SPOT', 'MARGIN', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product type the symbol belongs to."""
  symbol: str
  """Trading symbol to query."""


class FeeRateEndpoint(RpcEndpoint):
  """Get the account's maker/taker trading fee rate for one symbol, authenticated."""

  async def fee_rate(
    self,
    category: Literal['SPOT', 'MARGIN', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    *,
    symbol: str,
    validate: bool | None = None,
  ) -> FeeRate:
    """Get the account's maker/taker trading fee rate for one symbol, authenticated.

    Args:
      category: Product type the symbol belongs to.
      symbol: Trading symbol to query.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
    """
    return await self.request(
      Request(category=category, symbol=symbol),
      method='GET',
      path='/api/v3/account/fee-rate',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=FeeRate,
    )
