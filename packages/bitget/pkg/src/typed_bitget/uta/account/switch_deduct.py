# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.account.switch_deduct` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class SwitchDeductRequest(TypedDict):
  """Target BGB deduction state."""

  deduct: Literal['on', 'off']
  """Whether to enable BGB fee deduction."""


class SwitchDeduct(RpcEndpoint):
  """`uta.account.switch_deduct`."""

  async def switch_deduct(
    self,
    switch_deduct_request: SwitchDeductRequest,
    *,
    validate: bool | None = None,
  ) -> str:
    """Turn BGB fee deduction on or off for the unified account, authenticated.

    Args:
      switch_deduct_request: Target BGB deduction state.
    """
    return await self.authed_request(
      'POST',
      '/api/v3/account/switch-deduct',
      json=switch_deduct_request,
      validator=validator(str),
      validate=validate,
    )
