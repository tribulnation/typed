# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Target BGB deduction state."""

  deduct: Literal['on', 'off']
  """Whether to enable BGB fee deduction."""


Response = str


class SwitchDeduct(RpcEndpoint):
  """Turn BGB fee deduction on or off for the unified account, authenticated."""

  async def switch_deduct(
    self,
    deduct: Literal['on', 'off'],
    *,
    validate: bool | None = None,
  ) -> Response:
    """Turn BGB fee deduction on or off for the unified account, authenticated.

    Args:
      deduct: Whether to enable BGB fee deduction.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
    """
    return await self.request(
      Request(deduct=deduct),
      method='POST',
      path='/api/v3/account/switch-deduct',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
