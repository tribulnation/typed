# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class PaymentCoin(TypedDict):
  """One coin available to pay down a debt with."""

  coin: str
  """Coin name."""
  size: Decimal
  """Available size of this coin usable as payment."""
  amount: Decimal
  """Available value of this coin usable as payment, in USD."""


class PaymentCoins(TypedDict):
  """Coins usable to pay down a debt."""

  paymentCoinList: list[PaymentCoin]
  """Coins available as payment."""
  maxSelection: int
  """Maximum number of coins that may be selected in one `uta.account.repay` call."""


class PaymentCoinsEndpoint(RpcEndpoint):
  """List the coins the unified account can use to pay down a debt, authenticated."""

  async def payment_coins(self, *, validate: bool | None = None) -> PaymentCoins:
    """List the coins the unified account can use to pay down a debt, authenticated.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v3/account/payment-coins',
      meta={'required': True},
      validate=validate,
      response_type=PaymentCoins,
    )
