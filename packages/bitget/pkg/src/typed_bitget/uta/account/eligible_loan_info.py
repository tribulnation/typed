# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class EligibleLoanInfo(TypedDict):
  """One coin's auto-loan limits."""

  coin: str
  """Coin this limit applies to. Present live though not shown in the documented response example."""
  limit: Decimal
  """Single-UID loan limit."""
  masterSubLimit: Decimal
  """Combined master-and-sub-account loan limit."""
  platformRemaingQuota: Decimal
  """Platform-wide remaining loan quota. Field name matches the venue's wire spelling verbatim."""


class Request(TypedDict):
  coin: NotRequired[str]
  """Restrict to one coin, e.g. `BTC`."""


Response = list[EligibleLoanInfo]


class EligibleLoanInfoEndpoint(RpcEndpoint):
  """Get the caller's auto-loan limits."""

  async def eligible_loan_info(
    self,
    coin: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Get the caller's auto-loan limits.

    Args:
      coin: Restrict to one coin, e.g. `BTC`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/uta-account-risk-position/risk-position-eligible#get-eligible-loan-info)
    """
    request: Request = Request()
    if coin is not None:
      request['coin'] = coin
    return await self.request(
      request,
      method='GET',
      path='/api/v3/account/eligible-loan-info',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
