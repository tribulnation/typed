# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.account.all_fee_rates` -- generated from `spec/endpoints/`."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class AllSymbolFeeRate(TypedDict):
  """One symbol's fee rates."""

  symbol: str
  """Trading symbol."""
  makerFeeRate: str
  """Maker fee rate for this symbol."""
  takerFeeRate: str
  """Taker fee rate for this symbol."""


class AllFeeRates(RpcEndpoint):
  """`uta.account.all_fee_rates`."""

  async def all_fee_rates(
    self,
    *,
    category: Literal['SPOT', 'MARGIN', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    symbol: str | None = None,
    validate: bool | None = None,
  ) -> list[AllSymbolFeeRate]:
    """Get the account's maker/taker trading fee rate for every symbol under a product type, authenticated.

    Args:
      category: Product type to list fee rates for.
      symbol: Restrict to a single symbol. Omit to list every symbol under `category`.
    """
    params: dict = {
      'category': category,
    }
    if symbol is not None:
      params['symbol'] = symbol
    return await self.authed_request(
      'GET',
      '/api/v3/account/all-fee-rate',
      params=params,
      validator=validator(list[AllSymbolFeeRate]),
      validate=validate,
    )
