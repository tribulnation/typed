# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class RealityFill(TypedDict):
  """One public fill."""

  execId: str
  """Fill ID."""
  price: str
  """Fill price."""
  size: str
  """Fill quantity, in the base coin."""
  side: Literal['buy', 'sell']
  """Trade side."""
  ts: TimestampMillis
  """Fill time."""


class Request(TypedDict):
  symbol: str
  """Reality trading pair, e.g. `RAAPLUSDT`. Only Reality pairs are supported."""
  limit: NotRequired[int]
  """Results per page. Default and maximum: 100."""


Response = list[RealityFill]


class RealityFills(RpcEndpoint):
  """List recent public fills for a Reality (tokenized-stock) trading pair."""

  async def reality_fills(
    self,
    symbol: str,
    *,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """List recent public fills for a Reality (tokenized-stock) trading pair.

    Args:
      symbol: Reality trading pair, e.g. `RAAPLUSDT`. Only Reality pairs are supported.
      limit: Results per page. Default and maximum: 100.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/reality-market-data/reality-market-data#get-reality-fills)
    """
    request: Request = Request(symbol=symbol)
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/api/v3/account/reality-fills',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
