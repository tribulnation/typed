# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Margin adjustment to apply."""

  category: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Futures product type."""
  symbol: str
  """Trading symbol."""
  posSide: Literal['long', 'short']
  """Position direction."""
  operation: Literal['add', 'remove']
  """Whether to add margin to, or remove margin from, the position."""
  amount: str
  """Margin amount to add or remove."""


Response = str


class SetMargin(RpcEndpoint):
  """Add or remove margin on an isolated-margin futures position, authenticated."""

  async def set_margin(
    self,
    category: Literal['USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    *,
    symbol: str,
    pos_side: Literal['long', 'short'],
    operation: Literal['add', 'remove'],
    amount: str,
    validate: bool | None = None,
  ) -> Response:
    """Add or remove margin on an isolated-margin futures position, authenticated.

    Args:
      category: Futures product type.
      symbol: Trading symbol.
      pos_side: Position direction.
      operation: Whether to add margin to, or remove margin from, the position.
      amount: Margin amount to add or remove.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
    """
    return await self.request(
      Request(
        category=category,
        symbol=symbol,
        posSide=pos_side,
        operation=operation,
        amount=amount,
      ),
      method='POST',
      path='/api/v3/account/set-margin',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
