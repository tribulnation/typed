# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Leverage to configure."""

  category: Literal['MARGIN', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES']
  """Product type to set leverage for."""
  symbol: NotRequired[str]
  """Trading symbol. Required when setting leverage for a futures product."""
  leverage: str
  """Leverage multiplier to apply. Not used when `longLeverage`/`shortLeverage` are set."""
  coin: NotRequired[str]
  """Coin name. Required when setting leverage for margin trading."""
  posSide: NotRequired[Literal['long', 'short']]
  """Position direction. Required for isolated-margin, hedge-mode futures positions."""
  marginMode: NotRequired[Literal['crossed', 'isolated']]
  """Margin mode to apply. Futures only; defaults to crossed margin."""
  longLeverage: NotRequired[str]
  """Long-side leverage, isolated-margin two-way (hedge) mode only. Takes priority over `leverage` when both are set."""
  shortLeverage: NotRequired[str]
  """Short-side leverage, isolated-margin two-way (hedge) mode only. Takes priority over `leverage` when both are set."""


Response = str


class Set(RpcEndpoint):
  """Set leverage for a futures symbol or a margin coin, authenticated."""

  async def set(
    self,
    category: Literal['MARGIN', 'USDT-FUTURES', 'COIN-FUTURES', 'USDC-FUTURES'],
    *,
    symbol: str | None = None,
    leverage: str,
    coin: str | None = None,
    pos_side: Literal['long', 'short'] | None = None,
    margin_mode: Literal['crossed', 'isolated'] | None = None,
    long_leverage: str | None = None,
    short_leverage: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Set leverage for a futures symbol or a margin coin, authenticated.

    Args:
      category: Product type to set leverage for.
      symbol: Trading symbol. Required when setting leverage for a futures product.
      leverage: Leverage multiplier to apply. Not used when `longLeverage`/`shortLeverage` are set.
      coin: Coin name. Required when setting leverage for margin trading.
      pos_side: Position direction. Required for isolated-margin, hedge-mode futures positions.
      margin_mode: Margin mode to apply. Futures only; defaults to crossed margin.
      long_leverage: Long-side leverage, isolated-margin two-way (hedge) mode only. Takes priority over `leverage` when both are set.
      short_leverage: Short-side leverage, isolated-margin two-way (hedge) mode only. Takes priority over `leverage` when both are set.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/uta-account-account-settings/account-settings-leverage#change-leverage)
    """
    request: Request = Request(category=category, leverage=leverage)
    if symbol is not None:
      request['symbol'] = symbol
    if coin is not None:
      request['coin'] = coin
    if pos_side is not None:
      request['posSide'] = pos_side
    if margin_mode is not None:
      request['marginMode'] = margin_mode
    if long_leverage is not None:
      request['longLeverage'] = long_leverage
    if short_leverage is not None:
      request['shortLeverage'] = short_leverage
    return await self.request(
      request,
      method='POST',
      path='/api/v3/account/set-leverage',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
