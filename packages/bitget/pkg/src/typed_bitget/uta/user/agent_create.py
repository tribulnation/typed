# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.user.agent_create` -- generated from `spec/endpoints/`."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CreateAgentSubAccountRequest(TypedDict):
  """A new sub-account, plus API key, to create in one call."""

  username: str
  """Login name for the new sub-account."""
  passphrase: str
  """Passphrase to protect the new API key; required on every signed request made with it."""
  note: NotRequired[str]
  """Free-text remark stored against the sub-account and its key."""


class CreateAgentSubAccountResponse(TypedDict):
  """The newly created sub-account and API key."""

  username: str
  """Login name of the sub-account."""
  subUid: str
  """Unique identifier of the sub-account."""
  apiKey: str
  """The created API key."""
  secret: str
  """The API key's secret. Returned only on creation; the venue does not expose it again afterward."""
  note: str
  """Free-text remark stored against the sub-account and its key."""
  createdTime: TimestampMillis
  """Time the sub-account and key were created."""


class AgentCreate(RpcEndpoint):
  """`uta.user.agent_create`."""

  async def agent_create(
    self,
    create_agent_sub_account_request: CreateAgentSubAccountRequest,
    *,
    validate: bool | None = None,
  ) -> CreateAgentSubAccountResponse:
    """Create a unified sub-account and an API key for it in a single call. Unlike `create-sub`, which creates a sub-account with no key, this returns a usable `apiKey`/`secret` pair immediately.

    Args:
      create_agent_sub_account_request: A new sub-account, plus API key, to create in one call.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/user/Agent-Create-Sub-Account)
    """
    return await self.authed_request(
      'POST',
      '/api/v3/user/sub-account/agent-create',
      json=create_agent_sub_account_request,
      validator=validator(CreateAgentSubAccountResponse),
      validate=validate,
    )
