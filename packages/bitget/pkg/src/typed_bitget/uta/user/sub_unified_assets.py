# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  subUid: NotRequired[str]
  """Unique identifier of the sub-account to query. Omit to query across sub-accounts, per `cursor`/`limit`."""
  cursor: NotRequired[str]
  """Pagination cursor from a previous call's response; omit to fetch the first page."""
  limit: NotRequired[int]
  """Maximum number of entries to return per page."""


class SubUnifiedAsset(TypedDict):
  """One coin's balance within a sub-account's unified account."""

  coin: str
  """Coin this balance is denominated in, e.g. `USDT`."""
  equity: str
  """Total equity in this coin, as a decimal string."""
  usdValue: str
  """USD-equivalent value of this coin's equity, as a decimal string."""
  balance: str
  """Account balance in this coin, as a decimal string."""
  available: str
  """Amount of this coin currently available, as a decimal string."""
  debt: str
  """Outstanding debt in this coin, as a decimal string."""
  locked: str
  """Amount of this coin currently locked, as a decimal string."""


class SubUnifiedAssets(TypedDict):
  """One sub-account's unified-account asset balances."""

  subUid: str
  """Unique identifier of the sub-account these balances belong to."""
  cursor: NotRequired[str]
  """Cursor to pass as `cursor` to fetch the next page; absent when there is no further page."""
  assets: list[SubUnifiedAsset]
  """The sub-account's per-coin balances."""


SubUnifiedAssetsList = list[SubUnifiedAssets]


class SubUnifiedAssetsEndpoint(RpcEndpoint):
  """Get a unified sub-account's asset balances."""

  async def sub_unified_assets(
    self,
    *,
    sub_uid: str | None = None,
    cursor: str | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> SubUnifiedAssetsList:
    """Get a unified sub-account's asset balances.

    Args:
      sub_uid: Unique identifier of the sub-account to query. Omit to query across sub-accounts, per `cursor`/`limit`.
      cursor: Pagination cursor from a previous call's response; omit to fetch the first page.
      limit: Maximum number of entries to return per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/uta-account-sub-accounts/sub-accounts-assets#get-sub-account-unified-assets)
    """
    request: Request = Request()
    if sub_uid is not None:
      request['subUid'] = sub_uid
    if cursor is not None:
      request['cursor'] = cursor
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/api/v3/account/sub-unified-assets',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=SubUnifiedAssetsList,
    )
