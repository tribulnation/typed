# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CreateAgentSubAccountResponse(TypedDict):
  """The newly created sub-account and API key."""

  username: str
  """Login name of the sub-account."""
  subUid: str
  """Unique identifier of the sub-account."""
  apiKey: str
  """The created API key."""
  secret: str
  """The API key's secret. Returned only on creation; the venue does not expose it again afterward."""
  note: str
  """Free-text remark stored against the sub-account and its key."""
  createdTime: TimestampMillis
  """Time the sub-account and key were created."""


class Request(TypedDict):
  """A new sub-account, plus API key, to create in one call."""

  username: str
  """Login name for the new sub-account."""
  passphrase: str
  """Passphrase to protect the new API key; required on every signed request made with it."""
  note: NotRequired[str]
  """Free-text remark stored against the sub-account and its key."""


class CreateWithKey(RpcEndpoint):
  """Create a unified sub-account and an API key for it in a single call. Unlike `create-sub`, which creates a sub-account with no key, this returns a usable `apiKey`/`secret` pair immediately."""

  async def create_with_key(
    self,
    *,
    username: str,
    passphrase: str,
    note: str | None = None,
    validate: bool | None = None,
  ) -> CreateAgentSubAccountResponse:
    """Create a unified sub-account and an API key for it in a single call. Unlike `create-sub`, which creates a sub-account with no key, this returns a usable `apiKey`/`secret` pair immediately.

    Args:
      username: Login name for the new sub-account.
      passphrase: Passphrase to protect the new API key; required on every signed request made with it.
      note: Free-text remark stored against the sub-account and its key.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/uta-account-sub-accounts/sub-accounts-agent#create-agent-sub-account)
    """
    request: Request = Request(username=username, passphrase=passphrase)
    if note is not None:
      request['note'] = note
    return await self.request(
      request,
      method='POST',
      path='/api/v3/user/sub-account/agent-create',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=CreateAgentSubAccountResponse,
    )
