# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Changes to apply to an existing sub-account API key."""

  apiKey: str
  """The API key to modify."""
  passphrase: str
  """The API key's passphrase, required to authorize the change."""
  type: NotRequired[Literal['read_write', 'read_only']]
  """New trading permission level for the API key. Omit to leave unchanged."""
  permissions: NotRequired[list[str]]
  """New permission scopes for the API key. Omit to leave unchanged."""
  ips: NotRequired[list[str]]
  """New IP allowlist for the API key. Omit to leave unchanged."""


class UpdateSubAccountApiKeyResponse(TypedDict):
  """The updated API key."""

  note: str
  """Free-text remark stored against the API key."""
  apiKey: str
  """The API key."""
  type: str
  """Trading permission level of the API key. Left bare, not narrowed to the request's enum -- see `create-sub-api`'s notes for the same asymmetry."""
  permissions: list[str]
  """Current permission scopes granted to the API key."""
  ips: list[str]
  """Current IP allowlist for the API key."""


class Update(RpcEndpoint):
  """Modify an existing unified sub-account API key's permissions, IP allowlist, or type."""

  async def update(
    self,
    *,
    api_key: str,
    passphrase: str,
    type: Literal['read_write', 'read_only'] | None = None,
    permissions: list[str] | None = None,
    ips: list[str] | None = None,
    validate: bool | None = None,
  ) -> UpdateSubAccountApiKeyResponse:
    """Modify an existing unified sub-account API key's permissions, IP allowlist, or type.

    Args:
      api_key: The API key to modify.
      passphrase: The API key's passphrase, required to authorize the change.
      type: New trading permission level for the API key. Omit to leave unchanged.
      permissions: New permission scopes for the API key. Omit to leave unchanged.
      ips: New IP allowlist for the API key. Omit to leave unchanged.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/uta-account-sub-accounts/sub-accounts-api-key#modify-sub-account-api-key)
    """
    request: Request = Request(apiKey=api_key, passphrase=passphrase)
    if type is not None:
      request['type'] = type
    if permissions is not None:
      request['permissions'] = permissions
    if ips is not None:
      request['ips'] = ips
    return await self.request(
      request,
      method='POST',
      path='/api/v3/user/update-sub-api',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=UpdateSubAccountApiKeyResponse,
    )
