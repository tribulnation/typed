# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class ModifySubAccountResponse(TypedDict):
  """The modified sub-account. Shape inferred by symmetry with `create-sub`'s response -- unconfirmed, see `notes`."""

  username: str
  """Login name of the sub-account."""
  subUid: str
  """Unique identifier of the sub-account."""
  status: str
  """Current status of the sub-account."""
  note: str
  """Free-text remark stored against the sub-account."""
  createdTime: TimestampMillis
  """Time the sub-account was created."""
  updatedTime: TimestampMillis
  """Time the sub-account was last updated."""


class Request(TypedDict):
  """A sub-account to modify. Only `subUid` is confirmed; `note` is a best-effort inference -- see `notes`."""

  subUid: str
  """Unique identifier of the sub-account to modify."""
  note: NotRequired[str]
  """New free-text remark for the sub-account. Field name unconfirmed; inferred by symmetry with `create-sub`'s `note` field -- see `notes`."""


class Modify(RpcEndpoint):
  """Modify an existing unified sub-account's remark. Field set inferred by symmetry with `create-sub`; see `notes` -- this endpoint's exact request/response shape is unconfirmed by any surveyed source."""

  async def modify(
    self,
    *,
    sub_uid: str,
    note: str | None = None,
    validate: bool | None = None,
  ) -> ModifySubAccountResponse:
    """Modify an existing unified sub-account's remark. Field set inferred by symmetry with `create-sub`; see `notes` -- this endpoint's exact request/response shape is unconfirmed by any surveyed source.

    Args:
      sub_uid: Unique identifier of the sub-account to modify.
      note: New free-text remark for the sub-account. Field name unconfirmed; inferred by symmetry with `create-sub`'s `note` field -- see `notes`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/user/Modify-Sub-Account)
    """
    request: Request = Request(subUid=sub_uid)
    if note is not None:
      request['note'] = note
    return await self.request(
      request,
      method='POST',
      path='/api/v3/user/modify-sub',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=ModifySubAccountResponse,
    )
