# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class CreateSubAccountResponse(TypedDict):
  """The newly created sub-account."""

  username: str
  """Login name of the sub-account."""
  subUid: str
  """Unique identifier of the sub-account."""
  status: str
  """Current status of the sub-account."""
  note: str
  """Free-text remark stored against the sub-account."""
  createdTime: TimestampMillis
  """Time the sub-account was created."""
  updatedTime: TimestampMillis
  """Time the sub-account was last updated."""


class Request(TypedDict):
  """A new unified sub-account to create."""

  username: str
  """Login name for the new sub-account."""
  accountMode: NotRequired[Literal['classic', 'unified']]
  """Account mode to create the sub-account in."""
  note: NotRequired[str]
  """Free-text remark stored against the sub-account."""


class Create(RpcEndpoint):
  """Create a new unified sub-account under the main account."""

  async def create(
    self,
    *,
    username: str,
    account_mode: Literal['classic', 'unified'] | None = None,
    note: str | None = None,
    validate: bool | None = None,
  ) -> CreateSubAccountResponse:
    """Create a new unified sub-account under the main account.

    Args:
      username: Login name for the new sub-account.
      account_mode: Account mode to create the sub-account in.
      note: Free-text remark stored against the sub-account.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/uta-account-sub-accounts/sub-accounts-management#create-sub-account)
    """
    request: Request = Request(username=username)
    if account_mode is not None:
      request['accountMode'] = account_mode
    if note is not None:
      request['note'] = note
    return await self.request(
      request,
      method='POST',
      path='/api/v3/user/create-sub',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=CreateSubAccountResponse,
    )
