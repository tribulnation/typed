# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """The sub-account API key to delete."""

  apiKey: str
  """The API key to delete."""


Response = dict[str, Any]


class Delete(RpcEndpoint):
  """Delete a unified sub-account API key, permanently revoking it."""

  async def delete(self, api_key: str, *, validate: bool | None = None) -> Response:
    """Delete a unified sub-account API key, permanently revoking it.

    Args:
      api_key: The API key to delete.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/uta-account-sub-accounts/sub-accounts-api-key#delete-sub-account-api-key)
    """
    return await self.request(
      Request(apiKey=api_key),
      method='POST',
      path='/api/v3/user/delete-sub-api',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
