# Generated by typed-dev — see CONTRIBUTING.md.
"""`uta.user.subaccount.freeze` -- generated from `spec/endpoints/`."""

from typing_extensions import Any, Literal
from typed_core.validation import TypedDict, validator
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class FreezeSubAccountRequest(TypedDict):
  """A sub-account to freeze or unfreeze."""

  subUid: str
  """Unique identifier of the sub-account."""
  operation: Literal['freeze', 'unfreeze']
  """Whether to freeze or unfreeze the sub-account."""


class Freeze(RpcEndpoint):
  """`uta.user.subaccount.freeze`."""

  async def freeze(
    self,
    freeze_sub_account_request: FreezeSubAccountRequest,
    *,
    validate: bool | None = None,
  ) -> dict[str, Any]:
    """Freeze or unfreeze a unified sub-account, blocking or restoring its ability to trade and withdraw.

    Args:
      freeze_sub_account_request: A sub-account to freeze or unfreeze.

    References:
      - [Official docs](https://www.bitget.com/api-doc/uta/user/Freeze-Sub-Account)
    """
    return await self.authed_request(
      'POST',
      '/api/v3/user/freeze-sub',
      json=freeze_sub_account_request,
      validator=validator(dict[str, Any]),
      validate=validate,
    )
