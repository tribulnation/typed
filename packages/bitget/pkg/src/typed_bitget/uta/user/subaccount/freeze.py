# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, Literal, TypedDict
from typed_bitget.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """A sub-account to freeze or unfreeze."""

  subUid: str
  """Unique identifier of the sub-account."""
  operation: Literal['freeze', 'unfreeze']
  """Whether to freeze or unfreeze the sub-account."""


Response = dict[str, Any]


class Freeze(RpcEndpoint):
  """Freeze or unfreeze a unified sub-account, blocking or restoring its ability to trade and withdraw."""

  async def freeze(
    self,
    sub_uid: str,
    *,
    operation: Literal['freeze', 'unfreeze'],
    validate: bool | None = None,
  ) -> Response:
    """Freeze or unfreeze a unified sub-account, blocking or restoring its ability to trade and withdraw.

    Args:
      sub_uid: Unique identifier of the sub-account.
      operation: Whether to freeze or unfreeze the sub-account.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/catalog/uta-account-sub-accounts/sub-accounts-management#freeze-or-unfreeze-sub-account)
    """
    return await self.request(
      Request(subUid=sub_uid, operation=operation),
      method='POST',
      path='/api/v3/user/freeze-sub',
      meta={'required': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
