# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .classic import Classic
from .classic_streams import ClassicStreams
from .uta import Uta
from .uta_streams import UtaStreams
from typed_bitget.core.base import BitgetBase


class Bitget(BitgetBase):
  """Bitget client -- composes the Classic v2 and UTA v3 REST surfaces (sharing one `HttpRpcClient` -- same host, envelope and signing scheme either way) and their respective WebSocket surfaces (structurally distinct wire shapes, so each gets its own transport). One credential set is shared by both: a Bitget account is either Classic-mode or UTA-mode, never both at once, so it authenticates against whichever surface matches the account's actual mode -- call the methods matching your account.

  References:
    - [Upstream docs](https://www.bitget.com/docs/classic/catalog)
  """

  @cached_property
  def classic(self) -> Classic:
    """Bitget Classic -- the account and API system Bitget ran before introducing the Unified Trading Account: spot, margin, futures ('mix'), copy trading, earn, P2P, convert, broker, institutional loan, tax, and account-plumbing endpoints, together.

    References:
      - [Upstream docs](https://www.bitget.com/docs/classic/catalog)
    """
    return Classic(client=self.http_client)

  @cached_property
  def classic_streams(self) -> ClassicStreams:
    """Bitget Classic's WebSocket API: public and private channel subscriptions, login and HMAC authentication, the shared subscribe/unsubscribe wire format, and ping/pong keepalive used by every Classic stream endpoint.

    References:
      - [Upstream docs](https://www.bitget.com/docs/classic/websocket/intro)
    """
    return ClassicStreams(client=self.classic_streams_client)

  @cached_property
  def uta(self) -> Uta:
    """Bitget's Unified Trading Account (UTA) -- the account and API system covering spot, margin, and derivatives trading, market data, account management, copy trading, earn, crypto/institutional loans, P2P, broker, and tax, all under one unified account.

    References:
      - [Upstream docs](https://www.bitget.com/docs/uta/catalog)
    """
    return Uta(client=self.http_client)

  @cached_property
  def uta_streams(self) -> UtaStreams:
    """Bitget UTA's WebSocket API: public and private channel subscriptions, login and HMAC authentication, and the shared subscribe/unsubscribe wire format used by every UTA stream endpoint. The new docs site has no single UTA WebSocket intro page (unlike Classic's); this links the private Account channel page, which documents the shared login/auth flow, as the closest available reference among UTA's many per-channel pages.

    References:
      - [Upstream docs](https://www.bitget.com/docs/uta/websocket/private/Account-Channel)
    """
    return UtaStreams(client=self.uta_streams_client)
