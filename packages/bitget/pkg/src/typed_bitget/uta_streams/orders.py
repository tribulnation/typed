# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_bitget.core import TimestampMillis
from typed_bitget.core.endpoint.stream import StreamEndpoint
from typed_bitget.schemas import UtaOrderArg


class FeeDetail(TypedDict):
  """One fee charged, in a single coin."""

  feeCoin: str
  """Coin the fee was charged in."""
  fee: Decimal
  """Fee amount, negative when rebated."""


class Parameters(TypedDict):
  """Order subscription parameters."""

  instType: Literal['UTA']
  """Fixed unified-account instType. All UTA v3 private channels subscribe/push under `"UTA"`, not a per-category value -- unlike Classic v2, which splits private channels by `SPOT`/`USDT-FUTURES`/etc."""


class OrderUpdate(TypedDict):
  """One order status update."""

  category: str
  """Product line the fill/order belongs to."""
  symbol: str
  """Trading pair."""
  orderId: str
  """Venue-assigned order identifier."""
  clientOid: str
  """Caller-supplied client order ID."""
  price: Decimal
  """Order price. Empty for a market order."""
  qty: Decimal
  """Order quantity."""
  amount: Decimal
  """Order amount (quote-coin notional), for order types placed by amount rather than quantity."""
  holdMode: str
  """Position mode this order was placed under."""
  holdSide: str
  """Directional exposure this order contributes to. Futures only."""
  delegateType: str
  """Delegation type."""
  tradeSide: str
  """Whether this order opens or closes a position. Futures only."""
  orderType: Literal['limit', 'market']
  """Order type."""
  timeInForce: Literal['ioc', 'fok', 'gtc', 'post_only', 'rpi']
  """Time-in-force policy for a limit order."""
  side: Literal['buy', 'sell']
  """Order side."""
  marginMode: Literal['crossed', 'isolated']
  """Margin mode governing this order. Futures/margin only."""
  marginCoin: str
  """Margin coin. Futures/margin only."""
  reduceOnly: Literal['yes', 'no']
  """Whether this order is restricted to only reduce an existing position."""
  cumExecQty: Decimal
  """Cumulative executed quantity."""
  cumExecValue: Decimal
  """Cumulative executed value."""
  avgPrice: Decimal
  """Average execution price."""
  totalProfit: Decimal
  """Total realised profit attributed to this order. Futures only."""
  orderStatus: Literal['new', 'partially_filled', 'filled', 'cancelled']
  """Order lifecycle status."""
  cancelReason: str
  """Cancellation reason; empty string when not cancelled."""
  leverage: int
  """Leverage this order was placed under. Futures only."""
  feeDetail: list[FeeDetail]
  """Fees charged against this order, one entry per fee coin."""
  createdTime: TimestampMillis
  """Order creation time, Unix millisecond timestamp."""
  updatedTime: TimestampMillis
  """Last update time, Unix millisecond timestamp."""
  stpMode: str
  """Self-trade-prevention mode."""
  tpTriggerBy: str
  """Take-profit trigger price type, when a TP is attached. Empty string when not applicable -- confirmed live."""
  slTriggerBy: str
  """Stop-loss trigger price type, when a SL is attached. Empty string when not applicable -- confirmed live."""
  takeprofit: str
  """Take-profit trigger price, when a TP is attached. Empty string when not applicable -- confirmed live. Note the wire key is lowercase `takeprofit` on this push, unlike the REST `takeProfit`."""
  stoploss: str
  """Stop-loss trigger price, when a SL is attached. Empty string when not applicable -- confirmed live. Note the wire key is lowercase `stoploss` on this push, unlike the REST `stopLoss`."""
  tpOrderType: Literal['limit', 'market']
  """Take-profit order type, when a TP is attached."""
  slOrderType: Literal['limit', 'market']
  """Stop-loss order type, when a SL is attached."""
  tpLimitPrice: str
  """Take-profit execution price, when `tpOrderType` is `limit`. Empty string when not applicable -- confirmed live."""
  slLimitPrice: str
  """Stop-loss execution price, when `slOrderType` is `limit`. Empty string when not applicable -- confirmed live."""
  matchType: str
  """Matching-engine identifier for this update. Real meaning unconfirmed (docs page did not document it); '0' observed live on a normal market fill."""


class OrderPush(TypedDict):
  """Push frame for the `order` channel. The core performs no envelope unwrap for streams (spec/core.md WebSocket): callers receive this whole frame."""

  action: Literal['snapshot', 'update']
  """Push data action."""
  arg: UtaOrderArg
  data: list[OrderUpdate]
  """Subscription data."""
  ts: TimestampMillis
  """Message generation time, Unix epoch milliseconds."""


class Orders(StreamEndpoint):
  """Get order status updates across the unified account, private."""

  def orders(
    self,
    inst_type: Literal['UTA'],
    *,
    validate: bool | None = None,
  ) -> StreamManager[OrderPush, Any, Any]:
    """Get order status updates across the unified account, private.

    Args:
      inst_type: Fixed unified-account instType. All UTA v3 private channels subscribe/push under `"UTA"`, not a per-category value -- unlike Classic v2, which splits private channels by `SPOT`/`USDT-FUTURES`/etc.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.bitget.com/docs/uta/websocket/private/Order-Channel)
    """
    return self.subscribe(
      'order',
      Parameters(instType=inst_type),
      meta={'private': True},
      validate=validate,
      request_type=Parameters,
      response_type=OrderPush,
    )
