# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import FuturesContract


class Request(TypedDict):
  symbol: str
  """Contract symbol, e.g. `XBTUSDTM`."""


class Symbol(RpcEndpoint):
  """Get detailed contract configuration for one futures contract: pricing increments, margin/leverage parameters, fee rates, current mark/index/last price, funding rate parameters, and the related index symbols used to look up mark/spot-index/premium/interest data for this contract."""

  async def symbol(
    self, symbol: str, *, validate: bool | None = None
  ) -> FuturesContract:
    """Get detailed contract configuration for one futures contract: pricing increments, margin/leverage parameters, fee rates, current mark/index/last price, funding rate parameters, and the related index symbols used to look up mark/spot-index/premium/interest data for this contract.

    Args:
      symbol: Contract symbol, e.g. `XBTUSDTM`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/futures-trading/market-data/get-symbol)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/api/v1/contracts/{symbol}',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=FuturesContract,
    )
