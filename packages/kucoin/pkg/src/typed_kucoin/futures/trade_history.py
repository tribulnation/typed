# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /api/v1/trade/history` — Get Trade History."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint, TimestampNanos


class FuturesTradeItem(TypedDict):
  """One executed trade."""

  sequence: int
  """Trade sequence number."""
  contractId: int
  """Internal contract identifier. Undocumented, observed live."""
  tradeId: str
  """Trade ID. Observed always equal to `sequence` as a string."""
  makerOrderId: str
  """Maker order's ID."""
  takerOrderId: str
  """Taker order's ID."""
  ts: TimestampNanos
  """Trade time, Unix **nanoseconds**."""
  size: int
  """Quantity traded, lots."""
  price: str
  """Execution price."""
  side: Literal['buy', 'sell']
  """Taker side of the trade."""


_Type = list[FuturesTradeItem]
adapter = validator[_Type](_Type)  # type: ignore


class TradeHistory(RpcEndpoint):
  """`Get Trade History` — mixed into `Futures`, the product exposing `futures.trade_history`."""

  async def trade_history(
    self,
    *,
    symbol: str,
    validate: bool | None = None,
  ) -> list[FuturesTradeItem]:
    """Get the most recent trades for one futures contract, up to the last 100. No time-range parameters -- always the tail of the trade stream, not a paginated history.

    Args:
      symbol: Contract symbol, e.g. `XBTUSDTM`.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    params: dict = {
      'symbol': symbol,
    }
    return await self.request(
      'GET',
      '/api/v1/trade/history',
      params=params,
      validator=adapter,
      validate=validate,
    )
