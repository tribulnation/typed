# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, TypedDict
from typed_kucoin.core import TimestampNanos
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class FuturesTradeItem(TypedDict):
  """One executed trade."""

  sequence: int
  """Trade sequence number."""
  contractId: int
  """Internal contract identifier. Undocumented, observed live."""
  tradeId: str
  """Trade ID. Observed always equal to `sequence` as a string."""
  makerOrderId: str
  """Maker order's ID."""
  takerOrderId: str
  """Taker order's ID."""
  ts: TimestampNanos
  """Trade time, Unix **nanoseconds**."""
  size: int
  """Quantity traded, lots."""
  price: Decimal
  """Execution price."""
  side: Literal['buy', 'sell']
  """Taker side of the trade."""


class Request(TypedDict):
  symbol: str
  """Contract symbol, e.g. `XBTUSDTM`."""


FuturesTradeHistory = list[FuturesTradeItem]


class TradeHistory(RpcEndpoint):
  """Get the most recent trades for one futures contract, up to the last 100. No time-range parameters -- always the tail of the trade stream, not a paginated history."""

  async def trade_history(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> FuturesTradeHistory:
    """Get the most recent trades for one futures contract, up to the last 100. No time-range parameters -- always the tail of the trade stream, not a paginated history.

    Args:
      symbol: Contract symbol, e.g. `XBTUSDTM`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/futures-trading/market-data/get-trade-history)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/api/v1/trade/history',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=FuturesTradeHistory,
    )
