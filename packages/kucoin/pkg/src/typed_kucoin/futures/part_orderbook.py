# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import FuturesOrderBook


class Request(TypedDict):
  size: Literal['20', '100']
  """Number of price levels per side to return. Confirmed live: `20` and `100` are valid; `50` returns HTTP 404."""
  symbol: str
  """Contract symbol, e.g. `XBTUSDTM`."""


class PartOrderbook(RpcEndpoint):
  """Get order book depth for one futures contract, limited to the requested number of price levels per side. Faster and lighter than the full order book when a caller does not need every level."""

  async def part_orderbook(
    self,
    size: Literal['20', '100'],
    *,
    symbol: str,
    validate: bool | None = None,
  ) -> FuturesOrderBook:
    """Get order book depth for one futures contract, limited to the requested number of price levels per side. Faster and lighter than the full order book when a caller does not need every level.

    Args:
      size: Number of price levels per side to return. Confirmed live: `20` and `100` are valid; `50` returns HTTP 404.
      symbol: Contract symbol, e.g. `XBTUSDTM`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/futures-trading/market-data/get-part-orderbook)
    """
    return await self.request(
      Request(size=size, symbol=symbol),
      method='GET',
      path='/api/v1/level2/depth{size}',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=FuturesOrderBook,
    )
