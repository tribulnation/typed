# Generated by typed-dev — see CONTRIBUTING.md.
from datetime import timedelta
from typing_extensions import AsyncIterator, NotRequired, TypedDict
from typed_kucoin.core import TimestampMillis
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import FuturesIndexSnapshot


class Request(TypedDict):
  symbol: NotRequired[str]
  """Premium index symbol, e.g. `.XBTUSDTMPI` (see `futures.symbol`'s `premiumsSymbol1M`/`premiumsSymbol8H` fields for a given contract)."""
  startAt: NotRequired[TimestampMillis]
  """Start of the window, Unix ms. Inclusive -- confirmed live (see notes)."""
  endAt: NotRequired[TimestampMillis]
  """End of the window, Unix ms. Inclusive -- confirmed live (see notes)."""
  reverse: NotRequired[bool]
  """Reverse the sort order of results. Not independently verified live -- documented, left as-is."""
  offset: NotRequired[int]
  """Continuation offset for an alternative (undocumented-shape) paging mode -- see notes; this spec models the `startAt`/`endAt` bounded-window mode instead."""
  forward: NotRequired[bool]
  """Direction for offset-based paging -- see `offset`."""
  maxCount: NotRequired[int]
  """Maximum records to return. Confirmed live default `10` (an unfiltered call with no `maxCount` returned exactly 10 rows)."""


class FuturesPremiumIndexSnapshotPage(TypedDict):
  """One page of index history."""

  dataList: list[FuturesIndexSnapshot]
  """Index snapshots, newest first."""
  hasMore: bool
  """Whether more historical records exist beyond this page."""


class PremiumIndex(RpcEndpoint):
  """Get the premium index used in a futures contract's funding rate calculation -- the time-weighted average spread between mid-price and index price, normalized by index price -- either the current snapshot or a historical window."""

  async def premium_index_paged(
    self,
    symbol: str | None = None,
    *,
    start_at: TimestampMillis | None = None,
    end_at: TimestampMillis | None = None,
    reverse: bool | None = None,
    offset: int | None = None,
    forward: bool | None = None,
    max_count: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[FuturesPremiumIndexSnapshotPage]:
    """Get the premium index used in a futures contract's funding rate calculation -- the time-weighted average spread between mid-price and index price, normalized by index price -- either the current snapshot or a historical window.

    Paged variant of `premium_index`: Moves the `start_at`–`end_at` window forwards by its own width and stops on the first empty window, or after `max_pages` pages when one is given.

    Every request spans the width the caller's own `start_at` and `end_at` state, so choose a window the venue answers in one response: it caps a wider one, and the walk moves past the rows that were left out.

    Args:
      symbol: Premium index symbol, e.g. `.XBTUSDTMPI` (see `futures.symbol`'s `premiumsSymbol1M`/`premiumsSymbol8H` fields for a given contract).
      start_at: Start of the window, Unix ms. Inclusive -- confirmed live (see notes).
      end_at: End of the window, Unix ms. Inclusive -- confirmed live (see notes).
      reverse: Reverse the sort order of results. Not independently verified live -- documented, left as-is.
      offset: Continuation offset for an alternative (undocumented-shape) paging mode -- see notes; this spec models the `startAt`/`endAt` bounded-window mode instead.
      forward: Direction for offset-based paging -- see `offset`.
      max_count: Maximum records to return. Confirmed live default `10` (an unfiltered call with no `maxCount` returned exactly 10 rows).
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/futures-trading/market-data/get-premium-index)
    """
    if start_at is None or end_at is None:
      raise ValueError(
        '`premium_index_paged` walks a time window: pass both `start_at` and `end_at`'
      )
    lower = start_at
    upper = end_at
    width = upper - lower
    pages = 0
    while True:
      response = await self.premium_index(
        symbol,
        start_at=lower,
        end_at=upper,
        reverse=reverse,
        offset=offset,
        forward=forward,
        max_count=max_count,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      rows = response.get('dataList') if response is not None else None
      if not rows:
        break
      lower = upper + timedelta(milliseconds=1)
      upper = lower + width

  async def premium_index(
    self,
    symbol: str | None = None,
    *,
    start_at: TimestampMillis | None = None,
    end_at: TimestampMillis | None = None,
    reverse: bool | None = None,
    offset: int | None = None,
    forward: bool | None = None,
    max_count: int | None = None,
    validate: bool | None = None,
  ) -> FuturesPremiumIndexSnapshotPage:
    """Get the premium index used in a futures contract's funding rate calculation -- the time-weighted average spread between mid-price and index price, normalized by index price -- either the current snapshot or a historical window.

    Args:
      symbol: Premium index symbol, e.g. `.XBTUSDTMPI` (see `futures.symbol`'s `premiumsSymbol1M`/`premiumsSymbol8H` fields for a given contract).
      start_at: Start of the window, Unix ms. Inclusive -- confirmed live (see notes).
      end_at: End of the window, Unix ms. Inclusive -- confirmed live (see notes).
      reverse: Reverse the sort order of results. Not independently verified live -- documented, left as-is.
      offset: Continuation offset for an alternative (undocumented-shape) paging mode -- see notes; this spec models the `startAt`/`endAt` bounded-window mode instead.
      forward: Direction for offset-based paging -- see `offset`.
      max_count: Maximum records to return. Confirmed live default `10` (an unfiltered call with no `maxCount` returned exactly 10 rows).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/futures-trading/market-data/get-premium-index)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    if start_at is not None:
      request['startAt'] = start_at
    if end_at is not None:
      request['endAt'] = end_at
    if reverse is not None:
      request['reverse'] = reverse
    if offset is not None:
      request['offset'] = offset
    if forward is not None:
      request['forward'] = forward
    if max_count is not None:
      request['maxCount'] = max_count
    return await self.request(
      request,
      method='GET',
      path='/api/v1/premium/query',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=FuturesPremiumIndexSnapshotPage,
    )
