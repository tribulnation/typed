# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import PositionDetail


class Request(TypedDict):
  currency: NotRequired[str]
  """Quote currency code to filter by, e.g. `USDT`, `XBT`, `ETH`. Queries all positions when omitted."""


Response = list[PositionDetail]


class GetPositionList(RpcEndpoint):
  """Get the account's futures positions, optionally filtered by quote currency. Returns an entry per symbol the account has (or has had) a position or margin-mode setting for; an account with none returns an empty array."""

  async def get_position_list(
    self,
    currency: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Get the account's futures positions, optionally filtered by quote currency. Returns an entry per symbol the account has (or has had) a position or margin-mode setting for; an account with none returns an empty array.

    Args:
      currency: Quote currency code to filter by, e.g. `USDT`, `XBT`, `ETH`. Queries all positions when omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470253)
    """
    request: Request = Request()
    if currency is not None:
      request['currency'] = currency
    return await self.request(
      request,
      method='GET',
      path='/api/v1/positions',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
