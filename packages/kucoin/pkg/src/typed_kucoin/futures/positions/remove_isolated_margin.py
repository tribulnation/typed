# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Symbol and amount for an isolated-margin withdrawal."""

  symbol: str
  """Symbol of the contract, e.g. `XBTUSDTM`."""
  withdrawAmount: Decimal
  """Margin amount to withdraw. If USDT-margined, denominated in USDT; if coin-margined, denominated in the settlement coin."""


Response = Decimal


class RemoveIsolatedMargin(RpcEndpoint):
  """Remove isolated margin manually from an existing isolated position."""

  async def remove_isolated_margin(
    self,
    symbol: str,
    *,
    withdraw_amount: Decimal,
    validate: bool | None = None,
  ) -> Response:
    """Remove isolated margin manually from an existing isolated position.

    Args:
      symbol: Symbol of the contract, e.g. `XBTUSDTM`.
      withdraw_amount: Margin amount to withdraw. If USDT-margined, denominated in USDT; if coin-margined, denominated in the settlement coin.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470256)
    """
    return await self.request(
      Request(symbol=symbol, withdrawAmount=withdraw_amount),
      method='POST',
      path='/api/v1/margin/withdrawMargin',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
