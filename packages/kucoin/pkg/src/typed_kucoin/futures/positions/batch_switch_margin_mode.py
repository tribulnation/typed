# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class BatchMarginModeError(TypedDict):
  """A single symbol's failure to switch margin mode."""

  code: str
  """Error code, e.g. `50002`."""
  msg: str
  """Error message, e.g. `exist.order.or.position`."""
  symbol: str
  """Symbol that failed to switch."""


class Request(TypedDict):
  """Target margin mode and the list of symbols to apply it to."""

  marginMode: Literal['ISOLATED', 'CROSS']
  """Margin mode to switch to: `ISOLATED` or `CROSS`."""
  symbols: list[str]
  """Symbols of the contracts to switch, e.g. `["XBTUSDTM", "ETHUSDTM"]`."""


class BatchMarginModeResult(TypedDict):
  """Result of a batch margin-mode switch."""

  marginMode: dict[str, Literal['ISOLATED', 'CROSS']]
  """Map of symbol to resulting margin mode. Symbols that failed to be modified are also included, mapped to their (unchanged) mode."""
  errors: list[BatchMarginModeError]
  """One entry per symbol that failed to be modified."""


class BatchSwitchMarginMode(RpcEndpoint):
  """Modify the margin mode of multiple futures contract symbols in one call. Symbols that fail to be modified are reported in `errors` alongside the ones that succeeded."""

  async def batch_switch_margin_mode(
    self,
    margin_mode: Literal['ISOLATED', 'CROSS'],
    *,
    symbols: list[str],
    validate: bool | None = None,
  ) -> BatchMarginModeResult:
    """Modify the margin mode of multiple futures contract symbols in one call. Symbols that fail to be modified are reported in `errors` alongside the ones that succeeded.

    Args:
      margin_mode: Margin mode to switch to: `ISOLATED` or `CROSS`.
      symbols: Symbols of the contracts to switch, e.g. `["XBTUSDTM", "ETHUSDTM"]`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3472403)
    """
    return await self.request(
      Request(marginMode=margin_mode, symbols=symbols),
      method='POST',
      path='/api/v2/position/batchChangeMarginMode',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BatchMarginModeResult,
    )
