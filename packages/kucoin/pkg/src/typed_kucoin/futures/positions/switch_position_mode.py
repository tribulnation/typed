# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class PositionModeAfterSwitch(TypedDict):
  """Position mode currently set for the account, after the switch."""

  positionMode: str
  """Position mode now in effect: `"0"` for one-way mode, `"1"` for hedge mode."""


class Request(TypedDict):
  """Target position mode to switch the account to."""

  positionMode: str
  """Target position mode: `"0"` for one-way mode, `"1"` for hedge mode."""


class SwitchPositionMode(RpcEndpoint):
  """Switch the account between one-way and hedge position mode. All open positions must be closed and all active orders cancelled first, or the venue rejects the switch."""

  async def switch_position_mode(
    self,
    position_mode: str,
    *,
    validate: bool | None = None,
  ) -> PositionModeAfterSwitch:
    """Switch the account between one-way and hedge position mode. All open positions must be closed and all active orders cancelled first, or the venue rejects the switch.

    Args:
      position_mode: Target position mode: `"0"` for one-way mode, `"1"` for hedge mode.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/futures-trading/positions/switch-position-mode)
    """
    return await self.request(
      Request(positionMode=position_mode),
      method='POST',
      path='/api/v2/position/switchPositionMode',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PositionModeAfterSwitch,
    )
