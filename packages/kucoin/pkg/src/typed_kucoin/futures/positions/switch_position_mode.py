# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /api/v2/position/switchPositionMode` — Switch Position Mode."""

from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint


class PositionModeAfterSwitch(TypedDict):
  """Position mode currently set for the account, after the switch."""

  positionMode: str
  """Position mode now in effect: `"0"` for one-way mode, `"1"` for hedge mode."""


class SwitchPositionModeParams(TypedDict):
  """Target position mode to switch the account to."""

  positionMode: str
  """Target position mode: `"0"` for one-way mode, `"1"` for hedge mode."""


_Type = PositionModeAfterSwitch
adapter = validator[_Type](_Type)  # type: ignore


class SwitchPositionMode(RpcEndpoint):
  """`Switch Position Mode` — mixed into `Positions`, the product exposing `futures.positions.switch_position_mode`."""

  async def switch_position_mode(
    self,
    switch_position_mode_params: SwitchPositionModeParams,
    *,
    validate: bool | None = None,
  ) -> PositionModeAfterSwitch:
    """Switch the account between one-way and hedge position mode. All open positions must be closed and all active orders cancelled first, or the venue rejects the switch.

    Args:
      switch_position_mode_params: Target position mode.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/position/switchPositionMode',
      json=switch_position_mode_params,
      validator=adapter,
      validate=validate,
    )
