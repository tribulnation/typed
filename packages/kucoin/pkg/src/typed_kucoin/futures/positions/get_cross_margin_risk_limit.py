# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class CrossMarginRiskLimit(TypedDict):
  """Cross-margin max-position-size figures for one symbol at a given margin/leverage."""

  symbol: str
  """Symbol of the contract."""
  maxOpenSize: int
  """Maximum amount of open position, in lots."""
  maxOpenValue: Decimal
  """Maximum value of open position, in the quote currency."""
  totalMargin: Decimal
  """Margin amount used for the max-position calculation."""
  price: Decimal
  """Price used for the max-position calculation. Defaults to the latest transaction price."""
  leverage: Decimal
  """Leverage used for the max-position calculation."""
  mmr: Decimal
  """Maintenance margin rate."""
  imr: Decimal
  """Initial margin rate."""
  currency: str
  """Margin currency."""


class Request(TypedDict):
  symbol: str
  """Symbol(s) of the contract, comma-separated, up to 50, e.g. `XBTUSDTM` or `XBTUSDTM,ETHUSDTM`."""
  totalMargin: NotRequired[Decimal]
  """Position-opening amount, in the contract's settlement currency, used for the max-position calculation. Defaults to 10,000 in margin currency (10,000 USD for USDT/USDC-settled contracts; 10,000 divided by the token's USDT price otherwise)."""
  leverage: NotRequired[int]
  """Leverage to calculate the max position size at. Defaults to the symbol's max cross leverage."""


Response = list[CrossMarginRiskLimit]


class GetCrossMarginRiskLimit(RpcEndpoint):
  """Batch-get the cross-margin max-position-size figures for up to 50 symbols at once, at a given margin amount and leverage."""

  async def get_cross_margin_risk_limit(
    self,
    symbol: str,
    *,
    total_margin: Decimal | None = None,
    leverage: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Batch-get the cross-margin max-position-size figures for up to 50 symbols at once, at a given margin amount and leverage.

    Args:
      symbol: Symbol(s) of the contract, comma-separated, up to 50, e.g. `XBTUSDTM` or `XBTUSDTM,ETHUSDTM`.
      total_margin: Position-opening amount, in the contract's settlement currency, used for the max-position calculation. Defaults to 10,000 in margin currency (10,000 USD for USDT/USDC-settled contracts; 10,000 divided by the token's USDT price otherwise).
      leverage: Leverage to calculate the max position size at. Defaults to the symbol's max cross leverage.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3472655)
    """
    request: Request = Request(symbol=symbol)
    if total_margin is not None:
      request['totalMargin'] = total_margin
    if leverage is not None:
      request['leverage'] = leverage
    return await self.request(
      request,
      method='GET',
      path='/api/v2/batchGetCrossOrderLimit',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
