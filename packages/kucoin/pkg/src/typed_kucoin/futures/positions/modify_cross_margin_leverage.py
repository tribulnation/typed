# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Symbol and target cross-margin leverage."""

  symbol: str
  """Symbol of the contract, e.g. `XBTUSDTM`."""
  leverage: Decimal
  """Leverage multiple to set."""


Response = bool


class ModifyCrossMarginLeverage(RpcEndpoint):
  """Modify the cross-margin leverage multiple set for a symbol."""

  async def modify_cross_margin_leverage(
    self,
    symbol: str,
    *,
    leverage: Decimal,
    validate: bool | None = None,
  ) -> Response:
    """Modify the cross-margin leverage multiple set for a symbol.

    Args:
      symbol: Symbol of the contract, e.g. `XBTUSDTM`.
      leverage: Leverage multiple to set.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470261)
    """
    return await self.request(
      Request(symbol=symbol, leverage=leverage),
      method='POST',
      path='/api/v2/changeCrossUserLeverage',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
