# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /api/v2/changeCrossUserLeverage` — Modify Cross Margin Leverage."""

from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint


class ModifyCrossMarginLeverageParams(TypedDict):
  """Symbol and target cross-margin leverage."""

  symbol: str
  """Symbol of the contract, e.g. `XBTUSDTM`."""
  leverage: str
  """Leverage multiple to set."""


_Type = bool
adapter = validator[_Type](_Type)  # type: ignore


class ModifyCrossMarginLeverage(RpcEndpoint):
  """`Modify Cross Margin Leverage` — mixed into `Positions`, the product exposing `futures.positions.modify_cross_margin_leverage`."""

  async def modify_cross_margin_leverage(
    self,
    modify_cross_margin_leverage_params: ModifyCrossMarginLeverageParams,
    *,
    validate: bool | None = None,
  ) -> bool:
    """Modify the cross-margin leverage multiple set for a symbol.

    Args:
      modify_cross_margin_leverage_params: Symbol and target cross-margin leverage.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/changeCrossUserLeverage',
      json=modify_cross_margin_leverage_params,
      validator=adapter,
      validate=validate,
    )
