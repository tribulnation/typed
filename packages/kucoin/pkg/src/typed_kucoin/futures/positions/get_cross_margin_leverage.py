# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class CrossMarginLeverage(TypedDict):
  """Cross-margin leverage currently set for a symbol."""

  symbol: str
  """Symbol of the contract."""
  leverage: Decimal
  """Leverage multiple."""


class Request(TypedDict):
  symbol: str
  """Symbol of the contract, e.g. `XBTUSDTM`."""


class GetCrossMarginLeverage(RpcEndpoint):
  """Query the current cross-margin leverage multiple set for a symbol."""

  async def get_cross_margin_leverage(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> CrossMarginLeverage:
    """Query the current cross-margin leverage multiple set for a symbol.

    Args:
      symbol: Symbol of the contract, e.g. `XBTUSDTM`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470260)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/api/v2/getCrossUserLeverage',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CrossMarginLeverage,
    )
