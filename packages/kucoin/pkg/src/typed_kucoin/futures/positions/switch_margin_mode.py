# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /api/v2/position/changeMarginMode` — Switch Margin Mode."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint


class MarginMode(TypedDict):
  """Margin mode currently set for a contract symbol."""

  symbol: str
  """Symbol of the contract."""
  marginMode: Literal['ISOLATED', 'CROSS']
  """Margin mode: `ISOLATED` (isolated margin) or `CROSS` (cross margin)."""


class SwitchMarginModeParams(TypedDict):
  """Symbol and target margin mode to switch to."""

  symbol: str
  """Symbol of the contract, e.g. `XBTUSDTM`."""
  marginMode: Literal['ISOLATED', 'CROSS']
  """Margin mode to switch to: `ISOLATED` or `CROSS`."""


_Type = MarginMode
adapter = validator[_Type](_Type)  # type: ignore


class SwitchMarginMode(RpcEndpoint):
  """`Switch Margin Mode` — mixed into `Positions`, the product exposing `futures.positions.switch_margin_mode`."""

  async def switch_margin_mode(
    self,
    switch_margin_mode_params: SwitchMarginModeParams,
    *,
    validate: bool | None = None,
  ) -> MarginMode:
    """Modify the margin mode (cross or isolated) of a futures contract symbol.

    Args:
      switch_margin_mode_params: Symbol and target margin mode to switch to.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/position/changeMarginMode',
      json=switch_margin_mode_params,
      validator=adapter,
      validate=validate,
    )
