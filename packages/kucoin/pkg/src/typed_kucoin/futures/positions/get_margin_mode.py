# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /api/v2/position/getMarginMode` — Get Margin Mode."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint


class MarginMode(TypedDict):
  """Margin mode currently set for a contract symbol."""

  symbol: str
  """Symbol of the contract."""
  marginMode: Literal['ISOLATED', 'CROSS']
  """Margin mode: `ISOLATED` (isolated margin) or `CROSS` (cross margin)."""


_Type = MarginMode
adapter = validator[_Type](_Type)  # type: ignore


class GetMarginMode(RpcEndpoint):
  """`Get Margin Mode` — mixed into `Positions`, the product exposing `futures.positions.get_margin_mode`."""

  async def get_margin_mode(
    self, *, symbol: str, validate: bool | None = None
  ) -> MarginMode:
    """Query the margin mode (cross or isolated) currently set for a futures contract symbol.

    Args:
      symbol: Symbol of the contract, e.g. `XBTUSDTM`.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    params: dict = {
      'symbol': symbol,
    }
    return await self.authed_request(
      'GET',
      '/api/v2/position/getMarginMode',
      params=params,
      validator=adapter,
      validate=validate,
    )
