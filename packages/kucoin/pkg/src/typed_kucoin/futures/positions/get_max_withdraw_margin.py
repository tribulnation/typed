# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  symbol: str
  """Symbol of the contract, e.g. `XBTUSDTM`."""


Response = str


class GetMaxWithdrawMargin(RpcEndpoint):
  """Get the maximum amount of margin that the current position for a symbol supports withdrawing (isolated margin only -- see notes)."""

  async def get_max_withdraw_margin(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Get the maximum amount of margin that the current position for a symbol supports withdrawing (isolated margin only -- see notes).

    Args:
      symbol: Symbol of the contract, e.g. `XBTUSDTM`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470258)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/api/v1/margin/maxWithdrawMargin',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
