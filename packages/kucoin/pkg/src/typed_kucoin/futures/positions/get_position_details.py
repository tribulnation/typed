# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import PositionDetail


class Request(TypedDict):
  symbol: str
  """Symbol of the contract, e.g. `XBTUSDTM`."""


class GetPositionDetails(RpcEndpoint):
  """Get the position details for a specified futures contract symbol. Always returns one record per symbol, even when there is no open position -- in that case most numeric fields are zeroed rather than the call erroring or returning nothing."""

  async def get_position_details(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> PositionDetail:
    """Get the position details for a specified futures contract symbol. Always returns one record per symbol, even when there is no open position -- in that case most numeric fields are zeroed rather than the call erroring or returning nothing.

    Args:
      symbol: Symbol of the contract, e.g. `XBTUSDTM`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470252)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/api/v1/position',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PositionDetail,
    )
