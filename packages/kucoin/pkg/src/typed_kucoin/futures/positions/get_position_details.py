# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /api/v1/position` — Get Position Details."""

from typed_core.validation import validator
from typed_kucoin.types import PositionDetail
from typed_kucoin.core import RpcEndpoint


_Type = PositionDetail
adapter = validator[_Type](_Type)  # type: ignore


class GetPositionDetails(RpcEndpoint):
  """`Get Position Details` — mixed into `Positions`, the product exposing `futures.positions.get_position_details`."""

  async def get_position_details(
    self,
    *,
    symbol: str,
    validate: bool | None = None,
  ) -> PositionDetail:
    """Get the position details for a specified futures contract symbol. Always returns one record per symbol, even when there is no open position -- in that case most numeric fields are zeroed rather than the call erroring or returning nothing.

    Args:
      symbol: Symbol of the contract, e.g. `XBTUSDTM`.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    params: dict = {
      'symbol': symbol,
    }
    return await self.authed_request(
      'GET',
      '/api/v1/position',
      params=params,
      validator=adapter,
      validate=validate,
    )
