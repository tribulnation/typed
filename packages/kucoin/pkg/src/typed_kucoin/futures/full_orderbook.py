# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import FuturesOrderBook


class Request(TypedDict):
  symbol: str
  """Contract symbol, e.g. `XBTUSDTM`."""


class FullOrderbook(RpcEndpoint):
  """Get the complete order book depth, every price level, for one futures contract. Resource-intensive and strictly rate-limited -- the docs recommend one initial call here, then maintaining a local book from the WebSocket incremental feed."""

  async def full_orderbook(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> FuturesOrderBook:
    """Get the complete order book depth, every price level, for one futures contract. Resource-intensive and strictly rate-limited -- the docs recommend one initial call here, then maintaining a local book from the WebSocket incremental feed.

    Args:
      symbol: Contract symbol, e.g. `XBTUSDTM`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/futures-trading/market-data/get-full-orderbook)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/api/v1/level2/snapshot',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=FuturesOrderBook,
    )
