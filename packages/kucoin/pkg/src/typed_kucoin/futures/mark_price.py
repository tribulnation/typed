# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core import TimestampMillis
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class FuturesMarkPrice(TypedDict):
  """Current mark price snapshot for one contract."""

  symbol: str
  """Contract symbol."""
  granularity: int
  """Snapshot update interval, milliseconds (observed `1000` = 1 second). A raw interval value, not a documented closed set of categories -- left bare rather than an enum."""
  timePoint: TimestampMillis
  """Snapshot timestamp, Unix ms."""
  value: float
  """Current mark price."""
  indexPrice: float
  """Current spot index price backing this mark price."""


class Request(TypedDict):
  symbol: str
  """Contract symbol, e.g. `XBTUSDTM`."""


class MarkPrice(RpcEndpoint):
  """Get the current mark price for one futures contract. Snapshots update once per second."""

  async def mark_price(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> FuturesMarkPrice:
    """Get the current mark price for one futures contract. Snapshots update once per second.

    Args:
      symbol: Contract symbol, e.g. `XBTUSDTM`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/futures-trading/market-data/get-mark-price)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/api/v1/mark-price/{symbol}/current',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=FuturesMarkPrice,
    )
