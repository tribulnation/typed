# Generated by typed-dev — see CONTRIBUTING.md.
"""`DELETE /api/v3/orders` — Cancel All Orders."""

from typed_core.validation import validator
from typed_kucoin.types import FuturesCancelResult
from typed_kucoin.core import RpcEndpoint


_Type = FuturesCancelResult
adapter = validator[_Type](_Type)  # type: ignore


class CancelAll(RpcEndpoint):
  """`Cancel All Orders` — mixed into `Orders`, the product exposing `futures.orders.cancel_all`."""

  async def cancel_all(
    self,
    *,
    symbol: str | None = None,
    validate: bool | None = None,
  ) -> FuturesCancelResult:
    """Cancel all open orders, excluding stop orders. Optionally scoped to one contract symbol.

    Args:
      symbol: Cancel only this contract's orders; cancels every open order account-wide when omitted.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    params = {}
    if symbol is not None:
      params['symbol'] = symbol
    return await self.authed_request(
      'DELETE',
      '/api/v3/orders',
      params=params,
      validator=adapter,
      validate=validate,
    )
