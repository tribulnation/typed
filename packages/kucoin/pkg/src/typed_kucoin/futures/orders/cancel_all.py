# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import FuturesCancelResult


class Request(TypedDict):
  symbol: NotRequired[str]
  """Cancel only this contract's orders; cancels every open order account-wide when omitted."""


class CancelAll(RpcEndpoint):
  """Cancel all open orders, excluding stop orders. Optionally scoped to one contract symbol."""

  async def cancel_all(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> FuturesCancelResult:
    """Cancel all open orders, excluding stop orders. Optionally scoped to one contract symbol.

    Args:
      symbol: Cancel only this contract's orders; cancels every open order account-wide when omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/futures-trading/orders/cancel-all-orders)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='DELETE',
      path='/api/v3/orders',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FuturesCancelResult,
    )
