# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /api/v1/orders` — Add Order."""

from typed_core.validation import validator
from typed_kucoin.types import (
  FuturesAddOrderLimit,
  FuturesAddOrderMarket,
  FuturesAddOrderResult,
)
from typed_kucoin.core import RpcEndpoint


_Type = FuturesAddOrderResult
adapter = validator[_Type](_Type)  # type: ignore


class Add(RpcEndpoint):
  """`Add Order` — mixed into `Orders`, the product exposing `futures.orders.add`."""

  async def add(
    self,
    body: FuturesAddOrderLimit | FuturesAddOrderMarket,
    *,
    validate: bool | None = None,
  ) -> FuturesAddOrderResult:
    """Place a limit or market order in the futures trading system. Orders can only be placed if the account has sufficient funds; once placed, funds are held for the order's duration. Up to 100 limit orders and 200 stop orders may be open per contract, per account.

    Args:
      body: Order placement parameters.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    return await self.authed_request(
      'POST',
      '/api/v1/orders',
      json=body,
      validator=adapter,
      validate=validate,
    )
