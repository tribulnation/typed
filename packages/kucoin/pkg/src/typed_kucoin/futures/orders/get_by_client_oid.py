# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import FuturesOrder


class Request(TypedDict):
  clientOid: str
  """Client-provided id of the order to look up."""


class GetByClientOid(RpcEndpoint):
  """Get a single futures order (including a stop order) by its client-provided order id."""

  async def get_by_client_oid(
    self,
    client_oid: str,
    *,
    validate: bool | None = None,
  ) -> FuturesOrder:
    """Get a single futures order (including a stop order) by its client-provided order id.

    Args:
      client_oid: Client-provided id of the order to look up.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/futures-trading/get-stop-order-by-clientoid)
    """
    return await self.request(
      Request(clientOid=client_oid),
      method='GET',
      path='/api/v1/orders/byClientOid',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FuturesOrder,
    )
