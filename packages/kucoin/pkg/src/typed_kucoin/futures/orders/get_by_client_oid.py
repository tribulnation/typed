# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /api/v1/orders/byClientOid` — Get Order By ClientOid."""

from typed_core.validation import validator
from typed_kucoin.types import FuturesOrder
from typed_kucoin.core import RpcEndpoint


_Type = FuturesOrder
adapter = validator[_Type](_Type)  # type: ignore


class GetByClientOid(RpcEndpoint):
  """`Get Order By ClientOid` — mixed into `Orders`, the product exposing `futures.orders.get_by_client_oid`."""

  async def get_by_client_oid(
    self,
    *,
    client_oid: str,
    validate: bool | None = None,
  ) -> FuturesOrder:
    """Get a single futures order (including a stop order) by its client-provided order id.

    Args:
      client_oid: Client-provided id of the order to look up.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    params: dict = {
      'clientOid': client_oid,
    }
    return await self.authed_request(
      'GET',
      '/api/v1/orders/byClientOid',
      params=params,
      validator=adapter,
      validate=validate,
    )
