# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_kucoin.core import TimestampMillis
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import FuturesOrder


class Request(TypedDict):
  status: NotRequired[Literal['active', 'done']]
  """Restrict to active or done orders."""
  symbol: NotRequired[str]
  """Contract symbol to filter by."""
  side: NotRequired[Literal['buy', 'sell']]
  """Order side to filter by."""
  type: NotRequired[
    Literal['limit', 'market', 'limit_stop', 'market_stop', 'oco_limit', 'oco_stop']
  ]
  """Order type to filter by."""
  startAt: NotRequired[TimestampMillis]
  """Start of the time window, Unix milliseconds."""
  endAt: NotRequired[TimestampMillis]
  """End of the time window, Unix milliseconds."""
  currentPage: NotRequired[int]
  """Page number."""
  pageSize: NotRequired[int]
  """Results per page."""


class FuturesOrderPage(TypedDict):
  """One page of futures orders."""

  currentPage: int
  """Current page number."""
  pageSize: int
  """Results per page, as requested."""
  totalNum: int
  """Total matching orders."""
  totalPage: int
  """Total number of pages."""
  items: list[FuturesOrder]
  """Orders on this page."""


class GetOrderList(RpcEndpoint):
  """List this account's futures orders. `status=done` (the default) is limited to a 168-hour (7-day) query window between `startAt`/`endAt`; `status=active` has no such limit."""

  async def get_order_list_paged(
    self,
    status: Literal['active', 'done'] | None = None,
    *,
    symbol: str | None = None,
    side: Literal['buy', 'sell'] | None = None,
    type: Literal[
      'limit', 'market', 'limit_stop', 'market_stop', 'oco_limit', 'oco_stop'
    ]
    | None = None,
    start_at: TimestampMillis | None = None,
    end_at: TimestampMillis | None = None,
    page_size: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[FuturesOrderPage]:
    """List this account's futures orders. `status=done` (the default) is limited to a 168-hour (7-day) query window between `startAt`/`endAt`; `status=active` has no such limit.

    Paged variant of `get_order_list`: Requests `currentPage` from 1 upwards and stops once it has covered the `totalPage` pages the response reports, or after `max_pages` pages when one is given.

    Args:
      status: Restrict to active or done orders.
      symbol: Contract symbol to filter by.
      side: Order side to filter by.
      type: Order type to filter by.
      start_at: Start of the time window, Unix milliseconds.
      end_at: End of the time window, Unix milliseconds.
      page_size: Results per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/futures-trading/orders/get-order-list)
    """
    current_page = 1
    pages = 0
    while True:
      response = await self.get_order_list(
        status,
        symbol=symbol,
        side=side,
        type=type,
        start_at=start_at,
        end_at=end_at,
        page_size=page_size,
        current_page=current_page,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      total = response.get('totalPage') if response is not None else None
      total = int(total) if total is not None else None
      if total is None or pages >= total:
        break
      current_page += 1

  async def get_order_list(
    self,
    status: Literal['active', 'done'] | None = None,
    *,
    symbol: str | None = None,
    side: Literal['buy', 'sell'] | None = None,
    type: Literal[
      'limit', 'market', 'limit_stop', 'market_stop', 'oco_limit', 'oco_stop'
    ]
    | None = None,
    start_at: TimestampMillis | None = None,
    end_at: TimestampMillis | None = None,
    current_page: int | None = None,
    page_size: int | None = None,
    validate: bool | None = None,
  ) -> FuturesOrderPage:
    """List this account's futures orders. `status=done` (the default) is limited to a 168-hour (7-day) query window between `startAt`/`endAt`; `status=active` has no such limit.

    Args:
      status: Restrict to active or done orders.
      symbol: Contract symbol to filter by.
      side: Order side to filter by.
      type: Order type to filter by.
      start_at: Start of the time window, Unix milliseconds.
      end_at: End of the time window, Unix milliseconds.
      current_page: Page number.
      page_size: Results per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/futures-trading/orders/get-order-list)
    """
    request: Request = Request()
    if status is not None:
      request['status'] = status
    if symbol is not None:
      request['symbol'] = symbol
    if side is not None:
      request['side'] = side
    if type is not None:
      request['type'] = type
    if start_at is not None:
      request['startAt'] = start_at
    if end_at is not None:
      request['endAt'] = end_at
    if current_page is not None:
      request['currentPage'] = current_page
    if page_size is not None:
      request['pageSize'] = page_size
    return await self.request(
      request,
      method='GET',
      path='/api/v1/orders',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FuturesOrderPage,
    )
