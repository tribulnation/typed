# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import ClientOidRef, FuturesBatchCancelResult


class Request(TypedDict):
  """Choose one of `orderIdsList`/`clientOidsList`."""

  orderIdsList: NotRequired[list[str]]
  """System-generated order ids to cancel. Max 10."""
  clientOidsList: NotRequired[list[ClientOidRef]]
  """Client-provided order ids to cancel, with their symbols. Max 10."""


Response = list[FuturesBatchCancelResult]


class BatchCancel(RpcEndpoint):
  """Cancel up to 10 orders in one request, by system-generated order id or by client-provided order id. If both `orderIdsList`/`clientOidsList` are given, `orderIdsList` takes precedence. Cancelling by `clientOidsList` only affects normal orders; conditional (stop) orders sharing the same `clientOid` are left untouched."""

  async def batch_cancel(
    self,
    order_ids_list: list[str] | None = None,
    *,
    client_oids_list: list[ClientOidRef] | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Cancel up to 10 orders in one request, by system-generated order id or by client-provided order id. If both `orderIdsList`/`clientOidsList` are given, `orderIdsList` takes precedence. Cancelling by `clientOidsList` only affects normal orders; conditional (stop) orders sharing the same `clientOid` are left untouched.

    Args:
      order_ids_list: System-generated order ids to cancel. Max 10.
      client_oids_list: Client-provided order ids to cancel, with their symbols. Max 10.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/3470241e0)
    """
    request: Request = Request()
    if order_ids_list is not None:
      request['orderIdsList'] = order_ids_list
    if client_oids_list is not None:
      request['clientOidsList'] = client_oids_list
    return await self.request(
      request,
      method='DELETE',
      path='/api/v1/orders/multi-cancel',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
