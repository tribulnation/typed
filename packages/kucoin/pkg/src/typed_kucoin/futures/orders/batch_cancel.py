# Generated by typed-dev — see CONTRIBUTING.md.
"""`DELETE /api/v1/orders/multi-cancel` — Batch Cancel Orders."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint


class FuturesBatchCancelClientOid(TypedDict):
  """One client-provided order id to cancel."""

  symbol: str
  """Contract symbol the order belongs to."""
  clientOid: str
  """Client-provided order id."""


class FuturesBatchCancelResult(TypedDict):
  """One order's cancellation result -- success or failure."""

  orderId: str | None
  """Cancelled order id, when cancelling by `orderIdsList`; `null` when cancelling by `clientOidsList`."""
  clientOid: str | None
  """Cancelled client order id, when cancelling by `clientOidsList`; `null` when cancelling by `orderIdsList`."""
  code: str
  """This item's own result code -- `200000` on success, an error code otherwise."""
  msg: str
  """This item's own result message."""


class FuturesBatchCancelRequest(TypedDict):
  """Choose one of `orderIdsList`/`clientOidsList`."""

  orderIdsList: NotRequired[list[str]]
  """System-generated order ids to cancel. Max 10."""
  clientOidsList: NotRequired[list[FuturesBatchCancelClientOid]]
  """Client-provided order ids to cancel, with their symbols. Max 10."""


_Type = list[FuturesBatchCancelResult]
adapter = validator[_Type](_Type)  # type: ignore


class BatchCancel(RpcEndpoint):
  """`Batch Cancel Orders` — mixed into `Orders`, the product exposing `futures.orders.batch_cancel`."""

  async def batch_cancel(
    self,
    futures_batch_cancel_request: FuturesBatchCancelRequest,
    *,
    validate: bool | None = None,
  ) -> list[FuturesBatchCancelResult]:
    """Cancel up to 10 orders in one request, by system-generated order id or by client-provided order id. If both `orderIdsList`/`clientOidsList` are given, `orderIdsList` takes precedence. Cancelling by `clientOidsList` only affects normal orders; conditional (stop) orders sharing the same `clientOid` are left untouched.

    Args:
      futures_batch_cancel_request: Orders to cancel, by order id or by client order id.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    return await self.authed_request(
      'DELETE',
      '/api/v1/orders/multi-cancel',
      json=futures_batch_cancel_request,
      validator=adapter,
      validate=validate,
    )
