# Generated by typed-dev — see CONTRIBUTING.md.
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import (
  FuturesAddOrderLimit,
  FuturesAddOrderMarket,
  FuturesBatchAddOrderResult,
)


Request = list[FuturesAddOrderLimit | FuturesAddOrderMarket]


Response = list[FuturesBatchAddOrderResult]


class BatchAdd(RpcEndpoint):
  """Place up to 20 limit or market orders in a single request. Each item is validated and placed independently -- one order failing does not prevent the others from being placed."""

  async def batch_add(
    self,
    futures_batch_add_order_request: Request,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Place up to 20 limit or market orders in a single request. Each item is validated and placed independently -- one order failing does not prevent the others from being placed.

    Args:
      futures_batch_add_order_request: Orders to place, one item per order.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/futures-trading/orders/batch-add-orders)
    """
    return await self.request(
      futures_batch_add_order_request,
      method='POST',
      path='/api/v1/orders/multi',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
