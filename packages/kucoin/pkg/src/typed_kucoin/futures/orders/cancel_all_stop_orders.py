# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import FuturesCancelResult


class Request(TypedDict):
  symbol: NotRequired[str]
  """Cancel only this contract's untriggered stop orders; cancels every untriggered stop order account-wide when omitted."""


class CancelAllStopOrders(RpcEndpoint):
  """Cancel all untriggered stop orders. Optionally scoped to one contract symbol. A triggered stop order behaves like a normal order at that point and must be cancelled through Cancel Order By OrderId/ClientOid or Cancel All Orders instead."""

  async def cancel_all_stop_orders(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> FuturesCancelResult:
    """Cancel all untriggered stop orders. Optionally scoped to one contract symbol. A triggered stop order behaves like a normal order at that point and must be cancelled through Cancel Order By OrderId/ClientOid or Cancel All Orders instead.

    Args:
      symbol: Cancel only this contract's untriggered stop orders; cancels every untriggered stop order account-wide when omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/futures-trading/orders/cancel-all-stop-orders)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='DELETE',
      path='/api/v1/stopOrders',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FuturesCancelResult,
    )
