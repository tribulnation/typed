# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import FuturesOrder


class Request(TypedDict):
  symbol: NotRequired[str]
  """Contract symbol to filter by."""


Response = list[FuturesOrder]


class GetRecentClosedOrders(RpcEndpoint):
  """Get up to 1000 orders closed (filled or cancelled) in the last 24 hours -- a lower-latency alternative to Get Order List for recent activity."""

  async def get_recent_closed_orders(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Get up to 1000 orders closed (filled or cancelled) in the last 24 hours -- a lower-latency alternative to Get Order List for recent activity.

    Args:
      symbol: Contract symbol to filter by.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/futures-trading/orders/get-recent-closed-orders)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='/api/v1/recentDoneOrders',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
