# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import FuturesTrade


class Request(TypedDict):
  symbol: NotRequired[str]
  """Contract symbol to filter by."""


Response = list[FuturesTrade]


class GetRecentTradeHistory(RpcEndpoint):
  """Get up to 1000 of this account's own fills from the last 24 hours -- a lower-latency alternative to Get Trade History for recent activity. Data is not guaranteed real-time."""

  async def get_recent_trade_history(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Get up to 1000 of this account's own fills from the last 24 hours -- a lower-latency alternative to Get Trade History for recent activity. Data is not guaranteed real-time.

    Args:
      symbol: Contract symbol to filter by.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/futures-trading/orders/get-recent-trade-history)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='/api/v1/recentFills',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
