# Generated by typed-dev — see CONTRIBUTING.md.
"""`DELETE /api/v1/orders/client-order/{clientOid}` — Cancel Order By ClientOid."""

from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint


class FuturesCancelByClientOidResult(TypedDict):
  """Confirmation of the cancelled order."""

  clientOid: str
  """Client-provided order id, echoed back."""


_Type = FuturesCancelByClientOidResult
adapter = validator[_Type](_Type)  # type: ignore


class CancelByClientOid(RpcEndpoint):
  """`Cancel Order By ClientOid` — mixed into `Orders`, the product exposing `futures.orders.cancel_by_client_oid`."""

  async def cancel_by_client_oid(
    self,
    client_oid: str,
    *,
    symbol: str,
    validate: bool | None = None,
  ) -> FuturesCancelByClientOidResult:
    """Cancel a futures order (including a stop order) by its client-provided order id. The request is acknowledged immediately; the matching engine processes the cancellation asynchronously. Fails if the order is already filled or previously cancelled.

    Args:
      client_oid: Client-provided id of the order to cancel.
      symbol: Contract symbol the order belongs to.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    params: dict = {
      'symbol': symbol,
    }
    return await self.authed_request(
      'DELETE',
      f'/api/v1/orders/client-order/{client_oid}',
      params=params,
      validator=adapter,
      validate=validate,
    )
