# Generated by typed-dev — see CONTRIBUTING.md.
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import (
  FuturesAddOrderLimit,
  FuturesAddOrderMarket,
  FuturesAddOrderResult,
)


Request = FuturesAddOrderLimit | FuturesAddOrderMarket


class AddTest(RpcEndpoint):
  """Validate a futures order placement request -- parameters, signature -- without ever entering the matching engine. An order placed through this endpoint cannot be queried afterwards by any other endpoint."""

  async def add_test(
    self,
    request: Request,
    *,
    validate: bool | None = None,
  ) -> FuturesAddOrderResult:
    """Validate a futures order placement request -- parameters, signature -- without ever entering the matching engine. An order placed through this endpoint cannot be queried afterwards by any other endpoint.

    Args:
      request:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/futures-trading/orders/add-order-test)
    """
    return await self.request(
      request,
      method='POST',
      path='/api/v1/orders/test',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FuturesAddOrderResult,
    )
