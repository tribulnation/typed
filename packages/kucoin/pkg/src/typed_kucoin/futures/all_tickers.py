# Generated by typed-dev — see CONTRIBUTING.md.
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import FuturesTicker


FuturesTickerList = list[FuturesTicker]


class AllTickers(RpcEndpoint):
  """Get the last trade and best bid/ask for every futures contract -- the same shape as `futures.ticker`, for every symbol at once. The equivalent data is also available via WebSocket."""

  async def all_tickers(self, *, validate: bool | None = None) -> FuturesTickerList:
    """Get the last trade and best bid/ask for every futures contract -- the same shape as `futures.ticker`, for every symbol at once. The equivalent data is also available via WebSocket.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/futures-trading/market-data/get-all-tickers)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v1/allTickers',
      meta={},
      validate=validate,
      response_type=FuturesTickerList,
    )
