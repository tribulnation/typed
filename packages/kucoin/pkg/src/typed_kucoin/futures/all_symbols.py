# Generated by typed-dev — see CONTRIBUTING.md.
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import FuturesContract


FuturesContractList = list[FuturesContract]


class AllSymbols(RpcEndpoint):
  """List detailed contract configuration for every actively tradable futures contract -- the same shape as `futures.symbol`, for every symbol at once."""

  async def all_symbols(self, *, validate: bool | None = None) -> FuturesContractList:
    """List detailed contract configuration for every actively tradable futures contract -- the same shape as `futures.symbol`, for every symbol at once.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/futures-trading/market-data/get-all-symbols)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v1/contracts/active',
      meta={},
      validate=validate,
      response_type=FuturesContractList,
    )
