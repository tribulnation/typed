# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import FuturesTicker


class Request(TypedDict):
  symbol: str
  """Contract symbol, e.g. `XBTUSDTM`."""


class Ticker(RpcEndpoint):
  """Get the last trade and best bid/ask for one futures contract. The equivalent data is also available via WebSocket."""

  async def ticker(self, symbol: str, *, validate: bool | None = None) -> FuturesTicker:
    """Get the last trade and best bid/ask for one futures contract. The equivalent data is also available via WebSocket.

    Args:
      symbol: Contract symbol, e.g. `XBTUSDTM`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/futures-trading/market-data/get-ticker)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/api/v1/ticker',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=FuturesTicker,
    )
