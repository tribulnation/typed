# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /api/v3/deposit-address/create` — account.deposit.add."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint, TimestampMillis


class AddDepositAddressRequest(TypedDict):
  currency: str
  """Currency to create an address for, e.g. `BTC`, `ETH`, `USDT`."""
  chain: str
  """Chain identifier, e.g. `eth`, `bech32`, `btc`, `kcc`, `trx`, `bsc`, `arbitrum`, `ton`, `optimism`. For USDT: `OMNI`/`ERC20`/`TRC20` (default `ERC20`); for BTC: `Native`/`Segwit`/`TRC20` passed as `bech32`/`btc`/`trx` (default `Native`)."""
  to: NotRequired[Literal['main', 'trade']]
  """Destination account for the new address."""
  amount: NotRequired[str]
  """Deposit amount; only meaningful when applying for a Lightning Network invoice, ignored otherwise."""


class DepositAddressV3(TypedDict):
  address: str
  """The generated deposit address."""
  memo: str
  """Address remark/tag; empty string if none. Must be supplied when withdrawing from another platform to this address if non-empty, or the deposit may not be credited."""
  chainId: str
  """Chain identifier, e.g. `eth`, `bech32`, `btc`, `kcc`, `trx`, `bsc`, `arbitrum`, `ton`, `optimism`."""
  to: Literal['MAIN', 'TRADE']
  """Destination account type actually used."""
  expirationDate: TimestampMillis
  """Expiration timestamp for a Lightning Network invoice; `0` otherwise."""
  currency: str
  """Currency."""
  chainName: str
  """Human-readable chain name, e.g. `TON`, `ERC20`, `TRC20`."""


_Type = DepositAddressV3
adapter = validator[_Type](_Type)  # type: ignore


class Add(RpcEndpoint):
  """`account.deposit.add` — mixed into `Deposit`, the product exposing `account.deposit.add`."""

  async def add(
    self,
    add_deposit_address_request: AddDepositAddressRequest,
    *,
    validate: bool | None = None,
  ) -> DepositAddressV3:
    """Request the creation of a deposit address for a currency the caller intends to deposit.

    Args:
      add_deposit_address_request: Currency/chain to provision a deposit address for.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    return await self.authed_request(
      'POST',
      '/api/v3/deposit-address/create',
      json=add_deposit_address_request,
      validator=adapter,
      validate=validate,
    )
