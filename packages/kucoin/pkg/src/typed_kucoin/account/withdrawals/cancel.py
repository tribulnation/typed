# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  withdrawalId: str
  """Unique id of the withdrawal to cancel."""


class Cancel(RpcEndpoint):
  """Cancel a withdrawal. Only withdrawal requests currently in `PROCESSING` status can be canceled."""

  async def cancel(self, withdrawal_id: str, *, validate: bool | None = None) -> None:
    """Cancel a withdrawal. Only withdrawal requests currently in `PROCESSING` status can be canceled.

    Args:
      withdrawal_id: Unique id of the withdrawal to cancel.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470144)
    """
    return await self.request(
      Request(withdrawalId=withdrawal_id),
      method='DELETE',
      path='/api/v1/withdrawals/{withdrawalId}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=None,
    )
