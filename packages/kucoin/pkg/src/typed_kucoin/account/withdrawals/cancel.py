# Generated by typed-dev — see CONTRIBUTING.md.
"""`DELETE /api/v1/withdrawals/{withdrawalId}` — account.withdrawals.cancel."""

from typed_kucoin.core import RpcEndpoint


class Cancel(RpcEndpoint):
  """`account.withdrawals.cancel` — mixed into `Withdrawals`, the product exposing `account.withdrawals.cancel`."""

  async def cancel(self, withdrawal_id: str, *, validate: bool | None = None) -> None:
    """Cancel a withdrawal. Only withdrawal requests currently in `PROCESSING` status can be canceled.

    Args:
      withdrawal_id: Unique id of the withdrawal to cancel.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    await self.authed_request(
      'DELETE',
      f'/api/v1/withdrawals/{withdrawal_id}',
      validate=validate,
    )
