# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class DeleteSubAccountApiResult(TypedDict):
  subName: str
  """Sub-account name the deleted key belonged to."""
  apiKey: str
  """The deleted API key's identifier."""


class Request(TypedDict):
  apiKey: str
  """API key identifier to delete."""
  subName: str
  """Sub-account name the key belongs to."""
  passphrase: str
  """The key's existing API passphrase, required to authorize the deletion."""


class Delete(RpcEndpoint):
  """Delete an existing sub-account API key. The key's passphrase must be supplied to authorize the deletion."""

  async def delete(
    self,
    *,
    api_key: str,
    sub_name: str,
    passphrase: str,
    validate: bool | None = None,
  ) -> DeleteSubAccountApiResult:
    """Delete an existing sub-account API key. The key's passphrase must be supplied to authorize the deletion.

    Args:
      api_key: API key identifier to delete.
      sub_name: Sub-account name the key belongs to.
      passphrase: The key's existing API passphrase, required to authorize the deletion.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/account-info/sub-account-api/delete-subaccount-api)
    """
    return await self.request(
      Request(apiKey=api_key, subName=sub_name, passphrase=passphrase),
      method='DELETE',
      path='/api/v1/sub/api-key',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=DeleteSubAccountApiResult,
    )
