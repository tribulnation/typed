# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_kucoin.core import TimestampMillis
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class AddSubAccountApiResult(TypedDict):
  subName: str
  """Sub-account name."""
  remark: str
  """Remark passed at creation."""
  apiKey: str
  """Newly created API key identifier."""
  apiSecret: str
  """Newly created API secret. Shown only in this response -- KuCoin never returns it again."""
  apiVersion: int
  """API key version."""
  passphrase: str
  """The passphrase passed at creation, echoed back."""
  permission: str
  """Permission(s) granted to the key."""
  createdAt: TimestampMillis
  """Time the key was created."""


class Request(TypedDict):
  subName: str
  """Sub-account name to create the key for."""
  passphrase: str
  """API passphrase for the new key: 7-32 characters, no spaces."""
  remark: str
  """Optional label/description for the key: 1-24 characters."""
  permission: NotRequired[str]
  """Permission(s) to grant the key, comma-joined (e.g. `General,Spot,Futures,Unified`). Defaults to `General` when omitted -- see notes."""
  ipWhitelist: NotRequired[str]
  """Comma-separated IP addresses allowed to use this key. Unrestricted when omitted."""
  expire: NotRequired[Literal['-1', '30', '90', '180', '360']]
  """How many days until the key expires. Documented only in `kucoin-universal-sdk`'s machine-readable spec, not on the live docs-new page -- see notes."""


class Add(RpcEndpoint):
  """Create a new API key for an existing sub-account. Issues a real, usable credential (key, secret and passphrase) scoped to whatever permission(s) are granted."""

  async def add(
    self,
    *,
    sub_name: str,
    passphrase: str,
    remark: str,
    permission: str | None = None,
    ip_whitelist: str | None = None,
    expire: Literal['-1', '30', '90', '180', '360'] | None = None,
    validate: bool | None = None,
  ) -> AddSubAccountApiResult:
    """Create a new API key for an existing sub-account. Issues a real, usable credential (key, secret and passphrase) scoped to whatever permission(s) are granted.

    Args:
      sub_name: Sub-account name to create the key for.
      passphrase: API passphrase for the new key: 7-32 characters, no spaces.
      remark: Optional label/description for the key: 1-24 characters.
      permission: Permission(s) to grant the key, comma-joined (e.g. `General,Spot,Futures,Unified`). Defaults to `General` when omitted -- see notes.
      ip_whitelist: Comma-separated IP addresses allowed to use this key. Unrestricted when omitted.
      expire: How many days until the key expires. Documented only in `kucoin-universal-sdk`'s machine-readable spec, not on the live docs-new page -- see notes.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/account-info/sub-account-api/add-subaccount-api)
    """
    request: Request = Request(subName=sub_name, passphrase=passphrase, remark=remark)
    if permission is not None:
      request['permission'] = permission
    if ip_whitelist is not None:
      request['ipWhitelist'] = ip_whitelist
    if expire is not None:
      request['expire'] = expire
    return await self.request(
      request,
      method='POST',
      path='/api/v1/sub/api-key',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=AddSubAccountApiResult,
    )
