# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class ModifySubAccountApiResult(TypedDict):
  subName: str
  """Sub-account name."""
  apiKey: str
  """API key identifier."""
  permission: str
  """Updated permission(s)."""
  ipWhitelist: str
  """Updated IP whitelist."""


class Request(TypedDict):
  subName: str
  """Sub-account name the key belongs to."""
  apiKey: str
  """API key identifier to modify."""
  passphrase: str
  """The key's existing API passphrase, required to authorize the change."""
  permission: str
  """New permission(s) for the key, comma-joined (e.g. `General,Spot,Unified`)."""
  ipWhitelist: NotRequired[str]
  """New comma-separated IP whitelist for the key. Unrestricted when omitted."""


class Modify(RpcEndpoint):
  """Modify the permission(s) and/or IP whitelist of an existing sub-account API key. The key's passphrase must be supplied to authorize the change."""

  async def modify(
    self,
    *,
    sub_name: str,
    api_key: str,
    passphrase: str,
    permission: str,
    ip_whitelist: str | None = None,
    validate: bool | None = None,
  ) -> ModifySubAccountApiResult:
    """Modify the permission(s) and/or IP whitelist of an existing sub-account API key. The key's passphrase must be supplied to authorize the change.

    Args:
      sub_name: Sub-account name the key belongs to.
      api_key: API key identifier to modify.
      passphrase: The key's existing API passphrase, required to authorize the change.
      permission: New permission(s) for the key, comma-joined (e.g. `General,Spot,Unified`).
      ip_whitelist: New comma-separated IP whitelist for the key. Unrestricted when omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/account-info/sub-account-api/modify-subaccount-api)
    """
    request: Request = Request(
      subName=sub_name, apiKey=api_key, passphrase=passphrase, permission=permission
    )
    if ip_whitelist is not None:
      request['ipWhitelist'] = ip_whitelist
    return await self.request(
      request,
      method='POST',
      path='/api/v1/sub/api-key/update',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ModifySubAccountApiResult,
    )
