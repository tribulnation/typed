# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class BasicFee(TypedDict):
  takerFeeRate: Decimal
  """Base taker fee rate, decimal string."""
  makerFeeRate: Decimal
  """Base maker fee rate, decimal string."""


class Request(TypedDict):
  currencyType: NotRequired[Literal[0, 1]]
  """Which fee-rate currency to report."""


class BasicFeeEndpoint(RpcEndpoint):
  """Get the authenticated account's basic (non-symbol-specific) spot/margin fee rate -- the rate that applies before any per-symbol override."""

  async def basic_fee(
    self,
    currency_type: Literal[0, 1] | None = None,
    *,
    validate: bool | None = None,
  ) -> BasicFee:
    """Get the authenticated account's basic (non-symbol-specific) spot/margin fee rate -- the rate that applies before any per-symbol override.

    Args:
      currency_type: Which fee-rate currency to report.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470149)
    """
    request: Request = Request()
    if currency_type is not None:
      request['currencyType'] = currency_type
    return await self.request(
      request,
      method='GET',
      path='/api/v1/base-fee',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BasicFee,
    )
