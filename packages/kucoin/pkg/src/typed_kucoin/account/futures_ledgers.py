# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /api/v1/transaction-history` — Get Account Ledgers - Futures."""

from typing_extensions import Any, Literal
from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint, TimestampMillis, timestamp_millis


class FuturesLedgerEntry(TypedDict):
  time: TimestampMillis
  """Unix milliseconds this entry occurred."""
  type: Literal['RealisedPNL', 'Deposit', 'Withdrawal', 'TransferIn', 'TransferOut']
  """Ledger entry type."""
  amount: float
  """Transaction amount."""
  fee: float
  """Fee charged, if any."""
  accountEquity: float
  """Account equity after this entry."""
  status: Literal['Completed', 'Pending']
  """Entry status. During the current 8h settlement cycle, an open position's funding/PNL entry shows `Pending` until settled."""
  remark: str
  """Contract symbol, or other free-text remark, e.g. the source account for a transfer."""
  offset: int
  """Seek cursor for this row -- pass as the next page's `offset` to continue."""
  currency: str
  """Currency."""


class FuturesLedgerPage(TypedDict):
  """One page of futures ledger entries."""

  dataList: list[FuturesLedgerEntry]
  """Ledger entries on this page."""
  hasMore: bool
  """Whether more entries exist beyond this page."""


_Type = FuturesLedgerPage
adapter = validator[_Type](_Type)  # type: ignore


class FuturesLedgers(RpcEndpoint):
  """`Get Account Ledgers - Futures` — mixed into `Account`, the product exposing `account.futures_ledgers`."""

  async def futures_ledgers(
    self,
    *,
    currency: str | None = None,
    type: Any | None = None,
    offset: int | None = None,
    forward: bool | None = None,
    max_count: int | None = None,
    start_at: TimestampMillis | None = None,
    end_at: TimestampMillis | None = None,
    validate: bool | None = None,
  ) -> FuturesLedgerPage:
    """List ledger records for the futures business line, on `api-futures.kucoin.com`. `startAt`/`endAt` must be less than a day apart; if only one is given the other is computed +/-1 day away.

    Args:
      currency: Currency, e.g. `XBT` or `USDT`.
      type: Ledger entry type to filter to. Documented values: `RealizedPNL`, `Deposit`, `Withdrawal`, `Transferin`, `TransferOut` -- the response's own `type` field uses different casing for two of these (`RealisedPNL`, `TransferIn`); left as a bare string given the inconsistency (see notes).
      offset: Seek cursor: the `offset` field of the last row from the previous page. Omit for the first page.
      forward: Page forward (toward more recent entries) when true, backward when false.
      max_count: Results per page.
      start_at: Start time, Unix milliseconds.
      end_at: End time, Unix milliseconds.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    params = {}
    if currency is not None:
      params['currency'] = currency
    if type is not None:
      params['type'] = type
    if offset is not None:
      params['offset'] = offset
    if forward is not None:
      params['forward'] = forward
    if max_count is not None:
      params['maxCount'] = max_count
    if start_at is not None:
      params['startAt'] = timestamp_millis.dump(start_at)
    if end_at is not None:
      params['endAt'] = timestamp_millis.dump(end_at)
    return await self.authed_request(
      'GET',
      '/api/v1/transaction-history',
      params=params,
      validator=adapter,
      validate=validate,
    )
