# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class AddSubAccountResult(TypedDict):
  uid: int
  """Newly created sub-account's numeric user id."""
  subName: str
  """Sub-account name."""
  remarks: str
  """Remarks passed at creation, or empty when none was given."""
  access: str
  """Trading permission(s) granted to the sub-account."""


class Request(TypedDict):
  password: str
  """Login password for the new sub-account: 7-24 characters, must contain letters and numbers, cannot be numbers-only or contain special characters."""
  subName: str
  """Sub-account name: 7-32 characters, at least one letter and one number, no spaces."""
  access: Literal['Spot', 'Futures', 'Margin']
  """Trading permission(s) granted to the sub-account."""
  remarks: NotRequired[str]
  """Optional remarks/notes for the sub-account: 1-24 characters."""


class Add(RpcEndpoint):
  """Create a new sub-account under the master account. The master API key must carry General permission."""

  async def add(
    self,
    *,
    password: str,
    sub_name: str,
    access: Literal['Spot', 'Futures', 'Margin'],
    remarks: str | None = None,
    validate: bool | None = None,
  ) -> AddSubAccountResult:
    """Create a new sub-account under the master account. The master API key must carry General permission.

    Args:
      password: Login password for the new sub-account: 7-24 characters, must contain letters and numbers, cannot be numbers-only or contain special characters.
      sub_name: Sub-account name: 7-32 characters, at least one letter and one number, no spaces.
      access: Trading permission(s) granted to the sub-account.
      remarks: Optional remarks/notes for the sub-account: 1-24 characters.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470135)
    """
    request: Request = Request(password=password, subName=sub_name, access=access)
    if remarks is not None:
      request['remarks'] = remarks
    return await self.request(
      request,
      method='POST',
      path='/api/v2/sub/user/created',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=AddSubAccountResult,
    )
