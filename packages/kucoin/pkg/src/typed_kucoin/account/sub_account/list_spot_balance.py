# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /api/v2/sub-accounts` — account.sub_account.list_spot_balance."""

from typing_extensions import AsyncIterator, NotRequired
from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint


class SubAccountSpotBalanceAssetMainAccountsItem(TypedDict):
  currency: NotRequired[str]
  """Asset currency."""
  balance: NotRequired[str]
  """Total funds in the account."""
  available: NotRequired[str]
  """Funds available to withdraw or trade."""
  holds: NotRequired[str]
  """Funds on hold, unavailable for use."""
  baseCurrency: NotRequired[str]
  """Reference currency this balance is converted into."""
  baseCurrencyPrice: NotRequired[str]
  """Conversion price used."""
  baseAmount: NotRequired[str]
  """Balance converted into `baseCurrency`."""
  tag: NotRequired[str]
  """Account tag, e.g. `DEFAULT`."""


class SubAccountSpotBalanceAssetMarginAccountsItem(TypedDict):
  currency: NotRequired[str]
  """Asset currency."""
  balance: NotRequired[str]
  """Total funds in the account."""
  available: NotRequired[str]
  """Funds available to withdraw or trade."""
  holds: NotRequired[str]
  """Funds on hold, unavailable for use."""
  baseCurrency: NotRequired[str]
  """Reference currency this balance is converted into."""
  baseCurrencyPrice: NotRequired[str]
  """Conversion price used."""
  baseAmount: NotRequired[str]
  """Balance converted into `baseCurrency`."""
  tag: NotRequired[str]
  """Account tag, e.g. `DEFAULT`."""


class SubAccountSpotBalanceAssetTradeAccountsItem(TypedDict):
  currency: NotRequired[str]
  """Asset currency."""
  balance: NotRequired[str]
  """Total funds in the account."""
  available: NotRequired[str]
  """Funds available to withdraw or trade."""
  holds: NotRequired[str]
  """Funds on hold, unavailable for use."""
  baseCurrency: NotRequired[str]
  """Reference currency this balance is converted into."""
  baseCurrencyPrice: NotRequired[str]
  """Conversion price used."""
  baseAmount: NotRequired[str]
  """Balance converted into `baseCurrency`."""
  tag: NotRequired[str]
  """Account tag, e.g. `DEFAULT`."""


class SubAccountSpotBalance(TypedDict):
  subUserId: str
  """The sub-account's user id."""
  subName: str
  """The sub-account's name."""
  mainAccounts: list[SubAccountSpotBalanceAssetMainAccountsItem]
  """Funding-account balances, one entry per currency held."""
  tradeAccounts: list[SubAccountSpotBalanceAssetTradeAccountsItem]
  """Spot-account balances, one entry per currency held."""
  marginAccounts: list[SubAccountSpotBalanceAssetMarginAccountsItem]
  """Cross-margin-account balances, one entry per currency held."""
  tradeHFAccounts: list[str]
  """Deprecated; KuCoin documents this as valid only for some legacy users and it is otherwise always empty."""


class SubAccountSpotBalancePage(TypedDict):
  currentPage: int
  """Page number returned."""
  pageSize: int
  """Results per page."""
  totalNum: int
  """Total number of sub-accounts."""
  totalPage: int
  """Total number of pages."""
  items: list[SubAccountSpotBalance]
  """Sub-account balances on this page."""


_Type = SubAccountSpotBalancePage
adapter = validator[_Type](_Type)  # type: ignore


class ListSpotBalance(RpcEndpoint):
  """`account.sub_account.list_spot_balance` — mixed into `SubAccount`, the product exposing `account.sub_account.list_spot_balance`."""

  async def list_spot_balance(
    self,
    *,
    current_page: int | None = None,
    page_size: int | None = None,
    validate: bool | None = None,
  ) -> SubAccountSpotBalancePage:
    """Get a paginated list of every sub-account's Funding (main), Spot (trade) and Margin balances in one call.

    Args:
      current_page: Page number to fetch, starting at 1.
      page_size: Results per page.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    params = {}
    if current_page is not None:
      params['currentPage'] = current_page
    if page_size is not None:
      params['pageSize'] = page_size
    return await self.authed_request(
      'GET',
      '/api/v2/sub-accounts',
      params=params,
      validator=adapter,
      validate=validate,
    )

  async def list_spot_balance_paged(
    self,
    *,
    page_size: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[SubAccountSpotBalancePage]:
    """Yield successive pages of `list_spot_balance`.

    Requests `currentPage` from 1 upwards and stops once it has covered the `totalPage`
    pages the response reports, or after `max_pages` pages when one is given.
    """
    current_page = 1
    pages = 0
    while True:
      response = await self.list_spot_balance(
        page_size=page_size, current_page=current_page, validate=validate
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      total = response.get('totalPage') if response is not None else None
      total = int(total) if total is not None else None
      if total is None or pages >= total:
        break
      current_page += 1
