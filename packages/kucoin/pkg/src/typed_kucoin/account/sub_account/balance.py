# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import SubAccountBalanceDetail


class Request(TypedDict):
  subUserId: str
  """The sub-account's user id."""
  includeBaseAmount: NotRequired[bool]
  """Include currencies with a zero balance when true; omit zero-balance currencies when false."""
  baseCurrency: NotRequired[str]
  """Currency to convert every balance into, for the `baseAmount`/`baseCurrencyPrice` fields."""
  baseAmount: NotRequired[Decimal]
  """Only return currencies whose balance is greater than or equal to this amount, denominated in `baseCurrency`."""


class Balance(RpcEndpoint):
  """Get one sub-account's balances across its Funding (main), Spot (trade) and Margin accounts, by sub-account user id."""

  async def balance(
    self,
    *,
    sub_user_id: str,
    include_base_amount: bool | None = None,
    base_currency: str | None = None,
    base_amount: Decimal | None = None,
    validate: bool | None = None,
  ) -> SubAccountBalanceDetail:
    """Get one sub-account's balances across its Funding (main), Spot (trade) and Margin accounts, by sub-account user id.

    Args:
      sub_user_id: The sub-account's user id.
      include_base_amount: Include currencies with a zero balance when true; omit zero-balance currencies when false.
      base_currency: Currency to convert every balance into, for the `baseAmount`/`baseCurrencyPrice` fields.
      base_amount: Only return currencies whose balance is greater than or equal to this amount, denominated in `baseCurrency`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470132)
    """
    request: Request = Request(subUserId=sub_user_id)
    if include_base_amount is not None:
      request['includeBaseAmount'] = include_base_amount
    if base_currency is not None:
      request['baseCurrency'] = base_currency
    if base_amount is not None:
      request['baseAmount'] = base_amount
    return await self.request(
      request,
      method='GET',
      path='/api/v1/sub-accounts/{subUserId}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SubAccountBalanceDetail,
    )
