# Generated by typed-dev — see CONTRIBUTING.md.
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


Response = bool


class SpotAccountType(RpcEndpoint):
  """Determine whether the authenticated account is a spot high-frequency or spot low-frequency user. A legacy compatibility check, mainly relevant to accounts that opted into high-frequency trading before 2024 -- when `true`, `trade_hf`-prefixed endpoints must be used for asset transfers/queries instead of the plain `trade` ones."""

  async def spot_account_type(self, *, validate: bool | None = None) -> Response:
    """Determine whether the authenticated account is a spot high-frequency or spot low-frequency user. A legacy compatibility check, mainly relevant to accounts that opted into high-frequency trading before 2024 -- when `true`, `trade_hf`-prefixed endpoints must be used for asset transfers/queries instead of the plain `trade` ones.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470120)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v1/hf/accounts/opened',
      meta={'signed': True},
      validate=validate,
      response_type=Response,
    )
