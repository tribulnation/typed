# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class FuturesAccountOverview(TypedDict):
  """Futures account overview for one currency."""

  accountEquity: float
  """Account equity = `marginBalance + unrealisedPNL`."""
  unrealisedPNL: float
  """Unrealized profit and loss on open positions."""
  marginBalance: float
  """Margin balance = `positionMargin + orderMargin + frozenFunds + availableBalance - unrealisedPNL`."""
  positionMargin: float
  """Margin allocated to open positions."""
  orderMargin: float
  """Margin reserved for pending orders."""
  frozenFunds: float
  """Funds frozen for a pending out-transfer."""
  availableBalance: float
  """Balance available for new positions/orders."""
  currency: str
  """The queried currency."""
  riskRatio: float
  """Cross margin risk ratio."""
  maxWithdrawAmount: float
  """Maximum amount that can currently be withdrawn or transferred out."""


class Request(TypedDict):
  currency: NotRequired[str]
  """Currency to query, e.g. `USDT`, `USDC`, `XBT`, `ETH`."""


class FuturesAccount(RpcEndpoint):
  """Get the authenticated user's futures account overview for one currency, on `api-futures.kucoin.com`."""

  async def futures_account(
    self,
    currency: str | None = None,
    *,
    validate: bool | None = None,
  ) -> FuturesAccountOverview:
    """Get the authenticated user's futures account overview for one currency, on `api-futures.kucoin.com`.

    Args:
      currency: Currency to query, e.g. `USDT`, `USDC`, `XBT`, `ETH`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470129)
    """
    request: Request = Request()
    if currency is not None:
      request['currency'] = currency
    return await self.request(
      request,
      method='GET',
      path='/api/v1/account-overview',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FuturesAccountOverview,
    )
