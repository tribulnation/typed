# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /api/v3/margin/accounts` — Get Account - Cross Margin."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint


class CrossMarginCurrencyAccount(TypedDict):
  currency: str
  """Currency."""
  total: str
  """Total assets."""
  available: str
  """Available assets (total minus frozen)."""
  hold: str
  """Frozen assets."""
  liability: str
  """Liabilities."""
  liabilityPrincipal: str
  """Unpaid principal portion of `liability`."""
  liabilityInterest: str
  """Unpaid interest portion of `liability`."""
  maxBorrowSize: str
  """Remaining amount the user may still borrow."""
  borrowEnabled: bool
  """Whether borrowing this currency is supported."""
  transferInEnabled: bool
  """Whether transferring this currency in is supported."""


class CrossMarginAccount(TypedDict):
  """Cross margin account overview."""

  totalAssetOfQuoteCurrency: str
  """Total assets, valued in `quoteCurrency`."""
  totalLiabilityOfQuoteCurrency: str
  """Total liabilities, valued in `quoteCurrency`."""
  debtRatio: str
  """Current debt ratio."""
  status: Literal['EFFECTIVE', 'BANKRUPTCY', 'LIQUIDATION', 'REPAY', 'BORROW']
  """Account position status."""
  accounts: list[CrossMarginCurrencyAccount]
  """Per-currency margin account list."""


_Type = CrossMarginAccount
adapter = validator[_Type](_Type)  # type: ignore


class CrossMarginAccountEndpoint(RpcEndpoint):
  """`Get Account - Cross Margin` — mixed into `Account`, the product exposing `account.cross_margin_account`."""

  async def cross_margin_account(
    self,
    *,
    quote_currency: Literal['USDT', 'KCS', 'BTC'] | None = None,
    query_type: Literal['MARGIN', 'MARGIN_V2', 'ALL'] | None = None,
    validate: bool | None = None,
  ) -> CrossMarginAccount:
    """Get the authenticated user's cross margin account overview: total assets/liabilities valued in a quote currency, plus a per-currency breakdown.

    Args:
      quote_currency: Currency the totals are valued in.
      query_type: `MARGIN` queries only the low-frequency cross margin account; `MARGIN_V2` only the high-frequency one; `ALL` aggregates both, matching the web UI.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    params = {}
    if quote_currency is not None:
      params['quoteCurrency'] = quote_currency
    if query_type is not None:
      params['queryType'] = query_type
    return await self.authed_request(
      'GET',
      '/api/v3/margin/accounts',
      params=params,
      validator=adapter,
      validate=validate,
    )
