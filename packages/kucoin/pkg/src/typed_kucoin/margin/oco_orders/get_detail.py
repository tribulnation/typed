# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import OcoOrderDetail


class Request(TypedDict):
  orderId: str
  """The OCO order group's id, as returned by Add OCO Order."""


Response = OcoOrderDetail | None


class GetDetail(RpcEndpoint):
  """Get a margin OCO order's full detail by its order id, including both leg orders, or `null` when no such order exists for this account."""

  async def get_detail(
    self, order_id: str, *, validate: bool | None = None
  ) -> Response:
    """Get a margin OCO order's full detail by its order id, including both leg orders, or `null` when no such order exists for this account.

    Args:
      order_id: The OCO order group's id, as returned by Add OCO Order.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/margin-trading/orders/get-oco-order-detail-by-orderld)
    """
    return await self.request(
      Request(orderId=order_id),
      method='GET',
      path='/api/v3/hf/margin/oco-order/detail/orderId',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
