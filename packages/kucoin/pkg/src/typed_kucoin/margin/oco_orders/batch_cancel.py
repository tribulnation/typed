# Generated by typed-dev — see CONTRIBUTING.md.
"""`DELETE /api/v3/hf/margin/oco-order/cancel` — Batch Cancel OCO Order."""

from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint


class CancelMarginOcoOrderResult(TypedDict):
  cancelledOrderIds: list[str]
  """Leg order ids belonging to the cancelled OCO orders (two per OCO order cancelled)."""


_Type = CancelMarginOcoOrderResult
adapter = validator[_Type](_Type)  # type: ignore


class BatchCancel(RpcEndpoint):
  """`Batch Cancel OCO Order` — mixed into `OcoOrders`, the product exposing `margin.oco_orders.batch_cancel`."""

  async def batch_cancel(
    self,
    *,
    order_ids: str,
    symbol: str,
    trade_type: str,
    validate: bool | None = None,
  ) -> CancelMarginOcoOrderResult:
    """Cancel multiple margin OCO orders in a single request, scoped to a trading pair and margin mode, optionally narrowed to a specific list of order ids.

    Args:
      order_ids: Comma-separated OCO order ids to cancel.
      symbol: Trading pair to cancel OCO orders for.
      trade_type: Margin mode the targeted orders were placed on. KuCoin's own request example shows `MARGIN_ISOLATED_TRADE`; `MARGIN_TRADE` (cross) is presumed the sibling value by analogy with every other margin `tradeType` field in this client, but this endpoint's own docs page never states the field is a closed set, so it is left as a plain string rather than a guessed enum.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    params: dict = {
      'orderIds': order_ids,
      'symbol': symbol,
      'tradeType': trade_type,
    }
    return await self.authed_request(
      'DELETE',
      '/api/v3/hf/margin/oco-order/cancel',
      params=params,
      validator=adapter,
      validate=validate,
    )
