# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, NotRequired, TypedDict
from typed_kucoin.core import TimestampMillis
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import OcoOrderSummary


class Request(TypedDict):
  symbol: NotRequired[str]
  """Trading pair to filter by. Omitted returns OCO orders across all symbols."""
  startAt: NotRequired[TimestampMillis]
  """Start time (milliseconds)."""
  endAt: NotRequired[TimestampMillis]
  """End time (milliseconds)."""
  orderIds: NotRequired[str]
  """Comma-separated order ids to filter by."""
  tradeType: NotRequired[str]
  """Margin mode to filter by. Not stated as a closed set on this endpoint's docs page."""
  pageSize: NotRequired[int]
  """Results per page. Live-confirmed default 20 2026-08-09."""
  currentPage: NotRequired[int]
  """Page number to fetch, starting at 1."""


class MarginOcoOrderListPage(TypedDict):
  currentPage: int
  """Page number returned."""
  pageSize: int
  """Results per page."""
  totalNum: int
  """Total number of OCO orders matching the filter."""
  totalPage: int
  """Total number of pages."""
  items: list[OcoOrderSummary]
  """OCO order summaries on this page, newest first."""


class GetList(RpcEndpoint):
  """Get a paginated list of the account's margin OCO orders, newest first. Rows are the same lightweight shape as Get OCO Order By OrderId, with no leg detail."""

  async def get_list_paged(
    self,
    *,
    symbol: str | None = None,
    start_at: TimestampMillis | None = None,
    end_at: TimestampMillis | None = None,
    order_ids: str | None = None,
    trade_type: str | None = None,
    page_size: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[MarginOcoOrderListPage]:
    """Get a paginated list of the account's margin OCO orders, newest first. Rows are the same lightweight shape as Get OCO Order By OrderId, with no leg detail.

    Paged variant of `get_list`: Requests `currentPage` from 1 upwards and stops once it has covered the `totalPage` pages the response reports, or after `max_pages` pages when one is given.

    Args:
      symbol: Trading pair to filter by. Omitted returns OCO orders across all symbols.
      start_at: Start time (milliseconds).
      end_at: End time (milliseconds).
      order_ids: Comma-separated order ids to filter by.
      trade_type: Margin mode to filter by. Not stated as a closed set on this endpoint's docs page.
      page_size: Results per page. Live-confirmed default 20 2026-08-09.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/margin-trading/orders/get-oco-order-list)
    """
    current_page = 1
    pages = 0
    while True:
      response = await self.get_list(
        symbol=symbol,
        start_at=start_at,
        end_at=end_at,
        order_ids=order_ids,
        trade_type=trade_type,
        page_size=page_size,
        current_page=current_page,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      total = response.get('totalPage') if response is not None else None
      total = int(total) if total is not None else None
      if total is None or pages >= total:
        break
      current_page += 1

  async def get_list(
    self,
    *,
    symbol: str | None = None,
    start_at: TimestampMillis | None = None,
    end_at: TimestampMillis | None = None,
    order_ids: str | None = None,
    trade_type: str | None = None,
    page_size: int | None = None,
    current_page: int | None = None,
    validate: bool | None = None,
  ) -> MarginOcoOrderListPage:
    """Get a paginated list of the account's margin OCO orders, newest first. Rows are the same lightweight shape as Get OCO Order By OrderId, with no leg detail.

    Args:
      symbol: Trading pair to filter by. Omitted returns OCO orders across all symbols.
      start_at: Start time (milliseconds).
      end_at: End time (milliseconds).
      order_ids: Comma-separated order ids to filter by.
      trade_type: Margin mode to filter by. Not stated as a closed set on this endpoint's docs page.
      page_size: Results per page. Live-confirmed default 20 2026-08-09.
      current_page: Page number to fetch, starting at 1.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/margin-trading/orders/get-oco-order-list)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    if start_at is not None:
      request['startAt'] = start_at
    if end_at is not None:
      request['endAt'] = end_at
    if order_ids is not None:
      request['orderIds'] = order_ids
    if trade_type is not None:
      request['tradeType'] = trade_type
    if page_size is not None:
      request['pageSize'] = page_size
    if current_page is not None:
      request['currentPage'] = current_page
    return await self.request(
      request,
      method='GET',
      path='/api/v3/hf/margin/oco-orders',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=MarginOcoOrderListPage,
    )
