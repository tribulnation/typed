# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import OcoOrderSummary


class Request(TypedDict):
  clientOid: str
  """The client-provided order id supplied at placement."""


Response = OcoOrderSummary | None


class GetByClientOid(RpcEndpoint):
  """Get a lightweight margin OCO order summary (no leg detail) by its clientOid, or `null` when no such order exists for this account. Same lightweight shape as Get OCO Order By OrderId."""

  async def get_by_client_oid(
    self,
    client_oid: str,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Get a lightweight margin OCO order summary (no leg detail) by its clientOid, or `null` when no such order exists for this account. Same lightweight shape as Get OCO Order By OrderId.

    Args:
      client_oid: The client-provided order id supplied at placement.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/margin-trading/orders/get-oco-order-by-clientoid)
    """
    return await self.request(
      Request(clientOid=client_oid),
      method='GET',
      path='/api/v3/hf/margin/oco-order/clientOid',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
