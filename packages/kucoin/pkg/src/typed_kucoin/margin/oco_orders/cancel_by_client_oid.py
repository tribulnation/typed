# Generated by typed-dev — see CONTRIBUTING.md.
"""`DELETE /api/v3/hf/margin/oco-order/cancel-by-clientOid` — Cancel OCO Order By ClientOid."""

from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint


class CancelMarginOcoOrderResult(TypedDict):
  cancelledOrderIds: list[str]
  """The two leg order ids belonging to the cancelled OCO order."""


_Type = CancelMarginOcoOrderResult
adapter = validator[_Type](_Type)  # type: ignore


class CancelByClientOid(RpcEndpoint):
  """`Cancel OCO Order By ClientOid` — mixed into `OcoOrders`, the product exposing `margin.oco_orders.cancel_by_client_oid`."""

  async def cancel_by_client_oid(
    self,
    *,
    client_oid: str,
    validate: bool | None = None,
  ) -> CancelMarginOcoOrderResult:
    """Request cancellation of a single margin OCO order (both legs) by the caller-assigned `clientOid`.

    Args:
      client_oid: The client-provided order id supplied at placement.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    params: dict = {
      'clientOid': client_oid,
    }
    return await self.authed_request(
      'DELETE',
      '/api/v3/hf/margin/oco-order/cancel-by-clientOid',
      params=params,
      validator=adapter,
      validate=validate,
    )
