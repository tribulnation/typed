# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import MarkPriceEntry


class Request(TypedDict):
  symbol: str
  """Margin symbol."""


class MarkPriceDetail(RpcEndpoint):
  """Get the current mark price for one margin trading pair. Mark price values cross/isolated margin positions and drives liquidation checks, distinct from the symbol's last-traded price."""

  async def mark_price_detail(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> MarkPriceEntry:
    """Get the current mark price for one margin trading pair. Mark price values cross/isolated margin positions and drives liquidation checks, distinct from the symbol's last-traded price.

    Args:
      symbol: Margin symbol.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470193)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/api/v1/mark-price/{symbol}/current',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=MarkPriceEntry,
    )
