# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /api/v3/position/update-user-leverage` — Modify Leverage."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict
from typed_kucoin.core import RpcEndpoint


class ModifyLeverageRequest(TypedDict):
  leverage: str
  """New leverage multiplier. Must be greater than 1, up to two decimal places, no less than the account's current debt leverage, and no greater than the system's maximum leverage."""
  symbol: NotRequired[str]
  """Trading pair, e.g. `BTC-USDT`. Mandatory when `isIsolated` is true; not required for cross margin."""
  isIsolated: NotRequired[bool]
  """Whether this changes isolated-margin leverage (`true`) or cross-margin (`false`)."""


class ModifyLeverage(RpcEndpoint):
  """`Modify Leverage` — mixed into `Debit`, the product exposing `margin.debit.modify_leverage`."""

  async def modify_leverage(
    self,
    modify_leverage_request: ModifyLeverageRequest,
    *,
    validate: bool | None = None,
  ) -> None:
    """Modify the leverage multiplier for cross margin or isolated margin.

    Args:
      modify_leverage_request: Leverage change parameters.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    await self.authed_request(
      'POST',
      '/api/v3/position/update-user-leverage',
      json=modify_leverage_request,
      validate=validate,
    )
