# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  leverage: Decimal
  """New leverage multiplier. Must be greater than 1, up to two decimal places, no less than the account's current debt leverage, and no greater than the system's maximum leverage."""
  symbol: NotRequired[str]
  """Trading pair, e.g. `BTC-USDT`. Mandatory when `isIsolated` is true; not required for cross margin."""
  isIsolated: NotRequired[bool]
  """Whether this changes isolated-margin leverage (`true`) or cross-margin (`false`)."""


class ModifyLeverage(RpcEndpoint):
  """Modify the leverage multiplier for cross margin or isolated margin."""

  async def modify_leverage(
    self,
    leverage: Decimal,
    *,
    symbol: str | None = None,
    is_isolated: bool | None = None,
    validate: bool | None = None,
  ) -> None:
    """Modify the leverage multiplier for cross margin or isolated margin.

    Args:
      leverage: New leverage multiplier. Must be greater than 1, up to two decimal places, no less than the account's current debt leverage, and no greater than the system's maximum leverage.
      symbol: Trading pair, e.g. `BTC-USDT`. Mandatory when `isIsolated` is true; not required for cross margin.
      is_isolated: Whether this changes isolated-margin leverage (`true`) or cross-margin (`false`).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470211)
    """
    request: Request = Request(leverage=leverage)
    if symbol is not None:
      request['symbol'] = symbol
    if is_isolated is not None:
      request['isIsolated'] = is_isolated
    return await self.request(
      request,
      method='POST',
      path='/api/v3/position/update-user-leverage',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=None,
    )
