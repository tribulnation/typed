# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /api/v3/margin/symbols` — Get Symbols - Cross Margin."""

from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint, TimestampMillis


class CrossMarginSymbol(TypedDict):
  symbol: str
  """Symbol."""
  name: str
  """Symbol name."""
  enableTrading: bool
  """Whether trading is enabled for this symbol."""
  market: str
  """Trading market this symbol belongs to."""
  baseCurrency: str
  """Base currency, e.g. `BTC`."""
  quoteCurrency: str
  """Quote currency, e.g. `USDT`."""
  baseIncrement: str
  """Quantity increment: order size (base currency) must be a positive integer multiple of this."""
  baseMinSize: str
  """Minimum order quantity."""
  baseMaxSize: str
  """Maximum order quantity."""
  quoteIncrement: str
  """Funds increment: a market order's funds (quote currency) must be a positive integer multiple of this."""
  quoteMinSize: str
  """Minimum order funds for a market order."""
  quoteMaxSize: str
  """Maximum order funds for a market order."""
  priceIncrement: str
  """Price increment: order price must be a positive integer multiple of this."""
  feeCurrency: str
  """Currency trading fees are charged in."""
  priceLimitRate: str
  """Price-protection threshold."""
  minFunds: str
  """Minimum trading amount."""


class CrossMarginSymbols(TypedDict):
  """Cross-margin symbol configuration list, timestamped."""

  timestamp: TimestampMillis
  """Server timestamp this configuration was current as of, Unix milliseconds."""
  items: list[CrossMarginSymbol]
  """One entry per cross-margin symbol."""


_Type = CrossMarginSymbols
adapter = validator[_Type](_Type)  # type: ignore


class CrossMarginSymbolsEndpoint(RpcEndpoint):
  """`Get Symbols - Cross Margin` — mixed into `Margin`, the product exposing `margin.cross_margin_symbols`."""

  async def cross_margin_symbols(
    self,
    *,
    symbol: str | None = None,
    validate: bool | None = None,
  ) -> CrossMarginSymbols:
    """Get the trading configuration of cross-margin symbols: order size/price increments, min/max sizes, price-protection threshold and fee currency.

    Args:
      symbol: Specific symbol to query, e.g. `BTC-USDT`. Queries every cross-margin symbol if omitted.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    params = {}
    if symbol is not None:
      params['symbol'] = symbol
    return await self.request(
      'GET',
      '/api/v3/margin/symbols',
      params=params,
      validator=adapter,
      validate=validate,
    )
