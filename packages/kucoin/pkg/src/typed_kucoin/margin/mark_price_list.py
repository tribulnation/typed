# Generated by typed-dev — see CONTRIBUTING.md.
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import MarkPriceEntry


MarkPrice = list[MarkPriceEntry]


class MarkPriceList(RpcEndpoint):
  """Get the current mark price for every margin trading pair in one call."""

  async def mark_price_list(self, *, validate: bool | None = None) -> MarkPrice:
    """Get the current mark price for every margin trading pair in one call.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470192)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v3/mark-price/all-symbols',
      meta={},
      validate=validate,
      response_type=MarkPrice,
    )
