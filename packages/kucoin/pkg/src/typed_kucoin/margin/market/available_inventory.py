# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class MarginAvailableInventory(TypedDict):
  """Currently available borrowable amount for one currency."""

  currency: str
  """Currency code."""
  borrowableAmount: Decimal
  """Amount of this currency currently available to borrow platform-wide."""


class Request(TypedDict):
  currency: NotRequired[str]
  """Single currency code to filter by, e.g. `USDC`. Omitted returns every currency's inventory."""


MarginAvailableInventoryList = list[MarginAvailableInventory]


class AvailableInventory(RpcEndpoint):
  """Get the platform's currently available borrowable inventory, per currency. Public -- no authentication required."""

  async def available_inventory(
    self,
    currency: str | None = None,
    *,
    validate: bool | None = None,
  ) -> MarginAvailableInventoryList:
    """Get the platform's currently available borrowable inventory, per currency. Public -- no authentication required.

    Args:
      currency: Single currency code to filter by, e.g. `USDC`. Omitted returns every currency's inventory.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/margin-trading/market-data/get-margin)
    """
    request: Request = Request()
    if currency is not None:
      request['currency'] = currency
    return await self.request(
      request,
      method='GET',
      path='/api/v3/margin/available-inventory',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=MarginAvailableInventoryList,
    )
