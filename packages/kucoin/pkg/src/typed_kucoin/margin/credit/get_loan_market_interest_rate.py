# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class LoanMarketInterestRatePoint(TypedDict):
  time: str
  """Hour this rate applied, `YYYYMMDDHHmm` with minutes always `00` (hourly granularity)."""
  marketInterestRate: Decimal
  """Market interest rate for this hour."""


class Request(TypedDict):
  currency: str
  """Currency to query, e.g. `BTC`, `ETH`, `USDT`."""


LoanMarketInterestRateHistory = list[LoanMarketInterestRatePoint]


class GetLoanMarketInterestRate(RpcEndpoint):
  """Get the margin lending market's interest rate for one currency over the past 7 days, one row per hour."""

  async def get_loan_market_interest_rate(
    self,
    currency: str,
    *,
    validate: bool | None = None,
  ) -> LoanMarketInterestRateHistory:
    """Get the margin lending market's interest rate for one currency over the past 7 days, one row per hour.

    Args:
      currency: Currency to query, e.g. `BTC`, `ETH`, `USDT`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470215)
    """
    return await self.request(
      Request(currency=currency),
      method='GET',
      path='/api/v3/project/marketInterestRate',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=LoanMarketInterestRateHistory,
    )
