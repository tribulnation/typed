# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_kucoin.core import TimestampMillis
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class RedeemOrder(TypedDict):
  currency: str
  """Currency redeemed."""
  purchaseOrderNo: str
  """Id of the purchase order this redeem draws down."""
  redeemOrderNo: str
  """Unique redeem order id."""
  redeemSize: Decimal
  """Amount requested to redeem."""
  receiptSize: Decimal
  """Amount actually received."""
  applyTime: TimestampMillis | None
  """Unix milliseconds this redeem was submitted, or `null` -- KuCoin's own documented example shows `null` here despite the field being marked required and typed as a plain string in the SDK's OpenAPI spec; never observed non-null in this pass (this account has no redeem orders)."""
  status: Literal['DONE', 'PENDING']
  """Settlement status."""


class Request(TypedDict):
  status: Literal['DONE', 'PENDING']
  """Settlement status to filter by."""
  currency: NotRequired[str]
  """Currency to filter by, e.g. `BTC`, `ETH`, `KCS`. Queries every currency if omitted."""
  redeemOrderNo: NotRequired[str]
  """Filter by a specific redeem order id."""
  currentPage: NotRequired[int]
  """Page number."""
  pageSize: NotRequired[int]
  """Results per page. Documented range 1-50; the SDK's OpenAPI spec claims a default of 50, but a live call omitting this parameter returned `pageSize: 10` -- specced against the observed default."""


class RedeemOrdersPage(TypedDict):
  """One page of margin credit redeem orders."""

  currentPage: int
  """Current page number."""
  pageSize: int
  """Results per page, as applied."""
  totalNum: int
  """Total matching records."""
  totalPage: int
  """Total number of pages."""
  items: list[RedeemOrder]
  """Redeem orders on this page."""


class GetRedeemOrders(RpcEndpoint):
  """Page through this account's margin credit redeem orders, filtered by settlement status."""

  async def get_redeem_orders_paged(
    self,
    status: Literal['DONE', 'PENDING'],
    *,
    currency: str | None = None,
    redeem_order_no: str | None = None,
    page_size: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[RedeemOrdersPage]:
    """Page through this account's margin credit redeem orders, filtered by settlement status.

    Paged variant of `get_redeem_orders`: Requests `currentPage` from 1 upwards and stops once it has covered the `totalPage` pages the response reports, or after `max_pages` pages when one is given.

    Args:
      status: Settlement status to filter by.
      currency: Currency to filter by, e.g. `BTC`, `ETH`, `KCS`. Queries every currency if omitted.
      redeem_order_no: Filter by a specific redeem order id.
      page_size: Results per page. Documented range 1-50; the SDK's OpenAPI spec claims a default of 50, but a live call omitting this parameter returned `pageSize: 10` -- specced against the observed default.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470214)
    """
    current_page = 1
    pages = 0
    while True:
      response = await self.get_redeem_orders(
        status,
        currency=currency,
        redeem_order_no=redeem_order_no,
        page_size=page_size,
        current_page=current_page,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      total = response.get('totalPage') if response is not None else None
      total = int(total) if total is not None else None
      if total is None or pages >= total:
        break
      current_page += 1

  async def get_redeem_orders(
    self,
    status: Literal['DONE', 'PENDING'],
    *,
    currency: str | None = None,
    redeem_order_no: str | None = None,
    current_page: int | None = None,
    page_size: int | None = None,
    validate: bool | None = None,
  ) -> RedeemOrdersPage:
    """Page through this account's margin credit redeem orders, filtered by settlement status.

    Args:
      status: Settlement status to filter by.
      currency: Currency to filter by, e.g. `BTC`, `ETH`, `KCS`. Queries every currency if omitted.
      redeem_order_no: Filter by a specific redeem order id.
      current_page: Page number.
      page_size: Results per page. Documented range 1-50; the SDK's OpenAPI spec claims a default of 50, but a live call omitting this parameter returned `pageSize: 10` -- specced against the observed default.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470214)
    """
    request: Request = Request(status=status)
    if currency is not None:
      request['currency'] = currency
    if redeem_order_no is not None:
      request['redeemOrderNo'] = redeem_order_no
    if current_page is not None:
      request['currentPage'] = current_page
    if page_size is not None:
      request['pageSize'] = page_size
    return await self.request(
      request,
      method='GET',
      path='/api/v3/redeem/orders',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=RedeemOrdersPage,
    )
