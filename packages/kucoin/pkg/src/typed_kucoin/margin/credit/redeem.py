# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class RedeemResult(TypedDict):
  orderNo: str
  """Unique id assigned to the new redeem order."""


class Request(TypedDict):
  currency: str
  """Currency of the purchase order, e.g. `BTC`, `ETH`, `USDT`."""
  size: Decimal
  """Amount to redeem."""
  purchaseOrderNo: str
  """Id of the purchase order being redeemed."""


class Redeem(RpcEndpoint):
  """Redeem (pull back) a margin credit purchase order, in full or in part."""

  async def redeem(
    self,
    *,
    currency: str,
    size: Decimal,
    purchase_order_no: str,
    validate: bool | None = None,
  ) -> RedeemResult:
    """Redeem (pull back) a margin credit purchase order, in full or in part.

    Args:
      currency: Currency of the purchase order, e.g. `BTC`, `ETH`, `USDT`.
      size: Amount to redeem.
      purchase_order_no: Id of the purchase order being redeemed.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470218)
    """
    return await self.request(
      Request(currency=currency, size=size, purchaseOrderNo=purchase_order_no),
      method='POST',
      path='/api/v3/redeem',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=RedeemResult,
    )
