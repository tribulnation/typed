# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  currency: str
  """Currency of the purchase order, e.g. `BTC`, `ETH`, `USDT`."""
  purchaseOrderNo: str
  """Id of the purchase order to modify."""
  interestRate: Decimal
  """New interest rate to offer, effective from the start of the next hour."""


class ModifyPurchase(RpcEndpoint):
  """Change the interest rate of an existing purchase (lend) order. The new rate takes effect at the start of the next hour."""

  async def modify_purchase(
    self,
    *,
    currency: str,
    purchase_order_no: str,
    interest_rate: Decimal,
    validate: bool | None = None,
  ) -> None:
    """Change the interest rate of an existing purchase (lend) order. The new rate takes effect at the start of the next hour.

    Args:
      currency: Currency of the purchase order, e.g. `BTC`, `ETH`, `USDT`.
      purchase_order_no: Id of the purchase order to modify.
      interest_rate: New interest rate to offer, effective from the start of the next hour.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470217)
    """
    return await self.request(
      Request(
        currency=currency, purchaseOrderNo=purchase_order_no, interestRate=interest_rate
      ),
      method='POST',
      path='/api/v3/lend/purchase/update',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=None,
    )
