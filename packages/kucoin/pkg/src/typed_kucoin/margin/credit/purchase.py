# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class PurchaseResult(TypedDict):
  orderNo: str
  """Unique id assigned to the new purchase order."""


class Request(TypedDict):
  currency: str
  """Currency to lend, e.g. `BTC`, `ETH`, `USDT`."""
  size: Decimal
  """Amount to lend."""
  interestRate: Decimal
  """Interest rate to offer this lend at."""


class Purchase(RpcEndpoint):
  """Lend funds into the margin credit market at a chosen interest rate, earning interest as other margin traders borrow them. Funds must already be in the main (funding) account."""

  async def purchase(
    self,
    currency: str,
    *,
    size: Decimal,
    interest_rate: Decimal,
    validate: bool | None = None,
  ) -> PurchaseResult:
    """Lend funds into the margin credit market at a chosen interest rate, earning interest as other margin traders borrow them. Funds must already be in the main (funding) account.

    Args:
      currency: Currency to lend, e.g. `BTC`, `ETH`, `USDT`.
      size: Amount to lend.
      interest_rate: Interest rate to offer this lend at.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470216)
    """
    return await self.request(
      Request(currency=currency, size=size, interestRate=interest_rate),
      method='POST',
      path='/api/v3/purchase',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PurchaseResult,
    )
