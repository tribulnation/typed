# Generated by typed-dev — see CONTRIBUTING.md.
"""`DELETE /api/v3/hf/margin/stop-order/cancel-by-clientOid` — Cancel Stop Order By ClientOid."""

from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint


class CancelMarginStopOrderResult(TypedDict):
  cancelledOrderIds: list[str]
  """Ids of the stop orders cancelled by this call."""


_Type = CancelMarginStopOrderResult
adapter = validator[_Type](_Type)  # type: ignore


class CancelByClientOid(RpcEndpoint):
  """`Cancel Stop Order By ClientOid` — mixed into `StopOrders`, the product exposing `margin.stop_orders.cancel_by_client_oid`."""

  async def cancel_by_client_oid(
    self,
    *,
    client_oid: str,
    validate: bool | None = None,
  ) -> CancelMarginStopOrderResult:
    """Request cancellation of a single untriggered margin stop order by the caller-assigned `clientOid` instead of the venue's `orderId`.

    Args:
      client_oid: Caller-assigned order id passed at stop-order creation time.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    params: dict = {
      'clientOid': client_oid,
    }
    return await self.authed_request(
      'DELETE',
      '/api/v3/hf/margin/stop-order/cancel-by-clientOid',
      params=params,
      validator=adapter,
      validate=validate,
    )
