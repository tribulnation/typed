# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class CancelMarginStopOrderResult(TypedDict):
  cancelledOrderIds: list[str]
  """Ids of the stop orders cancelled by this call."""


class Request(TypedDict):
  clientOid: str
  """Caller-assigned order id passed at stop-order creation time."""


class CancelByClientOid(RpcEndpoint):
  """Request cancellation of a single untriggered margin stop order by the caller-assigned `clientOid` instead of the venue's `orderId`."""

  async def cancel_by_client_oid(
    self,
    client_oid: str,
    *,
    validate: bool | None = None,
  ) -> CancelMarginStopOrderResult:
    """Request cancellation of a single untriggered margin stop order by the caller-assigned `clientOid` instead of the venue's `orderId`.

    Args:
      client_oid: Caller-assigned order id passed at stop-order creation time.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/margin-trading/orders/cancel-stop-order-by-clientoid)
    """
    return await self.request(
      Request(clientOid=client_oid),
      method='DELETE',
      path='/api/v3/hf/margin/stop-order/cancel-by-clientOid',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CancelMarginStopOrderResult,
    )
