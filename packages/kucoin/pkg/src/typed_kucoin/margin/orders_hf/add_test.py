# Generated by typed-dev — see CONTRIBUTING.md.
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import (
  HfMarginAddOrderLimit,
  HfMarginAddOrderMarket,
  HfMarginAddOrderResult,
)


Request = HfMarginAddOrderLimit | HfMarginAddOrderMarket


class AddTest(RpcEndpoint):
  """Validate a margin hf order placement request against the same checks Add applies -- signature, symbol, parameters -- without ever entering the matching engine. An order placed through this endpoint cannot be queried afterwards by any other endpoint."""

  async def add_test(
    self,
    request: Request,
    *,
    validate: bool | None = None,
  ) -> HfMarginAddOrderResult:
    """Validate a margin hf order placement request against the same checks Add applies -- signature, symbol, parameters -- without ever entering the matching engine. An order placed through this endpoint cannot be queried afterwards by any other endpoint.

    Args:
      request:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/margin-trading/orders/add-order-test)
    """
    return await self.request(
      request,
      method='POST',
      path='/api/v3/hf/margin/order/test',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=HfMarginAddOrderResult,
    )
