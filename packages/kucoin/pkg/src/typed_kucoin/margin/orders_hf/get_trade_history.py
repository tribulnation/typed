# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_kucoin.core import TimestampMillis
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import HfFill


class Request(TypedDict):
  symbol: str
  """Trading pair symbol to filter by."""
  tradeType: str
  """Margin mode to look up. Confirmed live 2026-08-08: both `MARGIN_TRADE` (cross) and `MARGIN_ISOLATED_TRADE` (isolated) are accepted, but this endpoint's docs page never states the field is a closed set, so it is left as a plain string rather than a guessed enum."""
  orderId: NotRequired[str]
  """Order id to filter by."""
  side: NotRequired[Literal['buy', 'sell']]
  """Order side to filter by."""
  type: NotRequired[Literal['limit', 'market']]
  """Order type to filter by."""
  lastId: NotRequired[int]
  """Cursor from a previous page's `lastId`, to fetch the next older page."""
  limit: NotRequired[int]
  """Results per page."""
  startAt: NotRequired[TimestampMillis]
  """Start of the time range, inclusive."""
  endAt: NotRequired[TimestampMillis]
  """End of the time range, inclusive."""


class HfMarginFillsPage(TypedDict):
  """Page of margin hf trade fills."""

  items: list[HfFill]
  """Trade fills on this page, newest first."""
  lastId: int
  """Cursor to pass as `lastId` to fetch the next older page. `0` when there is no further page."""


class GetTradeHistory(RpcEndpoint):
  """Cursor-paginated list of the authenticated account's margin hf trade fills, for one trading pair and margin mode. Data is retained for up to 3 months; each query window is capped at 7 days."""

  def get_trade_history_paged(
    self,
    *,
    symbol: str,
    trade_type: str,
    order_id: str | None = None,
    side: Literal['buy', 'sell'] | None = None,
    type: Literal['limit', 'market'] | None = None,
    limit: int | None = None,
    start_at: TimestampMillis | None = None,
    end_at: TimestampMillis | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[HfFill, int]:
    """Cursor-paginated list of the authenticated account's margin hf trade fills, for one trading pair and margin mode. Data is retained for up to 3 months; each query window is capped at 7 days.

    Paged variant of `get_trade_history`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      symbol: Trading pair symbol to filter by.
      trade_type: Margin mode to look up. Confirmed live 2026-08-08: both `MARGIN_TRADE` (cross) and `MARGIN_ISOLATED_TRADE` (isolated) are accepted, but this endpoint's docs page never states the field is a closed set, so it is left as a plain string rather than a guessed enum.
      order_id: Order id to filter by.
      side: Order side to filter by.
      type: Order type to filter by.
      limit: Results per page.
      start_at: Start of the time range, inclusive.
      end_at: End of the time range, inclusive.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/margin-trading/orders/get-trade-history)
    """

    async def next(last_id: int) -> tuple[list[HfFill], int | None]:
      response = await self.get_trade_history(
        symbol=symbol,
        trade_type=trade_type,
        order_id=order_id,
        side=side,
        type=type,
        limit=limit,
        start_at=start_at,
        end_at=end_at,
        last_id=(last_id or None),
        validate=validate,
      )
      rows = response.get('items')
      state = response.get('lastId')
      return rows or [], state or None

    return PaginatedResponse(0, next)

  async def get_trade_history(
    self,
    *,
    symbol: str,
    trade_type: str,
    order_id: str | None = None,
    side: Literal['buy', 'sell'] | None = None,
    type: Literal['limit', 'market'] | None = None,
    last_id: int | None = None,
    limit: int | None = None,
    start_at: TimestampMillis | None = None,
    end_at: TimestampMillis | None = None,
    validate: bool | None = None,
  ) -> HfMarginFillsPage:
    """Cursor-paginated list of the authenticated account's margin hf trade fills, for one trading pair and margin mode. Data is retained for up to 3 months; each query window is capped at 7 days.

    Args:
      symbol: Trading pair symbol to filter by.
      trade_type: Margin mode to look up. Confirmed live 2026-08-08: both `MARGIN_TRADE` (cross) and `MARGIN_ISOLATED_TRADE` (isolated) are accepted, but this endpoint's docs page never states the field is a closed set, so it is left as a plain string rather than a guessed enum.
      order_id: Order id to filter by.
      side: Order side to filter by.
      type: Order type to filter by.
      last_id: Cursor from a previous page's `lastId`, to fetch the next older page.
      limit: Results per page.
      start_at: Start of the time range, inclusive.
      end_at: End of the time range, inclusive.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/margin-trading/orders/get-trade-history)
    """
    request: Request = Request(symbol=symbol, tradeType=trade_type)
    if order_id is not None:
      request['orderId'] = order_id
    if side is not None:
      request['side'] = side
    if type is not None:
      request['type'] = type
    if last_id is not None:
      request['lastId'] = last_id
    if limit is not None:
      request['limit'] = limit
    if start_at is not None:
      request['startAt'] = start_at
    if end_at is not None:
      request['endAt'] = end_at
    return await self.request(
      request,
      method='GET',
      path='/api/v3/hf/margin/fills',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=HfMarginFillsPage,
    )
