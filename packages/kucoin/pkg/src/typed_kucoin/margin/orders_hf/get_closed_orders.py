# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_kucoin.core import TimestampMillis
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import HfMarginOrder


class Request(TypedDict):
  symbol: str
  """Trading pair symbol to filter by."""
  tradeType: str
  """Margin mode to look up. Confirmed live 2026-08-08: both `MARGIN_TRADE` (cross) and `MARGIN_ISOLATED_TRADE` (isolated) are accepted, but this endpoint's docs page never states the field is a closed set, so it is left as a plain string rather than a guessed enum."""
  side: NotRequired[Literal['buy', 'sell']]
  """Order side to filter by."""
  type: NotRequired[Literal['limit', 'market']]
  """Order type to filter by."""
  lastId: NotRequired[int]
  """Cursor from a previous page's `lastId`, to fetch the next older page."""
  limit: NotRequired[int]
  """Results per page."""
  startAt: NotRequired[TimestampMillis]
  """Start of the time range, inclusive."""
  endAt: NotRequired[TimestampMillis]
  """End of the time range, inclusive."""


class HfMarginClosedOrdersPage(TypedDict):
  """Page of closed hf margin orders."""

  lastId: int
  """Cursor to pass as `lastId` to fetch the next older page."""
  items: list[HfMarginOrder]
  """Closed hf margin orders on this page, newest first."""


Response = HfMarginClosedOrdersPage | None


class GetClosedOrders(RpcEndpoint):
  """Cursor-paginated list of completed (fully filled or cancelled) margin hf orders for the authenticated account, for one trading pair and margin mode. 7-day historical lookback window."""

  def get_closed_orders_paged(
    self,
    *,
    symbol: str,
    trade_type: str,
    side: Literal['buy', 'sell'] | None = None,
    type: Literal['limit', 'market'] | None = None,
    limit: int | None = None,
    start_at: TimestampMillis | None = None,
    end_at: TimestampMillis | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[HfMarginOrder, int]:
    """Cursor-paginated list of completed (fully filled or cancelled) margin hf orders for the authenticated account, for one trading pair and margin mode. 7-day historical lookback window.

    Paged variant of `get_closed_orders`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      symbol: Trading pair symbol to filter by.
      trade_type: Margin mode to look up. Confirmed live 2026-08-08: both `MARGIN_TRADE` (cross) and `MARGIN_ISOLATED_TRADE` (isolated) are accepted, but this endpoint's docs page never states the field is a closed set, so it is left as a plain string rather than a guessed enum.
      side: Order side to filter by.
      type: Order type to filter by.
      limit: Results per page.
      start_at: Start of the time range, inclusive.
      end_at: End of the time range, inclusive.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/margin-trading/orders/get-closed-orders)
    """

    async def next(last_id: int) -> tuple[list[HfMarginOrder], int | None]:
      response = await self.get_closed_orders(
        symbol=symbol,
        trade_type=trade_type,
        side=side,
        type=type,
        limit=limit,
        start_at=start_at,
        end_at=end_at,
        last_id=(last_id or None),
        validate=validate,
      )
      rows = response.get('items') if response is not None else None
      state = response.get('lastId') if response is not None else None
      return rows or [], state or None

    return PaginatedResponse(0, next)

  async def get_closed_orders(
    self,
    *,
    symbol: str,
    trade_type: str,
    side: Literal['buy', 'sell'] | None = None,
    type: Literal['limit', 'market'] | None = None,
    last_id: int | None = None,
    limit: int | None = None,
    start_at: TimestampMillis | None = None,
    end_at: TimestampMillis | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Cursor-paginated list of completed (fully filled or cancelled) margin hf orders for the authenticated account, for one trading pair and margin mode. 7-day historical lookback window.

    Args:
      symbol: Trading pair symbol to filter by.
      trade_type: Margin mode to look up. Confirmed live 2026-08-08: both `MARGIN_TRADE` (cross) and `MARGIN_ISOLATED_TRADE` (isolated) are accepted, but this endpoint's docs page never states the field is a closed set, so it is left as a plain string rather than a guessed enum.
      side: Order side to filter by.
      type: Order type to filter by.
      last_id: Cursor from a previous page's `lastId`, to fetch the next older page.
      limit: Results per page.
      start_at: Start of the time range, inclusive.
      end_at: End of the time range, inclusive.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/margin-trading/orders/get-closed-orders)
    """
    request: Request = Request(symbol=symbol, tradeType=trade_type)
    if side is not None:
      request['side'] = side
    if type is not None:
      request['type'] = type
    if last_id is not None:
      request['lastId'] = last_id
    if limit is not None:
      request['limit'] = limit
    if start_at is not None:
      request['startAt'] = start_at
    if end_at is not None:
      request['endAt'] = end_at
    return await self.request(
      request,
      method='GET',
      path='/api/v3/hf/margin/orders/done',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
