# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import HfMarginOrder


class Request(TypedDict):
  symbol: str
  """Trading pair symbol to filter by."""
  tradeType: str
  """Margin mode to look up. Confirmed live 2026-08-08: both `MARGIN_TRADE` (cross) and `MARGIN_ISOLATED_TRADE` (isolated) are accepted, but this endpoint's docs page never states the field is a closed set, so it is left as a plain string rather than a guessed enum."""


HfMarginOpenOrders = list[HfMarginOrder]


class GetOpenOrders(RpcEndpoint):
  """List every currently open (active) margin hf order for the authenticated account, for one trading pair and margin mode, sorted descending by last update time. Unpaginated."""

  async def get_open_orders(
    self,
    *,
    symbol: str,
    trade_type: str,
    validate: bool | None = None,
  ) -> HfMarginOpenOrders:
    """List every currently open (active) margin hf order for the authenticated account, for one trading pair and margin mode, sorted descending by last update time. Unpaginated.

    Args:
      symbol: Trading pair symbol to filter by.
      trade_type: Margin mode to look up. Confirmed live 2026-08-08: both `MARGIN_TRADE` (cross) and `MARGIN_ISOLATED_TRADE` (isolated) are accepted, but this endpoint's docs page never states the field is a closed set, so it is left as a plain string rather than a guessed enum.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/margin-trading/orders/get-open-orders)
    """
    return await self.request(
      Request(symbol=symbol, tradeType=trade_type),
      method='GET',
      path='/api/v3/hf/margin/orders/active',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=HfMarginOpenOrders,
    )
