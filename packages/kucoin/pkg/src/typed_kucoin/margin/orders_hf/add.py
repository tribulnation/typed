# Generated by typed-dev — see CONTRIBUTING.md.
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import (
  HfMarginAddOrderLimit,
  HfMarginAddOrderMarket,
  HfMarginAddOrderResult,
)


Request = HfMarginAddOrderLimit | HfMarginAddOrderMarket


class Add(RpcEndpoint):
  """Place a margin high-frequency (hf) limit or market order against the cross-margin or isolated-margin trading system. Funds are held for the order's duration once placed; up to 2000 active orders per account and 200 per trading pair."""

  async def add(
    self,
    request: Request,
    *,
    validate: bool | None = None,
  ) -> HfMarginAddOrderResult:
    """Place a margin high-frequency (hf) limit or market order against the cross-margin or isolated-margin trading system. Funds are held for the order's duration once placed; up to 2000 active orders per account and 200 per trading pair.

    Args:
      request:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/margin-trading/orders/add-order)
    """
    return await self.request(
      request,
      method='POST',
      path='/api/v3/hf/margin/order',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=HfMarginAddOrderResult,
    )
