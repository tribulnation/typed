# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /api/v3/hf/margin/orders/client-order/{clientOid}` — Get Order By ClientOid."""

from typed_core.validation import validator
from typed_kucoin.types import HfMarginOrder
from typed_kucoin.core import RpcEndpoint


_Type = HfMarginOrder | None
adapter = validator[_Type](_Type)  # type: ignore


class GetByClientOid(RpcEndpoint):
  """`Get Order By ClientOid` — mixed into `OrdersHf`, the product exposing `margin.orders_hf.get_by_client_oid`."""

  async def get_by_client_oid(
    self,
    client_oid: str,
    *,
    symbol: str,
    validate: bool | None = None,
  ) -> HfMarginOrder | None:
    """Get a single margin hf order's full detail by its client-provided order id, or `null` when no such order exists for this account. Non-active (cancelled/filled) orders are only queryable within a 7-day window.

    Args:
      client_oid: Client-provided id of the order to look up.
      symbol: Trading pair symbol the order belongs to.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    params: dict = {
      'symbol': symbol,
    }
    return await self.authed_request(
      'GET',
      f'/api/v3/hf/margin/orders/client-order/{client_oid}',
      params=params,
      validator=adapter,
      validate=validate,
    )
