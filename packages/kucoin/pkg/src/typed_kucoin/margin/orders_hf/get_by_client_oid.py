# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import HfMarginOrder


class Request(TypedDict):
  clientOid: str
  """Client-provided id of the order to look up."""
  symbol: str
  """Trading pair symbol the order belongs to."""


Response = HfMarginOrder | None


class GetByClientOid(RpcEndpoint):
  """Get a single margin hf order's full detail by its client-provided order id, or `null` when no such order exists for this account. Non-active (cancelled/filled) orders are only queryable within a 7-day window."""

  async def get_by_client_oid(
    self,
    *,
    client_oid: str,
    symbol: str,
    validate: bool | None = None,
  ) -> Response:
    """Get a single margin hf order's full detail by its client-provided order id, or `null` when no such order exists for this account. Non-active (cancelled/filled) orders are only queryable within a 7-day window.

    Args:
      client_oid: Client-provided id of the order to look up.
      symbol: Trading pair symbol the order belongs to.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/margin-trading/orders/get-order-by-clientoid)
    """
    return await self.request(
      Request(clientOid=client_oid, symbol=symbol),
      method='GET',
      path='/api/v3/hf/margin/orders/client-order/{clientOid}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
