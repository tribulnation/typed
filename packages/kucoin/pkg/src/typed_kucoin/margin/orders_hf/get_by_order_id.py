# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import HfMarginOrder


class Request(TypedDict):
  orderId: str
  """System-generated id of the order to look up."""
  symbol: str
  """Trading pair symbol the order belongs to."""


Response = HfMarginOrder | None


class GetByOrderId(RpcEndpoint):
  """Get a single margin hf order's full detail by its system-generated order id, or `null` when no such order exists for this account. Non-active (cancelled/filled) orders are only queryable within a 7-day window."""

  async def get_by_order_id(
    self,
    *,
    order_id: str,
    symbol: str,
    validate: bool | None = None,
  ) -> Response:
    """Get a single margin hf order's full detail by its system-generated order id, or `null` when no such order exists for this account. Non-active (cancelled/filled) orders are only queryable within a 7-day window.

    Args:
      order_id: System-generated id of the order to look up.
      symbol: Trading pair symbol the order belongs to.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/margin-trading/orders/get-order-by-orderld)
    """
    return await self.request(
      Request(orderId=order_id, symbol=symbol),
      method='GET',
      path='/api/v3/hf/margin/orders/{orderId}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
