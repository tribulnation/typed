# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_kucoin.core import TimestampMillis
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class AffiliateKuminingRecord(TypedDict):
  """One Kumining payout attributed to this affiliate account."""

  uid: str
  """The invitee's UID."""
  payTime: TimestampMillis
  """Unix milliseconds the payout was made."""
  goodsName: str
  """Name of the Kumining product paid out, for example `BTC-10TH`."""
  amount: Decimal
  """Payout amount."""
  currency: str
  """Denomination unit for `amount`. KuCoin does not document a closed set for this field on this endpoint's own page (unlike the sibling Get Invited/Get Commission/Get Trade History/Get Transaction endpoints, which document `USDT`/`USDC` explicitly for their analogous fields), so it's left as a plain string -- see notes."""
  lastId: str
  """This record's cursor id -- pass as the next call's `lastId` to continue paging."""


class Request(TypedDict):
  uid: NotRequired[str]
  """Restrict to one invitee's UID."""
  startAt: NotRequired[TimestampMillis]
  """Restrict to payouts at or after this Unix millisecond timestamp."""
  endAt: NotRequired[TimestampMillis]
  """Restrict to payouts at or before this Unix millisecond timestamp."""
  lastId: NotRequired[str]
  """Cursor for the next page: omit for the first page, then pass the `lastId` of the last record from the previous response."""
  direction: NotRequired[Literal['PRE', 'NEXT']]
  """Which way to page from `lastId`. KuCoin documents no default here, unlike the sibling Get Transaction endpoint's `NEXT`."""
  pageSize: NotRequired[int]
  """Results per page."""


Response = list[AffiliateKuminingRecord]


class Kumining(RpcEndpoint):
  """Walk this affiliate account's Kumining payout records over a time window, cursor-paginated by `lastId`."""

  async def kumining(
    self,
    *,
    uid: str | None = None,
    start_at: TimestampMillis | None = None,
    end_at: TimestampMillis | None = None,
    last_id: str | None = None,
    direction: Literal['PRE', 'NEXT'] | None = None,
    page_size: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Walk this affiliate account's Kumining payout records over a time window, cursor-paginated by `lastId`.

    Args:
      uid: Restrict to one invitee's UID.
      start_at: Restrict to payouts at or after this Unix millisecond timestamp.
      end_at: Restrict to payouts at or before this Unix millisecond timestamp.
      last_id: Cursor for the next page: omit for the first page, then pass the `lastId` of the last record from the previous response.
      direction: Which way to page from `lastId`. KuCoin documents no default here, unlike the sibling Get Transaction endpoint's `NEXT`.
      page_size: Results per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/affiliate/get-kumining)
    """
    request: Request = Request()
    if uid is not None:
      request['uid'] = uid
    if start_at is not None:
      request['startAt'] = start_at
    if end_at is not None:
      request['endAt'] = end_at
    if last_id is not None:
      request['lastId'] = last_id
    if direction is not None:
      request['direction'] = direction
    if page_size is not None:
      request['pageSize'] = page_size
    return await self.request(
      request,
      method='GET',
      path='/api/v2/affiliate/queryKumining',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
