# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_kucoin.core import TimestampMillis
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class ConvertQuote(TypedDict):
  """A market convert quote, locking a price for a short window."""

  quoteId: str
  """Quote id; pass to Add Convert Order to execute a conversion at this price."""
  price: Decimal
  """Quoted exchange rate, `toCurrency` per `fromCurrency`."""
  fromCurrencySize: Decimal
  """Amount of `fromCurrency` this quote covers."""
  toCurrencySize: Decimal
  """Amount of `toCurrency` this quote covers."""
  validUntill: TimestampMillis
  """Unix milliseconds this quote expires. Sic: the venue's own field name misspells `validUntil`."""


class Request(TypedDict):
  fromCurrency: str
  """Source currency, for example `USDT`."""
  toCurrency: str
  """Target currency, for example `BTC`."""
  fromCurrencySize: NotRequired[Decimal]
  """Amount of `fromCurrency` to convert. Exactly one of `fromCurrencySize`/`toCurrencySize` is required by the venue -- it prices the conversion from whichever side an amount was given for."""
  toCurrencySize: NotRequired[Decimal]
  """Desired amount of `toCurrency`. Exactly one of `fromCurrencySize`/`toCurrencySize` is required by the venue."""


class Quote(RpcEndpoint):
  """Get a market convert quote for a currency pair, locking a price for a short window. Pass `quoteId` to `convert.add_order` to execute the conversion at this price."""

  async def quote(
    self,
    *,
    from_currency: str,
    to_currency: str,
    from_currency_size: Decimal | None = None,
    to_currency_size: Decimal | None = None,
    validate: bool | None = None,
  ) -> ConvertQuote:
    """Get a market convert quote for a currency pair, locking a price for a short window. Pass `quoteId` to `convert.add_order` to execute the conversion at this price.

    Args:
      from_currency: Source currency, for example `USDT`.
      to_currency: Target currency, for example `BTC`.
      from_currency_size: Amount of `fromCurrency` to convert. Exactly one of `fromCurrencySize`/`toCurrencySize` is required by the venue -- it prices the conversion from whichever side an amount was given for.
      to_currency_size: Desired amount of `toCurrency`. Exactly one of `fromCurrencySize`/`toCurrencySize` is required by the venue.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/convert/get-convert-quote)
    """
    request: Request = Request(fromCurrency=from_currency, toCurrency=to_currency)
    if from_currency_size is not None:
      request['fromCurrencySize'] = from_currency_size
    if to_currency_size is not None:
      request['toCurrencySize'] = to_currency_size
    return await self.request(
      request,
      method='GET',
      path='/api/v1/convert/quote',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ConvertQuote,
    )
