# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, NotRequired, TypedDict
from typed_kucoin.core import TimestampMillis
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import ConvertLimitOrder


class Request(TypedDict):
  startAt: NotRequired[TimestampMillis]
  """Restrict to orders placed at or after this Unix millisecond timestamp."""
  endAt: NotRequired[TimestampMillis]
  """Restrict to orders placed at or before this Unix millisecond timestamp."""
  page: NotRequired[int]
  """Page number."""
  pageSize: NotRequired[int]
  """Results per page. Confirmed live the same range as the sibling Get Convert Order History: [20, 100]; 10 rejects with `400100 'pageSize not in range value'`."""
  status: NotRequired[str]
  """Restrict to one order status, for example `CANCELLED`. KuCoin does not publish a closed set for this field, so it's left as a plain string rather than a guessed enum."""


class ConvertLimitOrdersPage(TypedDict):
  """One page of convert limit orders."""

  currentPage: int
  """Current page number."""
  pageSize: int
  """Results per page, as applied."""
  totalNum: int
  """Total matching records."""
  totalPage: int
  """Total number of pages."""
  items: list[ConvertLimitOrder]
  """Orders on this page, most recent first."""


class LimitOrders(RpcEndpoint):
  """Page through this account's convert limit orders."""

  async def limit_orders_paged(
    self,
    *,
    start_at: TimestampMillis | None = None,
    end_at: TimestampMillis | None = None,
    page_size: int | None = None,
    status: str | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[ConvertLimitOrdersPage]:
    """Page through this account's convert limit orders.

    Paged variant of `limit_orders`: Requests `page` from 1 upwards and stops once it has covered the `totalPage` pages the response reports, or after `max_pages` pages when one is given.

    Args:
      start_at: Restrict to orders placed at or after this Unix millisecond timestamp.
      end_at: Restrict to orders placed at or before this Unix millisecond timestamp.
      page_size: Results per page. Confirmed live the same range as the sibling Get Convert Order History: [20, 100]; 10 rejects with `400100 'pageSize not in range value'`.
      status: Restrict to one order status, for example `CANCELLED`. KuCoin does not publish a closed set for this field, so it's left as a plain string rather than a guessed enum.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/convert/get-convert-limit-orders)
    """
    page = 1
    pages = 0
    while True:
      response = await self.limit_orders(
        start_at=start_at,
        end_at=end_at,
        page_size=page_size,
        status=status,
        page=page,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      total = response.get('totalPage') if response is not None else None
      total = int(total) if total is not None else None
      if total is None or pages >= total:
        break
      page += 1

  async def limit_orders(
    self,
    *,
    start_at: TimestampMillis | None = None,
    end_at: TimestampMillis | None = None,
    page: int | None = None,
    page_size: int | None = None,
    status: str | None = None,
    validate: bool | None = None,
  ) -> ConvertLimitOrdersPage:
    """Page through this account's convert limit orders.

    Args:
      start_at: Restrict to orders placed at or after this Unix millisecond timestamp.
      end_at: Restrict to orders placed at or before this Unix millisecond timestamp.
      page: Page number.
      page_size: Results per page. Confirmed live the same range as the sibling Get Convert Order History: [20, 100]; 10 rejects with `400100 'pageSize not in range value'`.
      status: Restrict to one order status, for example `CANCELLED`. KuCoin does not publish a closed set for this field, so it's left as a plain string rather than a guessed enum.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/convert/get-convert-limit-orders)
    """
    request: Request = Request()
    if start_at is not None:
      request['startAt'] = start_at
    if end_at is not None:
      request['endAt'] = end_at
    if page is not None:
      request['page'] = page
    if page_size is not None:
      request['pageSize'] = page_size
    if status is not None:
      request['status'] = status
    return await self.request(
      request,
      method='GET',
      path='/api/v1/convert/limit/orders',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ConvertLimitOrdersPage,
    )
