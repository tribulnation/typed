# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import ConvertOrder


class Request(TypedDict):
  clientOrderId: NotRequired[str]
  """Client-chosen order id from the order's placement. At least one of `clientOrderId`/`orderId` is required by the venue."""
  orderId: NotRequired[int]
  """System-generated order id. At least one of `clientOrderId`/`orderId` is required by the venue."""


Response = ConvertOrder | None


class OrderDetail(RpcEndpoint):
  """Get one executed market convert order by id. Returns `null` rather than an error when no order matches."""

  async def order_detail(
    self,
    client_order_id: str | None = None,
    *,
    order_id: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get one executed market convert order by id. Returns `null` rather than an error when no order matches.

    Args:
      client_order_id: Client-chosen order id from the order's placement. At least one of `clientOrderId`/`orderId` is required by the venue.
      order_id: System-generated order id. At least one of `clientOrderId`/`orderId` is required by the venue.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/convert/get-convert-order-detail)
    """
    request: Request = Request()
    if client_order_id is not None:
      request['clientOrderId'] = client_order_id
    if order_id is not None:
      request['orderId'] = order_id
    return await self.request(
      request,
      method='GET',
      path='/api/v1/convert/order/detail',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
