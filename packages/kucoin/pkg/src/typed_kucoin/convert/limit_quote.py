# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_kucoin.core import TimestampMillis
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class ConvertLimitQuote(TypedDict):
  """The protection price threshold a convert limit order must meet or beat."""

  price: Decimal
  """Protection price: the least favorable rate a limit order at this pair/size may be placed at."""
  validUntill: TimestampMillis
  """Unix milliseconds this protection price expires. Sic: the venue's own field name misspells `validUntil`."""


class Request(TypedDict):
  fromCurrency: str
  """Source currency, for example `USDT`."""
  toCurrency: str
  """Target currency, for example `BTC`."""
  fromCurrencySize: NotRequired[Decimal]
  """Amount of `fromCurrency` the intended order would convert. Exactly one of `fromCurrencySize`/`toCurrencySize` is required by the venue."""
  toCurrencySize: NotRequired[Decimal]
  """Desired amount of `toCurrency`. Exactly one of `fromCurrencySize`/`toCurrencySize` is required by the venue."""


class LimitQuote(RpcEndpoint):
  """Get the protection price a convert limit order for a pair/size must meet or beat."""

  async def limit_quote(
    self,
    *,
    from_currency: str,
    to_currency: str,
    from_currency_size: Decimal | None = None,
    to_currency_size: Decimal | None = None,
    validate: bool | None = None,
  ) -> ConvertLimitQuote:
    """Get the protection price a convert limit order for a pair/size must meet or beat.

    Args:
      from_currency: Source currency, for example `USDT`.
      to_currency: Target currency, for example `BTC`.
      from_currency_size: Amount of `fromCurrency` the intended order would convert. Exactly one of `fromCurrencySize`/`toCurrencySize` is required by the venue.
      to_currency_size: Desired amount of `toCurrency`. Exactly one of `fromCurrencySize`/`toCurrencySize` is required by the venue.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/convert/get-convert-limit-quote)
    """
    request: Request = Request(fromCurrency=from_currency, toCurrency=to_currency)
    if from_currency_size is not None:
      request['fromCurrencySize'] = from_currency_size
    if to_currency_size is not None:
      request['toCurrencySize'] = to_currency_size
    return await self.request(
      request,
      method='GET',
      path='/api/v1/convert/limit/quote',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ConvertLimitQuote,
    )
