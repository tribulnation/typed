# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /api/v1/convert/symbol` — Get Convert Symbol."""

from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint


class ConvertSymbolLimits(TypedDict):
  """Conversion size limits for one currency pair and order type."""

  fromCurrency: str
  """Source currency, for example `BTC`."""
  toCurrency: str
  """Target currency, for example `USDT`."""
  fromCurrencyMaxSize: str
  """Maximum amount convertible from `fromCurrency` in one order."""
  fromCurrencyMinSize: str
  """Minimum amount convertible from `fromCurrency` in one order."""
  fromCurrencyStep: str
  """Size increment `fromCurrency` amounts must align to."""
  toCurrencyMaxSize: str
  """Maximum amount receivable in `toCurrency` in one order."""
  toCurrencyMinSize: str
  """Minimum amount receivable in `toCurrency` in one order."""
  toCurrencyStep: str
  """Size increment `toCurrency` amounts must align to."""


_Type = ConvertSymbolLimits
adapter = validator[_Type](_Type)  # type: ignore


class Symbol(RpcEndpoint):
  """`Get Convert Symbol` — mixed into `Convert`, the product exposing `convert.symbol`."""

  async def symbol(
    self,
    *,
    from_currency: str,
    to_currency: str,
    order_type: str,
    validate: bool | None = None,
  ) -> ConvertSymbolLimits:
    """Get the conversion size limits for one currency pair and convert order type.

    Args:
      from_currency: Source currency, for example `BTC`.
      to_currency: Target currency, for example `USDT`.
      order_type: Convert order type this pair's limits apply to. The venue's docs show `MARKET` in their only example and don't publish a closed set; confirmed live that `market`/`0`/`1` (wrong case or type) are rejected with `102411 orderType error`, while `MARKET` and `LIMIT` both pass that validation -- so the set is at least `{MARKET, LIMIT}`, but since the venue doesn't document the values in a table this is left as a plain string rather than a guessed enum.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    params: dict = {
      'fromCurrency': from_currency,
      'toCurrency': to_currency,
      'orderType': order_type,
    }
    return await self.authed_request(
      'GET',
      '/api/v1/convert/symbol',
      params=params,
      validator=adapter,
      validate=validate,
    )
