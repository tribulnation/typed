# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class ConvertCurrency(TypedDict):
  """One currency available for convert, with its overall size limits."""

  currency: str
  """Currency code, for example `BTC`."""
  maxSize: Decimal
  """Maximum convertible amount in one order."""
  minSize: Decimal
  """Minimum convertible amount in one order."""
  step: Decimal
  """Size increment amounts must align to."""
  tradeDirection: str
  """Which side(s) of a conversion this currency supports. Every live currency observed (333 rows, 2026-08-08) carries `ALL`; KuCoin does not publish a closed set for this field, so it's left as a plain string rather than a guessed enum."""


class ConvertCurrencyUsdtLimit(TypedDict):
  """One currency's convert size limit expressed against USDT."""

  currency: str
  """Currency code, for example `BTC`."""
  maxSize: Decimal
  """Maximum convertible amount, in USDT terms."""
  minSize: Decimal
  """Minimum convertible amount, in USDT terms."""
  step: Decimal
  """Size increment amounts must align to, in USDT terms."""


class ConvertCurrencies(TypedDict):
  """Every currency available for convert, plus the same limits expressed in USDT."""

  currencies: list[ConvertCurrency]
  """Currencies available for convert, with their own-unit size limits."""
  usdtCurrencyLimit: list[ConvertCurrencyUsdtLimit]
  """The same currencies' size limits, expressed in USDT. USDT itself is absent (it's the quote currency the limits are expressed against)."""


class Currencies(RpcEndpoint):
  """List every currency available for convert, with its own-unit size limits and the same limits expressed in USDT."""

  async def currencies(self, *, validate: bool | None = None) -> ConvertCurrencies:
    """List every currency available for convert, with its own-unit size limits and the same limits expressed in USDT.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/convert/get-convert-currencies)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v1/convert/currencies',
      meta={},
      validate=validate,
      response_type=ConvertCurrencies,
    )
