# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Identifies the convert limit order to cancel."""

  clientOrderId: str
  """Client-chosen order id from the order's placement."""


class CancelLimitOrder(RpcEndpoint):
  """Cancel a resting convert limit order by its client order id."""

  async def cancel_limit_order(
    self,
    client_order_id: str,
    *,
    validate: bool | None = None,
  ) -> None:
    """Cancel a resting convert limit order by its client order id.

    Args:
      client_order_id: Client-chosen order id from the order's placement.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/convert/cancel-convert-limit-order)
    """
    return await self.request(
      Request(clientOrderId=client_order_id),
      method='DELETE',
      path='/api/v1/convert/limit/order/cancel',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=None,
    )
