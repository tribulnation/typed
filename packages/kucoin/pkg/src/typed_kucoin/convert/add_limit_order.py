# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import ConvertOrderResult


class Request(TypedDict):
  """Parameters to place a convert limit order."""

  clientOrderId: str
  """Client-chosen order id, unique per account."""
  fromCurrency: str
  """Source currency, for example `USDT`."""
  toCurrency: str
  """Target currency, for example `BTC`."""
  fromCurrencySize: Decimal
  """Amount of `fromCurrency` to convert."""
  toCurrencySize: Decimal
  """Amount of `toCurrency` to receive; implies the order's limit price together with `fromCurrencySize`."""


class AddLimitOrder(RpcEndpoint):
  """Place a convert limit order, resting until it fills, is cancelled, or expires. The venue rejects a price worse than Get Convert Limit Quote's protection price; querying that endpoint first is recommended."""

  async def add_limit_order(
    self,
    *,
    client_order_id: str,
    from_currency: str,
    to_currency: str,
    from_currency_size: Decimal,
    to_currency_size: Decimal,
    validate: bool | None = None,
  ) -> ConvertOrderResult:
    """Place a convert limit order, resting until it fills, is cancelled, or expires. The venue rejects a price worse than Get Convert Limit Quote's protection price; querying that endpoint first is recommended.

    Args:
      client_order_id: Client-chosen order id, unique per account.
      from_currency: Source currency, for example `USDT`.
      to_currency: Target currency, for example `BTC`.
      from_currency_size: Amount of `fromCurrency` to convert.
      to_currency_size: Amount of `toCurrency` to receive; implies the order's limit price together with `fromCurrencySize`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/convert/add-convert-limit-order)
    """
    return await self.request(
      Request(
        clientOrderId=client_order_id,
        fromCurrency=from_currency,
        toCurrency=to_currency,
        fromCurrencySize=from_currency_size,
        toCurrencySize=to_currency_size,
      ),
      method='POST',
      path='/api/v1/convert/limit/order',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ConvertOrderResult,
    )
