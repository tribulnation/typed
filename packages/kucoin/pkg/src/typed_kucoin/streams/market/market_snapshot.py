# Generated by typed-dev — see CONTRIBUTING.md.
"""`/market/snapshot:{market}` — Subscribe to full trading-pair statistics snapshots for every symbol quoted against one market/currency, refreshed once every 2 seconds -- one push per symbol in that market, not one push for the market as a whole."""

from typing_extensions import Any
from typed_core.util import StreamManager
from typed_core.validation import TypedDict, validator
from typed_kucoin.types import SymbolStats, WsSubscriptionAck
from typed_kucoin.core import StreamEndpoint


class MarketSnapshotUpdate(TypedDict):
  """One symbol's snapshot within the subscribed market -- the unwrapped `data` field of the raw WebSocket frame (`SocketStreamClient.subscribe` extracts `data` before handing the stream to the caller; see `spec/core.md`'s WebSocket section). Same `{sequence, data}` wrapper shape as `symbolSnapshot`; see `notes` for a live type drift on `sequence`."""

  sequence: int
  """Confirmed live as a raw Unix-millisecond-scale integer on this channel, not the numeric string KuCoin's docs and machine spec claim -- see `notes`."""
  data: SymbolStats


_Type = MarketSnapshotUpdate
validate_update = validator[_Type](_Type)  # type: ignore


class MarketSnapshot(StreamEndpoint):
  """Subscribe to `/market/snapshot:{market}` — mixed into `Market`, the product exposing `streams.spot_margin.market_snapshot`."""

  def market_snapshot(
    self,
    market: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[MarketSnapshotUpdate, Any, Any]:
    """Subscribe to full trading-pair statistics snapshots for every symbol quoted against one market/currency, refreshed once every 2 seconds -- one push per symbol in that market, not one push for the market as a whole.

    Args:
      market: Market/currency grouping, for example `BTC` (every symbol quoted against BTC, such as `ETH-BTC`) -- not a trading-pair symbol itself.
      validate: Whether to validate pushed payloads against the expected schema.

    Returns:
      A manager for the subscription stream.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    return self.subscribe(
      f'/market/snapshot:{market}',
      validator=validate_update,
      validate=validate,
    )
