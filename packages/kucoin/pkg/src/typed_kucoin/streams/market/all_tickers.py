# Generated by typed-dev — see CONTRIBUTING.md.
"""`/market/ticker:all` — Subscribe to best-bid/offer (BBO) ticker pushes for every symbol on the exchange, on one fixed topic -- no subscription parameters."""

from typing_extensions import Any
from typed_core.util import StreamManager
from typed_core.validation import TypedDict, validator
from typed_kucoin.core import StreamEndpoint, TimestampMillis
from typed_kucoin.types import WsSubscriptionAck


class AllTickersUpdate(TypedDict):
  """One best-bid/offer snapshot for whichever symbol just changed -- the unwrapped `data` field of the raw WebSocket frame (`SocketStreamClient.subscribe` extracts `data` before handing the stream to the caller; see `spec/core.md`'s WebSocket section). Same shape as the single-symbol `Ticker` push (`streams.spot_margin.ticker`), but see `notes` for a caller-usability gap this channel exposes that `ticker` does not."""

  sequence: str
  """Sequence number."""
  price: str
  """Last traded price."""
  size: str
  """Last traded amount."""
  bestAsk: str
  """Best ask price."""
  bestAskSize: str
  """Best ask size."""
  bestBid: str
  """Best bid price."""
  bestBidSize: str
  """Best bid size."""
  time: TimestampMillis
  """Unix milliseconds of the latest matched transaction."""


_Type = AllTickersUpdate
validate_update = validator[_Type](_Type)  # type: ignore


class AllTickers(StreamEndpoint):
  """Subscribe to `/market/ticker:all` — mixed into `Market`, the product exposing `streams.spot_margin.all_tickers`."""

  def all_tickers(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[AllTickersUpdate, Any, Any]:
    """Subscribe to best-bid/offer (BBO) ticker pushes for every symbol on the exchange, on one fixed topic -- no subscription parameters.

    Args:
      validate: Whether to validate pushed payloads against the expected schema.

    Returns:
      A manager for the subscription stream.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    return self.subscribe(
      '/market/ticker:all',
      validator=validate_update,
      validate=validate,
    )
