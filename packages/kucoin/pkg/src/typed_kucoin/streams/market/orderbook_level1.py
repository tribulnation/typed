# Generated by typed-dev — see CONTRIBUTING.md.
"""`/spotMarket/level1:{symbol}` — Subscribe to the single best ask/bid order for one symbol -- pushed once every 10ms, only when the top of book changes."""

from typing_extensions import Any
from typed_core.util import StreamManager
from typed_core.validation import TypedDict, validator
from typed_kucoin.core import StreamEndpoint, TimestampMillis
from typed_kucoin.types import WsSubscriptionAck


class OrderbookLevel1update(TypedDict):
  """Best ask/bid snapshot for a symbol -- the unwrapped `data` field of the raw WebSocket frame (`SocketStreamClient.subscribe` extracts `data` before handing the stream to the caller; see `spec/core.md`'s WebSocket section)."""

  asks: tuple[str, str]
  """Best ask: `[price, size]`."""
  bids: tuple[str, str]
  """Best bid: `[price, size]`."""
  timestamp: TimestampMillis
  """Unix milliseconds of this update."""


_Type = OrderbookLevel1update
validate_update = validator[_Type](_Type)  # type: ignore


class OrderbookLevel1(StreamEndpoint):
  """Subscribe to `/spotMarket/level1:{symbol}` — mixed into `Market`, the product exposing `streams.spot_margin.orderbook_level1`."""

  def orderbook_level1(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[OrderbookLevel1update, Any, Any]:
    """Subscribe to the single best ask/bid order for one symbol -- pushed once every 10ms, only when the top of book changes.

    Args:
      symbol: Trading pair symbol, for example `BTC-USDT`. The venue's own topic syntax accepts a comma-joined list of up to 100 symbols on one subscription; this spec models the single-symbol form.
      validate: Whether to validate pushed payloads against the expected schema.

    Returns:
      A manager for the subscription stream.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    return self.subscribe(
      f'/spotMarket/level1:{symbol}',
      validator=validate_update,
      validate=validate,
    )
