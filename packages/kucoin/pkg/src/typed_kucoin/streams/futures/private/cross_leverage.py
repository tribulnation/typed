# Generated by typed-dev — see CONTRIBUTING.md.
"""`/contract/crossLeverage` — Subscribe to the authenticated account's cross-margin futures leverage change pushes, across every contract symbol -- fires whenever the account's cross-margin leverage multiple changes for a symbol (`futures.positions.modify_cross_margin_leverage`)."""

from typing_extensions import Any
from typed_core.util import StreamManager
from typed_core.validation import TypedDict, validator
from typed_kucoin.types import WsSubscriptionAck
from typed_kucoin.core import StreamEndpoint


class FuturesCrossLeverageValue(TypedDict):
  """One symbol's current cross-margin leverage."""

  leverage: str
  """Leverage multiple now in effect for this symbol."""


_Type = dict[str, FuturesCrossLeverageValue]
validate_update = validator[_Type](_Type)  # type: ignore


class CrossLeverage(StreamEndpoint):
  """Subscribe to `/contract/crossLeverage` — mixed into `Private`, the product exposing `streams.futures.cross_leverage`."""

  def cross_leverage(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[dict[str, FuturesCrossLeverageValue], Any, Any]:
    """Subscribe to the authenticated account's cross-margin futures leverage change pushes, across every contract symbol -- fires whenever the account's cross-margin leverage multiple changes for a symbol (`futures.positions.modify_cross_margin_leverage`).

    Args:
      validate: Whether to validate pushed payloads against the expected schema.

    Returns:
      A manager for the subscription stream.

    Raises:
      AuthError: This connection has no credentials — this is a private channel, unreachable from a `public=True` client.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    return self.authed_subscribe(
      '/contract/crossLeverage',
      validator=validate_update,
      validate=validate,
    )
