# Generated by typed-dev — see CONTRIBUTING.md.
"""`/contractMarket/tradeOrders` — Subscribe to the authenticated account's futures order lifecycle pushes across every contract symbol -- the all-symbol counterpart of `streams.futures.order`, fires on every state change (open, match, update, filled, canceled) of any futures order the account places, on any symbol."""

from typing_extensions import Any
from typed_core.util import StreamManager
from typed_core.validation import validator
from typed_kucoin.types import FuturesOrderEvent, WsSubscriptionAck
from typed_kucoin.core import StreamEndpoint


_Type = FuturesOrderEvent
validate_update = validator[_Type](_Type)  # type: ignore


class AllOrders(StreamEndpoint):
  """Subscribe to `/contractMarket/tradeOrders` — mixed into `Private`, the product exposing `streams.futures.all_orders`."""

  def all_orders(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[FuturesOrderEvent, Any, Any]:
    """Subscribe to the authenticated account's futures order lifecycle pushes across every contract symbol -- the all-symbol counterpart of `streams.futures.order`, fires on every state change (open, match, update, filled, canceled) of any futures order the account places, on any symbol.

    Args:
      validate: Whether to validate pushed payloads against the expected schema.

    Returns:
      A manager for the subscription stream.

    Raises:
      AuthError: This connection has no credentials — this is a private channel, unreachable from a `public=True` client.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    return self.authed_subscribe(
      '/contractMarket/tradeOrders',
      validator=validate_update,
      validate=validate,
    )
