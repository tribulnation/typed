# Generated by typed-dev — see CONTRIBUTING.md.
"""`/contractMarket/tradeOrders:{symbol}` — Subscribe to the authenticated account's futures order lifecycle pushes for one contract symbol -- fires on every state change (open, match, update, filled, canceled) of any order the account places on that symbol."""

from typing_extensions import Any
from typed_core.util import StreamManager
from typed_core.validation import validator
from typed_kucoin.types import FuturesOrderEvent, WsSubscriptionAck
from typed_kucoin.core import StreamEndpoint


_Type = FuturesOrderEvent
validate_update = validator[_Type](_Type)  # type: ignore


class Order(StreamEndpoint):
  """Subscribe to `/contractMarket/tradeOrders:{symbol}` — mixed into `Private`, the product exposing `streams.futures.order`."""

  def order(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[FuturesOrderEvent, Any, Any]:
    """Subscribe to the authenticated account's futures order lifecycle pushes for one contract symbol -- fires on every state change (open, match, update, filled, canceled) of any order the account places on that symbol.

    Args:
      symbol: Contract symbol to watch, for example `XBTUSDTM`.
      validate: Whether to validate pushed payloads against the expected schema.

    Returns:
      A manager for the subscription stream.

    Raises:
      AuthError: This connection has no credentials — this is a private channel, unreachable from a `public=True` client.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    return self.authed_subscribe(
      f'/contractMarket/tradeOrders:{symbol}',
      validator=validate_update,
      validate=validate,
    )
