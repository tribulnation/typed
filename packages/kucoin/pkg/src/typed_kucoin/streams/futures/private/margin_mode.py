# Generated by typed-dev — see CONTRIBUTING.md.
"""`/contract/marginMode` — Subscribe to the authenticated account's futures margin-mode change pushes, across every contract symbol -- fires whenever the account switches a symbol between cross and isolated margin (`futures.positions.switch_margin_mode`/`batch_switch_margin_mode`)."""

from typing_extensions import Any, Literal
from typed_core.util import StreamManager
from typed_core.validation import validator
from typed_kucoin.types import WsSubscriptionAck
from typed_kucoin.core import StreamEndpoint


_Type = dict[str, Literal['CROSS', 'ISOLATED']]
validate_update = validator[_Type](_Type)  # type: ignore


class MarginMode(StreamEndpoint):
  """Subscribe to `/contract/marginMode` — mixed into `Private`, the product exposing `streams.futures.margin_mode`."""

  def margin_mode(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[dict[str, Literal['CROSS', 'ISOLATED']], Any, Any]:
    """Subscribe to the authenticated account's futures margin-mode change pushes, across every contract symbol -- fires whenever the account switches a symbol between cross and isolated margin (`futures.positions.switch_margin_mode`/`batch_switch_margin_mode`).

    Args:
      validate: Whether to validate pushed payloads against the expected schema.

    Returns:
      A manager for the subscription stream.

    Raises:
      AuthError: This connection has no credentials — this is a private channel, unreachable from a `public=True` client.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    return self.authed_subscribe(
      '/contract/marginMode',
      validator=validate_update,
      validate=validate,
    )
