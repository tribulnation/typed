# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typed_core.util import StreamManager
from typing_extensions import Any, TypedDict
from typed_kucoin.core import TimestampMillis
from typed_kucoin.core.endpoint.stream import PublicStreamEndpoint


class OrderbookLevel5Update(TypedDict):
  """5-level ask/bid snapshot for a symbol -- the unwrapped `data` field of the raw WebSocket frame (`SocketStreamClient.subscribe` extracts `data` before handing the stream to the caller; see `spec/core.md`'s WebSocket section)."""

  asks: list[tuple[Decimal, Decimal]]
  """Up to 5 best ask levels, each `[price, size]`, nearest first."""
  bids: list[tuple[Decimal, Decimal]]
  """Up to 5 best bid levels, each `[price, size]`, nearest first."""
  timestamp: TimestampMillis
  """Unix milliseconds of this update."""


class OrderbookLevel5(PublicStreamEndpoint):
  """Subscribe to the 5 best ask/bid orders for one symbol -- pushed once every 100ms, only when the book changes within that depth."""

  def orderbook_level5(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[OrderbookLevel5Update, Any, Any]:
    """Subscribe to the 5 best ask/bid orders for one symbol -- pushed once every 100ms, only when the book changes within that depth.

    Args:
      symbol: Trading pair symbol, for example `BTC-USDT`. The venue's own topic syntax accepts a comma-joined list of up to 100 symbols on one subscription; this spec models the single-symbol form.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/3470069w0)
    """
    return self.subscribe(
      f'/spotMarket/level2Depth5:{symbol}',
      validate=validate,
      response_type=OrderbookLevel5Update,
    )
