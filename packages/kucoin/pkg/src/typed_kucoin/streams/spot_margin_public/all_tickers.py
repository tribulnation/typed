# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any
from typed_kucoin.core.endpoint.stream import PublicStreamEndpoint
from typed_kucoin.schemas import SpotTicker


class AllTickers(PublicStreamEndpoint):
  """Subscribe to best-bid/offer (BBO) ticker pushes for every symbol on the exchange, on one fixed topic -- no subscription parameters."""

  def all_tickers(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[SpotTicker, Any, Any]:
    """Subscribe to best-bid/offer (BBO) ticker pushes for every symbol on the exchange, on one fixed topic -- no subscription parameters.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/3470064w0)
    """
    return self.subscribe(
      '/market/ticker:all',
      None,
      validate=validate,
      response_type=SpotTicker,
    )
