# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_kucoin.core import TimestampNanos
from typed_kucoin.core.endpoint.stream import PublicStreamEndpoint


class TradeUpdate(TypedDict):
  """One executed trade -- the unwrapped `data` field of the raw WebSocket frame (`SocketStreamClient.subscribe` extracts `data` before handing the stream to the caller; see `spec/core.md`'s WebSocket section)."""

  makerOrderId: str
  """Order id of the maker side."""
  price: Decimal
  """Execution price."""
  sequence: str
  """Sequence number, equal to `tradeId` on live evidence -- see `notes`."""
  side: Literal['buy', 'sell']
  """Taker side of this trade."""
  size: Decimal
  """Amount executed."""
  symbol: str
  """Trading pair symbol."""
  takerOrderId: str
  """Order id of the taker side."""
  time: TimestampNanos
  """Unix nanoseconds of the match, sent as a numeric string on this channel -- unlike most REST timestamp fields, which are real JSON integers."""
  tradeId: str
  """Trade id generated by the matching engine."""
  type: Literal['match']
  """Event type. KuCoin's own docs and machine spec document only one value for this channel."""


class Trade(PublicStreamEndpoint):
  """Subscribe to Level 3 matching-event pushes (individual trade executions) for one symbol, in real time."""

  def trade(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[TradeUpdate, Any, Any]:
    """Subscribe to Level 3 matching-event pushes (individual trade executions) for one symbol, in real time.

    Args:
      symbol: Trading pair symbol, for example `BTC-USDT`. The venue's own topic syntax accepts a comma-joined list of up to 100 symbols on one subscription; this spec models the single-symbol form.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/3470072w0)
    """
    return self.subscribe(
      f'/market/match:{symbol}',
      validate=validate,
      response_type=TradeUpdate,
    )
