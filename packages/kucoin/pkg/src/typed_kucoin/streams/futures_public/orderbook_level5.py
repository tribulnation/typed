# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any
from typed_kucoin.core.endpoint.stream import PublicStreamEndpoint
from typed_kucoin.schemas import FuturesOrderBookLevelUpdate


class OrderbookLevel5(PublicStreamEndpoint):
  """Subscribe to the 5 best bid/ask price levels for one futures contract, pushed every 100ms while the book changes -- a full snapshot on every push, not an incremental update. No push when nothing has changed."""

  def orderbook_level5(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[FuturesOrderBookLevelUpdate, Any, Any]:
    """Subscribe to the 5 best bid/ask price levels for one futures contract, pushed every 100ms while the book changes -- a full snapshot on every push, not an incremental update. No push when nothing has changed.

    Args:
      symbol: Contract symbol to watch, for example `XBTUSDTM`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/3470083w0)
    """
    return self.subscribe(
      f'/contractMarket/level2Depth5:{symbol}',
      validate=validate,
      response_type=FuturesOrderBookLevelUpdate,
    )
