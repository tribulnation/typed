# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, TypedDict
from typed_kucoin.core import TimestampMillis
from typed_kucoin.core.endpoint.stream import PublicStreamEndpoint


class FuturesFundingSettlementEvent(TypedDict):
  """One funding fee settlement event -- the unwrapped `data` field of the raw WebSocket frame (`SocketStreamClient.subscribe` extracts `data` before handing the stream to the caller; see `spec/core.md`'s WebSocket section). The wire `subject` (`funding.begin`/`funding.end`) that distinguishes settlement start from end is stripped along with the rest of the envelope and never reaches the caller."""

  symbol: str
  """Contract symbol, for example `XBTUSDTM`."""
  fundingTime: TimestampMillis
  """Funding settlement time, Unix milliseconds."""
  fundingRate: float
  """Settled funding rate for this interval."""
  timestamp: TimestampMillis
  """Unix milliseconds when this push was generated -- equal to `fundingTime` on the `funding.begin` push, later on the paired `funding.end` push, per the venue's own worked example."""


class Announcement(PublicStreamEndpoint):
  """Subscribe to a futures contract's funding fee settlement events -- fires twice per settlement (settlement start, then settlement end), every 8 hours."""

  def announcement(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[FuturesFundingSettlementEvent, Any, Any]:
    """Subscribe to a futures contract's funding fee settlement events -- fires twice per settlement (settlement start, then settlement end), every 8 hours.

    Args:
      symbol: Contract symbol to watch, for example `XBTUSDTM`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/3470088w0)
    """
    return self.subscribe(
      f'/contract/announcement:{symbol}',
      validate=validate,
      response_type=FuturesFundingSettlementEvent,
    )
