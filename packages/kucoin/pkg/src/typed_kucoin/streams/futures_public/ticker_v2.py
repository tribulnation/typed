# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typed_core.util import StreamManager
from typing_extensions import Any, TypedDict
from typed_kucoin.core import TimestampNanos
from typed_kucoin.core.endpoint.stream import PublicStreamEndpoint


class FuturesTickerV2Update(TypedDict):
  """One best-bid/offer snapshot for a futures contract -- the unwrapped `data` field of the raw WebSocket frame (`SocketStreamClient.subscribe` extracts `data` before handing the stream to the caller; see `spec/core.md`'s WebSocket section)."""

  symbol: str
  """Contract symbol, for example `XBTUSDTM`."""
  sequence: int
  """Sequence number, shared with the Orderbook Increment topic -- increases chronologically but is not guaranteed continuous, since it only advances when the best-bid/best-ask price or size changes."""
  bestBidSize: int
  """Quantity (in contracts) at the best bid price."""
  bestBidPrice: Decimal
  """Best bid price."""
  bestAskPrice: Decimal
  """Best ask price."""
  bestAskSize: int
  """Quantity (in contracts) at the best ask price."""
  ts: TimestampNanos
  """Timestamp of this push, in nanoseconds."""


class TickerV2(PublicStreamEndpoint):
  """Subscribe to real-time best-bid/offer (BBO) pushes for one futures contract. The system pushes whenever the order book changes -- not necessarily a best-bid/best-ask price change -- in real time."""

  def ticker_v2(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[FuturesTickerV2Update, Any, Any]:
    """Subscribe to real-time best-bid/offer (BBO) pushes for one futures contract. The system pushes whenever the order book changes -- not necessarily a best-bid/best-ask price change -- in real time.

    Args:
      symbol: Contract symbol to watch, for example `XBTUSDTM`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/3470080w0)
    """
    return self.subscribe(
      f'/contractMarket/tickerV2:{symbol}',
      validate=validate,
      response_type=FuturesTickerV2Update,
    )
