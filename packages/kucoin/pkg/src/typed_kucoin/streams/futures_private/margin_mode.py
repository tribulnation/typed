# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal
from typed_kucoin.core.endpoint.stream import PrivateStreamEndpoint


FuturesMarginModeEvent = dict[str, Literal['CROSS', 'ISOLATED']]


class MarginMode(PrivateStreamEndpoint):
  """Subscribe to the authenticated account's futures margin-mode change pushes, across every contract symbol -- fires whenever the account switches a symbol between cross and isolated margin (`futures.positions.switch_margin_mode`/`batch_switch_margin_mode`)."""

  def margin_mode(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[FuturesMarginModeEvent, Any, Any]:
    """Subscribe to the authenticated account's futures margin-mode change pushes, across every contract symbol -- fires whenever the account switches a symbol between cross and isolated margin (`futures.positions.switch_margin_mode`/`batch_switch_margin_mode`).

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/3470095w0)
    """
    return self.subscribe(
      '/contract/marginMode',
      None,
      validate=validate,
      response_type=FuturesMarginModeEvent,
    )
