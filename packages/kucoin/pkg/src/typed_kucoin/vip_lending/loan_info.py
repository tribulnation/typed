# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /api/v1/otc-loan/loan` — Get Loan Info."""

from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint


class LoanMargin(TypedDict):
  """Margin detail for one currency backing the loan."""

  marginCcy: str
  """Margin currency."""
  marginQty: str
  """Maintenance quantity, calculated with `marginFactor`."""
  marginFactor: str
  """Margin coefficient converting this currency's quantity to a real-time USDT discount rate."""


class LoanOrder(TypedDict):
  """One outstanding loan order."""

  orderId: str
  """Loan order ID."""
  principal: str
  """Principal still to be repaid."""
  interest: str
  """Interest still to be repaid."""
  currency: str
  """Loan currency."""


class LoanToValue(TypedDict):
  """Loan-to-value thresholds and the account's current ratio."""

  transferLtv: str
  """LTV threshold above which transfers to the funding account are restricted."""
  onlyClosePosLtv: str
  """LTV threshold above which the account is restricted to closing positions (reduce-only)."""
  delayedLiquidationLtv: str
  """LTV threshold that triggers delayed liquidation."""
  instantLiquidationLtv: str
  """LTV threshold that triggers instant liquidation."""
  currentLtv: str
  """The account's current loan-to-value ratio."""


class LoanInfo(TypedDict):
  parentUid: str
  """Master account UID."""
  orders: list[LoanOrder]
  """Outstanding loan orders."""
  ltv: LoanToValue
  totalMarginAmount: str
  """Total margin amount, in USDT."""
  transferMarginAmount: str
  """Maintenance margin required before transfers to the funding account are restricted, in USDT."""
  margins: list[LoanMargin]
  """Per-currency margin detail."""


_Type = LoanInfo
adapter = validator[_Type](_Type)  # type: ignore


class LoanInfoEndpoint(RpcEndpoint):
  """`Get Loan Info` — mixed into `VipLending`, the product exposing `vip_lending.loan_info`."""

  async def loan_info(self, *, validate: bool | None = None) -> LoanInfo:
    """Get off-exchange (OTC) funding and loan details for the authenticated VIP loan: outstanding orders, loan-to-value thresholds, and per-currency margin. Only returns data for an account currently participating in a VIP loan -- KuCoin rejects the call for one that isn't (see notes).

    Args:
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    return await self.authed_request(
      'GET',
      '/api/v1/otc-loan/loan',
      validator=adapter,
      validate=validate,
    )
