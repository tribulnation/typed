# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_kucoin.core import TimestampSeconds
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  symbol: str
  """Trading pair symbol, e.g. `BTC-USDT`."""
  type: Literal[
    '1min',
    '3min',
    '5min',
    '15min',
    '30min',
    '1hour',
    '2hour',
    '4hour',
    '6hour',
    '8hour',
    '12hour',
    '1day',
    '1week',
    '1month',
  ]
  """Candle interval."""
  startAt: NotRequired[TimestampSeconds]
  """Start of the window, Unix seconds. Inclusive — confirmed live (see `notes`)."""
  endAt: NotRequired[TimestampSeconds]
  """End of the window, Unix seconds. Exclusive — confirmed live (see `notes`)."""


Response = list[
  tuple[TimestampSeconds, Decimal, Decimal, Decimal, Decimal, Decimal, Decimal]
]


class Klines(RpcEndpoint):
  """Get candlestick data for one trading pair at a given interval. The venue caps a single response at 1500 rows; a window wider than that needs multiple calls. No candle is published for an interval with no ticks, so a window can come back sparser than its nominal length."""

  async def klines_paged(
    self,
    symbol: str,
    *,
    type: Literal[
      '1min',
      '3min',
      '5min',
      '15min',
      '30min',
      '1hour',
      '2hour',
      '4hour',
      '6hour',
      '8hour',
      '12hour',
      '1day',
      '1week',
      '1month',
    ],
    start_at: TimestampSeconds | None = None,
    end_at: TimestampSeconds | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[Response]:
    """Get candlestick data for one trading pair at a given interval. The venue caps a single response at 1500 rows; a window wider than that needs multiple calls. No candle is published for an interval with no ticks, so a window can come back sparser than its nominal length.

    Paged variant of `klines`: Moves the `start_at`–`end_at` window forwards by its own width and stops on the first empty window, or after `max_pages` pages when one is given.

    Every request spans the width the caller's own `start_at` and `end_at` state, so choose a window the venue answers in one response: it caps a wider one, and the walk moves past the rows that were left out.

    Args:
      symbol: Trading pair symbol, e.g. `BTC-USDT`.
      type: Candle interval.
      start_at: Start of the window, Unix seconds. Inclusive — confirmed live (see `notes`).
      end_at: End of the window, Unix seconds. Exclusive — confirmed live (see `notes`).
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/market-data/get-klines)
    """
    if start_at is None or end_at is None:
      raise ValueError(
        '`klines_paged` walks a time window: pass both `start_at` and `end_at`'
      )
    lower = start_at
    upper = end_at
    width = upper - lower
    pages = 0
    while True:
      response = await self.klines(
        symbol, type=type, start_at=lower, end_at=upper, validate=validate
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      if not response:
        break
      lower = upper
      upper = lower + width

  async def klines(
    self,
    symbol: str,
    *,
    type: Literal[
      '1min',
      '3min',
      '5min',
      '15min',
      '30min',
      '1hour',
      '2hour',
      '4hour',
      '6hour',
      '8hour',
      '12hour',
      '1day',
      '1week',
      '1month',
    ],
    start_at: TimestampSeconds | None = None,
    end_at: TimestampSeconds | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get candlestick data for one trading pair at a given interval. The venue caps a single response at 1500 rows; a window wider than that needs multiple calls. No candle is published for an interval with no ticks, so a window can come back sparser than its nominal length.

    Args:
      symbol: Trading pair symbol, e.g. `BTC-USDT`.
      type: Candle interval.
      start_at: Start of the window, Unix seconds. Inclusive — confirmed live (see `notes`).
      end_at: End of the window, Unix seconds. Exclusive — confirmed live (see `notes`).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/market-data/get-klines)
    """
    request: Request = Request(symbol=symbol, type=type)
    if start_at is not None:
      request['startAt'] = start_at
    if end_at is not None:
      request['endAt'] = end_at
    return await self.request(
      request,
      method='GET',
      path='/api/v1/market/candles',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
