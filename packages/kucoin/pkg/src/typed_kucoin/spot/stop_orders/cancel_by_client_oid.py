# Generated by typed-dev — see CONTRIBUTING.md.
"""`DELETE /api/v1/stop-order/cancelOrderByClientOid` — spot.stop_orders.cancel_by_client_oid."""

from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint


class CancelStopOrderByClientOidResult(TypedDict):
  cancelledOrderId: str
  """Venue-assigned id of the cancelled stop order."""
  clientOid: str
  """Caller-assigned order id passed in the request."""


_Type = CancelStopOrderByClientOidResult
adapter = validator[_Type](_Type)  # type: ignore


class CancelByClientOid(RpcEndpoint):
  """`spot.stop_orders.cancel_by_client_oid` — mixed into `StopOrders`, the product exposing `spot.stop_orders.cancel_by_client_oid`."""

  async def cancel_by_client_oid(
    self,
    *,
    symbol: str,
    client_oid: str,
    validate: bool | None = None,
  ) -> CancelStopOrderByClientOidResult:
    """Cancel a single untriggered stop order by the caller-assigned `clientOid` instead of the venue's `orderId`.

    Args:
      symbol: Trading pair the order was placed on.
      client_oid: Caller-assigned order id passed at stop-order creation time.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    params: dict = {
      'symbol': symbol,
      'clientOid': client_oid,
    }
    return await self.authed_request(
      'DELETE',
      '/api/v1/stop-order/cancelOrderByClientOid',
      params=params,
      validator=adapter,
      validate=validate,
    )
