# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class BatchCancelStopOrdersResult(TypedDict):
  cancelledOrderIds: list[str]
  """Ids of the stop orders successfully cancelled by this call."""


class Request(TypedDict):
  symbol: str
  """Trading pair to cancel open stop orders for."""
  tradeType: Literal['TRADE', 'MARGIN_TRADE', 'MARGIN_ISOLATED_TRADE']
  """Trading account the targeted orders were placed on."""
  orderIds: str
  """Comma-separated stop order ids to cancel."""


class BatchCancel(RpcEndpoint):
  """Cancel multiple untriggered stop orders in a single request, scoped to a trading pair and trade type and optionally narrowed to a specific list of order ids."""

  async def batch_cancel(
    self,
    *,
    symbol: str,
    trade_type: Literal['TRADE', 'MARGIN_TRADE', 'MARGIN_ISOLATED_TRADE'],
    order_ids: str,
    validate: bool | None = None,
  ) -> BatchCancelStopOrdersResult:
    """Cancel multiple untriggered stop orders in a single request, scoped to a trading pair and trade type and optionally narrowed to a specific list of order ids.

    Args:
      symbol: Trading pair to cancel open stop orders for.
      trade_type: Trading account the targeted orders were placed on.
      order_ids: Comma-separated stop order ids to cancel.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/orders/batch-cancel-stop-orders)
    """
    return await self.request(
      Request(symbol=symbol, tradeType=trade_type, orderIds=order_ids),
      method='DELETE',
      path='/api/v1/stop-order/cancel',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BatchCancelStopOrdersResult,
    )
