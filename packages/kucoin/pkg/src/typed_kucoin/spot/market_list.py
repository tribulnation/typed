# Generated by typed-dev — see CONTRIBUTING.md.
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


Response = list[str]


class MarketList(RpcEndpoint):
  """List the current market categories trading pairs are grouped under (see each symbol's `market` field). The set changes over time as KuCoin reorganizes categories — not a fixed enum."""

  async def market_list(self, *, validate: bool | None = None) -> Response:
    """List the current market categories trading pairs are grouped under (see each symbol's `market` field). The set changes over time as KuCoin reorganizes categories — not a fixed enum.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/market-data/get-market-list)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v1/markets',
      meta={},
      validate=validate,
      response_type=Response,
    )
