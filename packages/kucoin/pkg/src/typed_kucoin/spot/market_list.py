# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /api/v1/markets` — Get Market List."""

from typed_core.validation import validator
from typed_kucoin.core import RpcEndpoint


_Type = list[str]
adapter = validator[_Type](_Type)  # type: ignore


class MarketList(RpcEndpoint):
  """`Get Market List` — mixed into `Spot`, the product exposing `spot.market_list`."""

  async def market_list(self, *, validate: bool | None = None) -> list[str]:
    """List the current market categories trading pairs are grouped under (see each symbol's `market` field). The set changes over time as KuCoin reorganizes categories — not a fixed enum.

    Args:
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    return await self.request(
      'GET',
      '/api/v1/markets',
      validator=adapter,
      validate=validate,
    )
