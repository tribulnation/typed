# Generated by typed-dev — see CONTRIBUTING.md.
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


Response = str


class ClientIp(RpcEndpoint):
  """Get the caller's IP address as seen by KuCoin's API servers."""

  async def client_ip(self, *, validate: bool | None = None) -> Response:
    """Get the caller's IP address as seen by KuCoin's API servers.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/market-data/get-client-ip-address)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v1/my-ip',
      meta={},
      validate=validate,
      response_type=Response,
    )
