# Generated by typed-dev — see CONTRIBUTING.md.
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import AddOrderResult, HfAddOrderLimit, HfAddOrderMarket


Request = HfAddOrderLimit | HfAddOrderMarket


class Add(RpcEndpoint):
  """Place a spot high-frequency (hf) limit or market order. hf orders match through a dedicated matching engine with lower latency than the plain spot order book."""

  async def add(
    self, request: Request, *, validate: bool | None = None
  ) -> AddOrderResult:
    """Place a spot high-frequency (hf) limit or market order. hf orders match through a dedicated matching engine with lower latency than the plain spot order book.

    Args:
      request:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/orders/add-order)
    """
    return await self.request(
      request,
      method='POST',
      path='/api/v1/hf/orders',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=AddOrderResult,
    )
