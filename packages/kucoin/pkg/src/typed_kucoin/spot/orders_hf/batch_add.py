# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import HfAddOrderLimit, HfAddOrderMarket


class HfBatchAddOrderResult(TypedDict):
  """Result of placing a single order from the batch."""

  orderId: str
  """System-generated order id, when placement succeeded."""
  clientOid: str
  """Client-provided order id, echoed back."""
  success: bool
  """Whether this order was placed successfully."""
  failMsg: NotRequired[str]
  """Error message, present only when `success` is `false`."""


HfBatchAddOrderResults = list[HfBatchAddOrderResult]


class Request(TypedDict):
  """Batch order placement request."""

  orderList: list[HfAddOrderLimit | HfAddOrderMarket]
  """Orders to place, up to 20."""


class BatchAdd(RpcEndpoint):
  """Place up to 20 spot hf orders in a single call. Each order in `orderList` is placed independently -- one failing does not prevent the others from being placed."""

  async def batch_add(
    self,
    order_list: list[HfAddOrderLimit | HfAddOrderMarket],
    *,
    validate: bool | None = None,
  ) -> HfBatchAddOrderResults:
    """Place up to 20 spot hf orders in a single call. Each order in `orderList` is placed independently -- one failing does not prevent the others from being placed.

    Args:
      order_list: Orders to place, up to 20.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/orders/batch-add-orders)
    """
    return await self.request(
      Request(orderList=order_list),
      method='POST',
      path='/api/v1/hf/orders/multi',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=HfBatchAddOrderResults,
    )
