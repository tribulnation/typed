# Generated by typed-dev — see CONTRIBUTING.md.
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import HfAddOrderLimit, HfAddOrderMarket, HfAddOrderSyncResult


Request = HfAddOrderLimit | HfAddOrderMarket


class AddSync(RpcEndpoint):
  """Place a spot hf limit or market order and wait for the matching engine's result before responding, rather than Add's fire-and-forget acknowledgement."""

  async def add_sync(
    self,
    request: Request,
    *,
    validate: bool | None = None,
  ) -> HfAddOrderSyncResult:
    """Place a spot hf limit or market order and wait for the matching engine's result before responding, rather than Add's fire-and-forget acknowledgement.

    Args:
      request:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/orders/add-order-sync)
    """
    return await self.request(
      request,
      method='POST',
      path='/api/v1/hf/orders/sync',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=HfAddOrderSyncResult,
    )
