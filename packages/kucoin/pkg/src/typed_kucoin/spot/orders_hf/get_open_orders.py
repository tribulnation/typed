# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import HfOrder


class Request(TypedDict):
  symbol: NotRequired[str]
  """Trading pair symbol to filter by. Every open hf order across all symbols is returned when omitted."""


Response = list[HfOrder] | None


class GetOpenOrders(RpcEndpoint):
  """List every currently open (uncompleted) spot hf order for the authenticated account, sorted descending by last update time. Unpaginated -- see Get Open Orders By Page for a paginated variant."""

  async def get_open_orders(
    self,
    symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """List every currently open (uncompleted) spot hf order for the authenticated account, sorted descending by last update time. Unpaginated -- see Get Open Orders By Page for a paginated variant.

    Args:
      symbol: Trading pair symbol to filter by. Every open hf order across all symbols is returned when omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/orders/get-open-orders)
    """
    request: Request = Request()
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='/api/v1/hf/orders/active',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
