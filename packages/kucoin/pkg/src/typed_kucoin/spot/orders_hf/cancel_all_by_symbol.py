# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict, cast
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  symbol: str
  """Trading pair symbol to cancel every open order for."""


Response = Literal['success']


class CancelAllBySymbol(RpcEndpoint):
  """Cancel every open spot hf order for one trading pair. Sends cancellation requests only -- confirm via Get Open Orders or the private order WebSocket feed."""

  async def cancel_all_by_symbol(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Cancel every open spot hf order for one trading pair. Sends cancellation requests only -- confirm via Get Open Orders or the private order WebSocket feed.

    Args:
      symbol: Trading pair symbol to cancel every open order for.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/orders/cancel-all-orders-by-symbol)
    """
    return await self.request(
      Request(symbol=symbol),
      method='DELETE',
      path='/api/v1/hf/orders',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=cast(type, Response),
    )
