# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /api/v1/hf/orders/active/page` — Get Open Orders By Page."""

from typing_extensions import AsyncIterator
from typed_core.validation import TypedDict, validator
from typed_kucoin.types import HfOrder
from typed_kucoin.core import RpcEndpoint


class HfOpenOrdersPage(TypedDict):
  """Paginated page of open hf orders."""

  currentPage: int
  """Page number returned."""
  pageSize: int
  """Results per page."""
  totalNum: int
  """Total number of open orders matching the filter."""
  totalPage: int
  """Total number of pages."""
  items: list[HfOrder]
  """Open hf orders on this page."""


_Type = HfOpenOrdersPage
adapter = validator[_Type](_Type)  # type: ignore


class GetOpenOrdersByPage(RpcEndpoint):
  """`Get Open Orders By Page` — mixed into `OrdersHf`, the product exposing `spot.orders_hf.get_open_orders_by_page`."""

  async def get_open_orders_by_page(
    self,
    *,
    symbol: str,
    page_num: int | None = None,
    page_size: int | None = None,
    validate: bool | None = None,
  ) -> HfOpenOrdersPage:
    """Paginated variant of Get Open Orders: list currently open (uncompleted) spot hf orders for the authenticated account, one page at a time.

    Args:
      symbol: Trading pair symbol to filter by.
      page_num: Page number to fetch, starting at 1.
      page_size: Results per page.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    params: dict = {
      'symbol': symbol,
    }
    if page_num is not None:
      params['pageNum'] = page_num
    if page_size is not None:
      params['pageSize'] = page_size
    return await self.authed_request(
      'GET',
      '/api/v1/hf/orders/active/page',
      params=params,
      validator=adapter,
      validate=validate,
    )

  async def get_open_orders_by_page_paged(
    self,
    *,
    symbol: str,
    page_size: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[HfOpenOrdersPage]:
    """Yield successive pages of `get_open_orders_by_page`.

    Requests `pageNum` from 1 upwards and stops once it has covered the `totalPage`
    pages the response reports, or after `max_pages` pages when one is given.
    """
    page_num = 1
    pages = 0
    while True:
      response = await self.get_open_orders_by_page(
        symbol=symbol, page_size=page_size, page_num=page_num, validate=validate
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      total = response.get('totalPage') if response is not None else None
      total = int(total) if total is not None else None
      if total is None or pages >= total:
        break
      page_num += 1
