# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import HfCancelOrderSyncResult


class Request(TypedDict):
  orderId: str
  """System-generated id of the order to cancel."""
  symbol: str
  """Trading pair symbol the order belongs to."""


class CancelByOrderIdSync(RpcEndpoint):
  """Cancel a single spot hf order by its system-generated order id and wait for the cancellation to complete before responding, rather than Cancel by OrderId's fire-and-forget acknowledgement."""

  async def cancel_by_order_id_sync(
    self,
    *,
    order_id: str,
    symbol: str,
    validate: bool | None = None,
  ) -> HfCancelOrderSyncResult:
    """Cancel a single spot hf order by its system-generated order id and wait for the cancellation to complete before responding, rather than Cancel by OrderId's fire-and-forget acknowledgement.

    Args:
      order_id: System-generated id of the order to cancel.
      symbol: Trading pair symbol the order belongs to.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/orders/cancel-order-by-orderld-sync)
    """
    return await self.request(
      Request(orderId=order_id, symbol=symbol),
      method='DELETE',
      path='/api/v1/hf/orders/sync/{orderId}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=HfCancelOrderSyncResult,
    )
