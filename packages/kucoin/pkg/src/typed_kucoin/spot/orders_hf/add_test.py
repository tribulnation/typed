# Generated by typed-dev — see CONTRIBUTING.md.
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import AddOrderResult, HfAddOrderLimit, HfAddOrderMarket


Request = HfAddOrderLimit | HfAddOrderMarket


class AddTest(RpcEndpoint):
  """Validate a spot hf order placement request against the same checks Add applies -- symbol, permissions, size/price limits -- without ever entering the matching engine. Used to test order-placement integration safely."""

  async def add_test(
    self,
    request: Request,
    *,
    validate: bool | None = None,
  ) -> AddOrderResult:
    """Validate a spot hf order placement request against the same checks Add applies -- symbol, permissions, size/price limits -- without ever entering the matching engine. Used to test order-placement integration safely.

    Args:
      request:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/orders/add-order-test)
    """
    return await self.request(
      request,
      method='POST',
      path='/api/v1/hf/orders/test',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=AddOrderResult,
    )
