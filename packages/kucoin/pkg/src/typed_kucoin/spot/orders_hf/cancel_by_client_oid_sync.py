# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import HfCancelOrderByClientOidSyncResult


class Request(TypedDict):
  clientOid: str
  """Client-provided id of the order to cancel."""
  symbol: str
  """Trading pair symbol the order belongs to."""


class CancelByClientOidSync(RpcEndpoint):
  """Cancel a single spot hf order by its client-provided order id and wait for the cancellation to complete before responding, rather than Cancel by ClientOid's fire-and-forget acknowledgement."""

  async def cancel_by_client_oid_sync(
    self,
    *,
    client_oid: str,
    symbol: str,
    validate: bool | None = None,
  ) -> HfCancelOrderByClientOidSyncResult:
    """Cancel a single spot hf order by its client-provided order id and wait for the cancellation to complete before responding, rather than Cancel by ClientOid's fire-and-forget acknowledgement.

    Args:
      client_oid: Client-provided id of the order to cancel.
      symbol: Trading pair symbol the order belongs to.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/orders/cancel-order-by-clientoid-sync)
    """
    return await self.request(
      Request(clientOid=client_oid, symbol=symbol),
      method='DELETE',
      path='/api/v1/hf/orders/sync/client-order/{clientOid}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=HfCancelOrderByClientOidSyncResult,
    )
