# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class HfOpenOrderSymbols(TypedDict):
  """Trading pairs currently holding at least one open hf order."""

  symbols: list[str]
  """Trading pair symbols with an open hf order."""


class GetSymbolsWithOpenOrder(RpcEndpoint):
  """List every trading pair symbol that currently has at least one open spot hf order for the authenticated account."""

  async def get_symbols_with_open_order(
    self,
    *,
    validate: bool | None = None,
  ) -> HfOpenOrderSymbols:
    """List every trading pair symbol that currently has at least one open spot hf order for the authenticated account.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/orders/get-symbols-with-open-order)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v1/hf/orders/active/symbols',
      meta={'signed': True},
      validate=validate,
      response_type=HfOpenOrderSymbols,
    )
