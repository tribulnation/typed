# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /api/v1/hf/orders/active/symbols` — Get Symbols With Open Order."""

from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint


class HfOpenOrderSymbols(TypedDict):
  """Trading pairs currently holding at least one open hf order."""

  symbols: list[str]
  """Trading pair symbols with an open hf order."""


_Type = HfOpenOrderSymbols
adapter = validator[_Type](_Type)  # type: ignore


class GetSymbolsWithOpenOrder(RpcEndpoint):
  """`Get Symbols With Open Order` — mixed into `OrdersHf`, the product exposing `spot.orders_hf.get_symbols_with_open_order`."""

  async def get_symbols_with_open_order(
    self,
    *,
    validate: bool | None = None,
  ) -> HfOpenOrderSymbols:
    """List every trading pair symbol that currently has at least one open spot hf order for the authenticated account.

    Args:
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    return await self.authed_request(
      'GET',
      '/api/v1/hf/orders/active/symbols',
      validator=adapter,
      validate=validate,
    )
