# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class HfCancelPartialOrderResult(TypedDict):
  """Quantity accepted for cancellation from the order."""

  orderId: str
  """System-generated id of the order."""
  cancelSize: Decimal
  """Quantity cancelled."""


class Request(TypedDict):
  orderId: str
  """System-generated id of the order to partially cancel."""
  symbol: str
  """Trading pair symbol the order belongs to."""
  cancelSize: Decimal
  """Quantity to cancel from the order's remaining unfilled size."""


class CancelPartial(RpcEndpoint):
  """Cancel part of a single spot hf order's remaining unfilled quantity, leaving the rest resting."""

  async def cancel_partial(
    self,
    *,
    order_id: str,
    symbol: str,
    cancel_size: Decimal,
    validate: bool | None = None,
  ) -> HfCancelPartialOrderResult:
    """Cancel part of a single spot hf order's remaining unfilled quantity, leaving the rest resting.

    Args:
      order_id: System-generated id of the order to partially cancel.
      symbol: Trading pair symbol the order belongs to.
      cancel_size: Quantity to cancel from the order's remaining unfilled size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/orders/cancel-partial-order)
    """
    return await self.request(
      Request(orderId=order_id, symbol=symbol, cancelSize=cancel_size),
      method='DELETE',
      path='/api/v1/hf/orders/cancel/{orderId}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=HfCancelPartialOrderResult,
    )
