# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import HfOrder


class Request(TypedDict):
  clientOid: str
  """Client-provided id of the order to look up."""
  symbol: str
  """Trading pair symbol the order belongs to."""


class GetByClientOid(RpcEndpoint):
  """Get a single spot hf order's full detail by its client-provided order id. Cancelled orders are queryable for 2 days after cancellation, filled orders for 7 days after filling."""

  async def get_by_client_oid(
    self,
    *,
    client_oid: str,
    symbol: str,
    validate: bool | None = None,
  ) -> HfOrder:
    """Get a single spot hf order's full detail by its client-provided order id. Cancelled orders are queryable for 2 days after cancellation, filled orders for 7 days after filling.

    Args:
      client_oid: Client-provided id of the order to look up.
      symbol: Trading pair symbol the order belongs to.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/orders/get-order-by-clientoid)
    """
    return await self.request(
      Request(clientOid=client_oid, symbol=symbol),
      method='GET',
      path='/api/v1/hf/orders/client-order/{clientOid}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=HfOrder,
    )
