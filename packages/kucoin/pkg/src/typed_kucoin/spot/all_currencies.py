# Generated by typed-dev — see CONTRIBUTING.md.
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import Currency


CurrencyResponse = list[Currency]


class AllCurrencies(RpcEndpoint):
  """List every currency KuCoin knows about. Not all of them are currently tradable — see each symbol's `enableTrading` for that. Currency codes generally follow ISO 4217 where one exists."""

  async def all_currencies(self, *, validate: bool | None = None) -> CurrencyResponse:
    """List every currency KuCoin knows about. Not all of them are currently tradable — see each symbol's `enableTrading` for that. Currency codes generally follow ISO 4217 where one exists.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/market-data/get-all-currencies)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v3/currencies',
      meta={},
      validate=validate,
      response_type=CurrencyResponse,
    )
