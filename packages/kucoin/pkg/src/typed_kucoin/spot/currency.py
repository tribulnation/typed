# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import Currency


class Request(TypedDict):
  currency: str
  """Currency code, e.g. `BTC`."""
  chain: NotRequired[str]
  """Restrict the response's `chains` to one specific blockchain network."""


class CurrencyEndpoint(RpcEndpoint):
  """Get detailed information for one currency, including its per-chain deposit/withdrawal parameters and margin/debit trading eligibility."""

  async def currency(
    self,
    *,
    currency: str,
    chain: str | None = None,
    validate: bool | None = None,
  ) -> Currency:
    """Get detailed information for one currency, including its per-chain deposit/withdrawal parameters and margin/debit trading eligibility.

    Args:
      currency: Currency code, e.g. `BTC`.
      chain: Restrict the response's `chains` to one specific blockchain network.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/market-data/get-currency)
    """
    request: Request = Request(currency=currency)
    if chain is not None:
      request['chain'] = chain
    return await self.request(
      request,
      method='GET',
      path='/api/v3/currencies/{currency}',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Currency,
    )
