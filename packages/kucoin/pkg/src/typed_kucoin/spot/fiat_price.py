# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  base: NotRequired[str]
  """Fiat currency to price against, e.g. `USD`, `EUR`. Defaults to `USD`."""
  currencies: NotRequired[str]
  """Comma-separated crypto currencies to price, e.g. `BTC,ETH`. Defaults to all currencies."""


Response = dict[str, str]


class FiatPrice(RpcEndpoint):
  """Get the fiat price of tradable currencies. Defaults to USD if `base` is omitted, and to every currency if `currencies` is omitted."""

  async def fiat_price(
    self,
    *,
    base: str | None = None,
    currencies: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get the fiat price of tradable currencies. Defaults to USD if `base` is omitted, and to every currency if `currencies` is omitted.

    Args:
      base: Fiat currency to price against, e.g. `USD`, `EUR`. Defaults to `USD`.
      currencies: Comma-separated crypto currencies to price, e.g. `BTC,ETH`. Defaults to all currencies.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/market-data/get-fiat-price)
    """
    request: Request = Request()
    if base is not None:
      request['base'] = base
    if currencies is not None:
      request['currencies'] = currencies
    return await self.request(
      request,
      method='GET',
      path='/api/v1/prices',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
