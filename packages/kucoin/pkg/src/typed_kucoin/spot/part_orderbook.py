# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import OrderBookSnapshot


class Request(TypedDict):
  size: Literal['20', '100']
  """Number of price levels per side to return."""
  symbol: str
  """Trading pair symbol, e.g. `BTC-USDT`."""


class PartOrderbook(RpcEndpoint):
  """Get order book depth aggregated by price, limited to the requested number of price levels per side. Recommended over Full OrderBook when the caller doesn't need every level — faster and lighter."""

  async def part_orderbook(
    self,
    size: Literal['20', '100'],
    *,
    symbol: str,
    validate: bool | None = None,
  ) -> OrderBookSnapshot:
    """Get order book depth aggregated by price, limited to the requested number of price levels per side. Recommended over Full OrderBook when the caller doesn't need every level — faster and lighter.

    Args:
      size: Number of price levels per side to return.
      symbol: Trading pair symbol, e.g. `BTC-USDT`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/market-data/get-part-orderbook)
    """
    return await self.request(
      Request(size=size, symbol=symbol),
      method='GET',
      path='/api/v1/market/orderbook/level2_{size}',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=OrderBookSnapshot,
    )
