# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_kucoin.core import TimestampMillis
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class SpotTickerSummary(TypedDict):
  """24h ticker summary for one trading pair."""

  symbol: str
  """Trading pair identifier."""
  symbolName: str
  """Alternative display name for the pair (differs from `symbol` if the venue renames a currency)."""
  buy: Decimal | None
  """Current best bid price; null when no bid is available."""
  bestBidSize: Decimal | None
  """Size at the best bid; null when no bid is available."""
  sell: Decimal | None
  """Current best ask price; null when no ask is available."""
  bestAskSize: Decimal | None
  """Size at the best ask; null when no ask is available."""
  changeRate: Decimal
  """24h price change rate (fraction)."""
  changePrice: Decimal | None
  """24h absolute price change. `null` for a pair with no trade history yet (see `open`/`high`/`low`/`vol` all reading `"0"` on the same entry)."""
  open: Decimal
  """Price 24h ago. Undocumented, observed live."""
  high: Decimal
  """24h high."""
  low: Decimal
  """24h low."""
  vol: Decimal
  """24h volume, base currency."""
  volValue: Decimal
  """24h volume, quote currency."""
  last: Decimal | None
  """Last traded price; null when no last trade is available."""
  lastSize: Decimal | None
  """Last traded size; null when no last trade is available. Undocumented, observed live."""
  averagePrice: Decimal | None
  """24h average trade price. `null` for a pair with no trade history yet."""
  takerFeeRate: Decimal
  """Current base taker fee rate."""
  makerFeeRate: Decimal
  """Current base maker fee rate."""
  takerCoefficient: Decimal
  """Taker fee multiplier for this pair's fee category."""
  makerCoefficient: Decimal
  """Maker fee multiplier for this pair's fee category."""
  priceChange: Decimal | None
  """Same value as `changePrice`, including its `null` case. Undocumented, observed live."""
  priceChangePercent: Decimal
  """Same value as `changeRate`. Undocumented, observed live."""


class AllSpotTickers(TypedDict):
  """Snapshot of every trading pair's ticker."""

  time: TimestampMillis
  """Snapshot timestamp, Unix ms."""
  ticker: list[SpotTickerSummary]
  """One entry per trading pair."""


class AllTickers(RpcEndpoint):
  """Snapshot of market tickers for every trading pair (including 24h volume), refreshed roughly every 2 seconds server-side."""

  async def all_tickers(self, *, validate: bool | None = None) -> AllSpotTickers:
    """Snapshot of market tickers for every trading pair (including 24h volume), refreshed roughly every 2 seconds server-side.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/market-data/get-all-tickers)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v1/market/allTickers',
      meta={},
      validate=validate,
      response_type=AllSpotTickers,
    )
