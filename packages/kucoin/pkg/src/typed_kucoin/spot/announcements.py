# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, NotRequired, TypedDict
from typed_kucoin.core import TimestampMillis
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class Announcement(TypedDict):
  """One news announcement."""

  annId: int
  """Unique announcement id."""
  annTitle: str
  """Announcement title."""
  annType: list[str]
  """Categories this announcement was tagged with. Not a documented closed set — see the endpoint `notes`."""
  annDesc: str
  """Short announcement description."""
  language: str
  """Language of this announcement, e.g. `en_US`."""
  annUrl: str
  """URL to the full announcement on kucoin.com."""
  cTime: TimestampMillis
  """Creation time, Unix milliseconds."""


class Request(TypedDict):
  currentPage: NotRequired[int]
  """Page number."""
  pageSize: NotRequired[int]
  """Results per page."""
  annType: NotRequired[str]
  """Announcement category filter, e.g. `latest-announcements`, `activities`, `new-listings`, `futures-announcements` (docs give these as examples, not an exhaustive set)."""
  lang: NotRequired[str]
  """Language code, e.g. `en_US`."""
  startTime: NotRequired[TimestampMillis]
  """Start of the search window, Unix milliseconds."""
  endTime: NotRequired[TimestampMillis]
  """End of the search window, Unix milliseconds."""


class AnnouncementsPage(TypedDict):
  """One page of the announcements list."""

  totalNum: int
  """Total number of announcements matching the search."""
  totalPage: int
  """Total number of pages."""
  currentPage: int
  """Current page number."""
  pageSize: int
  """Results per page."""
  items: list[Announcement]
  """Announcements on this page."""


class Announcements(RpcEndpoint):
  """Page through KuCoin's news announcements (new listings, delistings, maintenance, activities, ...), newest first. Default search window covers the last month unless `startTime`/`endTime` narrow it."""

  async def announcements_paged(
    self,
    *,
    page_size: int | None = None,
    ann_type: str | None = None,
    lang: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[AnnouncementsPage]:
    """Page through KuCoin's news announcements (new listings, delistings, maintenance, activities, ...), newest first. Default search window covers the last month unless `startTime`/`endTime` narrow it.

    Paged variant of `announcements`: Requests `currentPage` from 1 upwards and stops once it has covered the `totalPage` pages the response reports, or after `max_pages` pages when one is given.

    Args:
      page_size: Results per page.
      ann_type: Announcement category filter, e.g. `latest-announcements`, `activities`, `new-listings`, `futures-announcements` (docs give these as examples, not an exhaustive set).
      lang: Language code, e.g. `en_US`.
      start_time: Start of the search window, Unix milliseconds.
      end_time: End of the search window, Unix milliseconds.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/market-data/get-announcements)
    """
    current_page = 1
    pages = 0
    while True:
      response = await self.announcements(
        page_size=page_size,
        ann_type=ann_type,
        lang=lang,
        start_time=start_time,
        end_time=end_time,
        current_page=current_page,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      total = response.get('totalPage') if response is not None else None
      total = int(total) if total is not None else None
      if total is None or pages >= total:
        break
      current_page += 1

  async def announcements(
    self,
    *,
    current_page: int | None = None,
    page_size: int | None = None,
    ann_type: str | None = None,
    lang: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    validate: bool | None = None,
  ) -> AnnouncementsPage:
    """Page through KuCoin's news announcements (new listings, delistings, maintenance, activities, ...), newest first. Default search window covers the last month unless `startTime`/`endTime` narrow it.

    Args:
      current_page: Page number.
      page_size: Results per page.
      ann_type: Announcement category filter, e.g. `latest-announcements`, `activities`, `new-listings`, `futures-announcements` (docs give these as examples, not an exhaustive set).
      lang: Language code, e.g. `en_US`.
      start_time: Start of the search window, Unix milliseconds.
      end_time: End of the search window, Unix milliseconds.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/market-data/get-announcements)
    """
    request: Request = Request()
    if current_page is not None:
      request['currentPage'] = current_page
    if page_size is not None:
      request['pageSize'] = page_size
    if ann_type is not None:
      request['annType'] = ann_type
    if lang is not None:
      request['lang'] = lang
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    return await self.request(
      request,
      method='GET',
      path='/api/v3/announcements',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=AnnouncementsPage,
    )
