# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import SpotSymbol


class Request(TypedDict):
  symbol: str
  """Trading pair symbol, e.g. `BTC-USDT`."""


class Symbol(RpcEndpoint):
  """Get order-placement configuration for one trading pair: size/price increments and limits, margin eligibility, fee coefficients, and call-auction status."""

  async def symbol(self, symbol: str, *, validate: bool | None = None) -> SpotSymbol:
    """Get order-placement configuration for one trading pair: size/price increments and limits, margin eligibility, fee coefficients, and call-auction status.

    Args:
      symbol: Trading pair symbol, e.g. `BTC-USDT`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/market-data/get-symbol)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/api/v2/symbols/{symbol}',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=SpotSymbol,
    )
