# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import SpotSymbol


class Request(TypedDict):
  market: NotRequired[str]
  """Restrict to one market category (see `spot.market_list` for the live set — not a fixed enum, categories are reorganized over time)."""


SpotSymbolResponse = list[SpotSymbol]


class AllSymbols(RpcEndpoint):
  """List order-placement configuration for every trading pair, optionally restricted to one market category."""

  async def all_symbols(
    self,
    market: str | None = None,
    *,
    validate: bool | None = None,
  ) -> SpotSymbolResponse:
    """List order-placement configuration for every trading pair, optionally restricted to one market category.

    Args:
      market: Restrict to one market category (see `spot.market_list` for the live set — not a fixed enum, categories are reorganized over time).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/market-data/get-all-symbols)
    """
    request: Request = Request()
    if market is not None:
      request['market'] = market
    return await self.request(
      request,
      method='GET',
      path='/api/v2/symbols',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=SpotSymbolResponse,
    )
