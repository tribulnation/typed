# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import SpotTicker


class Request(TypedDict):
  symbol: str
  """Trading pair symbol, e.g. `BTC-USDT`."""


class Ticker(RpcEndpoint):
  """Get Level 1 market data for one trading pair: best bid/ask price and size, and the last trade's price and size."""

  async def ticker(self, symbol: str, *, validate: bool | None = None) -> SpotTicker:
    """Get Level 1 market data for one trading pair: best bid/ask price and size, and the last trade's price and size.

    Args:
      symbol: Trading pair symbol, e.g. `BTC-USDT`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/spot-trading/market-data/get-ticker)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/api/v1/market/orderbook/level1',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=SpotTicker,
    )
