# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class CancelOcoOrderResult(TypedDict):
  cancelledOrderIds: list[str]
  """The two leg order ids belonging to the cancelled OCO order."""


class Request(TypedDict):
  orderId: str
  """The OCO order group's id, as returned by Add OCO Order."""


class CancelByOrderId(RpcEndpoint):
  """Request cancellation of an OCO order by its order id. This only submits the cancellation request; the matching engine processes it asynchronously, so confirm the outcome via a lookup or the private WebSocket order push rather than assuming immediate effect."""

  async def cancel_by_order_id(
    self,
    order_id: str,
    *,
    validate: bool | None = None,
  ) -> CancelOcoOrderResult:
    """Request cancellation of an OCO order by its order id. This only submits the cancellation request; the matching engine processes it asynchronously, so confirm the outcome via a lookup or the private WebSocket order push rather than assuming immediate effect.

    Args:
      order_id: The OCO order group's id, as returned by Add OCO Order.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470354)
    """
    return await self.request(
      Request(orderId=order_id),
      method='DELETE',
      path='/api/v3/oco/order/{orderId}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CancelOcoOrderResult,
    )
