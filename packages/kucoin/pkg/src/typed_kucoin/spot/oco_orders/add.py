# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class AddOcoOrderResult(TypedDict):
  orderId: str
  """System-generated id for the OCO order group, used for later lookup/cancellation."""


class Request(TypedDict):
  clientOid: str
  """Client-generated unique order id (a UUID is recommended): letters, digits, underscores and hyphens only, max 40 characters. Echoed back on lookups; do not reuse a `clientOid` across requests."""
  symbol: str
  """Trading pair, e.g. `BTC-USDT`."""
  side: Literal['buy', 'sell']
  """Order direction, shared by both legs."""
  price: Decimal
  """Price for the upper leg (a plain limit order at this price)."""
  size: Decimal
  """Order quantity, shared by both legs."""
  stopPrice: Decimal
  """Trigger price for the lower leg."""
  limitPrice: Decimal
  """Limit price the lower leg executes at once `stopPrice` triggers."""
  remark: NotRequired[str]
  """Order placement remark, max 20 ASCII characters."""
  tradeType: NotRequired[Literal['TRADE']]
  """Transaction type. Only spot trading is currently supported."""


class Add(RpcEndpoint):
  """Place an OCO (One-Cancels-the-Other) order to the Spot trading engine: two linked leg orders -- an upper limit leg priced at `price`, and a lower leg that triggers at `stopPrice` and executes as a limit order at `limitPrice` -- where a fill or cancellation of either leg cancels the other. Maximum 20 untriggered OCO/stop orders per symbol per account."""

  async def add(
    self,
    *,
    client_oid: str,
    symbol: str,
    side: Literal['buy', 'sell'],
    price: Decimal,
    size: Decimal,
    stop_price: Decimal,
    limit_price: Decimal,
    remark: str | None = None,
    trade_type: Literal['TRADE'] | None = None,
    validate: bool | None = None,
  ) -> AddOcoOrderResult:
    """Place an OCO (One-Cancels-the-Other) order to the Spot trading engine: two linked leg orders -- an upper limit leg priced at `price`, and a lower leg that triggers at `stopPrice` and executes as a limit order at `limitPrice` -- where a fill or cancellation of either leg cancels the other. Maximum 20 untriggered OCO/stop orders per symbol per account.

    Args:
      client_oid: Client-generated unique order id (a UUID is recommended): letters, digits, underscores and hyphens only, max 40 characters. Echoed back on lookups; do not reuse a `clientOid` across requests.
      symbol: Trading pair, e.g. `BTC-USDT`.
      side: Order direction, shared by both legs.
      price: Price for the upper leg (a plain limit order at this price).
      size: Order quantity, shared by both legs.
      stop_price: Trigger price for the lower leg.
      limit_price: Limit price the lower leg executes at once `stopPrice` triggers.
      remark: Order placement remark, max 20 ASCII characters.
      trade_type: Transaction type. Only spot trading is currently supported.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470353)
    """
    request: Request = Request(
      clientOid=client_oid,
      symbol=symbol,
      side=side,
      price=price,
      size=size,
      stopPrice=stop_price,
      limitPrice=limit_price,
    )
    if remark is not None:
      request['remark'] = remark
    if trade_type is not None:
      request['tradeType'] = trade_type
    return await self.request(
      request,
      method='POST',
      path='/api/v3/oco/order',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=AddOcoOrderResult,
    )
