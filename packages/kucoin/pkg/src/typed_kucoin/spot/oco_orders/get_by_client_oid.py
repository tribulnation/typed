# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /api/v3/oco/client-order/{clientOid}` — spot.oco_orders.get_by_client_oid."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint, TimestampMillis


class OcoOrderSummary(TypedDict):
  """Lightweight OCO order summary, with no leg detail."""

  orderId: str
  """OCO order group id."""
  symbol: str
  """Trading pair."""
  clientOid: str
  """Client-provided order id, as supplied at placement."""
  orderTime: TimestampMillis
  """Time the OCO order was placed."""
  status: Literal['NEW', 'DONE', 'TRIGGERED', 'CANCELLED']
  """OCO order group status."""


_Type = OcoOrderSummary | None
adapter = validator[_Type](_Type)  # type: ignore


class GetByClientOid(RpcEndpoint):
  """`spot.oco_orders.get_by_client_oid` — mixed into `OcoOrders`, the product exposing `spot.oco_orders.get_by_client_oid`."""

  async def get_by_client_oid(
    self,
    client_oid: str,
    *,
    validate: bool | None = None,
  ) -> OcoOrderSummary | None:
    """Get a lightweight OCO order summary (no leg detail) by its clientOid. Same lightweight shape and the same live-tracking-window behavior as Get OCO Order By OrderId -- see `notes`.

    Args:
      client_oid: The client-provided order id supplied at placement.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    return await self.authed_request(
      'GET',
      f'/api/v3/oco/client-order/{client_oid}',
      validator=adapter,
      validate=validate,
    )
