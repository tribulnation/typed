# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import BrokerSubAccount


class Request(TypedDict):
  accountName: str
  """Sub-account name. Unique across the exchange -- add a distinguishing identifier to avoid collisions."""


class SubAccountAdd(RpcEndpoint):
  """Create a new sub-account under the authenticated Exchange (ND) Broker."""

  async def sub_account_add(
    self,
    account_name: str,
    *,
    validate: bool | None = None,
  ) -> BrokerSubAccount:
    """Create a new sub-account under the authenticated Exchange (ND) Broker.

    Args:
      account_name: Sub-account name. Unique across the exchange -- add a distinguishing identifier to avoid collisions.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470290)
    """
    return await self.request(
      Request(accountName=account_name),
      method='POST',
      path='/api/v1/broker/nd/account',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerSubAccount,
    )
