# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_kucoin.core import TimestampMillis
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class BrokerSubAccountApiKeyCreated(TypedDict):
  """A newly created broker sub-account API key."""

  uid: str
  """Sub-account UID."""
  label: str
  """API key remarks."""
  apiKey: str
  """The new API key."""
  secretKey: str
  """The new API secret. Shown only on creation; not retrievable afterward."""
  apiVersion: int
  """API key version."""
  permissions: list[str]
  """Permission group list assigned to the key."""
  ipWhitelist: list[str]
  """IP whitelist assigned to the key."""
  createdAt: TimestampMillis
  """Creation time, Unix milliseconds."""


class Request(TypedDict):
  uid: str
  """Sub-account UID to create the API key for."""
  passphrase: str
  """API passphrase for the new key."""
  ipWhitelist: list[str]
  """IP whitelist, up to 20 entries."""
  permissions: list[Literal['general', 'spot', 'futures']]
  """Permission group list -- only `general`, `spot` and `futures` may be set here."""
  label: str
  """API key remarks, 4-32 characters."""


class SubAccountApiAdd(RpcEndpoint):
  """Create a new API key for an Exchange (ND) Broker sub-account."""

  async def sub_account_api_add(
    self,
    *,
    uid: str,
    passphrase: str,
    ip_whitelist: list[str],
    permissions: list[Literal['general', 'spot', 'futures']],
    label: str,
    validate: bool | None = None,
  ) -> BrokerSubAccountApiKeyCreated:
    """Create a new API key for an Exchange (ND) Broker sub-account.

    Args:
      uid: Sub-account UID to create the API key for.
      passphrase: API passphrase for the new key.
      ip_whitelist: IP whitelist, up to 20 entries.
      permissions: Permission group list -- only `general`, `spot` and `futures` may be set here.
      label: API key remarks, 4-32 characters.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470291)
    """
    return await self.request(
      Request(
        uid=uid,
        passphrase=passphrase,
        ipWhitelist=ip_whitelist,
        permissions=permissions,
        label=label,
      ),
      method='POST',
      path='/api/v1/broker/nd/account/apikey',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerSubAccountApiKeyCreated,
    )
