# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_kucoin.core import TimestampMillis
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class BrokerUser(TypedDict):
  """One broker-onboarded user's lifetime trading summary. KuCoin's docs collapse this object without per-field descriptions (only a bare `[object {19}]`, undercounting the live example's own 21 keys -- see notes); descriptions below are inferred from the analogous, individually-documented fields on `broker.user_transactions`."""

  uid: str
  """User UID."""
  nickName: str
  """User nickname, defaults to phone number or email if none set."""
  registrationTime: TimestampMillis
  """User registration time, Unix milliseconds."""
  rateUp: bool
  """Whether this user's rate has been upgraded. KuCoin's docs give no further description."""
  totalCommission: Decimal
  """Total commission this user has contributed."""
  totalTradingVolume: Decimal
  """This user's total trading volume."""
  totalFee: Decimal
  """This user's total trading fee."""
  invitedByMe: bool
  """Whether this user was directly invited by this broker."""
  rcode: str | None
  """Referral code this user registered with; `null` when none (observed in the docs' own example row)."""
  tags: str
  """Broker tag this user traded under."""
  spotTradingVolumeWithTag: Decimal
  """Tagged spot trading volume."""
  futureTradingVolumeWithTag: Decimal
  """Tagged futures trading volume."""
  tradingFeeWithTag: Decimal
  """Tagged trading fee."""
  commissionWithTag: Decimal
  """Commission contributed by tagged trades."""
  spotTradingVolumeWithoutTag: Decimal
  """Untagged spot trading volume."""
  futureTradingVolumeWithoutTag: Decimal
  """Untagged futures trading volume."""
  tradingFeeWithoutTag: Decimal
  """Untagged trading fee."""
  commissionWithoutTag: Decimal
  """Commission contributed by untagged trades."""
  currency: str
  """Denomination unit for the volume/amount fields, e.g. `USDT`."""
  futuresTradingVolumeWithTag: Decimal
  """Tagged futures trading volume. Carries the same meaning as `futureTradingVolumeWithTag` (singular) above -- see notes."""
  futuresTradingVolumeWithoutTag: Decimal
  """Untagged futures trading volume. Carries the same meaning as `futureTradingVolumeWithoutTag` (singular) above -- see notes."""


class Request(TypedDict):
  tradeType: NotRequired[Literal['all', 'SPOT', 'FUTURE']]
  """Trade type to restrict the query to."""
  uid: NotRequired[str]
  """Restrict to one user UID."""
  rcode: NotRequired[str]
  """Restrict to users who registered through this referral code."""
  tag: NotRequired[str]
  """Restrict to users who traded under this broker tag."""
  startAt: NotRequired[TimestampMillis]
  """Restrict to users who registered at or after this time, Unix milliseconds."""
  endAt: NotRequired[TimestampMillis]
  """Restrict to users who registered at or before this time, Unix milliseconds."""
  page: NotRequired[int]
  """Page number."""
  pageSize: NotRequired[int]
  """Page size."""


class BrokerUsersResult(TypedDict):
  """One page of a broker's onboarded users."""

  currentPage: int
  """Current page number."""
  pageSize: int
  """Number of items per page."""
  totalNum: int
  """Total number of items."""
  totalPage: int
  """Total number of pages."""
  items: list[BrokerUser]
  """Users on this page."""


class Users(RpcEndpoint):
  """Page through the authenticated API Broker's onboarded users, with their lifetime trading volume, fee and commission -- broken out by whether the trades carried a broker tag -- optionally filtered by referral code, tag or registration date."""

  async def users_paged(
    self,
    trade_type: Literal['all', 'SPOT', 'FUTURE'] | None = None,
    *,
    uid: str | None = None,
    rcode: str | None = None,
    tag: str | None = None,
    start_at: TimestampMillis | None = None,
    end_at: TimestampMillis | None = None,
    page_size: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[BrokerUsersResult]:
    """Page through the authenticated API Broker's onboarded users, with their lifetime trading volume, fee and commission -- broken out by whether the trades carried a broker tag -- optionally filtered by referral code, tag or registration date.

    Paged variant of `users`: Requests `page` from 1 upwards and stops once it has covered the `totalPage` pages the response reports, or after `max_pages` pages when one is given.

    Args:
      trade_type: Trade type to restrict the query to.
      uid: Restrict to one user UID.
      rcode: Restrict to users who registered through this referral code.
      tag: Restrict to users who traded under this broker tag.
      start_at: Restrict to users who registered at or after this time, Unix milliseconds.
      end_at: Restrict to users who registered at or before this time, Unix milliseconds.
      page_size: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/broker/api-broker/get-user-list)
    """
    page = 1
    pages = 0
    while True:
      response = await self.users(
        trade_type,
        uid=uid,
        rcode=rcode,
        tag=tag,
        start_at=start_at,
        end_at=end_at,
        page_size=page_size,
        page=page,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      total = response.get('totalPage') if response is not None else None
      total = int(total) if total is not None else None
      if total is None or pages >= total:
        break
      page += 1

  async def users(
    self,
    trade_type: Literal['all', 'SPOT', 'FUTURE'] | None = None,
    *,
    uid: str | None = None,
    rcode: str | None = None,
    tag: str | None = None,
    start_at: TimestampMillis | None = None,
    end_at: TimestampMillis | None = None,
    page: int | None = None,
    page_size: int | None = None,
    validate: bool | None = None,
  ) -> BrokerUsersResult:
    """Page through the authenticated API Broker's onboarded users, with their lifetime trading volume, fee and commission -- broken out by whether the trades carried a broker tag -- optionally filtered by referral code, tag or registration date.

    Args:
      trade_type: Trade type to restrict the query to.
      uid: Restrict to one user UID.
      rcode: Restrict to users who registered through this referral code.
      tag: Restrict to users who traded under this broker tag.
      start_at: Restrict to users who registered at or after this time, Unix milliseconds.
      end_at: Restrict to users who registered at or before this time, Unix milliseconds.
      page: Page number.
      page_size: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/broker/api-broker/get-user-list)
    """
    request: Request = Request()
    if trade_type is not None:
      request['tradeType'] = trade_type
    if uid is not None:
      request['uid'] = uid
    if rcode is not None:
      request['rcode'] = rcode
    if tag is not None:
      request['tag'] = tag
    if start_at is not None:
      request['startAt'] = start_at
    if end_at is not None:
      request['endAt'] = end_at
    if page is not None:
      request['page'] = page
    if page_size is not None:
      request['pageSize'] = page_size
    return await self.request(
      request,
      method='GET',
      path='/api/v2/broker/queryUser',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerUsersResult,
    )
