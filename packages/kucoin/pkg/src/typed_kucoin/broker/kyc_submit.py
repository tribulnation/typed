# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_kucoin.core import DateIso
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  clientUid: str
  """Sub-account (client) UID."""
  firstName: str
  """First name."""
  lastName: str
  """Last name."""
  issueCountry: str
  """ISO country code of the identity document's issuing country, e.g. `CN`, `US`, `DE`, `JP`."""
  birthDate: DateIso
  """Date of birth, `YYYY-MM-DD`."""
  identityType: Literal['idcard', 'drivinglicense', 'passport', 'bvn']
  """Identity document type."""
  identityNumber: str
  """Identity document number."""
  expireDate: DateIso
  """Identity document expiry date, `YYYY-MM-DD`. If the document has no expiry, fill in `2099-01-01`."""
  frontPhoto: str
  """Front photo of the identity document, base64-encoded data URI (PNG or JPG recommended, max 2MB decoded). Optional when `identityType` is `bvn`."""
  backendPhoto: str
  """Back photo of the identity document, base64-encoded data URI, same constraints as `frontPhoto`. Optional when `identityType` is `passport` or `bvn`."""
  facePhoto: str
  """Selfie photo, base64-encoded data URI, same constraints as `frontPhoto`."""


Response = str | None


class KycSubmit(RpcEndpoint):
  """Submit identity-verification (KYC) information for a sub-account of the Exchange (ND) Broker, on the sub-account's behalf."""

  async def kyc_submit(
    self,
    *,
    client_uid: str,
    first_name: str,
    last_name: str,
    issue_country: str,
    birth_date: DateIso,
    identity_type: Literal['idcard', 'drivinglicense', 'passport', 'bvn'],
    identity_number: str,
    expire_date: DateIso,
    front_photo: str,
    backend_photo: str,
    face_photo: str,
    validate: bool | None = None,
  ) -> Response:
    """Submit identity-verification (KYC) information for a sub-account of the Exchange (ND) Broker, on the sub-account's behalf.

    Args:
      client_uid: Sub-account (client) UID.
      first_name: First name.
      last_name: Last name.
      issue_country: ISO country code of the identity document's issuing country, e.g. `CN`, `US`, `DE`, `JP`.
      birth_date: Date of birth, `YYYY-MM-DD`.
      identity_type: Identity document type.
      identity_number: Identity document number.
      expire_date: Identity document expiry date, `YYYY-MM-DD`. If the document has no expiry, fill in `2099-01-01`.
      front_photo: Front photo of the identity document, base64-encoded data URI (PNG or JPG recommended, max 2MB decoded). Optional when `identityType` is `bvn`.
      backend_photo: Back photo of the identity document, base64-encoded data URI, same constraints as `frontPhoto`. Optional when `identityType` is `passport` or `bvn`.
      face_photo: Selfie photo, base64-encoded data URI, same constraints as `frontPhoto`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3472406)
    """
    return await self.request(
      Request(
        clientUid=client_uid,
        firstName=first_name,
        lastName=last_name,
        issueCountry=issue_country,
        birthDate=birth_date,
        identityType=identity_type,
        identityNumber=identity_number,
        expireDate=expire_date,
        frontPhoto=front_photo,
        backendPhoto=backend_photo,
        facePhoto=face_photo,
      ),
      method='POST',
      path='/api/kyc/ndBroker/proxyClient/submit',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
