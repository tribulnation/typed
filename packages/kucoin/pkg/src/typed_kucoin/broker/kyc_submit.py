# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /api/kyc/ndBroker/proxyClient/submit` — Submit KYC."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_kucoin.core import DateIso, RpcEndpoint, date_iso


class KycSubmitRequest(TypedDict):
  clientUid: str
  """Sub-account (client) UID."""
  firstName: str
  """First name."""
  lastName: str
  """Last name."""
  issueCountry: str
  """ISO country code of the identity document's issuing country, e.g. `CN`, `US`, `DE`, `JP`."""
  birthDate: DateIso
  """Date of birth, `YYYY-MM-DD`."""
  identityType: Literal['idcard', 'drivinglicense', 'passport', 'bvn']
  """Identity document type."""
  identityNumber: str
  """Identity document number."""
  expireDate: DateIso
  """Identity document expiry date, `YYYY-MM-DD`. If the document has no expiry, fill in `2099-01-01`."""
  frontPhoto: str
  """Front photo of the identity document, base64-encoded data URI (PNG or JPG recommended, max 2MB decoded). Optional when `identityType` is `bvn`."""
  backendPhoto: str
  """Back photo of the identity document, base64-encoded data URI, same constraints as `frontPhoto`. Optional when `identityType` is `passport` or `bvn`."""
  facePhoto: str
  """Selfie photo, base64-encoded data URI, same constraints as `frontPhoto`."""


_Type = str | None
adapter = validator[_Type](_Type)  # type: ignore


class KycSubmit(RpcEndpoint):
  """`Submit KYC` — mixed into `Broker`, the product exposing `broker.kyc_submit`."""

  async def kyc_submit(
    self,
    kyc_submit_request: KycSubmitRequest,
    *,
    validate: bool | None = None,
  ) -> str | None:
    """Submit identity-verification (KYC) information for a sub-account of the Exchange (ND) Broker, on the sub-account's behalf.

    Args:
      kyc_submit_request: Identity document and selfie for one sub-account.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    kyc_submit_request_wire: dict = dict(kyc_submit_request)
    if kyc_submit_request_wire.get('birthDate') is not None:
      kyc_submit_request_wire['birthDate'] = date_iso.dump(
        kyc_submit_request_wire['birthDate']
      )
    if kyc_submit_request_wire.get('expireDate') is not None:
      kyc_submit_request_wire['expireDate'] = date_iso.dump(
        kyc_submit_request_wire['expireDate']
      )
    return await self.authed_request(
      'POST',
      '/api/kyc/ndBroker/proxyClient/submit',
      json=kyc_submit_request_wire,
      validator=adapter,
      validate=validate,
    )
