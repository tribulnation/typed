# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /api/v1/broker/nd/transfer` — Transfer."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint


class BrokerTransferRequest(TypedDict):
  currency: str
  """Currency to transfer."""
  amount: str
  """Amount to transfer, in the currency's precision; must be positive."""
  direction: Literal['OUT', 'IN']
  """Transfer direction."""
  accountType: Literal['MAIN', 'TRADE']
  """Broker account type."""
  specialUid: str
  """Sub-account UID; must be a sub-account created by this broker."""
  specialAccountType: Literal['MAIN', 'TRADE']
  """Sub-account type."""
  clientOid: str
  """Client-generated unique identifier for this transfer, e.g. a UUID. Max 128 characters."""


class BrokerTransferResult(TypedDict):
  """Result of a broker transfer."""

  orderId: str
  """New transfer order id."""


_Type = BrokerTransferResult
adapter = validator[_Type](_Type)  # type: ignore


class Transfer(RpcEndpoint):
  """`Transfer` — mixed into `Broker`, the product exposing `broker.transfer`."""

  async def transfer(
    self,
    broker_transfer_request: BrokerTransferRequest,
    *,
    validate: bool | None = None,
  ) -> BrokerTransferResult:
    """Transfer funds between the Exchange (ND) Broker's own account and one of its sub-accounts. Withdrawal from a sub-account is not direct -- funds must first be transferred to the broker account via this endpoint, then withdrawn from there.

    Args:
      broker_transfer_request: Currency, amount, direction and the two accounts (broker + sub-account) involved.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    return await self.authed_request(
      'POST',
      '/api/v1/broker/nd/transfer',
      json=broker_transfer_request,
      validator=adapter,
      validate=validate,
    )
