# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class BrokerTransferResult(TypedDict):
  """Result of a broker transfer."""

  orderId: str
  """New transfer order id."""


class Request(TypedDict):
  currency: str
  """Currency to transfer."""
  amount: Decimal
  """Amount to transfer, in the currency's precision; must be positive."""
  direction: Literal['OUT', 'IN']
  """Transfer direction."""
  accountType: Literal['MAIN', 'TRADE']
  """Broker account type."""
  specialUid: str
  """Sub-account UID; must be a sub-account created by this broker."""
  specialAccountType: Literal['MAIN', 'TRADE']
  """Sub-account type."""
  clientOid: str
  """Client-generated unique identifier for this transfer, e.g. a UUID. Max 128 characters."""


class Transfer(RpcEndpoint):
  """Transfer funds between the Exchange (ND) Broker's own account and one of its sub-accounts. Withdrawal from a sub-account is not direct -- funds must first be transferred to the broker account via this endpoint, then withdrawn from there."""

  async def transfer(
    self,
    *,
    currency: str,
    amount: Decimal,
    direction: Literal['OUT', 'IN'],
    account_type: Literal['MAIN', 'TRADE'],
    special_uid: str,
    special_account_type: Literal['MAIN', 'TRADE'],
    client_oid: str,
    validate: bool | None = None,
  ) -> BrokerTransferResult:
    """Transfer funds between the Exchange (ND) Broker's own account and one of its sub-accounts. Withdrawal from a sub-account is not direct -- funds must first be transferred to the broker account via this endpoint, then withdrawn from there.

    Args:
      currency: Currency to transfer.
      amount: Amount to transfer, in the currency's precision; must be positive.
      direction: Transfer direction.
      account_type: Broker account type.
      special_uid: Sub-account UID; must be a sub-account created by this broker.
      special_account_type: Sub-account type.
      client_oid: Client-generated unique identifier for this transfer, e.g. a UUID. Max 128 characters.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470293)
    """
    return await self.request(
      Request(
        currency=currency,
        amount=amount,
        direction=direction,
        accountType=account_type,
        specialUid=special_uid,
        specialAccountType=special_account_type,
        clientOid=client_oid,
      ),
      method='POST',
      path='/api/v1/broker/nd/transfer',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerTransferResult,
    )
