# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /api/v3/broker/nd/deposit/detail` — Get Deposit Detail."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint, TimestampMillis


class BrokerDepositDetail(TypedDict):
  """Detail of one sub-account deposit."""

  chain: str
  """Chain id of the currency."""
  hash: str
  """On-chain transaction hash."""
  walletTxId: str
  """Wallet transaction id."""
  uid: int
  """Depositing sub-account's UID."""
  updatedAt: TimestampMillis
  """Record last-update time, Unix milliseconds."""
  amount: str
  """Deposit amount."""
  memo: str
  """Address memo/tag; empty string if none."""
  fee: str
  """Fee charged for the deposit."""
  address: str
  """Deposit address."""
  remark: str
  """Free-text remark; empty string if none."""
  isInner: bool
  """Whether this was an internal (KuCoin-to-KuCoin) deposit rather than on-chain."""
  currency: str
  """Currency deposited."""
  status: Literal['PROCESSING', 'SUCCESS', 'FAILURE']
  """Deposit status."""
  createdAt: TimestampMillis
  """Record creation time, Unix milliseconds."""


_Type = BrokerDepositDetail
adapter = validator[_Type](_Type)  # type: ignore


class DepositDetail(RpcEndpoint):
  """`Get Deposit Detail` — mixed into `Broker`, the product exposing `broker.deposit_detail`."""

  async def deposit_detail(
    self,
    *,
    currency: str,
    hash: str,
    validate: bool | None = None,
  ) -> BrokerDepositDetail:
    """Get the detail of one deposit into an Exchange (ND) Broker sub-account, by currency and transaction hash. Excludes the broker's own main account.

    Args:
      currency: Currency deposited.
      hash: On-chain transaction hash.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    params: dict = {
      'currency': currency,
      'hash': hash,
    }
    return await self.authed_request(
      'GET',
      '/api/v3/broker/nd/deposit/detail',
      params=params,
      validator=adapter,
      validate=validate,
    )
