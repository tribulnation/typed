# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import BrokerDeposit


class Request(TypedDict):
  currency: str
  """Currency deposited."""
  hash: str
  """On-chain transaction hash."""


class DepositDetail(RpcEndpoint):
  """Get the detail of one deposit into an Exchange (ND) Broker sub-account, by currency and transaction hash. Excludes the broker's own main account."""

  async def deposit_detail(
    self,
    *,
    currency: str,
    hash: str,
    validate: bool | None = None,
  ) -> BrokerDeposit:
    """Get the detail of one deposit into an Exchange (ND) Broker sub-account, by currency and transaction hash. Excludes the broker's own main account.

    Args:
      currency: Currency deposited.
      hash: On-chain transaction hash.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470288)
    """
    return await self.request(
      Request(currency=currency, hash=hash),
      method='GET',
      path='/api/v3/broker/nd/deposit/detail',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerDeposit,
    )
