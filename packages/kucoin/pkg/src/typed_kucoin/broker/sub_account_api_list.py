# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_kucoin.core import TimestampMillis
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class BrokerSubAccountApiKey(TypedDict):
  """One broker sub-account API key."""

  uid: str
  """Sub-account UID."""
  label: str
  """API key remarks."""
  apiKey: str
  """The API key."""
  apiVersion: int
  """API key version."""
  permissions: list[Literal['General', 'Spot', 'Futures']]
  """Permission group list."""
  ipWhitelist: list[str]
  """IP whitelist assigned to the key."""
  createdAt: TimestampMillis
  """Creation time, Unix milliseconds."""


class Request(TypedDict):
  uid: str
  """Sub-account UID."""
  apiKey: NotRequired[str]
  """Restrict the response to one API key. Omit for every key belonging to `uid`."""


BrokerSubAccountApiKeys = list[BrokerSubAccountApiKey]


class SubAccountApiList(RpcEndpoint):
  """List API keys belonging to one Exchange (ND) Broker sub-account, optionally filtered to one key."""

  async def sub_account_api_list(
    self,
    *,
    uid: str,
    api_key: str | None = None,
    validate: bool | None = None,
  ) -> BrokerSubAccountApiKeys:
    """List API keys belonging to one Exchange (ND) Broker sub-account, optionally filtered to one key.

    Args:
      uid: Sub-account UID.
      api_key: Restrict the response to one API key. Omit for every key belonging to `uid`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470284)
    """
    request: Request = Request(uid=uid)
    if api_key is not None:
      request['apiKey'] = api_key
    return await self.request(
      request,
      method='GET',
      path='/api/v1/broker/nd/account/apikey',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerSubAccountApiKeys,
    )
