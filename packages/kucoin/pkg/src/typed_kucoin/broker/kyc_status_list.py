# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, NotRequired, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import KycStatusEntry


class Request(TypedDict):
  pageNumber: NotRequired[int]
  """Page number."""
  pageSize: NotRequired[int]
  """Results per page."""


class KycStatusPage(TypedDict):
  """One page of sub-account KYC statuses."""

  currentPage: int
  """Current page number."""
  pageSize: int
  """Page size used."""
  totalNum: int
  """Total number of sub-accounts."""
  totalPage: int
  """Total number of pages."""
  items: list[KycStatusEntry]
  """Sub-account KYC statuses on this page."""


class KycStatusList(RpcEndpoint):
  """List the KYC (identity-verification) status of every Exchange (ND) Broker sub-account, paginated."""

  async def kyc_status_list_paged(
    self,
    *,
    page_size: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[KycStatusPage]:
    """List the KYC (identity-verification) status of every Exchange (ND) Broker sub-account, paginated.

    Paged variant of `kyc_status_list`: Requests `pageNumber` from 1 upwards and stops once it has covered the `totalPage` pages the response reports, or after `max_pages` pages when one is given.

    Args:
      page_size: Results per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3472408)
    """
    page_number = 1
    pages = 0
    while True:
      response = await self.kyc_status_list(
        page_size=page_size, page_number=page_number, validate=validate
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      total = response.get('totalPage') if response is not None else None
      total = int(total) if total is not None else None
      if total is None or pages >= total:
        break
      page_number += 1

  async def kyc_status_list(
    self,
    *,
    page_number: int | None = None,
    page_size: int | None = None,
    validate: bool | None = None,
  ) -> KycStatusPage:
    """List the KYC (identity-verification) status of every Exchange (ND) Broker sub-account, paginated.

    Args:
      page_number: Page number.
      page_size: Results per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3472408)
    """
    request: Request = Request()
    if page_number is not None:
      request['pageNumber'] = page_number
    if page_size is not None:
      request['pageSize'] = page_size
    return await self.request(
      request,
      method='GET',
      path='/api/kyc/ndBroker/proxyClient/status/page',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=KycStatusPage,
    )
