# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class BrokerRebateDownload(TypedDict):
  """Download link for a broker rebate order file."""

  url: str
  """Pre-signed URL to the rebate order file (CSV); valid for 1 day."""


class Request(TypedDict):
  begin: str
  """Start date, `YYYYMMDD`, e.g. `20240610`."""
  end: str
  """End date, `YYYYMMDD`, e.g. `20241010`. The `begin`-`end` span may cover at most 6 months."""
  tradeType: Literal['1', '2']
  """Business line to download rebates for."""


class RebateDownload(RpcEndpoint):
  """Get a download link for the Exchange (ND) Broker's rebate orders over a date range, filtered to spot or futures trading. Distinct from the (separate) API Broker program's own rebate-download endpoint (`broker.rebate_download_api`)."""

  async def rebate_download(
    self,
    *,
    begin: str,
    end: str,
    trade_type: Literal['1', '2'],
    validate: bool | None = None,
  ) -> BrokerRebateDownload:
    """Get a download link for the Exchange (ND) Broker's rebate orders over a date range, filtered to spot or futures trading. Distinct from the (separate) API Broker program's own rebate-download endpoint (`broker.rebate_download_api`).

    Args:
      begin: Start date, `YYYYMMDD`, e.g. `20240610`.
      end: End date, `YYYYMMDD`, e.g. `20241010`. The `begin`-`end` span may cover at most 6 months.
      trade_type: Business line to download rebates for.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470281)
    """
    return await self.request(
      Request(begin=begin, end=end, tradeType=trade_type),
      method='GET',
      path='/api/v1/broker/nd/rebase/download',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerRebateDownload,
    )
