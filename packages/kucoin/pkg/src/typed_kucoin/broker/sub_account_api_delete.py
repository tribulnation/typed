# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  uid: str
  """Sub-account UID owning the key."""
  apiKey: str
  """API key to delete."""


Response = bool


class SubAccountApiDelete(RpcEndpoint):
  """Delete an existing Exchange (ND) Broker sub-account API key."""

  async def sub_account_api_delete(
    self,
    *,
    uid: str,
    api_key: str,
    validate: bool | None = None,
  ) -> Response:
    """Delete an existing Exchange (ND) Broker sub-account API key.

    Args:
      uid: Sub-account UID owning the key.
      api_key: API key to delete.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470289)
    """
    return await self.request(
      Request(uid=uid, apiKey=api_key),
      method='DELETE',
      path='/api/v1/broker/nd/account/apikey',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
