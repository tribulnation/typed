# Generated by typed-dev — see CONTRIBUTING.md.
"""`DELETE /api/v1/broker/nd/account/apikey` — Delete sub-account API."""

from typed_core.validation import validator
from typed_kucoin.core import RpcEndpoint


_Type = bool
adapter = validator[_Type](_Type)  # type: ignore


class SubAccountApiDelete(RpcEndpoint):
  """`Delete sub-account API` — mixed into `Broker`, the product exposing `broker.sub_account_api_delete`."""

  async def sub_account_api_delete(
    self,
    *,
    uid: str,
    api_key: str,
    validate: bool | None = None,
  ) -> bool:
    """Delete an existing Exchange (ND) Broker sub-account API key.

    Args:
      uid: Sub-account UID owning the key.
      api_key: API key to delete.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    params: dict = {
      'uid': uid,
      'apiKey': api_key,
    }
    return await self.authed_request(
      'DELETE',
      '/api/v1/broker/nd/account/apikey',
      params=params,
      validator=adapter,
      validate=validate,
    )
