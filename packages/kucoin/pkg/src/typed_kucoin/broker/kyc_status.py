# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import KycStatusEntry


class Request(TypedDict):
  clientUids: str
  """One or more sub-account (client) UIDs, comma-separated for multiple."""


KycStatuses = list[KycStatusEntry]


class KycStatus(RpcEndpoint):
  """Get the KYC (identity-verification) status of one or more Exchange (ND) Broker sub-accounts, by UID."""

  async def kyc_status(
    self,
    client_uids: str,
    *,
    validate: bool | None = None,
  ) -> KycStatuses:
    """Get the KYC (identity-verification) status of one or more Exchange (ND) Broker sub-accounts, by UID.

    Args:
      client_uids: One or more sub-account (client) UIDs, comma-separated for multiple.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3472407)
    """
    return await self.request(
      Request(clientUids=client_uids),
      method='GET',
      path='/api/kyc/ndBroker/proxyClient/status/list',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=KycStatuses,
    )
