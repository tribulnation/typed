# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_kucoin.core import TimestampMillis
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import BrokerDeposit


class Request(TypedDict):
  currency: NotRequired[str]
  """Filter to one currency. Omit for every currency."""
  status: NotRequired[Literal['PROCESSING', 'SUCCESS', 'FAILURE']]
  """Filter by deposit status."""
  hash: NotRequired[str]
  """Filter to one on-chain transaction hash."""
  startTimestamp: NotRequired[TimestampMillis]
  """Range start, Unix milliseconds."""
  endTimestamp: NotRequired[TimestampMillis]
  """Range end, Unix milliseconds."""
  limit: NotRequired[int]
  """Maximum rows to return, most recent first."""


BrokerDeposits = list[BrokerDeposit]


class DepositList(RpcEndpoint):
  """List deposit records across every sub-account of the authenticated Exchange (ND) Broker, most recent first, optionally filtered by currency, status, transaction hash and/or a time window."""

  async def deposit_list(
    self,
    *,
    currency: str | None = None,
    status: Literal['PROCESSING', 'SUCCESS', 'FAILURE'] | None = None,
    hash: str | None = None,
    start_timestamp: TimestampMillis | None = None,
    end_timestamp: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> BrokerDeposits:
    """List deposit records across every sub-account of the authenticated Exchange (ND) Broker, most recent first, optionally filtered by currency, status, transaction hash and/or a time window.

    Args:
      currency: Filter to one currency. Omit for every currency.
      status: Filter by deposit status.
      hash: Filter to one on-chain transaction hash.
      start_timestamp: Range start, Unix milliseconds.
      end_timestamp: Range end, Unix milliseconds.
      limit: Maximum rows to return, most recent first.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470285)
    """
    request: Request = Request()
    if currency is not None:
      request['currency'] = currency
    if status is not None:
      request['status'] = status
    if hash is not None:
      request['hash'] = hash
    if start_timestamp is not None:
      request['startTimestamp'] = start_timestamp
    if end_timestamp is not None:
      request['endTimestamp'] = end_timestamp
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/api/v1/asset/ndbroker/deposit/list',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerDeposits,
    )
