# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /api/v3/broker/nd/transfer/detail` — Get Transfer History."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint, TimestampMillis


class BrokerTransferDetail(TypedDict):
  """Detail of one broker transfer."""

  orderId: str
  """Transfer order id."""
  currency: str
  """Currency transferred."""
  amount: str
  """Amount transferred."""
  fromUid: int
  """UID of the account transferred out of."""
  fromAccountType: Literal['MAIN', 'TRADE', 'CONTRACT', 'MARGIN', 'ISOLATED']
  """Account type transferred out of."""
  fromAccountTag: str
  """Trading pair, required when `fromAccountType` is `ISOLATED`, e.g. `BTC-USDT`. `DEFAULT` otherwise, by analogy with other KuCoin isolated-margin account-tag fields."""
  toUid: int
  """UID of the account transferred into."""
  toAccountType: Literal['MAIN', 'TRADE', 'CONTRACT', 'MARGIN', 'ISOLATED']
  """Account type transferred into."""
  toAccountTag: str
  """Trading pair, required when `toAccountType` is `ISOLATED`, e.g. `BTC-USDT`. `DEFAULT` otherwise."""
  status: Literal['PROCESSING', 'SUCCESS', 'FAILURE']
  """Transfer status."""
  reason: str | None
  """Failure reason, when `status` is `FAILURE`; `null` otherwise."""
  createdAt: TimestampMillis
  """Creation time, Unix milliseconds."""


_Type = BrokerTransferDetail
adapter = validator[_Type](_Type)  # type: ignore


class TransferDetail(RpcEndpoint):
  """`Get Transfer History` — mixed into `Broker`, the product exposing `broker.transfer_detail`."""

  async def transfer_detail(
    self,
    *,
    order_id: str,
    validate: bool | None = None,
  ) -> BrokerTransferDetail:
    """Get the detail of one transfer between the Exchange (ND) Broker's own account and a sub-account, by transfer order id. Despite the upstream summary's name, this is a single-record lookup by `orderId`, not a paginated history list -- see notes.

    Args:
      order_id: Transfer order id, as returned by `broker.transfer`.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    params: dict = {
      'orderId': order_id,
    }
    return await self.authed_request(
      'GET',
      '/api/v3/broker/nd/transfer/detail',
      params=params,
      validator=adapter,
      validate=validate,
    )
