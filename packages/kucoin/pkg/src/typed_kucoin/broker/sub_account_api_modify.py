# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_kucoin.core import TimestampMillis
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class BrokerSubAccountApiKeyModified(TypedDict):
  """A broker sub-account API key after modification."""

  uid: str
  """Sub-account UID."""
  label: str
  """API key remarks."""
  apiKey: str
  """The API key."""
  apiVersion: int
  """API key version."""
  permissions: list[str]
  """Permission group list assigned to the key."""
  ipWhitelist: list[str]
  """IP whitelist assigned to the key."""
  createdAt: TimestampMillis
  """Creation time, Unix milliseconds."""


class Request(TypedDict):
  uid: str
  """Sub-account UID owning the key."""
  apiKey: str
  """API key to modify."""
  ipWhitelist: list[str]
  """New IP whitelist, up to 20 entries."""
  permissions: list[Literal['general', 'spot', 'futures']]
  """New permission group list -- only `general`, `spot` and `futures` may be set here."""
  label: str
  """New API key remarks, 4-32 characters."""


class SubAccountApiModify(RpcEndpoint):
  """Update the IP whitelist, permission set and/or label of an existing Exchange (ND) Broker sub-account API key."""

  async def sub_account_api_modify(
    self,
    *,
    uid: str,
    api_key: str,
    ip_whitelist: list[str],
    permissions: list[Literal['general', 'spot', 'futures']],
    label: str,
    validate: bool | None = None,
  ) -> BrokerSubAccountApiKeyModified:
    """Update the IP whitelist, permission set and/or label of an existing Exchange (ND) Broker sub-account API key.

    Args:
      uid: Sub-account UID owning the key.
      api_key: API key to modify.
      ip_whitelist: New IP whitelist, up to 20 entries.
      permissions: New permission group list -- only `general`, `spot` and `futures` may be set here.
      label: New API key remarks, 4-32 characters.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/api-3470292)
    """
    return await self.request(
      Request(
        uid=uid,
        apiKey=api_key,
        ipWhitelist=ip_whitelist,
        permissions=permissions,
        label=label,
      ),
      method='POST',
      path='/api/v1/broker/nd/account/update-apikey',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerSubAccountApiKeyModified,
    )
