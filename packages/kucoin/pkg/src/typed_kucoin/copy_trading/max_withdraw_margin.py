# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  symbol: str
  """Contract symbol, e.g. `XBTUSDTM`."""
  positionSide: Literal['LONG', 'SHORT']
  """Position side."""


Response = str


class MaxWithdrawMargin(RpcEndpoint):
  """Get the maximum amount of margin that the current copy-trading position for a symbol/side supports withdrawing (isolated margin only)."""

  async def max_withdraw_margin(
    self,
    symbol: str,
    *,
    position_side: Literal['LONG', 'SHORT'],
    validate: bool | None = None,
  ) -> Response:
    """Get the maximum amount of margin that the current copy-trading position for a symbol/side supports withdrawing (isolated margin only).

    Args:
      symbol: Contract symbol, e.g. `XBTUSDTM`.
      position_side: Position side.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/copy-trading/get-max-withdraw-margin)
    """
    return await self.request(
      Request(symbol=symbol, positionSide=position_side),
      method='GET',
      path='/api/v1/copy-trade/futures/position/margin/max-withdraw-margin',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
