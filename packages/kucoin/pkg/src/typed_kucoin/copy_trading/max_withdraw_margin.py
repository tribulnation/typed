# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /api/v1/copy-trade/futures/position/margin/max-withdraw-margin` — Get Max Withdraw Margin."""

from typing_extensions import Literal
from typed_core.validation import validator
from typed_kucoin.core import RpcEndpoint


_Type = str
adapter = validator[_Type](_Type)  # type: ignore


class MaxWithdrawMargin(RpcEndpoint):
  """`Get Max Withdraw Margin` — mixed into `CopyTrading`, the product exposing `copy_trading.max_withdraw_margin`."""

  async def max_withdraw_margin(
    self,
    *,
    symbol: str,
    position_side: Literal['LONG', 'SHORT'],
    validate: bool | None = None,
  ) -> str:
    """Get the maximum amount of margin that the current copy-trading position for a symbol/side supports withdrawing (isolated margin only).

    Args:
      symbol: Contract symbol, e.g. `XBTUSDTM`.
      position_side: Position side.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    params: dict = {
      'symbol': symbol,
      'positionSide': position_side,
    }
    return await self.authed_request(
      'GET',
      '/api/v1/copy-trade/futures/position/margin/max-withdraw-margin',
      params=params,
      validator=adapter,
      validate=validate,
    )
