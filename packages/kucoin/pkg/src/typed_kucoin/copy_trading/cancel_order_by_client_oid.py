# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class CopyTradeCancelByClientOidResult(TypedDict):
  """Confirmation of the cancelled order."""

  clientOid: str
  """Client-provided order id, echoed back."""


class Request(TypedDict):
  symbol: str
  """Contract symbol the order belongs to."""
  clientOid: str
  """Client-provided id of the order to cancel."""


class CancelOrderByClientOid(RpcEndpoint):
  """Cancel a copy-trading order (including a stop order) by its client-provided order id. The request is acknowledged immediately; the matching engine processes the cancellation asynchronously. Fails if the order is already filled or previously cancelled. Requires the API key's `LeadtradeFutures` permission."""

  async def cancel_order_by_client_oid(
    self,
    *,
    symbol: str,
    client_oid: str,
    validate: bool | None = None,
  ) -> CopyTradeCancelByClientOidResult:
    """Cancel a copy-trading order (including a stop order) by its client-provided order id. The request is acknowledged immediately; the matching engine processes the cancellation asynchronously. Fails if the order is already filled or previously cancelled. Requires the API key's `LeadtradeFutures` permission.

    Args:
      symbol: Contract symbol the order belongs to.
      client_oid: Client-provided id of the order to cancel.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/copy-trading/cancel-order-by-clientoid)
    """
    return await self.request(
      Request(symbol=symbol, clientOid=client_oid),
      method='DELETE',
      path='/api/v1/copy-trade/futures/orders/client-order',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CopyTradeCancelByClientOidResult,
    )
