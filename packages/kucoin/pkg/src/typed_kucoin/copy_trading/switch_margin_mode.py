# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import MarginModeSetting


class Request(TypedDict):
  """Symbol and target margin mode to switch to."""

  symbol: str
  """Symbol of the contract, e.g. `XBTUSDTM`."""
  marginMode: Literal['ISOLATED', 'CROSS']
  """Margin mode to switch to."""


class SwitchMarginMode(RpcEndpoint):
  """Modify the margin mode (cross or isolated) of a copy-trading futures position's symbol. Requires the API key's `LeadtradeFutures` permission."""

  async def switch_margin_mode(
    self,
    symbol: str,
    *,
    margin_mode: Literal['ISOLATED', 'CROSS'],
    validate: bool | None = None,
  ) -> MarginModeSetting:
    """Modify the margin mode (cross or isolated) of a copy-trading futures position's symbol. Requires the API key's `LeadtradeFutures` permission.

    Args:
      symbol: Symbol of the contract, e.g. `XBTUSDTM`.
      margin_mode: Margin mode to switch to.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/copy-trading/switch-margin-mode)
    """
    return await self.request(
      Request(symbol=symbol, marginMode=margin_mode),
      method='POST',
      path='/api/v1/copy-trade/futures/position/changeMarginMode',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=MarginModeSetting,
    )
