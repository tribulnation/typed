# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Symbol, position side and target auto-deposit status."""

  symbol: str
  """Contract symbol, e.g. `XBTUSDTM`."""
  status: bool
  """Enable (`true`) or disable (`false`) automatic margin deposit."""
  positionSide: Literal['LONG', 'SHORT']
  """Position side."""


Response = bool


class ModifyIsolatedMarginAutoDepositStatus(RpcEndpoint):
  """Enable or disable automatic margin deposit (to avoid liquidation) for an existing copy-trading isolated position. The docs page notes this feature "is no longer recommended" and suggests using cross margin instead. Requires the API key's `LeadtradeFutures` permission."""

  async def modify_isolated_margin_auto_deposit_status(
    self,
    symbol: str,
    *,
    status: bool,
    position_side: Literal['LONG', 'SHORT'],
    validate: bool | None = None,
  ) -> Response:
    """Enable or disable automatic margin deposit (to avoid liquidation) for an existing copy-trading isolated position. The docs page notes this feature "is no longer recommended" and suggests using cross margin instead. Requires the API key's `LeadtradeFutures` permission.

    Args:
      symbol: Contract symbol, e.g. `XBTUSDTM`.
      status: Enable (`true`) or disable (`false`) automatic margin deposit.
      position_side: Position side.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/copy-trading/modify-isolated-margin-auto-deposit-status)
    """
    return await self.request(
      Request(symbol=symbol, status=status, positionSide=position_side),
      method='POST',
      path='/api/v1/copy-trade/futures/position/margin/auto-deposit-status',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
