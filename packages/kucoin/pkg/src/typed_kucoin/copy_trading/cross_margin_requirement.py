# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /api/v2/copy-trade/getCrossModeMarginRequirement` — Get Cross Margin Requirement."""

from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint


class CopyTradeCrossMarginRequirement(TypedDict):
  """Cross-margin requirement for a symbol at the requested position value and leverage."""

  imr: str
  """Initial margin rate."""
  mmr: str
  """Maintenance margin rate."""
  positionValue: str
  """The requested position value, echoed back."""
  price: str
  """Latest trading price used for the calculation."""
  size: str
  """Position size, in contracts."""
  symbol: str
  """Symbol of the contract."""


class GetCopyTradeCrossMarginRequirementParams(TypedDict):
  """Symbol(s), position value and leverage to calculate margin requirements for."""

  symbol: str
  """Trading pair name, e.g. `XBTUSDTM`. Supports up to 50 trading pairs separated by commas."""
  positionValue: str
  """Position value. For USDT contracts the unit is USDT, for USDC contracts USDC, for coin-margined contracts USD."""
  leverage: str
  """Leverage multiple. Defaults to the symbol's maximum leverage; an error is returned if this exceeds the symbol's maximum leverage."""


_Type = list[CopyTradeCrossMarginRequirement]
adapter = validator[_Type](_Type)  # type: ignore


class CrossMarginRequirement(RpcEndpoint):
  """`Get Cross Margin Requirement` — mixed into `CopyTrading`, the product exposing `copy_trading.cross_margin_requirement`."""

  async def cross_margin_requirement(
    self,
    get_copy_trade_cross_margin_requirement_params: GetCopyTradeCrossMarginRequirementParams,
    *,
    validate: bool | None = None,
  ) -> list[CopyTradeCrossMarginRequirement]:
    """Query the cross-margin requirements (initial and maintenance margin rates) of a symbol at a given position value and leverage. Despite the `Get` name this is a `POST` request -- a stateless calculation rather than a read of stored account/position data, and the venue's own docs page groups it under Copy Trading regardless. Requires the API key's `LeadtradeFutures` permission.

    Args:
      get_copy_trade_cross_margin_requirement_params: Symbol(s), position value and leverage to calculate margin requirements for.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/copy-trade/getCrossModeMarginRequirement',
      json=get_copy_trade_cross_margin_requirement_params,
      validator=adapter,
      validate=validate,
    )
