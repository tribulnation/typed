# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /api/v2/copy-trade/position/switchPositionMode` — Switch Position Mode."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint


class CopyTradePositionMode(TypedDict):
  """Position mode currently set for the copy-trading account, after the switch."""

  positionMode: Literal['0', '1']
  """Position mode now in effect, applies to all futures trading pairs."""


class SwitchCopyTradePositionModeParams(TypedDict):
  """Target position mode to switch the copy-trading account to."""

  positionMode: Literal['0', '1']
  """Position mode, applies to all futures trading pairs."""


_Type = CopyTradePositionMode
adapter = validator[_Type](_Type)  # type: ignore


class SwitchPositionMode(RpcEndpoint):
  """`Switch Position Mode` — mixed into `CopyTrading`, the product exposing `copy_trading.switch_position_mode`."""

  async def switch_position_mode(
    self,
    switch_copy_trade_position_mode_params: SwitchCopyTradePositionModeParams,
    *,
    validate: bool | None = None,
  ) -> CopyTradePositionMode:
    """Toggle a copy-trading futures account between one-way and hedge position mode. Requires the API key's `LeadtradeFutures` permission.

    Args:
      switch_copy_trade_position_mode_params: Target position mode.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/copy-trade/position/switchPositionMode',
      json=switch_copy_trade_position_mode_params,
      validator=adapter,
      validate=validate,
    )
