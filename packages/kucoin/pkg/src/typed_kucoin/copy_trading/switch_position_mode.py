# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class CopyTradePositionMode(TypedDict):
  """Position mode currently set for the copy-trading account, after the switch."""

  positionMode: Literal['0', '1']
  """Position mode now in effect, applies to all futures trading pairs."""


class Request(TypedDict):
  """Target position mode to switch the copy-trading account to."""

  positionMode: Literal['0', '1']
  """Position mode, applies to all futures trading pairs."""


class SwitchPositionMode(RpcEndpoint):
  """Toggle a copy-trading futures account between one-way and hedge position mode. Requires the API key's `LeadtradeFutures` permission."""

  async def switch_position_mode(
    self,
    position_mode: Literal['0', '1'],
    *,
    validate: bool | None = None,
  ) -> CopyTradePositionMode:
    """Toggle a copy-trading futures account between one-way and hedge position mode. Requires the API key's `LeadtradeFutures` permission.

    Args:
      position_mode: Position mode, applies to all futures trading pairs.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/copy-trading/switch-position-mode)
    """
    return await self.request(
      Request(positionMode=position_mode),
      method='POST',
      path='/api/v2/copy-trade/position/switchPositionMode',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CopyTradePositionMode,
    )
