# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Symbol and target risk-limit level."""

  symbol: str
  """Contract symbol, e.g. `XBTUSDTM`."""
  level: int
  """Risk-limit level to switch to."""


Response = bool


class ModifyIsolatedMarginRiskLimit(RpcEndpoint):
  """Change the isolated-margin risk-limit level for an existing copy-trading position. Adjusting the level cancels any open orders on the symbol. Requires the API key's `LeadtradeFutures` permission."""

  async def modify_isolated_margin_risk_limit(
    self,
    symbol: str,
    *,
    level: int,
    validate: bool | None = None,
  ) -> Response:
    """Change the isolated-margin risk-limit level for an existing copy-trading position. Adjusting the level cancels any open orders on the symbol. Requires the API key's `LeadtradeFutures` permission.

    Args:
      symbol: Contract symbol, e.g. `XBTUSDTM`.
      level: Risk-limit level to switch to.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/copy-trading/modify-isolated-margin-risk-limit)
    """
    return await self.request(
      Request(symbol=symbol, level=level),
      method='POST',
      path='/api/v1/copy-trade/futures/position/risk-limit-level/change',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
