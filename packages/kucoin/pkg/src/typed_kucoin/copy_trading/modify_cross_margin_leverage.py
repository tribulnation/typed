# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /api/v2/copy-trade/futures/changeCrossUserLeverage` — Modify Cross Margin Leverage."""

from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint


class ModifyCopyTradeCrossMarginLeverageParams(TypedDict):
  """Symbol and target cross-margin leverage."""

  symbol: str
  """Symbol of the contract, e.g. `XBTUSDTM`."""
  leverage: str
  """Leverage multiple to set."""


_Type = bool
adapter = validator[_Type](_Type)  # type: ignore


class ModifyCrossMarginLeverage(RpcEndpoint):
  """`Modify Cross Margin Leverage` — mixed into `CopyTrading`, the product exposing `copy_trading.modify_cross_margin_leverage`."""

  async def modify_cross_margin_leverage(
    self,
    modify_copy_trade_cross_margin_leverage_params: ModifyCopyTradeCrossMarginLeverageParams,
    *,
    validate: bool | None = None,
  ) -> bool:
    """Modify the cross-margin leverage multiple set for a copy-trading futures symbol. Requires the API key's `LeadtradeFutures` permission.

    Args:
      modify_copy_trade_cross_margin_leverage_params: Symbol and target cross-margin leverage.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    return await self.authed_request(
      'POST',
      '/api/v2/copy-trade/futures/changeCrossUserLeverage',
      json=modify_copy_trade_cross_margin_leverage_params,
      validator=adapter,
      validate=validate,
    )
