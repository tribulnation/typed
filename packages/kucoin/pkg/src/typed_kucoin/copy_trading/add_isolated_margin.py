# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, TypedDict
from typed_kucoin.core import TimestampMillis
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class CopyTradeIsolatedMarginDepositResult(TypedDict):
  """A copy-trading futures position's detail as returned right after an isolated-margin deposit. Broader than `futures.positions.add_isolated_margin`'s equivalent `IsolatedMarginDepositResult` -- this variant additionally carries `marginMode`/`positionSide`/`leverage` (Hedge Mode fields Copy Trading exposes directly) and has no `userId`. Never called; field list and types are as rendered by the venue's docs page, unconfirmed live."""

  id: str
  """Position ID."""
  symbol: str
  """Symbol of the contract."""
  autoDeposit: bool
  """Whether margin is automatically deposited to avoid liquidation."""
  maintMarginReq: Decimal
  """Maintenance margin requirement."""
  riskLimit: int
  """Risk limit level."""
  realLeverage: Decimal
  """Leverage actually in effect for the position."""
  crossMode: bool
  """Whether this is a cross-margin position."""
  marginMode: Literal['ISOLATED', 'CROSS']
  """Margin mode. Presumably always `ISOLATED` for a result of adding isolated margin, but not confirmed live."""
  positionSide: Literal['LONG', 'SHORT']
  """Position side."""
  leverage: Decimal
  """Leverage the position is using."""
  delevPercentage: float
  """ADL ranking percentile."""
  openingTimestamp: TimestampMillis
  """First opening time, Unix milliseconds."""
  currentTimestamp: TimestampMillis
  """Current server timestamp, Unix milliseconds."""
  currentQty: int
  """Current position quantity."""
  currentCost: Decimal
  """Current position value."""
  currentComm: Decimal
  """Current commission."""
  unrealisedCost: Decimal
  """Unrealised value."""
  realisedGrossCost: Decimal
  """Accumulated realised gross profit value."""
  realisedCost: Decimal
  """Current realised position value."""
  isOpen: bool
  """Whether the position is currently open."""
  markPrice: Decimal
  """Mark price."""
  markValue: Decimal
  """Value at mark price."""
  posCost: Decimal
  """Position value."""
  posCross: Decimal
  """Added margin."""
  posInit: Decimal
  """Leverage margin."""
  posComm: Decimal
  """Bankruptcy cost."""
  posLoss: Decimal
  """Funding fees paid out."""
  posMargin: Decimal
  """Position margin."""
  posMaint: Decimal
  """Maintenance margin."""
  maintMargin: Decimal
  """Position margin."""
  realisedGrossPnl: Decimal
  """Accumulated realised gross profit value."""
  realisedPnl: Decimal
  """Realised profit and loss."""
  unrealisedPnl: Decimal
  """Unrealised profit and loss."""
  unrealisedPnlPcnt: Decimal
  """Profit-loss ratio of the position."""
  unrealisedRoePcnt: Decimal
  """Rate of return on investment."""
  avgEntryPrice: Decimal
  """Average entry price."""
  liquidationPrice: Decimal
  """Liquidation price."""
  bankruptPrice: Decimal
  """Bankruptcy price."""
  settleCurrency: str
  """Currency used to clear and settle the trades."""


class Request(TypedDict):
  """Symbol, position side, amount and idempotency key for an isolated-margin deposit."""

  symbol: str
  """Contract symbol, e.g. `XBTUSDTM`."""
  margin: float
  """Margin amount to add."""
  positionSide: Literal['LONG', 'SHORT']
  """Position side."""
  bizNo: str
  """Caller-generated unique id ensuring the deposit is processed only once."""


class AddIsolatedMargin(RpcEndpoint):
  """Manually add isolated margin to an existing copy-trading isolated position. Requires the API key's `LeadtradeFutures` permission."""

  async def add_isolated_margin(
    self,
    *,
    symbol: str,
    margin: float,
    position_side: Literal['LONG', 'SHORT'],
    biz_no: str,
    validate: bool | None = None,
  ) -> CopyTradeIsolatedMarginDepositResult:
    """Manually add isolated margin to an existing copy-trading isolated position. Requires the API key's `LeadtradeFutures` permission.

    Args:
      symbol: Contract symbol, e.g. `XBTUSDTM`.
      margin: Margin amount to add.
      position_side: Position side.
      biz_no: Caller-generated unique id ensuring the deposit is processed only once.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/copy-trading/add-isolated-margin)
    """
    return await self.request(
      Request(symbol=symbol, margin=margin, positionSide=position_side, bizNo=biz_no),
      method='POST',
      path='/api/v1/copy-trade/futures/position/margin/deposit-margin',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CopyTradeIsolatedMarginDepositResult,
    )
