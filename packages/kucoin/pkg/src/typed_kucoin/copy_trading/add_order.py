# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /api/v1/copy-trade/futures/orders` — Add Order."""

from typed_core.validation import validator
from typed_kucoin.types import (
  CopyTradeAddOrderLimit,
  CopyTradeAddOrderMarket,
  CopyTradeAddOrderResult,
)
from typed_kucoin.core import RpcEndpoint


_Type = CopyTradeAddOrderResult
adapter = validator[_Type](_Type)  # type: ignore


class AddOrder(RpcEndpoint):
  """`Add Order` — mixed into `CopyTrading`, the product exposing `copy_trading.add_order`."""

  async def add_order(
    self,
    body: CopyTradeAddOrderLimit | CopyTradeAddOrderMarket,
    *,
    validate: bool | None = None,
  ) -> CopyTradeAddOrderResult:
    """Place a limit or market copy-trading futures order. Requires the API key's `LeadtradeFutures` permission. Orders can only be placed if the account has sufficient funds; once placed, funds are held for the order's duration. Up to 100 limit orders and 200 stop orders may be open per contract, per account.

    Args:
      body: Order placement parameters.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    return await self.authed_request(
      'POST',
      '/api/v1/copy-trade/futures/orders',
      json=body,
      validator=adapter,
      validate=validate,
    )
