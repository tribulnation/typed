# Generated by typed-dev — see CONTRIBUTING.md.
from typed_kucoin.core.endpoint.rpc import RpcEndpoint
from typed_kucoin.schemas import (
  CopyTradeAddOrderLimit,
  CopyTradeAddOrderMarket,
  CopyTradeAddOrderResult,
)


Request = CopyTradeAddOrderLimit | CopyTradeAddOrderMarket


class AddOrder(RpcEndpoint):
  """Place a limit or market copy-trading futures order. Requires the API key's `LeadtradeFutures` permission. Orders can only be placed if the account has sufficient funds; once placed, funds are held for the order's duration. Up to 100 limit orders and 200 stop orders may be open per contract, per account."""

  async def add_order(
    self,
    request: Request,
    *,
    validate: bool | None = None,
  ) -> CopyTradeAddOrderResult:
    """Place a limit or market copy-trading futures order. Requires the API key's `LeadtradeFutures` permission. Orders can only be placed if the account has sufficient funds; once placed, funds are held for the order's duration. Up to 100 limit orders and 200 stop orders may be open per contract, per account.

    Args:
      request:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/copy-trading/add-order)
    """
    return await self.request(
      request,
      method='POST',
      path='/api/v1/copy-trade/futures/orders',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CopyTradeAddOrderResult,
    )
