# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /api/v1/copy-trade/futures/position/margin/withdraw-margin` — Remove Isolated Margin."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint


class RemoveCopyTradeIsolatedMarginParams(TypedDict):
  """Symbol, position side and amount for an isolated-margin withdrawal."""

  symbol: str
  """Contract symbol, e.g. `XBTUSDTM`."""
  positionSide: Literal['LONG', 'SHORT']
  """Position side."""
  withdrawAmount: float
  """Amount of margin to withdraw."""


_Type = str
adapter = validator[_Type](_Type)  # type: ignore


class RemoveIsolatedMargin(RpcEndpoint):
  """`Remove Isolated Margin` — mixed into `CopyTrading`, the product exposing `copy_trading.remove_isolated_margin`."""

  async def remove_isolated_margin(
    self,
    remove_copy_trade_isolated_margin_params: RemoveCopyTradeIsolatedMarginParams,
    *,
    validate: bool | None = None,
  ) -> str:
    """Manually remove isolated margin from an existing copy-trading isolated position. Requires the API key's `LeadtradeFutures` permission.

    Args:
      remove_copy_trade_isolated_margin_params: Symbol, side and amount to withdraw.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    return await self.authed_request(
      'POST',
      '/api/v1/copy-trade/futures/position/margin/withdraw-margin',
      json=remove_copy_trade_isolated_margin_params,
      validator=adapter,
      validate=validate,
    )
