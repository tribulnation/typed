# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  """Symbol, position side and amount for an isolated-margin withdrawal."""

  symbol: str
  """Contract symbol, e.g. `XBTUSDTM`."""
  positionSide: Literal['LONG', 'SHORT']
  """Position side."""
  withdrawAmount: float
  """Amount of margin to withdraw."""


Response = str


class RemoveIsolatedMargin(RpcEndpoint):
  """Manually remove isolated margin from an existing copy-trading isolated position. Requires the API key's `LeadtradeFutures` permission."""

  async def remove_isolated_margin(
    self,
    symbol: str,
    *,
    position_side: Literal['LONG', 'SHORT'],
    withdraw_amount: float,
    validate: bool | None = None,
  ) -> Response:
    """Manually remove isolated margin from an existing copy-trading isolated position. Requires the API key's `LeadtradeFutures` permission.

    Args:
      symbol: Contract symbol, e.g. `XBTUSDTM`.
      position_side: Position side.
      withdraw_amount: Amount of margin to withdraw.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/copy-trading/remove-isolated-margin)
    """
    return await self.request(
      Request(
        symbol=symbol, positionSide=position_side, withdrawAmount=withdraw_amount
      ),
      method='POST',
      path='/api/v1/copy-trade/futures/position/margin/withdraw-margin',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
