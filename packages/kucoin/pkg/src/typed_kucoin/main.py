# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .account import Account
from .affiliate import Affiliate
from .broker import Broker
from .convert import Convert
from .copy_trading import CopyTrading
from .earn import Earn
from .futures import Futures
from .margin import Margin
from .spot import Spot
from .streams import Streams
from .vip_lending import VipLending
from typed_kucoin.core.base import KuCoinBase


class KuCoin(KuCoinBase):
  """KuCoin's REST and WebSocket APIs -- Spot, Margin, Futures, Earn, VIP Lending, Convert, Broker, Affiliate, Copy Trading, and Classic WebSocket streams -- from one client.

  References:
    - [Upstream docs](https://www.kucoin.com/docs-new)
  """

  @cached_property
  def account(self) -> Account:
    """Account & Funding — account overviews and ledgers, sub-accounts and their API keys, deposits, withdrawals, trade fees, and internal (flex) transfers between account types.

    References:
      - [Upstream docs](https://www.kucoin.com/docs-new/rest/account-info/account-funding/get-account-summary-info)
    """
    return Account(client=self.default_client)

  @cached_property
  def affiliate(self) -> Affiliate:
    """KuCoin's Partner (affiliate) program — generate referral links, look up invited users, and read the commission/rebate history earned from those referrals' spot, margin, and futures trading.

    References:
      - [Upstream docs](https://www.kucoin.com/docs-new/rest/affiliate/introduction)
    """
    return Affiliate(client=self.default_client)

  @cached_property
  def broker(self) -> Broker:
    """KuCoin's broker programs — Broker Pro (third-party platforms invite traders and share in trading-fee commission) and Exchange Broker (a modular white-label exchange suite covering account architecture, order matching, fee models, liquidity, compliance, and risk controls).

    References:
      - [Upstream docs](https://www.kucoin.com/docs-new/rest/broker/introduction/main)
    """
    return Broker(client=self.broker_client)

  @cached_property
  def convert(self) -> Convert:
    """Convert — instant one-asset-for-another swaps priced outside the regular spot order book, via market and limit convert orders.

    References:
      - [Upstream docs](https://www.kucoin.com/docs-new/rest/convert/introduction)
    """
    return Convert(client=self.default_client)

  @cached_property
  def copy_trading(self) -> CopyTrading:
    """Copy trading — lead traders publish USDT-perpetual strategies that copy traders replicate proportionally or with a fixed margin allocation, splitting the resulting profit between the two.

    References:
      - [Upstream docs](https://www.kucoin.com/docs-new/rest/copy-trading/introduction)
    """
    return CopyTrading(client=self.futures_rest_client)

  @cached_property
  def earn(self) -> Earn:
    """Earn — savings, staking (ETH, KCS, and on-chain), dual investment, and structured products: browsing offered products, purchasing/redeeming a position, and reading the account's current holdings.

    References:
      - [Upstream docs](https://www.kucoin.com/docs-new/rest/earn/get-account-holding)
    """
    return Earn(client=self.default_client)

  @cached_property
  def futures(self) -> Futures:
    """KuCoin Futures — USDT-Margined (forward) contracts settled in USDT/USDC and Coin-Margined (inverse) contracts settled in the underlying cryptocurrency, covering market data, orders, positions, and funding fees.

    References:
      - [Upstream docs](https://www.kucoin.com/docs-new/rest/futures-trading/introduction)
    """
    return Futures(client=self.futures_rest_client)

  @cached_property
  def margin(self) -> Margin:
    """Margin Trading — cross/isolated margin market data, borrowing and repaying (debit), lending out assets for interest (credit), regular/high-frequency/OCO/stop order placement and management, and per-symbol risk limits.

    References:
      - [Upstream docs](https://www.kucoin.com/docs-new/rest/margin-trading/orders/add-order)
    """
    return Margin(client=self.default_client)

  @cached_property
  def spot(self) -> Spot:
    """Spot Trading — market data (symbols, order books, klines, tickers, currencies), and regular/high-frequency/OCO/stop order placement and management.

    References:
      - [Upstream docs](https://www.kucoin.com/docs-new/rest/spot-trading/orders/add-order)
    """
    return Spot(client=self.default_client)

  @cached_property
  def streams(self) -> Streams:
    """Streams — KuCoin's Classic WebSocket API: public/private push channels for futures, margin, and spot/margin market and account data, plus the separate Pro WebSocket order-placement connection (`trade_ws`).

    References:
      - [Upstream docs](https://www.kucoin.com/docs-new/websocket-api/base-info/introduction)
    """
    return Streams.new(
      self.spot_margin_stream_client, futures_stream_client=self.futures_stream_client
    )

  @cached_property
  def vip_lending(self) -> VipLending:
    """Institutional and VIP lending — loans KuCoin extends to institutional and VIP users at a chosen borrowing leverage, where both the posted margin and the borrowed assets can be used for cross/isolated margin trading.

    References:
      - [Upstream docs](https://www.kucoin.com/docs-new/rest/vip-lending/introduction)
    """
    return VipLending(client=self.default_client)
