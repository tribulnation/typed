# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /api/v1/earn/promotion/products` — Get Promotion Products."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint, TimestampMillis


class PromotionProductItem(TypedDict):
  id: str
  """Product id."""
  currency: str
  """Product currency, e.g. `BTC`."""
  category: Literal['ACTIVITY']
  """Product category -- promotion is the only one this endpoint offers."""
  type: Literal['TIME']
  """Product subtype. Every live promotion product observed is `TIME` (fixed-term) -- `DEMAND` is the sibling products' other value, not confirmed here."""
  originType: str
  """Undocumented field, absent from KuCoin's own reference -- every live promotion product observed carries `MULTI_TIME`. Left bare per rule 2: the venue never documents its closed set."""
  precision: int
  """Maximum decimal precision supported."""
  productUpperLimit: str
  """Total subscribable amount across every subscriber."""
  userUpperLimit: str
  """Maximum amount one user may subscribe."""
  userLowerLimit: str
  """Minimum amount one user may subscribe."""
  redeemPeriod: int
  """Redemption waiting period, in days."""
  lockStartTime: TimestampMillis
  """Earliest interest start time, Unix milliseconds."""
  lockEndTime: TimestampMillis
  """Product maturity time, Unix milliseconds."""
  applyStartTime: TimestampMillis
  """Subscription window start time, Unix milliseconds."""
  applyEndTime: TimestampMillis | None
  """Subscription window end time, Unix milliseconds, or `null` -- observed `null` on 61 of 79 live rows (undocumented; likely a promotion still accepting subscriptions with no fixed cutoff)."""
  returnRate: str
  """Annualized rate of return, e.g. `"0.35"` for 35%."""
  incomeCurrency: str
  """Currency interest is paid in."""
  earlyRedeemSupported: Literal[0, 1]
  """Whether early redemption is supported."""
  productRemainAmount: str
  """Remaining subscribable amount."""
  status: Literal['ONGOING', 'PENDING', 'FULL', 'INTERESTING']
  """Subscription status."""
  redeemType: Literal['MANUAL', 'TRANS_DEMAND', 'AUTO']
  """Redemption channel. Every live promotion product observed is `TRANS_DEMAND` (auto-transfers to a flexible/demand holding on redemption)."""
  incomeReleaseType: Literal['DAILY', 'AFTER']
  """Whether interest releases daily or only once the product ends."""
  interestDate: TimestampMillis
  """Most recent interest date, Unix milliseconds."""
  duration: int
  """Product duration, in days."""
  newUserOnly: Literal[0, 1]
  """Whether the product is restricted to new users."""


_Type = list[PromotionProductItem]
adapter = validator[_Type](_Type)  # type: ignore


class PromotionProducts(RpcEndpoint):
  """`Get Promotion Products` — mixed into `Earn`, the product exposing `earn.promotion_products`."""

  async def promotion_products(
    self,
    *,
    currency: str | None = None,
    validate: bool | None = None,
  ) -> list[PromotionProductItem]:
    """List the limited-duration promotional Earn products currently on offer. Returns an empty list if none are available.

    Args:
      currency: Restrict the response to one currency, e.g. `BTC`. Every currency's products are returned if omitted.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    params = {}
    if currency is not None:
      params['currency'] = currency
    return await self.authed_request(
      'GET',
      '/api/v1/earn/promotion/products',
      params=params,
      validator=adapter,
      validate=validate,
    )
