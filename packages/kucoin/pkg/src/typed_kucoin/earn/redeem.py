# Generated by typed-dev — see CONTRIBUTING.md.
"""`DELETE /api/v1/earn/orders` — Redeem."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint, TimestampMillis


class RedeemResult(TypedDict):
  orderTxId: str
  """Redemption order transaction id."""
  deliverTime: TimestampMillis
  """When redeemed funds would be delivered, Unix milliseconds."""
  status: Literal['SUCCESS', 'PENDING']
  """Redemption status."""
  amount: str
  """Amount redeemed."""


_Type = RedeemResult
adapter = validator[_Type](_Type)  # type: ignore


class Redeem(RpcEndpoint):
  """`Redeem` — mixed into `Earn`, the product exposing `earn.redeem`."""

  async def redeem(
    self,
    *,
    order_id: str,
    amount: str,
    from_account_type: Literal['MAIN', 'TRADE'] | None = None,
    confirm_punish_redeem: bool | None = None,
    validate: bool | None = None,
  ) -> RedeemResult:
    """Redeem an Earn holding by holding id, in full or in part. A holding that's already fully redeemed (or fully in the process of being redeemed) no longer resolves here.

    Args:
      order_id: Holding id, as returned by `earn.account_holding`'s `orderId`.
      amount: Amount to redeem.
      from_account_type: Source account type. Only meaningful for an ETH2 staking holding; irrelevant otherwise.
      confirm_punish_redeem: Confirmation that an early-redemption interest penalty (per `earn.redeem_preview`'s `penaltyInterestAmount`) is accepted. KuCoin's own rendered docs page types this `boolean`; the SDK's OpenAPI spec instead documents it as the literal string `"1"` -- never called, so this drift is recorded rather than resolved. Specced per the rendered docs page.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    params: dict = {
      'orderId': order_id,
      'amount': amount,
    }
    if from_account_type is not None:
      params['fromAccountType'] = from_account_type
    if confirm_punish_redeem is not None:
      params['confirmPunishRedeem'] = confirm_punish_redeem
    return await self.authed_request(
      'DELETE',
      '/api/v1/earn/orders',
      params=params,
      validator=adapter,
      validate=validate,
    )
