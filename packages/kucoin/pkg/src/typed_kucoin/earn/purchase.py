# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class PurchaseResult(TypedDict):
  orderId: str
  """New holding id -- the identifier used by `earn.redeem_preview`/`earn.redeem`/`earn.account_holding`."""
  orderTxId: str
  """Subscription order transaction id."""


class Request(TypedDict):
  productId: str
  """Product id to subscribe to, as returned by one of the `Get *Products` endpoints."""
  amount: Decimal
  """Amount to subscribe."""
  accountType: Literal['MAIN', 'TRADE']
  """Source account to draw funds from."""


class Purchase(RpcEndpoint):
  """Subscribe to an Earn product -- savings, promotion, staking, KCS-staking or ETH-staking -- moving funds from the given account into a new holding."""

  async def purchase(
    self,
    product_id: str,
    *,
    amount: Decimal,
    account_type: Literal['MAIN', 'TRADE'],
    validate: bool | None = None,
  ) -> PurchaseResult:
    """Subscribe to an Earn product -- savings, promotion, staking, KCS-staking or ETH-staking -- moving funds from the given account into a new holding.

    Args:
      product_id: Product id to subscribe to, as returned by one of the `Get *Products` endpoints.
      amount: Amount to subscribe.
      account_type: Source account to draw funds from.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/earn/purchase)
    """
    return await self.request(
      Request(productId=product_id, amount=amount, accountType=account_type),
      method='POST',
      path='/api/v1/earn/orders',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PurchaseResult,
    )
