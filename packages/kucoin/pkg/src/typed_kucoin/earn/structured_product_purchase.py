# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /api/v1/struct-earn/orders` — Structured Product Purchase."""

from typing_extensions import Literal
from typed_core.validation import TypedDict, validator
from typed_kucoin.core import RpcEndpoint


class StructuredProductPurchaseRequest(TypedDict):
  productId: str
  """Product id to subscribe to, as returned by `earn.dual_investment_products`."""
  investCurrency: str
  """Investment currency, e.g. `USDT`."""
  investAmount: str
  """Subscription amount."""
  accountType: Literal['MAIN', 'TRADE']
  """Source account to draw funds from: `MAIN` (funding account) or `TRADE` (spot trading account)."""


class StructuredProductPurchaseResult(TypedDict):
  orderId: str
  """New order (holding) id -- the identifier used by `earn.structured_product_orders`."""


_Type = StructuredProductPurchaseResult
adapter = validator[_Type](_Type)  # type: ignore


class StructuredProductPurchase(RpcEndpoint):
  """`Structured Product Purchase` — mixed into `Earn`, the product exposing `earn.structured_product_purchase`."""

  async def structured_product_purchase(
    self,
    structured_product_purchase_request: StructuredProductPurchaseRequest,
    *,
    validate: bool | None = None,
  ) -> StructuredProductPurchaseResult:
    """Subscribe to a Structured Earn - Dual product -- a dual-investment product settling in either the invested currency or its counterpart, depending on the strike price at expiry -- moving funds from the given account into a new order.

    Args:
      structured_product_purchase_request: Purchase parameters.
      validate: Validate the response against the generated schema.

    References:
      - [KuCoin API docs](https://www.kucoin.com/docs-new)
    """
    return await self.authed_request(
      'POST',
      '/api/v1/struct-earn/orders',
      json=structured_product_purchase_request,
      validator=adapter,
      validate=validate,
    )
