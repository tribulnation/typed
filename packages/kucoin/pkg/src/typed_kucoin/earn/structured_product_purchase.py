# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, TypedDict
from typed_kucoin.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  productId: str
  """Product id to subscribe to, as returned by `earn.dual_investment_products`."""
  investCurrency: str
  """Investment currency, e.g. `USDT`."""
  investAmount: Decimal
  """Subscription amount."""
  accountType: Literal['MAIN', 'TRADE']
  """Source account to draw funds from: `MAIN` (funding account) or `TRADE` (spot trading account)."""


class StructuredProductPurchaseResult(TypedDict):
  orderId: str
  """New order (holding) id -- the identifier used by `earn.structured_product_orders`."""


class StructuredProductPurchase(RpcEndpoint):
  """Subscribe to a Structured Earn - Dual product -- a dual-investment product settling in either the invested currency or its counterpart, depending on the strike price at expiry -- moving funds from the given account into a new order."""

  async def structured_product_purchase(
    self,
    *,
    product_id: str,
    invest_currency: str,
    invest_amount: Decimal,
    account_type: Literal['MAIN', 'TRADE'],
    validate: bool | None = None,
  ) -> StructuredProductPurchaseResult:
    """Subscribe to a Structured Earn - Dual product -- a dual-investment product settling in either the invested currency or its counterpart, depending on the strike price at expiry -- moving funds from the given account into a new order.

    Args:
      product_id: Product id to subscribe to, as returned by `earn.dual_investment_products`.
      invest_currency: Investment currency, e.g. `USDT`.
      invest_amount: Subscription amount.
      account_type: Source account to draw funds from: `MAIN` (funding account) or `TRADE` (spot trading account).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://www.kucoin.com/docs-new/rest/earn/structured-product-purchase)
    """
    return await self.request(
      Request(
        productId=product_id,
        investCurrency=invest_currency,
        investAmount=invest_amount,
        accountType=account_type,
      ),
      method='POST',
      path='/api/v1/struct-earn/orders',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=StructuredProductPurchaseResult,
    )
