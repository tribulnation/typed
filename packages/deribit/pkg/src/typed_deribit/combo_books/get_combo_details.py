# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import Combo


class Request(TypedDict):
  combo_id: str
  """Unique combo identifier."""


class GetComboDetails(RpcEndpoint):
  """Retrieves detailed information about a specific combo, including its leg structure, state, and other properties.

  Use `public/get_combo_ids` to get a list of available combo IDs."""

  async def get_combo_details(
    self,
    combo_id: str,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Combo:
    """Retrieves detailed information about a specific combo, including its leg structure, state, and other properties.

    Use `public/get_combo_ids` to get a list of available combo IDs.

    Args:
      combo_id: Unique combo identifier.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/combo-books/public-get_combo_details)
    """
    return await self.request(
      Request(combo_id=combo_id),
      method='POST',
      path='public/get_combo_details',
      meta={'public': True},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Combo,
    )
