# Generated by typed-dev — see CONTRIBUTING.md.
"""`public/get_combo_details` — `public/get_combo_details`."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core import RpcEndpoint, TimestampMillis
from typed_core.validation import validator


class ComboLeg(TypedDict):
  """One leg of a combo."""

  instrument_name: NotRequired[str]
  """Unique instrument identifier."""
  amount: NotRequired[int]
  """Size multiplier of this leg. A negative value indicates that trades on this leg run opposite to the combo trades they originate from."""


class Combo(TypedDict):
  """One combo book: a synthetic multi-leg instrument."""

  id: NotRequired[str]
  """Unique combo identifier."""
  instrument_id: NotRequired[int]
  """Instrument id."""
  state: NotRequired[Literal['active', 'inactive']]
  """Combo state: `"active"`, `"inactive"`."""
  state_timestamp: NotRequired[TimestampMillis]
  """When the combo last changed state (milliseconds since the Unix epoch)."""
  creation_timestamp: NotRequired[TimestampMillis]
  """When the combo was created (milliseconds since the Unix epoch)."""
  legs: NotRequired[list[ComboLeg]]
  """The combo's leg structure."""


validate_get_combo_details = validator[Combo](Combo)


class GetComboDetails(RpcEndpoint):
  """`public/get_combo_details`."""

  async def get_combo_details(
    self, *, combo_id: str, validate: bool | None = None
  ) -> Combo:
    """Retrieves detailed information about a specific combo, including its leg structure, state, and other properties.

    Use `public/get_combo_ids` to get a list of available combo IDs.

    Args:
      combo_id: Combo ID
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/combo-books/public-get_combo_details)
    """
    params: dict = {
      'combo_id': combo_id,
    }
    return await self.request(
      'public/get_combo_details',
      params=params,
      validator=validate_get_combo_details,
      validate=validate,
    )
