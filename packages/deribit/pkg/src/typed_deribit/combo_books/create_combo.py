# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import Combo, TradeLegRequest


class Request(TypedDict):
  trades: list[TradeLegRequest]
  """The combo's leg structure, as a list of trades."""


class CreateCombo(RpcEndpoint):
  """Verifies and creates a combo book or returns an existing combo matching the given trades. Combos allow trading on multiple instruments (futures and options) simultaneously as a single strategy.

  If a combo matching the provided trades already exists, this method returns the existing combo. Otherwise, it creates a new combo book with the specified leg structure."""

  async def create_combo(
    self,
    trades: list[TradeLegRequest],
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Combo:
    """Verifies and creates a combo book or returns an existing combo matching the given trades. Combos allow trading on multiple instruments (futures and options) simultaneously as a single strategy.

    If a combo matching the provided trades already exists, this method returns the existing combo. Otherwise, it creates a new combo book with the specified leg structure.

    Args:
      trades: The combo's leg structure, as a list of trades.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/combo-books/private-create_combo)
    """
    return await self.request(
      Request(trades=trades),
      method='POST',
      path='private/create_combo',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Combo,
    )
