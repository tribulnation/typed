# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import BlockRfqLegPrice, TradeLegRequest


class GetLegPricesResult(TypedDict):
  """Individual leg prices decomposed from the requested aggregate price."""

  price: NotRequired[float]
  """The requested aggregate price, echoed back. Not documented on the venue's own response schema; observed on every live call."""
  legs: NotRequired[list[BlockRfqLegPrice]]
  """One computed price per requested leg."""
  amount: NotRequired[float]
  """This value multiplied by the ratio of a leg gives the trade size on that leg."""


class Request(TypedDict):
  legs: list[TradeLegRequest]
  """The leg structure to decompose a price for."""
  price: float
  """Aggregate price for the whole leg structure, to be decomposed across the individual legs."""


class GetLegPrices(RpcEndpoint):
  """Returns individual leg prices for a given combo structure based on an aggregated price of the strategy and the mark prices of the individual legs.

  Leg prices change dynamically with mark price fluctuations, and the algorithm is calibrated only for conventional option structures and future spreads. This method supports both inverse strategies and known linear structures within a single currency pair."""

  async def get_leg_prices(
    self,
    legs: list[TradeLegRequest],
    *,
    price: float,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> GetLegPricesResult:
    """Returns individual leg prices for a given combo structure based on an aggregated price of the strategy and the mark prices of the individual legs.

    Leg prices change dynamically with mark price fluctuations, and the algorithm is calibrated only for conventional option structures and future spreads. This method supports both inverse strategies and known linear structures within a single currency pair.

    Args:
      legs: The leg structure to decompose a price for.
      price: Aggregate price for the whole leg structure, to be decomposed across the individual legs.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/combo-books/private-get_leg_prices)
    """
    return await self.request(
      Request(legs=legs, price=price),
      method='POST',
      path='private/get_leg_prices',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=GetLegPricesResult,
    )
