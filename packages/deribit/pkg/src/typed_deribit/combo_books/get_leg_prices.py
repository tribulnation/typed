# Generated by typed-dev — see CONTRIBUTING.md.
"""`private/get_leg_prices` — `private/get_leg_prices`."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_deribit.core import RpcEndpoint


class LegPrice(TypedDict):
  """The computed price for one leg."""

  ratio: NotRequired[int]
  """Ratio of amount between legs."""
  instrument_name: NotRequired[str]
  """Unique instrument identifier."""
  direction: NotRequired[Literal['buy', 'sell']]
  """Direction: `buy`, or `sell`."""
  price: NotRequired[float]
  """Computed price for this leg."""


class LegPricesLeg(TypedDict):
  """One leg of the structure."""

  instrument_name: str
  """Instrument name."""
  amount: float
  """It represents the requested trade size. For perpetual and inverse futures the amount is in USD units. For options and linear futures it is the underlying base currency coin."""
  direction: Literal['buy', 'sell']
  """Direction of this leg."""


class GetLegPricesResult(TypedDict):
  """Individual leg prices decomposed from the requested aggregate price."""

  price: NotRequired[float]
  """The requested aggregate price, echoed back. Not documented on the venue's own response schema; observed on every live call."""
  legs: NotRequired[list[LegPrice]]
  """One computed price per requested leg."""
  amount: NotRequired[float]
  """This value multiplied by the ratio of a leg gives the trade size on that leg."""


validate_get_leg_prices = validator[GetLegPricesResult](GetLegPricesResult)


class GetLegPrices(RpcEndpoint):
  """`private/get_leg_prices`."""

  async def get_leg_prices(
    self,
    *,
    legs: list[LegPricesLeg],
    price: float,
    validate: bool | None = None,
  ) -> GetLegPricesResult:
    """Returns individual leg prices for a given combo structure based on an aggregated price of the strategy and the mark prices of the individual legs.

    Leg prices change dynamically with mark price fluctuations, and the algorithm is calibrated only for conventional option structures and future spreads. This method supports both inverse strategies and known linear structures within a single currency pair.

    Args:
      legs: List of legs for which the prices will be calculated
      price: Price for the whole leg structure
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/combo-books/private-get_leg_prices)
    """
    params: dict = {
      'legs': legs,
      'price': price,
    }
    return await self.authed_request(
      'private/get_leg_prices',
      params=params,
      validator=validate_get_leg_prices,
      validate=validate,
    )
