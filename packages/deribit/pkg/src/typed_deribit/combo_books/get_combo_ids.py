# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR']
  """Currency, i.e `"BTC"`, `"ETH"`, `"USDC"`"""
  state: NotRequired[Literal['active', 'inactive']]
  """Combo state: `"active"`, `"inactive"`"""


Response = list[str]


class GetComboIds(RpcEndpoint):
  """Retrieves available combo IDs. This method can be used to get the list of all combos, or only the list of combos in the given state.

  Use `public/get_combo_details` to retrieve detailed information about a specific combo."""

  async def get_combo_ids(
    self,
    currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR'],
    *,
    state: Literal['active', 'inactive'] | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Retrieves available combo IDs. This method can be used to get the list of all combos, or only the list of combos in the given state.

    Use `public/get_combo_details` to retrieve detailed information about a specific combo.

    Args:
      currency: Currency, i.e `"BTC"`, `"ETH"`, `"USDC"`
      state: Combo state: `"active"`, `"inactive"`
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/combo-books/public-get_combo_ids)
    """
    request: Request = Request(currency=currency)
    if state is not None:
      request['state'] = state
    return await self.request(
      request,
      method='POST',
      path='public/get_combo_ids',
      meta={'public': True},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Response,
    )
