# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, cast
from typed_deribit.core.endpoint.rpc import RpcEndpoint


Response = Literal['ok']


class AuthedUnsubscribeAll(RpcEndpoint):
  """Unsubscribe from all the channels subscribed so far on this connection. This method takes no parameters."""

  async def authed_unsubscribe_all(self, *, validate: bool | None = None) -> Response:
    """Unsubscribe from all the channels subscribed so far on this connection. This method takes no parameters.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/subscription-management/private-unsubscribe_all)
    """
    return await self.request(
      None,
      path='private/unsubscribe_all',
      meta={},
      validate=validate,
      response_type=cast(type, Response),
    )
