# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, cast
from typed_deribit.core.endpoint.rpc import RpcEndpoint


Response = Literal['ok']


class UnsubscribeAll(RpcEndpoint):
  """Unsubscribe from all the channels subscribed so far on this connection. This method takes no parameters."""

  async def unsubscribe_all(self, *, validate: bool | None = None) -> Response:
    """Unsubscribe from all the channels subscribed so far on this connection. This method takes no parameters.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/subscription-management/public-unsubscribe_all)
    """
    return await self.request(
      None,
      path='public/unsubscribe_all',
      meta={'public': True},
      validate=validate,
      response_type=cast(type, Response),
    )
