# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core import TimestampMillis
from typed_deribit.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  instrument_name: str
  """Unique instrument identifier"""
  start_timestamp: TimestampMillis
  """The timestamp (milliseconds since the Unix epoch)"""
  end_timestamp: TimestampMillis
  """The timestamp (milliseconds since the Unix epoch)"""
  resolution: Literal[
    '1', '3', '5', '10', '15', '30', '60', '120', '180', '360', '720', '1D'
  ]
  """Chart bars resolution given in full minutes or keyword `1D` (only some specific resolutions are supported). The wire value is a string (`"60"`, per this endpoint's own live-captured `examples/01.request.json`), not a bare number -- the enum entries were previously typed as JSON numbers, contradicting the parameter's own declared `type: string` and rejecting the recorded example."""


class TradingviewChartData(TypedDict):
  """OHLCV candles formatted for TradingView charting."""

  status: NotRequired[Literal['ok', 'no_data']]
  """Status of the query: `ok` or `no_data`"""
  ticks: NotRequired[list[int]]
  """Values of the time axis given in milliseconds since UNIX epoch"""
  volume: NotRequired[list[float]]
  """List of volume bars (in base currency, one per candle)"""
  cost: NotRequired[list[float]]
  """List of cost bars (volume in quote currency, one per candle)"""
  open: NotRequired[list[float]]
  """List of prices at open (one per candle)"""
  close: NotRequired[list[float]]
  """List of prices at close (one per candle)"""
  high: NotRequired[list[float]]
  """List of highest price levels (one per candle)"""
  low: NotRequired[list[float]]
  """List of lowest price levels (one per candle)"""


class GetTradingviewChartData(RpcEndpoint):
  """Retrieves publicly available market data formatted for generating TradingView-compatible candle charts. The data includes open, high, low, close (OHLC) prices and volume for specified time intervals.

  Use the `chart_resolution` parameter to specify the candle interval (e.g., 1m, 5m, 1h, 1d). This method provides the standard format used by TradingView and other charting platforms."""

  async def get_tradingview_chart_data(
    self,
    instrument_name: str,
    *,
    start_timestamp: TimestampMillis,
    end_timestamp: TimestampMillis,
    resolution: Literal[
      '1', '3', '5', '10', '15', '30', '60', '120', '180', '360', '720', '1D'
    ],
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> TradingviewChartData:
    """Retrieves publicly available market data formatted for generating TradingView-compatible candle charts. The data includes open, high, low, close (OHLC) prices and volume for specified time intervals.

    Use the `chart_resolution` parameter to specify the candle interval (e.g., 1m, 5m, 1h, 1d). This method provides the standard format used by TradingView and other charting platforms.

    Args:
      instrument_name: Unique instrument identifier
      start_timestamp: The timestamp (milliseconds since the Unix epoch)
      end_timestamp: The timestamp (milliseconds since the Unix epoch)
      resolution: Chart bars resolution given in full minutes or keyword `1D` (only some specific resolutions are supported). The wire value is a string (`"60"`, per this endpoint's own live-captured `examples/01.request.json`), not a bare number -- the enum entries were previously typed as JSON numbers, contradicting the parameter's own declared `type: string` and rejecting the recorded example.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/market-data/public-get_tradingview_chart_data)
    """
    return await self.request(
      Request(
        instrument_name=instrument_name,
        start_timestamp=start_timestamp,
        end_timestamp=end_timestamp,
        resolution=resolution,
      ),
      method='POST',
      path='public/get_tradingview_chart_data',
      meta={'public': True},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=TradingviewChartData,
    )
