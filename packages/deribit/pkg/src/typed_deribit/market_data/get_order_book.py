# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import OrderBookSnapshot


class Request(TypedDict):
  instrument_name: str
  """The instrument name for which to retrieve the order book, see [`public/get_instruments`](#public-get_instruments) to obtain instrument names."""
  depth: NotRequired[Literal[1, 5, 10, 20, 50, 100, 1000, 10000]]
  """The number of entries to return for bids and asks, maximum - `10000`."""


class GetOrderBook(RpcEndpoint):
  """Retrieves the order book (bids and asks) for a given instrument, along with other market values such as best bid/ask prices, last trade price, mark price, and index price.

  The order book depth can be controlled using the `depth` parameter, which accepts values from 1 to 10000. The response includes price levels sorted by price (bids descending, asks ascending)."""

  async def get_order_book(
    self,
    instrument_name: str,
    *,
    depth: Literal[1, 5, 10, 20, 50, 100, 1000, 10000] | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> OrderBookSnapshot:
    """Retrieves the order book (bids and asks) for a given instrument, along with other market values such as best bid/ask prices, last trade price, mark price, and index price.

    The order book depth can be controlled using the `depth` parameter, which accepts values from 1 to 10000. The response includes price levels sorted by price (bids descending, asks ascending).

    Args:
      instrument_name: The instrument name for which to retrieve the order book, see [`public/get_instruments`](#public-get_instruments) to obtain instrument names.
      depth: The number of entries to return for bids and asks, maximum - `10000`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/market-data/public-get_order_book)
    """
    request: Request = Request(instrument_name=instrument_name)
    if depth is not None:
      request['depth'] = depth
    return await self.request(
      request,
      method='POST',
      path='public/get_order_book',
      meta={'public': True},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=OrderBookSnapshot,
    )
