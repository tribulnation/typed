# Generated by typed-dev — see CONTRIBUTING.md.
"""`public/get_supported_index_names` — `public/get_supported_index_names`."""

from typing_extensions import Literal
from typed_core.validation import validator
from typed_deribit.core import RpcEndpoint

validate_get_supported_index_names = validator[list[str]](list[str])


class GetSupportedIndexNames(RpcEndpoint):
  """`public/get_supported_index_names`."""

  async def get_supported_index_names(
    self,
    *,
    type: Literal['all', 'spot', 'derivative'] | None = None,
    validate: bool | None = None,
  ) -> list[str]:
    """Retrieves the identifiers (names) of all supported price indexes, optionally filtered by index type. Price indexes are reference prices used for mark price calculations, settlement, and other market operations.

    Use the `type` parameter to filter indexes by type (e.g., spot, futures, etc.). This method helps discover available indexes for use with other API methods.

    Args:
      type: Type of a cryptocurrency price index
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/market-data/public-get_supported_index_names)
    """
    params = {}
    if type is not None:
      params['type'] = type
    return await self.request(
      'public/get_supported_index_names',
      params=params,
      validator=validate_get_supported_index_names,
      validate=validate,
    )
