# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import InstrumentInfo


class Request(TypedDict):
  instrument_name: str
  """Unique instrument identifier"""


class GetInstrument(RpcEndpoint):
  """Retrieves detailed information about a specific instrument, including instrument specifications, contract details, tick size, settlement currency, expiration date (for futures and options), strike price (for options), underlying type, and other instrument parameters.

  This method is useful for obtaining instrument metadata needed for trading operations and calculations."""

  async def get_instrument(
    self,
    instrument_name: str,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> InstrumentInfo:
    """Retrieves detailed information about a specific instrument, including instrument specifications, contract details, tick size, settlement currency, expiration date (for futures and options), strike price (for options), underlying type, and other instrument parameters.

    This method is useful for obtaining instrument metadata needed for trading operations and calculations.

    Args:
      instrument_name: Unique instrument identifier
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/market-data/public-get_instrument)
    """
    return await self.request(
      Request(instrument_name=instrument_name),
      method='POST',
      path='public/get_instrument',
      meta={'public': True},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=InstrumentInfo,
    )
