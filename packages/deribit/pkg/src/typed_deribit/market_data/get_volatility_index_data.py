# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, Sequence, TypedDict
from typed_deribit.core import TimestampMillis
from typed_deribit.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  currency: str
  """Currency, i.e `"BTC"`, `"ETH"`, `"USDC"`"""
  start_timestamp: TimestampMillis
  """The timestamp (milliseconds since the Unix epoch)"""
  end_timestamp: TimestampMillis
  """The timestamp (milliseconds since the Unix epoch)"""
  resolution: Literal['1', '60', '3600', '43200', '1D']
  """Time resolution given in full seconds or keyword `1D` (only some specific resolutions are supported). The wire value is a string (`"3600"`, per this endpoint's own live-captured `examples/01.request.json`), not a bare number -- the enum entries were previously typed as JSON numbers, contradicting the parameter's own declared `type: string` and rejecting the recorded example."""


class VolatilityIndexData(TypedDict):
  """A page of volatility index candles."""

  data: NotRequired[list[tuple[int, float, float, float, float]]]
  """Candles for this page, ascending by timestamp."""
  continuation: NotRequired[TimestampMillis | None]
  """Continuation cursor -- `null` once the requested range is fully covered."""


class GetVolatilityIndexData(RpcEndpoint):
  """Retrieves volatility index (VIX) chart data formatted as candles. Volatility indexes measure market expectations of future volatility and are useful for risk assessment and trading strategies.

  Use the `vix_resolution` parameter to specify the candle interval. The data shows historical volatility index values over time and is formatted for use in charting applications."""

  def get_volatility_index_data_paged(
    self,
    currency: str,
    *,
    start_timestamp: TimestampMillis,
    end_timestamp: TimestampMillis,
    resolution: Literal['1', '60', '3600', '43200', '1D'],
    transport: Literal['http', 'ws'] = 'http',
    validate: bool | None = None,
  ) -> PaginatedResponse[tuple[int, float, float, float, float], TimestampMillis]:
    """Retrieves volatility index (VIX) chart data formatted as candles. Volatility indexes measure market expectations of future volatility and are useful for risk assessment and trading strategies.

    Use the `vix_resolution` parameter to specify the candle interval. The data shows historical volatility index values over time and is formatted for use in charting applications.

    Paged variant of `get_volatility_index_data`: Passes each page's token back as `end_timestamp` and stops when a response carries no `continuation`. Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      currency: Currency, i.e `"BTC"`, `"ETH"`, `"USDC"`
      start_timestamp: The timestamp (milliseconds since the Unix epoch)
      end_timestamp: The timestamp (milliseconds since the Unix epoch)
      resolution: Time resolution given in full seconds or keyword `1D` (only some specific resolutions are supported). The wire value is a string (`"3600"`, per this endpoint's own live-captured `examples/01.request.json`), not a bare number -- the enum entries were previously typed as JSON numbers, contradicting the parameter's own declared `type: string` and rejecting the recorded example.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/market-data/public-get_volatility_index_data)
    """

    async def next(
      end_timestamp: TimestampMillis,
    ) -> tuple[
      Sequence[tuple[int, float, float, float, float]], TimestampMillis | None
    ]:
      response = await self.get_volatility_index_data(
        currency,
        start_timestamp=start_timestamp,
        end_timestamp=(end_timestamp or None),
        resolution=resolution,
        transport=transport,
        validate=validate,
      )
      rows = response.get('data')
      rows = rows if rows is not None else []
      state = response.get('continuation')
      return rows, state or None

    return PaginatedResponse(end_timestamp, next)

  async def get_volatility_index_data(
    self,
    currency: str,
    *,
    start_timestamp: TimestampMillis,
    end_timestamp: TimestampMillis,
    resolution: Literal['1', '60', '3600', '43200', '1D'],
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> VolatilityIndexData:
    """Retrieves volatility index (VIX) chart data formatted as candles. Volatility indexes measure market expectations of future volatility and are useful for risk assessment and trading strategies.

    Use the `vix_resolution` parameter to specify the candle interval. The data shows historical volatility index values over time and is formatted for use in charting applications.

    Args:
      currency: Currency, i.e `"BTC"`, `"ETH"`, `"USDC"`
      start_timestamp: The timestamp (milliseconds since the Unix epoch)
      end_timestamp: The timestamp (milliseconds since the Unix epoch)
      resolution: Time resolution given in full seconds or keyword `1D` (only some specific resolutions are supported). The wire value is a string (`"3600"`, per this endpoint's own live-captured `examples/01.request.json`), not a bare number -- the enum entries were previously typed as JSON numbers, contradicting the parameter's own declared `type: string` and rejecting the recorded example.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/market-data/public-get_volatility_index_data)
    """
    return await self.request(
      Request(
        currency=currency,
        start_timestamp=start_timestamp,
        end_timestamp=end_timestamp,
        resolution=resolution,
      ),
      method='POST',
      path='public/get_volatility_index_data',
      meta={'public': True},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=VolatilityIndexData,
    )
