# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import AsyncIterator, Literal, TypedDict
from typed_deribit.core import TimestampMillis
from typed_deribit.core.endpoint.rpc import RpcEndpoint


MarkPricePoint = list[tuple[float, float]]


class Request(TypedDict):
  instrument_name: str
  """Unique instrument identifier"""
  start_timestamp: TimestampMillis
  """The timestamp (milliseconds since the Unix epoch)"""
  end_timestamp: TimestampMillis
  """The timestamp (milliseconds since the Unix epoch)"""


class GetMarkPriceHistory(RpcEndpoint):
  """Retrieves 5-minute historical mark price data for an instrument. Mark prices are used for margin calculations and position valuations.

  **Note:** Currently, mark price history is available only for a subset of options that participate in volatility index calculations. All other instruments, including futures and perpetuals, will return an empty list."""

  async def get_mark_price_history_paged(
    self,
    instrument_name: str,
    *,
    start_timestamp: TimestampMillis,
    end_timestamp: TimestampMillis,
    transport: Literal['http', 'ws'] = 'http',
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[MarkPricePoint]:
    """Retrieves 5-minute historical mark price data for an instrument. Mark prices are used for margin calculations and position valuations.

    **Note:** Currently, mark price history is available only for a subset of options that participate in volatility index calculations. All other instruments, including futures and perpetuals, will return an empty list.

    Paged variant of `get_mark_price_history`: Moves the `start_timestamp`–`end_timestamp` window forwards by its own width and stops on the first empty window, or after `max_pages` pages when one is given.

    Every request spans the width the caller's own `start_timestamp` and `end_timestamp` state, so choose a window the venue answers in one response: it caps a wider one, and the walk moves past the rows that were left out.

    Args:
      instrument_name: Unique instrument identifier
      start_timestamp: The timestamp (milliseconds since the Unix epoch)
      end_timestamp: The timestamp (milliseconds since the Unix epoch)
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/market-data/public-get_mark_price_history)
    """
    lower = start_timestamp
    upper = end_timestamp
    width = upper - lower
    pages = 0
    while True:
      response = await self.get_mark_price_history(
        instrument_name,
        start_timestamp=lower,
        end_timestamp=upper,
        transport=transport,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      if not response:
        break
      lower = upper
      upper = lower + width

  async def get_mark_price_history(
    self,
    instrument_name: str,
    *,
    start_timestamp: TimestampMillis,
    end_timestamp: TimestampMillis,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> MarkPricePoint:
    """Retrieves 5-minute historical mark price data for an instrument. Mark prices are used for margin calculations and position valuations.

    **Note:** Currently, mark price history is available only for a subset of options that participate in volatility index calculations. All other instruments, including futures and perpetuals, will return an empty list.

    Args:
      instrument_name: Unique instrument identifier
      start_timestamp: The timestamp (milliseconds since the Unix epoch)
      end_timestamp: The timestamp (milliseconds since the Unix epoch)
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/market-data/public-get_mark_price_history)
    """
    return await self.request(
      Request(
        instrument_name=instrument_name,
        start_timestamp=start_timestamp,
        end_timestamp=end_timestamp,
      ),
      method='POST',
      path='public/get_mark_price_history',
      meta={'public': True},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=MarkPricePoint,
    )
