# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core import TimestampMillis
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import TradesPage


class Request(TypedDict):
  currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR']
  """Currency, i.e `"BTC"`, `"ETH"`, `"USDC"`"""
  kind: NotRequired[
    Literal['future', 'option', 'spot', 'future_combo', 'option_combo', 'combo', 'any']
  ]
  """Instrument kind: `"future"`, `"option"`, `"spot"`, `"future_combo"`, `"option_combo"`, `"combo"` for any combo or `"any"` for all"""
  start_id: NotRequired[str]
  """Unique (per currency) trade identifier"""
  end_id: NotRequired[str]
  """Unique (per currency) trade identifier"""
  start_timestamp: NotRequired[TimestampMillis]
  """The timestamp (milliseconds since the Unix epoch)"""
  end_timestamp: NotRequired[TimestampMillis]
  """The timestamp (milliseconds since the Unix epoch)"""
  count: NotRequired[int]
  """Number of requested items, default - `10`, maximum - `1000`"""
  sorting: NotRequired[Literal['asc', 'desc', 'default']]
  """Direction of results sorting (`default` value means no sorting, results will be returned in order in which they left the database)"""


class GetLastTradesByCurrency(RpcEndpoint):
  """Retrieves the latest trades that have occurred for instruments in a specific currency. Returns trade details including price, amount, direction, timestamp, and trade ID for all instruments in the currency.

  Results can be filtered by instrument kind and trade ID range or timestamp range. Use the `count` parameter to limit the number of trades returned, and `sorting` to control the order (ascending or descending by trade ID)."""

  async def get_last_trades_by_currency(
    self,
    currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR'],
    *,
    kind: Literal[
      'future', 'option', 'spot', 'future_combo', 'option_combo', 'combo', 'any'
    ]
    | None = None,
    start_id: str | None = None,
    end_id: str | None = None,
    start_timestamp: TimestampMillis | None = None,
    end_timestamp: TimestampMillis | None = None,
    count: int | None = None,
    sorting: Literal['asc', 'desc', 'default'] | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> TradesPage:
    """Retrieves the latest trades that have occurred for instruments in a specific currency. Returns trade details including price, amount, direction, timestamp, and trade ID for all instruments in the currency.

    Results can be filtered by instrument kind and trade ID range or timestamp range. Use the `count` parameter to limit the number of trades returned, and `sorting` to control the order (ascending or descending by trade ID).

    Args:
      currency: Currency, i.e `"BTC"`, `"ETH"`, `"USDC"`
      kind: Instrument kind: `"future"`, `"option"`, `"spot"`, `"future_combo"`, `"option_combo"`, `"combo"` for any combo or `"any"` for all
      start_id: Unique (per currency) trade identifier
      end_id: Unique (per currency) trade identifier
      start_timestamp: The timestamp (milliseconds since the Unix epoch)
      end_timestamp: The timestamp (milliseconds since the Unix epoch)
      count: Number of requested items, default - `10`, maximum - `1000`
      sorting: Direction of results sorting (`default` value means no sorting, results will be returned in order in which they left the database)
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/market-data/public-get_last_trades_by_currency)
    """
    request: Request = Request(currency=currency)
    if kind is not None:
      request['kind'] = kind
    if start_id is not None:
      request['start_id'] = start_id
    if end_id is not None:
      request['end_id'] = end_id
    if start_timestamp is not None:
      request['start_timestamp'] = start_timestamp
    if end_timestamp is not None:
      request['end_timestamp'] = end_timestamp
    if count is not None:
      request['count'] = count
    if sorting is not None:
      request['sorting'] = sorting
    return await self.request(
      request,
      method='POST',
      path='public/get_last_trades_by_currency',
      meta={'public': True},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=TradesPage,
    )
