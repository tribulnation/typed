# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint


Expirations = dict[str, list[str]] | dict[str, dict[str, list[str]]]


class Request(TypedDict):
  currency: str
  """Currency name or `"any"` if don't care or `"grouped"` if grouped by currencies."""
  kind: Literal['future', 'option', 'any']
  """Instrument kind: `"future"`, `"option"` or `"any"` for all"""
  currency_pair: NotRequired[str]
  """Index identifier, matches (base) cryptocurrency with quote currency. The full, live list is `get_index_price_names`."""


class GetExpirations(RpcEndpoint):
  """Retrieves all available expiration timestamps for instruments. This method can be used to discover which expiration dates are available for trading, which is useful for finding instruments with specific expiration dates.

  Results can be filtered by settlement currency, instrument kind (future or option), and currency pair. The response includes expiration timestamps in milliseconds since the UNIX epoch."""

  async def get_expirations(
    self,
    *,
    currency: str,
    kind: Literal['future', 'option', 'any'],
    currency_pair: str | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Expirations:
    """Retrieves all available expiration timestamps for instruments. This method can be used to discover which expiration dates are available for trading, which is useful for finding instruments with specific expiration dates.

    Results can be filtered by settlement currency, instrument kind (future or option), and currency pair. The response includes expiration timestamps in milliseconds since the UNIX epoch.

    Args:
      currency: Currency name or `"any"` if don't care or `"grouped"` if grouped by currencies.
      kind: Instrument kind: `"future"`, `"option"` or `"any"` for all
      currency_pair: Index identifier, matches (base) cryptocurrency with quote currency. The full, live list is `get_index_price_names`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/market-data/public-get_expirations)
    """
    request: Request = Request(currency=currency, kind=kind)
    if currency_pair is not None:
      request['currency_pair'] = currency_pair
    return await self.request(
      request,
      method='POST',
      path='public/get_expirations',
      meta={'public': True},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Expirations,
    )
