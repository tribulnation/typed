# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import BookSummary


class Request(TypedDict):
  instrument_name: str
  """Unique instrument identifier"""


BookSummaryResponse = list[BookSummary]


class GetBookSummaryByInstrument(RpcEndpoint):
  """Retrieves summary information such as open interest, 24-hour volume, best bid/ask prices, last trade price, mark price, and other market statistics for a specific instrument.

  This method provides a quick overview of current market activity and liquidity for a single instrument."""

  async def get_book_summary_by_instrument(
    self,
    instrument_name: str,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> BookSummaryResponse:
    """Retrieves summary information such as open interest, 24-hour volume, best bid/ask prices, last trade price, mark price, and other market statistics for a specific instrument.

    This method provides a quick overview of current market activity and liquidity for a single instrument.

    Args:
      instrument_name: Unique instrument identifier
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/market-data/public-get_book_summary_by_instrument)
    """
    return await self.request(
      Request(instrument_name=instrument_name),
      method='POST',
      path='public/get_book_summary_by_instrument',
      meta={'public': True},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=BookSummaryResponse,
    )
