# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_deribit.core import TimestampMillis
from typed_deribit.core.endpoint.rpc import RpcEndpoint


class FundingChartPoint(TypedDict):
  """One funding chart data point."""

  timestamp: TimestampMillis
  """The timestamp (milliseconds since the Unix epoch)"""
  index_price: float
  """Current index price"""
  interest_8h: float
  """Historical interest 8h value"""


class Request(TypedDict):
  instrument_name: str
  """Unique instrument identifier"""
  length: Literal['8h', '24h', '1m']
  """Specifies time period. `8h` - 8 hours, `24h` - 24 hours, `1m` - 1 month"""


class FundingChartData(TypedDict):
  """Funding rate chart data for one PERPETUAL instrument."""

  current_interest: float
  """Current interest"""
  interest_8h: float
  """Current interest 8h"""
  data: list[FundingChartPoint]
  """Chart points, ascending by timestamp."""


class GetFundingChartData(RpcEndpoint):
  """Retrieves funding rate chart data points for a PERPETUAL instrument within a given time period. The data is formatted for use in charting applications and includes funding rate values at regular intervals.

  Use the `length` parameter to specify the time period for which to retrieve chart data. This method is useful for visualizing funding rate trends over time."""

  async def get_funding_chart_data(
    self,
    instrument_name: str,
    *,
    length: Literal['8h', '24h', '1m'],
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> FundingChartData:
    """Retrieves funding rate chart data points for a PERPETUAL instrument within a given time period. The data is formatted for use in charting applications and includes funding rate values at regular intervals.

    Use the `length` parameter to specify the time period for which to retrieve chart data. This method is useful for visualizing funding rate trends over time.

    Args:
      instrument_name: Unique instrument identifier
      length: Specifies time period. `8h` - 8 hours, `24h` - 24 hours, `1m` - 1 month
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/market-data/public-get_funding_chart_data)
    """
    return await self.request(
      Request(instrument_name=instrument_name, length=length),
      method='POST',
      path='public/get_funding_chart_data',
      meta={'public': True},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=FundingChartData,
    )
