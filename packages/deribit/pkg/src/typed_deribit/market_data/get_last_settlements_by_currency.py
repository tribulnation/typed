# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core import TimestampMillis
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import Settlement, SettlementsPage


class Request(TypedDict):
  currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR']
  """Currency, i.e `"BTC"`, `"ETH"`, `"USDC"`"""
  type: NotRequired[Literal['settlement', 'delivery', 'bankruptcy']]
  """The type of settlement. `settlement`, `delivery` or `bankruptcy`."""
  count: NotRequired[int]
  """Number of requested items, default - `20`, maximum - `1000`"""
  continuation: NotRequired[str]
  """Continuation token for pagination"""
  search_start_timestamp: NotRequired[TimestampMillis]
  """The timestamp (milliseconds since the Unix epoch)"""


class GetLastSettlementsByCurrency(RpcEndpoint):
  """Retrieves historical settlement, delivery, and bankruptcy events from all instruments within a given currency. Settlements occur when futures or options contracts expire and are settled at the delivery price.

  Results can be filtered by settlement type and timestamp. Use pagination parameters (`count` and `continuation`) to retrieve large settlement histories. This data is useful for analyzing historical contract settlements and understanding market events.

  **Note on `profit_loss` and `session_profit_loss`:** Because this is a public endpoint, these fields are **platform-wide aggregates**, not per-account values. `profit_loss` is the sum of realized P&L of all position holders at the settlement price. `session_profit_loss` is the sum of each holder's total session P&L (realized + unrealized) across all participants."""

  def get_last_settlements_by_currency_paged(
    self,
    currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR'],
    *,
    type: Literal['settlement', 'delivery', 'bankruptcy'] | None = None,
    count: int | None = None,
    search_start_timestamp: TimestampMillis | None = None,
    transport: Literal['http', 'ws'] = 'http',
    validate: bool | None = None,
  ) -> PaginatedResponse[Settlement, str]:
    """Retrieves historical settlement, delivery, and bankruptcy events from all instruments within a given currency. Settlements occur when futures or options contracts expire and are settled at the delivery price.

    Results can be filtered by settlement type and timestamp. Use pagination parameters (`count` and `continuation`) to retrieve large settlement histories. This data is useful for analyzing historical contract settlements and understanding market events.

    **Note on `profit_loss` and `session_profit_loss`:** Because this is a public endpoint, these fields are **platform-wide aggregates**, not per-account values. `profit_loss` is the sum of realized P&L of all position holders at the settlement price. `session_profit_loss` is the sum of each holder's total session P&L (realized + unrealized) across all participants.

    Paged variant of `get_last_settlements_by_currency`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      currency: Currency, i.e `"BTC"`, `"ETH"`, `"USDC"`
      type: The type of settlement. `settlement`, `delivery` or `bankruptcy`.
      count: Number of requested items, default - `20`, maximum - `1000`
      search_start_timestamp: The timestamp (milliseconds since the Unix epoch)
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/market-data/public-get_last_settlements_by_currency)
    """

    async def next(continuation: str) -> tuple[list[Settlement], str | None]:
      response = await self.get_last_settlements_by_currency(
        currency,
        type=type,
        count=count,
        search_start_timestamp=search_start_timestamp,
        transport=transport,
        continuation=(continuation or None),
        validate=validate,
      )
      rows = response.get('settlements')
      state = response.get('continuation')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def get_last_settlements_by_currency(
    self,
    currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR'],
    *,
    type: Literal['settlement', 'delivery', 'bankruptcy'] | None = None,
    count: int | None = None,
    continuation: str | None = None,
    search_start_timestamp: TimestampMillis | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> SettlementsPage:
    """Retrieves historical settlement, delivery, and bankruptcy events from all instruments within a given currency. Settlements occur when futures or options contracts expire and are settled at the delivery price.

    Results can be filtered by settlement type and timestamp. Use pagination parameters (`count` and `continuation`) to retrieve large settlement histories. This data is useful for analyzing historical contract settlements and understanding market events.

    **Note on `profit_loss` and `session_profit_loss`:** Because this is a public endpoint, these fields are **platform-wide aggregates**, not per-account values. `profit_loss` is the sum of realized P&L of all position holders at the settlement price. `session_profit_loss` is the sum of each holder's total session P&L (realized + unrealized) across all participants.

    Args:
      currency: Currency, i.e `"BTC"`, `"ETH"`, `"USDC"`
      type: The type of settlement. `settlement`, `delivery` or `bankruptcy`.
      count: Number of requested items, default - `20`, maximum - `1000`
      continuation: Continuation token for pagination
      search_start_timestamp: The timestamp (milliseconds since the Unix epoch)
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/market-data/public-get_last_settlements_by_currency)
    """
    request: Request = Request(currency=currency)
    if type is not None:
      request['type'] = type
    if count is not None:
      request['count'] = count
    if continuation is not None:
      request['continuation'] = continuation
    if search_start_timestamp is not None:
      request['search_start_timestamp'] = search_start_timestamp
    return await self.request(
      request,
      method='POST',
      path='public/get_last_settlements_by_currency',
      meta={'public': True},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=SettlementsPage,
    )
