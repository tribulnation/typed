# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint


IndexChartPoint = list[tuple[float, float]]


class Request(TypedDict):
  index_name: str
  """Index identifier, matches (base) cryptocurrency with quote currency. The full, live list is `get_index_price_names`."""
  range: Literal['1h', '1d', '2d', '1m', '1y', 'all']
  """Range of the data to return"""


class GetIndexChartData(RpcEndpoint):
  """Returns historical price index chart data for the specified index name and time range. The data is formatted for use in charting applications and shows price index values over time.

  Use the `range` parameter to specify the time period for which to retrieve chart data. This method is useful for visualizing price index trends and historical movements."""

  async def get_index_chart_data(
    self,
    index_name: str,
    *,
    range: Literal['1h', '1d', '2d', '1m', '1y', 'all'],
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> IndexChartPoint:
    """Returns historical price index chart data for the specified index name and time range. The data is formatted for use in charting applications and shows price index values over time.

    Use the `range` parameter to specify the time period for which to retrieve chart data. This method is useful for visualizing price index trends and historical movements.

    Args:
      index_name: Index identifier, matches (base) cryptocurrency with quote currency. The full, live list is `get_index_price_names`.
      range: Range of the data to return
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/market-data/public-get_index_chart_data)
    """
    return await self.request(
      Request(index_name=index_name, range=range),
      method='POST',
      path='public/get_index_chart_data',
      meta={'public': True},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=IndexChartPoint,
    )
