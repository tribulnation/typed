# Generated by typed-dev — see CONTRIBUTING.md.
"""`public/get_currencies` — `public/get_currencies`."""

from typing_extensions import NotRequired, TypedDict
from typed_core.validation import validator
from typed_deribit.core import RpcEndpoint


class CoinbaseNetwork(TypedDict):
  """One Coinbase-routed network option."""

  display_name: NotRequired[str]
  """Human-readable network name, e.g. `Ripple`."""
  resource_name: NotRequired[str]
  """Coinbase resource identifier for the network."""


class WithdrawalPriority(TypedDict):
  """One withdrawal priority tier."""

  name: str
  """Priority tier name, e.g. `very_low`, `very_high`."""
  value: float
  """Relative withdrawal fee multiplier for this tier."""


class CurrencyItem(TypedDict):
  """One cryptocurrency this venue supports."""

  withdrawal_fee: float
  """The total transaction fee paid for withdrawals"""
  withdrawal_priorities: NotRequired[list[WithdrawalPriority]]
  """Withdrawal priority tiers available for this currency, fastest/most expensive first."""
  min_withdrawal_fee: NotRequired[float]
  """The minimum transaction fee paid for withdrawals"""
  currency: str
  """The abbreviation of the currency. This abbreviation is used elsewhere in the API to identify the currency."""
  currency_long: str
  """The full name for the currency."""
  min_confirmations: int
  """Minimum number of block chain confirmations before deposit is accepted."""
  coin_type: str
  """The type of the currency."""
  in_cross_collateral_pool: bool
  """`true` if the currency is part of the cross collateral pool"""
  apr: NotRequired[float]
  """Simple Moving Average (SMA) of the last 7 days of rewards. If fewer than 7 days of reward data are available, the APR is calculated as the average of the available rewards. Only applicable to yield-generating tokens (`USDE`, `STETH`, `USDC`, `BUILD`)."""
  network_fee: NotRequired[float]
  """The network fee"""
  network_currency: NotRequired[str]
  """The currency of the network"""
  decimals: NotRequired[int]
  """The number of decimal places for the currency"""
  currency_uuid: NotRequired[str]
  """Internal identifier for this currency. Absent if the currency does not have an assigned identifier."""
  coinbase_networks: NotRequired[list[CoinbaseNetwork]]
  """Coinbase network options available for this currency's deposits/withdrawals. Observed live; not documented in the OpenAPI schema."""
  onchain_operations_precision: NotRequired[int]
  """Decimal precision used for on-chain deposit/withdrawal amounts. Observed live; not documented in the OpenAPI schema."""


validate_get_currencies = validator[list[CurrencyItem]](list[CurrencyItem])


class GetCurrencies(RpcEndpoint):
  """`public/get_currencies`."""

  async def get_currencies(self, *, validate: bool | None = None) -> list[CurrencyItem]:
    """Retrieves all cryptocurrencies supported by the Deribit API. Returns a list of available currencies with their codes and basic information.

    This method takes no parameters and is useful for discovering which currencies are available for trading on the platform.

    Args:
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/market-data/public-get_currencies)
    """
    return await self.request(
      'public/get_currencies', validator=validate_get_currencies, validate=validate
    )
