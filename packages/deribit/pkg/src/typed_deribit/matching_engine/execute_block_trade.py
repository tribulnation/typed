# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core import TimestampMillis
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import TradeLeg, UserTrade


class ExecuteBlockTradeResult(TypedDict):
  id: str
  """Block trade id"""
  timestamp: TimestampMillis
  """The timestamp (milliseconds since the Unix epoch)"""
  trades: list[UserTrade]
  """List of user trades created by executing the block trade"""
  app_name: NotRequired[str]
  """The name of the application that executed the block trade on behalf of the user (optional)."""
  broker_code: NotRequired[str]
  """Broker code associated with the broker block trade."""
  broker_name: NotRequired[str]
  """Name of the broker associated with the block trade."""


class Request(TypedDict):
  timestamp: TimestampMillis
  """The timestamp (milliseconds since the Unix epoch)"""
  nonce: str
  """Nonce"""
  role: Literal['maker', 'taker']
  """Trade role of the user: `maker` or `taker`"""
  trades: list[TradeLeg]
  """List of trades for block trade. Must match the trades array agreed with the counterparty in `private/verify_block_trade`."""
  counterparty_signature: str
  """Signature of block trade. Valid only for 5 minutes around the given timestamp."""


class ExecuteBlockTrade(RpcEndpoint):
  """Executes a block trade. This is the second step in the block trade workflow -- the second party calls this method with the signature received from the first party (`private/verify_block_trade`) to execute the trade.

  The whole request must be exactly the same as in `private/verify_block_trade`, only the `role` field should be set appropriately -- both sides have to agree on the same `timestamp`, `nonce`, and `trades` fields, and the server ensures the `role` field is different between sides (each party accepts their own role).

  Using the same `timestamp` and `nonce` by both sides ensures that even if unintentionally both sides execute the given block trade with a valid `counterparty_signature`, the block trade will be executed only once.

  In the API, the `direction` field is always expressed from the maker's perspective: when accepting a block trade as a taker, the direction shown represents the opposite side of the actual trade.

  Scope: `block_trade:read_write`."""

  async def execute_block_trade(
    self,
    timestamp: TimestampMillis,
    *,
    nonce: str,
    role: Literal['maker', 'taker'],
    trades: list[TradeLeg],
    counterparty_signature: str,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> ExecuteBlockTradeResult:
    """Executes a block trade. This is the second step in the block trade workflow -- the second party calls this method with the signature received from the first party (`private/verify_block_trade`) to execute the trade.

    The whole request must be exactly the same as in `private/verify_block_trade`, only the `role` field should be set appropriately -- both sides have to agree on the same `timestamp`, `nonce`, and `trades` fields, and the server ensures the `role` field is different between sides (each party accepts their own role).

    Using the same `timestamp` and `nonce` by both sides ensures that even if unintentionally both sides execute the given block trade with a valid `counterparty_signature`, the block trade will be executed only once.

    In the API, the `direction` field is always expressed from the maker's perspective: when accepting a block trade as a taker, the direction shown represents the opposite side of the actual trade.

    Scope: `block_trade:read_write`.

    Args:
      timestamp: The timestamp (milliseconds since the Unix epoch)
      nonce: Nonce
      role: Trade role of the user: `maker` or `taker`
      trades: List of trades for block trade. Must match the trades array agreed with the counterparty in `private/verify_block_trade`.
      counterparty_signature: Signature of block trade. Valid only for 5 minutes around the given timestamp.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/block-trade/private-execute_block_trade)
    """
    return await self.request(
      Request(
        timestamp=timestamp,
        nonce=nonce,
        role=role,
        trades=trades,
        counterparty_signature=counterparty_signature,
      ),
      method='POST',
      path='private/execute_block_trade',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=ExecuteBlockTradeResult,
    )
