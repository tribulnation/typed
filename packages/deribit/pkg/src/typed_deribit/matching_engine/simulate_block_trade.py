# Generated by typed-dev — see CONTRIBUTING.md.
"""`private/simulate_block_trade` — `private/simulate_block_trade`."""

from typing_extensions import Literal, TypedDict
from typed_core.validation import validator
from typed_deribit.core import RpcEndpoint


class SimulateBlockTradeLeg(TypedDict):
  instrument_name: str
  """Unique instrument identifier"""
  price: float
  """Price for the trade"""
  amount: float
  """It represents the requested trade size. For perpetual and inverse futures the amount is in USD units. For options and linear futures it is the underlying base currency coin."""
  direction: Literal['buy', 'sell']
  """Direction of the trade, from the maker's perspective"""


validate_simulate_block_trade = validator[bool](bool)


class SimulateBlockTrade(RpcEndpoint):
  """`private/simulate_block_trade`."""

  async def simulate_block_trade(
    self,
    *,
    role: Literal['maker', 'taker'] | None = None,
    trades: list[SimulateBlockTradeLeg],
    validate: bool | None = None,
  ) -> bool:
    """Checks if a block trade can be executed without actually executing it. Use this method to verify that a block trade will succeed before proceeding with the actual execution.

    Scope: `block_trade:read`.

    Args:
      role: Describes if user wants to be maker or taker of trades
      trades: List of trades for block trade
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/block-trade/private-simulate_block_trade)
    """
    params: dict = {
      'trades': trades,
    }
    if role is not None:
      params['role'] = role
    return await self.authed_request(
      'private/simulate_block_trade',
      params=params,
      validator=validate_simulate_block_trade,
      validate=validate,
    )
