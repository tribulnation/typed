# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import TradeLeg


Response = bool


class Request(TypedDict):
  role: NotRequired[Literal['maker', 'taker']]
  """Trade role of the user: `maker` or `taker`"""
  trades: list[TradeLeg]
  """List of trades for block trade"""


class SimulateBlockTrade(RpcEndpoint):
  """Checks if a block trade can be executed without actually executing it. Use this method to verify that a block trade will succeed before proceeding with the actual execution.

  Scope: `block_trade:read`."""

  async def simulate_block_trade(
    self,
    role: Literal['maker', 'taker'] | None = None,
    *,
    trades: list[TradeLeg],
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Checks if a block trade can be executed without actually executing it. Use this method to verify that a block trade will succeed before proceeding with the actual execution.

    Scope: `block_trade:read`.

    Args:
      role: Trade role of the user: `maker` or `taker`
      trades: List of trades for block trade
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/block-trade/private-simulate_block_trade)
    """
    request: Request = Request(trades=trades)
    if role is not None:
      request['role'] = role
    return await self.request(
      request,
      method='POST',
      path='private/simulate_block_trade',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Response,
    )
