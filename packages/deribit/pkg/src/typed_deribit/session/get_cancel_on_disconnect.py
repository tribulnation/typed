# Generated by typed-dev — see CONTRIBUTING.md.
"""`private/get_cancel_on_disconnect` — `private/get_cancel_on_disconnect`."""

from typing_extensions import Literal, TypedDict
from typed_core.validation import validator
from typed_deribit.core import RpcEndpoint


class CancelOnDisconnectStatus(TypedDict):
  """Cancel On Disconnect status for the queried scope."""

  scope: Literal['connection', 'account']
  """Whether this status reflects the current connection or the whole account -- echoes back the request's `scope` (or its `connection` default)."""
  enabled: bool
  """Whether Cancel On Disconnect is currently active for the queried scope."""


validate_get_cancel_on_disconnect = validator[CancelOnDisconnectStatus](
  CancelOnDisconnectStatus
)


class GetCancelOnDisconnect(RpcEndpoint):
  """`private/get_cancel_on_disconnect`."""

  async def get_cancel_on_disconnect(
    self,
    *,
    scope: Literal['connection', 'account'] | None = None,
    validate: bool | None = None,
  ) -> CancelOnDisconnectStatus:
    """Read the current Cancel On Disconnect configuration for the account. Requires `account:read` scope.

    Args:
      scope: Whether to read the current connection's setting or the account-wide default. Defaults to `connection`. `connection` scope is accepted over WebSocket only -- confirmed live, an HTTP attempt with `scope=connection` fails with error 10030 (`must_be_websocket_request`); `account` scope works over either transport.
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/session-management/private-get_cancel_on_disconnect)
    """
    params = {}
    if scope is not None:
      params['scope'] = scope
    return await self.authed_request(
      'private/get_cancel_on_disconnect',
      params=params,
      validator=validate_get_cancel_on_disconnect,
      validate=validate,
    )
