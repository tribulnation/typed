# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, cast
from typed_deribit.core.endpoint.rpc import RpcEndpoint


Response = Literal['ok']


class DisableHeartbeat(RpcEndpoint):
  """Stop sending heartbeat messages on this WebSocket connection."""

  async def disable_heartbeat(self, *, validate: bool | None = None) -> Response:
    """Stop sending heartbeat messages on this WebSocket connection.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/session-management/public-disable_heartbeat)
    """
    return await self.request(
      None,
      path='public/disable_heartbeat',
      meta={'public': True},
      validate=validate,
      response_type=cast(type, Response),
    )
