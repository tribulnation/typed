# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core import TimestampMillis
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import BrokerTradeRequestParty


class BrokerTradeRequestLeg(TypedDict):
  """One leg of a broker block trade request."""

  amount: NotRequired[float]
  """Trade amount."""
  direction: NotRequired[Literal['buy', 'sell']]
  """Trade direction."""
  price: NotRequired[float]
  """Trade price."""
  instrument_name: NotRequired[str]
  """Name of the traded instrument."""


class BrokerTradeRequest(TypedDict):
  """One broker block trade request."""

  timestamp: NotRequired[TimestampMillis]
  """Timestamp of the broker block trade request (milliseconds since the UNIX epoch)."""
  state: NotRequired[str]
  """State of the broker block trade request."""
  trades: NotRequired[list[BrokerTradeRequestLeg]]
  """The legs of the broker block trade request."""
  maker: NotRequired[BrokerTradeRequestParty]
  taker: NotRequired[BrokerTradeRequestParty]
  nonce: NotRequired[str]
  """Nonce for approving or rejecting the broker block trade request."""


class BrokerTradeRequests(TypedDict):
  """Broker block trade requests for the broker account."""

  requests: list[BrokerTradeRequest]
  """Pending, declined, and expired broker block trade requests."""


class GetBrokerTradeRequests(RpcEndpoint):
  """Broker Method. Provides a list of broker block trade requests including pending approvals, declined trades, and expired trades. `timestamp` and `nonce` received in response can be used to approve or reject the pending broker block trade. This method takes no parameters.

  Scope: `block_trade:read`."""

  async def get_broker_trade_requests(
    self,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> BrokerTradeRequests:
    """Broker Method. Provides a list of broker block trade requests including pending approvals, declined trades, and expired trades. `timestamp` and `nonce` received in response can be used to approve or reject the pending broker block trade. This method takes no parameters.

    Scope: `block_trade:read`.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/block-trade/private-get_broker_trade_requests)
    """
    return await self.request(
      None,
      method='POST',
      path='private/get_broker_trade_requests',
      meta={},
      validate=validate,
      transport=transport,
      response_type=BrokerTradeRequests,
    )
