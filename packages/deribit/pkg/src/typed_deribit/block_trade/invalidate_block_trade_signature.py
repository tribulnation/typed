# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict, cast
from typed_deribit.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  signature: str
  """Signature of block trade. It is valid only for 5 minutes around given timestamp"""


Response = Literal['ok']


class InvalidateBlockTradeSignature(RpcEndpoint):
  """Invalidates a block trade signature, effectively cancelling the block trade. This can be called at any time before `private/execute_block_trade` is called.

  Scope: `block_trade:read_write`."""

  async def invalidate_block_trade_signature(
    self,
    signature: str,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Invalidates a block trade signature, effectively cancelling the block trade. This can be called at any time before `private/execute_block_trade` is called.

    Scope: `block_trade:read_write`.

    Args:
      signature: Signature of block trade. It is valid only for 5 minutes around given timestamp
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/block-trade/private-invalidate_block_trade_signature)
    """
    return await self.request(
      Request(signature=signature),
      method='POST',
      path='private/invalidate_block_trade_signature',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=cast(type, Response),
    )
