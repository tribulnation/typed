# Generated by typed-dev — see CONTRIBUTING.md.
"""`private/invalidate_block_trade_signature` — `private/invalidate_block_trade_signature`."""

from typing_extensions import Literal
from typed_core.validation import validator
from typed_deribit.core import RpcEndpoint

validate_invalidate_block_trade_signature = validator[Literal['ok']](Literal['ok'])  # pyright: ignore[reportArgumentType]  # fmt: skip


class InvalidateBlockTradeSignature(RpcEndpoint):
  """`private/invalidate_block_trade_signature`."""

  async def invalidate_block_trade_signature(
    self,
    *,
    signature: str,
    validate: bool | None = None,
  ) -> Literal['ok']:
    """Invalidates a block trade signature, effectively cancelling the block trade. This can be called at any time before `private/execute_block_trade` is called.

    Scope: `block_trade:read_write`.

    Args:
      signature: Signature of block trade that will be invalidated
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/block-trade/private-invalidate_block_trade_signature)
    """
    params: dict = {
      'signature': signature,
    }
    return await self.authed_request(
      'private/invalidate_block_trade_signature',
      params=params,
      validator=validate_invalidate_block_trade_signature,
      validate=validate,
    )
