# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import BlockTrade


class Request(TypedDict):
  id: str
  """Block trade id"""


class GetBlockTrade(RpcEndpoint):
  """Returns information about a specific block trade identified by `block_trade_id`.

  Scope: `block_trade:read`."""

  async def get_block_trade(
    self,
    id: str,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> BlockTrade:
    """Returns information about a specific block trade identified by `block_trade_id`.

    Scope: `block_trade:read`.

    Args:
      id: Block trade id
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/block-trade/private-get_block_trade)
    """
    return await self.request(
      Request(id=id),
      method='POST',
      path='private/get_block_trade',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=BlockTrade,
    )
