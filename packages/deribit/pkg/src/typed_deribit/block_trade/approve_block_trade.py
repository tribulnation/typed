# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict, cast
from typed_deribit.core import TimestampMillis
from typed_deribit.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  timestamp: TimestampMillis
  """The timestamp (milliseconds since the Unix epoch)"""
  nonce: str
  """Nonce"""
  role: Literal['maker', 'taker']
  """Trade role of the user: `maker` or `taker`"""


Response = Literal['ok']


class ApproveBlockTrade(RpcEndpoint):
  """Approves a pending block trade. `nonce` and `timestamp` are used to identify the block trade while `role` should be opposite to the trading counterparty.

  Use `private/get_block_trade_requests` to retrieve pending block trades that require approval.

  To use the block trade approval feature, an additional API key setting feature called `enabled_features: block_trade_approval` is required. This key has to be given to the broker/registered partner who performs the trades on behalf of the user for the feature to be active. If the user wants to approve the trade, they must approve it from a different API key that doesn't have this feature enabled.

  Scope: `block_trade:read_write`."""

  async def approve_block_trade(
    self,
    timestamp: TimestampMillis,
    *,
    nonce: str,
    role: Literal['maker', 'taker'],
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Approves a pending block trade. `nonce` and `timestamp` are used to identify the block trade while `role` should be opposite to the trading counterparty.

    Use `private/get_block_trade_requests` to retrieve pending block trades that require approval.

    To use the block trade approval feature, an additional API key setting feature called `enabled_features: block_trade_approval` is required. This key has to be given to the broker/registered partner who performs the trades on behalf of the user for the feature to be active. If the user wants to approve the trade, they must approve it from a different API key that doesn't have this feature enabled.

    Scope: `block_trade:read_write`.

    Args:
      timestamp: The timestamp (milliseconds since the Unix epoch)
      nonce: Nonce
      role: Trade role of the user: `maker` or `taker`
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/block-trade/private-approve_block_trade)
    """
    return await self.request(
      Request(timestamp=timestamp, nonce=nonce, role=role),
      method='POST',
      path='private/approve_block_trade',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=cast(type, Response),
    )
