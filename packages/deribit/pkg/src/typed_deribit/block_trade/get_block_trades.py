# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import BlockTrade


class Request(TypedDict):
  currency: NotRequired[Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR']]
  """Currency, i.e `"BTC"`, `"ETH"`, `"USDC"`"""
  count: NotRequired[int]
  """Count of Block Trades returned, maximum - `101`"""
  start_id: NotRequired[str]
  """Block trade id"""
  end_id: NotRequired[str]
  """Block trade id"""
  block_rfq_id: NotRequired[int]
  """ID of the Block RFQ"""
  broker_code: NotRequired[str]
  """Broker code to filter block trades. Only broker clients can use `broker_code` to filter broker block trades. Use `any` for all block trades."""


BlockTradeResponse = list[BlockTrade]


class GetBlockTrades(RpcEndpoint):
  """Returns a list of the user's block trades. If currency is not provided, returns block trades for all currencies. `block_rfq_id` can be provided to receive block trades related to that particular Block RFQ.

  Scope: `block_trade:read`."""

  async def get_block_trades(
    self,
    currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR'] | None = None,
    *,
    count: int | None = None,
    start_id: str | None = None,
    end_id: str | None = None,
    block_rfq_id: int | None = None,
    broker_code: str | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> BlockTradeResponse:
    """Returns a list of the user's block trades. If currency is not provided, returns block trades for all currencies. `block_rfq_id` can be provided to receive block trades related to that particular Block RFQ.

    Scope: `block_trade:read`.

    Args:
      currency: Currency, i.e `"BTC"`, `"ETH"`, `"USDC"`
      count: Count of Block Trades returned, maximum - `101`
      start_id: Block trade id
      end_id: Block trade id
      block_rfq_id: ID of the Block RFQ
      broker_code: Broker code to filter block trades. Only broker clients can use `broker_code` to filter broker block trades. Use `any` for all block trades.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/block-trade/private-get_block_trades)
    """
    request: Request = Request()
    if currency is not None:
      request['currency'] = currency
    if count is not None:
      request['count'] = count
    if start_id is not None:
      request['start_id'] = start_id
    if end_id is not None:
      request['end_id'] = end_id
    if block_rfq_id is not None:
      request['block_rfq_id'] = block_rfq_id
    if broker_code is not None:
      request['broker_code'] = broker_code
    return await self.request(
      request,
      method='POST',
      path='private/get_block_trades',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=BlockTradeResponse,
    )
