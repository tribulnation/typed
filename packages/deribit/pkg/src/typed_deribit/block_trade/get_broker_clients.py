# Generated by typed-dev — see CONTRIBUTING.md.
"""`private/get_broker_clients` — `private/get_broker_clients`."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_deribit.core import RpcEndpoint


class BrokerClientLink(TypedDict):
  """One individual user connection within a broker client group."""

  client_link_id: int
  """Numeric identifier for an individual user within the client group. Used together with `client_id` in Broker Voice API calls."""
  user_id: str
  """Obscured user ID of the linked user."""
  name: str
  """Display name for this link."""
  note: NotRequired[str]
  """Optional note attached to this link."""
  state: Literal['connected', 'pending', 'rejected']
  """Connection state of the link: `connected` (user accepted the invitation and is active), `pending` (invitation sent, not yet accepted), or `rejected` (link is inactive)."""
  confirmations_required: int
  """Number of broker confirmations required for block trades involving this link."""
  broker_sub_user_id: NotRequired[int]
  """ID of the broker sub-account managing this link, if applicable."""


class BrokerClientItem(TypedDict):
  """One client group registered under the broker account."""

  client_id: int
  """Numeric identifier for the client group. Used in Broker Voice API calls to reference this client."""
  client_name: str
  """Name of the client."""
  company_name: str
  """Company name of the client."""
  broker_code: str
  """Broker-assigned code used in invitation links for this client."""
  main_user_id: NotRequired[str]
  """Obscured user ID of the main account linked to this client, if any."""
  broker_sub_user_id: NotRequired[int]
  """ID of the broker sub-account that manages this client, if applicable."""
  lei: NotRequired[str]
  """Legal Entity Identifier (LEI) of the client, if provided."""
  brn: NotRequired[str]
  """Business Registration Number (BRN) of the client, if provided."""
  links: list[BrokerClientLink]
  """Array of individual user connections belonging to this client."""


validate_get_broker_clients = validator[list[BrokerClientItem]](list[BrokerClientItem])


class GetBrokerClients(RpcEndpoint):
  """`private/get_broker_clients`."""

  async def get_broker_clients(
    self,
    *,
    client_id: int | None = None,
    include_subaccounts: bool | None = None,
    validate: bool | None = None,
  ) -> list[BrokerClientItem]:
    """Broker Method. Returns a list of clients registered under the broker account. Each client record includes `client_id` (a numeric identifier grouping one or more linked users under a single client name, used in other Broker Voice API calls) and `links` (an array of individual user connections belonging to that client, each carrying a `client_link_id` that uniquely identifies a single user within the client group).

    Call this endpoint without parameters to retrieve all clients. A link `state` of `connected` means the user has accepted the broker invitation and is ready to trade; `pending` links have not yet been accepted; `rejected` links are inactive.

    Optionally filter by a specific `client_id`, or set `include_subaccounts` to `true` to include clients managed by broker sub-accounts.

    Scope: `block_trade:read`.

    Args:
      client_id: If provided, returns only the client with this ID. Omit to return all clients.
      include_subaccounts: If `true`, includes clients managed by broker sub-accounts. Default: `false`.
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/block-trade/private-get_broker_clients)
    """
    params = {}
    if client_id is not None:
      params['client_id'] = client_id
    if include_subaccounts is not None:
      params['include_subaccounts'] = include_subaccounts
    return await self.authed_request(
      'private/get_broker_clients',
      params=params,
      validator=validate_get_broker_clients,
      validate=validate,
    )
