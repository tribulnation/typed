# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import TokenResult


class Request(TypedDict):
  refresh_token: str
  """Refresh token from an earlier public/auth (or exchange_token/fork_token) call."""
  subject_id: int
  """The subaccount id to act as. Must differ from the caller's current subject id."""
  scope: NotRequired[str]
  """Optional scope override for the new session; cannot exceed the caller's own permissions. Supports `session` scope for direct session creation during token exchange."""


class ExchangeToken(RpcEndpoint):
  """Generate a token for a new subject id, to switch between subaccounts without a full re-authentication."""

  async def exchange_token(
    self,
    *,
    refresh_token: str,
    subject_id: int,
    scope: str | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> TokenResult:
    """Generate a token for a new subject id, to switch between subaccounts without a full re-authentication.

    Args:
      refresh_token: Refresh token from an earlier public/auth (or exchange_token/fork_token) call.
      subject_id: The subaccount id to act as. Must differ from the caller's current subject id.
      scope: Optional scope override for the new session; cannot exceed the caller's own permissions. Supports `session` scope for direct session creation during token exchange.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/authentication/public-exchange_token)
    """
    request: Request = Request(refresh_token=refresh_token, subject_id=subject_id)
    if scope is not None:
      request['scope'] = scope
    return await self.request(
      request,
      method='POST',
      path='public/exchange_token',
      meta={'public': True},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=TokenResult,
    )
