# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import TokenResult


class Request(TypedDict):
  refresh_token: str
  """Refresh token from an earlier public/auth call, scoped session:name."""
  session_name: str
  """Name of the new session to create."""


class ForkToken(RpcEndpoint):
  """Generate a token for a new named session, cloned from a session-scoped token."""

  async def fork_token(
    self,
    *,
    refresh_token: str,
    session_name: str,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> TokenResult:
    """Generate a token for a new named session, cloned from a session-scoped token.

    Args:
      refresh_token: Refresh token from an earlier public/auth call, scoped session:name.
      session_name: Name of the new session to create.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/authentication/public-fork_token)
    """
    return await self.request(
      Request(refresh_token=refresh_token, session_name=session_name),
      method='POST',
      path='public/fork_token',
      meta={'public': True},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=TokenResult,
    )
