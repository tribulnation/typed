# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  expected_result: NotRequired[Literal['exception']]
  """Set to `"exception"` to force an error response instead of the normal result, for testing wrapper libraries. Any other value is not documented and should be omitted."""


class TestResult(TypedDict):
  """Deribit's own API version."""

  version: str
  """The API version."""


class Test(RpcEndpoint):
  """Tests the connection to the API server, and returns its version. Useful for confirming the API is reachable and matches the expected version. `expected_result="exception"` deliberately triggers an error response instead, for testing wrapper libraries' error handling."""

  async def test(
    self,
    expected_result: Literal['exception'] | None = None,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> TestResult:
    """Tests the connection to the API server, and returns its version. Useful for confirming the API is reachable and matches the expected version. `expected_result="exception"` deliberately triggers an error response instead, for testing wrapper libraries' error handling.

    Args:
      expected_result: Set to `"exception"` to force an error response instead of the normal result, for testing wrapper libraries. Any other value is not documented and should be omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/supporting/public-test)
    """
    request: Request = Request()
    if expected_result is not None:
      request['expected_result'] = expected_result
    return await self.request(
      request,
      method='POST',
      path='public/test',
      meta={'public': True},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=TestResult,
    )
