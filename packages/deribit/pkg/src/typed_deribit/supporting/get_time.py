# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal
from typed_deribit.core.endpoint.rpc import RpcEndpoint


Response = int


class GetTime(RpcEndpoint):
  """Retrieves the current time, in milliseconds since the UNIX epoch. Useful for checking the clock skew between the caller's software and Deribit's systems."""

  async def get_time(
    self,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Retrieves the current time, in milliseconds since the UNIX epoch. Useful for checking the clock skew between the caller's software and Deribit's systems.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/supporting/public-get_time)
    """
    return await self.request(
      None,
      method='POST',
      path='public/get_time',
      meta={'public': True},
      validate=validate,
      transport=transport,
      response_type=Response,
    )
