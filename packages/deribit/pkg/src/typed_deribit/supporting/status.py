# Generated by typed-dev — see CONTRIBUTING.md.
"""`public/status` — `public/status`."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_deribit.core import RpcEndpoint


class StatusResult(TypedDict):
  """Lock status of the platform."""

  locked: Literal['true', 'partial', 'false']
  """`"true"` when the platform is locked in all currencies, `"partial"` when some currencies are locked, `"false"` when no currencies are locked."""
  locked_indices: NotRequired[list[str]]
  """Currency indices locked platform-wide. Present (and non-empty) whenever `locked` is `"partial"` or `"true"`."""


validate_status = validator[StatusResult](StatusResult)


class Status(RpcEndpoint):
  """`public/status`."""

  async def status(self, *, validate: bool | None = None) -> StatusResult:
    """Retrieves information about locked currencies -- whether trading is currently locked platform-wide, for a subset of currencies, or not at all.

    Args:
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/supporting/public-status)
    """
    return await self.request(
      'public/status', validator=validate_status, validate=validate
    )
