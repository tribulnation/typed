# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .address_book import AddressBook
from .beneficiary import Beneficiary
from .deposits import Deposits
from .rewards import Rewards
from .transfers import Transfers
from .withdrawals import Withdrawals
from typed_deribit.core.endpoint.rpc import RpcEndpoint


class Wallet(RpcEndpoint):
  """Deposits, withdrawals, transfers, and address-book management for the account's wallet.

  References:
    - [Upstream docs](https://docs.deribit.com/api-reference/wallet)
  """

  @cached_property
  def address_book(self) -> AddressBook:
    """Deposit-address bookkeeping — the current and past deposit addresses generated for the account, plus a separate whitelist of external withdrawal addresses (the address book proper) that can be added, updated, listed, and removed.

    References:
      - [Upstream docs](https://docs.deribit.com/api-reference/wallet)
    """
    return AddressBook.new(self.client, ws_client=self.ws_client)

  @cached_property
  def beneficiary(self) -> Beneficiary:
    """Beneficiary (VASP travel-rule) information attached to withdrawal addresses — saving, listing, fetching, and deleting the beneficiary details Deribit requires for regulated withdrawals.

    References:
      - [Upstream docs](https://docs.deribit.com/api-reference/wallet)
    """
    return Beneficiary.new(self.client, ws_client=self.ws_client)

  @cached_property
  def deposits(self) -> Deposits:
    """Incoming deposit history and originator-clearance metadata for deposits into the account's wallet.

    References:
      - [Upstream docs](https://docs.deribit.com/api-reference/wallet)
    """
    return Deposits.new(self.client, ws_client=self.ws_client)

  @cached_property
  def rewards(self) -> Rewards:
    """Eligibility lookups for Deribit's wallet reward programs.

    References:
      - [Upstream docs](https://docs.deribit.com/api-reference/wallet)
    """
    return Rewards.new(self.client, ws_client=self.ws_client)

  @cached_property
  def transfers(self) -> Transfers:
    """Internal transfers of funds between a Deribit main account and its subaccounts, or to another Deribit user — submitting, cancelling, and listing transfer history.

    References:
      - [Upstream docs](https://docs.deribit.com/articles/managing-transfers-api)
    """
    return Transfers.new(self.client, ws_client=self.ws_client)

  @cached_property
  def withdrawals(self) -> Withdrawals:
    """Withdrawal lifecycle — submitting, cancelling, and listing withdrawals from the account's wallet.

    References:
      - [Upstream docs](https://docs.deribit.com/api-reference/wallet)
    """
    return Withdrawals.new(self.client, ws_client=self.ws_client)
