# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import Withdrawal


class Request(TypedDict):
  currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR']
  """The currency symbol."""
  address: str
  """Destination address, in `currency` format. Must already be present in this account's address book."""
  amount: float
  """Amount of funds to be withdrawn."""
  priority: NotRequired[
    Literal['insane', 'extreme_high', 'very_high', 'high', 'mid', 'low', 'very_low']
  ]
  """Withdrawal priority. Optional for BTC, where it defaults to `high`; the default for other currencies is not documented."""
  nonce: NotRequired[str]
  """Optional idempotency nonce. If provided, a subsequent request with the same nonce returns the previously created withdrawal instead of creating a new one. 8-128 characters; persisted on the resulting withdrawal and returned in the response."""


class Withdraw(RpcEndpoint):
  """Creates a new withdrawal request, sending funds from this account to an external address already present in the address book. Withdrawal funds are checked both when requested and again when confirmed via the emailed confirmation link; the withdrawal amount is only deducted once all checks pass and the transaction is scheduled."""

  async def withdraw(
    self,
    currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR'],
    *,
    address: str,
    amount: float,
    priority: Literal[
      'insane', 'extreme_high', 'very_high', 'high', 'mid', 'low', 'very_low'
    ]
    | None = None,
    nonce: str | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Withdrawal:
    """Creates a new withdrawal request, sending funds from this account to an external address already present in the address book. Withdrawal funds are checked both when requested and again when confirmed via the emailed confirmation link; the withdrawal amount is only deducted once all checks pass and the transaction is scheduled.

    Args:
      currency: The currency symbol.
      address: Destination address, in `currency` format. Must already be present in this account's address book.
      amount: Amount of funds to be withdrawn.
      priority: Withdrawal priority. Optional for BTC, where it defaults to `high`; the default for other currencies is not documented.
      nonce: Optional idempotency nonce. If provided, a subsequent request with the same nonce returns the previously created withdrawal instead of creating a new one. 8-128 characters; persisted on the resulting withdrawal and returned in the response.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/wallet/private-withdraw)
    """
    request: Request = Request(currency=currency, address=address, amount=amount)
    if priority is not None:
      request['priority'] = priority
    if nonce is not None:
      request['nonce'] = nonce
    return await self.request(
      request,
      method='POST',
      path='private/withdraw',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Withdrawal,
    )
