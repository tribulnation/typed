# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import Withdrawal


class Request(TypedDict):
  currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR']
  """The currency symbol."""
  id: int
  """The withdrawal id."""


class CancelWithdrawal(RpcEndpoint):
  """Cancels a pending withdrawal request. Once a withdrawal has been processed it can no longer be cancelled."""

  async def cancel_withdrawal(
    self,
    currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR'],
    *,
    id: int,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Withdrawal:
    """Cancels a pending withdrawal request. Once a withdrawal has been processed it can no longer be cancelled.

    Args:
      currency: The currency symbol.
      id: The withdrawal id.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/wallet/private-cancel_withdrawal)
    """
    return await self.request(
      Request(currency=currency, id=id),
      method='POST',
      path='private/cancel_withdrawal',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Withdrawal,
    )
