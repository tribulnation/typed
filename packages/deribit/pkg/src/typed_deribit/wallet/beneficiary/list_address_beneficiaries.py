# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import AddressBeneficiary


class Request(TypedDict):
  currency: NotRequired[
    Literal[
      'BTC',
      'ETH',
      'STETH',
      'ETHW',
      'USDC',
      'USDT',
      'EURR',
      'SOL',
      'XRP',
      'USYC',
      'PAXG',
      'BNB',
      'USDE',
    ]
  ]
  """Filter by currency symbol."""
  address: NotRequired[str]
  """Filter by address, in currency format."""
  tag: NotRequired[str]
  """Filter by tag (XRP addresses)."""
  created_before: NotRequired[int]
  """Filter by creation timestamp (before), milliseconds since the Unix epoch."""
  created_after: NotRequired[int]
  """Filter by creation timestamp (after), milliseconds since the Unix epoch."""
  updated_before: NotRequired[int]
  """Filter by update timestamp (before), milliseconds since the Unix epoch."""
  updated_after: NotRequired[int]
  """Filter by update timestamp (after), milliseconds since the Unix epoch."""
  personal: NotRequired[bool]
  """Filter by the personal-wallet flag."""
  unhosted: NotRequired[bool]
  """Filter by the unhosted-wallet flag."""
  beneficiary_vasp_name: NotRequired[str]
  """Filter by beneficiary VASP name."""
  beneficiary_vasp_did: NotRequired[str]
  """Filter by beneficiary VASP DID."""
  beneficiary_vasp_website: NotRequired[str]
  """Filter by beneficiary VASP website."""
  limit: NotRequired[int]
  """Maximum number of results to return."""
  continuation: NotRequired[str]
  """Continuation token for pagination, from a previous page's response."""


class AddressBeneficiariesPage(TypedDict):
  """One page of beneficiary records."""

  data: list[AddressBeneficiary]
  """Beneficiary records on this page."""
  continuation: NotRequired[str]
  """Token for the next page, passed back as `continuation` -- absent once results are exhausted."""
  count: NotRequired[int]
  """Total number of matching results available."""


class ListAddressBeneficiaries(RpcEndpoint):
  """Lists address beneficiaries with optional filtering and pagination. Returns all saved beneficiary information for addresses, with support for filtering by currency, address, wallet type, VASP details, and date ranges."""

  def list_address_beneficiaries_paged(
    self,
    currency: Literal[
      'BTC',
      'ETH',
      'STETH',
      'ETHW',
      'USDC',
      'USDT',
      'EURR',
      'SOL',
      'XRP',
      'USYC',
      'PAXG',
      'BNB',
      'USDE',
    ]
    | None = None,
    *,
    address: str | None = None,
    tag: str | None = None,
    created_before: int | None = None,
    created_after: int | None = None,
    updated_before: int | None = None,
    updated_after: int | None = None,
    personal: bool | None = None,
    unhosted: bool | None = None,
    beneficiary_vasp_name: str | None = None,
    beneficiary_vasp_did: str | None = None,
    beneficiary_vasp_website: str | None = None,
    limit: int | None = None,
    transport: Literal['http', 'ws'] = 'http',
    validate: bool | None = None,
  ) -> PaginatedResponse[AddressBeneficiary, str]:
    """Lists address beneficiaries with optional filtering and pagination. Returns all saved beneficiary information for addresses, with support for filtering by currency, address, wallet type, VASP details, and date ranges.

    Paged variant of `list_address_beneficiaries`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      currency: Filter by currency symbol.
      address: Filter by address, in currency format.
      tag: Filter by tag (XRP addresses).
      created_before: Filter by creation timestamp (before), milliseconds since the Unix epoch.
      created_after: Filter by creation timestamp (after), milliseconds since the Unix epoch.
      updated_before: Filter by update timestamp (before), milliseconds since the Unix epoch.
      updated_after: Filter by update timestamp (after), milliseconds since the Unix epoch.
      personal: Filter by the personal-wallet flag.
      unhosted: Filter by the unhosted-wallet flag.
      beneficiary_vasp_name: Filter by beneficiary VASP name.
      beneficiary_vasp_did: Filter by beneficiary VASP DID.
      beneficiary_vasp_website: Filter by beneficiary VASP website.
      limit: Maximum number of results to return.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/wallet/private-list_address_beneficiaries)
    """

    async def next(continuation: str) -> tuple[list[AddressBeneficiary], str | None]:
      response = await self.list_address_beneficiaries(
        currency,
        address=address,
        tag=tag,
        created_before=created_before,
        created_after=created_after,
        updated_before=updated_before,
        updated_after=updated_after,
        personal=personal,
        unhosted=unhosted,
        beneficiary_vasp_name=beneficiary_vasp_name,
        beneficiary_vasp_did=beneficiary_vasp_did,
        beneficiary_vasp_website=beneficiary_vasp_website,
        limit=limit,
        transport=transport,
        continuation=(continuation or None),
        validate=validate,
      )
      rows = response.get('data')
      state = response.get('continuation')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def list_address_beneficiaries(
    self,
    currency: Literal[
      'BTC',
      'ETH',
      'STETH',
      'ETHW',
      'USDC',
      'USDT',
      'EURR',
      'SOL',
      'XRP',
      'USYC',
      'PAXG',
      'BNB',
      'USDE',
    ]
    | None = None,
    *,
    address: str | None = None,
    tag: str | None = None,
    created_before: int | None = None,
    created_after: int | None = None,
    updated_before: int | None = None,
    updated_after: int | None = None,
    personal: bool | None = None,
    unhosted: bool | None = None,
    beneficiary_vasp_name: str | None = None,
    beneficiary_vasp_did: str | None = None,
    beneficiary_vasp_website: str | None = None,
    limit: int | None = None,
    continuation: str | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> AddressBeneficiariesPage:
    """Lists address beneficiaries with optional filtering and pagination. Returns all saved beneficiary information for addresses, with support for filtering by currency, address, wallet type, VASP details, and date ranges.

    Args:
      currency: Filter by currency symbol.
      address: Filter by address, in currency format.
      tag: Filter by tag (XRP addresses).
      created_before: Filter by creation timestamp (before), milliseconds since the Unix epoch.
      created_after: Filter by creation timestamp (after), milliseconds since the Unix epoch.
      updated_before: Filter by update timestamp (before), milliseconds since the Unix epoch.
      updated_after: Filter by update timestamp (after), milliseconds since the Unix epoch.
      personal: Filter by the personal-wallet flag.
      unhosted: Filter by the unhosted-wallet flag.
      beneficiary_vasp_name: Filter by beneficiary VASP name.
      beneficiary_vasp_did: Filter by beneficiary VASP DID.
      beneficiary_vasp_website: Filter by beneficiary VASP website.
      limit: Maximum number of results to return.
      continuation: Continuation token for pagination, from a previous page's response.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/wallet/private-list_address_beneficiaries)
    """
    request: Request = Request()
    if currency is not None:
      request['currency'] = currency
    if address is not None:
      request['address'] = address
    if tag is not None:
      request['tag'] = tag
    if created_before is not None:
      request['created_before'] = created_before
    if created_after is not None:
      request['created_after'] = created_after
    if updated_before is not None:
      request['updated_before'] = updated_before
    if updated_after is not None:
      request['updated_after'] = updated_after
    if personal is not None:
      request['personal'] = personal
    if unhosted is not None:
      request['unhosted'] = unhosted
    if beneficiary_vasp_name is not None:
      request['beneficiary_vasp_name'] = beneficiary_vasp_name
    if beneficiary_vasp_did is not None:
      request['beneficiary_vasp_did'] = beneficiary_vasp_did
    if beneficiary_vasp_website is not None:
      request['beneficiary_vasp_website'] = beneficiary_vasp_website
    if limit is not None:
      request['limit'] = limit
    if continuation is not None:
      request['continuation'] = continuation
    return await self.request(
      request,
      method='POST',
      path='private/list_address_beneficiaries',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=AddressBeneficiariesPage,
    )
