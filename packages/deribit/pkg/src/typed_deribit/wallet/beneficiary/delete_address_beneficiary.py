# Generated by typed-dev — see CONTRIBUTING.md.
"""`private/delete_address_beneficiary` — `private/delete_address_beneficiary`."""

from typing_extensions import Literal
from typed_core.validation import validator
from typed_deribit.core import RpcEndpoint

validate_delete_address_beneficiary = validator[Literal['ok']](Literal['ok'])  # pyright: ignore[reportArgumentType]  # fmt: skip


class DeleteAddressBeneficiary(RpcEndpoint):
  """`private/delete_address_beneficiary`."""

  async def delete_address_beneficiary(
    self,
    *,
    currency: Literal[
      'BTC',
      'ETH',
      'STETH',
      'ETHW',
      'USDC',
      'USDT',
      'EURR',
      'SOL',
      'XRP',
      'USYC',
      'PAXG',
      'BNB',
      'USDE',
    ],
    address: str,
    tag: str | None = None,
    validate: bool | None = None,
  ) -> Literal['ok']:
    """Deletes beneficiary information for a specific address.

    Args:
      currency: The currency symbol.
      address: Address in currency format.
      tag: Tag for XRP addresses.
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/wallet/private-delete_address_beneficiary)
    """
    params: dict = {
      'currency': currency,
      'address': address,
    }
    if tag is not None:
      params['tag'] = tag
    return await self.authed_request(
      'private/delete_address_beneficiary',
      params=params,
      validator=validate_delete_address_beneficiary,
      validate=validate,
    )
