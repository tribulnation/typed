# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import AddressBeneficiary


class Request(TypedDict):
  currency: Literal[
    'BTC',
    'ETH',
    'STETH',
    'ETHW',
    'USDC',
    'USDT',
    'EURR',
    'SOL',
    'XRP',
    'USYC',
    'PAXG',
    'BNB',
    'USDE',
  ]
  """The currency symbol."""
  address: str
  """Address in currency format."""
  tag: NotRequired[str]
  """Tag for XRP addresses."""


class GetAddressBeneficiary(RpcEndpoint):
  """Retrieves beneficiary information for a specific address. Returns the stored beneficiary details including VASP information, personal details, and wallet type classification."""

  async def get_address_beneficiary(
    self,
    currency: Literal[
      'BTC',
      'ETH',
      'STETH',
      'ETHW',
      'USDC',
      'USDT',
      'EURR',
      'SOL',
      'XRP',
      'USYC',
      'PAXG',
      'BNB',
      'USDE',
    ],
    *,
    address: str,
    tag: str | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> AddressBeneficiary:
    """Retrieves beneficiary information for a specific address. Returns the stored beneficiary details including VASP information, personal details, and wallet type classification.

    Args:
      currency: The currency symbol.
      address: Address in currency format.
      tag: Tag for XRP addresses.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/wallet/private-get_address_beneficiary)
    """
    request: Request = Request(currency=currency, address=address)
    if tag is not None:
      request['tag'] = tag
    return await self.request(
      request,
      method='POST',
      path='private/get_address_beneficiary',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=AddressBeneficiary,
    )
