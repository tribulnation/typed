# Generated by typed-dev — see CONTRIBUTING.md.
"""`private/remove_from_address_book` — `private/remove_from_address_book`."""

from typing_extensions import Literal
from typed_core.validation import validator
from typed_deribit.core import RpcEndpoint

validate_remove_from_address_book = validator[Literal['ok']](Literal['ok'])  # pyright: ignore[reportArgumentType]  # fmt: skip


class RemoveFromAddressBook(RpcEndpoint):
  """`private/remove_from_address_book`."""

  async def remove_from_address_book(
    self,
    *,
    currency: Literal[
      'BTC',
      'ETH',
      'STETH',
      'ETHW',
      'USDC',
      'USDT',
      'EURR',
      'SOL',
      'XRP',
      'USYC',
      'PAXG',
      'BNB',
      'USDE',
    ],
    type: Literal['transfer', 'withdrawal', 'deposit_source'],
    address: str,
    validate: bool | None = None,
  ) -> Literal['ok']:
    """Removes an entry from the address book. This method allows you to delete a saved address that is no longer needed.

    Args:
      currency: The currency symbol.
      type: Address book type.
      address: Address in currency format; it must already be in the address book.
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/wallet/private-remove_from_address_book)
    """
    params: dict = {
      'currency': currency,
      'type': type,
      'address': address,
    }
    return await self.authed_request(
      'private/remove_from_address_book',
      params=params,
      validator=validate_remove_from_address_book,
      validate=validate,
    )
