# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict, cast
from typed_deribit.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  currency: Literal[
    'BTC',
    'ETH',
    'STETH',
    'ETHW',
    'USDC',
    'USDT',
    'EURR',
    'SOL',
    'XRP',
    'USYC',
    'PAXG',
    'BNB',
    'USDE',
  ]
  """The currency symbol."""
  type: Literal['transfer', 'withdrawal', 'deposit_source']
  """Address book type."""
  address: str
  """Address in currency format; it must already be in the address book."""


Response = Literal['ok']


class RemoveFromAddressBook(RpcEndpoint):
  """Removes an entry from the address book. This method allows you to delete a saved address that is no longer needed."""

  async def remove_from_address_book(
    self,
    currency: Literal[
      'BTC',
      'ETH',
      'STETH',
      'ETHW',
      'USDC',
      'USDT',
      'EURR',
      'SOL',
      'XRP',
      'USYC',
      'PAXG',
      'BNB',
      'USDE',
    ],
    *,
    type: Literal['transfer', 'withdrawal', 'deposit_source'],
    address: str,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Removes an entry from the address book. This method allows you to delete a saved address that is no longer needed.

    Args:
      currency: The currency symbol.
      type: Address book type.
      address: Address in currency format; it must already be in the address book.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/wallet/private-remove_from_address_book)
    """
    return await self.request(
      Request(currency=currency, type=type, address=address),
      method='POST',
      path='private/remove_from_address_book',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=cast(type, Response),
    )
