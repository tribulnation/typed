# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core import TimestampMillis
from typed_deribit.core.endpoint.rpc import RpcEndpoint


class AddressBookEntry(TypedDict):
  """One address book entry."""

  currency: str
  """The currency symbol."""
  address: str
  """Address in currency format."""
  creation_timestamp: TimestampMillis
  """Time the entry was created (milliseconds since the Unix epoch)."""
  type: NotRequired[Literal['transfer', 'withdrawal', 'deposit_source']]
  """Address book type."""
  label: NotRequired[str]
  """Label of the address book entry."""
  beneficiary_vasp_name: NotRequired[str]
  """Name of beneficiary VASP."""
  beneficiary_vasp_did: NotRequired[str]
  """DID of beneficiary VASP."""
  beneficiary_vasp_website: NotRequired[str]
  """Website of the beneficiary VASP."""
  beneficiary_first_name: NotRequired[str]
  """First name of the beneficiary (if beneficiary is a person)."""
  beneficiary_last_name: NotRequired[str]
  """Last name of the beneficiary (if beneficiary is a person)."""
  beneficiary_company_name: NotRequired[str]
  """Company name of the beneficiary (if beneficiary is a company)."""
  beneficiary_address: NotRequired[str]
  """Geographical address of the beneficiary."""
  agreed: NotRequired[bool]
  """Whether the user agreed to share the provided information with 3rd parties."""
  personal: NotRequired[bool]
  """Whether the user confirmed the address belongs to them and is accessible via an un-hosted wallet."""
  info_required: NotRequired[bool]
  """Whether additional beneficiary information is still required for this entry."""
  status: NotRequired[Literal['admin_locked', 'waiting', 'confirmed', 'ready']]
  """Wallet address status."""
  waiting_timestamp: NotRequired[bool]
  """Documented as a boolean (`example: true`) despite the field name implying a timestamp of when the address will be ready -- kept as documented, not confirmed live; see this endpoint's notes."""
  requires_confirmation: NotRequired[bool]
  """Whether this address requires email confirmation for withdrawals."""
  requires_confirmation_change: NotRequired[bool]
  """Whether an email confirmation change is in progress for this address."""


class Request(TypedDict):
  currency: Literal[
    'BTC',
    'ETH',
    'STETH',
    'ETHW',
    'USDC',
    'USDT',
    'EURR',
    'SOL',
    'XRP',
    'USYC',
    'PAXG',
    'BNB',
    'USDE',
  ]
  """The currency symbol."""
  type: Literal['transfer', 'withdrawal', 'deposit_source']
  """Address book type."""


Response = list[AddressBookEntry]


class GetAddressBook(RpcEndpoint):
  """Retrieves the address book entries of the given type. Returns all saved addresses that can be used for withdrawals, along with their labels and beneficiary information if available."""

  async def get_address_book(
    self,
    currency: Literal[
      'BTC',
      'ETH',
      'STETH',
      'ETHW',
      'USDC',
      'USDT',
      'EURR',
      'SOL',
      'XRP',
      'USYC',
      'PAXG',
      'BNB',
      'USDE',
    ],
    *,
    type: Literal['transfer', 'withdrawal', 'deposit_source'],
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Retrieves the address book entries of the given type. Returns all saved addresses that can be used for withdrawals, along with their labels and beneficiary information if available.

    Args:
      currency: The currency symbol.
      type: Address book type.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/wallet/private-get_address_book)
    """
    return await self.request(
      Request(currency=currency, type=type),
      method='POST',
      path='private/get_address_book',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Response,
    )
