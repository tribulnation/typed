# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import DepositAddress


class Request(TypedDict):
  currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR']
  """The currency symbol."""


class CreateDepositAddress(RpcEndpoint):
  """Creates a new deposit address for the specified currency. Each currency can have multiple deposit addresses.

  For Bitcoin, a new address can be generated every 24 hours. For ERC20, Solana and XRP only one address can be generated -- once an ERC20 address is generated, it is automatically added for every asset that uses ERC20 addresses."""

  async def create_deposit_address(
    self,
    currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR'],
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> DepositAddress:
    """Creates a new deposit address for the specified currency. Each currency can have multiple deposit addresses.

    For Bitcoin, a new address can be generated every 24 hours. For ERC20, Solana and XRP only one address can be generated -- once an ERC20 address is generated, it is automatically added for every asset that uses ERC20 addresses.

    Args:
      currency: The currency symbol.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/wallet/private-create_deposit_address)
    """
    return await self.request(
      Request(currency=currency),
      method='POST',
      path='private/create_deposit_address',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=DepositAddress,
    )
