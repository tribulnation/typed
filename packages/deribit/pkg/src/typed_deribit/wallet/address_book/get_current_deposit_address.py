# Generated by typed-dev — see CONTRIBUTING.md.
"""`private/get_current_deposit_address` — `private/get_current_deposit_address`."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core import RpcEndpoint, TimestampMillis
from typed_core.validation import validator


class DepositAddress(TypedDict):
  """The account's deposit address for the requested currency."""

  creation_timestamp: TimestampMillis
  """Time the address was created (milliseconds since the Unix epoch)."""
  currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR']
  """The currency symbol."""
  address: str
  """The deposit address, in the currency's native format."""
  type: Literal['deposit']
  """Address type/purpose -- always `deposit` for this operation."""
  status: NotRequired[Literal['admin_locked', 'waiting', 'confirmed', 'ready']]
  """Wallet address status. Not in the venue's own documented schema for this field, observed live."""
  agreed: NotRequired[bool]
  """Whether the user agreed to share the provided information with 3rd parties. Not in the venue's own documented schema for this field, observed live."""
  personal: NotRequired[bool]
  """Whether the user confirmed the address belongs to them and is accessible via an un-hosted wallet. Not in the venue's own documented schema for this field, observed live."""
  requires_security_key: NotRequired[bool]
  """Whether a security key (WebAuthn/U2F) confirmation is required before this address can be edited or reused for a withdrawal-adjacent operation. Not in the venue's own documented schema for this field, observed live."""
  requires_confirmation: NotRequired[bool]
  """Whether this address requires email confirmation for withdrawals. Not in the venue's own documented schema for this field, observed live."""
  requires_confirmation_change: NotRequired[bool]
  """Whether an email confirmation change is in progress for this address. Not in the venue's own documented schema for this field, observed live."""
  info_required: NotRequired[bool]
  """Whether additional beneficiary information is still required for this address. Not in the venue's own documented schema for this field, observed live."""


validate_get_current_deposit_address = validator[DepositAddress | None](DepositAddress | None)  # pyright: ignore[reportArgumentType]  # fmt: skip


class GetCurrentDepositAddress(RpcEndpoint):
  """`private/get_current_deposit_address`."""

  async def get_current_deposit_address(
    self,
    *,
    currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR'],
    validate: bool | None = None,
  ) -> DepositAddress | None:
    """Retrieve the current deposit address for the specified currency. Returns the most recently created or used deposit address for receiving funds.

    Args:
      currency: The currency symbol.
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/wallet/private-get_current_deposit_address)
    """
    params: dict = {
      'currency': currency,
    }
    return await self.authed_request(
      'private/get_current_deposit_address',
      params=params,
      validator=validate_get_current_deposit_address,
      validate=validate,
    )
