# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import InternalTransfer


class Request(TypedDict):
  currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR']
  """The currency symbol."""
  amount: float
  """Amount of funds to be transferred."""
  destination: int
  """Id of the destination subaccount. Can be found in `My Account >> Subaccounts`."""
  source: NotRequired[int]
  """Id of the source (sub)account. Can be found in `My Account >> Subaccounts`. Defaults to the account making the request; naming a different source requires the `mainaccount` scope, and only another subaccount may be named."""
  nonce: NotRequired[str]
  """Optional idempotency nonce. If provided, a subsequent request with the same nonce returns the previously created transfer instead of creating a new one. 8-128 characters; persisted on the resulting transfer and returned in the response."""


class SubmitTransferBetweenSubaccounts(RpcEndpoint):
  """Transfer funds between two subaccounts, or between a subaccount and the main account."""

  async def submit_transfer_between_subaccounts(
    self,
    currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR'],
    *,
    amount: float,
    destination: int,
    source: int | None = None,
    nonce: str | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> InternalTransfer:
    """Transfer funds between two subaccounts, or between a subaccount and the main account.

    Args:
      currency: The currency symbol.
      amount: Amount of funds to be transferred.
      destination: Id of the destination subaccount. Can be found in `My Account >> Subaccounts`.
      source: Id of the source (sub)account. Can be found in `My Account >> Subaccounts`. Defaults to the account making the request; naming a different source requires the `mainaccount` scope, and only another subaccount may be named.
      nonce: Optional idempotency nonce. If provided, a subsequent request with the same nonce returns the previously created transfer instead of creating a new one. 8-128 characters; persisted on the resulting transfer and returned in the response.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/wallet/private-submit_transfer_between_subaccounts)
    """
    request: Request = Request(
      currency=currency, amount=amount, destination=destination
    )
    if source is not None:
      request['source'] = source
    if nonce is not None:
      request['nonce'] = nonce
    return await self.request(
      request,
      method='POST',
      path='private/submit_transfer_between_subaccounts',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=InternalTransfer,
    )
