# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import InternalTransfer


class Request(TypedDict):
  currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR']
  """The currency symbol."""
  amount: float
  """Amount of funds to be transferred."""
  destination: str
  """Destination address, taken from a `transfer`-type entry in this account's address book (see `wallet.get_address_book`)."""


class SubmitTransferToUser(RpcEndpoint):
  """Transfer funds to another Deribit user's account, via a saved `transfer`-type address-book entry. Processed internally; no blockchain transaction is involved."""

  async def submit_transfer_to_user(
    self,
    currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR'],
    *,
    amount: float,
    destination: str,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> InternalTransfer:
    """Transfer funds to another Deribit user's account, via a saved `transfer`-type address-book entry. Processed internally; no blockchain transaction is involved.

    Args:
      currency: The currency symbol.
      amount: Amount of funds to be transferred.
      destination: Destination address, taken from a `transfer`-type entry in this account's address book (see `wallet.get_address_book`).
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/wallet/private-submit_transfer_to_user)
    """
    return await self.request(
      Request(currency=currency, amount=amount, destination=destination),
      method='POST',
      path='private/submit_transfer_to_user',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=InternalTransfer,
    )
