# Generated by typed-dev — see CONTRIBUTING.md.
"""`private/submit_transfer_to_user` — `private/submit_transfer_to_user`."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core import RpcEndpoint, TimestampMillis
from typed_core.validation import validator


class Transfer(TypedDict):
  """One internal transfer."""

  id: int
  """Id of the transfer."""
  created_timestamp: TimestampMillis
  """Time the transfer was created (milliseconds since the Unix epoch)."""
  type: Literal['user', 'subaccount']
  """`user`: sent to another user. `subaccount`: sent to a subaccount."""
  currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR']
  """The currency symbol."""
  amount: float
  """Amount of funds transferred, in `currency`."""
  other_side: str
  """For a subaccount transfer, the subaccount's name; for a transfer to another account, the destination address; for a transfer from another account, the sender's username."""
  state: str
  """Transfer state. Documented as an open set (`prepared`, `confirmed`, `cancelled`, `waiting_for_admin`, `insufficient_funds`, `withdrawal_limit`, or otherwise a rejection-reason string) -- see the sibling `wallet.transfers.get_transfers`'s notes; left bare rather than a guessed closed `enum`."""
  direction: NotRequired[Literal['payment', 'income']]
  """Whether this transfer was sent (`payment`) or received (`income`)."""
  updated_timestamp: TimestampMillis
  """Time this transfer record was last updated (milliseconds since the Unix epoch)."""
  nonce: NotRequired[str]
  """Optional idempotency nonce, when one was provided in the originating request."""
  source: NotRequired[int]
  """Id of the (sub)account that initiated the transfer (observed live on sibling transfer endpoints; not in the venue's own documented `transfer_item` schema -- see this endpoint's notes)."""
  note: NotRequired[str]
  """Free-text note attached to the transfer (observed live on sibling transfer endpoints as an empty string; not in the venue's own documented `transfer_item` schema -- see this endpoint's notes)."""


validate_submit_transfer_to_user = validator[Transfer](Transfer)


class SubmitTransferToUser(RpcEndpoint):
  """`private/submit_transfer_to_user`."""

  async def submit_transfer_to_user(
    self,
    *,
    currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR'],
    amount: float,
    destination: str,
    validate: bool | None = None,
  ) -> Transfer:
    """Transfer funds to another Deribit user's account, via a saved `transfer`-type address-book entry. Processed internally; no blockchain transaction is involved.

    Args:
      currency: The currency symbol.
      amount: Amount of funds to be transferred.
      destination: Destination address, taken from a `transfer`-type entry in this account's address book (see `wallet.get_address_book`).
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/wallet/private-submit_transfer_to_user)
    """
    params: dict = {
      'currency': currency,
      'amount': amount,
      'destination': destination,
    }
    return await self.authed_request(
      'private/submit_transfer_to_user',
      params=params,
      validator=validate_submit_transfer_to_user,
      validate=validate,
    )
