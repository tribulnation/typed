# Generated by typed-dev — see CONTRIBUTING.md.
"""`quote.{instrument_name}` — subscription."""

from typing_extensions import Any, TypedDict
from typed_deribit.core import StreamEndpoint, TimestampMillis
from typed_core.util import StreamManager
from typed_core.validation import validator


class QuoteUpdate(TypedDict):
  """Top-of-book (best bid/ask) update for a specific instrument."""

  timestamp: TimestampMillis
  """The timestamp (milliseconds since the Unix epoch)."""
  instrument_name: str
  """Unique instrument identifier."""
  best_bid_price: float | None
  """The current best bid price, `null` if there aren't any bids."""
  best_bid_amount: float | None
  """The requested order size of all best bids."""
  best_ask_price: float | None
  """The current best ask price, `null` if there aren't any asks."""
  best_ask_amount: float | None
  """The requested order size of all best asks."""


validate_quote = validator[QuoteUpdate](QuoteUpdate)


class Quote(StreamEndpoint):
  """`quote.{instrument_name}` subscription."""

  def quote(
    self,
    instrument_name: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[QuoteUpdate, Any, Any]:
    """Best bid/ask price and size for a specific instrument -- top-of-book updates without the full order book depth.

    Args:
      instrument_name: The name of the instrument.
      validate: Validate pushed payloads against the expected schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/subscriptions/market-data/quoteinstrument_name)
    """
    channel = f'quote.{instrument_name}'
    return self.subscribe(channel, validator=validate_quote, validate=validate)
