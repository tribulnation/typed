# Generated by typed-dev — see CONTRIBUTING.md.
"""`platform_state` — subscription."""

from typing_extensions import Any, NotRequired, TypedDict
from typed_deribit.core import StreamEndpoint
from typed_core.util import StreamManager
from typed_core.validation import validator


class PlatformStateUpdate(TypedDict):
  """Platform operational-state notification."""

  price_index: NotRequired[str]
  """Name of the index that was locked or unlocked. Sent only together with `locked`."""
  locked: NotRequired[bool]
  """`true` when the given index is locked on the platform. Sent only together with `price_index`."""
  maintenance: NotRequired[bool]
  """`true` when a maintenance break begins."""


validate_platform_state = validator[PlatformStateUpdate](PlatformStateUpdate)


class PlatformState(StreamEndpoint):
  """`platform_state` subscription."""

  def platform_state(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[PlatformStateUpdate, Any, Any]:
    """Platform state notifications -- monitor whether the Deribit platform is operational and detect maintenance periods or partial outages, or a specific index being locked/unlocked.

    Args:
      validate: Validate pushed payloads against the expected schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/subscriptions/platform/platform_state)
    """
    channel = f'platform_state'
    return self.subscribe(channel, validator=validate_platform_state, validate=validate)
