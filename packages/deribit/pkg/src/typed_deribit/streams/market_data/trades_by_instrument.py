# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal
from typed_deribit.core.endpoint.stream import StreamEndpoint
from typed_deribit.schemas import MarketTradeUpdate


TradeUpdates = list[MarketTradeUpdate]


class TradesByInstrument(StreamEndpoint):
  """Trade notifications for a specific instrument, as they happen. `interval` controls how frequently trade events are aggregated into a push."""

  def trades_by_instrument(
    self,
    instrument_name: str,
    *,
    interval: Literal['raw', '100ms', 'agg2'],
    validate: bool | None = None,
  ) -> StreamManager[TradeUpdates, Any, Any]:
    """Trade notifications for a specific instrument, as they happen. `interval` controls how frequently trade events are aggregated into a push.

    Args:
      instrument_name: The name of the instrument.
      interval: Frequency of notifications; events are aggregated over this interval. `raw` means no aggregation and is only available to authorized users.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.deribit.com/subscriptions/trades/tradesinstrument_nameinterval)
    """
    return self.subscribe(
      f'trades.{instrument_name}.{interval}',
      meta={'public': True},
      validate=validate,
      response_type=TradeUpdates,
    )
