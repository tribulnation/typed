# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal
from typed_deribit.core.endpoint.stream import StreamEndpoint
from typed_deribit.schemas import InstrumentInfo


class InstrumentCreation(StreamEndpoint):
  """Notification published once when an instrument is created, carrying full instrument data in the same format as `public/get_instruments`. Fires exactly once at creation time, regardless of the instrument's initial state."""

  def instrument_creation(
    self,
    kind: Literal['future', 'option', 'spot', 'future_combo', 'option_combo'],
    *,
    currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR', 'any'],
    validate: bool | None = None,
  ) -> StreamManager[InstrumentInfo, Any, Any]:
    """Notification published once when an instrument is created, carrying full instrument data in the same format as `public/get_instruments`. Fires exactly once at creation time, regardless of the instrument's initial state.

    Args:
      kind: Instrument kind.
      currency: Currency code, or `any` for all.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.deribit.com/subscriptions/market-data/instrumentcreationkindcurrency)
    """
    return self.subscribe(
      f'instrument.creation.{kind}.{currency}',
      meta={'public': True},
      validate=validate,
      response_type=InstrumentInfo,
    )
