# Generated by typed-dev — see CONTRIBUTING.md.
"""`book.{instrument_name}.{group}.{depth}.{interval}` — subscription."""

from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_deribit.core import StreamEndpoint, TimestampMillis
from typed_core.util import StreamManager
from typed_core.validation import validator


class GroupedBookUpdate(TypedDict):
  """Aggregated, depth-limited order book snapshot for one `interval`."""

  instrument_name: str
  """Unique instrument identifier."""
  change_id: int
  """Identifier of the notification."""
  timestamp: NotRequired[TimestampMillis]
  """The timestamp of the last change (milliseconds since the Unix epoch)."""
  bids: list[tuple[float, float]]
  """Bid levels, best first, up to `depth` entries."""
  asks: list[tuple[float, float]]
  """Ask levels, best first, up to `depth` entries."""


validate_book_grouped = validator[GroupedBookUpdate](GroupedBookUpdate)


class BookGrouped(StreamEndpoint):
  """`book.{instrument_name}.{group}.{depth}.{interval}` subscription."""

  def book_grouped(
    self,
    instrument_name: str,
    group: Literal['none', '1', '2', '5', '10', '25', '100', '250'],
    depth: Literal['1', '10', '20'],
    interval: Literal['raw', '100ms', 'agg2'],
    *,
    validate: bool | None = None,
  ) -> StreamManager[GroupedBookUpdate, Any, Any]:
    """Aggregated order book updates for a specific instrument. Notifications are sent once per specified `interval`, with prices grouped (rounded) according to `group`, and the book truncated to the specified `depth` (number of price levels). For Starbase-routed instruments, the published price levels include accepted mass-quote liquidity. For perpetual and inverse futures `amount` is in USD units; for options and linear futures it is the underlying base currency coin.

    Args:
      instrument_name: The name of the instrument.
      group: Group prices (by rounding). Use `none` for no grouping. For ETH the real group is divided by 100.0 (e.g. `5` means `0.05`).
      depth: Number of price levels to include.
      interval: Frequency of notifications; events are aggregated over this interval. `raw` means no aggregation and is only available to authorized users.
      validate: Validate pushed payloads against the expected schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/subscriptions/orderbook/bookinstrument_namegroupdepthinterval)
    """
    channel = f'book.{instrument_name}.{group}.{depth}.{interval}'
    return self.subscribe(channel, validator=validate_book_grouped, validate=validate)
