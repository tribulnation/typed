# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal
from typed_deribit.core.endpoint.stream import StreamEndpoint
from typed_deribit.schemas import UserChangesUpdate


class ChangesByKindCurrency(StreamEndpoint):
  """User change stream (orders, trades, and related updates) across all instruments for a given kind and currency.

  This channel provides a consolidated private update stream for your account across all matching instruments. Use it when you want a single feed instead of subscribing to orders and trades separately."""

  def changes_by_kind_currency(
    self,
    kind: Literal['future', 'option', 'spot', 'future_combo', 'option_combo'],
    *,
    currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR', 'any'],
    interval: Literal['raw', '100ms', 'agg2'],
    validate: bool | None = None,
  ) -> StreamManager[UserChangesUpdate, Any, Any]:
    """User change stream (orders, trades, and related updates) across all instruments for a given kind and currency.

    This channel provides a consolidated private update stream for your account across all matching instruments. Use it when you want a single feed instead of subscribing to orders and trades separately.

    Args:
      kind: Instrument kind

    **Allowed values:** `future`, `option`, `spot`, `future_combo`, `option_combo`
      currency: Currency code or `any` for all

    **Allowed values:** `BTC`, `ETH`, `USDC`, `USDT`, `EURR`, `any`
      interval: Frequency of notifications. Events will be aggregated over this interval. The value `raw` means no aggregation will be applied **(Please note that `raw` interval is only available to authorized users)**

    **Allowed values:** `raw`, `100ms`, `agg2`
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/subscriptions/user-changes-kind-currency-interval)
    """
    return self.subscribe(
      f'user.changes.{kind}.{currency}.{interval}',
      meta={},
      validate=validate,
      response_type=UserChangesUpdate,
    )
