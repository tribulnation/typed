# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal
from typed_deribit.core.endpoint.stream import StreamEndpoint
from typed_deribit.schemas import ComboTradeUpdate


class ComboTradesByInstrument(StreamEndpoint):
  """User trade notifications for a specific combo instrument.

  Trades include a `legs` field describing the underlying legs of the combo. The `interval` controls aggregation frequency.

  **Block RFQ trades:** Block RFQ does not support combo instruments. RFQ-executed trades do not appear in this channel."""

  def combo_trades_by_instrument(
    self,
    instrument_name: str,
    *,
    interval: Literal['raw', '100ms', 'agg2'],
    validate: bool | None = None,
  ) -> StreamManager[ComboTradeUpdate, Any, Any]:
    """User trade notifications for a specific combo instrument.

    Trades include a `legs` field describing the underlying legs of the combo. The `interval` controls aggregation frequency.

    **Block RFQ trades:** Block RFQ does not support combo instruments. RFQ-executed trades do not appear in this channel.

    Args:
      instrument_name: The name of the instrument
      interval: Frequency of notifications. Events will be aggregated over this interval. The value `raw` means no aggregation will be applied **(Please note that `raw` interval is only available to authorized users)**

    **Allowed values:** `raw`, `100ms`, `agg2`
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/subscriptions/user-combo_trades-instrument_name-interval)
    """
    return self.subscribe(
      f'user.combo_trades.{instrument_name}.{interval}',
      meta={},
      validate=validate,
      response_type=ComboTradeUpdate,
    )
