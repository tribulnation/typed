# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal
from typed_deribit.core.endpoint.stream import StreamEndpoint
from typed_deribit.schemas import Order


class OrdersByKindCurrencyRaw(StreamEndpoint):
  """User order updates across all instruments for a given kind and currency (raw stream).

  Use this channel to receive private order updates across all matching instruments with the highest granularity (raw)."""

  def orders_by_kind_currency_raw(
    self,
    kind: Literal['future', 'option', 'spot', 'future_combo', 'option_combo'],
    *,
    currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR', 'any'],
    validate: bool | None = None,
  ) -> StreamManager[Order, Any, Any]:
    """User order updates across all instruments for a given kind and currency (raw stream).

    Use this channel to receive private order updates across all matching instruments with the highest granularity (raw).

    Args:
      kind: Instrument kind

    **Allowed values:** `future`, `option`, `spot`, `future_combo`, `option_combo`
      currency: Currency code or `any` for all

    **Allowed values:** `BTC`, `ETH`, `USDC`, `USDT`, `EURR`, `any`
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/subscriptions/user-orders-kind-currency-raw)
    """
    return self.subscribe(
      f'user.orders.{kind}.{currency}.raw',
      meta={},
      validate=validate,
      response_type=Order,
    )
