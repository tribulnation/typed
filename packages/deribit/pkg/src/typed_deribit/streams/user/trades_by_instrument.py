# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal
from typed_deribit.core.endpoint.stream import StreamEndpoint
from typed_deribit.schemas import UserTradeUpdate


TradesByInstrumentUpdates = list[UserTradeUpdate]


class TradesByInstrument(StreamEndpoint):
  """User trade notifications for a specific instrument.

  Receive private trade events for your account for the given instrument. The `interval` controls how frequently events are aggregated.

  **Block RFQ trades:** Trades resulting from Block RFQ execution are included in this channel. These trades carry the following additional fields:
  - `block_rfq_id` — ID of the Block RFQ.
  - `block_trade_id` — ID of the resulting block trade.
  - `block_rfq_quote_id` — ID of the accepted quote (present on the **maker** side only).
  - `order_id` — always present for direct trade participants (makers and takers)."""

  def trades_by_instrument(
    self,
    instrument_name: str,
    *,
    interval: Literal['raw', '100ms', 'agg2'],
    validate: bool | None = None,
  ) -> StreamManager[TradesByInstrumentUpdates, Any, Any]:
    """User trade notifications for a specific instrument.

    Receive private trade events for your account for the given instrument. The `interval` controls how frequently events are aggregated.

    **Block RFQ trades:** Trades resulting from Block RFQ execution are included in this channel. These trades carry the following additional fields:
    - `block_rfq_id` — ID of the Block RFQ.
    - `block_trade_id` — ID of the resulting block trade.
    - `block_rfq_quote_id` — ID of the accepted quote (present on the **maker** side only).
    - `order_id` — always present for direct trade participants (makers and takers).

    Args:
      instrument_name: The name of the instrument
      interval: Frequency of notifications. Events will be aggregated over this interval. The value `raw` means no aggregation will be applied **(Please note that `raw` interval is only available to authorized users)**

    **Allowed values:** `raw`, `100ms`, `agg2`
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/subscriptions/user-trades-instrument_name-interval)
    """
    return self.subscribe(
      f'user.trades.{instrument_name}.{interval}',
      meta={},
      validate=validate,
      response_type=TradesByInstrumentUpdates,
    )
