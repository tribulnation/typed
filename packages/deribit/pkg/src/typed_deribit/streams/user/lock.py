# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, TypedDict
from typed_deribit.core.endpoint.stream import StreamEndpoint


class LockUpdate(TypedDict):
  """Message schema for `user.lock` subscription - contains the data payload"""

  currency: str
  """Currency on which account lock has changed, `ALL` if changed for all currencies"""
  locked: bool
  """Value is set to 'true' when user account is locked in currency"""


class Lock(StreamEndpoint):
  """Notifications when the account is locked or unlocked.

  Use this channel to react to account lock events (e.g., pause trading workflows) and to detect when access is restored."""

  def lock(
    self, *, validate: bool | None = None
  ) -> StreamManager[LockUpdate, Any, Any]:
    """Notifications when the account is locked or unlocked.

    Use this channel to react to account lock events (e.g., pause trading workflows) and to detect when access is restored.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/subscriptions/user-lock)
    """
    return self.subscribe(
      'user.lock',
      None,
      meta={},
      validate=validate,
      response_type=LockUpdate,
    )
