# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any
from typed_deribit.core.endpoint.stream import StreamEndpoint
from typed_deribit.schemas import Order


class OrdersByInstrumentRaw(StreamEndpoint):
  """User order updates for a specific instrument (raw stream).

  Use this channel to receive private order updates for the given instrument with the highest granularity (raw). Prefer this for real-time order state tracking."""

  def orders_by_instrument_raw(
    self,
    instrument_name: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[Order, Any, Any]:
    """User order updates for a specific instrument (raw stream).

    Use this channel to receive private order updates for the given instrument with the highest granularity (raw). Prefer this for real-time order state tracking.

    Args:
      instrument_name: The name of the instrument
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/subscriptions/user-orders-instrument_name-raw)
    """
    return self.subscribe(
      f'user.orders.{instrument_name}.raw',
      meta={},
      validate=validate,
      response_type=Order,
    )
