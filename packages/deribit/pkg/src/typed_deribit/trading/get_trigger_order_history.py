# Generated by typed-dev — see CONTRIBUTING.md.
"""`private/get_trigger_order_history` — `private/get_trigger_order_history`."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core import RpcEndpoint, TimestampMillis
from typed_core.validation import validator


class TriggerOrderHistoryRecord(TypedDict):
  timestamp: TimestampMillis
  """The timestamp (milliseconds since the Unix epoch)"""
  trigger: Literal['index_price', 'mark_price', 'last_price']
  """Trigger type (only for trigger orders). Allowed values: `"index_price"`, `"mark_price"`, `"last_price"`."""
  trigger_price: float
  """Trigger price (Only for future trigger orders)"""
  trigger_offset: float | None
  """Trailing-stop trigger offset; null unless this is a trailing-stop trigger order."""
  trigger_order_id: str
  """Id of the user order used for the trigger-order reference before triggering"""
  order_id: str | None
  """Id of the user order that was spawned once triggered; null if the trigger order was cancelled before ever triggering."""
  order_state: str
  """Order state: `"triggered"`, `"cancelled"`, or `"rejected"` with rejection reason (e.g. `"rejected:reduce_direction"`)."""
  instrument_name: str
  """Unique instrument identifier"""
  request: str
  """Type of last request performed on the trigger order by user or system. `"cancel"` - when order was cancelled, `"trigger:order"` - when trigger order spawned market or limit order after being triggered"""
  direction: Literal['buy', 'sell']
  """Direction: `buy`, or `sell`"""
  price: float | Literal['market_price'] | None
  """The order price -- a number, the literal "market_price" for a market-type trigger order, or null."""
  amount: float
  """It represents the requested order size. For perpetual and inverse futures the amount is in USD units. For options and linear futures it is the underlying base currency coin."""
  last_update_timestamp: NotRequired[TimestampMillis]
  """The timestamp (milliseconds since the Unix epoch)"""
  reduce_only: bool | None
  """Whether the spawned order would be reduce-only; null if the trigger order was cancelled before ever triggering."""
  post_only: bool | None
  """Whether the spawned order would be post-only; null if the trigger order was cancelled before ever triggering."""
  order_type: Literal['limit', 'market']
  """Requested order type: `"limit` or `"market"`"""
  label: NotRequired[str]
  """User defined label (presented only when previously set for order by user)"""
  is_secondary_oto: NotRequired[bool]
  """`true` if the order is an order that can be triggered by another order, otherwise not present."""
  oco_ref: NotRequired[str]
  """Unique reference that identifies a one_cancels_others (OCO) pair."""
  source: NotRequired[str]
  """Source of the order that is linked to the trigger order."""


class GetTriggerOrderHistoryResult(TypedDict):
  entries: NotRequired[list[TriggerOrderHistoryRecord]]
  """Trigger-order history entries matching the query."""
  continuation: NotRequired[str]
  """Continuation token for pagination."""


validate_get_trigger_order_history = validator[GetTriggerOrderHistoryResult](
  GetTriggerOrderHistoryResult
)


class GetTriggerOrderHistory(RpcEndpoint):
  """`private/get_trigger_order_history`."""

  async def get_trigger_order_history(
    self,
    *,
    currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR'],
    instrument_name: str | None = None,
    count: int | None = None,
    continuation: str | None = None,
    validate: bool | None = None,
  ) -> GetTriggerOrderHistoryResult:
    """Retrieves a detailed log of all trigger orders (stop orders, take-profit orders, etc.) for the authenticated account. The log includes trigger order creation, activation, execution, and cancellation events.

    Results can be filtered by currency and instrument name. Use pagination parameters (`count` and `continuation`) to retrieve large trigger order histories. This is useful for tracking trigger order activity and debugging trigger order behavior.

    Args:
      currency: The currency symbol
      instrument_name: Instrument name
      count: Number of requested items, default - `20`, maximum - `1000`
      continuation: Continuation token for pagination
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/trading/private-get_trigger_order_history)
    """
    params: dict = {
      'currency': currency,
    }
    if instrument_name is not None:
      params['instrument_name'] = instrument_name
    if count is not None:
      params['count'] = count
    if continuation is not None:
      params['continuation'] = continuation
    return await self.authed_request(
      'private/get_trigger_order_history',
      params=params,
      validator=validate_get_trigger_order_history,
      validate=validate,
    )
