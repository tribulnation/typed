# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import OrderTradesResult


class ClosePositionLimitOrder(TypedDict):
  """Limit order parameters for `private/close_position`."""

  instrument_name: str
  """Unique instrument identifier"""
  type: Literal['limit']
  """The order type."""
  price: float
  """Price for the limit order, in the instrument's quote currency."""


class ClosePositionMarketOrder(TypedDict):
  """Market order parameters for `private/close_position`."""

  instrument_name: str
  """Unique instrument identifier"""
  type: Literal['market']
  """The order type."""


Request = ClosePositionLimitOrder | ClosePositionMarketOrder


class ClosePosition(RpcEndpoint):
  """Places a reduce-only order to close an existing position. Reduce-only orders can only reduce or close a position; they cannot open a new position or increase an existing one.

  You can specify whether to use a market or limit order. If using a limit order, provide the price. The order will automatically be set to reduce-only to ensure it only closes the position."""

  async def close_position(
    self,
    close_position_order_params: Request,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> OrderTradesResult:
    """Places a reduce-only order to close an existing position. Reduce-only orders can only reduce or close a position; they cannot open a new position or increase an existing one.

    You can specify whether to use a market or limit order. If using a limit order, provide the price. The order will automatically be set to reduce-only to ensure it only closes the position.

    Args:
      close_position_order_params: Parameters for `private/close_position`, one variant per order `type`: a limit close (`limit`) requires `price`; a `market` close takes no `price`. The resulting order is automatically reduce-only.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/trading/private-close_position)
    """
    return await self.request(
      close_position_order_params,
      method='POST',
      path='private/close_position',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=OrderTradesResult,
    )
