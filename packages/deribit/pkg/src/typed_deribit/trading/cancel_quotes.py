# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import CancelGroup


class Request(TypedDict):
  detailed: NotRequired[bool]
  """When `detailed` is set to `true`, the output format is changed to include a list of all cancelled orders.

**📖 Related Article:** [Detailed Response for Cancel Methods](https://docs.deribit.com/articles/json-rpc-overview#detailed-response-for-cancel-methods)

Default: `false`"""
  freeze_quotes: NotRequired[bool]
  """Whether or not to reject incoming quotes for 1 second after cancelling (`false` by default). Related to `private/mass_quote` request."""
  cancel_type: Literal[
    'delta',
    'quote_set_id',
    'instrument',
    'instrument_kind',
    'currency',
    'currency_pair',
    'all',
  ]
  """Type of cancel criteria."""
  min_delta: NotRequired[float]
  """Min delta to cancel by delta (for `cancel_type`: `delta`)."""
  max_delta: NotRequired[float]
  """Max delta to cancel by delta (for `cancel_type`: `delta`)."""
  quote_set_id: NotRequired[str]
  """Unique identifier for the Quote set."""
  instrument_name: NotRequired[str]
  """Instrument name."""
  kind: NotRequired[
    Literal['future', 'option', 'spot', 'future_combo', 'option_combo', 'combo', 'any']
  ]
  """Instrument kind: `"future"`, `"option"`, `"spot"`, `"future_combo"`, `"option_combo"`, `"combo"` for any combo or `"any"` for all"""
  currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR']
  """Currency, i.e `"BTC"`, `"ETH"`, `"USDC"`"""
  currency_pair: Literal[
    'btc_usd',
    'eth_usd',
    'ada_usdc',
    'algo_usdc',
    'avax_usdc',
    'bch_usdc',
    'bnb_usdc',
    'btc_usdc',
    'btcdvol_usdc',
    'buidl_usdc',
    'doge_usdc',
    'dot_usdc',
    'eurr_usdc',
    'eth_usdc',
    'ethdvol_usdc',
    'hype_usdc',
    'link_usdc',
    'ltc_usdc',
    'near_usdc',
    'paxg_usdc',
    'shib_usdc',
    'sol_usdc',
    'steth_usdc',
    'ton_usdc',
    'trump_usdc',
    'trx_usdc',
    'uni_usdc',
    'usde_usdc',
    'usyc_usdc',
    'xrp_usdc',
    'btc_usdt',
    'eth_usdt',
    'eurr_usdt',
    'sol_usdt',
    'steth_usdt',
    'usdc_usdt',
    'usde_usdt',
    'btc_eurr',
    'btc_usde',
    'btc_usyc',
    'eth_btc',
    'eth_eurr',
    'eth_usde',
    'eth_usyc',
    'steth_eth',
    'paxg_btc',
    'drbfix-btc_usdc',
    'drbfix-eth_usdc',
  ]
  """Index identifier, matches (base) cryptocurrency with quote currency"""


Response = float | list[CancelGroup]


class CancelQuotes(RpcEndpoint):
  """Cancels quotes (mass quote orders) based on various criteria. This method provides flexible options for cancelling quotes:

  - `delta`: Cancels quotes within a delta range defined by `min_delta` and `max_delta`
  - `quote_set_id`: Cancels quotes by a specific Quote Set identifier
  - `instrument`: Cancels all quotes associated with a particular instrument
  - `kind`: Cancels all quotes for a certain instrument kind
  - `currency`: Cancels all quotes in a specified currency
  - `currency_pair`: Cancels all quotes in a specified currency pair
  - `all`: Cancels all quotes

  Use the `detailed` parameter to receive a list of all cancelled quotes."""

  async def cancel_quotes(
    self,
    *,
    detailed: bool | None = None,
    freeze_quotes: bool | None = None,
    cancel_type: Literal[
      'delta',
      'quote_set_id',
      'instrument',
      'instrument_kind',
      'currency',
      'currency_pair',
      'all',
    ],
    min_delta: float | None = None,
    max_delta: float | None = None,
    quote_set_id: str | None = None,
    instrument_name: str | None = None,
    kind: Literal[
      'future', 'option', 'spot', 'future_combo', 'option_combo', 'combo', 'any'
    ]
    | None = None,
    currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR'],
    currency_pair: Literal[
      'btc_usd',
      'eth_usd',
      'ada_usdc',
      'algo_usdc',
      'avax_usdc',
      'bch_usdc',
      'bnb_usdc',
      'btc_usdc',
      'btcdvol_usdc',
      'buidl_usdc',
      'doge_usdc',
      'dot_usdc',
      'eurr_usdc',
      'eth_usdc',
      'ethdvol_usdc',
      'hype_usdc',
      'link_usdc',
      'ltc_usdc',
      'near_usdc',
      'paxg_usdc',
      'shib_usdc',
      'sol_usdc',
      'steth_usdc',
      'ton_usdc',
      'trump_usdc',
      'trx_usdc',
      'uni_usdc',
      'usde_usdc',
      'usyc_usdc',
      'xrp_usdc',
      'btc_usdt',
      'eth_usdt',
      'eurr_usdt',
      'sol_usdt',
      'steth_usdt',
      'usdc_usdt',
      'usde_usdt',
      'btc_eurr',
      'btc_usde',
      'btc_usyc',
      'eth_btc',
      'eth_eurr',
      'eth_usde',
      'eth_usyc',
      'steth_eth',
      'paxg_btc',
      'drbfix-btc_usdc',
      'drbfix-eth_usdc',
    ],
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Cancels quotes (mass quote orders) based on various criteria. This method provides flexible options for cancelling quotes:

    - `delta`: Cancels quotes within a delta range defined by `min_delta` and `max_delta`
    - `quote_set_id`: Cancels quotes by a specific Quote Set identifier
    - `instrument`: Cancels all quotes associated with a particular instrument
    - `kind`: Cancels all quotes for a certain instrument kind
    - `currency`: Cancels all quotes in a specified currency
    - `currency_pair`: Cancels all quotes in a specified currency pair
    - `all`: Cancels all quotes

    Use the `detailed` parameter to receive a list of all cancelled quotes.

    Args:
      detailed: When `detailed` is set to `true`, the output format is changed to include a list of all cancelled orders.

    **📖 Related Article:** [Detailed Response for Cancel Methods](https://docs.deribit.com/articles/json-rpc-overview#detailed-response-for-cancel-methods)

    Default: `false`
      freeze_quotes: Whether or not to reject incoming quotes for 1 second after cancelling (`false` by default). Related to `private/mass_quote` request.
      cancel_type: Type of cancel criteria.
      min_delta: Min delta to cancel by delta (for `cancel_type`: `delta`).
      max_delta: Max delta to cancel by delta (for `cancel_type`: `delta`).
      quote_set_id: Unique identifier for the Quote set.
      instrument_name: Instrument name.
      kind: Instrument kind: `"future"`, `"option"`, `"spot"`, `"future_combo"`, `"option_combo"`, `"combo"` for any combo or `"any"` for all
      currency: Currency, i.e `"BTC"`, `"ETH"`, `"USDC"`
      currency_pair: Index identifier, matches (base) cryptocurrency with quote currency
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/trading/private-cancel_quotes)
    """
    request: Request = Request(
      cancel_type=cancel_type, currency=currency, currency_pair=currency_pair
    )
    if detailed is not None:
      request['detailed'] = detailed
    if freeze_quotes is not None:
      request['freeze_quotes'] = freeze_quotes
    if min_delta is not None:
      request['min_delta'] = min_delta
    if max_delta is not None:
      request['max_delta'] = max_delta
    if quote_set_id is not None:
      request['quote_set_id'] = quote_set_id
    if instrument_name is not None:
      request['instrument_name'] = instrument_name
    if kind is not None:
      request['kind'] = kind
    return await self.request(
      request,
      method='POST',
      path='private/cancel_quotes',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Response,
    )
