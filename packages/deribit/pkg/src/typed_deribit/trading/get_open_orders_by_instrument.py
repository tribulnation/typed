# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import Order


class Request(TypedDict):
  instrument_name: str
  """Unique instrument identifier"""
  type: NotRequired[
    Literal[
      'all',
      'limit',
      'trigger_all',
      'stop_all',
      'stop_limit',
      'stop_market',
      'take_all',
      'take_limit',
      'take_market',
      'trailing_all',
      'trailing_stop',
    ]
  ]
  """Order type: `"all"`, `"limit"`, `"trigger_all"`, `"stop_all"`, `"stop_limit"`, `"stop_market"`, `"take_all"`, `"take_limit"`, `"take_market"`, `"trailing_all"`, `"trailing_stop"`"""


Response = list[Order]


class GetOpenOrdersByInstrument(RpcEndpoint):
  """Retrieves a list of all open orders for a specific instrument. Open orders are orders that have been placed but not yet filled or cancelled.

  Results can be filtered by order type. This method is useful for checking active orders for a particular instrument."""

  async def get_open_orders_by_instrument(
    self,
    instrument_name: str,
    *,
    type: Literal[
      'all',
      'limit',
      'trigger_all',
      'stop_all',
      'stop_limit',
      'stop_market',
      'take_all',
      'take_limit',
      'take_market',
      'trailing_all',
      'trailing_stop',
    ]
    | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Retrieves a list of all open orders for a specific instrument. Open orders are orders that have been placed but not yet filled or cancelled.

    Results can be filtered by order type. This method is useful for checking active orders for a particular instrument.

    Args:
      instrument_name: Unique instrument identifier
      type: Order type: `"all"`, `"limit"`, `"trigger_all"`, `"stop_all"`, `"stop_limit"`, `"stop_market"`, `"take_all"`, `"take_limit"`, `"take_market"`, `"trailing_all"`, `"trailing_stop"`
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/trading/private-get_open_orders_by_instrument)
    """
    request: Request = Request(instrument_name=instrument_name)
    if type is not None:
      request['type'] = type
    return await self.request(
      request,
      method='POST',
      path='private/get_open_orders_by_instrument',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Response,
    )
