# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import Order


class Request(TypedDict):
  currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR']
  """Currency, i.e `"BTC"`, `"ETH"`, `"USDC"`"""
  label: str
  """user defined label for the order (maximum 64 characters)"""


Response = list[Order]


class GetOpenOrdersByLabel(RpcEndpoint):
  """Retrieves a list of all open orders that have a specific label within a given currency. This is useful for tracking and managing groups of related orders that share the same label.

  Open orders are orders that have been placed but not yet filled or cancelled. The label helps organize and identify related orders."""

  async def get_open_orders_by_label(
    self,
    currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR'],
    *,
    label: str,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Retrieves a list of all open orders that have a specific label within a given currency. This is useful for tracking and managing groups of related orders that share the same label.

    Open orders are orders that have been placed but not yet filled or cancelled. The label helps organize and identify related orders.

    Args:
      currency: Currency, i.e `"BTC"`, `"ETH"`, `"USDC"`
      label: user defined label for the order (maximum 64 characters)
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/trading/private-get_open_orders_by_label)
    """
    return await self.request(
      Request(currency=currency, label=label),
      method='POST',
      path='private/get_open_orders_by_label',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Response,
    )
