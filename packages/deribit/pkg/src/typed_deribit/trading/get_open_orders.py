# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import Order


class Request(TypedDict):
  kind: NotRequired[Literal['future', 'option', 'spot', 'future_combo', 'option_combo']]
  """Instrument kind: `"future"`, `"option"`, `"spot"`, `"future_combo"`, `"option_combo"`"""
  type: NotRequired[
    Literal[
      'all',
      'limit',
      'trigger_all',
      'stop_all',
      'stop_limit',
      'stop_market',
      'take_all',
      'take_limit',
      'take_market',
      'trailing_all',
      'trailing_stop',
    ]
  ]
  """Order type: `"all"`, `"limit"`, `"trigger_all"`, `"stop_all"`, `"stop_limit"`, `"stop_market"`, `"take_all"`, `"take_limit"`, `"take_market"`, `"trailing_all"`, `"trailing_stop"`"""


Response = list[Order]


class GetOpenOrders(RpcEndpoint):
  """Retrieves a list of all open orders for the authenticated account across all currencies. Open orders are orders that have been placed but not yet filled or cancelled.

  Results can be filtered by instrument kind and order type. This method provides a comprehensive view of all active orders."""

  async def get_open_orders(
    self,
    kind: Literal['future', 'option', 'spot', 'future_combo', 'option_combo']
    | None = None,
    *,
    type: Literal[
      'all',
      'limit',
      'trigger_all',
      'stop_all',
      'stop_limit',
      'stop_market',
      'take_all',
      'take_limit',
      'take_market',
      'trailing_all',
      'trailing_stop',
    ]
    | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Retrieves a list of all open orders for the authenticated account across all currencies. Open orders are orders that have been placed but not yet filled or cancelled.

    Results can be filtered by instrument kind and order type. This method provides a comprehensive view of all active orders.

    Args:
      kind: Instrument kind: `"future"`, `"option"`, `"spot"`, `"future_combo"`, `"option_combo"`
      type: Order type: `"all"`, `"limit"`, `"trigger_all"`, `"stop_all"`, `"stop_limit"`, `"stop_market"`, `"take_all"`, `"take_limit"`, `"take_market"`, `"trailing_all"`, `"trailing_stop"`
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/trading/private-get_open_orders)
    """
    request: Request = Request()
    if kind is not None:
      request['kind'] = kind
    if type is not None:
      request['type'] = type
    return await self.request(
      request,
      method='POST',
      path='private/get_open_orders',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Response,
    )
