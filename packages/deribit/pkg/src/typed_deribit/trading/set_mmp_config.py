# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import MmpConfigEntry


class Request(TypedDict):
  index_name: Literal[
    'btc_usd',
    'eth_usd',
    'btc_usdc',
    'eth_usdc',
    'ada_usdc',
    'algo_usdc',
    'avax_usdc',
    'bch_usdc',
    'bnb_usdc',
    'doge_usdc',
    'dot_usdc',
    'hype_usdc',
    'link_usdc',
    'ltc_usdc',
    'near_usdc',
    'paxg_usdc',
    'shib_usdc',
    'sol_usdc',
    'ton_usdc',
    'trx_usdc',
    'trump_usdc',
    'uni_usdc',
    'xrp_usdc',
    'usde_usdc',
    'buidl_usdc',
    'btcdvol_usdc',
    'ethdvol_usdc',
    'btc_usdt',
    'eth_usdt',
    'all',
  ]
  """Index identifier of derivative instrument on the platform"""
  interval: int
  """The duration of the monitoring window in seconds. For example, an `interval` of `3` implies a 3-second window.

The `interval` begins after the first trade. If a new trade is executed after the `interval` has ended, a new `interval` is started, and counters reset. If a trade occurs during an already running `interval`, that `interval` continues unaffected.

This mechanism allows the platform to track activity in short, rolling windows to identify potentially risky trading behavior.

If set to `0`, the entire MMP configuration entry for this `index_name` (and `mmp_group`) is removed. This also releases any Initial Margin that was reserved by `max_quote_quantity`.

Maximum value: `3600` seconds (1 hour)."""
  frozen_time: int
  """Time in seconds that MMP remains active after being triggered. Once this frozen period has passed, MMP will automatically reset, allowing new orders to be submitted.

If you want to disable automatic reset, set `frozen_time` to `0`. In that case, a manual reset is required using the `private/reset_mmp` method.

Manual reset is also possible during the frozen time period.

Maximum value: `3600` seconds (1 hour)."""
  mmp_group: NotRequired[str]
  """Designates the MMP group for which the configuration is being set. If the specified group is already associated with a different `index_name`, an error is returned. This parameter enables distinct configurations for each MMP group, linked to particular `index_name`. Maximum 64 characters. Case sensitive.

**Omitting this parameter** targets the **orders MMP group** — the default group used for regular orders with the `mmp` flag set. This is not an error or an incomplete request.

Passing an explicit empty string (`""`) is **not** allowed and returns an error. Only omitting the parameter reaches the orders MMP group.

**📖 Related Article:** [Mass Quotes Specifications](https://docs.deribit.com/articles/mass-quotes-specifications)"""
  quantity_limit: NotRequired[float]
  """The total traded quantity, measured in units of the base currency (e.g., BTC in `BTC-PERPETUAL`), within the `interval`.

This count is direction-agnostic—a buy followed by a sell counts double.

Example: Buy `10` BTC and sell `10` BTC = `20` total quantity.

Applicable to both options and futures.

Positive value with maximum 4 decimal places."""
  delta_limit: NotRequired[float]
  """The maximum allowable net transaction delta change during the `interval`.

Expressed in units of base currency.

The `delta_limit` is treated as an absolute threshold: e.g., `delta_limit: 10` → MMP is triggered if net transaction delta exceeds `+10` or drops below `-10`.

Direction matters: buying `+5` delta and selling `−5` delta cancels out if within the same `interval`.

**Note:** Note that we use the net transaction delta instead of delta. Net Transaction Delta = `Delta - Mark Price`. In the rest of this document, "delta" actually refers to net transaction delta.

Positive value with maximum 4 decimal places."""
  vega_limit: NotRequired[float]
  """The maximum change in vega exposure allowed within a given `interval`, measured in absolute terms.

Expressed in USD, representing the change in sensitivity to implied volatility across executed trades.

This parameter is primarily relevant for options traders managing risk in volatile markets.

Similar to `delta_limit`, the `vega_limit` is direction-aware and evaluated on a net basis. If the exposure exceeds the set threshold (positively or negatively), MMP will be triggered.

**Notice:** When evaluating Delta and Vega limits for MMP, Deribit uses the greeks at the moment of trade execution. The system does not re-evaluate Delta or Vega using live greeks at the time of MMP checking.

Positive value with maximum 4 decimal places."""
  max_quote_quantity: float
  """Maximum Quote Quantity (MQQ) in base currency. MQQ is configured per index but enforced per side, per order book (instrument) — the total combined size of open MMP orders per side per instrument cannot exceed MQQ. **See response description for detailed information about MQQ behavior and limitations.** Maximum 4 decimal places."""
  block_rfq: NotRequired[bool]
  """If true, configures MMP for Block RFQ. When set, requires `block_rfq` scope instead of `trade` scope. Block RFQ MMP settings are completely separate from normal order/quote MMP settings."""
  trade_count_limit: NotRequired[int]
  """For Block RFQ only (`block_rfq` = `true`). Sets the maximum number of Block RFQ trades allowed in the lookback window. Each RFQ trade counts as `+1` towards the limit (not individual legs). Works across all currency pairs. When using this parameter, `index_name` must be set to `"all"`. Maximum - `1000`."""


Response = list[MmpConfigEntry]


class SetMmpConfig(RpcEndpoint):
  """Configures Market Maker Protection (MMP) for a specific index. This method sets the monitoring window, freeze duration, and exposure limits (quantity, delta, vega, and Maximum Quote Quantity).

  At least one limit parameter must be set. Maximum Quote Quantity (MQQ) is a required parameter that limits the total combined size of open MMP orders. MQQ is configured per index but enforced per side, per order book (instrument).

  The `interval` parameter defines the monitoring window duration in seconds. The `frozen_time` parameter sets how long MMP remains active after being triggered. Set `frozen_time` to `0` to disable automatic reset (manual reset required).

  For Mass Quotes, use the `mmp_group` parameter to configure MMP for a specific group. Set `block_rfq` to `true` to configure MMP for Block RFQ (requires `block_rfq:read_write` scope). Set `interval` to `0` to remove MMP configuration.

   or `block_rfq:read_write` (when `block_rfq` = `true`)"""

  async def set_mmp_config(
    self,
    index_name: Literal[
      'btc_usd',
      'eth_usd',
      'btc_usdc',
      'eth_usdc',
      'ada_usdc',
      'algo_usdc',
      'avax_usdc',
      'bch_usdc',
      'bnb_usdc',
      'doge_usdc',
      'dot_usdc',
      'hype_usdc',
      'link_usdc',
      'ltc_usdc',
      'near_usdc',
      'paxg_usdc',
      'shib_usdc',
      'sol_usdc',
      'ton_usdc',
      'trx_usdc',
      'trump_usdc',
      'uni_usdc',
      'xrp_usdc',
      'usde_usdc',
      'buidl_usdc',
      'btcdvol_usdc',
      'ethdvol_usdc',
      'btc_usdt',
      'eth_usdt',
      'all',
    ],
    *,
    interval: int,
    frozen_time: int,
    mmp_group: str | None = None,
    quantity_limit: float | None = None,
    delta_limit: float | None = None,
    vega_limit: float | None = None,
    max_quote_quantity: float,
    block_rfq: bool | None = None,
    trade_count_limit: int | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Configures Market Maker Protection (MMP) for a specific index. This method sets the monitoring window, freeze duration, and exposure limits (quantity, delta, vega, and Maximum Quote Quantity).

    At least one limit parameter must be set. Maximum Quote Quantity (MQQ) is a required parameter that limits the total combined size of open MMP orders. MQQ is configured per index but enforced per side, per order book (instrument).

    The `interval` parameter defines the monitoring window duration in seconds. The `frozen_time` parameter sets how long MMP remains active after being triggered. Set `frozen_time` to `0` to disable automatic reset (manual reset required).

    For Mass Quotes, use the `mmp_group` parameter to configure MMP for a specific group. Set `block_rfq` to `true` to configure MMP for Block RFQ (requires `block_rfq:read_write` scope). Set `interval` to `0` to remove MMP configuration.

     or `block_rfq:read_write` (when `block_rfq` = `true`)

    Args:
      index_name: Index identifier of derivative instrument on the platform
      interval: The duration of the monitoring window in seconds. For example, an `interval` of `3` implies a 3-second window.

    The `interval` begins after the first trade. If a new trade is executed after the `interval` has ended, a new `interval` is started, and counters reset. If a trade occurs during an already running `interval`, that `interval` continues unaffected.

    This mechanism allows the platform to track activity in short, rolling windows to identify potentially risky trading behavior.

    If set to `0`, the entire MMP configuration entry for this `index_name` (and `mmp_group`) is removed. This also releases any Initial Margin that was reserved by `max_quote_quantity`.

    Maximum value: `3600` seconds (1 hour).
      frozen_time: Time in seconds that MMP remains active after being triggered. Once this frozen period has passed, MMP will automatically reset, allowing new orders to be submitted.

    If you want to disable automatic reset, set `frozen_time` to `0`. In that case, a manual reset is required using the `private/reset_mmp` method.

    Manual reset is also possible during the frozen time period.

    Maximum value: `3600` seconds (1 hour).
      mmp_group: Designates the MMP group for which the configuration is being set. If the specified group is already associated with a different `index_name`, an error is returned. This parameter enables distinct configurations for each MMP group, linked to particular `index_name`. Maximum 64 characters. Case sensitive.

    **Omitting this parameter** targets the **orders MMP group** — the default group used for regular orders with the `mmp` flag set. This is not an error or an incomplete request.

    Passing an explicit empty string (`""`) is **not** allowed and returns an error. Only omitting the parameter reaches the orders MMP group.

    **📖 Related Article:** [Mass Quotes Specifications](https://docs.deribit.com/articles/mass-quotes-specifications)
      quantity_limit: The total traded quantity, measured in units of the base currency (e.g., BTC in `BTC-PERPETUAL`), within the `interval`.

    This count is direction-agnostic—a buy followed by a sell counts double.

    Example: Buy `10` BTC and sell `10` BTC = `20` total quantity.

    Applicable to both options and futures.

    Positive value with maximum 4 decimal places.
      delta_limit: The maximum allowable net transaction delta change during the `interval`.

    Expressed in units of base currency.

    The `delta_limit` is treated as an absolute threshold: e.g., `delta_limit: 10` → MMP is triggered if net transaction delta exceeds `+10` or drops below `-10`.

    Direction matters: buying `+5` delta and selling `−5` delta cancels out if within the same `interval`.

    **Note:** Note that we use the net transaction delta instead of delta. Net Transaction Delta = `Delta - Mark Price`. In the rest of this document, "delta" actually refers to net transaction delta.

    Positive value with maximum 4 decimal places.
      vega_limit: The maximum change in vega exposure allowed within a given `interval`, measured in absolute terms.

    Expressed in USD, representing the change in sensitivity to implied volatility across executed trades.

    This parameter is primarily relevant for options traders managing risk in volatile markets.

    Similar to `delta_limit`, the `vega_limit` is direction-aware and evaluated on a net basis. If the exposure exceeds the set threshold (positively or negatively), MMP will be triggered.

    **Notice:** When evaluating Delta and Vega limits for MMP, Deribit uses the greeks at the moment of trade execution. The system does not re-evaluate Delta or Vega using live greeks at the time of MMP checking.

    Positive value with maximum 4 decimal places.
      max_quote_quantity: Maximum Quote Quantity (MQQ) in base currency. MQQ is configured per index but enforced per side, per order book (instrument) — the total combined size of open MMP orders per side per instrument cannot exceed MQQ. **See response description for detailed information about MQQ behavior and limitations.** Maximum 4 decimal places.
      block_rfq: If true, configures MMP for Block RFQ. When set, requires `block_rfq` scope instead of `trade` scope. Block RFQ MMP settings are completely separate from normal order/quote MMP settings.
      trade_count_limit: For Block RFQ only (`block_rfq` = `true`). Sets the maximum number of Block RFQ trades allowed in the lookback window. Each RFQ trade counts as `+1` towards the limit (not individual legs). Works across all currency pairs. When using this parameter, `index_name` must be set to `"all"`. Maximum - `1000`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/trading/private-set_mmp_config)
    """
    request: Request = Request(
      index_name=index_name,
      interval=interval,
      frozen_time=frozen_time,
      max_quote_quantity=max_quote_quantity,
    )
    if mmp_group is not None:
      request['mmp_group'] = mmp_group
    if quantity_limit is not None:
      request['quantity_limit'] = quantity_limit
    if delta_limit is not None:
      request['delta_limit'] = delta_limit
    if vega_limit is not None:
      request['vega_limit'] = vega_limit
    if block_rfq is not None:
      request['block_rfq'] = block_rfq
    if trade_count_limit is not None:
      request['trade_count_limit'] = trade_count_limit
    return await self.request(
      request,
      method='POST',
      path='private/set_mmp_config',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Response,
    )
