# Generated by typed-dev — see CONTRIBUTING.md.
"""`private/move_positions` — `private/move_positions`."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_deribit.core import RpcEndpoint


class PositionMoveTrade(TypedDict):
  source_uid: NotRequired[int]
  """Trade source uid"""
  target_uid: NotRequired[int]
  """Trade target uid"""
  instrument_name: str
  """Unique instrument identifier"""
  direction: Literal['buy', 'sell']
  """Direction: `buy`, or `sell`"""
  price: float
  """Price in base currency"""
  amount: float
  """Trade amount. For perpetual and inverse futures the amount is in USD units. For options and linear futures it is the underlying base currency coin."""


class Trade(TypedDict):
  instrument_name: NotRequired[str]
  """Unique instrument identifier"""
  price: NotRequired[float]
  """Price for trade - if not provided average price of the position is used"""
  amount: NotRequired[float]
  """It represents the requested trade size. For perpetual and inverse futures the amount is in USD units. For options and linear futures it is the underlying base currency coin. Amount can't exceed position size."""


class PositionMove(TypedDict):
  trades: list[PositionMoveTrade]
  """Trades generated immediately by this call (empty if nothing filled yet)."""


validate_move_positions = validator[PositionMove](PositionMove)


class MovePositions(RpcEndpoint):
  """`private/move_positions`."""

  async def move_positions(
    self,
    *,
    currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR'] | None = None,
    source_uid: int,
    target_uid: int,
    trades: list[Trade],
    validate: bool | None = None,
  ) -> PositionMove:
    """Moves positions from a source subaccount to a target subaccount. This operation transfers open positions between subaccounts, which is useful for rebalancing or reorganizing trading activities.

    Positions can be filtered by currency. The operation creates trades to transfer positions, which may affect P&L and margin calculations.

    Args:
      currency: The currency symbol
      source_uid: Id of source subaccount. Can be found in `My Account >> Subaccounts` tab
      target_uid: Id of target subaccount. Can be found in `My Account >> Subaccounts` tab
      trades: List of trades for position move
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/trading/private-move_positions)
    """
    params: dict = {
      'source_uid': source_uid,
      'target_uid': target_uid,
      'trades': trades,
    }
    if currency is not None:
      params['currency'] = currency
    return await self.authed_request(
      'private/move_positions',
      params=params,
      validator=validate_move_positions,
      validate=validate,
    )
