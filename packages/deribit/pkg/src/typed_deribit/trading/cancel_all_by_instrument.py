# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import CancelGroup


class Request(TypedDict):
  instrument_name: str
  """Unique instrument identifier"""
  type: NotRequired[
    Literal['all', 'limit', 'trigger_all', 'stop', 'take', 'trailing_stop']
  ]
  """Order type: `"all"`, `"limit"`, `"stop"`, `"take"`, `"trailing_stop"`, `"trailing_take"`"""
  detailed: NotRequired[bool]
  """When `detailed` is set to `true`, the output format is changed to include a list of all cancelled orders.

**📖 Related Article:** [Detailed Response for Cancel Methods](https://docs.deribit.com/articles/json-rpc-overview#detailed-response-for-cancel-methods)

Default: `false`"""
  include_combos: NotRequired[bool]
  """When set to `true` orders in combo instruments affecting a given position will also be cancelled. Default: `false`"""
  freeze_quotes: NotRequired[bool]
  """Whether or not to reject incoming quotes for 1 second after cancelling (`false` by default). Related to `private/mass_quote` request."""


Response = float | list[CancelGroup]


class CancelAllByInstrument(RpcEndpoint):
  """Cancels all open orders for a specific instrument. This is useful for quickly clearing all orders for a single instrument.

  Orders can be optionally filtered by order type (limit, market, stop, etc.). Use the `detailed` parameter to receive a list of all cancelled orders. The `include_combos` parameter can be used to include combo orders in the cancellation."""

  async def cancel_all_by_instrument(
    self,
    instrument_name: str,
    *,
    type: Literal['all', 'limit', 'trigger_all', 'stop', 'take', 'trailing_stop']
    | None = None,
    detailed: bool | None = None,
    include_combos: bool | None = None,
    freeze_quotes: bool | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Cancels all open orders for a specific instrument. This is useful for quickly clearing all orders for a single instrument.

    Orders can be optionally filtered by order type (limit, market, stop, etc.). Use the `detailed` parameter to receive a list of all cancelled orders. The `include_combos` parameter can be used to include combo orders in the cancellation.

    Args:
      instrument_name: Unique instrument identifier
      type: Order type: `"all"`, `"limit"`, `"stop"`, `"take"`, `"trailing_stop"`, `"trailing_take"`
      detailed: When `detailed` is set to `true`, the output format is changed to include a list of all cancelled orders.

    **📖 Related Article:** [Detailed Response for Cancel Methods](https://docs.deribit.com/articles/json-rpc-overview#detailed-response-for-cancel-methods)

    Default: `false`
      include_combos: When set to `true` orders in combo instruments affecting a given position will also be cancelled. Default: `false`
      freeze_quotes: Whether or not to reject incoming quotes for 1 second after cancelling (`false` by default). Related to `private/mass_quote` request.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/trading/private-cancel_all_by_instrument)
    """
    request: Request = Request(instrument_name=instrument_name)
    if type is not None:
      request['type'] = type
    if detailed is not None:
      request['detailed'] = detailed
    if include_combos is not None:
      request['include_combos'] = include_combos
    if freeze_quotes is not None:
      request['freeze_quotes'] = freeze_quotes
    return await self.request(
      request,
      method='POST',
      path='private/cancel_all_by_instrument',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Response,
    )
