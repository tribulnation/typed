# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import UserTrade


class Request(TypedDict):
  order_id: str
  """Unique order identifier"""
  sorting: NotRequired[Literal['asc', 'desc', 'default']]
  """Direction of results sorting (`default` value means no sorting, results will be returned in order in which they left the database)"""
  historical: NotRequired[bool]
  """Determines whether historical trade and order records should be retrieved.

- `false` (default): Returns recent records: orders for 30 min, trades for 24h.
- `true`: Fetches historical records, available after a short delay due to indexing. Recent data is not included.

**📖 Related Article:** [Accessing Historical Trades and Orders Using API](https://docs.deribit.com/articles/accessing-historical-trades-orders)"""
  subaccount_id: NotRequired[int]
  """Id of a subaccount"""


Response = list[UserTrade]


class GetUserTradesByOrder(RpcEndpoint):
  """Retrieves all trades that were executed from a specific order. When an order is filled, it may result in multiple trades (partial fills). This method returns all trades associated with a given order ID.

  Results can be sorted in ascending or descending order by trade ID. Use `historical` to retrieve historical trade data. This is useful for tracking how an order was filled and analyzing execution quality.

  Main accounts may use the `subaccount_id` parameter to retrieve trade data for a specific subaccount (requires `mainaccount` scope)."""

  async def get_user_trades_by_order(
    self,
    order_id: str,
    *,
    sorting: Literal['asc', 'desc', 'default'] | None = None,
    historical: bool | None = None,
    subaccount_id: int | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Retrieves all trades that were executed from a specific order. When an order is filled, it may result in multiple trades (partial fills). This method returns all trades associated with a given order ID.

    Results can be sorted in ascending or descending order by trade ID. Use `historical` to retrieve historical trade data. This is useful for tracking how an order was filled and analyzing execution quality.

    Main accounts may use the `subaccount_id` parameter to retrieve trade data for a specific subaccount (requires `mainaccount` scope).

    Args:
      order_id: Unique order identifier
      sorting: Direction of results sorting (`default` value means no sorting, results will be returned in order in which they left the database)
      historical: Determines whether historical trade and order records should be retrieved.

    - `false` (default): Returns recent records: orders for 30 min, trades for 24h.
    - `true`: Fetches historical records, available after a short delay due to indexing. Recent data is not included.

    **📖 Related Article:** [Accessing Historical Trades and Orders Using API](https://docs.deribit.com/articles/accessing-historical-trades-orders)
      subaccount_id: Id of a subaccount
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/trading/private-get_user_trades_by_order)
    """
    request: Request = Request(order_id=order_id)
    if sorting is not None:
      request['sorting'] = sorting
    if historical is not None:
      request['historical'] = historical
    if subaccount_id is not None:
      request['subaccount_id'] = subaccount_id
    return await self.request(
      request,
      method='POST',
      path='private/get_user_trades_by_order',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Response,
    )
