# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import Order


class Request(TypedDict):
  currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR']
  """Currency, i.e `"BTC"`, `"ETH"`, `"USDC"`"""
  kind: NotRequired[
    Literal['future', 'option', 'spot', 'future_combo', 'option_combo', 'combo', 'any']
  ]
  """Instrument kind: `"future"`, `"option"`, `"spot"`, `"future_combo"`, `"option_combo"`, `"combo"` for any combo or `"any"` for all"""
  count: NotRequired[int]
  """Number of requested items, default - `20`, maximum - `1000`"""
  offset: NotRequired[int]
  """The offset for pagination, default - `0`"""
  include_old: NotRequired[bool]
  """Include in result orders older than 2 days, default - `false`"""
  include_unfilled: NotRequired[bool]
  """Include in result fully unfilled closed orders, default - `false`"""
  with_continuation: NotRequired[bool]
  """When set to true, the API response format changes from a simple list of orders to an object containing the orders and a continuation token."""
  continuation: NotRequired[str]
  """Continuation token for pagination"""
  historical: NotRequired[bool]
  """Determines whether historical trade and order records should be retrieved.

- `false` (default): Returns recent records: orders for 30 min, trades for 24h.
- `true`: Fetches historical records, available after a short delay due to indexing. Recent data is not included.

**📖 Related Article:** [Accessing Historical Trades and Orders Using API](https://docs.deribit.com/articles/accessing-historical-trades-orders)"""


Response = list[Order]


class GetOrderHistoryByCurrency(RpcEndpoint):
  """Retrieves the order history for a specific currency. The history includes orders that have been partially or fully filled, as well as cancelled orders (if `include_unfilled_orders` is set to true).

  Results can be filtered by instrument kind and paginated using `offset` and `count` parameters, or using continuation tokens. Use `include_old_orders` to include orders from before a certain date, and `historical` to retrieve historical order data."""

  async def get_order_history_by_currency(
    self,
    currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR'],
    *,
    kind: Literal[
      'future', 'option', 'spot', 'future_combo', 'option_combo', 'combo', 'any'
    ]
    | None = None,
    count: int | None = None,
    offset: int | None = None,
    include_old: bool | None = None,
    include_unfilled: bool | None = None,
    with_continuation: bool | None = None,
    continuation: str | None = None,
    historical: bool | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Retrieves the order history for a specific currency. The history includes orders that have been partially or fully filled, as well as cancelled orders (if `include_unfilled_orders` is set to true).

    Results can be filtered by instrument kind and paginated using `offset` and `count` parameters, or using continuation tokens. Use `include_old_orders` to include orders from before a certain date, and `historical` to retrieve historical order data.

    Args:
      currency: Currency, i.e `"BTC"`, `"ETH"`, `"USDC"`
      kind: Instrument kind: `"future"`, `"option"`, `"spot"`, `"future_combo"`, `"option_combo"`, `"combo"` for any combo or `"any"` for all
      count: Number of requested items, default - `20`, maximum - `1000`
      offset: The offset for pagination, default - `0`
      include_old: Include in result orders older than 2 days, default - `false`
      include_unfilled: Include in result fully unfilled closed orders, default - `false`
      with_continuation: When set to true, the API response format changes from a simple list of orders to an object containing the orders and a continuation token.
      continuation: Continuation token for pagination
      historical: Determines whether historical trade and order records should be retrieved.

    - `false` (default): Returns recent records: orders for 30 min, trades for 24h.
    - `true`: Fetches historical records, available after a short delay due to indexing. Recent data is not included.

    **📖 Related Article:** [Accessing Historical Trades and Orders Using API](https://docs.deribit.com/articles/accessing-historical-trades-orders)
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/trading/private-get_order_history_by_currency)
    """
    request: Request = Request(currency=currency)
    if kind is not None:
      request['kind'] = kind
    if count is not None:
      request['count'] = count
    if offset is not None:
      request['offset'] = offset
    if include_old is not None:
      request['include_old'] = include_old
    if include_unfilled is not None:
      request['include_unfilled'] = include_unfilled
    if with_continuation is not None:
      request['with_continuation'] = with_continuation
    if continuation is not None:
      request['continuation'] = continuation
    if historical is not None:
      request['historical'] = historical
    return await self.request(
      request,
      method='POST',
      path='private/get_order_history_by_currency',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Response,
    )
