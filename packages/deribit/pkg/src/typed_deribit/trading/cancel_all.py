# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import CancelGroup


class Request(TypedDict):
  detailed: NotRequired[bool]
  """When `detailed` is set to `true`, the output format is changed to include a list of all cancelled orders.

**📖 Related Article:** [Detailed Response for Cancel Methods](https://docs.deribit.com/articles/json-rpc-overview#detailed-response-for-cancel-methods)

Default: `false`"""
  freeze_quotes: NotRequired[bool]
  """Whether or not to reject incoming quotes for 1 second after cancelling (`false` by default). Related to `private/mass_quote` request."""


Response = float | list[CancelGroup]


class CancelAll(RpcEndpoint):
  """Cancels all open orders and trigger orders for the authenticated account across all currencies and instrument kinds. This is a bulk cancellation operation useful for quickly clearing all active orders.

  Use the `detailed` parameter to receive a list of all cancelled orders. The `freeze_quotes` parameter can be used to freeze quotes instead of cancelling them.

  **Note:** This operation cannot be undone. All open orders will be permanently cancelled."""

  async def cancel_all(
    self,
    *,
    detailed: bool | None = None,
    freeze_quotes: bool | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Cancels all open orders and trigger orders for the authenticated account across all currencies and instrument kinds. This is a bulk cancellation operation useful for quickly clearing all active orders.

    Use the `detailed` parameter to receive a list of all cancelled orders. The `freeze_quotes` parameter can be used to freeze quotes instead of cancelling them.

    **Note:** This operation cannot be undone. All open orders will be permanently cancelled.

    Args:
      detailed: When `detailed` is set to `true`, the output format is changed to include a list of all cancelled orders.

    **📖 Related Article:** [Detailed Response for Cancel Methods](https://docs.deribit.com/articles/json-rpc-overview#detailed-response-for-cancel-methods)

    Default: `false`
      freeze_quotes: Whether or not to reject incoming quotes for 1 second after cancelling (`false` by default). Related to `private/mass_quote` request.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/trading/private-cancel_all)
    """
    request: Request = Request()
    if detailed is not None:
      request['detailed'] = detailed
    if freeze_quotes is not None:
      request['freeze_quotes'] = freeze_quotes
    return await self.request(
      request,
      method='POST',
      path='private/cancel_all',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Response,
    )
