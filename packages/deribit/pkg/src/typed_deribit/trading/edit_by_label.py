# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import OrderTradesResult


class Request(TypedDict):
  label: NotRequired[str]
  """user defined label for the order (maximum 64 characters)"""
  instrument_name: str
  """Unique instrument identifier"""
  amount: NotRequired[float]
  """It represents the requested order size. For perpetual and inverse futures the amount is in USD units. For options and linear futures it is the underlying base currency coin. The `amount` is a mandatory parameter if `contracts` parameter is missing. If both `contracts` and `amount` parameter are passed they must match each other otherwise error is returned."""
  contracts: NotRequired[float]
  """It represents the requested order size in contract units and can be passed instead of `amount`. The `contracts` is a mandatory parameter if `amount` parameter is missing. If both `contracts` and `amount` parameter are passed they must match each other otherwise error is returned."""
  price: NotRequired[float]
  """The order price in base currency.

 When editing an option order with advanced=usd, the field price should be the option price value in USD.

 When editing an option order with advanced=implv, the field price should be a value of implied volatility in percentages. For example,  price=100, means implied volatility of 100%"""
  post_only: NotRequired[bool]
  """If true, the order is considered post-only. If the new price would cause the order to be filled immediately (as taker), the price will be changed to be just below or above the spread (accordingly to the original order type).

 Only valid in combination with time_in_force=`"good_til_cancelled"`"""
  reduce_only: NotRequired[bool]
  """If `true`, the order is considered reduce-only which is intended to only reduce a current position"""
  reject_post_only: NotRequired[bool]
  """If an order is considered post-only and this field is set to true then the order is put to the order book unmodified or the request is rejected.

 Only valid in combination with `"post_only"` set to true"""
  advanced: NotRequired[Literal['usd', 'implv']]
  """advanced type: `"usd"` or `"implv"` (Only for options; field is omitted if not applicable)."""
  trigger_price: NotRequired[float]
  """Trigger price, required for trigger orders only (Stop-loss or Take-profit orders)"""
  mmp: NotRequired[bool]
  """Order MMP flag, only for order_type 'limit'"""
  valid_until: NotRequired[int]
  """Timestamp, when provided server will start processing request in Matching Engine only before given timestamp, in other cases `timed_out` error will be responded. Remember that the given timestamp should be consistent with the server's time, use /public/time (#public-get_time) method to obtain current server time."""


class EditByLabel(RpcEndpoint):
  """Modifies an order identified by its label. This method works only when there is exactly one open order with the specified label.

  You can change the order's price, amount, and/or other properties such as time-in-force, post-only, reduce-only, trigger conditions, or advanced order type. Changes take effect immediately."""

  async def edit_by_label(
    self,
    *,
    label: str | None = None,
    instrument_name: str,
    amount: float | None = None,
    contracts: float | None = None,
    price: float | None = None,
    post_only: bool | None = None,
    reduce_only: bool | None = None,
    reject_post_only: bool | None = None,
    advanced: Literal['usd', 'implv'] | None = None,
    trigger_price: float | None = None,
    mmp: bool | None = None,
    valid_until: int | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> OrderTradesResult:
    """Modifies an order identified by its label. This method works only when there is exactly one open order with the specified label.

    You can change the order's price, amount, and/or other properties such as time-in-force, post-only, reduce-only, trigger conditions, or advanced order type. Changes take effect immediately.

    Args:
      label: user defined label for the order (maximum 64 characters)
      instrument_name: Unique instrument identifier
      amount: It represents the requested order size. For perpetual and inverse futures the amount is in USD units. For options and linear futures it is the underlying base currency coin. The `amount` is a mandatory parameter if `contracts` parameter is missing. If both `contracts` and `amount` parameter are passed they must match each other otherwise error is returned.
      contracts: It represents the requested order size in contract units and can be passed instead of `amount`. The `contracts` is a mandatory parameter if `amount` parameter is missing. If both `contracts` and `amount` parameter are passed they must match each other otherwise error is returned.
      price: The order price in base currency.

     When editing an option order with advanced=usd, the field price should be the option price value in USD.

     When editing an option order with advanced=implv, the field price should be a value of implied volatility in percentages. For example,  price=100, means implied volatility of 100%
      post_only: If true, the order is considered post-only. If the new price would cause the order to be filled immediately (as taker), the price will be changed to be just below or above the spread (accordingly to the original order type).

     Only valid in combination with time_in_force=`"good_til_cancelled"`
      reduce_only: If `true`, the order is considered reduce-only which is intended to only reduce a current position
      reject_post_only: If an order is considered post-only and this field is set to true then the order is put to the order book unmodified or the request is rejected.

     Only valid in combination with `"post_only"` set to true
      advanced: advanced type: `"usd"` or `"implv"` (Only for options; field is omitted if not applicable).
      trigger_price: Trigger price, required for trigger orders only (Stop-loss or Take-profit orders)
      mmp: Order MMP flag, only for order_type 'limit'
      valid_until: Timestamp, when provided server will start processing request in Matching Engine only before given timestamp, in other cases `timed_out` error will be responded. Remember that the given timestamp should be consistent with the server's time, use /public/time (#public-get_time) method to obtain current server time.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/trading/private-edit_by_label)
    """
    request: Request = Request(instrument_name=instrument_name)
    if label is not None:
      request['label'] = label
    if amount is not None:
      request['amount'] = amount
    if contracts is not None:
      request['contracts'] = contracts
    if price is not None:
      request['price'] = price
    if post_only is not None:
      request['post_only'] = post_only
    if reduce_only is not None:
      request['reduce_only'] = reduce_only
    if reject_post_only is not None:
      request['reject_post_only'] = reject_post_only
    if advanced is not None:
      request['advanced'] = advanced
    if trigger_price is not None:
      request['trigger_price'] = trigger_price
    if mmp is not None:
      request['mmp'] = mmp
    if valid_until is not None:
      request['valid_until'] = valid_until
    return await self.request(
      request,
      method='POST',
      path='private/edit_by_label',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=OrderTradesResult,
    )
