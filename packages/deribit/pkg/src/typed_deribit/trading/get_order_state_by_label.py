# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import Order


class Request(TypedDict):
  currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR']
  """Currency, i.e `"BTC"`, `"ETH"`, `"USDC"`"""
  label: str
  """user defined label for the order (maximum 64 characters)"""


Response = list[Order]


class GetOrderStateByLabel(RpcEndpoint):
  """Retrieves the state of recent orders that have a specific label. This is useful for tracking orders that share the same label, which is helpful for managing related orders.

  Results are filtered by currency and label. The response includes order details such as status, filled amount, remaining amount, and other order properties for all orders with the specified label."""

  async def get_order_state_by_label(
    self,
    currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR'],
    *,
    label: str,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Retrieves the state of recent orders that have a specific label. This is useful for tracking orders that share the same label, which is helpful for managing related orders.

    Results are filtered by currency and label. The response includes order details such as status, filled amount, remaining amount, and other order properties for all orders with the specified label.

    Args:
      currency: Currency, i.e `"BTC"`, `"ETH"`, `"USDC"`
      label: user defined label for the order (maximum 64 characters)
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/trading/private-get_order_state_by_label)
    """
    return await self.request(
      Request(currency=currency, label=label),
      method='POST',
      path='private/get_order_state_by_label',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Response,
    )
