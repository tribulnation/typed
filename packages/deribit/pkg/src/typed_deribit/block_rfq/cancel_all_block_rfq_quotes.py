# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  block_rfq_id: NotRequired[int]
  """ID of the Block RFQ."""
  detailed: NotRequired[bool]
  """Change the response shape to include the list of cancelled orders."""


Response = int


class CancelAllBlockRfqQuotes(RpcEndpoint):
  """**Maker method**

  Cancels all of the user's quotes across all Block RFQs. Optionally scoped to a single Block RFQ via `block_rfq_id`. Mass cancellation by label is not supported -- every quote is cancelled regardless of label; use `private/cancel_block_rfq_quote` to cancel a single quote by id or by `block_rfq_id`+`label`.

  Scope: `block_rfq:read_write`."""

  async def cancel_all_block_rfq_quotes(
    self,
    block_rfq_id: int | None = None,
    *,
    detailed: bool | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """**Maker method**

    Cancels all of the user's quotes across all Block RFQs. Optionally scoped to a single Block RFQ via `block_rfq_id`. Mass cancellation by label is not supported -- every quote is cancelled regardless of label; use `private/cancel_block_rfq_quote` to cancel a single quote by id or by `block_rfq_id`+`label`.

    Scope: `block_rfq:read_write`.

    Args:
      block_rfq_id: ID of the Block RFQ.
      detailed: Change the response shape to include the list of cancelled orders.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/block-rfq/private-cancel_all_block_rfq_quotes)
    """
    request: Request = Request()
    if block_rfq_id is not None:
      request['block_rfq_id'] = block_rfq_id
    if detailed is not None:
      request['detailed'] = detailed
    return await self.request(
      request,
      method='POST',
      path='private/cancel_all_block_rfq_quotes',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Response,
    )
