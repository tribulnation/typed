# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core import TimestampMillis
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import BlockRfqHedge, BlockRfqQuoteLeg


class BlockRfqQuote(TypedDict):
  """The quote just added, in its initial `open` state."""

  creation_timestamp: TimestampMillis
  """When the quote was created (ms since epoch)."""
  last_update_timestamp: NotRequired[TimestampMillis]
  """When the quote was last updated (ms since epoch)."""
  block_rfq_id: int
  """ID of the Block RFQ."""
  block_rfq_quote_id: int
  """ID of the newly created quote."""
  quote_state: str
  """State of the quote. No documented enumerated set of values -- left bare per docs/spec/authoring.md rule 2."""
  execution_instruction: NotRequired[Literal['any_part_of', 'all_or_none']]
  """Execution instruction of the quote, echoing the request or its default."""
  price: float
  """Price of the quote."""
  amount: float
  """This value multiplied by the ratio of a leg gives trade size on that leg."""
  direction: Literal['buy', 'sell']
  """Direction of trade from the maker's perspective."""
  filled_amount: NotRequired[float]
  """Filled amount of the quote, `0` immediately after adding."""
  legs: list[BlockRfqQuoteLeg]
  """The instrument legs this quote covers, echoing the request."""
  hedge: NotRequired[BlockRfqHedge]
  replaced: NotRequired[bool]
  """Whether the quote was edited, `false` immediately after adding."""
  label: NotRequired[str]
  """User defined label for the quote, echoing the request."""
  app_name: NotRequired[str]
  """The application that placed the quote on behalf of the user."""


class Request(TypedDict):
  label: NotRequired[str]
  """User defined label for the Block RFQ quote (maximum 64 characters)."""
  block_rfq_id: int
  """ID of the Block RFQ."""
  amount: float
  """The requested order size."""
  direction: Literal['buy', 'sell']
  """Direction: `buy`, or `sell`."""
  legs: list[BlockRfqQuoteLeg]
  """The instrument legs to quote."""
  hedge: NotRequired[str]
  """JSON-encoded object: `{instrument_name, direction, price, amount}`. Only one hedge leg is allowed per Block RFQ."""
  execution_instruction: NotRequired[Literal['all_or_none', 'any_part_of']]
  """`all_or_none` fills entirely or not at all and has priority over `any_part_of` at the same price level; `any_part_of` may fill partially."""
  price: NotRequired[float]
  """Aggregated price used for quoting future spreads."""
  expires_at: NotRequired[TimestampMillis]
  """When the quote expires (ms since epoch)."""


class AddBlockRfqQuote(RpcEndpoint):
  """**Maker method**

  Adds a quote to an existing Block RFQ. Use `combo_books.get_leg_prices` to compute individual leg prices first.

  Scope: `block_rfq:read_write`."""

  async def add_block_rfq_quote(
    self,
    *,
    label: str | None = None,
    block_rfq_id: int,
    amount: float,
    direction: Literal['buy', 'sell'],
    legs: list[BlockRfqQuoteLeg],
    hedge: str | None = None,
    execution_instruction: Literal['all_or_none', 'any_part_of'] | None = None,
    price: float | None = None,
    expires_at: TimestampMillis | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> BlockRfqQuote:
    """**Maker method**

    Adds a quote to an existing Block RFQ. Use `combo_books.get_leg_prices` to compute individual leg prices first.

    Scope: `block_rfq:read_write`.

    Args:
      label: User defined label for the Block RFQ quote (maximum 64 characters).
      block_rfq_id: ID of the Block RFQ.
      amount: The requested order size.
      direction: Direction: `buy`, or `sell`.
      legs: The instrument legs to quote.
      hedge: JSON-encoded object: `{instrument_name, direction, price, amount}`. Only one hedge leg is allowed per Block RFQ.
      execution_instruction: `all_or_none` fills entirely or not at all and has priority over `any_part_of` at the same price level; `any_part_of` may fill partially.
      price: Aggregated price used for quoting future spreads.
      expires_at: When the quote expires (ms since epoch).
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/block-rfq/private-add_block_rfq_quote)
    """
    request: Request = Request(
      block_rfq_id=block_rfq_id, amount=amount, direction=direction, legs=legs
    )
    if label is not None:
      request['label'] = label
    if hedge is not None:
      request['hedge'] = hedge
    if execution_instruction is not None:
      request['execution_instruction'] = execution_instruction
    if price is not None:
      request['price'] = price
    if expires_at is not None:
      request['expires_at'] = expires_at
    return await self.request(
      request,
      method='POST',
      path='private/add_block_rfq_quote',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=BlockRfqQuote,
    )
