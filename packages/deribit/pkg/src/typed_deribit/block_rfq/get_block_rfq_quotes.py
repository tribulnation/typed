# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import BlockRfqQuoteDetails


class Request(TypedDict):
  block_rfq_id: NotRequired[int]
  """ID of the Block RFQ."""
  label: NotRequired[str]
  """User defined label for the Block RFQ quote (maximum 64 characters)."""
  block_rfq_quote_id: NotRequired[int]
  """ID of the Block RFQ quote."""


Response = list[BlockRfqQuoteDetails]


class GetBlockRfqQuotes(RpcEndpoint):
  """**Maker method**

  Retrieves all open quotes for Block RFQs. When `block_rfq_id` is given, only that Block RFQ's open quotes are returned. When `label` is given, all quotes with that label are returned. `block_rfq_quote_id` returns one specific quote.

  Scope: `block_rfq:read`."""

  async def get_block_rfq_quotes(
    self,
    *,
    block_rfq_id: int | None = None,
    label: str | None = None,
    block_rfq_quote_id: int | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """**Maker method**

    Retrieves all open quotes for Block RFQs. When `block_rfq_id` is given, only that Block RFQ's open quotes are returned. When `label` is given, all quotes with that label are returned. `block_rfq_quote_id` returns one specific quote.

    Scope: `block_rfq:read`.

    Args:
      block_rfq_id: ID of the Block RFQ.
      label: User defined label for the Block RFQ quote (maximum 64 characters).
      block_rfq_quote_id: ID of the Block RFQ quote.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/block-rfq/private-get_block_rfq_quotes)
    """
    request: Request = Request()
    if block_rfq_id is not None:
      request['block_rfq_id'] = block_rfq_id
    if label is not None:
      request['label'] = label
    if block_rfq_quote_id is not None:
      request['block_rfq_quote_id'] = block_rfq_quote_id
    return await self.request(
      request,
      method='POST',
      path='private/get_block_rfq_quotes',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Response,
    )
