# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import BlockRfqQuoteDetails


class Request(TypedDict):
  block_rfq_quote_id: NotRequired[int]
  """ID of the Block RFQ quote."""
  label: NotRequired[str]
  """User defined label for the Block RFQ quote (maximum 64 characters)."""
  block_rfq_id: NotRequired[int]
  """ID of the Block RFQ."""


class CancelBlockRfqQuote(RpcEndpoint):
  """**Maker method**

  Cancels a single Block RFQ quote, identified by `block_rfq_quote_id` or by `block_rfq_id`+`label`. Mass cancellation by label is not supported -- this cancels only one quote; use `private/cancel_all_block_rfq_quotes` to cancel all of them.

  Scope: `block_rfq:read_write`."""

  async def cancel_block_rfq_quote(
    self,
    *,
    block_rfq_quote_id: int | None = None,
    label: str | None = None,
    block_rfq_id: int | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> BlockRfqQuoteDetails:
    """**Maker method**

    Cancels a single Block RFQ quote, identified by `block_rfq_quote_id` or by `block_rfq_id`+`label`. Mass cancellation by label is not supported -- this cancels only one quote; use `private/cancel_all_block_rfq_quotes` to cancel all of them.

    Scope: `block_rfq:read_write`.

    Args:
      block_rfq_quote_id: ID of the Block RFQ quote.
      label: User defined label for the Block RFQ quote (maximum 64 characters).
      block_rfq_id: ID of the Block RFQ.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/block-rfq/private-cancel_block_rfq_quote)
    """
    request: Request = Request()
    if block_rfq_quote_id is not None:
      request['block_rfq_quote_id'] = block_rfq_quote_id
    if label is not None:
      request['label'] = label
    if block_rfq_id is not None:
      request['block_rfq_id'] = block_rfq_id
    return await self.request(
      request,
      method='POST',
      path='private/cancel_block_rfq_quote',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=BlockRfqQuoteDetails,
    )
