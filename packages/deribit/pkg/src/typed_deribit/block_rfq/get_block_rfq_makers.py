# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal
from typed_deribit.core.endpoint.rpc import RpcEndpoint


Response = list[str]


class GetBlockRfqMakers(RpcEndpoint):
  """Returns a list of all available Block RFQ makers. Takes no parameters. Use this to see which aliases can be targeted by `private/create_block_rfq`'s `makers` list.

  Scope: `block_rfq:read`."""

  async def get_block_rfq_makers(
    self,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Returns a list of all available Block RFQ makers. Takes no parameters. Use this to see which aliases can be targeted by `private/create_block_rfq`'s `makers` list.

    Scope: `block_rfq:read`.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/block-rfq/private-get_block_rfq_makers)
    """
    return await self.request(
      None,
      method='POST',
      path='private/get_block_rfq_makers',
      meta={},
      validate=validate,
      transport=transport,
      response_type=Response,
    )
