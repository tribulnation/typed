# Generated by typed-dev — see CONTRIBUTING.md.
"""`private/toggle_notifications_from_subaccount` — `private/toggle_notifications_from_subaccount`."""

from typing_extensions import Literal
from typed_core.validation import validator
from typed_deribit.core import RpcEndpoint

validate_toggle_notifications_from_subaccount = validator[Literal['ok']](Literal['ok'])  # pyright: ignore[reportArgumentType]  # fmt: skip


class ToggleNotificationsFromSubaccount(RpcEndpoint):
  """`private/toggle_notifications_from_subaccount`."""

  async def toggle_notifications_from_subaccount(
    self,
    *,
    sid: int,
    state: bool,
    validate: bool | None = None,
  ) -> Literal['ok']:
    """Enables or disables email and other notifications for a subaccount. Only affects notifications sent to the subaccount's own email address; the main account's notifications are unaffected. Requires two-factor authentication on the calling session.

    Args:
      sid: Subaccount user id.
      state: Enable (`true`) or disable (`false`) notifications.
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/account-management/private-toggle_notifications_from_subaccount)
    """
    params: dict = {
      'sid': sid,
      'state': state,
    }
    return await self.authed_request(
      'private/toggle_notifications_from_subaccount',
      params=params,
      validator=validate_toggle_notifications_from_subaccount,
      validate=validate,
    )
