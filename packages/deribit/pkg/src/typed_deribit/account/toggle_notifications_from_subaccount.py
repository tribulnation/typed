# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict, cast
from typed_deribit.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  sid: int
  """Subaccount user id."""
  state: bool
  """Enable (`true`) or disable (`false`) notifications."""


Response = Literal['ok']


class ToggleNotificationsFromSubaccount(RpcEndpoint):
  """Enables or disables email and other notifications for a subaccount. Only affects notifications sent to the subaccount's own email address; the main account's notifications are unaffected. Requires two-factor authentication on the calling session."""

  async def toggle_notifications_from_subaccount(
    self,
    sid: int,
    *,
    state: bool,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Enables or disables email and other notifications for a subaccount. Only affects notifications sent to the subaccount's own email address; the main account's notifications are unaffected. Requires two-factor authentication on the calling session.

    Args:
      sid: Subaccount user id.
      state: Enable (`true`) or disable (`false`) notifications.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/account-management/private-toggle_notifications_from_subaccount)
    """
    return await self.request(
      Request(sid=sid, state=state),
      method='POST',
      path='private/toggle_notifications_from_subaccount',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=cast(type, Response),
    )
