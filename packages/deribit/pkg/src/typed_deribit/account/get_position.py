# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import Position


class Request(TypedDict):
  instrument_name: str
  """Unique instrument identifier."""


class GetPosition(RpcEndpoint):
  """Retrieves the open position for a specific instrument. If no position exists, the response reports a flat (zero-size) position."""

  async def get_position(
    self,
    instrument_name: str,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Position:
    """Retrieves the open position for a specific instrument. If no position exists, the response reports a flat (zero-size) position.

    Args:
      instrument_name: Unique instrument identifier.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/account-management/private-get_position)
    """
    return await self.request(
      Request(instrument_name=instrument_name),
      method='POST',
      path='private/get_position',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=Position,
    )
