# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict, cast
from typed_deribit.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  id: int
  """API key id."""


Response = Literal['ok']


class RemoveApiKey(RpcEndpoint):
  """Permanently deletes an API key. This cannot be undone; any application using the key immediately loses access. Disable a key first (`account.disable_api_key`) if the intent is only to temporarily suspend it."""

  async def remove_api_key(
    self,
    id: int,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Permanently deletes an API key. This cannot be undone; any application using the key immediately loses access. Disable a key first (`account.disable_api_key`) if the intent is only to temporarily suspend it.

    Args:
      id: API key id.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/account-management/private-remove_api_key)
    """
    return await self.request(
      Request(id=id),
      method='POST',
      path='private/remove_api_key',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=cast(type, Response),
    )
