# Generated by typed-dev — see CONTRIBUTING.md.
"""`private/remove_api_key` — `private/remove_api_key`."""

from typing_extensions import Literal
from typed_core.validation import validator
from typed_deribit.core import RpcEndpoint

validate_remove_api_key = validator[Literal['ok']](Literal['ok'])  # pyright: ignore[reportArgumentType]  # fmt: skip


class RemoveApiKey(RpcEndpoint):
  """`private/remove_api_key`."""

  async def remove_api_key(
    self, *, id: int, validate: bool | None = None
  ) -> Literal['ok']:
    """Permanently deletes an API key. This cannot be undone; any application using the key immediately loses access. Disable a key first (`account.disable_api_key`) if the intent is only to temporarily suspend it.

    Args:
      id: API key id.
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/account-management/private-remove_api_key)
    """
    params: dict = {
      'id': id,
    }
    return await self.authed_request(
      'private/remove_api_key',
      params=params,
      validator=validate_remove_api_key,
      validate=validate,
    )
