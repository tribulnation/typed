# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR']
  """Currency, i.e. `"BTC"`, `"ETH"`, `"USDC"`."""
  add_positions: NotRequired[bool]
  """If `true`, adds the simulated positions to the current positions; otherwise uses only the simulated positions. `true` by default."""
  simulated_positions: NotRequired[str]
  """JSON-encoded object of instrument name to simulated position size."""


class SimulatePortfolioResult(TypedDict):
  currency: str
  """Currency of the simulation."""
  equity: float
  """The account's current equity."""
  balance: float
  """The account's balance."""
  margin_balance: float
  """The account's margin balance."""
  initial_margin: float
  """The account's initial margin."""
  maintenance_margin: float
  """The maintenance margin."""
  projected_initial_margin: NotRequired[float]
  """Projected initial margin."""
  projected_maintenance_margin: NotRequired[float]
  """Projected maintenance margin."""
  available_funds: float
  """The account's available funds."""
  available_withdrawal_funds: float
  """The account's available-to-withdraw funds."""
  available_subaccount_transfer_funds: NotRequired[float]
  """The account's available funds for subaccount transfers."""
  total_pl: NotRequired[float]
  """Profit and loss."""
  session_rpl: NotRequired[float]
  """Session realized profit and loss."""
  session_upl: NotRequired[float]
  """Session unrealized profit and loss."""
  futures_pl: NotRequired[float]
  """Futures profit and loss."""
  futures_session_rpl: NotRequired[float]
  """Futures session realized profit and loss."""
  futures_session_upl: NotRequired[float]
  """Futures session unrealized profit and loss."""
  options_pl: NotRequired[float]
  """Options profit and loss."""
  options_session_rpl: NotRequired[float]
  """Options session realized profit and loss."""
  options_session_upl: NotRequired[float]
  """Options session unrealized profit and loss."""
  options_value: NotRequired[float]
  """Options value."""
  options_delta: NotRequired[float]
  """Options summary delta."""
  options_gamma: NotRequired[float]
  """Options summary gamma."""
  options_theta: NotRequired[float]
  """Options summary theta."""
  options_vega: NotRequired[float]
  """Options summary vega."""
  options_gamma_map: NotRequired[dict[str, float]]
  """Map of options' gammas per index."""
  options_theta_map: NotRequired[dict[str, float]]
  """Map of options' thetas per index."""
  options_vega_map: NotRequired[dict[str, float]]
  """Map of options' vegas per index."""
  delta_total: NotRequired[float]
  """The sum of position deltas."""
  delta_total_map: NotRequired[dict[str, float]]
  """Map of total deltas per index."""
  projected_delta_total: NotRequired[float]
  """The sum of position deltas excluding positions expiring in the closest expiration."""
  additional_reserve: NotRequired[float]
  """The account's balance reserved in other orders."""
  spot_reserve: NotRequired[float]
  """The account's balance reserved in active spot orders."""
  fee_balance: NotRequired[float]
  """The account's fee balance."""
  locked_balance: NotRequired[float]
  """The account's locked balance."""
  margin_model: NotRequired[str]
  """Name of the user's currently enabled margin model."""
  portfolio_margining_enabled: NotRequired[bool]
  """Whether portfolio margining is enabled for the user."""
  cross_collateral_enabled: NotRequired[bool]
  """Whether cross collateral is enabled for the user."""
  total_equity_usd: NotRequired[float]
  """Optional (cross-margin users only). Total equity across all cross collateral currencies, in USD."""
  total_initial_margin_usd: NotRequired[float]
  """Optional (cross-margin users only). Total initial margin across all cross collateral currencies, in USD."""
  total_maintenance_margin_usd: NotRequired[float]
  """Optional (cross-margin users only). Total maintenance margin across all cross collateral currencies, in USD."""
  total_margin_balance_usd: NotRequired[float]
  """Optional (cross-margin users only). Total margin balance across all cross collateral currencies, in USD."""
  total_delta_total_usd: NotRequired[float]
  """Optional (cross-margin users only). Total delta total across all cross collateral currencies, in USD."""


class SimulatePortfolio(RpcEndpoint):
  """Calculates portfolio margin requirements and risk metrics for simulated positions, the current portfolio, or both together -- useful for understanding margin impact before opening new positions. Restricted to at most one call per second due to computation cost."""

  async def simulate_portfolio(
    self,
    currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR'],
    *,
    add_positions: bool | None = None,
    simulated_positions: str | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> SimulatePortfolioResult:
    """Calculates portfolio margin requirements and risk metrics for simulated positions, the current portfolio, or both together -- useful for understanding margin impact before opening new positions. Restricted to at most one call per second due to computation cost.

    Args:
      currency: Currency, i.e. `"BTC"`, `"ETH"`, `"USDC"`.
      add_positions: If `true`, adds the simulated positions to the current positions; otherwise uses only the simulated positions. `true` by default.
      simulated_positions: JSON-encoded object of instrument name to simulated position size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/account-management/private-simulate_portfolio)
    """
    request: Request = Request(currency=currency)
    if add_positions is not None:
      request['add_positions'] = add_positions
    if simulated_positions is not None:
      request['simulated_positions'] = simulated_positions
    return await self.request(
      request,
      method='POST',
      path='private/simulate_portfolio',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=SimulatePortfolioResult,
    )
