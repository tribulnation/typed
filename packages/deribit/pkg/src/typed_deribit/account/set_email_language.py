# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict, cast
from typed_deribit.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  language: str
  """Abbreviated language name. The venue documents example values only (`en`, `ko`, `zh`, `ja`, `ru`) in prose, not a closed set -- left un-enum'd, matching `account.get_email_language`'s own read-half spec of the identical field."""


Response = Literal['ok']


class SetEmailLanguage(RpcEndpoint):
  """Sets the preferred language for email notifications sent to the account. Only affects future email notifications, not the web interface or API responses. Not gated behind two-factor authentication -- confirmed live."""

  async def set_email_language(
    self,
    language: str,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Sets the preferred language for email notifications sent to the account. Only affects future email notifications, not the web interface or API responses. Not gated behind two-factor authentication -- confirmed live.

    Args:
      language: Abbreviated language name. The venue documents example values only (`en`, `ko`, `zh`, `ja`, `ru`) in prose, not a closed set -- left un-enum'd, matching `account.get_email_language`'s own read-half spec of the identical field.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/account-management/private-set_email_language)
    """
    return await self.request(
      Request(language=language),
      method='POST',
      path='private/set_email_language',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=cast(type, Response),
    )
