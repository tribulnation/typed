# Generated by typed-dev — see CONTRIBUTING.md.
"""`private/get_email_language` — `private/get_email_language`."""

from typed_core.validation import validator
from typed_deribit.core import RpcEndpoint

validate_get_email_language = validator[str](str)


class GetEmailLanguage(RpcEndpoint):
  """`private/get_email_language`."""

  async def get_email_language(self, *, validate: bool | None = None) -> str:
    """Retrieves the currently configured language preference for email notifications sent to the account. Takes no parameters.

    Args:
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/account-management/private-get_email_language)
    """
    return await self.authed_request(
      'private/get_email_language',
      validator=validate_get_email_language,
      validate=validate,
    )
