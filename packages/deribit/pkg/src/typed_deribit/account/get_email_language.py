# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal
from typed_deribit.core.endpoint.rpc import RpcEndpoint


Response = str


class GetEmailLanguage(RpcEndpoint):
  """Retrieves the currently configured language preference for email notifications sent to the account. Takes no parameters."""

  async def get_email_language(
    self,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Retrieves the currently configured language preference for email notifications sent to the account. Takes no parameters.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/account-management/private-get_email_language)
    """
    return await self.request(
      None,
      method='POST',
      path='private/get_email_language',
      meta={},
      validate=validate,
      transport=transport,
      response_type=Response,
    )
