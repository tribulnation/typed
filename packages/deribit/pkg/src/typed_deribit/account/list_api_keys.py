# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import ApiKey


ListApiKeysResult = list[ApiKey]


class ListApiKeys(RpcEndpoint):
  """Retrieves a list of all API keys associated with the authenticated account, including scope, status, and metadata. Takes no parameters. The venue documents this as requiring two-factor authentication on the calling session."""

  async def list_api_keys(
    self,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> ListApiKeysResult:
    """Retrieves a list of all API keys associated with the authenticated account, including scope, status, and metadata. Takes no parameters. The venue documents this as requiring two-factor authentication on the calling session.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/account-management/private-list_api_keys)
    """
    return await self.request(
      None,
      method='POST',
      path='private/list_api_keys',
      meta={},
      validate=validate,
      transport=transport,
      response_type=ListApiKeysResult,
    )
