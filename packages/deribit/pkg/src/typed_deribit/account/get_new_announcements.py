# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import AccountAnnouncement


GetNewAnnouncementsResult = list[AccountAnnouncement]


class GetNewAnnouncements(RpcEndpoint):
  """Retrieves only unread announcements for the authenticated account. Announcements are marked as read via `set_announcement_as_read` or by viewing them through the web interface. Takes no parameters."""

  async def get_new_announcements(
    self,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> GetNewAnnouncementsResult:
    """Retrieves only unread announcements for the authenticated account. Announcements are marked as read via `set_announcement_as_read` or by viewing them through the web interface. Takes no parameters.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/account-management/private-get_new_announcements)
    """
    return await self.request(
      None,
      method='POST',
      path='private/get_new_announcements',
      meta={},
      validate=validate,
      transport=transport,
      response_type=GetNewAnnouncementsResult,
    )
