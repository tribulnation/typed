# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict, cast
from typed_deribit.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  announcement_id: float
  """Id of the announcement to mark as read."""


Response = Literal['ok']


class SetAnnouncementAsRead(RpcEndpoint):
  """Marks a specific announcement as read. Once marked, it no longer appears in `account.get_new_announcements`, though it remains available through the public `account.get_announcements`."""

  async def set_announcement_as_read(
    self,
    announcement_id: float,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Marks a specific announcement as read. Once marked, it no longer appears in `account.get_new_announcements`, though it remains available through the public `account.get_announcements`.

    Args:
      announcement_id: Id of the announcement to mark as read.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/account-management/private-set_announcement_as_read)
    """
    return await self.request(
      Request(announcement_id=announcement_id),
      method='POST',
      path='private/set_announcement_as_read',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=cast(type, Response),
    )
