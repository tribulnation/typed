# Generated by typed-dev — see CONTRIBUTING.md.
"""`private/set_announcement_as_read` — `private/set_announcement_as_read`."""

from typing_extensions import Literal
from typed_core.validation import validator
from typed_deribit.core import RpcEndpoint

validate_set_announcement_as_read = validator[Literal['ok']](Literal['ok'])  # pyright: ignore[reportArgumentType]  # fmt: skip


class SetAnnouncementAsRead(RpcEndpoint):
  """`private/set_announcement_as_read`."""

  async def set_announcement_as_read(
    self,
    *,
    announcement_id: float,
    validate: bool | None = None,
  ) -> Literal['ok']:
    """Marks a specific announcement as read. Once marked, it no longer appears in `account.get_new_announcements`, though it remains available through the public `account.get_announcements`.

    Args:
      announcement_id: Id of the announcement to mark as read.
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/account-management/private-set_announcement_as_read)
    """
    params: dict = {
      'announcement_id': announcement_id,
    }
    return await self.authed_request(
      'private/set_announcement_as_read',
      params=params,
      validator=validate_set_announcement_as_read,
      validate=validate,
    )
