# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict, cast
from typed_deribit.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  user_id: int
  """Id of the (sub)account to configure."""
  trading_products: list[
    Literal[
      'perpetual', 'futures', 'options', 'future_combos', 'option_combos', 'spots'
    ]
  ]
  """Trading products to disable for the account. Confirmed live that the bare repeated-key encoding this client sends is refused (`value must be a list`); a `trading_products[]` bracket-suffixed key is required -- see this endpoint's `unverified` detail."""


Response = Literal['ok']


class SetDisabledTradingProducts(RpcEndpoint):
  """Configures which trading products are disabled for a (sub)account -- disabled products cannot be traded by that account. Intended for risk management and compliance restrictions on subaccounts. Requires two-factor authentication on the calling session."""

  async def set_disabled_trading_products(
    self,
    user_id: int,
    *,
    trading_products: list[
      Literal[
        'perpetual', 'futures', 'options', 'future_combos', 'option_combos', 'spots'
      ]
    ],
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Configures which trading products are disabled for a (sub)account -- disabled products cannot be traded by that account. Intended for risk management and compliance restrictions on subaccounts. Requires two-factor authentication on the calling session.

    Args:
      user_id: Id of the (sub)account to configure.
      trading_products: Trading products to disable for the account. Confirmed live that the bare repeated-key encoding this client sends is refused (`value must be a list`); a `trading_products[]` bracket-suffixed key is required -- see this endpoint's `unverified` detail.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/account-management/private-set_disabled_trading_products)
    """
    return await self.request(
      Request(user_id=user_id, trading_products=trading_products),
      method='POST',
      path='private/set_disabled_trading_products',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=cast(type, Response),
    )
