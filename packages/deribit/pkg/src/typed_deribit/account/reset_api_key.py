# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import ApiKey


class Request(TypedDict):
  id: int
  """API key id."""


class ResetApiKey(RpcEndpoint):
  """Generates a new secret for an existing API key, keeping the same key id and other properties. The old secret becomes invalid immediately; the new secret is returned once and is not retrievable afterwards."""

  async def reset_api_key(
    self,
    id: int,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> ApiKey:
    """Generates a new secret for an existing API key, keeping the same key id and other properties. The old secret becomes invalid immediately; the new secret is returned once and is not retrievable afterwards.

    Args:
      id: API key id.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/account-management/private-reset_api_key)
    """
    return await self.request(
      Request(id=id),
      method='POST',
      path='private/reset_api_key',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=ApiKey,
    )
