# Generated by typed-dev — see CONTRIBUTING.md.
"""`private/get_subaccounts_details` — `private/get_subaccounts_details`."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core import RpcEndpoint, TimestampMillis
from typed_core.validation import validator


class SubaccountOpenOrder(TypedDict):
  order_id: str
  """Unique order identifier."""
  order_state: Literal[
    'open', 'filled', 'rejected', 'cancelled', 'untriggered', 'triggered'
  ]
  """Order state."""
  order_type: Literal[
    'market',
    'limit',
    'stop_market',
    'stop_limit',
    'take_market',
    'take_limit',
    'trailing_stop',
  ]
  """Order type."""
  time_in_force: Literal[
    'good_til_cancelled', 'good_til_day', 'fill_or_kill', 'immediate_or_cancel'
  ]
  """Order time in force."""
  instrument_name: str
  """Unique instrument identifier."""
  creation_timestamp: TimestampMillis
  """The timestamp (milliseconds since the Unix epoch) the order was created."""
  last_update_timestamp: TimestampMillis
  """The timestamp (milliseconds since the Unix epoch) the order was last updated."""
  direction: Literal['buy', 'sell']
  """Order direction."""
  price: float | Literal['market_price']
  """Price in base currency, or `"market_price"` for open trigger market orders."""
  label: str
  """User-defined label (up to 64 characters)."""
  post_only: bool
  """Whether this is a post-only order."""
  reduce_only: NotRequired[bool]
  """Whether this is a reduce-only order (not present for spot)."""
  api: bool
  """Whether the order was created with the API."""
  amount: NotRequired[float]
  """Requested order size."""
  filled_amount: NotRequired[float]
  """Filled amount of the order."""


class SubaccountPosition(TypedDict):
  instrument_name: str
  """Unique instrument identifier."""
  kind: Literal['future', 'option', 'spot', 'future_combo', 'option_combo']
  """Instrument kind."""
  average_price: float
  """Average price of trades that built this position."""
  direction: Literal['buy', 'sell', 'zero']
  """Position direction."""
  mark_price: float
  """Current mark price for the position's instrument."""
  delta: float
  """Delta parameter."""
  gamma: NotRequired[float]
  """Only for options: gamma parameter."""
  vega: NotRequired[float]
  """Only for options: vega parameter."""
  theta: NotRequired[float]
  """Only for options: theta parameter."""
  index_price: float
  """Current index price."""
  initial_margin: float
  """Initial margin."""
  maintenance_margin: float
  """Maintenance margin."""
  settlement_price: NotRequired[float]
  """Optional (not present for spot). Last settlement price for the position's instrument; 0 if the instrument was not yet settled."""
  total_profit_loss: float
  """Total profit or loss from the position."""
  floating_profit_loss: float
  """Floating (unrealized) profit or loss."""
  realized_profit_loss: float
  """Realized profit or loss."""
  size: float
  """Position size: for futures, in quote currency (e.g. USD); for options, in base currency (e.g. BTC)."""
  size_currency: NotRequired[float]
  """Only for futures: position size in base currency."""
  average_price_usd: NotRequired[float]
  """Only for options: average price in USD."""
  floating_profit_loss_usd: NotRequired[float]
  """Only for options: floating profit or loss in USD."""
  leverage: NotRequired[int]
  """Current available leverage for a future position."""
  realized_funding: NotRequired[float]
  """Realized funding in the current session, included in session realized profit or loss; only for perpetual positions."""
  interest_value: NotRequired[float]
  """Value used to calculate `realized_funding` (perpetual only)."""
  estimated_liquidation_price: NotRequired[float | None]
  """Estimated liquidation price; added only for futures, for users on the `segregated_sm` margin model. Confirmed live: present as `null` (not absent) on a real futures position on a non-`segregated_sm` account, so nullable in addition to optional."""
  open_orders_margin: NotRequired[float]
  """Margin reserved by open orders on this instrument."""
  user_id: NotRequired[int]
  """Id of the account this position belongs to."""


class SubaccountDetail(TypedDict):
  uid: int
  """Account/subaccount identifier."""
  positions: list[SubaccountPosition]
  """This (sub)account's open positions in the requested currency."""
  open_orders: NotRequired[list[SubaccountOpenOrder]]
  """This (sub)account's open orders. Only present when `with_open_orders` is `true`."""


validate_get_subaccounts_details = validator[list[SubaccountDetail]](
  list[SubaccountDetail]
)


class GetSubaccountsDetails(RpcEndpoint):
  """`private/get_subaccounts_details`."""

  async def get_subaccounts_details(
    self,
    *,
    currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR'],
    with_open_orders: bool | None = None,
    validate: bool | None = None,
  ) -> list[SubaccountDetail]:
    """Retrieves position details for all subaccounts (including the main account) for a specific currency. Optionally includes each subaccount's open orders.

    Args:
      currency: The currency symbol.
      with_open_orders: Whether to include each subaccount's open orders list. `false` by default.
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/account-management/private-get_subaccounts_details)
    """
    params: dict = {
      'currency': currency,
    }
    if with_open_orders is not None:
      params['with_open_orders'] = with_open_orders
    return await self.authed_request(
      'private/get_subaccounts_details',
      params=params,
      validator=validate_get_subaccounts_details,
      validate=validate,
    )
