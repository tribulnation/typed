# Generated by typed-dev — see CONTRIBUTING.md.
"""`private/remove_subaccount` — `private/remove_subaccount`."""

from typing_extensions import Literal
from typed_core.validation import validator
from typed_deribit.core import RpcEndpoint

validate_remove_subaccount = validator[Literal['ok']](Literal['ok'])  # pyright: ignore[reportArgumentType]  # fmt: skip


class RemoveSubaccount(RpcEndpoint):
  """`private/remove_subaccount`."""

  async def remove_subaccount(
    self,
    *,
    subaccount_id: int,
    validate: bool | None = None,
  ) -> Literal['ok']:
    """Permanently removes a subaccount. The subaccount must be empty first -- no open positions, no open orders, zero balance -- and this cannot be undone. Requires two-factor authentication on the calling session.

    Args:
      subaccount_id: Subaccount user id.
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/account-management/private-remove_subaccount)
    """
    params: dict = {
      'subaccount_id': subaccount_id,
    }
    return await self.authed_request(
      'private/remove_subaccount',
      params=params,
      validator=validate_remove_subaccount,
      validate=validate,
    )
