# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict, cast
from typed_deribit.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  subaccount_id: int
  """Subaccount user id."""


Response = Literal['ok']


class RemoveSubaccount(RpcEndpoint):
  """Permanently removes a subaccount. The subaccount must be empty first -- no open positions, no open orders, zero balance -- and this cannot be undone. Requires two-factor authentication on the calling session."""

  async def remove_subaccount(
    self,
    subaccount_id: int,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Permanently removes a subaccount. The subaccount must be empty first -- no open positions, no open orders, zero balance -- and this cannot be undone. Requires two-factor authentication on the calling session.

    Args:
      subaccount_id: Subaccount user id.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/account-management/private-remove_subaccount)
    """
    return await self.request(
      Request(subaccount_id=subaccount_id),
      method='POST',
      path='private/remove_subaccount',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=cast(type, Response),
    )
