# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import Position


class Request(TypedDict):
  currency: NotRequired[Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR', 'any']]
  """Currency, i.e. `"BTC"`, `"ETH"`, `"USDC"`, or `"any"`."""
  kind: NotRequired[Literal['future', 'option', 'future_combo', 'option_combo']]
  """Instrument kind."""
  subaccount_id: NotRequired[int]
  """The user id for the subaccount."""


GetPositionsResult = list[Position]


class GetPositions(RpcEndpoint):
  """Retrieves all open positions for the authenticated account, optionally filtered by currency and instrument kind. To retrieve positions for a specific subaccount, use the `subaccount_id` parameter."""

  async def get_positions(
    self,
    currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'EURR', 'any'] | None = None,
    *,
    kind: Literal['future', 'option', 'future_combo', 'option_combo'] | None = None,
    subaccount_id: int | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> GetPositionsResult:
    """Retrieves all open positions for the authenticated account, optionally filtered by currency and instrument kind. To retrieve positions for a specific subaccount, use the `subaccount_id` parameter.

    Args:
      currency: Currency, i.e. `"BTC"`, `"ETH"`, `"USDC"`, or `"any"`.
      kind: Instrument kind.
      subaccount_id: The user id for the subaccount.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/account-management/private-get_positions)
    """
    request: Request = Request()
    if currency is not None:
      request['currency'] = currency
    if kind is not None:
      request['kind'] = kind
    if subaccount_id is not None:
      request['subaccount_id'] = subaccount_id
    return await self.request(
      request,
      method='POST',
      path='private/get_positions',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=GetPositionsResult,
    )
