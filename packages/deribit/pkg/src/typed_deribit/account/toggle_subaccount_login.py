# Generated by typed-dev — see CONTRIBUTING.md.
"""`private/toggle_subaccount_login` — `private/toggle_subaccount_login`."""

from typing_extensions import Literal
from typed_core.validation import validator
from typed_deribit.core import RpcEndpoint

validate_toggle_subaccount_login = validator[Literal['ok']](Literal['ok'])  # pyright: ignore[reportArgumentType]  # fmt: skip


class ToggleSubaccountLogin(RpcEndpoint):
  """`private/toggle_subaccount_login`."""

  async def toggle_subaccount_login(
    self,
    *,
    sid: int,
    state: Literal['enable', 'disable'],
    validate: bool | None = None,
  ) -> Literal['ok']:
    """Enables or disables direct email/password login for a subaccount. When login is disabled, the subaccount is only reachable through the main account; disabling it while an active session exists for the subaccount immediately terminates that session. Requires two-factor authentication on the calling session.

    Args:
      sid: Subaccount user id.
      state: Enable or disable direct login.
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/account-management/private-toggle_subaccount_login)
    """
    params: dict = {
      'sid': sid,
      'state': state,
    }
    return await self.authed_request(
      'private/toggle_subaccount_login',
      params=params,
      validator=validate_toggle_subaccount_login,
      validate=validate,
    )
