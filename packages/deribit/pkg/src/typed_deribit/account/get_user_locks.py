# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint


class UserLock(TypedDict):
  currency: str
  """Currency this lock status applies to."""
  locked: bool
  """Whether the account is locked in this currency."""
  message: NotRequired[str]
  """Optional information for the user on why the account is locked."""


GetUserLocksResult = list[UserLock]


class GetUserLocks(RpcEndpoint):
  """Retrieves information about any account locks or restrictions currently active on the authenticated account, per currency. Some locks may prevent trading, withdrawals, or other account operations. Takes no parameters."""

  async def get_user_locks(
    self,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> GetUserLocksResult:
    """Retrieves information about any account locks or restrictions currently active on the authenticated account, per currency. Some locks may prevent trading, withdrawals, or other account operations. Takes no parameters.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/account-management/private-get_user_locks)
    """
    return await self.request(
      None,
      method='POST',
      path='private/get_user_locks',
      meta={},
      validate=validate,
      transport=transport,
      response_type=GetUserLocksResult,
    )
