# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint


class Member(TypedDict):
  member_id: NotRequired[int]
  """Member identifier. Only present when the request is made from the main account."""
  main_uid: NotRequired[int]
  """User id of the main account the member belongs to. Only present when the request is made from the main account."""
  name: str
  """Name of the member."""
  is_active: bool
  """Whether the member is active. Only active members have Direct Access trading rights."""
  accounts: NotRequired[list[int]]
  """List of (sub)account user ids the member has Direct Access trading rights on. Only present when the request is made from the main account."""
  m_tstamp: NotRequired[int]
  """Timestamp of the last modification of the member (milliseconds since the Unix epoch). Only present when the request is made from the main account."""


class GetMembersResult(TypedDict):
  members: list[Member]
  """Direct Access members configured for the account."""
  is_direct_access_allowed: bool
  """Whether Direct Access trading is enabled for the account."""


class GetMembers(RpcEndpoint):
  """Retrieves the list of Direct Access members configured for the account. A member is an external identity (e.g. a FIX or Direct Access order gateway user) that can be granted Direct Access trading rights on the account or one of its subaccounts. Dedicated to Starbase; see the Starbase Account Model docs. Takes no parameters."""

  async def get_members(
    self,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> GetMembersResult:
    """Retrieves the list of Direct Access members configured for the account. A member is an external identity (e.g. a FIX or Direct Access order gateway user) that can be granted Direct Access trading rights on the account or one of its subaccounts. Dedicated to Starbase; see the Starbase Account Model docs. Takes no parameters.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/account-management/private-get_members)
    """
    return await self.request(
      None,
      method='POST',
      path='private/get_members',
      meta={},
      validate=validate,
      transport=transport,
      response_type=GetMembersResult,
    )
