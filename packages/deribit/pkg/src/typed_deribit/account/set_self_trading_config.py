# Generated by typed-dev — see CONTRIBUTING.md.
"""`private/set_self_trading_config` — `private/set_self_trading_config`."""

from typing_extensions import Literal
from typed_core.validation import validator
from typed_deribit.core import RpcEndpoint

validate_set_self_trading_config = validator[Literal['ok']](Literal['ok'])  # pyright: ignore[reportArgumentType]  # fmt: skip


class SetSelfTradingConfig(RpcEndpoint):
  """`private/set_self_trading_config`."""

  async def set_self_trading_config(
    self,
    *,
    mode: Literal['reject_taker', 'cancel_maker'],
    extended_to_subaccounts: bool,
    block_rfq_self_match_prevention: bool | None = None,
    validate: bool | None = None,
  ) -> Literal['ok']:
    """Configures self-trading prevention for the account: what happens when an order would match against another order from the same account (or, if extended, a related subaccount).

    Args:
      mode: Self-trading prevention behavior.
      extended_to_subaccounts: When `true`, self-trading prevention also applies between subaccounts of this account; when `false`, each (sub)account is treated separately.
      block_rfq_self_match_prevention: When `true`, Block RFQs cannot execute between accounts belonging to the same legal entity. Configured independently of `mode`/`extended_to_subaccounts`.
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/account-management/private-set_self_trading_config)
    """
    params: dict = {
      'mode': mode,
      'extended_to_subaccounts': extended_to_subaccounts,
    }
    if block_rfq_self_match_prevention is not None:
      params['block_rfq_self_match_prevention'] = block_rfq_self_match_prevention
    return await self.authed_request(
      'private/set_self_trading_config',
      params=params,
      validator=validate_set_self_trading_config,
      validate=validate,
    )
