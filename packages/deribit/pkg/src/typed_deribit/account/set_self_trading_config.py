# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict, cast
from typed_deribit.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  mode: Literal['reject_taker', 'cancel_maker']
  """Self-trading prevention behavior."""
  extended_to_subaccounts: bool
  """When `true`, self-trading prevention also applies between subaccounts of this account; when `false`, each (sub)account is treated separately."""
  block_rfq_self_match_prevention: NotRequired[bool]
  """When `true`, Block RFQs cannot execute between accounts belonging to the same legal entity. Configured independently of `mode`/`extended_to_subaccounts`."""


Response = Literal['ok']


class SetSelfTradingConfig(RpcEndpoint):
  """Configures self-trading prevention for the account: what happens when an order would match against another order from the same account (or, if extended, a related subaccount)."""

  async def set_self_trading_config(
    self,
    mode: Literal['reject_taker', 'cancel_maker'],
    *,
    extended_to_subaccounts: bool,
    block_rfq_self_match_prevention: bool | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Configures self-trading prevention for the account: what happens when an order would match against another order from the same account (or, if extended, a related subaccount).

    Args:
      mode: Self-trading prevention behavior.
      extended_to_subaccounts: When `true`, self-trading prevention also applies between subaccounts of this account; when `false`, each (sub)account is treated separately.
      block_rfq_self_match_prevention: When `true`, Block RFQs cannot execute between accounts belonging to the same legal entity. Configured independently of `mode`/`extended_to_subaccounts`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/account-management/private-set_self_trading_config)
    """
    request: Request = Request(
      mode=mode, extended_to_subaccounts=extended_to_subaccounts
    )
    if block_rfq_self_match_prevention is not None:
      request['block_rfq_self_match_prevention'] = block_rfq_self_match_prevention
    return await self.request(
      request,
      method='POST',
      path='private/set_self_trading_config',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=cast(type, Response),
    )
