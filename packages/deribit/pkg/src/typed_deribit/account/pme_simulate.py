# Generated by typed-dev — see CONTRIBUTING.md.
"""`private/pme/simulate` — `private/pme/simulate`."""

from typing_extensions import Any, Literal
from typed_core.validation import validator
from typed_deribit.core import RpcEndpoint

validate_pme_simulate = validator[dict[str, Any]](dict[str, Any])


class PmeSimulate(RpcEndpoint):
  """`private/pme/simulate`."""

  async def pme_simulate(
    self,
    *,
    currency: Literal['BTC', 'ETH', 'USDC', 'USDT', 'CROSS'],
    add_positions: bool | None = None,
    simulated_positions: str | None = None,
    validate: bool | None = None,
  ) -> dict[str, Any]:
    """Calculates the Extended Risk Matrix (ERM) and detailed margin information for Portfolio Margin accounts, across market-condition scenarios. Pass `currency: "CROSS"` for a Cross-Collateral simulation instead of a single currency.

    Args:
      currency: The currency for which the Extended Risk Matrix is calculated. Use `"CROSS"` for a Cross Collateral simulation.
      add_positions: If `true`, adds the simulated positions to the current positions; otherwise uses only the simulated positions. `true` by default.
      simulated_positions: A JSON-encoded object of `{instrumentName: size, ...}`, e.g. `{"BTC-PERPETUAL": -1.0}` (URI-encoded for a GET request). Size in base currency.
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/account-management/private-simulate)
    """
    params: dict = {
      'currency': currency,
    }
    if add_positions is not None:
      params['add_positions'] = add_positions
    if simulated_positions is not None:
      params['simulated_positions'] = simulated_positions
    return await self.authed_request(
      'private/pme/simulate',
      params=params,
      validator=validate_pme_simulate,
      validate=validate,
    )
