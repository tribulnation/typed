# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict, cast
from typed_deribit.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  sid: int
  """Subaccount user id."""
  name: str
  """New username for the subaccount."""


Response = Literal['ok']


class ChangeSubaccountName(RpcEndpoint):
  """Updates the display name (username) of a subaccount. Only the main account owner can rename a subaccount; the new name is for identification only and does not affect functionality or access. Unlike most other subaccount-settings methods, this one is not gated behind two-factor authentication -- confirmed live."""

  async def change_subaccount_name(
    self,
    sid: int,
    *,
    name: str,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Updates the display name (username) of a subaccount. Only the main account owner can rename a subaccount; the new name is for identification only and does not affect functionality or access. Unlike most other subaccount-settings methods, this one is not gated behind two-factor authentication -- confirmed live.

    Args:
      sid: Subaccount user id.
      name: New username for the subaccount.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/account-management/private-change_subaccount_name)
    """
    return await self.request(
      Request(sid=sid, name=name),
      method='POST',
      path='private/change_subaccount_name',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=cast(type, Response),
    )
