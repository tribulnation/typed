# Generated by typed-dev — see CONTRIBUTING.md.
"""`private/change_subaccount_name` — `private/change_subaccount_name`."""

from typing_extensions import Literal
from typed_core.validation import validator
from typed_deribit.core import RpcEndpoint

validate_change_subaccount_name = validator[Literal['ok']](Literal['ok'])  # pyright: ignore[reportArgumentType]  # fmt: skip


class ChangeSubaccountName(RpcEndpoint):
  """`private/change_subaccount_name`."""

  async def change_subaccount_name(
    self,
    *,
    sid: int,
    name: str,
    validate: bool | None = None,
  ) -> Literal['ok']:
    """Updates the display name (username) of a subaccount. Only the main account owner can rename a subaccount; the new name is for identification only and does not affect functionality or access. Unlike most other subaccount-settings methods, this one is not gated behind two-factor authentication -- confirmed live.

    Args:
      sid: Subaccount user id.
      name: New username for the subaccount.
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/account-management/private-change_subaccount_name)
    """
    params: dict = {
      'sid': sid,
      'name': name,
    }
    return await self.authed_request(
      'private/change_subaccount_name',
      params=params,
      validator=validate_change_subaccount_name,
      validate=validate,
    )
