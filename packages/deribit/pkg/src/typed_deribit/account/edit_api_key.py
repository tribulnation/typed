# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import ApiKey


class Request(TypedDict):
  id: int
  """API key id."""
  max_scope: str
  """Maximal access scope for tokens generated with this key, as a space-separated string of `resource:permission` pairs. See `account.create_api_key`'s parameter of the same name for the string-vs-array discrepancy."""
  name: NotRequired[str]
  """New display name for the key. Letters, numbers and underscores only; maximum length 16 characters."""
  enabled: NotRequired[bool]
  """Enable (`true`) or disable (`false`) the key."""
  enabled_features: NotRequired[
    list[Literal['restricted_block_trades', 'block_trade_approval']]
  ]
  """Advanced on-key features to enable: `restricted_block_trades`, `block_trade_approval`. See `account.create_api_key` for what each does."""
  ip_whitelist: NotRequired[list[str]]
  """IP addresses to whitelist for this key."""


class EditApiKey(RpcEndpoint):
  """Updates one or more properties of an existing API key: name, scope, enabled state, on-key features, or IP whitelist. At least one optional parameter must be provided; changes take effect immediately. Requires two-factor authentication on the calling session."""

  async def edit_api_key(
    self,
    id: int,
    *,
    max_scope: str,
    name: str | None = None,
    enabled: bool | None = None,
    enabled_features: list[Literal['restricted_block_trades', 'block_trade_approval']]
    | None = None,
    ip_whitelist: list[str] | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> ApiKey:
    """Updates one or more properties of an existing API key: name, scope, enabled state, on-key features, or IP whitelist. At least one optional parameter must be provided; changes take effect immediately. Requires two-factor authentication on the calling session.

    Args:
      id: API key id.
      max_scope: Maximal access scope for tokens generated with this key, as a space-separated string of `resource:permission` pairs. See `account.create_api_key`'s parameter of the same name for the string-vs-array discrepancy.
      name: New display name for the key. Letters, numbers and underscores only; maximum length 16 characters.
      enabled: Enable (`true`) or disable (`false`) the key.
      enabled_features: Advanced on-key features to enable: `restricted_block_trades`, `block_trade_approval`. See `account.create_api_key` for what each does.
      ip_whitelist: IP addresses to whitelist for this key.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/account-management/private-edit_api_key)
    """
    request: Request = Request(id=id, max_scope=max_scope)
    if name is not None:
      request['name'] = name
    if enabled is not None:
      request['enabled'] = enabled
    if enabled_features is not None:
      request['enabled_features'] = enabled_features
    if ip_whitelist is not None:
      request['ip_whitelist'] = ip_whitelist
    return await self.request(
      request,
      method='POST',
      path='private/edit_api_key',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=ApiKey,
    )
