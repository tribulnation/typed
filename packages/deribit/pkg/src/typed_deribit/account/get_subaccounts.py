# Generated by typed-dev — see CONTRIBUTING.md.
"""`private/get_subaccounts` — `private/get_subaccounts`."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_deribit.core import RpcEndpoint


class SubaccountCurrencyPortfolio(TypedDict):
  currency: str
  """The currency this portfolio entry is for, lower-case (e.g. `"btc"`)."""
  balance: float
  """The account's balance."""
  locked_balance: NotRequired[float]
  """The account's locked balance (observed live; not in the venue's own published schema for this field)."""
  margin_balance: float
  """The account's margin balance. When cross collateral is enabled, this aggregated value converts the sum of each cross collateral currency's value using each currency's index."""
  equity: float
  """The account's current equity."""
  maintenance_margin: float
  """The maintenance margin."""
  initial_margin: float
  """The account's initial margin."""
  available_funds: float
  """The account's available funds."""
  available_withdrawal_funds: float
  """The account's available-to-withdraw funds."""
  spot_reserve: float
  """The account's balance reserved in active spot orders."""
  additional_reserve: float
  """The account's balance reserved in other orders."""


class TradingProductDetail(TypedDict):
  product: str
  """Trading product name, e.g. `"perpetual"`, `"futures"`, `"options"`, `"future_combos"`, `"option_combos"`, `"spots"`. Not declared `enum`: this field is not documented on this operation at all (an undocumented addition beyond the venue's published schema), so only the observed values are known, not a closed set."""
  enabled: bool
  """Whether this trading product is enabled for the account."""
  overwriteable: NotRequired[bool]
  """Whether this setting can be overridden."""
  requires_consent: NotRequired[bool]
  """Whether enabling this product requires user consent."""


class Subaccount(TypedDict):
  id: int
  """Account/subaccount identifier."""
  username: str
  """Subaccount username."""
  type: Literal['main', 'subaccount']
  """Whether this entry is the main account or a subaccount."""
  email: str
  """User email."""
  not_confirmed_email: NotRequired[str]
  """New email address that has not yet been confirmed. Only present when `with_portfolio` is `true`."""
  security_keys_enabled: NotRequired[bool]
  """Whether Security Keys authentication is enabled."""
  security_keys_assignments: NotRequired[list[str]]
  """Names of assignments with Security Keys assigned."""
  system_name: str
  """System-generated user nickname."""
  receive_notifications: bool
  """When `true`, receive all notification emails on the main email."""
  is_password: bool
  """Whether a password has been configured for the subaccount."""
  margin_model: NotRequired[str]
  """Name of the account's currently enabled margin model."""
  proof_id: NotRequired[str]
  """Hashed identifier used in the Proof of Liability for the subaccount. Sensitive: keep secret to avoid disclosing your entries in the Proof-of-Reserves files."""
  proof_id_signature: NotRequired[str]
  """Signature used as a base string for the `proof_id` hash. Sensitive: keep secret for the same reason as `proof_id`."""
  login_enabled: NotRequired[bool]
  """Whether login to the subaccount is enabled."""
  disabled_trading_products: NotRequired[list[str]]
  """Trading products disabled for this (sub)account. Not documented on this operation's own schema; observed live as an empty list."""
  trading_products_details: NotRequired[list[TradingProductDetail]]
  """Per-product trading enablement details. Not documented on this operation's own schema (an undocumented addition), observed live alongside `disabled_trading_products`."""
  referrals_count: NotRequired[int]
  """Number of users referred by this (sub)account. Not documented on this operation's own schema, observed live."""
  portfolio: NotRequired[dict[str, SubaccountCurrencyPortfolio]]
  """Per-currency portfolio detail, keyed by lower-case currency code. Only present when `with_portfolio` is `true`."""


validate_get_subaccounts = validator[list[Subaccount]](list[Subaccount])


class GetSubaccounts(RpcEndpoint):
  """`private/get_subaccounts`."""

  async def get_subaccounts(
    self,
    *,
    with_portfolio: bool | None = None,
    validate: bool | None = None,
  ) -> list[Subaccount]:
    """Retrieves information about all subaccounts associated with the main account (including the main account itself). When called from a subaccount, the response includes limited details for the main account and full details for the calling subaccount.

    Args:
      with_portfolio: Portfolio flag: `true` for portfolio information, `false` for subaccount information only. `false` by default.
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/account-management/private-get_subaccounts)
    """
    params = {}
    if with_portfolio is not None:
      params['with_portfolio'] = with_portfolio
    return await self.authed_request(
      'private/get_subaccounts',
      params=params,
      validator=validate_get_subaccounts,
      validate=validate,
    )
