# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict, cast
from typed_deribit.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  sid: int
  """Subaccount user id."""
  email: str
  """Email address to assign to the subaccount."""


Response = Literal['ok']


class SetEmailForSubaccount(RpcEndpoint):
  """Assigns an email address to a subaccount, usable for direct login and notifications once confirmed. The address owner receives a confirmation email with a verification link; the address is unusable for login until confirmed. Requires two-factor authentication on the calling session."""

  async def set_email_for_subaccount(
    self,
    sid: int,
    *,
    email: str,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Assigns an email address to a subaccount, usable for direct login and notifications once confirmed. The address owner receives a confirmation email with a verification link; the address is unusable for login until confirmed. Requires two-factor authentication on the calling session.

    Args:
      sid: Subaccount user id.
      email: Email address to assign to the subaccount.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/account-management/private-set_email_for_subaccount)
    """
    return await self.request(
      Request(sid=sid, email=email),
      method='POST',
      path='private/set_email_for_subaccount',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=cast(type, Response),
    )
