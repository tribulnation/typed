# Generated by typed-dev — see CONTRIBUTING.md.
"""`private/set_email_for_subaccount` — `private/set_email_for_subaccount`."""

from typing_extensions import Literal
from typed_core.validation import validator
from typed_deribit.core import RpcEndpoint

validate_set_email_for_subaccount = validator[Literal['ok']](Literal['ok'])  # pyright: ignore[reportArgumentType]  # fmt: skip


class SetEmailForSubaccount(RpcEndpoint):
  """`private/set_email_for_subaccount`."""

  async def set_email_for_subaccount(
    self,
    *,
    sid: int,
    email: str,
    validate: bool | None = None,
  ) -> Literal['ok']:
    """Assigns an email address to a subaccount, usable for direct login and notifications once confirmed. The address owner receives a confirmation email with a verification link; the address is unusable for login until confirmed. Requires two-factor authentication on the calling session.

    Args:
      sid: Subaccount user id.
      email: Email address to assign to the subaccount.
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/account-management/private-set_email_for_subaccount)
    """
    params: dict = {
      'sid': sid,
      'email': email,
    }
    return await self.authed_request(
      'private/set_email_for_subaccount',
      params=params,
      validator=validate_set_email_for_subaccount,
      validate=validate,
    )
