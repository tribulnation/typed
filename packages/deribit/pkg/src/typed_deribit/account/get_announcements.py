# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_deribit.core import TimestampMillis
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import AccountAnnouncement


class Request(TypedDict):
  start_timestamp: NotRequired[TimestampMillis]
  """The timestamp (milliseconds since the Unix epoch)."""
  count: NotRequired[int]
  """Maximum count of returned announcements, default 5, maximum 50."""


GetAnnouncementsResult = list[AccountAnnouncement]


class GetAnnouncements(RpcEndpoint):
  """Retrieves platform announcements and important notices. Announcements include system updates, maintenance schedules, new features, policy changes, and other important information.

  Results are returned in reverse chronological order (newest first)."""

  async def get_announcements(
    self,
    start_timestamp: TimestampMillis | None = None,
    *,
    count: int | None = None,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> GetAnnouncementsResult:
    """Retrieves platform announcements and important notices. Announcements include system updates, maintenance schedules, new features, policy changes, and other important information.

    Results are returned in reverse chronological order (newest first).

    Args:
      start_timestamp: The timestamp (milliseconds since the Unix epoch).
      count: Maximum count of returned announcements, default 5, maximum 50.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/account-management/public-get_announcements)
    """
    request: Request = Request()
    if start_timestamp is not None:
      request['start_timestamp'] = start_timestamp
    if count is not None:
      request['count'] = count
    return await self.request(
      request,
      method='POST',
      path='public/get_announcements',
      meta={'public': True},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=GetAnnouncementsResult,
    )
