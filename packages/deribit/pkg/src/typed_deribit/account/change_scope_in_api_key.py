# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import ApiKey


class Request(TypedDict):
  max_scope: str
  """New maximal access scope for tokens generated with this key, as a space-separated string of `resource:permission` pairs. See `account.create_api_key`'s parameter of the same name for the string-vs-array discrepancy."""
  id: int
  """API key id."""


class ChangeScopeInApiKey(RpcEndpoint):
  """Updates the scope (permissions) of an existing API key. The key immediately loses access to any operation outside the new scope. Requires two-factor authentication on the calling session."""

  async def change_scope_in_api_key(
    self,
    max_scope: str,
    *,
    id: int,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> ApiKey:
    """Updates the scope (permissions) of an existing API key. The key immediately loses access to any operation outside the new scope. Requires two-factor authentication on the calling session.

    Args:
      max_scope: New maximal access scope for tokens generated with this key, as a space-separated string of `resource:permission` pairs. See `account.create_api_key`'s parameter of the same name for the string-vs-array discrepancy.
      id: API key id.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/account-management/private-change_scope_in_api_key)
    """
    return await self.request(
      Request(max_scope=max_scope, id=id),
      method='POST',
      path='private/change_scope_in_api_key',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=ApiKey,
    )
