# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_deribit.core.endpoint.rpc import RpcEndpoint
from typed_deribit.schemas import ApiKey


class Request(TypedDict):
  id: int
  """API key id."""


class DisableApiKey(RpcEndpoint):
  """Disables an API key, preventing it from authenticating further requests, without deleting it. The key can be re-enabled later with `account.enable_api_key`. Requires two-factor authentication on the calling session."""

  async def disable_api_key(
    self,
    id: int,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> ApiKey:
    """Disables an API key, preventing it from authenticating further requests, without deleting it. The key can be re-enabled later with `account.enable_api_key`. Requires two-factor authentication on the calling session.

    Args:
      id: API key id.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/account-management/private-disable_api_key)
    """
    return await self.request(
      Request(id=id),
      method='POST',
      path='private/disable_api_key',
      meta={},
      validate=validate,
      transport=transport,
      request_type=Request,
      response_type=ApiKey,
    )
