# Generated by typed-dev — see CONTRIBUTING.md.
"""`private/enable_affiliate_program` — `private/enable_affiliate_program`."""

from typing_extensions import Literal
from typed_core.validation import validator
from typed_deribit.core import RpcEndpoint

validate_enable_affiliate_program = validator[Literal['ok']](Literal['ok'])  # pyright: ignore[reportArgumentType]  # fmt: skip


class EnableAffiliateProgram(RpcEndpoint):
  """`private/enable_affiliate_program`."""

  async def enable_affiliate_program(
    self,
    *,
    validate: bool | None = None,
  ) -> Literal['ok']:
    """Enables the affiliate program for the authenticated account, generating a unique referral link. Commissions are paid based on the trading activity of referred users. Takes no parameters.

    Args:
      validate: Validate the response against the generated schema.

    References:
      - [Deribit API docs](https://docs.deribit.com/api-reference/account-management/private-enable_affiliate_program)
    """
    return await self.authed_request(
      'private/enable_affiliate_program',
      validator=validate_enable_affiliate_program,
      validate=validate,
    )
