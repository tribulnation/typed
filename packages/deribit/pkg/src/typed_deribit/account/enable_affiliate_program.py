# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, cast
from typed_deribit.core.endpoint.rpc import RpcEndpoint


Response = Literal['ok']


class EnableAffiliateProgram(RpcEndpoint):
  """Enables the affiliate program for the authenticated account, generating a unique referral link. Commissions are paid based on the trading activity of referred users. Takes no parameters."""

  async def enable_affiliate_program(
    self,
    *,
    validate: bool | None = None,
    transport: Literal['http', 'ws'] = 'http',
  ) -> Response:
    """Enables the affiliate program for the authenticated account, generating a unique referral link. Commissions are paid based on the trading activity of referred users. Takes no parameters.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      transport: Transport to send this call over. Defaults to 'http'.

    References:
      - [Official docs](https://docs.deribit.com/api-reference/account-management/private-enable_affiliate_program)
    """
    return await self.request(
      None,
      method='POST',
      path='private/enable_affiliate_program',
      meta={},
      validate=validate,
      transport=transport,
      response_type=cast(type, Response),
    )
