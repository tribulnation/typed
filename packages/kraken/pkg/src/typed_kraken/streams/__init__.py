# Generated by typed-dev — see CONTRIBUTING.md.
"""Kraken Spot WebSocket v2: one public connection (market data), one private
connection (private channels). The private connection is also shared with the
top-level `TradingWs` surface -- see `Kraken.new()`.
"""

from dataclasses import dataclass
from functools import cached_property
from typing_extensions import Self

from ..core.transport.ws import KrakenSocketClient
from .market_data import MarketData
from .private import Private


@dataclass(kw_only=True, frozen=True)
class Streams:
  """Kraken Spot WebSocket v2."""

  market_client: KrakenSocketClient
  private_client: KrakenSocketClient

  @cached_property
  def market_data(self) -> MarketData:
    return MarketData(client=self.market_client)

  @cached_property
  def private(self) -> Private:
    return Private(client=self.private_client)

  async def __aenter__(self) -> Self:
    await self.market_client.__aenter__()
    await self.private_client.__aenter__()
    return self

  async def __aexit__(self, exc_type, exc_value, traceback):
    await self.market_client.__aexit__(exc_type, exc_value, traceback)
    await self.private_client.__aexit__(exc_type, exc_value, traceback)
