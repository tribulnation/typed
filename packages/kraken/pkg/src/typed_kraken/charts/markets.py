# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_kraken.core.endpoint.public import PublicEndpoint


class Request(TypedDict):
  """Parameters selecting chart data."""

  tick_type: Literal['spot', 'mark', 'trade']
  """Native chart tick type."""


Response = list[str]


class Markets(PublicEndpoint):
  """List chart market symbols for the selected native tick type."""

  async def markets(
    self,
    tick_type: Literal['spot', 'mark', 'trade'],
    *,
    validate: bool | None = None,
  ) -> Response:
    """List chart market symbols for the selected native tick type.

    Args:
      tick_type: Native chart tick type.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.kraken.com/api-reference/candles/markets)
    """
    return await self.request(
      Request(tick_type=tick_type),
      method='GET',
      path='/api/charts/v1/{tick_type}',
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
