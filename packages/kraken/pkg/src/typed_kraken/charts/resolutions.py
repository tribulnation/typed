# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_kraken.core.endpoint.public import PublicEndpoint


class Request(TypedDict):
  """Parameters selecting chart data."""

  tick_type: Literal['spot', 'mark', 'trade']
  """Native chart tick type."""
  symbol: str
  """Market symbol returned by the Charts markets endpoint."""


Response = list[Literal['1m', '5m', '15m', '30m', '1h', '4h', '12h', '1d', '1w']]


class Resolutions(PublicEndpoint):
  """List native candle resolutions for a chart tick type and market."""

  async def resolutions(
    self,
    tick_type: Literal['spot', 'mark', 'trade'],
    *,
    symbol: str,
    validate: bool | None = None,
  ) -> Response:
    """List native candle resolutions for a chart tick type and market.

    Args:
      tick_type: Native chart tick type.
      symbol: Market symbol returned by the Charts markets endpoint.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.kraken.com/api-reference/candles/resolutions)
    """
    return await self.request(
      Request(tick_type=tick_type, symbol=symbol),
      method='GET',
      path='/api/charts/v1/{tick_type}/{symbol}',
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
