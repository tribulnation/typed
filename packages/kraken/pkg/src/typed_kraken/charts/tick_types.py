# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal
from typed_kraken.core.endpoint.public import PublicEndpoint


Response = list[Literal['spot', 'mark', 'trade']]


class TickTypes(PublicEndpoint):
  """List the native tick types available from Kraken Futures Charts."""

  async def tick_types(self, *, validate: bool | None = None) -> Response:
    """List the native tick types available from Kraken Futures Charts.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.kraken.com/api-reference/candles/tick-types)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/charts/v1/',
      validate=validate,
      response_type=Response,
    )
