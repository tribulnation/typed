# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_kraken.core import TimestampIso
from typed_kraken.core.endpoint.public import PublicEndpoint
from typed_kraken.schemas import FuturesTicker


class Request(TypedDict):
  contractType: NotRequired[
    list[
      Literal[
        'futures_inverse', 'futures_vanilla', 'flexible_futures', 'options', 'all'
      ]
    ]
  ]
  """Contract type(s) to return statuses for.

By default, includes all futures instrument types.

Multi-value example: `?contractType=futures_inverse&contractType=futures_vanilla`"""
  symbol: NotRequired[list[str]]
  """Market symbol(s) to filter tickers by.

Symbols are case-insensitive. Multi-value example: `?symbol=PF_BTCUSD&symbol=pf_ethusd`"""


class FuturesTickersResult(TypedDict):
  """Current derivatives-market and index tickers with server timestamp."""

  tickers: list[FuturesTicker]
  """A list containing a structures for each available instrument. The list
is in no particular order."""
  result: Literal['success']
  """Successful completion of the request."""
  serverTime: TimestampIso
  """Server time in Coordinated Universal Time (UTC)"""


class Tickers(PublicEndpoint):
  """This endpoint returns current market data for all currently listed Futures contracts and
  indices."""

  async def tickers(
    self,
    contract_type: list[
      Literal[
        'futures_inverse', 'futures_vanilla', 'flexible_futures', 'options', 'all'
      ]
    ]
    | None = None,
    *,
    symbol: list[str] | None = None,
    validate: bool | None = None,
  ) -> FuturesTickersResult:
    """This endpoint returns current market data for all currently listed Futures contracts and
    indices.

    Args:
      contract_type: Contract type(s) to return statuses for.

    By default, includes all futures instrument types.

    Multi-value example: `?contractType=futures_inverse&contractType=futures_vanilla`
      symbol: Market symbol(s) to filter tickers by.

    Symbols are case-insensitive. Multi-value example: `?symbol=PF_BTCUSD&symbol=pf_ethusd`
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.kraken.com/api-reference/market-data/get-tickers)
    """
    request: Request = Request()
    if contract_type is not None:
      request['contractType'] = contract_type
    if symbol is not None:
      request['symbol'] = symbol
    return await self.request(
      request,
      method='GET',
      path='/derivatives/api/v3/tickers',
      validate=validate,
      request_type=Request,
      response_type=FuturesTickersResult,
    )
