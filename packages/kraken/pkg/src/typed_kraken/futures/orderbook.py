# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_kraken.core import TimestampIso
from typed_kraken.core.endpoint.public import PublicEndpoint


class FuturesOrderBook(TypedDict):
  """A structure containing lists with bid and ask prices and sizes."""

  asks: list[tuple[float, float]]
  """Asks as [price, size] levels in the order returned by Kraken."""
  bids: list[tuple[float, float]]
  """Bids as [price, size] levels in the order returned by Kraken."""


class Request(TypedDict):
  symbol: str
  """The symbol of the Futures."""


class FuturesOrderbookResult(TypedDict):
  """The complete aggregated, non-cumulative order book with server timestamp."""

  orderBook: FuturesOrderBook
  result: Literal['success']
  """Successful completion of the request."""
  serverTime: TimestampIso
  """Server time in Coordinated Universal Time (UTC)"""


class Orderbook(PublicEndpoint):
  """This endpoint returns the entire non-cumulative order book of currently listed Futures
  contracts."""

  async def orderbook(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> FuturesOrderbookResult:
    """This endpoint returns the entire non-cumulative order book of currently listed Futures
    contracts.

    Args:
      symbol: The symbol of the Futures.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.kraken.com/api-reference/market-data/get-orderbook)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/derivatives/api/v3/orderbook',
      validate=validate,
      request_type=Request,
      response_type=FuturesOrderbookResult,
    )
