# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_kraken.core import TimestampIso
from typed_kraken.core.endpoint.public import PublicEndpoint
from typed_kraken.schemas import FuturesTicker


class Request(TypedDict):
  symbol: str
  """Market symbol."""


class FuturesTickerResult(TypedDict):
  """The requested derivatives-market or index ticker with server timestamp."""

  ticker: FuturesTicker
  """Requested market or index ticker."""
  result: Literal['success']
  """Successful completion of the request."""
  serverTime: TimestampIso
  """Server time in Coordinated Universal Time (UTC)"""


class Ticker(PublicEndpoint):
  """Get market data for contract or index by symbol"""

  async def ticker(
    self, symbol: str, *, validate: bool | None = None
  ) -> FuturesTickerResult:
    """Get market data for contract or index by symbol

    Args:
      symbol: Market symbol.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.kraken.com/api-reference/market-data/get-ticker-by-symbol)
    """
    return await self.request(
      Request(symbol=symbol),
      method='GET',
      path='/derivatives/api/v3/tickers/{symbol}',
      validate=validate,
      request_type=Request,
      response_type=FuturesTickerResult,
    )
