# Generated by typed-dev — see CONTRIBUTING.md.
"""Kraken Spot: public and private REST endpoints under `https://api.kraken.com/0`."""

from functools import cached_property

from ..core.endpoint.rpc import RpcEndpoint
from .account import Account
from .earn import Earn
from .funding import Funding
from .market_data import MarketData
from .trading import Trading


class Spot(RpcEndpoint):
  """Kraken Spot REST -- market data, account data, trading, funding, and Earn."""

  @cached_property
  def account(self) -> Account:
    return Account(client=self.client)

  @cached_property
  def earn(self) -> Earn:
    return Earn(client=self.client)

  @cached_property
  def funding(self) -> Funding:
    return Funding(client=self.client)

  @cached_property
  def market_data(self) -> MarketData:
    return MarketData(client=self.client)

  @cached_property
  def trading(self) -> Trading:
    return Trading(client=self.client)
