# Generated by typed-dev — see CONTRIBUTING.md.
"""`spot.account.query_ledgers` -- private Spot endpoint."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_kraken.core import TimestampSeconds
from ...core.endpoint.rpc import RpcEndpoint


class LedgerEntry(TypedDict):
  """One ledger entry."""

  refid: NotRequired[str]
  """Reference ID of the parent transaction (trade, deposit, withdrawal, etc.) that caused the ledger entry."""
  time: NotRequired[TimestampSeconds]
  """Unix timestamp of the ledger entry."""
  type: NotRequired[
    Literal[
      'none',
      'trade',
      'deposit',
      'withdrawal',
      'transfer',
      'margin',
      'adjustment',
      'rollover',
      'spend',
      'receive',
      'settled',
      'credit',
      'staking',
      'reward',
      'dividend',
      'sale',
      'conversion',
      'nfttrade',
      'nftcreatorfee',
      'nftrebate',
      'custodytransfer',
    ]
  ]
  """Type of ledger entry."""
  subtype: NotRequired[str]
  """Additional info relating to the ledger entry type, where applicable."""
  aclass: NotRequired[str]
  """Asset class."""
  asset: NotRequired[str]
  """Asset."""
  amount: NotRequired[str]
  """Transaction amount."""
  fee: NotRequired[str]
  """Transaction fee."""
  balance: NotRequired[str]
  """Resulting balance."""


validate_query_ledgers = validator(dict[str, LedgerEntry])


class QueryLedgers(RpcEndpoint):
  """`spot.account.query_ledgers`."""

  async def query_ledgers(
    self,
    *,
    id: str,
    trades: bool | None = None,
    rebase_multiplier: Literal['rebased', 'base'] | None = None,
    validate: bool | None = None,
  ) -> dict[str, LedgerEntry]:
    """Retrieve information about specific ledger entries, by ledger ID.

    **Note on Staking/Earn assets:** assets migrated from the legacy Staking system to the new Earn system may appear with symbol extensions -- `.B` (new yield-bearing products), `.F` (auto-earning Kraken Rewards) -- these are read-only for transacting; use the base asset instead.

    **API Key Permissions Required:** `Data - Query ledger entries`

    Args:
      id: Comma-delimited list of ledger IDs to query info about (20 maximum).
      trades: Whether or not to include trades related to position in output.
      rebase_multiplier: Optional parameter for viewing xstocks data. `rebased` displays in terms of underlying equity, `base` displays in terms of SPV tokens.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.kraken.com/api-reference/account-data/query-ledgers)
    """
    data: dict = {
      'id': id,
    }
    if trades is not None:
      data['trades'] = trades
    if rebase_multiplier is not None:
      data['rebase_multiplier'] = rebase_multiplier

    return await self.authed_request(
      '/0/private/QueryLedgers',
      data,
      validator=validate_query_ledgers,
      validate=validate,
    )
