# Generated by typed-dev — see CONTRIBUTING.md.
"""`spot.trading.cancel_all` -- private Spot endpoint."""

from typing_extensions import NotRequired
from typed_core.validation import TypedDict, validator
from ...core.endpoint.rpc import RpcEndpoint


class OrderCancelled(TypedDict):
  """Result of a cancel request."""

  count: NotRequired[int]
  """Number of orders cancelled."""
  pending: NotRequired[bool]
  """If true, orders are pending cancellation."""


validate_cancel_all = validator(OrderCancelled)


class CancelAll(RpcEndpoint):
  """`spot.trading.cancel_all`."""

  async def cancel_all(self, *, validate: bool | None = None) -> OrderCancelled:
    """Cancel every open order for the account.

    **API Key Permissions Required:** `Orders and trades - Create & modify orders` or `Orders and trades - Cancel & close orders`

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.kraken.com/api-reference/trading/cancel-all-orders)
    """
    return await self.authed_request(
      '/0/private/CancelAll', validator=validate_cancel_all, validate=validate
    )
