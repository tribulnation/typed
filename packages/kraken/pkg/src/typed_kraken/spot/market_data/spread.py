# Generated by typed-dev — see CONTRIBUTING.md.
"""`spot.market_data.spread` -- public Spot market data."""

from typing_extensions import Literal, NotRequired
from typed_core.validation import TypedDict, validator
from typed_kraken.core import TimestampSeconds, timestamp_seconds
from ...core.endpoint.rpc import RpcEndpoint


class SpreadResult(TypedDict):
  """Spreads keyed by pair, plus the polling cursor."""

  last: NotRequired[int]
  """ID to be used as `since` when polling for new spread data."""


validate_spread = validator(SpreadResult)


class Spread(RpcEndpoint):
  """`spot.market_data.spread`."""

  async def spread(
    self,
    *,
    pair: str,
    asset_version: Literal[1] | None = None,
    since: TimestampSeconds | None = None,
    asset_class: Literal['tokenized_asset'] | None = None,
    validate: bool | None = None,
  ) -> SpreadResult:
    """Returns the last ~200 top-of-book spreads for a given pair.

    Args:
      pair: Asset pair to get data for.
      asset_version: Controls whether response keys use Kraken's internal names or display names. Omitted (default): internal names are used. `assetVersion=1`: display names are used. Only `assetVersion=1` is currently supported.
      since: Returns spread data since given timestamp. Optional, intended for incremental updates within available dataset (does not contain all historical spreads).
      asset_class: This parameter is required on requests for non-crypto pairs, i.e. use `tokenized_asset` for xstocks.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.kraken.com/api-reference/market-data/get-recent-spreads)
    """
    params: dict = {
      'pair': pair,
    }
    if asset_version is not None:
      params['assetVersion'] = asset_version
    if since is not None:
      params['since'] = timestamp_seconds.dump(since)
    if asset_class is not None:
      params['asset_class'] = asset_class

    return await self.request(
      '/0/public/Spread', params, validator=validate_spread, validate=validate
    )
