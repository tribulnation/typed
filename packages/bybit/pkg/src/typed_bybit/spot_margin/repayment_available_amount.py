# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class RepaymentAvailableAmountResult(TypedDict):
  """Maximum repayable amount for the coin."""

  currency: str
  """Coin name, uppercase."""
  lossLessRepaymentAmount: str
  """min(spot coin available balance, coin borrow amount)."""


class Request(TypedDict):
  currency: str
  """Coin name, uppercase only."""


class RepaymentAvailableAmount(RpcEndpoint):
  """Query the maximum amount available to repay borrowed cryptocurrency in spot margin trading: the minimum of the available spot balance and the borrowed amount."""

  async def repayment_available_amount(
    self,
    currency: str,
    *,
    validate: bool | None = None,
  ) -> RepaymentAvailableAmountResult:
    """Query the maximum amount available to repay borrowed cryptocurrency in spot margin trading: the minimum of the available spot balance and the borrowed amount.

    Args:
      currency: Coin name, uppercase only.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/repayment-available-amount)
    """
    return await self.request(
      Request(currency=currency),
      method='GET',
      path='/v5/spot-margin-trade/repayment-available-amount',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=RepaymentAvailableAmountResult,
    )
