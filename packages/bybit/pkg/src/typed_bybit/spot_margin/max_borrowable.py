# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class MaxBorrowableResult(TypedDict):
  """Maximum borrowable amount for the coin."""

  currency: str
  """Coin name, uppercase."""
  maxLoan: str
  """Maximum borrowable amount for the coin."""


class Request(TypedDict):
  currency: str
  """Coin name, uppercase only."""


class MaxBorrowable(RpcEndpoint):
  """Query the maximum borrowable amount for a specified cryptocurrency in spot margin trading."""

  async def max_borrowable(
    self,
    currency: str,
    *,
    validate: bool | None = None,
  ) -> MaxBorrowableResult:
    """Query the maximum borrowable amount for a specified cryptocurrency in spot margin trading.

    Args:
      currency: Coin name, uppercase only.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/max-borrowable)
    """
    return await self.request(
      Request(currency=currency),
      method='GET',
      path='/v5/spot-margin-trade/max-borrowable',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=MaxBorrowableResult,
    )
