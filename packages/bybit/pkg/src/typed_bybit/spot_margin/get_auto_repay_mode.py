# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class AutoRepayModeEntry(TypedDict):
  """Auto-repay mode for one currency."""

  currency: str
  """Coin name, uppercase."""
  autoRepayMode: Literal['0', '1']
  """Mode status."""


class Request(TypedDict):
  currency: NotRequired[str]
  """Coin name, uppercase only. Omit to return every currency."""


class AutoRepayModeResult(TypedDict):
  """Auto-repay mode by currency."""

  data: list[AutoRepayModeEntry]
  """Auto-repay mode for one currency."""


class GetAutoRepayMode(RpcEndpoint):
  """Query the spot automatic repayment mode for margin trading."""

  async def get_auto_repay_mode(
    self,
    currency: str | None = None,
    *,
    validate: bool | None = None,
  ) -> AutoRepayModeResult:
    """Query the spot automatic repayment mode for margin trading.

    Args:
      currency: Coin name, uppercase only. Omit to return every currency.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/get-auto-repay-mode)
    """
    request: Request = Request()
    if currency is not None:
      request['currency'] = currency
    return await self.request(
      request,
      method='GET',
      path='/v5/spot-margin-trade/get-auto-repay-mode',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=AutoRepayModeResult,
    )
