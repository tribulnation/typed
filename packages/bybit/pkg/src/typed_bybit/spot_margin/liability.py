# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class LiabilityResult(TypedDict):
  """Liability breakdown for the coin."""

  currency: str
  """Coin name, uppercase."""
  totalBorrowAmount: str
  """Total liability (equals borrowSize)."""
  fixedBorrowAmount: str
  """Fixed-rate liability amount."""
  flexibleBorrowAmount: str
  """Floating-rate liability (totalBorrowAmount minus fixedBorrowAmount)."""
  spotTotalBorrow: str
  """Spot liability plus open-order liability."""
  derivativesBorrow: str
  """Derivatives liability (borrowSize minus spotBorrow minus reservation)."""


class Request(TypedDict):
  currency: str
  """Coin name, uppercase only."""


class Liability(RpcEndpoint):
  """Query liability information for a specified cryptocurrency, including total borrow amounts across fixed-rate, flexible-rate, spot and derivatives accounts."""

  async def liability(
    self,
    currency: str,
    *,
    validate: bool | None = None,
  ) -> LiabilityResult:
    """Query liability information for a specified cryptocurrency, including total borrow amounts across fixed-rate, flexible-rate, spot and derivatives accounts.

    Args:
      currency: Coin name, uppercase only.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/liability)
    """
    return await self.request(
      Request(currency=currency),
      method='GET',
      path='/v5/spot-margin-trade/liability',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=LiabilityResult,
    )
