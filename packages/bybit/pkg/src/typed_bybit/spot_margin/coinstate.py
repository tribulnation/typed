# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class CoinState(TypedDict):
  """Spot margin leverage for one coin."""

  currency: str
  """Coin name, uppercase."""
  spotLeverage: str
  """Spot margin leverage. Empty string if spot margin mode is off."""


class Request(TypedDict):
  currency: NotRequired[str]
  """Coin name, uppercase only. Omit to return every coin."""


class CoinStateResult(TypedDict):
  """Spot margin leverage by coin."""

  list: list[CoinState]
  """Spot margin leverage for one coin."""


class Coinstate(RpcEndpoint):
  """Query spot margin leverage information for cryptocurrencies available on the platform."""

  async def coinstate(
    self,
    currency: str | None = None,
    *,
    validate: bool | None = None,
  ) -> CoinStateResult:
    """Query spot margin leverage information for cryptocurrencies available on the platform.

    Args:
      currency: Coin name, uppercase only. Omit to return every coin.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/coinstate)
    """
    request: Request = Request()
    if currency is not None:
      request['currency'] = currency
    return await self.request(
      request,
      method='GET',
      path='/v5/spot-margin-trade/coinstate',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CoinStateResult,
    )
