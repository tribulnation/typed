# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class FixedBorrowContract(TypedDict):
  """One fixed-rate borrow contract."""

  annualRate: str
  """Interest rate."""
  borrowCurrency: str
  """Borrowed coin."""
  borrowTime: TimestampMillis
  """Loan time."""
  interestPaid: str
  """Interest paid so far."""
  loanId: str
  """Loan contract ID."""
  orderId: str
  """Loan order ID."""
  repaymentTime: TimestampMillis
  """Repayment deadline."""
  residualPenaltyInterest: str
  """Unpaid penalty interest."""
  residualPrincipal: str
  """Outstanding principal balance."""
  status: Literal[1, 2, 3]
  """Contract status."""
  term: Literal['7', '14', '30', '90', '180']
  """Fixed term, in days."""
  repayType: Literal['0', '1', '2']
  """Repayment method."""
  strategyType: Literal['PARTIAL', 'FULL']
  """Fill strategy."""


class Request(TypedDict):
  orderId: NotRequired[str]
  """Loan order ID."""
  orderCurrency: NotRequired[str]
  """Loan coin name."""
  term: NotRequired[Literal['7', '14', '30', '90', '180']]
  """Fixed term, in days."""
  limit: NotRequired[int]
  """Data size per page."""
  cursor: NotRequired[str]
  """Pagination cursor. Pass the `nextPageCursor` returned by the previous page; omit for the first page."""


class FixedBorrowContractInfoResult(TypedDict):
  """Page of fixed-rate borrow contracts."""

  nextPageCursor: NotRequired[str]
  """Opaque cursor for the next page. Empty string means there are no further pages."""
  list: list[FixedBorrowContract]
  """One fixed-rate borrow contract."""


class ContractInfo(RpcEndpoint):
  """Query fixed-rate borrow contract information, newest first by `borrowTime`."""

  def contract_info_paged(
    self,
    *,
    order_id: str | None = None,
    order_currency: str | None = None,
    term: Literal['7', '14', '30', '90', '180'] | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[FixedBorrowContract, str]:
    """Query fixed-rate borrow contract information, newest first by `borrowTime`.

    Paged variant of `contract_info`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      order_id: Loan order ID.
      order_currency: Loan coin name.
      term: Fixed term, in days.
      limit: Data size per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/fixedborrow-contract-info)
    """

    async def next(cursor: str) -> tuple[list[FixedBorrowContract], str | None]:
      response = await self.contract_info(
        order_id=order_id,
        order_currency=order_currency,
        term=term,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('list')
      state = response.get('nextPageCursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def contract_info(
    self,
    *,
    order_id: str | None = None,
    order_currency: str | None = None,
    term: Literal['7', '14', '30', '90', '180'] | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> FixedBorrowContractInfoResult:
    """Query fixed-rate borrow contract information, newest first by `borrowTime`.

    Args:
      order_id: Loan order ID.
      order_currency: Loan coin name.
      term: Fixed term, in days.
      limit: Data size per page.
      cursor: Pagination cursor. Pass the `nextPageCursor` returned by the previous page; omit for the first page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/fixedborrow-contract-info)
    """
    request: Request = Request()
    if order_id is not None:
      request['orderId'] = order_id
    if order_currency is not None:
      request['orderCurrency'] = order_currency
    if term is not None:
      request['term'] = term
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/spot-margin-trade/fixedborrow-contract-info',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FixedBorrowContractInfoResult,
    )
