# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class FixedBorrowOrder(TypedDict):
  """One fixed-rate borrow order."""

  annualRate: str
  """Annual borrowing rate."""
  orderId: str
  """Loan order ID."""
  orderTime: TimestampMillis
  """Order creation time."""
  filledQty: str
  """Filled quantity."""
  orderQty: str
  """Order quantity."""
  orderCurrency: str
  """Coin name."""
  state: Literal[1, 2, 3, 4, 5]
  """Order status."""
  term: Literal[7, 14, 30, 90, 180]
  """Fixed term, in days."""
  repayType: Literal['1', '2']
  """Repayment method."""
  strategyType: Literal['PARTIAL', 'FULL']
  """Fill strategy."""


class Request(TypedDict):
  orderId: NotRequired[str]
  """Loan order ID."""
  orderCurrency: NotRequired[str]
  """Loan coin name."""
  state: NotRequired[Literal['1', '2', '3', '4']]
  """Borrow order status."""
  term: NotRequired[Literal['7', '14', '30', '90', '180']]
  """Fixed term, in days."""
  limit: NotRequired[int]
  """Data size per page."""
  cursor: NotRequired[str]
  """Pagination cursor. Pass the `nextPageCursor` returned by the previous page; omit for the first page."""


class FixedBorrowOrderInfoResult(TypedDict):
  """Page of fixed-rate borrow orders."""

  nextPageCursor: NotRequired[str]
  """Opaque cursor for the next page. Empty string means there are no further pages."""
  list: list[FixedBorrowOrder]
  """One fixed-rate borrow order."""


class OrderInfo(RpcEndpoint):
  """Query fixed-rate borrow orders, newest first by `orderTime`."""

  def order_info_paged(
    self,
    *,
    order_id: str | None = None,
    order_currency: str | None = None,
    state: Literal['1', '2', '3', '4'] | None = None,
    term: Literal['7', '14', '30', '90', '180'] | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[FixedBorrowOrder, str]:
    """Query fixed-rate borrow orders, newest first by `orderTime`.

    Paged variant of `order_info`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      order_id: Loan order ID.
      order_currency: Loan coin name.
      state: Borrow order status.
      term: Fixed term, in days.
      limit: Data size per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/fixedborrow-order-info)
    """

    async def next(cursor: str) -> tuple[list[FixedBorrowOrder], str | None]:
      response = await self.order_info(
        order_id=order_id,
        order_currency=order_currency,
        state=state,
        term=term,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('list')
      state_ = response.get('nextPageCursor')
      return rows or [], state_ or None

    return PaginatedResponse('', next)

  async def order_info(
    self,
    *,
    order_id: str | None = None,
    order_currency: str | None = None,
    state: Literal['1', '2', '3', '4'] | None = None,
    term: Literal['7', '14', '30', '90', '180'] | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> FixedBorrowOrderInfoResult:
    """Query fixed-rate borrow orders, newest first by `orderTime`.

    Args:
      order_id: Loan order ID.
      order_currency: Loan coin name.
      state: Borrow order status.
      term: Fixed term, in days.
      limit: Data size per page.
      cursor: Pagination cursor. Pass the `nextPageCursor` returned by the previous page; omit for the first page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/fixedborrow-order-info)
    """
    request: Request = Request()
    if order_id is not None:
      request['orderId'] = order_id
    if order_currency is not None:
      request['orderCurrency'] = order_currency
    if state is not None:
      request['state'] = state
    if term is not None:
      request['term'] = term
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/spot-margin-trade/fixedborrow-order-info',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FixedBorrowOrderInfoResult,
    )
