# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class FixedBorrowQuote(TypedDict):
  """One available quote."""

  orderCurrency: str
  """Coin name."""
  term: Literal[7, 14, 30, 60, 90, 180]
  """Fixed term, in days."""
  annualRate: str
  """Annual interest rate for this quote."""
  qty: str
  """Quantity available at this rate."""


class Request(TypedDict):
  orderCurrency: str
  """Coin name."""
  term: NotRequired[Literal['7', '14', '30', '90', '180']]
  """Fixed term, in days."""
  orderBy: NotRequired[Literal['apy', 'term', 'quantity']]
  """Field to sort by."""
  sort: NotRequired[Literal[0, 1]]
  """Sort direction."""
  limit: NotRequired[int]
  """Results per page."""


class FixedBorrowOrderQuoteResult(TypedDict):
  """Available fixed-rate borrow quotes."""

  list: list[FixedBorrowQuote]
  """One available quote."""


class OrderQuote(RpcEndpoint):
  """Query available fixed-rate borrow quotes for a coin, to see lending terms and rates before initiating a borrow."""

  async def order_quote(
    self,
    order_currency: str,
    *,
    term: Literal['7', '14', '30', '90', '180'] | None = None,
    order_by: Literal['apy', 'term', 'quantity'] | None = None,
    sort: Literal[0, 1] | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> FixedBorrowOrderQuoteResult:
    """Query available fixed-rate borrow quotes for a coin, to see lending terms and rates before initiating a borrow.

    Args:
      order_currency: Coin name.
      term: Fixed term, in days.
      order_by: Field to sort by.
      sort: Sort direction.
      limit: Results per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/fixedborrow-order-quote)
    """
    request: Request = Request(orderCurrency=order_currency)
    if term is not None:
      request['term'] = term
    if order_by is not None:
      request['orderBy'] = order_by
    if sort is not None:
      request['sort'] = sort
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/v5/spot-margin-trade/fixedborrow-order-quote',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FixedBorrowOrderQuoteResult,
    )
