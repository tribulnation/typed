# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class FixedBorrowResult(TypedDict):
  """Identifier of the created loan order."""

  orderId: str
  """Loan order ID."""


class Request(TypedDict):
  """Fixed-rate borrow order parameters."""

  orderCurrency: str
  """Currency to borrow."""
  orderAmount: str
  """Amount to borrow."""
  annualRate: str
  """Customisable annual interest rate, for example `0.02` for 2%."""
  term: Literal['7', '14', '30', '90', '180']
  """Fixed term, in days."""
  repayType: NotRequired[Literal['1', '2']]
  """Repayment strategy. Defaults to `1`."""
  strategyType: NotRequired[Literal['PARTIAL', 'FULL']]
  """Fill strategy. Defaults to `PARTIAL`."""


class Borrow(RpcEndpoint):
  """Create a fixed-rate borrow order."""

  async def borrow(
    self,
    *,
    order_currency: str,
    order_amount: str,
    annual_rate: str,
    term: Literal['7', '14', '30', '90', '180'],
    repay_type: Literal['1', '2'] | None = None,
    strategy_type: Literal['PARTIAL', 'FULL'] | None = None,
    validate: bool | None = None,
  ) -> FixedBorrowResult:
    """Create a fixed-rate borrow order.

    Args:
      order_currency: Currency to borrow.
      order_amount: Amount to borrow.
      annual_rate: Customisable annual interest rate, for example `0.02` for 2%.
      term: Fixed term, in days.
      repay_type: Repayment strategy. Defaults to `1`.
      strategy_type: Fill strategy. Defaults to `PARTIAL`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/fixedborrow)
    """
    request: Request = Request(
      orderCurrency=order_currency,
      orderAmount=order_amount,
      annualRate=annual_rate,
      term=term,
    )
    if repay_type is not None:
      request['repayType'] = repay_type
    if strategy_type is not None:
      request['strategyType'] = strategy_type
    return await self.request(
      request,
      method='POST',
      path='/v5/spot-margin-trade/fixedborrow',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FixedBorrowResult,
    )
