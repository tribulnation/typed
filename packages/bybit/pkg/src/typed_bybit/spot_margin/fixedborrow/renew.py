# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict, cast
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  """Loan to renew."""

  loanId: str
  """Loan ID to renew."""
  qty: NotRequired[str]
  """Renewal quantity. Omit to renew the entire remaining amount."""


Response = Literal['Success', 'Failure']


class Renew(RpcEndpoint):
  """Renew an existing fixed-rate borrow loan, for the entire remaining amount or a specified quantity."""

  async def renew(
    self,
    *,
    loan_id: str,
    qty: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Renew an existing fixed-rate borrow loan, for the entire remaining amount or a specified quantity.

    Args:
      loan_id: Loan ID to renew.
      qty: Renewal quantity. Omit to renew the entire remaining amount.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/fixedborrow-renew)
    """
    request: Request = Request(loanId=loan_id)
    if qty is not None:
      request['qty'] = qty
    return await self.request(
      request,
      method='POST',
      path='/v5/spot-margin-trade/fixedborrow-renew',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=cast(type, Response),
    )
