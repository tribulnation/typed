# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core import TimestampSeconds
from typed_bybit.core.http import RpcEndpoint


class FixedAvailableInventoryResult(TypedDict):
  """Available fixed-rate borrow inventory."""

  currency: str
  """Coin name, uppercase."""
  term: str
  """Fixed term, in days."""
  annualRate: str
  """Annual interest rate."""
  availableInventory: str
  """min(platform total lendable amount, UTA user remaining borrowing limit, current leverage tier's borrow limit), rounded down to borrow precision."""
  updateTime: TimestampSeconds
  """Last update time."""


class Request(TypedDict):
  currency: str
  """Coin name, uppercase only."""
  term: Literal['7', '14', '30', '90', '180']
  """Fixed term, in days."""
  annualRate: str
  """Annual interest rate, for example `0.02` for 2%."""


class AvailableInventory(RpcEndpoint):
  """Query borrowing capacity for fixed-rate loans by currency, term and rate: the minimum of the platform's total lendable amount, this account's remaining UTA borrowing limit, and the current leverage tier's borrow limit, rounded down to borrow precision."""

  async def available_inventory(
    self,
    *,
    currency: str,
    term: Literal['7', '14', '30', '90', '180'],
    annual_rate: str,
    validate: bool | None = None,
  ) -> FixedAvailableInventoryResult:
    """Query borrowing capacity for fixed-rate loans by currency, term and rate: the minimum of the platform's total lendable amount, this account's remaining UTA borrowing limit, and the current leverage tier's borrow limit, rounded down to borrow precision.

    Args:
      currency: Coin name, uppercase only.
      term: Fixed term, in days.
      annual_rate: Annual interest rate, for example `0.02` for 2%.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/fixed-available-inventory)
    """
    return await self.request(
      Request(currency=currency, term=term, annualRate=annual_rate),
      method='GET',
      path='/v5/spot-margin-trade/fixed-available-inventory',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FixedAvailableInventoryResult,
    )
