# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class PositionTier(TypedDict):
  """One position tier, smallest to largest."""

  tier: str
  """Tier number, displayed smallest to largest."""
  borrowLimit: str
  """Cumulative borrow limit for the tier."""
  positionMMR: str
  """Maintenance margin rate, 8 decimal precision."""
  positionIMR: str
  """Initial margin rate, 8 decimal precision."""
  maxLeverage: str
  """Maximum available leverage for the tier."""


class Request(TypedDict):
  currency: NotRequired[str]
  """Coin name, uppercase only. Omit to return every coin."""


class PositionTierCurrency(TypedDict):
  """Position tiers for one coin."""

  currency: str
  """Coin name, uppercase."""
  positionTiersRatioList: list[PositionTier]
  """One position tier."""


class PositionTiersResult(TypedDict):
  """Position tiers by coin."""

  list: list[PositionTierCurrency]
  """Position tiers for one coin."""


class PositionTiers(RpcEndpoint):
  """Query position tier information for spot margin trading. Query by a specific currency if provided; otherwise returns every configured currency."""

  async def position_tiers(
    self,
    currency: str | None = None,
    *,
    validate: bool | None = None,
  ) -> PositionTiersResult:
    """Query position tier information for spot margin trading. Query by a specific currency if provided; otherwise returns every configured currency.

    Args:
      currency: Coin name, uppercase only. Omit to return every coin.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/position-tiers)
    """
    request: Request = Request()
    if currency is not None:
      request['currency'] = currency
    return await self.request(
      request,
      method='GET',
      path='/v5/spot-margin-trade/position-tiers',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PositionTiersResult,
    )
