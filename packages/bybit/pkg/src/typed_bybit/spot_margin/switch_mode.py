# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  """Desired spot margin trading state."""

  spotMarginMode: Literal['0', '1']
  """`1` to enable spot margin trading, `0` to disable."""


class SwitchModeResult(TypedDict):
  """Spot margin trading state."""

  spotMarginMode: Literal['0', '1']
  """Current status: `1` enabled, `0` disabled."""


class SwitchMode(RpcEndpoint):
  """Enable or disable spot margin trading. The account must have activated spot margin trading through the web or app quiz first."""

  async def switch_mode(
    self,
    spot_margin_mode: Literal['0', '1'],
    *,
    validate: bool | None = None,
  ) -> SwitchModeResult:
    """Enable or disable spot margin trading. The account must have activated spot margin trading through the web or app quiz first.

    Args:
      spot_margin_mode: `1` to enable spot margin trading, `0` to disable.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/switch-mode)
    """
    return await self.request(
      Request(spotMarginMode=spot_margin_mode),
      method='POST',
      path='/v5/spot-margin-trade/switch-mode',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SwitchModeResult,
    )
