# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class AutoRepayModeEntry(TypedDict):
  """Auto-repay mode for one currency."""

  currency: str
  """Coin name, uppercase."""
  autoRepayMode: Literal['0', '1']
  """Mode status."""


class Request(TypedDict):
  """Auto-repay mode to set."""

  currency: NotRequired[str]
  """Coin name, uppercase only. Omit to enable automatic repayment for every currency."""
  autoRepayMode: Literal['0', '1']
  """Enables or disables automatic repayment mode."""


class SetAutoRepayModeResult(TypedDict):
  """Auto-repay mode after the change."""

  data: list[AutoRepayModeEntry]
  """Auto-repay mode for one currency."""


class SetAutoRepayMode(RpcEndpoint):
  """Configure automatic repayment for spot margin trading. When enabled, the system automatically repays liabilities without asset conversion at the 0- and 30-minute marks each hour."""

  async def set_auto_repay_mode(
    self,
    currency: str | None = None,
    *,
    auto_repay_mode: Literal['0', '1'],
    validate: bool | None = None,
  ) -> SetAutoRepayModeResult:
    """Configure automatic repayment for spot margin trading. When enabled, the system automatically repays liabilities without asset conversion at the 0- and 30-minute marks each hour.

    Args:
      currency: Coin name, uppercase only. Omit to enable automatic repayment for every currency.
      auto_repay_mode: Enables or disables automatic repayment mode.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/set-auto-repay-mode)
    """
    request: Request = Request(autoRepayMode=auto_repay_mode)
    if currency is not None:
      request['currency'] = currency
    return await self.request(
      request,
      method='POST',
      path='/v5/spot-margin-trade/set-auto-repay-mode',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SetAutoRepayModeResult,
    )
