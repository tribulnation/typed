# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampIso, TimestampMillis, TimestampSeconds
from typed_bybit.core.http import RpcEndpoint


class PointRecord(TypedDict):
  """One card reward point transaction record."""

  outOrderId: str
  """External order ID."""
  point: int
  """Point amount."""
  side: Literal['1', '2']
  """Point direction."""
  type: str
  """Point type."""
  subType: NotRequired[str]
  """Point sub-type."""
  createTime: TimestampMillis
  """Creation time, as a millisecond timestamp."""
  updateTime: NotRequired[TimestampMillis]
  """Update time, as a millisecond timestamp."""
  bizId: str
  """Point order ID."""
  bizTxnId: str
  """`consumeIdLifecycle` identifier."""
  transactionDate: NotRequired[TimestampIso]
  """Transaction date and time."""
  transactionId: NotRequired[str]
  """Transaction ID."""
  transactionAmount: NotRequired[str]
  """Transaction amount."""
  basicCurrency: NotRequired[str]
  """Transaction currency."""
  merchCategoryDesc: NotRequired[str]
  """Merchant category description."""
  merchName: NotRequired[str]
  """Merchant name."""
  merchCountry: NotRequired[str]
  """Merchant country."""
  merchCity: NotRequired[str]
  """Merchant city."""
  pan4: NotRequired[str]
  """Last 4 digits of the card number."""
  payFiatAmount: NotRequired[str]
  """Amount paid with fiat."""
  transactionCurrencyAmount: NotRequired[str]
  """Amount paid with crypto."""


class Request(TypedDict):
  """Filters for the point transaction record query."""

  type: NotRequired[str]
  """Point type filter."""
  pageSize: NotRequired[int]
  """Number of records per page. Minimum 1; defaults to 10."""
  pageNo: NotRequired[int]
  """Page number. Minimum 1; defaults to 1."""
  startTime: NotRequired[TimestampSeconds]
  """Start of the queried range, as a second timestamp."""
  endTime: NotRequired[TimestampSeconds]
  """End of the queried range, as a second timestamp."""
  outOrderId: NotRequired[str]
  """External order ID."""
  bizId: NotRequired[str]
  """Point order ID."""
  bizTxnId: NotRequired[str]
  """`consumeIdLifecycle` identifier."""
  side: NotRequired[Literal['1', '2']]
  """Point direction."""


class PointRecordsResult(TypedDict):
  """Page of matching card reward point transaction records."""

  pageSize: int
  """Number of records per page."""
  pageNo: int
  """Current page number."""
  totalCount: int
  """Total number of matching records."""
  data: list[PointRecord]
  """Point transaction records."""


class Records(RpcEndpoint):
  """Query the user's card reward point transaction records, with pagination and filtering by time range, type and direction."""

  def records_paged(
    self,
    *,
    type: str | None = None,
    page_size: int | None = None,
    start_time: TimestampSeconds | None = None,
    end_time: TimestampSeconds | None = None,
    out_order_id: str | None = None,
    biz_id: str | None = None,
    biz_txn_id: str | None = None,
    side: Literal['1', '2'] | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[PointRecord, int]:
    """Query the user's card reward point transaction records, with pagination and filtering by time range, type and direction.

    Paged variant of `records`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      type: Point type filter.
      page_size: Number of records per page. Minimum 1; defaults to 10.
      start_time: Start of the queried range, as a second timestamp.
      end_time: End of the queried range, as a second timestamp.
      out_order_id: External order ID.
      biz_id: Point order ID.
      biz_txn_id: `consumeIdLifecycle` identifier.
      side: Point direction.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/bybit-card/point/records)
    """

    async def next(page_no: int) -> tuple[list[PointRecord], int | None]:
      response = await self.records(
        type=type,
        page_size=page_size,
        start_time=start_time,
        end_time=end_time,
        out_order_id=out_order_id,
        biz_id=biz_id,
        biz_txn_id=biz_txn_id,
        side=side,
        page_no=page_no,
        validate=validate,
      )
      rows = response.get('data')
      rows = rows if rows is not None else []
      total = response.get('totalCount')
      total = int(total) if total is not None else None
      if total is None or page_size is None or (page_no - 1 + 1) * page_size >= total:
        return rows, None
      return rows, page_no + 1

    return PaginatedResponse(1, next)

  async def records(
    self,
    *,
    type: str | None = None,
    page_size: int | None = None,
    page_no: int | None = None,
    start_time: TimestampSeconds | None = None,
    end_time: TimestampSeconds | None = None,
    out_order_id: str | None = None,
    biz_id: str | None = None,
    biz_txn_id: str | None = None,
    side: Literal['1', '2'] | None = None,
    validate: bool | None = None,
  ) -> PointRecordsResult:
    """Query the user's card reward point transaction records, with pagination and filtering by time range, type and direction.

    Args:
      type: Point type filter.
      page_size: Number of records per page. Minimum 1; defaults to 10.
      page_no: Page number. Minimum 1; defaults to 1.
      start_time: Start of the queried range, as a second timestamp.
      end_time: End of the queried range, as a second timestamp.
      out_order_id: External order ID.
      biz_id: Point order ID.
      biz_txn_id: `consumeIdLifecycle` identifier.
      side: Point direction.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/bybit-card/point/records)
    """
    request: Request = Request()
    if type is not None:
      request['type'] = type
    if page_size is not None:
      request['pageSize'] = page_size
    if page_no is not None:
      request['pageNo'] = page_no
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if out_order_id is not None:
      request['outOrderId'] = out_order_id
    if biz_id is not None:
      request['bizId'] = biz_id
    if biz_txn_id is not None:
      request['bizTxnId'] = biz_txn_id
    if side is not None:
      request['side'] = side
    return await self.request(
      request,
      method='POST',
      path='/v5/card/reward/points/records',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PointRecordsResult,
    )
