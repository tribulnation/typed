# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class PointCashbackDetail(TypedDict):
  """Cashback detail for one card reward order."""

  points: str
  """Point amount."""
  amt: str
  """Cashback amount."""
  ccy: str
  """Currency code."""
  ccyType: Literal['FIAT', 'CRYPTO']
  """Currency type."""
  createTime: NotRequired[str]
  """Creation time."""
  bizTxnId: str
  """Order ID."""
  sourceId: int
  """Coupon/voucher ID."""
  sourceCode: str
  """External order ID."""
  orderStatus: int
  """Order status."""
  orderSubStatus: int
  """Order sub-status."""
  orderShowStatus: Literal[
    'NO_PAY', 'ORDER_PENDING_SHOW', 'ORDER_SUCCESS', 'ORDER_FAIL'
  ]
  """Display status."""
  failedBizCode: NotRequired[str]
  """Failure reason code."""


class Request(TypedDict):
  """Cashback order lookup."""

  bizTxnId: str
  """Order ID."""


class CashbackDetail(RpcEndpoint):
  """Query the cashback detail for a specific card reward order, including point amount, cashback value, currency and order status."""

  async def cashback_detail(
    self,
    biz_txn_id: str,
    *,
    validate: bool | None = None,
  ) -> PointCashbackDetail:
    """Query the cashback detail for a specific card reward order, including point amount, cashback value, currency and order status.

    Args:
      biz_txn_id: Order ID.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/bybit-card/point/cashback-detail)
    """
    return await self.request(
      Request(bizTxnId=biz_txn_id),
      method='POST',
      path='/v5/card/reward/point/cashback/detail',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PointCashbackDetail,
    )
