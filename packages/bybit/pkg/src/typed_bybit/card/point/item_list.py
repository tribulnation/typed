# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class PointMallItem(TypedDict):
  """One card reward mall item."""

  itemId: str
  """Item ID."""
  itemName: str
  """Item name."""
  priority: int
  """Display priority."""
  onTime: TimestampMillis
  """Listing time, as a millisecond timestamp."""
  offTime: TimestampMillis
  """Delisting time, as a millisecond timestamp."""
  price: str
  """Original price."""
  discountPrice: str
  """Discounted price."""
  totalNum: int
  """Total available quantity."""
  redeemNum: int
  """Quantity already redeemed."""
  picPath: str
  """Image URL."""
  currency: Literal['USDT', 'USDC', 'BTC', 'ETH', 'POINT']
  """Price currency."""
  currencyType: int
  """Currency type: fiat, crypto or point (`FIAT`/`CRYPTO`/`POINT`); the docs give no numeric code for each."""
  itemType: int
  """Item type: virtual or physical (`VIRTUAL`/`ENTITY`); the docs give no numeric code for each."""
  itemBizType: int
  """Item sub-type: points-priced or currency-priced (`POINTS`/`CURRENCY`); the docs give no numeric code for each."""


class Request(TypedDict):
  """Filters for the reward mall item list."""

  pageNo: NotRequired[int]
  """Page number. Minimum 1; defaults to 1."""
  pageSize: NotRequired[int]
  """Number of items per page. Minimum 1; defaults to 10."""
  itemType: NotRequired[Literal[1, 2]]
  """Item type."""
  itemBizType: NotRequired[Literal[1, 2]]
  """Item sub-type."""
  orderBy: NotRequired[Literal[1, 2, 3]]
  """Sort criteria."""
  asc: NotRequired[bool]
  """Whether to sort in ascending order."""
  source: NotRequired[Literal[0, 1]]
  """Query source."""


class PointItemListResult(TypedDict):
  """Page of card reward mall items."""

  pageNo: int
  """Current page number."""
  pageSize: int
  """Number of items per page."""
  totalCount: int
  """Total number of matching items."""
  data: list[PointMallItem]
  """Mall items."""


class ItemList(RpcEndpoint):
  """Query the list of items available in the card reward mall, with pagination, sorting and filtering by item type."""

  def item_list_paged(
    self,
    *,
    page_size: int | None = None,
    item_type: Literal[1, 2] | None = None,
    item_biz_type: Literal[1, 2] | None = None,
    order_by: Literal[1, 2, 3] | None = None,
    asc: bool | None = None,
    source: Literal[0, 1] | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[PointMallItem, int]:
    """Query the list of items available in the card reward mall, with pagination, sorting and filtering by item type.

    Paged variant of `item_list`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      page_size: Number of items per page. Minimum 1; defaults to 10.
      item_type: Item type.
      item_biz_type: Item sub-type.
      order_by: Sort criteria.
      asc: Whether to sort in ascending order.
      source: Query source.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/bybit-card/point/item-list)
    """

    async def next(page_no: int) -> tuple[list[PointMallItem], int | None]:
      response = await self.item_list(
        page_size=page_size,
        item_type=item_type,
        item_biz_type=item_biz_type,
        order_by=order_by,
        asc=asc,
        source=source,
        page_no=page_no,
        validate=validate,
      )
      rows = response.get('data')
      rows = rows if rows is not None else []
      total = response.get('totalCount')
      total = int(total) if total is not None else None
      if total is None or page_size is None or (page_no - 1 + 1) * page_size >= total:
        return rows, None
      return rows, page_no + 1

    return PaginatedResponse(1, next)

  async def item_list(
    self,
    *,
    page_no: int | None = None,
    page_size: int | None = None,
    item_type: Literal[1, 2] | None = None,
    item_biz_type: Literal[1, 2] | None = None,
    order_by: Literal[1, 2, 3] | None = None,
    asc: bool | None = None,
    source: Literal[0, 1] | None = None,
    validate: bool | None = None,
  ) -> PointItemListResult:
    """Query the list of items available in the card reward mall, with pagination, sorting and filtering by item type.

    Args:
      page_no: Page number. Minimum 1; defaults to 1.
      page_size: Number of items per page. Minimum 1; defaults to 10.
      item_type: Item type.
      item_biz_type: Item sub-type.
      order_by: Sort criteria.
      asc: Whether to sort in ascending order.
      source: Query source.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/bybit-card/point/item-list)
    """
    request: Request = Request()
    if page_no is not None:
      request['pageNo'] = page_no
    if page_size is not None:
      request['pageSize'] = page_size
    if item_type is not None:
      request['itemType'] = item_type
    if item_biz_type is not None:
      request['itemBizType'] = item_biz_type
    if order_by is not None:
      request['orderBy'] = order_by
    if asc is not None:
      request['asc'] = asc
    if source is not None:
      request['source'] = source
    return await self.request(
      request,
      method='POST',
      path='/v5/card/reward/mall/item/list',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PointItemListResult,
    )
