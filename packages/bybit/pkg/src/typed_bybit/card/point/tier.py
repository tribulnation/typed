# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class PointTier(TypedDict):
  """Card reward tier level and spending limit."""

  usedLimit: str
  """Spending limit consumed, converted from points at 0.002 per point and rounded to 2 decimal places."""
  limit: str
  """Total spending limit."""
  unit: str
  """Limit denomination, for example `USD`."""
  tier: str
  """User's tier level."""
  autoCashback: bool
  """Whether auto-cashback is enabled."""


class Tier(RpcEndpoint):
  """Query the user's card reward tier level and spending limit information, including used limit, total limit, tier level and auto-cashback setting."""

  async def tier(self, *, validate: bool | None = None) -> PointTier:
    """Query the user's card reward tier level and spending limit information, including used limit, total limit, tier level and auto-cashback setting.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/bybit-card/point/tier)
    """
    return await self.request(
      None,
      method='POST',
      path='/v5/card/reward/points/tier',
      meta={'signed': True},
      validate=validate,
      response_type=PointTier,
    )
