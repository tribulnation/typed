# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .asset_records import AssetRecords
from .point import Point


class Card(AssetRecords):
  """Bybit Card — the crypto debit card product: querying card transaction (asset) records, plus the `point` sub-grouping for the card's reward-points loyalty program (tier, balance, redeemable items, point history, and cashback detail).

  References:
    - [Upstream docs](https://bybit-exchange.github.io/docs/v5/bybit-card/asset-records)
  """

  @cached_property
  def point(self) -> Point:
    """Bybit Card Points — the card's reward-points loyalty program: query a user's point balance and reward tier level, browse the redeemable mall item list, query point transaction records, and check cashback detail.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/bybit-card/point/balance)
    """
    return Point(client=self.client)
