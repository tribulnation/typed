# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .discount_buy import DiscountBuy
from .double_win import DoubleWin
from .dual_asset import DualAsset
from .liquidity_mining import LiquidityMining
from .smart_lvg import SmartLvg
from typed_bybit.core.http import RpcEndpoint


class AdvancedEarn(RpcEndpoint):
  """Advanced Earn — Bybit's family of structured/derivative earn products (Discount Buy, Double Win, Dual Asset, Smart Leverage, and Liquidity Mining), each letting a user quote, place, and manage a position in a coin-settled or dual-currency structured product. The public WebSocket topics that push each product's live offer quotes live under the sibling `finance_ws` connection. Discount Buy, Double Win, Dual Asset, and Smart Leverage all share the same underlying product-listing wire endpoint, distinguished only by a `category` query parameter.

  References:
    - [Upstream docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/discount-buy/product-info)
  """

  @cached_property
  def discount_buy(self) -> DiscountBuy:
    """Discount Buy — an Advanced Earn structured product that lets a user commit to buying an underlying coin below the current index price if it dips to a set knockout price by expiry, or otherwise redeem a fixed coupon: get a live quote, place an order, and list open/historical orders and positions.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/discount-buy/create-order)
    """
    return DiscountBuy(client=self.client)

  @cached_property
  def double_win(self) -> DoubleWin:
    """Double Win — an Advanced Earn structured product paying an enhanced coupon whether the underlying settles above or below a chosen strike, available both as a fixed-range quote and as a custom RFQ-priced quote (via the leverage endpoint): get a quote, place an order, estimate the redemption amount, and list orders and positions.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/double-win/product-info)
    """
    return DoubleWin(client=self.client)

  @cached_property
  def dual_asset(self) -> DualAsset:
    """Dual Asset — an Advanced Earn structured product that invests in one of two coins (e.g. buy-low or sell-high) at a target settlement price chosen by the user, paying an enhanced yield regardless of which side settles: get a live quote, place an order, and list orders and positions.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/dual-asset/product-info)
    """
    return DualAsset(client=self.client)

  @cached_property
  def liquidity_mining(self) -> LiquidityMining:
    """Liquidity Mining — an Advanced Earn product for providing paired-coin liquidity to earn pool yield: add or remove liquidity, add margin, claim or reinvest accrued interest, and review liquidation and yield records for an open position.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/liquidity-mining/product-info)
    """
    return LiquidityMining(client=self.client)

  @cached_property
  def smart_lvg(self) -> SmartLvg:
    """Smart Leverage — an Advanced Earn structured product offering leveraged directional (long/short) exposure to an underlying coin over a fixed duration: get a live quote, place an order, estimate the redemption amount, and list orders and positions.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/smart-lvg/product-info)
    """
    return SmartLvg(client=self.client)
