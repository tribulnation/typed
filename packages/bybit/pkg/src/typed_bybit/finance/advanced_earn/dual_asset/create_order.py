# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class DualAssetInterestCard(TypedDict):
  """Interest bonus card to apply, if any."""

  awardId: int
  """Bonus card unique identifier."""
  specCode: str
  """Bonus card specification code."""


class DualAssetOrderResult(TypedDict):
  """Identifiers for a placed Dual Asset order."""

  orderId: str
  """System-generated order identifier."""
  orderLinkId: str
  """Caller-supplied order link identifier."""


class DualAssetsExtra(TypedDict):
  """Dual Asset pricing selection, from a prior product-quote call."""

  orderDirection: Literal['BuyLow', 'SellHigh']
  """Order direction."""
  selectPrice: str
  """Target price selected from a valid quote tier."""
  apyE8: int
  """Minimum acceptable APY, e8 precision, from the same quote tier."""


class Request(TypedDict):
  """Dual Asset structured product subscription."""

  category: Literal['DualAssets']
  """Product category."""
  productId: str
  """Product identifier."""
  orderType: Literal['Stake']
  """Operation type."""
  amount: str
  """Order quantity."""
  accountType: Literal['FUND', 'UNIFIED']
  """Source account funding the order."""
  coin: str
  """Investment coin."""
  orderLinkId: str
  """Caller-supplied order ID, max 36 characters."""
  dualAssetsExtra: DualAssetsExtra
  interestCard: NotRequired[DualAssetInterestCard]


class CreateOrder(RpcEndpoint):
  """Subscribe to a Dual Asset (Buy Low / Sell High) structured product using a price tier from a prior product-quote call. Requires API key "Earn" permission."""

  async def create_order(
    self,
    category: Literal['DualAssets'] = 'DualAssets',
    *,
    product_id: str,
    order_type: Literal['Stake'] = 'Stake',
    amount: str,
    account_type: Literal['FUND', 'UNIFIED'],
    coin: str,
    order_link_id: str,
    dual_assets_extra: DualAssetsExtra,
    interest_card: DualAssetInterestCard | None = None,
    validate: bool | None = None,
  ) -> DualAssetOrderResult:
    """Subscribe to a Dual Asset (Buy Low / Sell High) structured product using a price tier from a prior product-quote call. Requires API key "Earn" permission.

    Args:
      category: Product category.
      product_id: Product identifier.
      order_type: Operation type.
      amount: Order quantity.
      account_type: Source account funding the order.
      coin: Investment coin.
      order_link_id: Caller-supplied order ID, max 36 characters.
      dual_assets_extra: Dual Asset pricing selection, from a prior product-quote call.
      interest_card: Interest bonus card to apply, if any.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/dual-asset/create-order)
    """
    request: Request = Request(
      category=category,
      productId=product_id,
      orderType=order_type,
      amount=amount,
      accountType=account_type,
      coin=coin,
      orderLinkId=order_link_id,
      dualAssetsExtra=dual_assets_extra,
    )
    if interest_card is not None:
      request['interestCard'] = interest_card
    return await self.request(
      request,
      method='POST',
      path='/v5/earn/advance/place-order',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=DualAssetOrderResult,
    )
