# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  category: Literal['SmartLeverage']
  """Product category."""
  productId: NotRequired[str]
  """Product identifier."""
  orderId: NotRequired[str]
  """Order ID."""
  orderLinkId: NotRequired[str]
  """Order link ID."""
  startTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  limit: NotRequired[int]
  """Page size."""
  cursor: NotRequired[str]
  """Pagination cursor."""


class SmartLvgOrder(TypedDict):
  """One Smart Leverage order."""

  orderId: str
  """Unique order identifier."""
  orderLinkId: str
  """Caller-supplied order link identifier."""
  productId: str
  """Product identifier."""
  orderType: Literal['Stake', 'Redeem']
  """Operation type."""
  amount: str
  """Order quantity."""
  createdTime: TimestampMillis
  """Creation time, as a millisecond timestamp."""
  status: Literal['Pending', 'Success', 'Settled', 'Fail']
  """Order status."""
  duration: str
  """Product term, e.g. "1d", "2d"."""
  accountType: Literal['FUND', 'UNIFIED']
  """Account type."""
  investCoin: str
  """Investment coin."""
  underlyingAsset: str
  """Underlying asset, e.g. `BTC`, `ETH`."""
  direction: Literal['Long', 'Short']
  """Trade direction."""
  leverage: str
  """Leverage multiplier."""
  pnl: NotRequired[str]
  """Realized profit/loss. Populated once settled."""
  settlementPrice: NotRequired[str]
  """Final settlement price. Populated once settled."""


class SmartLvgOrderHistory(TypedDict):
  """One page of Smart Leverage order history."""

  category: Literal['SmartLeverage', '']
  """Product category. Empty on an empty page, observed live."""
  list: list[SmartLvgOrder]
  """Order records."""
  nextPageCursor: str
  """Cursor for the next page. Empty when there is no further page."""


class Order(RpcEndpoint):
  """Get the caller's Smart Leverage order history. Requires API key "Earn" permission."""

  def order_paged(
    self,
    category: Literal['SmartLeverage'] = 'SmartLeverage',
    *,
    product_id: str | None = None,
    order_id: str | None = None,
    order_link_id: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[SmartLvgOrder, str]:
    """Get the caller's Smart Leverage order history. Requires API key "Earn" permission.

    Paged variant of `order`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      category: Product category.
      product_id: Product identifier.
      order_id: Order ID.
      order_link_id: Order link ID.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/smart-lvg/order)
    """

    async def next(cursor: str) -> tuple[list[SmartLvgOrder], str | None]:
      response = await self.order(
        category,
        product_id=product_id,
        order_id=order_id,
        order_link_id=order_link_id,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('list')
      state = response.get('nextPageCursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def order(
    self,
    category: Literal['SmartLeverage'] = 'SmartLeverage',
    *,
    product_id: str | None = None,
    order_id: str | None = None,
    order_link_id: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> SmartLvgOrderHistory:
    """Get the caller's Smart Leverage order history. Requires API key "Earn" permission.

    Args:
      category: Product category.
      product_id: Product identifier.
      order_id: Order ID.
      order_link_id: Order link ID.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      limit: Page size.
      cursor: Pagination cursor.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/smart-lvg/order)
    """
    request: Request = Request(category=category)
    if product_id is not None:
      request['productId'] = product_id
    if order_id is not None:
      request['orderId'] = order_id
    if order_link_id is not None:
      request['orderLinkId'] = order_link_id
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/earn/advance/order',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SmartLvgOrderHistory,
    )
