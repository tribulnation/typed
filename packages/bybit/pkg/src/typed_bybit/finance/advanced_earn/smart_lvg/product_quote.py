# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  category: Literal['SmartLeverage']
  """Product category."""
  productId: str
  """Product identifier."""


class SmartLvgProductQuote(TypedDict):
  """Live institutional quote for a Smart Leverage product."""

  category: Literal['SmartLeverage']
  """Product category."""
  productId: str
  """Product identifier."""
  breakevenPrice: NotRequired[str]
  """Breakeven price, the best available institutional quote. Empty when no active quote."""
  currentPrice: str
  """Current market price of the underlying asset. Always present."""
  expireAt: NotRequired[TimestampMillis]
  """Quote expiry time, as a millisecond timestamp. Empty when no active quote."""
  maxInvestmentAmount: NotRequired[str]
  """Maximum single order amount at the current quote. Empty when no active quote."""


class ProductQuote(RpcEndpoint):
  """Get the live institutional quote for a Smart Leverage product, to be passed into a subsequent order. No authentication required."""

  async def product_quote(
    self,
    category: Literal['SmartLeverage'] = 'SmartLeverage',
    *,
    product_id: str,
    validate: bool | None = None,
  ) -> SmartLvgProductQuote:
    """Get the live institutional quote for a Smart Leverage product, to be passed into a subsequent order. No authentication required.

    Args:
      category: Product category.
      product_id: Product identifier.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/smart-lvg/product-quote)
    """
    return await self.request(
      Request(category=category, productId=product_id),
      method='GET',
      path='/v5/earn/advance/product-extra-info',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=SmartLvgProductQuote,
    )
