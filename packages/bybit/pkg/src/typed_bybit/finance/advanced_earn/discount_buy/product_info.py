# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class DiscountBuyProduct(TypedDict):
  """One Discount Buy product."""

  category: Literal['DiscountBuy']
  """Product category."""
  productId: str
  """Unique product identifier."""
  coin: str
  """Investment coin, e.g. `USDT`."""
  underlyingAsset: str
  """Underlying asset the product buys, e.g. `BTC`, `ETH`."""
  settlementTime: TimestampMillis
  """Settlement time, as a millisecond timestamp."""
  duration: str
  """Product duration, e.g. "7d", "1d"."""
  isVipProduct: NotRequired[bool]
  """Whether this product is VIP-exclusive."""
  subscribeStartAt: NotRequired[TimestampMillis]
  """Subscription window opening time, as a millisecond timestamp."""
  subscribeEndAt: NotRequired[TimestampMillis]
  """Subscription window closing time, as a millisecond timestamp."""
  minPurchaseAmount: str
  """Minimum investment amount."""
  remainingAmount: str
  """Remaining purchasable quota."""
  orderPrecisionDigital: int
  """Decimal precision for order amounts."""
  expectReceiveAt: NotRequired[TimestampMillis]
  """Expected settlement credit time, as a millisecond timestamp (typically `settlementTime` + 15 minutes)."""


class Request(TypedDict):
  category: Literal['DiscountBuy']
  """Product category."""
  coin: NotRequired[str]
  """Underlying asset."""


class DiscountBuyProductList(TypedDict):
  """Discount Buy structured products."""

  category: Literal['DiscountBuy']
  """Product category echoed back."""
  list: list[DiscountBuyProduct]
  """Product records."""


class ProductInfo(RpcEndpoint):
  """Get available Discount Buy structured products. No authentication required."""

  async def product_info(
    self,
    category: Literal['DiscountBuy'] = 'DiscountBuy',
    *,
    coin: str | None = None,
    validate: bool | None = None,
  ) -> DiscountBuyProductList:
    """Get available Discount Buy structured products. No authentication required.

    Args:
      category: Product category.
      coin: Underlying asset.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/discount-buy/product-info)
    """
    request: Request = Request(category=category)
    if coin is not None:
      request['coin'] = coin
    return await self.request(
      request,
      method='GET',
      path='/v5/earn/advance/product',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=DiscountBuyProductList,
    )
