# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class DiscountBuyExtra(TypedDict):
  """Pricing parameters, echoed unchanged from a prior `product_quote` call."""

  initialPrice: str
  """Current index price of the underlying asset. Max 8 decimal places."""
  purchasePrice: str
  """Anchor buy price. Max 8 decimal places."""
  knockoutPrice: str
  """Knockout threshold price."""
  knockoutCouponE8: str
  """Annualized interest rate, e8 precision."""
  settleType: Literal['Base', 'Quote']
  """Settlement preference."""
  instUid: str
  """Market maker institution identifier."""


class DiscountBuyOrderResult(TypedDict):
  """Identifiers for a placed Discount Buy order."""

  orderId: str
  """System-generated order identifier."""
  orderLinkId: str
  """Caller-supplied order link identifier."""


class Request(TypedDict):
  """Discount Buy structured product subscription."""

  category: Literal['DiscountBuy']
  """Product category."""
  productId: str
  """Product identifier."""
  orderType: Literal['Stake']
  """Operation type."""
  amount: str
  """Investment amount in USDT. Precision follows the product's `orderPrecisionDigital`."""
  coin: Literal['USDT']
  """Investment coin."""
  accountType: Literal['FUND', 'UNIFIED']
  """Source account funding the order. `UNIFIED` requires a Unified Trading Account."""
  orderLinkId: str
  """Caller-supplied order ID, max 40 characters, alphanumeric plus `_`/`-` only."""
  discountBuyExtra: DiscountBuyExtra


class CreateOrder(RpcEndpoint):
  """Subscribe to a Discount Buy structured product using pricing parameters from a prior product-quote call. Requires API key "Earn" permission."""

  async def create_order(
    self,
    category: Literal['DiscountBuy'] = 'DiscountBuy',
    *,
    product_id: str,
    order_type: Literal['Stake'] = 'Stake',
    amount: str,
    coin: Literal['USDT'] = 'USDT',
    account_type: Literal['FUND', 'UNIFIED'],
    order_link_id: str,
    discount_buy_extra: DiscountBuyExtra,
    validate: bool | None = None,
  ) -> DiscountBuyOrderResult:
    """Subscribe to a Discount Buy structured product using pricing parameters from a prior product-quote call. Requires API key "Earn" permission.

    Args:
      category: Product category.
      product_id: Product identifier.
      order_type: Operation type.
      amount: Investment amount in USDT. Precision follows the product's `orderPrecisionDigital`.
      coin: Investment coin.
      account_type: Source account funding the order. `UNIFIED` requires a Unified Trading Account.
      order_link_id: Caller-supplied order ID, max 40 characters, alphanumeric plus `_`/`-` only.
      discount_buy_extra: Pricing parameters, echoed unchanged from a prior `product_quote` call.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/discount-buy/create-order)
    """
    return await self.request(
      Request(
        category=category,
        productId=product_id,
        orderType=order_type,
        amount=amount,
        coin=coin,
        accountType=account_type,
        orderLinkId=order_link_id,
        discountBuyExtra=discount_buy_extra,
      ),
      method='POST',
      path='/v5/earn/advance/place-order',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=DiscountBuyOrderResult,
    )
