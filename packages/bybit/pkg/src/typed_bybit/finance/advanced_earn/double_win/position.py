# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class DoubleWinPosition(TypedDict):
  """One active Double Win position."""

  positionId: str
  """Unique position identifier."""
  productId: str
  """Product identifier."""
  investCoin: str
  """Investment coin."""
  underlyingAsset: str
  """Underlying asset."""
  amount: str
  """Invested quantity, in `investCoin`."""
  leverage: NotRequired[str]
  """Leverage multiplier applied to this position."""
  initialPrice: NotRequired[str]
  """Underlying asset price at position opening."""
  lowerPrice: NotRequired[str]
  """Price range lower bound."""
  upperPrice: NotRequired[str]
  """Price range upper bound."""
  duration: str
  """Product term, e.g. "1d", "3d"."""
  settlementTime: TimestampMillis
  """Settlement time, as a millisecond timestamp."""
  createdTime: TimestampMillis
  """Position creation time, as a millisecond timestamp."""
  status: Literal['Active', 'Redeeming']
  """Position status."""
  redeemable: bool
  """Whether early redemption is currently permitted."""
  accountType: Literal['FUND', 'UNIFIED']
  """Account type."""
  orderId: str
  """Order that created this position."""


class Request(TypedDict):
  category: Literal['DoubleWin']
  """Product category."""
  productId: NotRequired[str]
  """Product identifier."""
  coin: NotRequired[str]
  """Underlying asset."""
  limit: NotRequired[int]
  """Page size."""
  cursor: NotRequired[str]
  """Pagination cursor."""


class DoubleWinPositionHistory(TypedDict):
  """One page of active Double Win positions."""

  category: Literal['DoubleWin']
  """Product category."""
  list: list[DoubleWinPosition]
  """Position records."""
  nextPageCursor: str
  """Cursor for the next page. Empty when there is no further page."""


class Position(RpcEndpoint):
  """Get the caller's active Double Win positions. Requires API key "Earn" permission."""

  def position_paged(
    self,
    category: Literal['DoubleWin'] = 'DoubleWin',
    *,
    product_id: str | None = None,
    coin: str | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[DoubleWinPosition, str]:
    """Get the caller's active Double Win positions. Requires API key "Earn" permission.

    Paged variant of `position`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      category: Product category.
      product_id: Product identifier.
      coin: Underlying asset.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/double-win/position)
    """

    async def next(cursor: str) -> tuple[list[DoubleWinPosition], str | None]:
      response = await self.position(
        category,
        product_id=product_id,
        coin=coin,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('list')
      state = response.get('nextPageCursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def position(
    self,
    category: Literal['DoubleWin'] = 'DoubleWin',
    *,
    product_id: str | None = None,
    coin: str | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> DoubleWinPositionHistory:
    """Get the caller's active Double Win positions. Requires API key "Earn" permission.

    Args:
      category: Product category.
      product_id: Product identifier.
      coin: Underlying asset.
      limit: Page size.
      cursor: Pagination cursor.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/double-win/position)
    """
    request: Request = Request(category=category)
    if product_id is not None:
      request['productId'] = product_id
    if coin is not None:
      request['coin'] = coin
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/earn/advance/position',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=DoubleWinPositionHistory,
    )
