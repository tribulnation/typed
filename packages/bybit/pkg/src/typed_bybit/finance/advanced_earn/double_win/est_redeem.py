# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class DoubleWinEstRedeem(TypedDict):
  """Estimated redemption for one position."""

  success: bool
  """Whether the estimate calculation succeeded for this position."""
  positionId: str
  """Position identifier."""
  estRedeemAmount: NotRequired[str]
  """Estimated redemption amount, in `investCoin`."""
  estRedeemTime: NotRequired[TimestampMillis]
  """Expected fund delivery time, as a millisecond timestamp."""
  slippageRate: NotRequired[str]
  """Slippage tolerance rate for this redemption, e.g. "0.05" for 5%."""


class Request(TypedDict):
  category: Literal['DoubleWin']
  """Product category."""
  positionIds: str
  """Comma-separated position identifiers."""


class DoubleWinEstRedeemList(TypedDict):
  """Estimated redemption amounts for one or more positions."""

  list: list[DoubleWinEstRedeem]
  """One entry per requested position."""


class EstRedeem(RpcEndpoint):
  """Get the estimated early-redemption amount for one or more Double Win positions. Requires API key "Earn" permission."""

  async def est_redeem(
    self,
    category: Literal['DoubleWin'] = 'DoubleWin',
    *,
    position_ids: str,
    validate: bool | None = None,
  ) -> DoubleWinEstRedeemList:
    """Get the estimated early-redemption amount for one or more Double Win positions. Requires API key "Earn" permission.

    Args:
      category: Product category.
      position_ids: Comma-separated position identifiers.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/double-win/est-redeem)
    """
    return await self.request(
      Request(category=category, positionIds=position_ids),
      method='GET',
      path='/v5/earn/advance/get-redeem-est-amount-list',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=DoubleWinEstRedeemList,
    )
