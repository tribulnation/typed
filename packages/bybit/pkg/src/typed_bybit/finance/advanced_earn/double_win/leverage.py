# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class DoubleWinCustomQuote(TypedDict):
  """Leverage available for a custom price range, for an RFQ Double Win product."""

  productId: str
  """Product identifier, echoed back."""
  initialPrice: str
  """Index price submitted, echoed back."""
  lowerPrice: str
  """Lower price bound submitted, echoed back."""
  upperPrice: str
  """Upper price bound submitted, echoed back."""
  leverage: str
  """Leverage multiplier for the given price range, e.g. "3.2"."""
  expireTime: TimestampMillis
  """Quote expiry time, as a millisecond timestamp. The order must be placed before this time."""
  maxInvestmentAmount: str
  """Maximum single order amount for this quote."""


class Request(TypedDict):
  productId: str
  """Product identifier."""
  initialPrice: str
  """Index price."""
  lowerPrice: str
  """Lower price bound."""
  upperPrice: str
  """Upper price bound."""


class Leverage(RpcEndpoint):
  """Get the leverage available for a caller-chosen custom price range, for an RFQ Double Win product. Requires API key "Earn" permission."""

  async def leverage(
    self,
    *,
    product_id: str,
    initial_price: str,
    lower_price: str,
    upper_price: str,
    validate: bool | None = None,
  ) -> DoubleWinCustomQuote:
    """Get the leverage available for a caller-chosen custom price range, for an RFQ Double Win product. Requires API key "Earn" permission.

    Args:
      product_id: Product identifier.
      initial_price: Index price.
      lower_price: Lower price bound.
      upper_price: Upper price bound.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/double-win/leverage)
    """
    return await self.request(
      Request(
        productId=product_id,
        initialPrice=initial_price,
        lowerPrice=lower_price,
        upperPrice=upper_price,
      ),
      method='GET',
      path='/v5/earn/advance/double-win-leverage',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=DoubleWinCustomQuote,
    )
