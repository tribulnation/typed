# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class LiquidityMiningOrderResult(TypedDict):
  """Identifiers for a placed Liquidity Mining order."""

  orderId: str
  """System-generated order identifier."""
  orderLinkId: str
  """Caller-supplied order link identifier."""


class Request(TypedDict):
  """Liquidity Mining position margin top-up."""

  productId: str
  """Product identifier."""
  orderLinkId: str
  """Caller-supplied order ID, max 40 characters, used for idempotency."""
  positionId: str
  """Position to add margin to, from the position endpoint."""
  amount: str
  """Amount of quote coin to add as margin."""
  quoteAccountType: Literal['FUND', 'UNIFIED']
  """Source account for the margin."""


class AddMargin(RpcEndpoint):
  """Add margin to an open, leveraged Liquidity Mining position, to reduce liquidation risk. Requires API key "Earn" permission."""

  async def add_margin(
    self,
    *,
    product_id: str,
    order_link_id: str,
    position_id: str,
    amount: str,
    quote_account_type: Literal['FUND', 'UNIFIED'],
    validate: bool | None = None,
  ) -> LiquidityMiningOrderResult:
    """Add margin to an open, leveraged Liquidity Mining position, to reduce liquidation risk. Requires API key "Earn" permission.

    Args:
      product_id: Product identifier.
      order_link_id: Caller-supplied order ID, max 40 characters, used for idempotency.
      position_id: Position to add margin to, from the position endpoint.
      amount: Amount of quote coin to add as margin.
      quote_account_type: Source account for the margin.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/liquidity-mining/add-margin)
    """
    return await self.request(
      Request(
        productId=product_id,
        orderLinkId=order_link_id,
        positionId=position_id,
        amount=amount,
        quoteAccountType=quote_account_type,
      ),
      method='POST',
      path='/v5/earn/liquidity-mining/add-margin',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=LiquidityMiningOrderResult,
    )
