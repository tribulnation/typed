# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class LiquidityMiningOrderResult(TypedDict):
  """Identifiers for a placed Liquidity Mining order."""

  orderId: str
  """System-generated order identifier."""
  orderLinkId: str
  """Caller-supplied order link identifier."""


class Request(TypedDict):
  """Liquidity Mining position reinvestment."""

  productId: str
  """Product identifier."""
  orderLinkId: str
  """Caller-supplied order ID, max 40 characters, used for idempotency."""
  positionId: str
  """Position to reinvest into, from the position endpoint."""
  leverage: NotRequired[str]
  """Leverage multiplier for the reinvested amount, integer only. Defaults to "1"."""


class Reinvest(RpcEndpoint):
  """Reinvest accrued yield back into an open Liquidity Mining position. Requires API key "Earn" permission."""

  async def reinvest(
    self,
    *,
    product_id: str,
    order_link_id: str,
    position_id: str,
    leverage: str | None = None,
    validate: bool | None = None,
  ) -> LiquidityMiningOrderResult:
    """Reinvest accrued yield back into an open Liquidity Mining position. Requires API key "Earn" permission.

    Args:
      product_id: Product identifier.
      order_link_id: Caller-supplied order ID, max 40 characters, used for idempotency.
      position_id: Position to reinvest into, from the position endpoint.
      leverage: Leverage multiplier for the reinvested amount, integer only. Defaults to "1".
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/liquidity-mining/reinvest)
    """
    request: Request = Request(
      productId=product_id, orderLinkId=order_link_id, positionId=position_id
    )
    if leverage is not None:
      request['leverage'] = leverage
    return await self.request(
      request,
      method='POST',
      path='/v5/earn/liquidity-mining/reinvest',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=LiquidityMiningOrderResult,
    )
