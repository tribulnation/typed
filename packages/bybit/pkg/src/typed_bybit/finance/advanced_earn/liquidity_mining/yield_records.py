# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class LiquidityMiningYieldRecord(TypedDict):
  """One Liquidity Mining yield claim record."""

  coin: str
  """Yield coin received."""
  amount: str
  """Amount received."""
  baseCoin: str
  """Base coin of the pool this yield came from."""
  quoteCoin: str
  """Quote coin of the pool this yield came from."""
  type: Literal['Manual', 'RemoveLiquidity']
  """How the yield was claimed."""
  status: Literal['Complete', 'Processing']
  """Record status."""
  createdTime: TimestampMillis
  """Record creation time, as a millisecond timestamp."""


class Request(TypedDict):
  baseCoin: NotRequired[str]
  """Base coin."""
  quoteCoin: NotRequired[str]
  """Quote coin."""
  startTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  limit: NotRequired[int]
  """Page size."""
  cursor: NotRequired[str]
  """Pagination cursor."""


class LiquidityMiningYieldRecords(TypedDict):
  """One page of Liquidity Mining yield claim records."""

  records: list[LiquidityMiningYieldRecord]
  """Yield claim records."""
  nextPageCursor: str
  """Cursor for the next page. Empty when there is no further page."""


class YieldRecords(RpcEndpoint):
  """Get the caller's Liquidity Mining yield claim history. Requires API key "Earn" permission."""

  def yield_records_paged(
    self,
    *,
    base_coin: str | None = None,
    quote_coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[LiquidityMiningYieldRecord, str]:
    """Get the caller's Liquidity Mining yield claim history. Requires API key "Earn" permission.

    Paged variant of `yield_records`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      base_coin: Base coin.
      quote_coin: Quote coin.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/liquidity-mining/yield-records)
    """

    async def next(cursor: str) -> tuple[list[LiquidityMiningYieldRecord], str | None]:
      response = await self.yield_records(
        base_coin=base_coin,
        quote_coin=quote_coin,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('records')
      state = response.get('nextPageCursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def yield_records(
    self,
    *,
    base_coin: str | None = None,
    quote_coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> LiquidityMiningYieldRecords:
    """Get the caller's Liquidity Mining yield claim history. Requires API key "Earn" permission.

    Args:
      base_coin: Base coin.
      quote_coin: Quote coin.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      limit: Page size.
      cursor: Pagination cursor.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/liquidity-mining/yield-records)
    """
    request: Request = Request()
    if base_coin is not None:
      request['baseCoin'] = base_coin
    if quote_coin is not None:
      request['quoteCoin'] = quote_coin
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/earn/liquidity-mining/yield-records',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=LiquidityMiningYieldRecords,
    )
