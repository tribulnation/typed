# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, TypedDict
from typed_bybit.core.http import RpcEndpoint


LiquidityMiningClaimInterestResult = dict[str, Any]


class Request(TypedDict):
  """Liquidity Mining yield claim."""

  productId: str
  """Product identifier to claim yield for. `-1` claims across every open Liquidity Mining position at once."""


class ClaimInterest(RpcEndpoint):
  """Claim accrued yield from one Liquidity Mining position, or every position at once. Requires API key "Earn" permission."""

  async def claim_interest(
    self,
    product_id: str,
    *,
    validate: bool | None = None,
  ) -> LiquidityMiningClaimInterestResult:
    """Claim accrued yield from one Liquidity Mining position, or every position at once. Requires API key "Earn" permission.

    Args:
      product_id: Product identifier to claim yield for. `-1` claims across every open Liquidity Mining position at once.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/liquidity-mining/claim-interest)
    """
    return await self.request(
      Request(productId=product_id),
      method='POST',
      path='/v5/earn/liquidity-mining/claim-interest',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=LiquidityMiningClaimInterestResult,
    )
