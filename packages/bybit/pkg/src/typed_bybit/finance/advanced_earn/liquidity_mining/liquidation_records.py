# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class LiquidityMiningLiquidationRecord(TypedDict):
  """One Liquidity Mining position liquidation."""

  baseCoin: str
  """Base coin of the pool."""
  quoteCoin: str
  """Quote coin of the pool."""
  baseAmount: str
  """Base coin quantity returned after liquidation."""
  quoteAmount: str
  """Quote coin quantity returned after liquidation."""
  liquidationPrice: str
  """Liquidation price, base priced in quote."""
  liquidationTime: TimestampMillis
  """Liquidation time, as a millisecond timestamp."""


class Request(TypedDict):
  baseCoin: NotRequired[str]
  """Base coin."""
  quoteCoin: NotRequired[str]
  """Quote coin."""
  startTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  limit: NotRequired[int]
  """Page size."""
  cursor: NotRequired[str]
  """Pagination cursor."""


class LiquidityMiningLiquidationRecords(TypedDict):
  """One page of Liquidity Mining liquidation records."""

  records: list[LiquidityMiningLiquidationRecord]
  """Liquidation records."""
  nextPageCursor: str
  """Cursor for the next page. Empty when there is no further page."""


class LiquidationRecords(RpcEndpoint):
  """Get the caller's Liquidity Mining liquidation history. Requires API key "Earn" permission."""

  def liquidation_records_paged(
    self,
    *,
    base_coin: str | None = None,
    quote_coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[LiquidityMiningLiquidationRecord, str]:
    """Get the caller's Liquidity Mining liquidation history. Requires API key "Earn" permission.

    Paged variant of `liquidation_records`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      base_coin: Base coin.
      quote_coin: Quote coin.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/liquidity-mining/liquidation-records)
    """

    async def next(
      cursor: str,
    ) -> tuple[list[LiquidityMiningLiquidationRecord], str | None]:
      response = await self.liquidation_records(
        base_coin=base_coin,
        quote_coin=quote_coin,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('records')
      state = response.get('nextPageCursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def liquidation_records(
    self,
    *,
    base_coin: str | None = None,
    quote_coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> LiquidityMiningLiquidationRecords:
    """Get the caller's Liquidity Mining liquidation history. Requires API key "Earn" permission.

    Args:
      base_coin: Base coin.
      quote_coin: Quote coin.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      limit: Page size.
      cursor: Pagination cursor.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/liquidity-mining/liquidation-records)
    """
    request: Request = Request()
    if base_coin is not None:
      request['baseCoin'] = base_coin
    if quote_coin is not None:
      request['quoteCoin'] = quote_coin
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/earn/liquidity-mining/liquidation-records',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=LiquidityMiningLiquidationRecords,
    )
