# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class LiquidityMiningOrder(TypedDict):
  """One Liquidity Mining order."""

  orderId: str
  """Unique order identifier."""
  orderLinkId: str
  """Caller-supplied order link identifier."""
  productId: str
  """Product identifier."""
  orderType: Literal['AddLiquidity', 'RemoveLiquidity', 'Reinvest', 'AddMargin']
  """Operation type."""
  baseCoin: str
  """Base coin of the pool."""
  quoteCoin: str
  """Quote coin of the pool."""
  quoteAmount: NotRequired[str]
  """Quote coin amount involved in this order."""
  baseAmount: NotRequired[str]
  """Base coin amount involved in this order."""
  status: Literal['Success', 'Processing', 'Fail']
  """Order status."""
  removeType: NotRequired[Literal['Normal', 'SingleQuoteCoin', 'SingleBaseCoin']]
  """Withdrawal payout mode. `RemoveLiquidity` orders only."""
  removeRate: NotRequired[int]
  """Withdrawal percentage requested. `RemoveLiquidity` orders only."""
  leverage: NotRequired[str]
  """Leverage multiplier at order time. `AddLiquidity` orders only."""
  slippageValue: NotRequired[str]
  """Slippage deducted from the fill. `AddLiquidity` orders only, once `Success`."""
  createdTime: TimestampMillis
  """Order creation time, as a millisecond timestamp."""


class Request(TypedDict):
  orderId: NotRequired[str]
  """Order ID."""
  orderLinkId: NotRequired[str]
  """Order link ID."""
  productId: NotRequired[str]
  """Product identifier."""
  orderType: NotRequired[
    Literal['AddLiquidity', 'RemoveLiquidity', 'Reinvest', 'AddMargin']
  ]
  """Order type."""
  startTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  status: NotRequired[Literal['Success', 'Processing']]
  """Order status."""
  limit: NotRequired[int]
  """Page size."""
  cursor: NotRequired[str]
  """Pagination cursor."""


class LiquidityMiningOrderHistory(TypedDict):
  """One page of Liquidity Mining order history."""

  orders: list[LiquidityMiningOrder]
  """Order records."""
  nextPageCursor: str
  """Cursor for the next page. Empty when there is no further page."""


class Order(RpcEndpoint):
  """Get the caller's Liquidity Mining order history. Requires API key "Earn" permission."""

  def order_paged(
    self,
    *,
    order_id: str | None = None,
    order_link_id: str | None = None,
    product_id: str | None = None,
    order_type: Literal['AddLiquidity', 'RemoveLiquidity', 'Reinvest', 'AddMargin']
    | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    status: Literal['Success', 'Processing'] | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[LiquidityMiningOrder, str]:
    """Get the caller's Liquidity Mining order history. Requires API key "Earn" permission.

    Paged variant of `order`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      order_id: Order ID.
      order_link_id: Order link ID.
      product_id: Product identifier.
      order_type: Order type.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      status: Order status.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/liquidity-mining/order)
    """

    async def next(cursor: str) -> tuple[list[LiquidityMiningOrder], str | None]:
      response = await self.order(
        order_id=order_id,
        order_link_id=order_link_id,
        product_id=product_id,
        order_type=order_type,
        start_time=start_time,
        end_time=end_time,
        status=status,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('orders')
      state = response.get('nextPageCursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def order(
    self,
    *,
    order_id: str | None = None,
    order_link_id: str | None = None,
    product_id: str | None = None,
    order_type: Literal['AddLiquidity', 'RemoveLiquidity', 'Reinvest', 'AddMargin']
    | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    status: Literal['Success', 'Processing'] | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> LiquidityMiningOrderHistory:
    """Get the caller's Liquidity Mining order history. Requires API key "Earn" permission.

    Args:
      order_id: Order ID.
      order_link_id: Order link ID.
      product_id: Product identifier.
      order_type: Order type.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      status: Order status.
      limit: Page size.
      cursor: Pagination cursor.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/liquidity-mining/order)
    """
    request: Request = Request()
    if order_id is not None:
      request['orderId'] = order_id
    if order_link_id is not None:
      request['orderLinkId'] = order_link_id
    if product_id is not None:
      request['productId'] = product_id
    if order_type is not None:
      request['orderType'] = order_type
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if status is not None:
      request['status'] = status
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/earn/liquidity-mining/order',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=LiquidityMiningOrderHistory,
    )
