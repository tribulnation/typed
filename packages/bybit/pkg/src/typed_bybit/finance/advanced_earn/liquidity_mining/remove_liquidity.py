# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class LiquidityMiningOrderResult(TypedDict):
  """Identifiers for a placed Liquidity Mining order."""

  orderId: str
  """System-generated order identifier."""
  orderLinkId: str
  """Caller-supplied order link identifier."""


class Request(TypedDict):
  """Liquidity Mining position withdrawal."""

  productId: str
  """Product identifier."""
  orderLinkId: str
  """Caller-supplied order ID, max 40 characters, used for idempotency."""
  positionId: str
  """Position to remove liquidity from, from the position endpoint."""
  removeRate: NotRequired[int]
  """Withdrawal percentage. Defaults to 100."""
  removeType: NotRequired[Literal['Normal', 'SingleQuoteCoin', 'SingleBaseCoin']]
  """Withdrawal payout mode. Defaults to `Normal`."""


class RemoveLiquidity(RpcEndpoint):
  """Withdraw from an open Liquidity Mining position, in full or in part. Requires API key "Earn" permission."""

  async def remove_liquidity(
    self,
    *,
    product_id: str,
    order_link_id: str,
    position_id: str,
    remove_rate: int | None = None,
    remove_type: Literal['Normal', 'SingleQuoteCoin', 'SingleBaseCoin'] | None = None,
    validate: bool | None = None,
  ) -> LiquidityMiningOrderResult:
    """Withdraw from an open Liquidity Mining position, in full or in part. Requires API key "Earn" permission.

    Args:
      product_id: Product identifier.
      order_link_id: Caller-supplied order ID, max 40 characters, used for idempotency.
      position_id: Position to remove liquidity from, from the position endpoint.
      remove_rate: Withdrawal percentage. Defaults to 100.
      remove_type: Withdrawal payout mode. Defaults to `Normal`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/liquidity-mining/remove-liquidity)
    """
    request: Request = Request(
      productId=product_id, orderLinkId=order_link_id, positionId=position_id
    )
    if remove_rate is not None:
      request['removeRate'] = remove_rate
    if remove_type is not None:
      request['removeType'] = remove_type
    return await self.request(
      request,
      method='POST',
      path='/v5/earn/liquidity-mining/remove-liquidity',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=LiquidityMiningOrderResult,
    )
