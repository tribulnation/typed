# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class HoldToEarnYieldCoin(TypedDict):
  """One reward coin and its APR for this product."""

  coinName: str
  """Reward coin."""
  apy: str
  """Previous day's APR, e.g. "10%"."""
  personalApy: str
  """Caller's individual APR after the multiplier is applied, e.g. "10%"."""
  multiplier: str
  """Personal coefficient applied to the base APR. Defaults to "1"."""


class HoldToEarnProduct(TypedDict):
  """One Hold-to-Earn airdrop product."""

  coinName: str
  """Coin held to earn this airdrop."""
  yields: list[HoldToEarnYieldCoin]
  """Reward coins this product pays, which may differ from `coinName` in a cross-coin airdrop."""
  status: Literal['NotStarted', 'Online', 'Ended']
  """Product phase."""
  apy: str
  """Previous day's average APR across every yield coin, e.g. "10%"."""
  personalApy: str
  """Sum of the caller's personal APR across every yield coin, e.g. "10%"."""
  announcementUrl: NotRequired[str]
  """URL of the activity rules announcement."""


class HoldToEarnProductList(TypedDict):
  """Hold-to-Earn airdrop products."""

  products: list[HoldToEarnProduct]
  """Airdrop products, newest first."""


class Product(RpcEndpoint):
  """Get Hold-to-Earn airdrop products: holding a coin earns a yield in that coin or a related one. No parameters. Authentication is optional; an authenticated caller sees only products it's eligible for."""

  async def product(self, *, validate: bool | None = None) -> HoldToEarnProductList:
    """Get Hold-to-Earn airdrop products: holding a coin earns a yield in that coin or a related one. No parameters. Authentication is optional; an authenticated caller sees only products it's eligible for.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/earn/hold-to-earn/product)
    """
    return await self.request(
      None,
      method='GET',
      path='/v5/earn/hold-to-earn/product',
      meta={},
      validate=validate,
      response_type=HoldToEarnProductList,
    )
