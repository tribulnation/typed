# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class ByusdtOrderResult(TypedDict):
  """Identifiers for a placed byUSDT order."""

  orderId: str
  """System-generated order identifier."""
  orderLinkId: str
  """Caller-supplied order link identifier."""


class Request(TypedDict):
  """byUSDT mint or redeem order."""

  coin: Literal['BYUSDT']
  """Token identifier. Currently only `BYUSDT` is supported."""
  orderLinkId: str
  """Caller-supplied order ID, max 36 characters, used for idempotency."""
  orderType: Literal['Mint', 'Redeem']
  """Order direction."""
  amount: str
  """Order amount as a decimal string: USDT quantity for `Mint`, byUSDT quantity for `Redeem`."""
  accountType: Literal['FlexibleSaving', 'UNIFIED']
  """Source account for `Mint`, destination account for `Redeem`."""


class PlaceOrder(RpcEndpoint):
  """Mint byUSDT from USDT held in Flexible Saving, or redeem byUSDT back to USDT."""

  async def place_order(
    self,
    coin: Literal['BYUSDT'] = 'BYUSDT',
    *,
    order_link_id: str,
    order_type: Literal['Mint', 'Redeem'],
    amount: str,
    account_type: Literal['FlexibleSaving', 'UNIFIED'],
    validate: bool | None = None,
  ) -> ByusdtOrderResult:
    """Mint byUSDT from USDT held in Flexible Saving, or redeem byUSDT back to USDT.

    Args:
      coin: Token identifier. Currently only `BYUSDT` is supported.
      order_link_id: Caller-supplied order ID, max 36 characters, used for idempotency.
      order_type: Order direction.
      amount: Order amount as a decimal string: USDT quantity for `Mint`, byUSDT quantity for `Redeem`.
      account_type: Source account for `Mint`, destination account for `Redeem`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/earn/byusdt/place-order)
    """
    return await self.request(
      Request(
        coin=coin,
        orderLinkId=order_link_id,
        orderType=order_type,
        amount=amount,
        accountType=account_type,
      ),
      method='POST',
      path='/v5/earn/token/place-order',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ByusdtOrderResult,
    )
