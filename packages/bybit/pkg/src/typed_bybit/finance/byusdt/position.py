# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class ByusdtPosition(TypedDict):
  """The caller's byUSDT holdings and yield summary."""

  totalAmount: str
  """Total byUSDT holdings."""
  totalYield: str
  """Cumulative yield earned."""
  yesterdayYield: str
  """Yield earned the previous day."""
  aprE8: int
  """Base APR, e8 precision. Divide by 10^8 for the actual rate."""
  bonusAprE8: NotRequired[int]
  """Bonus APR, e8 precision, when the caller qualifies. Divide by 10^8 for the actual rate."""
  bonusMaxAmount: NotRequired[str]
  """Principal ceiling eligible for the bonus APR."""
  hasQuota: bool
  """Whether mintable quota remains available."""


class Request(TypedDict):
  coin: Literal['BYUSDT']
  """Token identifier."""


class Position(RpcEndpoint):
  """Get the caller's current byUSDT holdings, accrued yield and APR."""

  async def position(
    self,
    coin: Literal['BYUSDT'] = 'BYUSDT',
    *,
    validate: bool | None = None,
  ) -> ByusdtPosition:
    """Get the caller's current byUSDT holdings, accrued yield and APR.

    Args:
      coin: Token identifier.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/earn/byusdt/position)
    """
    return await self.request(
      Request(coin=coin),
      method='GET',
      path='/v5/earn/token/position',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ByusdtPosition,
    )
