# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core import TimestampSeconds
from typed_bybit.core.http import RpcEndpoint


class ByusdtAprRecord(TypedDict):
  """One day's APR for byUSDT."""

  timestamp: TimestampSeconds
  """Date, as a second timestamp."""
  aprE8: str
  """Yield rate, e8 precision. Divide by 10^8 for the actual rate."""


class Request(TypedDict):
  coin: Literal['BYUSDT']
  """Token identifier."""
  range: Literal[1, 2, 3]
  """Lookback window: 1 (7 days), 2 (30 days), 3 (180 days)."""


class ByusdtHistoryApr(TypedDict):
  """Daily APR history for byUSDT over the requested window."""

  list: list[ByusdtAprRecord]
  """One entry per day."""


class HistoryApr(RpcEndpoint):
  """Get byUSDT's historical daily APR over a fixed lookback window. No authentication required."""

  async def history_apr(
    self,
    coin: Literal['BYUSDT'] = 'BYUSDT',
    *,
    range: Literal[1, 2, 3],
    validate: bool | None = None,
  ) -> ByusdtHistoryApr:
    """Get byUSDT's historical daily APR over a fixed lookback window. No authentication required.

    Args:
      coin: Token identifier.
      range: Lookback window: 1 (7 days), 2 (30 days), 3 (180 days).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/earn/byusdt/history-apr)
    """
    return await self.request(
      Request(coin=coin, range=range),
      method='GET',
      path='/v5/earn/token/history-apr',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=ByusdtHistoryApr,
    )
