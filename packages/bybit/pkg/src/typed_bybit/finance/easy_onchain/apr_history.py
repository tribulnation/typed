# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class EasyOnchainAprRecord(TypedDict):
  """One day's APR for a product."""

  timestamp: TimestampMillis
  """Midnight of the recorded day, as a millisecond timestamp."""
  apr: str
  """Annual percentage rate, percent-suffixed, e.g. "1%"."""


class Request(TypedDict):
  category: Literal['FlexibleSaving', 'OnChain']
  """Product category."""
  productId: str
  """Product identifier."""
  startTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""


class EasyOnchainAprHistory(TypedDict):
  """Daily APR history for one product."""

  list: list[EasyOnchainAprRecord]
  """One entry per day."""


class AprHistory(RpcEndpoint):
  """Get historical daily APR for an Easy/On-Chain Earn product. No authentication required."""

  async def apr_history(
    self,
    category: Literal['FlexibleSaving', 'OnChain'],
    *,
    product_id: str,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    validate: bool | None = None,
  ) -> EasyOnchainAprHistory:
    """Get historical daily APR for an Easy/On-Chain Earn product. No authentication required.

    Args:
      category: Product category.
      product_id: Product identifier.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/earn/easy-onchain/apr-history)
    """
    request: Request = Request(category=category, productId=product_id)
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    return await self.request(
      request,
      method='GET',
      path='/v5/earn/apr-history',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=EasyOnchainAprHistory,
    )
