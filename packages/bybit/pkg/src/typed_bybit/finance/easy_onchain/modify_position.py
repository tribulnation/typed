# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


EasyOnchainModifyPositionResult = dict[str, Any]


class Request(TypedDict):
  """On-Chain Earn position auto-reinvest change."""

  category: Literal['OnChain']
  """Product category."""
  productId: int
  """Product identifier, from the product-info endpoint."""
  positionId: int
  """Position identifier, from the position endpoint."""
  autoReinvest: Literal[0, 1]
  """Auto-reinvestment toggle."""


class ModifyPosition(RpcEndpoint):
  """Toggle auto-reinvestment on an On-Chain Earn staked position. Requires API key "Earn" permission."""

  async def modify_position(
    self,
    category: Literal['OnChain'] = 'OnChain',
    *,
    product_id: int,
    position_id: int,
    auto_reinvest: Literal[0, 1],
    validate: bool | None = None,
  ) -> EasyOnchainModifyPositionResult:
    """Toggle auto-reinvestment on an On-Chain Earn staked position. Requires API key "Earn" permission.

    Args:
      category: Product category.
      product_id: Product identifier, from the product-info endpoint.
      position_id: Position identifier, from the position endpoint.
      auto_reinvest: Auto-reinvestment toggle.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/earn/easy-onchain/modify-position)
    """
    return await self.request(
      Request(
        category=category,
        productId=product_id,
        positionId=position_id,
        autoReinvest=auto_reinvest,
      ),
      method='POST',
      path='/v5/earn/position/modify',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=EasyOnchainModifyPositionResult,
    )
