# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class EasyOnchainHourlyYield(TypedDict):
  """One hourly yield distribution record."""

  productId: str
  """Product identifier."""
  coin: str
  """Coin name."""
  id: str
  """Unique record key, per user."""
  amount: str
  """Yield amount for this hour."""
  effectiveStakingAmount: str
  """Effective staked balance this yield was computed on."""
  status: Literal['Pending', 'Success', 'Fail']
  """Record status."""
  hourlyDate: TimestampMillis
  """Yield distribution hour, as a millisecond timestamp."""
  createdAt: TimestampMillis
  """Record creation time, as a millisecond timestamp."""


class Request(TypedDict):
  category: Literal['FlexibleSaving']
  """Product category."""
  productId: NotRequired[str]
  """Product identifier."""
  startTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  limit: NotRequired[int]
  """Page size."""
  cursor: NotRequired[str]
  """Pagination cursor."""


class EasyOnchainHourlyYieldHistory(TypedDict):
  """One page of hourly yield records."""

  nextPageCursor: str
  """Cursor for the next page. Empty when there is no further page."""
  list: list[EasyOnchainHourlyYield]
  """Hourly yield records."""


class HourlyYield(RpcEndpoint):
  """Get hourly yield distribution records for FlexibleSaving Earn products. Requires API key "Earn" permission."""

  def hourly_yield_paged(
    self,
    category: Literal['FlexibleSaving'] = 'FlexibleSaving',
    *,
    product_id: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[EasyOnchainHourlyYield, str]:
    """Get hourly yield distribution records for FlexibleSaving Earn products. Requires API key "Earn" permission.

    Paged variant of `hourly_yield`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      category: Product category.
      product_id: Product identifier.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/earn/easy-onchain/hourly-yield)
    """

    async def next(cursor: str) -> tuple[list[EasyOnchainHourlyYield], str | None]:
      response = await self.hourly_yield(
        category,
        product_id=product_id,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('list')
      state = response.get('nextPageCursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def hourly_yield(
    self,
    category: Literal['FlexibleSaving'] = 'FlexibleSaving',
    *,
    product_id: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> EasyOnchainHourlyYieldHistory:
    """Get hourly yield distribution records for FlexibleSaving Earn products. Requires API key "Earn" permission.

    Args:
      category: Product category.
      product_id: Product identifier.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      limit: Page size.
      cursor: Pagination cursor.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/earn/easy-onchain/hourly-yield)
    """
    request: Request = Request(category=category)
    if product_id is not None:
      request['productId'] = product_id
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/earn/hourly-yield',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=EasyOnchainHourlyYieldHistory,
    )
