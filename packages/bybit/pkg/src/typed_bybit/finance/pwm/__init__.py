# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .asset_manager import AssetManager
from .customize_plan import CustomizePlan
from .fund_transfer import FundTransfer
from .investment_plan import InvestmentPlan
from .query_fund_transfer_result import QueryFundTransferResult


class Pwm(FundTransfer, QueryFundTransferResult):
  """PWM (Private Wealth Management) — Bybit's custodian-account investment platform for wealth managers: move funds between a custodian's own sub-accounts and check transfer status at the top level, with sub-groupings for creating/managing funds and their investors (Asset Manager), one-off customized plans (Customize Plan), and standard investment plan subscriptions (Investment Plan).

  References:
    - [Upstream docs](https://bybit-exchange.github.io/docs/v5/finance/pwm/fund-transfer)
  """

  @cached_property
  def asset_manager(self) -> AssetManager:
    """PWM Asset Manager — the fund-manager side of Private Wealth Management: create a fund and its custodian sub-account, create and manage the fund's own investment plans, place and manage orders on the fund's behalf, settle fund profit, and list all funds and their orders.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/finance/pwm/asset-manager/all-funds)
    """
    return AssetManager(client=self.client)

  @cached_property
  def customize_plan(self) -> CustomizePlan:
    """PWM Customize Plan — a one-off, individually tailored investment plan built from Bybit's subscribable product catalog (equity funds, multi-coin earn, on-chain earn, fixed yield): list subscribable products and create a customized plan from them.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/finance/pwm/customize-plan/product)
    """
    return CustomizePlan(client=self.client)

  @cached_property
  def investment_plan(self) -> InvestmentPlan:
    """PWM Investment Plan — standard, fund-manager-published investment plans a PWM client subscribes into: browse and inspect plans and their historical NAV/asset trend, subscribe, invest more, redeem a holding, claim withdrawable funds, and list plan orders.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/finance/pwm/investment-plan/all)
    """
    return InvestmentPlan(client=self.client)
