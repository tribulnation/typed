# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class PwmFundTransferRecord(TypedDict):
  """One PWM custodian sub-account fund transfer record."""

  transferId: str
  """Unique transfer identifier."""
  status: Literal['SUCCESS', 'FAILED', 'PROCESSING']
  """Transfer state."""
  fromUserId: int
  """Originating user ID."""
  toUserId: int
  """Receiving user ID."""
  amount: str
  """Transfer quantity."""
  coin: str
  """Transferred coin."""


class Request(TypedDict):
  transferId: NotRequired[str]
  """Transfer request ID."""
  fromUserId: NotRequired[int]
  """Source user ID."""


class PwmFundTransferRecords(TypedDict):
  """PWM custodian sub-account fund transfer records."""

  list: list[PwmFundTransferRecord]
  """Transfer records."""


class QueryFundTransferResult(RpcEndpoint):
  """Get PWM custodian sub-account fund transfer records. Requires the fund custodian sub-account's own API key."""

  async def query_fund_transfer_result(
    self,
    transfer_id: str | None = None,
    *,
    from_user_id: int | None = None,
    validate: bool | None = None,
  ) -> PwmFundTransferRecords:
    """Get PWM custodian sub-account fund transfer records. Requires the fund custodian sub-account's own API key.

    Args:
      transfer_id: Transfer request ID.
      from_user_id: Source user ID.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/pwm/query-fund-transfer-result)
    """
    request: Request = Request()
    if transfer_id is not None:
      request['transferId'] = transfer_id
    if from_user_id is not None:
      request['fromUserId'] = from_user_id
    return await self.request(
      request,
      method='GET',
      path='/v5/earn/pwm/query-fund-transfer-result',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PwmFundTransferRecords,
    )
