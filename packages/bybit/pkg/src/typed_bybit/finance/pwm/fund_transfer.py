# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class PwmFundTransferResult(TypedDict):
  """Result of a PWM custodian sub-account fund transfer."""

  transferId: str
  """Transfer identifier."""
  status: Literal['SUCCESS', 'FAILED', 'PROCESSING']
  """Transfer outcome."""


class Request(TypedDict):
  """Fund transfer between two PWM custodian sub-accounts."""

  transferId: str
  """Caller-supplied transfer request identifier."""
  fromUserId: int
  """Source user ID. Must be a custodian sub-account."""
  toUserId: int
  """Destination user ID. Must be a custodian sub-account."""
  amount: str
  """Transfer quantity."""
  coin: str
  """Coin to transfer."""


class FundTransfer(RpcEndpoint):
  """Transfer funds between PWM fund custodian sub-accounts belonging to the same fund manager. Requires the fund custodian sub-account's own API key."""

  async def fund_transfer(
    self,
    *,
    transfer_id: str,
    from_user_id: int,
    to_user_id: int,
    amount: str,
    coin: str,
    validate: bool | None = None,
  ) -> PwmFundTransferResult:
    """Transfer funds between PWM fund custodian sub-accounts belonging to the same fund manager. Requires the fund custodian sub-account's own API key.

    Args:
      transfer_id: Caller-supplied transfer request identifier.
      from_user_id: Source user ID. Must be a custodian sub-account.
      to_user_id: Destination user ID. Must be a custodian sub-account.
      amount: Transfer quantity.
      coin: Coin to transfer.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/pwm/fund-transfer)
    """
    return await self.request(
      Request(
        transferId=transfer_id,
        fromUserId=from_user_id,
        toUserId=to_user_id,
        amount=amount,
        coin=coin,
      ),
      method='POST',
      path='/v5/earn/pwm/fund-transfer',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PwmFundTransferResult,
    )
