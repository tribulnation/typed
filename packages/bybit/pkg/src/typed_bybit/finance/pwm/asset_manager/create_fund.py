# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class PwmFund(TypedDict):
  """A created asset-manager fund."""

  fundId: str
  """Unique fund identifier."""
  fundName: str
  """Fund name."""
  coin: str
  """Fund base currency."""
  status: Literal['pending_subscribe']
  """Fund status. A newly created fund is `pending_subscribe`."""
  profitShareRate: str
  """Profit sharing rate, as a percentage."""
  managementFeeRate: str
  """Annualized management fee rate, as a percentage."""
  accountUid: str
  """UID of the fund's main sub-account, created automatically by the system."""
  createdTime: TimestampMillis
  """Creation time, as a millisecond timestamp."""


class Request(TypedDict):
  """New asset-manager fund."""

  fundName: str
  """Fund name. 1-50 characters."""
  coin: Literal['BTC', 'ETH', 'USDT', 'USDC', 'SOL', 'MNT', 'XRP']
  """Fund base currency."""
  profitShareRate: str
  """High-watermark profit sharing rate, as a percentage. Range [0, 100]."""
  managementFeeRate: str
  """Annualized management fee rate, as a percentage. Range [0, 100]."""
  fundIntroduction: NotRequired[str]
  """Fund introduction ID. Must be in the institution's allowed list."""
  reqLinkId: str
  """Caller-supplied request ID, max 36 characters, used for idempotency."""


class CreateFund(RpcEndpoint):
  """Create a new asset-manager fund. Institution-only."""

  async def create_fund(
    self,
    *,
    fund_name: str,
    coin: Literal['BTC', 'ETH', 'USDT', 'USDC', 'SOL', 'MNT', 'XRP'],
    profit_share_rate: str,
    management_fee_rate: str,
    fund_introduction: str | None = None,
    req_link_id: str,
    validate: bool | None = None,
  ) -> PwmFund:
    """Create a new asset-manager fund. Institution-only.

    Args:
      fund_name: Fund name. 1-50 characters.
      coin: Fund base currency.
      profit_share_rate: High-watermark profit sharing rate, as a percentage. Range [0, 100].
      management_fee_rate: Annualized management fee rate, as a percentage. Range [0, 100].
      fund_introduction: Fund introduction ID. Must be in the institution's allowed list.
      req_link_id: Caller-supplied request ID, max 36 characters, used for idempotency.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/pwm/asset-manager/create-fund)
    """
    request: Request = Request(
      fundName=fund_name,
      coin=coin,
      profitShareRate=profit_share_rate,
      managementFeeRate=management_fee_rate,
      reqLinkId=req_link_id,
    )
    if fund_introduction is not None:
      request['fundIntroduction'] = fund_introduction
    return await self.request(
      request,
      method='POST',
      path='/v5/earn/pwm/asset-manager/create-fund',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PwmFund,
    )
