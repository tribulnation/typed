# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class PwmFundSubAccount(TypedDict):
  """A created (or pending) fund sub-account."""

  fundId: str
  """Fund identifier."""
  subAccountUid: str
  """UID of the new sub-account. `"0"` while creation is still in progress."""
  createdTime: TimestampMillis
  """Creation time, as a millisecond timestamp."""
  status: Literal['pending', 'success', 'destroyed']
  """Creation status."""


class Request(TypedDict):
  """New fund sub-account."""

  fundId: str
  """Fund identifier to create a sub-account under."""
  reqLinkId: str
  """Caller-supplied request ID, used to prevent duplicate creation."""


class CreateSubAccount(RpcEndpoint):
  """Create a new sub-account under an active asset-manager fund. Institution-only."""

  async def create_sub_account(
    self,
    *,
    fund_id: str,
    req_link_id: str,
    validate: bool | None = None,
  ) -> PwmFundSubAccount:
    """Create a new sub-account under an active asset-manager fund. Institution-only.

    Args:
      fund_id: Fund identifier to create a sub-account under.
      req_link_id: Caller-supplied request ID, used to prevent duplicate creation.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/pwm/asset-manager/create-sub-account)
    """
    return await self.request(
      Request(fundId=fund_id, reqLinkId=req_link_id),
      method='POST',
      path='/v5/earn/pwm/asset-manager/create-sub-account',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PwmFundSubAccount,
    )
