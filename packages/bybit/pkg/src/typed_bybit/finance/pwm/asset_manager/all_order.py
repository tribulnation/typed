# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class PwmFundOrder(TypedDict):
  """One fund subscribe or redeem order."""

  orderId: str
  """Order identifier."""
  fundId: str
  """Fund identifier."""
  fundName: str
  """Fund name."""
  accountUid: str
  """Sub-account UID this order applies to."""
  orderType: Literal['Subscribe', 'Redeem']
  """Order direction."""
  coin: str
  """Fund base currency."""
  amount: NotRequired[str]
  """Order amount, for a `Subscribe` order."""
  shares: NotRequired[str]
  """Order share count, for a `Redeem` order."""
  status: Literal['Pending Review', 'Processing', 'Completed', 'Rejected', 'Failed']
  """Order status."""
  createdTime: TimestampMillis
  """Creation time, as a millisecond timestamp."""


class Request(TypedDict):
  fundId: NotRequired[str]
  """Fund identifier."""
  orderType: NotRequired[Literal['Subscribe', 'Redeem']]
  """Order direction."""
  status: NotRequired[
    Literal['Pending Review', 'Processing', 'Completed', 'Rejected', 'Failed']
  ]
  """Order status."""
  startTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  limit: NotRequired[int]
  """Page size."""
  cursor: NotRequired[str]
  """Pagination cursor."""


class PwmFundOrderList(TypedDict):
  """One page of fund order history."""

  list: list[PwmFundOrder]
  """Order records."""
  nextPageCursor: str
  """Cursor for the next page. Empty when there is no further page."""


class AllOrder(RpcEndpoint):
  """Get the caller institution's fund subscribe/redeem orders."""

  def all_order_paged(
    self,
    *,
    fund_id: str | None = None,
    order_type: Literal['Subscribe', 'Redeem'] | None = None,
    status: Literal['Pending Review', 'Processing', 'Completed', 'Rejected', 'Failed']
    | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[PwmFundOrder, str]:
    """Get the caller institution's fund subscribe/redeem orders.

    Paged variant of `all_order`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      fund_id: Fund identifier.
      order_type: Order direction.
      status: Order status.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/pwm/asset-manager/all-order)
    """

    async def next(cursor: str) -> tuple[list[PwmFundOrder], str | None]:
      response = await self.all_order(
        fund_id=fund_id,
        order_type=order_type,
        status=status,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('list')
      state = response.get('nextPageCursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def all_order(
    self,
    *,
    fund_id: str | None = None,
    order_type: Literal['Subscribe', 'Redeem'] | None = None,
    status: Literal['Pending Review', 'Processing', 'Completed', 'Rejected', 'Failed']
    | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> PwmFundOrderList:
    """Get the caller institution's fund subscribe/redeem orders.

    Args:
      fund_id: Fund identifier.
      order_type: Order direction.
      status: Order status.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      limit: Page size.
      cursor: Pagination cursor.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/pwm/asset-manager/all-order)
    """
    request: Request = Request()
    if fund_id is not None:
      request['fundId'] = fund_id
    if order_type is not None:
      request['orderType'] = order_type
    if status is not None:
      request['status'] = status
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/earn/pwm/asset-manager/all-order',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PwmFundOrderList,
    )
