# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class PwmInvestmentDistributionEntry(TypedDict):
  """One fund allocation within the plan."""

  productId: str
  """Fund identifier. Must belong to the managing institution."""
  amount: str
  """Allocated amount, in the base currency."""


class PwmInvestmentPlanCreateResult(TypedDict):
  """A created investment plan."""

  planId: str
  """New investment plan identifier."""
  planType: Literal['stable', 'advanced']
  """Plan risk classification."""
  accountUid: str
  """Associated user account UID."""
  status: Literal['pending_subscribe']
  """Plan status on creation."""
  createdTime: TimestampMillis
  """Creation time, as a millisecond timestamp."""


class Request(TypedDict):
  """New investment plan."""

  accountUid: str
  """Target user's account UID."""
  planName: str
  """Plan name. Max 50 characters."""
  planType: Literal['stable', 'advanced']
  """Plan risk classification."""
  investmentDistribution: list[PwmInvestmentDistributionEntry]
  """Fund allocation. At least one entry."""
  reqLinkId: str
  """Caller-supplied request ID, max 36 characters, used for idempotency."""


class CreateInvestmentPlan(RpcEndpoint):
  """Create an investment plan for a user, allocating funds across one or more asset-manager funds. Institution-only."""

  async def create_investment_plan(
    self,
    *,
    account_uid: str,
    plan_name: str,
    plan_type: Literal['stable', 'advanced'],
    investment_distribution: list[PwmInvestmentDistributionEntry],
    req_link_id: str,
    validate: bool | None = None,
  ) -> PwmInvestmentPlanCreateResult:
    """Create an investment plan for a user, allocating funds across one or more asset-manager funds. Institution-only.

    Args:
      account_uid: Target user's account UID.
      plan_name: Plan name. Max 50 characters.
      plan_type: Plan risk classification.
      investment_distribution: Fund allocation. At least one entry.
      req_link_id: Caller-supplied request ID, max 36 characters, used for idempotency.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/pwm/asset-manager/create-investment-plan)
    """
    return await self.request(
      Request(
        accountUid=account_uid,
        planName=plan_name,
        planType=plan_type,
        investmentDistribution=investment_distribution,
        reqLinkId=req_link_id,
      ),
      method='POST',
      path='/v5/earn/pwm/asset-manager/create-investment-plan',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PwmInvestmentPlanCreateResult,
    )
