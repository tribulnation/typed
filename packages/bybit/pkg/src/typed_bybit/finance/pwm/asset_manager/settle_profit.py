# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class PwmSettleProfitResult(TypedDict):
  """A fund profit-share settlement round."""

  fundId: str
  """Fund identifier."""
  status: Literal['Processing', 'Completed', 'Failed']
  """Settlement outcome."""
  totalProfitShared: str
  """Total profit-share amount settled this round, in the base coin."""
  instIncome: str
  """Institution income this round, in the base coin."""
  coin: str
  """Fund base currency."""
  createdTime: TimestampMillis
  """Settlement time, as a millisecond timestamp."""


class Request(TypedDict):
  """Fund profit-share settlement request."""

  fundId: str
  """Fund identifier. Must be in `Active` status."""
  reqLinkId: str
  """Caller-supplied request ID, max 36 characters, used for idempotency."""


class SettleProfit(RpcEndpoint):
  """Settle a round of profit-share and institution income for an active fund. Institution-only."""

  async def settle_profit(
    self,
    *,
    fund_id: str,
    req_link_id: str,
    validate: bool | None = None,
  ) -> PwmSettleProfitResult:
    """Settle a round of profit-share and institution income for an active fund. Institution-only.

    Args:
      fund_id: Fund identifier. Must be in `Active` status.
      req_link_id: Caller-supplied request ID, max 36 characters, used for idempotency.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/pwm/asset-manager/settle-profit)
    """
    return await self.request(
      Request(fundId=fund_id, reqLinkId=req_link_id),
      method='POST',
      path='/v5/earn/pwm/asset-manager/settle-profit',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PwmSettleProfitResult,
    )
