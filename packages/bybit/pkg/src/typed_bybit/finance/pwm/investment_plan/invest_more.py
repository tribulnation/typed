# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class PwmInvestMoreResult(TypedDict):
  """Result of an additional PWM plan investment."""

  planId: str
  """Investment plan identifier."""
  category: str
  """Product type."""
  productId: str
  """Product identifier."""
  coin: str
  """Subscription coin."""
  amount: str
  """Additional investment amount, in the product's base coin."""
  status: Literal['Success', 'Pending', 'failed']
  """Subscription status."""
  orderLinkId: str
  """Caller-supplied order link identifier."""
  orderId: str
  """System-generated order identifier."""


class Request(TypedDict):
  """Additional investment within an active PWM plan."""

  planId: str
  """Investment plan ID. Must be in `Active` status."""
  accountType: NotRequired[str]
  """Source account type. Defaults to `FUND`."""
  category: str
  """Product type."""
  productId: str
  """Product identifier."""
  amount: str
  """Additional investment amount, in the product's base coin."""
  orderLinkId: str
  """Caller-supplied order ID, max 36 characters, used for idempotency."""


class InvestMore(RpcEndpoint):
  """Add additional investment to a product within an active PWM plan. Requires authentication."""

  async def invest_more(
    self,
    *,
    plan_id: str,
    account_type: str | None = None,
    category: str,
    product_id: str,
    amount: str,
    order_link_id: str,
    validate: bool | None = None,
  ) -> PwmInvestMoreResult:
    """Add additional investment to a product within an active PWM plan. Requires authentication.

    Args:
      plan_id: Investment plan ID. Must be in `Active` status.
      account_type: Source account type. Defaults to `FUND`.
      category: Product type.
      product_id: Product identifier.
      amount: Additional investment amount, in the product's base coin.
      order_link_id: Caller-supplied order ID, max 36 characters, used for idempotency.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/pwm/investment-plan/invest-more)
    """
    request: Request = Request(
      planId=plan_id,
      category=category,
      productId=product_id,
      amount=amount,
      orderLinkId=order_link_id,
    )
    if account_type is not None:
      request['accountType'] = account_type
    return await self.request(
      request,
      method='POST',
      path='/v5/earn/pwm/investment-plan/invest-more',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PwmInvestMoreResult,
    )
