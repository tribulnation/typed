# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class PwmInvestmentPlanSubscribeResult(TypedDict):
  """Result of a PWM investment plan subscription."""

  planId: str
  """Investment plan identifier."""
  status: Literal['Active']
  """Plan status after subscription. Becomes `Active` on the first successful subscription."""
  orderLinkId: str
  """Caller-supplied order link identifier."""


class Request(TypedDict):
  """Subscription to a pending PWM investment plan."""

  planId: str
  """Investment plan ID. Must be in `PendingSubscription` status."""
  accountType: NotRequired[str]
  """Source account type. Defaults to `FUND`."""
  orderLinkId: str
  """Caller-supplied order ID, max 36 characters, used for idempotency."""


class Subscribe(RpcEndpoint):
  """Subscribe to a pending PWM investment plan, activating it. Requires authentication."""

  async def subscribe(
    self,
    *,
    plan_id: str,
    account_type: str | None = None,
    order_link_id: str,
    validate: bool | None = None,
  ) -> PwmInvestmentPlanSubscribeResult:
    """Subscribe to a pending PWM investment plan, activating it. Requires authentication.

    Args:
      plan_id: Investment plan ID. Must be in `PendingSubscription` status.
      account_type: Source account type. Defaults to `FUND`.
      order_link_id: Caller-supplied order ID, max 36 characters, used for idempotency.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/pwm/investment-plan/subscribe)
    """
    request: Request = Request(planId=plan_id, orderLinkId=order_link_id)
    if account_type is not None:
      request['accountType'] = account_type
    return await self.request(
      request,
      method='POST',
      path='/v5/earn/pwm/investment-plan/subscribe',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PwmInvestmentPlanSubscribeResult,
    )
