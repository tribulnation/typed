# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class PwmInvestmentPlanOrder(TypedDict):
  """One PWM investment plan order."""

  orderId: str
  """Unique order identifier."""
  planId: str
  """Associated investment plan identifier."""
  type: Literal['Subscribe', 'Redeem', 'AutoReinvest']
  """Order type."""
  accountType: Literal['Funding', 'Unified']
  """Source account type."""
  coin: str
  """Asset symbol."""
  amount: str
  """Order quantity, in the base coin."""
  category: Literal[
    'flexibleSavings', 'fundPool', 'fundPoolPremium', 'equityFund', 'onchainEarn'
  ]
  """Product classification."""
  productId: str
  """Product identifier."""
  status: Literal['Completed', 'Pending', 'Failed']
  """Order status."""
  orderTime: TimestampMillis
  """Creation time (UTC), as a millisecond timestamp."""


class Request(TypedDict):
  planId: NotRequired[str]
  """Investment plan identifier."""
  category: NotRequired[
    Literal[
      'flexibleSavings', 'fundPool', 'fundPoolPremium', 'equityFund', 'onchainEarn'
    ]
  ]
  """Product type."""
  type: NotRequired[Literal['Subscribe', 'Redeem', 'AutoReinvest']]
  """Order type."""
  status: NotRequired[Literal['Completed', 'Pending', 'Failed']]
  """Order status."""
  startTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  limit: NotRequired[int]
  """Page size."""
  cursor: NotRequired[str]
  """Pagination cursor."""
  orderLinkId: NotRequired[str]
  """Order link ID."""


class PwmInvestmentPlanOrderHistory(TypedDict):
  """One page of PWM investment plan order history."""

  list: list[PwmInvestmentPlanOrder]
  """Order records, newest first."""
  nextPageCursor: str
  """Cursor for the next page. Empty when there is no further page."""


class Order(RpcEndpoint):
  """Get the caller's PWM investment plan order history, across plans by default. Requires authentication."""

  def order_paged(
    self,
    *,
    plan_id: str | None = None,
    category: Literal[
      'flexibleSavings', 'fundPool', 'fundPoolPremium', 'equityFund', 'onchainEarn'
    ]
    | None = None,
    type: Literal['Subscribe', 'Redeem', 'AutoReinvest'] | None = None,
    status: Literal['Completed', 'Pending', 'Failed'] | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    order_link_id: str | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[PwmInvestmentPlanOrder, str]:
    """Get the caller's PWM investment plan order history, across plans by default. Requires authentication.

    Paged variant of `order`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      plan_id: Investment plan identifier.
      category: Product type.
      type: Order type.
      status: Order status.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      limit: Page size.
      order_link_id: Order link ID.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/pwm/investment-plan/order)
    """

    async def next(cursor: str) -> tuple[list[PwmInvestmentPlanOrder], str | None]:
      response = await self.order(
        plan_id=plan_id,
        category=category,
        type=type,
        status=status,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        order_link_id=order_link_id,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('list')
      state = response.get('nextPageCursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def order(
    self,
    *,
    plan_id: str | None = None,
    category: Literal[
      'flexibleSavings', 'fundPool', 'fundPoolPremium', 'equityFund', 'onchainEarn'
    ]
    | None = None,
    type: Literal['Subscribe', 'Redeem', 'AutoReinvest'] | None = None,
    status: Literal['Completed', 'Pending', 'Failed'] | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    order_link_id: str | None = None,
    validate: bool | None = None,
  ) -> PwmInvestmentPlanOrderHistory:
    """Get the caller's PWM investment plan order history, across plans by default. Requires authentication.

    Args:
      plan_id: Investment plan identifier.
      category: Product type.
      type: Order type.
      status: Order status.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      limit: Page size.
      cursor: Pagination cursor.
      order_link_id: Order link ID.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/pwm/investment-plan/order)
    """
    request: Request = Request()
    if plan_id is not None:
      request['planId'] = plan_id
    if category is not None:
      request['category'] = category
    if type is not None:
      request['type'] = type
    if status is not None:
      request['status'] = status
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    if order_link_id is not None:
      request['orderLinkId'] = order_link_id
    return await self.request(
      request,
      method='GET',
      path='/v5/earn/pwm/investment-plan/order',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PwmInvestmentPlanOrderHistory,
    )
