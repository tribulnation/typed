# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint
from typed_bybit.schemas import PwmPlanProductCategory


class Request(TypedDict):
  planId: str
  """Investment plan ID."""


class PwmNewPlanProducts(TypedDict):
  """Configured product allocations, grouped by category."""

  multiCoinsEarning: NotRequired[PwmPlanProductCategory]
  fixedYield: NotRequired[PwmPlanProductCategory]
  equityFunds: NotRequired[PwmPlanProductCategory]
  onchainEarn: NotRequired[PwmPlanProductCategory]


class PwmNewPlan(TypedDict):
  """Proposed configuration of a PWM investment plan pending subscription."""

  planId: str
  """Unique plan identifier."""
  planName: str
  """Plan display name."""
  planType: Literal['stable', 'advanced']
  """Plan risk classification."""
  status: Literal['PendingSubscription']
  """Plan status. Always `PendingSubscription` on this endpoint."""
  source: Literal['consultant', 'direct', 'institution']
  """How the plan was originated."""
  totalConfiguredAmountUsd: str
  """Total configured investment allocation, in USD."""
  products: PwmNewPlanProducts
  createdTime: TimestampMillis
  """Plan creation time, as a millisecond timestamp."""


class NewPlan(RpcEndpoint):
  """Get the proposed configuration of a PWM investment plan awaiting the caller's subscription. Requires authentication."""

  async def new_plan(self, plan_id: str, *, validate: bool | None = None) -> PwmNewPlan:
    """Get the proposed configuration of a PWM investment plan awaiting the caller's subscription. Requires authentication.

    Args:
      plan_id: Investment plan ID.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/pwm/investment-plan/new-plan)
    """
    return await self.request(
      Request(planId=plan_id),
      method='GET',
      path='/v5/earn/pwm/investment-plan/new-plan',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PwmNewPlan,
    )
