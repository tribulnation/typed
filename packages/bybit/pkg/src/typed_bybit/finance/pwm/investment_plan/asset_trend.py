# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bybit.core import TimestampIso, TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class PwmAssetTrendPoint(TypedDict):
  """One day's portfolio valuation."""

  date: TimestampIso
  """Calendar date, `YYYY-MM-DD`."""
  assetValueUsd: str
  """Portfolio valuation, in USD, from the daily settlement."""


class Request(TypedDict):
  planId: str
  """Investment plan identifier."""
  startTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""


class PwmInvestmentPlanAssetTrend(TypedDict):
  """Daily portfolio valuation history for a PWM investment plan."""

  planId: str
  """Investment plan identifier."""
  dataPoints: list[PwmAssetTrendPoint]
  """Daily asset value snapshots, chronological."""


class AssetTrend(RpcEndpoint):
  """Get a PWM investment plan's daily portfolio valuation history. Requires authentication."""

  async def asset_trend(
    self,
    plan_id: str,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    validate: bool | None = None,
  ) -> PwmInvestmentPlanAssetTrend:
    """Get a PWM investment plan's daily portfolio valuation history. Requires authentication.

    Args:
      plan_id: Investment plan identifier.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/pwm/investment-plan/asset-trend)
    """
    request: Request = Request(planId=plan_id)
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    return await self.request(
      request,
      method='GET',
      path='/v5/earn/pwm/investment-plan/asset-trend',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PwmInvestmentPlanAssetTrend,
    )
