# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class RwaPosition(TypedDict):
  """One active RWA position."""

  productId: int
  """Product identifier."""
  coin: str
  """Settlement currency."""
  assetSymbol: str
  """Underlying asset ticker symbol."""
  effectiveShare: str
  """Redeemable share quantity."""
  processingStakeAmount: NotRequired[str]
  """Stake amount awaiting settlement, during the T+N window."""
  processingRedeemShare: NotRequired[str]
  """Shares pending redemption settlement."""
  bonusEarned: str
  """Cumulative bonus earned, in the settlement coin."""
  nav: str
  """Net Asset Value per share, at the current time."""
  holdAmount: str
  """Hold value: `effectiveShare` times `nav`, in the settlement coin."""
  duration: int
  """Lock-up period, in days. `0` for flexible products."""


class RwaPositionList(TypedDict):
  """Active RWA positions."""

  list: list[RwaPosition]
  """Position records."""


class Position(RpcEndpoint):
  """Get the caller's active RWA positions. Requires authentication. No request parameters."""

  async def position(self, *, validate: bool | None = None) -> RwaPositionList:
    """Get the caller's active RWA positions. Requires authentication. No request parameters.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/rwa/position)
    """
    return await self.request(
      None,
      method='GET',
      path='/v5/earn/rwa/position',
      meta={'signed': True},
      validate=validate,
      response_type=RwaPositionList,
    )
