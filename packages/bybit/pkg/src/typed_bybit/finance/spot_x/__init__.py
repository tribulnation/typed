# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .launchpool import Launchpool
from .puzzle import Puzzle
from .token_splash import TokenSplash
from typed_bybit.core.http import RpcEndpoint


class SpotX(RpcEndpoint):
  """SpotX -- Bybit's family of promotional spot-trading campaign products: Launchpool (stake coins to farm a newly-listed project's token for free), Puzzle, and Token Splash (reward campaigns tied to new listings). Covers project/activity discovery, a user's staking position, and participation/activity history across all three sub-products.

  References:
    - [Upstream docs](https://bybit-exchange.github.io/docs/v5/finance/spot-x/launchpool/launchpool-project-list)
  """

  @cached_property
  def launchpool(self) -> Launchpool:
    """Launchpool -- stake supported coins (USDT, USDC, or a project's own token, depending on the pool) to farm a newly-listed project's token for free, redeemable anytime: browse the project (activity) list, check a user's current staking position, and read staking and activity history.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/finance/spot-x/launchpool/launchpool-project-list)
    """
    return Launchpool(client=self.client)

  @cached_property
  def puzzle(self) -> Puzzle:
    """Spot-X Puzzle -- a promotional listing campaign under the SpotX family; the sole endpoint here queries the list of Puzzle activities (projects) currently open to participate in.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/finance/spot-x/puzzle/puzzle-project-list)
    """
    return Puzzle(client=self.client)

  @cached_property
  def token_splash(self) -> TokenSplash:
    """Token Splash -- a promotional new-listing reward campaign under the SpotX family: browse the campaign (project) list and query a user's own participation parameters for a given campaign.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/finance/spot-x/token-splash/token-splash-project-list)
    """
    return TokenSplash(client=self.client)
