# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  status: Literal[0, 1, 2]
  """Activity phase."""
  projectId: NotRequired[str]
  """Activity identifier."""
  activityCoin: NotRequired[str]
  """Activity coin."""
  cursor: NotRequired[str]
  """Pagination cursor."""
  limit: NotRequired[int]
  """Page size."""


class TokenSplashProject(TypedDict):
  """One Token Splash activity."""

  status: Literal[0, 1, 2]
  """Activity phase."""
  projectId: str
  """Unique activity identifier."""
  activityCoin: str
  """Core activity coin."""
  rewardCoin: str
  """Distributed reward token."""
  totalReward: str
  """Sum of all prize pools."""
  participantCount: str
  """Registered participants."""
  registrationStartTime: TimestampMillis
  """Registration window opening time, as a millisecond timestamp."""
  activityEndTime: TimestampMillis
  """Activity end time, as a millisecond timestamp."""
  registrationStartTimeUtc0: NotRequired[str]
  """Registration window opening time, as a human-readable UTC string. Not documented on the upstream page; observed live."""
  activityEndTimeUtc0: NotRequired[str]
  """Activity end time, as a human-readable UTC string. Not documented on the upstream page; observed live."""


class TokenSplashProjectList(TypedDict):
  """One page of Token Splash activities."""

  list: list[TokenSplashProject]
  """Token Splash activity records."""
  nextPageCursor: str
  """Cursor for the next page. Empty when there is no further page."""


class ProjectList(RpcEndpoint):
  """Get Token Splash activities: a trading-volume-based reward campaign for a given coin. No authentication required."""

  def project_list_paged(
    self,
    status: Literal[0, 1, 2],
    *,
    project_id: str | None = None,
    activity_coin: str | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[TokenSplashProject, str]:
    """Get Token Splash activities: a trading-volume-based reward campaign for a given coin. No authentication required.

    Paged variant of `project_list`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      status: Activity phase.
      project_id: Activity identifier.
      activity_coin: Activity coin.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/spot-x/token-splash/token-splash-project-list)
    """

    async def next(cursor: str) -> tuple[list[TokenSplashProject], str | None]:
      response = await self.project_list(
        status,
        project_id=project_id,
        activity_coin=activity_coin,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('list')
      state = response.get('nextPageCursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def project_list(
    self,
    status: Literal[0, 1, 2],
    *,
    project_id: str | None = None,
    activity_coin: str | None = None,
    cursor: str | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> TokenSplashProjectList:
    """Get Token Splash activities: a trading-volume-based reward campaign for a given coin. No authentication required.

    Args:
      status: Activity phase.
      project_id: Activity identifier.
      activity_coin: Activity coin.
      cursor: Pagination cursor.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/spot-x/token-splash/token-splash-project-list)
    """
    request: Request = Request(status=status)
    if project_id is not None:
      request['projectId'] = project_id
    if activity_coin is not None:
      request['activityCoin'] = activity_coin
    if cursor is not None:
      request['cursor'] = cursor
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/v5/spot-x/token-splash/project/list',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=TokenSplashProjectList,
    )
