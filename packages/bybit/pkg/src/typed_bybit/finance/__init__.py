# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .advanced_earn import AdvancedEarn
from .byusdt import Byusdt
from .easy_onchain import EasyOnchain
from .fixed_saving import FixedSaving
from .hold_to_earn import HoldToEarn
from .pwm import Pwm
from .rwa import Rwa
from .spot_x import SpotX
from typed_bybit.core.http import RpcEndpoint


class Finance(RpcEndpoint):
  """Finance — Bybit's wealth-management product domain, grouping several distinct Earn-style yield products (byUSDT, Easy & On-Chain Earn, Fixed Saving, Hold-to-Earn, RWA, among others), each with its own product catalog, order placement, position, and yield-history endpoints.

  References:
    - [Upstream docs](https://bybit-exchange.github.io/docs/v5/finance/earn/easy-onchain/product-info)
  """

  @cached_property
  def advanced_earn(self) -> AdvancedEarn:
    """Advanced Earn — Bybit's family of structured/derivative earn products (Discount Buy, Double Win, Dual Asset, Smart Leverage, and Liquidity Mining), each letting a user quote, place, and manage a position in a coin-settled or dual-currency structured product. The public WebSocket topics that push each product's live offer quotes live under the sibling `finance_ws` connection. Discount Buy, Double Win, Dual Asset, and Smart Leverage all share the same underlying product-listing wire endpoint, distinguished only by a `category` query parameter.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/discount-buy/product-info)
    """
    return AdvancedEarn(client=self.client)

  @cached_property
  def byusdt(self) -> Byusdt:
    """byUSDT — a USDT-backed yield token: mint byUSDT from USDT or redeem it back, track order status, view held position and current APR, and query daily/hourly yield history.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/finance/earn/byusdt/product)
    """
    return Byusdt(client=self.client)

  @cached_property
  def easy_onchain(self) -> EasyOnchain:
    """Easy & On-Chain Earn — flexible savings and on-chain staking products: browse the product catalog and any active coupons, create and modify a position, and query order history plus hourly/APR yield history.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/finance/earn/easy-onchain/product-info)
    """
    return EasyOnchain(client=self.client)

  @cached_property
  def fixed_saving(self) -> FixedSaving:
    """Fixed Saving — locked-term savings products: browse the product catalog, subscribe with a place-order call, redeem before or at maturity, opt into auto-invest, and track open positions and past orders.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/finance/earn/fixed-saving/product)
    """
    return FixedSaving(client=self.client)

  @cached_property
  def hold_to_earn(self) -> HoldToEarn:
    """Hold-to-Earn — airdrop-style products that pay a yield, in the held coin or a related one, just for holding a qualifying balance: browse available products and query yield history. Read-only, no order-placement endpoints of its own.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/finance/earn/hold-to-earn/product)
    """
    return HoldToEarn(client=self.client)

  @cached_property
  def pwm(self) -> Pwm:
    """PWM (Private Wealth Management) — Bybit's custodian-account investment platform for wealth managers: move funds between a custodian's own sub-accounts and check transfer status at the top level, with sub-groupings for creating/managing funds and their investors (Asset Manager), one-off customized plans (Customize Plan), and standard investment plan subscriptions (Investment Plan).

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/finance/pwm/fund-transfer)
    """
    return Pwm(client=self.client)

  @cached_property
  def rwa(self) -> Rwa:
    """RWA — tokenized real-world-asset products (e.g. tokenized funds managed by asset firms like BlackRock): browse the product catalog and NAV chart, place orders, and track positions and order history.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/finance/rwa/product)
    """
    return Rwa(client=self.client)

  @cached_property
  def spot_x(self) -> SpotX:
    """SpotX -- Bybit's family of promotional spot-trading campaign products: Launchpool (stake coins to farm a newly-listed project's token for free), Puzzle, and Token Splash (reward campaigns tied to new listings). Covers project/activity discovery, a user's staking position, and participation/activity history across all three sub-products.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/finance/spot-x/launchpool/launchpool-project-list)
    """
    return SpotX(client=self.client)
