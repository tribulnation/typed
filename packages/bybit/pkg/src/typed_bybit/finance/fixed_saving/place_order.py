# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class FixedSavingOrderResult(TypedDict):
  """Identifiers for a placed fixed saving order."""

  orderId: str
  """Exchange-generated order identifier."""
  orderLinkId: str
  """Caller-supplied order link identifier."""


class Request(TypedDict):
  """Fixed-term saving stake order."""

  productId: str
  """Product identifier, from the product-info endpoint."""
  category: Literal['FixedTermSaving', 'FundPool', 'FundPoolPremium']
  """Product sub-type."""
  coin: str
  """Coin to stake."""
  amount: str
  """Stake amount. Must be greater than 0."""
  accountType: Literal['FUND', 'UNIFIED']
  """Source account funding the stake."""
  orderLinkId: str
  """Caller-supplied order ID, max 36 characters, used for idempotency. Reusing a value errors."""
  autoInvest: NotRequired[bool]
  """Enable auto-reinvestment on the resulting position. Applies only when `category` is `FundPool`."""


class PlaceOrder(RpcEndpoint):
  """Stake into a fixed-term saving product. Requires API key "Earn" permission."""

  async def place_order(
    self,
    *,
    product_id: str,
    category: Literal['FixedTermSaving', 'FundPool', 'FundPoolPremium'],
    coin: str,
    amount: str,
    account_type: Literal['FUND', 'UNIFIED'],
    order_link_id: str,
    auto_invest: bool | None = None,
    validate: bool | None = None,
  ) -> FixedSavingOrderResult:
    """Stake into a fixed-term saving product. Requires API key "Earn" permission.

    Args:
      product_id: Product identifier, from the product-info endpoint.
      category: Product sub-type.
      coin: Coin to stake.
      amount: Stake amount. Must be greater than 0.
      account_type: Source account funding the stake.
      order_link_id: Caller-supplied order ID, max 36 characters, used for idempotency. Reusing a value errors.
      auto_invest: Enable auto-reinvestment on the resulting position. Applies only when `category` is `FundPool`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/earn/fixed-saving/place-order)
    """
    request: Request = Request(
      productId=product_id,
      category=category,
      coin=coin,
      amount=amount,
      accountType=account_type,
      orderLinkId=order_link_id,
    )
    if auto_invest is not None:
      request['autoInvest'] = auto_invest
    return await self.request(
      request,
      method='POST',
      path='/v5/earn/fixed-term/place-order',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FixedSavingOrderResult,
    )
