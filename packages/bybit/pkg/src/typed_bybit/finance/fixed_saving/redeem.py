# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class FixedSavingRedeemResult(TypedDict):
  """Estimated principal and earnings returned by an early redemption."""

  redeemAmount: str
  """Estimated redeemed principal."""
  estEarnings: str
  """Estimated earnings at the early-redemption APY."""


class Request(TypedDict):
  """Early redemption of a FundPool fixed saving position."""

  productId: str
  """Product identifier."""
  category: Literal['FundPool']
  """Product sub-type. Must be `FundPool`."""
  positionId: str
  """Position identifier to redeem."""


class Redeem(RpcEndpoint):
  """Redeem a FundPool fixed-term saving position before maturity. Requires API key "Earn" permission."""

  async def redeem(
    self,
    *,
    product_id: str,
    category: Literal['FundPool'] = 'FundPool',
    position_id: str,
    validate: bool | None = None,
  ) -> FixedSavingRedeemResult:
    """Redeem a FundPool fixed-term saving position before maturity. Requires API key "Earn" permission.

    Args:
      product_id: Product identifier.
      category: Product sub-type. Must be `FundPool`.
      position_id: Position identifier to redeem.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/finance/earn/fixed-saving/redeem)
    """
    return await self.request(
      Request(productId=product_id, category=category, positionId=position_id),
      method='POST',
      path='/v5/earn/fixed-term/redeem',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FixedSavingRedeemResult,
    )
