# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class FreezeSubUidResult(TypedDict):
  """Empty on success; no fields."""


class Request(TypedDict):
  """Freeze/unfreeze parameters."""

  subuid: int
  """Sub-account user ID."""
  frozen: Literal[0, 1]
  """Target frozen state."""


class FreezeSubUid(RpcEndpoint):
  """Freeze or unfreeze a sub-account. Requires the master account's own API key with at least one of 'Account Transfer', 'Subaccount Transfer' or 'Withdrawal' permissions."""

  async def freeze_sub_uid(
    self,
    subuid: int,
    *,
    frozen: Literal[0, 1],
    validate: bool | None = None,
  ) -> FreezeSubUidResult:
    """Freeze or unfreeze a sub-account. Requires the master account's own API key with at least one of 'Account Transfer', 'Subaccount Transfer' or 'Withdrawal' permissions.

    Args:
      subuid: Sub-account user ID.
      frozen: Target frozen state.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/user/froze-subuid)
    """
    return await self.request(
      Request(subuid=subuid, frozen=frozen),
      method='POST',
      path='/v5/user/frozen-sub-member',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FreezeSubUidResult,
    )
