# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  """Agreement selection and consent."""

  category: NotRequired[Literal[2, 3]]
  """Legacy product selector, superseded by `categoryV2`."""
  categoryV2: NotRequired[Literal[1, 2]]
  """Product selector. Either this or `category` is required."""
  agree: Literal[True]
  """Must be `true` to consent to the agreement."""


class SignAgreementResult(TypedDict):
  """Empty on success; no fields."""


class SignAgreement(RpcEndpoint):
  """Sign the commodity contract trading agreement (metals or crude oil) for the master account, granting the whole account tree trading eligibility for that product."""

  async def sign_agreement(
    self,
    category: Literal[2, 3] | None = None,
    *,
    category_v2: Literal[1, 2] | None = None,
    agree: Literal[True] = True,
    validate: bool | None = None,
  ) -> SignAgreementResult:
    """Sign the commodity contract trading agreement (metals or crude oil) for the master account, granting the whole account tree trading eligibility for that product.

    Args:
      category: Legacy product selector, superseded by `categoryV2`.
      category_v2: Product selector. Either this or `category` is required.
      agree: Must be `true` to consent to the agreement.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/user/sign-agreement)
    """
    request: Request = Request(agree=agree)
    if category is not None:
      request['category'] = category
    if category_v2 is not None:
      request['categoryV2'] = category_v2
    return await self.request(
      request,
      method='POST',
      path='/v5/user/agreement',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SignAgreementResult,
    )
