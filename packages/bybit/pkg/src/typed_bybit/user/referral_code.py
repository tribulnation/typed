# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class ReferralCode(TypedDict):
  """One referral code."""

  referralCode: str
  """The referral code string."""
  referralLink: str
  """Registration link carrying the referral code."""
  scene: Literal[1, 2]
  """Referral code kind."""


class ReferralCodeResult(TypedDict):
  """Active referral codes owned by the calling user."""

  referralCodes: list[ReferralCode]
  """Codes owned by the calling user."""


class ReferralCodeEndpoint(RpcEndpoint):
  """Query the referral codes owned by the calling user and their corresponding referral registration links. User identity comes from the API key; only active (non-expired) codes are returned."""

  async def referral_code(self, *, validate: bool | None = None) -> ReferralCodeResult:
    """Query the referral codes owned by the calling user and their corresponding referral registration links. User identity comes from the API key; only active (non-expired) codes are returned.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/user/referral-code)
    """
    return await self.request(
      None,
      method='GET',
      path='/v5/user/invitation/code',
      meta={'signed': True},
      validate=validate,
      response_type=ReferralCodeResult,
    )
