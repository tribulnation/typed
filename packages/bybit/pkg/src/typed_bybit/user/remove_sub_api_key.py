# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class RemoveSubApiKeyResult(TypedDict):
  """Empty on success; no fields."""


class Request(TypedDict):
  """Sub-account API key deletion parameters."""

  apikey: NotRequired[str]
  """The sub-account API key to delete. Required when called with the master account's key; must be omitted when called with the sub-account's own key."""


class RemoveSubApiKey(RpcEndpoint):
  """Delete a sub-account's API key. Callable with either the sub-account's own key, or the master account's key managing it. The key becomes invalid immediately."""

  async def remove_sub_api_key(
    self,
    apikey: str | None = None,
    *,
    validate: bool | None = None,
  ) -> RemoveSubApiKeyResult:
    """Delete a sub-account's API key. Callable with either the sub-account's own key, or the master account's key managing it. The key becomes invalid immediately.

    Args:
      apikey: The sub-account API key to delete. Required when called with the master account's key; must be omitted when called with the sub-account's own key.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/user/rm-sub-apikey)
    """
    request: Request = Request()
    if apikey is not None:
      request['apikey'] = apikey
    return await self.request(
      request,
      method='POST',
      path='/v5/user/delete-sub-api',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=RemoveSubApiKeyResult,
    )
