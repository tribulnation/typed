# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampIso
from typed_bybit.core.http import RpcEndpoint
from typed_bybit.schemas import ApiKeyPermissions


class ApiKeyInfoResult(TypedDict):
  """Metadata and permission scope of the API key used to call this endpoint."""

  id: str
  """Unique ID, internal use."""
  note: str
  """The remark on the key."""
  apiKey: str
  """The API key itself."""
  readOnly: Literal[0, 1]
  """Access level."""
  secret: str
  """Always the empty string on this endpoint."""
  permissions: ApiKeyPermissions
  ips: list[str]
  """Bound IP addresses."""
  type: Literal[1, 2]
  """Key type."""
  deadlineDay: int
  """Remaining valid days of the key, when it has no bound IP or the account password has changed since it was issued. Can be negative once the key has expired."""
  expiredAt: TimestampIso
  """Expiry time of the key."""
  createdAt: TimestampIso
  """Creation time of the key."""
  unified: NotRequired[int]
  """Legacy unified-account flag, superseded by `uta`."""
  uta: Literal[0, 1]
  """Whether the account is a Unified Trading Account."""
  userID: int
  """User ID the key belongs to."""
  inviterID: NotRequired[int]
  """User ID of the account that referred this user; `0` when none."""
  vipLevel: str
  """VIP tier designation, for example `PRO-1`."""
  mktMakerLevel: NotRequired[str]
  """Market maker tier designation; `"0"` when not a market maker."""
  affiliateID: NotRequired[int]
  """Affiliate ID associated with the account; `0` when none."""
  rsaPublicKey: NotRequired[str]
  """RSA public key configured for a self-generated (RSA-signed) key. Empty for an HMAC-signed key."""
  isMaster: bool
  """Whether the key belongs to a master account."""
  parentUid: NotRequired[str]
  """UID of the parent (master) account of a sub account's key; `"0"` for a master account's own key."""
  kycLevel: Literal['LEVEL_DEFAULT', 'LEVEL_1', 'LEVEL_2']
  """Personal account KYC level."""
  kycRegion: NotRequired[str]
  """KYC region/country code."""
  userIDInt64: NotRequired[str]
  """64-bit variant of `userID`, for a UID exceeding safe JSON-number precision."""
  inviterIDInt64: NotRequired[str]
  """64-bit variant of `inviterID`."""
  affiliateIDInt64: NotRequired[str]
  """64-bit variant of `affiliateID`."""


class ApiKeyInfo(RpcEndpoint):
  """Get the information of the api key. Use the api key pending to be checked to call the endpoint -- the returned scope belongs to the key making the call. Both master and sub user's api key are applicable."""

  async def api_key_info(self, *, validate: bool | None = None) -> ApiKeyInfoResult:
    """Get the information of the api key. Use the api key pending to be checked to call the endpoint -- the returned scope belongs to the key making the call. Both master and sub user's api key are applicable.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/user/apikey-info)
    """
    return await self.request(
      None,
      method='GET',
      path='/v5/user/query-api',
      meta={'signed': True},
      validate=validate,
      response_type=ApiKeyInfoResult,
    )
