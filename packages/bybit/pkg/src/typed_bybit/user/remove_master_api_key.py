# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class RemoveMasterApiKeyResult(TypedDict):
  """Empty on success; no fields."""


class RemoveMasterApiKey(RpcEndpoint):
  """Delete the calling master account's own API key. Must be called using the key being deleted; the key becomes invalid immediately upon deletion. Requires at least one of 'Account Transfer', 'Subaccount Transfer' or 'Withdrawal' permissions."""

  async def remove_master_api_key(
    self,
    *,
    validate: bool | None = None,
  ) -> RemoveMasterApiKeyResult:
    """Delete the calling master account's own API key. Must be called using the key being deleted; the key becomes invalid immediately upon deletion. Requires at least one of 'Account Transfer', 'Subaccount Transfer' or 'Withdrawal' permissions.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/user/rm-master-apikey)
    """
    return await self.request(
      None,
      method='POST',
      path='/v5/user/delete-api',
      meta={'signed': True},
      validate=validate,
      response_type=RemoveMasterApiKeyResult,
    )
