# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class CreateSubUidResult(TypedDict):
  """The newly-created sub-account."""

  uid: str
  """Sub-account user ID."""
  username: str
  """Username of the newly-created sub-account."""
  memberType: Literal[1, 6]
  """Sub-account kind."""
  status: Literal[1, 2, 4]
  """Account status."""
  remark: str
  """Remark attached to the account."""


class Request(TypedDict):
  """Sub-account creation parameters."""

  username: str
  """6-16 characters, must include both numbers and letters. Cannot match an existing or previously-deleted username."""
  password: NotRequired[str]
  """8-30 characters, must include numbers, upper- and lower-case letters."""
  memberType: Literal[1, 6]
  """Sub-account kind."""
  switch: NotRequired[Literal[0, 1]]
  """Quick-login setting."""
  isUta: NotRequired[bool]
  """Deprecated; every sub-account is a UTA account regardless of this value."""
  note: NotRequired[str]
  """Remark to attach to the new sub-account."""


class CreateSubUid(RpcEndpoint):
  """Create a new sub user ID. Use the master account's own API key, holding at least one of 'Account Transfer', 'Subaccount Transfer' or 'Withdrawal' permissions."""

  async def create_sub_uid(
    self,
    *,
    username: str,
    password: str | None = None,
    member_type: Literal[1, 6],
    switch: Literal[0, 1] | None = None,
    is_uta: bool | None = None,
    note: str | None = None,
    validate: bool | None = None,
  ) -> CreateSubUidResult:
    """Create a new sub user ID. Use the master account's own API key, holding at least one of 'Account Transfer', 'Subaccount Transfer' or 'Withdrawal' permissions.

    Args:
      username: 6-16 characters, must include both numbers and letters. Cannot match an existing or previously-deleted username.
      password: 8-30 characters, must include numbers, upper- and lower-case letters.
      member_type: Sub-account kind.
      switch: Quick-login setting.
      is_uta: Deprecated; every sub-account is a UTA account regardless of this value.
      note: Remark to attach to the new sub-account.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/user/create-subuid)
    """
    request: Request = Request(username=username, memberType=member_type)
    if password is not None:
      request['password'] = password
    if switch is not None:
      request['switch'] = switch
    if is_uta is not None:
      request['isUta'] = is_uta
    if note is not None:
      request['note'] = note
    return await self.request(
      request,
      method='POST',
      path='/v5/user/create-sub-member',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CreateSubUidResult,
    )
