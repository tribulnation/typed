# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class RemoveSubUidResult(TypedDict):
  """Empty on success; no fields."""


class Request(TypedDict):
  """Sub UID removal parameters."""

  subMemberId: str
  """Sub UID to remove."""


class RemoveSubUid(RpcEndpoint):
  """Permanently remove a sub UID from the master account. The venue refuses the call while the sub-account's asset balance exceeds 0.001 USDT."""

  async def remove_sub_uid(
    self,
    sub_member_id: str,
    *,
    validate: bool | None = None,
  ) -> RemoveSubUidResult:
    """Permanently remove a sub UID from the master account. The venue refuses the call while the sub-account's asset balance exceeds 0.001 USDT.

    Args:
      sub_member_id: Sub UID to remove.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/user/rm-subuid)
    """
    return await self.request(
      Request(subMemberId=sub_member_id),
      method='POST',
      path='/v5/user/del-submember',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=RemoveSubUidResult,
    )
