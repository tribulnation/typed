# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_bybit.core.ws import StreamsEndpoint


class SpreadPublicTradePush(TypedDict):
  """One executed public trade on a spread combination."""

  T: int
  """Trade fill time, as a millisecond timestamp."""
  s: str
  """Spread combination symbol name."""
  S: Literal['Buy', 'Sell']
  """Side of the taker."""
  v: str
  """Trade size."""
  p: str
  """Trade price."""
  L: str
  """Direction of price change relative to the previous trade. See the venue's shared enum reference."""
  i: str
  """Trade ID."""
  seq: int
  """Cross sequence ID."""


Payload = list[SpreadPublicTradePush]


class PublicTrade(StreamsEndpoint):
  """Subscribe to recently executed public trades on one spread combination symbol."""

  def public_trade(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[Payload, Any, Any]:
    """Subscribe to recently executed public trades on one spread combination symbol.

    Args:
      symbol: Spread combination symbol name, for example `SOLUSDT_SOL/USDT`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/spread/websocket/public/trade)
    """
    return self.subscribe(
      f'publicTrade.{symbol}',
      validate=validate,
      response_type=Payload,
    )
