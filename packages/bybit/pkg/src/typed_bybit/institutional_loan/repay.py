# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class RepayResult(TypedDict):
  """Acceptance of the repayment request."""

  repayOrderStatus: Literal['P']
  """Status of the repayment request."""


class Request(TypedDict):
  """Coin and amount to repay."""

  token: str
  """Coin to repay, for example USDT."""
  quantity: str
  """Quantity to repay."""


class Repay(RpcEndpoint):
  """Repay principal on an institutional loan. Repayment is processed asynchronously, usually within 2-3 minutes; the amount is deducted from the Unified wallet and excludes interest charges. Callable only by the risk unit's designated UID -- contact the Risk Manager beforehand; reserved for urgent use."""

  async def repay(
    self,
    *,
    token: str,
    quantity: str,
    validate: bool | None = None,
  ) -> RepayResult:
    """Repay principal on an institutional loan. Repayment is processed asynchronously, usually within 2-3 minutes; the amount is deducted from the Unified wallet and excludes interest charges. Callable only by the risk unit's designated UID -- contact the Risk Manager beforehand; reserved for urgent use.

    Args:
      token: Coin to repay, for example USDT.
      quantity: Quantity to repay.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/otc/repay)
    """
    return await self.request(
      Request(token=token, quantity=quantity),
      method='POST',
      path='/v5/ins-loan/repay-loan',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=RepayResult,
    )
