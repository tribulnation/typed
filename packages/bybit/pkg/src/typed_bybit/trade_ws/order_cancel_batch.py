# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_bybit.core.ws import TradeEndpoint


class OrderCancelBatchOrder(TypedDict):
  """One order to cancel."""

  symbol: str
  """Symbol name, uppercase only."""
  orderId: NotRequired[str]
  """Order ID. Either `orderId` or `orderLinkId` is required."""
  orderLinkId: NotRequired[str]
  """User-customised order ID. Either `orderId` or `orderLinkId` is required."""


Response = dict[str, Any]


class Request(TypedDict):
  """Batch of orders to cancel, as the `order.cancel-batch` command's `args[0]`, mirroring `POST /v5/order/cancel-batch`'s body."""

  category: Literal['linear', 'option', 'spot', 'inverse']
  """Product type."""
  request: list[OrderCancelBatchOrder]
  """Orders, up to 20 (10 for spot)."""


class OrderCancelBatch(TradeEndpoint):
  """Build the signed `order.cancel-batch` frame this command would send, without sending it. Cancels more than one unfilled or partially filled order in a single request. Limits per request: 20 orders (option, inverse, linear), 10 (spot)."""

  async def order_cancel_batch(
    self,
    category: Literal['linear', 'option', 'spot', 'inverse'],
    *,
    request: list[OrderCancelBatchOrder],
    validate: bool | None = None,
  ) -> Response:
    """Build the signed `order.cancel-batch` frame this command would send, without sending it. Cancels more than one unfilled or partially filled order in a single request. Limits per request: 20 orders (option, inverse, linear), 10 (spot).

    Args:
      category: Product type.
      request: Orders, up to 20 (10 for spot).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/websocket/trade/guideline)
    """
    return await self.request(
      Request(category=category, request=request),
      path='order.cancel-batch',
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
