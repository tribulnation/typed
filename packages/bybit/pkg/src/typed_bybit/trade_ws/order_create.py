# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_bybit.core.ws import TradeEndpoint


class OrderCreateLimitArgs(TypedDict):
  """Limit order parameters, mirroring `POST /v5/order/create`."""

  category: Literal['spot', 'linear', 'inverse', 'option']
  """Product category."""
  symbol: str
  """Symbol name, for example `BTCUSDT`."""
  side: Literal['Buy', 'Sell']
  """Order side."""
  orderType: Literal['Limit']
  """Order type."""
  qty: str
  """Order quantity."""
  price: str
  """Limit price. Required for limit orders."""
  timeInForce: NotRequired[Literal['GTC', 'IOC', 'FOK', 'PostOnly']]
  """Time in force."""


class OrderCreateMarketArgs(TypedDict):
  """Market order parameters, mirroring `POST /v5/order/create`; `price` plays no role."""

  category: Literal['spot', 'linear', 'inverse', 'option']
  """Product category."""
  symbol: str
  """Symbol name, for example `BTCUSDT`."""
  side: Literal['Buy', 'Sell']
  """Order side."""
  orderType: Literal['Market']
  """Order type."""
  qty: str
  """Order quantity."""
  timeInForce: NotRequired[Literal['GTC', 'IOC', 'FOK', 'PostOnly']]
  """Time in force."""


Response = dict[str, Any]


Request = OrderCreateMarketArgs | OrderCreateLimitArgs


class OrderCreate(TradeEndpoint):
  """Build the signed `order.create` frame this command would send, without sending it."""

  async def order_create(
    self, request: Request, *, validate: bool | None = None
  ) -> Response:
    """Build the signed `order.create` frame this command would send, without sending it.

    Args:
      request: Order parameters, as the `order.create` command's `args[0]`, discriminated by `orderType` -- mirrors `trade.create_order`'s own anyOf split.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/websocket/trade/guideline)
    """
    return await self.request(
      request,
      path='order.create',
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
