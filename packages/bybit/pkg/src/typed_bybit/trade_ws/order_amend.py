# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_bybit.core.ws import TradeEndpoint


class Request(TypedDict):
  """Order fields to amend, as the `order.amend` command's `args[0]`, mirroring `POST /v5/order/amend`'s body."""

  category: Literal['linear', 'inverse', 'spot', 'option']
  """Product type."""
  symbol: str
  """Symbol name, uppercase only."""
  orderId: NotRequired[str]
  """Order ID. Either `orderId` or `orderLinkId` is required."""
  orderLinkId: NotRequired[str]
  """User-customised order ID. Either `orderId` or `orderLinkId` is required."""
  orderIv: NotRequired[str]
  """Implied volatility, option only. Pass the real value, e.g. `0.1` for 10%."""
  triggerPrice: NotRequired[str]
  """Conditional order trigger price (Perps/Futures) or TP/SL & conditional trigger price (spot)."""
  qty: NotRequired[str]
  """Order quantity after modification. Omit to leave unchanged."""
  price: NotRequired[str]
  """Order price after modification. Omit to leave unchanged."""
  tpslMode: NotRequired[Literal['Full', 'Partial']]
  """TP/SL mode. `Full`: entire position, `tpOrderType`/`slOrderType` must be `Market`. `Partial`: partial position, limit TP/SL supported. Valid for linear & inverse."""
  takeProfit: NotRequired[str]
  """Take profit price after modification. `"0"` cancels the existing take profit."""
  stopLoss: NotRequired[str]
  """Stop loss price after modification. `"0"` cancels the existing stop loss."""
  tpTriggerBy: NotRequired[Literal['LastPrice', 'IndexPrice', 'MarkPrice']]
  """Trigger price type for take profit."""
  slTriggerBy: NotRequired[Literal['LastPrice', 'IndexPrice', 'MarkPrice']]
  """Trigger price type for stop loss."""
  triggerBy: NotRequired[Literal['LastPrice', 'IndexPrice', 'MarkPrice']]
  """Trigger price type."""
  tpLimitPrice: NotRequired[str]
  """Limit price when take profit is triggered. Only applies when the original order set a partial limit TP/SL."""
  slLimitPrice: NotRequired[str]
  """Limit price when stop loss is triggered. Only applies when the original order set a partial limit TP/SL."""


Response = dict[str, Any]


class OrderAmend(TradeEndpoint):
  """Build the signed `order.amend` frame this command would send, without sending it. Modifies an unfilled or partially filled order's price, quantity, trigger price or attached TP/SL. The acknowledgement means the request was accepted, not that the amend has taken effect -- this is asynchronous."""

  async def order_amend(
    self,
    category: Literal['linear', 'inverse', 'spot', 'option'],
    *,
    symbol: str,
    order_id: str | None = None,
    order_link_id: str | None = None,
    order_iv: str | None = None,
    trigger_price: str | None = None,
    qty: str | None = None,
    price: str | None = None,
    tpsl_mode: Literal['Full', 'Partial'] | None = None,
    take_profit: str | None = None,
    stop_loss: str | None = None,
    tp_trigger_by: Literal['LastPrice', 'IndexPrice', 'MarkPrice'] | None = None,
    sl_trigger_by: Literal['LastPrice', 'IndexPrice', 'MarkPrice'] | None = None,
    trigger_by: Literal['LastPrice', 'IndexPrice', 'MarkPrice'] | None = None,
    tp_limit_price: str | None = None,
    sl_limit_price: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Build the signed `order.amend` frame this command would send, without sending it. Modifies an unfilled or partially filled order's price, quantity, trigger price or attached TP/SL. The acknowledgement means the request was accepted, not that the amend has taken effect -- this is asynchronous.

    Args:
      category: Product type.
      symbol: Symbol name, uppercase only.
      order_id: Order ID. Either `orderId` or `orderLinkId` is required.
      order_link_id: User-customised order ID. Either `orderId` or `orderLinkId` is required.
      order_iv: Implied volatility, option only. Pass the real value, e.g. `0.1` for 10%.
      trigger_price: Conditional order trigger price (Perps/Futures) or TP/SL & conditional trigger price (spot).
      qty: Order quantity after modification. Omit to leave unchanged.
      price: Order price after modification. Omit to leave unchanged.
      tpsl_mode: TP/SL mode. `Full`: entire position, `tpOrderType`/`slOrderType` must be `Market`. `Partial`: partial position, limit TP/SL supported. Valid for linear & inverse.
      take_profit: Take profit price after modification. `"0"` cancels the existing take profit.
      stop_loss: Stop loss price after modification. `"0"` cancels the existing stop loss.
      tp_trigger_by: Trigger price type for take profit.
      sl_trigger_by: Trigger price type for stop loss.
      trigger_by: Trigger price type.
      tp_limit_price: Limit price when take profit is triggered. Only applies when the original order set a partial limit TP/SL.
      sl_limit_price: Limit price when stop loss is triggered. Only applies when the original order set a partial limit TP/SL.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/websocket/trade/guideline)
    """
    request: Request = Request(category=category, symbol=symbol)
    if order_id is not None:
      request['orderId'] = order_id
    if order_link_id is not None:
      request['orderLinkId'] = order_link_id
    if order_iv is not None:
      request['orderIv'] = order_iv
    if trigger_price is not None:
      request['triggerPrice'] = trigger_price
    if qty is not None:
      request['qty'] = qty
    if price is not None:
      request['price'] = price
    if tpsl_mode is not None:
      request['tpslMode'] = tpsl_mode
    if take_profit is not None:
      request['takeProfit'] = take_profit
    if stop_loss is not None:
      request['stopLoss'] = stop_loss
    if tp_trigger_by is not None:
      request['tpTriggerBy'] = tp_trigger_by
    if sl_trigger_by is not None:
      request['slTriggerBy'] = sl_trigger_by
    if trigger_by is not None:
      request['triggerBy'] = trigger_by
    if tp_limit_price is not None:
      request['tpLimitPrice'] = tp_limit_price
    if sl_limit_price is not None:
      request['slLimitPrice'] = sl_limit_price
    return await self.request(
      request,
      path='order.amend',
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
