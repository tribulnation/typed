# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, TypedDict
from typed_bybit.core.ws import TradeEndpoint


class Request(TypedDict):
  """Empty -- `ping` takes no parameters."""


Response = dict[str, Any]


class Ping(TradeEndpoint):
  """Build the `ping` frame this command would send. Unlike every other WS Trade command, sending this one is safe and this pass has done so live."""

  async def ping(self, *, validate: bool | None = None) -> Response:
    """Build the `ping` frame this command would send. Unlike every other WS Trade command, sending this one is safe and this pass has done so live.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/websocket/trade/guideline)
    """
    return await self.request(
      Request(),
      path='ping',
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
