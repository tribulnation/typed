# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class PreUpgradeTransactionLogEntry(TypedDict):
  """One USDC-wallet transaction, recorded before this account's upgrade to a Unified account."""

  symbol: str
  """Symbol name."""
  category: Literal['linear', 'option']
  """Product type."""
  side: Literal['Buy', 'Sell', 'None']
  """Side."""
  transactionTime: TimestampMillis
  """Transaction time."""
  type: str
  """Transaction type."""
  qty: str
  """Quantity."""
  size: str
  """Position size after this transaction."""
  currency: Literal['USDC', 'USDT', 'BTC', 'ETH']
  """Settlement currency."""
  tradePrice: str
  """Trade price."""
  funding: str
  """Funding fee. Positive means received, negative means deducted."""
  fee: str
  """Trading fee. Positive means expense, negative means rebate."""
  cashFlow: str
  """Cash flow from this transaction."""
  change: str
  """Wallet balance change from this transaction."""
  cashBalance: str
  """Wallet balance after this transaction."""
  feeRate: str
  """Fee or funding rate applied, depending on `type`."""
  bonusChange: str
  """Change in bonus balance from this transaction."""
  tradeId: str
  """Trade ID."""
  orderId: str
  """Order ID."""
  orderLinkId: str
  """User-customised order ID."""


class Request(TypedDict):
  category: Literal['linear', 'option']
  """Product type."""
  baseCoin: NotRequired[str]
  """Base coin."""
  type: NotRequired[str]
  """Transaction log type."""
  startTime: NotRequired[TimestampMillis]
  """Start time."""
  endTime: NotRequired[TimestampMillis]
  """End time."""
  limit: NotRequired[int]
  """Page size."""
  cursor: NotRequired[str]
  """Pagination cursor."""


class PreUpgradeTransactionLogResult(TypedDict):
  """Page of matching pre-upgrade transaction log entries."""

  nextPageCursor: NotRequired[str]
  """Opaque cursor for the next page. Pass it back as `cursor`; an empty string means there are no further pages."""
  list: list[PreUpgradeTransactionLogEntry]
  """Matching transaction log entries."""


class TransactionLog(RpcEndpoint):
  """Query the USDC Derivatives wallet's transaction log from before this account upgraded to a Unified account (USDC perpetuals and options records from classic accounts). Data available for the most recent 6 months."""

  def transaction_log_paged(
    self,
    category: Literal['linear', 'option'],
    *,
    base_coin: str | None = None,
    type: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[PreUpgradeTransactionLogEntry, str]:
    """Query the USDC Derivatives wallet's transaction log from before this account upgraded to a Unified account (USDC perpetuals and options records from classic accounts). Data available for the most recent 6 months.

    Paged variant of `transaction_log`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      category: Product type.
      base_coin: Base coin.
      type: Transaction log type.
      start_time: Start time.
      end_time: End time.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/pre-upgrade/transaction-log)
    """

    async def next(
      cursor: str,
    ) -> tuple[list[PreUpgradeTransactionLogEntry], str | None]:
      response = await self.transaction_log(
        category,
        base_coin=base_coin,
        type=type,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('list')
      state = response.get('nextPageCursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def transaction_log(
    self,
    category: Literal['linear', 'option'],
    *,
    base_coin: str | None = None,
    type: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> PreUpgradeTransactionLogResult:
    """Query the USDC Derivatives wallet's transaction log from before this account upgraded to a Unified account (USDC perpetuals and options records from classic accounts). Data available for the most recent 6 months.

    Args:
      category: Product type.
      base_coin: Base coin.
      type: Transaction log type.
      start_time: Start time.
      end_time: End time.
      limit: Page size.
      cursor: Pagination cursor.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/pre-upgrade/transaction-log)
    """
    request: Request = Request(category=category)
    if base_coin is not None:
      request['baseCoin'] = base_coin
    if type is not None:
      request['type'] = type
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/pre-upgrade/account/transaction-log',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PreUpgradeTransactionLogResult,
    )
