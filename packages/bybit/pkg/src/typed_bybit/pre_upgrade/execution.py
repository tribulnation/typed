# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint
from typed_bybit.schemas import StopOrderType


class Request(TypedDict):
  category: Literal['linear', 'inverse', 'option', 'spot']
  """Product type."""
  symbol: NotRequired[str]
  """Symbol name."""
  orderId: NotRequired[str]
  """Order ID."""
  orderLinkId: NotRequired[str]
  """User-customised order ID."""
  baseCoin: NotRequired[str]
  """Base coin."""
  startTime: NotRequired[TimestampMillis]
  """Start time."""
  endTime: NotRequired[TimestampMillis]
  """End time."""
  execType: NotRequired[
    Literal[
      'Trade',
      'AdlTrade',
      'Funding',
      'BustTrade',
      'Delivery',
      'Settle',
      'BlockTrade',
      'MovePosition',
      'FutureSpread',
      'CorporateAction',
    ]
  ]
  """Execution type."""
  limit: NotRequired[int]
  """Page size."""
  cursor: NotRequired[str]
  """Pagination cursor."""


class PreUpgradeExecution(TypedDict):
  """One execution (fill) recorded before this account's upgrade to a Unified account."""

  symbol: str
  """Symbol name."""
  orderId: str
  """Order ID."""
  orderLinkId: str
  """User-customised order ID."""
  side: Literal['Buy', 'Sell']
  """Side."""
  orderPrice: str
  """Order price."""
  orderQty: str
  """Order quantity."""
  leavesQty: str
  """Remaining quantity not executed."""
  orderType: Literal['Market', 'Limit']
  """Order type."""
  stopOrderType: StopOrderType
  execFee: str
  """Executed trading fee."""
  execId: str
  """Execution ID."""
  execPrice: str
  """Execution price."""
  execQty: str
  """Execution quantity."""
  execType: Literal[
    'Trade',
    'AdlTrade',
    'Funding',
    'BustTrade',
    'Delivery',
    'Settle',
    'BlockTrade',
    'MovePosition',
    'FutureSpread',
    'CorporateAction',
    'UNKNOWN',
  ]
  """Execution type. `UNKNOWN` may be returned by a classic account."""
  execValue: str
  """Executed order value."""
  execTime: TimestampMillis
  """Execution time."""
  isMaker: bool
  """Whether the execution was a maker fill."""
  tradeIv: NotRequired[str]
  """Implied volatility of the trade. Option only."""
  markIv: NotRequired[str]
  """Implied volatility of the mark price. Option only."""
  markPrice: NotRequired[str]
  """Mark price of the symbol at execution."""
  indexPrice: NotRequired[str]
  """Index price of the symbol at execution. Option only."""
  underlyingPrice: NotRequired[str]
  """Underlying price of the symbol at execution. Option only."""
  closedSize: str
  """Closed position size."""


class PreUpgradeExecutionResult(TypedDict):
  """Page of matching pre-upgrade executions."""

  category: Literal['linear', 'inverse', 'option', 'spot']
  """Product type."""
  nextPageCursor: NotRequired[str]
  """Opaque cursor for the next page. Pass it back as `cursor`; an empty string means there are no further pages."""
  list: list[PreUpgradeExecution]
  """Matching executions, newest first by `execTime`."""


class Execution(RpcEndpoint):
  """Query execution records (fills) recorded before this account upgraded to a Unified account, sorted by `execTime` descending. Supports USDT perpetual, USDC perpetual, inverse perpetual, inverse futures, spot and option products."""

  def execution_paged(
    self,
    category: Literal['linear', 'inverse', 'option', 'spot'],
    *,
    symbol: str | None = None,
    order_id: str | None = None,
    order_link_id: str | None = None,
    base_coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    exec_type: Literal[
      'Trade',
      'AdlTrade',
      'Funding',
      'BustTrade',
      'Delivery',
      'Settle',
      'BlockTrade',
      'MovePosition',
      'FutureSpread',
      'CorporateAction',
    ]
    | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[PreUpgradeExecution, str]:
    """Query execution records (fills) recorded before this account upgraded to a Unified account, sorted by `execTime` descending. Supports USDT perpetual, USDC perpetual, inverse perpetual, inverse futures, spot and option products.

    Paged variant of `execution`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      category: Product type.
      symbol: Symbol name.
      order_id: Order ID.
      order_link_id: User-customised order ID.
      base_coin: Base coin.
      start_time: Start time.
      end_time: End time.
      exec_type: Execution type.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/pre-upgrade/execution)
    """

    async def next(cursor: str) -> tuple[list[PreUpgradeExecution], str | None]:
      response = await self.execution(
        category,
        symbol=symbol,
        order_id=order_id,
        order_link_id=order_link_id,
        base_coin=base_coin,
        start_time=start_time,
        end_time=end_time,
        exec_type=exec_type,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('list')
      state = response.get('nextPageCursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def execution(
    self,
    category: Literal['linear', 'inverse', 'option', 'spot'],
    *,
    symbol: str | None = None,
    order_id: str | None = None,
    order_link_id: str | None = None,
    base_coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    exec_type: Literal[
      'Trade',
      'AdlTrade',
      'Funding',
      'BustTrade',
      'Delivery',
      'Settle',
      'BlockTrade',
      'MovePosition',
      'FutureSpread',
      'CorporateAction',
    ]
    | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> PreUpgradeExecutionResult:
    """Query execution records (fills) recorded before this account upgraded to a Unified account, sorted by `execTime` descending. Supports USDT perpetual, USDC perpetual, inverse perpetual, inverse futures, spot and option products.

    Args:
      category: Product type.
      symbol: Symbol name.
      order_id: Order ID.
      order_link_id: User-customised order ID.
      base_coin: Base coin.
      start_time: Start time.
      end_time: End time.
      exec_type: Execution type.
      limit: Page size.
      cursor: Pagination cursor.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/pre-upgrade/execution)
    """
    request: Request = Request(category=category)
    if symbol is not None:
      request['symbol'] = symbol
    if order_id is not None:
      request['orderId'] = order_id
    if order_link_id is not None:
      request['orderLinkId'] = order_link_id
    if base_coin is not None:
      request['baseCoin'] = base_coin
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if exec_type is not None:
      request['execType'] = exec_type
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/pre-upgrade/execution/list',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PreUpgradeExecutionResult,
    )
