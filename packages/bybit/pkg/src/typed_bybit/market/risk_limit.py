# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  category: Literal['linear', 'inverse']
  """Product type."""
  symbol: NotRequired[str]
  """Symbol name."""
  cursor: NotRequired[str]
  """Pagination cursor."""


class RiskLimitTier(TypedDict):
  """One risk limit tier."""

  id: int
  """Tier identifier."""
  symbol: str
  """Symbol name."""
  riskLimitValue: str
  """Position value ceiling of the tier."""
  maintenanceMargin: str
  """Maintenance margin rate of the tier."""
  initialMargin: str
  """Initial margin rate of the tier."""
  isLowestRisk: Literal[0, 1]
  """Whether this is the lowest tier: `1` for yes, `0` for no."""
  maxLeverage: str
  """Maximum leverage allowed in the tier."""
  mmDeduction: str
  """Maintenance margin deduction applied when entering the tier."""


class RiskLimitResult(TypedDict):
  """Risk limit tiers."""

  category: Literal['linear', 'inverse']
  """Product type."""
  list: list[RiskLimitTier]
  """Risk limit tiers, ordered from the lowest tier upwards."""
  nextPageCursor: NotRequired[str]
  """Opaque cursor for the next page. Pass it back as `cursor`; an empty string means there are no further pages."""


class RiskLimit(RpcEndpoint):
  """Get the risk limit tiers of a contract: position ceiling, margin rates and maximum leverage per tier."""

  def risk_limit_paged(
    self,
    category: Literal['linear', 'inverse'],
    *,
    symbol: str | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[RiskLimitTier, str]:
    """Get the risk limit tiers of a contract: position ceiling, margin rates and maximum leverage per tier.

    Paged variant of `risk_limit`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      category: Product type.
      symbol: Symbol name.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/market/risk-limit)
    """

    async def next(cursor: str) -> tuple[list[RiskLimitTier], str | None]:
      response = await self.risk_limit(
        category, symbol=symbol, cursor=(cursor or None), validate=validate
      )
      rows = response.get('list')
      state = response.get('nextPageCursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def risk_limit(
    self,
    category: Literal['linear', 'inverse'],
    *,
    symbol: str | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> RiskLimitResult:
    """Get the risk limit tiers of a contract: position ceiling, margin rates and maximum leverage per tier.

    Args:
      category: Product type.
      symbol: Symbol name.
      cursor: Pagination cursor.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/market/risk-limit)
    """
    request: Request = Request(category=category)
    if symbol is not None:
      request['symbol'] = symbol
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/market/risk-limit',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=RiskLimitResult,
    )
