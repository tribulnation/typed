# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint
from typed_bybit.schemas import MetricInterval


class LongShortRatio(TypedDict):
  """One long/short account ratio sample."""

  symbol: str
  """Symbol name."""
  buyRatio: str
  """Share of accounts holding a long position, as a ratio."""
  sellRatio: str
  """Share of accounts holding a short position, as a ratio."""
  timestamp: TimestampMillis
  """Sample time, as a millisecond timestamp."""


class LongShortRatioResult(TypedDict):
  """Long/short account ratio series."""

  list: list[LongShortRatio]
  """Ratio samples, sorted by timestamp in descending order."""
  nextPageCursor: NotRequired[str]
  """Opaque cursor for the next page. Pass it back as `cursor`; an empty string means there are no further pages."""


class Request(TypedDict):
  category: Literal['linear', 'inverse']
  """Product type."""
  symbol: str
  """Symbol name."""
  period: MetricInterval
  startTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  limit: NotRequired[int]
  """Page size."""
  cursor: NotRequired[str]
  """Pagination cursor."""


class LongShortRatioEndpoint(RpcEndpoint):
  """Get the ratio of accounts holding long versus short positions in a contract, sampled at a fixed interval and newest first."""

  def long_short_ratio_paged(
    self,
    category: Literal['linear', 'inverse'],
    *,
    symbol: str,
    period: MetricInterval,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[LongShortRatio, str]:
    """Get the ratio of accounts holding long versus short positions in a contract, sampled at a fixed interval and newest first.

    Paged variant of `long_short_ratio`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      category: Product type.
      symbol: Symbol name.
      period:
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/market/long-short-ratio)
    """

    async def next(cursor: str) -> tuple[list[LongShortRatio], str | None]:
      response = await self.long_short_ratio(
        category,
        symbol=symbol,
        period=period,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('list')
      state = response.get('nextPageCursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def long_short_ratio(
    self,
    category: Literal['linear', 'inverse'],
    *,
    symbol: str,
    period: MetricInterval,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> LongShortRatioResult:
    """Get the ratio of accounts holding long versus short positions in a contract, sampled at a fixed interval and newest first.

    Args:
      category: Product type.
      symbol: Symbol name.
      period:
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      limit: Page size.
      cursor: Pagination cursor.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/market/long-short-ratio)
    """
    request: Request = Request(category=category, symbol=symbol, period=period)
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/market/account-ratio',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=LongShortRatioResult,
    )
