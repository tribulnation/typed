# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint
from typed_bybit.schemas import RpiOrderbookLevel


class Request(TypedDict):
  category: NotRequired[Literal['spot', 'linear', 'inverse']]
  """Product type."""
  symbol: str
  """Symbol name."""
  limit: int
  """Depth per side."""


class RpiOrderbook(TypedDict):
  """Order book snapshot separating ordinary and RPI size."""

  s: str
  """Symbol name."""
  b: list[RpiOrderbookLevel]
  """Bid levels, sorted by price in descending order."""
  a: list[RpiOrderbookLevel]
  """Ask levels, sorted by price in ascending order."""
  ts: TimestampMillis
  """Time the snapshot was generated, as a millisecond timestamp."""
  u: int
  """Update identifier, matching the websocket order book stream."""
  seq: NotRequired[int]
  """Cross sequence number; smaller values were generated earlier."""
  cts: NotRequired[int]
  """Matching engine timestamp, matching the public trade stream."""


class RpiOrderbookEndpoint(RpcEndpoint):
  """Get an order book snapshot that separates ordinary resting size from retail price improvement (RPI) size at each level."""

  async def rpi_orderbook(
    self,
    category: Literal['spot', 'linear', 'inverse'] | None = None,
    *,
    symbol: str,
    limit: int,
    validate: bool | None = None,
  ) -> RpiOrderbook:
    """Get an order book snapshot that separates ordinary resting size from retail price improvement (RPI) size at each level.

    Args:
      category: Product type.
      symbol: Symbol name.
      limit: Depth per side.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/market/rpi-orderbook)
    """
    request: Request = Request(symbol=symbol, limit=limit)
    if category is not None:
      request['category'] = category
    return await self.request(
      request,
      method='GET',
      path='/v5/market/rpi_orderbook',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=RpiOrderbook,
    )
