# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint
from typed_bybit.schemas import RiskParameters


class ContractLotSizeFilter(TypedDict):
  """Quantity constraints for a linear or inverse contract."""

  maxOrderQty: Decimal
  """Maximum quantity of a limit or PostOnly order."""
  minOrderQty: Decimal
  """Minimum order quantity."""
  qtyStep: Decimal
  """Quantity increment."""
  postOnlyMaxOrderQty: NotRequired[str]
  """Maximum quantity of a PostOnly order. Deprecated upstream."""
  maxMktOrderQty: NotRequired[str]
  """Maximum quantity of a market order."""
  minNotionalValue: Decimal
  """Minimum notional value of an order."""


class ContractPriceFilter(TypedDict):
  """Price constraints for a contract."""

  minPrice: Decimal
  """Minimum order price."""
  maxPrice: Decimal
  """Maximum order price."""
  tickSize: Decimal
  """Price increment."""


class LeverageFilter(TypedDict):
  """Leverage constraints for a contract."""

  minLeverage: Decimal
  """Minimum leverage."""
  maxLeverage: Decimal
  """Maximum leverage."""
  leverageStep: Decimal
  """Leverage increment."""


class OptionLotSizeFilter(TypedDict):
  """Quantity constraints for an option."""

  maxOrderQty: str
  """Maximum order quantity."""
  minOrderQty: str
  """Minimum order quantity."""
  qtyStep: str
  """Quantity increment."""


class OptionPriceFilter(TypedDict):
  """Price constraints for an option."""

  minPrice: str
  """Minimum order price."""
  maxPrice: str
  """Maximum order price."""
  tickSize: str
  """Price increment."""


class PreListingAuctionFee(TypedDict):
  """Fee rates applied during and after the auction."""

  auctionFeeRate: str
  """Fee rate charged during the auction phases."""
  takerFeeRate: str
  """Taker fee rate once continuous trading starts."""
  makerFeeRate: str
  """Maker fee rate once continuous trading starts."""


class PreListingPhase(TypedDict):
  """One scheduled phase of a pre-market auction."""

  phase: Literal[
    'NotStarted',
    'CallAuction',
    'CallAuctionNoCancel',
    'CrossMatching',
    'ContinuousTrading',
    'Finished',
  ]
  """Phase name."""
  startTime: TimestampMillis
  """Phase start time, as a millisecond timestamp."""
  endTime: TimestampMillis
  """Phase end time, as a millisecond timestamp."""


class Request(TypedDict):
  category: Literal['spot', 'linear', 'inverse', 'option']
  """Product type."""
  symbol: NotRequired[str]
  """Symbol name."""
  baseCoin: NotRequired[str]
  """Base coin."""
  status: NotRequired[Literal['PreLaunch', 'Trading', 'Delivering', 'Closed']]
  """Instrument status."""
  symbolType: NotRequired[str]
  """Symbol classification."""
  limit: NotRequired[int]
  """Page size."""
  cursor: NotRequired[str]
  """Pagination cursor."""


class SpotLotSizeFilter(TypedDict):
  """Quantity and amount constraints for a spot pair."""

  basePrecision: Decimal
  """Decimal precision of the base coin quantity."""
  quotePrecision: str
  """Decimal precision of the quote coin amount."""
  minOrderQty: Decimal
  """Minimum order quantity. Deprecated upstream in favour of `minOrderAmt`."""
  maxOrderQty: Decimal
  """Maximum order quantity. Deprecated upstream."""
  minOrderAmt: Decimal
  """Minimum order amount in the quote coin."""
  maxOrderAmt: str
  """Maximum order amount in the quote coin. Deprecated upstream."""
  maxLimitOrderQty: str
  """Maximum quantity of a limit order."""
  maxMarketOrderQty: str
  """Maximum quantity of a market order."""
  postOnlyMaxLimitOrderSize: str
  """Maximum size of a PostOnly or RPI limit order."""


class SpotPriceFilter(TypedDict):
  """Price constraints for a spot pair."""

  tickSize: Decimal
  """Price increment."""


class OptionInstrument(TypedDict):
  """A tradable option contract."""

  symbolId: int
  """Numeric symbol identifier."""
  symbol: str
  """Symbol name."""
  status: Literal['PreLaunch', 'Trading', 'Delivering', 'Closed']
  """Trading status of the option."""
  baseCoin: str
  """Base coin."""
  quoteCoin: str
  """Quote coin."""
  settleCoin: str
  """Settlement coin."""
  optionsType: Literal['Call', 'Put']
  """Option type."""
  launchTime: TimestampMillis
  """Launch time, as a millisecond timestamp."""
  deliveryTime: TimestampMillis
  """Delivery time, as a millisecond timestamp."""
  deliveryFeeRate: NotRequired[str]
  """Delivery fee rate."""
  priceFilter: OptionPriceFilter
  lotSizeFilter: OptionLotSizeFilter
  displayName: NotRequired[str]
  """Name shown on the Bybit web and app clients."""


class PreListingInfo(TypedDict):
  """Auction schedule and fees of a pre-market contract."""

  curAuctionPhase: Literal[
    'NotStarted',
    'CallAuction',
    'CallAuctionNoCancel',
    'CrossMatching',
    'ContinuousTrading',
    'Finished',
  ]
  """Current auction phase."""
  phases: list[PreListingPhase]
  """Scheduled auction phases."""
  auctionFeeInfo: PreListingAuctionFee
  skipCallAuction: NotRequired[bool]
  """Whether the call auction phase is skipped."""


class SpotInstrument(TypedDict):
  """A tradable spot pair."""

  symbolId: int
  """Numeric symbol identifier."""
  symbol: str
  """Symbol name."""
  baseCoin: str
  """Base coin."""
  quoteCoin: str
  """Quote coin."""
  innovation: NotRequired[Literal['0', '1']]
  """Whether the pair is in the innovation zone."""
  status: Literal['PreLaunch', 'Trading', 'Delivering', 'Closed']
  """Trading status of the pair."""
  marginTrading: Literal['none', 'both', 'utaOnly', 'normalSpotOnly']
  """Margin trading support for the pair."""
  stTag: Literal['0', '1']
  """Whether the pair carries the special treatment tag."""
  lotSizeFilter: SpotLotSizeFilter
  priceFilter: SpotPriceFilter
  riskParameters: NotRequired[RiskParameters]
  symbolType: NotRequired[str]
  """Regional or thematic classification, for example `stock` or an empty string."""
  xstockMultiplier: NotRequired[str]
  """Share multiplier; only meaningful for tokenised stock pairs."""


class ContractInstrument(TypedDict):
  """A tradable linear or inverse contract."""

  symbol: str
  """Symbol name."""
  symbolId: NotRequired[int]
  """Numeric symbol identifier."""
  contractType: Literal[
    'InversePerpetual', 'LinearPerpetual', 'LinearFutures', 'InverseFutures'
  ]
  """Contract type."""
  status: Literal['PreLaunch', 'Trading', 'Delivering', 'Closed']
  """Trading status of the contract."""
  baseCoin: str
  """Base coin."""
  quoteCoin: str
  """Quote coin."""
  launchTime: TimestampMillis
  """Launch time, as a millisecond timestamp."""
  deliveryTime: TimestampMillis
  """Delivery or delisting time, as a millisecond timestamp; `0` for perpetuals."""
  deliveryFeeRate: NotRequired[str]
  """Delivery fee rate; empty for perpetuals."""
  priceScale: str
  """Number of decimal places used for the price."""
  leverageFilter: LeverageFilter
  priceFilter: ContractPriceFilter
  lotSizeFilter: ContractLotSizeFilter
  unifiedMarginTrade: bool
  """Whether the contract is tradable from a unified account."""
  fundingInterval: int
  """Funding interval in minutes."""
  settleCoin: str
  """Settlement coin."""
  copyTrading: NotRequired[Literal['none', 'both', 'utaOnly', 'normalOnly']]
  """Copy trading eligibility of the contract."""
  upperFundingRate: NotRequired[str]
  """Upper bound of the funding rate."""
  lowerFundingRate: NotRequired[str]
  """Lower bound of the funding rate."""
  displayName: NotRequired[str]
  """Name shown on the Bybit web and app clients."""
  symbolType: NotRequired[str]
  """Regional or thematic classification, for example `stock` or an empty string."""
  forbidUplWithdrawal: NotRequired[bool]
  """Whether unrealised profit may not be withdrawn."""
  isPreListing: NotRequired[bool]
  """Whether the contract is a pre-market contract."""
  preListingInfo: NotRequired[PreListingInfo | None]
  """Pre-market auction details; `null` unless `isPreListing` is true."""
  riskParameters: NotRequired[RiskParameters]


class OptionInstrumentsInfo(TypedDict):
  """Instrument definitions for option contracts."""

  category: Literal['option']
  """Product type."""
  list: list[OptionInstrument]
  """Option contracts."""
  nextPageCursor: str
  """Opaque cursor for the next page. Pass it back as `cursor`; an empty string means there are no further pages."""


class SpotInstrumentsInfo(TypedDict):
  """Instrument definitions for spot pairs."""

  category: Literal['spot']
  """Product type."""
  list: list[SpotInstrument]
  """Spot pairs."""


class ContractInstrumentsInfo(TypedDict):
  """Instrument definitions for linear or inverse contracts."""

  category: Literal['linear', 'inverse']
  """Product type."""
  list: list[ContractInstrument]
  """Linear or inverse contracts."""
  nextPageCursor: str
  """Opaque cursor for the next page. Pass it back as `cursor`; an empty string means there are no further pages."""


Response = SpotInstrumentsInfo | ContractInstrumentsInfo | OptionInstrumentsInfo


class Instruments(RpcEndpoint):
  """Get the trading rules of every instrument in a product category: tick size, quantity step, leverage bounds, launch and delivery times."""

  async def instruments_paged(
    self,
    category: Literal['spot', 'linear', 'inverse', 'option'],
    *,
    symbol: str | None = None,
    base_coin: str | None = None,
    status: Literal['PreLaunch', 'Trading', 'Delivering', 'Closed'] | None = None,
    symbol_type: str | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[Response]:
    """Get the trading rules of every instrument in a product category: tick size, quantity step, leverage bounds, launch and delivery times.

    Paged variant of `instruments`: Passes each page's token back as `cursor` and stops when a response carries no `nextPageCursor`, or after `max_pages` pages when one is given.

    Args:
      category: Product type.
      symbol: Symbol name.
      base_coin: Base coin.
      status: Instrument status.
      symbol_type: Symbol classification.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/market/instrument)
    """
    cursor: str | None = None
    pages = 0
    while True:
      response = await self.instruments(
        category,
        symbol=symbol,
        base_coin=base_coin,
        status=status,
        symbol_type=symbol_type,
        limit=limit,
        cursor=cursor,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      cursor = response.get('nextPageCursor') if response is not None else None
      if not cursor:
        break

  async def instruments(
    self,
    category: Literal['spot', 'linear', 'inverse', 'option'],
    *,
    symbol: str | None = None,
    base_coin: str | None = None,
    status: Literal['PreLaunch', 'Trading', 'Delivering', 'Closed'] | None = None,
    symbol_type: str | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get the trading rules of every instrument in a product category: tick size, quantity step, leverage bounds, launch and delivery times.

    Args:
      category: Product type.
      symbol: Symbol name.
      base_coin: Base coin.
      status: Instrument status.
      symbol_type: Symbol classification.
      limit: Page size.
      cursor: Pagination cursor.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/market/instrument)
    """
    request: Request = Request(category=category)
    if symbol is not None:
      request['symbol'] = symbol
    if base_coin is not None:
      request['baseCoin'] = base_coin
    if status is not None:
      request['status'] = status
    if symbol_type is not None:
      request['symbolType'] = symbol_type
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/market/instruments-info',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
