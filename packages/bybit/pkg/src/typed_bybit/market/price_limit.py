# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class OrderPriceLimit(TypedDict):
  """Current order price band for one symbol."""

  symbol: str
  """Symbol name."""
  buyLmt: str
  """Highest price a buy order may be placed at."""
  sellLmt: str
  """Lowest price a sell order may be placed at."""
  ts: TimestampMillis
  """Time the band was computed, as a millisecond timestamp."""


class Request(TypedDict):
  category: NotRequired[Literal['spot', 'linear', 'inverse']]
  """Product type."""
  symbol: str
  """Symbol name."""


class PriceLimit(RpcEndpoint):
  """Get the current price band a resting order must fall inside for one symbol."""

  async def price_limit(
    self,
    category: Literal['spot', 'linear', 'inverse'] | None = None,
    *,
    symbol: str,
    validate: bool | None = None,
  ) -> OrderPriceLimit:
    """Get the current price band a resting order must fall inside for one symbol.

    Args:
      category: Product type.
      symbol: Symbol name.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/market/order-price-limit)
    """
    request: Request = Request(symbol=symbol)
    if category is not None:
      request['category'] = category
    return await self.request(
      request,
      method='GET',
      path='/v5/market/price-limit',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=OrderPriceLimit,
    )
