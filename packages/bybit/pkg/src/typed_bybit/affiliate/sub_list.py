# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import NotRequired, Sequence, TypedDict
from typed_bybit.core import DateIso, TimestampIso
from typed_bybit.core.http import RpcEndpoint


class AffiliateSubAffiliate(TypedDict):
  """One sub-affiliate."""

  subAffId: str
  """Sub-affiliate identifier."""
  userId: str
  """Sub-affiliate's user ID."""
  name: str
  """Account nickname."""
  email: str
  """Partially masked email address."""
  commissionsVol: dict[str, str]
  """Commission earned from this sub-affiliate over the queried period, keyed by coin."""
  commissionsForUsdt: str
  """Total commission earned from this sub-affiliate, converted to USDT."""
  becameAffTime: TimestampIso
  """When this user became a sub-affiliate."""
  startDate: TimestampIso
  """Echo of the queried period's start date."""
  endDate: TimestampIso
  """Echo of the queried period's end date."""


class Request(TypedDict):
  cursor: NotRequired[str]
  """Pagination cursor."""
  size: NotRequired[int]
  """Page size."""
  startDate: NotRequired[DateIso]
  """Period start date."""
  endDate: NotRequired[DateIso]
  """Period end date."""
  subAffId: NotRequired[int]
  """Sub-affiliate ID."""


class AffiliateSubListResult(TypedDict):
  """Page of sub-affiliates."""

  list: list[AffiliateSubAffiliate]
  """Sub-affiliates on this page."""
  nextPageCursor: str
  """Opaque cursor for the next page. Pass it back as `cursor`; an empty string means there are no further pages."""


class SubList(RpcEndpoint):
  """Get a paged list of the sub-affiliates under an affiliate account, with their commission earned per coin. Requires an affiliate account, called with an API key carrying only the Affiliate permission, from the master UID. Registered at https://affiliates.bybit.com."""

  def sub_list_paged(
    self,
    *,
    size: int | None = None,
    start_date: DateIso | None = None,
    end_date: DateIso | None = None,
    sub_aff_id: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[AffiliateSubAffiliate, str]:
    """Get a paged list of the sub-affiliates under an affiliate account, with their commission earned per coin. Requires an affiliate account, called with an API key carrying only the Affiliate permission, from the master UID. Registered at https://affiliates.bybit.com.

    Paged variant of `sub_list`: Passes each page's token back as `cursor` and stops when a response carries no `nextPageCursor`. Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      size: Page size.
      start_date: Period start date.
      end_date: Period end date.
      sub_aff_id: Sub-affiliate ID.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/affiliate/affiliate-sub-list)
    """

    async def next(cursor: str) -> tuple[Sequence[AffiliateSubAffiliate], str | None]:
      response = await self.sub_list(
        size=size,
        start_date=start_date,
        end_date=end_date,
        sub_aff_id=sub_aff_id,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('list')
      rows = rows if rows is not None else []
      state = response.get('nextPageCursor')
      return rows, state or None

    return PaginatedResponse('', next)

  async def sub_list(
    self,
    cursor: str | None = None,
    *,
    size: int | None = None,
    start_date: DateIso | None = None,
    end_date: DateIso | None = None,
    sub_aff_id: int | None = None,
    validate: bool | None = None,
  ) -> AffiliateSubListResult:
    """Get a paged list of the sub-affiliates under an affiliate account, with their commission earned per coin. Requires an affiliate account, called with an API key carrying only the Affiliate permission, from the master UID. Registered at https://affiliates.bybit.com.

    Args:
      cursor: Pagination cursor.
      size: Page size.
      start_date: Period start date.
      end_date: Period end date.
      sub_aff_id: Sub-affiliate ID.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/affiliate/affiliate-sub-list)
    """
    request: Request = Request()
    if cursor is not None:
      request['cursor'] = cursor
    if size is not None:
      request['size'] = size
    if start_date is not None:
      request['startDate'] = start_date
    if end_date is not None:
      request['endDate'] = end_date
    if sub_aff_id is not None:
      request['subAffId'] = sub_aff_id
    return await self.request(
      request,
      method='GET',
      path='/v5/affiliate/affiliate-sub-list',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=AffiliateSubListResult,
    )
