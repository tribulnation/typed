# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class AffiliateUserInfo(TypedDict):
  """Trading, deposit and commission detail for one affiliate client."""

  uid: str
  """Client's user ID."""
  vipLevel: Literal['0', '1', '2', '3', '4', '99']
  """VIP tier."""
  takerVol30Day: str
  """Taker trading volume over the last 30 days, in USDT."""
  makerVol30Day: str
  """Maker trading volume over the last 30 days, in USDT."""
  tradeVol30Day: str
  """Total trading volume over the last 30 days, in USDT."""
  depositAmount30Day: str
  """Total deposits over the last 30 days, in USDT. Updated every 5 minutes."""
  takerVol365Day: str
  """Lifetime taker trading volume, in USDT."""
  makerVol365Day: str
  """Lifetime maker trading volume, in USDT."""
  tradeVol365Day: str
  """Lifetime total trading volume, in USDT."""
  depositAmount365Day: str
  """Lifetime total deposits, in USDT."""
  totalWalletBalance: Literal['1', '2', '3', '4']
  """Wallet balance range."""
  depositUpdateTime: TimestampMillis
  """When the deposit figures above were last updated."""
  volUpdateTime: TimestampMillis
  """When the volume figures above were last updated."""
  KycLevel: Literal[0, 1, 2]
  """KYC level."""
  tradfiTradeVol30Day: str
  """TradFi trading volume over the last 30 days, in USDT."""
  tradfiTradeVol365Day: str
  """Lifetime TradFi trading volume, in USDT."""
  commissions30Day: dict[str, str]
  """Commission earned from this client over the last 30 days, keyed by coin."""
  commissions365Day: dict[str, str]
  """Lifetime commission earned from this client, keyed by coin."""
  paySendAmount30Day: NotRequired[str]
  """Bybit Pay send amount over the last 30 days."""
  payFtt: NotRequired[str]
  """Bybit Pay first-transaction-time indicator."""
  cardFtt: NotRequired[str]
  """Bybit Card first-transaction-time indicator."""


class Request(TypedDict):
  uid: str
  """User ID."""
  coin: NotRequired[str]
  """Coin."""
  business: NotRequired[Literal['1', '2', '3', '4', '5']]
  """Business line. `1`: Derivatives, `2`: Spot, `3`: ByFi, `4`: USDC, `5`: Options."""


class Info(RpcEndpoint):
  """Get detailed trading, deposit and commission information about one affiliate client. Requires an affiliate account, called with an API key carrying only the Affiliate permission, from the master UID."""

  async def info(
    self,
    *,
    uid: str,
    coin: str | None = None,
    business: Literal['1', '2', '3', '4', '5'] | None = None,
    validate: bool | None = None,
  ) -> AffiliateUserInfo:
    """Get detailed trading, deposit and commission information about one affiliate client. Requires an affiliate account, called with an API key carrying only the Affiliate permission, from the master UID.

    Args:
      uid: User ID.
      coin: Coin.
      business: Business line. `1`: Derivatives, `2`: Spot, `3`: ByFi, `4`: USDC, `5`: Options.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/affiliate/affiliate-info)
    """
    request: Request = Request(uid=uid)
    if coin is not None:
      request['coin'] = coin
    if business is not None:
      request['business'] = business
    return await self.request(
      request,
      method='GET',
      path='/v5/user/aff-customer-info',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=AffiliateUserInfo,
    )
