# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.ws import StreamsEndpoint
from typed_bybit.schemas import KlineInterval


class SpotKlineUpdate(TypedDict):
  """One candlestick, snapshotted mid-formation or on close."""

  start: TimestampMillis
  """Candle open time."""
  end: TimestampMillis
  """Candle close time."""
  interval: KlineInterval
  open: str
  """Opening price."""
  close: str
  """Latest traded price so far in this candle."""
  high: str
  """Highest price so far in this candle."""
  low: str
  """Lowest price so far in this candle."""
  volume: str
  """Trade volume so far in this candle."""
  turnover: str
  """Turnover so far in this candle."""
  confirm: bool
  """Whether this candle has closed. `false` while it is still forming."""
  timestamp: TimestampMillis
  """Timestamp of the last matched order included in this push."""


SpotKlineUpdates = list[SpotKlineUpdate]


class Kline(StreamsEndpoint):
  """Subscribe to live-updating candlesticks for one symbol and interval."""

  def kline(
    self,
    interval: KlineInterval,
    *,
    symbol: str,
    validate: bool | None = None,
  ) -> StreamManager[SpotKlineUpdates, Any, Any]:
    """Subscribe to live-updating candlesticks for one symbol and interval.

    Args:
      interval:
      symbol: Symbol name, for example `BTCUSDT`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/websocket/public/kline)
    """
    return self.subscribe(
      f'kline.{interval}.{symbol}',
      validate=validate,
      response_type=SpotKlineUpdates,
    )
