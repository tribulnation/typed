# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_bybit.core.ws import StreamsEndpoint


class SpotTicker(TypedDict):
  """One 24-hour rolling ticker snapshot for one symbol."""

  type: NotRequired[Literal['snapshot', 'delta']]
  """Whether this push is a full `snapshot` or a `delta` against the previous one. Read off the frame by the core, not part of the wire `data` object."""
  symbol: str
  """Symbol name."""
  lastPrice: Decimal
  """Last traded price."""
  highPrice24h: Decimal
  """24-hour high price."""
  lowPrice24h: Decimal
  """24-hour low price."""
  prevPrice24h: Decimal
  """Price 24 hours ago."""
  volume24h: Decimal
  """24-hour trading volume, in base currency."""
  turnover24h: Decimal
  """24-hour trading turnover, in quote currency."""
  price24hPcnt: Decimal
  """24-hour price change ratio."""
  usdIndexPrice: Decimal
  """USD index price, used to calculate risk on USDC/USD contracts. For reference only, not the real-time index price."""


class Ticker(StreamsEndpoint):
  """Subscribe to 24-hour rolling ticker snapshots for one spot symbol."""

  def ticker(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[SpotTicker, Any, Any]:
    """Subscribe to 24-hour rolling ticker snapshots for one spot symbol.

    Args:
      symbol: Symbol name, for example `BTCUSDT`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/websocket/public/ticker)
    """
    return self.subscribe(
      f'tickers.{symbol}',
      validate=validate,
      response_type=SpotTicker,
    )
