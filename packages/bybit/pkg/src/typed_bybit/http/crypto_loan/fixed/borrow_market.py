# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/crypto-loan-fixed/borrow-order-quote` — Get Fixed-term Borrowing Market."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class FixedBorrowMarketOffer(TypedDict):
  """One resting fixed-term borrowing offer."""

  orderCurrency: str
  """Coin name."""
  term: Literal[7, 14, 30, 60, 90, 180]
  """Fixed term, in days."""
  annualRate: str
  """Annualized interest rate."""
  qty: str
  """Quantity available at this rate and term."""


class FixedBorrowMarketResult(TypedDict):
  """Resting fixed-term borrowing offers, sorted per `orderBy`/`sort`."""

  list: list[FixedBorrowMarketOffer]
  """Resting offers."""


adapter = validator[FixedBorrowMarketResult](FixedBorrowMarketResult)


class BorrowMarket(Endpoint):
  """`Get Fixed-term Borrowing Market` — mixed into the router that owns `crypto_loan.fixed.borrow_market`."""

  async def borrow_market(
    self,
    *,
    order_currency: str,
    order_by: Literal['apy', 'term', 'quantity'],
    term: Literal['7', '14', '30', '60', '90', '180'] | None = None,
    sort: Literal[0, 1] | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> FixedBorrowMarketResult:
    """Query the resting fixed-term lending offers available to borrow against, for one coin.

    Args:
      order_currency: Coin name.
      order_by: Sort field.
      term: Fixed term, in days. Omit to return every term.
      sort: Sort direction.
      limit: Number of records per page. Range [1, 100]; defaults to 10.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/fixed/borrow-market)
    """
    params: dict = {
      'orderCurrency': order_currency,
      'orderBy': order_by,
    }
    if term is not None:
      params['term'] = term
    if sort is not None:
      params['sort'] = sort
    if limit is not None:
      params['limit'] = limit
    r = await self.request(
      'GET', '/v5/crypto-loan-fixed/borrow-order-quote', params=params
    )
    return self.result(r, adapter, validate)
