# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/crypto-loan-fixed/available-inventory` — Get Fixed-term Available Inventory."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class FixedAvailableInventoryResult(TypedDict):
  """Available inventory for the requested coin, term and rate."""

  currency: str
  """Coin name."""
  term: Literal[7, 14, 30, 60, 90, 180]
  """Fixed term, in days."""
  annualRate: str
  """Annualized interest rate this inventory figure applies to."""
  availableInventory: str
  """Available inventory in the lending pool at this rate and term."""
  updateTime: TimestampMillis
  """Time this inventory figure was last updated."""


adapter = validator[FixedAvailableInventoryResult](FixedAvailableInventoryResult)


class AvailableInventory(Endpoint):
  """`Get Fixed-term Available Inventory` — mixed into the router that owns `crypto_loan.fixed.available_inventory`."""

  async def available_inventory(
    self,
    *,
    currency: str,
    term: Literal['7', '14', '30', '60', '90', '180'],
    annual_rate: str,
    validate: bool | None = None,
  ) -> FixedAvailableInventoryResult:
    """Query the lending pool's available inventory for a fixed-term loan at a given coin, term and custom annual rate.

    Args:
      currency: Coin name, uppercase only.
      term: Fixed term, in days.
      annual_rate: Custom annualized interest rate, e.g. `0.02` for 2%.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/fixed/available-inventory)
    """
    params: dict = {
      'currency': currency,
      'term': term,
      'annualRate': annual_rate,
    }
    r = await self.authed_request(
      'GET', '/v5/crypto-loan-fixed/available-inventory', params=params
    )
    return self.result(r, adapter, validate)
