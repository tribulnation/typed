# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/crypto-loan-fixed/fully-repay` — Repay."""

from typing_extensions import NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class FixedRepayRequest(TypedDict):
  """Fixed-term full-repayment parameters."""

  loanId: NotRequired[str]
  """Loan contract ID. Either this or `loanCurrency` must be provided."""
  loanCurrency: NotRequired[str]
  """Loan coin. Either this or `loanId` must be provided."""


class FixedRepayResult(TypedDict):
  """Created repayment."""

  repayId: str
  """Repayment transaction ID."""


adapter = validator[FixedRepayResult](FixedRepayResult)


class Repay(Endpoint):
  """`Repay` — mixed into the router that owns `crypto_loan.fixed.repay`."""

  async def repay(
    self,
    fixed_repay_request: FixedRepayRequest,
    *,
    validate: bool | None = None,
  ) -> FixedRepayResult:
    """Fully repay an active fixed-term crypto loan, identified either by its loan contract or its loan currency.

    Args:
      fixed_repay_request: Loan to repay. Provide `loanId` or `loanCurrency`.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/fixed/repay)
    """
    r = await self.authed_request(
      'POST', '/v5/crypto-loan-fixed/fully-repay', json=fixed_repay_request
    )
    return self.result(r, adapter, validate)
