# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/crypto-loan-fixed/supply` — Create Supply Order."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.types import CryptoLoanTerm
from typed_bybit.core import Endpoint, validator


class FixedSupplyResult(TypedDict):
  """Created supply order."""

  orderId: str
  """Supply order ID."""


class FixedSupplyRequest(TypedDict):
  """Fixed-term supply order parameters."""

  orderCurrency: str
  """Coin to supply."""
  orderAmount: str
  """Amount to supply."""
  annualRate: str
  """Customized annual interest rate requested, for example `0.02` for 2%."""
  term: CryptoLoanTerm
  availableSource: NotRequired[Literal['0', '1', '2']]
  """Account to draw the supplied quantity from. Defaults to Funding."""


adapter = validator[FixedSupplyResult](FixedSupplyResult)


class Supply(Endpoint):
  """`Create Supply Order` — mixed into the router that owns `crypto_loan.fixed.supply`."""

  async def supply(
    self,
    fixed_supply_request: FixedSupplyRequest,
    *,
    validate: bool | None = None,
  ) -> FixedSupplyResult:
    """Supply a coin for fixed-term lending. Locks the supplied quantity for the chosen term, earning the specified annual interest rate once matched.

    Args:
      fixed_supply_request: Fixed-term supply order to place.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/fixed/supply)
    """
    r = await self.authed_request(
      'POST', '/v5/crypto-loan-fixed/supply', json=fixed_supply_request
    )
    return self.result(r, adapter, validate)
