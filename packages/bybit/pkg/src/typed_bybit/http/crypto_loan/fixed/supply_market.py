# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/crypto-loan-fixed/supply-order-quote` — Get Fixed-term Lending Market."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class FixedSupplyMarketQuote(TypedDict):
  """One resting fixed-term borrowing demand entry."""

  orderCurrency: str
  """Coin name."""
  term: Literal[7, 14, 30, 60, 90, 180]
  """Fixed term, in days."""
  annualRate: str
  """Annualized interest rate."""
  qty: str
  """Quantity demanded at this rate and term."""


class FixedSupplyMarketResult(TypedDict):
  """Resting fixed-term borrowing demand, sorted per `orderBy`/`sort`."""

  list: list[FixedSupplyMarketQuote]
  """Resting demand."""


adapter = validator[FixedSupplyMarketResult](FixedSupplyMarketResult)


class SupplyMarket(Endpoint):
  """`Get Fixed-term Lending Market` — mixed into the router that owns `crypto_loan.fixed.supply_market`."""

  async def supply_market(
    self,
    *,
    order_currency: str,
    term: Literal['7', '14', '30', '60', '90', '180'] | None = None,
    order_by: Literal['apy', 'term', 'quantity'],
    sort: Literal[0, 1] | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> FixedSupplyMarketResult:
    """Query the resting fixed-term borrowing demand available to supply against, for one coin.

    Args:
      order_currency: Coin name.
      term: Fixed term, in days. Omit to return every term.
      order_by: Sort field.
      sort: Sort direction.
      limit: Number of records per page. Range [1, 100]; defaults to 10.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/fixed/supply-market)
    """
    params: dict = {
      'orderCurrency': order_currency,
      'orderBy': order_by,
    }
    if term is not None:
      params['term'] = term
    if sort is not None:
      params['sort'] = sort
    if limit is not None:
      params['limit'] = limit
    r = await self.request(
      'GET', '/v5/crypto-loan-fixed/supply-order-quote', params=params
    )
    return self.result(r, adapter, validate)
