# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/crypto-loan-fixed/supply-order-info` — Get Fixed-term Supply Order Info."""

from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class FixedSupplyOrder(TypedDict):
  """One fixed-term supply order."""

  annualRate: str
  """Requested annualized interest rate for this order."""
  orderId: str
  """Supply order ID."""
  orderTime: TimestampMillis
  """Time the order was created."""
  filledQty: str
  """Quantity filled so far."""
  orderQty: str
  """Requested order quantity."""
  orderCurrency: str
  """Supply coin."""
  state: Literal[1, 2, 3, 4]
  """Supply order status."""
  term: Literal[7, 14, 30, 60, 90, 180]
  """Fixed term, in days."""


class FixedSupplyOrderResult(TypedDict):
  """Page of matching supply orders."""

  nextPageCursor: NotRequired[str]
  """Opaque cursor for the next page, per the `cursor` request parameter."""
  list: list[FixedSupplyOrder]
  """Matching supply orders."""


adapter = validator[FixedSupplyOrderResult](FixedSupplyOrderResult)


class SupplyOrder(Endpoint):
  """`Get Fixed-term Supply Order Info` — mixed into the router that owns `crypto_loan.fixed.supply_order`."""

  async def supply_order(
    self,
    *,
    order_id: str | None = None,
    order_currency: str | None = None,
    state: Literal['1', '2', '3', '4'] | None = None,
    term: Literal['7', '14', '30', '60', '90', '180'] | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> FixedSupplyOrderResult:
    """Query this account's fixed-term supply (lending) orders.

    Args:
      order_id: Supply order ID. Omit to return every order.
      order_currency: Supply coin name. Omit to return every coin.
      state: Supply order status. Omit to return every status.
      term: Fixed term, in days. Omit to return every term.
      limit: Number of records per page. Range [1, 100]; defaults to 10.
      cursor: Opaque pagination cursor. Pass the `nextPageCursor` returned by the previous page; omit for the first page.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/fixed/supply-order)
    """
    params = {}
    if order_id is not None:
      params['orderId'] = order_id
    if order_currency is not None:
      params['orderCurrency'] = order_currency
    if state is not None:
      params['state'] = state
    if term is not None:
      params['term'] = term
    if limit is not None:
      params['limit'] = limit
    if cursor is not None:
      params['cursor'] = cursor
    r = await self.authed_request(
      'GET', '/v5/crypto-loan-fixed/supply-order-info', params=params
    )
    return self.result(r, adapter, validate)

  async def supply_order_paged(
    self,
    *,
    order_id: str | None = None,
    order_currency: str | None = None,
    state: Literal['1', '2', '3', '4'] | None = None,
    term: Literal['7', '14', '30', '60', '90', '180'] | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[FixedSupplyOrderResult]:
    """Yield successive pages of `supply_order`.

    Passes each page's token back as `cursor` and stops when a response carries no
    `nextPageCursor`, or after `max_pages` pages when one is given.
    """
    cursor: str | None = None
    pages = 0
    while True:
      response = await self.supply_order(
        order_id=order_id,
        order_currency=order_currency,
        state=state,
        term=term,
        limit=limit,
        cursor=cursor,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      cursor = response.get('nextPageCursor') if response is not None else None
      if not cursor:
        break
