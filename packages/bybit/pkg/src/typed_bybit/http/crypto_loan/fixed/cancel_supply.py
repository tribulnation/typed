# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/crypto-loan-fixed/supply-order-cancel` — Cancel Supply Order."""

from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class FixedCancelSupplyRequest(TypedDict):
  """Cancel-supply-order parameters."""

  orderId: str
  """Supply order ID to cancel."""
  refundedAccount: NotRequired[Literal['0', '1']]
  """Destination account for the refunded supply. Defaults to Funding."""


adapter = validator[dict[str, Any]](dict[str, Any])


class CancelSupply(Endpoint):
  """`Cancel Supply Order` — mixed into the router that owns `crypto_loan.fixed.cancel_supply`."""

  async def cancel_supply(
    self,
    fixed_cancel_supply_request: FixedCancelSupplyRequest,
    *,
    validate: bool | None = None,
  ) -> dict[str, Any]:
    """Cancel an existing, not-yet-fully-filled fixed-term supply order, returning the supplied quantity to the chosen account.

    Args:
      fixed_cancel_supply_request: Order to cancel.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/fixed/cancel-supply)
    """
    r = await self.authed_request(
      'POST',
      '/v5/crypto-loan-fixed/supply-order-cancel',
      json=fixed_cancel_supply_request,
    )
    return self.result(r, adapter, validate)
