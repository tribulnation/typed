# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/crypto-loan-flexible/borrow-history` — Get Borrowing History."""

from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class FlexibleLoanOrder(TypedDict):
  """One flexible loan borrowing event."""

  borrowTime: TimestampMillis
  """Loan creation time, as a millisecond timestamp."""
  initialLoanAmount: str
  """Amount borrowed."""
  loanCurrency: str
  """Borrowed coin."""
  orderId: str
  """Loan order ID."""
  status: Literal[1, 2, 3]
  """Borrow status."""


class FlexibleLoanOrdersResult(TypedDict):
  """Page of borrowing records."""

  nextPageCursor: NotRequired[str]
  """Opaque cursor for the next page. Absent or empty means there are no further pages."""
  list: list[FlexibleLoanOrder]
  """Borrowing records."""


adapter = validator[FlexibleLoanOrdersResult](FlexibleLoanOrdersResult)


class LoanOrders(Endpoint):
  """`Get Borrowing History` — mixed into the router that owns `crypto_loan.flexible.loan_orders`."""

  async def loan_orders(
    self,
    *,
    order_id: str | None = None,
    loan_currency: str | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> FlexibleLoanOrdersResult:
    """Get this account's flexible loan borrowing history.

    Args:
      order_id: Loan order ID.
      loan_currency: Borrowed coin.
      limit: Results per page. Range [1, 100]; defaults to 10.
      cursor: Opaque pagination cursor. Pass the `nextPageCursor` returned by the previous page; omit for the first page.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/flexible/loan-orders)
    """
    params = {}
    if order_id is not None:
      params['orderId'] = order_id
    if loan_currency is not None:
      params['loanCurrency'] = loan_currency
    if limit is not None:
      params['limit'] = limit
    if cursor is not None:
      params['cursor'] = cursor
    r = await self.authed_request(
      'GET', '/v5/crypto-loan-flexible/borrow-history', params=params
    )
    return self.result(r, adapter, validate)

  async def loan_orders_paged(
    self,
    *,
    order_id: str | None = None,
    loan_currency: str | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[FlexibleLoanOrdersResult]:
    """Yield successive pages of `loan_orders`.

    Passes each page's token back as `cursor` and stops when a response carries no
    `nextPageCursor`, or after `max_pages` pages when one is given.
    """
    cursor: str | None = None
    pages = 0
    while True:
      response = await self.loan_orders(
        order_id=order_id,
        loan_currency=loan_currency,
        limit=limit,
        cursor=cursor,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      cursor = response.get('nextPageCursor') if response is not None else None
      if not cursor:
        break
