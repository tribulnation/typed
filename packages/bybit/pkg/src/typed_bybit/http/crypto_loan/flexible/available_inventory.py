# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/crypto-loan-flexible/available-inventory` — Get Available Inventory."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class FlexibleAvailableInventoryResult(TypedDict):
  """Inventory figure for the requested coin."""

  currency: str
  """Coin name."""
  availableInventory: str
  """Quantity currently available to borrow from the lending pool."""
  updateTime: TimestampMillis
  """Time this figure was last updated, as a millisecond timestamp."""


adapter = validator[FlexibleAvailableInventoryResult](FlexibleAvailableInventoryResult)


class AvailableInventory(Endpoint):
  """`Get Available Inventory` — mixed into the router that owns `crypto_loan.flexible.available_inventory`."""

  async def available_inventory(
    self,
    *,
    currency: str,
    validate: bool | None = None,
  ) -> FlexibleAvailableInventoryResult:
    """Get the flexible lending pool's available inventory for a coin.

    Args:
      currency: Coin name, uppercase.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/flexible/available-inventory)
    """
    params: dict = {
      'currency': currency,
    }
    r = await self.authed_request(
      'GET', '/v5/crypto-loan-flexible/available-inventory', params=params
    )
    return self.result(r, adapter, validate)
