# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/crypto-loan-flexible/borrow` — Borrow Flexible Loan."""

from typing_extensions import NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class FlexibleBorrowCollateralItem(TypedDict):
  """One collateral holding to pledge."""

  currency: NotRequired[str]
  """Coin to pledge as collateral."""
  amount: NotRequired[str]
  """Amount to pledge."""


class FlexibleBorrowResult(TypedDict):
  """Identifier of the accepted loan."""

  orderId: str
  """Loan order ID."""


class FlexibleBorrowRequest(TypedDict):
  """Flexible-loan borrow parameters."""

  loanCurrency: str
  """Coin to borrow."""
  loanAmount: str
  """Amount to borrow."""
  collateralList: NotRequired[list[FlexibleBorrowCollateralItem]]
  """Collateral to pledge in the same call, up to 100 coins."""


adapter = validator[FlexibleBorrowResult](FlexibleBorrowResult)


class Borrow(Endpoint):
  """`Borrow Flexible Loan` — mixed into the router that owns `crypto_loan.flexible.borrow`."""

  async def borrow(
    self,
    flexible_borrow_request: FlexibleBorrowRequest,
    *,
    validate: bool | None = None,
  ) -> FlexibleBorrowResult:
    """Borrow a coin under a flexible (open-ended, hourly-interest) crypto loan, optionally pledging collateral in the same call.

    Args:
      flexible_borrow_request: Flexible loan to open.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/flexible/borrow)
    """
    r = await self.authed_request(
      'POST', '/v5/crypto-loan-flexible/borrow', json=flexible_borrow_request
    )
    return self.result(r, adapter, validate)
