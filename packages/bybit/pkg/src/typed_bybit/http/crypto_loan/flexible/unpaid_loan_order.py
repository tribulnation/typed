# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/crypto-loan-flexible/ongoing-coin` — Get Flexible Loans."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class UnpaidLoan(TypedDict):
  """One coin's ongoing flexible loan."""

  hourlyInterestRate: str
  """Current hourly interest rate."""
  loanCurrency: str
  """Borrowed coin."""
  totalDebt: str
  """Unpaid principal and interest, combined."""
  unpaidAmount: str
  """Unpaid principal."""
  unpaidInterest: str
  """Unpaid interest."""


class UnpaidLoanOrderResult(TypedDict):
  """Ongoing flexible loans."""

  list: list[UnpaidLoan]
  """Ongoing loans, one per borrowed coin."""


adapter = validator[UnpaidLoanOrderResult](UnpaidLoanOrderResult)


class UnpaidLoanOrder(Endpoint):
  """`Get Flexible Loans` — mixed into the router that owns `crypto_loan.flexible.unpaid_loan_order`."""

  async def unpaid_loan_order(
    self,
    *,
    loan_currency: str | None = None,
    validate: bool | None = None,
  ) -> UnpaidLoanOrderResult:
    """Get this account's ongoing (unpaid) flexible loans.

    Args:
      loan_currency: Borrowed coin. Omit to return every ongoing loan.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/flexible/unpaid-loan-order)
    """
    params = {}
    if loan_currency is not None:
      params['loanCurrency'] = loan_currency
    r = await self.authed_request(
      'GET', '/v5/crypto-loan-flexible/ongoing-coin', params=params
    )
    return self.result(r, adapter, validate)
