# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/crypto-loan-common/collateral-data` — Get Collateral Coins."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class CollateralRatioTier(TypedDict):
  """One quantity tier's collateral ratio."""

  collateralRatio: str
  """Fraction of holdings counted as collateral value at this tier."""
  maxValue: str
  """Upper bound of this tier's quantity range."""
  minValue: str
  """Lower bound of this tier's quantity range."""


class CurrencyLiquidationOrder(TypedDict):
  """One coin's liquidation priority."""

  currency: str
  """Coin name."""
  liquidationOrder: int
  """Priority in which this coin is liquidated relative to others, lower first."""


class CollateralRatioConfig(TypedDict):
  """One tier table, shared by a set of coins."""

  collateralRatioList: list[CollateralRatioTier]
  """Collateral value ratio, tiered by quantity held."""
  currencies: str
  """Comma-separated coin names sharing this tier table."""


class CollateralCoinResult(TypedDict):
  """Collateral coin configuration."""

  collateralRatioConfigList: list[CollateralRatioConfig]
  """Tiered collateral ratios, grouped by the coins that share one tier table."""
  currencyLiquidationList: list[CurrencyLiquidationOrder]
  """Liquidation priority order of every collateral coin."""


adapter = validator[CollateralCoinResult](CollateralCoinResult)


class CollateralCoin(Endpoint):
  """`Get Collateral Coins` — mixed into the router that owns `crypto_loan.collateral_coin`."""

  async def __call__(
    self,
    *,
    currency: str | None = None,
    validate: bool | None = None,
  ) -> CollateralCoinResult:
    """Get the coins eligible as loan collateral, their tiered collateral ratios and liquidation priority order.

    Args:
      currency: Coin name, uppercase. Omit to return every collateral coin.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/collateral-coin)
    """
    params = {}
    if currency is not None:
      params['currency'] = currency
    r = await self.request(
      'GET', '/v5/crypto-loan-common/collateral-data', params=params
    )
    return self.result(r, adapter, validate)
