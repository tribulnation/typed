# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/crypto-loan-common/max-collateral-amount` — Get Max. Allowed Collateral Reduction Amount."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class MaxCollateralReductionResult(TypedDict):
  """Maximum collateral reduction calculation result."""

  maxCollateralAmount: str
  """Maximum quantity of this collateral coin that could be redeemed without breaching the required loan-to-value ratio."""


adapter = validator[MaxCollateralReductionResult](MaxCollateralReductionResult)


class ReduceMaxCollateralAmt(Endpoint):
  """`Get Max. Allowed Collateral Reduction Amount` — mixed into the router that owns `crypto_loan.reduce_max_collateral_amt`."""

  async def __call__(
    self,
    *,
    currency: str,
    validate: bool | None = None,
  ) -> MaxCollateralReductionResult:
    """Calculate the maximum quantity of a collateral coin that could currently be redeemed while keeping the account's loan-to-value ratio within its required bound. Read-only; does not move any collateral.

    Args:
      currency: Collateral coin, for example `BTC`.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/reduce-max-collateral-amt)
    """
    params: dict = {
      'currency': currency,
    }
    r = await self.authed_request(
      'GET', '/v5/crypto-loan-common/max-collateral-amount', params=params
    )
    return self.result(r, adapter, validate)
