# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/affiliate/aff-user-list` — Get Affiliate User List."""

from typing_extensions import AsyncIterator, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class AffiliateUser(TypedDict):
  """One affiliate client user."""

  userId: str
  """Client's user ID."""
  registerTime: str
  """Registration date."""
  source: str
  """Registration source or referrer."""
  remarks: str
  """Free-text notes on the client."""
  isKyc: bool
  """Whether the client has completed KYC."""
  takerVol30Day: NotRequired[str]
  """Taker trading volume over the last 30 days, in USDT. Present when `need30` is true."""
  makerVol30Day: NotRequired[str]
  """Maker trading volume over the last 30 days, in USDT. Present when `need30` is true."""
  tradeVol30Day: NotRequired[str]
  """Total trading volume over the last 30 days, in USDT. Present when `need30` is true."""
  depositAmount30Day: NotRequired[str]
  """Deposits over the last 30 days, in USDT. Present when `needDeposit` is true."""
  takerVol365Day: NotRequired[str]
  """Lifetime taker trading volume, in USDT. Present when `need365` is true."""
  makerVol365Day: NotRequired[str]
  """Lifetime maker trading volume, in USDT. Present when `need365` is true."""
  tradeVol365Day: NotRequired[str]
  """Lifetime total trading volume, in USDT. Present when `need365` is true."""
  depositAmount365Day: NotRequired[str]
  """Lifetime total deposits, in USDT. Present when `needDeposit` is true."""
  takerVol: NotRequired[str]
  """Taker trading volume over `startDate`-`endDate`, in USDT."""
  makerVol: NotRequired[str]
  """Maker trading volume over `startDate`-`endDate`, in USDT."""
  tradeVol: NotRequired[str]
  """Total trading volume over `startDate`-`endDate`, in USDT."""
  startDate: NotRequired[str]
  """Echo of the queried period's start date."""
  endDate: NotRequired[str]
  """Echo of the queried period's end date."""
  tradfiTradeVol: NotRequired[str]
  """TradFi trading volume over `startDate`-`endDate`, in USDT."""
  tradfiTradeVol30Day: NotRequired[str]
  """TradFi trading volume over the last 30 days, in USDT."""
  tradfiTradeVol365Day: NotRequired[str]
  """Lifetime TradFi trading volume, in USDT."""
  commissions30Day: NotRequired[dict[str, str]]
  """Commission earned from this client over the last 30 days, keyed by coin."""
  commissionsVol: NotRequired[dict[str, str]]
  """Commission earned from this client over `startDate`-`endDate`, keyed by coin."""
  commissions365Day: NotRequired[dict[str, str]]
  """Lifetime commission earned from this client, keyed by coin."""


class AffiliateUserListResult(TypedDict):
  """Page of affiliate client users."""

  nextPageCursor: str
  """Opaque cursor for the next page. Pass it back as `cursor`; an empty string means there are no further pages."""
  list: list[AffiliateUser]
  """Client users on this page."""


adapter = validator[AffiliateUserListResult](AffiliateUserListResult)


class UserList(Endpoint):
  """`Get Affiliate User List` — mixed into the router that owns `affiliate.user_list`."""

  async def user_list(
    self,
    *,
    size: int | None = None,
    cursor: str | None = None,
    need_deposit: bool | None = None,
    need30: bool | None = None,
    need365: bool | None = None,
    start_date: str | None = None,
    end_date: str | None = None,
    validate: bool | None = None,
  ) -> AffiliateUserListResult:
    """Get a paged list of an affiliate's client users, with optional deposit and 30/365-day trading metrics. Requires an affiliate account, called with an API key carrying only the Affiliate permission, from the master UID.

    Args:
      size: Records per page. Range [0, 100]; defaults to 0.
      cursor: Opaque pagination cursor. Pass the `nextPageCursor` returned by the previous page; omit it for the first page.
      need_deposit: Whether to include 30-day and lifetime deposit figures. Defaults to false.
      need30: Whether to include 30-day trading-volume figures. Defaults to false.
      need365: Whether to include lifetime (365-day) trading-volume figures. Defaults to false.
      start_date: Start of a custom reporting period, as `YYYY-MM-DD`.
      end_date: End of a custom reporting period, as `YYYY-MM-DD`.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/affiliate/affiliate-user-list)
    """
    params = {}
    if size is not None:
      params['size'] = size
    if cursor is not None:
      params['cursor'] = cursor
    if need_deposit is not None:
      params['needDeposit'] = need_deposit
    if need30 is not None:
      params['need30'] = need30
    if need365 is not None:
      params['need365'] = need365
    if start_date is not None:
      params['startDate'] = start_date
    if end_date is not None:
      params['endDate'] = end_date
    r = await self.authed_request('GET', '/v5/affiliate/aff-user-list', params=params)
    return self.result(r, adapter, validate)

  async def user_list_paged(
    self,
    *,
    size: int | None = None,
    need_deposit: bool | None = None,
    need30: bool | None = None,
    need365: bool | None = None,
    start_date: str | None = None,
    end_date: str | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[AffiliateUserListResult]:
    """Yield successive pages of `user_list`.

    Passes each page's token back as `cursor` and stops when a response carries no
    `nextPageCursor`, or after `max_pages` pages when one is given.
    """
    cursor: str | None = None
    pages = 0
    while True:
      response = await self.user_list(
        size=size,
        need_deposit=need_deposit,
        need30=need30,
        need365=need365,
        start_date=start_date,
        end_date=end_date,
        cursor=cursor,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      cursor = response.get('nextPageCursor') if response is not None else None
      if not cursor:
        break
