# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/spread/tickers` — Get Tickers."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class SpreadTicker(TypedDict):
  """Rolling 24-hour statistics and best quote for a spread combination."""

  symbol: str
  """Spread combination symbol name."""
  bidPrice: str
  """Best bid price. Empty string when there is no bid."""
  bidSize: str
  """Size resting at the best bid. Empty string when there is no bid."""
  askPrice: str
  """Best ask price. Empty string when there is no ask."""
  askSize: str
  """Size resting at the best ask. Empty string when there is no ask."""
  lastPrice: str
  """Last traded price."""
  highPrice24h: str
  """Highest traded price in the last 24 hours."""
  lowPrice24h: str
  """Lowest traded price in the last 24 hours."""
  prevPrice24h: str
  """Traded price 24 hours ago."""
  volume24h: str
  """Volume over the last 24 hours."""


class SpreadTickersResult(TypedDict):
  """Ticker snapshot for one spread combination symbol."""

  list: list[SpreadTicker]
  """Matching ticker; always one entry."""


adapter = validator[SpreadTickersResult](SpreadTickersResult)


class Tickers(Endpoint):
  """`Get Tickers` — mixed into the router that owns `spread.market.tickers`."""

  async def tickers(
    self,
    *,
    symbol: str,
    validate: bool | None = None,
  ) -> SpreadTickersResult:
    """Get the latest price snapshot and rolling 24-hour statistics for one spread combination symbol.

    Args:
      symbol: Spread combination symbol name.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/spread/market/tickers)
    """
    params: dict = {
      'symbol': symbol,
    }
    r = await self.request('GET', '/v5/spread/tickers', params=params)
    return self.result(r, adapter, validate)
