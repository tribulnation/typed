# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/spread/recent-trade` — Get Recent Public Trades."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class SpreadPublicTrade(TypedDict):
  """One executed public trade on a spread combination."""

  execId: str
  """Execution ID."""
  symbol: str
  """Spread combination symbol name."""
  price: str
  """Trade price."""
  size: str
  """Trade size."""
  side: Literal['Buy', 'Sell']
  """Side of the taker."""
  time: TimestampMillis
  """Trade time, as a millisecond timestamp."""
  seq: str
  """Cross sequence number."""


class SpreadRecentTradesResult(TypedDict):
  """Recent public trades for one spread combination symbol."""

  list: list[SpreadPublicTrade]
  """Recent trades, newest first."""


adapter = validator[SpreadRecentTradesResult](SpreadRecentTradesResult)


class RecentTrade(Endpoint):
  """`Get Recent Public Trades` — mixed into the router that owns `spread.market.recent_trade`."""

  async def recent_trade(
    self,
    *,
    symbol: str,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> SpreadRecentTradesResult:
    """Get recently executed public trades on one spread combination symbol.

    Args:
      symbol: Spread combination symbol name.
      limit: Number of trades to return. Range [1, 1000]; defaults to 500.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/spread/market/recent-trade)
    """
    params: dict = {
      'symbol': symbol,
    }
    if limit is not None:
      params['limit'] = limit
    r = await self.request('GET', '/v5/spread/recent-trade', params=params)
    return self.result(r, adapter, validate)
