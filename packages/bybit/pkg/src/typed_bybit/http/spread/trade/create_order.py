# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/spread/order/create` — Create Order."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class SpreadCreateLimitOrderRequest(TypedDict):
  """Limit spread order parameters."""

  symbol: str
  """Spread combination symbol name."""
  side: Literal['Buy', 'Sell']
  """Order side."""
  orderType: Literal['Limit']
  """Order type."""
  qty: str
  """Order quantity."""
  price: str
  """Order price. Required for limit orders."""
  orderLinkId: NotRequired[str]
  """User-customised order ID, max 45 characters (alphanumeric, dashes and underscores)."""
  timeInForce: NotRequired[Literal['GTC', 'IOC', 'FOK', 'PostOnly']]
  """Time in force."""


class SpreadCreateMarketOrderRequest(TypedDict):
  """Market spread order parameters. Executes as an IOC order internally; `price` plays no role."""

  symbol: str
  """Spread combination symbol name."""
  side: Literal['Buy', 'Sell']
  """Order side."""
  orderType: Literal['Market']
  """Order type."""
  qty: str
  """Order quantity."""
  orderLinkId: NotRequired[str]
  """User-customised order ID, max 45 characters (alphanumeric, dashes and underscores)."""
  timeInForce: NotRequired[Literal['GTC', 'IOC', 'FOK', 'PostOnly']]
  """Time in force."""


class SpreadCreateOrderResult(TypedDict):
  """Identifiers of the placed spread order."""

  orderId: str
  """Spread combination order ID."""
  orderLinkId: str
  """User-customised order ID."""


adapter = validator[SpreadCreateOrderResult](SpreadCreateOrderResult)


class CreateOrder(Endpoint):
  """`Create Order` — mixed into the router that owns `spread.trade.create_order`."""

  async def create_order(
    self,
    spread_create_market_order_request_spread_create_limit_order_request: SpreadCreateMarketOrderRequest
    | SpreadCreateLimitOrderRequest,
    *,
    validate: bool | None = None,
  ) -> SpreadCreateOrderResult:
    """Place a spread combination order. `orderType='Market'` executes as an IOC order internally. The acknowledgement means the request was accepted, not that the order is live -- this is asynchronous; use the private WebSocket order stream to confirm final status.

    Args:
      spread_create_market_order_request_spread_create_limit_order_request: Spread order to place.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/spread/trade/create-order)
    """
    r = await self.authed_request(
      'POST',
      '/v5/spread/order/create',
      json=spread_create_market_order_request_spread_create_limit_order_request,
    )
    return self.result(r, adapter, validate)
