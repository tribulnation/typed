# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/spread/order/cancel` — Cancel Order."""

from typing_extensions import NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class SpreadCancelOrderRequest(TypedDict):
  """Spread order cancellation parameters."""

  orderId: NotRequired[str]
  """Spread combination order ID. Either `orderId` or `orderLinkId` is required."""
  orderLinkId: NotRequired[str]
  """User-customised order ID. Either `orderId` or `orderLinkId` is required."""


class SpreadCancelOrderResult(TypedDict):
  """Identifiers of the cancelled spread order."""

  orderId: str
  """Spread combination order ID."""
  orderLinkId: str
  """User-customised order ID."""


adapter = validator[SpreadCancelOrderResult](SpreadCancelOrderResult)


class CancelOrder(Endpoint):
  """`Cancel Order` — mixed into the router that owns `spread.trade.cancel_order`."""

  async def cancel_order(
    self,
    spread_cancel_order_request: SpreadCancelOrderRequest,
    *,
    validate: bool | None = None,
  ) -> SpreadCancelOrderResult:
    """Cancel one open spread order, by `orderId` or `orderLinkId`.

    Args:
      spread_cancel_order_request: Order to cancel, by `orderId` or `orderLinkId`.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/spread/trade/cancel-order)
    """
    r = await self.authed_request(
      'POST', '/v5/spread/order/cancel', json=spread_cancel_order_request
    )
    return self.result(r, adapter, validate)
