# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/spread/max-qty` — Get Max Order Quantity."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class SpreadMaxQtyResult(TypedDict):
  """Maximum order quantity for one spread order."""

  ab: str
  """Maximum order quantity."""


adapter = validator[SpreadMaxQtyResult](SpreadMaxQtyResult)


class MaxQty(Endpoint):
  """`Get Max Order Quantity` — mixed into the router that owns `spread.trade.max_qty`."""

  async def max_qty(
    self,
    *,
    symbol: str,
    side: Literal['1', '2'],
    order_price: str,
    validate: bool | None = None,
  ) -> SpreadMaxQtyResult:
    """Get the maximum quantity this account could place for a spread order at a given price and side, given available balance and risk limits.

    Args:
      symbol: Spread combination symbol name.
      side: Order side.
      order_price: Order price.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/spread/trade/max-qty)
    """
    params: dict = {
      'symbol': symbol,
      'side': side,
      'orderPrice': order_price,
    }
    r = await self.authed_request('GET', '/v5/spread/max-qty', params=params)
    return self.result(r, adapter, validate)
