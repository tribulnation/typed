# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/strategy/stop` — Stop Strategy."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class StopStrategyRequest(TypedDict):
  """Strategy stop parameters."""

  strategyId: str
  """ID of the strategy to stop."""


class StopStrategyResult(TypedDict):
  """Result of stopping a strategy."""

  strategyId: str
  """ID of the stopped strategy."""


adapter = validator[StopStrategyResult](StopStrategyResult)


class StopStrategy(Endpoint):
  """`Stop Strategy` — mixed into the router that owns `strategy.stop_strategy`."""

  async def stop_strategy(
    self,
    stop_strategy_request: StopStrategyRequest,
    *,
    validate: bool | None = None,
  ) -> StopStrategyResult:
    """Stop a running strategy. Once stopped, the strategy cannot be resumed. Strategy status changes to Terminated; unfilled orders (and the unfilled remainder of partially filled orders) are automatically canceled. Already-filled orders are not affected.

    Args:
      stop_strategy_request: Strategy to stop.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/strategy/stop-strategy)
    """
    r = await self.authed_request(
      'POST', '/v5/strategy/stop', json=stop_strategy_request
    )
    return self.result(r, adapter, validate)
