# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/ins-loan/association-uid` — Bind Or Unbind UID."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class BindUidRequest(TypedDict):
  """UID and operation to perform."""

  uid: str
  """UID to bind or unbind."""
  operate: Literal['0', '1']
  """Operation to perform."""


class BindUidResult(TypedDict):
  """Echo of the operation performed."""

  uid: str
  """UID that was bound or unbound."""
  operate: Literal['0', '1']
  """Operation performed."""


adapter = validator[BindUidResult](BindUidResult)


class BindUid(Endpoint):
  """`Bind Or Unbind UID` — mixed into the router that owns `institutional_loan.bind_uid`."""

  async def bind_uid(
    self,
    bind_uid_request: BindUidRequest,
    *,
    validate: bool | None = None,
  ) -> BindUidResult:
    """Bind a new UID to this account's institutional loan risk unit, or unbind one from it. Binding requires the calling API key's UID to already be in the risk unit and the target UID to hold no institutional loan of its own; unbinding requires the same and additionally refuses unbinding the caller's own UID or the risk unit's own designated UID.

    Args:
      bind_uid_request: UID to bind or unbind.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/otc/bind-uid)
    """
    r = await self.authed_request(
      'POST', '/v5/ins-loan/association-uid', json=bind_uid_request
    )
    return self.result(r, adapter, validate)
