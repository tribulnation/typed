# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/ins-loan/loan-order` — Get Loan Orders."""

from typed_bybit.core import Endpoint, TimestampMillis, timestamp_millis, validator
from typing_extensions import Literal, TypedDict


class SymbolLeverageUsDCContractLeverageItem(TypedDict):
  """Leverage limit for one symbol."""

  symbol: str
  """Symbol name."""
  leverage: str
  """Leverage limit."""


class SymbolLeverageUsDTPerpetualLeverageItem(TypedDict):
  """Leverage limit for one symbol."""

  symbol: str
  """Symbol name."""
  leverage: str
  """Leverage limit."""


class LoanOrder(TypedDict):
  """One institutional loan order."""

  orderId: str
  """Loan order identifier."""
  orderProductId: str
  """Associated loan product ID."""
  parentUid: str
  """Designated risk-unit UID this loan is bound to."""
  loanTime: TimestampMillis
  """When the loan was initiated."""
  loanCoin: str
  """Coin borrowed."""
  loanAmount: str
  """Principal amount borrowed."""
  unpaidAmount: str
  """Outstanding principal balance."""
  unpaidInterest: str
  """Outstanding interest owed."""
  repaidAmount: str
  """Principal repaid to date."""
  repaidInterest: str
  """Interest repaid to date."""
  interestRate: str
  """Daily interest rate applied."""
  status: Literal['1', '2']
  """Loan status."""
  leverage: str
  """Maximum leverage available for this loan product."""
  supportSpot: Literal['0', '1']
  """Whether spot trading is enabled."""
  supportContract: Literal['0', '1']
  """Whether USDT Perpetual trading is enabled."""
  marginLeverage: str
  """Maximum leverage for spot margin trading."""
  contractLeverage: str
  """Default leverage for USDT Perpetual."""
  transferRatio: str
  """Ratio for transferring loan funds from the spot wallet to the contract wallet."""
  supportUSDCContract: Literal['0', '1']
  """Whether USDC contract trading is enabled."""
  supportUSDCOptions: Literal['0', '1']
  """Whether USDC options trading is enabled."""
  supportMarginTrading: Literal['0', '1']
  """Whether spot margin trading is enabled."""
  withdrawLine: str
  """LTV threshold restricting withdrawals."""
  transferLine: str
  """LTV threshold restricting transfers."""
  spotBuyLine: str
  """LTV threshold restricting spot buys."""
  spotSellLine: str
  """LTV threshold restricting spot sells."""
  contractOpenLine: str
  """LTV threshold restricting opening USDT Perpetual positions."""
  liquidationLine: str
  """LTV threshold at which liquidation triggers."""
  stopLiquidationLine: str
  """LTV threshold at which liquidation stops."""
  deferredLiquidationLine: str
  """LTV threshold for deferred liquidation."""
  deferredLiquidationTime: str
  """Deferral window for deferred liquidation."""
  spotSymbols: list[str]
  """Whitelisted spot trading pairs; empty when unrestricted."""
  contractSymbols: list[str]
  """Whitelisted USDT Perpetual trading pairs; empty when unsupported or unrestricted."""
  USDCContractSymbols: list[str]
  """Whitelisted USDC contract trading pairs."""
  USDCOptionsSymbols: list[str]
  """Whitelisted USDC options symbols."""
  USDTPerpetualLeverage: list[SymbolLeverageUsDTPerpetualLeverageItem]
  """Per-symbol leverage limits for USDT Perpetual."""
  USDCContractLeverage: list[SymbolLeverageUsDCContractLeverageItem]
  """Per-symbol leverage limits for USDC contracts."""


class LoanInfoResult(TypedDict):
  """Matching loan orders."""

  loanInfo: list[LoanOrder]
  """Matching loan orders, newest first."""


adapter = validator[LoanInfoResult](LoanInfoResult)


class LoanInfo(Endpoint):
  """`Get Loan Orders` — mixed into the router that owns `institutional_loan.loan_info`."""

  async def loan_info(
    self,
    *,
    order_id: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> LoanInfoResult:
    """Get up to 2 years of historical institutional loan order data, sorted by loan time descending.

    Args:
      order_id: Specific loan order ID. Omit to return every order, sorted by loan time descending.
      start_time: Start of the queried range, as a millisecond timestamp.
      end_time: End of the queried range, as a millisecond timestamp.
      limit: Data size limit. Range [1, 100]; defaults to 10.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/otc/loan-info)
    """
    params = {}
    if order_id is not None:
      params['orderId'] = order_id
    if start_time is not None:
      params['startTime'] = timestamp_millis.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp_millis.dump(end_time)
    if limit is not None:
      params['limit'] = limit
    r = await self.authed_request('GET', '/v5/ins-loan/loan-order', params=params)
    return self.result(r, adapter, validate)
