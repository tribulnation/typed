# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/ins-loan/repay-loan` — Repay."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class RepayRequest(TypedDict):
  """Coin and amount to repay."""

  token: str
  """Coin to repay, for example USDT."""
  quantity: str
  """Quantity to repay."""


class RepayResult(TypedDict):
  """Acceptance of the repayment request."""

  repayOrderStatus: Literal['P']
  """Status of the repayment request."""


adapter = validator[RepayResult](RepayResult)


class Repay(Endpoint):
  """`Repay` — mixed into the router that owns `institutional_loan.repay`."""

  async def repay(
    self,
    repay_request: RepayRequest,
    *,
    validate: bool | None = None,
  ) -> RepayResult:
    """Repay principal on an institutional loan. Repayment is processed asynchronously, usually within 2-3 minutes; the amount is deducted from the Unified wallet and excludes interest charges. Callable only by the risk unit's designated UID -- contact the Risk Manager beforehand; reserved for urgent use.

    Args:
      repay_request: Coin and amount to repay.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/otc/repay)
    """
    r = await self.authed_request('POST', '/v5/ins-loan/repay-loan', json=repay_request)
    return self.result(r, adapter, validate)
