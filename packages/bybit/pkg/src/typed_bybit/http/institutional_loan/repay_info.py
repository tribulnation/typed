# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/ins-loan/repaid-history` — Get Repayment Orders."""

from typed_bybit.core import Endpoint, TimestampMillis, timestamp_millis, validator
from typing_extensions import Literal, TypedDict


class RepaymentOrder(TypedDict):
  """One loan repayment order."""

  repayOrderId: str
  """Repayment order identifier."""
  repaidTime: TimestampMillis
  """When the repayment was made."""
  token: str
  """Coin repaid."""
  quantity: str
  """Principal amount repaid."""
  interest: str
  """Interest amount repaid."""
  businessType: Literal['1', '2']
  """Kind of repayment. `1`: normal repayment. `2`: liquidation."""
  status: Literal['1', '2']
  """Result of the repayment. `1`: success. `2`: fail."""


class RepayInfoResult(TypedDict):
  """Matching repayment orders."""

  repayInfo: list[RepaymentOrder]
  """Matching repayment orders."""


adapter = validator[RepayInfoResult](RepayInfoResult)


class RepayInfo(Endpoint):
  """`Get Repayment Orders` — mixed into the router that owns `institutional_loan.repay_info`."""

  async def repay_info(
    self,
    *,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> RepayInfoResult:
    """Get up to 2 years of historical loan repayment orders -- orders that repaid a loan.

    Args:
      start_time: Start of the queried range, as a millisecond timestamp.
      end_time: End of the queried range, as a millisecond timestamp.
      limit: Data size limit. Range [1, 100]; defaults to 100.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/otc/repay-info)
    """
    params = {}
    if start_time is not None:
      params['startTime'] = timestamp_millis.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp_millis.dump(end_time)
    if limit is not None:
      params['limit'] = limit
    r = await self.authed_request('GET', '/v5/ins-loan/repaid-history', params=params)
    return self.result(r, adapter, validate)
