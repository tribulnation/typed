# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/ins-loan/coin-delta-amount` — Get Coin Delta Amount."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class CoinDeltaAmount(TypedDict):
  """Delta amount detail for one coin."""

  coin: str
  """Coin name."""
  coinDeltaSize: str
  """Coin delta size (quantity)."""
  coinDeltaAvailableAmount: str
  """Coin delta available amount, in USD."""
  coinDeltaAmount: str
  """Coin delta total amount limit, in USD."""


class CoinDeltaAmountResult(TypedDict):
  """Risk unit and per-coin delta amount detail."""

  riskUnitDeltaAmount: str
  """Risk unit total delta amount limit, in USD."""
  riskUnitDeltaAvailableAmount: str
  """Risk unit available delta amount, in USD."""
  riskUnitDelta: str
  """Risk unit delta value."""
  list: list[CoinDeltaAmount]
  """Per-coin delta amount detail."""


adapter = validator[CoinDeltaAmountResult](CoinDeltaAmountResult)


class CoinDeltaAmountEndpoint(Endpoint):
  """`Get Coin Delta Amount` — mixed into the router that owns `institutional_loan.coin_delta_amount`."""

  async def coin_delta_amount(
    self,
    *,
    coin: str | None = None,
    validate: bool | None = None,
  ) -> CoinDeltaAmountResult:
    """Get coin delta amount details for an institutional loan hedge product. Unified account only.

    Args:
      coin: Coin name, uppercase, for example BTC. Omit to return every coin.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/otc/coin-delta-amount)
    """
    params = {}
    if coin is not None:
      params['coin'] = coin
    r = await self.authed_request(
      'GET', '/v5/ins-loan/coin-delta-amount', params=params
    )
    return self.result(r, adapter, validate)
