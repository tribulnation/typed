# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/card/reward/point/cashback/detail` — Query Cashback Detail."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class PointCashbackDetail(TypedDict):
  """Cashback detail for one card reward order."""

  points: str
  """Point amount."""
  amt: str
  """Cashback amount."""
  ccy: str
  """Currency code."""
  ccyType: Literal['FIAT', 'CRYPTO']
  """Currency type."""
  createTime: NotRequired[str]
  """Creation time."""
  bizTxnId: str
  """Order ID."""
  sourceId: int
  """Coupon/voucher ID."""
  sourceCode: str
  """External order ID."""
  orderStatus: int
  """Order status."""
  orderSubStatus: int
  """Order sub-status."""
  orderShowStatus: Literal[
    'NO_PAY', 'ORDER_PENDING_SHOW', 'ORDER_SUCCESS', 'ORDER_FAIL'
  ]
  """Display status."""
  failedBizCode: NotRequired[str]
  """Failure reason code."""


class PointCashbackDetailRequest(TypedDict):
  """Cashback order lookup."""

  bizTxnId: str
  """Order ID."""


adapter = validator[PointCashbackDetail](PointCashbackDetail)


class CashbackDetail(Endpoint):
  """`Query Cashback Detail` — mixed into the router that owns `card.point.cashback_detail`."""

  async def cashback_detail(
    self,
    point_cashback_detail_request: PointCashbackDetailRequest,
    *,
    validate: bool | None = None,
  ) -> PointCashbackDetail:
    """Query the cashback detail for a specific card reward order, including point amount, cashback value, currency and order status.

    Args:
      point_cashback_detail_request: Order to look up.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/bybit-card/point/cashback-detail)
    """
    r = await self.authed_request(
      'POST',
      '/v5/card/reward/point/cashback/detail',
      json=point_cashback_detail_request,
    )
    return self.result(r, adapter, validate)
