# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/card/reward/points/balance` — Query Point Balance."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class PointBalance(TypedDict):
  """Card reward point balance and account status."""

  accountId: str
  """Account ID."""
  availablePoint: str
  """Points currently available for use."""
  pendingPoint: str
  """Points that are pending or frozen."""
  status: str
  """Account status."""
  updateTime: TimestampMillis
  """Last update time, as a millisecond timestamp."""
  settlementPeriod: int
  """Settlement period."""


adapter = validator[PointBalance](PointBalance)


class Balance(Endpoint):
  """`Query Point Balance` — mixed into the router that owns `card.point.balance`."""

  async def balance(self, *, validate: bool | None = None) -> PointBalance:
    """Query the user's card reward point balance, including available points, pending (frozen) points, account status and related information.

    Args:
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/bybit-card/point/balance)
    """
    r = await self.authed_request('POST', '/v5/card/reward/points/balance')
    return self.result(r, adapter, validate)
