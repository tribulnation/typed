# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/fcombobot/close` — Close Futures Combo Bot."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class CloseFuturesComboBotRequest(TypedDict):
  """Futures combo bot close parameters."""

  bot_id: str
  """Bot ID to close, obtained from the Create Futures Combo Bot response."""


class CloseFuturesComboBotResult(TypedDict):
  """Result of closing a futures combo bot."""

  status_code: int
  """`0` on success, non-zero on error."""
  debug_msg: str
  """Debug message, populated on testnet."""


adapter = validator[CloseFuturesComboBotResult](CloseFuturesComboBotResult)


class Close(Endpoint):
  """`Close Futures Combo Bot` — mixed into the router that owns `bot.futures_combo.close`."""

  async def close(
    self,
    close_futures_combo_bot_request: CloseFuturesComboBotRequest,
    *,
    validate: bool | None = None,
  ) -> CloseFuturesComboBotResult:
    """Close a running futures combo trading bot. The bot cancels all pending orders and closes all positions across the portfolio. Only bots in a running state can be closed.

    Args:
      close_futures_combo_bot_request: Futures combo bot to close.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/bot/futures-combo/close)
    """
    r = await self.authed_request(
      'POST', '/v5/fcombobot/close', json=close_futures_combo_bot_request
    )
    return self.result(r, adapter, validate)
