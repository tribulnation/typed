# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/fmartingalebot/close` — Close Futures Martingale Bot."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class CloseFuturesMartingaleBotRequest(TypedDict):
  """Futures Martingale bot close parameters."""

  bot_id: str
  """Bot ID to close, obtained from the Create Futures Martingale Bot response."""


class CloseFuturesMartingaleBotResult(TypedDict):
  """Result of closing a futures Martingale bot."""

  status_code: int
  """`0` on success, non-zero on error."""
  debug_msg: str
  """Debug message, populated on testnet."""


adapter = validator[CloseFuturesMartingaleBotResult](CloseFuturesMartingaleBotResult)


class Close(Endpoint):
  """`Close Futures Martingale Bot` — mixed into the router that owns `bot.futures_martingale.close`."""

  async def close(
    self,
    close_futures_martingale_bot_request: CloseFuturesMartingaleBotRequest,
    *,
    validate: bool | None = None,
  ) -> CloseFuturesMartingaleBotResult:
    """Close a running futures Martingale trading bot. The bot cancels all pending orders and closes the position. Only bots in a running state can be closed.

    Args:
      close_futures_martingale_bot_request: Futures Martingale bot to close.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/bot/futures-martingale/close)
    """
    r = await self.authed_request(
      'POST', '/v5/fmartingalebot/close', json=close_futures_martingale_bot_request
    )
    return self.result(r, adapter, validate)
