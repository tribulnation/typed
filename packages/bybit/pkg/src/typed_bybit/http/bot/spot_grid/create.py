# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/grid/create-grid` — Create Spot Grid Bot."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class CreateSpotGridBotRequest(TypedDict):
  """Spot grid bot creation parameters."""

  symbol: str
  """Trading pair symbol, uppercase only, for example `BTCUSDT`."""
  max_price: str
  """Upper bound of the grid price range, as a decimal string."""
  min_price: str
  """Lower bound of the grid price range, as a decimal string."""
  cell_number: int
  """Number of grid intervals, minimum 2."""
  invest_mode: NotRequired[Literal[0, 1, 2]]
  """Investment mode. Defaults to `0` (quote only) when omitted."""
  base_investment: NotRequired[str]
  """Investment in the base token, as a decimal string. Required when `invest_mode` is `1` or `2`."""
  quote_investment: NotRequired[str]
  """Investment in the quote token, as a decimal string. Required when `invest_mode` is `0` or `2`."""
  entry_price: NotRequired[str]
  """Entry trigger price, as a decimal string. The bot activates when the market price reaches this level."""
  stop_loss_price: NotRequired[str]
  """Stop-loss trigger price, as a decimal string."""
  take_profit_price: NotRequired[str]
  """Take-profit trigger price, as a decimal string."""
  ts_percent: NotRequired[str]
  """Trailing stop callback ratio, range [0, 0.99], for example `"0.05"` means 5%."""
  enable_trailing: NotRequired[bool]
  """Enable grid trailing (auto-shift). Requires `cell_number >= 5`. Defaults to `false` when omitted."""
  limit_up_price: NotRequired[str]
  """Upper limit price for grid trailing, as a decimal string. Used when `enable_trailing` is `true`."""


class CreateSpotGridBotResult(TypedDict):
  """Result of creating a spot grid bot."""

  status_code: int
  """`200` on success, `421` if the user is banned."""
  grid_id: int
  """Created grid bot ID. Use for subsequent queries and close operations."""
  ban_reason_text: str
  """Ban reason in the user's locale. Populated only when `status_code` is `421`."""
  debug_msg: str
  """Debug message, populated on testnet."""


adapter = validator[CreateSpotGridBotResult](CreateSpotGridBotResult)


class Create(Endpoint):
  """`Create Spot Grid Bot` — mixed into the router that owns `bot.spot_grid.create`."""

  async def create(
    self,
    create_spot_grid_bot_request: CreateSpotGridBotRequest,
    *,
    validate: bool | None = None,
  ) -> CreateSpotGridBotResult:
    """Create a new spot grid trading bot with a specified trading pair, price range, grid count and investment amount. Subject to compliance wall and KYC verification.

    Args:
      create_spot_grid_bot_request: Spot grid bot configuration.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/bot/spot-grid/create)
    """
    r = await self.authed_request(
      'POST', '/v5/grid/create-grid', json=create_spot_grid_bot_request
    )
    return self.result(r, adapter, validate)
