# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/user/agreement` — Sign Agreement."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class SignAgreementRequest(TypedDict):
  """Agreement selection and consent."""

  category: NotRequired[Literal[2, 3]]
  """Legacy product selector, superseded by `categoryV2`."""
  categoryV2: NotRequired[Literal[1, 2]]
  """Product selector. Either this or `category` is required."""
  agree: bool
  """Must be `true` to consent to the agreement."""


class SignAgreementResult(TypedDict):
  """Empty on success; no fields."""


adapter = validator[SignAgreementResult](SignAgreementResult)


class SignAgreement(Endpoint):
  """`Sign Agreement` — mixed into the router that owns `user.sign_agreement`."""

  async def sign_agreement(
    self,
    sign_agreement_request: SignAgreementRequest,
    *,
    validate: bool | None = None,
  ) -> SignAgreementResult:
    """Sign the commodity contract trading agreement (metals or crude oil) for the master account, granting the whole account tree trading eligibility for that product.

    Args:
      sign_agreement_request: Agreement to sign.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/user/sign-agreement)
    """
    r = await self.authed_request(
      'POST', '/v5/user/agreement', json=sign_agreement_request
    )
    return self.result(r, adapter, validate)
