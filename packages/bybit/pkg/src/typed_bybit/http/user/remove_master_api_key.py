# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/user/delete-api` — Delete Master API Key."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class RemoveMasterApiKeyResult(TypedDict):
  """Empty on success; no fields."""


adapter = validator[RemoveMasterApiKeyResult](RemoveMasterApiKeyResult)


class RemoveMasterApiKey(Endpoint):
  """`Delete Master API Key` — mixed into the router that owns `user.remove_master_api_key`."""

  async def remove_master_api_key(
    self,
    *,
    validate: bool | None = None,
  ) -> RemoveMasterApiKeyResult:
    """Delete the calling master account's own API key. Must be called using the key being deleted; the key becomes invalid immediately upon deletion. Requires at least one of 'Account Transfer', 'Subaccount Transfer' or 'Withdrawal' permissions.

    Args:
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/user/rm-master-apikey)
    """
    r = await self.authed_request('POST', '/v5/user/delete-api')
    return self.result(r, adapter, validate)
