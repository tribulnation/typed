# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/user/del-submember` — Delete Sub UID."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class RemoveSubUidRequest(TypedDict):
  """Sub UID removal parameters."""

  subMemberId: str
  """Sub UID to remove."""


class RemoveSubUidResult(TypedDict):
  """Empty on success; no fields."""


adapter = validator[RemoveSubUidResult](RemoveSubUidResult)


class RemoveSubUid(Endpoint):
  """`Delete Sub UID` — mixed into the router that owns `user.remove_sub_uid`."""

  async def remove_sub_uid(
    self,
    remove_sub_uid_request: RemoveSubUidRequest,
    *,
    validate: bool | None = None,
  ) -> RemoveSubUidResult:
    """Permanently remove a sub UID from the master account. The venue refuses the call while the sub-account's asset balance exceeds 0.001 USDT.

    Args:
      remove_sub_uid_request: Sub UID to remove.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/user/rm-subuid)
    """
    r = await self.authed_request(
      'POST', '/v5/user/del-submember', json=remove_sub_uid_request
    )
    return self.result(r, adapter, validate)
