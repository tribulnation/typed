# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/user/frozen-sub-member` — Freeze Sub UID."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class FreezeSubUidRequest(TypedDict):
  """Freeze/unfreeze parameters."""

  subuid: int
  """Sub-account user ID."""
  frozen: Literal[0, 1]
  """Target frozen state."""


class FreezeSubUidResult(TypedDict):
  """Empty on success; no fields."""


adapter = validator[FreezeSubUidResult](FreezeSubUidResult)


class FreezeSubUid(Endpoint):
  """`Freeze Sub UID` — mixed into the router that owns `user.freeze_sub_uid`."""

  async def freeze_sub_uid(
    self,
    freeze_sub_uid_request: FreezeSubUidRequest,
    *,
    validate: bool | None = None,
  ) -> FreezeSubUidResult:
    """Freeze or unfreeze a sub-account. Requires the master account's own API key with at least one of 'Account Transfer', 'Subaccount Transfer' or 'Withdrawal' permissions.

    Args:
      freeze_sub_uid_request: Sub-account to freeze or unfreeze.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/user/froze-subuid)
    """
    r = await self.authed_request(
      'POST', '/v5/user/frozen-sub-member', json=freeze_sub_uid_request
    )
    return self.result(r, adapter, validate)
