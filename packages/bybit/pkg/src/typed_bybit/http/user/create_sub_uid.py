# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/user/create-sub-member` — Create Sub UID."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class CreateSubUidRequest(TypedDict):
  """Sub-account creation parameters."""

  username: str
  """6-16 characters, must include both numbers and letters. Cannot match an existing or previously-deleted username."""
  password: NotRequired[str]
  """8-30 characters, must include numbers, upper- and lower-case letters."""
  memberType: Literal[1, 6]
  """Sub-account kind."""
  switch: NotRequired[Literal[0, 1]]
  """Quick-login setting."""
  isUta: NotRequired[bool]
  """Deprecated; every sub-account is a UTA account regardless of this value."""
  note: NotRequired[str]
  """Remark to attach to the new sub-account."""


class CreateSubUidResult(TypedDict):
  """The newly-created sub-account."""

  uid: str
  """Sub-account user ID."""
  username: str
  """Username of the newly-created sub-account."""
  memberType: Literal[1, 6]
  """Sub-account kind."""
  status: Literal[1, 2, 4]
  """Account status."""
  remark: str
  """Remark attached to the account."""


adapter = validator[CreateSubUidResult](CreateSubUidResult)


class CreateSubUid(Endpoint):
  """`Create Sub UID` — mixed into the router that owns `user.create_sub_uid`."""

  async def create_sub_uid(
    self,
    create_sub_uid_request: CreateSubUidRequest,
    *,
    validate: bool | None = None,
  ) -> CreateSubUidResult:
    """Create a new sub user ID. Use the master account's own API key, holding at least one of 'Account Transfer', 'Subaccount Transfer' or 'Withdrawal' permissions.

    Args:
      create_sub_uid_request: Sub-account to create.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/user/create-subuid)
    """
    r = await self.authed_request(
      'POST', '/v5/user/create-sub-member', json=create_sub_uid_request
    )
    return self.result(r, adapter, validate)
