# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/user/delete-sub-api` — Delete Sub UID API Key."""

from typing_extensions import NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class RemoveSubApiKeyRequest(TypedDict):
  """Sub-account API key deletion parameters."""

  apikey: NotRequired[str]
  """The sub-account API key to delete. Required when called with the master account's key; must be omitted when called with the sub-account's own key."""


class RemoveSubApiKeyResult(TypedDict):
  """Empty on success; no fields."""


adapter = validator[RemoveSubApiKeyResult](RemoveSubApiKeyResult)


class RemoveSubApiKey(Endpoint):
  """`Delete Sub UID API Key` — mixed into the router that owns `user.remove_sub_api_key`."""

  async def remove_sub_api_key(
    self,
    remove_sub_api_key_request: RemoveSubApiKeyRequest,
    *,
    validate: bool | None = None,
  ) -> RemoveSubApiKeyResult:
    """Delete a sub-account's API key. Callable with either the sub-account's own key, or the master account's key managing it. The key becomes invalid immediately.

    Args:
      remove_sub_api_key_request: Sub-account API key to delete, when called by the master account.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/user/rm-sub-apikey)
    """
    r = await self.authed_request(
      'POST', '/v5/user/delete-sub-api', json=remove_sub_api_key_request
    )
    return self.result(r, adapter, validate)
