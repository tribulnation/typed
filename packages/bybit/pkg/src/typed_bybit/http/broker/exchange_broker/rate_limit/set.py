# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/broker/apilimit/set` — Set Rate Limit."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class SetRateLimitItem(TypedDict):
  """A rate limit to apply to a group of UIDs, for one business type."""

  uids: str
  """Comma-separated UIDs to configure, for example `"uid1,uid2,uid3"`."""
  bizType: Literal['SPOT', 'OPTIONS', 'DERIVATIVES']
  """Business type this limit applies to."""
  rate: int
  """API rate limit to apply, requests per second."""


class SetRateLimitOutcome(TypedDict):
  """The outcome of setting one rate limit item."""

  uids: str
  """Comma-separated UIDs this outcome applies to."""
  bizType: Literal['SPOT', 'OPTIONS', 'DERIVATIVES']
  """Business type this outcome applies to."""
  rate: int
  """The rate limit that was requested, requests per second."""
  success: bool
  """Whether this item was applied."""
  msg: str
  """Human-readable outcome message."""


class SetRateLimitRequest(TypedDict):
  """Rate limits to configure."""

  list: list[SetRateLimitItem]
  """One entry per group of UIDs to configure."""


class SetRateLimitResult(TypedDict):
  """Per-item outcome of the requested rate limit changes. Note the doubly-nested `result` -- see this endpoint's own `notes`."""

  result: list[SetRateLimitOutcome]
  """One outcome per requested item, in request order."""


adapter = validator[SetRateLimitResult](SetRateLimitResult)


class Set(Endpoint):
  """`Set Rate Limit` — mixed into the router that owns `broker.exchange_broker.rate_limit.set`."""

  async def set(
    self,
    set_rate_limit_request: SetRateLimitRequest,
    *,
    validate: bool | None = None,
  ) -> SetRateLimitResult:
    """Configure custom API rate limits for one or more UIDs. A master UID may only set limits for its own sub-accounts, never for itself; a sub-account may only set its own limit. Only Exchange Broker accounts can call this endpoint.

    Args:
      set_rate_limit_request: Rate limits to configure.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/broker/exchange-broker/rate-limit/set)
    """
    r = await self.authed_request(
      'POST', '/v5/broker/apilimit/set', json=set_rate_limit_request
    )
    return self.result(r, adapter, validate)
