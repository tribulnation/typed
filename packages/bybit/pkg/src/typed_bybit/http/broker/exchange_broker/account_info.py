# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/broker/account-info` — Get Exchange Broker Info."""

from typing_extensions import NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class BaseFeeRebateRate(TypedDict):
  """Base fee rebate rate per product line."""

  spot: NotRequired[str]
  """Base fee rebate rate for spot trading, as a percentage string."""
  derivatives: NotRequired[str]
  """Base fee rebate rate for derivatives trading, as a percentage string."""


class MarkupFeeRebateRate(TypedDict):
  """Markup fee rebate rate per product line."""

  spot: NotRequired[str]
  """Markup fee rebate rate for spot trading, as a percentage string."""
  derivatives: NotRequired[str]
  """Markup fee rebate rate for derivatives trading, as a percentage string."""
  convert: NotRequired[str]
  """Markup fee rebate rate for convert, as a percentage string."""


class BrokerAccountInfo(TypedDict):
  """Exchange Broker master account info."""

  subAcctQty: str
  """Number of sub-accounts currently created."""
  maxSubAcctQty: str
  """Maximum number of sub-accounts allowed."""
  baseFeeRebateRate: BaseFeeRebateRate
  markupFeeRebateRate: MarkupFeeRebateRate
  ts: TimestampMillis
  """System timestamp, in milliseconds."""


adapter = validator[BrokerAccountInfo](BrokerAccountInfo)


class AccountInfo(Endpoint):
  """`Get Exchange Broker Info` — mixed into the router that owns `broker.exchange_broker.account_info`."""

  async def __call__(self, *, validate: bool | None = None) -> BrokerAccountInfo:
    """Get the Exchange Broker master account's sub-account capacity and fee rebate rates. Only Exchange Broker main UIDs can call this endpoint.

    Args:
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/broker/exchange-broker/account-info)
    """
    r = await self.authed_request('GET', '/v5/broker/account-info')
    return self.result(r, adapter, validate)
