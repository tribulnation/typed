# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/broker/award/distribute-award` — Issue Voucher."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class IssueVoucherRequest(TypedDict):
  """Voucher to issue."""

  accountId: str
  """User ID to receive the voucher."""
  awardId: str
  """Voucher spec ID to issue."""
  specCode: str
  """Caller-chosen unique spec code, up to 8 characters."""
  amount: str
  """Amount to issue. Up to 16 decimals for spot airdrops, up to 4 decimals for other award types."""
  brokerId: str
  """This broker's unique identifier."""


adapter = validator[None](None)


class IssueVoucher(Endpoint):
  """`Issue Voucher` — mixed into the router that owns `broker.reward.issue_voucher`."""

  async def issue_voucher(
    self,
    issue_voucher_request: IssueVoucherRequest,
    *,
    validate: bool | None = None,
  ) -> None:
    """Issue a voucher to a user account. `amount` supports up to 16 decimals for spot airdrops, up to 4 decimals for other award types.

    Args:
      issue_voucher_request: Voucher to issue.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/broker/reward/issue-voucher)
    """
    r = await self.authed_request(
      'POST', '/v5/broker/award/distribute-award', json=issue_voucher_request
    )
    return self.result(r, adapter, validate)
