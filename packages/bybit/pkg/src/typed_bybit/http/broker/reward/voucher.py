# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/broker/award/info` — Get Voucher Spec."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class BrokerVoucherSpec(TypedDict):
  """A voucher's specification."""

  id: str
  """Voucher ID."""
  coin: str
  """Voucher coin."""
  amountUnit: Literal['AWARD_AMOUNT_UNIT_USD', 'AWARD_AMOUNT_UNIT_COIN']
  """Unit `totalAmount`/`usedAmount` are denominated in."""
  productLine: str
  """Product line the voucher applies to."""
  subProductLine: str
  """Product sub-line the voucher applies to."""
  totalAmount: str
  """Total voucher value."""
  usedAmount: str
  """Amount of the voucher already redeemed."""


class VoucherSpecRequest(TypedDict):
  """Voucher to look up."""

  id: str
  """Voucher ID."""


adapter = validator[BrokerVoucherSpec](BrokerVoucherSpec)


class Voucher(Endpoint):
  """`Get Voucher Spec` — mixed into the router that owns `broker.reward.voucher`."""

  async def voucher(
    self,
    voucher_spec_request: VoucherSpecRequest,
    *,
    validate: bool | None = None,
  ) -> BrokerVoucherSpec:
    """Get specification details for a voucher: coin, amount unit, product line and total/used amount.

    Args:
      voucher_spec_request: Voucher to look up.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/broker/reward/voucher)
    """
    r = await self.authed_request(
      'POST', '/v5/broker/award/info', json=voucher_spec_request
    )
    return self.result(r, adapter, validate)
