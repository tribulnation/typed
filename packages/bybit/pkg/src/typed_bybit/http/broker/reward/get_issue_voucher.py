# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/broker/award/distribution-record` — Get Issued Voucher."""

from typing_extensions import NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampSeconds, validator


class GetIssuedVoucherRequest(TypedDict):
  """Issued voucher to look up."""

  accountId: str
  """User ID the voucher was issued to."""
  awardId: str
  """Voucher spec ID."""
  specCode: str
  """Spec code the voucher was issued under."""
  withUsedAmount: NotRequired[bool]
  """Whether to include the amount already consumed. Defaults to `false`."""


class IssuedVoucherRecord(TypedDict):
  """An issued voucher's record."""

  accountId: str
  """User ID the voucher was issued to."""
  awardId: str
  """Voucher spec ID."""
  specCode: str
  """Spec code the voucher was issued under."""
  amount: str
  """Voucher value."""
  isClaimed: bool
  """Whether the user has claimed the voucher."""
  startAt: TimestampSeconds
  """Start of the claim window, Unix seconds."""
  endAt: TimestampSeconds
  """End of the claim window, Unix seconds."""
  effectiveAt: TimestampSeconds
  """When the voucher becomes usable after being claimed, Unix seconds."""
  ineffectiveAt: TimestampSeconds
  """When the voucher expires after being claimed, Unix seconds."""
  usedAmount: NotRequired[str]
  """Amount already consumed. Only present when `withUsedAmount` was passed."""


adapter = validator[IssuedVoucherRecord](IssuedVoucherRecord)


class GetIssueVoucher(Endpoint):
  """`Get Issued Voucher` — mixed into the router that owns `broker.reward.get_issue_voucher`."""

  async def get_issue_voucher(
    self,
    get_issued_voucher_request: GetIssuedVoucherRequest,
    *,
    validate: bool | None = None,
  ) -> IssuedVoucherRecord:
    """Get the issuance record of a voucher previously distributed to a user account: claim status, validity window and (optionally) how much has been used.

    Args:
      get_issued_voucher_request: Issued voucher to look up.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/broker/reward/get-issue-voucher)
    """
    r = await self.authed_request(
      'POST', '/v5/broker/award/distribution-record', json=get_issued_voucher_request
    )
    return self.result(r, adapter, validate)
