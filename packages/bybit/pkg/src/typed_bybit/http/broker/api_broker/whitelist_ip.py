# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/broker/whitelist/ip` — Get Broker Whitelist IP."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class BrokerWhitelistEntry(TypedDict):
  """Whitelisted IPs and dedicated endpoint domain for one registered client."""

  name: str
  """Client name."""
  endpointDomain: str
  """Dedicated hostname assigned exclusively to this client."""
  ipList: list[str]
  """Whitelisted IP addresses for this client."""


adapter = validator[list[BrokerWhitelistEntry]](list[BrokerWhitelistEntry])


class WhitelistIp(Endpoint):
  """`Get Broker Whitelist IP` — mixed into the router that owns `broker.api_broker.whitelist_ip`."""

  async def whitelist_ip(
    self,
    *,
    validate: bool | None = None,
  ) -> list[BrokerWhitelistEntry]:
    """Get the whitelisted IP addresses and dedicated endpoint domain assigned to each of this broker's registered clients. Must be called from a pre-whitelisted IP address.

    Args:
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/broker/api-broker/whitelist-ip)
    """
    r = await self.request('GET', '/v5/broker/whitelist/ip')
    return self.result(r, adapter, validate)
