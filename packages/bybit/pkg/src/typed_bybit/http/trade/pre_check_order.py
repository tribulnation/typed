# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/order/pre-check` — Pre Check Order."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class PreCheckLimitOrderRequest(TypedDict):
  """Limit order parameters to evaluate."""

  category: Literal['linear', 'inverse', 'option']
  """Product type. Spot is not supported by this endpoint."""
  symbol: str
  """Symbol name, uppercase only."""
  side: Literal['Buy', 'Sell']
  """Order side."""
  orderType: Literal['Limit']
  """Order type."""
  qty: str
  """Order quantity."""
  price: str
  """Order price. Required for limit orders."""
  orderIv: NotRequired[str]
  """Implied volatility, option only. Pass the real value, e.g. `0.1` for 10%; takes priority over `price` when both are passed."""
  timeInForce: NotRequired[Literal['GTC', 'IOC', 'FOK', 'PostOnly', 'RPI']]
  """Time in force. Market orders always use `IOC`; defaults to `GTC` if not passed."""
  positionIdx: NotRequired[Literal[0, 1, 2]]
  """Position mode index; required under hedge mode."""
  orderLinkId: NotRequired[str]
  """User-customised order ID, max 36 characters."""
  takeProfit: NotRequired[str]
  """Take profit price."""
  stopLoss: NotRequired[str]
  """Stop loss price."""
  tpTriggerBy: NotRequired[Literal['LastPrice', 'IndexPrice', 'MarkPrice']]
  """Trigger price type for take profit."""
  slTriggerBy: NotRequired[Literal['LastPrice', 'IndexPrice', 'MarkPrice']]
  """Trigger price type for stop loss."""
  reduceOnly: NotRequired[bool]
  """Position can only reduce in size if this order triggers."""
  closeOnTrigger: NotRequired[bool]
  """Closing order that can only reduce position, never increase it."""
  smpType: NotRequired[Literal['None', 'CancelMaker', 'CancelTaker', 'CancelBoth']]
  """Self-match prevention execution type."""
  mmp: NotRequired[bool]
  """Market maker protection order, option only."""
  tpslMode: NotRequired[Literal['Full', 'Partial']]
  """TP/SL mode. `Full`: entire position. `Partial`: partial position, limit TP/SL supported."""
  tpLimitPrice: NotRequired[str]
  """Limit price when take profit triggers, for `tpslMode=Partial` and `tpOrderType=Limit`."""
  slLimitPrice: NotRequired[str]
  """Limit price when stop loss triggers, for `tpslMode=Partial` and `slOrderType=Limit`."""
  tpOrderType: NotRequired[Literal['Market', 'Limit']]
  """Order type when take profit triggers. `Market` (default) or `Limit`; `Full` mode only supports `Market`."""
  slOrderType: NotRequired[Literal['Market', 'Limit']]
  """Order type when stop loss triggers, mirroring `tpOrderType`."""


class PreCheckMarketOrderRequest(TypedDict):
  """Market order parameters to evaluate; `price` plays no role."""

  category: Literal['linear', 'inverse', 'option']
  """Product type. Spot is not supported by this endpoint."""
  symbol: str
  """Symbol name, uppercase only."""
  side: Literal['Buy', 'Sell']
  """Order side."""
  orderType: Literal['Market']
  """Order type."""
  qty: str
  """Order quantity."""
  orderIv: NotRequired[str]
  """Implied volatility, option only. Pass the real value, e.g. `0.1` for 10%; takes priority over `price` when both are passed."""
  timeInForce: NotRequired[Literal['GTC', 'IOC', 'FOK', 'PostOnly', 'RPI']]
  """Time in force. Market orders always use `IOC`; defaults to `GTC` if not passed."""
  positionIdx: NotRequired[Literal[0, 1, 2]]
  """Position mode index; required under hedge mode."""
  orderLinkId: NotRequired[str]
  """User-customised order ID, max 36 characters."""
  takeProfit: NotRequired[str]
  """Take profit price."""
  stopLoss: NotRequired[str]
  """Stop loss price."""
  tpTriggerBy: NotRequired[Literal['LastPrice', 'IndexPrice', 'MarkPrice']]
  """Trigger price type for take profit."""
  slTriggerBy: NotRequired[Literal['LastPrice', 'IndexPrice', 'MarkPrice']]
  """Trigger price type for stop loss."""
  reduceOnly: NotRequired[bool]
  """Position can only reduce in size if this order triggers."""
  closeOnTrigger: NotRequired[bool]
  """Closing order that can only reduce position, never increase it."""
  smpType: NotRequired[Literal['None', 'CancelMaker', 'CancelTaker', 'CancelBoth']]
  """Self-match prevention execution type."""
  mmp: NotRequired[bool]
  """Market maker protection order, option only."""
  tpslMode: NotRequired[Literal['Full', 'Partial']]
  """TP/SL mode. `Full`: entire position. `Partial`: partial position, limit TP/SL supported."""
  tpLimitPrice: NotRequired[str]
  """Limit price when take profit triggers, for `tpslMode=Partial` and `tpOrderType=Limit`."""
  slLimitPrice: NotRequired[str]
  """Limit price when stop loss triggers, for `tpslMode=Partial` and `slOrderType=Limit`."""
  tpOrderType: NotRequired[Literal['Market', 'Limit']]
  """Order type when take profit triggers. `Market` (default) or `Limit`; `Full` mode only supports `Market`."""
  slOrderType: NotRequired[Literal['Market', 'Limit']]
  """Order type when stop loss triggers, mirroring `tpOrderType`."""


class PreCheckOrderResult(TypedDict):
  """Initial and maintenance margin rate before and after the checked order."""

  orderId: str
  """Order ID."""
  orderLinkId: str
  """User-customised order ID."""
  preImrE4: int
  """Initial margin rate before the check, x1e4 (e.g. `30` means IMR = 30/1e4 = 0.30%)."""
  preMmrE4: int
  """Maintenance margin rate before the check, x1e4."""
  postImrE4: int
  """Initial margin rate after the check, x1e4."""
  postMmrE4: int
  """Maintenance margin rate after the check, x1e4."""


adapter = validator[PreCheckOrderResult](PreCheckOrderResult)


class PreCheckOrder(Endpoint):
  """`Pre Check Order` — mixed into the router that owns `trade.pre_check_order`."""

  async def pre_check_order(
    self,
    pre_check_market_order_request_pre_check_limit_order_request: PreCheckMarketOrderRequest
    | PreCheckLimitOrderRequest,
    *,
    validate: bool | None = None,
  ) -> PreCheckOrderResult:
    """Calculate the change in a UTA account's initial and maintenance margin rate before and after placing an order, without placing it. Only Cross Margin and Portfolio Margin modes are supported; isolated margin and conditional orders are not. `inverse` is not supported in Cross Margin mode.

    Args:
      pre_check_market_order_request_pre_check_limit_order_request: Order to check, in the same shape as Place Order, restricted to derivatives and options.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/order/pre-check-order)
    """
    r = await self.authed_request(
      'POST',
      '/v5/order/pre-check',
      json=pre_check_market_order_request_pre_check_limit_order_request,
    )
    return self.result(r, adapter, validate)
