# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/order/history` — Get Order History."""

from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, timestamp_millis, validator
from typed_bybit.types import (
  CancelType,
  CreateType,
  OrderStatus,
  RejectReason,
  StopOrderType,
)


class HistoricalOrder(TypedDict):
  """One historical order."""

  orderId: str
  """Order ID."""
  orderLinkId: str
  """User-customised order ID."""
  parentOrderLinkId: NotRequired[str]
  """Linked parent order for an attached take-profit/stop-loss order. Futures & options only."""
  blockTradeId: str
  """Block trade ID."""
  symbol: str
  """Symbol name."""
  price: str
  """Order price."""
  qty: str
  """Order quantity."""
  side: Literal['Buy', 'Sell']
  """Side."""
  isLeverage: str
  """Whether the order borrows. `"0"`: false, `"1"`: true."""
  positionIdx: Literal[0, 1, 2]
  """Position index for hedge-mode positions."""
  orderStatus: OrderStatus
  createType: NotRequired[CreateType]
  cancelType: CancelType
  rejectReason: RejectReason
  avgPrice: str
  """Average filled price. Empty string for orders without an average price."""
  leavesQty: str
  """Remaining quantity not executed."""
  leavesValue: str
  """Estimated value not executed."""
  cumExecQty: str
  """Cumulative executed quantity."""
  cumExecValue: str
  """Cumulative executed value."""
  cumExecFee: str
  """Cumulative executed trading fee, inverse & option only. Deprecated for linear & spot; use `cumFeeDetail` instead."""
  timeInForce: Literal['GTC', 'IOC', 'FOK', 'PostOnly', 'RPI']
  """Time in force."""
  orderType: Literal[
    'Market', 'Limit', 'UNKNOWN', 'Block trade Roll Back', 'Block trade-Limit'
  ]
  """Order type. For TP/SL orders, the type after the order was triggered. `UNKNOWN` is response-only. Block trade Roll Back / Block trade-Limit are unique to Unified account block trades."""
  stopOrderType: StopOrderType
  orderIv: str
  """Implied volatility."""
  marketUnit: NotRequired[Literal['baseCoin', 'quoteCoin']]
  """Unit for `qty` on a spot market order."""
  slippageToleranceType: NotRequired[Literal['TickSize', 'Percent', 'UNKNOWN']]
  """Slippage tolerance type for a spot or futures market order."""
  slippageTolerance: NotRequired[str]
  """Slippage tolerance value."""
  triggerPrice: str
  """Trigger price. If `stopOrderType=TrailingStop`, this is the activation price; otherwise the trigger price."""
  takeProfit: str
  """Take profit price."""
  stopLoss: str
  """Stop loss price."""
  tpslMode: Literal['Full', 'Partial', '']
  """TP/SL mode. Absent for spot; always `""` for option."""
  ocoTriggerBy: NotRequired[
    Literal['OcoTriggerByUnknown', 'OcoTriggerByTp', 'OcoTriggerByBySl']
  ]
  """Trigger type of a spot OCO order."""
  tpLimitPrice: str
  """Limit order price when take profit is triggered."""
  slLimitPrice: str
  """Limit order price when stop loss is triggered."""
  tpTriggerBy: Literal['LastPrice', 'IndexPrice', 'MarkPrice', 'UNKNOWN']
  """Trigger price type for take profit."""
  slTriggerBy: Literal['LastPrice', 'IndexPrice', 'MarkPrice', 'UNKNOWN']
  """Trigger price type for stop loss."""
  triggerDirection: Literal[0, 1, 2]
  """Direction that triggers the conditional order. `0`: not a conditional order."""
  triggerBy: Literal['LastPrice', 'IndexPrice', 'MarkPrice', 'UNKNOWN']
  """Trigger price type."""
  lastPriceOnCreated: str
  """Last price when the order was placed. Not applicable to spot."""
  basePrice: NotRequired[str]
  """Last price when the order was placed. Spot only."""
  reduceOnly: bool
  """Whether the order can only reduce position size."""
  closeOnTrigger: bool
  """Whether the order is a close-on-trigger order."""
  placeType: Literal['iv', 'price', '']
  """Option-specific placement type."""
  smpType: Literal['None', 'CancelMaker', 'CancelTaker', 'CancelBoth']
  """Self-match prevention execution type."""
  smpGroup: str
  """SMP group ID. `"0"` when the UID has no group."""
  smpOrderId: str
  """Counterparty order ID that triggered this SMP execution."""
  createdTime: TimestampMillis
  """Order creation time, as a millisecond timestamp."""
  updatedTime: TimestampMillis
  """Order last-update time, as a millisecond timestamp."""
  extraFees: NotRequired[str]
  """Trading fee rate information. Returned only for spot orders placed on the Indonesian site or spot fiat-currency orders on the EU site; empty string otherwise."""
  cumFeeDetail: NotRequired[dict[str, str]]
  """Cumulative trading fee per currency, for linear & spot (replaces `cumExecFee`)."""
  rpiTakerAccess: NotRequired[bool]
  """Whether the order matched with an RPI order as the counterparty."""
  rpiMatchedQty: NotRequired[str]
  """Cumulative quantity matched against RPI orders as the counterparty."""


class OrderHistoryResult(TypedDict):
  """Page of matching historical orders."""

  category: Literal['linear', 'inverse', 'spot', 'option']
  """Product type."""
  nextPageCursor: NotRequired[str]
  """Opaque cursor for the next page. Pass it back as `cursor`; an empty string means there are no further pages."""
  list: list[HistoricalOrder]
  """Matching orders, newest first."""


adapter = validator[OrderHistoryResult](OrderHistoryResult)


class OrderHistory(Endpoint):
  """`Get Order History` — mixed into the router that owns `trade.order_history`."""

  async def order_history(
    self,
    *,
    category: Literal['linear', 'inverse', 'spot', 'option'],
    symbol: str | None = None,
    base_coin: str | None = None,
    settle_coin: str | None = None,
    order_id: str | None = None,
    order_link_id: str | None = None,
    order_filter: Literal[
      'Order', 'StopOrder', 'tpslOrder', 'OcoOrder', 'BidirectionalTpslOrder'
    ]
    | None = None,
    order_status: OrderStatus | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> OrderHistoryResult:
    """Query historical orders. Creation/cancellation is asynchronous, so results may lag real time; for real-time state use `trade.open_orders` or the private WebSocket order stream. Retention: last 7 days returns every closed status except `Cancelled`/`Rejected`/`Deactivated`; last 24 hours also returns those three; beyond 7 days only orders that had at least one fill.

    Args:
      category: Product type.
      symbol: Symbol name, uppercase only.
      base_coin: Base coin, uppercase only.
      settle_coin: Settle coin, uppercase only.
      order_id: Order ID.
      order_link_id: User-customised order ID.
      order_filter: Order kinds to return. All kinds by default.
      order_status: Filter by order status.
      start_time: Start of the queried range, as a millisecond timestamp. Neither passed: last 7 days. Only `startTime`: `[startTime, startTime+7d]`. Only `endTime`: `[endTime-7d, endTime]`. Both: `endTime - startTime <= 7 days`.
      end_time: End of the queried range, as a millisecond timestamp.
      limit: Number of orders per page. Range [1, 50]; defaults to 20.
      cursor: Opaque pagination cursor. Pass the `nextPageCursor` returned by the previous page; omit it for the first page.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/order/order-list)
    """
    params: dict = {
      'category': category,
    }
    if symbol is not None:
      params['symbol'] = symbol
    if base_coin is not None:
      params['baseCoin'] = base_coin
    if settle_coin is not None:
      params['settleCoin'] = settle_coin
    if order_id is not None:
      params['orderId'] = order_id
    if order_link_id is not None:
      params['orderLinkId'] = order_link_id
    if order_filter is not None:
      params['orderFilter'] = order_filter
    if order_status is not None:
      params['orderStatus'] = order_status
    if start_time is not None:
      params['startTime'] = timestamp_millis.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp_millis.dump(end_time)
    if limit is not None:
      params['limit'] = limit
    if cursor is not None:
      params['cursor'] = cursor
    r = await self.authed_request('GET', '/v5/order/history', params=params)
    return self.result(r, adapter, validate)

  async def order_history_paged(
    self,
    *,
    category: Literal['linear', 'inverse', 'spot', 'option'],
    symbol: str | None = None,
    base_coin: str | None = None,
    settle_coin: str | None = None,
    order_id: str | None = None,
    order_link_id: str | None = None,
    order_filter: Literal[
      'Order', 'StopOrder', 'tpslOrder', 'OcoOrder', 'BidirectionalTpslOrder'
    ]
    | None = None,
    order_status: OrderStatus | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[OrderHistoryResult]:
    """Yield successive pages of `order_history`.

    Passes each page's token back as `cursor` and stops when a response carries no
    `nextPageCursor`, or after `max_pages` pages when one is given.
    """
    cursor: str | None = None
    pages = 0
    while True:
      response = await self.order_history(
        category=category,
        symbol=symbol,
        base_coin=base_coin,
        settle_coin=settle_coin,
        order_id=order_id,
        order_link_id=order_link_id,
        order_filter=order_filter,
        order_status=order_status,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        cursor=cursor,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      cursor = response.get('nextPageCursor') if response is not None else None
      if not cursor:
        break
