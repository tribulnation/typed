# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/order/create` — Place Order."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class CreateLimitOrderRequest(TypedDict):
  """Limit order parameters."""

  category: Literal['linear', 'inverse', 'spot', 'option']
  """Product type."""
  symbol: str
  """Symbol name, uppercase only."""
  isLeverage: NotRequired[Literal[0, 1]]
  """Spot only. `0` (default): spot trading. `1`: margin trading (margin trading must be enabled, with the currency set as collateral)."""
  side: Literal['Buy', 'Sell']
  """Order side."""
  orderType: Literal['Limit']
  """Order type."""
  qty: str
  """Order quantity. Spot market buy orders default to order-by-value; `marketUnit` picks value vs quantity. Perps/Futures/Option always order by quantity. Perps & Futures: passing `qty="0"` with `reduceOnly=true` and `closeOnTrigger=true` closes the position up to `maxMktOrderQty`/`maxOrderQty`."""
  marketUnit: NotRequired[Literal['baseCoin', 'quoteCoin']]
  """Unit for `qty` when creating a spot market order."""
  rpiTakerAccess: NotRequired[bool]
  """Whether the order is eligible to match against RPI quotes."""
  price: str
  """Order price. Required for limit orders. If a position exists, must be better than the liquidation price."""
  triggerDirection: NotRequired[Literal[1, 2]]
  """Expected direction of a conditional order. `1`: triggers when price rises to `triggerPrice`. `2`: triggers when price falls to `triggerPrice`. Valid for linear & inverse."""
  orderFilter: NotRequired[Literal['Order', 'tpslOrder', 'StopOrder']]
  """Spot only. Defaults to `Order`."""
  triggerPrice: NotRequired[str]
  """For Perps & Futures, the conditional order trigger price. For spot, the TP/SL and conditional order trigger price."""
  triggerBy: NotRequired[Literal['LastPrice', 'IndexPrice', 'MarkPrice']]
  """Conditional order trigger price type. Valid for linear & inverse."""
  orderIv: NotRequired[str]
  """Implied volatility, option only. Pass the real value, e.g. `0.1` for 10%; takes priority over `price` when both are passed."""
  timeInForce: NotRequired[Literal['GTC', 'IOC', 'FOK', 'PostOnly', 'RPI']]
  """Time in force. Market orders always use `IOC`; defaults to `GTC` if not passed. `PostOnly` cancels the order if it would fill immediately. `RPI` (Retail Price Improvement) is post-only, restricted to assigned market makers, and matches only Web/App order flow."""
  positionIdx: NotRequired[Literal[0, 1, 2]]
  """Position mode index; required under hedge mode."""
  orderLinkId: NotRequired[str]
  """User-customised order ID, max 36 characters. Required and always-unique for options; optional and always-unique otherwise."""
  takeProfit: NotRequired[str]
  """Take profit price."""
  stopLoss: NotRequired[str]
  """Stop loss price."""
  tpTriggerBy: NotRequired[Literal['LastPrice', 'IndexPrice', 'MarkPrice']]
  """Trigger price type for take profit. Valid for linear & inverse."""
  slTriggerBy: NotRequired[Literal['LastPrice', 'IndexPrice', 'MarkPrice']]
  """Trigger price type for stop loss. Valid for linear & inverse."""
  reduceOnly: NotRequired[bool]
  """Position can only reduce in size if this order triggers. Cannot be combined with take profit/stop loss. Valid for linear, inverse & option."""
  closeOnTrigger: NotRequired[bool]
  """Closing order that can only reduce position, never increase it. Valid for linear & inverse."""
  smpType: NotRequired[Literal['None', 'CancelMaker', 'CancelTaker', 'CancelBoth']]
  """Self-match prevention execution type."""
  mmp: NotRequired[bool]
  """Market maker protection order, option only."""
  tpslMode: NotRequired[Literal['Full', 'Partial']]
  """TP/SL mode. `Full`: entire position, `tpOrderType`/`slOrderType` must be `Market`. `Partial`: partial position, limit TP/SL supported. Valid for linear & inverse."""
  tpLimitPrice: NotRequired[str]
  """Limit price when take profit triggers. linear & inverse: only with `tpslMode=Partial` and `tpOrderType=Limit`. Spot: required when `takeProfit` and `tpOrderType=Limit` are set."""
  slLimitPrice: NotRequired[str]
  """Limit price when stop loss triggers. linear & inverse: only with `tpslMode=Partial` and `slOrderType=Limit`. Spot: required when `stopLoss` and `slOrderType=Limit` are set."""
  tpOrderType: NotRequired[Literal['Market', 'Limit']]
  """Order type when take profit triggers. linear & inverse: `Market` (default) or `Limit`, and `Full` mode only supports `Market`. Spot: `Market` when `takeProfit` is set, `Limit` when `takeProfit` and `tpLimitPrice` are set."""
  slOrderType: NotRequired[Literal['Market', 'Limit']]
  """Order type when stop loss triggers. Same rules as `tpOrderType`, mirrored for stop loss."""
  bboSideType: NotRequired[Literal['Queue', 'Counterparty']]
  """Which side of the book a BBO order references. `Queue`: same direction as `side`. `Counterparty`: opposite direction. Valid for linear & inverse."""
  bboLevel: NotRequired[Literal['1', '2', '3', '4', '5']]
  """BBO price level. Valid for linear & inverse."""


class CreateMarketOrderRequest(TypedDict):
  """Market order parameters. Executes as an IOC limit order internally with slippage protection; `price` plays no role."""

  category: Literal['linear', 'inverse', 'spot', 'option']
  """Product type."""
  symbol: str
  """Symbol name, uppercase only."""
  isLeverage: NotRequired[Literal[0, 1]]
  """Spot only. `0` (default): spot trading. `1`: margin trading (margin trading must be enabled, with the currency set as collateral)."""
  side: Literal['Buy', 'Sell']
  """Order side."""
  orderType: Literal['Market']
  """Order type."""
  qty: str
  """Order quantity. Spot market buy orders default to order-by-value; `marketUnit` picks value vs quantity. Perps/Futures/Option always order by quantity. Perps & Futures: passing `qty="0"` with `reduceOnly=true` and `closeOnTrigger=true` closes the position up to `maxMktOrderQty`/`maxOrderQty`."""
  marketUnit: NotRequired[Literal['baseCoin', 'quoteCoin']]
  """Unit for `qty` when creating a spot market order."""
  rpiTakerAccess: NotRequired[bool]
  """Whether the order is eligible to match against RPI quotes."""
  slippageToleranceType: NotRequired[Literal['TickSize', 'Percent']]
  """Slippage tolerance type for a market order. Not supported for take profit, stop loss or conditional orders."""
  slippageTolerance: NotRequired[str]
  """Slippage tolerance value. `TickSize`: integer, range [1, 10000]. `Percent`: up to 2 decimals, range [0.01, 10]."""
  triggerDirection: NotRequired[Literal[1, 2]]
  """Expected direction of a conditional order. `1`: triggers when price rises to `triggerPrice`. `2`: triggers when price falls to `triggerPrice`. Valid for linear & inverse."""
  orderFilter: NotRequired[Literal['Order', 'tpslOrder', 'StopOrder']]
  """Spot only. Defaults to `Order`."""
  triggerPrice: NotRequired[str]
  """For Perps & Futures, the conditional order trigger price. For spot, the TP/SL and conditional order trigger price."""
  triggerBy: NotRequired[Literal['LastPrice', 'IndexPrice', 'MarkPrice']]
  """Conditional order trigger price type. Valid for linear & inverse."""
  orderIv: NotRequired[str]
  """Implied volatility, option only. Pass the real value, e.g. `0.1` for 10%; takes priority over `price` when both are passed."""
  timeInForce: NotRequired[Literal['GTC', 'IOC', 'FOK', 'PostOnly', 'RPI']]
  """Time in force. Market orders always use `IOC`; defaults to `GTC` if not passed. `PostOnly` cancels the order if it would fill immediately. `RPI` (Retail Price Improvement) is post-only, restricted to assigned market makers, and matches only Web/App order flow."""
  positionIdx: NotRequired[Literal[0, 1, 2]]
  """Position mode index; required under hedge mode."""
  orderLinkId: NotRequired[str]
  """User-customised order ID, max 36 characters. Required and always-unique for options; optional and always-unique otherwise."""
  takeProfit: NotRequired[str]
  """Take profit price."""
  stopLoss: NotRequired[str]
  """Stop loss price."""
  tpTriggerBy: NotRequired[Literal['LastPrice', 'IndexPrice', 'MarkPrice']]
  """Trigger price type for take profit. Valid for linear & inverse."""
  slTriggerBy: NotRequired[Literal['LastPrice', 'IndexPrice', 'MarkPrice']]
  """Trigger price type for stop loss. Valid for linear & inverse."""
  reduceOnly: NotRequired[bool]
  """Position can only reduce in size if this order triggers. Cannot be combined with take profit/stop loss. Valid for linear, inverse & option."""
  closeOnTrigger: NotRequired[bool]
  """Closing order that can only reduce position, never increase it. Valid for linear & inverse."""
  smpType: NotRequired[Literal['None', 'CancelMaker', 'CancelTaker', 'CancelBoth']]
  """Self-match prevention execution type."""
  mmp: NotRequired[bool]
  """Market maker protection order, option only."""
  tpslMode: NotRequired[Literal['Full', 'Partial']]
  """TP/SL mode. `Full`: entire position, `tpOrderType`/`slOrderType` must be `Market`. `Partial`: partial position, limit TP/SL supported. Valid for linear & inverse."""
  tpLimitPrice: NotRequired[str]
  """Limit price when take profit triggers. linear & inverse: only with `tpslMode=Partial` and `tpOrderType=Limit`. Spot: required when `takeProfit` and `tpOrderType=Limit` are set."""
  slLimitPrice: NotRequired[str]
  """Limit price when stop loss triggers. linear & inverse: only with `tpslMode=Partial` and `slOrderType=Limit`. Spot: required when `stopLoss` and `slOrderType=Limit` are set."""
  tpOrderType: NotRequired[Literal['Market', 'Limit']]
  """Order type when take profit triggers. linear & inverse: `Market` (default) or `Limit`, and `Full` mode only supports `Market`. Spot: `Market` when `takeProfit` is set, `Limit` when `takeProfit` and `tpLimitPrice` are set."""
  slOrderType: NotRequired[Literal['Market', 'Limit']]
  """Order type when stop loss triggers. Same rules as `tpOrderType`, mirrored for stop loss."""
  bboSideType: NotRequired[Literal['Queue', 'Counterparty']]
  """Which side of the book a BBO order references. `Queue`: same direction as `side`. `Counterparty`: opposite direction. Valid for linear & inverse."""
  bboLevel: NotRequired[Literal['1', '2', '3', '4', '5']]
  """BBO price level. Valid for linear & inverse."""


class CreateOrderResult(TypedDict):
  """Identifiers of the placed order."""

  orderId: str
  """Order ID."""
  orderLinkId: str
  """User-customised order ID."""


adapter = validator[CreateOrderResult](CreateOrderResult)


class CreateOrder(Endpoint):
  """`Place Order` — mixed into the router that owns `trade.create_order`."""

  async def create_order(
    self,
    create_market_order_request_create_limit_order_request: CreateMarketOrderRequest
    | CreateLimitOrderRequest,
    *,
    validate: bool | None = None,
  ) -> CreateOrderResult:
    """Create an order for Spot, Margin, USDT/USDC perpetual and futures, Inverse futures or Options. `orderType=Market` executes as an IOC limit order internally with slippage protection. Setting `triggerPrice` converts the order into a conditional order, which occupies no margin until triggered. The acknowledgement means the request was accepted, not that the order is live -- this is asynchronous.

    Args:
      create_market_order_request_create_limit_order_request: Order to place.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/order/create-order)
    """
    r = await self.authed_request(
      'POST',
      '/v5/order/create',
      json=create_market_order_request_create_limit_order_request,
    )
    return self.result(r, adapter, validate)
