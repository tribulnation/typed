# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/order/disconnected-cancel-all` — Set Disconnect Cancel All (DCP)."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class SetDcpRequest(TypedDict):
  """Disconnect Cancel Protect configuration."""

  product: NotRequired[Literal['OPTIONS', 'DERIVATIVES', 'SPOT']]
  """Product scope. Defaults to `OPTIONS`."""
  timeWindow: int
  """Disconnection timing window. Range [3, 300], unit seconds."""


adapter = validator[None](None)


class SetDcp(Endpoint):
  """`Set Disconnect Cancel All (DCP)` — mixed into the router that owns `trade.set_dcp`."""

  async def set_dcp(
    self,
    set_dcp_request: SetDcpRequest,
    *,
    validate: bool | None = None,
  ) -> None:
    """Configure Disconnection Protect: if the private WebSocket connection drops and the client does not resume the heartbeat within `timeWindow` seconds, all active futures/spot/option orders are cancelled automatically. Requires the private WebSocket connection to subscribe to the `dcp` topic to trigger. DCP itself must first be enabled for the account by a Bybit client manager.

    Args:
      set_dcp_request: Disconnect Cancel Protect window and product scope to set.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/order/dcp)
    """
    r = await self.authed_request(
      'POST', '/v5/order/disconnected-cancel-all', json=set_dcp_request
    )
    return self.result(r, adapter, validate)
