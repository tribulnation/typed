# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/execution/list` — Get Trade History."""

from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, timestamp_millis, validator
from typed_bybit.types import CreateType


class Execution(TypedDict):
  """One execution (fill)."""

  symbol: str
  """Symbol name."""
  orderId: str
  """Order ID."""
  orderLinkId: str
  """User-customised order ID."""
  side: Literal['Buy', 'Sell']
  """Side."""
  orderPrice: str
  """Order price."""
  orderQty: str
  """Order quantity."""
  leavesQty: str
  """Remaining quantity not executed."""
  createType: NotRequired[CreateType]
  orderType: Literal['Market', 'Limit']
  """Order type."""
  stopOrderType: Literal[
    'TakeProfit',
    'StopLoss',
    'TrailingStop',
    'Stop',
    'PartialTakeProfit',
    'PartialStopLoss',
    'tpslOrder',
    'OcoOrder',
    'MmRateClose',
    'BidirectionalTpslOrder',
    'UNKNOWN',
    '',
  ]
  """Stop order sub-type. `UNKNOWN` or `""` when the order is not a stop order."""
  execFee: str
  """Executed trading fee."""
  execFeeV2: NotRequired[str]
  """Spot-leg transaction fee. Only for `execType=FutureSpread`."""
  execId: str
  """Execution ID."""
  execPrice: str
  """Execution price."""
  execQty: str
  """Execution quantity."""
  execType: Literal[
    'Trade',
    'AdlTrade',
    'Funding',
    'BustTrade',
    'Delivery',
    'Settle',
    'BlockTrade',
    'MovePosition',
    'FutureSpread',
    'CorporateAction',
    'UNKNOWN',
  ]
  """Execution type. `UNKNOWN` may be returned by a classic account and cannot be queried by this type."""
  execValue: str
  """Executed order value."""
  execTime: TimestampMillis
  """Execution time, as a millisecond timestamp."""
  feeCurrency: str
  """Trading fee currency."""
  isMaker: bool
  """Whether the execution was a maker fill."""
  feeRate: str
  """Trading fee rate."""
  tradeIv: NotRequired[str]
  """Implied volatility of the trade. Option only."""
  markIv: NotRequired[str]
  """Implied volatility of the mark price. Option only."""
  markPrice: NotRequired[str]
  """Mark price of the symbol at execution."""
  indexPrice: NotRequired[str]
  """Index price of the symbol at execution. Option only."""
  underlyingPrice: NotRequired[str]
  """Underlying price of the symbol at execution. Option only."""
  blockTradeId: str
  """Paradigm block trade ID."""
  closedSize: str
  """Closed position size."""
  seq: int
  """Cross sequence, shared across fills and position updates concluded together. Different symbols may share a `seq`; combine with `symbol` for uniqueness."""
  extraFees: NotRequired[str]
  """Trading fee rate information. Returned only for Indian-KYC users, spot orders placed on the Indonesian site, or spot fiat-currency orders on the EU site; empty string otherwise."""


class TradeHistoryResult(TypedDict):
  """Page of matching executions."""

  category: Literal['linear', 'inverse', 'spot', 'option']
  """Product type."""
  nextPageCursor: NotRequired[str]
  """Opaque cursor for the next page. Pass it back as `cursor`; an empty string means there are no further pages."""
  list: list[Execution]
  """Matching executions, newest first by `execTime`."""


adapter = validator[TradeHistoryResult](TradeHistoryResult)


class TradeHistory(Endpoint):
  """`Get Trade History` — mixed into the router that owns `trade.trade_history`."""

  async def trade_history(
    self,
    *,
    category: Literal['linear', 'inverse', 'spot', 'option'],
    symbol: str | None = None,
    order_id: str | None = None,
    order_link_id: str | None = None,
    base_coin: str | None = None,
    settle_coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    exec_type: Literal[
      'Trade',
      'AdlTrade',
      'Funding',
      'BustTrade',
      'Delivery',
      'Settle',
      'BlockTrade',
      'MovePosition',
      'FutureSpread',
      'CorporateAction',
    ]
    | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> TradeHistoryResult:
    """Query execution records (fills), sorted by `execTime` descending. A single order may have multiple executions. Items can tie on `execTime`; sort by `execId`+`orderId`+`leavesQty` when that matters.

    Args:
      category: Product type.
      symbol: Symbol name, uppercase only.
      order_id: Order ID.
      order_link_id: User-customised order ID.
      base_coin: Base coin, uppercase only. Defaults to `BTC` for option.
      settle_coin: Settle coin, uppercase only. linear, inverse & option only.
      start_time: Start of the queried range, as a millisecond timestamp. Neither passed: last 7 days. Only `startTime`: `[startTime, startTime+7d]`. Only `endTime`: `[endTime-7d, endTime]`. Both: `endTime - startTime <= 7 days`.
      end_time: End of the queried range, as a millisecond timestamp.
      exec_type: Filter by execution type.
      limit: Number of executions per page. Range [1, 100]; defaults to 50.
      cursor: Opaque pagination cursor. Pass the `nextPageCursor` returned by the previous page; omit it for the first page.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/order/execution)
    """
    params: dict = {
      'category': category,
    }
    if symbol is not None:
      params['symbol'] = symbol
    if order_id is not None:
      params['orderId'] = order_id
    if order_link_id is not None:
      params['orderLinkId'] = order_link_id
    if base_coin is not None:
      params['baseCoin'] = base_coin
    if settle_coin is not None:
      params['settleCoin'] = settle_coin
    if start_time is not None:
      params['startTime'] = timestamp_millis.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp_millis.dump(end_time)
    if exec_type is not None:
      params['execType'] = exec_type
    if limit is not None:
      params['limit'] = limit
    if cursor is not None:
      params['cursor'] = cursor
    r = await self.authed_request('GET', '/v5/execution/list', params=params)
    return self.result(r, adapter, validate)

  async def trade_history_paged(
    self,
    *,
    category: Literal['linear', 'inverse', 'spot', 'option'],
    symbol: str | None = None,
    order_id: str | None = None,
    order_link_id: str | None = None,
    base_coin: str | None = None,
    settle_coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    exec_type: Literal[
      'Trade',
      'AdlTrade',
      'Funding',
      'BustTrade',
      'Delivery',
      'Settle',
      'BlockTrade',
      'MovePosition',
      'FutureSpread',
      'CorporateAction',
    ]
    | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[TradeHistoryResult]:
    """Yield successive pages of `trade_history`.

    Passes each page's token back as `cursor` and stops when a response carries no
    `nextPageCursor`, or after `max_pages` pages when one is given.
    """
    cursor: str | None = None
    pages = 0
    while True:
      response = await self.trade_history(
        category=category,
        symbol=symbol,
        order_id=order_id,
        order_link_id=order_link_id,
        base_coin=base_coin,
        settle_coin=settle_coin,
        start_time=start_time,
        end_time=end_time,
        exec_type=exec_type,
        limit=limit,
        cursor=cursor,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      cursor = response.get('nextPageCursor') if response is not None else None
      if not cursor:
        break
