# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/spot-margin-trade/coinstate` — Get Coin State."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class CoinState(TypedDict):
  """Spot margin leverage for one coin."""

  currency: str
  """Coin name, uppercase."""
  spotLeverage: str
  """Spot margin leverage. Empty string if spot margin mode is off."""


class CoinStateResult(TypedDict):
  """Spot margin leverage by coin."""

  list: list[CoinState]
  """Spot margin leverage for one coin."""


adapter = validator[CoinStateResult](CoinStateResult)


class Coinstate(Endpoint):
  """`Get Coin State` — mixed into the router that owns `spot_margin.coinstate`."""

  async def __call__(
    self,
    *,
    currency: str | None = None,
    validate: bool | None = None,
  ) -> CoinStateResult:
    """Query spot margin leverage information for cryptocurrencies available on the platform.

    Args:
      currency: Coin name, uppercase only. Omit to return every coin.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/coinstate)
    """
    params = {}
    if currency is not None:
      params['currency'] = currency
    r = await self.authed_request(
      'GET', '/v5/spot-margin-trade/coinstate', params=params
    )
    return self.result(r, adapter, validate)
