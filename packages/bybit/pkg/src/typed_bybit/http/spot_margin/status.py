# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/spot-margin-trade/state` — Get Status And Leverage."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class SpotMarginState(TypedDict):
  """Spot margin trading status and leverage."""

  spotLeverage: str
  """Configured spot margin leverage; empty string if margin trading is disabled."""
  spotMarginMode: Literal['0', '1']
  """Margin trading status."""
  effectiveLeverage: str
  """Current actual leverage ratio, truncated to 2 decimal places."""


adapter = validator[SpotMarginState](SpotMarginState)


class Status(Endpoint):
  """`Get Status And Leverage` — mixed into the router that owns `spot_margin.status`."""

  async def __call__(self, *, validate: bool | None = None) -> SpotMarginState:
    """Query the spot margin trading status and leverage settings for this account.

    Args:
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/status)
    """
    r = await self.authed_request('GET', '/v5/spot-margin-trade/state')
    return self.result(r, adapter, validate)
