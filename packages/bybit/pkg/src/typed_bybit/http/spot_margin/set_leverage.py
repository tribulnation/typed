# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/spot-margin-trade/set-leverage` — Set Leverage."""

from typing_extensions import NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class SetLeverageRequest(TypedDict):
  """Leverage parameters."""

  leverage: str
  """Leverage value, range [2, 10]."""
  currency: NotRequired[str]
  """Coin name, uppercase only. Omit to set the account-wide leverage."""


class SetLeverageResult(TypedDict):
  """Empty result."""


adapter = validator[SetLeverageResult](SetLeverageResult)


class SetLeverage(Endpoint):
  """`Set Leverage` — mixed into the router that owns `spot_margin.set_leverage`."""

  async def __call__(
    self,
    set_leverage_request: SetLeverageRequest,
    *,
    validate: bool | None = None,
  ) -> SetLeverageResult:
    """Configure the maximum leverage for spot cross margin trading. The account must have spot margin activated, and the requested leverage cannot exceed the currency's maximum allowable leverage.

    Args:
      set_leverage_request: Leverage to set.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/set-leverage)
    """
    r = await self.authed_request(
      'POST', '/v5/spot-margin-trade/set-leverage', json=set_leverage_request
    )
    return self.result(r, adapter, validate)
