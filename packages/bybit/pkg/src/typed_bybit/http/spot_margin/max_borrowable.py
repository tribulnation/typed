# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/spot-margin-trade/max-borrowable` — Get Max Borrowable Amount."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class MaxBorrowableResult(TypedDict):
  """Maximum borrowable amount for the coin."""

  currency: str
  """Coin name, uppercase."""
  maxLoan: str
  """Maximum borrowable amount for the coin."""


adapter = validator[MaxBorrowableResult](MaxBorrowableResult)


class MaxBorrowable(Endpoint):
  """`Get Max Borrowable Amount` — mixed into the router that owns `spot_margin.max_borrowable`."""

  async def __call__(
    self,
    *,
    currency: str,
    validate: bool | None = None,
  ) -> MaxBorrowableResult:
    """Query the maximum borrowable amount for a specified cryptocurrency in spot margin trading.

    Args:
      currency: Coin name, uppercase only.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/max-borrowable)
    """
    params: dict = {
      'currency': currency,
    }
    r = await self.authed_request(
      'GET', '/v5/spot-margin-trade/max-borrowable', params=params
    )
    return self.result(r, adapter, validate)
