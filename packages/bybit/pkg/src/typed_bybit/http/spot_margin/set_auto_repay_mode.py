# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/spot-margin-trade/set-auto-repay-mode` — Set Auto Repay Mode."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class AutoRepayModeEntry(TypedDict):
  """Auto-repay mode for one currency."""

  currency: str
  """Coin name, uppercase."""
  autoRepayMode: Literal['0', '1']
  """Mode status."""


class SetAutoRepayModeRequest(TypedDict):
  """Auto-repay mode to set."""

  currency: NotRequired[str]
  """Coin name, uppercase only. Omit to enable automatic repayment for every currency."""
  autoRepayMode: Literal['0', '1']
  """Enables or disables automatic repayment mode."""


class SetAutoRepayModeResult(TypedDict):
  """Auto-repay mode after the change."""

  data: list[AutoRepayModeEntry]
  """Auto-repay mode for one currency."""


adapter = validator[SetAutoRepayModeResult](SetAutoRepayModeResult)


class SetAutoRepayMode(Endpoint):
  """`Set Auto Repay Mode` — mixed into the router that owns `spot_margin.set_auto_repay_mode`."""

  async def __call__(
    self,
    set_auto_repay_mode_request: SetAutoRepayModeRequest,
    *,
    validate: bool | None = None,
  ) -> SetAutoRepayModeResult:
    """Configure automatic repayment for spot margin trading. When enabled, the system automatically repays liabilities without asset conversion at the 0- and 30-minute marks each hour.

    Args:
      set_auto_repay_mode_request: Auto-repay mode to set.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/set-auto-repay-mode)
    """
    r = await self.authed_request(
      'POST',
      '/v5/spot-margin-trade/set-auto-repay-mode',
      json=set_auto_repay_mode_request,
    )
    return self.result(r, adapter, validate)
