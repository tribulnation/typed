# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/spot-margin-trade/repayment-available-amount` — Get Available Amount to Repay."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class RepaymentAvailableAmountResult(TypedDict):
  """Maximum repayable amount for the coin."""

  currency: str
  """Coin name, uppercase."""
  lossLessRepaymentAmount: str
  """min(spot coin available balance, coin borrow amount)."""


adapter = validator[RepaymentAvailableAmountResult](RepaymentAvailableAmountResult)


class RepaymentAvailableAmount(Endpoint):
  """`Get Available Amount to Repay` — mixed into the router that owns `spot_margin.repayment_available_amount`."""

  async def __call__(
    self,
    *,
    currency: str,
    validate: bool | None = None,
  ) -> RepaymentAvailableAmountResult:
    """Query the maximum amount available to repay borrowed cryptocurrency in spot margin trading: the minimum of the available spot balance and the borrowed amount.

    Args:
      currency: Coin name, uppercase only.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/repayment-available-amount)
    """
    params: dict = {
      'currency': currency,
    }
    r = await self.authed_request(
      'GET', '/v5/spot-margin-trade/repayment-available-amount', params=params
    )
    return self.result(r, adapter, validate)
