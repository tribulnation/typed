# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/spot-margin-trade/flexible-available-inventory` — Get Flexible Available Inventory."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class FlexibleAvailableInventoryResult(TypedDict):
  """Available flexible-rate borrow inventory for the coin."""

  currency: str
  """Coin name, uppercase."""
  availableInventory: str
  """min(platform total lendable amount, UTA user remaining borrowing limit, current leverage tier's borrow limit)."""
  updateTime: TimestampMillis
  """Last update time."""


adapter = validator[FlexibleAvailableInventoryResult](FlexibleAvailableInventoryResult)


class FlexibleAvailableInventory(Endpoint):
  """`Get Flexible Available Inventory` — mixed into the router that owns `spot_margin.flexible_available_inventory`."""

  async def __call__(
    self,
    *,
    currency: str,
    validate: bool | None = None,
  ) -> FlexibleAvailableInventoryResult:
    """Query the flexible-rate borrowing capacity for a specified cryptocurrency: the minimum of the platform's total lendable amount, this account's remaining UTA borrowing limit, and the current leverage tier's borrow limit.

    Args:
      currency: Coin name, uppercase only.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/flexible-available-inventory)
    """
    params: dict = {
      'currency': currency,
    }
    r = await self.authed_request(
      'GET', '/v5/spot-margin-trade/flexible-available-inventory', params=params
    )
    return self.result(r, adapter, validate)
