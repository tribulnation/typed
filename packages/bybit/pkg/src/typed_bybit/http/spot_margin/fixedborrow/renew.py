# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/spot-margin-trade/fixedborrow-renew` — Renew Fixed-Rate Borrow."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class FixedBorrowRenewRequest(TypedDict):
  """Loan to renew."""

  loanId: str
  """Loan ID to renew."""
  qty: NotRequired[str]
  """Renewal quantity. Omit to renew the entire remaining amount."""


adapter = validator[Literal['Success', 'Failure']](Literal['Success', 'Failure'])


class Renew(Endpoint):
  """`Renew Fixed-Rate Borrow` — mixed into the router that owns `spot_margin.fixedborrow.renew`."""

  async def renew(
    self,
    fixed_borrow_renew_request: FixedBorrowRenewRequest,
    *,
    validate: bool | None = None,
  ) -> Literal['Success', 'Failure']:
    """Renew an existing fixed-rate borrow loan, for the entire remaining amount or a specified quantity.

    Args:
      fixed_borrow_renew_request: Loan to renew.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/fixedborrow-renew)
    """
    r = await self.authed_request(
      'POST', '/v5/spot-margin-trade/fixedborrow-renew', json=fixed_borrow_renew_request
    )
    return self.result(r, adapter, validate)
