# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/spot-margin-trade/fixedborrow-order-info` — Get Fixed-Rate Borrow Order Info."""

from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class FixedBorrowOrder(TypedDict):
  """One fixed-rate borrow order."""

  annualRate: str
  """Annual borrowing rate."""
  orderId: str
  """Loan order ID."""
  orderTime: TimestampMillis
  """Order creation time."""
  filledQty: str
  """Filled quantity."""
  orderQty: str
  """Order quantity."""
  orderCurrency: str
  """Coin name."""
  state: Literal[1, 2, 3, 4, 5]
  """Order status."""
  term: Literal[7, 14, 30, 90, 180]
  """Fixed term, in days."""
  repayType: Literal['1', '2']
  """Repayment method."""
  strategyType: Literal['PARTIAL', 'FULL']
  """Fill strategy."""


class FixedBorrowOrderInfoResult(TypedDict):
  """Page of fixed-rate borrow orders."""

  nextPageCursor: NotRequired[str]
  """Opaque cursor for the next page. Empty string means there are no further pages."""
  list: list[FixedBorrowOrder]
  """One fixed-rate borrow order."""


adapter = validator[FixedBorrowOrderInfoResult](FixedBorrowOrderInfoResult)


class OrderInfo(Endpoint):
  """`Get Fixed-Rate Borrow Order Info` — mixed into the router that owns `spot_margin.fixedborrow.order_info`."""

  async def order_info(
    self,
    *,
    order_id: str | None = None,
    order_currency: str | None = None,
    state: Literal['1', '2', '3', '4'] | None = None,
    term: Literal['7', '14', '30', '90', '180'] | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> FixedBorrowOrderInfoResult:
    """Query fixed-rate borrow orders, newest first by `orderTime`.

    Args:
      order_id: Loan order ID.
      order_currency: Loan coin name.
      state: Borrow order status.
      term: Fixed term, in days.
      limit: Data size per page.
      cursor: Pagination cursor. Pass the `nextPageCursor` returned by the previous page; omit for the first page.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/fixedborrow-order-info)
    """
    params = {}
    if order_id is not None:
      params['orderId'] = order_id
    if order_currency is not None:
      params['orderCurrency'] = order_currency
    if state is not None:
      params['state'] = state
    if term is not None:
      params['term'] = term
    if limit is not None:
      params['limit'] = limit
    if cursor is not None:
      params['cursor'] = cursor
    r = await self.authed_request(
      'GET', '/v5/spot-margin-trade/fixedborrow-order-info', params=params
    )
    return self.result(r, adapter, validate)

  async def order_info_paged(
    self,
    *,
    order_id: str | None = None,
    order_currency: str | None = None,
    state: Literal['1', '2', '3', '4'] | None = None,
    term: Literal['7', '14', '30', '90', '180'] | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[FixedBorrowOrderInfoResult]:
    """Yield successive pages of `order_info`.

    Passes each page's token back as `cursor` and stops when a response carries no
    `nextPageCursor`, or after `max_pages` pages when one is given.
    """
    cursor: str | None = None
    pages = 0
    while True:
      response = await self.order_info(
        order_id=order_id,
        order_currency=order_currency,
        state=state,
        term=term,
        limit=limit,
        cursor=cursor,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      cursor = response.get('nextPageCursor') if response is not None else None
      if not cursor:
        break
