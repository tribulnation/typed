# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/spot-margin-trade/fixedborrow` — Fixed-Rate Borrow."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class FixedBorrowRequest(TypedDict):
  """Fixed-rate borrow order parameters."""

  orderCurrency: str
  """Currency to borrow."""
  orderAmount: str
  """Amount to borrow."""
  annualRate: str
  """Customisable annual interest rate, for example `0.02` for 2%."""
  term: Literal['7', '14', '30', '90', '180']
  """Fixed term, in days."""
  repayType: NotRequired[Literal['1', '2']]
  """Repayment strategy. Defaults to `1`."""
  strategyType: NotRequired[Literal['PARTIAL', 'FULL']]
  """Fill strategy. Defaults to `PARTIAL`."""


class FixedBorrowResult(TypedDict):
  """Identifier of the created loan order."""

  orderId: str
  """Loan order ID."""


adapter = validator[FixedBorrowResult](FixedBorrowResult)


class Borrow(Endpoint):
  """`Fixed-Rate Borrow` — mixed into the router that owns `spot_margin.fixedborrow.borrow`."""

  async def borrow(
    self,
    fixed_borrow_request: FixedBorrowRequest,
    *,
    validate: bool | None = None,
  ) -> FixedBorrowResult:
    """Create a fixed-rate borrow order.

    Args:
      fixed_borrow_request: Fixed-rate borrow order to place.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/fixedborrow)
    """
    r = await self.authed_request(
      'POST', '/v5/spot-margin-trade/fixedborrow', json=fixed_borrow_request
    )
    return self.result(r, adapter, validate)
