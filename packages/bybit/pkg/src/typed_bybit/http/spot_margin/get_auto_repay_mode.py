# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/spot-margin-trade/get-auto-repay-mode` — Get Auto Repay Mode."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class AutoRepayModeEntry(TypedDict):
  """Auto-repay mode for one currency."""

  currency: str
  """Coin name, uppercase."""
  autoRepayMode: Literal['0', '1']
  """Mode status."""


class AutoRepayModeResult(TypedDict):
  """Auto-repay mode by currency."""

  data: list[AutoRepayModeEntry]
  """Auto-repay mode for one currency."""


adapter = validator[AutoRepayModeResult](AutoRepayModeResult)


class GetAutoRepayMode(Endpoint):
  """`Get Auto Repay Mode` — mixed into the router that owns `spot_margin.get_auto_repay_mode`."""

  async def __call__(
    self,
    *,
    currency: str | None = None,
    validate: bool | None = None,
  ) -> AutoRepayModeResult:
    """Query the spot automatic repayment mode for margin trading.

    Args:
      currency: Coin name, uppercase only. Omit to return every currency.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/get-auto-repay-mode)
    """
    params = {}
    if currency is not None:
      params['currency'] = currency
    r = await self.authed_request(
      'GET', '/v5/spot-margin-trade/get-auto-repay-mode', params=params
    )
    return self.result(r, adapter, validate)
