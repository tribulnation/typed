# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/spot-margin-trade/switch-mode` — Toggle Margin Trade."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class SwitchModeRequest(TypedDict):
  """Desired spot margin trading state."""

  spotMarginMode: Literal['0', '1']
  """`1` to enable spot margin trading, `0` to disable."""


class SwitchModeResult(TypedDict):
  """Spot margin trading state."""

  spotMarginMode: Literal['0', '1']
  """Current status: `1` enabled, `0` disabled."""


adapter = validator[SwitchModeResult](SwitchModeResult)


class SwitchMode(Endpoint):
  """`Toggle Margin Trade` — mixed into the router that owns `spot_margin.switch_mode`."""

  async def __call__(
    self,
    switch_mode_request: SwitchModeRequest,
    *,
    validate: bool | None = None,
  ) -> SwitchModeResult:
    """Enable or disable spot margin trading. The account must have activated spot margin trading through the web or app quiz first.

    Args:
      switch_mode_request: Margin trading toggle.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/switch-mode)
    """
    r = await self.authed_request(
      'POST', '/v5/spot-margin-trade/switch-mode', json=switch_mode_request
    )
    return self.result(r, adapter, validate)
