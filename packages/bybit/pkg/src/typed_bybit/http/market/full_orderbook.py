# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/market/full_orderbook` — Get Full Depth Orderbook."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator
from typed_bybit.types import OrderbookLevel


class FullOrderbook(TypedDict):
  """Full-depth order book snapshot for one symbol."""

  s: str
  """Symbol name."""
  b: list[OrderbookLevel]
  """Bid levels, sorted by price in descending order."""
  a: list[OrderbookLevel]
  """Ask levels, sorted by price in ascending order."""
  ts: TimestampMillis
  """Time the snapshot was generated, as a millisecond timestamp."""
  u: int
  """Update identifier, matching the websocket order book stream."""
  seq: NotRequired[int]
  """Cross sequence number; smaller values were generated earlier."""
  cts: NotRequired[int]
  """Matching engine timestamp, matching the public trade stream."""


adapter = validator[FullOrderbook](FullOrderbook)


class FullOrderbookEndpoint(Endpoint):
  """`Get Full Depth Orderbook` — mixed into the router that owns `market.full_orderbook`."""

  async def full_orderbook(
    self,
    *,
    category: Literal['spot', 'linear', 'inverse'],
    symbol: str,
    validate: bool | None = None,
  ) -> FullOrderbook:
    """Get the full order book for one symbol, up to 10000 levels per side. RPI orders are excluded.

    Args:
      category: Product type.
      symbol: Symbol name in uppercase, for example `BTCUSDT`.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/market/full-ob)
    """
    params: dict = {
      'category': category,
      'symbol': symbol,
    }
    r = await self.request('GET', '/v5/market/full_orderbook', params=params)
    return self.result(r, adapter, validate)
