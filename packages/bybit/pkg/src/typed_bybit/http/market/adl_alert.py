# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/market/adlAlert` — Get ADL Alert."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class AdlAlert(TypedDict):
  """ADL state of one contract."""

  coin: str
  """Coin held by the backing insurance pool."""
  symbol: str
  """Symbol name."""
  balance: str
  """Insurance pool balance backing the contract."""
  maxBalance: str
  """Deprecated upstream; always an empty string."""
  insurancePnlRatio: str
  """PnL ratio threshold at which insurance-driven ADL applies."""
  pnlRatio: str
  """Rolling 8-hour PnL drawdown ratio of the contract."""
  adlTriggerThreshold: str
  """Balance threshold at which ADL is triggered."""
  adlStopRatio: str
  """PnL ratio at which ADL stops."""


class AdlAlertResult(TypedDict):
  """Auto-deleveraging state per insurance pool."""

  updatedTime: TimestampMillis
  """Time the state was last updated, as a millisecond timestamp."""
  list: list[AdlAlert]
  """ADL state entries."""


adapter = validator[AdlAlertResult](AdlAlertResult)


class AdlAlertEndpoint(Endpoint):
  """`Get ADL Alert` — mixed into the router that owns `market.adl_alert`."""

  async def adl_alert(
    self,
    *,
    symbol: str | None = None,
    validate: bool | None = None,
  ) -> AdlAlertResult:
    """Get the auto-deleveraging state of each insurance pool: current balance, drawdown ratio and the thresholds at which ADL starts and stops.

    Args:
      symbol: Symbol name in uppercase, for example `BTCUSDT`. Omit to return every contract.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/market/adl-alert)
    """
    params = {}
    if symbol is not None:
      params['symbol'] = symbol
    r = await self.request('GET', '/v5/market/adlAlert', params=params)
    return self.result(r, adapter, validate)
