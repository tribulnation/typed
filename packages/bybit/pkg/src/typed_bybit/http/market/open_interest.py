# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/market/open-interest` — Get Open Interest."""

from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, timestamp_millis, validator
from typed_bybit.types import MetricInterval


class OpenInterest(TypedDict):
  """One open interest sample."""

  openInterest: str
  """Open interest at the sample, counting both sides."""
  singleOpenInterest: NotRequired[str]
  """Open interest at the sample, counting one side."""
  timestamp: TimestampMillis
  """Sample time, as a millisecond timestamp."""


class OpenInterestResult(TypedDict):
  """Open interest series for one contract."""

  symbol: str
  """Symbol name."""
  category: Literal['linear', 'inverse']
  """Product type."""
  nextPageCursor: NotRequired[str]
  """Opaque cursor for the next page. Pass it back as `cursor`; an empty string means there are no further pages."""
  list: list[OpenInterest]
  """Open interest samples, sorted by timestamp in descending order."""


adapter = validator[OpenInterestResult](OpenInterestResult)


class OpenInterestEndpoint(Endpoint):
  """`Get Open Interest` — mixed into the router that owns `market.open_interest`."""

  async def open_interest(
    self,
    *,
    category: Literal['linear', 'inverse'],
    symbol: str,
    interval_time: MetricInterval,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> OpenInterestResult:
    """Get the open interest series of a contract, sampled at a fixed interval and newest first.

    Args:
      category: Product type.
      symbol: Symbol name in uppercase, for example `BTCUSDT`.
      interval_time: Sampling interval of the series.
      start_time: Start of the queried range, as a millisecond timestamp.
      end_time: End of the queried range, as a millisecond timestamp.
      limit: Number of samples per page. Range [1, 200]; defaults to 50.
      cursor: Opaque pagination cursor. Pass the `nextPageCursor` returned by the previous page; omit it for the first page.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/market/open-interest)
    """
    params: dict = {
      'category': category,
      'symbol': symbol,
      'intervalTime': interval_time,
    }
    if start_time is not None:
      params['startTime'] = timestamp_millis.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp_millis.dump(end_time)
    if limit is not None:
      params['limit'] = limit
    if cursor is not None:
      params['cursor'] = cursor
    r = await self.request('GET', '/v5/market/open-interest', params=params)
    return self.result(r, adapter, validate)

  async def open_interest_paged(
    self,
    *,
    category: Literal['linear', 'inverse'],
    symbol: str,
    interval_time: MetricInterval,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[OpenInterestResult]:
    """Yield successive pages of `open_interest`.

    Passes each page's token back as `cursor` and stops when a response carries no
    `nextPageCursor`, or after `max_pages` pages when one is given.
    """
    cursor: str | None = None
    pages = 0
    while True:
      response = await self.open_interest(
        category=category,
        symbol=symbol,
        interval_time=interval_time,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        cursor=cursor,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      cursor = response.get('nextPageCursor') if response is not None else None
      if not cursor:
        break
