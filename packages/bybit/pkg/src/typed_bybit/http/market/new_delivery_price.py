# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/market/new-delivery-price` — Get New Delivery Price."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class NewDeliveryPrice(TypedDict):
  """One settlement price of the underlying."""

  deliveryPrice: str
  """Price the options settled at."""
  deliveryTime: TimestampMillis
  """Delivery time, as a millisecond timestamp."""


class NewDeliveryPriceResult(TypedDict):
  """Recent option settlement prices."""

  category: Literal['option']
  """Product type."""
  list: list[NewDeliveryPrice]
  """Settlements, sorted by delivery time in descending order."""


adapter = validator[NewDeliveryPriceResult](NewDeliveryPriceResult)


class NewDeliveryPriceEndpoint(Endpoint):
  """`Get New Delivery Price` — mixed into the router that owns `market.new_delivery_price`."""

  async def new_delivery_price(
    self,
    *,
    category: Literal['option'],
    base_coin: str,
    settle_coin: str | None = None,
    validate: bool | None = None,
  ) -> NewDeliveryPriceResult:
    """Get the recent daily settlement prices used to deliver options on a base coin. Unlike `delivery_price`, entries carry no symbol; they are the per-expiry settlement prices of the underlying.

    Args:
      category: Product type.
      base_coin: Base coin in uppercase.
      settle_coin: Settlement coin in uppercase. Defaults to `USDT`.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/market/new-delivery-price)
    """
    params: dict = {
      'category': category,
      'baseCoin': base_coin,
    }
    if settle_coin is not None:
      params['settleCoin'] = settle_coin
    r = await self.request('GET', '/v5/market/new-delivery-price', params=params)
    return self.result(r, adapter, validate)
