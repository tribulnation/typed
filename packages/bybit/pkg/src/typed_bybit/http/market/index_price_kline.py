# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/market/index-price-kline` — Get Index Price Kline."""

from datetime import timedelta
from typing_extensions import AsyncIterator, Literal, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, timestamp_millis, validator
from typed_bybit.types import KlineInterval
from typed_core.exceptions import LogicError


class IndexPriceKlineResult(TypedDict):
  """Candle series for one symbol."""

  category: Literal['linear', 'inverse']
  """Product type the candles belong to."""
  symbol: str
  """Symbol name."""
  list: list[tuple[str, str, str, str, str]]
  """Candles, sorted by start time in descending order."""


adapter = validator[IndexPriceKlineResult](IndexPriceKlineResult)


class IndexPriceKline(Endpoint):
  """`Get Index Price Kline` — mixed into the router that owns `market.index_price_kline`."""

  async def index_price_kline(
    self,
    *,
    category: Literal['linear', 'inverse'] | None = None,
    symbol: str,
    interval: KlineInterval,
    start: TimestampMillis | None = None,
    end: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> IndexPriceKlineResult:
    """Get index price candles for a contract. Candles are returned newest first.

    Args:
      category: Product type. Defaults to `linear` when omitted.
      symbol: Symbol name in uppercase, for example `BTCUSDT`.
      interval: Candle duration.
      start: Start of the queried range, as a millisecond timestamp.
      end: End of the queried range, as a millisecond timestamp.
      limit: Number of candles per page. Range [1, 1000]; defaults to 200.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/market/index-kline)
    """
    params: dict = {
      'symbol': symbol,
      'interval': interval,
    }
    if category is not None:
      params['category'] = category
    if start is not None:
      params['start'] = timestamp_millis.dump(start)
    if end is not None:
      params['end'] = timestamp_millis.dump(end)
    if limit is not None:
      params['limit'] = limit
    r = await self.request('GET', '/v5/market/index-price-kline', params=params)
    return self.result(r, adapter, validate)

  async def index_price_kline_paged(
    self,
    *,
    category: Literal['linear', 'inverse'] | None = None,
    symbol: str,
    interval: KlineInterval,
    start: TimestampMillis | None = None,
    end: TimestampMillis | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    allow_truncation: bool = False,
    validate: bool | None = None,
  ) -> AsyncIterator[IndexPriceKlineResult]:
    """Yield successive pages of `index_price_kline`.

    Moves the `start`–`end` window backwards by its own width and stops on the first
    empty window, or after `max_pages` pages when one is given.

    Every request spans the width the caller's own `start` and `end` state, so choose a
    window the venue answers in one response — at most `limit` rows. A full page is
    evidence the window was capped, and raises `LogicError` rather than walking past the
    rows that were left out; pass `allow_truncation=True` to accept the loss and keep
    going.
    """
    if start is None or end is None:
      raise ValueError(
        '`index_price_kline_paged` walks a time window: pass both `start` and `end`'
      )
    lower = start
    upper = end
    width = upper - lower
    pages = 0
    while True:
      response = await self.index_price_kline(
        category=category,
        symbol=symbol,
        interval=interval,
        start=lower,
        end=upper,
        limit=limit,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      rows = response.get('list') if response is not None else None
      if not rows:
        break
      if not allow_truncation and len(rows) >= (limit if limit is not None else 200):
        raise LogicError(
          f'`index_price_kline_paged` requested the window {lower} to {upper} and the venue returned a full page of {len(rows)} rows, so it may hold more; advancing would move past the rows that were left out. Narrow the window, or pass `allow_truncation=True` to accept the loss.'
        )
      upper = lower - timedelta(milliseconds=1)
      lower = upper - width
