# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /fht/compliance/tax/v3/private/url` — Retrieve Data Export."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class ReportDownloadRequest(TypedDict):
  """The report request to fetch."""

  queryId: str
  """Report request identifier, as returned by `tax.report_create`."""


class TaxReportDownload(TypedDict):
  """The completed report's download location."""

  url: str
  """URL of the exported report file(s) -- a base path plus file name(s). A report may be split across multiple files to keep generation time down."""


adapter = validator[TaxReportDownload](TaxReportDownload)


class ReportDownload(Endpoint):
  """`Retrieve Data Export` — mixed into the router that owns `tax.report_download`."""

  async def report_download(
    self,
    report_download_request: ReportDownloadRequest,
    *,
    validate: bool | None = None,
  ) -> TaxReportDownload:
    """Fetch the download URL(s) for a completed report export, by the `queryId` returned from `tax.report_create`. Only meaningful once `tax.report_status` reports the report as processed.

    Args:
      report_download_request: The report request to fetch.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/tax/data-export)
    """
    r = await self.authed_request(
      'POST', '/fht/compliance/tax/v3/private/url', json=report_download_request
    )
    return self.result(r, adapter, validate)
