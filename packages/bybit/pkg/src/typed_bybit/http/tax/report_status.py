# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /fht/compliance/tax/v3/private/status` — Get Export Report Status."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class ReportStatusRequest(TypedDict):
  """The report request to check."""

  queryId: str
  """Report request identifier, as returned by `tax.report_create`."""


class TaxReportStatus(TypedDict):
  """The report's current processing status. `-1` Failed, `0` Queued, `1` Processing, `2` Processed, `3` Expired."""

  status: Literal['-1', '0', '1', '2', '3']
  """Processing status. `-1` Failed, `0` Queued, `1` Processing, `2` Processed, `3` Expired."""


adapter = validator[TaxReportStatus](TaxReportStatus)


class ReportStatus(Endpoint):
  """`Get Export Report Status` — mixed into the router that owns `tax.report_status`."""

  async def report_status(
    self,
    report_status_request: ReportStatusRequest,
    *,
    validate: bool | None = None,
  ) -> TaxReportStatus:
    """Poll the processing status of a previously requested report export, by the `queryId` returned from `tax.report_create`.

    Args:
      report_status_request: The report request to check.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/tax/status)
    """
    r = await self.authed_request(
      'POST', '/fht/compliance/tax/v3/private/status', json=report_status_request
    )
    return self.result(r, adapter, validate)
