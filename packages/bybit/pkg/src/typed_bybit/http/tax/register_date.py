# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /fht/compliance/tax/v3/private/registertime` — Get User Register Date."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, TimestampSeconds, validator


class TaxRegisterDate(TypedDict):
  """The authenticated user's registration date."""

  registerTime: TimestampSeconds
  """Date the user registered on the platform, as a whole-seconds Unix timestamp."""


adapter = validator[TaxRegisterDate](TaxRegisterDate)


class RegisterDate(Endpoint):
  """`Get User Register Date` — mixed into the router that owns `tax.register_date`."""

  async def register_date(self, *, validate: bool | None = None) -> TaxRegisterDate:
    """Get the date the authenticated user registered on the platform, as a Unix timestamp in whole seconds.

    Args:
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/tax/time)
    """
    r = await self.authed_request('POST', '/fht/compliance/tax/v3/private/registertime')
    return self.result(r, adapter, validate)
