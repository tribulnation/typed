# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/asset/covert/small-balance-execute` — Confirm a Small Balance Convert Quote."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class ConvertSmallBalanceConfirmRequest(TypedDict):
  """Small-balance convert execution parameters."""

  quoteId: str
  """Quote ID, from `asset.convert_small_balance.quote`."""


class ConvertSmallBalanceConfirmResult(TypedDict):
  """Immediate execution status."""

  quoteId: str
  """The quote ID, echoed back."""
  exchangeTxId: str
  """Exchange ID; the same value as `quoteId`."""
  submitTime: TimestampMillis
  """Submission time, as a millisecond timestamp."""
  status: Literal['init', 'processing', 'success', 'failure', 'partial_fulfillment']
  """Immediate execution status."""
  msg: str
  """Status message; typically empty."""


adapter = validator[ConvertSmallBalanceConfirmResult](ConvertSmallBalanceConfirmResult)


class Confirm(Endpoint):
  """`Confirm a Small Balance Convert Quote` — mixed into the router that owns `asset.convert_small_balance.confirm`."""

  async def confirm(
    self,
    convert_small_balance_confirm_request: ConvertSmallBalanceConfirmRequest,
    *,
    validate: bool | None = None,
  ) -> ConvertSmallBalanceConfirmResult:
    """Execute a previously requested small-balance sweep quote before it expires.

    Args:
      convert_small_balance_confirm_request: Quote to execute.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/asset/convert-small-balance/confirm-quote)
    """
    r = await self.authed_request(
      'POST',
      '/v5/asset/covert/small-balance-execute',
      json=convert_small_balance_confirm_request,
    )
    return self.result(r, adapter, validate)
