# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/asset/covert/small-balance-list` — Get Small Balance Coins."""

from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class SmallAssetCoin(TypedDict):
  """One small balance."""

  fromCoin: str
  """Coin held."""
  supportConvert: Literal[1, 2]
  """Whether this coin currently supports being swept."""
  availableBalance: str
  """Balance available to sweep."""
  baseValue: str
  """Balance's USDT-equivalent valuation."""
  toCoin: NotRequired[str]
  """Reserved; not meaningful on this endpoint."""
  toAmount: NotRequired[str]
  """Reserved; not meaningful on this endpoint."""
  exchangeRate: NotRequired[str]
  """Reserved; not meaningful on this endpoint."""
  feeInfo: NotRequired[dict[str, Any] | None]
  """Reserved; not meaningful on this endpoint."""
  taxFeeInfo: NotRequired[dict[str, Any] | None]
  """Reserved; not meaningful on this endpoint."""


class ConvertSmallBalanceCoinListResult(TypedDict):
  """Convertible small balances, and supported destination coins."""

  smallAssetCoins: list[SmallAssetCoin]
  """One entry per small balance held."""
  supportToCoins: list[str]
  """Destination coins available to sweep into."""


adapter = validator[ConvertSmallBalanceCoinListResult](
  ConvertSmallBalanceCoinListResult
)


class CoinList(Endpoint):
  """`Get Small Balance Coins` — mixed into the router that owns `asset.convert_small_balance.coin_list`."""

  async def coin_list(
    self,
    *,
    account_type: Literal['eb_convert_uta'],
    from_coin: str | None = None,
    validate: bool | None = None,
  ) -> ConvertSmallBalanceCoinListResult:
    """List this account's convertible small crypto balances, and the coins they can be swept into.

    Args:
      account_type: Wallet type to list small balances for. Only the Unified wallet is supported.
      from_coin: Coin to filter by. Omit to return every held small balance.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/asset/convert-small-balance/small-balanc-coins)
    """
    params: dict = {
      'accountType': account_type,
    }
    if from_coin is not None:
      params['fromCoin'] = from_coin
    r = await self.authed_request(
      'GET', '/v5/asset/covert/small-balance-list', params=params
    )
    return self.result(r, adapter, validate)
