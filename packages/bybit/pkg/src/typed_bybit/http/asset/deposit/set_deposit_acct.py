# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/asset/deposit/deposit-to-account` — Set Deposit Account."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class SetDepositAcctRequest(TypedDict):
  """Destination wallet type."""

  accountType: Literal['UNIFIED', 'FUND', 'EARN']
  """Destination wallet type. `EARN` (flexible savings) reverts to the funding wallet if unsupported for the deposited coin."""


class SetDepositAcctResult(TypedDict):
  """Result of setting the deposit account."""

  status: Literal[0, 1]
  """Outcome."""


adapter = validator[SetDepositAcctResult](SetDepositAcctResult)


class SetDepositAcct(Endpoint):
  """`Set Deposit Account` — mixed into the router that owns `asset.deposit.set_deposit_acct`."""

  async def set_deposit_acct(
    self,
    set_deposit_acct_request: SetDepositAcctRequest,
    *,
    validate: bool | None = None,
  ) -> SetDepositAcctResult:
    """Set which wallet type future deposits are automatically routed into. Main account UIDs only.

    Args:
      set_deposit_acct_request: Destination wallet type for future deposits.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/asset/deposit/set-deposit-acct)
    """
    r = await self.authed_request(
      'POST', '/v5/asset/deposit/deposit-to-account', json=set_deposit_acct_request
    )
    return self.result(r, adapter, validate)
