# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/asset/fundinghistory` — Get Funding (Sub-)Account Transaction History."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampSeconds, validator


class FundHistoryRecord(TypedDict):
  """One funding-account ledger entry."""

  memberId: str
  """User (member) identifier."""
  currency: str
  """Coin name."""
  ioDirection: Literal['I', 'O']
  """Transaction direction."""
  txnAmt: str
  """Transaction amount."""
  afterAmt: str
  """Account balance following the transaction."""
  createTime: TimestampSeconds
  """Creation time, as a Unix-seconds timestamp (not milliseconds)."""
  showBusiType: str
  """Localized business-type key."""
  showBusiTypeEn: str
  """Business type, in English."""
  description: str
  """Localized transaction description key."""
  descriptionEn: str
  """Transaction description, in English."""


class FundHistoryResult(TypedDict):
  """Page of funding-account ledger records."""

  nextPageCursor: NotRequired[str]
  """Opaque cursor for the next page. Pass it back as `cursor`; an empty string means there are no further pages."""
  list: list[FundHistoryRecord]
  """Ledger records."""


adapter = validator[FundHistoryResult](FundHistoryResult)


class FundHistory(Endpoint):
  """`Get Funding (Sub-)Account Transaction History` — mixed into the router that owns `asset.fund_history`."""

  async def __call__(
    self,
    *,
    create_time_from: int | None = None,
    create_time_to: int | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> FundHistoryResult:
    """Query the funding-account ledger: inbound and outbound transactions with balances before/after each one.

    Args:
      create_time_from: Start of the queried range, as a Unix-seconds timestamp (not milliseconds). Must be paired with `createTimeTo`; the interval cannot exceed 7 days. Defaults to the last 7 days when neither bound is passed.
      create_time_to: End of the queried range, as a Unix-seconds timestamp (not milliseconds). Must be paired with `createTimeFrom`; the interval cannot exceed 7 days. Defaults to the last 7 days when neither bound is passed.
      limit: Number of records per page. Range [1, 100]; defaults to 10.
      cursor: Opaque pagination cursor. Pass the `nextPageCursor` returned by the previous page; omit it for the first page.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/asset/fund-history)
    """
    params = {}
    if create_time_from is not None:
      params['createTimeFrom'] = create_time_from
    if create_time_to is not None:
      params['createTimeTo'] = create_time_to
    if limit is not None:
      params['limit'] = limit
    if cursor is not None:
      params['cursor'] = cursor
    r = await self.authed_request('GET', '/v5/asset/fundinghistory', params=params)
    return self.result(r, adapter, validate)
