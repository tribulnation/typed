# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/asset/exchange/convert-result-query` — Get Convert Status."""

from typing_extensions import Any, Literal, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class ConvertResultResult(TypedDict):
  """Status of one convert."""

  accountType: Literal[
    'eb_convert_funding',
    'eb_convert_uta',
    'eb_convert_spot',
    'eb_convert_contract',
    'eb_convert_inverse',
  ]
  """Wallet type this convert happened in."""
  exchangeTxId: str
  """Exchange transaction ID; mirrors the original quote transaction ID."""
  userId: str
  """User identifier."""
  fromCoin: str
  """Coin sold."""
  fromCoinType: Literal['crypto']
  """Asset class of `fromCoin`."""
  toCoin: str
  """Coin purchased."""
  toCoinType: Literal['crypto']
  """Asset class of `toCoin`."""
  fromAmount: str
  """Quantity sold."""
  toAmount: str
  """Quantity received."""
  exchangeStatus: Literal['init', 'processing', 'success', 'failure']
  """Exchange status."""
  extInfo: dict[str, Any]
  """Reserved, currently unused."""
  convertRate: str
  """Conversion rate applied."""
  createdAt: TimestampMillis
  """Quote creation time, as a millisecond timestamp."""


adapter = validator[ConvertResultResult](ConvertResultResult)


class Status(Endpoint):
  """`Get Convert Status` — mixed into the router that owns `asset.convert.status`."""

  async def status(
    self,
    *,
    quote_tx_id: str,
    account_type: Literal[
      'eb_convert_funding',
      'eb_convert_uta',
      'eb_convert_spot',
      'eb_convert_contract',
      'eb_convert_inverse',
    ],
    validate: bool | None = None,
  ) -> ConvertResultResult:
    """Query the status of a crypto-to-crypto convert, by quote transaction ID.

    Args:
      quote_tx_id: Quote transaction ID to query.
      account_type: Wallet type the quote was requested in.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/asset/convert/get-convert-result)
    """
    params: dict = {
      'quoteTxId': quote_tx_id,
      'accountType': account_type,
    }
    r = await self.authed_request(
      'GET', '/v5/asset/exchange/convert-result-query', params=params
    )
    return self.result(r, adapter, validate)
