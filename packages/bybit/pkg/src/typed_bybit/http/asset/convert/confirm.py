# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/asset/exchange/convert-execute` — Confirm a Convert Quote."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class ConvertConfirmRequest(TypedDict):
  """Convert execution parameters."""

  quoteTxId: str
  """Quote transaction ID, from `asset.convert.quote`."""


class ConvertConfirmResult(TypedDict):
  """Immediate execution status."""

  quoteTxId: str
  """The quote's transaction ID, echoed back."""
  exchangeStatus: Literal['init', 'processing', 'success', 'failure']
  """Immediate execution status."""


adapter = validator[ConvertConfirmResult](ConvertConfirmResult)


class Confirm(Endpoint):
  """`Confirm a Convert Quote` — mixed into the router that owns `asset.convert.confirm`."""

  async def confirm(
    self,
    convert_confirm_request: ConvertConfirmRequest,
    *,
    validate: bool | None = None,
  ) -> ConvertConfirmResult:
    """Execute a previously requested convert quote before it expires. Asynchronous -- poll asset.convert.result/history for the final status.

    Args:
      convert_confirm_request: Quote to execute.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/asset/convert/confirm-quote)
    """
    r = await self.authed_request(
      'POST', '/v5/asset/exchange/convert-execute', json=convert_confirm_request
    )
    return self.result(r, adapter, validate)
