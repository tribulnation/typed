# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/asset/exchange/query-coin-list` — Get Convert Coin List."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class ConvertCoin(TypedDict):
  """One convertible coin."""

  coin: str
  """Coin symbol."""
  fullName: str
  """Coin's full name."""
  icon: str
  """URL to the light-mode icon."""
  iconNight: str
  """URL to the dark-mode icon."""
  accuracyLength: int
  """Decimal precision."""
  coinType: Literal['crypto']
  """Asset class."""
  balance: str
  """Available balance in this wallet."""
  uBalance: str
  """Balance's USDT-equivalent value."""
  singleFromMinLimit: str
  """Minimum sell amount."""
  singleFromMaxLimit: str
  """Maximum sell amount."""
  disableFrom: bool
  """Whether this coin is currently disabled as a source."""
  disableTo: bool
  """Whether this coin is currently disabled as a destination."""


class ConvertCoinListResult(TypedDict):
  """Convertible coins for one wallet type."""

  coins: list[ConvertCoin]
  """One entry per convertible coin."""


adapter = validator[ConvertCoinListResult](ConvertCoinListResult)


class CoinList(Endpoint):
  """`Get Convert Coin List` — mixed into the router that owns `asset.convert.coin_list`."""

  async def coin_list(
    self,
    *,
    account_type: Literal[
      'eb_convert_funding',
      'eb_convert_uta',
      'eb_convert_spot',
      'eb_convert_contract',
      'eb_convert_inverse',
    ],
    coin: str | None = None,
    side: Literal[0, 1] | None = None,
    validate: bool | None = None,
  ) -> ConvertCoinListResult:
    """List the coins convertible within one wallet type, as sources (with balance) or as destinations.

    Args:
      account_type: Wallet type to list coins for.
      coin: Coin to filter by, uppercase only, when `side=0`. Ignored when `side=1`.
      side: Which list to return.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/asset/convert/convert-coin-list)
    """
    params: dict = {
      'accountType': account_type,
    }
    if coin is not None:
      params['coin'] = coin
    if side is not None:
      params['side'] = side
    r = await self.authed_request(
      'GET', '/v5/asset/exchange/query-coin-list', params=params
    )
    return self.result(r, adapter, validate)
