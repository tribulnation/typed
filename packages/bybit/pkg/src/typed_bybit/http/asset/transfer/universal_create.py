# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/asset/transfer/universal-transfer` — Create Universal Transfer."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class UniversalTransferRequest(TypedDict):
  """Universal transfer parameters."""

  transferId: str
  """Caller-generated UUID, used as an idempotency key for this transfer."""
  coin: str
  """Coin name, uppercase only."""
  amount: str
  """Amount to transfer."""
  fromMemberId: int
  """Sender's UID."""
  toMemberId: int
  """Recipient's UID."""
  fromAccountType: Literal['CONTRACT', 'SPOT', 'OPTION', 'UNIFIED', 'FUND']
  """Sender's account type."""
  toAccountType: Literal['CONTRACT', 'SPOT', 'OPTION', 'UNIFIED', 'FUND']
  """Recipient's account type."""


class UniversalTransferResult(TypedDict):
  """Universal transfer outcome."""

  transferId: str
  """The transfer's UUID, echoed back."""
  status: Literal['STATUS_UNKNOWN', 'SUCCESS', 'PENDING', 'FAILED']
  """Transfer outcome."""


adapter = validator[UniversalTransferResult](UniversalTransferResult)


class UniversalCreate(Endpoint):
  """`Create Universal Transfer` — mixed into the router that owns `asset.transfer.universal_create`."""

  async def universal_create(
    self,
    universal_transfer_request: UniversalTransferRequest,
    *,
    validate: bool | None = None,
  ) -> UniversalTransferResult:
    """Transfer a coin between two UIDs and account types -- master to sub-account, sub-account to sub-account (with permission), or sub-account to master.

    Args:
      universal_transfer_request: Universal transfer request.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/asset/transfer/unitransfer)
    """
    r = await self.authed_request(
      'POST', '/v5/asset/transfer/universal-transfer', json=universal_transfer_request
    )
    return self.result(r, adapter, validate)
