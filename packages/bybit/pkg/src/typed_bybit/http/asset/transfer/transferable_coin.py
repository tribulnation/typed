# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/asset/transfer/query-transfer-coin-list` — Get Transferable Coin."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class TransferableCoinResult(TypedDict):
  """Coins transferable between two account types."""

  list: list[str]
  """Transferable coin names."""


adapter = validator[TransferableCoinResult](TransferableCoinResult)


class TransferableCoin(Endpoint):
  """`Get Transferable Coin` — mixed into the router that owns `asset.transfer.transferable_coin`."""

  async def transferable_coin(
    self,
    *,
    from_account_type: Literal['CONTRACT', 'SPOT', 'OPTION', 'UNIFIED', 'FUND'],
    to_account_type: Literal['CONTRACT', 'SPOT', 'OPTION', 'UNIFIED', 'FUND'],
    validate: bool | None = None,
  ) -> TransferableCoinResult:
    """List the coins transferable between two given account types.

    Args:
      from_account_type: Source account type.
      to_account_type: Destination account type.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/asset/transfer/transferable-coin)
    """
    params: dict = {
      'fromAccountType': from_account_type,
      'toAccountType': to_account_type,
    }
    r = await self.authed_request(
      'GET', '/v5/asset/transfer/query-transfer-coin-list', params=params
    )
    return self.result(r, adapter, validate)
