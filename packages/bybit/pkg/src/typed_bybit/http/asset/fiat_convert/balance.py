# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/fiat/balance-query` — Get Fiat Convert Balance."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class FiatConvertBalance(TypedDict):
  """Balance of one fiat currency."""

  currency: str
  """Fiat currency code (ISO 4217)."""
  totalBalance: str
  """Total balance."""
  balance: str
  """Available balance."""
  frozenBalance: str
  """Balance frozen (e.g. by a pending conversion)."""


adapter = validator[list[FiatConvertBalance]](list[FiatConvertBalance])


class Balance(Endpoint):
  """`Get Fiat Convert Balance` — mixed into the router that owns `asset.fiat_convert.balance`."""

  async def balance(
    self,
    *,
    currency: str | None = None,
    validate: bool | None = None,
  ) -> list[FiatConvertBalance]:
    """Query fiat balances held for fiat-convert, per currency or for every currency.

    Args:
      currency: Fiat currency code (ISO 4217), for example `KZT`. Omit to return every fiat currency.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/asset/fiat-convert/balance-query)
    """
    params = {}
    if currency is not None:
      params['currency'] = currency
    r = await self.authed_request('GET', '/v5/fiat/balance-query', params=params)
    return self.result(r, adapter, validate)
