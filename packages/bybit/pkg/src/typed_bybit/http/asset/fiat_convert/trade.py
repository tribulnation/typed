# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/fiat/trade-query` — Get Fiat Convert Status."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class FiatConvertTradeResult(TypedDict):
  """Status of one fiat conversion trade."""

  tradeNo: str
  """Trade order number."""
  status: Literal['processing', 'success', 'failed']
  """Trade status."""
  quoteTxId: str
  """Quote transaction ID this trade was confirmed from."""
  exchangeRate: str
  """Conversion rate applied."""
  fromCoin: str
  """Coin sold."""
  fromCoinType: Literal['fiat', 'crypto']
  """Whether `fromCoin` is fiat or crypto."""
  toCoin: str
  """Coin purchased."""
  toCoinType: Literal['fiat', 'crypto']
  """Whether `toCoin` is fiat or crypto."""
  fromAmount: str
  """Quantity sold."""
  toAmount: str
  """Quantity received."""
  createdAt: TimestampMillis
  """Trade creation time, as a millisecond timestamp."""
  subUserId: str
  """Sub-user ID this trade was made under."""


adapter = validator[FiatConvertTradeResult](FiatConvertTradeResult)


class Trade(Endpoint):
  """`Get Fiat Convert Status` — mixed into the router that owns `asset.fiat_convert.trade`."""

  async def trade(
    self,
    *,
    trade_no: str | None = None,
    merchant_request_id: str | None = None,
    validate: bool | None = None,
  ) -> FiatConvertTradeResult:
    """Query the status of a fiat conversion trade, by trade number or caller-supplied tracking ID.

    Args:
      trade_no: Trade order number to query. One of `tradeNo`/`merchantRequestId` is required.
      merchant_request_id: Caller-supplied tracking ID to query. One of `tradeNo`/`merchantRequestId` is required.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/asset/fiat-convert/query-trade)
    """
    params = {}
    if trade_no is not None:
      params['tradeNo'] = trade_no
    if merchant_request_id is not None:
      params['merchantRequestId'] = merchant_request_id
    r = await self.authed_request('GET', '/v5/fiat/trade-query', params=params)
    return self.result(r, adapter, validate)
