# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/fiat/reference-price` — Get Fiat Convert Reference Price."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, TimestampSeconds, validator


class FiatConvertReferenceQuoteBuysItem(TypedDict):
  """One reference price for a payment method."""

  unitPrice: str
  """Price of 1 unit of crypto, in fiat."""
  paymentMethod: str
  """Payment method this quote applies to, for example `Apple Pay`, `Credit Card`, `Cash Balance`, `My Bank SWIFT`, `Google Pay`, `SEPA`, `Card`. Confirmed live 2026-08-20 to be the payment method name, not a `fiat`/`crypto` flag as the doc page's own field summary suggests -- the venue's own set of payment methods isn't published as a closed list, so kept as a bare string rather than an invented enum."""


class FiatConvertReferenceQuoteSellsItem(TypedDict):
  """One reference price for a payment method."""

  unitPrice: str
  """Price of 1 unit of crypto, in fiat."""
  paymentMethod: str
  """Payment method this quote applies to, for example `Apple Pay`, `Credit Card`, `Cash Balance`, `My Bank SWIFT`, `Google Pay`, `SEPA`, `Card`. Confirmed live 2026-08-20 to be the payment method name, not a `fiat`/`crypto` flag as the doc page's own field summary suggests -- the venue's own set of payment methods isn't published as a closed list, so kept as a bare string rather than an invented enum."""


class FiatConvertReferencePriceResult(TypedDict):
  """Reference buy/sell prices for one pair."""

  symbol: str
  """Pair identifier, for example `EUR-USDT`."""
  fiat: str
  """Fiat currency of this pair."""
  crypto: str
  """Crypto coin of this pair."""
  timestamp: TimestampSeconds
  """Time this price was generated, as a Unix-seconds timestamp."""
  buys: list[FiatConvertReferenceQuoteBuysItem]
  """Reference prices for buying crypto with fiat, one entry per payment method."""
  sells: list[FiatConvertReferenceQuoteSellsItem]
  """Reference prices for selling crypto for fiat, one entry per payment method."""


adapter = validator[FiatConvertReferencePriceResult](FiatConvertReferencePriceResult)


class ReferencePrice(Endpoint):
  """`Get Fiat Convert Reference Price` — mixed into the router that owns `asset.fiat_convert.reference_price`."""

  async def reference_price(
    self,
    *,
    symbol: str,
    validate: bool | None = None,
  ) -> FiatConvertReferencePriceResult:
    """Query the current reference buy/sell prices for one fiat-crypto pair.

    Args:
      symbol: Coin pair, for example `EUR-USDT`.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/asset/fiat-convert/reference-price)
    """
    params: dict = {
      'symbol': symbol,
    }
    r = await self.authed_request('GET', '/v5/fiat/reference-price', params=params)
    return self.result(r, adapter, validate)
