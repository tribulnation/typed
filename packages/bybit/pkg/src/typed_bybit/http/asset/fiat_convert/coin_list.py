# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/fiat/query-coin-list` — Get Fiat Convert Coin List."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class FiatConvertCoinCryptosItem(TypedDict):
  """One supported currency."""

  coin: str
  """Currency code, for example `EUR` or `USDT`."""
  fullName: str
  """Currency's full name."""
  icon: str
  """URL to the light-mode icon."""
  iconNight: str
  """URL to the dark-mode icon."""
  precision: int
  """Decimal precision."""
  disable: bool
  """Whether this currency is currently disabled."""
  singleFromMinLimit: str
  """Minimum transaction amount."""
  singleFromMaxLimit: str
  """Maximum transaction amount."""


class FiatConvertCoinFiatsItem(TypedDict):
  """One supported currency."""

  coin: str
  """Currency code, for example `EUR` or `USDT`."""
  fullName: str
  """Currency's full name."""
  icon: str
  """URL to the light-mode icon."""
  iconNight: str
  """URL to the dark-mode icon."""
  precision: int
  """Decimal precision."""
  disable: bool
  """Whether this currency is currently disabled."""
  singleFromMinLimit: str
  """Minimum transaction amount."""
  singleFromMaxLimit: str
  """Maximum transaction amount."""


class FiatConvertCoinListResult(TypedDict):
  """Supported fiat currencies and crypto coins."""

  fiats: list[FiatConvertCoinFiatsItem]
  """Supported fiat currencies."""
  cryptos: list[FiatConvertCoinCryptosItem]
  """Supported crypto coins."""


adapter = validator[FiatConvertCoinListResult](FiatConvertCoinListResult)


class CoinList(Endpoint):
  """`Get Fiat Convert Coin List` — mixed into the router that owns `asset.fiat_convert.coin_list`."""

  async def coin_list(
    self,
    *,
    side: Literal['0', '1'] | None = None,
    validate: bool | None = None,
  ) -> FiatConvertCoinListResult:
    """List the fiat currencies and crypto coins supported by fiat-convert, for a given transaction direction.

    Args:
      side: Transaction direction to filter to. Omit to return every supported currency.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/asset/fiat-convert/query-coin-list)
    """
    params = {}
    if side is not None:
      params['side'] = side
    r = await self.authed_request('GET', '/v5/fiat/query-coin-list', params=params)
    return self.result(r, adapter, validate)
