# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/asset/withdraw/vasp/list` — Get Available VASPs."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class Vasp(TypedDict):
  """One Virtual Asset Service Provider."""

  vaspEntityId: str
  """Receiver platform identifier; `"others"` for an unlisted exchange."""
  vaspName: str
  """Receiver platform display name."""


class VaspListResult(TypedDict):
  """Available VASPs for this account."""

  vasp: list[Vasp]
  """One entry per VASP."""


adapter = validator[VaspListResult](VaspListResult)


class VaspList(Endpoint):
  """`Get Available VASPs` — mixed into the router that owns `asset.withdraw.vasp_list`."""

  async def vasp_list(self, *, validate: bool | None = None) -> VaspListResult:
    """List the Virtual Asset Service Providers this account may withdraw to, per its compliance zone and UID.

    Args:
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/asset/withdraw/vasp-list)
    """
    r = await self.authed_request('GET', '/v5/asset/withdraw/vasp/list')
    return self.result(r, adapter, validate)
