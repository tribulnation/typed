# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/asset/withdraw/cancel` — Cancel Withdrawal."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class WithdrawCancelRequest(TypedDict):
  """Withdrawal identifier."""

  id: str
  """Withdrawal ID to cancel."""


class WithdrawCancelResult(TypedDict):
  """Cancellation outcome."""

  status: Literal[0, 1]
  """Outcome."""


adapter = validator[WithdrawCancelResult](WithdrawCancelResult)


class Cancel(Endpoint):
  """`Cancel Withdrawal` — mixed into the router that owns `asset.withdraw.cancel`."""

  async def cancel(
    self,
    withdraw_cancel_request: WithdrawCancelRequest,
    *,
    validate: bool | None = None,
  ) -> WithdrawCancelResult:
    """Cancel a pending withdrawal request.

    Args:
      withdraw_cancel_request: Withdrawal to cancel.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/asset/withdraw/cancel-withdraw)
    """
    r = await self.authed_request(
      'POST', '/v5/asset/withdraw/cancel', json=withdraw_cancel_request
    )
    return self.result(r, adapter, validate)
