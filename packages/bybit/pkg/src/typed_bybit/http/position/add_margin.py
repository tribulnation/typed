# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/position/add-margin` — Add Or Reduce Margin."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class AddMarginRequest(TypedDict):
  """Margin adjustment."""

  category: Literal['linear', 'inverse']
  """Product type."""
  symbol: str
  """Symbol name, uppercase only."""
  margin: str
  """Signed margin delta, up to 4 decimals. Positive adds margin (e.g. `10`); negative reduces it (e.g. `-10`)."""
  positionIdx: NotRequired[Literal[0, 1, 2]]
  """Position index, identifying positions in different position modes. Required for a hedge-mode position."""


class AddMarginResult(TypedDict):
  """Position state after the margin adjustment."""

  category: Literal['linear', 'inverse']
  """Product type."""
  symbol: str
  """Symbol name."""
  positionIdx: Literal[0, 1, 2]
  """Position index, identifying positions in different position modes."""
  riskId: int
  """Risk limit ID."""
  riskLimitValue: str
  """Risk limit value."""
  size: str
  """Position size."""
  avgPrice: str
  """Average entry price."""
  liqPrice: str
  """Liquidation price."""
  bustPrice: str
  """Bankruptcy price."""
  markPrice: str
  """Latest mark price."""
  positionValue: str
  """Position value."""
  leverage: str
  """Position leverage."""
  autoAddMargin: Literal[0, 1]
  """Whether margin is added automatically."""
  positionStatus: Literal['Normal', 'Liq', 'Adl']
  """Position status."""
  positionIM: str
  """Initial margin."""
  positionMM: str
  """Maintenance margin."""
  takeProfit: str
  """Take profit price."""
  stopLoss: str
  """Stop loss price."""
  trailingStop: str
  """Trailing stop, as the distance from market price."""
  unrealisedPnl: str
  """Unrealised PnL."""
  cumRealisedPnl: str
  """Cumulative realised PnL."""
  createdTime: TimestampMillis
  """Timestamp of the first time a position was created on this symbol, in milliseconds."""
  updatedTime: TimestampMillis
  """Position last-updated timestamp, in milliseconds."""


adapter = validator[AddMarginResult](AddMarginResult)


class AddMargin(Endpoint):
  """`Add Or Reduce Margin` — mixed into the router that owns `position.add_margin`."""

  async def add_margin(
    self,
    add_margin_request: AddMarginRequest,
    *,
    validate: bool | None = None,
  ) -> AddMarginResult:
    """Manually add or reduce margin for an isolated-margin position.

    Args:
      add_margin_request: Margin adjustment.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/position/manual-add-margin)
    """
    r = await self.authed_request(
      'POST', '/v5/position/add-margin', json=add_margin_request
    )
    return self.result(r, adapter, validate)
