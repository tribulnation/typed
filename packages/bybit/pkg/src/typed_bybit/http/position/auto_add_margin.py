# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/position/set-auto-add-margin` — Set Auto Add Margin."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class AutoAddMarginRequest(TypedDict):
  """Auto-add-margin toggle."""

  category: Literal['linear']
  """Product type."""
  symbol: str
  """Symbol name, uppercase only."""
  autoAddMargin: Literal[0, 1]
  """Turn auto-add-margin on or off."""
  positionIdx: NotRequired[Literal[0, 1, 2]]
  """Position index, identifying positions in different position modes. Required for a hedge-mode position."""


class AutoAddMarginResult(TypedDict):
  """Empty on success."""


adapter = validator[AutoAddMarginResult](AutoAddMarginResult)


class AutoAddMargin(Endpoint):
  """`Set Auto Add Margin` — mixed into the router that owns `position.auto_add_margin`."""

  async def auto_add_margin(
    self,
    auto_add_margin_request: AutoAddMarginRequest,
    *,
    validate: bool | None = None,
  ) -> AutoAddMarginResult:
    """Turn automatic margin top-up on or off for an isolated-margin position.

    Args:
      auto_add_margin_request: Auto-add-margin toggle.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/position/auto-add-margin)
    """
    r = await self.authed_request(
      'POST', '/v5/position/set-auto-add-margin', json=auto_add_margin_request
    )
    return self.result(r, adapter, validate)
