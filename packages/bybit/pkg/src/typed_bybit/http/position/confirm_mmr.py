# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/position/confirm-pending-mmr` — Confirm New Risk Limit."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class ConfirmMmrRequest(TypedDict):
  """Position to confirm."""

  category: Literal['linear', 'inverse']
  """Product type."""
  symbol: str
  """Symbol name."""


class ConfirmMmrResult(TypedDict):
  """Empty on success."""


adapter = validator[ConfirmMmrResult](ConfirmMmrResult)


class ConfirmMmr(Endpoint):
  """`Confirm New Risk Limit` — mixed into the router that owns `position.confirm_mmr`."""

  async def confirm_mmr(
    self,
    confirm_mmr_request: ConfirmMmrRequest,
    *,
    validate: bool | None = None,
  ) -> ConfirmMmrResult:
    """Recalculate a position's risk level after the caller has adjusted it, and clear the position's isReduceOnly mark on success.

    Args:
      confirm_mmr_request: Position to confirm.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/position/confirm-mmr)
    """
    r = await self.authed_request(
      'POST', '/v5/position/confirm-pending-mmr', json=confirm_mmr_request
    )
    return self.result(r, adapter, validate)
