# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/position/switch-mode` — Switch Position Mode."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class SwitchModeRequest(TypedDict):
  """Position mode parameters."""

  category: Literal['linear']
  """Product type."""
  symbol: NotRequired[str]
  """Symbol name, uppercase only. One of `symbol`/`coin` is required; `symbol` takes priority."""
  coin: NotRequired[str]
  """Settle coin, uppercase only. One of `symbol`/`coin` is required."""
  mode: Literal[0, 3]
  """Position mode."""


class SwitchModeResult(TypedDict):
  """Empty on success."""


adapter = validator[SwitchModeResult](SwitchModeResult)


class SwitchMode(Endpoint):
  """`Switch Position Mode` — mixed into the router that owns `position.switch_mode`."""

  async def switch_mode(
    self,
    switch_mode_request: SwitchModeRequest,
    *,
    validate: bool | None = None,
  ) -> SwitchModeResult:
    """Switch between one-way and hedge position mode for USDT perpetual and inverse futures. One-way mode allows only one open position per symbol; hedge mode allows simultaneous Buy- and Sell-side positions.

    Args:
      switch_mode_request: Position mode to set.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/position/position-mode)
    """
    r = await self.authed_request(
      'POST', '/v5/position/switch-mode', json=switch_mode_request
    )
    return self.result(r, adapter, validate)
