# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/position/set-leverage` — Set Leverage."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class SetLeverageRequest(TypedDict):
  """Leverage parameters."""

  category: Literal['linear', 'inverse']
  """Product type."""
  symbol: str
  """Symbol name, uppercase only."""
  buyLeverage: str
  """Buy-side leverage. Range [1, max leverage for the symbol]."""
  sellLeverage: str
  """Sell-side leverage. Range [1, max leverage for the symbol]."""


class SetLeverageResult(TypedDict):
  """Empty on success."""


adapter = validator[SetLeverageResult](SetLeverageResult)


class SetLeverage(Endpoint):
  """`Set Leverage` — mixed into the router that owns `position.set_leverage`."""

  async def set_leverage(
    self,
    set_leverage_request: SetLeverageRequest,
    *,
    validate: bool | None = None,
  ) -> SetLeverageResult:
    """Set the leverage for a symbol. Per the risk limit, higher leverage allows a smaller maximum position value and vice versa.

    Args:
      set_leverage_request: Leverage to set.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/position/leverage)
    """
    r = await self.authed_request(
      'POST', '/v5/position/set-leverage', json=set_leverage_request
    )
    return self.result(r, adapter, validate)
