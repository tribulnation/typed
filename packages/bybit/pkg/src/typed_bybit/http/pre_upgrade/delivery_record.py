# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/pre-upgrade/asset/delivery-record` — Get Pre-upgrade Delivery Record."""

from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class PreUpgradeDeliveryRecord(TypedDict):
  """One option delivery, recorded before this account's upgrade to a Unified account."""

  symbol: str
  """Symbol name."""
  side: Literal['Buy', 'Sell']
  """Side."""
  deliveryTime: TimestampMillis
  """Delivery time."""
  strike: str
  """Exercise price."""
  fee: str
  """Trading fee."""
  position: str
  """Executed position size."""
  deliveryPrice: str
  """Delivery price."""
  deliveryRpl: str
  """Realized profit or loss from delivery."""


class PreUpgradeDeliveryRecordResult(TypedDict):
  """Page of matching pre-upgrade option delivery records."""

  category: Literal['option']
  """Product type."""
  nextPageCursor: NotRequired[str]
  """Opaque cursor for the next page. Pass it back as `cursor`; an empty string means there are no further pages."""
  list: list[PreUpgradeDeliveryRecord]
  """Matching delivery records, newest first by `deliveryTime`."""


adapter = validator[PreUpgradeDeliveryRecordResult](PreUpgradeDeliveryRecordResult)


class DeliveryRecord(Endpoint):
  """`Get Pre-upgrade Delivery Record` — mixed into the router that owns `pre_upgrade.delivery_record`."""

  async def delivery_record(
    self,
    *,
    category: Literal['option'],
    symbol: str | None = None,
    exp_date: str | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> PreUpgradeDeliveryRecordResult:
    """Query option delivery records from before this account upgraded to a Unified account, sorted by `deliveryTime` descending. Data available for the most recent 6 months.

    Args:
      category: Product type.
      symbol: Symbol name, uppercase only. Omit to return every symbol.
      exp_date: Option expiry date, for example `25MAR22`. Omit to return every expiry.
      limit: Number of records per page. Range [1, 50]; defaults to 20.
      cursor: Opaque pagination cursor. Pass the `nextPageCursor` returned by the previous page; omit for the first page.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/pre-upgrade/delivery)
    """
    params: dict = {
      'category': category,
    }
    if symbol is not None:
      params['symbol'] = symbol
    if exp_date is not None:
      params['expDate'] = exp_date
    if limit is not None:
      params['limit'] = limit
    if cursor is not None:
      params['cursor'] = cursor
    r = await self.authed_request(
      'GET', '/v5/pre-upgrade/asset/delivery-record', params=params
    )
    return self.result(r, adapter, validate)

  async def delivery_record_paged(
    self,
    *,
    category: Literal['option'],
    symbol: str | None = None,
    exp_date: str | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[PreUpgradeDeliveryRecordResult]:
    """Yield successive pages of `delivery_record`.

    Passes each page's token back as `cursor` and stops when a response carries no
    `nextPageCursor`, or after `max_pages` pages when one is given.
    """
    cursor: str | None = None
    pages = 0
    while True:
      response = await self.delivery_record(
        category=category,
        symbol=symbol,
        exp_date=exp_date,
        limit=limit,
        cursor=cursor,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      cursor = response.get('nextPageCursor') if response is not None else None
      if not cursor:
        break
