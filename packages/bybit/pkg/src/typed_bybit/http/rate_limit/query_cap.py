# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/apilimit/query-cap` — Get Rate Limit Cap."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class RateLimitCap(TypedDict):
  """Usage and cap for one business type."""

  bizType: Literal['SPOT', 'OPTIONS', 'DERIVATIVES']
  """Business type."""
  totalRate: int
  """Total API rate limit usage across all UIDs, in requests per second."""
  insCap: int
  """Institutional-level API rate limit cap, in requests per second. Depends on the account's pro level."""
  uidCap: int
  """UID-level API rate limit cap, in requests per second."""


class RateLimitCapResult(TypedDict):
  """Total rate limit usage and cap per business type."""

  list: list[RateLimitCap]
  """Usage and cap per business type."""


adapter = validator[RateLimitCapResult](RateLimitCapResult)


class QueryCap(Endpoint):
  """`Get Rate Limit Cap` — mixed into the router that owns `rate_limit.query_cap`."""

  async def query_cap(self, *, validate: bool | None = None) -> RateLimitCapResult:
    """Get this institution's total rate limit usage and cap across all UIDs, per business type. Main and sub UIDs may both call this, with visibility scoped by account hierarchy. Self-service, restricted to PRO1-PRO6/MM1-MM3 institutional tiers.

    Args:
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/rate-limit/rules-for-pros/apilimit-query-cap)
    """
    r = await self.authed_request('GET', '/v5/apilimit/query-cap')
    return self.result(r, adapter, validate)
