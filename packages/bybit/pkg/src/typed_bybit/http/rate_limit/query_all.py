# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/apilimit/query-all` — Get All Rate Limits."""

from typing_extensions import AsyncIterator, Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class RateLimitEntry(TypedDict):
  """Rate limit for one UID group and business type."""

  uids: str
  """UIDs sharing this rate limit, comma-separated."""
  bizType: Literal['SPOT', 'OPTIONS', 'DERIVATIVES']
  """Business type this limit applies to."""
  rate: int
  """API rate limit, in requests per second."""


class RateLimitQueryAllResult(TypedDict):
  """Page of rate limit entries."""

  nextPageCursor: str
  """Opaque cursor for the next page. Pass it back as `cursor`; an empty string means there are no further pages."""
  list: list[RateLimitEntry]
  """Rate limit entries on this page."""


adapter = validator[RateLimitQueryAllResult](RateLimitQueryAllResult)


class QueryAll(Endpoint):
  """`Get All Rate Limits` — mixed into the router that owns `rate_limit.query_all`."""

  async def query_all(
    self,
    *,
    limit: int | None = None,
    cursor: str | None = None,
    uids: str | None = None,
    validate: bool | None = None,
  ) -> RateLimitQueryAllResult:
    """Get every UID-level API rate limit under this account -- all master accounts and sub-accounts. Self-service, restricted to PRO1-PRO6/MM1-MM3 institutional tiers.

    Args:
      limit: Data size per page. Range [1, 1000]; defaults to 1000.
      cursor: Opaque pagination cursor. Pass the `nextPageCursor` returned by the previous page; omit it for the first page.
      uids: UIDs to filter by, comma-separated, spanning master and sub-accounts. Omit to return every UID.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/rate-limit/rules-for-pros/apilimit-query-all)
    """
    params = {}
    if limit is not None:
      params['limit'] = limit
    if cursor is not None:
      params['cursor'] = cursor
    if uids is not None:
      params['uids'] = uids
    r = await self.authed_request('GET', '/v5/apilimit/query-all', params=params)
    return self.result(r, adapter, validate)

  async def query_all_paged(
    self,
    *,
    limit: int | None = None,
    uids: str | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[RateLimitQueryAllResult]:
    """Yield successive pages of `query_all`.

    Passes each page's token back as `cursor` and stops when a response carries no
    `nextPageCursor`, or after `max_pages` pages when one is given.
    """
    cursor: str | None = None
    pages = 0
    while True:
      response = await self.query_all(
        limit=limit, uids=uids, cursor=cursor, validate=validate
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      cursor = response.get('nextPageCursor') if response is not None else None
      if not cursor:
        break
