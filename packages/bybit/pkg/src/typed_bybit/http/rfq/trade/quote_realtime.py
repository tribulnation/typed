# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/rfq/quote-realtime` — Get Quotes (Real-Time)."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class QuoteRealtimeLegQuoteBuyListItem(TypedDict):
  """A bid or ask leg of a quote."""

  category: Literal['spot', 'linear', 'option']
  """Product type."""
  symbol: str
  """Instrument name."""
  price: str
  """Quote or execution price."""
  qty: str
  """Trade quantity."""
  isHedge: bool
  """Whether this leg is the delta hedge leg."""


class QuoteRealtimeLegQuoteSellListItem(TypedDict):
  """A bid or ask leg of a quote."""

  category: Literal['spot', 'linear', 'option']
  """Product type."""
  symbol: str
  """Instrument name."""
  price: str
  """Quote or execution price."""
  qty: str
  """Trade quantity."""
  isHedge: bool
  """Whether this leg is the delta hedge leg."""


class QuoteRealtimeRecord(TypedDict):
  """One non-final quote."""

  rfqId: str
  """Inquiry ID being quoted."""
  rfqLinkId: str
  """Inquirer's custom RFQ ID; not publicly disclosed."""
  quoteId: str
  """Quote ID."""
  quoteLinkId: str
  """Custom quote ID; not publicly disclosed."""
  expiresAt: TimestampMillis
  """Expiration timestamp."""
  deskCode: str
  """Quoting party's identifier; hidden if `anonymous`."""
  anonymous: bool
  """Whether the quote is anonymous."""
  status: Literal['Active', 'PendingFill', 'Canceled', 'Filled', 'Expired', 'Failed']
  """Quote status."""
  execQuoteSide: NotRequired[Literal['Buy', 'Sell']]
  """Executed direction, once filled."""
  createdAt: TimestampMillis
  """Creation timestamp."""
  updatedAt: TimestampMillis
  """Last update timestamp."""
  quoteBuyList: NotRequired[list[QuoteRealtimeLegQuoteBuyListItem]]
  """Bid-side legs."""
  quoteSellList: NotRequired[list[QuoteRealtimeLegQuoteSellListItem]]
  """Ask-side legs (same shape as `quoteBuyList`)."""


class QuoteRealtimeResult(TypedDict):
  """The current non-final quotes."""

  list: list[QuoteRealtimeRecord]
  """Non-final quote records."""


adapter = validator[QuoteRealtimeResult](QuoteRealtimeResult)


class QuoteRealtime(Endpoint):
  """`Get Quotes (Real-Time)` — mixed into the router that owns `rfq.trade.quote_realtime`."""

  async def quote_realtime(
    self,
    *,
    rfq_id: str | None = None,
    quote_id: str | None = None,
    quote_link_id: str | None = None,
    trader_type: Literal['quote', 'request'] | None = None,
    validate: bool | None = None,
  ) -> QuoteRealtimeResult:
    """Obtain real-time (non-final) quote information, sorted by creation time descending. Up to 50 requests per second.

    Args:
      rfq_id: Filter to quotes on one inquiry.
      quote_id: Filter to one quote ID.
      quote_link_id: Filter to one custom quote ID; invalid when `traderType` is `request`.
      trader_type: Role to query as. Defaults to `quote`.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/rfq/trade/quote-realtime)
    """
    params = {}
    if rfq_id is not None:
      params['rfqId'] = rfq_id
    if quote_id is not None:
      params['quoteId'] = quote_id
    if quote_link_id is not None:
      params['quoteLinkId'] = quote_link_id
    if trader_type is not None:
      params['traderType'] = trader_type
    r = await self.authed_request('GET', '/v5/rfq/quote-realtime', params=params)
    return self.result(r, adapter, validate)
