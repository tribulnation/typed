# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/rfq/cancel-all-quotes` — Cancel All Quotes."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class CancelAllQuotesResult(TypedDict):
  """The cancellation outcome for one quote."""

  rfqId: str
  """Inquiry ID."""
  quoteId: str
  """Quote ID."""
  quoteLinkId: str
  """Custom quote ID."""
  code: str
  """Cancellation result code; `"0"` means success."""
  msg: str
  """Cancellation failure reason; empty on success."""


adapter = validator[list[CancelAllQuotesResult]](list[CancelAllQuotesResult])


class CancelAllQuotes(Endpoint):
  """`Cancel All Quotes` — mixed into the router that owns `rfq.trade.cancel_all_quotes`."""

  async def cancel_all_quotes(
    self,
    *,
    validate: bool | None = None,
  ) -> list[CancelAllQuotesResult]:
    """Cancel every active quote for this account. Up to 50 requests per second.

    Args:
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/rfq/trade/cancel-all-quotes)
    """
    r = await self.authed_request('POST', '/v5/rfq/cancel-all-quotes')
    return self.result(r, adapter, validate)
