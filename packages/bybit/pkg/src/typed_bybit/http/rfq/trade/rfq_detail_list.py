# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/rfq/rfq-detail-list` — Get RFQ Details."""

from typed_bybit.core import Endpoint, TimestampMillis, timestamp_millis, validator
from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict


class RfqDetailTradeLeg(TypedDict):
  """One executed leg."""

  orderId: str
  """Bybit order ID."""
  price: str
  """Execution price."""
  qty: str
  """Execution quantity."""
  markPrice: str
  """Mark price at execution time."""
  execFee: str
  """Fee paid, in the base currency."""
  execId: str
  """Unique execution ID."""
  resultCode: int
  """Execution result code; `0` means success."""
  resultMessage: str
  """Execution error message; empty on success."""


class RfqHedgeLegDetail(TypedDict):
  """A delta hedge leg."""

  category: Literal['spot', 'linear', 'option']
  """Product type."""
  symbol: str
  """Hedge contract name."""
  side: Literal['Buy', 'Sell']
  """Order side."""
  qty: str
  """Trade quantity."""
  price: NotRequired[str]
  """Taker's desired execution price, reference only."""


class RfqLegDetail(TypedDict):
  """One leg of the combination transaction."""

  category: Literal['spot', 'linear', 'option']
  """Product type."""
  symbol: str
  """Instrument name."""
  side: Literal['Buy', 'Sell']
  """Order side."""
  qty: str
  """Trade quantity."""


class RfqQuoteLegDetailQuoteBuyListItem(TypedDict):
  """A bid or ask price on one leg."""

  category: Literal['spot', 'linear', 'option']
  """Product type."""
  symbol: str
  """Instrument name."""
  price: str
  """Quote or execution price."""


class RfqQuoteLegDetailQuoteSellListItem(TypedDict):
  """A bid or ask price on one leg."""

  category: Literal['spot', 'linear', 'option']
  """Product type."""
  symbol: str
  """Instrument name."""
  price: str
  """Quote or execution price."""


class RfqDetailQuote(TypedDict):
  """One quote on this RFQ."""

  quoteId: str
  """Quote ID."""
  status: Literal['Active', 'PendingFill', 'Canceled', 'Filled', 'Expired', 'Failed']
  """Quote status."""
  quoteBuyList: NotRequired[list[RfqQuoteLegDetailQuoteBuyListItem]]
  """Bid-side legs."""
  quoteSellList: NotRequired[list[RfqQuoteLegDetailQuoteSellListItem]]
  """Ask-side legs."""


class RfqDetailRecord(TypedDict):
  """One historical RFQ, with nested quotes and execution legs."""

  rfqId: str
  """Inquiry ID."""
  rfqLinkId: str
  """Custom inquiry ID; empty for a quoting party."""
  counterparties: list[str]
  """Desk codes of the invited bidders."""
  expiresAt: TimestampMillis
  """Expiration timestamp."""
  strategyType: str
  """Inquiry strategy label."""
  status: Literal['Active', 'PendingFill', 'Canceled', 'Filled', 'Expired', 'Failed']
  """RFQ status."""
  acceptOtherQuoteStatus: bool
  """Whether non-LP quotes may be accepted."""
  execQuoteId: NotRequired[str]
  """The executed quote ID, once filled."""
  execQuoteSide: NotRequired[Literal['Buy', 'Sell']]
  """Executed direction, once filled."""
  deskCode: str
  """Inquiring party's identifier; hidden if `anonymous`."""
  anonymous: bool
  """Whether the RFQ is anonymous."""
  createdAt: TimestampMillis
  """Creation timestamp."""
  updatedAt: TimestampMillis
  """Last update timestamp."""
  legs: list[RfqLegDetail]
  """Combination transaction legs."""
  hedge: NotRequired[list[RfqHedgeLegDetail]]
  """Delta hedge legs, when the RFQ carried one."""
  quoteList: NotRequired[list[RfqDetailQuote]]
  """Active quotes on this RFQ (`request`-type queries only)."""
  tradeLegs: NotRequired[list[RfqDetailTradeLeg]]
  """Execution records, once this RFQ has traded."""


class RfqDetailListResult(TypedDict):
  """A page of historical RFQ details."""

  cursor: str
  """Pagination cursor for the next page; empty when there are no more pages."""
  list: list[RfqDetailRecord]
  """RFQ detail records."""


adapter = validator[RfqDetailListResult](RfqDetailListResult)


class RfqDetailList(Endpoint):
  """`Get RFQ Details` — mixed into the router that owns `rfq.trade.rfq_detail_list`."""

  async def rfq_detail_list(
    self,
    *,
    rfq_id: str | None = None,
    rfq_link_id: str | None = None,
    status: Literal['Active', 'PendingFill', 'Canceled', 'Filled', 'Expired', 'Failed']
    | None = None,
    trader_type: Literal['quote', 'request'] | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> RfqDetailListResult:
    """Obtain historical RFQ details, including nested quotes and execution legs, sorted by creation time descending. Up to 50 requests per second.

    Args:
      rfq_id: Filter to one inquiry ID.
      rfq_link_id: Filter to one custom inquiry ID. Only searches the last 3 months.
      status: Filter by RFQ status.
      trader_type: Role to query as. Defaults to `quote`. Defaults to `request` on this endpoint (unlike its siblings, which default to `quote`).
      start_time: Start of the queried range, as a millisecond timestamp.
      end_time: End of the queried range, as a millisecond timestamp.
      limit: Items per page. Range [1, 100].
      cursor: Pagination cursor, from a previous response's own `cursor`.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/rfq/trade/rfq-detail-list)
    """
    params = {}
    if rfq_id is not None:
      params['rfqId'] = rfq_id
    if rfq_link_id is not None:
      params['rfqLinkId'] = rfq_link_id
    if status is not None:
      params['status'] = status
    if trader_type is not None:
      params['traderType'] = trader_type
    if start_time is not None:
      params['startTime'] = timestamp_millis.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp_millis.dump(end_time)
    if limit is not None:
      params['limit'] = limit
    if cursor is not None:
      params['cursor'] = cursor
    r = await self.authed_request('GET', '/v5/rfq/rfq-detail-list', params=params)
    return self.result(r, adapter, validate)

  async def rfq_detail_list_paged(
    self,
    *,
    rfq_id: str | None = None,
    rfq_link_id: str | None = None,
    status: Literal['Active', 'PendingFill', 'Canceled', 'Filled', 'Expired', 'Failed']
    | None = None,
    trader_type: Literal['quote', 'request'] | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[RfqDetailListResult]:
    """Yield successive pages of `rfq_detail_list`.

    Passes each page's token back as `cursor` and stops when a response carries no
    `cursor`, or after `max_pages` pages when one is given.
    """
    cursor: str | None = None
    pages = 0
    while True:
      response = await self.rfq_detail_list(
        rfq_id=rfq_id,
        rfq_link_id=rfq_link_id,
        status=status,
        trader_type=trader_type,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        cursor=cursor,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      cursor = response.get('cursor') if response is not None else None
      if not cursor:
        break
