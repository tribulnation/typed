# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/rfq/cancel-quote` — Cancel Quote."""

from typing_extensions import NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class CancelQuoteRequest(TypedDict):
  """Identifies the quote to cancel."""

  quoteId: NotRequired[str]
  """Quote ID."""
  rfqId: NotRequired[str]
  """Inquiry ID."""
  quoteLinkId: NotRequired[str]
  """Custom quote ID."""


class CancelQuoteResult(TypedDict):
  """Identifiers of the cancelled quote."""

  rfqId: str
  """Inquiry ID."""
  quoteId: str
  """Quote ID."""
  quoteLinkId: str
  """Custom quote ID."""


adapter = validator[CancelQuoteResult](CancelQuoteResult)


class CancelQuote(Endpoint):
  """`Cancel Quote` — mixed into the router that owns `rfq.trade.cancel_quote`."""

  async def cancel_quote(
    self,
    cancel_quote_request: CancelQuoteRequest,
    *,
    validate: bool | None = None,
  ) -> CancelQuoteResult:
    """Cancel a quote by quote ID, custom quote ID, or inquiry ID. Up to 50 requests per second.

    Args:
      cancel_quote_request: Quote to cancel.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/rfq/trade/cancel-quote)
    """
    r = await self.authed_request(
      'POST', '/v5/rfq/cancel-quote', json=cancel_quote_request
    )
    return self.result(r, adapter, validate)
