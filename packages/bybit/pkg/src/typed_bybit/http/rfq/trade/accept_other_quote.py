# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/rfq/accept-other-quote` — Accept Non-LP Quote."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class AcceptOtherQuoteRequest(TypedDict):
  """Identifies the RFQ."""

  rfqId: str
  """Inquiry ID."""


class AcceptOtherQuoteResult(TypedDict):
  """Identifies the RFQ that was updated."""

  rfqId: str
  """Inquiry ID."""


adapter = validator[AcceptOtherQuoteResult](AcceptOtherQuoteResult)


class AcceptOtherQuote(Endpoint):
  """`Accept Non-LP Quote` — mixed into the router that owns `rfq.trade.accept_other_quote`."""

  async def accept_other_quote(
    self,
    accept_other_quote_request: AcceptOtherQuoteRequest,
    *,
    validate: bool | None = None,
  ) -> AcceptOtherQuoteResult:
    """Accept quotes from non-liquidity-provider counterparties on an RFQ. Up to 50 requests per second.

    Args:
      accept_other_quote_request: RFQ to accept non-LP quotes on.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/rfq/trade/accept-other-quote)
    """
    r = await self.authed_request(
      'POST', '/v5/rfq/accept-other-quote', json=accept_other_quote_request
    )
    return self.result(r, adapter, validate)
