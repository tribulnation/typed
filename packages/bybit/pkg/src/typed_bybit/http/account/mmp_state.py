# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/account/mmp-state` — Get MMP State."""

from typing_extensions import NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class MmpState(TypedDict):
  """MMP state for one base coin."""

  baseCoin: str
  """Base coin."""
  mmpEnabled: bool
  """Whether MMP is enabled on the account."""
  window: str
  """Time window, in milliseconds."""
  frozenPeriod: str
  """Frozen period, in milliseconds. `"0"` means frozen until manually reset."""
  qtyLimit: str
  """Trade quantity limit."""
  deltaLimit: str
  """Delta limit."""
  vegaLimit: str
  """Vega limit."""
  mmpFrozenUntil: NotRequired[TimestampMillis]
  """Timestamp the freeze lifts at, as a millisecond timestamp. Only meaningful when `mmpFrozen` is true."""
  mmpFrozen: bool
  """Whether MMP is currently triggered and the account frozen."""


adapter = validator[list[MmpState]](list[MmpState])


class MmpStateEndpoint(Endpoint):
  """`Get MMP State` — mixed into the router that owns `account.mmp_state`."""

  async def mmp_state(
    self,
    *,
    base_coin: str,
    validate: bool | None = None,
  ) -> list[MmpState]:
    """Query the account's current Market Maker Protection (MMP) state for one base coin. MMP applies to options only and requires prior enablement by Bybit's institutional team.

    Args:
      base_coin: Base coin, uppercase only.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/account/get-mmp-state)
    """
    params: dict = {
      'baseCoin': base_coin,
    }
    r = await self.authed_request('GET', '/v5/account/mmp-state', params=params)
    return self.result(r, adapter, validate)
