# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/account/set-margin-mode` — Set Margin Mode."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class SetMarginModeReason(TypedDict):
  """One reason the switch could not be applied."""

  reasonCode: str
  """Failure reason code."""
  reasonMsg: str
  """Human-readable failure description."""


class SetMarginModeRequest(TypedDict):
  """Set-margin-mode parameters."""

  setMarginMode: Literal['ISOLATED_MARGIN', 'REGULAR_MARGIN', 'PORTFOLIO_MARGIN']
  """Desired margin mode."""


class SetMarginModeResult(TypedDict):
  """Margin mode switch outcome."""

  reasons: list[SetMarginModeReason]
  """Failure reasons; empty on a fully successful switch."""


adapter = validator[SetMarginModeResult](SetMarginModeResult)


class SetMarginMode(Endpoint):
  """`Set Margin Mode` — mixed into the router that owns `account.set_margin_mode`."""

  async def set_margin_mode(
    self,
    set_margin_mode_request: SetMarginModeRequest,
    *,
    validate: bool | None = None,
  ) -> SetMarginModeResult:
    """Set the account's margin trading mode. Defaults to regular (cross) margin.

    Args:
      set_margin_mode_request: Desired margin mode.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/account/set-margin-mode)
    """
    r = await self.authed_request(
      'POST', '/v5/account/set-margin-mode', json=set_margin_mode_request
    )
    return self.result(r, adapter, validate)
