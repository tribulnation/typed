# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/account/smp-group` — Get SMP Group ID."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class SmpGroupResult(TypedDict):
  """SMP group ID."""

  smpGroup: int
  """SMP group ID. `0` when the account has no group."""


adapter = validator[SmpGroupResult](SmpGroupResult)


class SmpGroup(Endpoint):
  """`Get SMP Group ID` — mixed into the router that owns `account.smp_group`."""

  async def smp_group(self, *, validate: bool | None = None) -> SmpGroupResult:
    """Query the self-match-prevention (SMP) group ID of the account.

    Args:
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/account/smp-group)
    """
    r = await self.authed_request('GET', '/v5/account/smp-group')
    return self.result(r, adapter, validate)
