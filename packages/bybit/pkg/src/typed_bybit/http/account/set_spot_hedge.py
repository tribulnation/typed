# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/account/set-hedging-mode` — Set Spot Hedging."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class SetSpotHedgeRequest(TypedDict):
  """Set-spot-hedging parameters."""

  setHedgingMode: Literal['ON', 'OFF']
  """Desired hedging state."""


class SetSpotHedgeResult(TypedDict):
  """Empty object on success."""


adapter = validator[SetSpotHedgeResult](SetSpotHedgeResult)


class SetSpotHedge(Endpoint):
  """`Set Spot Hedging` — mixed into the router that owns `account.set_spot_hedge`."""

  async def set_spot_hedge(
    self,
    set_spot_hedge_request: SetSpotHedgeRequest,
    *,
    validate: bool | None = None,
  ) -> SetSpotHedgeResult:
    """Enable or disable the spot-hedging feature within a Portfolio Margin account.

    Args:
      set_spot_hedge_request: Desired hedging state.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/account/set-spot-hedge)
    """
    r = await self.authed_request(
      'POST', '/v5/account/set-hedging-mode', json=set_spot_hedge_request
    )
    return self.result(r, adapter, validate)
