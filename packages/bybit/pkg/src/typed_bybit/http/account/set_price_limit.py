# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/account/set-limit-px-action` — Set Price Limit Behaviour."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class SetPriceLimitRequest(TypedDict):
  """Set-price-limit parameters."""

  category: Literal['linear', 'inverse', 'spot']
  """Product type."""
  modifyEnable: bool
  """When `true`, out-of-range order prices are auto-adjusted to the nearest allowed level. When `false`, they are rejected."""


class SetPriceLimitResult(TypedDict):
  """Empty object on success."""


adapter = validator[SetPriceLimitResult](SetPriceLimitResult)


class SetPriceLimit(Endpoint):
  """`Set Price Limit Behaviour` — mixed into the router that owns `account.set_price_limit`."""

  async def set_price_limit(
    self,
    set_price_limit_request: SetPriceLimitRequest,
    *,
    validate: bool | None = None,
  ) -> SetPriceLimitResult:
    """Configure how the account handles a limit order whose price exceeds the market's allowed range: reject it, or auto-adjust it to the nearest allowed price. Configured per category.

    Args:
      set_price_limit_request: Category and desired behaviour.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/account/set-price-limit)
    """
    r = await self.authed_request(
      'POST', '/v5/account/set-limit-px-action', json=set_price_limit_request
    )
    return self.result(r, adapter, validate)
