# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/account/transaction-log` — Get Transaction Log."""

from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, timestamp_millis, validator


class TransactionLogEntry(TypedDict):
  """One transaction log entry."""

  id: str
  """Unique entry identifier."""
  symbol: str
  """Symbol name; empty when not applicable."""
  category: Literal['spot', 'linear', 'option', 'inverse']
  """Product type."""
  side: Literal['Buy', 'Sell', 'None']
  """Direction."""
  transactionTime: TimestampMillis
  """Transaction time, as a millisecond timestamp."""
  transSubType: NotRequired[str]
  """Transaction log sub-type; empty for most transactions."""
  qty: str
  """Quantity. Negative decreases, positive increases, for spot; unsigned for perpetuals/futures."""
  size: NotRequired[str]
  """Position size after this entry, signed by direction."""
  currency: str
  """Currency, for example `USDT`, `USDC`, `BTC`, `ETH`."""
  tradePrice: NotRequired[str]
  """Execution price."""
  funding: NotRequired[str]
  """Funding fee; positive when received, negative when paid."""
  fee: NotRequired[str]
  """Trading fee; positive when charged, negative when a rebate."""
  cashFlow: NotRequired[str]
  """Cash flow from a position close, settlement, or transfer."""
  change: str
  """Change to the wallet balance: `cashFlow + funding - fee`."""
  cashBalance: str
  """Wallet balance after this entry."""
  feeRate: NotRequired[str]
  """Trading or funding fee rate."""
  bonusChange: NotRequired[str]
  """Change in bonus amount."""
  tradeId: NotRequired[str]
  """Trade identifier."""
  orderId: NotRequired[str]
  """Order identifier."""
  orderLinkId: NotRequired[str]
  """User-customised order ID."""
  extraFees: NotRequired[str]
  """Additional fee detail. Returned only for Indonesian-site or EU fiat spot orders."""
  displayType: NotRequired[str]
  """UI display-type designation."""
  type: Literal[
    'TRANSFER_IN',
    'TRANSFER_OUT',
    'TRADE',
    'SETTLEMENT',
    'DELIVERY',
    'LIQUIDATION',
    'ADL',
    'AIRDROP',
    'BONUS',
    'BONUS_RECOLLECT',
    'FEE_REFUND',
    'INTEREST',
    'CURRENCY_BUY',
    'CURRENCY_SELL',
    'DIVIDEND_SETTLEMENT',
    'FORWARD_SPLIT_SETTLE',
    'REVERSE_SPLIT_SETTLE',
  ]
  """Transaction log type."""


class TransactionLogResult(TypedDict):
  """Page of matching transaction log entries."""

  nextPageCursor: NotRequired[str | None]
  """Opaque cursor for the next page. Pass it back as `cursor`; `null` (observed live) or an empty string means there are no further pages."""
  list: list[TransactionLogEntry]
  """Matching transaction log entries."""


adapter = validator[TransactionLogResult](TransactionLogResult)


class TransactionLog(Endpoint):
  """`Get Transaction Log` — mixed into the router that owns `account.transaction_log`."""

  async def transaction_log(
    self,
    *,
    account_type: Literal['UNIFIED'] | None = None,
    category: Literal['spot', 'linear', 'option', 'inverse'] | None = None,
    currency: str | None = None,
    base_coin: str | None = None,
    type: Literal[
      'TRANSFER_IN',
      'TRANSFER_OUT',
      'TRADE',
      'SETTLEMENT',
      'DELIVERY',
      'LIQUIDATION',
      'ADL',
      'AIRDROP',
      'BONUS',
      'BONUS_RECOLLECT',
      'FEE_REFUND',
      'INTEREST',
      'CURRENCY_BUY',
      'CURRENCY_SELL',
      'DIVIDEND_SETTLEMENT',
      'FORWARD_SPLIT_SETTLE',
      'REVERSE_SPLIT_SETTLE',
    ]
    | None = None,
    trans_sub_type: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> TransactionLogResult:
    """Query transaction logs in a Unified account, up to 2 years of history. May experience increased latency during extreme market volatility.

    Args:
      account_type: Account type.
      category: Product type.
      currency: Currency, uppercase only.
      base_coin: Base coin, uppercase only, for example `BTC` of `BTCPERP`.
      type: Transaction log type.
      trans_sub_type: Transaction log sub-type. Only `movePosition` is documented explicitly; empty string otherwise.
      start_time: Start of the queried range, as a millisecond timestamp. Neither passed: last 24 hours. Only `startTime`: forward 24 hours. Only `endTime`: back 24 hours. Both: `endTime - startTime <= 7 days`.
      end_time: End of the queried range, as a millisecond timestamp.
      limit: Number of log entries per page. Range [1, 50]; defaults to 20.
      cursor: Opaque pagination cursor. Pass the `nextPageCursor` returned by the previous page; omit it for the first page.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/account/transaction-log)
    """
    params = {}
    if account_type is not None:
      params['accountType'] = account_type
    if category is not None:
      params['category'] = category
    if currency is not None:
      params['currency'] = currency
    if base_coin is not None:
      params['baseCoin'] = base_coin
    if type is not None:
      params['type'] = type
    if trans_sub_type is not None:
      params['transSubType'] = trans_sub_type
    if start_time is not None:
      params['startTime'] = timestamp_millis.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp_millis.dump(end_time)
    if limit is not None:
      params['limit'] = limit
    if cursor is not None:
      params['cursor'] = cursor
    r = await self.authed_request('GET', '/v5/account/transaction-log', params=params)
    return self.result(r, adapter, validate)

  async def transaction_log_paged(
    self,
    *,
    account_type: Literal['UNIFIED'] | None = None,
    category: Literal['spot', 'linear', 'option', 'inverse'] | None = None,
    currency: str | None = None,
    base_coin: str | None = None,
    type: Literal[
      'TRANSFER_IN',
      'TRANSFER_OUT',
      'TRADE',
      'SETTLEMENT',
      'DELIVERY',
      'LIQUIDATION',
      'ADL',
      'AIRDROP',
      'BONUS',
      'BONUS_RECOLLECT',
      'FEE_REFUND',
      'INTEREST',
      'CURRENCY_BUY',
      'CURRENCY_SELL',
      'DIVIDEND_SETTLEMENT',
      'FORWARD_SPLIT_SETTLE',
      'REVERSE_SPLIT_SETTLE',
    ]
    | None = None,
    trans_sub_type: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[TransactionLogResult]:
    """Yield successive pages of `transaction_log`.

    Passes each page's token back as `cursor` and stops when a response carries no
    `nextPageCursor`, or after `max_pages` pages when one is given.
    """
    cursor: str | None = None
    pages = 0
    while True:
      response = await self.transaction_log(
        account_type=account_type,
        category=category,
        currency=currency,
        base_coin=base_coin,
        type=type,
        trans_sub_type=trans_sub_type,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        cursor=cursor,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      cursor = response.get('nextPageCursor') if response is not None else None
      if not cursor:
        break
