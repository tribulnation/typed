# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/account/option-asset-info` — Get Option Asset Info."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class OptionAssetInfo(TypedDict):
  """Option asset P&L for one coin."""

  coin: str
  """Coin name."""
  totalDelta: str
  """Aggregate delta from option positions only."""
  totalRPL: str
  """Total realised profit/loss."""
  totalUPL: str
  """Total unrealised profit/loss."""
  assetIM: str
  """Asset initial margin, including margin occupied by open option orders."""
  assetMM: str
  """Asset maintenance margin, including margin occupied by open option orders."""
  sendTime: TimestampMillis
  """Snapshot time, as a millisecond timestamp."""


adapter = validator[list[OptionAssetInfo]](list[OptionAssetInfo])


class OptionAssetInfoEndpoint(Endpoint):
  """`Get Option Asset Info` — mixed into the router that owns `account.option_asset_info`."""

  async def option_asset_info(
    self,
    *,
    validate: bool | None = None,
  ) -> list[OptionAssetInfo]:
    """Query the option asset profit and loss information for each coin under the account.

    Args:
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/account/option-asset-info)
    """
    r = await self.authed_request('GET', '/v5/account/option-asset-info')
    return self.result(r, adapter, validate)
