# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/account/set-collateral-switch` — Set Collateral Coin."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class SetCollateralRequest(TypedDict):
  """Set-collateral parameters."""

  coin: str
  """Coin to configure, uppercase only. `USDT`/`USDC` cannot be set."""
  collateralSwitch: Literal['ON', 'OFF']
  """Desired collateral state."""


class SetCollateralResult(TypedDict):
  """Empty object on success."""


adapter = validator[SetCollateralResult](SetCollateralResult)


class SetCollateral(Endpoint):
  """`Set Collateral Coin` — mixed into the router that owns `account.set_collateral`."""

  async def set_collateral(
    self,
    set_collateral_request: SetCollateralRequest,
    *,
    validate: bool | None = None,
  ) -> SetCollateralResult:
    """Set whether one coin held in a Unified account is usable as margin collateral. `USDT`/`USDC` cannot be configured; discover eligible coins via account.collateral_info.

    Args:
      set_collateral_request: Coin and desired collateral state.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/account/set-collateral)
    """
    r = await self.authed_request(
      'POST', '/v5/account/set-collateral-switch', json=set_collateral_request
    )
    return self.result(r, adapter, validate)
