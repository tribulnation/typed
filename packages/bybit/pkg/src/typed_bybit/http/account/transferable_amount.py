# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/account/withdrawal` — Get Transferable Amount."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class TransferableAmountResult(TypedDict):
  """Transferable amount for the requested coin(s)."""

  availableWithdrawal: str
  """Transferable amount for the first coin named in `coinName`."""
  availableWithdrawalMap: dict[str, str]
  """Transferable amount for every requested coin, keyed by coin name."""


adapter = validator[TransferableAmountResult](TransferableAmountResult)


class TransferableAmount(Endpoint):
  """`Get Transferable Amount` — mixed into the router that owns `account.transferable_amount`."""

  async def transferable_amount(
    self,
    *,
    coin_name: str,
    validate: bool | None = None,
  ) -> TransferableAmountResult:
    """Query the available amount to transfer out of the Unified wallet for one or more coins, accounting for collateral ratios, margin mode, unrealised P&L, reservations and frozen amounts.

    Args:
      coin_name: Coin name(s), uppercase only, comma-separated. Up to 20 coins per request, for example `BTC,USDC,USDT,SOL`.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/account/unified-trans-amnt)
    """
    params: dict = {
      'coinName': coin_name,
    }
    r = await self.authed_request('GET', '/v5/account/withdrawal', params=params)
    return self.result(r, adapter, validate)
