# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/account/set-delta-mode` — Set Delta Neutral Mode."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class SetDeltaModeRequest(TypedDict):
  """Set-delta-mode parameters."""

  deltaEnable: Literal['1', '0']
  """Desired mode. `"1"`: enable. `"0"`: disable."""


class SetDeltaModeResult(TypedDict):
  """Empty object on success."""


adapter = validator[SetDeltaModeResult](SetDeltaModeResult)


class SetDeltaMode(Endpoint):
  """`Set Delta Neutral Mode` — mixed into the router that owns `account.set_delta_mode`."""

  async def set_delta_mode(
    self,
    set_delta_mode_request: SetDeltaModeRequest,
    *,
    validate: bool | None = None,
  ) -> SetDeltaModeResult:
    """Enable or disable Delta Neutral Mode, which lowers ADL (Auto-Deleveraging) queue priority for qualifying delta-neutral positions during extreme market conditions.

    Args:
      set_delta_mode_request: Desired mode.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/account/set-delta-mode)
    """
    r = await self.authed_request(
      'POST', '/v5/account/set-delta-mode', json=set_delta_mode_request
    )
    return self.result(r, adapter, validate)
