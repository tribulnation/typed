# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/account/set-collateral-switch-batch` — Batch Set Collateral Coin."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class SetCollateralItem(TypedDict):
  """Desired collateral state of one coin."""

  coin: str
  """Coin to configure, uppercase only. `USDT`/`USDC` cannot be set."""
  collateralSwitch: Literal['ON', 'OFF']
  """Desired collateral state."""


class SetCollateralOutcome(TypedDict):
  """Applied collateral state of one coin."""

  coin: str
  """Coin that was configured."""
  collateralSwitch: Literal['ON', 'OFF']
  """Collateral state applied."""


class BatchSetCollateralRequest(TypedDict):
  """Batch-set-collateral parameters."""

  request: list[SetCollateralItem]
  """One entry per coin to configure."""


class BatchSetCollateralResult(TypedDict):
  """Applied collateral state."""

  list: list[SetCollateralOutcome]
  """One entry per coin processed."""


adapter = validator[BatchSetCollateralResult](BatchSetCollateralResult)


class BatchSetCollateral(Endpoint):
  """`Batch Set Collateral Coin` — mixed into the router that owns `account.batch_set_collateral`."""

  async def batch_set_collateral(
    self,
    batch_set_collateral_request: BatchSetCollateralRequest,
    *,
    validate: bool | None = None,
  ) -> BatchSetCollateralResult:
    """Set whether multiple coins held in a Unified account are usable as margin collateral, in one call. `USDT`/`USDC` cannot be configured.

    Args:
      batch_set_collateral_request: Coins and their desired collateral state.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/account/batch-set-collateral)
    """
    r = await self.authed_request(
      'POST',
      '/v5/account/set-collateral-switch-batch',
      json=batch_set_collateral_request,
    )
    return self.result(r, adapter, validate)
