# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/account/borrow` — Manual Borrow."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class BorrowRequest(TypedDict):
  """Borrow parameters."""

  coin: str
  """Coin to borrow, uppercase only, for example `BTC`."""
  amount: str
  """Quantity to borrow."""


class BorrowResult(TypedDict):
  """Confirmed borrow amount."""

  coin: str
  """Coin borrowed, uppercase."""
  amount: str
  """Confirmed borrow amount."""


adapter = validator[BorrowResult](BorrowResult)


class Borrow(Endpoint):
  """`Manual Borrow` — mixed into the router that owns `account.borrow`."""

  async def borrow(
    self,
    borrow_request: BorrowRequest,
    *,
    validate: bool | None = None,
  ) -> BorrowResult:
    """Borrow a cryptocurrency at the variable rate.

    Args:
      borrow_request: Coin and amount to borrow.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/account/borrow)
    """
    r = await self.authed_request('POST', '/v5/account/borrow', json=borrow_request)
    return self.result(r, adapter, validate)
