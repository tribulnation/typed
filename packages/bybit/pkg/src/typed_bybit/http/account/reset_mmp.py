# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/account/mmp-reset` — Reset MMP."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class ResetMmpRequest(TypedDict):
  """Reset-MMP parameters."""

  baseCoin: str
  """Base coin, uppercase only."""


class ResetMmpResult(TypedDict):
  """Empty object on success."""


adapter = validator[ResetMmpResult](ResetMmpResult)


class ResetMmp(Endpoint):
  """`Reset MMP` — mixed into the router that owns `account.reset_mmp`."""

  async def reset_mmp(
    self,
    reset_mmp_request: ResetMmpRequest,
    *,
    validate: bool | None = None,
  ) -> ResetMmpResult:
    """Unfreeze a frozen Market Maker Protection (MMP) account for one base coin and reset its `qtyLimit`/`deltaLimit`/`vegaLimit` accumulation to 0, even if the account is not currently frozen.

    Args:
      reset_mmp_request: Base coin to reset.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/account/reset-mmp)
    """
    r = await self.authed_request(
      'POST', '/v5/account/mmp-reset', json=reset_mmp_request
    )
    return self.result(r, adapter, validate)
