# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/account/upgrade-to-uta` — Upgrade to Unified Account Pro."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class UpgradeToUnifiedFailure(TypedDict):
  """Failure detail. `null` unless `unifiedUpdateStatus` is `FAIL`."""

  msg: NotRequired[list[str]]
  """Failure messages. Populated only when `unifiedUpdateStatus` is `FAIL`."""


class UpgradeToUnifiedRequest(TypedDict):
  """Empty request body."""


class UpgradeToUnifiedResult(TypedDict):
  """Upgrade outcome."""

  unifiedUpdateStatus: Literal['FAIL', 'PROCESS', 'SUCCESS']
  """Upgrade outcome."""
  unifiedUpdateMsg: NotRequired[UpgradeToUnifiedFailure]


adapter = validator[UpgradeToUnifiedResult](UpgradeToUnifiedResult)


class UpgradeToUnified(Endpoint):
  """`Upgrade to Unified Account Pro` — mixed into the router that owns `account.upgrade_to_unified`."""

  async def upgrade_to_unified(
    self,
    upgrade_to_unified_request: UpgradeToUnifiedRequest,
    *,
    validate: bool | None = None,
  ) -> UpgradeToUnifiedResult:
    """Upgrade a Unified Trading Account (UTA) 2.0 to UTA 2.0 Pro. Requires VIP or PRO tier and `unifiedMarginStatus=5`; the account must have no open orders, and should avoid the 50th-to-5th-minute window of each hour (and 07:55-08:05 UTC when upgrading from UTA Public to UTA Pro).

    Args:
      upgrade_to_unified_request: No parameters.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/account/upgrade-unified-account)
    """
    r = await self.authed_request(
      'POST', '/v5/account/upgrade-to-uta', json=upgrade_to_unified_request
    )
    return self.result(r, adapter, validate)
