# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/account/user-setting-config` — Get Trade Behaviour Config."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class UserSettingConfig(TypedDict):
  """Trade behaviour configuration."""

  lpaSpot: bool
  """Spot behaviour when a limit order price exceeds the allowed range. `true`: auto-adjust to the nearest allowed price. `false`: reject the order."""
  lpaPerp: bool
  """Futures behaviour when a limit order price exceeds the allowed range. `true`: reject the order. `false`: auto-adjust to the nearest allowed price."""
  deltaEnable: bool
  """Whether Delta Neutral Mode is enabled. Also settable via account.set_delta_mode."""
  smsef: bool
  """Whether spot fees are deducted via the MNT fee-deduction program."""
  fmsef: bool
  """Whether futures fees are deducted via the MNT fee-deduction program."""
  smpType: Literal[0, 1, 2, 3]
  """Self-match-prevention type."""


adapter = validator[UserSettingConfig](UserSettingConfig)


class UserSettingConfigEndpoint(Endpoint):
  """`Get Trade Behaviour Config` — mixed into the router that owns `account.user_setting_config`."""

  async def user_setting_config(
    self, *, validate: bool | None = None
  ) -> UserSettingConfig:
    """Get the account's configured behaviour when a limit order's price exceeds the market's allowed range, plus related trading settings.

    Args:
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/account/get-user-setting-config)
    """
    r = await self.authed_request('GET', '/v5/account/user-setting-config')
    return self.result(r, adapter, validate)
