# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/account/mmp-modify` — Set MMP."""

from typing_extensions import NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class SetMmpRequest(TypedDict):
  """Set-MMP parameters."""

  baseCoin: str
  """Base coin, uppercase only."""
  window: str
  """Time window, in milliseconds."""
  frozenPeriod: str
  """Frozen period, in milliseconds. `"0"` means frozen until manually reset."""
  qtyLimit: str
  """Trade quantity limit, positive, up to 2 decimal places."""
  deltaLimit: str
  """Delta limit, positive, up to 2 decimal places."""
  vegaLimit: NotRequired[str]
  """Vega limit, positive, up to 2 decimal places. Defaults to `500000`."""


class SetMmpResult(TypedDict):
  """Empty object on success."""


adapter = validator[SetMmpResult](SetMmpResult)


class SetMmp(Endpoint):
  """`Set MMP` — mixed into the router that owns `account.set_mmp`."""

  async def set_mmp(
    self,
    set_mmp_request: SetMmpRequest,
    *,
    validate: bool | None = None,
  ) -> SetMmpResult:
    """Configure Market Maker Protection (MMP) thresholds for one base coin. Options only; requires prior enablement by Bybit's institutional team.

    Args:
      set_mmp_request: Desired MMP thresholds.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/account/set-mmp)
    """
    r = await self.authed_request(
      'POST', '/v5/account/mmp-modify', json=set_mmp_request
    )
    return self.result(r, adapter, validate)
