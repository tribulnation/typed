# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/account/pay-info` — Get Pay Info."""

from typing_extensions import NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class PayBorrowInfo(TypedDict):
  """Outstanding liability of the requested coin. Present only when `coin` was passed."""

  coin: str
  """Coin name."""
  borrowSize: str
  """Amount borrowed."""
  borrowValue: str
  """Value of the borrowed amount, in USD."""
  assetFrozen: str
  """Frozen asset quantity."""
  availableBalance: str
  """Balance available for repayment."""


class PayCollateralItem(TypedDict):
  """Repayment collateral available in one coin."""

  coin: str
  """Coin name."""
  availableSize: str
  """Available quantity."""
  availableValue: str
  """Available value, in USD."""
  coinScale: NotRequired[int]
  """Decimal precision of the coin."""
  borrowSize: NotRequired[str]
  """Amount borrowed."""
  spotHedgeAmount: NotRequired[str]
  """Spot amount used to hedge in portfolio margin mode."""
  assetFrozen: NotRequired[str]
  """Frozen asset quantity."""


class PayCollateralInfo(TypedDict):
  """Available collateral usable for repayment."""

  collateralList: list[PayCollateralItem]
  """One entry per collateral coin."""


class PayInfoResult(TypedDict):
  """Repayment collateral information."""

  collateralInfo: PayCollateralInfo
  borrowInfo: NotRequired[PayBorrowInfo]


adapter = validator[PayInfoResult](PayInfoResult)


class PayInfo(Endpoint):
  """`Get Pay Info` — mixed into the router that owns `account.pay_info`."""

  async def pay_info(
    self,
    *,
    coin: str | None = None,
    validate: bool | None = None,
  ) -> PayInfoResult:
    """Query the account's repayment collateral information before repaying a liability. Typically called before account.repay or account.repay_liability.

    Args:
      coin: Coin with an outstanding liability, uppercase only. Omit to return aggregated totals across every liability.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/account/pay-info)
    """
    params = {}
    if coin is not None:
      params['coin'] = coin
    r = await self.authed_request('GET', '/v5/account/pay-info', params=params)
    return self.result(r, adapter, validate)
