# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/spot-x/token-splash/project/list` — Get Token Splash Project List."""

from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class TokenSplashProject(TypedDict):
  """One Token Splash activity."""

  status: Literal[0, 1, 2]
  """Activity phase."""
  projectId: str
  """Unique activity identifier."""
  activityCoin: str
  """Core activity coin."""
  rewardCoin: str
  """Distributed reward token."""
  totalReward: str
  """Sum of all prize pools."""
  participantCount: str
  """Registered participants."""
  registrationStartTime: TimestampMillis
  """Registration window opening time, as a millisecond timestamp."""
  activityEndTime: TimestampMillis
  """Activity end time, as a millisecond timestamp."""
  registrationStartTimeUtc0: NotRequired[str]
  """Registration window opening time, as a human-readable UTC string. Not documented on the upstream page; observed live."""
  activityEndTimeUtc0: NotRequired[str]
  """Activity end time, as a human-readable UTC string. Not documented on the upstream page; observed live."""


class TokenSplashProjectList(TypedDict):
  """One page of Token Splash activities."""

  nextPageCursor: str
  """Cursor for the next page. Empty when there is no further page."""
  list: list[TokenSplashProject]
  """Token Splash activity records."""


adapter = validator[TokenSplashProjectList](TokenSplashProjectList)


class ProjectList(Endpoint):
  """`Get Token Splash Project List` — mixed into the router that owns `finance.spot_x.token_splash.project_list`."""

  async def project_list(
    self,
    *,
    status: Literal[0, 1, 2],
    project_id: str | None = None,
    activity_coin: str | None = None,
    cursor: str | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> TokenSplashProjectList:
    """Get Token Splash activities: a trading-volume-based reward campaign for a given coin. No authentication required.

    Args:
      status: Activity phase to filter by.
      project_id: Exact activity code, to retrieve a single project.
      activity_coin: Coin to filter by, case-insensitive.
      cursor: Pagination cursor, from a prior response's `nextPageCursor`.
      limit: Results per page. Defaults to 10; maximum 10.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/spot-x/token-splash/token-splash-project-list)
    """
    params: dict = {
      'status': status,
    }
    if project_id is not None:
      params['projectId'] = project_id
    if activity_coin is not None:
      params['activityCoin'] = activity_coin
    if cursor is not None:
      params['cursor'] = cursor
    if limit is not None:
      params['limit'] = limit
    r = await self.request('GET', '/v5/spot-x/token-splash/project/list', params=params)
    return self.result(r, adapter, validate)

  async def project_list_paged(
    self,
    *,
    status: Literal[0, 1, 2],
    project_id: str | None = None,
    activity_coin: str | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[TokenSplashProjectList]:
    """Yield successive pages of `project_list`.

    Passes each page's token back as `cursor` and stops when a response carries no
    `nextPageCursor`, or after `max_pages` pages when one is given.
    """
    cursor: str | None = None
    pages = 0
    while True:
      response = await self.project_list(
        status=status,
        project_id=project_id,
        activity_coin=activity_coin,
        limit=limit,
        cursor=cursor,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      cursor = response.get('nextPageCursor') if response is not None else None
      if not cursor:
        break
