# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/spot-x/puzzle/project/list` — Get Puzzle Project List."""

from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class PuzzleProject(TypedDict):
  """One Puzzle activity."""

  projectId: str
  """Unique activity identifier."""
  activityCoin: str
  """Coin this activity centers on."""
  rewardCoin: str
  """Coin distributed as reward."""
  status: Literal[0, 1, 2]
  """Activity status."""
  startTime: TimestampMillis
  """Activity start time, as a millisecond timestamp."""
  endTime: TimestampMillis
  """Activity end time, as a millisecond timestamp."""
  startTimeUtc0: NotRequired[str]
  """Activity start time, as a human-readable UTC string. Not documented on the upstream page; observed live."""
  endTimeUtc0: NotRequired[str]
  """Activity end time, as a human-readable UTC string. Not documented on the upstream page; observed live."""
  totalReward: str
  """Total reward pool magnitude."""
  participantCount: str
  """Total registered participants."""


class PuzzleProjectList(TypedDict):
  """One page of Puzzle activities."""

  nextPageCursor: str
  """Cursor for the next page. Empty when there is no further page."""
  list: list[PuzzleProject]
  """Puzzle activity records."""


adapter = validator[PuzzleProjectList](PuzzleProjectList)


class ProjectList(Endpoint):
  """`Get Puzzle Project List` — mixed into the router that owns `finance.spot_x.puzzle.project_list`."""

  async def project_list(
    self,
    *,
    status: Literal[0, 1, 2],
    project_id: str | None = None,
    activity_coin: str | None = None,
    cursor: str | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PuzzleProjectList:
    """Get Puzzle activities: a gamified reward campaign for a given coin. No authentication required.

    Args:
      status: Activity phase to filter by.
      project_id: Specific activity identifier, to retrieve a single project.
      activity_coin: Activity coin to filter by, case-insensitive.
      cursor: Pagination cursor, from a prior response's `nextPageCursor`.
      limit: Results per page. Defaults to 10; maximum 10.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/spot-x/puzzle/puzzle-project-list)
    """
    params: dict = {
      'status': status,
    }
    if project_id is not None:
      params['projectId'] = project_id
    if activity_coin is not None:
      params['activityCoin'] = activity_coin
    if cursor is not None:
      params['cursor'] = cursor
    if limit is not None:
      params['limit'] = limit
    r = await self.request('GET', '/v5/spot-x/puzzle/project/list', params=params)
    return self.result(r, adapter, validate)

  async def project_list_paged(
    self,
    *,
    status: Literal[0, 1, 2],
    project_id: str | None = None,
    activity_coin: str | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[PuzzleProjectList]:
    """Yield successive pages of `project_list`.

    Passes each page's token back as `cursor` and stops when a response carries no
    `nextPageCursor`, or after `max_pages` pages when one is given.
    """
    cursor: str | None = None
    pages = 0
    while True:
      response = await self.project_list(
        status=status,
        project_id=project_id,
        activity_coin=activity_coin,
        limit=limit,
        cursor=cursor,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      cursor = response.get('nextPageCursor') if response is not None else None
      if not cursor:
        break
