# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/spot-x/launchpool/user/current-staking` — Get Launchpool Current Staking."""

from typing_extensions import NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class LaunchpoolStakingPosition(TypedDict):
  """One active Launchpool staking position."""

  stakeCoin: str
  """Coin staked into this position."""
  rewardCoin: str
  """Coin earned as reward."""
  stakeAmount: str
  """Total amount currently staked."""
  totalReward: str
  """Accumulated reward earned so far."""
  autoRedeemDate: NotRequired[TimestampMillis]
  """Auto-redeem time, as a millisecond timestamp. Empty string when not set."""


class LaunchpoolCurrentStaking(TypedDict):
  """Active Launchpool staking positions and totals."""

  totalInvestmentUsd: str
  """Total USD value of all active staking positions."""
  totalEarningsUsd: str
  """Total USD value of accumulated earnings across all positions."""
  todayEarningsUsd: str
  """Today's earnings across all positions, in USD."""
  list: list[LaunchpoolStakingPosition]
  """Per-position details, up to 30 items."""


adapter = validator[LaunchpoolCurrentStaking](LaunchpoolCurrentStaking)


class CurrentStaking(Endpoint):
  """`Get Launchpool Current Staking` — mixed into the router that owns `finance.spot_x.launchpool.current_staking`."""

  async def current_staking(
    self,
    *,
    validate: bool | None = None,
  ) -> LaunchpoolCurrentStaking:
    """Get the caller's active Launchpool staking positions and accumulated earnings. Requires authentication.

    Args:
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/spot-x/launchpool/launchpool-current-staking)
    """
    r = await self.authed_request('GET', '/v5/spot-x/launchpool/user/current-staking')
    return self.result(r, adapter, validate)
