# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/earn/position` — Get Staked Position."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampIso, validator


class EasyOnchainFreezeDetail(TypedDict):
  """One locked-asset detail on this position."""

  amount: NotRequired[str]
  """Locked amount."""
  reason: NotRequired[str]
  """Reason the amount is locked."""


class EasyOnchainPosition(TypedDict):
  """One Easy/On-Chain Earn staked position."""

  coin: str
  """Coin name."""
  productId: str
  """Product identifier."""
  amount: str
  """Total staked quantity."""
  totalPnl: NotRequired[str]
  """Position profit. Populated only in OnChain non-LST mode."""
  claimableYield: NotRequired[str]
  """Accrued but undistributed yield. Not populated for OnChain products."""
  id: NotRequired[str]
  """Position identifier. OnChain only."""
  status: NotRequired[Literal['Processing', 'Active', '']]
  """Position state. OnChain only; empty for FlexibleSaving."""
  orderId: NotRequired[str]
  """Order ID that created this position. OnChain only."""
  estimateRedeemTime: NotRequired[str]
  """Milliseconds until redemption completes. OnChain only."""
  estimateStakeTime: NotRequired[str]
  """Milliseconds until staking completes. OnChain only."""
  estimateInterestCalculationTime: NotRequired[str]
  """Milliseconds until interest accrual begins. OnChain only."""
  settlementTime: NotRequired[str]
  """Milliseconds until settlement. OnChain Fixed products only."""
  autoReinvest: NotRequired[Literal['Enable', 'Disable', '']]
  """Whether auto-reinvestment is enabled for this position. Empty when not applicable."""
  effectiveAmount: NotRequired[str]
  """Amount actually earning yield."""
  yesterdayYield: NotRequired[str]
  """Yield earned the previous day."""
  yesterdayYieldDate: NotRequired[TimestampIso]
  """Date `yesterdayYield` was earned, as a calendar date string."""
  availableAmount: NotRequired[str]
  """Amount currently redeemable."""
  freezeDetails: NotRequired[list[EasyOnchainFreezeDetail]]
  """Locked-asset details."""


class EasyOnchainPositionList(TypedDict):
  """Easy/On-Chain Earn staked positions."""

  list: list[EasyOnchainPosition]
  """Position records."""


adapter = validator[EasyOnchainPositionList](EasyOnchainPositionList)


class Position(Endpoint):
  """`Get Staked Position` — mixed into the router that owns `finance.easy_onchain.position`."""

  async def position(
    self,
    *,
    category: Literal['FlexibleSaving', 'OnChain'],
    product_id: str | None = None,
    coin: str | None = None,
    validate: bool | None = None,
  ) -> EasyOnchainPositionList:
    """Get current Easy/On-Chain Earn staked positions. Requires API key "Earn" permission.

    Args:
      category: Product category.
      product_id: Product identifier to filter by.
      coin: Coin to filter by, uppercase.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/earn/easy-onchain/position)
    """
    params: dict = {
      'category': category,
    }
    if product_id is not None:
      params['productId'] = product_id
    if coin is not None:
      params['coin'] = coin
    r = await self.authed_request('GET', '/v5/earn/position', params=params)
    return self.result(r, adapter, validate)
