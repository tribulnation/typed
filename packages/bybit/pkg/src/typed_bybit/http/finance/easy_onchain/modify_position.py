# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/earn/position/modify` — Modify Position."""

from typing_extensions import Any, Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class EasyOnchainModifyPositionRequest(TypedDict):
  """On-Chain Earn position auto-reinvest change."""

  category: Literal['OnChain']
  """Product category."""
  productId: int
  """Product identifier, from the product-info endpoint."""
  positionId: int
  """Position identifier, from the position endpoint."""
  autoReinvest: Literal[0, 1]
  """Auto-reinvestment toggle."""


adapter = validator[dict[str, Any]](dict[str, Any])


class ModifyPosition(Endpoint):
  """`Modify Position` — mixed into the router that owns `finance.easy_onchain.modify_position`."""

  async def modify_position(
    self,
    easy_onchain_modify_position_request: EasyOnchainModifyPositionRequest,
    *,
    validate: bool | None = None,
  ) -> dict[str, Any]:
    """Toggle auto-reinvestment on an On-Chain Earn staked position. Requires API key "Earn" permission.

    Args:
      easy_onchain_modify_position_request: Auto-reinvest setting to apply to a position.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/earn/easy-onchain/modify-position)
    """
    r = await self.authed_request(
      'POST', '/v5/earn/position/modify', json=easy_onchain_modify_position_request
    )
    return self.result(r, adapter, validate)
