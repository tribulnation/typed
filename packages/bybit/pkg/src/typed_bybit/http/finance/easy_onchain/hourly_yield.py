# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/earn/hourly-yield` — Get Hourly Yield History."""

from typing_extensions import AsyncIterator, Literal, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, timestamp_millis, validator


class EasyOnchainHourlyYield(TypedDict):
  """One hourly yield distribution record."""

  productId: str
  """Product identifier."""
  coin: str
  """Coin name."""
  id: str
  """Unique record key, per user."""
  amount: str
  """Yield amount for this hour."""
  effectiveStakingAmount: str
  """Effective staked balance this yield was computed on."""
  status: Literal['Pending', 'Success', 'Fail']
  """Record status."""
  hourlyDate: TimestampMillis
  """Yield distribution hour, as a millisecond timestamp."""
  createdAt: TimestampMillis
  """Record creation time, as a millisecond timestamp."""


class EasyOnchainHourlyYieldHistory(TypedDict):
  """One page of hourly yield records."""

  nextPageCursor: str
  """Cursor for the next page. Empty when there is no further page."""
  list: list[EasyOnchainHourlyYield]
  """Hourly yield records."""


adapter = validator[EasyOnchainHourlyYieldHistory](EasyOnchainHourlyYieldHistory)


class HourlyYield(Endpoint):
  """`Get Hourly Yield History` — mixed into the router that owns `finance.easy_onchain.hourly_yield`."""

  async def hourly_yield(
    self,
    *,
    category: Literal['FlexibleSaving'],
    product_id: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> EasyOnchainHourlyYieldHistory:
    """Get hourly yield distribution records for FlexibleSaving Earn products. Requires API key "Earn" permission.

    Args:
      category: Product category.
      product_id: Product identifier to filter by.
      start_time: Start of the queried range, as a millisecond timestamp. Defaults to 7 days prior.
      end_time: End of the queried range, as a millisecond timestamp. Span with `startTime` cannot exceed 7 days.
      limit: Results per page. Range [1, 100]; defaults to 50.
      cursor: Pagination cursor, from a prior response's `nextPageCursor`.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/earn/easy-onchain/hourly-yield)
    """
    params: dict = {
      'category': category,
    }
    if product_id is not None:
      params['productId'] = product_id
    if start_time is not None:
      params['startTime'] = timestamp_millis.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp_millis.dump(end_time)
    if limit is not None:
      params['limit'] = limit
    if cursor is not None:
      params['cursor'] = cursor
    r = await self.authed_request('GET', '/v5/earn/hourly-yield', params=params)
    return self.result(r, adapter, validate)

  async def hourly_yield_paged(
    self,
    *,
    category: Literal['FlexibleSaving'],
    product_id: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[EasyOnchainHourlyYieldHistory]:
    """Yield successive pages of `hourly_yield`.

    Passes each page's token back as `cursor` and stops when a response carries no
    `nextPageCursor`, or after `max_pages` pages when one is given.
    """
    cursor: str | None = None
    pages = 0
    while True:
      response = await self.hourly_yield(
        category=category,
        product_id=product_id,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        cursor=cursor,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      cursor = response.get('nextPageCursor') if response is not None else None
      if not cursor:
        break
