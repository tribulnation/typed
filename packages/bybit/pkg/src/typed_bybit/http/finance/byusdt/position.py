# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/earn/token/position` — Get byUSDT Position."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class ByusdtPosition(TypedDict):
  """The caller's byUSDT holdings and yield summary."""

  totalAmount: str
  """Total byUSDT holdings."""
  totalYield: str
  """Cumulative yield earned."""
  yesterdayYield: str
  """Yield earned the previous day."""
  aprE8: int
  """Base APR, e8 precision. Divide by 10^8 for the actual rate."""
  bonusAprE8: NotRequired[int]
  """Bonus APR, e8 precision, when the caller qualifies. Divide by 10^8 for the actual rate."""
  bonusMaxAmount: NotRequired[str]
  """Principal ceiling eligible for the bonus APR."""
  hasQuota: bool
  """Whether mintable quota remains available."""


adapter = validator[ByusdtPosition](ByusdtPosition)


class Position(Endpoint):
  """`Get byUSDT Position` — mixed into the router that owns `finance.byusdt.position`."""

  async def position(
    self,
    *,
    coin: Literal['BYUSDT'],
    validate: bool | None = None,
  ) -> ByusdtPosition:
    """Get the caller's current byUSDT holdings, accrued yield and APR.

    Args:
      coin: Token identifier. Currently only `BYUSDT` is supported.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/earn/byusdt/position)
    """
    params: dict = {
      'coin': coin,
    }
    r = await self.authed_request('GET', '/v5/earn/token/position', params=params)
    return self.result(r, adapter, validate)
