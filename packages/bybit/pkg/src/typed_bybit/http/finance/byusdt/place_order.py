# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/earn/token/place-order` — Place byUSDT Mint/Redeem Order."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class ByusdtOrderResult(TypedDict):
  """Identifiers for a placed byUSDT order."""

  orderId: str
  """System-generated order identifier."""
  orderLinkId: str
  """Caller-supplied order link identifier."""


class ByusdtPlaceOrderRequest(TypedDict):
  """byUSDT mint or redeem order."""

  coin: Literal['BYUSDT']
  """Token identifier. Currently only `BYUSDT` is supported."""
  orderLinkId: str
  """Caller-supplied order ID, max 36 characters, used for idempotency."""
  orderType: Literal['Mint', 'Redeem']
  """Order direction."""
  amount: str
  """Order amount as a decimal string: USDT quantity for `Mint`, byUSDT quantity for `Redeem`."""
  accountType: Literal['FlexibleSaving', 'UNIFIED']
  """Source account for `Mint`, destination account for `Redeem`."""


adapter = validator[ByusdtOrderResult](ByusdtOrderResult)


class PlaceOrder(Endpoint):
  """`Place byUSDT Mint/Redeem Order` — mixed into the router that owns `finance.byusdt.place_order`."""

  async def place_order(
    self,
    byusdt_place_order_request: ByusdtPlaceOrderRequest,
    *,
    validate: bool | None = None,
  ) -> ByusdtOrderResult:
    """Mint byUSDT from USDT held in Flexible Saving, or redeem byUSDT back to USDT.

    Args:
      byusdt_place_order_request: byUSDT mint or redeem order.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/earn/byusdt/place-order)
    """
    r = await self.authed_request(
      'POST', '/v5/earn/token/place-order', json=byusdt_place_order_request
    )
    return self.result(r, adapter, validate)
