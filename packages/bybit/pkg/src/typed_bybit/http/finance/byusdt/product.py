# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/earn/token/product` — Get byUSDT Product Info."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class ByusdtProduct(TypedDict):
  """byUSDT product terms and, for an authenticated caller, account-specific state."""

  productId: str
  """Unique product identifier."""
  coin: str
  """Token symbol."""
  mintFeeRateE8: str
  """Mint fee rate, e8 precision. `5000000` means 0.05%."""
  redeemFeeRateE8: str
  """Redeem fee rate, e8 precision."""
  minInvestment: str
  """Minimum mint amount, in USDT."""
  userHolding: NotRequired[str]
  """Caller's current byUSDT balance. Empty unauthenticated."""
  leftQuota: NotRequired[str]
  """Remaining mintable capacity. Empty unauthenticated."""
  canMint: bool
  """Whether minting is currently available. `false` unauthenticated."""
  savingsBalance: NotRequired[str]
  """Caller's Flexible Saving USDT balance available for minting. Empty unauthenticated."""
  aprE8: str
  """Base APR, e8 precision."""
  bonusAprE8: NotRequired[str]
  """Additional bonus APR, e8 precision, when eligible."""
  bonusMaxAmount: NotRequired[str]
  """Principal ceiling eligible for the bonus APR."""
  baseCoinPrecision: int
  """Decimal precision for USDT values."""
  tokenPrecision: int
  """Decimal precision for byUSDT values."""


adapter = validator[ByusdtProduct](ByusdtProduct)


class Product(Endpoint):
  """`Get byUSDT Product Info` — mixed into the router that owns `finance.byusdt.product`."""

  async def product(
    self,
    *,
    coin: Literal['BYUSDT'],
    validate: bool | None = None,
  ) -> ByusdtProduct:
    """Get byUSDT (USDT-backed yield token) product info: fees, precision, and current APR. Authentication is optional; some fields only populate for an authenticated caller.

    Args:
      coin: Token identifier. Currently only `BYUSDT` is supported.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/earn/byusdt/product)
    """
    params: dict = {
      'coin': coin,
    }
    r = await self.request('GET', '/v5/earn/token/product', params=params)
    return self.result(r, adapter, validate)
