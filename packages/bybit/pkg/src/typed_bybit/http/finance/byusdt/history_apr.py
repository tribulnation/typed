# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/earn/token/history-apr` — Get byUSDT Historical APR."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, TimestampSeconds, validator


class ByusdtAprRecord(TypedDict):
  """One day's APR for byUSDT."""

  timestamp: TimestampSeconds
  """Date, as a second timestamp."""
  aprE8: str
  """Yield rate, e8 precision. Divide by 10^8 for the actual rate."""


class ByusdtHistoryApr(TypedDict):
  """Daily APR history for byUSDT over the requested window."""

  list: list[ByusdtAprRecord]
  """One entry per day."""


adapter = validator[ByusdtHistoryApr](ByusdtHistoryApr)


class HistoryApr(Endpoint):
  """`Get byUSDT Historical APR` — mixed into the router that owns `finance.byusdt.history_apr`."""

  async def history_apr(
    self,
    *,
    coin: Literal['BYUSDT'],
    range: Literal[1, 2, 3],
    validate: bool | None = None,
  ) -> ByusdtHistoryApr:
    """Get byUSDT's historical daily APR over a fixed lookback window. No authentication required.

    Args:
      coin: Token identifier. Currently only `BYUSDT` is supported.
      range: Lookback window.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/earn/byusdt/history-apr)
    """
    params: dict = {
      'coin': coin,
      'range': range,
    }
    r = await self.request('GET', '/v5/earn/token/history-apr', params=params)
    return self.result(r, adapter, validate)
