# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/earn/pwm/investment-plan/detail` — Get Investment Plan Detail."""

from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class PwmPlanCoinHolding(TypedDict):
  """One coin holding within a plan."""

  coin: NotRequired[str]
  """Coin."""
  amount: NotRequired[str]
  """Held amount."""


class PwmInvestmentPlanDetail(TypedDict):
  """Current holdings and performance of an active or closed PWM investment plan."""

  planId: str
  """Unique plan identifier."""
  planName: str
  """Plan display name."""
  planType: Literal['stable', 'advanced']
  """Plan risk classification."""
  status: Literal['Active', 'Closed']
  """Plan status."""
  currentAssetUsd: str
  """Total current assets, in USD."""
  accumulateYieldUsd: str
  """Total accumulated yield, in USD."""
  weightedAvgApr: str
  """Weighted average annualized return rate, as a decimal."""
  currentAssets: list[PwmPlanCoinHolding]
  """Coin holdings."""
  positions: dict[str, Any]
  """Holdings, grouped by product type: `multiCoinsEarning`, `fixedYield`, `equityFunds`, `onchainEarn`, `fundingAccount`. Each category carries investment totals, accumulated yields, APR data, and detailed position items whose exact field names are not published upstream."""
  createdTime: TimestampMillis
  """Plan creation time, as a millisecond timestamp."""


adapter = validator[PwmInvestmentPlanDetail](PwmInvestmentPlanDetail)


class Detail(Endpoint):
  """`Get Investment Plan Detail` — mixed into the router that owns `finance.pwm.investment_plan.detail`."""

  async def detail(
    self,
    *,
    plan_id: str,
    validate: bool | None = None,
  ) -> PwmInvestmentPlanDetail:
    """Get the current holdings and performance of an Active or Closed PWM investment plan. Requires authentication.

    Args:
      plan_id: Investment plan ID. Must be in `Active` or `Closed` status.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/pwm/investment-plan/detail)
    """
    params: dict = {
      'planId': plan_id,
    }
    r = await self.authed_request(
      'GET', '/v5/earn/pwm/investment-plan/detail', params=params
    )
    return self.result(r, adapter, validate)
