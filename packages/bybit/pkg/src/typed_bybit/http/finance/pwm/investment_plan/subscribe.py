# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/earn/pwm/investment-plan/subscribe` — Subscribe Investment Plan."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class PwmInvestmentPlanSubscribeRequest(TypedDict):
  """Subscription to a pending PWM investment plan."""

  planId: str
  """Investment plan ID. Must be in `PendingSubscription` status."""
  accountType: NotRequired[str]
  """Source account type. Defaults to `FUND`."""
  orderLinkId: str
  """Caller-supplied order ID, max 36 characters, used for idempotency."""


class PwmInvestmentPlanSubscribeResult(TypedDict):
  """Result of a PWM investment plan subscription."""

  planId: str
  """Investment plan identifier."""
  status: Literal['Active']
  """Plan status after subscription. Becomes `Active` on the first successful subscription."""
  orderLinkId: str
  """Caller-supplied order link identifier."""


adapter = validator[PwmInvestmentPlanSubscribeResult](PwmInvestmentPlanSubscribeResult)


class Subscribe(Endpoint):
  """`Subscribe Investment Plan` — mixed into the router that owns `finance.pwm.investment_plan.subscribe`."""

  async def subscribe(
    self,
    pwm_investment_plan_subscribe_request: PwmInvestmentPlanSubscribeRequest,
    *,
    validate: bool | None = None,
  ) -> PwmInvestmentPlanSubscribeResult:
    """Subscribe to a pending PWM investment plan, activating it. Requires authentication.

    Args:
      pwm_investment_plan_subscribe_request: Subscription to a pending investment plan.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/pwm/investment-plan/subscribe)
    """
    r = await self.authed_request(
      'POST',
      '/v5/earn/pwm/investment-plan/subscribe',
      json=pwm_investment_plan_subscribe_request,
    )
    return self.result(r, adapter, validate)
