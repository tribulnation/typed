# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/earn/pwm/asset-manager/create-sub-account` — Create Fund Sub-Account."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class PwmCreateSubAccountRequest(TypedDict):
  """New fund sub-account."""

  fundId: str
  """Fund identifier to create a sub-account under."""
  reqLinkId: str
  """Caller-supplied request ID, used to prevent duplicate creation."""


class PwmFundSubAccount(TypedDict):
  """A created (or pending) fund sub-account."""

  fundId: str
  """Fund identifier."""
  subAccountUid: str
  """UID of the new sub-account. `"0"` while creation is still in progress."""
  createdTime: TimestampMillis
  """Creation time, as a millisecond timestamp."""
  status: Literal['pending', 'success', 'destroyed']
  """Creation status."""


adapter = validator[PwmFundSubAccount](PwmFundSubAccount)


class CreateSubAccount(Endpoint):
  """`Create Fund Sub-Account` — mixed into the router that owns `finance.pwm.asset_manager.create_sub_account`."""

  async def create_sub_account(
    self,
    pwm_create_sub_account_request: PwmCreateSubAccountRequest,
    *,
    validate: bool | None = None,
  ) -> PwmFundSubAccount:
    """Create a new sub-account under an active asset-manager fund. Institution-only.

    Args:
      pwm_create_sub_account_request: Fund sub-account to create.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/pwm/asset-manager/create-sub-account)
    """
    r = await self.authed_request(
      'POST',
      '/v5/earn/pwm/asset-manager/create-sub-account',
      json=pwm_create_sub_account_request,
    )
    return self.result(r, adapter, validate)
