# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/earn/pwm/asset-manager/settle-profit` — Settle Fund Profit."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class PwmSettleProfitRequest(TypedDict):
  """Fund profit-share settlement request."""

  fundId: str
  """Fund identifier. Must be in `Active` status."""
  reqLinkId: str
  """Caller-supplied request ID, max 36 characters, used for idempotency."""


class PwmSettleProfitResult(TypedDict):
  """A fund profit-share settlement round."""

  fundId: str
  """Fund identifier."""
  status: Literal['Processing', 'Completed', 'Failed']
  """Settlement outcome."""
  totalProfitShared: str
  """Total profit-share amount settled this round, in the base coin."""
  instIncome: str
  """Institution income this round, in the base coin."""
  coin: str
  """Fund base currency."""
  createdTime: TimestampMillis
  """Settlement time, as a millisecond timestamp."""


adapter = validator[PwmSettleProfitResult](PwmSettleProfitResult)


class SettleProfit(Endpoint):
  """`Settle Fund Profit` — mixed into the router that owns `finance.pwm.asset_manager.settle_profit`."""

  async def settle_profit(
    self,
    pwm_settle_profit_request: PwmSettleProfitRequest,
    *,
    validate: bool | None = None,
  ) -> PwmSettleProfitResult:
    """Settle a round of profit-share and institution income for an active fund. Institution-only.

    Args:
      pwm_settle_profit_request: Fund to settle profit for.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/pwm/asset-manager/settle-profit)
    """
    r = await self.authed_request(
      'POST', '/v5/earn/pwm/asset-manager/settle-profit', json=pwm_settle_profit_request
    )
    return self.result(r, adapter, validate)
