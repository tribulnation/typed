# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/earn/pwm/fund-transfer` — Fund Transfer Between Custodian Sub-Accounts."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class PwmFundTransferRequest(TypedDict):
  """Fund transfer between two PWM custodian sub-accounts."""

  transferId: str
  """Caller-supplied transfer request identifier."""
  fromUserId: int
  """Source user ID. Must be a custodian sub-account."""
  toUserId: int
  """Destination user ID. Must be a custodian sub-account."""
  amount: str
  """Transfer quantity."""
  coin: str
  """Coin to transfer."""


class PwmFundTransferResult(TypedDict):
  """Result of a PWM custodian sub-account fund transfer."""

  transferId: str
  """Transfer identifier."""
  status: Literal['SUCCESS', 'FAILED', 'PROCESSING']
  """Transfer outcome."""


adapter = validator[PwmFundTransferResult](PwmFundTransferResult)


class FundTransfer(Endpoint):
  """`Fund Transfer Between Custodian Sub-Accounts` — mixed into the router that owns `finance.pwm.fund_transfer`."""

  async def __call__(
    self,
    pwm_fund_transfer_request: PwmFundTransferRequest,
    *,
    validate: bool | None = None,
  ) -> PwmFundTransferResult:
    """Transfer funds between PWM fund custodian sub-accounts belonging to the same fund manager. Requires the fund custodian sub-account's own API key.

    Args:
      pwm_fund_transfer_request: Transfer between two fund custodian sub-accounts.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/pwm/fund-transfer)
    """
    r = await self.authed_request(
      'POST', '/v5/earn/pwm/fund-transfer', json=pwm_fund_transfer_request
    )
    return self.result(r, adapter, validate)
