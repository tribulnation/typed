# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/earn/pwm/query-fund-transfer-result` — Query Fund Transfer Result."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class PwmFundTransferRecord(TypedDict):
  """One PWM custodian sub-account fund transfer record."""

  transferId: str
  """Unique transfer identifier."""
  status: Literal['SUCCESS', 'FAILED', 'PROCESSING']
  """Transfer state."""
  fromUserId: int
  """Originating user ID."""
  toUserId: int
  """Receiving user ID."""
  amount: str
  """Transfer quantity."""
  coin: str
  """Transferred coin."""


class PwmFundTransferRecords(TypedDict):
  """PWM custodian sub-account fund transfer records."""

  list: list[PwmFundTransferRecord]
  """Transfer records."""


adapter = validator[PwmFundTransferRecords](PwmFundTransferRecords)


class QueryFundTransferResult(Endpoint):
  """`Query Fund Transfer Result` — mixed into the router that owns `finance.pwm.query_fund_transfer_result`."""

  async def __call__(
    self,
    *,
    transfer_id: str | None = None,
    from_user_id: int | None = None,
    validate: bool | None = None,
  ) -> PwmFundTransferRecords:
    """Get PWM custodian sub-account fund transfer records. Requires the fund custodian sub-account's own API key.

    Args:
      transfer_id: Transfer request ID. Omit to return up to the 20 most recent non-terminal transfer records within the past month.
      from_user_id: Source user ID to filter by.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/pwm/query-fund-transfer-result)
    """
    params = {}
    if transfer_id is not None:
      params['transferId'] = transfer_id
    if from_user_id is not None:
      params['fromUserId'] = from_user_id
    r = await self.authed_request(
      'GET', '/v5/earn/pwm/query-fund-transfer-result', params=params
    )
    return self.result(r, adapter, validate)
