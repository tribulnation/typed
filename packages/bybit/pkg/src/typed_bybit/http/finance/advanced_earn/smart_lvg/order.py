# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/earn/advance/order` — Get Smart Leverage Order List."""

from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, timestamp_millis, validator


class SmartLvgOrder(TypedDict):
  """One Smart Leverage order."""

  orderId: str
  """Unique order identifier."""
  orderLinkId: str
  """Caller-supplied order link identifier."""
  productId: str
  """Product identifier."""
  orderType: Literal['Stake', 'Redeem']
  """Operation type."""
  amount: str
  """Order quantity."""
  createdTime: TimestampMillis
  """Creation time, as a millisecond timestamp."""
  status: Literal['Pending', 'Success', 'Settled', 'Fail']
  """Order status."""
  duration: str
  """Product term, e.g. "1d", "2d"."""
  accountType: Literal['FUND', 'UNIFIED']
  """Account type."""
  investCoin: str
  """Investment coin."""
  underlyingAsset: str
  """Underlying asset, e.g. `BTC`, `ETH`."""
  direction: Literal['Long', 'Short']
  """Trade direction."""
  leverage: str
  """Leverage multiplier."""
  pnl: NotRequired[str]
  """Realized profit/loss. Populated once settled."""
  settlementPrice: NotRequired[str]
  """Final settlement price. Populated once settled."""


class SmartLvgOrderHistory(TypedDict):
  """One page of Smart Leverage order history."""

  category: Literal['SmartLeverage', '']
  """Product category. Empty on an empty page, observed live."""
  nextPageCursor: str
  """Cursor for the next page. Empty when there is no further page."""
  list: list[SmartLvgOrder]
  """Order records."""


adapter = validator[SmartLvgOrderHistory](SmartLvgOrderHistory)


class Order(Endpoint):
  """`Get Smart Leverage Order List` — mixed into the router that owns `finance.advanced_earn.smart_lvg.order`."""

  async def order(
    self,
    *,
    category: Literal['SmartLeverage'],
    product_id: str | None = None,
    order_id: str | None = None,
    order_link_id: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> SmartLvgOrderHistory:
    """Get the caller's Smart Leverage order history. Requires API key "Earn" permission.

    Args:
      category: Product category.
      product_id: Product identifier to filter by.
      order_id: Order ID to filter by.
      order_link_id: Caller-supplied order link ID to filter by.
      start_time: Start of the order-creation range, as a millisecond timestamp.
      end_time: End of the queried range, as a millisecond timestamp.
      limit: Results per page. Defaults to 20; maximum 20.
      cursor: Pagination cursor, from a prior response's `nextPageCursor`.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/smart-lvg/order)
    """
    params: dict = {
      'category': category,
    }
    if product_id is not None:
      params['productId'] = product_id
    if order_id is not None:
      params['orderId'] = order_id
    if order_link_id is not None:
      params['orderLinkId'] = order_link_id
    if start_time is not None:
      params['startTime'] = timestamp_millis.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp_millis.dump(end_time)
    if limit is not None:
      params['limit'] = limit
    if cursor is not None:
      params['cursor'] = cursor
    r = await self.authed_request('GET', '/v5/earn/advance/order', params=params)
    return self.result(r, adapter, validate)

  async def order_paged(
    self,
    *,
    category: Literal['SmartLeverage'],
    product_id: str | None = None,
    order_id: str | None = None,
    order_link_id: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[SmartLvgOrderHistory]:
    """Yield successive pages of `order`.

    Passes each page's token back as `cursor` and stops when a response carries no
    `nextPageCursor`, or after `max_pages` pages when one is given.
    """
    cursor: str | None = None
    pages = 0
    while True:
      response = await self.order(
        category=category,
        product_id=product_id,
        order_id=order_id,
        order_link_id=order_link_id,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        cursor=cursor,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      cursor = response.get('nextPageCursor') if response is not None else None
      if not cursor:
        break
