# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/earn/advance/position` — Get Double Win Position."""

from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class DoubleWinPosition(TypedDict):
  """One active Double Win position."""

  positionId: str
  """Unique position identifier."""
  productId: str
  """Product identifier."""
  investCoin: str
  """Investment coin."""
  underlyingAsset: str
  """Underlying asset."""
  amount: str
  """Invested quantity, in `investCoin`."""
  leverage: NotRequired[str]
  """Leverage multiplier applied to this position."""
  initialPrice: NotRequired[str]
  """Underlying asset price at position opening."""
  lowerPrice: NotRequired[str]
  """Price range lower bound."""
  upperPrice: NotRequired[str]
  """Price range upper bound."""
  duration: str
  """Product term, e.g. "1d", "3d"."""
  settlementTime: TimestampMillis
  """Settlement time, as a millisecond timestamp."""
  createdTime: TimestampMillis
  """Position creation time, as a millisecond timestamp."""
  status: Literal['Active', 'Redeeming']
  """Position status."""
  redeemable: bool
  """Whether early redemption is currently permitted."""
  accountType: Literal['FUND', 'UNIFIED']
  """Account type."""
  orderId: str
  """Order that created this position."""


class DoubleWinPositionHistory(TypedDict):
  """One page of active Double Win positions."""

  category: Literal['DoubleWin']
  """Product category."""
  nextPageCursor: str
  """Cursor for the next page. Empty when there is no further page."""
  list: list[DoubleWinPosition]
  """Position records."""


adapter = validator[DoubleWinPositionHistory](DoubleWinPositionHistory)


class Position(Endpoint):
  """`Get Double Win Position` — mixed into the router that owns `finance.advanced_earn.double_win.position`."""

  async def position(
    self,
    *,
    category: Literal['DoubleWin'],
    product_id: str | None = None,
    coin: str | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> DoubleWinPositionHistory:
    """Get the caller's active Double Win positions. Requires API key "Earn" permission.

    Args:
      category: Product category.
      product_id: Product identifier to filter by.
      coin: Underlying asset to filter by, e.g. `BTC`, `ETH`.
      limit: Results per page. Defaults to 20; maximum 20.
      cursor: Pagination cursor, from a prior response's `nextPageCursor`.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/double-win/position)
    """
    params: dict = {
      'category': category,
    }
    if product_id is not None:
      params['productId'] = product_id
    if coin is not None:
      params['coin'] = coin
    if limit is not None:
      params['limit'] = limit
    if cursor is not None:
      params['cursor'] = cursor
    r = await self.authed_request('GET', '/v5/earn/advance/position', params=params)
    return self.result(r, adapter, validate)

  async def position_paged(
    self,
    *,
    category: Literal['DoubleWin'],
    product_id: str | None = None,
    coin: str | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[DoubleWinPositionHistory]:
    """Yield successive pages of `position`.

    Passes each page's token back as `cursor` and stops when a response carries no
    `nextPageCursor`, or after `max_pages` pages when one is given.
    """
    cursor: str | None = None
    pages = 0
    while True:
      response = await self.position(
        category=category,
        product_id=product_id,
        coin=coin,
        limit=limit,
        cursor=cursor,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      cursor = response.get('nextPageCursor') if response is not None else None
      if not cursor:
        break
