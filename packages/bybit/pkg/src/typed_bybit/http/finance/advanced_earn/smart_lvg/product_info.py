# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/earn/advance/product` — Get Smart Leverage Product Info."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class SmartLvgProduct(TypedDict):
  """One Smart Leverage product."""

  productId: str
  """Unique product identifier."""
  investCoin: str
  """Investment coin, e.g. `USDT`."""
  underlyingAsset: str
  """Price-anchored asset, e.g. `BTC`, `ETH`."""
  direction: Literal['Long', 'Short']
  """Trade direction."""
  leverage: str
  """Fixed leverage multiplier, e.g. "3", "5"."""
  duration: str
  """Product duration, e.g. "1d", "3d"."""
  subscribeStartAt: NotRequired[TimestampMillis]
  """Subscription window opening time, as a millisecond timestamp."""
  subscribeEndAt: NotRequired[TimestampMillis]
  """Subscription window closing time, as a millisecond timestamp."""
  settlementTime: NotRequired[TimestampMillis]
  """Settlement time, as a millisecond timestamp."""
  expectReceiveAt: NotRequired[TimestampMillis]
  """Expected settlement credit time, as a millisecond timestamp."""
  minPurchaseAmount: str
  """Minimum order amount."""
  remainingAmount: str
  """Remaining purchasable quota."""
  orderPrecisionDigital: int
  """Decimal precision for order amounts."""


class SmartLvgProductList(TypedDict):
  """Smart Leverage structured products."""

  category: Literal['SmartLeverage']
  """Product category."""
  list: list[SmartLvgProduct]
  """Product records."""


adapter = validator[SmartLvgProductList](SmartLvgProductList)


class ProductInfo(Endpoint):
  """`Get Smart Leverage Product Info` — mixed into the router that owns `finance.advanced_earn.smart_lvg.product_info`."""

  async def product_info(
    self,
    *,
    category: Literal['SmartLeverage'],
    coin: str | None = None,
    duration: str | None = None,
    validate: bool | None = None,
  ) -> SmartLvgProductList:
    """Get available Smart Leverage structured products: fixed-leverage long/short exposure to an underlying asset. No authentication required.

    Args:
      category: Product category.
      coin: Underlying asset to filter by, uppercase, e.g. `BTC`, `ETH`.
      duration: Product duration to filter by, e.g. `1d`, `3d`, `6d`, `12d`.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/smart-lvg/product-info)
    """
    params: dict = {
      'category': category,
    }
    if coin is not None:
      params['coin'] = coin
    if duration is not None:
      params['duration'] = duration
    r = await self.request('GET', '/v5/earn/advance/product', params=params)
    return self.result(r, adapter, validate)
