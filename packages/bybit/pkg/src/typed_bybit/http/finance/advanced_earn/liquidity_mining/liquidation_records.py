# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/earn/liquidity-mining/liquidation-records` — Get Liquidity Mining Liquidation Records."""

from typed_bybit.core import Endpoint, TimestampMillis, timestamp_millis, validator
from typing_extensions import AsyncIterator, TypedDict


class LiquidityMiningLiquidationRecord(TypedDict):
  """One Liquidity Mining position liquidation."""

  baseCoin: str
  """Base coin of the pool."""
  quoteCoin: str
  """Quote coin of the pool."""
  baseAmount: str
  """Base coin quantity returned after liquidation."""
  quoteAmount: str
  """Quote coin quantity returned after liquidation."""
  liquidationPrice: str
  """Liquidation price, base priced in quote."""
  liquidationTime: TimestampMillis
  """Liquidation time, as a millisecond timestamp."""


class LiquidityMiningLiquidationRecords(TypedDict):
  """One page of Liquidity Mining liquidation records."""

  records: list[LiquidityMiningLiquidationRecord]
  """Liquidation records."""
  nextPageCursor: str
  """Cursor for the next page. Empty when there is no further page."""


adapter = validator[LiquidityMiningLiquidationRecords](
  LiquidityMiningLiquidationRecords
)


class LiquidationRecords(Endpoint):
  """`Get Liquidity Mining Liquidation Records` — mixed into the router that owns `finance.advanced_earn.liquidity_mining.liquidation_records`."""

  async def liquidation_records(
    self,
    *,
    base_coin: str | None = None,
    quote_coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> LiquidityMiningLiquidationRecords:
    """Get the caller's Liquidity Mining liquidation history. Requires API key "Earn" permission.

    Args:
      base_coin: Base coin to filter by, e.g. `BTC`, `ETH`.
      quote_coin: Quote coin to filter by, e.g. `USDT`.
      start_time: Start of the queried range, as a millisecond timestamp.
      end_time: End of the queried range, as a millisecond timestamp.
      limit: Results per page. Defaults to 20; maximum 50.
      cursor: Pagination cursor, from a prior response's `nextPageCursor`.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/liquidity-mining/liquidation-records)
    """
    params = {}
    if base_coin is not None:
      params['baseCoin'] = base_coin
    if quote_coin is not None:
      params['quoteCoin'] = quote_coin
    if start_time is not None:
      params['startTime'] = timestamp_millis.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp_millis.dump(end_time)
    if limit is not None:
      params['limit'] = limit
    if cursor is not None:
      params['cursor'] = cursor
    r = await self.authed_request(
      'GET', '/v5/earn/liquidity-mining/liquidation-records', params=params
    )
    return self.result(r, adapter, validate)

  async def liquidation_records_paged(
    self,
    *,
    base_coin: str | None = None,
    quote_coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[LiquidityMiningLiquidationRecords]:
    """Yield successive pages of `liquidation_records`.

    Passes each page's token back as `cursor` and stops when a response carries no
    `nextPageCursor`, or after `max_pages` pages when one is given.
    """
    cursor: str | None = None
    pages = 0
    while True:
      response = await self.liquidation_records(
        base_coin=base_coin,
        quote_coin=quote_coin,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        cursor=cursor,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      cursor = response.get('nextPageCursor') if response is not None else None
      if not cursor:
        break
