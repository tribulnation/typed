# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/earn/advance/product-extra-info` — Get Dual Asset Product Quote."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class DualAssetPriceTierBuyLowPriceItem(TypedDict):
  """One price tier for a Dual Asset order."""

  selectPrice: str
  """Target price for this tier."""
  apyE8: str
  """Annual percentage yield at this tier, e8 precision."""
  maxInvestmentAmount: str
  """Maximum investment amount at this tier."""
  expiredAt: TimestampMillis
  """Tier expiry time, as a millisecond timestamp."""


class DualAssetPriceTierSellHighPriceItem(TypedDict):
  """One price tier for a Dual Asset order."""

  selectPrice: str
  """Target price for this tier."""
  apyE8: str
  """Annual percentage yield at this tier, e8 precision."""
  maxInvestmentAmount: str
  """Maximum investment amount at this tier."""
  expiredAt: TimestampMillis
  """Tier expiry time, as a millisecond timestamp."""


class DualAssetProductQuote(TypedDict):
  """Live pricing tiers for one Dual Asset product."""

  productId: str
  """Product identifier."""
  currentPrice: str
  """Current market price."""
  buyLowPrice: list[DualAssetPriceTierBuyLowPriceItem]
  """Available price tiers for a Buy Low order."""
  sellHighPrice: list[DualAssetPriceTierSellHighPriceItem]
  """Available price tiers for a Sell High order."""


class DualAssetProductQuoteList(TypedDict):
  """Dual Asset product pricing tiers."""

  category: Literal['DualAssets']
  """Product category, echoed back."""
  list: list[DualAssetProductQuote]
  """Product quote records."""


adapter = validator[DualAssetProductQuoteList](DualAssetProductQuoteList)


class ProductQuote(Endpoint):
  """`Get Dual Asset Product Quote` — mixed into the router that owns `finance.advanced_earn.dual_asset.product_quote`."""

  async def product_quote(
    self,
    *,
    product_id: str,
    category: Literal['DualAssets'],
    validate: bool | None = None,
  ) -> DualAssetProductQuoteList:
    """Get live Buy Low / Sell High pricing tiers for a Dual Asset product. No authentication required.

    Args:
      product_id: Product identifier.
      category: Product category.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/dual-asset/product-quote)
    """
    params: dict = {
      'productId': product_id,
      'category': category,
    }
    r = await self.request('GET', '/v5/earn/advance/product-extra-info', params=params)
    return self.result(r, adapter, validate)
