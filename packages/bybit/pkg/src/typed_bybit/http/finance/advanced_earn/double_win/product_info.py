# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/earn/advance/product` — Get Double Win Product Info."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class DoubleWinProduct(TypedDict):
  """One Double Win product."""

  productId: str
  """Unique product identifier."""
  investCoin: str
  """Investment coin, e.g. `USDT`."""
  underlyingAsset: str
  """Underlying asset, e.g. `BTC`."""
  duration: str
  """Product duration, e.g. "1d", "2d", "3d"."""
  subscribeStartAt: NotRequired[TimestampMillis]
  """Subscription window opening time, as a millisecond timestamp."""
  subscribeEndAt: NotRequired[TimestampMillis]
  """Subscription window closing time, as a millisecond timestamp."""
  settlementTime: TimestampMillis
  """Settlement time, as a millisecond timestamp."""
  expectReceiveAt: NotRequired[TimestampMillis]
  """Expected settlement fund delivery time, as a millisecond timestamp."""
  minPurchaseAmount: str
  """Minimum order amount."""
  orderPrecisionDigital: int
  """Decimal precision for order amounts."""
  isRfqProduct: bool
  """`true` for a custom-pricing (RFQ) product, `false` for a fixed price-range product."""
  lowerPriceBuffer: NotRequired[str]
  """Offset below the initial price. Fixed price-range products only."""
  upperPriceBuffer: NotRequired[str]
  """Offset above the initial price. Fixed price-range products only."""
  minDeviationRatio: NotRequired[str]
  """Minimum allowed price adjustment. Custom/RFQ products only."""
  maxDeviationRatio: NotRequired[str]
  """Maximum allowed price adjustment. Custom/RFQ products only."""
  priceTickSize: NotRequired[str]
  """Price increment requirement. Custom/RFQ products only."""


class DoubleWinProductList(TypedDict):
  """Double Win structured products."""

  category: Literal['DoubleWin']
  """Product category."""
  list: list[DoubleWinProduct]
  """Product records."""


adapter = validator[DoubleWinProductList](DoubleWinProductList)


class ProductInfo(Endpoint):
  """`Get Double Win Product Info` — mixed into the router that owns `finance.advanced_earn.double_win.product_info`."""

  async def product_info(
    self,
    *,
    category: Literal['DoubleWin'],
    coin: str | None = None,
    duration: str | None = None,
    validate: bool | None = None,
  ) -> DoubleWinProductList:
    """Get available Double Win structured products. No authentication required.

    Args:
      category: Product category.
      coin: Underlying asset to filter by, e.g. `BTC`, `ETH`.
      duration: Product duration to filter by, e.g. "1d", "2d", "3d".
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/double-win/product-info)
    """
    params: dict = {
      'category': category,
    }
    if coin is not None:
      params['coin'] = coin
    if duration is not None:
      params['duration'] = duration
    r = await self.request('GET', '/v5/earn/advance/product', params=params)
    return self.result(r, adapter, validate)
