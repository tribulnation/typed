# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/earn/liquidity-mining/order` — Get Liquidity Mining Order List."""

from typed_bybit.core import Endpoint, TimestampMillis, timestamp_millis, validator
from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict


class LiquidityMiningOrder(TypedDict):
  """One Liquidity Mining order."""

  orderId: str
  """Unique order identifier."""
  orderLinkId: str
  """Caller-supplied order link identifier."""
  productId: str
  """Product identifier."""
  orderType: Literal['AddLiquidity', 'RemoveLiquidity', 'Reinvest', 'AddMargin']
  """Operation type."""
  baseCoin: str
  """Base coin of the pool."""
  quoteCoin: str
  """Quote coin of the pool."""
  quoteAmount: NotRequired[str]
  """Quote coin amount involved in this order."""
  baseAmount: NotRequired[str]
  """Base coin amount involved in this order."""
  status: Literal['Success', 'Processing', 'Fail']
  """Order status."""
  removeType: NotRequired[Literal['Normal', 'SingleQuoteCoin', 'SingleBaseCoin']]
  """Withdrawal payout mode. `RemoveLiquidity` orders only."""
  removeRate: NotRequired[int]
  """Withdrawal percentage requested. `RemoveLiquidity` orders only."""
  leverage: NotRequired[str]
  """Leverage multiplier at order time. `AddLiquidity` orders only."""
  slippageValue: NotRequired[str]
  """Slippage deducted from the fill. `AddLiquidity` orders only, once `Success`."""
  createdTime: TimestampMillis
  """Order creation time, as a millisecond timestamp."""


class LiquidityMiningOrderHistory(TypedDict):
  """One page of Liquidity Mining order history."""

  orders: list[LiquidityMiningOrder]
  """Order records."""
  nextPageCursor: str
  """Cursor for the next page. Empty when there is no further page."""


adapter = validator[LiquidityMiningOrderHistory](LiquidityMiningOrderHistory)


class Order(Endpoint):
  """`Get Liquidity Mining Order List` — mixed into the router that owns `finance.advanced_earn.liquidity_mining.order`."""

  async def order(
    self,
    *,
    order_id: str | None = None,
    order_link_id: str | None = None,
    product_id: str | None = None,
    order_type: Literal['AddLiquidity', 'RemoveLiquidity', 'Reinvest', 'AddMargin']
    | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    status: Literal['Success', 'Processing'] | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> LiquidityMiningOrderHistory:
    """Get the caller's Liquidity Mining order history. Requires API key "Earn" permission.

    Args:
      order_id: Order ID, to retrieve a single order. Mutually exclusive with `orderLinkId`.
      order_link_id: Caller-supplied order link ID, to retrieve a single order.
      product_id: Product identifier to filter by.
      order_type: Order type to filter by.
      start_time: Start of the order-creation range, as a millisecond timestamp.
      end_time: End of the order-creation range, as a millisecond timestamp.
      status: Order status to filter by. Every terminal status is returned when omitted.
      limit: Results per page. Defaults to 20; maximum 50.
      cursor: Pagination cursor, from a prior response's `nextPageCursor`.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/liquidity-mining/order)
    """
    params = {}
    if order_id is not None:
      params['orderId'] = order_id
    if order_link_id is not None:
      params['orderLinkId'] = order_link_id
    if product_id is not None:
      params['productId'] = product_id
    if order_type is not None:
      params['orderType'] = order_type
    if start_time is not None:
      params['startTime'] = timestamp_millis.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp_millis.dump(end_time)
    if status is not None:
      params['status'] = status
    if limit is not None:
      params['limit'] = limit
    if cursor is not None:
      params['cursor'] = cursor
    r = await self.authed_request(
      'GET', '/v5/earn/liquidity-mining/order', params=params
    )
    return self.result(r, adapter, validate)

  async def order_paged(
    self,
    *,
    order_id: str | None = None,
    order_link_id: str | None = None,
    product_id: str | None = None,
    order_type: Literal['AddLiquidity', 'RemoveLiquidity', 'Reinvest', 'AddMargin']
    | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    status: Literal['Success', 'Processing'] | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[LiquidityMiningOrderHistory]:
    """Yield successive pages of `order`.

    Passes each page's token back as `cursor` and stops when a response carries no
    `nextPageCursor`, or after `max_pages` pages when one is given.
    """
    cursor: str | None = None
    pages = 0
    while True:
      response = await self.order(
        order_id=order_id,
        order_link_id=order_link_id,
        product_id=product_id,
        order_type=order_type,
        start_time=start_time,
        end_time=end_time,
        status=status,
        limit=limit,
        cursor=cursor,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      cursor = response.get('nextPageCursor') if response is not None else None
      if not cursor:
        break
