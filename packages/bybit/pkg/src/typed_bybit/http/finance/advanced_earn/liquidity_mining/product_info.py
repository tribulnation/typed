# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/earn/liquidity-mining/product` — Get Liquidity Mining Product Info."""

from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class LiquidityMiningProduct(TypedDict):
  """One Liquidity Mining pool."""

  productId: str
  """Unique product identifier."""
  baseCoin: str
  """Base coin of the pool."""
  quoteCoin: str
  """Quote coin of the pool."""
  status: Literal['Available', 'NotAvailable']
  """Product availability."""
  maxLeverage: int
  """Maximum leverage multiplier available."""
  minInvestmentQuote: str
  """Minimum investment, in quote coin."""
  minInvestmentBase: str
  """Minimum investment, in base coin."""
  maxInvestmentQuote: str
  """Maximum investment, in quote coin."""
  maxInvestmentBase: str
  """Maximum investment, in base coin."""
  minWithdrawalAmount: NotRequired[str]
  """Minimum withdrawal amount, in quote coin."""
  minReinvestAmount: NotRequired[str]
  """Minimum reinvestment amount, in quote coin."""
  baseCoinPrecision: int
  """Decimal precision for base coin amounts."""
  quoteCoinPrecision: int
  """Decimal precision for quote coin amounts."""
  yieldCoins: NotRequired[list[dict[str, Any]]]
  """Coins this pool generates yield in. Sub-shape not published beyond this summary."""
  apyE8: NotRequired[int]
  """Current APY, e8 precision. Divide by 10^8 for the actual rate."""
  apy7dE8: NotRequired[int]
  """7-day trailing APY, e8 precision. Divide by 10^8 for the actual rate."""
  poolLiquidityValue: NotRequired[str]
  """Total pool liquidity, in quote coin."""
  dailyYield: NotRequired[str]
  """Expected daily yield amount."""
  slippageLevels: NotRequired[list[dict[str, Any]]]
  """Investment thresholds per slippage tier. Sub-shape not published beyond this summary."""
  slippageRateE8List: NotRequired[list[str]]
  """Slippage percentages, e8 precision, one per tier in `slippageLevels`."""
  apyBreakdown: NotRequired[list[dict[str, Any]]]
  """Current APY, broken down by individual coin. Sub-shape not published beyond this summary."""
  apy7dBreakdown: NotRequired[list[dict[str, Any]]]
  """7-day trailing APY, broken down by individual coin. Sub-shape not published beyond this summary."""


class LiquidityMiningProductList(TypedDict):
  """Liquidity Mining pool products."""

  products: list[LiquidityMiningProduct]
  """Pool records."""


adapter = validator[LiquidityMiningProductList](LiquidityMiningProductList)


class ProductInfo(Endpoint):
  """`Get Liquidity Mining Product Info` — mixed into the router that owns `finance.advanced_earn.liquidity_mining.product_info`."""

  async def product_info(
    self,
    *,
    base_coin: str | None = None,
    quote_coin: str | None = None,
    validate: bool | None = None,
  ) -> LiquidityMiningProductList:
    """Get available Liquidity Mining pools: leveraged, dual-coin LP-style yield products. No authentication required.

    Args:
      base_coin: Base coin to filter by, e.g. `BTC`, `ETH`.
      quote_coin: Quote coin to filter by, e.g. `USDT`.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/liquidity-mining/product-info)
    """
    params = {}
    if base_coin is not None:
      params['baseCoin'] = base_coin
    if quote_coin is not None:
      params['quoteCoin'] = quote_coin
    r = await self.request('GET', '/v5/earn/liquidity-mining/product', params=params)
    return self.result(r, adapter, validate)
