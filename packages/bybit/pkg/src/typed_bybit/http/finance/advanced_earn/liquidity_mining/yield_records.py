# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/earn/liquidity-mining/yield-records` — Get Liquidity Mining Yield Records."""

from typed_bybit.core import Endpoint, TimestampMillis, timestamp_millis, validator
from typing_extensions import AsyncIterator, Literal, TypedDict


class LiquidityMiningYieldRecord(TypedDict):
  """One Liquidity Mining yield claim record."""

  coin: str
  """Yield coin received."""
  amount: str
  """Amount received."""
  baseCoin: str
  """Base coin of the pool this yield came from."""
  quoteCoin: str
  """Quote coin of the pool this yield came from."""
  status: Literal['Complete', 'Processing']
  """Record status."""
  createdTime: TimestampMillis
  """Record creation time, as a millisecond timestamp."""
  type: Literal['Manual', 'RemoveLiquidity']
  """How the yield was claimed."""


class LiquidityMiningYieldRecords(TypedDict):
  """One page of Liquidity Mining yield claim records."""

  records: list[LiquidityMiningYieldRecord]
  """Yield claim records."""
  nextPageCursor: str
  """Cursor for the next page. Empty when there is no further page."""


adapter = validator[LiquidityMiningYieldRecords](LiquidityMiningYieldRecords)


class YieldRecords(Endpoint):
  """`Get Liquidity Mining Yield Records` — mixed into the router that owns `finance.advanced_earn.liquidity_mining.yield_records`."""

  async def yield_records(
    self,
    *,
    base_coin: str | None = None,
    quote_coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> LiquidityMiningYieldRecords:
    """Get the caller's Liquidity Mining yield claim history. Requires API key "Earn" permission.

    Args:
      base_coin: Base coin to filter by, e.g. `BTC`, `ETH`.
      quote_coin: Quote coin to filter by, e.g. `USDT`.
      start_time: Start of the queried range, as a millisecond timestamp.
      end_time: End of the queried range, as a millisecond timestamp.
      limit: Results per page. Defaults to 20; maximum 50.
      cursor: Pagination cursor, from a prior response's `nextPageCursor`.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/liquidity-mining/yield-records)
    """
    params = {}
    if base_coin is not None:
      params['baseCoin'] = base_coin
    if quote_coin is not None:
      params['quoteCoin'] = quote_coin
    if start_time is not None:
      params['startTime'] = timestamp_millis.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp_millis.dump(end_time)
    if limit is not None:
      params['limit'] = limit
    if cursor is not None:
      params['cursor'] = cursor
    r = await self.authed_request(
      'GET', '/v5/earn/liquidity-mining/yield-records', params=params
    )
    return self.result(r, adapter, validate)

  async def yield_records_paged(
    self,
    *,
    base_coin: str | None = None,
    quote_coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[LiquidityMiningYieldRecords]:
    """Yield successive pages of `yield_records`.

    Passes each page's token back as `cursor` and stops when a response carries no
    `nextPageCursor`, or after `max_pages` pages when one is given.
    """
    cursor: str | None = None
    pages = 0
    while True:
      response = await self.yield_records(
        base_coin=base_coin,
        quote_coin=quote_coin,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        cursor=cursor,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      cursor = response.get('nextPageCursor') if response is not None else None
      if not cursor:
        break
