# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/earn/advance/product-extra-info` — Get Double Win Fixed Product Quote."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class DoubleWinProductQuote(TypedDict):
  """Live pricing quote for a fixed price-range Double Win product."""

  category: Literal['DoubleWin']
  """Product category."""
  productId: str
  """Product identifier."""
  leverage: str
  """Current maximum leverage multiplier. Empty if there is no active quote."""
  expireTime: TimestampMillis
  """Quote expiry time, as a millisecond timestamp. The quote must be used before this time."""
  maxInvestmentAmount: str
  """Maximum single order amount at the current quote. Empty if there is no active quote."""
  currentPrice: str
  """Current index price of the underlying asset. Pass as `initialPrice` when placing an order."""


adapter = validator[DoubleWinProductQuote](DoubleWinProductQuote)


class ProductQuote(Endpoint):
  """`Get Double Win Fixed Product Quote` — mixed into the router that owns `finance.advanced_earn.double_win.product_quote`."""

  async def product_quote(
    self,
    *,
    category: Literal['DoubleWin'],
    product_id: str,
    validate: bool | None = None,
  ) -> DoubleWinProductQuote:
    """Get a live pricing quote for a fixed price-range Double Win product. No authentication required.

    Args:
      category: Product category.
      product_id: Product identifier.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/double-win/product-quote)
    """
    params: dict = {
      'category': category,
      'productId': product_id,
    }
    r = await self.request('GET', '/v5/earn/advance/product-extra-info', params=params)
    return self.result(r, adapter, validate)
