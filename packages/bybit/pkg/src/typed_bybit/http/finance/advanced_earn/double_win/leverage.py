# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/earn/advance/double-win-leverage` — Get Double Win Custom Product Quote."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class DoubleWinCustomQuote(TypedDict):
  """Leverage available for a custom price range, for an RFQ Double Win product."""

  productId: str
  """Product identifier, echoed back."""
  initialPrice: str
  """Index price submitted, echoed back."""
  lowerPrice: str
  """Lower price bound submitted, echoed back."""
  upperPrice: str
  """Upper price bound submitted, echoed back."""
  leverage: str
  """Leverage multiplier for the given price range, e.g. "3.2"."""
  expireTime: TimestampMillis
  """Quote expiry time, as a millisecond timestamp. The order must be placed before this time."""
  maxInvestmentAmount: str
  """Maximum single order amount for this quote."""


adapter = validator[DoubleWinCustomQuote](DoubleWinCustomQuote)


class Leverage(Endpoint):
  """`Get Double Win Custom Product Quote` — mixed into the router that owns `finance.advanced_earn.double_win.leverage`."""

  async def leverage(
    self,
    *,
    product_id: str,
    initial_price: str,
    lower_price: str,
    upper_price: str,
    validate: bool | None = None,
  ) -> DoubleWinCustomQuote:
    """Get the leverage available for a caller-chosen custom price range, for an RFQ Double Win product. Requires API key "Earn" permission.

    Args:
      product_id: Product identifier.
      initial_price: Current index price of the underlying asset.
      lower_price: Custom lower bound of the price range. Must be a multiple of `priceTickSize` and less than `initialPrice`.
      upper_price: Custom upper bound of the price range. Must be a multiple of `priceTickSize` and greater than `initialPrice`.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/double-win/leverage)
    """
    params: dict = {
      'productId': product_id,
      'initialPrice': initial_price,
      'lowerPrice': lower_price,
      'upperPrice': upper_price,
    }
    r = await self.authed_request(
      'GET', '/v5/earn/advance/double-win-leverage', params=params
    )
    return self.result(r, adapter, validate)
