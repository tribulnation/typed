# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/earn/advance/get-redeem-est-amount-list` — Get Smart Leverage Redeem Estimated Amount."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class SmartLvgRedeemEstimate(TypedDict):
  """One position's estimated redemption amount."""

  success: bool
  """Whether estimation succeeded for this position."""
  positionId: str
  """Position identifier."""
  estRedeemAmount: NotRequired[str]
  """Estimated redeem amount, in `investCoin`."""
  estRedeemTime: NotRequired[TimestampMillis]
  """Expected fund receipt time, as a millisecond timestamp."""
  slippageRate: NotRequired[str]
  """Slippage tolerance applied during redemption."""


class SmartLvgRedeemEstimateList(TypedDict):
  """Estimated redemption amounts for one or more Smart Leverage positions."""

  list: list[SmartLvgRedeemEstimate]
  """One estimate per requested position."""


adapter = validator[SmartLvgRedeemEstimateList](SmartLvgRedeemEstimateList)


class EstRedeem(Endpoint):
  """`Get Smart Leverage Redeem Estimated Amount` — mixed into the router that owns `finance.advanced_earn.smart_lvg.est_redeem`."""

  async def est_redeem(
    self,
    *,
    category: Literal['SmartLeverage'],
    position_ids: str,
    validate: bool | None = None,
  ) -> SmartLvgRedeemEstimateList:
    """Estimate the redemption amount for one or more open Smart Leverage positions. Required before submitting a redeem order. Requires API key "Earn" permission.

    Args:
      category: Product category.
      position_ids: Comma-separated position IDs to estimate. Maximum 5, e.g. "897,898".
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/smart-lvg/est-redeem)
    """
    params: dict = {
      'category': category,
      'positionIds': position_ids,
    }
    r = await self.authed_request(
      'GET', '/v5/earn/advance/get-redeem-est-amount-list', params=params
    )
    return self.result(r, adapter, validate)
