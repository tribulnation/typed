# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/earn/fixed-term/place-order` — Place Fixed Saving Order."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class FixedSavingOrderResult(TypedDict):
  """Identifiers for a placed fixed saving order."""

  orderId: str
  """Exchange-generated order identifier."""
  orderLinkId: str
  """Caller-supplied order link identifier."""


class FixedSavingPlaceOrderRequest(TypedDict):
  """Fixed-term saving stake order."""

  productId: str
  """Product identifier, from the product-info endpoint."""
  category: Literal['FixedTermSaving', 'FundPool', 'FundPoolPremium']
  """Product sub-type."""
  coin: str
  """Coin to stake."""
  amount: str
  """Stake amount. Must be greater than 0."""
  accountType: Literal['FUND', 'UNIFIED']
  """Source account funding the stake."""
  orderLinkId: str
  """Caller-supplied order ID, max 36 characters, used for idempotency. Reusing a value errors."""
  autoInvest: NotRequired[bool]
  """Enable auto-reinvestment on the resulting position. Applies only when `category` is `FundPool`."""


adapter = validator[FixedSavingOrderResult](FixedSavingOrderResult)


class PlaceOrder(Endpoint):
  """`Place Fixed Saving Order` — mixed into the router that owns `finance.fixed_saving.place_order`."""

  async def place_order(
    self,
    fixed_saving_place_order_request: FixedSavingPlaceOrderRequest,
    *,
    validate: bool | None = None,
  ) -> FixedSavingOrderResult:
    """Stake into a fixed-term saving product. Requires API key "Earn" permission.

    Args:
      fixed_saving_place_order_request: Fixed saving stake order.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/earn/fixed-saving/place-order)
    """
    r = await self.authed_request(
      'POST', '/v5/earn/fixed-term/place-order', json=fixed_saving_place_order_request
    )
    return self.result(r, adapter, validate)
