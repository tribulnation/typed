# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/earn/fixed-term/redeem` — Redeem Fixed Saving Position Early."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class FixedSavingRedeemRequest(TypedDict):
  """Early redemption of a FundPool fixed saving position."""

  productId: str
  """Product identifier."""
  category: Literal['FundPool']
  """Product sub-type. Must be `FundPool`."""
  positionId: str
  """Position identifier to redeem."""


class FixedSavingRedeemResult(TypedDict):
  """Estimated principal and earnings returned by an early redemption."""

  redeemAmount: str
  """Estimated redeemed principal."""
  estEarnings: str
  """Estimated earnings at the early-redemption APY."""


adapter = validator[FixedSavingRedeemResult](FixedSavingRedeemResult)


class Redeem(Endpoint):
  """`Redeem Fixed Saving Position Early` — mixed into the router that owns `finance.fixed_saving.redeem`."""

  async def redeem(
    self,
    fixed_saving_redeem_request: FixedSavingRedeemRequest,
    *,
    validate: bool | None = None,
  ) -> FixedSavingRedeemResult:
    """Redeem a FundPool fixed-term saving position before maturity. Requires API key "Earn" permission.

    Args:
      fixed_saving_redeem_request: Position to redeem early.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/earn/fixed-saving/redeem)
    """
    r = await self.authed_request(
      'POST', '/v5/earn/fixed-term/redeem', json=fixed_saving_redeem_request
    )
    return self.result(r, adapter, validate)
