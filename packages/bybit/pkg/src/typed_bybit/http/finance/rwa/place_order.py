# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/earn/rwa/place-order` — Place RWA Order."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class RwaOrderResult(TypedDict):
  """Identifiers for a placed RWA order."""

  orderId: str
  """System-generated order identifier."""
  orderLinkId: str
  """Caller-supplied order link identifier."""


class RwaRedeemOrderRequest(TypedDict):
  """Redeem shares from an RWA product."""

  productId: int
  """Product identifier."""
  orderType: Literal['Redeem']
  """Operation type."""
  coin: str
  """Settlement coin, uppercase, e.g. `USDC`."""
  orderLinkId: str
  """Caller-supplied idempotency key. Max 36 characters, `[a-zA-Z0-9-_]` only."""
  redeemShares: str
  """Share quantity to redeem, as a decimal string."""
  accountType: NotRequired[Literal['FUND', 'UNIFIED']]
  """Destination account. Defaults to `FUND`."""


class RwaStakeOrderRequest(TypedDict):
  """Subscribe (stake) into an RWA product."""

  productId: int
  """Product identifier."""
  orderType: Literal['Stake']
  """Operation type."""
  coin: str
  """Settlement coin, uppercase, e.g. `USDC`."""
  orderLinkId: str
  """Caller-supplied idempotency key. Max 36 characters, `[a-zA-Z0-9-_]` only."""
  stakeAmount: str
  """Amount to stake, as a decimal string."""
  accountType: NotRequired[Literal['FUND', 'UNIFIED']]
  """Source account. Defaults to `FUND`."""


adapter = validator[RwaOrderResult](RwaOrderResult)


class PlaceOrder(Endpoint):
  """`Place RWA Order` — mixed into the router that owns `finance.rwa.place_order`."""

  async def place_order(
    self,
    rwa_stake_order_request_rwa_redeem_order_request: RwaStakeOrderRequest
    | RwaRedeemOrderRequest,
    *,
    validate: bool | None = None,
  ) -> RwaOrderResult:
    """Subscribe to (stake into) or redeem from a Real World Asset product. Requires authentication.

    Args:
      rwa_stake_order_request_rwa_redeem_order_request: RWA stake or redeem order. `stakeAmount` is required for `Stake`, `redeemShares` is required for `Redeem`.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/rwa/place-order)
    """
    r = await self.authed_request(
      'POST',
      '/v5/earn/rwa/place-order',
      json=rwa_stake_order_request_rwa_redeem_order_request,
    )
    return self.result(r, adapter, validate)
