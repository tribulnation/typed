# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/earn/rwa/nav-chart` — Get RWA NAV Chart."""

from typed_bybit.core import (
  Endpoint,
  TimestampIso,
  TimestampSeconds,
  timestamp_seconds,
  validator,
)
from typing_extensions import TypedDict


class RwaNavPoint(TypedDict):
  """One day's NAV for the product."""

  date: TimestampIso
  """Calendar date, `YYYY-MM-DD`, UTC."""
  nav: str
  """Net Asset Value per share on this date."""


class RwaNavChart(TypedDict):
  """Daily NAV history for one RWA product, ordered chronologically."""

  productId: str
  """Product identifier. Documented as `integer` upstream; live capture (2026-08-20) shows it echoed back as a `string` -- the real shape wins per docs/spec/authoring.md rule 6."""
  list: list[RwaNavPoint]
  """One entry per day."""


adapter = validator[RwaNavChart](RwaNavChart)


class NavChart(Endpoint):
  """`Get RWA NAV Chart` — mixed into the router that owns `finance.rwa.nav_chart`."""

  async def nav_chart(
    self,
    *,
    product_id: int,
    start_time: TimestampSeconds | None = None,
    end_time: TimestampSeconds | None = None,
    validate: bool | None = None,
  ) -> RwaNavChart:
    """Get historical daily Net Asset Value per share for an RWA product. No authentication required.

    Args:
      product_id: Product identifier, from the product endpoint.
      start_time: Start of the queried range, as a second timestamp. Defaults to 7 days before `endTime`.
      end_time: End of the queried range, as a second timestamp. Defaults to now. Span with `startTime` cannot exceed 180 days.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/finance/rwa/nav-chart)
    """
    params: dict = {
      'productId': product_id,
    }
    if start_time is not None:
      params['startTime'] = timestamp_seconds.dump(start_time)
    if end_time is not None:
      params['endTime'] = timestamp_seconds.dump(end_time)
    r = await self.request('GET', '/v5/earn/rwa/nav-chart', params=params)
    return self.result(r, adapter, validate)
