# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/ins/whitelist/ip` — Get Institution Whitelist IP."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class InstitutionWhitelistEntry(TypedDict):
  """Whitelisted IP addresses for one client entity."""

  name: str
  """Client entity name."""
  endpointDomain: str
  """Dedicated hostname assigned to this client entity for the Market Maker Gateway/WebSocket."""
  ipList: list[str]
  """Whitelisted IP addresses for this hostname."""


adapter = validator[list[InstitutionWhitelistEntry]](list[InstitutionWhitelistEntry])


class WhitelistIp(Endpoint):
  """`Get Institution Whitelist IP` — mixed into the router that owns `institution.whitelist_ip`."""

  async def whitelist_ip(
    self,
    *,
    validate: bool | None = None,
  ) -> list[InstitutionWhitelistEntry]:
    """Get the whitelisted IP addresses configured for each of this institutional client's dedicated gateway/WebSocket hostnames. Must be requested with a main account API key.

    Args:
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/institution/whitelist-ip)
    """
    r = await self.authed_request('GET', '/v5/ins/whitelist/ip')
    return self.result(r, adapter, validate)
