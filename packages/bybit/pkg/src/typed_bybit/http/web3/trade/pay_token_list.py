# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/alpha/trade/pay-token-list` — Get Pay Token List."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class PayToken(TypedDict):
  """One usable payment token."""

  tokenCode: str
  """Payment token code, in `CEX_<id>` format."""
  symbol: str
  """Token symbol, e.g. `USDT`, `USDC`."""
  tokenDecimals: int
  """Token decimal precision."""
  tokenIconUrlDay: str
  """Token icon URL, light mode."""
  tokenIconUrlNight: str
  """Token icon URL, dark mode."""
  limit: str
  """Maximum trading amount for this payment token."""
  supportChains: list[str]
  """Supported blockchain codes, e.g. `["ETH", "BSC", "SOL"]`."""


adapter = validator[list[PayToken]](list[PayToken])


class PayTokenList(Endpoint):
  """`Get Pay Token List` — mixed into the router that owns `web3.trade.pay_token_list`."""

  async def pay_token_list(
    self,
    *,
    chain_code: str,
    token_address: str,
    validate: bool | None = None,
  ) -> list[PayToken]:
    """List the payment tokens (CEX-side balances) usable to purchase one specific on-chain token.

    Args:
      chain_code: Blockchain code of the target token, e.g. `SOL`, `MANTLE`.
      token_address: Contract address of the target token on the specified chain.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/alpha/trade/pay-token-list)
    """
    params: dict = {
      'chainCode': chain_code,
      'tokenAddress': token_address,
    }
    r = await self.authed_request(
      'POST', '/v5/alpha/trade/pay-token-list', params=params
    )
    return self.result(r, adapter, validate)
