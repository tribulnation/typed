# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/alpha/trade/purchase` — Execute Purchase."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class TradePurchaseRequest(TypedDict):
  """Purchase execution parameters."""

  fromTokenCode: str
  """Payment token identifier, in `CEX_<id>` format, e.g. `CEX_1` for USDT."""
  fromTokenAmount: str
  """Payment amount, as a positive decimal string."""
  toTokenCode: str
  """Target on-chain token code, in `DEX_<id>` format, e.g. `DEX_123`."""
  slippage: str
  """Slippage tolerance, e.g. `0.005` for 0.5%."""
  quoteData: str
  """Base64-encoded quote data, passed unchanged from `web3.trade.trade_quote`."""
  gas: str
  """Estimated gas fee, passed unchanged from `web3.trade.trade_quote`."""
  quoteMode: Literal[0, 1, 2]
  """Quote preference mode used for the underlying quote."""
  correctingCode: str
  """MD5 checksum for data integrity, passed unchanged from `web3.trade.trade_quote`."""
  tenant: NotRequired[str]
  """Optional tenant identifier."""


class TradePurchaseResult(TypedDict):
  """Identifier of the accepted purchase order."""

  orderNo: str
  """System-generated order identifier, for status tracking via `web3.trade.order_list`."""


adapter = validator[TradePurchaseResult](TradePurchaseResult)


class TradePurchase(Endpoint):
  """`Execute Purchase` — mixed into the router that owns `web3.trade.trade_purchase`."""

  async def trade_purchase(
    self,
    trade_purchase_request: TradePurchaseRequest,
    *,
    validate: bool | None = None,
  ) -> TradePurchaseResult:
    """Execute a purchase of an on-chain token using a prior quote from `web3.trade.trade_quote`. The acknowledgement means the request was accepted, not that the trade settled -- this is asynchronous; poll `web3.trade.order_list` for the final status.

    Args:
      trade_purchase_request: Purchase to execute, built from a prior quote.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/alpha/trade/trade-purchase)
    """
    r = await self.authed_request(
      'POST', '/v5/alpha/trade/purchase', json=trade_purchase_request
    )
    return self.result(r, adapter, validate)
