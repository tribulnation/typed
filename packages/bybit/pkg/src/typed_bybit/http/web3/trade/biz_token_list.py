# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/alpha/trade/biz-token-list` — Get Biz Token List."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampSeconds, validator


class BizToken(TypedDict):
  """One tradable on-chain token."""

  tokenCode: str
  """Unique identifier in `DEX_<id>` format, for use in quote/execution requests."""
  chainCode: str
  """Blockchain identifier, e.g. `ETH`, `BSC`, `SOL`, `TRX`, `BASE`."""
  chainIconUrl: str
  """Blockchain icon URL."""
  tokenAddress: str
  """Smart contract address on the chain."""
  symbol: str
  """Token ticker symbol, e.g. `PEPE`."""
  tokenDecimals: int
  """Decimal precision for token amounts."""
  tokenIconUrlDay: str
  """Token icon URL, light mode."""
  tokenIconUrlNight: str
  """Token icon URL, dark mode."""
  createTime: TimestampSeconds
  """Platform listing time."""
  createTimeOnchain: TimestampSeconds
  """Blockchain creation time."""
  riskFlag: Literal[0, 1]
  """Risk indicator."""
  minOrderQuantity: float
  """Minimum transaction size, in payment token units."""
  maxOrderQuantity: float
  """Maximum transaction size, in payment token units."""
  tokenTags: NotRequired[list[int]]
  """Associated tag identifiers."""
  payTokenCodes: NotRequired[list[str]]
  """Supported payment tokens, in `CEX_<id>` format."""


adapter = validator[list[BizToken]](list[BizToken])


class BizTokenList(Endpoint):
  """`Get Biz Token List` — mixed into the router that owns `web3.trade.biz_token_list`."""

  async def biz_token_list(
    self,
    *,
    token_tag: Literal[0, 1, 2] | None = None,
    validate: bool | None = None,
  ) -> list[BizToken]:
    """List on-chain tokens available for Web3/Alpha trading, optionally filtered by tag category.

    Args:
      token_tag: Filter by token category. Defaults to `0` (all tokens).
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/alpha/trade/biz-token-list)
    """
    params = {}
    if token_tag is not None:
      params['tokenTag'] = token_tag
    r = await self.authed_request(
      'POST', '/v5/alpha/trade/biz-token-list', params=params
    )
    return self.result(r, adapter, validate)
