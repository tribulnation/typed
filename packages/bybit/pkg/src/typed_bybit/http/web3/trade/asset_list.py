# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/alpha/trade/asset-list` — Get Asset List."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class AssetHolding(TypedDict):
  """One held on-chain token and its valuation."""

  chainCode: str
  """Blockchain identifier, e.g. `ETH`, `SOL`, `BSC`."""
  chainIconUrl: str
  """Chain icon image URL."""
  tokenAddress: str
  """Token contract address on the chain."""
  tokenCode: str
  """Token identifier in `DEX_<id>` format, for use in quote/execution requests."""
  tokenSymbol: str
  """Token ticker symbol."""
  tokenDecimals: int
  """Decimal precision of the token."""
  tokenIconUrlDay: str
  """Token icon URL, light mode."""
  tokenIconUrlNight: str
  """Token icon URL, dark mode."""
  tokenAmount: str
  """Quantity held."""
  tokenAmountUsd: str
  """USD valuation of the holding."""
  tradeFlag: Literal[0, 1]
  """Tradability status."""
  pnl: NotRequired[str]
  """Unrealized gain/loss, in USD. Absent when unavailable (no cost basis)."""
  pnlRatio: NotRequired[str]
  """Return ratio as a decimal, e.g. `0.15` for +15%. Absent when unavailable."""
  costPrice: NotRequired[str]
  """Average purchase price. Absent when unavailable."""
  lastPrice: str
  """Current market price."""
  costTotalValue: NotRequired[str]
  """Total cost basis, in USD. Absent when unavailable."""
  assetStatus: Literal[0, 1, 2]
  """Token lifecycle state."""
  announcementUrl: NotRequired[str]
  """Delisting announcement URL. Present when `assetStatus` is 1 or 2."""
  estimatedOfflineTime: NotRequired[TimestampMillis]
  """Projected delisting time. Present when `assetStatus` is 1."""
  delistingTime: NotRequired[TimestampMillis]
  """Actual delisting time. Present when `assetStatus` is 2."""


class AssetListResult(TypedDict):
  """Portfolio of on-chain token holdings."""

  totalAssetUsd: str
  """Total portfolio value, in USD."""
  assetList: list[AssetHolding]
  """Token holdings."""


adapter = validator[AssetListResult](AssetListResult)


class AssetList(Endpoint):
  """`Get Asset List` — mixed into the router that owns `web3.trade.asset_list`."""

  async def asset_list(self, *, validate: bool | None = None) -> AssetListResult:
    """List every on-chain token held in the caller's Web3/Alpha wallet, with USD valuation, unrealized PnL and delisting status.

    Args:
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/alpha/trade/asset-list)
    """
    r = await self.authed_request('POST', '/v5/alpha/trade/asset-list')
    return self.result(r, adapter, validate)
