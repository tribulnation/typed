# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/alpha/lp/pool-list` — Get LP Pool List."""

from typing_extensions import NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class PoolListRequest(TypedDict):
  """Pool list filter."""

  tokenSymbol: NotRequired[str]
  """Return only pools containing this token symbol, for example `ETH` or `USDC`."""


class PoolSummary(TypedDict):
  """One liquidity pool."""

  poolAddress: str
  """Pool contract address."""
  poolName: str
  """Pool display name."""
  poolTag: str
  """Pool category tag."""
  apy: str
  """Annual percentage yield."""
  tvl: str
  """Total value locked, in USD."""
  token0Symbol: str
  """First token's symbol."""
  token0IconUrlDay: str
  """First token's icon URL, light mode."""
  token0IconUrlNight: str
  """First token's icon URL, dark mode."""
  token1Symbol: str
  """Second token's symbol."""
  token1IconUrlDay: str
  """Second token's icon URL, light mode."""
  token1IconUrlNight: str
  """Second token's icon URL, dark mode."""
  chainCode: str
  """Blockchain identifier."""
  chainIconUrl: str
  """Blockchain icon URL."""


class PoolListResult(TypedDict):
  """Available liquidity pools."""

  pools: list[PoolSummary]
  """Matching pools."""


adapter = validator[PoolListResult](PoolListResult)


class PoolList(Endpoint):
  """`Get LP Pool List` — mixed into the router that owns `web3.lp.pool_list`."""

  async def pool_list(
    self,
    pool_list_request: PoolListRequest,
    *,
    validate: bool | None = None,
  ) -> PoolListResult:
    """Query available Web3 liquidity pools, with optional filtering by payment token symbol.

    Args:
      pool_list_request: Optional token-symbol filter.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/alpha/lp/pool-list)
    """
    r = await self.authed_request(
      'POST', '/v5/alpha/lp/pool-list', json=pool_list_request
    )
    return self.result(r, adapter, validate)
