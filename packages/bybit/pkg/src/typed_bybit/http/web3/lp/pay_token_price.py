# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/alpha/lp/pay-token-price` — Get LP Pay Token Price."""

from typing_extensions import NotRequired, TypedDict
from typed_bybit.core import Endpoint, TimestampSeconds, validator


class PayTokenPrice(TypedDict):
  """One payment token's current price."""

  tokenCode: str
  """Token code."""
  tokenSymbol: str
  """Token symbol."""
  priceUsd: str
  """Current price, in USD."""
  chainCode: str
  """Blockchain identifier."""
  updateTime: TimestampSeconds
  """Price last-update time."""


class PayTokenPriceRequest(TypedDict):
  """Payment tokens to price."""

  tokenCode: list[str]
  """Token codes to price, for example `["CEX_1", "CEX_2"]`. Up to 50 entries per call."""
  chainCode: NotRequired[str]
  """Filter by blockchain identifier."""


class PayTokenPriceResult(TypedDict):
  """Current payment-token prices."""

  prices: list[PayTokenPrice]
  """Matching token prices."""


adapter = validator[PayTokenPriceResult](PayTokenPriceResult)


class PayTokenPriceEndpoint(Endpoint):
  """`Get LP Pay Token Price` — mixed into the router that owns `web3.lp.pay_token_price`."""

  async def pay_token_price(
    self,
    pay_token_price_request: PayTokenPriceRequest,
    *,
    validate: bool | None = None,
  ) -> PayTokenPriceResult:
    """Query the current USD price for one or more payment tokens, to compute the USD value of a stake amount before the caller confirms it.

    Args:
      pay_token_price_request: Payment tokens to price.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/alpha/lp/pay-token-price)
    """
    r = await self.authed_request(
      'POST', '/v5/alpha/lp/pay-token-price', json=pay_token_price_request
    )
    return self.result(r, adapter, validate)
