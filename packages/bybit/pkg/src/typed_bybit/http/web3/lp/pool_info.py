# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/alpha/lp/pool-info` — Get LP Pool Info."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class PoolInfoRequest(TypedDict):
  """Pool lookup."""

  poolAddress: str
  """Pool contract address, as returned by `web3.lp.pool_list`."""


class PoolInfoResult(TypedDict):
  """Detailed liquidity pool information."""

  poolAddress: str
  """Pool contract address."""
  poolName: str
  """Pool display name."""
  apy: str
  """Annual percentage yield."""
  tvl: str
  """Total value locked, in USD."""
  feeRate: str
  """Pool fee rate, for example `0.003` for 0.3%."""
  token0Symbol: str
  """First token's symbol."""
  token0Reserve: str
  """First token's reserve amount."""
  token1Symbol: str
  """Second token's symbol."""
  token1Reserve: str
  """Second token's reserve amount."""
  priceRangeLower: str
  """Pool's price range lower bound."""
  priceRangeUpper: str
  """Pool's price range upper bound."""
  currentPrice: str
  """Current pool price."""


adapter = validator[PoolInfoResult](PoolInfoResult)


class PoolInfo(Endpoint):
  """`Get LP Pool Info` — mixed into the router that owns `web3.lp.pool_info`."""

  async def pool_info(
    self,
    pool_info_request: PoolInfoRequest,
    *,
    validate: bool | None = None,
  ) -> PoolInfoResult:
    """Query detailed information for one liquidity pool -- APY, fee rate, token reserves, and price range -- for display before staking.

    Args:
      pool_info_request: Pool to query.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/alpha/lp/pool-info)
    """
    r = await self.authed_request(
      'POST', '/v5/alpha/lp/pool-info', json=pool_info_request
    )
    return self.result(r, adapter, validate)
