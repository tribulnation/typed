# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/alpha/prediction/sell` — Execute Sell."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class PredictionSellRequest(TypedDict):
  """Prediction-market sell order parameters."""

  tokenId: str
  """Outcome token ID to sell, from the caller's held prediction-market positions."""
  size: str
  """Number of shares to sell, as a positive decimal string."""
  orderType: Literal[1]
  """Order type. Phase 1 supports only fill-or-kill."""
  slippage: str
  """Maximum acceptable price slippage, as a decimal string (`"0.05"` = 5%)."""
  eventId: str
  """Event ID associated with the token."""
  toTokenCode: NotRequired[Literal['USDC']]
  """Destination token code. Fixed to `USDC` in Phase 1."""


class PredictionSellResult(TypedDict):
  """Identifier of the placed sell order."""

  orderNo: str
  """Order number. Query `web3.prediction.order_list` to check its fill status."""


adapter = validator[PredictionSellResult](PredictionSellResult)


class Sell(Endpoint):
  """`Execute Sell` — mixed into the router that owns `web3.prediction.sell`."""

  async def __call__(
    self,
    prediction_sell_request: PredictionSellRequest,
    *,
    validate: bool | None = None,
  ) -> PredictionSellResult:
    """Execute a sell order for prediction-market outcome token shares, exchanging shares of the outcome token identified by `tokenId` for a destination token (USDC in Phase 1).

    Args:
      prediction_sell_request: Sell order to execute.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/alpha/prediction/sell)
    """
    r = await self.authed_request(
      'POST', '/v5/alpha/prediction/sell', json=prediction_sell_request
    )
    return self.result(r, adapter, validate)
