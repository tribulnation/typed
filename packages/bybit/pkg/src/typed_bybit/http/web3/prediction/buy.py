# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/alpha/prediction/buy` — Execute Buy."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, validator


class PredictionBuyRequest(TypedDict):
  """Prediction-market buy order parameters."""

  tokenId: str
  """Outcome token ID to buy, obtained from `web3.prediction.event_detail`."""
  amount: str
  """USDC investment amount, as a positive decimal string."""
  payTokenCode: Literal['USDC']
  """Payment token code. Fixed to `USDC` in Phase 1."""
  orderType: Literal[1]
  """Order type. Phase 1 supports only fill-or-kill."""
  slippage: str
  """Maximum acceptable price slippage, as a decimal string (`"0.05"` = 5%)."""
  eventId: str
  """Event ID associated with the token."""


class PredictionBuyResult(TypedDict):
  """Identifier of the placed buy order."""

  orderNo: str
  """Order number. Query `web3.prediction.order_list` to track its fill status."""


adapter = validator[PredictionBuyResult](PredictionBuyResult)


class Buy(Endpoint):
  """`Execute Buy` — mixed into the router that owns `web3.prediction.buy`."""

  async def __call__(
    self,
    prediction_buy_request: PredictionBuyRequest,
    *,
    validate: bool | None = None,
  ) -> PredictionBuyResult:
    """Execute a buy order for prediction-market outcome token shares, paying a pay token (USDC in Phase 1) to receive shares of the given outcome token. Upstream requires the caller to have already checked engine availability, fetched the token via the event detail endpoint, previewed the trade via the order estimate endpoint, and obtained explicit user confirmation before calling this endpoint.

    Args:
      prediction_buy_request: Buy order to execute.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/alpha/prediction/buy)
    """
    r = await self.authed_request(
      'POST', '/v5/alpha/prediction/buy', json=prediction_buy_request
    )
    return self.result(r, adapter, validate)
