# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/alpha/prediction/engine-status` — Get Engine Status."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class PredictionEngineStatusResult(TypedDict):
  """Matching-engine availability."""

  available: bool
  """Whether the matching engine is currently available for trading."""


adapter = validator[PredictionEngineStatusResult](PredictionEngineStatusResult)


class EngineStatus(Endpoint):
  """`Get Engine Status` — mixed into the router that owns `web3.prediction.engine_status`."""

  async def __call__(
    self, *, validate: bool | None = None
  ) -> PredictionEngineStatusResult:
    """Query the availability of the prediction-market matching engine. Buy and sell orders cannot be submitted while the engine is unavailable; upstream instructs callers to check this before submitting trading orders.

    Args:
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/alpha/prediction/engine-status)
    """
    r = await self.authed_request('GET', '/v5/alpha/prediction/engine-status')
    return self.result(r, adapter, validate)
