# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/alpha/prediction/pay-token-list` — Get Payment Token List."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class PredictionPayToken(TypedDict):
  """One accepted payment token for prediction-market trading."""

  tokenCode: str
  """Payment token code, for example `USDC`."""
  symbol: str
  """Token symbol."""
  tokenDecimals: int
  """Token decimal precision."""
  tokenIconUrlDay: str
  """Token icon URL, light mode."""
  tokenIconUrlNight: str
  """Token icon URL, dark mode."""
  supportChains: list[str]
  """Supported blockchain codes, for example `["POLYGON"]`."""


adapter = validator[list[PredictionPayToken]](list[PredictionPayToken])


class PayTokenList(Endpoint):
  """`Get Payment Token List` — mixed into the router that owns `web3.prediction.pay_token_list`."""

  async def __call__(self, *, validate: bool | None = None) -> list[PredictionPayToken]:
    """Query the payment tokens accepted for prediction-market trading. Phase 1 supports only USDC.

    Args:
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/alpha/prediction/pay-token-list)
    """
    r = await self.authed_request('GET', '/v5/alpha/prediction/pay-token-list')
    return self.result(r, adapter, validate)
