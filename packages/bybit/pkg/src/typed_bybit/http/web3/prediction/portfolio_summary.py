# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/alpha/prediction/portfolio-summary` — Get Portfolio Summary."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import Endpoint, validator


class PredictionPortfolioSummaryRequest(TypedDict):
  """Portfolio-summary filters."""

  eventType: NotRequired[Literal[1]]
  """Filter by event type."""


class PredictionPortfolioSummaryResult(TypedDict):
  """Prediction-market portfolio overview."""

  positionValue: str
  """Current total market value of all open positions, in USDC."""
  positionValueUsd: str
  """Current total market value of all open positions, in USD."""
  biggestWin: str
  """Largest single realized profit, in USDC."""
  winRate: str
  """Win rate, as a decimal ratio (`"0.67"` = 67%)."""
  winCount: int
  """Number of winning closed positions."""
  lossCount: int
  """Number of losing closed positions."""


adapter = validator[PredictionPortfolioSummaryResult](PredictionPortfolioSummaryResult)


class PortfolioSummary(Endpoint):
  """`Get Portfolio Summary` — mixed into the router that owns `web3.prediction.portfolio_summary`."""

  async def __call__(
    self,
    prediction_portfolio_summary_request: PredictionPortfolioSummaryRequest,
    *,
    validate: bool | None = None,
  ) -> PredictionPortfolioSummaryResult:
    """Retrieve the authenticated user's prediction-market portfolio overview: current position value and win/loss statistics.

    Args:
      prediction_portfolio_summary_request: Optional event-type filter.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/alpha/prediction/portfolio-summary)
    """
    r = await self.authed_request(
      'POST',
      '/v5/alpha/prediction/portfolio-summary',
      json=prediction_portfolio_summary_request,
    )
    return self.result(r, adapter, validate)
