# Generated by typed-dev — see CONTRIBUTING.md.
"""`GET /v5/alpha/prediction/sports/timeline-stages` — Get Sports Timeline Stages."""

from typing_extensions import Literal, TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class PredictionSportsStage(TypedDict):
  """One tournament timeline stage."""

  stageCode: Literal['Groups', 'R32', 'R16', 'QF', 'SF', 'Final']
  """Stage code."""
  stageName: str
  """Localized stage display name."""
  status: Literal[1, 2, 3]
  """Stage timeline status."""
  matchCount: int
  """Total number of matches in this stage."""
  startTime: TimestampMillis
  """Stage start time."""
  endTime: TimestampMillis
  """Stage end time."""


class PredictionSportsTimelineResult(TypedDict):
  """Tournament timeline stages for one sports event type."""

  eventType: Literal[1]
  """Sports event type."""
  stages: list[PredictionSportsStage]
  """Tournament stage timeline."""


adapter = validator[PredictionSportsTimelineResult](PredictionSportsTimelineResult)


class TimelineStages(Endpoint):
  """`Get Sports Timeline Stages` — mixed into the router that owns `web3.prediction.sports.timeline_stages`."""

  async def timeline_stages(
    self,
    *,
    event_type: Literal[1],
    validate: bool | None = None,
  ) -> PredictionSportsTimelineResult:
    """Query the tournament timeline stages for a sports prediction event, with each stage's status and match count -- useful for displaying tournament bracket progress.

    Args:
      event_type: Sports event type. Phase 1: `1` (FIFA_2026).
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/alpha/prediction/sports-timeline-stages)
    """
    params: dict = {
      'eventType': event_type,
    }
    r = await self.authed_request(
      'GET', '/v5/alpha/prediction/sports/timeline-stages', params=params
    )
    return self.result(r, adapter, validate)
