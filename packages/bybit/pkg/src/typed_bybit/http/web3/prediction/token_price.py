# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/alpha/prediction/token-price` — Get Token Price."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, TimestampMillis, validator


class PredictionTokenPrice(TypedDict):
  """Best bid/ask and last-traded price for one outcome token."""

  tokenId: str
  """Outcome token ID."""
  bestBid: str
  """Best bid price, in the [0, 1] range; represents implied probability."""
  bestAsk: str
  """Best ask price, in the [0, 1] range; represents implied probability."""
  lastPrice: str
  """Last traded price, in the [0, 1] range."""
  volume24h: str
  """Trading volume over the last 24 hours, in USDC."""
  updateTime: TimestampMillis
  """Price last-update time."""


class PredictionTokenPriceRequest(TypedDict):
  """Token-price query parameters."""

  tokenIds: list[str]
  """Outcome token IDs to query. Maximum `20`."""


adapter = validator[list[PredictionTokenPrice]](list[PredictionTokenPrice])


class TokenPrice(Endpoint):
  """`Get Token Price` — mixed into the router that owns `web3.prediction.token_price`."""

  async def __call__(
    self,
    prediction_token_price_request: PredictionTokenPriceRequest,
    *,
    validate: bool | None = None,
  ) -> list[PredictionTokenPrice]:
    """Query the current best bid/ask and last-traded prices for up to 20 prediction-market outcome tokens. For full order-book depth use `web3.prediction.order_book` instead.

    Args:
      prediction_token_price_request: Outcome tokens to price.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/alpha/prediction/token-price)
    """
    r = await self.authed_request(
      'POST', '/v5/alpha/prediction/token-price', json=prediction_token_price_request
    )
    return self.result(r, adapter, validate)
