# Generated by typed-dev — see CONTRIBUTING.md.
"""`POST /v5/alpha/prediction/order-book` — Get Order Book."""

from typing_extensions import TypedDict
from typed_bybit.core import Endpoint, validator


class PredictionOrderBookLevelAsksItem(TypedDict):
  """One order-book price level."""

  price: str
  """Price at this level."""
  size: str
  """Available quantity, in shares."""


class PredictionOrderBookLevelBidsItem(TypedDict):
  """One order-book price level."""

  price: str
  """Price at this level."""
  size: str
  """Available quantity, in shares."""


class PredictionOrderBookRequest(TypedDict):
  """Order-book query."""

  tokenIds: list[str]
  """List of outcome token IDs to query. Maximum 20."""


class PredictionOrderBook(TypedDict):
  """Bid/ask depth snapshot for one outcome token."""

  tokenId: str
  """Outcome token ID."""
  bids: list[PredictionOrderBookLevelBidsItem]
  """Buy orders, sorted by price descending."""
  asks: list[PredictionOrderBookLevelAsksItem]
  """Sell orders, sorted by price ascending."""


adapter = validator[list[PredictionOrderBook]](list[PredictionOrderBook])


class OrderBook(Endpoint):
  """`Get Order Book` — mixed into the router that owns `web3.prediction.order_book`."""

  async def __call__(
    self,
    prediction_order_book_request: PredictionOrderBookRequest,
    *,
    validate: bool | None = None,
  ) -> list[PredictionOrderBook]:
    """Query the full order-book snapshot (bid/ask depth) for one or more prediction-market outcome tokens. Useful for estimating price impact before placing a large order.

    Args:
      prediction_order_book_request: Outcome tokens to query.
      validate: Validate the response against the generated schema.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/alpha/prediction/order-book)
    """
    r = await self.authed_request(
      'POST', '/v5/alpha/prediction/order-book', json=prediction_order_book_request
    )
    return self.result(r, adapter, validate)
