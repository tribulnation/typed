# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampSeconds
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  strategyId: str
  """Strategy ID."""
  status: NotRequired[Literal['2', '3', '4', '5', '6', '7']]
  """Order status."""
  symbol: NotRequired[str]
  """Symbol name."""
  strategyType: NotRequired[Literal['twap', 'chaseOrder', 'iceberg', 'pov']]
  """Strategy type."""
  beginTimeE0: NotRequired[TimestampSeconds]
  """Start time, Unix seconds."""
  endTimeE0: NotRequired[TimestampSeconds]
  """End time, Unix seconds."""
  pageSize: NotRequired[int]
  """Page size. Defaults to `20`."""
  cursor: NotRequired[str]
  """Pagination cursor."""


class StrategySubOrder(TypedDict):
  """One order generated by a strategy."""

  strategyId: str
  """Strategy ID."""
  orderId: str
  """Exchange-assigned order ID."""
  symbol: str
  """Symbol name."""
  side: Literal['Buy', 'Sell']
  """Order side."""
  size: str
  """Order quantity."""
  price: str
  """Order price. Empty string for a market sub-order."""
  status: Literal['2', '3', '4', '5', '6', '7']
  """Order status."""
  executedSize: str
  """Executed quantity."""
  dealTimeE3: str
  """Deal time, Unix milliseconds. `"0"` means not yet filled."""
  parentOrderId: str
  """Parent order ID, used in retry scenarios to identify the original order. Empty string when this order is not a retry."""
  createdTimeE3: str
  """Order creation time, Unix milliseconds."""
  updatedTimeE3: str
  """Order last-updated time, Unix milliseconds."""
  code: int
  """Error code. `0` means success."""
  msg: str
  """Error message. Empty string on success."""
  category: Literal[
    'UTA_USDT',
    'UTA_USDC',
    'UTA_USDC_FUTURE',
    'UTA_SPOT',
    'UTA_INVERSE',
    'UTA_INVERSE_FUTURE',
    'UTA_USDT_FUTURE',
  ]
  """Product type."""
  positionIdx: Literal[0, 1, 2]
  """Position index."""
  orderType: str
  """Order type. Documented as an integer (`1`: market, `2`: limit), but the venue's own worked example shows the string `"UNKNOWN"` -- declared `string` to match the observed shape."""
  leverageType: Literal[0, 1]
  """Leverage type."""
  tpPrice: NotRequired[str]
  """Order take-profit price."""
  slPrice: NotRequired[str]
  """Order stop-loss price."""
  orderPriceOffset: str
  """Limit order price offset percentage."""
  positionValue: str
  """Total position value."""
  filledPositionValue: str
  """Filled position value."""


class StrategyOrderListResult(TypedDict):
  """Page of sub-orders generated by a strategy."""

  list: list[StrategySubOrder]
  """Matching sub-orders."""
  nextCursor: str
  """Cursor for the next page. Empty string means no more data."""
  prevCursor: str
  """Cursor for the previous page."""


class OrderList(RpcEndpoint):
  """Query the individual sub-orders generated by a specific strategy."""

  def order_list_paged(
    self,
    *,
    strategy_id: str,
    status: Literal['2', '3', '4', '5', '6', '7'] | None = None,
    symbol: str | None = None,
    strategy_type: Literal['twap', 'chaseOrder', 'iceberg', 'pov'] | None = None,
    begin_time_e0: TimestampSeconds | None = None,
    end_time_e0: TimestampSeconds | None = None,
    page_size: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[StrategySubOrder, str]:
    """Query the individual sub-orders generated by a specific strategy.

    Paged variant of `order_list`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      strategy_id: Strategy ID.
      status: Order status.
      symbol: Symbol name.
      strategy_type: Strategy type.
      begin_time_e0: Start time, Unix seconds.
      end_time_e0: End time, Unix seconds.
      page_size: Page size. Defaults to `20`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/strategy/order-list)
    """

    async def next(cursor: str) -> tuple[list[StrategySubOrder], str | None]:
      response = await self.order_list(
        strategy_id=strategy_id,
        status=status,
        symbol=symbol,
        strategy_type=strategy_type,
        begin_time_e0=begin_time_e0,
        end_time_e0=end_time_e0,
        page_size=page_size,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('list')
      state = response.get('nextCursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def order_list(
    self,
    *,
    strategy_id: str,
    status: Literal['2', '3', '4', '5', '6', '7'] | None = None,
    symbol: str | None = None,
    strategy_type: Literal['twap', 'chaseOrder', 'iceberg', 'pov'] | None = None,
    begin_time_e0: TimestampSeconds | None = None,
    end_time_e0: TimestampSeconds | None = None,
    page_size: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> StrategyOrderListResult:
    """Query the individual sub-orders generated by a specific strategy.

    Args:
      strategy_id: Strategy ID.
      status: Order status.
      symbol: Symbol name.
      strategy_type: Strategy type.
      begin_time_e0: Start time, Unix seconds.
      end_time_e0: End time, Unix seconds.
      page_size: Page size. Defaults to `20`.
      cursor: Pagination cursor.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/strategy/order-list)
    """
    request: Request = Request(strategyId=strategy_id)
    if status is not None:
      request['status'] = status
    if symbol is not None:
      request['symbol'] = symbol
    if strategy_type is not None:
      request['strategyType'] = strategy_type
    if begin_time_e0 is not None:
      request['beginTimeE0'] = begin_time_e0
    if end_time_e0 is not None:
      request['endTimeE0'] = end_time_e0
    if page_size is not None:
      request['pageSize'] = page_size
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/strategy/order-list',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=StrategyOrderListResult,
    )
