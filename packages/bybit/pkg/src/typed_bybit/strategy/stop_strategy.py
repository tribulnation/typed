# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  """Strategy stop parameters."""

  strategyId: str
  """ID of the strategy to stop."""


class StopStrategyResult(TypedDict):
  """Result of stopping a strategy."""

  strategyId: str
  """ID of the stopped strategy."""


class StopStrategy(RpcEndpoint):
  """Stop a running strategy. Once stopped, the strategy cannot be resumed. Strategy status changes to Terminated; unfilled orders (and the unfilled remainder of partially filled orders) are automatically canceled. Already-filled orders are not affected."""

  async def stop_strategy(
    self,
    strategy_id: str,
    *,
    validate: bool | None = None,
  ) -> StopStrategyResult:
    """Stop a running strategy. Once stopped, the strategy cannot be resumed. Strategy status changes to Terminated; unfilled orders (and the unfilled remainder of partially filled orders) are automatically canceled. Already-filled orders are not affected.

    Args:
      strategy_id: ID of the strategy to stop.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/strategy/stop-strategy)
    """
    return await self.request(
      Request(strategyId=strategy_id),
      method='POST',
      path='/v5/strategy/stop',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=StopStrategyResult,
    )
