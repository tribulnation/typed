# Generated by typed-dev — see CONTRIBUTING.md.
"""`order.cancel` — Cancel an order over the WS Trade connection."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.ws import TradeEndpoint


class OrderCancelArgs(TypedDict):
  """Order to cancel, as the `order.cancel` command's `args[0]`, mirroring `POST /v5/order/cancel`'s body."""

  category: Literal['linear', 'inverse', 'spot', 'option']
  """Product type."""
  symbol: str
  """Symbol name, uppercase only, for example `BTCUSDT`."""
  orderId: NotRequired[str]
  """Order ID. Either `orderId` or `orderLinkId` is required."""
  orderLinkId: NotRequired[str]
  """User-customised order ID. Either `orderId` or `orderLinkId` is required."""
  orderFilter: NotRequired[Literal['Order', 'tpslOrder', 'StopOrder']]
  """Spot trading only. Defaults to `Order` if not passed."""


class OrderCancel(TradeEndpoint):
  """Build the `order.cancel` WS Trade command frame."""

  def order_cancel(
    self, args: OrderCancelArgs, *, validate: bool | None = None
  ) -> dict:
    """Build the signed `order.cancel` frame this command would send, without sending it.

    Args:
      args: Command parameters, as the `order.cancel` command's `args[0]`.
      validate: Unused -- accepted so every generated `rpc` method shares one call
        shape (docs/production_standards.md S8). There is no response here to
        validate: this only builds the frame, it never sends it.

    Raises:
      AuthError: The client holds no credentials.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/websocket/trade/guideline)
    """
    return self.socket.build_request('order.cancel', [args])
