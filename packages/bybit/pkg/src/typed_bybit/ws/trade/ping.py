# Generated by typed-dev — see CONTRIBUTING.md.
"""`ping` — Send a heartbeat over the WS Trade connection."""

from typing_extensions import TypedDict
from typed_bybit.core.ws import TradeEndpoint


class PingArgs(TypedDict):
  """Empty -- `ping` takes no parameters."""


class Ping(TradeEndpoint):
  """Build the `ping` WS Trade command frame."""

  def ping(self, args: PingArgs, *, validate: bool | None = None) -> dict:
    """Build the signed `ping` frame this command would send, without sending it.

    Args:
      args: Command parameters, as the `ping` command's `args[0]`.
      validate: Unused -- accepted so every generated `rpc` method shares one call
        shape (docs/production_standards.md S8). There is no response here to
        validate: this only builds the frame, it never sends it.

    Raises:
      AuthError: The client holds no credentials.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/websocket/trade/guideline)
    """
    return self.socket.build_request('ping', [args])
