# Generated by typed-dev — see CONTRIBUTING.md.
"""`order.cancel-batch` — Cancel a batch of orders over the WS Trade connection."""

from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.ws import TradeEndpoint


class OrderCancelBatchOrder(TypedDict):
  """One order to cancel."""

  symbol: str
  """Symbol name, uppercase only."""
  orderId: NotRequired[str]
  """Order ID. Either `orderId` or `orderLinkId` is required."""
  orderLinkId: NotRequired[str]
  """User-customised order ID. Either `orderId` or `orderLinkId` is required."""


class OrderCancelBatchArgs(TypedDict):
  """Batch of orders to cancel, as the `order.cancel-batch` command's `args[0]`, mirroring `POST /v5/order/cancel-batch`'s body."""

  category: Literal['linear', 'option', 'spot', 'inverse']
  """Product type."""
  request: list[OrderCancelBatchOrder]
  """Orders, up to 20 (10 for spot)."""


class OrderCancelBatch(TradeEndpoint):
  """Build the `order.cancel-batch` WS Trade command frame."""

  def order_cancel_batch(
    self, args: OrderCancelBatchArgs, *, validate: bool | None = None
  ) -> dict:
    """Build the signed `order.cancel-batch` frame this command would send, without sending it.

    Args:
      args: Command parameters, as the `order.cancel-batch` command's `args[0]`.
      validate: Unused -- accepted so every generated `rpc` method shares one call
        shape (docs/production_standards.md S8). There is no response here to
        validate: this only builds the frame, it never sends it.

    Raises:
      AuthError: The client holds no credentials.

    References:
      - [Bybit API docs](https://bybit-exchange.github.io/docs/v5/websocket/trade/guideline)
    """
    return self.socket.build_request('order.cancel-batch', [args])
