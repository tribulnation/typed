# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  """The report request to check."""

  queryId: str
  """Report request identifier, as returned by `tax.report_create`."""


class TaxReportStatus(TypedDict):
  """The report's current processing status. `-1` Failed, `0` Queued, `1` Processing, `2` Processed, `3` Expired."""

  status: Literal['-1', '0', '1', '2', '3']
  """Processing status. `-1` Failed, `0` Queued, `1` Processing, `2` Processed, `3` Expired."""


class ReportStatus(RpcEndpoint):
  """Poll the processing status of a previously requested report export, by the `queryId` returned from `tax.report_create`."""

  async def report_status(
    self,
    query_id: str,
    *,
    validate: bool | None = None,
  ) -> TaxReportStatus:
    """Poll the processing status of a previously requested report export, by the `queryId` returned from `tax.report_create`.

    Args:
      query_id: Report request identifier, as returned by `tax.report_create`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/tax/status)
    """
    return await self.request(
      Request(queryId=query_id),
      method='POST',
      path='/fht/compliance/tax/v3/private/status',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=TaxReportStatus,
    )
