# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class FixedAvailableInventoryResult(TypedDict):
  """Available inventory for the requested coin, term and rate."""

  currency: str
  """Coin name."""
  term: Literal[7, 14, 30, 60, 90, 180]
  """Fixed term, in days."""
  annualRate: str
  """Annualized interest rate this inventory figure applies to."""
  availableInventory: str
  """Available inventory in the lending pool at this rate and term."""
  updateTime: TimestampMillis
  """Time this inventory figure was last updated."""


class Request(TypedDict):
  currency: str
  """Coin name."""
  term: Literal['7', '14', '30', '60', '90', '180']
  """Fixed term, in days."""
  annualRate: str
  """Annualized interest rate."""


class AvailableInventory(RpcEndpoint):
  """Query the lending pool's available inventory for a fixed-term loan at a given coin, term and custom annual rate."""

  async def available_inventory(
    self,
    *,
    currency: str,
    term: Literal['7', '14', '30', '60', '90', '180'],
    annual_rate: str,
    validate: bool | None = None,
  ) -> FixedAvailableInventoryResult:
    """Query the lending pool's available inventory for a fixed-term loan at a given coin, term and custom annual rate.

    Args:
      currency: Coin name.
      term: Fixed term, in days.
      annual_rate: Annualized interest rate.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/fixed/available-inventory)
    """
    return await self.request(
      Request(currency=currency, term=term, annualRate=annual_rate),
      method='GET',
      path='/v5/crypto-loan-fixed/available-inventory',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FixedAvailableInventoryResult,
    )
