# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint
from typed_bybit.schemas import CryptoLoanTerm


class FixedBorrowCollateralEntry(TypedDict):
  """One collateral coin and amount."""

  currency: NotRequired[str]
  """Collateral coin."""
  amount: NotRequired[str]
  """Collateral amount."""


class FixedBorrowResult(TypedDict):
  """Created borrow order."""

  orderId: str
  """Loan order ID."""


class Request(TypedDict):
  """Fixed-term borrow order parameters."""

  orderCurrency: str
  """Coin to borrow."""
  orderAmount: str
  """Amount to borrow."""
  annualRate: str
  """Customized annual interest rate offered, for example `0.02` for 2%."""
  term: CryptoLoanTerm
  autoRepay: NotRequired[Literal['true', 'false']]
  """Automatically repay this loan at expiration."""
  repayType: NotRequired[Literal['1', '2']]
  """Repayment strategy applied when the loan is not auto-repaid."""
  strategyType: NotRequired[Literal['PARTIAL', 'FULL']]
  """Order fill strategy."""
  collateralList: NotRequired[list[FixedBorrowCollateralEntry]]
  """Collateral to post, up to 100 coins."""


class Borrow(RpcEndpoint):
  """Place a fixed-term crypto loan borrow order. Matched loan funds are released to the Funding wallet; collateral is deducted from it."""

  async def borrow(
    self,
    *,
    order_currency: str,
    order_amount: str,
    annual_rate: str,
    term: CryptoLoanTerm,
    auto_repay: Literal['true', 'false'] | None = None,
    repay_type: Literal['1', '2'] | None = None,
    strategy_type: Literal['PARTIAL', 'FULL'] | None = None,
    collateral_list: list[FixedBorrowCollateralEntry] | None = None,
    validate: bool | None = None,
  ) -> FixedBorrowResult:
    """Place a fixed-term crypto loan borrow order. Matched loan funds are released to the Funding wallet; collateral is deducted from it.

    Args:
      order_currency: Coin to borrow.
      order_amount: Amount to borrow.
      annual_rate: Customized annual interest rate offered, for example `0.02` for 2%.
      term:
      auto_repay: Automatically repay this loan at expiration.
      repay_type: Repayment strategy applied when the loan is not auto-repaid.
      strategy_type: Order fill strategy.
      collateral_list: Collateral to post, up to 100 coins.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/fixed/borrow)
    """
    request: Request = Request(
      orderCurrency=order_currency,
      orderAmount=order_amount,
      annualRate=annual_rate,
      term=term,
    )
    if auto_repay is not None:
      request['autoRepay'] = auto_repay
    if repay_type is not None:
      request['repayType'] = repay_type
    if strategy_type is not None:
      request['strategyType'] = strategy_type
    if collateral_list is not None:
      request['collateralList'] = collateral_list
    return await self.request(
      request,
      method='POST',
      path='/v5/crypto-loan-fixed/borrow',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FixedBorrowResult,
    )
