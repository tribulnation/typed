# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, TypedDict
from typed_bybit.core.http import RpcEndpoint


FixedCancelBorrowResult = dict[str, Any]


class Request(TypedDict):
  """Cancel-borrow-order parameters."""

  orderId: str
  """Loan order ID to cancel."""


class CancelBorrow(RpcEndpoint):
  """Cancel an existing, not-yet-fully-filled fixed-term borrow order."""

  async def cancel_borrow(
    self,
    order_id: str,
    *,
    validate: bool | None = None,
  ) -> FixedCancelBorrowResult:
    """Cancel an existing, not-yet-fully-filled fixed-term borrow order.

    Args:
      order_id: Loan order ID to cancel.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/fixed/cancel-borrow)
    """
    return await self.request(
      Request(orderId=order_id),
      method='POST',
      path='/v5/crypto-loan-fixed/borrow-order-cancel',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FixedCancelBorrowResult,
    )
