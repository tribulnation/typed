# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


FixedCancelSupplyResult = dict[str, Any]


class Request(TypedDict):
  """Cancel-supply-order parameters."""

  orderId: str
  """Supply order ID to cancel."""
  refundedAccount: NotRequired[Literal['0', '1']]
  """Destination account for the refunded supply. Defaults to Funding."""


class CancelSupply(RpcEndpoint):
  """Cancel an existing, not-yet-fully-filled fixed-term supply order, returning the supplied quantity to the chosen account."""

  async def cancel_supply(
    self,
    order_id: str,
    *,
    refunded_account: Literal['0', '1'] | None = None,
    validate: bool | None = None,
  ) -> FixedCancelSupplyResult:
    """Cancel an existing, not-yet-fully-filled fixed-term supply order, returning the supplied quantity to the chosen account.

    Args:
      order_id: Supply order ID to cancel.
      refunded_account: Destination account for the refunded supply. Defaults to Funding.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/fixed/cancel-supply)
    """
    request: Request = Request(orderId=order_id)
    if refunded_account is not None:
      request['refundedAccount'] = refunded_account
    return await self.request(
      request,
      method='POST',
      path='/v5/crypto-loan-fixed/supply-order-cancel',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FixedCancelSupplyResult,
    )
