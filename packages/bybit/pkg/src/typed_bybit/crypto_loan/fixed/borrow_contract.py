# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class FixedBorrowContract(TypedDict):
  """One filled fixed-term loan contract."""

  annualRate: str
  """Annualized interest rate on this contract."""
  autoRepay: Literal['true', 'false']
  """Deprecated; use `repayType`. Whether auto-repay is enabled."""
  borrowCurrency: str
  """Loan coin."""
  borrowTime: TimestampMillis
  """Time the loan order was filled into this contract."""
  interestPaid: str
  """Interest paid on this contract so far."""
  loanId: str
  """Loan contract ID."""
  orderId: str
  """Loan order ID this contract was filled from."""
  repaymentTime: TimestampMillis
  """Time this contract is due for repayment."""
  residualPenaltyInterest: str
  """Unpaid penalty interest remaining on this contract."""
  residualPrincipal: str
  """Unpaid principal remaining on this contract."""
  status: Literal[1, 2, 3]
  """Contract status."""
  term: Literal['7', '14', '30', '60', '90', '180']
  """Fixed term, in days."""
  repayType: Literal['1', '2', '0']
  """Repayment handling at expiry."""


class Request(TypedDict):
  orderId: NotRequired[str]
  """Loan order ID."""
  loanId: NotRequired[str]
  """Loan contract ID."""
  orderCurrency: NotRequired[str]
  """Loan coin name."""
  term: NotRequired[Literal['7', '14', '30', '60', '90', '180']]
  """Fixed term, in days."""
  limit: NotRequired[int]
  """Page size."""
  cursor: NotRequired[str]
  """Pagination cursor."""


class FixedBorrowContractResult(TypedDict):
  """Page of matching loan contracts."""

  list: list[FixedBorrowContract]
  """Matching loan contracts."""
  nextPageCursor: NotRequired[str]
  """Opaque cursor for the next page. Pass it back as `cursor`; an empty string means there are no further pages."""


class BorrowContract(RpcEndpoint):
  """Query the filled/active loan contracts created from this account's fixed-term borrow orders."""

  def borrow_contract_paged(
    self,
    *,
    order_id: str | None = None,
    loan_id: str | None = None,
    order_currency: str | None = None,
    term: Literal['7', '14', '30', '60', '90', '180'] | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[FixedBorrowContract, str]:
    """Query the filled/active loan contracts created from this account's fixed-term borrow orders.

    Paged variant of `borrow_contract`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      order_id: Loan order ID.
      loan_id: Loan contract ID.
      order_currency: Loan coin name.
      term: Fixed term, in days.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/fixed/borrow-contract)
    """

    async def next(cursor: str) -> tuple[list[FixedBorrowContract], str | None]:
      response = await self.borrow_contract(
        order_id=order_id,
        loan_id=loan_id,
        order_currency=order_currency,
        term=term,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('list')
      state = response.get('nextPageCursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def borrow_contract(
    self,
    *,
    order_id: str | None = None,
    loan_id: str | None = None,
    order_currency: str | None = None,
    term: Literal['7', '14', '30', '60', '90', '180'] | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> FixedBorrowContractResult:
    """Query the filled/active loan contracts created from this account's fixed-term borrow orders.

    Args:
      order_id: Loan order ID.
      loan_id: Loan contract ID.
      order_currency: Loan coin name.
      term: Fixed term, in days.
      limit: Page size.
      cursor: Pagination cursor.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/fixed/borrow-contract)
    """
    request: Request = Request()
    if order_id is not None:
      request['orderId'] = order_id
    if loan_id is not None:
      request['loanId'] = loan_id
    if order_currency is not None:
      request['orderCurrency'] = order_currency
    if term is not None:
      request['term'] = term
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/crypto-loan-fixed/borrow-contract-info',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FixedBorrowContractResult,
    )
