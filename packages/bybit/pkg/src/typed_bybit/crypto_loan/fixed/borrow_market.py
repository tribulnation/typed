# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class FixedBorrowMarketOffer(TypedDict):
  """One resting fixed-term borrowing offer."""

  orderCurrency: str
  """Coin name."""
  term: Literal[7, 14, 30, 60, 90, 180]
  """Fixed term, in days."""
  annualRate: str
  """Annualized interest rate."""
  qty: str
  """Quantity available at this rate and term."""


class Request(TypedDict):
  orderCurrency: str
  """Coin name."""
  orderBy: Literal['apy', 'term', 'quantity']
  """Sort field."""
  term: NotRequired[Literal['7', '14', '30', '60', '90', '180']]
  """Fixed term, in days."""
  sort: NotRequired[Literal[0, 1]]
  """Sort direction."""
  limit: NotRequired[int]
  """Page size."""


class FixedBorrowMarketResult(TypedDict):
  """Resting fixed-term borrowing offers, sorted per `orderBy`/`sort`."""

  list: list[FixedBorrowMarketOffer]
  """Resting offers."""


class BorrowMarket(RpcEndpoint):
  """Query the resting fixed-term lending offers available to borrow against, for one coin."""

  async def borrow_market(
    self,
    order_currency: str,
    *,
    order_by: Literal['apy', 'term', 'quantity'],
    term: Literal['7', '14', '30', '60', '90', '180'] | None = None,
    sort: Literal[0, 1] | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> FixedBorrowMarketResult:
    """Query the resting fixed-term lending offers available to borrow against, for one coin.

    Args:
      order_currency: Coin name.
      order_by: Sort field.
      term: Fixed term, in days.
      sort: Sort direction.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/fixed/borrow-market)
    """
    request: Request = Request(orderCurrency=order_currency, orderBy=order_by)
    if term is not None:
      request['term'] = term
    if sort is not None:
      request['sort'] = sort
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/v5/crypto-loan-fixed/borrow-order-quote',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=FixedBorrowMarketResult,
    )
