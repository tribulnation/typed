# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class FixedRepayResult(TypedDict):
  """Created repayment."""

  repayId: str
  """Repayment transaction ID."""


class Request(TypedDict):
  """Fixed-term full-repayment parameters."""

  loanId: NotRequired[str]
  """Loan contract ID. Either this or `loanCurrency` must be provided."""
  loanCurrency: NotRequired[str]
  """Loan coin. Either this or `loanId` must be provided."""


class Repay(RpcEndpoint):
  """Fully repay an active fixed-term crypto loan, identified either by its loan contract or its loan currency."""

  async def repay(
    self,
    *,
    loan_id: str | None = None,
    loan_currency: str | None = None,
    validate: bool | None = None,
  ) -> FixedRepayResult:
    """Fully repay an active fixed-term crypto loan, identified either by its loan contract or its loan currency.

    Args:
      loan_id: Loan contract ID. Either this or `loanCurrency` must be provided.
      loan_currency: Loan coin. Either this or `loanId` must be provided.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/fixed/repay)
    """
    request: Request = Request()
    if loan_id is not None:
      request['loanId'] = loan_id
    if loan_currency is not None:
      request['loanCurrency'] = loan_currency
    return await self.request(
      request,
      method='POST',
      path='/v5/crypto-loan-fixed/fully-repay',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FixedRepayResult,
    )
