# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class FixedSupplyMarketQuote(TypedDict):
  """One resting fixed-term borrowing demand entry."""

  orderCurrency: str
  """Coin name."""
  term: Literal[7, 14, 30, 60, 90, 180]
  """Fixed term, in days."""
  annualRate: str
  """Annualized interest rate."""
  qty: str
  """Quantity demanded at this rate and term."""


class Request(TypedDict):
  orderCurrency: str
  """Coin name."""
  term: NotRequired[Literal['7', '14', '30', '60', '90', '180']]
  """Fixed term, in days."""
  orderBy: Literal['apy', 'term', 'quantity']
  """Sort field."""
  sort: NotRequired[Literal[0, 1]]
  """Sort direction."""
  limit: NotRequired[int]
  """Page size."""


class FixedSupplyMarketResult(TypedDict):
  """Resting fixed-term borrowing demand, sorted per `orderBy`/`sort`."""

  list: list[FixedSupplyMarketQuote]
  """Resting demand."""


class SupplyMarket(RpcEndpoint):
  """Query the resting fixed-term borrowing demand available to supply against, for one coin."""

  async def supply_market(
    self,
    order_currency: str,
    *,
    term: Literal['7', '14', '30', '60', '90', '180'] | None = None,
    order_by: Literal['apy', 'term', 'quantity'],
    sort: Literal[0, 1] | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> FixedSupplyMarketResult:
    """Query the resting fixed-term borrowing demand available to supply against, for one coin.

    Args:
      order_currency: Coin name.
      term: Fixed term, in days.
      order_by: Sort field.
      sort: Sort direction.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/fixed/supply-market)
    """
    request: Request = Request(orderCurrency=order_currency, orderBy=order_by)
    if term is not None:
      request['term'] = term
    if sort is not None:
      request['sort'] = sort
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/v5/crypto-loan-fixed/supply-order-quote',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=FixedSupplyMarketResult,
    )
