# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint
from typed_bybit.schemas import CryptoLoanTerm


class FixedSupplyResult(TypedDict):
  """Created supply order."""

  orderId: str
  """Supply order ID."""


class Request(TypedDict):
  """Fixed-term supply order parameters."""

  orderCurrency: str
  """Coin to supply."""
  orderAmount: str
  """Amount to supply."""
  annualRate: str
  """Customized annual interest rate requested, for example `0.02` for 2%."""
  term: CryptoLoanTerm
  availableSource: NotRequired[Literal['0', '1', '2']]
  """Account to draw the supplied quantity from. Defaults to Funding."""


class Supply(RpcEndpoint):
  """Supply a coin for fixed-term lending. Locks the supplied quantity for the chosen term, earning the specified annual interest rate once matched."""

  async def supply(
    self,
    *,
    order_currency: str,
    order_amount: str,
    annual_rate: str,
    term: CryptoLoanTerm,
    available_source: Literal['0', '1', '2'] | None = None,
    validate: bool | None = None,
  ) -> FixedSupplyResult:
    """Supply a coin for fixed-term lending. Locks the supplied quantity for the chosen term, earning the specified annual interest rate once matched.

    Args:
      order_currency: Coin to supply.
      order_amount: Amount to supply.
      annual_rate: Customized annual interest rate requested, for example `0.02` for 2%.
      term:
      available_source: Account to draw the supplied quantity from. Defaults to Funding.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/fixed/supply)
    """
    request: Request = Request(
      orderCurrency=order_currency,
      orderAmount=order_amount,
      annualRate=annual_rate,
      term=term,
    )
    if available_source is not None:
      request['availableSource'] = available_source
    return await self.request(
      request,
      method='POST',
      path='/v5/crypto-loan-fixed/supply',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FixedSupplyResult,
    )
