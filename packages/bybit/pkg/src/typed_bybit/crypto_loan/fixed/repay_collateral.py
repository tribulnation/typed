# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


FixedRepayCollateralResult = dict[str, Any]


class Request(TypedDict):
  """Fixed-term collateral-repayment parameters."""

  loanId: NotRequired[str]
  """Loan contract ID. When omitted, repayment is applied by currency offset across contracts automatically."""
  loanCurrency: str
  """Borrowed coin being repaid."""
  collateralCoin: str
  """Collateral coin(s) to convert, comma-separated for more than one."""
  amount: str
  """Repayment quantity, in the loan currency."""


class RepayCollateral(RpcEndpoint):
  """Repay a fixed-term loan by converting posted collateral assets into the loan currency, instead of repaying with the loan currency directly."""

  async def repay_collateral(
    self,
    *,
    loan_id: str | None = None,
    loan_currency: str,
    collateral_coin: str,
    amount: str,
    validate: bool | None = None,
  ) -> FixedRepayCollateralResult:
    """Repay a fixed-term loan by converting posted collateral assets into the loan currency, instead of repaying with the loan currency directly.

    Args:
      loan_id: Loan contract ID. When omitted, repayment is applied by currency offset across contracts automatically.
      loan_currency: Borrowed coin being repaid.
      collateral_coin: Collateral coin(s) to convert, comma-separated for more than one.
      amount: Repayment quantity, in the loan currency.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/fixed/repay-collateral)
    """
    request: Request = Request(
      loanCurrency=loan_currency, collateralCoin=collateral_coin, amount=amount
    )
    if loan_id is not None:
      request['loanId'] = loan_id
    return await self.request(
      request,
      method='POST',
      path='/v5/crypto-loan-fixed/repay-collateral',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FixedRepayCollateralResult,
    )
