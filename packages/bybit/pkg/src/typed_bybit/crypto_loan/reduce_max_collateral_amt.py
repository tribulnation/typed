# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class MaxCollateralReductionResult(TypedDict):
  """Maximum collateral reduction calculation result."""

  maxCollateralAmount: str
  """Maximum quantity of this collateral coin that could be redeemed without breaching the required loan-to-value ratio."""


class Request(TypedDict):
  currency: str
  """Collateral coin."""


class ReduceMaxCollateralAmt(RpcEndpoint):
  """Calculate the maximum quantity of a collateral coin that could currently be redeemed while keeping the account's loan-to-value ratio within its required bound. Read-only; does not move any collateral."""

  async def reduce_max_collateral_amt(
    self,
    currency: str,
    *,
    validate: bool | None = None,
  ) -> MaxCollateralReductionResult:
    """Calculate the maximum quantity of a collateral coin that could currently be redeemed while keeping the account's loan-to-value ratio within its required bound. Read-only; does not move any collateral.

    Args:
      currency: Collateral coin.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/reduce-max-collateral-amt)
    """
    return await self.request(
      Request(currency=currency),
      method='GET',
      path='/v5/crypto-loan-common/max-collateral-amount',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=MaxCollateralReductionResult,
    )
