# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class AdjustCollateralResult(TypedDict):
  """Identifier of the adjustment transaction."""

  adjustId: str
  """Adjustment transaction ID."""


class Request(TypedDict):
  """Collateral adjustment parameters."""

  currency: str
  """Collateral coin."""
  amount: str
  """Quantity to add or remove."""
  direction: Literal['0', '1']
  """Adjustment direction."""


class AdjustCollateral(RpcEndpoint):
  """Add or remove collateral backing this account's crypto loan position, changing its loan-to-value ratio. Adding debits the Funding wallet and increases posted collateral; reducing credits the Funding wallet and decreases posted collateral, subject to the maximum reduction the account's current LTV allows."""

  async def adjust_collateral(
    self,
    *,
    currency: str,
    amount: str,
    direction: Literal['0', '1'],
    validate: bool | None = None,
  ) -> AdjustCollateralResult:
    """Add or remove collateral backing this account's crypto loan position, changing its loan-to-value ratio. Adding debits the Funding wallet and increases posted collateral; reducing credits the Funding wallet and decreases posted collateral, subject to the maximum reduction the account's current LTV allows.

    Args:
      currency: Collateral coin.
      amount: Quantity to add or remove.
      direction: Adjustment direction.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/adjust-collateral)
    """
    return await self.request(
      Request(currency=currency, amount=amount, direction=direction),
      method='POST',
      path='/v5/crypto-loan-common/adjust-ltv',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=AdjustCollateralResult,
    )
