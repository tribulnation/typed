# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class FlexibleRepayCollateralResult(TypedDict):
  """Empty object confirming the repayment succeeded."""


class Request(TypedDict):
  """Collateral-repayment parameters."""

  loanCurrency: str
  """Loan coin."""
  collateralCoin: str
  """Collateral coin(s) to repay with, comma-separated for multiple."""
  amount: str
  """Amount to repay."""


class RepayCollateral(RpcEndpoint):
  """Repay flexible-loan debt by converting posted collateral, instead of debiting the Funding account."""

  async def repay_collateral(
    self,
    *,
    loan_currency: str,
    collateral_coin: str,
    amount: str,
    validate: bool | None = None,
  ) -> FlexibleRepayCollateralResult:
    """Repay flexible-loan debt by converting posted collateral, instead of debiting the Funding account.

    Args:
      loan_currency: Loan coin.
      collateral_coin: Collateral coin(s) to repay with, comma-separated for multiple.
      amount: Amount to repay.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/flexible/repay-collateral)
    """
    return await self.request(
      Request(
        loanCurrency=loan_currency, collateralCoin=collateral_coin, amount=amount
      ),
      method='POST',
      path='/v5/crypto-loan-flexible/repay-collateral',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FlexibleRepayCollateralResult,
    )
