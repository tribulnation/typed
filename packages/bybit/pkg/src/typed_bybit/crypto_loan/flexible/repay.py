# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class FlexibleRepayResult(TypedDict):
  """Identifier of the accepted repayment."""

  repayId: str
  """Repayment transaction ID."""


class Request(TypedDict):
  """Flexible-loan repayment parameters."""

  loanCurrency: str
  """Loan coin."""
  amount: str
  """Amount to repay."""


class Repay(RpcEndpoint):
  """Repay flexible-loan debt in one coin, up to the requested amount. Interest is paid before principal. The repaid amount is deducted from the Funding wallet."""

  async def repay(
    self,
    *,
    loan_currency: str,
    amount: str,
    validate: bool | None = None,
  ) -> FlexibleRepayResult:
    """Repay flexible-loan debt in one coin, up to the requested amount. Interest is paid before principal. The repaid amount is deducted from the Funding wallet.

    Args:
      loan_currency: Loan coin.
      amount: Amount to repay.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/flexible/repay)
    """
    return await self.request(
      Request(loanCurrency=loan_currency, amount=amount),
      method='POST',
      path='/v5/crypto-loan-flexible/repay',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FlexibleRepayResult,
    )
