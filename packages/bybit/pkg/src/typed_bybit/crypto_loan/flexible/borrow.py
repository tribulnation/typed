# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class FlexibleBorrowCollateralItem(TypedDict):
  """One collateral holding to pledge."""

  currency: NotRequired[str]
  """Coin to pledge as collateral."""
  amount: NotRequired[str]
  """Amount to pledge."""


class FlexibleBorrowResult(TypedDict):
  """Identifier of the accepted loan."""

  orderId: str
  """Loan order ID."""


class Request(TypedDict):
  """Flexible-loan borrow parameters."""

  loanCurrency: str
  """Coin to borrow."""
  loanAmount: str
  """Amount to borrow."""
  collateralList: NotRequired[list[FlexibleBorrowCollateralItem]]
  """Collateral to pledge in the same call, up to 100 coins."""


class Borrow(RpcEndpoint):
  """Borrow a coin under a flexible (open-ended, hourly-interest) crypto loan, optionally pledging collateral in the same call."""

  async def borrow(
    self,
    *,
    loan_currency: str,
    loan_amount: str,
    collateral_list: list[FlexibleBorrowCollateralItem] | None = None,
    validate: bool | None = None,
  ) -> FlexibleBorrowResult:
    """Borrow a coin under a flexible (open-ended, hourly-interest) crypto loan, optionally pledging collateral in the same call.

    Args:
      loan_currency: Coin to borrow.
      loan_amount: Amount to borrow.
      collateral_list: Collateral to pledge in the same call, up to 100 coins.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/flexible/borrow)
    """
    request: Request = Request(loanCurrency=loan_currency, loanAmount=loan_amount)
    if collateral_list is not None:
      request['collateralList'] = collateral_list
    return await self.request(
      request,
      method='POST',
      path='/v5/crypto-loan-flexible/borrow',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FlexibleBorrowResult,
    )
