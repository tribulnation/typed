# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, TypedDict
from typed_bybit.core.ws import StreamsEndpoint
from typed_bybit.schemas import RpiOrderbookLevel


class LinearRpiOrderbookUpdate(TypedDict):
  """One RPI order book snapshot or delta for one symbol."""

  s: str
  """Symbol name."""
  b: list[RpiOrderbookLevel]
  """Bid levels changed in this push."""
  a: list[RpiOrderbookLevel]
  """Ask levels changed in this push, in the same shape as `b`."""
  u: int
  """Update id, increasing per push."""
  seq: int
  """Cross-sequence id, shared across `linear` symbols."""


class OrderbookRpi(StreamsEndpoint):
  """Subscribe to level 50 RPI (Retail Price Improvement) order book snapshots and deltas for one symbol."""

  def orderbook_rpi(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[LinearRpiOrderbookUpdate, Any, Any]:
    """Subscribe to level 50 RPI (Retail Price Improvement) order book snapshots and deltas for one symbol.

    Args:
      symbol: Symbol name, for example `BTCUSDT`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/websocket/public/orderbook-rpi)
    """
    return self.subscribe(
      f'orderbook.rpi.{symbol}',
      validate=validate,
      response_type=LinearRpiOrderbookUpdate,
    )
