# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.ws import StreamsEndpoint
from typed_bybit.schemas import TickDirection


class LinearTicker(TypedDict):
  """One ticker snapshot, or a delta carrying only the fields that changed."""

  type: NotRequired[Literal['snapshot', 'delta']]
  """Whether this push is a full `snapshot` or a `delta` against the previous one. Read off the frame by the core, not part of the wire `data` object."""
  symbol: str
  """Symbol name."""
  tickDirection: NotRequired[TickDirection]
  price24hPcnt: NotRequired[Decimal]
  """24-hour price change ratio."""
  lastPrice: NotRequired[Decimal]
  """Last traded price."""
  prevPrice24h: NotRequired[Decimal]
  """Price 24 hours ago."""
  highPrice24h: NotRequired[Decimal]
  """24-hour high price."""
  lowPrice24h: NotRequired[Decimal]
  """24-hour low price."""
  prevPrice1h: NotRequired[Decimal]
  """Price 1 hour ago."""
  markPrice: NotRequired[Decimal]
  """Mark price."""
  indexPrice: NotRequired[Decimal]
  """Index price."""
  openInterest: NotRequired[Decimal]
  """Open interest size, in the base coin."""
  openInterestValue: NotRequired[Decimal]
  """Open interest value, in the quote coin."""
  turnover24h: NotRequired[Decimal]
  """24-hour turnover, in the quote coin."""
  volume24h: NotRequired[Decimal]
  """24-hour trading volume, in the base coin."""
  fundingIntervalHour: NotRequired[Literal[''] | int]
  """Hours between funding settlements. Perpetuals only. Empty string on a dated future, confirmed on the REST twin."""
  fundingCap: NotRequired[Literal[''] | Decimal]
  """Funding rate cap for this settlement interval. Perpetuals only. Empty string on a dated future, confirmed on the REST twin."""
  singleOpenInterest: NotRequired[Decimal]
  """Open interest size for this single contract, in the base coin."""
  singleOpenInterestValue: NotRequired[Decimal]
  """Open interest value for this single contract, in the quote coin."""
  nextFundingTime: NotRequired[TimestampMillis]
  """Next funding settlement time. Perpetuals only."""
  fundingRate: NotRequired[Literal[''] | Decimal]
  """Current funding rate. Perpetuals only. Empty string on a dated future, confirmed on the REST twin."""
  bid1Price: NotRequired[Decimal]
  """Best bid price."""
  bid1Size: NotRequired[Decimal]
  """Best bid size."""
  ask1Price: NotRequired[Decimal]
  """Best ask price."""
  ask1Size: NotRequired[Decimal]
  """Best ask size."""
  preOpenPrice: NotRequired[Literal[''] | Decimal]
  """Estimated pre-market opening price. Empty outside a pre-market listing phase."""
  preQty: NotRequired[Literal[''] | Decimal]
  """Estimated pre-market opening quantity. Empty outside a pre-market listing phase."""
  curPreListingPhase: NotRequired[str]
  """Current pre-market listing phase. Empty outside a pre-market listing phase."""
  deliveryTime: NotRequired[TimestampMillis]
  """Delivery date time. Futures only."""
  basisRate: NotRequired[Literal[''] | Decimal]
  """Basis rate. Futures only. Empty string on a perpetual, confirmed on the REST twin."""
  basisRateYear: NotRequired[Literal[''] | Decimal]
  """Annualized basis rate. Futures only. Empty string on a perpetual, confirmed on the REST twin."""
  predictedDeliveryPrice: NotRequired[Literal[''] | Decimal]
  """Predicted delivery price. Futures only, and only in the last hour before delivery. Empty string on a perpetual, confirmed on the REST twin."""


class Ticker(StreamsEndpoint):
  """Subscribe to ticker snapshots and deltas for one linear symbol."""

  def ticker(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[LinearTicker, Any, Any]:
    """Subscribe to ticker snapshots and deltas for one linear symbol.

    Args:
      symbol: Symbol name, for example `BTCUSDT`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/websocket/public/ticker)
    """
    return self.subscribe(
      f'tickers.{symbol}',
      validate=validate,
      response_type=LinearTicker,
    )
