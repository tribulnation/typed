# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.ws import StreamsEndpoint
from typed_bybit.schemas import TickDirection


class InversePublicTrade(TypedDict):
  """One matched trade."""

  i: str
  """Trade id."""
  T: TimestampMillis
  """Fill timestamp."""
  p: str
  """Trade price."""
  v: str
  """Trade quantity."""
  S: Literal['Buy', 'Sell']
  """Taker side."""
  L: TickDirection
  seq: int
  """Cross-sequence id. Multiple pushes may share one value."""
  s: str
  """Symbol name."""
  BT: bool
  """Whether this is a block trade."""
  RPI: bool
  """Whether this trade involved RPI (Retail Price Improvement) liquidity."""


InversePublicTrades = list[InversePublicTrade]


class Trade(StreamsEndpoint):
  """Subscribe to real-time public trade prints for one symbol."""

  def trade(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[InversePublicTrades, Any, Any]:
    """Subscribe to real-time public trade prints for one symbol.

    Args:
      symbol: Symbol name, for example `BTCUSD`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/websocket/public/trade)
    """
    return self.subscribe(
      f'publicTrade.{symbol}',
      validate=validate,
      response_type=InversePublicTrades,
    )
