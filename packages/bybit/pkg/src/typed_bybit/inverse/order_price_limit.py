# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, TypedDict
from typed_bybit.core.ws import StreamsEndpoint


class InverseOrderPriceLimitUpdate(TypedDict):
  """The current order price bounds for one symbol."""

  symbol: str
  """Symbol name."""
  buyLmt: str
  """Highest price a buy order may be placed at."""
  sellLmt: str
  """Lowest price a sell order may be placed at."""


class OrderPriceLimit(StreamsEndpoint):
  """Subscribe to the current allowable order price bounds for one symbol."""

  def order_price_limit(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[InverseOrderPriceLimitUpdate, Any, Any]:
    """Subscribe to the current allowable order price bounds for one symbol.

    Args:
      symbol: Symbol name, for example `BTCUSD`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/websocket/public/order-price-limit)
    """
    return self.subscribe(
      f'priceLimit.{symbol}',
      validate=validate,
      response_type=InverseOrderPriceLimitUpdate,
    )
