# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.ws import StreamsEndpoint


class InverseInsurancePoolBalance(TypedDict):
  """One insurance pool's balance, covering one or more symbols."""

  coin: str
  """Coin the pool is denominated in."""
  symbols: str
  """Comma-separated symbols sharing this pool."""
  balance: str
  """Pool balance."""
  updateTime: TimestampMillis
  """Time this balance was last updated."""


InverseInsurancePoolUpdates = list[InverseInsurancePoolBalance]


class InsurancePool(StreamsEndpoint):
  """Subscribe to insurance pool balance changes for the `inverse` settlement group."""

  def insurance_pool(
    self,
    coin: Literal['inverse'],
    *,
    validate: bool | None = None,
  ) -> StreamManager[InverseInsurancePoolUpdates, Any, Any]:
    """Subscribe to insurance pool balance changes for the `inverse` settlement group.

    Args:
      coin: Settlement group. Fixed to `inverse` on this connection.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/websocket/public/insurance-pool)
    """
    return self.subscribe(
      f'insurance.{coin}',
      validate=validate,
      response_type=InverseInsurancePoolUpdates,
    )
