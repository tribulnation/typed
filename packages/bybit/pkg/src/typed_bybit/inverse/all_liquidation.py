# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.ws import StreamsEndpoint


class InverseAllLiquidation(TypedDict):
  """One liquidated position."""

  T: TimestampMillis
  """Liquidation event timestamp."""
  s: str
  """Symbol name."""
  S: Literal['Buy', 'Sell']
  """Position side being liquidated. `Buy` means a long position was liquidated."""
  v: str
  """Liquidated quantity."""
  p: str
  """Bankruptcy price."""


InverseAllLiquidationUpdates = list[InverseAllLiquidation]


class AllLiquidation(StreamsEndpoint):
  """Subscribe to every liquidation order matched for one symbol."""

  def all_liquidation(
    self,
    symbol: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[InverseAllLiquidationUpdates, Any, Any]:
    """Subscribe to every liquidation order matched for one symbol.

    Args:
      symbol: Symbol name, for example `BTCUSD`.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/websocket/public/all-liquidation)
    """
    return self.subscribe(
      f'allLiquidation.{symbol}',
      validate=validate,
      response_type=InverseAllLiquidationUpdates,
    )
