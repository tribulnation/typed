# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.ws import StreamsEndpoint


class RfqTransactionLeg(TypedDict):
  """One executed leg."""

  category: Literal['spot', 'linear', 'option']
  """Product type."""
  orderId: str
  """Bybit order ID."""
  symbol: str
  """Instrument name."""
  side: Literal['Buy', 'Sell']
  """Order side."""
  price: str
  """Execution price."""
  qty: str
  """Trade quantity."""
  markPrice: str
  """Mark/index price at execution time."""
  execFee: str
  """Fee paid, in the base currency."""
  execId: str
  """Unique execution ID."""
  resultCode: int
  """Execution result code; `0` means success."""
  resultMessage: str
  """Execution error message; empty on success."""
  rejectParty: Literal['Taker', 'Maker', 'bybit']
  """Which side caused a rejection; empty when filled."""


class RfqTransactionUpdate(TypedDict):
  """One RFQ transaction, as of this update."""

  rfqId: str
  """Inquiry ID."""
  rfqLinkId: str
  """Custom inquiry ID."""
  quoteId: str
  """Quote ID."""
  quoteLinkId: str
  """Custom quote ID."""
  quoteSide: Literal['Buy', 'Sell']
  """Direction of the executed quote."""
  strategyType: str
  """Inquiry strategy label."""
  status: Literal['Filled', 'Failed']
  """Transaction status."""
  rfqDeskCode: str
  """Inquiring party's identifier."""
  quoteDeskCode: str
  """Quoting party's identifier."""
  createdAt: TimestampMillis
  """Trade creation timestamp."""
  updatedAt: TimestampMillis
  """Trade update timestamp."""
  legs: list[RfqTransactionLeg]
  """Executed legs."""


Payload = list[RfqTransactionUpdate]


class Transaction(StreamsEndpoint):
  """Stream this account's own executed (or failed) RFQ transactions."""

  def transaction(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[Payload, Any, Any]:
    """Stream this account's own executed (or failed) RFQ transactions.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/rfq/websocket/private/transaction)
    """
    return self.subscribe(
      'rfq.open.trades',
      None,
      validate=validate,
      response_type=Payload,
    )
