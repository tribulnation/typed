# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class CoinChainInfo(TypedDict):
  """Deposit/withdrawal metadata for one coin on one chain."""

  chain: str
  """Chain identifier."""
  chainType: str
  """Human-readable chain name."""
  confirmation: str
  """Confirmations required before a deposit is credited."""
  withdrawFee: Literal[''] | Decimal
  """Fixed withdrawal fee; empty when withdrawals are unsupported."""
  depositMin: str
  """Minimum deposit amount."""
  withdrawMin: str
  """Minimum withdrawal amount."""
  minAccuracy: str
  """Decimal precision for amounts on this chain."""
  chainDeposit: Literal['0', '1']
  """Deposit status."""
  chainWithdraw: Literal['0', '1']
  """Withdrawal status."""
  withdrawPercentageFee: str
  """Percentage-based withdrawal fee, for example `"0.022"` for 2.2%."""
  contractAddress: str
  """Smart contract address; empty when this coin/chain has none."""
  safeConfirmNumber: str
  """Confirmations required before funds unlock, beyond `confirmation`."""
  withdrawMax: str
  """Maximum withdrawal per transaction; `-1` means no limit."""


class Request(TypedDict):
  coin: NotRequired[str]
  """Coin symbol."""


class CoinInfoRow(TypedDict):
  """Metadata and supported chains for one coin."""

  name: str
  """Coin name."""
  coin: str
  """Coin symbol."""
  remainAmount: str
  """Deprecated by the venue; no longer meaningful."""
  chains: list[CoinChainInfo]
  """Chains this coin can be deposited/withdrawn on."""


class CoinInfoResult(TypedDict):
  """Coin metadata rows."""

  rows: list[CoinInfoRow]
  """One row per coin."""


class CoinInfo(RpcEndpoint):
  """Query coin metadata: supported chains, deposit/withdrawal status and limits, per coin or for every coin."""

  async def coin_info(
    self,
    coin: str | None = None,
    *,
    validate: bool | None = None,
  ) -> CoinInfoResult:
    """Query coin metadata: supported chains, deposit/withdrawal status and limits, per coin or for every coin.

    Args:
      coin: Coin symbol.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/asset/coin-info)
    """
    request: Request = Request()
    if coin is not None:
      request['coin'] = coin
    return await self.request(
      request,
      method='GET',
      path='/v5/asset/coin/query-info',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CoinInfoResult,
    )
