# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  """Destination wallet type."""

  accountType: Literal['UNIFIED', 'FUND', 'EARN']
  """Destination wallet type. `EARN` (flexible savings) reverts to the funding wallet if unsupported for the deposited coin."""


class SetDepositAcctResult(TypedDict):
  """Result of setting the deposit account."""

  status: Literal[0, 1]
  """Outcome."""


class SetDepositAcct(RpcEndpoint):
  """Set which wallet type future deposits are automatically routed into. Main account UIDs only."""

  async def set_deposit_acct(
    self,
    account_type: Literal['UNIFIED', 'FUND', 'EARN'],
    *,
    validate: bool | None = None,
  ) -> SetDepositAcctResult:
    """Set which wallet type future deposits are automatically routed into. Main account UIDs only.

    Args:
      account_type: Destination wallet type. `EARN` (flexible savings) reverts to the funding wallet if unsupported for the deposited coin.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/asset/deposit/set-deposit-acct)
    """
    return await self.request(
      Request(accountType=account_type),
      method='POST',
      path='/v5/asset/deposit/deposit-to-account',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SetDepositAcctResult,
    )
