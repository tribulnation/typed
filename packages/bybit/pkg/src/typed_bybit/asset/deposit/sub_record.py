# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  id: NotRequired[str]
  """Internal deposit ID."""
  txID: NotRequired[str]
  """Transaction ID."""
  subMemberId: str
  """Sub-account UID."""
  coin: NotRequired[str]
  """Coin name."""
  startTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  limit: NotRequired[int]
  """Page size."""
  cursor: NotRequired[str]
  """Pagination cursor."""


class SubDepositRecord(TypedDict):
  """One on-chain deposit into a sub-account."""

  id: str
  """Internal deposit ID, unique."""
  coin: str
  """Coin name."""
  chain: str
  """Chain the deposit arrived on."""
  amount: str
  """Deposit amount."""
  txID: str
  """On-chain transaction hash. Not queryable via `txID` for deposits before 2024-01-01."""
  status: Literal[0, 1, 2, 3, 4, 7, 10011, 10012, 70011, 70012, 70013]
  """Deposit status."""
  toAddress: str
  """Destination wallet address."""
  tag: str
  """Address tag/memo, when the chain uses one."""
  depositFee: str
  """Fee charged on this deposit."""
  successAt: TimestampMillis
  """Completion time, as a millisecond timestamp."""
  confirmations: str
  """Block confirmations observed."""
  txIndex: str
  """Transaction's sequence position within its block."""
  blockHash: str
  """Block hash the transaction was included in."""
  batchReleaseLimit: str
  """Daily deposit limit for this coin/chain; `-1` means unlimited."""
  depositType: Literal['0', '10', '20', '50']
  """Deposit classification."""
  fromAddress: str
  """Source wallet address; empty when the origin isn't unique."""
  taxDepositRecordsId: str
  """Tax reporting record ID (EU users)."""
  taxStatus: Literal[0, 1, 2]
  """Tax reporting status (EU users)."""
  travelRuleStatus: Literal[0, 1, 2, 3]
  """Travel Rule compliance review status."""


class SubDepositRecordResult(TypedDict):
  """Page of a sub-account's on-chain deposit records."""

  rows: list[SubDepositRecord]
  """Deposit records."""
  nextPageCursor: str
  """Opaque cursor for the next page. Pass it back as `cursor`; an empty string means there are no further pages."""


class SubRecord(RpcEndpoint):
  """Query a sub-account's on-chain deposit records, using the master account's API key."""

  def sub_record_paged(
    self,
    *,
    id: str | None = None,
    tx_id: str | None = None,
    sub_member_id: str,
    coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[SubDepositRecord, str]:
    """Query a sub-account's on-chain deposit records, using the master account's API key.

    Paged variant of `sub_record`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      id: Internal deposit ID.
      tx_id: Transaction ID.
      sub_member_id: Sub-account UID.
      coin: Coin name.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/asset/deposit/sub-deposit-record)
    """

    async def next(cursor: str) -> tuple[list[SubDepositRecord], str | None]:
      response = await self.sub_record(
        id=id,
        tx_id=tx_id,
        sub_member_id=sub_member_id,
        coin=coin,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('rows')
      state = response.get('nextPageCursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def sub_record(
    self,
    *,
    id: str | None = None,
    tx_id: str | None = None,
    sub_member_id: str,
    coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> SubDepositRecordResult:
    """Query a sub-account's on-chain deposit records, using the master account's API key.

    Args:
      id: Internal deposit ID.
      tx_id: Transaction ID.
      sub_member_id: Sub-account UID.
      coin: Coin name.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      limit: Page size.
      cursor: Pagination cursor.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/asset/deposit/sub-deposit-record)
    """
    request: Request = Request(subMemberId=sub_member_id)
    if id is not None:
      request['id'] = id
    if tx_id is not None:
      request['txID'] = tx_id
    if coin is not None:
      request['coin'] = coin
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/asset/deposit/query-sub-member-record',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SubDepositRecordResult,
    )
