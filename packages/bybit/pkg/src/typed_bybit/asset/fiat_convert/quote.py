# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class FiatConvertQuoteResult(TypedDict):
  """A fiat conversion quote."""

  quoteTxId: str
  """System-generated quote transaction ID; pass to `asset.fiat_convert.confirm` to execute."""
  exchangeRate: str
  """Conversion rate quoted."""
  fromCoin: str
  """Coin being sold."""
  fromCoinType: Literal['fiat', 'crypto']
  """Whether `fromCoin` is fiat or crypto."""
  toCoin: str
  """Coin being purchased."""
  toCoinType: Literal['fiat', 'crypto']
  """Whether `toCoin` is fiat or crypto."""
  fromAmount: str
  """Quantity being sold."""
  toAmount: str
  """Quantity to receive at this rate."""
  expiredTime: TimestampMillis
  """Absolute expiry time of this quote, as a millisecond timestamp."""


class Request(TypedDict):
  """Fiat conversion quote parameters."""

  fromCoin: str
  """Coin being sold."""
  fromCoinType: Literal['fiat', 'crypto']
  """Whether `fromCoin` is a fiat currency or a crypto coin."""
  toCoin: str
  """Coin being purchased."""
  toCoinType: Literal['fiat', 'crypto']
  """Whether `toCoin` is a fiat currency or a crypto coin."""
  requestAmount: str
  """Amount of `fromCoin` to sell."""
  requestCoinType: NotRequired[Literal['fiat', 'crypto']]
  """Which side `requestAmount` is denominated in. Defaults to `fiat`."""


class Quote(RpcEndpoint):
  """Request a quote to convert between a fiat currency and a crypto coin. Does not reserve or lock funds."""

  async def quote(
    self,
    *,
    from_coin: str,
    from_coin_type: Literal['fiat', 'crypto'],
    to_coin: str,
    to_coin_type: Literal['fiat', 'crypto'],
    request_amount: str,
    request_coin_type: Literal['fiat', 'crypto'] | None = None,
    validate: bool | None = None,
  ) -> FiatConvertQuoteResult:
    """Request a quote to convert between a fiat currency and a crypto coin. Does not reserve or lock funds.

    Args:
      from_coin: Coin being sold.
      from_coin_type: Whether `fromCoin` is a fiat currency or a crypto coin.
      to_coin: Coin being purchased.
      to_coin_type: Whether `toCoin` is a fiat currency or a crypto coin.
      request_amount: Amount of `fromCoin` to sell.
      request_coin_type: Which side `requestAmount` is denominated in. Defaults to `fiat`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/asset/fiat-convert/quote-apply)
    """
    request: Request = Request(
      fromCoin=from_coin,
      fromCoinType=from_coin_type,
      toCoin=to_coin,
      toCoinType=to_coin_type,
      requestAmount=request_amount,
    )
    if request_coin_type is not None:
      request['requestCoinType'] = request_coin_type
    return await self.request(
      request,
      method='POST',
      path='/v5/fiat/quote-apply',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FiatConvertQuoteResult,
    )
