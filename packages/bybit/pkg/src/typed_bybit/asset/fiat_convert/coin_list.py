# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class FiatConvertCoinCryptosItem(TypedDict):
  """One supported currency."""

  coin: str
  """Currency code, for example `EUR` or `USDT`."""
  fullName: str
  """Currency's full name."""
  icon: str
  """URL to the light-mode icon."""
  iconNight: str
  """URL to the dark-mode icon."""
  precision: int
  """Decimal precision."""
  disable: bool
  """Whether this currency is currently disabled."""
  singleFromMinLimit: str
  """Minimum transaction amount."""
  singleFromMaxLimit: str
  """Maximum transaction amount."""


class FiatConvertCoinFiatsItem(TypedDict):
  """One supported currency."""

  coin: str
  """Currency code, for example `EUR` or `USDT`."""
  fullName: str
  """Currency's full name."""
  icon: str
  """URL to the light-mode icon."""
  iconNight: str
  """URL to the dark-mode icon."""
  precision: int
  """Decimal precision."""
  disable: bool
  """Whether this currency is currently disabled."""
  singleFromMinLimit: str
  """Minimum transaction amount."""
  singleFromMaxLimit: str
  """Maximum transaction amount."""


class Request(TypedDict):
  side: NotRequired[Literal['0', '1']]
  """Transaction direction."""


class FiatConvertCoinListResult(TypedDict):
  """Supported fiat currencies and crypto coins."""

  fiats: list[FiatConvertCoinFiatsItem]
  """Supported fiat currencies."""
  cryptos: list[FiatConvertCoinCryptosItem]
  """Supported crypto coins."""


class CoinList(RpcEndpoint):
  """List the fiat currencies and crypto coins supported by fiat-convert, for a given transaction direction."""

  async def coin_list(
    self,
    side: Literal['0', '1'] | None = None,
    *,
    validate: bool | None = None,
  ) -> FiatConvertCoinListResult:
    """List the fiat currencies and crypto coins supported by fiat-convert, for a given transaction direction.

    Args:
      side: Transaction direction.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/asset/fiat-convert/query-coin-list)
    """
    request: Request = Request()
    if side is not None:
      request['side'] = side
    return await self.request(
      request,
      method='GET',
      path='/v5/fiat/query-coin-list',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FiatConvertCoinListResult,
    )
