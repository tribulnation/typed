# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class FiatConvertNotifyAck(TypedDict):
  """Acknowledgement expected from the webhook receiver; shape is the receiver's own choice, not documented by the venue."""


class Request(TypedDict):
  """Trade status change notification."""

  tradeNo: str
  """Trade order number."""
  status: Literal['processing', 'success', 'failed']
  """Trade status."""
  quoteTxId: str
  """Quote transaction ID this trade was confirmed from."""
  exchangeRate: str
  """Conversion rate applied."""
  fromCoin: str
  """Coin sold."""
  toCoin: str
  """Coin purchased."""
  fromCoinType: Literal['fiat', 'crypto']
  """Whether `fromCoin` is fiat or crypto."""
  toCoinType: Literal['fiat', 'crypto']
  """Whether `toCoin` is fiat or crypto."""
  fromAmount: str
  """Quantity sold."""
  toAmount: str
  """Quantity received."""
  createdAt: TimestampMillis
  """Trade creation time, as a millisecond timestamp."""


class Notify(RpcEndpoint):
  """Payload shape of the outbound webhook Bybit sends to a caller's own `webhookUrl` when a fiat conversion trade's status changes. Documented for reference; this client neither sends nor receives it."""

  async def notify(
    self,
    *,
    trade_no: str,
    status: Literal['processing', 'success', 'failed'],
    quote_tx_id: str,
    exchange_rate: str,
    from_coin: str,
    to_coin: str,
    from_coin_type: Literal['fiat', 'crypto'],
    to_coin_type: Literal['fiat', 'crypto'],
    from_amount: str,
    to_amount: str,
    created_at: TimestampMillis,
    validate: bool | None = None,
  ) -> FiatConvertNotifyAck:
    """Payload shape of the outbound webhook Bybit sends to a caller's own `webhookUrl` when a fiat conversion trade's status changes. Documented for reference; this client neither sends nor receives it.

    Args:
      trade_no: Trade order number.
      status: Trade status.
      quote_tx_id: Quote transaction ID this trade was confirmed from.
      exchange_rate: Conversion rate applied.
      from_coin: Coin sold.
      to_coin: Coin purchased.
      from_coin_type: Whether `fromCoin` is fiat or crypto.
      to_coin_type: Whether `toCoin` is fiat or crypto.
      from_amount: Quantity sold.
      to_amount: Quantity received.
      created_at: Trade creation time, as a millisecond timestamp.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/asset/fiat-convert/trade-notify)
    """
    return await self.request(
      Request(
        tradeNo=trade_no,
        status=status,
        quoteTxId=quote_tx_id,
        exchangeRate=exchange_rate,
        fromCoin=from_coin,
        toCoin=to_coin,
        fromCoinType=from_coin_type,
        toCoinType=to_coin_type,
        fromAmount=from_amount,
        toAmount=to_amount,
        createdAt=created_at,
      ),
      method='POST',
      path='/v5/fiat/trade-notify',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=FiatConvertNotifyAck,
    )
