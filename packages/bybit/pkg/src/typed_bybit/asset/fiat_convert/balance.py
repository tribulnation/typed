# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class FiatConvertBalance(TypedDict):
  """Balance of one fiat currency."""

  currency: str
  """Fiat currency code (ISO 4217)."""
  totalBalance: str
  """Total balance."""
  balance: str
  """Available balance."""
  frozenBalance: str
  """Balance frozen (e.g. by a pending conversion)."""


class Request(TypedDict):
  currency: NotRequired[str]
  """Fiat currency code."""


FiatConvertBalanceResult = list[FiatConvertBalance]


class Balance(RpcEndpoint):
  """Query fiat balances held for fiat-convert, per currency or for every currency."""

  async def balance(
    self,
    currency: str | None = None,
    *,
    validate: bool | None = None,
  ) -> FiatConvertBalanceResult:
    """Query fiat balances held for fiat-convert, per currency or for every currency.

    Args:
      currency: Fiat currency code.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/asset/fiat-convert/balance-query)
    """
    request: Request = Request()
    if currency is not None:
      request['currency'] = currency
    return await self.request(
      request,
      method='GET',
      path='/v5/fiat/balance-query',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FiatConvertBalanceResult,
    )
