# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class FiatConvertTradeResult(TypedDict):
  """Status of one fiat conversion trade."""

  tradeNo: str
  """Trade order number."""
  status: Literal['processing', 'success', 'failed']
  """Trade status."""
  quoteTxId: str
  """Quote transaction ID this trade was confirmed from."""
  exchangeRate: str
  """Conversion rate applied."""
  fromCoin: str
  """Coin sold."""
  fromCoinType: Literal['fiat', 'crypto']
  """Whether `fromCoin` is fiat or crypto."""
  toCoin: str
  """Coin purchased."""
  toCoinType: Literal['fiat', 'crypto']
  """Whether `toCoin` is fiat or crypto."""
  fromAmount: str
  """Quantity sold."""
  toAmount: str
  """Quantity received."""
  createdAt: TimestampMillis
  """Trade creation time, as a millisecond timestamp."""
  subUserId: str
  """Sub-user ID this trade was made under."""


class Request(TypedDict):
  tradeNo: NotRequired[str]
  """Trade order number."""
  merchantRequestId: NotRequired[str]
  """Caller-supplied tracking ID."""


class Trade(RpcEndpoint):
  """Query the status of a fiat conversion trade, by trade number or caller-supplied tracking ID."""

  async def trade(
    self,
    *,
    trade_no: str | None = None,
    merchant_request_id: str | None = None,
    validate: bool | None = None,
  ) -> FiatConvertTradeResult:
    """Query the status of a fiat conversion trade, by trade number or caller-supplied tracking ID.

    Args:
      trade_no: Trade order number.
      merchant_request_id: Caller-supplied tracking ID.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/asset/fiat-convert/query-trade)
    """
    request: Request = Request()
    if trade_no is not None:
      request['tradeNo'] = trade_no
    if merchant_request_id is not None:
      request['merchantRequestId'] = merchant_request_id
    return await self.request(
      request,
      method='GET',
      path='/v5/fiat/trade-query',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FiatConvertTradeResult,
    )
