# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class FiatConvertConfirmResult(TypedDict):
  """Identifiers of the created fiat conversion trade."""

  tradeNo: str
  """Trade order number."""
  merchantRequestId: str
  """Caller-supplied tracking ID, echoed back."""


class Request(TypedDict):
  """Fiat conversion execution parameters."""

  quoteTxId: str
  """Quote transaction ID, from `asset.fiat_convert.quote`."""
  subUserId: str
  """This user's sub-user ID on the fiat-convert side."""
  webhookUrl: NotRequired[str]
  """URL to receive order-status notifications, max 256 characters. See `asset.fiat_convert.notify`."""
  MerchantRequestId: NotRequired[str]
  """Caller-supplied tracking ID, max 36 characters."""


class Confirm(RpcEndpoint):
  """Execute a previously requested fiat conversion quote. Asynchronous -- poll the trade query/history endpoints for the final status."""

  async def confirm(
    self,
    *,
    quote_tx_id: str,
    sub_user_id: str,
    webhook_url: str | None = None,
    merchant_request_id: str | None = None,
    validate: bool | None = None,
  ) -> FiatConvertConfirmResult:
    """Execute a previously requested fiat conversion quote. Asynchronous -- poll the trade query/history endpoints for the final status.

    Args:
      quote_tx_id: Quote transaction ID, from `asset.fiat_convert.quote`.
      sub_user_id: This user's sub-user ID on the fiat-convert side.
      webhook_url: URL to receive order-status notifications, max 256 characters. See `asset.fiat_convert.notify`.
      merchant_request_id: Caller-supplied tracking ID, max 36 characters.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/asset/fiat-convert/confirm-quote)
    """
    request: Request = Request(quoteTxId=quote_tx_id, subUserId=sub_user_id)
    if webhook_url is not None:
      request['webhookUrl'] = webhook_url
    if merchant_request_id is not None:
      request['MerchantRequestId'] = merchant_request_id
    return await self.request(
      request,
      method='POST',
      path='/v5/fiat/trade-execute',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FiatConvertConfirmResult,
    )
