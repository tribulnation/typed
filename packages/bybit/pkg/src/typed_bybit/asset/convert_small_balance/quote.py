# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, Literal, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class ConvertSmallBalanceQuoteResult(TypedDict):
  """A small-balance convert quote."""

  quoteId: str
  """System-generated quote ID; pass to `asset.convert_small_balance.confirm` to execute."""
  quoteCreateTime: TimestampMillis
  """Quote creation time, as a millisecond timestamp."""
  quoteExpireTime: TimestampMillis
  """Quote expiry time, as a millisecond timestamp; valid for 30 seconds from creation."""
  exchangeCoins: list[dict[str, Any]]
  """Per-coin quote detail, one entry per coin in `fromCoinList`."""
  totalFeeInfo: dict[str, Any]
  """Aggregated exchange fee across every coin in the sweep."""
  totalTaxFeeInfo: dict[str, Any]
  """Aggregated tax fee across every coin in the sweep."""


class Request(TypedDict):
  """Small-balance convert quote parameters."""

  accountType: Literal['eb_convert_uta']
  """Wallet type to convert within. Only the Unified wallet is supported."""
  fromCoinList: list[str]
  """Coins to sweep, max 20."""
  toCoin: Literal['MNT', 'USDT', 'USDC']
  """Destination coin to sweep into."""


class Quote(RpcEndpoint):
  """Request a quote to sweep one or more small crypto balances into a single destination coin, within the Unified wallet. Does not reserve or lock funds; the quote stays valid for 30 seconds."""

  async def quote(
    self,
    account_type: Literal['eb_convert_uta'] = 'eb_convert_uta',
    *,
    from_coin_list: list[str],
    to_coin: Literal['MNT', 'USDT', 'USDC'],
    validate: bool | None = None,
  ) -> ConvertSmallBalanceQuoteResult:
    """Request a quote to sweep one or more small crypto balances into a single destination coin, within the Unified wallet. Does not reserve or lock funds; the quote stays valid for 30 seconds.

    Args:
      account_type: Wallet type to convert within. Only the Unified wallet is supported.
      from_coin_list: Coins to sweep, max 20.
      to_coin: Destination coin to sweep into.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/asset/convert-small-balance/request-quote)
    """
    return await self.request(
      Request(accountType=account_type, fromCoinList=from_coin_list, toCoin=to_coin),
      method='POST',
      path='/v5/asset/covert/get-quote',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ConvertSmallBalanceQuoteResult,
    )
