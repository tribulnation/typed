# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, Literal, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class ConvertResultResult(TypedDict):
  """Status of one convert."""

  accountType: Literal[
    'eb_convert_funding',
    'eb_convert_uta',
    'eb_convert_spot',
    'eb_convert_contract',
    'eb_convert_inverse',
  ]
  """Wallet type this convert happened in."""
  exchangeTxId: str
  """Exchange transaction ID; mirrors the original quote transaction ID."""
  userId: str
  """User identifier."""
  fromCoin: str
  """Coin sold."""
  fromCoinType: Literal['crypto']
  """Asset class of `fromCoin`."""
  toCoin: str
  """Coin purchased."""
  toCoinType: Literal['crypto']
  """Asset class of `toCoin`."""
  fromAmount: str
  """Quantity sold."""
  toAmount: str
  """Quantity received."""
  exchangeStatus: Literal['init', 'processing', 'success', 'failure']
  """Exchange status."""
  extInfo: dict[str, Any]
  """Reserved, currently unused."""
  convertRate: str
  """Conversion rate applied."""
  createdAt: TimestampMillis
  """Quote creation time, as a millisecond timestamp."""


class Request(TypedDict):
  quoteTxId: str
  """Quote transaction ID."""
  accountType: Literal[
    'eb_convert_funding',
    'eb_convert_uta',
    'eb_convert_spot',
    'eb_convert_contract',
    'eb_convert_inverse',
  ]
  """Wallet type."""


class Status(RpcEndpoint):
  """Query the status of a crypto-to-crypto convert, by quote transaction ID."""

  async def status(
    self,
    quote_tx_id: str,
    *,
    account_type: Literal[
      'eb_convert_funding',
      'eb_convert_uta',
      'eb_convert_spot',
      'eb_convert_contract',
      'eb_convert_inverse',
    ],
    validate: bool | None = None,
  ) -> ConvertResultResult:
    """Query the status of a crypto-to-crypto convert, by quote transaction ID.

    Args:
      quote_tx_id: Quote transaction ID.
      account_type: Wallet type.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/asset/convert/get-convert-result)
    """
    return await self.request(
      Request(quoteTxId=quote_tx_id, accountType=account_type),
      method='GET',
      path='/v5/asset/exchange/convert-result-query',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ConvertResultResult,
    )
