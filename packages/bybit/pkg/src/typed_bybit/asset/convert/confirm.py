# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class ConvertConfirmResult(TypedDict):
  """Immediate execution status."""

  quoteTxId: str
  """The quote's transaction ID, echoed back."""
  exchangeStatus: Literal['init', 'processing', 'success', 'failure']
  """Immediate execution status."""


class Request(TypedDict):
  """Convert execution parameters."""

  quoteTxId: str
  """Quote transaction ID, from `asset.convert.quote`."""


class Confirm(RpcEndpoint):
  """Execute a previously requested convert quote before it expires. Asynchronous -- poll asset.convert.result/history for the final status."""

  async def confirm(
    self,
    quote_tx_id: str,
    *,
    validate: bool | None = None,
  ) -> ConvertConfirmResult:
    """Execute a previously requested convert quote before it expires. Asynchronous -- poll asset.convert.result/history for the final status.

    Args:
      quote_tx_id: Quote transaction ID, from `asset.convert.quote`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/asset/convert/confirm-quote)
    """
    return await self.request(
      Request(quoteTxId=quote_tx_id),
      method='POST',
      path='/v5/asset/exchange/convert-execute',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ConvertConfirmResult,
    )
