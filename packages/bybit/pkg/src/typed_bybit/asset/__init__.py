# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .balance import Balance
from .coin_info import CoinInfo
from .convert import Convert
from .convert_small_balance import ConvertSmallBalance
from .delivery_record import DeliveryRecordEndpoint
from .deposit import Deposit
from .exchange_record import ExchangeRecordEndpoint
from .fiat_convert import FiatConvert
from .fund_history import FundHistory
from .portfolio_margin import PortfolioMargin
from .settlement_record import SettlementRecordEndpoint
from .sub_uid_list import SubUidList
from .total_members_assets import TotalMembersAssets
from .transfer import Transfer
from .withdraw import Withdraw


class Asset(
  CoinInfo,
  DeliveryRecordEndpoint,
  ExchangeRecordEndpoint,
  FundHistory,
  PortfolioMargin,
  SettlementRecordEndpoint,
  SubUidList,
  TotalMembersAssets,
):
  """Asset — cross-account asset management: coin metadata, delivery and USDC session settlement records, funding (sub-)account transaction history, small-balance conversion (exchange) records, portfolio margin info, and member/sub-UID asset listings across a master-sub account structure. Broader than any one sub-grouping: also composes the balance, convert, convert-small-balance, deposit, fiat-convert, transfer, and withdraw endpoint groups underneath it.

  References:
    - [Upstream docs](https://bybit-exchange.github.io/docs/v5/asset/coin-info)
  """

  @cached_property
  def balance(self) -> Balance:
    """Balance — querying coin and account balances across account types: a single coin's balance in one account, all coins' balances across master and sub-accounts, delayed (unavailable/frozen) balance amounts, and a cross-category asset overview of a master or sub-account's total holdings.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/asset/balance/asset-overview)
    """
    return Balance(client=self.client)

  @cached_property
  def convert(self) -> Convert:
    """Convert — swap between supported coins across account types via a quote-then-confirm flow: list convertible coins, request a quote, confirm it to execute the swap, then poll the result or list past conversion history.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/asset/convert/guideline)
    """
    return Convert(client=self.client)

  @cached_property
  def convert_small_balance(self) -> ConvertSmallBalance:
    """Convert Small Balance — bulk-convert dust holdings (each worth under 10 USDT) out of a Unified wallet into a single target coin via a quote-then-confirm flow: list the eligible small-balance coins, request a quote, confirm it to execute the conversion, then list past conversion history.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/asset/convert-small-balance/request-quote)
    """
    return ConvertSmallBalance(client=self.client)

  @cached_property
  def deposit(self) -> Deposit:
    """Deposit — coin deposit management: configure the account a UID's on-chain deposits are credited to, fetch a master or sub-account's deposit address for a chain, query on-chain deposit records for a master or sub-account plus internal (off-chain, within-Bybit) deposit records, and submit originator information for deposits that need it for compliance.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/asset/deposit/deposit-record)
    """
    return Deposit(client=self.client)

  @cached_property
  def fiat_convert(self) -> FiatConvert:
    """Fiat Convert — converting between a fiat currency and a crypto coin via a quote-then-confirm flow: list convertible coins and get a reference price, check fiat balance, request a quote and confirm it to execute the trade, query one trade or the full trade history, and receive/acknowledge trade-status push notifications.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/asset/fiat-convert/quote-apply)
    """
    return FiatConvert(client=self.client)

  @cached_property
  def transfer(self) -> Transfer:
    """Transfer — moving funds between account types: internal transfers between product wallets within one UID and universal transfers between main-sub or sub-sub UIDs, each with its own create-and-list endpoint pair, plus checking which coins are transferable for a given account/wallet pairing.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/asset/transfer/unitransfer)
    """
    return Transfer(client=self.client)

  @cached_property
  def withdraw(self) -> Withdraw:
    """Withdraw — on-chain withdrawal management: create and cancel a withdrawal, list past withdrawal records, plus the compliance-facing whitelisted withdrawal address book and the list of supported VASPs (Virtual Asset Service Providers) for travel-rule-regulated withdrawals.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/asset/withdraw)
    """
    return Withdraw(client=self.client)
