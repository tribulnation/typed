# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class SubUidListResult(TypedDict):
  """Sub-account UIDs, and which have universal transfer enabled."""

  subMemberIds: list[str]
  """Every sub-UID under this master UID, up to 2000."""
  transferableSubMemberIds: list[str]
  """Sub-UIDs, among the above, that have universal transfer enabled."""


class SubUidList(RpcEndpoint):
  """List every sub-account UID under this master UID, and which of them have universal transfer enabled. Restricted to a master account's API key; returns at most 2000 sub-UIDs."""

  async def sub_uid_list(self, *, validate: bool | None = None) -> SubUidListResult:
    """List every sub-account UID under this master UID, and which of them have universal transfer enabled. Restricted to a master account's API key; returns at most 2000 sub-UIDs.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/asset/sub-uid-list)
    """
    return await self.request(
      None,
      method='GET',
      path='/v5/asset/transfer/query-sub-member-list',
      meta={'signed': True},
      validate=validate,
      response_type=SubUidListResult,
    )
