# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  withdrawID: NotRequired[str]
  """Withdrawal ID."""
  txID: NotRequired[str]
  """Transaction ID."""
  coin: NotRequired[str]
  """Coin name."""
  withdrawType: NotRequired[Literal[0, 1, 2]]
  """Withdrawal type filter."""
  startTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  limit: NotRequired[int]
  """Page size."""
  cursor: NotRequired[str]
  """Pagination cursor."""


class WithdrawRecord(TypedDict):
  """One withdrawal."""

  txID: str
  """On-chain transaction ID; empty when the withdrawal failed or was cancelled."""
  coin: str
  """Coin name."""
  chain: str
  """Chain withdrawn on."""
  amount: str
  """Withdrawal amount."""
  withdrawFee: str
  """Fee charged on this withdrawal."""
  status: Literal[
    'SecurityCheck',
    'Pending',
    'success',
    'CancelByUser',
    'Reject',
    'Fail',
    'BlockchainConfirmed',
    'MoreInformationRequired',
    'Unknown',
  ]
  """Withdrawal status."""
  toAddress: str
  """Destination address; a Bybit UID for an internal (UID) transfer."""
  tag: str
  """Address tag/memo, when used."""
  createTime: TimestampMillis
  """Creation time, as a millisecond timestamp."""
  updateTime: TimestampMillis
  """Last update time, as a millisecond timestamp."""
  withdrawId: str
  """Withdrawal ID, unique."""
  withdrawType: Literal[0, 1]
  """Withdrawal type."""
  tax: str
  """Tax amount; `"0"` when none applies."""
  taxRate: str
  """Tax rate; `"0"` when none applies."""
  taxType: str
  """Tax classification; empty when none applies."""


class WithdrawRecordResult(TypedDict):
  """Page of withdrawal records."""

  rows: list[WithdrawRecord]
  """Withdrawal records."""
  nextPageCursor: str
  """Opaque cursor for the next page. Pass it back as `cursor`; an empty string means there are no further pages."""


class Record(RpcEndpoint):
  """Query withdrawal records for the master account. Returns the last 30 days by default."""

  def record_paged(
    self,
    *,
    withdraw_id: str | None = None,
    tx_id: str | None = None,
    coin: str | None = None,
    withdraw_type: Literal[0, 1, 2] | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[WithdrawRecord, str]:
    """Query withdrawal records for the master account. Returns the last 30 days by default.

    Paged variant of `record`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      withdraw_id: Withdrawal ID.
      tx_id: Transaction ID.
      coin: Coin name.
      withdraw_type: Withdrawal type filter.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/asset/withdraw/withdraw-record)
    """

    async def next(cursor: str) -> tuple[list[WithdrawRecord], str | None]:
      response = await self.record(
        withdraw_id=withdraw_id,
        tx_id=tx_id,
        coin=coin,
        withdraw_type=withdraw_type,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('rows')
      state = response.get('nextPageCursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def record(
    self,
    *,
    withdraw_id: str | None = None,
    tx_id: str | None = None,
    coin: str | None = None,
    withdraw_type: Literal[0, 1, 2] | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> WithdrawRecordResult:
    """Query withdrawal records for the master account. Returns the last 30 days by default.

    Args:
      withdraw_id: Withdrawal ID.
      tx_id: Transaction ID.
      coin: Coin name.
      withdraw_type: Withdrawal type filter.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      limit: Page size.
      cursor: Pagination cursor.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/asset/withdraw/withdraw-record)
    """
    request: Request = Request()
    if withdraw_id is not None:
      request['withdrawID'] = withdraw_id
    if tx_id is not None:
      request['txID'] = tx_id
    if coin is not None:
      request['coin'] = coin
    if withdraw_type is not None:
      request['withdrawType'] = withdraw_type
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/asset/withdraw/query-record',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=WithdrawRecordResult,
    )
