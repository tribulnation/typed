# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  """Withdrawal identifier."""

  id: str
  """Withdrawal ID to cancel."""


class WithdrawCancelResult(TypedDict):
  """Cancellation outcome."""

  status: Literal[0, 1]
  """Outcome."""


class Cancel(RpcEndpoint):
  """Cancel a pending withdrawal request."""

  async def cancel(
    self, id: str, *, validate: bool | None = None
  ) -> WithdrawCancelResult:
    """Cancel a pending withdrawal request.

    Args:
      id: Withdrawal ID to cancel.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/asset/withdraw/cancel-withdraw)
    """
    return await self.request(
      Request(id=id),
      method='POST',
      path='/v5/asset/withdraw/cancel',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=WithdrawCancelResult,
    )
