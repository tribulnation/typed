# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampSeconds
from typed_bybit.core.http import RpcEndpoint


class FundHistoryRecord(TypedDict):
  """One funding-account ledger entry."""

  memberId: str
  """User (member) identifier."""
  currency: str
  """Coin name."""
  ioDirection: Literal['I', 'O']
  """Transaction direction."""
  txnAmt: str
  """Transaction amount."""
  afterAmt: str
  """Account balance following the transaction."""
  createTime: TimestampSeconds
  """Creation time, as a Unix-seconds timestamp (not milliseconds)."""
  showBusiType: str
  """Localized business-type key."""
  showBusiTypeEn: str
  """Business type, in English."""
  description: str
  """Localized transaction description key."""
  descriptionEn: str
  """Transaction description, in English."""


class Request(TypedDict):
  createTimeFrom: NotRequired[int]
  """Unix-seconds timestamp."""
  createTimeTo: NotRequired[int]
  """Unix-seconds timestamp."""
  limit: NotRequired[int]
  """Page size."""
  cursor: NotRequired[str]
  """Pagination cursor."""


class FundHistoryResult(TypedDict):
  """Page of funding-account ledger records."""

  nextPageCursor: NotRequired[str]
  """Opaque cursor for the next page. Pass it back as `cursor`; an empty string means there are no further pages."""
  list: list[FundHistoryRecord]
  """Ledger records."""


class FundHistory(RpcEndpoint):
  """Query the funding-account ledger: inbound and outbound transactions with balances before/after each one."""

  async def fund_history(
    self,
    *,
    create_time_from: int | None = None,
    create_time_to: int | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> FundHistoryResult:
    """Query the funding-account ledger: inbound and outbound transactions with balances before/after each one.

    Args:
      create_time_from: Unix-seconds timestamp.
      create_time_to: Unix-seconds timestamp.
      limit: Page size.
      cursor: Pagination cursor.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/asset/fund-history)
    """
    request: Request = Request()
    if create_time_from is not None:
      request['createTimeFrom'] = create_time_from
    if create_time_to is not None:
      request['createTimeTo'] = create_time_to
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/asset/fundinghistory',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=FundHistoryResult,
    )
