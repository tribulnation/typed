# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  fromAccountType: Literal['CONTRACT', 'SPOT', 'OPTION', 'UNIFIED', 'FUND']
  """Account type."""
  toAccountType: Literal['CONTRACT', 'SPOT', 'OPTION', 'UNIFIED', 'FUND']
  """Account type."""


class TransferableCoinResult(TypedDict):
  """Coins transferable between two account types."""

  list: list[str]
  """Transferable coin names."""


class TransferableCoin(RpcEndpoint):
  """List the coins transferable between two given account types."""

  async def transferable_coin(
    self,
    *,
    from_account_type: Literal['CONTRACT', 'SPOT', 'OPTION', 'UNIFIED', 'FUND'],
    to_account_type: Literal['CONTRACT', 'SPOT', 'OPTION', 'UNIFIED', 'FUND'],
    validate: bool | None = None,
  ) -> TransferableCoinResult:
    """List the coins transferable between two given account types.

    Args:
      from_account_type: Account type.
      to_account_type: Account type.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/asset/transfer/transferable-coin)
    """
    return await self.request(
      Request(fromAccountType=from_account_type, toAccountType=to_account_type),
      method='GET',
      path='/v5/asset/transfer/query-transfer-coin-list',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=TransferableCoinResult,
    )
