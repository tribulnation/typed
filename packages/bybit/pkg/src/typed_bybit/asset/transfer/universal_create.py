# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  """Universal transfer parameters."""

  transferId: str
  """Caller-generated UUID, used as an idempotency key for this transfer."""
  coin: str
  """Coin name, uppercase only."""
  amount: str
  """Amount to transfer."""
  fromMemberId: int
  """Sender's UID."""
  toMemberId: int
  """Recipient's UID."""
  fromAccountType: Literal['CONTRACT', 'SPOT', 'OPTION', 'UNIFIED', 'FUND']
  """Sender's account type."""
  toAccountType: Literal['CONTRACT', 'SPOT', 'OPTION', 'UNIFIED', 'FUND']
  """Recipient's account type."""


class UniversalTransferResult(TypedDict):
  """Universal transfer outcome."""

  transferId: str
  """The transfer's UUID, echoed back."""
  status: Literal['STATUS_UNKNOWN', 'SUCCESS', 'PENDING', 'FAILED']
  """Transfer outcome."""


class UniversalCreate(RpcEndpoint):
  """Transfer a coin between two UIDs and account types -- master to sub-account, sub-account to sub-account (with permission), or sub-account to master."""

  async def universal_create(
    self,
    *,
    transfer_id: str,
    coin: str,
    amount: str,
    from_member_id: int,
    to_member_id: int,
    from_account_type: Literal['CONTRACT', 'SPOT', 'OPTION', 'UNIFIED', 'FUND'],
    to_account_type: Literal['CONTRACT', 'SPOT', 'OPTION', 'UNIFIED', 'FUND'],
    validate: bool | None = None,
  ) -> UniversalTransferResult:
    """Transfer a coin between two UIDs and account types -- master to sub-account, sub-account to sub-account (with permission), or sub-account to master.

    Args:
      transfer_id: Caller-generated UUID, used as an idempotency key for this transfer.
      coin: Coin name, uppercase only.
      amount: Amount to transfer.
      from_member_id: Sender's UID.
      to_member_id: Recipient's UID.
      from_account_type: Sender's account type.
      to_account_type: Recipient's account type.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/asset/transfer/unitransfer)
    """
    return await self.request(
      Request(
        transferId=transfer_id,
        coin=coin,
        amount=amount,
        fromMemberId=from_member_id,
        toMemberId=to_member_id,
        fromAccountType=from_account_type,
        toAccountType=to_account_type,
      ),
      method='POST',
      path='/v5/asset/transfer/universal-transfer',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=UniversalTransferResult,
    )
