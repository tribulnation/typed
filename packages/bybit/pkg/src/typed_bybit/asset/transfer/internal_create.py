# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class InternalTransferResult(TypedDict):
  """Internal transfer outcome."""

  transferId: str
  """The transfer's UUID, echoed back."""
  status: Literal['STATUS_UNKNOWN', 'SUCCESS', 'PENDING', 'FAILED']
  """Transfer outcome."""


class Request(TypedDict):
  """Internal transfer parameters."""

  transferId: str
  """Caller-generated UUID, used as an idempotency key for this transfer."""
  coin: str
  """Coin name, uppercase only."""
  amount: str
  """Amount to transfer."""
  fromAccountType: Literal['CONTRACT', 'SPOT', 'OPTION', 'UNIFIED', 'FUND']
  """Source account type."""
  toAccountType: Literal['CONTRACT', 'SPOT', 'OPTION', 'UNIFIED', 'FUND']
  """Destination account type."""


class InternalCreate(RpcEndpoint):
  """Transfer a coin between this account's own account types (wallets) under the same UID."""

  async def internal_create(
    self,
    *,
    transfer_id: str,
    coin: str,
    amount: str,
    from_account_type: Literal['CONTRACT', 'SPOT', 'OPTION', 'UNIFIED', 'FUND'],
    to_account_type: Literal['CONTRACT', 'SPOT', 'OPTION', 'UNIFIED', 'FUND'],
    validate: bool | None = None,
  ) -> InternalTransferResult:
    """Transfer a coin between this account's own account types (wallets) under the same UID.

    Args:
      transfer_id: Caller-generated UUID, used as an idempotency key for this transfer.
      coin: Coin name, uppercase only.
      amount: Amount to transfer.
      from_account_type: Source account type.
      to_account_type: Destination account type.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/asset/transfer/create-inter-transfer)
    """
    return await self.request(
      Request(
        transferId=transfer_id,
        coin=coin,
        amount=amount,
        fromAccountType=from_account_type,
        toAccountType=to_account_type,
      ),
      method='POST',
      path='/v5/asset/transfer/inter-transfer',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=InternalTransferResult,
    )
