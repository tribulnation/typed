# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.ws import StreamsEndpoint
from typed_bybit.schemas import CreateType, ExtraFee


class ExecutionUpdate(TypedDict):
  """One execution (fill)."""

  category: Literal['spot', 'linear', 'inverse', 'option']
  """Product type."""
  symbol: str
  """Symbol name."""
  isLeverage: Literal['0', '1']
  """Whether margin was borrowed for this execution. Unified spot only."""
  orderId: str
  """Order ID."""
  orderLinkId: str
  """User-customised order ID."""
  side: Literal['Buy', 'Sell']
  """Side."""
  orderPrice: Decimal
  """Order price."""
  orderQty: Decimal
  """Order quantity."""
  leavesQty: Decimal
  """Remaining quantity not executed."""
  createType: NotRequired[CreateType]
  orderType: Literal['Market', 'Limit']
  """Order type."""
  stopOrderType: Literal[
    'TakeProfit',
    'StopLoss',
    'TrailingStop',
    'Stop',
    'PartialTakeProfit',
    'PartialStopLoss',
    'tpslOrder',
    'OcoOrder',
    'MmRateClose',
    'BidirectionalTpslOrder',
    'UNKNOWN',
    '',
  ]
  """Stop order sub-type. `UNKNOWN` or `""` when the order is not a stop order."""
  execFee: Decimal
  """Executed trading fee."""
  execId: str
  """Execution ID."""
  execPrice: Decimal
  """Execution price."""
  execQty: Decimal
  """Execution quantity."""
  execPnl: NotRequired[Literal[''] | Decimal]
  """Realised profit and loss from an execution that closes position, consistent with `cashFlow` in the transaction log. Documented as a string; the venue's own example carries `"0.05"`, and the empty string is admitted for an execution that closes nothing, by the convention every other conditionally applicable numeric on this row follows."""
  execType: Literal[
    'Trade',
    'AdlTrade',
    'Funding',
    'BustTrade',
    'Delivery',
    'Settle',
    'BlockTrade',
    'MovePosition',
    'FutureSpread',
    'ForwardSplitSettle',
    'ReverseSplitSettle',
    'Dividend',
    'UNKNOWN',
  ]
  """Execution type."""
  execValue: Decimal
  """Executed order value."""
  execTime: TimestampMillis
  """Execution time, as a millisecond timestamp."""
  isMaker: bool
  """Whether the execution was a maker fill."""
  feeRate: Decimal
  """Trading fee rate."""
  tradeIv: NotRequired[Literal[''] | Decimal]
  """Implied volatility of the trade. Option only; empty string confirmed live on a spot execution of the REST `trade.trade_history` counterpart, whose declaration this mirrors."""
  markIv: NotRequired[Literal[''] | Decimal]
  """Implied volatility of the mark price. Option only; empty string confirmed live on a spot execution of the REST `trade.trade_history` counterpart, whose declaration this mirrors."""
  markPrice: NotRequired[Literal[''] | Decimal]
  """Mark price of the symbol at execution; empty string confirmed live on a spot execution of the REST `trade.trade_history` counterpart, whose declaration this mirrors."""
  indexPrice: NotRequired[Literal[''] | Decimal]
  """Index price of the symbol at execution. Option only; empty string confirmed live on a spot execution of the REST `trade.trade_history` counterpart, whose declaration this mirrors."""
  underlyingPrice: NotRequired[Literal[''] | Decimal]
  """Underlying price of the symbol at execution. Option only; empty string confirmed live on a spot execution of the REST `trade.trade_history` counterpart, whose declaration this mirrors."""
  blockTradeId: str
  """Paradigm block trade ID."""
  closedSize: Literal[''] | Decimal
  """Closed position size; empty string confirmed live on a spot execution of the REST `trade.trade_history` counterpart, whose declaration this mirrors."""
  extraFees: NotRequired[Literal[''] | list[ExtraFee]]
  """Extra fee breakdown. Returned only for Indian-KYC users, spot orders placed on the Indonesian site, or spot fiat-currency orders on the EU site; empty string otherwise."""
  seq: int
  """Cross-sequence id, shared across fills and position updates concluded together. Combine with `symbol` for uniqueness."""
  feeCurrency: str
  """Trading fee currency."""


Payload = list[ExecutionUpdate]


class Execution(StreamsEndpoint):
  """Stream trade executions for the authenticated account. A single order may generate multiple executions in one push."""

  def execution(
    self, *, validate: bool | None = None
  ) -> StreamManager[Payload, Any, Any]:
    """Stream trade executions for the authenticated account. A single order may generate multiple executions in one push.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/websocket/private/execution)
    """
    return self.subscribe(
      'execution',
      None,
      validate=validate,
      response_type=Payload,
    )
