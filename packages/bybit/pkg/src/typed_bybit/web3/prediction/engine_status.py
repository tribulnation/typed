# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class PredictionEngineStatusResult(TypedDict):
  """Matching-engine availability."""

  available: bool
  """Whether the matching engine is currently available for trading."""


class EngineStatus(RpcEndpoint):
  """Query the availability of the prediction-market matching engine. Buy and sell orders cannot be submitted while the engine is unavailable; upstream instructs callers to check this before submitting trading orders."""

  async def engine_status(
    self,
    *,
    validate: bool | None = None,
  ) -> PredictionEngineStatusResult:
    """Query the availability of the prediction-market matching engine. Buy and sell orders cannot be submitted while the engine is unavailable; upstream instructs callers to check this before submitting trading orders.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/alpha/prediction/engine-status)
    """
    return await self.request(
      None,
      method='GET',
      path='/v5/alpha/prediction/engine-status',
      meta={'signed': True},
      validate=validate,
      response_type=PredictionEngineStatusResult,
    )
