# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class PredictionClosedPosition(TypedDict):
  """One closed prediction-market position."""

  positionId: str
  """Position identifier."""
  tokenId: str
  """Outcome token ID."""
  eventId: str
  """Event ID."""
  outcomeName: str
  """Outcome label, for example `YES` or `NO`."""
  shares: str
  """Total shares that were held."""
  cost: str
  """Total USDC cost of this position."""
  avgPrice: str
  """Average cost price per share."""
  exitPrice: str
  """Price at which the position was closed."""
  realizedPnl: str
  """Realized profit or loss, in USDC. Positive means profit."""
  realizedPnlRate: str
  """Realized profit/loss as a decimal ratio."""
  result: Literal[1, 2, 3]
  """Position settlement outcome."""
  closedAt: TimestampMillis
  """Position close time."""


class Request(TypedDict):
  """Filters and pagination for the closed-position query."""

  tokenId: NotRequired[str]
  """Filter by outcome token ID."""
  eventId: NotRequired[str]
  """Filter by event ID."""
  result: NotRequired[Literal[1, 2, 3]]
  """Filter by position settlement outcome."""
  days: NotRequired[int]
  """Look back N days. Max `90`."""
  limit: NotRequired[int]
  """Number of records per page."""
  pageIndex: NotRequired[int]
  """Page number, starting from `1`."""


class PredictionPositionHistoryResult(TypedDict):
  """Page of closed prediction-market positions."""

  positions: list[PredictionClosedPosition]
  """Closed position records."""
  total: int
  """Total matching records."""


class PositionHistory(RpcEndpoint):
  """Query the authenticated user's closed prediction-market positions, including the settlement result for resolved events."""

  def position_history_paged(
    self,
    *,
    token_id: str | None = None,
    event_id: str | None = None,
    result: Literal[1, 2, 3] | None = None,
    days: int | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[PredictionClosedPosition, int]:
    """Query the authenticated user's closed prediction-market positions, including the settlement result for resolved events.

    Paged variant of `position_history`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      token_id: Filter by outcome token ID.
      event_id: Filter by event ID.
      result: Filter by position settlement outcome.
      days: Look back N days. Max `90`.
      limit: Number of records per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/alpha/prediction/position-history)
    """

    async def next(
      page_index: int,
    ) -> tuple[list[PredictionClosedPosition], int | None]:
      response = await self.position_history(
        token_id=token_id,
        event_id=event_id,
        result=result,
        days=days,
        limit=limit,
        page_index=page_index,
        validate=validate,
      )
      rows = response.get('positions')
      rows = rows if rows is not None else []
      total = response.get('total')
      total = int(total) if total is not None else None
      if total is None or limit is None or (page_index - 1 + 1) * limit >= total:
        return rows, None
      return rows, page_index + 1

    return PaginatedResponse(1, next)

  async def position_history(
    self,
    *,
    token_id: str | None = None,
    event_id: str | None = None,
    result: Literal[1, 2, 3] | None = None,
    days: int | None = None,
    limit: int | None = None,
    page_index: int | None = None,
    validate: bool | None = None,
  ) -> PredictionPositionHistoryResult:
    """Query the authenticated user's closed prediction-market positions, including the settlement result for resolved events.

    Args:
      token_id: Filter by outcome token ID.
      event_id: Filter by event ID.
      result: Filter by position settlement outcome.
      days: Look back N days. Max `90`.
      limit: Number of records per page.
      page_index: Page number, starting from `1`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/alpha/prediction/position-history)
    """
    request: Request = Request()
    if token_id is not None:
      request['tokenId'] = token_id
    if event_id is not None:
      request['eventId'] = event_id
    if result is not None:
      request['result'] = result
    if days is not None:
      request['days'] = days
    if limit is not None:
      request['limit'] = limit
    if page_index is not None:
      request['pageIndex'] = page_index
    return await self.request(
      request,
      method='POST',
      path='/v5/alpha/prediction/position-history',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PredictionPositionHistoryResult,
    )
