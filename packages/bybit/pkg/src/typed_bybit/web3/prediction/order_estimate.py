# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class PredictionOrderEstimateFeeDetail(TypedDict):
  """Fee breakdown."""

  serverFee: str
  """Platform service fee, in USDC."""
  polymarketFee: str
  """Polymarket protocol fee, in USDC."""


class Request(TypedDict):
  """Prediction-market trade estimate parameters."""

  tokenId: str
  """Outcome token ID, from `web3.prediction.event_detail`."""
  side: Literal[1, 2]
  """Trade direction."""
  eventId: str
  """Event ID associated with the token."""
  amount: str
  """For a buy: USDC investment amount. For a sell: share quantity."""
  orderType: Literal[1]
  """Order type. Phase 1 supports only fill-or-kill."""
  payTokenCode: NotRequired[Literal['USDC']]
  """Payment token code. Required for buy estimates; fixed to `USDC` in Phase 1."""


class PredictionOrderEstimateResult(TypedDict):
  """Estimated price, amounts and fees for a prediction-market trade."""

  avgPrice: str
  """Estimated average execution price per share, in the [0, 1] range."""
  estimatedCost: str
  """For a buy: USDC required. For a sell: shares to sell."""
  estimatedReceive: str
  """For a buy: expected shares received. For a sell: expected USDC received."""
  toWin: NotRequired[str]
  """Buy estimates only: potential profit if the prediction resolves in the caller's favor."""
  feeAmount: str
  """Total platform fee (`feeDetail.serverFee` plus `feeDetail.polymarketFee`)."""
  feeDetail: PredictionOrderEstimateFeeDetail
  slippage: str
  """Applied slippage tolerance, as a decimal string."""


class OrderEstimate(RpcEndpoint):
  """Estimate the outcome of a prediction-market buy or sell before executing it -- price, cost/receive amounts, and fees. Upstream requires this to be called, and its estimate explicitly confirmed by the user, before calling `web3.prediction.buy` or `web3.prediction.sell`."""

  async def order_estimate(
    self,
    *,
    token_id: str,
    side: Literal[1, 2],
    event_id: str,
    amount: str,
    order_type: Literal[1] = 1,
    pay_token_code: Literal['USDC'] | None = None,
    validate: bool | None = None,
  ) -> PredictionOrderEstimateResult:
    """Estimate the outcome of a prediction-market buy or sell before executing it -- price, cost/receive amounts, and fees. Upstream requires this to be called, and its estimate explicitly confirmed by the user, before calling `web3.prediction.buy` or `web3.prediction.sell`.

    Args:
      token_id: Outcome token ID, from `web3.prediction.event_detail`.
      side: Trade direction.
      event_id: Event ID associated with the token.
      amount: For a buy: USDC investment amount. For a sell: share quantity.
      order_type: Order type. Phase 1 supports only fill-or-kill.
      pay_token_code: Payment token code. Required for buy estimates; fixed to `USDC` in Phase 1.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/alpha/prediction/order-estimate)
    """
    request: Request = Request(
      tokenId=token_id, side=side, eventId=event_id, amount=amount, orderType=order_type
    )
    if pay_token_code is not None:
      request['payTokenCode'] = pay_token_code
    return await self.request(
      request,
      method='POST',
      path='/v5/alpha/prediction/order-estimate',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PredictionOrderEstimateResult,
    )
