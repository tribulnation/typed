# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class PredictionEventMarket(TypedDict):
  """One outcome market of the event."""

  tokenId: str
  """Outcome token ID, for use with `web3.prediction.buy`/`sell`/`order_estimate`."""
  outcome: str
  """Outcome label, for example `YES`, `NO`, or a team name."""
  price: str
  """Current price, in the [0, 1] range; represents implied probability."""
  side: Literal[1, 2]
  """Trade direction."""
  volume: str
  """Token trading volume, in USD."""
  liquidity: str
  """Token available liquidity, in USD."""


class Request(TypedDict):
  """Event lookup parameters."""

  eventId: NotRequired[str]
  """Event identifier. Use `slug` instead when available."""
  slug: NotRequired[str]
  """URL-friendly event slug. Takes priority over `eventId` when both are given."""
  hasMoreMarkets: NotRequired[bool]
  """Include markets from related sub-events. Defaults to `false`."""


class PredictionEventDetailResult(TypedDict):
  """A prediction-market event and its outcome markets."""

  eventId: str
  """Unique event identifier."""
  slug: str
  """URL-friendly slug for the event."""
  title: str
  """Event question or title."""
  description: str
  """Event details and resolution criteria."""
  category: str
  """Event classification."""
  endDate: TimestampMillis
  """Resolution time."""
  resolved: bool
  """Whether the event has been resolved."""
  volume: str
  """Total trading volume, in USD."""
  liquidity: str
  """Available liquidity, in USD."""
  markets: list[PredictionEventMarket]
  """Outcome markets for this event."""


class EventDetail(RpcEndpoint):
  """Retrieve a prediction-market event: its outcome markets and current pricing. Upstream instructs callers to fetch this before placing an order, to obtain the outcome `tokenId` and confirm market status."""

  async def event_detail(
    self,
    *,
    event_id: str | None = None,
    slug: str | None = None,
    has_more_markets: bool | None = None,
    validate: bool | None = None,
  ) -> PredictionEventDetailResult:
    """Retrieve a prediction-market event: its outcome markets and current pricing. Upstream instructs callers to fetch this before placing an order, to obtain the outcome `tokenId` and confirm market status.

    Args:
      event_id: Event identifier. Use `slug` instead when available.
      slug: URL-friendly event slug. Takes priority over `eventId` when both are given.
      has_more_markets: Include markets from related sub-events. Defaults to `false`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/alpha/prediction/event-detail)
    """
    request: Request = Request()
    if event_id is not None:
      request['eventId'] = event_id
    if slug is not None:
      request['slug'] = slug
    if has_more_markets is not None:
      request['hasMoreMarkets'] = has_more_markets
    return await self.request(
      request,
      method='POST',
      path='/v5/alpha/prediction/event-detail',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PredictionEventDetailResult,
    )
