# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class PredictionSideMarketToken(TypedDict):
  """One outcome token of a side market."""

  tokenId: str
  """Outcome token ID."""
  outcome: str
  """Outcome label, for example `YES`, `NO`, or a team name."""
  price: str
  """Current price, in the [0, 1] range; represents implied probability."""
  liquidity: str
  """Available liquidity, in USDC."""


class Request(TypedDict):
  """Side-market query parameters."""

  eventId: str
  """Event ID to query markets for."""
  sortBy: NotRequired[Literal['volume', 'liquidity', 'endDate']]
  """Sort field."""
  marketType: NotRequired[Literal[1, 2]]
  """Filter by market type."""
  limit: NotRequired[int]
  """Number of records per page."""
  pageIndex: NotRequired[int]
  """Page number, starting from `1`."""


class PredictionSideMarket(TypedDict):
  """One outcome market of an event."""

  marketId: str
  """Side-market identifier."""
  eventId: str
  """Parent event ID."""
  marketType: Literal[1, 2]
  """Market type."""
  question: str
  """Market question, for example "Will Argentina win the 2026 FIFA World Cup?"."""
  tokens: list[PredictionSideMarketToken]
  """Outcome tokens available in this market."""
  volume24h: str
  """Trading volume over the last 24 hours, in USDC."""
  endDate: TimestampMillis
  """Market resolution time."""
  status: Literal[1, 2, 3]
  """Market status."""


class PredictionSideMarketListResult(TypedDict):
  """Page of side markets for the requested event."""

  list: list[PredictionSideMarket]
  """Side market records."""
  total: int
  """Total matching records."""
  pageIndex: int
  """Current page number."""
  pageSize: int
  """Items returned per page."""


class SideMarketList(RpcEndpoint):
  """Query the outcome markets available for a prediction-market event, with current prices, liquidity, and trading status. Use this to discover which outcomes can be traded within an event; use `web3.prediction.event_detail` instead for full event detail."""

  def side_market_list_paged(
    self,
    event_id: str,
    *,
    sort_by: Literal['volume', 'liquidity', 'endDate'] | None = None,
    market_type: Literal[1, 2] | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[PredictionSideMarket, int]:
    """Query the outcome markets available for a prediction-market event, with current prices, liquidity, and trading status. Use this to discover which outcomes can be traded within an event; use `web3.prediction.event_detail` instead for full event detail.

    Paged variant of `side_market_list`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      event_id: Event ID to query markets for.
      sort_by: Sort field.
      market_type: Filter by market type.
      limit: Number of records per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/alpha/prediction/side-market-list)
    """

    async def next(page_index: int) -> tuple[list[PredictionSideMarket], int | None]:
      response = await self.side_market_list(
        event_id,
        sort_by=sort_by,
        market_type=market_type,
        limit=limit,
        page_index=page_index,
        validate=validate,
      )
      rows = response.get('list')
      rows = rows if rows is not None else []
      total = response.get('total')
      total = int(total) if total is not None else None
      if total is None or limit is None or (page_index - 1 + 1) * limit >= total:
        return rows, None
      return rows, page_index + 1

    return PaginatedResponse(1, next)

  async def side_market_list(
    self,
    event_id: str,
    *,
    sort_by: Literal['volume', 'liquidity', 'endDate'] | None = None,
    market_type: Literal[1, 2] | None = None,
    limit: int | None = None,
    page_index: int | None = None,
    validate: bool | None = None,
  ) -> PredictionSideMarketListResult:
    """Query the outcome markets available for a prediction-market event, with current prices, liquidity, and trading status. Use this to discover which outcomes can be traded within an event; use `web3.prediction.event_detail` instead for full event detail.

    Args:
      event_id: Event ID to query markets for.
      sort_by: Sort field.
      market_type: Filter by market type.
      limit: Number of records per page.
      page_index: Page number, starting from `1`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/alpha/prediction/side-market-list)
    """
    request: Request = Request(eventId=event_id)
    if sort_by is not None:
      request['sortBy'] = sort_by
    if market_type is not None:
      request['marketType'] = market_type
    if limit is not None:
      request['limit'] = limit
    if page_index is not None:
      request['pageIndex'] = page_index
    return await self.request(
      request,
      method='POST',
      path='/v5/alpha/prediction/side-market-list',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PredictionSideMarketListResult,
    )
