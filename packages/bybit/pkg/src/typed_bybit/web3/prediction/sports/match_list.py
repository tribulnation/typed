# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class PredictionSportsMatch(TypedDict):
  """One sports match."""

  matchId: str
  """Match identifier."""
  eventType: Literal[1]
  """Sports event type."""
  stageCode: Literal['Groups', 'R32', 'R16', 'QF', 'SF', 'Final']
  """Tournament stage."""
  homeTeam: str
  """Home team name."""
  awayTeam: str
  """Away team name."""
  matchTime: TimestampMillis
  """Match start time."""
  status: Literal[1, 2, 3]
  """Match status."""
  eventId: NotRequired[str]
  """Linked prediction-market event ID, present if markets are available for this match."""


class Request(TypedDict):
  """Filters and pagination for the sports-match query."""

  eventType: Literal[1]
  """Sports event type. Phase 1: `1` (FIFA_2026)."""
  status: NotRequired[Literal[1, 2, 3]]
  """Filter by match status."""
  startTime: NotRequired[TimestampMillis]
  """Filter matches starting after this time."""
  endTime: NotRequired[TimestampMillis]
  """Filter matches starting before this time."""
  stageCode: NotRequired[Literal['Groups', 'R32', 'R16', 'QF', 'SF', 'Final']]
  """Filter by tournament stage."""
  limit: NotRequired[int]
  """Number of records per page."""
  pageIndex: NotRequired[int]
  """Page number, starting from `1`."""


class PredictionSportsMatchListResult(TypedDict):
  """Page of matching sports matches."""

  list: list[PredictionSportsMatch]
  """Match records."""
  total: int
  """Total matching records."""
  pageIndex: int
  """Current page number."""
  pageSize: int
  """Items returned per page."""


class MatchList(RpcEndpoint):
  """Query sports matches available in the prediction market, filterable by event type, match status, and date range. Phase 1 supports the FIFA 2026 World Cup (`eventType=1`)."""

  def match_list_paged(
    self,
    event_type: Literal[1] = 1,
    *,
    status: Literal[1, 2, 3] | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    stage_code: Literal['Groups', 'R32', 'R16', 'QF', 'SF', 'Final'] | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[PredictionSportsMatch, int]:
    """Query sports matches available in the prediction market, filterable by event type, match status, and date range. Phase 1 supports the FIFA 2026 World Cup (`eventType=1`).

    Paged variant of `match_list`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      event_type: Sports event type. Phase 1: `1` (FIFA_2026).
      status: Filter by match status.
      start_time: Filter matches starting after this time.
      end_time: Filter matches starting before this time.
      stage_code: Filter by tournament stage.
      limit: Number of records per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/alpha/prediction/sports-match-list)
    """

    async def next(page_index: int) -> tuple[list[PredictionSportsMatch], int | None]:
      response = await self.match_list(
        event_type,
        status=status,
        start_time=start_time,
        end_time=end_time,
        stage_code=stage_code,
        limit=limit,
        page_index=page_index,
        validate=validate,
      )
      rows = response.get('list')
      rows = rows if rows is not None else []
      total = response.get('total')
      total = int(total) if total is not None else None
      if total is None or limit is None or (page_index - 1 + 1) * limit >= total:
        return rows, None
      return rows, page_index + 1

    return PaginatedResponse(1, next)

  async def match_list(
    self,
    event_type: Literal[1] = 1,
    *,
    status: Literal[1, 2, 3] | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    stage_code: Literal['Groups', 'R32', 'R16', 'QF', 'SF', 'Final'] | None = None,
    limit: int | None = None,
    page_index: int | None = None,
    validate: bool | None = None,
  ) -> PredictionSportsMatchListResult:
    """Query sports matches available in the prediction market, filterable by event type, match status, and date range. Phase 1 supports the FIFA 2026 World Cup (`eventType=1`).

    Args:
      event_type: Sports event type. Phase 1: `1` (FIFA_2026).
      status: Filter by match status.
      start_time: Filter matches starting after this time.
      end_time: Filter matches starting before this time.
      stage_code: Filter by tournament stage.
      limit: Number of records per page.
      page_index: Page number, starting from `1`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/alpha/prediction/sports-match-list)
    """
    request: Request = Request(eventType=event_type)
    if status is not None:
      request['status'] = status
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if stage_code is not None:
      request['stageCode'] = stage_code
    if limit is not None:
      request['limit'] = limit
    if page_index is not None:
      request['pageIndex'] = page_index
    return await self.request(
      request,
      method='POST',
      path='/v5/alpha/prediction/sports/match-list',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PredictionSportsMatchListResult,
    )
