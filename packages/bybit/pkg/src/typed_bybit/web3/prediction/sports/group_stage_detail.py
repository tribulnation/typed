# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class PredictionSportsGroupMatch(TypedDict):
  """One match within a group stage."""

  matchId: str
  """Match identifier."""
  homeTeam: str
  """Home team name."""
  awayTeam: str
  """Away team name."""
  matchTime: TimestampMillis
  """Match start time."""
  status: Literal[1, 2, 3]
  """Match status."""
  homeScore: int
  """Home team score. `-1` when not yet played."""
  awayScore: int
  """Away team score. `-1` when not yet played."""
  eventId: NotRequired[str]
  """Linked prediction-market event ID, present if markets are available for this match."""


class PredictionSportsStanding(TypedDict):
  """One team's group-stage standing."""

  rank: int
  """Current rank within the group."""
  teamName: str
  """Team name."""
  played: int
  """Matches played."""
  won: int
  """Matches won."""
  drawn: int
  """Matches drawn."""
  lost: int
  """Matches lost."""
  goalsFor: int
  """Goals scored."""
  goalsAgainst: int
  """Goals conceded."""
  goalDifference: int
  """Goal difference (`goalsFor` minus `goalsAgainst`)."""
  points: int
  """Total points."""


class Request(TypedDict):
  """Group-stage detail query parameters."""

  eventType: Literal[1]
  """Sports event type. Phase 1: `1` (FIFA_2026)."""
  groupName: NotRequired[str]
  """Filter by group name, for example `A`, `B`, ... `L` for FIFA 2026."""


class PredictionSportsGroup(TypedDict):
  """One group's standings and match schedule."""

  groupName: str
  """Group identifier, for example `A`."""
  standings: list[PredictionSportsStanding]
  """Team standings for this group, sorted by rank."""
  matches: list[PredictionSportsGroupMatch]
  """Matches in this group."""


class PredictionSportsGroupStageResult(TypedDict):
  """Group-stage standings and match schedule."""

  groups: list[PredictionSportsGroup]
  """Group stage data."""


class GroupStageDetail(RpcEndpoint):
  """Query the group-stage standings and match schedule for a sports prediction event: every group's team standings (points, goal difference, win/draw/loss) and its matches."""

  async def group_stage_detail(
    self,
    event_type: Literal[1] = 1,
    *,
    group_name: str | None = None,
    validate: bool | None = None,
  ) -> PredictionSportsGroupStageResult:
    """Query the group-stage standings and match schedule for a sports prediction event: every group's team standings (points, goal difference, win/draw/loss) and its matches.

    Args:
      event_type: Sports event type. Phase 1: `1` (FIFA_2026).
      group_name: Filter by group name, for example `A`, `B`, ... `L` for FIFA 2026.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/alpha/prediction/sports-group-stage-detail)
    """
    request: Request = Request(eventType=event_type)
    if group_name is not None:
      request['groupName'] = group_name
    return await self.request(
      request,
      method='POST',
      path='/v5/alpha/prediction/sports/group-stage-detail',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PredictionSportsGroupStageResult,
    )
