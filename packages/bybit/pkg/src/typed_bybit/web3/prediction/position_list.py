# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class PredictionPosition(TypedDict):
  """One active prediction-market position."""

  positionId: str
  """Position identifier."""
  tokenId: str
  """Outcome token ID."""
  eventId: str
  """Event ID."""
  outcomeName: str
  """Outcome label, for example `YES`, `NO`, or a team name."""
  shares: str
  """Total shares held."""
  cost: str
  """Total USDC cost of this position."""
  avgPrice: str
  """Average cost price per share."""
  currentPrice: str
  """Current market price per share, in the [0, 1] range."""
  value: str
  """Current market value of the position, in USDC."""
  unrealizedPnl: str
  """Unrealized profit or loss, in USDC."""
  unrealizedPnlRate: str
  """Unrealized profit/loss as a decimal ratio (`0.107` = 10.7%)."""
  createdAt: TimestampMillis
  """Position open time."""
  finished: bool
  """Whether the associated event has ended."""


class Request(TypedDict):
  """Filters and pagination for the active-position query."""

  tokenId: NotRequired[str]
  """Filter by outcome token ID."""
  eventId: NotRequired[str]
  """Filter by event ID."""
  limit: NotRequired[int]
  """Number of records per page."""
  pageIndex: NotRequired[int]
  """Page number, starting from `1`."""


class PredictionPositionListResult(TypedDict):
  """Page of active prediction-market positions."""

  positions: list[PredictionPosition]
  """Active position records."""
  total: int
  """Total matching records."""
  totalAssetUsd: str
  """Total asset value across all positions, in USD."""


class PositionList(RpcEndpoint):
  """Query the authenticated user's active prediction-market positions. Call this before placing a sell order to confirm the position exists."""

  def position_list_paged(
    self,
    *,
    token_id: str | None = None,
    event_id: str | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[PredictionPosition, int]:
    """Query the authenticated user's active prediction-market positions. Call this before placing a sell order to confirm the position exists.

    Paged variant of `position_list`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      token_id: Filter by outcome token ID.
      event_id: Filter by event ID.
      limit: Number of records per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/alpha/prediction/position-list)
    """

    async def next(page_index: int) -> tuple[list[PredictionPosition], int | None]:
      response = await self.position_list(
        token_id=token_id,
        event_id=event_id,
        limit=limit,
        page_index=page_index,
        validate=validate,
      )
      rows = response.get('positions')
      rows = rows if rows is not None else []
      total = response.get('total')
      total = int(total) if total is not None else None
      if total is None or limit is None or (page_index - 1 + 1) * limit >= total:
        return rows, None
      return rows, page_index + 1

    return PaginatedResponse(1, next)

  async def position_list(
    self,
    *,
    token_id: str | None = None,
    event_id: str | None = None,
    limit: int | None = None,
    page_index: int | None = None,
    validate: bool | None = None,
  ) -> PredictionPositionListResult:
    """Query the authenticated user's active prediction-market positions. Call this before placing a sell order to confirm the position exists.

    Args:
      token_id: Filter by outcome token ID.
      event_id: Filter by event ID.
      limit: Number of records per page.
      page_index: Page number, starting from `1`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/alpha/prediction/position-list)
    """
    request: Request = Request()
    if token_id is not None:
      request['tokenId'] = token_id
    if event_id is not None:
      request['eventId'] = event_id
    if limit is not None:
      request['limit'] = limit
    if page_index is not None:
      request['pageIndex'] = page_index
    return await self.request(
      request,
      method='POST',
      path='/v5/alpha/prediction/position-list',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PredictionPositionListResult,
    )
