# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class PredictionSellResult(TypedDict):
  """Identifier of the placed sell order."""

  orderNo: str
  """Order number. Query `web3.prediction.order_list` to check its fill status."""


class Request(TypedDict):
  """Prediction-market sell order parameters."""

  tokenId: str
  """Outcome token ID to sell, from the caller's held prediction-market positions."""
  size: str
  """Number of shares to sell, as a positive decimal string."""
  orderType: Literal[1]
  """Order type. Phase 1 supports only fill-or-kill."""
  slippage: str
  """Maximum acceptable price slippage, as a decimal string (`"0.05"` = 5%)."""
  eventId: str
  """Event ID associated with the token."""
  toTokenCode: NotRequired[Literal['USDC']]
  """Destination token code. Fixed to `USDC` in Phase 1."""


class Sell(RpcEndpoint):
  """Execute a sell order for prediction-market outcome token shares, exchanging shares of the outcome token identified by `tokenId` for a destination token (USDC in Phase 1)."""

  async def sell(
    self,
    *,
    token_id: str,
    size: str,
    order_type: Literal[1] = 1,
    slippage: str,
    event_id: str,
    to_token_code: Literal['USDC'] | None = None,
    validate: bool | None = None,
  ) -> PredictionSellResult:
    """Execute a sell order for prediction-market outcome token shares, exchanging shares of the outcome token identified by `tokenId` for a destination token (USDC in Phase 1).

    Args:
      token_id: Outcome token ID to sell, from the caller's held prediction-market positions.
      size: Number of shares to sell, as a positive decimal string.
      order_type: Order type. Phase 1 supports only fill-or-kill.
      slippage: Maximum acceptable price slippage, as a decimal string (`"0.05"` = 5%).
      event_id: Event ID associated with the token.
      to_token_code: Destination token code. Fixed to `USDC` in Phase 1.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/alpha/prediction/sell)
    """
    request: Request = Request(
      tokenId=token_id,
      size=size,
      orderType=order_type,
      slippage=slippage,
      eventId=event_id,
    )
    if to_token_code is not None:
      request['toTokenCode'] = to_token_code
    return await self.request(
      request,
      method='POST',
      path='/v5/alpha/prediction/sell',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PredictionSellResult,
    )
