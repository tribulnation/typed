# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class PredictionPayToken(TypedDict):
  """One accepted payment token for prediction-market trading."""

  tokenCode: str
  """Payment token code, for example `USDC`."""
  symbol: str
  """Token symbol."""
  tokenDecimals: int
  """Token decimal precision."""
  tokenIconUrlDay: str
  """Token icon URL, light mode."""
  tokenIconUrlNight: str
  """Token icon URL, dark mode."""
  supportChains: list[str]
  """Supported blockchain codes, for example `["POLYGON"]`."""


Response = list[PredictionPayToken]


class PayTokenList(RpcEndpoint):
  """Query the payment tokens accepted for prediction-market trading. Phase 1 supports only USDC."""

  async def pay_token_list(self, *, validate: bool | None = None) -> Response:
    """Query the payment tokens accepted for prediction-market trading. Phase 1 supports only USDC.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/alpha/prediction/pay-token-list)
    """
    return await self.request(
      None,
      method='GET',
      path='/v5/alpha/prediction/pay-token-list',
      meta={'signed': True},
      validate=validate,
      response_type=Response,
    )
