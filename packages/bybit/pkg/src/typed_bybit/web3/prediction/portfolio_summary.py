# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class PredictionPortfolioSummaryResult(TypedDict):
  """Prediction-market portfolio overview."""

  positionValue: str
  """Current total market value of all open positions, in USDC."""
  positionValueUsd: str
  """Current total market value of all open positions, in USD."""
  biggestWin: str
  """Largest single realized profit, in USDC."""
  winRate: str
  """Win rate, as a decimal ratio (`"0.67"` = 67%)."""
  winCount: int
  """Number of winning closed positions."""
  lossCount: int
  """Number of losing closed positions."""


class Request(TypedDict):
  """Portfolio-summary filters."""

  eventType: NotRequired[Literal[1]]
  """Filter by event type."""


class PortfolioSummary(RpcEndpoint):
  """Retrieve the authenticated user's prediction-market portfolio overview: current position value and win/loss statistics."""

  async def portfolio_summary(
    self,
    event_type: Literal[1] | None = None,
    *,
    validate: bool | None = None,
  ) -> PredictionPortfolioSummaryResult:
    """Retrieve the authenticated user's prediction-market portfolio overview: current position value and win/loss statistics.

    Args:
      event_type: Filter by event type.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/alpha/prediction/portfolio-summary)
    """
    request: Request = Request()
    if event_type is not None:
      request['eventType'] = event_type
    return await self.request(
      request,
      method='POST',
      path='/v5/alpha/prediction/portfolio-summary',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PredictionPortfolioSummaryResult,
    )
