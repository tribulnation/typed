# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class PoolInfoResult(TypedDict):
  """Detailed liquidity pool information."""

  poolAddress: str
  """Pool contract address."""
  poolName: str
  """Pool display name."""
  apy: str
  """Annual percentage yield."""
  tvl: str
  """Total value locked, in USD."""
  feeRate: str
  """Pool fee rate, for example `0.003` for 0.3%."""
  token0Symbol: str
  """First token's symbol."""
  token0Reserve: str
  """First token's reserve amount."""
  token1Symbol: str
  """Second token's symbol."""
  token1Reserve: str
  """Second token's reserve amount."""
  priceRangeLower: str
  """Pool's price range lower bound."""
  priceRangeUpper: str
  """Pool's price range upper bound."""
  currentPrice: str
  """Current pool price."""


class Request(TypedDict):
  """Pool lookup."""

  poolAddress: str
  """Pool contract address, as returned by `web3.lp.pool_list`."""


class PoolInfo(RpcEndpoint):
  """Query detailed information for one liquidity pool -- APY, fee rate, token reserves, and price range -- for display before staking."""

  async def pool_info(
    self,
    pool_address: str,
    *,
    validate: bool | None = None,
  ) -> PoolInfoResult:
    """Query detailed information for one liquidity pool -- APY, fee rate, token reserves, and price range -- for display before staking.

    Args:
      pool_address: Pool contract address, as returned by `web3.lp.pool_list`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/alpha/lp/pool-info)
    """
    return await self.request(
      Request(poolAddress=pool_address),
      method='POST',
      path='/v5/alpha/lp/pool-info',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PoolInfoResult,
    )
