# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class PoolSummary(TypedDict):
  """One liquidity pool."""

  poolAddress: str
  """Pool contract address."""
  poolName: str
  """Pool display name."""
  poolTag: str
  """Pool category tag."""
  apy: str
  """Annual percentage yield."""
  tvl: str
  """Total value locked, in USD."""
  token0Symbol: str
  """First token's symbol."""
  token0IconUrlDay: str
  """First token's icon URL, light mode."""
  token0IconUrlNight: str
  """First token's icon URL, dark mode."""
  token1Symbol: str
  """Second token's symbol."""
  token1IconUrlDay: str
  """Second token's icon URL, light mode."""
  token1IconUrlNight: str
  """Second token's icon URL, dark mode."""
  chainCode: str
  """Blockchain identifier."""
  chainIconUrl: str
  """Blockchain icon URL."""


class Request(TypedDict):
  """Pool list filter."""

  tokenSymbol: NotRequired[str]
  """Return only pools containing this token symbol, for example `ETH` or `USDC`."""


class PoolListResult(TypedDict):
  """Available liquidity pools."""

  pools: list[PoolSummary]
  """Matching pools."""


class PoolList(RpcEndpoint):
  """Query available Web3 liquidity pools, with optional filtering by payment token symbol."""

  async def pool_list(
    self,
    token_symbol: str | None = None,
    *,
    validate: bool | None = None,
  ) -> PoolListResult:
    """Query available Web3 liquidity pools, with optional filtering by payment token symbol.

    Args:
      token_symbol: Return only pools containing this token symbol, for example `ETH` or `USDC`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/alpha/lp/pool-list)
    """
    request: Request = Request()
    if token_symbol is not None:
      request['tokenSymbol'] = token_symbol
    return await self.request(
      request,
      method='POST',
      path='/v5/alpha/lp/pool-list',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PoolListResult,
    )
