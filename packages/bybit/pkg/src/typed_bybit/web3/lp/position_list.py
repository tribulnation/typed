# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core import TimestampSeconds
from typed_bybit.core.http import RpcEndpoint


class LpPosition(TypedDict):
  """One liquidity pool position."""

  positionId: int
  """Position ID."""
  poolAddress: str
  """Pool contract address."""
  poolName: str
  """Pool display name."""
  stakedAmount: str
  """Total staked amount, in the payment token."""
  stakedTokenCode: str
  """Payment token code."""
  stakedTokenSymbol: str
  """Payment token symbol."""
  currentValueUsd: str
  """Current position valuation, in USD."""
  earnedRewardsUsd: str
  """Cumulative rewards earned, in USD."""
  unrealizedPnl: str
  """Unrealized profit/loss."""
  realizedPnl: str
  """Realized profit/loss."""
  apy: str
  """Current annual percentage yield."""
  token0Amount: str
  """First token's quantity in this position."""
  token0Symbol: str
  """First token's symbol."""
  token1Amount: str
  """Second token's quantity in this position."""
  token1Symbol: str
  """Second token's symbol."""
  rangeUpper: str
  """Position's upper range boundary."""
  rangeLower: str
  """Position's lower range boundary."""
  createTime: TimestampSeconds
  """Position creation time."""
  updateTime: TimestampSeconds
  """Position last-update time."""
  status: Literal[1, 2, 3]
  """Position state. `1`: Active. `2`: Closed. `3`: Processing."""


class PositionListResult(TypedDict):
  """Caller's liquidity pool positions."""

  positions: list[LpPosition]
  """Matching positions."""


class PositionList(RpcEndpoint):
  """Query the caller's Web3 liquidity pool positions, with real-time valuation, earned rewards and APY."""

  async def position_list(self, *, validate: bool | None = None) -> PositionListResult:
    """Query the caller's Web3 liquidity pool positions, with real-time valuation, earned rewards and APY.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/alpha/lp/position-list)
    """
    return await self.request(
      None,
      method='POST',
      path='/v5/alpha/lp/position-list',
      meta={'signed': True},
      validate=validate,
      response_type=PositionListResult,
    )
