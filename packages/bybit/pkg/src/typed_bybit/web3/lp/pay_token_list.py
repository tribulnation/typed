# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class PayToken(TypedDict):
  """One supported payment token."""

  tokenCode: str
  """Token code, for example `CEX_1`."""
  tokenSymbol: str
  """Token symbol, for example `USDT`."""
  chainCode: str
  """Blockchain identifier."""
  chainIconUrl: str
  """Blockchain icon URL."""
  decimals: int
  """Token decimal precision."""
  availableBalance: str
  """Caller's available balance for this token."""
  tokenIconUrlDay: str
  """Token icon URL, light mode."""
  tokenIconUrlNight: str
  """Token icon URL, dark mode."""
  minStakeAmount: str
  """Minimum stake amount for this token."""
  maxStakeAmount: str
  """Maximum stake amount for this token."""


class Request(TypedDict):
  """Payment-token filter."""

  chainCode: NotRequired[str]
  """Filter by blockchain identifier."""
  tokenAddress: NotRequired[str]
  """Filter by token contract address."""


class PayTokenListResult(TypedDict):
  """Supported LP payment tokens."""

  tokens: list[PayToken]
  """Matching payment tokens."""


class PayTokenList(RpcEndpoint):
  """Query the supported payment tokens for LP staking, including the caller's available balance for each. Call before staking to confirm token availability and verify balances."""

  async def pay_token_list(
    self,
    *,
    chain_code: str | None = None,
    token_address: str | None = None,
    validate: bool | None = None,
  ) -> PayTokenListResult:
    """Query the supported payment tokens for LP staking, including the caller's available balance for each. Call before staking to confirm token availability and verify balances.

    Args:
      chain_code: Filter by blockchain identifier.
      token_address: Filter by token contract address.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/alpha/lp/pay-token-list)
    """
    request: Request = Request()
    if chain_code is not None:
      request['chainCode'] = chain_code
    if token_address is not None:
      request['tokenAddress'] = token_address
    return await self.request(
      request,
      method='POST',
      path='/v5/alpha/lp/pay-token-list',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PayTokenListResult,
    )
