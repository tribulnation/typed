# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class TokenAddressInfo(TypedDict):
  """One on-chain token identifier."""

  chainCode: str
  """Blockchain code, e.g. `ETH`, `SOL`, `BSC`."""
  tokenAddress: str
  """Token contract address on the specified chain."""


class TokenPriceInfo(TypedDict):
  """Live price and market statistics for one on-chain token."""

  chainCode: str
  """Blockchain identifier."""
  tokenAddress: str
  """Token contract address."""
  price: str
  """Current price, in USD."""
  change24h: str
  """24-hour price change, as a decimal, e.g. `0.15` for +15%."""
  vol24h: str
  """24-hour trading volume, in USD."""
  marketCap: str
  """Market capitalization, in USD."""
  liquidity: str
  """Total liquidity, in USD."""
  holders: str
  """Number of token holders."""


class BizTokenPriceListResult(TypedDict):
  """Batch price query result."""

  tokenPriceInfoList: list[TokenPriceInfo]
  """One entry per requested token."""


class Request(TypedDict):
  """Batch of token identifiers to price."""

  tokenAddressInfo: list[TokenAddressInfo]
  """Token identifiers to query. Maximum 20 items per request."""


class BizTokenPriceList(RpcEndpoint):
  """Batch-query live price and market statistics for a set of on-chain tokens identified by chain and contract address."""

  async def biz_token_price_list(
    self,
    token_address_info: list[TokenAddressInfo],
    *,
    validate: bool | None = None,
  ) -> BizTokenPriceListResult:
    """Batch-query live price and market statistics for a set of on-chain tokens identified by chain and contract address.

    Args:
      token_address_info: Token identifiers to query. Maximum 20 items per request.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/alpha/trade/biz-token-price-list)
    """
    return await self.request(
      Request(tokenAddressInfo=token_address_info),
      method='POST',
      path='/v5/alpha/trade/biz-token-price-list',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BizTokenPriceListResult,
    )
