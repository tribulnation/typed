# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class AssetHoldingDetail(TypedDict):
  """One held on-chain token and its valuation."""

  chainCode: str
  """Blockchain code identifier."""
  chainIconUrl: str
  """Chain icon URL."""
  tokenAddress: str
  """Token contract address on the chain."""
  tokenCode: str
  """Token code in `DEX_<id>` format, for use in quote/execution requests."""
  tokenSymbol: str
  """Token symbol identifier."""
  tokenDecimals: int
  """Token decimal precision."""
  tokenIconUrlDay: str
  """Token icon URL, light mode."""
  tokenIconUrlNight: str
  """Token icon URL, dark mode."""
  tokenAmount: str
  """Quantity of tokens held."""
  tokenAmountUsd: str
  """Holdings value, in USD."""
  tradeFlag: Literal[0, 1]
  """Tradability status."""
  pnl: NotRequired[str]
  """Unrealized PnL, in USD. Positive means profit. Absent when there is no cost basis."""
  pnlRatio: NotRequired[str]
  """PnL ratio as a decimal, e.g. `0.15` for +15%. Absent when unavailable."""
  costPrice: NotRequired[str]
  """Average cost price. Absent when unavailable."""
  lastPrice: str
  """Current market price."""
  costTotalValue: NotRequired[str]
  """Total cost basis, in USD. Absent when unavailable."""
  assetStatus: Literal[0, 1, 2]
  """Token lifecycle state."""
  announcementUrl: NotRequired[str]
  """Delisting announcement URL. Present when `assetStatus` is 1 or 2."""
  estimatedOfflineTime: NotRequired[TimestampMillis]
  """Projected delisting time. Present when `assetStatus` is 1."""
  delistingTime: NotRequired[TimestampMillis]
  """Actual delisting time. Present when `assetStatus` is 2."""


class Request(TypedDict):
  chainCode: str
  """Blockchain code."""
  tokenAddress: str
  """Token contract address."""


class AssetDetailResult(TypedDict):
  """Holding detail for one token."""

  assetList: list[AssetHoldingDetail]
  """The requested token's holding. Empty when the caller does not hold this token; otherwise exactly one element."""


class AssetDetail(RpcEndpoint):
  """Get the caller's holding of one specific on-chain token, identified by chain and contract address."""

  async def asset_detail(
    self,
    *,
    chain_code: str,
    token_address: str,
    validate: bool | None = None,
  ) -> AssetDetailResult:
    """Get the caller's holding of one specific on-chain token, identified by chain and contract address.

    Args:
      chain_code: Blockchain code.
      token_address: Token contract address.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/alpha/trade/asset-detail)
    """
    return await self.request(
      Request(chainCode=chain_code, tokenAddress=token_address),
      method='POST',
      path='/v5/alpha/trade/asset-detail',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=AssetDetailResult,
    )
