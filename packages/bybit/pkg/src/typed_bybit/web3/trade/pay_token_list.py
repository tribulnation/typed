# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class PayToken(TypedDict):
  """One usable payment token."""

  tokenCode: str
  """Payment token code, in `CEX_<id>` format."""
  symbol: str
  """Token symbol, e.g. `USDT`, `USDC`."""
  tokenDecimals: int
  """Token decimal precision."""
  tokenIconUrlDay: str
  """Token icon URL, light mode."""
  tokenIconUrlNight: str
  """Token icon URL, dark mode."""
  limit: str
  """Maximum trading amount for this payment token."""
  supportChains: list[str]
  """Supported blockchain codes, e.g. `["ETH", "BSC", "SOL"]`."""


class Request(TypedDict):
  chainCode: str
  """Blockchain code."""
  tokenAddress: str
  """Token contract address."""


PayTokenListResult = list[PayToken]


class PayTokenList(RpcEndpoint):
  """List the payment tokens (CEX-side balances) usable to purchase one specific on-chain token."""

  async def pay_token_list(
    self,
    *,
    chain_code: str,
    token_address: str,
    validate: bool | None = None,
  ) -> PayTokenListResult:
    """List the payment tokens (CEX-side balances) usable to purchase one specific on-chain token.

    Args:
      chain_code: Blockchain code.
      token_address: Token contract address.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/alpha/trade/pay-token-list)
    """
    return await self.request(
      Request(chainCode=chain_code, tokenAddress=token_address),
      method='POST',
      path='/v5/alpha/trade/pay-token-list',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PayTokenListResult,
    )
