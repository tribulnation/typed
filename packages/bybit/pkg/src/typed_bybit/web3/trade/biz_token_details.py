# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampSeconds
from typed_bybit.core.http import RpcEndpoint


class BizTokenDetails(TypedDict):
  """Project detail for one tradable on-chain token."""

  tokenCode: str
  """Token identifier in `DEX_<id>` format."""
  chainCode: str
  """Blockchain code."""
  chainIconUrl: str
  """Chain icon URL."""
  tokenAddress: str
  """Token contract address."""
  symbol: str
  """Token symbol."""
  tokenDecimals: int
  """Decimal precision."""
  tokenIconUrlDay: str
  """Token icon URL, light mode."""
  tokenIconUrlNight: str
  """Token icon URL, dark mode."""
  minOrderQuantity: str
  """Minimum order size."""
  maxOrderQuantity: str
  """Maximum order size."""
  maxPositionQuantity: str
  """Maximum position size per user."""
  tokenDesc: NotRequired[str]
  """Project description."""
  xUrl: NotRequired[str]
  """X (Twitter) profile URL."""
  officialUrl: NotRequired[str]
  """Official website URL."""
  whitePaperUrl: NotRequired[str]
  """Whitepaper URL."""
  tokenTag: Literal[0, 1, 2]
  """Tag classification."""
  riskFlag: Literal[0, 1]
  """Risk indicator."""
  createTimeOnchain: TimestampSeconds
  """Blockchain creation time."""
  status: Literal[0, 1, 2, 3, 4]
  """Token lifecycle status."""
  tokenTags: NotRequired[list[int]]
  """Associated tag identifiers."""
  showMessage: Literal[0, 1]
  """Whether a notification banner should be displayed for this token."""
  content: NotRequired[str]
  """Notification banner text. Present when `showMessage` is `1`."""
  linkName: NotRequired[str]
  """Notification banner link label. Present when `showMessage` is `1`."""
  linkAddress: NotRequired[str]
  """Notification banner link URL. Present when `showMessage` is `1`."""


class Request(TypedDict):
  chainCode: str
  """Blockchain identifier."""
  tokenAddress: str
  """Token contract address."""


class BizTokenDetailsEndpoint(RpcEndpoint):
  """Get full project detail for one on-chain token available for Web3/Alpha trading: description, links, listing status and any active notification banner."""

  async def biz_token_details(
    self,
    *,
    chain_code: str,
    token_address: str,
    validate: bool | None = None,
  ) -> BizTokenDetails:
    """Get full project detail for one on-chain token available for Web3/Alpha trading: description, links, listing status and any active notification banner.

    Args:
      chain_code: Blockchain identifier.
      token_address: Token contract address.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/alpha/trade/biz-token-details)
    """
    return await self.request(
      Request(chainCode=chain_code, tokenAddress=token_address),
      method='POST',
      path='/v5/alpha/trade/biz-token-details',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BizTokenDetails,
    )
