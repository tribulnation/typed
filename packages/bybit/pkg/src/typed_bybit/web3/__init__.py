# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .lp import Lp
from .prediction import Prediction
from .trade import Trade
from typed_bybit.core.http import RpcEndpoint


class Web3(RpcEndpoint):
  """Web3 — Bybit's on-chain wallet/DeFi product suite: liquidity-pool farming (web3/lp), on-chain prediction markets including sports (web3/prediction), and web3 token swap/trade execution (web3/trade).

  References:
    - [Upstream docs](https://bybit-exchange.github.io/docs/v5/alpha/trade/trade-quote)
  """

  @cached_property
  def lp(self) -> Lp:
    """Web3 LP (liquidity-pool) farming — browse farmable pools and their pay tokens/prices, stake into and redeem from a pool, and list a wallet's own LP positions and orders.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/alpha/lp/pool-list)
    """
    return Lp(client=self.client)

  @cached_property
  def prediction(self) -> Prediction:
    """Web3 prediction markets — browse events, side markets and order books, token prices and price history, place and cancel buy/sell orders, and track a wallet's own positions, portfolio summary, and order history.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/alpha/prediction/engine-status)
    """
    return Prediction(client=self.client)

  @cached_property
  def trade(self) -> Trade:
    """Web3 token trade/swap — quote and execute purchases and redemptions of business tokens against supported pay tokens, and browse a wallet's on-chain asset holdings, token details/prices, and order history.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/alpha/trade/trade-quote)
    """
    return Trade(client=self.client)
