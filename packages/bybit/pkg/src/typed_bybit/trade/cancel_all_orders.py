# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class CancelledOrder(TypedDict):
  """One cancelled order."""

  orderId: str
  """Order ID."""
  orderLinkId: str
  """User-customised order ID."""


class Request(TypedDict):
  """Scope of orders to cancel."""

  category: Literal['linear', 'inverse', 'spot', 'option']
  """Product type."""
  symbol: NotRequired[str]
  """Symbol name, uppercase only. Required for linear/inverse if `baseCoin`/`settleCoin` are not passed."""
  baseCoin: NotRequired[str]
  """Base coin, uppercase only. linear & inverse only."""
  settleCoin: NotRequired[str]
  """Settle coin, uppercase only. linear & inverse required if `symbol`/`baseCoin` not passed; option: `USDT` or `USDC`. Not supported for spot."""
  orderFilter: NotRequired[
    Literal[
      'Order',
      'tpslOrder',
      'StopOrder',
      'OcoOrder',
      'BidirectionalTpslOrder',
      'OpenOrder',
    ]
  ]
  """Order kinds to cancel. Valid set is category-dependent: spot allows all six values; linear/inverse allow `Order`, `StopOrder`, `OpenOrder` (all kinds cancelled if omitted); option allows `Order`, `StopOrder` (all kinds cancelled if omitted)."""
  stopOrderType: NotRequired[Literal['Stop']]
  """Conditional order sub-type to cancel. Only for `category=linear`/`inverse` and `orderFilter=StopOrder`."""


class CancelAllOrdersResult(TypedDict):
  """Every order the request cancelled."""

  list: list[CancelledOrder]
  """Cancelled orders."""
  success: NotRequired[Literal['0', '1']]
  """`"1"`: success, `"0"`: fail. Not returned for UTA1.0 (inverse)."""


class CancelAllOrders(RpcEndpoint):
  """Cancel all open orders matching the given scope. Spot cancels all open spot orders without further scoping; linear/inverse require one of `symbol`, `baseCoin` or `settleCoin`; option needs no scoping param. If multiple scoping params are passed, priority is `symbol` > `baseCoin` > `settleCoin`. Futures cancel up to 500 orders per call (picked randomly if more than 500 are open); spot and options have no limit."""

  async def cancel_all_orders(
    self,
    category: Literal['linear', 'inverse', 'spot', 'option'],
    *,
    symbol: str | None = None,
    base_coin: str | None = None,
    settle_coin: str | None = None,
    order_filter: Literal[
      'Order',
      'tpslOrder',
      'StopOrder',
      'OcoOrder',
      'BidirectionalTpslOrder',
      'OpenOrder',
    ]
    | None = None,
    stop_order_type: Literal['Stop'] | None = None,
    validate: bool | None = None,
  ) -> CancelAllOrdersResult:
    """Cancel all open orders matching the given scope. Spot cancels all open spot orders without further scoping; linear/inverse require one of `symbol`, `baseCoin` or `settleCoin`; option needs no scoping param. If multiple scoping params are passed, priority is `symbol` > `baseCoin` > `settleCoin`. Futures cancel up to 500 orders per call (picked randomly if more than 500 are open); spot and options have no limit.

    Args:
      category: Product type.
      symbol: Symbol name, uppercase only. Required for linear/inverse if `baseCoin`/`settleCoin` are not passed.
      base_coin: Base coin, uppercase only. linear & inverse only.
      settle_coin: Settle coin, uppercase only. linear & inverse required if `symbol`/`baseCoin` not passed; option: `USDT` or `USDC`. Not supported for spot.
      order_filter: Order kinds to cancel. Valid set is category-dependent: spot allows all six values; linear/inverse allow `Order`, `StopOrder`, `OpenOrder` (all kinds cancelled if omitted); option allows `Order`, `StopOrder` (all kinds cancelled if omitted).
      stop_order_type: Conditional order sub-type to cancel. Only for `category=linear`/`inverse` and `orderFilter=StopOrder`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/order/cancel-all)
    """
    request: Request = Request(category=category)
    if symbol is not None:
      request['symbol'] = symbol
    if base_coin is not None:
      request['baseCoin'] = base_coin
    if settle_coin is not None:
      request['settleCoin'] = settle_coin
    if order_filter is not None:
      request['orderFilter'] = order_filter
    if stop_order_type is not None:
      request['stopOrderType'] = stop_order_type
    return await self.request(
      request,
      method='POST',
      path='/v5/order/cancel-all',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CancelAllOrdersResult,
    )
