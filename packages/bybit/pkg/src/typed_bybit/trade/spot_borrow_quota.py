# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  category: Literal['spot']
  """Product type."""
  symbol: str
  """Symbol name."""
  side: Literal['Buy', 'Sell']
  """Transaction side."""


class SpotBorrowQuota(TypedDict):
  """Available spot and margin trading balance."""

  symbol: str
  """Symbol name, uppercase only."""
  side: Literal['Buy', 'Sell']
  """Transaction side."""
  maxTradeQty: str
  """Max base-coin qty tradeable. With spot margin on for a margin pair: available balance plus max borrowable qty. Otherwise: actual available balance. Up to 4 decimals."""
  maxTradeAmount: str
  """Max quote-coin amount tradeable, same margin-on/off distinction as `maxTradeQty`. Up to 8 decimals."""
  spotMaxTradeQty: str
  """Actual base-coin qty tradeable or held regardless of margin switch (borrowable qty not included). Up to 4 decimals."""
  spotMaxTradeAmount: str
  """Actual quote-coin amount tradeable or held regardless of margin switch (borrowable amount not included). Up to 8 decimals."""
  borrowCoin: str
  """Borrow coin."""


class SpotBorrowQuotaEndpoint(RpcEndpoint):
  """Query the available balance for spot trading and margin trading."""

  async def spot_borrow_quota(
    self,
    category: Literal['spot'] = 'spot',
    *,
    symbol: str,
    side: Literal['Buy', 'Sell'],
    validate: bool | None = None,
  ) -> SpotBorrowQuota:
    """Query the available balance for spot trading and margin trading.

    Args:
      category: Product type.
      symbol: Symbol name.
      side: Transaction side.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/order/spot-borrow-quota)
    """
    return await self.request(
      Request(category=category, symbol=symbol, side=side),
      method='GET',
      path='/v5/order/spot-borrow-check',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SpotBorrowQuota,
    )
