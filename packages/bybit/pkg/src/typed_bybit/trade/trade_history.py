# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint
from typed_bybit.schemas import CreateType


class Request(TypedDict):
  category: Literal['linear', 'inverse', 'spot', 'option']
  """Product type."""
  symbol: NotRequired[str]
  """Symbol name."""
  orderId: NotRequired[str]
  """Order ID."""
  orderLinkId: NotRequired[str]
  """User-customised order ID."""
  baseCoin: NotRequired[str]
  """Base coin."""
  settleCoin: NotRequired[str]
  """Settle coin."""
  startTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  execType: NotRequired[
    Literal[
      'Trade',
      'AdlTrade',
      'Funding',
      'BustTrade',
      'Delivery',
      'Settle',
      'BlockTrade',
      'MovePosition',
      'FutureSpread',
      'CorporateAction',
    ]
  ]
  """Execution type."""
  limit: NotRequired[int]
  """Page size."""
  cursor: NotRequired[str]
  """Pagination cursor."""


class Execution(TypedDict):
  """One execution (fill)."""

  symbol: str
  """Symbol name."""
  orderId: str
  """Order ID."""
  orderLinkId: str
  """User-customised order ID."""
  side: Literal['Buy', 'Sell']
  """Side."""
  orderPrice: Decimal
  """Order price."""
  orderQty: Decimal
  """Order quantity."""
  leavesQty: Decimal
  """Remaining quantity not executed."""
  createType: NotRequired[CreateType]
  orderType: Literal['Market', 'Limit']
  """Order type."""
  stopOrderType: Literal[
    'TakeProfit',
    'StopLoss',
    'TrailingStop',
    'Stop',
    'PartialTakeProfit',
    'PartialStopLoss',
    'tpslOrder',
    'OcoOrder',
    'MmRateClose',
    'BidirectionalTpslOrder',
    'UNKNOWN',
    '',
  ]
  """Stop order sub-type. `UNKNOWN` or `""` when the order is not a stop order."""
  execFee: Decimal
  """Executed trading fee."""
  execFeeV2: NotRequired[Decimal]
  """Transaction fee; documented as spot-leg-only for `execType=FutureSpread`, but confirmed live populated on an ordinary spot `Trade` execution too."""
  execId: str
  """Execution ID."""
  execPrice: Decimal
  """Execution price."""
  execQty: Decimal
  """Execution quantity."""
  execType: Literal[
    'Trade',
    'AdlTrade',
    'Funding',
    'BustTrade',
    'Delivery',
    'Settle',
    'BlockTrade',
    'MovePosition',
    'FutureSpread',
    'CorporateAction',
    'UNKNOWN',
  ]
  """Execution type. `UNKNOWN` may be returned by a classic account and cannot be queried by this type."""
  execValue: Decimal
  """Executed order value."""
  execTime: TimestampMillis
  """Execution time, as a millisecond timestamp."""
  feeCurrency: str
  """Trading fee currency."""
  isMaker: bool
  """Whether the execution was a maker fill."""
  feeRate: Decimal
  """Trading fee rate."""
  tradeIv: NotRequired[Literal[''] | Decimal]
  """Implied volatility of the trade. Option only; empty string confirmed live on a spot execution."""
  markIv: NotRequired[Literal[''] | Decimal]
  """Implied volatility of the mark price. Option only; empty string confirmed live on a spot execution."""
  markPrice: NotRequired[Literal[''] | Decimal]
  """Mark price of the symbol at execution; empty string confirmed live on a spot execution."""
  indexPrice: NotRequired[Literal[''] | Decimal]
  """Index price of the symbol at execution. Option only; empty string confirmed live on a spot execution."""
  underlyingPrice: NotRequired[Literal[''] | Decimal]
  """Underlying price of the symbol at execution. Option only; empty string confirmed live on a spot execution."""
  blockTradeId: str
  """Paradigm block trade ID."""
  closedSize: Literal[''] | Decimal
  """Closed position size; empty string confirmed live on a spot execution (a derivatives-position concept)."""
  seq: int
  """Cross sequence, shared across fills and position updates concluded together. Different symbols may share a `seq`; combine with `symbol` for uniqueness."""
  extraFees: NotRequired[str]
  """Trading fee rate information. Returned only for Indian-KYC users, spot orders placed on the Indonesian site, or spot fiat-currency orders on the EU site; empty string otherwise."""


class TradeHistoryResult(TypedDict):
  """Page of matching executions."""

  category: Literal['linear', 'inverse', 'spot', 'option']
  """Product type."""
  nextPageCursor: NotRequired[str]
  """Opaque cursor for the next page. Pass it back as `cursor`; an empty string means there are no further pages."""
  list: list[Execution]
  """Matching executions, newest first by `execTime`."""


class TradeHistory(RpcEndpoint):
  """Query execution records (fills), sorted by `execTime` descending. A single order may have multiple executions. Items can tie on `execTime`; sort by `execId`+`orderId`+`leavesQty` when that matters."""

  def trade_history_paged(
    self,
    category: Literal['linear', 'inverse', 'spot', 'option'],
    *,
    symbol: str | None = None,
    order_id: str | None = None,
    order_link_id: str | None = None,
    base_coin: str | None = None,
    settle_coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    exec_type: Literal[
      'Trade',
      'AdlTrade',
      'Funding',
      'BustTrade',
      'Delivery',
      'Settle',
      'BlockTrade',
      'MovePosition',
      'FutureSpread',
      'CorporateAction',
    ]
    | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[Execution, str]:
    """Query execution records (fills), sorted by `execTime` descending. A single order may have multiple executions. Items can tie on `execTime`; sort by `execId`+`orderId`+`leavesQty` when that matters.

    Paged variant of `trade_history`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      category: Product type.
      symbol: Symbol name.
      order_id: Order ID.
      order_link_id: User-customised order ID.
      base_coin: Base coin.
      settle_coin: Settle coin.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      exec_type: Execution type.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/order/execution)
    """

    async def next(cursor: str) -> tuple[list[Execution], str | None]:
      response = await self.trade_history(
        category,
        symbol=symbol,
        order_id=order_id,
        order_link_id=order_link_id,
        base_coin=base_coin,
        settle_coin=settle_coin,
        start_time=start_time,
        end_time=end_time,
        exec_type=exec_type,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('list')
      state = response.get('nextPageCursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def trade_history(
    self,
    category: Literal['linear', 'inverse', 'spot', 'option'],
    *,
    symbol: str | None = None,
    order_id: str | None = None,
    order_link_id: str | None = None,
    base_coin: str | None = None,
    settle_coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    exec_type: Literal[
      'Trade',
      'AdlTrade',
      'Funding',
      'BustTrade',
      'Delivery',
      'Settle',
      'BlockTrade',
      'MovePosition',
      'FutureSpread',
      'CorporateAction',
    ]
    | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> TradeHistoryResult:
    """Query execution records (fills), sorted by `execTime` descending. A single order may have multiple executions. Items can tie on `execTime`; sort by `execId`+`orderId`+`leavesQty` when that matters.

    Args:
      category: Product type.
      symbol: Symbol name.
      order_id: Order ID.
      order_link_id: User-customised order ID.
      base_coin: Base coin.
      settle_coin: Settle coin.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      exec_type: Execution type.
      limit: Page size.
      cursor: Pagination cursor.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/order/execution)
    """
    request: Request = Request(category=category)
    if symbol is not None:
      request['symbol'] = symbol
    if order_id is not None:
      request['orderId'] = order_id
    if order_link_id is not None:
      request['orderLinkId'] = order_link_id
    if base_coin is not None:
      request['baseCoin'] = base_coin
    if settle_coin is not None:
      request['settleCoin'] = settle_coin
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if exec_type is not None:
      request['execType'] = exec_type
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/execution/list',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=TradeHistoryResult,
    )
