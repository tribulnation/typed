# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class CancelQuoteResult(TypedDict):
  """Identifiers of the cancelled quote."""

  rfqId: str
  """Inquiry ID."""
  quoteId: str
  """Quote ID."""
  quoteLinkId: str
  """Custom quote ID."""


class Request(TypedDict):
  """Identifies the quote to cancel."""

  quoteId: NotRequired[str]
  """Quote ID."""
  rfqId: NotRequired[str]
  """Inquiry ID."""
  quoteLinkId: NotRequired[str]
  """Custom quote ID."""


class CancelQuote(RpcEndpoint):
  """Cancel a quote by quote ID, custom quote ID, or inquiry ID. Up to 50 requests per second."""

  async def cancel_quote(
    self,
    *,
    quote_id: str | None = None,
    rfq_id: str | None = None,
    quote_link_id: str | None = None,
    validate: bool | None = None,
  ) -> CancelQuoteResult:
    """Cancel a quote by quote ID, custom quote ID, or inquiry ID. Up to 50 requests per second.

    Args:
      quote_id: Quote ID.
      rfq_id: Inquiry ID.
      quote_link_id: Custom quote ID.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/rfq/trade/cancel-quote)
    """
    request: Request = Request()
    if quote_id is not None:
      request['quoteId'] = quote_id
    if rfq_id is not None:
      request['rfqId'] = rfq_id
    if quote_link_id is not None:
      request['quoteLinkId'] = quote_link_id
    return await self.request(
      request,
      method='POST',
      path='/v5/rfq/cancel-quote',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CancelQuoteResult,
    )
