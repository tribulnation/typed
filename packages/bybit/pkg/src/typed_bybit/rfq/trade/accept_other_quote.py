# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class AcceptOtherQuoteResult(TypedDict):
  """Identifies the RFQ that was updated."""

  rfqId: str
  """Inquiry ID."""


class Request(TypedDict):
  """Identifies the RFQ."""

  rfqId: str
  """Inquiry ID."""


class AcceptOtherQuote(RpcEndpoint):
  """Accept quotes from non-liquidity-provider counterparties on an RFQ. Up to 50 requests per second."""

  async def accept_other_quote(
    self,
    rfq_id: str,
    *,
    validate: bool | None = None,
  ) -> AcceptOtherQuoteResult:
    """Accept quotes from non-liquidity-provider counterparties on an RFQ. Up to 50 requests per second.

    Args:
      rfq_id: Inquiry ID.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/rfq/trade/accept-other-quote)
    """
    return await self.request(
      Request(rfqId=rfq_id),
      method='POST',
      path='/v5/rfq/accept-other-quote',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=AcceptOtherQuoteResult,
    )
