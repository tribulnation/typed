# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class RfqCounterparty(TypedDict):
  """One available counterparty."""

  traderName: str
  """The counterparty's display name."""
  deskCode: str
  """The counterparty's own unique desk identification code."""
  type: NotRequired[Literal['LP'] | None]
  """`"LP"` for an automated liquidity provider, `null` for a standard (non-LP) quoter."""


class RfqStrategyType(TypedDict):
  """One available combination strategy."""

  strategyName: str
  """Strategy name, e.g. `custom` or `FundingRate`."""


class RfqConfig(TypedDict):
  """This account's RFQ configuration."""

  deskCode: str
  """This account's own unique desk identification code."""
  maxLegs: int
  """Maximum number of legs in one RFQ."""
  maxLP: int
  """Maximum number of liquidity providers that may be selected on one inquiry."""
  maxActiveRfq: int
  """Maximum number of unfinished RFQs this account may have open at once."""
  rfqExpireTime: int
  """RFQ expiration time, in minutes."""
  minLimitQtySpotOrder: int
  """Minimum order quantity for a spot leg."""
  minLimitQtyContractOrder: int
  """Minimum order quantity for a linear contract leg."""
  minLimitQtyOptionOrder: int
  """Minimum order quantity for an option leg."""
  strategyTypes: list[RfqStrategyType]
  """Combination strategy types available for this account's `strategyType` parameter."""
  counterparties: list[RfqCounterparty]
  """Counterparties available to quote this account's RFQs."""


class RfqConfigEndpoint(RpcEndpoint):
  """Query information on the counterparties that can quote this account's RFQs, this account's own desk code, and other RFQ configuration. Up to 50 requests per second."""

  async def rfq_config(self, *, validate: bool | None = None) -> RfqConfig:
    """Query information on the counterparties that can quote this account's RFQs, this account's own desk code, and other RFQ configuration. Up to 50 requests per second.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/rfq/trade/rfq-config)
    """
    return await self.request(
      None,
      method='GET',
      path='/v5/rfq/config',
      meta={'signed': True},
      validate=validate,
      response_type=RfqConfig,
    )
