# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class CancelAllQuotesResult(TypedDict):
  """The cancellation outcome for one quote."""

  rfqId: str
  """Inquiry ID."""
  quoteId: str
  """Quote ID."""
  quoteLinkId: str
  """Custom quote ID."""
  code: str
  """Cancellation result code; `"0"` means success."""
  msg: str
  """Cancellation failure reason; empty on success."""


Response = list[CancelAllQuotesResult]


class CancelAllQuotes(RpcEndpoint):
  """Cancel every active quote for this account. Up to 50 requests per second."""

  async def cancel_all_quotes(self, *, validate: bool | None = None) -> Response:
    """Cancel every active quote for this account. Up to 50 requests per second.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/rfq/trade/cancel-all-quotes)
    """
    return await self.request(
      None,
      method='POST',
      path='/v5/rfq/cancel-all-quotes',
      meta={'signed': True},
      validate=validate,
      response_type=Response,
    )
