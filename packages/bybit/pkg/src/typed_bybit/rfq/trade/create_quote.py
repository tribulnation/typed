# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class CreateQuoteResult(TypedDict):
  """The created quote."""

  rfqId: str
  """Inquiry ID."""
  quoteId: str
  """Quote ID."""
  quoteLinkId: str
  """Custom quote ID."""
  expiresAt: TimestampMillis
  """Expiration timestamp."""
  deskCode: str
  """Quoting party's identifier."""
  status: Literal['Active', 'Canceled', 'Filled', 'Expired', 'Failed']
  """Quote status."""


class QuoteCreateLegQuoteBuyListItem(TypedDict):
  """A bid-side price for one leg of the RFQ."""

  category: Literal['spot', 'linear', 'option']
  """Product type."""
  symbol: str
  """Contract name."""
  price: str
  """Quote or execution price."""


class QuoteCreateLegQuoteSellListItem(TypedDict):
  """An ask-side price for one leg of the RFQ."""

  category: Literal['spot', 'linear', 'option']
  """Product type."""
  symbol: str
  """Contract name."""
  price: str
  """Quote or execution price."""


class Request(TypedDict):
  """Quote parameters."""

  rfqId: str
  """Inquiry ID being quoted."""
  quoteLinkId: NotRequired[str]
  """Custom quote ID, 1-32 characters (letters/numbers). Expires after 3 months."""
  anonymous: NotRequired[bool]
  """Hide the quoter's identity from the inquirer."""
  expireIn: NotRequired[int]
  """Quote duration, in seconds."""
  quoteBuyList: NotRequired[list[QuoteCreateLegQuoteBuyListItem]]
  """Bid-side prices, one per leg of the RFQ being quoted."""
  quoteSellList: NotRequired[list[QuoteCreateLegQuoteSellListItem]]
  """Ask-side prices, one per leg of the RFQ being quoted."""


class CreateQuote(RpcEndpoint):
  """Send a quote in response to an inquirer's RFQ. Up to 50 requests per second."""

  async def create_quote(
    self,
    *,
    rfq_id: str,
    quote_link_id: str | None = None,
    anonymous: bool | None = None,
    expire_in: int | None = None,
    quote_buy_list: list[QuoteCreateLegQuoteBuyListItem] | None = None,
    quote_sell_list: list[QuoteCreateLegQuoteSellListItem] | None = None,
    validate: bool | None = None,
  ) -> CreateQuoteResult:
    """Send a quote in response to an inquirer's RFQ. Up to 50 requests per second.

    Args:
      rfq_id: Inquiry ID being quoted.
      quote_link_id: Custom quote ID, 1-32 characters (letters/numbers). Expires after 3 months.
      anonymous: Hide the quoter's identity from the inquirer.
      expire_in: Quote duration, in seconds.
      quote_buy_list: Bid-side prices, one per leg of the RFQ being quoted.
      quote_sell_list: Ask-side prices, one per leg of the RFQ being quoted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/rfq/trade/create-quote)
    """
    request: Request = Request(rfqId=rfq_id)
    if quote_link_id is not None:
      request['quoteLinkId'] = quote_link_id
    if anonymous is not None:
      request['anonymous'] = anonymous
    if expire_in is not None:
      request['expireIn'] = expire_in
    if quote_buy_list is not None:
      request['quoteBuyList'] = quote_buy_list
    if quote_sell_list is not None:
      request['quoteSellList'] = quote_sell_list
    return await self.request(
      request,
      method='POST',
      path='/v5/rfq/create-quote',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CreateQuoteResult,
    )
