# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  rfqId: NotRequired[str]
  """Filter to one inquiry ID."""
  rfqLinkId: NotRequired[str]
  """Filter to one custom inquiry ID. Only searches the last 3 months."""
  quoteId: NotRequired[str]
  """Filter to one quote ID."""
  quoteLinkId: NotRequired[str]
  """Filter to one custom quote ID. Only searches the last 3 months."""
  traderType: NotRequired[Literal['quote', 'request']]
  """Role to query as. Defaults to `quote`."""
  status: NotRequired[Literal['Filled', 'Failed']]
  """Filter by trade status."""
  limit: NotRequired[int]
  """Items per page. Range [1, 100]."""
  cursor: NotRequired[str]
  """Pagination cursor, from a previous response's own `cursor`."""


class RfqTradeLeg(TypedDict):
  """One executed leg."""

  category: Literal['spot', 'linear', 'option']
  """Product type."""
  orderId: str
  """Bybit order ID."""
  symbol: str
  """Instrument name."""
  side: Literal['Buy', 'Sell']
  """Order side."""
  price: str
  """Execution price."""
  qty: str
  """Trade quantity."""
  markPrice: str
  """Mark/index price at execution time."""
  execFee: str
  """Fee paid, in the base currency."""
  execId: str
  """Unique execution ID."""
  resultCode: int
  """Execution result code; `0` means success."""
  resultMessage: str
  """Execution error message; empty on success."""
  rejectParty: Literal['Taker', 'Maker', 'bybit']
  """Which side caused a rejection; empty when filled."""


class RfqTrade(TypedDict):
  """One RFQ transaction."""

  rfqId: str
  """Inquiry ID."""
  rfqLinkId: str
  """Custom inquiry ID."""
  quoteId: str
  """Quote ID."""
  quoteLinkId: str
  """Custom quote ID."""
  quoteSide: Literal['Buy', 'Sell']
  """Direction of the executed quote."""
  strategyType: str
  """Inquiry strategy label."""
  status: Literal['Filled', 'Failed']
  """Transaction status."""
  rfqDeskCode: str
  """Inquiring party's identifier."""
  quoteDeskCode: str
  """Quoting party's identifier."""
  createdAt: TimestampMillis
  """Trade creation timestamp."""
  updatedAt: TimestampMillis
  """Trade update timestamp."""
  legs: list[RfqTradeLeg]
  """Executed legs."""


class RfqTradeListResult(TypedDict):
  """A page of RFQ transactions."""

  cursor: str
  """Pagination cursor for the next page; empty when there are no more pages."""
  list: list[RfqTrade]
  """Transaction records."""


class TradeList(RpcEndpoint):
  """Obtain this account's executed (or failed) RFQ transactions, sorted by creation time descending. Up to 50 requests per second."""

  def trade_list_paged(
    self,
    *,
    rfq_id: str | None = None,
    rfq_link_id: str | None = None,
    quote_id: str | None = None,
    quote_link_id: str | None = None,
    trader_type: Literal['quote', 'request'] | None = None,
    status: Literal['Filled', 'Failed'] | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[RfqTrade, str]:
    """Obtain this account's executed (or failed) RFQ transactions, sorted by creation time descending. Up to 50 requests per second.

    Paged variant of `trade_list`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      rfq_id: Filter to one inquiry ID.
      rfq_link_id: Filter to one custom inquiry ID. Only searches the last 3 months.
      quote_id: Filter to one quote ID.
      quote_link_id: Filter to one custom quote ID. Only searches the last 3 months.
      trader_type: Role to query as. Defaults to `quote`.
      status: Filter by trade status.
      limit: Items per page. Range [1, 100].
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/rfq/trade/trade-list)
    """

    async def next(cursor: str) -> tuple[list[RfqTrade], str | None]:
      response = await self.trade_list(
        rfq_id=rfq_id,
        rfq_link_id=rfq_link_id,
        quote_id=quote_id,
        quote_link_id=quote_link_id,
        trader_type=trader_type,
        status=status,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('list')
      state = response.get('cursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def trade_list(
    self,
    *,
    rfq_id: str | None = None,
    rfq_link_id: str | None = None,
    quote_id: str | None = None,
    quote_link_id: str | None = None,
    trader_type: Literal['quote', 'request'] | None = None,
    status: Literal['Filled', 'Failed'] | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> RfqTradeListResult:
    """Obtain this account's executed (or failed) RFQ transactions, sorted by creation time descending. Up to 50 requests per second.

    Args:
      rfq_id: Filter to one inquiry ID.
      rfq_link_id: Filter to one custom inquiry ID. Only searches the last 3 months.
      quote_id: Filter to one quote ID.
      quote_link_id: Filter to one custom quote ID. Only searches the last 3 months.
      trader_type: Role to query as. Defaults to `quote`.
      status: Filter by trade status.
      limit: Items per page. Range [1, 100].
      cursor: Pagination cursor, from a previous response's own `cursor`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/rfq/trade/trade-list)
    """
    request: Request = Request()
    if rfq_id is not None:
      request['rfqId'] = rfq_id
    if rfq_link_id is not None:
      request['rfqLinkId'] = rfq_link_id
    if quote_id is not None:
      request['quoteId'] = quote_id
    if quote_link_id is not None:
      request['quoteLinkId'] = quote_link_id
    if trader_type is not None:
      request['traderType'] = trader_type
    if status is not None:
      request['status'] = status
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/rfq/trade-list',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=RfqTradeListResult,
    )
