# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class CancelRfqResult(TypedDict):
  """Identifiers of the cancelled RFQ."""

  rfqId: str
  """Inquiry ID."""
  rfqLinkId: str
  """Custom inquiry ID."""


class Request(TypedDict):
  """Identifies the RFQ to cancel."""

  rfqId: NotRequired[str]
  """Inquiry ID."""
  rfqLinkId: NotRequired[str]
  """Custom inquiry ID."""


class CancelRfq(RpcEndpoint):
  """Cancel an RFQ by inquiry ID or custom inquiry ID. Up to 50 requests per second."""

  async def cancel_rfq(
    self,
    *,
    rfq_id: str | None = None,
    rfq_link_id: str | None = None,
    validate: bool | None = None,
  ) -> CancelRfqResult:
    """Cancel an RFQ by inquiry ID or custom inquiry ID. Up to 50 requests per second.

    Args:
      rfq_id: Inquiry ID.
      rfq_link_id: Custom inquiry ID.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/rfq/trade/cancel-rfq)
    """
    request: Request = Request()
    if rfq_id is not None:
      request['rfqId'] = rfq_id
    if rfq_link_id is not None:
      request['rfqLinkId'] = rfq_link_id
    return await self.request(
      request,
      method='POST',
      path='/v5/rfq/cancel-rfq',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CancelRfqResult,
    )
