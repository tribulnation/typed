# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class CancelAllRfqResult(TypedDict):
  """The cancellation outcome for one RFQ."""

  rfqId: str
  """Inquiry ID."""
  rfqLinkId: str
  """Custom inquiry ID."""
  code: str
  """Cancellation result code; `"0"` means success."""
  msg: str
  """Cancellation failure reason; empty on success."""


Response = list[CancelAllRfqResult]


class CancelAllRfq(RpcEndpoint):
  """Cancel every active RFQ for this account. Up to 50 requests per second."""

  async def cancel_all_rfq(self, *, validate: bool | None = None) -> Response:
    """Cancel every active RFQ for this account. Up to 50 requests per second.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/rfq/trade/cancel-all-rfq)
    """
    return await self.request(
      None,
      method='POST',
      path='/v5/rfq/cancel-all-rfq',
      meta={'signed': True},
      validate=validate,
      response_type=Response,
    )
