# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class Position(TypedDict):
  """One position."""

  positionIdx: Literal[0, 1, 2]
  """Position index, used to identify positions in different position modes."""
  riskId: int
  """Risk tier ID. `0` under portfolio margin mode, where risk-limit rules are invalid."""
  riskLimitValue: str
  """Risk limit value; meaningless once an auto risk-limit tier applies. `0` under portfolio margin mode."""
  symbol: str
  """Symbol name."""
  side: Literal['Buy', 'Sell', '']
  """Position side. `Buy`: long, `Sell`: short. An empty string means an empty position, confirmed live on a flat (zero-size) position."""
  size: str
  """Position size, always positive."""
  avgPrice: Literal[''] | Decimal
  """Average entry price. For USDC Perp & Futures this does not change with 8-hour session settlement. Empty string confirmed live on a flat (zero-size) position."""
  positionValue: Literal[''] | Decimal
  """Position value. Empty string confirmed live on a flat (zero-size) position."""
  autoAddMargin: Literal[0, 1]
  """Whether margin is added automatically under isolated margin mode."""
  positionStatus: Literal[''] | Literal['Normal', 'Liq', 'Adl']
  """Position status. Empty string confirmed live 2026-09-04 on a flat (zero-size) BTCUSDT linear position, region='global'."""
  leverage: str
  """Position leverage. Empty string under portfolio margin mode, where leverage rules are invalid."""
  breakEvenPrice: NotRequired[str]
  """Break-even price. Linear and inverse only."""
  markPrice: Literal[''] | Decimal
  """Mark price. Empty string confirmed live on a flat (zero-size) position."""
  liqPrice: Literal[''] | Decimal
  """Position liquidation price. Empty when it falls outside the tradable price range, or under portfolio margin mode, where no liquidation price is provided. Empty string also confirmed live on a flat (zero-size) position."""
  positionIM: Literal[''] | Decimal
  """Initial margin. Empty string under portfolio margin mode. Empty string also confirmed live on a flat (zero-size) position."""
  positionIMByMp: NotRequired[str]
  """Initial margin calculated by mark price. Empty string under portfolio margin mode."""
  positionMM: Literal[''] | Decimal
  """Maintenance margin. Empty string under portfolio margin mode. Empty string also confirmed live on a flat (zero-size) position."""
  positionMMByMp: NotRequired[str]
  """Maintenance margin calculated by mark price. Empty string under portfolio margin mode."""
  takeProfit: Literal[''] | Decimal
  """Take profit price. Empty string confirmed live on a flat (zero-size) position."""
  stopLoss: Literal[''] | Decimal
  """Stop loss price. Empty string confirmed live on a flat (zero-size) position."""
  trailingStop: Literal[''] | Decimal
  """Trailing stop, as the distance from market price. Empty string confirmed live on a flat (zero-size) position."""
  sessionAvgPrice: NotRequired[Literal[''] | Decimal]
  """USDC contract session average price; the same figure shown as average entry price in the web UI. Empty string confirmed live on a flat (zero-size) position."""
  delta: NotRequired[str]
  """Option delta."""
  gamma: NotRequired[str]
  """Option gamma."""
  vega: NotRequired[str]
  """Option vega."""
  theta: NotRequired[str]
  """Option theta."""
  unrealisedPnl: Literal[''] | Decimal
  """Unrealised PnL. Empty string confirmed live on a flat (zero-size) position."""
  curRealisedPnl: NotRequired[Literal[''] | Decimal]
  """Realised PnL for the current holding position. Empty string confirmed live on a flat (zero-size) position."""
  cumRealisedPnl: Literal[''] | Decimal
  """Cumulative realised PnL. Futures & perps: all-time cumulative. Option: always empty, meaningless. Empty string also confirmed live on a flat (zero-size) linear position."""
  adlRankIndicator: int
  """Auto-deleverage rank indicator."""
  createdTime: Literal[''] | TimestampMillis
  """Timestamp of the first time a position was created on this symbol, in milliseconds. Empty string confirmed live 2026-09-04 on a flat (zero-size) BTCUSDT linear position, region='global'."""
  updatedTime: Literal[''] | TimestampMillis
  """Position last-updated timestamp, in milliseconds. Empty string confirmed live on a flat (zero-size) position."""
  openTime: NotRequired[TimestampMillis]
  """Position open timestamp, in milliseconds. `0` by default."""
  seq: int
  """Cross sequence, used to associate each fill and position update. Different symbols may share a `seq`; combine with `symbol` for uniqueness. `-1` if the symbol has never traded."""
  isReduceOnly: bool
  """Whether the position is marked reduce-only, set by Bybit when systematically lowering a risk limit. Meaningful only for isolated & cross margin of USDT/USDC Perp, USDC Futures, Inverse Perp and Inverse Futures."""
  mmrSysUpdatedTime: NotRequired[Literal[''] | TimestampMillis]
  """Timestamp (ms) of the system-driven MMR adjustment tied to `isReduceOnly`; empty by default. Meaningful only for the same product set as `isReduceOnly`. Empty string confirmed live on a flat (zero-size) position."""
  leverageSysUpdatedTime: NotRequired[Literal[''] | TimestampMillis]
  """Timestamp (ms) of the system-driven leverage adjustment tied to `isReduceOnly`; empty by default. Meaningful only for the same product set as `isReduceOnly`. Empty string confirmed live on a flat (zero-size) position."""
  tpslMode: NotRequired[Literal[''] | Literal['Full']]
  """Deprecated. Documented as always `Full`, but confirmed live to also return an empty string on a flat (zero-size) position -- the 'always `Full`' claim only holds for a position that actually carries a TP/SL mode."""
  bustPrice: NotRequired[Literal['']]
  """Deprecated; always an empty string. Confirmed live on a flat (zero-size) position."""
  positionBalance: NotRequired[Literal[''] | Decimal]
  """Deprecated; refer to `positionIM`/`positionIMByMp` instead. Empty string confirmed live on a flat (zero-size) position."""
  tradeMode: NotRequired[Literal[0]]
  """Deprecated; always `0`. Use Get Account Info for the real margin mode."""


class Request(TypedDict):
  category: Literal['linear', 'inverse', 'option']
  """Product type."""
  symbol: NotRequired[str]
  """Symbol name, uppercase only. If passed, returns data regardless of whether a position exists. If omitted and `settleCoin` is given, returns only positions with a size greater than zero."""
  baseCoin: NotRequired[str]
  """Base coin, uppercase only. Option only; returns every option position if omitted."""
  settleCoin: NotRequired[str]
  """Settle coin. Linear: one of `symbol`/`settleCoin` is required; `symbol` takes priority."""
  limit: NotRequired[int]
  """Data size per page. Range [1, 200]; defaults to 20."""
  cursor: NotRequired[str]
  """Opaque pagination cursor. Pass the `nextPageCursor` returned by the previous page; omit it for the first page."""


class PositionListResult(TypedDict):
  """Page of matching positions."""

  category: Literal['linear', 'inverse', 'option']
  """Product type."""
  nextPageCursor: NotRequired[str]
  """Opaque cursor for the next page; refer to the `cursor` request parameter."""
  list: list[Position]
  """Matching positions."""


class List(RpcEndpoint):
  """Query real-time position data: position size, cumulative realized PnL, and related risk fields, for one symbol or a whole product category."""

  def list_paged(
    self,
    category: Literal['linear', 'inverse', 'option'],
    *,
    symbol: str | None = None,
    base_coin: str | None = None,
    settle_coin: str | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[Position, str]:
    """Query real-time position data: position size, cumulative realized PnL, and related risk fields, for one symbol or a whole product category.

    Paged variant of `list`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      category: Product type.
      symbol: Symbol name, uppercase only. If passed, returns data regardless of whether a position exists. If omitted and `settleCoin` is given, returns only positions with a size greater than zero.
      base_coin: Base coin, uppercase only. Option only; returns every option position if omitted.
      settle_coin: Settle coin. Linear: one of `symbol`/`settleCoin` is required; `symbol` takes priority.
      limit: Data size per page. Range [1, 200]; defaults to 20.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/position)
    """

    async def next(cursor: str) -> tuple[list[Position], str | None]:
      response = await self.list(
        category,
        symbol=symbol,
        base_coin=base_coin,
        settle_coin=settle_coin,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('list')
      state = response.get('nextPageCursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def list(
    self,
    category: Literal['linear', 'inverse', 'option'],
    *,
    symbol: str | None = None,
    base_coin: str | None = None,
    settle_coin: str | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> PositionListResult:
    """Query real-time position data: position size, cumulative realized PnL, and related risk fields, for one symbol or a whole product category.

    Args:
      category: Product type.
      symbol: Symbol name, uppercase only. If passed, returns data regardless of whether a position exists. If omitted and `settleCoin` is given, returns only positions with a size greater than zero.
      base_coin: Base coin, uppercase only. Option only; returns every option position if omitted.
      settle_coin: Settle coin. Linear: one of `symbol`/`settleCoin` is required; `symbol` takes priority.
      limit: Data size per page. Range [1, 200]; defaults to 20.
      cursor: Opaque pagination cursor. Pass the `nextPageCursor` returned by the previous page; omit it for the first page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/position)
    """
    request: Request = Request(category=category)
    if symbol is not None:
      request['symbol'] = symbol
    if base_coin is not None:
      request['baseCoin'] = base_coin
    if settle_coin is not None:
      request['settleCoin'] = settle_coin
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/position/list',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=PositionListResult,
    )
