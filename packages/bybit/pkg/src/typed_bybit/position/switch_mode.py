# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  """Position mode parameters."""

  category: Literal['linear']
  """Product type."""
  symbol: NotRequired[str]
  """Symbol name, uppercase only. One of `symbol`/`coin` is required; `symbol` takes priority."""
  coin: NotRequired[str]
  """Settle coin, uppercase only. One of `symbol`/`coin` is required."""
  mode: Literal[0, 3]
  """Position mode."""


class SwitchModeResult(TypedDict):
  """Empty on success."""


class SwitchMode(RpcEndpoint):
  """Switch between one-way and hedge position mode for USDT perpetual and inverse futures. One-way mode allows only one open position per symbol; hedge mode allows simultaneous Buy- and Sell-side positions."""

  async def switch_mode(
    self,
    category: Literal['linear'] = 'linear',
    *,
    symbol: str | None = None,
    coin: str | None = None,
    mode: Literal[0, 3],
    validate: bool | None = None,
  ) -> SwitchModeResult:
    """Switch between one-way and hedge position mode for USDT perpetual and inverse futures. One-way mode allows only one open position per symbol; hedge mode allows simultaneous Buy- and Sell-side positions.

    Args:
      category: Product type.
      symbol: Symbol name, uppercase only. One of `symbol`/`coin` is required; `symbol` takes priority.
      coin: Settle coin, uppercase only. One of `symbol`/`coin` is required.
      mode: Position mode.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/position/position-mode)
    """
    request: Request = Request(category=category, mode=mode)
    if symbol is not None:
      request['symbol'] = symbol
    if coin is not None:
      request['coin'] = coin
    return await self.request(
      request,
      method='POST',
      path='/v5/position/switch-mode',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SwitchModeResult,
    )
