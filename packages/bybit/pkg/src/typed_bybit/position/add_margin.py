# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class AddMarginResult(TypedDict):
  """Position state after the margin adjustment."""

  category: Literal['linear', 'inverse']
  """Product type."""
  symbol: str
  """Symbol name."""
  positionIdx: Literal[0, 1, 2]
  """Position index, identifying positions in different position modes."""
  riskId: int
  """Risk limit ID."""
  riskLimitValue: str
  """Risk limit value."""
  size: str
  """Position size."""
  avgPrice: str
  """Average entry price."""
  liqPrice: str
  """Liquidation price."""
  bustPrice: str
  """Bankruptcy price."""
  markPrice: str
  """Latest mark price."""
  positionValue: str
  """Position value."""
  leverage: str
  """Position leverage."""
  autoAddMargin: Literal[0, 1]
  """Whether margin is added automatically."""
  positionStatus: Literal['Normal', 'Liq', 'Adl']
  """Position status."""
  positionIM: str
  """Initial margin."""
  positionMM: str
  """Maintenance margin."""
  takeProfit: str
  """Take profit price."""
  stopLoss: str
  """Stop loss price."""
  trailingStop: str
  """Trailing stop, as the distance from market price."""
  unrealisedPnl: str
  """Unrealised PnL."""
  cumRealisedPnl: str
  """Cumulative realised PnL."""
  createdTime: TimestampMillis
  """Timestamp of the first time a position was created on this symbol, in milliseconds."""
  updatedTime: TimestampMillis
  """Position last-updated timestamp, in milliseconds."""


class Request(TypedDict):
  """Margin adjustment."""

  category: Literal['linear', 'inverse']
  """Product type."""
  symbol: str
  """Symbol name, uppercase only."""
  margin: str
  """Signed margin delta, up to 4 decimals. Positive adds margin (e.g. `10`); negative reduces it (e.g. `-10`)."""
  positionIdx: NotRequired[Literal[0, 1, 2]]
  """Position index, identifying positions in different position modes. Required for a hedge-mode position."""


class AddMargin(RpcEndpoint):
  """Manually add or reduce margin for an isolated-margin position."""

  async def add_margin(
    self,
    category: Literal['linear', 'inverse'],
    *,
    symbol: str,
    margin: str,
    position_idx: Literal[0, 1, 2] | None = None,
    validate: bool | None = None,
  ) -> AddMarginResult:
    """Manually add or reduce margin for an isolated-margin position.

    Args:
      category: Product type.
      symbol: Symbol name, uppercase only.
      margin: Signed margin delta, up to 4 decimals. Positive adds margin (e.g. `10`); negative reduces it (e.g. `-10`).
      position_idx: Position index, identifying positions in different position modes. Required for a hedge-mode position.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/position/manual-add-margin)
    """
    request: Request = Request(category=category, symbol=symbol, margin=margin)
    if position_idx is not None:
      request['positionIdx'] = position_idx
    return await self.request(
      request,
      method='POST',
      path='/v5/position/add-margin',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=AddMarginResult,
    )
