# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class AutoAddMarginResult(TypedDict):
  """Empty on success."""


class Request(TypedDict):
  """Auto-add-margin toggle."""

  category: Literal['linear']
  """Product type."""
  symbol: str
  """Symbol name, uppercase only."""
  autoAddMargin: Literal[0, 1]
  """Turn auto-add-margin on or off."""
  positionIdx: NotRequired[Literal[0, 1, 2]]
  """Position index, identifying positions in different position modes. Required for a hedge-mode position."""


class AutoAddMargin(RpcEndpoint):
  """Turn automatic margin top-up on or off for an isolated-margin position."""

  async def auto_add_margin(
    self,
    category: Literal['linear'] = 'linear',
    *,
    symbol: str,
    auto_add_margin: Literal[0, 1],
    position_idx: Literal[0, 1, 2] | None = None,
    validate: bool | None = None,
  ) -> AutoAddMarginResult:
    """Turn automatic margin top-up on or off for an isolated-margin position.

    Args:
      category: Product type.
      symbol: Symbol name, uppercase only.
      auto_add_margin: Turn auto-add-margin on or off.
      position_idx: Position index, identifying positions in different position modes. Required for a hedge-mode position.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/position/auto-add-margin)
    """
    request: Request = Request(
      category=category, symbol=symbol, autoAddMargin=auto_add_margin
    )
    if position_idx is not None:
      request['positionIdx'] = position_idx
    return await self.request(
      request,
      method='POST',
      path='/v5/position/set-auto-add-margin',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=AutoAddMarginResult,
    )
