# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class ClosedOptionsPosition(TypedDict):
  """One closed option position."""

  symbol: str
  """Symbol name."""
  side: Literal['Buy', 'Sell']
  """Position side."""
  totalOpenFee: str
  """Total open fee."""
  deliveryFee: str
  """Delivery fee."""
  totalCloseFee: str
  """Total close fee."""
  qty: str
  """Order quantity."""
  closeTime: TimestampMillis
  """Closed time, in milliseconds."""
  avgExitPrice: str
  """Average exit price."""
  deliveryPrice: str
  """Delivery price."""
  openTime: TimestampMillis
  """Opened time, in milliseconds."""
  avgEntryPrice: str
  """Average entry price."""
  totalPnl: str
  """Total PnL."""


class Request(TypedDict):
  category: Literal['option']
  """Product type."""
  symbol: NotRequired[str]
  """Symbol name."""
  startTime: NotRequired[TimestampMillis]
  """Start timestamp, in milliseconds."""
  endTime: NotRequired[TimestampMillis]
  """End timestamp, in milliseconds."""
  limit: NotRequired[int]
  """Data size per page. Range [1, 100]; defaults to 50."""
  cursor: NotRequired[str]
  """Opaque pagination cursor. Pass the `nextPageCursor` returned by the previous page; omit it for the first page."""


class ClosedOptionsPositionsResult(TypedDict):
  """Page of closed option positions."""

  category: Literal['option']
  """Product type."""
  nextPageCursor: NotRequired[str]
  """Opaque cursor for the next page; refer to the `cursor` request parameter."""
  list: list[ClosedOptionsPosition]
  """Closed option positions."""


class ClosedOptionsPositions(RpcEndpoint):
  """Query the user's closed option positions, covering the last 6 months, sorted by close time descending."""

  def closed_options_positions_paged(
    self,
    category: Literal['option'] = 'option',
    *,
    symbol: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[ClosedOptionsPosition, str]:
    """Query the user's closed option positions, covering the last 6 months, sorted by close time descending.

    Paged variant of `closed_options_positions`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      category: Product type.
      symbol: Symbol name.
      start_time: Start timestamp, in milliseconds.
      end_time: End timestamp, in milliseconds.
      limit: Data size per page. Range [1, 100]; defaults to 50.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/position/close-position)
    """

    async def next(cursor: str) -> tuple[list[ClosedOptionsPosition], str | None]:
      response = await self.closed_options_positions(
        category,
        symbol=symbol,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('list')
      state = response.get('nextPageCursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def closed_options_positions(
    self,
    category: Literal['option'] = 'option',
    *,
    symbol: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> ClosedOptionsPositionsResult:
    """Query the user's closed option positions, covering the last 6 months, sorted by close time descending.

    Args:
      category: Product type.
      symbol: Symbol name.
      start_time: Start timestamp, in milliseconds.
      end_time: End timestamp, in milliseconds.
      limit: Data size per page. Range [1, 100]; defaults to 50.
      cursor: Opaque pagination cursor. Pass the `nextPageCursor` returned by the previous page; omit it for the first page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/position/close-position)
    """
    request: Request = Request(category=category)
    if symbol is not None:
      request['symbol'] = symbol
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/position/get-closed-positions',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ClosedOptionsPositionsResult,
    )
