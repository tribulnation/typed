# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class ClosedPnl(TypedDict):
  """One closed PnL record."""

  symbol: str
  """Symbol name."""
  orderId: str
  """Order ID."""
  side: Literal['Buy', 'Sell']
  """Order side."""
  qty: str
  """Order quantity."""
  orderPrice: str
  """Order price."""
  orderType: Literal['Market', 'Limit']
  """Order type."""
  execType: Literal[
    'Trade',
    'BustTrade',
    'SessionSettlePnL',
    'Settle',
    'MovePosition',
    'ForwardSplitSettle',
    'ReverseSplitSettle',
  ]
  """Execution type that produced this record."""
  closedSize: str
  """Closed size."""
  cumEntryValue: str
  """Cumulative entry position value."""
  avgEntryPrice: str
  """Average entry price."""
  cumExitValue: str
  """Cumulative exit position value."""
  avgExitPrice: str
  """Average exit price."""
  closedPnl: str
  """Closed PnL."""
  fillCount: str
  """Number of fills in this order."""
  leverage: str
  """Leverage."""
  openFee: str
  """Open-position trading fee."""
  closeFee: str
  """Close-position trading fee."""
  createdTime: TimestampMillis
  """Created time, in milliseconds."""
  updatedTime: TimestampMillis
  """Updated time, in milliseconds."""


class Request(TypedDict):
  category: Literal['linear', 'inverse']
  """Product type."""
  symbol: NotRequired[str]
  """Symbol name, uppercase only."""
  startTime: NotRequired[TimestampMillis]
  """Start timestamp, in milliseconds."""
  endTime: NotRequired[TimestampMillis]
  """End timestamp, in milliseconds."""
  limit: NotRequired[int]
  """Data size per page. Range [1, 100]; defaults to 50."""
  cursor: NotRequired[str]
  """Opaque pagination cursor. Pass the `nextPageCursor` returned by the previous page; omit it for the first page."""


class ClosedPnlResult(TypedDict):
  """Page of closed PnL records."""

  category: Literal['linear', 'inverse']
  """Product type."""
  nextPageCursor: NotRequired[str]
  """Opaque cursor for the next page; refer to the `cursor` request parameter."""
  list: list[ClosedPnl]
  """Closed PnL records."""


class ClosedPnlEndpoint(RpcEndpoint):
  """Query the user's closed profit-and-loss records, covering the last 2 years."""

  def closed_pnl_paged(
    self,
    category: Literal['linear', 'inverse'],
    *,
    symbol: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[ClosedPnl, str]:
    """Query the user's closed profit-and-loss records, covering the last 2 years.

    Paged variant of `closed_pnl`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      category: Product type.
      symbol: Symbol name, uppercase only.
      start_time: Start timestamp, in milliseconds.
      end_time: End timestamp, in milliseconds.
      limit: Data size per page. Range [1, 100]; defaults to 50.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/position/close-pnl)
    """

    async def next(cursor: str) -> tuple[list[ClosedPnl], str | None]:
      response = await self.closed_pnl(
        category,
        symbol=symbol,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('list')
      state = response.get('nextPageCursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def closed_pnl(
    self,
    category: Literal['linear', 'inverse'],
    *,
    symbol: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> ClosedPnlResult:
    """Query the user's closed profit-and-loss records, covering the last 2 years.

    Args:
      category: Product type.
      symbol: Symbol name, uppercase only.
      start_time: Start timestamp, in milliseconds.
      end_time: End timestamp, in milliseconds.
      limit: Data size per page. Range [1, 100]; defaults to 50.
      cursor: Opaque pagination cursor. Pass the `nextPageCursor` returned by the previous page; omit it for the first page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/position/close-pnl)
    """
    request: Request = Request(category=category)
    if symbol is not None:
      request['symbol'] = symbol
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/position/closed-pnl',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ClosedPnlResult,
    )
