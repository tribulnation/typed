# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  """Leverage parameters."""

  category: Literal['linear', 'inverse']
  """Product type."""
  symbol: str
  """Symbol name, uppercase only."""
  buyLeverage: str
  """Buy-side leverage. Range [1, max leverage for the symbol]."""
  sellLeverage: str
  """Sell-side leverage. Range [1, max leverage for the symbol]."""


class SetLeverageResult(TypedDict):
  """Empty on success."""


class SetLeverage(RpcEndpoint):
  """Set the leverage for a symbol. Per the risk limit, higher leverage allows a smaller maximum position value and vice versa."""

  async def set_leverage(
    self,
    category: Literal['linear', 'inverse'],
    *,
    symbol: str,
    buy_leverage: str,
    sell_leverage: str,
    validate: bool | None = None,
  ) -> SetLeverageResult:
    """Set the leverage for a symbol. Per the risk limit, higher leverage allows a smaller maximum position value and vice versa.

    Args:
      category: Product type.
      symbol: Symbol name, uppercase only.
      buy_leverage: Buy-side leverage. Range [1, max leverage for the symbol].
      sell_leverage: Sell-side leverage. Range [1, max leverage for the symbol].
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/position/leverage)
    """
    return await self.request(
      Request(
        category=category,
        symbol=symbol,
        buyLeverage=buy_leverage,
        sellLeverage=sell_leverage,
      ),
      method='POST',
      path='/v5/position/set-leverage',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SetLeverageResult,
    )
