# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class ConfirmMmrResult(TypedDict):
  """Empty on success."""


class Request(TypedDict):
  """Position to confirm."""

  category: Literal['linear', 'inverse']
  """Product type."""
  symbol: str
  """Symbol name."""


class ConfirmMmr(RpcEndpoint):
  """Recalculate a position's risk level after the caller has adjusted it, and clear the position's isReduceOnly mark on success."""

  async def confirm_mmr(
    self,
    category: Literal['linear', 'inverse'],
    *,
    symbol: str,
    validate: bool | None = None,
  ) -> ConfirmMmrResult:
    """Recalculate a position's risk level after the caller has adjusted it, and clear the position's isReduceOnly mark on success.

    Args:
      category: Product type.
      symbol: Symbol name.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/position/confirm-mmr)
    """
    return await self.request(
      Request(category=category, symbol=symbol),
      method='POST',
      path='/v5/position/confirm-pending-mmr',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ConfirmMmrResult,
    )
