# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class BrokerVoucherSpec(TypedDict):
  """A voucher's specification."""

  id: str
  """Voucher ID."""
  coin: str
  """Voucher coin."""
  amountUnit: Literal['AWARD_AMOUNT_UNIT_USD', 'AWARD_AMOUNT_UNIT_COIN']
  """Unit `totalAmount`/`usedAmount` are denominated in."""
  productLine: str
  """Product line the voucher applies to."""
  subProductLine: str
  """Product sub-line the voucher applies to."""
  totalAmount: str
  """Total voucher value."""
  usedAmount: str
  """Amount of the voucher already redeemed."""


class Request(TypedDict):
  """Voucher to look up."""

  id: str
  """Voucher ID."""


class Voucher(RpcEndpoint):
  """Get specification details for a voucher: coin, amount unit, product line and total/used amount."""

  async def voucher(
    self, id: str, *, validate: bool | None = None
  ) -> BrokerVoucherSpec:
    """Get specification details for a voucher: coin, amount unit, product line and total/used amount.

    Args:
      id: Voucher ID.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/broker/reward/voucher)
    """
    return await self.request(
      Request(id=id),
      method='POST',
      path='/v5/broker/award/info',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BrokerVoucherSpec,
    )
