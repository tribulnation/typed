# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  """Voucher to issue."""

  accountId: str
  """User ID to receive the voucher."""
  awardId: str
  """Voucher spec ID to issue."""
  specCode: str
  """Caller-chosen unique spec code, up to 8 characters."""
  amount: str
  """Amount to issue. Up to 16 decimals for spot airdrops, up to 4 decimals for other award types."""
  brokerId: str
  """This broker's unique identifier."""


class IssueVoucher(RpcEndpoint):
  """Issue a voucher to a user account. `amount` supports up to 16 decimals for spot airdrops, up to 4 decimals for other award types."""

  async def issue_voucher(
    self,
    *,
    account_id: str,
    award_id: str,
    spec_code: str,
    amount: str,
    broker_id: str,
    validate: bool | None = None,
  ) -> None:
    """Issue a voucher to a user account. `amount` supports up to 16 decimals for spot airdrops, up to 4 decimals for other award types.

    Args:
      account_id: User ID to receive the voucher.
      award_id: Voucher spec ID to issue.
      spec_code: Caller-chosen unique spec code, up to 8 characters.
      amount: Amount to issue. Up to 16 decimals for spot airdrops, up to 4 decimals for other award types.
      broker_id: This broker's unique identifier.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/broker/reward/issue-voucher)
    """
    return await self.request(
      Request(
        accountId=account_id,
        awardId=award_id,
        specCode=spec_code,
        amount=amount,
        brokerId=broker_id,
      ),
      method='POST',
      path='/v5/broker/award/distribute-award',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=None,
    )
