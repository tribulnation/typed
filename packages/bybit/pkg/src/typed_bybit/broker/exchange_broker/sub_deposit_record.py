# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  id: NotRequired[str]
  """Internal deposit record ID."""
  txID: NotRequired[str]
  """Transaction ID."""
  subMemberId: NotRequired[str]
  """Sub-account UID."""
  coin: NotRequired[str]
  """Coin."""
  startTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  limit: NotRequired[int]
  """Page size."""
  cursor: NotRequired[str]
  """Pagination cursor."""


class SubDepositRecord(TypedDict):
  """One sub-account deposit record."""

  id: str
  """Unique internal deposit record ID."""
  subMemberId: str
  """Sub-account UID."""
  coin: str
  """Coin deposited."""
  chain: str
  """Blockchain network."""
  amount: str
  """Deposit amount."""
  txID: str
  """Transaction ID."""
  status: Literal[0, 1, 2, 3, 4, 7, 10011, 10012, 70011, 70012, 70013]
  """Deposit status."""
  toAddress: str
  """Destination deposit address."""
  tag: str
  """Destination address tag, when the chain uses one."""
  depositFee: str
  """Fee charged on the deposit."""
  successAt: TimestampMillis
  """Deposit completion timestamp, as a millisecond timestamp."""
  confirmations: str
  """Number of block confirmations observed."""
  txIndex: str
  """Transaction index within its block."""
  blockHash: str
  """Hash of the block the deposit transaction was included in."""
  batchReleaseLimit: str
  """Daily deposit release limit remaining; `"-1"` means unlimited."""
  depositType: Literal['0', '10', '20', '50']
  """Deposit classification."""
  fromAddress: NotRequired[str]
  """Source address, on-chain deposits only."""
  taxDepositRecordsId: NotRequired[str]
  """Tax reporting record ID, EU users only."""
  taxStatus: NotRequired[Literal[0, 1, 2]]
  """Tax reporting status, EU users only."""
  travelRuleStatus: NotRequired[Literal[0, 1, 2, 3]]
  """Travel Rule review status."""


class SubDepositRecordResult(TypedDict):
  """Page of sub-account deposit records."""

  rows: list[SubDepositRecord]
  """Deposit records, newest first."""
  nextPageCursor: str
  """Opaque cursor for the next page. Pass it back as `cursor`; an empty string means there are no further pages."""


class SubDepositRecordEndpoint(RpcEndpoint):
  """Get deposit records across an Exchange Broker's sub-accounts, sorted by deposit creation time in descending order. Queried with the main UID's own credentials; no per-sub-account credential is needed."""

  def sub_deposit_record_paged(
    self,
    *,
    id: str | None = None,
    tx_id: str | None = None,
    sub_member_id: str | None = None,
    coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[SubDepositRecord, str]:
    """Get deposit records across an Exchange Broker's sub-accounts, sorted by deposit creation time in descending order. Queried with the main UID's own credentials; no per-sub-account credential is needed.

    Paged variant of `sub_deposit_record`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      id: Internal deposit record ID.
      tx_id: Transaction ID.
      sub_member_id: Sub-account UID.
      coin: Coin.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/broker/exchange-broker/sub-deposit-record)
    """

    async def next(cursor: str) -> tuple[list[SubDepositRecord], str | None]:
      response = await self.sub_deposit_record(
        id=id,
        tx_id=tx_id,
        sub_member_id=sub_member_id,
        coin=coin,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('rows')
      state = response.get('nextPageCursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def sub_deposit_record(
    self,
    *,
    id: str | None = None,
    tx_id: str | None = None,
    sub_member_id: str | None = None,
    coin: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> SubDepositRecordResult:
    """Get deposit records across an Exchange Broker's sub-accounts, sorted by deposit creation time in descending order. Queried with the main UID's own credentials; no per-sub-account credential is needed.

    Args:
      id: Internal deposit record ID.
      tx_id: Transaction ID.
      sub_member_id: Sub-account UID.
      coin: Coin.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      limit: Page size.
      cursor: Pagination cursor.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/broker/exchange-broker/sub-deposit-record)
    """
    request: Request = Request()
    if id is not None:
      request['id'] = id
    if tx_id is not None:
      request['txID'] = tx_id
    if sub_member_id is not None:
      request['subMemberId'] = sub_member_id
    if coin is not None:
      request['coin'] = coin
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/broker/asset/query-sub-member-deposit-record',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SubDepositRecordResult,
    )
