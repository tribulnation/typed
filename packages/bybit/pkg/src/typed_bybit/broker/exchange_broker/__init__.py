# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .account_info import AccountInfo
from .exchange_earning import ExchangeEarning
from .rate_limit import RateLimit
from .sub_deposit_record import SubDepositRecordEndpoint


class ExchangeBroker(AccountInfo, ExchangeEarning, SubDepositRecordEndpoint):
  """Exchange Broker — endpoints for an Exchange Broker entity to manage its master and sub-accounts: query broker/account info, exchange earnings, sub-account deposit records, and the entity's own UID-level API rate limits (query, set, and aggregate-usage check).

  References:
    - [Upstream docs](https://bybit-exchange.github.io/docs/v5/broker/exchange-broker/account-info)
  """

  @cached_property
  def rate_limit(self) -> RateLimit:
    """Exchange Broker API rate limit management — self-service rate-limit control for an Exchange Broker's master and sub-accounts: list configured UID-level limits, check aggregate usage against the entity's own cap, and set custom limits for sub-accounts.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/broker/exchange-broker/rate-limit/introduction)
    """
    return RateLimit(client=self.client)
