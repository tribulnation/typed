# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class RateLimitCapEntry(TypedDict):
  """Rate limit usage and capacity for one business type."""

  bizType: Literal['SPOT', 'OPTIONS', 'DERIVATIVES']
  """Business type."""
  totalRate: str
  """Aggregate API rate consumption across the master account and all sub-accounts."""
  ebCap: str
  """Entity-level API rate limit, requests per second."""
  uidCap: str
  """Per-UID API rate limit, requests per second."""


class RateLimitCapResult(TypedDict):
  """Rate limit usage and capacity per business type."""

  list: list[RateLimitCapEntry]
  """One entry per business type."""


class QueryCap(RpcEndpoint):
  """Get this Exchange Broker's total API rate limit usage and capacity across the entity and its UIDs, per business type. Only Exchange Broker main UIDs can call this endpoint."""

  async def query_cap(self, *, validate: bool | None = None) -> RateLimitCapResult:
    """Get this Exchange Broker's total API rate limit usage and capacity across the entity and its UIDs, per business type. Only Exchange Broker main UIDs can call this endpoint.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/broker/exchange-broker/rate-limit/query-cap)
    """
    return await self.request(
      None,
      method='GET',
      path='/v5/broker/apilimit/query-cap',
      meta={'signed': True},
      validate=validate,
      response_type=RateLimitCapResult,
    )
