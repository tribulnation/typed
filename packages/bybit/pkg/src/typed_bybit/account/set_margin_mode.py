# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  """Set-margin-mode parameters."""

  setMarginMode: Literal['ISOLATED_MARGIN', 'REGULAR_MARGIN', 'PORTFOLIO_MARGIN']
  """Desired margin mode."""


class SetMarginModeReason(TypedDict):
  """One reason the switch could not be applied."""

  reasonCode: str
  """Failure reason code."""
  reasonMsg: str
  """Human-readable failure description."""


class SetMarginModeResult(TypedDict):
  """Margin mode switch outcome."""

  reasons: list[SetMarginModeReason]
  """Failure reasons; empty on a fully successful switch."""


class SetMarginMode(RpcEndpoint):
  """Set the account's margin trading mode. Defaults to regular (cross) margin."""

  async def set_margin_mode(
    self,
    set_margin_mode: Literal['ISOLATED_MARGIN', 'REGULAR_MARGIN', 'PORTFOLIO_MARGIN'],
    *,
    validate: bool | None = None,
  ) -> SetMarginModeResult:
    """Set the account's margin trading mode. Defaults to regular (cross) margin.

    Args:
      set_margin_mode: Desired margin mode.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/account/set-margin-mode)
    """
    return await self.request(
      Request(setMarginMode=set_margin_mode),
      method='POST',
      path='/v5/account/set-margin-mode',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SetMarginModeResult,
    )
