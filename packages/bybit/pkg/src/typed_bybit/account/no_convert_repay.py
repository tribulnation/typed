# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class NoConvertRepayResult(TypedDict):
  """Repayment operation result."""

  resultStatus: Literal['P', 'SU', 'FA']
  """Repayment status. `FIXED`/`ALL` process asynchronously; `FLEXIBLE` processes synchronously."""


class Request(TypedDict):
  """No-convert-repay parameters."""

  coin: str
  """Coin to repay, uppercase only."""
  amount: NotRequired[str]
  """Quantity to repay. Only valid when `coin` is passed; cannot exceed the corresponding `repaymentType` liability total."""
  repaymentType: NotRequired[Literal['ALL', 'FIXED', 'FLEXIBLE']]
  """Liabilities to repay. Defaults to `FLEXIBLE`."""


class NoConvertRepay(RpcEndpoint):
  """Repay a loan liability using only the debt coin's own spot available balance, without converting any other asset. Floating-rate liabilities repay before fixed-rate ones. Prohibited 04:00-05:30 UTC hourly."""

  async def no_convert_repay(
    self,
    *,
    coin: str,
    amount: str | None = None,
    repayment_type: Literal['ALL', 'FIXED', 'FLEXIBLE'] | None = None,
    validate: bool | None = None,
  ) -> NoConvertRepayResult:
    """Repay a loan liability using only the debt coin's own spot available balance, without converting any other asset. Floating-rate liabilities repay before fixed-rate ones. Prohibited 04:00-05:30 UTC hourly.

    Args:
      coin: Coin to repay, uppercase only.
      amount: Quantity to repay. Only valid when `coin` is passed; cannot exceed the corresponding `repaymentType` liability total.
      repayment_type: Liabilities to repay. Defaults to `FLEXIBLE`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/account/no-convert-repay)
    """
    request: Request = Request(coin=coin)
    if amount is not None:
      request['amount'] = amount
    if repayment_type is not None:
      request['repaymentType'] = repayment_type
    return await self.request(
      request,
      method='POST',
      path='/v5/account/no-convert-repay',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=NoConvertRepayResult,
    )
