# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class AccountInfo(TypedDict):
  """Account configuration."""

  unifiedMarginStatus: Literal[1, 3, 4, 5, 6]
  """Account structure status."""
  marginMode: Literal['ISOLATED_MARGIN', 'REGULAR_MARGIN', 'PORTFOLIO_MARGIN']
  """Margin trading mode."""
  isMasterTrader: bool
  """Whether this account is a copy-trading lead trader."""
  spotHedgingStatus: Literal['ON', 'OFF']
  """Unified account spot-hedging status."""
  updatedTime: TimestampMillis
  """Account data last-update time, as a millisecond timestamp."""
  dcpStatus: NotRequired[str]
  """Deprecated; always `OFF`. Use `account.dcp_info` instead."""
  timeWindow: NotRequired[int]
  """Deprecated; always `0`. Use `account.dcp_info` instead."""
  smpGroup: NotRequired[int]
  """Deprecated; always `0`. Use `account.smp_group` instead."""


class Info(RpcEndpoint):
  """Query the account's margin mode, account mode and other configuration."""

  async def info(self, *, validate: bool | None = None) -> AccountInfo:
    """Query the account's margin mode, account mode and other configuration.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/account/account-info)
    """
    return await self.request(
      None,
      method='GET',
      path='/v5/account/info',
      meta={'signed': True},
      validate=validate,
      response_type=AccountInfo,
    )
