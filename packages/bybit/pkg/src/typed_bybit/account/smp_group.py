# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class SmpGroupResult(TypedDict):
  """SMP group ID."""

  smpGroup: int
  """SMP group ID. `0` when the account has no group."""


class SmpGroup(RpcEndpoint):
  """Query the self-match-prevention (SMP) group ID of the account."""

  async def smp_group(self, *, validate: bool | None = None) -> SmpGroupResult:
    """Query the self-match-prevention (SMP) group ID of the account.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/account/smp-group)
    """
    return await self.request(
      None,
      method='GET',
      path='/v5/account/smp-group',
      meta={'signed': True},
      validate=validate,
      response_type=SmpGroupResult,
    )
