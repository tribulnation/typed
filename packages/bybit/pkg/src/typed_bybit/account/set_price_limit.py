# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  """Set-price-limit parameters."""

  category: Literal['linear', 'inverse', 'spot']
  """Product type."""
  modifyEnable: bool
  """When `true`, out-of-range order prices are auto-adjusted to the nearest allowed level. When `false`, they are rejected."""


class SetPriceLimitResult(TypedDict):
  """Empty object on success."""


class SetPriceLimit(RpcEndpoint):
  """Configure how the account handles a limit order whose price exceeds the market's allowed range: reject it, or auto-adjust it to the nearest allowed price. Configured per category."""

  async def set_price_limit(
    self,
    category: Literal['linear', 'inverse', 'spot'],
    *,
    modify_enable: bool,
    validate: bool | None = None,
  ) -> SetPriceLimitResult:
    """Configure how the account handles a limit order whose price exceeds the market's allowed range: reject it, or auto-adjust it to the nearest allowed price. Configured per category.

    Args:
      category: Product type.
      modify_enable: When `true`, out-of-range order prices are auto-adjusted to the nearest allowed level. When `false`, they are rejected.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/account/set-price-limit)
    """
    return await self.request(
      Request(category=category, modifyEnable=modify_enable),
      method='POST',
      path='/v5/account/set-limit-px-action',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SetPriceLimitResult,
    )
