# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class OptionAssetInfo(TypedDict):
  """Option asset P&L for one coin."""

  coin: str
  """Coin name."""
  totalDelta: str
  """Aggregate delta from option positions only."""
  totalRPL: str
  """Total realised profit/loss."""
  totalUPL: str
  """Total unrealised profit/loss."""
  assetIM: str
  """Asset initial margin, including margin occupied by open option orders."""
  assetMM: str
  """Asset maintenance margin, including margin occupied by open option orders."""
  sendTime: TimestampMillis
  """Snapshot time, as a millisecond timestamp."""


Response = list[OptionAssetInfo]


class OptionAssetInfoEndpoint(RpcEndpoint):
  """Query the option asset profit and loss information for each coin under the account."""

  async def option_asset_info(self, *, validate: bool | None = None) -> Response:
    """Query the option asset profit and loss information for each coin under the account.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/account/option-asset-info)
    """
    return await self.request(
      None,
      method='GET',
      path='/v5/account/option-asset-info',
      meta={'signed': True},
      validate=validate,
      response_type=Response,
    )
