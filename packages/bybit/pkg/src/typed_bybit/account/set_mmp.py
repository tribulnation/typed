# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  """Set-MMP parameters."""

  baseCoin: str
  """Base coin, uppercase only."""
  window: str
  """Time window, in milliseconds."""
  frozenPeriod: str
  """Frozen period, in milliseconds. `"0"` means frozen until manually reset."""
  qtyLimit: str
  """Trade quantity limit, positive, up to 2 decimal places."""
  deltaLimit: str
  """Delta limit, positive, up to 2 decimal places."""
  vegaLimit: NotRequired[str]
  """Vega limit, positive, up to 2 decimal places. Defaults to `500000`."""


class SetMmpResult(TypedDict):
  """Empty object on success."""


class SetMmp(RpcEndpoint):
  """Configure Market Maker Protection (MMP) thresholds for one base coin. Options only; requires prior enablement by Bybit's institutional team."""

  async def set_mmp(
    self,
    *,
    base_coin: str,
    window: str,
    frozen_period: str,
    qty_limit: str,
    delta_limit: str,
    vega_limit: str | None = None,
    validate: bool | None = None,
  ) -> SetMmpResult:
    """Configure Market Maker Protection (MMP) thresholds for one base coin. Options only; requires prior enablement by Bybit's institutional team.

    Args:
      base_coin: Base coin, uppercase only.
      window: Time window, in milliseconds.
      frozen_period: Frozen period, in milliseconds. `"0"` means frozen until manually reset.
      qty_limit: Trade quantity limit, positive, up to 2 decimal places.
      delta_limit: Delta limit, positive, up to 2 decimal places.
      vega_limit: Vega limit, positive, up to 2 decimal places. Defaults to `500000`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/account/set-mmp)
    """
    request: Request = Request(
      baseCoin=base_coin,
      window=window,
      frozenPeriod=frozen_period,
      qtyLimit=qty_limit,
      deltaLimit=delta_limit,
    )
    if vega_limit is not None:
      request['vegaLimit'] = vega_limit
    return await self.request(
      request,
      method='POST',
      path='/v5/account/mmp-modify',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SetMmpResult,
    )
