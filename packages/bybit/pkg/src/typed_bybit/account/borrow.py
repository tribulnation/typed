# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class BorrowResult(TypedDict):
  """Confirmed borrow amount."""

  coin: str
  """Coin borrowed, uppercase."""
  amount: str
  """Confirmed borrow amount."""


class Request(TypedDict):
  """Borrow parameters."""

  coin: str
  """Coin to borrow, uppercase only, for example `BTC`."""
  amount: str
  """Quantity to borrow."""


class Borrow(RpcEndpoint):
  """Borrow a cryptocurrency at the variable rate."""

  async def borrow(
    self,
    *,
    coin: str,
    amount: str,
    validate: bool | None = None,
  ) -> BorrowResult:
    """Borrow a cryptocurrency at the variable rate.

    Args:
      coin: Coin to borrow, uppercase only, for example `BTC`.
      amount: Quantity to borrow.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/account/borrow)
    """
    return await self.request(
      Request(coin=coin, amount=amount),
      method='POST',
      path='/v5/account/borrow',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=BorrowResult,
    )
