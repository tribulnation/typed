# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  """Reset-MMP parameters."""

  baseCoin: str
  """Base coin, uppercase only."""


class ResetMmpResult(TypedDict):
  """Empty object on success."""


class ResetMmp(RpcEndpoint):
  """Unfreeze a frozen Market Maker Protection (MMP) account for one base coin and reset its `qtyLimit`/`deltaLimit`/`vegaLimit` accumulation to 0, even if the account is not currently frozen."""

  async def reset_mmp(
    self,
    base_coin: str,
    *,
    validate: bool | None = None,
  ) -> ResetMmpResult:
    """Unfreeze a frozen Market Maker Protection (MMP) account for one base coin and reset its `qtyLimit`/`deltaLimit`/`vegaLimit` accumulation to 0, even if the account is not currently frozen.

    Args:
      base_coin: Base coin, uppercase only.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/account/reset-mmp)
    """
    return await self.request(
      Request(baseCoin=base_coin),
      method='POST',
      path='/v5/account/mmp-reset',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ResetMmpResult,
    )
