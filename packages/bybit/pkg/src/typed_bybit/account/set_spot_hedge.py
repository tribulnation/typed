# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  """Set-spot-hedging parameters."""

  setHedgingMode: Literal['ON', 'OFF']
  """Desired hedging state."""


class SetSpotHedgeResult(TypedDict):
  """Empty object on success."""


class SetSpotHedge(RpcEndpoint):
  """Enable or disable the spot-hedging feature within a Portfolio Margin account."""

  async def set_spot_hedge(
    self,
    set_hedging_mode: Literal['ON', 'OFF'],
    *,
    validate: bool | None = None,
  ) -> SetSpotHedgeResult:
    """Enable or disable the spot-hedging feature within a Portfolio Margin account.

    Args:
      set_hedging_mode: Desired hedging state.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/account/set-spot-hedge)
    """
    return await self.request(
      Request(setHedgingMode=set_hedging_mode),
      method='POST',
      path='/v5/account/set-hedging-mode',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SetSpotHedgeResult,
    )
