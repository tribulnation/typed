# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  """Set-delta-mode parameters."""

  deltaEnable: Literal['1', '0']
  """Desired mode. `"1"`: enable. `"0"`: disable."""


class SetDeltaModeResult(TypedDict):
  """Empty object on success."""


class SetDeltaMode(RpcEndpoint):
  """Enable or disable Delta Neutral Mode, which lowers ADL (Auto-Deleveraging) queue priority for qualifying delta-neutral positions during extreme market conditions."""

  async def set_delta_mode(
    self,
    delta_enable: Literal['1', '0'],
    *,
    validate: bool | None = None,
  ) -> SetDeltaModeResult:
    """Enable or disable Delta Neutral Mode, which lowers ADL (Auto-Deleveraging) queue priority for qualifying delta-neutral positions during extreme market conditions.

    Args:
      delta_enable: Desired mode. `"1"`: enable. `"0"`: disable.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/account/set-delta-mode)
    """
    return await self.request(
      Request(deltaEnable=delta_enable),
      method='POST',
      path='/v5/account/set-delta-mode',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SetDeltaModeResult,
    )
