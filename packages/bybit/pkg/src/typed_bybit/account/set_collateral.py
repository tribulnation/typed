# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  """Set-collateral parameters."""

  coin: str
  """Coin to configure, uppercase only. `USDT`/`USDC` cannot be set."""
  collateralSwitch: Literal['ON', 'OFF']
  """Desired collateral state."""


class SetCollateralResult(TypedDict):
  """Empty object on success."""


class SetCollateral(RpcEndpoint):
  """Set whether one coin held in a Unified account is usable as margin collateral. `USDT`/`USDC` cannot be configured; discover eligible coins via account.collateral_info."""

  async def set_collateral(
    self,
    coin: str,
    *,
    collateral_switch: Literal['ON', 'OFF'],
    validate: bool | None = None,
  ) -> SetCollateralResult:
    """Set whether one coin held in a Unified account is usable as margin collateral. `USDT`/`USDC` cannot be configured; discover eligible coins via account.collateral_info.

    Args:
      coin: Coin to configure, uppercase only. `USDT`/`USDC` cannot be set.
      collateral_switch: Desired collateral state.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/account/set-collateral)
    """
    return await self.request(
      Request(coin=coin, collateralSwitch=collateral_switch),
      method='POST',
      path='/v5/account/set-collateral-switch',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SetCollateralResult,
    )
