# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class DcpProductInfo(TypedDict):
  """DCP configuration for one product."""

  product: Literal['SPOT', 'DERIVATIVES', 'OPTIONS']
  """Product covered by this DCP configuration."""
  dcpStatus: Literal['ON']
  """DCP status."""
  timeWindow: str
  """Pre-set trigger window, in seconds. Range [3, 300]; defaults to 10."""


class DcpInfoResult(TypedDict):
  """DCP configuration."""

  dcpInfos: list[DcpProductInfo]
  """DCP configuration, one entry per configured product. Empty when the account has no DCP configuration."""


class DcpInfo(RpcEndpoint):
  """Query the Disconnected-CancelAll-Prevention (DCP) configuration of the account. Requires account-manager approval to configure; an account with no DCP configuration returns an empty list rather than an error."""

  async def dcp_info(self, *, validate: bool | None = None) -> DcpInfoResult:
    """Query the Disconnected-CancelAll-Prevention (DCP) configuration of the account. Requires account-manager approval to configure; an account with no DCP configuration returns an empty list rather than an error.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/account/dcp-info)
    """
    return await self.request(
      None,
      method='GET',
      path='/v5/account/query-dcp-info',
      meta={'signed': True},
      validate=validate,
      response_type=DcpInfoResult,
    )
