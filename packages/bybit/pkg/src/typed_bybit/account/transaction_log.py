# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, Sequence, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  accountType: NotRequired[Literal['UNIFIED']]
  """Account type."""
  category: NotRequired[Literal['spot', 'linear', 'option', 'inverse']]
  """Product type."""
  currency: NotRequired[str]
  """Currency."""
  baseCoin: NotRequired[str]
  """Base coin."""
  type: NotRequired[
    Literal[
      'TRANSFER_IN',
      'TRANSFER_OUT',
      'TRADE',
      'SETTLEMENT',
      'DELIVERY',
      'LIQUIDATION',
      'ADL',
      'AIRDROP',
      'BONUS',
      'BONUS_RECOLLECT',
      'FEE_REFUND',
      'INTEREST',
      'CURRENCY_BUY',
      'CURRENCY_SELL',
      'DIVIDEND_SETTLEMENT',
      'FORWARD_SPLIT_SETTLE',
      'REVERSE_SPLIT_SETTLE',
    ]
  ]
  """Transaction log type."""
  transSubType: NotRequired[str]
  """Transaction log sub-type."""
  startTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  endTime: NotRequired[TimestampMillis]
  """Millisecond timestamp."""
  limit: NotRequired[int]
  """Page size."""
  cursor: NotRequired[str]
  """Pagination cursor."""


class TransactionLogEntry(TypedDict):
  """One transaction log entry."""

  id: str
  """Unique entry identifier."""
  symbol: str
  """Symbol name; empty when not applicable."""
  category: Literal['spot', 'linear', 'option', 'inverse']
  """Product type."""
  side: Literal['Buy', 'Sell', 'None']
  """Direction."""
  transactionTime: TimestampMillis
  """Transaction time, as a millisecond timestamp."""
  type: Literal[
    'TRANSFER_IN',
    'TRANSFER_OUT',
    'TRADE',
    'SETTLEMENT',
    'DELIVERY',
    'LIQUIDATION',
    'ADL',
    'AIRDROP',
    'BONUS',
    'BONUS_RECOLLECT',
    'FEE_REFUND',
    'INTEREST',
    'CURRENCY_BUY',
    'CURRENCY_SELL',
    'DIVIDEND_SETTLEMENT',
    'FORWARD_SPLIT_SETTLE',
    'REVERSE_SPLIT_SETTLE',
  ]
  """Transaction log type."""
  transSubType: NotRequired[str]
  """Transaction log sub-type; empty for most transactions."""
  qty: Decimal
  """Quantity. Negative decreases, positive increases, for spot; unsigned for perpetuals/futures."""
  size: NotRequired[Literal[''] | Decimal]
  """Position size after this entry, signed by direction. Empty string expected on an entry the field does not apply to (a transfer), by the same convention `funding` follows; not yet seen on the wire."""
  currency: str
  """Currency, for example `USDT`, `USDC`, `BTC`, `ETH`."""
  tradePrice: NotRequired[Literal[''] | Decimal]
  """Execution price. Empty string expected on an entry the field does not apply to (a transfer), by the same convention `funding` follows; not yet seen on the wire."""
  funding: NotRequired[Literal[''] | Decimal]
  """Funding fee; positive when received, negative when paid. Empty string on an entry that settles no funding (a `TRADE` row)."""
  fee: NotRequired[Decimal]
  """Trading fee; positive when charged, negative when a rebate."""
  cashFlow: NotRequired[Decimal]
  """Cash flow from a position close, settlement, or transfer."""
  change: Decimal
  """Change to the wallet balance: `cashFlow + funding - fee`."""
  cashBalance: Decimal
  """Wallet balance after this entry."""
  feeRate: NotRequired[Literal[''] | Decimal]
  """Trading or funding fee rate. Empty string expected on an entry the field does not apply to (a transfer), by the same convention `funding` follows; not yet seen on the wire."""
  bonusChange: NotRequired[Literal[''] | Decimal]
  """Change in bonus amount. Empty string when no bonus moved."""
  tradeId: NotRequired[str]
  """Trade identifier."""
  orderId: NotRequired[str]
  """Order identifier."""
  orderLinkId: NotRequired[str]
  """User-customised order ID."""
  extraFees: NotRequired[str]
  """Additional fee detail. Returned only for Indonesian-site or EU fiat spot orders."""
  displayType: NotRequired[str]
  """UI display-type designation."""


class TransactionLogResult(TypedDict):
  """Page of matching transaction log entries."""

  list: list[TransactionLogEntry]
  """Matching transaction log entries."""
  nextPageCursor: NotRequired[str | None]
  """Opaque cursor for the next page. Pass it back as `cursor`; `null` (observed live) or an empty string means there are no further pages."""


class TransactionLog(RpcEndpoint):
  """Query transaction logs in a Unified account, up to 2 years of history. May experience increased latency during extreme market volatility."""

  def transaction_log_paged(
    self,
    account_type: Literal['UNIFIED'] | None = None,
    *,
    category: Literal['spot', 'linear', 'option', 'inverse'] | None = None,
    currency: str | None = None,
    base_coin: str | None = None,
    type: Literal[
      'TRANSFER_IN',
      'TRANSFER_OUT',
      'TRADE',
      'SETTLEMENT',
      'DELIVERY',
      'LIQUIDATION',
      'ADL',
      'AIRDROP',
      'BONUS',
      'BONUS_RECOLLECT',
      'FEE_REFUND',
      'INTEREST',
      'CURRENCY_BUY',
      'CURRENCY_SELL',
      'DIVIDEND_SETTLEMENT',
      'FORWARD_SPLIT_SETTLE',
      'REVERSE_SPLIT_SETTLE',
    ]
    | None = None,
    trans_sub_type: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[TransactionLogEntry, str]:
    """Query transaction logs in a Unified account, up to 2 years of history. May experience increased latency during extreme market volatility.

    Paged variant of `transaction_log`: Passes each page's token back as `cursor` and stops when a response carries no `nextPageCursor`. Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      account_type: Account type.
      category: Product type.
      currency: Currency.
      base_coin: Base coin.
      type: Transaction log type.
      trans_sub_type: Transaction log sub-type.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/account/transaction-log)
    """

    async def next(cursor: str) -> tuple[Sequence[TransactionLogEntry], str | None]:
      response = await self.transaction_log(
        account_type,
        category=category,
        currency=currency,
        base_coin=base_coin,
        type=type,
        trans_sub_type=trans_sub_type,
        start_time=start_time,
        end_time=end_time,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('list')
      rows = rows if rows is not None else []
      state = response.get('nextPageCursor')
      return rows, state or None

    return PaginatedResponse('', next)

  async def transaction_log(
    self,
    account_type: Literal['UNIFIED'] | None = None,
    *,
    category: Literal['spot', 'linear', 'option', 'inverse'] | None = None,
    currency: str | None = None,
    base_coin: str | None = None,
    type: Literal[
      'TRANSFER_IN',
      'TRANSFER_OUT',
      'TRADE',
      'SETTLEMENT',
      'DELIVERY',
      'LIQUIDATION',
      'ADL',
      'AIRDROP',
      'BONUS',
      'BONUS_RECOLLECT',
      'FEE_REFUND',
      'INTEREST',
      'CURRENCY_BUY',
      'CURRENCY_SELL',
      'DIVIDEND_SETTLEMENT',
      'FORWARD_SPLIT_SETTLE',
      'REVERSE_SPLIT_SETTLE',
    ]
    | None = None,
    trans_sub_type: str | None = None,
    start_time: TimestampMillis | None = None,
    end_time: TimestampMillis | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> TransactionLogResult:
    """Query transaction logs in a Unified account, up to 2 years of history. May experience increased latency during extreme market volatility.

    Args:
      account_type: Account type.
      category: Product type.
      currency: Currency.
      base_coin: Base coin.
      type: Transaction log type.
      trans_sub_type: Transaction log sub-type.
      start_time: Millisecond timestamp.
      end_time: Millisecond timestamp.
      limit: Page size.
      cursor: Pagination cursor.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/account/transaction-log)
    """
    request: Request = Request()
    if account_type is not None:
      request['accountType'] = account_type
    if category is not None:
      request['category'] = category
    if currency is not None:
      request['currency'] = currency
    if base_coin is not None:
      request['baseCoin'] = base_coin
    if type is not None:
      request['type'] = type
    if trans_sub_type is not None:
      request['transSubType'] = trans_sub_type
    if start_time is not None:
      request['startTime'] = start_time
    if end_time is not None:
      request['endTime'] = end_time
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/v5/account/transaction-log',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=TransactionLogResult,
    )
