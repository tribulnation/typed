# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class CollateralInfo(TypedDict):
  """Collateral configuration and limits of one currency."""

  currency: str
  """Currency."""
  hourlyBorrowRate: str
  """Hourly borrow rate."""
  maxBorrowingAmount: str
  """Maximum borrow amount, shared across main and sub UIDs."""
  freeBorrowingLimit: NotRequired[str]
  """Maximum amount eligible for interest-free borrowing."""
  freeBorrowAmount: NotRequired[str]
  """Portion of the total borrowed amount exempt from interest."""
  borrowAmount: NotRequired[str]
  """Amount currently borrowed."""
  otherBorrowAmount: NotRequired[str]
  """Sum of borrowed amount across other accounts under the same main account."""
  availableToBorrow: str
  """Amount still available to borrow, shared across main and sub UIDs."""
  borrowable: bool
  """Whether this currency can be borrowed."""
  borrowUsageRate: str
  """Borrow usage rate: sum of main and sub account `borrowAmount` divided by `maxBorrowingAmount`."""
  marginCollateral: bool
  """Whether the platform allows this currency to be used as margin collateral."""
  collateralSwitch: bool
  """Whether the user has turned collateral on for this currency."""


class Request(TypedDict):
  currency: NotRequired[str]
  """Currency."""


class CollateralInfoResult(TypedDict):
  """Collateral information."""

  list: list[CollateralInfo]
  """One entry per collateral currency."""


class CollateralInfoEndpoint(RpcEndpoint):
  """Get loan interest rate, borrowing limits, collateral conversion rate and margin eligibility for a Unified margin account's collateral coins."""

  async def collateral_info(
    self,
    currency: str | None = None,
    *,
    validate: bool | None = None,
  ) -> CollateralInfoResult:
    """Get loan interest rate, borrowing limits, collateral conversion rate and margin eligibility for a Unified margin account's collateral coins.

    Args:
      currency: Currency.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/account/collateral-info)
    """
    request: Request = Request()
    if currency is not None:
      request['currency'] = currency
    return await self.request(
      request,
      method='GET',
      path='/v5/account/collateral-info',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CollateralInfoResult,
    )
