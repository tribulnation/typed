# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class RateLimitEntry(TypedDict):
  """Rate limit for one UID group and business type."""

  uids: str
  """UIDs sharing this rate limit, comma-separated."""
  bizType: Literal['SPOT', 'DERIVATIVES']
  """Business type this limit applies to."""
  rate: int
  """API rate limit, in requests per second."""


class Request(TypedDict):
  uids: str
  """UIDs."""


class RateLimitQueryResult(TypedDict):
  """Rate limit per queried UID and business type."""

  list: list[RateLimitEntry]
  """Matching rate limit entries."""


class Query(RpcEndpoint):
  """Get the UID-level API rate limit for one or more UIDs. A master account may query its own and its sub-accounts' limits; a sub-account may only query its own. Self-service, restricted to PRO1-PRO6/MM1-MM3 institutional tiers."""

  async def query(
    self, uids: str, *, validate: bool | None = None
  ) -> RateLimitQueryResult:
    """Get the UID-level API rate limit for one or more UIDs. A master account may query its own and its sub-accounts' limits; a sub-account may only query its own. Self-service, restricted to PRO1-PRO6/MM1-MM3 institutional tiers.

    Args:
      uids: UIDs.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/rate-limit/rules-for-pros/apilimit-query)
    """
    return await self.request(
      Request(uids=uids),
      method='GET',
      path='/v5/apilimit/query',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=RateLimitQueryResult,
    )
