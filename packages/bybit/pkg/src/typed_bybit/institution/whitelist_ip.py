# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class InstitutionWhitelistEntry(TypedDict):
  """Whitelisted IP addresses for one client entity."""

  name: str
  """Client entity name."""
  endpointDomain: str
  """Dedicated hostname assigned to this client entity for the Market Maker Gateway/WebSocket."""
  ipList: list[str]
  """Whitelisted IP addresses for this hostname."""


Response = list[InstitutionWhitelistEntry]


class WhitelistIp(RpcEndpoint):
  """Get the whitelisted IP addresses configured for each of this institutional client's dedicated gateway/WebSocket hostnames. Must be requested with a main account API key."""

  async def whitelist_ip(self, *, validate: bool | None = None) -> Response:
    """Get the whitelisted IP addresses configured for each of this institutional client's dedicated gateway/WebSocket hostnames. Must be requested with a main account API key.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/institution/whitelist-ip)
    """
    return await self.request(
      None,
      method='GET',
      path='/v5/ins/whitelist/ip',
      meta={'signed': True},
      validate=validate,
      response_type=Response,
    )
