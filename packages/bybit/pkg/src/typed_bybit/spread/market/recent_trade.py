# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  symbol: str
  """Spread combination symbol name."""
  limit: NotRequired[int]
  """Page size."""


class SpreadPublicTrade(TypedDict):
  """One executed public trade on a spread combination."""

  execId: str
  """Execution ID."""
  symbol: str
  """Spread combination symbol name."""
  price: str
  """Trade price."""
  size: str
  """Trade size."""
  side: Literal['Buy', 'Sell']
  """Side of the taker."""
  time: TimestampMillis
  """Trade time, as a millisecond timestamp."""
  seq: str
  """Cross sequence number."""


class SpreadRecentTradesResult(TypedDict):
  """Recent public trades for one spread combination symbol."""

  list: list[SpreadPublicTrade]
  """Recent trades, newest first."""


class RecentTrade(RpcEndpoint):
  """Get recently executed public trades on one spread combination symbol."""

  async def recent_trade(
    self,
    symbol: str,
    *,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> SpreadRecentTradesResult:
    """Get recently executed public trades on one spread combination symbol.

    Args:
      symbol: Spread combination symbol name.
      limit: Page size.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/spread/market/recent-trade)
    """
    request: Request = Request(symbol=symbol)
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/v5/spread/recent-trade',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=SpreadRecentTradesResult,
    )
