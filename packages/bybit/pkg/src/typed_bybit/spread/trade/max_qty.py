# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  symbol: str
  """Spread combination symbol name."""
  side: Literal['1', '2']
  """Order side. `1`: Buy, `2`: Sell."""
  orderPrice: str
  """Order price."""


class SpreadMaxQtyResult(TypedDict):
  """Maximum order quantity for one spread order."""

  ab: str
  """Maximum order quantity."""


class MaxQty(RpcEndpoint):
  """Get the maximum quantity this account could place for a spread order at a given price and side, given available balance and risk limits."""

  async def max_qty(
    self,
    *,
    symbol: str,
    side: Literal['1', '2'],
    order_price: str,
    validate: bool | None = None,
  ) -> SpreadMaxQtyResult:
    """Get the maximum quantity this account could place for a spread order at a given price and side, given available balance and risk limits.

    Args:
      symbol: Spread combination symbol name.
      side: Order side. `1`: Buy, `2`: Sell.
      order_price: Order price.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/spread/trade/max-qty)
    """
    return await self.request(
      Request(symbol=symbol, side=side, orderPrice=order_price),
      method='GET',
      path='/v5/spread/max-qty',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SpreadMaxQtyResult,
    )
