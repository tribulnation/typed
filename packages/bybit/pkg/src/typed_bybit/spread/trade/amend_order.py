# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class Request(TypedDict):
  """Spread order amendment parameters."""

  symbol: str
  """Spread combination symbol name."""
  orderId: NotRequired[str]
  """Spread combination order ID. Either `orderId` or `orderLinkId` is required."""
  orderLinkId: NotRequired[str]
  """User-customised order ID. Either `orderId` or `orderLinkId` is required."""
  qty: NotRequired[str]
  """Order quantity after modification. Either `qty` or `price` is required."""
  price: NotRequired[str]
  """Order price after modification. Either `qty` or `price` is required. An empty string preserves the existing price; `"0"` updates it to zero."""


class SpreadAmendOrderResult(TypedDict):
  """Identifiers of the amended spread order."""

  orderId: str
  """Spread combination order ID."""
  orderLinkId: str
  """User-customised order ID."""


class AmendOrder(RpcEndpoint):
  """Modify the quantity and/or price of an existing unfilled or partially-filled spread order."""

  async def amend_order(
    self,
    *,
    symbol: str,
    order_id: str | None = None,
    order_link_id: str | None = None,
    qty: str | None = None,
    price: str | None = None,
    validate: bool | None = None,
  ) -> SpreadAmendOrderResult:
    """Modify the quantity and/or price of an existing unfilled or partially-filled spread order.

    Args:
      symbol: Spread combination symbol name.
      order_id: Spread combination order ID. Either `orderId` or `orderLinkId` is required.
      order_link_id: User-customised order ID. Either `orderId` or `orderLinkId` is required.
      qty: Order quantity after modification. Either `qty` or `price` is required.
      price: Order price after modification. Either `qty` or `price` is required. An empty string preserves the existing price; `"0"` updates it to zero.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/spread/trade/amend-order)
    """
    request: Request = Request(symbol=symbol)
    if order_id is not None:
      request['orderId'] = order_id
    if order_link_id is not None:
      request['orderLinkId'] = order_link_id
    if qty is not None:
      request['qty'] = qty
    if price is not None:
      request['price'] = price
    return await self.request(
      request,
      method='POST',
      path='/v5/spread/order/amend',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=SpreadAmendOrderResult,
    )
