# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .account import Account
from .affiliate import Affiliate
from .asset import Asset
from .bot import Bot
from .broker import Broker
from .card import Card
from .crypto_loan import CryptoLoan
from .finance import Finance
from .finance_ws import FinanceWs
from .institution import Institution
from .institutional_loan import InstitutionalLoan
from .inverse import Inverse
from .linear import Linear
from .market import Market
from .option import Option
from .position import Position
from .pre_upgrade import PreUpgrade
from .private import Private
from .rate_limit import RateLimit
from .rfq import Rfq
from .rfq_ws import RfqWs
from .spot import Spot
from .spot_margin import SpotMargin
from .spread import Spread
from .spread_ws import SpreadWs
from .strategy import Strategy
from .tax import Tax
from .trade import Trade
from .trade_ws import TradeWs
from .user import User
from .web3 import Web3
from typed_bybit.core.base import BybitBase


class Bybit(BybitBase):
  """Bybit v5 client -- REST and nine separate WebSocket connections (four public category feeds -- spot, linear, inverse, option -- plus Spread Trading's and RFQ Trading's own public streams, Advanced Earn's fixed-price offer stream, one private account stream, and one order-entry "WS Trade" connection). Every REST product domain (market, account, asset, trade, spread, rfq, finance, ...) hangs directly off this root alongside each WebSocket connection's own top-level surface.

  References:
    - [Upstream docs](https://bybit-exchange.github.io/docs/v5/guide)
  """

  @cached_property
  def account(self) -> Account:
    """Account — single Unified Trading Account configuration and query surface: wallet balance and account info, margin mode/collateral/spot-hedge settings, unified-margin manual borrow and repay (with borrow history and repay liability), market maker protection (MMP) and self-match prevention (SMP), fee rate, coin greeks, transaction/trade logs, price-limit and delta-neutral-mode settings, and upgrading a legacy account to Unified Trading Account Pro.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/account/wallet-balance)
    """
    return Account(client=self.client)

  @cached_property
  def affiliate(self) -> Affiliate:
    """Affiliate — reporting endpoints for a registered affiliate account: list an affiliate's referred client users with their deposit/trading metrics, list sub-affiliates under the account with commission earned per coin, and pull detailed trading/deposit/commission info for one client. Every endpoint requires an affiliate account and an API key carrying only the Affiliate permission, called from the master UID.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/affiliate/affiliate-info)
    """
    return Affiliate(client=self.client)

  @cached_property
  def asset(self) -> Asset:
    """Asset — cross-account asset management: coin metadata, delivery and USDC session settlement records, funding (sub-)account transaction history, small-balance conversion (exchange) records, portfolio margin info, and member/sub-UID asset listings across a master-sub account structure. Broader than any one sub-grouping: also composes the balance, convert, convert-small-balance, deposit, fiat-convert, transfer, and withdraw endpoint groups underneath it.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/asset/coin-info)
    """
    return Asset(client=self.client)

  @cached_property
  def bot(self) -> Bot:
    """Bybit Trading Bots — automated strategy bots managed via the API: DCA (fixed-interval dollar-cost-averaging), Spot Grid, Futures Grid, Futures Combo (multi-symbol portfolio rebalancing), and Futures Martingale, each with its own create/close and, where offered, parameter-validation, parameter-limit, and detail-query endpoints. Every bot type shares the same compliance-wall/KYC gating and the same bot-wide error-code catalog.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/bot/err)
    """
    return Bot(client=self.client)

  @cached_property
  def broker(self) -> Broker:
    """Bybit Broker Program — endpoints for approved broker partners: API Broker's dedicated, IP-whitelisted connectivity for registered clients (`api_broker`), Exchange Broker's master/sub-account info, earnings, deposit records, and self-service rate-limit management (`exchange_broker`), and issuing/querying reward vouchers for broker-referred users (`reward`).

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/broker/api-broker/connection)
    """
    return Broker(client=self.client)

  @cached_property
  def card(self) -> Card:
    """Bybit Card — the crypto debit card product: querying card transaction (asset) records, plus the `point` sub-grouping for the card's reward-points loyalty program (tier, balance, redeemable items, point history, and cashback detail).

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/bybit-card/asset-records)
    """
    return Card(client=self.client)

  @cached_property
  def crypto_loan(self) -> CryptoLoan:
    """Crypto Loan — borrow against crypto collateral, either as a fixed-term loan or an open-ended flexible loan, plus the account-wide operations shared by both: borrowable/collateral coin lists, max loan and max collateral-reduction calculators, collateral top-up/withdrawal, LTV adjustment history, and a combined position view spanning both loan types.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/new-crypto-loan/crypto-loan-position)
    """
    return CryptoLoan(client=self.client)

  @cached_property
  def finance(self) -> Finance:
    """Finance — Bybit's wealth-management product domain, grouping several distinct Earn-style yield products (byUSDT, Easy & On-Chain Earn, Fixed Saving, Hold-to-Earn, RWA, among others), each with its own product catalog, order placement, position, and yield-history endpoints.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/finance/earn/easy-onchain/product-info)
    """
    return Finance(client=self.client)

  @cached_property
  def finance_ws(self) -> FinanceWs:
    """Advanced Earn WebSocket streams — public, unauthenticated push topics carrying live offer quotes for Discount Buy, Double Win, Dual Asset, and Smart Leverage, meant to be seeded with an initial REST product-quote call and then kept live by subscribing to the matching topic.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/finance/advanced-earn/websocket/discount-buy-offer)
    """
    return FinanceWs(client=self.finance_client)

  @cached_property
  def institution(self) -> Institution:
    """Institution Connection whitelist -- self-service management of a dedicated institutional client's whitelisted IP addresses for its private connection to Bybit: list the current whitelist and audit the change log of updates made to it.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/institution/whitelist-ip)
    """
    return Institution(client=self.client)

  @cached_property
  def institutional_loan(self) -> InstitutionalLoan:
    """Institutional Loan (OTC lending) -- Bybit's collateralized-lending product for institutional clients: bind or unbind a UID to a risk unit, query margin/collateral product and coin-conversion info, convert LTV, look up outstanding loan orders and repayment info, and submit a repayment.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/otc/bind-uid)
    """
    return InstitutionalLoan(client=self.client)

  @cached_property
  def inverse(self) -> Inverse:
    """Public WebSocket market-data topics for Bybit's inverse (coin-margined) perpetual and futures contracts, on the dedicated `wss://stream.bybit.com/v5/public/inverse` connection: order book (including RPI and full-depth variants), trades, klines, tickers, ADL alerts, all-liquidation pushes, order-price-limit and insurance-pool updates.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/ws/connect)
    """
    return Inverse(client=self.inverse_client)

  @cached_property
  def linear(self) -> Linear:
    """Public WebSocket market-data topics for Bybit's linear (USDT/USDC-margined) perpetual and futures contracts, on the dedicated `wss://stream.bybit.com/v5/public/linear` connection: order book (including RPI and full-depth variants), trades, klines, tickers, ADL alerts, all-liquidation pushes, order-price-limit and insurance-pool updates.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/ws/connect)
    """
    return Linear(client=self.linear_client)

  @cached_property
  def market(self) -> Market:
    """Public REST market-data endpoints spanning the spot, linear, inverse and option categories: order books (including full-depth and RPI variants), tickers, klines (plain, mark-price, index-price and premium-index variants), recent trades, open interest, funding-rate history, the insurance fund, risk limits, instrument specifications, long/short account ratio, historical volatility, delivery price, and server time.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/market/tickers)
    """
    return Market(client=self.client)

  @cached_property
  def option(self) -> Option:
    """Options public market data — WebSocket pushes for one option symbol's level-2 order book snapshots/deltas, ticker snapshots, and real-time public trade prints across every option symbol on a base coin.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/websocket/public/orderbook)
    """
    return Option(client=self.option_client)

  @cached_property
  def position(self) -> Position:
    """Position management for derivatives accounts — query open positions and closed PnL, switch position mode (one-way/hedge) and add/reduce margin, set leverage and risk-limit tiers, set a trading-stop (TP/SL/trailing), and move positions between accounts.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/position)
    """
    return Position(client=self.client)

  @cached_property
  def pre_upgrade(self) -> PreUpgrade:
    """Pre-upgrade historical data — after an account is upgraded to a Unified Trading Account, the classic-account order, execution, closed-PnL, delivery-record, USDC-session-settlement, and transaction-log history from before that upgrade is only reachable through these endpoints rather than the normal live ones.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/pre-upgrade/order-list)
    """
    return PreUpgrade(client=self.client)

  @cached_property
  def private(self) -> Private:
    """Private (authenticated) WebSocket topics — real-time pushes for the caller's own account: order and position lifecycle, fills (both the standard execution feed and the low-latency fast-execution variant), wallet balance, spread-combination orders and fills, options Greeks, algo-strategy updates, and per-category Disconnect-Cancel-Protect arming.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/websocket/private/position)
    """
    return Private(client=self.private_client)

  @cached_property
  def rate_limit(self) -> RateLimit:
    """Rules for Pros — self-service UID-level API rate limit management for PRO1-PRO6/MM1-MM3 institutional tiers: query or set a UID's own rate limit cap, list every UID's limit under an account, and check aggregate usage against the institution-wide cap.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/rate-limit/rules-for-pros/introduction)
    """
    return RateLimit(client=self.client)

  @cached_property
  def rfq(self) -> Rfq:
    """RFQ (Request for Quote) block trading — an inquiring party creates an RFQ, quoting parties respond with quotes, and the inquirer accepts and executes the best one, with the trade then settled and published; covers the REST RFQ/quote/trade endpoints. The public and private RFQ WebSocket topics that carry inquiry, quote, and execution updates live under the sibling `rfq_ws` connection.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/rfq/basic-workflow)
    """
    return Rfq(client=self.client)

  @cached_property
  def rfq_ws(self) -> RfqWs:
    """RFQ Trading's public and private WebSocket channels: inquiry, quote, transaction, and public-transaction pushes, over their own dedicated connection -- the WebSocket counterpart to the REST `rfq` trade grouping.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/rfq/websocket/inquiry)
    """
    return RfqWs(client=self.rfq_client)

  @cached_property
  def spot(self) -> Spot:
    """Spot public market data — WebSocket pushes for one spot symbol: level-2 order book (standard and RPI depth-50 variant), full order-book deltas, candlesticks, ticker, order price limit, and public trade prints.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/websocket/public/orderbook)
    """
    return Spot(client=self.spot_client)

  @cached_property
  def spot_margin(self) -> SpotMargin:
    """Spot Margin Trade for Unified accounts — toggle and query margin-trade status/leverage, VIP and tiered collateral-ratio/position-tier margin data, per-coin borrowable state and currency data, max-borrowable and available-repayment amounts, auto-repay mode, liability and historical interest, plus fixed-rate (fixed-term) borrowing as its own sub-feature.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/spot-margin-uta/vip-margin)
    """
    return SpotMargin(client=self.client)

  @cached_property
  def spread(self) -> Spread:
    """Spread Trading — Bybit's calendar/inter-instrument spread combinations (perpetual-vs-spot funding-rate arb, futures-vs-spot carry trade, futures-vs-futures calendar spread, futures-vs-perpetual basis), traded as a single synthetic instrument; covers the REST market-data and trade-execution sub-groupings. The public WebSocket order-book/ticker/trade feeds for spread symbols live under the sibling `spread_ws` connection.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/spread/market/instrument)
    """
    return Spread(client=self.client)

  @cached_property
  def spread_ws(self) -> SpreadWs:
    """Spread Trading's public WebSocket channels: order book, ticker, and public trade pushes for spread symbols, over their own dedicated connection -- the WebSocket counterpart to the REST `spread` market-data/trade groupings.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/spread/websocket/public/orderbook)
    """
    return SpreadWs(client=self.spread_client)

  @cached_property
  def strategy(self) -> Strategy:
    """Strategy (algo) orders — Bybit's server-side conditional order strategies: submit a new strategy order, list active/historical strategy orders and their underlying child order legs, and stop a running strategy.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/strategy/create-strategy)
    """
    return Strategy(client=self.client)

  @cached_property
  def tax(self) -> Tax:
    """Tax reporting — export a user's trading and transaction history for tax purposes: check the account's earliest exportable register date, request a new export report, poll its generation status, and download the finished report once ready.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/tax/explain)
    """
    return Tax(client=self.client)

  @cached_property
  def trade(self) -> Trade:
    """Order and trade execution — the core v5 Order endpoints (place, amend, cancel, their batch variants, open/historical orders, execution/trade history, disconnect-cancel-protection, pre-check, and spot borrow quota) over REST. The equivalent create/amend/cancel/batch order operations and ping carried over the private WebSocket trade channel live under the sibling `trade_ws` connection.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/order/create-order)
    """
    return Trade(client=self.client)

  @cached_property
  def trade_ws(self) -> TradeWs:
    """WS Trade -- the order-entry WebSocket connection: create/amend/cancel orders and their batch variants, plus a keepalive ping, each built as a signed command frame and sent over the dedicated trade WebSocket rather than REST.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/websocket/trade/guideline)
    """
    return TradeWs(client=self.trade_client)

  @cached_property
  def user(self) -> User:
    """User and sub-account management — master/sub API key lifecycle (create, modify, remove, list, info), sub-UID creation, listing and freezing, custodial sub-account fund lists, UID wallet-type lookup, and referral-code/friend-referral/agreement endpoints.

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/user/apikey-info)
    """
    return User(client=self.client)

  @cached_property
  def web3(self) -> Web3:
    """Web3 — Bybit's on-chain wallet/DeFi product suite: liquidity-pool farming (web3/lp), on-chain prediction markets including sports (web3/prediction), and web3 token swap/trade execution (web3/trade).

    References:
      - [Upstream docs](https://bybit-exchange.github.io/docs/v5/alpha/trade/trade-quote)
    """
    return Web3(client=self.client)
