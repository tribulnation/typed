# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.ws import StreamsEndpoint


class OptionPublicTrade(TypedDict):
  """One matched option trade."""

  i: str
  """Trade id."""
  T: TimestampMillis
  """Fill timestamp."""
  p: str
  """Trade price."""
  v: str
  """Trade quantity."""
  S: Literal['Buy', 'Sell']
  """Taker side."""
  seq: int
  """Cross-sequence id. Multiple pushes may share one value."""
  s: str
  """Option symbol name."""
  BT: bool
  """Whether this is a block trade."""
  mP: str
  """Mark price at the time of the trade."""
  iP: str
  """Index price at the time of the trade."""
  mIv: str
  """Mark implied volatility at the time of the trade."""
  iv: str
  """Implied volatility of the trade."""


OptionPublicTrades = list[OptionPublicTrade]


class Trade(StreamsEndpoint):
  """Subscribe to real-time public trade prints for every option symbol on one base coin."""

  def trade(
    self,
    base_coin: str,
    *,
    validate: bool | None = None,
  ) -> StreamManager[OptionPublicTrades, Any, Any]:
    """Subscribe to real-time public trade prints for every option symbol on one base coin.

    Args:
      base_coin: Base coin, for example `BTC`. Unlike other categories, `option` subscribes per base coin rather than per symbol.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/websocket/public/trade)
    """
    return self.subscribe(
      f'publicTrade.{base_coin}',
      validate=validate,
      response_type=OptionPublicTrades,
    )
