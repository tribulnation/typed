# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class CreateFuturesComboBotResult(TypedDict):
  """Result of creating a futures combo bot."""

  status_code: int
  """`0` on success, `421` if the user is banned."""
  bot_id: int
  """Unique bot ID. Use for Get Detail and Close."""
  ban_reason_text: str
  """Localized ban reason. Populated only when `status_code` is `421`."""
  debug_msg: str
  """Debug message, populated on testnet."""


class FuturesComboSymbolSetting0SymbolSettingsItem(TypedDict):
  """One symbol's portfolio weight and direction."""

  symbol: str
  """Trading pair symbol, for example `BTCUSDT`."""
  target_position_percent: str
  """Target portfolio weight as a decimal ratio, for example `"0.5"` means 50%."""
  side: Literal[1, 2]
  """Position direction."""


class FuturesComboSymbolSetting1SymbolSettingsItem(TypedDict):
  """One symbol's portfolio weight and direction."""

  symbol: str
  """Trading pair symbol, for example `BTCUSDT`."""
  target_position_percent: str
  """Target portfolio weight as a decimal ratio, for example `"0.5"` means 50%."""
  side: Literal[1, 2]
  """Position direction."""


class FuturesComboSymbolSetting2SymbolSettingsItem(TypedDict):
  """One symbol's portfolio weight and direction."""

  symbol: str
  """Trading pair symbol, for example `BTCUSDT`."""
  target_position_percent: str
  """Target portfolio weight as a decimal ratio, for example `"0.5"` means 50%."""
  side: Literal[1, 2]
  """Position direction."""


class FuturesComboSymbolSetting3SymbolSettingsItem(TypedDict):
  """One symbol's portfolio weight and direction."""

  symbol: str
  """Trading pair symbol, for example `BTCUSDT`."""
  target_position_percent: str
  """Target portfolio weight as a decimal ratio, for example `"0.5"` means 50%."""
  side: Literal[1, 2]
  """Position direction."""


class CreateFuturesComboBotManualAdjustRequest(TypedDict):
  """Manual, on-settings-change, or on-transfer rebalancing (`adjust_position_mode` `4`, `5` or `6`); no interval or drift threshold required."""

  leverage: str
  """Position leverage multiplier, for example `"5"` means 5x. Must be >= 1."""
  init_margin: str
  """Initial investment in quote currency, as a decimal string, for example `"1000"` for 1000 USDT."""
  adjust_position_mode: Literal[4, 5, 6]
  """Rebalancing trigger mode."""
  symbol_settings: list[FuturesComboSymbolSetting3SymbolSettingsItem]
  """Per-symbol portfolio configuration. At least one entry required. All target_position_percent values must sum to 1."""
  sl_percent: NotRequired[str]
  """Stop-loss as a decimal ratio of total margin, for example `"0.2"` means close when loss reaches 20%."""
  tp_percent: NotRequired[str]
  """Take-profit as a decimal ratio of total margin, for example `"0.5"` means close when profit reaches 50%."""
  trailing_stop_percent: NotRequired[str]
  """Trailing stop callback as a decimal ratio, for example `"0.05"` means 5%."""


class CreateFuturesComboBotPercentAdjustRequest(TypedDict):
  """Percentage-based rebalancing (`adjust_position_mode` `2`)."""

  leverage: str
  """Position leverage multiplier, for example `"5"` means 5x. Must be >= 1."""
  init_margin: str
  """Initial investment in quote currency, as a decimal string, for example `"1000"` for 1000 USDT."""
  adjust_position_mode: Literal[2]
  """Rebalancing trigger mode."""
  symbol_settings: list[FuturesComboSymbolSetting1SymbolSettingsItem]
  """Per-symbol portfolio configuration. At least one entry required. All target_position_percent values must sum to 1."""
  adjust_position_percent: str
  """Rebalancing drift threshold as a decimal ratio, range [0.01, 0.5], for example `"0.05"` means rebalance when allocation drifts 5%. Required when `adjust_position_mode` includes percentage."""
  sl_percent: NotRequired[str]
  """Stop-loss as a decimal ratio of total margin, for example `"0.2"` means close when loss reaches 20%."""
  tp_percent: NotRequired[str]
  """Take-profit as a decimal ratio of total margin, for example `"0.5"` means close when profit reaches 50%."""
  trailing_stop_percent: NotRequired[str]
  """Trailing stop callback as a decimal ratio, for example `"0.05"` means 5%."""


class CreateFuturesComboBotTimeAdjustRequest(TypedDict):
  """Time-based rebalancing (`adjust_position_mode` `1`)."""

  leverage: str
  """Position leverage multiplier, for example `"5"` means 5x. Must be >= 1."""
  init_margin: str
  """Initial investment in quote currency, as a decimal string, for example `"1000"` for 1000 USDT."""
  adjust_position_mode: Literal[1]
  """Rebalancing trigger mode."""
  symbol_settings: list[FuturesComboSymbolSetting0SymbolSettingsItem]
  """Per-symbol portfolio configuration. At least one entry required. All target_position_percent values must sum to 1."""
  adjust_position_time_interval: int
  """Rebalancing time interval in seconds. Required when `adjust_position_mode` includes time."""
  sl_percent: NotRequired[str]
  """Stop-loss as a decimal ratio of total margin, for example `"0.2"` means close when loss reaches 20%."""
  tp_percent: NotRequired[str]
  """Take-profit as a decimal ratio of total margin, for example `"0.5"` means close when profit reaches 50%."""
  trailing_stop_percent: NotRequired[str]
  """Trailing stop callback as a decimal ratio, for example `"0.05"` means 5%."""


class CreateFuturesComboBotTimeOrPercentAdjustRequest(TypedDict):
  """Time-or-percentage rebalancing (`adjust_position_mode` `3`)."""

  leverage: str
  """Position leverage multiplier, for example `"5"` means 5x. Must be >= 1."""
  init_margin: str
  """Initial investment in quote currency, as a decimal string, for example `"1000"` for 1000 USDT."""
  adjust_position_mode: Literal[3]
  """Rebalancing trigger mode."""
  symbol_settings: list[FuturesComboSymbolSetting2SymbolSettingsItem]
  """Per-symbol portfolio configuration. At least one entry required. All target_position_percent values must sum to 1."""
  adjust_position_percent: str
  """Rebalancing drift threshold as a decimal ratio, range [0.01, 0.5], for example `"0.05"` means rebalance when allocation drifts 5%. Required when `adjust_position_mode` includes percentage."""
  adjust_position_time_interval: int
  """Rebalancing time interval in seconds. Required when `adjust_position_mode` includes time."""
  sl_percent: NotRequired[str]
  """Stop-loss as a decimal ratio of total margin, for example `"0.2"` means close when loss reaches 20%."""
  tp_percent: NotRequired[str]
  """Take-profit as a decimal ratio of total margin, for example `"0.5"` means close when profit reaches 50%."""
  trailing_stop_percent: NotRequired[str]
  """Trailing stop callback as a decimal ratio, for example `"0.05"` means 5%."""


Request = (
  CreateFuturesComboBotTimeAdjustRequest
  | CreateFuturesComboBotPercentAdjustRequest
  | CreateFuturesComboBotTimeOrPercentAdjustRequest
  | CreateFuturesComboBotManualAdjustRequest
)


class Create(RpcEndpoint):
  """Create a new futures combo trading bot that manages a portfolio of multiple futures symbols with automatic position rebalancing. Subject to compliance wall, GEO IP check and KYC verification."""

  async def create(
    self,
    request: Request,
    *,
    validate: bool | None = None,
  ) -> CreateFuturesComboBotResult:
    """Create a new futures combo trading bot that manages a portfolio of multiple futures symbols with automatic position rebalancing. Subject to compliance wall, GEO IP check and KYC verification.

    Args:
      request: Futures combo bot creation parameters, discriminated by `adjust_position_mode`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/bot/futures-combo/create)
    """
    return await self.request(
      request,
      method='POST',
      path='/v5/fcombobot/create',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CreateFuturesComboBotResult,
    )
