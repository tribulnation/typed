# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class CloseFuturesComboBotResult(TypedDict):
  """Result of closing a futures combo bot."""

  status_code: int
  """`0` on success, non-zero on error."""
  debug_msg: str
  """Debug message, populated on testnet."""


class Request(TypedDict):
  """Futures combo bot close parameters."""

  bot_id: str
  """Bot ID to close, obtained from the Create Futures Combo Bot response."""


class Close(RpcEndpoint):
  """Close a running futures combo trading bot. The bot cancels all pending orders and closes all positions across the portfolio. Only bots in a running state can be closed."""

  async def close(
    self,
    bot_id: str,
    *,
    validate: bool | None = None,
  ) -> CloseFuturesComboBotResult:
    """Close a running futures combo trading bot. The bot cancels all pending orders and closes all positions across the portfolio. Only bots in a running state can be closed.

    Args:
      bot_id: Bot ID to close, obtained from the Create Futures Combo Bot response.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/bot/futures-combo/close)
    """
    return await self.request(
      Request(bot_id=bot_id),
      method='POST',
      path='/v5/fcombobot/close',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CloseFuturesComboBotResult,
    )
