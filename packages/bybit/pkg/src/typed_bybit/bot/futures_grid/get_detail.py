# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_bybit.core import TimestampMillis
from typed_bybit.core.http import RpcEndpoint


class FuturesGridBotDetail(TypedDict):
  """Futures grid bot detail."""

  bot_id: str
  """Unique grid bot ID."""
  symbol: str
  """Trading pair, for example `BTCUSDT`."""
  status: str
  """Bot lifecycle status: Unspecified, Rejected, New, Initializing, Running, Cancelling, Completed or Await activation."""
  base_token: str
  """Base coin, for example `BTC`."""
  quote_token: str
  """Quote coin, for example `USDT`."""
  min_price: str
  """Grid lower price bound."""
  max_price: str
  """Grid upper price bound."""
  cell_number: int
  """Number of grid levels."""
  grid_type: str
  """Grid spacing type: Unspecified, Arithmetic or Geometric."""
  grid_mode: str
  """Strategy direction: Unspecified, Neutral, Long or Short."""
  leverage: str
  """Initial leverage multiplier, for example `"5"` means 5x."""
  real_leverage: str
  """Actual effective leverage, for example `"4.8"` means 4.8x."""
  total_investment: str
  """Total investment including additional top-ups."""
  total_value: NotRequired[str]
  """Total position value in quote currency."""
  current_position: NotRequired[str]
  """Current position size in contracts."""
  pnl: str
  """Cumulative total PnL in quote currency."""
  pnl_per: str
  """PnL as a decimal ratio of total investment, for example `"0.0505"` means 5.05%."""
  grid_profit: str
  """Profit from grid trades only."""
  grid_apr: str
  """Annualized grid profit rate as a decimal ratio."""
  total_apr: str
  """Total annualized profit rate as a decimal ratio."""
  realised_pnl: NotRequired[str]
  """Cumulative realized PnL."""
  unrealised_pnl: NotRequired[str]
  """Unrealized PnL based on mark price."""
  funding_fee: NotRequired[str]
  """Cumulative funding fee; positive means received, negative means paid."""
  position_balance: NotRequired[str]
  """Position margin balance."""
  available_balance: NotRequired[str]
  """Available (free) margin balance."""
  total_order_balance: NotRequired[str]
  """Margin locked in pending orders."""
  equity: str
  """Running equity value."""
  last_price: NotRequired[str]
  """Current last traded price."""
  mark_price: NotRequired[str]
  """Current mark price."""
  liquidation_price: NotRequired[str]
  """Estimated liquidation price."""
  stop_loss_per: NotRequired[str]
  """Stop-loss as a decimal ratio, for example `"0.1"` means 10%."""
  take_profit_per: NotRequired[str]
  """Take-profit as a decimal ratio, for example `"0.2"` means 20%."""
  stop_loss_price: NotRequired[str]
  """Stop-loss price."""
  take_profit_price: NotRequired[str]
  """Take-profit price."""
  tp_sl_type: str
  """TP/SL trigger mode: Unspecified, Both by percentage, Both by price, TP price + SL percentage, or TP percentage + SL price."""
  entry_price: str
  """Entry trigger price."""
  real_close_price: NotRequired[str]
  """Actual price when the bot was stopped."""
  min_price_precision: str
  """Minimum price precision for the symbol."""
  tick_size: str
  """Tick size, the minimum price increment."""
  trailing_stop_per: str
  """Trailing stop callback as a decimal ratio, for example `"0.05"` means 5%."""
  trailing_stop_exit_equity: str
  """Equity at trailing stop exit."""
  close_reason: NotRequired[str]
  """Reason the bot stopped, for example init failure, user stopped, take-profit/stop-loss triggered, liquidation, contract delisted."""
  bot_close_code: str
  """UI close code describing how the bot was closed."""
  futures_pos_side: str
  """Position side: Unspecified, Short position or Long position."""
  arbitrage_num: int
  """Total arbitrage (grid fill) count."""
  arbitrage_num_24: int
  """Arbitrage count in the last 24 hours."""
  min_profit: str
  """Minimum per-grid profit rate as a decimal ratio."""
  max_profit: str
  """Maximum per-grid profit rate as a decimal ratio."""
  move_up_price: NotRequired[str]
  """Move-up max price for grid shifting."""
  move_down_price: NotRequired[str]
  """Move-down min price for grid shifting."""
  curr_min_price: NotRequired[str]
  """Current effective minimum grid price."""
  curr_max_price: NotRequired[str]
  """Current effective maximum grid price."""
  i_m_rate: NotRequired[str]
  """Initial margin rate as a decimal ratio, for example `"0.1"` means 10%."""
  m_m_rate: NotRequired[str]
  """Maintenance margin rate as a decimal ratio, for example `"0.005"` means 0.5%."""
  account_type: str
  """Account funding source: Unspecified, Derivative account, Unified margin account, Account upgrading, Spot account, UTA or Fund account."""
  allow_follow: int
  """Whether copy-trading is allowed: `1` yes, `0` no."""
  follow_num: int
  """Number of users copying this bot."""
  copy_trade_is_master: bool
  """Whether this bot is a copy-trade master."""
  copy_trade_follower_num: str
  """Number of copy-trade followers."""
  used_reward_amount: str
  """Total voucher/reward amount used."""
  used_reward_id: str
  """ID of the first insurance voucher used."""
  init_bonus: str
  """Initial bonus amount."""
  used_bonus_amount: str
  """Bonus amount already consumed."""
  settlement_assets: str
  """Settlement asset information on close."""
  cum_withdrew_amount: str
  """Cumulative withdrawn margin amount."""
  current_profit: NotRequired[str]
  """Current running PnL."""
  current_per: NotRequired[str]
  """Current running PnL as a decimal ratio, for example `"0.032"` means 3.2%."""
  taker_fee_rate: str
  """Taker fee rate as a decimal ratio, for example `"0.0006"` means 0.06%."""
  maker_fee_rate: str
  """Maker fee rate as a decimal ratio, for example `"0.0001"` means 0.01%."""
  operation_time: str
  """Milliseconds since bot creation."""
  create_time: TimestampMillis
  """Bot creation time, as a Unix-milliseconds decimal string."""
  modify_time: TimestampMillis
  """Last update time, as a Unix-milliseconds decimal string."""
  end_time: TimestampMillis
  """Bot end time, as a Unix-milliseconds decimal string. `"0"` if still running."""
  adl_rank_indicator: NotRequired[int]
  """Auto-deleveraging rank indicator."""


class Request(TypedDict):
  """Futures grid bot detail query parameters."""

  bot_id: str
  """Bot ID to query, obtained from the Create Futures Grid Bot response or a bot listing endpoint."""


class GetFuturesGridBotDetailResult(TypedDict):
  """Result of querying a futures grid bot's detail."""

  status_code: int
  """`0` on success, `404` when `bot_id` does not exist, or non-zero on other errors."""
  debug_msg: str
  """Debug message, for example a `[...] grid not found`-style note or testnet debug info."""
  detail: FuturesGridBotDetail | None
  """Grid bot detail, or `null` when `bot_id` does not exist."""


class GetDetail(RpcEndpoint):
  """Retrieve comprehensive details for a specific futures grid bot, including configuration, status, PnL metrics, position info, margin balances, and timestamps."""

  async def get_detail(
    self,
    bot_id: str,
    *,
    validate: bool | None = None,
  ) -> GetFuturesGridBotDetailResult:
    """Retrieve comprehensive details for a specific futures grid bot, including configuration, status, PnL metrics, position info, margin balances, and timestamps.

    Args:
      bot_id: Bot ID to query, obtained from the Create Futures Grid Bot response or a bot listing endpoint.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/bot/futures-grid/get-detail)
    """
    return await self.request(
      Request(bot_id=bot_id),
      method='POST',
      path='/v5/fgridbot/detail',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=GetFuturesGridBotDetailResult,
    )
