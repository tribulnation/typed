# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_bybit.core.http import RpcEndpoint


class CreateFuturesGridBotNoTpSlRequest(TypedDict):
  """No take-profit/stop-loss configured (`tp_sl_type` omitted)."""

  symbol: str
  """Trading pair symbol, uppercase only, for example `BTCUSDT`."""
  grid_mode: Literal[1, 2, 3]
  """Strategy direction."""
  min_price: str
  """Lower price bound of the grid range, as a decimal string."""
  max_price: str
  """Upper price bound of the grid range, as a decimal string."""
  cell_number: int
  """Number of grid levels, minimum 2."""
  leverage: str
  """Position leverage multiplier, for example `"5"` means 5x. Must be >= 1."""
  grid_type: Literal[1, 2]
  """Grid spacing type."""
  total_investment: str
  """Initial investment in quote currency, as a decimal string, for example `"1000"` for 1000 USDT."""
  entry_price: NotRequired[str]
  """Entry trigger price for delayed activation, as a decimal string."""
  trailing_stop_per: NotRequired[str]
  """Trailing stop exit as a decimal ratio, for example `"0.05"` means 5%."""
  move_up_price: NotRequired[str]
  """Move-up price for grid shifting, as a decimal string. Not applicable when `grid_type` is `2`."""
  move_down_price: NotRequired[str]
  """Move-down price for grid shifting, as a decimal string. Not applicable when `grid_type` is `2`."""


class CreateFuturesGridBotPercentTpPriceSlRequest(TypedDict):
  """Percentage-based take-profit, price-based stop-loss (`tp_sl_type` `4`)."""

  symbol: str
  """Trading pair symbol, uppercase only, for example `BTCUSDT`."""
  grid_mode: Literal[1, 2, 3]
  """Strategy direction."""
  min_price: str
  """Lower price bound of the grid range, as a decimal string."""
  max_price: str
  """Upper price bound of the grid range, as a decimal string."""
  cell_number: int
  """Number of grid levels, minimum 2."""
  leverage: str
  """Position leverage multiplier, for example `"5"` means 5x. Must be >= 1."""
  grid_type: Literal[1, 2]
  """Grid spacing type."""
  total_investment: str
  """Initial investment in quote currency, as a decimal string, for example `"1000"` for 1000 USDT."""
  take_profit_per: str
  """Take-profit as a decimal ratio, for example `"0.2"` means 20%. Used when `tp_sl_type` includes percentage-based take-profit."""
  stop_loss_price: str
  """Stop-loss trigger price, as a decimal string. Used when `tp_sl_type` includes price-based stop-loss."""
  tp_sl_type: Literal[4]
  """TP/SL trigger mode."""
  entry_price: NotRequired[str]
  """Entry trigger price for delayed activation, as a decimal string."""
  trailing_stop_per: NotRequired[str]
  """Trailing stop exit as a decimal ratio, for example `"0.05"` means 5%."""
  move_up_price: NotRequired[str]
  """Move-up price for grid shifting, as a decimal string. Not applicable when `grid_type` is `2`."""
  move_down_price: NotRequired[str]
  """Move-down price for grid shifting, as a decimal string. Not applicable when `grid_type` is `2`."""


class CreateFuturesGridBotPercentTpSlRequest(TypedDict):
  """Percentage-based take-profit and stop-loss (`tp_sl_type` `1`)."""

  symbol: str
  """Trading pair symbol, uppercase only, for example `BTCUSDT`."""
  grid_mode: Literal[1, 2, 3]
  """Strategy direction."""
  min_price: str
  """Lower price bound of the grid range, as a decimal string."""
  max_price: str
  """Upper price bound of the grid range, as a decimal string."""
  cell_number: int
  """Number of grid levels, minimum 2."""
  leverage: str
  """Position leverage multiplier, for example `"5"` means 5x. Must be >= 1."""
  grid_type: Literal[1, 2]
  """Grid spacing type."""
  total_investment: str
  """Initial investment in quote currency, as a decimal string, for example `"1000"` for 1000 USDT."""
  take_profit_per: str
  """Take-profit as a decimal ratio, for example `"0.2"` means 20%. Used when `tp_sl_type` includes percentage-based take-profit."""
  stop_loss_per: str
  """Stop-loss as a decimal ratio, for example `"0.1"` means 10%. Used when `tp_sl_type` includes percentage-based stop-loss."""
  tp_sl_type: Literal[1]
  """TP/SL trigger mode."""
  entry_price: NotRequired[str]
  """Entry trigger price for delayed activation, as a decimal string."""
  trailing_stop_per: NotRequired[str]
  """Trailing stop exit as a decimal ratio, for example `"0.05"` means 5%."""
  move_up_price: NotRequired[str]
  """Move-up price for grid shifting, as a decimal string. Not applicable when `grid_type` is `2`."""
  move_down_price: NotRequired[str]
  """Move-down price for grid shifting, as a decimal string. Not applicable when `grid_type` is `2`."""


class CreateFuturesGridBotPriceTpPercentSlRequest(TypedDict):
  """Price-based take-profit, percentage-based stop-loss (`tp_sl_type` `3`)."""

  symbol: str
  """Trading pair symbol, uppercase only, for example `BTCUSDT`."""
  grid_mode: Literal[1, 2, 3]
  """Strategy direction."""
  min_price: str
  """Lower price bound of the grid range, as a decimal string."""
  max_price: str
  """Upper price bound of the grid range, as a decimal string."""
  cell_number: int
  """Number of grid levels, minimum 2."""
  leverage: str
  """Position leverage multiplier, for example `"5"` means 5x. Must be >= 1."""
  grid_type: Literal[1, 2]
  """Grid spacing type."""
  total_investment: str
  """Initial investment in quote currency, as a decimal string, for example `"1000"` for 1000 USDT."""
  stop_loss_per: str
  """Stop-loss as a decimal ratio, for example `"0.1"` means 10%. Used when `tp_sl_type` includes percentage-based stop-loss."""
  take_profit_price: str
  """Take-profit trigger price, as a decimal string. Used when `tp_sl_type` includes price-based take-profit."""
  tp_sl_type: Literal[3]
  """TP/SL trigger mode."""
  entry_price: NotRequired[str]
  """Entry trigger price for delayed activation, as a decimal string."""
  trailing_stop_per: NotRequired[str]
  """Trailing stop exit as a decimal ratio, for example `"0.05"` means 5%."""
  move_up_price: NotRequired[str]
  """Move-up price for grid shifting, as a decimal string. Not applicable when `grid_type` is `2`."""
  move_down_price: NotRequired[str]
  """Move-down price for grid shifting, as a decimal string. Not applicable when `grid_type` is `2`."""


class CreateFuturesGridBotPriceTpSlRequest(TypedDict):
  """Price-based take-profit and stop-loss (`tp_sl_type` `2`)."""

  symbol: str
  """Trading pair symbol, uppercase only, for example `BTCUSDT`."""
  grid_mode: Literal[1, 2, 3]
  """Strategy direction."""
  min_price: str
  """Lower price bound of the grid range, as a decimal string."""
  max_price: str
  """Upper price bound of the grid range, as a decimal string."""
  cell_number: int
  """Number of grid levels, minimum 2."""
  leverage: str
  """Position leverage multiplier, for example `"5"` means 5x. Must be >= 1."""
  grid_type: Literal[1, 2]
  """Grid spacing type."""
  total_investment: str
  """Initial investment in quote currency, as a decimal string, for example `"1000"` for 1000 USDT."""
  take_profit_price: str
  """Take-profit trigger price, as a decimal string. Used when `tp_sl_type` includes price-based take-profit."""
  stop_loss_price: str
  """Stop-loss trigger price, as a decimal string. Used when `tp_sl_type` includes price-based stop-loss."""
  tp_sl_type: Literal[2]
  """TP/SL trigger mode."""
  entry_price: NotRequired[str]
  """Entry trigger price for delayed activation, as a decimal string."""
  trailing_stop_per: NotRequired[str]
  """Trailing stop exit as a decimal ratio, for example `"0.05"` means 5%."""
  move_up_price: NotRequired[str]
  """Move-up price for grid shifting, as a decimal string. Not applicable when `grid_type` is `2`."""
  move_down_price: NotRequired[str]
  """Move-down price for grid shifting, as a decimal string. Not applicable when `grid_type` is `2`."""


class CreateFuturesGridBotResult(TypedDict):
  """Result of creating a futures grid bot."""

  status_code: int
  """`0` on success, `421` if the user is banned."""
  bot_id: int
  """Unique bot ID. Use for Get Detail and Close."""
  check_code: str
  """Validation result. `FGRID_CHECK_CODE_SUCCESS` means OK; see bot.futures_grid.validate_input for the check-code vocabulary."""
  ban_reason_text: str
  """Ban reason in the user's locale. Populated only when `status_code` is `421`."""
  debug_msg: str
  """Debug message, populated on testnet."""


Request = (
  CreateFuturesGridBotNoTpSlRequest
  | CreateFuturesGridBotPercentTpSlRequest
  | CreateFuturesGridBotPriceTpSlRequest
  | CreateFuturesGridBotPriceTpPercentSlRequest
  | CreateFuturesGridBotPercentTpPriceSlRequest
)


class Create(RpcEndpoint):
  """Create a new futures grid trading bot that automatically places grid orders within a specified price range. Subject to compliance wall and KYC verification."""

  async def create(
    self,
    request: Request,
    *,
    validate: bool | None = None,
  ) -> CreateFuturesGridBotResult:
    """Create a new futures grid trading bot that automatically places grid orders within a specified price range. Subject to compliance wall and KYC verification.

    Args:
      request: Futures grid bot creation parameters, discriminated by `tp_sl_type`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/bot/futures-grid/create)
    """
    return await self.request(
      request,
      method='POST',
      path='/v5/fgridbot/create',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CreateFuturesGridBotResult,
    )
