# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_bybit.core.http import RpcEndpoint


class CloseFuturesMartingaleBotResult(TypedDict):
  """Result of closing a futures Martingale bot."""

  status_code: int
  """`0` on success, non-zero on error."""
  debug_msg: str
  """Debug message, populated on testnet."""


class Request(TypedDict):
  """Futures Martingale bot close parameters."""

  bot_id: str
  """Bot ID to close, obtained from the Create Futures Martingale Bot response."""


class Close(RpcEndpoint):
  """Close a running futures Martingale trading bot. The bot cancels all pending orders and closes the position. Only bots in a running state can be closed."""

  async def close(
    self,
    bot_id: str,
    *,
    validate: bool | None = None,
  ) -> CloseFuturesMartingaleBotResult:
    """Close a running futures Martingale trading bot. The bot cancels all pending orders and closes the position. Only bots in a running state can be closed.

    Args:
      bot_id: Bot ID to close, obtained from the Create Futures Martingale Bot response.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://bybit-exchange.github.io/docs/v5/bot/futures-martingale/close)
    """
    return await self.request(
      Request(bot_id=bot_id),
      method='POST',
      path='/v5/fmartingalebot/close',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CloseFuturesMartingaleBotResult,
    )
