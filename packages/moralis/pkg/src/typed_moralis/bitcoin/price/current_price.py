# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_moralis.core import RpcEndpoint, TimestampIso, TimestampSeconds, clean_params
from pydantic import TypeAdapter


class DexInformation(TypedDict):
  """DEX information."""

  exchangeName: str
  """The name of the exchange."""
  exchangeAddress: str
  """The address of the exchange."""
  pairAddress: str
  """The pair address."""
  pairLiquidityUsd: float
  """The pair liquidity in USD."""


class Meta(TypedDict):
  """Meta."""

  syncedAt: TimestampSeconds
  """Synced at."""


class NativePrice(TypedDict):
  """Native price."""

  value: str
  """Value."""
  valueRaw: str
  """Value raw."""
  decimals: int
  """Decimals."""
  name: str
  """Name."""
  symbol: str
  """Symbol."""


class UpdatedAt(TypedDict):
  """Updated at."""

  timestamp: TimestampIso
  """Timestamp."""
  blockNumber: int
  """Block number."""


CurrentPriceResponseKeywords = TypedDict(
  'CurrentPriceResponseKeywords', {'24hrChangeUsd': int, '24hrChangePercent': float}
)
"""
  - `24hrChangeUsd`: 24hr change USD.
  - `24hrChangePercent`: 24hr change percent.
"""


class CurrentPriceResponse(CurrentPriceResponseKeywords):
  """Schema inferred from a live Moralis response captured by spec collection."""

  chain: str
  """Chain."""
  tokenName: str
  """Token name."""
  tokenSymbol: str
  """Token symbol."""
  tokenAddress: str | None
  """Token address."""
  tokenLogo: str
  """Token logo."""
  tokenDecimals: int
  """Token decimals."""
  usdPrice: int
  """USD price."""
  nativePrice: NativePrice
  verifiedContract: bool
  """Verified contract."""
  securityScore: int
  """Security score."""
  possibleSpam: bool
  """Possible spam."""
  updatedAt: UpdatedAt
  dexInformation: DexInformation | None
  """DEX information."""
  meta: Meta


current_price_adapter = TypeAdapter(CurrentPriceResponse)


class CurrentPrice(RpcEndpoint):
  async def current_price(
    self,
    chain_alias: Literal[
      '0x1',
      'ethereum',
      '0x171',
      'pulse',
      '0x2105',
      'base',
      '0x38',
      'binance',
      '0x89',
      'polygon',
      '0xa',
      'optimism',
      '0xa86a',
      'avalanche',
      'bitcoin-mainnet',
      'bitcoin',
      'solana-mainnet',
      'sol',
    ],
    token_alias_or_token_address: str,
    *,
    to_block: float | None = None,
    max_token_inactivity: float | None = None,
    liquidity_threshold: float | None = None,
    validate: bool | None = None,
  ) -> CurrentPriceResponse:
    """Get the current price and additional information for a specific token address.

    Args:
      chain_alias: The alias of the chain.
      token_alias_or_token_address: The token address or alias
      to_block: Block number to check the price from
      max_token_inactivity: Exclude tokens inactive for more than the specified days
      liquidity_threshold: Minimum liquidity in USD
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/data-api/bitcoin/price/current-price)
    """
    params = clean_params(
      {
        'toBlock': to_block,
        'maxTokenInactivity': max_token_inactivity,
        'liquidityThreshold': liquidity_threshold,
      }
    )
    response = await self.request(
      'GET',
      f'/v1/chains/{chain_alias}/tokens/{token_alias_or_token_address}/price',
      params=params,
    )
    if self.should_validate(validate):
      return current_price_adapter.validate_json(response.text)
    return response.json()
