# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_moralis.core import TimestampIso, TimestampSeconds
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import BitcoinPricePoint


class Meta(TypedDict):
  """Meta."""

  syncedAt: TimestampSeconds
  """Synced at."""


class Request(TypedDict):
  chainAlias: Literal[
    '0x1',
    'ethereum',
    '0x171',
    'pulse',
    '0x2105',
    'base',
    '0x38',
    'binance',
    '0x89',
    'polygon',
    '0xa',
    'optimism',
    '0xa86a',
    'avalanche',
    'bitcoin-mainnet',
    'bitcoin',
    'solana-mainnet',
    'sol',
  ]
  """The alias of the chain."""
  tokenAliasOrTokenAddress: str
  """The token address or alias"""
  limit: NotRequired[float]
  """The limit per page"""
  cursor: NotRequired[str]
  """The cursor to the next page"""
  interval: NotRequired[Literal['5m', 'hourly', 'daily', 'weekly', 'monthly']]
  """The time interval for price data points. Defaults to 5m if not provided."""
  fromDate: NotRequired[str]
  """The starting date (format in seconds or datestring accepted by momentjs). Defaults to 24 hours before toDate if not provided."""
  toDate: NotRequired[str]
  """The ending date (format in seconds or datestring accepted by momentjs). Defaults to now if not provided."""
  forwardFill: NotRequired[bool]
  """When true, fills gaps in the time series with interpolated values"""


class HistoricalPriceResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  chain: str
  """Chain."""
  fromDate: TimestampIso
  """From date."""
  toDate: TimestampIso
  """To date."""
  interval: str
  """Interval."""
  page: int
  """Page."""
  cursor: str | None
  """Cursor."""
  result: list[BitcoinPricePoint]
  """Result."""
  meta: Meta


class HistoricalPrice(DeepIndexEndpoint):
  """Get historical price data points for a specific token address, suitable for rendering line charts. Supports various time intervals from 5 minutes to monthly."""

  def historical_price_paged(
    self,
    chain_alias: Literal[
      '0x1',
      'ethereum',
      '0x171',
      'pulse',
      '0x2105',
      'base',
      '0x38',
      'binance',
      '0x89',
      'polygon',
      '0xa',
      'optimism',
      '0xa86a',
      'avalanche',
      'bitcoin-mainnet',
      'bitcoin',
      'solana-mainnet',
      'sol',
    ],
    *,
    token_alias_or_token_address: str,
    limit: float | None = None,
    interval: Literal['5m', 'hourly', 'daily', 'weekly', 'monthly'] | None = None,
    from_date: str | None = None,
    to_date: str | None = None,
    forward_fill: bool | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[BitcoinPricePoint, str]:
    """Get historical price data points for a specific token address, suitable for rendering line charts. Supports various time intervals from 5 minutes to monthly.

    Paged variant of `historical_price`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      chain_alias: The alias of the chain.
      token_alias_or_token_address: The token address or alias
      limit: The limit per page
      interval: The time interval for price data points. Defaults to 5m if not provided.
      from_date: The starting date (format in seconds or datestring accepted by momentjs). Defaults to 24 hours before toDate if not provided.
      to_date: The ending date (format in seconds or datestring accepted by momentjs). Defaults to now if not provided.
      forward_fill: When true, fills gaps in the time series with interpolated values
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/bitcoin/price/historical-price)
    """

    async def next(cursor: str) -> tuple[list[BitcoinPricePoint], str | None]:
      response = await self.historical_price(
        chain_alias,
        token_alias_or_token_address=token_alias_or_token_address,
        limit=limit,
        interval=interval,
        from_date=from_date,
        to_date=to_date,
        forward_fill=forward_fill,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('result')
      state = response.get('cursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def historical_price(
    self,
    chain_alias: Literal[
      '0x1',
      'ethereum',
      '0x171',
      'pulse',
      '0x2105',
      'base',
      '0x38',
      'binance',
      '0x89',
      'polygon',
      '0xa',
      'optimism',
      '0xa86a',
      'avalanche',
      'bitcoin-mainnet',
      'bitcoin',
      'solana-mainnet',
      'sol',
    ],
    *,
    token_alias_or_token_address: str,
    limit: float | None = None,
    cursor: str | None = None,
    interval: Literal['5m', 'hourly', 'daily', 'weekly', 'monthly'] | None = None,
    from_date: str | None = None,
    to_date: str | None = None,
    forward_fill: bool | None = None,
    validate: bool | None = None,
  ) -> HistoricalPriceResponse:
    """Get historical price data points for a specific token address, suitable for rendering line charts. Supports various time intervals from 5 minutes to monthly.

    Args:
      chain_alias: The alias of the chain.
      token_alias_or_token_address: The token address or alias
      limit: The limit per page
      cursor: The cursor to the next page
      interval: The time interval for price data points. Defaults to 5m if not provided.
      from_date: The starting date (format in seconds or datestring accepted by momentjs). Defaults to 24 hours before toDate if not provided.
      to_date: The ending date (format in seconds or datestring accepted by momentjs). Defaults to now if not provided.
      forward_fill: When true, fills gaps in the time series with interpolated values
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/bitcoin/price/historical-price)
    """
    request: Request = Request(
      chainAlias=chain_alias, tokenAliasOrTokenAddress=token_alias_or_token_address
    )
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    if interval is not None:
      request['interval'] = interval
    if from_date is not None:
      request['fromDate'] = from_date
    if to_date is not None:
      request['toDate'] = to_date
    if forward_fill is not None:
      request['forwardFill'] = forward_fill
    return await self.request(
      request,
      method='GET',
      path='/v1/chains/{chainAlias}/tokens/{tokenAliasOrTokenAddress}/price/timeseries',
      validate=validate,
      request_type=Request,
      response_type=HistoricalPriceResponse,
    )
