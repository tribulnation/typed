# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .blockchain import Blockchain
from .price import Price
from .utility import Utility
from .wallet import Wallet
from typed_moralis.core.rest import DeepIndexEndpoint


class Bitcoin(DeepIndexEndpoint):
  """Bitcoin Data API — raw blocks and transactions, market prices with sparklines, wallet address history and balances, and native xpub-key utilities.

  References:
    - [Upstream docs](https://docs.moralis.com/data-api/bitcoin/overview)
  """

  @cached_property
  def blockchain(self) -> Blockchain:
    """Raw Bitcoin blocks and transactions — block headers, full transaction payloads, inputs, outputs, and UTXO-level detail.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/bitcoin/blockchain/overview)
    """
    return Blockchain(client=self.client)

  @cached_property
  def price(self) -> Price:
    """BTC market data, served through Moralis's Universal Token Price endpoints via the `bitcoin` chain alias and `native` token alias.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/bitcoin/price/overview)
    """
    return Price(client=self.client)

  @cached_property
  def utility(self) -> Utility:
    """Native Bitcoin xpub tooling — deriving every address (and its transfer count) from a Bitcoin extended public key.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/bitcoin/utility/overview)
    """
    return Utility(client=self.client)

  @cached_property
  def wallet(self) -> Wallet:
    """Transaction history and token balances for any Bitcoin address or xpub.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/bitcoin/wallet/overview)
    """
    return Wallet(client=self.client)
