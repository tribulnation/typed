# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint


class Request(TypedDict):
  walletAddressOrPublicKey: str
  """The address or public key of the account"""
  chains: NotRequired[
    list[
      Literal[
        '0x1',
        'ethereum',
        '0x171',
        'pulse',
        '0x2105',
        'base',
        '0x38',
        'binance',
        '0x530',
        'sei-testnet',
        '0x531',
        'sei',
        '0x89',
        'polygon',
        '0x8f',
        'monad',
        '0xa',
        'optimism',
        '0xa86a',
        'avalanche',
        'all',
        'mainnets',
        'testnets',
        'bitcoin',
      ]
    ]
  ]
  """Chains to query"""
  cursor: NotRequired[str]
  """The cursor to the next page"""
  limit: NotRequired[float]
  """The limit per page"""
  tokenAddresses: NotRequired[list[str]]
  """The token addresses to be filtered in"""
  excludeSpam: NotRequired[bool]
  """Should exclude spam contract"""
  excludeUnverifiedContracts: NotRequired[bool]
  """Should exclude unverified contract"""
  excludeNative: NotRequired[bool]
  """Should exclude native contract"""
  maxTokenInactivity: NotRequired[float]
  """Should filter out contract exceeding max token inactivity"""
  liquidityThreshold: NotRequired[float]
  """Should filter out contract under liquidity threshold"""


class Result(TypedDict):
  tokenAddress: str
  """Token address."""
  chainId: str
  """Chain ID."""
  balanceRaw: str
  """Balance raw."""
  balance: str
  """Balance."""
  name: str
  """Name."""
  symbol: str
  """Symbol."""
  decimals: int
  """Decimals."""
  logo: str
  """Logo."""
  possibleSpam: bool
  """Possible spam."""
  verifiedContract: bool
  """Verified contract."""
  nativeToken: bool
  """Native token."""
  securityScore: int
  """Security score."""
  portfolioPercentage: int
  """Portfolio percentage."""
  usdPrice: int
  """USD price."""
  usdPrice24hrPercentChange: float
  """USD price24hr percent change."""
  usdPrice24hrUsdChange: int
  """USD price24hr USD change."""
  usdValue: float
  """USD value."""
  usdValue24hrUsdChange: float | None
  """USD value24hr USD change."""
  derivedAddress: str | None
  """Derived address."""
  derivedPath: str | None
  """Derived path."""
  sparkline: dict[str, Any] | None
  """Sparkline."""


SyncedAtKeywords = TypedDict('SyncedAtKeywords', {'bitcoin-mainnet': int})
"""
- `bitcoin-mainnet`: Bitcoin mainnet.
"""


class SyncedAt(SyncedAtKeywords):
  """Synced at."""


class Meta(TypedDict):
  """Meta."""

  syncedAt: SyncedAt
  unsupportedChains: list[str]
  """Unsupported chains."""
  failedChains: list[str] | None
  """Failed chains."""


class BtcBalancesResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  address: str
  """Address."""
  addressType: str
  """Address type."""
  cursor: str | None
  """Cursor."""
  meta: Meta
  result: list[Result]
  """Result."""


class BtcBalances(DeepIndexEndpoint):
  """Get token balances from multiple chains for a specific wallet address."""

  def btc_balances_paged(
    self,
    *,
    wallet_address_or_public_key: str,
    chains: list[
      Literal[
        '0x1',
        'ethereum',
        '0x171',
        'pulse',
        '0x2105',
        'base',
        '0x38',
        'binance',
        '0x530',
        'sei-testnet',
        '0x531',
        'sei',
        '0x89',
        'polygon',
        '0x8f',
        'monad',
        '0xa',
        'optimism',
        '0xa86a',
        'avalanche',
        'all',
        'mainnets',
        'testnets',
        'bitcoin',
      ]
    ]
    | None = None,
    limit: float | None = None,
    token_addresses: list[str] | None = None,
    exclude_spam: bool | None = None,
    exclude_unverified_contracts: bool | None = None,
    exclude_native: bool | None = None,
    max_token_inactivity: float | None = None,
    liquidity_threshold: float | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[Result, str]:
    """Get token balances from multiple chains for a specific wallet address.

    Paged variant of `btc_balances`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      wallet_address_or_public_key: The address or public key of the account
      chains: Chains to query
      limit: The limit per page
      token_addresses: The token addresses to be filtered in
      exclude_spam: Should exclude spam contract
      exclude_unverified_contracts: Should exclude unverified contract
      exclude_native: Should exclude native contract
      max_token_inactivity: Should filter out contract exceeding max token inactivity
      liquidity_threshold: Should filter out contract under liquidity threshold
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/bitcoin/wallet/address-balances)
    """

    async def next(cursor: str) -> tuple[list[Result], str | None]:
      response = await self.btc_balances(
        wallet_address_or_public_key=wallet_address_or_public_key,
        chains=chains,
        limit=limit,
        token_addresses=token_addresses,
        exclude_spam=exclude_spam,
        exclude_unverified_contracts=exclude_unverified_contracts,
        exclude_native=exclude_native,
        max_token_inactivity=max_token_inactivity,
        liquidity_threshold=liquidity_threshold,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('result')
      state = response.get('cursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def btc_balances(
    self,
    *,
    wallet_address_or_public_key: str,
    chains: list[
      Literal[
        '0x1',
        'ethereum',
        '0x171',
        'pulse',
        '0x2105',
        'base',
        '0x38',
        'binance',
        '0x530',
        'sei-testnet',
        '0x531',
        'sei',
        '0x89',
        'polygon',
        '0x8f',
        'monad',
        '0xa',
        'optimism',
        '0xa86a',
        'avalanche',
        'all',
        'mainnets',
        'testnets',
        'bitcoin',
      ]
    ]
    | None = None,
    cursor: str | None = None,
    limit: float | None = None,
    token_addresses: list[str] | None = None,
    exclude_spam: bool | None = None,
    exclude_unverified_contracts: bool | None = None,
    exclude_native: bool | None = None,
    max_token_inactivity: float | None = None,
    liquidity_threshold: float | None = None,
    validate: bool | None = None,
  ) -> BtcBalancesResponse:
    """Get token balances from multiple chains for a specific wallet address.

    Args:
      wallet_address_or_public_key: The address or public key of the account
      chains: Chains to query
      cursor: The cursor to the next page
      limit: The limit per page
      token_addresses: The token addresses to be filtered in
      exclude_spam: Should exclude spam contract
      exclude_unverified_contracts: Should exclude unverified contract
      exclude_native: Should exclude native contract
      max_token_inactivity: Should filter out contract exceeding max token inactivity
      liquidity_threshold: Should filter out contract under liquidity threshold
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/bitcoin/wallet/address-balances)
    """
    request: Request = Request(walletAddressOrPublicKey=wallet_address_or_public_key)
    if chains is not None:
      request['chains'] = chains
    if cursor is not None:
      request['cursor'] = cursor
    if limit is not None:
      request['limit'] = limit
    if token_addresses is not None:
      request['tokenAddresses'] = token_addresses
    if exclude_spam is not None:
      request['excludeSpam'] = exclude_spam
    if exclude_unverified_contracts is not None:
      request['excludeUnverifiedContracts'] = exclude_unverified_contracts
    if exclude_native is not None:
      request['excludeNative'] = exclude_native
    if max_token_inactivity is not None:
      request['maxTokenInactivity'] = max_token_inactivity
    if liquidity_threshold is not None:
      request['liquidityThreshold'] = liquidity_threshold
    return await self.request(
      request,
      method='GET',
      path='/v1/wallets/{walletAddressOrPublicKey}/tokens',
      validate=validate,
      request_type=Request,
      response_type=BtcBalancesResponse,
    )
