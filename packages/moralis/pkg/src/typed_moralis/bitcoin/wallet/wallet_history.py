# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_moralis.core import TimestampIso
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import BitcoinHistoryTxIo, BitcoinTransactionParty


class Block(TypedDict):
  """Block."""

  number: int
  """Number."""
  timestamp: TimestampIso
  """Timestamp."""
  hash: str
  """Hash."""


class Fee(TypedDict):
  """Fee."""

  amountRaw: str
  """Amount raw."""
  amount: str
  """Amount."""


class Item(TypedDict):
  fromAddress: str
  """From address."""
  toAddress: str
  """To address."""
  amount: str
  """Amount."""
  direction: str
  """Direction."""
  isInternal: bool
  """Is internal."""


class ItemMeta(TypedDict):
  """Meta."""


class NftTransfers(TypedDict):
  """NFT transfers."""

  items: list[dict[str, Any]]
  """Items."""
  hasMore: bool
  """Has more."""


class Request(TypedDict):
  walletAddressOrPublicKey: str
  """The address or public key of the account"""
  chains: NotRequired[
    list[
      Literal[
        '0x1',
        'ethereum',
        '0x171',
        'pulse',
        '0x2105',
        'base',
        '0x38',
        'binance',
        '0x530',
        'sei-testnet',
        '0x531',
        'sei',
        '0x89',
        'polygon',
        '0x8f',
        'monad',
        '0xa',
        'optimism',
        '0xa86a',
        'avalanche',
        'solana-mainnet',
        'sol',
        'all',
        'mainnets',
        'testnets',
        'bitcoin',
      ]
    ]
  ]
  """Chains to query"""
  limit: NotRequired[float]
  """The limit per page"""
  cursor: NotRequired[str]
  """The cursor to the next page"""
  order: NotRequired[Literal['ASC', 'DESC']]
  """The order of items"""
  fromDate: NotRequired[str]
  """The start date from which to get the wallet history (format in seconds or string accepted by momentjs)
* Provide the param 'fromBlock' or 'fromDate'
* If 'fromDate' and 'fromBlock' are provided, 'fromBlock' will be used."""
  toDate: NotRequired[str]
  """The end date from which to get the wallet history (format in seconds or string accepted by momentjs)
* Provide the param 'toBlock' or 'toDate'
* If 'toDate' and 'toBlock' are provided, 'toBlock' will be used."""
  fromBlock: NotRequired[float]
  """The minimum block number from which to get the wallet history
* Provide the param 'fromBlock' or 'fromDate'
* If 'fromDate' and 'fromBlock' are provided, 'fromBlock' will be used."""
  toBlock: NotRequired[float]
  """The block number to get the wallet history until"""
  verbose: NotRequired[bool]
  """When true, returns full raw chain-specific data including logs, input data, and vin/vout arrays. Array fields are capped at 100 items with a hasMore flag."""


SyncedAtKeywords = TypedDict('SyncedAtKeywords', {'bitcoin-mainnet': int})
"""
- `bitcoin-mainnet`: Bitcoin mainnet.
"""


class SyncedAt(SyncedAtKeywords):
  """Synced at."""


class TokenTransfers(TypedDict):
  """Token transfers."""

  items: list[dict[str, Any]]
  """Items."""
  hasMore: bool
  """Has more."""


class Bitcoin(TypedDict):
  """Bitcoin."""

  version: int
  """Version."""
  size: int
  """Size."""
  vsize: int
  """Vsize."""
  coinbase: bool
  """Coinbase."""
  rbf: bool
  """Rbf."""
  inputCount: int
  """Input count."""
  outputCount: int
  """Output count."""
  vin: list[BitcoinHistoryTxIo] | None
  """Vin."""
  vout: list[BitcoinHistoryTxIo] | None
  """Vout."""


class Meta(TypedDict):
  """Meta."""

  syncedAt: SyncedAt
  unsupportedChains: list[str] | None
  """Unsupported chains."""
  failedChains: list[str] | None
  """Failed chains."""


class NativeTransfers(TypedDict):
  """Native transfers."""

  items: list[Item]
  """Items."""
  hasMore: bool
  """Has more."""


class Raw(TypedDict):
  """Raw."""

  bitcoin: Bitcoin


TransactionKeywords = TypedDict(
  'TransactionKeywords', {'from': BitcoinTransactionParty}
)


class Transaction(TransactionKeywords):
  """Transaction."""

  hash: str
  """Hash."""
  index: int | None
  """Index."""
  status: str | None
  """Status."""
  direction: str
  """Direction."""
  category: str | None
  """Category."""
  summary: str | None
  """Summary."""
  methodLabel: str | None
  """Method label."""
  possibleSpam: bool | None
  """Possible spam."""
  confirmations: int
  """Confirmations."""
  initiatedBy: BitcoinTransactionParty | None
  """Initiated by."""
  to: BitcoinTransactionParty
  fee: Fee
  nativeTransfers: NativeTransfers
  tokenTransfers: TokenTransfers
  nftTransfers: NftTransfers


class Result(TypedDict):
  chainId: str
  """Chain ID."""
  block: Block
  transaction: Transaction
  raw: Raw
  meta: ItemMeta


class WalletHistoryResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  address: str
  """Address."""
  addressType: str
  """Address type."""
  cursor: str | None
  """Cursor."""
  page: int
  """Page."""
  pageSize: int
  """Page size."""
  result: list[Result]
  """Result."""
  meta: Meta


class WalletHistory(DeepIndexEndpoint):
  """Get enriched wallet transaction history using ClickHouse pointers and Envelope Service hydration.

  **Supported chain families:**
  - **EVM** — Ethereum, Polygon, BSC, etc. Includes native, ERC-20, ERC-721, and ERC-1155 transfers.
  - **Bitcoin** — Native BTC transfers only.
  - **Solana** — Native SOL transfers.

  Cross-family requests (e.g. EVM address with Bitcoin chain) return 400. When no `chains` param is provided, all mainnets for the detected address family are queried."""

  def wallet_history_paged(
    self,
    *,
    wallet_address_or_public_key: str,
    chains: list[
      Literal[
        '0x1',
        'ethereum',
        '0x171',
        'pulse',
        '0x2105',
        'base',
        '0x38',
        'binance',
        '0x530',
        'sei-testnet',
        '0x531',
        'sei',
        '0x89',
        'polygon',
        '0x8f',
        'monad',
        '0xa',
        'optimism',
        '0xa86a',
        'avalanche',
        'solana-mainnet',
        'sol',
        'all',
        'mainnets',
        'testnets',
        'bitcoin',
      ]
    ]
    | None = None,
    limit: float | None = None,
    order: Literal['ASC', 'DESC'] | None = None,
    from_date: str | None = None,
    to_date: str | None = None,
    from_block: float | None = None,
    to_block: float | None = None,
    verbose: bool | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[Result, str]:
    """Get enriched wallet transaction history using ClickHouse pointers and Envelope Service hydration.

    **Supported chain families:**
    - **EVM** — Ethereum, Polygon, BSC, etc. Includes native, ERC-20, ERC-721, and ERC-1155 transfers.
    - **Bitcoin** — Native BTC transfers only.
    - **Solana** — Native SOL transfers.

    Cross-family requests (e.g. EVM address with Bitcoin chain) return 400. When no `chains` param is provided, all mainnets for the detected address family are queried.

    Paged variant of `wallet_history`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      wallet_address_or_public_key: The address or public key of the account
      chains: Chains to query
      limit: The limit per page
      order: The order of items
      from_date: The start date from which to get the wallet history (format in seconds or string accepted by momentjs)
    * Provide the param 'fromBlock' or 'fromDate'
    * If 'fromDate' and 'fromBlock' are provided, 'fromBlock' will be used.
      to_date: The end date from which to get the wallet history (format in seconds or string accepted by momentjs)
    * Provide the param 'toBlock' or 'toDate'
    * If 'toDate' and 'toBlock' are provided, 'toBlock' will be used.
      from_block: The minimum block number from which to get the wallet history
    * Provide the param 'fromBlock' or 'fromDate'
    * If 'fromDate' and 'fromBlock' are provided, 'fromBlock' will be used.
      to_block: The block number to get the wallet history until
      verbose: When true, returns full raw chain-specific data including logs, input data, and vin/vout arrays. Array fields are capped at 100 items with a hasMore flag.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/bitcoin/wallet/address-transactions)
    """

    async def next(cursor: str) -> tuple[list[Result], str | None]:
      response = await self.wallet_history(
        wallet_address_or_public_key=wallet_address_or_public_key,
        chains=chains,
        limit=limit,
        order=order,
        from_date=from_date,
        to_date=to_date,
        from_block=from_block,
        to_block=to_block,
        verbose=verbose,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('result')
      state = response.get('cursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def wallet_history(
    self,
    *,
    wallet_address_or_public_key: str,
    chains: list[
      Literal[
        '0x1',
        'ethereum',
        '0x171',
        'pulse',
        '0x2105',
        'base',
        '0x38',
        'binance',
        '0x530',
        'sei-testnet',
        '0x531',
        'sei',
        '0x89',
        'polygon',
        '0x8f',
        'monad',
        '0xa',
        'optimism',
        '0xa86a',
        'avalanche',
        'solana-mainnet',
        'sol',
        'all',
        'mainnets',
        'testnets',
        'bitcoin',
      ]
    ]
    | None = None,
    limit: float | None = None,
    cursor: str | None = None,
    order: Literal['ASC', 'DESC'] | None = None,
    from_date: str | None = None,
    to_date: str | None = None,
    from_block: float | None = None,
    to_block: float | None = None,
    verbose: bool | None = None,
    validate: bool | None = None,
  ) -> WalletHistoryResponse:
    """Get enriched wallet transaction history using ClickHouse pointers and Envelope Service hydration.

    **Supported chain families:**
    - **EVM** — Ethereum, Polygon, BSC, etc. Includes native, ERC-20, ERC-721, and ERC-1155 transfers.
    - **Bitcoin** — Native BTC transfers only.
    - **Solana** — Native SOL transfers.

    Cross-family requests (e.g. EVM address with Bitcoin chain) return 400. When no `chains` param is provided, all mainnets for the detected address family are queried.

    Args:
      wallet_address_or_public_key: The address or public key of the account
      chains: Chains to query
      limit: The limit per page
      cursor: The cursor to the next page
      order: The order of items
      from_date: The start date from which to get the wallet history (format in seconds or string accepted by momentjs)
    * Provide the param 'fromBlock' or 'fromDate'
    * If 'fromDate' and 'fromBlock' are provided, 'fromBlock' will be used.
      to_date: The end date from which to get the wallet history (format in seconds or string accepted by momentjs)
    * Provide the param 'toBlock' or 'toDate'
    * If 'toDate' and 'toBlock' are provided, 'toBlock' will be used.
      from_block: The minimum block number from which to get the wallet history
    * Provide the param 'fromBlock' or 'fromDate'
    * If 'fromDate' and 'fromBlock' are provided, 'fromBlock' will be used.
      to_block: The block number to get the wallet history until
      verbose: When true, returns full raw chain-specific data including logs, input data, and vin/vout arrays. Array fields are capped at 100 items with a hasMore flag.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/bitcoin/wallet/address-transactions)
    """
    request: Request = Request(walletAddressOrPublicKey=wallet_address_or_public_key)
    if chains is not None:
      request['chains'] = chains
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    if order is not None:
      request['order'] = order
    if from_date is not None:
      request['fromDate'] = from_date
    if to_date is not None:
      request['toDate'] = to_date
    if from_block is not None:
      request['fromBlock'] = from_block
    if to_block is not None:
      request['toBlock'] = to_block
    if verbose is not None:
      request['verbose'] = verbose
    return await self.request(
      request,
      method='GET',
      path='/v1/wallets/{walletAddressOrPublicKey}/history',
      validate=validate,
      request_type=Request,
      response_type=WalletHistoryResponse,
    )
