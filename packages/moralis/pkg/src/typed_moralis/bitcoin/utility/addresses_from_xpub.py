# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint


class Address(TypedDict):
  address: str
  """The derived Bitcoin address"""
  path: str
  """BIP44 derivation path"""
  transfers: float
  """Number of transactions involving this address"""


class Meta(TypedDict):
  """Meta."""

  syncedAt: dict[str, float]
  """Last ingested block number per chain ID"""


class Request(TypedDict):
  publicKey: str
  """The Bitcoin extended public key (xpub)"""
  chainAlias: Literal['bitcoin-mainnet', 'bitcoin']
  """The alias of the chain."""


class AddressesFromXpubResponse(TypedDict):
  addresses: list[Address]
  """Derived addresses from the xpub key"""
  meta: Meta


class AddressesFromXpub(DeepIndexEndpoint):
  """Returns all derived addresses and their transfer counts for a given Bitcoin xpub key."""

  async def addresses_from_xpub(
    self,
    public_key: str,
    *,
    chain_alias: Literal['bitcoin-mainnet', 'bitcoin'],
    validate: bool | None = None,
  ) -> AddressesFromXpubResponse:
    """Returns all derived addresses and their transfer counts for a given Bitcoin xpub key.

    Args:
      public_key: The Bitcoin extended public key (xpub)
      chain_alias: The alias of the chain.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/bitcoin/utility/addresses-from-xpub)
    """
    return await self.request(
      Request(publicKey=public_key, chainAlias=chain_alias),
      method='GET',
      path='/v1/chains/{chainAlias}/wallets/{publicKey}/addresses',
      validate=validate,
      request_type=Request,
      response_type=AddressesFromXpubResponse,
    )
