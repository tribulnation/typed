# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.core.rest import AuthEndpoint


class Request(TypedDict):
  profileId: str
  """Unique identifier with a length of 66 characters"""


Response = list[str]


class AddressesThatAreBoundToTheSpecificProfileid(AuthEndpoint):
  """Get addresses that are bound to the specific profileId"""

  async def addresses_that_are_bound_to_the_specific_profileid(
    self,
    profile_id: str,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Get addresses that are bound to the specific profileId

    Args:
      profile_id: Unique identifier with a length of 66 characters
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/api-reference/profile/get-addresses-that-are-bound-to-the-specific-profileid)
    """
    return await self.request(
      Request(profileId=profile_id),
      method='GET',
      path='/profile/{profileId}/addresses',
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
