# Generated by typed-dev — see CONTRIBUTING.md.
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter

addresses_that_are_bound_to_the_specific_profileid_adapter = TypeAdapter(list[str])


class AddressesThatAreBoundToTheSpecificProfileid(RpcEndpoint):
  async def addresses_that_are_bound_to_the_specific_profileid(
    self,
    profile_id: str,
    *,
    validate: bool | None = None,
  ) -> list[str]:
    """Get addresses that are bound to the specific profileId

    Args:
      profile_id: Unique identifier with a length of 66 characters
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/api-reference/profile/get-addresses-that-are-bound-to-the-specific-profileid)
    """
    response = await self.request('GET', f'/profile/{profile_id}/addresses')
    if self.should_validate(validate):
      return addresses_that_are_bound_to_the_specific_profileid_adapter.validate_json(
        response.text
      )
    return response.json()
