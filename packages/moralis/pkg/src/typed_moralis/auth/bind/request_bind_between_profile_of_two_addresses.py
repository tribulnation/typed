# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_moralis.core.rest import AuthEndpoint


class Address(TypedDict):
  blockchainType: Literal['evm', 'solana', 'aptos']
  """The chain in which the address belongs to"""
  address: str
  """Address performing the signing conformant to capitalization encoded checksum specified in EIP-55 where applicable."""
  publicKey: NotRequired[str]
  """Public key performing the signing conformant. (This is only needed for Aptos address)"""


class RequestBindBetweenProfileOfTwoAddressesResponse(TypedDict):
  messages: list[str]
  """Message that needs to be signed by the end user"""


class Request(TypedDict):
  addresses: list[Address]
  """An array of addresses that needs to be bind"""


class RequestBindBetweenProfileOfTwoAddresses(AuthEndpoint):
  """Request for message to bind profile that is belong to the two addresses<br>
  All profiles under the addresses will be bound and new profile will be generated."""

  async def request_bind_between_profile_of_two_addresses(
    self,
    addresses: list[Address],
    *,
    validate: bool | None = None,
  ) -> RequestBindBetweenProfileOfTwoAddressesResponse:
    """Request for message to bind profile that is belong to the two addresses<br>
            All profiles under the addresses will be bound and new profile will be generated.

    Args:
      addresses: An array of addresses that needs to be bind
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/api-reference/bind/request-bind-between-profile-of-two-addresses)
    """
    return await self.request(
      Request(addresses=addresses),
      method='POST',
      path='/bind/request',
      validate=validate,
      request_type=Request,
      response_type=RequestBindBetweenProfileOfTwoAddressesResponse,
    )
