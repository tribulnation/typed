# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter


class Address(TypedDict):
  blockchainType: Literal['evm', 'solana', 'aptos']
  """The chain in which the address belongs to"""
  address: str
  """Address performing the signing conformant to capitalization encoded checksum specified in EIP-55 where applicable."""
  publicKey: NotRequired[str]
  """Public key performing the signing conformant. (This is only needed for Aptos address)"""


class RequestBindBetweenProfileOfTwoAddressesResponse(TypedDict):
  messages: list[str]
  """Message that needs to be signed by the end user"""


class RequestBindBetweenProfileOfTwoAddressesRequest(TypedDict):
  addresses: list[Address]
  """An array of addresses that needs to be bind"""


request_bind_between_profile_of_two_addresses_adapter = TypeAdapter(
  RequestBindBetweenProfileOfTwoAddressesResponse
)


class RequestBindBetweenProfileOfTwoAddresses(RpcEndpoint):
  async def request_bind_between_profile_of_two_addresses(
    self,
    request_bind_between_profile_of_two_addresses_request: RequestBindBetweenProfileOfTwoAddressesRequest,
    *,
    validate: bool | None = None,
  ) -> RequestBindBetweenProfileOfTwoAddressesResponse:
    """Request for message to bind profile that is belong to the two addresses<br>
            All profiles under the addresses will be bound and new profile will be generated.

    Args:
      request_bind_between_profile_of_two_addresses_request: The two addresses that are required to be bind.
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/api-reference/bind/request-bind-between-profile-of-two-addresses)
    """
    response = await self.request(
      'POST',
      '/bind/request',
      json=request_bind_between_profile_of_two_addresses_request,
    )
    if self.should_validate(validate):
      return request_bind_between_profile_of_two_addresses_adapter.validate_json(
        response.text
      )
    return response.json()
