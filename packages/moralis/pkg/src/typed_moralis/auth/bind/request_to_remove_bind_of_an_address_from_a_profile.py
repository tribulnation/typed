# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_moralis.core.rest import AuthEndpoint


class Request(TypedDict):
  blockchainType: Literal['evm', 'solana', 'aptos']
  """The chain in which the address belongs to"""
  address: str
  """Address performing the signing conformant to capitalization encoded checksum specified in EIP-55 where applicable."""
  publicKey: NotRequired[str]
  """Public key performing the signing conformant. (This is only needed for Aptos address)"""
  profileId: str
  """Unique identifier with a length of 66 characters"""


class RequestToRemoveBindOfAnAddressFromAProfileResponse(TypedDict):
  message: str
  """Message that needs to be signed by the end user"""


class RequestToRemoveBindOfAnAddressFromAProfile(AuthEndpoint):
  """Request to remove bind of an address from a profile"""

  async def request_to_remove_bind_of_an_address_from_a_profile(
    self,
    blockchain_type: Literal['evm', 'solana', 'aptos'],
    *,
    address: str,
    public_key: str | None = None,
    profile_id: str,
    validate: bool | None = None,
  ) -> RequestToRemoveBindOfAnAddressFromAProfileResponse:
    """Request to remove bind of an address from a profile

    Args:
      blockchain_type: The chain in which the address belongs to
      address: Address performing the signing conformant to capitalization encoded checksum specified in EIP-55 where applicable.
      public_key: Public key performing the signing conformant. (This is only needed for Aptos address)
      profile_id: Unique identifier with a length of 66 characters
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/api-reference/bind/request-to-remove-bind-of-an-address-from-a-profile)
    """
    request: Request = Request(
      blockchainType=blockchain_type, address=address, profileId=profile_id
    )
    if public_key is not None:
      request['publicKey'] = public_key
    return await self.request(
      request,
      method='POST',
      path='/bind/remove',
      validate=validate,
      request_type=Request,
      response_type=RequestToRemoveBindOfAnAddressFromAProfileResponse,
    )
