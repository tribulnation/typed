# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.core.rest import AuthEndpoint


class Request(TypedDict):
  message: str
  """Message that needs to be signed by the end user"""
  signature: str
  """EIP-191 compliant signature signed by the Ethereum account address requesting authentication."""


class VerifyRemoveBindRequestResponse(TypedDict):
  profileId: str
  """Unique identifier with a length of 66 characters"""


class VerifyRemoveBindRequest(AuthEndpoint):
  """Verify remove bind request"""

  async def verify_remove_bind_request(
    self,
    *,
    message: str,
    signature: str,
    validate: bool | None = None,
  ) -> VerifyRemoveBindRequestResponse:
    """Verify remove bind request

    Args:
      message: Message that needs to be signed by the end user
      signature: EIP-191 compliant signature signed by the Ethereum account address requesting authentication.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/api-reference/bind/verify-remove-bind-request)
    """
    return await self.request(
      Request(message=message, signature=signature),
      method='POST',
      path='/bind/remove/verify',
      validate=validate,
      request_type=Request,
      response_type=VerifyRemoveBindRequestResponse,
    )
