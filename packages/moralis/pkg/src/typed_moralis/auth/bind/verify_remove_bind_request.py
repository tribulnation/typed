# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter


class VerifyRemoveBindRequestRequest(TypedDict):
  message: str
  """Message that needs to be signed by the end user"""
  signature: str
  """EIP-191 compliant signature signed by the Ethereum account address requesting authentication."""


class VerifyRemoveBindRequestResponse(TypedDict):
  profileId: str
  """Unique identifier with a length of 66 characters"""


verify_remove_bind_request_adapter = TypeAdapter(VerifyRemoveBindRequestResponse)


class VerifyRemoveBindRequest(RpcEndpoint):
  async def verify_remove_bind_request(
    self,
    verify_remove_bind_request_request: VerifyRemoveBindRequestRequest,
    *,
    validate: bool | None = None,
  ) -> VerifyRemoveBindRequestResponse:
    """Verify remove bind request

    Args:
      verify_remove_bind_request_request: Messages and its signatures that is used for verification
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/api-reference/bind/verify-remove-bind-request)
    """
    response = await self.request(
      'POST',
      '/bind/remove/verify',
      json=verify_remove_bind_request_request,
    )
    if self.should_validate(validate):
      return verify_remove_bind_request_adapter.validate_json(response.text)
    return response.json()
