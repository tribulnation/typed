# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.core.rest import AuthEndpoint


class Verification(TypedDict):
  message: str
  """Message that needs to be signed by the end user"""
  signature: str
  """EIP-191 compliant signature signed by the Ethereum account address requesting authentication."""


class VerifyBindRequestResponse(TypedDict):
  profileId: str
  """Unique identifier with a length of 66 characters"""


class Request(TypedDict):
  verifications: list[Verification]
  """Message that needs to be signed by the end user"""


class VerifyBindRequest(AuthEndpoint):
  """Verify bind request"""

  async def verify_bind_request(
    self,
    verifications: list[Verification],
    *,
    validate: bool | None = None,
  ) -> VerifyBindRequestResponse:
    """Verify bind request

    Args:
      verifications: Message that needs to be signed by the end user
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/api-reference/bind/verify-bind-request)
    """
    return await self.request(
      Request(verifications=verifications),
      method='POST',
      path='/bind/request/verify',
      validate=validate,
      request_type=Request,
      response_type=VerifyBindRequestResponse,
    )
