# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .bind import Bind
from .challenge import Challenge
from .profile import Profile
from typed_moralis.core.rest import AuthEndpoint


class Auth(AuthEndpoint):
  """Auth API — Web3 wallet-signature authentication built on EIP-4361 (Sign-In with Ethereum, extended to Solana), issuing a chain-agnostic profileId and binding multiple wallet addresses to one profile.

  References:
    - [Upstream docs](https://docs.moralis.com/auth-api/overview)
  """

  @cached_property
  def bind(self) -> Bind:
    """Bind and unbind additional wallet addresses to an existing Auth API profileId: request/verify pairs for linking a second address to, or removing one from, one profile.

    References:
      - [Upstream docs](https://docs.moralis.com/auth-api/overview)
    """
    return Bind(client=self.client)

  @cached_property
  def challenge(self) -> Challenge:
    """Sign-In with Ethereum style challenge request/verify pairs, for EVM, Solana, and Aptos wallets, used to open a new Auth API session.

    References:
      - [Upstream docs](https://docs.moralis.com/auth-api/overview)
    """
    return Challenge(client=self.client)

  @cached_property
  def profile(self) -> Profile:
    """Lookup of the wallet addresses currently bound to a given Auth API profileId.

    References:
      - [Upstream docs](https://docs.moralis.com/auth-api/overview)
    """
    return Profile(client=self.client)
