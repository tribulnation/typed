# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_moralis.core.rest import StreamsEndpoint
from typed_moralis.schemas import SolanaStreamWebhookPayload


class Request(TypedDict):
  chainId: str
  """Chain ID the webhook event belongs to."""
  blockNumber: float
  """Block number to fetch webhook trigger data for."""
  tag: NotRequired[str]
  """A user-provided tag that will be send along the webhook"""
  allAddresses: NotRequired[bool]
  """Include events for all addresses"""
  addresses: NotRequired[list[str]]
  """Solana addresses to filter by"""
  programIds: NotRequired[list[str]]
  """Solana program IDs to filter by"""
  mintAddresses: NotRequired[list[str]]
  """Solana token mint addresses to filter by"""


Response = SolanaStreamWebhookPayload | None


class WebhookDataByBlockNumber(StreamsEndpoint):
  """Get Webhook Data by Block Number"""

  async def webhook_data_by_block_number(
    self,
    *,
    chain_id: str,
    block_number: float,
    tag: str | None = None,
    all_addresses: bool | None = None,
    addresses: list[str] | None = None,
    program_ids: list[str] | None = None,
    mint_addresses: list[str] | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get Webhook Data by Block Number

    Args:
      chain_id: Chain ID the webhook event belongs to.
      block_number: Block number to fetch webhook trigger data for.
      tag: A user-provided tag that will be send along the webhook
      all_addresses: Include events for all addresses
      addresses: Solana addresses to filter by
      program_ids: Solana program IDs to filter by
      mint_addresses: Solana token mint addresses to filter by
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/streams/api-reference/solana/streams/get-webhook-data-by-block-number)
    """
    request: Request = Request(chainId=chain_id, blockNumber=block_number)
    if tag is not None:
      request['tag'] = tag
    if all_addresses is not None:
      request['allAddresses'] = all_addresses
    if addresses is not None:
      request['addresses'] = addresses
    if program_ids is not None:
      request['programIds'] = program_ids
    if mint_addresses is not None:
      request['mintAddresses'] = mint_addresses
    return await self.request(
      request,
      method='POST',
      path='/streams/solana/{chainId}/block/{blockNumber}',
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
