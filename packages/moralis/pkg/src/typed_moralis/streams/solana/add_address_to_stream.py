# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.schemas import StreamAddress
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter


class AddAddressToStreamRequest(TypedDict):
  address: str | list[str]


add_address_to_stream_adapter = TypeAdapter(StreamAddress)


class AddAddressToStream(RpcEndpoint):
  async def add_address_to_stream(
    self,
    id: str,
    add_address_to_stream_request: AddAddressToStreamRequest,
    *,
    validate: bool | None = None,
  ) -> StreamAddress:
    """Add Address to Stream

    Args:
      id: Stream ID.
      add_address_to_stream_request: Request payload for add Address to Stream.
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/streams/api-reference/solana/address/add-address-to-stream)
    """
    response = await self.request(
      'POST',
      f'/streams/solana/{id}/address',
      json=add_address_to_stream_request,
    )
    if self.should_validate(validate):
      return add_address_to_stream_adapter.validate_json(response.text)
    return response.json()
