# Generated by typed-dev — see CONTRIBUTING.md.
from typed_moralis.schemas import SolanaStream, SolanaStreamCreateRequest
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter

create_stream_adapter = TypeAdapter(SolanaStream)


class CreateStream(RpcEndpoint):
  async def create_stream(
    self,
    solana_stream_create_request: SolanaStreamCreateRequest,
    *,
    validate: bool | None = None,
  ) -> SolanaStream:
    """Create Stream

    Args:
      solana_stream_create_request: Request payload for create Stream.
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/streams/api-reference/solana/streams/create-streams)
    """
    response = await self.request(
      'PUT',
      '/streams/solana',
      json=solana_stream_create_request,
    )
    if self.should_validate(validate):
      return create_stream_adapter.validate_json(response.text)
    return response.json()
