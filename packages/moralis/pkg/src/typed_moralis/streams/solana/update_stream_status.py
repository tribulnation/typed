# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_moralis.core.rest import StreamsEndpoint
from typed_moralis.schemas import StreamStatusUpdate


class Request(TypedDict):
  id: str
  """Stream ID."""
  status: Literal['active', 'paused', 'error', 'terminated']
  """The status of the stream."""


class UpdateStreamStatus(StreamsEndpoint):
  """Update Stream Status"""

  async def update_stream_status(
    self,
    id: str,
    *,
    status: Literal['active', 'paused', 'error', 'terminated'],
    validate: bool | None = None,
  ) -> StreamStatusUpdate:
    """Update Stream Status

    Args:
      id: Stream ID.
      status: The status of the stream.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/streams/api-reference/solana/streams/update-stream-status)
    """
    return await self.request(
      Request(id=id, status=status),
      method='POST',
      path='/streams/solana/{id}/status',
      validate=validate,
      request_type=Request,
      response_type=StreamStatusUpdate,
    )
