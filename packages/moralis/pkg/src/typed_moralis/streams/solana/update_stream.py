# Generated by typed-dev — see CONTRIBUTING.md.
from typed_moralis.schemas import SolanaStream, SolanaStreamUpdateRequest
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter

update_stream_adapter = TypeAdapter(SolanaStream)


class UpdateStream(RpcEndpoint):
  async def update_stream(
    self,
    id: str,
    solana_stream_update_request: SolanaStreamUpdateRequest,
    *,
    validate: bool | None = None,
  ) -> SolanaStream:
    """Update Stream

    Args:
      id: Stream ID.
      solana_stream_update_request: Request payload for update Stream.
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/streams/api-reference/solana/streams/update-stream)
    """
    response = await self.request(
      'POST',
      f'/streams/solana/{id}',
      json=solana_stream_update_request,
    )
    if self.should_validate(validate):
      return update_stream_adapter.validate_json(response.text)
    return response.json()
