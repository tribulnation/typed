# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_moralis.core.rest import StreamsEndpoint
from typed_moralis.schemas import SolanaStream


class Request(TypedDict):
  id: str
  """Stream ID."""
  allAddresses: NotRequired[bool]
  """Include events for all addresses"""
  description: NotRequired[str]
  """A description for this stream"""
  mintAddresses: NotRequired[list[str]]
  """Solana token mint addresses to filter transactions by"""
  network: NotRequired[list[Literal['mainnet', 'devnet']]]
  """The network to listen to"""
  programIds: NotRequired[list[str]]
  """Solana program IDs to filter transactions by"""
  tag: NotRequired[str]
  """A user-provided tag that will be send along the webhook"""
  webhookUrl: NotRequired[str]
  """Webhook URL where moralis will send the POST request."""


class UpdateStream(StreamsEndpoint):
  """Update Stream"""

  async def update_stream(
    self,
    *,
    id: str,
    all_addresses: bool | None = None,
    description: str | None = None,
    mint_addresses: list[str] | None = None,
    network: list[Literal['mainnet', 'devnet']] | None = None,
    program_ids: list[str] | None = None,
    tag: str | None = None,
    webhook_url: str | None = None,
    validate: bool | None = None,
  ) -> SolanaStream:
    """Update Stream

    Args:
      id: Stream ID.
      all_addresses: Include events for all addresses
      description: A description for this stream
      mint_addresses: Solana token mint addresses to filter transactions by
      network: The network to listen to
      program_ids: Solana program IDs to filter transactions by
      tag: A user-provided tag that will be send along the webhook
      webhook_url: Webhook URL where moralis will send the POST request.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/streams/api-reference/solana/streams/update-stream)
    """
    request: Request = Request(id=id)
    if all_addresses is not None:
      request['allAddresses'] = all_addresses
    if description is not None:
      request['description'] = description
    if mint_addresses is not None:
      request['mintAddresses'] = mint_addresses
    if network is not None:
      request['network'] = network
    if program_ids is not None:
      request['programIds'] = program_ids
    if tag is not None:
      request['tag'] = tag
    if webhook_url is not None:
      request['webhookUrl'] = webhook_url
    return await self.request(
      request,
      method='POST',
      path='/streams/solana/{id}',
      validate=validate,
      request_type=Request,
      response_type=SolanaStream,
    )
