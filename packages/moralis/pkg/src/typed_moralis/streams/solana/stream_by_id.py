# Generated by typed-dev — see CONTRIBUTING.md.
from typed_moralis.schemas import SolanaStream
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter

stream_by_id_adapter = TypeAdapter(SolanaStream)


class StreamById(RpcEndpoint):
  async def stream_by_id(
    self, id: str, *, validate: bool | None = None
  ) -> SolanaStream:
    """Get Stream by ID

    Args:
      id: Stream ID.
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/streams/api-reference/solana/streams/get-stream-by-id)
    """
    response = await self.request('GET', f'/streams/solana/{id}')
    if self.should_validate(validate):
      return stream_by_id_adapter.validate_json(response.text)
    return response.json()
