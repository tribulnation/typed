# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import NotRequired, TypedDict
from typed_moralis.core.rest import StreamsEndpoint
from typed_moralis.schemas import StreamHistoryRecord, StreamHistoryResponse


class Request(TypedDict):
  limit: float
  """Maximum number of results to return per page."""
  cursor: NotRequired[str]
  """Pagination cursor returned by a previous response."""
  transactionHash: NotRequired[str]
  """Filter by transaction hash."""
  excludePayload: NotRequired[bool]
  """Exclude the full decoded webhook payload from each history entry."""
  streamId: NotRequired[str]
  """Stream ID the webhook payload should be generated for."""
  chainId: NotRequired[list[str]]
  """Chain ID the webhook event belongs to."""
  blockNumber: NotRequired[list[float]]
  """Block number to fetch webhook trigger data for."""
  fromTimestamp: NotRequired[float]
  """Only include entries recorded at or after this Unix timestamp."""
  toTimestamp: NotRequired[float]
  """Only include entries recorded at or before this Unix timestamp."""


class Entries(StreamsEndpoint):
  """Get all history"""

  def entries_paged(
    self,
    *,
    limit: float,
    transaction_hash: str | None = None,
    exclude_payload: bool | None = None,
    stream_id: str | None = None,
    chain_id: list[str] | None = None,
    block_number: list[float] | None = None,
    from_timestamp: float | None = None,
    to_timestamp: float | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[StreamHistoryRecord, str]:
    """Get all history

    Paged variant of `entries`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      limit: Maximum number of results to return per page.
      transaction_hash: Filter by transaction hash.
      exclude_payload: Exclude the full decoded webhook payload from each history entry.
      stream_id: Stream ID the webhook payload should be generated for.
      chain_id: Chain ID the webhook event belongs to.
      block_number: Block number to fetch webhook trigger data for.
      from_timestamp: Only include entries recorded at or after this Unix timestamp.
      to_timestamp: Only include entries recorded at or before this Unix timestamp.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/streams/api-reference/history/get-history)
    """

    async def next(cursor: str) -> tuple[list[StreamHistoryRecord], str | None]:
      response = await self.entries(
        limit=limit,
        transaction_hash=transaction_hash,
        exclude_payload=exclude_payload,
        stream_id=stream_id,
        chain_id=chain_id,
        block_number=block_number,
        from_timestamp=from_timestamp,
        to_timestamp=to_timestamp,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('result')
      state = response.get('cursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def entries(
    self,
    *,
    limit: float,
    cursor: str | None = None,
    transaction_hash: str | None = None,
    exclude_payload: bool | None = None,
    stream_id: str | None = None,
    chain_id: list[str] | None = None,
    block_number: list[float] | None = None,
    from_timestamp: float | None = None,
    to_timestamp: float | None = None,
    validate: bool | None = None,
  ) -> StreamHistoryResponse:
    """Get all history

    Args:
      limit: Maximum number of results to return per page.
      cursor: Pagination cursor returned by a previous response.
      transaction_hash: Filter by transaction hash.
      exclude_payload: Exclude the full decoded webhook payload from each history entry.
      stream_id: Stream ID the webhook payload should be generated for.
      chain_id: Chain ID the webhook event belongs to.
      block_number: Block number to fetch webhook trigger data for.
      from_timestamp: Only include entries recorded at or after this Unix timestamp.
      to_timestamp: Only include entries recorded at or before this Unix timestamp.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/streams/api-reference/history/get-history)
    """
    request: Request = Request(limit=limit)
    if cursor is not None:
      request['cursor'] = cursor
    if transaction_hash is not None:
      request['transactionHash'] = transaction_hash
    if exclude_payload is not None:
      request['excludePayload'] = exclude_payload
    if stream_id is not None:
      request['streamId'] = stream_id
    if chain_id is not None:
      request['chainId'] = chain_id
    if block_number is not None:
      request['blockNumber'] = block_number
    if from_timestamp is not None:
      request['fromTimestamp'] = from_timestamp
    if to_timestamp is not None:
      request['toTimestamp'] = to_timestamp
    return await self.request(
      request,
      method='GET',
      path='/history',
      validate=validate,
      request_type=Request,
      response_type=StreamHistoryResponse,
    )
