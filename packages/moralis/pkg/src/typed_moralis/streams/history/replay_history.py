# Generated by typed-dev — see CONTRIBUTING.md.
from typed_moralis.schemas import StreamHistoryRecord
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter

replay_history_adapter = TypeAdapter(StreamHistoryRecord)


class ReplayHistory(RpcEndpoint):
  async def replay_history(
    self,
    stream_id: str,
    id: str,
    *,
    validate: bool | None = None,
  ) -> StreamHistoryRecord:
    """Replay a specific history.

    Args:
      stream_id: The id of the stream the history will be replayed
      id: The id of the history to replay
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/streams/api-reference/history/replay-history)
    """
    response = await self.request('POST', f'/history/replay/{stream_id}/{id}')
    if self.should_validate(validate):
      return replay_history_adapter.validate_json(response.text)
    return response.json()
