# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.core.rest import StreamsEndpoint
from typed_moralis.schemas import StreamHistoryRecord


class Request(TypedDict):
  streamId: str
  """The id of the stream the history will be replayed"""
  id: str
  """The id of the history to replay"""


class ReplayHistory(StreamsEndpoint):
  """Replay a specific history."""

  async def replay_history(
    self,
    *,
    stream_id: str,
    id: str,
    validate: bool | None = None,
  ) -> StreamHistoryRecord:
    """Replay a specific history.

    Args:
      stream_id: The id of the stream the history will be replayed
      id: The id of the history to replay
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/streams/api-reference/history/replay-history)
    """
    return await self.request(
      Request(streamId=stream_id, id=id),
      method='POST',
      path='/history/replay/{streamId}/{id}',
      validate=validate,
      request_type=Request,
      response_type=StreamHistoryRecord,
    )
