# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_moralis.schemas import StreamStatusUpdate
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter


class UpdateStreamStatusRequest(TypedDict):
  status: Literal['active', 'paused', 'error', 'terminated']
  """The status of the stream."""


update_stream_status_adapter = TypeAdapter(StreamStatusUpdate)


class UpdateStreamStatus(RpcEndpoint):
  async def update_stream_status(
    self,
    id: str,
    update_stream_status_request: UpdateStreamStatusRequest,
    *,
    validate: bool | None = None,
  ) -> StreamStatusUpdate:
    """Update Stream Status

    Args:
      id: Stream ID.
      update_stream_status_request: Request payload for update Stream Status.
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/streams/api-reference/bitcoin/streams/update-stream-status)
    """
    response = await self.request(
      'POST',
      f'/streams/bitcoin/{id}/status',
      json=update_stream_status_request,
    )
    if self.should_validate(validate):
      return update_stream_status_adapter.validate_json(response.text)
    return response.json()
