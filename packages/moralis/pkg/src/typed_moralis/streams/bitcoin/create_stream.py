# Generated by typed-dev — see CONTRIBUTING.md.
from typed_moralis.schemas import BitcoinStream, BitcoinStreamCreateRequest
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter

create_stream_adapter = TypeAdapter(BitcoinStream)


class CreateStream(RpcEndpoint):
  async def create_stream(
    self,
    bitcoin_stream_create_request: BitcoinStreamCreateRequest,
    *,
    validate: bool | None = None,
  ) -> BitcoinStream:
    """Create Stream

    Args:
      bitcoin_stream_create_request: Request payload for create Stream.
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/streams/api-reference/bitcoin/streams/create-streams)
    """
    response = await self.request(
      'PUT',
      '/streams/bitcoin',
      json=bitcoin_stream_create_request,
    )
    if self.should_validate(validate):
      return create_stream_adapter.validate_json(response.text)
    return response.json()
