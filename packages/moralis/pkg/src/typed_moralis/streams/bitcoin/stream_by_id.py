# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.core.rest import StreamsEndpoint
from typed_moralis.schemas import BitcoinStream


class Request(TypedDict):
  id: str
  """Stream ID."""


class StreamById(StreamsEndpoint):
  """Get Stream by ID"""

  async def stream_by_id(
    self, id: str, *, validate: bool | None = None
  ) -> BitcoinStream:
    """Get Stream by ID

    Args:
      id: Stream ID.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/streams/api-reference/bitcoin/streams/get-stream-by-id)
    """
    return await self.request(
      Request(id=id),
      method='GET',
      path='/streams/bitcoin/{id}',
      validate=validate,
      request_type=Request,
      response_type=BitcoinStream,
    )
