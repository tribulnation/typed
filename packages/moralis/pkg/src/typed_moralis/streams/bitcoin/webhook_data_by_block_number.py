# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_moralis.core.rest import StreamsEndpoint
from typed_moralis.schemas import BitcoinStreamWebhookPayload


class Request(TypedDict):
  chainId: str
  """Chain ID the webhook event belongs to."""
  blockNumber: float
  """Block number to fetch webhook trigger data for."""
  tag: NotRequired[str]
  """A user-provided tag that will be send along the webhook"""
  allAddresses: NotRequired[bool]
  """Include events for all addresses"""
  includeInputs: NotRequired[bool]
  """Include or not input details in webhook defaults to true"""
  includeOutputs: NotRequired[bool]
  """Include or not output details in webhook defaults to true"""
  addresses: NotRequired[list[str]]
  """Bitcoin addresses to filter by"""


Response = BitcoinStreamWebhookPayload | None


class WebhookDataByBlockNumber(StreamsEndpoint):
  """Get Webhook Data by Block Number"""

  async def webhook_data_by_block_number(
    self,
    *,
    chain_id: str,
    block_number: float,
    tag: str | None = None,
    all_addresses: bool | None = None,
    include_inputs: bool | None = None,
    include_outputs: bool | None = None,
    addresses: list[str] | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get Webhook Data by Block Number

    Args:
      chain_id: Chain ID the webhook event belongs to.
      block_number: Block number to fetch webhook trigger data for.
      tag: A user-provided tag that will be send along the webhook
      all_addresses: Include events for all addresses
      include_inputs: Include or not input details in webhook defaults to true
      include_outputs: Include or not output details in webhook defaults to true
      addresses: Bitcoin addresses to filter by
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/streams/api-reference/bitcoin/streams/get-webhook-data-by-block-number)
    """
    request: Request = Request(chainId=chain_id, blockNumber=block_number)
    if tag is not None:
      request['tag'] = tag
    if all_addresses is not None:
      request['allAddresses'] = all_addresses
    if include_inputs is not None:
      request['includeInputs'] = include_inputs
    if include_outputs is not None:
      request['includeOutputs'] = include_outputs
    if addresses is not None:
      request['addresses'] = addresses
    return await self.request(
      request,
      method='POST',
      path='/streams/bitcoin/{chainId}/block/{blockNumber}',
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
