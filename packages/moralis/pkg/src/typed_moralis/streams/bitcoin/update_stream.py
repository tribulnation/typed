# Generated by typed-dev — see CONTRIBUTING.md.
from typed_moralis.schemas import BitcoinStream, BitcoinStreamUpdateRequest
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter

update_stream_adapter = TypeAdapter(BitcoinStream)


class UpdateStream(RpcEndpoint):
  async def update_stream(
    self,
    id: str,
    bitcoin_stream_update_request: BitcoinStreamUpdateRequest,
    *,
    validate: bool | None = None,
  ) -> BitcoinStream:
    """Update Stream

    Args:
      id: Stream ID.
      bitcoin_stream_update_request: Request payload for update Stream.
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/streams/api-reference/bitcoin/streams/update-stream)
    """
    response = await self.request(
      'POST',
      f'/streams/bitcoin/{id}',
      json=bitcoin_stream_update_request,
    )
    if self.should_validate(validate):
      return update_stream_adapter.validate_json(response.text)
    return response.json()
