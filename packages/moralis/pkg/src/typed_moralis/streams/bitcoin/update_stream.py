# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_moralis.core.rest import StreamsEndpoint
from typed_moralis.schemas import BitcoinStream


class Request(TypedDict):
  id: str
  """Stream ID."""
  allAddresses: NotRequired[bool]
  """Include events for all addresses"""
  demo: NotRequired[bool]
  """Indicator if it is a demo stream"""
  description: NotRequired[str]
  """A description for this stream"""
  includeInputs: NotRequired[bool]
  """Include or not input details in webhook defaults to true"""
  includeOutputs: NotRequired[bool]
  """Include or not output details in webhook defaults to true"""
  network: NotRequired[list[Literal['mainnet', 'testnet']]]
  """The network to listen to"""
  tag: NotRequired[str]
  """A user-provided tag that will be send along the webhook"""
  webhookUrl: NotRequired[str]
  """Webhook URL where moralis will send the POST request."""


class UpdateStream(StreamsEndpoint):
  """Update Stream"""

  async def update_stream(
    self,
    *,
    id: str,
    all_addresses: bool | None = None,
    demo: bool | None = None,
    description: str | None = None,
    include_inputs: bool | None = None,
    include_outputs: bool | None = None,
    network: list[Literal['mainnet', 'testnet']] | None = None,
    tag: str | None = None,
    webhook_url: str | None = None,
    validate: bool | None = None,
  ) -> BitcoinStream:
    """Update Stream

    Args:
      id: Stream ID.
      all_addresses: Include events for all addresses
      demo: Indicator if it is a demo stream
      description: A description for this stream
      include_inputs: Include or not input details in webhook defaults to true
      include_outputs: Include or not output details in webhook defaults to true
      network: The network to listen to
      tag: A user-provided tag that will be send along the webhook
      webhook_url: Webhook URL where moralis will send the POST request.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/streams/api-reference/bitcoin/streams/update-stream)
    """
    request: Request = Request(id=id)
    if all_addresses is not None:
      request['allAddresses'] = all_addresses
    if demo is not None:
      request['demo'] = demo
    if description is not None:
      request['description'] = description
    if include_inputs is not None:
      request['includeInputs'] = include_inputs
    if include_outputs is not None:
      request['includeOutputs'] = include_outputs
    if network is not None:
      request['network'] = network
    if tag is not None:
      request['tag'] = tag
    if webhook_url is not None:
      request['webhookUrl'] = webhook_url
    return await self.request(
      request,
      method='POST',
      path='/streams/bitcoin/{id}',
      validate=validate,
      request_type=Request,
      response_type=BitcoinStream,
    )
