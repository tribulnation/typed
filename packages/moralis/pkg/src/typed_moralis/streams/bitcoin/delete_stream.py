# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.core.rest import StreamsEndpoint


class Request(TypedDict):
  id: str
  """Stream ID."""


Response = bool


class DeleteStream(StreamsEndpoint):
  """Delete Stream"""

  async def delete_stream(self, id: str, *, validate: bool | None = None) -> Response:
    """Delete Stream

    Args:
      id: Stream ID.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/streams/api-reference/bitcoin/streams/delete-stream)
    """
    return await self.request(
      Request(id=id),
      method='DELETE',
      path='/streams/bitcoin/{id}',
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
