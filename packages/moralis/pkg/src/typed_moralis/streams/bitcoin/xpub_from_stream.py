# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.core.rest import StreamsEndpoint


class Request(TypedDict):
  id: str
  """Stream ID."""
  xpubId: str
  """Xpub ID to remove from the stream."""


Response = bool


class XpubFromStream(StreamsEndpoint):
  """Delete Xpub from Stream"""

  async def xpub_from_stream(
    self,
    *,
    id: str,
    xpub_id: str,
    validate: bool | None = None,
  ) -> Response:
    """Delete Xpub from Stream

    Args:
      id: Stream ID.
      xpub_id: Xpub ID to remove from the stream.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/streams/api-reference/bitcoin/xpub/delete-xpub)
    """
    return await self.request(
      Request(id=id, xpubId=xpub_id),
      method='DELETE',
      path='/streams/bitcoin/{id}/xpub/{xpubId}',
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
