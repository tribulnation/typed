# Generated by typed-dev — see CONTRIBUTING.md.
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter

xpub_from_stream_adapter = TypeAdapter(bool)


class XpubFromStream(RpcEndpoint):
  async def xpub_from_stream(
    self,
    id: str,
    xpub_id: str,
    *,
    validate: bool | None = None,
  ) -> bool:
    """Delete Xpub from Stream

    Args:
      id: Stream ID.
      xpub_id: Xpub ID to remove from the stream.
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/streams/api-reference/bitcoin/xpub/delete-xpub)
    """
    response = await self.request('DELETE', f'/streams/bitcoin/{id}/xpub/{xpub_id}')
    if self.should_validate(validate):
      return xpub_from_stream_adapter.validate_json(response.text)
    return response.json()
