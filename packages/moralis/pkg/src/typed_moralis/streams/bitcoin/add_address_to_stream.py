# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.core.rest import StreamsEndpoint
from typed_moralis.schemas import StreamAddress


class Request(TypedDict):
  id: str
  """Stream ID."""
  address: str | list[str]
  """Bitcoin address, or list of addresses, to add to the stream."""


class AddAddressToStream(StreamsEndpoint):
  """Add Address to Stream"""

  async def add_address_to_stream(
    self,
    id: str,
    *,
    address: str | list[str],
    validate: bool | None = None,
  ) -> StreamAddress:
    """Add Address to Stream

    Args:
      id: Stream ID.
      address: Bitcoin address, or list of addresses, to add to the stream.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/streams/api-reference/bitcoin/address/add-address-to-stream)
    """
    return await self.request(
      Request(id=id, address=address),
      method='POST',
      path='/streams/bitcoin/{id}/address',
      validate=validate,
      request_type=Request,
      response_type=StreamAddress,
    )
