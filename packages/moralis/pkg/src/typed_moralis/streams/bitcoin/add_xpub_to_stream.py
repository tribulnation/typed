# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.schemas import StreamXpub
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter


class AddXpubToStreamRequest(TypedDict):
  xpub: str


add_xpub_to_stream_adapter = TypeAdapter(StreamXpub)


class AddXpubToStream(RpcEndpoint):
  async def add_xpub_to_stream(
    self,
    id: str,
    add_xpub_to_stream_request: AddXpubToStreamRequest,
    *,
    validate: bool | None = None,
  ) -> StreamXpub:
    """Add Xpub to Stream

    Args:
      id: Stream ID.
      add_xpub_to_stream_request: Request payload for add Xpub to Stream.
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/streams/api-reference/bitcoin/xpub/add-xpub)
    """
    response = await self.request(
      'POST',
      f'/streams/bitcoin/{id}/xpub',
      json=add_xpub_to_stream_request,
    )
    if self.should_validate(validate):
      return add_xpub_to_stream_adapter.validate_json(response.text)
    return response.json()
