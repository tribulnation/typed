# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.core.rest import StreamsEndpoint
from typed_moralis.schemas import StreamXpub


class Request(TypedDict):
  id: str
  """Stream ID."""
  xpub: str
  """Extended public key (xpub) to add to the stream."""


class AddXpubToStream(StreamsEndpoint):
  """Add Xpub to Stream"""

  async def add_xpub_to_stream(
    self,
    *,
    id: str,
    xpub: str,
    validate: bool | None = None,
  ) -> StreamXpub:
    """Add Xpub to Stream

    Args:
      id: Stream ID.
      xpub: Extended public key (xpub) to add to the stream.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/streams/api-reference/bitcoin/xpub/add-xpub)
    """
    return await self.request(
      Request(id=id, xpub=xpub),
      method='POST',
      path='/streams/bitcoin/{id}/xpub',
      validate=validate,
      request_type=Request,
      response_type=StreamXpub,
    )
