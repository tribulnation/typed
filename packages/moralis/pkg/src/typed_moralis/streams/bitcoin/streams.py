# Generated by typed-dev — see CONTRIBUTING.md.
from typed_moralis.schemas import BitcoinStreamsResponse
from typed_moralis.core import RpcEndpoint, clean_params
from pydantic import TypeAdapter

streams_adapter = TypeAdapter(BitcoinStreamsResponse)


class Streams(RpcEndpoint):
  async def streams(
    self,
    *,
    limit: float,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> BitcoinStreamsResponse:
    """Get Streams

    Args:
      limit: Maximum number of results to return per page.
      cursor: Pagination cursor returned by a previous response.
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/streams/api-reference/bitcoin/streams/get-streams)
    """
    params = clean_params(
      {
        'limit': limit,
        'cursor': cursor,
      }
    )
    response = await self.request('GET', '/streams/bitcoin', params=params)
    if self.should_validate(validate):
      return streams_adapter.validate_json(response.text)
    return response.json()
