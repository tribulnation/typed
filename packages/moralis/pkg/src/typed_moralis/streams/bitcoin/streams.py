# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_moralis.core.rest import StreamsEndpoint
from typed_moralis.schemas import BitcoinStreamsResponse


class Request(TypedDict):
  limit: float
  """Maximum number of results to return per page."""
  cursor: NotRequired[str]
  """Pagination cursor returned by a previous response."""


class Streams(StreamsEndpoint):
  """Get Streams"""

  async def streams(
    self,
    limit: float,
    *,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> BitcoinStreamsResponse:
    """Get Streams

    Args:
      limit: Maximum number of results to return per page.
      cursor: Pagination cursor returned by a previous response.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/streams/api-reference/bitcoin/streams/get-streams)
    """
    request: Request = Request(limit=limit)
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/streams/bitcoin',
      validate=validate,
      request_type=Request,
      response_type=BitcoinStreamsResponse,
    )
