# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.core.rest import StreamsEndpoint
from typed_moralis.schemas import StreamAddress


class Request(TypedDict):
  id: str
  """Stream ID."""
  address: str | list[str]
  """Bitcoin address, or list of addresses, to remove from the stream."""


class AddressFromStream(StreamsEndpoint):
  """Delete Address from Stream"""

  async def address_from_stream(
    self,
    id: str,
    *,
    address: str | list[str],
    validate: bool | None = None,
  ) -> StreamAddress:
    """Delete Address from Stream

    Args:
      id: Stream ID.
      address: Bitcoin address, or list of addresses, to remove from the stream.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/streams/api-reference/bitcoin/address/delete-address-from-stream)
    """
    return await self.request(
      Request(id=id, address=address),
      method='DELETE',
      path='/streams/bitcoin/{id}/address',
      validate=validate,
      request_type=Request,
      response_type=StreamAddress,
    )
