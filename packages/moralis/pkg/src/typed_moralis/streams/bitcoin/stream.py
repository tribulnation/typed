# Generated by typed-dev — see CONTRIBUTING.md.
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter

stream_adapter = TypeAdapter(bool)


class StreamEndpoint(RpcEndpoint):
  async def stream(self, id: str, *, validate: bool | None = None) -> bool:
    """Delete Stream

    Args:
      id: Stream ID.
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/streams/api-reference/bitcoin/streams/delete-stream)
    """
    response = await self.request('DELETE', f'/streams/bitcoin/{id}')
    if self.should_validate(validate):
      return stream_adapter.validate_json(response.text)
    return response.json()
