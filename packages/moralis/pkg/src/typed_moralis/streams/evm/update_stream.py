# Generated by typed-dev — see CONTRIBUTING.md.
from typed_moralis.schemas import EvmStream, EvmStreamUpdateRequest
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter

update_stream_adapter = TypeAdapter(EvmStream)


class UpdateStream(RpcEndpoint):
  async def update_stream(
    self,
    id: str,
    evm_stream_update_request: EvmStreamUpdateRequest,
    *,
    validate: bool | None = None,
  ) -> EvmStream:
    """Updates a specific evm stream.

    Args:
      id: The id of the stream to update
      evm_stream_update_request: Provide a Stream Model
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/streams/api-reference/streams/update-stream)
    """
    response = await self.request(
      'POST',
      f'/streams/evm/{id}',
      json=evm_stream_update_request,
    )
    if self.should_validate(validate):
      return update_stream_adapter.validate_json(response.text)
    return response.json()
