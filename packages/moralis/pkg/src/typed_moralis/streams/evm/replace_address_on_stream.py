# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.schemas import StreamAddress
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter


class ReplaceAddressOnStreamRequest(TypedDict):
  """Provide a Address Model"""

  address: str | list[str]
  """The address or a list of addresses to be removed from the Stream."""


replace_address_on_stream_adapter = TypeAdapter(StreamAddress)


class ReplaceAddressOnStream(RpcEndpoint):
  async def replace_address_on_stream(
    self,
    id: str,
    replace_address_on_stream_request: ReplaceAddressOnStreamRequest,
    *,
    validate: bool | None = None,
  ) -> StreamAddress:
    """Replaces address from a Stream.

    Args:
      id: The id of the stream to replace the address from
      replace_address_on_stream_request: Provide a Address Model
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/streams/api-reference/streams/replace-address-on-stream)
    """
    response = await self.request(
      'PATCH',
      f'/streams/evm/{id}/address',
      json=replace_address_on_stream_request,
    )
    if self.should_validate(validate):
      return replace_address_on_stream_adapter.validate_json(response.text)
    return response.json()
