# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.core.rest import StreamsEndpoint
from typed_moralis.schemas import EvmStream


class Request(TypedDict):
  id: str
  """The id of the stream to delete"""


class DeleteStream(StreamsEndpoint):
  """Delete a specific evm stream."""

  async def delete_stream(self, id: str, *, validate: bool | None = None) -> EvmStream:
    """Delete a specific evm stream.

    Args:
      id: The id of the stream to delete
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/streams/api-reference/streams/delete-stream)
    """
    return await self.request(
      Request(id=id),
      method='DELETE',
      path='/streams/evm/{id}',
      validate=validate,
      request_type=Request,
      response_type=EvmStream,
    )
