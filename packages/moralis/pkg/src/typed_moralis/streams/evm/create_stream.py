# Generated by typed-dev — see CONTRIBUTING.md.
from typed_moralis.schemas import EvmStream, EvmStreamCreateRequest
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter

create_stream_adapter = TypeAdapter(EvmStream)


class CreateStream(RpcEndpoint):
  async def create_stream(
    self,
    evm_stream_create_request: EvmStreamCreateRequest,
    *,
    validate: bool | None = None,
  ) -> EvmStream:
    """Creates a new evm stream.

    Args:
      evm_stream_create_request: Provide a Stream Model
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/streams/api-reference/streams/create-streams)
    """
    response = await self.request('PUT', '/streams/evm', json=evm_stream_create_request)
    if self.should_validate(validate):
      return create_stream_adapter.validate_json(response.text)
    return response.json()
