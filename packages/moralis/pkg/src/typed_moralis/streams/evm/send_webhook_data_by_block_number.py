# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter

send_webhook_data_by_block_number_adapter = TypeAdapter(Literal[None] | None)


class SendWebhookDataByBlockNumber(RpcEndpoint):
  async def send_webhook_data_by_block_number(
    self,
    chain_id: str,
    block_number: float,
    stream_id: str,
    *,
    validate: bool | None = None,
  ) -> Literal[None] | None:
    """Execute.

    Args:
      chain_id: Chain ID the webhook event belongs to.
      block_number: Block number to fetch webhook trigger data for.
      stream_id: Stream ID the webhook payload should be generated for.
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/streams/api-reference/streams/send-webhook-data-by-block-number)
    """
    response = await self.request(
      'POST',
      f'/streams/evm/{chain_id}/block-to-webhook/{block_number}/{stream_id}',
    )
    if self.should_validate(validate):
      return send_webhook_data_by_block_number_adapter.validate_json(response.text)
    return response.json()
