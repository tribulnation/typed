# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict, cast
from typed_moralis.core.rest import StreamsEndpoint


class Request(TypedDict):
  chainId: str
  """Chain ID the webhook event belongs to."""
  blockNumber: float
  """Block number to fetch webhook trigger data for."""
  streamId: str
  """Stream ID the webhook payload should be generated for."""


Response = Literal[None] | None


class SendWebhookDataByBlockNumber(StreamsEndpoint):
  """Execute."""

  async def send_webhook_data_by_block_number(
    self,
    *,
    chain_id: str,
    block_number: float,
    stream_id: str,
    validate: bool | None = None,
  ) -> Response:
    """Execute.

    Args:
      chain_id: Chain ID the webhook event belongs to.
      block_number: Block number to fetch webhook trigger data for.
      stream_id: Stream ID the webhook payload should be generated for.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/streams/api-reference/streams/send-webhook-data-by-block-number)
    """
    return await self.request(
      Request(chainId=chain_id, blockNumber=block_number, streamId=stream_id),
      method='POST',
      path='/streams/evm/{chainId}/block-to-webhook/{blockNumber}/{streamId}',
      validate=validate,
      request_type=Request,
      response_type=cast(type, Response),
    )
