# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.schemas import StreamAddress
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter


class AddressFromStreamRequest(TypedDict):
  """Provide a Address Model"""

  address: str | list[str]
  """The address or a list of addresses to be removed from the Stream."""


address_from_stream_adapter = TypeAdapter(StreamAddress)


class AddressFromStream(RpcEndpoint):
  async def address_from_stream(
    self,
    id: str,
    address_from_stream_request: AddressFromStreamRequest,
    *,
    validate: bool | None = None,
  ) -> StreamAddress:
    """Deletes an address from a Stream.

    Args:
      id: The id of the stream to delete the address from
      address_from_stream_request: Provide a Address Model
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/streams/api-reference/streams/delete-address-from-stream)
    """
    response = await self.request(
      'DELETE',
      f'/streams/evm/{id}/address',
      json=address_from_stream_request,
    )
    if self.should_validate(validate):
      return address_from_stream_adapter.validate_json(response.text)
    return response.json()
