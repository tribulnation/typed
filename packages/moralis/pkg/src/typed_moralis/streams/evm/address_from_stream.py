# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.core.rest import StreamsEndpoint
from typed_moralis.schemas import StreamAddress


class Request(TypedDict):
  id: str
  """The id of the stream to delete the address from"""
  address: str | list[str]
  """The address or a list of addresses to be removed from the Stream."""


class AddressFromStream(StreamsEndpoint):
  """Deletes an address from a Stream."""

  async def address_from_stream(
    self,
    id: str,
    *,
    address: str | list[str],
    validate: bool | None = None,
  ) -> StreamAddress:
    """Deletes an address from a Stream.

    Args:
      id: The id of the stream to delete the address from
      address: The address or a list of addresses to be removed from the Stream.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/streams/api-reference/streams/delete-address-from-stream)
    """
    return await self.request(
      Request(id=id, address=address),
      method='DELETE',
      path='/streams/evm/{id}/address',
      validate=validate,
      request_type=Request,
      response_type=StreamAddress,
    )
