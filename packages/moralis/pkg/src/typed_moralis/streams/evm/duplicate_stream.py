# Generated by typed-dev — see CONTRIBUTING.md.
from typed_moralis.schemas import EvmStream
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter

duplicate_stream_adapter = TypeAdapter(EvmStream)


class DuplicateStream(RpcEndpoint):
  async def duplicate_stream(
    self, id: str, *, validate: bool | None = None
  ) -> EvmStream:
    """Duplicate a specific evm stream.

    Args:
      id: The id of the stream to duplicate
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/streams/api-reference/streams/duplicate-stream)
    """
    response = await self.request('POST', f'/streams/evm/{id}/duplicate')
    if self.should_validate(validate):
      return duplicate_stream_adapter.validate_json(response.text)
    return response.json()
