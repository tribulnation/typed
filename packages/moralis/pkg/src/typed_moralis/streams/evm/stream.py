# Generated by typed-dev — see CONTRIBUTING.md.
from typed_moralis.schemas import EvmStream
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter

stream_adapter = TypeAdapter(EvmStream)


class StreamEndpoint(RpcEndpoint):
  async def stream(self, id: str, *, validate: bool | None = None) -> EvmStream:
    """Delete a specific evm stream.

    Args:
      id: The id of the stream to delete
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/streams/api-reference/streams/delete-stream)
    """
    response = await self.request('DELETE', f'/streams/evm/{id}')
    if self.should_validate(validate):
      return stream_adapter.validate_json(response.text)
    return response.json()
