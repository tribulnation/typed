# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_moralis.core.rest import StreamsEndpoint
from typed_moralis.schemas import EvmStream


class Request(TypedDict):
  id: str
  """The id of the stream to update"""
  status: Literal['active', 'paused', 'error', 'terminated']
  """The status of the stream."""


class UpdateStreamStatus(StreamsEndpoint):
  """Updates the status of specific evm stream."""

  async def update_stream_status(
    self,
    id: str,
    *,
    status: Literal['active', 'paused', 'error', 'terminated'],
    validate: bool | None = None,
  ) -> EvmStream:
    """Updates the status of specific evm stream.

    Args:
      id: The id of the stream to update
      status: The status of the stream.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/streams/api-reference/streams/update-stream-status)
    """
    return await self.request(
      Request(id=id, status=status),
      method='POST',
      path='/streams/evm/{id}/status',
      validate=validate,
      request_type=Request,
      response_type=EvmStream,
    )
