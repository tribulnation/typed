# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.core.rest import StreamsEndpoint
from typed_moralis.schemas import StreamAddress


class Request(TypedDict):
  id: str
  """The id of the stream to add the address to"""
  address: str | list[str]
  """The address or a list of addresses to be added to the Stream."""


class AddAddressToStream(StreamsEndpoint):
  """Adds an address to a Stream."""

  async def add_address_to_stream(
    self,
    id: str,
    *,
    address: str | list[str],
    validate: bool | None = None,
  ) -> StreamAddress:
    """Adds an address to a Stream.

    Args:
      id: The id of the stream to add the address to
      address: The address or a list of addresses to be added to the Stream.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/streams/api-reference/streams/add-address-to-stream)
    """
    return await self.request(
      Request(id=id, address=address),
      method='POST',
      path='/streams/evm/{id}/address',
      validate=validate,
      request_type=Request,
      response_type=StreamAddress,
    )
