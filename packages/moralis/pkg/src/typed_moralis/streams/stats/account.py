# Generated by typed-dev — see CONTRIBUTING.md.
from typed_moralis.core.rest import StreamsEndpoint
from typed_moralis.schemas import StreamStats


class Account(StreamsEndpoint):
  """Get the global stats for the account."""

  async def account(self, *, validate: bool | None = None) -> StreamStats:
    """Get the global stats for the account.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/streams/api-reference/stats/get-stats)
    """
    return await self.request(
      None,
      method='GET',
      path='/stats',
      validate=validate,
      response_type=StreamStats,
    )
