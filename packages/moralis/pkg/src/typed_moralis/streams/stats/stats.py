# Generated by typed-dev — see CONTRIBUTING.md.
from typed_moralis.schemas import StreamStats
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter

stats_adapter = TypeAdapter(StreamStats)


class StatsEndpoint(RpcEndpoint):
  async def stats(self, *, validate: bool | None = None) -> StreamStats:
    """Get the global stats for the account.

    Args:
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/streams/api-reference/stats/get-stats)
    """
    response = await self.request('GET', '/stats')
    if self.should_validate(validate):
      return stats_adapter.validate_json(response.text)
    return response.json()
