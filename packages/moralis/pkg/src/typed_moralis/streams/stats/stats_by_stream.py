# Generated by typed-dev — see CONTRIBUTING.md.
from typed_moralis.schemas import StreamStats
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter

stats_by_stream_adapter = TypeAdapter(StreamStats)


class StatsByStream(RpcEndpoint):
  async def stats_by_stream(
    self,
    stream_id: str,
    *,
    validate: bool | None = None,
  ) -> StreamStats:
    """Get the stats for the streamId specified

    Args:
      stream_id: The id of the stream to get the stats
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/streams/api-reference/stats/get-stats-by-stream)
    """
    response = await self.request('GET', f'/stats/{stream_id}')
    if self.should_validate(validate):
      return stats_by_stream_adapter.validate_json(response.text)
    return response.json()
