# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.core.rest import StreamsEndpoint
from typed_moralis.schemas import StreamStats


class Request(TypedDict):
  streamId: str
  """The id of the stream to get the stats"""


class StatsByStream(StreamsEndpoint):
  """Get the stats for the streamId specified"""

  async def stats_by_stream(
    self,
    stream_id: str,
    *,
    validate: bool | None = None,
  ) -> StreamStats:
    """Get the stats for the streamId specified

    Args:
      stream_id: The id of the stream to get the stats
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/streams/api-reference/stats/get-stats-by-stream)
    """
    return await self.request(
      Request(streamId=stream_id),
      method='GET',
      path='/stats/{streamId}',
      validate=validate,
      request_type=Request,
      response_type=StreamStats,
    )
