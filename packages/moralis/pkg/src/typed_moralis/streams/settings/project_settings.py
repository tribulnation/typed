# Generated by typed-dev — see CONTRIBUTING.md.
from typed_moralis.core.rest import StreamsEndpoint
from typed_moralis.schemas import StreamProjectSettings


class ProjectSettings(StreamsEndpoint):
  """Get the settings for the current project based on the project api-key."""

  async def project_settings(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamProjectSettings:
    """Get the settings for the current project based on the project api-key.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/streams/api-reference/project/get-project-settings)
    """
    return await self.request(
      None,
      method='GET',
      path='/settings',
      validate=validate,
      response_type=StreamProjectSettings,
    )
