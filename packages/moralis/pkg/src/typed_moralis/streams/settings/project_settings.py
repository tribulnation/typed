# Generated by typed-dev — see CONTRIBUTING.md.
from typed_moralis.schemas import StreamProjectSettings
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter

project_settings_adapter = TypeAdapter(StreamProjectSettings)


class ProjectSettings(RpcEndpoint):
  async def project_settings(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamProjectSettings:
    """Get the settings for the current project based on the project api-key.

    Args:
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/streams/api-reference/project/get-project-settings)
    """
    response = await self.request('GET', '/settings')
    if self.should_validate(validate):
      return project_settings_adapter.validate_json(response.text)
    return response.json()
