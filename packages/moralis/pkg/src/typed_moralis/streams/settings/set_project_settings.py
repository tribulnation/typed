# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_moralis.core.rest import StreamsEndpoint
from typed_moralis.schemas import StreamProjectSettings


class Request(TypedDict):
  region: NotRequired[
    Literal['us-east-1', 'us-west-2', 'eu-central-1', 'ap-southeast-1']
  ]
  """The region from where all the webhooks will be posted for this project"""
  secretKey: NotRequired[str]
  """The secret key to validate the webhooks"""


class SetProjectSettings(StreamsEndpoint):
  """Set the settings for the current project based on the project api-key."""

  async def set_project_settings(
    self,
    region: Literal['us-east-1', 'us-west-2', 'eu-central-1', 'ap-southeast-1']
    | None = None,
    *,
    secret_key: str | None = None,
    validate: bool | None = None,
  ) -> StreamProjectSettings:
    """Set the settings for the current project based on the project api-key.

    Args:
      region: The region from where all the webhooks will be posted for this project
      secret_key: The secret key to validate the webhooks
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/streams/api-reference/project/set-project-settings)
    """
    request: Request = Request()
    if region is not None:
      request['region'] = region
    if secret_key is not None:
      request['secretKey'] = secret_key
    return await self.request(
      request,
      method='POST',
      path='/settings',
      validate=validate,
      request_type=Request,
      response_type=StreamProjectSettings,
    )
