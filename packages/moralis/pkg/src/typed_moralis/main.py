# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .auth import Auth
from .bitcoin import Bitcoin
from .cortex import Cortex
from .evm import Evm
from .solana import Solana
from .streams import Streams
from .universal import Universal
from typed_moralis.core.base import ClientBase


class Moralis(ClientBase):
  """Moralis data-API client -- read-only EVM, Solana, Bitcoin, and cross-chain wallet/token/NFT data, an AI chat endpoint (Cortex), wallet sign-in (Auth), and webhook stream management (Streams), all behind a single api-key-authenticated REST surface.

  References:
    - [Upstream docs](https://docs.moralis.com/)
  """

  @cached_property
  def auth(self) -> Auth:
    """Auth API — Web3 wallet-signature authentication built on EIP-4361 (Sign-In with Ethereum, extended to Solana), issuing a chain-agnostic profileId and binding multiple wallet addresses to one profile.

    References:
      - [Upstream docs](https://docs.moralis.com/auth-api/overview)
    """
    return Auth(client=self.client)

  @cached_property
  def bitcoin(self) -> Bitcoin:
    """Bitcoin Data API — raw blocks and transactions, market prices with sparklines, wallet address history and balances, and native xpub-key utilities.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/bitcoin/overview)
    """
    return Bitcoin(client=self.client)

  @cached_property
  def cortex(self) -> Cortex:
    """Cortex API: an LLM-backed chat endpoint that answers natural-language questions grounded in Moralis's own onchain data. Deprecated in favor of Onchain Skills.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/cortex-api/chat)
    """
    return Cortex(client=self.client)

  @cached_property
  def evm(self) -> Evm:
    """EVM Data API — wallet, token, NFT, price, DeFi, and raw blockchain data across 30+ EVM-compatible chains (Ethereum, Base, BNB Smart Chain, Polygon, and more), Moralis's most specialized and deepest chain-family API.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/evm/overview)
    """
    return Evm(client=self.client)

  @cached_property
  def solana(self) -> Solana:
    """Solana Data API — wallet, token, and NFT data with Solana-native semantics and structures, distinct from the cross-chain Universal API.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/solana/overview)
    """
    return Solana(client=self.client)

  @cached_property
  def streams(self) -> Streams:
    """Streams API — real-time blockchain webhooks across EVM, Bitcoin, and Solana, with guaranteed delivery and address/topic filtering.

    References:
      - [Upstream docs](https://docs.moralis.com/streams/overview)
    """
    return Streams(client=self.client)

  @cached_property
  def universal(self) -> Universal:
    """Universal API — a single, consistent interface spanning 30+ EVM networks plus Solana and Bitcoin, covering cross-chain wallet balances, trending tokens, and category/entity metadata that don't belong to one chain family.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/universal/overview)
    """
    return Universal(client=self.client)
