# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .entity import Entity
from .global_metrics import GlobalMetrics
from .token import Token
from typed_moralis.core.rest import DeepIndexEndpoint


class Universal(DeepIndexEndpoint):
  """Universal API — a single, consistent interface spanning 30+ EVM networks plus Solana and Bitcoin, covering cross-chain wallet balances, trending tokens, and category/entity metadata that don't belong to one chain family.

  References:
    - [Upstream docs](https://docs.moralis.com/data-api/universal/overview)
  """

  @cached_property
  def entity(self) -> Entity:
    """Search and discovery of blockchain entities — organizations, protocols, exchanges, and notable addresses with verified labels and metadata.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/universal/entity/overview)
    """
    return Entity(client=self.client)

  @cached_property
  def global_metrics(self) -> GlobalMetrics:
    """Trading-volume and category-metrics lookups, by chain or by category, current and timeseries.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/universal/overview)
    """
    return GlobalMetrics(client=self.client)

  @cached_property
  def token(self) -> Token:
    """Search, score, rank, and analyze tokens across EVM chains and Solana through one multichain interface.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/universal/token/overview)
    """
    return Token(client=self.client)
