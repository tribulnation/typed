# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_moralis.core import TimestampSeconds
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmOrSolanaChain, TrendingTokenCountWindow


PricePercentChangeKeywords = TypedDict(
  'PricePercentChangeKeywords', {'1h': float, '4h': float, '12h': float, '24h': float}
)
"""
  - `1h`: 1h.
  - `4h`: 4h.
  - `12h`: 12h.
  - `24h`: 24h.
"""


class PricePercentChange(PricePercentChangeKeywords):
  """Price percent change."""


class Request(TypedDict):
  chain: NotRequired[EvmOrSolanaChain]
  limit: NotRequired[int]
  """The desired page size of the result."""


class TrendingTokensSchema(TypedDict):
  chainId: str
  """Chain ID."""
  tokenAddress: str
  """Token address."""
  name: str
  """Name."""
  uniqueName: str
  """Unique name."""
  symbol: str
  """Symbol."""
  decimals: int
  """Decimals."""
  logo: str
  """Logo."""
  usdPrice: float
  """USD price."""
  createdAt: TimestampSeconds
  """Created at."""
  marketCap: int
  """Market cap."""
  liquidityUsd: int
  """Liquidity USD."""
  holders: int
  """Holders."""
  pricePercentChange: PricePercentChange
  totalVolume: TrendingTokenCountWindow
  transactions: TrendingTokenCountWindow
  buyTransactions: TrendingTokenCountWindow
  sellTransactions: TrendingTokenCountWindow
  buyers: TrendingTokenCountWindow
  sellers: TrendingTokenCountWindow


Response = list[TrendingTokensSchema]


class TrendingTokens(DeepIndexEndpoint):
  """List top tokens trending based on trading activity, volume, liquidity and more. By default this returns cross-chain results, including Solana. Optionally filter by `chain` for single chain results."""

  async def trending_tokens(
    self,
    chain: EvmOrSolanaChain | None = None,
    *,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> Response:
    """List top tokens trending based on trading activity, volume, liquidity and more. By default this returns cross-chain results, including Solana. Optionally filter by `chain` for single chain results.

    Args:
      chain:
      limit: The desired page size of the result.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/universal/token/trending-tokens)
    """
    request: Request = Request()
    if chain is not None:
      request['chain'] = chain
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/tokens/trending',
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
