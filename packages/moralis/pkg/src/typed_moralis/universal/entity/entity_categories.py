# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint


class Request(TypedDict):
  limit: NotRequired[int]
  """The desired page size of the result."""


class Result(TypedDict):
  name: str
  """Name."""
  id: str
  """ID."""
  total_entities: int
  """Total entities."""


class EntityCategoriesResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  page: int
  """Page."""
  page_size: int
  """Page size."""
  result: list[Result]
  """Result."""


class EntityCategories(DeepIndexEndpoint):
  """List available categories for blockchain entities."""

  async def entity_categories(
    self,
    limit: int | None = None,
    *,
    validate: bool | None = None,
  ) -> EntityCategoriesResponse:
    """List available categories for blockchain entities.

    Args:
      limit: The desired page size of the result.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/universal/entity/endpoints/entity-categories)
    """
    request: Request = Request()
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/entities/categories',
      validate=validate,
      request_type=Request,
      response_type=EntityCategoriesResponse,
    )
