# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_moralis.core import RpcEndpoint, clean_params
from pydantic import TypeAdapter


class Result(TypedDict):
  total_addresses: NotRequired[int]
  """The total number of addresses"""
  name: NotRequired[str]
  """The name of the entity"""
  id: NotRequired[str]
  """The id of the entity"""
  logo: NotRequired[str]
  """The logo of the entity"""
  bio: NotRequired[str]
  """The bio of the entity"""
  description: NotRequired[str]
  """The description of the entity"""
  website: NotRequired[str]
  """The website of the entity"""
  twitter: NotRequired[str]
  """The twitter of the entity"""
  type: NotRequired[str]
  """The type of the entity"""


class EntitiesByCategoryResponse(TypedDict):
  page: NotRequired[int]
  """The current page of the result"""
  page_size: NotRequired[int]
  """The number of results per page"""
  result: list[Result]
  """Result."""


entities_by_category_adapter = TypeAdapter(EntitiesByCategoryResponse)


class EntitiesByCategory(RpcEndpoint):
  async def entities_by_category(
    self,
    category_id: str,
    *,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> EntitiesByCategoryResponse:
    """Fetch entities belonging to a specific category. Each entity returns name, logo, description, external links, total addresses and more.

    Args:
      category_id: The category Id
      limit: The desired page size of the result.
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/data-api/universal/entity/endpoints/entity-by-category)
    """
    params = clean_params(
      {
        'limit': limit,
      }
    )
    response = await self.request(
      'GET',
      f'/entities/categories/{category_id}',
      params=params,
    )
    if self.should_validate(validate):
      return entities_by_category_adapter.validate_json(response.text)
    return response.json()
