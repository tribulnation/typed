# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict, deprecated
from typed_moralis.core.rest import SolanaEndpoint
from typed_moralis.schemas import SnipersResponse


class Request(TypedDict):
  network: Literal['mainnet', 'devnet']
  """The network to query"""
  pairAddress: str
  """The address of the pair to query"""
  blocksAfterCreation: NotRequired[float]
  """Number of blocks after token creation to scan for sniper wallets."""


class Snipers(SolanaEndpoint):
  """Get all snipers."""

  @deprecated('Deprecated method')
  async def snipers(
    self,
    network: Literal['mainnet', 'devnet'],
    *,
    pair_address: str,
    blocks_after_creation: float | None = None,
    validate: bool | None = None,
  ) -> SnipersResponse:
    """Get all snipers.

    Args:
      network: The network to query
      pair_address: The address of the pair to query
      blocks_after_creation: Number of blocks after token creation to scan for sniper wallets.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/solana/token/advanced-signals/snipers)
    """
    request: Request = Request(network=network, pairAddress=pair_address)
    if blocks_after_creation is not None:
      request['blocksAfterCreation'] = blocks_after_creation
    return await self.request(
      request,
      method='GET',
      path='/token/{network}/pairs/{pairAddress}/snipers',
      validate=validate,
      request_type=Request,
      response_type=SnipersResponse,
    )
