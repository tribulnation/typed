# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_moralis.core.rest import SolanaEndpoint
from typed_moralis.schemas import SolanaTokenPrice


class Request(TypedDict):
  network: Literal['mainnet', 'devnet']
  """The network to query"""
  addresses: list[str]
  """Token mint addresses to fetch prices for (up to 100)."""


Response = list[SolanaTokenPrice]


class TokenPriceBatch(SolanaEndpoint):
  """Gets the token price (usd and native) for a given contract address and network."""

  async def token_price_batch(
    self,
    network: Literal['mainnet', 'devnet'],
    *,
    addresses: list[str],
    validate: bool | None = None,
  ) -> Response:
    """Gets the token price (usd and native) for a given contract address and network.

    Args:
      network: The network to query
      addresses: Token mint addresses to fetch prices for (up to 100).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/solana/token/prices/token-prices-batch)
    """
    return await self.request(
      Request(network=network, addresses=addresses),
      method='POST',
      path='/token/{network}/prices',
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
