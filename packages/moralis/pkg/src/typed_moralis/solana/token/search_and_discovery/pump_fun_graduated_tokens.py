# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_moralis.core import TimestampIso
from typed_moralis.core.rest import SolanaEndpoint


class Request(TypedDict):
  network: Literal['mainnet', 'devnet']
  """The network to query"""
  exchange: str
  """DEX exchange to query (currently only Pump.fun is supported)."""
  cursor: NotRequired[str]
  """The cursor to the next page"""
  limit: NotRequired[float]
  """The limit per page"""


class Result(TypedDict):
  tokenAddress: NotRequired[str]
  """Token address."""
  name: NotRequired[str]
  """Name."""
  symbol: NotRequired[str]
  """Symbol."""
  logo: NotRequired[str]
  """Logo."""
  decimals: NotRequired[str]
  """Decimals."""
  priceNative: NotRequired[str]
  """Price native."""
  priceUsd: NotRequired[str]
  """Price USD."""
  liquidity: NotRequired[str]
  """Liquidity."""
  fullyDilutedValuation: NotRequired[str]
  """Fully diluted valuation."""
  graduatedAt: NotRequired[TimestampIso]
  """Graduated at."""


class PumpFunGraduatedTokensResponse(TypedDict):
  result: list[Result]
  """Result."""
  pageSize: NotRequired[int]
  """Page size."""
  page: NotRequired[int]
  """Page."""
  cursor: NotRequired[str | None]
  """Cursor."""


class PumpFunGraduatedTokens(SolanaEndpoint):
  """Get the list of graduated tokens by given exchange."""

  def pump_fun_graduated_tokens_paged(
    self,
    network: Literal['mainnet', 'devnet'],
    *,
    exchange: str,
    limit: float | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[Result, str]:
    """Get the list of graduated tokens by given exchange.

    Paged variant of `pump_fun_graduated_tokens`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      network: The network to query
      exchange: DEX exchange to query (currently only Pump.fun is supported).
      limit: The limit per page
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/solana/token/search-and-discovery/pump-fun-graduated-tokens)
    """

    async def next(cursor: str) -> tuple[list[Result], str | None]:
      response = await self.pump_fun_graduated_tokens(
        network,
        exchange=exchange,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('result')
      state = response.get('cursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def pump_fun_graduated_tokens(
    self,
    network: Literal['mainnet', 'devnet'],
    *,
    exchange: str,
    cursor: str | None = None,
    limit: float | None = None,
    validate: bool | None = None,
  ) -> PumpFunGraduatedTokensResponse:
    """Get the list of graduated tokens by given exchange.

    Args:
      network: The network to query
      exchange: DEX exchange to query (currently only Pump.fun is supported).
      cursor: The cursor to the next page
      limit: The limit per page
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/solana/token/search-and-discovery/pump-fun-graduated-tokens)
    """
    request: Request = Request(network=network, exchange=exchange)
    if cursor is not None:
      request['cursor'] = cursor
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/token/{network}/exchange/{exchange}/graduated',
      validate=validate,
      request_type=Request,
      response_type=PumpFunGraduatedTokensResponse,
    )
