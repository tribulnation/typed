# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter


class PumpFunBondingStatusResponse(TypedDict):
  mint: NotRequired[str]
  """Mint."""
  bondingProgress: NotRequired[float]
  """Bonding progress."""


pump_fun_bonding_status_adapter = TypeAdapter(PumpFunBondingStatusResponse)


class PumpFunBondingStatus(RpcEndpoint):
  async def pump_fun_bonding_status(
    self,
    network: Literal['mainnet', 'devnet'],
    address: str,
    *,
    validate: bool | None = None,
  ) -> PumpFunBondingStatusResponse:
    """Get the token bonding status for a given network and contract (if relevant).

    Args:
      network: The network to query
      address: The address to query
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/data-api/solana/token/search-and-discovery/pump-fun-bonding-status)
    """
    response = await self.request('GET', f'/token/{network}/{address}/bonding-status')
    if self.should_validate(validate):
      return pump_fun_bonding_status_adapter.validate_json(response.text)
    return response.json()
