# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_moralis.core.rest import SolanaEndpoint


class PumpFunBondingStatusResponse(TypedDict):
  mint: NotRequired[str]
  """Mint."""
  bondingProgress: NotRequired[float]
  """Bonding progress."""


class Request(TypedDict):
  network: Literal['mainnet', 'devnet']
  """The network to query"""
  address: str
  """The address to query"""


class PumpFunBondingStatus(SolanaEndpoint):
  """Get the token bonding status for a given network and contract (if relevant)."""

  async def pump_fun_bonding_status(
    self,
    network: Literal['mainnet', 'devnet'],
    *,
    address: str,
    validate: bool | None = None,
  ) -> PumpFunBondingStatusResponse:
    """Get the token bonding status for a given network and contract (if relevant).

    Args:
      network: The network to query
      address: The address to query
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/solana/token/search-and-discovery/pump-fun-bonding-status)
    """
    return await self.request(
      Request(network=network, address=address),
      method='GET',
      path='/token/{network}/{address}/bonding-status',
      validate=validate,
      request_type=Request,
      response_type=PumpFunBondingStatusResponse,
    )
