# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .advanced_signals import AdvancedSignals
from .holders import Holders
from .market_metrics import MarketMetrics
from .pairs import Pairs
from .prices import Prices
from .search_and_discovery import SearchAndDiscovery
from .swaps import Swaps
from .token_metadata import TokenMetadata
from .token_metadata_batch import TokenMetadataBatch


class Token(TokenMetadata, TokenMetadataBatch):
  """Solana token metadata, prices, holders, swaps, pairs, and advanced analytics for SPL tokens.

  References:
    - [Upstream docs](https://docs.moralis.com/data-api/solana/token/overview)
  """

  @cached_property
  def advanced_signals(self) -> AdvancedSignals:
    """Sniper detection for a Solana trading pair.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/solana/token/overview)
    """
    return AdvancedSignals(client=self.client)

  @cached_property
  def holders(self) -> Holders:
    """SPL token holder counts, ranked holder lists, and historical holder trends.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/solana/token/overview)
    """
    return Holders(client=self.client)

  @cached_property
  def market_metrics(self) -> MarketMetrics:
    """Trading analytics for one or many SPL tokens, including a timeseries view.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/solana/token/overview)
    """
    return MarketMetrics(client=self.client)

  @cached_property
  def pairs(self) -> Pairs:
    """Trading pairs, pair statistics, and swap history for an SPL token or a specific pair address.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/solana/token/overview)
    """
    return Pairs(client=self.client)

  @cached_property
  def prices(self) -> Prices:
    """Batched SPL token price lookups.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/solana/token/overview)
    """
    return Prices(client=self.client)

  @cached_property
  def search_and_discovery(self) -> SearchAndDiscovery:
    """Pump.fun bonding-curve status and new/graduated token listings.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/solana/token/overview)
    """
    return SearchAndDiscovery(client=self.client)

  @cached_property
  def swaps(self) -> Swaps:
    """Swap transaction history, by wallet address or by SPL token address.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/solana/token/overview)
    """
    return Swaps(client=self.client)
