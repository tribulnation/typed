# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal
from typed_moralis.schemas import TokenHolderMetrics
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter

token_holder_metrics_adapter = TypeAdapter(TokenHolderMetrics | None)


class TokenHolderMetricsEndpoint(RpcEndpoint):
  async def token_holder_metrics(
    self,
    network: Literal['mainnet', 'devnet'],
    address: str,
    *,
    validate: bool | None = None,
  ) -> TokenHolderMetrics | None:
    """Get the summary of holders for a given token token.

    Args:
      network: The network to query
      address: The address to query
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/data-api/solana/token/holders/holder-metrics)
    """
    response = await self.request('GET', f'/token/{network}/holders/{address}')
    if self.should_validate(validate):
      return token_holder_metrics_adapter.validate_json(response.text)
    return response.json()
