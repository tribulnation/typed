# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_moralis.core import TimestampIso, TimestampSeconds
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmOrSolanaChain


class Timesery(TypedDict):
  timestamp: NotRequired[TimestampIso]
  """Timestamp."""
  buyVolume: NotRequired[float]
  """Buy volume."""
  sellVolume: NotRequired[float]
  """Sell volume."""
  liquidityUsd: NotRequired[int | float]
  """Liquidity USD."""
  fullyDilutedValuation: NotRequired[int | float]
  """Fully diluted valuation."""


class Result(TypedDict):
  chainId: NotRequired[str]
  """Chain ID."""
  tokenAddress: NotRequired[str]
  """Token address."""
  timeseries: NotRequired[list[Timesery]]
  """Timeseries."""


class Token(TypedDict):
  chain: EvmOrSolanaChain
  tokenAddress: str
  """The token address"""


class Request(TypedDict):
  timeframe: Literal['1d', '7d', '30d']
  """The timeframe to query"""
  fromDate: NotRequired[TimestampSeconds]
  """Inclusive lower timestamp bound for the analytics timeseries window."""
  toDate: NotRequired[TimestampSeconds]
  """Inclusive upper timestamp bound for the analytics timeseries window."""
  tokens: list[Token]
  """The tokens to be fetched"""


class TokenAnalyticsTimeseriesResponse(TypedDict):
  result: NotRequired[list[Result]]
  """Result."""


class TokenAnalyticsTimeseries(DeepIndexEndpoint):
  """Fetch timeseries buy volume, sell volume, liquidity and FDV for multiple tokens. Accepts an array of up to 200 `tokens`, each requiring `chain` and `tokenAddress`."""

  async def token_analytics_timeseries(
    self,
    timeframe: Literal['1d', '7d', '30d'],
    *,
    from_date: TimestampSeconds | None = None,
    to_date: TimestampSeconds | None = None,
    tokens: list[Token],
    validate: bool | None = None,
  ) -> TokenAnalyticsTimeseriesResponse:
    """Fetch timeseries buy volume, sell volume, liquidity and FDV for multiple tokens. Accepts an array of up to 200 `tokens`, each requiring `chain` and `tokenAddress`.

    Args:
      timeframe: The timeframe to query
      from_date: Inclusive lower timestamp bound for the analytics timeseries window.
      to_date: Inclusive upper timestamp bound for the analytics timeseries window.
      tokens: The tokens to be fetched
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/solana/token/market-metrics/token-analytics-timeseries)
    """
    request: Request = Request(timeframe=timeframe, tokens=tokens)
    if from_date is not None:
      request['fromDate'] = from_date
    if to_date is not None:
      request['toDate'] = to_date
    return await self.request(
      request,
      method='POST',
      path='/tokens/analytics/timeseries',
      validate=validate,
      request_type=Request,
      response_type=TokenAnalyticsTimeseriesResponse,
    )
