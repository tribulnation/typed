# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_moralis.core import RpcEndpoint


class TokenMetadataBatchRequest(TypedDict):
  addresses: list[str]


class TokenMetadataBatch(RpcEndpoint):
  async def __call__(
    self,
    network: Literal['mainnet', 'devnet'],
    token_metadata_batch_request: TokenMetadataBatchRequest,
    *,
    validate: bool | None = None,
  ):
    """Get multiple global token metadata for a given network and contract (mint, standard, name, symbol, metaplex).

    Args:
      network: The network to query
      token_metadata_batch_request: Request payload for get multiple token metadata.
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/data-api/solana/token/token-metadata-batch)
    """
    response = await self.request(
      'POST',
      f'/token/{network}/metadata',
      json=token_metadata_batch_request,
    )
