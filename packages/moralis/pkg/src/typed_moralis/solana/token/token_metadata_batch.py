# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, Literal, TypedDict
from typed_moralis.core.rest import SolanaEndpoint


class Request(TypedDict):
  network: Literal['mainnet', 'devnet']
  """The network to query"""
  addresses: list[str]
  """Solana token mint addresses to fetch metadata for (up to 100)."""


Response = list[dict[str, Any]]


class TokenMetadataBatch(SolanaEndpoint):
  """Get multiple global token metadata for a given network and contract (mint, standard, name, symbol, metaplex)."""

  async def token_metadata_batch(
    self,
    network: Literal['mainnet', 'devnet'],
    *,
    addresses: list[str],
    validate: bool | None = None,
  ) -> Response:
    """Get multiple global token metadata for a given network and contract (mint, standard, name, symbol, metaplex).

    Args:
      network: The network to query
      addresses: Solana token mint addresses to fetch metadata for (up to 100).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/solana/token/token-metadata-batch)
    """
    return await self.request(
      Request(network=network, addresses=addresses),
      method='POST',
      path='/token/{network}/metadata',
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
