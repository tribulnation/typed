# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_moralis.core.rest import SolanaEndpoint
from typed_moralis.schemas import SolanaTokenPrice


class Request(TypedDict):
  network: Literal['mainnet', 'devnet']
  """The network to query"""
  address: str
  """The address to query"""


class TokenPrice(SolanaEndpoint):
  """Gets the token price (usd and native) for a given contract address and network."""

  async def token_price(
    self,
    network: Literal['mainnet', 'devnet'],
    *,
    address: str,
    validate: bool | None = None,
  ) -> SolanaTokenPrice:
    """Gets the token price (usd and native) for a given contract address and network.

    Args:
      network: The network to query
      address: The address to query
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/solana/price/token-price)
    """
    return await self.request(
      Request(network=network, address=address),
      method='GET',
      path='/token/{network}/{address}/price',
      validate=validate,
      request_type=Request,
      response_type=SolanaTokenPrice,
    )
