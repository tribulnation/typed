# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter


class NativePrice(TypedDict):
  """Native price."""

  value: str
  """Value."""
  symbol: str
  """Symbol."""
  name: str
  """Name."""
  decimals: int
  """Decimals."""


class TokenPriceResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  tokenAddress: str
  """Token address."""
  pairAddress: str
  """Pair address."""
  exchangeName: str
  """Exchange name."""
  exchangeAddress: str
  """Exchange address."""
  nativePrice: NativePrice
  usdPrice: float
  """USD price."""
  usdPrice24hrUsdChange: float
  """USD price24hr USD change."""
  usdPrice24hrPercentChange: float
  """USD price24hr percent change."""
  usdPrice24h: float
  """USD price24h."""
  logo: str
  """Logo."""
  name: str
  """Name."""
  symbol: str
  """Symbol."""
  score: int
  """Score."""
  isVerifiedContract: bool
  """Is verified contract."""


token_price_adapter = TypeAdapter(TokenPriceResponse)


class TokenPrice(RpcEndpoint):
  async def token_price(
    self,
    network: Literal['mainnet', 'devnet'],
    address: str,
    *,
    validate: bool | None = None,
  ) -> TokenPriceResponse:
    """Gets the token price (usd and native) for a given contract address and network.

    Args:
      network: The network to query
      address: The address to query
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/data-api/solana/price/token-price)
    """
    response = await self.request('GET', f'/token/{network}/{address}/price')
    if self.should_validate(validate):
      return token_price_adapter.validate_json(response.text)
    return response.json()
