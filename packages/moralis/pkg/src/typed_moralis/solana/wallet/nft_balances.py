# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_moralis.core.rest import SolanaEndpoint


class Request(TypedDict):
  network: Literal['mainnet', 'devnet']
  """The network to query"""
  address: str
  """The address to query"""
  nftMetadata: NotRequired[bool]
  """Should return the full NFT metadata"""
  mediaItems: NotRequired[bool]
  """Should return media items"""
  excludeSpam: NotRequired[bool]
  """Should exclude spam NFTs"""
  includeFungibleAssets: NotRequired[bool]
  """Should include fungible assets (tokenStandard:1)"""


Response = list[dict[str, Any]]


class NftBalances(SolanaEndpoint):
  """Gets NFTs owned by the given address"""

  async def nft_balances(
    self,
    network: Literal['mainnet', 'devnet'],
    *,
    address: str,
    nft_metadata: bool | None = None,
    media_items: bool | None = None,
    exclude_spam: bool | None = None,
    include_fungible_assets: bool | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Gets NFTs owned by the given address

    Args:
      network: The network to query
      address: The address to query
      nft_metadata: Should return the full NFT metadata
      media_items: Should return media items
      exclude_spam: Should exclude spam NFTs
      include_fungible_assets: Should include fungible assets (tokenStandard:1)
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/solana/wallet/nft-balances)
    """
    request: Request = Request(network=network, address=address)
    if nft_metadata is not None:
      request['nftMetadata'] = nft_metadata
    if media_items is not None:
      request['mediaItems'] = media_items
    if exclude_spam is not None:
      request['excludeSpam'] = exclude_spam
    if include_fungible_assets is not None:
      request['includeFungibleAssets'] = include_fungible_assets
    return await self.request(
      request,
      method='GET',
      path='/account/{network}/{address}/nft',
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
