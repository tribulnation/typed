# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, Literal
from typed_moralis.core import RpcEndpoint, clean_params
from pydantic import TypeAdapter

nft_balances_adapter = TypeAdapter(list[dict[str, Any]])


class NftBalances(RpcEndpoint):
  async def nft_balances(
    self,
    network: Literal['mainnet', 'devnet'],
    address: str,
    *,
    nft_metadata: bool | None = None,
    media_items: bool | None = None,
    exclude_spam: bool | None = None,
    include_fungible_assets: bool | None = None,
    validate: bool | None = None,
  ) -> list[dict[str, Any]]:
    """Gets NFTs owned by the given address

    Args:
      network: The network to query
      address: The address to query
      nft_metadata: Should return the full NFT metadata
      media_items: Should return media items
      exclude_spam: Should exclude spam NFTs
      include_fungible_assets: Should include fungible assets (tokenStandard:1)
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/data-api/solana/wallet/nft-balances)
    """
    params = clean_params(
      {
        'nftMetadata': nft_metadata,
        'mediaItems': media_items,
        'excludeSpam': exclude_spam,
        'includeFungibleAssets': include_fungible_assets,
      }
    )
    response = await self.request(
      'GET', f'/account/{network}/{address}/nft', params=params
    )
    if self.should_validate(validate):
      return nft_balances_adapter.validate_json(response.text)
    return response.json()
