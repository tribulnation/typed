# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, TypedDict
from typed_moralis.core.rest import SolanaEndpoint
from typed_moralis.schemas import SolBalance


class Request(TypedDict):
  network: Literal['mainnet', 'devnet']
  """The network to query"""
  address: str
  """The address to query"""


class NativeBalance(SolanaEndpoint):
  """Gets native balance owned by the given address"""

  async def native_balance(
    self,
    network: Literal['mainnet', 'devnet'],
    *,
    address: str,
    validate: bool | None = None,
  ) -> SolBalance:
    """Gets native balance owned by the given address

    Args:
      network: The network to query
      address: The address to query
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/solana/wallet/native-balance)
    """
    return await self.request(
      Request(network=network, address=address),
      method='GET',
      path='/account/{network}/{address}/balance',
      validate=validate,
      request_type=Request,
      response_type=SolBalance,
    )
