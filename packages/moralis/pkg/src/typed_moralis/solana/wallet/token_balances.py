# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_moralis.core.rest import SolanaEndpoint


class Request(TypedDict):
  network: Literal['mainnet', 'devnet']
  """The network to query"""
  address: str
  """The address to query"""
  excludeSpam: NotRequired[bool]
  """Should exclude spam tokens"""


Response = list[dict[str, Any]]


class TokenBalances(SolanaEndpoint):
  """Gets token balances owned by the given address"""

  async def token_balances(
    self,
    network: Literal['mainnet', 'devnet'],
    *,
    address: str,
    exclude_spam: bool | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Gets token balances owned by the given address

    Args:
      network: The network to query
      address: The address to query
      exclude_spam: Should exclude spam tokens
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/solana/wallet/token-balances)
    """
    request: Request = Request(network=network, address=address)
    if exclude_spam is not None:
      request['excludeSpam'] = exclude_spam
    return await self.request(
      request,
      method='GET',
      path='/account/{network}/{address}/tokens',
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
