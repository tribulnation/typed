# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .nft import Nft
from .price import Price
from .token import Token
from .token_search import TokenSearch
from .wallet import Wallet


class Solana(TokenSearch):
  """Solana Data API — wallet, token, and NFT data with Solana-native semantics and structures, distinct from the cross-chain Universal API.

  References:
    - [Upstream docs](https://docs.moralis.com/data-api/solana/overview)
  """

  @cached_property
  def nft(self) -> Nft:
    """Solana NFT metadata — token details, attributes, and media URLs.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/solana/nft/overview)
    """
    return Nft(client=self.client)

  @cached_property
  def price(self) -> Price:
    """Real-time and historical price data for Solana SPL tokens, including OHLC candlestick data.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/solana/price/overview)
    """
    return Price(client=self.client)

  @cached_property
  def token(self) -> Token:
    """Solana token metadata, prices, holders, swaps, pairs, and advanced analytics for SPL tokens.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/solana/token/overview)
    """
    return Token(client=self.client)

  @cached_property
  def wallet(self) -> Wallet:
    """Solana wallet data — native SOL balance, SPL token holdings, NFT balances, portfolio overview, and swap history.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/solana/wallet/overview)
    """
    return Wallet(client=self.client)
