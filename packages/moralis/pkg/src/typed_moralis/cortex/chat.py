# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict, deprecated
from typed_moralis.core.rest import CortexEndpoint


class ChatResponse(TypedDict):
  text: str
  """Text."""


class Request(TypedDict):
  prompt: str
  """Natural-language question to ask, grounded in Moralis's own onchain data."""
  model: NotRequired[Literal['gpt-4.1-mini', 'gpt-4.1-nano']]
  """Select model"""
  stream: NotRequired[bool]
  """Stream over SSE"""


class Chat(CortexEndpoint):
  """Chat"""

  @deprecated('Deprecated method')
  async def chat(
    self,
    prompt: str,
    *,
    model: Literal['gpt-4.1-mini', 'gpt-4.1-nano'] | None = None,
    stream: bool | None = None,
    validate: bool | None = None,
  ) -> ChatResponse:
    """Chat

    Args:
      prompt: Natural-language question to ask, grounded in Moralis's own onchain data.
      model: Select model
      stream: Stream over SSE
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/cortex-api/chat)
    """
    request: Request = Request(prompt=prompt)
    if model is not None:
      request['model'] = model
    if stream is not None:
      request['stream'] = stream
    return await self.request(
      request,
      method='POST',
      path='/chat',
      validate=validate,
      request_type=Request,
      response_type=ChatResponse,
    )
