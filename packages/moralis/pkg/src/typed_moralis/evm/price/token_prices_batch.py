# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain, EvmTokenPrice


class Token(TypedDict):
  token_address: str
  """The contract address"""
  exchange: NotRequired[str]
  """The exchange"""
  to_block: NotRequired[str]
  """The block number"""


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  include: NotRequired[Literal['percent_change']]
  """This parameter is now deprecated as percentage change are included by default"""
  max_token_inactivity: NotRequired[float]
  """Exclude tokens inactive for more than the given amount of days"""
  min_pair_side_liquidity_usd: NotRequired[float]
  """Exclude tokens with liquidity less than the specified amount in USD. This parameter refers to the liquidity on a single side of the pair."""
  tokens: list[Token]
  """The tokens to be fetched"""


Response = list[EvmTokenPrice]


class TokenPricesBatch(DeepIndexEndpoint):
  """Retrieve the current or historical prices for multiple ERC20 tokens in the blockchain’s native currency and USD. Accepts an array of up to 100 `tokens`, each requiring `token_address` and optional fields such as `to_block` or `exchange`. Each token returned includes on-chain metadata, as well as off-chain metadata, logos, spam status and more. Additional options to exclude low-liquidity tokens and inactive tokens."""

  async def token_prices_batch(
    self,
    chain: EvmChain | None = None,
    *,
    include: Literal['percent_change'] | None = None,
    max_token_inactivity: float | None = None,
    min_pair_side_liquidity_usd: float | None = None,
    tokens: list[Token],
    validate: bool | None = None,
  ) -> Response:
    """Retrieve the current or historical prices for multiple ERC20 tokens in the blockchain’s native currency and USD. Accepts an array of up to 100 `tokens`, each requiring `token_address` and optional fields such as `to_block` or `exchange`. Each token returned includes on-chain metadata, as well as off-chain metadata, logos, spam status and more. Additional options to exclude low-liquidity tokens and inactive tokens.

    Args:
      chain:
      include: This parameter is now deprecated as percentage change are included by default
      max_token_inactivity: Exclude tokens inactive for more than the given amount of days
      min_pair_side_liquidity_usd: Exclude tokens with liquidity less than the specified amount in USD. This parameter refers to the liquidity on a single side of the pair.
      tokens: The tokens to be fetched
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/price/token-prices-batch)
    """
    request: Request = Request(tokens=tokens)
    if chain is not None:
      request['chain'] = chain
    if include is not None:
      request['include'] = include
    if max_token_inactivity is not None:
      request['max_token_inactivity'] = max_token_inactivity
    if min_pair_side_liquidity_usd is not None:
      request['min_pair_side_liquidity_usd'] = min_pair_side_liquidity_usd
    return await self.request(
      request,
      method='POST',
      path='/erc20/prices',
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
