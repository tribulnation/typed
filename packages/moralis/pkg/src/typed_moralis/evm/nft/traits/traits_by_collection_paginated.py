# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain


class TraitsByCollectionPaginatedResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  page: int
  """Page."""
  page_size: int
  """Page size."""
  cursor: str | None
  """Cursor."""
  result: list[dict[str, Any]]
  """Result."""


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  address: str
  """The address of the NFT contract"""
  cursor: NotRequired[str]
  """The cursor returned in the previous response (used for getting the next page)."""
  limit: NotRequired[int]
  """The desired page size of the result."""
  order: NotRequired[Literal['ASC', 'DESC']]
  """The order of the result, in ascending (ASC) or descending (DESC)"""


class TraitsByCollectionPaginated(DeepIndexEndpoint):
  """Fetch NFT traits for a contract with pagination for efficient navigation of large collections."""

  def traits_by_collection_paginated_paged(
    self,
    chain: EvmChain | None = None,
    *,
    address: str,
    limit: int | None = None,
    order: Literal['ASC', 'DESC'] | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[dict[str, Any], str]:
    """Fetch NFT traits for a contract with pagination for efficient navigation of large collections.

    Paged variant of `traits_by_collection_paginated`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      chain:
      address: The address of the NFT contract
      limit: The desired page size of the result.
      order: The order of the result, in ascending (ASC) or descending (DESC)
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/nft/traits/traits-by-collection-paginated)
    """

    async def next(cursor: str) -> tuple[list[dict[str, Any]], str | None]:
      response = await self.traits_by_collection_paginated(
        chain,
        address=address,
        limit=limit,
        order=order,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('result')
      state = response.get('cursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def traits_by_collection_paginated(
    self,
    chain: EvmChain | None = None,
    *,
    address: str,
    cursor: str | None = None,
    limit: int | None = None,
    order: Literal['ASC', 'DESC'] | None = None,
    validate: bool | None = None,
  ) -> TraitsByCollectionPaginatedResponse:
    """Fetch NFT traits for a contract with pagination for efficient navigation of large collections.

    Args:
      chain:
      address: The address of the NFT contract
      cursor: The cursor returned in the previous response (used for getting the next page).
      limit: The desired page size of the result.
      order: The order of the result, in ascending (ASC) or descending (DESC)
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/nft/traits/traits-by-collection-paginated)
    """
    request: Request = Request(address=address)
    if chain is not None:
      request['chain'] = chain
    if cursor is not None:
      request['cursor'] = cursor
    if limit is not None:
      request['limit'] = limit
    if order is not None:
      request['order'] = order
    return await self.request(
      request,
      method='GET',
      path='/nft/{address}/traits/paginate',
      validate=validate,
      request_type=Request,
      response_type=TraitsByCollectionPaginatedResponse,
    )
