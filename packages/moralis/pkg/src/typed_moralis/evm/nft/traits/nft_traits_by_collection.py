# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain


class NftTraitsByCollectionResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  traits: list[dict[str, Any]]
  """Traits."""


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  address: str
  """The address of the NFT contract"""


class NftTraitsByCollection(DeepIndexEndpoint):
  """Explore the distinct traits of NFTs in a contract, showcasing their unique attributes."""

  async def nft_traits_by_collection(
    self,
    chain: EvmChain | None = None,
    *,
    address: str,
    validate: bool | None = None,
  ) -> NftTraitsByCollectionResponse:
    """Explore the distinct traits of NFTs in a contract, showcasing their unique attributes.

    Args:
      chain:
      address: The address of the NFT contract
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/nft/traits/traits-by-collection)
    """
    request: Request = Request(address=address)
    if chain is not None:
      request['chain'] = chain
    return await self.request(
      request,
      method='GET',
      path='/nft/{address}/traits',
      validate=validate,
      request_type=Request,
      response_type=NftTraitsByCollectionResponse,
    )
