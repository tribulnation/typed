# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Any, NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain


class NftTradesByTokenIdResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  page: int
  """Page."""
  page_size: int
  """Page size."""
  cursor: str | None
  """Cursor."""
  result: list[dict[str, Any]]
  """Result."""


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  from_block: NotRequired[int]
  """The minimum block number from which to get the transfers
* Provide the param 'from_block' or 'from_date'
* If 'from_date' and 'from_block' are provided, 'from_block' will be used."""
  to_block: NotRequired[str]
  """The block number to get the trades from"""
  from_date: NotRequired[str]
  """The start date from which to get the transfers (format in seconds or datestring accepted by momentjs)
* Provide the param 'from_block' or 'from_date'
* If 'from_date' and 'from_block' are provided, 'from_block' will be used."""
  to_date: NotRequired[str]
  """The end date from which to get the transfers (format in seconds or datestring accepted by momentjs)
* Provide the param 'to_block' or 'to_date'
* If 'to_date' and 'to_block' are provided, 'to_block' will be used."""
  cursor: NotRequired[str]
  """The cursor returned in the previous response (used for getting the next page)."""
  limit: NotRequired[int]
  """The desired page size of the result."""
  nft_metadata: NotRequired[bool]
  """Include the NFT Metadata of the NFT Token"""
  address: str
  """The address of the NFT contract"""
  token_id: str
  """The token ID of the NFT contract"""


class NftTradesByTokenId(DeepIndexEndpoint):
  """Get NFT trades for a specific NFT by specifying a contract addres and token ID."""

  def nft_trades_by_token_id_paged(
    self,
    chain: EvmChain | None = None,
    *,
    from_block: int | None = None,
    to_block: str | None = None,
    from_date: str | None = None,
    to_date: str | None = None,
    limit: int | None = None,
    nft_metadata: bool | None = None,
    address: str,
    token_id: str,
    validate: bool | None = None,
  ) -> PaginatedResponse[dict[str, Any], str]:
    """Get NFT trades for a specific NFT by specifying a contract addres and token ID.

    Paged variant of `nft_trades_by_token_id`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      chain:
      from_block: The minimum block number from which to get the transfers
    * Provide the param 'from_block' or 'from_date'
    * If 'from_date' and 'from_block' are provided, 'from_block' will be used.
      to_block: The block number to get the trades from
      from_date: The start date from which to get the transfers (format in seconds or datestring accepted by momentjs)
    * Provide the param 'from_block' or 'from_date'
    * If 'from_date' and 'from_block' are provided, 'from_block' will be used.
      to_date: The end date from which to get the transfers (format in seconds or datestring accepted by momentjs)
    * Provide the param 'to_block' or 'to_date'
    * If 'to_date' and 'to_block' are provided, 'to_block' will be used.
      limit: The desired page size of the result.
      nft_metadata: Include the NFT Metadata of the NFT Token
      address: The address of the NFT contract
      token_id: The token ID of the NFT contract
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/nft/trades/trades-by-token-id)
    """

    async def next(cursor: str) -> tuple[list[dict[str, Any]], str | None]:
      response = await self.nft_trades_by_token_id(
        chain,
        from_block=from_block,
        to_block=to_block,
        from_date=from_date,
        to_date=to_date,
        limit=limit,
        nft_metadata=nft_metadata,
        address=address,
        token_id=token_id,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('result')
      state = response.get('cursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def nft_trades_by_token_id(
    self,
    chain: EvmChain | None = None,
    *,
    from_block: int | None = None,
    to_block: str | None = None,
    from_date: str | None = None,
    to_date: str | None = None,
    cursor: str | None = None,
    limit: int | None = None,
    nft_metadata: bool | None = None,
    address: str,
    token_id: str,
    validate: bool | None = None,
  ) -> NftTradesByTokenIdResponse:
    """Get NFT trades for a specific NFT by specifying a contract addres and token ID.

    Args:
      chain:
      from_block: The minimum block number from which to get the transfers
    * Provide the param 'from_block' or 'from_date'
    * If 'from_date' and 'from_block' are provided, 'from_block' will be used.
      to_block: The block number to get the trades from
      from_date: The start date from which to get the transfers (format in seconds or datestring accepted by momentjs)
    * Provide the param 'from_block' or 'from_date'
    * If 'from_date' and 'from_block' are provided, 'from_block' will be used.
      to_date: The end date from which to get the transfers (format in seconds or datestring accepted by momentjs)
    * Provide the param 'to_block' or 'to_date'
    * If 'to_date' and 'to_block' are provided, 'to_block' will be used.
      cursor: The cursor returned in the previous response (used for getting the next page).
      limit: The desired page size of the result.
      nft_metadata: Include the NFT Metadata of the NFT Token
      address: The address of the NFT contract
      token_id: The token ID of the NFT contract
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/nft/trades/trades-by-token-id)
    """
    request: Request = Request(address=address, token_id=token_id)
    if chain is not None:
      request['chain'] = chain
    if from_block is not None:
      request['from_block'] = from_block
    if to_block is not None:
      request['to_block'] = to_block
    if from_date is not None:
      request['from_date'] = from_date
    if to_date is not None:
      request['to_date'] = to_date
    if cursor is not None:
      request['cursor'] = cursor
    if limit is not None:
      request['limit'] = limit
    if nft_metadata is not None:
      request['nft_metadata'] = nft_metadata
    return await self.request(
      request,
      method='GET',
      path='/nft/{address}/{token_id}/trades',
      validate=validate,
      request_type=Request,
      response_type=NftTradesByTokenIdResponse,
    )
