# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain


class OwnersByTokenIdResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  status: str
  """Status."""
  page: int
  """Page."""
  page_size: int
  """Page size."""
  cursor: str | None
  """Cursor."""
  result: list[dict[str, Any]]
  """Result."""


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  address: str
  """The address of the NFT contract"""
  token_id: str
  """The ID of the token"""
  format: NotRequired[Literal['decimal', 'hex']]
  """The format of the token ID"""
  limit: NotRequired[int]
  """The desired page size of the result."""
  cursor: NotRequired[str]
  """The cursor returned in the previous response (used for getting the next page)."""
  normalizeMetadata: NotRequired[bool]
  """Should normalized metadata be returned?"""
  media_items: NotRequired[bool]
  """Should preview media data be returned?"""


class OwnersByTokenId(DeepIndexEndpoint):
  """List wallets owning a specific NFT, defined by its contract and token ID."""

  def owners_by_token_id_paged(
    self,
    chain: EvmChain | None = None,
    *,
    address: str,
    token_id: str,
    format: Literal['decimal', 'hex'] | None = None,
    limit: int | None = None,
    normalize_metadata: bool | None = None,
    media_items: bool | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[dict[str, Any], str]:
    """List wallets owning a specific NFT, defined by its contract and token ID.

    Paged variant of `owners_by_token_id`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      chain:
      address: The address of the NFT contract
      token_id: The ID of the token
      format: The format of the token ID
      limit: The desired page size of the result.
      normalize_metadata: Should normalized metadata be returned?
      media_items: Should preview media data be returned?
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/nft/ownership/owners-by-token-id)
    """

    async def next(cursor: str) -> tuple[list[dict[str, Any]], str | None]:
      response = await self.owners_by_token_id(
        chain,
        address=address,
        token_id=token_id,
        format=format,
        limit=limit,
        normalize_metadata=normalize_metadata,
        media_items=media_items,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('result')
      state = response.get('cursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def owners_by_token_id(
    self,
    chain: EvmChain | None = None,
    *,
    address: str,
    token_id: str,
    format: Literal['decimal', 'hex'] | None = None,
    limit: int | None = None,
    cursor: str | None = None,
    normalize_metadata: bool | None = None,
    media_items: bool | None = None,
    validate: bool | None = None,
  ) -> OwnersByTokenIdResponse:
    """List wallets owning a specific NFT, defined by its contract and token ID.

    Args:
      chain:
      address: The address of the NFT contract
      token_id: The ID of the token
      format: The format of the token ID
      limit: The desired page size of the result.
      cursor: The cursor returned in the previous response (used for getting the next page).
      normalize_metadata: Should normalized metadata be returned?
      media_items: Should preview media data be returned?
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/nft/ownership/owners-by-token-id)
    """
    request: Request = Request(address=address, token_id=token_id)
    if chain is not None:
      request['chain'] = chain
    if format is not None:
      request['format'] = format
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    if normalize_metadata is not None:
      request['normalizeMetadata'] = normalize_metadata
    if media_items is not None:
      request['media_items'] = media_items
    return await self.request(
      request,
      method='GET',
      path='/nft/{address}/{token_id}/owners',
      validate=validate,
      request_type=Request,
      response_type=OwnersByTokenIdResponse,
    )
