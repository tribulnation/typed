# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain


class NftTransfersByTokenIdResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  page: int
  """Page."""
  page_size: int
  """Page size."""
  cursor: str | None
  """Cursor."""
  result: list[dict[str, Any]]
  """Result."""
  block_exists: bool
  """Block exists."""


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  address: str
  """The address of the NFT contract"""
  token_id: str
  """The ID of the token"""
  format: NotRequired[Literal['decimal', 'hex']]
  """The format of the token ID"""
  include_prices: NotRequired[bool]
  """Should NFT last sale prices be included in the result?"""
  limit: NotRequired[int]
  """The desired page size of the result."""
  order: NotRequired[Literal['ASC', 'DESC']]
  """The order of the result, in ascending (ASC) or descending (DESC)"""
  cursor: NotRequired[str]
  """The cursor returned in the previous response (used for getting the next page)."""


class NftTransfersByTokenId(DeepIndexEndpoint):
  """Get transfers of a specific NFT given a contract address and token ID."""

  def nft_transfers_by_token_id_paged(
    self,
    chain: EvmChain | None = None,
    *,
    address: str,
    token_id: str,
    format: Literal['decimal', 'hex'] | None = None,
    include_prices: bool | None = None,
    limit: int | None = None,
    order: Literal['ASC', 'DESC'] | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[dict[str, Any], str]:
    """Get transfers of a specific NFT given a contract address and token ID.

    Paged variant of `nft_transfers_by_token_id`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      chain:
      address: The address of the NFT contract
      token_id: The ID of the token
      format: The format of the token ID
      include_prices: Should NFT last sale prices be included in the result?
      limit: The desired page size of the result.
      order: The order of the result, in ascending (ASC) or descending (DESC)
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/nft/transfers/token-id-transfers)
    """

    async def next(cursor: str) -> tuple[list[dict[str, Any]], str | None]:
      response = await self.nft_transfers_by_token_id(
        chain,
        address=address,
        token_id=token_id,
        format=format,
        include_prices=include_prices,
        limit=limit,
        order=order,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('result')
      state = response.get('cursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def nft_transfers_by_token_id(
    self,
    chain: EvmChain | None = None,
    *,
    address: str,
    token_id: str,
    format: Literal['decimal', 'hex'] | None = None,
    include_prices: bool | None = None,
    limit: int | None = None,
    order: Literal['ASC', 'DESC'] | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> NftTransfersByTokenIdResponse:
    """Get transfers of a specific NFT given a contract address and token ID.

    Args:
      chain:
      address: The address of the NFT contract
      token_id: The ID of the token
      format: The format of the token ID
      include_prices: Should NFT last sale prices be included in the result?
      limit: The desired page size of the result.
      order: The order of the result, in ascending (ASC) or descending (DESC)
      cursor: The cursor returned in the previous response (used for getting the next page).
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/nft/transfers/token-id-transfers)
    """
    request: Request = Request(address=address, token_id=token_id)
    if chain is not None:
      request['chain'] = chain
    if format is not None:
      request['format'] = format
    if include_prices is not None:
      request['include_prices'] = include_prices
    if limit is not None:
      request['limit'] = limit
    if order is not None:
      request['order'] = order
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/nft/{address}/{token_id}/transfers',
      validate=validate,
      request_type=Request,
      response_type=NftTransfersByTokenIdResponse,
    )
