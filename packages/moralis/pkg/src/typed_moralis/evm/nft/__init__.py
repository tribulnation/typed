# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .collections import Collections
from .discovery import Discovery
from .metadata import Metadata
from .ownership import Ownership
from .prices import Prices
from .trades import Trades
from .traits import Traits
from .transfers import Transfers
from .utilities import Utilities
from typed_moralis.core.rest import DeepIndexEndpoint


class Nft(DeepIndexEndpoint):
  """NFT metadata, ownership, trading history, pricing, and collection analytics across EVM chains.

  References:
    - [Upstream docs](https://docs.moralis.com/data-api/evm/nft/overview)
  """

  @cached_property
  def collections(self) -> Collections:
    """NFT collection lookup: every collection held by a wallet, or every NFT within one contract.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/evm/nft/overview)
    """
    return Collections(client=self.client)

  @cached_property
  def discovery(self) -> Discovery:
    """Trending EVM NFT collections, ranked by market cap or by trading volume.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/evm/nft/overview)
    """
    return Discovery(client=self.client)

  @cached_property
  def metadata(self) -> Metadata:
    """Contract- and token-level NFT metadata, plus per-collection summary stats, single or batched.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/evm/nft/overview)
    """
    return Metadata(client=self.client)

  @cached_property
  def ownership(self) -> Ownership:
    """Current owners of an NFT collection, or of one specific token within it.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/evm/nft/overview)
    """
    return Ownership(client=self.client)

  @cached_property
  def prices(self) -> Prices:
    """Floor and sale price data for NFT collections and individual tokens, including historical floor-price series.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/evm/nft/overview)
    """
    return Prices(client=self.client)

  @cached_property
  def trades(self) -> Trades:
    """Historical trade activity for an NFT collection, or for one specific token within it.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/evm/nft/overview)
    """
    return Trades(client=self.client)

  @cached_property
  def traits(self) -> Traits:
    """NFT trait listing and rarity data for a collection, plus trait-based NFT search.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/evm/nft/overview)
    """
    return Traits(client=self.client)

  @cached_property
  def transfers(self) -> Transfers:
    """NFT transfer history, by contract address or by individual token ID.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/evm/nft/overview)
    """
    return Transfers(client=self.client)

  @cached_property
  def utilities(self) -> Utilities:
    """Resync jobs that force Moralis to refresh a stale NFT's cached metadata or traits.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/evm/nft/overview)
    """
    return Utilities(client=self.client)
