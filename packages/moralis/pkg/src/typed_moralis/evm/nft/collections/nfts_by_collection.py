# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain


class NftsByCollectionResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  cursor: str | None
  """Cursor."""
  page: int
  """Page."""
  page_size: int
  """Page size."""
  result: list[dict[str, Any]]
  """Result."""


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  address: str
  """The address of the NFT contract"""
  format: NotRequired[Literal['decimal', 'hex']]
  """The format of the token ID"""
  limit: NotRequired[int]
  """The desired page size of the result."""
  totalRanges: NotRequired[int]
  """The number of subranges to split the results into"""
  range: NotRequired[int]
  """The desired subrange to query"""
  cursor: NotRequired[str]
  """The cursor returned in the previous response (used for getting the next page)."""
  normalizeMetadata: NotRequired[bool]
  """Should normalized metadata be returned?"""
  media_items: NotRequired[bool]
  """Should preview media data be returned?"""
  include_prices: NotRequired[bool]
  """Should NFT last sale prices be included in the result?"""


class NftsByCollection(DeepIndexEndpoint):
  """Fetch all NFTs for a given collection. Each NFT returned includes on-chain metadata as well as off-chain metadata, floor prices, rarity and more where available."""

  def nfts_by_collection_paged(
    self,
    chain: EvmChain | None = None,
    *,
    address: str,
    format: Literal['decimal', 'hex'] | None = None,
    limit: int | None = None,
    total_ranges: int | None = None,
    range: int | None = None,
    normalize_metadata: bool | None = None,
    media_items: bool | None = None,
    include_prices: bool | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[dict[str, Any], str]:
    """Fetch all NFTs for a given collection. Each NFT returned includes on-chain metadata as well as off-chain metadata, floor prices, rarity and more where available.

    Paged variant of `nfts_by_collection`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      chain:
      address: The address of the NFT contract
      format: The format of the token ID
      limit: The desired page size of the result.
      total_ranges: The number of subranges to split the results into
      range: The desired subrange to query
      normalize_metadata: Should normalized metadata be returned?
      media_items: Should preview media data be returned?
      include_prices: Should NFT last sale prices be included in the result?
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/nft/collections/nfts-by-collection)
    """

    async def next(cursor: str) -> tuple[list[dict[str, Any]], str | None]:
      response = await self.nfts_by_collection(
        chain,
        address=address,
        format=format,
        limit=limit,
        total_ranges=total_ranges,
        range=range,
        normalize_metadata=normalize_metadata,
        media_items=media_items,
        include_prices=include_prices,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('result')
      state = response.get('cursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def nfts_by_collection(
    self,
    chain: EvmChain | None = None,
    *,
    address: str,
    format: Literal['decimal', 'hex'] | None = None,
    limit: int | None = None,
    total_ranges: int | None = None,
    range: int | None = None,
    cursor: str | None = None,
    normalize_metadata: bool | None = None,
    media_items: bool | None = None,
    include_prices: bool | None = None,
    validate: bool | None = None,
  ) -> NftsByCollectionResponse:
    """Fetch all NFTs for a given collection. Each NFT returned includes on-chain metadata as well as off-chain metadata, floor prices, rarity and more where available.

    Args:
      chain:
      address: The address of the NFT contract
      format: The format of the token ID
      limit: The desired page size of the result.
      total_ranges: The number of subranges to split the results into
      range: The desired subrange to query
      cursor: The cursor returned in the previous response (used for getting the next page).
      normalize_metadata: Should normalized metadata be returned?
      media_items: Should preview media data be returned?
      include_prices: Should NFT last sale prices be included in the result?
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/nft/collections/nfts-by-collection)
    """
    request: Request = Request(address=address)
    if chain is not None:
      request['chain'] = chain
    if format is not None:
      request['format'] = format
    if limit is not None:
      request['limit'] = limit
    if total_ranges is not None:
      request['totalRanges'] = total_ranges
    if range is not None:
      request['range'] = range
    if cursor is not None:
      request['cursor'] = cursor
    if normalize_metadata is not None:
      request['normalizeMetadata'] = normalize_metadata
    if media_items is not None:
      request['media_items'] = media_items
    if include_prices is not None:
      request['include_prices'] = include_prices
    return await self.request(
      request,
      method='GET',
      path='/nft/{address}',
      validate=validate,
      request_type=Request,
      response_type=NftsByCollectionResponse,
    )
