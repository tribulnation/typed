# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain


class Result(TypedDict):
  token_address: str
  """Token address."""
  possible_spam: bool
  """Possible spam."""
  contract_type: str
  """Contract type."""
  name: str
  """Name."""
  symbol: str
  """Symbol."""
  verified_collection: bool
  """Verified collection."""
  collection_logo: str
  """Collection logo."""
  collection_banner_image: str
  """Collection banner image."""
  floor_price: str | None
  """Floor price."""
  floor_price_usd: str | None
  """Floor price USD."""
  floor_price_currency: str | None
  """Floor price currency."""


class NftCollectionsByWalletResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  status: str
  """Status."""
  page: int
  """Page."""
  cursor: str | None
  """Cursor."""
  page_size: int
  """Page size."""
  result: list[Result]
  """Result."""


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  address: str
  """The wallet address of the owner of NFTs in the collections"""
  include_prices: NotRequired[bool]
  """Should NFT last sale prices be included in the result?"""
  limit: NotRequired[int]
  """The desired page size of the result."""
  exclude_spam: NotRequired[bool]
  """Should spam NFTs be excluded from the result?"""
  cursor: NotRequired[str]
  """The cursor returned in the previous response (used for getting the next page)."""
  token_counts: NotRequired[bool]
  """Should token counts per collection be included in the response?"""


class NftCollectionsByWallet(DeepIndexEndpoint):
  """Fetch all NFT Collections held by a specified wallet address. Each Collection returned includes on-chain metadata as well as off-chain metadata, floor prices and more where available."""

  def nft_collections_by_wallet_paged(
    self,
    chain: EvmChain | None = None,
    *,
    address: str,
    include_prices: bool | None = None,
    limit: int | None = None,
    exclude_spam: bool | None = None,
    token_counts: bool | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[Result, str]:
    """Fetch all NFT Collections held by a specified wallet address. Each Collection returned includes on-chain metadata as well as off-chain metadata, floor prices and more where available.

    Paged variant of `nft_collections_by_wallet`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      chain:
      address: The wallet address of the owner of NFTs in the collections
      include_prices: Should NFT last sale prices be included in the result?
      limit: The desired page size of the result.
      exclude_spam: Should spam NFTs be excluded from the result?
      token_counts: Should token counts per collection be included in the response?
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/nft/collections/nft-collections-by-wallet)
    """

    async def next(cursor: str) -> tuple[list[Result], str | None]:
      response = await self.nft_collections_by_wallet(
        chain,
        address=address,
        include_prices=include_prices,
        limit=limit,
        exclude_spam=exclude_spam,
        token_counts=token_counts,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('result')
      state = response.get('cursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def nft_collections_by_wallet(
    self,
    chain: EvmChain | None = None,
    *,
    address: str,
    include_prices: bool | None = None,
    limit: int | None = None,
    exclude_spam: bool | None = None,
    cursor: str | None = None,
    token_counts: bool | None = None,
    validate: bool | None = None,
  ) -> NftCollectionsByWalletResponse:
    """Fetch all NFT Collections held by a specified wallet address. Each Collection returned includes on-chain metadata as well as off-chain metadata, floor prices and more where available.

    Args:
      chain:
      address: The wallet address of the owner of NFTs in the collections
      include_prices: Should NFT last sale prices be included in the result?
      limit: The desired page size of the result.
      exclude_spam: Should spam NFTs be excluded from the result?
      cursor: The cursor returned in the previous response (used for getting the next page).
      token_counts: Should token counts per collection be included in the response?
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/nft/collections/nft-collections-by-wallet)
    """
    request: Request = Request(address=address)
    if chain is not None:
      request['chain'] = chain
    if include_prices is not None:
      request['include_prices'] = include_prices
    if limit is not None:
      request['limit'] = limit
    if exclude_spam is not None:
      request['exclude_spam'] = exclude_spam
    if cursor is not None:
      request['cursor'] = cursor
    if token_counts is not None:
      request['token_counts'] = token_counts
    return await self.request(
      request,
      method='GET',
      path='/{address}/nft/collections',
      validate=validate,
      request_type=Request,
      response_type=NftCollectionsByWalletResponse,
    )
