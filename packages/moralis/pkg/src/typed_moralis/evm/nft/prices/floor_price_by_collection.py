# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain, NftMarketplace


class FloorPriceByCollectionResponse(TypedDict):
  address: NotRequired[str]
  """Address."""
  floor_price: NotRequired[str]
  """Floor price."""
  floor_price_usd: NotRequired[str]
  """Floor price USD."""
  currency: NotRequired[str]
  """Currency."""
  marketplace: NotRequired[NftMarketplace]
  last_updated: NotRequired[str]
  """Last updated."""


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  address: str
  """The address of the NFT contract"""


class FloorPriceByCollection(DeepIndexEndpoint):
  """Get floor price for a given collection. Refreshes every 30 minutes."""

  async def floor_price_by_collection(
    self,
    chain: EvmChain | None = None,
    *,
    address: str,
    validate: bool | None = None,
  ) -> FloorPriceByCollectionResponse:
    """Get floor price for a given collection. Refreshes every 30 minutes.

    Args:
      chain:
      address: The address of the NFT contract
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/nft/prices/collection-floor-price)
    """
    request: Request = Request(address=address)
    if chain is not None:
      request['chain'] = chain
    return await self.request(
      request,
      method='GET',
      path='/nft/{address}/floor-price',
      validate=validate,
      request_type=Request,
      response_type=FloorPriceByCollectionResponse,
    )
