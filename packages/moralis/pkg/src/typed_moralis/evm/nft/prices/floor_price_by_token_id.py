# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain, NftMarketplace


class FloorPriceByTokenIdResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  address: str
  """Address."""
  token_id: str
  """Token ID."""
  floor_price: str | None
  """Floor price."""
  floor_price_usd: str | None
  """Floor price USD."""
  currency: str | None
  """Currency."""
  marketplace: NftMarketplace | None
  """Marketplace."""
  last_updated: str | None
  """Last updated."""


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  address: str
  """The address of the NFT contract"""
  token_id: str
  """The token ID of the NFT"""


class FloorPriceByTokenId(DeepIndexEndpoint):
  """Get the floor price for a specific NFT, defined by its contract and token ID. Refreshes every 30 minutes."""

  async def floor_price_by_token_id(
    self,
    chain: EvmChain | None = None,
    *,
    address: str,
    token_id: str,
    validate: bool | None = None,
  ) -> FloorPriceByTokenIdResponse:
    """Get the floor price for a specific NFT, defined by its contract and token ID. Refreshes every 30 minutes.

    Args:
      chain:
      address: The address of the NFT contract
      token_id: The token ID of the NFT
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/nft/prices/floor)
    """
    request: Request = Request(address=address, token_id=token_id)
    if chain is not None:
      request['chain'] = chain
    return await self.request(
      request,
      method='GET',
      path='/nft/{address}/{token_id}/floor-price',
      validate=validate,
      request_type=Request,
      response_type=FloorPriceByTokenIdResponse,
    )
