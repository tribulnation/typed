# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain


class SalePriceByTokenIdResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  message: str
  """Message."""


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  days: NotRequired[int]
  """The number of days to look back to find the lowest price
If not provided 7 days will be the default and 365 is the maximum"""
  address: str
  """The address of the NFT collection"""
  token_id: str
  """The token id of the NFT collection"""


class SalePriceByTokenId(DeepIndexEndpoint):
  """Fetch sale prices for a specific NFT over a specified number of days. Returns the last sale, lowest sale, highest sale, average sale and total trades within the specified period."""

  async def sale_price_by_token_id(
    self,
    chain: EvmChain | None = None,
    *,
    days: int | None = None,
    address: str,
    token_id: str,
    validate: bool | None = None,
  ) -> SalePriceByTokenIdResponse:
    """Fetch sale prices for a specific NFT over a specified number of days. Returns the last sale, lowest sale, highest sale, average sale and total trades within the specified period.

    Args:
      chain:
      days: The number of days to look back to find the lowest price
    If not provided 7 days will be the default and 365 is the maximum
      address: The address of the NFT collection
      token_id: The token id of the NFT collection
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/nft/prices/sale-price-by-token-id)
    """
    request: Request = Request(address=address, token_id=token_id)
    if chain is not None:
      request['chain'] = chain
    if days is not None:
      request['days'] = days
    return await self.request(
      request,
      method='GET',
      path='/nft/{address}/{token_id}/price',
      validate=validate,
      request_type=Request,
      response_type=SalePriceByTokenIdResponse,
    )
