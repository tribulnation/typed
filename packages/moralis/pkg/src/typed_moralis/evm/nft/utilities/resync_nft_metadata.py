# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain


class NftResyncStatus(TypedDict):
  status: str
  """The status of the resync request"""


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  address: str
  """The address of the NFT contract"""
  token_id: str
  """The ID of the token"""
  flag: NotRequired[Literal['uri', 'metadata']]
  """The type of resync to operate"""
  mode: NotRequired[Literal['async', 'sync']]
  """To define the behaviour of the endpoint"""


class ResyncNftMetadata(DeepIndexEndpoint):
  """Update an NFT’s metadata, either from its current token URI or a new one. Choose sync for immediate results or async for background processing."""

  async def resync_nft_metadata(
    self,
    chain: EvmChain | None = None,
    *,
    address: str,
    token_id: str,
    flag: Literal['uri', 'metadata'] | None = None,
    mode: Literal['async', 'sync'] | None = None,
    validate: bool | None = None,
  ) -> NftResyncStatus:
    """Update an NFT’s metadata, either from its current token URI or a new one. Choose sync for immediate results or async for background processing.

    Args:
      chain:
      address: The address of the NFT contract
      token_id: The ID of the token
      flag: The type of resync to operate
      mode: To define the behaviour of the endpoint
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/nft/utilities/resync-nft-metadata)
    """
    request: Request = Request(address=address, token_id=token_id)
    if chain is not None:
      request['chain'] = chain
    if flag is not None:
      request['flag'] = flag
    if mode is not None:
      request['mode'] = mode
    return await self.request(
      request,
      method='GET',
      path='/nft/{address}/{token_id}/metadata/resync',
      validate=validate,
      request_type=Request,
      response_type=NftResyncStatus,
    )
