# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain


class ResyncNftTraitsResponse(TypedDict):
  status: str
  """The status of the resync request"""


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  address: str
  """The address of the NFT contract"""


class ResyncNftTraits(DeepIndexEndpoint):
  """Refresh trait and rarity data for NFTs in a contract to ensure accuracy."""

  async def resync_nft_traits(
    self,
    chain: EvmChain | None = None,
    *,
    address: str,
    validate: bool | None = None,
  ) -> ResyncNftTraitsResponse:
    """Refresh trait and rarity data for NFTs in a contract to ensure accuracy.

    Args:
      chain:
      address: The address of the NFT contract
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/nft/utilities/resync-nft-traits)
    """
    request: Request = Request(address=address)
    if chain is not None:
      request['chain'] = chain
    return await self.request(
      request,
      method='GET',
      path='/nft/{address}/traits/resync',
      validate=validate,
      request_type=Request,
      response_type=ResyncNftTraitsResponse,
    )
