# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain, NftMetadata


class Token(TypedDict):
  token_address: NotRequired[str]
  """The contract address"""
  token_id: NotRequired[str]
  """The id of the token"""


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  tokens: list[Token]
  """The tokens to be fetched (max 25 tokens)"""
  normalizeMetadata: NotRequired[bool]
  """Should normalized metadata be returned?"""
  media_items: NotRequired[bool]
  """Should preview media data be returned?"""


Response = list[NftMetadata]


class NftMetadataBatch(DeepIndexEndpoint):
  """Get NFT metadata for one or many NFTs. Accepts an array of up to 25 `tokens`, each requiring `token_address` and `token_id`. Each NFT returned includes on-chain metadata as well as off-chain metadata, floor prices, rarity and more where available."""

  async def nft_metadata_batch(
    self,
    chain: EvmChain | None = None,
    *,
    tokens: list[Token],
    normalize_metadata: bool | None = None,
    media_items: bool | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get NFT metadata for one or many NFTs. Accepts an array of up to 25 `tokens`, each requiring `token_address` and `token_id`. Each NFT returned includes on-chain metadata as well as off-chain metadata, floor prices, rarity and more where available.

    Args:
      chain:
      tokens: The tokens to be fetched (max 25 tokens)
      normalize_metadata: Should normalized metadata be returned?
      media_items: Should preview media data be returned?
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/nft/metadata/nft-metadata-batch)
    """
    request: Request = Request(tokens=tokens)
    if chain is not None:
      request['chain'] = chain
    if normalize_metadata is not None:
      request['normalizeMetadata'] = normalize_metadata
    if media_items is not None:
      request['media_items'] = media_items
    return await self.request(
      request,
      method='POST',
      path='/nft/getMultipleNFTs',
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
