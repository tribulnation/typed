# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import CollectionMetadata, EvmChain


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  address: str
  """The address of the NFT contract"""
  include_prices: NotRequired[bool]
  """Should NFT last sale prices be included in the result?"""


class CollectionMetadataEndpoint(DeepIndexEndpoint):
  """Fetch on-chain metadata like name, symbol, and base token URI for an NFT contract. Also returns off-chain metadata, floor prices and more where available."""

  async def collection_metadata(
    self,
    chain: EvmChain | None = None,
    *,
    address: str,
    include_prices: bool | None = None,
    validate: bool | None = None,
  ) -> CollectionMetadata:
    """Fetch on-chain metadata like name, symbol, and base token URI for an NFT contract. Also returns off-chain metadata, floor prices and more where available.

    Args:
      chain:
      address: The address of the NFT contract
      include_prices: Should NFT last sale prices be included in the result?
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/nft/metadata/collection-metadata)
    """
    request: Request = Request(address=address)
    if chain is not None:
      request['chain'] = chain
    if include_prices is not None:
      request['include_prices'] = include_prices
    return await self.request(
      request,
      method='GET',
      path='/nft/{address}/metadata',
      validate=validate,
      request_type=Request,
      response_type=CollectionMetadata,
    )
