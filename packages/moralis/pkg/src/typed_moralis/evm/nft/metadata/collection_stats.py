# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain


class Owners(TypedDict):
  """Owners."""

  current: str
  """Current."""


class Transfers(TypedDict):
  """Transfers."""

  total: str
  """Total."""


class CollectionStatsResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  total_tokens: str
  """Total tokens."""
  owners: Owners
  transfers: Transfers


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  address: str
  """The address of the NFT collection"""


class CollectionStats(DeepIndexEndpoint):
  """Get summary stats by NFT collection including total NFT transfers, total tokens and total owners."""

  async def collection_stats(
    self,
    chain: EvmChain | None = None,
    *,
    address: str,
    validate: bool | None = None,
  ) -> CollectionStatsResponse:
    """Get summary stats by NFT collection including total NFT transfers, total tokens and total owners.

    Args:
      chain:
      address: The address of the NFT collection
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/nft/metadata/collection-stats)
    """
    request: Request = Request(address=address)
    if chain is not None:
      request['chain'] = chain
    return await self.request(
      request,
      method='GET',
      path='/nft/{address}/stats',
      validate=validate,
      request_type=Request,
      response_type=CollectionStatsResponse,
    )
