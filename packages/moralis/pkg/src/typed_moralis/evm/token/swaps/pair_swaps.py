# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain, SwapToken


class PairSwapsResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  page: int
  """Page."""
  cursor: str | None
  """Cursor."""
  pageSize: int
  """Page size."""
  exchangeAddress: str | None
  """Exchange address."""
  exchangeLogo: str | None
  """Exchange logo."""
  exchangeName: str | None
  """Exchange name."""
  pairAddress: str | None
  """Pair address."""
  pairLabel: str | None
  """Pair label."""
  baseToken: SwapToken
  quoteToken: SwapToken
  result: list[dict[str, Any]]
  """Result."""


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  address: str
  """The pair address token-transactions are to be retrieved for."""
  cursor: NotRequired[str]
  """The cursor returned in the previous response (used for getting the next page)."""
  limit: NotRequired[int]
  """The desired page size of the result."""
  fromBlock: NotRequired[int]
  """The minimum block number from which to get the token transactions
* Provide the param 'fromBlock' or 'fromDate'
* If 'fromDate' and 'fromBlock' are provided, 'fromBlock' will be used."""
  toBlock: NotRequired[str]
  """The block number to get the token transactions from"""
  fromDate: NotRequired[str]
  """The start date from which to get the token transactions (format in seconds or datestring accepted by momentjs)
* Provide the param 'fromBlock' or 'fromDate'
* If 'fromDate' and 'fromBlock' are provided, 'fromBlock' will be used."""
  toDate: NotRequired[str]
  """The end date from which to get the token transactions (format in seconds or datestring accepted by momentjs)
* Provide the param 'toBlock' or 'toDate'
* If 'toDate' and 'toBlock' are provided, 'toBlock' will be used."""
  order: NotRequired[Literal['ASC', 'DESC']]
  """The order of the result, in ascending (ASC) or descending (DESC)"""
  transactionTypes: NotRequired[str]
  """Array of transaction types. Allowed values are 'buy', 'sell', 'addLiquidity', 'removeLiquidity'."""


class PairSwaps(DeepIndexEndpoint):
  """Fetch swap transactions (buy, sell, add/remove liquidity) for a specific token pair."""

  def pair_swaps_paged(
    self,
    chain: EvmChain | None = None,
    *,
    address: str,
    limit: int | None = None,
    from_block: int | None = None,
    to_block: str | None = None,
    from_date: str | None = None,
    to_date: str | None = None,
    order: Literal['ASC', 'DESC'] | None = None,
    transaction_types: str | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[dict[str, Any], str]:
    """Fetch swap transactions (buy, sell, add/remove liquidity) for a specific token pair.

    Paged variant of `pair_swaps`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      chain:
      address: The pair address token-transactions are to be retrieved for.
      limit: The desired page size of the result.
      from_block: The minimum block number from which to get the token transactions
    * Provide the param 'fromBlock' or 'fromDate'
    * If 'fromDate' and 'fromBlock' are provided, 'fromBlock' will be used.
      to_block: The block number to get the token transactions from
      from_date: The start date from which to get the token transactions (format in seconds or datestring accepted by momentjs)
    * Provide the param 'fromBlock' or 'fromDate'
    * If 'fromDate' and 'fromBlock' are provided, 'fromBlock' will be used.
      to_date: The end date from which to get the token transactions (format in seconds or datestring accepted by momentjs)
    * Provide the param 'toBlock' or 'toDate'
    * If 'toDate' and 'toBlock' are provided, 'toBlock' will be used.
      order: The order of the result, in ascending (ASC) or descending (DESC)
      transaction_types: Array of transaction types. Allowed values are 'buy', 'sell', 'addLiquidity', 'removeLiquidity'.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/token/swaps/pair-swaps)
    """

    async def next(cursor: str) -> tuple[list[dict[str, Any]], str | None]:
      response = await self.pair_swaps(
        chain,
        address=address,
        limit=limit,
        from_block=from_block,
        to_block=to_block,
        from_date=from_date,
        to_date=to_date,
        order=order,
        transaction_types=transaction_types,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('result')
      state = response.get('cursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def pair_swaps(
    self,
    chain: EvmChain | None = None,
    *,
    address: str,
    cursor: str | None = None,
    limit: int | None = None,
    from_block: int | None = None,
    to_block: str | None = None,
    from_date: str | None = None,
    to_date: str | None = None,
    order: Literal['ASC', 'DESC'] | None = None,
    transaction_types: str | None = None,
    validate: bool | None = None,
  ) -> PairSwapsResponse:
    """Fetch swap transactions (buy, sell, add/remove liquidity) for a specific token pair.

    Args:
      chain:
      address: The pair address token-transactions are to be retrieved for.
      cursor: The cursor returned in the previous response (used for getting the next page).
      limit: The desired page size of the result.
      from_block: The minimum block number from which to get the token transactions
    * Provide the param 'fromBlock' or 'fromDate'
    * If 'fromDate' and 'fromBlock' are provided, 'fromBlock' will be used.
      to_block: The block number to get the token transactions from
      from_date: The start date from which to get the token transactions (format in seconds or datestring accepted by momentjs)
    * Provide the param 'fromBlock' or 'fromDate'
    * If 'fromDate' and 'fromBlock' are provided, 'fromBlock' will be used.
      to_date: The end date from which to get the token transactions (format in seconds or datestring accepted by momentjs)
    * Provide the param 'toBlock' or 'toDate'
    * If 'toDate' and 'toBlock' are provided, 'toBlock' will be used.
      order: The order of the result, in ascending (ASC) or descending (DESC)
      transaction_types: Array of transaction types. Allowed values are 'buy', 'sell', 'addLiquidity', 'removeLiquidity'.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/token/swaps/pair-swaps)
    """
    request: Request = Request(address=address)
    if chain is not None:
      request['chain'] = chain
    if cursor is not None:
      request['cursor'] = cursor
    if limit is not None:
      request['limit'] = limit
    if from_block is not None:
      request['fromBlock'] = from_block
    if to_block is not None:
      request['toBlock'] = to_block
    if from_date is not None:
      request['fromDate'] = from_date
    if to_date is not None:
      request['toDate'] = to_date
    if order is not None:
      request['order'] = order
    if transaction_types is not None:
      request['transactionTypes'] = transaction_types
    return await self.request(
      request,
      method='GET',
      path='/pairs/{address}/swaps',
      validate=validate,
      request_type=Request,
      response_type=PairSwapsResponse,
    )
