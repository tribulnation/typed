# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain


class Pair2(TypedDict):
  liquidity_usd: float
  """Liquidity USD."""
  pair_token_type: str
  """Pair token type."""
  token_address: str
  """Token address."""
  token_decimals: str
  """Token decimals."""
  token_logo: str
  """Token logo."""
  token_name: str
  """Token name."""
  token_symbol: str
  """Token symbol."""


class Pair(TypedDict):
  exchange_address: str
  """Exchange address."""
  exchange_name: str
  """Exchange name."""
  exchange_logo: str
  """Exchange logo."""
  pair_label: str
  """Pair label."""
  pair_address: str
  """Pair address."""
  usd_price: int
  """USD price."""
  usd_price_24hr: int
  """USD price 24hr."""
  usd_price_24hr_percent_change: int
  """USD price 24hr percent change."""
  usd_price_24hr_usd_change: int
  """USD price 24hr USD change."""
  liquidity_usd: float
  """Liquidity USD."""
  liquidity_skew: float
  """Liquidity skew."""
  inactive_pair: bool
  """Inactive pair."""
  base_token: str
  """Base token."""
  quote_token: str
  """Quote token."""
  volume_24h_native: float
  """Volume 24h native."""
  volume_24h_usd: float
  """Volume 24h USD."""
  pair: list[Pair2]
  """Pair."""


class Request(TypedDict):
  token_address: str
  """The address of the token"""
  chain: EvmChain
  limit: NotRequired[int]
  """The number of results to return"""
  cursor: NotRequired[str]
  """The cursor returned in the previous response (used for getting the next page)"""


class TokenPairsResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  cursor: str | None
  """Cursor."""
  page_size: int
  """Page size."""
  page: int
  """Page."""
  pairs: list[Pair]
  """Pairs."""


class TokenPairs(DeepIndexEndpoint):
  """List supported trading pairs for a specific ERC20 token. Each pair returned includes price, liquidity, volume and more."""

  def token_pairs_paged(
    self,
    *,
    token_address: str,
    chain: EvmChain,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[Pair, str]:
    """List supported trading pairs for a specific ERC20 token. Each pair returned includes price, liquidity, volume and more.

    Paged variant of `token_pairs`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      token_address: The address of the token
      chain:
      limit: The number of results to return
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/token/swaps/token-pairs)
    """

    async def next(cursor: str) -> tuple[list[Pair], str | None]:
      response = await self.token_pairs(
        token_address=token_address,
        chain=chain,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('pairs')
      state = response.get('cursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def token_pairs(
    self,
    *,
    token_address: str,
    chain: EvmChain,
    limit: int | None = None,
    cursor: str | None = None,
    validate: bool | None = None,
  ) -> TokenPairsResponse:
    """List supported trading pairs for a specific ERC20 token. Each pair returned includes price, liquidity, volume and more.

    Args:
      token_address: The address of the token
      chain:
      limit: The number of results to return
      cursor: The cursor returned in the previous response (used for getting the next page)
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/token/swaps/token-pairs)
    """
    request: Request = Request(token_address=token_address, chain=chain)
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/erc20/{token_address}/pairs',
      validate=validate,
      request_type=Request,
      response_type=TokenPairsResponse,
    )
