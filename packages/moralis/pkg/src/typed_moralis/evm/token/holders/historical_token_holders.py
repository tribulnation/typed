# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_moralis.core import TimestampIso
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import (
  EvmChain,
  HolderSizeDistributionCrab,
  HoldersByAcquisition,
)


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  tokenAddress: str
  """The token address"""
  fromDate: str
  """The starting date (format in seconds or datestring accepted by momentjs)"""
  toDate: str
  """The ending date (format in seconds or datestring accepted by momentjs)"""
  limit: NotRequired[int]
  """The number of results to return"""
  cursor: NotRequired[str]
  """The cursor returned in the previous response (used for getting the next page)"""
  timeFrame: Literal[
    '1min', '5min', '10min', '30min', '1h', '4h', '12h', '1d', '1w', '1m'
  ]
  """The time frame to group the data by"""


class Result(TypedDict):
  timestamp: NotRequired[TimestampIso]
  """Timestamp."""
  totalHolders: NotRequired[int]
  """Total holders."""
  netHolderChange: NotRequired[int]
  """Net holder change."""
  holderPercentChange: NotRequired[float]
  """Holder percent change."""
  newHoldersByAcquisition: NotRequired[HoldersByAcquisition]
  holdersIn: NotRequired[HolderSizeDistributionCrab]
  holdersOut: NotRequired[HolderSizeDistributionCrab]


class HistoricalTokenHoldersResponse(TypedDict):
  page: NotRequired[int]
  """Page."""
  cursor: NotRequired[str | None]
  """Cursor."""
  result: list[Result]
  """Result."""
  pageSize: NotRequired[int]
  """Page size."""


class HistoricalTokenHolders(DeepIndexEndpoint):
  """Track changes in the holder base of an ERC20 token over time. Supports timeseries data for total holders as well as change metrics such as holder distribution and holder acquisition."""

  def historical_token_holders_paged(
    self,
    chain: EvmChain | None = None,
    *,
    token_address: str,
    from_date: str,
    to_date: str,
    limit: int | None = None,
    time_frame: Literal[
      '1min', '5min', '10min', '30min', '1h', '4h', '12h', '1d', '1w', '1m'
    ],
    validate: bool | None = None,
  ) -> PaginatedResponse[Result, str]:
    """Track changes in the holder base of an ERC20 token over time. Supports timeseries data for total holders as well as change metrics such as holder distribution and holder acquisition.

    Paged variant of `historical_token_holders`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      chain:
      token_address: The token address
      from_date: The starting date (format in seconds or datestring accepted by momentjs)
      to_date: The ending date (format in seconds or datestring accepted by momentjs)
      limit: The number of results to return
      time_frame: The time frame to group the data by
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/token/holders/historical-token-holders)
    """

    async def next(cursor: str) -> tuple[list[Result], str | None]:
      response = await self.historical_token_holders(
        chain,
        token_address=token_address,
        from_date=from_date,
        to_date=to_date,
        limit=limit,
        time_frame=time_frame,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('result')
      state = response.get('cursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def historical_token_holders(
    self,
    chain: EvmChain | None = None,
    *,
    token_address: str,
    from_date: str,
    to_date: str,
    limit: int | None = None,
    cursor: str | None = None,
    time_frame: Literal[
      '1min', '5min', '10min', '30min', '1h', '4h', '12h', '1d', '1w', '1m'
    ],
    validate: bool | None = None,
  ) -> HistoricalTokenHoldersResponse:
    """Track changes in the holder base of an ERC20 token over time. Supports timeseries data for total holders as well as change metrics such as holder distribution and holder acquisition.

    Args:
      chain:
      token_address: The token address
      from_date: The starting date (format in seconds or datestring accepted by momentjs)
      to_date: The ending date (format in seconds or datestring accepted by momentjs)
      limit: The number of results to return
      cursor: The cursor returned in the previous response (used for getting the next page)
      time_frame: The time frame to group the data by
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/token/holders/historical-token-holders)
    """
    request: Request = Request(
      tokenAddress=token_address,
      fromDate=from_date,
      toDate=to_date,
      timeFrame=time_frame,
    )
    if chain is not None:
      request['chain'] = chain
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    return await self.request(
      request,
      method='GET',
      path='/erc20/{tokenAddress}/holders/historical',
      validate=validate,
      request_type=Request,
      response_type=HistoricalTokenHoldersResponse,
    )
