# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain, TokenHolderMetrics


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  tokenAddress: str
  """The token address to get transaction for"""


class TokenHolderMetricsEndpoint(DeepIndexEndpoint):
  """Returns total holders for a given token, as well as aggregated stats holder supply, holder trends, holder distribution and holder acquisition metrics."""

  async def token_holder_metrics(
    self,
    chain: EvmChain | None = None,
    *,
    token_address: str,
    validate: bool | None = None,
  ) -> TokenHolderMetrics:
    """Returns total holders for a given token, as well as aggregated stats holder supply, holder trends, holder distribution and holder acquisition metrics.

    Args:
      chain:
      token_address: The token address to get transaction for
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/token/holders/token-holder-stats)
    """
    request: Request = Request(tokenAddress=token_address)
    if chain is not None:
      request['chain'] = chain
    return await self.request(
      request,
      method='GET',
      path='/erc20/{tokenAddress}/holders',
      validate=validate,
      request_type=Request,
      response_type=TokenHolderMetrics,
    )
