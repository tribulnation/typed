# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain


class Result(TypedDict):
  balance: str
  """Balance."""
  balance_formatted: str
  """Balance formatted."""
  is_contract: bool
  """Is contract."""
  owner_address: str
  """Owner address."""
  owner_address_label: str | None
  """Owner address label."""
  entity: str | None
  """Entity."""
  entity_logo: str | None
  """Entity logo."""
  usd_value: str
  """USD value."""
  percentage_relative_to_total_supply: float
  """Percentage relative to total supply."""


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  token_address: str
  """The address of the token contract"""
  limit: NotRequired[int]
  """The desired page size of the result."""
  cursor: NotRequired[str]
  """The cursor returned in the previous response (used for getting the next page)."""
  order: NotRequired[Literal['ASC', 'DESC']]
  """The order of the result, in ascending (ASC) or descending (DESC)"""


class TopTokenHoldersResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  cursor: str | None
  """Cursor."""
  totalSupply: str
  """Total supply."""
  page: int
  """Page."""
  page_size: int
  """Page size."""
  result: list[Result]
  """Result."""


class TopTokenHolders(DeepIndexEndpoint):
  """Identify the major holders of an ERC20 token and understand their ownership percentages. Includes known entities, exchanges and wallet labels."""

  def top_token_holders_paged(
    self,
    chain: EvmChain | None = None,
    *,
    token_address: str,
    limit: int | None = None,
    order: Literal['ASC', 'DESC'] | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[Result, str]:
    """Identify the major holders of an ERC20 token and understand their ownership percentages. Includes known entities, exchanges and wallet labels.

    Paged variant of `top_token_holders`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      chain:
      token_address: The address of the token contract
      limit: The desired page size of the result.
      order: The order of the result, in ascending (ASC) or descending (DESC)
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/token/holders/token-holders)
    """

    async def next(cursor: str) -> tuple[list[Result], str | None]:
      response = await self.top_token_holders(
        chain,
        token_address=token_address,
        limit=limit,
        order=order,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('result')
      state = response.get('cursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def top_token_holders(
    self,
    chain: EvmChain | None = None,
    *,
    token_address: str,
    limit: int | None = None,
    cursor: str | None = None,
    order: Literal['ASC', 'DESC'] | None = None,
    validate: bool | None = None,
  ) -> TopTokenHoldersResponse:
    """Identify the major holders of an ERC20 token and understand their ownership percentages. Includes known entities, exchanges and wallet labels.

    Args:
      chain:
      token_address: The address of the token contract
      limit: The desired page size of the result.
      cursor: The cursor returned in the previous response (used for getting the next page).
      order: The order of the result, in ascending (ASC) or descending (DESC)
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/token/holders/token-holders)
    """
    request: Request = Request(token_address=token_address)
    if chain is not None:
      request['chain'] = chain
    if limit is not None:
      request['limit'] = limit
    if cursor is not None:
      request['cursor'] = cursor
    if order is not None:
      request['order'] = order
    return await self.request(
      request,
      method='GET',
      path='/erc20/{token_address}/owners',
      validate=validate,
      request_type=Request,
      response_type=TopTokenHoldersResponse,
    )
