# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, NotRequired, TypedDict
from typed_moralis.core import TimestampIso
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain


class Links(TypedDict):
  """Links."""


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  addresses: list[str]
  """The addresses to get metadata for"""


class TokenMetadataSchema(TypedDict):
  address: str
  """Address."""
  address_label: str | None
  """Address label."""
  name: str
  """Name."""
  symbol: str
  """Symbol."""
  decimals: str
  """Decimals."""
  logo: str | None
  """Logo."""
  logo_hash: str | None
  """Logo hash."""
  thumbnail: str | None
  """Thumbnail."""
  total_supply: str | None
  """Total supply."""
  total_supply_formatted: str | None
  """Total supply formatted."""
  fully_diluted_valuation: str
  """Fully diluted valuation."""
  block_number: str | None
  """Block number."""
  validated: int | None
  """Validated."""
  created_at: TimestampIso | None
  """Created at."""
  possible_spam: bool
  """Possible spam."""
  verified_contract: bool
  """Verified contract."""
  categories: list[dict[str, Any]]
  """Categories."""
  links: Links
  security_score: int | None
  """Security score."""
  description: str | None
  """Description."""
  circulating_supply: str | None
  """Circulating supply."""
  market_cap: str
  """Market cap."""
  implementations: list[dict[str, Any]]
  """Implementations."""


Response = list[TokenMetadataSchema]


class TokenMetadata(DeepIndexEndpoint):
  """Retrieve metadata (name, symbol, decimals, logo) for an ERC20 token contract, as well as off-chain metadata, total supply, categories, logos, spam status and more."""

  async def token_metadata(
    self,
    chain: EvmChain | None = None,
    *,
    addresses: list[str],
    validate: bool | None = None,
  ) -> Response:
    """Retrieve metadata (name, symbol, decimals, logo) for an ERC20 token contract, as well as off-chain metadata, total supply, categories, logos, spam status and more.

    Args:
      chain:
      addresses: The addresses to get metadata for
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/token/metadata/token-metadata)
    """
    request: Request = Request(addresses=addresses)
    if chain is not None:
      request['chain'] = chain
    return await self.request(
      request,
      method='GET',
      path='/erc20/metadata',
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
