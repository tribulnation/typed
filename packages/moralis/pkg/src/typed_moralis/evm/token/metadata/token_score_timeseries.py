# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_moralis.core import TimestampIso
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmOrSolanaChain


class Timesery(TypedDict):
  timestamp: NotRequired[TimestampIso]
  """Timestamp."""
  score: NotRequired[float]
  """Score."""


class Request(TypedDict):
  chain: NotRequired[EvmOrSolanaChain]
  tokenAddress: str
  """The token address to query"""
  timeframe: Literal['1d', '7d', '30d']
  """The timeframe to query"""


class TokenScoreTimeseriesResponse(TypedDict):
  chainId: NotRequired[str]
  """Chain ID."""
  tokenAddress: NotRequired[str]
  """Token address."""
  timeseries: NotRequired[list[Timesery]]
  """Timeseries."""


class TokenScoreTimeseries(DeepIndexEndpoint):
  """Retrieve historical score data for a specific token over time."""

  async def token_score_timeseries(
    self,
    chain: EvmOrSolanaChain | None = None,
    *,
    token_address: str,
    timeframe: Literal['1d', '7d', '30d'],
    validate: bool | None = None,
  ) -> TokenScoreTimeseriesResponse:
    """Retrieve historical score data for a specific token over time.

    Args:
      chain:
      token_address: The token address to query
      timeframe: The timeframe to query
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/token/metadata/token-score-timeseries)
    """
    request: Request = Request(tokenAddress=token_address, timeframe=timeframe)
    if chain is not None:
      request['chain'] = chain
    return await self.request(
      request,
      method='GET',
      path='/tokens/{tokenAddress}/score/historical',
      validate=validate,
      request_type=Request,
      response_type=TokenScoreTimeseriesResponse,
    )
