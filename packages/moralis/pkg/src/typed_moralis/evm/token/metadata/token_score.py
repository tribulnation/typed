# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_moralis.core import TimestampIso
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmOrSolanaChain, TokenScoreMetricWindow


class Supply(TypedDict):
  """Supply."""

  total: NotRequired[float]
  """Total."""
  top10Percent: NotRequired[float]
  """Top10 percent."""


class Metrics(TypedDict):
  usdPrice: NotRequired[float]
  """USD price."""
  liquidityUsd: NotRequired[float]
  """Liquidity USD."""
  volumeUsd: NotRequired[TokenScoreMetricWindow]
  transactions: NotRequired[TokenScoreMetricWindow]
  supply: NotRequired[Supply]


class Request(TypedDict):
  chain: NotRequired[EvmOrSolanaChain]
  tokenAddress: str
  """The token address to query"""


class TokenScoreResponse(TypedDict):
  tokenAddress: NotRequired[str]
  """Token address."""
  chainId: NotRequired[str]
  """Chain ID."""
  score: NotRequired[int]
  """Score."""
  updatedAt: NotRequired[TimestampIso]
  """Updated at."""
  metrics: NotRequired[Metrics | None]
  """Metrics."""


class TokenScore(DeepIndexEndpoint):
  """Retrieve a score for a specific token along with detailed metrics including price, volume, liquidity, transaction counts, and supply information."""

  async def token_score(
    self,
    chain: EvmOrSolanaChain | None = None,
    *,
    token_address: str,
    validate: bool | None = None,
  ) -> TokenScoreResponse:
    """Retrieve a score for a specific token along with detailed metrics including price, volume, liquidity, transaction counts, and supply information.

    Args:
      chain:
      token_address: The token address to query
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/token/metadata/token-score)
    """
    request: Request = Request(tokenAddress=token_address)
    if chain is not None:
      request['chain'] = chain
    return await self.request(
      request,
      method='GET',
      path='/tokens/{tokenAddress}/score',
      validate=validate,
      request_type=Request,
      response_type=TokenScoreResponse,
    )
