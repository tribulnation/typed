# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict, deprecated
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain, SnipersResponse


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  address: str
  """The pair address token-transactions are to be retrieved for."""
  blocksAfterCreation: NotRequired[int]
  """Number of blocks after the creation"""


class Snipers(DeepIndexEndpoint):
  """Identify sniper wallets that bought a token within a specified timeframe (`blocksAfterCreation`). Each wallet returned includes detailed information about how much was bought, sold as well as PnL stats and more."""

  @deprecated('Deprecated method')
  async def snipers(
    self,
    chain: EvmChain | None = None,
    *,
    address: str,
    blocks_after_creation: int | None = None,
    validate: bool | None = None,
  ) -> SnipersResponse:
    """Identify sniper wallets that bought a token within a specified timeframe (`blocksAfterCreation`). Each wallet returned includes detailed information about how much was bought, sold as well as PnL stats and more.

    Args:
      chain:
      address: The pair address token-transactions are to be retrieved for.
      blocks_after_creation: Number of blocks after the creation
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/token/signals/snipers)
    """
    request: Request = Request(address=address)
    if chain is not None:
      request['chain'] = chain
    if blocks_after_creation is not None:
      request['blocksAfterCreation'] = blocks_after_creation
    return await self.request(
      request,
      method='GET',
      path='/pairs/{address}/snipers',
      validate=validate,
      request_type=Request,
      response_type=SnipersResponse,
    )
