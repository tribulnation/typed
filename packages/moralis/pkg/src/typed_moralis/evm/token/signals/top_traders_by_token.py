# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain


class TopTradersByTokenResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  name: str
  """Name."""
  symbol: str
  """Symbol."""
  decimals: str
  """Decimals."""
  logo: str
  """Logo."""
  possible_spam: bool
  """Possible spam."""
  result: list[dict[str, Any]]
  """Result."""


class Request(TypedDict):
  address: str
  """The ERC20 token address."""
  days: NotRequired[str]
  """Timeframe in days for which profitability is calculated, Options include 'all', '7', '30' default is 'all'."""
  chain: NotRequired[EvmChain]


class TopTradersByToken(DeepIndexEndpoint):
  """List the most profitable wallets that have traded a specific ERC20 token."""

  async def top_traders_by_token(
    self,
    *,
    address: str,
    days: str | None = None,
    chain: EvmChain | None = None,
    validate: bool | None = None,
  ) -> TopTradersByTokenResponse:
    """List the most profitable wallets that have traded a specific ERC20 token.

    Args:
      address: The ERC20 token address.
      days: Timeframe in days for which profitability is calculated, Options include 'all', '7', '30' default is 'all'.
      chain:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/token/signals/top-traders)
    """
    request: Request = Request(address=address)
    if days is not None:
      request['days'] = days
    if chain is not None:
      request['chain'] = chain
    return await self.request(
      request,
      method='GET',
      path='/erc20/{address}/top-gainers',
      validate=validate,
      request_type=Request,
      response_type=TopTradersByTokenResponse,
    )
