# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict, deprecated
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmOrSolanaChain, TokenMoversRow


class Request(TypedDict):
  chain: NotRequired[EvmOrSolanaChain]
  min_market_cap: NotRequired[float]
  """The minimum market cap in usd of a token"""
  security_score: NotRequired[float]
  """The minimum security score of a token"""
  time_frame: NotRequired[Literal['1h', '1d', '1w', '1M']]
  """The time frame used for price percent change ordering in response"""


Response = list[TokenMoversRow]


class TopLosers(DeepIndexEndpoint):
  """List tokens with the largest price decreases over a period."""

  @deprecated('Deprecated method')
  async def top_losers(
    self,
    chain: EvmOrSolanaChain | None = None,
    *,
    min_market_cap: float | None = None,
    security_score: float | None = None,
    time_frame: Literal['1h', '1d', '1w', '1M'] | None = None,
    validate: bool | None = None,
  ) -> Response:
    """List tokens with the largest price decreases over a period.

    Args:
      chain:
      min_market_cap: The minimum market cap in usd of a token
      security_score: The minimum security score of a token
      time_frame: The time frame used for price percent change ordering in response
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/token/discovery/top-losers)
    """
    request: Request = Request()
    if chain is not None:
      request['chain'] = chain
    if min_market_cap is not None:
      request['min_market_cap'] = min_market_cap
    if security_score is not None:
      request['security_score'] = security_score
    if time_frame is not None:
      request['time_frame'] = time_frame
    return await self.request(
      request,
      method='GET',
      path='/discovery/tokens/top-losers',
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
