# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter


class Category(TypedDict):
  id: str
  """ID."""
  name: str
  """Name."""


class TokenCategoriesResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  categories: list[Category]
  """Categories."""


token_categories_adapter = TypeAdapter(TokenCategoriesResponse)


class TokenCategories(RpcEndpoint):
  async def token_categories(
    self,
    *,
    validate: bool | None = None,
  ) -> TokenCategoriesResponse:
    """Get ERC20 token categories.

    Args:
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/data-api/evm/token/discovery/token-categories)
    """
    response = await self.request('GET', '/tokens/categories')
    if self.should_validate(validate):
      return token_categories_adapter.validate_json(response.text)
    return response.json()
