# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import Category


class TokenCategoriesResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  categories: list[Category]
  """Categories."""


class TokenCategories(DeepIndexEndpoint):
  """Get ERC20 token categories."""

  async def token_categories(
    self,
    *,
    validate: bool | None = None,
  ) -> TokenCategoriesResponse:
    """Get ERC20 token categories.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/token/discovery/token-categories)
    """
    return await self.request(
      None,
      method='GET',
      path='/tokens/categories',
      validate=validate,
      response_type=TokenCategoriesResponse,
    )
