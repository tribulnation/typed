# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .discovery import Discovery
from .holders import Holders
from .metadata import Metadata
from .signals import Signals
from .swaps import Swaps
from .transfers import Transfers
from typed_moralis.core.rest import DeepIndexEndpoint


class Token(DeepIndexEndpoint):
  """Token search, prices, balances, transfers, holders, and swaps for ERC20 tokens across EVM chains.

  References:
    - [Upstream docs](https://docs.moralis.com/data-api/evm/token/overview)
  """

  @cached_property
  def discovery(self) -> Discovery:
    """ERC20 token discovery: category listings, top gainers and losers, and filtered token search.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/evm/token/overview)
    """
    return Discovery(client=self.client)

  @cached_property
  def holders(self) -> Holders:
    """ERC20 token holder counts, ranked holder lists, and historical holder trends.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/evm/token/overview)
    """
    return Holders(client=self.client)

  @cached_property
  def metadata(self) -> Metadata:
    """ERC20 token metadata, plus Moralis's own token score and its history.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/evm/token/overview)
    """
    return Metadata(client=self.client)

  @cached_property
  def signals(self) -> Signals:
    """Sniper and top-trader detection for an ERC20 token or trading pair.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/evm/token/overview)
    """
    return Signals(client=self.client)

  @cached_property
  def swaps(self) -> Swaps:
    """Swap transaction history and pair statistics for ERC20 tokens, wallets, and trading pairs.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/evm/token/overview)
    """
    return Swaps(client=self.client)

  @cached_property
  def transfers(self) -> Transfers:
    """ERC20 token transfer history by contract address.

    References:
      - [Upstream docs](https://docs.moralis.com/data-api/evm/token/overview)
    """
    return Transfers(client=self.client)
