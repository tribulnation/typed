# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_moralis.core import TimestampIso, TimestampSeconds
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain


class BlockByDateResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  block: int
  """Block."""
  date: TimestampIso
  """Date."""
  timestamp: TimestampSeconds
  """Timestamp."""
  block_timestamp: TimestampIso
  """Block timestamp."""
  hash: str
  """Hash."""
  parent_hash: str
  """Parent hash."""


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  date: NotRequired[str]
  """Unix date in milliseconds or a datestring (format in seconds or datestring accepted by momentjs)"""


class BlockByDate(DeepIndexEndpoint):
  """Find the closest block to a specific date on a blockchain."""

  async def block_by_date(
    self,
    chain: EvmChain | None = None,
    *,
    date: str | None = None,
    validate: bool | None = None,
  ) -> BlockByDateResponse:
    """Find the closest block to a specific date on a blockchain.

    Args:
      chain:
      date: Unix date in milliseconds or a datestring (format in seconds or datestring accepted by momentjs)
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/blockchain/block-by-date)
    """
    request: Request = Request()
    if chain is not None:
      request['chain'] = chain
    if date is not None:
      request['date'] = date
    return await self.request(
      request,
      method='GET',
      path='/dateToBlock',
      validate=validate,
      request_type=Request,
      response_type=BlockByDateResponse,
    )
