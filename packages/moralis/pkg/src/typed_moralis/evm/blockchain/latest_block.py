# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain


Response = str


class Request(TypedDict):
  chain: EvmChain


class LatestBlock(DeepIndexEndpoint):
  """Get the most recent block number for a specified blockchain."""

  async def latest_block(
    self, chain: EvmChain, *, validate: bool | None = None
  ) -> Response:
    """Get the most recent block number for a specified blockchain.

    Args:
      chain:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/blockchain/latest-block)
    """
    return await self.request(
      Request(chain=chain),
      method='GET',
      path='/latestBlockNumber/{chain}',
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
