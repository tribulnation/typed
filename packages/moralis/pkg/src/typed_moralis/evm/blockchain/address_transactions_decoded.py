# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_moralis.core import TimestampIso
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain, EvmInternalTransaction


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  from_block: NotRequired[int]
  """The minimum block number from which to get the transactions
* Provide the param 'from_block' or 'from_date'
* If 'from_date' and 'from_block' are provided, 'from_block' will be used."""
  to_block: NotRequired[int]
  """The maximum block number from which to get the transactions.
* Provide the param 'to_block' or 'to_date'
* If 'to_date' and 'to_block' are provided, 'to_block' will be used."""
  from_date: NotRequired[str]
  """The start date from which to get the transactions (format in seconds or datestring accepted by momentjs)
* Provide the param 'from_block' or 'from_date'
* If 'from_date' and 'from_block' are provided, 'from_block' will be used."""
  to_date: NotRequired[str]
  """Get the transactions up to this date (format in seconds or datestring accepted by momentjs)
* Provide the param 'to_block' or 'to_date'
* If 'to_date' and 'to_block' are provided, 'to_block' will be used."""
  address: str
  """The address of the wallet"""
  include: NotRequired[Literal['internal_transactions']]
  """If the result should contain the internal transactions."""
  cursor: NotRequired[str]
  """The cursor returned in the previous response (used for getting the next page)."""
  order: NotRequired[Literal['ASC', 'DESC']]
  """The order of the result, in ascending (ASC) or descending (DESC)"""
  limit: NotRequired[int]
  """The desired page size of the result."""


class Result(TypedDict):
  hash: str
  """Hash."""
  nonce: str
  """Nonce."""
  transaction_index: str
  """Transaction index."""
  from_address_entity: str | None
  """From address entity."""
  from_address_entity_logo: str | None
  """From address entity logo."""
  from_address: str
  """From address."""
  from_address_label: str | None
  """From address label."""
  to_address_entity: str | None
  """To address entity."""
  to_address_entity_logo: str | None
  """To address entity logo."""
  to_address: str
  """To address."""
  to_address_label: str | None
  """To address label."""
  value: str
  """Value."""
  gas: str
  """Gas."""
  gas_price: str
  """Gas price."""
  input: str
  """Input."""
  receipt_cumulative_gas_used: str
  """Receipt cumulative gas used."""
  receipt_gas_used: str
  """Receipt gas used."""
  receipt_contract_address: str | None
  """Receipt contract address."""
  receipt_root: str | None
  """Receipt root."""
  receipt_status: str
  """Receipt status."""
  block_timestamp: TimestampIso
  """Block timestamp."""
  block_number: str
  """Block number."""
  block_hash: str
  """Block hash."""
  transaction_fee: str
  """Transaction fee."""
  logs: list[dict[str, Any]]
  """Logs."""
  internal_transactions: list[EvmInternalTransaction]
  """Internal transactions."""


class AddressTransactionsDecodedResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  page: int
  """Page."""
  page_size: int
  """Page size."""
  cursor: str | None
  """Cursor."""
  result: list[Result]
  """Result."""


class AddressTransactionsDecoded(DeepIndexEndpoint):
  """Get ABI-decoded native transactions ordered by block number in descending order."""

  def address_transactions_decoded_paged(
    self,
    chain: EvmChain | None = None,
    *,
    from_block: int | None = None,
    to_block: int | None = None,
    from_date: str | None = None,
    to_date: str | None = None,
    address: str,
    include: Literal['internal_transactions'] | None = None,
    order: Literal['ASC', 'DESC'] | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[Result, str]:
    """Get ABI-decoded native transactions ordered by block number in descending order.

    Paged variant of `address_transactions_decoded`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      chain:
      from_block: The minimum block number from which to get the transactions
    * Provide the param 'from_block' or 'from_date'
    * If 'from_date' and 'from_block' are provided, 'from_block' will be used.
      to_block: The maximum block number from which to get the transactions.
    * Provide the param 'to_block' or 'to_date'
    * If 'to_date' and 'to_block' are provided, 'to_block' will be used.
      from_date: The start date from which to get the transactions (format in seconds or datestring accepted by momentjs)
    * Provide the param 'from_block' or 'from_date'
    * If 'from_date' and 'from_block' are provided, 'from_block' will be used.
      to_date: Get the transactions up to this date (format in seconds or datestring accepted by momentjs)
    * Provide the param 'to_block' or 'to_date'
    * If 'to_date' and 'to_block' are provided, 'to_block' will be used.
      address: The address of the wallet
      include: If the result should contain the internal transactions.
      order: The order of the result, in ascending (ASC) or descending (DESC)
      limit: The desired page size of the result.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/blockchain/address-transactions-decoded)
    """

    async def next(cursor: str) -> tuple[list[Result], str | None]:
      response = await self.address_transactions_decoded(
        chain,
        from_block=from_block,
        to_block=to_block,
        from_date=from_date,
        to_date=to_date,
        address=address,
        include=include,
        order=order,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('result')
      state = response.get('cursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def address_transactions_decoded(
    self,
    chain: EvmChain | None = None,
    *,
    from_block: int | None = None,
    to_block: int | None = None,
    from_date: str | None = None,
    to_date: str | None = None,
    address: str,
    include: Literal['internal_transactions'] | None = None,
    cursor: str | None = None,
    order: Literal['ASC', 'DESC'] | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> AddressTransactionsDecodedResponse:
    """Get ABI-decoded native transactions ordered by block number in descending order.

    Args:
      chain:
      from_block: The minimum block number from which to get the transactions
    * Provide the param 'from_block' or 'from_date'
    * If 'from_date' and 'from_block' are provided, 'from_block' will be used.
      to_block: The maximum block number from which to get the transactions.
    * Provide the param 'to_block' or 'to_date'
    * If 'to_date' and 'to_block' are provided, 'to_block' will be used.
      from_date: The start date from which to get the transactions (format in seconds or datestring accepted by momentjs)
    * Provide the param 'from_block' or 'from_date'
    * If 'from_date' and 'from_block' are provided, 'from_block' will be used.
      to_date: Get the transactions up to this date (format in seconds or datestring accepted by momentjs)
    * Provide the param 'to_block' or 'to_date'
    * If 'to_date' and 'to_block' are provided, 'to_block' will be used.
      address: The address of the wallet
      include: If the result should contain the internal transactions.
      cursor: The cursor returned in the previous response (used for getting the next page).
      order: The order of the result, in ascending (ASC) or descending (DESC)
      limit: The desired page size of the result.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/blockchain/address-transactions-decoded)
    """
    request: Request = Request(address=address)
    if chain is not None:
      request['chain'] = chain
    if from_block is not None:
      request['from_block'] = from_block
    if to_block is not None:
      request['to_block'] = to_block
    if from_date is not None:
      request['from_date'] = from_date
    if to_date is not None:
      request['to_date'] = to_date
    if include is not None:
      request['include'] = include
    if cursor is not None:
      request['cursor'] = cursor
    if order is not None:
      request['order'] = order
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/{address}/verbose',
      validate=validate,
      request_type=Request,
      response_type=AddressTransactionsDecodedResponse,
    )
