# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property

from typed_moralis.core import RpcEndpoint
from .detailed_positions import DetailedPositions
from .legacy import Legacy
from .wallet_positions import WalletPositions
from .wallet_protocols import WalletProtocols


class Defi(RpcEndpoint):
  """Moralis Defi endpoints."""

  @cached_property
  def detailed_positions(self) -> DetailedPositions:
    """DetailedPositions endpoints."""
    return DetailedPositions(client=self.client)

  @cached_property
  def legacy(self) -> Legacy:
    """Legacy endpoints."""
    return Legacy(client=self.client)

  @cached_property
  def wallet_positions(self) -> WalletPositions:
    """WalletPositions endpoints."""
    return WalletPositions(client=self.client)

  @cached_property
  def wallet_protocols(self) -> WalletProtocols:
    """WalletProtocols endpoints."""
    return WalletProtocols(client=self.client)
