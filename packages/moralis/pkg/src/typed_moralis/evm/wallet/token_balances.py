# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import WalletEvmChain


class TokenBalance(TypedDict):
  token_address: NotRequired[str | None]
  """Token address."""
  symbol: NotRequired[str | None]
  """Symbol."""
  name: NotRequired[str | None]
  """Name."""
  decimals: NotRequired[int | None]
  """Decimals."""
  balance: str
  """Raw token balance."""
  balance_formatted: NotRequired[str | None]
  """Balance formatted."""
  native_token: NotRequired[bool | None]
  """Native token."""
  possible_spam: NotRequired[bool | None]
  """Possible spam."""
  verified_contract: NotRequired[bool | None]
  """Verified contract."""
  usd_price: NotRequired[str | float | None]
  """USD price."""
  usd_value: NotRequired[float | None]
  """USD value."""


class Request(TypedDict):
  address: str
  """Wallet address whose balances should be fetched."""
  chain: NotRequired[WalletEvmChain]
  to_block: NotRequired[int]
  """Block number at which balances should be checked."""
  token_addresses: NotRequired[list[str]]
  """Optional token contract addresses to restrict the balance query."""
  exclude_spam: NotRequired[bool]
  """Exclude tokens Moralis marks as possible spam."""
  exclude_unverified_contracts: NotRequired[bool]
  """Exclude tokens whose contracts are not verified by Moralis."""
  cursor: NotRequired[str]
  """Pagination cursor returned by the previous response."""
  limit: NotRequired[int]
  """Maximum number of balances to return in this page."""
  exclude_native: NotRequired[bool]
  """Exclude the native gas-token balance from the response."""
  max_token_inactivity: NotRequired[float]
  """Exclude tokens inactive for more than this number of days."""
  min_pair_side_liquidity_usd: NotRequired[float]
  """Exclude tokens whose pair-side liquidity is below this USD threshold."""


class TokenBalancesResponse(TypedDict):
  page: NotRequired[int | str]
  """Page."""
  page_size: NotRequired[int | str]
  """Page size."""
  cursor: NotRequired[str | None]
  """Cursor."""
  block_number: NotRequired[str | int | None]
  """Block number."""
  result: list[TokenBalance]
  """Token balances for the wallet."""


class TokenBalances(DeepIndexEndpoint):
  """Fetch ERC20 and native token balances for an EVM wallet, including token metadata, spam and verification flags, optional USD values, pagination, and the snapshot block number."""

  def token_balances_paged(
    self,
    *,
    address: str,
    chain: WalletEvmChain | None = None,
    to_block: int | None = None,
    token_addresses: list[str] | None = None,
    exclude_spam: bool | None = None,
    exclude_unverified_contracts: bool | None = None,
    limit: int | None = None,
    exclude_native: bool | None = None,
    max_token_inactivity: float | None = None,
    min_pair_side_liquidity_usd: float | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[TokenBalance, str]:
    """Fetch ERC20 and native token balances for an EVM wallet, including token metadata, spam and verification flags, optional USD values, pagination, and the snapshot block number.

    Paged variant of `token_balances`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      address: Wallet address whose balances should be fetched.
      chain:
      to_block: Block number at which balances should be checked.
      token_addresses: Optional token contract addresses to restrict the balance query.
      exclude_spam: Exclude tokens Moralis marks as possible spam.
      exclude_unverified_contracts: Exclude tokens whose contracts are not verified by Moralis.
      limit: Maximum number of balances to return in this page.
      exclude_native: Exclude the native gas-token balance from the response.
      max_token_inactivity: Exclude tokens inactive for more than this number of days.
      min_pair_side_liquidity_usd: Exclude tokens whose pair-side liquidity is below this USD threshold.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/wallet/token-balances)
    """

    async def next(cursor: str) -> tuple[list[TokenBalance], str | None]:
      response = await self.token_balances(
        address=address,
        chain=chain,
        to_block=to_block,
        token_addresses=token_addresses,
        exclude_spam=exclude_spam,
        exclude_unverified_contracts=exclude_unverified_contracts,
        limit=limit,
        exclude_native=exclude_native,
        max_token_inactivity=max_token_inactivity,
        min_pair_side_liquidity_usd=min_pair_side_liquidity_usd,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('result')
      state = response.get('cursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def token_balances(
    self,
    *,
    address: str,
    chain: WalletEvmChain | None = None,
    to_block: int | None = None,
    token_addresses: list[str] | None = None,
    exclude_spam: bool | None = None,
    exclude_unverified_contracts: bool | None = None,
    cursor: str | None = None,
    limit: int | None = None,
    exclude_native: bool | None = None,
    max_token_inactivity: float | None = None,
    min_pair_side_liquidity_usd: float | None = None,
    validate: bool | None = None,
  ) -> TokenBalancesResponse:
    """Fetch ERC20 and native token balances for an EVM wallet, including token metadata, spam and verification flags, optional USD values, pagination, and the snapshot block number.

    Args:
      address: Wallet address whose balances should be fetched.
      chain:
      to_block: Block number at which balances should be checked.
      token_addresses: Optional token contract addresses to restrict the balance query.
      exclude_spam: Exclude tokens Moralis marks as possible spam.
      exclude_unverified_contracts: Exclude tokens whose contracts are not verified by Moralis.
      cursor: Pagination cursor returned by the previous response.
      limit: Maximum number of balances to return in this page.
      exclude_native: Exclude the native gas-token balance from the response.
      max_token_inactivity: Exclude tokens inactive for more than this number of days.
      min_pair_side_liquidity_usd: Exclude tokens whose pair-side liquidity is below this USD threshold.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/wallet/token-balances)
    """
    request: Request = Request(address=address)
    if chain is not None:
      request['chain'] = chain
    if to_block is not None:
      request['to_block'] = to_block
    if token_addresses is not None:
      request['token_addresses'] = token_addresses
    if exclude_spam is not None:
      request['exclude_spam'] = exclude_spam
    if exclude_unverified_contracts is not None:
      request['exclude_unverified_contracts'] = exclude_unverified_contracts
    if cursor is not None:
      request['cursor'] = cursor
    if limit is not None:
      request['limit'] = limit
    if exclude_native is not None:
      request['exclude_native'] = exclude_native
    if max_token_inactivity is not None:
      request['max_token_inactivity'] = max_token_inactivity
    if min_pair_side_liquidity_usd is not None:
      request['min_pair_side_liquidity_usd'] = min_pair_side_liquidity_usd
    return await self.request(
      request,
      method='GET',
      path='/wallets/{address}/tokens',
      validate=validate,
      request_type=Request,
      response_type=TokenBalancesResponse,
    )
