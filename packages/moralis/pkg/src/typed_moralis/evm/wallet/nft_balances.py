# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain, NftListPrice


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  address: str
  """The address of the wallet"""
  format: NotRequired[Literal['decimal', 'hex']]
  """The format of the token ID"""
  limit: NotRequired[int]
  """The desired page size of the result."""
  exclude_spam: NotRequired[bool]
  """Should spam NFTs be excluded from the result?"""
  token_addresses: NotRequired[list[str]]
  """The addresses to get balances for (optional)"""
  cursor: NotRequired[str]
  """The cursor returned in the previous response (used for getting the next page)."""
  normalizeMetadata: NotRequired[bool]
  """Should normalized metadata be returned?"""
  media_items: NotRequired[bool]
  """Should preview media data be returned?"""
  include_prices: NotRequired[bool]
  """Should NFT last sale prices be included in the result?"""


class Result(TypedDict):
  amount: str
  """Amount."""
  token_id: str
  """Token ID."""
  token_address: str
  """Token address."""
  contract_type: str
  """Contract type."""
  owner_of: str
  """Owner of."""
  last_metadata_sync: str
  """Last metadata sync."""
  last_token_uri_sync: str
  """Last token uri sync."""
  metadata: str | None
  """Metadata."""
  block_number: str
  """Block number."""
  block_number_minted: str | None
  """Block number minted."""
  name: str
  """Name."""
  symbol: str
  """Symbol."""
  token_hash: str
  """Token hash."""
  token_uri: str
  """Token uri."""
  minter_address: str | None
  """Minter address."""
  rarity_rank: int | None
  """Rarity rank."""
  rarity_percentage: float | None
  """Rarity percentage."""
  rarity_label: str | None
  """Rarity label."""
  verified_collection: bool
  """Verified collection."""
  possible_spam: bool
  """Possible spam."""
  collection_logo: str
  """Collection logo."""
  collection_banner_image: str
  """Collection banner image."""
  collection_category: str
  """Collection category."""
  project_url: str
  """Project URL."""
  wiki_url: str
  """Wiki URL."""
  discord_url: str
  """Discord URL."""
  telegram_url: str
  """Telegram URL."""
  twitter_username: str | None
  """Twitter username."""
  instagram_username: str
  """Instagram username."""
  list_price: NftListPrice
  floor_price: str | None
  """Floor price."""
  floor_price_usd: str | None
  """Floor price USD."""
  floor_price_currency: str | None
  """Floor price currency."""


class NftBalancesResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  status: str
  """Status."""
  page: int
  """Page."""
  page_size: int
  """Page size."""
  cursor: str | None
  """Cursor."""
  result: list[Result]
  """Result."""


class NftBalances(DeepIndexEndpoint):
  """Fetch all NFTs held by a specified wallet address. Use `token_addresses` to filter by one or many specific contract(s). Each NFT returned includes on-chain metadata as well as off-chain metadata, floor prices, rarity and more where available."""

  def nft_balances_paged(
    self,
    chain: EvmChain | None = None,
    *,
    address: str,
    format: Literal['decimal', 'hex'] | None = None,
    limit: int | None = None,
    exclude_spam: bool | None = None,
    token_addresses: list[str] | None = None,
    normalize_metadata: bool | None = None,
    media_items: bool | None = None,
    include_prices: bool | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[Result, str]:
    """Fetch all NFTs held by a specified wallet address. Use `token_addresses` to filter by one or many specific contract(s). Each NFT returned includes on-chain metadata as well as off-chain metadata, floor prices, rarity and more where available.

    Paged variant of `nft_balances`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      chain:
      address: The address of the wallet
      format: The format of the token ID
      limit: The desired page size of the result.
      exclude_spam: Should spam NFTs be excluded from the result?
      token_addresses: The addresses to get balances for (optional)
      normalize_metadata: Should normalized metadata be returned?
      media_items: Should preview media data be returned?
      include_prices: Should NFT last sale prices be included in the result?
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/wallet/nft-balances)
    """

    async def next(cursor: str) -> tuple[list[Result], str | None]:
      response = await self.nft_balances(
        chain,
        address=address,
        format=format,
        limit=limit,
        exclude_spam=exclude_spam,
        token_addresses=token_addresses,
        normalize_metadata=normalize_metadata,
        media_items=media_items,
        include_prices=include_prices,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('result')
      state = response.get('cursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def nft_balances(
    self,
    chain: EvmChain | None = None,
    *,
    address: str,
    format: Literal['decimal', 'hex'] | None = None,
    limit: int | None = None,
    exclude_spam: bool | None = None,
    token_addresses: list[str] | None = None,
    cursor: str | None = None,
    normalize_metadata: bool | None = None,
    media_items: bool | None = None,
    include_prices: bool | None = None,
    validate: bool | None = None,
  ) -> NftBalancesResponse:
    """Fetch all NFTs held by a specified wallet address. Use `token_addresses` to filter by one or many specific contract(s). Each NFT returned includes on-chain metadata as well as off-chain metadata, floor prices, rarity and more where available.

    Args:
      chain:
      address: The address of the wallet
      format: The format of the token ID
      limit: The desired page size of the result.
      exclude_spam: Should spam NFTs be excluded from the result?
      token_addresses: The addresses to get balances for (optional)
      cursor: The cursor returned in the previous response (used for getting the next page).
      normalize_metadata: Should normalized metadata be returned?
      media_items: Should preview media data be returned?
      include_prices: Should NFT last sale prices be included in the result?
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/wallet/nft-balances)
    """
    request: Request = Request(address=address)
    if chain is not None:
      request['chain'] = chain
    if format is not None:
      request['format'] = format
    if limit is not None:
      request['limit'] = limit
    if exclude_spam is not None:
      request['exclude_spam'] = exclude_spam
    if token_addresses is not None:
      request['token_addresses'] = token_addresses
    if cursor is not None:
      request['cursor'] = cursor
    if normalize_metadata is not None:
      request['normalizeMetadata'] = normalize_metadata
    if media_items is not None:
      request['media_items'] = media_items
    if include_prices is not None:
      request['include_prices'] = include_prices
    return await self.request(
      request,
      method='GET',
      path='/{address}/nft',
      validate=validate,
      request_type=Request,
      response_type=NftBalancesResponse,
    )
