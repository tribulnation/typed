# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain


class NativeBalanceResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  balance: str
  """Balance."""


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  address: str
  """The address from which the native balance will be checked"""
  to_block: NotRequired[float]
  """The block number up to which the balances will be checked."""


class NativeBalance(DeepIndexEndpoint):
  """Check the native token balance (e.g. ETH) for a specific wallet."""

  async def native_balance(
    self,
    chain: EvmChain | None = None,
    *,
    address: str,
    to_block: float | None = None,
    validate: bool | None = None,
  ) -> NativeBalanceResponse:
    """Check the native token balance (e.g. ETH) for a specific wallet.

    Args:
      chain:
      address: The address from which the native balance will be checked
      to_block: The block number up to which the balances will be checked.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/wallet/native-balance)
    """
    request: Request = Request(address=address)
    if chain is not None:
      request['chain'] = chain
    if to_block is not None:
      request['to_block'] = to_block
    return await self.request(
      request,
      method='GET',
      path='/{address}/balance',
      validate=validate,
      request_type=Request,
      response_type=NativeBalanceResponse,
    )
