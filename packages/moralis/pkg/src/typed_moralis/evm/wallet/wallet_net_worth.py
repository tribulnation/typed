# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain


class Chain(TypedDict):
  chain: NotRequired[str]
  """Chain."""
  native_balance: NotRequired[str]
  """Native balance."""
  native_balance_formatted: NotRequired[str]
  """Native balance formatted."""
  native_balance_usd: NotRequired[str]
  """Native balance USD."""
  token_balance_usd: NotRequired[str]
  """Token balance USD."""
  networth_usd: NotRequired[str]
  """Networth USD."""


class Request(TypedDict):
  chains: NotRequired[list[EvmChain]]
  """The chains to query"""
  address: str
  """The wallet address"""
  exclude_spam: NotRequired[bool]
  """Exclude spam tokens from the result"""
  exclude_unverified_contracts: NotRequired[bool]
  """Exclude unverified contracts from the result"""
  max_token_inactivity: NotRequired[float]
  """Exclude tokens inactive for more than the given amount of days"""
  min_pair_side_liquidity_usd: NotRequired[float]
  """Exclude tokens with liquidity less than the specified amount in USD. This parameter refers to the liquidity on a single side of the pair."""


class WalletNetWorthResponse(TypedDict):
  total_networth_usd: NotRequired[str]
  """Total networth USD."""
  chains: NotRequired[list[Chain]]
  """Chains."""


class WalletNetWorth(DeepIndexEndpoint):
  """Calculate the total net worth of a wallet in USD, with options to exclude spam tokens for accuracy. Options to query cross-chain using the `chains` parameter, as well as additional options to exclude spam tokens, low-liquidity tokens and inactive tokens."""

  async def wallet_net_worth(
    self,
    chains: list[EvmChain] | None = None,
    *,
    address: str,
    exclude_spam: bool | None = None,
    exclude_unverified_contracts: bool | None = None,
    max_token_inactivity: float | None = None,
    min_pair_side_liquidity_usd: float | None = None,
    validate: bool | None = None,
  ) -> WalletNetWorthResponse:
    """Calculate the total net worth of a wallet in USD, with options to exclude spam tokens for accuracy. Options to query cross-chain using the `chains` parameter, as well as additional options to exclude spam tokens, low-liquidity tokens and inactive tokens.

    Args:
      chains: The chains to query
      address: The wallet address
      exclude_spam: Exclude spam tokens from the result
      exclude_unverified_contracts: Exclude unverified contracts from the result
      max_token_inactivity: Exclude tokens inactive for more than the given amount of days
      min_pair_side_liquidity_usd: Exclude tokens with liquidity less than the specified amount in USD. This parameter refers to the liquidity on a single side of the pair.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/wallet/net-worth)
    """
    request: Request = Request(address=address)
    if chains is not None:
      request['chains'] = chains
    if exclude_spam is not None:
      request['exclude_spam'] = exclude_spam
    if exclude_unverified_contracts is not None:
      request['exclude_unverified_contracts'] = exclude_unverified_contracts
    if max_token_inactivity is not None:
      request['max_token_inactivity'] = max_token_inactivity
    if min_pair_side_liquidity_usd is not None:
      request['min_pair_side_liquidity_usd'] = min_pair_side_liquidity_usd
    return await self.request(
      request,
      method='GET',
      path='/wallets/{address}/net-worth',
      validate=validate,
      request_type=Request,
      response_type=WalletNetWorthResponse,
    )
