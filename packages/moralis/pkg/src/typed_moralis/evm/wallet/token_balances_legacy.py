# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain


class TokenBalancesLegacySchema(TypedDict):
  token_address: str
  """The address of the token contract"""
  name: str
  """The name of the token contract"""
  symbol: str
  """The symbol of the NFT contract"""
  logo: NotRequired[str]
  """The logo of the token"""
  thumbnail: NotRequired[str]
  """The thumbnail of the logo"""
  decimals: int
  """The number of decimals on the token"""
  balance: str
  """Timestamp of when the contract was last synced with the node"""
  possible_spam: bool
  """Indicates if a contract is possibly a spam contract"""
  verified_contract: NotRequired[bool]
  """Indicates if a contract is verified"""


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  address: str
  """The address from which token balances will be checked"""
  to_block: NotRequired[float]
  """The block number up to which the balances will be checked."""
  token_addresses: NotRequired[list[str]]
  """The addresses to get balances for (optional)"""
  exclude_spam: NotRequired[bool]
  """Exclude spam tokens from the result"""


Response = list[TokenBalancesLegacySchema]


class TokenBalancesLegacy(DeepIndexEndpoint):
  """Get an unpaginated list of token balances, without token prices, for a specific wallet address. Maximum of 2,000 tokens. Each token returned includes on-chain metadata, as well as off-chain metadata, logos, spam status and more."""

  async def token_balances_legacy(
    self,
    chain: EvmChain | None = None,
    *,
    address: str,
    to_block: float | None = None,
    token_addresses: list[str] | None = None,
    exclude_spam: bool | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get an unpaginated list of token balances, without token prices, for a specific wallet address. Maximum of 2,000 tokens. Each token returned includes on-chain metadata, as well as off-chain metadata, logos, spam status and more.

    Args:
      chain:
      address: The address from which token balances will be checked
      to_block: The block number up to which the balances will be checked.
      token_addresses: The addresses to get balances for (optional)
      exclude_spam: Exclude spam tokens from the result
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/wallet/token-balances-legacy)
    """
    request: Request = Request(address=address)
    if chain is not None:
      request['chain'] = chain
    if to_block is not None:
      request['to_block'] = to_block
    if token_addresses is not None:
      request['token_addresses'] = token_addresses
    if exclude_spam is not None:
      request['exclude_spam'] = exclude_spam
    return await self.request(
      request,
      method='GET',
      path='/{address}/erc20',
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
