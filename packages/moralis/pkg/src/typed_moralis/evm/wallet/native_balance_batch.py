# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_moralis.core import TimestampSeconds
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import EvmChain


class WalletBalance(TypedDict):
  address: str
  """Address."""
  balance: str
  """Balance."""
  balance_formatted: str
  """Balance formatted."""


class NativeBalanceBatchSchema(TypedDict):
  block_number: str
  """Block number."""
  block_timestamp: TimestampSeconds
  """Block timestamp."""
  chain: str
  """Chain."""
  chain_id: str
  """Chain ID."""
  total_balance: str
  """Total balance."""
  total_balance_formatted: str
  """Total balance formatted."""
  wallet_balances: list[WalletBalance]
  """Wallet balances."""


class Request(TypedDict):
  chain: NotRequired[EvmChain]
  to_block: NotRequired[float]
  """The block number on which the balances should be checked"""
  wallet_addresses: list[str]
  """The addresses to get metadata for"""


Response = list[NativeBalanceBatchSchema]


class NativeBalanceBatch(DeepIndexEndpoint):
  """Retrieve native token balances (e.g. ETH) for one or many wallet addresses in single request."""

  async def native_balance_batch(
    self,
    chain: EvmChain | None = None,
    *,
    to_block: float | None = None,
    wallet_addresses: list[str],
    validate: bool | None = None,
  ) -> Response:
    """Retrieve native token balances (e.g. ETH) for one or many wallet addresses in single request.

    Args:
      chain:
      to_block: The block number on which the balances should be checked
      wallet_addresses: The addresses to get metadata for
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/wallet/native-balances-batch)
    """
    request: Request = Request(wallet_addresses=wallet_addresses)
    if chain is not None:
      request['chain'] = chain
    if to_block is not None:
      request['to_block'] = to_block
    return await self.request(
      request,
      method='GET',
      path='/wallets/balances',
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
