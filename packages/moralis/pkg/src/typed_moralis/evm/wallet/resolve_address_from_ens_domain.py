# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter


class ResolveAddressFromEnsDomainResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  address: str
  """Address."""


resolve_address_from_ens_domain_adapter = TypeAdapter(
  ResolveAddressFromEnsDomainResponse
)


class ResolveAddressFromEnsDomain(RpcEndpoint):
  async def resolve_address_from_ens_domain(
    self,
    domain: str,
    *,
    validate: bool | None = None,
  ) -> ResolveAddressFromEnsDomainResponse:
    """Resolve an ENS domain to its associated Ethereum address.

    Args:
      domain: The domain to be resolved
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/data-api/evm/wallet/resolve-address)
    """
    response = await self.request('GET', f'/resolve/ens/{domain}')
    if self.should_validate(validate):
      return resolve_address_from_ens_domain_adapter.validate_json(response.text)
    return response.json()
