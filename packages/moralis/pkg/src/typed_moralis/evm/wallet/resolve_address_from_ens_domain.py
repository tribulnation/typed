# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint


class Request(TypedDict):
  domain: str
  """The domain to be resolved"""


class ResolveAddressFromEnsDomainResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  address: str
  """Address."""


class ResolveAddressFromEnsDomain(DeepIndexEndpoint):
  """Resolve an ENS domain to its associated Ethereum address."""

  async def resolve_address_from_ens_domain(
    self,
    domain: str,
    *,
    validate: bool | None = None,
  ) -> ResolveAddressFromEnsDomainResponse:
    """Resolve an ENS domain to its associated Ethereum address.

    Args:
      domain: The domain to be resolved
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/wallet/resolve-address)
    """
    return await self.request(
      Request(domain=domain),
      method='GET',
      path='/resolve/ens/{domain}',
      validate=validate,
      request_type=Request,
      response_type=ResolveAddressFromEnsDomainResponse,
    )
