# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_moralis.core import TimestampIso
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import WalletEvmChain


class TokenTransfer(TypedDict):
  transaction_hash: str
  """Transaction hash containing this transfer."""
  address: str
  """ERC20 contract address."""
  block_number: str | int
  """Block number."""
  block_hash: NotRequired[str | None]
  """Block hash."""
  block_timestamp: NotRequired[TimestampIso | None]
  """Block timestamp."""
  from_address: str | None
  """From address."""
  to_address: str | None
  """To address."""
  value: str | float
  """Value."""
  value_formatted: NotRequired[str | None]
  """Value formatted."""
  token_decimals: NotRequired[str | int | None]
  """Token decimals."""
  token_symbol: NotRequired[str | None]
  """Token symbol."""
  token_name: NotRequired[str | None]
  """Token name."""
  transaction_index: NotRequired[str | int | None]
  """Transaction index."""
  log_index: NotRequired[str | int | None]
  """Log index."""
  possible_spam: NotRequired[bool | str | None]
  """Possible spam."""
  verified_contract: NotRequired[bool | str | None]
  """Verified contract."""


class Request(TypedDict):
  address: str
  """Wallet address whose ERC20 transfers should be fetched."""
  chain: NotRequired[WalletEvmChain]
  from_block: NotRequired[int]
  """Minimum block number to include."""
  to_block: NotRequired[int]
  """Maximum block number to include."""
  from_date: NotRequired[str]
  """Start date for transfer collection when `from_block` is not provided."""
  to_date: NotRequired[str]
  """End date for transfer collection when `to_block` is not provided."""
  cursor: NotRequired[str]
  """Pagination cursor returned by the previous response."""
  order: NotRequired[Literal['ASC', 'DESC']]
  """Sort order for returned transfers."""
  limit: NotRequired[int]
  """Maximum number of transfers to return in this page."""


class TokenTransfersResponse(TypedDict):
  page: NotRequired[int | str]
  """Page."""
  page_size: NotRequired[int | str]
  """Page size."""
  cursor: NotRequired[str | None]
  """Cursor."""
  result: list[TokenTransfer]
  """ERC20 transfers for the wallet."""


class TokenTransfers(DeepIndexEndpoint):
  """Get ERC20 token transfers for an EVM wallet, sorted newest first by default, including token metadata, block data, transaction hashes, spam flags, verification flags, and pagination metadata."""

  def token_transfers_paged(
    self,
    *,
    address: str,
    chain: WalletEvmChain | None = None,
    from_block: int | None = None,
    to_block: int | None = None,
    from_date: str | None = None,
    to_date: str | None = None,
    order: Literal['ASC', 'DESC'] | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[TokenTransfer, str]:
    """Get ERC20 token transfers for an EVM wallet, sorted newest first by default, including token metadata, block data, transaction hashes, spam flags, verification flags, and pagination metadata.

    Paged variant of `token_transfers`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      address: Wallet address whose ERC20 transfers should be fetched.
      chain:
      from_block: Minimum block number to include.
      to_block: Maximum block number to include.
      from_date: Start date for transfer collection when `from_block` is not provided.
      to_date: End date for transfer collection when `to_block` is not provided.
      order: Sort order for returned transfers.
      limit: Maximum number of transfers to return in this page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/wallet/token-transfers)
    """

    async def next(cursor: str) -> tuple[list[TokenTransfer], str | None]:
      response = await self.token_transfers(
        address=address,
        chain=chain,
        from_block=from_block,
        to_block=to_block,
        from_date=from_date,
        to_date=to_date,
        order=order,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('result')
      state = response.get('cursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def token_transfers(
    self,
    *,
    address: str,
    chain: WalletEvmChain | None = None,
    from_block: int | None = None,
    to_block: int | None = None,
    from_date: str | None = None,
    to_date: str | None = None,
    cursor: str | None = None,
    order: Literal['ASC', 'DESC'] | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> TokenTransfersResponse:
    """Get ERC20 token transfers for an EVM wallet, sorted newest first by default, including token metadata, block data, transaction hashes, spam flags, verification flags, and pagination metadata.

    Args:
      address: Wallet address whose ERC20 transfers should be fetched.
      chain:
      from_block: Minimum block number to include.
      to_block: Maximum block number to include.
      from_date: Start date for transfer collection when `from_block` is not provided.
      to_date: End date for transfer collection when `to_block` is not provided.
      cursor: Pagination cursor returned by the previous response.
      order: Sort order for returned transfers.
      limit: Maximum number of transfers to return in this page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/wallet/token-transfers)
    """
    request: Request = Request(address=address)
    if chain is not None:
      request['chain'] = chain
    if from_block is not None:
      request['from_block'] = from_block
    if to_block is not None:
      request['to_block'] = to_block
    if from_date is not None:
      request['from_date'] = from_date
    if to_date is not None:
      request['to_date'] = to_date
    if cursor is not None:
      request['cursor'] = cursor
    if order is not None:
      request['order'] = order
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/{address}/erc20/transfers',
      validate=validate,
      request_type=Request,
      response_type=TokenTransfersResponse,
    )
