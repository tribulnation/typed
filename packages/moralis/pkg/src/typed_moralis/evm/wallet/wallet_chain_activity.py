# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import ChainActivityTxRef, EvmChain


class ActiveChain(TypedDict):
  chain: str
  """Chain."""
  chain_id: str
  """Chain ID."""
  first_transaction: ChainActivityTxRef
  last_transaction: ChainActivityTxRef


class Request(TypedDict):
  address: str
  """Wallet address"""
  chains: NotRequired[list[EvmChain]]
  """The chains to query"""


class WalletChainActivityResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  address: str
  """Address."""
  active_chains: list[ActiveChain]
  """Active chains."""


class WalletChainActivity(DeepIndexEndpoint):
  """List the blockchain networks a wallet is active on, including their first and last seen timestamps. Options to query cross-chain using the `chains` parameter."""

  async def wallet_chain_activity(
    self,
    address: str,
    *,
    chains: list[EvmChain] | None = None,
    validate: bool | None = None,
  ) -> WalletChainActivityResponse:
    """List the blockchain networks a wallet is active on, including their first and last seen timestamps. Options to query cross-chain using the `chains` parameter.

    Args:
      address: Wallet address
      chains: The chains to query
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/wallet/chain-activity)
    """
    request: Request = Request(address=address)
    if chains is not None:
      request['chains'] = chains
    return await self.request(
      request,
      method='GET',
      path='/wallets/{address}/chains',
      validate=validate,
      request_type=Request,
      response_type=WalletChainActivityResponse,
    )
