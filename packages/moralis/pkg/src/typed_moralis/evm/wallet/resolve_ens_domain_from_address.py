# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.core import RpcEndpoint
from pydantic import TypeAdapter


class ResolveEnsDomainFromAddressResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  name: str
  """Name."""


resolve_ens_domain_from_address_adapter = TypeAdapter(
  ResolveEnsDomainFromAddressResponse
)


class ResolveEnsDomainFromAddress(RpcEndpoint):
  async def resolve_ens_domain_from_address(
    self,
    address: str,
    *,
    validate: bool | None = None,
  ) -> ResolveEnsDomainFromAddressResponse:
    """Convert an Ethereum address to its associated ENS domain, if registered.

    Args:
      address: The address to be resolved
      validate: Per-call response validation override.

    References:
      - [Moralis API docs](https://docs.moralis.com/data-api/evm/wallet/ens-lookup)
    """
    response = await self.request('GET', f'/resolve/{address}/reverse')
    if self.should_validate(validate):
      return resolve_ens_domain_from_address_adapter.validate_json(response.text)
    return response.json()
