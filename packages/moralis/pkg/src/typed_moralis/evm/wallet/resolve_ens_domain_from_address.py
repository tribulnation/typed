# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_moralis.core.rest import DeepIndexEndpoint


class Request(TypedDict):
  address: str
  """The address to be resolved"""


class ResolveEnsDomainFromAddressResponse(TypedDict):
  """Schema inferred from a live Moralis response captured by spec collection."""

  name: str
  """Name."""


class ResolveEnsDomainFromAddress(DeepIndexEndpoint):
  """Convert an Ethereum address to its associated ENS domain, if registered."""

  async def resolve_ens_domain_from_address(
    self,
    address: str,
    *,
    validate: bool | None = None,
  ) -> ResolveEnsDomainFromAddressResponse:
    """Convert an Ethereum address to its associated ENS domain, if registered.

    Args:
      address: The address to be resolved
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/wallet/ens-lookup)
    """
    return await self.request(
      Request(address=address),
      method='GET',
      path='/resolve/{address}/reverse',
      validate=validate,
      request_type=Request,
      response_type=ResolveEnsDomainFromAddressResponse,
    )
