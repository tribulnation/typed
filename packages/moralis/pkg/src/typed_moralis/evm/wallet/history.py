# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, TypedDict
from typed_moralis.core import TimestampIso
from typed_moralis.core.rest import DeepIndexEndpoint
from typed_moralis.schemas import WalletEvmChain


class Erc20Transfer(TypedDict):
  address: NotRequired[str]
  """ERC20 contract address."""
  from_address: NotRequired[str | None]
  """From address."""
  to_address: NotRequired[str | None]
  """To address."""
  value: NotRequired[str | float]
  """Value."""
  value_formatted: NotRequired[str | None]
  """Value formatted."""
  token_decimals: NotRequired[str | int | None]
  """Token decimals."""
  token_symbol: NotRequired[str | None]
  """Token symbol."""
  possible_spam: NotRequired[bool | str | None]
  """Possible spam."""
  verified_contract: NotRequired[bool | str | None]
  """Verified contract."""


class NativeTransfer(TypedDict):
  from_address: NotRequired[str | None]
  """From address."""
  to_address: NotRequired[str | None]
  """To address."""
  value: NotRequired[str | float]
  """Value."""
  value_formatted: NotRequired[str | None]
  """Value formatted."""
  direction: NotRequired[str | None]
  """Direction."""
  token_symbol: NotRequired[str | None]
  """Token symbol."""


class Request(TypedDict):
  address: str
  """Wallet address whose decoded history should be fetched."""
  chain: NotRequired[WalletEvmChain]
  from_block: NotRequired[int]
  """Minimum block number to include. Moralis uses this over `from_date` when both are provided."""
  to_block: NotRequired[int]
  """Maximum block number to include. Moralis uses this over `to_date` when both are provided."""
  from_date: NotRequired[str]
  """Start date for history collection when `from_block` is not provided. Moralis accepts seconds or a date string."""
  to_date: NotRequired[str]
  """End date for history collection when `to_block` is not provided. Moralis accepts seconds or a date string."""
  cursor: NotRequired[str]
  """Pagination cursor returned by the previous response."""
  order: NotRequired[Literal['ASC', 'DESC']]
  """Sort order for returned activity."""
  limit: NotRequired[int]
  """Maximum number of history rows to return in this page."""


class WalletHistoryTransaction(TypedDict):
  hash: str
  """Transaction hash."""
  receipt_status: NotRequired[str | int | None]
  """Receipt status."""
  from_address: NotRequired[str | None]
  """From address."""
  to_address: NotRequired[str | None]
  """To address."""
  value: NotRequired[str | float | None]
  """Value."""
  transaction_fee: NotRequired[str | None]
  """Transaction fee."""
  block_number: str | int
  """Block number."""
  block_timestamp: TimestampIso
  """Block timestamp as an ISO-8601 string."""
  category: NotRequired[str | None]
  """Category."""
  native_transfers: NotRequired[list[NativeTransfer]]
  """Decoded native wallet-level transfer movements."""
  erc20_transfers: NotRequired[list[Erc20Transfer]]
  """ERC20 transfers associated with the activity row."""


class WalletHistoryResponse(TypedDict):
  page: NotRequired[int | str]
  """Page."""
  page_size: NotRequired[int | str]
  """Page size."""
  cursor: NotRequired[str | None]
  """Cursor."""
  result: list[WalletHistoryTransaction]
  """Decoded wallet activity rows."""


class History(DeepIndexEndpoint):
  """Get decoded wallet activity for an EVM address, including native transfers, ERC20 transfers, NFT transfers, fees, logs, labels, and pagination metadata."""

  def history_paged(
    self,
    *,
    address: str,
    chain: WalletEvmChain | None = None,
    from_block: int | None = None,
    to_block: int | None = None,
    from_date: str | None = None,
    to_date: str | None = None,
    order: Literal['ASC', 'DESC'] | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[WalletHistoryTransaction, str]:
    """Get decoded wallet activity for an EVM address, including native transfers, ERC20 transfers, NFT transfers, fees, logs, labels, and pagination metadata.

    Paged variant of `history`: Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      address: Wallet address whose decoded history should be fetched.
      chain:
      from_block: Minimum block number to include. Moralis uses this over `from_date` when both are provided.
      to_block: Maximum block number to include. Moralis uses this over `to_date` when both are provided.
      from_date: Start date for history collection when `from_block` is not provided. Moralis accepts seconds or a date string.
      to_date: End date for history collection when `to_block` is not provided. Moralis accepts seconds or a date string.
      order: Sort order for returned activity.
      limit: Maximum number of history rows to return in this page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/wallet/wallet-history)
    """

    async def next(cursor: str) -> tuple[list[WalletHistoryTransaction], str | None]:
      response = await self.history(
        address=address,
        chain=chain,
        from_block=from_block,
        to_block=to_block,
        from_date=from_date,
        to_date=to_date,
        order=order,
        limit=limit,
        cursor=(cursor or None),
        validate=validate,
      )
      rows = response.get('result')
      state = response.get('cursor')
      return rows or [], state or None

    return PaginatedResponse('', next)

  async def history(
    self,
    *,
    address: str,
    chain: WalletEvmChain | None = None,
    from_block: int | None = None,
    to_block: int | None = None,
    from_date: str | None = None,
    to_date: str | None = None,
    cursor: str | None = None,
    order: Literal['ASC', 'DESC'] | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> WalletHistoryResponse:
    """Get decoded wallet activity for an EVM address, including native transfers, ERC20 transfers, NFT transfers, fees, logs, labels, and pagination metadata.

    Args:
      address: Wallet address whose decoded history should be fetched.
      chain:
      from_block: Minimum block number to include. Moralis uses this over `from_date` when both are provided.
      to_block: Maximum block number to include. Moralis uses this over `to_date` when both are provided.
      from_date: Start date for history collection when `from_block` is not provided. Moralis accepts seconds or a date string.
      to_date: End date for history collection when `to_block` is not provided. Moralis accepts seconds or a date string.
      cursor: Pagination cursor returned by the previous response.
      order: Sort order for returned activity.
      limit: Maximum number of history rows to return in this page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.moralis.com/data-api/evm/wallet/wallet-history)
    """
    request: Request = Request(address=address)
    if chain is not None:
      request['chain'] = chain
    if from_block is not None:
      request['from_block'] = from_block
    if to_block is not None:
      request['to_block'] = to_block
    if from_date is not None:
      request['from_date'] = from_date
    if to_date is not None:
      request['to_date'] = to_date
    if cursor is not None:
      request['cursor'] = cursor
    if order is not None:
      request['order'] = order
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='GET',
      path='/wallets/{address}/history',
      validate=validate,
      request_type=Request,
      response_type=WalletHistoryResponse,
    )
