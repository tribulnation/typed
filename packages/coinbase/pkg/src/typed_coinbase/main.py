# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .app import App
from .exchange import Exchange
from .international import International
from typed_coinbase.core.base import CoinbaseBase


class Coinbase(CoinbaseBase):
  """Coinbase App (CDP API Key), Coinbase Exchange (separate HMAC key), and credential-free International Exchange instrument metadata and funding history.

  References:
    - [Upstream docs](https://docs.cdp.coinbase.com/coinbase-app/docs/welcome)
  """

  @cached_property
  def app(self) -> App:
    """Coinbase App (Consumer APIs), including Advanced Trade. Coinbase Exchange and International Exchange are separate sibling product namespaces, not part of app.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/coinbase-app/docs/welcome)
    """
    return App.new(
      self.app_client, market_client=self.market_client, user_client=self.user_client
    )

  @cached_property
  def exchange(self) -> Exchange:
    """Coinbase Exchange (formerly Pro/GDAX) — the institutional REST API and WebSocket feed: accounts, orders and fills, products/market data, transfers (deposits, withdrawals, conversions), profiles, reports, lending/margin, wrapped assets, and compliance (travel rule, address book).

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/exchange-api/rest-api/introduction)
    """
    return Exchange.new(self.exchange_client, feed_client=self.feed_client)

  @cached_property
  def international(self) -> International:
    """Coinbase International Exchange public instrument metadata and funding history. No account credentials are used.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/international-exchange-api/rest-api/instruments/get-historical-funding-rate)
    """
    return International(client=self.international_client)
