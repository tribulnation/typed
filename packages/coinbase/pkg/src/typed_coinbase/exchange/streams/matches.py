# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_coinbase.core import TimestampIso
from typed_coinbase.core.endpoint.stream import StreamEndpoint


class ExchangeLastMatch(TypedDict):
  """One executed trade on a product."""

  type: Literal['last_match']
  """Always `last_match`."""
  trade_id: int
  """Trade id."""
  sequence: NotRequired[int]
  """Per-connection sequence number."""
  maker_order_id: str
  """The maker order's id."""
  taker_order_id: str
  """The taker order's id."""
  time: TimestampIso
  """Trade execution time."""
  product_id: str
  """The product this trade occurred on."""
  size: Decimal
  """Trade size, in base currency."""
  price: Decimal
  """Trade price."""
  side: Literal['buy', 'sell']
  """The taker's side of the trade."""


class ExchangeMatch(TypedDict):
  """One executed trade on a product."""

  type: Literal['match']
  """Always `match`."""
  trade_id: int
  """Trade id."""
  sequence: NotRequired[int]
  """Per-connection sequence number."""
  maker_order_id: str
  """The maker order's id."""
  taker_order_id: str
  """The taker order's id."""
  time: TimestampIso
  """Trade execution time."""
  product_id: str
  """The product this trade occurred on."""
  size: Decimal
  """Trade size, in base currency."""
  price: Decimal
  """Trade price."""
  side: Literal['buy', 'sell']
  """The taker's side of the trade."""


class Parameters(TypedDict):
  """Subscribe parameters."""

  product_ids: list[str]
  """Products to receive updates for."""


Payload = ExchangeLastMatch | ExchangeMatch


class Matches(StreamEndpoint):
  """Every trade executed on a product. No authentication required."""

  def matches(
    self,
    product_ids: list[str],
    *,
    validate: bool | None = None,
  ) -> StreamManager[Payload, Any, Any]:
    """Every trade executed on a product. No authentication required.

    Args:
      product_ids: Products to receive updates for.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/websocket-feed/channels)
    """
    return self.subscribe(
      'matches',
      Parameters(product_ids=product_ids),
      meta={},
      validate=validate,
      request_type=Parameters,
      response_type=Payload,
    )
