# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_coinbase.core import TimestampIso
from typed_coinbase.core.endpoint.stream import StreamEndpoint


class ExchangeAuction(TypedDict):
  """One indicative (non-firm) auction quote for a product in auction mode."""

  type: Literal['auction']
  """Always `auction`."""
  product_id: str
  """The product this quote reports on."""
  sequence: int
  """Per-connection sequence number."""
  auction_state: str
  """Auction phase, e.g. `collection`. Not documented as an exhaustive enumeration."""
  best_bid_price: Decimal
  """Best indicative bid price."""
  best_bid_size: Decimal
  """Best indicative bid size."""
  best_ask_price: Decimal
  """Best indicative ask price."""
  best_ask_size: Decimal
  """Best indicative ask size."""
  open_price: Decimal
  """Indicative auction open price."""
  open_size: Decimal
  """Indicative auction open size."""
  can_open: NotRequired[str]
  """Whether the auction can open, e.g. `yes`/`no`. Not documented as an exhaustive enumeration."""
  timestamp: TimestampIso
  """Time this indicative quote was generated."""


class Parameters(TypedDict):
  """Subscribe parameters."""

  product_ids: list[str]
  """Products to receive updates for."""


class Auction(StreamEndpoint):
  """Indicative (non-firm) auction quotes for a product while it is in auction mode, sent approximately every second. No authentication required."""

  def auction(
    self,
    product_ids: list[str],
    *,
    validate: bool | None = None,
  ) -> StreamManager[ExchangeAuction, Any, Any]:
    """Indicative (non-firm) auction quotes for a product while it is in auction mode, sent approximately every second. No authentication required.

    Args:
      product_ids: Products to receive updates for.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/websocket-feed/channels)
    """
    return self.subscribe(
      'auctionfeed',
      Parameters(product_ids=product_ids),
      meta={},
      validate=validate,
      request_type=Parameters,
      response_type=ExchangeAuction,
    )
