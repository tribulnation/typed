# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typed_core.util import StreamManager
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_coinbase.core.endpoint.stream import StreamEndpoint
from typed_coinbase.schemas import ExchangeCurrencyDetails, ExchangeNetwork


class ExchangeStatusProduct(TypedDict):
  """One product and its status, as reported on the status stream -- carries an extra `type` field not present in the REST `ExchangeProduct` shape, confirmed live."""

  id: str
  """Trading pair id, e.g. `BTC-USD`."""
  base_currency: str
  """Base currency code."""
  quote_currency: str
  """Quote currency code."""
  base_increment: Decimal
  """Minimum order size increment."""
  quote_increment: Decimal
  """Minimum order price increment."""
  display_name: str
  """Human-readable pair name."""
  status: Literal['online', 'offline', 'internal', 'delisted']
  """Trading status."""
  margin_enabled: bool
  """Whether margin trading is enabled for this product."""
  status_message: NotRequired[str]
  """Free-text elaboration on `status`."""
  min_market_funds: Decimal
  """Minimum funds for a market order."""
  post_only: bool
  """Whether only post-only orders are accepted."""
  limit_only: bool
  """Whether only limit orders are accepted."""
  cancel_only: bool
  """Whether only order cancellations are accepted."""
  auction_mode: bool
  """Whether the product is in auction mode."""
  type: NotRequired[str]
  """Product type, e.g. `spot`. Not documented as an exhaustive enumeration -- present here but absent from the REST `ExchangeProduct` shape, confirmed live."""
  fx_stablecoin: NotRequired[bool]
  """Whether this is a stablecoin FX pair."""
  max_slippage_percentage: NotRequired[Decimal]
  """Maximum allowed slippage, as a percentage."""


class ExchangeStatusCurrency(TypedDict):
  """One currency and its status, as reported on the status stream."""

  id: str
  """Currency code."""
  name: str
  """Currency display name."""
  display_name: NotRequired[str]
  """Human-readable display name."""
  min_size: Decimal
  """Minimum order size for this currency."""
  status: str
  """Currency status. Not documented as an exhaustive enumeration."""
  funding_account_id: NotRequired[str]
  """Internal funding account id, usually empty."""
  status_message: NotRequired[str]
  """Free-text elaboration on `status`."""
  max_precision: Decimal
  """Smallest fractional unit tradable for this currency."""
  convertible_to: NotRequired[list[str]]
  """Currency codes this currency can be directly converted to."""
  details: ExchangeCurrencyDetails
  default_network: NotRequired[str]
  """Id of the default network for deposits/withdrawals."""
  supported_networks: NotRequired[list[ExchangeNetwork]]
  """Every network this currency can be deposited/withdrawn over."""
  network_map: NotRequired[dict[str, Any] | None]
  """Internal network routing metadata, or null -- confirmed live, shape not documented."""


class ExchangeStatus(TypedDict):
  """Every known product and currency and its current trading status."""

  type: Literal['status']
  """Always `status`."""
  products: list[ExchangeStatusProduct]
  """Every known product."""
  currencies: list[ExchangeStatusCurrency]
  """Every known currency (this channel's own shape, distinct from `ExchangeCurrency` -- it nests `default_network`/`supported_networks` under a `funding_account_id`-bearing frame confirmed live)."""


class Status(StreamEndpoint):
  """A snapshot of every known product and currency and its trading status, sent once on subscribe and again on a preset interval. No authentication required."""

  def status(
    self,
    *,
    validate: bool | None = None,
  ) -> StreamManager[ExchangeStatus, Any, Any]:
    """A snapshot of every known product and currency and its trading status, sent once on subscribe and again on a preset interval. No authentication required.

    Args:
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/websocket-feed/channels)
    """
    return self.subscribe(
      'status',
      None,
      meta={},
      validate=validate,
      response_type=ExchangeStatus,
    )
