# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core.util import StreamManager
from typing_extensions import Any, TypedDict
from typed_coinbase.core.endpoint.stream import StreamEndpoint
from typed_coinbase.schemas import (
  ExchangeActivate,
  ExchangeChangeModifyOrder,
  ExchangeChangeStp,
  ExchangeFullMatch,
  ExchangeOrderDone,
  ExchangeOrderOpen,
  ExchangeReceivedLimit,
  ExchangeReceivedMarket,
)


class Parameters(TypedDict):
  """Subscribe parameters."""

  product_ids: list[str]
  """Products to receive updates for."""


Payload = (
  ExchangeReceivedLimit
  | ExchangeReceivedMarket
  | ExchangeOrderOpen
  | ExchangeOrderDone
  | ExchangeFullMatch
  | ExchangeChangeStp
  | ExchangeChangeModifyOrder
  | ExchangeActivate
)


class User(StreamEndpoint):
  """Every order lifecycle event for the authenticated caller's own orders on a product: received, open, done, match, change, and stop-order activation. Requires a signed subscribe."""

  def user(
    self,
    product_ids: list[str],
    *,
    validate: bool | None = None,
  ) -> StreamManager[Payload, Any, Any]:
    """Every order lifecycle event for the authenticated caller's own orders on a product: received, open, done, match, change, and stop-order activation. Requires a signed subscribe.

    Args:
      product_ids: Products to receive updates for.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/websocket-feed/channels)
    """
    return self.subscribe(
      'user',
      Parameters(product_ids=product_ids),
      meta={'signed': True},
      validate=validate,
      request_type=Parameters,
      response_type=Payload,
    )
