# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_coinbase.core import TimestampIso
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeReport


class Request(TypedDict):
  profile_id: NotRequired[str]
  """Filter results by a specific profile id."""
  after: NotRequired[TimestampIso]
  """Filter results to reports created after this time."""
  limit: NotRequired[int]
  """Limit results to a specific number."""
  type: NotRequired[
    Literal[
      'account',
      'balance',
      'fills',
      'otc-fills',
      'rfq-fills',
      'tax-invoice',
      '1099k-transaction-history',
    ]
  ]
  """Filter results by report type."""
  ignore_expired: NotRequired[bool]
  """Exclude expired reports from the results."""


Response = list[ExchangeReport]


class List(RpcEndpoint):
  """Get a list of all reports the caller has generated."""

  async def list(
    self,
    profile_id: str | None = None,
    *,
    after: TimestampIso | None = None,
    limit: int | None = None,
    type: Literal[
      'account',
      'balance',
      'fills',
      'otc-fills',
      'rfq-fills',
      'tax-invoice',
      '1099k-transaction-history',
    ]
    | None = None,
    ignore_expired: bool | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get a list of all reports the caller has generated.

    Args:
      profile_id: Filter results by a specific profile id.
      after: Filter results to reports created after this time.
      limit: Limit results to a specific number.
      type: Filter results by report type.
      ignore_expired: Exclude expired reports from the results.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = Request()
    if profile_id is not None:
      request['profile_id'] = profile_id
    if after is not None:
      request['after'] = after
    if limit is not None:
      request['limit'] = limit
    if type is not None:
      request['type'] = type
    if ignore_expired is not None:
      request['ignore_expired'] = ignore_expired
    return await self.request(
      request,
      method='GET',
      path='/reports',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
