# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_coinbase.core import TimestampIso
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeReportDateRange


class Exchange1099kTransactionHistoryReportRequest(TypedDict):
  """Generate a 1099-K transaction history report for a tax year."""

  type: Literal['1099k-transaction-history']
  """Always `1099k-transaction-history`."""
  format: NotRequired[Literal['pdf', 'csv']]
  """Report file format."""
  email: NotRequired[str]
  """Email to send the report-ready notification to."""
  profile_id: NotRequired[str]
  """Profile to generate the report for. If set, must be the profile linked to the API key."""
  year: str
  """Tax year to generate the 1099-K report for, e.g. `2023`. Required for this report type."""


class ExchangeAccountReportParams(TypedDict):
  """Account report parameters."""

  start_date: NotRequired[TimestampIso]
  """Start date for items to include in the report. Defaults to 30 days before `end_date`."""
  end_date: NotRequired[TimestampIso]
  """End date for items to include in the report. Defaults to today."""
  account_id: NotRequired[str]
  """Account to generate the report for, or `ALL`."""


class ExchangeBalanceReportParams(TypedDict):
  """Balance report parameters."""

  datetime: NotRequired[TimestampIso]
  """Designated date and time of the balance statement, always UTC. Omit for the caller's current balance."""
  group_by_profile: NotRequired[bool]
  """Group results by profile. Only available through report generation via the Exchange UI, not through an API key."""


class ExchangeReportCreateResult(TypedDict):
  """Acknowledgement that a report has begun generating."""

  id: str
  """Id of the newly created report."""
  type: Literal[
    'account',
    'balance',
    'fills',
    'otc-fills',
    'rfq-fills',
    'tax-invoice',
    '1099k-transaction-history',
  ]
  """Report type."""
  status: Literal['pending']
  """Report generation status. Always `pending` immediately after creation."""


class ExchangeAccountReportRequest(TypedDict):
  """Generate an account report: account activity over a date range."""

  type: Literal['account']
  """Always `account`."""
  format: NotRequired[Literal['pdf', 'csv']]
  """Report file format."""
  email: NotRequired[str]
  """Email to send the report-ready notification to."""
  profile_id: NotRequired[str]
  """Profile to generate the report for. If set, must be the profile linked to the API key."""
  account: NotRequired[ExchangeAccountReportParams]


class ExchangeBalanceReportRequest(TypedDict):
  """Generate a balance report: a snapshot of balances at a point in time (or the current balance, if `datetime` is omitted)."""

  type: Literal['balance']
  """Always `balance`."""
  format: NotRequired[Literal['pdf', 'csv']]
  """Report file format."""
  email: NotRequired[str]
  """Email to send the report-ready notification to."""
  profile_id: NotRequired[str]
  """Profile to generate the report for. If set, must be the profile linked to the API key."""
  balance: NotRequired[ExchangeBalanceReportParams]


class ExchangeFillsReportRequest(TypedDict):
  """Generate a fills report: matched trades over a date range."""

  type: Literal['fills']
  """Always `fills`."""
  format: NotRequired[Literal['pdf', 'csv']]
  """Report file format."""
  email: NotRequired[str]
  """Email to send the report-ready notification to."""
  profile_id: NotRequired[str]
  """Profile to generate the report for. If set, must be the profile linked to the API key."""
  fills: NotRequired[ExchangeReportDateRange]


class ExchangeOtcFillsReportRequest(TypedDict):
  """Generate an OTC fills report: matched OTC (over-the-counter) trades over a date range."""

  type: Literal['otc-fills']
  """Always `otc-fills`."""
  format: NotRequired[Literal['pdf', 'csv']]
  """Report file format."""
  email: NotRequired[str]
  """Email to send the report-ready notification to."""
  profile_id: NotRequired[str]
  """Profile to generate the report for. If set, must be the profile linked to the API key."""
  otc_fills: NotRequired[ExchangeReportDateRange]


class ExchangeRfqFillsReportRequest(TypedDict):
  """Generate an RFQ fills report: matched request-for-quote trades over a date range."""

  type: Literal['rfq-fills']
  """Always `rfq-fills`."""
  format: NotRequired[Literal['pdf', 'csv']]
  """Report file format."""
  email: NotRequired[str]
  """Email to send the report-ready notification to."""
  profile_id: NotRequired[str]
  """Profile to generate the report for. If set, must be the profile linked to the API key."""
  rfq_fills: NotRequired[ExchangeReportDateRange]


class ExchangeTaxInvoiceReportRequest(TypedDict):
  """Generate a tax invoice report over a date range."""

  type: Literal['tax-invoice']
  """Always `tax-invoice`."""
  format: NotRequired[Literal['pdf', 'csv']]
  """Report file format."""
  email: NotRequired[str]
  """Email to send the report-ready notification to."""
  profile_id: NotRequired[str]
  """Profile to generate the report for. If set, must be the profile linked to the API key."""
  tax_invoice: NotRequired[ExchangeReportDateRange]


Request = (
  ExchangeAccountReportRequest
  | ExchangeBalanceReportRequest
  | ExchangeFillsReportRequest
  | ExchangeOtcFillsReportRequest
  | ExchangeTaxInvoiceReportRequest
  | ExchangeRfqFillsReportRequest
  | Exchange1099kTransactionHistoryReportRequest
)


class Create(RpcEndpoint):
  """Generate a report. Reports with historical data can be created for any report type; balance reports can also be a snapshot of current data."""

  async def create(
    self,
    report_request: Request,
    *,
    validate: bool | None = None,
  ) -> ExchangeReportCreateResult:
    """Generate a report. Reports with historical data can be created for any report type; balance reports can also be a snapshot of current data.

    Args:
      report_request: The report to generate.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      report_request,
      method='POST',
      path='/reports',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeReportCreateResult,
    )
