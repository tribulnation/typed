# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_coinbase.core import TimestampIso
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class ExchangeCounterpartyAddress(TypedDict):
  """A single counterparty address book entry."""

  id: str
  """Counterparty address book entry id."""
  address: str
  """The stored counterparty address."""
  label: str
  """Label/nickname for this counterparty address book entry."""
  added_at: TimestampIso
  """Time this entry was added to the counterparty address book."""


ExchangeCounterpartyAddresses = list[ExchangeCounterpartyAddress]


class Counterparty(RpcEndpoint):
  """Get all counterparty addresses stored in the address book."""

  async def counterparty(
    self,
    *,
    validate: bool | None = None,
  ) -> ExchangeCounterpartyAddresses:
    """Get all counterparty addresses stored in the address book.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      None,
      method='GET',
      path='/address-book/counterparty',
      meta={'signed': True},
      validate=validate,
      response_type=ExchangeCounterpartyAddresses,
    )
