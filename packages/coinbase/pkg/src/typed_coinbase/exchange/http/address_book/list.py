# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core import TimestampIso
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class ExchangeAddressBookEntry(TypedDict):
  """A single address book entry."""

  id: str
  """Address book entry id."""
  address: str
  """The stored crypto address."""
  destination_tag: NotRequired[str]
  """Destination tag/memo, for currencies that use one."""
  currency: str
  """Asset symbol for the saved address (e.g., `BTC`, `ETH`, `USDC`). `_ALL_ASSETS_` indicates that this address is stored globally for all assets, rather than a specific one. The `network` field determines which blockchain network the address applies to."""
  label: str
  """Label/nickname for this address book entry."""
  address_book_added_at: TimestampIso
  """Time this entry was added to the address book."""
  is_verified_self_hosted_wallet: NotRequired[bool]
  """Flag to indicate if the crypto address has previously been digitally signed and verified when added in the Address Book UI tab."""
  vasp_id: NotRequired[str]
  """The VASP identifier if the address is owned by one of the supported Virtual Asset Service Providers."""
  business_name: NotRequired[str]
  """Business name of the originator's account -- only populated for travel rules regions."""
  business_country_code: NotRequired[str]
  """The country code (ISO 3166-1 alpha-2) of the originator's account location -- only populated for travel rules regions."""
  network: NotRequired[str]
  """Blockchain network of the address. If omitted or `null`, the address is available on all supported networks compatible with the asset (e.g., both `ethereum` and `arbitrum` for an ERC-20 token). When `currency` is `_ALL_ASSETS_` and network is `_ALL_EVM_NETWORKS_`, the address is available on all assets with EVM-compatible networks."""


ExchangeAddressBookEntries = list[ExchangeAddressBookEntry]


class List(RpcEndpoint):
  """Get all addresses stored in the caller's address book."""

  async def list(self, *, validate: bool | None = None) -> ExchangeAddressBookEntries:
    """Get all addresses stored in the caller's address book.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      None,
      method='GET',
      path='/address-book',
      meta={'signed': True},
      validate=validate,
      response_type=ExchangeAddressBookEntries,
    )
