# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import (
  ExchangeAddressBookWhitelistedAddress,
  ExchangeCryptoDestination,
)


class ExchangeAddressBookCreateResponse(TypedDict):
  """The addresses that were created."""

  body: list[ExchangeAddressBookWhitelistedAddress]
  """The created address book entries."""


class ExchangeAddressBookNewAddress(TypedDict):
  """A new address book entry to create."""

  currency: str
  """Asset symbol for the saved address (e.g., `BTC`, `ETH`, `USDC`). Use `_ALL_ASSETS_` to indicate that this address is stored globally for all assets, rather than a specific one. When `currency` is set to `_ALL_ASSETS_`, the `network` field is required to define the applicable blockchain network."""
  to: ExchangeCryptoDestination
  label: str
  """Label/nickname for this address book entry."""
  is_verified_self_hosted_wallet: NotRequired[bool]
  """Flag indicating whether the crypto address was digitally signed and verified when added in the Address Book UI. This is mandatory for crypto withdrawals in jurisdictions governed by travel rules."""
  vasp_id: NotRequired[
    Literal[
      'Coinbase',
      'Anchorage',
      'Balance',
      'bitFlyer',
      'BitGo',
      'Bittrex',
      'Circle',
      'Coinhako',
      'Fidelity',
      'Gemini',
      'Kraken',
      'Paxos',
      'PayPal',
      'Robinhood',
      'Shakepay',
      'Standard Custody',
      'Tradestation',
      'Zero Hash',
    ]
  ]
  """VASP identifier for addresses owned by supported Virtual Asset Service Providers. Required for crypto withdrawals in jurisdictions governed by travel rules."""
  business_name: NotRequired[str]
  """Business name of the originator's account."""
  business_country_code: NotRequired[str]
  """The country code (ISO 3166-1 alpha-2) of the originator's account location."""
  is_certified_self_send: NotRequired[bool]
  """Flag to indicate if the caller owns the crypto address."""
  network: NotRequired[str]
  """Blockchain network of the address. Provide a single network name (e.g., `ethereum`, `bitcoin`). If omitted or `null`, the address is available on all supported networks compatible with the asset (e.g., both `ethereum` and `arbitrum` for an ERC-20 token). When `currency` is `_ALL_ASSETS_`, `network` is required. Use `_ALL_EVM_NETWORKS_` only with `_ALL_ASSETS_` to apply the address to all EVM-compatible networks."""
  wallet_verification_network: NotRequired[str]
  """Blockchain network used to verify ownership of the wallet address. Required for self-hosted wallets in jurisdictions governed by travel rules."""


class Request(TypedDict):
  """Addresses to add to the address book."""

  addresses: NotRequired[list[ExchangeAddressBookNewAddress]]
  """List of addresses to add to the address book."""


class Create(RpcEndpoint):
  """Add one or more new addresses to the caller's address book."""

  async def create(
    self,
    addresses: list[ExchangeAddressBookNewAddress] | None = None,
    *,
    validate: bool | None = None,
  ) -> ExchangeAddressBookCreateResponse:
    """Add one or more new addresses to the caller's address book.

    Args:
      addresses: List of addresses to add to the address book.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = Request()
    if addresses is not None:
      request['addresses'] = addresses
    return await self.request(
      request,
      method='POST',
      path='/address-book',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeAddressBookCreateResponse,
    )
