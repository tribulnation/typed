# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


ExchangeAddressBookDeleteResponse = dict[str, Any]


class Request(TypedDict):
  id: str
  """Address book identifier."""


class Delete(RpcEndpoint):
  """Delete an address from the address book."""

  async def delete(
    self,
    id: str,
    *,
    validate: bool | None = None,
  ) -> ExchangeAddressBookDeleteResponse:
    """Delete an address from the address book.

    Args:
      id: Address book identifier.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      Request(id=id),
      method='DELETE',
      path='/address-book/{id}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeAddressBookDeleteResponse,
    )
