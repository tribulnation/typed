# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeProduct


class Request(TypedDict):
  type: NotRequired[str]
  """Filter by product type. Not documented as a closed set by the official OpenAPI spec."""


Response = list[ExchangeProduct]


class List(RpcEndpoint):
  """List every known trading pair, live and delisted. No authentication required."""

  async def list(
    self, type: str | None = None, *, validate: bool | None = None
  ) -> Response:
    """List every known trading pair, live and delisted. No authentication required.

    Args:
      type: Filter by product type. Not documented as a closed set by the official OpenAPI spec.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = Request()
    if type is not None:
      request['type'] = type
    return await self.request(
      request,
      method='GET',
      path='/products',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
