# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeProduct


class Request(TypedDict):
  product_id: str
  """Trading pair id, e.g. `BTC-USD`."""


class Get(RpcEndpoint):
  """Get information on a single product. No authentication required."""

  async def get(
    self, product_id: str, *, validate: bool | None = None
  ) -> ExchangeProduct:
    """Get information on a single product. No authentication required.

    Args:
      product_id: Trading pair id, e.g. `BTC-USD`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      Request(product_id=product_id),
      method='GET',
      path='/products/{product_id}',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=ExchangeProduct,
    )
