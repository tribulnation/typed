# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class ExchangeProductVolume(TypedDict):
  """24-hour and 30-day volume for one product, broken out by market type."""

  id: str
  """Trading pair id, e.g. `BTC-USD`."""
  base_currency: str
  """Base currency code."""
  quote_currency: str
  """Quote currency code."""
  display_name: str
  """Human-readable pair name."""
  market_types: list[str]
  """Market types this product trades under, e.g. `spot`, `rfq`."""
  spot_volume_24hour: str
  """Spot trading volume over the last 24 hours, as a decimal string, or empty string when no spot volume applies to this product."""
  spot_volume_30day: str
  """Spot trading volume over the last 30 days, as a decimal string, or empty string when no spot volume applies to this product."""
  rfq_volume_24hour: Decimal
  """RFQ (request-for-quote) trading volume over the last 24 hours."""
  rfq_volume_30day: Decimal
  """RFQ trading volume over the last 30 days."""
  conversion_volume_24hour: Decimal
  """Conversion volume over the last 24 hours."""
  conversion_volume_30day: Decimal
  """Conversion volume over the last 30 days."""


Response = list[ExchangeProductVolume]


class VolumeSummary(RpcEndpoint):
  """30-day and 24-hour trading volume for every product and market type. No authentication required."""

  async def volume_summary(self, *, validate: bool | None = None) -> Response:
    """30-day and 24-hour trading volume for every product and market type. No authentication required.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      None,
      method='GET',
      path='/products/volume-summary',
      meta={},
      validate=validate,
      response_type=Response,
    )
