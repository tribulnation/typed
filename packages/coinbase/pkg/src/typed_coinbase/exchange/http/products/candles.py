# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_coinbase.core import TimestampIso, TimestampSeconds
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  product_id: str
  """Trading pair id, e.g. `BTC-USD`."""
  granularity: NotRequired[Literal[60, 300, 900, 3600, 21600, 86400]]
  """Bucket size in seconds. One of 60 (1 minute), 300 (5 minutes), 900 (15 minutes), 3600 (1 hour), 21600 (6 hours), 86400 (1 day)."""
  start: NotRequired[TimestampIso]
  """Start time of the requested range, RFC 3339."""
  end: NotRequired[TimestampIso]
  """End time of the requested range, RFC 3339."""


Response = list[tuple[TimestampSeconds, float, float, float, float, float]]


class Candles(RpcEndpoint):
  """Historic rates (candles) for a product, in fixed-size time buckets. Candles are old-to-new grouped into buckets of size `granularity`; a request spanning more than 300 buckets is rejected. No authentication required."""

  async def candles(
    self,
    product_id: str,
    *,
    granularity: Literal[60, 300, 900, 3600, 21600, 86400] | None = None,
    start: TimestampIso | None = None,
    end: TimestampIso | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Historic rates (candles) for a product, in fixed-size time buckets. Candles are old-to-new grouped into buckets of size `granularity`; a request spanning more than 300 buckets is rejected. No authentication required.

    Args:
      product_id: Trading pair id, e.g. `BTC-USD`.
      granularity: Bucket size in seconds. One of 60 (1 minute), 300 (5 minutes), 900 (15 minutes), 3600 (1 hour), 21600 (6 hours), 86400 (1 day).
      start: Start time of the requested range, RFC 3339.
      end: End time of the requested range, RFC 3339.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = Request(product_id=product_id)
    if granularity is not None:
      request['granularity'] = granularity
    if start is not None:
      request['start'] = start
    if end is not None:
      request['end'] = end
    return await self.request(
      request,
      method='GET',
      path='/products/{product_id}/candles',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
