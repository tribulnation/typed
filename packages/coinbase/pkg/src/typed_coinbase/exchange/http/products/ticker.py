# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core import TimestampIso
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class ExchangeProductTicker(TypedDict):
  """A snapshot of a product's most recent trade, best bid/ask, and 24-hour volume."""

  ask: Decimal
  """Best ask price."""
  bid: Decimal
  """Best bid price."""
  volume: Decimal
  """Base currency trading volume over the last 24 hours."""
  trade_id: int
  """Id of the most recent trade."""
  price: Decimal
  """Price of the most recent trade."""
  size: Decimal
  """Size of the most recent trade."""
  time: TimestampIso
  """Time of the most recent trade."""
  rfq_volume: NotRequired[Decimal]
  """RFQ (request-for-quote) trading volume over the last 24 hours."""
  conversions_volume: NotRequired[Decimal]
  """Conversion trading volume over the last 24 hours."""


class Request(TypedDict):
  product_id: str
  """Trading pair id, e.g. `BTC-USD`."""


class Ticker(RpcEndpoint):
  """Snapshot of the last trade, best bid/ask, and 24-hour volume for a product. No authentication required."""

  async def ticker(
    self,
    product_id: str,
    *,
    validate: bool | None = None,
  ) -> ExchangeProductTicker:
    """Snapshot of the last trade, best bid/ask, and 24-hour volume for a product. No authentication required.

    Args:
      product_id: Trading pair id, e.g. `BTC-USD`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      Request(product_id=product_id),
      method='GET',
      path='/products/{product_id}/ticker',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=ExchangeProductTicker,
    )
