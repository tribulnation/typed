# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class ExchangeProductStats(TypedDict):
  """24-hour and 30-day trading stats for one product."""

  open: Decimal
  """Opening price 24 hours ago."""
  high: Decimal
  """Highest price in the last 24 hours."""
  low: Decimal
  """Lowest price in the last 24 hours."""
  last: Decimal
  """Last traded price."""
  volume: Decimal
  """Base currency trading volume over the last 24 hours."""
  volume_30day: NotRequired[Decimal]
  """Base currency trading volume over the last 30 days."""
  rfq_volume_24hour: NotRequired[Decimal]
  """RFQ (request-for-quote) trading volume over the last 24 hours."""
  rfq_volume_30day: NotRequired[Decimal]
  """RFQ trading volume over the last 30 days."""


class Request(TypedDict):
  product_id: str
  """Trading pair id, e.g. `BTC-USD`."""


class Stats(RpcEndpoint):
  """30-day and 24-hour trading stats for a product. No authentication required."""

  async def stats(
    self,
    product_id: str,
    *,
    validate: bool | None = None,
  ) -> ExchangeProductStats:
    """30-day and 24-hour trading stats for a product. No authentication required.

    Args:
      product_id: Trading pair id, e.g. `BTC-USD`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      Request(product_id=product_id),
      method='GET',
      path='/products/{product_id}/stats',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=ExchangeProductStats,
    )
