# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeWrappedAsset


class Request(TypedDict):
  wrapped_asset_id: str
  """Wrapped asset symbol, e.g. `CBETH`."""


class Get(RpcEndpoint):
  """Get the circulating and total supply, and current conversion rate, of a single wrapped asset. No authentication required."""

  async def get(
    self,
    wrapped_asset_id: str,
    *,
    validate: bool | None = None,
  ) -> ExchangeWrappedAsset:
    """Get the circulating and total supply, and current conversion rate, of a single wrapped asset. No authentication required.

    Args:
      wrapped_asset_id: Wrapped asset symbol, e.g. `CBETH`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      Request(wrapped_asset_id=wrapped_asset_id),
      method='GET',
      path='/wrapped-assets/{wrapped_asset_id}',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=ExchangeWrappedAsset,
    )
