# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeStakeWrap


class Request(TypedDict):
  """Request to create a new stake-wrap."""

  from_currency: str
  """Source currency to stake, e.g. `ETH`."""
  to_currency: str
  """Destination (wrapped-asset) currency, e.g. `CBETH`."""
  amount: Decimal
  """Amount of `from_currency` to stake and wrap."""


class StakeWrapCreate(RpcEndpoint):
  """Stake and wrap `from_currency` into `to_currency`. Funds are stake-wrapped in the profile associated with the API key."""

  async def stake_wrap_create(
    self,
    *,
    from_currency: str,
    to_currency: str,
    amount: Decimal,
    validate: bool | None = None,
  ) -> ExchangeStakeWrap:
    """Stake and wrap `from_currency` into `to_currency`. Funds are stake-wrapped in the profile associated with the API key.

    Args:
      from_currency: Source currency to stake, e.g. `ETH`.
      to_currency: Destination (wrapped-asset) currency, e.g. `CBETH`.
      amount: Amount of `from_currency` to stake and wrap.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      Request(from_currency=from_currency, to_currency=to_currency, amount=amount),
      method='POST',
      path='/wrapped-assets/stake-wrap',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeStakeWrap,
    )
