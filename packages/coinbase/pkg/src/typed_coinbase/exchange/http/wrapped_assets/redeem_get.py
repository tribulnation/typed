# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeRedeem


class Request(TypedDict):
  redeem_id: str
  """Redeem id to fetch."""


class RedeemGet(RpcEndpoint):
  """Get details for a single redeem in the profile associated with the API key."""

  async def redeem_get(
    self,
    redeem_id: str,
    *,
    validate: bool | None = None,
  ) -> ExchangeRedeem:
    """Get details for a single redeem in the profile associated with the API key.

    Args:
      redeem_id: Redeem id to fetch.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      Request(redeem_id=redeem_id),
      method='GET',
      path='/wrapped-assets/redeem/{redeem_id}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeRedeem,
    )
