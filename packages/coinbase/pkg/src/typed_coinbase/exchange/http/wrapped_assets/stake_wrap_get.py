# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeStakeWrap


class Request(TypedDict):
  stake_wrap_id: str
  """Stake-wrap id to fetch."""


class StakeWrapGet(RpcEndpoint):
  """Get details for a single stake-wrap in the profile associated with the API key."""

  async def stake_wrap_get(
    self,
    stake_wrap_id: str,
    *,
    validate: bool | None = None,
  ) -> ExchangeStakeWrap:
    """Get details for a single stake-wrap in the profile associated with the API key.

    Args:
      stake_wrap_id: Stake-wrap id to fetch.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      Request(stake_wrap_id=stake_wrap_id),
      method='GET',
      path='/wrapped-assets/stake-wrap/{stake_wrap_id}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeStakeWrap,
    )
