# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core import TimestampIso
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeRedeem


class Request(TypedDict):
  before: NotRequired[TimestampIso]
  """Pagination cursor: return redeems with `created_at` timestamps newer than this date, e.g. `2023-03-15T23:40:04.505Z`."""
  after: NotRequired[TimestampIso]
  """Pagination cursor: return redeems with `created_at` timestamps older than this date, e.g. `2023-03-15T23:40:04.505Z`."""
  limit: NotRequired[int]
  """Limit on the number of results to return."""
  from_currency: NotRequired[str]
  """Filter by source (wrapped-asset) currency, e.g. `CBETH`."""
  to_currency: NotRequired[str]
  """Filter by destination currency, e.g. `ETH`."""
  status: NotRequired[str]
  """Filter by redeem status."""


Response = list[ExchangeRedeem]


class RedeemList(RpcEndpoint):
  """Get details for all redeems in the profile associated with the API key."""

  async def redeem_list(
    self,
    *,
    before: TimestampIso | None = None,
    after: TimestampIso | None = None,
    limit: int | None = None,
    from_currency: str | None = None,
    to_currency: str | None = None,
    status: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get details for all redeems in the profile associated with the API key.

    Args:
      before: Pagination cursor: return redeems with `created_at` timestamps newer than this date, e.g. `2023-03-15T23:40:04.505Z`.
      after: Pagination cursor: return redeems with `created_at` timestamps older than this date, e.g. `2023-03-15T23:40:04.505Z`.
      limit: Limit on the number of results to return.
      from_currency: Filter by source (wrapped-asset) currency, e.g. `CBETH`.
      to_currency: Filter by destination currency, e.g. `ETH`.
      status: Filter by redeem status.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = Request()
    if before is not None:
      request['before'] = before
    if after is not None:
      request['after'] = after
    if limit is not None:
      request['limit'] = limit
    if from_currency is not None:
      request['from_currency'] = from_currency
    if to_currency is not None:
      request['to_currency'] = to_currency
    if status is not None:
      request['status'] = status
    return await self.request(
      request,
      method='GET',
      path='/wrapped-assets/redeem',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
