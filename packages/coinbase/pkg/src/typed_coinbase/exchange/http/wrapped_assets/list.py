# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeWrappedAsset


class ExchangeWrappedAssetsList(TypedDict):
  """The whole wire frame -- not unwrapped by the core."""

  wrapped_assets: list[ExchangeWrappedAsset]
  """Every supported wrapped asset."""


class List(RpcEndpoint):
  """List every supported wrapped asset (e.g. cbETH). No authentication required."""

  async def list(self, *, validate: bool | None = None) -> ExchangeWrappedAssetsList:
    """List every supported wrapped asset (e.g. cbETH). No authentication required.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      None,
      method='GET',
      path='/wrapped-assets',
      meta={},
      validate=validate,
      response_type=ExchangeWrappedAssetsList,
    )
