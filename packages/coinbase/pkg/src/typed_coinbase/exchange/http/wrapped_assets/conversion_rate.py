# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class ExchangeWrappedAssetConversionRate(TypedDict):
  """The current conversion rate of a wrapped asset to its underlying asset."""

  amount: Decimal
  """Units of the underlying asset per one unit of the wrapped asset."""


class Request(TypedDict):
  wrapped_asset_id: str
  """Wrapped asset symbol, e.g. `CBETH`."""


class ConversionRate(RpcEndpoint):
  """Get the current conversion rate of a wrapped asset to its underlying asset. No authentication required."""

  async def conversion_rate(
    self,
    wrapped_asset_id: str,
    *,
    validate: bool | None = None,
  ) -> ExchangeWrappedAssetConversionRate:
    """Get the current conversion rate of a wrapped asset to its underlying asset. No authentication required.

    Args:
      wrapped_asset_id: Wrapped asset symbol, e.g. `CBETH`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      Request(wrapped_asset_id=wrapped_asset_id),
      method='GET',
      path='/wrapped-assets/{wrapped_asset_id}/conversion-rate',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=ExchangeWrappedAssetConversionRate,
    )
