# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core import TimestampIso
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeStakeWrap


RequestKeywords = TypedDict('RequestKeywords', {'from': NotRequired[str]})
"""
- `from`: Filter by source currency, e.g. `ETH`.
"""


class Request(RequestKeywords):
  before: NotRequired[TimestampIso]
  """Pagination cursor: return stake-wraps with `created_at` timestamps newer than this date, e.g. `2023-03-15T23:40:04.505Z`."""
  after: NotRequired[TimestampIso]
  """Pagination cursor: return stake-wraps with `created_at` timestamps older than this date, e.g. `2023-03-15T23:40:04.505Z`."""
  limit: NotRequired[int]
  """Limit on the number of results to return."""
  to: NotRequired[str]
  """Filter by destination currency, e.g. `CBETH`."""
  status: NotRequired[str]
  """Filter by stake-wrap status."""


Response = list[ExchangeStakeWrap]


class StakeWrapList(RpcEndpoint):
  """Get details for all stake-wraps in the profile associated with the API key."""

  async def stake_wrap_list(
    self,
    *,
    before: TimestampIso | None = None,
    after: TimestampIso | None = None,
    limit: int | None = None,
    from_: str | None = None,
    to: str | None = None,
    status: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get details for all stake-wraps in the profile associated with the API key.

    Args:
      before: Pagination cursor: return stake-wraps with `created_at` timestamps newer than this date, e.g. `2023-03-15T23:40:04.505Z`.
      after: Pagination cursor: return stake-wraps with `created_at` timestamps older than this date, e.g. `2023-03-15T23:40:04.505Z`.
      limit: Limit on the number of results to return.
      from_: Filter by source currency, e.g. `ETH`.
      to: Filter by destination currency, e.g. `CBETH`.
      status: Filter by stake-wrap status.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = Request()
    if before is not None:
      request['before'] = before
    if after is not None:
      request['after'] = after
    if limit is not None:
      request['limit'] = limit
    if from_ is not None:
      request['from'] = from_
    if to is not None:
      request['to'] = to
    if status is not None:
      request['status'] = status
    return await self.request(
      request,
      method='GET',
      path='/wrapped-assets/stake-wrap',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
