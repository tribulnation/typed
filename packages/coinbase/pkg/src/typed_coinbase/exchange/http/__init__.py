# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .accounts import Accounts
from .address_book import AddressBook
from .coinbase_accounts import CoinbaseAccounts
from .conversions import Conversions
from .currencies import Currencies
from .deposits import Deposits
from .fees import Fees
from .fills import Fills
from .loans import Loans
from .margin import Margin
from .orders import Orders
from .payment_methods import PaymentMethods
from .products import Products
from .profiles import Profiles
from .reports import Reports
from .transfers import Transfers
from .travel_rules import TravelRules
from .users import Users
from .withdrawals import Withdrawals
from .wrapped_assets import WrappedAssets
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class Http(RpcEndpoint):
  """Coinbase Exchange REST: accounts, orders and fills, products/currencies, transfers (deposits, withdrawals, conversions), profiles, reports, lending/margin, wrapped assets, and compliance (travel rule, address book).

  References:
    - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/exchange-api/rest-api/introduction)
  """

  @cached_property
  def accounts(self) -> Accounts:
    """Trading accounts on the calling profile — balances, holds, ledger entries, and transfers per account.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/exchange-api/rest-api/accounts/get-all-account-profile)
    """
    return Accounts(client=self.client)

  @cached_property
  def address_book(self) -> AddressBook:
    """The profile's whitelisted withdrawal addresses — list, add, update, and delete address book entries, and look up a counterparty's own published address book.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/exchange-api/rest-api/address-book/get-address-book)
    """
    return AddressBook(client=self.client)

  @cached_property
  def coinbase_accounts(self) -> CoinbaseAccounts:
    """The linked Coinbase.com (App) wallets available as a transfer source/destination for Exchange deposits and withdrawals, and generating a fresh crypto deposit address on one of them.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/exchange-api/rest-api/coinbase-accounts/get-all-coinbase-wallets)
    """
    return CoinbaseAccounts(client=self.client)

  @cached_property
  def conversions(self) -> Conversions:
    """Stablecoin conversions (e.g. USD to USDC) between two accounts on the same profile — create one, look one up, list past conversions, and read the current fee rate.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/exchange-api/rest-api/conversions/convert-currency)
    """
    return Conversions(client=self.client)

  @cached_property
  def currencies(self) -> Currencies:
    """Currencies known to Exchange — list every supported currency, or look one up by id.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/exchange-api/rest-api/currencies/get-all-known-currencies)
    """
    return Currencies(client=self.client)

  @cached_property
  def deposits(self) -> Deposits:
    """Fiat/crypto deposits into an Exchange account — deposit from a linked payment method or from a linked Coinbase.com wallet.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/exchange-api/rest-api/transfers/deposit-from-payment-method)
    """
    return Deposits(client=self.client)

  @cached_property
  def fees(self) -> Fees:
    """The profile's current maker/taker fee rate and trailing volume tier.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/exchange-api/rest-api/fees/get-fee)
    """
    return Fees(client=self.client)

  @cached_property
  def fills(self) -> Fills:
    """The profile's fill history — matched trades, filterable by order id or product id.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/exchange-api/rest-api/orders/get-all-fills)
    """
    return Fills(client=self.client)

  @cached_property
  def loans(self) -> Loans:
    """Coinbase Exchange's lending program — list loan assets and options, open a loan, preview and repay interest/principal (standard and cross-margin "xm" variants), and read interest-rate history and summaries.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/exchange-api/rest-api/loan/list-loans)
    """
    return Loans(client=self.client)

  @cached_property
  def margin(self) -> Margin:
    """Exchange futures margin settings — read and set the auto-loan (automatic margin-call loan) and USDC-conversion preferences.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/exchange-api/rest-api/futures/get-auto-loan-setting)
    """
    return Margin(client=self.client)

  @cached_property
  def orders(self) -> Orders:
    """Order lifecycle on Exchange — place, cancel (single or all), and look up orders.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/exchange-api/rest-api/orders/get-all-orders)
    """
    return Orders(client=self.client)

  @cached_property
  def payment_methods(self) -> PaymentMethods:
    """Fiat payment methods linked to the account, usable as a deposit/withdrawal source on Exchange.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/exchange-api/rest-api/transfers/get-all-payment-methods)
    """
    return PaymentMethods(client=self.client)

  @cached_property
  def products(self) -> Products:
    """Exchange's public market data — tradable products, order book, ticker, recent trades, historical candles, stats, and aggregate 24h volume.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/exchange-api/rest-api/products/get-all-known-trading-pairs)
    """
    return Products(client=self.client)

  @cached_property
  def profiles(self) -> Profiles:
    """Sub-account "profiles" under an Exchange account — create, rename, delete, and list them, and transfer funds between two profiles.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/exchange-api/rest-api/profiles/get-profiles)
    """
    return Profiles(client=self.client)

  @cached_property
  def reports(self) -> Reports:
    """Generated account/balance/fills/tax reports — request one (async, produced in the background), list past reports, and poll one by id for completion.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/exchange-api/rest-api/reports/get-all-reports)
    """
    return Reports(client=self.client)

  @cached_property
  def transfers(self) -> Transfers:
    """Transfer history on Exchange — list and look up past transfers (deposits, withdrawals, conversions), and submit travel-rule information for one that requires it.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/exchange-api/rest-api/transfers/get-all-transfers)
    """
    return Transfers(client=self.client)

  @cached_property
  def travel_rules(self) -> TravelRules:
    """Stored travel-rule (FATF originator/beneficiary) records on the profile — list, create, and delete them.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/exchange-api/rest-api/travel-rules/get-all-travel-rule)
    """
    return TravelRules(client=self.client)

  @cached_property
  def users(self) -> Users:
    """The calling user's own Exchange account facts — trading volume, trading limits, and updating settlement preferences.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/exchange-api/rest-api/users/get-user-exchange-limits)
    """
    return Users(client=self.client)

  @cached_property
  def withdrawals(self) -> Withdrawals:
    """Withdrawals from an Exchange account — to a crypto address, a counterparty, a linked payment method, or a linked Coinbase.com wallet, plus a network-fee estimate for a crypto withdrawal.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/exchange-api/rest-api/transfers/withdraw-to-crypto-address)
    """
    return Withdrawals(client=self.client)

  @cached_property
  def wrapped_assets(self) -> WrappedAssets:
    """Wrapped-asset (e.g. cbETH) staking — list supported wrapped assets and conversion rates, create/list/look up stake-wraps and redeems.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/exchange-api/rest-api/wrapped-assets/get-all-wrapped-assets)
    """
    return WrappedAssets(client=self.client)
