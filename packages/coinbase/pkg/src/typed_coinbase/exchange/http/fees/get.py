# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class ExchangeFees(TypedDict):
  """Taker and maker fee rates for the caller, and the trailing 30-day volume in USD they're based on."""

  taker_fee_rate: Decimal
  """Taker fee rate."""
  maker_fee_rate: Decimal
  """Maker fee rate."""
  usd_volume: NotRequired[Decimal]
  """The trailing 30-day volume in USD."""


class Get(RpcEndpoint):
  """Get the caller's current maker/taker fee rates and 30-day trailing trading volume."""

  async def get(self, *, validate: bool | None = None) -> ExchangeFees:
    """Get the caller's current maker/taker fee rates and 30-day trailing trading volume.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      None,
      method='GET',
      path='/fees',
      meta={'signed': True},
      validate=validate,
      response_type=ExchangeFees,
    )
