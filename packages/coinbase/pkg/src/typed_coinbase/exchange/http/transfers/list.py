# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeTransfer


class Request(TypedDict):
  profile_id: NotRequired[str]
  """Filter to transfers from this profile id."""
  before: NotRequired[str]
  """Pagination cursor: the `CB-BEFORE` header value from a previous call."""
  after: NotRequired[str]
  """Pagination cursor: the `CB-AFTER` header value from a previous call."""
  limit: NotRequired[int]
  """Limit on number of results to return."""
  type: NotRequired[
    Literal['deposit', 'withdraw', 'internal_deposit', 'internal_withdraw']
  ]
  """Filter to a specific transfer type. Internal transfer types represent transfers made between the caller's own profiles, and are excluded from the response when this field is not set."""
  currency_type: NotRequired[Literal['crypto', 'fiat']]
  """Filter to a category of currency."""
  transfer_reason: NotRequired[Literal['usdc_reward']]
  """Filter to a specific transfer reason."""
  currency: NotRequired[str]
  """Filter to a specific currency code."""


Response = list[ExchangeTransfer]


class List(RpcEndpoint):
  """Get a list of in-progress and completed transfers of funds in/out of any of the caller's accounts."""

  async def list(
    self,
    *,
    profile_id: str | None = None,
    before: str | None = None,
    after: str | None = None,
    limit: int | None = None,
    type: Literal['deposit', 'withdraw', 'internal_deposit', 'internal_withdraw']
    | None = None,
    currency_type: Literal['crypto', 'fiat'] | None = None,
    transfer_reason: Literal['usdc_reward'] | None = None,
    currency: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get a list of in-progress and completed transfers of funds in/out of any of the caller's accounts.

    Args:
      profile_id: Filter to transfers from this profile id.
      before: Pagination cursor: the `CB-BEFORE` header value from a previous call.
      after: Pagination cursor: the `CB-AFTER` header value from a previous call.
      limit: Limit on number of results to return.
      type: Filter to a specific transfer type. Internal transfer types represent transfers made between the caller's own profiles, and are excluded from the response when this field is not set.
      currency_type: Filter to a category of currency.
      transfer_reason: Filter to a specific transfer reason.
      currency: Filter to a specific currency code.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = Request()
    if profile_id is not None:
      request['profile_id'] = profile_id
    if before is not None:
      request['before'] = before
    if after is not None:
      request['after'] = after
    if limit is not None:
      request['limit'] = limit
    if type is not None:
      request['type'] = type
    if currency_type is not None:
      request['currency_type'] = currency_type
    if transfer_reason is not None:
      request['transfer_reason'] = transfer_reason
    if currency is not None:
      request['currency'] = currency
    return await self.request(
      request,
      method='GET',
      path='/transfers',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
