# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeTransfer


class Request(TypedDict):
  transfer_id: str
  """Transfer id to retrieve."""


class Get(RpcEndpoint):
  """Get information on a single transfer."""

  async def get(
    self, transfer_id: str, *, validate: bool | None = None
  ) -> ExchangeTransfer:
    """Get information on a single transfer.

    Args:
      transfer_id: Transfer id to retrieve.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      Request(transfer_id=transfer_id),
      method='GET',
      path='/transfers/{transfer_id}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeTransfer,
    )
