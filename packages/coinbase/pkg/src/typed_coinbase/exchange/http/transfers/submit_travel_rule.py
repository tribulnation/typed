# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeStreetAddress


class ExchangeSubmitTravelRuleResponse(TypedDict):
  """Confirmation of a travel-rule submission."""

  message: NotRequired[str]
  """Confirmation message."""


class Request(TypedDict):
  transfer_id: str
  """Transfer id (redundant with the path parameter)."""
  originator_name: NotRequired[str]
  """Name of the originator of the funds."""
  originator_country: NotRequired[str]
  """ISO 3166-1 alpha-2 country code of the originator of the funds."""
  vasp_id: NotRequired[str]
  """Virtual Asset Service Provider id of the counterparty."""
  vasp_country: NotRequired[str]
  """ISO 3166-1 alpha-2 country code of the counterparty VASP."""
  lei_number: NotRequired[str]
  """Legal Entity Identifier (LEI) of the counterparty VASP."""
  business_address: NotRequired[ExchangeStreetAddress]
  wallet_type: NotRequired[Literal['UNKNOWN_WALLET_TYPE', 'EXCHANGE', 'SELF_HOSTED']]
  """Type of wallet the funds originate from."""
  is_self_certified: NotRequired[bool]
  """True if the caller self-certifies ownership of the originating wallet."""


class SubmitTravelRule(RpcEndpoint):
  """Submit travel-rule (originator) information for a pending transfer."""

  async def submit_travel_rule(
    self,
    *,
    transfer_id: str,
    originator_name: str | None = None,
    originator_country: str | None = None,
    vasp_id: str | None = None,
    vasp_country: str | None = None,
    lei_number: str | None = None,
    business_address: ExchangeStreetAddress | None = None,
    wallet_type: Literal['UNKNOWN_WALLET_TYPE', 'EXCHANGE', 'SELF_HOSTED']
    | None = None,
    is_self_certified: bool | None = None,
    validate: bool | None = None,
  ) -> ExchangeSubmitTravelRuleResponse:
    """Submit travel-rule (originator) information for a pending transfer.

    Args:
      transfer_id: Transfer id (redundant with the path parameter).
      originator_name: Name of the originator of the funds.
      originator_country: ISO 3166-1 alpha-2 country code of the originator of the funds.
      vasp_id: Virtual Asset Service Provider id of the counterparty.
      vasp_country: ISO 3166-1 alpha-2 country code of the counterparty VASP.
      lei_number: Legal Entity Identifier (LEI) of the counterparty VASP.
      business_address:
      wallet_type: Type of wallet the funds originate from.
      is_self_certified: True if the caller self-certifies ownership of the originating wallet.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = Request(transfer_id=transfer_id)
    if originator_name is not None:
      request['originator_name'] = originator_name
    if originator_country is not None:
      request['originator_country'] = originator_country
    if vasp_id is not None:
      request['vasp_id'] = vasp_id
    if vasp_country is not None:
      request['vasp_country'] = vasp_country
    if lei_number is not None:
      request['lei_number'] = lei_number
    if business_address is not None:
      request['business_address'] = business_address
    if wallet_type is not None:
      request['wallet_type'] = wallet_type
    if is_self_certified is not None:
      request['is_self_certified'] = is_self_certified
    return await self.request(
      request,
      method='POST',
      path='/transfers/{transfer_id}/travel-rules',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeSubmitTravelRuleResponse,
    )
