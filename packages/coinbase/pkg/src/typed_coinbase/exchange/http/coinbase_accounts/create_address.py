# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core import TimestampIso
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeCryptoDestination


class ExchangeCoinbaseAccountAddressWarning(TypedDict):
  """A warning about using this deposit address."""

  title: str
  """Warning title."""
  details: str
  """Warning details."""
  image_url: str
  """URL of an image illustrating the warning."""


class ExchangeInlineWarningTooltip(TypedDict):
  """Tooltip content elaborating on the warning."""

  title: NotRequired[str]
  """Tooltip title."""
  subtitle: NotRequired[str]
  """Tooltip subtitle."""


class ExchangeShareAddressCopy(TypedDict):
  """Suggested copy for sharing this address with someone else."""

  line1: NotRequired[str]
  """First line of the suggested share text."""
  line2: NotRequired[str]
  """Second line of the suggested share text."""


class Request(TypedDict):
  account_id: str
  """Coinbase account id to generate a deposit address for."""
  profile_id: NotRequired[str]
  """Generate the address under a specific profile. Defaults to the `default` profile."""
  network: NotRequired[str]
  """Blockchain network to generate the address on, for assets that support more than one network."""


class ExchangeInlineWarning(TypedDict):
  """An inline warning to display alongside this address."""

  text: NotRequired[str]
  """Warning text."""
  tooltip: NotRequired[ExchangeInlineWarningTooltip]


class ExchangeCoinbaseAccountGeneratedDepositAddress(TypedDict):
  """A generated one-time deposit address."""

  id: str
  """Deposit address id."""
  address: str
  """The generated crypto address."""
  address_info: ExchangeCryptoDestination
  name: str
  """Address display name."""
  created_at: TimestampIso
  """Time this address was generated."""
  updated_at: TimestampIso
  """Time this address was last updated."""
  network: str
  """Blockchain network this address is on."""
  uri_scheme: str
  """URI scheme for this address's network (e.g. `bitcoin`, `ethereum`)."""
  resource: str
  """Resource type identifier."""
  resource_path: str
  """API resource path for this address."""
  warnings: list[ExchangeCoinbaseAccountAddressWarning]
  """Warnings to show the caller about this address, if any."""
  legacy_address: NotRequired[str]
  """Legacy address format, for currencies that changed address format (e.g. BTC)."""
  destination_tag: NotRequired[str]
  """Destination tag/memo, for currencies that use one."""
  deposit_uri: NotRequired[str]
  """URI encoding this address as a payment request."""
  callback_url: NotRequired[str]
  """Callback URL associated with this address, if any."""
  qr_code_image_url: NotRequired[str]
  """URL of a QR code image encoding this address."""
  address_label: NotRequired[str]
  """Label shown for this address."""
  share_address_copy: NotRequired[ExchangeShareAddressCopy]
  exchange_deposit_address: NotRequired[bool]
  """Whether this is an Exchange (as opposed to a Coinbase App) deposit address."""
  inline_warning: NotRequired[ExchangeInlineWarning]


class CreateAddress(RpcEndpoint):
  """Generate a one-time crypto address for depositing crypto into a Coinbase wallet."""

  async def create_address(
    self,
    *,
    account_id: str,
    profile_id: str | None = None,
    network: str | None = None,
    validate: bool | None = None,
  ) -> ExchangeCoinbaseAccountGeneratedDepositAddress:
    """Generate a one-time crypto address for depositing crypto into a Coinbase wallet.

    Args:
      account_id: Coinbase account id to generate a deposit address for.
      profile_id: Generate the address under a specific profile. Defaults to the `default` profile.
      network: Blockchain network to generate the address on, for assets that support more than one network.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = Request(account_id=account_id)
    if profile_id is not None:
      request['profile_id'] = profile_id
    if network is not None:
      request['network'] = network
    return await self.request(
      request,
      method='POST',
      path='/coinbase-accounts/{account_id}/addresses',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeCoinbaseAccountGeneratedDepositAddress,
    )
