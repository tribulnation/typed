# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class ExchangeCoinbaseAccount(TypedDict):
  """A single Coinbase wallet/account."""

  id: str
  """Coinbase account id."""
  name: str
  """Account display name."""
  balance: Decimal
  """Account balance."""
  currency: str
  """Account currency."""
  type: Literal['wallet', 'fiat']
  """The kind of Coinbase account."""
  primary: bool
  """Whether this is the caller's primary account for its currency."""
  active: bool
  """Whether this account is active."""
  hold_balance: Decimal
  """Amount of this account's balance currently on hold."""
  hold_currency: str
  """Currency the hold balance is denominated in."""
  destination_tag_name: NotRequired[str]
  """Name of the destination tag/memo field used by this account's currency, if any."""
  destination_tag_regex: NotRequired[str]
  """Validation regex for this account's destination tag/memo field, if any."""


ExchangeCoinbaseAccounts = list[ExchangeCoinbaseAccount]


class List(RpcEndpoint):
  """Get all of the caller's available Coinbase (App) wallets -- these are the wallets/accounts used for buying and selling on www.coinbase.com, distinct from this venue's own Exchange trading accounts."""

  async def list(self, *, validate: bool | None = None) -> ExchangeCoinbaseAccounts:
    """Get all of the caller's available Coinbase (App) wallets -- these are the wallets/accounts used for buying and selling on www.coinbase.com, distinct from this venue's own Exchange trading accounts.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      None,
      method='GET',
      path='/coinbase-accounts',
      meta={'signed': True},
      validate=validate,
      response_type=ExchangeCoinbaseAccounts,
    )
