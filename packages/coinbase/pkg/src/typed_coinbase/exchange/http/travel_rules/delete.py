# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class ExchangeDeleteTravelRuleResponse(TypedDict):
  """Empty confirmation of a travel-rule entry deletion."""


class Request(TypedDict):
  id: str
  """Travel-rule entry id to delete."""


class Delete(RpcEndpoint):
  """Delete an existing travel-rule information entry."""

  async def delete(
    self,
    id: str,
    *,
    validate: bool | None = None,
  ) -> ExchangeDeleteTravelRuleResponse:
    """Delete an existing travel-rule information entry.

    Args:
      id: Travel-rule entry id to delete.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      Request(id=id),
      method='DELETE',
      path='/travel-rules/{id}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeDeleteTravelRuleResponse,
    )
