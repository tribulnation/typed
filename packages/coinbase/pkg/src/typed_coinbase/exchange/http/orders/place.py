# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeOrder


class ExchangeLimitOrderRequest(TypedDict):
  """Place a limit order: rests on the book at a fixed price until filled, canceled, or (with a time-in-force) expired."""

  type: Literal['limit']
  """Always `limit`."""
  side: Literal['buy', 'sell']
  """Order side."""
  product_id: str
  """The product to place this order on."""
  price: Decimal
  """Limit price per unit of base currency."""
  size: Decimal
  """Amount of base currency to buy or sell."""
  time_in_force: NotRequired[Literal['GTC', 'GTT', 'IOC', 'FOK']]
  """Time-in-force policy."""
  cancel_after: NotRequired[Literal['min', 'hour', 'day']]
  """For a `GTT` time-in-force order, when it is automatically canceled."""
  post_only: NotRequired[bool]
  """If true, this order only executes as a `maker` order (rejected if it would immediately match)."""
  max_floor: NotRequired[Decimal]
  """For an iceberg order, the visible size shown on the book."""
  profile_id: NotRequired[str]
  """Place the order under a specific profile. Defaults to the `default` profile."""
  stp: NotRequired[Literal['dc', 'co', 'cn', 'cb']]
  """Self-trade prevention flag."""
  client_oid: NotRequired[str]
  """Caller-chosen order id, used to identify the order before the venue assigns its own `id`."""


class ExchangeMarketOrderRequest(TypedDict):
  """Place a market order: executes immediately at the best available price. A buy is denominated by `funds` (quote currency to spend); a sell is denominated by `size` (base currency to sell) -- the venue documents both as conditionally required by `side` rather than as two separate order types, so both stay optional here with that conditional stated in prose (docs/spec/authoring.md rule 0's discriminated-union guidance targets the primary `limit`/`market`/`stop` split; splitting further by `side` was judged not to earn a fourth variant)."""

  type: Literal['market']
  """Always `market`."""
  side: Literal['buy', 'sell']
  """Order side."""
  product_id: str
  """The product to place this order on."""
  size: NotRequired[Decimal]
  """Amount of base currency to buy or sell -- required for a `sell`."""
  funds: NotRequired[Decimal]
  """Amount of quote currency to spend -- required for a `buy`."""
  profile_id: NotRequired[str]
  """Place the order under a specific profile. Defaults to the `default` profile."""
  stp: NotRequired[Literal['dc', 'co', 'cn', 'cb']]
  """Self-trade prevention flag."""
  client_oid: NotRequired[str]
  """Caller-chosen order id, used to identify the order before the venue assigns its own `id`."""


class ExchangeStopOrderRequest(TypedDict):
  """Place a stop order: rests inactive until the market price crosses `stop_price`, then activates as a limit order at `price` (or, for a take-profit/stop-loss order, at `stop_limit_price`)."""

  type: Literal['stop']
  """Always `stop`."""
  side: Literal['buy', 'sell']
  """Order side."""
  product_id: str
  """The product to place this order on."""
  stop: Literal['loss', 'entry']
  """Which side of the book this stop triggers relative to."""
  stop_price: Decimal
  """Price (quote currency) that activates this order."""
  price: Decimal
  """Limit price once activated."""
  size: Decimal
  """Amount of base currency to buy or sell."""
  stop_limit_price: NotRequired[Decimal]
  """For a take-profit/stop-loss order, the limit price to use once the stop price triggers, in place of `price`."""
  time_in_force: NotRequired[Literal['GTC', 'GTT', 'IOC', 'FOK']]
  """Time-in-force policy, applied once the order activates."""
  cancel_after: NotRequired[Literal['min', 'hour', 'day']]
  """For a `GTT` time-in-force order, when it is automatically canceled."""
  profile_id: NotRequired[str]
  """Place the order under a specific profile. Defaults to the `default` profile."""
  stp: NotRequired[Literal['dc', 'co', 'cn', 'cb']]
  """Self-trade prevention flag."""
  client_oid: NotRequired[str]
  """Caller-chosen order id, used to identify the order before the venue assigns its own `id`."""


Request = (
  ExchangeLimitOrderRequest | ExchangeMarketOrderRequest | ExchangeStopOrderRequest
)


class Place(RpcEndpoint):
  """Place a new order: `limit`, `market`, or `stop`. Requires sufficient available funds; a successful call holds the necessary funds for the duration of the order."""

  async def place(
    self,
    order_request: Request,
    *,
    validate: bool | None = None,
  ) -> ExchangeOrder:
    """Place a new order: `limit`, `market`, or `stop`. Requires sufficient available funds; a successful call holds the necessary funds for the duration of the order.

    Args:
      order_request: The order to place.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      order_request,
      method='POST',
      path='/orders',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeOrder,
    )
