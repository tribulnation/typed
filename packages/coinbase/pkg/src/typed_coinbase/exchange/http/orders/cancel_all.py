# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  profile_id: NotRequired[str]
  """Cancel orders on a specific profile only."""
  product_id: NotRequired[str]
  """Cancel orders on a specific product only."""


Response = list[str]


class CancelAll(RpcEndpoint):
  """With best effort, cancel every open order. May require repeating the call until every open order is gone."""

  async def cancel_all(
    self,
    *,
    profile_id: str | None = None,
    product_id: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """With best effort, cancel every open order. May require repeating the call until every open order is gone.

    Args:
      profile_id: Cancel orders on a specific profile only.
      product_id: Cancel orders on a specific product only.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = Request()
    if profile_id is not None:
      request['profile_id'] = profile_id
    if product_id is not None:
      request['product_id'] = product_id
    return await self.request(
      request,
      method='DELETE',
      path='/orders',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
