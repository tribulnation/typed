# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeOrder


class Request(TypedDict):
  order_id: str
  """The venue-assigned order id, or the caller's own `client_oid` prefixed with `client:` (e.g. `client:496ffbba-...`)."""
  market_type: NotRequired[str]
  """Market type the order was traded in."""


class Get(RpcEndpoint):
  """Get a single order by id."""

  async def get(
    self,
    *,
    order_id: str,
    market_type: str | None = None,
    validate: bool | None = None,
  ) -> ExchangeOrder:
    """Get a single order by id.

    Args:
      order_id: The venue-assigned order id, or the caller's own `client_oid` prefixed with `client:` (e.g. `client:496ffbba-...`).
      market_type: Market type the order was traded in.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = Request(order_id=order_id)
    if market_type is not None:
      request['market_type'] = market_type
    return await self.request(
      request,
      method='GET',
      path='/orders/{order_id}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeOrder,
    )
