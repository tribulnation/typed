# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  order_id: str
  """The venue-assigned order id, or the caller's own `client_oid` prefixed with `client:`."""
  profile_id: NotRequired[str]
  """Cancel an order on a specific profile."""
  product_id: NotRequired[str]
  """Product the order was placed on. Speeds up cancellation when provided."""


Response = str


class Cancel(RpcEndpoint):
  """Cancel a single open order by id."""

  async def cancel(
    self,
    *,
    order_id: str,
    profile_id: str | None = None,
    product_id: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Cancel a single open order by id.

    Args:
      order_id: The venue-assigned order id, or the caller's own `client_oid` prefixed with `client:`.
      profile_id: Cancel an order on a specific profile.
      product_id: Product the order was placed on. Speeds up cancellation when provided.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = Request(order_id=order_id)
    if profile_id is not None:
      request['profile_id'] = profile_id
    if product_id is not None:
      request['product_id'] = product_id
    return await self.request(
      request,
      method='DELETE',
      path='/orders/{order_id}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
