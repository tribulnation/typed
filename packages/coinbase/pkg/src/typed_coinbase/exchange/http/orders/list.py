# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_coinbase.core import TimestampIso
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeOrder


class Request(TypedDict):
  profile_id: NotRequired[str]
  """Filter to a specific profile."""
  product_id: NotRequired[str]
  """Filter to a specific product."""
  sortedBy: NotRequired[
    Literal['created_at', 'price', 'size', 'order_id', 'side', 'type']
  ]
  """Field to sort results by."""
  sorting: NotRequired[Literal['desc', 'asc']]
  """Sort direction."""
  start_date: NotRequired[TimestampIso]
  """Filter to orders posted at or after this time."""
  end_date: NotRequired[TimestampIso]
  """Filter to orders posted at or before this time."""
  before: NotRequired[str]
  """Pagination cursor: the `CB-BEFORE` header value from a previous call."""
  after: NotRequired[str]
  """Pagination cursor: the `CB-AFTER` header value from a previous call."""
  limit: int
  """Number of results to return."""
  status: list[
    Literal['open', 'pending', 'rejected', 'done', 'active', 'received', 'all']
  ]
  """Order statuses to include."""
  market_type: NotRequired[str]
  """Market type the order was traded in."""


Response = list[ExchangeOrder]


class List(RpcEndpoint):
  """List the caller's current open orders. Only open or unsettled orders are returned by default; once an order is no longer open and is settled, it stops appearing here."""

  async def list(
    self,
    *,
    profile_id: str | None = None,
    product_id: str | None = None,
    sorted_by: Literal['created_at', 'price', 'size', 'order_id', 'side', 'type']
    | None = None,
    sorting: Literal['desc', 'asc'] | None = None,
    start_date: TimestampIso | None = None,
    end_date: TimestampIso | None = None,
    before: str | None = None,
    after: str | None = None,
    limit: int,
    status: list[
      Literal['open', 'pending', 'rejected', 'done', 'active', 'received', 'all']
    ],
    market_type: str | None = None,
    validate: bool | None = None,
  ) -> Response:
    """List the caller's current open orders. Only open or unsettled orders are returned by default; once an order is no longer open and is settled, it stops appearing here.

    Args:
      profile_id: Filter to a specific profile.
      product_id: Filter to a specific product.
      sorted_by: Field to sort results by.
      sorting: Sort direction.
      start_date: Filter to orders posted at or after this time.
      end_date: Filter to orders posted at or before this time.
      before: Pagination cursor: the `CB-BEFORE` header value from a previous call.
      after: Pagination cursor: the `CB-AFTER` header value from a previous call.
      limit: Number of results to return.
      status: Order statuses to include.
      market_type: Market type the order was traded in.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = Request(limit=limit, status=status)
    if profile_id is not None:
      request['profile_id'] = profile_id
    if product_id is not None:
      request['product_id'] = product_id
    if sorted_by is not None:
      request['sortedBy'] = sorted_by
    if sorting is not None:
      request['sorting'] = sorting
    if start_date is not None:
      request['start_date'] = start_date
    if end_date is not None:
      request['end_date'] = end_date
    if before is not None:
      request['before'] = before
    if after is not None:
      request['after'] = after
    if market_type is not None:
      request['market_type'] = market_type
    return await self.request(
      request,
      method='GET',
      path='/orders',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
