# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_coinbase.core import TimestampIso
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class ExchangeFill(TypedDict):
  """A partial or complete match on an order."""

  trade_id: int
  """Id of the trade that created this fill."""
  product_id: str
  """The product this fill occurred on."""
  order_id: str
  """The filled order's id."""
  user_id: str
  """The order owner's user id."""
  profile_id: str
  """The profile that placed the order."""
  liquidity: Literal['M', 'T', 'O']
  """Whether this fill was the maker, taker, or an other-liquidity leg."""
  price: Decimal
  """Fill price per unit of base currency."""
  size: Decimal
  """Fill size, in base currency."""
  fee: Decimal
  """Fee paid on this fill."""
  created_at: TimestampIso
  """Time of this fill."""
  side: Literal['buy', 'sell']
  """The order's side."""
  settled: bool
  """Whether matched funds have been exchanged and settled."""
  usd_volume: Decimal
  """Fill value in USD."""
  market_type: NotRequired[str]
  """Market type the order was filled in, e.g. `spot`. Not documented as an exhaustive enumeration."""
  funding_currency: str
  """Funding currency for the order that produced this fill."""


class Request(TypedDict):
  order_id: NotRequired[str]
  """Limit to fills on a specific order. Either this or `product_id` is required."""
  product_id: NotRequired[str]
  """Limit to fills on a specific product. Either this or `order_id` is required."""
  limit: NotRequired[int]
  """Number of results to return."""
  before: NotRequired[str]
  """Pagination cursor: the `CB-BEFORE` header value from a previous call."""
  after: NotRequired[str]
  """Pagination cursor: the `CB-AFTER` header value from a previous call."""
  market_type: NotRequired[Literal['spot', 'rfq']]
  """Market type the fill occurred in."""
  start_date: NotRequired[TimestampIso]
  """Filter to fills at or after this time. Accepts RFC 3339 date or date-time."""
  end_date: NotRequired[TimestampIso]
  """Filter to fills at or before this time. Accepts RFC 3339 date or date-time."""


Response = list[ExchangeFill]


class List(RpcEndpoint):
  """List fills: a fill is a partial or complete match on a specific order. Either `order_id` or `product_id` is required."""

  async def list(
    self,
    *,
    order_id: str | None = None,
    product_id: str | None = None,
    limit: int | None = None,
    before: str | None = None,
    after: str | None = None,
    market_type: Literal['spot', 'rfq'] | None = None,
    start_date: TimestampIso | None = None,
    end_date: TimestampIso | None = None,
    validate: bool | None = None,
  ) -> Response:
    """List fills: a fill is a partial or complete match on a specific order. Either `order_id` or `product_id` is required.

    Args:
      order_id: Limit to fills on a specific order. Either this or `product_id` is required.
      product_id: Limit to fills on a specific product. Either this or `order_id` is required.
      limit: Number of results to return.
      before: Pagination cursor: the `CB-BEFORE` header value from a previous call.
      after: Pagination cursor: the `CB-AFTER` header value from a previous call.
      market_type: Market type the fill occurred in.
      start_date: Filter to fills at or after this time. Accepts RFC 3339 date or date-time.
      end_date: Filter to fills at or before this time. Accepts RFC 3339 date or date-time.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = Request()
    if order_id is not None:
      request['order_id'] = order_id
    if product_id is not None:
      request['product_id'] = product_id
    if limit is not None:
      request['limit'] = limit
    if before is not None:
      request['before'] = before
    if after is not None:
      request['after'] = after
    if market_type is not None:
      request['market_type'] = market_type
    if start_date is not None:
      request['start_date'] = start_date
    if end_date is not None:
      request['end_date'] = end_date
    return await self.request(
      request,
      method='GET',
      path='/fills',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
