# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Any, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


ExchangeProfileTransferResponse = dict[str, Any]


RequestKeywords = TypedDict('RequestKeywords', {'from': str})
"""
- `from`: Profile id to transfer funds from.
"""


class Request(RequestKeywords):
  """Request to transfer funds between two of the caller's profiles."""

  to: str
  """Profile id to transfer funds to."""
  currency: str
  """Currency to transfer, e.g. `BTC`."""
  amount: Decimal
  """Amount to transfer."""


class Transfer(RpcEndpoint):
  """Transfer an amount of a currency from one of the caller's profiles to another."""

  async def transfer(
    self,
    *,
    from_: str,
    to: str,
    currency: str,
    amount: Decimal,
    validate: bool | None = None,
  ) -> ExchangeProfileTransferResponse:
    """Transfer an amount of a currency from one of the caller's profiles to another.

    Args:
      from_: Profile id to transfer funds from.
      to: Profile id to transfer funds to.
      currency: Currency to transfer, e.g. `BTC`.
      amount: Amount to transfer.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = {'from': from_, 'to': to, 'currency': currency, 'amount': amount}
    return await self.request(
      request,
      method='POST',
      path='/profiles/transfer',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeProfileTransferResponse,
    )
