# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeProfile


class Request(TypedDict):
  active: NotRequired[bool]
  """Filter to only active (`true`) or only inactive (`false`) profiles. Omit to return both."""


Response = list[ExchangeProfile]


class List(RpcEndpoint):
  """Get a list of all of the caller's profiles."""

  async def list(
    self,
    active: bool | None = None,
    *,
    validate: bool | None = None,
  ) -> Response:
    """Get a list of all of the caller's profiles.

    Args:
      active: Filter to only active (`true`) or only inactive (`false`) profiles. Omit to return both.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = Request()
    if active is not None:
      request['active'] = active
    return await self.request(
      request,
      method='GET',
      path='/profiles',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
