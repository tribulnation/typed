# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeProfile


class Request(TypedDict):
  """Request to create a new profile."""

  name: str
  """Name for the new profile."""


class Create(RpcEndpoint):
  """Create a new profile. Fails if no name is provided or if the caller already has the maximum number of profiles."""

  async def create(self, name: str, *, validate: bool | None = None) -> ExchangeProfile:
    """Create a new profile. Fails if no name is provided or if the caller already has the maximum number of profiles.

    Args:
      name: Name for the new profile.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      Request(name=name),
      method='POST',
      path='/profiles',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeProfile,
    )
