# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


ExchangeProfileDeactivateResponse = dict[str, Any]


class Request(TypedDict):
  profile_id: str
  """Profile id to deactivate. Must match the `profile_id` path parameter."""
  to: str
  """Profile id to receive the deactivated profile's funds."""


class Deactivate(RpcEndpoint):
  """Delete the profile specified by `profile_id`, transferring all its funds to the profile specified by `to`. Fails if the profile to delete has any open orders."""

  async def deactivate(
    self,
    *,
    profile_id: str,
    to: str,
    validate: bool | None = None,
  ) -> ExchangeProfileDeactivateResponse:
    """Delete the profile specified by `profile_id`, transferring all its funds to the profile specified by `to`. Fails if the profile to delete has any open orders.

    Args:
      profile_id: Profile id to deactivate. Must match the `profile_id` path parameter.
      to: Profile id to receive the deactivated profile's funds.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      Request(profile_id=profile_id, to=to),
      method='PUT',
      path='/profiles/{profile_id}/deactivate',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeProfileDeactivateResponse,
    )
