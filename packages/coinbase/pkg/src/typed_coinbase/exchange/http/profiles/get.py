# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeProfile


class Request(TypedDict):
  profile_id: str
  """Profile id to fetch."""
  active: NotRequired[bool]
  """Filter to only an active (`true`) or only an inactive (`false`) profile. Omit to return the profile regardless of its active state."""


class Get(RpcEndpoint):
  """Get a single profile by `profile_id`."""

  async def get(
    self,
    profile_id: str,
    *,
    active: bool | None = None,
    validate: bool | None = None,
  ) -> ExchangeProfile:
    """Get a single profile by `profile_id`.

    Args:
      profile_id: Profile id to fetch.
      active: Filter to only an active (`true`) or only an inactive (`false`) profile. Omit to return the profile regardless of its active state.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = Request(profile_id=profile_id)
    if active is not None:
      request['active'] = active
    return await self.request(
      request,
      method='GET',
      path='/profiles/{profile_id}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeProfile,
    )
