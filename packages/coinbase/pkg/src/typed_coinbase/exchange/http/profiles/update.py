# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeProfile


class Request(TypedDict):
  profile_id: str
  """Profile id to rename. Must match the `profile_id` path parameter."""
  name: str
  """New name for the profile."""


class Update(RpcEndpoint):
  """Rename a profile. Names `default` and `margin` are reserved."""

  async def update(
    self,
    *,
    profile_id: str,
    name: str,
    validate: bool | None = None,
  ) -> ExchangeProfile:
    """Rename a profile. Names `default` and `margin` are reserved.

    Args:
      profile_id: Profile id to rename. Must match the `profile_id` path parameter.
      name: New name for the profile.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      Request(profile_id=profile_id, name=name),
      method='PUT',
      path='/profiles/{profile_id}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeProfile,
    )
