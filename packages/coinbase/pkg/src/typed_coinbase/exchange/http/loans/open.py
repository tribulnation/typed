# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core import TimestampIso
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeLoan


class Request(TypedDict):
  """A request to open a new loan."""

  loan_id: NotRequired[str]
  """Caller-chosen id for this loan."""
  currency: str
  """Currency to borrow."""
  native_amount: Decimal
  """Principal amount to borrow, in `currency`'s native units."""
  interest_rate: NotRequired[Decimal]
  """Interest rate to open the loan at, typically the rate most recently quoted by `loans.options`/`loans.loan_preview`."""
  term_start_date: NotRequired[TimestampIso]
  """Start of this loan's term."""
  term_end_date: NotRequired[TimestampIso]
  """End of this loan's term."""
  profile_id: NotRequired[str]
  """Open the loan under a specific profile. Defaults to the `default` profile."""


class ExchangeOpenLoanResponse(TypedDict):
  """The result of opening a new loan."""

  loan: ExchangeLoan


class Open(RpcEndpoint):
  """Trigger a loan open request. Funding is not necessarily instantaneous and there is no SLA; the caller is notified once funds have settled in their Exchange account. Loan open requests, once initiated, cannot be canceled."""

  async def open(
    self,
    *,
    loan_id: str | None = None,
    currency: str,
    native_amount: Decimal,
    interest_rate: Decimal | None = None,
    term_start_date: TimestampIso | None = None,
    term_end_date: TimestampIso | None = None,
    profile_id: str | None = None,
    validate: bool | None = None,
  ) -> ExchangeOpenLoanResponse:
    """Trigger a loan open request. Funding is not necessarily instantaneous and there is no SLA; the caller is notified once funds have settled in their Exchange account. Loan open requests, once initiated, cannot be canceled.

    Args:
      loan_id: Caller-chosen id for this loan.
      currency: Currency to borrow.
      native_amount: Principal amount to borrow, in `currency`'s native units.
      interest_rate: Interest rate to open the loan at, typically the rate most recently quoted by `loans.options`/`loans.loan_preview`.
      term_start_date: Start of this loan's term.
      term_end_date: End of this loan's term.
      profile_id: Open the loan under a specific profile. Defaults to the `default` profile.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = Request(currency=currency, native_amount=native_amount)
    if loan_id is not None:
      request['loan_id'] = loan_id
    if interest_rate is not None:
      request['interest_rate'] = interest_rate
    if term_start_date is not None:
      request['term_start_date'] = term_start_date
    if term_end_date is not None:
      request['term_end_date'] = term_end_date
    if profile_id is not None:
      request['profile_id'] = profile_id
    return await self.request(
      request,
      method='POST',
      path='/loans/open',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeOpenLoanResponse,
    )
