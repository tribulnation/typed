# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core import TimestampIso
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class ExchangeInterestCharge(TypedDict):
  """A single interest accrual charged against a loan."""

  date: TimestampIso
  """Date this interest was accrued."""
  currency: str
  """Currency this interest accrued in."""
  principal_amount: Decimal
  """Outstanding principal amount at the time this interest was accrued."""
  interest_rate: Decimal
  """Interest rate applied for this accrual."""
  interest_accrued: Decimal
  """Interest amount accrued on `date`."""
  accrual_price: NotRequired[Decimal]
  """Price of the loan's principal asset (e.g. BTC) denominated in the interest currency (e.g. USDC) at the time interest was accrued. Absent if not available for this accrual."""


class Request(TypedDict):
  loan_id: str
  """Loan to list interest charges for."""


Response = list[ExchangeInterestCharge]


class InterestGet(RpcEndpoint):
  """List interest charges for a loan."""

  async def interest_get(
    self, loan_id: str, *, validate: bool | None = None
  ) -> Response:
    """List interest charges for a loan.

    Args:
      loan_id: Loan to list interest charges for.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      Request(loan_id=loan_id),
      method='GET',
      path='/loans/interest/{loan_id}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
