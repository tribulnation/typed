# Generated by typed-dev — see CONTRIBUTING.md.
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeLendingOverviewResult


class LendingOverview(RpcEndpoint):
  """Summarize lending for the caller: the overall loan balance, collateral level, and amounts available to borrow, plus any withdrawal restrictions in force on the caller."""

  async def lending_overview(
    self,
    *,
    validate: bool | None = None,
  ) -> ExchangeLendingOverviewResult:
    """Summarize lending for the caller: the overall loan balance, collateral level, and amounts available to borrow, plus any withdrawal restrictions in force on the caller.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      None,
      method='GET',
      path='/loans/lending-overview',
      meta={'signed': True},
      validate=validate,
      response_type=ExchangeLendingOverviewResult,
    )
