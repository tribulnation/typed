# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeLoanPreview


class Request(TypedDict):
  currency: NotRequired[str]
  """Currency of the hypothetical new loan to preview."""
  native_amount: NotRequired[Decimal]
  """Principal amount of the hypothetical new loan, in `currency`'s native units."""


class LoanPreview(RpcEndpoint):
  """Preview the results of opening a new loan, without actually opening one. The response accounts for existing loans and collateral plus the hypothetical change being previewed. The preview accepts a native currency amount as input."""

  async def loan_preview(
    self,
    currency: str | None = None,
    *,
    native_amount: Decimal | None = None,
    validate: bool | None = None,
  ) -> ExchangeLoanPreview:
    """Preview the results of opening a new loan, without actually opening one. The response accounts for existing loans and collateral plus the hypothetical change being previewed. The preview accepts a native currency amount as input.

    Args:
      currency: Currency of the hypothetical new loan to preview.
      native_amount: Principal amount of the hypothetical new loan, in `currency`'s native units.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = Request()
    if currency is not None:
      request['currency'] = currency
    if native_amount is not None:
      request['native_amount'] = native_amount
    return await self.request(
      request,
      method='GET',
      path='/loans/loan-preview',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeLoanPreview,
    )
