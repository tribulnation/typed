# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeLoanPreview


class Request(TypedDict):
  loan_id: NotRequired[str]
  """Loan to preview a principal repayment for."""
  currency: NotRequired[str]
  """Currency of the hypothetical repayment."""
  native_amount: NotRequired[Decimal]
  """Repayment amount, in `currency`'s native units."""


class RepaymentPreview(RpcEndpoint):
  """Preview the results of a loan principal repayment."""

  async def repayment_preview(
    self,
    *,
    loan_id: str | None = None,
    currency: str | None = None,
    native_amount: Decimal | None = None,
    validate: bool | None = None,
  ) -> ExchangeLoanPreview:
    """Preview the results of a loan principal repayment.

    Args:
      loan_id: Loan to preview a principal repayment for.
      currency: Currency of the hypothetical repayment.
      native_amount: Repayment amount, in `currency`'s native units.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = Request()
    if loan_id is not None:
      request['loan_id'] = loan_id
    if currency is not None:
      request['currency'] = currency
    if native_amount is not None:
      request['native_amount'] = native_amount
    return await self.request(
      request,
      method='GET',
      path='/loans/repayment-preview',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeLoanPreview,
    )
