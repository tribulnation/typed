# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeAmount


class ExchangeNewLoanOption(TypedDict):
  """A currency currently available to borrow, with its current rate and available inventory."""

  currency: str
  """Currency available to borrow."""
  max_principal_amount: ExchangeAmount
  interest_rate: Decimal
  """Interest rate that would apply to a new loan in this currency."""
  available_inventory: ExchangeAmount


Response = list[ExchangeNewLoanOption]


class Options(RpcEndpoint):
  """Get available currencies and interest rates for new loans."""

  async def options(self, *, validate: bool | None = None) -> Response:
    """Get available currencies and interest rates for new loans.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      None,
      method='GET',
      path='/loans/options',
      meta={'signed': True},
      validate=validate,
      response_type=Response,
    )
