# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeLoanPreviewXM


class Request(TypedDict):
  loan_id: str
  """Loan to preview a principal repayment for."""
  currency: str
  """Currency of the hypothetical repayment."""
  native_amount: Decimal
  """Repayment amount, in `currency`'s native units."""


class RepaymentPreviewXm(RpcEndpoint):
  """Preview the results of a loan principal repayment for a cross-margin (portfolio-margined) account."""

  async def repayment_preview_xm(
    self,
    *,
    loan_id: str,
    currency: str,
    native_amount: Decimal,
    validate: bool | None = None,
  ) -> ExchangeLoanPreviewXM:
    """Preview the results of a loan principal repayment for a cross-margin (portfolio-margined) account.

    Args:
      loan_id: Loan to preview a principal repayment for.
      currency: Currency of the hypothetical repayment.
      native_amount: Repayment amount, in `currency`'s native units.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      Request(loan_id=loan_id, currency=currency, native_amount=native_amount),
      method='GET',
      path='/loans/repayment-preview-xm',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeLoanPreviewXM,
    )
