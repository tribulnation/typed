# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeLendingOverviewXMResult


class Request(TypedDict):
  user_id: NotRequired[str]
  """Look up the lending overview for a specific user. Admin-scoped credentials only; omit to use the caller's own account."""


class ExchangeLendingOverviewXMResponse(TypedDict):
  """The result of a cross-margin lending overview lookup."""

  body: ExchangeLendingOverviewXMResult


class LendingOverviewXm(RpcEndpoint):
  """Get lending overview for a cross-margin (portfolio-margined) account, with margin requirement and account equity information."""

  async def lending_overview_xm(
    self,
    user_id: str | None = None,
    *,
    validate: bool | None = None,
  ) -> ExchangeLendingOverviewXMResponse:
    """Get lending overview for a cross-margin (portfolio-margined) account, with margin requirement and account equity information.

    Args:
      user_id: Look up the lending overview for a specific user. Admin-scoped credentials only; omit to use the caller's own account.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = Request()
    if user_id is not None:
      request['user_id'] = user_id
    return await self.request(
      request,
      method='GET',
      path='/loans/lending-overview-xm',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeLendingOverviewXMResponse,
    )
