# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeLoan


class Request(TypedDict):
  ids: NotRequired[str]
  """Loan ids to filter to, comma-separated. Returns every loan for the caller when omitted."""


Response = list[ExchangeLoan]


class List(RpcEndpoint):
  """List the caller's loans. Accepts zero or more loan ids as input; if none are specified, every loan for the caller is returned, otherwise only the loans specified."""

  async def list(
    self, ids: str | None = None, *, validate: bool | None = None
  ) -> Response:
    """List the caller's loans. Accepts zero or more loan ids as input; if none are specified, every loan for the caller is returned, otherwise only the loans specified.

    Args:
      ids: Loan ids to filter to, comma-separated. Returns every loan for the caller when omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = Request()
    if ids is not None:
      request['ids'] = ids
    return await self.request(
      request,
      method='GET',
      path='/loans',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
