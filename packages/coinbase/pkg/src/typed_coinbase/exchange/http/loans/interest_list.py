# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_coinbase.core import TimestampIso
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class ExchangeInterestSummary(TypedDict):
  """A summary of interest owed on a single asset."""

  currency: str
  """Asset this interest summary applies to."""
  current_owed: Decimal
  """Amount accrued in the current month and not yet due."""
  last_payment_date: NotRequired[TimestampIso]
  """Time of the most recent interest payment. Absent if no payment has been made yet."""
  payment_status: Literal[
    'STATUS_UNSPECIFIED', 'PAID_PAYMENT', 'PAYABLE_PAYMENT', 'OVERDUE_PAYMENT'
  ]
  """Current payment status for this period's interest."""
  last_payment_amount: NotRequired[Decimal]
  """Amount of the most recent interest payment. Absent if no payment has been made yet."""
  prior_period_overdue: Decimal
  """Interest payable that accrued in previous months and is now overdue."""
  current_interest_due_date: TimestampIso
  """Date the current period's interest is due."""


Response = list[ExchangeInterestSummary]


class InterestList(RpcEndpoint):
  """List summaries of interest owed, broken out by asset."""

  async def interest_list(self, *, validate: bool | None = None) -> Response:
    """List summaries of interest owed, broken out by asset.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      None,
      method='GET',
      path='/loans/interest',
      meta={'signed': True},
      validate=validate,
      response_type=Response,
    )
