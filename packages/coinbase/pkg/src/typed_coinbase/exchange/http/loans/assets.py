# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class ExchangeLoanAssets(TypedDict):
  """Assets available to lend, and assets accepted as loan collateral."""

  collateral_assets: dict[str, Decimal]
  """Map of collateral asset code to its haircut weight -- the fraction of that asset's market value counted toward available collateral."""
  diversification_ratio: Decimal
  """Minimum fraction of posted collateral value that must come from more than a single asset."""
  borrowable_assets: list[str]
  """Currencies that can currently be borrowed via a new loan."""


class Assets(RpcEndpoint):
  """Get a list of lendable assets, a map of assets accepted as collateral for loans, and each collateral asset's haircut weight."""

  async def assets(self, *, validate: bool | None = None) -> ExchangeLoanAssets:
    """Get a list of lendable assets, a map of assets accepted as collateral for loans, and each collateral asset's haircut weight.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      None,
      method='GET',
      path='/loans/assets',
      meta={'signed': True},
      validate=validate,
      response_type=ExchangeLoanAssets,
    )
