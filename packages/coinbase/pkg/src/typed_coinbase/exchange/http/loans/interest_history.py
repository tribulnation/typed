# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_coinbase.core import TimestampIso
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class ExchangeInterestRate(TypedDict):
  """An interest rate that took effect on a loan at a point in time."""

  interest_rate: Decimal
  """Interest rate that took effect at `effective_at`."""
  effective_at: TimestampIso
  """Time this interest rate took effect."""


class Request(TypedDict):
  loan_id: str
  """Loan to list interest rate history for."""


Response = list[ExchangeInterestRate]


class InterestHistory(RpcEndpoint):
  """List interest rate history for a loan."""

  async def interest_history(
    self, loan_id: str, *, validate: bool | None = None
  ) -> Response:
    """List interest rate history for a loan.

    Args:
      loan_id: Loan to list interest rate history for.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      Request(loan_id=loan_id),
      method='GET',
      path='/loans/interest/history/{loan_id}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
