# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeLoanPreviewXM


class Request(TypedDict):
  currency: str
  """Currency of the hypothetical new loan to preview."""
  native_amount: Decimal
  """Principal amount of the hypothetical new loan, in `currency`'s native units."""


class LoanPreviewXm(RpcEndpoint):
  """Preview the results of opening a new loan for a cross-margin (portfolio-margined) account."""

  async def loan_preview_xm(
    self,
    currency: str,
    *,
    native_amount: Decimal,
    validate: bool | None = None,
  ) -> ExchangeLoanPreviewXM:
    """Preview the results of opening a new loan for a cross-margin (portfolio-margined) account.

    Args:
      currency: Currency of the hypothetical new loan to preview.
      native_amount: Principal amount of the hypothetical new loan, in `currency`'s native units.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      Request(currency=currency, native_amount=native_amount),
      method='GET',
      path='/loans/loan-preview-xm',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeLoanPreviewXM,
    )
