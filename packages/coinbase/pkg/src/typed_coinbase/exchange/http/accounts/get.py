# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeAccount


class Request(TypedDict):
  account_id: str
  """The account to look up."""


class Get(RpcEndpoint):
  """Information for a single account. Use this endpoint when you know the account_id. The API key must belong to the same profile as the account."""

  async def get(
    self, account_id: str, *, validate: bool | None = None
  ) -> ExchangeAccount:
    """Information for a single account. Use this endpoint when you know the account_id. The API key must belong to the same profile as the account.

    Args:
      account_id: The account to look up.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      Request(account_id=account_id),
      method='GET',
      path='/accounts/{account_id}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeAccount,
    )
