# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class ExchangeUserTransferLimitForCurrency(TypedDict):
  """Transfer limit for one currency."""

  period_in_days: int
  """Length, in days, of the rolling period this limit applies to."""
  remaining: Decimal
  """Amount remaining under this limit for the current period."""
  max: Decimal
  """Maximum amount allowed per period."""


class Request(TypedDict):
  user_id: str
  """Id of the user to fetch limits for."""


class ExchangeUserTransferLimitsEntry(TypedDict):
  """The user's transfer limits, by direction."""

  exchange_withdraw: dict[str, ExchangeUserTransferLimitForCurrency]
  """Exchange-withdraw limits, keyed by currency code."""


class ExchangeUserTransferLimits(TypedDict):
  """A user's exchange (transfer) limits."""

  transfer_limits: ExchangeUserTransferLimitsEntry
  limit_currency: str
  """Currency the limits are denominated in."""


class ExchangeLimits(RpcEndpoint):
  """Get exchange (transfer) limits information for a single user."""

  async def exchange_limits(
    self,
    user_id: str,
    *,
    validate: bool | None = None,
  ) -> ExchangeUserTransferLimits:
    """Get exchange (transfer) limits information for a single user.

    Args:
      user_id: Id of the user to fetch limits for.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      Request(user_id=user_id),
      method='GET',
      path='/users/{user_id}/exchange-limits',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeUserTransferLimits,
    )
