# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class ExchangeSettlementPreference(TypedDict):
  """A user's settlement preference."""

  settlement_preference: NotRequired[Literal['USD', 'USDC', 'opt-out']]
  """Settlement preference."""


class Request(TypedDict):
  user_id: str
  """Id of the user who owns the account, or `self` for the caller."""
  settlement_preference: NotRequired[Literal['USD', 'USDC', 'opt-out']]
  """Settlement preference."""


class SetSettlementPreferences(RpcEndpoint):
  """Update the user-level settlement preference, i.e. whether to hold settled funds in USD, USDC, or neither."""

  async def set_settlement_preferences(
    self,
    user_id: str,
    *,
    settlement_preference: Literal['USD', 'USDC', 'opt-out'] | None = None,
    validate: bool | None = None,
  ) -> ExchangeSettlementPreference:
    """Update the user-level settlement preference, i.e. whether to hold settled funds in USD, USDC, or neither.

    Args:
      user_id: Id of the user who owns the account, or `self` for the caller.
      settlement_preference: Settlement preference.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = Request(user_id=user_id)
    if settlement_preference is not None:
      request['settlement_preference'] = settlement_preference
    return await self.request(
      request,
      method='POST',
      path='/users/{user_id}/settlement-preferences',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeSettlementPreference,
    )
