# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class ExchangeWithdrawalFeeEstimate(TypedDict):
  """Estimated network fee for a crypto withdrawal."""

  fee: NotRequired[Decimal]
  """Estimated network fee for this withdrawal."""
  fee_before_subsidy: NotRequired[Decimal]
  """Estimated network fee before any fee subsidy is applied."""


class Request(TypedDict):
  currency: NotRequired[str]
  """Currency to estimate the withdrawal fee for."""
  crypto_address: NotRequired[str]
  """Destination crypto address."""
  network: NotRequired[str]
  """Crypto network to use, for a currency supporting more than one."""


class FeeEstimate(RpcEndpoint):
  """Gets the fee estimate for a crypto withdrawal to a crypto address."""

  async def fee_estimate(
    self,
    *,
    currency: str | None = None,
    crypto_address: str | None = None,
    network: str | None = None,
    validate: bool | None = None,
  ) -> ExchangeWithdrawalFeeEstimate:
    """Gets the fee estimate for a crypto withdrawal to a crypto address.

    Args:
      currency: Currency to estimate the withdrawal fee for.
      crypto_address: Destination crypto address.
      network: Crypto network to use, for a currency supporting more than one.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = Request()
    if currency is not None:
      request['currency'] = currency
    if crypto_address is not None:
      request['crypto_address'] = crypto_address
    if network is not None:
      request['network'] = network
    return await self.request(
      request,
      method='GET',
      path='/withdrawals/fee-estimate',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeWithdrawalFeeEstimate,
    )
