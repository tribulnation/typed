# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangePaymentMethodTransfer


class Request(TypedDict):
  """A transfer between this profile and a linked external payment method (deposit or withdrawal)."""

  amount: Decimal
  """Amount to deposit, denominated in `currency`."""
  payment_method_id: str
  """Id of the linked payment method to deposit from."""
  currency: str
  """Currency to deposit."""


class PaymentMethod(RpcEndpoint):
  """Withdraws funds from the specified `profile_id` to a linked external payment method."""

  async def payment_method(
    self,
    amount: Decimal,
    *,
    payment_method_id: str,
    currency: str,
    validate: bool | None = None,
  ) -> ExchangePaymentMethodTransfer:
    """Withdraws funds from the specified `profile_id` to a linked external payment method.

    Args:
      amount: Amount to deposit, denominated in `currency`.
      payment_method_id: Id of the linked payment method to deposit from.
      currency: Currency to deposit.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      Request(amount=amount, payment_method_id=payment_method_id, currency=currency),
      method='POST',
      path='/withdrawals/payment-method',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangePaymentMethodTransfer,
    )
