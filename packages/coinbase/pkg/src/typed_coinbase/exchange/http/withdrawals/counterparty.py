# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class ExchangeCounterpartyTransferResult(TypedDict):
  """A withdrawal to a counterparty account."""

  id: NotRequired[str]
  """Unique identifier for this transfer."""
  counterparty_id: NotRequired[str]
  """Id of the counterparty account."""
  amount: NotRequired[Decimal]
  """Amount withdrawn."""
  currency: NotRequired[str]
  """Currency withdrawn."""
  status: NotRequired[Literal['UNKNOWN', 'PENDING', 'DONE']]
  """Transfer status."""


class Request(TypedDict):
  """A withdrawal from this profile to a counterparty account."""

  amount: Decimal
  """Amount to withdraw, denominated in `currency`."""
  counterparty_id: str
  """Id of the counterparty account to withdraw to."""
  currency: str
  """Currency to withdraw."""


class Counterparty(RpcEndpoint):
  """Withdraw funds from a specified currency account to a counterparty account."""

  async def counterparty(
    self,
    amount: Decimal,
    *,
    counterparty_id: str,
    currency: str,
    validate: bool | None = None,
  ) -> ExchangeCounterpartyTransferResult:
    """Withdraw funds from a specified currency account to a counterparty account.

    Args:
      amount: Amount to withdraw, denominated in `currency`.
      counterparty_id: Id of the counterparty account to withdraw to.
      currency: Currency to withdraw.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      Request(amount=amount, counterparty_id=counterparty_id, currency=currency),
      method='POST',
      path='/withdrawals/counterparty',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeCounterpartyTransferResult,
    )
