# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeConversion


class Request(TypedDict):
  conversion_id: str
  """The conversion's id."""
  profile_id: NotRequired[str]
  """Filter to a specific profile."""


class Get(RpcEndpoint):
  """Gets a currency conversion by id (i.e. USD -> USDC)."""

  async def get(
    self,
    *,
    conversion_id: str,
    profile_id: str | None = None,
    validate: bool | None = None,
  ) -> ExchangeConversion:
    """Gets a currency conversion by id (i.e. USD -> USDC).

    Args:
      conversion_id: The conversion's id.
      profile_id: Filter to a specific profile.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = Request(conversion_id=conversion_id)
    if profile_id is not None:
      request['profile_id'] = profile_id
    return await self.request(
      request,
      method='GET',
      path='/conversions/{conversion_id}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeConversion,
    )
