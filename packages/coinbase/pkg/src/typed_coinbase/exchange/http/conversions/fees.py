# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class ExchangeConversionFeeTier(TypedDict):
  """One fee-rate tier of a conversion fee schedule."""

  from_amount: Decimal
  """Lower bound of cumulative volume for this fee tier."""
  to_amount: Decimal
  """Upper bound of cumulative volume for this fee tier."""
  fee_rate: Decimal
  """Fee rate applied within this tier."""


class ExchangeConversionFee(TypedDict):
  """Fee schedule and trailing volume for conversions between one currency pair."""

  from_currency: str
  """Source currency for this fee schedule."""
  to_currency: str
  """Destination currency for this fee schedule."""
  min_fee_rate: NotRequired[Decimal]
  """Minimum fee rate charged for a conversion between this currency pair."""
  thirty_day_net_volume: NotRequired[Decimal]
  """Trailing 30-day net conversion volume between this currency pair, used to determine the applicable fee tier."""
  fee_tiers: NotRequired[list[ExchangeConversionFeeTier]]
  """Fee rate tiers by cumulative conversion volume."""
  available_credit: Decimal
  """Remaining fee-free conversion credit available for this currency pair."""


Response = list[ExchangeConversionFee]


class Fees(RpcEndpoint):
  """Gets a list of current conversion fee rates and trailing 30 day volume by currency pair."""

  async def fees(self, *, validate: bool | None = None) -> Response:
    """Gets a list of current conversion fee rates and trailing 30 day volume by currency pair.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      None,
      method='GET',
      path='/conversions/fees',
      meta={'signed': True},
      validate=validate,
      response_type=Response,
    )
