# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeConversion


RequestKeywords = TypedDict('RequestKeywords', {'from': str})
"""
- `from`: Currency to convert from.
"""


class Request(RequestKeywords):
  """A currency conversion to perform between two accounts in the same profile."""

  profile_id: NotRequired[str]
  """Convert funds within a specific profile. Defaults to the `default` profile."""
  to: str
  """Currency to convert to."""
  amount: Decimal
  """Amount to convert, denominated in the `from` currency."""
  nonce: NotRequired[str]
  """Client-supplied nonce for this request."""
  idem: NotRequired[str]
  """Idempotency key: repeating a call with the same `idem` value returns the original conversion instead of creating a duplicate."""


class Create(RpcEndpoint):
  """Converts funds from `from` currency to `to` currency. Funds are converted on the `from` account in the `profile_id` profile."""

  async def create(
    self,
    *,
    profile_id: str | None = None,
    from_: str,
    to: str,
    amount: Decimal,
    nonce: str | None = None,
    idem: str | None = None,
    validate: bool | None = None,
  ) -> ExchangeConversion:
    """Converts funds from `from` currency to `to` currency. Funds are converted on the `from` account in the `profile_id` profile.

    Args:
      profile_id: Convert funds within a specific profile. Defaults to the `default` profile.
      from_: Currency to convert from.
      to: Currency to convert to.
      amount: Amount to convert, denominated in the `from` currency.
      nonce: Client-supplied nonce for this request.
      idem: Idempotency key: repeating a call with the same `idem` value returns the original conversion instead of creating a duplicate.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    request: Request = {'from': from_, 'to': to, 'amount': amount}
    if profile_id is not None:
      request['profile_id'] = profile_id
    if nonce is not None:
      request['nonce'] = nonce
    if idem is not None:
      request['idem'] = idem
    return await self.request(
      request,
      method='POST',
      path='/conversions',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeConversion,
    )
