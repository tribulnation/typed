# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeCurrency


class Request(TypedDict):
  currency_id: str
  """Currency code, e.g. `BTC`."""


class Get(RpcEndpoint):
  """Get a single currency by id. No authentication required."""

  async def get(
    self, currency_id: str, *, validate: bool | None = None
  ) -> ExchangeCurrency:
    """Get a single currency by id. No authentication required.

    Args:
      currency_id: Currency code, e.g. `BTC`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      Request(currency_id=currency_id),
      method='GET',
      path='/currencies/{currency_id}',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=ExchangeCurrency,
    )
