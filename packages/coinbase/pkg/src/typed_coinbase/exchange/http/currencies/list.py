# Generated by typed-dev — see CONTRIBUTING.md.
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ExchangeCurrency


Response = list[ExchangeCurrency]


class List(RpcEndpoint):
  """List every known currency. Not every listed currency is necessarily available for trading. No authentication required."""

  async def list(self, *, validate: bool | None = None) -> Response:
    """List every known currency. Not every listed currency is necessarily available for trading. No authentication required.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      None,
      method='GET',
      path='/currencies',
      meta={},
      validate=validate,
      response_type=Response,
    )
