# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class ExchangeUsdcMarginSetting(TypedDict):
  """Whether USDC-to-USD auto-conversion is enabled to meet margin calls with an eligible Coinbase affiliate."""

  enabled: bool
  """Whether USDC-to-USD auto-conversion is enabled."""


class Request(TypedDict):
  """USDC-to-USD auto-conversion setting to apply for the caller."""

  enabled: bool
  """Whether to enable USDC-to-USD auto-conversion."""


class SetUsdc(RpcEndpoint):
  """Set the USDC-to-USD auto-conversion setting used to meet margin calls with an eligible Coinbase affiliate."""

  async def set_usdc(
    self,
    enabled: bool,
    *,
    validate: bool | None = None,
  ) -> ExchangeUsdcMarginSetting:
    """Set the USDC-to-USD auto-conversion setting used to meet margin calls with an eligible Coinbase affiliate.

    Args:
      enabled: Whether to enable USDC-to-USD auto-conversion.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      Request(enabled=enabled),
      method='POST',
      path='/margin/usdc',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ExchangeUsdcMarginSetting,
    )
