# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class ExchangeUsdcMarginSetting(TypedDict):
  """Whether USDC-to-USD auto-conversion is enabled to meet margin calls with an eligible Coinbase affiliate."""

  enabled: bool
  """Whether USDC-to-USD auto-conversion is enabled."""


class GetUsdc(RpcEndpoint):
  """Get the USDC-to-USD auto-conversion setting used to meet margin calls with an eligible Coinbase affiliate."""

  async def get_usdc(
    self, *, validate: bool | None = None
  ) -> ExchangeUsdcMarginSetting:
    """Get the USDC-to-USD auto-conversion setting used to meet margin calls with an eligible Coinbase affiliate.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/exchange/rest-api/api-reference)
    """
    return await self.request(
      None,
      method='GET',
      path='/margin/usdc',
      meta={'signed': True},
      validate=validate,
      response_type=ExchangeUsdcMarginSetting,
    )
