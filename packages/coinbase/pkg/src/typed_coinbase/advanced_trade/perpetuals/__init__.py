# Generated by typed-dev — see CONTRIBUTING.md.
from dataclasses import dataclass, field
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from .allocate import Allocate
from .balances import Balances
from .multi_asset_collateral import MultiAssetCollateral
from .portfolio_summary import PortfolioSummary
from .positions import Positions


@dataclass(frozen=True, kw_only=True)
class Perpetuals(RpcEndpoint):
  """`perpetuals` endpoints."""

  allocate: Allocate = field(init=False)
  balances: Balances = field(init=False)
  multi_asset_collateral: MultiAssetCollateral = field(init=False)
  portfolio_summary: PortfolioSummary = field(init=False)
  positions: Positions = field(init=False)

  def __post_init__(self):
    object.__setattr__(self, 'allocate', Allocate(client=self.client))
    object.__setattr__(self, 'balances', Balances(client=self.client))
    object.__setattr__(
      self, 'multi_asset_collateral', MultiAssetCollateral(client=self.client)
    )
    object.__setattr__(self, 'portfolio_summary', PortfolioSummary(client=self.client))
    object.__setattr__(self, 'positions', Positions(client=self.client))
