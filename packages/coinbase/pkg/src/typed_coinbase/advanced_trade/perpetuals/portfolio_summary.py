# Generated by typed-dev — see CONTRIBUTING.md.
from dataclasses import dataclass
from typed_core.validation import validator
from typing_extensions import Literal, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.types import AdvancedTradeAmount


class PerpetualsPortfolio(TypedDict):
  """Collateral, margin and PnL detail for one perpetuals portfolio."""

  portfolio_uuid: str
  """Portfolio identifier."""
  collateral: str
  """Total collateral value in USDC for the portfolio."""
  position_notional: str
  """Total position notional value in USDC for all positions."""
  open_position_notional: str
  """Notional value for open orders and positions."""
  pending_fees: str
  """Accrued fees not yet paid, in USDC."""
  borrow: str
  """Total borrow amount in USDC."""
  accrued_interest: str
  """Interest accrued on borrowed USDC."""
  rolling_debt: str
  """Amount of settled transactions not yet paid."""
  portfolio_initial_margin: str
  """Initial margin utilization for the portfolio."""
  portfolio_im_notional: AdvancedTradeAmount
  portfolio_maintenance_margin: str
  """Maintenance margin for the portfolio."""
  portfolio_mm_notional: AdvancedTradeAmount
  liquidation_percentage: str
  """Liquidation percentage for the portfolio."""
  liquidation_buffer: str
  """Buffer above the liquidation threshold."""
  margin_type: Literal[
    'MARGIN_TYPE_UNSPECIFIED', 'MARGIN_TYPE_CROSS', 'MARGIN_TYPE_ISOLATED'
  ]
  """Whether margin is shared across positions or isolated per position."""
  margin_flags: Literal[
    'PORTFOLIO_MARGIN_FLAGS_UNSPECIFIED', 'PORTFOLIO_MARGIN_FLAGS_IN_LIQUIDATION'
  ]
  """Liquidation-related status flags for the portfolio."""
  liquidation_status: Literal[
    'PORTFOLIO_LIQUIDATION_STATUS_UNSPECIFIED',
    'PORTFOLIO_LIQUIDATION_STATUS_CANCELING',
    'PORTFOLIO_LIQUIDATION_STATUS_AUTO_LIQUIDATING',
    'PORTFOLIO_LIQUIDATION_STATUS_LSP_ASSIGNMENT',
    'PORTFOLIO_LIQUIDATION_STATUS_CUSTOMER_ASSIGNMENT',
    'PORTFOLIO_LIQUIDATION_STATUS_MANUAL',
    'PORTFOLIO_LIQUIDATION_STATUS_NOT_LIQUIDATING',
  ]
  """Current liquidation state of the portfolio."""
  unrealized_pnl: AdvancedTradeAmount
  total_balance: AdvancedTradeAmount


class PerpetualsPortfoliosSummary(TypedDict):
  """Aggregate summary across the requested portfolio(s)."""

  unrealized_pnl: AdvancedTradeAmount
  buying_power: AdvancedTradeAmount
  total_balance: AdvancedTradeAmount
  max_withdrawal_amount: AdvancedTradeAmount


class GetPerpetualsPortfolioSummaryResponse(TypedDict):
  """A perpetuals portfolio and its aggregate summary."""

  portfolios: list[PerpetualsPortfolio]
  """Portfolio detail (a single-element array for the requested portfolio)."""
  summary: PerpetualsPortfoliosSummary


@dataclass(frozen=True, kw_only=True)
class PortfolioSummary(RpcEndpoint):
  """`GET /api/v3/brokerage/intx/portfolio/{portfolio_uuid}`."""

  async def __call__(
    self,
    portfolio_uuid: str,
    *,
    validate: bool | None = None,
  ) -> GetPerpetualsPortfolioSummaryResponse:
    """Get a summary of a perpetuals portfolio on Coinbase International Exchange (INTX). Deprecated: this INTX perpetuals surface retires 2026-09-09, replaced by a Deribit-powered derivatives gateway (see `upstream.md`).

    Args:
      portfolio_uuid: The portfolio UUID.
      validate: Override this client's default response validation for this call.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/api-reference/advanced-trade-api/rest-api/perpetuals/get-perpetuals-portfolio-summary)
    """
    return await self.authed_request(
      'GET',
      f'/api/v3/brokerage/intx/portfolio/{portfolio_uuid}',
      validator=validator(GetPerpetualsPortfolioSummaryResponse),
      validate=validate,
    )
