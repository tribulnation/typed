# Generated by typed-dev — see CONTRIBUTING.md.
from dataclasses import dataclass
from typed_core.validation import validator
from typing_extensions import Literal, NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.core import TimestampIso


class HistoricalMarketTrade(TypedDict):
  """One historical trade for a product."""

  trade_id: str
  """Unique identifier for the trade."""
  product_id: str
  """The trading pair the trade occurred on."""
  price: str
  """Trade price in quote currency."""
  size: str
  """Trade size in base currency."""
  time: TimestampIso
  """Time the trade occurred."""
  side: Literal['BUY', 'SELL']
  """Which side of the book took liquidity."""
  exchange: NotRequired[str]
  """Venue the trade occurred on."""


class MarketTradesResponse(TypedDict):
  """A page of recent trades for a product."""

  trades: list[HistoricalMarketTrade]
  """Trades, newest first."""
  best_bid: NotRequired[str]
  """Current best bid price."""
  best_ask: NotRequired[str]
  """Current best ask price."""


@dataclass(frozen=True, kw_only=True)
class MarketTrades(RpcEndpoint):
  """`GET /api/v3/brokerage/products/{product_id}/ticker`."""

  async def __call__(
    self,
    product_id: str,
    *,
    limit: int,
    start: int | None = None,
    end: int | None = None,
    validate: bool | None = None,
  ) -> MarketTradesResponse:
    """Get the most recent trades for one product. Named 'ticker' on the wire, but returns a trade-history page, not best bid/ask (use `best_bid_ask` for that).

    Args:
      product_id: The trading pair, e.g. `BTC-USD`.
      limit: Number of trades to return.
      start: UNIX timestamp for the start of the requested window.
      end: UNIX timestamp for the end of the requested window.
      validate: Override this client's default response validation for this call.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/api-reference/advanced-trade-api/rest-api/products/get-market-trades)
    """
    params: dict = {
      'limit': limit,
    }
    if start is not None:
      params['start'] = start
    if end is not None:
      params['end'] = end
    return await self.authed_request(
      'GET',
      f'/api/v3/brokerage/products/{product_id}/ticker',
      params=params,
      validator=validator(MarketTradesResponse),
      validate=validate,
    )
