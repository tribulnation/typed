# Generated by typed-dev — see CONTRIBUTING.md.
from dataclasses import dataclass
from typed_core.validation import validator
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class Price(TypedDict):
  """A price quote in the requested currency pair."""

  amount: str
  """Price, as a decimal string."""
  currency: str
  """Quote currency of `amount`."""


class PriceResponse(TypedDict):
  """The whole v2 wire frame — not unwrapped by the core."""

  data: Price


@dataclass(frozen=True, kw_only=True)
class Spot(RpcEndpoint):
  """`GET /v2/prices/{currency_pair}/spot`."""

  async def spot(
    self,
    currency_pair: str,
    *,
    date: str | None = None,
    validate: bool | None = None,
  ) -> PriceResponse:
    """Get the current market spot price for a currency pair, or a historic spot price on a given date. No authentication required.

    Args:
      currency_pair: Currency pair, e.g. `BTC-USD`.
      date: Historic date to price against, `YYYY-MM-DD`. Defaults to the current spot price when omitted.
      validate: Override this client's default response validation for this call.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/coinbase-app/track-apis/prices)
    """
    params = {}
    if date is not None:
      params['date'] = date
    return await self.request(
      'GET',
      f'/v2/prices/{currency_pair}/spot',
      params=params,
      validator=validator(PriceResponse),
      validate=validate,
    )
