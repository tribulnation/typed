# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class CoinbaseOneSubscription(TypedDict):
  """The user's Coinbase One subscription state. Field set is inferred from prose only — no worked example or field table is published (see `upstream.md`)."""

  status: str
  """Subscription status. Not documented as an exhaustive enumeration."""
  tier: str
  """Coinbase One subscription tier. Not documented as an exhaustive enumeration."""
  current_period_end: str
  """End of the current billing period. Format not documented; other v2 timestamp fields use ISO 8601."""


class GetCoinbaseOneSubscriptionResponse(TypedDict):
  """The whole v2 wire frame — not unwrapped by the core."""

  data: CoinbaseOneSubscription


class CoinbaseOne(RpcEndpoint):
  """Get the current user's Coinbase One subscription status, tier, and current billing period end. Requires the `wallet:subscription:read` scope; a token without it returns `403`."""

  async def coinbase_one(
    self,
    *,
    validate: bool | None = None,
  ) -> GetCoinbaseOneSubscriptionResponse:
    """Get the current user's Coinbase One subscription status, tier, and current billing period end. Requires the `wallet:subscription:read` scope; a token without it returns `403`.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/coinbase-app/oauth2-integration/available-apis)
    """
    return await self.request(
      None,
      method='GET',
      path='/v2/subscriptions/coinbase-one',
      meta={'signed': True},
      validate=validate,
      response_type=GetCoinbaseOneSubscriptionResponse,
    )
