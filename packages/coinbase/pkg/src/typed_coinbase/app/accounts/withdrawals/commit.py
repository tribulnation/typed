# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import V2Transfer


class Request(TypedDict):
  account_id: str
  """The fiat account the withdrawal belongs to."""
  withdrawal_id: str
  """The withdrawal to commit."""


class CommitWithdrawalResponse(TypedDict):
  """Wrapper around the committed withdrawal transfer."""

  transfer: V2Transfer


class Commit(RpcEndpoint):
  """Complete a withdrawal that was created with `commit: false`."""

  async def commit(
    self,
    *,
    account_id: str,
    withdrawal_id: str,
    validate: bool | None = None,
  ) -> CommitWithdrawalResponse:
    """Complete a withdrawal that was created with `commit: false`.

    Args:
      account_id: The fiat account the withdrawal belongs to.
      withdrawal_id: The withdrawal to commit.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/coinbase-app/transfer-apis/withdraw-fiat)
    """
    return await self.request(
      Request(account_id=account_id, withdrawal_id=withdrawal_id),
      method='POST',
      path='/v2/accounts/{account_id}/withdrawals/{withdrawal_id}/commit',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CommitWithdrawalResponse,
    )
