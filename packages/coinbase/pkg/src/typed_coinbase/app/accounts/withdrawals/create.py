# Generated by typed-dev — see CONTRIBUTING.md.
from dataclasses import dataclass
from typed_core.validation import validator
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.types import V2Transfer


class WithdrawFundsResponse(TypedDict):
  """Wrapper around the created withdrawal transfer."""

  transfer: V2Transfer


@dataclass(frozen=True, kw_only=True)
class Create(RpcEndpoint):
  """`POST /v2/accounts/{account_id}/withdrawals`."""

  async def create(
    self,
    account_id: str,
    *,
    amount: str,
    currency: str,
    payment_method: str,
    commit: bool | None = None,
    validate: bool | None = None,
  ) -> WithdrawFundsResponse:
    """Withdraw a user-defined amount of funds from a fiat account to a linked payment method. Pass `commit: false` to create the withdrawal without executing it, to be completed later with Commit Withdrawal.

    Args:
      account_id: The fiat account to withdraw from.
      amount: Withdrawal amount.
      currency: Currency for `amount`.
      payment_method: Id of the payment method to withdraw to. Payment methods are listed via `advanced_trade.payment_methods.list`.
      commit: If false, this withdrawal is not immediately completed; complete it later with Commit Withdrawal.
      validate: Override this client's default response validation for this call.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/coinbase-app/transfer-apis/withdraw-fiat)
    """
    body: dict = {
      'amount': amount,
      'currency': currency,
      'payment_method': payment_method,
    }
    if commit is not None:
      body['commit'] = commit
    return await self.authed_request(
      'POST',
      f'/v2/accounts/{account_id}/withdrawals',
      json=body,
      validator=validator(WithdrawFundsResponse),
      validate=validate,
    )
