# Generated by typed-dev — see CONTRIBUTING.md.
from dataclasses import dataclass
from typed_coinbase.core.endpoint.rpc import RpcClient, RpcEndpoint
from .addresses import Addresses
from .currencies import Currencies
from .deposits import Deposits
from .exchange_rates import ExchangeRates
from .get import Get
from .list import List
from .prices import Prices
from .subscriptions import Subscriptions
from .time import Time
from .transactions import Transactions
from .withdrawals import Withdrawals


@dataclass(frozen=True, kw_only=True)
class Accounts(RpcEndpoint):
  """`accounts` endpoints."""

  addresses: Addresses
  currencies: Currencies
  deposits: Deposits
  exchange_rates: ExchangeRates
  get: Get
  list: List
  prices: Prices
  subscriptions: Subscriptions
  time: Time
  transactions: Transactions
  withdrawals: Withdrawals

  @classmethod
  def new(cls, *, client: RpcClient) -> 'Accounts':
    """Construct every child endpoint from one shared client."""
    return cls(
      client=client,
      addresses=Addresses.new(client=client),
      currencies=Currencies.new(client=client),
      deposits=Deposits.new(client=client),
      exchange_rates=ExchangeRates.new(client=client),
      get=Get(client=client),
      list=List(client=client),
      prices=Prices.new(client=client),
      subscriptions=Subscriptions.new(client=client),
      time=Time(client=client),
      transactions=Transactions.new(client=client),
      withdrawals=Withdrawals.new(client=client),
    )
