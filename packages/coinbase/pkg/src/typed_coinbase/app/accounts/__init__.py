# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .addresses import Addresses
from .currencies import Currencies
from .deposits import Deposits
from .exchange_rates import ExchangeRates
from .get import Get
from .list import List
from .prices import Prices
from .subscriptions import Subscriptions
from .time import Time
from .transactions import Transactions
from .withdrawals import Withdrawals


class Accounts(Get, List, Time):
  """Coinbase App v2 (Track/Transfer APIs) — the linked wallet/vault/fiat accounts behind a CDP API key: balances, addresses, transactions, deposits, withdrawals, prices, currencies, exchange rates, and subscription status.

  References:
    - [Upstream docs](https://docs.cdp.coinbase.com/coinbase-app/track-apis/accounts)
  """

  @cached_property
  def addresses(self) -> Addresses:
    """Onchain deposit addresses for a Coinbase App v2 wallet account — create and list them, look one up, and page through the transactions received on it.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/coinbase-app/transfer-apis/onchain-addresses)
    """
    return Addresses(client=self.client)

  @cached_property
  def currencies(self) -> Currencies:
    """Fiat and crypto currencies Coinbase supports — the Coinbase App v2 Track API's reference lists, not the calling user's own held balances.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/coinbase-app/track-apis/currencies)
    """
    return Currencies(client=self.client)

  @cached_property
  def deposits(self) -> Deposits:
    """Fiat deposits into a Coinbase App v2 account from a linked payment method — create, list, look one up, and commit one still pending confirmation.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/coinbase-app/transfer-apis/deposit-fiat)
    """
    return Deposits(client=self.client)

  @cached_property
  def exchange_rates(self) -> ExchangeRates:
    """Current exchange rates between a base currency and every other Coinbase-supported currency — the Coinbase App v2 Track API's public quote list.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/coinbase-app/track-apis/exchange-rates)
    """
    return ExchangeRates(client=self.client)

  @cached_property
  def prices(self) -> Prices:
    """Spot, buy, and sell price quotes for a currency pair — the Coinbase App v2 Track API's public pricing endpoints.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/coinbase-app/track-apis/prices)
    """
    return Prices(client=self.client)

  @cached_property
  def subscriptions(self) -> Subscriptions:
    """Coinbase App subscription status checks reachable through the OAuth2-scoped Track API surface — currently the account's Coinbase One membership state.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/coinbase-app/oauth2-integration/available-apis)
    """
    return Subscriptions(client=self.client)

  @cached_property
  def transactions(self) -> Transactions:
    """Sends and receives on a Coinbase App v2 account — create a crypto send, and list or look up past transactions.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/coinbase-app/track-apis/transactions)
    """
    return Transactions(client=self.client)

  @cached_property
  def withdrawals(self) -> Withdrawals:
    """Fiat withdrawals from a Coinbase App v2 account to a linked payment method — create, list, look one up, and commit one still pending confirmation.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/coinbase-app/transfer-apis/withdraw-fiat)
    """
    return Withdrawals(client=self.client)
