# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import Money


class Request(TypedDict):
  currency_pair: str
  """Currency pair, e.g. `BTC-USD`."""


class PriceResponse(TypedDict):
  """The whole v2 wire frame — not unwrapped by the core."""

  data: Money


class Buy(RpcEndpoint):
  """Get the total price, inclusive of fees, to buy one unit of the base currency. No authentication required."""

  async def buy(
    self, currency_pair: str, *, validate: bool | None = None
  ) -> PriceResponse:
    """Get the total price, inclusive of fees, to buy one unit of the base currency. No authentication required.

    Args:
      currency_pair: Currency pair, e.g. `BTC-USD`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/coinbase-app/track-apis/prices)
    """
    return await self.request(
      Request(currency_pair=currency_pair),
      method='GET',
      path='/v2/prices/{currency_pair}/buy',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=PriceResponse,
    )
