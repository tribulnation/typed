# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core import DateIso
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import Money


class Request(TypedDict):
  currency_pair: str
  """Currency pair, e.g. `BTC-USD`."""
  date: NotRequired[DateIso]
  """Historic date to price against, `YYYY-MM-DD`. Defaults to the current spot price when omitted."""


class PriceResponse(TypedDict):
  """The whole v2 wire frame — not unwrapped by the core."""

  data: Money


class Spot(RpcEndpoint):
  """Get the current market spot price for a currency pair, or a historic spot price on a given date. No authentication required."""

  async def spot(
    self,
    currency_pair: str,
    *,
    date: DateIso | None = None,
    validate: bool | None = None,
  ) -> PriceResponse:
    """Get the current market spot price for a currency pair, or a historic spot price on a given date. No authentication required.

    Args:
      currency_pair: Currency pair, e.g. `BTC-USD`.
      date: Historic date to price against, `YYYY-MM-DD`. Defaults to the current spot price when omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/coinbase-app/track-apis/prices)
    """
    request: Request = Request(currency_pair=currency_pair)
    if date is not None:
      request['date'] = date
    return await self.request(
      request,
      method='GET',
      path='/v2/prices/{currency_pair}/spot',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=PriceResponse,
    )
