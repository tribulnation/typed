# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import Address


class Request(TypedDict):
  account_id: str
  """The account to create the address under."""
  name: NotRequired[str]
  """User-defined label for the address."""
  network: NotRequired[str]
  """The blockchain network for the address, e.g. `ethereum`, `solana`, `bitcoin`, `base`, `polygon`. Defaults to the account's asset's default network when omitted."""


class CreateAddressResponse(TypedDict):
  """Wrapper around the newly created address."""

  data: Address


class Create(RpcEndpoint):
  """Create a new receive address for an account. All arguments are optional, so an empty request is enough to generate a new address on-demand."""

  async def create(
    self,
    *,
    account_id: str,
    name: str | None = None,
    network: str | None = None,
    validate: bool | None = None,
  ) -> CreateAddressResponse:
    """Create a new receive address for an account. All arguments are optional, so an empty request is enough to generate a new address on-demand.

    Args:
      account_id: The account to create the address under.
      name: User-defined label for the address.
      network: The blockchain network for the address, e.g. `ethereum`, `solana`, `bitcoin`, `base`, `polygon`. Defaults to the account's asset's default network when omitted.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/coinbase-app/transfer-apis/onchain-addresses)
    """
    request: Request = Request(account_id=account_id)
    if name is not None:
      request['name'] = name
    if network is not None:
      request['network'] = network
    return await self.request(
      request,
      method='POST',
      path='/v2/accounts/{account_id}/addresses',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CreateAddressResponse,
    )
