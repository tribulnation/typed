# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import Address


class Request(TypedDict):
  account_id: str
  """The account the address belongs to."""
  address_id: str
  """The address to fetch."""


class GetAddressResponse(TypedDict):
  """Wrapper around a single address."""

  data: Address


class Get(RpcEndpoint):
  """Get a single address for an account. An address belongs to exactly one account -- use Create Address to mint new ones."""

  async def get(
    self,
    *,
    account_id: str,
    address_id: str,
    validate: bool | None = None,
  ) -> GetAddressResponse:
    """Get a single address for an account. An address belongs to exactly one account -- use Create Address to mint new ones.

    Args:
      account_id: The account the address belongs to.
      address_id: The address to fetch.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/coinbase-app/transfer-apis/onchain-addresses)
    """
    return await self.request(
      Request(account_id=account_id, address_id=address_id),
      method='GET',
      path='/v2/accounts/{account_id}/addresses/{address_id}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=GetAddressResponse,
    )
