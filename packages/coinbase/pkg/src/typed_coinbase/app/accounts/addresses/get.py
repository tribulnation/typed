# Generated by typed-dev — see CONTRIBUTING.md.
from dataclasses import dataclass
from typed_core.validation import validator
from typing_extensions import Literal, NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.core import TimestampIso


class Address(TypedDict):
  """An address for a Coinbase-supported asset. An account can have more than one address, but an address belongs to exactly one account."""

  id: str
  """Resource id."""
  address: str
  """The address string for the asset's network."""
  name: str | None
  """User-defined label for the address, or null when unset."""
  callback_url: NotRequired[str | None]
  """Notification callback URL for this address, or null when unset. Undocumented in the field table. Verified live: the key is omitted entirely (not sent as null) when unset, so it is not required."""
  created_at: TimestampIso
  """When this address was created, RFC3339."""
  updated_at: TimestampIso
  """When this address was last updated, RFC3339."""
  network: str
  """Name of the blockchain this address belongs to, e.g. `ethereum`, `bitcoin`."""
  resource: Literal['addresses']
  """Resource type. Documented as `address`, but verified live to be `addresses` (plural) on both the list and get responses."""
  resource_path: str
  """API path to fetch this address resource."""
  destination_tag: NotRequired[str]
  """Destination tag / memo for networks that use one (e.g. XRP, XLM), or an empty string when not applicable. Verified live, undocumented in the field table."""


class GetAddressResponse(TypedDict):
  """Wrapper around a single address."""

  data: Address


@dataclass(frozen=True, kw_only=True)
class Get(RpcEndpoint):
  """`GET /v2/accounts/{account_id}/addresses/{address_id}`."""

  async def get(
    self,
    account_id: str,
    address_id: str,
    *,
    validate: bool | None = None,
  ) -> GetAddressResponse:
    """Get a single address for an account. An address belongs to exactly one account -- use Create Address to mint new ones.

    Args:
      account_id: The account the address belongs to.
      address_id: The address to fetch.
      validate: Override this client's default response validation for this call.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/coinbase-app/transfer-apis/onchain-addresses)
    """
    return await self.authed_request(
      'GET',
      f'/v2/accounts/{account_id}/addresses/{address_id}',
      validator=validator(GetAddressResponse),
      validate=validate,
    )
