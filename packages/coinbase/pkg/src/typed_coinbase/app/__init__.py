# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .accounts import Accounts
from .advanced_trade import AdvancedTrade
from typed_coinbase.app.core import AppBase


class App(AppBase):
  """Coinbase App (Consumer APIs) — the only product tier this client implements under `app`. Business APIs (Coinbase Business) and Institutional APIs (Exchange, International Exchange, Prime, Derivatives) are separate, unimplemented product families documented on the same `docs.cdp.coinbase.com` host.

  References:
    - [Upstream docs](https://docs.cdp.coinbase.com/coinbase-app/docs/welcome)
  """

  @cached_property
  def accounts(self) -> Accounts:
    """Coinbase App v2 (Track/Transfer APIs) — the linked wallet/vault/fiat accounts behind a CDP API key: balances, addresses, transactions, deposits, withdrawals, prices, currencies, exchange rates, and subscription status.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/coinbase-app/track-apis/accounts)
    """
    return Accounts(client=self.client)

  @cached_property
  def advanced_trade(self) -> AdvancedTrade:
    """Advanced Trade (v3) — orders, products, portfolios, fees, convert, futures/perpetuals, and both WebSocket connections (public market data, private user updates).

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/coinbase-app/advanced-trade-apis/overview)
    """
    return AdvancedTrade.new(
      self.client, market_client=self.market_client, user_client=self.user_client
    )
