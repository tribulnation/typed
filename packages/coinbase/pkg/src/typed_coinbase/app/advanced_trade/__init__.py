# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .http import Http
from .streams import Streams
from typed_coinbase.app.advanced_trade.core import AdvancedTradeBase


class AdvancedTrade(AdvancedTradeBase):
  """Advanced Trade (v3) — orders, products, portfolios, fees, convert, futures/perpetuals, and both WebSocket connections (public market data, private user updates).

  References:
    - [Upstream docs](https://docs.cdp.coinbase.com/coinbase-app/advanced-trade-apis/overview)
  """

  @cached_property
  def http(self) -> Http:
    """Advanced Trade v3 REST: accounts, products and market data, order placement and management, convert, fees, payment methods, portfolios, futures, and INTX perpetuals.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/advanced-trade-api/rest-api/introduction)
    """
    return Http(client=self.client)

  @cached_property
  def streams(self) -> Streams:
    """Advanced Trade WebSocket: two separate physical connections composed as one group — public market data (`market_data`) and the private, per-portfolio `user` channel.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/coinbase-app/advanced-trade-apis/websocket/websocket-overview)
    """
    return Streams.new(self.market_client, user_client=self.user_client)
