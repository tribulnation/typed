# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .market_data import MarketData
from .user import User
from typed_coinbase.app.advanced_trade.streams.core import StreamsGroupBase


class Streams(StreamsGroupBase):
  """Advanced Trade WebSocket: two separate physical connections composed as one group — public market data (`market_data`) and the private, per-portfolio `user` channel.

  References:
    - [Upstream docs](https://docs.cdp.coinbase.com/coinbase-app/advanced-trade-apis/websocket/websocket-overview)
  """

  @cached_property
  def market_data(self) -> MarketData:
    """Advanced Trade's public WebSocket market-data channels — ticker, batched ticker, level2 order book, market trades, candles, product status, and heartbeats.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/coinbase-app/advanced-trade-apis/websocket/websocket-endpoints)
    """
    return MarketData(client=self.market_client)

  @cached_property
  def user(self) -> User:
    """Advanced Trade's private WebSocket `user` channel — open-order and futures/perpetuals/prediction-market position snapshots and updates for the calling key's own portfolio.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/coinbase-app/advanced-trade-apis/websocket/websocket-endpoints)
    """
    return User(client=self.user_client)
