# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import MarketTradesResponse


class Request(TypedDict):
  product_id: str
  """The trading pair, e.g. `BTC-USD`."""
  limit: int
  """Number of trades to return."""
  start: NotRequired[int]
  """UNIX timestamp for the start of the requested window."""
  end: NotRequired[int]
  """UNIX timestamp for the end of the requested window."""


class MarketTrades(RpcEndpoint):
  """Get the most recent trades for one product. Named 'ticker' on the wire, but returns a trade-history page, not best bid/ask (use `best_bid_ask` for that)."""

  async def market_trades(
    self,
    product_id: str,
    *,
    limit: int,
    start: int | None = None,
    end: int | None = None,
    validate: bool | None = None,
  ) -> MarketTradesResponse:
    """Get the most recent trades for one product. Named 'ticker' on the wire, but returns a trade-history page, not best bid/ask (use `best_bid_ask` for that).

    Args:
      product_id: The trading pair, e.g. `BTC-USD`.
      limit: Number of trades to return.
      start: UNIX timestamp for the start of the requested window.
      end: UNIX timestamp for the end of the requested window.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/api-reference/advanced-trade-api/rest-api/products/get-market-trades)
    """
    request: Request = Request(product_id=product_id, limit=limit)
    if start is not None:
      request['start'] = start
    if end is not None:
      request['end'] = end
    return await self.request(
      request,
      method='GET',
      path='/api/v3/brokerage/products/{product_id}/ticker',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=MarketTradesResponse,
    )
