# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ConvertTrade


class Request(TypedDict):
  trade_id: str
  """The id of the trade to fetch."""
  from_account: str
  """Currency of the account the trade converts from, e.g. `USD`."""
  to_account: str
  """Currency of the account the trade converts to, e.g. `USDC`."""


class GetConvertTradeResponse(TypedDict):
  trade: NotRequired[ConvertTrade]


class Get(RpcEndpoint):
  """Get a previously created or committed convert trade by id."""

  async def get(
    self,
    *,
    trade_id: str,
    from_account: str,
    to_account: str,
    validate: bool | None = None,
  ) -> GetConvertTradeResponse:
    """Get a previously created or committed convert trade by id.

    Args:
      trade_id: The id of the trade to fetch.
      from_account: Currency of the account the trade converts from, e.g. `USD`.
      to_account: Currency of the account the trade converts to, e.g. `USDC`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/api-reference/advanced-trade-api/rest-api/convert/get-convert-trade)
    """
    return await self.request(
      Request(trade_id=trade_id, from_account=from_account, to_account=to_account),
      method='GET',
      path='/api/v3/brokerage/convert/trade/{trade_id}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=GetConvertTradeResponse,
    )
