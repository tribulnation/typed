# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import V3Account


class Request(TypedDict):
  account_uuid: str
  """The account's UUID."""


class V3AccountResponse(TypedDict):
  account: V3Account


class Get(RpcEndpoint):
  """Get a single Advanced Trade v3 brokerage account by UUID."""

  async def get(
    self,
    account_uuid: str,
    *,
    validate: bool | None = None,
  ) -> V3AccountResponse:
    """Get a single Advanced Trade v3 brokerage account by UUID.

    Args:
      account_uuid: The account's UUID.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/api-reference/advanced-trade-api/rest-api/accounts/get-account)
    """
    return await self.request(
      Request(account_uuid=account_uuid),
      method='GET',
      path='/api/v3/brokerage/accounts/{account_uuid}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=V3AccountResponse,
    )
