# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import (
  NewOrderErrorResponse,
  NewOrderSuccessResponse,
  OrderConfiguration,
)


class Request(TypedDict):
  client_order_id: str
  """A caller-supplied unique id for the closing order."""
  product_id: str
  """The contract's product id, e.g. `BIT-28JUL23-CDE`."""
  size: NotRequired[Decimal]
  """Number of contracts to close; omit to close the full position."""


class ClosePositionFailure(TypedDict):
  success: Literal[False]
  """Whether the closing order was created."""
  error_response: NewOrderErrorResponse
  order_configuration: NotRequired[OrderConfiguration]


class ClosePositionSuccess(TypedDict):
  success: Literal[True]
  """Whether the closing order was created."""
  success_response: NewOrderSuccessResponse
  order_configuration: NotRequired[OrderConfiguration]


ClosePositionResponse = ClosePositionSuccess | ClosePositionFailure


class ClosePosition(RpcEndpoint):
  """Place a market order to close an open futures/perpetuals position, in full or in part."""

  async def close_position(
    self,
    *,
    client_order_id: str,
    product_id: str,
    size: Decimal | None = None,
    validate: bool | None = None,
  ) -> ClosePositionResponse:
    """Place a market order to close an open futures/perpetuals position, in full or in part.

    Args:
      client_order_id: A caller-supplied unique id for the closing order.
      product_id: The contract's product id, e.g. `BIT-28JUL23-CDE`.
      size: Number of contracts to close; omit to close the full position.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/api-reference/advanced-trade-api/rest-api/orders/close-position)
    """
    request: Request = Request(client_order_id=client_order_id, product_id=product_id)
    if size is not None:
      request['size'] = size
    return await self.request(
      request,
      method='POST',
      path='/api/v3/brokerage/orders/close_position',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ClosePositionResponse,
    )
