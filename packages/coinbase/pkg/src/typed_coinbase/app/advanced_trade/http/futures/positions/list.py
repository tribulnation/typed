# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import FuturesPosition


class GetFuturesPositionsResponse(TypedDict):
  """Open futures positions."""

  positions: list[FuturesPosition]
  """One entry per open futures position."""


class List(RpcEndpoint):
  """List all open positions in the CFM futures wallet."""

  async def list(self, *, validate: bool | None = None) -> GetFuturesPositionsResponse:
    """List all open positions in the CFM futures wallet.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/api-reference/advanced-trade-api/rest-api/futures/list-futures-positions)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v3/brokerage/cfm/positions',
      meta={'signed': True},
      validate=validate,
      response_type=GetFuturesPositionsResponse,
    )
