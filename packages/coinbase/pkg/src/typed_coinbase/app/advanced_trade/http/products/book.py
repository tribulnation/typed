# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ProductBookResponse


class Request(TypedDict):
  product_id: str
  """The trading pair, e.g. `BTC-USD`."""
  limit: NotRequired[int]
  """Number of bid/ask levels to return."""
  aggregation_price_increment: NotRequired[Decimal]
  """Minimum price interval to group bids/asks into."""


class Book(RpcEndpoint):
  """Get an order book snapshot for one product."""

  async def book(
    self,
    product_id: str,
    *,
    limit: int | None = None,
    aggregation_price_increment: Decimal | None = None,
    validate: bool | None = None,
  ) -> ProductBookResponse:
    """Get an order book snapshot for one product.

    Args:
      product_id: The trading pair, e.g. `BTC-USD`.
      limit: Number of bid/ask levels to return.
      aggregation_price_increment: Minimum price interval to group bids/asks into.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/api-reference/advanced-trade-api/rest-api/products/get-product-book)
    """
    request: Request = Request(product_id=product_id)
    if limit is not None:
      request['limit'] = limit
    if aggregation_price_increment is not None:
      request['aggregation_price_increment'] = aggregation_price_increment
    return await self.request(
      request,
      method='GET',
      path='/api/v3/brokerage/product_book',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=ProductBookResponse,
    )
