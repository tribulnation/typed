# Generated by typed-dev — see CONTRIBUTING.md.
from decimal import Decimal
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import ConvertTrade


class TradeIncentiveMetadata(TypedDict):
  """A trade incentive to waive trade fees."""

  user_incentive_id: NotRequired[str]
  """Id of the user incentive to apply."""
  code_val: NotRequired[str]
  """A promo code to waive fees."""


class CreateConvertQuoteResponse(TypedDict):
  trade: NotRequired[ConvertTrade]


class Request(TypedDict):
  from_account: str
  """Currency of the account to convert from, e.g. `USD`."""
  to_account: str
  """Currency of the account to convert to, e.g. `USDC`."""
  amount: Decimal
  """Amount to convert, denominated in `from_account`'s currency."""
  trade_incentive_metadata: NotRequired[TradeIncentiveMetadata]


class Quote(RpcEndpoint):
  """Create a quote for converting funds from one currency to another, e.g. USD to USDC. Returns a trade id to be fetched or committed."""

  async def quote(
    self,
    *,
    from_account: str,
    to_account: str,
    amount: Decimal,
    trade_incentive_metadata: TradeIncentiveMetadata | None = None,
    validate: bool | None = None,
  ) -> CreateConvertQuoteResponse:
    """Create a quote for converting funds from one currency to another, e.g. USD to USDC. Returns a trade id to be fetched or committed.

    Args:
      from_account: Currency of the account to convert from, e.g. `USD`.
      to_account: Currency of the account to convert to, e.g. `USDC`.
      amount: Amount to convert, denominated in `from_account`'s currency.
      trade_incentive_metadata: A trade incentive to waive trade fees.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/api-reference/advanced-trade-api/rest-api/convert/create-convert-quote)
    """
    request: Request = Request(
      from_account=from_account, to_account=to_account, amount=amount
    )
    if trade_incentive_metadata is not None:
      request['trade_incentive_metadata'] = trade_incentive_metadata
    return await self.request(
      request,
      method='POST',
      path='/api/v3/brokerage/convert/quote',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=CreateConvertQuoteResponse,
    )
