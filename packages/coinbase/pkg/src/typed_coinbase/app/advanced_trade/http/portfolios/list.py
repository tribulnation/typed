# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import Portfolio


class Request(TypedDict):
  portfolio_type: NotRequired[Literal['UNDEFINED', 'DEFAULT', 'CONSUMER', 'INTX']]
  """Only return portfolios matching this portfolio type."""


class GetPortfoliosResponse(TypedDict):
  portfolios: list[Portfolio]
  """The portfolio list."""


class List(RpcEndpoint):
  """List all portfolios belonging to the calling user."""

  async def list(
    self,
    portfolio_type: Literal['UNDEFINED', 'DEFAULT', 'CONSUMER', 'INTX'] | None = None,
    *,
    validate: bool | None = None,
  ) -> GetPortfoliosResponse:
    """List all portfolios belonging to the calling user.

    Args:
      portfolio_type: Only return portfolios matching this portfolio type.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/api-reference/advanced-trade-api/rest-api/portfolios/list-portfolios)
    """
    request: Request = Request()
    if portfolio_type is not None:
      request['portfolio_type'] = portfolio_type
    return await self.request(
      request,
      method='GET',
      path='/api/v3/brokerage/portfolios',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=GetPortfoliosResponse,
    )
