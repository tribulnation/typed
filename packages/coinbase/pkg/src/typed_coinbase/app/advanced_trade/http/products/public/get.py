# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import Product


class Request(TypedDict):
  product_id: str
  """The trading pair, e.g. `BTC-USD`."""


class Get(RpcEndpoint):
  """No authentication required. Get one product from the public catalog."""

  async def get(self, product_id: str, *, validate: bool | None = None) -> Product:
    """No authentication required. Get one product from the public catalog.

    Args:
      product_id: The trading pair, e.g. `BTC-USD`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/api-reference/advanced-trade-api/rest-api/public/get-public-product)
    """
    return await self.request(
      Request(product_id=product_id),
      method='GET',
      path='/api/v3/brokerage/market/products/{product_id}',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Product,
    )
