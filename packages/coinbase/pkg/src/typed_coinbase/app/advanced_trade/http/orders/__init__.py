# Generated by typed-dev — see CONTRIBUTING.md.
from dataclasses import dataclass
from typed_coinbase.core.endpoint.rpc import RpcClient, RpcEndpoint
from .batch_cancel import BatchCancel
from .close_position import ClosePosition
from .create import Create
from .edit import Edit
from .edit_preview import EditPreview
from .historical import Historical
from .preview import Preview


@dataclass(frozen=True, kw_only=True)
class Orders(RpcEndpoint):
  """`orders` endpoints."""

  batch_cancel: BatchCancel
  close_position: ClosePosition
  create: Create
  edit: Edit
  edit_preview: EditPreview
  historical: Historical
  preview: Preview

  @classmethod
  def new(cls, *, client: RpcClient) -> 'Orders':
    """Construct every child endpoint from one shared client."""
    return cls(
      client=client,
      batch_cancel=BatchCancel(client=client),
      close_position=ClosePosition(client=client),
      create=Create(client=client),
      edit=Edit(client=client),
      edit_preview=EditPreview(client=client),
      historical=Historical.new(client=client),
      preview=Preview(client=client),
    )
