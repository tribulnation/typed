# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import NotRequired, TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import Order


class Request(TypedDict):
  order_id: str
  """The id of the order to fetch."""
  client_order_id: NotRequired[str]
  """Deprecated. Fetch the order by client-supplied id instead of `order_id`."""
  user_native_currency: NotRequired[str]
  """Deprecated. Native currency to express order values in; defaults to `USD`."""


class GetOrderResponse(TypedDict):
  order: NotRequired[Order]


class Get(RpcEndpoint):
  """Get a single order by id. Equity orders identify themselves with `product_type: EQUITY`; `displayed_order_config` carries the equity time in force and `equity_trading_session` the session the order is eligible to execute in."""

  async def get(
    self,
    *,
    order_id: str,
    client_order_id: str | None = None,
    user_native_currency: str | None = None,
    validate: bool | None = None,
  ) -> GetOrderResponse:
    """Get a single order by id. Equity orders identify themselves with `product_type: EQUITY`; `displayed_order_config` carries the equity time in force and `equity_trading_session` the session the order is eligible to execute in.

    Args:
      order_id: The id of the order to fetch.
      client_order_id: Deprecated. Fetch the order by client-supplied id instead of `order_id`.
      user_native_currency: Deprecated. Native currency to express order values in; defaults to `USD`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/api-reference/advanced-trade-api/rest-api/orders/get-order)
    """
    request: Request = Request(order_id=order_id)
    if client_order_id is not None:
      request['client_order_id'] = client_order_id
    if user_native_currency is not None:
      request['user_native_currency'] = user_native_currency
    return await self.request(
      request,
      method='GET',
      path='/api/v3/brokerage/orders/historical/{order_id}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=GetOrderResponse,
    )
