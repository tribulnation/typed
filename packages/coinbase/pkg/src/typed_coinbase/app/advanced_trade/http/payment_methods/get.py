# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import PaymentMethod


class Request(TypedDict):
  payment_method_id: str
  """The payment method's id. See `advanced_trade.payment_methods.list` for the account's available payment methods and their ids."""


class GetPaymentMethodResponse(TypedDict):
  """A single payment method."""

  payment_method: PaymentMethod


class Get(RpcEndpoint):
  """Get a single payment method linked to the calling CDP API Key's Coinbase account."""

  async def get(
    self,
    payment_method_id: str,
    *,
    validate: bool | None = None,
  ) -> GetPaymentMethodResponse:
    """Get a single payment method linked to the calling CDP API Key's Coinbase account.

    Args:
      payment_method_id: The payment method's id. See `advanced_trade.payment_methods.list` for the account's available payment methods and their ids.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/api-reference/advanced-trade-api/rest-api/payment-methods/get-payment-method)
    """
    return await self.request(
      Request(payment_method_id=payment_method_id),
      method='GET',
      path='/api/v3/brokerage/payment_methods/{payment_method_id}',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=GetPaymentMethodResponse,
    )
