# Generated by typed-dev — see CONTRIBUTING.md.
from dataclasses import dataclass
from typed_coinbase.core.endpoint.rpc import RpcClient, RpcEndpoint
from .best_bid_ask import BestBidAsk
from .book import Book
from .candles import Candles
from .get import Get
from .list import List
from .market_trades import MarketTrades
from .public import Public


@dataclass(frozen=True, kw_only=True)
class Products(RpcEndpoint):
  """`products` endpoints."""

  best_bid_ask: BestBidAsk
  book: Book
  candles: Candles
  get: Get
  list: List
  market_trades: MarketTrades
  public: Public

  @classmethod
  def new(cls, *, client: RpcClient) -> 'Products':
    """Construct every child endpoint from one shared client."""
    return cls(
      client=client,
      best_bid_ask=BestBidAsk(client=client),
      book=Book(client=client),
      candles=Candles(client=client),
      get=Get(client=client),
      list=List(client=client),
      market_trades=MarketTrades(client=client),
      public=Public.new(client=client),
    )
