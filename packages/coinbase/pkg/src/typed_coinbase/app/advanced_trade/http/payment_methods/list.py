# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import TypedDict
from typed_coinbase.core.endpoint.rpc import RpcEndpoint
from typed_coinbase.schemas import PaymentMethod


class GetPaymentMethodsResponse(TypedDict):
  """The account's linked payment methods."""

  payment_methods: list[PaymentMethod]
  """The account's linked payment methods."""


class List(RpcEndpoint):
  """List the payment methods linked to the calling CDP API Key's Coinbase account."""

  async def list(self, *, validate: bool | None = None) -> GetPaymentMethodsResponse:
    """List the payment methods linked to the calling CDP API Key's Coinbase account.

    Args:
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/api-reference/advanced-trade-api/rest-api/payment-methods/list-payment-methods)
    """
    return await self.request(
      None,
      method='GET',
      path='/api/v3/brokerage/payment_methods',
      meta={'signed': True},
      validate=validate,
      response_type=GetPaymentMethodsResponse,
    )
