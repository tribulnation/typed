# Generated by typed-dev — see CONTRIBUTING.md.
from dataclasses import dataclass
from typed_coinbase.core.endpoint.rpc import RpcClient, RpcEndpoint
from .allocate import Allocate
from .balances import Balances
from .multi_asset_collateral import MultiAssetCollateral
from .portfolio_summary import PortfolioSummary
from .positions import Positions


@dataclass(frozen=True, kw_only=True)
class Perpetuals(RpcEndpoint):
  """`perpetuals` endpoints."""

  allocate: Allocate
  balances: Balances
  multi_asset_collateral: MultiAssetCollateral
  portfolio_summary: PortfolioSummary
  positions: Positions

  @classmethod
  def new(cls, *, client: RpcClient) -> 'Perpetuals':
    """Construct every child endpoint from one shared client."""
    return cls(
      client=client,
      allocate=Allocate(client=client),
      balances=Balances(client=client),
      multi_asset_collateral=MultiAssetCollateral(client=client),
      portfolio_summary=PortfolioSummary(client=client),
      positions=Positions.new(client=client),
    )
