# Generated by typed-dev — see CONTRIBUTING.md.
from functools import cached_property
from .balance_summary import BalanceSummary
from .intraday_margin import IntradayMargin
from .positions import Positions
from .sweeps import Sweeps


class Futures(BalanceSummary):
  """CFTC-regulated futures (CFM) trading on Advanced Trade — balance summary, open positions, intraday margin settings, and scheduling or cancelling a sweep of futures funds back to spot.

  References:
    - [Upstream docs](https://docs.cdp.coinbase.com/coinbase-app/advanced-trade-apis/guides/futures)
  """

  @cached_property
  def intraday_margin(self) -> IntradayMargin:
    """CFM futures intraday margin window setting — read the current margin window and toggle whether the account opts into intraday (extended-hours) margin rates.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/advanced-trade-api/rest-api/futures/get-intraday-margin-setting)
    """
    return IntradayMargin(client=self.client)

  @cached_property
  def positions(self) -> Positions:
    """CFM futures open positions — list every open futures position, or look one up by product id.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/advanced-trade-api/rest-api/futures/list-futures-positions)
    """
    return Positions(client=self.client)

  @cached_property
  def sweeps(self) -> Sweeps:
    """CFM futures sweeps — schedule or cancel the standing sweep that moves futures funds back to the CBI spot account, and list pending sweeps.

    References:
      - [Upstream docs](https://docs.cdp.coinbase.com/api-reference/advanced-trade-api/rest-api/futures/list-futures-sweeps)
    """
    return Sweeps(client=self.client)
