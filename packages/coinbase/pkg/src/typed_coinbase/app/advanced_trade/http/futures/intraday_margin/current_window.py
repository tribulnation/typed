# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_coinbase.core import TimestampIso
from typed_coinbase.core.endpoint.rpc import RpcEndpoint


class MarginWindow(TypedDict):
  """Details of the current margin window."""

  margin_window_type: Literal[
    'MARGIN_WINDOW_TYPE_UNSPECIFIED',
    'MARGIN_WINDOW_TYPE_OVERNIGHT',
    'MARGIN_WINDOW_TYPE_WEEKEND',
    'MARGIN_WINDOW_TYPE_INTRADAY',
    'MARGIN_WINDOW_TYPE_TRANSITION',
  ]
  """Which kind of margin window is currently active."""
  end_time: TimestampIso
  """The end time of the margin window, RFC 3339."""


class Request(TypedDict):
  margin_profile_type: NotRequired[
    Literal[
      'MARGIN_PROFILE_TYPE_UNSPECIFIED',
      'MARGIN_PROFILE_TYPE_RETAIL_REGULAR',
      'MARGIN_PROFILE_TYPE_RETAIL_INTRADAY_MARGIN_1',
    ]
  ]
  """The margin profile type for the account."""


class GetCurrentMarginWindowResponse(TypedDict):
  """The account's current futures margin window."""

  margin_window: MarginWindow
  is_intraday_margin_killswitch_enabled: bool
  """Whether the intraday margin killswitch is enabled."""
  is_intraday_margin_enrollment_killswitch_enabled: bool
  """Whether the intraday margin enrollment killswitch is enabled."""


class CurrentWindow(RpcEndpoint):
  """Get the current futures margin window and whether intraday margin is enabled for the account. Not documented in `spec/discovery.md`'s original survey — found while specmaking the futures/CFM surface; see the endpoint's own notes."""

  async def current_window(
    self,
    margin_profile_type: Literal[
      'MARGIN_PROFILE_TYPE_UNSPECIFIED',
      'MARGIN_PROFILE_TYPE_RETAIL_REGULAR',
      'MARGIN_PROFILE_TYPE_RETAIL_INTRADAY_MARGIN_1',
    ]
    | None = None,
    *,
    validate: bool | None = None,
  ) -> GetCurrentMarginWindowResponse:
    """Get the current futures margin window and whether intraday margin is enabled for the account. Not documented in `spec/discovery.md`'s original survey — found while specmaking the futures/CFM surface; see the endpoint's own notes.

    Args:
      margin_profile_type: The margin profile type for the account.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.cdp.coinbase.com/api-reference/advanced-trade-api/rest-api/futures/get-current-margin-window)
    """
    request: Request = Request()
    if margin_profile_type is not None:
      request['margin_profile_type'] = margin_profile_type
    return await self.request(
      request,
      method='GET',
      path='/api/v3/brokerage/cfm/intraday/current_margin_window',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=GetCurrentMarginWindowResponse,
    )
