# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, Sequence, TypedDict
from typed_etherscan.core.rest import RpcEndpoint


class Erc721TokenBalance(TypedDict):
  """One ERC-721 collection held by the queried address."""

  TokenAddress: NotRequired[str]
  """Contract address of the ERC-721 collection."""
  TokenName: NotRequired[str]
  """Name of the collection."""
  TokenSymbol: NotRequired[str]
  """Symbol of the collection."""
  TokenQuantity: NotRequired[int]
  """Number of tokens from this collection held by the address."""


class Request(TypedDict):
  chainid: NotRequired[str]
  """Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet."""
  module: Literal['account']
  """Fixed Etherscan API module selector for this operation; use `account`."""
  action: Literal['addresstokennftbalance']
  """Fixed Etherscan API action selector for this operation; use `addresstokennftbalance`."""
  address: str
  """Wallet address whose token holdings or inventory should be returned."""
  page: NotRequired[int]
  """Page number for paginated results, starting at 1."""
  offset: NotRequired[int]
  """Maximum number of records to return per page."""


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: list[Erc721TokenBalance]
  """ERC-721 token holdings for the queried address."""


class AddressNftBalance(RpcEndpoint):
  """Get the ERC-721 token contracts and balances held by an address."""

  def address_nft_balance_paged(
    self,
    *,
    chainid: str | None = None,
    module: Literal['account'] = 'account',
    action: Literal['addresstokennftbalance'] = 'addresstokennftbalance',
    address: str,
    offset: int | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[Erc721TokenBalance, int]:
    """Get the ERC-721 token contracts and balances held by an address.

    Paged variant of `address_nft_balance`: Requests `page` from 1 upwards and stops on the first page shorter than `offset`. Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `account`.
      action: Fixed Etherscan API action selector for this operation; use `addresstokennftbalance`.
      address: Wallet address whose token holdings or inventory should be returned.
      offset: Maximum number of records to return per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/addresstokennftbalance)
    """

    async def next(page: int) -> tuple[Sequence[Erc721TokenBalance], int | None]:
      response = await self.address_nft_balance(
        chainid=chainid,
        module=module,
        action=action,
        address=address,
        offset=offset,
        page=page,
        validate=validate,
      )
      rows = response.get('result')
      rows = rows if rows is not None else []
      if not rows or len(rows) < (offset if offset is not None else 10):
        return rows, None
      return rows, page + 1

    return PaginatedResponse(1, next)

  async def address_nft_balance(
    self,
    *,
    chainid: str | None = None,
    module: Literal['account'] = 'account',
    action: Literal['addresstokennftbalance'] = 'addresstokennftbalance',
    address: str,
    page: int | None = None,
    offset: int | None = None,
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Get the ERC-721 token contracts and balances held by an address.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `account`.
      action: Fixed Etherscan API action selector for this operation; use `addresstokennftbalance`.
      address: Wallet address whose token holdings or inventory should be returned.
      page: Page number for paginated results, starting at 1.
      offset: Maximum number of records to return per page.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/addresstokennftbalance)
    """
    request: Request = Request(module=module, action=action, address=address)
    if chainid is not None:
      request['chainid'] = chainid
    if page is not None:
      request['page'] = page
    if offset is not None:
      request['offset'] = offset
    return await self.request(
      request,
      method='GET',
      path='/v2/api',
      meta={'credentials': True},
      validate=validate,
      request_type=Request,
      response_type=EtherscanResponse,
    )
