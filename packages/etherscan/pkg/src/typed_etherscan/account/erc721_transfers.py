# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, AsyncIterator, Literal, NotRequired, TypedDict
from typed_etherscan.core.rest import RpcEndpoint


class EtherscanListResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: list[dict[str, Any]]
  """List of records returned by Etherscan. Empty when no records match."""


class Request(TypedDict):
  chainid: NotRequired[str]
  """Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet."""
  module: Literal['account']
  """Fixed Etherscan API module selector for this operation; use `account`."""
  action: Literal['tokennfttx']
  """Fixed Etherscan API action selector for this operation; use `tokennfttx`."""
  address: str
  """Account or contract address to query."""
  contractaddress: str
  """Token or contract address used to scope the query."""
  startblock: NotRequired[int]
  """Lowest block number to include in the result set."""
  endblock: NotRequired[int]
  """Highest block number to include in the result set."""
  page: NotRequired[int]
  """Page number for paginated results, starting at 1."""
  offset: NotRequired[int]
  """Maximum number of records to return per page."""
  sort: NotRequired[str]
  """Sort order for returned records, either ascending or descending."""


class Erc721Transfers(RpcEndpoint):
  """Get ERC-721 token transfer events for an address, optionally filtered by contract and block range."""

  async def erc721_transfers_paged(
    self,
    *,
    chainid: str | None = None,
    module: Literal['account'] = 'account',
    action: Literal['tokennfttx'] = 'tokennfttx',
    address: str,
    contractaddress: str,
    startblock: int | None = None,
    endblock: int | None = None,
    offset: int | None = None,
    sort: str | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[EtherscanListResponse]:
    """Get ERC-721 token transfer events for an address, optionally filtered by contract and block range.

    Paged variant of `erc721_transfers`: Requests `page` from 1 upwards and stops on the first page shorter than `offset`, or after `max_pages` pages when one is given.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `account`.
      action: Fixed Etherscan API action selector for this operation; use `tokennfttx`.
      address: Account or contract address to query.
      contractaddress: Token or contract address used to scope the query.
      startblock: Lowest block number to include in the result set.
      endblock: Highest block number to include in the result set.
      offset: Maximum number of records to return per page.
      sort: Sort order for returned records, either ascending or descending.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/tokennfttx)
    """
    page = 1
    pages = 0
    while True:
      response = await self.erc721_transfers(
        chainid=chainid,
        module=module,
        action=action,
        address=address,
        contractaddress=contractaddress,
        startblock=startblock,
        endblock=endblock,
        offset=offset,
        sort=sort,
        page=page,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      rows = response.get('result') if response is not None else None
      if not rows or (offset is not None and len(rows) < offset):
        break
      page += 1

  async def erc721_transfers(
    self,
    *,
    chainid: str | None = None,
    module: Literal['account'] = 'account',
    action: Literal['tokennfttx'] = 'tokennfttx',
    address: str,
    contractaddress: str,
    startblock: int | None = None,
    endblock: int | None = None,
    page: int | None = None,
    offset: int | None = None,
    sort: str | None = None,
    validate: bool | None = None,
  ) -> EtherscanListResponse:
    """Get ERC-721 token transfer events for an address, optionally filtered by contract and block range.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `account`.
      action: Fixed Etherscan API action selector for this operation; use `tokennfttx`.
      address: Account or contract address to query.
      contractaddress: Token or contract address used to scope the query.
      startblock: Lowest block number to include in the result set.
      endblock: Highest block number to include in the result set.
      page: Page number for paginated results, starting at 1.
      offset: Maximum number of records to return per page.
      sort: Sort order for returned records, either ascending or descending.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/tokennfttx)
    """
    request: Request = Request(
      module=module, action=action, address=address, contractaddress=contractaddress
    )
    if chainid is not None:
      request['chainid'] = chainid
    if startblock is not None:
      request['startblock'] = startblock
    if endblock is not None:
      request['endblock'] = endblock
    if page is not None:
      request['page'] = page
    if offset is not None:
      request['offset'] = offset
    if sort is not None:
      request['sort'] = sort
    return await self.request(
      request,
      method='GET',
      path='/v2/api',
      meta={'credentials': True},
      validate=validate,
      request_type=Request,
      response_type=EtherscanListResponse,
    )
