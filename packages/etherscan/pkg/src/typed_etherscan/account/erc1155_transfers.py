# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, AsyncIterator, Literal, NotRequired, TypedDict
from typed_etherscan.core.rest import RpcEndpoint


Erc1155TransferKeywords = TypedDict(
  'Erc1155TransferKeywords', {'from': NotRequired[Any]}
)
"""
- `from`: from field returned by Etherscan.
"""


class Erc1155Transfer(Erc1155TransferKeywords):
  """Result item returned by Etherscan."""

  blockNumber: NotRequired[Any]
  """blockNumber field returned by Etherscan."""
  timeStamp: NotRequired[Any]
  """timeStamp field returned by Etherscan."""
  hash: NotRequired[Any]
  """hash field returned by Etherscan."""
  nonce: NotRequired[Any]
  """nonce field returned by Etherscan."""
  blockHash: NotRequired[Any]
  """blockHash field returned by Etherscan."""
  transactionIndex: NotRequired[Any]
  """transactionIndex field returned by Etherscan."""
  gas: NotRequired[Any]
  """gas field returned by Etherscan."""
  gasPrice: NotRequired[Any]
  """gasPrice field returned by Etherscan."""
  gasUsed: NotRequired[Any]
  """gasUsed field returned by Etherscan."""
  cumulativeGasUsed: NotRequired[Any]
  """cumulativeGasUsed field returned by Etherscan."""
  input: NotRequired[Any]
  """input field returned by Etherscan."""
  methodId: NotRequired[Any]
  """methodId field returned by Etherscan."""
  functionName: NotRequired[Any]
  """functionName field returned by Etherscan."""
  contractAddress: NotRequired[Any]
  """contractAddress field returned by Etherscan."""
  to: NotRequired[Any]
  """to field returned by Etherscan."""
  tokenID: NotRequired[Any]
  """tokenID field returned by Etherscan."""
  tokenValue: NotRequired[Any]
  """tokenValue field returned by Etherscan."""
  tokenName: NotRequired[Any]
  """tokenName field returned by Etherscan."""
  tokenSymbol: NotRequired[Any]
  """tokenSymbol field returned by Etherscan."""
  confirmations: NotRequired[Any]
  """confirmations field returned by Etherscan."""


class Request(TypedDict):
  chainid: NotRequired[str]
  """Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet."""
  module: Literal['account']
  """Fixed Etherscan API module selector for this operation; use `account`."""
  action: Literal['token1155tx']
  """Fixed Etherscan API action selector for this operation; use `token1155tx`."""
  address: str
  """Account or contract address to query."""
  startblock: NotRequired[int]
  """Lowest block number to include in the result set."""
  endblock: NotRequired[int]
  """Highest block number to include in the result set."""
  page: NotRequired[int]
  """Page number for paginated results, starting at 1."""
  offset: NotRequired[int]
  """Maximum number of records to return per page."""
  sort: NotRequired[str]
  """Sort order for returned records, either ascending or descending."""


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: list[Erc1155Transfer]
  """List of result objects returned by Etherscan."""


class Erc1155Transfers(RpcEndpoint):
  """Get ERC-1155 token transfer events for an address, optionally filtered by block range."""

  async def erc1155_transfers_paged(
    self,
    *,
    chainid: str | None = None,
    module: Literal['account'] = 'account',
    action: Literal['token1155tx'] = 'token1155tx',
    address: str,
    startblock: int | None = None,
    endblock: int | None = None,
    offset: int | None = None,
    sort: str | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[EtherscanResponse]:
    """Get ERC-1155 token transfer events for an address, optionally filtered by block range.

    Paged variant of `erc1155_transfers`: Requests `page` from 1 upwards and stops on the first page shorter than `offset`, or after `max_pages` pages when one is given.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `account`.
      action: Fixed Etherscan API action selector for this operation; use `token1155tx`.
      address: Account or contract address to query.
      startblock: Lowest block number to include in the result set.
      endblock: Highest block number to include in the result set.
      offset: Maximum number of records to return per page.
      sort: Sort order for returned records, either ascending or descending.
      validate: Override this call's response validation; falls back to the client-level default when omitted.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/token1155tx)
    """
    page = 1
    pages = 0
    while True:
      response = await self.erc1155_transfers(
        chainid=chainid,
        module=module,
        action=action,
        address=address,
        startblock=startblock,
        endblock=endblock,
        offset=offset,
        sort=sort,
        page=page,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      rows = response.get('result') if response is not None else None
      if not rows or (offset is not None and len(rows) < offset):
        break
      page += 1

  async def erc1155_transfers(
    self,
    *,
    chainid: str | None = None,
    module: Literal['account'] = 'account',
    action: Literal['token1155tx'] = 'token1155tx',
    address: str,
    startblock: int | None = None,
    endblock: int | None = None,
    page: int | None = None,
    offset: int | None = None,
    sort: str | None = None,
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Get ERC-1155 token transfer events for an address, optionally filtered by block range.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `account`.
      action: Fixed Etherscan API action selector for this operation; use `token1155tx`.
      address: Account or contract address to query.
      startblock: Lowest block number to include in the result set.
      endblock: Highest block number to include in the result set.
      page: Page number for paginated results, starting at 1.
      offset: Maximum number of records to return per page.
      sort: Sort order for returned records, either ascending or descending.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/token1155tx)
    """
    request: Request = Request(module=module, action=action, address=address)
    if chainid is not None:
      request['chainid'] = chainid
    if startblock is not None:
      request['startblock'] = startblock
    if endblock is not None:
      request['endblock'] = endblock
    if page is not None:
      request['page'] = page
    if offset is not None:
      request['offset'] = offset
    if sort is not None:
      request['sort'] = sort
    return await self.request(
      request,
      method='GET',
      path='/v2/api',
      meta={'credentials': True},
      validate=validate,
      request_type=Request,
      response_type=EtherscanResponse,
    )
