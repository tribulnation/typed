# Generated by typed-dev — see CONTRIBUTING.md.
from typed_core import PaginatedResponse
from typing_extensions import Literal, NotRequired, Sequence, TypedDict
from typed_etherscan.core import TimestampSeconds
from typed_etherscan.core.rest import RpcEndpoint


AccountTransactionKeywords = TypedDict('AccountTransactionKeywords', {'from': str})
"""
- `from`: Sender address.
"""


class AccountTransaction(AccountTransactionKeywords):
  """One normal (external) transaction involving the queried address."""

  blockNumber: int
  """Block number the transaction was included in."""
  timeStamp: TimestampSeconds
  """Block timestamp, Unix seconds."""
  hash: str
  """Transaction hash."""
  nonce: int
  """Sender account nonce of the transaction."""
  blockHash: str
  """Hash of the block the transaction was included in."""
  to: str
  """Recipient address. Empty string on a contract-creation transaction."""
  transactionIndex: int
  """Position of the transaction within its block."""
  value: int
  """Ether value transferred, in wei."""
  gas: int
  """Gas limit of the transaction, in gas units."""
  gasPrice: int
  """Gas price paid, in wei per gas unit."""
  input: str
  """Transaction calldata, hex-encoded. `0x` on a plain transfer."""
  methodId: str
  """First four bytes of the calldata (the function selector), hex-encoded. `0x` on a plain transfer."""
  functionName: str
  """Decoded function signature of the call, when Etherscan knows the ABI. Empty string otherwise."""
  contractAddress: str
  """Address of the contract created by this transaction. Empty string when the transaction created none."""
  cumulativeGasUsed: int
  """Gas consumed by the block up to and including this transaction, in gas units."""
  txreceipt_status: Literal['', '0', '1']
  """Receipt status: `1` success, `0` failure. Empty string for pre-Byzantium blocks, which carry no receipt status."""
  gasUsed: int
  """Gas consumed by the transaction, in gas units."""
  confirmations: int
  """Number of blocks mined on top of the containing block at query time."""
  isError: Literal['0', '1']
  """Execution result: `0` success, `1` failure."""


class Request(TypedDict):
  chainid: NotRequired[str]
  """Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet."""
  module: Literal['account']
  """Fixed Etherscan API module selector for this operation; use `account`."""
  action: Literal['txlist']
  """Fixed Etherscan API action selector for this operation; use `txlist`."""
  address: str
  """Account or contract address to query."""
  startblock: NotRequired[int]
  """Lowest block number to include in the result set."""
  endblock: NotRequired[int]
  """Highest block number to include in the result set."""
  page: NotRequired[int]
  """Page number for paginated results, starting at 1."""
  offset: NotRequired[int]
  """Maximum number of records to return per page."""
  sort: NotRequired[Literal['asc', 'desc']]
  """Sort order: `asc` for the oldest records first, `desc` for the latest first."""


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: list[AccountTransaction]
  """List of result objects returned by Etherscan."""


class Transactions(RpcEndpoint):
  """Get normal transactions for an address, optionally filtered by block range and pagination settings."""

  def transactions_paged(
    self,
    *,
    chainid: str | None = None,
    module: Literal['account'] = 'account',
    action: Literal['txlist'] = 'txlist',
    address: str,
    startblock: int | None = None,
    endblock: int | None = None,
    offset: int | None = None,
    sort: Literal['asc', 'desc'] | None = None,
    validate: bool | None = None,
  ) -> PaginatedResponse[AccountTransaction, int]:
    """Get normal transactions for an address, optionally filtered by block range and pagination settings.

    Paged variant of `transactions`: Requests `page` from 1 upwards and stops on the first page shorter than `offset`. Awaitable (flattens every page) or async-iterable (one page at a time).

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `account`.
      action: Fixed Etherscan API action selector for this operation; use `txlist`.
      address: Account or contract address to query.
      startblock: Lowest block number to include in the result set.
      endblock: Highest block number to include in the result set.
      offset: Maximum number of records to return per page.
      sort: Sort order: `asc` for the oldest records first, `desc` for the latest first.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/txlist)
    """

    async def next(page: int) -> tuple[Sequence[AccountTransaction], int | None]:
      response = await self.transactions(
        chainid=chainid,
        module=module,
        action=action,
        address=address,
        startblock=startblock,
        endblock=endblock,
        offset=offset,
        sort=sort,
        page=page,
        validate=validate,
      )
      rows = response.get('result')
      rows = rows if rows is not None else []
      if not rows or len(rows) < (offset if offset is not None else 10):
        return rows, None
      return rows, page + 1

    return PaginatedResponse(1, next)

  async def transactions(
    self,
    *,
    chainid: str | None = None,
    module: Literal['account'] = 'account',
    action: Literal['txlist'] = 'txlist',
    address: str,
    startblock: int | None = None,
    endblock: int | None = None,
    page: int | None = None,
    offset: int | None = None,
    sort: Literal['asc', 'desc'] | None = None,
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Get normal transactions for an address, optionally filtered by block range and pagination settings.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `account`.
      action: Fixed Etherscan API action selector for this operation; use `txlist`.
      address: Account or contract address to query.
      startblock: Lowest block number to include in the result set.
      endblock: Highest block number to include in the result set.
      page: Page number for paginated results, starting at 1.
      offset: Maximum number of records to return per page.
      sort: Sort order: `asc` for the oldest records first, `desc` for the latest first.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/txlist)
    """
    request: Request = Request(module=module, action=action, address=address)
    if chainid is not None:
      request['chainid'] = chainid
    if startblock is not None:
      request['startblock'] = startblock
    if endblock is not None:
      request['endblock'] = endblock
    if page is not None:
      request['page'] = page
    if offset is not None:
      request['offset'] = offset
    if sort is not None:
      request['sort'] = sort
    return await self.request(
      request,
      method='GET',
      path='/v2/api',
      meta={'credentials': True},
      validate=validate,
      request_type=Request,
      response_type=EtherscanResponse,
    )
