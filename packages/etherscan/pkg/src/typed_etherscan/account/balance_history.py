# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_etherscan.core.rest import RpcEndpoint


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: Any
  """Endpoint-specific payload."""


class Request(TypedDict):
  chainid: NotRequired[str]
  """Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet."""
  module: Literal['account']
  """Fixed Etherscan API module selector for this operation; use `account`."""
  action: Literal['balancehistory']
  """Fixed Etherscan API action selector for this operation; use `balancehistory`."""
  address: str
  """Account or contract address to query."""
  blockno: int
  """Historical block number at which to evaluate the requested value."""


class BalanceHistory(RpcEndpoint):
  """Get the historical native coin balance for an address at a specific block number."""

  async def balance_history(
    self,
    *,
    chainid: str | None = None,
    module: Literal['account'] = 'account',
    action: Literal['balancehistory'] = 'balancehistory',
    address: str,
    blockno: int,
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Get the historical native coin balance for an address at a specific block number.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `account`.
      action: Fixed Etherscan API action selector for this operation; use `balancehistory`.
      address: Account or contract address to query.
      blockno: Historical block number at which to evaluate the requested value.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/balancehistory)
    """
    request: Request = Request(
      module=module, action=action, address=address, blockno=blockno
    )
    if chainid is not None:
      request['chainid'] = chainid
    return await self.request(
      request,
      method='GET',
      path='/v2/api',
      meta={'credentials': True},
      validate=validate,
      request_type=Request,
      response_type=EtherscanResponse,
    )
