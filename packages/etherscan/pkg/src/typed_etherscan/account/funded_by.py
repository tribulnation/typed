# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_etherscan.core import TimestampSeconds
from typed_etherscan.core.rest import RpcEndpoint


class FundedByResult(TypedDict):
  """The transaction that first funded the queried address."""

  block: NotRequired[int]
  """Block number of the funding transaction."""
  timeStamp: NotRequired[TimestampSeconds]
  """Time of the funding transaction, as a UNIX timestamp in seconds."""
  fundingAddress: NotRequired[str]
  """Address that first funded the queried address."""
  fundingTxn: NotRequired[str]
  """Hash of the funding transaction."""
  value: NotRequired[int]
  """Amount transferred in the funding transaction, in wei."""


class Request(TypedDict):
  chainid: NotRequired[str]
  """Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet."""
  module: Literal['account']
  """Fixed Etherscan API module selector for this operation; use `account`."""
  action: Literal['fundedby']
  """Fixed Etherscan API action selector for this operation; use `fundedby`."""
  address: str
  """Address whose initial funding transaction should be looked up."""


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: FundedByResult


class FundedBy(RpcEndpoint):
  """Get the address that first funded the supplied address and the funding transaction details."""

  async def funded_by(
    self,
    *,
    chainid: str | None = None,
    module: Literal['account'] = 'account',
    action: Literal['fundedby'] = 'fundedby',
    address: str,
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Get the address that first funded the supplied address and the funding transaction details.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `account`.
      action: Fixed Etherscan API action selector for this operation; use `fundedby`.
      address: Address whose initial funding transaction should be looked up.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/fundedby)
    """
    request: Request = Request(module=module, action=action, address=address)
    if chainid is not None:
      request['chainid'] = chainid
    return await self.request(
      request,
      method='GET',
      path='/v2/api',
      meta={'credentials': True},
      validate=validate,
      request_type=Request,
      response_type=EtherscanResponse,
    )
