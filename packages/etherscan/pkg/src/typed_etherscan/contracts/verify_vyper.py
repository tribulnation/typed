# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_etherscan.core.rest import RpcEndpoint


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: str
  """GUID identifying this verification submission; pass it to the verification-status endpoint to poll the outcome."""


class Request(TypedDict):
  chainid: NotRequired[str]
  """Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet."""
  module: Literal['contract']
  """Fixed Etherscan API module selector for this operation; use `contract`."""
  action: Literal['verifysourcecode']
  """Fixed Etherscan API action selector for this operation; use `verifysourcecode`."""
  contractaddress: str
  """Contract address being submitted for verification."""
  sourceCode: str
  """Contract source code or standard JSON input submitted for verification."""
  codeformat: NotRequired[str]
  """Source-code format expected by Etherscan for the verification request."""
  contractname: str
  """Fully qualified contract name to compile and verify."""
  compilerversion: str
  """Exact compiler version used to build the deployed contract."""
  optimizationUsed: NotRequired[str]
  """Whether compiler optimization was enabled for the deployed bytecode."""
  constructorArguments: NotRequired[str]
  """ABI-encoded constructor arguments appended to the deployed bytecode."""


class VerifyVyper(RpcEndpoint):
  """Submit Vyper source code for contract verification."""

  async def verify_vyper(
    self,
    *,
    chainid: str | None = None,
    module: Literal['contract'] = 'contract',
    action: Literal['verifysourcecode'] = 'verifysourcecode',
    contractaddress: str,
    source_code: str,
    codeformat: str | None = None,
    contractname: str,
    compilerversion: str,
    optimization_used: str | None = None,
    constructor_arguments: str | None = None,
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Submit Vyper source code for contract verification.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `contract`.
      action: Fixed Etherscan API action selector for this operation; use `verifysourcecode`.
      contractaddress: Contract address being submitted for verification.
      source_code: Contract source code or standard JSON input submitted for verification.
      codeformat: Source-code format expected by Etherscan for the verification request.
      contractname: Fully qualified contract name to compile and verify.
      compilerversion: Exact compiler version used to build the deployed contract.
      optimization_used: Whether compiler optimization was enabled for the deployed bytecode.
      constructor_arguments: ABI-encoded constructor arguments appended to the deployed bytecode.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/verifyvyper)
    """
    request: Request = Request(
      module=module,
      action=action,
      contractaddress=contractaddress,
      sourceCode=source_code,
      contractname=contractname,
      compilerversion=compilerversion,
    )
    if chainid is not None:
      request['chainid'] = chainid
    if codeformat is not None:
      request['codeformat'] = codeformat
    if optimization_used is not None:
      request['optimizationUsed'] = optimization_used
    if constructor_arguments is not None:
      request['constructorArguments'] = constructor_arguments
    return await self.request(
      request,
      method='POST',
      path='/v2/api',
      meta={'credentials': True},
      validate=validate,
      request_type=Request,
      response_type=EtherscanResponse,
    )
