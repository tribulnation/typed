# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_etherscan.core.rest import RpcEndpoint


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: str
  """Result message of the proxy contract verification, e.g. `Pass - Verified` on success or an error/pending description on failure."""


class Request(TypedDict):
  chainid: NotRequired[str]
  """Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet."""
  module: Literal['contract']
  """Fixed Etherscan API module selector for this operation; use `contract`."""
  action: Literal['checkproxyverification']
  """Fixed Etherscan API action selector for this operation; use `checkproxyverification`."""
  guid: str
  """Verification request identifier returned by a previous verification submission."""


class ProxyVerificationStatus(RpcEndpoint):
  """Check the verification status of a proxy contract verification request."""

  async def proxy_verification_status(
    self,
    *,
    chainid: str | None = None,
    module: Literal['contract'] = 'contract',
    action: Literal['checkproxyverification'] = 'checkproxyverification',
    guid: str,
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Check the verification status of a proxy contract verification request.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `contract`.
      action: Fixed Etherscan API action selector for this operation; use `checkproxyverification`.
      guid: Verification request identifier returned by a previous verification submission.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/checkproxyverification)
    """
    request: Request = Request(module=module, action=action, guid=guid)
    if chainid is not None:
      request['chainid'] = chainid
    return await self.request(
      request,
      method='GET',
      path='/v2/api',
      meta={'credentials': True},
      validate=validate,
      request_type=Request,
      response_type=EtherscanResponse,
    )
