# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_etherscan.core import TimestampSeconds
from typed_etherscan.core.rest import RpcEndpoint


class ContractCreation(TypedDict):
  """Result item returned by Etherscan."""

  contractAddress: NotRequired[str]
  """Address of the created contract."""
  contractCreator: NotRequired[str]
  """Address that created the contract."""
  txHash: NotRequired[str]
  """Hash of the transaction that created the contract."""
  blockNumber: NotRequired[int]
  """Block number in which the contract was created."""
  timestamp: NotRequired[TimestampSeconds]
  """Time the contract was created, as a Unix timestamp in seconds (sent as a decimal string)."""
  contractFactory: NotRequired[str]
  """Address of the factory contract responsible for deployment, or an empty string when not applicable."""
  creationBytecode: NotRequired[str]
  """Bytecode submitted to create the contract, hex-encoded with a `0x` prefix."""


class Request(TypedDict):
  chainid: NotRequired[str]
  """Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet."""
  module: Literal['contract']
  """Fixed Etherscan API module selector for this operation; use `contract`."""
  action: Literal['getcontractcreation']
  """Fixed Etherscan API action selector for this operation; use `getcontractcreation`."""
  contractaddresses: str
  """Comma-separated contract addresses whose creation information should be returned."""


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: list[ContractCreation]
  """List of result objects returned by Etherscan."""


class Creation(RpcEndpoint):
  """Get contract creator addresses and creation transaction hashes for contract addresses."""

  async def creation(
    self,
    *,
    chainid: str | None = None,
    module: Literal['contract'] = 'contract',
    action: Literal['getcontractcreation'] = 'getcontractcreation',
    contractaddresses: str,
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Get contract creator addresses and creation transaction hashes for contract addresses.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `contract`.
      action: Fixed Etherscan API action selector for this operation; use `getcontractcreation`.
      contractaddresses: Comma-separated contract addresses whose creation information should be returned.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/getcontractcreation)
    """
    request: Request = Request(
      module=module, action=action, contractaddresses=contractaddresses
    )
    if chainid is not None:
      request['chainid'] = chainid
    return await self.request(
      request,
      method='GET',
      path='/v2/api',
      meta={'credentials': True},
      validate=validate,
      request_type=Request,
      response_type=EtherscanResponse,
    )
