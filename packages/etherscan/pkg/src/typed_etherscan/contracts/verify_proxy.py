# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_etherscan.core.rest import RpcEndpoint


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: str
  """GUID identifying this proxy verification submission; pass it to the proxy verification-status endpoint to poll the outcome."""


class Request(TypedDict):
  chainid: NotRequired[str]
  """Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet."""
  module: Literal['contract']
  """Fixed Etherscan API module selector for this operation; use `contract`."""
  action: Literal['verifyproxycontract']
  """Fixed Etherscan API action selector for this operation; use `verifyproxycontract`."""
  address: str
  """Proxy contract address to verify."""
  expectedimplementation: NotRequired[str]
  """Optional implementation address to enforce during proxy verification."""


class VerifyProxy(RpcEndpoint):
  """Submit a proxy contract for proxy verification."""

  async def verify_proxy(
    self,
    *,
    chainid: str | None = None,
    module: Literal['contract'] = 'contract',
    action: Literal['verifyproxycontract'] = 'verifyproxycontract',
    address: str,
    expectedimplementation: str | None = None,
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Submit a proxy contract for proxy verification.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `contract`.
      action: Fixed Etherscan API action selector for this operation; use `verifyproxycontract`.
      address: Proxy contract address to verify.
      expectedimplementation: Optional implementation address to enforce during proxy verification.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/verifyproxycontract)
    """
    request: Request = Request(module=module, action=action, address=address)
    if chainid is not None:
      request['chainid'] = chainid
    if expectedimplementation is not None:
      request['expectedimplementation'] = expectedimplementation
    return await self.request(
      request,
      method='POST',
      path='/v2/api',
      meta={'credentials': True},
      validate=validate,
      request_type=Request,
      response_type=EtherscanResponse,
    )
