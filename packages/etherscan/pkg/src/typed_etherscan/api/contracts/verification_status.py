# Generated by typed-dev — see CONTRIBUTING.md.
"""Check the source-code verification status for a submitted verification GUID."""

from typing_extensions import Any, Literal, TypedDict
from typed_core.validation import validator
from typed_etherscan.core import RpcEndpoint


class CheckVerifyStatusRequest(TypedDict):
  guid: str
  """Verification request identifier returned by a previous verification submission."""


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: Any
  """Endpoint-specific payload."""


validate_response = validator(EtherscanResponse)


class VerificationStatus(RpcEndpoint):
  """Check Source Code Verification Status"""

  async def verification_status(
    self,
    check_verify_status_request: CheckVerifyStatusRequest,
    *,
    chainid: str | None = None,
    module: Literal['contract'] = 'contract',
    action: Literal['checkverifystatus'] = 'checkverifystatus',
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Check the source-code verification status for a submitted verification GUID.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `contract`.
      action: Fixed Etherscan API action selector for this operation; use `checkverifystatus`.
      check_verify_status_request: Form fields submitted for Check Source Code Verification Status.
      validate: Whether to validate the response.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/checkverifystatus)
    """
    params: dict = {
      'module': module,
      'action': action,
    }
    if chainid is not None:
      params['chainid'] = chainid
    return await self.authed_request(
      'POST',
      '/v2/api',
      params=params,
      data=check_verify_status_request,
      validator=validate_response,
      validate=validate,
    )
