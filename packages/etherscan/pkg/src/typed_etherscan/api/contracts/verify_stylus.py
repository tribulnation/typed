# Generated by typed-dev — see CONTRIBUTING.md.
"""Submit Stylus source code for contract verification."""

from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_etherscan.core import RpcEndpoint


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: Any
  """Endpoint-specific payload."""


class VerifyStylusContractRequest(TypedDict):
  contractaddress: str
  """Contract address being submitted for verification."""
  sourceCode: str
  """Contract source code or standard JSON input submitted for verification."""
  codeformat: NotRequired[str]
  """Source-code format expected by Etherscan for the verification request."""
  contractname: str
  """Fully qualified contract name to compile and verify."""
  compilerversion: str
  """Exact compiler version used to build the deployed contract."""
  licenseType: NotRequired[str]
  """Etherscan license type identifier for the verified source code."""


validate_response = validator(EtherscanResponse)


class VerifyStylus(RpcEndpoint):
  """Verify Stylus Source Code"""

  async def verify_stylus(
    self,
    verify_stylus_contract_request: VerifyStylusContractRequest,
    *,
    module: Literal['contract'] = 'contract',
    action: Literal['verifysourcecode'] = 'verifysourcecode',
    chainid: str | None = None,
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Submit Stylus source code for contract verification.

    Args:
      module: Fixed Etherscan API module selector for this operation; use `contract`.
      action: Fixed Etherscan API action selector for this operation; use `verifysourcecode`.
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      verify_stylus_contract_request: Form fields submitted for Verify Stylus Source Code.
      validate: Whether to validate the response.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/verifystylus)
    """
    params: dict = {
      'module': module,
      'action': action,
    }
    if chainid is not None:
      params['chainid'] = chainid
    return await self.authed_request(
      'POST',
      '/v2/api',
      params=params,
      data=verify_stylus_contract_request,
      validator=validate_response,
      validate=validate,
    )
