# Generated by typed-dev — see CONTRIBUTING.md.
"""Get the current total node count."""

from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_etherscan.core import RpcEndpoint


class NodeCount(TypedDict):
  """Result object returned by Etherscan."""

  UTCDate: NotRequired[Any]
  """UTCDate field returned by Etherscan."""
  TotalNodeCount: NotRequired[Any]
  """TotalNodeCount field returned by Etherscan."""


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: NodeCount


validate_response = validator(EtherscanResponse)


class NodeCountEndpoint(RpcEndpoint):
  """Get Total Nodes Count"""

  async def node_count(
    self,
    *,
    chainid: str | None = None,
    module: Literal['stats'] = 'stats',
    action: Literal['nodecount'] = 'nodecount',
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Get the current total node count.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `stats`.
      action: Fixed Etherscan API action selector for this operation; use `nodecount`.
      validate: Whether to validate the response.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/nodecount)
    """
    params: dict = {
      'module': module,
      'action': action,
    }
    if chainid is not None:
      params['chainid'] = chainid
    return await self.authed_request(
      'GET',
      '/v2/api',
      params=params,
      validator=validate_response,
      validate=validate,
    )
