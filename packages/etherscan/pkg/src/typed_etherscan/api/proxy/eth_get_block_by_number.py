# Generated by typed-dev — see CONTRIBUTING.md.
"""Fetch information about a block by block number."""

from typing_extensions import Any, Literal
from typed_core.validation import validator
from typed_etherscan.core import RpcEndpoint

validate_response = validator(dict[str, Any])


class EthGetBlockByNumber(RpcEndpoint):
  """eth_getBlockByNumber"""

  async def eth_get_block_by_number(
    self,
    *,
    chainid: str | None = None,
    module: Literal['proxy'] = 'proxy',
    action: Literal['eth_getBlockByNumber'] = 'eth_getBlockByNumber',
    tag: str | None = None,
    boolean: str,
    validate: bool | None = None,
  ) -> dict[str, Any]:
    """Fetch information about a block by block number.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `proxy`.
      action: Fixed Etherscan API action selector for this operation; use `eth_getBlockByNumber`.
      tag: Block number encoded as hex, or a supported block tag.
      boolean: Whether block transactions should be returned as full objects instead of hashes.
      validate: Whether to validate the response.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/ethgetblockbynumber)
    """
    params: dict = {
      'module': module,
      'action': action,
      'boolean': boolean,
    }
    if chainid is not None:
      params['chainid'] = chainid
    if tag is not None:
      params['tag'] = tag
    return await self.authed_request(
      'GET',
      '/v2/api',
      params=params,
      validator=validate_response,
      validate=validate,
    )
