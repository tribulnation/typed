# Generated by typed-dev — see CONTRIBUTING.md.
"""Submit a signed raw transaction to the network."""

from typing_extensions import Any, Literal
from typed_core.validation import validator
from typed_etherscan.core import RpcEndpoint

validate_response = validator(dict[str, Any])


class EthSendRawTransaction(RpcEndpoint):
  """eth_sendRawTransaction"""

  async def eth_send_raw_transaction(
    self,
    *,
    chainid: str | None = None,
    module: Literal['proxy'] = 'proxy',
    action: Literal['eth_sendRawTransaction'] = 'eth_sendRawTransaction',
    hex: str,
    validate: bool | None = None,
  ) -> dict[str, Any]:
    """Submit a signed raw transaction to the network.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `proxy`.
      action: Fixed Etherscan API action selector for this operation; use `eth_sendRawTransaction`.
      hex: Signed raw transaction hex string to broadcast.
      validate: Whether to validate the response.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/ethsendrawtransaction)
    """
    params: dict = {
      'module': module,
      'action': action,
      'hex': hex,
    }
    if chainid is not None:
      params['chainid'] = chainid
    return await self.authed_request(
      'GET',
      '/v2/api',
      params=params,
      validator=validate_response,
      validate=validate,
    )
