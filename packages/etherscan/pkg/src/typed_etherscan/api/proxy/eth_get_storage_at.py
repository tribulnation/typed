# Generated by typed-dev — see CONTRIBUTING.md.
"""Fetch the value from a contract storage slot at an address."""

from typing_extensions import Any, Literal
from typed_core.validation import validator
from typed_etherscan.core import RpcEndpoint

validate_response = validator(dict[str, Any])


class EthGetStorageAt(RpcEndpoint):
  """eth_getStorageAt"""

  async def eth_get_storage_at(
    self,
    *,
    chainid: str | None = None,
    module: Literal['proxy'] = 'proxy',
    action: Literal['eth_getStorageAt'] = 'eth_getStorageAt',
    address: str,
    position: str,
    tag: str | None = None,
    validate: bool | None = None,
  ) -> dict[str, Any]:
    """Fetch the value from a contract storage slot at an address.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `proxy`.
      action: Fixed Etherscan API action selector for this operation; use `eth_getStorageAt`.
      address: Contract address whose storage slot should be read.
      position: Hex-encoded storage slot position to read from the contract.
      tag: Block tag or hex block number at which to execute the read.
      validate: Whether to validate the response.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/ethgetstorageat)
    """
    params: dict = {
      'module': module,
      'action': action,
      'address': address,
      'position': position,
    }
    if chainid is not None:
      params['chainid'] = chainid
    if tag is not None:
      params['tag'] = tag
    return await self.authed_request(
      'GET',
      '/v2/api',
      params=params,
      validator=validate_response,
      validate=validate,
    )
