# Generated by typed-dev — see CONTRIBUTING.md.
"""Fetch the contract bytecode at an address."""

from typing_extensions import Any, Literal
from typed_core.validation import validator
from typed_etherscan.core import RpcEndpoint

validate_response = validator(dict[str, Any])


class EthGetCode(RpcEndpoint):
  """eth_getCode"""

  async def eth_get_code(
    self,
    *,
    chainid: str | None = None,
    module: Literal['proxy'] = 'proxy',
    action: Literal['eth_getCode'] = 'eth_getCode',
    address: str,
    tag: str | None = None,
    validate: bool | None = None,
  ) -> dict[str, Any]:
    """Fetch the contract bytecode at an address.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `proxy`.
      action: Fixed Etherscan API action selector for this operation; use `eth_getCode`.
      address: Address whose deployed bytecode should be returned.
      tag: Block tag or hex block number at which to execute the read.
      validate: Whether to validate the response.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/ethgetcode)
    """
    params: dict = {
      'module': module,
      'action': action,
      'address': address,
    }
    if chainid is not None:
      params['chainid'] = chainid
    if tag is not None:
      params['tag'] = tag
    return await self.authed_request(
      'GET',
      '/v2/api',
      params=params,
      validator=validate_response,
      validate=validate,
    )
