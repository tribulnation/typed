# Generated by typed-dev — see CONTRIBUTING.md.
"""Fetch a transaction by block number and transaction index."""

from typing_extensions import Any, Literal
from typed_core.validation import validator
from typed_etherscan.core import RpcEndpoint

validate_response = validator(dict[str, Any])


class EthGetTransactionByBlockNumberAndIndex(RpcEndpoint):
  """eth_getTransactionByBlockNumberAndIndex"""

  async def eth_get_transaction_by_block_number_and_index(
    self,
    *,
    chainid: str | None = None,
    module: Literal['proxy'] = 'proxy',
    action: Literal[
      'eth_getTransactionByBlockNumberAndIndex'
    ] = 'eth_getTransactionByBlockNumberAndIndex',
    tag: str | None = None,
    index: str,
    validate: bool | None = None,
  ) -> dict[str, Any]:
    """Fetch a transaction by block number and transaction index.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `proxy`.
      action: Fixed Etherscan API action selector for this operation; use `eth_getTransactionByBlockNumberAndIndex`.
      tag: Block tag to use for the balance query, usually `latest`.
      index: Hex-encoded transaction or uncle index within the block.
      validate: Whether to validate the response.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/ethgettransactionbyblocknumberandindex)
    """
    params: dict = {
      'module': module,
      'action': action,
      'index': index,
    }
    if chainid is not None:
      params['chainid'] = chainid
    if tag is not None:
      params['tag'] = tag
    return await self.authed_request(
      'GET',
      '/v2/api',
      params=params,
      validator=validate_response,
      validate=validate,
    )
