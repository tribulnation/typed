# Generated by typed-dev — see CONTRIBUTING.md.
"""Fetch uncle block information by block number and uncle index."""

from typing_extensions import Any, Literal
from typed_core.validation import validator
from typed_etherscan.core import RpcEndpoint

validate_response = validator(dict[str, Any])


class EthGetUncleByBlockNumberAndIndex(RpcEndpoint):
  """eth_getUncleByBlockNumberAndIndex"""

  async def eth_get_uncle_by_block_number_and_index(
    self,
    *,
    chainid: str | None = None,
    module: Literal['proxy'] = 'proxy',
    action: Literal[
      'eth_getUncleByBlockNumberAndIndex'
    ] = 'eth_getUncleByBlockNumberAndIndex',
    tag: str | None = None,
    index: str,
    validate: bool | None = None,
  ) -> dict[str, Any]:
    """Fetch uncle block information by block number and uncle index.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `proxy`.
      action: Fixed Etherscan API action selector for this operation; use `eth_getUncleByBlockNumberAndIndex`.
      tag: Block number encoded as hex, or a supported block tag.
      index: Hex-encoded transaction or uncle index within the block.
      validate: Whether to validate the response.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/ethgetunclebyblocknumberandindex)
    """
    params: dict = {
      'module': module,
      'action': action,
      'index': index,
    }
    if chainid is not None:
      params['chainid'] = chainid
    if tag is not None:
      params['tag'] = tag
    return await self.authed_request(
      'GET',
      '/v2/api',
      params=params,
      validator=validate_response,
      validate=validate,
    )
