# Generated by typed-dev — see CONTRIBUTING.md.
"""Fetch the number of transactions in a block by block number."""

from typing_extensions import Any, Literal
from typed_core.validation import validator
from typed_etherscan.core import RpcEndpoint

validate_response = validator(dict[str, Any])


class EthGetBlockTransactionCountByNumber(RpcEndpoint):
  """eth_getBlockTransactionCountByNumber"""

  async def eth_get_block_transaction_count_by_number(
    self,
    *,
    chainid: str | None = None,
    module: Literal['proxy'] = 'proxy',
    action: Literal[
      'eth_getBlockTransactionCountByNumber'
    ] = 'eth_getBlockTransactionCountByNumber',
    tag: str | None = None,
    validate: bool | None = None,
  ) -> dict[str, Any]:
    """Fetch the number of transactions in a block by block number.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `proxy`.
      action: Fixed Etherscan API action selector for this operation; use `eth_getBlockTransactionCountByNumber`.
      tag: Block number encoded as hex, or a supported block tag.
      validate: Whether to validate the response.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/ethgetblocktransactioncountbynumber)
    """
    params: dict = {
      'module': module,
      'action': action,
    }
    if chainid is not None:
      params['chainid'] = chainid
    if tag is not None:
      params['tag'] = tag
    return await self.authed_request(
      'GET',
      '/v2/api',
      params=params,
      validator=validate_response,
      validate=validate,
    )
