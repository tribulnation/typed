# Generated by typed-dev — see CONTRIBUTING.md.
"""Fetch the transaction count for an address."""

from typing_extensions import Any, Literal
from typed_core.validation import validator
from typed_etherscan.core import RpcEndpoint

validate_response = validator(dict[str, Any])


class EthGetTransactionCount(RpcEndpoint):
  """eth_getTransactionCount"""

  async def eth_get_transaction_count(
    self,
    *,
    chainid: str | None = None,
    module: Literal['proxy'] = 'proxy',
    action: Literal['eth_getTransactionCount'] = 'eth_getTransactionCount',
    address: str,
    tag: str | None = None,
    validate: bool | None = None,
  ) -> dict[str, Any]:
    """Fetch the transaction count for an address.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `proxy`.
      action: Fixed Etherscan API action selector for this operation; use `eth_getTransactionCount`.
      address: Account address whose transaction count should be returned.
      tag: Block tag to use for the balance query, usually `latest`.
      validate: Whether to validate the response.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/ethgettransactioncount)
    """
    params: dict = {
      'module': module,
      'action': action,
      'address': address,
    }
    if chainid is not None:
      params['chainid'] = chainid
    if tag is not None:
      params['tag'] = tag
    return await self.authed_request(
      'GET',
      '/v2/api',
      params=params,
      validator=validate_response,
      validate=validate,
    )
