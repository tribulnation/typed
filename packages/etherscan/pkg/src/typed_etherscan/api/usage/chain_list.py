# Generated by typed-dev — see CONTRIBUTING.md.
"""Get the list of chains supported by the Etherscan API V2."""

from typing_extensions import Literal, TypedDict
from typed_core.validation import validator
from typed_etherscan.core import RpcEndpoint


class ChainInfo(TypedDict):
  chainname: str
  """Display name of the chain."""
  chainid: str
  """Chain ID."""
  blockexplorer: str
  """Block explorer URL."""
  apiurl: str
  """Etherscan API URL for the chain."""
  status: Literal[0, 1, 2]
  """Chain API status: 0 = offline, 1 = ok, 2 = degraded."""
  comment: str
  """Status comment."""


class EtherscanChainListResponse(TypedDict):
  comments: str
  """Description of the chain list and status codes."""
  totalcount: int
  """Number of chain records returned."""
  result: list[ChainInfo]
  """Supported Etherscan chain records."""


validate_response = validator(EtherscanChainListResponse)


class ChainList(RpcEndpoint):
  """Chainlist"""

  async def chain_list(
    self, *, validate: bool | None = None
  ) -> EtherscanChainListResponse:
    """Get the list of chains supported by the Etherscan API V2.

    Args:
      validate: Whether to validate the response.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/chainlist)
    """
    return await self.request(
      'GET',
      '/v2/chainlist',
      validator=validate_response,
      validate=validate,
    )
