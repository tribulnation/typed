# Generated by typed-dev — see CONTRIBUTING.md.
"""Get L2 deposit transactions for an address."""

from typing_extensions import Any, AsyncIterator, Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_etherscan.core import RpcEndpoint

L2DepositTransactionKeywords = TypedDict(
  'L2DepositTransactionKeywords', {'from': NotRequired[Any]}
)
"""
- `from`: from field returned by Etherscan.
"""


class L2DepositTransaction(L2DepositTransactionKeywords):
  """Result item returned by Etherscan."""

  blockNumber: NotRequired[Any]
  """blockNumber field returned by Etherscan."""
  timeStamp: NotRequired[Any]
  """timeStamp field returned by Etherscan."""
  blockHash: NotRequired[Any]
  """blockHash field returned by Etherscan."""
  hash: NotRequired[Any]
  """hash field returned by Etherscan."""
  nonce: NotRequired[Any]
  """nonce field returned by Etherscan."""
  to: NotRequired[Any]
  """to field returned by Etherscan."""
  value: NotRequired[Any]
  """value field returned by Etherscan."""
  gas: NotRequired[Any]
  """gas field returned by Etherscan."""
  gasPrice: NotRequired[Any]
  """gasPrice field returned by Etherscan."""
  input: NotRequired[Any]
  """input field returned by Etherscan."""
  cumulativeGasUsed: NotRequired[Any]
  """cumulativeGasUsed field returned by Etherscan."""
  gasUsed: NotRequired[Any]
  """gasUsed field returned by Etherscan."""
  isError: NotRequired[Any]
  """isError field returned by Etherscan."""
  errDescription: NotRequired[Any]
  """errDescription field returned by Etherscan."""
  txreceipt_status: NotRequired[Any]
  """txreceipt_status field returned by Etherscan."""
  queueIndex: NotRequired[Any]
  """queueIndex field returned by Etherscan."""
  L1transactionhash: NotRequired[Any]
  """L1transactionhash field returned by Etherscan."""
  L1TxOrigin: NotRequired[Any]
  """L1TxOrigin field returned by Etherscan."""
  tokenAddress: NotRequired[Any]
  """tokenAddress field returned by Etherscan."""
  tokenSentFrom: NotRequired[Any]
  """tokenSentFrom field returned by Etherscan."""
  tokenSentTo: NotRequired[Any]
  """tokenSentTo field returned by Etherscan."""
  tokenValue: NotRequired[Any]
  """tokenValue field returned by Etherscan."""


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: list[L2DepositTransaction]
  """List of result objects returned by Etherscan."""


validate_response = validator(EtherscanResponse)


class DepositTransactions(RpcEndpoint):
  """Get Deposit Transactions by Address"""

  async def deposit_transactions(
    self,
    *,
    chainid: str | None = None,
    module: Literal['account'] = 'account',
    action: Literal['getdeposittxs'] = 'getdeposittxs',
    address: str,
    page: int | None = None,
    offset: int | None = None,
    sort: str | None = None,
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Get L2 deposit transactions for an address.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query for this endpoint.
      module: Fixed Etherscan API module selector for this operation; use `account`.
      action: Fixed Etherscan API action selector for this operation; use `getdeposittxs`.
      address: Address to check for the documented L2 bridge transaction type.
      page: Page number for paginated results, starting at 1.
      offset: Maximum number of records to return per page.
      sort: Sort order for returned records, either ascending or descending.
      validate: Whether to validate the response.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/getdeposittxs)
    """
    params: dict = {
      'module': module,
      'action': action,
      'address': address,
    }
    if chainid is not None:
      params['chainid'] = chainid
    if page is not None:
      params['page'] = page
    if offset is not None:
      params['offset'] = offset
    if sort is not None:
      params['sort'] = sort
    return await self.authed_request(
      'GET',
      '/v2/api',
      params=params,
      validator=validate_response,
      validate=validate,
    )

  async def deposit_transactions_paged(
    self,
    *,
    chainid: str | None = None,
    module: Literal['account'] = 'account',
    action: Literal['getdeposittxs'] = 'getdeposittxs',
    address: str,
    offset: int | None = None,
    sort: str | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[EtherscanResponse]:
    """Yield successive pages of `deposit_transactions`.

    Requests `page` from 1 upwards and stops on the first page shorter than `offset`, or
    after `max_pages` pages when one is given.
    """
    page = 1
    pages = 0
    while True:
      response = await self.deposit_transactions(
        chainid=chainid,
        module=module,
        action=action,
        address=address,
        offset=offset,
        sort=sort,
        page=page,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      rows = response.get('result') if response is not None else None
      if not rows or (offset is not None and len(rows) < offset):
        break
      page += 1
