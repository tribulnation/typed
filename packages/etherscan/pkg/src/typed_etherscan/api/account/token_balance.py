# Generated by typed-dev — see CONTRIBUTING.md.
"""Get the ERC-20 token balance for an address and token contract."""

from typing_extensions import Literal, TypedDict
from typed_core.validation import validator
from typed_etherscan.core import RpcEndpoint


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: str
  """Endpoint result value returned as a string."""


validate_response = validator(EtherscanResponse)


class TokenBalance(RpcEndpoint):
  """Get ERC20-Token Account Balance for TokenContractAddress"""

  async def token_balance(
    self,
    *,
    chainid: str | None = None,
    module: Literal['account'] = 'account',
    action: Literal['tokenbalance'] = 'tokenbalance',
    contractaddress: str,
    address: str,
    tag: str | None = None,
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Get the ERC-20 token balance for an address and token contract.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `account`.
      action: Fixed Etherscan API action selector for this operation; use `tokenbalance`.
      contractaddress: Token or contract address used to scope the query.
      address: Account or contract address to query.
      tag: Block tag to use for the balance query, usually `latest`.
      validate: Whether to validate the response.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/tokenbalance)
    """
    params: dict = {
      'module': module,
      'action': action,
      'contractaddress': contractaddress,
      'address': address,
    }
    if chainid is not None:
      params['chainid'] = chainid
    if tag is not None:
      params['tag'] = tag
    return await self.authed_request(
      'GET',
      '/v2/api',
      params=params,
      validator=validate_response,
      validate=validate,
    )
