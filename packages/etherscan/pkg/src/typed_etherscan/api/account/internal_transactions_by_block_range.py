# Generated by typed-dev — see CONTRIBUTING.md.
"""Get internal transactions that occurred within a block range."""

from typing_extensions import Any, AsyncIterator, Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_etherscan.core import RpcEndpoint

InternalTransactionKeywords = TypedDict(
  'InternalTransactionKeywords', {'from': NotRequired[Any]}
)
"""
- `from`: from field returned by Etherscan.
"""


class InternalTransaction(InternalTransactionKeywords):
  """Result item returned by Etherscan."""

  blockNumber: NotRequired[Any]
  """blockNumber field returned by Etherscan."""
  timeStamp: NotRequired[Any]
  """timeStamp field returned by Etherscan."""
  hash: NotRequired[Any]
  """hash field returned by Etherscan."""
  to: NotRequired[Any]
  """to field returned by Etherscan."""
  value: NotRequired[Any]
  """value field returned by Etherscan."""
  contractAddress: NotRequired[Any]
  """contractAddress field returned by Etherscan."""
  input: NotRequired[Any]
  """input field returned by Etherscan."""
  type: NotRequired[Any]
  """type field returned by Etherscan."""
  gas: NotRequired[Any]
  """gas field returned by Etherscan."""
  gasUsed: NotRequired[Any]
  """gasUsed field returned by Etherscan."""
  traceId: NotRequired[Any]
  """traceId field returned by Etherscan."""
  isError: NotRequired[Any]
  """isError field returned by Etherscan."""
  errCode: NotRequired[Any]
  """errCode field returned by Etherscan."""


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: list[InternalTransaction]
  """List of result objects returned by Etherscan."""


validate_response = validator(EtherscanResponse)


class InternalTransactionsByBlockRange(RpcEndpoint):
  """Get Internal Transactions by Block Range"""

  async def internal_transactions_by_block_range(
    self,
    *,
    chainid: str | None = None,
    module: Literal['account'] = 'account',
    action: Literal['txlistinternal'] = 'txlistinternal',
    startblock: int | None = None,
    endblock: int | None = None,
    page: int | None = None,
    offset: int | None = None,
    sort: str | None = None,
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Get internal transactions that occurred within a block range.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `account`.
      action: Fixed Etherscan API action selector for this operation; use `txlistinternal`.
      startblock: Lowest block number to include in the result set.
      endblock: Highest block number to include in the result set.
      page: Page number for paginated results, starting at 1.
      offset: Maximum number of records to return per page.
      sort: Sort order for returned records, either ascending or descending.
      validate: Whether to validate the response.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/txlistinternal-blockrange)
    """
    params: dict = {
      'module': module,
      'action': action,
    }
    if chainid is not None:
      params['chainid'] = chainid
    if startblock is not None:
      params['startblock'] = startblock
    if endblock is not None:
      params['endblock'] = endblock
    if page is not None:
      params['page'] = page
    if offset is not None:
      params['offset'] = offset
    if sort is not None:
      params['sort'] = sort
    return await self.authed_request(
      'GET',
      '/v2/api',
      params=params,
      validator=validate_response,
      validate=validate,
    )

  async def internal_transactions_by_block_range_paged(
    self,
    *,
    chainid: str | None = None,
    module: Literal['account'] = 'account',
    action: Literal['txlistinternal'] = 'txlistinternal',
    startblock: int | None = None,
    endblock: int | None = None,
    offset: int | None = None,
    sort: str | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[EtherscanResponse]:
    """Yield successive pages of `internal_transactions_by_block_range`.

    Requests `page` from 1 upwards and stops on the first page shorter than `offset`, or
    after `max_pages` pages when one is given.
    """
    page = 1
    pages = 0
    while True:
      response = await self.internal_transactions_by_block_range(
        chainid=chainid,
        module=module,
        action=action,
        startblock=startblock,
        endblock=endblock,
        offset=offset,
        sort=sort,
        page=page,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      rows = response.get('result') if response is not None else None
      if not rows or (offset is not None and len(rows) < offset):
        break
      page += 1
