# Generated by typed-dev — see CONTRIBUTING.md.
"""Get the native coin balances for multiple addresses in one request."""

from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_etherscan.core import RpcEndpoint


class AddressBalance(TypedDict):
  """Result item returned by Etherscan."""

  account: NotRequired[Any]
  """account field returned by Etherscan."""
  balance: NotRequired[Any]
  """balance field returned by Etherscan."""


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: list[AddressBalance]
  """List of result objects returned by Etherscan."""


validate_response = validator(EtherscanResponse)


class Balances(RpcEndpoint):
  """Get Native Balance for Multiple Addresses"""

  async def balances(
    self,
    *,
    chainid: str | None = None,
    module: Literal['account'] = 'account',
    action: Literal['balancemulti'] = 'balancemulti',
    address: str,
    tag: str | None = None,
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Get the native coin balances for multiple addresses in one request.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `account`.
      action: Fixed Etherscan API action selector for this operation; use `balancemulti`.
      address: Account or contract address to query.
      tag: Block tag to use for the balance query, usually `latest`.
      validate: Whether to validate the response.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/balancemulti)
    """
    params: dict = {
      'module': module,
      'action': action,
      'address': address,
    }
    if chainid is not None:
      params['chainid'] = chainid
    if tag is not None:
      params['tag'] = tag
    return await self.authed_request(
      'GET',
      '/v2/api',
      params=params,
      validator=validate_response,
      validate=validate,
    )
