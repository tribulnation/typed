# Generated by typed-dev — see CONTRIBUTING.md.
"""Get current safe, proposed, and fast gas oracle estimates."""

from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_etherscan.core import RpcEndpoint


class GasOracle(TypedDict):
  """Result object returned by Etherscan."""

  LastBlock: NotRequired[Any]
  """LastBlock field returned by Etherscan."""
  SafeGasPrice: NotRequired[Any]
  """SafeGasPrice field returned by Etherscan."""
  ProposeGasPrice: NotRequired[Any]
  """ProposeGasPrice field returned by Etherscan."""
  FastGasPrice: NotRequired[Any]
  """FastGasPrice field returned by Etherscan."""
  suggestBaseFee: NotRequired[Any]
  """suggestBaseFee field returned by Etherscan."""
  gasUsedRatio: NotRequired[Any]
  """gasUsedRatio field returned by Etherscan."""


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: GasOracle


validate_response = validator(EtherscanResponse)


class Oracle(RpcEndpoint):
  """Get Gas Oracle"""

  async def oracle(
    self,
    *,
    chainid: str | None = None,
    module: Literal['gastracker'] = 'gastracker',
    action: Literal['gasoracle'] = 'gasoracle',
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Get current safe, proposed, and fast gas oracle estimates.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `gastracker`.
      action: Fixed Etherscan API action selector for this operation; use `gasoracle`.
      validate: Whether to validate the response.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/gasoracle)
    """
    params: dict = {
      'module': module,
      'action': action,
    }
    if chainid is not None:
      params['chainid'] = chainid
    return await self.authed_request(
      'GET',
      '/v2/api',
      params=params,
      validator=validate_response,
      validate=validate,
    )
