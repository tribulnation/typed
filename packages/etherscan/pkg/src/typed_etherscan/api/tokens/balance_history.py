# Generated by typed-dev — see CONTRIBUTING.md.
"""Get the historical ERC-20 token balance for an address at a specific block number."""

from typing_extensions import Any, Literal, TypedDict
from typed_core.validation import validator
from typed_etherscan.core import RpcEndpoint


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: Any
  """Endpoint-specific payload."""


validate_response = validator(EtherscanResponse)


class BalanceHistory(RpcEndpoint):
  """Get Historical ERC20-Token Account Balance by BlockNo"""

  async def balance_history(
    self,
    *,
    chainid: str | None = None,
    module: Literal['account'] = 'account',
    action: Literal['tokenbalancehistory'] = 'tokenbalancehistory',
    contractaddress: str,
    address: str,
    blockno: int,
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Get the historical ERC-20 token balance for an address at a specific block number.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `account`.
      action: Fixed Etherscan API action selector for this operation; use `tokenbalancehistory`.
      contractaddress: Token or contract address used to scope the query.
      address: Account or contract address to query.
      blockno: Historical block number at which to evaluate the requested value.
      validate: Whether to validate the response.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/tokenbalancehistory)
    """
    params: dict = {
      'module': module,
      'action': action,
      'contractaddress': contractaddress,
      'address': address,
      'blockno': blockno,
    }
    if chainid is not None:
      params['chainid'] = chainid
    return await self.authed_request(
      'GET',
      '/v2/api',
      params=params,
      validator=validate_response,
      validate=validate,
    )
