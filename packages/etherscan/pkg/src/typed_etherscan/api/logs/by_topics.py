# Generated by typed-dev — see CONTRIBUTING.md.
"""Get event logs matching topic filters over a block range."""

from typing_extensions import AsyncIterator, Literal, NotRequired, TypedDict
from typed_core.validation import validator
from typed_etherscan.core import RpcEndpoint


class LogEntry(TypedDict):
  """Event log object."""

  address: NotRequired[str]
  """Address that emitted the log."""
  topics: NotRequired[list[str]]
  """Indexed event topics."""
  data: NotRequired[str]
  """Unindexed event data."""
  blockNumber: NotRequired[str]
  """Block number encoded as hex."""
  transactionHash: NotRequired[str]
  """Transaction hash."""


class EtherscanLogsResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: list[LogEntry]
  """Matching event logs."""


validate_response = validator(EtherscanLogsResponse)


class ByTopics(RpcEndpoint):
  """Get Event Logs by Topics"""

  async def by_topics(
    self,
    *,
    chainid: str | None = None,
    module: Literal['logs'] = 'logs',
    action: Literal['getLogs'] = 'getLogs',
    from_block: int | None = None,
    to_block: int | None = None,
    topic0: str,
    topic0_1_opr: str | None = None,
    topic1: str | None = None,
    topic1_2_opr: str | None = None,
    topic2: str | None = None,
    topic2_3_opr: str | None = None,
    topic3: str | None = None,
    topic0_2_opr: str | None = None,
    topic0_3_opr: str | None = None,
    topic1_3_opr: str | None = None,
    page: int | None = None,
    offset: int | None = None,
    validate: bool | None = None,
  ) -> EtherscanLogsResponse:
    """Get event logs matching topic filters over a block range.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `logs`.
      action: Fixed Etherscan API action selector for this operation; use `getLogs`.
      from_block: Lowest block number to scan for matching event logs.
      to_block: Highest block number to scan for matching event logs.
      topic0: First indexed event topic used to filter logs.
      topic0_1_opr: Topic operator between `topic0` and `topic1`, either `and` or `or`.
      topic1: Second indexed event topic used to filter logs.
      topic1_2_opr: Topic operator between `topic1` and `topic2`, either `and` or `or`.
      topic2: Third indexed event topic used to filter logs.
      topic2_3_opr: Topic operator between `topic2` and `topic3`, either `and` or `or`.
      topic3: Fourth indexed event topic used to filter logs.
      topic0_2_opr: Topic operator between `topic0` and `topic2`, either `and` or `or`.
      topic0_3_opr: Topic operator between `topic0` and `topic3`, either `and` or `or`.
      topic1_3_opr: Topic operator between `topic1` and `topic3`, either `and` or `or`.
      page: Page number for paginated results, starting at 1.
      offset: Maximum number of records to return per page.
      validate: Whether to validate the response.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/getlogs-topics)
    """
    params: dict = {
      'module': module,
      'action': action,
      'topic0': topic0,
    }
    if chainid is not None:
      params['chainid'] = chainid
    if from_block is not None:
      params['fromBlock'] = from_block
    if to_block is not None:
      params['toBlock'] = to_block
    if topic0_1_opr is not None:
      params['topic0_1_opr'] = topic0_1_opr
    if topic1 is not None:
      params['topic1'] = topic1
    if topic1_2_opr is not None:
      params['topic1_2_opr'] = topic1_2_opr
    if topic2 is not None:
      params['topic2'] = topic2
    if topic2_3_opr is not None:
      params['topic2_3_opr'] = topic2_3_opr
    if topic3 is not None:
      params['topic3'] = topic3
    if topic0_2_opr is not None:
      params['topic0_2_opr'] = topic0_2_opr
    if topic0_3_opr is not None:
      params['topic0_3_opr'] = topic0_3_opr
    if topic1_3_opr is not None:
      params['topic1_3_opr'] = topic1_3_opr
    if page is not None:
      params['page'] = page
    if offset is not None:
      params['offset'] = offset
    return await self.authed_request(
      'GET',
      '/v2/api',
      params=params,
      validator=validate_response,
      validate=validate,
    )

  async def by_topics_paged(
    self,
    *,
    chainid: str | None = None,
    module: Literal['logs'] = 'logs',
    action: Literal['getLogs'] = 'getLogs',
    from_block: int | None = None,
    to_block: int | None = None,
    topic0: str,
    topic0_1_opr: str | None = None,
    topic1: str | None = None,
    topic1_2_opr: str | None = None,
    topic2: str | None = None,
    topic2_3_opr: str | None = None,
    topic3: str | None = None,
    topic0_2_opr: str | None = None,
    topic0_3_opr: str | None = None,
    topic1_3_opr: str | None = None,
    offset: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[EtherscanLogsResponse]:
    """Yield successive pages of `by_topics`.

    Requests `page` from 1 upwards and stops on the first page shorter than `offset`, or
    after `max_pages` pages when one is given.
    """
    page = 1
    pages = 0
    while True:
      response = await self.by_topics(
        chainid=chainid,
        module=module,
        action=action,
        from_block=from_block,
        to_block=to_block,
        topic0=topic0,
        topic0_1_opr=topic0_1_opr,
        topic1=topic1,
        topic1_2_opr=topic1_2_opr,
        topic2=topic2,
        topic2_3_opr=topic2_3_opr,
        topic3=topic3,
        topic0_2_opr=topic0_2_opr,
        topic0_3_opr=topic0_3_opr,
        topic1_3_opr=topic1_3_opr,
        offset=offset,
        page=page,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      rows = response.get('result') if response is not None else None
      if not rows or (offset is not None and len(rows) < offset):
        break
      page += 1
