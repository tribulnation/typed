# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_etherscan.core.rest import RpcEndpoint


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: Any
  """Endpoint-specific payload."""


class Request(TypedDict):
  chainid: NotRequired[str]
  """Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet."""
  module: Literal['stats']
  """Fixed Etherscan API module selector for this operation; use `stats`."""
  action: Literal['dailyblkcount']
  """Fixed Etherscan API action selector for this operation; use `dailyblkcount`."""
  startdate: NotRequired[str]
  """Start date for the historical statistics range."""
  enddate: NotRequired[str]
  """End date for the historical statistics range."""
  sort: NotRequired[str]
  """Sort order for returned records, either ascending or descending."""


class DailyCount(RpcEndpoint):
  """Get the daily block count and block rewards over a date range."""

  async def daily_count(
    self,
    *,
    chainid: str | None = None,
    module: Literal['stats'] = 'stats',
    action: Literal['dailyblkcount'] = 'dailyblkcount',
    startdate: str | None = None,
    enddate: str | None = None,
    sort: str | None = None,
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Get the daily block count and block rewards over a date range.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `stats`.
      action: Fixed Etherscan API action selector for this operation; use `dailyblkcount`.
      startdate: Start date for the historical statistics range.
      enddate: End date for the historical statistics range.
      sort: Sort order for returned records, either ascending or descending.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/dailyblkcount)
    """
    request: Request = Request(module=module, action=action)
    if chainid is not None:
      request['chainid'] = chainid
    if startdate is not None:
      request['startdate'] = startdate
    if enddate is not None:
      request['enddate'] = enddate
    if sort is not None:
      request['sort'] = sort
    return await self.request(
      request,
      method='GET',
      path='/v2/api',
      meta={'credentials': True},
      validate=validate,
      request_type=Request,
      response_type=EtherscanResponse,
    )
