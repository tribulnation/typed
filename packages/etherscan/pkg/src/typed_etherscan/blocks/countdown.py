# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_etherscan.core.rest import RpcEndpoint


class BlockCountdown(TypedDict):
  """Result object returned by Etherscan."""

  CurrentBlock: NotRequired[Any]
  """CurrentBlock field returned by Etherscan."""
  CountdownBlock: NotRequired[Any]
  """CountdownBlock field returned by Etherscan."""
  RemainingBlock: NotRequired[Any]
  """RemainingBlock field returned by Etherscan."""
  EstimateTimeInSec: NotRequired[Any]
  """EstimateTimeInSec field returned by Etherscan."""


class Request(TypedDict):
  chainid: NotRequired[str]
  """Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet."""
  module: Literal['block']
  """Fixed Etherscan API module selector for this operation; use `block`."""
  action: Literal['getblockcountdown']
  """Fixed Etherscan API action selector for this operation; use `getblockcountdown`."""
  blockno: int
  """Block number whose details should be returned."""


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: BlockCountdown


class Countdown(RpcEndpoint):
  """Get an estimated countdown to a target block number."""

  async def countdown(
    self,
    chainid: str | None = None,
    *,
    module: Literal['block'] = 'block',
    action: Literal['getblockcountdown'] = 'getblockcountdown',
    blockno: int,
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Get an estimated countdown to a target block number.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `block`.
      action: Fixed Etherscan API action selector for this operation; use `getblockcountdown`.
      blockno: Block number whose details should be returned.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/getblockcountdown)
    """
    request: Request = Request(module=module, action=action, blockno=blockno)
    if chainid is not None:
      request['chainid'] = chainid
    return await self.request(
      request,
      method='GET',
      path='/v2/api',
      meta={'credentials': True},
      validate=validate,
      request_type=Request,
      response_type=EtherscanResponse,
    )
