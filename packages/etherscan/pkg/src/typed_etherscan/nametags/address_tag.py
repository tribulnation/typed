# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_etherscan.core.rest import RpcEndpoint


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: Any
  """Endpoint-specific payload."""


class Request(TypedDict):
  chainid: NotRequired[str]
  """Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet."""
  module: Literal['nametag']
  """Fixed Etherscan API module selector for this operation; use `nametag`."""
  action: Literal['getaddresstag']
  """Fixed Etherscan API action selector for this operation; use `getaddresstag`."""
  address: str
  """Account or contract address to query."""


class AddressTag(RpcEndpoint):
  """Get public metadata and labels for an address."""

  async def address_tag(
    self,
    *,
    chainid: str | None = None,
    module: Literal['nametag'] = 'nametag',
    action: Literal['getaddresstag'] = 'getaddresstag',
    address: str,
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Get public metadata and labels for an address.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `nametag`.
      action: Fixed Etherscan API action selector for this operation; use `getaddresstag`.
      address: Account or contract address to query.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/getaddresstag)
    """
    request: Request = Request(module=module, action=action, address=address)
    if chainid is not None:
      request['chainid'] = chainid
    return await self.request(
      request,
      method='GET',
      path='/v2/api',
      meta={'credentials': True},
      validate=validate,
      request_type=Request,
      response_type=EtherscanResponse,
    )
