# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Any, Literal, NotRequired, TypedDict
from typed_etherscan.core.rest import RpcEndpoint


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: Any
  """Endpoint-specific payload."""


class Request(TypedDict):
  chainid: NotRequired[str]
  """Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet."""
  module: Literal['nametag']
  """Fixed Etherscan API module selector for this operation; use `nametag`."""
  action: Literal['exportaddresstags']
  """Fixed Etherscan API action selector for this operation; use `exportaddresstags`."""
  format: str
  """Export format requested for address tag metadata."""


class ExportAddressTags(RpcEndpoint):
  """Export Etherscan address tag metadata using the current export endpoint."""

  async def export_address_tags(
    self,
    *,
    chainid: str | None = None,
    module: Literal['nametag'] = 'nametag',
    action: Literal['exportaddresstags'] = 'exportaddresstags',
    format: str,
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Export Etherscan address tag metadata using the current export endpoint.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `nametag`.
      action: Fixed Etherscan API action selector for this operation; use `exportaddresstags`.
      format: Export format requested for address tag metadata.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/exportaddresstags-v2)
    """
    request: Request = Request(module=module, action=action, format=format)
    if chainid is not None:
      request['chainid'] = chainid
    return await self.request(
      request,
      method='GET',
      path='/v2/api',
      meta={'credentials': True},
      validate=validate,
      request_type=Request,
      response_type=EtherscanResponse,
    )
