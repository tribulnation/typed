# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_etherscan.core.rest import RpcEndpoint


class ApiUsageLimit(TypedDict):
  """Result object returned by Etherscan."""

  creditsUsed: NotRequired[int]
  """Number of API credits used in the current interval."""
  creditsAvailable: NotRequired[int]
  """Number of API credits remaining in the current interval."""
  creditLimit: NotRequired[int]
  """Total API credits allowed per interval."""
  limitInterval: NotRequired[str]
  """Length of the usage interval, such as `daily`."""
  intervalExpiryTimespan: NotRequired[str]
  """Time remaining until the interval resets, as `HH:MM:SS`."""


class Request(TypedDict):
  chainid: NotRequired[str]
  """Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet."""
  module: Literal['getapilimit']
  """Fixed Etherscan API module selector for this operation; use `getapilimit`."""
  action: Literal['getapilimit']
  """Fixed Etherscan API action selector for this operation; use `getapilimit`."""


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: ApiUsageLimit


class ApiLimit(RpcEndpoint):
  """Get API usage and rate-limit information for the current API key."""

  async def api_limit(
    self,
    chainid: str | None = None,
    *,
    module: Literal['getapilimit'] = 'getapilimit',
    action: Literal['getapilimit'] = 'getapilimit',
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Get API usage and rate-limit information for the current API key.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `getapilimit`.
      action: Fixed Etherscan API action selector for this operation; use `getapilimit`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/getapilimit)
    """
    request: Request = Request(module=module, action=action)
    if chainid is not None:
      request['chainid'] = chainid
    return await self.request(
      request,
      method='GET',
      path='/v2/api',
      meta={'credentials': True},
      validate=validate,
      request_type=Request,
      response_type=EtherscanResponse,
    )
