# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_etherscan.core.rest import RpcEndpoint


class JsonRpcError(TypedDict):
  """JSON-RPC error object for failed proxy calls."""

  code: int
  """JSON-RPC error code."""
  message: str
  """JSON-RPC error message."""


class Request(TypedDict):
  chainid: NotRequired[str]
  """Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet."""
  module: Literal['proxy']
  """Fixed Etherscan API module selector for this operation; use `proxy`."""
  action: Literal['eth_call']
  """Fixed Etherscan API action selector for this operation; use `eth_call`."""
  to: str
  """Destination address for the proxy call or gas estimation request."""
  data: str
  """Hex-encoded calldata to execute in the read-only proxy call."""
  tag: NotRequired[str]
  """Block tag or hex block number at which to execute the read."""


class EtherscanJsonRpcResponse(TypedDict):
  jsonrpc: Literal['2.0']
  """JSON-RPC protocol version."""
  id: int
  """JSON-RPC request identifier returned by Etherscan."""
  result: NotRequired[str]
  """Hex-encoded JSON-RPC result."""
  error: NotRequired[JsonRpcError]


class EthCall(RpcEndpoint):
  """Execute a read-only contract call without creating a transaction on chain."""

  async def eth_call(
    self,
    *,
    chainid: str | None = None,
    module: Literal['proxy'] = 'proxy',
    action: Literal['eth_call'] = 'eth_call',
    to: str,
    data: str,
    tag: str | None = None,
    validate: bool | None = None,
  ) -> EtherscanJsonRpcResponse:
    """Execute a read-only contract call without creating a transaction on chain.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `proxy`.
      action: Fixed Etherscan API action selector for this operation; use `eth_call`.
      to: Destination address for the proxy call or gas estimation request.
      data: Hex-encoded calldata to execute in the read-only proxy call.
      tag: Block tag or hex block number at which to execute the read.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/ethcall)
    """
    request: Request = Request(module=module, action=action, to=to, data=data)
    if chainid is not None:
      request['chainid'] = chainid
    if tag is not None:
      request['tag'] = tag
    return await self.request(
      request,
      method='GET',
      path='/v2/api',
      meta={'credentials': True},
      validate=validate,
      request_type=Request,
      response_type=EtherscanJsonRpcResponse,
    )
