# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_etherscan.core import DateIso
from typed_etherscan.core.rest import RpcEndpoint


class ChainSizeEntry(TypedDict):
  """Result item returned by Etherscan."""

  blockNumber: NotRequired[int]
  """Block number at the end of the measured day."""
  chainTimeStamp: NotRequired[DateIso]
  """Date the chain size was measured, in UTC (YYYY-MM-DD)."""
  chainSize: NotRequired[int]
  """Chain data size at the end of the measured day, in bytes."""
  clientType: NotRequired[str]
  """Node client software the size was measured with (the request `clienttype` parameter accepts `geth`/`parity`, but the response value is not documented as a formally closed enum, so this stays a plain string)."""
  syncMode: NotRequired[str]
  """Node sync mode the size was measured with (the request `syncmode` parameter accepts `default`/`archive`, but the response value is not documented as a formally closed enum, so this stays a plain string)."""


class Request(TypedDict):
  chainid: NotRequired[str]
  """Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet."""
  module: Literal['stats']
  """Fixed Etherscan API module selector for this operation; use `stats`."""
  action: Literal['chainsize']
  """Fixed Etherscan API action selector for this operation; use `chainsize`."""
  startdate: NotRequired[str]
  """Start date for the historical statistics range."""
  enddate: NotRequired[str]
  """End date for the historical statistics range."""
  sort: NotRequired[str]
  """Sort order for returned records, either ascending or descending."""
  clienttype: NotRequired[str]
  """Node client to query, either `geth` or `parity`."""
  syncmode: NotRequired[str]
  """Node sync mode to query, either `default` or `archive`."""


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: list[ChainSizeEntry]
  """List of result objects returned by Etherscan."""


class ChainSize(RpcEndpoint):
  """Get the daily blockchain size over a date range."""

  async def chain_size(
    self,
    *,
    chainid: str | None = None,
    module: Literal['stats'] = 'stats',
    action: Literal['chainsize'] = 'chainsize',
    startdate: str | None = None,
    enddate: str | None = None,
    sort: str | None = None,
    clienttype: str | None = None,
    syncmode: str | None = None,
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Get the daily blockchain size over a date range.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `stats`.
      action: Fixed Etherscan API action selector for this operation; use `chainsize`.
      startdate: Start date for the historical statistics range.
      enddate: End date for the historical statistics range.
      sort: Sort order for returned records, either ascending or descending.
      clienttype: Node client to query, either `geth` or `parity`.
      syncmode: Node sync mode to query, either `default` or `archive`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/chainsize)
    """
    request: Request = Request(module=module, action=action)
    if chainid is not None:
      request['chainid'] = chainid
    if startdate is not None:
      request['startdate'] = startdate
    if enddate is not None:
      request['enddate'] = enddate
    if sort is not None:
      request['sort'] = sort
    if clienttype is not None:
      request['clienttype'] = clienttype
    if syncmode is not None:
      request['syncmode'] = syncmode
    return await self.request(
      request,
      method='GET',
      path='/v2/api',
      meta={'credentials': True},
      validate=validate,
      request_type=Request,
      response_type=EtherscanResponse,
    )
