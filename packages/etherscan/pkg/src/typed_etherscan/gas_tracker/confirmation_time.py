# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_etherscan.core.rest import RpcEndpoint


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: str
  """Endpoint result value returned as a string."""


class Request(TypedDict):
  chainid: NotRequired[str]
  """Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet."""
  module: Literal['gastracker']
  """Fixed Etherscan API module selector for this operation; use `gastracker`."""
  action: Literal['gasestimate']
  """Fixed Etherscan API action selector for this operation; use `gasestimate`."""
  gasprice: str
  """Gas price paid per unit of gas, in wei, encoded as a decimal string."""


class ConfirmationTime(RpcEndpoint):
  """Estimate the confirmation time for a transaction at a supplied gas price."""

  async def confirmation_time(
    self,
    *,
    chainid: str | None = None,
    module: Literal['gastracker'] = 'gastracker',
    action: Literal['gasestimate'] = 'gasestimate',
    gasprice: str,
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Estimate the confirmation time for a transaction at a supplied gas price.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `gastracker`.
      action: Fixed Etherscan API action selector for this operation; use `gasestimate`.
      gasprice: Gas price paid per unit of gas, in wei, encoded as a decimal string.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/gasestimate)
    """
    request: Request = Request(module=module, action=action, gasprice=gasprice)
    if chainid is not None:
      request['chainid'] = chainid
    return await self.request(
      request,
      method='GET',
      path='/v2/api',
      meta={'credentials': True},
      validate=validate,
      request_type=Request,
      response_type=EtherscanResponse,
    )
