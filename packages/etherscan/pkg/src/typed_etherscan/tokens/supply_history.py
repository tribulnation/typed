# Generated by typed-dev — see CONTRIBUTING.md.
from typing_extensions import Literal, NotRequired, TypedDict
from typed_etherscan.core.rest import RpcEndpoint


class EtherscanResponse(TypedDict):
  status: Literal['0', '1']
  """Etherscan status code."""
  message: str
  """Etherscan status message."""
  result: int
  """Total supply of the token at the given block, in the token's smallest unit."""


class Request(TypedDict):
  chainid: NotRequired[str]
  """Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet."""
  module: Literal['stats']
  """Fixed Etherscan API module selector for this operation; use `stats`."""
  action: Literal['tokensupplyhistory']
  """Fixed Etherscan API action selector for this operation; use `tokensupplyhistory`."""
  contractaddress: str
  """Token contract address whose total supply should be returned."""
  blockno: int
  """Historical block number at which to evaluate the requested value."""


class SupplyHistory(RpcEndpoint):
  """Get the historical total supply for an ERC-20 token contract at a specific block number."""

  async def supply_history(
    self,
    *,
    chainid: str | None = None,
    module: Literal['stats'] = 'stats',
    action: Literal['tokensupplyhistory'] = 'tokensupplyhistory',
    contractaddress: str,
    blockno: int,
    validate: bool | None = None,
  ) -> EtherscanResponse:
    """Get the historical total supply for an ERC-20 token contract at a specific block number.

    Args:
      chainid: Chain ID of the Etherscan-supported network to query, such as `1` for Ethereum mainnet.
      module: Fixed Etherscan API module selector for this operation; use `stats`.
      action: Fixed Etherscan API action selector for this operation; use `tokensupplyhistory`.
      contractaddress: Token contract address whose total supply should be returned.
      blockno: Historical block number at which to evaluate the requested value.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.etherscan.io/api-reference/endpoint/tokensupplyhistory)
    """
    request: Request = Request(
      module=module, action=action, contractaddress=contractaddress, blockno=blockno
    )
    if chainid is not None:
      request['chainid'] = chainid
    return await self.request(
      request,
      method='GET',
      path='/v2/api',
      meta={'credentials': True},
      validate=validate,
      request_type=Request,
      response_type=EtherscanResponse,
    )
